/_build/
/deps/
/.elixir_ls/
/.mix/
*.beam
.DS_Store

# Secrets / keys — NEVER commit (signing keys, certs, tokens)
*.asc
*.gpg
*.pem
*.p12
crosswake-signing.asc

# Generated docs (mix docs)
/doc/

# Hex build artifacts (mix hex.build / verify_hex_tarball.sh)
/crosswake-*/
/*.tar
erl_crash.dump

# Sub-projects
/**/_build/
/**/deps/

# Example Phoenix host runtime SQLite state
/examples/phoenix_host/crosswake_example.db*

# iOS
/**/DerivedData/
/**/build/
**/.build/

# Android
/**/.gradle/
/**/build/
/**/local.properties

# Local tooling / harness state (not part of the package)
/.claude/
/.gsd/
/.bg-shell/

# brandbook tooling (Node processing — not committed)
/brandbook/tools/node_modules/
/brandbook/tools/fonts/

# brandbook e2e (Playwright brand-verification suite — deps/artifacts not committed)
/brandbook/e2e/node_modules/
/brandbook/e2e/playwright-report/
/brandbook/e2e/test-results/

# Node deps + Playwright artifacts (anywhere — examples, tooling)
**/node_modules/
**/playwright-report/
**/playwright-artifacts/
**/test-results/
