{
  "scope": "test/**/*.exs",
  "generator": "script/inventory_collection_assertions.exs",
  "sunset": "When VACG-01 (absence.collection_assertion_non_empty) lands as a merge-blocking guard, lift this script's detection core into that guard rather than rewriting it, then delete script/inventory_collection_assertions.exs, script/collection_assertion_ledger.json, script/collection_assertion_remediation.json, and test/crosswake/proof/phase170_vacuous_assertion_ledger_test.exs. Two mechanisms that can silently disagree about what counts as safe are worse than one.",
  "rows": [
    {"key": "sha256:f72add79944af756", "shape": "assert_any", "bucket": "safe-by-construction", "rationale": "Enum.any?/2 is false on an empty collection, so this assertion FAILS on the empty case by construction — it cannot silently pass on absence.", "display": "test/crosswake/bridge/catalog_guard_test.exs:354", "enclosing": "check_native_enum_parity/2 > a command present in Elixir but in neither native enum is a GAP violation", "expression": "violations"},
    {"key": "sha256:d863a9323ccb4211", "shape": "assert_any", "bucket": "safe-by-construction", "rationale": "Enum.any?/2 is false on an empty collection, so this assertion FAILS on the empty case by construction — it cannot silently pass on absence.", "display": "test/crosswake/bridge/catalog_guard_test.exs:368", "enclosing": "check_native_enum_parity/2 > a native enum case with no Elixir command is an ORPHAN violation", "expression": "violations"},
    {"key": "sha256:c6887db2c40b6573", "shape": "assert_any", "bucket": "safe-by-construction", "rationale": "Enum.any?/2 is false on an empty collection, so this assertion FAILS on the empty case by construction — it cannot silently pass on absence.", "display": "test/crosswake/bridge/catalog_guard_test.exs:515", "enclosing": "check_attestation/3 > a catalog entry with no shipped command is a GAP", "expression": "violations"},
    {"key": "sha256:8a2c61eb444a2375", "shape": "assert_any", "bucket": "safe-by-construction", "rationale": "Enum.any?/2 is false on an empty collection, so this assertion FAILS on the empty case by construction — it cannot silently pass on absence.", "display": "test/crosswake/bridge/catalog_guard_test.exs:526", "enclosing": "check_attestation/3 > a shipped command with no catalog entry is an ORPHAN", "expression": "violations"},
    {"key": "sha256:098fee2f72e58f72", "shape": "assert_any", "bucket": "safe-by-construction", "rationale": "Enum.any?/2 is false on an empty collection, so this assertion FAILS on the empty case by construction — it cannot silently pass on absence.", "display": "test/crosswake/bridge/catalog_guard_test.exs:537", "enclosing": "check_attestation/3 > a mapped command absent from the shipped command list is an ORPHAN", "expression": "violations"},
    {"key": "sha256:f9492e50d5ccc4af", "shape": "refute_any", "bucket": "needs-fix", "rationale": "runtime-derived collection at test/crosswake/bridge/push_test.exs:544: `Map.values(envelope)` inside test \"Task 1: epoch, exactly-once delivery, and resolve/2 (D-20..D-25) > the adopter's ref never appears in the pushed client-event payload\" — no preceding cardinality-pinning or emptiness guard was found for this expression; needs-fix pending VAC-02 remediation.", "display": "test/crosswake/bridge/push_test.exs:544", "enclosing": "Task 1: epoch, exactly-once delivery, and resolve/2 (D-20..D-25) > the adopter's ref never appears in the pushed client-event payload", "expression": "Map.values(envelope)"},
    {"key": "sha256:da349bf6b59c5666", "shape": "assert_any", "bucket": "safe-by-construction", "rationale": "Enum.any?/2 is false on an empty collection, so this assertion FAILS on the empty case by construction — it cannot silently pass on absence.", "display": "test/crosswake/compatibility/compatibility_test.exs:295", "enclosing": "route findings deny missing stale invalidated and unverified pack inventory through pack_incompatible posture", "expression": "findings"},
    {"key": "sha256:692ba1b7251fd4b2", "shape": "assert_any", "bucket": "safe-by-construction", "rationale": "Enum.any?/2 is false on an empty collection, so this assertion FAILS on the empty case by construction — it cannot silently pass on absence.", "display": "test/crosswake/compatibility/compatibility_test.exs:388", "enclosing": "route gate emits runtime_incompatible and policy_blocked corridor denials deterministically", "expression": "runtime_incompatible_decision.denials"},
    {"key": "sha256:f1c379f9ac4d3867", "shape": "assert_any", "bucket": "safe-by-construction", "rationale": "Enum.any?/2 is false on an empty collection, so this assertion FAILS on the empty case by construction — it cannot silently pass on absence.", "display": "test/crosswake/compatibility/compatibility_test.exs:395", "enclosing": "route gate emits runtime_incompatible and policy_blocked corridor denials deterministically", "expression": "policy_blocked_decision.denials"},
    {"key": "sha256:355b79f029109d48", "shape": "assert_any", "bucket": "safe-by-construction", "rationale": "Enum.any?/2 is false on an empty collection, so this assertion FAILS on the empty case by construction — it cannot silently pass on absence.", "display": "test/crosswake/contract/contract_drift_test.exs:146", "enclosing": "synthetic regression: manifest with wrong bridge_protocol_version produces :manifest_version_drift failure", "expression": "failures"},
    {"key": "sha256:8c79b02c56caad73", "shape": "assert_any", "bucket": "safe-by-construction", "rationale": "Enum.any?/2 is false on an empty collection, so this assertion FAILS on the empty case by construction — it cannot silently pass on absence.", "display": "test/crosswake/contract/contract_drift_test.exs:165", "enclosing": "synthetic regression: generated JSON with wrong bridge_protocol_version produces :generated_version_drift failure", "expression": "failures"},
    {"key": "sha256:9d4015134a9108c6", "shape": "assert_any", "bucket": "safe-by-construction", "rationale": "Enum.any?/2 is false on an empty collection, so this assertion FAILS on the empty case by construction — it cannot silently pass on absence.", "display": "test/crosswake/doctor/doctor_test.exs:1085", "enclosing": "notification findings surface in formatted Doctor output when configured", "expression": "notification_findings"},
    {"key": "sha256:e9e03832e66d7f05", "shape": "assert_any", "bucket": "safe-by-construction", "rationale": "Enum.any?/2 is false on an empty collection, so this assertion FAILS on the empty case by construction — it cannot silently pass on absence.", "display": "test/crosswake/doctor/doctor_test.exs:1090", "enclosing": "notification findings surface in formatted Doctor output when configured", "expression": "notification_findings"},
    {"key": "sha256:b09556c4ea447721", "shape": "assert_all", "bucket": "safe-guarded", "rationale": "guarded by an explicit non-emptiness check on the same collection at line 1450 (`assert legacy_findings != []`).", "display": "test/crosswake/doctor/doctor_test.exs:1451", "enclosing": "legacy capability id doctor advisory (D-58, D-59, D-63) > the finding severity is advisory (:warning) — never :error — so a legacy declaration never fails doctor (D-58, D-63)", "expression": "legacy_findings"},
    {"key": "sha256:0ec5fc97f2c6a72f", "shape": "assert_all", "bucket": "safe-guarded", "rationale": "guarded by an explicit non-emptiness check on the same collection at line 1450 (`assert legacy_findings != []`).", "display": "test/crosswake/doctor/doctor_test.exs:1452", "enclosing": "legacy capability id doctor advisory (D-58, D-59, D-63) > the finding severity is advisory (:warning) — never :error — so a legacy declaration never fails doctor (D-58, D-63)", "expression": "legacy_findings"},
    {"key": "sha256:0ebd3d492ad5d385", "shape": "refute_any", "bucket": "needs-fix", "rationale": "runtime-derived collection at test/crosswake/doctor/doctor_test.exs:1549: `report.findings` inside test \"capability rebuild finding (D-49, CTRL-05) > a capability with a non-:none rebuild class declared on no active route produces no finding\" — no preceding cardinality-pinning or emptiness guard was found for this expression; needs-fix pending VAC-02 remediation.", "display": "test/crosswake/doctor/doctor_test.exs:1549", "enclosing": "capability rebuild finding (D-49, CTRL-05) > a capability with a non-:none rebuild class declared on no active route produces no finding", "expression": "report.findings"},
    {"key": "sha256:1641a66be01e5816", "shape": "refute_any", "bucket": "needs-fix", "rationale": "runtime-derived collection at test/crosswake/doctor/doctor_test.exs:1684: `findings` inside test \"bridge hook wiring findings > the wiring grep never fails doctor on its own\" — no preceding cardinality-pinning or emptiness guard was found for this expression; needs-fix pending VAC-02 remediation.", "display": "test/crosswake/doctor/doctor_test.exs:1684", "enclosing": "bridge hook wiring findings > the wiring grep never fails doctor on its own", "expression": "findings"},
    {"key": "sha256:f58c8f3a5af81d61", "shape": "assert_any", "bucket": "safe-by-construction", "rationale": "Enum.any?/2 is false on an empty collection, so this assertion FAILS on the empty case by construction — it cannot silently pass on absence.", "display": "test/crosswake/doctor/doctor_test.exs:174", "enclosing": "doctor reports shell, bridge, route unavailable, pack posture, and proof verification requirements", "expression": "report.findings"},
    {"key": "sha256:83f0be7bcec980d6", "shape": "refute_any", "bucket": "needs-fix", "rationale": "runtime-derived collection at test/crosswake/doctor/doctor_test.exs:1751: `findings` inside test \"native controls ui findings > a stamp matching the current template_version produces no drift finding (negative control)\" — no preceding cardinality-pinning or emptiness guard was found for this expression; needs-fix pending VAC-02 remediation.", "display": "test/crosswake/doctor/doctor_test.exs:1751", "enclosing": "native controls ui findings > a stamp matching the current template_version produces no drift finding (negative control)", "expression": "findings"},
    {"key": "sha256:624a769ffb7c272a", "shape": "assert_any", "bucket": "safe-by-construction", "rationale": "Enum.any?/2 is false on an empty collection, so this assertion FAILS on the empty case by construction — it cannot silently pass on absence.", "display": "test/crosswake/doctor/doctor_test.exs:179", "enclosing": "doctor reports shell, bridge, route unavailable, pack posture, and proof verification requirements", "expression": "report.findings"},
    {"key": "sha256:570cf4e911e7c597", "shape": "assert_any", "bucket": "safe-by-construction", "rationale": "Enum.any?/2 is false on an empty collection, so this assertion FAILS on the empty case by construction — it cannot silently pass on absence.", "display": "test/crosswake/doctor/doctor_test.exs:1792", "enclosing": "native controls ui findings > a stylesheet stamp behind the current template_version also produces a drift finding", "expression": "Doctor.native_controls_ui_findings(host)"},
    {"key": "sha256:abfa2493925a171e", "shape": "assert_any", "bucket": "safe-by-construction", "rationale": "Enum.any?/2 is false on an empty collection, so this assertion FAILS on the empty case by construction — it cannot silently pass on absence.", "display": "test/crosswake/doctor/doctor_test.exs:181", "enclosing": "doctor reports shell, bridge, route unavailable, pack posture, and proof verification requirements", "expression": "report.findings"},
    {"key": "sha256:15cfee86ce4d6ac7", "shape": "refute_any", "bucket": "needs-fix", "rationale": "runtime-derived collection at test/crosswake/doctor/doctor_test.exs:1823: `findings` inside test \"native controls ui findings > the wiring finding never fails doctor on its own\" — no preceding cardinality-pinning or emptiness guard was found for this expression; needs-fix pending VAC-02 remediation.", "display": "test/crosswake/doctor/doctor_test.exs:1823", "enclosing": "native controls ui findings > the wiring finding never fails doctor on its own", "expression": "findings"},
    {"key": "sha256:7f246a1a062ab2fa", "shape": "assert_any", "bucket": "safe-by-construction", "rationale": "Enum.any?/2 is false on an empty collection, so this assertion FAILS on the empty case by construction — it cannot silently pass on absence.", "display": "test/crosswake/doctor/doctor_test.exs:186", "enclosing": "doctor reports shell, bridge, route unavailable, pack posture, and proof verification requirements", "expression": "report.findings"},
    {"key": "sha256:f8f4231f87dd97d0", "shape": "assert_any", "bucket": "safe-by-construction", "rationale": "Enum.any?/2 is false on an empty collection, so this assertion FAILS on the empty case by construction — it cannot silently pass on absence.", "display": "test/crosswake/doctor/doctor_test.exs:213", "enclosing": "a pre-shell host stays green without making native support claims", "expression": "report.findings"},
    {"key": "sha256:2e2fc823af2b3d19", "shape": "refute_any", "bucket": "needs-fix", "rationale": "runtime-derived collection at test/crosswake/doctor/doctor_test.exs:217: `report.findings` inside test \"a pre-shell host stays green without making native support claims\" — no preceding cardinality-pinning or emptiness guard was found for this expression; needs-fix pending VAC-02 remediation.", "display": "test/crosswake/doctor/doctor_test.exs:217", "enclosing": "a pre-shell host stays green without making native support claims", "expression": "report.findings"},
    {"key": "sha256:8cd90350db7bc437", "shape": "assert_any", "bucket": "safe-by-construction", "rationale": "Enum.any?/2 is false on an empty collection, so this assertion FAILS on the empty case by construction — it cannot silently pass on absence.", "display": "test/crosswake/doctor/doctor_test.exs:238", "enclosing": "doctor reports status :error when a shell proof hook exits non-zero (:failed posture)", "expression": "report.findings"},
    {"key": "sha256:5bb7f023d4fbb3a8", "shape": "assert_all", "bucket": "needs-fix", "rationale": "runtime-derived collection at test/crosswake/doctor/doctor_test.exs:265: `report.findings` inside test \"doctor findings are structured and formatter output stays stable when proof hooks pass\" — no preceding cardinality-pinning or emptiness guard was found for this expression; needs-fix pending VAC-02 remediation.", "display": "test/crosswake/doctor/doctor_test.exs:265", "enclosing": "doctor findings are structured and formatter output stays stable when proof hooks pass", "expression": "report.findings"},
    {"key": "sha256:4e86f99afe63a483", "shape": "assert_any", "bucket": "safe-by-construction", "rationale": "Enum.any?/2 is false on an empty collection, so this assertion FAILS on the empty case by construction — it cannot silently pass on absence.", "display": "test/crosswake/doctor/doctor_test.exs:308", "enclosing": "doctor findings are structured and formatter output stays stable when proof hooks pass", "expression": "decoded[\"findings\"]"},
    {"key": "sha256:a1cff1810f0532ad", "shape": "assert_any", "bucket": "safe-by-construction", "rationale": "Enum.any?/2 is false on an empty collection, so this assertion FAILS on the empty case by construction — it cannot silently pass on absence.", "display": "test/crosswake/doctor/doctor_test.exs:343", "enclosing": "doctor exposes publish readiness only when check_publish? is enabled", "expression": "report.findings"},
    {"key": "sha256:a7c1719f3ec63d5b", "shape": "assert_any", "bucket": "safe-by-construction", "rationale": "Enum.any?/2 is false on an empty collection, so this assertion FAILS on the empty case by construction — it cannot silently pass on absence.", "display": "test/crosswake/doctor/doctor_test.exs:349", "enclosing": "doctor exposes publish readiness only when check_publish? is enabled", "expression": "report.findings"},
    {"key": "sha256:74085b2bb26b13b0", "shape": "assert_any", "bucket": "safe-by-construction", "rationale": "Enum.any?/2 is false on an empty collection, so this assertion FAILS on the empty case by construction — it cannot silently pass on absence.", "display": "test/crosswake/doctor/doctor_test.exs:430", "enclosing": "doctor emits canonical commerce corridor findings and keeps taxonomy parity with support matrix", "expression": "corridor_findings"},
    {"key": "sha256:4bca134ab7a93390", "shape": "assert_any", "bucket": "safe-by-construction", "rationale": "Enum.any?/2 is false on an empty collection, so this assertion FAILS on the empty case by construction — it cannot silently pass on absence.", "display": "test/crosswake/doctor/doctor_test.exs:445", "enclosing": "doctor emits canonical commerce corridor findings and keeps taxonomy parity with support matrix", "expression": "decoded[\"findings\"]"},
    {"key": "sha256:fd8dcab14ffd9890", "shape": "assert_any", "bucket": "safe-by-construction", "rationale": "Enum.any?/2 is false on an empty collection, so this assertion FAILS on the empty case by construction — it cannot silently pass on absence.", "display": "test/crosswake/doctor/doctor_test.exs:465", "enclosing": "doctor maps undeclared corridor compile failures onto canonical commerce denial IDs", "expression": "report.findings"},
    {"key": "sha256:e176507cc53504b3", "shape": "assert_any", "bucket": "safe-by-construction", "rationale": "Enum.any?/2 is false on an empty collection, so this assertion FAILS on the empty case by construction — it cannot silently pass on absence.", "display": "test/crosswake/doctor/doctor_test.exs:528", "enclosing": "doctor exposes a typed commerce_summary surface with the canonical keys", "expression": "summary.corridors"},
    {"key": "sha256:3cb51aa8d2403d35", "shape": "refute_any", "bucket": "needs-fix", "rationale": "runtime-derived collection at test/crosswake/doctor/doctor_test.exs:553: `report.findings` inside test \"commerce_summary returns the not_applicable freshness baseline when no commerce routes exist\" — no preceding cardinality-pinning or emptiness guard was found for this expression; needs-fix pending VAC-02 remediation.", "display": "test/crosswake/doctor/doctor_test.exs:553", "enclosing": "commerce_summary returns the not_applicable freshness baseline when no commerce routes exist", "expression": "report.findings"},
    {"key": "sha256:efd0ce6d2d908c1b", "shape": "refute_any", "bucket": "needs-fix", "rationale": "runtime-derived collection at test/crosswake/doctor/doctor_test.exs:554: `report.findings` inside test \"commerce_summary returns the not_applicable freshness baseline when no commerce routes exist\" — no preceding cardinality-pinning or emptiness guard was found for this expression; needs-fix pending VAC-02 remediation.", "display": "test/crosswake/doctor/doctor_test.exs:554", "enclosing": "commerce_summary returns the not_applicable freshness baseline when no commerce routes exist", "expression": "report.findings"},
    {"key": "sha256:c99b5ade9c4a0de4", "shape": "refute_any", "bucket": "needs-fix", "rationale": "runtime-derived collection at test/crosswake/doctor/doctor_test.exs:575: `report.findings` inside test \"commerce_summary does not emit commerce.corridor.role_unknown when every route role maps to canonical SupportMatrix taxonomy\" — no preceding cardinality-pinning or emptiness guard was found for this expression; needs-fix pending VAC-02 remediation.", "display": "test/crosswake/doctor/doctor_test.exs:575", "enclosing": "commerce_summary does not emit commerce.corridor.role_unknown when every route role maps to canonical SupportMatrix taxonomy", "expression": "report.findings"},
    {"key": "sha256:e29280e0a922fa89", "shape": "refute_any", "bucket": "needs-fix", "rationale": "runtime-derived collection at test/crosswake/doctor/doctor_test.exs:579: `report.commerce_summary.corridors` inside test \"commerce_summary does not emit commerce.corridor.role_unknown when every route role maps to canonical SupportMatrix taxonomy\" — no preceding cardinality-pinning or emptiness guard was found for this expression; needs-fix pending VAC-02 remediation.", "display": "test/crosswake/doctor/doctor_test.exs:579", "enclosing": "commerce_summary does not emit commerce.corridor.role_unknown when every route role maps to canonical SupportMatrix taxonomy", "expression": "report.commerce_summary.corridors"},
    {"key": "sha256:d7bed9b42ac73ff4", "shape": "assert_any", "bucket": "safe-by-construction", "rationale": "Enum.any?/2 is false on an empty collection, so this assertion FAILS on the empty case by construction — it cannot silently pass on absence.", "display": "test/crosswake/doctor/doctor_test.exs:619", "enclosing": "unknown entitlement freshness defaults to fail-closed merge-blocking truth when commerce routes exist", "expression": "report.findings"},
    {"key": "sha256:88e1d7126f996e86", "shape": "assert_any", "bucket": "safe-by-construction", "rationale": "Enum.any?/2 is false on an empty collection, so this assertion FAILS on the empty case by construction — it cannot silently pass on absence.", "display": "test/crosswake/doctor/doctor_test.exs:644", "enclosing": "entitlement_snapshot_freshness: :not_applicable is coerced to :unknown when commerce routes are present", "expression": "report.findings"},
    {"key": "sha256:8f07078b2fa6137c", "shape": "refute_any", "bucket": "needs-fix", "rationale": "runtime-derived collection at test/crosswake/doctor/doctor_test.exs:665: `report.findings` inside test \"fresh entitlement freshness suppresses the stale snapshot finding\" — no preceding cardinality-pinning or emptiness guard was found for this expression; needs-fix pending VAC-02 remediation.", "display": "test/crosswake/doctor/doctor_test.exs:665", "enclosing": "fresh entitlement freshness suppresses the stale snapshot finding", "expression": "report.findings"},
    {"key": "sha256:10e627e69b1802ed", "shape": "assert_any", "bucket": "safe-by-construction", "rationale": "Enum.any?/2 is false on an empty collection, so this assertion FAILS on the empty case by construction — it cannot silently pass on absence.", "display": "test/crosswake/doctor/doctor_test.exs:697", "enclosing": "native rebuild requirements are derived from canonical support matrix metadata", "expression": "report.commerce_summary.rebuild_requirements"},
    {"key": "sha256:a657df808836b1bb", "shape": "refute_any", "bucket": "needs-fix", "rationale": "runtime-derived collection at test/crosswake/doctor/doctor_test.exs:713: `report.findings` inside test \"native_rebuild_satisfied? suppresses the native rebuild finding without removing rebuild_requirements truth\" — no preceding cardinality-pinning or emptiness guard was found for this expression; needs-fix pending VAC-02 remediation.", "display": "test/crosswake/doctor/doctor_test.exs:713", "enclosing": "native_rebuild_satisfied? suppresses the native rebuild finding without removing rebuild_requirements truth", "expression": "report.findings"},
    {"key": "sha256:0bda013275b1a741", "shape": "assert_any", "bucket": "safe-by-construction", "rationale": "Enum.any?/2 is false on an empty collection, so this assertion FAILS on the empty case by construction — it cannot silently pass on absence.", "display": "test/crosswake/doctor/doctor_test.exs:715", "enclosing": "native_rebuild_satisfied? suppresses the native rebuild finding without removing rebuild_requirements truth", "expression": "report.commerce_summary.rebuild_requirements"},
    {"key": "sha256:874c776302504b2a", "shape": "assert_all", "bucket": "needs-fix", "rationale": "runtime-derived collection at test/crosswake/doctor/doctor_test.exs:731: `report.findings\n             |> Enum.filter(&String.starts_with?(&1.code, \"commerce.corridor.\"))` inside test \"phase 19 commerce corridor findings now carry proof_class labels\" — no preceding cardinality-pinning or emptiness guard was found for this expression; needs-fix pending VAC-02 remediation.", "display": "test/crosswake/doctor/doctor_test.exs:731", "enclosing": "phase 19 commerce corridor findings now carry proof_class labels", "expression": "report.findings\n             |> Enum.filter(&String.starts_with?(&1.code, \"commerce.corridor.\"))"},
    {"key": "sha256:5b19b82d225ac01d", "shape": "assert_any", "bucket": "safe-by-construction", "rationale": "Enum.any?/2 is false on an empty collection, so this assertion FAILS on the empty case by construction — it cannot silently pass on absence.", "display": "test/crosswake/doctor/doctor_test.exs:939", "enclosing": "gating findings surface in formatted Doctor output (human and JSON)", "expression": "gating_findings"},
    {"key": "sha256:aa9e208985a41211", "shape": "assert_any", "bucket": "safe-by-construction", "rationale": "Enum.any?/2 is false on an empty collection, so this assertion FAILS on the empty case by construction — it cannot silently pass on absence.", "display": "test/crosswake/doctor/doctor_test.exs:944", "enclosing": "gating findings surface in formatted Doctor output (human and JSON)", "expression": "gating_findings"},
    {"key": "sha256:ea77f2084a5f9cc5", "shape": "assert_any", "bucket": "safe-by-construction", "rationale": "Enum.any?/2 is false on an empty collection, so this assertion FAILS on the empty case by construction — it cannot silently pass on absence.", "display": "test/crosswake/doctor/doctor_test.exs:971", "enclosing": "auth findings surface in formatted Doctor output (human and JSON) without sensitive artifacts", "expression": "auth_findings"},
    {"key": "sha256:05b69b7c7dc09ab2", "shape": "assert_any", "bucket": "safe-by-construction", "rationale": "Enum.any?/2 is false on an empty collection, so this assertion FAILS on the empty case by construction — it cannot silently pass on absence.", "display": "test/crosswake/doctor/doctor_test.exs:980", "enclosing": "auth findings surface in formatted Doctor output (human and JSON) without sensitive artifacts", "expression": "auth_findings"},
    {"key": "sha256:a0f7c35dcfa0b55f", "shape": "refute_any", "bucket": "needs-fix", "rationale": "runtime-derived collection at test/crosswake/doctor/doctor_threadline_test.exs:260: `report.findings` inside test \"phase_95_threadline_findings (OPER-03) > runs threadline schema checks with bare-atom audit_ledger config and canonical schema passes\" — no preceding cardinality-pinning or emptiness guard was found for this expression; needs-fix pending VAC-02 remediation.", "display": "test/crosswake/doctor/doctor_threadline_test.exs:260", "enclosing": "phase_95_threadline_findings (OPER-03) > runs threadline schema checks with bare-atom audit_ledger config and canonical schema passes", "expression": "report.findings"},
    {"key": "sha256:beac98703588644e", "shape": "refute_any", "bucket": "needs-fix", "rationale": "runtime-derived collection at test/crosswake/doctor/doctor_threadline_test.exs:263: `report.findings` inside test \"phase_95_threadline_findings (OPER-03) > runs threadline schema checks with bare-atom audit_ledger config and canonical schema passes\" — no preceding cardinality-pinning or emptiness guard was found for this expression; needs-fix pending VAC-02 remediation.", "display": "test/crosswake/doctor/doctor_threadline_test.exs:263", "enclosing": "phase_95_threadline_findings (OPER-03) > runs threadline schema checks with bare-atom audit_ledger config and canonical schema passes", "expression": "report.findings"},
    {"key": "sha256:70b991a662e1c8f3", "shape": "refute_any", "bucket": "needs-fix", "rationale": "runtime-derived collection at test/crosswake/doctor/doctor_threadline_test.exs:290: `report.findings` inside test \"phase_95_threadline_findings (OPER-03) > emits threadline.pii_forbidden_field_present even when install manifest is missing\" — no preceding cardinality-pinning or emptiness guard was found for this expression; needs-fix pending VAC-02 remediation.", "display": "test/crosswake/doctor/doctor_threadline_test.exs:290", "enclosing": "phase_95_threadline_findings (OPER-03) > emits threadline.pii_forbidden_field_present even when install manifest is missing", "expression": "report.findings"},
    {"key": "sha256:8f6b39a1046e8cb9", "shape": "refute_any", "bucket": "needs-fix", "rationale": "runtime-derived collection at test/crosswake/doctor/doctor_threadline_test.exs:320: `report.findings` inside test \"phase_95_threadline_findings (OPER-03) > emits threadline.ledger_schema_invalid :advisory when configured module is not an Ecto schema\" — no preceding cardinality-pinning or emptiness guard was found for this expression; needs-fix pending VAC-02 remediation.", "display": "test/crosswake/doctor/doctor_threadline_test.exs:320", "enclosing": "phase_95_threadline_findings (OPER-03) > emits threadline.ledger_schema_invalid :advisory when configured module is not an Ecto schema", "expression": "report.findings"},
    {"key": "sha256:ea80a035d20493fb", "shape": "refute_any", "bucket": "needs-fix", "rationale": "runtime-derived collection at test/crosswake/doctor/doctor_threadline_test.exs:323: `report.findings` inside test \"phase_95_threadline_findings (OPER-03) > emits threadline.ledger_schema_invalid :advisory when configured module is not an Ecto schema\" — no preceding cardinality-pinning or emptiness guard was found for this expression; needs-fix pending VAC-02 remediation.", "display": "test/crosswake/doctor/doctor_threadline_test.exs:323", "enclosing": "phase_95_threadline_findings (OPER-03) > emits threadline.ledger_schema_invalid :advisory when configured module is not an Ecto schema", "expression": "report.findings"},
    {"key": "sha256:fef522abe3144020", "shape": "refute_any", "bucket": "needs-fix", "rationale": "runtime-derived collection at test/crosswake/doctor/doctor_threadline_test.exs:99: `report.findings` inside test \"phase_95_threadline_findings (OPER-03) > does NOT emit threadline.plug_missing when plug is present in router\" — no preceding cardinality-pinning or emptiness guard was found for this expression; needs-fix pending VAC-02 remediation.", "display": "test/crosswake/doctor/doctor_threadline_test.exs:99", "enclosing": "phase_95_threadline_findings (OPER-03) > does NOT emit threadline.plug_missing when plug is present in router", "expression": "report.findings"},
    {"key": "sha256:2245db02fe323f5f", "shape": "assert_any", "bucket": "safe-by-construction", "rationale": "Enum.any?/2 is false on an empty collection, so this assertion FAILS on the empty case by construction — it cannot silently pass on absence.", "display": "test/crosswake/guides/capability_claims_test.exs:117", "enclosing": "D-14/D-18/D-23 screenshot wording is allowed only as collateral after route-tour assertions", "expression": "rejected"},
    {"key": "sha256:83d03e90d1317fd6", "shape": "assert_any", "bucket": "safe-by-construction", "rationale": "Enum.any?/2 is false on an empty collection, so this assertion FAILS on the empty case by construction — it cannot silently pass on absence.", "display": "test/crosswake/guides/capability_claims_test.exs:97", "enclosing": "D-24/D-29 synthetic regressions catch native, offline, commerce, plugin, and screenshot overclaims", "expression": "failures"},
    {"key": "sha256:1751b5efaa09b438", "shape": "assert_any", "bucket": "safe-by-construction", "rationale": "Enum.any?/2 is false on an empty collection, so this assertion FAILS on the empty case by construction — it cannot silently pass on absence.", "display": "test/crosswake/guides/capture_collateral_test.exs:285", "enclosing": "scanner rejects a missing emulator-evidence label", "expression": "failures"},
    {"key": "sha256:206469b3564d0da9", "shape": "assert_any", "bucket": "safe-by-construction", "rationale": "Enum.any?/2 is false on an empty collection, so this assertion FAILS on the empty case by construction — it cannot silently pass on absence.", "display": "test/crosswake/guides/collateral_table_test.exs:203", "enclosing": "scanner rejects a missing raw reference URL", "expression": "failures"},
    {"key": "sha256:998b05675c447d3b", "shape": "assert_all", "bucket": "needs-fix", "rationale": "runtime-derived collection at test/crosswake/guides/evidence_manifest_test.exs:110: `manifest_values(manifest, \"proof_class\")` inside test \"allowed labels are literal capability-map and proof-posture vocabulary\" — no preceding cardinality-pinning or emptiness guard was found for this expression; needs-fix pending VAC-02 remediation.", "display": "test/crosswake/guides/evidence_manifest_test.exs:110", "enclosing": "allowed labels are literal capability-map and proof-posture vocabulary", "expression": "manifest_values(manifest, \"proof_class\")"},
    {"key": "sha256:a2e7909780a0455c", "shape": "assert_all", "bucket": "needs-fix", "rationale": "runtime-derived collection at test/crosswake/guides/evidence_manifest_test.exs:111: `manifest_values(manifest, \"support_label\")` inside test \"allowed labels are literal capability-map and proof-posture vocabulary\" — no preceding cardinality-pinning or emptiness guard was found for this expression; needs-fix pending VAC-02 remediation.", "display": "test/crosswake/guides/evidence_manifest_test.exs:111", "enclosing": "allowed labels are literal capability-map and proof-posture vocabulary", "expression": "manifest_values(manifest, \"support_label\")"},
    {"key": "sha256:27dddbbed36d08e1", "shape": "assert_all", "bucket": "needs-fix", "rationale": "runtime-derived collection at test/crosswake/guides/evidence_manifest_test.exs:113: `manifest_values(manifest, \"capability_posture\")` inside test \"allowed labels are literal capability-map and proof-posture vocabulary\" — no preceding cardinality-pinning or emptiness guard was found for this expression; needs-fix pending VAC-02 remediation.", "display": "test/crosswake/guides/evidence_manifest_test.exs:113", "enclosing": "allowed labels are literal capability-map and proof-posture vocabulary", "expression": "manifest_values(manifest, \"capability_posture\")"},
    {"key": "sha256:38aa78e229a72e41", "shape": "assert_all", "bucket": "needs-fix", "rationale": "runtime-derived collection at test/crosswake/guides/evidence_manifest_test.exs:118: `manifest_values(manifest, \"package_owner\")` inside test \"allowed labels are literal capability-map and proof-posture vocabulary\" — no preceding cardinality-pinning or emptiness guard was found for this expression; needs-fix pending VAC-02 remediation.", "display": "test/crosswake/guides/evidence_manifest_test.exs:118", "enclosing": "allowed labels are literal capability-map and proof-posture vocabulary", "expression": "manifest_values(manifest, \"package_owner\")"},
    {"key": "sha256:dcd5778036e576e6", "shape": "assert_any", "bucket": "safe-by-construction", "rationale": "Enum.any?/2 is false on an empty collection, so this assertion FAILS on the empty case by construction — it cannot silently pass on absence.", "display": "test/crosswake/guides/evidence_manifest_test.exs:235", "enclosing": "D-15/D-16 capability-pressure rows distinguish limitations, retention, posture, and owner", "expression": "route[\"known_limitations\"]"},
    {"key": "sha256:7b185c219bfa5666", "shape": "assert_any", "bucket": "safe-by-construction", "rationale": "Enum.any?/2 is false on an empty collection, so this assertion FAILS on the empty case by construction — it cannot silently pass on absence.", "display": "test/crosswake/guides/native_evidence_drift_test.exs:106", "enclosing": "synthetic regressions fail on stale Android UAT version wording and overclaims", "expression": "stale_version_failures"},
    {"key": "sha256:87c42807ac7ce212", "shape": "assert_any", "bucket": "safe-by-construction", "rationale": "Enum.any?/2 is false on an empty collection, so this assertion FAILS on the empty case by construction — it cannot silently pass on absence.", "display": "test/crosswake/guides/native_evidence_drift_test.exs:117", "enclosing": "synthetic regressions fail on stale Android UAT version wording and overclaims", "expression": "overclaim_failures"},
    {"key": "sha256:383de7fd71894af0", "shape": "assert_any", "bucket": "safe-by-construction", "rationale": "Enum.any?/2 is false on an empty collection, so this assertion FAILS on the empty case by construction — it cannot silently pass on absence.", "display": "test/crosswake/guides/native_evidence_drift_test.exs:156", "enclosing": "synthetic regressions fail when native collateral overclaims simulator or emulator support", "expression": "simulator_overclaim"},
    {"key": "sha256:f983d49dad98a4c3", "shape": "assert_any", "bucket": "safe-by-construction", "rationale": "Enum.any?/2 is false on an empty collection, so this assertion FAILS on the empty case by construction — it cannot silently pass on absence.", "display": "test/crosswake/guides/native_evidence_drift_test.exs:164", "enclosing": "synthetic regressions fail when native collateral overclaims simulator or emulator support", "expression": "merge_blocking_overclaim"},
    {"key": "sha256:be723c82a756ae2c", "shape": "assert_any", "bucket": "safe-by-construction", "rationale": "Enum.any?/2 is false on an empty collection, so this assertion FAILS on the empty case by construction — it cannot silently pass on absence.", "display": "test/crosswake/guides/native_evidence_drift_test.exs:174", "enclosing": "synthetic regressions fail on screenshot-only proof and native authority claims", "expression": "screenshot_failures"},
    {"key": "sha256:4de9e64f1facc04f", "shape": "assert_any", "bucket": "safe-by-construction", "rationale": "Enum.any?/2 is false on an empty collection, so this assertion FAILS on the empty case by construction — it cannot silently pass on absence.", "display": "test/crosswake/guides/native_evidence_drift_test.exs:182", "enclosing": "synthetic regressions fail on screenshot-only proof and native authority claims", "expression": "authority_failures"},
    {"key": "sha256:ae9093b239ab7610", "shape": "assert_any", "bucket": "safe-by-construction", "rationale": "Enum.any?/2 is false on an empty collection, so this assertion FAILS on the empty case by construction — it cannot silently pass on absence.", "display": "test/crosswake/guides/native_evidence_drift_test.exs:197", "enclosing": "synthetic regressions fail on missing native collateral coordinate mode and unavailable reason", "expression": "missing_coordinate_mode"},
    {"key": "sha256:e79737ea18ab7851", "shape": "assert_any", "bucket": "safe-by-construction", "rationale": "Enum.any?/2 is false on an empty collection, so this assertion FAILS on the empty case by construction — it cannot silently pass on absence.", "display": "test/crosswake/guides/native_evidence_drift_test.exs:205", "enclosing": "synthetic regressions fail on missing native collateral coordinate mode and unavailable reason", "expression": "missing_unavailable_reason"},
    {"key": "sha256:bf1fda820aff49a3", "shape": "assert_all", "bucket": "safe-compile-time-literal", "rationale": "@scanned_paths is a module attribute holding a fixed literal collection; it cannot be empty, so the empty case is unreachable.", "display": "test/crosswake/guides/native_evidence_drift_test.exs:29", "enclosing": "source-derived facts used by the drift scanner are readable", "expression": "@scanned_paths"},
    {"key": "sha256:83d756555d2fed80", "shape": "assert_any", "bucket": "safe-by-construction", "rationale": "Enum.any?/2 is false on an empty collection, so this assertion FAILS on the empty case by construction — it cannot silently pass on absence.", "display": "test/crosswake/guides/native_evidence_drift_test.exs:48", "enclosing": "synthetic regressions fail on stale checked-in iOS local refs", "expression": "failures"},
    {"key": "sha256:d70a72c2b847a2ed", "shape": "assert_any", "bucket": "safe-by-construction", "rationale": "Enum.any?/2 is false on an empty collection, so this assertion FAILS on the empty case by construction — it cannot silently pass on absence.", "display": "test/crosswake/guides/native_evidence_drift_test.exs:64", "enclosing": "synthetic regressions fail on stale Android coordinates and dynamic versions", "expression": "stale_gav_failures"},
    {"key": "sha256:6983b59d502ed63f", "shape": "assert_any", "bucket": "safe-by-construction", "rationale": "Enum.any?/2 is false on an empty collection, so this assertion FAILS on the empty case by construction — it cannot silently pass on absence.", "display": "test/crosswake/guides/native_evidence_drift_test.exs:76", "enclosing": "synthetic regressions fail on stale Android coordinates and dynamic versions", "expression": "dynamic_version_failures"},
    {"key": "sha256:52700a6f33e8276b", "shape": "assert_any", "bucket": "safe-by-construction", "rationale": "Enum.any?/2 is false on an empty collection, so this assertion FAILS on the empty case by construction — it cannot silently pass on absence.", "display": "test/crosswake/guides/native_evidence_drift_test.exs:90", "enclosing": "synthetic regressions fail when native labels disappear from public proof surfaces", "expression": "failures"},
    {"key": "sha256:0edf35365e88987d", "shape": "assert_any", "bucket": "safe-by-construction", "rationale": "Enum.any?/2 is false on an empty collection, so this assertion FAILS on the empty case by construction — it cannot silently pass on absence.", "display": "test/crosswake/guides/port_registry_test.exs:167", "enclosing": "PORT-REGISTRY scanner rejects missing 10.0.2.2 Android note", "expression": "failures"},
    {"key": "sha256:aa58b00f7d27ff48", "shape": "assert_all", "bucket": "safe-compile-time-literal", "rationale": "@quick_start_paths is a module attribute holding a fixed literal collection; it cannot be empty, so the empty case is unreachable.", "display": "test/crosswake/guides/quick_start_adoption_drift_test.exs:39", "enclosing": "source-derived facts used by the drift scanner are readable", "expression": "@quick_start_paths"},
    {"key": "sha256:dbcd395fc4fc686b", "shape": "assert_any", "bucket": "safe-by-construction", "rationale": "Enum.any?/2 is false on an empty collection, so this assertion FAILS on the empty case by construction — it cannot silently pass on absence.", "display": "test/crosswake/guides/quick_start_adoption_drift_test.exs:594", "enclosing": "adoption scanner rejects missing app-owned offline proof teaching", "expression": "failures"},
    {"key": "sha256:c30734e448ef965f", "shape": "assert_any", "bucket": "safe-by-construction", "rationale": "Enum.any?/2 is false on an empty collection, so this assertion FAILS on the empty case by construction — it cannot silently pass on absence.", "display": "test/crosswake/guides/readme_see_it_run_test.exs:317", "enclosing": "order scanner rejects the section appearing before What-this-is-not", "expression": "failures"},
    {"key": "sha256:fefaaaac6f6d9411", "shape": "assert_all", "bucket": "needs-fix", "rationale": "runtime-derived collection at test/crosswake/guides/release_boundaries_test.exs:101: `companions` inside test \"read-only release status projects the exact candidate boundary\" — no preceding cardinality-pinning or emptiness guard was found for this expression; needs-fix pending VAC-02 remediation.", "display": "test/crosswake/guides/release_boundaries_test.exs:101", "enclosing": "read-only release status projects the exact candidate boundary", "expression": "companions"},
    {"key": "sha256:f2bc374d9532941c", "shape": "assert_any", "bucket": "safe-by-construction", "rationale": "Enum.any?/2 is false on an empty collection, so this assertion FAILS on the empty case by construction — it cannot silently pass on absence.", "display": "test/crosswake/guides/release_boundaries_test.exs:142", "enclosing": "live candidate status preserves partial linked-coordinate truth", "expression": "status.checks"},
    {"key": "sha256:b0b84c213057a556", "shape": "assert_any", "bucket": "safe-by-construction", "rationale": "Enum.any?/2 is false on an empty collection, so this assertion FAILS on the empty case by construction — it cannot silently pass on absence.", "display": "test/crosswake/guides/release_boundaries_test.exs:655", "enclosing": "release-truth scanner flags synthetic stale manifest claims", "expression": "failures"},
    {"key": "sha256:a6c0ce3d736963f6", "shape": "assert_any", "bucket": "safe-by-construction", "rationale": "Enum.any?/2 is false on an empty collection, so this assertion FAILS on the empty case by construction — it cannot silently pass on absence.", "display": "test/crosswake/guides/see_it_run_banner_test.exs:239", "enclosing": "scanner rejects missing native posture label", "expression": "failures"},
    {"key": "sha256:eddd9d2f657a813e", "shape": "assert_any", "bucket": "safe-by-construction", "rationale": "Enum.any?/2 is false on an empty collection, so this assertion FAILS on the empty case by construction — it cannot silently pass on absence.", "display": "test/crosswake/guides/see_it_run_test.exs:488", "enclosing": "guide is registered as item 2 in ExDoc extras and Start group", "expression": "failures"},
    {"key": "sha256:e5c8e4006a63123d", "shape": "assert_any", "bucket": "safe-by-construction", "rationale": "Enum.any?/2 is false on an empty collection, so this assertion FAILS on the empty case by construction — it cannot silently pass on absence.", "display": "test/crosswake/guides/troubleshooting_test.exs:362", "enclosing": "synthetic regressions fail on native advisory evidence overclaims", "expression": "failures"},
    {"key": "sha256:baf32ed5f44aa4ae", "shape": "assert_all", "bucket": "safe-cardinality-pinned", "rationale": "pinned to an exact set at line 239 (`assert map_size(registry) == 15`), which fixes registry's cardinality — the empty case is unreachable while that pin holds.", "display": "test/crosswake/manifest/builder_test.exs:241", "enclosing": "Capability rebuild + interaction — structurally unconstructable without both (D-51, D-52, D-54) > all 15 public catalog entries build successfully and each declares an explicit interaction value", "expression": "Map.values(registry)"},
    {"key": "sha256:39546e11e8403d35", "shape": "assert_all", "bucket": "needs-fix", "rationale": "runtime-derived collection at test/crosswake/manifest/manifest_test.exs:71: `baseline_manifest.routes` inside test \"manifest keeps schema 1.1.0 while commerce corridor fields remain additive\" — no preceding cardinality-pinning or emptiness guard was found for this expression; needs-fix pending VAC-02 remediation.", "display": "test/crosswake/manifest/manifest_test.exs:71", "enclosing": "manifest keeps schema 1.1.0 while commerce corridor fields remain additive", "expression": "baseline_manifest.routes"},
    {"key": "sha256:b29026fa4ac2a659", "shape": "assert_any", "bucket": "safe-by-construction", "rationale": "Enum.any?/2 is false on an empty collection, so this assertion FAILS on the empty case by construction — it cannot silently pass on absence.", "display": "test/crosswake/manifest/validator_test.exs:100", "enclosing": "malformed topology entries fail closed without raising or echoing supplied values", "expression": "errors"},
    {"key": "sha256:8aa6ba56182f6c58", "shape": "refute_any", "bucket": "needs-fix", "rationale": "runtime-derived collection at test/crosswake/manifest/validator_test.exs:101: `errors` inside test \"malformed topology entries fail closed without raising or echoing supplied values\" — no preceding cardinality-pinning or emptiness guard was found for this expression; needs-fix pending VAC-02 remediation.", "display": "test/crosswake/manifest/validator_test.exs:101", "enclosing": "malformed topology entries fail closed without raising or echoing supplied values", "expression": "errors"},
    {"key": "sha256:51213b6e4521d973", "shape": "assert_any", "bucket": "safe-by-construction", "rationale": "Enum.any?/2 is false on an empty collection, so this assertion FAILS on the empty case by construction — it cannot silently pass on absence.", "display": "test/crosswake/manifest/validator_test.exs:137", "enclosing": "topology validation rejects root-parent, cross-tab, cyclic, and unsupported runtime relationships", "expression": "rule_ids"},
    {"key": "sha256:5736fd965e21d65b", "shape": "assert_any", "bucket": "safe-by-construction", "rationale": "Enum.any?/2 is false on an empty collection, so this assertion FAILS on the empty case by construction — it cannot silently pass on absence.", "display": "test/crosswake/manifest/validator_test.exs:138", "enclosing": "topology validation rejects root-parent, cross-tab, cyclic, and unsupported runtime relationships", "expression": "rule_ids"},
    {"key": "sha256:6cce33779b46e38a", "shape": "assert_any", "bucket": "safe-by-construction", "rationale": "Enum.any?/2 is false on an empty collection, so this assertion FAILS on the empty case by construction — it cannot silently pass on absence.", "display": "test/crosswake/manifest/validator_test.exs:139", "enclosing": "topology validation rejects root-parent, cross-tab, cyclic, and unsupported runtime relationships", "expression": "rule_ids"},
    {"key": "sha256:cb9b370d81e38f6c", "shape": "assert_any", "bucket": "safe-by-construction", "rationale": "Enum.any?/2 is false on an empty collection, so this assertion FAILS on the empty case by construction — it cannot silently pass on absence.", "display": "test/crosswake/manifest/validator_test.exs:140", "enclosing": "topology validation rejects root-parent, cross-tab, cyclic, and unsupported runtime relationships", "expression": "rule_ids"},
    {"key": "sha256:2449a060fe776572", "shape": "assert_all", "bucket": "needs-fix", "rationale": "runtime-derived collection at test/crosswake/manifest/validator_test.exs:199: `errors` inside test \"manifest compile returns structured validation failures instead of opaque strings\" — no preceding cardinality-pinning or emptiness guard was found for this expression; needs-fix pending VAC-02 remediation.", "display": "test/crosswake/manifest/validator_test.exs:199", "enclosing": "manifest compile returns structured validation failures instead of opaque strings", "expression": "errors"},
    {"key": "sha256:5fd60e712ac38bcc", "shape": "assert_any", "bucket": "safe-by-construction", "rationale": "Enum.any?/2 is false on an empty collection, so this assertion FAILS on the empty case by construction — it cannot silently pass on absence.", "display": "test/crosswake/manifest/validator_test.exs:209", "enclosing": "manifest validation rejects route pack references that are missing from the root pack registry", "expression": "errors"},
    {"key": "sha256:d9f254135f44e918", "shape": "assert_any", "bucket": "safe-by-construction", "rationale": "Enum.any?/2 is false on an empty collection, so this assertion FAILS on the empty case by construction — it cannot silently pass on absence.", "display": "test/crosswake/manifest/validator_test.exs:226", "enclosing": "manifest validation rejects route pack references that drift from the canonical registry version", "expression": "errors"},
    {"key": "sha256:5ba46418db7a9394", "shape": "assert_any", "bucket": "safe-by-construction", "rationale": "Enum.any?/2 is false on an empty collection, so this assertion FAILS on the empty case by construction — it cannot silently pass on absence.", "display": "test/crosswake/manifest/validator_test.exs:249", "enclosing": "manifest validation rejects transfer seams that are missing required endpoint truth", "expression": "errors"},
    {"key": "sha256:905e4990d2cd84e6", "shape": "assert_any", "bucket": "safe-by-construction", "rationale": "Enum.any?/2 is false on an empty collection, so this assertion FAILS on the empty case by construction — it cannot silently pass on absence.", "display": "test/crosswake/manifest/validator_test.exs:261", "enclosing": "manifest validation rejects route runtime drift for native-capture transfer seams", "expression": "errors"},
    {"key": "sha256:48ad371eaf75e8a5", "shape": "assert_any", "bucket": "safe-by-construction", "rationale": "Enum.any?/2 is false on an empty collection, so this assertion FAILS on the empty case by construction — it cannot silently pass on absence.", "display": "test/crosswake/manifest/validator_test.exs:276", "enclosing": "manifest validation rejects unsupported route entry vocabulary", "expression": "errors"},
    {"key": "sha256:935e03a0d5b6f704", "shape": "assert_any", "bucket": "safe-by-construction", "rationale": "Enum.any?/2 is false on an empty collection, so this assertion FAILS on the empty case by construction — it cannot silently pass on absence.", "display": "test/crosswake/manifest/validator_test.exs:291", "enclosing": "manifest validation rejects undeclared corridor_ref values with deterministic messaging", "expression": "errors"},
    {"key": "sha256:6bdd78c41b8ae37e", "shape": "refute_any", "bucket": "needs-fix", "rationale": "runtime-derived collection at test/crosswake/manifest/validator_test.exs:300: `errors` inside test \"manifest validation stays backward-compatible for routes without commerce declarations\" — no preceding cardinality-pinning or emptiness guard was found for this expression; needs-fix pending VAC-02 remediation.", "display": "test/crosswake/manifest/validator_test.exs:300", "enclosing": "manifest validation stays backward-compatible for routes without commerce declarations", "expression": "errors"},
    {"key": "sha256:66a88434f4a0b1f7", "shape": "assert_any", "bucket": "safe-by-construction", "rationale": "Enum.any?/2 is false on an empty collection, so this assertion FAILS on the empty case by construction — it cannot silently pass on absence.", "display": "test/crosswake/manifest/validator_test.exs:312", "enclosing": "commerce corridor field guidance only triggers when a route declares commerce", "expression": "errors"},
    {"key": "sha256:d341ec428005b957", "shape": "assert_any", "bucket": "safe-by-construction", "rationale": "Enum.any?/2 is false on an empty collection, so this assertion FAILS on the empty case by construction — it cannot silently pass on absence.", "display": "test/crosswake/manifest/validator_test.exs:34", "enclosing": "manifest validation rejects missing sections and invalid compatibility truth before serialization", "expression": "errors"},
    {"key": "sha256:0a6f1a38bcd27801", "shape": "assert_any", "bucket": "safe-by-construction", "rationale": "Enum.any?/2 is false on an empty collection, so this assertion FAILS on the empty case by construction — it cannot silently pass on absence.", "display": "test/crosswake/manifest/validator_test.exs:35", "enclosing": "manifest validation rejects missing sections and invalid compatibility truth before serialization", "expression": "errors"},
    {"key": "sha256:f08ace4e2cefc165", "shape": "assert_any", "bucket": "safe-by-construction", "rationale": "Enum.any?/2 is false on an empty collection, so this assertion FAILS on the empty case by construction — it cannot silently pass on absence.", "display": "test/crosswake/manifest/validator_test.exs:351", "enclosing": "manifest validation rejects provider-specific terms in nested entitlement and evidence commerce structures", "expression": "errors"},
    {"key": "sha256:65d4edb72ad4dedc", "shape": "assert_any", "bucket": "safe-by-construction", "rationale": "Enum.any?/2 is false on an empty collection, so this assertion FAILS on the empty case by construction — it cannot silently pass on absence.", "display": "test/crosswake/manifest/validator_test.exs:393", "enclosing": "manifest validation rejects provider-specific terms in corridor semantic metadata", "expression": "errors"},
    {"key": "sha256:09fc128f68683fa3", "shape": "assert_any", "bucket": "safe-by-construction", "rationale": "Enum.any?/2 is false on an empty collection, so this assertion FAILS on the empty case by construction — it cannot silently pass on absence.", "display": "test/crosswake/manifest/validator_test.exs:420", "enclosing": "manifest validation rejects invalid capability metadata vocabulary and empty support facts", "expression": "errors"},
    {"key": "sha256:f6d965431e4f3218", "shape": "assert_any", "bucket": "safe-by-construction", "rationale": "Enum.any?/2 is false on an empty collection, so this assertion FAILS on the empty case by construction — it cannot silently pass on absence.", "display": "test/crosswake/manifest/validator_test.exs:421", "enclosing": "manifest validation rejects invalid capability metadata vocabulary and empty support facts", "expression": "errors"},
    {"key": "sha256:f1e545715ede43aa", "shape": "assert_any", "bucket": "safe-by-construction", "rationale": "Enum.any?/2 is false on an empty collection, so this assertion FAILS on the empty case by construction — it cannot silently pass on absence.", "display": "test/crosswake/manifest/validator_test.exs:422", "enclosing": "manifest validation rejects invalid capability metadata vocabulary and empty support facts", "expression": "errors"},
    {"key": "sha256:05c168c098c38084", "shape": "assert_any", "bucket": "safe-by-construction", "rationale": "Enum.any?/2 is false on an empty collection, so this assertion FAILS on the empty case by construction — it cannot silently pass on absence.", "display": "test/crosswake/manifest/validator_test.exs:423", "enclosing": "manifest validation rejects invalid capability metadata vocabulary and empty support facts", "expression": "errors"},
    {"key": "sha256:9bb5904516fd0927", "shape": "assert_any", "bucket": "safe-by-construction", "rationale": "Enum.any?/2 is false on an empty collection, so this assertion FAILS on the empty case by construction — it cannot silently pass on absence.", "display": "test/crosswake/manifest/validator_test.exs:424", "enclosing": "manifest validation rejects invalid capability metadata vocabulary and empty support facts", "expression": "errors"},
    {"key": "sha256:fff91e7a3749f39f", "shape": "refute_any", "bucket": "needs-fix", "rationale": "runtime-derived collection at test/crosswake/manifest/validator_test.exs:452: `errors` inside test \"policy validation prefers family-first capability vocabulary while keeping compatibility aliases narrow\" — no preceding cardinality-pinning or emptiness guard was found for this expression; needs-fix pending VAC-02 remediation.", "display": "test/crosswake/manifest/validator_test.exs:452", "enclosing": "policy validation prefers family-first capability vocabulary while keeping compatibility aliases narrow", "expression": "errors"},
    {"key": "sha256:3398111c1c522c75", "shape": "refute_any", "bucket": "needs-fix", "rationale": "runtime-derived collection at test/crosswake/manifest/validator_test.exs:453: `errors` inside test \"policy validation prefers family-first capability vocabulary while keeping compatibility aliases narrow\" — no preceding cardinality-pinning or emptiness guard was found for this expression; needs-fix pending VAC-02 remediation.", "display": "test/crosswake/manifest/validator_test.exs:453", "enclosing": "policy validation prefers family-first capability vocabulary while keeping compatibility aliases narrow", "expression": "errors"},
    {"key": "sha256:ca416f883aa891ba", "shape": "assert_any", "bucket": "safe-by-construction", "rationale": "Enum.any?/2 is false on an empty collection, so this assertion FAILS on the empty case by construction — it cannot silently pass on absence.", "display": "test/crosswake/manifest/validator_test.exs:470", "enclosing": "policy validation prefers family-first capability vocabulary while keeping compatibility aliases narrow", "expression": "invalid_errors"},
    {"key": "sha256:017416d7869267ac", "shape": "assert_any", "bucket": "safe-by-construction", "rationale": "Enum.any?/2 is false on an empty collection, so this assertion FAILS on the empty case by construction — it cannot silently pass on absence.", "display": "test/crosswake/manifest/validator_test.exs:62", "enclosing": "topology validation accumulates graph and version failures without echoing rejected values", "expression": "messages"},
    {"key": "sha256:de363780fe919663", "shape": "assert_any", "bucket": "safe-by-construction", "rationale": "Enum.any?/2 is false on an empty collection, so this assertion FAILS on the empty case by construction — it cannot silently pass on absence.", "display": "test/crosswake/manifest/validator_test.exs:63", "enclosing": "topology validation accumulates graph and version failures without echoing rejected values", "expression": "messages"},
    {"key": "sha256:9019d5e90e74052a", "shape": "assert_any", "bucket": "safe-by-construction", "rationale": "Enum.any?/2 is false on an empty collection, so this assertion FAILS on the empty case by construction — it cannot silently pass on absence.", "display": "test/crosswake/manifest/validator_test.exs:64", "enclosing": "topology validation accumulates graph and version failures without echoing rejected values", "expression": "messages"},
    {"key": "sha256:65674037cc085e7d", "shape": "assert_any", "bucket": "safe-by-construction", "rationale": "Enum.any?/2 is false on an empty collection, so this assertion FAILS on the empty case by construction — it cannot silently pass on absence.", "display": "test/crosswake/manifest/validator_test.exs:65", "enclosing": "topology validation accumulates graph and version failures without echoing rejected values", "expression": "messages"},
    {"key": "sha256:eb9391f671076ce5", "shape": "assert_any", "bucket": "safe-by-construction", "rationale": "Enum.any?/2 is false on an empty collection, so this assertion FAILS on the empty case by construction — it cannot silently pass on absence.", "display": "test/crosswake/manifest/validator_test.exs:66", "enclosing": "topology validation accumulates graph and version failures without echoing rejected values", "expression": "messages"},
    {"key": "sha256:2174c71e78392330", "shape": "refute_any", "bucket": "needs-fix", "rationale": "runtime-derived collection at test/crosswake/manifest/validator_test.exs:67: `messages` inside test \"topology validation accumulates graph and version failures without echoing rejected values\" — no preceding cardinality-pinning or emptiness guard was found for this expression; needs-fix pending VAC-02 remediation.", "display": "test/crosswake/manifest/validator_test.exs:67", "enclosing": "topology validation accumulates graph and version failures without echoing rejected values", "expression": "messages"},
    {"key": "sha256:c4d970660dbcccb0", "shape": "assert_any", "bucket": "safe-by-construction", "rationale": "Enum.any?/2 is false on an empty collection, so this assertion FAILS on the empty case by construction — it cannot silently pass on absence.", "display": "test/crosswake/manifest/validator_test.exs:77", "enclosing": "unknown-blocking topology cannot serialize promoted entries", "expression": "errors"},
    {"key": "sha256:e98dd963a0991078", "shape": "refute_any", "bucket": "needs-fix", "rationale": "runtime-derived collection at test/crosswake/manifest/validator_test.exs:85: `Validator.validate(manifest)` inside test \"an empty unknown-blocking topology remains a valid non-promoting manifest section\" — no preceding cardinality-pinning or emptiness guard was found for this expression; needs-fix pending VAC-02 remediation.", "display": "test/crosswake/manifest/validator_test.exs:85", "enclosing": "an empty unknown-blocking topology remains a valid non-promoting manifest section", "expression": "Validator.validate(manifest)"},
    {"key": "sha256:9833b514ee1ea537", "shape": "assert_all", "bucket": "needs-fix", "rationale": "runtime-derived collection at test/crosswake/planning/first_adopter_context_test.exs:22: `paths` inside test \"routing matrix classifies every active path once with non-empty required destinations\" — no preceding cardinality-pinning or emptiness guard was found for this expression; needs-fix pending VAC-02 remediation.", "display": "test/crosswake/planning/first_adopter_context_test.exs:22", "enclosing": "routing matrix classifies every active path once with non-empty required destinations", "expression": "paths"},
    {"key": "sha256:36575ec13936cbc3", "shape": "refute_any", "bucket": "needs-fix", "rationale": "runtime-derived collection at test/crosswake/planning/first_adopter_context_test.exs:280: `FirstAdopterContext.scan_filesystem(root, [])` inside test \"single-digit commercial amounts are prose-aware while positional placeholders are preserved\" — no preceding cardinality-pinning or emptiness guard was found for this expression; needs-fix pending VAC-02 remediation.", "display": "test/crosswake/planning/first_adopter_context_test.exs:280", "enclosing": "single-digit commercial amounts are prose-aware while positional placeholders are preserved", "expression": "FirstAdopterContext.scan_filesystem(root, [])"},
    {"key": "sha256:6db52081b0c0edaa", "shape": "assert_any", "bucket": "safe-by-construction", "rationale": "Enum.any?/2 is false on an empty collection, so this assertion FAILS on the empty case by construction — it cannot silently pass on absence.", "display": "test/crosswake/planning/first_adopter_context_test.exs:33", "enclosing": "routing matrix classifies every active path once with non-empty required destinations", "expression": "matrix"},
    {"key": "sha256:e60e54423b924b8e", "shape": "assert_any", "bucket": "safe-by-construction", "rationale": "Enum.any?/2 is false on an empty collection, so this assertion FAILS on the empty case by construction — it cannot silently pass on absence.", "display": "test/crosswake/planning/first_adopter_context_test.exs:44", "enclosing": "routing matrix classifies every active path once with non-empty required destinations", "expression": "FirstAdopterContext.validate_routing_matrix(missing_destination)"},
    {"key": "sha256:4564f5dfc5729d48", "shape": "assert_any", "bucket": "safe-by-construction", "rationale": "Enum.any?/2 is false on an empty collection, so this assertion FAILS on the empty case by construction — it cannot silently pass on absence.", "display": "test/crosswake/policy/compiler_test.exs:106", "enclosing": "semantic contradictions fail while environment-dependent checks stay out of compile time", "expression": "errors"},
    {"key": "sha256:57e9dc126eb0f103", "shape": "assert_any", "bucket": "safe-by-construction", "rationale": "Enum.any?/2 is false on an empty collection, so this assertion FAILS on the empty case by construction — it cannot silently pass on absence.", "display": "test/crosswake/policy/compiler_test.exs:111", "enclosing": "semantic contradictions fail while environment-dependent checks stay out of compile time", "expression": "errors"},
    {"key": "sha256:eae6ae5ca278dcb6", "shape": "assert_all", "bucket": "safe-cardinality-pinned", "rationale": "pinned to an exact set at line 11 (`assert Enum.map(routes, &{&1.id, &1.runtime}) == [`), which fixes routes's cardinality — the empty case is unreachable while that pin holds.", "display": "test/crosswake/policy/compiler_test.exs:18", "enclosing": "valid routes across all three public runtimes compile into normalized policy output", "expression": "routes"},
    {"key": "sha256:b03c7b34e016880a", "shape": "assert_any", "bucket": "safe-by-construction", "rationale": "Enum.any?/2 is false on an empty collection, so this assertion FAILS on the empty case by construction — it cannot silently pass on absence.", "display": "test/crosswake/policy/compiler_test.exs:203", "enclosing": "provider-specific commerce vocabulary fails with explicit errors and guidance", "expression": "errors"},
    {"key": "sha256:54f7b89eae58244c", "shape": "assert_any", "bucket": "safe-by-construction", "rationale": "Enum.any?/2 is false on an empty collection, so this assertion FAILS on the empty case by construction — it cannot silently pass on absence.", "display": "test/crosswake/policy/compiler_test.exs:208", "enclosing": "provider-specific commerce vocabulary fails with explicit errors and guidance", "expression": "errors"},
    {"key": "sha256:c43fa87c989fbf95", "shape": "assert_any", "bucket": "safe-by-construction", "rationale": "Enum.any?/2 is false on an empty collection, so this assertion FAILS on the empty case by construction — it cannot silently pass on absence.", "display": "test/crosswake/policy/compiler_test.exs:216", "enclosing": "provider-specific commerce vocabulary fails with explicit errors and guidance", "expression": "errors"},
    {"key": "sha256:1da888f64b22f1bc", "shape": "assert_any", "bucket": "safe-by-construction", "rationale": "Enum.any?/2 is false on an empty collection, so this assertion FAILS on the empty case by construction — it cannot silently pass on absence.", "display": "test/crosswake/policy/compiler_test.exs:43", "enclosing": "duplicate ids, missing required metadata, invalid enum values, and adapter runtime fail compilation", "expression": "errors"},
    {"key": "sha256:16ba282f779ddf4a", "shape": "assert_any", "bucket": "safe-by-construction", "rationale": "Enum.any?/2 is false on an empty collection, so this assertion FAILS on the empty case by construction — it cannot silently pass on absence.", "display": "test/crosswake/policy/compiler_test.exs:44", "enclosing": "duplicate ids, missing required metadata, invalid enum values, and adapter runtime fail compilation", "expression": "errors"},
    {"key": "sha256:96c44cac0e38b28e", "shape": "assert_any", "bucket": "safe-by-construction", "rationale": "Enum.any?/2 is false on an empty collection, so this assertion FAILS on the empty case by construction — it cannot silently pass on absence.", "display": "test/crosswake/policy/compiler_test.exs:45", "enclosing": "duplicate ids, missing required metadata, invalid enum values, and adapter runtime fail compilation", "expression": "errors"},
    {"key": "sha256:2cb28677966f6eaa", "shape": "assert_any", "bucket": "safe-by-construction", "rationale": "Enum.any?/2 is false on an empty collection, so this assertion FAILS on the empty case by construction — it cannot silently pass on absence.", "display": "test/crosswake/policy/compiler_test.exs:46", "enclosing": "duplicate ids, missing required metadata, invalid enum values, and adapter runtime fail compilation", "expression": "errors"},
    {"key": "sha256:d6ca2be084007076", "shape": "assert_any", "bucket": "safe-by-construction", "rationale": "Enum.any?/2 is false on an empty collection, so this assertion FAILS on the empty case by construction — it cannot silently pass on absence.", "display": "test/crosswake/policy/compiler_test.exs:93", "enclosing": "semantic contradictions fail while environment-dependent checks stay out of compile time", "expression": "errors"},
    {"key": "sha256:e0417be5f814443c", "shape": "assert_any", "bucket": "safe-by-construction", "rationale": "Enum.any?/2 is false on an empty collection, so this assertion FAILS on the empty case by construction — it cannot silently pass on absence.", "display": "test/crosswake/policy/compiler_test.exs:98", "enclosing": "semantic contradictions fail while environment-dependent checks stay out of compile time", "expression": "errors"},
    {"key": "sha256:28305b92ec3ba2d0", "shape": "assert_any", "bucket": "safe-by-construction", "rationale": "Enum.any?/2 is false on an empty collection, so this assertion FAILS on the empty case by construction — it cannot silently pass on absence.", "display": "test/crosswake/proof/phase133_telemetry_contract_test.exs:474", "enclosing": "TELEM-01 companion merge: stub companion's declared events appear in events/0", "expression": "result"},
    {"key": "sha256:8bd561ae94653859", "shape": "assert_any", "bucket": "safe-by-construction", "rationale": "Enum.any?/2 is false on an empty collection, so this assertion FAILS on the empty case by construction — it cannot silently pass on absence.", "display": "test/crosswake/proof/phase154_catalog_guard_test.exs:286", "enclosing": "CTRL-04 non-vacuity kind 4 — attestation is bidirectional (D-46) > a catalog entry with no shipped command is a GAP", "expression": "violations"},
    {"key": "sha256:bf3bf58de23a7072", "shape": "assert_any", "bucket": "safe-by-construction", "rationale": "Enum.any?/2 is false on an empty collection, so this assertion FAILS on the empty case by construction — it cannot silently pass on absence.", "display": "test/crosswake/proof/phase154_catalog_guard_test.exs:306", "enclosing": "CTRL-04 non-vacuity kind 4 — attestation is bidirectional (D-46) > a shipped command with no catalog entry is an ORPHAN", "expression": "violations"},
    {"key": "sha256:0eda188c3854b127", "shape": "assert_any", "bucket": "safe-by-construction", "rationale": "Enum.any?/2 is false on an empty collection, so this assertion FAILS on the empty case by construction — it cannot silently pass on absence.", "display": "test/crosswake/proof/phase154_catalog_guard_test.exs:391", "enclosing": "CTRL-04 — native command enum parity against BOTH native sources > non-vacuity: a synthetic native enum missing a shipped command is a GAP", "expression": "violations"},
    {"key": "sha256:f191e006eea72049", "shape": "assert_any", "bucket": "safe-by-construction", "rationale": "Enum.any?/2 is false on an empty collection, so this assertion FAILS on the empty case by construction — it cannot silently pass on absence.", "display": "test/crosswake/proof/phase154_catalog_guard_test.exs:413", "enclosing": "CTRL-04 — native command enum parity against BOTH native sources > non-vacuity: a synthetic native enum case with no Elixir command is an ORPHAN", "expression": "violations"},
    {"key": "sha256:56a017c14742bb02", "shape": "assert_all", "bucket": "safe-guarded", "rationale": "guarded by an explicit non-emptiness check on the same collection at line 216 (`assert violations != []`).", "display": "test/crosswake/proof/phase154_recipe_followable_test.exs:217", "enclosing": "omitting a single mechanically-enforced step leaves the gate RED > omitting step 4 on iOS is RED and names the iOS enum source", "expression": "violations"},
    {"key": "sha256:8d35039c77209b91", "shape": "refute_any", "bucket": "safe-guarded", "rationale": "guarded by an explicit non-emptiness check on the same collection at line 216 (`assert violations != []`).", "display": "test/crosswake/proof/phase154_recipe_followable_test.exs:218", "enclosing": "omitting a single mechanically-enforced step leaves the gate RED > omitting step 4 on iOS is RED and names the iOS enum source", "expression": "violations"},
    {"key": "sha256:0a2fe13adc84c011", "shape": "assert_all", "bucket": "safe-guarded", "rationale": "guarded by an explicit non-emptiness check on the same collection at line 228 (`assert violations != []`).", "display": "test/crosswake/proof/phase154_recipe_followable_test.exs:229", "enclosing": "omitting a single mechanically-enforced step leaves the gate RED > omitting step 4 on Android is RED and names the Android enum source", "expression": "violations"},
    {"key": "sha256:2a5d8500fb60f4c1", "shape": "refute_any", "bucket": "safe-guarded", "rationale": "guarded by an explicit non-emptiness check on the same collection at line 228 (`assert violations != []`).", "display": "test/crosswake/proof/phase154_recipe_followable_test.exs:230", "enclosing": "omitting a single mechanically-enforced step leaves the gate RED > omitting step 4 on Android is RED and names the Android enum source", "expression": "violations"},
    {"key": "sha256:bcd0e6bc17453075", "shape": "assert_all", "bucket": "safe-compile-time-literal", "rationale": "@egress_sources is a module attribute holding a fixed literal collection; it cannot be empty, so the empty case is unreachable.", "display": "test/crosswake/proof/phase160_scoped_replay_privacy_test.exs:27", "enclosing": "matrix cannot be made vacuous by removing an egress", "expression": "@egress_sources"},
    {"key": "sha256:c7e61caba4e20c41", "shape": "refute_any", "bucket": "needs-fix", "rationale": "runtime-derived collection at test/crosswake/proof/phase165_ci_integrity_test.exs:149: `manifest[\"proof_leaves\"]` inside test \"brand visual proof stays visibly red and outside required authority\" — no preceding cardinality-pinning or emptiness guard was found for this expression; needs-fix pending VAC-02 remediation.", "display": "test/crosswake/proof/phase165_ci_integrity_test.exs:149", "enclosing": "brand visual proof stays visibly red and outside required authority", "expression": "manifest[\"proof_leaves\"]"},
    {"key": "sha256:6f3ae4531cdf3a61", "shape": "refute_any", "bucket": "needs-fix", "rationale": "runtime-derived collection at test/crosswake/proof/phase165_ci_integrity_test.exs:150: `manifest[\"required_control_nodes\"]` inside test \"brand visual proof stays visibly red and outside required authority\" — no preceding cardinality-pinning or emptiness guard was found for this expression; needs-fix pending VAC-02 remediation.", "display": "test/crosswake/proof/phase165_ci_integrity_test.exs:150", "enclosing": "brand visual proof stays visibly red and outside required authority", "expression": "manifest[\"required_control_nodes\"]"},
    {"key": "sha256:b72e83b1cb382bec", "shape": "refute_any", "bucket": "needs-fix", "rationale": "runtime-derived collection at test/crosswake/proof/phase165_ci_integrity_test.exs:152: `manifest[\"legacy_compatibility_contexts\"]` inside test \"brand visual proof stays visibly red and outside required authority\" — no preceding cardinality-pinning or emptiness guard was found for this expression; needs-fix pending VAC-02 remediation.", "display": "test/crosswake/proof/phase165_ci_integrity_test.exs:152", "enclosing": "brand visual proof stays visibly red and outside required authority", "expression": "manifest[\"legacy_compatibility_contexts\"]"},
    {"key": "sha256:d8d7e5699ce05876", "shape": "assert_all", "bucket": "needs-fix", "rationale": "runtime-derived collection at test/crosswake/proof/phase165_ci_policy_test.exs:127: `fixture[\"cases\"]` inside test \"fixture corpus is closed, adversarial, and deterministic\" — no preceding cardinality-pinning or emptiness guard was found for this expression; needs-fix pending VAC-02 remediation.", "display": "test/crosswake/proof/phase165_ci_policy_test.exs:127", "enclosing": "fixture corpus is closed, adversarial, and deterministic", "expression": "fixture[\"cases\"]"},
    {"key": "sha256:1ff0895f7da0cde7", "shape": "assert_all", "bucket": "safe-cardinality-pinned", "rationale": "pinned to an exact set at line 151 (`assert length(executable) == 45`), which fixes executable's cardinality — the empty case is unreachable while that pin holds.", "display": "test/crosswake/proof/phase165_ci_policy_test.exs:153", "enclosing": "manifest preserves the documentation owner and one required control node", "expression": "executable"},
    {"key": "sha256:87e36d3014d8ba97", "shape": "assert_all", "bucket": "safe-guarded", "rationale": "guarded by an explicit non-emptiness check on the same collection at line 106 (`assert record[\"paths\"] != []`).", "display": "test/crosswake/proof/phase166_repository_quality_test.exs:108", "enclosing": "artifact policy is closed, ordered, non-overlapping, and narrow", "expression": "record[\"paths\"]"},
    {"key": "sha256:678644b84474b247", "shape": "assert_all", "bucket": "needs-fix", "rationale": "runtime-derived collection at test/crosswake/proof/phase166_repository_quality_test.exs:122: `policy[\"generated_contracts\"]` inside test \"artifact policy is closed, ordered, non-overlapping, and narrow\" — no preceding cardinality-pinning or emptiness guard was found for this expression; needs-fix pending VAC-02 remediation.", "display": "test/crosswake/proof/phase166_repository_quality_test.exs:122", "enclosing": "artifact policy is closed, ordered, non-overlapping, and narrow", "expression": "policy[\"generated_contracts\"]"},
    {"key": "sha256:d60b4717ce19a34a", "shape": "assert_all", "bucket": "safe-compile-time-literal", "rationale": "[legacy, docs] is a literal list written directly in source; it cannot be empty by construction.", "display": "test/crosswake/proof/phase166_repository_quality_test.exs:171", "enclosing": "generated contract registry retains legacy generation and adds exact docs sync ownership", "expression": "[legacy, docs]"},
    {"key": "sha256:bf0c515ac407622d", "shape": "assert_all", "bucket": "needs-fix", "rationale": "runtime-derived collection at test/crosswake/proof/phase166_repository_quality_test.exs:266: `rows` inside test \"ownership validator emits the exact deterministic remediation queue\" — no preceding cardinality-pinning or emptiness guard was found for this expression; needs-fix pending VAC-02 remediation.", "display": "test/crosswake/proof/phase166_repository_quality_test.exs:266", "enclosing": "ownership validator emits the exact deterministic remediation queue", "expression": "rows"},
    {"key": "sha256:dd38b72f6137341b", "shape": "assert_any", "bucket": "safe-by-construction", "rationale": "Enum.any?/2 is false on an empty collection, so this assertion FAILS on the empty case by construction — it cannot silently pass on absence.", "display": "test/crosswake/proof/phase166_repository_quality_test.exs:299", "enclosing": "stage parity preserves the literal Phase 165 authority graph", "expression": "manifest[\"proof_leaves\"]"},
    {"key": "sha256:3c0d2266e40440a4", "shape": "assert_any", "bucket": "safe-by-construction", "rationale": "Enum.any?/2 is false on an empty collection, so this assertion FAILS on the empty case by construction — it cannot silently pass on absence.", "display": "test/crosswake/proof/phase166_repository_quality_test.exs:304", "enclosing": "stage parity preserves the literal Phase 165 authority graph", "expression": "manifest[\"proof_leaves\"]"},
    {"key": "sha256:11204609409963b3", "shape": "refute_any", "bucket": "needs-fix", "rationale": "runtime-derived collection at test/crosswake/proof/phase169_diagnostic_legibility_test.exs:387: `status.checks` inside test \"Task 2: the two crash shapes classify distinctly and cascade loudly (D-03, D-08) > the crash-before-roster fixture yields :unavailable with the never-started message, and the five scoped checks + owner check are :unverifiable (exit 3)\" — no preceding cardinality-pinning or emptiness guard was found for this expression; needs-fix pending VAC-02 remediation.", "display": "test/crosswake/proof/phase169_diagnostic_legibility_test.exs:387", "enclosing": "Task 2: the two crash shapes classify distinctly and cascade loudly (D-03, D-08) > the crash-before-roster fixture yields :unavailable with the never-started message, and the five scoped checks + owner check are :unverifiable (exit 3)", "expression": "status.checks"},
    {"key": "sha256:6aabd6825a0fc6f8", "shape": "refute_any", "bucket": "needs-fix", "rationale": "runtime-derived collection at test/crosswake/proof/phase169_diagnostic_legibility_test.exs:412: `status.checks` inside test \"Task 2: the two crash shapes classify distinctly and cascade loudly (D-03, D-08) > the roster-then-crash fixture yields :unverifiable with the terminated-early message, and the five scoped checks + owner check are :unverifiable (exit 3)\" — no preceding cardinality-pinning or emptiness guard was found for this expression; needs-fix pending VAC-02 remediation.", "display": "test/crosswake/proof/phase169_diagnostic_legibility_test.exs:412", "enclosing": "Task 2: the two crash shapes classify distinctly and cascade loudly (D-03, D-08) > the roster-then-crash fixture yields :unverifiable with the terminated-early message, and the five scoped checks + owner check are :unverifiable (exit 3)", "expression": "status.checks"},
    {"key": "sha256:b955becb7a2ec208", "shape": "assert_any", "bucket": "safe-by-construction", "rationale": "Enum.any?/2 is false on an empty collection, so this assertion FAILS on the empty case by construction — it cannot silently pass on absence.", "display": "test/crosswake/proof/phase23_commerce_support_proof_test.exs:250", "enclosing": "unknown entitlement snapshot freshness defaults to fail-closed merge-blocking truth", "expression": "report.findings"},
    {"key": "sha256:241c76a038c90d1b", "shape": "assert_all", "bucket": "safe-cardinality-pinned", "rationale": "pinned to an exact set at line 418 (`assert length(error_findings) == 3,`), which fixes error_findings's cardinality — the empty case is unreachable while that pin holds.", "display": "test/crosswake/proof/phase41_gating_doctor_test.exs:421", "enclosing": "SC#1d: doctor emits :error gating.flag_reference_unknown for all gated routes when companions list is empty", "expression": "error_findings"},
    {"key": "sha256:a2053ad4ff851bd7", "shape": "assert_all", "bucket": "needs-fix", "rationale": "runtime-derived collection at test/crosswake/proof/phase64_runtime_line_policy_test.exs:594: `rows` inside test \"rebuild_matrix carries no row with evidence_tier :device_verified (blind spot closure)\" — no preceding cardinality-pinning or emptiness guard was found for this expression; needs-fix pending VAC-02 remediation.", "display": "test/crosswake/proof/phase64_runtime_line_policy_test.exs:594", "enclosing": "rebuild_matrix carries no row with evidence_tier :device_verified (blind spot closure)", "expression": "rows"},
    {"key": "sha256:a41a4e383b32a3c2", "shape": "refute_any", "bucket": "needs-fix", "rationale": "runtime-derived collection at test/crosswake/proof/phase65_diagnostic_export_seam_test.exs:22: `commands` inside test \"Bridge.Contract.commands/0 has no diagnostics.* entry\" — no preceding cardinality-pinning or emptiness guard was found for this expression; needs-fix pending VAC-02 remediation.", "display": "test/crosswake/proof/phase65_diagnostic_export_seam_test.exs:22", "enclosing": "Bridge.Contract.commands/0 has no diagnostics.* entry", "expression": "commands"},
    {"key": "sha256:89c4ea35dc6328f8", "shape": "assert_any", "bucket": "safe-by-construction", "rationale": "Enum.any?/2 is false on an empty collection, so this assertion FAILS on the empty case by construction — it cannot silently pass on absence.", "display": "test/crosswake/proof/phase70_subscription_saas_commerce_proof_test.exs:301", "enclosing": "hermeticity self-scan guard > requires exactly the allowed pure commerce files", "expression": "allowed_modules"},
    {"key": "sha256:cd9b5603b4e953cb", "shape": "assert_all", "bucket": "safe-cardinality-pinned", "rationale": "pinned to an exact set at line 143 (`assert length(fixtures.approvals) == 3`), which fixes fixtures.approvals's cardinality — the empty case is unreachable while that pin holds.", "display": "test/crosswake/proof/phase7_saas_lane_test.exs:145", "enclosing": "fixture realism stays minimal with one account, two users, and a small approval queue", "expression": "fixtures.approvals"},
    {"key": "sha256:b7f25959ca57329e", "shape": "refute_any", "bucket": "safe-cardinality-pinned", "rationale": "pinned to an exact set at line 85 (`assert manifest.routes[\"saas-approval\"].capabilities == [\"haptics\"]`), which fixes manifest.routes's cardinality — the empty case is unreachable while that pin holds.", "display": "test/crosswake/proof/phase7_saas_lane_test.exs:90", "enclosing": "only the approval detail route declares the bounded haptics capability", "expression": "manifest.routes[\"saas-approval\"].capabilities"},
    {"key": "sha256:9b62fb4ab5214a59", "shape": "assert_any", "bucket": "safe-by-construction", "rationale": "Enum.any?/2 is false on an empty collection, so this assertion FAILS on the empty case by construction — it cannot silently pass on absence.", "display": "test/crosswake/proof_lane/evidence_test.exs:446", "enclosing": "concurrent promoters preserve one winner and return a stable loser result", "expression": "results"},
    {"key": "sha256:db35d435cf7464b5", "shape": "assert_all", "bucket": "needs-fix", "rationale": "runtime-derived collection at test/crosswake/proof_lane/navigation_shell_advisory_test.exs:20: `assertions` inside test \"derives passed assertions only from an exact canonical current-run observation\" — no preceding cardinality-pinning or emptiness guard was found for this expression; needs-fix pending VAC-02 remediation.", "display": "test/crosswake/proof_lane/navigation_shell_advisory_test.exs:20", "enclosing": "derives passed assertions only from an exact canonical current-run observation", "expression": "assertions"},
    {"key": "sha256:38846c0c11c3c2b7", "shape": "assert_all", "bucket": "safe-cardinality-pinned", "rationale": "pinned to an exact set at line 111 (`assert length(assertions) == 19`), which fixes assertions's cardinality — the empty case is unreachable while that pin holds.", "display": "test/crosswake/proof_lane/physical_iphone_preflight_test.exs:113", "enclosing": "physical contract exposes a fixed ordered owner-tagged assertion manifest", "expression": "assertions"},
    {"key": "sha256:5264bde5f55b841e", "shape": "assert_all", "bucket": "safe-cardinality-pinned", "rationale": "pinned to an exact set at line 111 (`assert length(assertions) == 19`), which fixes assertions's cardinality — the empty case is unreachable while that pin holds.", "display": "test/crosswake/proof_lane/physical_iphone_preflight_test.exs:119", "enclosing": "physical contract exposes a fixed ordered owner-tagged assertion manifest", "expression": "assertions"},
    {"key": "sha256:e23b972b199d791b", "shape": "assert_all", "bucket": "safe-cardinality-pinned", "rationale": "pinned to an exact set at line 34 (`assert Enum.map(result.profile_results, & &1.profile) == @profiles`), which fixes result.profile_results's cardinality — the empty case is unreachable while that pin holds.", "display": "test/crosswake/release_candidate/cleanroom_test.exs:35", "enclosing": "candidate-local proof normalizes six payloads, five profiles, and two isolated installs", "expression": "result.profile_results"},
    {"key": "sha256:79329cd28b3746b7", "shape": "assert_all", "bucket": "safe-cardinality-pinned", "rationale": "pinned to an exact set at line 34 (`assert Enum.map(result.profile_results, & &1.profile) == @profiles`), which fixes result.profile_results's cardinality — the empty case is unreachable while that pin holds.", "display": "test/crosswake/release_candidate/cleanroom_test.exs:36", "enclosing": "candidate-local proof normalizes six payloads, five profiles, and two isolated installs", "expression": "result.profile_results"},
    {"key": "sha256:eaa6651b9e1042a0", "shape": "assert_all", "bucket": "safe-cardinality-pinned", "rationale": "pinned to an exact set at line 34 (`assert Enum.map(result.profile_results, & &1.profile) == @profiles`), which fixes result.profile_results's cardinality — the empty case is unreachable while that pin holds.", "display": "test/crosswake/release_candidate/cleanroom_test.exs:37", "enclosing": "candidate-local proof normalizes six payloads, five profiles, and two isolated installs", "expression": "result.profile_results"},
    {"key": "sha256:9b3c8dd7a0ec9f4a", "shape": "refute_any", "bucket": "safe-cardinality-pinned", "rationale": "pinned to an exact set at line 32 (`assert Enum.map(result.companions, & &1.package) == @companions`), which fixes result.companions's cardinality — the empty case is unreachable while that pin holds.", "display": "test/crosswake/release_candidate/coordinate_test.exs:33", "enclosing": "links only Hex, iOS, and Android at the candidate coordinate", "expression": "result.companions"},
    {"key": "sha256:31e6a7441a4f79a2", "shape": "refute_any", "bucket": "safe-cardinality-pinned", "rationale": "pinned to an exact set at line 136 (`assert result.push_arguments == [`), which fixes result.push_arguments's cardinality — the empty case is unreachable while that pin holds.", "display": "test/crosswake/release_candidate/mirror_test.exs:142", "enclosing": "publish permits only immediate ancestor or equal main with immutable atomic refs", "expression": "result.push_arguments"},
    {"key": "sha256:d630e430752f2102", "shape": "refute_any", "bucket": "safe-cardinality-pinned", "rationale": "pinned to an exact set at line 136 (`assert result.push_arguments == [`), which fixes result.push_arguments's cardinality — the empty case is unreachable while that pin holds.", "display": "test/crosswake/release_candidate/mirror_test.exs:144", "enclosing": "publish permits only immediate ancestor or equal main with immutable atomic refs", "expression": "result.push_arguments"},
    {"key": "sha256:d151d66128c154ab", "shape": "refute_any", "bucket": "needs-fix", "rationale": "runtime-derived collection at test/crosswake/release_candidate/mirror_test.exs:204: `Mirror.evaluate!(mode) |> Map.get(:push_arguments, [])` inside test \"recovery alone constructs exact force-with-lease after separate receipt approval\" — no preceding cardinality-pinning or emptiness guard was found for this expression; needs-fix pending VAC-02 remediation.", "display": "test/crosswake/release_candidate/mirror_test.exs:204", "enclosing": "recovery alone constructs exact force-with-lease after separate receipt approval", "expression": "Mirror.evaluate!(mode) |> Map.get(:push_arguments, [])"},
    {"key": "sha256:29757bcfc1373362", "shape": "assert_any", "bucket": "safe-by-construction", "rationale": "Enum.any?/2 is false on an empty collection, so this assertion FAILS on the empty case by construction — it cannot silently pass on absence.", "display": "test/crosswake/shell/activation_test.exs:411", "enclosing": "commerce corridor denials stay fail closed with explicit recovery guidance", "expression": "denial.recovery[:actions]"},
    {"key": "sha256:a0ba607d94e6e9e5", "shape": "assert_all", "bucket": "needs-fix", "rationale": "runtime-derived collection at test/crosswake/shell/activation_test.exs:63: `requests` inside test \"cold start, deep link, notification, and in-app navigation normalize into one activation request shape\" — no preceding cardinality-pinning or emptiness guard was found for this expression; needs-fix pending VAC-02 remediation.", "display": "test/crosswake/shell/activation_test.exs:63", "enclosing": "cold start, deep link, notification, and in-app navigation normalize into one activation request shape", "expression": "requests"},
    {"key": "sha256:07a9c3c934756c02", "shape": "refute_any", "bucket": "needs-fix", "rationale": "runtime-derived collection at test/crosswake/shell/diagnostic_export_test.exs:237: `m` inside test \"to_map/1 > stringifies atom values and rejects nils\" — no preceding cardinality-pinning or emptiness guard was found for this expression; needs-fix pending VAC-02 remediation.", "display": "test/crosswake/shell/diagnostic_export_test.exs:237", "enclosing": "to_map/1 > stringifies atom values and rejects nils", "expression": "m"},
    {"key": "sha256:902c497b8b0e9e45", "shape": "refute_any", "bucket": "needs-fix", "rationale": "runtime-derived collection at test/crosswake/shell/diagnostic_export_test.exs:238: `m` inside test \"to_map/1 > stringifies atom values and rejects nils\" — no preceding cardinality-pinning or emptiness guard was found for this expression; needs-fix pending VAC-02 remediation.", "display": "test/crosswake/shell/diagnostic_export_test.exs:238", "enclosing": "to_map/1 > stringifies atom values and rejects nils", "expression": "m"},
    {"key": "sha256:5174bfd5e39ebeb3", "shape": "assert_any", "bucket": "safe-by-construction", "rationale": "Enum.any?/2 is false on an empty collection, so this assertion FAILS on the empty case by construction — it cannot silently pass on absence.", "display": "test/crosswake/support_matrix/support_matrix_test.exs:158", "enclosing": "canonical support truth publishes one primary crosswake package and explicit package classes", "expression": "matrix.package_surfaces"},
    {"key": "sha256:82661c2ea3d2a777", "shape": "assert_any", "bucket": "safe-by-construction", "rationale": "Enum.any?/2 is false on an empty collection, so this assertion FAILS on the empty case by construction — it cannot silently pass on absence.", "display": "test/crosswake/support_matrix/support_matrix_test.exs:162", "enclosing": "canonical support truth publishes one primary crosswake package and explicit package classes", "expression": "matrix.package_surfaces"},
    {"key": "sha256:556ba03eac3e9fd5", "shape": "assert_any", "bucket": "safe-by-construction", "rationale": "Enum.any?/2 is false on an empty collection, so this assertion FAILS on the empty case by construction — it cannot silently pass on absence.", "display": "test/crosswake/support_matrix/support_matrix_test.exs:167", "enclosing": "canonical support truth publishes one primary crosswake package and explicit package classes", "expression": "matrix.package_surfaces"},
    {"key": "sha256:7a408fb18fb870bd", "shape": "assert_any", "bucket": "safe-by-construction", "rationale": "Enum.any?/2 is false on an empty collection, so this assertion FAILS on the empty case by construction — it cannot silently pass on absence.", "display": "test/crosswake/support_matrix/support_matrix_test.exs:176", "enclosing": "release policy stays hybrid instead of repo-wide lockstep versioning", "expression": "matrix.release_boundaries"},
    {"key": "sha256:b66abd369c9f32c8", "shape": "refute_any", "bucket": "needs-fix", "rationale": "runtime-derived collection at test/crosswake/support_matrix/support_matrix_test.exs:182: `matrix.release_boundaries` inside test \"release policy stays hybrid instead of repo-wide lockstep versioning\" — no preceding cardinality-pinning or emptiness guard was found for this expression; needs-fix pending VAC-02 remediation.", "display": "test/crosswake/support_matrix/support_matrix_test.exs:182", "enclosing": "release policy stays hybrid instead of repo-wide lockstep versioning", "expression": "matrix.release_boundaries"},
    {"key": "sha256:ab9a7098158dd406", "shape": "assert_all", "bucket": "needs-fix", "rationale": "runtime-derived collection at test/crosswake/support_matrix/support_matrix_test.exs:187: `Enum.filter(\n               matrix.release_boundaries,\n               &(&1.target in [\"ios_shell\", \"android_shell\"])\n             )` inside test \"release policy stays hybrid instead of repo-wide lockstep versioning\" — no preceding cardinality-pinning or emptiness guard was found for this expression; needs-fix pending VAC-02 remediation.", "display": "test/crosswake/support_matrix/support_matrix_test.exs:187", "enclosing": "release policy stays hybrid instead of repo-wide lockstep versioning", "expression": "Enum.filter(\n               matrix.release_boundaries,\n               &(&1.target in [\"ios_shell\", \"android_shell\"])\n             )"},
    {"key": "sha256:8f332b98ec20ab63", "shape": "assert_any", "bucket": "safe-by-construction", "rationale": "Enum.any?/2 is false on an empty collection, so this assertion FAILS on the empty case by construction — it cannot silently pass on absence.", "display": "test/crosswake/support_matrix/support_matrix_test.exs:275", "enclosing": "phase 51 promotion rules keep advisory proof criteria explicit", "expression": "entries"},
    {"key": "sha256:71355d929504db9c", "shape": "assert_any", "bucket": "safe-by-construction", "rationale": "Enum.any?/2 is false on an empty collection, so this assertion FAILS on the empty case by construction — it cannot silently pass on absence.", "display": "test/crosswake/support_matrix/support_matrix_test.exs:280", "enclosing": "phase 51 promotion rules keep advisory proof criteria explicit", "expression": "entries"},
    {"key": "sha256:863ce38bef74bfba", "shape": "assert_any", "bucket": "safe-by-construction", "rationale": "Enum.any?/2 is false on an empty collection, so this assertion FAILS on the empty case by construction — it cannot silently pass on absence.", "display": "test/crosswake/support_matrix/support_matrix_test.exs:469", "enclosing": "audit_ledger_support_truth/0 (OPER-02) > entitlement and evidence support entries encode freshness and non-authoritative posture", "expression": "entitlement_snapshot.prerequisites"},
    {"key": "sha256:dbc8a78f8c27d2cf", "shape": "assert_any", "bucket": "safe-by-construction", "rationale": "Enum.any?/2 is false on an empty collection, so this assertion FAILS on the empty case by construction — it cannot silently pass on absence.", "display": "test/crosswake/support_matrix/support_matrix_test.exs:479", "enclosing": "audit_ledger_support_truth/0 (OPER-02) > entitlement and evidence support entries encode freshness and non-authoritative posture", "expression": "reconciliation_evidence.prerequisites"},
    {"key": "sha256:368c0916b595a13f", "shape": "refute_any", "bucket": "safe-cardinality-pinned", "rationale": "pinned to an exact set at line 518 (`assert Enum.map(entries, & &1.corridor_role) == [`), which fixes entries's cardinality — the empty case is unreachable while that pin holds.", "display": "test/crosswake/support_matrix/support_matrix_test.exs:536", "enclosing": "audit_ledger_support_truth/0 (OPER-02) > commerce corridor support truth stays provider-neutral and uses canonical denial taxonomy", "expression": "entries"},
    {"key": "sha256:ad55af19a9db96d5", "shape": "assert_any", "bucket": "safe-by-construction", "rationale": "Enum.any?/2 is false on an empty collection, so this assertion FAILS on the empty case by construction — it cannot silently pass on absence.", "display": "test/crosswake/support_matrix/support_matrix_test.exs:902", "enclosing": "audit_ledger_support_truth/0 (OPER-02) > validate/1 rejects a rebuild_matrix row with evidence_tier :device_verified (evidence laundering gate)", "expression": "errors"},
    {"key": "sha256:8dd057ab9b2e7ee2", "shape": "assert_all", "bucket": "safe-cardinality-pinned", "rationale": "pinned to an exact set at line 27 (`assert Enum.map(checks, & &1.id) == [`), which fixes checks's cardinality — the empty case is unreachable while that pin holds.", "display": "test/mix/tasks/crosswake.proof_lane.physical_iphone_test.exs:44", "enclosing": "readiness JSON returns all stable prerequisite categories without running reports", "expression": "checks"},
    {"key": "sha256:cce3a74c3adbbdca", "shape": "assert_all", "bucket": "safe-cardinality-pinned", "rationale": "assertion_ids (bound from the device report telemetry at line 67, `assert_receive {:device, %{assertion_ids: assertion_ids}}`) is asserted equal to a 19-item literal list immediately above (line 69); assertions and assertion_ids are the same underlying collection surfaced under two different field names on the same struct, which a text-only scanner cannot infer from identifier text alone — recorded as a manual override with this citation, not a heuristic match.", "display": "test/mix/tasks/crosswake.proof_lane.physical_iphone_test.exs:92", "enclosing": "a complete owner-disjoint report joins only after ready preflight", "expression": "assertions"},
    {"key": "sha256:972cd6e05a66c51f", "shape": "assert_any", "bucket": "safe-by-construction", "rationale": "Enum.any?/2 is false on an empty collection, so this assertion FAILS on the empty case by construction — it cannot silently pass on absence.", "display": "test/mix/tasks/crosswake_doctor_test.exs:186", "enclosing": "mix crosswake.doctor --check-publish emits conditional json readiness and raises on blocking readiness", "expression": "decoded[\"publish_readiness\"][\"checks\"]"},
    {"key": "sha256:44e5ea4881076f97", "shape": "assert_any", "bucket": "safe-by-construction", "rationale": "Enum.any?/2 is false on an empty collection, so this assertion FAILS on the empty case by construction — it cannot silently pass on absence.", "display": "test/mix/tasks/crosswake_doctor_test.exs:191", "enclosing": "mix crosswake.doctor --check-publish emits conditional json readiness and raises on blocking readiness", "expression": "decoded[\"findings\"]"},
    {"key": "sha256:1d21bba78ec71ea8", "shape": "assert_all", "bucket": "safe-guarded", "rationale": "guarded by an explicit non-emptiness check on the same collection at line 295 (`assert corridor_findings != []`).", "display": "test/mix/tasks/crosswake_doctor_test.exs:297", "enclosing": "mix crosswake.doctor json output serializes commerce corridor fields", "expression": "corridor_findings"},
    {"key": "sha256:6b9fd022ada518b4", "shape": "assert_any", "bucket": "safe-by-construction", "rationale": "Enum.any?/2 is false on an empty collection, so this assertion FAILS on the empty case by construction — it cannot silently pass on absence.", "display": "test/mix/tasks/crosswake_doctor_test.exs:304", "enclosing": "mix crosswake.doctor json output serializes commerce corridor fields", "expression": "corridor_findings"},
    {"key": "sha256:d18a98253e2ac781", "shape": "assert_all", "bucket": "needs-fix", "rationale": "runtime-derived collection at test/mix/tasks/crosswake_gen_proof_lane_test.exs:129: `diff` inside test \"check and diff are byte-for-byte read-only and diff reports only safe sorted statuses\" — no preceding cardinality-pinning or emptiness guard was found for this expression; needs-fix pending VAC-02 remediation.", "display": "test/mix/tasks/crosswake_gen_proof_lane_test.exs:129", "enclosing": "check and diff are byte-for-byte read-only and diff reports only safe sorted statuses", "expression": "diff"},
    {"key": "sha256:581fa063c5b02c4e", "shape": "refute_any", "bucket": "needs-fix", "rationale": "runtime-derived collection at test/mix/tasks/crosswake_gen_proof_lane_test.exs:130: `diff` inside test \"check and diff are byte-for-byte read-only and diff reports only safe sorted statuses\" — no preceding cardinality-pinning or emptiness guard was found for this expression; needs-fix pending VAC-02 remediation.", "display": "test/mix/tasks/crosswake_gen_proof_lane_test.exs:130", "enclosing": "check and diff are byte-for-byte read-only and diff reports only safe sorted statuses", "expression": "diff"},
    {"key": "sha256:f4f5a3b9405ace35", "shape": "assert_all", "bucket": "needs-fix", "rationale": "runtime-derived collection at test/mix/tasks/crosswake_gen_proof_lane_test.exs:155: `outcomes` inside test \"concurrent generators preserve host-owned destinations\" — no preceding cardinality-pinning or emptiness guard was found for this expression; needs-fix pending VAC-02 remediation.", "display": "test/mix/tasks/crosswake_gen_proof_lane_test.exs:155", "enclosing": "concurrent generators preserve host-owned destinations", "expression": "outcomes"},
    {"key": "sha256:de76c4df733d37b3", "shape": "assert_all", "bucket": "needs-fix", "rationale": "runtime-derived collection at test/mix/tasks/crosswake_gen_proof_lane_test.exs:178: `Generator.diff(config)` inside test \"generator actions ignore a poisoned former shared helper cache\" — no preceding cardinality-pinning or emptiness guard was found for this expression; needs-fix pending VAC-02 remediation.", "display": "test/mix/tasks/crosswake_gen_proof_lane_test.exs:178", "enclosing": "generator actions ignore a poisoned former shared helper cache", "expression": "Generator.diff(config)"},
    {"key": "sha256:c98a318947b63d01", "shape": "assert_any", "bucket": "safe-by-construction", "rationale": "Enum.any?/2 is false on an empty collection, so this assertion FAILS on the empty case by construction — it cannot silently pass on absence.", "display": "test/mix/tasks/crosswake_gen_proof_lane_test.exs:353", "enclosing": "generator actions reject generated namespace and manifest symlinks without writes outside root", "expression": "findings"},
    {"key": "sha256:4bf8a28b7666773f", "shape": "assert_any", "bucket": "safe-by-construction", "rationale": "Enum.any?/2 is false on an empty collection, so this assertion FAILS on the empty case by construction — it cannot silently pass on absence.", "display": "test/mix/tasks/crosswake_gen_proof_lane_test.exs:91", "enclosing": "version-1 manifests remain host-owned but fail the version-2 provenance check", "expression": "findings"},
    {"key": "sha256:e9c688886493a94b", "shape": "assert_any", "bucket": "safe-by-construction", "rationale": "Enum.any?/2 is false on an empty collection, so this assertion FAILS on the empty case by construction — it cannot silently pass on absence.", "display": "test/mix/tasks/crosswake_release_status_test.exs:148", "enclosing": "json task output is a stable automation contract", "expression": "decoded[\"checks\"]"},
    {"key": "sha256:f891c52affeb5f81", "shape": "assert_any", "bucket": "safe-by-construction", "rationale": "Enum.any?/2 is false on an empty collection, so this assertion FAILS on the empty case by construction — it cannot silently pass on absence.", "display": "test/mix/tasks/crosswake_release_status_test.exs:151", "enclosing": "json task output is a stable automation contract", "expression": "decoded[\"checks\"]"},
    {"key": "sha256:55b005e664b00714", "shape": "refute_any", "bucket": "needs-fix", "rationale": "runtime-derived collection at test/mix/tasks/crosswake_release_status_test.exs:235: `presence_evidence` inside test \"live probes split definite absence from unverifiable, and both fail closed (D-18)\" — no preceding cardinality-pinning or emptiness guard was found for this expression; needs-fix pending VAC-02 remediation.", "display": "test/mix/tasks/crosswake_release_status_test.exs:235", "enclosing": "live probes split definite absence from unverifiable, and both fail closed (D-18)", "expression": "presence_evidence"},
    {"key": "sha256:5558a223f8664969", "shape": "assert_any", "bucket": "safe-by-construction", "rationale": "Enum.any?/2 is false on an empty collection, so this assertion FAILS on the empty case by construction — it cannot silently pass on absence.", "display": "test/mix/tasks/crosswake_release_status_test.exs:250", "enclosing": "live probes split definite absence from unverifiable, and both fail closed (D-18)", "expression": "unverifiable_evidence"},
    {"key": "sha256:3af4e37b564a2a44", "shape": "assert_any", "bucket": "safe-by-construction", "rationale": "Enum.any?/2 is false on an empty collection, so this assertion FAILS on the empty case by construction — it cannot silently pass on absence.", "display": "test/mix/tasks/crosswake_release_status_test.exs:27", "enclosing": "release status reports local graph and scanner-backed guard checks", "expression": "status.core"},
    {"key": "sha256:986013509ce2bd25", "shape": "refute_any", "bucket": "needs-fix", "rationale": "runtime-derived collection at test/mix/tasks/crosswake_release_status_test.exs:277: `status.checks` inside test \"all-ok live probes produce one ok presence check, no unverifiable check, and exit 0\" — no preceding cardinality-pinning or emptiness guard was found for this expression; needs-fix pending VAC-02 remediation.", "display": "test/mix/tasks/crosswake_release_status_test.exs:277", "enclosing": "all-ok live probes produce one ok presence check, no unverifiable check, and exit 0", "expression": "status.checks"},
    {"key": "sha256:c986c8bea96d43d6", "shape": "assert_any", "bucket": "safe-by-construction", "rationale": "Enum.any?/2 is false on an empty collection, so this assertion FAILS on the empty case by construction — it cannot silently pass on absence.", "display": "test/mix/tasks/crosswake_release_status_test.exs:28", "enclosing": "release status reports local graph and scanner-backed guard checks", "expression": "status.companions"},
    {"key": "sha256:598407211cefde97", "shape": "assert_any", "bucket": "safe-by-construction", "rationale": "Enum.any?/2 is false on an empty collection, so this assertion FAILS on the empty case by construction — it cannot silently pass on absence.", "display": "test/mix/tasks/crosswake_release_status_test.exs:29", "enclosing": "release status reports local graph and scanner-backed guard checks", "expression": "status.checks"},
    {"key": "sha256:a92b0dd53c7807c5", "shape": "refute_any", "bucket": "needs-fix", "rationale": "runtime-derived collection at test/mix/tasks/crosswake_release_status_test.exs:410: `status.checks` inside test \"an unreleased bootstrap companion is advisory, not fatal\" — no preceding cardinality-pinning or emptiness guard was found for this expression; needs-fix pending VAC-02 remediation.", "display": "test/mix/tasks/crosswake_release_status_test.exs:410", "enclosing": "an unreleased bootstrap companion is advisory, not fatal", "expression": "status.checks"},
    {"key": "sha256:0d2ba1d9e100fa3c", "shape": "refute_any", "bucket": "needs-fix", "rationale": "runtime-derived collection at test/mix/tasks/crosswake_release_status_test.exs:446: `status.checks` inside test \"an unreachable registry is never laundered into the bootstrap carve-out\" — no preceding cardinality-pinning or emptiness guard was found for this expression; needs-fix pending VAC-02 remediation.", "display": "test/mix/tasks/crosswake_release_status_test.exs:446", "enclosing": "an unreachable registry is never laundered into the bootstrap carve-out", "expression": "status.checks"}
]
}
