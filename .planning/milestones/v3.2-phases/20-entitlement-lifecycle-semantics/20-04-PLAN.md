---
phase: 20-entitlement-lifecycle-semantics
plan: 04
type: execute
wave: 4
depends_on:
  - 20-02
  - 20-03
files_modified:
  - lib/crosswake/commerce/contracts.ex
  - lib/crosswake/commerce/reconciliation.ex
  - test/crosswake/commerce/contracts_test.exs
  - test/crosswake/commerce/reconciliation_test.exs
autonomous: true
requirements:
  - ENTL-03
user_setup: []
must_haves:
  truths:
    - "Evidence source vocabulary is enforced fail-closed at runtime boundaries, not only documented."
    - "Only canonical evidence sources `:device | :storefront | :webhook | :support` are accepted in core entitlement/reconciliation paths."
    - "Invalid evidence source values are rejected in both snapshot construction and reconciliation ingestion with explicit errors."
    - "Evidence remains non-authoritative; source-validation hardening does not widen provider-specific semantics."
  artifacts:
    - path: lib/crosswake/commerce/contracts.ex
      provides: "Fail-closed source validation in `Contracts.new_entitlement_snapshot/1` for `EvidenceLane.source`"
      contains: "new_entitlement_snapshot"
    - path: lib/crosswake/commerce/reconciliation.ex
      provides: "Fail-closed source validation in `Reconciliation.ingest_evidence/2` for `ReconciliationEvidence.source`"
      contains: "ingest_evidence"
    - path: test/crosswake/commerce/contracts_test.exs
      provides: "Negative regression proving invalid snapshot evidence source is rejected"
      contains: "invalid evidence source"
    - path: test/crosswake/commerce/reconciliation_test.exs
      provides: "Negative regression proving invalid reconciliation evidence source is rejected"
      contains: "invalid source"
  key_links:
    - from: lib/crosswake/commerce/contracts.ex
      to: test/crosswake/commerce/contracts_test.exs
      via: "Snapshot constructor rejects non-canonical evidence source values"
      pattern: "new_entitlement_snapshot"
    - from: lib/crosswake/commerce/reconciliation.ex
      to: test/crosswake/commerce/reconciliation_test.exs
      via: "Ingestion rejects non-canonical evidence source values before producing an `EvidenceResult`"
      pattern: "ingest_evidence"
---

<objective>
Close the unresolved HIGH ENTL-03 verification gap by enforcing canonical evidence source vocabulary fail-closed in runtime contract entry points.

Purpose: Eliminate acceptance of invalid source values (for example `:device_callback`) in both snapshot construction and reconciliation ingestion.
Output: Runtime guards in contract/reconciliation modules plus negative tests that fail if invalid sources are accepted.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-entitlement-lifecycle-semantics/20-CONTEXT.md
@.planning/phases/20-entitlement-lifecycle-semantics/20-RESEARCH.md
@.planning/phases/20-entitlement-lifecycle-semantics/20-PATTERNS.md
@.planning/phases/20-entitlement-lifecycle-semantics/20-VERIFICATION.md
@.planning/phases/20-entitlement-lifecycle-semantics/20-REVIEW.md
@lib/crosswake/commerce/contracts.ex
@lib/crosswake/commerce/reconciliation.ex
@test/crosswake/commerce/contracts_test.exs
@test/crosswake/commerce/reconciliation_test.exs
</context>

<tasks>
<task type="auto">
  <name>Task 1: Enforce canonical `EvidenceLane.source` in `Contracts.new_entitlement_snapshot/1`</name>
  <files>lib/crosswake/commerce/contracts.ex, test/crosswake/commerce/contracts_test.exs</files>
  <read_first>lib/crosswake/commerce/contracts.ex, test/crosswake/commerce/contracts_test.exs, .planning/phases/20-entitlement-lifecycle-semantics/20-VERIFICATION.md, .planning/phases/20-entitlement-lifecycle-semantics/20-REVIEW.md</read_first>
  <action>Add explicit runtime validation so `Contracts.new_entitlement_snapshot/1` accepts only `EvidenceLane.source` values in `:device | :storefront | :webhook | :support`. Reject any non-canonical atom/string source fail-closed with `{:error, ...}` and actionable reason metadata. Keep provider-neutral semantics and do not introduce provider-specific vocabulary.</action>
  <verify>mix test test/crosswake/commerce/contracts_test.exs</verify>
  <acceptance_criteria>
    - `lib/crosswake/commerce/contracts.ex` contains a canonical evidence-source allowlist used by `new_entitlement_snapshot/1`
    - `lib/crosswake/commerce/contracts.ex` path for `new_entitlement_snapshot/1` returns `{:error, ...}` for invalid evidence source values
    - `test/crosswake/commerce/contracts_test.exs` contains a negative test for `%EvidenceLane{source: :device_callback}` rejection
    - `mix test test/crosswake/commerce/contracts_test.exs` passes
  </acceptance_criteria>
  <done>Snapshot construction no longer accepts out-of-vocabulary evidence sources.</done>
</task>

<task type="auto">
  <name>Task 2: Enforce canonical `ReconciliationEvidence.source` in `Reconciliation.ingest_evidence/2`</name>
  <files>lib/crosswake/commerce/reconciliation.ex, test/crosswake/commerce/reconciliation_test.exs</files>
  <read_first>lib/crosswake/commerce/reconciliation.ex, test/crosswake/commerce/reconciliation_test.exs, lib/crosswake/commerce/contracts.ex, .planning/phases/20-entitlement-lifecycle-semantics/20-VERIFICATION.md</read_first>
  <action>Add explicit source validation at ingestion boundary so `Reconciliation.ingest_evidence/2` fails closed for invalid `ReconciliationEvidence.source` values before producing `%EvidenceResult{}`. Keep valid-source behavior unchanged and keep evidence non-authoritative (`ENTL-03`).</action>
  <verify>mix test test/crosswake/commerce/reconciliation_test.exs</verify>
  <acceptance_criteria>
    - `lib/crosswake/commerce/reconciliation.ex` contains canonical source validation for `ingest_evidence/2`
    - `lib/crosswake/commerce/reconciliation.ex` returns `{:error, ...}` (not `{:ok, %EvidenceResult{...}}`) for `source: :device_callback`
    - `test/crosswake/commerce/reconciliation_test.exs` contains a negative rejection case for invalid source vocabulary
    - `mix test test/crosswake/commerce/reconciliation_test.exs` passes
  </acceptance_criteria>
  <done>Reconciliation ingestion no longer accepts invalid source vocabulary at runtime.</done>
</task>

<task type="auto">
  <name>Task 3: Lock regression coverage for invalid source rejection across both ENTL-03 paths</name>
  <files>test/crosswake/commerce/contracts_test.exs, test/crosswake/commerce/reconciliation_test.exs</files>
  <read_first>test/crosswake/commerce/contracts_test.exs, test/crosswake/commerce/reconciliation_test.exs, .planning/phases/20-entitlement-lifecycle-semantics/20-REVIEW.md</read_first>
  <action>Add focused negative tests that prove invalid source values are rejected in both constructor/ingestion paths and that valid canonical values continue to pass. Keep test naming explicit to preserve traceability to ENTL-03 gap closure.</action>
  <verify>mix test test/crosswake/commerce/contracts_test.exs test/crosswake/commerce/reconciliation_test.exs</verify>
  <acceptance_criteria>
    - `test/crosswake/commerce/contracts_test.exs` contains a test name referencing invalid source rejection for `new_entitlement_snapshot/1`
    - `test/crosswake/commerce/reconciliation_test.exs` contains a test name referencing invalid source rejection for `ingest_evidence/2`
    - `rg ":device_callback" test/crosswake/commerce/contracts_test.exs test/crosswake/commerce/reconciliation_test.exs` returns matching negative test fixtures
    - `mix test test/crosswake/commerce/contracts_test.exs test/crosswake/commerce/reconciliation_test.exs` passes
  </acceptance_criteria>
  <done>ENTL-03 invalid-source boundary enforcement is merge-blocking and reproducible.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

- Evidence source labels cross from device/storefront/webhook/support edges into core snapshot/reconciliation contracts.
- Any acceptance of non-canonical source labels weakens fail-closed semantics and allows vocabulary drift.

## STRIDE Threat Register

| threat_id | category | component | disposition | mitigation_plan |
| --- | --- | --- | --- | --- |
| P20-04-T01 | spoofing | Evidence source attribution | mitigated | Enforce canonical source allowlist at both constructor and ingestion boundaries; reject unknown values fail-closed. |
| P20-04-T02 | tampering | Runtime contract input | mitigated | Validate source before producing snapshot or evidence result structs; return explicit errors for invalid values. |
| P20-04-T03 | elevation_of_privilege | ENTL-03 authority boundary | mitigated | Preserve non-authoritative evidence flow and prevent invalid source values from entering reconciliation/snapshot pipelines. |
</threat_model>

<verification>
Before declaring plan complete:
- [ ] mix test test/crosswake/commerce/contracts_test.exs
- [ ] mix test test/crosswake/commerce/reconciliation_test.exs
- [ ] mix test test/crosswake/commerce/contracts_test.exs test/crosswake/commerce/reconciliation_test.exs
- [ ] rg "new_entitlement_snapshot|ingest_evidence|EvidenceLane|ReconciliationEvidence" lib/crosswake/commerce/contracts.ex lib/crosswake/commerce/reconciliation.ex
- [ ] rg "device_callback|invalid source|invalid evidence source" test/crosswake/commerce/contracts_test.exs test/crosswake/commerce/reconciliation_test.exs
</verification>

<success_criteria>
- All tasks completed
- `Contracts.new_entitlement_snapshot/1` rejects out-of-vocabulary `EvidenceLane.source` values fail-closed
- `Reconciliation.ingest_evidence/2` rejects out-of-vocabulary `ReconciliationEvidence.source` values fail-closed
- Negative tests in both required commerce test files prevent ENTL-03 regression
- Core semantics remain provider-neutral and preserve non-authoritative evidence posture
</success_criteria>

<output>
After completion, create `.planning/phases/20-entitlement-lifecycle-semantics/20-04-SUMMARY.md`
</output>
