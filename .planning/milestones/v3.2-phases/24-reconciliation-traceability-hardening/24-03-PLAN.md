---
phase: 24-reconciliation-traceability-hardening
plan: 03
type: execute
wave: 3
depends_on:
  - 24-01
  - 24-02
files_modified:
  - .planning/v3.2-MILESTONE-AUDIT.md
autonomous: true
requirements:
  - RECN-01
  - RECN-02
  - RECN-03
user_setup: []
must_haves:
  truths:
    - "`.planning/v3.2-MILESTONE-AUDIT.md` has a new `reaudits:` YAML list key in frontmatter with one entry: `phase: 24`, `current_status: \"satisfied\"`, evidence pointing at the closed gaps and the parity test path"
    - "The original top-level `status: gaps_found` field is UNCHANGED (immutable historical verdict per D-07 and Footgun 2)"
    - "A new `## Re-Audit (Phase 24)` H2 body section exists at end-of-file (after `## Recommendation`) containing a three-source cross-check table with before/after diff cells for RECN-01/02/03"
    - "The body section explicitly cites the closed-gap commits and the parity test path `test/crosswake/planning/summary_frontmatter_test.exs`"
    - "No `lib/`, `examples/`, `test/`, or `.github/` files mutated (audit-doc-append-only scope per Footgun 4)"
  artifacts:
    - path: ".planning/v3.2-MILESTONE-AUDIT.md"
      provides: "Append-only re-audit evidence: reaudits[] frontmatter entry + `## Re-Audit (Phase 24)` body section"
      contains: "## Re-Audit (Phase 24)"
  key_links:
    - from: ".planning/v3.2-MILESTONE-AUDIT.md reaudits[0].evidence"
      to: "Plan 24-01 and Plan 24-02 fix commits"
      via: "Evidence list citing renamed SUMMARY files, flipped bullets, updated table cells, and parity test path"
      pattern: "test/crosswake/planning/summary_frontmatter_test.exs"
    - from: ".planning/v3.2-MILESTONE-AUDIT.md `## Re-Audit (Phase 24)` body"
      to: "future 24-VERIFICATION.md (per D-08, VERIFICATION.md cites this section)"
      via: "Canonical re-audit evidence section, not duplicated in VERIFICATION.md"
      pattern: "^## Re-Audit \\(Phase 24\\)"
---

<objective>
Append the Phase 24 re-audit evidence to `.planning/v3.2-MILESTONE-AUDIT.md` — both the frontmatter `reaudits:` list entry and the body `## Re-Audit (Phase 24)` section — without mutating the original `status: gaps_found` verdict.

Purpose: The milestone audit doc is the canonical source the milestone-archive automation reads. The original audit verdict is historical fact and must not be flipped; the re-audit lives as an append-only event that records the current state of the three RECN requirements after Plans 24-01 and 24-02 closed the artifact-shape gap. This is the Phase 21 D-04 (append-only normalized evidence events) and Phase 23 D-11 (proof-lane evidence preservation) posture applied to the audit doc.

Output: Exactly one file modified — `.planning/v3.2-MILESTONE-AUDIT.md` — with a new frontmatter key `reaudits:` (list shape, ready for N≥2 future re-audits) and a new H2 body section at end-of-file.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/v3.2-MILESTONE-AUDIT.md
@.planning/phases/24-reconciliation-traceability-hardening/24-CONTEXT.md
@.planning/phases/24-reconciliation-traceability-hardening/24-RESEARCH.md
@.planning/phases/24-reconciliation-traceability-hardening/24-PATTERNS.md
@.planning/phases/24-reconciliation-traceability-hardening/24-01-SUMMARY.md
@.planning/phases/24-reconciliation-traceability-hardening/24-02-SUMMARY.md

<interfaces>
<!-- Append target shape (from PATTERNS.md "Mutation 1" and "Mutation 2" for v3.2-MILESTONE-AUDIT.md and from RESEARCH Finding 4). -->

Current last frontmatter key (lines 76-84 of .planning/v3.2-MILESTONE-AUDIT.md):

```yaml
nyquist:
  compliant_phases:
    - "Phase 21"
  partial_phases: []
  missing_phases:
    - "Phase 19"
    - "Phase 20"
    - "Phase 22"
  overall: "partial"
---
```

Frontmatter append target (insert new top-level key `reaudits:` AFTER the `nyquist:` block, BEFORE the closing `---`):

```yaml
reaudits:
  - phase: 24
    date: "2026-05-27"
    current_status: "satisfied"
    evidence:
      - "21-01-SUMMARY.md: requirements → requirements-completed (Plan 24-01 Task 1)"
      - "21-02-SUMMARY.md: requirements → requirements-completed (Plan 24-01 Task 1)"
      - "REQUIREMENTS.md: RECN-01/02/03 bullets flipped to [x] (Plan 24-01 Task 2)"
      - "REQUIREMENTS.md: traceability table updated with Phase 21 + Phase 24 (Plan 24-01 Task 2)"
      - "Parity test: test/crosswake/planning/summary_frontmatter_test.exs (Plan 24-02 Task 1; passes 2 tests, 0 failures)"
      - "CI merge gate: .github/workflows/phase23-proof.yml merge-blocking-commerce-proof job includes the parity test step (Plan 24-02 Task 2)"
```

IMMUTABLE — DO NOT CHANGE — the top-level `status: gaps_found` field on line 4 (D-07, Footgun 2). All derived current state lives in `reaudits[].current_status`.

Body append target — the file currently ends at line 187 with `## Recommendation` content ending with `/gsd-plan-milestone-gaps`. Append the new body section at end-of-file, separated by a blank line. The body section format (from PATTERNS.md Mutation 2):

```markdown

## Re-Audit (Phase 24)

**Re-audited:** 2026-05-27
**Trigger:** Phase 21 SUMMARY frontmatter key normalization (`requirements:` → `requirements-completed:`)
**Scope:** RECN-01, RECN-02, RECN-03 only

### Three-Source Cross-Check: RECN-01/02/03

| Requirement | REQUIREMENTS.md | VERIFICATION.md | SUMMARY frontmatter (`requirements-completed:`) | Final status |
|-------------|-----------------|-----------------|------------------------------------------------|--------------|
| RECN-01 | before: `[ ]` → after: `[x]` | passed (Phase 21) — unchanged | before: missing (`requirements:` key) → after: listed (`requirements-completed:`) | **satisfied** |
| RECN-02 | before: `[ ]` → after: `[x]` | passed (Phase 21) — unchanged | before: missing (`requirements:` key) → after: listed (`requirements-completed:`) | **satisfied** |
| RECN-03 | before: `[ ]` → after: `[x]` | passed (Phase 21) — unchanged | before: missing (`requirements:` key) → after: listed (`requirements-completed:`) | **satisfied** |

### What Changed

- `.planning/phases/21-reconciliation-example/21-01-SUMMARY.md`: `requirements:` key renamed to `requirements-completed:`. List contents (RECN-01/02/03) preserved exactly.
- `.planning/phases/21-reconciliation-example/21-02-SUMMARY.md`: Same rename.
- `.planning/REQUIREMENTS.md`: RECN-01/02/03 bullets flipped from `[ ]` to `[x]`. Traceability table cells updated from `Phase 24 / Pending` to `Phase 21 (validated); Phase 24 (traceability normalized) / Complete`.
- `test/crosswake/planning/summary_frontmatter_test.exs`: New merge-blocking parity test. Passes against all 13 `.planning/phases/*/*-SUMMARY.md` files.
- `.github/workflows/phase23-proof.yml`: Merge-blocking job amended to include the new parity test path.

### Original Gap (from initial audit)

The initial audit found RECN-01/02/03 as `partial` because Phase 21's two SUMMARY files used `requirements:` instead of the canonical `requirements-completed:` key, preventing the cross-check from detecting completion. Phase 21's `21-VERIFICATION.md` was already green and the implementation was fully verified — the gap was purely artifact-shape inconsistency.

### Re-Audit Verdict

RECN-01, RECN-02, and RECN-03 are now **satisfied**. All three sources agree: REQUIREMENTS.md checkboxes are `[x]`, VERIFICATION.md passes, and SUMMARY frontmatter uses `requirements-completed:` with the correct IDs listed.

Parity test evidence: `mix test test/crosswake/planning/summary_frontmatter_test.exs` → 2 tests, 0 failures.
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Append `reaudits:` frontmatter list and `## Re-Audit (Phase 24)` body section to `.planning/v3.2-MILESTONE-AUDIT.md`</name>
  <read_first>
    - .planning/v3.2-MILESTONE-AUDIT.md (the file to modify — read fully; frontmatter ends line 85, body ends line 187 with the `## Recommendation` section)
    - .planning/phases/24-reconciliation-traceability-hardening/24-CONTEXT.md (D-07: append `reaudits:` to frontmatter; leave `status: gaps_found` UNCHANGED; add `## Re-Audit (Phase 24)` body section at EOF; D-08: VERIFICATION.md cites this section, does not duplicate body)
    - .planning/phases/24-reconciliation-traceability-hardening/24-PATTERNS.md (Mutation 1 and Mutation 2 for v3.2-MILESTONE-AUDIT.md show exact YAML and markdown to append)
    - .planning/phases/24-reconciliation-traceability-hardening/24-RESEARCH.md (Finding 4 shows current frontmatter shape, body shape, and exact placement of new section; Pitfall 3 warns against mutating `status: gaps_found`)
    - .planning/phases/24-reconciliation-traceability-hardening/24-01-SUMMARY.md (must exist — Plan 24-01 closed the artifact-shape gap)
    - .planning/phases/24-reconciliation-traceability-hardening/24-02-SUMMARY.md (must exist — Plan 24-02 committed the parity test and CI amendment)
  </read_first>
  <files>
    - .planning/v3.2-MILESTONE-AUDIT.md
  </files>
  <action>
    Two append-only edits to `.planning/v3.2-MILESTONE-AUDIT.md`. The audit document is append-only per D-01 / D-07 (mirrors Phase 21 D-04 append-only normalized evidence events). Sequence the edits as below:

    **Edit A — Frontmatter append (per D-07, RESEARCH Finding 4 (a) and (e), PATTERNS Mutation 1):**

    Locate the `nyquist:` block (currently the last frontmatter key, lines 76-84 ending with `  overall: "partial"` on line 84). Immediately after line 84 (after `overall: "partial"`) and BEFORE the closing `---` on line 85, insert a new top-level frontmatter key `reaudits:` as a YAML list with exactly one entry. The entry must use this exact shape (top-level keys: `phase`, `date`, `current_status`, `evidence`):

    ```yaml
    reaudits:
      - phase: 24
        date: "2026-05-27"
        current_status: "satisfied"
        evidence:
          - "21-01-SUMMARY.md: requirements → requirements-completed (Plan 24-01 Task 1)"
          - "21-02-SUMMARY.md: requirements → requirements-completed (Plan 24-01 Task 1)"
          - "REQUIREMENTS.md: RECN-01/02/03 bullets flipped to [x] (Plan 24-01 Task 2)"
          - "REQUIREMENTS.md: traceability table updated with Phase 21 + Phase 24 (Plan 24-01 Task 2)"
          - "Parity test: test/crosswake/planning/summary_frontmatter_test.exs (Plan 24-02 Task 1; passes 2 tests, 0 failures)"
          - "CI merge gate: .github/workflows/phase23-proof.yml merge-blocking-commerce-proof job includes the parity test step (Plan 24-02 Task 2)"
    ```

    **IMMUTABLE — DO NOT CHANGE (Footgun 2, Pitfall 3):**
    - The top-level `status: gaps_found` field on line 4. Top-level `status:` is the original verdict-at-time; flipping it destroys the audit chain. All derived current state lives in `reaudits[N].current_status`.
    - The `audited:` timestamp on line 3 — historical fact, never updated.
    - The `gaps:`, `tech_debt:`, `nyquist:`, and `scores:` blocks — historical record, never mutated. Future re-audits append entries to `reaudits:`; they do not overwrite prior gap snapshots.

    **Edit B — Body append (per D-07, RESEARCH Finding 4 (d), PATTERNS Mutation 2):**

    Locate the end-of-file. The file currently ends with the `## Recommendation` H2 section, whose last content line is `/gsd-plan-milestone-gaps`. Append a blank line, then the new H2 section:

    ```markdown

    ## Re-Audit (Phase 24)

    **Re-audited:** 2026-05-27
    **Trigger:** Phase 21 SUMMARY frontmatter key normalization (`requirements:` → `requirements-completed:`)
    **Scope:** RECN-01, RECN-02, RECN-03 only

    ### Three-Source Cross-Check: RECN-01/02/03

    | Requirement | REQUIREMENTS.md | VERIFICATION.md | SUMMARY frontmatter (`requirements-completed:`) | Final status |
    |-------------|-----------------|-----------------|------------------------------------------------|--------------|
    | RECN-01 | before: `[ ]` → after: `[x]` | passed (Phase 21) — unchanged | before: missing (`requirements:` key) → after: listed (`requirements-completed:`) | **satisfied** |
    | RECN-02 | before: `[ ]` → after: `[x]` | passed (Phase 21) — unchanged | before: missing (`requirements:` key) → after: listed (`requirements-completed:`) | **satisfied** |
    | RECN-03 | before: `[ ]` → after: `[x]` | passed (Phase 21) — unchanged | before: missing (`requirements:` key) → after: listed (`requirements-completed:`) | **satisfied** |

    ### What Changed

    - `.planning/phases/21-reconciliation-example/21-01-SUMMARY.md`: `requirements:` key renamed to `requirements-completed:`. List contents (RECN-01/02/03) preserved exactly.
    - `.planning/phases/21-reconciliation-example/21-02-SUMMARY.md`: Same rename.
    - `.planning/REQUIREMENTS.md`: RECN-01/02/03 bullets flipped from `[ ]` to `[x]`. Traceability table cells updated from `Phase 24 / Pending` to `Phase 21 (validated); Phase 24 (traceability normalized) / Complete`.
    - `test/crosswake/planning/summary_frontmatter_test.exs`: New merge-blocking parity test. Passes against all 13 `.planning/phases/*/*-SUMMARY.md` files.
    - `.github/workflows/phase23-proof.yml`: Merge-blocking job amended to include the new parity test path.

    ### Original Gap (from initial audit)

    The initial audit found RECN-01/02/03 as `partial` because Phase 21's two SUMMARY files used `requirements:` instead of the canonical `requirements-completed:` key, preventing the cross-check from detecting completion. Phase 21's `21-VERIFICATION.md` was already green and the implementation was fully verified — the gap was purely artifact-shape inconsistency.

    ### Re-Audit Verdict

    RECN-01, RECN-02, and RECN-03 are now **satisfied**. All three sources agree: REQUIREMENTS.md checkboxes are `[x]`, VERIFICATION.md passes, and SUMMARY frontmatter uses `requirements-completed:` with the correct IDs listed.

    Parity test evidence: `mix test test/crosswake/planning/summary_frontmatter_test.exs` → 2 tests, 0 failures.
    ```

    Per D-06: Do NOT add narrative prose explaining the validated-in-21-normalized-in-24 distinction beyond what the table and "Original Gap" paragraph already capture. Resist the urge to add a "Why this matters" paragraph — the structured cross-check is the evidence.

    Per Footgun 4: This task mutates exactly one file (`.planning/v3.2-MILESTONE-AUDIT.md`). Do NOT touch `lib/`, `examples/`, `test/`, or any other CI workflow.

    **Self-check sequence before declaring done:**

    1. Verify `status: gaps_found` still appears on line 4 (`grep -n '^status: gaps_found$' .planning/v3.2-MILESTONE-AUDIT.md` returns `4:status: gaps_found`).
    2. Verify `reaudits:` is now a top-level frontmatter key (`grep -q '^reaudits:$' .planning/v3.2-MILESTONE-AUDIT.md`).
    3. Verify the new `## Re-Audit (Phase 24)` body section exists (`grep -q '^## Re-Audit (Phase 24)$' .planning/v3.2-MILESTONE-AUDIT.md`).
    4. Verify `## Recommendation` section is still intact and precedes the new section.
  </action>
  <verify>
    <automated>grep -q '^## Re-Audit (Phase 24)$' .planning/v3.2-MILESTONE-AUDIT.md &amp;&amp; grep -q '^status: gaps_found$' .planning/v3.2-MILESTONE-AUDIT.md &amp;&amp; grep -q '^reaudits:$' .planning/v3.2-MILESTONE-AUDIT.md &amp;&amp; grep -q 'current_status: "satisfied"' .planning/v3.2-MILESTONE-AUDIT.md &amp;&amp; grep -q 'test/crosswake/planning/summary_frontmatter_test.exs' .planning/v3.2-MILESTONE-AUDIT.md &amp;&amp; awk '/^## Recommendation$/{rec=NR} /^## Re-Audit \(Phase 24\)$/{rea=NR} END{exit !(rec &amp;&amp; rea &amp;&amp; rea &gt; rec)}' .planning/v3.2-MILESTONE-AUDIT.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q '^## Re-Audit (Phase 24)$' .planning/v3.2-MILESTONE-AUDIT.md` exits 0 (new body section present)
    - `grep -q '^status: gaps_found$' .planning/v3.2-MILESTONE-AUDIT.md` exits 0 (original verdict UNCHANGED — Footgun 2)
    - `grep -q '^reaudits:$' .planning/v3.2-MILESTONE-AUDIT.md` exits 0 (frontmatter key added)
    - `grep -q 'current_status: "satisfied"' .planning/v3.2-MILESTONE-AUDIT.md` exits 0 (re-audit entry verdict present)
    - `grep -q 'phase: 24' .planning/v3.2-MILESTONE-AUDIT.md` exits 0 (re-audit entry phase number present)
    - `grep -q 'test/crosswake/planning/summary_frontmatter_test.exs' .planning/v3.2-MILESTONE-AUDIT.md` exits 0 (parity test path cited in evidence)
    - New section follows `## Recommendation` (positional check): `awk '/^## Recommendation$/{rec=NR} /^## Re-Audit \(Phase 24\)$/{rea=NR} END{exit !(rec && rea && rea > rec)}' .planning/v3.2-MILESTONE-AUDIT.md` exits 0
    - No other `## Re-Audit` sections present (only this one): `grep -c '^## Re-Audit' .planning/v3.2-MILESTONE-AUDIT.md` returns 1
    - Original frontmatter keys still present and not duplicated: `grep -c '^milestone: v3.2$' .planning/v3.2-MILESTONE-AUDIT.md` returns 1; `grep -c '^audited: ' .planning/v3.2-MILESTONE-AUDIT.md` returns 1; `grep -c '^status: gaps_found$' .planning/v3.2-MILESTONE-AUDIT.md` returns 1
    - Cross-check table has all three RECN rows with `**satisfied**`: `grep -c '| RECN-0[123] .* | \*\*satisfied\*\* |' .planning/v3.2-MILESTONE-AUDIT.md` returns 3 (only the new table cells match this exact `**satisfied**` shape; existing table rows use `satisfied` without bold)
    - No other files modified: `git diff --name-only | grep -v 'v3.2-MILESTONE-AUDIT.md' | wc -l` returns 0 (only the audit doc changed in this task's diff scope)
  </acceptance_criteria>
  <done>
    `.planning/v3.2-MILESTONE-AUDIT.md` has (a) a new top-level `reaudits:` YAML list key in frontmatter with one entry (phase 24, satisfied, evidence citing the closed gaps and parity test path), (b) the original `status: gaps_found` field unchanged, and (c) a new `## Re-Audit (Phase 24)` H2 section at end-of-file containing the three-source cross-check table, "What Changed" list, "Original Gap" paragraph, and "Re-Audit Verdict" section. No other files modified.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| audit doc → milestone archive automation | Milestone-archive automation reads the audit doc to determine readiness. Mutating `status: gaps_found` would falsely signal readiness; appending to `reaudits:` preserves the historical chain. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-07 | Tampering | Top-level `status: gaps_found` field | mitigate | Acceptance criterion `grep -q '^status: gaps_found$' .planning/v3.2-MILESTONE-AUDIT.md` exits 0 post-edit, ensuring the original verdict is preserved (D-07, Footgun 2). |
| T-24-08 | Tampering | Duplicate `## Re-Audit` sections | mitigate | Acceptance criterion `grep -c '^## Re-Audit' .planning/v3.2-MILESTONE-AUDIT.md` returns 1 (exactly one re-audit section). |
| T-24-09 | Repudiation | Re-audit entry without provenance | mitigate | Each evidence list item names the Plan and Task that produced the artifact change (e.g., "Plan 24-01 Task 1"), enabling git-log traceback. |
| T-24-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan. Rationale: markdown append to one planning artifact. |

**Severity:** low — markdown-only append to a single planning artifact. No auth/session/crypto/API/CI surface.
</threat_model>

<verification>
After Task 1 completes:

1. `grep -q '^## Re-Audit (Phase 24)$' .planning/v3.2-MILESTONE-AUDIT.md` exits 0.
2. `grep -q '^status: gaps_found$' .planning/v3.2-MILESTONE-AUDIT.md` exits 0 (IMMUTABLE per Footgun 2).
3. `grep -q '^reaudits:$' .planning/v3.2-MILESTONE-AUDIT.md` exits 0.
4. Cross-check table contains three `**satisfied**` rows for RECN-01/02/03.
5. New `## Re-Audit (Phase 24)` section comes AFTER `## Recommendation` section (positional awk check).
6. `git diff --stat` reports exactly 1 file changed: `.planning/v3.2-MILESTONE-AUDIT.md`.
7. Plan 24-01 and Plan 24-02 verifications still pass: re-running `mix test test/crosswake/planning/summary_frontmatter_test.exs` exits 0 (the parity test is unaffected by audit-doc edits — verifies the artifact-shape chain is intact end-to-end).
</verification>

<success_criteria>
- `.planning/v3.2-MILESTONE-AUDIT.md` frontmatter has a new `reaudits:` YAML list with one entry: `phase: 24`, `date: "2026-05-27"`, `current_status: "satisfied"`, and an evidence list citing the closed-gap commits and the parity test path.
- The original top-level `status: gaps_found` field is preserved verbatim.
- A new `## Re-Audit (Phase 24)` H2 section appears at end-of-file (after `## Recommendation`), containing a three-source cross-check table with before/after diff cells for RECN-01/02/03, a "What Changed" list, an "Original Gap" paragraph, and a "Re-Audit Verdict" section.
- The Phase 24 parity test (`mix test test/crosswake/planning/summary_frontmatter_test.exs`) still passes — the audit-doc edit must not invalidate the artifact-shape chain.
- Plan touches only the one file declared in `files_modified`; no `lib/`, `examples/`, `test/`, or other workflow file modified.
</success_criteria>

<output>
Create `.planning/phases/24-reconciliation-traceability-hardening/24-03-SUMMARY.md` when done. Frontmatter MUST use `requirements-completed:` listing RECN-01, RECN-02, RECN-03 (the parity test now enforces this). The SUMMARY should record the append-only audit-doc shape and cite the canonical re-audit section, per D-08 (24-VERIFICATION.md will later cite this section as canonical re-audit evidence).
</output>
