---
phase: 25-address-tech-debt-phase-20-verification-text-phase-24-parity
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/20-entitlement-lifecycle-semantics/20-VERIFICATION.md
autonomous: true
requirements: []

must_haves:
  truths:
    - "20-VERIFICATION.md no longer contains the contradictory trailing sentence at line 63."
    - "Line 61 (`Phase 20 goal is achieved...`) is byte-identical to its pre-edit state."
    - "The `## Final Determination` heading and every other line of 20-VERIFICATION.md is byte-identical to its pre-edit state."
    - "After the delete, the `## Final Determination` section is internally consistent: one final-determination sentence (the achieved-line at :61), no contradiction."
  artifacts:
    - path: ".planning/phases/20-entitlement-lifecycle-semantics/20-VERIFICATION.md"
      provides: "Phase 20 verification artifact with contradictory trailing sentence removed."
      contains: "Phase 20 goal is achieved. All required ENTL-01, ENTL-02, and ENTL-03 semantics"
    - path: ".planning/phases/25-address-tech-debt-phase-20-verification-text-phase-24-parity/25-01-SUMMARY.md"
      provides: "Plan 25-01 summary declaring requirements-completed: [] per D-04 (created by 25-02 in the same commit as the test hardening — see D-10)."
      contains: "requirements-completed: []"
  key_links:
    - from: ".planning/phases/20-entitlement-lifecycle-semantics/20-VERIFICATION.md (line 61, post-edit)"
      to: ".planning/v3.2-MILESTONE-AUDIT.md §Tech Debt (Non-Blocking) — Phase 20 text-fix item"
      via: "audit verdict text-fix scope (`drop the trailing sentence`)"
      pattern: "Phase 20 goal is achieved\\."
---

<objective>
Close the Phase 20 VERIFICATION.md doc-debt item flagged in `.planning/v3.2-MILESTONE-AUDIT.md` (Tech Debt — Non-Blocking) by deleting the single contradictory trailing sentence at line 63 of `20-VERIFICATION.md`. No other edits to that file. No edits to any other file in this plan. The Phase 25 SUMMARY for this plan is created downstream in 25-02-PLAN.md so that the new parity-test presence assertion (D-03) and its required input (`requirements-completed: []` in `25-01-SUMMARY.md`) land in the same commit (D-10 atomic-commit constraint).

Purpose: The contradictory sentence at :63 conflicts with the final-determination line at :61 ("Phase 20 goal is achieved...") even though Phase 20's runtime behavior is correct. The audit's stated minimal fix is "drop the trailing sentence" (CONTEXT.md D-02, Footgun 5). Behavior unchanged; planning-corpus text becomes self-consistent.

Output:
- `.planning/phases/20-entitlement-lifecycle-semantics/20-VERIFICATION.md` with exactly line 63 deleted; every other line byte-identical.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/v3.2-MILESTONE-AUDIT.md
@.planning/phases/25-address-tech-debt-phase-20-verification-text-phase-24-parity/25-CONTEXT.md
@.planning/phases/25-address-tech-debt-phase-20-verification-text-phase-24-parity/25-PATTERNS.md
@.planning/phases/20-entitlement-lifecycle-semantics/20-VERIFICATION.md

<interfaces>
<!-- Pre-edit lines 59–63 of 20-VERIFICATION.md, copied verbatim from the file. Executor uses these directly to confirm pre-state before editing. -->

```
## Final Determination

Phase 20 goal is achieved. All required ENTL-01, ENTL-02, and ENTL-03 semantics are implemented with runtime fail-closed evidence-source enforcement and regression coverage.

Phase 20 goal is mostly implemented, but not fully achieved due to unresolved ENTL-03 runtime fail-closed enforcement gap for invalid evidence source values.
```

Line 59 = `## Final Determination`
Line 60 = blank
Line 61 = `Phase 20 goal is achieved. All required ENTL-01, ENTL-02, and ENTL-03 semantics are implemented with runtime fail-closed evidence-source enforcement and regression coverage.`
Line 62 = blank
Line 63 = `Phase 20 goal is mostly implemented, but not fully achieved due to unresolved ENTL-03 runtime fail-closed enforcement gap for invalid evidence source values.`
Line 64 = trailing blank (file ends here)

**Post-edit expected final region (lines 59–61, end of file):**

```
## Final Determination

Phase 20 goal is achieved. All required ENTL-01, ENTL-02, and ENTL-03 semantics are implemented with runtime fail-closed evidence-source enforcement and regression coverage.
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete the contradictory trailing sentence at 20-VERIFICATION.md:63</name>
  <files>.planning/phases/20-entitlement-lifecycle-semantics/20-VERIFICATION.md</files>
  <read_first>
    - .planning/phases/20-entitlement-lifecycle-semantics/20-VERIFICATION.md (full file; confirm current state of lines 59–64 matches the pre-edit shape in this plan's `<interfaces>` block before touching anything)
    - .planning/phases/25-address-tech-debt-phase-20-verification-text-phase-24-parity/25-CONTEXT.md (D-02 minimal-scope rule; Footgun 5 — no improvements to line 61, no retitle of the heading, no closing paragraph)
    - .planning/v3.2-MILESTONE-AUDIT.md (Tech Debt — Non-Blocking section confirming the stated fix is "drop the trailing sentence")
  </read_first>
  <action>
    Per D-02 and Footgun 5: delete exactly the contradictory final-determination sentence currently at line 63 ("Phase 20 goal is mostly implemented, but not fully achieved due to unresolved ENTL-03 runtime fail-closed enforcement gap for invalid evidence source values."). Use the Edit tool with `old_string` equal to the full line 63 text plus the leading newline that joins it to the blank line 62, so that after the edit the file ends with line 61 followed by exactly one trailing newline — matching the existing file-end convention of other VERIFICATION artifacts in this corpus. Do NOT rewrite line 61. Do NOT modify the `## Final Determination` heading. Do NOT add a paragraph explaining the deletion. Do NOT add a commit-message footnote disguised as artifact prose. Do NOT touch any other section of the file (Verification Steps, Findings, frontmatter, etc.). The edit is a pure single-sentence deletion with no narrative replacement.
  </action>
  <verify>
    <automated>grep -c "Phase 20 goal is mostly implemented, but not fully achieved" .planning/phases/20-entitlement-lifecycle-semantics/20-VERIFICATION.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "Phase 20 goal is mostly implemented, but not fully achieved" .planning/phases/20-entitlement-lifecycle-semantics/20-VERIFICATION.md` returns exactly `0` (the contradictory sentence is gone from anywhere in the file).
    - `grep -c "Phase 20 goal is achieved\. All required ENTL-01, ENTL-02, and ENTL-03 semantics are implemented" .planning/phases/20-entitlement-lifecycle-semantics/20-VERIFICATION.md` returns exactly `1` (the kept final-determination sentence remains, byte-identical).
    - `grep -c "^## Final Determination$" .planning/phases/20-entitlement-lifecycle-semantics/20-VERIFICATION.md` returns exactly `1` (heading unchanged).
    - `git diff --stat .planning/phases/20-entitlement-lifecycle-semantics/20-VERIFICATION.md` shows `1 file changed, 0 insertions(+), 2 deletions(-)` (the contradictory sentence line plus its preceding blank — or `2 deletions` total; if the executor instead deletes only the sentence and leaves the blank, `1 deletion(-)` is also acceptable as long as the file does not end with two trailing blank lines).
    - `git diff .planning/phases/20-entitlement-lifecycle-semantics/20-VERIFICATION.md` shows ONLY removed lines, no added lines (pure deletion, no rewrite, no insertion).
    - The post-edit file ends with the achieved-sentence followed by exactly one trailing newline; `tail -3 .planning/phases/20-entitlement-lifecycle-semantics/20-VERIFICATION.md` shows the achieved sentence as the final non-blank content.
  </acceptance_criteria>
  <done>
    Line 63's contradictory sentence is removed from 20-VERIFICATION.md. Line 61 and all other content are byte-identical to pre-edit. `## Final Determination` section is internally consistent with exactly one final-determination sentence. The audit's `status: gaps_found` verdict is NOT touched (out of scope per CONTEXT.md canonical_refs).
  </done>
</task>

<task type="auto">
  <name>Task 2: Commit the single-line delete with a Conventional Commits message</name>
  <files>.planning/phases/20-entitlement-lifecycle-semantics/20-VERIFICATION.md</files>
  <read_first>
    - .planning/phases/25-address-tech-debt-phase-20-verification-text-phase-24-parity/25-CONTEXT.md (D-10 — plan 25-01 is independent and lands in its OWN commit; D-11 — natural order is 25-01 first, 25-02 second)
    - Last 10 commits on `main` (`git log --oneline -10`) to confirm Conventional Commits style and the Phase-25 commit-message conventions used in 24-* commits (the only canonical-style precedent for this milestone).
  </read_first>
  <action>
    Stage ONLY `.planning/phases/20-entitlement-lifecycle-semantics/20-VERIFICATION.md` (do NOT pass `-A` or `.`; specify the path explicitly per CRITICAL git-safety rules). Commit with a Conventional Commits message that names Phase 20 doc-debt scope and references the audit. Suggested form: `docs(phase-20): drop contradictory final-determination sentence`. Body (optional, one paragraph max) may cite `v3.2-MILESTONE-AUDIT.md §Tech Debt (Non-Blocking)` and CONTEXT.md D-02. Do NOT amend any prior commit. Do NOT skip hooks. The 25-01 SUMMARY is intentionally NOT created in this commit — it ships with 25-02's atomic test+SUMMARY commit (CONTEXT.md D-10, Footgun 1).
  </action>
  <verify>
    <automated>git log -1 --pretty=format:'%s' | grep -E '^docs\(phase-20\)' && git diff HEAD~1 HEAD --stat | grep -E '20-VERIFICATION\.md.*\| *[12] '</automated>
  </verify>
  <acceptance_criteria>
    - `git log -1 --name-only --pretty=format:''` lists exactly one path: `.planning/phases/20-entitlement-lifecycle-semantics/20-VERIFICATION.md` (no other files in this commit — proves D-10 plan-independence).
    - `git log -1 --pretty=format:'%s'` matches the regex `^docs\(phase-20\):` (Conventional Commits scope is `phase-20`, type is `docs`).
    - `git status` shows a clean working tree (no untracked/uncommitted residue from this task).
    - The commit subject does NOT include scope-creep words like `feat`, `fix(lib`, `commerce`, or any path under `lib/crosswake/`.
  </acceptance_criteria>
  <done>
    One commit on `main` (or current working branch) contains the single-line delete to `20-VERIFICATION.md` and nothing else. The commit message follows Conventional Commits with `docs(phase-20):` scope. The Phase 25 plan is now ready for 25-02 to ship the parity-test hardening + both 25-* SUMMARYs in its own atomic commit.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Phase 25 planner intent → 20-VERIFICATION.md byte state | Doc-debt fix is "delete one sentence"; any drift toward "improve line 61", "rewrite the section", or "add explanatory prose" is silent scope creep across this boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25-01-T01 | Tampering | 20-VERIFICATION.md line 61 (final-determination kept sentence) | mitigate | Acceptance criteria asserts `grep -c "Phase 20 goal is achieved\. All required ENTL-01, ENTL-02, and ENTL-03 semantics are implemented"` returns exactly 1; `git diff` shows ZERO added lines. Pure-deletion invariant catches accidental rewrites. |
| T-25-01-I01 | Information Disclosure | v3.2-MILESTONE-AUDIT.md `status: gaps_found` verdict | accept | Plan 25-01 explicitly does not touch the audit verdict. Verdict-at-time is historical fact per Phase 24 D-07 (append-only re-audit pattern). No mitigation needed because no edit is performed. |
| T-25-01-D01 | Denial of Service (silent scope creep) | Files under `lib/crosswake/`, `examples/`, `.github/workflows/`, commerce code | mitigate | Acceptance criteria for Task 2 asserts `git log -1 --name-only` shows exactly one path. Footgun 4 in CONTEXT.md explicitly forbids any change outside the named files. |
</threat_model>

<verification>
- `grep -c "Phase 20 goal is mostly implemented, but not fully achieved" .planning/phases/20-entitlement-lifecycle-semantics/20-VERIFICATION.md` returns `0`.
- `grep -c "Phase 20 goal is achieved\. All required ENTL-01, ENTL-02, and ENTL-03 semantics are implemented" .planning/phases/20-entitlement-lifecycle-semantics/20-VERIFICATION.md` returns `1`.
- `git log -1 --name-only --pretty=format:''` lists exactly `.planning/phases/20-entitlement-lifecycle-semantics/20-VERIFICATION.md`.
- `git diff HEAD~1 HEAD` for the commit shows only deletions, no additions.
- Plan 25-02 will subsequently create `25-01-SUMMARY.md` with `requirements-completed: []` in its atomic commit per D-10 / Footgun 1; this plan does NOT create that SUMMARY.
</verification>

<success_criteria>
- 20-VERIFICATION.md line 63 contradictory sentence deleted; line 61 byte-identical.
- One Conventional Commits commit (`docs(phase-20):`) touches exactly one file.
- No scope creep into `lib/crosswake/`, `examples/`, `.github/workflows/`, commerce code, or any other phase artifact (Footgun 4).
- The audit's `gaps_found` verdict is not flipped (out of scope per CONTEXT.md canonical_refs).
</success_criteria>

<output>
Create `.planning/phases/25-address-tech-debt-phase-20-verification-text-phase-24-parity/25-01-SUMMARY.md` only as part of plan **25-02**'s atomic commit (D-10 / Footgun 1). Do NOT create the 25-01 SUMMARY in this plan; doing so would either (a) split the atomic commit constraint, or (b) leave the new parity-test presence assertion failing on 25-02-SUMMARY.md's absence at the moment 25-02 ships. The 25-02 plan is the canonical place for both 25-01-SUMMARY.md and 25-02-SUMMARY.md.
</output>
