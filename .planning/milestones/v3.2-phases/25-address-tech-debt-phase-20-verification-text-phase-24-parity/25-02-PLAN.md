---
phase: 25-address-tech-debt-phase-20-verification-text-phase-24-parity
plan: 02
type: execute
wave: 2
depends_on:
  - 25-01
files_modified:
  - test/crosswake/planning/summary_frontmatter_test.exs
  - .planning/phases/25-address-tech-debt-phase-20-verification-text-phase-24-parity/25-01-SUMMARY.md
  - .planning/phases/25-address-tech-debt-phase-20-verification-text-phase-24-parity/25-02-SUMMARY.md
autonomous: true
requirements: []

must_haves:
  truths:
    - "summary_frontmatter_test.exs declares a third test that asserts every `.planning/phases/*/*-SUMMARY.md` declares a parseable `requirements-completed:` key (WR-01 fix per D-03)."
    - "`extract_completed_ids/1` raises with a path-naming message when `requirements-completed:` is present but neither the inline `[..]` nor the multi-line `\\n  - X` shape matches (WR-02 fix per D-06, raise lives in the helper per D-07)."
    - "Empty inline `requirements-completed: []` parses cleanly via the existing inline arm — does NOT trigger the new raise (Footgun 2 invariant)."
    - "summary_frontmatter_test.exs contains the literal compile-time attribute `@requirements_path Path.join(File.cwd!(), \".planning/REQUIREMENTS.md\")` and `parse_requirement_ids_from_requirements_md/0` reads from it (IN-01 fix per D-08)."
    - "The REQUIREMENTS.md bullet regex character class is `[xX ]` exactly (IN-02 fix per D-09)."
    - "25-01-SUMMARY.md exists and contains the literal line `requirements-completed: []` (D-04, canonical tech-debt shape)."
    - "25-02-SUMMARY.md exists and contains the literal line `requirements-completed: []` (D-04, canonical tech-debt shape)."
    - "ALL four changes (the strengthened test file + both 25-* SUMMARYs) ship in ONE atomic git commit. Committing the test alone would fail the new presence assertion on Phase 25's own un-shipped SUMMARYs (Footgun 1, D-10). Loud constraint, not an aside."
    - "Pre-commit discovery survey: `mix test test/crosswake/planning/summary_frontmatter_test.exs` passes against the full corpus (16 existing SUMMARYs + Phase 25's 2 new SUMMARYs = 18) BEFORE the commit is created. If any existing SUMMARY fails, the planner backfills in the same commit OR adds an explicit phase-number floor allowlist with an inline comment — silent weakening of the assertion is forbidden (Footgun 3, D-12)."
  artifacts:
    - path: "test/crosswake/planning/summary_frontmatter_test.exs"
      provides: "Hardened parity test: third presence assertion (WR-01), loud-fail helper (WR-02), compile-time @requirements_path (IN-01), [xX ] checkbox regex (IN-02)."
      exports: ["test \"every phase summary declares requirements-completed:\"", "@requirements_path", "extract_completed_ids/1 raise branch"]
    - path: ".planning/phases/25-address-tech-debt-phase-20-verification-text-phase-24-parity/25-01-SUMMARY.md"
      provides: "Plan 25-01 summary declaring requirements-completed: [] (D-04 canonical tech-debt shape)."
      contains: "requirements-completed: []"
    - path: ".planning/phases/25-address-tech-debt-phase-20-verification-text-phase-24-parity/25-02-SUMMARY.md"
      provides: "Plan 25-02 summary declaring requirements-completed: [] (D-04 canonical tech-debt shape)."
      contains: "requirements-completed: []"
  key_links:
    - from: "test/crosswake/planning/summary_frontmatter_test.exs (third test, WR-01)"
      to: ".planning/phases/25-address-tech-debt-phase-20-verification-text-phase-24-parity/25-01-SUMMARY.md AND 25-02-SUMMARY.md"
      via: "Path.wildcard(@summary_glob) iteration — both 25-* SUMMARYs MUST be present in the same commit as the test change"
      pattern: "requirements-completed:[ \\t]*(?:\\[|\\r?\\n[ \\t]+-)"
    - from: "test/crosswake/planning/summary_frontmatter_test.exs (`extract_completed_ids/1` raise branch, WR-02)"
      to: "summary frontmatter shapes (inline `[..]`, multi-line `\\n  - X`, bare-key, indentation-broken)"
      via: "third cond arm: `Regex.match?(~r/^requirements-completed:/m, frontmatter) -> raise`"
      pattern: "Regex.match\\?\\(~r/\\^requirements-completed:/m"
    - from: "test/crosswake/planning/summary_frontmatter_test.exs (`@requirements_path`, IN-01)"
      to: ".planning/REQUIREMENTS.md"
      via: "compile-time `Path.join(File.cwd!(), \".planning/REQUIREMENTS.md\")` module attribute, symmetric with existing `@summary_glob`"
      pattern: "@requirements_path Path\\.join\\(File\\.cwd!\\(\\), \"\\.planning/REQUIREMENTS\\.md\"\\)"
    - from: "test/crosswake/planning/summary_frontmatter_test.exs (`parse_requirement_ids_from_requirements_md/0`, IN-02)"
      to: ".planning/REQUIREMENTS.md bullets (case-tolerant)"
      via: "widened character class `[xX ]` in `Regex.scan(~r/- \\[[xX ]\\] \\*\\*([A-Z]+-\\d+)\\*\\*/, content)`"
      pattern: "- \\\\\\[\\[xX \\]\\\\\\]"
---

<objective>
Harden `test/crosswake/planning/summary_frontmatter_test.exs` to close the two advisory warnings (WR-01, WR-02) and two info items (IN-01, IN-02) carried forward from `24-REVIEW.md`, and create both Phase 25 SUMMARYs (`25-01-SUMMARY.md`, `25-02-SUMMARY.md`) — all in a SINGLE atomic git commit (CONTEXT.md D-10, Footgun 1). Run a pre-commit discovery survey against the full SUMMARY corpus (Footgun 3, D-12) before the commit is created.

Purpose: WR-01 (missing-key escape hatch) and WR-02 (un-indented bullets silently extract `[]`) are two distinct silent-acceptance paths through the parity guard. Closing both — plus the IN-01 cwd-symmetry and IN-02 `[xX ]` case-tolerance fixes that ride in the same file edit — applies the "loud, not silent" thesis from `24-REVIEW.md` symmetrically across the helper. Phase 25's own two SUMMARYs declare `requirements-completed: []` (D-04, the canonical tech-debt shape), so the new presence assertion passes against Phase 25's own artifacts in the same commit.

**ATOMIC COMMIT CONSTRAINT (LOUD — Footgun 1):** The hardened test introduces a new assertion that every `.planning/phases/*/*-SUMMARY.md` declare `requirements-completed:`. Phase 25's two SUMMARYs are themselves new SUMMARYs that will be glob-walked by the new assertion. Committing the test alone would FAIL the new presence assertion on `25-01-SUMMARY.md` and `25-02-SUMMARY.md` (which would not yet exist or would lack the key). Committing the SUMMARYs alone is benign but leaves the contract un-enforced for one commit. The only correctness-preserving sequence is: write test edits + write both SUMMARYs + run `mix test` to confirm pass + create ONE commit containing all three files. No two-step staging.

Output:
- Hardened `test/crosswake/planning/summary_frontmatter_test.exs` (WR-01 third test, WR-02 helper raise, IN-01 `@requirements_path`, IN-02 `[xX ]`).
- `.planning/phases/25-address-tech-debt-phase-20-verification-text-phase-24-parity/25-01-SUMMARY.md` (new, `requirements-completed: []`).
- `.planning/phases/25-address-tech-debt-phase-20-verification-text-phase-24-parity/25-02-SUMMARY.md` (new, `requirements-completed: []`).
- ONE atomic commit containing all three files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-address-tech-debt-phase-20-verification-text-phase-24-parity/25-CONTEXT.md
@.planning/phases/25-address-tech-debt-phase-20-verification-text-phase-24-parity/25-PATTERNS.md
@.planning/phases/24-reconciliation-traceability-hardening/24-REVIEW.md
@.planning/phases/24-reconciliation-traceability-hardening/24-CONTEXT.md
@.planning/phases/24-reconciliation-traceability-hardening/24-VERIFICATION.md
@.planning/phases/23-commerce-support-and-proof-closure/23-CONTEXT.md
@test/crosswake/planning/summary_frontmatter_test.exs
@.planning/phases/19-commerce-route-corridors/19-01-SUMMARY.md
@.planning/phases/24-reconciliation-traceability-hardening/24-01-SUMMARY.md

<interfaces>
<!-- Pre-edit shape of `summary_frontmatter_test.exs` (current 95-line file). -->
<!-- Executor uses these locators to apply edits precisely; do NOT re-derive line numbers from the file — use these as the source of truth for current shape and the 25-PATTERNS.md "Four changes to make" block for post-edit shape. -->

Current module attribute at line 4:
`@summary_glob Path.join(File.cwd!(), ".planning/phases/*/*-SUMMARY.md")`

Current `extract_completed_ids/1` third arm at lines 73–74:
`      true ->`
`        []`

Current `parse_requirement_ids_from_requirements_md/0` at lines 87–94 — uses runtime `Path.join(File.cwd!(), ".planning/REQUIREMENTS.md")` at line 88 and regex `~r/- \[[x ]\] \*\*([A-Z]+-\d+)\*\*/` at line 91.

Test 2 ends at line 32; the new third test inserts AFTER test 2 and BEFORE the first `defp` (i.e., between line 32 and the comment block starting at line 34).

<!-- Canonical fix snippets (read from 24-REVIEW.md lines 49–63 for WR-01 and 76–92 for WR-02). 25-PATTERNS.md "Four changes to make" block contains the exact code; executor reproduces it verbatim with two exceptions: (1) preserve the existing two-line comment style above each helper, (2) the new test description string wording is at Claude's discretion per CONTEXT.md as long as the assertion-message regex below is satisfied. -->

<!-- Canonical 25-* SUMMARY frontmatter shape (from 25-PATTERNS.md, derived from 19-01-SUMMARY.md analog). -->

For 25-01-SUMMARY.md frontmatter (verbatim shape):
```
---
phase: 25-address-tech-debt-phase-20-verification-text-phase-24-parity
plan: 01
subsystem: planning-artifacts
tags: [tech-debt, doc-fix, verification-text]
requirements-completed: []
---
```

For 25-02-SUMMARY.md frontmatter (verbatim shape):
```
---
phase: 25-address-tech-debt-phase-20-verification-text-phase-24-parity
plan: 02
subsystem: planning-artifacts
tags: [tech-debt, parity-test, hardening, wr-01, wr-02]
requirements-completed: []
---
```

The SUMMARY body (after the closing `---`) follows the template at `$HOME/.claude/get-shit-done/templates/summary.md` and includes a `# Phase 25 Plan NN: ... Summary` heading. Body content is at executor discretion as long as the frontmatter shape above is byte-exact and the file passes the new parity-test presence assertion.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Apply all four edits to summary_frontmatter_test.exs (WR-01, WR-02, IN-01, IN-02) in one file pass</name>
  <files>test/crosswake/planning/summary_frontmatter_test.exs</files>
  <read_first>
    - test/crosswake/planning/summary_frontmatter_test.exs (full current 95-line file; confirm current shape matches the pre-edit interface block in this plan before editing)
    - .planning/phases/25-address-tech-debt-phase-20-verification-text-phase-24-parity/25-PATTERNS.md (§"Four changes to make (D-03, D-06, D-08, D-09)" — canonical post-edit code for each of the four changes, copied from 24-REVIEW.md fix snippets)
    - .planning/phases/24-reconciliation-traceability-hardening/24-REVIEW.md (lines 47–110 — WR-01, WR-02, IN-01, IN-02 fix snippets; the canonical source of truth for each edit's exact shape)
    - .planning/phases/25-address-tech-debt-phase-20-verification-text-phase-24-parity/25-CONTEXT.md (D-03, D-06, D-07, D-08, D-09, Footgun 2 — empty `[]` must NOT trip the new raise)
  </read_first>
  <action>
    Apply all four edits to the same file in a single coherent pass — do not split across multiple commits, do not commit anything yet (commit happens after all three files are in place, see Task 3). The four edits, in order, sourced verbatim from 25-PATTERNS.md §"Four changes to make":

    (1) **IN-01 (D-08) — Add `@requirements_path` compile-time module attribute.** Insert directly after the existing `@summary_glob` line (current line 4), preserving the two-space indentation: `@requirements_path Path.join(File.cwd!(), ".planning/REQUIREMENTS.md")`. Symmetric with `@summary_glob`. No blank line between the two attributes (match the existing module-attribute density).

    (2) **WR-01 (D-03) — Insert new third test.** Insert after test 2 (current line 32) and BEFORE the first `defp` helper. Test name string at executor discretion per CONTEXT.md, recommended: `"every phase summary declares requirements-completed:"`. Test body verbatim from 25-PATTERNS.md "Change 2" block — asserts `summaries != []`, then per-path asserts (a) `fm != ""` with message `"#{path} has no YAML frontmatter block"` and (b) `Regex.match?(~r/^requirements-completed:[ \t]*(?:\[|\r?\n[ \t]+-)/m, fm)` with message `"#{path} is missing required `requirements-completed:` key"`. Preserve `async: true` module posture (do NOT change line 2). Preserve the path-in-message idiom established in tests 1 and 2.

    (3) **WR-02 (D-06, D-07) — Tighten `extract_completed_ids/1`.** Insert a new third `cond` arm BEFORE the existing `true -> []` fallback. Per 25-PATTERNS.md "Change 3" block: `Regex.match?(~r/^requirements-completed:/m, frontmatter) -> raise "requirements-completed: is present but neither inline `[A, B]` nor multi-line `  - X` shape parsed"`. The raise message wording is at executor discretion per CONTEXT.md as long as it names which shape was attempted (raise lives in the HELPER per D-07, not in the test body). The existing `true -> []` fallback REMAINS as the final arm — it now only fires when the key is genuinely absent, which is the WR-01 missing-key path the new third test catches. **Footgun 2 invariant:** the inline arm's `[^\]]*` capture still matches `requirements-completed: []` (capture = `""`, `scan_ids("")` returns `[]`), so empty inline `[]` parses cleanly and the new raise branch is NEVER reached for the canonical tech-debt shape. Verify by mental-execution that `requirements-completed: []` reaches the inline arm before the raise arm in cond ordering.

    (4) **IN-02 (D-09) — Widen REQUIREMENTS.md bullet regex character class.** In `parse_requirement_ids_from_requirements_md/0`, change the regex from `~r/- \[[x ]\] \*\*([A-Z]+-\d+)\*\*/` to `~r/- \[[xX ]\] \*\*([A-Z]+-\d+)\*\*/`. Same edit also updates the function body to use `@requirements_path` instead of `Path.join(File.cwd!(), ".planning/REQUIREMENTS.md")` per 25-PATTERNS.md "Also update" block — the `Path.join` call moves to the compile-time attribute (edit 1).

    Run `mix compile` after the edits to confirm the file is syntactically valid before proceeding to Task 2 (do not commit yet; Task 3 owns the commit).
  </action>
  <verify>
    <automated>mix compile 2>&1 | grep -v '^Compiling' | grep -E '(error|warning:)' || echo "compile clean"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '^  @requirements_path Path\.join(File\.cwd!(), "\.planning/REQUIREMENTS\.md")$' test/crosswake/planning/summary_frontmatter_test.exs` returns exactly `1` (IN-01 / D-08 fix present, byte-exact line).
    - `grep -c '^  test "every phase summary declares requirements-completed:"' test/crosswake/planning/summary_frontmatter_test.exs` returns `1` (or, if executor chose a different test description, `grep -c 'declares requirements-completed' test/crosswake/planning/summary_frontmatter_test.exs` is at least `1` AND a description naming the WR-01 behavior is present).
    - `grep -c 'Regex.match?(~r/\^requirements-completed:/m, frontmatter) ->' test/crosswake/planning/summary_frontmatter_test.exs` returns `1` (WR-02 / D-06 raise branch present).
    - `grep -c 'raise "requirements-completed:' test/crosswake/planning/summary_frontmatter_test.exs` returns at least `1` (raise lives in the helper per D-07; the message names the shape).
    - `grep -c '- \\\[\[xX \]\\\] \\\*\\\*' test/crosswake/planning/summary_frontmatter_test.exs` returns `1` — i.e., the file contains the literal regex character class `[xX ]` inside the REQUIREMENTS.md bullet regex (IN-02 / D-09 fix).
    - `grep -c 'Path\.join(File\.cwd!(), "\.planning/REQUIREMENTS\.md")' test/crosswake/planning/summary_frontmatter_test.exs` returns exactly `1` — the `Path.join` call appears ONCE (in the new `@requirements_path` attribute), NOT twice (the runtime call in `parse_requirement_ids_from_requirements_md/0` body is removed).
    - `grep -c '^  use ExUnit.Case, async: true$' test/crosswake/planning/summary_frontmatter_test.exs` returns `1` — async posture preserved.
    - `mix compile` exits 0 with no new warnings introduced by this file.
  </acceptance_criteria>
  <done>
    All four edits applied to `summary_frontmatter_test.exs` in one coherent pass. File compiles. Empty `requirements-completed: []` would still parse via the inline arm (Footgun 2 invariant intact). No commit yet — Task 3 ships the atomic commit.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create 25-01-SUMMARY.md and 25-02-SUMMARY.md with the canonical tech-debt frontmatter shape</name>
  <files>
    .planning/phases/25-address-tech-debt-phase-20-verification-text-phase-24-parity/25-01-SUMMARY.md,
    .planning/phases/25-address-tech-debt-phase-20-verification-text-phase-24-parity/25-02-SUMMARY.md
  </files>
  <read_first>
    - $HOME/.claude/get-shit-done/templates/summary.md (canonical SUMMARY body template — frontmatter is overridden per this plan, but the body structure / section headings follow the template)
    - .planning/phases/19-commerce-route-corridors/19-01-SUMMARY.md (closest frontmatter analog — inline `requirements-completed: [..]` shape; empty `[]` variant for tech-debt phases per D-04)
    - .planning/phases/24-reconciliation-traceability-hardening/24-01-SUMMARY.md (multi-line `requirements-completed:` shape for CONTRAST — do NOT use for tech-debt SUMMARYs; D-04 mandates inline empty `[]`)
    - .planning/phases/25-address-tech-debt-phase-20-verification-text-phase-24-parity/25-CONTEXT.md (D-04: empty inline `[]` is the canonical tech-debt shape; D-05: no `tech-debt: true` opt-out flag; Footgun 4: no scope creep into other files)
  </read_first>
  <action>
    Create both new SUMMARY files. Frontmatter is byte-exact as specified in this plan's `<interfaces>` block (and 25-PATTERNS.md §"Required frontmatter for ..."). The `requirements-completed: []` line is the inline empty-list form per D-04 — NOT a bare key, NOT `~`, NOT a multi-line block with zero bullets, NOT a `tech-debt: true` opt-out (D-05 forbids the opt-out). The body of each SUMMARY follows the standard template (headings: "What Was Built", "Verification", "Files Modified", "Next Steps" or equivalent) and describes the actual plan output:

    - **25-01-SUMMARY.md body** summarizes Plan 25-01's single-line delete to `20-VERIFICATION.md:63` — what was deleted (the contradictory sentence), what was preserved (line 61 byte-identical), why (CONTEXT.md D-02 minimal-scope rule + audit's stated fix), how verified (the grep assertions from 25-01-PLAN Task 1's acceptance criteria). Body content is at executor discretion as long as it reflects what Plan 25-01 actually shipped.

    - **25-02-SUMMARY.md body** summarizes Plan 25-02's four edits to `summary_frontmatter_test.exs` (WR-01 third test, WR-02 helper raise, IN-01 `@requirements_path`, IN-02 `[xX ]`) plus the creation of both 25-* SUMMARYs in this atomic commit. Cite 24-REVIEW.md WR-01/WR-02/IN-01/IN-02 as the source of truth. Note the discovery survey result (Task 3 confirms the strengthened test passes against all 16 existing SUMMARYs + both new 25-* SUMMARYs = 18 total).

    Both SUMMARYs declare `requirements-completed: []` literally; both will be glob-walked by the new third test (Task 1) and must satisfy the new presence assertion before the commit in Task 3 is created.
  </action>
  <verify>
    <automated>grep -c '^requirements-completed: \[\]$' .planning/phases/25-address-tech-debt-phase-20-verification-text-phase-24-parity/25-01-SUMMARY.md .planning/phases/25-address-tech-debt-phase-20-verification-text-phase-24-parity/25-02-SUMMARY.md</automated>
  </verify>
  <acceptance_criteria>
    - Both `25-01-SUMMARY.md` and `25-02-SUMMARY.md` exist at `.planning/phases/25-address-tech-debt-phase-20-verification-text-phase-24-parity/`.
    - `grep -l '^requirements-completed: \[\]$' .planning/phases/25-address-tech-debt-phase-20-verification-text-phase-24-parity/25-0{1,2}-SUMMARY.md` lists BOTH files.
    - `grep -c '^---$' .planning/phases/25-address-tech-debt-phase-20-verification-text-phase-24-parity/25-01-SUMMARY.md` returns `2` (opening + closing frontmatter fence; standard YAML frontmatter shape).
    - `grep -c '^---$' .planning/phases/25-address-tech-debt-phase-20-verification-text-phase-24-parity/25-02-SUMMARY.md` returns `2`.
    - Neither SUMMARY contains the string `tech-debt: true` (D-05 — no opt-out flag).
    - Neither SUMMARY contains a multi-line `requirements-completed:` block (i.e., `grep -A1 '^requirements-completed:$' .planning/phases/25-address-tech-debt-phase-20-verification-text-phase-24-parity/25-0{1,2}-SUMMARY.md | grep -E '^[ \t]+- '` returns empty — D-04 mandates inline `[]`, not block-with-zero-bullets).
    - `grep -c '^phase: 25-address-tech-debt-phase-20-verification-text-phase-24-parity$' .planning/phases/25-address-tech-debt-phase-20-verification-text-phase-24-parity/25-0{1,2}-SUMMARY.md` returns `2` (one per file; matches the directory phase slug).
    - 25-01-SUMMARY.md frontmatter contains `plan: 01`; 25-02-SUMMARY.md frontmatter contains `plan: 02`.
  </acceptance_criteria>
  <done>
    Both Phase 25 SUMMARYs exist with the canonical `requirements-completed: []` shape. No commit yet — Task 3 ships the atomic commit containing the test file edits (Task 1) AND both SUMMARYs together.
  </done>
</task>

<task type="auto">
  <name>Task 3: Discovery survey, then ONE atomic commit containing the test edits and both SUMMARYs (Footgun 1, D-10, D-12)</name>
  <files>
    test/crosswake/planning/summary_frontmatter_test.exs,
    .planning/phases/25-address-tech-debt-phase-20-verification-text-phase-24-parity/25-01-SUMMARY.md,
    .planning/phases/25-address-tech-debt-phase-20-verification-text-phase-24-parity/25-02-SUMMARY.md
  </files>
  <read_first>
    - .planning/phases/25-address-tech-debt-phase-20-verification-text-phase-24-parity/25-CONTEXT.md (D-10 atomic commit, D-12 discovery-before-commit, Footgun 1 atomic-coupling, Footgun 3 silent-weakening prohibition)
    - .planning/phases/24-reconciliation-traceability-hardening/24-CONTEXT.md (D-11 — discovery-before-commit pattern the survey extends)
    - Output of `git status` and `git diff --stat` (verify Task 1 + Task 2 changes are staged for the same commit; verify no other modifications are present)
  </read_first>
  <action>
    **Step 1 — Discovery survey (Footgun 3, D-12).** Run `mix test test/crosswake/planning/summary_frontmatter_test.exs` against the working tree (which now contains the strengthened test from Task 1 AND both new Phase 25 SUMMARYs from Task 2). The full SUMMARY corpus is 16 existing + 2 new = 18 SUMMARYs. The test MUST pass. If it fails:
      (a) If the failure is on a pre-existing SUMMARY (any phase other than 25), backfill that SUMMARY's frontmatter with `requirements-completed: <correct value>` IN THIS SAME COMMIT — do NOT split into a follow-up commit. Determine the correct value from that phase's actual requirement closures (read its VERIFICATION.md / ROADMAP.md entry).
      (b) If the failure is on a 25-* SUMMARY, Task 2 produced malformed frontmatter — fix the SUMMARY shape.
      (c) DO NOT silently weaken the assertion to make the survey pass. Footgun 3 explicitly forbids re-introducing the silent-acceptance hole the phase exists to close. If a phase-number floor allowlist becomes genuinely necessary (option (c) of D-12), add it with an INLINE COMMENT in the test file citing the floor decision and the failing SUMMARY path. The allowlist must be visible at code-review time, not buried.

    **Step 2 — Atomic-commit verification.** Before invoking `git commit`, confirm via `git status` that the staged file set is exactly:
      - `test/crosswake/planning/summary_frontmatter_test.exs`
      - `.planning/phases/25-address-tech-debt-phase-20-verification-text-phase-24-parity/25-01-SUMMARY.md` (new file)
      - `.planning/phases/25-address-tech-debt-phase-20-verification-text-phase-24-parity/25-02-SUMMARY.md` (new file)
    No other paths. Specifically, neither `.planning/phases/20-entitlement-lifecycle-semantics/20-VERIFICATION.md` (that already shipped in Plan 25-01's commit) nor any file under `lib/crosswake/`, `examples/`, `.github/workflows/`, or commerce code (Footgun 4 invariant).

    **Step 3 — Atomic commit.** Stage the three paths explicitly by name (do NOT use `git add -A` or `git add .`). Commit with a Conventional Commits message that names the parity-test hardening scope. Suggested form: `test(phase-25): harden summary frontmatter parity (WR-01, WR-02, IN-01, IN-02)`. Body should explicitly note that the new presence assertion plus Phase 25's own two SUMMARYs ship together to satisfy the atomic-commit constraint from CONTEXT.md D-10 / Footgun 1, and cite 24-REVIEW.md as the canonical source of the four fix snippets. Do NOT amend any prior commit (specifically, do NOT amend the 25-01 docs commit from Plan 25-01). Do NOT skip hooks.

    **Step 4 — Post-commit sanity.** Run `mix test test/crosswake/planning/summary_frontmatter_test.exs` one more time against the just-committed state to confirm green. Also run `mix test` (full suite) to confirm no other test regression (the existing renderer / support_matrix / doctor / guides / proof parity tests should all remain green; the existing 225-test count from Phase 20 expands by 1 with the new third test).
  </action>
  <verify>
    <automated>mix test test/crosswake/planning/summary_frontmatter_test.exs && git log -1 --name-only --pretty=format:'%n--FILES--%n' | sed -n '/--FILES--/,$p' | sort -u</automated>
  </verify>
  <acceptance_criteria>
    - **Pre-commit survey passes:** `mix test test/crosswake/planning/summary_frontmatter_test.exs` exits 0 against the working tree after Task 1 + Task 2 changes are present, BEFORE `git commit` is invoked. (Proves the strengthened test + both new SUMMARYs are mutually consistent.)
    - **Atomicity invariant:** `git log -1 --name-only --pretty=format:''` lists exactly three paths (sorted): `.planning/phases/25-address-tech-debt-phase-20-verification-text-phase-24-parity/25-01-SUMMARY.md`, `.planning/phases/25-address-tech-debt-phase-20-verification-text-phase-24-parity/25-02-SUMMARY.md`, `test/crosswake/planning/summary_frontmatter_test.exs`. No fourth file. No `lib/` path. No `.github/` path. No `examples/` path.
    - **Conventional Commits scope:** `git log -1 --pretty=format:'%s'` matches `^test\(phase-25\):` (or the equivalent project-canonical scope used in 24-* commits if it differs).
    - **Post-commit green:** `mix test test/crosswake/planning/summary_frontmatter_test.exs` exits 0 against the just-committed `HEAD`.
    - **Full-suite no regression:** `mix test` exits 0 with the test count incremented by exactly 1 over Phase 24's count (the new third test in `summary_frontmatter_test.exs`).
    - **Atomic-commit narrative present:** `git log -1 --pretty=format:'%b'` contains a line referencing the atomic constraint (e.g., "atomic commit", "D-10", "Footgun 1", or "test + summary together"). This makes the constraint visible at git-archaeology time, not just at planning time.
    - **No silent-weakening:** if a phase-number floor allowlist was added during Step 1, `summary_frontmatter_test.exs` contains an inline comment naming the failing SUMMARY path and citing the floor decision (D-12 audit trail).
  </acceptance_criteria>
  <done>
    Exactly one new commit on `main` (or current working branch) contains: the four edits to `summary_frontmatter_test.exs`, the new `25-01-SUMMARY.md`, and the new `25-02-SUMMARY.md`. `mix test` passes against `HEAD`. No file under `lib/crosswake/`, `examples/`, `.github/workflows/`, or commerce code is touched. The atomic-commit constraint from D-10 / Footgun 1 is honored AND documented in the commit body. The discovery survey from D-12 / Footgun 3 was run pre-commit and passed against the full 18-SUMMARY corpus (or any pre-existing failures were backfilled in the same commit per D-12 option (a) / loud-allowlisted per option (c)).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Untrusted SUMMARY frontmatter shape → `extract_completed_ids/1` helper | Currently silent on missing-key (WR-01) and indentation-broken multi-line (WR-02). This phase tightens the boundary to loud-reject both. |
| Untrusted REQUIREMENTS.md checkbox case → `parse_requirement_ids_from_requirements_md/0` | Currently silent on uppercase `[X]` (IN-02). This phase widens to case-tolerant `[xX ]`. |
| Two-step commit (test then SUMMARYs, or SUMMARYs then test) → enforced contract | Committing in two steps either makes the test fail on Phase 25's own SUMMARYs (test first) or leaves the contract un-enforced for one commit (SUMMARYs first). Atomic-commit boundary prevents either silent-acceptance window. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25-02-T01 | Tampering (silent-input acceptance) | `extract_completed_ids/1` third `cond` arm | mitigate | Per D-06/WR-02: insert `Regex.match?(~r/^requirements-completed:/m, frontmatter) -> raise ...` branch BEFORE the existing `true -> []` fallback. Helper-level raise (D-07) protects all current and future callers, not just the test caller. Acceptance criteria asserts the raise branch is present via `grep -c 'Regex.match?(~r/\^requirements-completed:/m, frontmatter) ->'`. |
| T-25-02-T02 | Tampering (missing-key silent pass) | `summary_frontmatter_test.exs` Test 1 + Test 2 | mitigate | Per D-03/WR-01: add third test asserting every SUMMARY declares the key with parseable shape. `fm != ""` sub-assertion also catches the compounding-silence case from `parse_frontmatter/1` returning `""` on missing frontmatter. Acceptance criteria asserts the new test is present. |
| T-25-02-T03 | Tampering (case-sensitivity silent drop) | `parse_requirement_ids_from_requirements_md/0` regex `[x ]` | mitigate | Per D-09/IN-02: widen character class to `[xX ]`. Uppercase `[X]` (common in some markdown editors) currently silently drops the requirement ID from `known_ids`, which would cause Test 2 to falsely report a referenced ID as "not in REQUIREMENTS.md". Acceptance criteria asserts `[xX ]` literal is present. |
| T-25-02-R01 | Repudiation (commit-archaeology cannot reconstruct atomic constraint) | Commit message body | mitigate | Per Footgun 1 / D-10: commit body explicitly references the atomic constraint. Future maintainers reviewing the commit (or trying to revert it, or trying to split it) see the constraint inline. Acceptance criteria asserts the commit body contains `atomic commit`, `D-10`, `Footgun 1`, or `test + summary together`. |
| T-25-02-I01 | Information Disclosure (compile-time vs runtime cwd drift) | `@summary_glob` (compile-time) vs `parse_requirement_ids_from_requirements_md/0` runtime `File.cwd!()` | mitigate | Per D-08/IN-01: add `@requirements_path` compile-time attribute, refactor function body to use it. Symmetric resolution; no divergence under unusual invocation paths (custom Mix tasks, IEx `cd` re-runs). |
| T-25-02-D01 | Denial of Service (silent-weakening of the parity guard during discovery survey) | Pre-commit run of strengthened test against full corpus | mitigate | Per D-12/Footgun 3: if a pre-existing SUMMARY fails the new presence assertion, the planner MUST either backfill in the same commit OR add a phase-number floor allowlist with an INLINE COMMENT citing the floor decision. Silent weakening is explicitly forbidden. Acceptance criteria checks for the inline-comment audit trail if the floor allowlist is used. |
| T-25-02-E01 | Elevation of Privilege (scope creep beyond named files) | Files under `lib/crosswake/`, `examples/`, `.github/workflows/`, commerce code | mitigate | Per Footgun 4: this phase mutates EXACTLY two existing files (`20-VERIFICATION.md` in Plan 25-01, `summary_frontmatter_test.exs` in Plan 25-02) and creates EXACTLY two new SUMMARYs (`25-01-SUMMARY.md`, `25-02-SUMMARY.md`). Task 3 acceptance criteria asserts `git log -1 --name-only` lists exactly the three paths owned by this plan; any fourth path is a Footgun-4 violation. |
| T-25-SC | Tampering | npm/pip/cargo installs | n/a | No new package installs in this phase. Phase touches one Elixir test file (no new deps) and three planning artifacts (no deps). Package legitimacy gate does not apply. |
</threat_model>

<verification>
- All seven acceptance criteria of Task 1 pass (every `grep -c` returns the expected count; `mix compile` clean).
- All seven acceptance criteria of Task 2 pass (both SUMMARYs exist with byte-exact frontmatter; no `tech-debt: true` opt-out flag; no multi-line block form).
- All seven acceptance criteria of Task 3 pass (pre-commit survey passes; commit lists exactly three paths; Conventional Commits scope `test(phase-25):`; post-commit `mix test` green; full-suite count +1; atomic-commit narrative in commit body; loud-allowlist audit trail if used).
- The audit's `status: gaps_found` verdict is NOT flipped (out of scope per CONTEXT.md canonical_refs).
- No file under `lib/crosswake/`, `examples/`, `.github/workflows/`, commerce code, or any phase artifact outside CONTEXT.md `<canonical_refs>` "Files This Phase Mutates" is touched (Footgun 4).
- The strengthened test's existing `async: true` posture is preserved (line 2 untouched).
- The empty `requirements-completed: []` shape from D-04 does NOT trip the new WR-02 raise (Footgun 2 invariant — verified by mental execution of the cond ordering and by the post-commit `mix test` passing against both Phase 25 SUMMARYs).
</verification>

<success_criteria>
- `summary_frontmatter_test.exs` is hardened with all four edits (WR-01 third test, WR-02 helper raise, IN-01 `@requirements_path`, IN-02 `[xX ]` regex).
- Both `25-01-SUMMARY.md` and `25-02-SUMMARY.md` exist, each declaring `requirements-completed: []` inline (D-04 canonical tech-debt shape).
- ONE atomic commit contains all three files (the four test edits + both SUMMARYs); Footgun 1 / D-10 honored and visible in the commit body.
- The pre-commit discovery survey (Footgun 3, D-12) ran and passed against the full 18-SUMMARY corpus, or any pre-existing failures were backfilled in the same commit / loudly allowlisted with an inline audit-trail comment.
- `mix test` passes against `HEAD`; test count increments by exactly 1.
- No scope creep into `lib/crosswake/`, `examples/`, `.github/workflows/`, commerce code, or any phase artifact outside CONTEXT.md `<canonical_refs>` "Files This Phase Mutates".
</success_criteria>

<output>
Create `.planning/phases/25-address-tech-debt-phase-20-verification-text-phase-24-parity/25-02-SUMMARY.md` (and `25-01-SUMMARY.md`) as part of this plan's atomic commit (Task 2 creates the files; Task 3 commits them together with the test edits). The 25-02-SUMMARY body should summarize:
- The four test-file edits and their canonical sources in 24-REVIEW.md.
- The creation of 25-01-SUMMARY.md in this same commit (Footgun 1 atomic constraint).
- The pre-commit discovery survey result (corpus size, pass/fail, any allowlist additions).
- Verification evidence: `mix test` result, `git log -1 --name-only` confirming exactly three paths, commit-body atomic-constraint narrative.
</output>
