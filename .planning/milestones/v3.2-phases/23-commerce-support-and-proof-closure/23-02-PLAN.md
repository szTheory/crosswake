---
phase: 23-commerce-support-and-proof-closure
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/crosswake/support_matrix/support_matrix.ex
  - lib/crosswake/support_matrix/renderer.ex
  - guides/support_matrix.md
  - guides/commerce.md
  - guides/compatibility.md
  - test/crosswake/support_matrix/support_matrix_test.exs
  - test/crosswake/support_matrix/renderer_test.exs
  - test/crosswake/guides/commerce_test.exs
autonomous: true
requirements:
  - SUPP-04
  - SUPP-05
user_setup: []
must_haves:
  truths:
    - "Each commerce support row includes owner posture, prerequisite classes, canonical denial codes, fallback behavior, rebuild requirement, and proof class."
    - "Taxonomy parity across support matrix, doctor, guides, and tests is enforced by deterministic parity tests."
    - "Renderer output and guides stay byte-identical to support matrix source truth; guides are rendered artifacts, not independent sources."
    - "Commerce support granularity stays contract-surface-level (corridor role and commerce capability family), not per-route matrix expansion."
  artifacts:
    - path: lib/crosswake/support_matrix/support_matrix.ex
      provides: "Enriched commerce corridor entries with full SUPP-04 metadata"
      contains: "prerequisite_classes"
    - path: lib/crosswake/support_matrix/renderer.ex
      provides: "Renderer producing commerce corridor table with proof_class and rebuild columns"
      contains: "proof_class"
    - path: guides/support_matrix.md
      provides: "Regenerated support truth with enriched commerce corridor section"
      contains: "proof_class"
    - path: guides/commerce.md
      provides: "Enriched commerce corridor ownership section with proof-class and rebuild truth"
      contains: "proof_class"
  key_links:
    - from: lib/crosswake/support_matrix/support_matrix.ex
      to: lib/crosswake/support_matrix/renderer.ex
      via: "Renderer reads enriched corridor entries and produces deterministic markdown"
      pattern: "commerce_corridors"
    - from: lib/crosswake/support_matrix/renderer.ex
      to: guides/support_matrix.md
      via: "Generated guide is byte-identical to rendered output"
      pattern: "Commerce Corridors"
    - from: test/crosswake/support_matrix/renderer_test.exs
      to: guides/support_matrix.md
      via: "Parity test asserts generated guide matches renderer output"
      pattern: "byte-identical"
---

<objective>
Enrich support matrix commerce entries with full SUPP-04/SUPP-05 metadata and synchronize rendered guides.

Purpose: Close the support matrix enrichment gap where commerce corridor entries lack proof_class, prerequisite_classes, and rebuild_requirement columns. Ensure rendered guides remain deterministic artifacts of the typed support matrix source.
Output: Enriched support matrix entries, updated renderer with new columns, regenerated guides, and parity tests proving taxonomy synchronization.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/23-commerce-support-and-proof-closure/23-CONTEXT.md
@lib/crosswake/support_matrix/support_matrix.ex
@lib/crosswake/support_matrix/renderer.ex
@guides/support_matrix.md
@guides/commerce.md
@guides/compatibility.md
@test/crosswake/support_matrix/support_matrix_test.exs
@test/crosswake/support_matrix/renderer_test.exs
@test/crosswake/guides/commerce_test.exs
</context>

<tasks>
<task type="auto">
  <name>Task 1: Enrich support matrix commerce entries with prerequisite_classes, proof_class, and rebuild metadata</name>
  <files>lib/crosswake/support_matrix/support_matrix.ex, test/crosswake/support_matrix/support_matrix_test.exs</files>
  <read_first>lib/crosswake/support_matrix/support_matrix.ex, test/crosswake/support_matrix/support_matrix_test.exs, .planning/phases/23-commerce-support-and-proof-closure/23-CONTEXT.md</read_first>
  <action>Extend each `@commerce_corridor_entries` map with: `prerequisite_classes` (list of required prerequisite categories, e.g. `[:backend_reconciliation, :route_declaration]` for paywall_entry, `[:native_adapter, :provider_setup, :backend_reconciliation]` for purchase_intent), `proof_class` (`:merge_blocking` for hermetic contract proof, `:advisory` for provider/storefront checks), and `rebuild_requirement` (structured map with `native_rebuild_required` boolean and `rebuild_trigger` description). Keep `paywall_entry` and `account_management` as merge-blocking with no native rebuild for core route metadata. Keep `purchase_intent` and `restore_intent` as merge-blocking for core contracts with advisory provider proof and native rebuild required for adapter code changes. Add `commerce_corridor_prerequisite_taxonomy/0` returning the canonical prerequisite class set. Update tests to assert every corridor has `prerequisite_classes`, `proof_class`, and `rebuild_requirement` fields, and that prerequisite classes are drawn from the canonical taxonomy.</action>
  <verify>mix test test/crosswake/support_matrix/support_matrix_test.exs</verify>
  <acceptance_criteria>
    - `lib/crosswake/support_matrix/support_matrix.ex` commerce corridor entries contain `prerequisite_classes`, `proof_class`, and `rebuild_requirement` keys
    - `lib/crosswake/support_matrix/support_matrix.ex` contains `def commerce_corridor_prerequisite_taxonomy`
    - `test/crosswake/support_matrix/support_matrix_test.exs` asserts all corridors have prerequisite_classes, proof_class, and rebuild_requirement
    - `test/crosswake/support_matrix/support_matrix_test.exs` asserts prerequisite classes come from canonical taxonomy
  </acceptance_criteria>
  <done>Support matrix commerce entries carry full SUPP-04 metadata for downstream rendering and doctor consumption.</done>
</task>

<task type="auto">
  <name>Task 2: Update renderer to produce enriched commerce corridor table with new columns</name>
  <files>lib/crosswake/support_matrix/renderer.ex, guides/support_matrix.md, test/crosswake/support_matrix/renderer_test.exs</files>
  <read_first>lib/crosswake/support_matrix/renderer.ex, guides/support_matrix.md, test/crosswake/support_matrix/renderer_test.exs</read_first>
  <action>Update `commerce_corridor_section/1` and `commerce_corridor_row/1` to include `prerequisite_classes`, `proof_class`, and `rebuild_requirement` columns in the rendered markdown table. Regenerate `guides/support_matrix.md` by running the renderer and writing the output. The guide must be byte-identical to renderer output. Update renderer tests to assert: new columns appear in table headers, proof_class values appear in rendered rows, prerequisite classes are rendered as semicolon-separated lists, and the byte-identity parity check still passes.</action>
  <verify>mix test test/crosswake/support_matrix/renderer_test.exs</verify>
  <acceptance_criteria>
    - `lib/crosswake/support_matrix/renderer.ex` renders `proof_class` and `prerequisite_classes` columns
    - `guides/support_matrix.md` contains `proof_class` in the Commerce Corridors table
    - `test/crosswake/support_matrix/renderer_test.exs` asserts new columns in commerce corridor section
    - `test/crosswake/support_matrix/renderer_test.exs` byte-identity parity assertion passes
  </acceptance_criteria>
  <done>Rendered support guide includes enriched commerce corridor truth with proof-class and prerequisite columns, deterministically generated.</done>
</task>

<task type="auto">
  <name>Task 3: Synchronize commerce guide with enriched corridor truth and enforce parity</name>
  <files>guides/commerce.md, guides/compatibility.md, test/crosswake/guides/commerce_test.exs</files>
  <read_first>guides/commerce.md, guides/compatibility.md, test/crosswake/guides/commerce_test.exs, lib/crosswake/support_matrix/support_matrix.ex</read_first>
  <action>Update the Commerce Corridor Ownership section in `guides/commerce.md` to include proof_class and rebuild columns matching support matrix truth. Add a "Proof Posture" subsection that explicitly states which corridor checks are merge-blocking hermetic and which are advisory provider/storefront checks, with a clear non-claim that advisory checks cannot redefine core support truth. Update the Canonical Corridor Denial And Fallback Codes table to include proof_class per denial code. Update `guides/compatibility.md` to reference commerce rebuild truth. Update `test/crosswake/guides/commerce_test.exs` with parity assertions: commerce guide must contain `proof_class`, `merge-blocking`, `advisory`, and explicit non-claim language about advisory checks. Add a cross-check that corridor roles in commerce.md match support matrix corridor roles exactly.</action>
  <verify>mix test test/crosswake/guides/commerce_test.exs</verify>
  <acceptance_criteria>
    - `guides/commerce.md` contains `proof_class` in corridor ownership section
    - `guides/commerce.md` contains explicit non-claim about advisory checks
    - `test/crosswake/guides/commerce_test.exs` contains parity assertion for proof_class presence
    - `test/crosswake/guides/commerce_test.exs` contains cross-check that corridor roles match support matrix
  </acceptance_criteria>
  <done>Commerce guide reflects enriched corridor truth with proof-class labels and taxonomy parity is mechanically enforced.</done>
</task>
</tasks>

<threat_model>
## Security Gate

- standard: ASVS L1
- block_on: high

## Trust Boundaries

- Support matrix entries are consumed by doctor, renderer, and guides as canonical truth.
- Rendered guides cross from internal truth to public-facing documentation.
- Proof_class metadata drives CI gate decisions; incorrect classification weakens merge trust.

## STRIDE Threat Register

| threat_id | category | component | disposition | mitigation_plan |
| --- | --- | --- | --- | --- |
| P23-02-T01 | tampering | Guide divergence from support matrix | mitigated | Byte-identity parity test for support_matrix.md; cross-check test for commerce.md corridor roles against support matrix source. |
| P23-02-T02 | spoofing | Advisory proof class presented as merge-blocking | mitigated | Explicit non-claim language in guides and proof_class enum restricted to two values with test coverage. |
| P23-02-T03 | repudiation | Missing prerequisite classes | mitigated | Canonical prerequisite taxonomy function with test asserting all corridor prerequisites are drawn from the taxonomy set. |
</threat_model>

<verification>
Before declaring plan complete:
- [ ] mix test test/crosswake/support_matrix/support_matrix_test.exs test/crosswake/support_matrix/renderer_test.exs test/crosswake/guides/commerce_test.exs
- [ ] rg "proof_class|prerequisite_classes|rebuild_requirement" lib/crosswake/support_matrix guides/support_matrix.md guides/commerce.md
- [ ] rg "advisory.*cannot.*redefine|advisory.*not.*core" guides/commerce.md
</verification>

<success_criteria>
- All tasks completed
- SUPP-04 is further satisfied: support matrix rows include all required metadata fields (owner, prerequisites, denial codes, fallback, rebuild, proof class)
- SUPP-05 is partially satisfied: commerce guide includes proof-class and rebuild truth with explicit non-claims
- Taxonomy parity is mechanically enforced across support matrix, renderer, guides, and tests
</success_criteria>

<output>
After completion, create `.planning/phases/23-commerce-support-and-proof-closure/23-02-SUMMARY.md`
</output>
