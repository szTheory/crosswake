---
phase: 56-step-up-intent-and-plug-liveview-ceremony
plan: "03"
type: execute
wave: 3
depends_on: ["56-01", "56-02"]
autonomous: true
requirements: [STEP-02, STEP-03]
files_modified:
  - lib/crosswake/companions/sigra/step_up_ceremony.ex
  - examples/phoenix_host/lib/crosswake_example/saas_portal/step_up_plug.ex
  - examples/phoenix_host/lib/crosswake_example/saas_portal/step_up_on_mount.ex
  - test/crosswake/proof/phase56_step_up_ceremony_test.exs
---

# Plan 56-03: Shared Ceremony Core And Plug/LiveView Adapters

<objective>
Add one shared step-up ceremony decision core and prove Plug/controller and LiveView `on_mount` paths fail closed into the same challenge/intent flow without duplicating auth evaluation logic.
</objective>

<context>
STEP-02 is the center of this plan: controller plugs and LiveView mounts must share one ceremony evaluator while preserving transport-specific redirect and halt mechanics.
</context>

<decision_coverage>
D-09, D-10, D-11, D-12, D-13, D-14, D-15, D-19, D-22, D-28, D-29
</decision_coverage>

<tasks>
  <task id="56-03-01" type="execute">
    <name>Add shared Sigra step-up ceremony core</name>
    <read_first>
      .planning/phases/56-step-up-intent-and-plug-liveview-ceremony/56-CONTEXT.md
      .planning/phases/56-step-up-intent-and-plug-liveview-ceremony/56-RESEARCH.md
      lib/crosswake/companions/sigra/evaluator.ex
      lib/crosswake/companions/sigra/step_up.ex
      lib/crosswake/compatibility/route_gate.ex
      lib/crosswake/shell/denial.ex
      test/crosswake/compatibility/route_gate_test.exs
    </read_first>
    <files>
      lib/crosswake/companions/sigra/step_up_ceremony.ex
      test/crosswake/proof/phase56_step_up_ceremony_test.exs
    </files>
    <action>Create `Crosswake.Companions.Sigra.StepUpCeremony` with `evaluate_or_issue/3`. The function must call `Crosswake.Companions.Sigra.Evaluator.evaluate_route_auth/3` unless an evaluator result is explicitly provided in opts for tests. It must return `{:allow, facts}` when Sigra evaluation allows, return `{:deny, %Crosswake.Shell.Denial{}}` for non-challengeable denials, and call a host-provided `:issue_intent` callback only for `:step_up_required` denials that are challengeable. The callback result shape must be `{:ok, %{intent: %StepUp.StepUpIntentRecord{}, challenge: %StepUp.StepUpChallenge{}}}` or `{:error, %Crosswake.Shell.Denial{}}`. The returned challenge must include `challenge_route_id`, `return_route_id`, `challenge_kind`, `required_assurance_level`, `max_auth_age_seconds`, and safe support ref. The module must not import `Ecto`, `Phoenix.Controller`, `Plug.Conn`, `Phoenix.LiveView`, `Phoenix.Token`, or example-host modules.</action>
    <acceptance_criteria>
      - `StepUpCeremony.evaluate_or_issue/3` returns `{:allow, facts}` for an already sufficient `AuthContext`.
      - It returns `{:challenge, intent, challenge}` when `Sigra.Evaluator` denies with public reason `:step_up_required` for insufficient assurance or stale auth and the host issuer succeeds.
      - It returns `{:deny, %Crosswake.Shell.Denial{}}` when the host issuer returns a denial.
      - The ceremony module source does not contain `Ecto`, `Phoenix.Controller`, `Plug.Conn`, `Phoenix.LiveView`, `Phoenix.Token`, or `CrosswakeExample`.
      - Tests assert `Sigra.Evaluator` remains free of intent issuing, redirecting, halting, session renewal, and CSRF logic.
    </acceptance_criteria>
    <automated>mix test test/crosswake/proof/phase56_step_up_ceremony_test.exs --trace</automated>
    <done>The shared ceremony core can convert route-auth denials into challenge outcomes without owning transport or host persistence.</done>
  </task>

  <task id="56-03-02" type="execute">
    <name>Add thin Plug and LiveView adapters over the shared ceremony</name>
    <read_first>
      .planning/phases/56-step-up-intent-and-plug-liveview-ceremony/56-CONTEXT.md
      .planning/phases/56-step-up-intent-and-plug-liveview-ceremony/56-RESEARCH.md
      lib/crosswake/companions/sigra/step_up_ceremony.ex
      examples/phoenix_host/lib/crosswake_example/saas_portal/step_up.ex
      examples/phoenix_host/lib/crosswake_example/saas_portal/auth.ex
      examples/phoenix_host/lib/crosswake_example/selective_native/on_mount.ex
      examples/phoenix_host/lib/crosswake_example/router.ex
    </read_first>
    <files>
      examples/phoenix_host/lib/crosswake_example/saas_portal/step_up_plug.ex
      examples/phoenix_host/lib/crosswake_example/saas_portal/step_up_on_mount.ex
      test/crosswake/proof/phase56_step_up_ceremony_test.exs
    </files>
    <action>Create `CrosswakeExample.SaaSPortal.StepUpPlug` and `CrosswakeExample.SaaSPortal.StepUpOnMount` as thin host adapters. Both must call `Crosswake.Companions.Sigra.StepUpCeremony.evaluate_or_issue/3` with the same route, auth context, expected session version, and `issue_intent` callback to `CrosswakeExample.SaaSPortal.StepUp.issue/1`. The Plug adapter must redirect to the host challenge path or route target from the challenge and halt the connection on `{:challenge, ...}` or `{:deny, ...}`. The LiveView adapter must redirect the socket and return `{:halt, socket}` on challenge or denial. Neither adapter may reimplement assurance, freshness, session-version, remembered, cached, expiry, or revocation checks; tests should assert those identifiers do not appear as local logic branches outside option plumbing. Add completion helper usage so LiveView completion forces `live_socket_invalidation: %{reason: :step_up_completed}` or equivalent remount/re-evaluation proof.</action>
    <acceptance_criteria>
      - Plug and LiveView tests produce the same `challenge_kind`, `return_route_id`, `required_assurance_level`, and `max_auth_age_seconds` for the same stale/weak auth route.
      - Plug challenge path returns a halted connection with redirect location containing a server-issued step-up locator or support ref, not a raw `return_to` URL.
      - LiveView `on_mount` challenge path returns `{:halt, socket}` after redirect.
      - Source assertions show `StepUpPlug` and `StepUpOnMount` call `StepUpCeremony.evaluate_or_issue` and do not contain duplicated checks for `idle_expires_at`, `absolute_expires_at`, `assurance_level_meets?`, `auth_age_seconds`, `remembered`, or `cached`.
      - Completion proof shows LiveView stale socket state is invalidated or remounted after step-up completion.
    </acceptance_criteria>
    <automated>mix test test/crosswake/proof/phase56_step_up_ceremony_test.exs --trace</automated>
    <done>Controller and LiveView entry points share the same step-up ceremony decision path and differ only in redirect/halt transport mechanics.</done>
  </task>
</tasks>

<threat_model>
| ID | STRIDE | Asset | Risk | Mitigation |
|----|--------|-------|------|------------|
| T-56-08 | Elevation of Privilege | Plug/LiveView auth semantics | Controller and LiveView paths drift and one allows stale/weak auth. | Both adapters call `StepUpCeremony.evaluate_or_issue/3`; source assertions forbid duplicated checks. |
| T-56-09 | Tampering | Redirect/halt flow | A challenge response continues request processing after redirect. | Plug halts; LiveView returns `{:halt, socket}`. |
| T-56-10 | Information Disclosure | Challenge transport | Redirect exposes raw intent/session/return URL material. | Challenge carries support refs and route IDs only; raw `return_to` remains rejected. |
</threat_model>

<verification>
Run `mix test test/crosswake/proof/phase56_step_up_ceremony_test.exs --trace`.
</verification>

<success_criteria>
- Plug/controller and LiveView `on_mount` paths call one shared evaluator/ceremony core.
- Both paths fail closed into the same challenge/intent flow.
- Transport adapters handle redirect/halt only and do not duplicate route auth semantics.
- Completion invalidates stale LiveView socket state after refreshed authority projection.
</success_criteria>

<must_haves>
- STEP-02 is covered by direct source and behavior proof.
- STEP-03 completion semantics remain tied to Plan 56-02 backend consume/projection, not client-side evidence.
- No generic policy framework or provider auth UI is introduced.
</must_haves>
