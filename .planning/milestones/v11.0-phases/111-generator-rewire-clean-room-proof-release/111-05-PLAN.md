---
phase: 111-generator-rewire-clean-room-proof-release
plan: 05
type: execute
wave: 4
depends_on: ["111-01", "111-02", "111-03", "111-04"]
files_modified:
  - release-please-config.json
autonomous: false
requirements: [REL-01]
user_setup:
  - service: hex-spm-maven coordinated publish
    why: "The 0.1.2 cut fires the publish-hex / publish-ios-core / publish-android-core jobs and the clean-room proof; it requires the 8 provisioned secrets and the 4 human-UAT prerequisites from Phase 110 to be live"
    dashboard_config:
      - task: "Exercise the 4 deferred 110-HUMAN-UAT items (Android fire-drill, lockstep-truth lane, GPG keyserver upload, Sonatype namespace verify) before merging the Release PR"
        location: ".planning/phases/110-native-publish-lockstep-infrastructure/110-HUMAN-UAT.md + SETUP.md"
      - task: "Merge the release-please-generated Release PR for 0.1.2"
        location: "GitHub Pull Requests"
must_haves:
  truths:
    - "Hex 0.1.2 is published; the iOS mirror tag and the Android Maven artifact are all at 0.1.2 from a single release-please run"
    - "An adopter adding the Hex dep and running gen.shell gets a buildable scaffold with no manual coordinate correction (clean-room proof green)"
    - "The one-time release-as: 0.1.2 pin is removed from release-please-config.json in a chore: commit AFTER 0.1.2 ships"
    - "grep 'release-as' release-please-config.json returns no match after the post-cut removal"
  artifacts:
    - path: "release-please-config.json"
      provides: "release-as pin removed post-cut"
  key_links:
    - from: "Release PR merge"
      to: "publish-hex + publish-ios-core + publish-android-core + clean-room-proof"
      via: "release-please releases_created fan-out at 0.1.2"
      pattern: "releases_created"
    - from: "post-cut chore commit"
      to: "release-please-config.json"
      via: "remove the release-as: 0.1.2 root-package pin"
      pattern: "release-as"
---

<objective>
Cut the real coordinated `0.1.2` release — Hex + iOS mirror tag + Android Maven artifact all advancing together via the release-please-driven lockstep — then remove the one-time `release-as` pin so future releases bump normally.

Purpose: This is the milestone payoff: the first real coordinated cut that an external adopter can consume. It must land LAST, after the generator emits correct coordinates (Plans 01/02), the docs are reconciled (Plan 03), and the clean-room lane exists to prove it (Plan 04). Cutting earlier would publish a Hex package whose `gen.shell` still emits broken coordinates.

Output: Published `0.1.2` across all three registries, clean-room proof green, and a `chore:` commit removing the `release-as: "0.1.2"` pin (the #1 post-bootstrap footgun).

This plan is NOT autonomous: it requires human merge of the Release PR and post-publish confirmation before the pin removal is sequenced (D-04, REL-01 tail).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/111-generator-rewire-clean-room-proof-release/111-CONTEXT.md
@.planning/phases/111-generator-rewire-clean-room-proof-release/111-RESEARCH.md
@.planning/phases/110-native-publish-lockstep-infrastructure/110-HUMAN-UAT.md

<interfaces>
<!-- From release-please-config.json (current) — the root package pin to remove post-cut. -->

release-please-config.json root package ("."):
  "component": "hex", "release-type": "elixir",
  "release-as": "0.1.2",   <-- the one-time bootstrap pin (D-02/D-04) — REMOVE post-cut
  "extra-files": ["mix.exs"], ...

On Release PR merge, release-please emits releases_created: true, tag_name, version: "0.1.2"
and fans out: publish-hex, publish-ios-core, publish-android-core, then clean-room-proof-* .

Leaving "release-as": "0.1.2" in permanently makes every future Release PR propose 0.1.2
forever and blocks all future releases (RESEARCH Pitfall 3 / REC-PIPELINE Footgun 2).
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Exercise Phase 110 human-UAT prerequisites and merge the 0.1.2 Release PR</name>
  <what-built>
    Plans 01-04 have landed: the generator emits correct published coordinates, the merge-blocking
    parity guard is green, docs are reconciled with a ## [0.1.2] CHANGELOG entry, and the clean-room
    proof jobs are wired into release-please.yml. The release-please config already carries
    "release-as": "0.1.2". Everything required to cut 0.1.2 is in place.
  </what-built>
  <how-to-verify>
    1. Confirm the 4 deferred items in .planning/phases/110-native-publish-lockstep-infrastructure/110-HUMAN-UAT.md are exercised and passing:
       (a) Android publish fire-drill (validated-upload then drop) — credentials work
       (b) lockstep-truth CI lane — version coordinates agree on 0.1.0 before bumping
       (c) GPG public key on two keyservers (keys.openpgp.org + keyserver.ubuntu.com)
       (d) Sonatype namespace io.github.sztheory verified
       All 8 secrets present: run `gh secret list` per SETUP.md.
    2. Confirm `mix test --exclude requires_example_host` and `mix crosswake.doctor --check-publish` are green on main.
    3. Merge the release-please-generated Release PR for 0.1.2 in GitHub.
    4. Watch the release-please run fan out: publish-hex, publish-ios-core, publish-android-core, then clean-room-proof-ios + clean-room-proof-android.
    5. Confirm hex.pm shows crosswake 0.1.2, the szTheory/crosswake-shell-core-ios mirror has the 0.1.2 tag, and Maven Central shows io.github.sztheory:crosswake-shell-core-android:0.1.2.
    6. Confirm clean-room-proof-ios and clean-room-proof-android exited 0 (swift build + gradle build resolved the published deps and compiled).
  </how-to-verify>
  <resume-signal>Type "0.1.2 shipped" once all three registries show 0.1.2 and both clean-room jobs are green, or describe what failed.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Remove the release-as pin in a chore: commit (REL-01 tail)</name>
  <files>release-please-config.json</files>
  <read_first>
    - release-please-config.json (the root "." package block — locate the `"release-as": "0.1.2"` line)
    - .planning/phases/111-generator-rewire-clean-room-proof-release/111-RESEARCH.md § Release Sequence (post-cut release-as removal, D-04)
  </read_first>
  <action>
    ONLY after Task 1 confirms 0.1.2 shipped across all three registries and both clean-room jobs are green: remove the `"release-as": "0.1.2"` line from the root (".") package in `release-please-config.json` (D-04 / RESEARCH Pitfall 3). Leave every other key (`component`, `release-type`, `extra-files`, `changelog-sections`, the iOS/Android package entries, `plugins`) untouched. Commit with message `chore: remove release-as pin after 0.1.2 ships`. This prevents every future Release PR from proposing 0.1.2 indefinitely.
  </action>
  <verify>
    <automated>cd /Users/jon/projects/crosswake && grep -c "release-as" release-please-config.json | grep -qx 0 && python3 -c "import json; json.load(open('release-please-config.json'))" && echo PIN_REMOVED_AND_VALID</automated>
  </verify>
  <acceptance_criteria>
    - `grep "release-as" release-please-config.json` returns no match
    - `release-please-config.json` is still valid JSON (python json.load succeeds)
    - the iOS/Android package entries and `plugins` block are intact (diff shows only the release-as line removed)
    - commit message is `chore: remove release-as pin after 0.1.2 ships`
  </acceptance_criteria>
  <done>The release-as pin is gone, config remains valid JSON with all other keys intact, future releases will bump normally.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Release PR merge → three registries | a single merge publishes immutable artifacts (Maven Central) and a SwiftPM tag that adopters resolve |
| config edit → future release machinery | leaving/removing the release-as pin determines whether future releases can cut at all |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-111-13 | Tampering | tag hijack / force-move on the szTheory iOS mirror after an adopter resolves it | mitigate | no force-push; tag protection ruleset on mirror (Phase 110); single tag per release |
| T-111-14 | Denial of Service | release-as pin left in permanently blocks all future releases | mitigate | Task 2 removes the pin in a chore: commit immediately after the cut (D-04, RESEARCH Pitfall 3) |
| T-111-15 | Repudiation | cutting 0.1.2 before UAT prerequisites are live → publish jobs fail / burn an immutable Maven version | mitigate | Task 1 human checkpoint gates the merge on the 4 Phase-110 UAT items + secret verification before the cut |
| T-111-SC | Tampering | npm/pip/cargo installs | accept | no package-manager installs in this plan; only a JSON config edit + a registry publish driven by Phase-110-pinned actions |
</threat_model>

<verification>
- Manual (release-time, VALIDATION.md Manual-Only): hex.pm, the mirror tag, and Maven Central all show 0.1.2; both clean-room jobs exit 0
- `grep "release-as" release-please-config.json` returns no match after Task 2
- `release-please-config.json` is valid JSON post-edit
</verification>

<success_criteria>
- Hex `0.1.2` is published and the published Hex version, the iOS mirror tag, and the Android Maven artifact are all at `0.1.2` from one release-please run (ROADMAP success criterion 5 + REL-01)
- An adopter adding the Hex dep and running `gen.shell` gets a buildable scaffold with no manual coordinate correction (clean-room proof green)
- The `release-as` pin is removed post-cut so future releases bump normally
</success_criteria>

<output>
Create `.planning/phases/111-generator-rewire-clean-room-proof-release/111-05-SUMMARY.md` when done
</output>
