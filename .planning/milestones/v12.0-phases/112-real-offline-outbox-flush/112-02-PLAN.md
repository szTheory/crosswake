---
phase: 112-real-offline-outbox-flush
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - examples/phoenix_host/lib/crosswake_example/local_first/study_session_live.ex
autonomous: true
requirements: [E2E-02]

must_haves:
  truths:
    - "The StudySessionLive sync_outbox handler is removed entirely"
    - "The 'Simulate Network Sync' button and the outbox/sync_result assigns are removed entirely"
    - "mix test passes with the mock removed — no test depends on the deleted handler"
  artifacts:
    - path: "examples/phoenix_host/lib/crosswake_example/local_first/study_session_live.ex"
      provides: "De-mocked LiveView with no fake server-side outbox simulation"
      contains: "def mount"
  key_links:
    - from: "study_session_live.ex mount/3"
      to: "socket assigns"
      via: "current_card_id only (no outbox/sync_result)"
      pattern: "current_card_id"
---

<objective>
Remove the vestigial server-side outbox mock from `study_session_live.ex` entirely. Delete the `sync_outbox` handler, the "Simulate Network Sync" button, and the `outbox`/`sync_result` assigns. This mock is a server-side in-memory Elixir list flushed by a LiveView event — not a client IndexedDB outbox — and relabeling it would be a fresh dishonesty, so it is removed, not renamed.

Purpose: The real client outbox lives on `/offline` (Plan 01). This LiveView mock papered over the missing flush and must not survive into the honest milestone.
Output: Modified `study_session_live.ex` with the mock removed and `mix test` still green. No files created.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/112-real-offline-outbox-flush/112-CONTEXT.md
@.planning/phases/112-real-offline-outbox-flush/112-PATTERNS.md

<scope_fence>
Remove the mock entirely — do NOT relabel it "Manual Sync" (the StudySessionLive outbox is a server-side Elixir list, not the client IndexedDB outbox; relabeling misrepresents the mechanism). Do NOT add any new sync mechanism to this LiveView. Server context files (study.ex, sync_controller.ex, review_event.ex) are unchanged. Do NOT touch Plan 01's files (offline_study.js, index.html.heex).
</scope_fence>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Remove the sync_outbox mock, button, and outbox/sync_result assigns</name>
  <files>examples/phoenix_host/lib/crosswake_example/local_first/study_session_live.ex</files>
  <read_first>
    - examples/phoenix_host/lib/crosswake_example/local_first/study_session_live.ex (mount assigns lines 5-9; rate handler lines 14-26; sync_outbox handler lines 29-47; render outbox-status block lines 64-74)
    - .planning/phases/112-real-offline-outbox-flush/112-PATTERNS.md (Pattern 8: full delete inventory with exact line ranges)
  </read_first>
  <action>
    Per the Pattern 8 inventory: In mount/3, remove the `outbox: []` and `sync_result: nil` assigns so mount assigns only `current_card_id: 1`. Delete the entire `handle_event("sync_outbox", ...)` handler. In the `rate` handler, remove the `outbox = [event | socket.assigns.outbox]` accumulation and drop `outbox`/`sync_result` from its assign call so it becomes `{:noreply, assign(socket, current_card_id: next_card_id)}` — the rate handler still progresses the card; only the outbox accumulation is removed. The local `event` variable built in the rate handler (lines 14-19) becomes unused after this — delete it so the compiler does not warn. In the render template, delete the entire `<div class="outbox-status">…</div>` block (the `Local Outbox (… items)` heading, the `Simulate Network Sync` button, and the `@sync_result` paragraph). Per the de-mock decision and E2E-02.
  </action>
  <verify>
    <automated>! grep -nE "sync_outbox|sync_result|:outbox|@outbox|Simulate Network Sync|outbox:" examples/phoenix_host/lib/crosswake_example/local_first/study_session_live.ex && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - `grep -nE "sync_outbox|sync_result|:outbox|@outbox|Simulate Network Sync" examples/phoenix_host/lib/crosswake_example/local_first/study_session_live.ex` returns nothing
    - mount/3 assigns only `current_card_id: 1`
    - The rate handler ends with `{:noreply, assign(socket, current_card_id: next_card_id)}` and has no unused `event` binding
  </acceptance_criteria>
  <done>The fake server-side outbox simulation is gone — no handler, no button, no assigns reference it.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Verify the suite stays green and compiles warning-free</name>
  <files>examples/phoenix_host/lib/crosswake_example/local_first/study_session_live.ex</files>
  <read_first>
    - examples/phoenix_host/lib/crosswake_example/local_first/study_session_live.ex (post-edit state from Task 1)
    - .planning/phases/112-real-offline-outbox-flush/112-PATTERNS.md (Pattern 8 note: test scan found zero references to sync_outbox/outbox/sync_result — deletion is safe)
  </read_first>
  <action>
    Confirm no test references the deleted mock before relying on green: run `grep -rnE "sync_outbox|sync_result|:outbox|@outbox" examples/phoenix_host/test/` and expect zero matches. Then from the examples/phoenix_host directory run `mix compile --warnings-as-errors` (the rate handler's removed `event` binding must not leave an unused-variable warning) and `mix test`. If either fails, fix the cause in study_session_live.ex only (do not edit tests or server context files). The mix test green-with-mock-removed gate is a hard acceptance criterion for this phase.
  </action>
  <verify>
    <automated>cd examples/phoenix_host && mix compile --warnings-as-errors && mix test</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rnE "sync_outbox|sync_result|:outbox|@outbox" examples/phoenix_host/test/` returns nothing
    - `cd examples/phoenix_host && mix compile --warnings-as-errors` exits 0 (no unused-variable warning from the removed event binding)
    - `cd examples/phoenix_host && mix test` exits 0
  </acceptance_criteria>
  <done>The mock is removed and the full Elixir suite passes with no compile warnings.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none introduced) | This plan only DELETES code — it removes a fake server-side outbox simulation and adds no new input surface, endpoint, or trust boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-112-05 | Repudiation/Integrity | removing the mock could mask a real test dependency | mitigate | Task 2 greps test/ for any reference to the deleted symbols and runs the full `mix test` before the plan is considered done — a removal that broke a test fails the gate. |
| T-112-06 | Tampering | removing the LiveView mock leaves no fake "sync proof" | accept (intended) | Removal is the goal: the mock was a dishonest proof surface. No new attack surface is created by deleting it. |
| T-112-SC | Tampering | npm/pip/cargo installs | n/a | No package installs in this plan — deletion only. No legitimacy gate required. |
</threat_model>

<verification>
- `grep -nE "sync_outbox|sync_result|:outbox|@outbox|Simulate Network Sync" study_session_live.ex` returns nothing.
- `cd examples/phoenix_host && mix compile --warnings-as-errors && mix test` exits 0.
- No test file, server context file, or Plan 01 file was modified.
</verification>

<success_criteria>
- E2E-02 (de-mock side): the StudySessionLive sync_outbox mock handler, its "Simulate Network Sync" button, and the outbox/sync_result assigns are removed entirely.
- mix test passes with the mock removed — no test depends on the deleted handler.
</success_criteria>

<output>
Create `.planning/phases/112-real-offline-outbox-flush/112-02-SUMMARY.md` when done.
</output>
