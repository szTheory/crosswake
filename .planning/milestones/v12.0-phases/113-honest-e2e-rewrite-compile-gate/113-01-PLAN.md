---
phase: 113-honest-e2e-rewrite-compile-gate
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - examples/phoenix_host/test/support/flashcards_fixtures.ex
  - examples/phoenix_host/e2e/offline_storage.spec.ts
  - examples/phoenix_host/lib/crosswake_example/e2e/sync_state_controller.ex
autonomous: true
requirements: [E2E-03, E2E-04]

must_haves:
  truths:
    - "MIX_ENV=test mix compile --warnings-as-errors passes clean in examples/phoenix_host (the create_progress warning is gone) — so the Plan 03 compile gate can land green"
    - "npx playwright test e2e/offline_storage.spec.ts runs green (the #btn-pass dead selector left by Phase 112's rename is fixed) — so the whole Playwright lane is no longer red"
    - "GET /_e2e/sync-state/:client_mutation_id returns a `count` field scoped to that id (count of ReviewEvent rows WHERE client_mutation_id == ^id), so the spec can prove exactly-one-row on duplicate flush"
    - "sync_state_controller.ex carries a @moduledoc stating the endpoint is test-only and mounted only under :test/:e2e (pre-stages Phase 114 GUARD-02)"
  artifacts:
    - path: "examples/phoenix_host/lib/crosswake_example/e2e/sync_state_controller.ex"
      provides: "scoped count field in the sync-state JSON + test-only @moduledoc + import Ecto.Query"
      contains: "from(r in ReviewEvent, where: r.client_mutation_id == ^id)"
    - path: "examples/phoenix_host/test/support/flashcards_fixtures.ex"
      provides: "compile-clean fixture (upsert_progress, not the renamed-away create_progress)"
      contains: "upsert_progress"
    - path: "examples/phoenix_host/e2e/offline_storage.spec.ts"
      provides: "green sibling spec (live #btn-good selector)"
      contains: "#btn-good"
  key_links:
    - from: "examples/phoenix_host/lib/crosswake_example/e2e/sync_state_controller.ex"
      to: "examples/phoenix_host/lib/crosswake_example/local_first/review_event.ex"
      via: "scoped Ecto aggregate over ReviewEvent by client_mutation_id"
      pattern: "from\\(r in ReviewEvent, where: r.client_mutation_id == \\^id\\)"
  prohibitions:
    - "Do NOT rename the CI job e2e-offline-sync (that is Phase 114 GATE-01; renaming early drops it from any future required-checks list)"
    - "Do NOT change Study.sync_events/1, SyncController, ReviewEvent, or router.ex — the server side is already correct and idempotent"
    - "Do NOT touch HEEx / data-testid (Phase 115); the sibling-spec fix is a selector/text change in the .spec.ts only"
    - "Do NOT add a bare Repo.aggregate(ReviewEvent, :count, :id) — it counts the whole table and returns > 1 in any multi-test run; the count MUST be scoped to ^id"
    - "Do NOT add a second _e2e route or endpoint — only extend the existing show/2 (keeps GUARD-02's Phase-114 surface a single endpoint)"
---

<objective>
Lay the green foundation the honest rewrite (Plan 02) and the compile gate (Plan 03) both depend on. Three independent, surgical edits:

1. **Pre-flight compile fix (D-04b, E2E-04):** `flashcards_fixtures.ex:47` calls `CrosswakeExample.Flashcards.create_progress/1`, which was renamed to `upsert_progress/1`. This is the ONLY warning that makes `MIX_ENV=test mix compile --warnings-as-errors` fail today. The Plan 03 gate is born red without this fix.
2. **Sibling-spec hygiene (D-05, E2E-04):** `offline_storage.spec.ts:89` clicks `#btn-pass`, a selector Phase 112's D-01 rename killed. The click times out and the whole Playwright lane is red today — which would block E2E-04's "loud only for honest reasons" claim and Phase 114's required green run. Tighten line 92's storage-error text locator to the full string (honesty tightening).
3. **Controller extension (D-02, E2E-03):** Extend the existing `GET /_e2e/sync-state/:client_mutation_id` `show/2` to also return a `count` **scoped to the id**, plus a test-only `@moduledoc`. Plan 02's duplicate-flush proof polls `count === 1`; that field must exist before the spec can assert on it.

Purpose: Every downstream proof (compile gate green-on-first-run, full Playwright suite green, exactly-one-row assertion) requires these three to be true first.
Output: A compile-clean test tree, a green sibling spec, and a `count`-returning, documented sync-state endpoint.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/113-honest-e2e-rewrite-compile-gate/113-CONTEXT.md
@.planning/phases/113-honest-e2e-rewrite-compile-gate/113-RESEARCH.md
@.planning/phases/113-honest-e2e-rewrite-compile-gate/113-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pre-flight compile fix — create_progress → upsert_progress (D-04b)</name>
  <files>examples/phoenix_host/test/support/flashcards_fixtures.ex</files>
  <read_first>
    - examples/phoenix_host/test/support/flashcards_fixtures.ex (the file; the broken call is at line 47)
    - examples/phoenix_host/lib/crosswake_example/flashcards.ex (line 82 — confirm upsert_progress/1 is the live function name)
    - 113-RESEARCH.md § "Compile Gate Pre-Flight" (the verbatim warning + root cause) and "Compile Warning Fix (D-04b)"
  </read_first>
  <action>
    In flashcards_fixtures.ex line 47, change the piped call from `CrosswakeExample.Flashcards.create_progress()` to `CrosswakeExample.Flashcards.upsert_progress()`. This is a single-identifier rename — `create_progress/1` was renamed to `upsert_progress/1` (live at flashcards.ex:82) and the stale call is the only warning blocking `--warnings-as-errors`. Do not change the pipeline shape or arguments. RESEARCH confirmed this is the ONLY warning in the pre-flight compile run; do not hunt for or fix unrelated `mix test` failures (TODO-001 is Phase 115).
  </action>
  <acceptance_criteria>
    - `cd examples/phoenix_host && MIX_ENV=test mix compile --warnings-as-errors` exits 0 (no `create_progress/1 is undefined` warning, no other warnings)
    - `flashcards_fixtures.ex` contains `upsert_progress` and no longer contains `create_progress`
  </acceptance_criteria>
  <verify>
    <automated>cd examples/phoenix_host && MIX_ENV=test mix compile --warnings-as-errors</automated>
  </verify>
  <done>`MIX_ENV=test mix compile --warnings-as-errors` passes clean in examples/phoenix_host; the fixture calls upsert_progress.</done>
</task>

<task type="auto">
  <name>Task 2: Sibling-spec hygiene — #btn-pass → #btn-good + tighten storage-error text (D-05)</name>
  <files>examples/phoenix_host/e2e/offline_storage.spec.ts</files>
  <read_first>
    - examples/phoenix_host/e2e/offline_storage.spec.ts (lines 89 and 92 are the targets)
    - examples/phoenix_host/priv/static/offline_study.js (line 277 — confirm #btn-good is the live button; line 317 — the full storage-error string)
    - 113-PATTERNS.md § "offline_storage.spec.ts" (the two-line fix table) and 113-RESEARCH.md § "Pattern 7: Sibling Spec Fix (D-05)"
  </read_first>
  <action>
    Line 89: change `await page.click('#btn-pass')` to `await page.click('#btn-good')` — `#btn-pass` is a dead selector after Phase 112's D-01 button rename; `#btn-good` is live (offline_study.js:277, index.html.heex:87). This is a hard fix: the click currently times out and reds the whole Playwright lane.
    Line 92: extend the storage-error text locator from the partial `Device storage limit reached! Cannot save more progress.` to the full live string `Device storage limit reached! Cannot save more progress. Please free up space on your device.` (the full string is the app's actual message at offline_study.js:317). This is an honesty tightening — the partial passes today by substring; the full string closes a lie-by-omission.
    Do NOT touch any app code, HEEx, or data-testid — selector/text changes in the .spec.ts only.
  </action>
  <acceptance_criteria>
    - `offline_storage.spec.ts` contains `#btn-good` and does NOT contain `#btn-pass`
    - `offline_storage.spec.ts` line ~92 locator includes the full string `Please free up space on your device.`
    - `cd examples/phoenix_host && npx playwright test e2e/offline_storage.spec.ts` exits 0
  </acceptance_criteria>
  <verify>
    <automated>cd examples/phoenix_host && npx playwright test e2e/offline_storage.spec.ts</automated>
  </verify>
  <done>The sibling spec runs green; the dead #btn-pass selector is gone and the storage-error locator matches the full app string.</done>
</task>

<task type="auto">
  <name>Task 3: Extend sync-state controller — scoped count + test-only @moduledoc (D-02)</name>
  <files>examples/phoenix_host/lib/crosswake_example/e2e/sync_state_controller.ex</files>
  <read_first>
    - examples/phoenix_host/lib/crosswake_example/e2e/sync_state_controller.ex (the current show/2 — 16 lines; Repo and ReviewEvent already aliased; import Ecto.Query NOT present)
    - examples/phoenix_host/lib/crosswake_example/local_first/review_event.ex (the schema — client_mutation_id is the unique field being counted)
    - examples/phoenix_host/lib/crosswake_example/router.ex (lines ~378–383 — confirm the route is gated `if Mix.env() in [:test, :e2e]`; this is what the @moduledoc documents)
    - 113-PATTERNS.md § "sync_state_controller.ex" (the verbatim before/after) and 113-RESEARCH.md § "Pattern 4: Scoped Ecto Count"
  </read_first>
  <action>
    Extend the existing `show/2` (do NOT add a new function or route). Three additive changes, exactly as PATTERNS.md § "sync_state_controller.ex" shows:
    (1) Add a `@moduledoc` at the top of the module stating the endpoint is test-only, mounted only under :test/:e2e (reference router.ex ~line 378), never mounted in :prod, and noting Phase 114 GUARD-02 enforces the assertion. This pre-stages GUARD-02; do not add the route-gating assertion itself (that is Phase 114).
    (2) Add `import Ecto.Query, warn: false` after the `use Phoenix.Controller` line — it is not currently present and is required for `from/2`. Use `warn: false` so an unused-import does not trip the very --warnings-as-errors gate this milestone adds.
    (3) Compute a `count` SCOPED to the id and add it to both JSON responses: `count` via `from(r in ReviewEvent, where: r.client_mutation_id == ^id) |> Repo.aggregate(:count, :id)`. Return `count: 0` in the `nil` branch and `count: count` in the `record` branch. The scoping to `^id` is load-bearing — a bare `Repo.aggregate(ReviewEvent, :count, :id)` counts the whole table and returns > 1 in any multi-test run (the red-team-caught deterministic-failure bug). Add the inline comment noting the count MUST be scoped. Leave the existing `synced` and `status` fields unchanged.
  </action>
  <acceptance_criteria>
    - `sync_state_controller.ex` contains `from(r in ReviewEvent, where: r.client_mutation_id == ^id)` and `Repo.aggregate(:count, :id)`
    - `sync_state_controller.ex` contains `import Ecto.Query` and a `@moduledoc` whose body mentions test-only / `:test`/`:e2e`
    - `sync_state_controller.ex` does NOT contain a bare `Repo.aggregate(ReviewEvent, :count` (unscoped) form
    - `cd examples/phoenix_host && MIX_ENV=test mix compile --warnings-as-errors` exits 0 (no unused-import or other warning)
  </acceptance_criteria>
  <verify>
    <automated>cd examples/phoenix_host && MIX_ENV=test mix compile --warnings-as-errors && grep -q 'where: r.client_mutation_id == \^id' lib/crosswake_example/e2e/sync_state_controller.ex</automated>
  </verify>
  <done>The sync-state endpoint returns a scoped `count` for the given client_mutation_id and carries a test-only @moduledoc; the tree still compiles clean under --warnings-as-errors.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Playwright test / CI → `/_e2e/sync-state/:id` | Test-only HTTP read of server sync state; the only changed server surface this phase (a `count` field added to an existing handler) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-113-01 | Information Disclosure | `/_e2e/sync-state/:id` endpoint (now also returns row `count`) | mitigate | Endpoint is mounted only under `Mix.env() in [:test, :e2e]` (router.ex:378, compile-time gate, never :prod). The added `@moduledoc` documents this test-only purpose; Phase 114 GUARD-02 adds the enforced `routes_test.exs` assertion. No new route added. |
| T-113-02 | Tampering (injection) | `client_mutation_id` path param flowing into the Ecto count query | mitigate | `id` is bound as a named param and parameterized by Ecto via `^id` in `from(r in ReviewEvent, where: r.client_mutation_id == ^id)` — no string interpolation, no SQL-injection surface (ASVS V5). |
| T-113-SC | Tampering | npm/pip/cargo installs | accept | No packages installed this phase (RESEARCH § Package Legitimacy Audit is empty — all Playwright/Phoenix/Ecto deps already present). No supply-chain surface. |
</threat_model>

<verification>
- `cd examples/phoenix_host && MIX_ENV=test mix compile --warnings-as-errors` exits 0 (Tasks 1 + 3)
- `cd examples/phoenix_host && npx playwright test e2e/offline_storage.spec.ts` exits 0 (Task 2)
- `grep -q 'where: r.client_mutation_id == \^id' examples/phoenix_host/lib/crosswake_example/e2e/sync_state_controller.ex` (Task 3 — scoped count present)
</verification>

<success_criteria>
- The compile gate's pre-flight is green: `MIX_ENV=test mix compile --warnings-as-errors` passes (unblocks Plan 03).
- The Playwright lane is no longer red on the sibling spec (unblocks the full-suite green required by Plan 02 and Phase 114).
- The sync-state endpoint returns a scoped `count` field + test-only `@moduledoc` (unblocks Plan 02's duplicate-flush `count === 1` proof).
</success_criteria>

<output>
Create `.planning/phases/113-honest-e2e-rewrite-compile-gate/113-01-SUMMARY.md` when done
</output>
