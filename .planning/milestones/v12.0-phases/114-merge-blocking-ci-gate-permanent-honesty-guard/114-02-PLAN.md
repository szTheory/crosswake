---
phase: 114-merge-blocking-ci-gate-permanent-honesty-guard
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - script/check-e2e-honesty.mjs
  - examples/phoenix_host/package.json
autonomous: true
requirements: [GUARD-01]
must_haves:
  truths:
    - "script/check-e2e-honesty.mjs parses offline_sync.spec.ts via the TypeScript compiler API (AST, not regex) and exits non-zero on any fabrication shape"
    - "The script exits 1 if the spec file is missing (anti-rename/delete evasion)"
    - "It bans three shapes unconditionally with no OBSERVATION_ONLY allowlist: literal crosswake_offline_mutations; a network call inside a page.evaluate callback; crypto.randomUUID/uuid"
    - "Legitimate constructs pass: IndexedDB reads and window.dispatchEvent('online') inside page.evaluate, and the duplicate-flush request.post (APIRequestContext, outside page.evaluate)"
    - "The current honest offline_sync.spec.ts passes the check (exit 0)"
    - "typescript is pinned in examples/phoenix_host/package.json devDependencies as insurance"
  artifacts:
    - path: "script/check-e2e-honesty.mjs"
      provides: "AST-based structural honesty guard for the offline-sync spec"
      contains: "import ts from 'typescript'"
    - path: "examples/phoenix_host/package.json"
      provides: "Explicit typescript devDependency pin"
      contains: "typescript"
  key_links:
    - from: "script/check-e2e-honesty.mjs"
      to: "examples/phoenix_host/e2e/offline_sync.spec.ts"
      via: "ts.createSourceFile on the spec path; exit 1 if absent"
      pattern: "offline_sync.spec.ts"
---

<objective>
Build the GUARD-01 structural honesty check (D-04, D-05): a zero-new-dependency Node ESM
script that parses the offline-sync spec with the TypeScript compiler API and fails the
build if any of three fabrication shapes reappear — making it structurally impossible for
a future PR to silently revert the test to injection-based fakery.

Purpose: GUARD-01 must catch the regression shapes the honest 112–113 rewrite removed,
unconditionally (no bypassable allowlist), while leaving every legitimate observation
construct untouched.

Output: script/check-e2e-honesty.mjs + a typescript pin in the demo-app package.json.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/114-merge-blocking-ci-gate-permanent-honesty-guard/114-CONTEXT.md
@.planning/phases/114-merge-blocking-ci-gate-permanent-honesty-guard/114-RESEARCH-SYNTHESIS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Author the AST honesty check script</name>
  <files>script/check-e2e-honesty.mjs</files>
  <behavior>
    - PASS: current honest examples/phoenix_host/e2e/offline_sync.spec.ts → exit 0
    - FAIL (inject-global): a spec containing the string literal crosswake_offline_mutations → exit 1
    - FAIL (fetch-in-evaluate): a spec with fetch(/XMLHttpRequest/*.sendBeacon inside a page.evaluate callback → exit 1
    - FAIL (minted-uuid): a spec referencing crypto.randomUUID or a uuid import → exit 1
    - FAIL (missing-file): spec file absent → exit 1
    - NOT flagged: IndexedDB indexedDB.open reads and window.dispatchEvent(new Event('online')) inside page.evaluate; request.post (APIRequestContext) for the duplicate-flush, which is outside page.evaluate
  </behavior>
  <read_first>
    - .planning/phases/114-merge-blocking-ci-gate-permanent-honesty-guard/114-RESEARCH-SYNTHESIS.md §3 (the ready-to-lift ESM AST script — lift it)
    - examples/phoenix_host/e2e/offline_sync.spec.ts (the spec under scan: note OBSERVATION_ONLY page.evaluate blocks at lines ~17/27/52/64 that must keep passing, and the request.post duplicate-flush at ~line 84)
    - examples/phoenix_host/package.json (confirms @playwright/test provides typescript transitively)
  </read_first>
  <action>
    Lift the script from RESEARCH-SYNTHESIS §3. It must: `import ts from 'typescript'`;
    exit 1 with a clear message if examples/phoenix_host/e2e/offline_sync.spec.ts is absent;
    walk the AST and collect violations for exactly three rules — (1) any string literal
    matching crosswake_offline_mutations; (2) a network call (`fetch`/`XMLHttpRequest`
    identifiers, or `.fetch`/`.sendBeacon` property accesses) found by scanning INSIDE the
    first argument of a `.evaluate(...)` call expression; (3) any `randomUUID` property
    access (and treat a `uuid` import as the same tell). Do NOT key off the
    `// OBSERVATION_ONLY` comment — ban the shapes unconditionally (D-05). The fetch scan
    must descend only into the page.evaluate callback body so that IndexedDB reads and
    window.dispatchEvent in the SAME callback are NOT flagged. On any violation, print a
    failure message that teaches the WHY and references REQUIREMENTS.md GUARD-01 and the
    phase 112–113 honest rewrite, then process.exit(1); otherwise print a pass line and
    exit 0. AST (TypeScript compiler API) is mandatory — regex is rejected because two
    rules are scope/ordering-sensitive.
  </action>
  <verify>
    <automated>cd /Users/jon/projects/crosswake/examples/phoenix_host && npm ci >/dev/null 2>&1; cd /Users/jon/projects/crosswake && node script/check-e2e-honesty.mjs; echo "exit=$?"</automated>
  </verify>
  <acceptance_criteria>
    - Running `node script/check-e2e-honesty.mjs` against the current honest spec exits 0 (behavior assertion)
    - The script `import ts from 'typescript'` and calls `ts.createSourceFile` (source assertion — AST not regex)
    - Temporarily appending a line containing the literal crosswake_offline_mutations to a copy of the spec makes the check exit 1 (behavior assertion — executor demonstrates then reverts; do NOT commit the tampered spec)
    - The check exits 1 when the spec path does not exist (behavior assertion)
    - The `// OBSERVATION_ONLY` comment text is NOT used as a gate condition anywhere in the script (source assertion — no allowlist)
  </acceptance_criteria>
  <done>The honesty check passes the honest spec, fails each of the three fabrication shapes and the missing-file case, leaves legitimate page.evaluate observation untouched, and teaches the WHY on failure.</done>
</task>

<task type="auto">
  <name>Task 2: Pin typescript in the demo-app devDependencies</name>
  <files>examples/phoenix_host/package.json</files>
  <read_first>
    - examples/phoenix_host/package.json (current devDependencies: only @playwright/test ^1.40.0)
    - .planning/phases/114-merge-blocking-ci-gate-permanent-honesty-guard/114-RESEARCH-SYNTHESIS.md §3 (rationale: typescript resolves transitively via @playwright/test; pin explicitly as insurance against a future Playwright major dropping it)
  </read_first>
  <action>
    Add `typescript` to the `devDependencies` of examples/phoenix_host/package.json with a
    caret range matching the version Playwright currently resolves (inspect
    node_modules/typescript/package.json after npm ci, or use a recent stable major such as
    ^5.x consistent with the resolved tree). This is insurance only — the honesty check
    already works via the transitive copy. Keep package-lock.json consistent if one exists
    (run `npm install` in examples/phoenix_host so the lockfile records the explicit dep).
  </action>
  <verify>
    <automated>cd /Users/jon/projects/crosswake && grep -q '"typescript"' examples/phoenix_host/package.json && node -e "require('/Users/jon/projects/crosswake/examples/phoenix_host/package.json')" </automated>
  </verify>
  <acceptance_criteria>
    - package.json devDependencies contains a `typescript` entry (source assertion)
    - package.json remains valid JSON (behavior assertion: node require succeeds)
    - The pinned range is satisfiable by the version @playwright/test resolves (behavior assertion: npm install succeeds with no peer-conflict error)
  </acceptance_criteria>
  <done>typescript is an explicit devDependency; package.json is valid; the lockfile (if present) records it.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| future PR → offline_sync.spec.ts | A future edit could re-introduce injection-based fabrication; the AST guard is the structural barrier crossing this boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-114-04 | Tampering | offline_sync.spec.ts reverting to fabrication | mitigate | check-e2e-honesty.mjs bans the three fabrication shapes via AST unconditionally; exit 1 on missing spec defeats rename/delete evasion |
| T-114-05 | Tampering | bypass via OBSERVATION_ONLY allowlist | mitigate | guard keys on structural shapes, NOT on the bypassable `// OBSERVATION_ONLY` comment (D-05) |
| T-114-06 | Repudiation | silent guard breakage if Playwright drops typescript | mitigate | explicit typescript devDependency pin so the AST parser cannot vanish silently |
| T-114-SC | Tampering | npm devDependency install (typescript) | mitigate | typescript is a first-party, ASSUMED-legit toolchain dep already present transitively via @playwright/test; pin matches the resolved version — see Package Legitimacy note below |
</threat_model>

<verification>
- node script/check-e2e-honesty.mjs exits 0 on the honest spec
- Each fabrication shape and the missing-file case force exit 1
- typescript appears in devDependencies; package.json valid
</verification>

<success_criteria>
The honesty check is AST-based, bans the three fabrication shapes unconditionally with no allowlist, exits 1 on a missing spec, passes the current honest spec, and typescript is pinned as insurance.
</success_criteria>

<output>
Create `.planning/phases/114-merge-blocking-ci-gate-permanent-honesty-guard/114-02-SUMMARY.md` when done
</output>
