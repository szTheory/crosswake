---
phase: 132-generalization-proof-rindle-compat-matrix
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/crosswake_rindle/mix.exs
  - packages/crosswake_rindle/test/test_helper.exs
  - packages/crosswake_rindle/config/config.exs
  - packages/crosswake_rindle/test/engine_present/rindle.ex
  - packages/crosswake_rindle/README.md
  - packages/crosswake_rindle/CHANGELOG.md
  - packages/crosswake_rindle/LICENSE
  - test/support/stub_companion.ex
autonomous: true
requirements:
  - EXTRACT-07
user_setup: []

must_haves:
  truths:
    - "packages/crosswake_rindle/mix.exs exists with the env-conditional crosswake_dep/0 resolver so the drift test (132-02) can AST-parse a real Hex requirement and the non-vacuity guard counts >= 2 companion packages"
    - "Crosswake.TestSupport.StubRindleAbsentCompanion exists implementing the full Crosswake.Companion behaviour with companion_id :rindle, so core seam tests (132-03) can register a rindle-shaped companion without aliasing the about-to-move adapter"
  artifacts:
    - packages/crosswake_rindle/mix.exs
    - packages/crosswake_rindle/test/test_helper.exs
    - packages/crosswake_rindle/config/config.exs
    - packages/crosswake_rindle/test/engine_present/rindle.ex
    - test/support/stub_companion.ex
  key_links:
    - "crosswake_dep/0 in packages/crosswake_rindle/mix.exs is the exact AST shape the 132-02 drift test parses; the do: branch literal {:crosswake, \"~> 0.1\"} must match the matrix doc row verbatim"
    - "StubRindleAbsentCompanion.companion_id/0 == :rindle wires Doctor findings to check == companion.rindle so 132-03 seam rewrites assert through the registry, not a direct module alias"
---

<objective>
Scaffold the `packages/crosswake_rindle/` Hex package skeleton (mix.exs + test
infrastructure, NO source moved yet) and add `StubRindleAbsentCompanion` to core
test support. This is the Wave 0 foundation from VALIDATION.md: the drift test
(132-02) needs a real `packages/crosswake_rindle/mix.exs` to count >= 2 packages
and AST-parse a Hex requirement, and the seam rewrites (132-03) need the stub to
register a rindle-shaped companion without aliasing the soon-to-move adapter.

This plan follows `script/extract_companion.md` Steps 4, 6 (skeleton + engine
stub) and the rulestead copy-template. The actual source move is 132-03.

Purpose: Establish the package shell and stub so the parallel Wave-2 plans
(matrix/drift + extraction) both have a real package directory to assert against.
Output: A compiling `packages/crosswake_rindle/` skeleton (no companion source
yet) and `StubRindleAbsentCompanion`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/132-generalization-proof-rindle-compat-matrix/132-PATTERNS.md
@.planning/phases/132-generalization-proof-rindle-compat-matrix/132-RESEARCH.md
@script/extract_companion.md

# Copy-templates (read the real shape, substitute rindle):
@packages/crosswake_rulestead/mix.exs
@packages/crosswake_rulestead/test/test_helper.exs
@test/support/stub_companion.ex
</context>

<artifacts_this_phase_produces>
This plan creates these NEW symbols (downstream drift/guard verification must not
flag them as unexpected): `CrosswakeRindle.MixProject`, the
`packages/crosswake_rindle/mix.exs` `crosswake_dep/0` resolver, the engine-present
fake `Rindle` stub module, and `Crosswake.TestSupport.StubRindleAbsentCompanion`.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold packages/crosswake_rindle/ skeleton from the rulestead copy-template</name>
  <read_first>
    - packages/crosswake_rulestead/mix.exs (full file — the exact copy-template; PATTERNS.md §packages/crosswake_rindle/mix.exs reproduces lines 1-119 with substitutions)
    - packages/crosswake_rulestead/test/test_helper.exs:1 (verbatim copy, no substitution)
    - packages/crosswake_rulestead/config/config.exs (the test flag_source wiring shape, extract_companion.md Step 5)
    - packages/crosswake_rulestead/test/support/engine_present/rulestead.ex (the fake engine stub shape, extract_companion.md Step 6)
    - packages/crosswake_rulestead/README.md, CHANGELOG.md, LICENSE (the files: allowlist members — copy-substitute)
  </read_first>
  <files>packages/crosswake_rindle/mix.exs, packages/crosswake_rindle/test/test_helper.exs, packages/crosswake_rindle/config/config.exs, packages/crosswake_rindle/test/engine_present/rindle.ex, packages/crosswake_rindle/README.md, packages/crosswake_rindle/CHANGELOG.md, packages/crosswake_rindle/LICENSE</files>
  <action>
    Copy the entire packages/crosswake_rulestead/ skeleton structure to
    packages/crosswake_rindle/, substituting CrosswakeRulestead -> CrosswakeRindle,
    crosswake_rulestead -> crosswake_rindle, rulestead -> rindle, Rulestead -> Rindle
    throughout. Do NOT copy any lib/ source or any moved test files — those land in
    132-03. Create only the skeleton: mix.exs, test/test_helper.exs, config/config.exs,
    test/engine_present/rindle.ex, README.md, CHANGELOG.md, LICENSE.

    mix.exs requirements (per script/extract_companion.md Step 4 + PATTERNS.md):
    - Module CrosswakeRindle.MixProject; app: :crosswake_rindle; name "crosswake_rindle".
    - @version "0.1.0" with the marker comment x-release-please-version on the SAME line
      as @version (per D-22 / 131 recipe; the comment is required for release-please).
    - deps/0 returns [crosswake_dep(), {:rindle, "~> 0.1", optional: true}]. The engine
      cap stays ~> 0.1 per D-16 (rindle 0.3.0 is outside ~> 0.1; widening is deferred).
    - crosswake_dep/0 is the env-conditional resolver: when CROSSWAKE_RELEASE == "1"
      return {:crosswake, "~> 0.1"}, else {:crosswake, path: "../.."}. This exact shape
      is the AST parse target for the 132-02 drift test — match it to the rulestead
      literal verbatim (per D-11).
    - elixirc_paths(:test) appends test/engine_present only when ENGINE_PRESENT_LANE == "1"
      (D-33); elixirc_paths(_) returns ["lib"].
    - aliases/0 defines engine-present.test running clean then ENGINE_PRESENT_LANE=1
      mix test --only engine_present.
    - package/0 files: allowlist is lib mix.exs README.md LICENSE CHANGELOG.md — test/
      EXCLUDED (D-24); links point at hexdocs.pm/crosswake_rindle and the shared repo.

    test/test_helper.exs: copy verbatim — ExUnit.start excluding :engine_present,
    :collateral_binaries, :advisory_only.

    config/config.exs: wire the test flag_source to the rindle companion mock if the
    rulestead analog does so; if rindle has no flag-source mock, mirror whatever the
    rulestead config.exs contains, substituting names. Keep it minimal — only what the
    moved tests (132-03) will need.

    test/engine_present/rindle.ex: a fake top-level Rindle stub module (empty body with
    a moduledoc saying it is a fake stub compiled ONLY under ENGINE_PRESENT_LANE=1, NOT a
    real Rindle). Per extract_companion.md Step 6.

    Do NOT add crosswake_rindle to release-please-config.json, the manifest, or
    release-please.yml here — that is 132-04 (recipe Step 12 is Phase-131+ only). Do NOT
    add Crosswake.Companions.Rindle to CompanionGuard yet — that lands with the source
    move in 132-03 (same-PR rule).
  </action>
  <verify>
    <automated>test -f packages/crosswake_rindle/mix.exs && grep -q 'x-release-please-version' packages/crosswake_rindle/mix.exs && grep -q 'crosswake_dep' packages/crosswake_rindle/mix.exs && grep -q 'rindle.*~> 0.1.*optional: true' packages/crosswake_rindle/mix.exs && test "$(ls packages/crosswake_*/mix.exs 2>/dev/null | wc -l | tr -d ' ')" -ge 2 && echo SKELETON_OK</automated>
  </verify>
  <done>
    packages/crosswake_rindle/ contains mix.exs (with x-release-please-version marker,
    crosswake_dep/0 env-conditional resolver, {:rindle, "~> 0.1", optional: true} engine
    cap, ENGINE_PRESENT_LANE elixirc_paths, files allowlist excluding test/),
    test/test_helper.exs, config/config.exs, test/engine_present/rindle.ex, README.md,
    CHANGELOG.md, LICENSE. No lib/ source present yet. Path.wildcard over
    packages/crosswake_*/mix.exs returns >= 2.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add StubRindleAbsentCompanion to core test support</name>
  <read_first>
    - test/support/stub_companion.ex (the StubRulesteadAbsentCompanion definition — the exact analog; PATTERNS.md §test/support/stub_companion.ex reproduces the full shape including the report_state/0 Application.get_env line the RESEARCH.md draft omitted)
    - lib/crosswake/companion/state.ex (the Crosswake.Companion.State struct fields, to populate report_state/0 correctly)
  </read_first>
  <files>test/support/stub_companion.ex</files>
  <action>
    Append a Crosswake.TestSupport.StubRindleAbsentCompanion module to
    test/support/stub_companion.ex, immediately after the existing
    StubRulesteadAbsentCompanion. Copy the rulestead stub exactly, substituting
    :rulestead -> :rindle, Rulestead -> Rindle, [:"Elixir.Rulestead"] -> [:"Elixir.Rindle"].

    The stub @behaviour Crosswake.Companion and implements all callbacks the existing
    StubRulesteadAbsentCompanion implements (companion_id/0 -> :rindle, enabled?/1,
    route_gated?/2, kill_switch_active?/1, validate_dependency/0 -> {:error,
    [:"Elixir.Rindle"]}, report_state/0). For report_state/0, copy the rulestead pattern
    EXACTLY including config = Application.get_env(:crosswake, :rindle, %{}) and reading
    enabled from that config (PATTERNS.md flags that the RESEARCH.md draft dropped this
    line — use the rulestead analog, not the RESEARCH.md draft). validate_dependency
    returns {:error, [:"Elixir.Rindle"]} because rindle is absent from core deps.

    Match the existing @moduledoc voice; the moduledoc should state this stands in for
    Crosswake.Companions.Rindle in core tests after Phase 132 extracts the real adapter.
  </action>
  <verify>
    <automated>mix compile --warnings-as-errors 2>&1 | tail -5 && grep -q 'StubRindleAbsentCompanion' test/support/stub_companion.ex && echo STUB_OK</automated>
  </verify>
  <done>
    Crosswake.TestSupport.StubRindleAbsentCompanion exists in
    test/support/stub_companion.ex, implements the full Crosswake.Companion behaviour
    with companion_id/0 == :rindle and validate_dependency/0 == {:error,
    [:"Elixir.Rindle"]}, and the core project compiles with --warnings-as-errors.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Hex registry -> companion engine dep | `{:rindle, "~> 0.1", optional: true}` resolves an external package author-controlled (`szTheory`) at adopter/CI build time |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-132-01 | Spoofing | `rindle` Hex engine dep | medium | mitigate | `rindle` confirmed on Hex, same author `szTheory`, versions 0.1.4–0.1.10 + 0.3.0; engine cap `~> 0.1` is no wildcard — resolves to a known `0.1.x`, cannot silently pull a hijacked major (RESEARCH.md §Security Domain) |
| T-132-02 | Tampering | engine-present test stub leaking into engine-absent lane | low | mitigate | engine_present stub compiled ONLY under `ENGINE_PRESENT_LANE=1` via conditional `elixirc_paths/1` (D-33); the lane runs `mix clean` first (132-03 CI) |
| T-132-SC | Tampering | npm/pip/cargo installs | low | accept | No package-manager installs in this plan — only an in-tree skeleton copy and a stub module; no new external packages added (RESEARCH.md §Package Legitimacy Audit: none) |
</threat_model>

<verification>
- `packages/crosswake_rindle/mix.exs` exists with the env-conditional `crosswake_dep/0`
  and the `x-release-please-version` marker.
- `Path.wildcard("packages/crosswake_*/mix.exs")` returns >= 2 (non-vacuity precondition
  for 132-02).
- `mix compile --warnings-as-errors` is clean with `StubRindleAbsentCompanion` present.
</verification>

<success_criteria>
- The `packages/crosswake_rindle/` skeleton compiles; no companion source moved yet.
- `StubRindleAbsentCompanion` is registerable and reports `:rindle`.
- The package directory count is >= 2 so the Wave-2 drift test cannot pass vacuously.
</success_criteria>

<output>
Create `.planning/phases/132-generalization-proof-rindle-compat-matrix/132-01-SUMMARY.md` when done
</output>
