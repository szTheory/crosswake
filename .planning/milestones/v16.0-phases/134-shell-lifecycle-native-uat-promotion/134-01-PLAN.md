---
phase: 134-shell-lifecycle-native-uat-promotion
plan: 01
type: execute
wave: 1
depends_on: ["134-00"]
files_modified:
  - lib/mix/tasks/crosswake.gen.shell.ex
  - priv/templates/crosswake/shell/ios/CrosswakeShellApp.swift.eex
  - priv/templates/crosswake/shell/ios/CrosswakeCoordinator.swift.eex
  - priv/templates/crosswake/shell/ios/Info.plist.eex
  - priv/templates/crosswake/shell/ios/CrosswakeShell.entitlements.eex
  - priv/templates/crosswake/shell/ios/PrivacyInfo.xcprivacy.eex
  - priv/templates/crosswake/shell/ios/CrosswakeShell.xcodeproj/xcshareddata/xcschemes/CrosswakeShell.xcscheme.eex
  - priv/templates/crosswake/shell/android/build.gradle.eex
  - priv/templates/crosswake/shell/android/settings.gradle.eex
  - priv/templates/crosswake/shell/android/gradle.properties.eex
  - priv/templates/crosswake/shell/android/app/build.gradle.eex
  - priv/templates/crosswake/shell/android/app/src/main/java/dev/crosswake/shell/MainActivity.kt.eex
  - priv/templates/crosswake/shell/android/app/src/main/java/dev/crosswake/shell/CrosswakeViewModel.kt.eex
  - priv/templates/crosswake/shell/android/app/src/main/AndroidManifest.xml.eex
  - priv/templates/crosswake/shell/android/app/src/main/res/values/themes.xml.eex
  - priv/templates/crosswake/shell/android/gradle/wrapper/gradle-wrapper.properties.eex
  - lib/mix/tasks/crosswake.bump_template_version.ex
  - test/crosswake/proof/phase134_template_version_drift_test.exs
  - test/mix/tasks/crosswake_gen_shell_stamp_test.exs
autonomous: true
requirements: [LIFE-02a]
must_haves:
  truths:
    - "A generated shell file carries a readable 2-line stamp: producing crosswake version + integer template_version (D-02)"
    - "Each generation run writes the manifest at the PER-PLATFORM generated root <target>/native/<platform>/crosswake_shell/.crosswake/shell.json with crosswake_version, template_version, platform, generated_at, params (D-03; REVIEW FIX finding 1)"
    - "Mix.Tasks.Crosswake.Gen.Shell.template_version/0 is a public accessor — the single source of truth Plans 02/03 compare against (REVIEW FIX finding 3)"
    - "The drift test fails :merge_blocking if any .eex template changes without bumping @template_version (D-04)"
    - "mix crosswake.bump_template_version increments the integer AND rewrites the checked-in hash in one command (D-05)"
  artifacts:
    - "@template_version module attribute in lib/mix/tasks/crosswake.gen.shell.ex (integer, starts at 1)"
    - "public Mix.Tasks.Crosswake.Gen.Shell.template_version/0 accessor (REVIEW FIX finding 3)"
    - "2-line stamp header in the 15 stamp-eligible .eex templates (derived from @ios_templates/@android_templates incl. gradle.properties, excl. pbxproj/gradlew/gradlew.bat and the never-rendered README .eex — REVIEW FIX finding 4)"
    - "generated-output stamp test test/mix/tasks/crosswake_gen_shell_stamp_test.exs (REVIEW FIX finding 4)"
    - ".crosswake/shell.json manifest written via File.write!/2 at the per-platform generated root"
    - lib/mix/tasks/crosswake.bump_template_version.ex
    - "checked-in SHA-256 hash in phase134_template_version_drift_test.exs (real, non-placeholder)"
  key_links:
    - "@template_version assign must reach EEx.eval_file in render_template/4 so templates can interpolate it; template_version/0 exposes the same attribute publicly for Plans 02/03"
    - "Manifest write MUST use File.write!/2 not ensure_file/2 (ensure_file skips when present → stale manifest, D-03 pitfall 3), and lands at <target>/native/<platform>/crosswake_shell/.crosswake/shell.json (the per-platform root, gen.shell.ex:82/103)"
    - "Stamp inventory MUST be derived from @ios_templates/@android_templates (the actually-rendered set) — gradle.properties IS stamped, README .eex are NOT (never rendered; README comes from shell_readme/1)"
    - "bump_template_version's hash recompute MUST match the drift test's live_template_hash/0 byte-for-byte (sorted glob, sha256, lowercase hex)"
---

<objective>
Keystone of Phase 134 (D-01..D-05). Stamp every generated shell file with the producing crosswake version + an integer template_version epoch, write a `.crosswake/shell.json` provenance manifest on every generation run, make the drift test catch un-bumped template changes, and ship `mix crosswake.bump_template_version` as the one-command bump ceremony. All of status/diff (Plans 03/04) depend on this manifest + stamp.

Purpose: LIFE-02a — give hosts a readable provenance stamp and a machine-readable manifest, and enforce version-bump discipline with a merge-blocking drift test.
Output: modified gen.shell.ex (@template_version, stamp assign, manifest write, NO line-254 change here), 17 stamped templates, a new bump task, and a real checked-in hash that turns Plan 00's drift test GREEN.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/134-shell-lifecycle-native-uat-promotion/134-CONTEXT.md
@.planning/phases/134-shell-lifecycle-native-uat-promotion/134-PATTERNS.md
@lib/mix/tasks/crosswake.gen.shell.ex
@lib/mix/tasks/crosswake.demo.ex
</context>

<artifacts_this_phase_produces>
This plan creates (full phase inventory across all plans is in 134-04-PLAN.md):
- `@template_version` module attribute (integer, starts at `1`) in `lib/mix/tasks/crosswake.gen.shell.ex`
- **public `Mix.Tasks.Crosswake.Gen.Shell.template_version/0` accessor** (REVIEW FIX finding 3 — the single source of truth Plans 02/03 read)
- 2-line stamp comment header in the **15** stamp-eligible `.eex` templates, derived from `@ios_templates`/`@android_templates` (incl. `gradle.properties`; excl. `project.pbxproj`/`gradlew`/`gradlew.bat`; the README `.eex` are NOT stamped — never rendered) (REVIEW FIX finding 4)
- generated-output stamp test `test/mix/tasks/crosswake_gen_shell_stamp_test.exs` (REVIEW FIX finding 4)
- `.crosswake/shell.json` manifest at the **per-platform generated root** (`<target>/native/<platform>/crosswake_shell/.crosswake/shell.json`) + fields: `crosswake_version`, `template_version`, `platform`, `generated_at`, `params{router,local,target}`, plus a leading do-not-hand-edit comment key (REVIEW FIX finding 1)
- `mix crosswake.bump_template_version` task
- real checked-in SHA-256 hash in `phase134_template_version_drift_test.exs`
</artifacts_this_phase_produces>

<review_feedback_addressed>
Cross-AI review (134-REVIEWS.md) resolution for this plan:
- **Finding 1 [HIGH — both] — manifest path.** INCORPORATED (Task 1 behavior/action/verify/acceptance +
  must_haves): manifest lands at the per-platform generated root
  `<target>/native/<platform>/crosswake_shell/.crosswake/shell.json` (gen.shell.ex:82/103), matching what
  Plan 02 probes. The Plan-01 action already wrote under `root`; the misleading `DIR/.crosswake/shell.json`
  acceptance/verify text is corrected. Consistent with D-03's "native project root" intent.
- **Finding 3 [MEDIUM — both] — shared template-version source.** INCORPORATED (Task 1): added a public
  `template_version/0` accessor in this plan (Plan 02's files_modified excludes gen.shell.ex, so it MUST be
  added here). Plans 02/03 consume it instead of parsing source or duplicating the integer.
- **Finding 4 [HIGH — Codex] — wrong stamp set.** INCORPORATED (Task 2): stamp inventory derived from
  `@ios_templates`/`@android_templates`; `gradle.properties.eex` is now stamped (it IS rendered);
  README `.eex` are NOT stamped (never rendered — generated README comes from `shell_readme/1`); added a
  generated-output test asserting every comment-supporting generated file carries a stamp (incl.
  gradle.properties) and the three exclusions do not.
</review_feedback_addressed>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add @template_version + stamp assign + manifest write to gen.shell.ex</name>
  <read_first>
    - lib/mix/tasks/crosswake.gen.shell.ex (the file being modified — @switches ~14, render_template ~148-160, ensure_file ~261-275, fetch_version! ~199)
    - 134-PATTERNS.md "crosswake.gen.shell.ex (MODIFY)" section (lines 280-341)
    - 134-RESEARCH.md RQ2 "Manifest + Re-render Assigns" (lines 299-359)
  </read_first>
  <files>lib/mix/tasks/crosswake.gen.shell.ex</files>
  <behavior>
    - After `mix crosswake.gen.shell android --target DIR`: the manifest exists at the PER-PLATFORM GENERATED ROOT — `DIR/native/android/crosswake_shell/.crosswake/shell.json` (iOS → `DIR/native/ios/crosswake_shell/.crosswake/shell.json`) — with platform "android", integer template_version, crosswake_version string, generated_at ISO8601, and params{router,local,target}. (REVIEW FIX, finding 1: NOT at the bare `DIR` target root — the generator roots are `<target>/native/<platform>/crosswake_shell`, gen.shell.ex:82/103.)
    - Re-running generation overwrites the manifest (File.write!/2 semantics), never skips it.
    - The template_version integer is available to every template render as the `@template_version` assign AND is exposed as a public `Mix.Tasks.Crosswake.Gen.Shell.template_version/0` accessor (REVIEW FIX, finding 3) so Plan 02 status / Plan 03 diff read ONE source of truth, not a parsed/duplicated integer.
  </behavior>
    REVIEW FIX (finding 3, both reviewers MEDIUM) — public template_version accessor: also add a
    PUBLIC, documented `@spec template_version() :: pos_integer()` function
    `def template_version, do: @template_version` to `Mix.Tasks.Crosswake.Gen.Shell`. This is the
    SINGLE source of truth that Plan 02 (`shell.status`) compares the stamped manifest version
    against, and that Plan 03 (`--diff`) / any future bump test consume. Plans 02/03 MUST call
    `Mix.Tasks.Crosswake.Gen.Shell.template_version/0` — NEVER re-parse source or duplicate the
    integer. (Plan 02's files_modified does not include this file, which is exactly why the accessor
    must be PUBLIC and added HERE in Plan 01.)

  <action>
    Add a module attribute `@template_version 1` (integer epoch, decoupled from library SemVer per D-01). Pass it into the assigns list in render_template/4 alongside the existing `capabilities`, `local`, `version` so templates can interpolate `<%= @template_version %>` (D-02). Do NOT alter the existing assigns; append `template_version: @template_version`.

    Add a PUBLIC accessor `def template_version, do: @template_version` (with `@spec template_version() :: pos_integer()` and a one-line `@doc`) — REVIEW FIX finding 3. This is the only sanctioned way for `shell.status` (Plan 02) and `--diff` (Plan 03) to read the live current epoch; they must not parse the module source or copy the integer.

    Add a manifest write at the end of the per-platform generation flow (after files are written, in the non-diff path): build a map with keys `crosswake_version` (from fetch_version!/0), `template_version` (@template_version), `platform` (the platform atom/string), `generated_at` (DateTime.utc_now |> DateTime.to_iso8601), and a `params` sub-map capturing `router` (the --router opt value or null), `local` (the --local boolean), `target` (the resolved target/root path). Include a leading do-not-hand-edit marker key (e.g. `_comment: "do not hand-edit — regenerate to update"`) per copier discipline (D-03). Encode with Jason.encode!(manifest, pretty: true) and write to `Path.join([root, ".crosswake", "shell.json"])` using `File.mkdir_p!/1` + `File.write!/2` — NOT ensure_file/2 (ensure_file skips when present → stale manifest, pitfall 3). NOTE (REVIEW FIX finding 1): `root` here is the per-platform generated root (`<target>/native/ios/crosswake_shell` or `<target>/native/android/crosswake_shell`, gen.shell.ex:82/103), so the manifest lands at `<generated shell root>/.crosswake/shell.json` — NOT at the bare `<target>` dir. This is the canonical manifest location for the whole phase; Plan 02 probes exactly these per-platform roots. The manifest path must NOT be gitignored (D-03); do not add it to any ignore file.

    Do NOT touch line 254 (the placeholder) in this plan — that is Plan 04's edit (LIFE-02c). Do NOT add the --diff switch here — that is Plan 03.
  </action>
  <verify>
    <!-- REVIEW FIX (finding 1): manifest is at the PER-PLATFORM root, not the bare target dir. -->
    <automated>cd /tmp && rm -rf cw_stamp_test && mkdir cw_stamp_test && (cd /Users/jon/projects/crosswake && mix crosswake.gen.shell android --target /tmp/cw_stamp_test) && test -f /tmp/cw_stamp_test/native/android/crosswake_shell/.crosswake/shell.json && grep -q '"template_version"' /tmp/cw_stamp_test/native/android/crosswake_shell/.crosswake/shell.json && grep -q '"platform"' /tmp/cw_stamp_test/native/android/crosswake_shell/.crosswake/shell.json && (cd /Users/jon/projects/crosswake && mix run -e 'IO.puts(Mix.Tasks.Crosswake.Gen.Shell.template_version())') | grep -qE '^[0-9]+$' && echo "manifest written at per-platform root with required fields; template_version/0 accessor public"</automated>
  </verify>
  <acceptance_criteria>
    - `mix crosswake.gen.shell android --target /tmp/cw_stamp_test` writes `/tmp/cw_stamp_test/native/android/crosswake_shell/.crosswake/shell.json` (per-platform generated root — REVIEW FIX finding 1, NOT the bare `--target` dir).
    - The manifest JSON contains keys `crosswake_version`, `template_version`, `platform`, `generated_at`, and a `params` object with `router`/`local`/`target`.
    - `Mix.Tasks.Crosswake.Gen.Shell.template_version/0` is public and returns the integer epoch (REVIEW FIX finding 3).
    - Re-running generation overwrites (not skips) the manifest — second run's `generated_at` differs.
    - `mix compile --warnings-as-errors` succeeds.
  </acceptance_criteria>
  <done>gen.shell.ex defines @template_version 1, passes it as an assign, and writes the .crosswake/shell.json manifest via File.write!/2 on every generation run.</done>
</task>

<task type="auto">
  <name>Task 2: Add 2-line stamp header to all 17 stamp-eligible templates</name>
  <read_first>
    - 134-RESEARCH.md RQ6 "Comment Header Syntax Per Template" (lines 559-627) — exact per-file comment syntax + the no-stamp exclusions
    - 134-PATTERNS.md "priv/templates/.../*.eex (MODIFY)" section (lines 345-377)
    - priv/templates/crosswake/shell/ios/Info.plist.eex (to place the XML comment AFTER the <?xml ?> declaration, not before)
  </read_first>
  <files>priv/templates/crosswake/shell/ios/CrosswakeShellApp.swift.eex, priv/templates/crosswake/shell/ios/CrosswakeCoordinator.swift.eex, priv/templates/crosswake/shell/ios/Info.plist.eex, priv/templates/crosswake/shell/ios/CrosswakeShell.entitlements.eex, priv/templates/crosswake/shell/ios/PrivacyInfo.xcprivacy.eex, priv/templates/crosswake/shell/ios/CrosswakeShell.xcodeproj/xcshareddata/xcschemes/CrosswakeShell.xcscheme.eex, priv/templates/crosswake/shell/android/build.gradle.eex, priv/templates/crosswake/shell/android/settings.gradle.eex, priv/templates/crosswake/shell/android/gradle.properties.eex, priv/templates/crosswake/shell/android/app/build.gradle.eex, priv/templates/crosswake/shell/android/app/src/main/java/dev/crosswake/shell/MainActivity.kt.eex, priv/templates/crosswake/shell/android/app/src/main/java/dev/crosswake/shell/CrosswakeViewModel.kt.eex, priv/templates/crosswake/shell/android/app/src/main/AndroidManifest.xml.eex, priv/templates/crosswake/shell/android/app/src/main/res/values/themes.xml.eex, priv/templates/crosswake/shell/android/gradle/wrapper/gradle-wrapper.properties.eex, test/mix/tasks/crosswake_gen_shell_stamp_test.exs</files>
  <action>
    REVIEW FIX (finding 4 [HIGH — Codex] — stamp the RIGHT template set): derive the stamp inventory
    from the generator's OWN `@ios_templates` and `@android_templates` lists (gen.shell.ex:17-43), NOT
    from a hand-typed file list. The generator only renders the files in those two lists. Two corrections
    versus the prior plan revision, both verified against live source:
      (a) `android/gradle.properties.eex` IS in `@android_templates` (gen.shell.ex:20) — it IS generated
          and MUST be stamped (`#` comment). The prior "do not edit gradle.properties" instruction was
          wrong.
      (b) `ios/README.md.eex` and `android/README.md.eex` are NOT in either template list — they exist on
          disk but the generator NEVER renders them; the generated README comes from the `shell_readme/1`
          STRING LITERAL (gen.shell.ex:88/106/235-256). Stamping the README .eex files would stamp dead
          templates while the actual generated READMEs stay unstamped. So: do NOT stamp the README .eex
          files. (If a generated-output stamp on the README is desired, stamp it inside `shell_readme/1`'s
          string — but that is OPTIONAL; the README is human-prose and not a comment-supporting code file,
          so it is acceptable to leave the generated README unstamped. Whichever you choose, the
          generated-output test below is the authority.)

    Stamp-eligible set = (every entry in `@ios_templates` ++ `@android_templates`) MINUS the D-02 no-stamp
    exclusions. That is the 15 templates named in <files> above. Add the 2-line provenance stamp to the top
    of each, interpolating the assigns: line 1 = "Generated by crosswake <%= @version %> (template_version:
    <%= @template_version %>)", line 2 = "Host-owned after generation. See guides/native_shell_upgrade.md".
    Never write a "DO NOT EDIT" header — it contradicts the host-owned contract in guides/native_shell.md (D-02).

    Comment syntax by file type: Swift/Kotlin/Groovy (`build.gradle`, `settings.gradle`, `app/build.gradle`, `MainActivity.kt`, `CrosswakeViewModel.kt`, `CrosswakeShellApp.swift`, `CrosswakeCoordinator.swift`) → `//` prefix, stamp as the FIRST lines. Properties (`gradle.properties`, `gradle-wrapper.properties`) → `#` prefix, FIRST lines. XML/plist (`Info.plist`, `CrosswakeShell.entitlements`, `PrivacyInfo.xcprivacy`, `CrosswakeShell.xcscheme`, `AndroidManifest.xml`, `themes.xml`) → `<!-- ... -->` placed immediately AFTER the existing `<?xml ... ?>` declaration line (never before it — a comment before the prolog is malformed XML).

    EXCLUDE entirely (no stamp, per D-02): `ios/CrosswakeShell.xcodeproj/project.pbxproj.eex` (Xcode-managed), `android/gradlew.eex` and `android/gradlew.bat.eex` (shell/batch parse risk). These three ARE in the template lists but are the documented D-02 stamp exclusions.

    GENERATED-OUTPUT TEST (REVIEW FIX finding 4 — Codex's "add a generated-output test"): create
    `test/mix/tasks/crosswake_gen_shell_stamp_test.exs`. It generates both shells into a tmp dir, then
    asserts EVERY generated comment-supporting file carries the `Generated by crosswake` + `template_version:`
    stamp, by enumerating the generator's `@ios_templates`/`@android_templates` relative output paths
    (reference them via the module or re-derive the same list in the test) MINUS the three exclusions, and
    File.read!-checking each generated file contains the stamp. The test MUST positively include
    `app/src/main/.../gradle.properties` (regression guard for correction (a)) and MUST assert the three
    excluded files (`project.pbxproj`, `gradlew`, `gradlew.bat`) do NOT contain the stamp. Async-safe (own tmp dir).

    Because adding the stamp changes the template bytes, the drift test will go further RED until Task 4 recomputes the hash — that is expected and correct.
  </action>
  <verify>
    <automated>cd /tmp && rm -rf cw_stamp2 && (cd /Users/jon/projects/crosswake && mix crosswake.gen.shell android --target /tmp/cw_stamp2 && mix crosswake.gen.shell ios --target /tmp/cw_stamp2) && grep -rq "Generated by crosswake" /tmp/cw_stamp2/native/android && grep -rq "Generated by crosswake" /tmp/cw_stamp2/native/ios && grep -q "Generated by crosswake" /tmp/cw_stamp2/native/android/crosswake_shell/gradle.properties && ! grep -q "Generated by crosswake" /tmp/cw_stamp2/native/ios/crosswake_shell/CrosswakeShell.xcodeproj/project.pbxproj && ! grep -q "Generated by crosswake" /tmp/cw_stamp2/native/android/crosswake_shell/gradlew && (cd /Users/jon/projects/crosswake && mix test test/mix/tasks/crosswake_gen_shell_stamp_test.exs 2>&1 | grep -qE "0 failures") && echo "stamps present incl gradle.properties, absent in pbxproj/gradlew, generated-output test GREEN"</automated>
  </verify>
  <acceptance_criteria>
    - The generated `gradle.properties` contains the stamp `# Generated by crosswake ... (template_version: N)` (REVIEW FIX finding 4 correction (a)).
    - A generated Android code file (e.g. `MainActivity.kt`) contains `// Generated by crosswake ... (template_version: N)`.
    - A generated iOS XML file (e.g. `Info.plist`) contains `<!-- Generated by crosswake ... -->` AFTER the `<?xml ?>` line.
    - The generated `project.pbxproj`, `gradlew`, and `gradlew.bat` contain NO stamp.
    - The README .eex templates are NOT stamped (they are never rendered — REVIEW FIX finding 4 correction (b)); the test does not require a README stamp.
    - The generated-output test `crosswake_gen_shell_stamp_test.exs` is GREEN and asserts every generated comment-supporting file (incl. gradle.properties) carries a stamp and the three exclusions do not.
    - Generated XML files still parse (the `<?xml ?>` prolog remains the first line).
    - No template contains the literal text "DO NOT EDIT".
  </acceptance_criteria>
  <done>The 15 stamp-eligible templates (derived from @ios_templates/@android_templates, incl. gradle.properties, excl. pbxproj/gradlew/gradlew.bat and the never-rendered README .eex) carry the correct-syntax 2-line stamp; a generated-output test enforces stamp coverage; generated XML remains well-formed.</done>
</task>

<task type="auto">
  <name>Task 3: Ship mix crosswake.bump_template_version</name>
  <read_first>
    - lib/mix/tasks/crosswake.inspect.ex (task skeleton analog)
    - lib/mix/tasks/crosswake.demo.ex (exit({:shutdown, N}) pattern + repo-root path resolution)
    - 134-PATTERNS.md "crosswake.bump_template_version.ex" section (lines 211-277)
    - 134-RESEARCH.md RQ1 "Where to store the checked-in hash" (lines 268-282) + pitfall 7
  </read_first>
  <files>lib/mix/tasks/crosswake.bump_template_version.ex</files>
  <action>
    Create Mix.Tasks.Crosswake.BumpTemplateVersion (`use Mix.Task`, @shortdoc, @moduledoc explaining it is the maintainer bump ceremony). `run(_args)` does three steps atomically, failing loudly (Mix.raise → exit 1, or exit({:shutdown, 1})) if any file write fails:

    1. Read lib/mix/tasks/crosswake.gen.shell.ex, regex-find the `@template_version N` line, parse N, write `@template_version N+1` back via String.replace/3 with an anchored regex (match `@template_version` followed by whitespace and an integer). Use File.cwd!()-anchored paths for consistency with the drift test.
    2. Recompute the SHA-256 over the sorted `.eex` templates — this computation MUST be byte-for-byte identical to the drift test's live_template_hash/0: glob `priv/templates/crosswake/shell/**/*.eex`, Enum.sort/1, File.read!/1 each, Enum.join, `:crypto.hash(:sha256, _)`, Base.encode16(case: :lower).
    3. Read test/crosswake/proof/phase134_template_version_drift_test.exs, regex-replace the `@checked_in_hash "..."` literal with the new hash, File.write!/2.

    Emit `Mix.shell().info("[crosswake] bumped template_version to N+1; drift hash updated.")` on success. Pitfall 7: both edits must happen — never bump the integer without rewriting the hash, or vice versa.
  </action>
  <verify>
    <automated>cd /Users/jon/projects/crosswake && mix compile --warnings-as-errors 2>&1 | tail -1 && mix help crosswake.bump_template_version >/dev/null 2>&1 && echo "bump task compiles and is registered"</automated>
  </verify>
  <acceptance_criteria>
    - `mix help crosswake.bump_template_version` shows the @shortdoc (task is registered).
    - `mix compile --warnings-as-errors` succeeds.
    - The task's hash computation reads the same glob/sort/hash pipeline as the drift test's `live_template_hash/0` (verify by inspection — identical sorted glob, sha256, lowercase hex).
  </acceptance_criteria>
  <done>crosswake.bump_template_version exists, compiles, and atomically increments @template_version and rewrites the drift-test hash.</done>
</task>

<task type="auto">
  <name>Task 4: Run the bump to set the real hash — turn the drift test GREEN</name>
  <read_first>
    - test/crosswake/proof/phase134_template_version_drift_test.exs (the Plan 00 scaffold whose @checked_in_hash this turns real)
    - 134-VALIDATION.md "Drift-Catch Validation" (the positive/negative/non-vacuity cases)
  </read_first>
  <files>test/crosswake/proof/phase134_template_version_drift_test.exs, lib/mix/tasks/crosswake.gen.shell.ex</files>
  <action>
    Run `mix crosswake.bump_template_version` once to (a) increment @template_version from 1 to 2 and (b) write the real SHA-256 of the now-stamped templates into @checked_in_hash, replacing the all-zero placeholder from Plan 00. This is the single ceremony that makes the drift test reflect the as-shipped (stamped) templates.

    NOTE on @template_version final value: after Plan 00 shipped templates WITHOUT stamps and this plan added stamps, the templates changed exactly once within the phase. Running bump once moves the epoch to 2 and locks the hash to the stamped bytes. Accept template_version: 2 as the phase's shipped epoch (the stamp WAS a template change). If the executor prefers epoch 1 as the shipped value, instead manually recompute and write the hash without incrementing — either is acceptable as long as the drift test passes GREEN and the stamp interpolates the same integer the manifest records. Document the chosen value in the SUMMARY.

    After the bump, run the drift test — it MUST pass. Then perform the manual negative-case check from VALIDATION.md (mutate one .eex byte, confirm the drift test fails :merge_blocking, revert) and record the result in the SUMMARY. Do NOT commit the mutated template.
  </action>
  <verify>
    <automated>cd /Users/jon/projects/crosswake && mix test test/crosswake/proof/phase134_template_version_drift_test.exs 2>&1 | grep -qE "0 failures" && echo "drift test GREEN after bump"</automated>
  </verify>
  <acceptance_criteria>
    - `mix test test/crosswake/proof/phase134_template_version_drift_test.exs` reports 0 failures (drift-guard + both non-vacuity tests GREEN).
    - `@checked_in_hash` is no longer the all-zero placeholder.
    - The `@template_version` integer in gen.shell.ex matches the value the manifest + stamp record for a fresh generation.
    - Negative-case manual check recorded in SUMMARY (mutate→fail→revert).
  </acceptance_criteria>
  <done>The drift test is GREEN; @checked_in_hash reflects the stamped templates; @template_version is the phase's shipped epoch; negative-case verified.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI args → gen.shell | `--target`/`--router` user input crosses into file paths and manifest content |
| manifest file → status/diff (downstream) | shell.json is later read by Plans 03/04; treated as advisory |

## STRIDE Threat Register (ASVS L1, block-on: high)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-134-01-01 | Tampering | `--target` path | medium | mitigate | Path.expand/1 on target (already done in gen.shell.ex per RESEARCH Security Domain); manifest written under the expanded root only |
| T-134-01-02 | Tampering | template_version in stamp | low | mitigate | Integer-only module attribute; no string interpolation of user input into the version field (RESEARCH Security Domain) |
| T-134-01-03 | Information Disclosure | manifest `target` field records an absolute path | low | accept | Path is host-local provenance; manifest is host-owned and not published |
| T-134-01-04 | Denial of Service | stale manifest from ensure_file skip | medium | mitigate | Use File.write!/2 (always overwrite) not ensure_file/2 (D-03 pitfall 3) |
</threat_model>

<verification>
- `mix crosswake.gen.shell android --target /tmp/...` writes a complete `.crosswake/shell.json`.
- Generated files carry the stamp; pbxproj/gradlew/gradlew.bat do not.
- `mix test test/crosswake/proof/phase134_template_version_drift_test.exs` → 0 failures.
- `mix help crosswake.bump_template_version` registered.
- `mix compile --warnings-as-errors` clean.
</verification>

<success_criteria>
LIFE-02a satisfied: generated shells carry a readable template_version + crosswake version stamp and a `.crosswake/shell.json` manifest; the drift test fails merge-blocking if templates change without a bump; `mix crosswake.bump_template_version` performs the bump in one command.
</success_criteria>

<output>
Create `.planning/phases/134-shell-lifecycle-native-uat-promotion/134-01-SUMMARY.md` when done. Record the chosen @template_version shipped value and the negative-case drift-catch result.
</output>
