---
phase: 135-ci-ops-hardening-release-as-automation-proof-03
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - test/crosswake/proof/phase135_ci_ops_proof_test.exs
autonomous: true
requirements:
  - PROOF-03
must_haves:
  truths:
    - "SC1: script/check_release_as_staleness.sh exits 1 (RED) against a fixture whose release-as pin equals an existing {component}-v{X} git tag, and exits 0 (GREEN) once the pin is removed — demonstrated RED→GREEN in one hermetic test (D-01 audit, D-02 SC1 bar)"
    - "SC2: script/strip_release_as.py strips release-as + _TODO_release_as on run 1 and is a no-op on run 2 (idempotent, minimal-diff), proven against a temp fixture where release-as is NOT the last key"
    - "SC2: the release-as-cleanup job is structurally wired in .github/workflows/release-please.yml (invokes strip_release_as.py for each crosswake_* component)"
    - "SC3: the release-failure-alert job exists with `if: ${{ failure() }}` and needs: the four companion publish + clean-room-proof jobs — structurally asserted, dormant-on-green (D-02 SC3 bar)"
    - "SC4: script/extract_companion.md Step 12f references the CI automation (PROOF-03, release-as-cleanup, merge-blocking-release-as-staleness) so future companions inherit 0-human release ops"
    - "SC5: register_required_checks.sh is default-dry-run, parametric (list_merge_blocking_checks.py), idempotent (unique_by(.context)); check_required_checks_registered.sh is fail-closed (exit 1 GAP, exit 3 UNVERIFIED); merge-blocking-release-as-staleness appears in discovery output (no 20-lane hardcode)"
    - "Both previously-deferred core-hermetic tests (milestone_transition_reset, phase52_operator_truth) are confirmed GREEN by the proof test"
  artifacts:
    - "test/crosswake/proof/phase135_ci_ops_proof_test.exs"
  key_links:
    - "GIT_DIR env injection connects the ExUnit temp git repo to the bare `git rev-parse` call in check_release_as_staleness.sh — no script change"
    - "The proof test joins the existing hermetic suite (test/crosswake/proof/) as one merge-blocking lane, mirroring phase133_telemetry_contract_test.exs"
  prohibitions:
    - "SC3: the alert job must use `if: ${{ failure() }}`, NOT `if: always()` — always() would fire on skipped jobs every non-release run (the anti-pattern PROOF-03c eliminates)"
    - "SC1: the test must NOT reference real repo tags — only a synthetic tag in a System.unique_integer temp repo (hermetic isolation)"
    - "SC2: the test must NOT mutate the real release-please-config.json — only a temp copy"
    - "SC5: the test must NOT hardcode all 20 merge-blocking lane names — assert only that discovery runs clean AND merge-blocking-release-as-staleness is present"
---

<objective>
Audit-then-prove the five already-landed PROOF-03 artifacts (SC1–SC5) by writing ONE hermetic
ExUnit proof test that demonstrates SC1 RED→GREEN, unit-proves SC2 idempotency, and structurally
asserts SC2-wiring/SC3/SC4/SC5 — plus a self-assertion that the two previously-deferred
core-hermetic tests are now green. Apply a minimal fix ONLY where an audit assertion fails;
research (135-RESEARCH.md, HIGH confidence) found all five SCs already match their criteria, so
no production-code change is expected.

Purpose: Make the "fix cannot be forgotten" guarantee permanent and merge-blocking — the marquee
SC1 staleness guard is demonstrated (not merely asserted), and SC2–SC5 wiring is pinned so it
cannot silently drift. This formalizes work that landed on local main 2026-06-26; the deliverable
is the proof test (+ the verification record), not a rebuild.

Output: test/crosswake/proof/phase135_ci_ops_proof_test.exs (joins the hermetic suite, one
merge-blocking lane). Per D-01, any audit-driven minimal fix is a clearly-flagged conditional.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/135-ci-ops-hardening-release-as-automation-proof-03/135-CONTEXT.md
@.planning/phases/135-ci-ops-hardening-release-as-automation-proof-03/135-RESEARCH.md
@.planning/phases/135-ci-ops-hardening-release-as-automation-proof-03/135-REQUIRED-CHECKS-REGISTRATION.md

# The closest analog — clone its ExUnit proof + structural-presence-assert pattern:
@test/crosswake/proof/phase133_telemetry_contract_test.exs

# The artifacts under audit (read each before writing its assertion — D-01 audit-then-prove):
@script/check_release_as_staleness.sh
@script/strip_release_as.py
@.github/workflows/release-please.yml
@script/extract_companion.md
@script/register_required_checks.sh
@script/check_required_checks_registered.sh
@script/list_merge_blocking_checks.py
</context>

<artifacts_this_phase_produces>
This phase creates exactly one new symbol/file. The drift-verification source-grounding pass
must treat the following as newly-created (not pre-existing drift):

- `test/crosswake/proof/phase135_ci_ops_proof_test.exs` — module
  `Crosswake.Proof.Phase135CiOpsProofTest`. All test functions, any private helper functions
  inside it (e.g. a temp-git-repo setup helper, a temp-config-fixture helper), and the
  `GIT_DIR`-injection fixture strings are introduced by this phase.

No production code is expected to change. IF an audit assertion fails (research expects none),
the minimal fix touches the specific landed script/workflow named in that SC — those edits are
also produced by this phase and listed in the SUMMARY.
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: SC1 marquee proof — staleness guard RED→GREEN via GIT_DIR injection + SC2 idempotency</name>
  <files>test/crosswake/proof/phase135_ci_ops_proof_test.exs</files>
  <read_first>
    - test/crosswake/proof/phase133_telemetry_contract_test.exs (module shape, `use ExUnit.Case`, on_exit cleanup, System.tmp_dir!/System.unique_integer fixture idiom, the bottom-of-file hermetic self-assertion)
    - script/check_release_as_staleness.sh (confirm: arg 1 = config path default `release-please-config.json`; bare `git rev-parse -q --verify "refs/tags/${tag}"` inherits GIT_DIR with NO --git-dir flag; RED prints `STALE`/exit 1; bootstrap+clean print `OK`/exit 0)
    - script/strip_release_as.py (confirm: invoked `python3 script/strip_release_as.py <component> [config_path]`; run-1 prints `stripped`/exit 0; run-2 prints `no change`/exit 0; line-surgery leaves a trailing comma — hence fixture must NOT place release-as last)
    - .github/workflows/release-please.yml (confirm the `release-as-cleanup:` job name and the two `python3 script/strip_release_as.py crosswake_rulestead` / `crosswake_rindle` invocations exist verbatim)
  </read_first>
  <behavior>
    - Test "SC1: staleness guard turns RED on a stale pin and GREEN after removal": create a fresh temp git repo (path includes `System.unique_integer([:positive])`, cleaned in on_exit), `git init`, empty commit, `git tag crosswake_rulestead-v0.1.0`. Write a RED config fixture where `crosswake_rulestead` has `"release-as": "0.1.0"` AND at least one key after it (e.g. `"extra-files": []`). Run `bash script/check_release_as_staleness.sh <red_config>` with env `GIT_DIR` = the temp repo's `.git`, `stderr_to_stdout: true`; assert exit 1 AND output contains `STALE`. Write a GREEN fixture identical but with the `release-as` line removed; run the same way; assert exit 0.
    - Test "SC2: strip_release_as.py strips both keys then is a no-op (idempotent, minimal-diff)": write a temp config (release-as NOT the final key — followed by `"extra-files": []`) with `"release-as"` + `"_TODO_release_as"`. Run 1 via `System.cmd("python3", [...])` → assert exit 0 + output contains `stripped`; parse the result with Jason and refute the block has either key. Run 2 → assert exit 0 + output contains `no change`. (Minimal-diff is implied: surgical line removal keeps all other structure; assert the temp file still parses and other keys like `"component"` survive.)
  </behavior>
  <action>
Create the proof module Crosswake.Proof.Phase135CiOpsProofTest using `use ExUnit.Case` with
`async: true` (per RESEARCH §Architecture Patterns — each test owns a unique GIT_DIR / temp file,
so async is safe; NO Application.put_env / shared global state, unlike phase133). Carry NO
@moduletag so it runs in the hermetic untagged lane (D-18 precedent from phase133).

Implement the two SC1 and SC2 tests per `<behavior>`. For SC1 use `GIT_DIR`-env injection — this
is the resolved Q1 fixture mechanism: the script calls bare `git`, which inherits GIT_DIR, so NO
script change is needed (D-01: audit confirms pass, no fix). Build the env list by merging
`System.get_env()` with the GIT_DIR override (e.g. `System.get_env() |> Map.put("GIT_DIR", git_dir)
|> Map.to_list()`) so PATH and friends are preserved.

For SC2, the fixture MUST place at least one key AFTER `release-as` to avoid the known trailing-comma
JSONDecodeError edge-case (RESEARCH Pitfall 1) — this matches the real config (release-as is key
4/6 for rulestead) so it is a faithful, not contrived, fixture. Parse run-1 output with Jason and
assert the keys are gone. Use `System.tmp_dir!()` + `System.unique_integer([:positive])` for every
temp path and `on_exit(fn -> File.rm_rf!(...) end)` for cleanup. NEVER touch the real
release-please-config.json or real repo tags.

Use the phase133 `ProofAssertions.stable_id_message/7` helper for assert/refute failure messages so
each assertion carries a stable id, expected/actual, the source file to fix, and a `:merge_blocking`
severity — mirror that call shape exactly (alias `Crosswake.TestSupport.ProofAssertions`).

This task is audit-then-prove (D-01): if either SC1 or SC2 assertion goes RED, the audit has found a
real SC↔behavior gap — STOP and apply the MINIMAL fix to the named landed artifact only
(check_release_as_staleness.sh for SC1, strip_release_as.py for SC2), re-run, and record the fix in
the SUMMARY. Research (HIGH confidence) expects both to prove GREEN with no production change.
  </action>
  <verify>
    <automated>mix test test/crosswake/proof/phase135_ci_ops_proof_test.exs --seed 0</automated>
  </verify>
  <done>SC1 RED→GREEN test passes (script exits 1 on stale pin, 0 after removal, both via GIT_DIR temp repo); SC2 idempotency test passes (run-1 strips both keys, run-2 is a no-op); temp fixtures are unique-pathed and cleaned in on_exit; no real config/tags touched; any audit-driven minimal fix (if needed) is applied to the specific landed script and recorded.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: SC2-wiring + SC3 + SC4 + SC5 structural assertions + deferred-failure green self-assertion</name>
  <files>test/crosswake/proof/phase135_ci_ops_proof_test.exs</files>
  <read_first>
    - .github/workflows/release-please.yml (anchors: `release-as-cleanup:` job; `release-failure-alert:` job with `if: ${{ failure() }}` and needs of publish-hex-rulestead / publish-hex-rindle / clean-room-proof-rulestead / clean-room-proof-rindle)
    - script/extract_companion.md (anchors: Step `12f`; `PROOF-03`; `CI-automated`; `release-as-cleanup`; `merge-blocking-release-as-staleness`; the `[x]` checklist line; the `Recipe version: Phase 135` line)
    - script/register_required_checks.sh (anchors: `DRY_RUN="${DRY_RUN:-1}"`; `list_merge_blocking_checks.py`; `unique_by(.context)`)
    - script/check_required_checks_registered.sh (anchors: `list_merge_blocking_checks.py`; `exit 1`; `exit 3`)
    - script/list_merge_blocking_checks.py (confirm it exits 0 and emits `merge-blocking-release-as-staleness` in its output)
    - 135-RESEARCH.md §Q5 + §Open Questions Q1 (the deferred-failure decision; the git-log evidence that those two files were green before Phase 135 landed)
  </read_first>
  <behavior>
    - Test "SC2: release-as-cleanup job is wired in release-please.yml": read the workflow; assert it contains `release-as-cleanup`, `script/strip_release_as.py crosswake_rulestead`, and `script/strip_release_as.py crosswake_rindle`.
    - Test "SC3: release-failure-alert is wired with if: failure() over the four companion jobs": read the workflow; assert it contains `release-failure-alert:`, `if: ${{ failure() }}`, and all four needs (publish-hex-rulestead, publish-hex-rindle, clean-room-proof-rulestead, clean-room-proof-rindle); AND refute it gates that alert on `if: always()` (the PROOF-03c anti-pattern).
    - Test "SC4: extract_companion.md Step 12f references the automation": read the recipe; assert it contains `12f`, `PROOF-03`, `CI-automated`, `release-as-cleanup`, and `merge-blocking-release-as-staleness`.
    - Test "SC5: registration tooling is dry-run-default / parametric / idempotent and detector is fail-closed": read register script — assert it contains `DRY_RUN="${DRY_RUN:-1}"`, `list_merge_blocking_checks.py`, `unique_by(.context)`; read detector — assert `list_merge_blocking_checks.py`, `exit 1`, `exit 3`; run `python3 script/list_merge_blocking_checks.py` via System.cmd, assert exit 0 AND output contains `merge-blocking-release-as-staleness` (do NOT hardcode all 20 lanes — Q2).
    - Test "deferred core-hermetic failures are now green": run `mix test` (via System.cmd, fresh `mix` invocation) on the two files and assert exit 0 for each. See `<action>` for the decision rationale.
    - Test "hermetic lane guard: this proof file carries no @moduletag": read `__ENV__.file`; refute a `@moduletag :` line (mirrors phase133's bottom-of-file self-assertion).
  </behavior>
  <action>
Add the structural-assertion tests to the same module from Task 1, in tagged sections (one per SC),
cloning phase133's `File.read!/1` + `=~` / `String.contains?/2` presence-check pattern. Use
`ProofAssertions.stable_id_message/7` for every assertion message with `:merge_blocking` severity and
the offending source file path (D-02: structural wiring is the proof bar for the un-hermetic
GitHub-side effects).

SC3 negative assertion (prohibition): assert the alert job uses `if: ${{ failure() }}` AND refute the
workflow expresses the alert as `if: always()`. Phrase the refute by the concept; the literal
`if: always()` does not appear in the file under audit, so a `refute String.contains?(source,
"if: always()")` is a safe whole-file negative gate here. (Comment-text discipline: do not echo any
acceptance-literal that a sibling assertion negative-greps; none of these assertions negative-grep a
token used by another assertion in this file.)

SC5: invoke `python3 script/list_merge_blocking_checks.py` via `System.cmd` and assert exit 0 + output
contains `merge-blocking-release-as-staleness`. This proves the parametric discovery is live and the
Phase 135 lane is registered in the source-of-truth, WITHOUT pinning the full 20-lane list (Q2 — would
force a test edit on every future lane). PyYAML is a runtime dependency of that script (RESEARCH
Pitfall 4); a non-zero exit surfaces a missing-PyYAML environment problem rather than passing silently.

Deferred-failure self-assertion (resolves Open Question Q1): run `mix test
test/crosswake/planning/milestone_transition_reset_test.exs` and `mix test
test/crosswake/proof/phase52_operator_truth_test.exs` as separate `System.cmd("mix", ["test", ...])`
calls and assert each exits 0. DECISION: nested `mix test` (assert exit code), NOT a structural file
read. Rationale: git-log shows both files were already green before the 2026-06-26 Phase 135 window
(their last edits are in phases 111/124/124-02, unrelated to v16.0) — the "fix" was upstream
STATE/REQUIREMENTS state that unblocked them, with no code change to those test files. A structural
read could not prove "green", so the faithful proof is to actually run them and assert pass. Surface
this decision explicitly in the SUMMARY.

End the file with the bottom-of-file hermetic self-assertion (no `@moduletag :` in this proof file),
copied from phase133 — it must be the last test in the file.

Audit-then-prove (D-01): every assertion here is expected GREEN per research (SC2/SC3/SC4/SC5 all
match). If any goes RED, apply the MINIMAL fix to the specific landed artifact named in that
assertion's stable-id message (release-please.yml, extract_companion.md, or the two registration
scripts) — never a rewrite — re-run, and record it.
  </action>
  <verify>
    <automated>mix test test/crosswake/proof/phase135_ci_ops_proof_test.exs --seed 0</automated>
  </verify>
  <done>All SC2-wiring/SC3/SC4/SC5 structural tests pass against the landed artifacts; the SC3 `if: always()` negative gate holds; SC5 asserts live discovery + the release-as-staleness lane without hardcoding 20 names; both deferred-failure files prove green via nested `mix test`; the no-@moduletag self-assertion passes; full file is green; any audit-driven minimal fix is applied to the named artifact and recorded in the SUMMARY.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ExUnit test → shell-out (git/python3/bash/mix) | The proof test invokes external processes via System.cmd; all arguments are test-controlled literals, no untrusted/external input crosses here |
| Test → filesystem (System.tmp_dir!) | The test creates temp git repos and temp JSON fixtures; must not touch the real repo's tags or release-please-config.json |
| (out-of-phase) registrar scripts → GitHub branch protection | `DRY_RUN=0 register_required_checks.sh` mutates branch protection — NOT executed in this phase (default dry-run; admin action deferred per D-03) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-135-01 | Tampering | Test temp git repo / config fixtures leaking into the real repo | low | mitigate | Every fixture path uses `System.tmp_dir!()` + `System.unique_integer([:positive])`; `on_exit(fn -> File.rm_rf!/1 end)` cleanup; GIT_DIR isolation prevents touching real tags; real config is never written |
| T-135-02 | Elevation of Privilege | Accidentally running the registrar with `DRY_RUN=0` (mutating branch protection) | medium | mitigate | The proof test only READS the registration scripts (File.read! structural assertions) and runs `list_merge_blocking_checks.py` (read-only discovery); it never executes register_required_checks.sh. Default `DRY_RUN=1` is itself asserted. Actual registration is a deferred out-of-phase admin gate (D-03) |
| T-135-03 | Denial of Service | Nested `mix test` shell-out hangs or runs an unbounded suite | low | accept | Both targeted files are tiny (5 + 6 tests) and hermetic; `mix test` on a single file is bounded (<10s). No watch-mode flags. Accepted: bounded, deterministic |
| T-135-04 | Information Disclosure | Test executes untrusted input via shell-out | low | accept | All System.cmd arguments are hardcoded literals (script paths, component names, temp fixture paths); no environment- or network-sourced input is interpolated into a command. No injection surface |
| T-135-SC | Tampering | npm/pip/cargo installs | n/a | accept | No package installs in this phase — uses already-present python3/jq/git/Jason/ExUnit (RESEARCH §Environment Availability: no missing deps, no new secrets). No legitimacy gate required |
</threat_model>

<verification>
Phase-level checks (run before /gsd-verify-work):

1. `mix test test/crosswake/proof/phase135_ci_ops_proof_test.exs --seed 0` — the new proof lane is fully green (SC1 RED→GREEN, SC2 idempotency + wiring, SC3, SC4, SC5, deferred-failure green, no-@moduletag self-assertion).
2. `mix test` — the full hermetic suite stays green (baseline 1173/0 per RESEARCH; the new lane joins untagged).
3. `git status --porcelain release-please-config.json` is empty after the run — the real config was not mutated by any test.
4. If any audit-driven minimal fix was applied: it is scoped to the single landed artifact named in the failing assertion, and the SUMMARY records what was fixed and why (D-01).
</verification>

<success_criteria>
- test/crosswake/proof/phase135_ci_ops_proof_test.exs exists and is green (SC1–SC5 + deferred-failure self-assertion + no-@moduletag guard).
- SC1 is DEMONSTRATED RED→GREEN (not merely asserted) via the GIT_DIR temp-repo fixture (D-02 marquee bar).
- SC2 idempotency proven (strip both keys → no-op) on a non-final-key fixture; release-as-cleanup wiring asserted.
- SC3/SC4/SC5 structural wiring asserted against the landed workflow/recipe/scripts; SC5 avoids the 20-lane hardcode.
- Both deferred-failure files (milestone_transition_reset, phase52_operator_truth) confirmed green via nested mix test.
- No production code changed UNLESS an audit assertion went RED — in which case a minimal, artifact-scoped fix was applied and recorded (D-01).
- Full hermetic suite remains green.
</success_criteria>

<output>
Create `.planning/phases/135-ci-ops-hardening-release-as-automation-proof-03/135-01-SUMMARY.md` when done.

The SUMMARY MUST explicitly record (per CONTEXT specifics):
- That the PROOF-03 production code pre-existed (landed 2026-06-26); the deliverable is the proof test.
- The per-SC audit verdict (proven-no-change vs minimal-fix-applied), and for any fix, the artifact + the gap.
- The Open-Question-Q1 decision (nested `mix test` for the two deferred files, with the git-log rationale).
- The deferred/out-of-scope items NOT done in this phase: v16.0→origin sync; `DRY_RUN=0` admin required-check registration; live-fire of the real cleanup PR + alert issue (D-03 — SC5 closes on tooling correctness in-phase).
</output>
