---
phase: 129-stable-companion-contract-surface
plan: 02
type: execute
wave: 2
depends_on: ["129-01"]
files_modified:
  - lib/crosswake/companion.ex
  - lib/crosswake/companion/state.ex
  - lib/crosswake/compatibility/compatibility.ex
  - lib/crosswake/manifest/types.ex
  - lib/crosswake/shell/denial.ex
  - guides/companion_contract.md
  - guides/companions.md
  - mix.exs
  - test/crosswake/hex_page_test.exs
autonomous: true
requirements: [SEAM-01, SEAM-02, SEAM-03, SEAM-04]
nyquist_compliant: true

must_haves:
  truths:
    - "All 5 contract modules carry a non-false @moduledoc with the verbatim ## Stability section; the 4 promoted struct types also carry @moduledoc since: 0.1.0 and @typedoc on t/0"
    - "Crosswake.Companion's stale v3.5 in-tree sentence is replaced with a frozen-surface paragraph naming the 5 public modules"
    - "Crosswake.Shell.Denial carries the verbatim steering note and stays OUT of the Companion Contract group"
    - "guides/companion_contract.md exists, enumerates exactly the 5-module public surface, states companions never reference Denial.reasons/0, and is registered in mix.exs extras"
    - "hexdocs shows a Companion Contract groups_for_modules group (5 full module names, no regex) and an Extension Authors groups_for_extras group"
    - "The Phase 129 freeze test (129-01) and hex_page_test are green"
  artifacts:
    - "guides/companion_contract.md (new Diataxis reference guide)"
    - "Promoted moduledocs on Crosswake.Companion.State, Crosswake.Compatibility.Finding, Crosswake.Compatibility.Target, Crosswake.Manifest.Types.RouteEntry"
    - "mix.exs docs/0: Companion Contract groups_for_modules entry + Extension Authors groups_for_extras entry + companion_contract.md in extras"
  key_links:
    - "mix.exs groups_for_modules :\"Companion Contract\" full-name atom list feeds both hexdocs AND the 129-01 freeze test's contract_modules/0 (D-15)"
    - "guides/companion_contract.md MUST be in mix.exs extras in the SAME commit it is created — hex_page_test orphan guard fails otherwise (Pitfall 2)"
    - "moduledoc promotions MUST land before/with the groups_for_modules addition or the hexdocs group renders empty (Pitfall 1, D-11)"
---

<objective>
Land every source and config change that makes the 129-01 freeze test go green: promote the 4 @moduledoc false struct types, replace the stale Companion moduledoc line and add the frozen-surface paragraph, add the Shell.Denial steering note, create guides/companion_contract.md, register it in mix.exs extras, add the two new ExDoc groups (Companion Contract + Extension Authors), add the forward cross-link in companions.md, and extend hex_page_test to guard the two new groups.

Critical ordering (honored by task split): Task 1 promotes moduledocs (source only — build stays green). Task 2 creates the guide AND registers it in extras AND adds the groups IN ONE COMMIT — satisfying the orphan-guide guard (guide+extras same commit) and the empty-group prerequisite (docs already promoted in Task 1). Do not split the guide creation from its extras registration.

Purpose: Deliver SEAM-01 (moduledoc/typedoc/stability), SEAM-02 (curated guide), SEAM-03 (Denial boundary steering + guide language), SEAM-04 (hexdocs grouping).
Output: 5 modified lib files, 1 new guide, 1 cross-link, mix.exs config, hex_page_test extension.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/129-stable-companion-contract-surface/129-CONTEXT.md
@.planning/phases/129-stable-companion-contract-surface/129-RESEARCH.md
@.planning/phases/129-stable-companion-contract-surface/129-PATTERNS.md
@.planning/phases/129-stable-companion-contract-surface/129-VALIDATION.md

# Source files to modify (read before editing)
@lib/crosswake/companion.ex
@lib/crosswake/companion/state.ex
@lib/crosswake/compatibility/compatibility.ex
@lib/crosswake/manifest/types.ex
@lib/crosswake/shell/denial.ex
@mix.exs
@guides/companions.md
@guides/compatibility.md
@test/crosswake/hex_page_test.exs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Promote the 4 struct moduledocs, fix Companion moduledoc, add Denial steering note</name>
  <files>lib/crosswake/companion.ex, lib/crosswake/companion/state.ex, lib/crosswake/compatibility/compatibility.ex, lib/crosswake/manifest/types.ex, lib/crosswake/shell/denial.ex</files>
  <read_first>
129-PATTERNS.md sections for each of the 5 lib files — they contain verbatim target moduledoc prose, the canonical ## Stability template (D-04), the @moduledoc since: placement rule (A1), the @typedoc lines, the frozen-surface paragraph for companion.ex (D-05), the RouteEntry scoping sentence (D-06), and the Denial steering note (D-20). RESEARCH Findings 2, 3, 4, 13. CONTEXT.md D-04 and D-20 contain the verbatim ## Stability template and steering note — use as-is, do NOT re-author.
  </read_first>
  <action>
Apply the moduledoc/doc edits exactly as specified in 129-PATTERNS.md. Use the verbatim ## Stability template (D-04) and the verbatim Denial steering note (D-20) from CONTEXT.md — do NOT re-author either.

In lib/crosswake/companion.ex (D-05): replace the stale opening-paragraph sentence ("Companions live in-tree under `lib/crosswake/companions/<name>/` for the v3.5 milestone and may be extracted to separate packages in a future milestone once the seam stabilizes.") with the frozen-surface paragraph from 129-PATTERNS.md naming the exactly-5 public modules and stating semver stability under crosswake >= 0.1.0 with all other modules internal. Place it immediately before the `## Implementing a companion` heading. Do NOT add @moduledoc since: to Companion (it already has a real moduledoc). Do NOT change any @callback, @type, or @spec — moduledoc prose only.

In lib/crosswake/companion/state.ex, lib/crosswake/compatibility/compatibility.ex (nested Finding AND Target), and lib/crosswake/manifest/types.ex (nested RouteEntry): replace each `@moduledoc false` with the real moduledoc + verbatim ## Stability section from 129-PATTERNS.md. Add `@moduledoc since: "0.1.0"` on its OWN LINE immediately after the closing `"""` (D-03, A1). Add the `@typedoc` line immediately preceding each module's `@type t :: ...` declaration (D-14). For RouteEntry include the D-06 scoping sentence using the FULL module names (e.g. `Crosswake.Manifest.Types.Root`, `Crosswake.Manifest.Types.Compatibility`) to avoid the naming collision with the public `Crosswake.Compatibility` (RESEARCH Finding 4, Pitfall 3). Do NOT modify defstruct fields, @enforce_keys, or the @type definitions themselves — docs only. Do NOT touch any OTHER nested module in types.ex or compatibility.ex.

In lib/crosswake/shell/denial.ex (D-20): APPEND the verbatim steering note paragraph to the existing one-sentence moduledoc (do not replace the existing sentence). Do NOT add @moduledoc since: and do NOT add Denial to any group.
  </action>
  <verify>
    <automated>mix compile --warnings-as-errors 2>&1 | tail -15 && mix test test/crosswake/proof/phase129_companion_contract_freeze_test.exs 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
mix compile --warnings-as-errors succeeds (confirms @moduledoc since: placement is valid Elixir — A1). The 5 contract modules no longer carry @moduledoc false. The Companion moduledoc no longer contains the word "v3.5" in the stale sentence and now names the 5 public modules. Denial moduledoc contains the steering note. The freeze test's moduledoc/typedoc loops would pass for these modules once contract_modules/0 is populated (still red on Finding-present + guide-exists until Task 2 — expected). No struct field, @enforce_keys, @callback, or @type definition changed.
  </acceptance_criteria>
  <done>
All 4 struct types promoted with verbatim ## Stability + @moduledoc since: "0.1.0" + @typedoc on t/0; Companion stale sentence replaced with frozen-surface paragraph; RouteEntry carries the D-06 full-name scoping sentence; Denial carries the verbatim steering note; mix compile --warnings-as-errors is clean.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create the contract guide, register it + the two ExDoc groups in mix.exs, cross-link companions.md, extend hex_page_test</name>
  <files>guides/companion_contract.md, mix.exs, guides/companions.md, test/crosswake/hex_page_test.exs</files>
  <read_first>
129-PATTERNS.md sections "guides/companion_contract.md (CREATE)" (full guide skeleton with the 5-row surface table, Stability Tiers, What Is Not Contract incl. the Denial.reasons/0 language, Declaring Compatibility cross-link, Telemetry Events), "mix.exs defp docs/0" (exact extras + groups_for_modules + groups_for_extras edits), and "test/crosswake/hex_page_test.exs" (the two membership-list extensions). RESEARCH Findings 6, 11, 12, 14, and Open Questions 2 + 3. CONTEXT.md D-07, D-08, D-09, D-10, D-21. The 3 telemetry span names are verbatim from lib/crosswake/companion.ex "## Telemetry events" (validate_dependency, route_gate, kill_switch) — cross-link that moduledoc as source of truth (Open Question 3), do not invent new names.
  </read_first>
  <action>
Do ALL of the following in a SINGLE commit so the hex_page_test orphan guard (guide must be in extras the moment the file exists, Pitfall 2) and the empty-group prerequisite (D-11) are both satisfied.

Create guides/companion_contract.md as a pure Diataxis REFERENCE (D-07) following the skeleton in 129-PATTERNS.md: a reference banner cross-linking companions.md and compatibility.md#companion-compatibility-contract; the 5-row Contract Surface table (Module / role / what companion code does with it / stability tier); a Stability Tiers section (public stable + private bullet defs, D-02); a "What Is Not Contract" section that explicitly lists Crosswake.Shell.Denial as core-owned and states companions NEVER reference Denial.reasons/0 — they emit their own denial-code strings via Finding.t() (D-21, Pitfall 5); a Declaring Compatibility section that cross-links compatibility.md#companion-compatibility-contract WITHOUT duplicating it (D-08); and a Telemetry Events section listing the 3 static companion span names (validate_dependency, route_gate, kill_switch) with one-line descriptions, naming Crosswake.Companion's moduledoc as source of truth (Open Question 3, DRY per D-07). Microcopy in the established calm/explicit/honest brand voice (Claude's discretion). Do NOT reproduce the callback specs (stays DRY via cross-link to Crosswake.Companion).

In mix.exs defp docs/0: (a) add "guides/companion_contract.md" to the extras list; (b) add a "Companion Contract" entry to groups_for_modules as a FULL-NAME atom list of exactly the 5 modules (Crosswake.Companion, Crosswake.Companion.State, Crosswake.Compatibility.Finding, Crosswake.Compatibility.Target, Crosswake.Manifest.Types.RouteEntry) — NOT a regex (D-10, Pitfall 3); (c) add an "Extension Authors" entry to groups_for_extras holding "guides/companion_contract.md", positioned between "Truth" and "Advanced/Companions" (D-10). Do NOT add Crosswake.Shell.Denial to the Companion Contract group (D-19).

In guides/companions.md: add a forward cross-link near the top pointing to companion_contract.md (D-08 reader journey). Do NOT remove or alter the existing Denial.reasons/0 mention at ~line 167 — it is accurate for the core-operator audience (D-21 scope fence, Pitfall 5).

In test/crosswake/hex_page_test.exs: extend the groups_for_modules membership list to include :"Companion Contract" and the groups_for_extras membership list to include :"Extension Authors" (RESEARCH Finding 14, Open Question 2) — guarding both new groups against future regression. Do not otherwise restructure the test.
  </action>
  <verify>
    <automated>mix test test/crosswake/proof/phase129_companion_contract_freeze_test.exs test/crosswake/hex_page_test.exs 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
Both the freeze test and hex_page_test are fully GREEN. The orphan-guide guard passes (guide is in extras). The Companion Contract group lists exactly the 5 full module names (no regex, no Denial). guides/companion_contract.md states companions never reference Denial.reasons/0. companions.md has a forward cross-link and still contains its original Denial.reasons/0 mention. hex_page_test asserts both new groups are present.
  </acceptance_criteria>
  <done>
guides/companion_contract.md created + registered in mix.exs extras (same commit); Companion Contract groups_for_modules (5 full names) + Extension Authors groups_for_extras added; companions.md forward cross-link added without removing the existing Denial.reasons/0 mention; hex_page_test guards both new groups; freeze test + hex_page_test green.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | This plan edits moduledocs, one markdown guide, ExDoc config, and a config-assertion test. No user input, no network, no auth, no crypto, no data storage, no runtime behaviour change (RESEARCH "Security Domain: Not applicable"). |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-129-02 | Information Disclosure | Public moduledocs / guide | low | accept | Honest assessment — the docs deliberately publish the intended public surface only; nothing internal is exposed. No L1-relevant threat above the `high` blocking threshold. |
| T-129-03 | Tampering | mix.exs groups_for_modules ↔ test single source | low | accept | D-15 single-source-of-truth + hex_page_test + the 129-01 freeze test guard config drift; no external attack surface. No package installs in this plan (no T-129-SC). |
</threat_model>

<verification>
- `mix compile --warnings-as-errors` clean (validates @moduledoc since: placement, A1).
- `mix test test/crosswake/proof/phase129_companion_contract_freeze_test.exs test/crosswake/hex_page_test.exs` fully green.
- `mix test` (full suite) green at wave merge.
- Manual (not gating): `mix docs` renders the Companion Contract module group + Extension Authors extras group with @moduledoc since: badges (VALIDATION.md manual row).
</verification>

<success_criteria>
- SEAM-01: 5 modules carry non-false @moduledoc + stability note; 4 struct types carry @typedoc on t/0 and @moduledoc since: "0.1.0".
- SEAM-02: guides/companion_contract.md enumerates exactly the public surface and is in extras.
- SEAM-03: Shell.Denial steering note added + absent from contract group; guide states companions never author Denial / Denial.reasons/0.
- SEAM-04: Companion Contract groups_for_modules (5 full names) + Extension Authors groups_for_extras present and guarded by hex_page_test.
</success_criteria>

<output>
Create `.planning/phases/129-stable-companion-contract-surface/129-02-SUMMARY.md` when done.
</output>
