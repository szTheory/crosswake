---
phase: 131-publish-pipeline-clean-room-lane-rulestead
plan: 03
type: execute
wave: 3
depends_on: ["131-02"]
files_modified:
  - .github/workflows/release-please.yml
  - .planning/phases/131-publish-pipeline-clean-room-lane-rulestead/131-RELEASE-AS-REMOVAL.md
autonomous: true
requirements: [PROOF-02]
must_haves:
  truths:
    - "clean-room-proof-rulestead job needs [release-please, publish-hex-rulestead] and runs only when rulestead_release_created == 'true' (SC#3, D-15)"
    - "no hex.publish for rulestead can run until the in-job --dry-run gate and the merge-blocking phase130-proof lanes are green; the post-publish clean-room job needs the publish job (SC#4 ordering, PROOF-02)"
    - "the clean-room CI job invokes bash script/verify_companion_cleanroom.sh crosswake_rulestead <version> (D-16, thin YAML)"
    - "a documented follow-up removes release-as 0.1.0 from the companion config after the first Release PR merges (D-04, Pitfall 6)"
  artifacts:
    - ".github/workflows/release-please.yml clean-room-proof-rulestead job"
    - ".planning/phases/131-publish-pipeline-clean-room-lane-rulestead/131-RELEASE-AS-REMOVAL.md follow-up runbook"
  key_links:
    - "needs: [release-please, publish-hex-rulestead] -> clean-room runs ONLY after publish (PROOF-02 ordering)"
    - "rulestead_version output -> verify_companion_cleanroom.sh VERSION arg"
    - "release-as removal runbook -> next companion cut is 0.1.1 not a stuck 0.1.0 (D-04)"
---

<objective>
Wire the post-publish `clean-room-proof-rulestead` CI job into `release-please.yml`
(`needs: [release-please, publish-hex-rulestead]`), which runs the
`script/verify_companion_cleanroom.sh` authored in Plan 02 against the freshly-published
version. Record the mandatory `release-as` removal follow-up (D-04) as a runbook so the first
cut publishes exactly 0.1.0 and the NEXT cut is 0.1.1 rather than a stuck 0.1.0.

Purpose: PROOF-02 — the `needs:` dependency graph enforces that the clean-room resolvability
proof runs AFTER the publish job (which itself is gated behind the `--dry-run` step and the
merge-blocking in-monorepo `phase130-proof.yml` lanes). This mirrors the existing
`clean-room-proof-ios`/`-android` post-publish idiom (D-15). This plan completes the SC#4
ordering: no irreversible publish without the dry-run gate; resolvability proven post-publish.

Output: the `clean-room-proof-rulestead` job + the `release-as` removal runbook.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/131-publish-pipeline-clean-room-lane-rulestead/131-CONTEXT.md
@.planning/phases/131-publish-pipeline-clean-room-lane-rulestead/131-RESEARCH.md
@.planning/phases/131-publish-pipeline-clean-room-lane-rulestead/131-02-SUMMARY.md
</context>

<artifacts_this_phase_produces>
This plan (03) creates:
- `clean-room-proof-rulestead` job in `.github/workflows/release-please.yml`
- `131-RELEASE-AS-REMOVAL.md` follow-up runbook (D-04 removal of the one-shot `release-as`)

Consumed here, NOT re-created: the `publish-hex-rulestead` job + `rulestead_version`/
`rulestead_release_created` outputs (Plan 01/02); `script/verify_companion_cleanroom.sh` (Plan 02);
the `release-as: "0.1.0"` config entry (Plan 01 — removed per the runbook AFTER first merge).
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Add post-publish clean-room-proof-rulestead job to release-please.yml (D-15, PROOF-02)</name>
  <files>.github/workflows/release-please.yml</files>
  <read_first>
    - .github/workflows/release-please.yml lines 200-311 (`clean-room-proof-ios`/`-android` — the post-publish idiom to mirror: `needs: [release-please, publish-*]`, `if: releases_created`, checkout -> setup-beam -> install hex+rebar -> install/resolve published artifact -> run proof; note Android uses MAX_ATTEMPTS=40/DELAY=45 but the companion uses the script's own 36/10 poll) and the `publish-hex-rulestead` job added in Plan 02
    - .planning/phases/131-publish-pipeline-clean-room-lane-rulestead/131-RESEARCH.md §"clean-room-proof-ios/-android Jobs — Pattern to Mirror" (the `needs: [release-please, publish-hex-rulestead]` shape; thin YAML delegating to the script) and §Per-Task Verification Map PROOF-01/PROOF-02 rows
    - .planning/phases/131-publish-pipeline-clean-room-lane-rulestead/131-02-SUMMARY.md (confirm verify_companion_cleanroom.sh signature: PACKAGE VERSION)
    - brandbook/BRAND-SPEC.md §6 (CI failure microcopy)
  </read_first>
  <action>
    Add a `clean-room-proof-rulestead` job to `.github/workflows/release-please.yml`: `needs: [release-please, publish-hex-rulestead]` (D-15 — runs ONLY after the publish job, enforcing PROOF-02 ordering); `if: ${{ needs.release-please.outputs.rulestead_release_created == 'true' }}` (per-component gate, consistent with D-07); `runs-on: ubuntu-latest`; `permissions: { contents: read }`. Steps: `actions/checkout`; `erlef/setup-beam` via `.tool-versions` (pin to the same action SHAs used by the sibling jobs); install hex+rebar (`mix local.hex --force && mix local.rebar --force`); then a single thin step that runs `bash script/verify_companion_cleanroom.sh crosswake_rulestead "${{ needs.release-please.outputs.rulestead_version }}"` (D-16 — logic lives in the script; YAML stays thin). The script itself owns the propagation poll, throwaway-host build, install, smoke test, and doctor check. Do NOT inline any of that logic in the YAML. Do NOT modify the iOS/Android clean-room jobs or `phase130-proof.yml`.
  </action>
  <verify>
    <automated>python3 -c "import yaml; d=yaml.safe_load(open('.github/workflows/release-please.yml')); j=d['jobs']['clean-room-proof-rulestead']; n=j['needs']; assert 'release-please' in n and 'publish-hex-rulestead' in n; assert 'rulestead_release_created' in j['if']; steps=' '.join(str(s) for s in j['steps']); assert 'verify_companion_cleanroom.sh crosswake_rulestead' in steps; assert 'rulestead_version' in steps; print('clean-room-proof-rulestead OK')"</automated>
  </verify>
  <acceptance_criteria>
    - The python3+yaml assertion prints `clean-room-proof-rulestead OK`
    - `j['needs']` contains BOTH `release-please` and `publish-hex-rulestead` (PROOF-02 ordering — clean-room cannot run before publish)
    - `j['if']` references `rulestead_release_created`
    - The job calls `bash script/verify_companion_cleanroom.sh crosswake_rulestead "${{ needs.release-please.outputs.rulestead_version }}"` and contains no inline mix-new/doctor logic (thin YAML, D-16)
    - `git diff` shows `clean-room-proof-ios`, `clean-room-proof-android`, and `phase130-proof.yml` unchanged
    - `python3 -c "import yaml; yaml.safe_load(open('.github/workflows/release-please.yml'))"` parses without error
  </acceptance_criteria>
  <done>clean-room-proof-rulestead is wired post-publish, gated on the per-component output, delegating all proof logic to the parameterized script; the SC#4/PROOF-02 ordering (dry-run + merge-blocking lanes before publish, resolvability proven after) is complete.</done>
</task>

<task type="auto">
  <name>Task 2: Document the mandatory release-as removal follow-up (D-04, Pitfall 6)</name>
  <files>.planning/phases/131-publish-pipeline-clean-room-lane-rulestead/131-RELEASE-AS-REMOVAL.md</files>
  <read_first>
    - release-please-config.json (the `packages/crosswake_rulestead` entry carrying `"release-as": "0.1.0"` added in Plan 01)
    - .planning/phases/131-publish-pipeline-clean-room-lane-rulestead/131-RESEARCH.md §"D-04: First-Release Bootstrap" (the docs requirement: remove or set `""` after the first Release PR merges, else subsequent runs stay stuck on 0.1.0) and §"Common Pitfalls" Pitfall 6
    - script/extract_companion.md (the recipe updated in Plan 01 Step 12 — this runbook is the concrete instance of step "remove release-as after first Release PR merges")
  </read_first>
  <action>
    Create `131-RELEASE-AS-REMOVAL.md`: a short, imperative follow-up runbook stating that `"release-as": "0.1.0"` in the `packages/crosswake_rulestead` entry of `release-please-config.json` is a ONE-SHOT first-cut override (D-04) and MUST be removed (or set to `""`) IMMEDIATELY AFTER the first `crosswake_rulestead-v0.1.0` Release PR merges — otherwise every subsequent release-please run stays pinned to 0.1.0 and can never cut 0.1.1 (Pitfall 6). Include: the exact JSON edit (delete the `"release-as": "0.1.0"` line from that one entry), the trigger condition (first Release PR merged + `crosswake_rulestead-v0.1.0` tag exists), a one-line verification (`grep -L 'release-as' release-please-config.json` after removal, or assert the companion entry no longer carries the key), and a cross-reference to `script/extract_companion.md` Step 12 so rindle (Phase 132) follows the same removal step. This is a planning-doc artifact (the actual config edit happens post-merge by the maintainer); do NOT delete `release-as` from `release-please-config.json` in this plan — it must persist through the first cut.
  </action>
  <verify>
    <automated>test -f .planning/phases/131-publish-pipeline-clean-room-lane-rulestead/131-RELEASE-AS-REMOVAL.md && grep -q 'release-as' .planning/phases/131-publish-pipeline-clean-room-lane-rulestead/131-RELEASE-AS-REMOVAL.md && grep -q '0.1.0' .planning/phases/131-publish-pipeline-clean-room-lane-rulestead/131-RELEASE-AS-REMOVAL.md && python3 -c "import json; e=json.load(open('release-please-config.json'))['packages']['packages/crosswake_rulestead']; assert e.get('release-as')=='0.1.0', 'release-as must STILL be present pre-first-cut'; print('release-as intact pre-cut OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `131-RELEASE-AS-REMOVAL.md` exists and contains the strings `release-as` and `0.1.0`
    - The runbook states the trigger (first `crosswake_rulestead-v0.1.0` Release PR merged) and the exact edit (remove the `release-as` line or set `""`)
    - The runbook cross-references `script/extract_companion.md` Step 12 for rindle reuse
    - `release-please-config.json` STILL carries `"release-as": "0.1.0"` on the companion entry (the python3 assertion prints `release-as intact pre-cut OK`) — this plan must NOT delete it (it must persist through the first cut)
  </acceptance_criteria>
  <done>The one-shot release-as removal follow-up is documented as an actionable runbook, while the config entry is intentionally left intact for the first cut.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| publish job -> clean-room job | the `needs:` graph is the structural guarantee that resolvability is proven only AFTER (never instead of) the dry-run-gated publish |
| one-shot release-as -> future cuts | a stale `release-as` silently pins all future companion cuts to 0.1.0 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-131-08 | Tampering / ordering bypass | clean-room-proof-rulestead needs graph (PROOF-02) | high | mitigate | `needs: [release-please, publish-hex-rulestead]` makes the clean-room job structurally unable to run before the dry-run-gated publish; the merge-blocking `phase130-proof.yml` lanes remain the PR-time pre-publish gate |
| T-131-09 | Denial of service (release pipeline) | stale release-as 0.1.0 (Pitfall 6) | low | mitigate | documented removal runbook with explicit trigger + verification; cross-referenced in the generalized extract_companion.md recipe so rindle inherits the removal step |
| T-131-SC | Tampering | npm/pip/cargo installs | low | accept | no package-manager installs in this plan; CI job only adds hex+rebar (`mix local.hex`/`mix local.rebar` — first-party Hex tooling) and delegates to the Plan 02 script |
</threat_model>

<verification>
- `python3 -c "import yaml; yaml.safe_load(open('.github/workflows/release-please.yml'))"` — workflow valid YAML with the clean-room job
- `python3 -c "import json; json.load(open('release-please-config.json'))"` — config still valid, release-as intact
- `test -f .planning/phases/131-publish-pipeline-clean-room-lane-rulestead/131-RELEASE-AS-REMOVAL.md` — runbook present
- Manual-Only / CI-only: the clean-room job's actual green run is post-publish (PROOF-02 irreversible); structural `needs:` ordering is statically verifiable now
</verification>

<success_criteria>
- clean-room-proof-rulestead runs post-publish (needs publish-hex-rulestead), delegating to the parameterized script (SC#3, D-15)
- the SC#4 / PROOF-02 ordering is structurally enforced: dry-run gate + merge-blocking lanes precede the irreversible publish; resolvability is proven after
- the release-as removal follow-up is documented so the next companion cut is 0.1.1, not a stuck 0.1.0 (D-04)
</success_criteria>

<output>
Create `.planning/phases/131-publish-pipeline-clean-room-lane-rulestead/131-03-SUMMARY.md` when done
</output>