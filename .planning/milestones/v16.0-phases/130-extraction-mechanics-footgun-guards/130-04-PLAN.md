---
phase: 130-extraction-mechanics-footgun-guards
plan: 04
type: execute
wave: 2
depends_on: ["130-02", "130-03"]
files_modified:
  - mix.exs
  - lib/crosswake/companions/rulestead.ex
  - lib/crosswake/companions/rulestead/mock_flag_source.ex
  - packages/crosswake_rulestead/lib/crosswake/companions/rulestead.ex
  - packages/crosswake_rulestead/test/support/mock_flag_source.ex
  - packages/crosswake_rulestead/test/support/study_session_live.ex
  - packages/crosswake_rulestead/test/crosswake/proof/phase42_rulestead_companion_test.exs
  - packages/crosswake_rulestead/test/crosswake/proof/phase43_rulestead_advisory_test.exs
  - packages/crosswake_rulestead/test/support/engine_present/rulestead.ex
  - packages/crosswake_rulestead/config/config.exs
  - packages/crosswake_rulestead/mix.lock
  - script/extract_companion.md
autonomous: true
requirements: [EXTRACT-01, EXTRACT-02]
must_haves:
  truths:
    - "Core mix.exs has no MIX_INCLUDE_RULESTEAD / MIX_INCLUDE_RINDLE / companion-conditional dep block; core names no companion in any env (EXTRACT-01, SC#1, D-21)"
    - "packages/crosswake_rulestead/ is a self-contained Hex project with the adapter source + adapter-behavior tests moved (module name Crosswake.Companions.Rulestead preserved) and its tests pass as a path: dep against core (EXTRACT-02, SC#2, D-19)"
    - "The companion compiles --warnings-as-errors in the engine-ABSENT state (D-29 @compile no_warn_undefined) and hex.build --unpack excludes test/ (D-24)"
    - "lib/ references the flag-source config-indirection symbol, never the test MockFlagSource module (D-31)"
    - "mix companions.test and mix verify root aliases run the companion lane (D-26)"
  artifacts:
    - packages/crosswake_rulestead/mix.exs
    - packages/crosswake_rulestead/lib/crosswake/companions/rulestead.ex
    - mix.exs
    - script/extract_companion.md
  key_links:
    - "The adapter source LEAVES core lib/ — this is what makes the EXTRACT-03 static-ref guard assert-able green in Plan 05"
    - "COMPAT-01 fail-closed contract test STAYS in core (Plan 02); only adapter-behavior tests (phase42/43) move to the companion lane (D-20 test split)"
    - "engine-present advisory lane uses a fake top-level Rulestead stub via conditional elixirc_paths; default test = engine-ABSENT, merge-blocking (D-33)"
---

<objective>
The extraction (EXTRACT-01 + EXTRACT-02): move the Crosswake.Companions.Rulestead adapter source and its
MockFlagSource out of core into packages/crosswake_rulestead/ (module name preserved), split the tests per
D-20 (adapter-behavior phase42/43 to the companion lane; COMPAT-01 stays in core, already done in Plan 02),
delete BOTH MIX_INCLUDE_* blocks from core mix.exs (D-21), add the config-indirection so core lib/ never names
the test MockFlagSource (D-31), set up the two dep-states without MIX_INCLUDE (engine-absent default +
engine-present advisory via a fake Rulestead stub, D-33), add @compile {:no_warn_undefined, Rulestead} (D-29),
commit the package mix.lock, and wire the mix companions.test / mix verify root aliases (D-26). Ship the
reusable extraction recipe checklist (D-25).

Purpose: This is the no-publish dress rehearsal (D-22/D-24) that makes the companion an ordinary path: dep and
removes the env hack. Removing the adapter from core lib/ unblocks Plan 05's EXTRACT-03 assert-green.
Output: self-contained companion package, env-hack gone, config-indirection, test split, root aliases, recipe.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/130-extraction-mechanics-footgun-guards/130-CONTEXT.md
@.planning/phases/130-extraction-mechanics-footgun-guards/130-PATTERNS.md
@.planning/phases/130-extraction-mechanics-footgun-guards/130-RESEARCH.md
@lib/crosswake/companions/rulestead.ex
@lib/crosswake/companions/rulestead/mock_flag_source.ex
</context>

<tasks>

<task type="auto">
  <name>Task 1: Move adapter + MockFlagSource into the package; add @compile + config-indirection (D-29/D-31)</name>
  <files>lib/crosswake/companions/rulestead.ex, lib/crosswake/companions/rulestead/mock_flag_source.ex, packages/crosswake_rulestead/lib/crosswake/companions/rulestead.ex, packages/crosswake_rulestead/test/support/mock_flag_source.ex, packages/crosswake_rulestead/config/config.exs</files>
  <read_first>
    - lib/crosswake/companions/rulestead.ex (the full adapter to move; validate_dependency/0 at 99-108 + report_state/0 at 121 hold the Code.ensure_loaded?(Rulestead) probe — already function-body, EXTRACT-04-clean; any MockFlagSource alias to replace with config-indirection)
    - lib/crosswake/companions/rulestead/mock_flag_source.ex (moves verbatim to package test/support/; moduledoc already frames the engine reader as deferred)
    - 130-PATTERNS.md § packages/crosswake_rulestead/lib/.../rulestead.ex (@compile directive + flag_source/0 config-indirection via Application.compile_env(:crosswake, [:rulestead, :flag_source], nil))
    - 130-RESEARCH.md § Pitfall 6 (D-29 @compile required) and § D-31 (config-indirection; lib/ never references the test module; shipped default nil is the honest no-flag-source state)
  </read_first>
  <action>
    Move lib/crosswake/companions/rulestead.ex to packages/crosswake_rulestead/lib/crosswake/companions/rulestead.ex
    PRESERVING the module name Crosswake.Companions.Rulestead (non-breaking, EXTRACT-02). Add
    @compile {:no_warn_undefined, Rulestead} at the top of the moved module (D-29 — optional: true alone does NOT
    silence the undefined-module warning in the engine-ABSENT build). Replace any direct MockFlagSource reference
    with a one-symbol config-indirection: a private flag_source/0 reading
    Application.compile_env(:crosswake, [:rulestead, :flag_source], nil) (D-31) — lib/ references the indirection
    symbol, never the test module; shipped default nil is the honest no-flag-source-configured state (already
    fail-closed-gated upstream by validate_dependency/0). Do NOT introduce a FlagSource behaviour or an
    engine-backed default reader (D-30 — out of scope this phase; the config-indirection is exactly one symbol,
    not a Mox-style behaviour, because there is exactly one eventual real backend). Keep the
    Code.ensure_loaded?(Rulestead) probe inside its
    function bodies verbatim (EXTRACT-04-clean). Move lib/crosswake/companions/rulestead/mock_flag_source.ex to
    packages/crosswake_rulestead/test/support/mock_flag_source.ex verbatim. DELETE both original core files (the
    adapter MUST leave core lib/ — this unblocks EXTRACT-03 in Plan 05). Create
    packages/crosswake_rulestead/config/config.exs wiring the :test-env flag_source to the moved MockFlagSource
    (D-31). After the move, grep core lib/ to confirm zero remaining references to the moved modules.
  </action>
  <verify>
    <automated>mix test test/crosswake/proof/phase130_extraction_guards_test.exs test/crosswake/proof/phase130_fail_closed_contract_test.exs 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - lib/crosswake/companions/rulestead.ex and its mock_flag_source.ex are DELETED from core
    - packages/crosswake_rulestead/lib/crosswake/companions/rulestead.ex exists with module name preserved + @compile {:no_warn_undefined, Rulestead}
    - lib/ (core) has zero references to the moved modules (grep clean)
    - core flag-source resolution goes through Application.compile_env(:crosswake, [:rulestead, :flag_source], nil) indirection (no test-module reference in any shipped lib/)
    - core mix test still green (COMPAT-01 contract + guard tests unaffected by the move; EXTRACT-03 real-lib test still skipped)
  </acceptance_criteria>
  <done>Adapter + mock moved out of core, @compile + config-indirection in place, core tests green.</done>
</task>

<task type="auto">
  <name>Task 2: Split tests into companion lane + engine-present stub; delete MIX_INCLUDE_*; add root aliases (D-20/D-21/D-26/D-33)</name>
  <files>mix.exs, packages/crosswake_rulestead/test/crosswake/proof/phase42_rulestead_companion_test.exs, packages/crosswake_rulestead/test/crosswake/proof/phase43_rulestead_advisory_test.exs, packages/crosswake_rulestead/test/support/study_session_live.ex, packages/crosswake_rulestead/test/support/engine_present/rulestead.ex</files>
  <read_first>
    - mix.exs:38-63 (the two MIX_INCLUDE_* blocks to DELETE; the project/0 keyword list has NO aliases: key today — add aliases: aliases() to project/0 AND a defp aliases/0)
    - test/crosswake/proof/phase42_rulestead_companion_test.exs (SC#1 adapter-behavior + sole test/support dep Crosswake.TestSupport.StudySessionLive — moves to companion lane)
    - test/crosswake/proof/phase43_rulestead_advisory_test.exs (engine-present green path — moves, gets @moduletag :engine_present per D-33)
    - test/support/router_fixtures.ex:40-46 (the 3-line StudySessionLive stub to COPY into the package test/support/, D-23)
    - 130-RESEARCH.md § "mix companions.test Root Alias" (the exact aliases keyword list) and § D-33 (engine-present fake top-level Rulestead stub in test/support/engine_present/ via conditional elixirc_paths; :engine_present / :engine_absent tags; mix clean between lane switches)
    - 130-PATTERNS.md § mix.exs (modify) (delete blocks -> just base; aliases pattern)
  </read_first>
  <action>
    Delete BOTH the rulestead and rindle MIX_INCLUDE_* if-blocks from core mix.exs deps/0, leaving deps/0
    returning just base (D-21 — rindle source stays in-tree until 132 but its dead env-hack dep block goes now;
    core names NO companion in any env). Add aliases: aliases() to project/0 and a defp aliases/0 with
    "companions.test": ["cmd --cd packages/crosswake_rulestead mix test"] and "verify":
    ["companions.test", "test --exclude requires_example_host --exclude advisory_only"] (D-26). MOVE the adapter-
    behavior tests (phase42 + phase43 content) into packages/crosswake_rulestead/test/crosswake/proof/ (D-20 test
    split — the COMPAT-01 fail-closed contract test STAYS in core, already in Plan 02). COPY the 3-line
    StudySessionLive stub verbatim into packages/crosswake_rulestead/test/support/study_session_live.ex (D-23 —
    sole test/support dep of phase42). For the engine-present advisory lane (D-33): create a fake top-level
    Rulestead stub at packages/crosswake_rulestead/test/support/engine_present/rulestead.ex; wire the package
    mix.exs elixirc_paths so the engine_present support dir is appended ONLY when the advisory lane is selected
    (pick the cleaner of a tag-conditional elixirc_paths or a separate alias — Claude's Discretion per D-33);
    tag the engine-present test @moduletag :engine_present and the engine-absent assertions appropriately; the
    advisory lane must mix clean before switching states to avoid a stale stub .beam leaking into the absent lane.
    Default mix test in the package = engine-ABSENT (hermetic, merge-blocking; Code.ensure_loaded?(Rulestead)
    false -> fail-closed; @compile no_warn_undefined lets it compile).
  </action>
  <verify>
    <automated>mix run -e "src = File.read!(\"mix.exs\"); false = String.contains?(src, \"MIX_\" <> \"INCLUDE_RULESTEAD\"); false = String.contains?(src, \"MIX_\" <> \"INCLUDE_RINDLE\"); IO.puts(\"NO-ENV-HACK-OK\")"</automated>
  </verify>
  <acceptance_criteria>
    - Core mix.exs deps/0 returns just base; no MIX_INCLUDE_RULESTEAD / MIX_INCLUDE_RINDLE / companion-conditional block; core names no companion (SC#1)
    - project/0 has aliases: aliases(); defp aliases/0 defines companions.test + verify (D-26)
    - phase42/phase43 adapter tests live under packages/crosswake_rulestead/test/; StudySessionLive stub copied (D-23)
    - engine-present fake Rulestead stub exists under test/support/engine_present/, wired via conditional elixirc_paths/alias; engine-present test tagged :engine_present (D-33)
    - Default package mix test runs engine-ABSENT and merge-blocking
  </acceptance_criteria>
  <done>Env hack deleted, aliases wired, tests split, engine-present lane stubbed and tagged.</done>
</task>

<task type="auto">
  <name>Task 3: Finalize package deps + commit mix.lock; run companion lane + verify script; write extraction recipe (D-22/D-24/D-25)</name>
  <files>packages/crosswake_rulestead/mix.exs, packages/crosswake_rulestead/mix.lock, script/extract_companion.md</files>
  <read_first>
    - packages/crosswake_rulestead/mix.exs (the Wave-0 skeleton — confirm {:crosswake, path: "../.."} has NO runtime: false (D-19), {:rulestead, "~> 0.1", optional: true} (D-28), @version "0.1.0" + marker (D-22), files: excludes test/ (D-24))
    - script/verify_companion_package.sh (the Wave-0 verify script — now run end-to-end against the real moved source)
    - 130-RESEARCH.md § "Dress-Rehearsal Verify Commands" (hex.build --unpack assertions, --dry-run, --warnings-as-errors) and § D-24 (commit mix.lock + CI --check-locked; document the path: -> {:crosswake, "~> 0.1"} lock pivot for 131)
    - 130-CONTEXT.md § D-25 (the extraction recipe headline structure)
  </read_first>
  <action>
    Confirm/finalize packages/crosswake_rulestead/mix.exs deps: {:crosswake, path: "../.."} with NO runtime: false
    (D-19), {:rulestead, "~> 0.1", optional: true} (D-28); @version "0.1.0" with the x-release-please-version
    marker (D-22 — do NOT touch .release-please-manifest.json / release-please-config.json, do NOT join the
    linked-versions crosswake group). Run mix deps.get in the package and commit packages/crosswake_rulestead/mix.lock
    (D-24). Run the companion lane (mix companions.test via the new root alias) and assert it passes in the
    engine-ABSENT default state. Run script/verify_companion_package.sh end-to-end against the real moved source:
    mix hex.build --unpack (assert test/ EXCLUDED, lib/crosswake/companions/rulestead.ex PRESENT), mix hex.publish
    --dry-run (exit 0), mix compile --warnings-as-errors in the engine-ABSENT state (passes because of the @compile
    directive). Write script/extract_companion.md as the parameterized reusable recipe checklist (D-25, NOT a
    generator) with the headline structure: move source (preserve module names) -> split tests (SC#1 -> companion
    lane, SC#5 -> core lane) -> copy minimal test/support stubs -> companion mix.exs (version+marker,
    {:crosswake, path:} no runtime: false) -> commit lock -> delete MIX_INCLUDE_{COMPANION} -> wire CI lane + root
    alias -> run verify script -> run the three guards -> DON'T touch release-please. Document the
    path: -> {:crosswake, "~> 0.1"} lock pivot reserved for Phase 131.
  </action>
  <verify>
    <automated>bash script/verify_companion_package.sh crosswake_rulestead 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - Package mix.exs: {:crosswake, path: "../.."} with NO runtime: false; {:rulestead, "~> 0.1", optional: true}; @version "0.1.0" + marker
    - packages/crosswake_rulestead/mix.lock committed
    - mix companions.test passes (engine-absent default)
    - script/verify_companion_package.sh passes end-to-end: tarball excludes test/ + includes adapter source; --dry-run exit 0; --warnings-as-errors clean in engine-absent state
    - script/extract_companion.md present with the full D-25 headline structure including "DON'T touch release-please" and the 131 lock-pivot note
    - release-please config/manifest untouched
  </acceptance_criteria>
  <done>Self-contained package verified by the dress-rehearsal script; lock committed; recipe shipped.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| core lib/ -> companion package | After the move, core must name no companion; the seam is the :companions registry + @behaviour only |
| companion package -> Hex tarball | The published tarball must exclude test/ and include the adapter source — the dress rehearsal verifies before the irreversible 131 publish |
| engine-present stub -> engine-absent default lane | A stale stub .beam leaking into the absent lane would falsely mark the engine present (test integrity) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-130-09 | Tampering | Compile-baked engine presence -> stale .beam confuses engine-present/absent state | medium | mitigate | Runtime probe only; engine-present fake Rulestead via conditional elixirc_paths; mix clean between lane switches (D-33) |
| T-130-10 | Information Disclosure | test/ leaks into the published Hex tarball | low | mitigate | files: allowlist excludes test/; verify script asserts test/ absent in mix hex.build --unpack output (D-24) |
| T-130-11 | Elevation of Privilege | runtime: false on the core dep leaves :crosswake un-started for a real adopter (latent fail-open) | high | mitigate | {:crosswake, path:} carries NO runtime: false (D-19); the D-27 guard (Plan 03) asserts its absence |
| T-130-SC | Tampering | npm/pip/cargo installs | high | mitigate | Package declares {:rulestead, "~> 0.1", optional: true} + {:crosswake, path: "../.."}; rulestead is an existing in-tree engine probed via Code.ensure_loaded? (RESEARCH.md Package Legitimacy Audit: zero NEW external packages introduced this phase); mix.lock committed + dress-rehearsal --dry-run gates before any publish |
</threat_model>

<verification>
- Core mix.exs has no MIX_INCLUDE_* and names no companion; mix companions.test + mix verify aliases work.
- packages/crosswake_rulestead/ compiles standalone, tests pass as a path: dep, mix.lock committed.
- script/verify_companion_package.sh green: tarball excludes test/, --dry-run exit 0, --warnings-as-errors clean engine-absent.
- Core mix test still green (COMPAT-01 contract + EXTRACT-04 guard unaffected; EXTRACT-03 real-lib test still skipped pending Plan 05).
- script/extract_companion.md present.
</verification>

<success_criteria>
EXTRACT-01 + EXTRACT-02 complete: the env hack is gone, core names no companion, the adapter lives in a
self-contained crosswake_rulestead path: dep whose tests pass and whose tarball/dry-run/warnings-as-errors
dress rehearsal is green, with the config-indirection, engine-present advisory lane, root aliases, and a
reusable extraction recipe in place — and the adapter source has left core lib/, unblocking Plan 05.
</success_criteria>

<output>
Create `.planning/phases/130-extraction-mechanics-footgun-guards/130-04-SUMMARY.md` when done
</output>
