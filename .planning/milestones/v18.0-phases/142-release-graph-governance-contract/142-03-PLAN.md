---
phase: 142-release-graph-governance-contract
plan: 03
type: execute
wave: 3
depends_on:
  - 142-02
files_modified:
  - lib/crosswake/release_status.ex
  - lib/mix/tasks/crosswake.release.status.ex
  - test/mix/tasks/crosswake_release_status_test.exs
autonomous: true
requirements:
  - RELG-01
  - RELG-02
  - RELG-03
must_haves:
  truths:
    - "Maintainers can see Phase 142 release-governance guard posture through `mix crosswake.release.status`."
    - "Text and JSON status output identify exact governance checks without claiming PREF, MIRR, or STAT requirements complete."
    - "The release-status task remains deterministic by default and keeps live registry probes opt-in."
  artifacts:
    - path: "lib/crosswake/release_status.ex"
      provides: "Local release governance status model and check codes"
      exports: ["build/1", "render/1"]
    - path: "lib/mix/tasks/crosswake.release.status.ex"
      provides: "Operator-facing Mix task for text/JSON release status"
      exports: ["run/1"]
    - path: "test/mix/tasks/crosswake_release_status_test.exs"
      provides: "Regression proof for local, text, JSON, and live-probe status output"
  key_links:
    - from: "lib/crosswake/release_status.ex"
      to: ".github/workflows/release-please.yml"
      via: "Reads local workflow source and reports guard posture"
      pattern: "release-please.yml"
    - from: "lib/crosswake/release_status.ex"
      to: "script/check_release_workflow_integrity.exs"
      via: "Status output names the semantic proof command as the authoritative check"
      pattern: "check_release_workflow_integrity"
---

<objective>
Expose the Phase 142 release-governance contract through the existing operator status surface without over-claiming downstream v18 work.

Purpose: RELG proof should be visible to maintainers in the same calm, local-first status command used for release operations, while PREF/MIRR/STAT spillover remains owned by later phases.
Output: `Crosswake.ReleaseStatus` and `mix crosswake.release.status` report governance checks for exact gates, non-replacing concurrency, and cleanup-after-proof, with tests covering text and JSON output.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/142-release-graph-governance-contract/142-CONTEXT.md
@.planning/phases/142-release-graph-governance-contract/142-RESEARCH.md
@.planning/phases/142-release-graph-governance-contract/142-VALIDATION.md
@brandbook/BRAND-SPEC.md
@lib/crosswake/release_status.ex
@lib/mix/tasks/crosswake.release.status.ex
@test/mix/tasks/crosswake_release_status_test.exs
@script/check_release_workflow_integrity.exs
@.github/workflows/release-please.yml
@guides/companion_compatibility.md
</context>

## Artifacts this phase produces

- `.github/workflows/release-please.yml` top-level `concurrency.queue: max` with `cancel-in-progress: false`.
- `.github/workflows/release-please.yml` root/native behavioral gates using `contains(fromJSON(needs.release-please.outputs.paths_released), exact_path)` for `.`, `packages/crosswake-shell-core-ios`, and `packages/crosswake-shell-core-android`.
- `.github/workflows/release-please.yml` companion behavioral gates using `needs.release-please.outputs.<component>_release_created == 'true'` for `rulestead`, `rindle`, `sigra`, `chimeway`, and `threadline`.
- `.github/workflows/release-please.yml` `release-as-cleanup` needs on all five `publish-hex-*` jobs and all five `clean-room-proof-*` jobs.
- `.github/workflows/release-please.yml` `release-as-cleanup` per-component implication condition: unreleased components may skip, released components require publish and proof `success`.
- `.github/workflows/release-please.yml` cleanup PR-only behavior through branch creation, `git push origin "$branch"`, and `gh pr create`; no direct mutation of protected `main`.
- `.github/workflows/release-please.yml` actionlint quote fix: `basename "$ARTIFACT"`.
- `script/check_release_workflow_integrity.exs` check IDs produced by this phase in Plan 02: `release.concurrency.queue_max`, `release.concurrency.not_cancelled`, `release.concurrency.no_true_cancellation`, `release.aggregate_gate.behavioral_jobs_absent`, `release.cleanup.after_publish_and_proof`, `release.cleanup.pr_only`, root/native path gates, native proof decoupling, mirror preflight, and component gate checks.
- `test/crosswake/proof/phase142_release_integrity_test.exs` adversarial proof cases produced by this phase in Plan 02: comment-only false pass, aggregate behavioral gate, missing queue, conflicting cancellation, and cleanup ignoring proof.
- `lib/crosswake/release_status.ex` ReleaseStatus governance check codes produced by this phase in Plan 03: `release.governance_queue_max`, `release.governance_behavioral_identity_gates`, and `release.governance_cleanup_after_proof`.
- Existing CLI flags retained and tested by this phase in Plan 03: `mix crosswake.release.status`, `--json`, and `--live`.

## Source Coverage Audit

| SOURCE | ID | Feature/Requirement | Plan | Status | Notes |
|---|---|---|---|---|---|
| GOAL | - | Encode and test the release DAG so core/native/companion publish jobs cannot be triggered by the wrong Release Please output | 01, 02, 03 | COVERED | Plan 03 gives the operator-visible summary of the contract. |
| REQ | RELG-01 | Path-specific gates prove companion-only releases cannot publish core Hex or native artifacts | 01, 02, 03 | COVERED | Status reports exact-gate posture. |
| REQ | RELG-02 | Release publish/proof jobs are not canceled or replaced mid-run | 01, 02, 03 | COVERED | Status reports queue/cancel posture. |
| REQ | RELG-03 | Stale release-as cleanup runs only after relevant publish success and proof success | 01, 02, 03 | COVERED | Status reports cleanup-after-proof posture. |
| RESEARCH | release-status spillover | Treat existing release-status work as operator surface but keep STAT ownership honest | 03 | COVERED | Does not mark STAT-01, STAT-02, or STAT-03 complete. |
| RESEARCH | exact gates | Use `paths_released` for root/native and aliased per-component outputs for companions | 01, 02, 03 | COVERED | Plan 03 mirrors the scanner outcome for maintainers. |
| RESEARCH | non-replacing concurrency | Require `cancel-in-progress: false` plus `queue: max` | 01, 02, 03 | COVERED | Status check code names this invariant. |
| RESEARCH | cleanup-after-proof | Cleanup waits on all companion publish and clean-room proof jobs | 01, 02, 03 | COVERED | Status check code names this invariant. |
| CONTEXT | D-01, D-02, D-03 | Keep bundled v18 worktree intact, map ownership honestly, avoid product breadth claims | 01, 02, 03 | COVERED | ReleaseStatus reports RELG facts without downstream completion claims. |
| CONTEXT | D-04, D-05, D-06, D-07, D-08, D-09 | Exact release identity gates and independent companion versioning | 01, 02, 03 | COVERED | Status output is based on local workflow source. |
| CONTEXT | D-10, D-11, D-12 | Release workflow concurrency preserves pending work without cross-run needs | 01, 02, 03 | COVERED | Status output includes queue max and false cancellation. |
| CONTEXT | D-13, D-14, D-15, D-16, D-17 | Cleanup opens PR only after released component publish and proof success | 01, 02, 03 | COVERED | Status output includes cleanup-after-proof. |
| CONTEXT | D-18, D-19, D-20, D-21, D-22 | Keep Elixir semantic proof primary and harden it with negative fixtures | 02, 03 | COVERED | Status names the semantic proof command but does not replace it. |
| CONTEXT | D-23, D-24, D-25, D-26, D-27 | Operator surface uses calm, specific, actionable output | 03 | COVERED | Mix task docs and tests cover text/JSON output. |
| CONTEXT | D-28, D-29, D-30 | Publish irreversibility, package identity, and pinned Release Please behavior remain explicit | 01, 02, 03 | COVERED | Status remains local/read-only by default. |
| CONTEXT | Deferred Ideas | Release Please major upgrade, YAML parser, live rehearsal automation, dashboard/UI, product breadth | NONE | EXCLUDED | Explicitly out of Phase 142 scope. |

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add release-governance checks to Crosswake.ReleaseStatus</name>
  <files>lib/crosswake/release_status.ex, lib/mix/tasks/crosswake.release.status.ex</files>
  <read_first>
    @lib/crosswake/release_status.ex
    @lib/mix/tasks/crosswake.release.status.ex
    @script/check_release_workflow_integrity.exs
    @.github/workflows/release-please.yml
    @.planning/phases/142-release-graph-governance-contract/142-CONTEXT.md
    @.planning/phases/142-release-graph-governance-contract/142-RESEARCH.md
    @brandbook/BRAND-SPEC.md
  </read_first>
  <behavior>
    - `Crosswake.ReleaseStatus.build/1` returns local governance check maps with stable `code`, `status`, and `message` values.
    - Human output names the exact invariant and points to `elixir script/check_release_workflow_integrity.exs` for the authoritative merge-blocking proof.
    - JSON output includes the same check codes and does not require network probes unless `--live` is passed.
  </behavior>
  <action>Extend the existing `checks/6` pipeline in `Crosswake.ReleaseStatus` instead of adding a separate status model. Add local workflow checks with codes `release.governance_queue_max`, `release.governance_behavioral_identity_gates`, and `release.governance_cleanup_after_proof`. The queue check must require `queue: max`, `cancel-in-progress: false`, and absence of a true cancellation conflict. The behavioral identity check must confirm root/native path gates and companion alias gates from D-04 through D-09, and must not reject aggregate output exposure in the `release-please` job itself. The cleanup check must mirror the Plan 02 scanner expectations for proof needs and publish/proof result implications from D-13 through D-17. Use calm, specific messages per D-24 and D-26, and include `next: elixir script/check_release_workflow_integrity.exs` or equivalent wording in governance error messages per D-25. Update the Mix task moduledoc or short descriptive text only as needed to clarify that the task is read-only local release status by default and that `--live` is the only registry-probe path. Do not add new CLI flags. Do not claim PREF-01, PREF-02, PREF-03, MIRR-01, MIRR-02, MIRR-03, STAT-01, STAT-02, or STAT-03 complete; existing clean-room, mirror, doctor, and JSON/live facts remain downstream verification scope per D-02.</action>
  <verify>
    <automated>mix test test/mix/tasks/crosswake_release_status_test.exs</automated>
  </verify>
  <acceptance_criteria>`Crosswake.ReleaseStatus.build/1` returns the three new `release.governance_*` check codes, text output names them under `Checks:`, JSON output contains them, and default status remains local-only unless `--live` is requested.</acceptance_criteria>
  <done>Release-status output exposes RELG-owned governance facts without changing the command's existing flag contract.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prove release-status text and JSON do not over-claim downstream scope</name>
  <files>test/mix/tasks/crosswake_release_status_test.exs, lib/crosswake/release_status.ex, lib/mix/tasks/crosswake.release.status.ex</files>
  <read_first>
    @test/mix/tasks/crosswake_release_status_test.exs
    @lib/crosswake/release_status.ex
    @lib/mix/tasks/crosswake.release.status.ex
    @.planning/phases/142-release-graph-governance-contract/142-CONTEXT.md
    @.planning/phases/142-release-graph-governance-contract/142-VALIDATION.md
  </read_first>
  <behavior>
    - Focused release-status tests assert local graph, text output, JSON output, and live-probe warning behavior.
    - Tests assert governance codes exist and downstream requirement IDs are not rendered as completed claims.
    - ReleaseStatus remains safe to call in tests without making live network requests unless probes are injected.
  </behavior>
  <action>Extend `test/mix/tasks/crosswake_release_status_test.exs` to assert `Crosswake.ReleaseStatus.build/1` includes `release.governance_queue_max`, `release.governance_behavioral_identity_gates`, and `release.governance_cleanup_after_proof`. Extend the human-output test to assert those codes appear and that output does not include completion claims for `PREF-01`, `MIRR-01`, or `STAT-01`. Extend the JSON test to assert the same check codes are present in decoded `checks` and that the JSON object remains scriptable with `schema_version`, `core`, `companions`, and `checks`. Keep the injected `http_probe` and `git_ref_probe` pattern for `--live` so tests remain deterministic. If Task 1 adjusted wording in `ReleaseStatus.render/1` or the Mix task moduledoc, update tests to lock the new exact check codes rather than brittle full prose.</action>
  <verify>
    <automated>mix test test/mix/tasks/crosswake_release_status_test.exs &amp;&amp; elixir script/check_release_workflow_integrity.exs &amp;&amp; mix test test/crosswake/proof/phase142_release_integrity_test.exs</automated>
  </verify>
  <acceptance_criteria>Focused status tests pass, semantic release-governance script passes, focused Phase 142 proof passes, and neither text nor JSON output renders downstream requirement IDs as complete.</acceptance_criteria>
  <done>Operator status surface is RELG-aware, deterministic, and honest about downstream ownership.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Local release files -> operator CLI | Local source and manifest files are summarized into human and JSON output that maintainers may act on. |
| Optional live probes -> status model | Network checks are opt-in and must not affect default deterministic CI status. |
| Governance checks -> downstream requirement claims | RELG checks must not imply PREF, MIRR, or STAT completion. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-142-11 | Repudiation | `mix crosswake.release.status` | mitigate | Add stable governance check codes and actionable next-command messages. |
| T-142-12 | Spoofing | Local workflow status | mitigate | Base checks on checked-in workflow source and keep semantic script as authoritative proof. |
| T-142-13 | Information Disclosure | `--live` registry probes | accept | Existing live probes are opt-in and return only package/version presence labels. |
| T-142-14 | Tampering | Downstream completion claims | mitigate | Tests assert text and JSON do not render PREF/MIRR/STAT requirement IDs as completed claims. |
| T-142-SC | Tampering | npm/pip/cargo installs | accept | No package-manager installs; only existing Elixir source and tests are modified. |
</threat_model>

<verification>
Overall plan checks:

- `mix test test/mix/tasks/crosswake_release_status_test.exs`
- `elixir script/check_release_workflow_integrity.exs`
- `mix test test/crosswake/proof/phase142_release_integrity_test.exs`
</verification>

<success_criteria>
Plan 03 is complete when `mix crosswake.release.status` text and JSON include the Phase 142 governance check codes, keep live probes opt-in, and avoid downstream PREF/MIRR/STAT completion claims while all focused tests and the semantic proof pass.
</success_criteria>

<output>
Create `.planning/phases/142-release-graph-governance-contract/142-03-SUMMARY.md` when done.
</output>
