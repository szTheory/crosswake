---
phase: 142-release-graph-governance-contract
plan: 02
type: execute
wave: 2
depends_on:
  - 142-01
files_modified:
  - script/check_release_workflow_integrity.exs
  - test/crosswake/proof/phase142_release_integrity_test.exs
autonomous: true
requirements:
  - RELG-01
  - RELG-02
  - RELG-03
must_haves:
  truths:
    - "RELG-01: The semantic proof fails if any behavioral release job uses aggregate Release Please identity."
    - "RELG-02: The semantic proof fails if `queue: max` is missing or true cancellation conflicts with release preservation."
    - "RELG-03: The semantic proof fails if cleanup ignores companion clean-room proof results."
  artifacts:
    - path: "script/check_release_workflow_integrity.exs"
      provides: "Named semantic release workflow proof with adversarial-fixture support"
      exports: ["release.concurrency.queue_max", "release.aggregate_gate.behavioral_jobs_absent", "release.cleanup.after_publish_and_proof"]
    - path: "test/crosswake/proof/phase142_release_integrity_test.exs"
      provides: "Merge-blocking ExUnit wrapper and negative-fixture proof for Phase 142"
      min_lines: 100
  key_links:
    - from: "script/check_release_workflow_integrity.exs"
      to: ".github/workflows/release-please.yml"
      via: "Default script target reads the real release workflow"
      pattern: "release-please.yml"
    - from: "test/crosswake/proof/phase142_release_integrity_test.exs"
      to: "script/check_release_workflow_integrity.exs"
      via: "ExUnit runs the scanner against the real workflow and mutated fixture workflows"
      pattern: "System.cmd\\(\"elixir\""
---

<objective>
Harden the Phase 142 semantic proof so the workflow changes from Plan 01 cannot regress silently.

Purpose: The release workflow is product surface; this plan turns RELG-01, RELG-02, and RELG-03 into named, adversarial, deterministic ExUnit and script checks.
Output: `script/check_release_workflow_integrity.exs` exposes specific check IDs and fixture-friendly inputs, and `test/crosswake/proof/phase142_release_integrity_test.exs` proves current and intentionally-broken workflows.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/142-release-graph-governance-contract/142-CONTEXT.md
@.planning/phases/142-release-graph-governance-contract/142-RESEARCH.md
@.planning/phases/142-release-graph-governance-contract/142-VALIDATION.md
@.planning/phases/141-core-first-publish-family-release/141-CONTEXT.md
@.github/workflows/release-please.yml
@script/check_release_workflow_integrity.exs
@test/crosswake/proof/phase142_release_integrity_test.exs
</context>

## Artifacts this phase produces

- `.github/workflows/release-please.yml` top-level `concurrency.queue: max` with `cancel-in-progress: false`.
- `.github/workflows/release-please.yml` root/native behavioral gates using `contains(fromJSON(needs.release-please.outputs.paths_released), exact_path)` for `.`, `packages/crosswake-shell-core-ios`, and `packages/crosswake-shell-core-android`.
- `.github/workflows/release-please.yml` companion behavioral gates using `needs.release-please.outputs.<component>_release_created == 'true'` for `rulestead`, `rindle`, `sigra`, `chimeway`, and `threadline`.
- `.github/workflows/release-please.yml` `release-as-cleanup` needs on all five `publish-hex-*` jobs and all five `clean-room-proof-*` jobs.
- `.github/workflows/release-please.yml` `release-as-cleanup` per-component implication condition: unreleased components may skip, released components require publish and proof `success`.
- `.github/workflows/release-please.yml` cleanup PR-only behavior through branch creation, `git push origin "$branch"`, and `gh pr create`; no direct mutation of protected `main`.
- `.github/workflows/release-please.yml` actionlint quote fix: `basename "$ARTIFACT"`.
- `script/check_release_workflow_integrity.exs` check IDs produced by this phase in Plan 02: `release.concurrency.queue_max`, `release.concurrency.not_cancelled`, `release.concurrency.no_true_cancellation`, `release.aggregate_gate.behavioral_jobs_absent`, `release.cleanup.after_publish_and_proof`, `release.cleanup.pr_only`, root/native path gates, native proof decoupling, mirror preflight, and component gate checks.
- `test/crosswake/proof/phase142_release_integrity_test.exs` adversarial proof cases produced by this phase in Plan 02: comment-only false pass, aggregate behavioral gate, missing queue, conflicting cancellation, and cleanup ignoring proof.
- `lib/crosswake/release_status.ex` ReleaseStatus governance check codes produced by this phase in Plan 03: `release.governance_queue_max`, `release.governance_behavioral_identity_gates`, and `release.governance_cleanup_after_proof`.
- Existing CLI flags retained and tested by this phase in Plan 03: `mix crosswake.release.status`, `--json`, and `--live`.

## Source Coverage Audit

| SOURCE | ID | Feature/Requirement | Plan | Status | Notes |
|---|---|---|---|---|---|
| GOAL | - | Encode and test the release DAG so core/native/companion publish jobs cannot be triggered by the wrong Release Please output | 01, 02, 03 | COVERED | Workflow, semantic proof, and operator status each cover the goal from a different surface. |
| REQ | RELG-01 | Path-specific gates prove companion-only releases cannot publish core Hex or native artifacts | 01, 02, 03 | COVERED | Plan 02 is the merge-blocking proof surface. |
| REQ | RELG-02 | Release publish/proof jobs are not canceled or replaced mid-run | 01, 02, 03 | COVERED | Plan 02 adds missing-queue and conflicting-cancel negative coverage. |
| REQ | RELG-03 | Stale release-as cleanup runs only after relevant publish success and proof success | 01, 02, 03 | COVERED | Plan 02 adds cleanup-without-proof negative coverage. |
| RESEARCH | exact gates | Use `paths_released` for root/native and aliased per-component outputs for companions | 01, 02 | COVERED | Implements D-04, D-05, D-06, D-07, D-08, D-09. |
| RESEARCH | non-replacing concurrency | Require `cancel-in-progress: false` plus `queue: max` | 01, 02 | COVERED | Implements D-10, D-11, D-12. |
| RESEARCH | cleanup-after-proof | Cleanup waits on all companion publish and clean-room proof jobs | 01, 02 | COVERED | Implements D-13, D-14, D-15, D-16, D-17. |
| RESEARCH | semantic proof hardening | Add named checks and adversarial negative fixtures | 02 | COVERED | Implements D-18, D-19, D-20, D-21, D-22. |
| RESEARCH | actionlint additive | Keep actionlint additive and semantic proof authoritative | 01, 02 | COVERED | ExUnit and script are the phase gate. |
| CONTEXT | D-01, D-02, D-03 | Keep bundled v18 worktree intact, map ownership honestly, avoid product breadth claims | 01, 02, 03 | COVERED | Plans modify only RELG-owned surfaces and mark PREF/MIRR/STAT as downstream-owned facts. |
| CONTEXT | D-04, D-05, D-06, D-07, D-08, D-09 | Exact release identity gates and independent companion versioning | 01, 02 | COVERED | Script checks exact job blocks outside comments. |
| CONTEXT | D-10, D-11, D-12 | Release workflow concurrency preserves pending work without cross-run needs | 01, 02 | COVERED | Scanner checks `queue: max`, false cancellation, and no true cancellation conflict. |
| CONTEXT | D-13, D-14, D-15, D-16, D-17 | Cleanup opens PR only after released component publish and proof success | 01, 02 | COVERED | Scanner checks needs, result implications, and PR-only shape. |
| CONTEXT | D-18, D-19, D-20, D-21, D-22 | Keep Elixir semantic proof primary and harden it with negative fixtures | 02 | COVERED | No structured YAML parser and no new dependency. |
| CONTEXT | D-23, D-24, D-25, D-26, D-27 | Operator surface uses calm, specific, actionable output | 03 | COVERED | Plan 03 owns status output. |
| CONTEXT | D-28, D-29, D-30 | Publish irreversibility, package identity, and pinned Release Please behavior remain explicit | 01, 02, 03 | COVERED | Proof names exact package identity and does not publish. |
| CONTEXT | Deferred Ideas | Release Please major upgrade, YAML parser, live rehearsal automation, dashboard/UI, product breadth | NONE | EXCLUDED | Explicitly out of Phase 142 scope. |

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Upgrade the release workflow integrity scanner with named semantic checks</name>
  <files>script/check_release_workflow_integrity.exs</files>
  <read_first>
    @script/check_release_workflow_integrity.exs
    @.github/workflows/release-please.yml
    @.planning/phases/142-release-graph-governance-contract/142-CONTEXT.md
    @.planning/phases/142-release-graph-governance-contract/142-RESEARCH.md
    @.planning/phases/142-release-graph-governance-contract/142-VALIDATION.md
  </read_first>
  <behavior>
    - The scanner defaults to `.github/workflows/release-please.yml` and can also scan an explicit workflow path passed as the first CLI argument or `RELEASE_WORKFLOW_PATH`.
    - The scanner extracts job blocks and evaluates semantic checks against non-comment YAML lines so comments alone cannot satisfy or violate a check.
    - The scanner exits 0 only when every named check passes and exits 1 with `[crosswake] FAIL:` lines for failed check IDs.
  </behavior>
  <action>Refactor `script/check_release_workflow_integrity.exs` into small local helpers while keeping it dependency-free per D-18 and D-22. Add workflow path selection from `System.argv()` first argument or `RELEASE_WORKFLOW_PATH`, defaulting to `.github/workflows/release-please.yml`, so ExUnit can pass adversarial fixtures. Keep `job_block` extraction, but add a non-comment view of each job block before checking policy strings; lines whose first non-space character is `#` must not count for a required or forbidden invariant per D-20. Define component names `rulestead`, `rindle`, `sigra`, `chimeway`, and `threadline` once. Define behavioral jobs as publish, mirror, clean-room proof, and cleanup jobs; do not classify the `release-please` output job itself as behavioral because it legitimately exposes aggregate outputs for non-destructive summaries per D-05. Add or preserve named check IDs: `release.concurrency.not_cancelled`, `release.concurrency.queue_max`, `release.concurrency.no_true_cancellation`, `release.outputs.paths_released`, `release.root_hex.path_gate`, `release.ios.path_gate`, `release.android.path_gate`, `release.aggregate_gate.behavioral_jobs_absent`, `release.ios_proof.decoupled`, `release.android_proof.decoupled`, `release.mirror_token.preflight`, `release.cleanup.after_publish_and_proof`, `release.cleanup.pr_only`, and `release.<component>.component_gate` for every component. The cleanup check must require all five publish needs, all five proof needs, each released-component publish/proof `success` result, `always()`, and no direct `main` mutation. Keep output prefixed `[crosswake]` per D-24 and keep check details actionable with the command `elixir script/check_release_workflow_integrity.exs` per D-25.</action>
  <verify>
    <automated>elixir script/check_release_workflow_integrity.exs</automated>
  </verify>
  <acceptance_criteria>The scanner passes on the real workflow after Plan 01 and prints OK lines for `release.concurrency.queue_max`, `release.aggregate_gate.behavioral_jobs_absent`, and `release.cleanup.after_publish_and_proof`.</acceptance_criteria>
  <done>Semantic script coverage for RELG-01, RELG-02, and RELG-03 exists with named check IDs and fixture input support.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add adversarial ExUnit proof for release governance regressions</name>
  <files>test/crosswake/proof/phase142_release_integrity_test.exs</files>
  <read_first>
    @test/crosswake/proof/phase142_release_integrity_test.exs
    @script/check_release_workflow_integrity.exs
    @.github/workflows/release-please.yml
    @.planning/phases/142-release-graph-governance-contract/142-CONTEXT.md
    @.planning/phases/142-release-graph-governance-contract/142-RESEARCH.md
  </read_first>
  <behavior>
    - Current workflow proof passes and asserts the new check IDs appear in script output.
    - Negative fixtures fail with the precise check ID tied to the mutated invariant.
    - A fixture with required/forbidden strings in comments only does not produce a false pass.
  </behavior>
  <action>Extend `Crosswake.Proof.Phase142ReleaseIntegrityTest` rather than creating a parallel proof file. Add helper functions that read the real workflow, write mutated workflows to a temporary file, run `elixir script/check_release_workflow_integrity.exs <fixture_path>`, and return output plus exit code. Add discriminating tests per D-20: missing `queue: max` fails with `release.concurrency.queue_max`; `cancel-in-progress: true` fails with `release.concurrency.not_cancelled` or `release.concurrency.no_true_cancellation`; aggregate identity used in a behavioral job `if:` fails with `release.aggregate_gate.behavioral_jobs_absent`; required strings present only in comments do not make the fixture pass; cleanup missing clean-room proof needs or proof-result implications fails with `release.cleanup.after_publish_and_proof`; cleanup that mutates `main` directly fails with `release.cleanup.pr_only`. Keep the existing clean-room dependency floor assertions as existing work to avoid regression, but do not promote PREF-01 completion in this test file per D-02. Keep async true only if the temporary fixture helper does not mutate cwd or shared files.</action>
  <verify>
    <automated>mix test test/crosswake/proof/phase142_release_integrity_test.exs</automated>
  </verify>
  <acceptance_criteria>The focused ExUnit suite passes on the real workflow and contains negative coverage for comment-only false pass, aggregate behavioral gate, missing queue, conflicting cancellation, and cleanup ignoring proof.</acceptance_criteria>
  <done>Merge-blocking ExUnit proof closes the current semantic scanner gaps named in 142-RESEARCH.md.</done>
</task>

<task type="auto">
  <name>Task 3: Run the focused release-governance proof loop</name>
  <files>script/check_release_workflow_integrity.exs, test/crosswake/proof/phase142_release_integrity_test.exs</files>
  <read_first>
    @script/check_release_workflow_integrity.exs
    @test/crosswake/proof/phase142_release_integrity_test.exs
    @.planning/phases/142-release-graph-governance-contract/142-VALIDATION.md
  </read_first>
  <action>Run the Phase 142 validation loop after the scanner and tests are in place. Confirm the standalone script and focused ExUnit proof both pass. If `actionlint` is available and Plan 01 fixed the quote issue, run it as additive evidence; if it fails on a new syntax issue, fix the workflow syntax only if the failure is in RELG-owned workflow lines touched by Phase 142. Do not remove the Elixir semantic proof and do not treat actionlint as a substitute for Crosswake release identity policy per D-21.</action>
  <verify>
    <automated>elixir script/check_release_workflow_integrity.exs &amp;&amp; mix test test/crosswake/proof/phase142_release_integrity_test.exs &amp;&amp; (if command -v actionlint >/dev/null 2&gt;&amp;1; then actionlint .github/workflows/release-please.yml; else echo "[crosswake] actionlint unavailable; semantic proof remains authoritative"; fi)</automated>
  </verify>
  <acceptance_criteria>The script exits 0, the focused ExUnit suite exits 0, and actionlint is either green or explicitly unavailable with the semantic proof still authoritative.</acceptance_criteria>
  <done>Phase 142 governance proof is ready for Plan 03 status surfacing and phase verification.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Workflow source -> semantic scanner | Text parsing decides whether the release DAG satisfies security and governance policy. |
| Comments -> proof logic | Explanatory comments must not satisfy or violate release policy checks. |
| Fixture tests -> scanner process | ExUnit passes controlled workflow files to the script and asserts failure IDs. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-142-06 | Tampering | `script/check_release_workflow_integrity.exs` | mitigate | Add named checks for path gates, queue max, cleanup-after-proof, and PR-only cleanup. |
| T-142-07 | Repudiation | Release governance proof output | mitigate | Emit stable check IDs with `[crosswake] OK/FAIL` lines so failures name the exact broken invariant. |
| T-142-08 | Tampering | Comment-only proof bypass | mitigate | Strip comment-only lines before semantic checks and add an adversarial ExUnit fixture. |
| T-142-09 | Elevation of Privilege | Aggregate behavioral gate fixture | mitigate | Fail if behavioral jobs use aggregate Release Please identity outside comments. |
| T-142-10 | Denial of Service | Missing queue fixture | mitigate | Fail missing `queue: max` and conflicting true cancellation settings. |
| T-142-SC | Tampering | npm/pip/cargo installs | accept | No package-manager installs; scanner remains stdlib Elixir plus ExUnit. |
</threat_model>

<verification>
Overall plan checks:

- `elixir script/check_release_workflow_integrity.exs`
- `mix test test/crosswake/proof/phase142_release_integrity_test.exs`
- `actionlint .github/workflows/release-please.yml` when installed and after Plan 01 quote hygiene
</verification>

<success_criteria>
Plan 02 is complete when the real workflow passes the scanner, the focused ExUnit proof passes, and at least the five adversarial classes from D-20 fail for the right named check IDs.
</success_criteria>

<output>
Create `.planning/phases/142-release-graph-governance-contract/142-02-SUMMARY.md` when done.
</output>
