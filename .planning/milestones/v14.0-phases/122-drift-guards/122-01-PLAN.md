---
phase: 122-drift-guards
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - test/crosswake/contract/contract_drift_test.exs
autonomous: true
requirements: [GUARD-01]
must_haves:
  truths:
    - "A pure-Elixir ExUnit test (no Xcode, no Gradle) fails when any committed contract surface's bridge_protocol_version diverges from Crosswake.Bridge.Contract.version/0."
    - "The test parses the two hand-maintained crosswake_manifest.json files via a JSON parser (Jason) and asserts their top-level bridge_protocol_version equals the canonical version."
    - "The test asserts the three generated JSONs (route_activation ×2, bridge_contract_vectors) as a fast local tripwire, documented in-test as redundant with GUARD-02 which is their source of truth."
    - "Every failure message names (1) Crosswake.Bridge.Contract.version/0 at lib/crosswake/bridge/contract.ex, (2) each drifted surface with actual-vs-expected, (3) the fix command mix crosswake.contract.gen."
    - "Synthetic-regression tests prove the guard is non-vacuous: a deliberately drifted in-memory surface produces a failure of the expected category."
  artifacts:
    - path: "test/crosswake/contract/contract_drift_test.exs"
      provides: "GUARD-01 parse-based merge-blocking drift test for all committed contract version surfaces"
      contains: "Crosswake.Bridge.Contract.version"
  key_links:
    - from: "test/crosswake/contract/contract_drift_test.exs"
      to: "lib/crosswake/bridge/contract.ex"
      via: "reads Crosswake.Bridge.Contract.version() as the canonical expected value"
      pattern: "Crosswake\\.Bridge\\.Contract\\.version"
  prohibitions:
    - "GUARD-01 must NOT use a text grep / regex / String.contains? to extract bridge_protocol_version from any surface — it MUST decode JSON with Jason and read the top-level key (the manifests carry prose lines mentioning bridge_protocol_version at lines 1135+ that a grep would falsely match)."
    - "GUARD-01 must NOT parse or extract a version from docs/_contract_snippet.md (markdown, owned byte-exact by GUARD-02); the optional snippet tripwire, if added, is a golden equality against the gen render fn, not a value extraction."
    - "GUARD-01 must NOT read build/, intermediates/, app/build/, or .claude/worktrees/ copies — committed source paths only."
---

<objective>
Ship GUARD-01: a pure-Elixir, browser/native-free, merge-blocking ExUnit drift test that reads each committed contract-version surface via a JSON parser (never a text grep) and asserts it equals `Crosswake.Bridge.Contract.version()`. The test is the sole automated guardian of the two hand-maintained `crosswake_manifest.json` files (no generator emits them — they are exactly the drift class that required the manual 121-04 gap-closure) and a fast local tripwire on the three generated JSONs.

Purpose: Make the manifest-vs-canonical divergence that broke Phase 121 impossible to land silently, with a precise, human-readable local failure ("which file, actual vs expected, run this command") before CI's opaque diff ever runs.
Output: `test/crosswake/contract/contract_drift_test.exs`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/122-drift-guards/122-CONTEXT.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Parse-assert all committed contract surfaces against the canonical version</name>
  <files>test/crosswake/contract/contract_drift_test.exs</files>
  <read_first>
    - lib/crosswake/bridge/contract.ex:1-113 (the @version "1.1.0" constant and version/0 — THE canonical authority this test reads at runtime)
    - test/crosswake/guides/native_evidence_drift_test.exs:1-91, 208-226, 737-771 (the established ExUnit drift-test idiom: File.read! → parse → collect %{path, line, category, detail} failures → assert failures == [] with a formatted message; mirror this structure and location-forward failure voice)
    - lib/mix/tasks/crosswake.contract.gen.ex:33-36, 65-159 (the four generator output paths and their structure; route_activation.json ×2 and bridge_contract_vectors.json carry top-level "bridge_protocol_version")
    - examples/ios_shell_host/Fixtures/crosswake_manifest.json (top-level "bridge_protocol_version" at ~line 350 — the hand-maintained primary surface; note prose mentions at lines 1135+ that a grep would falsely match)
  </read_first>
  <behavior>
    - Reads Crosswake.Bridge.Contract.version() once as the canonical expected value (never a literal "1.1.0").
    - For each PRIMARY surface (examples/ios_shell_host/Fixtures/crosswake_manifest.json, examples/android_shell_host/app/src/main/assets/crosswake_manifest.json): File.read! → Jason.decode! → read the TOP-LEVEL "bridge_protocol_version" key → assert it equals the canonical version; mismatch collects a failure with category :manifest_version_drift.
    - For each TRIPWIRE surface (examples/ios_shell_host/Fixtures/route_activation.json, examples/android_shell_host/app/src/main/assets/route_activation.json, test/fixtures/bridge_contract_vectors.json): same parse-and-compare, category :generated_version_drift; an in-test comment states GUARD-02 (generate-and-diff) is the source of truth for these and GUARD-01 asserts them only as a friendlier local echo.
    - A guard/readability test asserts all six surface paths exist and the canonical version matches the semver shape, so the suite fails loudly if a surface is moved rather than silently skipping it.
    - All surfaces agreeing → zero failures → test passes. Any mismatch → a single aggregated flunk whose message lists every drifted surface.
  </behavior>
  <action>
    Create test/crosswake/contract/contract_drift_test.exs as a `use ExUnit.Case, async: true` module named Crosswake.Contract.ContractDriftTest. Capture the canonical version into a module attribute via `Crosswake.Bridge.Contract.version()`. Enumerate the two PRIMARY manifest paths and the three TRIPWIRE generated-JSON paths in two explicit lists (committed source paths only — do NOT walk the tree, do NOT touch build/ or .claude/worktrees/ copies, per D-03). For each surface, File.read! then Jason.decode! then fetch the top-level "bridge_protocol_version" string and compare to the canonical value; collect mismatches as %{path, category, actual, expected} failures (category :manifest_version_drift for the two manifests, :generated_version_drift for the three JSONs, per D-05). Decode with Jason — NEVER grep/regex/String.contains? — because each manifest carries the literal token bridge_protocol_version inside compatibility-prose strings at lines 1135+ that a text scan would falsely match (D-01, D-02). Add a readability test asserting File.exists? for all six surfaces and that the canonical version matches a semver regex. Add an in-test comment block documenting that GUARD-02 owns the three generated JSONs byte-exact and GUARD-01 tripwires them for DX (D-05). Do NOT read or parse docs/_contract_snippet.md (markdown, GUARD-02-owned, D-02). Run with `mix test test/crosswake/contract/contract_drift_test.exs`.
  </action>
  <verify>
    <automated>mix test test/crosswake/contract/contract_drift_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: the file contains `Crosswake.Bridge.Contract.version` and `Jason.decode` and does NOT contain `String.contains?` / `Regex.match?` / `=~` applied to manifest contents for version extraction.
    - Behavior assertion: `mix test test/crosswake/contract/contract_drift_test.exs` passes against the current tree (all six surfaces at 1.1.0).
    - CLI output: the test reads the top-level bridge_protocol_version of both manifests and all three generated JSONs and compares each to `Crosswake.Bridge.Contract.version()`.
  </acceptance_criteria>
  <done>The test passes on the current tree; mutating any one committed surface's top-level bridge_protocol_version to a wrong value makes it fail with the drifted path named.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Contract-form failure message + anti-vacuous synthetic regressions</name>
  <files>test/crosswake/contract/contract_drift_test.exs</files>
  <read_first>
    - test/crosswake/guides/native_evidence_drift_test.exs:38-115, 746-771 (the synthetic-regression idiom: feed deliberately-drifted in-memory content into the same scan/decode helper and assert the expected failure category appears; the format_failures/flunk message shape)
    - test/crosswake/guides/quick_start_adoption_drift_test.exs (sibling drift test with synthetic stale-input regressions to mirror)
    - lib/crosswake/bridge/contract.ex:102-109 (version/0 path, for the message's canonical-source reference)
  </read_first>
  <behavior>
    - The aggregated failure message names (1) the canonical source Crosswake.Bridge.Contract.version/0 at lib/crosswake/bridge/contract.ex and its value, (2) each drifted surface with `actual` vs `expected`, (3) the exact fix command mix crosswake.contract.gen.
    - A synthetic-regression test feeds a deliberately drifted in-memory JSON (a manifest map with a wrong bridge_protocol_version) into the same compare helper used by Task 1 and asserts a :manifest_version_drift failure is produced.
    - A second synthetic-regression test does the same for a generated-JSON surface and asserts a :generated_version_drift failure.
    - A test asserts the formatted message string contains the literal mix crosswake.contract.gen and the literal lib/crosswake/bridge/contract.ex, rejecting a bare ExUnit "left/right" message.
  </behavior>
  <action>
    Refactor Task 1's inline compare into a small pure helper (e.g. compare_surface/4 taking path, category, decoded-or-raw content, expected) so synthetic content can be fed without touching disk, mirroring native_evidence_drift_test.exs scan_surface. Add a format_failures/flunk path whose message follows the Crosswake house contract (D-12): a header naming Crosswake.Bridge.Contract.version/0 at lib/crosswake/bridge/contract.ex with its value, one line per drifted surface as `<path>: bridge_protocol_version <actual> != expected <expected>`, and a trailing `Fix: run mix crosswake.contract.gen and commit the regenerated surfaces` line. Add synthetic-regression tests: one passing a manifest-shaped map with a bad version asserts a :manifest_version_drift failure; one passing a generated-JSON-shaped map with a bad version asserts a :generated_version_drift failure; one asserting the rendered message string contains both "mix crosswake.contract.gen" and "lib/crosswake/bridge/contract.ex". Do NOT let the negative-grep-able literals trip the executor: these strings legitimately must appear in the test source.
    <!-- planner-discipline-allow: mix crosswake.contract.gen -->
    <!-- planner-discipline-allow: lib/crosswake/bridge/contract.ex -->
  </action>
  <verify>
    <automated>mix test test/crosswake/contract/contract_drift_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: the test module contains a synthetic-regression test that feeds drifted in-memory content and asserts a `:manifest_version_drift` failure (proving the guard is non-vacuous).
    - Behavior assertion: a test asserts the formatted failure message contains both `mix crosswake.contract.gen` and `lib/crosswake/bridge/contract.ex`.
    - Test command: `mix test test/crosswake/contract/contract_drift_test.exs` passes (positive surfaces green, synthetic regressions assert failures are produced).
  </acceptance_criteria>
  <done>Synthetic drift inputs produce the expected failure categories; the message names canonical source, each drifted surface actual-vs-expected, and the fix command; no bare ExUnit assertion message is used for the drift verdict.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| committed JSON surfaces → test | a hand-edited or stale manifest/fixture is the untrusted input this guard inspects |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-122-01 | Tampering | hand edit to crosswake_manifest.json bridge_protocol_version | mitigate | GUARD-01 parse-asserts the top-level key against Crosswake.Bridge.Contract.version/0; mismatch fails the merge-blocking test |
| T-122-02 | Spoofing | a grep-based guard fooled by prose lines containing the token bridge_protocol_version | mitigate | mandatory Jason.decode of the top-level key, never a text scan (lines 1135+ carry decoy prose) |
| T-122-03 | Repudiation | a vacuous/false-green guard that asserts nothing | mitigate | synthetic-regression tests feed drifted in-memory content and assert the expected failure category is produced |
</threat_model>

<verification>
- `mix test test/crosswake/contract/contract_drift_test.exs` is green on the current tree.
- Temporarily editing the top-level bridge_protocol_version in either crosswake_manifest.json to a wrong value makes the test fail and the message names that file with actual-vs-expected and the fix command (revert the edit after verifying).
</verification>

<success_criteria>
A pure-Elixir ExUnit drift test reads each committed contract-version surface via Jason (not grep) and asserts it equals Crosswake.Bridge.Contract.version/0; the failure message names the canonical file, each drifted surface, and the regenerate command; synthetic regressions prove it is non-vacuous.
</success_criteria>

<artifacts_this_phase_produces>
## Artifacts this phase produces (all of Phase 122 — for the source-grounding drift pass)

New symbols created across Phase 122 plans (exclude these from "undefined symbol" flags — they are net-new this phase):

- **GUARD-01 (plan 01):** test module `Crosswake.Contract.ContractDriftTest` at `test/crosswake/contract/contract_drift_test.exs`; failure categories `:manifest_version_drift`, `:generated_version_drift`.
- **GUARD-03 (plan 02):** check function `contract_version_parity_check/1` in `lib/crosswake/doctor/publish_readiness.ex`; doctor check id `"contract.version_parity"`, category `:contract_version_parity`, codes `diag.contract.version_parity_ok` / `diag.contract.version_parity_failed`.
- **GUARD-02 + GUARD-04 (plan 03):** workflow `.github/workflows/contract-drift-gate.yml` with job names `guard-01-contract-drift-test`, `guard-02-generate-and-diff`, and aggregator `merge-blocking-contract-drift`; registration script `script/register-contract-gate.sh`.

Pre-existing symbols the guards read (NOT new): `Crosswake.Bridge.Contract.version/0`, `mix crosswake.contract.gen`, `generator_coordinate_parity_check/1`, the four generator output paths, the two `crosswake_manifest.json` files.
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/122-drift-guards/122-01-SUMMARY.md` when done.
</output>
