---
phase: 123-native-package-behavioral-proof
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/mix/tasks/crosswake.contract.gen.ex
  - test/fixtures/bridge_contract_vectors.json
  - packages/crosswake-shell-core-ios/Tests/CrosswakeShellCoreTests/Resources/bridge_contract_vectors.json
  - packages/crosswake-shell-core-android/src/test/resources/bridge_contract_vectors.json
  - test/crosswake/bridge/bridge_behavioral_vector_test.exs
  - test/crosswake/contract/contract_drift_test.exs
autonomous: true
requirements: [NTEST-01]
must_haves:
  truths:
    - "Running mix crosswake.contract.gen emits the expanded 7-vector bridge_contract_vectors.json to the repo-root fixture AND to byte-identical copies in both native test-resource directories"
    - "Each of the 7 vectors carries id, description, request_override, session_override, expected_outcome, and expected_denial_reason"
    - "A new Elixir behavioral test runs every vector through Crosswake.Compatibility.bridge_findings/2 and asserts each vector's expected_outcome and expected_denial_reason against real Elixir decision logic"
    - "Running the gen task twice produces zero git diff (idempotent, GUARD-02-safe)"
    - "A bridge-protocol-version bump in the canonical Elixir source makes the committed vectors stale so GUARD-01 and GUARD-02 fail until regenerated"
  artifacts:
    - path: "lib/mix/tasks/crosswake.contract.gen.ex"
      provides: "Expanded seed_vectors/1 (7 vectors with session_override) + two new @*_vectors_path constants + two write_if_changed emit calls"
      contains: "@ios_vectors_path"
    - path: "test/fixtures/bridge_contract_vectors.json"
      provides: "Regenerated canonical 7-vector fixture"
      contains: "vec-007-capability-version-deny"
    - path: "packages/crosswake-shell-core-ios/Tests/CrosswakeShellCoreTests/Resources/bridge_contract_vectors.json"
      provides: "DO-NOT-EDIT iOS copy of the vectors (byte-identical to root)"
      contains: "bridge_protocol_version"
    - path: "packages/crosswake-shell-core-android/src/test/resources/bridge_contract_vectors.json"
      provides: "DO-NOT-EDIT Android copy of the vectors (byte-identical to root)"
      contains: "bridge_protocol_version"
    - path: "test/crosswake/bridge/bridge_behavioral_vector_test.exs"
      provides: "Elixir behavioral vector test (anti-vacuous proof) via bridge_findings/2 + finding_to_denial/2"
      contains: "Crosswake.Compatibility.bridge_findings"
  key_links:
    - from: "lib/mix/tasks/crosswake.contract.gen.ex"
      to: "packages/crosswake-shell-core-ios/Tests/CrosswakeShellCoreTests/Resources/bridge_contract_vectors.json"
      via: "write_if_changed(@ios_vectors_path, vectors_json(...))"
      pattern: "write_if_changed\\(@ios_vectors_path"
    - from: "test/crosswake/bridge/bridge_behavioral_vector_test.exs"
      to: "test/fixtures/bridge_contract_vectors.json"
      via: "module-attribute load of the committed vectors file at compile time"
      pattern: "Jason.decode!\\(File.read!"
    - from: "test/crosswake/bridge/bridge_behavioral_vector_test.exs"
      to: "lib/crosswake/compatibility/compatibility.ex"
      via: "Crosswake.Compatibility.bridge_findings/2 + finding_to_denial/2 drive every vector"
      pattern: "bridge_findings|finding_to_denial"
---

<objective>
Author the expanded 7-vector bridge contract schema in the canonical Elixir gen task, emit byte-identical DO-NOT-EDIT copies into both native test-resource directories, regenerate the committed fixtures, and write the Elixir behavioral vector test that proves the vectors describe real Elixir bridge-decision behavior (the anti-vacuous proof for NTEST-01).

This plan is the upstream dependency for the iOS (Plan 02) and Android (Plan 03) suites: those suites load the vector copies this plan emits. The version-field "bump fails the Elixir suite" property is ALREADY owned by Phase 122 GUARD-01 — this plan does NOT duplicate it; it adds the *behavioral* proof that the vectors map to genuine Elixir denial reasons.

Purpose: Single committed vector file becomes the sole source of expected request→outcome pairs for all three language suites (NTEST-01, SC-1), and the Elixir suite proves the vectors are not fabricated.
Output: Expanded gen task, regenerated fixture + two native copies, new Elixir behavioral test.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/123-native-package-behavioral-proof/123-CONTEXT.md
@.planning/phases/123-native-package-behavioral-proof/123-RESEARCH.md
@.planning/phases/123-native-package-behavioral-proof/123-PATTERNS.md

# Gen task to extend (read in full before editing — uses pairs-list encoding + write_if_changed)
@lib/mix/tasks/crosswake.contract.gen.ex

# Elixir seam + struct shapes the behavioral test drives (READ before writing the test)
@lib/crosswake/compatibility/compatibility.ex
@lib/crosswake/manifest/types.ex
@lib/crosswake/bridge/contract.ex
@lib/crosswake/shell/denial.ex

# Analog test — manifest fixture builder + bridge_findings/finding_to_denial call patterns
@test/crosswake/compatibility/compatibility_test.exs

# GUARD-01 tripwire list (D-05 decision: extend with the two native copies)
@test/crosswake/contract/contract_drift_test.exs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Expand the gen task to author 7 vectors with session_override and emit two native copies</name>
  <files>lib/mix/tasks/crosswake.contract.gen.ex, test/fixtures/bridge_contract_vectors.json, packages/crosswake-shell-core-ios/Tests/CrosswakeShellCoreTests/Resources/bridge_contract_vectors.json, packages/crosswake-shell-core-android/src/test/resources/bridge_contract_vectors.json</files>
  <read_first>
    - lib/mix/tasks/crosswake.contract.gen.ex (full file — @vectors_path/@*_activation_path constant block lines 33-36; run/1 write_if_changed block lines 47-50; seed_vectors/1 lines 115-141; pairs-list encoding pairs_to_map/convert_value lines 170-220; write_if_changed/2 lines 226-248)
    - .planning/phases/123-native-package-behavioral-proof/123-RESEARCH.md §"Expanded Vector Schema" (the 7-vector table + session_override field semantics + the worked vec-004..vec-007 seed_vectors snippet)
    - .planning/phases/123-native-package-behavioral-proof/123-PATTERNS.md §"lib/mix/tasks/crosswake.contract.gen.ex (EDIT — gen task)" (the @path + emit + summary patterns to copy verbatim)
  </read_first>
  <behavior>
    - After regeneration, test/fixtures/bridge_contract_vectors.json contains exactly 7 vectors: vec-001-version-mismatch-deny (compatibility_mismatch), vec-002-unknown-command-deny (undeclared_capability), vec-003-canonical-version-ok (ok / null reason), vec-004-inactive-route-deny (inactive_route), vec-005-origin-denied-deny (origin_denied), vec-006-pack-incompatible-deny (pack_incompatible), vec-007-capability-version-deny (unavailable_capability)
    - Every vector object carries the keys: id, description, request_override, session_override, expected_outcome, expected_denial_reason (existing vec-001..003 gain an empty session_override list so the schema is uniform per D-02)
    - The iOS copy and the Android copy are byte-identical to the repo-root fixture
    - Re-running mix crosswake.contract.gen a second time produces zero git diff (idempotent)
  </behavior>
  <action>
Extend the canonical gen task per D-02/D-03/D-04 and the RESEARCH §"Expanded Vector Schema" seed snippet.

(1) Add two module attributes immediately after @vectors_path (PATTERNS §gen-task shows the exact lines): @ios_vectors_path pointing at packages/crosswake-shell-core-ios/Tests/CrosswakeShellCoreTests/Resources/bridge_contract_vectors.json and @android_vectors_path pointing at packages/crosswake-shell-core-android/src/test/resources/bridge_contract_vectors.json.

(2) In run/1, after the existing @vectors_path write_if_changed and before @docs_snippet_path, add two write_if_changed calls passing the SAME vectors_json(protocol, bridge_vsn, commands, denial_reasons) expression to @ios_vectors_path and @android_vectors_path so the copies are byte-identical. write_if_changed already calls File.mkdir_p! on the dirname, so the Resources/ and resources/ directories are created automatically (Pitfall 5: add the @path constant AND the write_if_changed call together — do not add one without the other).

(3) Extend the run/1 trailing Mix.shell().info summary block to list the two new output paths (the summary doubles as the emit checklist).

(4) Expand seed_vectors/1 from 3 to 7 vectors using the pairs-list encoding already in the file. Add a {"session_override", [...]} key to EVERY vector (existing vec-001..003 get an empty list; new vec-004..007 get their override per the RESEARCH table). Use the worked seed_vectors snippet in RESEARCH §"How to extend vectors_json/4" for vec-004..vec-007 verbatim in structure. For the ok vector (vec-003) and the capability/pack vectors, follow the exact field set in the RESEARCH 7-vector table (request_override carries version/command/capability/route_id/active_route_id/origin as needed; session_override carries capabilities/route_required_packs/installed_packs). Drive all version literals through the passed bridge_vsn variable, never a hardcoded string, EXCEPT vec-001 whose request_override.version is the deliberately-stale "1.0.0" (it must be a literal older version to exercise the mismatch — this is the one intentional non-canonical literal and it is a denial fixture, mirroring the existing vec-001 which already hardcodes "1.0.0").

(5) Do NOT change pairs_to_map/convert_value/encode_doc/write_if_changed — the new content routes through the same deterministic sorted-key path, inheriting GUARD-02 safety. Do NOT introduce any second hand-maintained version literal (D-10).

(6) After editing, RUN mix crosswake.contract.gen so the committed fixtures (root + both copies) are fresh; then run it a second time and confirm zero diff.
  </action>
  <verify>
    <automated>mix crosswake.contract.gen && mix crosswake.contract.gen && git add -A && git diff --cached --stat -- test/fixtures/bridge_contract_vectors.json packages/crosswake-shell-core-ios/Tests/CrosswakeShellCoreTests/Resources/bridge_contract_vectors.json packages/crosswake-shell-core-android/src/test/resources/bridge_contract_vectors.json && test "$(grep -c '"id"' test/fixtures/bridge_contract_vectors.json)" = "7" && diff test/fixtures/bridge_contract_vectors.json packages/crosswake-shell-core-ios/Tests/CrosswakeShellCoreTests/Resources/bridge_contract_vectors.json && diff test/fixtures/bridge_contract_vectors.json packages/crosswake-shell-core-android/src/test/resources/bridge_contract_vectors.json</automated>
  </verify>
  <done>seed_vectors/1 emits 7 vectors each with a session_override key; the gen task writes the iOS and Android copies byte-identical to the root fixture; two consecutive gen runs produce zero diff; the iOS and Android copies match the root fixture exactly.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Write the Elixir behavioral vector test driving bridge_findings/2 (anti-vacuous proof)</name>
  <files>test/crosswake/bridge/bridge_behavioral_vector_test.exs</files>
  <read_first>
    - lib/crosswake/compatibility/compatibility.ex (bridge_findings/2 at line 92 — the 7 checks and their order; finding_to_denial/2 at line 107; the >= semver floor compatible_version?/2 around line 616)
    - lib/crosswake/manifest/types.ex (new_root/1 line 657, new_host/1 line 674, new_compatibility/1 line 685, new_capability/1 line 701, new_route_entry/1 line 720, and default_origin/0 — the factory functions the manifest fixture must use; never build raw %Root{} struct literals)
    - lib/crosswake/bridge/contract.ex (new_request/1 line 115 — the request constructor and its accepted keyword fields)
    - lib/crosswake/shell/denial.ex (the Denial.reason atom set; Atom.to_string maps 1:1 to the JSON denial_reasons vocabulary)
    - test/crosswake/compatibility/compatibility_test.exs (the bridge_manifest_fixture/0 builder ~lines 445-482 and the bridge_findings + finding_to_denial call patterns ~lines 161-233 — copy the factory-call shape)
    - .planning/phases/123-native-package-behavioral-proof/123-RESEARCH.md §"CRITICAL: D-09 Answer" (the harness shape, the >= floor note, the check-order difference) and §"Common Pitfalls" (Pitfalls 2 and 7)
    - .planning/phases/123-native-package-behavioral-proof/123-PATTERNS.md §"bridge_behavioral_vector_test.exs" (the manifest fixture + iteration patterns)
  </read_first>
  <behavior>
    - For each of the 7 vectors loaded from test/fixtures/bridge_contract_vectors.json, the test applies the vector's request_override to a base permissive request and its session_override to a base permissive manifest, calls Crosswake.Compatibility.bridge_findings/2, and reduces findings to {outcome, reason}: empty findings -> {"ok", nil}; non-empty -> {"deny", Atom.to_string(finding_to_denial(first).reason)}
    - Every vector's computed outcome equals its expected_outcome
    - Every deny vector's computed reason equals its expected_denial_reason
    - The base permissive manifest declares bridge_protocol_version from @vectors["bridge_protocol_version"] (loaded from the file, never a hardcoded literal per D-10)
    - The ok vector (vec-003) produces an empty findings list (Elixir bridge_findings does not check delegate wiring, only that the capability is declared on the route + registry — document this asymmetry vs native in the moduledoc per RESEARCH Open Question 3)
  </behavior>
  <action>
Create test/crosswake/bridge/bridge_behavioral_vector_test.exs, module Crosswake.Bridge.BridgeVectorBehavioralTest, use ExUnit.Case async: true. This is the D-09 anti-vacuous Elixir behavioral proof; it runs in the normal mix test invocation (already merge-blocking) and does NOT belong in the new native CI gate.

Load the committed vectors at compile time via a module attribute: @vectors Jason.decode!(File.read!("test/fixtures/bridge_contract_vectors.json")) so a missing file fails fast. Read the canonical version from @vectors["bridge_protocol_version"] — never hardcode it (D-10).

Build a make_permissive_manifest/1 helper using ONLY the Types factory functions confirmed in types.ex (new_root, new_host, new_compatibility, new_route_entry, new_capability, default_origin) and SupportMatrix.canonical/0, following the bridge_manifest_fixture/0 shape in compatibility_test.exs. The base manifest must make ALL checks pass: a "dashboard" route present with the app.info.get capability and allowlisted_origins including the default origin; capability_registry declaring app.info.get; compatibility.bridge_protocol_version set to @vectors["bridge_protocol_version"]. Accept keyword overrides so session_override fields (capabilities, route_required_packs, installed_packs, origin, route_id) can target exactly one denial path per vector (Pitfall 2 + Pitfall 7: each vector must fail exactly one check given the Elixir check order, which differs from native — design the base so non-targeted checks always pass).

Build a make_permissive_request/1 helper using Contract.new_request/1 with all fields defaulting to the passing values (command app.info.get, capability app.info.get, version @vectors["bridge_protocol_version"], route_id/active_route_id dashboard, origin default, native_runtime_version 1.0.0, capabilities map declaring the requested capability). Accept keyword overrides mapped from each vector's request_override.

Write a helper that translates a vector's JSON request_override/session_override maps into the keyword overrides the two builders accept (string keys -> the right keyword), so the loop is genuinely data-driven (Pitfall 1: actually apply the overrides — do not ignore them).

Write one test that iterates @vectors["vectors"], computes {outcome, reason} via bridge_findings/2 then finding_to_denial/2, and asserts outcome == expected_outcome and (when expected_denial_reason is non-null) reason == expected_denial_reason, with the failing vector id in the assertion message.

Add a moduledoc documenting the Elixir-vs-native check-order difference and the delegate asymmetry on the ok path (RESEARCH Open Question 3).

Note for the executor on the >= floor: vec-001 sends request version "1.0.0" against a manifest at the canonical version (e.g. 1.1.0); since Elixir uses available >= required (>= floor), 1.0.0 < 1.1.0 still DENIES with compatibility_mismatch — so vec-001 works under >= semantics. Do not "fix" this by switching to exact-match; the >= floor is correct Elixir behavior (the native exact-match reconciliation is Phase 124, out of scope here).
  </action>
  <verify>
    <automated>mix test test/crosswake/bridge/bridge_behavioral_vector_test.exs</automated>
  </verify>
  <done>The behavioral test passes for all 7 vectors; each vector's expected_outcome and expected_denial_reason match the real Crosswake.Compatibility.bridge_findings/2 result; the test loads the version from the committed JSON and hardcodes no version literal.</done>
</task>

<task type="auto">
  <name>Task 3: Extend GUARD-01 tripwire list with the two native vector copies (D-05)</name>
  <files>test/crosswake/contract/contract_drift_test.exs</files>
  <read_first>
    - test/crosswake/contract/contract_drift_test.exs (the @generated_json_paths list lines 34-38 — generated files whose bridge_protocol_version lives at the JSON document root; compare_generated_surface/3 is already shape-correct for these copies)
    - .planning/phases/123-native-package-behavioral-proof/123-CONTEXT.md D-05 (planner discretion to extend GUARD-01 tripwire — DECISION: extend, for a friendlier local mix-test failure since the two new copies carry bridge_protocol_version at the document root identical to the existing tripwire entries)
  </read_first>
  <action>
DECISION (D-05): extend GUARD-01's tripwire list. The two new native copies carry bridge_protocol_version at the JSON document root, exactly like the existing @generated_json_paths entries, so they fit compare_generated_surface/3 with zero new helper code and give developers a fast local mix-test tripwire alongside the CI-level GUARD-02 diff.

Add the two paths to the @generated_json_paths list in test/crosswake/contract/contract_drift_test.exs: packages/crosswake-shell-core-ios/Tests/CrosswakeShellCoreTests/Resources/bridge_contract_vectors.json and packages/crosswake-shell-core-android/src/test/resources/bridge_contract_vectors.json. Make no other change — the existing "readability: all surfaces exist" test, the primary drift assertion, and the synthetic regressions all already iterate @generated_json_paths and the @all_surface_paths derivation, so they pick up the new entries automatically. Update the inline count word in the "readability" test description only if it names a specific number ("six"); if it does, bump it to match the new total, otherwise leave the comments accurate.
  </action>
  <verify>
    <automated>mix test test/crosswake/contract/contract_drift_test.exs && grep -c "packages/crosswake-shell-core" test/crosswake/contract/contract_drift_test.exs | grep -qx 2 && echo OK</automated>
  </verify>
  <done>GUARD-01 now parse-asserts the two native vector copies; the full contract_drift_test suite (primary + synthetic regressions + readability) passes with the two added paths.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| canonical Elixir source → committed JSON fixtures | The gen task is the sole author; hand-edits to generated copies are the drift risk |
| test process → filesystem | The behavioral test reads committed fixtures only; builds manifests in-memory (hermetic) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-123-01 | Tampering | DO-NOT-EDIT native vector copies could be hand-edited to mask drift | mitigate | GUARD-02 (generate-and-diff) byte-diffs all gen outputs incl. the two new copies for free; this plan additionally adds them to GUARD-01's parse tripwire (D-05) for a fast local failure |
| T-123-02 | Tampering | Vectors could be authored as a vacuous proof (native-only assertions, no real Elixir behavior) | mitigate | Task 2 runs every vector through the real Crosswake.Compatibility.bridge_findings/2 decision path and asserts the genuine denial reason (anti-vacuous, PITFALLS rule) |
| T-123-03 | Information disclosure / escape | Behavioral test touching network or real manifest files | accept | Test builds in-memory manifests via Types factories and reads only the committed fixture; no network, no external manifest load (RESEARCH §Elixir Seam) |
| T-123-SC | Tampering | npm/pip/cargo installs | accept | No package-manager installs in this plan (Elixir-only edits; no new deps) — package legitimacy gate N/A |
</threat_model>

<verification>
- `mix crosswake.contract.gen && mix crosswake.contract.gen && git add -A && git diff --cached --exit-code` (idempotent emit; GUARD-02-safe)
- `mix test test/crosswake/bridge/bridge_behavioral_vector_test.exs` (7 vectors map to real Elixir denial reasons)
- `mix test test/crosswake/contract/contract_drift_test.exs` (GUARD-01 incl. the two native copies)
- `diff` of root fixture vs both native copies (byte-identical)
</verification>

<success_criteria>
- The expanded 7-vector fixture exists at the repo root and is emitted byte-identically into both native test-resource directories by the gen task.
- The Elixir behavioral vector test passes, proving every vector maps to real Crosswake.Compatibility.bridge_findings/2 behavior (anti-vacuous, NTEST-01 / SC-1).
- No version literal is hand-maintained outside the canonical Elixir source (D-10).
- Two consecutive gen runs produce zero diff.
</success_criteria>

<output>
Create `.planning/phases/123-native-package-behavioral-proof/123-01-SUMMARY.md` when done. Record: the final vector ids + denial reasons, the two new @*_vectors_path constants, the new test module name (Crosswake.Bridge.BridgeVectorBehavioralTest), and the GUARD-01 tripwire decision (D-05: extended).
</output>
