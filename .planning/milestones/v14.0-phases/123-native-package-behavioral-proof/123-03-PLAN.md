---
phase: 123-native-package-behavioral-proof
plan: 03
type: execute
wave: 2
depends_on: ["123-01"]
files_modified:
  - packages/crosswake-shell-core-android/build.gradle.kts
  - packages/crosswake-shell-core-android/src/test/java/dev/crosswake/shell/core/BridgeConformanceTest.kt
  - packages/crosswake-shell-core-android/src/test/java/dev/crosswake/shell/core/ActivationConformanceTest.kt
autonomous: true
requirements: [NTEST-03]
must_haves:
  truths:
    - "./gradlew test runs JVM JUnit 4 tests in packages/crosswake-shell-core-android/ with no emulator"
    - "The Android tests load bridge_contract_vectors.json from the classpath root via getResourceAsStream(\"/bridge_contract_vectors.json\")"
    - "A data-driven JUnit test iterates all bridge vectors and asserts each reply's status and denial reason string against the vector's expected values via evaluateForTesting"
    - "All six behaviors are covered: activation success/failure, bridge version-mismatch denial, capability allowlist, active-route, pack-version, delegate/escape-hatch (present->ok, absent->deny)"
    - "No test hardcodes a bridge protocol version literal — every version is read from the loaded vectors file; async paths (if any) use runTest, never runBlocking"
  artifacts:
    - path: "packages/crosswake-shell-core-android/build.gradle.kts"
      provides: "kotlinx-coroutines-test test dependency (only if an async path is exercised)"
      contains: "kotlinx-coroutines-test"
    - path: "packages/crosswake-shell-core-android/src/test/java/dev/crosswake/shell/core/BridgeConformanceTest.kt"
      provides: "Data-driven bridge evaluateForTesting() vector tests + delegate escape-hatch tests"
      contains: "evaluateForTesting"
    - path: "packages/crosswake-shell-core-android/src/test/java/dev/crosswake/shell/core/ActivationConformanceTest.kt"
      provides: "ActivationCoordinator activate() success + denial tests, version-anchored to the vectors file"
      contains: "bridge_protocol_version"
  key_links:
    - from: "packages/crosswake-shell-core-android/src/test/java/dev/crosswake/shell/core/BridgeConformanceTest.kt"
      to: "packages/crosswake-shell-core-android/src/test/resources/bridge_contract_vectors.json"
      via: "javaClass.getResourceAsStream(\"/bridge_contract_vectors.json\") loads the gen-emitted copy from the classpath root"
      pattern: "getResourceAsStream\\(\"/bridge_contract_vectors.json\"\\)"
    - from: "packages/crosswake-shell-core-android/src/test/java/dev/crosswake/shell/core/BridgeConformanceTest.kt"
      to: "packages/crosswake-shell-core-android/src/main/java/dev/crosswake/shell/core/BridgeChannel.kt"
      via: "BridgeChannel.evaluateForTesting(request) driven per vector; denial.denial.reason asserted from parsed reply JSON"
      pattern: "evaluateForTesting"
---

<objective>
Write the Android JVM JUnit 4 behavioral suites covering all six native behaviors — bridge vectors driven data-driven from the gen-emitted bridge_contract_vectors.json classpath resource (NTEST-03), activation paths code-level but version-anchored to the same file (D-01) — and add kotlinx-coroutines-test only if an async path is exercised.

Purpose: The published crosswake-shell-core-android package self-certifies its contract behavior on the JVM with no emulator, parametrized from the shared vectors (SC-3). This is the merge-blocking native lane.
Output: build.gradle.kts dependency (conditional), two new JUnit test files.

Depends on Plan 01: the Android copy of bridge_contract_vectors.json is emitted by the gen task into src/test/resources/ (do not author it here).

Parallel sibling of Plan 02 (iOS) — disjoint package tree, same wave.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/123-native-package-behavioral-proof/123-CONTEXT.md
@.planning/phases/123-native-package-behavioral-proof/123-RESEARCH.md
@.planning/phases/123-native-package-behavioral-proof/123-PATTERNS.md

# Android seams to drive (READ before writing tests — signatures, denial-reason strings, reply JSON paths)
@packages/crosswake-shell-core-android/src/main/java/dev/crosswake/shell/core/BridgeChannel.kt
@packages/crosswake-shell-core-android/src/main/java/dev/crosswake/shell/core/ActivationCoordinator.kt
@packages/crosswake-shell-core-android/src/main/java/dev/crosswake/shell/core/packs/PackStore.kt
@packages/crosswake-shell-core-android/src/main/java/dev/crosswake/shell/core/CrosswakeDelegates.kt
@packages/crosswake-shell-core-android/build.gradle.kts

# Existing JUnit 4 pattern to extend (anonymous delegate objects; no mocking library)
@packages/crosswake-shell-core-android/src/test/java/dev/crosswake/shell/core/CrosswakeShellConfigTest.kt

# Full working JUnit example (§4) and seam inventory (§6) — superseded only on §9 (native constants, rejected D-10) and §10 (split, advisory iOS per D-07)
@.planning/research/NATIVE-TESTING.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write the data-driven bridge conformance JUnit suite</name>
  <files>packages/crosswake-shell-core-android/src/test/java/dev/crosswake/shell/core/BridgeConformanceTest.kt, packages/crosswake-shell-core-android/build.gradle.kts</files>
  <read_first>
    - packages/crosswake-shell-core-android/src/main/java/dev/crosswake/shell/core/BridgeChannel.kt (evaluateForTesting(request): String ~line 307 is synchronous; init ~line 43; denial-reason strings at lines 102/106/110/119/122/135/143; reply JSON path: JSONObject(reply).getString("status") and getJSONObject("denial").getJSONObject("denial").getString("reason"))
    - packages/crosswake-shell-core-android/src/main/java/dev/crosswake/shell/core/CrosswakeDelegates.kt (delegate interfaces for the escape-hatch tests)
    - packages/crosswake-shell-core-android/src/test/java/dev/crosswake/shell/core/CrosswakeShellConfigTest.kt (anonymous object : Delegate pattern; package + imports)
    - .planning/phases/123-native-package-behavioral-proof/123-RESEARCH.md §"Code Examples" Kotlin section (the JSONObject load, evaluateForTesting + reply parse), §"Kotlin Resource Loading" (leading-slash requirement), §"Common Pitfalls" Pitfall 1 (apply session_override) + Pitfall 4 (leading slash)
    - .planning/phases/123-native-package-behavioral-proof/123-PATTERNS.md §"BridgeConformanceTest.kt (NEW)"
    - .planning/research/NATIVE-TESTING.md §4 (the fuller JUnit example + makeSession/makeRequest/makeConfig helper shapes)
  </read_first>
  <behavior>
    - A @Before method loads bridge_contract_vectors.json via javaClass.getResourceAsStream("/bridge_contract_vectors.json") (leading slash required, Pitfall 4) into a JSONObject
    - A data-driven @Test iterates vectors; for each it builds a session (applying session_override), a request (applying request_override), and a config, constructs BridgeChannel, calls evaluateForTesting, and asserts JSONObject(reply).getString("status") == expected_outcome and (when present) denial.denial.reason == expected_denial_reason — with the vector id in the failure message
    - The ok vector configures an appInfoDelegate so it does not deny (the Android delegate-missing path returns unavailable_capability per BridgeChannel.kt:143)
    - Two symmetric delegate escape-hatch tests (present->ok, absent->deny) using anonymous delegate objects
    - The bridge version used to build sessions/requests is read from vectorsJson.getString("bridge_protocol_version"), never a literal (D-10)
  </behavior>
  <action>
Create BridgeConformanceTest.kt in package dev.crosswake.shell.core per RESEARCH §Code Examples (Kotlin) and NATIVE-TESTING §4, driving the real evaluateForTesting seam.

Use org.json.JSONObject (available on the JVM test classpath, as the reply-parse path in BridgeChannel.kt already uses it). In @Before, load via javaClass.getResourceAsStream("/bridge_contract_vectors.json")!! (leading slash mandatory — without it the resource resolves relative to the package and returns null, Pitfall 4). Read bridge_protocol_version from the JSONObject.

Write makeSession(bridgeVersion, sessionOverride), makeRequest(bridgeVersion, requestOverride), and makeConfig(vector) helpers that START permissive (everything passes) and APPLY each vector's session_override/request_override before constructing BridgeChannel (Pitfall 1 — actually apply overrides). The ok-path config includes an appInfoDelegate stub via object : AppInfoDelegate { ... } (existing pattern in CrosswakeShellConfigTest.kt).

Write the data-driven @Test iterating the vectors array. Construct BridgeChannel(session, transferCoordinator = null, config = config), call evaluateForTesting(request) (synchronous — no runTest/runBlocking needed for bridge tests), parse the reply JSON, and assert status == expected_outcome and, when expected_denial_reason is present, the nested denial.denial.reason string. Include the vector id in every assertEquals message.

Add the two delegate escape-hatch tests (present->ok, absent->deny) with anonymous delegate objects.

Only if a test actually exercises an async path (e.g. PackStore.installRequiredPack()'s delay()): add testImplementation("org.jetbrains.kotlinx:kotlinx-coroutines-test:1.7.3") to build.gradle.kts (version matches the existing kotlinx-coroutines-android:1.7.3) and wrap that single async test in runTest — NEVER runBlocking (D-13). The bridge evaluateForTesting path is synchronous, so most/all tests need no coroutines; do NOT add the dependency speculatively if no async test is written.

Assert on the reply JSON's reason string only — never on an internal guard firing (D-14). Never declare a BridgeChannel.PROTOCOL_VERSION constant (D-10).
  </action>
  <verify>
    <automated>cd packages/crosswake-shell-core-android && ./gradlew test --tests "dev.crosswake.shell.core.BridgeConformanceTest" 2>&1 | tail -25</automated>
  </verify>
  <done>BridgeConformanceTest loads the vectors via classpath getResourceAsStream with leading slash, drives evaluateForTesting for every vector, and passes status + denial-reason assertions per vector plus the two delegate escape-hatch tests; no version literal is hardcoded; if any async path is tested it uses runTest (never runBlocking).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Write the activation conformance JUnit suite (success + denial paths)</name>
  <files>packages/crosswake-shell-core-android/src/test/java/dev/crosswake/shell/core/ActivationConformanceTest.kt</files>
  <read_first>
    - packages/crosswake-shell-core-android/src/main/java/dev/crosswake/shell/core/ActivationCoordinator.kt (activate(request): ShellPresentation seam; RouteDenialReason enum ~lines 28-36; the injected config/manifestLoader/requestLoader/packStore constructor)
    - packages/crosswake-shell-core-android/src/main/java/dev/crosswake/shell/core/packs/PackStore.kt (PackStore.inMemory(...) static factory ~line 87; blockingStatus(...))
    - .planning/research/NATIVE-TESTING.md §6.5 (PackStore.inMemory) and §6.6 (ActivationCoordinator injected loaders) and §4 (activation test examples)
    - .planning/phases/123-native-package-behavioral-proof/123-PATTERNS.md §"ActivationConformanceTest.kt (NEW)" (@Before loads bridge_protocol_version for version-anchoring)
  </read_first>
  <behavior>
    - A @Before loads bridge_protocol_version from the classpath vectors file for version-anchoring per D-01
    - Activation success: ActivationCoordinator with permissive injected loaders + PackStore.inMemory(...) produces a LiveView/success ShellPresentation
    - Activation failure: an inactive-route or required-pack scenario produces a denial with the expected RouteDenialReason
    - No hardcoded version literal (version comes from the loaded file)
  </behavior>
  <action>
Create ActivationConformanceTest.kt in package dev.crosswake.shell.core covering the activation half of the six behaviors (activation success/failure) at the code level, version-anchored to the vectors file per D-01 (load bridge_protocol_version in @Before; use it when constructing manifests/requests so a version bump in the regenerated copy flows through).

Construct ActivationCoordinator with injected config/manifestLoader/requestLoader/packStore per NATIVE-TESTING §6.6 and PackStore.inMemory(...) per §6.5 — no Context, no emulator. Write at least: one success test (success ShellPresentation), one inactive-route denial test, and one required-pack denial test, asserting on the resulting presentation / RouteDenialReason. Use anonymous object : ... stubs for delegates/loaders (no mocking library).

Assert on the resolved outcome value, not an internal guard (D-14). Do not add a native version constant (D-10).
  </action>
  <verify>
    <automated>cd packages/crosswake-shell-core-android && ./gradlew test 2>&1 | tail -25</automated>
  </verify>
  <done>ActivationConformanceTest passes; activation success, inactive-route denial, and required-pack denial are covered; the full ./gradlew test suite (bridge + activation + existing config test) is green on the JVM with no emulator; version is read from the vectors file.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test classpath → vectors resource | Tests read the gen-emitted DO-NOT-EDIT copy from src/test/resources via getResourceAsStream; no external file load |
| test code → BridgeChannel/ActivationCoordinator seams | Public test seams only (evaluateForTesting, inMemory); no production code change |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-123-07 | Tampering | A native PROTOCOL_VERSION constant reintroduced in Kotlin would create a new drift surface | mitigate | D-10 enforced: tests read bridge_protocol_version from the loaded vectors file; no BridgeChannel.PROTOCOL_VERSION constant added |
| T-123-08 | Spoofing of proof | Tests could assert on an internal guard rather than the observable reply JSON ("test the fake") | mitigate | D-14 enforced: assertions are on parsed reply JSON status + denial.denial.reason strings only |
| T-123-09 | Non-determinism | runBlocking causes real-time waits and flaky async assertions | mitigate | D-13 enforced: any async path uses runTest from kotlinx-coroutines-test; runBlocking forbidden; bridge path is synchronous and needs no coroutines |
| T-123-SC | Tampering | npm/pip/cargo installs (Gradle/Maven) | accept | Only dep possibly added is kotlinx-coroutines-test:1.7.3 — JetBrains first-party, matches the already-declared kotlinx-coroutines-android:1.7.3 (RESEARCH Package Legitimacy Audit: VERIFIED, official repo); no [ASSUMED]/[SUS] package, no checkpoint needed |
</threat_model>

<verification>
- `cd packages/crosswake-shell-core-android && ./gradlew test` (full suite green on JVM, no emulator)
- Bridge suite asserts status + denial reason per vector, loaded via classpath getResourceAsStream with leading slash
- Activation suite covers success + inactive-route + required-pack denial
- `grep` confirms no hardcoded bridge version literal in the Kotlin test files; no runBlocking
</verification>

<success_criteria>
- ./gradlew test compiles and passes in packages/crosswake-shell-core-android/ with no emulator (NTEST-03 / SC-3).
- All six behaviors covered; bridge behaviors data-driven from the gen-emitted vectors copy.
- No version literal hardcoded in any Android test file (D-10); any async path uses runTest not runBlocking (D-13).
- kotlinx-coroutines-test added only if an async path is exercised.
</success_criteria>

<output>
Create `.planning/phases/123-native-package-behavioral-proof/123-03-SUMMARY.md` when done. Record: the new Kotlin test class names (BridgeConformanceTest, ActivationConformanceTest), whether kotlinx-coroutines-test was added (and why), and which behaviors each file covers.
</output>
