---
phase: 123-native-package-behavioral-proof
plan: 04
type: execute
wave: 3
depends_on: ["123-02", "123-03"]
files_modified:
  - .github/workflows/native-behavioral-proof-gate.yml
  - script/register-native-gate.sh
autonomous: false
requirements: [NTEST-04]
user_setup:
  - service: github-branch-protection
    why: "Registering merge-blocking-native-behavioral-proof as a required status check needs an admin-scoped gh api PATCH; historically human/harness-gated (v12.0 pattern) and refuses until the aggregator is green on main"
    dashboard_config:
      - task: "Run script/register-native-gate.sh after the gate is green on main (the green-first preflight refuses otherwise); confirm the documented gh api -X PATCH is applied out-of-band"
        location: "Local shell with gh CLI authenticated (repo admin scope)"
must_haves:
  truths:
    - "A new workflow native-behavioral-proof-gate.yml runs android-package-unit (ubuntu, ./gradlew test) as merge-blocking and ios-package-unit (macos, swift test) as advisory"
    - "The aggregator merge-blocking-native-behavioral-proof needs ONLY the Android job (iOS is NOT in needs:) and uses if: always() + re-actors/alls-green"
    - "A register-native-gate.sh script registers merge-blocking-native-behavioral-proof as the sole new required check, with a green-first refuse guard and a granular gh api PATCH that preserves existing required checks"
    - "The script documents the PATCH and does NOT auto-toggle branch protection"
  artifacts:
    - path: ".github/workflows/native-behavioral-proof-gate.yml"
      provides: "Android-blocking + iOS-advisory + alls-green aggregator CI gate"
      contains: "merge-blocking-native-behavioral-proof"
    - path: "script/register-native-gate.sh"
      provides: "Green-first, granular-PATCH, script-not-auto-toggle registration for the new required check"
      contains: "merge-blocking-native-behavioral-proof"
  key_links:
    - from: ".github/workflows/native-behavioral-proof-gate.yml"
      to: "packages/crosswake-shell-core-android"
      via: "android-package-unit job runs ./gradlew test in the package dir"
      pattern: "gradlew test"
    - from: "script/register-native-gate.sh"
      to: ".github/workflows/native-behavioral-proof-gate.yml"
      via: "registers the aggregator job name as a required status check"
      pattern: "NEW_CHECK.*merge-blocking-native-behavioral-proof"
---

<objective>
Create the dedicated native behavioral proof CI gate (Android merge-blocking, iOS advisory) and the registration script, mirroring the Phase-122 contract-drift-gate idiom (NTEST-04). The aggregator merge-blocking-native-behavioral-proof is the sole new required status check; the actual branch-protection PATCH is human/harness-gated (D-08).

Purpose: Establishes the required-vs-advisory split (SC-4) — deterministic JVM lane blocks merges, macOS Xcode lane is advisory — and the documented, green-first registration path.
Output: native-behavioral-proof-gate.yml, register-native-gate.sh.

Depends on Plans 02 and 03: the iOS and Android suites must exist so swift test and ./gradlew test have real tests to run.
This plan is NOT autonomous: the registration PATCH is a documented human step (branch protection has historically been harness-blocked in this environment).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/123-native-package-behavioral-proof/123-CONTEXT.md
@.planning/phases/123-native-package-behavioral-proof/123-RESEARCH.md
@.planning/phases/123-native-package-behavioral-proof/123-PATTERNS.md

# Exact CI topology + register-script idiom to clone (Phase 122 / v12.0 pattern)
@.github/workflows/contract-drift-gate.yml
@script/register-contract-gate.sh
@.github/workflows/native-collateral-advisory.yml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create native-behavioral-proof-gate.yml (Android blocking, iOS advisory, alls-green aggregator)</name>
  <files>.github/workflows/native-behavioral-proof-gate.yml</files>
  <read_first>
    - .github/workflows/contract-drift-gate.yml (full file — permissions/on triggers lines 34-43; sibling-job shape; the merge-blocking-contract-drift aggregator block lines 113-122 with if: always() + needs + re-actors/alls-green)
    - .planning/phases/123-native-package-behavioral-proof/123-RESEARCH.md section "CI Gate Topology" (the substitution table, cache keys, working-directory patterns) and section "Architecture Patterns" (the system diagram)
    - .planning/phases/123-native-package-behavioral-proof/123-PATTERNS.md section "native-behavioral-proof-gate.yml (NEW)" (the verbatim job blocks)
  </read_first>
  <action>
Create .github/workflows/native-behavioral-proof-gate.yml mirroring contract-drift-gate.yml's topology per D-07. Copy the permissions (contents: read) and the on: (push '**', pull_request main) blocks verbatim. Add a header comment documenting the topology (Android blocking, iOS advisory, aggregator is the sole required check) and the out-of-band registration ordering, matching the contract-drift-gate.yml header style.

Three jobs:
(1) android-package-unit — runs-on ubuntu-latest; steps: actions/checkout@v4, actions/cache@v4 keyed on hashFiles of the android build.gradle.kts (cache path packages/crosswake-shell-core-android/.gradle), then a step with working-directory packages/crosswake-shell-core-android running ./gradlew test, then a step summary line. No erlef/setup-beam (Gradle uses the runner's Java 17). This job is MERGE-BLOCKING (it is in the aggregator needs:).

(2) ios-package-unit — runs-on macos-latest; steps: actions/checkout@v4, actions/cache@v4 keyed on hashFiles of Package.swift (cache path packages/crosswake-shell-core-ios/.build), then a step with working-directory packages/crosswake-shell-core-ios running swift test, then a step summary line. This job is ADVISORY — it runs but is NOT in the aggregator needs: (D-07; this deliberately overrides NATIVE-TESTING.md section 10 which wanted iOS blocking — note that in the header comment).

(3) merge-blocking-native-behavioral-proof — name and job key both merge-blocking-native-behavioral-proof; if: always(); needs lists ONLY android-package-unit (do NOT list ios-package-unit); runs-on ubuntu-latest; single step uses re-actors/alls-green@release/v1 with jobs set to toJSON of needs. This aggregator name is the sole new required status check.
  </action>
  <verify>
    <automated>test -f .github/workflows/native-behavioral-proof-gate.yml && grep -q "merge-blocking-native-behavioral-proof" .github/workflows/native-behavioral-proof-gate.yml && grep -q "macos-latest" .github/workflows/native-behavioral-proof-gate.yml && grep -q "re-actors/alls-green@release/v1" .github/workflows/native-behavioral-proof-gate.yml && grep -A2 "merge-blocking-native-behavioral-proof:" .github/workflows/native-behavioral-proof-gate.yml | grep -q "android-package-unit" && ! grep -q "needs: \[android-package-unit, ios-package-unit\]" .github/workflows/native-behavioral-proof-gate.yml && echo OK</automated>
  </verify>
  <done>The workflow file exists with android-package-unit (ubuntu, gradlew test, blocking), ios-package-unit (macos, swift test, advisory and NOT in needs:), and the merge-blocking-native-behavioral-proof aggregator (if: always(), needs only the Android job, re-actors/alls-green).</done>
</task>

<task type="auto">
  <name>Task 2: Create register-native-gate.sh (green-first guard, granular append-only PATCH, no auto-toggle)</name>
  <files>script/register-native-gate.sh</files>
  <read_first>
    - script/register-contract-gate.sh (full file — the near-verbatim clone source: parameters block lines 38-46, jq desired-payload lines 56-69, DRY_RUN gate, green-first preflight lines 81-100, gh api -X PATCH application lines 102-105)
    - .planning/phases/123-native-package-behavioral-proof/123-RESEARCH.md section "register-native-gate.sh pattern" (the exact NEW_CHECK/OLD_CHECK substitutions)
    - .planning/phases/123-native-package-behavioral-proof/123-PATTERNS.md section "script/register-native-gate.sh (NEW)" (the three-change list)
  </read_first>
  <action>
Create script/register-native-gate.sh as a near-verbatim clone of script/register-contract-gate.sh per D-08. Keep all logic identical EXCEPT:
(1) NEW_CHECK default becomes merge-blocking-native-behavioral-proof (OLD_CHECK stays empty — append-only PATCH; the map(select(.context != $old)) is a no-op).
(2) Header comments reference native-behavioral-proof-gate.yml and NTEST-04 instead of contract-drift-gate.yml and GUARD-02/04.
(3) The final echo / status messages reference the new check name.

Preserve verbatim: the granular required_status_checks endpoint (NOT the full PUT .../protection, so enforce_admins and review requirements stay untouched), strict read from the live value, unique_by(.context) idempotency, the DRY_RUN=1 print-and-exit path, and the green-first preflight that exits 2 until merge-blocking-native-behavioral-proof has a successful run on main (matching on the AGGREGATOR job's conclusion, not a sibling job). Make the script executable (chmod +x).

Do NOT run the PATCH from this task and do NOT auto-toggle branch protection — that is the human checkpoint below (D-08, v12.0 pattern).
  </action>
  <verify>
    <automated>test -x script/register-native-gate.sh && grep -q 'NEW_CHECK="${NEW_CHECK:-merge-blocking-native-behavioral-proof}"' script/register-native-gate.sh && grep -q "unique_by(.context)" script/register-native-gate.sh && grep -q "REFUSING (exit 2)" script/register-native-gate.sh && grep -q "gh api -X PATCH" script/register-native-gate.sh && DRY_RUN=1 bash script/register-native-gate.sh >/dev/null 2>&1 || true; echo "script structurally present"</automated>
  </verify>
  <done>script/register-native-gate.sh exists, is executable, defaults NEW_CHECK to merge-blocking-native-behavioral-proof, keeps the green-first refuse guard + granular append-only PATCH + unique_by(.context), and does not auto-toggle branch protection.</done>
</task>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 3: Human-gated branch-protection registration of the new required check</name>
  <action>Branch-protection registration is human/harness-gated (D-08); do not automate the PATCH. Drive the human checkpoint below.</action>
  <what-built>native-behavioral-proof-gate.yml (Android merge-blocking + iOS advisory + aggregator) and register-native-gate.sh (green-first, documented PATCH).</what-built>
  <how-to-verify>
    Branch protection registration is human/harness-gated (D-08, v12.0 pattern) and is deliberately NOT automated. After this phase's PR is merged and merge-blocking-native-behavioral-proof has gone GREEN on main at least once:
    1. From a shell with gh CLI authenticated (repo admin scope), run: DRY_RUN=1 script/register-native-gate.sh  — review the desired required_status_checks payload (it should append merge-blocking-native-behavioral-proof and preserve all existing required checks + strict).
    2. Run: script/register-native-gate.sh  — the green-first preflight will refuse (exit 2) if the aggregator has not yet gone green on main; that refusal is correct, not a failure.
    3. Confirm merge-blocking-native-behavioral-proof appears in the resulting required checks and that no existing check or enforce_admins setting was altered.
    If the environment harness blocks the PATCH, record it as a documented manual step (matching the Phase 122 register-contract-gate.sh carried item) — the script + documentation are the deliverable, not the applied toggle.
  </how-to-verify>
  <resume-signal>Type "approved" once the registration is applied (or recorded as a documented harness-blocked manual step).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI workflow → repo | Workflow runs untrusted PR code with contents: read only; no write/secrets scope |
| register script → branch protection API | Admin-scoped gh api PATCH mutates required_status_checks; must not weaken existing protection |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-123-10 | Tampering / EoP | register-native-gate.sh PATCH could drop existing required checks or weaken branch protection | mitigate | Uses the GRANULAR required_status_checks endpoint (not full PUT .../protection) so enforce_admins + review reqs are untouched; reads strict from live value; append-only via unique_by(.context); OLD_CHECK empty so nothing is removed |
| T-123-11 | DoS | Registering a never-run required check freezes all open PRs ("Expected — Waiting for status" deadlock) | mitigate | Green-first preflight refuses (exit 2) until merge-blocking-native-behavioral-proof has a successful run on main |
| T-123-12 | EoP | Auto-toggling branch protection without human review | mitigate | autonomous: false; the PATCH is a blocking-human checkpoint; script documents the PATCH and never applies it unattended (D-08, v12.0 pattern) |
| T-123-13 | Elevation via CI | Workflow granting excessive token scope to PR code | mitigate | permissions: contents: read only, copied verbatim from contract-drift-gate.yml; no secrets exposed to the package-test jobs |
| T-123-SC | Tampering | npm/pip/cargo installs in CI | accept | The Android job resolves the existing Gradle graph and the iOS job the existing SwiftPM graph; no new package added in this plan; CI pins re-actors/alls-green@release/v1 and actions/* by the same convention as the existing gates |
</threat_model>

<verification>
- `grep` confirms the workflow has the three jobs with the correct runners and the aggregator needs only android-package-unit (iOS advisory, not in needs:).
- `register-native-gate.sh` is executable, defaults to the new check name, keeps the green-first guard + granular append-only PATCH, and does not auto-toggle.
- The branch-protection PATCH is a blocking-human checkpoint, not an automated step.
</verification>

<success_criteria>
- native-behavioral-proof-gate.yml runs Android JVM tests merge-blocking and Swift macOS tests advisory, rolled up by re-actors/alls-green with the aggregator depending only on Android (NTEST-04 / SC-4).
- register-native-gate.sh mirrors register-contract-gate.sh with the new check name, green-first guard, and documented (not auto-applied) PATCH (D-08).
- No existing required check or branch-protection setting is weakened; registration is human-gated.
</success_criteria>

<output>
Create `.planning/phases/123-native-package-behavioral-proof/123-04-SUMMARY.md` when done. Record: the workflow job names + which is blocking vs advisory, the aggregator name (the sole new required check), the register script path, and the status of the branch-protection PATCH (applied vs documented-harness-blocked).
</output>
