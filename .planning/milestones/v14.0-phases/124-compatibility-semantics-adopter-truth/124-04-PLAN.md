---
phase: 124-compatibility-semantics-adopter-truth
plan: 04
type: execute
wave: 2
depends_on: ["124-02"]
files_modified:
  - lib/crosswake/doctor/publish_readiness.ex
  - test/crosswake/doctor/publish_readiness_test.exs
autonomous: true
requirements: [COMPAT-04]
must_haves:
  truths:
    - "Doctor has a NEW advisory check compatibility_rebuild_guidance separate from contract_version_parity_check"
    - "The advisory baseline is ALWAYS present (change-class taxonomy + denial vocabulary + ordered action sequence + guide link)"
    - "It NEVER emits :error; it elevates to :warning only on detected committed-surface drift"
    - "The action sequence for native-rebuild lists the 4 ordered steps: regenerate shell → rebuild native app → resubmit App Store/Play Store → coordinated deploy"
    - "Detector and adviser share ONE extracted contract_version_parity_errors/1 so they can never disagree"
  artifacts:
    - path: "lib/crosswake/doctor/publish_readiness.ex"
      provides: "compatibility_rebuild_guidance_check + action_sequence_for/1 + extracted contract_version_parity_errors/1"
      contains: "compatibility_rebuild_guidance"
  key_links:
    - from: "lib/crosswake/doctor/publish_readiness.ex"
      to: "lib/crosswake/support_matrix/support_matrix.ex"
      via: "action_sequence_for/1 derives per-class guidance from change_class_entries/0 .adopter_action"
      pattern: "change_class_entries"
    - from: "lib/crosswake/doctor/publish_readiness.ex"
      to: "lib/crosswake/shell/denial.ex"
      via: "alias Crosswake.Shell.Denial; denial vocabulary in the advisory"
      pattern: "Denial"
  prohibitions:
    - "MUST NOT extend contract_version_parity_check — it keeps its single responsibility + the one merge-blocking voice (D-12)"
    - "MUST NOT emit :error / re-block from the new check — parity check owns the blocking voice (D-13)"
    - "MUST NOT claim it observed a live shell denial — microcopy must state it is guidance, not a detected failure (D-13)"
    - "MUST NOT mint new change-class names or denial codes — reuse the 4 verbatim strings + the Denial.reasons/0 vocabulary (D-18)"
    - "MUST NOT cross runtime_line/rebuild_policy.ex atoms (:ota_safe, {:rebuild_required,_}) into this check — different axis (D-18)"
prohibitions_are_blocking: true
---

<objective>
Add a NEW advisory doctor check `compatibility_rebuild_guidance` to `lib/crosswake/doctor/publish_readiness.ex` that names the change class, the full ordered action sequence (regenerate shell → rebuild native app → resubmit App Store/Play Store → coordinated deploy), the denial vocabulary, and a link to the compatibility guide — with honest two-tier severity that NEVER emits `:error`. This is COMPAT-04.

Purpose: When an adopter hits a version/rebuild mismatch, doctor should tell them exactly what to do, in order, without claiming it saw a live denial it cannot observe. Honest-evidence DNA: advise what you can't prove (static baseline), claim only what you detected (elevate to `:warning` only on committed-surface drift it actually sees, sharing the parity check's detector so the two can never disagree).

Output: `compatibility_rebuild_guidance_check`, `action_sequence_for/1`, an extracted shared `contract_version_parity_errors/1`, added to `build_checks/0`, with tests.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/124-compatibility-semantics-adopter-truth/124-CONTEXT.md
@.planning/phases/124-compatibility-semantics-adopter-truth/124-PATTERNS.md
@.planning/phases/124-compatibility-semantics-adopter-truth/124-02-SUMMARY.md
</context>

<artifacts_this_phase_produces>
NEW symbols created here (drift pass: treat as newly-minted):
- `compatibility_rebuild_guidance_check/1` (private) in publish_readiness.ex
- `action_sequence_for/1` (private) — takes a change-class string, returns the ordered action list
- `contract_version_parity_errors/1` (private) — EXTRACTED from the existing contract_version_parity_check error-collection logic so both the parity check and the new adviser call it
- A per-class guidance map helper (e.g. `per_class_guidance_map/0`) and the new check's `details` keys (`active_action_sequence`, `change_class_guidance`, `docs_reference`) — exact key names = Claude's discretion provided D-12..D-15 hold
- `alias Crosswake.Shell.Denial`
Consumes (does NOT create): `change_class_entries/0`, `Denial.reasons/0`, `advisory_check/1`, the 4 verbatim strings.
</artifacts_this_phase_produces>

<context>
@lib/crosswake/doctor/publish_readiness.ex
@lib/crosswake/shell/denial.ex
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract contract_version_parity_errors/1 as the shared detector</name>
  <files>lib/crosswake/doctor/publish_readiness.ex</files>
  <read_first>
    - lib/crosswake/doctor/publish_readiness.ex — `contract_version_parity_check/1` (lines ~594-674), specifically the error-collection logic inlined around lines ~596-647 (manifest_errors ++ generated_errors). Read it fully to extract it without behavior change.
    - .planning/phases/124-compatibility-semantics-adopter-truth/124-PATTERNS.md — "publish_readiness.ex — new check + helpers" section (the `contract_version_parity_errors/1` extraction spec, signature `defp contract_version_parity_errors(cwd) :: [String.t()]`).
    - .planning/phases/124-compatibility-semantics-adopter-truth/124-CONTEXT.md — D-14 (extract so detector and adviser share one detection function).
  </read_first>
  <action>
    Extract the error-collection logic currently inlined inside `contract_version_parity_check/1` into a new private `contract_version_parity_errors(cwd)` returning `[String.t()]` (the list of drift error strings). Refactor `contract_version_parity_check/1` to call it — its observable output (id, code, message, severity, blocking, details) MUST be byte-for-byte unchanged so its existing tests still pass. This is a pure extraction with no behavioral change; it exists so the new advisory check (Task 2) can reuse the EXACT same detection and never disagree with the parity check.
  </action>
  <acceptance_criteria>
    - New private `contract_version_parity_errors/1` exists returning a list of strings.
    - `contract_version_parity_check/1` now delegates error collection to it and is otherwise unchanged.
    - Existing parity tests still pass: `mix test test/crosswake/doctor/publish_readiness_test.exs` (the parity check's id/code/severity/blocking behavior is preserved).
    - `mix compile` is warning-clean for the module.
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/jon/projects/crosswake && mix test test/crosswake/doctor/publish_readiness_test.exs 2>&1 | tail -5</automated>
  </verify>
  <done>contract_version_parity_errors/1 is the single shared detector; the parity check delegates to it with zero observable change and its tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Add the advisory compatibility_rebuild_guidance_check + action_sequence_for/1</name>
  <files>lib/crosswake/doctor/publish_readiness.ex, test/crosswake/doctor/publish_readiness_test.exs</files>
  <read_first>
    - lib/crosswake/doctor/publish_readiness.ex — `generator_coordinate_parity_check/1` (~537-578) and `contract_version_parity_check/1` (~594-674) as structural templates; `advisory_check/1` (~783-792, the builder that respects an explicit `severity:`); `result_check/1` (~776-782, do NOT use — it always emits :error); `build_checks/0` (~162-175, where to append); the `%ReadinessCheck{}` field list + `rebuild_requirement` default (~794-815).
    - lib/crosswake/shell/denial.ex — `reasons/0`: the denial vocabulary (`compatibility_mismatch`, `inactive_route`, `undeclared_capability`, `pack_incompatible`).
    - lib/crosswake/support_matrix/support_matrix.ex — `change_class_entries/0` `.adopter_action` field (the per-class guidance source for action_sequence_for/1).
    - .planning/phases/124-compatibility-semantics-adopter-truth/124-CONTEXT.md — D-12 (new check, do not extend parity), D-13 (two-tier severity, never :error, microcopy disclaimer), D-14 (single-source action sequence via change_class_entries/0; expand native-rebuild into 4 steps), D-15 (output shape: numbered prose in message/hint + ordered details arrays; docs_reference; emit the literal `mix crosswake.contract.gen` command first).
    - .planning/phases/124-compatibility-semantics-adopter-truth/124-PATTERNS.md — "publish_readiness.ex" section (advisory_check usage, severity rules, details shape, build_checks append, required alias).
  </read_first>
  <action>
    Add `alias Crosswake.Shell.Denial`. Add `action_sequence_for/1` taking a change-class string and returning an ordered action list, derived from `change_class_entries/0` `.adopter_action`; expand the `native or companion rebuild required` class into the four discrete ordered steps (regenerate shell → rebuild native app → resubmit App Store/Play Store → coordinated deploy); the per-class subset falls out correctly (`docs-only`/`core-only` → no rebuild/resubmit; `compatibility-bump only` → coordinated core redeploy, no native rebuild; native-rebuild → full 4 steps). Add `compatibility_rebuild_guidance_check/1` built via `advisory_check/1` (never `result_check/1`). Severity: baseline (no detected drift, using the shared `contract_version_parity_errors/1` from Task 1) → `severity: :advisory, result: :pass` carrying the FULL static baseline (the 4-class taxonomy guidance, the Denial denial-code vocabulary, the ordered action sequence, and a `docs_reference: "guides/compatibility.md"`); when `contract_version_parity_errors/1` returns drift → elevate to `severity: :warning, result: :fail`, naming the detected class `native or companion rebuild required`, but NEVER `:error` and NEVER claiming a live denial. Include explicit microcopy in `message`/`hint` stating doctor cannot observe a live shell's denial — this is guidance, not a detected failure — and emit the literal next command `mix crosswake.contract.gen` first, then the numbered imperative steps (`1) … 2) …`). Populate `details` with machine arrays: `active_action_sequence` (ordered), `change_class_guidance` (per-class map), and `docs_reference`. Append `compatibility_rebuild_guidance_check(cwd)` LAST in `build_checks/0`. Do NOT modify the JSONFormatter (details pass through). Do NOT cross `runtime_line/rebuild_policy.ex` atoms into this check. Add tests to publish_readiness_test.exs covering: baseline present-and-advisory, warning-on-detected-drift, never-:error, the 4 ordered steps, denial vocabulary present, guide link present, microcopy disclaimer present.
  </action>
  <acceptance_criteria>
    - `compatibility_rebuild_guidance_check` appears in `build_checks/0` output and is built via `advisory_check/1` (not `result_check/1`).
    - Baseline result: when no parity drift, the check is `severity: :advisory, result: :pass` and its `details.active_action_sequence` is non-empty.
    - The native-rebuild action sequence is exactly 4 ordered steps in order: regenerate shell, rebuild native app, resubmit App Store/Play Store, coordinated deploy.
    - On injected committed-surface drift (same input that makes `contract_version_parity_errors/1` non-empty), the check elevates to `severity: :warning, result: :fail` and names class `native or companion rebuild required`.
    - The check NEVER emits `severity: :error` and is never `blocking: true` (advisory_check keeps blocking false unless severity==:error, which this check never sets): assert across both baseline and drift states.
    - `details` contains `active_action_sequence` + `change_class_guidance` + `docs_reference: "guides/compatibility.md"`.
    - `message`/`hint` contain the disclaimer that doctor cannot observe a live denial, the literal `mix crosswake.contract.gen`, and numbered steps.
    - The denial vocabulary (`compatibility_mismatch` etc., sourced from `Denial`) appears in the advisory.
    - `mix test test/crosswake/doctor/publish_readiness_test.exs` passes (new + existing).
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/jon/projects/crosswake && mix test test/crosswake/doctor/publish_readiness_test.exs 2>&1 | tail -8</automated>
  </verify>
  <done>The advisory compatibility_rebuild_guidance check ships: always-present static baseline, :warning-only-on-detected-drift, never :error, 4-step ordered action sequence, denial vocabulary, guide link, honest microcopy, machine-readable details — sharing the extracted parity detector and tested both ways.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| doctor advisory output → adopter decision | Doctor guidance influences whether an adopter rebuilds/resubmits; overclaiming a detection it didn't make, or re-blocking, would erode trust or block valid releases |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-124-09 | Repudiation / Information Disclosure | compatibility_rebuild_guidance severity | mitigate | Check is built via `advisory_check/1`, never `result_check/1`; tests assert it never emits `:error` and never blocks (D-13). The parity check retains the single blocking voice. |
| T-124-10 | Spoofing (false claim of detection) | advisory microcopy | mitigate | Required microcopy states doctor cannot observe a live denial — guidance, not a detected failure. Elevation to :warning happens ONLY when the shared `contract_version_parity_errors/1` detector returns drift it actually saw (D-13/D-14). |
| T-124-11 | Tampering (detector divergence) | parity check vs adviser | mitigate | Both call the single extracted `contract_version_parity_errors/1` — they cannot disagree by construction (D-14). |
| T-124-SC | Tampering | dependency surface | accept | No package-manager installs (pure Elixir + ExUnit). No legitimacy checkpoint required. |
</threat_model>

<verification>
- `mix test test/crosswake/doctor/publish_readiness_test.exs` passes (parity-extraction preserves behavior; new advisory tested both baseline and drift).
- The new check is advisory-only, never :error, never blocking.
- `mix crosswake.doctor` (json formatter) surfaces the new finding with details arrays passing through.
</verification>

<success_criteria>
COMPAT-04 satisfied: doctor output for a version/rebuild mismatch names the change class, the full ordered action sequence (regenerate → rebuild → resubmit App Store/Play Store → coordinated deploy), the denial vocabulary, and a link to the compatibility guide — as honest advisory guidance that never claims a live denial and never re-blocks, sharing the parity check's detector.
</success_criteria>

<output>
Create `.planning/phases/124-compatibility-semantics-adopter-truth/124-04-SUMMARY.md` when done. Record the final check `code`/`category`/`details` keys and confirm the parity check's tests stayed green after extraction.
</output>
