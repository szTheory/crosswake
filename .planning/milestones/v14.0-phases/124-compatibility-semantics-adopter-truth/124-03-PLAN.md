---
phase: 124-compatibility-semantics-adopter-truth
plan: 03
type: execute
wave: 2
depends_on: ["124-02"]
files_modified:
  - guides/compatibility.md
  - test/crosswake/guides/compatibility_test.exs
autonomous: true
requirements: [COMPAT-03]
must_haves:
  truths:
    - "guides/compatibility.md leads with a JTBD decision table BEFORE any explanatory prose"
    - "The table's rebuild-class names mirror Renderer.render(canonical()) exactly (anti-drift)"
    - "The table has a Denial signal column tying each skipped action to a concrete fail-closed denial code"
    - "All three version axes appear in the table region"
    - "The native_runtime → native-rebuild asymmetry is stated in the doc and locked by the test"
    - "The guide is machine-tested by a new compatibility_test.exs docs-contract test"
  artifacts:
    - path: "guides/compatibility.md"
      provides: "Decision-table-first compatibility guide with Denial signal column"
      contains: "Do I need to rebuild?"
    - path: "test/crosswake/guides/compatibility_test.exs"
      provides: "Docs-contract test: table-first ordering + renderer-mirror + axes + asymmetry + no-second-support-matrix"
      contains: "count_occurrences"
  key_links:
    - from: "test/crosswake/guides/compatibility_test.exs"
      to: "lib/crosswake/support_matrix/renderer.ex"
      via: "asserts each rebuild-class name in compatibility.md also appears in Renderer.render(canonical())"
      pattern: "Renderer.render"
    - from: "test/crosswake/guides/compatibility_test.exs"
      to: "guides/compatibility.md"
      via: "File.read! + ordering/presence asserts"
      pattern: "File.read!"
  prohibitions:
    - "MUST NOT introduce a second support-status table (Target/Version/Status header) — adopter_profiles_test boundary (D-10)"
    - "MUST NOT mint new change-class/rebuild-class names — mirror the canonical 4 strings verbatim (D-10/D-18)"
    - "MUST NOT place explanatory prose before the decision table (table-first is the load-bearing criterion, D-10/D-11)"
    - "MUST NOT delete the existing sections (## Compatibility Axes, ## Companion Compatibility Contract, ## Release Choreography, runtime-line rules) or their cross-references (D-10)"
prohibitions_are_blocking: true
---

<objective>
Restructure `guides/compatibility.md` to lead with a JTBD decision table ("Do I need to rebuild? (start here)") that mirrors the canonical `rebuild_decision_table/0`, demoting prose below it, and add a `compatibility_test.exs` docs-contract test whose two teeth are (1) table-before-first-prose ordering and (2) mirror-agrees-with-renderer. This is COMPAT-03.

Purpose: Tell adopters the truth about upgrade cost without making them read a diff. The `Denial signal` column — naming the concrete fail-closed denial code emitted if they skip an action — is Crosswake's differentiator over prose-only upgrade guides. The guide is a CONSUMER of the canonical source built in 124-02, not a parallel taxonomy.

Output: A decision-table-first `guides/compatibility.md` and a new machine-test guarding ordering, renderer-mirror, axes, asymmetry, and the no-second-support-matrix boundary.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/124-compatibility-semantics-adopter-truth/124-CONTEXT.md
@.planning/phases/124-compatibility-semantics-adopter-truth/124-PATTERNS.md
@.planning/phases/124-compatibility-semantics-adopter-truth/124-02-SUMMARY.md
</context>

<artifacts_this_phase_produces>
NEW symbols/sections created here (drift pass: treat as newly-minted):
- The `## Do I need to rebuild? (start here)` lead section in guides/compatibility.md
- The new file `test/crosswake/guides/compatibility_test.exs` (module e.g. `Crosswake.Guides.CompatibilityTest`) with a `count_occurrences/2` private helper copied from the analog
Consumes (does NOT create): `rebuild_decision_table/0`, the 4 verbatim change-class strings, `Renderer.render/1` (all from 124-02 / existing).
</artifacts_this_phase_produces>

<context>
@guides/compatibility.md
@test/crosswake/guides/adopter_profiles_test.exs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restructure compatibility.md decision-table-first with a Denial signal column</name>
  <files>guides/compatibility.md</files>
  <read_first>
    - guides/compatibility.md — full current structure: lead prose at lines 1-7 (which moves DOWN), the prose-only rebuild content ~lines 66-84 (replaced by the JTBD table), and the existing sections to PRESERVE (`## Compatibility Axes`, `## Companion Compatibility Contract`, `## Release Choreography`, runtime-line rules) plus their cross-references.
    - guides/adopter_profiles.md — the decision-table-first structure to emulate (table at top, prose below).
    - guides/support_matrix.md — the rendered `## Rebuild Decision Table` (from 124-02): the rebuild-class names and rows the guide must MIRROR (not duplicate the support matrix; use the rebuild-class column header).
    - .planning/phases/124-compatibility-semantics-adopter-truth/124-CONTEXT.md — D-10 (column set, denial-signal column, table-first, preserve sections, no-second-support-matrix), D-09 (asymmetry must be stated), D-18 (4 verbatim strings).
  </read_first>
  <action>
    Add a new lead section `## Do I need to rebuild? (start here)` at the TOP of guides/compatibility.md (before any explanatory prose), containing a JTBD table with columns `Change type | Axis touched | Rebuild class | Adopter action | Denial signal if you skip it | Guide anchor` (exact ordering/wording = Claude's discretion provided these columns + the denial-signal column + table-first ordering hold). Populate rows mirroring the canonical `rebuild_decision_table/0` rebuild classes — use the rebuild-class column header, NOT a support-status header. The `Denial signal if you skip it` column names the concrete fail-closed denial code the shell emits (`compatibility_mismatch`, `inactive_route`, `undeclared_capability`, `pack_incompatible`) for each skipped action; use `n/a` for docs-only/core-only rows. Explicitly state the asymmetry in adjacent prose: `manifest_schema`/`bridge_protocol` additive moves are `compatibility-bump only` PRECISELY BECAUSE the native floor is now `>=` (D-01/D-02), while `native_runtime` has no such row (every move is `native or companion rebuild required`). Move the existing lead prose (lines 1-7) and the old prose-only rebuild content below the new table. PRESERVE the `## Compatibility Axes`, `## Companion Compatibility Contract`, `## Release Choreography`, and runtime-line-rule sections verbatim along with their cross-references. Do NOT introduce a Target/Version/Status support-status table.
  </action>
  <acceptance_criteria>
    - The first `##` content section is `## Do I need to rebuild? (start here)` and it contains a markdown table; the table appears BEFORE the line `Crosswake keeps runtime ownership` (the prose ordering sentinel from D-11) — verify byte offset of the table header < offset of that prose line.
    - Table columns include a denial-signal column naming at least `compatibility_mismatch` and `pack_incompatible` somewhere in the table region.
    - All three axes (`manifest_schema`, `bridge_protocol`, `native_runtime`) named in the table region.
    - All 4 verbatim rebuild-class strings present in the file.
    - Prose states the native_runtime asymmetry (search for both `native_runtime` and `native or companion rebuild required` co-located).
    - Preserved sections still present: `grep -c '## Compatibility Axes\|## Companion Compatibility Contract\|## Release Choreography' guides/compatibility.md` ≥ 3.
    - NO support-status header: `grep -F '| Target | Version | Status |' guides/compatibility.md` returns nothing.
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/jon/projects/crosswake && grep -n '## Do I need to rebuild?\|Crosswake keeps runtime ownership' guides/compatibility.md</automated>
  </verify>
  <done>guides/compatibility.md leads with the JTBD decision table (with denial-signal column), mirrors the canonical rebuild classes verbatim, states the native_runtime asymmetry, preserves the existing sections, and introduces no second support matrix.</done>
</task>

<task type="auto">
  <name>Task 2: Add compatibility_test.exs docs-contract test (ordering + renderer-mirror)</name>
  <files>test/crosswake/guides/compatibility_test.exs</files>
  <read_first>
    - test/crosswake/guides/adopter_profiles_test.exs — the FULL idiom to mirror: `File.read!`, `count_occurrences/2` (lines 81-86, copy verbatim), the locked-names + presence asserts, and the `refute guide =~ "| Target | Version | Status | Proof | Notes |"` no-second-support-matrix assert.
    - lib/crosswake/support_matrix/renderer.ex + lib/crosswake/support_matrix/support_matrix.ex — the `Renderer.render(canonical())` call used in the mirror assertion.
    - .planning/phases/124-compatibility-semantics-adopter-truth/124-PATTERNS.md — "compatibility_test.exs (new)" section listing the 5 assertions and the exact `:binary.match`/`String.contains?` ordering check.
    - .planning/phases/124-compatibility-semantics-adopter-truth/124-CONTEXT.md — D-11 (the two teeth + the additional asserts).
  </read_first>
  <action>
    Create `test/crosswake/guides/compatibility_test.exs` (module `Crosswake.Guides.CompatibilityTest`, `use ExUnit.Case, async: true`) mirroring the adopter_profiles_test idiom. Read `guides/compatibility.md` via `File.read!`. Implement these assertions: (1) TABLE-BEFORE-PROSE ORDERING — assert the JTBD table header is present and its byte position is LESS THAN the position of the prose sentinel `Crosswake keeps runtime ownership` (the criterion most likely to silently regress); (2) MIRROR-AGREES-WITH-RENDERER — compute `rendered = Crosswake.SupportMatrix.Renderer.render(Crosswake.SupportMatrix.canonical())` and for each of the 4 verbatim rebuild-class strings assert it appears in BOTH `rendered` and the guide; (3) all three axis names present in the guide; (4) the native_runtime → `native or companion rebuild required` asymmetry locked (assert the guide does NOT contain a native_runtime additive-no-rebuild claim and DOES co-locate native_runtime with the native-rebuild class); (5) `refute` any support-status table header (`| Target | Version | Status |`). Copy `count_occurrences/2` verbatim from the analog. Define the locked 4-string list once at module top and reuse.
  </action>
  <acceptance_criteria>
    - `mix test test/crosswake/guides/compatibility_test.exs` passes.
    - The test FAILS if the table is moved below the prose sentinel (ordering tooth is real, not vacuous) — confirm by reasoning the assert uses positional comparison, not mere presence.
    - The test FAILS if any rebuild-class string is renamed in support_matrix (mirror tooth couples guide to renderer).
    - The test asserts all of: table-first ordering, renderer-mirror for all 4 strings, three axes present, native_runtime asymmetry, refute support-status header.
    - `count_occurrences/2` present and used.
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/jon/projects/crosswake && mix test test/crosswake/guides/compatibility_test.exs 2>&1 | tail -5</automated>
  </verify>
  <done>compatibility_test.exs machine-guards the guide: table-first ordering and renderer-mirror are the teeth, plus axes/asymmetry/no-second-support-matrix asserts; it passes against the restructured guide and would fail on regression.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| canonical source → adopter-facing guide | guides/compatibility.md is the doc adopters read to triage upgrade cost; drift from the canonical rebuild table publishes a false claim |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-124-06 | Tampering / Repudiation | guides/compatibility.md vs canonical renderer | mitigate | compatibility_test.exs mirror-tooth asserts each rebuild-class string in the guide also appears in `Renderer.render(canonical())`; a rename or guide-drift fails the test (D-11). |
| T-124-07 | Information Disclosure (false reassurance) | table-first ordering regression | mitigate | The positional ordering assert (table before prose sentinel) catches the silent regression where prose creeps above the decision table (D-11). |
| T-124-08 | Tampering | second support matrix introduced | mitigate | `refute` support-status header preserves the adopter_profiles_test "no second support matrix" boundary (D-10). |
| T-124-SC | Tampering | dependency surface | accept | No package-manager installs (markdown + ExUnit only). No legitimacy checkpoint required. |
</threat_model>

<verification>
- `mix test test/crosswake/guides/compatibility_test.exs` passes.
- `guides/compatibility.md` leads with the JTBD table; preserved sections intact; no second support matrix.
- Re-run with 124-02's renderer to confirm the mirror tooth couples guide ↔ canonical source.
</verification>

<success_criteria>
COMPAT-03 satisfied: `guides/compatibility.md` leads with a decision table ("this change type → this rebuild class → this action") before any explanatory prose, including the differentiating Denial signal column; the guide is machine-tested via a docs-contract ExUnit test whose teeth are table-first ordering and renderer-mirror agreement.
</success_criteria>

<output>
Create `.planning/phases/124-compatibility-semantics-adopter-truth/124-03-SUMMARY.md` when done. Record the exact prose sentinel used for the ordering assert and the final table column wording.
</output>
