---
phase: 124-compatibility-semantics-adopter-truth
plan: 05
type: execute
wave: 2
depends_on: ["124-02"]
files_modified:
  - CHANGELOG.md
  - CONTRIBUTING.md
  - test/crosswake/guides/release_boundaries_test.exs
autonomous: true
requirements: [COMPAT-05]
must_haves:
  truths:
    - "Each non-historical ## [x.y.z] release carries exactly one ### Upgrade Impact subsection, FIRST under the heading"
    - "Upgrade Impact labels use the 4 canonical change-class strings VERBATIM (no minted tokens)"
    - "A mixed release headlines the worst-case (highest-impact) class and enumerates lower-impact exceptions"
    - "release_boundaries_test enforces structure + vocabulary/legend parity against support_matrix.md"
    - "CONTRIBUTING.md documents the human intent-gate for choosing the correct class"
  artifacts:
    - path: "CHANGELOG.md"
      provides: "### Upgrade Impact subsections on non-historical releases"
      contains: "### Upgrade Impact"
    - path: "CONTRIBUTING.md"
      provides: "Human intent-gate convention for upgrade-impact labels"
      contains: "Upgrade Impact"
    - path: "test/crosswake/guides/release_boundaries_test.exs"
      provides: "Structural + vocabulary-parity assertions for upgrade-impact labels"
      contains: "Upgrade Impact"
  key_links:
    - from: "test/crosswake/guides/release_boundaries_test.exs"
      to: "guides/support_matrix.md"
      via: "vocabulary parity — any impact label must still exist verbatim in the Change Classes table"
      pattern: "support_matrix.md"
    - from: "CHANGELOG.md"
      to: "guides/support_matrix.md"
      via: "labels drawn from the same 4-string change-class vocabulary"
      pattern: "native or companion rebuild required"
  prohibitions:
    - "MUST NOT mint short/synonym tokens — reuse the 4 verbatim change-class strings (D-18)"
    - "MUST NOT add a conventional-commit BREAKING-CHANGE auto-derivation pipeline — skip-changelog stays true, hand-authored (D-16)"
    - "MUST NOT assert 'this entry touches the contract but lacks a label' — unprovable from prose, guaranteed to cry wolf (D-19)"
    - "MUST NOT cross runtime_line/rebuild_policy.ex atoms (:ota_safe, {:rebuild_required,_}) into the CHANGELOG (D-18)"
    - "MUST NOT retro-label historical entries as required — historical exemption via historical_changelog_line?/1 (D-19, retro optional)"
prohibitions_are_blocking: true
---

<objective>
Add an `### Upgrade Impact` subsection — first under each non-historical `## [version]` heading — that headlines the release's highest-impact change class (worst-case-wins) using the 4 verbatim change-class strings, enumerating lower-impact exceptions when mixed; document the human intent-gate in `CONTRIBUTING.md`; and extend `release_boundaries_test.exs` with two honest checks (structural presence + vocabulary/legend parity). This is COMPAT-05.

Purpose: Adopters should triage release notes without reading the full diff. The label is Keep-a-Changelog-native (per-release category), hand-authored (release-please runs `skip-changelog: true`), and reuses the SINGLE canonical vocabulary so it can't desync from the support matrix. Enforcement asserts only what's mechanically decidable; the intent gate (picking the right class) is a documented human review gate, not a cry-wolf detector.

Output: `### Upgrade Impact` blocks in CHANGELOG.md, a CONTRIBUTING.md intent-gate convention, and two new test blocks in release_boundaries_test.exs.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/124-compatibility-semantics-adopter-truth/124-CONTEXT.md
@.planning/phases/124-compatibility-semantics-adopter-truth/124-PATTERNS.md
@.planning/phases/124-compatibility-semantics-adopter-truth/124-02-SUMMARY.md
</context>

<artifacts_this_phase_produces>
NEW symbols/sections created here (drift pass: treat as newly-minted):
- The `### Upgrade Impact` subsection convention in CHANGELOG.md (one per non-historical release)
- `CONTRIBUTING.md` (new file OR a new section in an existing contributor doc — Claude's discretion) documenting the human intent-gate
- Two new `test` blocks in release_boundaries_test.exs (structural + vocabulary/legend parity)
Consumes (does NOT create): the locked 4-string list (already in release_boundaries_test ~lines 29-32 and support_matrix change_class_entries/0), `historical_changelog_line?/1` (~lines 456-461, reused not duplicated).
</artifacts_this_phase_produces>

<context>
@CHANGELOG.md
@test/crosswake/guides/release_boundaries_test.exs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add ### Upgrade Impact subsections to CHANGELOG.md + CONTRIBUTING intent gate</name>
  <files>CHANGELOG.md, CONTRIBUTING.md</files>
  <read_first>
    - CHANGELOG.md — the existing `## [version]` / `### Added` / `### Notes` structure; identify which entries are non-historical (the current/unreleased + recent contract-touching entries) vs historical (`[0.1.2]`/`[0.1.0]` — retro-labeling optional).
    - test/crosswake/guides/release_boundaries_test.exs — the locked 4-string list (~lines 29-32) and `historical_changelog_line?/1` (~456-461) so the CHANGELOG you author matches what the test will accept; confirm which versions the historical exemption covers.
    - guides/support_matrix.md — the Change Classes table where the 4 strings must still exist verbatim (legend parity target).
    - .planning/phases/124-compatibility-semantics-adopter-truth/124-CONTEXT.md — D-16 (subsection first under the heading, hand-authored), D-17 (worst-case headline + per-bullet exceptions when mixed), D-18 (4 verbatim strings; keep rebuild_policy atoms out), D-19 (historical exemption; the human intent gate lives in CONTRIBUTING.md).
  </read_first>
  <action>
    For each non-historical `## [x.y.z]` heading in CHANGELOG.md, add an `### Upgrade Impact` subsection as the FIRST subsection (before `### Added`/`### Notes`). The subsection headlines the release's HIGHEST-impact change class using one of the 4 verbatim strings (`docs-only`, `core-only/no native rebuild`, `compatibility-bump only`, `native or companion rebuild required`) — fail-safe toward "rebuild". When a release bundles multiple classes, headline the worst case and then enumerate which specific bullets are actually lower-impact (e.g. a release mixing core-only + rebuild MUST headline `native or companion rebuild required` and list the core-only exceptions). Use the verbatim string; do NOT mint short tokens; do NOT pull the runtime_line/rebuild_policy.ex classifier atoms (the OTA-safe / rebuild-required atom pair — see D-18) into the CHANGELOG. Retro-labeling `[0.1.2]`/`[0.1.0]` is OPTIONAL (historical exemption) — skip unless trivial. Then create/extend `CONTRIBUTING.md` (new file or a section in an existing contributor doc — discretion) documenting the human intent-gate: how a contributor chooses the correct change class for a release, the worst-case-wins rule, the requirement that the label is one of the 4 verbatim strings, and that this is a human review gate (not auto-derived — `skip-changelog: true`). Link CONTRIBUTING.md to `guides/compatibility.md` and the support matrix Change Classes table.
  </action>
  <acceptance_criteria>
    - Every non-historical `## [x.y.z]` heading is immediately followed (first subsection) by exactly one `### Upgrade Impact` block: verify by inspecting each non-historical release region.
    - Every `### Upgrade Impact` label is one of the 4 verbatim strings: `grep -A2 '### Upgrade Impact' CHANGELOG.md` shows only canonical strings.
    - At least one release demonstrates the worst-case-wins rule (if any non-historical release is mixed, it headlines the highest class and lists exceptions).
    - No minted synonym tokens and no rebuild_policy atoms in the Upgrade Impact regions: `grep -i ':ota_safe\|rebuild_required\|native-rebuild-required\|compat-bump' CHANGELOG.md` returns nothing in the impact blocks (the hyphen-token forms are forbidden synonyms).
    - `CONTRIBUTING.md` exists and contains an "Upgrade Impact" intent-gate section naming the 4 verbatim strings, the worst-case-wins rule, and the human-review (not auto-derived) note, with a link to guides/compatibility.md.
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/jon/projects/crosswake && grep -c '### Upgrade Impact' CHANGELOG.md; grep -c 'Upgrade Impact' CONTRIBUTING.md</automated>
  </verify>
  <done>Non-historical CHANGELOG releases carry a first ### Upgrade Impact subsection using verbatim change-class strings with worst-case-wins headlining; CONTRIBUTING.md documents the human intent-gate; no minted tokens, no rebuild_policy atoms.</done>
</task>

<task type="auto">
  <name>Task 2: Extend release_boundaries_test.exs with structural + vocabulary-parity asserts</name>
  <files>test/crosswake/guides/release_boundaries_test.exs</files>
  <read_first>
    - test/crosswake/guides/release_boundaries_test.exs — the locked 4-string list (~lines 29-32), the CHANGELOG helpers (`stale_package_versions/1`, `historical_changelog_line?/1` ~456-461), and the existing test idiom to extend.
    - guides/support_matrix.md — the Change Classes table (legend-parity target: the 4 strings must still exist verbatim there).
    - .planning/phases/124-compatibility-semantics-adopter-truth/124-PATTERNS.md — "release_boundaries_test.exs — extend with D-19 assertions" section (the two test blocks + reuse of historical_changelog_line?/1).
    - .planning/phases/124-compatibility-semantics-adopter-truth/124-CONTEXT.md — D-19 (the two honest checks; the explicit refusal to detect "touches contract but lacks label").
  </read_first>
  <action>
    Add two new `test` blocks to release_boundaries_test.exs. TEST 1 (structural): every non-historical `## [x.y.z]` heading in CHANGELOG.md has exactly one `### Upgrade Impact` block; REUSE the existing `historical_changelog_line?/1` helper for the exemption (do not duplicate it) so old entries are not required to carry a label. TEST 2 (vocabulary/legend parity): any `### Upgrade Impact` label present in CHANGELOG.md is drawn from the locked 4-string set AND each such string still exists verbatim in `guides/support_matrix.md`'s Change Classes table — share the SAME locked 4-string list already used at lines ~29-32 so a rename breaks the guide test and the changelog test together. Do NOT add any assertion that tries to detect "this entry touches the contract but is missing a label" — it is unprovable from prose and will cry wolf (D-19 explicitly forbids it).
  </action>
  <acceptance_criteria>
    - Two new test blocks exist; both pass: `mix test test/crosswake/guides/release_boundaries_test.exs`.
    - Structural test uses `historical_changelog_line?/1` (the same helper, not a copy) for the historical exemption.
    - Vocabulary test references the SAME locked 4-string list as the existing assertions (single source) and checks presence in `guides/support_matrix.md`.
    - There is NO assertion of the form "entry touches contract but lacks label" (verify by reading the new tests — only structural presence + vocabulary parity).
    - A synthetic regression (a minted token like `native-rebuild-required` in a fake Upgrade Impact line) would FAIL the vocabulary test — confirm the assertion compares against the locked set, not a loose match.
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/jon/projects/crosswake && mix test test/crosswake/guides/release_boundaries_test.exs 2>&1 | tail -5</automated>
  </verify>
  <done>release_boundaries_test.exs gains a structural check (one Upgrade Impact per non-historical release, historical-exempt) and a vocabulary/legend-parity check (labels ∈ locked 4-string set ∩ support_matrix.md), with no cry-wolf intent detection; both pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| hand-authored CHANGELOG → adopter triage | The upgrade-impact label drives whether an adopter rebuilds; a mislabeled or under-reported release misleads them |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-124-12 | Information Disclosure (under-reporting) | mixed-release labeling | mitigate | Worst-case-wins rule (D-17): a mixed release headlines the highest-impact class and enumerates exceptions, never under-reporting toward "no rebuild". Documented in CONTRIBUTING.md and acceptance-checked. |
| T-124-13 | Tampering (vocabulary desync) | CHANGELOG labels vs support_matrix | mitigate | Vocabulary/legend-parity test couples CHANGELOG labels to the locked 4-string set AND support_matrix.md; a rename breaks guide + changelog tests together (D-19). |
| T-124-14 | Repudiation (false positives) | intent detection | accept | D-19 deliberately does NOT mechanically assert "touches contract but lacks label" (unprovable from prose, cries wolf). The intent gate is a documented human review gate in CONTRIBUTING.md — accepted residual, mitigated by human review. |
| T-124-SC | Tampering | dependency surface | accept | No package-manager installs (markdown + ExUnit only). No legitimacy checkpoint required. |
</threat_model>

<verification>
- `mix test test/crosswake/guides/release_boundaries_test.exs` passes (both new blocks + existing).
- CHANGELOG non-historical releases each carry one `### Upgrade Impact` with a verbatim label.
- CONTRIBUTING.md documents the worst-case-wins human intent gate.
- A minted-token regression fails the vocabulary test.
</verification>

<success_criteria>
COMPAT-05 satisfied: CHANGELOG entries that touch the bridge/runtime contract carry an upgrade-impact label drawn from the canonical 4-string vocabulary, headlining the worst case for mixed releases, so adopters triage release notes without reading the diff; structure + vocabulary are machine-guarded; the class-choice intent gate is a documented human review step.
</success_criteria>

<output>
Create `.planning/phases/124-compatibility-semantics-adopter-truth/124-05-SUMMARY.md` when done. Record which CHANGELOG releases were labeled, whether historical entries were retro-labeled, and whether CONTRIBUTING.md is new or an extended section.
</output>