---
phase: 121-canonical-contract-source
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/crosswake/manifest/types.ex
  - lib/crosswake/shell/fixtures.ex
  - test/crosswake/compatibility/compatibility_test.exs
  - test/crosswake/doctor/doctor_test.exs
  - test/mix/tasks/crosswake_doctor_test.exs
autonomous: true
requirements: [CANON-01, CANON-02, CANON-04]
must_haves:
  truths:
    - "Crosswake.Manifest.Types bridge-protocol axis resolves to Crosswake.Bridge.Contract.version() (1.1.0) at compile time — no independent literal (D-01, D-04)"
    - "Crosswake.Shell.Fixtures.activation_request/1 derives bridge_protocol_version from Crosswake.Bridge.Contract.version() (D-05)"
    - "manifest_schema_version and native_runtime_version axes stay 1.0.0 — only the bridge axis moves (D-03)"
    - "Doctor/release-policy surfaces report bridge_protocol_version == 1.1.0, not 1.0.0 (CANON-04)"
    - "Full ExUnit suite is green after the source change + test-drift updates land together (no behavioral regression for 0.1.x adopters)"
  artifacts:
    - path: "lib/crosswake/manifest/types.ex"
      provides: "@bridge_protocol_version derives from Crosswake.Bridge.Contract.version()"
      contains: "Crosswake.Bridge.Contract.version()"
    - path: "lib/crosswake/shell/fixtures.ex"
      provides: "activation_request/1 bridge_protocol_version derived from canonical constant"
      contains: "Crosswake.Bridge.Contract.version()"
  key_links:
    - from: "lib/crosswake/manifest/types.ex"
      to: "lib/crosswake/bridge/contract.ex"
      via: "compile-time fully-qualified call to Crosswake.Bridge.Contract.version() in @bridge_protocol_version module attribute (NO alias — cycle risk)"
      pattern: "@bridge_protocol_version Crosswake\\.Bridge\\.Contract\\.version\\(\\)"
    - from: "lib/crosswake/shell/fixtures.ex"
      to: "lib/crosswake/bridge/contract.ex"
      via: "fully-qualified call to Crosswake.Bridge.Contract.version() in activation_request/1"
      pattern: "bridge_protocol_version: Crosswake\\.Bridge\\.Contract\\.version\\(\\)"
  prohibitions:
    - "lib/crosswake/manifest/types.ex MUST NOT add `alias Crosswake.Bridge.Contract` (contract.ex already aliases Manifest.Types — adding the reverse alias creates a compile-order cycle). Use the fully-qualified call instead (CANON-01)."
    - "After this plan, no hand-maintained `bridge_protocol_version` literal carrying a version value may remain in lib/ source — the only literal version string for the bridge axis lives in contract.ex @version (CANON-02)."
    - "manifest_schema_version and native_runtime_version MUST remain 1.0.0 — do NOT bump them (D-03)."
---

<objective>
Collapse the bridge-protocol-version drift to one Elixir authority. Replace the stale hand-maintained `@bridge_protocol_version "1.0.0"` literal in `Crosswake.Manifest.Types` and the hardcoded `"1.0.0"` in `Crosswake.Shell.Fixtures` with compile-time references to `Crosswake.Bridge.Contract.version()` (the already-authoritative `1.1.0` constant), then repair every test assertion and input fixture that the value change breaks — all in one wave so the suite returns green.

Purpose: CANON-01 (single canonical source), CANON-02 (one named source per axis, no second copy), CANON-04 (resolve 1.1.0-vs-1.0.0 to the one correct current value without breaking 0.1.x adopters). Per locked decisions D-01, D-03, D-04, D-05.

Output: Two source files reference the canonical constant; the three version axes each have exactly one home (bridge → derived from `Contract.version()`; schema + native-runtime → their own literals in `Manifest.Types`); the full ExUnit suite is green.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/121-canonical-contract-source/121-CONTEXT.md
@.planning/phases/121-canonical-contract-source/121-RESEARCH.md
@.planning/phases/121-canonical-contract-source/121-VALIDATION.md

@lib/crosswake/bridge/contract.ex
@lib/crosswake/compatibility/compatibility.ex
</context>

<artifacts_this_phase_produces>
This plan creates NO new symbols. It modifies existing module attributes and function bodies only:
- `Crosswake.Manifest.Types` `@bridge_protocol_version` module attribute — value changes from a literal to a compile-time call (same attribute name, same `new_compatibility/1` consumer).
- `Crosswake.Shell.Fixtures.activation_request/1` — internal `bridge_protocol_version` value changes from a literal to a call.
- No new functions, modules, files, or constants. `@manifest_schema_version` and `@native_runtime_version` keep their literal homes unchanged.
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Single-source the bridge-protocol axis in Manifest.Types and Shell.Fixtures</name>
  <files>lib/crosswake/manifest/types.ex, lib/crosswake/shell/fixtures.ex</files>
  <read_first>
    - lib/crosswake/bridge/contract.ex lines 1-20 — confirm `@version "1.1.0"` (line 10), `version/0`, and `alias Crosswake.Manifest.Types` (line 7). This alias direction is WHY the reverse alias is forbidden.
    - lib/crosswake/manifest/types.ex lines 645-665 — the three `@..._version` attributes (651-653) and `new_root/1` / consumers. Confirm there is NO `alias Crosswake.Bridge.Contract` at the top of this module before editing.
    - lib/crosswake/shell/fixtures.ex lines 1-15 (existing aliases — no Bridge.Contract) and lines 76-89 (`activation_request/1`).
  </read_first>
  <behavior>
    - After change: `Crosswake.Manifest.Types.new_compatibility().bridge_protocol_version` equals `Crosswake.Bridge.Contract.version()` (== "1.1.0").
    - `Crosswake.Manifest.Types.new_compatibility().manifest_schema_version` stays "1.0.0".
    - `Crosswake.Manifest.Types.new_compatibility().native_runtime_version` stays "1.0.0".
    - The project compiles with no circular-dependency CompileError.
  </behavior>
  <action>
    In `lib/crosswake/manifest/types.ex`, replace the `@bridge_protocol_version "1.0.0"` module attribute (line 652) with the fully-qualified compile-time call `Crosswake.Bridge.Contract.version()` per D-04. Leave `@manifest_schema_version "1.0.0"` (651) and `@native_runtime_version "1.0.0"` (653) exactly as they are per D-03 — only the bridge axis moves. Do NOT add `alias Crosswake.Bridge.Contract` to this module: `contract.ex:7` already aliases `Manifest.Types`, so the reverse alias creates a compile-order cycle. The fully-qualified call resolves at compile time without an alias (no cycle, because contract.ex uses Types only in function bodies, not in module attributes).

    In `lib/crosswake/shell/fixtures.ex`, in `activation_request/1` (line 82), replace `bridge_protocol_version: "1.0.0"` with `bridge_protocol_version: Crosswake.Bridge.Contract.version()` per D-05. Leave the adjacent `native_runtime_version: "1.0.0"` (line 83) unchanged per D-03. Use the fully-qualified call (no new alias needed).

    Run `mix compile --warnings-as-errors` and confirm there is NO circular-dependency CompileError between `Crosswake.Manifest.Types` and `Crosswake.Bridge.Contract`. If a cycle error appears, STOP — the fully-qualified-call approach is the verified fix; an alias was mistakenly added.
  </action>
  <verify>
    <automated>cd /Users/jon/projects/crosswake && mix compile --warnings-as-errors && grep -n '@bridge_protocol_version Crosswake.Bridge.Contract.version()' lib/crosswake/manifest/types.ex && grep -n 'bridge_protocol_version: Crosswake.Bridge.Contract.version()' lib/crosswake/shell/fixtures.ex</automated>
  </verify>
  <acceptance_criteria>
    - `mix compile --warnings-as-errors` exits 0 (no circular-dependency CompileError).
    - `grep -n '@bridge_protocol_version' lib/crosswake/manifest/types.ex` shows the value is `Crosswake.Bridge.Contract.version()`, NOT a string literal.
    - `grep -c 'alias Crosswake.Bridge.Contract' lib/crosswake/manifest/types.ex` returns 0 (no forbidden alias added).
    - `grep -n '@manifest_schema_version "1.0.0"' lib/crosswake/manifest/types.ex` and `grep -n '@native_runtime_version "1.0.0"' lib/crosswake/manifest/types.ex` both still match (other axes unchanged).
    - `grep -n 'bridge_protocol_version: Crosswake.Bridge.Contract.version()' lib/crosswake/shell/fixtures.ex` matches; `native_runtime_version: "1.0.0"` still present in `activation_request/1`.
  </acceptance_criteria>
  <done>The bridge-protocol axis is single-sourced from `Contract.version()` at compile time in both files; schema and native-runtime axes remain literal 1.0.0; project compiles with no cycle.</done>
</task>

<task type="auto">
  <name>Task 2: Repair value-driven test drift (assertions and compatibility input fixtures)</name>
  <files>test/crosswake/compatibility/compatibility_test.exs, test/crosswake/doctor/doctor_test.exs, test/mix/tasks/crosswake_doctor_test.exs</files>
  <read_first>
    - lib/crosswake/compatibility/compatibility.ex lines 300-320 (bridge-axis `compatible_version?` check — line 308) and lines 616-627 (`compatible_version?/2` is `available >= required`, NOT exact-match). This is the behavioral key: after Task 1, the manifest's required bridge version becomes 1.1.0, so an input Target with `bridge_protocol_version: "1.0.0"` now evaluates `1.0.0 >= 1.1.0` → false → DENY. Tests asserting a PASS must raise their Target's available value to 1.1.0; tests asserting a DENY may keep 1.0.0.
    - test/crosswake/compatibility/compatibility_test.exs — every `bridge_protocol_version: "1.0.0"` occurrence (lines 22, 47, 70, 119, 144, 286, 355, 373, 453) WITH its surrounding `assert` to classify each as pass-expecting (bump) vs deny-expecting (keep). Note line 22 pairs with `native_runtime_version: "0.9.0"` (a deliberate-mismatch case) and line 453 is `bridge_manifest_fixture`'s explicit `Types.new_compatibility(bridge_protocol_version: "1.0.0")` override.
    - test/crosswake/doctor/doctor_test.exs lines 93-98 and 193-196 — release-policy assertions including the human-format `"bridge_protocol_version=1.0.0"` string (line 195).
    - test/mix/tasks/crosswake_doctor_test.exs lines 107-110 and 142-145 — both human (`=1.0.0`) and decoded JSON (`== "1.0.0"`) assertions.
  </read_first>
  <action>
    Run the suite first to see exactly which assertions fail after Task 1, then repair them by class. Do NOT blanket-replace every `1.0.0` — classify each occurrence:

    1. SYSTEM-REPORT assertions (what the doctor/release-policy actually emits) MUST become 1.1.0 because the source now reports 1.1.0:
       - `test/crosswake/doctor/doctor_test.exs:96` and `:214`: `bridge_protocol_version == "1.0.0"` → `== Crosswake.Bridge.Contract.version()` (preferred — keeps the assertion derived) or `== "1.1.0"`.
       - `test/crosswake/doctor/doctor_test.exs:195`: human string `=~ "bridge_protocol_version=1.0.0"` → `=~ "bridge_protocol_version=#{Crosswake.Bridge.Contract.version()}"` or `=~ "bridge_protocol_version=1.1.0"`.
       - `test/mix/tasks/crosswake_doctor_test.exs:109` (human `=1.0.0`) and `:144` (decoded `== "1.0.0"`): same treatment.
       Leave the sibling `manifest_schema_version=1.0.0` and `native_runtime_version=1.0.0` assertions in those same blocks UNCHANGED (those axes did not move).

    2. COMPATIBILITY INPUT fixtures in `compatibility_test.exs` — for each `%Target{... bridge_protocol_version: "1.0.0" ...}` and the `bridge_manifest_fixture` override: read the test's assertion. If the test asserts a compatible/allowed/pass outcome, raise the Target's `bridge_protocol_version` to `Crosswake.Bridge.Contract.version()` so the `>=` floor still passes against the now-1.1.0 manifest requirement. If the test deliberately asserts a denial/mismatch finding on the bridge axis, keep 1.0.0 (it now legitimately mismatches — confirm the asserted denial reason still matches). Prefer `Crosswake.Bridge.Contract.version()` over a bare `"1.1.0"` literal in pass cases so the fixtures stay drift-proof. Do NOT change `manifest_schema_version` or `native_runtime_version` values in these Targets.

    Re-run the full suite until green. The classification is behavioral, not mechanical — let the failing/passing assertions guide each occurrence.
  </action>
  <verify>
    <automated>cd /Users/jon/projects/crosswake && mix test test/crosswake/compatibility/compatibility_test.exs test/crosswake/doctor/doctor_test.exs test/mix/tasks/crosswake_doctor_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - `mix test test/crosswake/compatibility/compatibility_test.exs test/crosswake/doctor/doctor_test.exs test/mix/tasks/crosswake_doctor_test.exs` passes (0 failures).
    - `grep -rn 'bridge_protocol_version.*"1.0.0"' test/crosswake/doctor/doctor_test.exs test/mix/tasks/crosswake_doctor_test.exs` returns no system-report assertion still claiming the bridge axis is 1.0.0 (manifest_schema/native_runtime 1.0.0 assertions may remain).
    - Any `compatibility_test.exs` occurrence still at `bridge_protocol_version: "1.0.0"` is provably inside a test asserting a denial/mismatch (documented in the SUMMARY), not a pass case.
  </acceptance_criteria>
  <done>All bridge-axis system-report assertions read 1.1.0 (via the canonical constant where possible); all pass-expecting compatibility input fixtures clear the new 1.1.0 `>=` floor; any retained 1.0.0 fixture is a deliberate deny case; the three targeted test files are green.</done>
</task>

<task type="auto">
  <name>Task 3: Full-suite green gate + no-stray-literal invariant (CANON-02)</name>
  <files>test/crosswake/compatibility/compatibility_test.exs, test/crosswake/doctor/doctor_test.exs, test/mix/tasks/crosswake_doctor_test.exs</files>
  <read_first>
    - Output of `mix test` (full suite) — catch any drift in test files NOT enumerated in research (e.g. integration or proof-adjacent tests that build a manifest and assert on bridge_protocol_version).
    - ROADMAP.md Phase 121 Success Criteria 1 and 2 (canonical source + single-value grep invariant).
  </read_first>
  <action>
    Run the FULL ExUnit suite (`mix test`). Fix any remaining failures using the same classification rule from Task 2 (system-report → 1.1.0; deny-input → keep). Research enumerated the known drift files but the suite is the authority — repair whatever else turns red, applying the same axis discipline (never touch manifest_schema/native_runtime values).

    Then verify the CANON-02 single-source invariant: `grep -rn '"bridge_protocol_version"' lib/` for the bridge axis must resolve to exactly one declared version value (in `contract.ex` `@version`), with no hand-maintained literal version string for the bridge axis remaining in `lib/` source (`Manifest.Types` and `Shell.Fixtures` now carry calls, not literals). The string `"bridge_protocol_version"` as a JSON/struct KEY is fine — only a hand-maintained version VALUE literal for that axis is forbidden.
  </action>
  <verify>
    <automated>cd /Users/jon/projects/crosswake && mix test && test -z "$(grep -rn 'bridge_protocol_version[: ]*"1\.0\.0"' lib/crosswake/manifest/types.ex lib/crosswake/shell/fixtures.ex)"</automated>
  </verify>
  <acceptance_criteria>
    - `mix test` (full suite) passes with 0 failures.
    - `grep -rn 'bridge_protocol_version[: ]*"1.0.0"' lib/crosswake/manifest/types.ex lib/crosswake/shell/fixtures.ex` returns empty (no stale bridge literal in the two single-sourced files).
    - `grep -rn '@version' lib/crosswake/bridge/contract.ex` shows the sole declared bridge version constant (`"1.1.0"`).
  </acceptance_criteria>
  <done>Full suite is green; the bridge-protocol axis has exactly one declared version value (contract.ex), satisfying CANON-02; the value is 1.1.0 everywhere it is reported, satisfying CANON-04.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| native shell → manifest | Native reads `bridge_protocol_version` from the manifest at runtime; aligning the manifest value moves both sides of the exact-match together (D-02) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-121-01 | Tampering | `Manifest.Types` `@bridge_protocol_version` | mitigate | Derive from `Contract.version()` at compile time so the value cannot drift from the authority (CANON-01/02) |
| T-121-02 | Denial of Service | bridge exact-match (`BridgeChannel.swift:182`) | accept | Live latent denial (manifest 1.0.0 vs Elixir 1.1.0) is RESOLVED by this change aligning both sides to 1.1.0 (D-02); native `>=` floor is Phase 124, not required for safety here |
| T-121-SC | Tampering | npm/pip/cargo installs | accept | No package-manager installs in this plan — pure Elixir source + test edits, no new dependencies |
</threat_model>

<verification>
- `mix compile --warnings-as-errors` — no circular dependency.
- `mix test` — full suite green.
- `grep` invariants for the single-source bridge value (CANON-01/02) and the 1.1.0 resolution (CANON-04).
</verification>

<success_criteria>
- CANON-01: `Contract.version()` is the sole declared bridge constant; `Manifest.Types` and `Shell.Fixtures` derive from it at compile time.
- CANON-02: bridge axis has exactly one named source; schema + native-runtime axes keep their own literal homes; no second hand-maintained bridge literal.
- CANON-04: 1.1.0-vs-1.0.0 resolved to 1.1.0; full suite green proves 0.1.x adopter-visible behavior is intact.
</success_criteria>

<output>
Create `.planning/phases/121-canonical-contract-source/121-01-SUMMARY.md` when done. In it, list which `compatibility_test.exs` occurrences were bumped to 1.1.0 vs deliberately kept at 1.0.0 (with the asserted denial reason for each kept case), and any drift in test files beyond the research-enumerated set.
</output>
