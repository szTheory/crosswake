---
phase: 121-canonical-contract-source
plan: 02
type: execute
wave: 2
depends_on: ["121-01"]
files_modified:
  - lib/mix/tasks/crosswake.contract.gen.ex
  - examples/ios_shell_host/Fixtures/route_activation.json
  - examples/android_shell_host/app/src/main/assets/route_activation.json
  - test/fixtures/bridge_contract_vectors.json
  - docs/_contract_snippet.md
autonomous: true
requirements: [CANON-02, CANON-03]
must_haves:
  truths:
    - "mix crosswake.contract.gen exists, is hermetic and network-free, and reads Crosswake.Bridge.Contract.version() as the authority (D-06)"
    - "Running the task emits the iOS + Android route_activation.json with bridge_protocol_version=1.1.0 and native_runtime_version unchanged at 1.0.0 (D-03)"
    - "Running the task emits test/fixtures/bridge_contract_vectors.json with the canonical version axes and a seed vector set (D-07)"
    - "Running the task emits a docs snippet carrying the canonical version (D-06)"
    - "Each generated file carries a DO-NOT-EDIT / generated-by marker (D-06)"
    - "Re-running the task is idempotent: mix crosswake.contract.gen && git diff --exit-code is clean (CANON-03)"
  artifacts:
    - path: "lib/mix/tasks/crosswake.contract.gen.ex"
      provides: "Mix.Tasks.Crosswake.Contract.Gen task — mix crosswake.contract.gen"
      exports: ["run/1"]
      min_lines: 40
    - path: "examples/ios_shell_host/Fixtures/route_activation.json"
      provides: "iOS example-host activation fixture, bridge_protocol_version regenerated to 1.1.0"
      contains: "1.1.0"
    - path: "examples/android_shell_host/app/src/main/assets/route_activation.json"
      provides: "Android example-host activation fixture, bridge_protocol_version regenerated to 1.1.0"
      contains: "1.1.0"
    - path: "test/fixtures/bridge_contract_vectors.json"
      provides: "Canonical bridge contract conformance vectors (seed set); Phase 123 consumes"
      contains: "bridge_protocol_version"
    - path: "docs/_contract_snippet.md"
      provides: "Generated docs snippet carrying the canonical bridge protocol version"
      contains: "1.1.0"
  key_links:
    - from: "lib/mix/tasks/crosswake.contract.gen.ex"
      to: "lib/crosswake/bridge/contract.ex"
      via: "reads Crosswake.Bridge.Contract.version() at task runtime after Mix.Task.run(\"app.start\")"
      pattern: "Crosswake\\.Bridge\\.Contract\\.version\\(\\)"
    - from: "lib/mix/tasks/crosswake.contract.gen.ex"
      to: "examples/ios_shell_host/Fixtures/route_activation.json"
      via: "write_if_changed of the example-host activation fixture (bridge axis only)"
      pattern: "route_activation\\.json"
  prohibitions:
    - "The gen task MUST NOT use Shell.Fixtures.export/1 to produce route_activation.json — that emits a different 'dashboard' route fixture. Reproduce the existing 'selective-native-claim-capture' / camera-pack content, changing ONLY bridge_protocol_version (CANON-03)."
    - "The gen task MUST NOT write to examples/ios_shell_host/build/ or examples/android_shell_host/app/build/ — those are build artifacts (D-06)."
    - "The gen task MUST NOT bump native_runtime_version in route_activation.json — it stays 1.0.0 (D-03)."
    - "The gen task MUST NOT touch crosswake_manifest.json, denial.json, or other Fixtures.export targets — only the two route_activation.json files, the vectors file, and the docs snippet (CANON-03)."
---

<objective>
Ship `mix crosswake.contract.gen`: a hermetic, network-free Mix task that reads `Crosswake.Bridge.Contract.version()` (the canonical authority) and regenerates exactly four non-Elixir surfaces — the iOS and Android example-host `route_activation.json` fixtures (bridge axis → 1.1.0), the new `test/fixtures/bridge_contract_vectors.json` conformance-vector seed, and a docs snippet. Each generated file carries a DO-NOT-EDIT marker. Then run the task once and commit the emitted outputs.

Purpose: CANON-03 (the gen task renders the canonical contract into every derived non-Elixir surface) and CANON-02 (those surfaces now derive from the one authority rather than carrying hand-maintained literals). Per locked decisions D-03, D-06, D-07. The generate-and-diff CI freshness GUARD is Phase 122 — this plan produces the task + committed outputs and proves idempotency locally, but does NOT add a CI check.

Output: One new Mix task module, two regenerated JSON fixtures, one new vectors fixture, one docs snippet; `mix crosswake.contract.gen && git diff --exit-code` is clean.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/121-canonical-contract-source/121-CONTEXT.md
@.planning/phases/121-canonical-contract-source/121-RESEARCH.md
@.planning/phases/121-canonical-contract-source/121-VALIDATION.md

@lib/crosswake/bridge/contract.ex
@lib/mix/tasks/crosswake.gen.shell.ex
</context>

<artifacts_this_phase_produces>
NEW symbols/files introduced by this plan (the plan-review source-grounding pass should treat these as newly-created, not pre-existing):
- `Mix.Tasks.Crosswake.Contract.Gen` — new module at `lib/mix/tasks/crosswake.contract.gen.ex`, invoked as `mix crosswake.contract.gen`. Public `run/1`.
- Internal helpers in that module (names at executor discretion, e.g. `write_if_changed/2`, path helpers, content builders for the activation JSON / vectors / docs snippet) — mirror `crosswake.gen.shell.ex`'s `ensure_file/2` + `fetch_version!/0` shape.
- `test/fixtures/bridge_contract_vectors.json` — NEW file, emitted by the gen task. Phase 123 consumes it.
- `docs/_contract_snippet.md` — NEW generated docs snippet (exact path is executor discretion per CONTEXT.md; this is the recommended location).
- Regenerated (not new) outputs: the two `route_activation.json` example-host fixtures.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Create the crosswake.contract.gen Mix task</name>
  <files>lib/mix/tasks/crosswake.contract.gen.ex</files>
  <read_first>
    - lib/mix/tasks/crosswake.gen.shell.ex — the mirror analog. Specifically: `@shortdoc`/`@moduledoc`, `@impl Mix.Task` + `def run`, `fetch_version!/0` (lines 199-214, reads a live value), and `ensure_file/2` (lines 261-275, the idempotent write idiom). Mirror this structure.
    - lib/crosswake/bridge/contract.ex lines 1-30 — `@version "1.1.0"`, `version/0`, `@protocol "crosswake.bridge"`, and the `@commands` list (for the vectors `commands` field). Read the full `@commands` and any denial-reason list so the vectors file derives its enumerations from the source, not from hand-authored strings.
    - examples/ios_shell_host/Fixtures/route_activation.json and examples/android_shell_host/app/src/main/assets/route_activation.json — the EXACT current content (camera pack, route `selective-native-claim-capture`, correlation_id differs per platform: `ios-example-capture-1` vs `android-example-capture-1`). The task must reproduce every field, changing only `bridge_protocol_version`.
  </read_first>
  <action>
    Create `lib/mix/tasks/crosswake.contract.gen.ex` defining `Mix.Tasks.Crosswake.Contract.Gen` with `use Mix.Task`, a `@shortdoc`, and a `@moduledoc` that names the four output files and the regenerate command. Mirror `crosswake.gen.shell.ex` structure.

    `run/1`: call `Mix.Task.run("app.start")` (ensures `Crosswake.Bridge.Contract` is loaded — established Mix idiom), then read `bridge_vsn = Crosswake.Bridge.Contract.version()`. Use this single authority value for all outputs. Write four artifacts via an idempotent `write_if_changed/2` helper (mirror `ensure_file/2`: mkdir_p the dir, read existing content, write only if different so re-runs produce no git churn):

    1. `examples/ios_shell_host/Fixtures/route_activation.json` — reproduce the existing content with `bridge_protocol_version` set to `bridge_vsn` and `native_runtime_version` left at `"1.0.0"` (D-03). Preserve correlation_id `ios-example-capture-1` and ALL other fields. Add a generated-by marker (a `"_generated_by": "mix crosswake.contract.gen"` JSON key per research recommendation, since JSON has no comments).
    2. `examples/android_shell_host/app/src/main/assets/route_activation.json` — identical except correlation_id `android-example-capture-1`.
    3. `test/fixtures/bridge_contract_vectors.json` — emit the canonical vectors seed: a `_generated_by` (or `_comment` + `_regenerate`) marker, `protocol` and `commands` derived from `Crosswake.Bridge.Contract`, `bridge_protocol_version: bridge_vsn`, `manifest_schema_version: "1.0.0"`, `native_runtime_version: "1.0.0"`, a `denial_reasons` list, and a minimal `vectors` array seed (a handful of request-override → expected-deny cases per the RESEARCH §Pattern 5 schema). This is a FORMAT-defining seed; Phase 123 extends it.
    4. `docs/_contract_snippet.md` — a short generated markdown snippet carrying the canonical bridge protocol version and a DO-NOT-EDIT header comment with the regenerate command.

    Encode all JSON with `Jason.encode!(data, pretty: true)` and ensure deterministic key ordering (sort keys before encoding, or build with an ordered structure) so re-runs are byte-stable. Keep the task hermetic and network-free — no HTTP, no external process. Hardcode the four target paths; do NOT derive them from `Shell.Fixtures.export/1`. Do NOT write to any `build/` path.

    Run `mix compile --warnings-as-errors`. Do NOT run the task yet (Task 2 runs and commits outputs).
  </action>
  <verify>
    <automated>cd /Users/jon/projects/crosswake && mix compile --warnings-as-errors && mix help crosswake.contract.gen >/dev/null && grep -n 'Crosswake.Bridge.Contract.version()' lib/mix/tasks/crosswake.contract.gen.ex</automated>
  </verify>
  <acceptance_criteria>
    - `mix help crosswake.contract.gen` succeeds (task is registered).
    - `grep -c 'Crosswake.Bridge.Contract.version()' lib/mix/tasks/crosswake.contract.gen.ex` >= 1 (reads the authority, no hardcoded version literal driving output).
    - `grep -c 'Shell.Fixtures.export' lib/mix/tasks/crosswake.contract.gen.ex` returns 0 (does not reuse the wrong fixture source).
    - `grep -c 'build/' lib/mix/tasks/crosswake.contract.gen.ex` returns 0 (never targets build artifacts).
    - The module references all four target paths (two route_activation.json, bridge_contract_vectors.json, docs snippet).
  </acceptance_criteria>
  <done>`mix crosswake.contract.gen` exists, compiles clean, reads `Contract.version()` as the sole version authority, targets exactly the four intended surfaces, and never touches build artifacts or the wrong fixture source.</done>
</task>

<task type="auto">
  <name>Task 2: Run the gen task, commit outputs, prove idempotency</name>
  <files>examples/ios_shell_host/Fixtures/route_activation.json, examples/android_shell_host/app/src/main/assets/route_activation.json, test/fixtures/bridge_contract_vectors.json, docs/_contract_snippet.md</files>
  <read_first>
    - ROADMAP.md Phase 121 Success Criteria 3 (hermetic regeneration of all derived surfaces).
    - 121-VALIDATION.md §Per-Task Verification Map CANON-03 row — the exact idempotency command (`mix crosswake.contract.gen && git diff --exit-code` on the three regenerated/new files).
  </read_first>
  <action>
    Run `mix crosswake.contract.gen`. Inspect the diff: the two `route_activation.json` files should now show `bridge_protocol_version` = the canonical version (1.1.0) with `native_runtime_version` still `1.0.0` and all other fields preserved; `test/fixtures/bridge_contract_vectors.json` and `docs/_contract_snippet.md` should be newly created. Confirm no `build/` path and no unrelated fixture (crosswake_manifest.json, denial.json) was modified.

    Then prove idempotency: run `mix crosswake.contract.gen` a SECOND time and confirm `git diff --exit-code` on the four files is clean (exit 0) — the task must not rewrite byte-identical content or reorder keys on re-run. If the second run dirties the tree, fix the determinism (key ordering / trailing newline) in the gen task before proceeding.
  </action>
  <verify>
    <automated>cd /Users/jon/projects/crosswake && mix crosswake.contract.gen && mix crosswake.contract.gen && git diff --exit-code examples/ios_shell_host/Fixtures/route_activation.json examples/android_shell_host/app/src/main/assets/route_activation.json test/fixtures/bridge_contract_vectors.json docs/_contract_snippet.md</automated>
  </verify>
  <acceptance_criteria>
    - First `mix crosswake.contract.gen` run exits 0 and emits/updates the four files.
    - Second consecutive run leaves the tree clean: `git diff --exit-code` on the four files exits 0 (idempotent).
    - `grep -n '"bridge_protocol_version"' examples/ios_shell_host/Fixtures/route_activation.json examples/android_shell_host/app/src/main/assets/route_activation.json` shows `1.1.0` in both.
    - `grep -n '"native_runtime_version"' examples/ios_shell_host/Fixtures/route_activation.json examples/android_shell_host/app/src/main/assets/route_activation.json` still shows `1.0.0` in both (axis discipline, D-03).
    - `test test/fixtures/bridge_contract_vectors.json` exists and `grep -c '"bridge_protocol_version"' test/fixtures/bridge_contract_vectors.json` >= 1 showing `1.1.0`.
    - `git status` shows NO modification under any `build/` path and NO change to `crosswake_manifest.json` / `denial.json`.
  </acceptance_criteria>
  <done>The gen task has been run, the four derived surfaces are emitted with correct per-axis values, and a consecutive re-run is byte-stable (idempotent), satisfying CANON-03. Generated files carry the DO-NOT-EDIT / generated-by marker.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| gen task → generated surfaces | The task is the only writer of the non-Elixir derived surfaces; it reads the Elixir authority and renders downstream files (untrusted hand-edits are what the Phase 122 guard will later catch) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-121-03 | Tampering | generated JSON fixtures + vectors | mitigate | DO-NOT-EDIT / `_generated_by` marker on every emitted file declares them generated and names the regenerate command; idempotency proven locally (the merge-blocking generate-and-diff guard is Phase 122) |
| T-121-04 | Information Disclosure | gen task hermeticity | accept | Task is network-free and reads only the in-repo Elixir constant — no secrets, no external calls |
| T-121-SC | Tampering | npm/pip/cargo installs | accept | No package-manager installs — Jason and Mix are already in deps; no new dependencies added |
</threat_model>

<verification>
- `mix help crosswake.contract.gen` — task registered.
- `mix crosswake.contract.gen` twice + `git diff --exit-code` — hermetic and idempotent (CANON-03).
- Per-axis grep on the regenerated fixtures — bridge 1.1.0, native-runtime 1.0.0 (D-03).
- `git status` — no `build/` or unrelated-fixture churn.
</verification>

<success_criteria>
- CANON-03: `mix crosswake.contract.gen` regenerates all derived non-Elixir surfaces (two route_activation.json, vectors, docs snippet) from the canonical constant, hermetically and idempotently.
- CANON-02: those surfaces now derive from `Contract.version()` rather than carrying hand-maintained literals.
- D-07: `bridge_contract_vectors.json` exists with its format defined for Phase 123 to consume.
</success_criteria>

<output>
Create `.planning/phases/121-canonical-contract-source/121-02-SUMMARY.md` when done. Record the chosen docs-snippet path, the DO-NOT-EDIT marker convention used, and the seed vector ids in `bridge_contract_vectors.json` so Phase 123 knows the starting schema.
</output>