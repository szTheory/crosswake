---
phase: 121-canonical-contract-source
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/crosswake-shell-core-android/src/main/java/dev/crosswake/shell/core/ActivationCoordinator.kt
autonomous: true
requirements: [CANON-05]
must_haves:
  truths:
    - "ActivationCoordinator.kt line 594 no longer contains the silent ?: \"1.0.0\" native-runtime fallback (D-08)"
    - "Native reads native_runtime_version from the manifest and fails closed (throws) if the compatibility block is absent, rather than silently defaulting"
    - "ShellManifest.nativeRuntimeVersion remains non-nullable String — the fail-closed form preserves the existing type contract"
  artifacts:
    - path: "packages/crosswake-shell-core-android/src/main/java/dev/crosswake/shell/core/ActivationCoordinator.kt"
      provides: "ActivationFixtures.loadManifest reads native_runtime_version with no silent default"
  key_links:
    - from: "ActivationCoordinator.kt loadManifest"
      to: "crosswake_manifest.json compatibility block"
      via: "getString(\"native_runtime_version\") with fail-closed error on absent compatibility block instead of ?: \"1.0.0\""
      pattern: "native_runtime_version"
  prohibitions:
    - "ActivationCoordinator.kt MUST NOT contain `?: \"1.0.0\"` anywhere after this plan (CANON-05). <!-- planner-discipline-allow: ?: \"1.0.0\" -->"
    - "MUST NOT change ShellManifest.nativeRuntimeVersion to a nullable type — line 614 assigns it into a non-nullable String field; the fix must keep a non-null String (use a fail-closed error, not nullable typing)."
    - "MUST NOT touch BridgeChannel.swift / BridgeChannel.kt exact-match comparison — the `>=` floor is Phase 124, out of scope here."
---

<objective>
Remove the silent native-runtime-version fallback at `ActivationCoordinator.kt:594` so the Android shell core reads `native_runtime_version` from the manifest and fails closed when the compatibility block is absent, instead of silently assuming `"1.0.0"`.

Purpose: CANON-05, per locked decision D-08. This is an independent native-side change with zero overlap with the Elixir plans — it runs in parallel with Plan 01.

Output: One Kotlin file edited; the `?: "1.0.0"` fallback gone; native fails closed; `ShellManifest.nativeRuntimeVersion` stays a non-nullable `String`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/121-canonical-contract-source/121-CONTEXT.md
@.planning/phases/121-canonical-contract-source/121-RESEARCH.md
</context>

<artifacts_this_phase_produces>
This plan creates NO new symbols. It edits one existing assignment inside `ActivationFixtures.loadManifest` in `ActivationCoordinator.kt`. No new functions, classes, or files.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Remove the silent native-runtime fallback; fail closed</name>
  <files>packages/crosswake-shell-core-android/src/main/java/dev/crosswake/shell/core/ActivationCoordinator.kt</files>
  <read_first>
    - ActivationCoordinator.kt lines 585-635 — the full `ActivationFixtures.loadManifest` body. Line 593 reads `val compatibilityJson = root.optJSONObject("compatibility")` (can be null); line 594 is the fallback; line 614 assigns `nativeRuntimeVersion = nativeRuntimeVersion` into the `ShellManifest(...)` constructor.
    - ActivationCoordinator.kt line 98 — `val nativeRuntimeVersion: String` on the `ShellManifest` type (NON-nullable). This is why the fix must keep a non-null String: removing the fallback and leaving a nullable `String?` would fail to compile at line 614 ("null cannot be a value of a non-null type String").
  </read_first>
  <action>
    At line 594, the current code reads the native runtime version from a possibly-null compatibility block and silently defaults when absent. Replace the silent-default form so it fails closed: keep `getString("native_runtime_version")` on the compatibility block but, when the compatibility block (or the key) is absent, raise a Kotlin `error(...)` with a clear message (e.g. that `crosswake_manifest.json` is missing `native_runtime_version` in its compatibility block) rather than substituting a default version. The result must remain a non-nullable `String` so the existing `ShellManifest(nativeRuntimeVersion = ...)` assignment at line 614 still type-checks.

    Do NOT introduce a nullable `String?` for `nativeRuntimeVersion`. Do NOT alter `ShellManifest`'s type (line 98). Do NOT touch any other line, the `route_activation.json` loader below it, or the bridge exact-match comparison (Phase 124 scope).

    Note: the silent-default literal being removed is the exact string an acceptance grep negative-matches; do not re-introduce it anywhere in the file, including in comments.
  </action>
  <verify>
    <automated>cd /Users/jon/projects/crosswake && test -z "$(grep -n '?: \"1.0.0\"' packages/crosswake-shell-core-android/src/main/java/dev/crosswake/shell/core/ActivationCoordinator.kt)" && grep -n 'native_runtime_version' packages/crosswake-shell-core-android/src/main/java/dev/crosswake/shell/core/ActivationCoordinator.kt</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n '?: "1.0.0"' packages/crosswake-shell-core-android/src/main/java/dev/crosswake/shell/core/ActivationCoordinator.kt` returns EMPTY (CANON-05).
    - The `loadManifest` body still reads `native_runtime_version` from the compatibility block and now raises `error(...)` (or equivalent fail-closed throw) when absent.
    - `grep -n 'val nativeRuntimeVersion: String' packages/crosswake-shell-core-android/src/main/java/dev/crosswake/shell/core/ActivationCoordinator.kt` still matches at the `ShellManifest` type (line ~98) — type unchanged, still non-nullable.
    - No other line in the file changed (diff is scoped to the line-594 assignment).
  </acceptance_criteria>
  <done>The silent `?: "1.0.0"` fallback is gone; native reads the manifest value and fails closed on an absent compatibility block; `ShellManifest.nativeRuntimeVersion` is still a non-nullable `String`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| manifest JSON → native runtime | Native parses `crosswake_manifest.json`; a malformed/absent compatibility block must fail closed, not silently assume a version |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-121-05 | Spoofing | `ActivationFixtures.loadManifest` native-runtime read | mitigate | Remove the silent `1.0.0` assumption so a manifest lacking the compatibility block fails closed (throws) rather than masquerading as a valid `1.0.0` runtime (CANON-05 / D-08) |
| T-121-SC | Tampering | npm/pip/cargo installs | accept | No package-manager installs — single-line Kotlin source edit, no dependency changes |
</threat_model>

<verification>
- `grep -n '?: "1.0.0"' ActivationCoordinator.kt` — must be empty (CANON-05 smoke check; Kotlin compiler is not on CI Linux per RESEARCH §Environment Availability, so grep is the merge-blocking proof; Android JUnit is advisory and wired in Phase 123).
- Diff scoped to line 594; `ShellManifest` type unchanged.
</verification>

<success_criteria>
- CANON-05: `ActivationCoordinator.kt` line 594 no longer contains `?: "1.0.0"`; native always reads the manifest-provided value and fails closed if absent.
</success_criteria>

<output>
Create `.planning/phases/121-canonical-contract-source/121-03-SUMMARY.md` when done. Record the exact fail-closed form chosen (the `error(...)` message text) and confirm the diff is a single-line change with `ShellManifest.nativeRuntimeVersion` still non-nullable.
</output>
