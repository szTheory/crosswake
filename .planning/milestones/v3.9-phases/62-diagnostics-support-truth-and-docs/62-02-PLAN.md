---
phase: 62-diagnostics-support-truth-and-docs
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/crosswake/operator_inspection.ex
  - test/crosswake/operator_inspection/operator_inspection_test.exs
autonomous: true
requirements:
  - DIAG-01
must_haves:
  truths:
    - "Operator Inspection exposes open routing active status for routes"
  artifacts:
    - path: lib/crosswake/operator_inspection.ex
      provides: "Notification route posture inspection"
  key_links:
    - from: lib/crosswake/operator_inspection.ex
      to: lib/crosswake/manifest/types.ex
      via: "RouteEntry notification_open"
---

<objective>
Update `Crosswake.OperatorInspection` to compute and expose `open_routing_active` for each route based on `notification_open`.

Purpose: Enable operator dashboards and workflow tools to determine which routes can be opened via push notification.
Output: Updated `OperatorInspection` returning `open_routing_active`.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/ROADMAP.md
@lib/crosswake/operator_inspection.ex
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Expose open_routing_active in OperatorInspection</name>
  <files>lib/crosswake/operator_inspection.ex, test/crosswake/operator_inspection/operator_inspection_test.exs</files>
  <behavior>
    - `notifications_entry(route)` (or added to `inspect_route`) should return a map containing `open_routing_active: boolean`.
    - `open_routing_active` is `true` when `route.notification_open` is not `false` and not `nil`.
  </behavior>
  <action>
    In `lib/crosswake/operator_inspection.ex`, identify where route capabilities are extracted (likely near `auth_entry/1` or `companion_entry/1`). Add or update a `notifications_entry(route)` function and include it in the `inspect_route` map.
    Compute `open_routing_active: route.notification_open != false and route.notification_open != nil`.
    Update `test/crosswake/operator_inspection/operator_inspection_test.exs` to assert that `open_routing_active` is generated properly in the inspected output.
  </action>
  <verify>
    <automated>mix test test/crosswake/operator_inspection/operator_inspection_test.exs</automated>
  </verify>
  <done>The generated operator inspection document includes `open_routing_active` for each route.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Operator Inspection | Output generated for downstream dashboard consumption |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-62-02 | Information Disclosure | OperatorInspection | accept | Only aggregates existing public manifest metadata |
</threat_model>

<verification>
`mix test` confirms OperatorInspection schema includes `open_routing_active`.
</verification>

<success_criteria>
Downstream tools inspecting Crosswake apps can see notification routing eligibility per route.
</success_criteria>

<output>
Create `.planning/phases/62-diagnostics-support-truth-and-docs/62-02-SUMMARY.md` when done
</output>