---
phase: 62-diagnostics-support-truth-and-docs
plan: 03
type: execute
wave: 2
depends_on: ["62-01", "62-02"]
files_modified:
  - lib/crosswake/doctor/doctor.ex
  - test/crosswake/doctor/doctor_test.exs
autonomous: true
requirements:
  - DIAG-01
must_haves:
  truths:
    - "Doctor CLI highlights strict telemetry and missing delivery support when push is configured"
  artifacts:
    - path: lib/crosswake/doctor/doctor.ex
      provides: "Diagnostic findings generation"
  key_links:
    - from: lib/crosswake/doctor/doctor.ex
      to: lib/crosswake/support_matrix/support_matrix.ex
      via: "SupportMatrix.notification_support_truth"
---

<objective>
Update `Crosswake.Doctor` to output diagnostic findings that reflect the SupportMatrix truth for notifications, advising operators on telemetry limits and delivery semantics.

Purpose: Prevent developers from expecting first-party push delivery guarantees or logging raw provider payloads.
Output: New `phase_62_notification_findings/1` pass emitting `notification.telemetry_contract` and `notification.delivery_deferred` advisories.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/ROADMAP.md
@lib/crosswake/doctor/doctor.ex
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add phase_62_notification_findings check</name>
  <files>lib/crosswake/doctor/doctor.ex, test/crosswake/doctor/doctor_test.exs</files>
  <behavior>
    - A new check function `phase_62_notification_findings(manifest)` emits `:advisory` findings when `notification_token` capabilities or active `notification_open` routes exist.
    - Findings should be named `"notification.telemetry_contract"` and `"notification.delivery_deferred"`.
    - Message strings should match the posture defined in `SupportMatrix`.
  </behavior>
  <action>
    Add `phase_62_notification_findings(manifest)` near the other diagnostic passes in `lib/crosswake/doctor/doctor.ex`.
    Detect notification presence via `manifest.routes` having `notification_open` truthy or `manifest.capabilities` containing `"notification_token"`.
    If present, emit `:advisory` checks indicating telemetry does not leak tokens/PII, and that Crosswake bindings are active but APNs/FCM delivery remains deferred. Use the standard `check/6` structure.
    Update `doctor_test.exs` to verify these findings appear when notifications are configured and are absent otherwise.
  </action>
  <verify>
    <automated>mix test test/crosswake/doctor/doctor_test.exs</automated>
  </verify>
  <done>Doctor outputs correct push notification guidance during CLI runs.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Local CLI output | Local generation of diagnostics |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-62-03 | Information Disclosure | Doctor | accept | Doctor output only analyzes existing project state |
</threat_model>

<verification>
`mix test test/crosswake/doctor/doctor_test.exs` passes with new assertions.
</verification>

<success_criteria>
`mix crosswake.doctor` alerts users of strict telemetry shapes and missing delivery pipelines before they attempt to debug missing push notifications.
</success_criteria>

<output>
Create `.planning/phases/62-diagnostics-support-truth-and-docs/62-03-SUMMARY.md` when done
</output>