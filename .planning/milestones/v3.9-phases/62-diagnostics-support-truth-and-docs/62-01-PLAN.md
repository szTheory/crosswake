---
phase: 62-diagnostics-support-truth-and-docs
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/crosswake/support_matrix/support_matrix.ex
  - test/crosswake/support_matrix/support_matrix_test.exs
  - lib/crosswake/companions/chimeway/telemetry.ex
  - test/crosswake/companions/chimeway/telemetry_test.exs
autonomous: true
requirements:
  - DIAG-02
must_haves:
  truths:
    - "Telemetry contract clearly defines restricted low-cardinality open events and denies PII"
    - "SupportMatrix exposes notification telemetry contract details"
    - "SupportMatrix clarifies that open routing is fully supported but delivery is deferred"
  artifacts:
    - path: lib/crosswake/companions/chimeway/telemetry.ex
      provides: "Chimeway telemetry open events and missing metadata"
    - path: lib/crosswake/support_matrix/support_matrix.ex
      provides: "Notification Support Truth enhancements"
  key_links:
    - from: lib/crosswake/support_matrix/support_matrix.ex
      to: lib/crosswake/companions/chimeway/telemetry.ex
      via: "event maps"
---

<objective>
Update `Chimeway.Telemetry` to include strict open-routing events/metadata and update `SupportMatrix` to broadcast these contracts along with the accurate support posture (token binding and open routing are fully supported; push delivery guarantees are deferred).

Purpose: Fulfill Phase 62 requirements for stable telemetry mapping and SupportMatrix truth.
Output: Updated `Telemetry` module and `SupportMatrix.notification_support_truth/0`.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/ROADMAP.md
@lib/crosswake/support_matrix/support_matrix.ex
@lib/crosswake/companions/chimeway/telemetry.ex
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add Notification Open Telemetry Events</name>
  <files>lib/crosswake/companions/chimeway/telemetry.ex, test/crosswake/companions/chimeway/telemetry_test.exs</files>
  <behavior>
    - `Telemetry.event_names/0` should include `[:crosswake, :notification, :open, :received]`, `[:crosswake, :notification, :open, :resolved]`, and `[:crosswake, :notification, :open, :denied]`.
    - `Telemetry.metadata_keys/0` should include `:route_id`, `:action_ref`, and `:denial_code`.
  </behavior>
  <action>
    Add the three `notification.open.*` events to `@event_names`.
    Add `:route_id`, `:action_ref`, and `:denial_code` to `@metadata_keys`.
    Also update `test/crosswake/companions/chimeway/telemetry_test.exs` lists so assertions continue to pass perfectly.
  </action>
  <verify>
    <automated>mix test test/crosswake/companions/chimeway/telemetry_test.exs</automated>
  </verify>
  <done>Telemetry module includes open-related event names and metadata keys.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Update SupportMatrix Notification Truth</name>
  <files>lib/crosswake/support_matrix/support_matrix.ex, test/crosswake/support_matrix/support_matrix_test.exs</files>
  <behavior>
    - `@notification_support_truth` includes a `telemetry` map listing the `event_names`, `metadata_keys`, and `forbidden_metadata_keys` from `Chimeway.Telemetry`.
    - `posture` and descriptions in the `notification_support_truth` entries explicitly state that token binding and open-routing are fully supported/resolvable, but APNs/FCM delivery remains deferred and unsupported.
  </behavior>
  <action>
    Update `@notification_support_truth` in `lib/crosswake/support_matrix/support_matrix.ex`.
    Extract telemetry data from `Crosswake.Companions.Chimeway.Telemetry`. Add `telemetry: %{event_names: [...], metadata_keys: [...], forbidden_metadata_keys: [...]}` matching the style used for `auth_contract_truth`.
    Refine the `posture` string and add `delivery_supported: false` details explicitly separating token/open readiness (supported) from delivery execution (deferred).
    Update any tests in `support_matrix_test.exs` evaluating this truth.
  </action>
  <verify>
    <automated>mix test test/crosswake/support_matrix/support_matrix_test.exs</automated>
  </verify>
  <done>SupportMatrix exports correct telemetry restrictions and properly scopes notification delivery vs binding.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Telemetry Export | Preventing PII and raw payload material from leaking to logs/APM |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-62-01 | Information Disclosure | Chimeway.Telemetry | mitigate | Strictly govern `@metadata_keys` and drop unknown/long keys |
</threat_model>

<verification>
`mix test` passes for modified files, verifying static telemetry structures and SupportMatrix outputs.
</verification>

<success_criteria>
`Chimeway.Telemetry` surfaces open events, and `SupportMatrix` broadcasts the phase 62 constraints accurately to downstream tools.
</success_criteria>

<output>
Create `.planning/phases/62-diagnostics-support-truth-and-docs/62-01-SUMMARY.md` when done
</output>