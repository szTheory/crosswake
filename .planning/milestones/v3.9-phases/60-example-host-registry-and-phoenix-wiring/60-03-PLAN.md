---
phase: 60-example-host-registry-and-phoenix-wiring
plan: "03"
title: "Phase 60 Proof Closure And Optional Worker Guidance"
type: execute
wave: 3
depends_on: [60-02]
status: pending
requirements: [TOKN-03]
requirements_addressed: [TOKN-03]
autonomous: true
files_modified:
  - examples/phoenix_host/README.md
  - test/crosswake/proof/phase60_chimeway_registry_test.exs
must_haves:
  truths:
    - "Phase 60 proof covers schema safety, lifecycle transitions, audit retention, telemetry rollback safety, and raw-token absence end to end."
    - "D-33/D-35: worker integration remains optional host-owned guidance that calls the synchronous registry APIs instead of adding Crosswake-managed workers."
    - "D-34/D-36/D-37: no Oban, Quantum, Broadway, or scheduler dependency is introduced into compiled Crosswake or example-host code; Quantum/cron are secondary pruning alternatives and GenServer loops are demo-grade only."
    - "D-38/D-42: provider token evidence is treated as mutable backend lifecycle input, never as delivery, route-open, or auth authority."
  artifacts:
    - "test/crosswake/proof/phase60_chimeway_registry_test.exs becomes the full merge-blocking TOKN-03 proof lane."
    - "examples/phoenix_host/README.md provides narrow non-compiled worker guidance tied to the shipped registry functions."
  key_links:
    - "README worker guidance must call `CrosswakeExample.Chimeway.Registry.prune_stale/1` or `apply_provider_feedback/2`, not duplicate lifecycle writes."
    - "phase60 proof must scan `examples/phoenix_host/mix.exs` and `examples/phoenix_host/lib/crosswake_example/chimeway/*.ex` to prove no worker dependencies or compiled worker code were added."
---

# Plan 60-03: Phase 60 Proof Closure And Optional Worker Guidance

<objective>
Close Phase 60 with the full proof lane and a narrow host-owned worker recipe that does not widen Crosswake into a delivery/worker platform.

Purpose: Keep TOKN-03 mechanically enforced while documenting how adopters may schedule pruning or provider feedback handling without introducing compiled worker ownership or dependency creep.
Output: Final Phase 60 proof file plus example-host README guidance for optional background integration.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/60-example-host-registry-and-phoenix-wiring/60-CONTEXT.md
@.planning/phases/60-example-host-registry-and-phoenix-wiring/60-RESEARCH.md
@.planning/phases/60-example-host-registry-and-phoenix-wiring/60-PATTERNS.md
@.planning/phases/60-example-host-registry-and-phoenix-wiring/60-VALIDATION.md
@examples/phoenix_host/README.md
@examples/phoenix_host/mix.exs
@examples/phoenix_host/lib/crosswake_example/chimeway/registry.ex
@test/crosswake/proof/phase60_chimeway_registry_test.exs
@test/crosswake/proof/phase59_chimeway_contract_test.exs

<interfaces>
Expected from `60-02`:

```elixir
CrosswakeExample.Chimeway.Registry.bind_or_rotate(context, evidence_or_attrs, opts \\ [])
CrosswakeExample.Chimeway.Registry.revoke_for_logout(context, opts \\ [])
CrosswakeExample.Chimeway.Registry.revoke_for_session_revocation(session_ref, opts \\ [])
CrosswakeExample.Chimeway.Registry.revoke_for_permission_loss(context, opts \\ [])
CrosswakeExample.Chimeway.Registry.apply_provider_feedback(provider_feedback_or_attrs, opts \\ [])
CrosswakeExample.Chimeway.Registry.prune_stale(opts \\ [])
```

From `examples/phoenix_host/mix.exs`:

```elixir
defp deps do
  [
    {:crosswake, path: "../.."},
    {:phoenix, "~> 1.8"},
    {:phoenix_live_view, "~> 1.1"},
    {:plug, "~> 1.16"},
    {:jason, "~> 1.4"},
    {:ecto_sql, "~> 3.10"},
    {:ecto_sqlite3, "~> 0.16"}
  ]
end
```
</interfaces>
</context>

<tasks>
  <task id="60-03-01" type="execute">
    <name>Finish the merge-blocking Phase 60 proof lane with source and dependency assertions</name>
    <files>
      test/crosswake/proof/phase60_chimeway_registry_test.exs
    </files>
    <read_first>
      AGENTS.md
      .planning/PROJECT.md
      .planning/ROADMAP.md
      .planning/phases/60-example-host-registry-and-phoenix-wiring/60-CONTEXT.md
      .planning/phases/60-example-host-registry-and-phoenix-wiring/60-VALIDATION.md
      examples/phoenix_host/mix.exs
      examples/phoenix_host/lib/crosswake_example/chimeway/registry.ex
      examples/phoenix_host/lib/crosswake_example/chimeway/token_binding.ex
      examples/phoenix_host/lib/crosswake_example/chimeway/token_binding_event.ex
      test/crosswake/proof/phase59_chimeway_contract_test.exs
      test/crosswake/proof/phase60_chimeway_registry_test.exs
    </read_first>
    <action>Finish `test/crosswake/proof/phase60_chimeway_registry_test.exs` as the merge-blocking TOKN-03 proof per D-26 through D-32, D-34, and D-38 through D-42. Keep the temporary SQLite harness from 60-01, then add end-to-end assertions for initial bind, same-token refresh, rotation, logout revocation, session revocation with `session_version` protection, permission loss, provider invalidation, non-invalidating feedback, and staleness pruning. Add explicit source assertions that the new migrations define the expected table names and partial unique indexes, that `examples/phoenix_host/mix.exs` does not mention `:oban`, `:quantum`, or `:broadway`, and that no compiled file under `examples/phoenix_host/lib/crosswake_example/chimeway/` contains `use Oban.Worker`, `Quantum`, `Broadway`, or a scheduler loop. Re-run the seeded raw-token checks against persisted rows, audit rows, result maps, telemetry metadata, and proof output so `raw_apns_token_should_not_leak_123` fails the lane anywhere it surfaces.</action>
    <verify>
      <automated>mix test test/crosswake/proof/phase60_chimeway_registry_test.exs</automated>
    </verify>
    <done>Phase 60 has one merge-blocking proof lane that catches lifecycle regressions, telemetry leakage, audit-loss regressions, and worker/dependency overclaims.</done>
    <acceptance_criteria>
      - `mix test test/crosswake/proof/phase60_chimeway_registry_test.exs` passes.
      - The proof contains explicit assertions for all TOKN-03 lifecycle branches listed in `60-VALIDATION.md`.
      - The proof fails if `examples/phoenix_host/mix.exs` gains `:oban`, `:quantum`, or `:broadway`.
      - The proof fails if any compiled Chimeway example-host file contains `use Oban.Worker`, `Quantum`, `Broadway`, or an in-tree scheduler implementation.
      - The proof fails if `raw_apns_token_should_not_leak_123` appears in persisted data, telemetry metadata, or inspected proof output.
    </acceptance_criteria>
  </task>

  <task id="60-03-02" type="execute">
    <name>Add narrow host-owned worker guidance without widening compiled scope</name>
    <files>
      examples/phoenix_host/README.md
      test/crosswake/proof/phase60_chimeway_registry_test.exs
    </files>
    <read_first>
      .planning/phases/60-example-host-registry-and-phoenix-wiring/60-CONTEXT.md
      .planning/phases/60-example-host-registry-and-phoenix-wiring/60-PATTERNS.md
      examples/phoenix_host/README.md
      examples/phoenix_host/mix.exs
      examples/phoenix_host/lib/crosswake_example/chimeway/registry.ex
      test/crosswake/proof/phase60_chimeway_registry_test.exs
    </read_first>
    <action>Update `examples/phoenix_host/README.md` per D-33 through D-37 with one narrow "Optional Chimeway background jobs" section. The section must say Crosswake ships synchronous registry APIs only, show host-owned examples that call `CrosswakeExample.Chimeway.Registry.prune_stale/1` and `CrosswakeExample.Chimeway.Registry.apply_provider_feedback/2`, identify Oban as the primary durable recipe, mention cron/Quantum only as host scheduling alternatives for pruning, and explicitly state that Broadway belongs only to future high-volume queue ingestion outside Phase 60. Do not add compiled worker modules, dependencies, or README language that implies Crosswake now owns notification delivery orchestration. Extend `test/crosswake/proof/phase60_chimeway_registry_test.exs` so it asserts the README section exists, names `prune_stale/1` and `apply_provider_feedback/2`, states that workers remain host-owned, and rejects wording that claims Chimeway ships bundled workers, push delivery guarantees, or route-open authority.</action>
    <verify>
      <automated>mix test test/crosswake/proof/phase60_chimeway_registry_test.exs test/crosswake/proof/phase59_chimeway_contract_test.exs</automated>
    </verify>
    <done>The example host documents optional worker usage as a host-owned integration recipe without changing the shipped dependency or authority boundary.</done>
    <acceptance_criteria>
      - `mix test test/crosswake/proof/phase60_chimeway_registry_test.exs test/crosswake/proof/phase59_chimeway_contract_test.exs` passes.
      - `examples/phoenix_host/README.md` contains an "Optional Chimeway background jobs" section that names `CrosswakeExample.Chimeway.Registry.prune_stale/1` and `CrosswakeExample.Chimeway.Registry.apply_provider_feedback/2`.
      - The README explicitly says Crosswake ships synchronous registry APIs only and that background jobs remain host-owned.
      - The README does not claim bundled Oban/Quantum/Broadway integration, push delivery guarantees, or notification-open routing support in Phase 60.
      - The proof file checks the README wording so worker overclaims become merge-blocking failures.
    </acceptance_criteria>
  </task>
</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| shipped registry API -> public proof/docs surface | Public proof and guidance must preserve the same authority and non-claim boundaries as the code. |
| example-host docs -> adopter expectations | Optional worker guidance can overclaim Crosswake ownership if not tightly scoped. |
| dependency file -> compiled runtime | Background-job dependencies or worker code can silently widen the shipped surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-60-03A | Information Disclosure | `phase60_chimeway_registry_test.exs` | mitigate | Seed raw-token strings through full proof runs and fail if they appear in persisted rows, telemetry metadata, or source examples. |
| T-60-03B | Repudiation | proof/source assertions | mitigate | Assert audit rows and migrations remain append-only and present after revocation/pruning so lifecycle truth cannot be silently removed. |
| T-60-03C | Spoofing | README guidance | mitigate | Require language that backend-owned registry APIs remain authoritative and reject any wording that makes provider evidence or workers look like delivery/open authority. |
| T-60-03D | Tampering | dependency/runtime scope | mitigate | Scan `examples/phoenix_host/mix.exs` and compiled Chimeway files for Oban/Quantum/Broadway or scheduler code and fail the proof if found. |
| T-60-03E | Information Disclosure | docs/proof examples | mitigate | Keep optional worker examples in markdown only and assert they call registry APIs using sanitized refs, not raw token/provider payload examples. |
| T-60-03F | Elevation of Privilege | worker guidance | mitigate | Document workers as host-owned optional recipes only, with no compiled worker modules or bundled job orchestration in Phase 60. |
</threat_model>

<verification>
Run:

```sh
mix test test/crosswake/companions/chimeway test/crosswake/proof/phase59_chimeway_contract_test.exs test/crosswake/proof/phase60_chimeway_registry_test.exs
```
</verification>

<success_criteria>
- Phase 60 ends with one proof lane that covers schema safety, lifecycle transitions, audit retention, telemetry rollback safety, and no-dependency assertions.
- Optional worker guidance exists only as host-owned markdown and points adopters back to the synchronous registry APIs instead of adding compiled workers.
- The phase still does not claim push delivery, notification-open routing, or Crosswake-managed background orchestration.
</success_criteria>

<output>
Create `.planning/phases/60-example-host-registry-and-phoenix-wiring/60-03-SUMMARY.md` when done
</output>
