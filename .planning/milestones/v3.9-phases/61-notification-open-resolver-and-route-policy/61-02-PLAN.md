---
phase: 61-notification-open-resolver-and-route-policy
plan: 2
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/crosswake/policy/schema.ex
  - lib/crosswake/policy/route.ex
  - lib/crosswake/manifest/types.ex
  - lib/crosswake/manifest/builder.ex
  - lib/crosswake/compatibility/compatibility.ex
  - test/crosswake/manifest/builder_test.exs
autonomous: true
requirements:
  - OPEN-01
  - OPEN-03

must_haves:
  truths:
    - "Routes can explicitly opt in to notification open activation"
    - "Notification open opt-in defaults to fail-closed if absent"
    - "Routes can specify an allowlist of permitted notification actions"
  artifacts:
    - path: "lib/crosswake/policy/schema.ex"
      provides: "notification_open validation logic"
    - path: "lib/crosswake/compatibility/compatibility.ex"
      provides: "notification_open capability check"
  key_links:
    - from: "lib/crosswake/policy/schema.ex"
      to: "lib/crosswake/policy/route.ex"
      via: "schema validation"
---

<objective>
Implement the `notification_open` DSL attribute for explicit route opt-in.

Purpose: Ensure routes can declare whether they accept notification opens and define which specific notification actions they support, defaulting to fail-closed.
Output: Updated DSL schemas, manifest materialized route attributes, and compatibility layer checks.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add `notification_open` to route DSL</name>
  <files>lib/crosswake/policy/schema.ex, lib/crosswake/policy/route.ex, test/crosswake/policy/schema_test.exs</files>
  <behavior>
    - `notification_open: true` is valid.
    - `notification_open: [actions: [:view]]` is valid.
    - Missing `notification_open` defaults to `nil`.
    - Invalid types (e.g. integer) cause NimbleOptions validation errors.
  </behavior>
  <action>
    Add the `notification_open` field to `Crosswake.Policy.Route` struct.
    In `Crosswake.Policy.Schema`, declare `notification_open` using a custom validator (similar to `auth_return` or `commerce`). It must accept `true` or a keyword list `[actions: [atom()]]`.
    Ensure backwards compatibility (the field is optional and defaults to `nil` or `false`). (Decisions D-14, D-15)
  </action>
  <verify>
    <automated>mix test test/crosswake/policy/schema_test.exs</automated>
  </verify>
  <done>Routes can declare `notification_open` and it correctly validates via NimbleOptions.</done>
</task>

<task type="auto">
  <name>Task 2: Materialize attribute in Manifest</name>
  <files>lib/crosswake/manifest/types.ex, lib/crosswake/manifest/builder.ex, test/crosswake/manifest/builder_test.exs</files>
  <action>
    Add `notification_open` to `Crosswake.Manifest.Types.RouteEntry`.
    Update `Crosswake.Manifest.Builder.build_route_entry/1` to transfer the `notification_open` value from the parsed policy route struct into the materialized `RouteEntry`.
  </action>
  <verify>
    <automated>mix test test/crosswake/manifest/builder_test.exs</automated>
  </verify>
  <done>The parsed `notification_open` policy becomes embedded in the immutable `RouteEntry`.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add notification_open compatibility check</name>
  <files>lib/crosswake/compatibility/compatibility.ex, test/crosswake/compatibility/compatibility_test.exs</files>
  <behavior>
    - If `activation_source` is `:notification`, it warns/errors if `notification_open` is absent.
  </behavior>
  <action>
    In `Crosswake.Compatibility.Compatibility`, add `validate_notification_open/3` to the `route_findings` pipeline.
    Mirroring `validate_external_entry`: if `Keyword.get(opts, :activation_source) == :notification` and the route does not have `notification_open` enabled, append a finding. 
    This provides defense-in-depth and surfaces the mismatch in support tooling. (Decision D-17)
  </action>
  <verify>
    <automated>mix test test/crosswake/compatibility/compatibility_test.exs</automated>
  </verify>
  <done>Compatibility pipeline checks notification activation against route opt-in.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Policy → Runtime | Push activations represent an external threat vector; explicit opt-in enforces isolation. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-61-02 | Elevation of Privilege | RouteGate (Compatibility) | mitigate | Fail-closed default. Deep links cannot implicitly enable push opens; `notification_open` must be explicit. |
</threat_model>

<verification>
- DSL accepts `notification_open` without breaking existing configurations.
- Manifest materializes it.
- Compatibility findings accurately reflect unsupported notification opens.
</verification>

<success_criteria>
Crosswake policies natively model notification open permissions and actions, completely decoupling push entry from deep link entry.
</success_criteria>