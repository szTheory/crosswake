---
phase: 28-release-please-configuration-files
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: ["release-please-config.json", ".release-please-manifest.json", ".tool-versions"]
autonomous: true
requirements: ["REL-01", "REL-02", "REL-09"]
must_haves:
  truths:
    - "release-please configuration accurately pinpoints elixir and erlang versions for reproducible builds"
    - "release-please manifest starts exactly at version 0.0.0"
    - "release-please config is correctly pinned to version 0.1.0 with correct bootstap SHA and minor/patch configurations"
  artifacts:
    - path: "release-please-config.json"
      provides: "release-please behavior configuration"
      contains: "bootstrap-sha"
    - path: ".release-please-manifest.json"
      provides: "current version tracking for release-please"
      contains: "\"0.0.0\""
    - path: ".tool-versions"
      provides: "pinned erlang and elixir versions"
      contains: "elixir 1.19.5-otp-27"
  key_links:
    - from: "release-please-config.json"
      to: "release-please"
      via: "packages.\".\".release-type"
      pattern: "\"elixir\""
---

<objective>
Land the three release-please config artifacts on disk so the workflow added in Phase 29 has a manifest, a config, and a tool-versions file to read.

Purpose: These files ensure reproducible builds across environments and configure how release-please interacts with our repository history, preventing off-by-one versioning errors and backwards CHANGELOG walks.
Output: `release-please-config.json`, `.release-please-manifest.json`, and `.tool-versions`.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/milestones/v3.3-phases/28-release-please-configuration-files/28-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create release-please-config.json</name>
  <files>release-please-config.json</files>
  <action>Create `release-please-config.json` at the root of the repository. Ensure it contains the following exactly:
- `release-type` set to `"elixir"` for the `.` package.
- `release-as` set to `"0.1.0"` for the `.` package.
- `bootstrap-sha` set to `"f788e312747e14b157c8498ba710f057534530f5"`.
- `bump-minor-pre-major` set to `false`.
- `bump-patch-for-minor-pre-major` set to `true`.

The JSON file must be correctly formatted, indented with 2 spaces, and have no trailing commas.
</action>
  <verify>
    <automated>cat release-please-config.json | jq -e '.packages["."]["release-type"] == "elixir"' > /dev/null && cat release-please-config.json | jq -e '."bootstrap-sha" == "f788e312747e14b157c8498ba710f057534530f5"' > /dev/null</automated>
  </verify>
  <done>release-please-config.json exists with the specified parameters, correctly formatted as valid JSON.</done>
</task>

<task type="auto">
  <name>Task 2: Create .release-please-manifest.json</name>
  <files>.release-please-manifest.json</files>
  <action>Create `.release-please-manifest.json` at the root of the repository. It MUST contain exactly `{"." : "0.0.0"}` (NOT `"0.1.0"`). This prevents the off-by-one footgun where release-please would propose `0.2.0` first. Format the file with 2-space indentation and no trailing commas.
</action>
  <verify>
    <automated>cat .release-please-manifest.json | jq -e '."." == "0.0.0"' > /dev/null</automated>
  </verify>
  <done>.release-please-manifest.json is created correctly and holds "0.0.0" for the "." package.</done>
</task>

<task type="auto">
  <name>Task 3: Create .tool-versions file</name>
  <files>.tool-versions</files>
  <action>Create `.tool-versions` at the root of the repository. It must pin the Erlang and Elixir versions used across the project's CI and local development. Based on prior decisions, it must contain exactly:
```
erlang 27.3
elixir 1.19.5-otp-27
```
Ensure there is a newline after the last line.
</action>
  <verify>
    <automated>grep -q "erlang 27.3" .tool-versions && grep -q "elixir 1.19.5-otp-27" .tool-versions</automated>
  </verify>
  <done>.tool-versions correctly pins elixir and erlang versions as required.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Tool Versions -> CI Runner | The environment tools dictated by the repo are installed and run on the CI host. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28-01 | Tampering | `.tool-versions` | mitigate | Setting exact versions ensures reproducibility and mitigates supply-chain alterations of untagged version targets. |
| T-28-02 | Information Disclosure | Configuration files | accept | These configuration files do not contain secrets or sensitive information; they define public open-source build tooling versions. |
</threat_model>

<verification>
- `release-please-config.json` contains valid json and standard constraints.
- `.release-please-manifest.json` contains `{"." : "0.0.0"}`.
- `.tool-versions` pins erlang and elixir correctly.
</verification>

<success_criteria>
1. `release-please-config.json` exists at repo root with `release-type: "elixir"`, `bump-minor-pre-major: false`, `bump-patch-for-minor-pre-major: true`, a one-time `release-as: "0.1.0"` pin on the `.` package, and a `bootstrap-sha` anchored.
2. `.release-please-manifest.json` exists at repo root with exactly `{".": "0.0.0"}`.
3. `.tool-versions` exists at repo root and pins Elixir + Erlang/OTP versions.
</success_criteria>

<output>
After completion, create `.planning/milestones/v3.3-phases/28-release-please-configuration-files/28-01-SUMMARY.md`
</output>