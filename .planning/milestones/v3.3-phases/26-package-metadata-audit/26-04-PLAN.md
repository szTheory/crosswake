---
phase: 26-package-metadata-audit
plan: 04
type: execute
wave: 3
depends_on:
  - 26-01
  - 26-02
  - 26-03
files_modified: []
autonomous: true
requirements:
  - META-06
must_haves:
  truths:
    - "mix deps.get exits 0 after the Phase 26 mix.exs changes"
    - "mix compile --warnings-as-errors exits 0 (no warnings) after the Phase 26 mix.exs changes"
    - "ex_doc 0.38.x is resolved into mix.lock"
    - "Cross-source consistency holds: every directory referenced in docs/0 :extras is present in :files (Pitfall 14 cross-check)"
  artifacts:
    - path: "mix.lock"
      provides: "Resolved dependency tree including ex_doc"
      contains: "ex_doc"
  key_links:
    - from: "mix deps.get"
      to: "mix.lock entry for :ex_doc"
      via: "hex.pm resolves ~> 0.38 constraint"
      pattern: "\"ex_doc\""
---

<objective>
Verify that the Phase 26 metadata audit changes (plans 01, 02, 03) produce a `mix.exs` + `LICENSE` state that satisfies all 5 ROADMAP success criteria and that `mix deps.get && mix compile --warnings-as-errors` exits cleanly with zero warnings.

Purpose: This is the Phase 26 verification gate per D-15. It runs the only build/compile command in scope for Phase 26 (`mix deps.get && mix compile`). `mix docs` and `mix hex.build --unpack` are INTENTIONALLY NOT RUN HERE — they are deferred to Phase 30 (HEX-02 / HEX-03). Running them here would introduce a cross-phase coupling on CHANGELOG.md (which Phase 27 creates) and on README absolute-URL audit (Phase 30 HEX-01).

Output: An updated `mix.lock` (auto-generated by `mix deps.get`) and a clean compile run. No source files are modified by this plan. A summary that records the resolved ex_doc version and confirms all five ROADMAP success criteria.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/milestones/v3.3-phases/26-package-metadata-audit/26-CONTEXT.md
@.planning/research/PITFALLS.md
@mix.exs
@LICENSE
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run mix deps.get and mix compile --warnings-as-errors</name>
  <files></files>
  <read_first>
    - mix.exs (post-plans 01/02/03 state — must show @source_url, full package block, ex_doc dep, defp docs)
    - LICENSE (post-plan-01 state — must exist with Apache-2.0 canonical text)
    - .planning/milestones/v3.3-phases/26-package-metadata-audit/26-CONTEXT.md (D-15 — verification posture)
  </read_first>
  <action>
    Run the following commands in order. Each MUST exit 0. Do not modify any source file in this task.

    1. `mix deps.get` — resolves and downloads dependencies, including the new `ex_doc ~> 0.38`. Updates `mix.lock`.
    2. `mix compile --warnings-as-errors` — compiles the project with all warnings treated as errors. Per SC #5, this must succeed cleanly.

    If `mix compile --warnings-as-errors` fails:
    - Read the warning/error output.
    - If the error is in `mix.exs` itself (syntax error, undefined attribute, mismatched parens), this indicates a defect in plan 02 or plan 03 — STOP and report which plan needs revision.
    - If the error is in `lib/` (warnings from existing application code surfaced by adding deps), this is pre-existing tech debt outside Phase 26's scope — STOP and report as a blocker; do NOT mutate `lib/` to make this pass.

    Do NOT run any of the following — they are explicitly out of scope for Phase 26 per D-15:
    - `mix docs` (deferred to Phase 30 HEX-03)
    - `mix hex.build` (deferred to Phase 30 HEX-02)
    - `mix hex.build --unpack` (deferred to Phase 30 HEX-02)
    - `mix hex.publish --dry-run` (deferred to Phase 30 PRF-01)
    - `mix test` (not a Phase 26 gate; no test files added in this phase)
  </action>
  <verify>
    <automated>mix deps.get && mix compile --warnings-as-errors && grep -q '"ex_doc"' mix.lock && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `mix deps.get` exits 0.
    - `mix compile --warnings-as-errors` exits 0 with no warning output.
    - `mix.lock` is updated and contains an `"ex_doc"` entry (verify with `grep -c '"ex_doc"' mix.lock` returns at least 1).
    - The resolved ex_doc version in `mix.lock` matches the `~> 0.38` constraint (verify with `grep '"ex_doc"' mix.lock | grep -E '"0\.(38|39)\.'` returns at least 1 — accept 0.38.x or 0.39.x within the `~> 0.38` semver-flexible range).
    - No source files in `lib/` are modified by this task.
  </acceptance_criteria>
  <done>
    Build is green. `mix deps.get && mix compile --warnings-as-errors` exits 0. `mix.lock` reflects the new ex_doc dependency.
  </done>
</task>

<task type="auto">
  <name>Task 2: Cross-source consistency audit (Pitfall 14)</name>
  <files></files>
  <read_first>
    - mix.exs (final Phase 26 state)
    - .planning/research/PITFALLS.md (Pitfall 14 — guides-in-extras-but-not-:files inverse-failure cross-check)
  </read_first>
  <action>
    Run a static consistency check between `docs/0` `:extras` and `:package :files` in the final `mix.exs`. Every directory or file referenced by extras MUST be in the `:files` allowlist (or be a top-level file the allowlist explicitly enumerates), else hexdocs.pm builds from tarball will silently omit content (Pitfall 14).

    Manual cross-check (no script required):

    1. Read the `:files` allowlist tokens from the `~w(...)` sigil in `defp package`. Expected tokens: `lib priv .formatter.exs mix.exs README.md LICENSE CHANGELOG.md guides`.
    2. Read the extras list from `defp docs`. Expected entries: README.md, LICENSE, and 11 `guides/*.md` files.
    3. For each extras entry, confirm the referenced file or directory is in `:files`:
       - `"README.md"` → covered by token `README.md` ✓
       - `"LICENSE"` → covered by token `LICENSE` ✓
       - `"guides/*.md"` (11 entries) → all covered by directory token `guides` ✓
    4. Confirm `"CHANGELOG.md"` is NOT in extras (D-12). It MAY be in `:files` (forward-reference per D-06 is intentional for one-commit shape) — that asymmetry is acceptable; the inverse (in extras, not in `:files`) is the failure case.

    Also verify the 11 guide files exist on disk so that a fresh checkout of the Phase 26 commit would build clean if ex_doc were run (even though we don't run mix docs here):

    `ls guides/install.md guides/support_matrix.md guides/adopter_profiles.md guides/user_flows.md guides/capabilities.md guides/bridge.md guides/offline.md guides/commerce.md guides/compatibility.md guides/native_shell.md guides/packs.md`

    All 11 MUST exist (already confirmed during planning).
  </action>
  <verify>
    <automated>test -f guides/install.md && test -f guides/support_matrix.md && test -f guides/adopter_profiles.md && test -f guides/user_flows.md && test -f guides/capabilities.md && test -f guides/bridge.md && test -f guides/offline.md && test -f guides/commerce.md && test -f guides/compatibility.md && test -f guides/native_shell.md && test -f guides/packs.md && grep -F 'files: ~w(lib priv .formatter.exs mix.exs README.md LICENSE CHANGELOG.md guides)' mix.exs && ! grep -F '"CHANGELOG.md"' mix.exs && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - All 11 `guides/*.md` files exist on disk (verified by 11 `test -f` checks).
    - `:files` allowlist contains the `guides` token: `grep -c 'files: ~w(lib priv \.formatter\.exs mix\.exs README\.md LICENSE CHANGELOG\.md guides)' mix.exs` returns exactly 1.
    - `"CHANGELOG.md"` does NOT appear in the extras list (D-12 invariant): `grep -c '"CHANGELOG.md"' mix.exs` returns 0.
    - `"README.md"` is referenced in extras and `README.md` token is in `:files`.
    - `"LICENSE"` is referenced in extras and `LICENSE` token is in `:files`, and the LICENSE file exists on disk: `test -f LICENSE` exits 0.
  </acceptance_criteria>
  <done>
    Cross-source consistency confirmed: every extras entry has a backing file or directory in `:files`, all referenced files exist on disk, and the D-12 invariant (CHANGELOG.md NOT in extras) holds. Pitfall 14 inverse-failure case is avoided.
  </done>
</task>

<task type="auto">
  <name>Task 3: ROADMAP success criteria final sweep</name>
  <files></files>
  <read_first>
    - .planning/ROADMAP.md §"Phase 26: Package Metadata Audit" (5 success criteria)
    - mix.exs (final state)
    - LICENSE (final state)
  </read_first>
  <action>
    Run the 5 ROADMAP success-criteria checks as concrete grep/test commands. Each MUST pass. This is the explicit Phase 26 final-state proof.

    Note on ROADMAP vs CONTEXT.md naming asymmetry (D-04): ROADMAP SC #2 says the `:links` map has a `"Docs"` key; CONTEXT.md D-04 (authoritative design contract) says the key is `"Documentation"`. Per the planning context, CONTEXT.md is authoritative — verify `"Documentation"`, not `"Docs"`. The ROADMAP wording is equivalent in intent; CONTEXT.md's `"Documentation"` is what plan 02 implements.
  </action>
  <verify>
    <automated>
      grep -q '@source_url "https://github.com/szTheory/crosswake"' mix.exs &&
      ! grep -q 'github.com/example/crosswake' mix.exs &&
      grep -q 'name: "crosswake"' mix.exs &&
      grep -q 'licenses: \["Apache-2.0"\]' mix.exs &&
      grep -q 'files: ~w(lib priv .formatter.exs mix.exs README.md LICENSE CHANGELOG.md guides)' mix.exs &&
      grep -q '"Changelog" => "#{@source_url}/blob/main/CHANGELOG.md"' mix.exs &&
      grep -q '"Documentation" => "https://hexdocs.pm/crosswake"' mix.exs &&
      grep -q '"GitHub" => @source_url' mix.exs &&
      test -f LICENSE && head -1 LICENSE | grep -q "Apache License" &&
      grep -q "Copyright 2026 szTheory" LICENSE &&
      grep -q 'defp docs do' mix.exs &&
      grep -q 'main: "readme"' mix.exs &&
      grep -q 'source_ref: "v#{@version}"' mix.exs &&
      grep -q '{:ex_doc, "~> 0.38", only: :dev, runtime: false}' mix.exs &&
      mix deps.get && mix compile --warnings-as-errors &&
      echo "ALL 5 ROADMAP SUCCESS CRITERIA: PASS"
    </automated>
  </verify>
  <acceptance_criteria>
    - **SC #1 (META-01):** `grep -c '@source_url "https://github.com/szTheory/crosswake"' mix.exs` returns 1 AND `grep -c 'github.com/example/crosswake' mix.exs` returns 0.
    - **SC #2 (META-02, META-03, META-04):** `grep -c 'name: "crosswake"' mix.exs` returns at least 2 (project/0 and package/0) AND `grep -c 'licenses: \["Apache-2.0"\]' mix.exs` returns 1 AND `grep -c 'files: ~w(lib priv \.formatter\.exs mix\.exs README\.md LICENSE CHANGELOG\.md guides)' mix.exs` returns 1 AND the three link keys (`"Changelog"`, `"Documentation"`, `"GitHub"`) each appear exactly once.
    - **SC #3 (META-05):** `test -f LICENSE` exits 0 AND `head -1 LICENSE | grep -q "Apache License"` exits 0 AND `grep -c "Copyright 2026 szTheory" LICENSE` returns 1.
    - **SC #4 (META-06, part 1):** `grep -c 'defp docs do' mix.exs` returns 1 AND `grep -c 'main: "readme"' mix.exs` returns 1 AND `grep -c 'source_ref: "v#{@version}"' mix.exs` returns 1 AND all 11 `"guides/<name>.md"` entries appear exactly once each in extras.
    - **SC #5 (META-06, part 2):** `grep -c '{:ex_doc, "~> 0.38", only: :dev, runtime: false}' mix.exs` returns 1 AND `mix deps.get` exits 0 AND `mix compile --warnings-as-errors` exits 0.
    - All 6 META-XX requirements (META-01 through META-06) are now satisfied by the Phase 26 final state.
  </acceptance_criteria>
  <done>
    All 5 ROADMAP success criteria pass. All 6 META-XX requirements are satisfied. Phase 26 is verified complete and ready for Phase 27 (which will set `@version "0.1.0"` as a no-op verification and create `CHANGELOG.md` plus add the single `"CHANGELOG.md"` line to extras).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `mix deps.get` → hex.pm registry | Resolving `~> 0.38` for ex_doc fetches package tarballs over HTTPS from hex.pm. Mix's `hex` plugin verifies checksums against `mix.lock` (after first resolution). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-04-01 | Tampering | mix.lock vs hex.pm registry | mitigate | First `mix deps.get` resolves and records SHA256 checksums in `mix.lock`. Subsequent runs verify against the lock. Standard Elixir tooling behavior; no additional plan-level mitigation required. |
| T-26-04-02 | Denial of Service | Phase 26 build gate vs cross-phase coupling | mitigate | D-15 explicitly forbids `mix docs` and `mix hex.build --unpack` at Phase 26. Plan 04 only runs `mix deps.get && mix compile --warnings-as-errors`. This prevents Phase 26 from being blocked by missing CHANGELOG.md (Phase 27) or README.md absolute-URL audit (Phase 30). |
| T-26-04-SC | Supply Chain | ex_doc install | accept | ex_doc is the canonical first-party Elixir docs generator from the Elixir core team. Not `[ASSUMED]`, `[SUS]`, or `[SLOP]`. No blocking-human checkpoint required. mix.lock pin after first resolution provides supply-chain hardening for future builds. |
</threat_model>

<verification>
This plan IS the Phase 26 verification gate. After it completes:
- `mix deps.get && mix compile --warnings-as-errors` exits 0.
- All 5 ROADMAP SC checks (task 3 `<verify>`) pass.
- All 6 META-XX requirements are satisfied.
- Pitfall 14 cross-check confirms `:files` vs extras consistency.
- `mix.lock` is updated and ready for commit alongside `mix.exs` and `LICENSE`.

Out-of-scope verification (deferred per D-15):
- `mix docs` — Phase 30 HEX-03.
- `mix hex.build --unpack` — Phase 30 HEX-02.
- `mix hex.publish --dry-run` — Phase 30 PRF-01.
</verification>

<success_criteria>
- All 5 ROADMAP Phase 26 success criteria pass via the grep/test commands in task 3.
- `mix deps.get` and `mix compile --warnings-as-errors` both exit 0.
- `mix.lock` contains a resolved `"ex_doc"` entry in the 0.38.x or 0.39.x range.
- Pitfall 14 cross-check passes: every extras entry has a backing file in `:files`.
- D-12 invariant holds: `"CHANGELOG.md"` is NOT in extras.
- No source files in `lib/` were modified by this plan.
</success_criteria>

<output>
Create `.planning/milestones/v3.3-phases/26-package-metadata-audit/26-04-SUMMARY.md` when done.
</output>
