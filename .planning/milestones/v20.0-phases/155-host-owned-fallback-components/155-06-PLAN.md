---
phase: 155-host-owned-fallback-components
plan: 06
type: execute
wave: 3
depends_on: ["155-01", "155-02", "155-03"]
files_modified:
  - priv/templates/crosswake/native_controls_ui/crosswake_fallbacks.ex.eex
  - priv/templates/crosswake/native_controls_ui/crosswake_fallback.css.eex
  - lib/mix/tasks/crosswake.gen.native_controls_ui.ex
  - test/mix/tasks/crosswake.gen.native_controls_ui_test.exs
  - test/crosswake/proof/phase155_native_controls_template_drift_test.exs
  - examples/phoenix_host/lib/crosswake_example_web/components/crosswake_fallbacks.ex
  - examples/phoenix_host/priv/static/assets/crosswake_fallback.css
  - examples/phoenix_host/lib/crosswake_example/saas_portal/approval_live.ex
autonomous: false
requirements: [FALL-01, FALL-02]

must_haves:
  truths:
    - "The destructive confirm modal renders `role=\"alertdialog\"`, has NO click-away dismissal, and puts initial focus on Cancel — the one tone branch (D-17, D-19)."
    - "The destructive action button is a FILLED `--cwfb-danger-bg` surface with `--cwfb-danger-fg` text at 6.02:1 in both themes, PLUS a border-left shape cue — shape in addition to colour, never instead of it (D-32)."
    - "The destructive confirm's `body` attr is required with no default, so the consequence sentence can never be missing (D-58, D-59, UI-SPEC E2/empty, E2/partial)."
    - "The action menu ships a mandatory `<h2>`, a `<ul>`/`<li>`/`<button>` structure, and no icons; shape, colour, and text carry all signal (D-20, E3/populated)."
    - "The action menu does NOT declare the APG menu role; the trigger takes `aria-expanded` plus `aria-controls`, not `aria-haspopup`, because the APG contract obliges roving tabindex and arrow keys — the JavaScript this phase exists to avoid (D-21)."
    - "The action menu is a centred modal at every viewport in this phase, not a trigger-anchored dropdown; anchoring is deferred to SEED-005 where elevation and z tokens land (D-22)."
    - "Menu rows scroll inside `max-height: min(60vh, 420px)` with the heading and Cancel outside the scroll container, so both stay reachable at any row count (D-20, E3/overflow)."
    - "The destructive row is last, behind a 4px gap band with a 3px left bar, and always routes through the destructive confirm — never straight to the mutation (D-20, retained: destructive confirmation)."
    - "Zero actions renders the menu empty-state row inside the normal chassis with heading and Cancel still present — never a bare box (D-59, E3/empty)."
    - "Zero rows shows the empty state; one through seven render without scroll; eight or more engage the scroll container; row copy is authored per action so no singular/plural interpolation exists (E3/zero-one-many)."
    - "An action present but not currently permitted renders as a disabled row carrying its reason inline — never a silently greyed control and never omitted (D-59, E3/partial)."
    - "Menu pending state renders on the trigger only, never per row, because a per-row spinner would need the icons this phase bans (D-20, E3/loading)."
    - "The menu itself renders no error state: it emits select-with-id or dismiss; a selection that cannot proceed surfaces as the inline alert and a destructive selection routes to the destructive confirm first (E3/error)."
    - "The destructive confirm's pending state renders on the action button using the destructive pending string, and a failed answer renders an inline `role=\"alert\"` in the still-open panel so it reads as 'nothing happened' rather than ambiguity (D-59, E2/loading, E2/error)."
    - "The destructive confirm's Cancel/Delete row sits outside the scroll container so the escape route is never scrolled away (E2/overflow)."
    - "Fail-closed states render inline with `role=\"alert\"`, never in a modal: the stale-binary string and the undeclared string from the copywriting contract; `:shell_unreachable` produces no string at all and the surface simply renders (D-59)."
    - "The inline alert region has no fixed height and never scrolls or clips — a clipped denial would read as silent degradation (E4/overflow)."
    - "Both inline alert variants are fixed literal strings with no interpolated values, so maximum length is known at author time (E4/long-text)."
    - "The `actions` shape is frozen as a list of maps carrying id, label, destructive, and icon keys, with exactly two outcomes — select-with-id and dismiss (D-53)."
    - "An inert commented Phase-156 hand-off block ships inside the generated action-menu source itself, because the generator has no force flag and a comment in the adopter-owned file is the only upgrade instruction that survives (D-55)."
    - "This phase's generated surfaces make no `Bridge.push/3` call and add no bridge command; they are complete, working, zero-shell UI on their own (D-01)."
    - "`@template_version` is bumped and the template drift hash is updated in the same commit as the template change (D-35)."
    - statement: "The required destructive consequence body wraps to as many lines as it needs and the bottom sheet grows and scrolls rather than clipping, at 320px (UI-SPEC E2/long-text)."
      verification: backstop
    - statement: "A long action-menu row label wraps to a second line and the row grows rather than truncating or ellipsizing, at 320px — the label is the row's only signal because icons are banned (UI-SPEC E3/long-text)."
      verification: backstop
  artifacts:
    - path: "priv/templates/crosswake/native_controls_ui/crosswake_fallbacks.ex.eex"
      provides: "confirm_modal/1 both tones plus action_menu/1 plus the inline fail-closed alert"
      contains: "def action_menu"
    - path: "examples/phoenix_host/lib/crosswake_example_web/components/crosswake_fallbacks.ex"
      provides: "regenerated committed output carrying the bumped stamp"
      contains: "template_version=2"
  key_links:
    - from: "priv/templates/crosswake/native_controls_ui/crosswake_fallbacks.ex.eex"
      to: "lib/crosswake/bridge.ex"
      via: "the menu's answer and dismiss handlers call Crosswake.Bridge.resolve/2 — the only dedup mechanism (D-25)"
      pattern: "Crosswake\\.Bridge\\.resolve"
    - from: "priv/templates/crosswake/native_controls_ui/crosswake_fallback.css.eex"
      to: "brandbook/tokens/crosswake.tokens.json"
      via: "--cwfb-danger-fg resolves through --cw-status-error-fg, added in 155-03"
      pattern: "cwfb-danger-fg"
  prohibitions:
    - statement: "MUST NOT declare an ARIA role whose contract the implementation does not honor. Claiming the APG menu role without roving tabindex, arrow keys, Home/End, and Tab-closes is worse than never claiming it — no ARIA is better than bad ARIA, and assistive-technology users pay for the difference (D-21)."
      category: safety
    - statement: "MUST NOT route a destructive menu row straight to the mutation. Every destructive selection passes through the destructive confirm first; there is no direct-to-mutation path and no configuration that creates one (D-20)."
      category: safety
    - statement: "MUST NOT let internal vocabulary — fallback, bridge, shell, capability, route policy, seam, manifest, hook, push, reply, answer, denial — appear in any string a product end user can read (D-09)."
      category: transparency

flagged_assumptions:
  - "UI-SPEC retained consideration `aria-modal` + VoiceOver is UNRESOLVED and must stay that way. D-23: `aria-modal=\"true\"` is unreliable in Safari + VoiceOver, which is why React Aria ships `ariaHideOutside`. The server-rendered `inert` background attribute is BELIEVED to mitigate it but is explicitly NOT PROVEN. Record it as a manual-check limitation in the generated module's moduledoc and in the phase summary. Do NOT write a passing assertion for it and do NOT schedule a checkpoint claiming it is verified."
---

<objective>
Expand out from the proven tracer: the destructive confirm tone, the action menu, the inline
fail-closed alerts, and the Phase-156 hand-off block.

Everything here builds on the slice 155-01 already proved end-to-end. The chassis, the alias
layer, the focus trap, the stamp, and the gate registrations exist; this plan adds the second
tone, the second surface, and the copy that makes a failure legible without ever putting
internal vocabulary in front of an end user.

Two things make this plan different from routine UI work. The `actions` shape is the phase's
real one-way door — it is host-owned, never regenerated, so getting it wrong means hand-editing
in every adopter's app — and CONTEXT.md asks for a dedicated review before it is frozen. And
the action menu deliberately does NOT claim the APG menu role, because honoring that contract
would require exactly the JavaScript this phase exists to avoid.

Purpose: FALL-01's second surface and second tone, complete, with no scope reduction.
Output: a complete two-surface component template, a regenerated committed host artifact
carrying a bumped stamp, and the wiring on the A1 route that 155-07 will drive.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/155-host-owned-fallback-components/155-CONTEXT.md
@.planning/phases/155-host-owned-fallback-components/155-UI-SPEC.md
@.planning/phases/155-host-owned-fallback-components/155-PATTERNS.md
@.planning/phases/155-host-owned-fallback-components/155-01-SUMMARY.md
@.planning/phases/155-host-owned-fallback-components/155-03-SUMMARY.md
</context>

## Artifacts this phase produces

Symbols created by Phase 155 (all plans). Newly-created — not drift.

**Mix tasks:** `mix crosswake.gen.native_controls_ui`
**Modules:** `Mix.Tasks.Crosswake.Gen.NativeControlsUi`, `Crosswake.ComponentTierGuard`,
`Crosswake.Bridge.UnknownCapabilityFamilyError`, `CrosswakeExampleWeb.CrosswakeFallbacks`,
`CrosswakeExample.E2E.UndeclaredControlLive`,
`Crosswake.Proof.Phase155NativeControlsTemplateDriftTest`, `Crosswake.ComponentTierGuardTest`
**Functions:** `confirm_modal/1`, `action_menu/1`, `template_version/0`, `stamp_prefix/0`,
`assert_no_component_tier!/1`, `raise_unknown_capability_family!/3`, `maybe_fetch_state/1`
**Template paths:** `priv/templates/crosswake/native_controls_ui/crosswake_fallbacks.ex.eex`,
`priv/templates/crosswake/native_controls_ui/crosswake_fallback.css.eex`
**Generated host paths:** `lib/<app>_web/components/crosswake_fallbacks.ex`,
`priv/static/assets/crosswake_fallback.css`
**Route paths:** `/crosswake/tokens.css`, `/_e2e/undeclared-control`
**Event names:** `crosswake_fallback_answer`, `crosswake_fallback_dismiss`
**Data attributes:** `data-cw-fallback`, `data-cw-fallback-tone`, `data-cw-fallback-surface`
**CSS custom properties:** `--cw-overlay-scrim`, `--cw-status-error-fg`, `--cwfb-*` (17 aliases)
**Token JSON keys:** `overlay.scrim`, `status.error-fg`, `primitive.current.950a72`
**Env vars:** `CROSSWAKE_PROOF_BREAK_FALLBACK`
**Test file paths:** `test/mix/tasks/crosswake.gen.native_controls_ui_test.exs`,
`test/crosswake/component_tier_guard_test.exs`,
`test/crosswake/proof/phase155_native_controls_template_drift_test.exs`,
`examples/phoenix_host/e2e/native_controls_fallback.spec.ts`
**Stable failure ids:** `proof.fall_02.no_component_tier.*`,
`crosswake.bridge.unknown_capability_family`, `proof.fall_01.template_version_drift`

<!-- planner-discipline-allow: role="menu" -->
<!-- planner-discipline-allow: aria-haspopup -->

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 1: Freeze the `actions` data shape — the phase's one-way door</name>
  <decision>The `actions` attr shape that `action_menu/1` accepts, and the two outcomes it emits.</decision>
  <context>
CONTEXT.md D-53 rates this the phase's real one-way door and explicitly asks for a dedicated
planning review before it is frozen. The reason is ownership, not difficulty: the generated
component is host-owned and never regenerated (there is no force flag and never will be), so
every adopter's copy keeps whatever shape ships today, forever. Getting it wrong means
hand-editing in every adopter's app rather than shipping a fix.

D-53 records the intended shape as a list of maps carrying `id`, `label`, `destructive`, and
`icon`, with exactly two outcomes — select-with-id and dismiss (`UX-CONTRACT.md:41`). D-54
records that Phase 156 adds the catalog entry, both native enum cases, the registry mapping,
and the transport choice with ZERO breaking changes to adopter-owned files; the cost is
additive hand-editing only, and an adopter who never takes Phase 156 keeps a working menu
forever.

The `icon` key is the point of tension worth naming: D-20 bans icons in this phase's rendering,
so `icon` ships as a reserved, unrendered key. Carrying it now is cheap; adding it later to a
never-regenerated file is not.
  </context>
  <options>
    <option id="freeze-as-recorded">
      <name>Freeze exactly as D-53 records: `[%{id, label, destructive, icon}]`, two outcomes</name>
      <pros>Matches the locked decision and `UX-CONTRACT.md:41`. Reserving `icon` now costs one unrendered key and saves a hand-edit in every adopter app when Phase 156 or SEED-005 wants it. Phase 156 lands additively (D-54).</pros>
      <cons>Ships a key this phase deliberately never renders, which a reader may mistake for dead code unless the moduledoc says why.</cons>
    </option>
    <option id="drop-icon">
      <name>Freeze without `icon`: `[%{id, label, destructive}]`</name>
      <pros>No unrendered key; nothing to explain.</pros>
      <cons>Adding `icon` later means every adopter hand-edits a file we never regenerate — exactly the cost D-53 flags as one-way. Diverges from `UX-CONTRACT.md:41`.</cons>
    </option>
    <option id="widen-outcomes">
      <name>Freeze the shape as recorded but add a third outcome beyond select and dismiss</name>
      <pros>More expressive for a future control.</pros>
      <cons>No third outcome is specified anywhere in the source artifacts, and inventing one now widens a one-way contract on speculation. `UX-CONTRACT.md:41` fixes two.</cons>
    </option>
  </options>
  <resume-signal>Select: freeze-as-recorded, drop-icon, or widen-outcomes. Planner recommendation is `freeze-as-recorded` — it is what D-53 locks, and the only cost is one documented reserved key.</resume-signal>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Destructive confirm tone and the inline fail-closed alert</name>
  <reversibility rating="costly">The generated function arities and attr names are called by adopter code in a file Crosswake never regenerates; renaming them later has no deprecation path (D-03).</reversibility>
  <precondition>`--cw-status-error-fg` resolves in the compiled `priv/static/crosswake/tokens.css` (landed by 155-03 Task 1).</precondition>
  <files>priv/templates/crosswake/native_controls_ui/crosswake_fallbacks.ex.eex, priv/templates/crosswake/native_controls_ui/crosswake_fallback.css.eex</files>
  <read_first>
    - `priv/templates/crosswake/native_controls_ui/crosswake_fallbacks.ex.eex` — the neutral `confirm_modal/1` as landed by 155-01; the destructive tone is a branch on the same component, not a second component.
    - `priv/templates/crosswake/native_controls_ui/crosswake_fallback.css.eex` — the `--cwfb-*` alias block and modal chassis to extend.
    - `.planning/phases/155-host-owned-fallback-components/155-UI-SPEC.md` § Confirm Modal — Contract, § Color, and § Copywriting Contract — the literal target state.
    - `deps/phoenix_live_view/lib/phoenix_live_view/js.ex` lines 990-1010 — `JS.focus/2`, used for the Cancel-focus tone branch.
    - `examples/phoenix_host/lib/crosswake_example/saas_portal/approval_live.ex` around lines 285-300 — the precedent where an unreachable shell produces no user-facing string at all.
    - `brandbook/tools/contrast.test.mjs` — the focus-ring and destructive-pair assertions from 155-03, which pin the ratios this markup must honor.
  </read_first>
  <behavior>
    - `confirm_modal/1` with `tone: :destructive` renders `role="alertdialog"`; with `tone: :neutral` it renders `role="dialog"`.
    - Destructive tone renders no click-away binding; neutral tone does.
    - Destructive tone sets initial focus on Cancel; neutral tone on the action button.
    - Destructive tone requires `body`; omitting it is a compile-time or render-time failure, not a silently missing consequence sentence.
    - The destructive action button carries both a filled danger background and a left border shape cue; removing either is a visible difference.
    - `@pending` renders the destructive pending string on the action button; the panel does not close.
    - `@error` renders an inline alert region inside the still-open panel and mutates nothing.
    - The inline fail-closed alert renders the stale-binary string or the undeclared string as a bare inline region, never inside a modal, and renders nothing at all for the unreachable-shell case.
  </behavior>
  <action>
Extend `confirm_modal/1` with a `tone` attr (`:atom`, `values: [:neutral, :destructive]`,
`default: :neutral`). Do not create a second component — one object, one a11y contract, one
chassis (D-15).

Destructive branch per D-17/D-19/D-32: `role="alertdialog"`; omit the click-away binding
entirely on this tone (the Radix Dialog/AlertDialog split shadcn inherits); `JS.focus(to:)`
targets Cancel; `data-cw-fallback-tone="destructive"`. Enforce `body` on this tone — if the
component DSL cannot express a conditionally-required attr, raise at render with a message
naming the tone and the missing attr, so the consequence sentence can never be absent (D-58's
compile-error-as-teaching-surface extended to the one attr that carries the danger).

Destructive action button styling per D-32, which was the user's decision over the cheaper
zero-token alternative: a FILLED `--cwfb-danger-bg` surface with `--cwfb-danger-fg` text
(6.02:1 in both themes), PLUS a 3px left-border shape cue. Both, always. The error token as
TEXT measures 2.44:1 on the dark inset surface, so a red-text or red-border-only destructive
confirm has no colour channel at all on dark — losing the danger channel on half of users'
devices for a sentence like "Delete this job? It cannot be undone" is a safety regression, not
a style preference.

Ship D-59's destructive strings as the defaults: title `Delete this job?`, body
`This removes the job and its 3 photos. It cannot be undone.`, action `Delete job`, pending
`Deleting…`, cancel `Cancel`, success `Deleted. The job and its photos are gone.` The failed
answer string is shared with the neutral tone.

Add a `fallback_alert/1` inline region: `role="alert"`, rendered inline in normal document
flow, never inside a modal, with no fixed height and no clipping — a clipped denial would read
as silent degradation, which is precisely what PROOF-01 forbids. Two literal variants from the
copywriting contract, the stale-binary one and the undeclared one, both fixed strings with no
interpolation. For the unreachable-shell case it renders nothing at all and the surface simply
appears, following the `approval_live.ex:292-297` precedent.

Extend the stylesheet template with the destructive treatment and the inline alert region,
using only `--cwfb-*` aliases. Keep the panel's scroll container rule so the Cancel/Delete row
stays outside it and the escape route is never scrolled away.

No end-user-visible string may use internal vocabulary. `push`, `reply`, `answer`, `denial`,
bridge, shell, capability, route policy, seam, manifest, and hook are legitimate in the
moduledoc and in attribute names, and nowhere a product user can read (D-09).
  </action>
  <verify>
    <automated>mix test test/mix/tasks/crosswake.gen.native_controls_ui_test.exs --warnings-as-errors && node brandbook/tools/check-consumer-drift.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'alertdialog' priv/templates/crosswake/native_controls_ui/crosswake_fallbacks.ex.eex` is at least 1.
    - `grep -c 'phx-click-away' priv/templates/crosswake/native_controls_ui/crosswake_fallbacks.ex.eex` is at least 1, and reading the template confirms the binding is inside the neutral-tone branch only.
    - `grep -c 'cwfb-danger-bg' priv/templates/crosswake/native_controls_ui/crosswake_fallback.css.eex` is at least 1 and the same for `cwfb-danger-fg`.
    - `grep -c 'border-left' priv/templates/crosswake/native_controls_ui/crosswake_fallback.css.eex` is at least 1 — the shape cue exists in addition to the fill.
    - `grep -c 'def fallback_alert' priv/templates/crosswake/native_controls_ui/crosswake_fallbacks.ex.eex` is 1 and `grep -c 'role="alert"' priv/templates/crosswake/native_controls_ui/crosswake_fallbacks.ex.eex` is at least 1.
    - `grep -c 'This action needs a newer version of the app' priv/templates/crosswake/native_controls_ui/crosswake_fallbacks.ex.eex` is 1 and `grep -c "This action isn't available here" priv/templates/crosswake/native_controls_ui/crosswake_fallbacks.ex.eex` is 1.
    - End-user copy scan: extract every double-quoted string that appears inside a `~H` block and confirm none contains `fallback`, `bridge`, `shell`, `capability`, `manifest`, `denial`, or `hook` (D-09). Record the extraction command and its empty result in the summary.
    - `grep -v '^ *[#/*]' priv/templates/crosswake/native_controls_ui/crosswake_fallback.css.eex | grep -c 'prefers-color-scheme'` is 0 and the same filtered grep for `data-theme` is 0 — the destructive treatment introduced no theme logic (D-25).
    - `node brandbook/tools/check-consumer-drift.mjs` exits 0 — the extended templates stay brand-clean and use literal class attributes only.
  </acceptance_criteria>
  <done>The destructive tone renders with an alert dialog role, no click-away, Cancel-first focus, and a danger signal carried by both fill and shape in both themes; the inline fail-closed alert renders outside any modal with fixed literal copy.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: action_menu/1, the Phase-156 hand-off block, and the template-version bump</name>
  <reversibility rating="one-way">The `actions` shape is host-owned and never regenerated; a wrong shape means hand-editing in every adopter's app (D-53). Frozen by the Task 1 checkpoint.</reversibility>
  <precondition>`Crosswake.Bridge.resolve/2` returns the socket unchanged on a socket that never called `attach/1` (landed by 155-02 Task 1) — the generated menu handlers call it directly.</precondition>
  <files>priv/templates/crosswake/native_controls_ui/crosswake_fallbacks.ex.eex, priv/templates/crosswake/native_controls_ui/crosswake_fallback.css.eex, lib/mix/tasks/crosswake.gen.native_controls_ui.ex, test/mix/tasks/crosswake.gen.native_controls_ui_test.exs, test/crosswake/proof/phase155_native_controls_template_drift_test.exs, examples/phoenix_host/lib/crosswake_example_web/components/crosswake_fallbacks.ex, examples/phoenix_host/priv/static/assets/crosswake_fallback.css, examples/phoenix_host/lib/crosswake_example/saas_portal/approval_live.ex</files>
  <read_first>
    - `priv/templates/crosswake/native_controls_ui/crosswake_fallbacks.ex.eex` — the chassis and both confirm tones as landed by Task 2; the menu reuses the same panel, not a new one.
    - `.planning/phases/155-host-owned-fallback-components/155-UI-SPEC.md` § Action Menu — Contract and § Copywriting Contract — the literal target state including the row cap, the gap band, and every string.
    - `lib/crosswake/bridge.ex` lines 264-292 — `resolve/2`'s docstring and its compare-and-delete body; the menu's handlers call it and must not invent a second dedup mechanism (D-25).
    - `lib/mix/tasks/crosswake.gen.native_controls_ui.ex` — `@template_version`, `stamp_prefix/0`, and the printed next-steps heredoc to extend with the menu's clauses.
    - `test/crosswake/proof/phase155_native_controls_template_drift_test.exs` — `@checked_in_hash`, which must be recomputed in this same commit.
    - `examples/phoenix_host/lib/crosswake_example/saas_portal/approval_live.ex` — the wiring landed by 155-01, to extend with the menu trigger and the destructive route-through.
  </read_first>
  <behavior>
    - `action_menu/1` with zero actions renders the empty-state row inside the normal chassis, with heading and Cancel present.
    - One through seven actions render without engaging the scroll container; eight or more engage it.
    - A row with `destructive: true` renders last, behind the gap band, with the left bar, and its click opens the destructive confirm rather than emitting the mutation.
    - A row marked unavailable renders disabled with its reason inline, not omitted and not silently greyed.
    - `@pending` renders on the trigger only; no row renders a pending state.
    - Selecting a row emits select-with-id; dismissing emits dismiss; there is no third outcome.
    - Both handlers call `Crosswake.Bridge.resolve/2` exactly once and no `Bridge.push/3` call exists anywhere in the template.
    - Running the generator produces files stamped `template_version=2`.
    - The drift test passes against the recomputed hash and fails against the previous one.
  </behavior>
  <action>
Add `action_menu/1` to the component template, reusing the same panel chassis as
`confirm_modal/1` (D-15 — one object). Attrs: `id` (required), `open` (default false),
`heading` (`:string`, default `Choose an action`), `actions` (`:list`, required) in the shape
frozen by Task 1's checkpoint, `pending` (default false), `trigger_id` (`:string`, required for
the `aria-controls` pairing).

Structure per D-20: a mandatory `<h2>` heading; a `<ul>` of `<li>` each containing a
`<button>`; NO icons — shape, colour, and text carry all signal. Row cap: the list scrolls
inside `max-height: min(60vh, 420px)` with the heading and the Cancel button OUTSIDE the
scroll container, so both remain reachable at any row count. The destructive row renders last,
separated by a 4px gap band, carrying a 3px left bar. A destructive row's click sets the
destructive confirm's open assign — it never emits the mutation directly. A row that is present
but not permitted renders as a disabled button carrying its reason inline using the
copywriting contract's disabled-row string form. Zero actions renders the empty-state string
inside the normal chassis. Pending renders on the trigger only.

ARIA per D-21: do NOT declare the APG menu role. That contract obliges arrow keys, Home/End,
Tab-closes, and roving tabindex, which cannot be honored without host JavaScript — and
declaring it unimplemented is worse than never claiming it. These are buttons in a dialog. The
trigger takes `aria-expanded` and `aria-controls`, not `aria-haspopup`. Record in the moduledoc
that a native action sheet is exposed to assistive technology as an alert-with-buttons anyway,
so this is not a downgrade.

Layout per D-22: a centred modal at every viewport in this phase, not a trigger-anchored
dropdown. An unanchored dropdown can render off-screen, which is a worse first impression than
slight heaviness; anchoring is deferred to SEED-005 where the elevation and z tokens it needs
will exist.

Handlers: the generated `crosswake_fallback_answer` and `crosswake_fallback_dismiss` clauses
call `Crosswake.Bridge.resolve(socket, ref)` exactly once each. Do NOT invent a second dedup
mechanism and do NOT route a native reply and a fallback click into the same event name — that
guarantees a double mutation, and `resolve/2`'s compare-and-delete is the only mechanism
(D-25). This phase issues no `Bridge.push/3` call and adds no bridge command: there is no menu
capability, `Contract.commands/0` holds none, the catalog guard makes adding one a merge-
blocking enum gap, and the policy validator would kill the route at mount (D-01).

Add the D-55 hand-off block: an inert comment inside the generated `action_menu` source itself
stating what Phase 156 adds (the catalog entry, both native enum cases simultaneously, the
registry mapping, the transport choice), that it lands additively with no breaking change to
this file, and that an adopter who never takes Phase 156 keeps a working menu forever. The
generator has no force flag, so a comment in the file the adopter owns is the only upgrade
instruction that survives.

Also record in the moduledoc, as a manual-check limitation and never as a passing assertion:
`aria-modal="true"` is unreliable in Safari with VoiceOver, and the server-rendered `inert`
background attribute is believed but not proven to mitigate it (D-23).

Extend the generator's printed next-steps heredoc with the menu's clauses so the pasted block
covers both surfaces. Bump `@template_version` from 1 to 2 and recompute
`@checked_in_hash` in `test/crosswake/proof/phase155_native_controls_template_drift_test.exs`
IN THIS SAME COMMIT — that is the mechanism working, not a chore. Extend the generator test
with the menu-specific printed-output assertions.

Regenerate the committed reference-host output by deleting the two generated files and re-
running `mix crosswake.gen.native_controls_ui --app CrosswakeExample --dir examples/phoenix_host`
(the generator never clobbers, so a delete-then-generate is the correct refresh, and it also
exercises the no-clobber path honestly). Wire the menu into `approval_live.ex`: a trigger
button carrying `aria-expanded`/`aria-controls`, a `:menu_open` assign, an `actions` list
containing at least one neutral and one destructive row and one disabled row, and the
destructive row routing into the destructive confirm.
  </action>
  <verify>
    <automated>mix test test/mix/tasks/crosswake.gen.native_controls_ui_test.exs test/crosswake/proof/phase155_native_controls_template_drift_test.exs --warnings-as-errors</automated>
    <automated>cd examples/phoenix_host && mix compile --warnings-as-errors && npx playwright test e2e/native_controls_fallback.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'def action_menu' priv/templates/crosswake/native_controls_ui/crosswake_fallbacks.ex.eex` is 1.
    - `grep -c 'role="menu"' priv/templates/crosswake/native_controls_ui/crosswake_fallbacks.ex.eex` is 0 and `grep -c 'aria-haspopup' priv/templates/crosswake/native_controls_ui/crosswake_fallbacks.ex.eex` is 0 (D-21).
    - `grep -c 'aria-expanded' priv/templates/crosswake/native_controls_ui/crosswake_fallbacks.ex.eex` is at least 1 and the same for `aria-controls`.
    - `grep -c 'Bridge.push' priv/templates/crosswake/native_controls_ui/crosswake_fallbacks.ex.eex` is 0 (D-01).
    - `grep -c 'Crosswake.Bridge.resolve' priv/templates/crosswake/native_controls_ui/crosswake_fallbacks.ex.eex` is exactly 2 — one per handler, no more.
    - `grep -c 'max-height: min(60vh, 420px)' priv/templates/crosswake/native_controls_ui/crosswake_fallback.css.eex` is at least 1.
    - `grep -c 'Choose an action' priv/templates/crosswake/native_controls_ui/crosswake_fallbacks.ex.eex` is 1 and `grep -c 'No actions are available for this record' priv/templates/crosswake/native_controls_ui/crosswake_fallbacks.ex.eex` is 1.
    - `grep -ci 'phase 156' priv/templates/crosswake/native_controls_ui/crosswake_fallbacks.ex.eex` is at least 1 — the D-55 hand-off block is inside the template that becomes the adopter-owned file.
    - `grep -c 'aria-modal' priv/templates/crosswake/native_controls_ui/crosswake_fallbacks.ex.eex` is at least 1 AND the moduledoc records the VoiceOver limitation as unproven (D-23).
    - `grep -c '@template_version 2' lib/mix/tasks/crosswake.gen.native_controls_ui.ex` is 1.
    - `head -1 examples/phoenix_host/lib/crosswake_example_web/components/crosswake_fallbacks.ex` contains `template_version=2`.
    - `mix test test/crosswake/proof/phase155_native_controls_template_drift_test.exs` exits 0 with the recomputed hash; reverting the hash to its 155-01 value makes it exit non-zero (record the observed failure — this proves the bump mechanism has teeth).
    - `cd examples/phoenix_host && mix compile --warnings-as-errors` exits 0 — no unused alias or variable in the regenerated output or the extended LiveView.
    - `cd examples/phoenix_host && npx playwright test e2e/native_controls_fallback.spec.ts` exits 0 — the 155-01 A1 assertion still passes against the regenerated artifact.
    - `mix run -e 'Crosswake.ComponentTierGuard.assert_no_component_tier!()'` exits 0 — the expanded templates still satisfy the anti-vacuity twin and nothing UI-shaped leaked into `lib/`.
  </acceptance_criteria>
  <done>Both surfaces are complete: two confirm tones, an action menu with the frozen `actions` shape, no APG menu role, no bridge push, one `resolve/2` call per handler, the Phase-156 hand-off comment inside the adopter-owned file, a bumped stamp, and a recomputed drift hash — with the bump mechanism observed working.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → LiveView `handle_event` | menu selection and confirm answer payloads are attacker-controlled |
| generated component → adopter mutation logic | the component translates an event into a call the adopter's own authorization gates |
| ARIA declarations → assistive technology | a declared role is a promise to an AT user |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-155-21 | Tampering | forged menu-selection payload bypassing the destructive confirm | high | mitigate | A destructive row's click sets the confirm's open assign and emits no mutation; the mutation only ever leaves the confirm's answer handler, which pattern-matches a literal expected answer value. A forged selection cannot reach the mutation without also forging a matching confirm answer, and the adopter's own authorization still gates the call (ASVS V5) |
| T-155-22 | Tampering | double mutation from a native reply racing a fallback click | high | mitigate | Exactly one `Crosswake.Bridge.resolve/2` call per handler and no second dedup mechanism; `resolve/2`'s compare-and-delete against `state.in_flight` is the only path (D-25). Asserted by an exact-count grep of 2 |
| T-155-23 | Spoofing | claiming an ARIA contract the implementation does not honor | medium | mitigate | The APG menu role is never declared; the trigger uses `aria-expanded` plus `aria-controls`, and the moduledoc records the reasoning so a later contributor does not "improve" it back (D-21) |
| T-155-24 | Information Disclosure | internal vocabulary leaking into end-user copy | medium | mitigate | Every `~H`-block string literal is scanned for the banned internal terms and the empty result is recorded in the summary (D-09) |
| T-155-25 | Repudiation | a template change landing without a version bump, making adopter drift undetectable | medium | mitigate | `@template_version` bumped to 2 and `@checked_in_hash` recomputed in the same commit, with the pre-bump hash observed failing |
| T-155-SC | Tampering | npm/pip/cargo installs | high | accept | Zero package-manager installs in this plan and in this phase (RESEARCH.md § Package Legitimacy Audit: not applicable) |
</threat_model>

<verification>
- `mix test test/mix/tasks/crosswake.gen.native_controls_ui_test.exs test/crosswake/proof/phase155_native_controls_template_drift_test.exs --warnings-as-errors`
- `mix run -e 'Crosswake.ComponentTierGuard.assert_no_component_tier!()'`
- `mix test --warnings-as-errors` (full core suite)
- `cd examples/phoenix_host && mix compile --warnings-as-errors && npx playwright test` (unfiltered)
- `node brandbook/tools/check-consumer-drift.mjs`
- Drift-hash mutation control observed red and recorded.
</verification>

<success_criteria>
- Two confirm tones and one action menu, complete, with no deferred sub-feature and no reduced scope.
- No APG menu role, no `Bridge.push/3`, no second dedup mechanism.
- The `actions` shape is frozen by an explicit human decision and carries the Phase-156 hand-off comment.
- The template-version bump mechanism was observed working.
</success_criteria>

<output>
Create `.planning/phases/155-host-owned-fallback-components/155-06-SUMMARY.md` when done.
</output>
