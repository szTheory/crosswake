---
phase: 156-native-menu-action-button-control
plan: 03
type: execute
wave: 3
depends_on: [156-02]
files_modified:
  - packages/crosswake-shell-core-ios/Sources/CrosswakeShellCore/BridgeChannel.swift
  - packages/crosswake-shell-core-ios/Sources/CrosswakeShellCore/ActionMenuPresenter.swift
  - packages/crosswake-shell-core-ios/Tests/CrosswakeShellCoreTests/BridgeConformanceTests.swift
  - packages/crosswake-shell-core-ios/Tests/CrosswakeShellCoreTests/ActionMenuPresenterTests.swift
autonomous: true
requirements: [MENU-02, MENU-03, PROOF-03]
must_haves:
  truths:
    - "iOS renders D-17's real UIAlertController action sheet, using a mandatory sourceView/sourceRect on iPad, and returns exactly one D-26 selected or dismissed outcome."
    - "Disabled explanatory rows remain visible and disabled; destructive actions sort last and use UIKit destructive treatment without gaining mutation authority."
    - "Hostless tests prove labels, enabled state, destructive semantics, anchor refusal, selection, and dismissal without claiming VoiceOver speech or final pixels."
  artifacts:
    - "packages/crosswake-shell-core-ios/Sources/CrosswakeShellCore/ActionMenuPresenter.swift contains the bounded presenter protocol/model and UIKit adapter."
    - "packages/crosswake-shell-core-ios/Tests/CrosswakeShellCoreTests/ActionMenuPresenterTests.swift proves presenter semantics hostlessly."
  key_links:
    - "BridgeChannel action_menu.present -> ActionMenuPresenter -> one completion authority -> BridgeReplyEnvelope"
    - "WebView anchor rectangle -> iPad popover sourceView/sourceRect"
  prohibitions:
    - statement: "MUST NOT present an iPad action sheet without a valid sourceView and sourceRect."
      status: resolved
      verification: test
    - statement: "MUST NOT claim hostless source/vector tests prove real VoiceOver speech, device interaction, or final pixels."
      status: resolved
      verification: judgment
---

<objective>
Implement the production iOS `action_menu` presenter and fake-presenter harness behind the contract 1.2.0 bridge.

Purpose: Deliver the iOS half of MENU-02/MENU-03 with crash-safe anchoring and honest semantic proof.
Output: Structured JSON decoding, bounded presenter protocol, UIKit action-sheet adapter, and hostless selected/dismissed tests.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/156-native-menu-action-button-control/156-CONTEXT.md
@.planning/phases/156-native-menu-action-button-control/156-RESEARCH.md
@.planning/phases/156-native-menu-action-button-control/156-PATTERNS.md
@.planning/phases/156-native-menu-action-button-control/156-UI-SPEC.md
@.planning/phases/156-native-menu-action-button-control/156-02-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Decode the shared action projection and drive a fake iOS presenter exactly once</name>
  <files>packages/crosswake-shell-core-ios/Sources/CrosswakeShellCore/BridgeChannel.swift, packages/crosswake-shell-core-ios/Sources/CrosswakeShellCore/ActionMenuPresenter.swift, packages/crosswake-shell-core-ios/Tests/CrosswakeShellCoreTests/BridgeConformanceTests.swift, packages/crosswake-shell-core-ios/Tests/CrosswakeShellCoreTests/ActionMenuPresenterTests.swift</files>
  <read_first>
    - packages/crosswake-shell-core-ios/Sources/CrosswakeShellCore/BridgeChannel.swift
    - packages/crosswake-shell-core-ios/Tests/CrosswakeShellCoreTests/BridgeConformanceTests.swift
    - .planning/phases/156-native-menu-action-button-control/156-PATTERNS.md
    - .planning/phases/156-native-menu-action-button-control/156-UI-SPEC.md
  </read_first>
  <behavior>
    - "D-31: Swift decodes structured JSON arrays/objects while existing scalar payloads remain valid."
    - "D-22/D-23: fake presenter records identical labels/enabled/destructive/order projection, keeps nil-id rows disabled, and never mutates."
    - "D-26/D-27: fake selection and dismissal each complete once; duplicate callback and unknown/disabled selection cannot reach adopter delivery."
  </behavior>
  <action>Start with failing Swift tests and follow PATTERNS.md's exact `BridgeCommand`/`BridgeRequestEnvelope` dispatch analog plus the `FilesPickHandler` deferred-callback and delegate/presenter seam in `BridgeChannel.swift`. Widen the envelope payload/reply representation to a Codable structured JSON value, add `.actionMenuPresent` mapped to family `action_menu`, and introduce a bounded private presenter protocol plus immutable presentation model. Decode only the already-validated shared projection and D-06 anchor metadata; preserve nil-id rows and destructive-last ordering per D-14/D-22/D-23. Route fake presenter selection/dismissal through one guarded completion authority returning D-26 payloads; treat presenter callbacks and selected ids as untrusted per D-27. Keep native selection side-effect-free and do not add confirmation or mutation authority.</action>
  <acceptance_criteria>The fake presenter records the shared projection, selected and dismissed paths each reply once, disabled/unknown selections fail closed, and existing structured/scalar bridge tests remain green.</acceptance_criteria>
  <done>The iOS fake-presenter contract satisfies the acceptance criteria and Swift tests pass.</done>
  <verify>
    <automated>swift test --package-path packages/crosswake-shell-core-ios --filter 'ActionMenuPresenterTests|BridgeConformanceTests'</automated>
  </verify>
  <reversibility rating="costly">Implements D-26/D-31 after the blocking one-way publication checkpoint in 156-01.</reversibility>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Present real UIKit action-sheet chrome with crash-safe iPad anchoring</name>
  <files>packages/crosswake-shell-core-ios/Sources/CrosswakeShellCore/ActionMenuPresenter.swift, packages/crosswake-shell-core-ios/Tests/CrosswakeShellCoreTests/ActionMenuPresenterTests.swift</files>
  <read_first>
    - packages/crosswake-shell-core-ios/Sources/CrosswakeShellCore/ActionMenuPresenter.swift
    - packages/crosswake-shell-core-ios/Tests/CrosswakeShellCoreTests/ActionMenuPresenterTests.swift
    - .planning/phases/156-native-menu-action-button-control/156-UI-SPEC.md
  </read_first>
  <behavior>
    - "D-17/D-19: UIAlertController.Style.actionSheet is used; iPad refuses presentation unless sourceView and sourceRect are valid."
    - "D-21/D-22: localized labels are visible/spoken names, nil-id rows remain disabled, and native cancel/tap-outside maps to dismissed."
    - "D-23: destructive items use UIAlertAction destructive style, sort last, and return an id without mutation."
  </behavior>
  <action>Implement the real UIKit adapter behind the tested presenter protocol using `UIAlertController.Style.actionSheet`. Map validated WebView coordinates to the bounded host view; require a valid popover `sourceView` and `sourceRect` on iPad before presentation per D-19. Use system action labels, enabled state, destructive treatment, focus/text scaling/appearance, and cancellation semantics per D-17/D-21..D-23. Deny before UIKit presentation on invalid mapping. Keep platform divergence intentional per D-20 and add no custom pixel-parity layer or icon vocabulary per D-24.</action>
  <acceptance_criteria>The production adapter calls UIKit action-sheet APIs, refuses unanchored iPad presentation, represents all rows semantically, and maps every terminal user path to one selected or dismissed callback.</acceptance_criteria>
  <done>The UIKit adapter satisfies the acceptance criteria and the full SwiftPM suite passes.</done>
  <verify>
    <automated>swift test --package-path packages/crosswake-shell-core-ios</automated>
  </verify>
  <reversibility rating="reversible">The private UIKit presenter adapter can evolve behind the stable semantic contract.</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| JSON envelope -> Swift model | Untrusted structured request data enters native decoding. |
| WebView coordinates -> UIKit | Client geometry is mapped into iPad popover presentation. |
| UIKit callback -> bridge reply | User selection becomes untrusted action-id data. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-156-I1 | Tampering | Swift action projection | high | mitigate | Decode bounded typed rows and reject malformed, duplicate, unknown, or disabled selections. |
| T-156-I2 | Denial of Service | iPad popover | high | mitigate | Require valid sourceView/sourceRect and deny before UIKit presentation. |
| T-156-I3 | Elevation of Privilege | presenter callback | high | mitigate | Native returns ids only; core and Phoenix reauthorize before mutation. |
| T-156-I4 | Repudiation | duplicate callbacks | medium | mitigate | One guarded completion authority records exactly one outcome. |

ASVS Level 1: unresolved high-severity threats block plan completion. T-156-I1 and T-156-I3 are enforced by Task 1's malformed/unknown/disabled and exactly-once acceptance plus filtered Swift tests; T-156-I2 is enforced by Task 2's refusal-before-presentation acceptance and full SwiftPM command.
</threat_model>

<verification>
Run the full SwiftPM suite; source checks establish adapter wiring only, while device VoiceOver/visual evidence remains advisory.
</verification>

<success_criteria>
iOS presents a real, anchored, semantic system action sheet and returns exactly one typed selected/dismissed reply through a fake-testable presenter boundary.
</success_criteria>

## Artifacts this phase produces

- Swift enum case `BridgeCommand.actionMenuPresent`.
- Swift structured JSON value type used by `BridgeRequestEnvelope` and `BridgeReplyEnvelope`.
- New path `Sources/CrosswakeShellCore/ActionMenuPresenter.swift`.
- Private Swift action-menu presentation model, presenter protocol, fake presenter, and UIKit adapter.
- New test path `Tests/CrosswakeShellCoreTests/ActionMenuPresenterTests.swift`.

<output>
Create `.planning/phases/156-native-menu-action-button-control/156-03-SUMMARY.md` when done.
</output>
