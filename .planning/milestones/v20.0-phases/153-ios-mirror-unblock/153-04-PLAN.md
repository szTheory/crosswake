---
phase: 153-ios-mirror-unblock
plan: 04
type: execute
wave: 4
depends_on: ["153-03"]
files_modified:
  - script/check_ios_mirror_parity.sh
  - .github/workflows/merge-blocking-ios-mirror-parity.yml
  - lib/crosswake/release_status.ex
  - test/mix/tasks/crosswake_release_status_test.exs
  - test/crosswake/proof/phase153_ios_mirror_unblock_test.exs
autonomous: false
requirements: [MIRROR-01, MIRROR-02]
user_setup:
  - service: github
    why: "Register the new merge-blocking parity lane as a required status check on branch protection. Can only happen AFTER the lane has gone green on main at least once — this is the repo's existing two-step 'merge, then register' ritual, not something phase execution can complete."
    dashboard_config:
      - task: "Register the new required check"
        location: "`DRY_RUN=0 script/register_required_checks.sh` (repo-admin rights required)"

must_haves:
  truths:
    - "D-16: for every `refs/tags/ios-core-vX` in this repo, `refs/tags/vX` must exist on szTheory/crosswake-shell-core-ios — and a violation turns the merge button red, not a post-merge Actions run nobody watches."
    - "D-16: the gate keys on RELEASED `ios-core-v*` git tags, never on `.release-please-manifest.json` — keying on the manifest would make the Release PR block itself, permanently."
    - "D-18: `mix crosswake.release.status --live` returns `:error` (exit 1) on a missing mirror tag, not `:warning` (exit 0) — the project's own release-truth command can no longer report success while every iOS adopter is broken."
    - "D-18: `:missing` (registry answered, tag absent — a definite negative) is reported under `release.live_registry_presence`, and `:unavailable` (probe failed after 3 retries — an unknown, not a negative) under the distinct code `release.live_registry_unverifiable`. Both exit 1; the message never misreports WHICH failure occurred."
    - "D-19: failure microcopy matches the existing `[crosswake] FAIL: <id> - <detail>` parser shape and states adopter impact in adopter language, with one command to fix."
    - "D-20: the parity invariant has decoy tests proving it is not vacuous."
  artifacts:
    - script/check_ios_mirror_parity.sh
    - .github/workflows/merge-blocking-ios-mirror-parity.yml
    - lib/crosswake/release_status.ex
    - test/crosswake/proof/phase153_ios_mirror_unblock_test.exs
  key_links:
    - "local `ios-core-v*` tags -> check_ios_mirror_parity.sh -> `git ls-remote --tags` (unauthenticated) -> merge-blocking required check"
    - "job name substring `merge-blocking` -> script/list_merge_blocking_checks.py auto-discovery -> register_required_checks.sh -> branch protection"
    - "probe `:missing`/`:unavailable` -> live_registry_checks/2 -> distinct :error codes -> exit_code/1 -> exit 1"
---

<objective>
Build the two durable guards that would have caught what nothing else could: a merge-blocking parity gate that compares the mirror's ACTUAL remote state against this repo's released tags on every PR (D-16), and an honest release-truth CLI that stops exiting 0 while adopters cannot resolve the package (D-18).

Purpose: **MIRROR-02's standing half.** A structural scanner check could have caught fuse #1 offline at any point in the last three months. Nothing offline could have caught fuse #2 — it required comparing the mirror's real remote state against locally-computable output. That is exactly what the parity gate now does, on every PR rather than only at release time. It also catches drift a release-time guard structurally cannot see: a manual force-push, a tag deletion, a half-finished backfill.

Output: red that lands on the merge button (unignorable, fail-closed, cannot be scrolled past), and a `mix crosswake.release.status --live` that tells the truth.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/153-ios-mirror-unblock/153-CONTEXT.md
@.planning/phases/153-ios-mirror-unblock/153-RESEARCH.md
@.planning/phases/153-ios-mirror-unblock/153-PATTERNS.md
@.planning/phases/153-ios-mirror-unblock/153-02-SUMMARY.md
@.planning/phases/153-ios-mirror-unblock/153-03-SUMMARY.md
</context>

<artifacts_this_phase_produces>
| Symbol | Kind | Location |
|---|---|---|
| `script/check_ios_mirror_parity.sh` | NEW script | repo root `script/` |
| `.github/workflows/merge-blocking-ios-mirror-parity.yml` | NEW workflow | `.github/workflows/` |
| `merge-blocking-ios-mirror-parity` | NEW required-check context (job id AND `name:`; the `merge-blocking` substring is what `list_merge_blocking_checks.py` auto-discovers) | branch protection |
| `release.ios_mirror_parity` | NEW failure-code string | `script/check_ios_mirror_parity.sh` |
| `release.live_registry_unverifiable` | NEW failure-code string (D-18 — the `:unavailable` split) | `lib/crosswake/release_status.ex` |
| `release.live_registry_presence` | EXISTING code, semantics narrowed to definite negatives only, status raised `:warning` → `:error` | `lib/crosswake/release_status.ex` |
| `CROSSWAKE_IOS_PARITY_MIRROR_REMOTE`, `CROSSWAKE_IOS_PARITY_RELEASE_REPO` | NEW env-var test seams | `script/check_ios_mirror_parity.sh` |
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: The merge-blocking iOS mirror parity gate (D-16, D-19)</name>

  <read_first>
    - `.github/workflows/release-as-staleness-gate.yml` (READ IN FULL, 57 lines — `merge-blocking-release-as-staleness` is the EXACT structural precedent: a state-vs-config tripwire as a required check, one thin job that IS the check, delegating to a script)
    - `script/list_merge_blocking_checks.py` (READ IN FULL — auto-discovery is a case-insensitive substring match on `merge-blocking` in the job's `name:` field, falling back to the job id; names containing an unresolved `${{ ... }}` expression are SKIPPED and can never become a required check)
    - `script/register_required_checks.sh` (READ IN FULL — its green-first preflight only registers a lane after it has gone green at least once on `main`)
    - `script/verify_ios_mirror_backfill.sh` (the house bash style: `set -euo pipefail`, the `[crosswake]` `log`/`ok`/`fail` helpers, the `CROSSWAKE_IOS_BACKFILL_*` env-seam pattern to mirror)
    - `.planning/phases/153-ios-mirror-unblock/153-PATTERNS.md` §`script/check_ios_mirror_parity.sh` (NEW) — contains the copy-pasteable retry loop, the `comm -23` invariant implementation, and the exact D-19 failure microcopy
  </read_first>

  <files>script/check_ios_mirror_parity.sh, .github/workflows/merge-blocking-ios-mirror-parity.yml, test/crosswake/proof/phase153_ios_mirror_unblock_test.exs</files>

  <behavior>
    Write the fixture tests FIRST, in `phase153_ios_mirror_unblock_test.exs`, using the bare-repo harness plan 01 established and the `CROSSWAKE_IOS_PARITY_*` env seams:

    - Parity holds: local `ios-core-v0.1.2` + `ios-core-v0.2.0`; fixture mirror has `v0.1.2` + `v0.2.0` → script exits 0.
    - Parity violated: local has `ios-core-v0.2.0`; fixture mirror has only `v0.1.2` → script exits 1, and stdout contains a `[crosswake] FAIL: release.ios_mirror_parity - ` line naming `0.2.0`, the adopter impact, and one `gh workflow run ios-mirror-backfill.yml` command.
    - Extra mirror tags are NOT a violation: mirror has a `v9.9.9` this repo never released → exits 0. The invariant is one-directional.
    - Network flake: an unreachable remote → script retries 3 times, then fails with a message that names unreachability, NOT a missing tag. (An unknown must never be reported as a definite negative — that is the same class of lie D-18 fixes in the Elixir path.)
  </behavior>

  <action>
    **(a) `script/check_ios_mirror_parity.sh`.** Copy the house bash style verbatim from `verify_ios_mirror_backfill.sh`: `set -euo pipefail`, `log()`, `ok()`, `fail()` with a `next_action` second argument.

    Add the env seams so the tests can inject a fake mirror, mirroring the `CROSSWAKE_IOS_BACKFILL_*` pattern:
    - `MIRROR_REMOTE="${CROSSWAKE_IOS_PARITY_MIRROR_REMOTE:-https://github.com/szTheory/crosswake-shell-core-ios.git}"`
    - `RELEASE_REPO="${CROSSWAKE_IOS_PARITY_RELEASE_REPO:-$REPO_ROOT}"`

    Read the mirror's tags with an UNAUTHENTICATED probe — no credential is needed and none should be used; the mirror is public and this lane must stay hermetic and runnable by any contributor:

    ```
    git -c core.askPass= ls-remote --tags "$MIRROR_REMOTE"
    ```

    Wrap it in a 3-attempt retry loop (D-16, network flake), adapting the `for i in 1 2 3; do ... sleep; done` shape already used in `release-please.yml`'s `clean-room-proof-ios` swift-build retry. On exhaustion, `fail` with a message that names UNREACHABILITY explicitly — never let a network failure masquerade as a missing tag.

    **The invariant, in one sentence:** for every `refs/tags/ios-core-vX` in this repo, `refs/tags/vX` must exist on `szTheory/crosswake-shell-core-ios`. Implement it as a set difference: enumerate local versions with `git -C "$RELEASE_REPO" tag --list 'ios-core-v*'` stripped of the prefix, enumerate mirror versions from the `ls-remote` output stripped of `refs/tags/v`, and `comm -23` the sorted sets.

    ⚠️ **DEADLOCK TRAP — key on released `ios-core-v*` git tags, NEVER on `.release-please-manifest.json`.** The manifest bumps to X in the Release PR, BEFORE the tag and the publish exist — so a manifest-keyed gate would ask "does the mirror have vX?" on the Release PR's own commit, before X is released anywhere, and block that PR forever. Keying on released tags is deadlock-free: on the Release PR commit no new `ios-core-v*` tag exists yet, so the gate re-validates the PREVIOUS release's parity (already green), and the new tag only appears after merge — exactly when `publish-ios-core` pushes the matching mirror tag.

    Ship tag-EXISTENCE only. Do NOT add a `--deep` splitsh-SHA-identity mode in v1: it would require running `splitsh-lite` on every PR, destroying the sub-second hermetic property that makes this lane safely merge-blocking. The stated invariant says nothing about SHA identity. Leave `--deep` as a documented future flag.

    **(b) D-19 microcopy.** The failure output must match the existing `[crosswake] FAIL: <id> - <detail>` parser shape (a no-whitespace id token, a literal ` - ` separator), so the script is machine-consumable by `mix crosswake.release.status` for free. Note the house `fail()` helper does NOT prepend an id — so pass the id as part of the message. Emit exactly this shape, substituting the real versions:

    ```
    [crosswake] FAIL: release.ios_mirror_parity - SwiftPM mirror is missing refs/tags/v0.2.0.
    [crosswake]   released here:  refs/tags/ios-core-v0.2.0
    [crosswake]   mirror state:   szTheory/crosswake-shell-core-ios has no refs/tags/v0.2.0
    [crosswake]   adopter impact: .package(url: "…/crosswake-shell-core-ios", from: "0.2.0")
    [crosswake]                   CANNOT RESOLVE. Every iOS adopter of 0.2.0 is broken right now.
    [crosswake] What to do next:
    [crosswake]   gh workflow run ios-mirror-backfill.yml -f version=0.2.0 \
    [crosswake]     -f release_ref=refs/tags/ios-core-v0.2.0 -f apply=true
    [crosswake] This gate stays RED and merges stay BLOCKED until the mirror tag exists.
    ```

    The reader is the maintainer at 2am. State the stakes in adopter language, not internal job names, and give ONE command.

    `chmod +x script/check_ios_mirror_parity.sh`.

    **(c) `.github/workflows/merge-blocking-ios-mirror-parity.yml`.** Copy `release-as-staleness-gate.yml`'s topology: `on: push (branches '**') + pull_request (branches: [main])`, `permissions: contents: read`, a concurrency group, ONE job that IS the check, and an `if: always()` step-summary echo.

    ⚠️ Naming contract: use `merge-blocking-ios-mirror-parity` as BOTH the YAML job key AND the literal `name:` field — matching the precedent exactly. The `merge-blocking` substring is what `list_merge_blocking_checks.py` auto-discovers, and the literal string printed becomes the required-check context. Never put a `${{ ... }}` expression in `name:` — those are explicitly skipped and the lane could never become a required check.

    Checkout needs `fetch-depth: 0` AND `fetch-tags: true` — the LOCAL side of the comparison enumerates `ios-core-v*` tags, which a shallow clone will not have. The remote side needs no fetch and no credential.

    One deviation from the precedent: SHA-pin `actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0`. The precedent's `@v7` tag-pin is a known minor inconsistency in that one file; this repo's dominant discipline is SHA + version comment. Do not copy that detail.

    This lane is hermetic on ubuntu — ONE unauthenticated network call, sub-second — so it does not violate the v15 COLL-01 iOS-advisory precedent. That precedent is about Xcode/macOS runner instability, not about iOS-shaped concerns.
  </action>

  <verify>
    <automated>bash -n script/check_ios_mirror_parity.sh && python3 script/list_merge_blocking_checks.py | grep merge-blocking-ios-mirror-parity && mix test --only phase153_ios_mirror_unblock</automated>
  </verify>

  <acceptance_criteria>
    - `bash -n script/check_ios_mirror_parity.sh` exits 0 and the file is executable (`test -x`).
    - `python3 script/list_merge_blocking_checks.py` output includes the literal context `merge-blocking-ios-mirror-parity` (auto-discovery works — the naming contract is satisfied).
    - `.github/workflows/merge-blocking-ios-mirror-parity.yml` parses as valid YAML; its checkout carries BOTH `fetch-depth: 0` and `fetch-tags: true`; its `actions/checkout` is SHA-pinned.
    - `grep -c 'release-please-manifest' script/check_ios_mirror_parity.sh` returns 0 — the deadlock trap is structurally avoided, not merely commented around.
    - `grep -c 'ios-core-v' script/check_ios_mirror_parity.sh` returns ≥ 1 (the gate keys on released tags).
    - `grep -c 'release.ios_mirror_parity - ' script/check_ios_mirror_parity.sh` returns ≥ 1 (parser-compatible id + ` - ` separator, D-19).
    - `grep -c 'CANNOT RESOLVE' script/check_ios_mirror_parity.sh` returns ≥ 1 (adopter-language impact).
    - Run against the real mirror right now: `script/check_ios_mirror_parity.sh` exits 0 (plan 02 landed `v0.2.0`, so the invariant holds). If it exits 1, plan 02 did not complete — stop.
    - `mix test --only phase153_ios_mirror_unblock` green, including the four new parity fixture tests from `<behavior>` (parity-holds, parity-violated, extra-mirror-tags-ok, network-flake-is-not-a-missing-tag).
  </acceptance_criteria>

  <done>
    A drift between this repo's released iOS tags and the mirror's tags turns the merge button red. The failure message tells a 2am maintainer exactly what is broken for adopters and gives one command to fix it.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Release-truth honesty — split `:missing` from `:unavailable`, both exit 1 (D-18)</name>

  <read_first>
    - `lib/crosswake/release_status.ex` lines 440-540 (`live_registry_checks/2` at 462-491 — the bug is entirely in this aggregation function), lines 620-690 (`exit_code/1` at 626-628), lines 760-840 (`maybe_ios_mirror_live/3` at 772-787 and `git_ref_live_probe/2` at 817-838)
    - `test/mix/tasks/crosswake_release_status_test.exs` (⚠️ THIS is the real path — RESEARCH guessed `test/crosswake/release_status_test.exs`, which does not exist; PATTERNS corrected it. Read lines 140-210: there is ALREADY a complete test with a `probes`-map injection seam covering `:ok`, `:missing`, and `:unavailable` distinctly per-source, named "live probes distinguish ok, missing, and unavailable as advisory warnings". Extend this file in place; do not create a new one.)
    - `.planning/phases/153-ios-mirror-unblock/153-RESEARCH.md` §Q6 (contains the complete, mechanically-derived replacement body for `live_registry_checks/2` — copy it verbatim)
  </read_first>

  <files>lib/crosswake/release_status.ex, test/mix/tasks/crosswake_release_status_test.exs</files>

  <behavior>
    Update the existing test first — its name and its assertions both encode the bug.

    - The existing test asserts `status.status == :warning`. That assertion must flip to `:error`. Rename the test so it no longer calls these "advisory warnings".
    - A `:missing` probe result (registry answered; ref/release absent — a DEFINITE negative) produces a check with code `release.live_registry_presence` and status `:error`.
    - An `:unavailable` probe result (the probe itself failed — an UNKNOWN, not a negative) produces a check with the DISTINCT code `release.live_registry_unverifiable` and status `:error`.
    - Both exit 1: assert `Crosswake.ReleaseStatus.exit_code/1` returns 1 for each. (Fail-closed on unknowns — but the message must never misreport WHICH failure occurred.)
    - When both kinds are present, BOTH checks appear, each naming only its own sources. The existing fixture already produces exactly this mix (`maven -> :missing`, `hex -> :unavailable`, `git_ref -> :missing`), so this is a direct extension.
    - All-`:ok` still produces a single `:ok` `release.live_registry_presence` check and exit 0.
    - A probe that fails twice and then succeeds is NOT `:unavailable` — the retry short-circuits on the first real answer.
  </behavior>

  <action>
    **(a) `live_registry_checks/2` (lines 462-491).** The per-probe layer ALREADY distinguishes `:ok | :missing | :unavailable` correctly — `normalize_live_status/1` accepts all three and both concrete probes emit them as distinct statuses. The entire bug is in the aggregation: it does `missing = Enum.reject(entries, &(&1.live.status == :ok))`, which LUMPS `:missing` and `:unavailable` together, then reports `status: :warning` under one code. And `exit_code/1` maps only `:error` to 1 — so `:warning` exits 0.

    Net effect today: `mix crosswake.release.status --live` exits 0 while every iOS adopter's `.package(from: "0.2.0")` cannot resolve. That is a support-truth lie in the project's own voice, and it violates the OSS DNA's fail-closed / no-silent-fallback rule.

    Replace the function body with RESEARCH §Q6's complete replacement: filter `:missing` and `:unavailable` SEPARATELY into two lists; emit a `release.live_registry_presence` check with `status: :error` when `:missing` is non-empty (message: probes found no release for these sources); emit a `release.live_registry_unverifiable` check with `status: :error` when `:unavailable` is non-empty (message must say the probe FAILED — a probe failure, not a confirmed absence); emit the existing `:ok` `release.live_registry_presence` check only when BOTH lists are empty.

    **`exit_code/1` needs NO change.** Both new codes already carry `status: :error`, which already maps to exit 1. Do not touch it.

    **(b) Retry belongs in the PROBE layer, not the aggregation.** Add a 3-attempt loop with a short backoff to `git_ref_live_probe/2` (817-838) and `http_live_probe/2`, wrapping the existing `System.cmd` / HTTP call. Classify `:unavailable` ONLY after all 3 attempts fail; a single successful attempt short-circuits immediately to `:ok` or `:missing` based on that attempt's real result. Do not add a retry library — a simple recursive helper is right for "try 3 times", and a generic abstraction here would be overkill.

    Preserve the `probes` map injection seam that the existing test depends on (`http_probe:` and `git_ref_probe:` options on `build/1`); the retry must sit on the real probe implementations, not between `build/1` and the injected fakes, or every fixture-driven test would start retrying fake failures three times.

    `maybe_ios_mirror_live/3` (772-787) needs no change — it already emits the right `next_action`.

    **(c) Extend `test/mix/tasks/crosswake_release_status_test.exs` in place.** Flip the `:warning` assertion to `:error`, rename the test, and add a companion `check!(status, "release.live_registry_unverifiable")` assertion alongside the existing `check!(status, "release.live_registry_presence")`. Add the exit-code assertions and the retry-short-circuit case.
  </action>

  <verify>
    <automated>mix test test/mix/tasks/crosswake_release_status_test.exs && mix test</automated>
  </verify>

  <acceptance_criteria>
    - `grep -c 'release.live_registry_unverifiable' lib/crosswake/release_status.ex` returns ≥ 1.
    - In `live_registry_checks/2`, both the `:missing` branch and the `:unavailable` branch carry `status: :error` — `grep -c 'status: :warning' lib/crosswake/release_status.ex` returns 0 within that function.
    - `exit_code/1` is unchanged (`def exit_code(:error), do: 1` still maps both new codes to 1).
    - `git_ref_live_probe/2` and `http_live_probe/2` each retry up to 3 times before classifying `:unavailable`.
    - The `probes` injection seam (`http_probe:` / `git_ref_probe:` options on `build/1`) still works — the existing fixture-driven test still drives fakes without retrying them.
    - `mix test test/mix/tasks/crosswake_release_status_test.exs` green, with assertions for BOTH `release.live_registry_presence` (`:error`, definite negative) and `release.live_registry_unverifiable` (`:error`, unknown-after-retries), and `exit_code == 1` for each.
    - `mix crosswake.release.status --live` exits 0 against the CURRENT live state (plan 02 landed the mirror tag; Hex and Maven are live at 0.2.0). This is Success Criterion #1 of the phase, end-to-end.
    - `mix test` full suite green.
  </acceptance_criteria>

  <done>
    The project's own release-truth command can no longer report success while adopters are broken, and it never confuses "the tag is absent" with "I could not reach the registry".
  </done>
</task>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 3: Register the parity lane as a required status check (D-16, post-merge ritual)</name>

  <read_first>
    - `script/register_required_checks.sh` (its green-first preflight: it fetches `check-runs` for `main`'s HEAD and only registers lanes with `conclusion == "success"` — it will REFUSE to register a lane that has never been green on `main`)
    - `.github/workflows/release-as-staleness-gate.yml` header comment (documents this identical two-step "merge, then register" ritual for the precedent lane)
  </read_first>

  <what-built>
    Task 1 created `merge-blocking-ios-mirror-parity`. `script/list_merge_blocking_checks.py` already auto-discovers it by the `merge-blocking` substring in its job name — zero registration code was needed. But branch protection does not yet REQUIRE it, so today it is advisory: it can be scrolled past.
  </what-built>

  <how-to-verify>
    This step cannot happen during phase execution. `register_required_checks.sh` refuses to register a lane that has never gone green on `main`, and the lane cannot be green on `main` until this phase's PR merges. That two-step ordering is deliberate — registering a never-green lane would block every PR including the one that fixes it.

    **After this phase's PR merges and the parity lane shows green on `main`:**

    ```
    script/register_required_checks.sh                 # DRY RUN first — confirm the new context appears
    DRY_RUN=0 script/register_required_checks.sh       # actually register (repo-admin rights required)
    ```

    Confirm: `gh api repos/szTheory/crosswake/branches/main/protection/required_status_checks -q '.contexts'` lists `merge-blocking-ios-mirror-parity`.

    Once registered, the invariant is enforced on the merge button — unignorable, fail-closed, cannot be scrolled past. That placement is the entire point: `publish-ios-core` was ALREADY hard-failing for three months and nobody saw it, because a post-merge Actions run has no persistence and no inbox. Redness was never the variable; where red LANDS is.
  </how-to-verify>

  <acceptance_criteria>
    - `script/list_merge_blocking_checks.py` lists `merge-blocking-ios-mirror-parity`.
    - The lane has run green on `main` at least once.
    - `gh api repos/szTheory/crosswake/branches/main/protection/required_status_checks -q '.contexts'` includes `merge-blocking-ios-mirror-parity`.
  </acceptance_criteria>

  <action>
    Run `python3 script/list_merge_blocking_checks.py` and confirm the new context is discovered, then HALT and present the registration ritual to the operator.

    Do NOT run `DRY_RUN=0 script/register_required_checks.sh` during phase execution. It requires repo-admin rights, and its green-first preflight will refuse a lane that has never been green on `main` — which this one cannot be until the phase's PR merges. Attempting it early either no-ops or, worse, registers a context that blocks the very PR that introduces it.

    If the operator defers, record it in the summary as an explicit carried post-merge item (matching this repo's existing carried-ship-gate convention). Do not silently drop it: an unregistered lane is advisory, and advisory red is exactly what went unnoticed for three months.
  </action>

  <verify>
    <automated>python3 script/list_merge_blocking_checks.py | grep -qx 'merge-blocking-ios-mirror-parity' && echo "discovered"</automated>
    <human-check>`gh api repos/szTheory/crosswake/branches/main/protection/required_status_checks -q '.contexts'` includes `merge-blocking-ios-mirror-parity`.</human-check>
  </verify>

  <done>
    The parity invariant is enforced on the merge button, or the registration is recorded as an explicit carried post-merge step with the exact command to run.
  </done>

  <resume-signal>Type "registered" once the context appears in branch protection, or "deferred" if the PR has not merged yet (this is a legitimate carried post-merge step — record it in the summary either way).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI runner → mirror repo (unauthenticated read) | The parity gate reads remote state with no credential; a compromised response could only cause a false PASS, never a write |
| `git ls-remote` output → shell parsing | Remote-controlled text enters a shell script |
| Branch protection config → merge button | Whether the invariant is advisory or enforced |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-153-09 | Tampering / DoS | A network failure reported as a MISSING tag (a definite negative), or a missing tag reported as a network failure — either lie erodes trust in the gate and gets it ignored | high | mitigate | Both code paths split the two cases explicitly and report distinct messages: bash side, a 3-retry loop whose exhaustion message names unreachability; Elixir side, `release.live_registry_presence` (definite) vs `release.live_registry_unverifiable` (unknown-after-retries). Both fail closed (exit 1) but neither misreports which failure occurred. |
| T-153-10 | Tampering | Shell injection via remote-controlled `git ls-remote` output | low | mitigate | Output is parsed with a fixed tab/prefix split (`awk`/`sed`/`comm`); no `eval`, no unquoted expansion of remote content into a command. V5 input-validation control per RESEARCH's security domain. |
| T-153-11 | Repudiation / DoS | The gate keyed on `.release-please-manifest.json` instead of released tags, causing the Release PR to permanently block itself | high | mitigate | Structurally avoided: the gate enumerates `refs/tags/ios-core-v*`, never the manifest. Asserted by an acceptance-criteria grep, not merely by a comment. |
| T-153-12 | Repudiation | The lane exists but is never registered, so a real violation is advisory and gets scrolled past | medium | mitigate | Task 3 is a blocking human checkpoint whose acceptance criterion is the `required_status_checks` context list itself. If deferred, it is recorded as a carried item rather than silently dropped. |
| T-153-13 | Information Disclosure | The parity gate needing a credential it should not have | low | mitigate | The mirror is public; the probe is deliberately unauthenticated (`git -c core.askPass= ls-remote`). No secret is available to this lane, so none can leak from it. |
</threat_model>

<verification>
- `script/check_ios_mirror_parity.sh` exits 0 against the real mirror (proves MIRROR-01 landed and the invariant holds).
- `python3 script/list_merge_blocking_checks.py` discovers `merge-blocking-ios-mirror-parity`.
- `mix crosswake.release.status --live` exits 0 (phase Success Criterion #1, end-to-end).
- `mix test` full suite green; `mix test --only phase153_ios_mirror_unblock` green.
- `elixir script/check_release_workflow_integrity.exs` exits 0 (no regression from plan 03).
</verification>

<success_criteria>
MIRROR-02's standing half. Phase definition of done, per D-21:
1. `git ls-remote` on the mirror shows `refs/tags/v0.2.0` present and equal to the splitsh-lite split SHA.
2. `refs/tags/v0.1.2` still resolves.
3. `mix crosswake.release.status --live` exits 0.
4. The parity gate is green and registered as a required check (task 3; may be carried as a documented post-merge step).
</success_criteria>

<output>
Create `.planning/phases/153-ios-mirror-unblock/153-04-SUMMARY.md` when done. Record: the parity script's exit status against the live mirror, the `mix crosswake.release.status --live` exit code, and whether the required-check registration was completed or carried.
</output>
