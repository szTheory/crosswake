---
phase: 153-ios-mirror-unblock
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - script/verify_ios_mirror_backfill.sh
  - .github/workflows/ios-mirror-backfill.yml
  - script/check_release_workflow_integrity.exs
  - test/crosswake/proof/phase153_ios_mirror_unblock_test.exs
autonomous: true
requirements: [MIRROR-01, MIRROR-02]
user_setup: []

must_haves:
  truths:
    - "D-01/D-04: the backfill lane's checkout no longer persists a GITHUB_TOKEN extraheader that can hijack the mirror remote."
    - "D-03: the backfill lane authenticates to the mirror over SSH with MIRROR_DEPLOY_KEY, never an HTTPS URL-embedded token."
    - "D-07: an `apply=false` backfill run performs a real `git push --dry-run --porcelain` against the mirror, so verify-only proves WRITE scope and not merely anonymous read."
    - "D-13: the script's `--update-main` push uses the explicit-lease form `--force-with-lease=refs/heads/main:<sha-read-via-ls-remote>` and therefore does not fail with `stale info` in a CI checkout that never fetched the mirror."
    - "D-08: the ancestry guard distinguishes `mirror main is an object unknown to this repo` (advisory log, proceed) from `mirror main is known and genuinely not an ancestor` (fail-closed)."
    - "D-10: an existing published mirror tag is never moved — the tag refspec is never forced."
    - "D-02: hermetic bare-repo fixtures prove the push semantics offline, so a lease/atomic regression fails at `mix test`, not at release time."
  artifacts:
    - test/crosswake/proof/phase153_ios_mirror_unblock_test.exs
    - script/verify_ios_mirror_backfill.sh
    - .github/workflows/ios-mirror-backfill.yml
    - script/check_release_workflow_integrity.exs
  key_links:
    - "ios-mirror-backfill.yml checkout (persist-credentials:false) -> webfactory/ssh-agent (MIRROR_DEPLOY_KEY) -> ssh-keyscan -> verify_ios_mirror_backfill.sh SSH remote"
    - "verify_ios_mirror_backfill.sh `--update-main` -> `git ls-remote refs/heads/main` -> explicit lease `<expect>` value"
    - "check_release_workflow_integrity.exs `release.ios_backfill.*` checks -> phase153 decoy tests"
---

<objective>
Defuse fuse #1 (D-01, the `actions/checkout` credential hijack) and fuse #2's script-side guard (D-08) on the **backfill lane only**, and turn the already-dispatchable `apply=false` run into the iOS fire-drill this repo lacks (D-07).

Purpose: D-21 step 1. The backfill workflow shares the hijacked checkout, so *no* push may be attempted — not even the recovery push — until transport is fixed. Both armed fuses currently report as the SAME misleading "cannot push" message, which has already caused two misdiagnoses.

Output: a backfill lane that authenticates over SSH, proves write scope before mutating, and pushes `main` with a lease form that actually works in a never-fetched CI checkout.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/153-ios-mirror-unblock/153-CONTEXT.md
@.planning/phases/153-ios-mirror-unblock/153-RESEARCH.md
@.planning/phases/153-ios-mirror-unblock/153-PATTERNS.md
</context>

<artifacts_this_phase_produces>
Introduced by THIS plan (later plans add more):

| Symbol | Kind | Location |
|---|---|---|
| `MIRROR_DEPLOY_KEY` | GitHub Actions secret name (referenced; minted in plan 02) | `.github/workflows/ios-mirror-backfill.yml` |
| `CROSSWAKE_IOS_BACKFILL_MIRROR_REMOTE` | existing env seam, now SSH-shaped | `script/verify_ios_mirror_backfill.sh` |
| `phase153_ios_mirror_unblock` | new mix test tag | `test/crosswake/proof/phase153_ios_mirror_unblock_test.exs` |
| `Crosswake.Proof.Phase153IosMirrorUnblockTest` | new test module | `test/crosswake/proof/phase153_ios_mirror_unblock_test.exs` |
| `release.ios_backfill.write_probe` | new scanner check id | `script/check_release_workflow_integrity.exs` |
| `release.ios_backfill.explicit_lease` | new scanner check id | `script/check_release_workflow_integrity.exs` |
| `release.ios_backfill.ssh_transport` | new scanner check id | `script/check_release_workflow_integrity.exs` |
| `ios_backfill_write_probe/2`, `ios_backfill_explicit_lease/1`, `ios_backfill_ssh_transport/2` | new checker functions | `script/check_release_workflow_integrity.exs` |
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wave 0 — hermetic bare-repo fixtures for the atomic + explicit-lease push semantics</name>

  <read_first>
    - `test/crosswake/proof/phase145_ios_backfill_script_test.exs` (READ IN FULL — the ONLY existing test in this repo that builds temp git repos as fixtures; its `git!/1` helper, `backfill_fixture/0` shape, and `System.cmd("bash", [@script, ...], env: base_env)` driving pattern at lines ~117-132 are the exact skeleton to copy)
    - `test/crosswake/proof/phase142_release_integrity_test.exs` lines 1-90 (the `@phaseNNN_ids ~w(...)` + decoy house style)
    - `script/verify_ios_mirror_backfill.sh` (READ IN FULL — the env-var override seams at lines 22-26 are how the test injects a fake mirror)
    - `.planning/phases/153-ios-mirror-unblock/153-RESEARCH.md` §Q1 (Findings 1-5 — the five empirically-verified push scenarios this task encodes as tests)
  </read_first>

  <files>test/crosswake/proof/phase153_ios_mirror_unblock_test.exs</files>

  <behavior>
    Three git-semantics proofs, run against real local `git init --bare` fixtures (no network, sub-second), plus two script-behavior proofs. These encode RESEARCH's empirical findings so a regression fails at `mix test`, not at release time.

    - Test A (RESEARCH Finding 2 — the D-13/D-08 happy path): a bare "mirror" repo whose `main` is a commit with **no common ancestor** to the release repo (build two independent `git init` trees — this is a NEW fixture shape; phase145's mismatch test uses a *related* commit, not a disjoint history). Run the atomic + explicit-lease push form. Expect: exit 0, mirror `main` == split SHA, mirror `refs/tags/v0.2.0` == split SHA, and the pre-existing `refs/tags/v0.1.2` still resolves to its original SHA.
    - Test B (RESEARCH Finding 3 — stale lease fails the WHOLE transaction): same fixture, but pass a deliberately wrong `<expect>` SHA in the lease. Expect: non-zero exit, mirror `main` UNCHANGED, and `refs/tags/v0.2.0` **absent** (no partial apply).
    - Test C (RESEARCH Finding 4 — D-10 tag immutability is structural): attempt to move the pre-existing `v0.1.2` tag to the split SHA inside the same atomic push, with the lease scoped to `main` alone. Expect: rejected, and `v0.1.2` still points at its original SHA. No application logic asserts this — git's `refs/tags/*` update rules do.
    - Test D (D-07): drive `verify_ios_mirror_backfill.sh` with `apply=false` against the fixture; assert the output contains evidence that a `--dry-run` push probe was actually attempted (see task 2's `ok` line), i.e. verify-only no longer returns before proving write scope.
    - Test E (D-08): drive the script's ancestry guard with a `current_main` object that is unknown to the release repo; assert the run does NOT fail-closed and instead logs the "unknown locally" advisory, while a *known-but-not-an-ancestor* `current_main` still fails closed.

    Tests A/B/C invoke `git push` directly (they prove git's semantics, which the release job in plan 03 will then encode). Tests D/E invoke the real bash script via its env seams.

    Module header, per PATTERNS:
    - `defmodule Crosswake.Proof.Phase153IosMirrorUnblockTest`, `use ExUnit.Case, async: true`
    - `@moduletag :phase153_ios_mirror_unblock` (or `@tag` on each test, matching phase145's per-test `@tag :phase145_ios_backfill_script` style)
    - `@phase153_ids ~w(release.ios_backfill.write_probe release.ios_backfill.explicit_lease release.ios_backfill.ssh_transport)` — decoy tests asserting each id is emitted with `:ok` by the scanner (added by task 3; these decoys go RED until then, which is the intended TDD sequence).
  </behavior>

  <action>
    Create `test/crosswake/proof/phase153_ios_mirror_unblock_test.exs`.

    Copy `phase145_ios_backfill_script_test.exs`'s fixture harness verbatim (`git!/1`, tmp-dir root, `on_exit` cleanup, bare mirror at `mirror.git`). Extend it with a `disjoint_mirror_fixture/0` that creates the mirror's `main` from a SECOND, independently-`git init`'d tree so it shares zero history with the release repo — this reproduces D-08's real off-lineage divergence.

    Tests A/B/C shell out to git with EXACTLY this command form (copy character-for-character; a paraphrase here re-arms the fuse):

    ```
    git push --atomic <mirror> --force-with-lease="refs/heads/main:${CURRENT_MAIN_SHA}" "${SPLIT_SHA}:refs/heads/main" "${SPLIT_SHA}:refs/tags/v${VERSION}"
    ```

    where `CURRENT_MAIN_SHA` is read via `git ls-remote <mirror> refs/heads/main | awk '{print $1}' | head -1`.

    Do NOT use bare `--force-with-lease`, and do NOT use the named-without-expect form `--force-with-lease=refs/heads/main`. RESEARCH Finding 1 proved both are rejected as `stale info` 100% of the time in this CI shape (no `git fetch` ever runs, so no remote-tracking ref exists). If you find yourself writing either, stop — that is the exact bug this phase exists to prevent.

    Tests D/E drive the real script via `System.cmd("bash", ["script/verify_ios_mirror_backfill.sh", ...], env: ...)` with the `CROSSWAKE_IOS_BACKFILL_*` seams pointed at the fixture, mirroring the analog's lines 117-132.

    Tests A/B/C are expected to PASS immediately (they prove git, not our code). Tests D/E and the `@phase153_ids` decoys are expected to FAIL until tasks 2 and 3 land — that is the RED state.
  </action>

  <verify>
    <automated>mix test test/crosswake/proof/phase153_ios_mirror_unblock_test.exs --only phase153_ios_mirror_unblock 2>&1 | tail -20</automated>
  </verify>

  <acceptance_criteria>
    - `test/crosswake/proof/phase153_ios_mirror_unblock_test.exs` exists and compiles.
    - `grep -c 'force-with-lease="refs/heads/main:' test/crosswake/proof/phase153_ios_mirror_unblock_test.exs` returns ≥ 1 (the explicit-lease form is present).
    - `grep -v '^\s*#' test/crosswake/proof/phase153_ios_mirror_unblock_test.exs | grep -c -- '--force-with-lease=refs/heads/main[^:]' ` returns 0 (the broken named-without-expect form is absent from executable lines).
    - `grep -c -- '--atomic' test/crosswake/proof/phase153_ios_mirror_unblock_test.exs` returns ≥ 1.
    - The file defines at least 5 tests covering scenarios A, B, C, D, E as described in `<behavior>`.
    - Tests A, B, C pass now (`mix test ... --only phase153_ios_mirror_unblock` shows them green); D, E and the decoys are RED, which is expected at this task boundary.
  </acceptance_criteria>

  <done>
    A hermetic, network-free proof exists for: off-lineage leased push succeeds; stale lease fails the whole transaction with no tag landing; an existing tag cannot be moved inside the atomic push. Tests D/E define the contract tasks 2-3 must satisfy.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Rewrite verify_ios_mirror_backfill.sh — SSH transport, write probe in the verify branch, correct lease, correct ancestry message</name>

  <read_first>
    - `script/verify_ios_mirror_backfill.sh` (READ IN FULL, 252 lines — `mirror_push_remote()` at 192-198, `verify_or_apply_mirror()` at 204-245, the `--update-main` block at 236-244)
    - `.planning/phases/153-ios-mirror-unblock/153-RESEARCH.md` §Q1 "Same fix applies to the script's `--update-main` path" and §Q2 (the ancestry-guard fix, given as complete copy-pasteable bash)
    - `.planning/phases/153-ios-mirror-unblock/153-PATTERNS.md` §`script/verify_ios_mirror_backfill.sh` — MODIFY (before/after excerpts of the exact block to change)
    - `test/crosswake/proof/phase153_ios_mirror_unblock_test.exs` (task 1 — the contract this task must satisfy)
  </read_first>

  <files>script/verify_ios_mirror_backfill.sh</files>

  <action>
    Four changes. Preserve the existing `set -euo pipefail`, the `[crosswake]` `log`/`ok`/`fail` helpers, the `validate_inputs` → `verify_release_refs` → `verify_manifest_versions` → `verify_live_registries` → `compute_split_sha` → `verify_or_apply_mirror` pipeline, and every existing fail-closed guard. Do NOT rewrite the script; edit it in place.

    **(a) D-03/D-04 — SSH transport.** Replace `mirror_push_remote()` (lines 192-198) so the HTTPS URL-embedded-token branch is gone entirely. The default mirror remote becomes the SSH form `git@github.com:szTheory/crosswake-shell-core-ios.git`; the function simply returns `$MIRROR_REMOTE`. The calling workflow (task 3) loads the deploy key into an ssh-agent, so auth is transparent to the script. Retire the old PAT env var from this file: the `[ -n "${MIRROR_PUSH_TOKEN:-}" ]` branch at 193, the required-for-apply guard at 228-230, and the "after ... is configured" next-action line at 224 all go away and are replaced with deploy-key language.
    <!-- planner-discipline-allow: MIRROR_PUSH_TOKEN -->
    Keep the `CROSSWAKE_IOS_BACKFILL_MIRROR_REMOTE` override seam intact — the task-1 fixtures depend on it to inject a local bare repo.

    **(b) D-07 — write probe in the verify-only branch.** Today `verify_or_apply_mirror()` returns at line 222-225 (`if [ "$APPLY" -ne 1 ]`) BEFORE any push is attempted, so `apply=false` proves only anonymous read — and the mirror is public, so read succeeds with no credential at all (D-01.2). That makes verify-only nearly worthless as a permission check. Before that early return, run a real non-mutating write probe:

    ```
    git -C "$RELEASE_REPO" push --dry-run --porcelain "$push_remote" "${SPLIT_SHA}:refs/tags/v${VERSION}"
    ```

    On success emit an `ok` line that names the probe explicitly (e.g. `ok "dry-run push to ${MIRROR_REPO} succeeded; MIRROR_DEPLOY_KEY has WRITE scope."`). On failure, `fail` with a message that names the deploy key and distinguishes an auth failure from a lineage failure — see (d). This turns the existing `apply=false` dispatch into the iOS fire drill (the repo has an `android-publish-fire-drill` but no iOS equivalent, on the exact surface that broke). Zero new jobs, zero new recurring surface.

    **(c) D-13 — the explicit lease on the `--update-main` push.** The current line 242 uses the named-without-expect form, which RESEARCH Finding 1 proved fails 100% of the time in a never-fetched CI checkout ("stale info") — a pre-existing latent bug, never exercised. `current_main` is ALREADY computed at line 237 via `git ls-remote`; reuse that exact value as the lease expect:

    ```
    git -C "$RELEASE_REPO" push --porcelain --force-with-lease="refs/heads/main:${current_main}" "$push_remote" "${SPLIT_SHA}:refs/heads/main"
    ```

    Keep this as a SEPARATE push from the tag push. Per D-21, the tag (step 4, a one-way door) and the `main` re-baseline (step 5, reversible) must be separately approvable and must NOT be bundled behind a single `--apply`. The atomic bundling of `main`+tag belongs to the *release job* (plan 03), where both refs land in one coordinated pass; it does not belong here.

    Note this lease value is an object that does NOT exist in the local repo (`6417ae65…`). RESEARCH Finding 2 verified directly against the real mirror that the explicit-lease form works anyway — `--force-with-lease=<ref>:<expect>` does not require a remote-tracking branch or a local object. **Do NOT add a `git fetch mirror` step.** D-08's feared blocker is not real.

    **(d) D-08 — the ancestry guard must distinguish "unknown" from "would lose commits".** The current guard at 238 runs `merge-base --is-ancestor` on an object the repo has never seen; git exits non-zero for "not a valid object name", the `!` inverts it, and the script fail-closes with `mirror main has commits not reachable from expected split SHA` — a message that is directionally right but diagnostically wrong. Replace with the RESEARCH §Q2 form: `git cat-file -e "${current_main}^{commit}"` first. If the object is UNKNOWN → `log` an advisory line stating that ancestry cannot be proven either way and that this is expected exactly once, for the approved one-time re-baseline. If the object is KNOWN and genuinely not an ancestor → keep the existing `fail` (that is the real danger this guard exists to catch: mirror-only commits that would be lost). Preserve the `mirror-only commit evidence` phrasing in the fail branch — task 3's scanner check asserts on it.

    Do NOT add a new `--rebaseline` flag. The existing `--update-main` input plus the `ls-remote` read already carry all the information needed; a new flag would duplicate state that can drift (RESEARCH §Q2 recommendation, CONTEXT leaves this to discretion).

    Also update the script's header comment (line 9) to describe the deploy-key/SSH mechanism rather than the retired PAT.
  </action>

  <verify>
    <automated>mix test test/crosswake/proof/phase153_ios_mirror_unblock_test.exs test/crosswake/proof/phase145_ios_backfill_script_test.exs && bash -n script/verify_ios_mirror_backfill.sh</automated>
  </verify>

  <acceptance_criteria>
    - `bash -n script/verify_ios_mirror_backfill.sh` exits 0.
    - `grep -c 'force-with-lease="refs/heads/main:${current_main}"' script/verify_ios_mirror_backfill.sh` returns 1.
    - `grep -v '^\s*#' script/verify_ios_mirror_backfill.sh | grep -c -- '--force-with-lease=refs/heads/main[^:]'` returns 0.
    - `grep -c 'cat-file -e' script/verify_ios_mirror_backfill.sh` returns ≥ 1 (the D-08 unknown-object discriminator).
    - `grep -c 'mirror-only commit evidence' script/verify_ios_mirror_backfill.sh` returns 1 (the known-but-not-ancestor fail branch is preserved).
    - `grep -c 'push --dry-run --porcelain' script/verify_ios_mirror_backfill.sh` returns ≥ 2 (one in the verify-only branch per D-07, one on the apply path).
    - `grep -c 'git@github.com:szTheory/crosswake-shell-core-ios.git' script/verify_ios_mirror_backfill.sh` returns ≥ 1.
    - `grep -v '^\s*#' script/verify_ios_mirror_backfill.sh | grep -c 'x-access-token'` returns 0 (the HTTPS URL-embedded-token transport is gone).
    - `grep -c 'CROSSWAKE_IOS_BACKFILL_MIRROR_REMOTE' script/verify_ios_mirror_backfill.sh` returns ≥ 1 (the test seam survives).
    - `mix test test/crosswake/proof/phase153_ios_mirror_unblock_test.exs` — tests D and E from task 1 now PASS.
    - `mix test test/crosswake/proof/phase145_ios_backfill_script_test.exs` still green (no regression in the existing backfill proofs).
    - `grep -c 'git fetch' script/verify_ios_mirror_backfill.sh` returns 0 — no fetch of the mirror was added (D-08's feared blocker is not real; RESEARCH Finding 2).
  </acceptance_criteria>

  <done>
    `apply=false` now proves WRITE scope, not just anonymous read. `--update-main` uses a lease form that works in a never-fetched CI checkout. The ancestry guard says what it actually knows. The script authenticates over SSH.
  </done>
</task>

<task type="auto">
  <name>Task 3: Backfill workflow SSH block + scanner checks that assert the new mechanism</name>

  <read_first>
    - `.github/workflows/ios-mirror-backfill.yml` (READ IN FULL, 107 lines — checkout at 69-71, credential env at 81)
    - `.planning/phases/153-ios-mirror-unblock/153-RESEARCH.md` §Q3 (the exact three-step SSH block, SHA-pinned, and the action-pinning discipline it must match)
    - `script/check_release_workflow_integrity.exs` lines 60-115 (the `checks = [...]` registry — new checks must be added here or they never run) and lines 848-898 (the `ios_backfill_*` checker functions)
    - `.planning/phases/153-ios-mirror-unblock/153-PATTERNS.md` §"Structural checker function shape" and §"Action-Pinning Discipline"
  </read_first>

  <files>.github/workflows/ios-mirror-backfill.yml, script/check_release_workflow_integrity.exs, test/crosswake/proof/phase153_ios_mirror_unblock_test.exs</files>

  <action>
    **(a) `.github/workflows/ios-mirror-backfill.yml` — the SSH transport block (D-03/D-04).** Replace the checkout at 69-71 and the credential env at 81 with the three-step block RESEARCH §Q3 verified. SHA-pin `webfactory/ssh-agent` per this repo's dominant discipline (every third-party action in `release-please.yml` is SHA-pinned with a trailing version comment; the one tag-pinned exception, `actions/upload-artifact@v4`, is a deliberately tracked exception — do not imitate it):

    ```
    - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
      with:
        fetch-depth: 0
        persist-credentials: false

    - uses: webfactory/ssh-agent@e83874834305fe9a4a2997156cb26c5de65a8555 # v0.10.0
      with:
        ssh-private-key: ${{ secrets.MIRROR_DEPLOY_KEY }}

    - name: Add GitHub to known_hosts
      run: |
        mkdir -p ~/.ssh
        ssh-keyscan -t ed25519 github.com >> ~/.ssh/known_hosts
    ```

    `persist-credentials: false` is the D-04 fix: `actions/checkout`'s default writes `http.https://github.com/.extraheader` into `.git/config`, a key with no username component that therefore matches EVERY `https://github.com/*` remote in the workspace — including the mirror remote — and an explicit Authorization header beats URL userinfo. That is fuse #1. SSH transport is structurally immune anyway; this is belt-and-braces at the source.

    The `ssh-keyscan` step is not optional: without a known_hosts entry, `StrictHostKeyChecking` can hang the job silently. Never `cat`, `echo`, or otherwise print the private key in any step — `webfactory/ssh-agent` loads it without printing it, and that property must be preserved.

    Retire the old PAT from the env block: the deploy key replaces it entirely. Leave the `workflow_dispatch` input surface (`version`, `release_ref`, `apply`, `update_main`) and all `validate_inputs` guards untouched — the exact-ref rejection of `main|master|HEAD|heads/*|refs/heads/*|v*|[0-9]*` is load-bearing and stays. Update the file's header comment (line 3) to name the deploy key.
    <!-- planner-discipline-allow: MIRROR_PUSH_TOKEN -->

    **(b) `script/check_release_workflow_integrity.exs` — rewrite, do not rename.** Three existing `ios_backfill_*` checks now assert on machinery that no longer exists. A find-and-replace on the secret name would leave them asserting a mechanism that literally disappeared.

    - `ios_backfill_verify_first/2` (848-861): its `includes?(script, "MIRROR_PUSH_TOKEN is required for --apply")` assertion targets a guard task 2 deleted. Replace that clause with an assertion on the new deploy-key apply guard, and ADD an assertion that the verify branch still emits `verification-only mode made no changes`. Keep every workflow-side assertion (`workflow_dispatch`, `verify-or-backfill-ios-mirror`, `type: boolean`, `apply` default `false`, `bash script/verify_ios_mirror_backfill.sh`) — those all survive the SSH change.
    - `ios_backfill_no_default_main_force/2` (888-898): its `includes?(script, "--force-with-lease=refs/heads/main")` clause no longer matches, because the correct form has a quote and a `:` after the ref name. Retarget it at the explicit-lease substring. Keep the `--update-main`, `mirror-only commit evidence`, `update_main` default-`false` clauses.
    - `ios_backfill_tag_idempotent/1` (877-886) and `ios_backfill_exact_release_ref/2` (863-875): verify they still pass unchanged; they assert on behavior task 2 preserved.

    Add three NEW checker functions, in the established `defp <name>(args) do ... check("<dotted.id>", <boolean>, "<message ending with 'run elixir script/check_release_workflow_integrity.exs'>") end` shape, and register each in the `checks = [...]` list around lines 88-108 (a checker that is defined but not registered never runs):

    - `release.ios_backfill.write_probe` — asserts the script performs a `push --dry-run --porcelain` inside the verify-only branch (D-07), i.e. `apply=false` proves write scope.
    - `release.ios_backfill.explicit_lease` — asserts the script contains `--force-with-lease="refs/heads/main:${current_main}"` AND does not contain the broken named-without-expect form (D-13).
    - `release.ios_backfill.ssh_transport` — asserts the backfill workflow has `persist-credentials: false` on its checkout, the SHA-pinned `webfactory/ssh-agent` line, an `ssh-keyscan` step, and that the script's mirror remote is the `git@github.com:` SSH form and contains no `x-access-token` HTTPS transport (D-01/D-03/D-04).

    Line numbers cited above are pre-edit and WILL drift as you edit. Re-grep after every edit in this file; do not carry cumulative line numbers across a multi-edit session.

    **(c) Register the new ids in the phase153 test.** Add the three ids to `@phase153_ids` in `test/crosswake/proof/phase153_ios_mirror_unblock_test.exs` and follow phase142's decoy pattern (assert each id is emitted with `:ok` by the scanner). This turns the task-1 RED decoys green.
  </action>

  <verify>
    <automated>elixir script/check_release_workflow_integrity.exs && mix test test/crosswake/proof/phase153_ios_mirror_unblock_test.exs test/crosswake/proof/phase142_release_integrity_test.exs test/crosswake/proof/phase145_ios_backfill_script_test.exs</automated>
  </verify>

  <acceptance_criteria>
    - `elixir script/check_release_workflow_integrity.exs` exits 0.
    - `grep -c 'persist-credentials: false' .github/workflows/ios-mirror-backfill.yml` returns 1.
    - `grep -c 'webfactory/ssh-agent@e83874834305fe9a4a2997156cb26c5de65a8555' .github/workflows/ios-mirror-backfill.yml` returns 1 (SHA-pinned, matching repo discipline).
    - `grep -c 'ssh-keyscan' .github/workflows/ios-mirror-backfill.yml` returns ≥ 1.
    - `grep -c 'MIRROR_DEPLOY_KEY' .github/workflows/ios-mirror-backfill.yml` returns ≥ 1.
    - `grep -vE '^\s*#' .github/workflows/ios-mirror-backfill.yml | grep -c 'x-access-token'` returns 0.
    - `grep -vE '^\s*#' .github/workflows/ios-mirror-backfill.yml | grep -c 'cat .*ssh\|echo .*PRIVATE KEY'` returns 0 (the deploy key is never printed — T-153-02).
    - `elixir script/check_release_workflow_integrity.exs 2>&1 | grep -c 'release.ios_backfill.write_probe'` returns ≥ 1; same for `release.ios_backfill.explicit_lease` and `release.ios_backfill.ssh_transport` — i.e. all three new checks are REGISTERED in the `checks` list, not merely defined.
    - `mix test --only phase153_ios_mirror_unblock` is fully green (all five behavior tests plus all decoys).
    - `mix test` full suite green — `phase142_release_integrity_test.exs` and `phase145_ios_backfill_script_test.exs` still pass.
  </acceptance_criteria>

  <done>
    The backfill lane is SSH-transported, its checkout cannot hijack the mirror remote, and three structural invariants now fail CI if any of it regresses. The `apply=false` dispatch is ready to serve as the fire drill.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GitHub Actions runner → `szTheory/crosswake-shell-core-ios` | A cross-repo git push crosses an authentication boundary; the credential used here is the entire attack surface of this plan |
| `actions/checkout` workspace `.git/config` → any remote added later in the job | An extraheader written by checkout applies to remotes checkout never saw |
| Actions secret store → job process memory/logs | The private key material transits the runner |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-153-01 | Spoofing / EoP | `actions/checkout` in `ios-mirror-backfill.yml` writing `http.https://github.com/.extraheader`, silently authenticating the mirror remote as `github-actions[bot]` | high | mitigate | `persist-credentials: false` on the checkout (task 3a) + SSH transport, which an `http.*` extraheader structurally cannot target (task 2a). Asserted by `release.ios_backfill.ssh_transport`. |
| T-153-02 | Information Disclosure | `MIRROR_DEPLOY_KEY` private half leaked into job logs by a step that echoes or cats it | high | mitigate | Key is loaded exclusively by `webfactory/ssh-agent`, which never prints it; no step may `cat`/`echo` the key. Asserted by an acceptance-criteria negative grep in task 3. GitHub log secret-masking applies as defense in depth. |
| T-153-03 | Tampering | Silently moving an already-published, adopter-resolved SwiftPM tag (SwiftPM caches resolved tags aggressively; a moved tag is cache-poisoning-adjacent) | high | mitigate | Structural, not policy: the lease is scoped to `refs/heads/main` alone, so the tag refspec is never forced even inside an atomic multi-ref push; `refs/tags/*` rejects any update to an existing ref absent an explicit force on that exact refspec. Proven by task 1 test C. |
| T-153-04 | Tampering | A stale or wrong lease value silently overwriting concurrent legitimate mirror state | medium | mitigate | Explicit-lease form with `<expect>` read fresh via `git ls-remote` immediately before the push; a lease mismatch fails the ENTIRE transaction with no partial apply. Proven by task 1 test B. |
| T-153-SC | Tampering | Supply chain — third-party action `webfactory/ssh-agent` | medium | mitigate | SHA-pinned to `e83874834305fe9a4a2997156cb26c5de65a8555` (v0.10.0), matching this repo's existing action-pinning discipline. RESEARCH's Package Legitimacy Audit found no `[SLOP]`/`[SUS]` concerns: public source, 8+ years old, actively maintained. No package-manager installs occur in this plan. |
</threat_model>

<verification>
- `mix test --only phase153_ios_mirror_unblock` green.
- `mix test` full suite green (no regression in phase142 / phase145 proofs).
- `elixir script/check_release_workflow_integrity.exs` exits 0 with all three new `release.ios_backfill.*` ids reported `:ok`.
- `bash -n script/verify_ios_mirror_backfill.sh` exits 0.
- No `x-access-token` HTTPS transport survives in either the script or the backfill workflow.
</verification>

<success_criteria>
The backfill lane can be dispatched safely: its checkout cannot hijack the mirror remote (D-01/D-04 defused), it authenticates over SSH with a zero-rotation deploy key (D-03), `apply=false` proves WRITE scope rather than anonymous read (D-07), `--update-main` uses the only lease form that works here (D-13), and the ancestry guard reports what it actually knows (D-08). Nothing has been pushed to the mirror yet — that is plan 02, and it is human-gated.
</success_criteria>

<output>
Create `.planning/phases/153-ios-mirror-unblock/153-01-SUMMARY.md` when done. Record: the exact final push command strings landed in the script, and confirmation that no `git fetch mirror` step was added.
</output>
