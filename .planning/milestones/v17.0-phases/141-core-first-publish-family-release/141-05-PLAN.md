---
phase: 141-core-first-publish-family-release
plan: 05
type: execute
wave: 2
depends_on: ["141-01", "141-02"]
gap_closure: true
files_modified:
  - release-please-config.json
  - README.md
  - guides/android_uat.md
  - examples/android_shell_host/app/build.gradle
  - examples/ios_shell_host/CrosswakeShell.xcodeproj/project.pbxproj
autonomous: true
requirements: [FAMILY-05, FAMILY-04]
must_haves:
  truths:
    - "The version-metadata drift set (JSON manifests + native coords + README/android_uat) is registered in release-please `extra-files` so recomputing #25 auto-bumps each to 0.2.0 — the durable fix (Option B) that unblocks the core publish and prevents recurrence."
    - "On main the bumpable files stay at 0.1.2 (only inert `x-release-please-version` annotations / config added), so all merge-blocking version-drift guards stay GREEN on main."
    - "The residual manual-at-release pieces (CHANGELOG.md [0.2.0] section + 'current published' line, and the phase52 operator-truth goldens) are documented as the #25-branch reconciliation step (this repo uses skip-changelog:true + regenerated goldens, so release-please structurally cannot own them)."
  artifacts:
    - release-please-config.json
  key_links:
    - "release-please `.` component extra-files (json jsonpath + generic annotations) → the files release-please bumps to 0.2.0 when it recomputes #25."
    - "phase52 operator-truth goldens + CHANGELOG published-truth line → the manual reconcile committed on the #25 release branch at publish time (Task 3, coupled to 141-03 Gate 3)."
---

<objective>
Close the Phase 141 planning gap that blocks the core 0.2.0 publish: the `0.1.2 → 0.2.0` bump in Release PR #25 trips 5 merge-blocking version-drift guards because ~8 version-metadata files still say 0.1.2 and release-please's `extra-files` only covered `mix.exs` + one `gradle.kts`.

Root cause: release-please bumped `@version` (and `x-release-please-version`-marked native lines) but not the JSON manifests, README, android_uat, native example coords, the operator-truth goldens, or the CHANGELOG published-truth line. Guards derive `@current_version` from `Application.spec(:crosswake, :vsn)`, so on #25 (vsn=0.2.0) every embedded 0.1.2 is stale.

Durable fix (Option B, user-chosen): register the automatable drift files in release-please `extra-files` so recompute bumps them. The two mechanism-incompatible pieces (regenerated phase52 goldens + hand-maintained CHANGELOG under skip-changelog:true) are handled as the #25-branch reconciliation at publish time.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/141-core-first-publish-family-release/141-CONTEXT.md
@release-please-config.json
@test/crosswake/guides/native_evidence_drift_test.exs
@test/crosswake/guides/release_boundaries_test.exs
@test/crosswake/guides/evidence_manifest_test.exs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Register the automatable drift files in release-please extra-files + add inert version markers (main stays 0.1.2/green)</name>
  <files>release-please-config.json, README.md, guides/android_uat.md, examples/android_shell_host/app/build.gradle, examples/ios_shell_host/CrosswakeShell.xcodeproj/project.pbxproj</files>
  <action>
  In `release-please-config.json` `.` (component `hex`) `extra-files`, ADD (do not remove `mix.exs` or the existing gradle.kts entry):
   - `{"type":"json","path":"examples/phoenix_host/evidence/evidence-manifest.example.json","jsonpath":"$.crosswake_version"}`
   - `{"type":"json","path":"examples/phoenix_host/priv/crosswake/install_manifest.json","jsonpath":"$.crosswake_version"}`
   - `{"type":"json","path":"examples/ios_shell_host/Fixtures/crosswake_manifest.json","jsonpath":"$.crosswake_version"}`
   - `{"type":"json","path":"examples/android_shell_host/app/src/main/assets/crosswake_manifest.json","jsonpath":"$.crosswake_version"}`
   - `{"type":"generic","path":"README.md"}`
   - `{"type":"generic","path":"guides/android_uat.md"}`
   - `{"type":"generic","path":"examples/android_shell_host/app/build.gradle"}`
   - `{"type":"generic","path":"examples/ios_shell_host/CrosswakeShell.xcodeproj/project.pbxproj"}`
  Then add the `x-release-please-version` annotation to the version line in each generic-updater file, LEAVING THE VALUE AT 0.1.2 (release-please bumps it on #25 recompute; on main the value must stay 0.1.2 so guards pass):
   - `README.md` line ~190 `- Crosswake version \`0.1.2\`` → append ` <!-- x-release-please-version -->`
   - `guides/android_uat.md` line ~3 `**Last verified against:** Crosswake v0.1.2 on Android 15 (API 35)` → append ` <!-- x-release-please-version -->`
   - `examples/android_shell_host/app/build.gradle` line ~15 `versionName "0.1.2"` → append ` // x-release-please-version`; line ~76 `implementation 'io.github.sztheory:crosswake-shell-core-android:0.1.2'` → append ` // x-release-please-version`
   - `examples/ios_shell_host/CrosswakeShell.xcodeproj/project.pbxproj` line ~59 `minimumVersion = 0.1.2;` → append ` /* x-release-please-version */`
  Do NOT add markers to the JSON files (the json updater keys off jsonpath, not annotations). Do NOT change any 0.1.2 value. Do NOT touch the CHANGELOG or the phase52 goldens here (Task 3).
  </action>
  <verify>
    <automated>node -e "const c=require('./release-please-config.json'); const ef=c.packages['.']['extra-files']; const paths=ef.map(e=>typeof e==='string'?e:e.path); const need=['examples/phoenix_host/evidence/evidence-manifest.example.json','examples/phoenix_host/priv/crosswake/install_manifest.json','examples/ios_shell_host/Fixtures/crosswake_manifest.json','examples/android_shell_host/app/src/main/assets/crosswake_manifest.json','README.md','guides/android_uat.md','examples/android_shell_host/app/build.gradle','examples/ios_shell_host/CrosswakeShell.xcodeproj/project.pbxproj']; for(const p of need){if(!paths.includes(p)){console.error('MISSING extra-file: '+p);process.exit(1)}}; if(!paths.includes('mix.exs')){console.error('mix.exs dropped');process.exit(1)}; console.log('OK config')" && grep -q 'x-release-please-version' README.md && grep -q 'x-release-please-version' guides/android_uat.md && grep -c 'x-release-please-version' examples/android_shell_host/app/build.gradle && grep -q 'x-release-please-version' examples/ios_shell_host/CrosswakeShell.xcodeproj/project.pbxproj && echo OK-markers</automated>
    <automated>MIX_ENV=test mix test test/crosswake/guides/native_evidence_drift_test.exs test/crosswake/guides/release_boundaries_test.exs test/crosswake/guides/evidence_manifest_test.exs 2>&1 | grep -qE '0 failures' && echo GUARDS-GREEN-ON-MAIN</automated>
  </verify>
  <done>release-please `.` extra-files lists all 8 drift files; the 4 generic-updater files carry an inert `x-release-please-version` annotation with value still 0.1.2; the three version-drift guard suites still pass locally (main stays green because no value changed); config JSON parses.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Land Task 1 on origin/main via PR and confirm release-please recomputes #25 with auto-bumped files</name>
  <action>Blocking human checkpoint. Open a PR carrying the Task 1 commit to protected main, get the merge-blocking lanes green (values unchanged → guards pass), merge it. Then confirm release-please recomputes #25 and that #25's diff now bumps the 8 registered files 0.1.2→0.2.0. Report which (if any) files release-please did NOT bump — those inform Task 3.</action>
  <what-built>The durable extra-files config must live on main for release-please to apply it when it recomputes #25. This is a config + inert-marker change (no value change), so main's version-drift guards stay green.</what-built>
  <how-to-verify>
    1. Open a `sync/main-141-05-extrafiles` PR (or fold into boundary flow) with the Task 1 commit. Confirm merge-blocking lanes green on the PR.
    2. Merge it. `git switch main && git pull --ff-only`.
    3. Wait for the release-please workflow run to complete; confirm #25 recomputed.
    4. `gh pr diff 25` — confirm the 8 registered files now show `0.1.2 → 0.2.0`. Note any that did NOT bump (release-please generic/json updater miss) — these plus the goldens/CHANGELOG are Task 3 residual.
  </how-to-verify>
  <resume-signal>Type "extrafiles-landed" once merged and #25 shows the auto-bumped files, listing any files release-please missed — or report the failure.</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Reconcile the residual manual pieces on #25's release branch (goldens + CHANGELOG + any release-please misses), green #25</name>
  <action>Blocking human checkpoint, coupled to 141-03 Gate 3 (this is the reconciliation that finally greens #25 for the core publish). On the `release-please--branches--main` branch (vsn=0.2.0), commit the pieces release-please structurally cannot own, then confirm all merge-blocking lanes go green. Do NOT let anything else merge to main in the meantime (release-please force-pushes its branch on any new main commit, wiping the reconcile commit).</action>
  <what-built>The mechanism-incompatible residuals: (a) CHANGELOG.md — add a `[0.2.0]` section and update the "current published Hex release is `0.2.0`" line (core is skip-changelog:true, hand-maintained); (b) the phase52 operator-truth goldens `test/fixtures/proof/phase52_operator_inspection.json` + `phase52_publish_readiness.json` — regenerate against the now-0.2.0 install_manifest + CHANGELOG via their canonical refresh path; (c) any files release-please's updater missed in Task 2.</what-built>
  <how-to-verify>
    1. On #25's branch (vsn=0.2.0), regenerate the phase52 goldens via their canonical update path (see phase52_operator_truth_test.exs hint "update canonical inspection output and fixture together" — use the repo's fixture-refresh flag/task, do NOT hand-edit the normalized blobs).
    2. Update CHANGELOG.md: add the `## [0.2.0]` section and bump the "current published Hex release" line to 0.2.0.
    3. Run the full merge-blocking suite locally against vsn=0.2.0 (CROSSWAKE_RELEASE path or mix set) until 0 failures; commit onto the #25 branch; push.
    4. Confirm #25's merge-blocking lanes (`hex page proof`, `core hermetic proof`, `route-tour-proof`, `merge-blocking-offline-sync-e2e`, `merge-blocking commerce support`) all go green.
  </how-to-verify>
  <resume-signal>Type "sharp-25-green" once #25 is fully green on all merge-blocking lanes and ready for the core-publish go (141-03 Gate 3) — or report the residual failure.</resume-signal>
</task>

</tasks>

<verification>
- release-please-config.json `.` extra-files lists the 8 automatable drift files; JSON parses; mix.exs + gradle.kts retained.
- On main: the 4 generic-updater files carry inert `x-release-please-version` annotations at value 0.1.2; the 3 version-drift guard suites pass (main green).
- After landing: #25 recompute auto-bumps the registered files to 0.2.0.
- After Task 3: #25 fully green on all merge-blocking lanes (goldens + CHANGELOG reconciled at vsn=0.2.0).
</verification>

<success_criteria>
Release PR #25 goes green — the version-drift guards are satisfied because release-please auto-bumps the registered files (durable) and the release-time goldens/CHANGELOG are reconciled on the release branch — unblocking the core 0.2.0 publish (141-03 Gate 3) without any irreversible action taken by this plan.
</success_criteria>

<output>
Create `.planning/phases/141-core-first-publish-family-release/141-05-SUMMARY.md` when done.
</output>
