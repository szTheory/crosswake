---
phase: 141-core-first-publish-family-release
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - release-please-config.json
  - docs/COMPANION-PUBLISH-RUNBOOK.md
autonomous: true
requirements: [FAMILY-05]
must_haves:
  truths:
    - "The root release-please Release PR (#25) recomputes to core 0.2.0, not the pre-major patch 0.1.3 it would otherwise cut."
    - "The publish runbook names core-first as the mandatory step before any companion publish."
  artifacts:
    - release-please-config.json
    - docs/COMPANION-PUBLISH-RUNBOOK.md
  key_links:
    - "release-as on '.' → the version release-please writes into mix.exs @version + the hex-vX.Y.Z tag the publish-hex job checks out."
    - "runbook core-first section → the human's execution order in 141-03/141-04."
---

<objective>
Prepare the release-please configuration so the root Release PR (#25) lands core at **0.2.0** (D-141-A), and extend the publish runbook with the mandatory **core-first** step that the FAMILY-04 plan (140-05) omitted — the root cause of the blocked sigra publish (D-141-B).

Purpose: Without a `release-as` nudge, release-please computes a PATCH (0.1.3) because the config is pre-major (`bump-minor-pre-major: false`, `bump-patch-for-minor-pre-major: true`) — so the 53 `feat(...)` commits since 0.1.2 bump patch, not minor. The honest v17.0 floor is `0.2.0` (additive `Finding.{code,details}`, `:auth` clause, `:companions` registry — no breaking changes). The runbook is the operator's driving procedure; it must encode "publish core first."

Output: `release-as: "0.2.0"` pin on the `.` component with a one-shot removal note; a new core-first section in the runbook.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/141-core-first-publish-family-release/141-CONTEXT.md
@release-please-config.json
@docs/COMPANION-PUBLISH-RUNBOOK.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pin core '.' component to release-as 0.2.0 with one-shot removal note</name>
  <files>release-please-config.json</files>
  <action>In the `.` (component `hex`) package block, add `"release-as": "0.2.0"` and a sibling `"_TODO_release_as"` note explaining this is a ONE-SHOT override to force the honest v17.0 minor floor (per D-141-A: additive Finding.code/details + :auth clause + :companions registry, no breaking change) and MUST be removed after the root Release PR #25 merges, exactly like the companion one-shot pins — otherwise every subsequent core release re-targets 0.2.0. Do NOT touch `bump-minor-pre-major`/`bump-patch-for-minor-pre-major` (leaving them keeps the repo pre-major for future auto-computes once the pin is removed). Do NOT touch any companion block. Rationale for release-as over a synthetic `feat!`: an explicit pin is auditable, reversible, and does not fabricate a breaking-change commit. Note the core CHANGELOG [Unreleased] section is owned/rewritten by release-please on PR recompute — do not hand-edit it here.</action>
  <verify>
    <automated>node -e "const c=require('./release-please-config.json'); const dot=c.packages['.']; if(dot['release-as']!=='0.2.0'){console.error('FAIL: . release-as not 0.2.0');process.exit(1)}; if(!dot['_TODO_release_as']){console.error('FAIL: missing _TODO_release_as note');process.exit(1)}; const comps=['crosswake_rulestead','crosswake_rindle','crosswake_sigra','crosswake_chimeway','crosswake_threadline']; for(const p of comps){if(c.packages['packages/'+p]['release-as']!=='0.1.0'){console.error('FAIL: companion '+p+' release-as changed');process.exit(1)}}; console.log('OK')"</automated>
  </verify>
  <done>`release-please-config.json` `.` component has `release-as: "0.2.0"` plus a one-shot `_TODO_release_as` note; all five companion `release-as: "0.1.0"` pins are untouched; JSON parses.</done>
</task>

<task type="auto">
  <name>Task 2: Extend COMPANION-PUBLISH-RUNBOOK.md with the mandatory core-first step</name>
  <files>docs/COMPANION-PUBLISH-RUNBOOK.md</files>
  <action>Add a new top-level section (placed BEFORE the "Per-companion publish loop" section) titled to make core-first unmissable — e.g. "## Step 0 — Publish core FIRST (the load-bearing prerequisite, D-141-B)". It must state: (a) WHY — companions build `%Finding{code: ...}` / other v17.0 core API added post-0.1.2; the `crosswake_dep()` seam resolves `{:crosswake, "~> 0.2"}` at publish, so published core MUST already carry that API or the companion fails at `mix compile` before hex.publish (cite the 2026-07-03 sigra KeyError incident). (b) HOW — merge the root release-please Release PR (#25); the `publish-hex` job (`release-please.yml` ~L92, gated on `releases_created`, checks out the `hex-vX.Y.Z` tag, verifies `@version` in mix.exs, publishes) takes core to Hex. (c) THE GATE — do NOT merge ANY companion Release PR until `mix hex.info crosswake` shows 0.2.0 AND `https://hexdocs.pm/crosswake/0.2.0` resolves. Add a one-line amendment to the existing "Preconditions" section adding "core 0.2.0 is live on Hex" as a precondition for the per-companion loop, cross-referencing the new Step 0. Also update the per-companion loop's hexdocs-confirm step language to note companions now resolve against core 0.2.0 (the clean-room proof is what actually verifies this). Keep the existing "never register publish-hex-*/clean-room-proof-* as required" hard rule intact. Prose only — no fenced code blocks that duplicate acceptance-grep literals.</action>
  <verify>
    <automated>grep -qi 'core' docs/COMPANION-PUBLISH-RUNBOOK.md && grep -qi '0.2.0' docs/COMPANION-PUBLISH-RUNBOOK.md && grep -qiE 'step 0|core.first|publish core' docs/COMPANION-PUBLISH-RUNBOOK.md && grep -q 'hexdocs.pm/crosswake/0.2.0' docs/COMPANION-PUBLISH-RUNBOOK.md && echo OK</automated>
  </verify>
  <done>Runbook has a core-first Step 0 section naming version 0.2.0, the `publish-hex` vehicle, and the "core live before any companion" gate; the Preconditions section references it; no existing safety rule was removed.</done>
</task>

</tasks>

<verification>
- `release-please-config.json` parses and `.` carries `release-as: "0.2.0"` + one-shot note; companion pins unchanged.
- Runbook names core-first, version 0.2.0, and the hexdocs gate.
- No workflow YAML or mix.exs touched by this plan (those belong to 141-02 and the publish plans).
</verification>

<success_criteria>
Release-please will recompute #25 to 0.2.0 on next run, and the operator has a written core-first procedure — the two prerequisites that make the Wave 2 publish safe.
</success_criteria>

<output>
Create `.planning/phases/141-core-first-publish-family-release/141-01-SUMMARY.md` when done.
</output>
