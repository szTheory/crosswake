---
phase: 141-core-first-publish-family-release
plan: 04
type: execute
wave: 3
depends_on: ["141-03"]
files_modified: []
autonomous: false
requirements: [FAMILY-04]
must_haves:
  truths:
    - "sigra, then chimeway, then threadline each publish to Hex sequentially — one Release PR merged at a time, never batched."
    - "Each companion's publish-hex-* AND clean-room-proof-* are green, and clean-room resolves it against published core 0.2.0 (not 0.1.2) — the real version-mismatch verification the path-dep dress rehearsal could not catch."
    - "Each companion's hexdocs (crosswake_<name>/0.1.0) resolves, and its release-as one-shot pin is cleaned up before the next companion starts."
  artifacts: []
  key_links:
    - "core 0.2.0 live (141-03) → each companion's ~> 0.2 seam resolves at publish-time compile → clean-room-proof green."
    - "release-as-cleanup PR per companion → merge-blocking-release-as-staleness gate goes green → next companion may proceed."
---

<objective>
Execute the sequential companion publish — **sigra → chimeway → threadline** — now that core 0.2.0 is live (depends_on 141-03). Each publish is a human go/no-go gate (autonomous: false, D-141-E). This is the runbook's per-companion loop (D-141-B / FAMILY-04): never batch, complete all steps for one companion before touching the next.

Purpose: With core 0.2.0 on Hex, each companion's `~> 0.2` seam now resolves against a core that carries the `Finding.{code,details}`/`:auth`/`:companions` API — so the `mix compile` that blocked sigra on 2026-07-03 now succeeds. The clean-room-proof (throwaway host resolving the freshly published package from Hex against published core 0.2.0) is the real verification that the version mismatch is gone — the path-dep dress rehearsal structurally cannot catch it.

Output: `crosswake_sigra`, `crosswake_chimeway`, `crosswake_threadline` all live at 0.1.0 on Hex + hexdocs, clean-room-proofed against core 0.2.0.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/141-core-first-publish-family-release/141-CONTEXT.md
@docs/COMPANION-PUBLISH-RUNBOOK.md
@.github/workflows/release-please.yml
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Publish crosswake_sigra 0.1.0 (irreversible, first in order)</name>
  <action>Blocking human go/no-go. Only after core 0.2.0 is live (141-03). Merge exactly the crosswake_sigra Release PR, get publish-hex-sigra + clean-room-proof-sigra green, confirm hexdocs, merge the release-as-cleanup PR. Do NOT touch chimeway/threadline yet. Follow the how-to-verify steps.</action>
  <what-built>PUBLISH SIGRA (irreversible). The `~> 0.2` floor bump (141-02) made a source change to crosswake_sigra, so release-please opens a fresh `crosswake_sigra` Release PR (the failed 2026-07-03 0.1.0 tag/release were already cleaned up; package is NOT on Hex). Merging it triggers `publish-hex-sigra` (release-please.yml ~L350) then, within the same run, `clean-room-proof-sigra` (needs: publish-hex-sigra).</what-built>
  <how-to-verify>
    1. PRECONDITION: core 0.2.0 is live (from 141-03). If not, STOP.
    2. Merge EXACTLY the `crosswake_sigra` Release PR. Do not merge chimeway/threadline PRs yet.
    3. Wait for GREEN: `publish-hex-sigra` (sets CROSSWAKE_RELEASE=1 → seam resolves `{:crosswake, "~> 0.2"}` → compiles against published core 0.2.0 → dry-run → publish) AND `clean-room-proof-sigra` (throwaway host installs crosswake_sigra + crosswake 0.2.0 from Hex, runs doctor). BOTH must be green.
    4. Confirm `https://hexdocs.pm/crosswake_sigra/0.1.0` resolves (give it a minute).
    5. Merge the auto-opened `release-as-cleanup` PR stripping sigra's one-shot `release-as: "0.1.0"` pin; confirm `merge-blocking-release-as-staleness` goes green.
    6. If `release-failure-alert` fires, STOP and investigate before proceeding.
  </how-to-verify>
  <resume-signal>Type "sigra-live" once publish-hex-sigra + clean-room-proof-sigra are green, hexdocs resolves, and the cleanup PR merged — or report the failure.</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Publish crosswake_chimeway 0.1.0 (irreversible, second in order)</name>
  <action>Blocking human go/no-go. Only after sigra is fully live + cleaned up. Merge exactly the crosswake_chimeway Release PR, get publish-hex-chimeway + clean-room-proof-chimeway green, confirm hexdocs, merge the cleanup PR. Follow the how-to-verify steps.</action>
  <what-built>PUBLISH CHIMEWAY (irreversible) — only after sigra is fully live. Merging the fresh `crosswake_chimeway` Release PR triggers `publish-hex-chimeway` (~L441) then `clean-room-proof-chimeway`. The chimeway clean-room installs crosswake + crosswake_chimeway but NOT crosswake_sigra (the vacuity/no-sibling-dep guard, CHIME-02).</what-built>
  <how-to-verify>
    1. PRECONDITION: sigra is live + hexdocs resolved + its cleanup PR merged. If not, STOP.
    2. Merge EXACTLY the `crosswake_chimeway` Release PR (one at a time).
    3. Wait for GREEN: `publish-hex-chimeway` (compiles against published core 0.2.0 via `~> 0.2`) AND `clean-room-proof-chimeway` (resolves against core 0.2.0, excludes sigra).
    4. Confirm `https://hexdocs.pm/crosswake_chimeway/0.1.0` resolves.
    5. Merge the auto-opened chimeway `release-as-cleanup` PR; confirm the staleness gate goes green.
    6. If release-failure-alert fires, STOP.
  </how-to-verify>
  <resume-signal>Type "chimeway-live" once publish + clean-room are green, hexdocs resolves, and cleanup merged — or report the failure.</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Publish crosswake_threadline 0.1.0 (irreversible, observer last)</name>
  <action>Blocking human go/no-go. Only after sigra AND chimeway are both live + cleaned up. Merge exactly the crosswake_threadline Release PR, get publish-hex-threadline + clean-room-proof-threadline green, confirm hexdocs, merge the cleanup PR, then run the final family check. Follow the how-to-verify steps.</action>
  <what-built>PUBLISH THREADLINE (irreversible) — the pure observer, published LAST (D-7 belt-and-suspenders). Merging the fresh `crosswake_threadline` Release PR triggers `publish-hex-threadline` (~L532) then `clean-room-proof-threadline`, which installs crosswake + crosswake_threadline only (zero sibling dep — not sigra, not chimeway).</what-built>
  <how-to-verify>
    1. PRECONDITION: sigra AND chimeway both live + hexdocs resolved + cleanup PRs merged. If not, STOP.
    2. Merge EXACTLY the `crosswake_threadline` Release PR.
    3. Wait for GREEN: `publish-hex-threadline` (compiles against published core 0.2.0) AND `clean-room-proof-threadline` (resolves against core 0.2.0; three module-shipment canaries + zero-sibling-dep invariant).
    4. Confirm `https://hexdocs.pm/crosswake_threadline/0.1.0` resolves.
    5. Merge the auto-opened threadline `release-as-cleanup` PR; confirm the staleness gate goes green.
    6. If release-failure-alert fires, STOP.
    7. FINAL FAMILY CHECK: all three companion hexdocs resolve; core is 0.2.0; the family is live. Optionally write confirmed `Current Version` numbers back into guides/companion_compatibility.md from each hex.pm release page (the human-write-back the matrix format-guard permits) — note this is a follow-up doc edit, not a publish action.
  </how-to-verify>
  <resume-signal>Type "threadline-live" (or "family-live") once publish + clean-room are green, hexdocs resolves, and cleanup merged — or report the failure.</resume-signal>
</task>

</tasks>

<verification>
- `mix hex.info crosswake_sigra` / `_chimeway` / `_threadline` each show 0.1.0; all three hexdocs resolve.
- Each `clean-room-proof-*` green (resolved against published core 0.2.0, not 0.1.2).
- rulestead/rindle Release PRs untouched (D-141-D).
- No batching — exactly one companion Release PR merged per step, in order sigra → chimeway → threadline.
</verification>

<success_criteria>
The three v17.0 companions are live on Hex at 0.1.0, each clean-room-proofed against published core 0.2.0, published strictly in order — closing FAMILY-04 execution and the v17.0 companion family.
</success_criteria>

<output>
Create `.planning/phases/141-core-first-publish-family-release/141-04-SUMMARY.md` when done.
</output>
