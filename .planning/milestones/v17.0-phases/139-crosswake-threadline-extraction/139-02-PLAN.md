---
phase: 139-crosswake-threadline-extraction
plan: 02
type: execute
wave: 2
depends_on: [139-01]
files_modified:
  - packages/crosswake_threadline/test/crosswake/threadline/id_test.exs
  - packages/crosswake_threadline/test/crosswake/threadline/telemetry_test.exs
  - packages/crosswake_threadline/test/crosswake/plug/threadline_test.exs
  - packages/crosswake_threadline/test/crosswake/live/threadline_test.exs
  - packages/crosswake_threadline/test/crosswake/audit/ledger_test.exs
  - packages/crosswake_threadline/test/mix/tasks/crosswake.gen.audit_test.exs
  - packages/crosswake_threadline/test/mix/tasks/crosswake.threadline_test.exs
  - packages/crosswake_threadline/test/crosswake/proof/phase91_threadline_contract_closeout_test.exs
  - packages/crosswake_threadline/test/crosswake/proof/phase92_server_propagation_closeout_test.exs
  - packages/crosswake_threadline/test/crosswake/proof/phase96_threadline_docs_contract_test.exs
  - packages/crosswake_threadline/test/crosswake/proof/phase139_threadline_cleanroom_test.exs
  - packages/crosswake_threadline/priv/templates/crosswake/audit/ledger.ex.eex
  - packages/crosswake_threadline/lib/mix/tasks/crosswake.threadline.ex
  - packages/crosswake_threadline/lib/mix/tasks/crosswake.gen.audit.ex
  - packages/crosswake_threadline/lib/crosswake/audit/ledger.ex
  - test/crosswake/threadline/
  - test/crosswake/plug/threadline_test.exs
  - test/crosswake/live/threadline_test.exs
  - test/crosswake/audit/ledger_test.exs
  - test/mix/tasks/crosswake.gen.audit_test.exs
  - test/mix/tasks/crosswake.threadline_test.exs
  - test/crosswake/proof/phase91_threadline_contract_closeout_test.exs
  - test/crosswake/proof/phase92_server_propagation_closeout_test.exs
  - test/crosswake/proof/phase96_threadline_docs_contract_test.exs
  - test/crosswake/telemetry_test.exs
autonomous: true
requirements: [THREAD-01, THREAD-02]
must_haves:
  truths:
    - "the moved threadline unit tests (id/telemetry/plug/live/ledger + both task tests) pass in the crosswake_threadline package lane; no threadline-internal test remains in core (THREAD-01)"
    - "the anti-drift test asserts core @baseline_forbidden_keys ⊆ union(registered-companion forbidden_metadata_keys) — a core key without companion provenance fails loudly; the inverse is NOT asserted (companion-domain keys legitimately exceed the floor) (D-5)"
    - "any existing core test asserting the @baseline_forbidden_keys count (was 10) is updated to the post-Phase-139 universal-floor count in the same task"
    - "the generated gen.audit ledger.ex.eex handler wraps its write in try/rescue → catch, Logger.error, NEVER reraise (telemetry auto-detaches a raising handler → silent audit blackout); on_conflict: :nothing + conflict_target: :idempotency_key added; write stays SYNCHRONOUS inside Ecto.Multi (THREAD-02)"
    - "the ledger.ex.eex @moduledoc marks row_hash/prev_hash ADVISORY (not tamper-evidence); the idempotency_key UNIQUE index is the real integrity guarantee"
    - "the vacuity-safe clean-room ExUnit proof asserts Telemetry.event_names/0 == 3 canary, Plug.Threadline.init([]) OK, Audit.Ledger.actor_ref/2 → 64-char hex, AND refute :crosswake_sigra in deps + refute :crosswake_chimeway in deps (proves zero-sibling non-vacuously)"
    - "the 4 brand-voice DX wins land, each in its OWN task/commit separate from the mechanical move: NO_COLOR/ASCII tree fallback + empty-result guard in crosswake.threadline; 4 microcopy rewrites (ephemeral/durable posture, gen.audit Next steps, HMAC ArgumentError) per brandbook/BRAND-SPEC.md and 139-CONTEXT §D"
  artifacts:
    - "packages/crosswake_threadline/test/crosswake/{threadline/{id,telemetry}_test.exs, plug/threadline_test.exs, live/threadline_test.exs, audit/ledger_test.exs} + test/mix/tasks/{crosswake.gen.audit,crosswake.threadline}_test.exs + test/crosswake/proof/{phase91,phase92,phase96}_*.exs (MOVED)"
    - "packages/crosswake_threadline/test/crosswake/proof/phase139_threadline_cleanroom_test.exs (NEW — vacuity-safe canary proof, async: true)"
    - "hardened priv/templates/crosswake/audit/ledger.ex.eex (try/rescue handler + on_conflict + advisory moduledoc)"
    - "core anti-drift test (in test/crosswake/telemetry_test.exs or a new proof) + updated baseline-count assertion"
    - "DX: crosswake.threadline.ex NO_COLOR/ASCII fallback + empty-result guard; microcopy rewrites in crosswake.threadline.ex + crosswake.gen.audit.ex + audit/ledger.ex"
  prohibitions:
    - "the generated audit handler MUST NEVER reraise inside the telemetry handler (reraise → telemetry auto-detach → silent audit blackout)"
    - "MUST NOT make the audit write async / GenServer / Oban — the row commits atomically with the action inside Ecto.Multi (no new deps, no atomicity break)"
    - "MUST NOT assert the inverse of the anti-drift subset (companion-domain keys legitimately exceed the universal floor — asserting equality would false-fail)"
    - "MUST NOT fold a DX microcopy/output change into the same commit as the mechanical test move — each DX change is its own task/commit so the pure move stays reviewable and the clean-room proof stays meaningful"
    - "MUST NOT add an explicit --ascii flag (DEFERRED); NO_COLOR env detection only"
    - "the clean-room proof MUST NOT assert a companion-behaviour function (enabled?/0 etc.) — threadline is not a Crosswake.Companion implementor"
  key_links:
    - "anti-drift subset direction: core_baseline ⊆ union(companion keys) — asserts every floor key has companion provenance; NOT the reverse (Plan 01 SITE 2 curated the floor; this test guards drift)"
    - "gen.audit handler try/rescue is a TEMPLATE change (ledger.ex.eex), not host source — the generated handler must keep itself attached on write failure"
    - "clean-room proof async: true — threadline is NOT a :companions registrant, no Application.put_env, no process-global mutation (contrast chimeway async: false)"
    - "DX microcopy grounded in real anchors: crosswake.threadline.ex:62 (ephemeral) / :181 (durable) / :209,220,221 (box glyphs); crosswake.gen.audit.ex:54-63 (Next steps) + :80 (reused→skipping); audit/ledger.ex:56 (HMAC ArgumentError)"
---

<objective>
Move the threadline test suite into the package lane (THREAD-01), harden the generated audit handler
template (THREAD-02), add the anti-drift PII-floor test and the vacuity-safe clean-room proof, and
fold in the four brand-voice DX wins — each DX change in its OWN commit, separate from the mechanical
move, so the "pure move" stays reviewable and the clean-room proof stays meaningful (139-CONTEXT §D).

This is the analog of Phase 138 Plan 02, MINUS the phase59 support-truth split and the phase71
sigra-integration move (threadline has no companion facade and no sibling coupling), PLUS three
threadline-novel pieces: the gen.audit template hardening (try/rescue handler + on_conflict + advisory
moduledoc), the anti-drift baseline test (core_baseline ⊆ union(companion forbidden keys), D-5), and
the folded-in DX wins (NO_COLOR/ASCII tree fallback + empty-result guard + 4 microcopy rewrites).

Purpose: The publish gate (Plans 03-04) is only trustworthy if the package lane is green with real
threadline machinery, the audit handler cannot silently blackout, and the clean-room proves module
shipment with both siblings absent. The DX wins are cheap and the files are already moving — but they
stay in isolated commits so a reviewer can see the pure move untouched.
Output: the threadline package test lane is green (moved + clean-room proof); the gen.audit handler
template is crash-isolated and idempotent; the anti-drift test guards the PII floor; the four DX wins
are in place, each atomic.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/139-crosswake-threadline-extraction/139-CONTEXT.md
@.planning/phases/139-crosswake-threadline-extraction/139-RESEARCH.md
@.planning/phases/139-crosswake-threadline-extraction/139-PATTERNS.md
@.planning/phases/139-crosswake-threadline-extraction/139-VALIDATION.md
@.planning/phases/139-crosswake-threadline-extraction/139-01-SUMMARY.md
@brandbook/BRAND-SPEC.md
</context>

<artifacts_this_phase_produces>
This plan (02) produces:
- MOVED threadline tests: `id_test.exs`, `telemetry_test.exs`, `plug/threadline_test.exs`, `live/threadline_test.exs`, `audit/ledger_test.exs`, `mix/tasks/{crosswake.gen.audit,crosswake.threadline}_test.exs`, and the `phase91/phase92/phase96` proof closeouts → the package lane (deleted from core)
- NEW `packages/crosswake_threadline/test/crosswake/proof/phase139_threadline_cleanroom_test.exs` — vacuity-safe canary proof (async: true; event_names/0 == 3, Plug.init/1, Ledger.actor_ref/2 hex, refute sibling deps)
- HARDENED `priv/templates/crosswake/audit/ledger.ex.eex` — try/rescue handler + on_conflict/conflict_target + advisory-hash moduledoc
- core anti-drift test + updated @baseline_forbidden_keys count assertion (in `test/crosswake/telemetry_test.exs`)
- DX (each own commit): `crosswake.threadline.ex` NO_COLOR/ASCII tree fallback + empty-result guard + posture microcopy; `crosswake.gen.audit.ex` "Next steps" rewrite + reused→skipping; `audit/ledger.ex` HMAC ArgumentError rewrite
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Move the threadline test suite into the package lane + add the anti-drift PII-floor test and update the baseline-count assertion</name>
  <files>packages/crosswake_threadline/test/crosswake/threadline/id_test.exs, packages/crosswake_threadline/test/crosswake/threadline/telemetry_test.exs, packages/crosswake_threadline/test/crosswake/plug/threadline_test.exs, packages/crosswake_threadline/test/crosswake/live/threadline_test.exs, packages/crosswake_threadline/test/crosswake/audit/ledger_test.exs, packages/crosswake_threadline/test/mix/tasks/crosswake.gen.audit_test.exs, packages/crosswake_threadline/test/mix/tasks/crosswake.threadline_test.exs, packages/crosswake_threadline/test/crosswake/proof/phase91_threadline_contract_closeout_test.exs, packages/crosswake_threadline/test/crosswake/proof/phase92_server_propagation_closeout_test.exs, packages/crosswake_threadline/test/crosswake/proof/phase96_threadline_docs_contract_test.exs, test/crosswake/threadline/, test/crosswake/plug/threadline_test.exs, test/crosswake/live/threadline_test.exs, test/crosswake/audit/ledger_test.exs, test/mix/tasks/crosswake.gen.audit_test.exs, test/mix/tasks/crosswake.threadline_test.exs, test/crosswake/proof/phase91_threadline_contract_closeout_test.exs, test/crosswake/proof/phase92_server_propagation_closeout_test.exs, test/crosswake/proof/phase96_threadline_docs_contract_test.exs, test/crosswake/telemetry_test.exs</files>
  <read_first>
    - 139-PATTERNS.md § Test moves (the 10-file MOVE table) — each moves verbatim, module names preserved so aliases resolve inside the package
    - 139-RESEARCH.md § What STAYS in Core (doctor_threadline_test.exs, support_matrix_test.exs threadline entries, telemetry_test.exs event atoms — these STAY) + § Recommended Package Structure (test tree)
    - the 10 core test files being moved: test/crosswake/threadline/{id,telemetry}_test.exs, test/crosswake/plug/threadline_test.exs, test/crosswake/live/threadline_test.exs, test/crosswake/audit/ledger_test.exs, test/mix/tasks/{crosswake.gen.audit,crosswake.threadline}_test.exs, test/crosswake/proof/{phase91_threadline_contract_closeout,phase92_server_propagation_closeout,phase96_threadline_docs_contract}_test.exs
    - test/crosswake/telemetry_test.exs (full — find ANY assertion on @baseline_forbidden_keys length/count OR a hardcoded 10-key list; L113 has the D-15 denylist comment — read the surrounding assertion); 139-01 SITE 2 changed the baseline, so any count assertion must update to the new universal-floor count
    - lib/crosswake/telemetry.ex (post-139-01: the expanded @baseline_forbidden_keys + companion_forbidden_keys aggregation in attach_default_logger/1) — the seam the anti-drift test reads
    - lib/crosswake/companion.ex + the :companions registry aggregation (how registered companions contribute forbidden_metadata_keys/0 at runtime) — the union side of the subset assertion
    - 139-CONTEXT.md §C (anti-drift: core_baseline ⊆ union(registered-companion forbidden_metadata_keys); do NOT assert the inverse) + § "grep for + update any existing test asserting the baseline count"
  </read_first>
  <action>
    MOVE all 10 threadline test files verbatim from core `test/` into the matching `packages/crosswake_threadline/test/` sub-paths (module names are preserved by Plan 01, so `Crosswake.Threadline.*` / `Crosswake.Plug.Threadline` / `Crosswake.Live.Threadline` / `Crosswake.Audit.Ledger` aliases resolve inside the package via the `{:crosswake, path:}` dep). Delete each from core. Do NOT move `test/crosswake/doctor/doctor_threadline_test.exs`, `test/crosswake/support_matrix/support_matrix_test.exs`, or the threadline-event-atom assertions in `test/crosswake/telemetry_test.exs` — those test core logic and STAY in core (RESEARCH § What STAYS in Core). If any moved test references a test-support helper not yet in the package, copy it into `packages/crosswake_threadline/test/support/`.
    BASELINE-COUNT UPDATE: grep `test/crosswake/telemetry_test.exs` for any assertion that pins `@baseline_forbidden_keys` to a specific length or an exact 10-key list. If present, update it to the post-Phase-139 universal-floor set/count established by Plan 01 SITE 2 (read the actual expanded list from telemetry.ex — do not guess the number). If no such assertion exists, note that in the SUMMARY.
    ANTI-DRIFT TEST: add a test (in `test/crosswake/telemetry_test.exs`, or a new `test/crosswake/proof/phase139_baseline_floor_drift_test.exs` if cleaner) asserting `MapSet.subset?(MapSet.new(@baseline_forbidden_keys or the core baseline accessor), MapSet.new(union of every registered-companion forbidden_metadata_keys/0))` — i.e. every core universal-floor key has companion provenance. Register the companions the union needs (via the runtime `:companions` registry / put_env, restoring in on_exit) so the union is populated and the assertion is non-vacuous. Do NOT assert the reverse subset — companion-domain keys legitimately exceed the floor (D-5). The test must FAIL LOUDLY if a future edit adds a core floor key with no companion provenance. Reference sigra's domain by concept in comments, not as a package atom (comment-text / grep hygiene).
  </action>
  <verify>
    <automated>cd packages/crosswake_threadline && mix test</automated>
    <automated>mix test --exclude requires_example_host test/crosswake/telemetry_test.exs</automated>
    <automated>test ! -d test/crosswake/threadline || test -z "$(ls -A test/crosswake/threadline 2>/dev/null)"</automated>
    <automated>test ! -f test/crosswake/audit/ledger_test.exs && test ! -f test/crosswake/plug/threadline_test.exs && echo core-threadline-tests-moved</automated>
  </verify>
  <acceptance_criteria>
    - all moved threadline unit + task + proof tests pass in the crosswake_threadline package lane
    - no threadline-internal test remains in core (test/crosswake/threadline, plug/threadline_test, live/threadline_test, audit/ledger_test, the 2 task tests, the 3 proof closeouts all gone from core)
    - doctor_threadline_test.exs, support_matrix_test.exs threadline entries, and the telemetry_test event-atom assertions STAY in core and pass
    - any baseline-count assertion updated to the post-139 universal-floor count (or SUMMARY notes none existed)
    - the anti-drift test asserts core_baseline ⊆ union(companion forbidden keys), is non-vacuous (companions registered), and does NOT assert the inverse
  </acceptance_criteria>
  <done>The threadline test suite lives in the package lane and is green; core keeps only its own threadline-touching tests; the PII floor is guarded by a non-vacuous subset drift test and the baseline-count assertion is reconciled.</done>
</task>

<task type="auto">
  <name>Task 2: Harden the generated gen.audit ledger.ex.eex handler (try/rescue never-reraise + on_conflict idempotency + advisory-hash moduledoc)</name>
  <files>packages/crosswake_threadline/priv/templates/crosswake/audit/ledger.ex.eex, packages/crosswake_threadline/test/mix/tasks/crosswake.gen.audit_test.exs</files>
  <read_first>
    - packages/crosswake_threadline/priv/templates/crosswake/audit/ledger.ex.eex (full — the generated host Ecto schema + the telemetry handler section that writes the audit row; find where the Repo write / Ecto.Multi happens and where the moduledoc goes)
    - packages/crosswake_threadline/priv/templates/crosswake/audit/migration.exs.eex (the idempotency_key column + UNIQUE index — confirm the conflict_target field name is :idempotency_key)
    - 139-CONTEXT.md §B (try/rescue → catch, Logger.error, NEVER reraise; on_conflict: :nothing + conflict_target: :idempotency_key; keep write SYNCHRONOUS in Ecto.Multi; hash-chain advisory moduledoc) + §deferred (async/batched + real hash-chain integrity are DEFERRED — do NOT add here). This task implements the D-7 observer-resilience decision (try/rescue crash-isolation + app_dir fix are the D-7 core).
    - 139-RESEARCH.md § try/rescue Crash Isolation (the exact handler pattern) + § Security Domain (the Repudiation → try/rescue mitigation; the advisory hash-chain note)
    - packages/crosswake_threadline/test/mix/tasks/crosswake.gen.audit_test.exs (the render/generate test — extend it to assert the generated output contains the rescue + on_conflict, without reraise)
  </read_first>
  <action>
    In the generated telemetry-handler section of `ledger.ex.eex`, wrap the audit-row write in `try do ... rescue e -> Logger.error("Audit ledger write failed for event #{inspect(event)}: #{Exception.message(e)}"); :ok end` — the handler MUST return `:ok` (or the telemetry-expected value) on failure and MUST NEVER reraise, because `:telemetry` auto-detaches a handler that raises (silent audit blackout). Ensure `require Logger` / `import Logger` is present in the generated module as needed.
    Add `on_conflict: :nothing` and `conflict_target: :idempotency_key` to the generated insert so replays are idempotent (confirm the column name against migration.exs.eex). Keep the write SYNCHRONOUS inside `Ecto.Multi` — the audit row commits atomically with the action it describes. Do NOT introduce async / GenServer / Oban (would break atomicity + add deps — DEFERRED).
    Add an `@moduledoc` note (or a clearly-marked section) to the generated ledger module stating that `row_hash`/`prev_hash` are an ADVISORY fingerprint (caller-supplied prev, hashes only a few fields, forks under concurrency), NOT tamper-evidence; the `idempotency_key` UNIQUE index is the real integrity guarantee; real integrity (Postgres trigger / Carbonite-style) is a deferred hardening phase.
    Extend `crosswake.gen.audit_test.exs` to render/generate the template and assert the generated source contains the rescue-without-reraise and the on_conflict/conflict_target. If asserting the presence of a literal that a negative-grep elsewhere might trip, phrase the assertion to match by concept or add a `<!-- planner-discipline-allow: ... -->` marker only where genuinely required.
  </action>
  <verify>
    <automated>cd packages/crosswake_threadline && mix test test/mix/tasks/crosswake.gen.audit_test.exs</automated>
    <automated>grep -q "rescue" packages/crosswake_threadline/priv/templates/crosswake/audit/ledger.ex.eex && grep -q "on_conflict" packages/crosswake_threadline/priv/templates/crosswake/audit/ledger.ex.eex && echo handler-hardened</automated>
    <automated>grep -vi "reraise\|do NOT reraise\|never reraise" packages/crosswake_threadline/priv/templates/crosswake/audit/ledger.ex.eex | grep -qi "advisory" && echo advisory-note-present</automated>
  </verify>
  <acceptance_criteria>
    - the generated handler wraps the write in try/rescue, logs via Logger.error, and NEVER reraises (keeps the handler attached)
    - the generated insert uses on_conflict: :nothing + conflict_target: :idempotency_key (matching the migration's UNIQUE index)
    - the write stays synchronous inside Ecto.Multi (no async/GenServer/Oban)
    - the ledger.ex.eex moduledoc marks row_hash/prev_hash advisory and names idempotency_key as the real integrity guarantee
    - the gen.audit render test asserts the rescue + on_conflict are present in generated output
  </acceptance_criteria>
  <done>The generated audit handler is crash-isolated (never reraises → no silent blackout) and replay-idempotent, the write stays atomically synchronous in Ecto.Multi, and the advisory-hash caveat is documented — verified by the extended render test.</done>
</task>

<task type="auto">
  <name>Task 3: Add the vacuity-safe clean-room ExUnit proof (module-shipment canaries + no-sibling-dep guard, async: true)</name>
  <files>packages/crosswake_threadline/test/crosswake/proof/phase139_threadline_cleanroom_test.exs</files>
  <read_first>
    - 139-RESEARCH.md § Pattern 2 (the drafted non-vacuous clean-room proof: event_names/0 == 3, Plug.init/1 header_name, Ledger.actor_ref/2 64-char hex, refute sibling deps) + § Key Differences from Chimeway (async: true, no setup, module-shipment canaries not companion dispatch)
    - 139-PATTERNS.md § phase139_threadline_cleanroom_test.exs (the drafted assertions + async: true note; contrast chimeway async: false)
    - packages/crosswake_chimeway/test/crosswake/proof/phase138_chimeway_cleanroom_test.exs (structural template — but threadline is NOT a companion, so drop the put_env setup + the enabled?/companion-dispatch assertions)
    - packages/crosswake_threadline/lib/crosswake/threadline/telemetry.ex event_names/0 (the 3 request-span names) + lib/crosswake/plug/threadline.ex init/1 (header_name "x-crosswake-thread-id") + lib/crosswake/audit/ledger.ex actor_ref/2 (HMAC → 64-char hex)
  </read_first>
  <action>
    Create `packages/crosswake_threadline/test/crosswake/proof/phase139_threadline_cleanroom_test.exs` with `use ExUnit.Case, async: true` (threadline is NOT a `:companions` registrant — no `Application.put_env`, no process-global mutation, so async is safe; contrast chimeway's async: false). NO setup block.
    Test 1 (Telemetry shipped): `Crosswake.Threadline.Telemetry.event_names()` is a list of length 3 containing `[:crosswake, :threadline, :request, :start]`, `[..., :stop]`, `[..., :exception]`, with a message that a missing/short list means the Telemetry module did not ship in the tarball.
    Test 2 (Plug shipped): `Crosswake.Plug.Threadline.init([])` returns a keyword list with `header_name == "x-crosswake-thread-id"`.
    Test 3 (Ledger shipped): `Crosswake.Audit.Ledger.actor_ref("test-user-id", secret: "test-secret")` is a binary of `byte_size == 64` (SHA-256 HMAC hex).
    Test 4 (vacuity guard): read `Mix.Project.config()[:deps]`, normalize each entry to its name, `refute :crosswake_sigra in dep_names` and `refute :crosswake_chimeway in dep_names` — proving zero-sibling-dep NON-vacuously (a real dep list checked, not an empty pass). Do NOT assert any companion-behaviour function (`enabled?/0` etc.) — threadline is not a Crosswake.Companion implementor.
  </action>
  <verify>
    <automated>cd packages/crosswake_threadline && mix test test/crosswake/proof/phase139_threadline_cleanroom_test.exs</automated>
    <automated>cd packages/crosswake_threadline && mix test</automated>
    <automated>grep -q "async: true" packages/crosswake_threadline/test/crosswake/proof/phase139_threadline_cleanroom_test.exs && echo async-true</automated>
  </verify>
  <acceptance_criteria>
    - the proof asserts event_names/0 == 3 (with the 3 request-span names), Plug.init/1 header_name, and actor_ref/2 → 64-char hex (all three module-shipment canaries)
    - the vacuity guard reads the real deps and refutes both crosswake_sigra and crosswake_chimeway (non-vacuous)
    - use ExUnit.Case, async: true; no setup / put_env; no companion-behaviour assertion
    - the full threadline package lane is green
  </acceptance_criteria>
  <done>A vacuity-safe clean-room proof exists proving all three threadline core modules shipped and that neither sibling companion is a dep — using module-shipment canaries (threadline is not a companion), async and setup-free.</done>
</task>

<task type="auto">
  <name>Task 4 (DX, own commit): NO_COLOR/ASCII tree fallback + empty-result guard + posture microcopy in mix crosswake.threadline</name>
  <files>packages/crosswake_threadline/lib/mix/tasks/crosswake.threadline.ex, packages/crosswake_threadline/test/mix/tasks/crosswake.threadline_test.exs</files>
  <read_first>
    - packages/crosswake_threadline/lib/mix/tasks/crosswake.threadline.ex (post-move) — render_durable/1 at ~L180 (the "Posture: Durable" line + the tree render); the box glyphs at ~L209 (tier_connector "└──"/"├──"), ~L220 (branch_prefix "│   "), ~L221 (event_connector); the ephemeral posture line at ~L62 ("Posture: Ephemeral. No ledger configured.")
    - 139-CONTEXT.md §D (NO_COLOR env + ASCII tree fallback +--/\--/|   ; empty-result guard "No events found for thread_id=…"; ephemeral posture line adds config-keys guidance; durable posture line "Posture: Durable — querying audit ledger"; each DX change its OWN commit; NO explicit --ascii flag — DEFERRED)
    - brandbook/BRAND-SPEC.md (voice/tone for the posture microcopy — verb-first, concrete, on-brand)
    - packages/crosswake_threadline/test/mix/tasks/crosswake.threadline_test.exs (extend for NO_COLOR fallback + empty-result + posture strings)
  </read_first>
  <action>
    Commit A (NO_COLOR/ASCII tree fallback): in `render_durable/1`, detect `System.get_env("NO_COLOR")` (per no-color.org: any non-nil value, including "", triggers fallback). When set, substitute the ASCII glyphs `+--` (for `├──`), `\--` (for `└──`), and `|   ` (for `│   `) wherever the Unicode box-drawing glyphs are emitted (tier_connector, event_connector, branch_prefix). Keep the Unicode glyphs when NO_COLOR is unset. This also aids screen-readers. Do NOT add an explicit `--ascii` flag (DEFERRED).
    Commit B (empty-result guard): in the durable path, when the ledger is queried and ZERO events match, print `No events found for thread_id=…` (interpolating the queried thread_id / actor_ref filter) instead of emitting only the bare "Posture: Durable" line with an empty tree. Keep the honest append-only "Other (unrecognized tier)" bucket behavior for non-empty results.
    Commit C (posture microcopy, brand-voice): rewrite the ephemeral posture line (~L62) to add config-keys guidance (name the :audit_repo / :audit_ledger keys the host must set to go durable); rewrite the durable posture line (~L181) to `Posture: Durable — querying audit ledger`. Match brandbook/BRAND-SPEC.md voice.
    Keep A, B, C as THREE separate commits (139-CONTEXT §D: each DX change its own commit, separate from the mechanical move). Extend `crosswake.threadline_test.exs` to cover: NO_COLOR yields ASCII glyphs; empty durable result prints the No-events line; the posture lines match the new copy.
  </action>
  <verify>
    <automated>cd packages/crosswake_threadline && mix test test/mix/tasks/crosswake.threadline_test.exs</automated>
    <automated>grep -q 'NO_COLOR' packages/crosswake_threadline/lib/mix/tasks/crosswake.threadline.ex && grep -q 'No events found' packages/crosswake_threadline/lib/mix/tasks/crosswake.threadline.ex && echo dx-wins-present</automated>
    <automated>grep -q 'querying audit ledger' packages/crosswake_threadline/lib/mix/tasks/crosswake.threadline.ex && echo durable-microcopy</automated>
  </verify>
  <acceptance_criteria>
    - NO_COLOR (any non-nil value) makes render_durable emit +-- / \-- / |   ASCII glyphs; unset keeps the Unicode box glyphs; no --ascii flag added
    - an empty durable query prints "No events found for thread_id=…" instead of a bare empty tree
    - the ephemeral posture line names the audit_repo/audit_ledger config keys; the durable line reads "Posture: Durable — querying audit ledger"
    - A/B/C are three separate commits; the task tests cover all three
  </acceptance_criteria>
  <done>mix crosswake.threadline honors NO_COLOR with an ASCII tree, guards empty results with a helpful line, and carries brand-voice posture copy — each change landed as its own commit.</done>
</task>

<task type="auto">
  <name>Task 5 (DX, own commit): brand-voice microcopy for gen.audit "Next steps" + reused→skipping, and the HMAC-secret ArgumentError</name>
  <files>packages/crosswake_threadline/lib/mix/tasks/crosswake.gen.audit.ex, packages/crosswake_threadline/lib/crosswake/audit/ledger.ex, packages/crosswake_threadline/test/mix/tasks/crosswake.gen.audit_test.exs, packages/crosswake_threadline/test/crosswake/audit/ledger_test.exs</files>
  <read_first>
    - packages/crosswake_threadline/lib/mix/tasks/crosswake.gen.audit.ex L54-63 (the "Next steps" info block: "1. Run your migrations: mix ecto.migrate", "2. Start recording audit events ... record_in_multi(:audit_event, %{...})") + L44/L80 (the "reused …" messages from ensure_file/2)
    - packages/crosswake_threadline/lib/crosswake/audit/ledger.ex L53-59 (the secret resolution: opts[:secret] || Application.get_env(:crosswake, :audit_hmac_secret); the `raise ArgumentError, "Missing audit HMAC secret in options or application environment"` at L56)
    - 139-CONTEXT.md §D (generator "Next steps": verb-first, concrete record_in_multi fields, closing `mix crosswake.threadline` CTA, note `mix ecto.create` if needed, reused→skipping; the HMAC-secret ArgumentError names BOTH resolution paths)
    - brandbook/BRAND-SPEC.md (voice/tone)
    - the two test files to extend (assert the new copy)
  </read_first>
  <action>
    Commit A (gen.audit "Next steps" + reused→skipping, brand-voice): rewrite the L54-63 "Next steps" block to be verb-first and concrete — number the steps, note `mix ecto.create` if the database does not yet exist (before `mix ecto.migrate`), show the concrete `record_in_multi` call with real field names (not `%{...}`), and close with a `mix crosswake.threadline` CTA to inspect the recorded timeline. Change the `ensure_file/2` "reused …" message (L44/L80) to a clearer verb like "skipping …" (already-exists semantics). Match brandbook/BRAND-SPEC.md voice.
    Commit B (HMAC ArgumentError, brand-voice): rewrite the `raise ArgumentError` message in `Audit.Ledger.actor_ref/2` (ledger.ex:56) to name BOTH resolution paths explicitly — the `:secret` option AND the `:audit_hmac_secret` application-env key — so a host hitting it knows exactly which two places to set it. Keep it a hard `ArgumentError` (fail-loud, not silent — this is the DoS mitigation).
    Keep A and B as SEPARATE commits (139-CONTEXT §D). Extend `crosswake.gen.audit_test.exs` (assert the new Next-steps copy + skipping message) and `ledger_test.exs` (assert the ArgumentError message names both paths).
  </action>
  <verify>
    <automated>cd packages/crosswake_threadline && mix test test/mix/tasks/crosswake.gen.audit_test.exs test/crosswake/audit/ledger_test.exs</automated>
    <automated>grep -q 'crosswake.threadline' packages/crosswake_threadline/lib/mix/tasks/crosswake.gen.audit.ex && grep -q 'skipping' packages/crosswake_threadline/lib/mix/tasks/crosswake.gen.audit.ex && echo gen-audit-microcopy</automated>
    <automated>grep -q 'audit_hmac_secret' packages/crosswake_threadline/lib/crosswake/audit/ledger.ex && echo hmac-msg-names-envkey</automated>
  </verify>
  <acceptance_criteria>
    - the gen.audit "Next steps" block is verb-first with a real record_in_multi call, a mix ecto.create note, and a closing mix crosswake.threadline CTA; the "reused" message became "skipping"
    - the HMAC ArgumentError names both the :secret option and the :audit_hmac_secret app-env key, and stays a hard raise (fail-loud)
    - A and B are separate commits; both test files assert the new copy
  </acceptance_criteria>
  <done>The generator's Next-steps and the HMAC-secret error read as brand-voice, actionable guidance naming concrete fields, keys, and the inspect CTA — landed as two isolated commits.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| generated audit handler → :telemetry runtime | a raising handler is auto-detached by :telemetry → silent audit blackout; the template must self-isolate |
| core @baseline_forbidden_keys → companion provenance | a core floor key without companion provenance signals drift; the subset test guards it |
| clean-room proof → tarball module shipment | with siblings absent, the canaries must prove the three core modules actually shipped (non-vacuous) |
| host operator → telemetry/gen.audit output | microcopy must not leak PII and must name the exact config keys/paths (fail-loud on missing HMAC secret) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-139-03 | Repudiation (silent audit blackout) | generated gen.audit telemetry handler | high | mitigate | The generated handler wraps the write in try/rescue, logs via Logger.error, and NEVER reraises — :telemetry keeps the handler attached; on_conflict: :nothing makes replays idempotent; the write stays synchronous in Ecto.Multi (atomic with the action). Verified by the render test grep (Task 2). |
| T-139-04 | Tampering (append-only ledger integrity misrepresented) | ledger.ex.eex hash-chain | medium | mitigate | The moduledoc marks row_hash/prev_hash ADVISORY (not tamper-evidence) so operators don't over-trust it; the idempotency_key UNIQUE index is the real guarantee; real integrity is a deferred hardening phase (no false security claim shipped). |
| T-139-07 | Tampering (unintended PII-floor weakening via test failure) | core @baseline_forbidden_keys assertion | medium | mitigate | The baseline-count assertion is reconciled to the post-139 universal-floor and the anti-drift subset test (core_baseline ⊆ union(companion keys), non-vacuous) fails loudly if a floor key loses companion provenance (Task 1, D-5). |
| T-139-08 | Tampering (vacuous clean-room) | phase139_threadline_cleanroom_test.exs | high | mitigate | The proof asserts three real module-shipment canaries (event_names/0 == 3, Plug.init/1 header, actor_ref/2 64-hex) AND refutes both sibling deps against the real dep list — a compile-only pass cannot satisfy it; async: true is safe (no companion registration) (Task 3). |
| T-139-16 | Information Disclosure (PII in timeline output) | mix crosswake.threadline render | medium | mitigate | The render path only displays event_type + timestamp + tier from host-owned rows; the DX changes (NO_COLOR glyphs, empty-result line, posture copy) add no new field to the output and the empty-result line echoes only the operator-supplied filter, not row PII (Task 4). |
| T-139-17 | Denial of Service (missing HMAC secret) | Audit.Ledger.actor_ref/2 | medium | mitigate | actor_ref/2 keeps its hard ArgumentError (fail-loud, not silent); the rewritten message names both the :secret option and the :audit_hmac_secret env key so the operator can fix it fast (Task 5). |
| T-139-SC | Tampering | npm/pip/cargo installs | low | accept | No package installs — test moves + template/microcopy edits only (RESEARCH Package Legitimacy Audit: first-party). No [ASSUMED]/[SUS] packages. |
</threat_model>

<verification>
- `cd packages/crosswake_threadline && mix test` — moved unit/task/proof tests + the new clean-room proof + the extended DX tests all green.
- `mix test --exclude requires_example_host test/crosswake/telemetry_test.exs` — the anti-drift subset test + reconciled baseline-count assertion pass in core; the retained core threadline tests pass.
- grep confirms: `rescue` + `on_conflict` in ledger.ex.eex; `NO_COLOR` + `No events found` + `querying audit ledger` in crosswake.threadline.ex; `skipping` + `crosswake.threadline` in crosswake.gen.audit.ex; `audit_hmac_secret` in ledger.ex; `async: true` in the clean-room proof.
- no threadline-internal test remains in core (`test/crosswake/threadline` empty; audit/plug threadline test files gone).
</verification>

<success_criteria>
- The threadline test suite moved to the package lane and is green; core keeps only its own threadline-touching tests (THREAD-01).
- The generated gen.audit handler is crash-isolated (try/rescue, never reraise), replay-idempotent (on_conflict), synchronous-in-Multi, and documents the advisory hash-chain (THREAD-02).
- The anti-drift PII-floor subset test is non-vacuous and the baseline-count assertion is reconciled (D-5).
- The vacuity-safe clean-room proof passes with module-shipment canaries and both siblings refuted.
- The four brand-voice DX wins are in place, each as its own isolated commit, separate from the mechanical move.
</success_criteria>

<output>
Create `.planning/phases/139-crosswake-threadline-extraction/139-02-SUMMARY.md` when done
</output>
