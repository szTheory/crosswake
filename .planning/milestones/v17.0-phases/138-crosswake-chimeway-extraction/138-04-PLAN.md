---
phase: 138-crosswake-chimeway-extraction
plan: 04
type: execute
wave: 4
depends_on: [138-03]
files_modified: []
autonomous: false
requirements: [CHIME-03]
user_setup:
  - service: hex-release-pr
    why: "Merging the chimeway Release PR IS the auditable human go/no-go before the irreversible Hex publish of crosswake_chimeway (D-8, D-9) — mirrors the 137 wave-5 sigra gate"
    dashboard_config:
      - task: "Merge the crosswake_chimeway Release PR only after dress-rehearsal + hex.publish --dry-run + clean-room lane (sigra absent) are green"
        location: "GitHub PR (release-please-generated)"
      - task: "Merge the auto-generated release-as-cleanup PR after the first chimeway Release PR merges"
        location: "GitHub PR (release-as-cleanup-*)"
must_haves:
  truths:
    - "the full pre-publish gate chain is green: path-dep dress rehearsal → hex.publish --dry-run → clean-room lane (crosswake + crosswake_chimeway, NOT crosswake_sigra)"
    - "the chimeway Release PR merge triggers the auto publish + post-publish clean-room + release-as cleanup"
    - "crosswake_chimeway 0.1.0 is live on Hex as an independent component, published AFTER sigra (sequential family order)"
  artifacts: []
  key_links:
    - "ordered gate: dry-run → clean-room (sigra absent) → HUMAN merge Release PR → auto publish → post-publish clean-room → auto cleanup PR → HUMAN merge cleanup PR"
    - "the clean-room lane is the CHIME-02 vacuity guard: it installs crosswake + crosswake_chimeway with sigra deliberately absent and asserts real notification behavior (chimeway-correct enabled? + Telemetry canary from Plan 03)"
---

<objective>
Execute the human-gated publish gate chain (CHIME-03). This plan is deliberately NON-autonomous: the
two human actions (merge the Release PR, merge the cleanup PR) are the auditable go/no-go points. The
automated verification (dry-run, clean-room with sigra absent) is confirmed here; the irreversible
Hex publish fires only on the human Release-PR merge. This mirrors Phase 137 wave 5 exactly, MINUS
the required-checks registration step — the merge-blocking-lane registration ship-gate is scheduled
for Phase 140 (FAMILY-04), so chimeway does NOT register required checks here (it relies on the same
already-registered lanes plus the new clean-room-proof-chimeway lane, which Phase 140 arms).

The ordered gate sequence: ① path-dep dress rehearsal (Plans 01-02, already green) → ②
`hex.publish --dry-run` from the package with CROSSWAKE_RELEASE=1 → ③ clean-room lane (crosswake +
crosswake_chimeway, NOT crosswake_sigra — the CHIME-02 vacuity guard) → ④ HUMAN merge Release PR →
⑤ auto publish → ⑥ post-publish clean-room from Hex → ⑦ auto failure-alert on any step 5-6 failure →
⑧ auto release-as cleanup PR → ⑨ HUMAN merge cleanup PR.

Purpose: chimeway is the SECOND irreversible Hex publish of v17.0 (after sigra). The merge is the
human decision; the dry-run + clean-room (with sigra deliberately absent) cover the irreversible risk
AND prove the no-sigra-dep invariant at publish time.
Output: crosswake_chimeway 0.1.0 live on Hex as an independent component; the one-shot release-as
0.1.0 pin stripped.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/138-crosswake-chimeway-extraction/138-RESEARCH.md
@.planning/phases/138-crosswake-chimeway-extraction/138-VALIDATION.md
@.planning/phases/138-crosswake-chimeway-extraction/138-03-SUMMARY.md
@script/extract_companion.md
</context>

<artifacts_this_phase_produces>
This plan (04) produces no source artifacts — it executes and verifies the publish gate chain and
performs the two human go/no-go actions. Outcome: crosswake_chimeway 0.1.0 published to Hex as an
independent component, and the one-shot release-as 0.1.0 pin stripped via the merged cleanup PR. The
merge-blocking-lane required-check registration is intentionally NOT done here (deferred to Phase 140
FAMILY-04, per the milestone plan).
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Verify the automated pre-publish gates (dress rehearsal + hex.publish --dry-run + clean-room readiness)</name>
  <files>packages/crosswake_chimeway/mix.exs</files>
  <read_first>
    - 138-03-SUMMARY.md (the CI lanes wired: publish-hex-chimeway, clean-room-proof-chimeway; the no-engine chimeway smoke fix)
    - 138-02-SUMMARY.md (the path-dep dress rehearsal already green; the non-vacuous cleanroom proof)
    - script/extract_companion.md publish steps (dry-run invocation)
    - 138-RESEARCH.md § Environment Availability (HEX_API_KEY, CROSSWAKE_RELEASE=1) + § Common Pitfalls (CROSSWAKE_RELEASE on all mix steps)
  </read_first>
  <action>
    Confirm the path-dep dress rehearsal from Plans 01-02 is still green (core suite excluding host/engine tags + the chimeway package lane). Then run the Hex dry-run from the package with the release env so hex.build sees the Hex dep (not the path dep) AND so the test-only crosswake_sigra dep is not compiled into the release build: `cd packages/crosswake_chimeway && CROSSWAKE_RELEASE=1 mix hex.publish --dry-run --yes`. Confirm it reports the correct package name (crosswake_chimeway), version (0.1.0), and the files: allowlist (lib, mix.exs, README, LICENSE, CHANGELOG — test/ excluded, so neither the phase71 sigra test-only dep nor any test ships). Do NOT run a real publish here — publishing fires only on the human Release-PR merge (Task 2). Record the dry-run output summary in the SUMMARY, explicitly noting the tarball file list excludes test/ (confirming the test-only sigra dep is not shipped — the CHIME-02 guarantee at build time).
  </action>
  <verify>
    <automated>cd packages/crosswake_chimeway && mix test</automated>
    <automated>mix test --exclude requires_example_host --exclude engine_present</automated>
  </verify>
  <acceptance_criteria>
    - dress rehearsal (core + chimeway package lanes) green
    - hex.publish --dry-run succeeds with the right name/version and a files: allowlist that excludes test/ (no real publish; test-only sigra dep confirmed absent from the tarball)
  </acceptance_criteria>
  <done>The automated pre-publish gates (dress rehearsal + dry-run) are confirmed green and the tarball excludes test/; the package is ready for the human go/no-go.</done>
</task>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 2 (HUMAN): Merge the chimeway Release PR (irreversible publish go/no-go)</name>
  <action>HUMAN go/no-go (non-autonomous): after all pre-publish gates are green — dress rehearsal, hex.publish --dry-run, and clean-room-proof-chimeway (crosswake + crosswake_chimeway, sigra absent) on main — merge the release-please-generated crosswake_chimeway 0.1.0 Release PR. This fires the irreversible Hex publish + post-publish clean-room. See how-to-verify for the exact ordered steps and the live-on-Hex confirmation.</action>
  <what-built>release-please generated the crosswake_chimeway Release PR (version 0.1.0). The dry-run + clean-room (sigra deliberately absent, chimeway-correct enabled? + Telemetry canary) + dress rehearsal are green; merging this PR is the auditable go/no-go that triggers the irreversible Hex publish.</what-built>
  <how-to-verify>
    1. Confirm the pre-publish gates are all green: dress rehearsal (Task 1), hex.publish --dry-run (Task 1), and clean-room-proof-chimeway on main (installs crosswake + crosswake_chimeway, NOT crosswake_sigra — the CHIME-02 vacuity guard).
    2. Open the release-please-generated Release PR for crosswake_chimeway and confirm it targets version 0.1.0 and the CHANGELOG is correct.
    3. MERGE the Release PR. This fires: auto publish-hex-chimeway (⑤) → post-publish clean-room from Hex (⑥) → and, on any step 5-6 failure, the auto failure-alert issue (⑦).
    4. After merge, confirm crosswake_chimeway 0.1.0 is live: `curl -s https://hex.pm/api/packages/crosswake_chimeway | python3 -c "import sys,json; d=json.load(sys.stdin); print([r['version'] for r in d['releases']])"` shows 0.1.0, and the post-publish clean-room-proof-chimeway job is green.
    Why human: this merge is the single irreversible go/no-go before the crosswake_chimeway Hex name/version coordinate is claimed (D-9). chimeway publishes AFTER sigra (sequential family order).
  </how-to-verify>
  <resume-signal>Type "published" once crosswake_chimeway 0.1.0 is live on Hex and the post-publish clean-room is green, or describe any publish/clean-room failure (check the auto failure-alert issue).</resume-signal>
</task>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 3 (HUMAN): Merge the release-as-cleanup PR (strip the one-shot 0.1.0 pin)</name>
  <action>HUMAN confirmation (non-autonomous): merge the auto-generated release-as-cleanup PR that strips the one-shot release-as "0.1.0" pin from the crosswake_chimeway block, after confirming it is scoped to chimeway only. See how-to-verify for the exact steps.</action>
  <what-built>The first chimeway Release PR merged, so the release-as-cleanup job auto-opened a PR stripping the one-shot release-as "0.1.0" pin from the crosswake_chimeway block in release-please-config.json (else every future chimeway release re-targets 0.1.0 forever).</what-built>
  <how-to-verify>
    1. Find the auto-generated release-as-cleanup-* PR: `gh pr list --search "release-as-cleanup"`.
    2. Confirm it removes the release-as (and _TODO_release_as) keys from the crosswake_chimeway block in release-please-config.json and touches nothing else (NOT the sigra or core blocks).
    3. MERGE the cleanup PR.
    4. Confirm the release-as-staleness-gate (if present) is green and the crosswake_chimeway block no longer contains release-as.
    Why human: confirmation that the one-shot strip is correct and scoped to chimeway only.
  </how-to-verify>
  <resume-signal>Type "cleanup-merged" once the release-as-cleanup PR is merged and the chimeway block no longer pins release-as, or describe any staleness-gate issue.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| human → Hex.pm (Release PR merge) | the single irreversible go/no-go; upstream gates cover the risk |
| clean-room lane → sigra absence | the post-publish clean-room installs crosswake + crosswake_chimeway from Hex WITHOUT crosswake_sigra — the CHIME-02 invariant proven against the real published tarball |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-138-13 | Tampering (supply chain) | irreversible Hex publish | high | mitigate | The publish fires only on the human Release-PR merge, after dry-run + clean-room (sigra absent) + dress rehearsal are green; a bad version is patch-recoverable, only the name/version coordinate is irreversible and the gate chain protects it (D-9). |
| T-138-14 | Elevation of Privilege (inter-companion dep at publish) | tarball contents | high | mitigate | The dry-run confirms the files: allowlist excludes test/, so the phase71 test-only crosswake_sigra dep never ships; the post-publish clean-room installs crosswake + crosswake_chimeway from Hex WITHOUT sigra and passes — proving the no-sigra-dep invariant against the real tarball (CHIME-02). |
| T-138-15 | Tampering (stale version) | release-as one-shot | high | mitigate | The auto release-as-cleanup PR strips the 0.1.0 pin after first merge; human confirms scope-to-chimeway and merges (Task 3). Staleness gate catches a missed strip. |
| T-138-16 | Repudiation | Release PR = audit record | low | accept | The Release-PR merge is the auditable human decision of record; no extra environment-protection approval is added (0-recurring-intervention principle). |
| T-138-SC | Tampering | npm/pip/cargo installs | low | accept | No package installs; this plan runs the publish of the first-party crosswake_chimeway package only (RESEARCH Package Legitimacy Audit: first-party). |
</threat_model>

<verification>
- Task 1: dress rehearsal (core + chimeway package) green; hex.publish --dry-run succeeds with a test-excluding files: allowlist (no real publish).
- Task 2 (human): Release PR merged → crosswake_chimeway 0.1.0 live on Hex; post-publish clean-room-proof-chimeway green (sigra absent).
- Task 3 (human): release-as-cleanup PR merged → the one-shot 0.1.0 pin stripped from the chimeway block.
</verification>

<success_criteria>
- The full ordered gate chain executed: dry-run + clean-room (sigra absent) green, Release PR merged, publish + post-publish clean-room green, cleanup PR merged.
- crosswake_chimeway 0.1.0 is live on Hex as an independent component, published after sigra (sequential family order — CHIME-03).
- The no-sigra-dep invariant is proven against the real published tarball by the post-publish clean-room.
- The one-shot release-as pin is stripped; required-check registration remains deferred to Phase 140.
</success_criteria>

<output>
Create `.planning/phases/138-crosswake-chimeway-extraction/138-04-SUMMARY.md` when done
</output>
