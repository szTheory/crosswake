---
phase: 137-crosswake-sigra-extraction
plan: 05
type: execute
wave: 5
depends_on: [137-04]
files_modified: []
autonomous: false
requirements: [SIGRA-03]
user_setup:
  - service: github-branch-protection
    why: "Register merge-blocking-* required checks (carried admin ship-gate + new clean-room-proof-sigra lane) — branch-protection mutation is a deliberate human admin action, harness-blocked"
    dashboard_config:
      - task: "Run DRY_RUN=0 script/register_required_checks.sh AFTER clean-room-proof-sigra is green once on main (green-first preflight dodges the 'expected — waiting for status' deadlock)"
        location: "Local terminal with admin-scoped gh CLI"
  - service: hex-release-pr
    why: "Merging the sigra Release PR IS the auditable human go/no-go before the irreversible Hex publish (D-137-C)"
    dashboard_config:
      - task: "Merge the crosswake_sigra Release PR only after dress-rehearsal + hex.publish --dry-run + clean-room lane are green"
        location: "GitHub PR (release-please-generated)"
      - task: "Merge the auto-generated release-as-cleanup PR after the first sigra Release PR merges"
        location: "GitHub PR (release-as-cleanup-*)"
must_haves:
  truths:
    - "the full pre-publish gate chain is green: dress rehearsal → hex.publish --dry-run → clean-room lane"
    - "merge-blocking required checks (incl clean-room-proof-sigra) are registered after the lane is green once on main"
    - "the sigra Release PR merge triggers the auto publish + post-publish clean-room + release-as cleanup"
  artifacts: []
  key_links:
    - "ordered gate: HUMAN register checks → dry-run → clean-room → HUMAN merge Release PR → auto publish → post-publish clean-room → auto cleanup PR → HUMAN merge cleanup PR"
    - "register_required_checks.sh green-first preflight — prevents the required-check-before-green deadlock"
---

<objective>
Execute the human-gated publish gate chain (SIGRA-03 / D-137-C). This plan is deliberately
NON-autonomous: the three human actions (register required checks, merge the Release PR, merge the
cleanup PR) are the auditable go/no-go points. The automated verification (dry-run, clean-room) is
confirmed here; the irreversible Hex publish fires only on the human Release-PR merge.

The ordered gate sequence (D-137-C): ① HUMAN register required checks (after clean-room green once
on main) → ② path-dep dress rehearsal (Plan 03, already green) → ③ `hex.publish --dry-run` → ④
clean-room lane (crosswake + crosswake_sigra only) → ⑤ HUMAN merge Release PR → ⑥ auto publish → ⑦
post-publish clean-room from Hex → ⑧ auto failure-alert on any step 6-7 failure → ⑨ auto release-as
cleanup PR → ⑩ HUMAN merge cleanup PR.

Purpose: Sigra is the first irreversible Hex publish of v17.0 and it is the auth companion. The
merge is the human decision; the dry-run + clean-room cover the irreversible risk. Also clears the
carried admin ship-gate (register merge-blocking-* lanes) at the right moment (green-first).
Output: crosswake_sigra 0.1.0 live on Hex; required checks registered; release-as pin stripped.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/137-crosswake-sigra-extraction/137-CONTEXT.md
@.planning/phases/137-crosswake-sigra-extraction/137-VALIDATION.md
@.planning/phases/137-crosswake-sigra-extraction/137-04-SUMMARY.md
@script/register_required_checks.sh
</context>

<artifacts_this_phase_produces>
This plan (05) produces no source artifacts — it executes and verifies the publish gate chain and
performs the three human go/no-go actions. Outcome: crosswake_sigra 0.1.0 published to Hex,
merge-blocking required checks registered (incl clean-room-proof-sigra), and the one-shot
release-as: 0.1.0 pin stripped via the merged cleanup PR.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Verify the automated pre-publish gates (dress rehearsal + hex.publish --dry-run)</name>
  <files>packages/crosswake_sigra/mix.exs</files>
  <read_first>
    - 137-CONTEXT.md D-137-C ordered gate sequence (steps ①-⑩)
    - 137-03-SUMMARY.md (dress rehearsal already green)
    - script/extract_companion.md publish steps (dry-run invocation)
    - 137-RESEARCH.md § Environment Availability (HEX_API_KEY, CROSSWAKE_RELEASE=1)
  </read_first>
  <action>
    Confirm the path-dep dress rehearsal from Plan 03 is still green (core suite + package lane). Then run the Hex dry-run from the package with the release env so hex.build sees the Hex dep (not the path dep): `cd packages/crosswake_sigra && CROSSWAKE_RELEASE=1 mix hex.publish --dry-run --yes`. Confirm it reports the correct package name (crosswake_sigra), version (0.1.0), and the `files:` allowlist (lib, mix.exs, README, LICENSE, CHANGELOG — test/ excluded). Do NOT run a real publish here — publishing fires only on the human Release-PR merge (Task 3). Record the dry-run output summary in the SUMMARY.
  </action>
  <verify>
    <automated>cd packages/crosswake_sigra && mix test</automated>
    <automated>mix test --exclude requires_example_host --exclude engine_present</automated>
  </verify>
  <acceptance_criteria>
    - dress rehearsal (core + package lanes) green
    - hex.publish --dry-run succeeds with the right name/version/files (no real publish)
  </acceptance_criteria>
  <done>The automated pre-publish gates (dress rehearsal + dry-run) are confirmed green; the package is ready for the human go/no-go.</done>
</task>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 2 (HUMAN): Register merge-blocking required checks green-first</name>
  <action>HUMAN admin action (harness-blocked, non-autonomous): after clean-room-proof-sigra is green once on main, run `DRY_RUN=0 script/register_required_checks.sh` with admin-scoped gh to register the merge-blocking-* required checks (incl clean-room-proof-sigra) green-first. See how-to-verify for the exact ordered steps.</action>
  <what-built>The clean-room-proof-sigra CI lane and per-component publish jobs (Plan 04) are wired; the carried admin ship-gate requires the merge-blocking-* required checks (incl clean-room-proof-sigra) to be registered on main.</what-built>
  <how-to-verify>
    1. Push the phase branch and let CI run; confirm `clean-room-proof-sigra` (and the other merge-blocking-* lanes) is GREEN once on `main` (or the integration branch that lands to main): `gh run list --branch main --limit 15`.
    2. ONLY after it is green once, run the registrar with admin-scoped gh on your machine: `DRY_RUN=0 script/register_required_checks.sh`. Its green-first preflight prevents the "Expected — Waiting for status" deadlock that would otherwise block every PR.
    3. Confirm the required checks are now registered: re-run the script in DRY_RUN mode or check branch protection.
    Why human: branch-protection mutation is a deliberate admin action the harness cannot (and must not) perform autonomously.
  </how-to-verify>
  <resume-signal>Type "registered" once the merge-blocking-* required checks (incl clean-room-proof-sigra) are green-first registered, or describe any deadlock/preflight issue.</resume-signal>
</task>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 3 (HUMAN): Merge the sigra Release PR (irreversible publish go/no-go)</name>
  <action>HUMAN go/no-go (non-autonomous): after all pre-publish gates are green, merge the release-please-generated crosswake_sigra 0.1.0 Release PR — this fires the irreversible Hex publish + post-publish clean-room. See how-to-verify for the exact ordered steps and the live-on-Hex confirmation.</action>
  <what-built>release-please generated the crosswake_sigra Release PR (version 0.1.0). The dry-run + clean-room + dress rehearsal are green; merging this PR is the auditable go/no-go that triggers the irreversible Hex publish.</what-built>
  <how-to-verify>
    1. Confirm the pre-publish gates are all green: dress rehearsal (Task 1), `hex.publish --dry-run` (Task 1), and `clean-room-proof-sigra` on main (Task 2 checkpoint).
    2. Open the release-please-generated Release PR for `crosswake_sigra` and confirm it targets version 0.1.0 and the CHANGELOG is correct.
    3. MERGE the Release PR. This fires: auto publish-hex-sigra (⑥) → post-publish clean-room from Hex (⑦) → and, on any step 6-7 failure, the auto failure-alert issue (⑧).
    4. After merge, confirm crosswake_sigra 0.1.0 is live: `curl -s https://hex.pm/api/packages/crosswake_sigra | python3 -c "import sys,json; d=json.load(sys.stdin); print([r['version'] for r in d['releases']])"` shows 0.1.0, and the post-publish clean-room job is green.
    Why human: this merge is the single irreversible go/no-go before the Hex name/version coordinate is claimed (D-137-C).
  </how-to-verify>
  <resume-signal>Type "published" once crosswake_sigra 0.1.0 is live on Hex and the post-publish clean-room is green, or describe any publish/clean-room failure (check the auto failure-alert issue).</resume-signal>
</task>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 4 (HUMAN): Merge the release-as-cleanup PR (strip the one-shot 0.1.0 pin)</name>
  <action>HUMAN confirmation (non-autonomous): merge the auto-generated release-as-cleanup PR that strips the one-shot `release-as: "0.1.0"` pin from the crosswake_sigra block, after confirming it is scoped to sigra only. See how-to-verify for the exact steps.</action>
  <what-built>The first sigra Release PR merged, so the release-as-cleanup job auto-opened a PR stripping the one-shot `release-as: "0.1.0"` pin from release-please-config.json (else every future sigra release re-targets 0.1.0 forever).</what-built>
  <how-to-verify>
    1. Find the auto-generated `release-as-cleanup-*` PR: `gh pr list --search "release-as-cleanup"`.
    2. Confirm it removes the `release-as` (and `_TODO_release_as`) keys from the crosswake_sigra block in release-please-config.json and touches nothing else.
    3. MERGE the cleanup PR.
    4. Confirm the `release-as-staleness-gate` (if present) is green and the crosswake_sigra block no longer contains `release-as`.
    Why human: confirmation that the one-shot strip is correct and scoped to sigra only.
  </how-to-verify>
  <resume-signal>Type "cleanup-merged" once the release-as-cleanup PR is merged and the sigra block no longer pins release-as, or describe any staleness-gate issue.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| human → Hex.pm (Release PR merge) | the single irreversible go/no-go; upstream gates cover the risk |
| human → branch protection | required-check registration must be green-first to avoid a self-inflicted merge deadlock |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-137-17 | Tampering (supply chain) | irreversible Hex publish | high | mitigate | The publish fires only on the human Release-PR merge, after dry-run + clean-room + dress rehearsal are green; a bad version is patch-recoverable, only the name/version coordinate is irreversible and the gate chain protects it (D-137-C). |
| T-137-18 | Denial of Service (merge deadlock) | register_required_checks.sh | medium | mitigate | Register required checks ONLY after clean-room-proof-sigra is green once on main; the script's green-first preflight prevents the "expected — waiting for status" deadlock (Task 2 checkpoint ordering). |
| T-137-19 | Tampering (stale version) | release-as one-shot | high | mitigate | The auto release-as-cleanup PR strips the 0.1.0 pin after first merge; human confirms scope-to-sigra and merges (Task 4). Staleness gate catches a missed strip. |
| T-137-20 | Repudiation | Release PR = audit record | low | accept | The Release-PR merge is the auditable human decision of record; no extra environment-protection approval is added (D-137-C, 0-recurring-intervention principle). |
| T-137-SC | Tampering | npm/pip/cargo installs | low | accept | No package installs; this plan runs the publish of the first-party crosswake_sigra package only (RESEARCH Package Legitimacy Audit: first-party). |
</threat_model>

<verification>
- Task 1: dress rehearsal (core + package) green; `hex.publish --dry-run` succeeds (no real publish).
- Task 2 (human): clean-room-proof-sigra green once on main, then required checks registered green-first.
- Task 3 (human): Release PR merged → crosswake_sigra 0.1.0 live on Hex; post-publish clean-room green.
- Task 4 (human): release-as-cleanup PR merged → the one-shot 0.1.0 pin stripped.
</verification>

<success_criteria>
- The full ordered gate chain (D-137-C ①-⑩) executed: dry-run + clean-room green, Release PR merged, publish + post-publish clean-room green, cleanup PR merged.
- crosswake_sigra 0.1.0 is live on Hex as an independent component.
- merge-blocking required checks (incl clean-room-proof-sigra) are registered green-first.
</success_criteria>

<output>
Create `.planning/phases/137-crosswake-sigra-extraction/137-05-SUMMARY.md` when done
</output>
