---
phase: 140-family-discipline-close
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/COMPANION-PUBLISH-RUNBOOK.md
autonomous: true
requirements: [FAMILY-04]
must_haves:
  truths:
    - "A publish-readiness runbook doc exists at docs/COMPANION-PUBLISH-RUNBOOK.md documenting the exact human steps to publish sigra → chimeway → threadline sequentially, one Release PR at a time."
    - "The runbook documents that sequencing is RUNBOOK discipline, NOT GitHub Actions needs: edges — and explains why cross-companion needs edges cannot span workflow runs."
    - "The runbook documents register_required_checks.sh green-first ordering (DRY_RUN=1 then DRY_RUN=0, after new lanes go green on main) and that publish-hex-*/clean-room-proof-* jobs must NOT be registered as required checks."
    - "The runbook verifies (and cites the confirmed on-disk locations of) the already-present pipeline primitives: per-component release-as config, separate-pull-requests:true, publish-hex-<name> + clean-room-proof-<name> jobs, and release-failure-alert."
  artifacts:
    - docs/COMPANION-PUBLISH-RUNBOOK.md
  key_links:
    - "The runbook connects release-please.yml behavior → the human operator → hex.pm; it is documentation, not new CI edges."
  prohibitions:
    - "MUST NOT publish anything to Hex, run register_required_checks.sh, merge any Release PR, or add/modify any CI workflow edge in this plan — this plan authors readiness documentation only."
    - "MUST NOT add cross-companion `needs:` edges to release-please.yml (they cannot span workflow runs and would silently skip or deadlock)."
---

<objective>
Author the publish-readiness runbook and document the ship-gate ordering, so the deferred batched
family publish can be executed later by a human with zero ambiguity. Implements the readiness half
of FAMILY-04 per locked decisions D-01, D-02, D-03, D-04.

This plan is READINESS ONLY. Per D-01 (USER-LOCKED), Phase 140 delivers Waves 1–3 autonomously; the
actual hex publish + `register_required_checks.sh DRY_RUN=0` registration is a SEPARATE human trigger
fired after origin-sync + Phase 137/138/139 execution-verification (see plan 05, which is the
`autonomous: false` execution plan). No publish and no registration happen here.

All the pipeline machinery already exists and is verified present in-repo: the three companions are
registered as independent release-please components (`separate-pull-requests: true` + per-component
`release-as: "0.1.0"`), each has a `publish-hex-<name>` job and a within-run
`clean-room-proof-<name>: needs: [release-please, publish-hex-<name>]` job, and `release-failure-alert`
fires on `failure()`. The remaining deliverable is the human-facing runbook that wraps these
primitives with the correct, safe operating discipline.

Purpose: Hex is irreversible after a ~60-minute grace window. The runbook is the safety rail — it
enforces "one Release PR at a time, verify green + hexdocs resolves + merge the cleanup PR, only then
next," in the fixed order sigra → chimeway → threadline (observer last, belt-and-suspenders).
Output: `docs/COMPANION-PUBLISH-RUNBOOK.md`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/140-family-discipline-close/140-CONTEXT.md
@docs/MILESTONE-BOUNDARY-HYGIENE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify the pipeline primitives are present and record their exact locations</name>
  <read_first>
    - release-please-config.json (confirm the three companion component blocks with separate-pull-requests:true + release-as + _TODO_release_as)
    - .release-please-manifest.json (confirm the three "packages/crosswake_<name>": "0.1.0" baselines)
    - .github/workflows/release-please.yml (confirm publish-hex-sigra ~L350, publish-hex-chimeway ~L441, publish-hex-threadline ~L532; clean-room-proof-sigra ~L1090, -chimeway ~L1124, -threadline ~L1162; release-failure-alert ~L1258)
    - script/register_required_checks.sh (confirm the green-first preflight and DRY_RUN semantics)
  </read_first>
  <files>docs/COMPANION-PUBLISH-RUNBOOK.md</files>
  <action>
    This task is a verification pass whose OUTPUT is the "Pipeline primitives (verified present)"
    section of the new runbook doc. Do NOT modify any workflow, config, or script — only read them and
    record confirmed facts.

    Grep/read each file above and confirm, then write into docs/COMPANION-PUBLISH-RUNBOOK.md a section
    that lists, for each of sigra/chimeway/threadline, the exact confirmed locations of: (1) the
    release-please-config.json component block (with `separate-pull-requests: true` and the one-shot
    `release-as: "0.1.0"` bootstrap pin + its `_TODO_release_as` note), (2) the manifest baseline in
    .release-please-manifest.json, (3) the `publish-hex-<name>` job in release-please.yml gated on
    `<name>_release_created == 'true'`, (4) the within-run `clean-room-proof-<name>` job with
    `needs: [release-please, publish-hex-<name>]`, and (5) that `release-failure-alert` lists all
    companion publish + clean-room jobs in its `needs:` and fires on `if: ${{ failure() }}`.

    State plainly in this section that NO new CI wiring is required for the publish (D-02): the
    ordering primitive is the within-run `clean-room-proof needs: publish-hex-<name>` edge, and
    cross-companion sequencing is the human's job via the runbook, because GitHub `needs:` cannot span
    workflow runs (each companion's Release PR merges in its own run).

    If any expected primitive is MISSING or malformed, do NOT invent it — record it as a BLOCKER in the
    runbook's readiness section and surface it in the plan SUMMARY (the publish cannot proceed until a
    follow-up fixes it). Based on the current repo it is expected all primitives are present.
  </action>
  <verify>
    <automated>grep -c 'publish-hex-sigra\|publish-hex-chimeway\|publish-hex-threadline' .github/workflows/release-please.yml</automated>
  </verify>
  <acceptance_criteria>
    - docs/COMPANION-PUBLISH-RUNBOOK.md contains a "Pipeline primitives (verified present)" section naming, per companion, the release-please-config block, manifest baseline, publish-hex job, clean-room-proof job, and the release-failure-alert coverage — each with a file reference.
    - The section explicitly states no cross-companion `needs:` edge exists or should be added (D-02) and explains the cross-run-skip reason.
    - No workflow/config/script file was modified: `git diff --name-only` in the executor's run shows only docs/COMPANION-PUBLISH-RUNBOOK.md (plus the SUMMARY) changed by this plan.
    - Any missing primitive is recorded as an explicit BLOCKER rather than silently omitted.
  </acceptance_criteria>
  <done>The runbook's "Pipeline primitives (verified present)" section records the confirmed on-disk locations of every publish primitive for all three companions, with no CI files modified.</done>
</task>

<task type="auto">
  <name>Task 2: Write the sequential publish runbook and the ship-gate ordering</name>
  <read_first>
    - docs/MILESTONE-BOUNDARY-HYGIENE.md (the origin-sync-before-publish sequencing this runbook waits on)
    - script/register_required_checks.sh (green-first preflight, DRY_RUN=1/DRY_RUN=0 semantics, the "green-first" deadlock-avoidance note)
    - .planning/phases/140-family-discipline-close/140-CONTEXT.md (D-01..D-04 — the binding sequencing contract)
  </read_first>
  <files>docs/COMPANION-PUBLISH-RUNBOOK.md</files>
  <action>
    Complete docs/COMPANION-PUBLISH-RUNBOOK.md with the operating procedure. Sections to author:

    (Preconditions) State the batched-publish preconditions per D-01: origin-sync has landed (local
    main → origin, CI green — cite docs/MILESTONE-BOUNDARY-HYGIENE.md), and Phases 137/138/139 are
    execution-verified. Note "deferred ≠ indefinite" — the family should go live once these are done.

    (Per-companion publish loop, D-03) Document the exact repeatable loop, to be run for ONE companion
    at a time in the fixed order sigra → chimeway → threadline (observer last, belt-and-suspenders):
      1. Merge exactly ONE Release PR (the companion's release-please PR). One at a time — never batch.
      2. Wait for `publish-hex-<name>` AND `clean-room-proof-<name>` to go GREEN on main.
      3. Confirm `hexdocs.pm/crosswake_<name>/0.1.0` resolves (the docs actually publish).
      4. Merge that companion's auto-opened `release-as-cleanup` PR (strips the one-shot `release-as`
         pin so subsequent runs don't re-target 0.1.0 forever — the `merge-blocking-release-as-staleness`
         gate stays RED until this merges).
      5. ONLY THEN proceed to the next companion.
    Include the ~60-minute Hex revert-window note: a mistaken publish is corrected with `mix hex.retire`
    within the grace window, NOT by re-pushing; do NOT retry a failed publish without first checking the
    revert window. Include the hard stop: "if `release-failure-alert` fires (auto-opened tracking issue),
    STOP — investigate the failed job before touching the next companion."

    (Ship-gate ordering, D-04) Document `register_required_checks.sh` green-first ordering as a separate
    subsection: after Phase 140 lands and any NEW `merge-blocking-*` lane has run GREEN at least once on
    main, run `DRY_RUN=1 script/register_required_checks.sh` (preview), then `DRY_RUN=0
    script/register_required_checks.sh` (apply), then let the daily `required-checks-audit.yml` confirm.
    State the hard rule: the companion `publish-hex-*` and `clean-room-proof-*` jobs are POST-MERGE jobs
    that skip on normal PRs and MUST NOT be registered as required checks — requiring them creates a
    permanent PR deadlock. Explain the green-first reason: requiring a check that never ran green on HEAD
    freezes every open PR ("Expected — Waiting for status").

    (Scope reconciliation, D-01) Add a short "Why this is deferred, and what counts as done for Phase 140"
    note stating that authoring + verifying this runbook (readiness) IS the complete Phase-140 deliverable
    for FAMILY-04; the publish + registration EXECUTION is a separate human trigger (plan 05,
    `autonomous: false`) fired outside this phase. This makes "readiness delivered, execution deferred by
    design" explicit for downstream execute-phase and the checker.

    Voice: adopter/operator-facing, imperative, concise (reference prompts/crosswake-brand-book.md tone if
    helpful). This is a doc — no fenced automation that publishes anything.
  </action>
  <verify>
    <automated>grep -c 'sigra → chimeway → threadline\|sigra -> chimeway -> threadline' docs/COMPANION-PUBLISH-RUNBOOK.md</automated>
  </verify>
  <acceptance_criteria>
    - docs/COMPANION-PUBLISH-RUNBOOK.md documents the five-step per-companion loop (merge one Release PR → wait publish+clean-room green → confirm hexdocs resolves → merge release-as-cleanup PR → next) in the fixed order sigra → chimeway → threadline.
    - The runbook contains the ~60-minute Hex revert-window note naming `mix hex.retire` (not re-push) and the "if release-failure-alert fires, STOP" instruction.
    - The runbook documents `register_required_checks.sh` DRY_RUN=1-then-DRY_RUN=0 green-first ordering and states that publish-hex-*/clean-room-proof-* MUST NOT be registered as required checks.
    - The runbook contains the scope-reconciliation note stating readiness = complete Phase-140 FAMILY-04 deliverable, execution deferred to the autonomous:false plan.
    - No hex publish, no registration, and no CI-edge change is performed by this plan.
  </acceptance_criteria>
  <done>The runbook fully documents the sequential per-companion publish loop, the Hex revert-window + failure-alert stop rules, the green-first ship-gate ordering with the required-check exclusion, and the readiness-vs-execution scope reconciliation.</done>
</task>

</tasks>

<artifacts>
## Artifacts this phase produces

- `docs/COMPANION-PUBLISH-RUNBOOK.md` — NEW readiness runbook:
  - "Pipeline primitives (verified present)" section citing per-companion release-please-config blocks, manifest baselines, publish-hex-<name> + clean-room-proof-<name> jobs, release-failure-alert
  - Preconditions (origin-sync + 137/138/139 execution-verified)
  - Per-companion publish loop (D-03) in fixed order sigra → chimeway → threadline
  - Hex ~60-min revert-window (`mix hex.retire`) + "if release-failure-alert fires, STOP"
  - `register_required_checks.sh` green-first ordering (DRY_RUN=1→0) + required-check exclusion for publish/clean-room jobs (D-04)
  - Scope-reconciliation note (readiness = done; execution deferred to autonomous:false plan)
</artifacts>

<verification>
- `docs/COMPANION-PUBLISH-RUNBOOK.md` exists and covers all four FAMILY-04 sub-decisions (D-01..D-04).
- No CI workflow, release-please config, manifest, or script file is modified by this plan.
- Nothing is published and no required check is registered.
</verification>

<success_criteria>
FAMILY-04 readiness satisfied: the human-facing publish runbook + ship-gate ordering are authored and
grounded in the verified-present pipeline primitives, with the readiness-vs-execution boundary made
explicit. Execution is carried by plan 05 (autonomous: false), fired separately by the human.
</success_criteria>

<output>
Create `.planning/phases/140-family-discipline-close/140-04-SUMMARY.md` when done.
</output>
