---
phase: 136-core-decoupling
plan: 06
type: execute
wave: 4
depends_on: ["136-02", "136-03", "136-04", "136-05"]
gap_closure: true
files_modified:
  - lib/crosswake/companions/sigra.ex
  - lib/crosswake/companions/chimeway.ex
  - lib/crosswake/support_matrix/support_matrix.ex
  - mix.exs
  - .planning/REQUIREMENTS.md
autonomous: true
requirements: [DECOUPLE-03, DECOUPLE-01, DECOUPLE-02, DECOUPLE-04, DECOUPLE-05]
must_haves:
  truths:
    - "Crosswake.Companions.Sigra implements @behaviour Crosswake.Companion with all six required callbacks plus auth_authority?/0 (true), evaluate_auth/3 (delegating to Sigra.Evaluator), denial_codes/0, forbidden_metadata_keys/0, and telemetry_events/0."
    - "evaluate_auth/3 returns {:allow, map()} | {:deny, Crosswake.Shell.Denial.t()} — reason :step_up_required preserved exactly as the pre-inversion Sigra.Evaluator produced it."
    - "The :companions registry is configured with Sigra (and Chimeway) via the `env:` key in `application/0` in mix.exs (Crosswake is a Hex library with no config/ dir) so the whole suite and dev see them without per-test put_env; `Application.get_env(:crosswake, :companions, [])` returns the two-module list in dev and test."
    - "SupportMatrix.auth_contract_truth/0 returns a row whose telemetry.event_names, telemetry.metadata_keys, telemetry.forbidden_metadata_keys, denial_codes, and safe_detail_keys are populated at runtime from the registered auth-authority companion (not [] sentinels)."
    - "mix test --exclude requires_example_host --exclude advisory_only reports 0 failures (down from 34)."
    - "mix compile --warnings-as-errors exits 0 and Crosswake.CompanionGuard.assert_no_static_refs!() exits 0."
    - "REQUIREMENTS.md DECOUPLE-03 is marked Complete in both the checkbox and the tracking table."
  artifacts:
    - "lib/crosswake/companions/sigra.ex (NEW — full companion facade delegating to Crosswake.Companions.Sigra.* sub-modules)"
    - "lib/crosswake/companions/chimeway.ex (extended with telemetry_events/0, forbidden_metadata_keys/0, and denial_codes/0 if Chimeway owns denial codes; auth_authority?/0 absent or false)"
    - "lib/crosswake/support_matrix/support_matrix.ex (auth_contract_truth/0 aggregates telemetry event_names/metadata_keys/forbidden_metadata_keys/denial_codes/safe_detail_keys from the auth-authority companion at runtime)"
    - "mix.exs (`application/0` gains `env: [companions: [Crosswake.Companions.Sigra, Crosswake.Companions.Chimeway]]` — the in-tree registration bridge; removed for Sigra at Phase-137 extraction)"
    - ".planning/REQUIREMENTS.md (DECOUPLE-03 flipped to Complete)"
  key_links:
    - ":companions registry -> auth_authority?/0 + evaluate_auth/3 -> RouteGate.prepend_auth_evaluation_denials/4 dispatches to the registered Sigra facade (Category-A :step_up_required restored)."
    - ":companions registry -> denial_codes/0 + telemetry accessors -> SupportMatrix.auth_contract_truth/0 -> Doctor.phase_46_auth_findings/1 (Category-B populated fields restored)."
    - "mix.exs application/0 `env: [companions: ...]` -> Application.get_env(:crosswake, :companions, []) at every runtime call site (get_env, never compile_env — EXTRACT-04 stale-beam guard)."
  prohibitions:
    - "No Finding-boundary refactor introduced — evaluate_auth/3 returns Denial.t() directly (the D-4 :auth-axis Finding work is Phase 137)."
    - "No compile_env/3 added anywhere — registry and companion callbacks are read via Application.get_env/3 at call time only."
    - "No static Crosswake.Companions.Sigra / Crosswake.Companions.Chimeway references reintroduced into core lib/ outside lib/crosswake/companions/** (the AST guard must stay green)."
    - "No weakening of the 5 phase136 backstop proof tests or the phase130 fail-closed test — they stay green unchanged."
    - "No editing of the 13 legacy test files to force green EXCEPT the two that clear the registry in setup and now assert an impossible post-inversion outcome (phase46, route_gate_test) — and only with the justification recorded below."
---

<objective>
Close the single blocking Phase-136 gap: the full ExUnit suite has 34 regressions because no in-tree
companion implements the four new optional callbacks (`evaluate_auth/3`, `auth_authority?/0`,
`denial_codes/0`, `forbidden_metadata_keys/0`, plus `telemetry_events/0`) and no companion is
registered in the app environment. The registry inversion (Plans 02-05) is correct; it just has no
concrete auth authority wired behind the seam.

Build a real in-tree `Crosswake.Companions.Sigra` companion facade that declares
`@behaviour Crosswake.Companion` and DELEGATES to the existing `Crosswake.Companions.Sigra.*`
sub-modules, extend the existing `Chimeway` facade with the optional callbacks it legitimately owns,
register both companions via the `env:` key of `application/0` in `mix.exs` (Crosswake is a Hex library
with NO `config/` directory — the idiomatic way a library ships default application env), and extend
`SupportMatrix.auth_contract_truth/0` to aggregate the companion-sourced fields the Category-B tests
assert. Then flip DECOUPLE-03 to Complete.

Purpose: DECOUPLE-03. This is the pre-extraction step D-136-B flags as a Phase-137 prerequisite —
landing it in 136 satisfies the VALIDATION.md full-green contract AND gives Phase 137 a working
baseline to dress-rehearse against.

Output: full suite 0 failures; compile + AST guard green; DECOUPLE-03 Complete.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/136-core-decoupling/136-CONTEXT.md
@.planning/phases/136-core-decoupling/136-VERIFICATION.md
@lib/crosswake/companions/chimeway.ex
@lib/crosswake/companion.ex
@lib/crosswake/companions/sigra/evaluator.ex
@lib/crosswake/companions/sigra/denial_codes.ex
@lib/crosswake/companions/sigra/telemetry.ex
@lib/crosswake/compatibility/route_gate.ex
</context>

<artifacts_produced>
## Artifacts this phase produces (Plan 06 — gap closure)

- **NEW** `lib/crosswake/companions/sigra.ex` — `Crosswake.Companions.Sigra`, `@behaviour Crosswake.Companion`. Required callbacks (`companion_id/0`→`:sigra`, `enabled?/1`, `route_gated?/2`, `kill_switch_active?/1`, `validate_dependency/0`, `report_state/0`) mirror `chimeway.ex`. Optional callbacks: `auth_authority?/0`→`true`; `evaluate_auth/3` delegating to `Crosswake.Companions.Sigra.Evaluator.evaluate_route_auth/3`; `denial_codes/0` delegating to `Sigra.DenialCodes.codes/0`; `forbidden_metadata_keys/0` delegating to `Sigra.Telemetry.forbidden_metadata_keys/0`; `telemetry_events/0` mapping `Sigra.Telemetry.event_names/0` into `event_doc()` maps with `tier: :reserved`. Plus two non-behaviour public accessors used by `auth_contract_truth/0`: `telemetry_event_names/0` and `safe_detail_keys/0`.
- `lib/crosswake/companions/chimeway.ex` — add `telemetry_events/0` (if Chimeway has reserved telemetry) and `forbidden_metadata_keys/0`; `denial_codes/0` only if Chimeway owns denial codes. `auth_authority?/0` stays absent (Chimeway is NOT an auth authority).
- `lib/crosswake/support_matrix/support_matrix.ex` — `auth_contract_truth/0` extended to aggregate `telemetry.event_names`, `telemetry.metadata_keys`, `telemetry.forbidden_metadata_keys`, `denial_codes`, and `safe_detail_keys` from the registered auth-authority companion at runtime via `function_exported?/3`.
- `mix.exs` — `application/0` gains `env: [companions: [Crosswake.Companions.Sigra, Crosswake.Companions.Chimeway]]`, merged into the existing keyword list (do NOT clobber `extra_applications: [:logger]`). This is the in-tree registration bridge: Crosswake has no `config/` dir, so the library's default application env is where `:companions` belongs. It covers dev/test/prod in one place. At Phase-137 extraction the `Crosswake.Companions.Sigra` entry is removed from core's `env:` because that module leaves core.
- New application-env key: `:companions` registry list (declared via `application/0` `env:`).
- `.planning/REQUIREMENTS.md` — DECOUPLE-03 checkbox + tracking-table row flipped to Complete.
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: Build the Crosswake.Companions.Sigra facade + extend Chimeway; register both via mix.exs application env</name>
  <files>lib/crosswake/companions/sigra.ex, lib/crosswake/companions/chimeway.ex, mix.exs</files>
  <read_first>
    - lib/crosswake/companions/chimeway.ex (the precedent facade: @behaviour, @impl clauses, report_state/0 shape using Crosswake.Companion.State, Application.get_env for its config key)
    - lib/crosswake/companion.ex (the 11 callbacks; @optional_callbacks list at L203-207; the evaluate_auth/3 contract {:allow, map()} | {:deny, Denial.t()}; auth_authority?/0 semantics; forbidden_metadata_keys/0; denial_codes/0; telemetry_events/0 returning event_doc() maps)
    - lib/crosswake/companions/sigra/evaluator.ex (evaluate_route_auth/3 accepts RouteEntry|nil, AuthContext|nil, opts; returns {:allow, %Result{status, denial, facts}} | {:deny, Denial.t()}; Result is a plain struct — must be mapped to a plain map for the {:allow, map()} contract)
    - lib/crosswake/companions/sigra/denial_codes.ex (codes/0; allowed_detail_keys/0 — these feed denial_codes/0 and safe_detail_keys/0)
    - lib/crosswake/companions/sigra/telemetry.ex (event_names/0 returns [[atom()]]; metadata_keys/0; forbidden_metadata_keys/0 — the fuller PII key list)
    - lib/crosswake/telemetry.ex (@type event_doc L31-37: %{event, tier, description, measurements, metadata}; build_reserved_events/0 at L177-187 filters returned maps by %{tier: :reserved} — so every map telemetry_events/0 returns MUST have a :tier key and it must be :reserved to appear)
    - test/crosswake/proof/phase136_decouple_proof_test.exs (StubForbiddenKeyCompanion + StubAuthFirstCompanion show the exact @impl shape; the setup save/restore of :companions — a non-empty application-env default is safe because the zero-companion backstop tests explicitly `Application.put_env(:crosswake, :companions, [])` to override it)
    - mix.exs (`def application` at ~L25 returns `[extra_applications: [:logger]]` — there is NO `env:` key yet, and `def project` has NO `config_path:`; there is NO `config/` directory in this repo. The `env:` key of `application/0` is where a Hex library declares default application env.)
    - lib/crosswake/companions/chimeway/telemetry.ex if it exists (to know Chimeway's telemetry event_names + forbidden keys for its telemetry_events/0 + forbidden_metadata_keys/0)
    - .planning/phases/136-core-decoupling/136-CONTEXT.md (D-136-B: evaluate_auth/3 returns Denial.t() in 136, no Finding refactor)
  </read_first>
  <action>
    Create `lib/crosswake/companions/sigra.ex` defining `Crosswake.Companions.Sigra` with
    `@behaviour Crosswake.Companion`, mirroring the structure of `chimeway.ex`.

    Required callbacks (mirror chimeway.ex conventions; all `@impl true`):
    - `companion_id/0` returns `:sigra`.
    - `enabled?/1` reads the `:enabled` key from the passed config map defaulting to true (same
      pattern as chimeway.ex).
    - `route_gated?/2` returns `:pass` (Sigra is an auth authority, not a feature-flag gate).
    - `kill_switch_active?/1` returns `false`.
    - `validate_dependency/0` returns `:ok` (the in-tree Sigra modules are always loaded in 136).
    - `report_state/0` returns a `%Crosswake.Companion.State{}` with `companion_id: :sigra`,
      `enabled: enabled?(config)`, `dependency_status: :present`, and `:unconfigured` gate/kill
      statuses, reading config via `Application.get_env(:crosswake, :sigra, %{})` (mirror chimeway).

    Optional callbacks (each `@impl true`):
    - `auth_authority?/0` returns `true`.
    - `evaluate_auth/3` delegates to `Crosswake.Companions.Sigra.Evaluator.evaluate_route_auth(route, auth_context, opts)`.
      The Evaluator returns `{:allow, %Evaluator.Result{}}` or `{:deny, Denial.t()}`. The behaviour
      contract is `{:allow, map()} | {:deny, Denial.t()}`, so on the allow branch convert the
      `%Result{}` to a plain map (e.g. `Map.from_struct/1`, keeping `:status` and `:facts`); pass
      the `{:deny, Denial.t()}` through UNCHANGED so `:step_up_required` and its sanitized details
      reach RouteGate exactly as before the inversion. Do NOT introduce any Finding conversion
      (D-136-B — Denial passthrough only; the Finding-boundary refactor is Phase 137).
    - `denial_codes/0` delegates to `Crosswake.Companions.Sigra.DenialCodes.codes/0`.
    - `forbidden_metadata_keys/0` delegates to `Crosswake.Companions.Sigra.Telemetry.forbidden_metadata_keys/0`
      (the fuller Sigra PII list that unions with core's 10-atom baseline).
    - `telemetry_events/0` maps each event name from `Crosswake.Companions.Sigra.Telemetry.event_names/0`
      into an `event_doc()` map: `%{event: name, tier: :reserved, description: "...", measurements: [],
      metadata: Crosswake.Companions.Sigra.Telemetry.metadata_keys()}`. Every map MUST carry
      `tier: :reserved` or `build_reserved_events/0`'s `%{tier: tier}` filter will crash.

    Add two plain (non-behaviour, no `@impl`) public accessors that `auth_contract_truth/0` will
    consume in Task 2 (defined here so the facade owns its own surface):
    - `telemetry_event_names/0` delegates to `Sigra.Telemetry.event_names/0`.
    - `safe_detail_keys/0` delegates to `Sigra.DenialCodes.allowed_detail_keys/0`.
    Also expose `telemetry_metadata_keys/0` delegating to `Sigra.Telemetry.metadata_keys/0` so the
    aggregation in Task 2 has a companion-owned accessor for `telemetry.metadata_keys`.

    Extend `lib/crosswake/companions/chimeway.ex`: add `@impl true forbidden_metadata_keys/0`
    (delegating to Chimeway's telemetry forbidden-key list — read the chimeway telemetry module to
    find it; if Chimeway has no telemetry module, return `[]`), and `@impl true telemetry_events/0`
    returning Chimeway's reserved `event_doc()` maps (or `[]` if none). Add `denial_codes/0` ONLY if
    Chimeway owns denial codes (it emits `:notification_open_denied` codes per the resolver — if a
    canonical code list module exists, delegate to it; otherwise omit `denial_codes/0`). Do NOT add
    `auth_authority?/0` to Chimeway — Chimeway is not an auth authority (leaving it absent means
    `function_exported?/3` returns false and RouteGate never dispatches auth to it).

    Register both companions via the application environment in `mix.exs`. Crosswake is a Hex
    LIBRARY: there is NO `config/` directory (no `config/config.exs`, `config/test.exs`, or
    `config/dev.exs`), and `application/0` has no `env:` key today — so `Application.get_env(:crosswake,
    :companions, [])` currently always returns `[]`. The idiomatic library mechanism is the `env:` key
    of `application/0`. Edit `mix.exs` `application/0` to add:
    `env: [companions: [Crosswake.Companions.Sigra, Crosswake.Companions.Chimeway]]`
    MERGING it into the existing keyword list — keep `extra_applications: [:logger]` intact (do not
    clobber `:mod`/`:extra_applications`). This single edit covers dev, test, and prod with no new
    `config/` dir. Sigra MUST be listed ahead of Chimeway so the first auth-authority scan finds Sigra
    (order matters for the first-registered-wins rule).

    Record in the plan/SUMMARY that this is the pre-extraction in-tree registration bridge: at Phase-137
    extraction the `Crosswake.Companions.Sigra` entry is removed from core's `env:` because that module
    leaves core. This does NOT violate "core operates with no companion present": the fail-closed
    backstop tests (phase136 proof Test 1, phase130 fail-closed) explicitly
    `Application.put_env(:crosswake, :companions, [])` to override this default at runtime, and they MUST
    stay green unchanged — the application-env default is only the ambient bridge, never a hard-coded
    coupling.
  </action>
  <verify>
    <automated>mix compile --warnings-as-errors 2>&1 | tail -5 && mix run -e "IO.inspect(Application.get_env(:crosswake, :companions, []), label: :registry)" 2>&1 | tail -3 && mix run -e "Crosswake.CompanionGuard.assert_no_static_refs!(); IO.puts(:guard_ok)" 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - Source: `lib/crosswake/companions/sigra.ex` exists, declares `@behaviour Crosswake.Companion`, and defines `companion_id/0`, `enabled?/1`, `route_gated?/2`, `kill_switch_active?/1`, `validate_dependency/0`, `report_state/0`, `auth_authority?/0`, `evaluate_auth/3`, `denial_codes/0`, `forbidden_metadata_keys/0`, `telemetry_events/0`.
    - Source: `evaluate_auth/3` delegates to `Crosswake.Companions.Sigra.Evaluator.evaluate_route_auth/3` and passes the `{:deny, Denial.t()}` branch through unchanged (no Finding conversion; no `compile_env`).
    - Source: `telemetry_events/0` returns maps each carrying `tier: :reserved`.
    - Source: `chimeway.ex` has `forbidden_metadata_keys/0` and `telemetry_events/0`; it does NOT define `auth_authority?/0`.
    - Source: `mix.exs` `application/0` returns a keyword list containing `env: [companions: [Crosswake.Companions.Sigra, Crosswake.Companions.Chimeway]]` (Sigra first, then Chimeway) alongside the intact `extra_applications: [:logger]`; no `config/` files are created.
    - Behavior: `mix run -e "IO.inspect(Application.get_env(:crosswake, :companions, []))"` prints `[Crosswake.Companions.Sigra, Crosswake.Companions.Chimeway]` in a fresh dev run, and the ambient registry is likewise visible in the test env.
    - Behavior: `mix compile --warnings-as-errors` exits 0.
    - Behavior: `Crosswake.CompanionGuard.assert_no_static_refs!()` still returns `:ok` (the new file is under `lib/crosswake/companions/**` which the guard excludes).
  </acceptance_criteria>
  <done>A real Sigra companion facade delegating to the in-tree Sigra sub-modules exists, Chimeway carries its optional callbacks, and both are registered in the app environment — the registry now has a concrete auth authority behind the seam.</done>
</task>

<task type="auto">
  <name>Task 2: Extend auth_contract_truth/0 to aggregate companion-sourced fields at runtime</name>
  <files>lib/crosswake/support_matrix/support_matrix.ex</files>
  <read_first>
    - lib/crosswake/support_matrix/support_matrix.ex (auth_contract_truth/0 at L723-735 — currently fills only denial_codes and safe_detail_keys: []; the @auth_contract_truth_static map with telemetry.event_names/metadata_keys/forbidden_metadata_keys [] sentinels at ~L214-233; the existing function_exported?/3 pattern already used for denial_codes)
    - test/crosswake/support_matrix/support_matrix_test.exs (L662-759 — the exact fields asserted: row.telemetry.event_names must include [:crosswake, :auth, :denial]; row.telemetry.metadata_keys must include :denial_code; row.telemetry.forbidden_metadata_keys must include :access_token; row.denial_codes must include the auth codes; row.safe_detail_keys must include "auth_posture"/"handoff_ref"/"step_up_intent_ref"; and the exact Map.keys shape at L665-691 — do NOT add or remove keys, only populate existing ones)
    - lib/crosswake/companions/sigra.ex (the accessors added in Task 1: denial_codes/0, forbidden_metadata_keys/0, telemetry_event_names/0, telemetry_metadata_keys/0, safe_detail_keys/0)
    - lib/crosswake/doctor/doctor.ex (phase_46_auth_findings/1 at L734-800 — already reads auth_contract_truth/0 and falls back to registry denial_codes; confirm the extended truth row does not break its Map.get access)
  </read_first>
  <action>
    Extend `auth_contract_truth/0` so the companion-sourced fields are populated from the registered
    auth-authority companion at call time, keeping `Application.get_env(:crosswake, :companions, [])`
    (never `compile_env`) and `function_exported?/3` dispatch discipline.

    Find the auth-authority companion: from the `:companions` registry, select modules where
    `function_exported?(mod, :auth_authority?, 0)` and `mod.auth_authority?()` is true (first wins,
    matching RouteGate). From that companion aggregate:
    - `denial_codes` — from `denial_codes/0` (keep the existing flat_map, but it can now target the
      auth authority specifically; preserving the current all-companion flat_map is also acceptable
      as long as the Sigra codes appear).
    - `safe_detail_keys` — from the companion's `safe_detail_keys/0` accessor (was hardcoded `[]`).
    - `telemetry.event_names` — from the companion's `telemetry_event_names/0`.
    - `telemetry.metadata_keys` — from the companion's `telemetry_metadata_keys/0`.
    - `telemetry.forbidden_metadata_keys` — from the companion's `forbidden_metadata_keys/0`.

    Merge these into the returned row: keep `@auth_contract_truth_static` as the base, then
    `Map.merge` the top-level `denial_codes` and `safe_detail_keys`, and deep-merge the nested
    `telemetry` map (merge the three telemetry fields into `@auth_contract_truth_static.telemetry`
    without dropping its other keys like `status`, `authority_source`, `proof_class`). Guard every
    call with `function_exported?/3` so that when no auth-authority companion is registered the
    fields stay `[]` (the sentinel behavior the fail-closed / zero-companion tests rely on).

    Do NOT change the set of keys in the returned row — the support_matrix_test asserts an exact
    `Map.keys` shape. Only populate values that were `[]` sentinels.
  </action>
  <verify>
    <automated>mix test test/crosswake/support_matrix/support_matrix_test.exs 2>&1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - Source: `auth_contract_truth/0` selects the auth-authority companion via `function_exported?(mod, :auth_authority?, 0)` and populates `telemetry.event_names`, `telemetry.metadata_keys`, `telemetry.forbidden_metadata_keys`, `denial_codes`, and `safe_detail_keys` from it.
    - Source: no `compile_env`; registry read via `Application.get_env(:crosswake, :companions, [])`; the returned `Map.keys` set is unchanged from the static shape.
    - Source: with no auth-authority companion registered, the companion-sourced fields fall back to `[]` (guarded by `function_exported?/3`).
    - Behavior: `mix test test/crosswake/support_matrix/support_matrix_test.exs` reports 0 failures (the L662-759 auth_contract_truth test passes, including telemetry.event_names / metadata_keys / forbidden_metadata_keys / denial_codes / safe_detail_keys assertions).
  </acceptance_criteria>
  <done>auth_contract_truth/0 aggregates the auth-authority companion's telemetry, denial, and safe-detail fields at runtime, closing the Category-B `denial_codes: []` / empty-telemetry regressions while staying fail-closed when no companion is registered.</done>
</task>

<task type="auto">
  <name>Task 3: Full-suite green — reconcile the two registry-clearing legacy tests, then flip DECOUPLE-03</name>
  <files>test/crosswake/compatibility/route_gate_test.exs, test/crosswake/proof/phase46_sigra_auth_contract_test.exs, .planning/REQUIREMENTS.md</files>
  <read_first>
    - test/crosswake/compatibility/route_gate_test.exs (setup at L86-87 does `Application.delete_env(:crosswake, :companions)`; L92-208 auth-predicated tests then assert `:step_up_required` with NO companion registered — impossible post-inversion, which now yields :dependency_missing)
    - test/crosswake/proof/phase46_sigra_auth_contract_test.exs (setup at L120-123 does `delete_env(:crosswake, :companions)`; L150-190 assert `:step_up_required` with no companion; L219/L223 tests that put_env their own companions are fine and must stay)
    - test/crosswake/proof/phase136_decouple_proof_test.exs (Test 1 at L231-271: auth-predicated route + NO auth authority -> :dependency_missing — this is the authoritative post-inversion contract that CONFLICTS with the two legacy setups; do NOT weaken it)
    - .planning/phases/136-core-decoupling/136-VERIFICATION.md (Category A / Category B failure lists — run each named group to confirm green)
    - .planning/REQUIREMENTS.md (DECOUPLE-03 checkbox + tracking-table row)
    - .planning/phases/136-core-decoupling/136-CONTEXT.md (D-136-B — no Finding refactor; guardrails)
  </read_first>
  <action>
    First run the full suite: `mix test --exclude requires_example_host --exclude advisory_only`.
    The Sigra facade + config registration (Task 1) fixes every Category-A/B test that relies on the
    AMBIENT registry (they get Sigra as the auth authority and see populated denial_codes). Confirm
    this by re-running the named groups: phase46, phase47, phase52, phase54, phase55, phase56, phase57,
    phase58, phase71, phase73, chimeway_resolver_test, support_matrix_test, doctor_test,
    operator_inspection_test, phase135_ci_ops_proof_test, publish_readiness_test.

    Then handle the TWO tests that clear the registry in their own `setup`
    (`Application.delete_env(:crosswake, :companions)`): `route_gate_test.exs` and
    `phase46_sigra_auth_contract_test.exs`. These wipe even the config default, then assert an
    auth-predicated route denies with `:step_up_required` — which is IMPOSSIBLE after the inversion:
    with zero registered auth authorities, RouteGate now (correctly, per phase136 backstop Test 1)
    returns `:dependency_missing`. This is the "assertion is genuinely wrong post-inversion" case the
    guardrails permit. The correct fix is to make these tests REGISTER the Sigra facade instead of
    clearing it:
    - In each affected test's setup (or per-test), replace `Application.delete_env(:crosswake, :companions)`
      with `Application.put_env(:crosswake, :companions, [Crosswake.Companions.Sigra])` and restore
      the original in `on_exit` (save `Application.get_env(:crosswake, :companions, [])` first),
      mirroring the phase136 proof test's save/restore. For phase46, preserve the tests that
      `put_env` their own `[KillSwitchCompanion]` / `[GateDenyCompanion]` (L219/L223) — those set
      the registry explicitly and must keep doing so; only the auth-predicated `:step_up_required`
      tests need Sigra registered.
    - Do NOT change any expected `:step_up_required` assertions — once Sigra is registered, the
      Evaluator runs and produces `:step_up_required` exactly as before. The ONLY edit is swapping
      the registry-clearing/setup line so an auth authority is present.
    JUSTIFY each edited test file in the SUMMARY: state that the pre-inversion behavior called
    Sigra.Evaluator directly regardless of the registry, so a `delete_env` setup was valid then but
    is contradictory now that auth evaluation is registry-dispatched; the test intent
    (auth-predicated route denies with step_up) is preserved by registering the real Sigra facade.

    Bounded scope for any remaining red test after Tasks 1-2 + these two edits: the ONLY files
    permitted to receive legacy-test registration/assertion edits are those in the VERIFICATION.md
    Category-A list (phase46, phase47, phase55, phase56, phase71, phase73, route_gate_test,
    chimeway_resolver_test). Of those, this plan PRE-AUTHORIZES exactly two edits: route_gate_test:87
    and phase46:121 (the two registry-clearing setups handled above). If any file OUTSIDE the
    Category-A list is still red after Tasks 1-2, STOP and ESCALATE — surface it in the SUMMARY as a
    deviation rather than silently editing it. Do NOT edit any test whose failure is fixed by the
    ambient registration alone. Do NOT weaken the phase136 or phase130 backstop tests.

    Note on `operator_inspection_test.exs`: it also uses `delete_env` in a setup, but it is
    Category-B, not Category-A. Its assertions must be fixed by the `SupportMatrix.auth_contract_truth/0`
    aggregation in Task 2 (which is fail-closed via `function_exported?/3`), NOT by a test edit. Do NOT
    edit `operator_inspection_test.exs`; if it is still red after Task 2, treat that as a Task-2
    aggregation bug to fix, and escalate if it cannot be resolved there.

    Once `mix test --exclude requires_example_host --exclude advisory_only` reports 0 failures AND
    `mix compile --warnings-as-errors` exits 0 AND `Crosswake.CompanionGuard.assert_no_static_refs!()`
    exits 0, flip DECOUPLE-03 in `.planning/REQUIREMENTS.md`: check the `[ ]` box and change the
    tracking-table status from `Pending` to `Complete`. This flip is legitimate ONLY after the suite
    is green — do it last.
  </action>
  <verify>
    <automated>mix compile --warnings-as-errors 2>&1 | tail -3 && mix test --exclude requires_example_host --exclude advisory_only 2>&1 | tail -8 && mix run -e "Crosswake.CompanionGuard.assert_no_static_refs!(); IO.puts(:guard_ok)" 2>&1 | tail -2</automated>
  </verify>
  <acceptance_criteria>
    - Behavior: `mix test --exclude requires_example_host --exclude advisory_only` reports 0 failures (down from 34).
    - Behavior: all 5 phase136 backstop tests and the phase130 fail-closed test remain green (unchanged).
    - Behavior: each named Category-A group (phase46, phase47, phase55, phase56, phase71, phase73, route_gate_test, chimeway_resolver_test) and Category-B group (phase52, phase54, phase56, phase57, phase58, support_matrix_test, doctor_test, operator_inspection_test, phase135_ci_ops_proof_test, publish_readiness_test) passes.
    - Source: `route_gate_test.exs` and `phase46_sigra_auth_contract_test.exs` register `Crosswake.Companions.Sigra` (via put_env with save/restore) in place of the registry-clearing setup; every `:step_up_required` assertion is UNCHANGED.
    - Behavior: `mix compile --warnings-as-errors` exits 0 and `Crosswake.CompanionGuard.assert_no_static_refs!()` returns `:ok`.
    - Source: `.planning/REQUIREMENTS.md` DECOUPLE-03 checkbox is `[x]` and its tracking-table row reads `Complete`.
  </acceptance_criteria>
  <done>Full suite is green (0 failures), the two registry-clearing legacy tests register the real Sigra facade instead of an impossible no-authority assertion, backstops stay green, and DECOUPLE-03 is marked Complete.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| :companions registry -> RouteGate auth dispatch | The registered auth-authority companion is the sole evaluator of auth-predicated routes; a missing/misregistered authority must fail closed (:dependency_missing), never fail open. |
| companion evaluate_auth/3 -> shell Denial exposure | The Denial.t() returned by Sigra reaches the shell; its details must stay sanitized (no PII/secret leakage). |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-136-07 | Spoofing | Sigra facade auth_authority?/0 registration | high | mitigate | Only the application-env-registered Sigra facade (via `application/0` `env: [companions: ...]` in mix.exs) returns auth_authority?/0 = true; RouteGate selects first-registered and emits an auth_authority_conflict telemetry event if >1 (phase136 backstop Test 3 covers this). Sigra registered ahead of Chimeway; Chimeway never claims auth authority. |
| T-136-08 | Information Disclosure | evaluate_auth/3 Denial details -> shell | high | mitigate | Facade passes the Evaluator's Denial through unchanged; the Evaluator already runs DenialCodes.sanitize_details/1 (allowlist of safe detail keys, ref regex, length caps). No new detail keys introduced; no Finding conversion that could bypass sanitization (D-136-B). |
| T-136-09 | Denial of Service | evaluate_auth/3 raising | medium | mitigate | RouteGate wraps evaluate_auth/3 in try/rescue -> :dependency_missing (fail-closed); the facade delegates to a pure Evaluator with no external I/O. phase136 backstop Test 1 proves the rescue path. |
| T-136-SC | Tampering | npm/pip/cargo installs | n/a | accept | No package-manager installs in this plan — pure in-tree Elixir facade + config + test edits. No new deps. |
</threat_model>

<verification>
- `mix compile --warnings-as-errors` exits 0.
- `Crosswake.CompanionGuard.assert_no_static_refs!()` returns `:ok` (new sigra.ex is under the excluded companions dir).
- `mix test test/crosswake/support_matrix/support_matrix_test.exs` passes.
- `mix test --exclude requires_example_host --exclude advisory_only` reports 0 failures.
- All 5 phase136 backstop tests + phase130 fail-closed test remain green.
- `.planning/REQUIREMENTS.md` DECOUPLE-03 is Complete.
</verification>

<success_criteria>
- Crosswake.Companions.Sigra facade exists, delegates to the in-tree Sigra sub-modules, implements evaluate_auth/3 (Denial.t() passthrough) + auth_authority?/0 + denial_codes/0 + forbidden_metadata_keys/0 + telemetry_events/0, and is registered via the `application/0` `env: [companions: ...]` key in mix.exs.
- Chimeway carries its optional telemetry/PII callbacks and is registered; it is not an auth authority.
- auth_contract_truth/0 aggregates companion telemetry/denial/safe-detail fields at runtime, fail-closed when no companion is registered.
- Full suite 0 failures; compile + AST guard green; no Finding refactor, no compile_env, no reintroduced static core->companion refs.
- DECOUPLE-03 marked Complete.
</success_criteria>

<output>
Create `.planning/phases/136-core-decoupling/136-06-SUMMARY.md` when done
</output>
