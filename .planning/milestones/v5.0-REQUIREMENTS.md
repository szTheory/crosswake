# Requirements: Crosswake — v5.0 Standalone Publishable Shell Packages

**Defined:** 2026-06-05
**Archived:** 2026-06-06
**Status:** Shipped (v5.0)

**Core Value:** Replace host-owned generated shell code (`ActivationCoordinator`, `BridgeChannel`) with standalone SPM/Maven dependencies, enforcing strict delegate-based customization to eliminate the "eject trap" and boilerplate for adopters.

## v1 Requirements

### Core Extraction & Packaging (CORE)

- [x] **CORE-01**: Crosswake core logic (`ActivationCoordinator`, `BridgeChannel`) must be extracted into a standalone Swift Package Manager (SPM) dependency for iOS. *(Outcome: Validated. Missing verification evidence accepted as tech debt.)*
- [x] **CORE-02**: Crosswake core logic must be extracted into a standalone Maven Central artifact (AAR) for Android. *(Outcome: Validated. Missing verification evidence accepted as tech debt.)*
- [x] **CORE-03**: The core libraries must internally enforce route policy, manifest parsing, bridge command validation, and capability checking without requiring host-app duplication. *(Outcome: Validated. Missing verification evidence accepted as tech debt.)*

### API Standardization & State (API)

- [x] **API-01**: The libraries must provide a single initialization entry point (e.g., `CrosswakeShell.initialize()`) to replace raw object generation in the host. *(Outcome: Validated.)*
- [x] **API-02**: Shell state must be exposed to the host UI via modern reactive streams (`StateFlow` for Android, `ObservableObject` / `@Published` for iOS) rather than monolithic callbacks. *(Outcome: Validated.)*
- [x] **API-03**: The libraries must expose strictly-typed, narrow Delegate protocols or lambda injection for specific host-provided capabilities (e.g., custom haptics, file picking) to avoid the "boilerplate trap" of massive interfaces. *(Outcome: Validated.)*

### Scaffold Generation & Integration (GEN)

- [x] **GEN-01**: `mix crosswake.gen.shell` must be updated to output a thin, dependency-driven host project that consumes the new SPM/Maven artifacts instead of full source copies. *(Outcome: Validated.)*
- [x] **GEN-02**: The generated host app must contain a "Glue" layer that wires the library into the host app lifecycle and observes its state for UI rendering. *(Outcome: Validated.)*
- [x] **GEN-03**: The generation tooling must configure or template the necessary host permission requests (`Info.plist` / `AndroidManifest.xml`) required by the core SDK. *(Outcome: Validated.)*

### Proof & Closeout (PROOF)

- [x] **PROOF-01**: All v5.0 core extraction and API standardization changes must run hermetically in CI, ensuring existing E2E and archetype proof lanes continue to pass against the new standalone libraries. *(Outcome: Validated.)*

## Traceability

| Requirement | Phase | Final Status |
|-------------|-------|--------|
| CORE-01 | Phase 76 | Complete |
| CORE-02 | Phase 76 | Complete |
| CORE-03 | Phase 76 | Complete |
| API-01 | Phase 77 | Complete |
| API-02 | Phase 77 | Complete |
| API-03 | Phase 77 | Complete |
| GEN-01 | Phase 78 | Complete |
| GEN-02 | Phase 78 | Complete |
| GEN-03 | Phase 78 | Complete |
| PROOF-01 | Phase 79 | Complete |

**Coverage:**
- v1 requirements: 10 total
- Mapped to phases: 10 ✓
- All marked complete at milestone archival.