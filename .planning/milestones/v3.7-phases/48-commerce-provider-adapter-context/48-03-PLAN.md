---
phase: 48-commerce-provider-adapter-context
plan: "03"
type: execute
wave: 2
depends_on:
  - "48-01"
  - "48-02"
files_modified:
  - lib/crosswake/commerce/provider_evidence.ex
  - lib/crosswake/commerce/contracts.ex
  - examples/phoenix_host/lib/crosswake_example/commerce/provider_adapter_storefront.ex
  - examples/phoenix_host/lib/crosswake_example/paywall_entry_live.ex
  - test/crosswake/commerce/provider_evidence_test.exs
  - test/crosswake/proof/phase48_provider_adapter_proof_test.exs
autonomous: true
requirements:
  - ADPT-01
  - ADPT-02
must_haves:
  truths:
    - "Purchase and restore are one-shot intent/result handoffs."
    - "Lifecycle hints are UX/recovery hints only and cannot update entitlement authority."
    - "Example-host provider adapter wiring replaces the mock evidence emitter without changing reconciliation/projection authority."
---

<objective>
Add shared purchase/restore result contracts and example-host swap-target wiring for StoreKit and Play Billing evidence emitters.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/48-commerce-provider-adapter-context/48-CONTEXT.md
@.planning/phases/48-commerce-provider-adapter-context/48-RESEARCH.md
@.planning/phases/48-commerce-provider-adapter-context/48-PATTERNS.md
@lib/crosswake/commerce/contracts.ex
@lib/crosswake/commerce/provider_evidence.ex
@examples/phoenix_host/lib/crosswake_example/commerce/mock_storefront.ex
@examples/phoenix_host/lib/crosswake_example/commerce/reconciliation_inbox.ex
@examples/phoenix_host/lib/crosswake_example/commerce/entitlement_projection.ex
@examples/phoenix_host/lib/crosswake_example/paywall_entry_live.ex
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Lock result and lifecycle-hint contracts</name>
  <read_first>
    lib/crosswake/commerce/contracts.ex, lib/crosswake/commerce/provider_evidence.ex, test/crosswake/commerce/provider_evidence_test.exs, .planning/phases/48-commerce-provider-adapter-context/48-CONTEXT.md
  </read_first>
  <files>test/crosswake/commerce/provider_evidence_test.exs</files>
  <action>Add failing tests that assert result statuses are exactly `[:submitted, :user_canceled, :pending, :provider_error, :prerequisite_missing, :reconcile_required]` and lifecycle hints are exactly `[:flow_opened, :flow_closed, :pending_external, :reconcile_required, :reconcile_timeout]`. Assert `Crosswake.Commerce.ProviderEvidence.authority_mutation_allowed_from_lifecycle_hint?/1` or equivalent helper returns `false` for every lifecycle hint.</action>
  <acceptance_criteria>
    - Result and lifecycle vocabularies are closed and exported from one shared module.
    - Tests fail if `:granted`, `:active`, raw StoreKit status strings, or raw Play Billing state strings enter the public result/lifecycle vocabularies.
    - Lifecycle hints cannot imply authority or access changes.
  </acceptance_criteria>
  <verify><automated>mix test test/crosswake/commerce/provider_evidence_test.exs</automated></verify>
  <done>Shared result and lifecycle hint contracts are locked.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Wire example-host provider adapter swap target without changing projection authority</name>
  <read_first>
    examples/phoenix_host/lib/crosswake_example/commerce/mock_storefront.ex, examples/phoenix_host/lib/crosswake_example/commerce/reconciliation_inbox.ex, examples/phoenix_host/lib/crosswake_example/commerce/entitlement_projection.ex, examples/phoenix_host/lib/crosswake_example/paywall_entry_live.ex, lib/crosswake/companions/store_kit/evidence.ex, lib/crosswake/companions/play_billing/evidence.ex
  </read_first>
  <files>examples/phoenix_host/lib/crosswake_example/commerce/provider_adapter_storefront.ex, examples/phoenix_host/lib/crosswake_example/paywall_entry_live.ex, test/crosswake/proof/phase48_provider_adapter_proof_test.exs</files>
  <action>Create `CrosswakeExample.Commerce.ProviderAdapterStorefront` as a pure-Elixir example adapter facade with `simulate_storekit_purchase/2`, `simulate_storekit_restore/2`, `simulate_play_billing_purchase/2`, and `simulate_play_billing_restore/2`. Each function must build provider-specific normalized evidence and return `ReconciliationEvidence`. Update `PaywallEntryLive` only enough to keep the mock default intact and document the exact swap point; do not replace the existing mock corridor as the default runnable path. Add proof tests that downstream `ReconciliationInbox` and `EntitlementProjection` behavior stays provider-neutral and still requires backend projection before access is granted.</action>
  <acceptance_criteria>
    - Example-host provider adapter facade compiles with no Apple or Android SDK dependency.
    - Existing mock paywall proof remains runnable.
    - Phase 48 proof shows StoreKit and Play Billing evidence reaches the same inbox/projection path as mock evidence.
    - No task adds continuous transaction streams to Phoenix/LiveView.
  </acceptance_criteria>
  <verify><automated>mix test test/crosswake/proof/phase48_provider_adapter_proof_test.exs test/crosswake/proof/phase34_paywall_corridor_proof_test.exs</automated></verify>
  <done>Example-host wiring proves provider adapters are swap targets, not new authority owners.</done>
</task>

</tasks>

<threat_model>
## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-48-05 | Information disclosure | Raw provider event streams | mitigate | Keep bridge public contract to one-shot result/evidence handoff; do not expose provider lifecycle streams. |
| T-48-06 | Elevation of privilege | Example-host paywall | mitigate | Proof must show provider adapter facade still requires backend entitlement projection before access. |
</threat_model>

<verification>
Run `mix test test/crosswake/commerce/provider_evidence_test.exs test/crosswake/proof/phase48_provider_adapter_proof_test.exs test/crosswake/proof/phase34_paywall_corridor_proof_test.exs`.
</verification>

<success_criteria>
- Shared result/lifecycle vocabularies are closed.
- Example-host provider adapter facade uses the existing reconciliation path.
- Mock paywall corridor remains the default hermetic runnable path.
</success_criteria>

<output>
Create `.planning/phases/48-commerce-provider-adapter-context/48-03-SUMMARY.md` when done.
</output>
