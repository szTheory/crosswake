# Phase 48.1: Close gap: ADPT-01/ADPT-02 - provider facade paywall swap-target contract - Research

**Researched:** 2026-06-01  
**Domain:** Example-host storefront adapter contract closure for provider facade  
**Confidence:** HIGH

<user_constraints>
## User Constraints (from CONTEXT.md)

### Locked Decisions
### 1. Storefront Adapter Contract - LOCKED
- **D-01:** Define an explicit example-host storefront adapter contract for
  `simulate_purchase/1` and `simulate_restore/1`. The contract should be
  behaviour-backed or equivalently compiler-visible so future facades cannot
  repeat the current mismatch.
- **D-02:** Prefer typed result callbacks:
  `simulate_purchase(Contracts.PurchaseIntent.t()) ::
  {:ok, Contracts.ReconciliationEvidence.t()} | {:error, term()}` and the same
  shape for restore. This is more durable than preserving the current direct
  evidence return because provider normalizers already return tagged results and
  provider failures must fail closed.
- **D-03:** Update `PaywallEntryLive` only enough to consume the typed result
  contract and keep the existing Phoenix-owned reconciliation path. It should
  not branch on StoreKit vs Play Billing and should not interpret provider
  statuses as entitlement authority.
- **D-04:** Keep `MockStorefront` as the default adapter, updated to satisfy the
  same explicit contract. The mock remains pure Elixir and provider-neutral.

### 2. Provider Selection - LOCKED
- **D-05:** Use a closed provider selection contract, not hidden inference. The
  provider vocabulary is `:storekit | :play_billing` and must map to the existing
  closed `Crosswake.Commerce.ProviderEvidence` provider vocabulary.
- **D-06:** Strong default: keep `ProviderAdapterStorefront` as the single
  ergonomic paywall swap target and have it delegate to explicit provider-owned
  wrapper modules or private dispatch branches based on runtime app config, for
  example `config :crosswake_example, :paywall_storefront_provider, :storekit`.
- **D-07:** Do not pass provider choice from `PaywallEntryLive` event handlers.
  The LiveView owns paywall UI and intent creation; provider ownership belongs
  at the adapter boundary.
- **D-08:** Do not auto-select the provider from route/platform context in this
  closure phase. That would add hidden behavior, weaker hermetic tests, and
  ambiguity for web/dev environments. If future mobile shell context becomes
  authoritative, it should be a later explicit phase.
- **D-09:** Invalid or missing provider configuration for the provider facade
  must fail closed with a typed error, not silently fall back to mock and not
  grant access.

### 3. Proof And Documentation - LOCKED
- **D-10:** Add merge-blocking hermetic proof for the exact audit gap:
  configuring `ProviderAdapterStorefront` as the paywall storefront adapter must
  drive both purchase and restore through the existing `PaywallEntryLive`
  callback path into `ReconciliationInbox`.
- **D-11:** The proof must assert authority boundaries, not just function
  existence: provider evidence is accepted as reconciliation evidence, immediate
  state is pending/awaiting verification, and access becomes granted only after
  backend-owned projection refresh.
- **D-12:** Tests should cover both StoreKit and Play Billing provider choices
  against the same public storefront adapter contract. Testing only
  provider-specific helper functions is insufficient because that is the gap the
  audit found.
- **D-13:** Add docs-contract or guide assertions only where they protect public
  support truth: the provider facade is a paywall swap target, the default mock
  remains default, and StoreKit/Play Billing seams emit evidence only.
- **D-14:** Keep provider/device/storefront lanes advisory until existing
  promotion criteria pass. Green sandbox/device runs must not close this
  phase's merge-blocking proof requirement by themselves.

### 4. Ecosystem Lessons Applied - LOCKED
- **D-15:** Follow the Elixir/Phoenix/Plug/Ecto idiom of explicit module
  contracts plus explicit runtime config. Behaviours and callbacks should name
  required functions; runtime app config is appropriate for deploy-time choices;
  per-request UI branching is not the right place for provider ownership.
- **D-16:** Apply the billing ecosystem lesson from Apple and Google: provider
  transaction identifiers and purchase tokens are evidence for backend
  verification/reconciliation, not client-side entitlement authority.
- **D-17:** Apply the Crosswake prompt-corpus lesson: native/provider breadth is
  safe only when route ownership, typed boundaries, support truth, and proof
  class stay explicit.

### the agent's Discretion
- Exact module names are planner discretion. Strong defaults:
  `CrosswakeExample.Commerce.StorefrontAdapter` for the behaviour and either
  explicit wrapper modules such as
  `CrosswakeExample.Commerce.StoreKitStorefront` /
  `CrosswakeExample.Commerce.PlayBillingStorefront`, or private dispatch inside
  `ProviderAdapterStorefront` if that keeps the closure smaller.
- Exact config key names are planner discretion if provider selection remains
  closed, explicit, and runtime-configurable.
- Exact docs anchors are planner discretion. Prefer stable semantic assertions
  over brittle prose snapshots.

### Deferred Ideas (OUT OF SCOPE)
- Auto-selecting provider from mobile shell platform context belongs in a later
  phase if shell context becomes authoritative and diagnosable.
- Companion diagnostic auto-registration remains tech debt from the v3.7 audit,
  but it is not the ADPT-01/ADPT-02 paywall swap-target blocker.
- Provider/device/storefront promotion remains governed by ADPT-03 promotion
  criteria and should not be widened in this closure phase.
</user_constraints>

<phase_requirements>
## Phase Requirements

| ID | Description | Research Support |
|----|-------------|------------------|
| ADPT-01 | Host apps can use a StoreKit adapter seam that emits provider evidence into backend-owned reconciliation contracts. | Enforce `simulate_purchase/1` + `simulate_restore/1` storefront adapter callbacks returning typed tagged result; provider facade dispatches to StoreKit normalizer via closed config. [VERIFIED: codebase grep] |
| ADPT-02 | Host apps can use a Play Billing adapter seam that emits provider evidence into backend-owned reconciliation contracts. | Same public adapter callbacks and typed result, dispatching to Play Billing normalizer via closed config, with fail-closed config errors. [VERIFIED: codebase grep] |
</phase_requirements>

## Summary

Phase 48.1 is a contract-closure phase, not a new provider capability phase. The concrete break is already identified: `PaywallEntryLive` calls `storefront_adapter().simulate_purchase(intent)` and `simulate_restore(intent)`, while `ProviderAdapterStorefront` exports only provider-specific methods (`simulate_storekit_*`, `simulate_play_billing_*`). [VERIFIED: codebase grep] This is why milestone audit marks ADPT-01 and ADPT-02 as partial despite passing phase 48 proof. [VERIFIED: codebase grep]

The shortest safe closure is to introduce one compiler-visible storefront adapter contract (behaviour) that both `MockStorefront` and `ProviderAdapterStorefront` implement, using tagged `{:ok, evidence} | {:error, reason}` results. `PaywallEntryLive` should consume the tagged contract, keep reconciliation ingestion and authority boundaries unchanged, and remain provider-agnostic. [VERIFIED: codebase grep]

**Primary recommendation:** Implement a behaviour-backed `simulate_purchase/1` + `simulate_restore/1` contract with typed tagged results, make provider selection explicit via runtime config at the facade boundary, and add merge-blocking proof that exercises the public paywall swap target for both providers. [VERIFIED: codebase grep]

## Architectural Responsibility Map

| Capability | Primary Tier | Secondary Tier | Rationale |
|------------|-------------|----------------|-----------|
| Paywall intent creation and UI events | Frontend Server (SSR) | Browser / Client | `PaywallEntryLive` owns event handlers and intent structs. [VERIFIED: codebase grep] |
| Provider selection for facade | API / Backend | Frontend Server (SSR) | Choice must come from runtime config at adapter boundary, not from UI params. [VERIFIED: codebase grep] |
| Provider evidence normalization | API / Backend | — | StoreKit/PlayBilling evidence modules normalize to shared contracts. [VERIFIED: codebase grep] |
| Reconciliation ingest/state transition | API / Backend | Database / Storage | `ReconciliationInbox` and projection own status transitions and authority fence. [VERIFIED: codebase grep] |
| Entitlement authority/access grant | API / Backend | Database / Storage | Authority granted only after backend projection refresh, never directly from storefront evidence. [VERIFIED: codebase grep] |

## Project Constraints (from AGENTS.md)

- Preserve Phoenix-first thesis; do not recast Crosswake as generic WebView wrapper. [CITED: AGENTS.md]
- Keep runtime ownership explicit per route. [CITED: AGENTS.md]
- Keep bridge contracts semantic/typed/versioned/low-frequency. [CITED: AGENTS.md]
- Keep offline claims honest; distinguish cached read-only from local-first mutation. [CITED: AGENTS.md]
- Treat diagnostics/support/proof/rough-edge docs as product surface. [CITED: AGENTS.md]
- Respect v1 scope boundaries before widening integrations. [CITED: AGENTS.md]

## Standard Stack

### Core
| Library | Version | Purpose | Why Standard |
|---------|---------|---------|--------------|
| Elixir | 1.19.5 | Host language for contracts/facade/liveview glue | Already project runtime; no new dependency needed. [VERIFIED: codebase grep] |
| Phoenix LiveView | in-repo usage | Paywall swap-target callsite and event handling | Existing paywall flow is live and proven; closure must keep this path. [VERIFIED: codebase grep] |
| Crosswake contracts/reconciliation modules | in-repo | Evidence contract + authority fence | Existing ADPT posture depends on these structures and invariants. [VERIFIED: codebase grep] |

### Supporting
| Library | Version | Purpose | When to Use |
|---------|---------|---------|-------------|
| ExUnit | in-repo usage | Merge-blocking proof for swap-target compatibility | Required to prove closure in hermetic lane. [VERIFIED: codebase grep] |

### Alternatives Considered
| Instead of | Could Use | Tradeoff |
|------------|-----------|----------|
| Behaviour-backed adapter contract | Ad hoc duck-typing | Recreates current mismatch risk; no compiler guard. [VERIFIED: codebase grep] |
| Explicit runtime config provider selection | Infer from route/platform/session | Hidden behavior and weaker hermetic tests; locked out by context decisions. [VERIFIED: codebase grep] |

## Package Legitimacy Audit

No external package installation is required for Phase 48.1 implementation. [VERIFIED: codebase grep]

## Architecture Patterns

### System Architecture Diagram

`PaywallEntryLive` (`subscribe`/`restore`)  
-> `storefront_adapter().simulate_purchase/1 | simulate_restore/1` (behaviour contract)  
-> `ProviderAdapterStorefront` dispatch by explicit config (`:storekit | :play_billing`)  
-> `StoreKitEvidence`/`PlayBillingEvidence` normalize to `Contracts.ReconciliationEvidence`  
-> `ReconciliationInbox.ingest_evidence/1` (`:awaiting_verification`)  
-> `MockBackend.verify_and_broadcast`  
-> `EntitlementProjection.project_snapshot/2` (`:projection_refreshed`)  
-> LiveView state update (`:granted` only after backend projection)

### Recommended Project Structure
```text
examples/phoenix_host/lib/crosswake_example/commerce/
├── storefront_adapter.ex        # behaviour: simulate_purchase/1, simulate_restore/1
├── mock_storefront.ex           # default implementation, tagged return
└── provider_adapter_storefront.ex # facade implementing behaviour + config dispatch
```

### Pattern 1: Behaviour-Enforced Swap Target
**What:** Define a storefront adapter behaviour and enforce it on mock/facade modules. [VERIFIED: codebase grep]  
**When to use:** Any module intended for `:paywall_storefront_adapter` config swap. [VERIFIED: codebase grep]

### Anti-Patterns to Avoid
- **Provider branching in LiveView:** provider ownership belongs at adapter boundary, not UI handlers. [VERIFIED: codebase grep]
- **Silent fallback to mock on invalid provider config:** must fail closed with typed error. [VERIFIED: codebase grep]
- **Provider evidence as authority grant:** preserve backend projection as sole authority path. [VERIFIED: codebase grep]

## Don't Hand-Roll

| Problem | Don't Build | Use Instead | Why |
|---------|-------------|-------------|-----|
| Runtime adapter compatibility checks | Ad hoc runtime checks only | Elixir behaviour callbacks | Compiler-visible contract prevents drift. [VERIFIED: codebase grep] |
| Provider/event vocabularies | Free-form provider strings | `Crosswake.Commerce.ProviderEvidence` vocab helpers | Maintains closed, tested vocabulary. [VERIFIED: codebase grep] |

**Key insight:** The gap is interface mismatch, not provider normalization capability; reuse existing normalized evidence modules and close the public adapter contract. [VERIFIED: codebase grep]

## Common Pitfalls

### Pitfall 1: Contract Shape Drift Between Mock and Facade
**What goes wrong:** Facade compiles but cannot be used as configured swap target. [VERIFIED: codebase grep]  
**Why it happens:** No shared behaviour/typed callback contract. [VERIFIED: codebase grep]  
**How to avoid:** Introduce behaviour + enforce in both adapters + proof that config swap compiles/runs. [VERIFIED: codebase grep]  
**Warning signs:** `undefined function simulate_purchase/1` or `/simulate_restore/1` at LiveView event path. [VERIFIED: codebase grep]

### Pitfall 2: Authority Leakage from Provider Result
**What goes wrong:** Immediate grant based on provider callback outcome. [VERIFIED: codebase grep]  
**Why it happens:** Conflating evidence ingestion with entitlement projection. [VERIFIED: codebase grep]  
**How to avoid:** Keep `:awaiting_verification` ingest and grant only post-projection refresh. [VERIFIED: codebase grep]

## Code Examples

### Public Adapter Contract
```elixir
@callback simulate_purchase(Crosswake.Commerce.Contracts.PurchaseIntent.t()) ::
  {:ok, Crosswake.Commerce.Contracts.ReconciliationEvidence.t()} | {:error, term()}

@callback simulate_restore(Crosswake.Commerce.Contracts.RestoreIntent.t()) ::
  {:ok, Crosswake.Commerce.Contracts.ReconciliationEvidence.t()} | {:error, term()}
```
[ASSUMED]

### LiveView Consumption Shape
```elixir
case storefront_adapter().simulate_purchase(intent) do
  {:ok, evidence} -> ReconciliationInbox.ingest_evidence(evidence)
  {:error, _reason} -> {:noreply, put_flash(socket, :error, "...")}
end
```
[ASSUMED]

## State of the Art

| Old Approach | Current Approach | When Changed | Impact |
|--------------|------------------|--------------|--------|
| Mock adapter returns raw evidence directly | Provider modules return typed `{:ok, evidence} | {:error, term()}`; facade currently provider-specific | Phase 48 (2026-06-01) [VERIFIED: codebase grep] | Phase 48.1 must align paywall swap target to typed contract. [VERIFIED: codebase grep] |

**Deprecated/outdated:**
- Treating provider-specific facade methods as sufficient for paywall swap target is now proven incorrect by milestone audit. [VERIFIED: codebase grep]

## Assumptions Log

| # | Claim | Section | Risk if Wrong |
|---|-------|---------|---------------|
| A1 | A new behaviour module path `CrosswakeExample.Commerce.StorefrontAdapter` is best naming choice. | Architecture Patterns | Low; rename-only impact if planner chooses different module path. |
| A2 | Example snippets reflect final exact callback arity/result structure chosen by planner. | Code Examples | Medium; tests may fail if signatures differ from locked context. |

## Open Questions (RESOLVED)

1. **Should `MockStorefront` keep optional `opts` arity-2 functions alongside contract arity-1?**
   - What we know: Existing mock uses `/2`; LiveView currently calls `/1`. [VERIFIED: codebase grep]
   - Resolution: Keep `/2` as internal convenience wrappers for existing fixtures and guide anchors, and make `/1` the canonical behaviour contract used by `PaywallEntryLive` and `ProviderAdapterStorefront`.

## Environment Availability

| Dependency | Required By | Available | Version | Fallback |
|------------|------------|-----------|---------|----------|
| Elixir | compile/test closure | ✓ | 1.19.5 | — |
| Mix | run proof/unit tests | ✓ | 1.19.5 | — |
| ripgrep (`rg`) | fast contract discovery | ✓ | present | `grep` |

**Missing dependencies with no fallback:**
- None. [VERIFIED: codebase grep]

## Validation Architecture

### Test Framework
| Property | Value |
|----------|-------|
| Framework | ExUnit (project standard) [VERIFIED: codebase grep] |
| Config file | `test/test_helper.exs` [VERIFIED: codebase grep] |
| Quick run command | `mix test test/crosswake/proof/phase48_provider_adapter_proof_test.exs test/crosswake/proof/phase35_paywall_live_test.exs` [VERIFIED: codebase grep] |
| Full suite command | `mix test` [VERIFIED: codebase grep] |

### Phase Requirements → Test Map
| Req ID | Behavior | Test Type | Automated Command | File Exists? |
|--------|----------|-----------|-------------------|-------------|
| ADPT-01 | Provider facade as storefront adapter for purchase/restore via public contract | proof/integration | `mix test test/crosswake/proof/phase48_provider_adapter_proof_test.exs` | ✅ |
| ADPT-02 | Same swap-target contract under Play Billing provider selection | proof/integration | `mix test test/crosswake/proof/phase48_provider_adapter_proof_test.exs` | ✅ |

### Sampling Rate
- **Per task commit:** `mix test test/crosswake/proof/phase48_provider_adapter_proof_test.exs`
- **Per wave merge:** `mix test test/crosswake/proof/phase48_provider_adapter_proof_test.exs test/crosswake/proof/phase35_paywall_live_test.exs`
- **Phase gate:** `mix test` green before `$gsd-verify-work`

### Wave 0 Gaps
- [ ] Add/extend proof test that configures `ProviderAdapterStorefront` into LiveView swap-target path and asserts both events use public `simulate_purchase/1` + `simulate_restore/1` callbacks. [VERIFIED: codebase grep]
- [ ] Add docs-contract assertion that provider facade is valid paywall swap target while mock remains default. [VERIFIED: codebase grep]

## Security Domain

### Applicable ASVS Categories

| ASVS Category | Applies | Standard Control |
|---------------|---------|-----------------|
| V2 Authentication | no | — |
| V3 Session Management | no | — |
| V4 Access Control | yes | Backend projection remains authority boundary. [VERIFIED: codebase grep] |
| V5 Input Validation | yes | Closed provider/event/source vocabularies and typed contracts. [VERIFIED: codebase grep] |
| V6 Cryptography | no | — |

### Known Threat Patterns for Elixir/Phoenix commerce evidence path

| Pattern | STRIDE | Standard Mitigation |
|---------|--------|---------------------|
| Forged provider success payload grants access | Elevation of privilege | `ingest_evidence` yields non-granting states; projection gate required. [VERIFIED: codebase grep] |
| Hidden provider selection changes behavior | Tampering | Closed runtime config contract and fail-closed invalid provider errors. [ASSUMED] |

## Sources

### Primary (HIGH confidence)
- `examples/phoenix_host/lib/crosswake_example/paywall_entry_live.ex` - swap-target callsites and current arity expectations
- `examples/phoenix_host/lib/crosswake_example/commerce/provider_adapter_storefront.ex` - facade function surface mismatch
- `examples/phoenix_host/lib/crosswake_example/commerce/mock_storefront.ex` - default adapter behavior and signatures
- `examples/phoenix_host/lib/crosswake_example/commerce/reconciliation_inbox.ex` - reconciliation ingest status behavior
- `lib/crosswake/commerce/contracts.ex` - evidence structs and source vocabulary
- `lib/crosswake/commerce/provider_evidence.ex` - closed provider/event vocabularies
- `lib/crosswake/commerce/reconciliation.ex` - authority/access non-granting invariants
- `.planning/v3.7-MILESTONE-AUDIT.md` - authoritative ADPT-01/02 gap evidence
- `.planning/phases/48.1-close-gap-adpt-01-adpt-02-provider-facade-paywall-swap-target-contract/48.1-CONTEXT.md` - locked decisions for closure scope

### Secondary (MEDIUM confidence)
- `AGENTS.md` - project-level working constraints and scope discipline

### Tertiary (LOW confidence)
- None.

## Metadata

**Confidence breakdown:**
- Standard stack: HIGH - no new dependencies; all required surfaces already in repo.
- Architecture: HIGH - mismatch and ownership boundaries are directly observable in code/tests/audit.
- Pitfalls: HIGH - primary failure mode already reproduced by milestone audit evidence.

**Research date:** 2026-06-01  
**Valid until:** 2026-07-01
