---
phase: 128-collateral-see-it-run-guide
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - bin/capture-collateral.sh
  - brandbook/collateral/see-it-run/README.md
autonomous: true
requirements: [COLL-01, COLL-02]
user_setup:
  - service: "maintainer Mac (Xcode + Android SDK + emulator/simulator)"
    why: "iOS-simulator and Android-emulator screenshots and the screen recording are human-captured on the maintainer's machine — they cannot be automated in CI (requires macOS + Xcode + SDK + the Phase-126 Dev build). This is a phase-closure prerequisite, not an executor task."
    dashboard_config:
      - task: "Run bin/see-it-run.sh --build, then bin/capture-collateral.sh, then capture native screenshots (xcrun simctl io booted screenshot, adb exec-out screencap -p) + the screen recording, then commit the real binaries under brandbook/collateral/see-it-run/"
        location: "maintainer's Mac terminal + iOS Simulator + Android emulator"

must_haves:
  truths:
    - "bin/capture-collateral.sh captures the three web screenshots deterministically against localhost:4700 (/, /offline, /bridge-proof) and supports --web-only for headless/CI"
    - "The harness PRINTS (does not run) the exact human-gated native-capture commands (xcrun simctl io booted screenshot, adb exec-out screencap -p) plus montage + GIF guidance"
    - "brandbook/collateral/see-it-run/README.md honestly labels all seven assets — web rows say proof, native/montage rows say emulator evidence (advisory, not physical device)"
    - "No proof fixture, bin/see-it-run.sh, native dev wiring, or Docker backend file is modified"
  artifacts:
    - path: "bin/capture-collateral.sh"
      provides: "Collateral capture harness — web automated, native commands printed (COLL-01, COLL-02)"
      contains: "set -euo pipefail"
      min_lines: 30
    - path: "brandbook/collateral/see-it-run/README.md"
      provides: "Honest-label table for the seven collateral assets (D-06)"
      contains: "emulator evidence"
      min_lines: 20
  key_links:
    - from: "bin/capture-collateral.sh"
      to: "examples/phoenix_host/e2e/route_tour.spec.ts"
      via: "drives the in-repo Playwright route-proof mechanism for web shots"
      pattern: "playwright|route_tour"
    - from: "brandbook/collateral/see-it-run/README.md"
      to: "bin/capture-collateral.sh"
      via: "documented capture workflow references the harness"
      pattern: "capture-collateral\\.sh"
---

<objective>
Ship the collateral capture harness `bin/capture-collateral.sh` and the
`brandbook/collateral/see-it-run/README.md` honest-label table (COLL-01, COLL-02). The
harness automates the deterministic WEB screenshots via the in-repo Playwright and prints
the exact copy-paste commands for the HUMAN-gated native shots + recording (D-03).

Purpose: Make the three-runtime collateral capturable and honestly labeled, without
automating what cannot be automated (native sim/emulator capture requires Xcode + SDK on
the maintainer's Mac). Binaries themselves are human-captured and committed in a manual gate
AFTER this phase's automated work lands (D-19 — that is why no test asserts their existence
in-phase).

Output: `bin/capture-collateral.sh` (executable), `brandbook/collateral/see-it-run/README.md`
label table mirroring `brandbook/collateral/README.md`.

NOTE: This plan ships the harness + label scaffolding. The real PNG/GIF binaries are
human-captured per the `user_setup` manual gate and are NOT produced by an executor task.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/128-collateral-see-it-run-guide/128-CONTEXT.md
@.planning/phases/128-collateral-see-it-run-guide/128-UI-SPEC.md
@script/capture-native-collateral.mjs
@examples/phoenix_host/e2e/route_tour.spec.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create bin/capture-collateral.sh (web automated, native commands printed)</name>
  <files>bin/capture-collateral.sh</files>
  <read_first>
    - bin/see-it-run.sh (the sibling launch script — match its shell style, TTY/NO_COLOR/CI ANSI gating, repo-root resolution, and `set -euo pipefail` discipline)
    - script/capture-native-collateral.mjs (the existing native capture mechanism — its exact xcrun/adb commands: `xcrun simctl io booted screenshot <path>`, `adb exec-out screencap -p`; reuse these literally in the printed human-gate instructions)
    - examples/phoenix_host/e2e/route_tour.spec.ts (the in-repo Playwright spec that proves routes then screenshots — the web-shot mechanism to drive; note it screenshots /library, /bridge-proof, /offline, native-fallback)
    - examples/phoenix_host/config/runtime.exs (port 4700 source — the web shots target localhost:4700 at /, /offline, /bridge-proof)
    - .planning/phases/128-collateral-see-it-run-guide/128-UI-SPEC.md (Collateral Layout Contract — file set, 900px width, the seven target filenames)
  </read_first>
  <action>
    Create `bin/capture-collateral.sh` (chmod +x), a POSIX-ish bash script with `set -euo pipefail` and repo-root resolution mirroring bin/see-it-run.sh. Behavior:
    - WEB (automated, deterministic): drive the in-repo Playwright to capture the three web shots against localhost:4700 at `/`, `/offline`, `/bridge-proof`, writing `web-home.png`, `web-offline.png`, `web-bridge-proof.png` into `brandbook/collateral/see-it-run/`. Reuse the route_tour.spec.ts mechanism (run via the existing playwright config from examples/phoenix_host) — the routes are PROVEN before the screenshot is taken, per the route_tour ownerMessage discipline. Capture at 1x / ~900px readable width (NOT Retina), per D-04.
    - `--web-only` flag: run ONLY the web capture (for headless/CI); skip and do not print native instructions.
    - NATIVE (human-gated, printed not executed): when not `--web-only`, print exact copy-paste commands for the maintainer to capture the native shots — `xcrun simctl io booted screenshot brandbook/collateral/see-it-run/ios-simulator.png` and `adb exec-out screencap -p > brandbook/collateral/see-it-run/android-emulator.png` — plus a one-line reminder that these require the Phase-126 Dev build booted against the local backend, and guidance to compose `three-runtime-montage.png` (ImageMagick `convert +append` or hand-made) and record `see-it-run.gif` (terminal -> banner -> browser; ~900px, 12fps, 10-15s, `gifsicle -O3`, <8MB hard cap). Do NOT attempt to run these — the script PRINTS them.
    - Honest-label discipline in printed output: refer to native shots as `emulator evidence` (advisory, not physical device); never imply device support.
    - Idempotent: re-running overwrites the web PNGs; safe to run repeatedly.
    Comment-text discipline: the script head-comment may describe the native gate but must NOT contain any literal that a negative-grep gate forbids (no such gate targets this script). Keep printed port references derived/consistent with 4700.
    Do NOT modify bin/see-it-run.sh, native dev wiring, the Docker backend, or any proof fixture — touch only this new script.
  </action>
  <verify>
    <automated>bash -n bin/capture-collateral.sh && test -x bin/capture-collateral.sh && bin/capture-collateral.sh --help 2>&1 | head -5; echo "exit=$?"</automated>
  </verify>
  <acceptance_criteria>
    - Script is executable: `test -x bin/capture-collateral.sh` succeeds
    - Syntax valid: `bash -n bin/capture-collateral.sh` exits 0
    - `--web-only` flag is recognized: `grep -c -- '--web-only' bin/capture-collateral.sh` returns at least 1
    - Native human-gate commands are PRINTED (present as literals): `grep -c 'xcrun simctl io booted screenshot' bin/capture-collateral.sh` >= 1 AND `grep -c 'adb exec-out screencap -p' bin/capture-collateral.sh` >= 1
    - The seven target filenames are referenced: `grep -E 'web-home.png|web-offline.png|web-bridge-proof.png|ios-simulator.png|android-emulator.png|three-runtime-montage.png|see-it-run.gif' bin/capture-collateral.sh` matches all seven
    - Honest label in output: `grep -c 'emulator evidence' bin/capture-collateral.sh` >= 1
    - `set -euo pipefail` present: `grep -c 'set -euo pipefail' bin/capture-collateral.sh` returns 1
  </acceptance_criteria>
  <done>bin/capture-collateral.sh exists, is executable, syntax-valid, captures the three web shots automatically (reusing the in-repo Playwright route-proof mechanism), supports --web-only, and PRINTS the exact human-gated xcrun/adb native-capture + montage + GIF commands with honest emulator-evidence labeling — without touching see-it-run.sh, native wiring, the backend, or proof fixtures.</done>
</task>

<task type="auto">
  <name>Task 2: Create brandbook/collateral/see-it-run/README.md label table</name>
  <files>brandbook/collateral/see-it-run/README.md</files>
  <read_first>
    - brandbook/collateral/README.md (the existing collateral label-table format to mirror: Assets table with File/Dimensions/Description columns, a manual-step section, a Regenerating section)
    - .planning/phases/128-collateral-see-it-run-guide/128-UI-SPEC.md (Collateral Layout Contract — the exact label table File/Type/Label rows for the seven assets; the raw.githubusercontent.com reference pattern)
    - .planning/phases/128-collateral-see-it-run-guide/128-CONTEXT.md (D-01 storage rationale: brandbook is Hex-excluded; D-06 honest labels; D-03/D-19 the manual capture gate)
  </read_first>
  <action>
    Create `brandbook/collateral/see-it-run/README.md` mirroring `brandbook/collateral/README.md`'s structure. Include:
    - A short intro: this subdir holds the See It Run collateral, Hex-excluded (brandbook is in `exclude_patterns`), referenced from docs via `https://raw.githubusercontent.com/szTheory/crosswake/main/brandbook/collateral/see-it-run/<file>` (D-01/D-02).
    - The label table from the UI-SPEC Collateral Layout Contract with the seven rows: `web-home.png` (Web proof — localhost:4700/), `web-offline.png` (Web proof — localhost:4700/offline), `web-bridge-proof.png` (Web proof — localhost:4700/bridge-proof), `ios-simulator.png` (emulator evidence — iOS Simulator, advisory, not physical device), `android-emulator.png` (emulator evidence — Android Emulator, advisory, not physical device), `three-runtime-montage.png` (emulator evidence — three-runtime comparison, advisory, not physical device), `see-it-run.gif` (Demo — terminal -> banner -> web browser).
    - A "Capturing / Regenerating" section that points to `bin/capture-collateral.sh` for the web shots and documents the human-gated native + recording capture steps (the D-03/D-19 manual gate): the maintainer runs `bin/see-it-run.sh --build`, then `bin/capture-collateral.sh`, then captures native shots + recording, then commits — and that the binary-existence guard test (`see_it_run_collateral_test.exs`) is added AFTER the binaries land (D-19), not in this phase.
    Honest-label discipline: native rows say `emulator evidence` / advisory / not physical device; only the web rows use "proof".
  </action>
  <verify>
    <automated>test -f brandbook/collateral/see-it-run/README.md && grep -c 'emulator evidence\|capture-collateral.sh\|raw.githubusercontent' brandbook/collateral/see-it-run/README.md</automated>
  </verify>
  <acceptance_criteria>
    - File exists: `test -f brandbook/collateral/see-it-run/README.md` succeeds
    - All seven asset filenames listed: `grep -E 'web-home.png|web-offline.png|web-bridge-proof.png|ios-simulator.png|android-emulator.png|three-runtime-montage.png|see-it-run.gif' brandbook/collateral/see-it-run/README.md` matches all seven
    - Honest native labels: `grep -c 'emulator evidence' brandbook/collateral/see-it-run/README.md` >= 3 (the three native/montage rows)
    - Raw URL pattern documented: `grep -c 'raw.githubusercontent.com/szTheory/crosswake/main/brandbook/collateral/see-it-run' brandbook/collateral/see-it-run/README.md` >= 1
    - Manual gate documented: `grep -c 'capture-collateral.sh' brandbook/collateral/see-it-run/README.md` >= 1
  </acceptance_criteria>
  <done>brandbook/collateral/see-it-run/README.md exists mirroring the sibling collateral README, with the seven-row honest-label table (web = proof, native = emulator evidence/advisory), the raw.githubusercontent.com reference pattern, and the documented human-gated capture workflow referencing bin/capture-collateral.sh and the deferred collateral-existence test.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| capture script → file system | Writes only under brandbook/collateral/see-it-run/; never mutates proof fixtures or see-it-run.sh |
| collateral labels → reader trust | Native assets must be labeled emulator evidence, never device support |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-128-06 | Tampering | capture script mutating proof fixtures or native wiring | mitigate | script writes only to brandbook/collateral/see-it-run/; acceptance criteria scope file output; no see-it-run.sh/fixture edits |
| T-128-07 | Spoofing | placeholder/fake native binaries presented as real evidence | mitigate | no placeholder PNGs committed (D-03 honesty); real binaries human-captured under the user_setup manual gate; labels say emulator evidence |
| T-128-08 | Tampering | native shot mislabeled as device proof | mitigate | label table + printed script output use `emulator evidence` / advisory / not physical device for all native rows |
| T-128-SC | Tampering | npm/pip/cargo installs | accept | no package installs; harness reuses the already-vetted in-repo Playwright; no new dependencies |
</threat_model>

<verification>
- `bash -n bin/capture-collateral.sh` exits 0 and the script is executable
- `--web-only` flag and the human-gated xcrun/adb commands are present
- brandbook/collateral/see-it-run/README.md carries the seven-row honest-label table
- No proof fixture, see-it-run.sh, native wiring, or Docker backend file is modified
- Manual gate (native capture + recording) surfaced in user_setup and the collateral README
</verification>

<success_criteria>
COLL-01 + COLL-02 satisfied at the harness/scaffolding level: the capture harness produces the deterministic web shots and prints the human-gated native-shot + recording commands, and the collateral README honestly labels all seven assets (web = proof, native = emulator evidence). The real native binaries + recording are committed in the human manual gate (user_setup) before phase closure.
</success_criteria>

<output>
Create `.planning/phases/128-collateral-see-it-run-guide/128-03-SUMMARY.md` when done
</output>
