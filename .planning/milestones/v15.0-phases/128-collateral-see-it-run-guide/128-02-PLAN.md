---
phase: 128-collateral-see-it-run-guide
plan: 02
type: execute
wave: 2
depends_on: ["128-01"]
files_modified:
  - README.md
  - examples/QUICK_START.md
autonomous: true
requirements: [DOCS-02]
must_haves:
  truths:
    - "A reader landing on the README finds a `## See it run` section between `## What this is not` and `## Choose your path` that fronts the single hero command bin/see-it-run.sh"
    - "The README section names the three route owners, carries an advisory-native blockquote with the emulator-evidence legend link, and links forward to guides/see_it_run.md and examples/QUICK_START.md"
    - "examples/QUICK_START.md opens with a `> New here?` pointer to guides/see_it_run.md and renames Option A to lead with bin/see-it-run.sh, demoting raw docker compose to a fallback"
    - "No native overclaim is introduced; existing honest support labels are preserved"
  artifacts:
    - path: "README.md"
      provides: "The `## See it run` routing section (DOCS-02)"
      contains: "## See it run"
    - path: "examples/QUICK_START.md"
      provides: "Top-of-file guide pointer + Option A rename (DOCS-02)"
      contains: "guides/see_it_run.md"
  key_links:
    - from: "README.md"
      to: "guides/see_it_run.md"
      via: "forward link in the See it run section"
      pattern: "guides/see_it_run\\.md"
    - from: "examples/QUICK_START.md"
      to: "guides/see_it_run.md"
      via: "New here? pointer blockquote"
      pattern: "guides/see_it_run\\.md"
    - from: "README.md"
      to: "guides/support_matrix.md"
      via: "emulator-evidence legend link in the advisory blockquote"
      pattern: "support_matrix\\.md#support-truth-label-legend"
---

<objective>
Route readers to the new guide and the one-command path from both README and
`examples/QUICK_START.md` (DOCS-02), preserving honest support labels (no native overclaim).

Purpose: README gives scope, then experiential proof, then path-routing — the `## See it run`
section is the experiential proof. QUICK_START becomes the forward-anchored command reference
the guide points into. The routing is one-directional and non-circular: README → see_it_run →
(forward) QUICK_START.

Output: README.md `## See it run` section + optional Choose-your-path bullet; QUICK_START
top pointer + Option-A rename.

Depends on 128-01 because both edits link to `guides/see_it_run.md` (must exist) and the
QUICK_START "Option A: One Command (Docker)" rename is the anchor the guide links forward into.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/128-collateral-see-it-run-guide/128-CONTEXT.md
@.planning/phases/128-collateral-see-it-run-guide/128-UI-SPEC.md
@guides/see_it_run.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the `## See it run` section to README</name>
  <files>README.md</files>
  <read_first>
    - README.md (the existing section order: `## What this is`, `## What this is not`, `## Choose your path`, ...; the `## Guide map` section; the raw.githubusercontent.com image-reference pattern at the top for readme-header)
    - .planning/phases/128-collateral-see-it-run-guide/128-UI-SPEC.md (Copywriting Contract — README section heading `## See it run`, montage alt text, advisory blockquote verbatim; Collateral Layout Contract — the raw URL pattern)
    - guides/see_it_run.md (the guide this section links forward to — confirm its title and path)
    - guides/support_matrix.md (the #support-truth-label-legend anchor for the advisory blockquote link)
  </read_first>
  <action>
    Insert a new `## See it run` section in README.md positioned BETWEEN `## What this is not` and `## Choose your path` (D-12). Section body (D-12/D-13/D-15):
    - The single hero command `bin/see-it-run.sh` in a fenced code block, with a one-line Docker caveat noting it boots the shared backend on port 4700 and auto-opens the browser.
    - Name the three route owners: `/` (home), `/offline` (offline island), `/bridge-proof` (bounded bridge), at `http://localhost:4700`.
    - A `> Advisory native collateral` blockquote carrying the canonical term `emulator evidence` plus a link to the legend: `[support-truth label legend](guides/support_matrix.md#support-truth-label-legend)`.
    - Optionally (D-13, recommended): one inline `<picture>`/`<img>` embedding ONLY `three-runtime-montage.png` via `https://raw.githubusercontent.com/szTheory/crosswake/main/brandbook/collateral/see-it-run/three-runtime-montage.png`, placed between the hero command block and the route sentence, with the UI-SPEC montage alt text carrying `emulator evidence` for each native frame. If a single image is omitted, link to the guide for the visual instead (the section MUST reference the visual one way or the other).
    - Two forward links: `[guides/see_it_run.md](guides/see_it_run.md)` (guided tour) and `[examples/QUICK_START.md](examples/QUICK_START.md)` (command reference).
    Optional secondary bullet (discretion, recommended yes): add a `guides/see_it_run.md` bullet to the README "Evaluating Crosswake"/Guide-map list, secondary to the new section.
    Honest-label discipline: never write unqualified "supported"/"proven"/"works on device"/"cross-platform" near the native references; "proven" stays reserved for the web path. Do not alter the existing `## Proof and support posture` wording.
    Comment-text discipline: not applicable (no negative-grep literals authored here).
  </action>
  <verify>
    <automated>grep -n '^## ' README.md && grep -n 'see-it-run\|see_it_run\|emulator evidence' README.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '^## See it run' README.md` returns 1
    - The section is between `## What this is not` and `## Choose your path`: the line number of `## See it run` is greater than `## What this is not` and less than `## Choose your path` (verify via `grep -n '^## '`)
    - Hero command present: `grep -c 'bin/see-it-run.sh' README.md` returns at least 1
    - Forward links present: `grep -c 'guides/see_it_run.md' README.md` >= 1 AND `grep -c 'examples/QUICK_START.md' README.md` >= 1
    - Advisory honesty present: `grep -c 'emulator evidence' README.md` >= 1 AND `grep -c 'support_matrix.md#support-truth-label-legend' README.md` >= 1
    - No native overclaim introduced: the new section does not contain the phrase "works on device" or "cross-platform" near native references (manual scan of the inserted block)
  </acceptance_criteria>
  <done>README has a `## See it run` section between scope and path-routing that fronts bin/see-it-run.sh, names the three route owners, carries an advisory-native blockquote with the emulator-evidence legend link, references the visual, and forward-links to the guide and QUICK_START — with honest labels preserved.</done>
</task>

<task type="auto">
  <name>Task 2: Add QUICK_START top pointer + rename Option A to lead with the hero command</name>
  <files>examples/QUICK_START.md</files>
  <read_first>
    - examples/QUICK_START.md (the intro paragraph; `## First Run`; `### Option A: Docker (no local Elixir/Node/SQLite toolchain required)`; `### Option B: Native`; existing support labels and the `## What This Does Not Prove` section)
    - .planning/phases/128-collateral-see-it-run-guide/128-UI-SPEC.md (Copywriting Contract — `> New here?` pointer verbatim, `### Option A: One Command (Docker)` rename, Docker fallback note verbatim)
    - guides/see_it_run.md (the forward-link target; confirm relative path `../guides/see_it_run.md` from examples/)
  </read_first>
  <action>
    In `examples/QUICK_START.md`, before the existing intro paragraph (D-14), add the pointer blockquote verbatim from UI-SPEC: `> **New here?** Start with [guides/see_it_run.md](../guides/see_it_run.md) to see the three runtimes in action. Come back here for the full proof command reference.`
    Rename the Option A heading `### Option A: Docker (no local Elixir/Node/SQLite toolchain required)` → `### Option A: One Command (Docker)` (D-11/D-14). Make Option A LEAD with the hero command `bin/see-it-run.sh` (run from the repo root; the one-command path that boots the shared backend, prints the banner, and auto-opens the browser). DEMOTE the raw `cd examples/phoenix_host && docker compose up` to a "run the Docker backend directly" fallback note beneath the hero command — keep the command itself (the drift test in the QUICK_START culture still scans for it) but frame it as the fallback, not the primary.
    Do NOT remove or weaken any existing honest support labels (the `## What This Does Not Prove` section, native-step proof labels, `advisory` language). Do NOT modify Option B / dev-wiring command literals (those stay the canonical commands the guide links to). The relationship stays one-directional: QUICK_START's pointer goes forward to the guide; the guide links forward into QUICK_START — neither creates a loop back to README.
  </action>
  <verify>
    <automated>grep -n 'New here?\|### Option A\|bin/see-it-run.sh\|docker compose' examples/QUICK_START.md | head -20</automated>
  </verify>
  <acceptance_criteria>
    - Pointer present: `grep -c 'New here?' examples/QUICK_START.md` returns 1 AND `grep -c '../guides/see_it_run.md' examples/QUICK_START.md` returns at least 1
    - Option A renamed: `grep -c '### Option A: One Command (Docker)' examples/QUICK_START.md` returns 1 AND the old heading `### Option A: Docker (no local Elixir/Node/SQLite toolchain required)` is gone (`grep -c` returns 0)
    - Hero command fronts Option A: `grep -c 'bin/see-it-run.sh' examples/QUICK_START.md` returns at least 1
    - Docker fallback retained: `grep -c 'docker compose up' examples/QUICK_START.md` returns at least 1
    - Honest labels intact: `grep -c '## What This Does Not Prove' examples/QUICK_START.md` returns 1
    - The existing quick_start drift test still passes: `mix test test/crosswake/guides/quick_start_adoption_drift_test.exs` exits 0
  </acceptance_criteria>
  <done>QUICK_START opens with the `> New here?` pointer to the guide, Option A is renamed and leads with bin/see-it-run.sh with raw docker compose demoted to a fallback, all existing honest labels preserved, and the existing quick_start drift test still passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| README/QUICK_START → reader trust | Routing copy must not introduce native overclaim |
| doc cross-links → ExDoc/GitHub render | Forward-only, non-circular link graph (D-14) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-128-04 | Information disclosure | native overclaim in README/QUICK_START routing copy | mitigate | acceptance criteria forbid unqualified support claims; advisory blockquote carries emulator-evidence legend link; existing labels preserved |
| T-128-05 | Tampering | breaking the existing quick_start drift contract | mitigate | Task 2 retains `docker compose up` and `## What This Does Not Prove`; re-runs quick_start_adoption_drift_test.exs |
| T-128-SC | Tampering | npm/pip/cargo installs | accept | no package installs in this plan (docs-only) |
</threat_model>

<verification>
- README has `## See it run` between scope and path sections, fronting bin/see-it-run.sh
- QUICK_START opens with the guide pointer and Option A leads with the hero command
- `mix test test/crosswake/guides/quick_start_adoption_drift_test.exs` exits 0 (no regression)
- No native overclaim introduced; existing honest labels intact
</verification>

<success_criteria>
DOCS-02 satisfied: README and examples/QUICK_START.md both route readers to guides/see_it_run.md and the one-command path with honest support labels preserved (no native overclaim).
</success_criteria>

<output>
Create `.planning/phases/128-collateral-see-it-run-guide/128-02-SUMMARY.md` when done
</output>
