---
phase: 125-containerized-shared-backend-port-convention
plan: 03
type: execute
wave: 2
depends_on: ["125-01"]
files_modified:
  - docs/PORT-REGISTRY.md
  - test/crosswake/guides/port_registry_test.exs
  - examples/QUICK_START.md
autonomous: true
requirements: [PORT-01, PORT-02, PORT-03]
must_haves:
  truths:
    - "docs/PORT-REGISTRY.md documents a reserved 4700-4799 block, exclusions, an allocation rule, and a crosswake=4700 seed row"
    - "PORT-REGISTRY honestly explains that COMPOSE_PROJECT_NAME namespaces names (not host ports) and that Android reaches the host at 10.0.2.2"
    - "A new source-derived doc-contract test asserts the registry's port matches the committed PORT and mentions COMPOSE_PROJECT_NAME + 10.0.2.2"
    - "examples/QUICK_START.md uses 4700 everywhere (no 4002) and documents the docker compose up path"
    - "The existing quick_start_adoption_drift_test passes end-to-end (port 4700 derived, QUICK_START scan clean)"
  artifacts:
    - path: "docs/PORT-REGISTRY.md"
      provides: "Reusable per-lib port-allocation convention + crosswake seed row"
      contains: "4700"
    - path: "test/crosswake/guides/port_registry_test.exs"
      provides: "Source-derived drift guard for the registry"
      contains: "COMPOSE_PROJECT_NAME"
    - path: "examples/QUICK_START.md"
      provides: "4700 migration + Docker boot path"
      contains: "docker compose up"
  key_links:
    - from: "test/crosswake/guides/port_registry_test.exs"
      to: "examples/phoenix_host/config/runtime.exs"
      via: "committed_port/0 derives PORT from runtime.exs and asserts the registry row matches"
      pattern: "runtime\\.exs"
    - from: "docs/PORT-REGISTRY.md"
      to: "examples/phoenix_host/.env"
      via: "registry seed row records the same committed PORT/COMPOSE_PROJECT_NAME as .env"
      pattern: "crosswake"
---

<objective>
Document the reusable port-allocation convention (reserved 4700-4799 block, exclusions, allocation rule,
crosswake=4700 seed row, honest COMPOSE_PROJECT_NAME + 10.0.2.2 caveats) in docs/PORT-REGISTRY.md; guard it
with a new source-derived doc-contract test mirroring quick_start_adoption_drift_test.exs; and finish the
port migration in examples/QUICK_START.md (4002 → 4700 + a Docker boot section), turning the existing drift
test fully green.

Purpose: Delivers PORT-03 (reusable registry), reinforces PORT-01/PORT-02 (committed 4700, Android 10.0.2.2
reachability), and upholds the doc-contract honesty culture.
Output: docs/PORT-REGISTRY.md, test/crosswake/guides/port_registry_test.exs, updated examples/QUICK_START.md.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/125-containerized-shared-backend-port-convention/125-CONTEXT.md
@.planning/phases/125-containerized-shared-backend-port-convention/125-PATTERNS.md
</context>

<artifacts_this_phase_produces>
This plan (125-03) creates/modifies:
- `docs/PORT-REGISTRY.md` — NEW: reserved 4700-4799 block, exclusion list, allocation rule, crosswake seed row, COMPOSE_PROJECT_NAME + 10.0.2.2 caveats
- `test/crosswake/guides/port_registry_test.exs` — NEW: source-derived drift guard (port from runtime.exs; asserts registry mentions COMPOSE_PROJECT_NAME, 10.0.2.2, crosswake row, committed port)
- `examples/QUICK_START.md` — MODIFIED: all 4002 → 4700 (9 refs), add Docker `docker compose up` path section
</artifacts_this_phase_produces>

<context_dependency>
Depends on 125-01: runtime.exs holds the PORT default (the test derives from it); .env holds COMPOSE_PROJECT_NAME/PORT.
Same-wave coordination with 125-01 Task 3: that task redirected quick_start_adoption_drift_test.exs to runtime.exs
but left QUICK_START.md untouched. THIS plan's Task 3 edits QUICK_START.md and re-runs the full drift file to green.
</context_dependency>

<tasks>

<task type="auto">
  <name>Task 1: Write docs/PORT-REGISTRY.md (reserved block, exclusions, rule, seed row, honest caveats)</name>
  <read_first>
    - examples/phoenix_host/.env (created in 125-01 — the committed PORT/COMPOSE_PROJECT_NAME the registry records)
    - .planning/phases/125-containerized-shared-backend-port-convention/125-PATTERNS.md (docs/PORT-REGISTRY.md content shape; the three asserted strings)
    - .planning/phases/125-containerized-shared-backend-port-convention/125-CONTEXT.md (D-11, D-12)
    - prompts/crosswake-brand-book.md (voice/tone — brand-voiced but honest, no native overclaim) — skim
  </read_first>
  <files>docs/PORT-REGISTRY.md</files>
  <action>
    Author docs/PORT-REGISTRY.md (brand-voiced, honest). It MUST contain, as plain content the drift test can scan:
    - A reserved block statement: ports 4700 through 4799 are reserved for the maintainer's OSS lib demos.
    - An exclusions list naming: 3000 (React/Next), 4000 (Phoenix default), 4002 (old crosswake demo default), 5000 (macOS AirPlay), 5173 (Vite), 8080, and the IANA ephemeral range starting 49152.
    - An allocation rule in prose: take the next free port in the block, one per lib, commit PORT= and COMPOSE_PROJECT_NAME= in the lib's .env, and add a row to the registry table.
    - A registry table whose seed row records crosswake at port 4700, project name crosswake, host path examples/phoenix_host. The literal `crosswake` and the literal `4700` must both appear.
    - The COMPOSE_PROJECT_NAME caveat (D-12), stated honestly: COMPOSE_PROJECT_NAME namespaces container/network/volume NAMES, NOT host port bindings — the unique committed port is the real collision guard. The literal `COMPOSE_PROJECT_NAME` must appear.
    - The Android note: the emulator reaches the host backend at `10.0.2.2` (not localhost). The literal `10.0.2.2` must appear.

    Do NOT overclaim native support. Keep it a reusable convention doc the maintainer's other repos can adopt.
  </action>
  <verify>
    <automated>test -f docs/PORT-REGISTRY.md && for s in 'COMPOSE_PROJECT_NAME' '10.0.2.2' 'crosswake' '4700' '4700' '4799'; do grep -qF "$s" docs/PORT-REGISTRY.md || { echo "MISSING $s"; exit 1; }; done && grep -q '4002' docs/PORT-REGISTRY.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `docs/PORT-REGISTRY.md` exists and contains the literals `COMPOSE_PROJECT_NAME`, `10.0.2.2`, `crosswake`, `4700`, `4799`
    - It documents the reserved 4700-4799 block, the exclusion list (including 4002 as the old default), the allocation rule, and a crosswake=4700 seed row
    - It honestly states COMPOSE_PROJECT_NAME namespaces names not host ports, and that the committed port is the collision guard
  </acceptance_criteria>
  <done>A reusable, honest, brand-voiced PORT-REGISTRY documents the convention with the crosswake seed row and the COMPOSE_PROJECT_NAME / Android 10.0.2.2 caveats.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: New source-derived drift test for PORT-REGISTRY (mirror quick_start_adoption_drift_test)</name>
  <read_first>
    - test/crosswake/guides/quick_start_adoption_drift_test.exs (lines 6-8 attrs, 134-138 phoenix_host_port, 388-415 failure/format helpers, 430-435 source_port!, 588-594 require_contains)
    - examples/phoenix_host/config/runtime.exs (created in 125-01 — the port source of truth)
    - docs/PORT-REGISTRY.md (Task 1 output — the doc under test)
    - .planning/phases/125-containerized-shared-backend-port-convention/125-PATTERNS.md (port_registry_test.exs section + Shared Patterns)
    - .planning/phases/125-containerized-shared-backend-port-convention/125-CONTEXT.md (D-13)
  </read_first>
  <behavior>
    - Test: committed_port/0 derives the port from runtime.exs via the `System.get_env("PORT") || "NNNN"` regex and returns "4700"
    - Test: PORT-REGISTRY.md contains the derived port, the literal COMPOSE_PROJECT_NAME, the literal 10.0.2.2, and a crosswake seed row → zero failures
    - Test (negative): a synthetic registry with COMPOSE_PROJECT_NAME stripped yields a :missing_caveat failure (scanner rejects stale input)
    - Test (negative): a synthetic registry with 10.0.2.2 stripped yields a :missing_android_note failure
  </behavior>
  <files>test/crosswake/guides/port_registry_test.exs</files>
  <action>
    Create test/crosswake/guides/port_registry_test.exs as `Crosswake.Guides.PortRegistryTest` using `ExUnit.Case, async: true`. Module attributes: `@port_registry_path "docs/PORT-REGISTRY.md"`, `@env_path "examples/phoenix_host/.env"`, `@runtime_config_path "examples/phoenix_host/config/runtime.exs"`.

    Copy these helpers VERBATIM from the analog quick_start_adoption_drift_test.exs (do not rewrite): `source_port!/3` (lines ~430-435), `failure/3` and `format_failures/1` (lines ~388-415), and `require_contains/5` (lines ~588-594). Implement `committed_port/0` to read `@runtime_config_path` and call `source_port!` with regex `~r/System\.get_env\("PORT"\)\s*\|\|\s*"(\d+)"/` (the port lives in runtime.exs after 125-01 — this is the single source of truth; do NOT hardcode 4700).

    Write a positive test asserting committed_port() is a digit string and that PORT-REGISTRY contains it; and a `require_contains`-based test that aggregates failures for COMPOSE_PROJECT_NAME (`:missing_caveat`), 10.0.2.2 (`:missing_android_note`), the derived port (`:wrong_port`), and `crosswake` (`:missing_seed_row`), asserting failures == []. Add at least two negative tests that strip COMPOSE_PROJECT_NAME / 10.0.2.2 from a copy of the contents and assert the corresponding failure category is produced (use a small `scan_registry/1` over `{synthetic_path, contents}` mirroring the analog's scanner shape, or apply `require_contains` directly to the mutated contents). The test must be fully source-derived — no literal 4700 in any assertion.
  </action>
  <verify>
    <automated>mix test test/crosswake/guides/port_registry_test.exs --color 2>&1 | tail -5; mix test test/crosswake/guides/port_registry_test.exs >/dev/null 2>&1 && grep -q '@runtime_config_path "examples/phoenix_host/config/runtime.exs"' test/crosswake/guides/port_registry_test.exs && ! grep -q 'assert.*4700' test/crosswake/guides/port_registry_test.exs && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `mix test test/crosswake/guides/port_registry_test.exs` exits 0
    - The test reads the port from `examples/phoenix_host/config/runtime.exs` (not config.exs, not a literal)
    - No assertion hardcodes `4700`; the port is derived
    - Negative tests prove the scanner rejects a registry missing COMPOSE_PROJECT_NAME and missing 10.0.2.2
  </acceptance_criteria>
  <done>A new doc-contract test guards PORT-REGISTRY drift the same way the quick-start test guards the quick-start doc, deriving the port from runtime.exs and rejecting stale registries.</done>
</task>

<task type="auto">
  <name>Task 3: Migrate QUICK_START.md to 4700 + add Docker path; run full quick-start drift test green</name>
  <read_first>
    - examples/QUICK_START.md (9 refs to 4002: lines ~27, 30, 43, 44, 45, 60, 61, 147, 165)
    - test/crosswake/guides/quick_start_adoption_drift_test.exs (line ~186 PORT= assertion now derives 4700; lines 501-515 wrong_port scan for localhost:NNNN / PORT=NNNN)
    - .planning/phases/125-containerized-shared-backend-port-convention/125-PATTERNS.md (examples/QUICK_START.md section)
    - .planning/phases/125-containerized-shared-backend-port-convention/125-CONTEXT.md (D-02, D-03)
  </read_first>
  <files>examples/QUICK_START.md</files>
  <action>
    Replace every `4002` occurrence in examples/QUICK_START.md with `4700` (all ~9 refs: the `PORT=4002 mix phx.server` commands, the `http://localhost:4002/...` URLs, the Playwright port note, and the troubleshooting line). After the change, the file must contain zero `4002` references.

    Add a Docker boot section documenting the one-command path: state that `docker compose up` from `examples/phoenix_host/` boots the shared backend with no local Elixir/Node/SQLite toolchain and serves the app at `http://localhost:4700`, and note that `PORT=4700 mix phx.server` remains the documented native alternative. Keep the existing source-derived command string `PORT=4700 mix phx.server` intact (the drift test asserts `PORT=#{port} mix phx.server` where port is now 4700). Do NOT overclaim native support; keep support labels honest.

    Then run the full quick_start_adoption_drift_test.exs — it is now fully satisfiable: the port derives to 4700 from runtime.exs (redirected in 125-01), and QUICK_START shows 4700 everywhere with the `PORT=4700 mix phx.server` command present.
  </action>
  <verify>
    <automated>! grep -q '4002' examples/QUICK_START.md && grep -q 'docker compose up' examples/QUICK_START.md && grep -q 'PORT=4700 mix phx.server' examples/QUICK_START.md && grep -q 'localhost:4700' examples/QUICK_START.md && mix test test/crosswake/guides/quick_start_adoption_drift_test.exs >/dev/null 2>&1 && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `examples/QUICK_START.md` contains no `4002` and uses `4700` for all ports/URLs/commands
    - It documents the `docker compose up` boot path serving `http://localhost:4700` and keeps `PORT=4700 mix phx.server` as the native alternative
    - `mix test test/crosswake/guides/quick_start_adoption_drift_test.exs` exits 0
  </acceptance_criteria>
  <done>QUICK_START is fully migrated to 4700 with an added Docker path; the existing quick-start drift test is green end-to-end, closing the port migration.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| committed docs → adopters | PORT-REGISTRY and QUICK_START are public guidance; misstatements mislead other repos/adopters |
| source config → doc claims | doc-contract test asserts docs match the source-of-truth port (prevents drift/dishonesty) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-125-08 | Repudiation / Drift | PORT-REGISTRY.md vs source port | mitigate | port_registry_test.exs derives the port from runtime.exs and fails the build if the registry drifts — claims cannot silently diverge from source |
| T-125-09 | Information Disclosure | PORT-REGISTRY / QUICK_START content | mitigate | Docs carry only ports, project names, and host paths — no secrets, keys, or credentials are documented (Android 10.0.2.2 is a public loopback alias, not sensitive) |
| T-125-10 | Spoofing (overclaim) | QUICK_START native-support labels | mitigate | Docker path is documented without overclaiming native support; honest support labels preserved per the repo's no-overclaim culture |
</threat_model>

<verification>
- `docs/PORT-REGISTRY.md` contains COMPOSE_PROJECT_NAME, 10.0.2.2, crosswake, 4700, 4799 and the exclusion list
- `mix test test/crosswake/guides/port_registry_test.exs` exits 0 (source-derived, no hardcoded 4700)
- `grep -rn 4002 examples/QUICK_START.md` returns no matches
- `mix test test/crosswake/guides/quick_start_adoption_drift_test.exs` exits 0 (full port migration green)
</verification>

<success_criteria>
- Reusable PORT-REGISTRY documents the convention + crosswake seed row honestly (PORT-03)
- New drift test guards the registry against source-port drift (DOCS-03 culture)
- QUICK_START fully on 4700 with a Docker boot path; quick-start drift test green (PORT-01/PORT-02)
</success_criteria>

<output>
Create `.planning/phases/125-containerized-shared-backend-port-convention/125-03-SUMMARY.md` when done
</output>
