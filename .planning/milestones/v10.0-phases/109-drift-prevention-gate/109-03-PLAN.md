---
phase: 109-drift-prevention-gate
plan: 03
type: execute
wave: 2
depends_on: [109-01]
files_modified:
  - .github/workflows/brandbook-verify.yml
autonomous: true
requirements: [PROOF-01]
nyquist_compliant: true

must_haves:
  truths:
    - "A new step runs `node check-consumer-drift.mjs` inside the existing `brand-structural` job (same required check name, no new job)"
    - "The new step is placed after the `WCAG contrast matrix` step and before the `Install brand e2e dependencies` step (fails fast, before Playwright)"
    - "The workflow `on.paths` triggers on the 4 consumer globs for both pull_request and push"
    - "`priv/static/crosswake/**` is in on.paths so the existing tokens.css byte-parity test (D-04) also fires on direct tokens.css edits"
    - "The advisory `brand-visual` job is unchanged; no new required-status-check name is introduced"
  artifacts:
    - path: ".github/workflows/brandbook-verify.yml"
      provides: "brand-structural drift-gate step + broadened on.paths triggers"
      contains: "check-consumer-drift.mjs"
  key_links:
    - from: ".github/workflows/brandbook-verify.yml brand-structural job"
      to: "brandbook/tools/check-consumer-drift.mjs"
      via: "new step: working-directory brandbook/tools, run node check-consumer-drift.mjs"
      pattern: "node check-consumer-drift\\.mjs"
    - from: "on.paths (pull_request + push)"
      to: "the normalized consumer files"
      via: "4 added consumer globs"
      pattern: "priv/static/crosswake/\\*\\*"
---

<objective>
Wire the drift gate into CI by editing `.github/workflows/brandbook-verify.yml`: add one new step running `node check-consumer-drift.mjs` inside the existing required `brand-structural` job (D-01 — same check name, no new job, no new branch-protection entry), positioned after `WCAG contrast matrix` and before the Playwright install steps so drift fails fast and cheap; and broaden `on.paths` (both `pull_request` and `push`) with the 4 consumer globs (D-01a) so the gate actually fires on consumer/token edits.

Purpose: PROOF-01 requires the check to run in the required `brand-structural` lane (SC #3). Without the `on.paths` broadening the gate would never fire on consumer edits, and the existing tokens.css byte-parity test (D-04) would never run on direct tokens.css edits — adding `priv/static/crosswake/**` closes both gaps with zero new code.
Output: An edited `brandbook-verify.yml` with the new step and broadened triggers; `brand-visual` and the existing required check name untouched.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/109-drift-prevention-gate/109-CONTEXT.md
@.planning/phases/109-drift-prevention-gate/109-RESEARCH.md
@.planning/phases/109-drift-prevention-gate/109-PATTERNS.md

# Plan 01 produced the script this step invokes.
@.planning/phases/109-drift-prevention-gate/109-01-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Broaden on.paths with the 4 consumer globs (pull_request + push)</name>
  <files>.github/workflows/brandbook-verify.yml</files>
  <read_first>
    - .github/workflows/brandbook-verify.yml (lines 1-30 — the header comment and the current on.paths blocks at lines 21-25, currently `brandbook/**` only for both triggers)
    - .planning/phases/109-drift-prevention-gate/109-PATTERNS.md (lines 393-423 — exact on.paths replacement block + the D-04 free-win note)
    - .planning/phases/109-drift-prevention-gate/109-RESEARCH.md (YAML Anchors section — current state of on.paths)
  </read_first>
  <action>
    In `.github/workflows/brandbook-verify.yml`, edit BOTH the `pull_request.paths` and `push.paths` lists (currently each contains only `- "brandbook/**"`). Append these 4 globs to EACH list, keeping `brandbook/**` first:
      - `examples/phoenix_host/priv/static/css/app.css`
      - `priv/static/crosswake/**`
      - `priv/templates/crosswake/offline_ui/**`
      - `examples/phoenix_host/lib/crosswake_example_web/controllers/offline_html/**`
    Do not touch any other part of the file in this task. The `priv/static/crosswake/**` glob is the D-04 free win — it makes the existing `compile-tokens.test.mjs:222` byte-parity test fire on direct `tokens.css` edits. No fenced code in this action.
  </action>
  <verify>
    <automated>grep -c 'examples/phoenix_host/priv/static/css/app.css' .github/workflows/brandbook-verify.yml | grep -qx 2 && grep -c 'priv/static/crosswake/\*\*' .github/workflows/brandbook-verify.yml | grep -qx 2 && grep -c 'priv/templates/crosswake/offline_ui/\*\*' .github/workflows/brandbook-verify.yml | grep -qx 2 && grep -c 'controllers/offline_html/\*\*' .github/workflows/brandbook-verify.yml | grep -qx 2 && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - Each of the 4 consumer globs appears exactly twice in the file (once under `pull_request.paths`, once under `push.paths`) — the grep gate prints `PASS`.
    - `brandbook/**` remains present (not replaced) in both trigger lists.
    - No step, job, or comment outside the two `paths:` lists is modified in this task.
  </acceptance_criteria>
  <done>Both `on.paths` lists trigger on the 4 consumer globs plus the original `brandbook/**`, so consumer and tokens.css edits fire the gate.</done>
</task>

<task type="auto">
  <name>Task 2: Add the drift-gate step inside brand-structural before Playwright install</name>
  <files>.github/workflows/brandbook-verify.yml</files>
  <read_first>
    - .github/workflows/brandbook-verify.yml (lines 33-100 — the brand-structural step list; `WCAG contrast matrix` step at ~line 68, `Install brand e2e dependencies` at ~line 72; the plain-node step shape of `SVG structural validation - production marks`)
    - .planning/phases/109-drift-prevention-gate/109-PATTERNS.md (lines 373-391 — exact new-step YAML shape and insertion point)
    - .planning/phases/109-drift-prevention-gate/109-RESEARCH.md (brand-structural step list — confirmed insertion point after WCAG contrast matrix, before Install brand e2e dependencies)
  </read_first>
  <action>
    Insert a single new step into the `brand-structural` job, AFTER the `WCAG contrast matrix` step and BEFORE the `Install brand e2e dependencies` step (the "before Playwright install" position per D-01, so drift fails fast and cheap). Use the established plain-node step shape:
      - name: `Consumer drift gate (no hex / no primitives / semantic coverage)`
      - working-directory: `brandbook/tools`
      - run: `node check-consumer-drift.mjs`
    The `working-directory: brandbook/tools` is correct because the script resolves manifest paths via its internal `ROOT = resolve(__dirname, '../..')` regardless of cwd. Do NOT add a new job, do NOT introduce a new required-status-check name, and do NOT touch the `brand-visual` job or its `continue-on-error: true`. (The contract test from Plan 02 can optionally be added as a second `node --test` step in a follow-up, but the required deliverable for PROOF-01 SC #3 is this single drift-gate step in the required lane.) No fenced code in this action.
  </action>
  <verify>
    <automated>cd /Users/jon/projects/crosswake && node -e "const fs=require('fs');const y=fs.readFileSync('.github/workflows/brandbook-verify.yml','utf8');const i=y.indexOf('node check-consumer-drift.mjs');const w=y.indexOf('WCAG contrast matrix');const p=y.indexOf('Install brand e2e dependencies');if(i>-1 && w>-1 && p>-1 && i>w && i<p){console.log('PASS')}else{console.error('FAIL',{i,w,p});process.exit(1)}"</automated>
  </verify>
  <acceptance_criteria>
    - `.github/workflows/brandbook-verify.yml` contains a `node check-consumer-drift.mjs` run line positioned textually after `WCAG contrast matrix` and before `Install brand e2e dependencies` — the node ordering gate prints `PASS`.
    - The step uses `working-directory: brandbook/tools` and the step name `Consumer drift gate (no hex / no primitives / semantic coverage)`.
    - The `brand-visual` job and its `continue-on-error: true` are unchanged; no new job and no new required-status-check name introduced (D-01).
    - The YAML still parses: `node -e "require('js-yaml')"` is not assumed available, so the ordering check above doubles as a structural sanity check that the run line is present in the brand-structural step list.
  </acceptance_criteria>
  <done>The required `brand-structural` job runs `node check-consumer-drift.mjs` before the Playwright install steps, keeping one required check name and leaving the advisory job untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI trigger config → brand-structural job | The workflow runs on PR/push to the listed paths and executes the read-only drift script. No untrusted input enters the script beyond committed files; no secrets are referenced by the new step. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-109-05 | Tampering (gate bypass) | on.paths coverage | mitigate | If `on.paths` omits a consumer glob, edits to that file would never trigger the gate. Mitigated by the Task 1 grep gate asserting all 4 globs appear exactly twice (PR + push), so every manifested consumer path is covered. |
| T-109-06 | Elevation of Privilege | new required check name | accept | No new required-status-check name is introduced (D-01) — the step lives inside the existing `brand-structural` check, so branch-protection (harness-blocked) needs no change. No privilege surface added. |
| T-109-07 | Denial of Service | full brandbook job on consumer edits | accept | Broadening on.paths means consumer-only edits now also run the Playwright steps. Accepted trade-off per D-01a for this low-frequency repo; the drift step runs first and fails fast before Playwright install. |
| T-109-SC | Tampering | npm/pip/cargo installs | accept | No package installs added by this workflow edit; the new step runs an existing committed Node script. Package legitimacy gate N/A. |
</threat_model>

<verification>
- The 4 consumer globs appear exactly twice each in `on.paths` (Task 1 grep gate prints `PASS`).
- `node check-consumer-drift.mjs` runs in `brand-structural` after `WCAG contrast matrix` and before `Install brand e2e dependencies` (Task 2 ordering gate prints `PASS`).
- `brand-visual` job and the existing required check name are unchanged.
</verification>

<success_criteria>
- `brandbook-verify.yml` runs the drift gate inside the required `brand-structural` job before Playwright install (PROOF-01 SC #3 — runs in the required, deterministic lane).
- `on.paths` triggers on all 4 consumer globs for both pull_request and push, including `priv/static/crosswake/**` (D-04 free win).
- No new job, no new required-status-check name, advisory `brand-visual` untouched.
</success_criteria>

<output>
Create `.planning/phases/109-drift-prevention-gate/109-03-SUMMARY.md` when done.
</output>
