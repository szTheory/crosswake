---
phase: 109-drift-prevention-gate
plan: 02
type: tdd
wave: 2
depends_on: [109-01]
files_modified:
  - brandbook/tools/check-consumer-drift.test.mjs
autonomous: true
requirements: [PROOF-01]
nyquist_compliant: true

must_haves:
  truths:
    - "`node --test brandbook/tools/check-consumer-drift.test.mjs` passes on the current clean tree"
    - "The contract test pins all 5 manifest paths exist on disk (manifest completeness)"
    - "The contract test asserts the gate exits 0 on the current normalized tree (green baseline)"
    - "Each PROOF-01 success criterion maps to an explicit automated assertion: hex injection, lost-var, primitive injection"
    - "Each false-positive guard has a passing assertion: #id selector, rgba shadow, inline display:flex, [scrollbar-gutter:stable]"
    - "The retired-Tailwind blocklist is pinned so the gate's own rules cannot silently rot"
  artifacts:
    - path: "brandbook/tools/check-consumer-drift.test.mjs"
      provides: "node:test contract/pin test exercising the exported check functions with synthetic fixtures + green-baseline assertion"
      min_lines: 70
      contains: "check-consumer-drift.mjs"
  key_links:
    - from: "brandbook/tools/check-consumer-drift.test.mjs"
      to: "brandbook/tools/check-consumer-drift.mjs"
      via: "named import of exported check functions + MANIFEST"
      pattern: "from '\\./check-consumer-drift\\.mjs'"
    - from: "green-baseline test"
      to: "node brandbook/tools/check-consumer-drift.mjs"
      via: "execSync run asserting exit 0 on the real tree"
      pattern: "execSync"
---

<objective>
Create the companion contract/pin test `brandbook/tools/check-consumer-drift.test.mjs` that mirrors the NORM-04 generator-test pattern: it pins the manifest completeness, asserts the green baseline (gate exits 0 on the current tree), and proves every PROOF-01 success criterion with synthetic positive fixtures (injected hex, primitive ref, stripped `var`) and every false-positive guard with negative fixtures. This is the Wave 0/validation deliverable that prevents the gate's own assertions from silently rotting.

Purpose: A gate that can silently PASS when it should fail is the genuine risk for a confirmatory drift gate (T-109-01). This test is the mitigation — it exercises the exported check functions with synthetic fixtures so a future regression in the detection logic fails the build.
Output: `brandbook/tools/check-consumer-drift.test.mjs` — `node:test` suite, green on the current tree, with one assertion per PROOF-01 SC and one per false-positive guard.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/109-drift-prevention-gate/109-RESEARCH.md
@.planning/phases/109-drift-prevention-gate/109-PATTERNS.md
@.planning/phases/109-drift-prevention-gate/109-VALIDATION.md

# Plan 01 produced the script under test — its exports are the contract this test pins.
@.planning/phases/109-drift-prevention-gate/109-01-PLAN.md

<interfaces>
<!-- Contract exported by brandbook/tools/check-consumer-drift.mjs (Plan 01). Import these directly. -->

Named exports to import from './check-consumer-drift.mjs':
  findHexColors(content) -> [{ line, text, rule: 'hex-color-forbidden' }]
  findPrimitiveRefs(content) -> [{ line, text, rule: 'primitive-token-forbidden' }]
  checkCssSemanticCoverage(content) -> { ok: boolean }
  findRetiredTailwindInClassAttrs(content) -> [{ line, text, rule: 'retired-tailwind-class-forbidden' }]
  checkFile(entry, content) -> violations[]
  MANIFEST -> [{ path, type }] (5 entries)

node:test convention (from compile-tokens.test.mjs:1-16):
  import { test } from 'node:test';
  import assert from 'node:assert/strict';
  import { readFileSync, existsSync } from 'node:fs';
  import { resolve, dirname } from 'node:path';
  import { fileURLToPath } from 'node:url';
  const ROOT = resolve(dirname(fileURLToPath(import.meta.url)), '../..');
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Synthetic-fixture assertions for every rule + false-positive guard</name>
  <files>brandbook/tools/check-consumer-drift.test.mjs</files>
  <read_first>
    - brandbook/tools/check-consumer-drift.mjs (the module from Plan 01 — confirm the exact exported names before importing)
    - brandbook/tools/compile-tokens.test.mjs (the node:test idiom: top-level test(), assert.strictEqual/assert.ok, ROOT derivation, existsSync pin pattern)
    - .planning/phases/109-drift-prevention-gate/109-PATTERNS.md (lines 249-356 — exact test block shapes for each fixture)
    - .planning/phases/109-drift-prevention-gate/109-RESEARCH.md (Contract/Pin Test section, the 8-item assertion list)
  </read_first>
  <action>
    Create `brandbook/tools/check-consumer-drift.test.mjs` using the `node:test` convention (`test()` top-level, `assert` from `node:assert/strict`). Derive `ROOT = resolve(dirname(fileURLToPath(import.meta.url)), '../..')`. Import the named exports `findHexColors, findPrimitiveRefs, checkCssSemanticCoverage, findRetiredTailwindInClassAttrs, MANIFEST` from `./check-consumer-drift.mjs`.

    Write these assertions (each a separate top-level `test()` with a descriptive name), using in-memory string fixtures — do NOT write temp files and do NOT mutate real consumers:
      - Manifest completeness: every `MANIFEST` entry path resolves to an existing file via `existsSync(resolve(ROOT, entry.path))`; assert exactly 5 entries.
      - PROOF-01 SC #1 (hex injection): `findHexColors('color: #2B756A;')` has length 1 and text includes `#2B756A` (and the palette name `wake-700`).
      - PROOF-01 SC #2 (lost coverage): `checkCssSemanticCoverage('body { color: red; }').ok === false`; and `checkCssSemanticCoverage('a { color: var(--cw-text-default); }').ok === true`.
      - Primitive injection: `findPrimitiveRefs('color: var(--cw-primitive-foam-50);')` length ≥ 1; `findPrimitiveRefs('color: var(--cw-surface-default);')` length 0.
      - Retired Tailwind in class attr: `findRetiredTailwindInClassAttrs('<div class="flex items-center">')` flags `flex`.
      - Guard — `#id` selector: `findHexColors('#status { color: var(--cw-text-muted); }')` length 0.
      - Guard — rgba shadow: `findHexColors('box-shadow: 0 4px 6px rgba(9,20,26,0.06);')` length 0.
      - Guard — inline `display:flex`: `findRetiredTailwindInClassAttrs('<style>body { display: flex; }</style><div class="btn-primary">')` length 0.
      - Guard — `[scrollbar-gutter:stable]`: `findRetiredTailwindInClassAttrs('<html lang="en" class="[scrollbar-gutter:stable]">')` length 0.
      - Blocklist pin: assert each of `flex`, `bg-white`, `bg-cw-`, `text-cw-`, `min-h-screen`, `border-cw-`, `border-gray-`, `space-y-`, `max-w-md` is detected when placed in a class attr (loop over the 9 tokens, building `<div class="${t}">`), pinning the NORM-04-derived blocklist so it cannot silently shrink.

    No fenced code in this action — the exact test bodies are in 109-PATTERNS.md lines 249-356.
  </action>
  <verify>
    <automated>node --test brandbook/tools/check-consumer-drift.test.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `node --test brandbook/tools/check-consumer-drift.test.mjs` reports all tests passing, zero failures.
    - There is a distinct passing test for each: hex injection (SC #1), lost-var (SC #2), primitive injection, retired-Tailwind-in-class, manifest completeness, and each of the 4 false-positive guards.
    - The retired-Tailwind blocklist pin loops over all 9 tokens and asserts each is flagged in a class attr.
    - No temp files written; all fixtures are in-memory strings; no real consumer files mutated.
  </acceptance_criteria>
  <done>The contract test imports the exported check functions and passes with one explicit assertion per PROOF-01 success criterion and per false-positive guard, plus a 9-token blocklist pin.</done>
</task>

<task type="auto">
  <name>Task 2: Green-baseline integration assertion against the real tree</name>
  <files>brandbook/tools/check-consumer-drift.test.mjs</files>
  <read_first>
    - brandbook/tools/check-consumer-drift.test.mjs (extend the file from Task 1)
    - brandbook/tools/compile-tokens.test.mjs (lines ~155-227 — the execSync determinism/byte-parity test pattern to mirror for an end-to-end run)
    - .planning/phases/109-drift-prevention-gate/109-PATTERNS.md (lines 358-369 — green-baseline test shape)
  </read_first>
  <action>
    Add a green-baseline integration test that runs the actual script end-to-end against the live tree and asserts exit 0. Use `execSync` from `node:child_process` with `cwd: ROOT` and `stdio: 'pipe'`, invoking `node brandbook/tools/check-consumer-drift.mjs` (resolve the absolute script path from ROOT). Wrap in `assert.doesNotThrow(...)` with the message `gate must exit 0 on the current normalized tree` — `execSync` throws on non-zero exit, so a clean tree passes and any drift fails.

    This is the single test that proves PROOF-01's green-baseline acceptance: the gate lands green on the confirmatory tree. No fenced code in this action.
  </action>
  <verify>
    <automated>node --test brandbook/tools/check-consumer-drift.test.mjs</automated>
  </verify>
  <acceptance_criteria>
    - The green-baseline test runs `node brandbook/tools/check-consumer-drift.mjs` via `execSync` and asserts it does not throw (exit 0) on the current tree.
    - Full suite `node --test brandbook/tools/check-consumer-drift.test.mjs` passes including the new integration test.
    - The test would FAIL (execSync throws) if any manifested consumer reintroduced a hex literal or lost its `var(--cw-` coverage — confirming the gate is not a silent pass (T-109-01 mitigation).
  </acceptance_criteria>
  <done>The contract test includes a passing end-to-end green-baseline assertion that runs the real script and verifies exit 0 on the current normalized tree.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test fixtures → check functions | All inputs are in-memory string literals authored in the test; no untrusted input, no network. The green-baseline test reads only committed repo files. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-109-01 | Tampering (gate correctness) | check-consumer-drift detection rules | mitigate | THIS PLAN is the mitigation for the phase's primary risk — a gate that silently passes. Synthetic positive fixtures (injected hex/primitive/stripped-var) force each rule to fire; the 9-token blocklist pin prevents silent rule shrinkage; the green-baseline test proves exit 0 on the clean tree. |
| T-109-04 | Repudiation | test coverage drift | accept | The test file itself could be weakened in a future edit; this is governed by code review, not a runtime control. Low risk for an internal CI test. |
| T-109-SC | Tampering | npm/pip/cargo installs | accept | No package installs — uses only Node built-ins (`node:test`, `node:assert`, `node:fs`, `node:path`, `node:url`, `node:child_process`) plus the Plan 01 sibling module. Package legitimacy gate N/A. |
</threat_model>

<verification>
- `node --test brandbook/tools/check-consumer-drift.test.mjs` passes with zero failures on the current tree.
- One passing assertion exists per PROOF-01 success criterion and per false-positive guard.
- The green-baseline integration test proves the gate exits 0 end-to-end.
</verification>

<success_criteria>
- `brandbook/tools/check-consumer-drift.test.mjs` exists and passes on the current clean tree.
- Manifest completeness, all four drift rules, all four false-positive guards, and the 9-token blocklist are each pinned by an automated assertion.
- The green-baseline integration test runs the real script and asserts exit 0 (confirmatory gate lands green).
</success_criteria>

<output>
Create `.planning/phases/109-drift-prevention-gate/109-02-SUMMARY.md` when done.
</output>
