---
phase: 108-consumer-normalization
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - examples/phoenix_host/priv/static/css/app.css
  - examples/phoenix_host/assets/css/app.css
  - examples/phoenix_host/lib/crosswake_example_web/controllers/offline_html/index.html.heex
autonomous: true
requirements: [NORM-01]
must_haves:
  truths:
    - "The SERVED example host CSS renders flashcard/card/button/badge styling in light AND dark mode with zero CSS additions to app.css (D-12, D-04)"
    - "examples/phoenix_host/priv/static/css/app.css contains zero flat primitive aliases and zero hand-declared font stacks — its :root palette/font block is deleted (D-04, D-12)"
    - "Every color/font/radius/size value in app.css and index.html.heex resolves through a --cw-* SEMANTIC token; no --cw-primitive-* references and no var() fallbacks remain (D-04, D-10)"
    - "Secondary/muted text uses --cw-text-muted (stone-600), never --cw-text-subtle (stone-500) (D-05)"
    - "The example offline page index.html.heex consumes tokens.css (linked) and references semantic tokens instead of Tailwind-system hex (#3b82f6/#10b981/#ef4444/system-ui) (D-12)"
    - "The unserved duplicate assets/css/app.css is removed (or its unique still-used rules folded into the served file) after a JS-reference check confirms safety (D-12)"
  artifacts:
    - path: "examples/phoenix_host/priv/static/css/app.css"
      provides: "Normalized SERVED host CSS on semantic tokens, no flat palette"
      contains: "var(--cw-surface-default)"
    - path: "examples/phoenix_host/lib/crosswake_example_web/controllers/offline_html/index.html.heex"
      provides: "Example offline page restyled onto semantic tokens"
      contains: "var(--cw-"
  key_links:
    - from: "examples/phoenix_host/lib/.../deck_live/index.ex"
      to: "examples/phoenix_host/priv/static/css/app.css"
      via: "<link href=/css/app.css> after /css/tokens.css (lines 15-16)"
      pattern: "css/app\\.css"
    - from: "examples/phoenix_host/priv/static/css/app.css"
      to: "priv/static/css/tokens.css"
      via: "consumes --cw-* semantic custom properties defined upstream"
      pattern: "var\\(--cw-(surface|text|action|border|radius|text-scale)"
---

<objective>
Normalize the example host's REAL served consumers onto the semantic token tier (NORM-01). This is the file DeckLive actually renders, so it is what makes dark mode visibly work on the demo.

Three files: the SERVED `priv/static/css/app.css` (delete its hand-authored flat `:root` palette + font stacks, remap every component-rule value to a semantic `var(--cw-*)`), the unserved duplicate `assets/css/app.css` (reconcile per D-12 — delete after a JS-reference safety check), and the example's standalone offline page `index.html.heex` (replace its inline Tailwind-system-hex `<style>` block with semantic-token references and a `tokens.css` link).

Purpose: Make the demo a genuine, correct, dark-mode-capable demonstration of token consumption — and leave each consumer as a single greppable, semantic-only file so Phase 109's drift gate has a stable target.
Output: One normalized SERVED stylesheet, one removed/reconciled duplicate, one token-backed example offline page. Wiring only — no values change, no new tokens.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/108-consumer-normalization/108-CONTEXT.md
@.planning/phases/108-consumer-normalization/108-PATTERNS.md

<interfaces>
Semantic token names available (the COMPLETE public contract — primitive refs forbidden).
Read priv/static/crosswake/tokens.css for the authoritative source; the names are:

Surfaces: --cw-surface-default, --cw-surface-inset, --cw-surface-raised, --cw-surface-inverse
Text: --cw-text-default, --cw-text-muted (secondary text — use THIS, not --cw-text-subtle), --cw-text-inverse, --cw-text-code
Borders: --cw-border-default, --cw-border-strong, --cw-border-subtle
Actions: --cw-action-bg, --cw-action-hover, --cw-action-fg, --cw-action-focus-ring
Status: --cw-status-success, --cw-status-warning, --cw-status-error, --cw-status-info
Runtime badges: --cw-runtime-offline (kelp-800 light / flips dark), --cw-runtime-native (brass-500, stable)
Fonts: --cw-font-display, --cw-font-body, --cw-font-mono
Sizes: --cw-text-scale-xs(12px), -sm(14px), -md(16px), -lg(18px), -xl(20px)
Radii: --cw-radius-sm(6px), -md(10px), -lg(14px), -xl(20px)
Other: --cw-spacing-base(4px), --cw-focus-ring-width(2px), --cw-tracking-tight(-0.02em)

Dark-mode FLIPPING tokens (D-06 footgun): --cw-status-success (→ wake-500: white text fails AA),
--cw-status-warning, --cw-runtime-offline flip. --cw-runtime-native and --cw-status-error do NOT flip.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Normalize the SERVED examples/phoenix_host/priv/static/css/app.css onto semantic tokens</name>
  <files>examples/phoenix_host/priv/static/css/app.css</files>
  <read_first>
    - examples/phoenix_host/priv/static/css/app.css (the file being modified — 155 lines: flat :root palette lines ~1-24, component rules ~26-156)
    - priv/static/crosswake/tokens.css (SOURCE OF TRUTH for the available semantic --cw-* names; do NOT modify)
    - .planning/phases/108-consumer-normalization/108-PATTERNS.md § "5. examples/phoenix_host/priv/static/css/app.css" (exact line-by-line remap table)
    - .planning/phases/108-consumer-normalization/108-CONTEXT.md (D-04, D-05, D-10)
    - examples/phoenix_host/lib/crosswake_example/flashcards/deck_live/index.ex lines 15-16 (confirms tokens.css is linked before app.css, so deleting the flat :root block is safe)
  </read_first>
  <action>
    Delete the entire hand-authored flat-primitive `:root { ... }` block (the `--cw-current-950: #09141A` … `--cw-white: #FFFFFF` palette AND the `--cw-font-display/body/mono` font-stack declarations, ~lines 1-24). tokens.css is already linked before app.css by DeckLive, so the custom properties are defined upstream.

    Remap every component-rule value to its SEMANTIC token per the PATTERNS.md §5 table (semantic-tier ONLY, D-04 — no `--cw-primitive-*`):
    body bg → var(--cw-surface-default); body text → var(--cw-text-default).
    .page-title color → var(--cw-text-default).
    .card bg → var(--cw-surface-inset); .card border → var(--cw-border-default); .card border-radius 14px → var(--cw-radius-lg).
    .card-title color → var(--cw-text-default); font-size 20px → var(--cw-text-scale-xl).
    .btn-primary bg → var(--cw-action-bg); color → var(--cw-action-fg); border-radius 10px → var(--cw-radius-md).
    .btn-primary:hover → var(--cw-action-hover).
    .btn-primary:focus outline → outline: var(--cw-focus-ring-width) solid var(--cw-action-focus-ring) (D-07: outline, not box-shadow).
    .btn-secondary color → var(--cw-text-default); border → var(--cw-border-default); border-radius 10px → var(--cw-radius-md); :hover → var(--cw-surface-raised).
    .badge bg → var(--cw-surface-raised); color → var(--cw-text-default); border-radius 6px → var(--cw-radius-sm); font-size 12px → var(--cw-text-scale-xs).
    .text-sm color → var(--cw-text-muted) (D-05: muted/stone-600, NOT text-subtle/stone-500); font-size 14px → var(--cw-text-scale-sm).

    Apply NO var() fallbacks anywhere (D-10): write `var(--cw-radius-lg)`, never `var(--cw-radius-lg, 14px)` and never `var(--cw-x, #hex)`.

    LEAVE the ONE non-token value as-is: the `.card` box-shadow `rgba(9, 20, 26, 0.06)` (no shadow token exists; it is an rgba of current-950, not a brand-hex color literal — CONTEXT.md deferred). Do not invent a token for it.

    Do not add any dark-mode block to app.css — tokens.css supplies the dark flips automatically once the flat :root is gone.
  </action>
  <verify>
    <automated>cd /Users/jon/projects/crosswake && test "$(grep -cE '#[0-9A-Fa-f]{6}' examples/phoenix_host/priv/static/css/app.css)" = "0" && grep -q 'var(--cw-surface-default)' examples/phoenix_host/priv/static/css/app.css && grep -q 'var(--cw-text-muted)' examples/phoenix_host/priv/static/css/app.css && test "$(grep -c -- '--cw-primitive-' examples/phoenix_host/priv/static/css/app.css)" = "0" && ! grep -qE 'var\(--cw-[a-z-]+,' examples/phoenix_host/priv/static/css/app.css && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - `grep -cE '#[0-9A-Fa-f]{6}' examples/phoenix_host/priv/static/css/app.css` == 0 (the only remaining rgba is the documented shadow rgba(9,20,26,0.06), which is not a 6-hex literal)
    - File contains `var(--cw-surface-default)` AND `var(--cw-text-default)` AND `var(--cw-action-bg)` AND `var(--cw-text-muted)`
    - `grep -c -- '--cw-primitive-' examples/phoenix_host/priv/static/css/app.css` == 0
    - No `var(--cw-..., fallback)` pattern present (no var() fallbacks, D-10)
    - The flat `:root` block declaring `--cw-current-950`/`--cw-foam-50`/`--cw-white`/inline font stacks is gone
    - File does NOT use `--cw-text-subtle`
  </acceptance_criteria>
  <done>Served app.css references semantic tokens only, no flat palette, no hex (except documented shadow rgba), no primitives, no var fallbacks.</done>
</task>

<task type="auto">
  <name>Task 2: Reconcile the unserved assets/css/app.css duplicate (delete after JS-reference safety check)</name>
  <files>examples/phoenix_host/assets/css/app.css</files>
  <read_first>
    - examples/phoenix_host/assets/css/app.css (the unserved file — 165 lines; carries unique .card-front/.card-back/.btn-success/.btn-danger/.sync-status-*/.badge-offline/.badge-native/@keyframes fadeIn rules)
    - .planning/phases/108-consumer-normalization/108-PATTERNS.md § "6. examples/phoenix_host/assets/css/app.css" and § "Ground-Truth Verification" (the D-12 landmine report)
    - .planning/phases/108-consumer-normalization/108-CONTEXT.md (D-12)
  </read_first>
  <action>
    Run the D-12 safety gate BEFORE deleting. Confirm two things by grep:
    (a) No served HTML/HEEx/router/config references the path `assets/css/app.css` (all link tags use `/css/app.css` → the served `priv/static/css/app.css`).
    (b) The unique class names in this file (`sync-status-pending`, `sync-status-complete`, `card-front`, `card-back`, `btn-success`, `btn-danger`, `badge-offline`, `badge-native`) are styled ELSEWHERE for any SERVED surface. Ground truth established during planning: `sync-status-*` is referenced only by the UNSERVED `examples/phoenix_host/assets/js/app.js` (no asset pipeline serves it); `card-front/card-back/btn-success/btn-danger` are self-contained in `controllers/offline_html/index.html.heex` (its own inline styles, file in Task 3) which loads the served `/offline_study.js`. Therefore the unique rules in this unserved file style only unserved/self-styled surfaces = dead code.

    If BOTH gate conditions hold (no path reference; unique rules style only unserved/self-styled surfaces), DELETE `examples/phoenix_host/assets/css/app.css` entirely so the served file is the single canonical example-host stylesheet that cannot re-drift.

    If the gate reveals a SERVED page that links `assets/css/app.css` OR relies on these unique rules without its own styling, do NOT delete — instead fold the genuinely-used unique rules into the served `priv/static/css/app.css` using semantic tokens (same D-04/D-05/D-10 mapping as Task 1), then delete the unserved file. Record which path was taken in the SUMMARY.
  </action>
  <verify>
    <automated>cd /Users/jon/projects/crosswake && test "$(grep -rl --include='*.heex' --include='*.ex' --include='*.html' 'assets/css/app.css' examples/phoenix_host/lib examples/phoenix_host/config 2>/dev/null | wc -l | tr -d ' ')" = "0" && ! test -f examples/phoenix_host/assets/css/app.css && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn 'assets/css/app.css' examples/phoenix_host/lib examples/phoenix_host/config` returns no served-path link references
    - `examples/phoenix_host/assets/css/app.css` no longer exists (deleted) — OR, if the fold path was taken, the file is deleted AND the genuinely-used unique rules now live in `priv/static/css/app.css` on semantic tokens with zero hex
    - SUMMARY records which reconciliation path (delete vs fold) was taken and the grep evidence
  </acceptance_criteria>
  <done>Exactly one canonical example-host stylesheet remains; the unserved duplicate is gone with documented safety evidence.</done>
</task>

<task type="auto">
  <name>Task 3: Normalize the example offline page index.html.heex inline style block onto semantic tokens</name>
  <files>examples/phoenix_host/lib/crosswake_example_web/controllers/offline_html/index.html.heex</files>
  <read_first>
    - examples/phoenix_host/lib/crosswake_example_web/controllers/offline_html/index.html.heex (the file being modified — inline `<style>` block lines ~7-65 with Tailwind-system hex)
    - priv/static/crosswake/tokens.css (SOURCE OF TRUTH for semantic names)
    - .planning/phases/108-consumer-normalization/108-PATTERNS.md § "7. ...index.html.heex" (exact hex→token remap) and § "D-06 status-button contrast guard"
    - .planning/phases/108-consumer-normalization/108-CONTEXT.md (D-04, D-05, D-06, D-10)
    - examples/phoenix_host/priv/static/css/tokens.css exists and is served at /css/tokens.css (confirmed)
  </read_first>
  <action>
    Add a `<link rel="stylesheet" href="/css/tokens.css">` in the page `<head>` (plain link tag — this is a standalone HTML file without the PhxVerifiedRoutes `~p` sigil) so the semantic custom properties are defined before the inline `<style>` consumes them.

    In the inline `<style>` block, replace every Tailwind-system hex / generic value with a semantic token reference (semantic-tier ONLY, D-04; no var fallbacks, D-10):
    body font-family system-ui,sans-serif → var(--cw-font-body); bg #f3f4f6 → var(--cw-surface-default); color #1f2937 → var(--cw-text-default).
    #flashcard-container bg white → var(--cw-surface-inset); border-radius 0.5rem → var(--cw-radius-md). (Leave the plain `rgba(0,0,0,0.1)` box-shadow as-is — no shadow token, not a brand hex.)
    .card-back border-top #e5e7eb → var(--cw-border-default); color #4b5563 → var(--cw-text-muted) (D-05).
    button border-radius 0.25rem → var(--cw-radius-sm).
    .btn-primary bg #3b82f6 → var(--cw-action-bg); color white → var(--cw-action-fg).
    .btn-success bg #10b981 → var(--cw-status-success) WITH the D-06 footgun: success flips to wake-500 in dark mode where white text fails AA. Set foreground to var(--cw-text-inverse) (theme-aware), NOT hardcoded white. The final solid-vs-outlined choice is confirmed by the render-verify plan (108-04); for now wire the theme-aware foreground.
    .btn-danger bg #ef4444 → var(--cw-status-error) (error does NOT flip; white text is safe but prefer var(--cw-text-inverse) for consistency).
    #status font-size 0.875rem → var(--cw-text-scale-sm); color #6b7280 → var(--cw-text-muted) (D-05).

    Replace no microcopy and do not restructure the page beyond the style values and the tokens.css link.
  </action>
  <verify>
    <automated>cd /Users/jon/projects/crosswake && f=examples/phoenix_host/lib/crosswake_example_web/controllers/offline_html/index.html.heex && grep -q '/css/tokens.css' "$f" && grep -q 'var(--cw-surface-default)' "$f" && grep -q 'var(--cw-status-success)' "$f" && grep -q 'var(--cw-text-inverse)' "$f" && test "$(grep -coE '#(3b82f6|10b981|ef4444|f3f4f6|1f2937|e5e7eb|4b5563|6b7280)' "$f")" = "0" && test "$(grep -c -- '--cw-primitive-' "$f")" = "0" && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - File contains `<link rel="stylesheet" href="/css/tokens.css">`
    - `grep -coE '#(3b82f6|10b981|ef4444|f3f4f6|1f2937|e5e7eb|4b5563|6b7280)' <file>` == 0 (all Tailwind-system hex retired)
    - File contains `var(--cw-surface-default)`, `var(--cw-status-success)`, `var(--cw-status-error)`, `var(--cw-action-bg)`, `var(--cw-text-inverse)`
    - `.btn-success` / `.btn-danger` foreground is `var(--cw-text-inverse)`, NOT hardcoded white (D-06)
    - `grep -c -- '--cw-primitive-' <file>` == 0; no `var(--cw-x, fallback)` patterns
    - `#status` and `.card-back` secondary text use `var(--cw-text-muted)`, not `--cw-text-subtle` (D-05)
  </acceptance_criteria>
  <done>The example offline page links tokens.css and styles entirely through semantic tokens with a theme-aware success-button foreground.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none meaningful) | This plan edits static CSS and one static HEEx page. No auth, no network surface, no user-controlled input flows into these files. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-108-01 | Tampering | index.html.heex inline `<style>` | accept | Values are static brand-token names, no EEx/user interpolation reaches the style block; CSS injection not in play. Honest low-surface reality. |
| T-108-02 | Information Disclosure | served app.css | accept | CSS exposes no secrets; it is brand styling. No PII, no credentials. |
| T-108-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs in this plan; nothing to verify. |
</threat_model>

<verification>
- `grep -cE '#[0-9A-Fa-f]{6}' examples/phoenix_host/priv/static/css/app.css` == 0
- `grep -rc -- '--cw-primitive-' examples/phoenix_host/priv/static/css/app.css examples/phoenix_host/lib/crosswake_example_web/controllers/offline_html/index.html.heex` == 0 across both
- `examples/phoenix_host/assets/css/app.css` does not exist (or fold path documented)
- Both consumers contain `var(--cw-` semantic references
- (Visual confirmation of light+dark rendering is deferred to 108-04 render-verify gate, D-13)
</verification>

<success_criteria>
NORM-01 satisfied: the example host's served CSS and example offline page consume the semantic token tier exclusively, with no duplicated flat palette, no inline font stacks, no Tailwind-system hex, no primitive references, and dark mode flows automatically from tokens.css. One canonical example-host stylesheet remains.
</success_criteria>

<output>
Create `.planning/phases/108-consumer-normalization/108-01-SUMMARY.md` when done. Record the duplicate-reconciliation path taken (delete vs fold) with grep evidence.
</output>
