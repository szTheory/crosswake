---
phase: 39-route-policy-gating-dsl-and-manifest-binding
plan: 02
type: execute
wave: 2
depends_on: ["39-01"]
files_modified:
  - lib/crosswake/manifest/types.ex
  - lib/crosswake/manifest/builder.ex
  - test/crosswake/proof/phase39_route_policy_gating_test.exs
autonomous: true
requirements: [GATE-02]
tags: [elixir, manifest, serialization, route-policy, gating]

must_haves:
  truths:
    - "A route declared with `gated_by: :my_flag` produces a compiled `RouteEntry` whose `gated_by` field holds `:my_flag` (GATE-02, SC#2)"
    - "The compiled `RouteEntry` records the flag *binding* (key + posture) but NO flag *value* — there is no `gated_by_value`/`gate_enabled`/`flag_state` field (GATE-02, SC#3 — the build-time binding / runtime value split, D-07)"
    - "`to_map/1` of a gated `RouteEntry` includes `\"gated_by\" => \"my_flag\"` and `\"on_unavailable\" => \"deny\"` (offline manifest inspection works with no runtime flag service)"
    - "`to_map/1` of a non-gated `RouteEntry` omits the `gated_by` and `on_unavailable` keys entirely (D-06, D-08 nil-omission)"
    - "`{:fallback_phoenix, :home}` serializes reversibly to `\"fallback_phoenix:home\"` (D-08 planner-discretion shape)"
  artifacts:
    - path: "lib/crosswake/manifest/types.ex"
      provides: "RouteEntry gated_by/on_unavailable fields, new_route_entry/1 carry-through, to_map/1 serialization + serialize_on_unavailable/1"
      contains: "serialize_on_unavailable"
    - path: "lib/crosswake/manifest/builder.ex"
      provides: "route_entries/3 pass-through of route.gated_by and route.on_unavailable"
      contains: "gated_by: route.gated_by"
    - path: "test/crosswake/proof/phase39_route_policy_gating_test.exs"
      provides: "GATE-02 manifest round-trip + binding-vs-value split + nil-omission proof cases"
      contains: "gated_by_value"
  key_links:
    - from: "lib/crosswake/manifest/builder.ex route_entries/3"
      to: "lib/crosswake/manifest/types.ex new_route_entry/1"
      via: "gated_by: route.gated_by, on_unavailable: route.on_unavailable"
      pattern: "gated_by: route\\.gated_by"
    - from: "lib/crosswake/manifest/types.ex to_map(%RouteEntry{})"
      to: "JSON map"
      via: "Atom.to_string + serialize_on_unavailable + nil-rejection"
      pattern: "serialize_on_unavailable"
---

<objective>
Carry the `gated_by` and `on_unavailable` bindings from `Policy.Route` (Plan 01) all the way through the manifest layer: add both fields to the `RouteEntry` struct, pass them through `Manifest.Builder.route_entries/3` into `new_route_entry/1`, and serialize them in `to_map/1` with nil-omission for non-gated routes. Then complete the Phase 39 proof test with the GATE-02 manifest round-trip, the SC#3 binding-vs-value split assertion, and the non-gated boundary cases.

This is the manifest-binding layer of Phase 39: the compiled manifest records *which flag governs which route and the declared unavailable posture* — auditable offline with no running flag service — while carrying NO evaluated flag value (the central SC#3 correctness invariant, D-07).

Purpose: Satisfies GATE-02 — the flag *binding* is recorded in the runtime manifest at build time, distinct from the flag *value* (evaluated at runtime in Phase 40).
Output: Extended `RouteEntry`/`new_route_entry/1`/`to_map/1`, extended `Builder.route_entries/3`, completed GATE-02 proof cases.

Scope guard (do NOT pull forward): No runtime evaluation or flag value (Phase 40), no doctor gating category (Phase 41 — SC#2 doctor visibility is satisfied by manifest field presence/introspection alone per PATTERNS "Doctor Visibility" decision; do NOT add a `phase_39_gating_posture` doctor finding), no `{:fallback_phoenix, route_id}` cross-checking against declared routes (Phase 41).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/39-route-policy-gating-dsl-and-manifest-binding/39-CONTEXT.md
@.planning/phases/39-route-policy-gating-dsl-and-manifest-binding/39-RESEARCH.md
@.planning/phases/39-route-policy-gating-dsl-and-manifest-binding/39-PATTERNS.md
@.planning/phases/39-route-policy-gating-dsl-and-manifest-binding/39-VALIDATION.md
@.planning/phases/39-route-policy-gating-dsl-and-manifest-binding/39-01-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend RouteEntry struct, new_route_entry/1, and to_map/1 in Manifest.Types</name>
  <files>lib/crosswake/manifest/types.ex</files>
  <read_first>
    - lib/crosswake/manifest/types.ex — the file being modified. Study the `RouteEntry` `defstruct` and `@type t` (lines 192-229); `new_route_entry/1` (lines 558-576) — note `struct!(RouteEntry, %{...})` with `Keyword.get` for optional fields; the `to_map(%RouteEntry{} = route)` clause (lines 802-819) and the adjacent `to_map(%TransferSeam{} = seam)` clause (lines 828-843) which shows the `&& Atom.to_string` nil-guard and the `|> Enum.reject(fn {_k, v} -> is_nil(v) end) |> Map.new()` nil-omission pipeline.
    - .planning/phases/39-route-policy-gating-dsl-and-manifest-binding/39-CONTEXT.md — D-06 (fields default nil), D-07 (CRITICAL: NO flag-value field), D-08 (serialization shapes + nil-omission).
    - .planning/phases/39-route-policy-gating-dsl-and-manifest-binding/39-PATTERNS.md — "Serialization shape decision": flat `"fallback_phoenix:<route_id>"` string, reversible via `String.split(value, ":", parts: 2)`.
  </read_first>
  <behavior>
    - Types.new_route_entry(id: "r", path: "/r", runtime: :live_view, gated_by: :my_flag, on_unavailable: :deny).gated_by == :my_flag
    - Types.new_route_entry(id: "r", path: "/r", runtime: :live_view).gated_by == nil  (no field passed -> nil)
    - to_map of a RouteEntry with gated_by: :my_flag, on_unavailable: :deny includes "gated_by" => "my_flag" and "on_unavailable" => "deny"
    - to_map of a RouteEntry with on_unavailable: {:fallback_phoenix, :home} includes "on_unavailable" => "fallback_phoenix:home"
    - to_map of a RouteEntry with gated_by: nil, on_unavailable: nil does NOT contain the keys "gated_by" or "on_unavailable"
    - RouteEntry struct has no :gated_by_value, :gate_enabled, or :flag_state key (D-07)
  </behavior>
  <action>
    Extend `RouteEntry` and its serialization in `Crosswake.Manifest.Types`.

    1. Add `:gated_by` and `:on_unavailable` to the `RouteEntry` `defstruct` (lines 196-211), in the no-default leading section (both default `nil`; do NOT add to `@enforce_keys`). CRITICAL (D-07): add ONLY these two fields — no `gated_by_value`, `gate_enabled`, or `flag_state`. The struct carries the binding key and declared posture, never an evaluated flag value.

    2. Add `gated_by: atom() | nil` and `on_unavailable: :deny | {:fallback_phoenix, atom()} | nil` to the `RouteEntry` `@type t` (lines 213-228).

    3. In `new_route_entry/1` (lines 558-576), add `gated_by: Keyword.get(attrs, :gated_by)` and `on_unavailable: Keyword.get(attrs, :on_unavailable)` to the `struct!(RouteEntry, %{...})` map (no default beyond nil).

    4. In `to_map(%RouteEntry{} = route)` (lines 802-819): add `"gated_by" => route.gated_by && Atom.to_string(route.gated_by)` (mirrors the existing `"security"` `&&`-guard) and `"on_unavailable" => serialize_on_unavailable(route.on_unavailable)`. Then pipe the map through nil-omission scoped to these two keys so non-gated routes omit them while existing always-present keys are untouched: `|> Enum.reject(fn {k, v} -> k in ["gated_by", "on_unavailable"] and is_nil(v) end) |> Map.new()`. (Scope the reject to the two new keys only — the existing RouteEntry to_map does not blanket-reject nils and other fields like `"cache_contract"` may legitimately be nil-valued maps.)

    5. Add a private `serialize_on_unavailable/1`: `nil -> nil`; `:deny -> "deny"`; `{:fallback_phoenix, route_id} -> "fallback_phoenix:#{route_id}"`.
  </action>
  <verify>
    <automated>cd /Users/jon/projects/crosswake && mix compile --warnings-as-errors 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - lib/crosswake/manifest/types.ex RouteEntry `defstruct` lists `:gated_by` and `:on_unavailable`
    - lib/crosswake/manifest/types.ex contains `def to_map(%RouteEntry{}` clause referencing `serialize_on_unavailable`
    - lib/crosswake/manifest/types.ex contains `defp serialize_on_unavailable`
    - lib/crosswake/manifest/types.ex RouteEntry does NOT contain `gated_by_value`, `gate_enabled`, or `flag_state` (grep -v '^#' | grep -c 'gated_by_value\|gate_enabled\|flag_state' == 0)
    - `mix compile --warnings-as-errors` exits 0
    - Behavior: `Crosswake.Manifest.Types.to_map(Crosswake.Manifest.Types.new_route_entry(id: "r", path: "/r", runtime: :live_view, gated_by: :my_flag, on_unavailable: :deny))` returns a map with `"gated_by" => "my_flag"` and `"on_unavailable" => "deny"`
    - Behavior: `to_map(new_route_entry(id: "r", path: "/r", runtime: :live_view))` returns a map NOT containing key `"gated_by"`
  </acceptance_criteria>
  <done>RouteEntry carries gated_by/on_unavailable (no flag-value field); new_route_entry passes them through; to_map serializes them with reversible fallback_phoenix string and omits both keys for non-gated routes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Pass gated_by + on_unavailable through Builder.route_entries/3</name>
  <files>lib/crosswake/manifest/builder.ex</files>
  <read_first>
    - lib/crosswake/manifest/builder.ex — the file being modified. Study `route_entries/3` (lines 115-141), specifically the `Types.new_route_entry(...)` call (lines 121-137) listing explicit keyword args read off the `%Route{}` struct.
    - lib/crosswake/policy/route.ex — confirm `Route` now has `gated_by` and `on_unavailable` fields (added in Plan 01).
    - lib/crosswake/manifest/types.ex — confirm `new_route_entry/1` now accepts `:gated_by` and `:on_unavailable` (added in Task 1).
    - .planning/phases/39-route-policy-gating-dsl-and-manifest-binding/39-RESEARCH.md — Pitfall 4 (Builder not updated -> RouteEntry.gated_by always nil).
  </read_first>
  <behavior>
    - For a compiled router whose route declares gated_by: :my_flag, manifest.routes[id].gated_by == :my_flag
    - For a compiled router whose route declares gated_by: :my_flag with no on_unavailable, manifest.routes[id].on_unavailable == :deny (default flowed from Route.new)
    - For a non-gated route, manifest.routes[id].gated_by == nil
  </behavior>
  <action>
    In `route_entries/3`, add two keyword args to the `Types.new_route_entry(...)` call (after `allowlisted_origins: [origin]`): `gated_by: route.gated_by` and `on_unavailable: route.on_unavailable`. Direct pass-through from the `%Route{}` struct — no transformation. This closes Pitfall 4: without it, `RouteEntry.gated_by` is silently dropped to nil even for gated routes.
  </action>
  <verify>
    <automated>cd /Users/jon/projects/crosswake && mix compile --warnings-as-errors 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - lib/crosswake/manifest/builder.ex `route_entries/3` `new_route_entry` call contains `gated_by: route.gated_by` and `on_unavailable: route.on_unavailable`
    - `mix compile --warnings-as-errors` exits 0
    - Behavior (verified by the Task 3 proof): a compiled gated route yields `manifest.routes[id].gated_by == :my_flag`, not nil
  </acceptance_criteria>
  <done>Builder passes both gating fields from Route into RouteEntry; gated routes carry their atom flag key into the compiled manifest.</done>
</task>

<task type="auto">
  <name>Task 3: Complete the Phase 39 proof with GATE-02 manifest round-trip, binding-vs-value split, and nil boundary cases</name>
  <files>test/crosswake/proof/phase39_route_policy_gating_test.exs</files>
  <read_first>
    - test/crosswake/proof/phase39_route_policy_gating_test.exs — the file being extended (created in Plan 01 with GATE-01 cases). Read it to see the existing module name, aliases, and test style so the new tests match.
    - test/crosswake/proof/phase38_companion_contract_test.exs — for the inline router fixture pattern (`defmodule GatedRouter do use Crosswake.Router ... crosswake_defaults runtime: :live_view, offline: :unavailable, security: :standard do live "/checkout", Crosswake.TestSupport.StudySessionLive, crosswake: [id: "checkout", runtime: :live_view, gated_by: :feature_payment_v2] end end`).
    - test/crosswake/proof/phase8_selective_native_lane_test.exs — for the `Manifest.compile/1` assertion idiom: `assert {:ok, %{manifest: manifest}} = Manifest.compile(SomeRouter)` then `manifest.routes["id"]`. NOTE: that file is tagged `:requires_example_host` because it depends on the example host — the Phase 39 fixtures are INLINE/hermetic, so do NOT copy the moduletag.
    - lib/crosswake/manifest/types.ex — `to_map/1` and `serialize_on_unavailable/1` (from Task 1) for the serialization assertions.
    - .planning/phases/39-route-policy-gating-dsl-and-manifest-binding/39-CONTEXT.md — D-11 (full coverage: SC#2/SC#3 round-trip + introspection + split + boundary).
  </read_first>
  <action>
    Append GATE-02 proof cases to `Crosswake.Proof.Phase39RoutePolicyGatingTest`. Add inline hermetic router fixtures (`GatedRouter` declaring `live "/checkout"` with `crosswake: [id: "checkout", runtime: :live_view, gated_by: :feature_payment_v2]`, and `NonGatedRouter` with a plain `home` route) following the Phase 38 inline-router pattern. Alias `Crosswake.Manifest` and `Crosswake.Manifest.Types`.

    Add tests:

    1. SC#2 introspection: `assert {:ok, %{manifest: manifest}} = Manifest.compile(GatedRouter)`; `route = manifest.routes["checkout"]`; `assert route.gated_by == :feature_payment_v2`; `assert route.on_unavailable == :deny`; `assert inspect(route.gated_by) == ":feature_payment_v2"`.

    2. SC#3 binding-vs-value split (CRITICAL, D-07): from the same compiled `route`, `refute Map.has_key?(Map.from_struct(route), :gated_by_value)`, and likewise for `:gate_enabled` and `:flag_state`. Assert the struct carries only the key + posture.

    3. SC#2/SC#3 round-trip: `route_map = Types.to_map(manifest.routes["checkout"])`; `assert route_map["gated_by"] == "feature_payment_v2"`; `assert route_map["on_unavailable"] == "deny"`.

    4. fallback_phoenix serialization: build (or compile a router with) a route declaring `on_unavailable: {:fallback_phoenix, :home}` and assert `Types.to_map(entry)["on_unavailable"] == "fallback_phoenix:home"` and that `String.split("fallback_phoenix:home", ":", parts: 2)` reverses it.

    5. Non-gated boundary: `assert {:ok, %{manifest: manifest}} = Manifest.compile(NonGatedRouter)`; `route_map = Types.to_map(manifest.routes["home"])`; `refute Map.has_key?(route_map, "gated_by")`; `refute Map.has_key?(route_map, "on_unavailable")`; and `assert manifest.routes["home"].gated_by == nil`.

    Keep the file untagged and hermetic (no `:requires_example_host`, no example-host references) — the inline fixtures are self-contained.
  </action>
  <verify>
    <automated>cd /Users/jon/projects/crosswake && mix test test/crosswake/proof/phase39_route_policy_gating_test.exs 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - test/crosswake/proof/phase39_route_policy_gating_test.exs contains `gated_by_value`, `gate_enabled`, and `flag_state` (inside `refute Map.has_key?` assertions for the SC#3 split)
    - File contains inline `GatedRouter` and `NonGatedRouter` fixtures and `Manifest.compile`
    - File still does NOT contain `@moduletag :requires_example_host` and no example-host reference (hermetic)
    - `mix test test/crosswake/proof/phase39_route_policy_gating_test.exs` exits 0 — all GATE-01 (Plan 01) and GATE-02 (this plan) cases pass
    - `mix test --exclude requires_example_host` exits 0 (no regressions in the full hermetic suite)
  </acceptance_criteria>
  <done>The proof asserts the compiled manifest carries the flag binding (key + posture) but no flag value, serializes both keys correctly, reverses the fallback_phoenix string, and omits both keys for non-gated routes — all hermetically.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `Policy.Route` → compiled manifest (`RouteEntry`) | The gating binding crosses from validated DSL into the persisted/serialized manifest; the manifest is consumed offline (diffs, inspection) and at runtime (Phase 40) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-39-04 | Tampering / Repudiation | `RouteEntry` carrying an evaluated flag value | mitigate | D-07 enforced structurally: `RouteEntry` has only `gated_by` (key) and `on_unavailable` (posture) — no `gated_by_value`/`gate_enabled`/`flag_state`. The SC#3 proof test asserts the absence of those keys, so an accidental future field addition fails CI. Offline manifest inspection cannot misreport a route as enabled/killed because no value is stored |
| T-39-05 | Information disclosure | `to_map/1` serialization of gating fields | mitigate | Only the flag key (atom→string) and declared posture are serialized; no secrets, tokens, or runtime state. Non-gated routes omit the keys entirely (no empty/null leakage that could be misread) |
| T-39-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this phase — RESEARCH confirms zero new dependencies; `mix deps.get` is not invoked |
</threat_model>

<verification>
- `mix compile --warnings-as-errors` exits 0 after both source tasks.
- `mix test test/crosswake/proof/phase39_route_policy_gating_test.exs` exits 0 (GATE-01 + GATE-02 cases green).
- `mix test --exclude requires_example_host` exits 0 (full hermetic suite — this is the merge-blocking lane that `phase34-proof.yml` runs; the new untagged file is picked up automatically).
</verification>

<success_criteria>
- GATE-02 SC#2 satisfied: gated route's binding is recorded in the compiled `RouteEntry`, readable by introspection and serialized by `to_map/1`.
- GATE-02 SC#3 satisfied: `RouteEntry` carries the flag *key* + posture only — NO flag value field; offline manifest inspection is accurate with no runtime flag evaluation (binding-vs-value split, D-07).
- Non-gated routes carry nil and omit both keys from `to_map/1`.
- `{:fallback_phoenix, route_id}` serializes reversibly.
</success_criteria>

<output>
Create `.planning/phases/39-route-policy-gating-dsl-and-manifest-binding/39-02-SUMMARY.md` when done.
</output>
