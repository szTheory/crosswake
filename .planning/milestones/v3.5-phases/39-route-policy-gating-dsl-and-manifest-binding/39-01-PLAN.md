---
phase: 39-route-policy-gating-dsl-and-manifest-binding
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/crosswake/policy/schema.ex
  - lib/crosswake/policy/route.ex
  - test/crosswake/proof/phase39_route_policy_gating_test.exs
autonomous: true
requirements: [GATE-01]
tags: [elixir, nimble-options, dsl, route-policy, gating]

must_haves:
  truths:
    - "A Phoenix team can write `gated_by: :my_flag` in a route's crosswake options and it compiles (GATE-01, SC#1)"
    - "Invalid `gated_by` values (true, false, nil, strings, integers, quoted atoms like :\"my-flag\") are rejected at compile time with a clear error message (GATE-01, SC#1)"
    - "`on_unavailable: :deny` or `on_unavailable: {:fallback_phoenix, :home}` is valid only when `gated_by` is also set; without it the route fails to compile (D-05c)"
    - "When `gated_by` is set and `on_unavailable` is omitted, the validated route defaults to `on_unavailable: :deny` (fail-closed, D-05d)"
    - "`Policy.Route.new!/1` returns a struct whose `gated_by` field holds the atom `:my_flag` (not the string \"my_flag\") (D-04)"
  artifacts:
    - path: "lib/crosswake/policy/schema.ex"
      provides: "gated_by + on_unavailable schema entries, validate_flag_key/1 and validate_on_unavailable/1 validators"
      contains: "def validate_flag_key"
    - path: "lib/crosswake/policy/route.ex"
      provides: "gated_by + on_unavailable struct fields, validate_gating_posture/1 cross-key validation"
      contains: "validate_gating_posture"
    - path: "test/crosswake/proof/phase39_route_policy_gating_test.exs"
      provides: "Hermetic GATE-01 proof: happy paths, error cases, cross-key validation"
      contains: "Phase39RoutePolicyGatingTest"
  key_links:
    - from: "lib/crosswake/policy/schema.ex @schema"
      to: "validate_flag_key/1"
      via: "{:custom, __MODULE__, :validate_flag_key, []}"
      pattern: "validate_flag_key"
    - from: "lib/crosswake/policy/route.ex new/1 with-chain"
      to: "validate_gating_posture/1"
      via: "with {:ok, validated} <- validate_gating_posture(validated)"
      pattern: "validate_gating_posture"
---

<objective>
Add the `gated_by` and `on_unavailable` keys to the route-policy NimbleOptions schema (`Policy.Schema`) with compile-time atom-identifier validation, and carry both fields into the `Policy.Route` struct with cross-key validation. This is the DSL-declaration layer of Phase 39: a Phoenix team can declare a named flag gate on a route, validated at compile time, before any manifest serialization or runtime evaluation exists.

This plan also creates the Wave 0 proof test file (`phase39_route_policy_gating_test.exs`) and populates it with the GATE-01 (DSL validation) test cases. Plan 02 extends the same file with GATE-02 (manifest binding) cases.

Purpose: Satisfies GATE-01 — `gated_by` is a valid, typed, compile-time-validated key in the route-policy DSL.
Output: Extended `Policy.Schema`, extended `Policy.Route`, new proof test file covering DSL validation.

Scope guard (do NOT pull forward): No runtime evaluation, no `route_gated?/2` consumption, no `RouteEntry`/manifest changes (Plan 02), no doctor gating category (Phase 41), no `{:fallback_phoenix, route_id}` cross-checking against declared routes (Phase 41). This plan validates the *shape* of the `route_id` atom only.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/39-route-policy-gating-dsl-and-manifest-binding/39-CONTEXT.md
@.planning/phases/39-route-policy-gating-dsl-and-manifest-binding/39-RESEARCH.md
@.planning/phases/39-route-policy-gating-dsl-and-manifest-binding/39-PATTERNS.md
@.planning/phases/39-route-policy-gating-dsl-and-manifest-binding/39-VALIDATION.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add gated_by + on_unavailable validators and schema entries to Policy.Schema</name>
  <files>lib/crosswake/policy/schema.ex</files>
  <read_first>
    - lib/crosswake/policy/schema.ex — the file being modified. Study `validate_identifier/1` (lines ~124-127), `validate_runtime/1` (lines ~129-135), `validate_commerce_declaration/1` (lines ~137-157) for the `{:ok, value} | {:error, String.t()}` return convention and catch-all error clause idiom. Study the `@schema NimbleOptions.new!([...])` block (lines 16-73) for the `{:custom, __MODULE__, :fn, []}` entry shape, and the `@type validated_options` block (lines 93-106).
    - .planning/phases/39-route-policy-gating-dsl-and-manifest-binding/39-CONTEXT.md — D-01 (validator regex + accepted/rejected values), D-02 (exact error message string), D-04 (atom preserved, NOT converted to string), D-05b/D-05e (on_unavailable shape).
  </read_first>
  <behavior>
    - validate_flag_key(:my_flag) returns {:ok, :my_flag} — accepts :feature_rollout_v2, :gating_enabled?
    - validate_flag_key(true), (false), (nil), ("string"), (123), (:"feature.flag"), (:"my-flag"), (:CamelCase), (:camelCase) each return {:error, "expected a plain atom identifier (e.g. :my_flag), got: ..."}
    - validate_on_unavailable(nil) returns {:ok, nil}
    - validate_on_unavailable(:deny) returns {:ok, :deny}
    - validate_on_unavailable({:fallback_phoenix, :home}) returns {:ok, {:fallback_phoenix, :home}}
    - validate_on_unavailable({:fallback_phoenix, "home"}) and ({:fallback_phoenix, :"bad-id"}) return {:error, ...}
    - validate_on_unavailable(:something_else) returns {:error, "expected on_unavailable to be :deny or {:fallback_phoenix, route_id}, got: ..."}
  </behavior>
  <action>
    Add two public custom validators and two schema entries to `Crosswake.Policy.Schema`.

    1. `validate_flag_key/1` (spec `@spec validate_flag_key(term()) :: {:ok, atom()} | {:error, String.t()}`): a guarded clause `when is_atom(value) and value not in [true, false, nil]` that converts to string with `Atom.to_string/1`, tests `Regex.match?(~r/^[a-z_][a-z0-9_]*[?!]?$/, str)`, returns `{:ok, value}` on match else `{:error, "expected a plain atom identifier (e.g. :my_flag), got: #{inspect(value)}"}`; plus a catch-all clause returning the same error message. CRITICAL (D-04, RESEARCH Pitfall 1): the success return is `{:ok, value}` — the ATOM, NOT `{:ok, Atom.to_string(value)}`. Do not reuse `validate_identifier/1` (it string-converts).

    2. `validate_on_unavailable/1` (spec `@spec validate_on_unavailable(term()) :: {:ok, :deny | {:fallback_phoenix, atom()} | nil} | {:error, String.t()}`): clauses for `nil` -> `{:ok, nil}`; `:deny` -> `{:ok, :deny}`; `{:fallback_phoenix, route_id}` -> delegate to `validate_flag_key(route_id)`, returning `{:ok, {:fallback_phoenix, valid_id}}` on success or `{:error, "on_unavailable fallback_phoenix route_id must be a plain atom identifier (e.g. :home), got: #{inspect(route_id)}"}` on failure; catch-all -> `{:error, "expected on_unavailable to be :deny or {:fallback_phoenix, route_id}, got: #{inspect(value)}"}`.

    3. Add two `@schema` entries after the `security:` entry (line ~72): `gated_by: [type: {:custom, __MODULE__, :validate_flag_key, []}, type_spec: quote(do: atom() | nil)]` and `on_unavailable: [type: {:custom, __MODULE__, :validate_on_unavailable, []}, type_spec: quote(do: :deny | {:fallback_phoenix, atom()} | nil)]`. Neither has `required:` and neither has `default:` — per RESEARCH Pitfall 2, do NOT add `default: :deny` here (it would leak onto non-gated routes); the `:deny` default is applied in Plan 01 Task 2's cross-key validator.

    4. Add `gated_by: atom() | nil` and `on_unavailable: :deny | {:fallback_phoenix, atom()} | nil` to the `@type validated_options` keyword list (lines 93-106).
  </action>
  <verify>
    <automated>cd /Users/jon/projects/crosswake && mix compile --warnings-as-errors 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - lib/crosswake/policy/schema.ex contains `def validate_flag_key`
    - lib/crosswake/policy/schema.ex contains `def validate_on_unavailable`
    - lib/crosswake/policy/schema.ex contains `gated_by:` and `on_unavailable:` schema entries (grep: `grep -c 'gated_by:\|on_unavailable:' lib/crosswake/policy/schema.ex` >= 2)
    - lib/crosswake/policy/schema.ex does NOT contain `default: :deny` (grep -v '^#' | grep -c 'default: :deny' == 0)
    - `mix compile --warnings-as-errors` exits 0
    - In IEx/test: `Crosswake.Policy.Schema.validate_flag_key(:my_flag) == {:ok, :my_flag}` and `Crosswake.Policy.Schema.validate_flag_key("my_flag")` returns `{:error, _}`
  </acceptance_criteria>
  <done>Both validators and schema entries compile cleanly with no warnings; validate_flag_key returns the atom (not a string) for valid input and {:error, msg} for all D-01 rejection cases.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add gated_by + on_unavailable fields and cross-key validation to Policy.Route</name>
  <files>lib/crosswake/policy/route.ex</files>
  <read_first>
    - lib/crosswake/policy/route.ex — the file being modified. Study the `defstruct` (lines 11-25) and `@type t` (lines 27-40); the `new/1` with-chain (lines 42-59) and `new!/1` bang-pipe (lines 61-72); the `validate_offline_contracts/1` cross-key pattern (lines 79-101) and its bang variant `validate_offline_contracts!/1` (lines 103-108); the `validation_error/3` helper (search for `defp validation_error`).
    - lib/crosswake/policy/schema.ex — for the `Schema.validated_options` shape now including gated_by/on_unavailable (modified in Task 1).
    - .planning/phases/39-route-policy-gating-dsl-and-manifest-binding/39-CONTEXT.md — D-05c (on_unavailable requires gated_by), D-05d (:deny default), D-09 (Route struct gets both fields).
  </read_first>
  <behavior>
    - Route.new!(id: "r", runtime: :live_view, gated_by: :my_flag).gated_by == :my_flag
    - Route.new!(id: "r", runtime: :live_view, gated_by: :my_flag).on_unavailable == :deny  (default applied)
    - Route.new!(id: "r", runtime: :live_view, gated_by: :my_flag, on_unavailable: {:fallback_phoenix, :home}).on_unavailable == {:fallback_phoenix, :home}
    - Route.new(id: "r", runtime: :live_view, on_unavailable: :deny) returns {:error, %NimbleOptions.ValidationError{key: :on_unavailable}} whose message contains "requires gated_by"
    - Route.new!(id: "r", runtime: :live_view).gated_by == nil  (non-gated route, no default leaked)
    - Route.new!(id: "r", runtime: :live_view).on_unavailable == nil
  </behavior>
  <action>
    Extend `Crosswake.Policy.Route` to carry the two gating fields and enforce the cross-key constraint.

    1. Add `:gated_by` and `:on_unavailable` to `defstruct` (lines 11-25) with no default value (they sit in the keyword-list/no-default section like `:security`, so both default to `nil` for non-gated routes — do NOT add them to `@enforce_keys`).

    2. Add `gated_by: atom() | nil` and `on_unavailable: :deny | {:fallback_phoenix, atom()} | nil` to `@type t` (lines 27-40).

    3. Add a private `validate_gating_posture/1` mirroring `validate_offline_contracts/1`: read `validated[:gated_by]` and `validated[:on_unavailable]`; `cond` with branches — (a) `on_unavailable != nil and is_nil(gated_by)` -> `{:error, validation_error(:on_unavailable, validated[:on_unavailable], "on_unavailable requires gated_by to be set")}`; (b) `gated_by != nil and is_nil(on_unavailable)` -> `{:ok, Keyword.put(validated, :on_unavailable, :deny)}`; (c) `true` -> `{:ok, validated}`.

    4. Add the bang variant `validate_gating_posture!/1` mirroring `validate_offline_contracts!/1` (`case ... do {:ok, v} -> v; {:error, e} -> raise e end`).

    5. Wire `validate_gating_posture/1` into the `new/1` with-chain (after `validate_offline_contracts/1`) and `validate_gating_posture!/1` into the `new!/1` pipe (after `validate_offline_contracts!/1`).
  </action>
  <verify>
    <automated>cd /Users/jon/projects/crosswake && mix compile --warnings-as-errors 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - lib/crosswake/policy/route.ex contains `validate_gating_posture` (both the defp and the bang variant)
    - lib/crosswake/policy/route.ex `defstruct` lists `:gated_by` and `:on_unavailable`
    - lib/crosswake/policy/route.ex `new/1` with-chain references `validate_gating_posture(validated)` and `new!/1` references `validate_gating_posture!`
    - `mix compile --warnings-as-errors` exits 0
    - Behavior: `Crosswake.Policy.Route.new!(id: "r", runtime: :live_view, gated_by: :my_flag).on_unavailable == :deny`
    - Behavior: `Crosswake.Policy.Route.new(id: "r", runtime: :live_view, on_unavailable: :deny)` returns `{:error, _}` with message containing "requires gated_by"
  </acceptance_criteria>
  <done>Route struct carries gated_by (atom) and on_unavailable; cross-key validation rejects on_unavailable-without-gated_by and applies the :deny default when gated_by is set; non-gated routes keep both fields nil.</done>
</task>

<task type="auto">
  <name>Task 3: Create hermetic GATE-01 proof test (Wave 0 scaffold + DSL validation cases)</name>
  <files>test/crosswake/proof/phase39_route_policy_gating_test.exs</files>
  <read_first>
    - test/crosswake/proof/phase38_companion_contract_test.exs — full file. Copy the module-docstring style, `use ExUnit.Case`, the hermeticity self-assertion test (the `File.read!(__ENV__.file)` / `refute String.contains?` / `refute Regex.match?(~r/code\.require_file/)` test), the inline router fixture pattern (`defmodule MinimalRouter do use Crosswake.Router ... crosswake_defaults ... live "/home", Crosswake.TestSupport.StudySessionLive, crosswake: [...] end end`), and the assert/assert_raise style.
    - lib/crosswake/policy/route.ex — for `Route.new/1` and `Route.new!/1` signatures used in error-case tests.
    - lib/crosswake/policy/schema.ex — for the validator error message strings asserted in tests.
    - .planning/phases/39-route-policy-gating-dsl-and-manifest-binding/39-CONTEXT.md — D-10 (untagged file, picked up by phase34-proof.yml), D-11 (full coverage list), D-12 (shift-left).
  </read_first>
  <action>
    Create `test/crosswake/proof/phase39_route_policy_gating_test.exs`, module `Crosswake.Proof.Phase39RoutePolicyGatingTest`, `use ExUnit.Case, async: true` (this plan's tests write no Application env — `async: true` is safe per PATTERNS note; Plan 02 may revisit if it adds env writes). Leave it UNtagged (no `@moduletag :requires_example_host`) so `phase34-proof.yml`'s `mix test --exclude requires_example_host` picks it up automatically (D-10) — do NOT add a new CI workflow file.

    Include in this plan (GATE-01 cases — Plan 02 appends GATE-02 manifest cases to this same file):

    1. Hermeticity self-assertion test (first test): read `__ENV__.file`, downcase, `refute String.contains?(source, "crosswake" <> "example.router")` and `refute Regex.match?(~r/code\.require_file\s*\(/, source)`, error messages naming "phase 39".

    2. SC#1 happy paths via `Crosswake.Policy.Route.new!/1`: `gated_by: :my_flag` yields `.gated_by == :my_flag` and `.on_unavailable == :deny` (default); `gated_by: :my_flag, on_unavailable: {:fallback_phoenix, :home}` yields `.on_unavailable == {:fallback_phoenix, :home}`; assert `inspect(route.gated_by) == ":my_flag"` (Pitfall 5 — unquoted atom).

    3. SC#1 error cases: `assert_raise NimbleOptions.ValidationError, ~r/plain atom identifier/, fn -> Route.new!(id: "bad", runtime: :live_view, gated_by: true) end` and likewise for `gated_by: "string"`, `gated_by: :"feature.flag"`. For `on_unavailable` without `gated_by`: `assert {:error, error} = Route.new(id: "bad", runtime: :live_view, on_unavailable: :deny)`; assert `error.key == :on_unavailable` and `String.contains?(error.message, "requires gated_by")`.

    4. Boundary: non-gated `Route.new!(id: "home", runtime: :live_view)` has `.gated_by == nil` and `.on_unavailable == nil`.

    Use the proof file as the GATE-01 truth surface. Plan 02 will add an inline `GatedRouter`/`NonGatedRouter` fixture and `Manifest.compile/1` round-trip assertions for GATE-02; this plan may use `Route.new/1` directly (no router fixture required for the DSL-layer cases) but if a router fixture is clearer, mirror the Phase 38 inline-router pattern.
  </action>
  <verify>
    <automated>cd /Users/jon/projects/crosswake && mix test test/crosswake/proof/phase39_route_policy_gating_test.exs 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - test/crosswake/proof/phase39_route_policy_gating_test.exs exists and defines `Crosswake.Proof.Phase39RoutePolicyGatingTest`
    - File does NOT contain `@moduletag :requires_example_host` (grep -v '^#' | grep -c 'requires_example_host' == 0)
    - File contains a hermeticity self-assertion test referencing `__ENV__.file`
    - `mix test test/crosswake/proof/phase39_route_policy_gating_test.exs` exits 0 with all tests passing
    - Test output shows assertions for `gated_by: :my_flag`, the `:deny` default, the `on_unavailable`-without-`gated_by` error, and the non-gated nil boundary
  </acceptance_criteria>
  <done>Proof file exists, is untagged/hermetic, and all GATE-01 DSL-validation test cases pass via `mix test test/crosswake/proof/phase39_route_policy_gating_test.exs`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| DSL author → compiled schema | Route policy options are author-supplied at compile time; `gated_by`/`on_unavailable` cross this boundary and must be shape-validated before reaching the manifest |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-39-01 | Tampering | `gated_by` value in `Policy.Schema` | mitigate | `validate_flag_key/1` enforces the snake_case-atom regex at compile time via NimbleOptions; quoted/kebab/dot atoms and non-atoms are rejected before reaching `RouteEntry` |
| T-39-02 | Spoofing/Information disclosure | `{:fallback_phoenix, route_id}` in `on_unavailable` | accept | Phase 39 validates only the identifier *shape*; cross-checking `route_id` against declared routes is explicitly Phase 41 doctor scope (D-05e). No fail-open is introduced — `:deny` is the default and the only fail-open path is the explicit tuple |
| T-39-03 | Elevation of privilege | `on_unavailable` declared without `gated_by` (a posture with no gate) | mitigate | `validate_gating_posture/1` rejects `on_unavailable` set without `gated_by` at compile time (D-05c), preventing a meaningless/ambiguous fail-open declaration |
| T-39-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this phase — RESEARCH confirms zero new dependencies; `mix deps.get` is not invoked |
</threat_model>

<verification>
- `mix compile --warnings-as-errors` exits 0 after both source tasks.
- `mix test test/crosswake/proof/phase39_route_policy_gating_test.exs` exits 0 (GATE-01 cases green).
- `mix test --exclude requires_example_host` exits 0 (full hermetic suite, no regressions).
</verification>

<success_criteria>
- GATE-01 SC#1 satisfied: `gated_by: :my_flag` is a valid compile-time-validated DSL key; invalid values rejected with clear errors.
- `on_unavailable` is valid only with `gated_by`; defaults to `:deny` when omitted; non-gated routes carry `nil` for both fields.
- `Policy.Route` struct carries `gated_by` as an atom (not a string).
- Hermetic proof file exists, is untagged, and passes.
</success_criteria>

<output>
Create `.planning/phases/39-route-policy-gating-dsl-and-manifest-binding/39-01-SUMMARY.md` when done.
</output>
