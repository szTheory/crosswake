---
phase: 47-companion-arc-guide-and-milestone-proof
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - test/crosswake/proof/phase47_companion_arc_test.exs
autonomous: true
requirements:
  - PROOF-02
must_haves:
  truths:
    - "The v3.5 hermetic merge path proves that enabled missing Rulestead and Rindle dependencies emit doctor `companion.dependency_missing` errors instead of crashing or silently passing."
    - "The milestone proof covers Sigra through auth-contract truth and `:step_up_required` posture, not by inventing optional-dependency claims."
    - "The new Phase 47 proof is picked up by the existing hermetic suite without advisory-lane env bleed."
  artifacts:
    - path: test/crosswake/proof/phase47_companion_arc_test.exs
      provides: "Aggregate milestone hermetic proof for the companion arc"
      min_lines: 120
      exports: []
  key_links:
    - from: test/crosswake/proof/phase47_companion_arc_test.exs
      to: lib/crosswake/doctor/doctor.ex
      via: "Doctor.run/1 dependency-missing and auth-contract assertions"
      pattern: "Doctor\\.run"
    - from: test/crosswake/proof/phase47_companion_arc_test.exs
      to: lib/crosswake/support_matrix/support_matrix.ex
      via: "auth contract truth assertions"
      pattern: "SupportMatrix\\.auth_contract_truth"
    - from: .github/workflows/phase43-proof.yml
      to: test/crosswake/proof/phase47_companion_arc_test.exs
      via: "existing hermetic test command"
      pattern: "mix test --exclude requires_example_host --exclude advisory_only"
---

<objective>
Close the milestone-proof half of Phase 47 by adding one untagged aggregate ExUnit proof that composes Rulestead and Rindle fail-closed optional-dependency behavior with Sigra auth-contract posture, while reusing the existing hermetic suite instead of creating another CI island.

Purpose: satisfy the hermetic proof half of `PROOF-02` and block fail-open or advisory-bleed regressions at the milestone level.
Output: `test/crosswake/proof/phase47_companion_arc_test.exs`, included automatically in the current merge-blocking hermetic command.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/47-companion-arc-guide-and-milestone-proof/47-CONTEXT.md
@.planning/phases/47-companion-arc-guide-and-milestone-proof/47-RESEARCH.md
@.planning/phases/47-companion-arc-guide-and-milestone-proof/47-PATTERNS.md
@.planning/phases/47-companion-arc-guide-and-milestone-proof/47-VALIDATION.md
@test/crosswake/proof/phase42_rulestead_companion_test.exs
@test/crosswake/proof/phase45_rindle_companion_test.exs
@test/crosswake/proof/phase46_sigra_auth_contract_test.exs
@lib/crosswake/doctor/doctor.ex
@lib/crosswake/support_matrix/support_matrix.ex
@lib/crosswake/shell/denial.ex
@.github/workflows/phase43-proof.yml
@.github/workflows/phase45-proof.yml
@mix.exs
@test/test_helper.exs

<interfaces>
From `test/crosswake/proof/phase45_rindle_companion_test.exs`:
```elixir
setup do
  Application.put_env(:crosswake, :companions, [Rindle])
  Application.put_env(:crosswake, :rindle, %{enabled: true})
  on_exit(fn ->
    Application.delete_env(:crosswake, :companions)
    Application.delete_env(:crosswake, :rindle)
  end)
end

report =
  Doctor.run(
    route_source: MediaRouter,
    install_manifest_path: install_manifest_path,
    cwd: target
  )
```

From `test/crosswake/proof/phase46_sigra_auth_contract_test.exs`:
```elixir
decision = RouteGate.evaluate(manifest, "secure", target, [])
auth_truth = SupportMatrix.auth_contract_truth()

assert row.route_predicates == [:auth_min_level, :requires_recent_auth]
assert row.denial_vocabulary == :step_up_required
assert row.fallback == :step_up_required
assert decision.denial.reason == :step_up_required
```

From `test/test_helper.exs` and `mix.exs`:
```elixir
unless System.get_env("MIX_INCLUDE_RULESTEAD") == "1" do
  ExUnit.configure(exclude: [advisory_only: true])
end

if System.get_env("MIX_INCLUDE_RULESTEAD") == "1", do: [{:rulestead, "~> 0.1.6"}], else: []
if System.get_env("MIX_INCLUDE_RINDLE") == "1", do: [{:rindle, "~> 0.1"}], else: []
```

From `.github/workflows/phase43-proof.yml` and `.github/workflows/phase45-proof.yml`:
```yaml
run: mix test --exclude requires_example_host --exclude advisory_only
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create the aggregate fail-closed companion dependency proof</name>
  <files>test/crosswake/proof/phase47_companion_arc_test.exs</files>
  <read_first>
test/crosswake/proof/phase42_rulestead_companion_test.exs
test/crosswake/proof/phase45_rindle_companion_test.exs
test/crosswake/proof/phase46_sigra_auth_contract_test.exs
lib/crosswake/doctor/doctor.ex
mix.exs
test/test_helper.exs
.github/workflows/phase43-proof.yml
.github/workflows/phase45-proof.yml
  </read_first>
  <acceptance_criteria>
- `test/crosswake/proof/phase47_companion_arc_test.exs` exists as an untagged ExUnit module per D-15 and is therefore included by the existing hermetic `mix test --exclude requires_example_host --exclude advisory_only` command.
- The proof configures Rulestead and Rindle together and asserts doctor `companion.dependency_missing` `:error` findings for both when enabled with optional deps absent, with no crash or silent pass per D-14, D-16, and D-20.
- The proof also asserts the disabled-path guard for optional dependencies so fail-closed behavior does not over-fire when the companion is off.
  </acceptance_criteria>
  <action>Create `test/crosswake/proof/phase47_companion_arc_test.exs` as one aggregate hermetic proof module per D-14, D-15, and D-16. Reuse the inline router/install-manifest/Application env patterns from the Phase 42 and Phase 45 proof files to register `Crosswake.Companions.Rulestead` and `Crosswake.Companions.Rindle` together, then assert that `Doctor.run/1` emits separate `companion.dependency_missing` `:error` findings for `companion.rulestead` and `companion.rindle` when both companions are enabled and their optional deps are absent. Add the matching disabled-path assertion so the proof still refutes false positives when companion config is off. Keep the file untagged, do not introduce `@moduletag :advisory_only`, and do not edit workflows, `mix.exs`, or `test/test_helper.exs`; the existing hermetic command already proves pickup and avoids D-20 env-bleed footguns.</action>
  <verify>
    <automated>mix test test/crosswake/proof/phase47_companion_arc_test.exs</automated>
  </verify>
  <done>The milestone proof exercises the shipped optional-dependency companions together and fails if either Rulestead or Rindle becomes fail-open, silently skipped, or incorrectly tagged out of the hermetic suite.</done>
</task>

<task type="auto">
  <name>Task 2: Fold Sigra auth posture and hermetic lane guards into the milestone proof</name>
  <files>test/crosswake/proof/phase47_companion_arc_test.exs</files>
  <read_first>
test/crosswake/proof/phase47_companion_arc_test.exs
test/crosswake/proof/phase46_sigra_auth_contract_test.exs
lib/crosswake/support_matrix/support_matrix.ex
lib/crosswake/shell/denial.ex
lib/crosswake/doctor/doctor.ex
.github/workflows/phase43-proof.yml
.github/workflows/phase45-proof.yml
test/test_helper.exs
  </read_first>
  <acceptance_criteria>
- The aggregate proof asserts Sigra through `Crosswake.SupportMatrix.auth_contract_truth/0`, route predicate anchors, and `:step_up_required` posture rather than invented missing-dependency checks per D-17.
- The file contains hermeticity guards that keep example-host loads, advisory-only tagging, and dependency-present env assumptions out of the merge-blocking milestone proof per D-18, D-19, and D-20.
- The targeted proof test passes without requiring workflow edits or advisory env vars; the full hermetic suite remains a plan-level/wave-level verification gate.
  </acceptance_criteria>
  <action>Extend `test/crosswake/proof/phase47_companion_arc_test.exs` with the Sigra half of the milestone claim per D-17: assert `Crosswake.SupportMatrix.auth_contract_truth/0` carries `[:auth_min_level, :requires_recent_auth]`, `:step_up_required`, and contract-only wording; compile a minimal auth-predicated router and assert `RouteGate.evaluate/4` still denies with `:step_up_required` under missing or insufficient auth context using the established Phase 46 minimal-details posture. Add self-hermeticity assertions that refute example-host coupling, `Code.require_file` drift, and advisory-only tagging or env dependence per D-18, D-19, and D-20, then rely on the existing full-suite command to prove the file is naturally consumed by the merge-blocking path. Do not create `phase47-proof.yml` and do not widen advisory-lane scope.</action>
  <verify>
    <automated>mix test test/crosswake/proof/phase47_companion_arc_test.exs</automated>
  </verify>
  <done>The Phase 47 proof covers Rulestead, Rindle, and Sigra at the milestone level and remains safely inside the current hermetic/advisory lane contract with no new CI surface.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| hermetic proof -> optional dependency seam | Missing optional libraries must never become silent pass-throughs for enabled companions. |
| merge-blocking lane -> advisory env vars | Hermetic proof must stay isolated from dependency-present `MIX_INCLUDE_RULESTEAD` / `MIX_INCLUDE_RINDLE` lanes. |
| auth proof -> denial details | Sigra assertions must not widen into sensitive auth material or invented security claims. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-47-03 | Elevation of Privilege | optional companion dependency checks | mitigate | Assert two separate `companion.dependency_missing` `:error` findings for enabled missing Rulestead and Rindle, plus disabled-path suppression, in one aggregate doctor proof. |
| T-47-04 | Information Disclosure | Sigra proof assertions | mitigate | Reuse Phase 46 contract-only posture and `:step_up_required` semantics; assert stable support truth and minimal denial posture rather than sensitive challenge/token details. |
| T-47-05 | Tampering | hermetic/advisory CI boundary | mitigate | Keep the new file untagged, avoid workflow/env edits, and verify with `mix test --exclude requires_example_host --exclude advisory_only` so advisory-only or env-bleed regressions are caught. |
| T-47-SC | Tampering | npm/pip/cargo installs | accept | No package-manager installs or new dependencies are permitted in this plan. |
</threat_model>

<verification>
- `mix test test/crosswake/proof/phase47_companion_arc_test.exs`
- `mix test --exclude requires_example_host --exclude advisory_only`
</verification>

<success_criteria>
- `test/crosswake/proof/phase47_companion_arc_test.exs` proves the v3.5 companion arc claim at milestone level.
- Enabled missing optional deps for Rulestead and Rindle fail closed through doctor `:error` findings in the aggregate proof.
- Sigra is proven via auth contract truth and `:step_up_required` route posture, not by invented optional-dependency behavior.
- Existing hermetic lanes pick up the new proof without workflow changes or advisory env bleed.
</success_criteria>

<output>
Create `.planning/phases/47-companion-arc-guide-and-milestone-proof/47-02-SUMMARY.md` when done
</output>
