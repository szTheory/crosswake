---
phase: 47-companion-arc-guide-and-milestone-proof
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - guides/companions.md
  - test/crosswake/guides/companions_test.exs
autonomous: true
requirements:
  - PROOF-02
must_haves:
  truths:
    - "Maintainers can read one canonical guide that explains the companion contract before the individual companion examples."
    - "The guide names the shipped Rulestead, Rindle, and Sigra surfaces plus the deferred non-goals without implying provider delivery, generic plugin-bus behavior, or hidden runtime authority."
    - "Docs-contract tests fail when guide vocabulary drifts from exported companion callbacks, denial reasons, support truth, or doctor finding codes."
  artifacts:
    - path: guides/companions.md
      provides: "Single canonical v3.5 companion guide"
      min_lines: 140
    - path: test/crosswake/guides/companions_test.exs
      provides: "Anchor assertions and semantic parity guards against live code"
      exports: []
  key_links:
    - from: test/crosswake/guides/companions_test.exs
      to: guides/companions.md
      via: "File.read! setup_all guide load"
      pattern: "File\\.read!\\(@guide_path\\)"
    - from: test/crosswake/guides/companions_test.exs
      to: lib/crosswake/support_matrix/support_matrix.ex
      via: "gating_truth/auth_contract_truth parity helpers"
      pattern: "gating_truth|auth_contract_truth"
    - from: test/crosswake/guides/companions_test.exs
      to: lib/crosswake/shell/denial.ex
      via: "denial vocabulary parity"
      pattern: "Crosswake\\.Shell\\.Denial\\.reasons"
---

<objective>
Close the guide-and-docs half of Phase 47 by turning `guides/companions.md` into the single canonical v3.5 companion operating guide and by upgrading `companions_test.exs` from anchor-only checks to semantic parity against live companion, support-matrix, denial, and doctor truth.

Purpose: satisfy the documentation half of `PROOF-02` without splitting the guide surface or widening scope past the locked non-goals.
Output: an expanded guide and a docs-contract test that fails on real contract drift.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/47-companion-arc-guide-and-milestone-proof/47-CONTEXT.md
@.planning/phases/47-companion-arc-guide-and-milestone-proof/47-RESEARCH.md
@.planning/phases/47-companion-arc-guide-and-milestone-proof/47-PATTERNS.md
@.planning/phases/47-companion-arc-guide-and-milestone-proof/47-VALIDATION.md
@guides/companions.md
@guides/commerce.md
@test/crosswake/guides/companions_test.exs
@test/crosswake/guides/commerce_test.exs
@lib/crosswake/companion.ex
@lib/crosswake/support_matrix/support_matrix.ex
@lib/crosswake/shell/denial.ex
@lib/crosswake/doctor/doctor.ex

<interfaces>
From `lib/crosswake/companion.ex`:
```elixir
@callback companion_id() :: atom()
@callback enabled?(config :: map()) :: boolean()
@callback route_gated?(route :: RouteEntry.t(), context :: Target.t()) ::
            {:deny, Finding.t()} | :pass
@callback kill_switch_active?(context :: Target.t()) :: boolean()
@callback validate_dependency() :: :ok | {:error, [module()]}
@callback report_state() :: State.t()
```

From `lib/crosswake/support_matrix/support_matrix.ex`:
```elixir
@auth_contract_truth [
  %{
    proof_class: :merge_blocking,
    route_predicates: [:auth_min_level, :requires_recent_auth],
    denial_vocabulary: :step_up_required,
    fallback: :step_up_required
  }
]

def gating_truth_label, do: "Runtime Gate State — not build-proof posture"
def gating_truth do
  Application.get_env(:crosswake, :companions, [])
  |> Enum.map(fn companion ->
    state = companion.report_state()
    %{companion_id: state.companion_id, gate_state: gate_state_display(state)}
  end)
end

def auth_contract_truth, do: @auth_contract_truth
```

From `lib/crosswake/shell/denial.ex`:
```elixir
@reasons [
  :compatibility_mismatch,
  :undeclared_capability,
  :unavailable_capability,
  :commerce_corridor,
  :origin_denied,
  :inactive_route,
  :external_entry_denied,
  :pack_incompatible,
  :gate_denied,
  :kill_switch_active,
  :step_up_required
]

def reasons, do: @reasons
```

From `lib/crosswake/doctor/doctor.ex`:
```elixir
check(:error, "companion.dependency_missing", ...)
check(:advisory, "auth.route_predicated", ...)
check(:advisory, "auth.step_up_required_contract", ...)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite the single canonical companion guide</name>
  <files>guides/companions.md</files>
  <read_first>
guides/companions.md
guides/commerce.md
.planning/phases/47-companion-arc-guide-and-milestone-proof/47-CONTEXT.md
.planning/phases/47-companion-arc-guide-and-milestone-proof/47-RESEARCH.md
lib/crosswake/companion.ex
lib/crosswake/support_matrix/support_matrix.ex
lib/crosswake/shell/denial.ex
lib/crosswake/doctor/doctor.ex
  </read_first>
  <acceptance_criteria>
- `guides/companions.md` remains the only companion guide file and is restructured by reader intent per D-01 and D-02.
- The guide leads with `Crosswake.Companion`, the six callbacks, `lib/crosswake/companions/&lt;name&gt;/`, host registration, `[:crosswake, :companion, :validate_dependency]`, and fail-closed semantics per D-03, D-21, and D-22.
- The guide includes the required Rulestead, Rindle, and Sigra anchors from D-04, D-05, D-06, and D-09, plus explicit deferred non-goals for chimeway delivery, full Sigra machinery, threadline, and separate-package extraction.
- The prose keeps Crosswake’s typed first-party seam posture and does not imply a plugin bus, optional-dependency fail-open behavior, provider delivery, or committed media on direct upload success per D-07, D-23, and D-24.
  </acceptance_criteria>
  <action>Rewrite `guides/companions.md` into one contract-first guide per D-01, D-02, and D-03: open with orientation and mental model, explain the `Crosswake.Companion` behaviour and six callbacks, document host registration plus `validate_dependency/0`/telemetry/fail-closed posture, then add reader-facing sections for Rulestead per D-04, Rindle per D-05, and Sigra per D-06. Carry the exact anchors from D-09 into the prose, cite `Crosswake.SupportMatrix.gating_truth/0` and `Crosswake.SupportMatrix.auth_contract_truth/0` as live truth surfaces, and add an explicit proof-posture and non-goals section covering chimeway seam-only, full Sigra machinery, threadline, and separate-package extraction. Do not create per-companion guide files, do not introduce marketing language, and do not describe direct upload success as committed media per D-07, D-12, D-23, D-24, D-25, and D-26.</action>
  <verify>
    <automated>mix test test/crosswake/guides/companions_test.exs</automated>
  </verify>
  <done>The guide is a single canonical Phase 47 surface, covers all required companion and non-goal vocabulary, and its wording is narrow enough that the strengthened docs test can lock it to live code.</done>
</task>

<task type="auto">
  <name>Task 2: Strengthen companions docs-contract parity against live code</name>
  <files>test/crosswake/guides/companions_test.exs</files>
  <read_first>
test/crosswake/guides/companions_test.exs
test/crosswake/guides/commerce_test.exs
guides/companions.md
lib/crosswake/companion.ex
lib/crosswake/support_matrix/support_matrix.ex
lib/crosswake/shell/denial.ex
lib/crosswake/doctor/doctor.ex
test/crosswake/proof/phase42_rulestead_companion_test.exs
test/crosswake/proof/phase46_sigra_auth_contract_test.exs
  </read_first>
  <acceptance_criteria>
- The test keeps readable anchor assertions for the D-09 terms and adds `Code.ensure_loaded!` / `function_exported?` guards for the D-10 surfaces.
- Semantic parity helpers assert documented companion ids, auth predicates, denial reasons, and doctor/auth codes against live code instead of snapshot text alone per D-08, D-11, D-12, and D-13.
- The test stays pure ExUnit with `setup_all`/`File.read!`, no new dependency, and no markdown snapshot workflow per D-12, D-13, D-25, and D-26.
  </acceptance_criteria>
  <action>Expand `test/crosswake/guides/companions_test.exs` per D-08 through D-13. Keep explicit `content =~` assertions for the required anchors in D-09, add `Code.ensure_loaded!` plus `function_exported?/3` checks for `Crosswake.Companion`, `Crosswake.Companion.State`, `Crosswake.Companions.Rulestead`, `Crosswake.Companions.Rindle`, `Crosswake.Companions.Sigra.Contracts`, `Crosswake.Companions.Rulestead.MockFlagSource.set_flag/2`, `Crosswake.SupportMatrix.gating_truth/0`, and `Crosswake.SupportMatrix.auth_contract_truth/0`, and add helper-driven set parity that compares documented companion ids, `auth_min_level` / `requires_recent_auth`, and denial reasons against `Crosswake.SupportMatrix` and `Crosswake.Shell.Denial.reasons/0`. For doctor finding codes, derive live evidence from real `Doctor.run/1` output using a minimal inline fixture or existing proof-test setup pattern rather than a copied list, so `companion.dependency_missing`, `auth.route_predicated`, and `auth.step_up_required_contract` stay parity-locked to runtime behavior. Do not add snapshot fixtures, markdown parsing dependencies, or broad text snapshots per D-11, D-12, D-13, D-25, and D-26.</action>
  <verify>
    <automated>mix test test/crosswake/guides/companions_test.exs</automated>
  </verify>
  <done>The docs-contract test fails when guide vocabulary drifts from exported companion callbacks, support-truth accessors, denial vocabulary, or emitted doctor codes, while still producing readable failures.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| guide prose -> exported contract truth | Public docs can drift from the actual companion/support/doctor contract and mislead adopters. |
| docs tests -> doctor/support helpers | Tests must pull live truth without leaking sensitive auth material into assertions or guide prose. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-47-01 | Tampering | `guides/companions.md` | mitigate | Lock D-09 anchors plus set parity against `Crosswake.Companion`, `SupportMatrix`, `Shell.Denial`, and live `Doctor.run/1` output so docs drift breaks tests. |
| T-47-02 | Information Disclosure | guide + docs test auth sections | mitigate | Keep Sigra wording contract-only, assert only `:step_up_required` posture and stable codes, and avoid challenge/token examples beyond the sanitized contract surface per D-06 and D-24. |
| T-47-SC | Tampering | npm/pip/cargo installs | accept | No package-manager installs or new dependencies are permitted in this plan. |
</threat_model>

<verification>
- `mix test test/crosswake/guides/companions_test.exs`
- `mix test --exclude requires_example_host --exclude advisory_only`
</verification>

<success_criteria>
- `guides/companions.md` is the single canonical companion guide and covers contract, Rulestead, Rindle, Sigra, proof posture, and deferred non-goals with no plugin-bus drift.
- `test/crosswake/guides/companions_test.exs` enforces both anchor presence and semantic parity against live code surfaces.
- The guide can change only in ways that remain consistent with exported contract/support/doctor truth.
</success_criteria>

<output>
Create `.planning/phases/47-companion-arc-guide-and-milestone-proof/47-01-SUMMARY.md` when done
</output>
