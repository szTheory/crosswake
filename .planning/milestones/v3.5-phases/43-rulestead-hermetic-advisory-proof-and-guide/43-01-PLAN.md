---
phase: 43-rulestead-hermetic-advisory-proof-and-guide
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - mix.exs
  - test/crosswake/proof/phase43_rulestead_advisory_test.exs
  - .github/workflows/phase43-proof.yml
autonomous: true
requirements: [PROOF-01]

must_haves:
  truths:
    - "Running `mix test --exclude requires_example_host` with no env var (hermetic) keeps the Phase 42 rulestead proof green — rulestead absent, validate_dependency/0 returns {:error, [:\"Elixir.Rulestead\"]}"
    - "Running the advisory test with MIX_INCLUDE_RULESTEAD=1 (rulestead present) asserts validate_dependency/0 returns :ok"
    - "phase43-proof.yml defines a merge-blocking hermetic job (PR/push/dispatch) and an advisory job (schedule/dispatch, continue-on-error: true)"
    - "The advisory-to-merge-blocking promotion path is documented in the workflow header with the 4 named conditions from D-03"
  artifacts:
    - path: "mix.exs"
      provides: "Conditional rulestead dep gated by MIX_INCLUDE_RULESTEAD env var in deps/0"
      contains: "MIX_INCLUDE_RULESTEAD"
    - path: "test/crosswake/proof/phase43_rulestead_advisory_test.exs"
      provides: "Advisory-only proof asserting validate_dependency/0 == :ok"
      contains: "Rulestead.validate_dependency() == :ok"
    - path: ".github/workflows/phase43-proof.yml"
      provides: "Two-job hermetic + advisory CI workflow"
      contains: "merge-blocking-rulestead-proof"
  key_links:
    - from: ".github/workflows/phase43-proof.yml advisory job"
      to: "mix.exs deps/0 conditional"
      via: "MIX_INCLUDE_RULESTEAD=1 env on deps.get/compile/test steps"
      pattern: "MIX_INCLUDE_RULESTEAD"
    - from: ".github/workflows/phase43-proof.yml advisory job"
      to: "test/crosswake/proof/phase43_rulestead_advisory_test.exs"
      via: "mix test of the advisory test file only"
      pattern: "phase43_rulestead_advisory_test.exs"
---

<objective>
Ship the CI proof posture for the rulestead companion: a hermetic merge-blocking lane that compiles and passes with `rulestead` absent (proving fail-closed), and an advisory lane that compiles and passes with `rulestead` present (proving `validate_dependency/0 == :ok`).

Purpose: PROOF-01 requires every shipped companion to carry a hermetic merge-blocking proof lane that passes without its optional dependency, plus an advisory lane exercising it with the dependency present. This is the established hermetic+advisory template applied to rulestead.

Output: `mix.exs` conditional dep, `test/crosswake/proof/phase43_rulestead_advisory_test.exs` (the inverted-assertion advisory test), and `.github/workflows/phase43-proof.yml` (two-job workflow).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/43-rulestead-hermetic-advisory-proof-and-guide/43-CONTEXT.md
@.planning/phases/43-rulestead-hermetic-advisory-proof-and-guide/43-RESEARCH.md

<interfaces>
<!-- Key contracts the executor needs. Extracted from codebase — no exploration required. -->

From lib/crosswake/companions/rulestead.ex (module: Crosswake.Companions.Rulestead):
  def validate_dependency()
    # Returns :ok when Code.ensure_loaded?(Rulestead) is true (dep present, advisory lane)
    # Returns {:error, [:"Elixir.Rulestead"]} when absent (hermetic lane)
    # `Rulestead` here is the Hex package's root module, atom :"Elixir.Rulestead"

From mix.exs — current deps/0 returns a flat list:
  [
    {:jason, "~> 1.4"},
    {:nimble_options, "~> 1.1"},
    {:phoenix, "~> 1.8"},
    {:phoenix_live_view, "~> 1.1"},
    {:telemetry, "~> 1.0"},
    {:ex_doc, "~> 0.38", only: :dev, runtime: false}
  ]

From test/crosswake/proof/phase42_rulestead_companion_test.exs — the HERMETIC test (do NOT modify):
  test "validate_dependency/0 returns {:error, [Rulestead]} — library absent in Phase 42" do
    assert Crosswake.Companions.Rulestead.validate_dependency() == {:error, [:"Elixir.Rulestead"]}
  end
  # This file is untagged → picked up by `mix test --exclude requires_example_host`.
  # It is CORRECT in the hermetic lane and MUST stay unchanged.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add MIX_INCLUDE_RULESTEAD conditional rulestead dep to mix.exs</name>
  <files>mix.exs</files>
  <read_first>
    - mix.exs (current deps/0 is a flat literal list — must be restructured to base ++ conditional)
    - .planning/phases/43-rulestead-hermetic-advisory-proof-and-guide/43-RESEARCH.md (Pattern 1: env-var conditional dep isolation; Pitfall 3: do NOT use `optional: true`)
  </read_first>
  <action>
    Restructure `deps/0` in mix.exs so the existing six deps become a `base` list, then append a conditional `rulestead` list. The conditional is `if System.get_env("MIX_INCLUDE_RULESTEAD") == "1", do: [{:rulestead, "~> 0.1.6"}], else: []`, and `deps/0` returns `base ++ rulestead`. Keep all six existing deps unchanged verbatim: `{:jason, "~> 1.4"}`, `{:nimble_options, "~> 1.1"}`, `{:phoenix, "~> 1.8"}`, `{:phoenix_live_view, "~> 1.1"}`, `{:telemetry, "~> 1.0"}`, `{:ex_doc, "~> 0.38", only: :dev, runtime: false}`.

    Do NOT use the Hex `optional: true` flag — that only affects downstream transitive resolution and still compiles rulestead into crosswake's own build, breaking the hermetic premise (research Pitfall 3). Do NOT set `MIX_INCLUDE_RULESTEAD` anywhere except as documented; the default (env var unset) path MUST exclude rulestead entirely.

    Do NOT commit a `mix.lock` entry for rulestead — the hermetic lock is the committed state (research Pitfall 2). If running deps.get locally introduces a rulestead lock line, remove it before finishing.

    Version pin is `~> 0.1.6` (rulestead is szTheory's own first-party library, verified on hex.pm; legitimacy approved in 43-RESEARCH.md Package Legitimacy Audit — no blocking checkpoint needed).
  </action>
  <verify>
    <automated>MIX_INCLUDE_RULESTEAD= mix compile --warnings-as-errors 2>&1 | tail -3 && grep -q "MIX_INCLUDE_RULESTEAD" mix.exs && echo MIXEXS_OK</automated>
  </verify>
  <acceptance_criteria>
    - mix.exs deps/0 contains the literal string `MIX_INCLUDE_RULESTEAD`
    - mix.exs deps/0 contains `{:rulestead, "~> 0.1.6"}` inside the conditional branch
    - mix.exs deps/0 still contains all six original deps unchanged
    - mix.exs does NOT contain `optional: true` on the rulestead entry
    - `mix compile --warnings-as-errors` with no env var exits 0 (hermetic build still compiles)
    - `mix.lock` contains no `rulestead` entry (`grep -c rulestead mix.lock` returns 0)
  </acceptance_criteria>
  <done>deps/0 returns base deps plus a MIX_INCLUDE_RULESTEAD-gated rulestead entry; hermetic compile passes; mix.lock has no rulestead line.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create the advisory proof test (inverted validate_dependency assertion)</name>
  <files>test/crosswake/proof/phase43_rulestead_advisory_test.exs</files>
  <read_first>
    - test/crosswake/proof/phase42_rulestead_companion_test.exs (the hermetic test; copy its setup/teardown style — Application.put_env for :companions and :rulestead, on_exit cleanup, async: false)
    - lib/crosswake/companions/rulestead.ex (validate_dependency/0 returns :ok when Rulestead is loaded)
    - .planning/phases/43-rulestead-hermetic-advisory-proof-and-guide/43-RESEARCH.md (Pattern 2: advisory test file; Pitfall 1: why advisory must NOT run the phase42 suite)
  </read_first>
  <behavior>
    - Test 1: With rulestead present, `Crosswake.Companions.Rulestead.validate_dependency()` returns `:ok` (NOT `{:error, [:"Elixir.Rulestead"]}`).
    - Setup: `Application.put_env(:crosswake, :companions, [Crosswake.Companions.Rulestead])` and `Application.put_env(:crosswake, :rulestead, %{enabled: true})`, with on_exit deleting both keys (mirror phase42 setup).
  </behavior>
  <action>
    Create `test/crosswake/proof/phase43_rulestead_advisory_test.exs` defining module `Crosswake.Proof.Phase43RulesteadAdvisoryTest` with `use ExUnit.Case, async: false`. Alias `Crosswake.Companions.Rulestead`. Add a `@moduledoc` stating this test runs ONLY in the advisory CI lane (where `MIX_INCLUDE_RULESTEAD=1` was set during `mix deps.get`) and must never run in the hermetic lane — the assertion inverts the Phase 42 hermetic assertion.

    Setup block mirrors phase42: put `:companions` = `[Rulestead]` and `:rulestead` = `%{enabled: true}` into Application env, with `on_exit` deleting both. Do NOT start MockFlagSource (this test only checks dependency presence, not gate evaluation).

    The single test asserts `Rulestead.validate_dependency() == :ok`. Reference the module via the full name `Crosswake.Companions.Rulestead` (the alias `Rulestead` in this file would shadow the Hex package root module — call `Crosswake.Companions.Rulestead.validate_dependency()` explicitly to avoid ambiguity, matching the phase42 test's explicit-call convention).

    Do NOT tag this file with any `@moduletag` that would cause `mix test --exclude requires_example_host` to pick it up in the hermetic lane — but ALSO note this file only passes when rulestead is present. The hermetic lane (Task 3) does NOT run this file by path; the advisory lane runs it by explicit path. Because it has no example-host dependency it is technically untagged; the hermetic lane's `--exclude requires_example_host` WOULD pick it up and FAIL (rulestead absent → :error, not :ok). To prevent that, tag the module `@moduletag :advisory_only` and document in the moduledoc that the hermetic lane must exclude it. (See acceptance criteria for the exclusion contract Task 3 enforces.)
  </action>
  <verify>
    <automated>MIX_INCLUDE_RULESTEAD=1 mix test test/crosswake/proof/phase43_rulestead_advisory_test.exs 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - File test/crosswake/proof/phase43_rulestead_advisory_test.exs exists
    - Contains `defmodule Crosswake.Proof.Phase43RulesteadAdvisoryTest`
    - Contains `use ExUnit.Case, async: false`
    - Contains `@moduletag :advisory_only`
    - Contains the assertion `Crosswake.Companions.Rulestead.validate_dependency() == :ok`
    - `MIX_INCLUDE_RULESTEAD=1 mix test test/crosswake/proof/phase43_rulestead_advisory_test.exs` exits 0 (rulestead present → :ok)
    - Running the hermetic suite `mix test --exclude requires_example_host --exclude advisory_only` exits 0 (the advisory file is excluded; phase42 hermetic assertion still passes)
  </acceptance_criteria>
  <done>Advisory test file exists, tagged `:advisory_only`, asserts `validate_dependency() == :ok`, passes with rulestead present, and is excluded from the hermetic suite by tag.</done>
</task>

<task type="auto">
  <name>Task 3: Create phase43-proof.yml two-job CI workflow (hermetic + advisory)</name>
  <files>.github/workflows/phase43-proof.yml</files>
  <read_first>
    - .github/workflows/phase34-proof.yml (PRIMARY template: header comment structure, two-job split, `if:` guards, schedule cron, `continue-on-error: true` advisory job, advisory status-summary step)
    - .github/workflows/phase41-proof.yml (simpler hermetic-only reference for the gating proof header style)
    - .planning/phases/43-rulestead-hermetic-advisory-proof-and-guide/43-RESEARCH.md (Pattern 3: two-job workflow; Code Examples: hermetic + advisory job YAML)
    - .planning/phases/43-rulestead-hermetic-advisory-proof-and-guide/43-CONTEXT.md (D-03: 4-condition promotion path; specifics: name the real Rulestead.Snapshot adapter as the promotion target)
  </read_first>
  <action>
    Create `.github/workflows/phase43-proof.yml` named `Phase 43 Proof`, following `phase34-proof.yml` structure closely.

    Header comment block must explain the hermetic+advisory split for rulestead AND document the advisory-to-merge-blocking promotion path with these 4 named conditions from D-03: (1) the real `Rulestead.Snapshot` adapter shipped and in-tree (name it explicitly — the production swap target that replaces MockFlagSource, reading actual flag state from the rulestead library), (2) the advisory lane exercising actual flag reads (not just dependency presence), (3) sustained stability evidence from scheduled advisory runs, (4) an explicit requirement/roadmap scope change in .planning/REQUIREMENTS.md and .planning/ROADMAP.md.

    Triggers: `pull_request`, `push` (branches: main), `workflow_dispatch`, and `schedule` cron `"0 6 * * 1"` (Monday 06:00 UTC, same as phase34).

    Job 1 `merge-blocking-rulestead-proof`:
    - `name: merge-blocking rulestead proof (hermetic)`
    - `runs-on: macos-15`
    - `timeout-minutes: 20`
    - `if: ${{ github.event_name == 'pull_request' || github.event_name == 'push' || github.event_name == 'workflow_dispatch' }}`
    - Steps: Checkout (`actions/checkout@v6`), Setup BEAM (`erlef/setup-beam@v1`, elixir `1.19.5`, otp `27.3`), `mix deps.get`, `mix compile --warnings-as-errors`, then `mix test --exclude requires_example_host --exclude advisory_only`. NO `MIX_INCLUDE_RULESTEAD` env var anywhere in this job (research Pitfall 4 — env bleed must not occur). The `--exclude advisory_only` flag keeps the advisory-only test (which requires rulestead) out of the hermetic run; phase42_rulestead_companion_test.exs is untagged and still runs, proving fail-closed.

    Job 2 `advisory-rulestead-proof`:
    - `name: advisory rulestead proof (dep present, validate_dependency :ok)`
    - `runs-on: ubuntu-latest`
    - `timeout-minutes: 30`
    - `if: ${{ github.event_name == 'schedule' || github.event_name == 'workflow_dispatch' }}`
    - `continue-on-error: true`
    - Steps: Checkout, Setup BEAM (same versions), then deps.get / compile / test steps each carrying step-level `env: MIX_INCLUDE_RULESTEAD: "1"` (step-level only, NEVER job-level or workflow-level — research Pitfall 4): `mix deps.get`, `mix compile --warnings-as-errors`, `mix test test/crosswake/proof/phase43_rulestead_advisory_test.exs`. Final step: advisory lane status summary echoing `::notice` lines stating the lane is advisory only, cannot gate merge, and promotion requires explicit requirement/roadmap scope change plus sustained stability evidence (mirror phase34's summary step).
  </action>
  <verify>
    <automated>test -f .github/workflows/phase43-proof.yml && python3 -c "import yaml,sys; d=yaml.safe_load(open('.github/workflows/phase43-proof.yml')); j=d['jobs']; assert 'merge-blocking-rulestead-proof' in j and 'advisory-rulestead-proof' in j; assert j['advisory-rulestead-proof'].get('continue-on-error')==True; print('YAML_OK')"</automated>
  </verify>
  <acceptance_criteria>
    - File .github/workflows/phase43-proof.yml exists and is valid YAML
    - Defines exactly two jobs: `merge-blocking-rulestead-proof` and `advisory-rulestead-proof`
    - `merge-blocking-rulestead-proof` runs-on macos-15, has `if:` guarding to pull_request/push/workflow_dispatch, runs `mix test --exclude requires_example_host --exclude advisory_only`, and sets NO MIX_INCLUDE_RULESTEAD env
    - `advisory-rulestead-proof` has `continue-on-error: true`, `if:` guarding to schedule/workflow_dispatch, and sets `MIX_INCLUDE_RULESTEAD: "1"` only at step level
    - Workflow triggers include `schedule` with cron `"0 6 * * 1"`
    - Header comment names `Rulestead.Snapshot` as the promotion target and lists the 4 promotion conditions
    - `grep -c "MIX_INCLUDE_RULESTEAD" .github/workflows/phase43-proof.yml` returns ≥ 3 (one per advisory step), and the merge-blocking job block contains zero occurrences
  </acceptance_criteria>
  <done>Two-job workflow exists; hermetic job is merge-blocking and rulestead-free; advisory job is continue-on-error with step-level MIX_INCLUDE_RULESTEAD=1; promotion path documented with Rulestead.Snapshot named.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI workflow → Hex registry | `mix deps.get` fetches packages; advisory lane fetches the optional `rulestead` dep |
| Optional dep inclusion | `MIX_INCLUDE_RULESTEAD` env var controls whether rulestead enters the build/dep tree |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-43-01 | Tampering | Hermetic lane silently including rulestead (env bleed) | mitigate | MIX_INCLUDE_RULESTEAD set ONLY at step level in advisory job; hermetic job sets it nowhere; hermetic test `validate_dependency == {:error, ...}` fails loudly if rulestead leaks in (research Pitfall 4) |
| T-43-02 | Tampering | rulestead supply-chain (optional dep) | accept | rulestead is szTheory's own first-party library (same author as crosswake); verified on hex.pm; never executes in production paths (advisory CI only); 43-RESEARCH Package Legitimacy Audit approved |
| T-43-03 | Information disclosure | Committed mix.lock leaking a rulestead pin into hermetic state | mitigate | Task 1 acceptance criterion asserts `grep -c rulestead mix.lock == 0`; hermetic lock is the committed state (research Pitfall 2) |
| T-43-SC | Tampering | npm/pip/cargo installs | n/a | No npm/pip/cargo installs in this phase; the only package addition is the Hex `rulestead` dep, covered by 43-RESEARCH Package Legitimacy Audit (slopcheck does not support Hex; manual szTheory first-party verification performed) |
</threat_model>

<verification>
- Hermetic lane green: `mix test --exclude requires_example_host --exclude advisory_only` exits 0 with rulestead absent (phase42 fail-closed assertions pass).
- Advisory assertion green: `MIX_INCLUDE_RULESTEAD=1 mix test test/crosswake/proof/phase43_rulestead_advisory_test.exs` exits 0 with rulestead present.
- mix.lock hermetic: `grep -c rulestead mix.lock` returns 0.
- Workflow well-formed: two jobs, advisory continue-on-error, schedule cron present, promotion path documented.
</verification>

<success_criteria>
- PROOF-01 (hermetic): the Phase 42 rulestead proof suite passes with rulestead absent from the dep tree; the hermetic job is merge-blocking (runs on PR/push).
- PROOF-01 (advisory): the advisory job runs the same proof logic with rulestead present and asserts `validate_dependency/0 == :ok`; it is `continue-on-error: true` with a documented 4-condition promotion path naming the real `Rulestead.Snapshot` adapter.
</success_criteria>

<output>
Create `.planning/phases/43-rulestead-hermetic-advisory-proof-and-guide/43-01-SUMMARY.md` when done
</output>
