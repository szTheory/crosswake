---
phase: 45-rindle-in-tree-companion-mock-example-and-proof
plan: 03
type: execute
wave: 3
depends_on: [45-01, 45-02]
files_modified:
  - test/crosswake/proof/phase45_rindle_companion_test.exs
  - test/crosswake/proof/phase45_rindle_mock_media_test.exs
  - test/crosswake/proof/phase45_rindle_advisory_test.exs
  - .github/workflows/phase45-proof.yml
autonomous: true
requirements: [MEDIA-03, PROOF-01]

must_haves:
  truths:
    - "D-19: the merge-blocking Phase 45 proof runs with no optional Rindle dependency present"
    - "D-20: .github/workflows/phase45-proof.yml contains hermetic and advisory jobs with MIX_INCLUDE_RINDLE only in advisory steps"
    - "D-21: advisory assertions live in a separate @moduletag :advisory_only test file"
    - "D-22: hermetic proof covers companion fail-closed behavior, mock media idempotency, replay, and non-authoritative queued/uploaded/scanning states"
    - "D-23: advisory proof uses the real optional dependency, not a fake Rindle fixture module"
    - "The workflow documents a promotion path before advisory Rindle checks can become merge-blocking"
  artifacts:
    - path: ".github/workflows/phase45-proof.yml"
      provides: "Phase 45 hermetic plus advisory proof workflow"
      contains: "merge-blocking-rindle-proof"
    - path: "test/crosswake/proof/phase45_rindle_advisory_test.exs"
      provides: "Advisory dependency-present Rindle assertion"
      contains: "@moduletag :advisory_only"
---

<objective>
Close Phase 45 proof posture with a merge-blocking hermetic lane and an
advisory dependency-present lane, proving Rindle generalizes the companion seam
beyond feature flags.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/45-rindle-in-tree-companion-mock-example-and-proof/45-CONTEXT.md
@.planning/phases/45-rindle-in-tree-companion-mock-example-and-proof/45-RESEARCH.md
@.planning/phases/45-rindle-in-tree-companion-mock-example-and-proof/45-PATTERNS.md
@.github/workflows/phase43-proof.yml
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Harden hermetic proof coverage for Phase 45 invariants</name>
  <files>test/crosswake/proof/phase45_rindle_companion_test.exs, test/crosswake/proof/phase45_rindle_mock_media_test.exs</files>
  <read_first>
    - test/crosswake/proof/phase45_rindle_companion_test.exs
    - test/crosswake/proof/phase45_rindle_mock_media_test.exs
    - .planning/phases/45-rindle-in-tree-companion-mock-example-and-proof/45-CONTEXT.md
    - .planning/phases/45-rindle-in-tree-companion-mock-example-and-proof/45-VALIDATION.md
  </read_first>
  <action>
    Audit and extend the Phase 45 hermetic proof tests so they cover every D-22
    assertion from `45-CONTEXT.md`: dependency missing, doctor error, valid grants
    and evidence, mandatory idempotency, replay, evidence cannot create available
    media, and queued media is not committed.

    Add source-fence assertions that the mock media files do not contain storage
    provider SDK vocabulary such as `s3`, `gcs`, `azure`, `tus`, or provider billing
    terms unless a term is part of an explicit negative assertion.
  </action>
  <verify>
    <automated>mix test test/crosswake/proof/phase45_rindle_companion_test.exs test/crosswake/proof/phase45_rindle_mock_media_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - Hermetic proof asserts enabled-missing Rindle doctor error
    - Hermetic proof asserts valid grant/evidence/media object creation through Phase 44 contracts
    - Hermetic proof asserts missing idempotency is rejected
    - Hermetic proof asserts replay with changed correlation id is `replay?: true`
    - Hermetic proof asserts evidence-only flow cannot produce `:available`
    - Hermetic proof asserts queued media is not committed
    - Focused proof command exits 0
  </acceptance_criteria>
  <done>Phase 45 hermetic proof covers the companion and media lane invariants before CI wiring.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add advisory-only Rindle dependency-present proof</name>
  <files>test/crosswake/proof/phase45_rindle_advisory_test.exs</files>
  <read_first>
    - test/crosswake/proof/phase43_rulestead_advisory_test.exs
    - lib/crosswake/companions/rindle.ex
    - mix.exs
  </read_first>
  <action>
    Create `test/crosswake/proof/phase45_rindle_advisory_test.exs` with module
    `Crosswake.Proof.Phase45RindleAdvisoryTest`, `use ExUnit.Case, async: false`,
    and `@moduletag :advisory_only`.

    The test should set `Application.put_env(:crosswake, :companions, [Crosswake.Companions.Rindle])`
    and `Application.put_env(:crosswake, :rindle, %{enabled: true})`, then assert
    `Crosswake.Companions.Rindle.validate_dependency() == :ok`. The file must be
    excluded from hermetic runs and run only in CI with `MIX_INCLUDE_RINDLE=1`.
  </action>
  <verify>
    <automated>MIX_INCLUDE_RINDLE=1 mix test test/crosswake/proof/phase45_rindle_advisory_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - File exists with `@moduletag :advisory_only`
    - File asserts `Crosswake.Companions.Rindle.validate_dependency() == :ok`
    - File does not start example-host processes
    - Advisory command exits 0 when `MIX_INCLUDE_RINDLE=1`
    - Hermetic suite excludes the file via `--exclude advisory_only`
  </acceptance_criteria>
  <done>Dependency-present Rindle behavior is isolated to an advisory-only test file.</done>
</task>

<task type="auto">
  <name>Task 3: Add phase45-proof.yml hermetic and advisory CI workflow</name>
  <files>.github/workflows/phase45-proof.yml</files>
  <read_first>
    - .github/workflows/phase43-proof.yml
    - .github/workflows/phase34-proof.yml
    - .planning/phases/45-rindle-in-tree-companion-mock-example-and-proof/45-CONTEXT.md
  </read_first>
  <action>
    Create `.github/workflows/phase45-proof.yml` named `Phase 45 Proof`.

    Add triggers `pull_request`, `push` to `main`, `workflow_dispatch`, and a
    weekly `schedule` cron `"0 6 * * 1"`.

    Add job `merge-blocking-rindle-proof`:
    - `name: merge-blocking rindle proof (hermetic)`
    - `runs-on: macos-15`
    - `timeout-minutes: 20`
    - `if:` limited to pull_request, push, and workflow_dispatch
    - no `MIX_INCLUDE_RINDLE` env anywhere in this job
    - checkout, setup BEAM Elixir `1.19.5` and OTP `27.3`, `mix deps.get`,
      `mix compile --warnings-as-errors`, and
      `mix test --exclude requires_example_host --exclude advisory_only`

    Add job `advisory-rindle-proof`:
    - `name: advisory rindle proof (dep present, validate_dependency :ok)`
    - `runs-on: ubuntu-latest`
    - `timeout-minutes: 30`
    - `if:` limited to schedule and workflow_dispatch
    - `continue-on-error: true`
    - step-level `MIX_INCLUDE_RINDLE: "1"` on deps.get, compile, and advisory test steps only
    - runs `mix test test/crosswake/proof/phase45_rindle_advisory_test.exs`

    Header comments must document promotion conditions: real Rindle adapter work
    in scope, advisory lane exercising real upload/verify behavior, sustained
    stable scheduled runs, and explicit requirement/roadmap scope change.
  </action>
  <verify>
    <automated>python3 -c "import yaml; d=yaml.safe_load(open('.github/workflows/phase45-proof.yml')); assert 'merge-blocking-rindle-proof' in d['jobs']; assert d['jobs']['advisory-rindle-proof']['continue-on-error'] is True; print('YAML_OK')"</automated>
  </verify>
  <acceptance_criteria>
    - Workflow file exists and parses as YAML
    - Workflow defines `merge-blocking-rindle-proof`
    - Workflow defines `advisory-rindle-proof`
    - Merge-blocking job contains zero `MIX_INCLUDE_RINDLE` occurrences
    - Advisory job has `continue-on-error: true`
    - Advisory job sets `MIX_INCLUDE_RINDLE: "1"` only at step level
    - Workflow header documents the advisory promotion path
  </acceptance_criteria>
  <done>Phase 45 CI proves the hermetic Rindle seam and exposes dependency-present checks without blocking merges.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI hermetic job -> project claims | Merge-blocking proof must remain dependency-absent |
| CI advisory job -> optional dependency | Advisory job fetches optional Rindle package |
| Proof tests -> documentation truth | Tests become the source of support claims for Phase 45 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-45-07 | Tampering | Hermetic CI job | mitigate | No `MIX_INCLUDE_RINDLE` in hermetic job; advisory test excluded by tag |
| T-45-08 | Denial of service | Advisory dependency instability | accept | Advisory job uses `continue-on-error: true` and cannot gate merge |
| T-45-09 | Repudiation | Promotion claims | mitigate | Workflow header records promotion conditions before advisory proof can become merge-blocking |
</threat_model>

<verification>
Run:
- `mix test test/crosswake/proof/phase45_rindle_companion_test.exs test/crosswake/proof/phase45_rindle_mock_media_test.exs`
- `mix test --exclude requires_example_host --exclude advisory_only`
- `python3 -c "import yaml; d=yaml.safe_load(open('.github/workflows/phase45-proof.yml')); assert 'merge-blocking-rindle-proof' in d['jobs']; print('YAML_OK')"`
</verification>

<success_criteria>
- PROOF-01 has Phase 45 hermetic plus advisory proof coverage.
- MEDIA-03 is proven by merge-blocking tests with no external SDK.
- Advisory Rindle dependency checks remain visible but non-blocking.
</success_criteria>
