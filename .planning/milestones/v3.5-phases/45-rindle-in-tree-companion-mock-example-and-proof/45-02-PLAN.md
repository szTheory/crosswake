---
phase: 45-rindle-in-tree-companion-mock-example-and-proof
plan: 02
type: execute
wave: 2
depends_on: [45-01]
files_modified:
  - examples/phoenix_host/lib/crosswake_example/media/mock_capture.ex
  - examples/phoenix_host/lib/crosswake_example/media/reconciliation_keys.ex
  - examples/phoenix_host/lib/crosswake_example/media/reconciliation_inbox.ex
  - examples/phoenix_host/lib/crosswake_example/media/media_projection.ex
  - examples/phoenix_host/lib/crosswake_example/media/media_lane_live.ex
  - examples/phoenix_host/lib/crosswake_example/router.ex
  - test/crosswake/proof/phase45_rindle_mock_media_test.exs
  - test/crosswake/proof/phase45_rindle_live_test.exs
autonomous: true
requirements: [MEDIA-03]

must_haves:
  truths:
    - "D-03: example-host orchestration lives under examples/phoenix_host/lib/crosswake_example/media/ and remains pure Elixir/provider-neutral"
    - "D-04: MockCapture, ReconciliationInbox, MediaProjection, and MediaLaneLive responsibilities stay separate"
    - "D-05: the mock capture API is function-level and deterministic; no upload_and_verify/1 shortcut hides the authority boundary"
    - "D-06: stable event identity is derived from grant_id and idempotency_key plus event kind/storage target, not correlation_id"
    - "D-07: replay with changed correlation_id is marked replay?: true and never promotes availability"
    - "D-08: the example uses a LiveView-first proof route backed by explicit media modules"
    - "D-09: /media/proof uses runtime :live_view and offline :unavailable without claiming offline capture ownership"
    - "D-10: the proof route exposes queued, uploaded, scanning, available, and optional rejected/failure states with honest copy"
    - "D-11: important proof path is MockCapture -> ReconciliationInbox.ingest_capture_evidence/2 -> MediaProjection.project_object/2 -> derived display state"
    - "D-12: no controller/API endpoint is added unless strictly needed for proof ergonomics"
    - "D-13: no native shell, bridge, simulator, or actual file upload code is added"
    - "D-24: direct upload identity is useful, but upload completion is not attached/available authority"
    - "D-25: cache-to-store/promotion remains backend-owned and distinct from finished storage"
    - "D-26: idempotency keys are stable and backend-recognized, never transient correlation/request data"
  artifacts:
    - path: "examples/phoenix_host/lib/crosswake_example/media/mock_capture.ex"
      provides: "Pure-Elixir mock capture evidence emitter"
      contains: "def issue_upload_grant"
    - path: "examples/phoenix_host/lib/crosswake_example/media/media_projection.ex"
      provides: "Backend-owned media projection and derived display state"
      contains: "def project_object"
    - path: "examples/phoenix_host/lib/crosswake_example/media/media_lane_live.ex"
      provides: "LiveView proof route for media lane states"
      contains: "def render"
---

<objective>
Add the runnable pure-Elixir media mock lane in `examples/phoenix_host`, including
the `/media/proof` LiveView route that exposes honest media states without
claiming native capture, storage SDKs, or committed queued media.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/45-rindle-in-tree-companion-mock-example-and-proof/45-CONTEXT.md
@.planning/phases/45-rindle-in-tree-companion-mock-example-and-proof/45-RESEARCH.md
@.planning/phases/45-rindle-in-tree-companion-mock-example-and-proof/45-PATTERNS.md
@.planning/phases/44-rindle-media-seam-contracts-and-reconciliation-vocabulary/44-CONTEXT.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add pure media mock modules and stable key derivation</name>
  <files>examples/phoenix_host/lib/crosswake_example/media/mock_capture.ex, examples/phoenix_host/lib/crosswake_example/media/reconciliation_keys.ex, examples/phoenix_host/lib/crosswake_example/media/reconciliation_inbox.ex, test/crosswake/proof/phase45_rindle_mock_media_test.exs</files>
  <read_first>
    - lib/crosswake/companions/rindle/contracts.ex
    - lib/crosswake/companions/rindle/reconciliation.ex
    - examples/phoenix_host/lib/crosswake_example/commerce/mock_storefront.ex
    - examples/phoenix_host/lib/crosswake_example/commerce/reconciliation_keys.ex
    - examples/phoenix_host/lib/crosswake_example/commerce/reconciliation_inbox.ex
  </read_first>
  <action>
    Create `CrosswakeExample.Media.MockCapture` with function-level deterministic APIs:
    `issue_upload_grant/2`, `emit_capture_evidence/2`, and no `upload_and_verify/1`.
    Return Phase 44 Rindle contract structs through `Contracts.new_upload_grant/1` and
    `Contracts.new_capture_evidence/1`.

    Create `CrosswakeExample.Media.ReconciliationKeys` with `event_key/2`,
    `subject_key/1`, and `trace_metadata/2`. Event key fields must be
    `storage_target`, `grant_id`, `idempotency_key`, and event kind. Do not include
    `correlation_id`, timestamps, local queue ids, or progress fields.

    Create `CrosswakeExample.Media.ReconciliationInbox.ingest_capture_evidence/2`
    that calls `Crosswake.Companions.Rindle.Reconciliation.ingest_capture_evidence/2`,
    adds the stable event key, subject key, replay flag, and trace metadata.

    Add `phase45_rindle_mock_media_test.exs` with `Code.require_file/2` for the new
    example modules and tests for valid grant/evidence shape, stable event keys under
    changed `correlation_id`, replay marking, and mandatory idempotency failure when
    `idempotency_key` is missing or mismatched.
  </action>
  <verify>
    <automated>mix test test/crosswake/proof/phase45_rindle_mock_media_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - `MockCapture.issue_upload_grant/2` returns `{:ok, %Crosswake.Companions.Rindle.Contracts.UploadGrant{}}`
    - `MockCapture.emit_capture_evidence/2` returns `{:ok, %Crosswake.Companions.Rindle.Contracts.CaptureEvidence{}}`
    - Same grant/idempotency pair with changed `correlation_id` yields the same event key
    - Replay with the same event key returns `replay?: true`
    - Missing blank `idempotency_key` returns an error, not evidence
    - No new module contains `upload_and_verify`
    - Focused test command exits 0
  </acceptance_criteria>
  <done>The pure mock evidence lane is deterministic, idempotent, and evidence-only.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add backend-owned media projection and display state derivation</name>
  <files>examples/phoenix_host/lib/crosswake_example/media/media_projection.ex, test/crosswake/proof/phase45_rindle_mock_media_test.exs</files>
  <read_first>
    - examples/phoenix_host/lib/crosswake_example/commerce/entitlement_projection.ex
    - lib/crosswake/companions/rindle/contracts.ex
    - lib/crosswake/companions/rindle/reconciliation.ex
    - .planning/phases/45-rindle-in-tree-companion-mock-example-and-proof/45-CONTEXT.md
  </read_first>
  <action>
    Create `CrosswakeExample.Media.MediaProjection` with `project_object/2` and
    `derived_state/1`.

    `project_object/2` must accept nil or a current `MediaObject` plus either an
    evidence ingestion result or a verified backend projection input. Evidence-only
    upload results may create or update `:uploaded` or `:scanning`, but must not
    create `:available`. Only an explicit backend verification path using
    `Crosswake.Companions.Rindle.Contracts.verified_media_object/2` can produce
    `:available`.

    `derived_state/1` must return one of `:queued`, `:uploaded`, `:scanning`,
    `:available`, or `:rejected`. It must not collapse `:queued` into committed or
    available states.
  </action>
  <verify>
    <automated>mix test test/crosswake/proof/phase45_rindle_mock_media_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - Evidence-only ingestion cannot produce `%MediaObject{state: :available}`
    - `project_object/2` can produce `:uploaded` from device evidence
    - `project_object/2` can produce `:scanning` from verifier-owned work
    - `project_object/2` produces `:available` only through explicit backend verification fields
    - `derived_state(%MediaObject{state: :queued})` returns `:queued`
    - Focused test command exits 0
  </acceptance_criteria>
  <done>Backend projection owns availability, and queued/uploaded states stay visibly non-authoritative.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add /media/proof LiveView route and render-state proof</name>
  <files>examples/phoenix_host/lib/crosswake_example/media/media_lane_live.ex, examples/phoenix_host/lib/crosswake_example/router.ex, test/crosswake/proof/phase45_rindle_live_test.exs</files>
  <read_first>
    - examples/phoenix_host/lib/crosswake_example/paywall_entry_live.ex
    - examples/phoenix_host/lib/crosswake_example/router.ex
    - test/crosswake/proof/phase35_paywall_live_test.exs
    - .planning/phases/45-rindle-in-tree-companion-mock-example-and-proof/45-CONTEXT.md
  </read_first>
  <action>
    Create `CrosswakeExample.Media.MediaLaneLive` with deterministic events that
    drive `:queued`, `:uploaded`, `:scanning`, and `:available` through the mock
    modules. The render copy must explicitly distinguish:
    - queued: pending intent, not committed media
    - uploaded: device evidence recorded, not available
    - scanning: backend verification in progress
    - available: backend verified media

    Add a `/media/proof` route under a `/media` scope in `examples/phoenix_host/lib/crosswake_example/router.ex`.
    Use `crosswake_defaults runtime: :live_view, offline: :unavailable, security: :standard`
    and route policy id `media-proof-lane`. Do not add native shell, bridge, controller,
    or actual file upload code.

    Add `phase45_rindle_live_test.exs` following the Phase 35 direct callback/render
    pattern. Tag `:requires_example_host` only if the file depends on loading the
    example host.
  </action>
  <verify>
    <automated>mix test test/crosswake/proof/phase45_rindle_live_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - Router contains `live "/proof", Media.MediaLaneLive` or equivalent under `/media`
    - Route policy contains `id: "media-proof-lane"`
    - Route policy uses `runtime: :live_view`
    - Route policy uses `offline: :unavailable`
    - Rendered queued state contains copy meaning "not committed" or equivalent exact assertion chosen by the test
    - Rendered uploaded state does not contain "available"
    - Rendered available state appears only after explicit backend verification event/test path
    - Focused LiveView test exits 0
  </acceptance_criteria>
  <done>The checked-in example host exposes the media lane honestly as a LiveView proof route.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Device/mock evidence -> inbox | Evidence can be replayed and can carry trace metadata |
| Inbox -> projection | Evidence records are not authoritative media availability |
| Projection -> LiveView | UI copy can accidentally overclaim availability |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-45-04 | Repudiation | Replay handling | mitigate | Stable event keys exclude `correlation_id`; replay is asserted with changed correlation ids |
| T-45-05 | Elevation of privilege | Availability projection | mitigate | Only `Contracts.verified_media_object/2` can produce `:available` |
| T-45-06 | Information disclosure | UI state labels | mitigate | Tests assert queued/uploaded/scanning copy does not present media as committed or available |
</threat_model>

<verification>
Run:
- `mix test test/crosswake/proof/phase45_rindle_mock_media_test.exs`
- `mix test test/crosswake/proof/phase45_rindle_live_test.exs`
- `mix test --exclude requires_example_host --exclude advisory_only`
</verification>

<success_criteria>
- MEDIA-03 pure-Elixir mock lane exists and runs without external SDKs.
- Stable idempotency and replay behavior are mechanically tested.
- The example route communicates queued/uploaded/scanning/available truth without offline or native overclaiming.
</success_criteria>
