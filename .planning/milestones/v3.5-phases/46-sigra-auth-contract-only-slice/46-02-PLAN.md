---
phase: 46-sigra-auth-contract-only-slice
plan: 02
type: execute
wave: 2
depends_on: ["46-01"]
files_modified:
  - lib/crosswake/policy/schema.ex
  - lib/crosswake/policy/route.ex
  - lib/crosswake/manifest/types.ex
  - lib/crosswake/manifest/builder.ex
  - examples/phoenix_host/lib/crosswake_example/router.ex
  - examples/ios_shell_host/Fixtures/crosswake_manifest.json
  - examples/android_shell_host/app/src/main/assets/crosswake_manifest.json
  - test/crosswake/policy/schema_test.exs
  - test/crosswake/proof/phase46_sigra_auth_contract_test.exs
autonomous: true
requirements: [AUTH-02]

must_haves:
  truths:
    - "Route policy accepts `auth_min_level` and `requires_recent_auth` as explicit route-local DSL keys and rejects invalid values."
    - "Manifest RouteEntry truth carries auth predicates when declared and omits them when nil."
    - "The checked-in iOS and Android shell fixture manifests are regenerated from the example host and carry the auth predicate keys for the chosen example route, per D-14."
    - "The Phase 46 proof file exists as a hermetic AUTH-02 scaffold covering route-policy and manifest truth before RouteGate wiring lands."
  artifacts:
    - path: "lib/crosswake/policy/schema.ex"
      provides: "DSL validation for auth_min_level and requires_recent_auth"
      contains: "validate_auth_min_level"
    - path: "lib/crosswake/policy/route.ex"
      provides: "Route struct fields for auth predicates"
      contains: ":auth_min_level"
    - path: "lib/crosswake/manifest/types.ex"
      provides: "RouteEntry auth predicate fields and serialization"
      contains: "\"auth_min_level\""
    - path: "examples/phoenix_host/lib/crosswake_example/router.ex"
      provides: "stable example-host route carrying auth predicates so checked-in shell manifests expose the new truth"
      contains: "auth_min_level"
    - path: "examples/ios_shell_host/Fixtures/crosswake_manifest.json"
      provides: "checked-in iOS shell fixture manifest with auth predicate truth"
      contains: "\"auth_min_level\""
    - path: "examples/android_shell_host/app/src/main/assets/crosswake_manifest.json"
      provides: "checked-in Android shell fixture manifest with auth predicate truth"
      contains: "\"auth_min_level\""
    - path: "test/crosswake/proof/phase46_sigra_auth_contract_test.exs"
      provides: "Hermetic AUTH-02 proof scaffold for DSL and manifest truth"
      contains: "Phase46SigraAuthContractTest"
  key_links:
    - from: "Crosswake.Policy.Schema"
      to: "Crosswake.Policy.Route"
      via: "validated auth_min_level / requires_recent_auth fields"
      pattern: "auth_min_level|requires_recent_auth"
    - from: "Crosswake.Manifest.Builder"
      to: "Crosswake.Manifest.Types.RouteEntry"
      via: "builder projection"
      pattern: "auth_min_level: route.auth_min_level"
    - from: "examples/phoenix_host/lib/crosswake_example/router.ex"
      to: "examples/ios_shell_host/Fixtures/crosswake_manifest.json"
      via: "`mix run examples/phoenix_host/gen_manifest.exs` regeneration path"
      pattern: "auth_min_level|requires_recent_auth"
---

<objective>
Wire the compile-time half of AUTH-02: declare auth predicates on routes, carry them into manifest
truth, and regenerate the checked-in shell fixture manifests so the auth predicate surface is
visible in product-owned assets before RouteGate denial logic lands.

Purpose: D-14 makes manifest truth part of the shipped contract, not an internal detail. Before
runtime denial wiring can be trusted, the DSL, manifest types, example-host route declaration, and
checked-in shell fixture manifests must all agree on the auth predicate shape.

Output: auth predicate support in policy/schema/route/manifest code, regenerated iOS/Android shell
fixture manifests, and a hermetic phase proof file scaffold covering the compile-time path.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/MILESTONE-ARC.md
@.planning/phases/46-sigra-auth-contract-only-slice/46-CONTEXT.md
@.planning/phases/46-sigra-auth-contract-only-slice/46-RESEARCH.md
@.planning/phases/46-sigra-auth-contract-only-slice/46-VALIDATION.md
@.planning/phases/46-sigra-auth-contract-only-slice/46-PATTERNS.md
@.planning/phases/46-sigra-auth-contract-only-slice/46-01-SUMMARY.md

<interfaces>
From `lib/crosswake/policy/schema.ex` and `lib/crosswake/policy/route.ex`:

```elixir
@schema NimbleOptions.new!([
  gated_by: [type: {:custom, __MODULE__, :validate_flag_key, []}],
  on_unavailable: [type: {:custom, __MODULE__, :validate_on_unavailable, []}]
])

def validate_flag_key(value) when is_atom(value) and value not in [true, false, nil] do
  ...
end

defstruct [..., :gated_by, :on_unavailable, ...]
@type t :: %__MODULE__{..., gated_by: atom() | nil, on_unavailable: :deny | {:fallback_phoenix, atom()} | nil}
```

From `lib/crosswake/manifest/types.ex` and `lib/crosswake/manifest/builder.ex`:

```elixir
defmodule RouteEntry do
  defstruct [..., :gated_by, :on_unavailable, ...]
end

Types.new_route_entry(
  ...,
  gated_by: route.gated_by,
  on_unavailable: route.on_unavailable
)
```

From `examples/phoenix_host/gen_manifest.exs`:

```elixir
{:ok, %{manifest: manifest}} = Crosswake.Manifest.compile(CrosswakeExample.Router)
json = Crosswake.Manifest.Serializer.render(manifest)
File.write!("../../examples/ios_shell_host/Fixtures/crosswake_manifest.json", json)
File.write!("../../examples/android_shell_host/app/src/main/assets/crosswake_manifest.json", json)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add auth predicates to Policy.Schema and Policy.Route</name>
  <files>lib/crosswake/policy/schema.ex, lib/crosswake/policy/route.ex, test/crosswake/policy/schema_test.exs</files>
  <read_first>
    - lib/crosswake/policy/schema.ex (current `@schema`, custom validator style, and typespec for route DSL fields)
    - lib/crosswake/policy/route.ex (current defstruct, `@type t`, and cross-field validation helpers)
    - test/crosswake/policy/schema_test.exs (positive/negative NimbleOptions cases)
    - lib/crosswake/companions/sigra/contracts.ex (Plan 46-01 comparison helpers and closed MFA vocabulary)
    - .planning/phases/46-sigra-auth-contract-only-slice/46-CONTEXT.md (D-12 through D-18)
    - .planning/phases/46-sigra-auth-contract-only-slice/46-RESEARCH.md (`requires_recent_auth` resolved to positive integer seconds only)
  </read_first>
  <behavior>
    - Test 1: `Schema.validate!/1` accepts valid `auth_min_level` and positive-integer `requires_recent_auth` values.
    - Test 2: invalid auth vocabulary or non-positive/invalid recency values raise `NimbleOptions.ValidationError`.
    - Test 3: route compilation can thread the new auth fields through `Policy.Route` without coupling them to `gated_by` or `on_unavailable` defaults.
  </behavior>
  <action>
    Extend the route-policy DSL per D-12 through D-18. In `Crosswake.Policy.Schema`, add
    `auth_min_level` and `requires_recent_auth` to `@schema`, add a
    `validate_auth_min_level/1` custom validator that delegates to the closed MFA vocabulary or
    comparison helper exported by `Crosswake.Companions.Sigra.Contracts` (D-05, D-13), and add a
    `validate_requires_recent_auth/1` validator that accepts only positive integer seconds (choose
    integer seconds explicitly per D-06/D-13; do not introduce duration strings or maps). In
    `Crosswake.Policy.Route`, extend the defstruct and `@type t` with both fields, thread them
    through `new/1` and `new!/1`, and keep them route-local declarations that do not depend on
    `gated_by`, `on_unavailable`, capabilities, or commerce state (D-12, D-17). Preserve the
    existing gating posture helper; do not reuse gating vocabulary or denial codes for auth
    predicates (D-18). Extend `schema_test.exs` first with positive and negative predicate cases,
    then implement until the focused validation tests pass.
  </action>
  <verify>
    <automated>cd /Users/jon/projects/crosswake && mix test test/crosswake/policy/schema_test.exs -x</automated>
  </verify>
  <acceptance_criteria>
    - `lib/crosswake/policy/schema.ex` defines validators for `auth_min_level` and `requires_recent_auth`
    - The schema accepts only the closed MFA vocabulary exported by the Sigra contracts module
    - `requires_recent_auth` accepts only positive integer seconds
    - `lib/crosswake/policy/route.ex` carries both auth fields in the defstruct and `@type t`
    - The new auth fields do not modify the existing `gated_by`/`on_unavailable` defaulting semantics
    - `mix test test/crosswake/policy/schema_test.exs -x` exits 0
  </acceptance_criteria>
  <done>Route DSL and normalized Route structs carry auth predicates with strict validation and focused tests proving the contract.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Carry auth predicates into RouteEntry truth, regenerate shell fixture manifests, and create the AUTH-02 proof scaffold</name>
  <files>lib/crosswake/manifest/types.ex, lib/crosswake/manifest/builder.ex, examples/phoenix_host/lib/crosswake_example/router.ex, examples/ios_shell_host/Fixtures/crosswake_manifest.json, examples/android_shell_host/app/src/main/assets/crosswake_manifest.json, test/crosswake/proof/phase46_sigra_auth_contract_test.exs</files>
  <read_first>
    - lib/crosswake/manifest/types.ex (RouteEntry defstruct, `new_route_entry/1`, `to_map/1`, additive nil-omission pattern)
    - lib/crosswake/manifest/builder.ex (route -> RouteEntry projection)
    - examples/phoenix_host/lib/crosswake_example/router.ex (stable example-host route declarations that can carry auth predicate truth)
    - examples/phoenix_host/gen_manifest.exs (canonical regeneration path for the checked-in shell fixture manifests)
    - examples/ios_shell_host/Fixtures/crosswake_manifest.json (existing checked-in iOS manifest asset)
    - examples/android_shell_host/app/src/main/assets/crosswake_manifest.json (existing checked-in Android manifest asset)
    - test/crosswake/proof/phase40_gate_evaluation_test.exs (hermetic proof structure for RouteGate work)
    - .planning/phases/46-sigra-auth-contract-only-slice/46-PATTERNS.md (RouteEntry extension and builder projection pattern)
    - .planning/phases/46-sigra-auth-contract-only-slice/46-VALIDATION.md (Wave 0 AUTH-02 proof file requirement)
    - .planning/phases/46-sigra-auth-contract-only-slice/46-CONTEXT.md (D-14 checked-in shell fixture manifests are part of manifest truth)
  </read_first>
  <behavior>
    - Test 1: manifest route entries include stringified `auth_min_level` and integer `requires_recent_auth` when declared.
    - Test 2: manifest route entries omit both keys when the route declares no auth predicates.
    - Test 3: the checked-in iOS and Android shell fixture manifests both carry the new auth predicate keys for the same example-host route after regeneration.
    - Test 4: the proof file stays hermetic and compiles a minimal router instead of reaching into example hosts.
  </behavior>
  <action>
    Extend manifest truth per D-14. In `Crosswake.Manifest.Types.RouteEntry`, add
    `auth_min_level: atom() | nil` and `requires_recent_auth: pos_integer() | nil` to the defstruct,
    `@type t`, `new_route_entry/1`, and `to_map/1`. Serialize `auth_min_level` as its string atom
    name and `requires_recent_auth` as an integer; omit either key when nil, matching the additive
    omission pattern used for `gated_by` and `on_unavailable`. In `Crosswake.Manifest.Builder`,
    project `route.auth_min_level` and `route.requires_recent_auth` directly into
    `Types.new_route_entry(...)`. In `examples/phoenix_host/lib/crosswake_example/router.ex`, choose
    one stable existing route that already represents authenticated or member-only access and add
    explicit `auth_min_level` and positive-integer `requires_recent_auth` declarations there solely
    to surface manifest truth; do not add Sigra runtime machinery, step-up UI, or any host-side
    ceremony flow to the example host. Regenerate the checked-in fixture manifests with
    `mix run examples/phoenix_host/gen_manifest.exs`, and treat the generated JSON as the source of
    truth rather than hand-editing the assets. Create the Wave 0 proof file
    `test/crosswake/proof/phase46_sigra_auth_contract_test.exs` with hermetic manifest-focused
    cases first: inline router fixture, compiled manifest assertions for declared-vs-undeclared auth
    keys, and a self-assertion that the proof file does not depend on example-host routers or
    external Sigra machinery. Keep the proof file ready to be extended by the RouteGate and
    doctor/support plans.
  </action>
  <verify>
    <automated>cd /Users/jon/projects/crosswake && mix run examples/phoenix_host/gen_manifest.exs && mix test test/crosswake/proof/phase46_sigra_auth_contract_test.exs -x && rg -n "\"auth_min_level\"|\"requires_recent_auth\"" examples/ios_shell_host/Fixtures/crosswake_manifest.json examples/android_shell_host/app/src/main/assets/crosswake_manifest.json</automated>
  </verify>
  <acceptance_criteria>
    - `RouteEntry` includes `auth_min_level` and `requires_recent_auth` in both struct and type definitions
    - `new_route_entry/1` and `to_map/1` thread the auth predicate fields through the manifest type
    - `to_map/1` stringifies `auth_min_level`, keeps `requires_recent_auth` numeric, and omits nil auth keys
    - `Manifest.Builder` copies the auth predicate fields from `Crosswake.Policy.Route`
    - `examples/phoenix_host/lib/crosswake_example/router.ex` declares auth predicates on one stable example route used to regenerate shell fixtures
    - `examples/ios_shell_host/Fixtures/crosswake_manifest.json` and `examples/android_shell_host/app/src/main/assets/crosswake_manifest.json` are regenerated and each contain the new auth predicate keys for that route
    - `test/crosswake/proof/phase46_sigra_auth_contract_test.exs` exists with manifest serialization assertions and a hermeticity self-check
  </acceptance_criteria>
  <done>Auth predicates now exist in manifest truth, the checked-in shell fixture manifests expose them per D-14, and the phase46 proof file exists to prove additive serialization and hermetic scope.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| route-policy declaration -> manifest RouteEntry | Developer-authored route config becomes manifest truth consumed by shells, doctor, and later RouteGate work |
| example-host route declaration -> checked-in shell fixture manifests | Example-host policy truth is regenerated into product-owned JSON assets shipped as shell fixtures |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-46-04 | Tampering / Elevation | `Policy.Schema` -> `Route` -> `RouteEntry` auth predicate path | mitigate | Add focused schema/route tests plus phase46 manifest proof so invalid vocab or missing fields cannot drift silently across the compile path. |
| T-46-05 | Tampering | checked-in shell fixture manifest assets | mitigate | Regenerate `examples/ios_shell_host/Fixtures/crosswake_manifest.json` and `examples/android_shell_host/app/src/main/assets/crosswake_manifest.json` from `examples/phoenix_host/gen_manifest.exs` in the same task so D-14 fixture truth stays aligned with live manifest serialization. |
| T-46-06 | Repudiation | example-host route chosen to surface auth predicates | mitigate | Use one stable existing authenticated route only to surface manifest truth; do not add step-up machinery or mutate unrelated example-host behavior. |
| T-46-SC | Tampering | npm/pip/cargo installs | accept | No package-manager installs in Phase 46; pure in-tree Elixir changes only. |
</threat_model>

<verification>
- `cd /Users/jon/projects/crosswake && mix test test/crosswake/policy/schema_test.exs -x` exits 0
- `cd /Users/jon/projects/crosswake && mix run examples/phoenix_host/gen_manifest.exs && mix test test/crosswake/proof/phase46_sigra_auth_contract_test.exs -x` exits 0
- `cd /Users/jon/projects/crosswake && mix compile --warnings-as-errors` exits 0
</verification>

<success_criteria>
- Route policy accepts `auth_min_level` and `requires_recent_auth` and manifests serialize them honestly
- Checked-in iOS and Android shell fixture manifests are regenerated from the example host and carry auth predicate truth per D-14
- The phase46 proof file covers DSL and manifest behavior hermetically and is ready for RouteGate/doctor/support extensions
</success_criteria>

<output>
Create `.planning/phases/46-sigra-auth-contract-only-slice/46-02-SUMMARY.md` when done
</output>
