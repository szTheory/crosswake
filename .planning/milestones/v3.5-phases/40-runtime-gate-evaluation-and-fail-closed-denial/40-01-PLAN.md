---
phase: 40-runtime-gate-evaluation-and-fail-closed-denial
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/crosswake/shell/denial.ex
  - lib/crosswake/compatibility/route_gate.ex
  - test/crosswake/proof/phase40_gate_evaluation_test.exs
autonomous: true
requirements: [GATE-03, GATE-04]

must_haves:
  truths:
    - "A gated route with an active flag (no companion denies, no kill switch) activates normally (transition :activate)."
    - "When a companion returns {:deny, finding} from route_gated?/2, RouteGate.evaluate/4 produces a :gate_denied denial whose details map has flag_key, reason, variant, evaluated_at all present and non-nil (D-03)."
    - "When a companion returns true from kill_switch_active?/1, RouteGate produces a :kill_switch_active denial and route_gated?/2 is never called on any companion (D-10, D-11)."
    - "A denied gated route with on_unavailable: :deny yields transition :halt; with on_unavailable: {:fallback_phoenix, :home} yields transition {:redirect, :home} (D-01)."
    - "Non-gated routes (gated_by == nil) skip both kill-switch and gate evaluation entirely (D-11)."
    - "RouteGate.evaluate/4 makes no network call — all inputs are the compiled manifest, target, and registered companion modules (SC#4)."
    - "RouteGate iterates ALL enabled companions (from Application.get_env(:crosswake, :companions, [])) for both kill-switch and gate checks; a companion returning false/pass abstains without opening the route (D-09)."
    - "Hermetic proof test covers SC#1-4: :gate_denied details with all four fields, kill_switch short-circuit spy confirming route_gated?/2 never called, on_unavailable transition variants, and network-free hermeticity assertion (D-14)."
  artifacts:
    - path: "lib/crosswake/shell/denial.ex"
      provides: ":gate_denied and :kill_switch_active in @reasons and reason typespec"
      contains: ":gate_denied"
    - path: "lib/crosswake/compatibility/route_gate.ex"
      provides: "kill-switch short-circuit + gate evaluation steps wired into evaluate/4; transition_for reads on_unavailable"
      contains: "prepend_gate_evaluation_findings"
    - path: "test/crosswake/proof/phase40_gate_evaluation_test.exs"
      provides: "Hermetic proof covering SC#1-4"
      contains: "Phase40GateEvaluationTest"
  key_links:
    - from: "lib/crosswake/compatibility/route_gate.ex"
      to: "Application.get_env(:crosswake, :companions, [])"
      via: "companion registry read at evaluation time"
      pattern: "Application.get_env\\(:crosswake, :companions"
    - from: "lib/crosswake/compatibility/route_gate.ex"
      to: "Crosswake.Shell.Denial.new/1"
      via: "direct :gate_denied / :kill_switch_active denial construction"
      pattern: "Denial.new\\("
    - from: "lib/crosswake/compatibility/route_gate.ex"
      to: ":telemetry.span/3"
      via: "wrapping each companion callback"
      pattern: ":telemetry.span"
---

<objective>
Wire `kill_switch_active?/1` and `route_gated?/2` companion callbacks into
`RouteGate.evaluate/4` so gated routes fail closed by default with structured,
explainable denials, and kill switches short-circuit ahead of all other gate
evaluation. Extend `Shell.Denial` with the two new reason atoms and broaden the
`Decision.transition` typespec to carry redirect intent.

Purpose: Satisfies GATE-03 (`:gate_denied` with OpenFeature-shaped `details`) and
GATE-04 (kill-switch short-circuit; `{:fallback_phoenix}` is the only fail-open
path). Makes the previously-specified-but-unwired Phase 38 companion contract
live.

Output:
- `lib/crosswake/shell/denial.ex` — `:gate_denied`, `:kill_switch_active` reasons
- `lib/crosswake/compatibility/route_gate.ex` — gate/kill-switch evaluation steps,
  broadened `Decision.transition`, `transition_for/3` reading `on_unavailable`
- `test/crosswake/proof/phase40_gate_evaluation_test.exs` — hermetic SC#1-4 proof
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/40-runtime-gate-evaluation-and-fail-closed-denial/40-CONTEXT.md
@.planning/phases/40-runtime-gate-evaluation-and-fail-closed-denial/40-RESEARCH.md
@.planning/phases/40-runtime-gate-evaluation-and-fail-closed-denial/40-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write the failing hermetic proof for SC#1-4</name>
  <files>test/crosswake/proof/phase40_gate_evaluation_test.exs</files>
  <read_first>
    - test/crosswake/proof/phase38_companion_contract_test.exs (exact analog: async: false, Application.put_env companion fixture, on_exit cleanup, telemetry attach/detach, hermeticity self-assertion at lines 95-103)
    - test/crosswake/proof/phase39_route_policy_gating_test.exs (inline router fixture pattern lines 29-65: `use Crosswake.Router`, `crosswake_defaults`, `live` macro with `crosswake: [...]` opts)
    - test/support/stub_companion.ex (minimal Crosswake.Companion fixture: all callbacks companion_id/0, enabled?/1, kill_switch_active?/1, route_gated?/2, validate_dependency/0, report_state/0)
    - lib/crosswake/companion.ex (behaviour callback specs and required arities)
    - lib/crosswake/compatibility/route_gate.ex (current evaluate/4 signature and Decision struct)
    - lib/crosswake/manifest/types.ex §RouteEntry (gated_by, on_unavailable fields; new_route_entry/1 builder)
  </read_first>
  <behavior>
    - SC#1: With a fixture companion returning `{:deny, %Finding{axis: :gate_denied, ...}}` from route_gated?/2 for a route gated by :test_flag, RouteGate.evaluate/4 returns a Decision with status :deny whose denial.reason == :gate_denied; denial.details has "flag_key" == "test_flag", "reason" == "DISABLED", "variant" == "off", and "evaluated_at" a non-nil ISO8601 string (assert String.match? against an ISO8601 pattern, not exact value).
    - SC#2: With a fixture companion whose kill_switch_active?/1 returns true (and which records calls via Process.put), RouteGate.evaluate/4 returns denial.reason == :kill_switch_active; assert Process.get(:route_gated_called) == nil (route_gated?/2 never called) and Process.get(:kill_switch_active_called) == true. Clear both keys in setup with Process.delete to avoid stale state.
    - SC#3a: Fixture companion denies a gated route whose on_unavailable == :deny → decision.transition == :halt.
    - SC#3b: Fixture companion denies a gated route whose on_unavailable == {:fallback_phoenix, :home} → decision.transition == {:redirect, :home}.
    - SC#3c: Non-gated route (gated_by nil) with a kill-switch+gate companion registered → decision is unaffected by gate logic (companion gate/kill callbacks not invoked); assert via the same Process spy keys remaining unset.
    - SC#4 (hermeticity): Read __ENV__.file source; refute it contains the example-host router reference (assemble the string at runtime so this file does not itself contain it) and refute it matches `code\.require_file\s*\(`. Mirror phase38 lines 95-103.
    - Telemetry: attach a handler on [:crosswake, :companion, :route_gate, :stop]; after a gate-denied evaluation assert_receive {:telemetry_stop, %{companion_id: <fixture id>}}, 1000; detach in on_exit.
  </behavior>
  <action>
    Create `test/crosswake/proof/phase40_gate_evaluation_test.exs` as module
    `Crosswake.Proof.Phase40GateEvaluationTest` using `use ExUnit.Case, async: false`
    (async: false is mandatory — `:companions` is a shared global Application key per D-13).
    Untagged so `phase34-proof.yml` picks it up automatically (D-12) — do NOT add an
    `@tag` or create a new workflow file.
    Define inline fixture companion modules implementing the full `Crosswake.Companion`
    behaviour: `GateDenyCompanion` (companion_id :gate_deny_companion, enabled?/1 true,
    kill_switch_active?/1 false, route_gated?/2 returns {:deny, %Finding{axis: :gate_denied,
    route_id: "gated", message: ...}} for `%RouteEntry{gated_by: :test_flag}` and :pass
    otherwise) and `KillSwitchCompanion` (kill_switch_active?/1 records Process.put(:kill_switch_active_called, true)
    and returns true; route_gated?/2 records Process.put(:route_gated_called, true)).
    Both must implement report_state/0 returning a %Crosswake.Companion.State{} with
    checked_at: System.monotonic_time(:millisecond). Build routes via the manifest/RouteEntry
    builder with gated_by and on_unavailable set per each SC. Register companions per-test
    with `Application.put_env(:crosswake, :companions, [...])` and clean up in `on_exit` with
    `Application.delete_env(:crosswake, :companions)`. In `setup`, `Process.delete(:route_gated_called)`
    and `Process.delete(:kill_switch_active_called)`. This task is RED — the test references
    `:gate_denied` / `:kill_switch_active` reasons and the `{:redirect, _}` transition that do
    not exist yet, so it MUST fail to compile or fail assertions until Tasks 2-3 land.
  </action>
  <verify>
    <automated>mix test test/crosswake/proof/phase40_gate_evaluation_test.exs; test $? -ne 0 && echo "RED as expected"</automated>
  </verify>
  <acceptance_criteria>
    - File `test/crosswake/proof/phase40_gate_evaluation_test.exs` exists and defines module `Crosswake.Proof.Phase40GateEvaluationTest`.
    - `grep -c "async: false" test/crosswake/proof/phase40_gate_evaluation_test.exs` returns >= 1.
    - File contains no `@tag` annotation: `grep -v '^#' test/crosswake/proof/phase40_gate_evaluation_test.exs | grep -c '@tag'` returns 0.
    - File contains test cases referencing `:gate_denied`, `:kill_switch_active`, `:halt`, and `{:redirect, :home}`.
    - `mix test test/crosswake/proof/phase40_gate_evaluation_test.exs` exits non-zero (RED — reasons/transition not yet implemented).
  </acceptance_criteria>
  <done>Proof file exists, is untagged + async: false, encodes SC#1-4 plus the route_gate telemetry assertion, and fails (RED) because the production changes are not yet made.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add :gate_denied and :kill_switch_active to Shell.Denial</name>
  <files>lib/crosswake/shell/denial.ex</files>
  <read_first>
    - lib/crosswake/shell/denial.ex (current @reasons list lines 8-17, @type reason lines 22-30, new/1 lines 46-61, to_map/1 lines 63-76, ensure_commerce_corridor_payload catch-all)
  </read_first>
  <behavior>
    - `Crosswake.Shell.Denial.reasons/0` returns a list that includes `:gate_denied` and `:kill_switch_active`.
    - `Denial.new(reason: :gate_denied, message: "x", details: %{"flag_key" => "f", "reason" => "DISABLED", "variant" => "off", "evaluated_at" => "2026-01-01T00:00:00Z"})` builds without raising and `to_map/1` round-trips the details map with string keys/values intact (evaluated_at passes through as a string).
    - `Denial.new(reason: :kill_switch_active, message: "x", details: %{"companion_id" => "c"})` builds without raising.
  </behavior>
  <action>
    Append `:gate_denied` and `:kill_switch_active` to the `@reasons` module attribute
    list (after `:pack_incompatible`). Append `| :gate_denied | :kill_switch_active` union
    arms to the `@type reason` typespec — BOTH locations are required: `struct!` does not
    validate against `@reasons`, but the typespec drives Dialyzer and `reasons/0` is consumed
    by the Phase 41 doctor (per RESEARCH Pitfall 2). Do NOT add any clause to
    `ensure_commerce_corridor_payload/3` — its catch-all already passes these reasons through
    unchanged (per PATTERNS line 245). Do NOT add a `:transition` field to Denial — routing
    intent lives on `Decision.transition`, and `Denial.status` stays `:deny` (D-02).
  </action>
  <verify>
    <automated>mix compile --warnings-as-errors 2>&1 | grep -v '^#' | grep -ci "warning:" | grep -qx 0 && grep -q ":gate_denied" lib/crosswake/shell/denial.ex && grep -q ":kill_switch_active" lib/crosswake/shell/denial.ex && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c ":gate_denied" lib/crosswake/shell/denial.ex` returns >= 2 (appears in @reasons and typespec).
    - `grep -c ":kill_switch_active" lib/crosswake/shell/denial.ex` returns >= 2.
    - `mix run -e 'true = :gate_denied in Crosswake.Shell.Denial.reasons() and :kill_switch_active in Crosswake.Shell.Denial.reasons()'` exits 0.
    - `mix compile --warnings-as-errors` succeeds.
  </acceptance_criteria>
  <done>Both new reason atoms appear in `@reasons` and the `reason` typespec; `Denial.reasons/0` returns them; module compiles warning-free.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Wire gate + kill-switch evaluation into RouteGate.evaluate/4 (GREEN)</name>
  <files>lib/crosswake/compatibility/route_gate.ex</files>
  <read_first>
    - lib/crosswake/compatibility/route_gate.ex (full file: Decision typespec lines 18-24, evaluate/4 lines 32-52, transition_for/2 lines 54-62, prepend_commerce_corridor_findings/3 two-clause pattern lines 64-74, maybe_add_finding/2 lines 182-183)
    - lib/crosswake/shell/denial.ex (Denial.new/1 keyword signature, required keys reason + message)
    - lib/crosswake/companion.ex (kill_switch_active?/1 and route_gated?/2 callback specs; telemetry event-name contract [:crosswake, :companion, :kill_switch] / [:crosswake, :companion, :route_gate])
    - lib/crosswake/doctor/doctor.ex lines 514-530 (companion registry dispatch: Application.get_env(:crosswake, companion.companion_id(), %{}) for config, enabled?/1 filter; :telemetry.span/3 convention)
    - lib/crosswake/manifest/types.ex §RouteEntry (gated_by, on_unavailable fields)
  </read_first>
  <behavior>
    - Gated route (gated_by != nil) with a companion returning {:deny, finding}: evaluate/4 returns status :deny, denial.reason :gate_denied, details %{"flag_key" => Atom.to_string(route.gated_by), "reason" => "DISABLED", "variant" => "off", "evaluated_at" => ISO8601 string}.
    - Gated route with a companion whose kill_switch_active?/1 returns true: denial.reason :kill_switch_active, details includes "companion_id"; route_gated?/2 is never invoked (kill-switch short-circuits first).
    - Denied gated route: transition :halt when on_unavailable :deny; {:redirect, id} when on_unavailable {:fallback_phoenix, id}.
    - Non-gated route (gated_by nil) and nil route (unknown route_id): gate/kill-switch steps are skipped, existing pipeline behavior unchanged.
    - Each companion callback invocation is wrapped in :telemetry.span/3 with the contracted event names and %{companion_id: ..., route_id: route.id} metadata.
  </behavior>
  <action>
    Broaden `Decision.transition` typespec to `:activate | :halt | :stay_put | {:redirect, atom()}` (D-01);
    leave `status` as `:allow | :deny` (D-02). Add a private `prepend_gate_evaluation_findings/3`
    (or accumulate gate denials directly — planner-chosen Option A) and insert it into the
    `evaluate/4` pipeline BEFORE `prepend_commerce_corridor_findings/3`. Use a three-clause guard
    structure: `(_, nil, _)` returns input unchanged (unknown route — RESEARCH Pitfall 3),
    `(_, %RouteEntry{gated_by: nil}, _)` returns input unchanged (D-11), and the gated clause
    `(_, %RouteEntry{} = route, %Target{} = target)` does the work. In the gated clause:
    read `companions = Application.get_env(:crosswake, :companions, [])`, filter to those where
    `companion.enabled?(Application.get_env(:crosswake, companion.companion_id(), %{}))` is true
    (config key is the companion's own atom id, NOT :companions — RESEARCH Pitfall 4). Then
    short-circuit kill-switch FIRST: iterate enabled companions, wrap each
    `companion.kill_switch_active?(target)` in `:telemetry.span([:crosswake, :companion, :kill_switch], %{companion_id: companion.companion_id(), route_id: route.id}, fn -> ... end)`;
    first companion returning true → build `Denial.new(reason: :kill_switch_active, message: ...,
    route_id: route.id, details: %{"companion_id" => Atom.to_string(companion.companion_id())})`
    (D-08) and STOP without calling any route_gated?/2 (D-10). If no kill switch fires, run the
    gate check: iterate enabled companions, wrap each `companion.route_gated?(route, target)` in
    `:telemetry.span([:crosswake, :companion, :route_gate], ...)`; first `{:deny, finding}` →
    build `Denial.new(reason: :gate_denied, message: ..., route_id: route.id, details: %{"flag_key" => Atom.to_string(route.gated_by), "reason" => "DISABLED", "variant" => "off", "evaluated_at" => DateTime.utc_now() |> DateTime.to_iso8601()})`
    (D-03 through D-07) and STOP; `:pass` continues. Produce these as `Denial.t()` directly and
    prepend to the `denials` list — do NOT route them through `Compatibility.finding_to_denial/2`
    (flag_key/evaluated_at are RouteGate-scoped; RESEARCH anti-pattern + PATTERNS note 177).
    Replace `transition_for/2` with `transition_for/3` taking `(status, route, opts)` and update
    the call site in `evaluate/4` to `transition_for(status, route, opts)`. Clause order MUST be:
    `(:allow, _, _) -> :activate`; then `(:deny, %RouteEntry{on_unavailable: {:fallback_phoenix, id}}, _) -> {:redirect, id}`
    (this specific clause FIRST among deny clauses — RESEARCH Pitfall 5); then the general
    `(:deny, _route, opts)` clause preserving existing :stay_put (in_app_navigation) / :halt
    behavior; include a `(:deny, nil, opts)` path equivalent to the general deny (nil route has
    no on_unavailable). Use `"DISABLED"`/`"off"` as STRINGS not atoms (RESEARCH anti-pattern);
    `evaluated_at` is a pre-serialized ISO8601 STRING not a DateTime struct (D-04, RESEARCH anti-pattern).
  </action>
  <verify>
    <automated>mix test test/crosswake/proof/phase40_gate_evaluation_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "{:redirect, atom()}" lib/crosswake/compatibility/route_gate.ex` returns >= 1 (Decision typespec broadened).
    - `grep -q "prepend_gate_evaluation_findings\|gate_evaluation_step" lib/crosswake/compatibility/route_gate.ex` succeeds.
    - `grep -q ":telemetry.span" lib/crosswake/compatibility/route_gate.ex` succeeds; both `[:crosswake, :companion, :kill_switch]` and `[:crosswake, :companion, :route_gate]` event names present.
    - `grep -q "transition_for(status, route, opts)" lib/crosswake/compatibility/route_gate.ex` (call site updated to arity 3).
    - `grep -q "finding_to_denial" lib/crosswake/compatibility/route_gate.ex` still present for non-gate findings, but gate/kill denials are built via `Denial.new(reason: :gate_denied` / `:kill_switch_active` (grep confirms both literals present).
    - `mix test test/crosswake/proof/phase40_gate_evaluation_test.exs` passes (GREEN — SC#1-4).
    - `mix test --exclude requires_example_host` passes (no regression in existing callers Activation / Doctor / compatibility_test).
  </acceptance_criteria>
  <done>SC#1-4 proof passes; `:gate_denied` and `:kill_switch_active` denials are produced with the locked detail shapes; transition reflects on_unavailable; full suite green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| host route config → RouteGate | Compiled manifest + `Application.get_env(:crosswake, :companions, [])` cross into the activation decision. All inputs are developer-controlled compile/config data, not end-user input. |
| companion callback → RouteGate | `route_gated?/2` / `kill_switch_active?/1` return values determine whether a gated route opens. A buggy/missing companion must never silently fail-open. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-40-01 | Elevation of Privilege | `prepend_gate_evaluation_findings/3` gate check | mitigate | Fail-closed AND-semantics: a route only activates when NO enabled companion denies and NO kill switch fires. A companion returning `:pass` does not open a route; it merely abstains. SC#1/SC#3 prove deny is produced. (ASVS V4 Access Control) |
| T-40-02 | Tampering | kill-switch ordering | mitigate | Kill-switch check runs and short-circuits BEFORE any gate check (D-10); no downstream logic can unblock a killed route. SC#2 asserts `route_gated?/2` is never reached when a kill switch fires. |
| T-40-03 | Information Disclosure | `Denial.details` OpenFeature map | accept | `flag_key`/`reason`/`variant`/`evaluated_at` are non-sensitive flag metadata surfaced for explainability by design (GATE-03). No secrets, no PII in the details map. |
| T-40-04 | Denial of Service | network call in decision path | mitigate | Evaluation is a pure function over compiled manifest + registered modules; SC#4 asserts no network dependency in the activation decision path (GATE-04 / ASVS V4). |
| T-40-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this phase (RESEARCH Package Legitimacy Audit: zero new dependencies). No supply-chain surface introduced. |
</threat_model>

<verification>
- `mix test test/crosswake/proof/phase40_gate_evaluation_test.exs` — SC#1-4 hermetic proof green (per-task sampling).
- `mix test --exclude requires_example_host` — full suite green; existing RouteGate callers (`Activation.resolve/2`, `Doctor`, `compatibility_test.exs`) unaffected by the additive typespec broadening.
- `mix compile --warnings-as-errors` — no Dialyzer/compile warnings from the new reason atoms or `transition_for/3` signature change.
- Manifest-only check (no network): SC#4 self-asserts hermeticity; the proof file contains no example-host router reference and no `Code.require_file`.
</verification>

<success_criteria>
- SC#1 (GATE-03): `:gate_denied` denial carries `flag_key`, `reason` ("DISABLED"), `variant` ("off"), `evaluated_at` (ISO8601 string) — all present and non-nil.
- SC#2 (GATE-04): kill switch produces `:kill_switch_active` and short-circuits — `route_gated?/2` never called.
- SC#3 (GATE-04): `on_unavailable: :deny` → `transition :halt`; `on_unavailable: {:fallback_phoenix, :home}` → `transition {:redirect, :home}`.
- SC#4 (GATE-04): no network call in the evaluation path — pure over manifest + registered companion modules.
- Non-gated routes skip gate/kill-switch evaluation entirely (D-11).
- `Shell.Denial.reasons/0` includes both new atoms; `Decision.transition` typespec includes `{:redirect, atom()}`.
</success_criteria>

<output>
Create `.planning/phases/40-runtime-gate-evaluation-and-fail-closed-denial/40-01-SUMMARY.md` when done.
</output>
