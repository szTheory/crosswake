---
context: phase
phase: 163.1-close-gap-v21-physical-adopter-composition
task: 2
total_tasks: 2
status: parked_external_dependency
last_updated: 2026-08-28T16:11:46.000Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

> These are not suggestions. Each constraint below was discovered through failure.
> Acknowledge each one explicitly before proceeding.

- [ ] CONSTRAINT: Source-bound adopter authority — an eligible handoff may come only from real,
  sanitized adopter route facts. Fixtures, defaults, guessed paths, reference-host rows, and
  synthetic values cannot satisfy `PI-PREFLIGHT-ADOPTER-HANDOFF`. The structural prevention is the
  exact `AdopterHandoff` → `RouteInventory` → `NavigationTopology` validation chain before any
  device or evidence side effect.

**Do not proceed until all boxes are checked.**

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Fabricated adopter promotion | The workflow reached a real external-input gate while the user requested full automation; substituting a fixture or guessed route would create false physical support truth. | blocking | Require a caller-supplied sanitized handoff to pass the exact validation chain and current-run readiness before device/evidence callbacks. |
| Placeholder setup commands | An illustrative `cp /path/to/...` command was presented as executable and failed because no source export existed. | advisory | Do not present placeholder paths as runnable steps; use conventional discovery and report the missing source plainly. |
| Manual configuration for discoverable state | Requiring UDID and local-host URL environment values created avoidable setup confusion. | advisory | Auto-detect exactly one eligible device and manage the local LAN host; retain environment variables only as explicit overrides. |

<current_state>
Phase 163.1 is parked in Plan 08, Task 2. Plans 01-07 are complete. Plan 08 Task 1 is
complete, and its automation-first preflight deviation is committed. Fresh readiness without
`.env.local` reports every prerequisite ready except `PI-PREFLIGHT-ADOPTER-HANDOFF`. No physical
transaction ran, no evidence was promoted, and the fixed evidence destination remains absent.
The separate v22 quality-ratchet workstream may proceed without completing, archiving, or
support-promoting this milestone.
</current_state>

<completed_work>

Completed phase plans:
- 163.1-01 through 163.1-07 — complete with committed summaries.

Plan 163.1-08 completed work:
- Task 1: exact device/backend evidence transaction — commits `4768e67e`, `ae234ac6`,
  `8736683e`.
- Automation deviation: placeholder-safe single-device discovery, local LAN host management,
  conventional private handoff lookup, deterministic cleanup, and focused tests — commit
  `01e0cc68`.
- Empty plan-owned evidence parent prepared without creating the evidence destination — commit
  `ccab7559`.
</completed_work>

<remaining_work>

- Plan 163.1-08 Task 2: validate real private handoff, rerun readiness, execute exactly one
  source-bound physical transaction, verify evidence, run full gates, and write aggregate-only
  `163.1-VALIDATION.md`.
- Plan 163.1-09: refresh Nyquist validations and independently rerun the v21 milestone audit.
- Plan 163.1-10: reconcile final requirement, policy, capability, support, and guide truth.
- Phase-level code review, regression gate, independent verification, and completion remain
  pending after all plans finish.
</remaining_work>

<decisions_made>

- Convention-over-configuration is authoritative for the physical proof setup: discover exactly
  one connected iPhone and manage the local host automatically; environment variables are optional
  overrides for ambiguity, remote hosts, or CI.
- The private handoff default is `~/.config/crosswake/first-adopter-handoff.json`; an explicit
  `CROSSWAKE_FIRST_ADOPTER_HANDOFF_PATH` may override it.
- No eligible handoff may be created, inferred, or promoted from repository fixtures, defaults,
  guessed product facts, or reference-host data.
- If real adopter route facts do not exist, Crosswake pauses here under the governing ADR.
</decisions_made>

<blockers>
- `PI-PREFLIGHT-ADOPTER-HANDOFF`: a genuinely validated sanitized adopter route handoff is absent.
  There is no technical workaround and no additional device/host setup is required from the user.
</blockers>

## Required Reading (in order)

1. `AGENTS.md` — project privacy, route-ownership, automation, and stop rules.
2. `.planning/ADR-FIRST-B2C-ADOPTER.md` — governing stop condition and non-goals.
3. `.planning/FIRST-B2C-ADOPTER-ROUTE-POLICY-MAP.md` — concrete-route input and promotion rules.
4. `.planning/todos/TODO-002-first-b2c-adopter-route-inputs.md` — external input still missing.
5. `.planning/phases/163.1-close-gap-v21-physical-adopter-composition/163.1-08-PLAN.md` — current task contract.
6. `.planning/HANDOFF.json` — machine-readable current state.

## Critical Anti-Patterns (do NOT repeat these)

- FABRICATED-HANDOFF: never turn reference fixtures or recommended defaults into adopter authority
  merely to make readiness green. The exact external handoff validation must gate all side effects.
- PLACEHOLDER-AS-INSTRUCTION: never give a placeholder filesystem path as if it were an existing
  copy source. Use an explicit missing-source state.

## Infrastructure State

- No Phoenix server, watcher, Xcode run, or other background process remains active.
- Exactly one physical iPhone and signing were ready during the last automated preflight, but must
  be rediscovered on resume; no identifier is retained here.
- Local host binding and device-reachable URL derivation are automated; no private URL is retained.
- `.env.local` is ignored and not needed for the default device/host path.
- No `.planning/async-jobs/*.json` jobs exist.
- Existing unrelated dirty/untracked files are listed in `.planning/HANDOFF.json`; preserve them.

<context>
The code path is ready for the physical transaction, but the product-authority input is not. The
correct next move is not more framework implementation. Resume only when real sanitized route facts
exist; otherwise keep Crosswake paused. The retained support claim must remain blocked.
</context>

<next_action>
Start with: verify that `~/.config/crosswake/first-adopter-handoff.json` exists because real
sanitized adopter route facts became available. Demonstrate understanding of the blocking
anti-pattern, validate the handoff without echoing its contents, rerun readiness, and proceed only
if every check is ready and the evidence destination is absent. Otherwise stop immediately.
</next_action>
