---
phase: 158-adoption-reset-and-route-map
plan: "11"
type: execute
wave: 8
depends_on: [158-10]
files_modified:
  - lib/crosswake/adoption/route_inventory.ex
  - test/crosswake/adoption/route_inventory_test.exs
autonomous: true
gap_closure: true
requirements: [RESET-02, RESET-04]
must_haves:
  truths:
    - "A validated concrete-route row can retain only a mechanically opaque route reference and a path assembled from the closed non-identifying route-template grammar (D-05, D-08, D-09, D-10, RESET-02, RESET-04)."
    - "Customer-like, proprietary-taxonomy-like, record-instance, and descriptive route references fail closed with stable rule IDs and without echoing rejected input (D-09, D-10, RESET-04)."
    - "The coherent synthetic study-route fixture remains eligible while TODO-002 and adopter-instance completeness remain blocked (D-01, D-03)."
  artifacts:
    - path: "lib/crosswake/adoption/route_inventory.ex"
      provides: "Mechanical opacity and sanitized Phoenix route-template validation"
    - path: "test/crosswake/adoption/route_inventory_test.exs"
      provides: "Positive and negative route-reference boundary regressions"
  key_links:
    - from: "lib/crosswake/adoption/route_inventory.ex"
      to: "Crosswake.Adoption.RouteInventory.validate/1"
      via: "validate_route_id/1 and validate_path_pattern/2 execute before posture validation and struct construction"
      pattern: "validate_route_id|validate_path_pattern"
---

<objective>
Close the bypass that lets identifying route references enter a validated durable inventory.

Purpose: Make the D-05/D-08/D-09 opaque-reference boundary mechanical while retaining one honest synthetic route-template path through validation and promotion.
Output: Tightened route-reference validators and discriminating non-echoing regressions.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/158-adoption-reset-and-route-map/158-CONTEXT.md
@.planning/phases/158-adoption-reset-and-route-map/158-VERIFICATION.md
@.planning/phases/158-adoption-reset-and-route-map/158-05-SUMMARY.md
@lib/crosswake/adoption/route_inventory.ex
@test/crosswake/adoption/route_inventory_test.exs

<interfaces>
`Crosswake.Adoption.RouteInventory.validate/1` returns `{:ok, %RouteInventory{}}` or `{:error, %ValidationError{rule_id:, route_ref:, field:}}`. Preserve `validate!/1`, `validate_inventory/1`, `promotion_status/1`, the closed posture vocabulary, route invariants, declaration order, and collision behavior. Error messages expose only rule ID, safe route reference or `unresolved`, field, and remediation.
</interfaces>
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Carry one opaque sanitized route through validation and reject descriptive references</name>
  <reversibility rating="costly">This tightens the observable `validate/1` input contract, so undoing it would re-admit durable identifying references and require coordinated fixture changes.</reversibility>
  <read_first>
    - AGENTS.md
    - .planning/phases/158-adoption-reset-and-route-map/158-CONTEXT.md
    - .planning/phases/158-adoption-reset-and-route-map/158-VERIFICATION.md
    - lib/crosswake/adoption/route_inventory.ex
    - test/crosswake/adoption/route_inventory_test.exs
  </read_first>
  <files>lib/crosswake/adoption/route_inventory.ex, test/crosswake/adoption/route_inventory_test.exs</files>
  <behavior>
    - "A route ID shaped as `route-` followed by exactly 16 lowercase hexadecimal characters and `/study/session/:id` validates and retains its explicit route-local posture."
    - "A descriptive route ID, an ID with the wrong prefix/length/character class, or an input carrying a customer/taxonomy word returns `RI-OPAQUE_ROUTE_ID` for `route_id` without echoing the rejected value."
    - "A path accepts one or more static segments drawn only from `study`, `session`, `learning`, `path`, `dashboard`, `history`, `auth`, `settings`, and `billing`, plus the dynamic segment `:id`; root, empty segments, wildcard segments, file extensions, instance values, descriptive static segments, and descriptive parameter names return `RI-SANITIZED_PATH_PATTERN` without echo."
    - "Duplicate opaque IDs and sanitized paths still return the existing duplicate rule IDs; a coherent validated row still promotes."
  </behavior>
  <action>Start RED by replacing the existing descriptive fixture IDs with deterministic synthetic hexadecimal references and adding table-driven positive and negative tests around the exact grammar above. Include the verifier's bypass shape only as a synthetic non-identifying construction and assert that neither the rejected ID nor path appears in `Exception.message/1`. Then replace the broad regexes in `validate_route_id/1` and `validate_path_pattern/2` with named module-level grammar constants/helpers: route IDs are `route-` plus exactly 16 lowercase hex characters; path patterns are slash-delimited tokens where static tokens belong to the closed generic surface allowlist and the only dynamic token is `:id`. Return `RI-OPAQUE_ROUTE_ID` and `RI-SANITIZED_PATH_PATTERN` respectively, keep the safe route reference `unresolved` whenever the ID itself is invalid, and preserve every existing D-06/D-07 posture, invariant, collision, ordering, and promotion behavior. Do not add a concrete adopter row, infer a host route, admit arbitrary slugs, or broaden the durable schema per D-01, D-03, D-05, D-08, D-09, and D-10.</action>
  <acceptance_criteria>
    - The exact opaque-ID and sanitized-path grammar is encoded once in production and exercised by positive and negative tables.
    - Descriptive/customer-like/taxonomy-like references, record instances, arbitrary parameters, malformed routes, and non-allowlisted static segments are rejected before struct construction.
    - Rejection output contains only the stable rule ID, safe route reference, field, and remediation; supplied rejected content is absent.
    - Existing posture, invariant, collision, empty-inventory, ordering, and promotion regressions remain green using synthetic opaque fixtures.
    - TODO-002 remains open and no adopter-instance route fact is added.
  </acceptance_criteria>
  <verify>
    <automated>mix test test/crosswake/adoption/route_inventory_test.exs &amp;&amp; mix format --check-formatted lib/crosswake/adoption/route_inventory.ex test/crosswake/adoption/route_inventory_test.exs</automated>
  </verify>
  <done>Only mechanically opaque IDs and closed, non-identifying Phoenix path templates can reach a validated route row.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| caller input → durable route struct | Untrusted route references can otherwise persist customer or proprietary facts. |
| rejected input → diagnostics | Validation failures must identify the rule and field without reflecting supplied content. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- | --- |
| T-158-G11-01 | Information Disclosure | `validate_route_id/1` | high | mitigate | Enforce a fixed hexadecimal opaque-ID grammar before struct construction and test non-echoing rejection. |
| T-158-G11-02 | Information Disclosure | `validate_path_pattern/2` | high | mitigate | Parse slash-delimited tokens against a closed generic allowlist and one opaque parameter token. |
| T-158-G11-03 | Tampering | route-reference collision checks | medium | mitigate | Retain duplicate ID/path regression coverage after fixture migration. |
| T-158-G11-04 | Elevation of Privilege | `promotion_status/1` | high | mitigate | Only rows that passed the tightened reference boundary and existing posture invariants can promote. |
</threat_model>

<verification>
- Focused route-inventory tests prove one production-quality synthetic happy path and all required negative boundary cases.
- The focused formatter check covers both changed Elixir files.
- No compile or full-suite rerun occurs here; the final reconciliation plan runs those only after every implementation gate passes.
</verification>

<success_criteria>
- The verifier's arbitrary human-readable route-reference bypass is closed.
- Validated errors never echo rejected route-reference content.
- Existing fail-closed route-local safety and promotion semantics remain unchanged.
</success_criteria>

## Source Coverage Audit

| SOURCE | ID | Gap / decision | Plan | Status | Notes |
| --- | --- | --- | --- | --- | --- |
| GOAL | — | Honest sanitized route classification | 158-11 | COVERED | Closes the durable route-reference bypass only. |
| REQ | RESET-02 | Explicit route posture on sanitized route patterns | 158-11 | COVERED | One closed path grammar reaches the existing posture validator. |
| REQ | RESET-04 | No identifying/personal information in planning artifacts | 158-11 | COVERED | Descriptive references cannot persist. |
| RESEARCH | — | Closed validation before durable output | 158-11 | COVERED | Uses the established RouteInventory validation boundary. |
| CONTEXT | D-01, D-03 | Policy completeness without fabricated adopter rows | 158-11 | COVERED | Synthetic fixture only; TODO-002 stays open. |
| CONTEXT | D-05, D-08, D-09, D-10 | Opaque ID, sanitized pattern, forbidden content, calm errors | 158-11 | COVERED | Mechanical grammar and non-echoing tests implement the locked boundary. |

## Artifacts this phase produces

- Stable validation rule IDs `RI-OPAQUE_ROUTE_ID` and `RI-SANITIZED_PATH_PATTERN`.
- A closed generic static-segment allowlist and the single `:id` dynamic route-template token.
- Synthetic opacity/path negative regression tables in `route_inventory_test.exs`.

<output>
Create `.planning/phases/158-adoption-reset-and-route-map/158-11-SUMMARY.md` when done.
</output>
