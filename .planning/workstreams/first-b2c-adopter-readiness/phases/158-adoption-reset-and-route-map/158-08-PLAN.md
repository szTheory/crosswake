---
phase: 158-adoption-reset-and-route-map
plan: "08"
type: execute
wave: 6
depends_on: [158-07]
files_modified:
  - .github/workflows/hex-page-proof.yml
  - test/mix/tasks/crosswake_adoption_context_scan_test.exs
autonomous: true
gap_closure: true
requirements: [RESET-04]
must_haves:
  truths:
    - "Trusted same-repository pull requests run the secret-backed private-term scan before merge, while the secret remains unavailable to fork code (D-16, D-17, RESET-04)."
    - "Fork-originated pull requests cannot satisfy the protected privacy check through the untrusted pull-request workflow; the merge gate fails closed with stable remediation that requires a trusted maintainer path (D-17)."
    - "Pushes to main and manual protected runs still require secret input and fail closed when it is absent (D-17)."
  artifacts:
    - path: ".github/workflows/hex-page-proof.yml"
      provides: "Event-provenance-aware protected privacy merge gate"
    - path: "test/mix/tasks/crosswake_adoption_context_scan_test.exs"
      provides: "Workflow trust-boundary and non-echoing protected-scan regressions"
  key_links:
    - from: ".github/workflows/hex-page-proof.yml"
      to: "lib/mix/tasks/crosswake.adoption_context.scan.ex"
      via: "trusted PR/main/manual events invoke `mix crosswake.adoption_context.scan --require-private-terms` with repository secret input"
      pattern: "--require-private-terms"
  flagged_assumptions:
    - "[FLAGGED/UNVERIFIED] The spec-less RESET-04 edge prompt is generic; this plan treats event provenance, secret non-exposure, non-echoing output, and fail-closed fork merge behavior as its only applicable CI obligations and invents no adopter facts."
  prohibitions:
    - statement: "MUST NOT expose repository secrets to fork-originated pull-request code or use pull_request_target to execute untrusted checkout content."
      category: privacy
      verification: flagged-unverified
    - statement: "MUST NOT print, persist, upload, or embed protected terms, matched lines, or surrounding content in logs or artifacts."
      category: privacy
      verification: flagged-unverified
    - statement: "MUST NOT search git history or external sources to identify the adopter."
      category: values
      verification: flagged-unverified
    - statement: "MUST NOT add Android features, parity work, device proof, or release scope."
      category: scope
      verification: flagged-unverified
---

<objective>
Close the pull-request trust-boundary gap in protected private-term enforcement.

Purpose: Ensure prohibited adopter identity cannot merge through a trusted PR while fork code never receives the protected term list and cannot pass without a trusted maintainer-controlled check.
Output: A provenance-aware merge-blocking workflow and focused structural regressions.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/158-adoption-reset-and-route-map/158-CONTEXT.md
@.planning/phases/158-adoption-reset-and-route-map/158-VERIFICATION.md
@.planning/phases/158-adoption-reset-and-route-map/158-06-SUMMARY.md
@.github/workflows/hex-page-proof.yml
@lib/mix/tasks/crosswake.adoption_context.scan.ex
@test/mix/tasks/crosswake_adoption_context_scan_test.exs

<interfaces>
Preserve `mix crosswake.adoption_context.scan --require-private-terms` and its `CROSSWAKE_PRIVATE_ADOPTER_TERMS` input. The workflow must distinguish a same-repository PR by comparing the head repository identity with `github.repository`; fork code receives no secret and encounters an explicit failing protected-check path.
</interfaces>
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Enforce one protected PR path end to end without crossing the fork boundary</name>
  <read_first>
    - AGENTS.md
    - .planning/phases/158-adoption-reset-and-route-map/158-CONTEXT.md
    - .planning/phases/158-adoption-reset-and-route-map/158-VERIFICATION.md
    - .planning/phases/158-adoption-reset-and-route-map/158-06-SUMMARY.md
    - .github/workflows/hex-page-proof.yml
    - lib/mix/tasks/crosswake.adoption_context.scan.ex
    - test/mix/tasks/crosswake_adoption_context_scan_test.exs
  </read_first>
  <files>.github/workflows/hex-page-proof.yml, test/mix/tasks/crosswake_adoption_context_scan_test.exs</files>
  <behavior>
    - "The workflow's protected scan runs for same-repository pull requests, pushes to main, and manual dispatches with the secret-backed environment input."
    - "The protected scan is skipped before secret injection for fork-originated PRs, and a separate merge-blocking step fails that fork path with a stable maintainer-controlled remediation."
    - "No pull_request_target event or untrusted fork checkout receives the protected environment value."
    - "The Mix task continues to fail closed on required-but-missing input and reports only stable rule/path output."
  </behavior>
  <action>Start RED by extending the focused Mix-task test file with structural assertions over the checked-in workflow: trusted same-repository PR provenance must activate the protected scan, fork provenance must activate a separate failing step, the secret-backed environment is present only on the trusted protected step, and `pull_request_target` is absent. Then update `.github/workflows/hex-page-proof.yml` per D-16 and D-17. Keep the always-on generic scan for every PR. Gate the protected scan for main/manual events and same-repository PRs using GitHub event metadata. Add an explicit fork-only step that exits nonzero with calm remediation requiring a trusted maintainer-controlled branch or merge-queue check; do not interpolate or inspect protected values there. Preserve the existing missing-secret failure from the Mix task, stable non-echoing diagnostics, hermetic job posture, and all unrelated workflow steps. This is a reversible workflow arrangement and introduces no external dependency or adopter-instance data.</action>
  <acceptance_criteria>
    - Same-repository PRs cannot reach merge without the required protected scan.
    - Fork PR code never receives the repository secret and the protected gate remains unsatisfied until a trusted maintainer path runs it.
    - Main pushes and manual runs retain fail-closed secret enforcement.
    - Structural tests prevent regression to PR-wide skipping or unsafe target-context execution.
  </acceptance_criteria>
  <verify>
    <automated>mix test test/mix/tasks/crosswake_adoption_context_scan_test.exs &amp;&amp; mix crosswake.adoption_context.scan &amp;&amp; mix format --check-formatted test/mix/tasks/crosswake_adoption_context_scan_test.exs</automated>
  </verify>
  <done>The merge-blocking workflow enforces protected terms on trusted PRs and blocks fork bypass without exposing secrets.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| fork PR code → GitHub Actions | Untrusted repository content must not receive protected repository secrets. |
| trusted PR/main/manual event → protected scanner | Maintainer-controlled code receives an in-memory term list and must fail closed if unavailable. |
| scanner → CI logs | Violations may disclose only stable rule IDs and repository-relative paths. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- | --- |
| T-158-G08-01 | Information Disclosure | `.github/workflows/hex-page-proof.yml` fork execution | high | mitigate | Restrict secret injection and protected execution to main/manual or same-repository PR provenance; structurally reject `pull_request_target`. |
| T-158-G08-02 | Spoofing | PR repository provenance condition | high | mitigate | Compare the pull request head repository identity to `github.repository` and cover the exact workflow expression with a regression. |
| T-158-G08-03 | Elevation of Privilege | fork PR merge status | high | mitigate | Add an explicit failing fork-only protected-check step requiring a trusted maintainer-controlled rerun path. |
| T-158-G08-04 | Information Disclosure | Mix task failure output | high | mitigate | Retain rule/path-only output tests and never expose terms or matched content. |
| T-158-G08-05 | Denial of Service | fork contribution flow | medium | accept | Fork PRs require maintainer promotion before merge; this operational cost is the selected fail-closed privacy posture. |
</threat_model>

<verification>
- `mix test test/mix/tasks/crosswake_adoption_context_scan_test.exs` proves the workflow trust boundary and Mix-task failure contract.
- `mix crosswake.adoption_context.scan` proves the repository's generic approved-artifact gate remains green.
- Review the workflow diff to confirm secret interpolation exists only on the trusted protected step and no target-context PR trigger was introduced.
</verification>

<success_criteria>
- Trusted same-repository PRs receive secret-backed enforcement before merge.
- Fork PRs remain secret-free and fail closed pending a trusted maintainer-controlled check.
- Protected scan failures remain non-echoing and actionable.
</success_criteria>

## Source Coverage Audit

| SOURCE | ID | Gap / decision | Plan | Status | Notes |
| --- | --- | --- | --- | --- | --- |
| GOAL | — | Privacy-safe context routing blocks prohibited identity before promotion | 158-08 | COVERED | Enforces the protected scanner at the PR boundary. |
| REQ | RESET-04 | No prohibited identity or personal information in planning/public artifacts | 158-08 | COVERED | Trusted PR enforcement plus fail-closed fork handling. |
| RESEARCH | — | Secret-backed terms remain in memory and failures disclose only rule/path | 158-08 | COVERED | Existing scanner contract is preserved and retested. |
| CONTEXT | D-16 | Always-on CI plus truthful synthetic/private enforcement | 158-08 | COVERED | Generic scan remains universal. |
| CONTEXT | D-17 | Privileged scan, no secret exposure to forks, protected validation before promotion | 158-08 | COVERED | Event provenance and fork blocker close the verifier gap. |

## Artifacts this phase produces

- No new runtime symbol, path, field, or flag. This plan modifies the existing workflow conditions and focused test coverage only.

<output>
Create `.planning/phases/158-adoption-reset-and-route-map/158-08-SUMMARY.md` when done.
</output>
