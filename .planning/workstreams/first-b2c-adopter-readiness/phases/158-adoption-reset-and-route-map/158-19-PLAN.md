---
phase: 158-adoption-reset-and-route-map
plan: "19"
type: execute
wave: 15
depends_on: [158-18]
files_modified:
  - lib/crosswake/planning/first_adopter_context.ex
  - test/crosswake/planning/first_adopter_context_test.exs
  - test/mix/tasks/crosswake_adoption_context_scan_test.exs
  - lib/crosswake/adoption/route_inventory.ex
  - test/crosswake/adoption/route_inventory_test.exs
autonomous: true
gap_closure: true
requirements: [RESET-02, RESET-04]
must_haves:
  truths:
    - "Every repository candidate classified as `scan?: true` receives generic personal/commercial privacy checks, independent of exact-path registration or destination-specific policy (D-14, D-15, D-16, RESET-04)."
    - "An unregistered guide, source, action, script, and later-phase planning artifact each fail through both `scan_filesystem/2` and the production Mix task when they contain a generic privacy violation and no configured private-term input (D-16, D-17, RESET-04)."
    - "Destination-specific public wording checks remain scoped to the named public destination while generic privacy rules cover all recognized textual artifacts (D-14, D-16)."
    - "`RouteInventory.validate/1` accepts map input only after proving every map key is an atom; malformed key/value pairs return a stable `%ValidationError{}` rather than reaching a Keyword API (D-10, RESET-02)."
    - "Malformed route-map failures expose only a stable rule, `unresolved` route reference, safe field reference, and remediation; neither an untrusted key nor its value appears in `Exception.message/1` (D-09, D-10, RESET-04)."
    - "TODO-002 remains open and concrete adopter-instance completeness remains `unknown_blocking` (D-01, D-02, D-03)."
  artifacts:
    - path: "lib/crosswake/planning/first_adopter_context.ex"
      provides: "Scan-by-default generic privacy enforcement with destination-specific checks kept separate"
    - path: "test/crosswake/planning/first_adopter_context_test.exs"
      provides: "Direct scanner regressions for generic violations across five unregistered textual path classes"
    - path: "test/mix/tasks/crosswake_adoption_context_scan_test.exs"
      provides: "Production Mix-task regressions for the same generic privacy path classes"
    - path: "lib/crosswake/adoption/route_inventory.ex"
      provides: "Fail-closed pre-Keyword validation for map keys"
    - path: "test/crosswake/adoption/route_inventory_test.exs"
      provides: "Non-atom key/value crash and non-echo regressions"
  key_links:
    - from: "Git-backed repository classification"
      to: "generic_violations/2"
      via: "Every `scan?: true` entry is generically checked before destination-specific checks"
      pattern: "scan\?: true|generic_violations"
    - from: "test/mix/tasks/crosswake_adoption_context_scan_test.exs"
      to: "Mix.Tasks.Crosswake.AdoptionContext.Scan.run/1"
      via: "Temporary Git repositories exercise the production `scan_filesystem/2` caller with no private terms configured"
      pattern: "Scan.run|privacy.commercial_detail|privacy.identifying_field"
    - from: "RouteInventory.validate/1"
      to: "Keyword.keys/1"
      via: "Map normalization rejects any non-atom key before producing a keyword list"
      pattern: "normalize_input|is_atom"
  prohibitions:
    - statement: "MUST NOT record or infer adopter identity, founder, price, geography, customer data, proprietary taxonomy, or revealing links."
      category: privacy
      verification: flagged-unverified
    - statement: "MUST NOT echo configured terms, matched text, malformed keys, malformed values, or sensitive route payloads in diagnostics."
      category: privacy
      verification: flagged-unverified
    - statement: "MUST NOT add Android work, UI, dashboards, generic sync/storage, API coverage, schema changes, or later-phase proof claims."
      category: scope
      verification: flagged-unverified
flagged_assumptions:
  - "[FLAGGED/UNVERIFIED] With no phase SPEC, the relevant RESET-04 backstop is that every recognized `scan?: true` textual artifact receives generic privacy checks; raw/binary exclusions and destination-specific public wording remain governed by their existing explicit classifiers."
  - "[FLAGGED/UNVERIFIED] With no phase SPEC, the relevant RESET-02 backstop is that arbitrary map keys are untrusted boundary input and must fail through the same stable non-echoing validator contract as other malformed route rows."
---

<objective>
Close the two verifier-demonstrated production gaps: repository-wide generic privacy enforcement and
fail-closed route-map normalization.

Purpose: Restore the scanner and route inventory contracts without widening Phase 158 or changing
the route-local ownership model.
Output: Corrected internal scanner/validator behavior plus direct and production regressions.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/ADR-FIRST-B2C-ADOPTER.md
@.planning/FIRST-B2C-ADOPTER-ADOPTION-BRIEF.md
@.planning/FIRST-B2C-ADOPTER-ROUTE-POLICY-MAP.md
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/158-adoption-reset-and-route-map/158-CONTEXT.md
@.planning/phases/158-adoption-reset-and-route-map/158-RESEARCH.md
@.planning/phases/158-adoption-reset-and-route-map/158-VERIFICATION.md
@.planning/phases/158-adoption-reset-and-route-map/158-17-SUMMARY.md
@.planning/phases/158-adoption-reset-and-route-map/158-18-SUMMARY.md
@lib/crosswake/planning/first_adopter_context.ex
@test/crosswake/planning/first_adopter_context_test.exs
@lib/mix/tasks/crosswake.adoption_context.scan.ex
@test/mix/tasks/crosswake_adoption_context_scan_test.exs
@lib/crosswake/adoption/route_inventory.ex
@test/crosswake/adoption/route_inventory_test.exs

<interfaces>
Preserve `Crosswake.Planning.FirstAdopterContext.scan/1`, `scan_private_terms/2`,
`scan_filesystem/2`, `routing_matrix/0`, `active_paths/0`, `artifact_globs/0`, and
`discover_paths/1`. Preserve `mix crosswake.adoption_context.scan [--root PATH]
[--require-private-terms]`. Preserve `Crosswake.Adoption.RouteInventory.validate/1` and its
`{:ok, row} | {:error, %ValidationError{}}` return contract. No new public API is required.
</interfaces>
</context>

<assumption_delta_decision>
Primary noun: route-local runtime owner/authority contract.
Decision: no-change.
Rationale: The roadmap's fallback wording describes outcomes owned by the existing route-local
contract; this plan repairs scanner and validator correctness without pluralizing, replacing, or
adding an identity model.
</assumption_delta_decision>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Trace generic privacy violations through every textual path class and the production Mix task</name>
  <read_first>
    - AGENTS.md
    - .planning/phases/158-adoption-reset-and-route-map/158-CONTEXT.md
    - .planning/phases/158-adoption-reset-and-route-map/158-VERIFICATION.md
    - .planning/phases/158-adoption-reset-and-route-map/158-17-SUMMARY.md
    - lib/crosswake/planning/first_adopter_context.ex
    - test/crosswake/planning/first_adopter_context_test.exs
    - lib/mix/tasks/crosswake.adoption_context.scan.ex
    - test/mix/tasks/crosswake_adoption_context_scan_test.exs
  </read_first>
  <files>lib/crosswake/planning/first_adopter_context.ex, test/crosswake/planning/first_adopter_context_test.exs, test/mix/tasks/crosswake_adoption_context_scan_test.exs</files>
  <behavior>
    - "With an empty private-term list, a generic commercial-detail or identifying-field pattern in each of an unregistered guide, Elixir source, action, script, and arbitrary later-phase planning file returns its stable generic privacy rule and relative path from `scan_filesystem/2`."
    - "The same five path classes fail through `Mix.Tasks.Crosswake.AdoptionContext.Scan.run/1`, proving production uses the corrected scanner rather than a test-only seam."
    - "Violations contain sorted rule/path pairs only and omit the matched value and surrounding content."
    - "Public phrase/codename checks still apply only to named public destinations; private-term checks still apply to every scanned textual candidate."
  </behavior>
  <action>Start RED by extending the existing temporary-Git-repository tests in both scanner test files. Build generic-rule canaries from neutral fragments inside the test process so checked-in text does not itself trigger the production scan. Cover exactly five recognized-but-unregistered text classes: `guides/`, `lib/`, `.github/actions/`, `script/`, and an arbitrary numeric later-phase directory. Invoke `scan_filesystem(root, [])` directly and the real `Scan.run(["--root", root])`; assert stable sorted generic rule/path results and refute matched values and surrounding content in returned or raised messages. Then separate generic privacy evaluation from destination policy in `FirstAdopterContext`: every classified entry with `scan?: true` must call `generic_violations/2`, while `destination_violations/3` remains keyed only by destination. Apply the same split in `scan/1` and filesystem scanning so the in-memory and production seams cannot diverge. Remove or narrow `policy_scan?` only as needed to eliminate its role as a generic-check gate; preserve repository enumeration, explicit raw/binary exclusions, fail-closed unknown candidates, private-term handling, public destination classification, deterministic sorting, and non-echoing diagnostics per D-14 through D-20. Do not change the Mix task unless a caller defect is demonstrated.</action>
  <verify>
    <automated>mix test test/crosswake/planning/first_adopter_context_test.exs test/mix/tasks/crosswake_adoption_context_scan_test.exs &amp;&amp; mix crosswake.adoption_context.scan &amp;&amp; mix format --check-formatted lib/crosswake/planning/first_adopter_context.ex test/crosswake/planning/first_adopter_context_test.exs test/mix/tasks/crosswake_adoption_context_scan_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - Direct scanner tests reject generic violations in all five unregistered path classes with an empty configured-term list.
    - Production Mix-task tests reject the same five path classes through `Scan.run/1`.
    - Rule/path diagnostics contain neither the matched value nor surrounding file content.
    - Existing public phrase, protected private-term, raw/binary exclusion, unclassified-path, enumeration-failure, and clean-live-repository cases remain green.
    - No external scanner, dependency, workflow change, API, schema, UI, or adopter-specific fact is introduced.
  </acceptance_criteria>
  <done>One generic violation crosses Git discovery, textual classification, file read, generic privacy evaluation, and the production Mix failure boundary for every demonstrated unregistered path class.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Reject non-atom route-map keys before Keyword normalization</name>
  <read_first>
    - AGENTS.md
    - .planning/FIRST-B2C-ADOPTER-ROUTE-POLICY-MAP.md
    - .planning/phases/158-adoption-reset-and-route-map/158-CONTEXT.md
    - .planning/phases/158-adoption-reset-and-route-map/158-VERIFICATION.md
    - lib/crosswake/adoption/route_inventory.ex
    - test/crosswake/adoption/route_inventory_test.exs
  </read_first>
  <files>lib/crosswake/adoption/route_inventory.ex, test/crosswake/adoption/route_inventory_test.exs</files>
  <behavior>
    - "A map containing any non-atom key returns `{:error, %RouteInventory.ValidationError{}}` and never raises `ArgumentError`."
    - "String, numeric, tuple, and mixed atom/non-atom key cases use one stable invalid-row rule, `unresolved` route reference, and safe field reference."
    - "Neither the rejected key representation nor its associated scalar/structured value appears in `Exception.message/1`."
    - "Atom-key maps and keyword lists continue through the existing closed-field, forbidden-field, required-field, posture, and promotion validation paths unchanged."
  </behavior>
  <action>Start RED in `route_inventory_test.exs` with table-driven malformed map cases whose keys and associated values are unique runtime-assembled canaries. Call `RouteInventory.validate/1` without an `assert_raise`, require a `%RouteInventory.ValidationError{}`, and assert `Exception.message/1` contains the stable invalid-row contract but neither key nor value. Include a mixed map containing otherwise valid atom-key route data plus one non-atom key so the regression proves pre-Keyword validation, not merely rejection of a one-entry map. In `normalize_input/1`, inspect map keys before `Map.to_list/1` is allowed to feed `Keyword.keys/1`, `Keyword.has_key?/2`, or other Keyword functions. If every key is an atom, preserve the existing map behavior. Otherwise return a stable non-echoing `RI-INVALID` error with `unresolved` route reference and the safe `route_row` field. Keep forbidden atom keys on their existing `RI-FORBIDDEN_FIELD` path, unknown atom keys on `RI-UNKNOWN_FIELD`, and all D-05 through D-10 route/status/promotion semantics unchanged.</action>
  <verify>
    <automated>mix test test/crosswake/adoption/route_inventory_test.exs &amp;&amp; mix format --check-formatted lib/crosswake/adoption/route_inventory.ex test/crosswake/adoption/route_inventory_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - Every non-atom and mixed-key case returns a stable `%ValidationError{}` without raising.
    - `Exception.message/1` contains `RI-INVALID`, `route unresolved`, `field route_row`, and remediation, but contains neither rejected key nor value.
    - Existing valid atom-key map, keyword-list, forbidden atom-key, unknown atom-key, route invariant, and promotion tests remain green.
    - TODO-002 and the route-policy map remain unchanged; adopter-instance completeness stays `unknown_blocking`.
  </acceptance_criteria>
  <done>Arbitrary map input cannot reach Keyword APIs with a non-atom key and cannot escape or disclose through the documented validator error boundary.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| Recognized repository text -> privacy policy | Generic personal/commercial patterns must be evaluated regardless of registry or destination. |
| Scanner result -> Mix task/CI diagnostics | Failures may expose only stable rule IDs and relative paths. |
| Caller map -> RouteInventory keyword validation | Arbitrary map keys and values are untrusted and must fail before Keyword APIs. |
| Route validation error -> maintainer output | Malformed input must not be interpolated into exception text. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- | --- |
| T-158-G19-01 | Information Disclosure | `FirstAdopterContext` generic privacy flow | high | mitigate | Apply generic rules to every `scan?: true` entry and regress five unregistered path classes directly and through the Mix task. |
| T-158-G19-02 | Elevation of Privilege / Tampering | `policy_scan?` gate | high | mitigate | Decouple generic evaluation from destination-specific policy so exact-path metadata cannot create a bypass. |
| T-158-G19-03 | Information Disclosure | scanner diagnostics | high | mitigate | Runtime-assembled canaries plus exact rule/path assertions prevent matched content from entering returned or raised messages. |
| T-158-G19-04 | Denial of Service | `RouteInventory.normalize_input/1` | high | mitigate | Validate every map key is an atom before any Keyword function can execute. |
| T-158-G19-05 | Information Disclosure | `ValidationError.message/1` | high | mitigate | Use a fixed invalid-row rule/reference/field and assert both malformed key and value are absent from exception text. |
| T-158-G19-06 | Tampering | existing route ownership contract | medium | mitigate | Retain all atom-key validation and `unknown_blocking` promotion regressions unchanged. |

ASVS Level 1: every high-severity bypass, crash, and disclosure threat has a blocking automated mitigation.
</threat_model>

<verification>
- Run the focused scanner and production Mix-task suites with no private terms configured; all five generic path classes must fail in their negative fixtures and the live repository must pass.
- Run the focused route inventory suite; malformed map keys must return stable errors and all established ownership/promotion behavior must remain green.
- Run explicit formatter checks for all five changed Elixir files.
</verification>

<success_criteria>
- RESET-04 has no generic-policy bypass among recognized textual repository artifacts.
- RESET-02's public map-input contract fails closed for arbitrary keys without crashing or echoing input.
- Destination-specific policy, TODO-002, `unknown_blocking`, Android freeze, and later-phase scope are unchanged.
</success_criteria>

## Specless Probe Resolution

- RESET-04 relevant covered edge: every `scan?: true` textual artifact receives generic privacy checks; destination-specific checks stay scoped and explicit raw/binary exclusions remain intact.
- RESET-02 relevant covered edge: map input with arbitrary keys fails through a stable non-echoing `ValidationError` before Keyword processing.
- RESET-01 and RESET-03 unresolved heuristic prompts add no relevant semantics to these demonstrated gaps and remain covered by executed plans without reinterpretation.

## Multi-Source Coverage Audit

| Source | ID | Feature / requirement | Covering plans | Status | Notes |
| --- | --- | --- | --- | --- | --- |
| GOAL | — | Classify adopter routes and install privacy-safe context routing | 158-01 through 158-19 | COVERED | Plan 19 repairs only the verified scanner and validator boundaries. |
| REQ | RESET-01 | Durable infrastructure decision, scope audit, non-goals, and stop list | 158-01 through 158-18 | COVERED | Already satisfied; no new claim in Plan 19. |
| REQ | RESET-02 | Explicit route-local owner, authority, offline, fallback, and disablement contract | 158-01, 158-05, 158-11, 158-14, 158-19 | COVERED | Plan 19 makes the documented map-input error boundary fail closed. |
| REQ | RESET-03 | Honest v20 stopped/partial state | 158-03, 158-04, 158-07, 158-10, 158-14 | COVERED | Already satisfied; unchanged. |
| REQ | RESET-04 | No prohibited identity or personal/commercial information in planning/public artifacts | 158-03, 158-06, 158-08, 158-12, 158-15 through 158-19 | COVERED | Plan 19 closes the current generic-policy bypass and non-echoing map boundary. |
| RESEARCH | — | Centralized deterministic browser-free privacy scanning with stable non-echoing failures | 158-19 | COVERED | Existing module and Mix task are corrected; no dependency or service is added. |
| RESEARCH | — | Closed structured route validation modeled on established policy validation | 158-19 | COVERED | Pre-Keyword key validation preserves the existing validator shape. |
| CONTEXT | D-01-D-03 | Policy completeness stays separate from adopter-instance completeness; TODO-002 remains open | 158-19 | COVERED | No concrete route facts or promotion claim are added. |
| CONTEXT | D-05-D-10 | Closed sanitized route-row contract and non-echoing validation | 158-19 | COVERED | Non-atom keys now fail inside the stable contract. |
| CONTEXT | D-14-D-18 | Centralized routing, generic/private scans, and secret-safe diagnostics | 158-19 | COVERED | Generic evaluation reaches every scanned text candidate; destination rules remain scoped. |
| CONTEXT | D-19-D-20 | Calm maintainer errors and deterministic service-free proof | 158-19 | COVERED | Focused ExUnit and Mix-task tests prove exact rule/path behavior. |

No source item is missing. Deferred and later-phase proof/replay/pack/device work, Android, UI,
dashboard, API, schema, and generic sync/storage remain excluded by the governing phase boundary.

## Artifacts this phase produces

- None. This plan modifies existing scanner and route-inventory symbols plus their existing test files; it creates no new file, public symbol, API, schema, route, UI, dependency, or support claim.

<output>
Create `.planning/phases/158-adoption-reset-and-route-map/158-19-SUMMARY.md` when done.
</output>
