---
phase: 158-adoption-reset-and-route-map
plan: "17"
type: execute
wave: 13
depends_on: [158-16]
files_modified:
  - lib/crosswake/planning/first_adopter_context.ex
  - test/crosswake/planning/first_adopter_context_test.exs
  - test/mix/tasks/crosswake_adoption_context_scan_test.exs
autonomous: true
gap_closure: true
requirements: [RESET-04]
must_haves:
  truths:
    - "Every tracked or non-ignored repository candidate with a recognized textual extension is scanned by default, including files below `.github/actions/`, `script/`, and any future `.planning/phases/<phase>/` directory (D-14, D-15, D-16, RESET-04)."
    - "Only narrowly identified raw fixture/evidence material and binary content remain non-scanned; an unknown tracked candidate fails closed instead of disappearing from the protected scan (D-15, D-18, D-20)."
    - "Direct `scan_filesystem/2` tests and the production Mix-task tests independently prove the three former bypass classes reject a process-only private term (D-16, D-17, RESET-04)."
    - "Scanner and Mix-task failures expose only stable rule IDs and repository-relative paths; supplied terms, matched content, Git output, and adopter identity never enter diagnostics or artifacts (D-14, D-17, D-19)."
  artifacts:
    - path: "lib/crosswake/planning/first_adopter_context.ex"
      provides: "Scan-by-default tracked-text classification with narrow raw/binary exclusions"
      exports: ["routing_matrix/0", "active_paths/0", "artifact_globs/0", "discover_paths/1", "scan_filesystem/2"]
    - path: "test/crosswake/planning/first_adopter_context_test.exs"
      provides: "Direct scanner regressions for action, script, and unbounded future-phase candidates"
    - path: "test/mix/tasks/crosswake_adoption_context_scan_test.exs"
      provides: "Production Mix-task regressions for all three former bypass classes"
  key_links:
    - from: "lib/crosswake/planning/first_adopter_context.ex"
      to: "tracked and non-ignored Git candidates"
      via: "repository candidate enumeration feeds ordered text/binary/raw classification before content checks"
      pattern: "repository_candidates|classify_repository_path|scannable_text_path"
    - from: "test/crosswake/planning/first_adopter_context_test.exs"
      to: "Crosswake.Planning.FirstAdopterContext.scan_filesystem/2"
      via: "temporary Git repository paths exercise the direct production scanner boundary"
      pattern: "scan_filesystem|privacy.private_term"
    - from: "test/mix/tasks/crosswake_adoption_context_scan_test.exs"
      to: "Mix.Tasks.Crosswake.AdoptionContext.Scan.run/1"
      via: "the production task receives the same three Git-enumerated candidate classes"
      pattern: "Scan.run|privacy.private_term"
  prohibitions:
    - "Do not print, persist, upload, or return configured private terms, matched lines, surrounding content, or Git command output."
    - "Do not search git history, remotes, or external sources to infer the adopter identity."
    - "Do not add Android, UI, API, schema, sync, storage, proof-lane, replay, pack-provider, or physical-device scope."
    - "Do not alter the pre-existing `.planning/config.json` modification."
---

<objective>
Close the verified RESET-04 classification bypass for tracked textual action, script, and future
planning artifacts.

Purpose: Make protected private-term enforcement scan tracked/non-ignored repository text by
default while preserving only explicit raw/binary evidence exclusions and non-echoing failures.
Output: Corrected repository classification plus direct and production Mix-task regressions for
all three bypass classes.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/ADR-FIRST-B2C-ADOPTER.md
@.planning/FIRST-B2C-ADOPTER-ADOPTION-BRIEF.md
@.planning/FIRST-B2C-ADOPTER-ROUTE-POLICY-MAP.md
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/158-adoption-reset-and-route-map/158-CONTEXT.md
@.planning/phases/158-adoption-reset-and-route-map/158-RESEARCH.md
@.planning/phases/158-adoption-reset-and-route-map/158-PATTERNS.md
@.planning/phases/158-adoption-reset-and-route-map/158-VERIFICATION.md
@.planning/phases/158-adoption-reset-and-route-map/158-15-SUMMARY.md
@.planning/phases/158-adoption-reset-and-route-map/158-16-SUMMARY.md
@lib/crosswake/planning/first_adopter_context.ex
@lib/mix/tasks/crosswake.adoption_context.scan.ex
@test/crosswake/planning/first_adopter_context_test.exs
@test/mix/tasks/crosswake_adoption_context_scan_test.exs

<interfaces>
Preserve `Crosswake.Planning.FirstAdopterContext.scan/1`, `scan_private_terms/2`,
`scan_filesystem/2`, `routing_matrix/0`, `active_paths/0`, `artifact_globs/0`, and
`discover_paths/1`. Preserve `mix crosswake.adoption_context.scan [--root PATH]
[--require-private-terms]`. Scanner violations remain sorted `%{rule_id: binary(), path:
binary()}` maps, and Mix errors remain stable rule/path lines. No new public API is required.
</interfaces>
</context>

<assumption_delta_decision>
Primary noun: repository artifact path. Decision: no-change — the detector's existing
`authority/fallback story` phrase does not establish an identity-model transition. This plan
retains repository-relative paths as the diagnostic identity and changes only whether tracked
text reaches the existing scanner.
</assumption_delta_decision>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Trace all three excluded tracked-text classes through the direct scanner</name>
  <reversibility rating="costly">Changing the default classification can cause additional checked-in text to block protected CI, so reversal would require coordinated policy and regression changes.</reversibility>
  <read_first>
    - AGENTS.md
    - .planning/ADR-FIRST-B2C-ADOPTER.md
    - .planning/FIRST-B2C-ADOPTER-ADOPTION-BRIEF.md
    - .planning/FIRST-B2C-ADOPTER-ROUTE-POLICY-MAP.md
    - .planning/PROJECT.md
    - .planning/STATE.md
    - .planning/ROADMAP.md
    - .planning/REQUIREMENTS.md
    - .planning/phases/158-adoption-reset-and-route-map/158-CONTEXT.md
    - .planning/phases/158-adoption-reset-and-route-map/158-VERIFICATION.md
    - .planning/phases/158-adoption-reset-and-route-map/158-15-SUMMARY.md
    - .planning/phases/158-adoption-reset-and-route-map/158-16-SUMMARY.md
    - lib/crosswake/planning/first_adopter_context.ex
    - test/crosswake/planning/first_adopter_context_test.exs
  </read_first>
  <files>lib/crosswake/planning/first_adopter_context.ex, test/crosswake/planning/first_adopter_context_test.exs</files>
  <behavior>
    - "A tracked YAML action below `.github/actions/` containing a process-assembled private term yields exactly `privacy.private_term` plus its relative path."
    - "A tracked shell source below `script/` containing the same process-only term yields exactly the stable rule/path pair."
    - "A tracked Markdown file below a phase directory whose numeric prefix is outside 158-162 yields exactly the stable rule/path pair without a finite phase allowlist."
    - "The three paths appear in `discover_paths/1`, while an explicit raw fixture/evidence path and a binary extension remain outside the read-and-scan set."
    - "Unknown tracked candidates, unsafe paths, unreadable files, and repository enumeration failure continue to return stable routing violations."
    - "Neither the assembled term, matched content, nor Git output appears in returned values or assertion messages."
  </behavior>
  <action>Begin with failing table-driven tests in the existing temporary Git repository helper for one `.github/actions/` YAML file, one `script/` shell file, and one Markdown artifact below a phase directory beyond the current milestone range. Assemble the private term inside the test process from neutral fragments and assert exact sorted rule/path results plus `discover_paths/1` inclusion without embedding or echoing the complete value. Then revise the ordered path classifier per D-14 through D-20: named destinations keep their specialized destination; any safe `.planning/phases/&lt;phase&gt;/...` textual artifact is future-safe durable scan input without enumerating phase numbers; a recognized textual extension is scanned regardless of repository subtree; only narrowly named raw fixture/evidence trees and known binary extensions are excluded from content reads. Remove the action and script exclusions and the finite phase matcher. Ensure broad directory exclusions do not precede recognized-text classification, and keep unrecognized candidates fail-closed through `routing.unclassified_path`. Preserve traversal, containment, regular-file, unreadable-file, and Git-enumeration guards. Keep `artifact_globs/0` as compatibility metadata and do not change public APIs, private-term normalization, stable sorting, or rule/path-only diagnostics.</action>
  <verify>
    <automated>mix test test/crosswake/planning/first_adopter_context_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - All three verifier-reproduced path classes are discovered from tracked/non-ignored Git candidates and rejected by `scan_filesystem/2` when they contain the process-only term.
    - Future planning coverage is expressed without a finite phase-number range.
    - Recognized tracked text scans by default independent of its top-level directory.
    - Explicit raw fixture/evidence and binary exclusions are narrow, named, and tested; unknown tracked candidates do not silently pass.
    - Results and failures contain stable rule IDs and repository-relative paths only.
    - Existing generic, public-phrase, identifying-field, safety-path, enumeration, current-phase, v20, and TODO-002 tests remain green.
    - No adopter-instance fact, private value, payload, identity, endpoint, flag, archive metadata, or revealing link is added.
  </acceptance_criteria>
  <done>The direct production scanner can no longer classify the action, script, or future-phase textual candidates as non-scanned.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prove the production Mix task blocks every former bypass class</name>
  <read_first>
    - AGENTS.md
    - .planning/ADR-FIRST-B2C-ADOPTER.md
    - .planning/FIRST-B2C-ADOPTER-ADOPTION-BRIEF.md
    - .planning/FIRST-B2C-ADOPTER-ROUTE-POLICY-MAP.md
    - .planning/PROJECT.md
    - .planning/STATE.md
    - .planning/ROADMAP.md
    - .planning/REQUIREMENTS.md
    - .planning/phases/158-adoption-reset-and-route-map/158-CONTEXT.md
    - .planning/phases/158-adoption-reset-and-route-map/158-VERIFICATION.md
    - lib/crosswake/planning/first_adopter_context.ex
    - lib/mix/tasks/crosswake.adoption_context.scan.ex
    - test/crosswake/planning/first_adopter_context_test.exs
    - test/mix/tasks/crosswake_adoption_context_scan_test.exs
    - .github/workflows/hex-page-proof.yml
  </read_first>
  <files>test/mix/tasks/crosswake_adoption_context_scan_test.exs</files>
  <behavior>
    - "`Scan.run([\"--root\", root])` raises one sorted `privacy.private_term PATH` line for each action, script, and future-phase candidate in a temporary Git repository."
    - "The raised Mix error omits the configured term, matched content, and Git output."
    - "A clean temporary Git repository and the live repository pass through the unchanged production task."
    - "The protected workflow remains wired to the required-private-terms production invocation."
  </behavior>
  <action>Add a production-seam regression that writes the same three path classes into the existing temporary Git root, stages them or leaves them non-ignored as the helper contract requires, configures a private term assembled only in process, and calls the real `Mix.Tasks.Crosswake.AdoptionContext.Scan.run/1`. Assert the exact sorted three-line error contains only `privacy.private_term` and relative paths, and explicitly refute the assembled input and surrounding fixture content. Retain all existing clean-root, generic, public-spelling, identifying-field, required-secret, unregistered-guide/later-phase, and protected-workflow cases. Do not change the Mix task or workflow unless the new regression exposes a caller-wiring defect; the intended link is the existing task delegating to the corrected `scan_filesystem/2`. Run the focused direct and Mix-task suites, live production scan, and explicit formatter gate over all three changed Elixir files.</action>
  <verify>
    <automated>mix test test/crosswake/planning/first_adopter_context_test.exs test/mix/tasks/crosswake_adoption_context_scan_test.exs &amp;&amp; mix crosswake.adoption_context.scan &amp;&amp; mix format --check-formatted lib/crosswake/planning/first_adopter_context.ex test/crosswake/planning/first_adopter_context_test.exs test/mix/tasks/crosswake_adoption_context_scan_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - The production Mix task rejects `.github/actions/`, `script/`, and an out-of-range future planning phase in one deterministic regression.
    - The Mix error is the sorted stable rule/path ledger and contains no supplied term, matching line, surrounding content, or Git output.
    - The test exercises `Scan.run/1`; it does not substitute a second scanner or direct-only assertion.
    - Existing protected workflow wiring and missing-secret fail-closed behavior remain green.
    - The live scan and formatter gate pass without editing `.planning/config.json`.
  </acceptance_criteria>
  <done>Both direct and production Mix-task seams mechanically block every bypass class named by the authoritative verification report.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| Git index/non-ignored worktree -> candidate classifier | A tracked text omission can bypass protected private-term enforcement. |
| Candidate path/type -> file read | Raw evidence, binaries, unsafe paths, and ordinary text require distinct closed handling. |
| Secret environment -> scanner/Mix diagnostics | Private terms must remain process-only and non-echoing. |
| Scanner library -> production Mix task/CI | Direct coverage is insufficient unless the merge-blocking caller inherits it. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- | --- |
| T-158-G17-01 | Tampering / Elevation of Privilege | action/script/future-phase classification | high | mitigate | Scan recognized tracked text by default and add direct plus Mix-task regressions for all three paths. |
| T-158-G17-02 | Tampering | finite planning matcher | high | mitigate | Replace the bounded phase range with a future-safe planning artifact rule. |
| T-158-G17-03 | Information Disclosure | private-term and matched content handling | high | mitigate | Assemble the term in process and assert rule/path-only direct and Mix-task results. |
| T-158-G17-04 | Elevation of Privilege | broad exclusions | high | mitigate | Retain only narrow raw/binary exclusions and fail closed for every remaining unknown candidate. |
| T-158-G17-05 | Spoofing / Tampering | candidate path/file type | high | mitigate | Preserve relative-path, containment, regular-file, symlink, unreadable, and enumeration guards. |
| T-158-G17-06 | Denial of Service | raw and binary inputs | medium | mitigate | Exclude explicitly classified raw evidence and known binary extensions before content reads. |
| T-158-G17-07 | Repudiation | production caller coverage | medium | mitigate | Exact Mix-task error assertions prove the corrected scanner reaches the merge-blocking seam. |

ASVS Level 1: all high-severity bypass, path-safety, and disclosure threats have blocking automated mitigations.
</threat_model>

<verification>
- The focused module suite proves scan-by-default classification, narrow exclusions, safety failures, and non-echoing direct results.
- The Mix-task suite proves the production caller rejects all three bypass classes with exact rule/path output.
- The live scanner and explicit formatter gate must pass before evidence reconciliation begins.
</verification>

<success_criteria>
- `.github/actions/`, `script/`, and arbitrary future planning phase text all reach private-term checks.
- Only explicit raw/binary evidence exclusions remain outside content scanning.
- Unknown tracked candidates remain blocking, and all diagnostics remain non-echoing.
- No work expands beyond RESET-04 or changes adopter-instance/later-phase support truth.
</success_criteria>

## Specless Probe Resolution

The RESET-04 `unclassified` probe resolves to this explicit contract: every safe tracked or
non-ignored candidate must be classified; recognized text scans by default, explicit raw/binary
evidence is excluded without being read, and every other candidate blocks with a stable routing
violation. No candidate may silently disappear.

## Multi-Source Coverage Audit

| Source | ID | Feature/Requirement | Plan | Status | Notes |
| --- | --- | --- | --- | --- | --- |
| GOAL | — | Privacy-safe context routing for Phase 158 | 158-17 | COVERED | Closes the only current verification blocker without reopening completed route/reset work. |
| REQ | RESET-04 | No prohibited identity or personal information in planning/public adoption artifacts | 158-17 | COVERED | Direct and production regressions cover all three bypass classes. |
| RESEARCH | — | Centralized routing, private-term env input, stable non-echoing diagnostics, no new dependency | 158-17 | COVERED | Existing module and Mix task are retained. |
| CONTEXT | D-14-D-18 | Central routing, explicit scope, CI/private-term enforcement, no scanner dependency | 158-17 | COVERED | Scan-by-default text classification closes drift without widening inputs or output. |
| CONTEXT | D-19-D-20 | Calm maintainer diagnostics and browser-free deterministic checks | 158-17 | COVERED | Exact rule/path assertions and ExUnit remain the maintainer surface. |

## Deterministic Planner Gate Evaluation

- **API coverage:** Not applicable; this is internal repository scanning and ExUnit/Mix-task testing, with no external API or HTTP surface.
- **Assumption delta:** No-change; repository-relative path identity and the existing destination model remain authoritative.
- **Schema push:** Not applicable; no supported ORM or schema file is created or modified.

## Artifacts this phase produces

- Modified `lib/crosswake/planning/first_adopter_context.ex`, including the private `classify_repository_path/1`, `phase_artifact_path?/1`, `scannable_text_path?/1`, and explicit exclusion policy used by `scan_filesystem/2`.
- Modified `test/crosswake/planning/first_adopter_context_test.exs` with direct action/script/future-phase scan regressions.
- Modified `test/mix/tasks/crosswake_adoption_context_scan_test.exs` with production Mix-task action/script/future-phase regressions.
- No new dependency, public API, endpoint, schema, route, UI, service, Android surface, adopter-instance fact, or support claim.

<output>
Create `.planning/phases/158-adoption-reset-and-route-map/158-17-SUMMARY.md` when done.
</output>
