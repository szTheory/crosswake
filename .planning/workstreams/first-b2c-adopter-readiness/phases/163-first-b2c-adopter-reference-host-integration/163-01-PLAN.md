---
phase: 163-first-b2c-adopter-reference-host-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - examples/phoenix_host/config/config.exs
  - examples/phoenix_host/lib/crosswake_example/physical_iphone_proof_host.ex
  - examples/phoenix_host/lib/crosswake_example/local_first/physical_iphone_authority.ex
  - examples/phoenix_host/native/ios/CrosswakeProofLane/ProofLaneDriver.swift
  - examples/phoenix_host/native/ios/CrosswakeProofLane/ProofLaneApp.swift
  - examples/phoenix_host/native/ios/CrosswakeProofLaneUITests/ProofLaneUITests.swift
  - examples/phoenix_host/test/crosswake_example/physical_iphone_proof_host_test.exs
autonomous: true
requirements: [ALPHA-01, ALPHA-02, ALPHA-03, ALPHA-04]
---

<objective>
Build the bounded First B2C Adopter reference host needed by Phase 162: one verified foreground
learning bundle, one persisted offline study island, one independent Phoenix replay-authority
producer, and one signed-device report producer. Keep promotion behind the existing all-ready
physical-iPhone gate.
</objective>

<tasks>

<task type="auto">
  <name>Generate and configure the host-owned proof lane</name>
  <action>Run the sanctioned iOS proof-lane generator in the Phoenix example host, retain generated host ownership, configure the closed route and physical host adapter, and fix configured-module loading without adding Android scope.</action>
  <verify>Run `mix crosswake.gen.proof_lane ios --check` and the generated Phoenix contract test.</verify>
</task>

<task type="auto">
  <name>Implement the bounded device and backend producers</name>
  <action>Install and verify exact manifest/image/audio bytes atomically, play real local audio, persist only closed study lifecycle markers across actual app terminate/relaunch, and independently execute Phoenix replay, conflict, scope, logout, and feature-control cases. Retain no raw output or payload values.</action>
  <verify>Run the focused ExUnit authority/evidence tests, generated Playwright flow, and the generated XCUITest contract on an iOS simulator as non-promoting serialization/behavior advice.</verify>
</task>

<task type="auto">
  <name>Wire the physical-only runner and evidence input</name>
  <action>Discover exactly one physical iOS destination without emitting identity, require local signing, run only the host XCUITest in a mode-0700 temporary root, join it with the Phoenix report, build the fixed canonical evidence input, and leave Evidence as the sole no-replace promoter.</action>
  <verify>Run the core physical preflight/report/evidence suite and `mix crosswake.proof_lane.physical_iphone --readiness --json`; never invoke promotion unless every check is ready.</verify>
</task>

</tasks>

<success_criteria>
- The reference host satisfies ALPHA-01 through ALPHA-04 without generic storage/sync or Android work.
- Generated Phoenix, browser, and iOS advisory contracts pass.
- Readiness reports every host/device/backend prerequisite ready except any genuine external Apple signing gate.
- No physical evidence artifact is manually created or promoted.
</success_criteria>
