---
phase: 161.1-first-adopter-ios-navigation-shell
plan: "13"
type: execute
wave: 12
depends_on: ["161.1-12"]
files_modified:
  - examples/ios_shell_host/CrosswakeShellTests/NavigationShellTests.swift
  - scripts/verify_phase_161_1.sh
  - test/crosswake/proof/phase161_1_navigation_gate_integrity_test.exs
  - test/crosswake/proof_lane/navigation_shell_advisory_test.exs
  - test/mix/tasks/crosswake_proof_lane_verify_navigation_shell_test.exs
autonomous: true
gap_closure: true
requirements: [NAV-01, NAV-02, NAV-03, NAV-04, NAV-05, NAV-06, NAV-07]
must_haves:
  truths:
    - "A fresh same-tree gate derives each retained PL-IOS-NAV assertion from one exact closed marker emitted only after the corresponding current production-host behavior succeeds (D-10, D-11; NAV-01 through NAV-06)."
    - "The invocation-private Xcode transcript must contain the seven allowlisted markers exactly once and in canonical order; an absent, duplicate, reordered, unknown, or externally supplied observation fails before advisory promotion (D-10, D-11; NAV-06)."
    - "A host XCTest process may exit successfully and every selected test case may report passed, but the complete gate still fails if any required behavior marker is absent (D-10; NAV-06)."
    - "Only the closed marker IDs, passed outcome, invocation nonce, canonical observation digest, and existing subject digests cross the ephemeral transcript boundary; no transcript line or prohibited data is retained (D-04, D-08, D-11)."
    - "Simulator evidence remains advisory; TODO-002 remains unknown_blocking, Phoenix confirmation remains required, Android remains frozen, and Phase 162 alone owns physical-iPhone promotion (D-01, D-02, D-06, D-11, D-12; NAV-01, NAV-06, NAV-07)."
  artifacts:
    - path: "examples/ios_shell_host/CrosswakeShellTests/NavigationShellTests.swift"
      provides: "One ordered production-host assertion path that emits the seven closed navigation markers only after their real UIKit/WebKit/coordinator behaviors pass."
    - path: "scripts/verify_phase_161_1.sh"
      provides: "Invocation-private Xcode transcript reduction that encodes the observed ordered markers rather than authoring a literal success list."
    - path: "test/crosswake/proof/phase161_1_navigation_gate_integrity_test.exs"
      provides: "A full-gate negative control where Xcode reports passing host tests while one required marker is missing."
    - path: "test/crosswake/proof_lane/navigation_shell_advisory_test.exs"
      provides: "Closed canonical observation regressions for absent, duplicate, reordered, unknown, and widened observations."
    - path: "test/mix/tasks/crosswake_proof_lane_verify_navigation_shell_test.exs"
      provides: "Invocation-private observation provenance and forged-input rejection at the promotion boundary."
  key_links:
    - from: "production host behavior assertions"
      to: "invocation-private Xcode transcript"
      via: "an exact anchored closed marker line emitted after each assertion group succeeds"
      pattern: "PL-IOS-NAV-(TOPOLOGY|PATCH-DEPTH|NAVIGATE-ONCE|RESTORE|TABS-BACK|MARKER-INSETS|FOCUS)"
    - from: "invocation-private Xcode transcript"
      to: "navigation-shell-observation.json"
      via: "exact ordered extraction and count validation using the extracted IDs as the JSON input"
      pattern: "assertion_ids|run_nonce|schema_version|scope"
    - from: "navigation-shell-observation.json"
      to: "retained navigation_shell_advisory evidence"
      via: "existing private-root, canonical-byte, nonce, subject-digest, privacy-scan, and invalidation controls"
      pattern: "decode_observation|NavigationShellAdvisory.build|Evidence.check"
---

<objective>
Close the remaining Phase 161.1 provenance blocker by making current host behavior—not passing test names or a literal ID list—the sole source of retained navigation assertion success.

Purpose: Ensure the advisory gate fails closed whenever the fresh host run did not explicitly observe every claimed bounded navigation behavior.
Output: Production-host closed marker emission, exact transcript reduction, promotion-boundary regressions, and a non-vacuous full-gate missing-marker negative control.
</objective>

## Artifacts this phase produces

- Seven exact, non-sensitive `PL-IOS-NAV-*` marker lines emitted in canonical order from one selected production-host XCTest path after the corresponding behavior assertions succeed.
- An invocation-private canonical observation reduced from those actual transcript markers, with no retained transcript, screenshot, `.xcresult`, route fact, identity, or device data.
- A regression harness proving the complete phase gate rejects a successful host-test process that omits one required marker.
- No new topology authority, navigation framework, Android artifact, physical-device claim, native confirmation surface, dependency, or permanent CI lane.

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/ADR-FIRST-B2C-ADOPTER.md
@.planning/FIRST-B2C-ADOPTER-ADOPTION-BRIEF.md
@.planning/FIRST-B2C-ADOPTER-ROUTE-POLICY-MAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/161.1-first-adopter-ios-navigation-shell/161.1-CONTEXT.md
@.planning/phases/161.1-first-adopter-ios-navigation-shell/161.1-RESEARCH.md
@.planning/phases/161.1-first-adopter-ios-navigation-shell/161.1-PATTERNS.md
@.planning/phases/161.1-first-adopter-ios-navigation-shell/161.1-VERIFICATION.md
@.planning/phases/161.1-first-adopter-ios-navigation-shell/161.1-12-SUMMARY.md
@scripts/verify_phase_161_1.sh
@lib/crosswake/proof_lane/navigation_shell_advisory.ex
@lib/mix/tasks/crosswake.proof_lane.verify_navigation_shell.ex
@examples/ios_shell_host/CrosswakeShellTests/NavigationShellTests.swift
@examples/ios_shell_host/CrosswakeShellUITests/NavigationShellTests.swift
@test/crosswake/proof_lane/navigation_shell_advisory_test.exs
@test/mix/tasks/crosswake_proof_lane_verify_navigation_shell_test.exs

<interfaces>
- `NavigationShellAdvisory.assertion_ids/0` is the canonical ordered seven-ID vocabulary; the shell gate may compare extracted IDs with it, but the observation it writes must be encoded from the extracted transcript IDs, never from a second success list.
- `mix crosswake.proof_lane.verify_navigation_shell` continues to accept only a canonical observation and nonce in its caller-owned private run root, invalidates both after use, and retains the existing closed advisory object plus final-tree subject digests.
- The host marker path exercises current `NavigationShellViewController`, `NavigationCoordinator`, and `LiveViewContainerViewController` seams. It is evidence instrumentation only and gains no route, auth, leaf-rendering, or browser-history authority.
</interfaces>
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Trace each real host behavior into one exact transcript marker</name>
  <reversibility rating="reversible">The marker instrumentation and phase-local reducer can be changed without altering the production navigation, topology, or retained evidence schemas.</reversibility>
  <read_first>
    - AGENTS.md
    - .planning/phases/161.1-first-adopter-ios-navigation-shell/161.1-CONTEXT.md
    - .planning/phases/161.1-first-adopter-ios-navigation-shell/161.1-VERIFICATION.md
    - .planning/phases/161.1-first-adopter-ios-navigation-shell/161.1-12-SUMMARY.md
    - examples/ios_shell_host/CrosswakeShellTests/NavigationShellTests.swift
    - examples/ios_shell_host/CrosswakeShellUITests/NavigationShellTests.swift
    - scripts/verify_phase_161_1.sh
    - lib/crosswake/proof_lane/navigation_shell_advisory.ex
    - lib/mix/tasks/crosswake.proof_lane.verify_navigation_shell.ex
    - test/crosswake/proof_lane/navigation_shell_advisory_test.exs
    - test/mix/tasks/crosswake_proof_lane_verify_navigation_shell_test.exs
  </read_first>
  <files>examples/ios_shell_host/CrosswakeShellTests/NavigationShellTests.swift, scripts/verify_phase_161_1.sh, test/crosswake/proof_lane/navigation_shell_advisory_test.exs, test/mix/tasks/crosswake_proof_lane_verify_navigation_shell_test.exs</files>
  <behavior>
    - "RED: passing named XCTest/XCUITest cases without all seven closed marker lines cannot create a navigation observation or reach the Mix promotion task."
    - "RED: absent, duplicate, reordered, unknown, malformed, or non-passed marker lines fail with one stable non-echoing host-marker rule before retention."
    - "The ordered host proof exercises topology/root authorization, patch depth, navigate idempotency, restoration/pop, tabs/back, document marker plus separate insets, and one completed-navigation focus handoff through current production objects before emitting each corresponding ID."
    - "The canonical observation encodes the IDs extracted from the current transcript, one closed passed outcome, schema version 1, advisory scope, and the current invocation nonce only."
  </behavior>
  <action>Implement this gap closure under every locked boundary: compiled bounded truth per D-01 and fail-closed unknown input per D-02; resolver-owned leaves per D-03; closed low-frequency evidence vocabulary per D-04; exact patch/navigate/native-stack meaning per D-05; Phoenix confirmation and state-preserving denial per D-06; current UIKit containers per D-07; non-identifying layout data per D-08; completed-navigation-only focus semantics per D-09; the existing host proof lane per D-10; private advisory-only evidence per D-11; and unchanged bounded support truth per D-12. Add one explicitly selected, ordered host XCTest proof method that uses the existing synthetic topology and current production container/coordinator/WebKit helpers. For each behavior group, complete the discriminating assertions first and only then emit one anchored line containing the matching `PL-IOS-NAV-*` ID and closed `passed` token. Keep marker lines free of routes, paths, payloads, histories, account/device/token values, test diagnostics, and free-form text. Update `scripts/verify_phase_161_1.sh` so the private Xcode transcript is parsed for only that exact anchored grammar; require the extracted sequence to equal `NavigationShellAdvisory.assertion_ids/0`, require every ID exactly once, and reject missing, duplicate, reordered, unknown, or malformed lines before nonce/observation creation. Build `assertion_ids` from the extracted sequence itself rather than the module allowlist or a literal shell list. Preserve named host-test pass checks as a separate prerequisite, the private `mktemp` lifecycle, canonical JSON, nonce binding, subject digests, privacy scan, observation invalidation, and advisory-only promotion. Expand the advisory and Mix-task negative matrices for reordered/duplicate/unknown/forged observations without echoing input. Do not modify production navigation ownership or any support/verification ledger.</action>
  <acceptance_criteria>
    - Each of the seven retained assertion IDs has one exact marker emission positioned after a discriminating current-host behavior assertion.
    - The reducer accepts exactly seven ordered unique anchored marker lines and writes the observation from those extracted values.
    - Test-case pass lines alone cannot satisfy the reducer, and no literal post-test seven-ID success document remains in the gate.
    - The observation and retained evidence contain only closed IDs/outcome, nonce-derived observation digest, and existing content digests; the transcript remains ephemeral.
    - D-01 through D-12 boundaries remain unchanged: no real adopter topology, generic navigation, Android work, device claim, or native confirmation is introduced.
  </acceptance_criteria>
  <verify>
    <automated>mix test test/crosswake/proof_lane/navigation_shell_advisory_test.exs test/mix/tasks/crosswake_proof_lane_verify_navigation_shell_test.exs &amp;&amp; xcodebuild -project examples/ios_shell_host/CrosswakeShell.xcodeproj -scheme CrosswakeShell -destination 'platform=iOS Simulator,name=iPhone 17' test -only-testing:CrosswakeShellTests/NavigationShellTests -only-testing:CrosswakeShellUITests/NavigationShellTests CODE_SIGNING_ALLOWED=NO</automated>
  </verify>
  <done>The current production-host run emits one closed ordered marker per asserted navigation behavior, and only those observed markers can form the advisory input.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prove a passing host test without one marker fails the complete gate</name>
  <reversibility rating="reversible">The negative-control fixture is test-only and the same-tree script remains the phase-local verification entry point.</reversibility>
  <read_first>
    - AGENTS.md
    - .planning/phases/161.1-first-adopter-ios-navigation-shell/161.1-CONTEXT.md
    - .planning/phases/161.1-first-adopter-ios-navigation-shell/161.1-VERIFICATION.md
    - scripts/verify_phase_161_1.sh
    - test/crosswake/proof/phase153_1_gate_integrity_test.exs
    - test/crosswake/proof_lane/navigation_shell_advisory_test.exs
    - test/mix/tasks/crosswake_proof_lane_verify_navigation_shell_test.exs
  </read_first>
  <files>scripts/verify_phase_161_1.sh, test/crosswake/proof/phase161_1_navigation_gate_integrity_test.exs</files>
  <behavior>
    - "A fixture Xcode command exits zero and prints every selected host test as passed, but omits one required closed marker; the real complete script exits nonzero before observation creation or promotion."
    - "Duplicate and reordered marker fixtures also exit nonzero with the same stable rule/path-only diagnostic."
    - "A complete exact marker fixture reaches the next gate stage, proving the negative control is discriminating rather than an always-failing wrapper."
    - "The actual fresh same-tree gate still passes with all deterministic, Swift, host XCTest/XCUITest, privacy, digest-binding, formatting, and diff checks."
  </behavior>
  <action>Create an ExUnit gate-integrity test following the repository's negative-control fixture pattern. Put temporary command shims ahead of `PATH` so all pre-Xcode suites report success, `xcrun` exposes a synthetic available simulator, and the fake `xcodebuild` prints the exact selected test-case pass records but omits one marker while exiting zero. Run the real `scripts/verify_phase_161_1.sh` against a fresh absolute destination and assert nonzero status, the stable marker failure ID, absence of the observation/promotion path, and no echoed transcript content. Add duplicate and reordered variants plus a complete-marker control that advances past marker reduction. Keep the shims and transcripts under the test temporary directory and delete them automatically. Then run the real complete gate with a fresh destination; it must derive all seven markers from the actual private Xcode transcript and promote only the existing advisory evidence. Preserve D-11 privacy exclusions and all D-01/D-02/D-06/D-12 non-claims; do not edit `161.1-VALIDATION.md`, `161.1-VERIFICATION.md`, roadmap, requirements, state, or support truth.</action>
  <acceptance_criteria>
    - The regression demonstrates a zero-exit host test process with every named test reported passed is insufficient when one behavior marker is absent.
    - Missing, duplicate, and reordered marker cases fail before observation creation and before the Mix promotion task; the exact-marker control advances.
    - The fresh real same-tree command succeeds only with seven actual ordered current-run markers and existing canonical/digest/privacy controls.
    - No raw Xcode output, route/payload/history, identity, screenshot, `.xcresult`, or stable device data is retained or echoed.
    - Simulator evidence remains advisory; TODO-002 is unknown_blocking, Android is frozen, Phoenix confirmation is required, and physical-iPhone proof remains Phase 162-only.
  </acceptance_criteria>
  <verify>
    <automated>mix test test/crosswake/proof/phase161_1_navigation_gate_integrity_test.exs test/crosswake/proof_lane/navigation_shell_advisory_test.exs test/mix/tasks/crosswake_proof_lane_verify_navigation_shell_test.exs &amp;&amp; phase161_1_marker_root="$(mktemp -d "${TMPDIR:-/tmp}/crosswake-161-1-marker.XXXXXX")" &amp;&amp; bash scripts/verify_phase_161_1.sh "$phase161_1_marker_root/retained"</automated>
  </verify>
  <done>The complete gate has a non-vacuous regression for the verifier's exact blocker and passes only when current host behavior supplies every required closed marker.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| production host assertions -> Xcode transcript | Test output is untrusted until the exact selected host path completes each behavior and emits the closed marker grammar. |
| invocation-private transcript -> canonical observation | Transcript content may be absent, duplicated, reordered, malformed, stale, forged, or sensitive; only exact ordered IDs may cross. |
| canonical observation -> advisory promotion | Existing private-root, nonce, canonical-byte, invalidation, subject-digest, and evidence checks must reject caller-forged input. |
| simulator advisory -> project support truth | Simulator evidence cannot establish adopter-instance, physical-iPhone, Android, generic-navigation, or native-confirmation support. |

## STRIDE Threat Register — ASVS L1, block on high

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-161.1-G13-01 | Spoofing / Repudiation | host behavior observation | high | mitigate | Task 1 emits exact markers only after discriminating production-host assertions and Task 2 proves passing test names alone are insufficient. |
| T-161.1-G13-02 | Tampering | transcript reduction | high | mitigate | Task 1 requires exact anchored grammar, canonical order, uniqueness, and encoding from extracted IDs; missing/duplicate/reordered/unknown input stops before promotion. |
| T-161.1-G13-03 | Spoofing / Tampering | forged observation | high | mitigate | Task 1 preserves invocation-private containment, fresh nonce equality, canonical bytes, one-use invalidation, and promotion-boundary negative tests. |
| T-161.1-G13-04 | Information Disclosure | Xcode transcript and diagnostics | high | mitigate | Tasks 1-2 keep transcripts private and ephemeral, cross only closed IDs/outcome, and assert stable non-echoing failure output. |
| T-161.1-G13-05 | Elevation of Privilege | advisory claim promotion | high | mitigate | Both tasks retain advisory scope, TODO-002 unknown_blocking, Android freeze, Phoenix confirmation, and Phase-162-only device authority. |
| T-161.1-G13-SC | Tampering | dependency supply chain | low | accept | No dependency installation, package change, external service, or CI workflow is added. |
</threat_model>

<source_coverage_audit>
| Source | ID | Feature / requirement | Plan | Status | Notes |
|---|---|---|---|---|---|
| GOAL | — | Native-feeling bounded iOS roots/details without generic navigation or leaf-owner transfer. | 01-13 | COVERED | Plan 13 repairs only proof provenance; implemented ownership and behavior remain unchanged. |
| REQ | NAV-01 | Compiled topology and leaf ownership. | 01-02,10,13 | COVERED | Topology behavior gets a current-host marker; real adopter topology remains unknown_blocking. |
| REQ | NAV-02 | Native tabs, push/back, resolver authority. | 01,04-05,10,13 | COVERED | Current production-host tabs/back behavior emits closed evidence. |
| REQ | NAV-03 | Typed fail-closed patch/navigate semantics. | 01,03-04,10,13 | COVERED | Patch-depth and navigate-once markers are derived from actual coordinator behavior. |
| REQ | NAV-04 | Four safe-area facts separate from keyboard. | 05,10,13 | COVERED | Marker follows the production layout contract assertions. |
| REQ | NAV-05 | Synchronous non-identifying document marker. | 05,10,13 | COVERED | Marker follows fixed document-start marker/default assertions. |
| REQ | NAV-06 | Executable bounded proof without Android/general claim. | 06,09-13 | COVERED | Plan 13 closes the verifier's sole provenance blocker and adds the full-gate negative control. |
| REQ | NAV-07 | Phoenix confirmation fallback and reversal condition. | 07-09,12-13 | COVERED | No fallback or support truth changes; boundary is asserted during the full gate. |
| RESEARCH | proof/evidence constraint | Extend existing host lane; closed advisory evidence; no transcript retention. | 13 | COVERED | No new harness, dependency, or support surface. |
| RESEARCH | UIKit/WebKit/XCTest path | Exercise production container/coordinator/layout seams. | 10,13 | COVERED | Marker method uses existing current-host objects. |
| CONTEXT | D-01-D-03 | Bounded topology, unknown_blocking, resolver-authorized native state. | 01-05,10,13 | COVERED | One marker per relevant current behavior; no topology change. |
| CONTEXT | D-04-D-06 | Closed synchronization, exact effects, Phoenix confirmation. | 03-04,07-10,13 | COVERED | Marker proof preserves closed protocol and fallback ownership. |
| CONTEXT | D-07-D-09 | UIKit shell, layout, marker, accessibility. | 05,10,13 | COVERED | Production-host assertions become marker-backed. |
| CONTEXT | D-10-D-12 | Host proof, privacy/advisory boundary, bounded support truth. | 06-13 | COVERED | Exact transcript reduction and non-promotion regression directly implement these decisions. |
| CONTEXT | Deferred ideas | Generic/cross-platform navigation, Android, renderer, modal breadth, telemetry, device farm/CI, native confirm. | — | EXCLUDED | No task implements a deferred item. |
| VERIFICATION | blocker | Actual markers must drive promotion; passing host test without a marker must fail the full gate. | 13 | COVERED | Tasks 1-2 directly implement both missing items. |
</source_coverage_audit>

<dependency_graph>
| Plan | Needs | Creates | Wave |
|---|---|---|---|
| 161.1-13 | Executed Plan 12 private observation/advisory promotion and Plan 10 production-host behavior coverage | Marker-backed transcript provenance and full-gate negative control | 12 |

Task 2 depends on Task 1's exact marker grammar and reducer. No same-wave plan shares these files.
</dependency_graph>

<verification>
Run the focused advisory, Mix-task, and gate-integrity regressions first. Then run the complete same-tree command once against a fresh absolute destination. Completion requires actual ordered current-run markers, the existing nonce/canonical/digest/privacy controls, and explicit advisory-only boundaries; no human checkpoint or UAT is permitted.
</verification>

<success_criteria>
- Every retained navigation assertion is traced to one exact current host behavior marker.
- Absent, duplicate, reordered, unknown, malformed, or forged marker/observation input fails before promotion.
- A passing host test without one required marker is proven insufficient by the real full-gate entry point.
- Raw test output and prohibited data remain ephemeral and unretained.
- TODO-002, Phoenix confirmation, Android freeze, simulator advisory scope, and Phase 162 device authority remain unchanged.
</success_criteria>

<output>
Create `.planning/phases/161.1-first-adopter-ios-navigation-shell/161.1-13-SUMMARY.md` when done.
</output>
