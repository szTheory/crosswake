---
phase: 161.1-first-adopter-ios-navigation-shell
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/crosswake/adoption/navigation_topology.ex
  - test/crosswake/adoption/navigation_topology_test.exs
  - priv/contract_vectors/navigation_topology_vectors.json
  - packages/crosswake-shell-core-ios/Sources/CrosswakeShellCore/NavigationTopology.swift
  - packages/crosswake-shell-core-ios/Sources/CrosswakeShellCore/NavigationCoordinator.swift
  - packages/crosswake-shell-core-ios/Tests/CrosswakeShellCoreTests/NavigationCoordinatorTests.swift
autonomous: true
requirements: [NAV-01, NAV-02, NAV-03]
must_haves:
  truths:
    - "One opaque validated synthetic route row compiles into a version-bound topology artifact, decodes in iOS, reauthorizes through the existing activation resolver, and becomes one main-actor root stack without changing leaf ownership (D-01, D-03; NAV-01, NAV-02)."
    - "Empty or incomplete adopter input remains unknown_blocking; the tracer never fabricates a real root, tab order, path, or restoration fact while TODO-002 is unvalidated (D-02)."
    - "Malformed, incompatible, unknown, or unauthorized topology input yields one stable non-echoing denial and leaves the current safe route and stack unchanged (D-02, D-03, D-06; NAV-03)."
  artifacts:
    - path: "lib/crosswake/adoption/navigation_topology.ex"
      provides: "Closed topology row/compiler and unknown_blocking result derived only from validated RouteInventory rows."
    - path: "priv/contract_vectors/navigation_topology_vectors.json"
      provides: "Opaque cross-language tracer and negative topology vectors."
    - path: "packages/crosswake-shell-core-ios/Sources/CrosswakeShellCore/NavigationTopology.swift"
      provides: "Immutable Codable topology artifact and closed validation result."
    - path: "packages/crosswake-shell-core-ios/Sources/CrosswakeShellCore/NavigationCoordinator.swift"
      provides: "Main-actor selected-tab/stack state with activation-first mutation."
  key_links:
    - from: "Crosswake.Adoption.NavigationTopology.compile/1"
      to: "NavigationTopology.decode/validate"
      via: "the same versioned opaque JSON contract vector"
      pattern: "navigation_topology|topology_schema_version|route_id|root_tab_id"
    - from: "NavigationCoordinator.selectRoot"
      to: "ActivationCoordinator.resolve"
      via: "authorization before stack-entry creation"
      pattern: "resolve|denied|selectedTab|stacks"
  prohibitions:
    - "requirement_id: NAV-01 | category: values | statement: MUST NOT turn the bounded first-adopter topology or coordinator into a generic navigation framework, generic WebView wrapper, browser-history router, or second host route authority. | status: unresolved | verification: flagged-unverified"
    - "requirement_id: NAV-03 | category: privacy | statement: MUST NOT carry or retain route payloads, account or device identifiers, credentials, tokens, browser history, URLs, or stable identity across navigation, layout, diagnostics, or proof seams. | status: unresolved | verification: flagged-unverified"
    - "requirement_id: NAV-06 | category: transparency | statement: MUST NOT present synthetic fixtures, package tests, simulator runs, or generated-host results as adopter-instance, physical-iPhone, Android, or generic-navigation proof. | status: unresolved | verification: flagged-unverified"
    - "requirement_id: NAV-07 | category: values | statement: MUST NOT add native alert or confirm, or describe Phoenix-owned confirmation as optional, before physical-iPhone proof, a demonstrated active-adopter blocker, and an explicit maintainer roadmap decision. | status: unresolved | verification: flagged-unverified"
flagged_assumptions:
  - "[UNRESOLVED-SPEC-PROBE][NAV-01 unclassified] CONTEXT D-01 through D-03 and REQUIREMENTS.md are exhaustive; no additional topology field, real route fact, or ownership transfer is inferred."
  - "[UNRESOLVED-SPEC-PROBE][NAV-02 adjacency] Only compiled root/parent relationships may be traversed; no arbitrary sibling, cross-tab, URL, or browser-history adjacency is inferred."
  - "[UNRESOLVED-SPEC-PROBE][NAV-02 empty input] Zero eligible validated rows remains unknown_blocking and produces no native tab or promoted shell claim."
  - "[UNRESOLVED-SPEC-PROBE][NAV-02 ordering] No user-facing tab ordering semantics are supplied; synthetic vectors may be deterministic, but adopter order remains unknown_blocking until sanitized input explicitly authorizes it."
  - "[UNRESOLVED-SPEC-PROBE][NAV-03 unclassified] D-04 through D-06 define the complete transition vocabulary; no third transition kind or browser-history state is inferred."
  - "[UNRESOLVED-SPEC-PROBE][NAV-04 unclassified] D-08 defines exactly four safe-area variables and one separate keyboard variable; no additional layout or identity fact is inferred."
  - "[UNRESOLVED-SPEC-PROBE][NAV-05 unclassified] D-08 defines a non-identifying document-start marker only; no custom user agent or host/account/device marker is inferred."
  - "[UNRESOLVED-SPEC-PROBE][NAV-06 unclassified] D-10 and D-11 define deterministic contract proof plus advisory simulator proof; physical-device and Android promotion remain excluded."
  - "[UNRESOLVED-SPEC-PROBE][NAV-07 unclassified] D-12 and the governing ADR define Phoenix-owned confirmation and the entire reversal condition; no native alert/confirm work is inferred."
---

<objective>
Prove the hardest architecture path first: sanitized Phoenix route truth becomes a bounded iOS root only after cross-language validation and existing resolver authorization.

Purpose: Establish a production-quality vertical tracer before manifest breadth, transition breadth, UIKit presentation, or proof-lane expansion.
Output: Closed topology compiler, shared opaque vector, Swift topology decoder, main-actor coordinator, and end-to-end tests.
</objective>

<artifacts_this_phase_produces>
- New Elixir module `Crosswake.Adoption.NavigationTopology`, nested `Entry`, `CompileError`, `compile/1`, `render/1`, and `promotion_status/1` symbols in `lib/crosswake/adoption/navigation_topology.ex`.
- New topology fields `topology_schema_version`, `manifest_schema_version`, `status`, `entries`, `route_id`, `root_tab_id`, `presentation`, `parent_route_id`, `deep_link_posture`, and `restoration_posture`; all IDs remain opaque and values closed.
- New `priv/contract_vectors/navigation_topology_vectors.json` with synthetic opaque success, empty-input, invalid-parent, incompatibility, and authorization-denial vectors.
- New Swift `NavigationTopology`, `NavigationTopologyEntry`, `NavigationPresentation`, `NavigationEntryPosture`, `NavigationTopologyDenial`, `NavigationStackEntry`, and `NavigationCoordinator` symbols.
- New `NavigationCoordinatorTests.swift`; no app-specific route, path, tab label/order, Android artifact, bridge capability, custom user agent, evidence field, native confirmation surface, or external dependency.
</artifacts_this_phase_produces>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/ADR-FIRST-B2C-ADOPTER.md
@.planning/FIRST-B2C-ADOPTER-ROUTE-POLICY-MAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/161.1-first-adopter-ios-navigation-shell/161.1-CONTEXT.md
@.planning/phases/161.1-first-adopter-ios-navigation-shell/161.1-RESEARCH.md
@.planning/phases/161.1-first-adopter-ios-navigation-shell/161.1-PATTERNS.md
@lib/crosswake/adoption/route_inventory.ex
@packages/crosswake-shell-core-ios/Sources/CrosswakeShellCore/ActivationCoordinator.swift

<interfaces>
- `RouteInventory.validate_inventory/1` and `promotion_status/1` are the only input-admission seam; topology compilation consumes their validated closed rows and propagates unknown_blocking.
- `ActivationCoordinator.resolve(request:manifest:)` is the existing route/runtime/pack/origin authority; `NavigationCoordinator` must call it before committing selected-tab or stack state.
- Cross-language types use explicit snake_case coding keys, closed enum values, and compatible schema versions; failures expose stable reason IDs without rejected values.
</interfaces>
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Trace one compiled opaque root through iOS authorization</name>
  <reversibility rating="costly">D-01 publishes a versioned topology shape and D-03 fixes the coordinator authority seam, so reversal would require coordinated manifest, Swift, fixture, and host migration.</reversibility>
  <read_first>
    - AGENTS.md
    - .planning/ADR-FIRST-B2C-ADOPTER.md
    - .planning/FIRST-B2C-ADOPTER-ROUTE-POLICY-MAP.md
    - .planning/phases/161.1-first-adopter-ios-navigation-shell/161.1-CONTEXT.md
    - .planning/phases/161.1-first-adopter-ios-navigation-shell/161.1-RESEARCH.md
    - .planning/phases/161.1-first-adopter-ios-navigation-shell/161.1-PATTERNS.md
    - lib/crosswake/adoption/route_inventory.ex
    - packages/crosswake-shell-core-ios/Sources/CrosswakeShellCore/ActivationCoordinator.swift
    - packages/crosswake-shell-core-ios/Tests/CrosswakeShellCoreTests/ActivationConformanceTests.swift
  </read_first>
  <files>lib/crosswake/adoption/navigation_topology.ex, priv/contract_vectors/navigation_topology_vectors.json, packages/crosswake-shell-core-ios/Sources/CrosswakeShellCore/NavigationTopology.swift, packages/crosswake-shell-core-ios/Sources/CrosswakeShellCore/NavigationCoordinator.swift, packages/crosswake-shell-core-ios/Tests/CrosswakeShellCoreTests/NavigationCoordinatorTests.swift</files>
  <behavior>
    - "RED: the shared vector's single validated opaque root cannot become selected until the existing resolver returns an authorized leaf presentation."
    - "RED: a resolver denial or topology/manifest mismatch returns a closed reason and preserves the prior selected tab, stack, and active route."
    - "The Elixir compiler renders the same canonical success vector shape that Swift decodes, with no path, label, route payload, account, device, token, or history field."
    - "A completed root selection creates exactly one stack entry owned by the resolved leaf runtime; the coordinator does not render or reinterpret the leaf."
  </behavior>
  <action>Write the failing Swift tracer first against the shared synthetic vector. Implement `Crosswake.Adoption.NavigationTopology` after the `RouteInventory` validation pattern: reject unknown/non-atom/forbidden fields before normalization, accept only validated eligible rows, and render a deterministic versioned artifact whose entries carry exactly the D-01 fields. Implement immutable Codable Swift values with explicit snake_case coding keys. Add `@MainActor NavigationCoordinator` as the sole selected-tab/stack mutator; inject or call the existing activation resolver, resolve the root before creating `NavigationStackEntry`, and retain the resolved leaf presentation without converting it into generic WebView state per D-03. Use only opaque `route-` fixture IDs and synthetic tab refs. A denial must be non-echoing, explicit, and state-preserving per D-02/D-06. Keep TODO-002 absent input outside the success fixture and do not infer an adopter tab order.</action>
  <acceptance_criteria>
    - One canonical opaque topology vector is accepted by the Elixir compiler contract and Swift decoder, then reaches one authorized coordinator root stack.
    - The resolver is observed before stack mutation; denial leaves prior state byte-for-byte/equality unchanged.
    - The artifact contains exactly the closed topology fields and no second Swift-host topology configuration.
    - No real path, tab name/order, adopter fact, sensitive value, browser-history state, Android code, or generic renderer is introduced.
  </acceptance_criteria>
  <verify><automated>mix run -e 'alias Crosswake.Adoption.NavigationTopology; {:ok, json} = NavigationTopology.render([]); true = is_binary(json)' &amp;&amp; swift test --package-path packages/crosswake-shell-core-ios --filter NavigationCoordinatorTests/testCompiledRootRequiresResolverAuthorization</automated></verify>
  <done>One retained production path proves Phoenix-compiled topology and resolver authority reach iOS native stack state without transferring leaf ownership.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Close empty, malformed, incompatible, and unauthorized topology paths</name>
  <reversibility rating="reversible">The negative matrix extends validation around the costly schema without changing its published fields.</reversibility>
  <read_first>
    - .planning/phases/161.1-first-adopter-ios-navigation-shell/161.1-CONTEXT.md
    - lib/crosswake/adoption/route_inventory.ex
    - lib/crosswake/adoption/navigation_topology.ex
    - priv/contract_vectors/navigation_topology_vectors.json
    - packages/crosswake-shell-core-ios/Sources/CrosswakeShellCore/NavigationTopology.swift
    - packages/crosswake-shell-core-ios/Sources/CrosswakeShellCore/NavigationCoordinator.swift
  </read_first>
  <files>lib/crosswake/adoption/navigation_topology.ex, test/crosswake/adoption/navigation_topology_test.exs, priv/contract_vectors/navigation_topology_vectors.json, packages/crosswake-shell-core-ios/Sources/CrosswakeShellCore/NavigationTopology.swift, packages/crosswake-shell-core-ios/Tests/CrosswakeShellCoreTests/NavigationCoordinatorTests.swift</files>
  <behavior>
    - "Empty, unknown_blocking, or ineligible rows return unknown_blocking and create zero coordinator roots."
    - "Unknown routes, duplicate roots, cross-tab parents, missing parents, cycles, unsupported presentation/runtime pairs, and schema mismatch return closed stable denials."
    - "Every rejection leaves the active safe route and native state unchanged and never echoes rejected row content."
    - "Synthetic ordering tests assert determinism only; no adopter-facing order is promoted."
  </behavior>
  <action>Expand the shared vectors and both language tests with the complete D-02 validation matrix. Reuse `RouteInventory.ValidationError` conventions: stable rule ID plus safe field/opaque route ref only, never supplied values. Validate the parent graph exhaustively before any coordinator construction, require one closed root/push presentation and allowed runtime pairing, bind topology and manifest versions, and keep empty/incomplete inventory as `unknown_blocking`. Add an explicit test proving deterministic synthetic encoding does not manufacture a user-facing tab-order claim. Ensure Swift decoder/validator and Elixir compiler agree on status and reason vocabulary.</action>
  <acceptance_criteria>
    - All D-02 malformed graph and compatibility cases fail before UIKit or leaf construction.
    - Empty/incomplete input is represented as unknown_blocking in Elixir and Swift.
    - Denials contain only stable reason/field/opaque refs and no rejected payload.
    - Existing RouteInventory behavior and TODO-002 remain unchanged.
  </acceptance_criteria>
  <verify><automated>mix test test/crosswake/adoption/navigation_topology_test.exs &amp;&amp; swift test --package-path packages/crosswake-shell-core-ios --filter NavigationCoordinatorTests</automated></verify>
  <done>The tracer is fail closed across every known topology boundary while preserving unknown adopter input honestly.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| sanitized route rows -> topology compiler | Host-supplied planning input must pass RouteInventory and closed graph validation before artifact creation. |
| topology JSON -> Swift decoder | A generated artifact crosses language/version boundaries and must be rejected before state construction when incompatible. |
| topology destination -> activation resolver | A candidate route is untrusted until existing Phoenix/manifest authority approves it. |
| coordinator denial -> logs/tests/UI | Rejected values must collapse to stable non-echoing reasons. |

## STRIDE Threat Register — ASVS L1, block on high

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-161.1-01 | Tampering | route/topology input | high | mitigate | Tasks 1-2 admit only validated sanitized rows, bind versions, exhaustively validate graphs, and reject before mutation. |
| T-161.1-02 | Tampering / Repudiation | stale or duplicate transitions | high | mitigate | Tracer establishes the sole main-actor mutator; Plan 03 adds transition-ID replay/staleness vectors before breadth. |
| T-161.1-03 | Elevation of Privilege | restore/deep-link destination | high | mitigate | Every candidate uses ActivationCoordinator before stack construction; Plan 03 expands reconstruction. |
| T-161.1-04 | Information Disclosure | topology/denial output | high | mitigate | Closed fields and non-echoing tests prohibit identity, payload, token, URL, history, and rejected values. |
| T-161.1-05 | Information Disclosure / Repudiation | proof evidence | high | mitigate | Only opaque vectors exist here; Plans 05-06 retain closed assertion IDs/outcomes and scan final artifacts. |
| T-161.1-06 | Spoofing / Elevation of Privilege | silent fallback | high | mitigate | Denial preserves the current safe route and never enters generic WebView/history navigation. |
| T-161.1-SC | Tampering | dependency supply chain | low | accept | No package install is planned; repository code and Apple/Elixir toolchains already present are reused. |
</threat_model>

<source_coverage_audit>
| Source | ID | Contract | Coverage | Status |
|---|---|---|---|---|
| GOAL | Phase 161.1 | Native-feeling roots/details without generic navigation or leaf-owner transfer. | Root authorization tracer establishes the governing seam. | COVERED |
| REQ | NAV-01 | Compiled stable bounded topology. | Tasks 1-2. | COVERED |
| REQ | NAV-02 | Native state with resolver authority. | Task 1 tracer; UIKit expands in Plan 04. | COVERED |
| REQ | NAV-03 | Typed/versioned/fail-closed synchronization. | State-preserving tracer; protocol breadth in Plan 03. | COVERED |
| RESEARCH | Architecture | Compiler truth -> resolver -> main-actor coordinator. | Tasks 1-2 follow PATTERNS.md role matches. | COVERED |
| CONTEXT | D-01-D-03, D-06 | Compiled graph, unknown_blocking, activation-first native state, explicit denial. | Tasks 1-2. | COVERED |
| EDGE | nine rows | Spec-less unresolved edge semantics. | All nine retained verbatim as flagged assumptions. | FLAGGED |
| PROHIBITION | four bespoke constraints | Framework scope, privacy, proof honesty, confirmation fallback. | Descriptor-less flagged-unverified entries retained. | FLAGGED |
</source_coverage_audit>

<verification>
Run the cross-language tracer first, then the full negative matrix. No later plan may treat a topology as mountable until this plan proves compile, decode, authorization, and state-preserving denial.
</verification>

<success_criteria>
- A compiled opaque root reaches one authorized native stack entry through the real resolver seam.
- Empty/incomplete input remains unknown_blocking and all malformed graphs fail closed.
- No app-specific route/order fact, sensitive data, browser authority, generic navigation layer, or Android work appears.
</success_criteria>

<output>
Create `.planning/phases/161.1-first-adopter-ios-navigation-shell/161.1-01-SUMMARY.md` when done.
</output>
