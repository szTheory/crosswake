---
phase: 161.1
plan: "05"
subsystem: ios-shell
tags: [ios, uikit, swiftui, webkit, accessibility]
requires: [161.1-02, 161.1-04]
provides: [bounded-uikit-navigation-shell, shell-layout-contract]
affects: [first-adopter-ios-host]
tech-stack:
  added: []
  patterns: [UIKit tabs and stacks, retained authorized leaf controllers, document-start WebKit state]
key-files:
  created:
    - examples/ios_shell_host/CrosswakeShell/NavigationShellViewController.swift
    - examples/ios_shell_host/CrosswakeShellTests/NavigationShellTests.swift
    - examples/ios_shell_host/CrosswakeShellUITests/NavigationShellTests.swift
  modified:
    - examples/ios_shell_host/CrosswakeShell/CrosswakeShellApp.swift
    - examples/ios_shell_host/CrosswakeShell/LiveViewContainerViewController.swift
    - examples/ios_shell_host/CrosswakeShell.xcodeproj/project.pbxproj
decisions:
  - UIKit materializes only validated coordinator roots; blocked topology has no native chrome.
  - The document-start shell marker carries only fixed CSS defaults, while live layout values stay numeric and separate.
metrics:
  tasks_completed: 3
status: complete
---

# Phase 161.1 Plan 05: iOS Navigation Shell Summary

Bounded UIKit tabs/stacks now retain resolver-authorized leaf controllers while a main-frame WebKit contract publishes a non-identifying shell marker and independent layout facts.

## Completed Tasks

1. Mounted validated compiled roots in `UITabBarController` / `UINavigationController` containers with retained opaque-entry controllers and coordinator callback synchronization.
2. Added document-start `data-cw-native-shell="ios"` defaults plus coalesced safe-area and keyboard-bottom CSS delivery.
3. Added post-navigation semantic accessibility focus handoff and advisory UI-test membership without attachment retention.

## Verification

- `xcodebuild build-for-testing -project examples/ios_shell_host/CrosswakeShell.xcodeproj -scheme CrosswakeShell -destination 'platform=iOS Simulator,name=iPhone 17' CODE_SIGNING_ALLOWED=NO` — passed.
- `swift test --package-path packages/crosswake-shell-core-ios --filter NavigationCoordinatorTests` — passed (7 tests).
- `xcodebuild test -project examples/ios_shell_host/CrosswakeShell.xcodeproj -scheme CrosswakeShell -destination 'platform=iOS Simulator,name=iPhone 17' -only-testing:CrosswakeShellTests/NavigationShellTests CODE_SIGNING_ALLOWED=NO` — passed (2 tests).

## Deviations from Plan

### Auto-fixed Issues

1. [Rule 3 - Blocking issue] Registered the existing navigation-core sources in the reference host Xcode framework target.
- **Found during:** Task 1
- **Issue:** Earlier coordinator/topology implementation was absent from the Xcode target membership, so the host could not compile its existing navigation references.
- **Fix:** Added the four existing navigation-core source files to the framework target while adding the shell source.
- **Files modified:** `examples/ios_shell_host/CrosswakeShell.xcodeproj/project.pbxproj`
- **Commit:** `2cbd50ce`

**Total deviations:** 1 auto-fixed. **Impact:** Restores the host build without widening the runtime contract.

## Known Stubs

None.

## Self-Check: PASSED

- Created host shell and unit/UI test files exist.
- Task commits `c4c884ab`, `2cbd50ce`, `df139b6f`, and `b69b28f7` exist.
