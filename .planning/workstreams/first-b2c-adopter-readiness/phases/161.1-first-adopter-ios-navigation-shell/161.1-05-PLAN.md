---
phase: 161.1-first-adopter-ios-navigation-shell
plan: "05"
type: execute
wave: 4
depends_on: ["161.1-02", "161.1-04"]
files_modified:
  - examples/ios_shell_host/CrosswakeShell/NavigationShellViewController.swift
  - examples/ios_shell_host/CrosswakeShell/CrosswakeShellApp.swift
  - examples/ios_shell_host/CrosswakeShell/LiveViewContainerViewController.swift
  - examples/ios_shell_host/CrosswakeShellTests/NavigationShellTests.swift
  - examples/ios_shell_host/CrosswakeShellUITests/NavigationShellTests.swift
  - examples/ios_shell_host/CrosswakeShell.xcodeproj/project.pbxproj
autonomous: true
requirements: [NAV-02, NAV-04, NAV-05]
must_haves:
  truths:
    - "The existing SwiftUI host mounts one UITabBarController with one UINavigationController per compiled root, retains one authorized leaf controller per live stack entry, and preserves native tab/back/edge-swipe behavior without replacing RootSceneView leaf/denial ownership (D-07; NAV-02)."
    - "A fixed main-frame document-start script sets a non-identifying native-shell marker and safe default values before app CSS, without custom user agent or identity/route facts (D-08; NAV-05)."
    - "Four live safe-area CSS variables and a separate keyboard-bottom variable update through coalesced numeric injection-safe delivery; rotation and keyboard changes cannot conflate the two (D-08; NAV-04)."
    - "System appearance/motion, Dynamic Type, 44pt text-plus-icon/status controls, and exactly one completed-navigation semantic focus handoff are preserved; inset-only changes create no accessibility announcement (D-09)."
  artifacts:
    - path: "examples/ios_shell_host/CrosswakeShell/NavigationShellViewController.swift"
      provides: "Bounded UIKit tabs/stacks wrapped for SwiftUI with retained authorized leaf controllers."
    - path: "examples/ios_shell_host/CrosswakeShell/LiveViewContainerViewController.swift"
      provides: "Document-start marker/defaults and coalesced safe-area/keyboard delivery."
    - path: "examples/ios_shell_host/CrosswakeShellTests/NavigationShellTests.swift"
      provides: "Deterministic controller/layout/script contract tests."
    - path: "examples/ios_shell_host/CrosswakeShellUITests/NavigationShellTests.swift"
      provides: "Advisory user-observable tab, push/back/edge-swipe, marker, inset, and focus flows."
  key_links:
    - from: "NavigationShellViewController"
      to: "CrosswakeShell.navigationCoordinator"
      via: "UIKit delegate callbacks and published authorized stack state"
      pattern: "UITabBarController|UINavigationController|navigationCoordinator"
    - from: "NavigationStackEntry"
      to: "RootSceneView authorized leaf presentation"
      via: "one retained controller per entry"
      pattern: "controllerByEntry|ShellPresentation"
    - from: "safeAreaInsets/keyboard frame"
      to: "documentElement.style custom properties"
      via: "main-actor coalescing and safe numeric JSON delivery"
      pattern: "cw-safe-area|cw-keyboard-inset-bottom"
  prohibitions:
    - "requirement_id: NAV-01 | category: values | statement: MUST NOT turn the bounded first-adopter topology or coordinator into a generic navigation framework, generic WebView wrapper, browser-history router, or second host route authority. | status: unresolved | verification: flagged-unverified"
    - "requirement_id: NAV-03 | category: privacy | statement: MUST NOT carry or retain route payloads, account or device identifiers, credentials, tokens, browser history, URLs, or stable identity across navigation, layout, diagnostics, or proof seams. | status: unresolved | verification: flagged-unverified"
    - "requirement_id: NAV-06 | category: transparency | statement: MUST NOT present synthetic fixtures, package tests, simulator runs, or generated-host results as adopter-instance, physical-iPhone, Android, or generic-navigation proof. | status: unresolved | verification: flagged-unverified"
    - "requirement_id: NAV-07 | category: values | statement: MUST NOT add native alert or confirm, or describe Phoenix-owned confirmation as optional, before physical-iPhone proof, a demonstrated active-adopter blocker, and an explicit maintainer roadmap decision. | status: unresolved | verification: flagged-unverified"
flagged_assumptions:
  - "[UNRESOLVED-SPEC-PROBE][NAV-01 unclassified] CONTEXT D-01 through D-03 and REQUIREMENTS.md are exhaustive; no additional topology field, real route fact, or ownership transfer is inferred."
  - "[UNRESOLVED-SPEC-PROBE][NAV-02 adjacency] Only compiled root/parent relationships may be traversed; no arbitrary sibling, cross-tab, URL, or browser-history adjacency is inferred."
  - "[UNRESOLVED-SPEC-PROBE][NAV-02 empty input] Zero eligible validated rows remains unknown_blocking and produces no native tab or promoted shell claim."
  - "[UNRESOLVED-SPEC-PROBE][NAV-02 ordering] No user-facing tab ordering semantics are supplied; synthetic vectors may be deterministic, but adopter order remains unknown_blocking until sanitized input explicitly authorizes it."
  - "[UNRESOLVED-SPEC-PROBE][NAV-03 unclassified] D-04 through D-06 define the complete transition vocabulary; no third transition kind or browser-history state is inferred."
  - "[UNRESOLVED-SPEC-PROBE][NAV-04 unclassified] D-08 defines exactly four safe-area variables and one separate keyboard variable; no additional layout or identity fact is inferred."
  - "[UNRESOLVED-SPEC-PROBE][NAV-05 unclassified] D-08 defines a non-identifying document-start marker only; no custom user agent or host/account/device marker is inferred."
  - "[UNRESOLVED-SPEC-PROBE][NAV-06 unclassified] D-10 and D-11 define deterministic contract proof plus advisory simulator proof; physical-device and Android promotion remain excluded."
  - "[UNRESOLVED-SPEC-PROBE][NAV-07 unclassified] D-12 and the governing ADR define Phoenix-owned confirmation and the entire reversal condition; no native alert/confirm work is inferred."
---

<objective>
Materialize the compiled bounded graph as the first adopter's host-owned iOS navigation and layout experience.

Purpose: Deliver native tabs/back and synchronous shell-aware CSS while preserving the existing resolver, leaf containers, denials, and host presentation ownership.
Output: UIKit shell controller, SwiftUI mounting, WebKit marker/layout contract, host tests, advisory UI flows, and Xcode membership.
</objective>

<artifacts_this_phase_produces>
- New `NavigationShellView`, `NavigationShellViewController`, `NavigationShellControllerDelegate`, and private retained-entry controller map in `NavigationShellViewController.swift`.
- Changed `RootSceneView` mounts the navigation shell only for a validated promotable topology and preserves existing `.liveView`, `.nativeCapture`, `.requiredPack`, `.denied`, and `.booting` leaf presentations.
- New fixed marker `data-cw-native-shell="ios"` and CSS fields `--cw-safe-area-top`, `--cw-safe-area-right`, `--cw-safe-area-bottom`, `--cw-safe-area-left`, and `--cw-keyboard-inset-bottom`.
- New private `ShellLayoutInsets`, main-actor equality/coalescing state, safe numeric script builder, and navigation-completion focus handoff; no per-inset accessibility notification.
- New `NavigationShellTests.swift` in unit and UI targets and exact project membership; no custom UA, app-specific tab labels/order, generic renderer, native alert/confirm, screenshot/evidence retention, device CI, or Android file.
</artifacts_this_phase_produces>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/161.1-first-adopter-ios-navigation-shell/161.1-CONTEXT.md
@.planning/phases/161.1-first-adopter-ios-navigation-shell/161.1-RESEARCH.md
@.planning/phases/161.1-first-adopter-ios-navigation-shell/161.1-PATTERNS.md
@.planning/phases/161.1-first-adopter-ios-navigation-shell/161.1-02-SUMMARY.md
@.planning/phases/161.1-first-adopter-ios-navigation-shell/161.1-04-SUMMARY.md
@examples/ios_shell_host/CrosswakeShell/CrosswakeShellApp.swift
@examples/ios_shell_host/CrosswakeShell/LiveViewContainerViewController.swift
@examples/ios_shell_host/CrosswakeShellUITests/RequiredPackViewAccessibilityTests.swift

<interfaces>
- Follow the existing `UIViewControllerRepresentable` and root dependency-injection shape; the host supplies presentation metadata, never topology truth.
- Keep `WKWebView.allowsBackForwardNavigationGestures = false`; native state comes from `NavigationCoordinator` and authorized in-frame patch delivery.
- Use system UIKit tab/navigation appearance and accessibility APIs; no custom routing, gesture, transition, theme, or user-agent subsystem.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Mount compiled roots as retained UIKit tab stacks</name>
  <reversibility rating="reversible">D-07 uses standard UIKit containers behind the host composition boundary and can be adjusted without changing the published topology or transition protocol.</reversibility>
  <read_first>
    - .planning/phases/161.1-first-adopter-ios-navigation-shell/161.1-CONTEXT.md
    - .planning/phases/161.1-first-adopter-ios-navigation-shell/161.1-PATTERNS.md
    - examples/ios_shell_host/CrosswakeShell/CrosswakeShellApp.swift
    - examples/ios_shell_host/CrosswakeShell/LiveViewContainerViewController.swift
    - packages/crosswake-shell-core-ios/Sources/CrosswakeShellCore/NavigationCoordinator.swift
  </read_first>
  <files>examples/ios_shell_host/CrosswakeShell/NavigationShellViewController.swift, examples/ios_shell_host/CrosswakeShell/CrosswakeShellApp.swift, examples/ios_shell_host/CrosswakeShellTests/NavigationShellTests.swift, examples/ios_shell_host/CrosswakeShell.xcodeproj/project.pbxproj</files>
  <behavior>
    - "A promotable synthetic topology creates one UITabBarController and one UINavigationController per compiled root; unknown_blocking creates none and preserves the existing denial/boot surface."
    - "Tab selection, programmatic push, back-button pop, completed edge swipe, and canceled edge swipe delegate to the main-actor coordinator."
    - "Returning to a tab reuses each live stack entry's controller and state; popping releases only the removed entry."
    - "Each retained controller comes from the authorized ShellPresentation owner; no generic WebView renderer or host route matcher exists."
  </behavior>
  <action>Create the UIKit controller and SwiftUI representable after D-07. Materialize only roots from the validated coordinator. Build one navigation controller per root and synchronize UIKit from coordinator state without recursive delegate feedback. Retain one leaf controller keyed by immutable stack-entry identity and release it on actual pop/topology invalidation; never key by URL, browser history, account, or mutable path. Feed tab selection and navigation-controller completion/cancellation callbacks back to coordinator, using the selected deployment target's transition coordinator to distinguish canceled interactive pops. Preserve RootSceneView's existing leaf/denial/required-pack/native-screen branches and host-owned label/SF Symbol/copy inputs. Use system bars, standard motion, system colors, Dynamic Type, and 44pt targets per D-09.</action>
  <acceptance_criteria>
    - UIKit container counts exactly match the compiled synthetic root set; unknown_blocking mounts no fabricated tabs.
    - Tab return retains authorized leaf state; completed/canceled pop behavior is discriminated.
    - RootSceneView remains the owner of leaf presentation and explicit denial.
    - No browser history, generic router/renderer, custom gesture/animation/theme, or real adopter route fact is introduced.
  </acceptance_criteria>
  <verify><automated>xcodebuild build-for-testing -project examples/ios_shell_host/CrosswakeShell.xcodeproj -scheme CrosswakeShell -destination 'platform=iOS Simulator,name=iPhone 17' CODE_SIGNING_ALLOWED=NO &amp;&amp; swift test --package-path packages/crosswake-shell-core-ios --filter NavigationCoordinatorTests</automated></verify>
  <done>The existing host composition displays the bounded compiled topology through native system tabs/stacks while every leaf remains resolver-owned.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Install synchronous marker and independent live inset facts</name>
  <reversibility rating="reversible">D-08's CSS variable names are contract fields, while the internal coalescing and lifecycle mechanics remain local to the host container.</reversibility>
  <read_first>
    - .planning/phases/161.1-first-adopter-ios-navigation-shell/161.1-CONTEXT.md
    - examples/ios_shell_host/CrosswakeShell/LiveViewContainerViewController.swift
    - packages/crosswake-shell-core-ios/Sources/CrosswakeShellCore/CrosswakeShell.swift
    - packages/crosswake-shell-core-ios/Tests/CrosswakeShellCoreTests/BridgeChannelTests.swift
  </read_first>
  <files>examples/ios_shell_host/CrosswakeShell/LiveViewContainerViewController.swift, examples/ios_shell_host/CrosswakeShellTests/NavigationShellTests.swift</files>
  <behavior>
    - "A fixed main-frame atDocumentStart script sets the shell marker and all five CSS values to safe defaults before page CSS executes."
    - "Safe-area top/right/bottom/left values update independently from keyboard bottom through numeric-only safe serialization."
    - "Equal/bursty layout values coalesce to one main-actor delivery; rotation and keyboard hide/show produce the correct separate snapshots."
    - "Source and runtime contracts contain no customUserAgent and no account, route payload, token, URL, device, or stable ID fact."
  </behavior>
  <action>Extend the existing WebKit configuration with one fixed `forMainFrameOnly: true` document-start script per D-08. Set only `document.documentElement.dataset.cwNativeShell = "ios"` and zero defaults for the exact five CSS variables. Observe `viewSafeAreaInsetsDidChange` and keyboard-frame notifications, calculate numeric pixel values relative to the visible view, keep safe-area and occlusion separate, drop equal snapshots, and coalesce changes on the main actor. Deliver values through the existing injection-safe JSON/string-literal technique, never interpolation of host-influenced text. Keep back-forward navigation gestures disabled and do not set a custom user agent.</action>
  <acceptance_criteria>
    - Source-contract tests prove marker/defaults are at document start and main-frame only.
    - Rotation and keyboard vectors prove four safe-area properties and keyboard bottom are independent.
    - Repeated equal/bursty updates emit once and carry numeric values only.
    - Forbidden identity/history/URL/user-agent fields and unsafe interpolation are absent.
  </acceptance_criteria>
  <verify><automated>xcodebuild test -project examples/ios_shell_host/CrosswakeShell.xcodeproj -scheme CrosswakeShell -destination 'platform=iOS Simulator,name=iPhone 17' -only-testing:CrosswakeShellTests/NavigationShellTests CODE_SIGNING_ALLOWED=NO</automated></verify>
  <done>App CSS sees a synchronous privacy-safe shell marker and correct live layout facts without turning WebKit into navigation authority.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Prove system navigation and one semantic focus handoff</name>
  <reversibility rating="reversible">Accessibility identifiers and host presentation can evolve while the semantic D-09 contract remains stable.</reversibility>
  <read_first>
    - .planning/phases/161.1-first-adopter-ios-navigation-shell/161.1-CONTEXT.md
    - examples/ios_shell_host/CrosswakeShell/NavigationShellViewController.swift
    - examples/ios_shell_host/CrosswakeShell/LiveViewContainerViewController.swift
    - examples/ios_shell_host/CrosswakeShellUITests/RequiredPackViewAccessibilityTests.swift
    - examples/ios_shell_host/CrosswakeShell.xcodeproj/project.pbxproj
  </read_first>
  <files>examples/ios_shell_host/CrosswakeShell/NavigationShellViewController.swift, examples/ios_shell_host/CrosswakeShellTests/NavigationShellTests.swift, examples/ios_shell_host/CrosswakeShellUITests/NavigationShellTests.swift, examples/ios_shell_host/CrosswakeShell.xcodeproj/project.pbxproj</files>
  <behavior>
    - "Completed tab/push/pop navigation posts one semantic screen-change focus handoff to the authorized destination; canceled or denied navigation posts none."
    - "Safe-area/keyboard changes post no accessibility focus/state announcement."
    - "XXXL type, light/dark/system appearance, Reduce Motion, text-plus-icon/status, and 44pt targets remain reachable and untruncated."
    - "Advisory UI flows exercise tabs, push/back, completed/canceled edge swipe, marker/insets, and focus without retaining screenshots or raw output."
  </behavior>
  <action>Add a single post-completion accessibility handoff owned by `NavigationShellViewController` per D-09; target a host-supplied semantic destination element and suppress it for denial, cancellation, duplicate transitions, and inset-only changes. Add deterministic unit/source contract tests for notification count, target, system components, target size, Dynamic Type, Reduce Motion, and forbidden announcements. Add the UI test file and exact Xcode target membership. The XCUITest run is advisory evidence only: use stable identifiers and user-observable states, disable attachment/screenshot retention, and never let simulator success promote NAV-06 or physical-device support.</action>
  <acceptance_criteria>
    - Exactly one completed-navigation focus handoff is mechanically asserted; inset/cancel/denial changes assert zero.
    - System controls remain reachable at accessibility sizes with 44pt targets and text in addition to icon/status.
    - UI test source covers the bounded flows and retains no screenshot, `.xcresult`, raw log, route path/history, or device identifier.
    - Android and native alert/confirm sources remain untouched.
  </acceptance_criteria>
  <verify><automated>xcodebuild test -project examples/ios_shell_host/CrosswakeShell.xcodeproj -scheme CrosswakeShell -destination 'platform=iOS Simulator,name=iPhone 17' -only-testing:CrosswakeShellTests/NavigationShellTests CODE_SIGNING_ALLOWED=NO</automated></verify>
  <done>The bounded shell uses platform-native behavior and accessible semantics, with simulator UI execution explicitly advisory.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| coordinator state -> UIKit containers | Only validated authorized stack state may create/retain controllers. |
| UIKit callbacks -> coordinator | Tab/pop/gesture events are candidates and must be serialized/revalidated. |
| UIKit layout/keyboard -> WebKit script | Numeric layout facts cross into document state and must be injection-safe/non-identifying. |
| UI tests -> retained claims | Simulator behavior is advisory and raw collateral is unsafe to retain. |

## STRIDE Threat Register — ASVS L1, block on high

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-161.1-01 | Tampering | mounted topology | high | mitigate | Task 1 mounts only validated coordinator roots; unknown_blocking mounts none. |
| T-161.1-02 | Tampering / Repudiation | tab/pop callbacks | high | mitigate | Task 1 funnels callbacks to the sole main-actor coordinator and discriminates canceled/completed gestures. |
| T-161.1-03 | Elevation of Privilege | restored/selected leaf | high | mitigate | Retained controllers originate only from resolver-approved ShellPresentation entries. |
| T-161.1-04 | Information Disclosure | marker/layout script | high | mitigate | Task 2 uses fixed marker/defaults and numeric-only safe serialization; forbidden-field/source tests block leakage. |
| T-161.1-05 | Information Disclosure / Repudiation | UI-test collateral | high | mitigate | Task 3 disables retained screenshots/raw output and labels simulator execution advisory only. |
| T-161.1-06 | Spoofing / Elevation of Privilege | fallback UI | high | mitigate | Existing explicit denial remains mounted; no generic WebView/history or native alert/confirm fallback. |
| T-161.1-SC | Tampering | dependency supply chain | low | accept | Only UIKit, SwiftUI, WebKit, XCTest, and repository sources are used. |
</threat_model>

<source_coverage_audit>
| Source | ID | Contract | Coverage | Status |
|---|---|---|---|---|
| GOAL | Phase 161.1 | Roots/details feel native while leaf owner remains unchanged. | Tasks 1 and 3. | COVERED |
| REQ | NAV-02 | Native tabs/push/back/edge swipe, resolver authoritative. | Task 1. | COVERED |
| REQ | NAV-04 | Four safe-area vars plus separate keyboard. | Task 2. | COVERED |
| REQ | NAV-05 | Synchronous non-identifying marker. | Task 2. | COVERED |
| RESEARCH | UIKit/WebKit/accessibility | Standard containers, retained entries, document-start/coalescing. | Tasks 1-3 follow mapped analogs. | COVERED |
| CONTEXT | D-07-D-09 | Native shell, live marker/insets, system UX/accessibility. | Tasks 1-3. | COVERED |
| EDGE | nine rows | Unresolved semantics. | All retained. | FLAGGED |
| PROHIBITION | four bespoke constraints | Scope/privacy/proof/fallback. | Descriptor-less flagged-unverified. | FLAGGED |
</source_coverage_audit>

<verification>
Primary gates are the Swift coordinator suite, build-for-testing, and deterministic host unit/source contracts. Run XCUITest only as advisory observation; its success cannot promote this phase or satisfy physical-device proof.
</verification>

<success_criteria>
- Validated roots render as system UIKit tabs/stacks and retain authorized leaf state.
- Patch/navigation/pop semantics remain synchronized through the coordinator.
- Marker and all layout variables are timely, separate, coalesced, injection-safe, and non-identifying.
- Accessibility and system behavior are deterministic; UI-runtime results remain advisory.
</success_criteria>

<output>
Create `.planning/phases/161.1-first-adopter-ios-navigation-shell/161.1-05-SUMMARY.md` when done.
</output>
