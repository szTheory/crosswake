---
phase: 161.1-first-adopter-ios-navigation-shell
plan: "10"
type: tracer
wave: 9
depends_on: ["161.1-09"]
files_modified:
  - examples/ios_shell_host/CrosswakeShell/CrosswakeShellApp.swift
  - examples/ios_shell_host/CrosswakeShell/NavigationShellViewController.swift
  - examples/ios_shell_host/CrosswakeShell/LiveViewContainerViewController.swift
  - examples/ios_shell_host/CrosswakeShellTests/NavigationShellTests.swift
  - examples/ios_shell_host/CrosswakeShellUITests/NavigationShellTests.swift
  - examples/ios_shell_host/CrosswakeShell.xcodeproj/project.pbxproj
autonomous: true
gap_closure: true
requirements: [NAV-01, NAV-02, NAV-03, NAV-04, NAV-05, NAV-06]
must_haves:
  truths:
    - "A validated opaque synthetic topology mounts the production NavigationCoordinator and NavigationShellViewController in the existing reference host; resolver authorization occurs before every selected-tab or stack mutation (D-01, D-02, D-03, D-07; NAV-01, NAV-02)."
    - "The host XCTest/XCUITest suite observes tab selection, exactly one push_navigate, push_patch stack-depth invariance, completed and cancelled native pop/edge-swipe, and no duplicate stack entry (D-04, D-05, D-06; NAV-03)."
    - "The mounted production LiveViewContainerViewController emits four safe-area values separately from keyboard occlusion and performs exactly one accessibility focus handoff only after completed navigation (D-08, D-09; NAV-04, NAV-05, NAV-06)."
    - "Synthetic host execution remains advisory and creates no adopter-instance, physical-iPhone, Android, generic-navigation, or native-confirmation claim (D-10, D-11, D-12)."
  artifacts:
    - path: "examples/ios_shell_host/CrosswakeShellTests/NavigationShellTests.swift"
      provides: "Production-container integration tests for resolver-first stacks, pop cancellation, layout delivery, and focus."
    - path: "examples/ios_shell_host/CrosswakeShellUITests/NavigationShellTests.swift"
      provides: "Simulator-driven native tab, navigate, patch, completed/cancelled swipe, layout, and focus observations."
    - path: "examples/ios_shell_host/CrosswakeShell/CrosswakeShellApp.swift"
      provides: "Exact test-environment-only synthetic topology launch seam that composes production navigation types."
  key_links:
    - from: "CrosswakeShellApp test launch seam"
      to: "NavigationCoordinator and NavigationShellViewController"
      via: "validated opaque synthetic manifest/topology and the production resolver"
      pattern: "NavigationCoordinator|NavigationShellView|NavigationShellViewController"
    - from: "native tab/push/pop gestures"
      to: "NavigationCoordinator selectedTabID/stacks/activeRouteID"
      via: "production UIKit delegates and transition application"
      pattern: "selectRoot|apply|completeNativePop"
    - from: "safe-area/keyboard lifecycle"
      to: "document-root CSS facts"
      via: "production coalescing and JavaScript delivery sink"
      pattern: "viewSafeAreaInsetsDidChange|keyboardWillChangeFrame|cw-keyboard-inset-bottom"
---

<objective>
Close the production-observation gap with one vertical host/simulator tracer through the real topology, coordinator, UIKit shell, WebKit layout, and accessibility seams.

Purpose: Replace compile-only confidence with executable behavior evidence from the exact production objects that Phase 161.1 ships.
Output: A bounded test-only synthetic launch composition plus host XCTest/XCUITest coverage of every missing navigation/layout/focus behavior.
</objective>

## Artifacts this phase produces

- A test-environment-only opaque synthetic topology composition in the existing reference iOS host; it invokes production `NavigationCoordinator`, `NavigationShellViewController`, and `LiveViewContainerViewController` and is not a second route authority.
- Host XCTest/XCUITest observations for tab selection, one `push_navigate`, patch depth invariance, completed/cancelled pop or edge swipe, independent safe-area/keyboard updates, and completed-navigation focus.
- Stable closed test markers suitable for ephemeral transcript reduction in Plan 12; no raw output or device claim is retained.

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/ADR-FIRST-B2C-ADOPTER.md
@.planning/FIRST-B2C-ADOPTER-ROUTE-POLICY-MAP.md
@.planning/phases/161.1-first-adopter-ios-navigation-shell/161.1-CONTEXT.md
@.planning/phases/161.1-first-adopter-ios-navigation-shell/161.1-RESEARCH.md
@.planning/phases/161.1-first-adopter-ios-navigation-shell/161.1-PATTERNS.md
@.planning/phases/161.1-first-adopter-ios-navigation-shell/161.1-VERIFICATION.md
@.planning/phases/161.1-first-adopter-ios-navigation-shell/161.1-05-SUMMARY.md
@examples/ios_shell_host/CrosswakeShell/CrosswakeShellApp.swift
@examples/ios_shell_host/CrosswakeShell/NavigationShellViewController.swift
@examples/ios_shell_host/CrosswakeShell/LiveViewContainerViewController.swift
@packages/crosswake-shell-core-ios/Sources/CrosswakeShellCore/NavigationCoordinator.swift

<interfaces>
- The synthetic launch seam is accepted only under one exact XCTest/XCUITest process argument and constructs opaque `route-`/`nav-` fixtures locally; normal host startup still loads only the compiled manifest topology per D-01/D-02.
- Tests drive public/internal production methods and UIKit delegate/lifecycle callbacks. They may add injectable observation sinks around existing side effects, but must not duplicate coordinator, stack, layout, focus, or route-resolution logic.
- The reference host remains iOS-only and advisory. TODO-002 remains `unknown_blocking`; Phase 162 alone owns physical-iPhone promotion.
</interfaces>
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Mount and drive one authorized synthetic topology through production UIKit state</name>
  <reversibility rating="reversible">The test-only composition and observation seams are local to the reference host and do not change the published topology or transition protocols.</reversibility>
  <read_first>
    - AGENTS.md
    - .planning/phases/161.1-first-adopter-ios-navigation-shell/161.1-CONTEXT.md
    - .planning/phases/161.1-first-adopter-ios-navigation-shell/161.1-VERIFICATION.md
    - packages/crosswake-shell-core-ios/Sources/CrosswakeShellCore/NavigationCoordinator.swift
    - packages/crosswake-shell-core-ios/Sources/CrosswakeShellCore/NavigationTopology.swift
    - packages/crosswake-shell-core-ios/Sources/CrosswakeShellCore/NavigationTransition.swift
    - examples/ios_shell_host/CrosswakeShell/CrosswakeShellApp.swift
    - examples/ios_shell_host/CrosswakeShell/NavigationShellViewController.swift
    - examples/ios_shell_host/CrosswakeShellTests/NavigationShellTests.swift
  </read_first>
  <files>examples/ios_shell_host/CrosswakeShell/CrosswakeShellApp.swift, examples/ios_shell_host/CrosswakeShell/NavigationShellViewController.swift, examples/ios_shell_host/CrosswakeShellTests/NavigationShellTests.swift, examples/ios_shell_host/CrosswakeShell.xcodeproj/project.pbxproj</files>
  <behavior>
    - "RED: a validated two-root/one-push opaque topology mounts the production UITabBarController and one UINavigationController per root only after the injected production resolver authorizes each root."
    - "RED: selecting the second native tab changes the coordinator selected tab and active route through NavigationShellViewController's real delegate path."
    - "RED: applying one compatible push_navigate grows the selected production stack by exactly one; replaying the same transition changes neither coordinator nor UIKit depth."
    - "RED: applying push_patch to the current leaf invokes the production patch sink once and preserves both coordinator and UINavigationController depth."
    - "Denied or malformed candidates leave the visible tab, active route, retained controllers, and both stacks unchanged."
  </behavior>
  <action>Add an exact test-process launch branch to the existing host composition that builds only opaque synthetic `NavigationTopology`, `ShellManifest`, and resolver results, then mounts the normal `NavigationShellView`/`NavigationShellViewController`; normal startup must never consult this branch per D-01/D-02. Give the production controller stable test identifiers and narrowly injectable observation hooks only where XCTest cannot inspect UIKit state directly. Write host integration tests before implementation, instantiate and load the real controller, and drive its actual tab delegate plus `NavigationCoordinator.apply` for `push_navigate` and `push_patch`. Assert resolver call order precedes mutation, UIKit stack state mirrors coordinator state, duplicate navigate is inert, patch depth is invariant, and leaf presentations remain resolver-owned per D-03-D-07. Emit only stable assertion IDs from tests; never include routes, paths, labels, payloads, accounts, devices, tokens, or browser history.</action>
  <acceptance_criteria>
    - The host integration test mounts the real production coordinator and controller from a validated synthetic topology, not a Boolean/model double of navigation behavior.
    - Tab selection is observed through the production delegate; one navigate grows both production stacks once; its duplicate is inert; patch changes no stack depth.
    - Authorization order and state preservation on denial are asserted.
    - Normal app startup remains compiled-manifest-only and TODO-002 remains unknown_blocking.
  </acceptance_criteria>
  <verify><automated>xcodebuild test -project examples/ios_shell_host/CrosswakeShell.xcodeproj -scheme CrosswakeShell -destination 'platform=iOS Simulator,name=iPhone 17' -only-testing:CrosswakeShellTests/NavigationShellTests CODE_SIGNING_ALLOWED=NO</automated></verify>
  <done>The real reference-host navigation container proves tab, navigate-once, and patch-depth behavior without introducing a second production topology source.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Exercise completed/cancelled native pop, live layout separation, and focus on simulator</name>
  <reversibility rating="reversible">Test identifiers and injectable side-effect sinks can change without altering the D-05/D-08/D-09 contracts.</reversibility>
  <read_first>
    - .planning/phases/161.1-first-adopter-ios-navigation-shell/161.1-CONTEXT.md
    - .planning/phases/161.1-first-adopter-ios-navigation-shell/161.1-VERIFICATION.md
    - examples/ios_shell_host/CrosswakeShell/NavigationShellViewController.swift
    - examples/ios_shell_host/CrosswakeShell/LiveViewContainerViewController.swift
    - examples/ios_shell_host/CrosswakeShellTests/NavigationShellTests.swift
    - examples/ios_shell_host/CrosswakeShellUITests/NavigationShellTests.swift
  </read_first>
  <files>examples/ios_shell_host/CrosswakeShell/NavigationShellViewController.swift, examples/ios_shell_host/CrosswakeShell/LiveViewContainerViewController.swift, examples/ios_shell_host/CrosswakeShellTests/NavigationShellTests.swift, examples/ios_shell_host/CrosswakeShellUITests/NavigationShellTests.swift, examples/ios_shell_host/CrosswakeShell.xcodeproj/project.pbxproj</files>
  <behavior>
    - "RED: a completed production pop/edge swipe commits one coordinator pop and focuses the authorized destination; a cancelled interactive gesture preserves active route, coordinator depth, UIKit depth, and focus count."
    - "RED: changing host safe-area insets updates the four safe-area CSS properties while keyboard bottom remains unchanged."
    - "RED: keyboard show/hide notifications update only the keyboard property for a constant safe area; equal/coalesced snapshots emit once."
    - "RED: completed tab/push/pop produces one semantic screen-change focus handoff; patch, cancellation, denial, and layout-only updates produce none."
    - "XCUITest launches the exact synthetic branch, taps tabs/navigate, checks patch depth, completes and cancels an edge gesture, and observes closed layout/focus markers from production state."
  </behavior>
  <action>Make production side effects test-observable without changing their authority: inject a default `UIAccessibility.post` sink into `NavigationShellViewController`, and inject a default WebKit JavaScript evaluation sink into `LiveViewContainerViewController`; lifecycle calculations and coordinator calls must remain production code. In XCTest, mount real views in a UIWindow, change `additionalSafeAreaInsets`, invoke the safe-area lifecycle, post real keyboard-frame notifications, drain main-actor coalescing, and assert the exact five numeric CSS facts with safe area independent from keyboard per D-08. Drive `UINavigationController` completion/cancellation behavior and assert D-05 state plus exactly one D-09 focus handoff only after completion. Replace the unrelated host UI test with a bounded flow launched via the exact test argument; use native controls/gestures and stable accessibility values to distinguish completed from cancelled swipe, patch depth, selected tab, active route class, layout separation, and focus completion. Do not attach screenshots or retain simulator transcripts.</action>
  <acceptance_criteria>
    - Host XCTest discriminates completed and cancelled production pop paths and asserts focus/no-focus outcomes.
    - Host XCTest invokes actual safe-area and keyboard lifecycle inputs and observes five independent numeric CSS updates from the production delivery path.
    - Host XCUITest drives the production container and covers every missing verification behavior with stable closed markers.
    - No layout event announces accessibility state; no simulator result claims device, adopter, Android, or generic-navigation support.
  </acceptance_criteria>
  <verify><automated>xcodebuild test -project examples/ios_shell_host/CrosswakeShell.xcodeproj -scheme CrosswakeShell -destination 'platform=iOS Simulator,name=iPhone 17' -only-testing:CrosswakeShellTests/NavigationShellTests -only-testing:CrosswakeShellUITests/NavigationShellTests CODE_SIGNING_ALLOWED=NO</automated></verify>
  <done>The reference host's real UIKit/WebKit objects are exercised across navigation, gesture, layout, and focus behavior, with advisory-only simulator scope.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| test launch input -> host composition | Only one exact test-process argument may select opaque synthetic topology; production startup remains compiled-manifest-only. |
| synthetic destination -> production resolver/coordinator | Test fixtures are candidates and must traverse the real resolver before state mutation. |
| UIKit/WebKit side effects -> test observations | Observation hooks must not become alternate mutation, route, or rendering authority. |
| simulator transcript -> phase claim | Runtime output is ephemeral advisory evidence and may not promote device/adopter/platform support. |

## STRIDE Threat Register — ASVS L1, block on high

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-161.1-G10-01 | Spoofing / Elevation of Privilege | synthetic launch branch | high | mitigate | Task 1 requires an exact test-process argument, opaque fixtures, and the production resolver; normal startup cannot select it. |
| T-161.1-G10-02 | Tampering | coordinator/UIKit state | high | mitigate | Tasks 1-2 assert resolver-before-mutation, exact depths, duplicate inertness, and cancelled-pop state preservation. |
| T-161.1-G10-03 | Tampering | layout/focus observation seams | high | mitigate | Task 2 injects only sinks around existing production side effects; calculations and mutation stay in production owners. |
| T-161.1-G10-04 | Information Disclosure | fixtures/test output | high | mitigate | Tests use opaque IDs and stable markers only; no URL, payload, history, identity, screenshot, or raw retained output. |
| T-161.1-G10-05 | Repudiation | simulator promotion | high | mitigate | All markers are advisory and Plan 12 preserves Phase-162-only promotion. |
| T-161.1-G10-SC | Tampering | dependency supply chain | low | accept | No package or dependency installation occurs. |
</threat_model>

<source_coverage_audit>
| Source | ID | Feature / requirement | Coverage | Status |
|---|---|---|---|---|
| GOAL | Phase 161.1 gap | Native roots/details must be behaviorally proven without generic navigation or leaf-owner transfer. | Tasks 1-2 use production host objects. | COVERED |
| REQ | NAV-01 | Validated compiled topology and leaf ownership. | Task 1 synthetic validated topology through real resolver. | COVERED |
| REQ | NAV-02 | Native tabs/push/back with resolver authority. | Tasks 1-2. | COVERED |
| REQ | NAV-03 | Patch invariance, navigate once, fail-closed sync. | Task 1. | COVERED |
| REQ | NAV-04 | Four safe-area facts plus separate keyboard. | Task 2. | COVERED |
| REQ | NAV-05 | Declarative marker and non-identifying layout surface. | Existing marker plus Task 2 runtime observation. | COVERED |
| REQ | NAV-06 | Executable simulator/accessibility proof without broader claim. | Task 2. | COVERED |
| RESEARCH | A1 | Interactive-pop completion/cancellation required implementation-time validation. | Task 2 validates selected UIKit seam. | COVERED |
| CONTEXT | D-01-D-09 | Single topology authority, resolver-first state, exact effects, UIKit/layout/focus. | Tasks 1-2 cite and exercise all decisions. | COVERED |
| CONTEXT | D-10-D-12 | Generated/advisory/support boundaries. | Preserved here; Plans 11-12 close proof publication. | COVERED |
| CONTEXT | Deferred ideas | Generic/Android navigation, renderer, modal breadth, telemetry, device CI, native confirm. | No task includes them. | EXCLUDED |
</source_coverage_audit>

<verification>
Run the named host XCTest and XCUITest targets on one available iPhone simulator. Passing proves only the bounded synthetic reference-host behavior; it does not establish real adopter topology or physical-device support.
</verification>

<success_criteria>
- Production coordinator/UIKit/WebKit behavior is observed for every missing navigation, layout, and focus case.
- The synthetic fixture is test-only, opaque, resolver-authorized, and unavailable to normal startup.
- All results remain advisory and iOS-only; Android, native confirmation, generic navigation, and Phase 162 responsibilities remain untouched.
</success_criteria>

<output>
Create `.planning/phases/161.1-first-adopter-ios-navigation-shell/161.1-10-SUMMARY.md` when done.
</output>
