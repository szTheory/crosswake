# Phase 161.1: first-adopter-ios-navigation-shell - Context

**Gathered:** 2026-08-04
**Status:** Ready for planning

<domain>
## Phase Boundary

Deliver one bounded, host-owned iOS shell for the first adopter's confirmed root tabs and pushed
details. A compiled topology preserves Phoenix route-policy ownership while native state owns tabs,
push/pop, edge-swipe back, deep-link reconstruction, and live layout signals. The web/offline/native
leaf runtime remains selected by the existing resolver; this phase does not become a general navigation
framework, a native renderer for Phoenix routes, or an Android feature.

Concrete topology may be compiled only from validated sanitized TODO-002 route rows. Until then it
remains `unknown_blocking`; no app-specific tab, path, or restoration fact is inferred. Simulator proof
is advisory. Phase 162 alone owns physical-iPhone promotion.

</domain>

<decisions>
## Implementation Decisions

### Compiled bounded topology

- **D-01:** Compile a versioned `NavigationTopology` with the route manifest from validated sanitized
  route rows only. Entries contain stable opaque `route_id`, `root_tab_id`, closed `root` or `push`
  presentation, optional `parent_route_id`, and closed deep-link/restoration posture. Do not add host
  Swift topology configuration as a second source of route truth. — **Reversibility:** costly — a
  published manifest/topology schema requires a compatibility migration across generated shells and
  host fixtures.
- **D-02:** Fail closed on unknown routes, duplicate roots, cross-tab parents, invalid or cyclic parent
  graphs, unsupported runtime/presentation combinations, and topology/manifest version mismatch. If
  TODO-002 does not validate the needed rows, retain `unknown_blocking` rather than fabricating a
  navigation graph.
- **D-03:** Let iOS own live selected-tab and stack state, while reconstructing deep links or restoration
  only by reauthorizing each destination through the existing activation coordinator and route resolver.
  Invalid reconstruction retains the current safe route or presents the existing explicit denial surface;
  it never falls back to generic WebView navigation.

### Typed stack synchronization

- **D-04:** Use one low-frequency, typed, versioned web/native transition envelope with a transition ID,
  closed kind (`push_patch` or `push_navigate`), destination route reference, and only an opaque safe
  restoration reference where needed. Dedupe/replay handling keys on the transition ID; no route
  payload, account, device, token, or browser-history state crosses the seam. — **Reversibility:**
  costly — the protocol is consumed by the Phoenix/web client, iOS shell, generated vectors, and host
  proof adapter.
- **D-05:** `push_patch` changes the current web route in its existing container and never grows native
  stack depth. `push_navigate` authorizes the target then creates exactly one native push. Native tab
  selections, native back/edge-swipe pops, deep links, and restoration all use the same coordinator;
  browser history is not native navigation authority.
- **D-06:** Serialize native mutations on the UI/main-actor boundary, reject stale/duplicate/invalid
  transitions without changing the active route, and expose the existing calm, explicit denial rather
  than silently degrading. Phoenix-owned confirmation remains the required fallback; no native
  alert/confirm capability is introduced.

### Native shell and layout experience

- **D-07:** Implement the bounded graph with a UIKit `UITabBarController` and one
  `UINavigationController` per compiled root tab, wrapped by the existing SwiftUI composition. Lazily
  retain one route container per active stack entry so normal tab return behavior does not lose the
  active web/offline state. The host owns tab labels, SF Symbols, learner copy, and presentation polish;
  Crosswake owns topology, synchronization semantics, and denials.
- **D-08:** At document start, install a non-identifying declarative native-shell marker and safe default
  CSS values before app CSS evaluates. Publish `--cw-safe-area-top`, `--cw-safe-area-right`,
  `--cw-safe-area-bottom`, and `--cw-safe-area-left` independently of
  `--cw-keyboard-inset-bottom`; coalesce layout updates. Never use a custom user agent or expose account,
  route payload, device, or stable identity data.
- **D-09:** Use system tab/navigation appearance, Dynamic Type, 44pt targets, text plus icon/status,
  platform push/pop motion with Reduce Motion respected, and standard light/dark/system behavior. After
  completed navigation use one semantic accessibility focus handoff; do not announce focus or state
  changes merely because insets changed. Host UI keeps learner copy task-oriented and hides shell/backend
  mechanics unless an explicit recovery action needs them.

### Proof, support truth, and developer experience

- **D-10:** Extend the Phase 159 generated, host-owned proof lane rather than create a demo app or
  parallel navigation harness. Deterministic graph and contract vectors cover topology validation,
  compatibility, restoration input, deduplication, and patch-no-stack-growth. The narrow generated iOS
  host adapter owns XCUITest flows for tabs, navigate/back including edge-swipe, shell marker, insets,
  and accessibility focus.
- **D-11:** Keep simulator/XCUITest results advisory and retained evidence limited to closed assertion IDs
  and outcomes under the existing privacy allowlist. Do not retain URLs, route payloads, navigation
  history, screenshots, device identifiers, transcripts, or raw test output. Phase 162 reuses this
  vocabulary for its dated physical-iPhone evidence; this phase adds no device farm or permanent device
  CI lane.
- **D-12:** Update canonical and generated support truth to state bounded iOS-only shell verification,
  frozen Android posture, and Phoenix-owned confirm/alert fallback with its reversible future condition.
  Developer ergonomics are one Phoenix declaration/compiled artifact, one typed iOS adapter, clear
  stable denials, exhaustive graph vectors, and generated host proof—not a provider-shaped navigation API.

### the agent's Discretion

The user selected every gray area and accepted a single research-backed recommendation. Planning may
choose exact module/type names, manifest encoding, UIKit coordinator shape, accessibility identifiers,
generated filenames, vector fixtures, and update coalescing mechanics. It may not add a second topology
authority, infer concrete routes, make browser history authoritative, widen the protocol with sensitive
data, claim simulator success as device evidence, add Android work, or turn this slice into generic
navigation.

</decisions>

<canonical_refs>
## Canonical References

**Downstream agents MUST read these before planning or implementing.**

### Governing first-adopter decisions

- `AGENTS.md` — current priority, privacy constraints, workflow, Android freeze, and stop list.
- `.planning/ADR-FIRST-B2C-ADOPTER.md` — bounded native-navigation decision, reversal condition,
  ownership, confirmation fallback, physical-device promotion, and non-goals.
- `.planning/FIRST-B2C-ADOPTER-ADOPTION-BRIEF.md` — first-adopter route/runtime ownership, testing
  seam, host/core boundaries, and dated proof sequence.
- `.planning/FIRST-B2C-ADOPTER-ROUTE-POLICY-MAP.md` — navigation handoff state, `unknown_blocking`
  gate, root/pushed-navigation ownership, safe-area/marker contract, and sensitive-data exclusions.
- `.planning/todos/TODO-002-first-b2c-adopter-route-inputs.md` — required sanitized host route inputs;
  remains open and blocks concrete adopter topology/device promotion.

### Active milestone and prior contracts

- `.planning/PROJECT.md` — Phoenix-first route-policy thesis and explicit-runtime ownership.
- `.planning/REQUIREMENTS.md` — NAV-01 through NAV-07 acceptance requirements.
- `.planning/ROADMAP.md` — Phase 161.1 goal, strict time box, smallest shippable version, success
  criteria, Phase 162 handoff, and Android freeze.
- `.planning/STATE.md` — current phase, TODO-002 blocker, and physical-device dependency.
- `.planning/phases/159-host-reusable-proof-lane/159-CONTEXT.md` — host-owned proof lane, closed
  outcomes, evidence allowlist, and browser-proof preservation.
- `.planning/phases/160-scoped-replay-and-auth-safety/160-CONTEXT.md` — backend authority, scoped
  privacy, fail-closed state, and generated proof seam.
- `.planning/phases/161-ios-pronunciation-pack-seam/161-CONTEXT.md` — iOS host composition, advisory
  simulator/device boundary, accessibility proof precedent, and verified-pack activation contract.

### Current implementation and guidance

- `packages/crosswake-shell-core-ios/Sources/CrosswakeShellCore/CrosswakeShell.swift` — current shell
  composition and bridge-channel creation seam.
- `packages/crosswake-shell-core-ios/Sources/CrosswakeShellCore/BridgeChannel.swift` — typed,
  versioned, request/reply bridge and safe native-to-web delivery precedent.
- `examples/ios_shell_host/CrosswakeShell/CrosswakeShellApp.swift` — host-owned SwiftUI composition,
  activation presentation, host delegates, and current root scene.
- `examples/ios_shell_host/CrosswakeShell/LiveViewContainerViewController.swift` — same-origin
  WKWebView boundary, document-start injection, route load, and bridge integration.
- `guides/native_shell.md` — manifest-first activation, host ownership, denial behavior, rebuild truth,
  and native-shell scope.
- `guides/route_policy.md` — route-policy authority and explicit defer guidance.
- `brandbook/BRAND-SPEC.md` — current visual/voice authority; supersedes the historical prompt brand book.
- `prompts/crosswake-research-synthesis.md` — route-owner architecture and honest boundary research.
- `prompts/elixir-mobile-architecture-apptypes-stresstest-deep-research.md` — host-owned adapters,
  app-type boundaries, and native/media pressure.
- `prompts/elixir-mobile-apptypes-design-stresstest-deep-research.md` — design and compatibility
  lessons for explicit runtime boundaries.

</canonical_refs>

<code_context>
## Existing Code Insights

### Reusable Assets

- `CrosswakeShell`: central shell composition with manifest/activation state and existing safe bridge
  construction.
- `BridgeChannel` and its reply delivery: established typed/versioned low-frequency envelope and
  script-injection safety precedent for the new transition seam.
- `CrosswakeShellApp`/`RootSceneView`: host-owned SwiftUI composition where a bounded UIKit navigation
  controller can be mounted without changing leaf runtime ownership.
- `LiveViewContainerViewController`: existing same-origin WebKit container, document-start user scripts,
  and lifecycle/update hooks for marker, CSS properties, and in-frame patches.
- Phase 159 proof lane: host-owned generator, closed outcome vocabulary, XCTest/XCUITest scaffolding,
  and privacy-safe evidence promotion.

### Established Patterns

- Route activation is manifest-first, native-first, and fail closed before a web container is mounted.
- Hosts own product presentation and native integration; Crosswake owns semantic contracts, validation,
  stable denials, and support truth.
- Bridge contracts use compatible versions, explicit request/reply envelopes, and no string interpolation
  of host-influenced data into script source.
- Deterministic contract proof, advisory simulator proof, and physical-device promotion are intentionally
  distinct levels of evidence.

### Integration Points

- Compile topology beside existing route manifest generation and validate it with route inventory/manifest
  truth.
- Add a narrow coordinator and topology types to `CrosswakeShellCore`, then compose the UIKit shell in
  the host example without moving LiveView/offline/native leaf rendering into a generic renderer.
- Extend the existing document-start injection and WKWebView update seam for root marker, CSS variables,
  and authorized in-frame patch handling.
- Extend the Phase 159 generated proof-lane templates and support/capability rendering for the new
  bounded assertion vocabulary and iOS-only truth.

</code_context>

<specifics>
## Specific Ideas

- The user requested research through architecture, Phoenix ecosystem, successful-library, UX, UI,
  accessibility, performance, operational, security, privacy, developer-experience, and JTBD lenses,
  followed by one coherent recommendation rather than piecemeal decisions.
- The accepted direction is UIKit tabs/stacks under the current SwiftUI composition, a compiled topology,
  typed idempotent synchronization, document-start shell state, and proof-lane extension.
- Brandbook guidance takes precedence over the historical brand prompt: calm, exact, task-oriented copy;
  visible boundaries; standard platform affordances; no hidden backend mechanics.

</specifics>

<deferred>
## Deferred Ideas

- Generic cross-platform navigation, Android implementation/parity, a native renderer for Phoenix leaves,
  arbitrary modal/sheet/overflow navigation, generalized restoration, navigation telemetry, device farm,
  permanent device CI, and native alert/confirm are outside Phase 161.1. Native alert/confirm can only be
  reconsidered after physical-iPhone proof, an active-adopter route blocker, and an explicit maintainer
  roadmap decision.

</deferred>

---

*Phase: 161.1-first-adopter-ios-navigation-shell*
*Context gathered: 2026-08-04*
