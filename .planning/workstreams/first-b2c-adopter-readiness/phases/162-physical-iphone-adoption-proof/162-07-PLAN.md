---
phase: 162-physical-iphone-adoption-proof
plan: "07"
type: execute
wave: 7
depends_on: [162-06]
files_modified:
  - priv/templates/crosswake/proof_lane/ios/ProofLaneDriver.swift.eex
  - priv/templates/crosswake/proof_lane/ios/CrosswakeProofLaneTests/ProofLaneContractTests.swift.eex
  - test/crosswake/proof_lane/template_contract_test.exs
  - lib/crosswake/proof_lane/physical_iphone_contract.ex
  - test/crosswake/proof_lane/physical_iphone_preflight_test.exs
  - test/crosswake/proof_lane/evidence_test.exs
autonomous: true
gap_closure: true
requirements: [DEVICE-01, DEVICE-02, DEVICE-03, DEVICE-04, DEVICE-05, DEVICE-06]
must_haves:
  truths:
    - "Per D-01 and D-04, the generated physical-iPhone sequence stops at the first blocked or unavailable prerequisite, emits a complete closed report for assertions that were not run, and never invokes a later host mutation, relaunch, replay, or recovery operation."
    - "A production adapter can reach selected-answer, free-form-answer, relaunch/reconnect, and retained-recovery operations only after every earlier pack, offline-audio, and authorized-entry prerequisite has passed."
    - "Per D-05 and D-06, malformed report entries such as nil, atoms, and wrong-shape maps return the stable PI-ASSERTIONS-COMPLETE denial rather than raising, including when canonical physical run-contract bytes enter Evidence.build/1."
    - "Per D-02 and D-03, this repair changes only device-local sequencing and closed contract validation; Phoenix remains the independent backend authority and no generic replay, conflict, or storage service is introduced."
    - "Per D-07, D-08, and D-09, the existing retained-work recovery semantics and accessible learner status surface remain unchanged and are never inferred from a code-only run."
    - "The repository-default physical command remains blocked at PI-PREFLIGHT-INVENTORY until TODO-002, an eligible signed host/backend, host adapters, and a physical iPhone genuinely exist; this repair creates no physical artifact and changes no support claim."
  artifacts:
    - path: "priv/templates/crosswake/proof_lane/ios/ProofLaneDriver.swift.eex"
      provides: "Fail-closed sequential device driver that short-circuits before later host-owned operations."
    - path: "priv/templates/crosswake/proof_lane/ios/CrosswakeProofLaneTests/ProofLaneContractTests.swift.eex"
      provides: "Per-operation fake-adapter regressions proving exact call prefixes and closed outcomes for every unrun assertion."
    - path: "lib/crosswake/proof_lane/physical_iphone_contract.ex"
      provides: "Total report-entry shape validation with stable non-raising denials."
    - path: "test/crosswake/proof_lane/evidence_test.exs"
      provides: "Adversarial evidence-build regressions for non-map and malformed physical assertion entries."
  key_links:
    - from: "PhysicalIphoneSequence.run(adapter:)"
      to: "PhysicalIphoneHostAdapter operations"
      via: "one ordered guard after every awaited prerequisite before the next adapter call"
      pattern: "installAndVerifyPack|playInstalledAudioOffline|enterAuthorizedStudy|submitSelectedAnswerOffline|submitFreeFormAnswerOffline|relaunchWithoutResetAndReconnect|observeRecoveryAndRetainedWork"
    - from: "Evidence physical run-contract decoder"
      to: "PhysicalIphoneContract.validate_report/1"
      via: "shape-safe report normalization and stable closed validation before canonical acceptance"
      pattern: "decode_physical_run_contract|validate_report|PI-ASSERTIONS-COMPLETE"
---

<objective>
Close CR-01 and WR-01 without fabricating the externally blocked physical proof.

Purpose: Prevent a known-invalid device run from reaching later adopter mutations/replay, and make malformed integration input fail closed throughout the physical evidence boundary.
Output: A short-circuiting generated Swift sequence, exhaustive fake-adapter call-order tests, and total Elixir report/evidence validation.
</objective>

<artifacts_this_phase_produces>
- Production-safe generated sequence control flow for the existing seven-operation physical adapter; no new adapter methods, platform abstraction, or device infrastructure.
- Generated XCTest fake-adapter coverage for every operation failing in turn with both `blocked` and `unavailable`, plus the all-passed call order.
- Direct validator and Evidence.build/1 adversarial regressions for non-map entries and malformed maps.
- No physical evidence artifact, completion marker, support-matrix change, support claim, simulator substitution, or Android surface.
</artifacts_this_phase_produces>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/ADR-FIRST-B2C-ADOPTER.md
@.planning/FIRST-B2C-ADOPTER-ADOPTION-BRIEF.md
@.planning/FIRST-B2C-ADOPTER-ROUTE-POLICY-MAP.md
@.planning/phases/162-physical-iphone-adoption-proof/162-CONTEXT.md
@.planning/phases/162-physical-iphone-adoption-proof/162-VERIFICATION.md
@.planning/phases/162-physical-iphone-adoption-proof/162-REVIEW.md
@.planning/phases/162-physical-iphone-adoption-proof/162-06-SUMMARY.md

<interfaces>
- `PhysicalIphoneHostAdapter` has exactly seven awaited operations in production order: `installAndVerifyPack`, `playInstalledAudioOffline`, `enterAuthorizedStudy`, `submitSelectedAnswerOffline`, `submitFreeFormAnswerOffline`, `relaunchWithoutResetAndReconnect`, and `observeRecoveryAndRetainedWork`.
- `PhysicalIphoneAssertion.allCases` fixes five ordered device-local observations. A failed operation must return a complete report in this order; every assertion whose required work was not run receives the same terminal closed outcome (`blocked` or `unavailable`) that stopped the sequence.
- `PhysicalIphoneContract.validate_report/1` accepts only the exact ordered ten-entry atom-keyed `%{id:, owner:, outcome:}` report. Entry container/shape errors use `PI-ASSERTIONS-COMPLETE`; exact-shape ownership/outcome mismatches retain the existing semantic denial and exact valid reports remain unchanged.
- `Evidence.build/1` scans `:physical_iphone_run_contract` canonical bytes through the physical decoder. Malformed assertion arrays must become the existing non-echoing `PL-EVIDENCE-HASH` public error after the validator's stable denial, never an exception.
</interfaces>
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Short-circuit every failed generated-device prerequisite</name>
  <reversibility rating="costly">D-01 fixes a generated host-consumed operation sequence and assertion order; changing it requires coordinated Swift template, generated XCTest, report-contract, and host-adapter updates.</reversibility>
  <read_first>
    - .planning/phases/162-physical-iphone-adoption-proof/162-PATTERNS.md
    - .planning/phases/162-physical-iphone-adoption-proof/162-CONTEXT.md
    - .planning/phases/162-physical-iphone-adoption-proof/162-VERIFICATION.md
    - .planning/phases/162-physical-iphone-adoption-proof/162-REVIEW.md
    - priv/templates/crosswake/proof_lane/ios/ProofLaneDriver.swift.eex
    - priv/templates/crosswake/proof_lane/ios/CrosswakeProofLaneTests/ProofLaneContractTests.swift.eex
    - test/crosswake/proof_lane/template_contract_test.exs
    - script/verify_physical_iphone_report_contract.sh
  </read_first>
  <files>priv/templates/crosswake/proof_lane/ios/ProofLaneDriver.swift.eex, priv/templates/crosswake/proof_lane/ios/CrosswakeProofLaneTests/ProofLaneContractTests.swift.eex, test/crosswake/proof_lane/template_contract_test.exs</files>
  <behavior>
    - "RED: a recording fake adapter whose install operation returns blocked records only install; audio, entry, submissions, relaunch/reconnect, and recovery are absent, while all five report observations are blocked."
    - "RED: repeat for unavailable install, then for blocked and unavailable at audio, entry, selected answer, free-form answer, relaunch/reconnect, and recovery; each call log equals only the prefix through the failing operation."
    - "When pack install or offline audio fails, every unrun device assertion is closed with that terminal outcome; when entry or a later study step fails, completed earlier observations remain passed and every dependent unrun assertion is closed with the terminal outcome."
    - "The all-passed fake executes each adapter operation exactly once in the declared order and returns the existing complete ordered passed device report."
    - "The nil-adapter unavailable report, owner-free JSON envelope, and advisory serialization harness remain unchanged and non-promoting."
  </behavior>
  <action>Per `162-PATTERNS.md`, extend `ProofLaneDriver.swift.eex` and `ProofLaneContractTests.swift.eex` from their mapped exact-extension analogs under `priv/templates/crosswake/proof_lane/ios/`; preserve the generated-template conventions and the mapped closed-outcome/fail-closed pattern of finite assertion IDs, only `passed`/`blocked`/`unavailable` outcomes, stable non-echoing denials, and no inferred pass. Write the recording fake-adapter matrix first in the generated XCTest template. Give the fake an exact call log and one configurable failing operation/outcome, run every operation as the failure point for both closed non-passing outcomes, and assert the exact permitted call prefix plus the complete ordered report. Then replace `PhysicalIphoneSequence.run`'s eager awaits with explicit sequential guards per D-01/D-04: await one adapter method, stop immediately unless it passed, and synthesize all dependent unrun observations from that same blocked/unavailable terminal outcome without calling the adapter again. Preserve the current adapter protocol, fixed assertion IDs, report schema, owner-free serialization, operation ordering, and uninterrupted storage lifecycle. Do not add reset behavior, retry, batching, background work, generic device abstractions, backend authority, or a simulated pass. Add a lightweight source/render contract assertion only if needed to ensure the generated XCTest matrix and ordered guard seam cannot disappear while the executable Swift gate remains the behavioral authority.</action>
  <acceptance_criteria>
    - Every one of the seven adapter operations is tested as the first blocked operation and the first unavailable operation.
    - Each failure case proves the exact call prefix and proves every later adapter operation was not invoked.
    - Every returned report remains complete, ordered, owner-free, and closed; completed independent observations stay passed and unrun dependent observations use the stopping outcome.
    - The all-passed path calls all seven operations once in order and retains the existing physical report contract.
  </acceptance_criteria>
  <verify><automated>bash script/verify_physical_iphone_report_contract.sh &amp;&amp; mix test test/crosswake/proof_lane/template_contract_test.exs --max-failures 1</automated></verify>
  <done>CR-01 is closed: after any blocked or unavailable prerequisite, no later host mutation, relaunch/reconnect, replay-adjacent, or recovery method can execute.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Make malformed physical reports total across validator and evidence</name>
  <read_first>
    - .planning/phases/162-physical-iphone-adoption-proof/162-PATTERNS.md
    - .planning/phases/162-physical-iphone-adoption-proof/162-VERIFICATION.md
    - .planning/phases/162-physical-iphone-adoption-proof/162-REVIEW.md
    - lib/crosswake/proof_lane/physical_iphone_contract.ex
    - lib/crosswake/proof_lane/evidence.ex
    - test/crosswake/proof_lane/physical_iphone_preflight_test.exs
    - test/crosswake/proof_lane/evidence_test.exs
  </read_first>
  <files>lib/crosswake/proof_lane/physical_iphone_contract.ex, test/crosswake/proof_lane/physical_iphone_preflight_test.exs, test/crosswake/proof_lane/evidence_test.exs</files>
  <behavior>
    - "RED: validate_report/1 returns {:error, \"PI-ASSERTIONS-COMPLETE\"} for [nil], [:bad], [%{}], missing-key maps, extra-key maps, mixed valid/non-map entries, and string-key entry maps; none raises."
    - "An exact complete report still returns :ok; missing, duplicate, reordered, wrong-owner, and invalid-outcome reports retain their established stable result categories after shape admission."
    - "Evidence.build/1 given a physical candidate whose approved canonical run-contract assertion array contains nil, an atom-equivalent malformed decoded value, or malformed maps returns the stable non-echoing PL-EVIDENCE-HASH error and never raises."
    - "Evidence build/decode errors do not echo malformed entry content and do not create or promote an artifact."
  </behavior>
  <action>Per `162-PATTERNS.md`, extend `evidence_test.exs` from its mapped exact-extension analog and `physical_iphone_preflight_test.exs` from its mapped `ios_verifier_test.exs` role-match analog; preserve the mapped closed-schema/privacy-safe canonical-evidence conventions: exact allowlists, finite closed outcomes, stable non-echoing rule IDs, canonical JSON reparse, and no raw values or inferred pass. Add adversarial tests before implementation. Refactor `PhysicalIphoneContract.validate_report/1` so it validates every entry is a map with exactly the atom keys `:id`, `:owner`, and `:outcome` before any `Map.get/2`, sorting, or set comparison. Return `{:error, "PI-ASSERTIONS-COMPLETE"}` immediately for a non-list, non-map entry, wrong-size/wrong-key map, or mixed malformed report. Only after shape admission derive IDs and apply the existing completeness, order, ownership, and closed-outcome checks; preserve valid behavior and non-echoing rule strings. Exercise the private Evidence decoder only through public `Evidence.build/1` by replacing the approved `physical_iphone_run_contract` canonical assertion array with JSON-representable non-map and malformed-map entries. Assert the validator's denial is translated into Evidence's existing `PL-EVIDENCE-HASH` public error contract, with no `BadMapError`, function-clause error, candidate echo, destination, or support-truth mutation. Do not widen the evidence schema or add rescued catch-all success paths.</action>
  <acceptance_criteria>
    - Direct validation of `[nil]`, `[:bad]`, malformed maps, and mixed malformed reports always returns `{:error, "PI-ASSERTIONS-COMPLETE"}`.
    - Exact valid reports and the established missing/duplicate/order/owner/outcome denials remain deterministic.
    - Public Evidence.build/1 adversarial cases fail closed with `PL-EVIDENCE-HASH`, never raise, and never echo supplied malformed values.
    - No evidence schema, retained field, support claim, or physical promotion behavior changes.
  </acceptance_criteria>
  <verify><automated>mix test test/crosswake/proof_lane/physical_iphone_preflight_test.exs test/crosswake/proof_lane/evidence_test.exs --max-failures 1 &amp;&amp; mix format --check-formatted lib/crosswake/proof_lane/physical_iphone_contract.ex test/crosswake/proof_lane/physical_iphone_preflight_test.exs test/crosswake/proof_lane/evidence_test.exs</automated></verify>
  <done>WR-01 is closed: report validation and evidence building are total for adversarial entry shapes and preserve stable non-echoing denials.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| generated Swift driver -> host adapter | A proof sequence invokes real host-owned device, storage, mutation, relaunch/reconnect, and recovery operations; later calls are forbidden once proof validity is lost. |
| canonical physical run-contract bytes -> Elixir validator | Decoded integration input is untrusted and may contain arbitrary JSON entry types or malformed maps. |
| validated run contract -> evidence build/promotion | A malformed report must not crash, bypass validation, create an artifact, or change support truth. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-162-34 | Tampering | `PhysicalIphoneSequence.run` prerequisite order | high | mitigate | Task 1 inserts a guard after every awaited operation and executable fake-adapter tests prove exact call prefixes for blocked and unavailable at all seven steps. |
| T-162-35 | Elevation of Privilege | later host mutation/replay-adjacent operations | high | mitigate | Task 1 permits each later adapter call only after all prerequisites before it passed; terminal outcomes synthesize unrun observations without invoking host code. |
| T-162-36 | Denial of Service | `validate_report/1` malformed entries | high | mitigate | Task 2 shape-checks entries before map access and tests non-map/malformed/mixed lists through the direct public validator. |
| T-162-37 | Information Disclosure | evidence decode error path | high | mitigate | Task 2 preserves stable non-echoing PI/PL rule IDs and tests canary malformed content is absent from returned errors. |
| T-162-38 | Spoofing | simulator or synthetic code-only result | medium | accept | Existing preflight and promotion gates remain unchanged; these repairs produce no physical assertion success, retained artifact, or support claim. |

ASVS L1 with `security_block_on: high`: all high-severity threats are mitigated by automated negative tests in this plan before any genuine physical host is exposed to the command.
</threat_model>

<source_coverage_audit>
| SOURCE | ID | Feature/Requirement | Plan | Status | Notes |
|---|---|---|---|---|---|
| GOAL | — | Safe physical-iPhone offline-study proof | 162-07 + external gate | COVERED | Code safety gaps close here; genuine proof remains blocked on required external inputs. |
| REQ | DEVICE-01 | Verified offline pack/audio prerequisite | 162-07 Task 1 | COVERED | Pack and offline audio failures stop all later host operations. |
| REQ | DEVICE-02 | Offline answers, relaunch, and exactly-once path | 162-07 Task 1 | COVERED | Selected/free-form/relaunch operations execute only after earlier prerequisites pass. |
| REQ | DEVICE-03 | Retained recovery without silent continuation | 162-07 Task 1 | COVERED | Recovery observation is never called after an invalid earlier sequence. |
| REQ | DEVICE-04 | Account isolation | 162-07 Task 1 + existing 162-06 | COVERED | Device operations cannot continue toward backend scope proof after local prerequisite failure; existing Phoenix authority remains unchanged. |
| REQ | DEVICE-05 | Entry/replay disablement | 162-07 Task 1 + existing 162-06 | COVERED | Authorized entry gates mutation calls; independent server gate tests remain unchanged. |
| REQ | DEVICE-06 | Redacted, validated evidence | 162-07 Task 2 | COVERED | Malformed reports fail closed through direct and evidence-build paths. |
| REQ | DEVICE-07 | Narrow support claim | external gate | COVERED | Not directly changed; current verification-required wording must remain until genuine physical promotion. |
| RESEARCH | Fail-closed sequence | Ordered host-owned physical driver | 162-07 Task 1 | COVERED | Exact per-step guards and fake-adapter call logs. |
| RESEARCH | Closed evidence | Non-echoing total input validation | 162-07 Task 2 | COVERED | Shape admission precedes map reads and canonical evidence acceptance. |
| CONTEXT | D-01, D-02, D-03, D-04 | Sequential driver, dual authority, narrow host boundary, hard preflight | 162-07 Task 1 | COVERED | Repairs execution order without adding authority or platform scope. |
| CONTEXT | D-05, D-06 | Fixed assertion vocabulary and privacy-safe evidence | 162-07 Task 2 | COVERED | Existing schema stays fixed; malformed entries fail closed. |
| CONTEXT | D-07, D-08, D-09 | Retained recovery and accessible learner UI | existing 162-04/06 | COVERED | This code-only repair does not alter learner UI or Phoenix authority. |
| REVIEW | CR-01 | Later adapter calls after failed prerequisite | 162-07 Task 1 | COVERED | Every operation fails in turn under call-log assertions. |
| REVIEW | WR-01 | Validator crash on non-map entries | 162-07 Task 2 | COVERED | Direct and Evidence.build/1 adversarial regressions. |
| VERIFICATION | External physical artifact | TODO-002, signed host/backend, adapters, physical iPhone | external gate | COVERED | Unknown-blocking prerequisite is preserved; no simulation or support change is planned. |
</source_coverage_audit>

<verification>
- Swift fail-closed behavior: `bash script/verify_physical_iphone_report_contract.sh`; the freshly generated XCTest target must run the complete fake-adapter matrix before its advisory serialization report is accepted.
- Elixir total-validation behavior: `mix test test/crosswake/proof_lane/physical_iphone_preflight_test.exs test/crosswake/proof_lane/evidence_test.exs --max-failures 1`.
- Full focused Phase 162 code gate: `mix test test/crosswake/proof_lane/physical_iphone_preflight_test.exs test/mix/tasks/crosswake.proof_lane.physical_iphone_test.exs test/crosswake/proof_lane/evidence_test.exs test/crosswake/proof_lane/template_contract_test.exs --max-failures 1`.
- External gate preservation: `mix crosswake.proof_lane.physical_iphone --run --promote --json` must exit 2 with only `PI-PREFLIGHT-INVENTORY`, and `.planning/phases/162-physical-iphone-adoption-proof/evidence/physical_iphone/proof-lane-evidence.json` must remain absent.
- No support promotion: `mix crosswake.support_matrix.gen --check` must pass with the physical-iPhone row still verification-required and no shipped claim.
</verification>

<success_criteria>
- CR-01 is closed by executable per-step call-order tests and short-circuiting production Swift control flow.
- WR-01 is closed by total direct validation and public Evidence.build/1 adversarial regressions.
- DEVICE-01 through DEVICE-06 code safety is repaired without producing physical evidence or altering DEVICE-07 support truth.
- TODO-002, the eligible signed host/backend, host adapters, and the physical iPhone remain genuine unknown-blocking prerequisites to the exact existing production command.
</success_criteria>

<output>
Create `.planning/phases/162-physical-iphone-adoption-proof/162-07-SUMMARY.md` when done.
</output>
