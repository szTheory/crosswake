---
phase: 162-physical-iphone-adoption-proof
plan: "14"
type: execute
wave: 14
depends_on: [162-12, 162-13]
files_modified:
  - test/crosswake/proof_lane/evidence_test.exs
  - test/crosswake/proof_lane/physical_iphone_evidence_transaction_test.exs
  - examples/phoenix_host/lib/crosswake_example/physical_iphone_proof_host.ex
  - examples/phoenix_host/test/crosswake_example/physical_iphone_proof_host_test.exs
  - script/retain_physical_iphone_evidence_transaction.sh
  - lib/crosswake/support_matrix/renderer.ex
  - test/crosswake/support_matrix/renderer_test.exs
  - guides/support_matrix.md
  - .planning/phases/162-physical-iphone-adoption-proof/evidence/physical_iphone/proof-lane-evidence.json
  - .planning/phases/162-physical-iphone-adoption-proof/evidence/physical_iphone/.complete
autonomous: true
gap_closure: true
requirements: [DEVICE-01, DEVICE-02, DEVICE-03, DEVICE-04, DEVICE-05, DEVICE-06, DEVICE-07]
user_setup:
  - service: first-adopter-host
    why: "The standard proof task requires the validated TODO-002 row plus configured host fixture/media/replay/session/scope/gate controls and a device-reachable Phoenix endpoint."
    env_vars:
      - name: CROSSWAKE_PHYSICAL_IPHONE_HOST_BASE_URL
        source: "Device-reachable base URL for the configured examples/phoenix_host MIX_ENV=test endpoint"
  - service: physical-iphone
    why: "Apple signing and one connected trusted physical iPhone are unavoidable preflight inputs; the standard task discovers and validates the destination."
    dashboard_config:
      - task: "Connect and trust one physical iPhone and make the existing CrosswakeProofLane scheme signable."
        location: "Local Xcode signing/device setup"
must_haves:
  truths:
    - "Task 1 remains complete at RED 3ef8a959 and GREEN 60efab5d: public physical-study support is `verification required`, and stale evidence is absent."
    - "Before any retry, an automated synthetic owner-disjoint promotion proves that a successful `--run --promote --json`-equivalent result remains present after its producing BEAM/subprocess exits."
    - "Before any retry, an isolated temporary-Git-repository contract suite proves every one-shot transaction gate through SAFE injected fake readiness, run, and source-bound verification executables; synthetic paths and results never name or touch the production evidence destination."
    - "The first production invocation has exhausted its authority; at most one new exact standard invocation is allowed, and only after the subprocess-lifetime regression is RED then GREEN."
    - "If native publication already retains bytes across subprocess exit, Evidence, NativePromotion, and the C helper remain unchanged; the recovery is a durable host execution transaction, not an invented retention fix."
    - "All lifecycle and transaction/host/script GREEN code commits precede the durable empty retry ledger; the evidence commit_ref is pinned to the pre-ledger GREEN code commit actually executed, never to the ledger or evidence commit."
    - "The durable transaction does not return passed until it has validated and committed exactly the fresh two-file physical record, then safely deserialized the exact same-run approved_hashes term in a fresh process and passed Evidence.check/2 against committed-byte-identical evidence."
    - "Per D-04 through D-06 and DEVICE-07, support remains `verification required` unless the one retry passes all 10 owner-disjoint assertions, no-replace promotion, retention after exit, commit, and fresh-process source-bound recheck."
    - "Any restored prose remains one first-adopter flow on one recorded iOS runtime line and preserves every Android/background/generic-storage/generic-sync/multiple-island/simulator/every-iPhone non-claim."
  prohibitions:
    - statement: "The exhausted first invocation, its transient postconditions, stale summaries, or Evidence.check/1 must not authorize a retry or support promotion."
      verification: flagged-unverified
    - statement: "Synthetic regression bytes must never substitute for physical evidence or change public support."
      verification: flagged-unverified
    - statement: "Output and artifacts must not expose sensitive payloads, identity, host facts, or private paths."
      verification: flagged-unverified
  artifacts:
    - path: "test/crosswake/proof_lane/evidence_test.exs"
      provides: "Subprocess-exit retention regression using synthetic owner-disjoint reports, canonical safe bytes, and a private temporary destination."
    - path: "test/crosswake/proof_lane/physical_iphone_evidence_transaction_test.exs"
      provides: "Isolated temporary-Git-repository contract coverage for the one-shot shell transaction and its SAFE fake command boundaries."
    - path: "script/retain_physical_iphone_evidence_transaction.sh"
      provides: "One-shot durable host execution transaction around the exact standard command."
    - path: "examples/phoenix_host/lib/crosswake_example/physical_iphone_proof_host.ex"
      provides: "Private same-run canonical-source handoff used only by the durable transaction; no reconstructed bytes."
    - path: "examples/phoenix_host/test/crosswake_example/physical_iphone_proof_host_test.exs"
      provides: "Focused private-seam coverage for exact approved_hashes serialization, pinned code provenance, default disablement, strict permissions, and cleanup inputs."
    - path: ".planning/phases/162-physical-iphone-adoption-proof/evidence/physical_iphone/proof-lane-evidence.json"
      provides: "Fresh physical record retained after command exit and committed before support re-promotion."
    - path: "lib/crosswake/support_matrix/renderer.ex"
      provides: "Fail-closed or freshly re-earned canonical physical-study support row."
    - path: "guides/support_matrix.md"
      provides: "Deterministic narrow public support truth."
  key_links:
    - from: "synthetic child BEAM promotion"
      to: "parent-process retained destination"
      via: "child exits before the parent checks the private two-file destination with Evidence.check/2"
      pattern: "Evidence\.check"
    - from: "GREEN lifecycle and transaction code commit"
      to: "durable retry ledger then exact standard physical command"
      via: "the transaction records the clean pre-attempt HEAD, creates the ledger afterward, pins the run's commit_ref to the recorded code commit, and refuses tracked non-evidence dirt"
      pattern: "chore\(162-14\): consume physical retry"
    - from: "exact standard physical command"
      to: "committed fixed evidence destination"
      via: "one-shot transaction validates the passed result and exact two-file destination before staging and committing only those files"
      pattern: "crosswake\.proof_lane\.physical_iphone --run --promote --json"
    - from: "evidence_input/1 approved_hashes object"
      to: "committed physical evidence"
      via: "private mode-0600 Erlang external-term serialization, strict safe deserialization in a fresh BEAM process, Evidence.check/2 only, and unconditional cleanup"
      pattern: "Evidence\.check"
    - from: "Crosswake.SupportMatrix.Renderer.render/1"
      to: "guides/support_matrix.md"
      via: "deterministic regeneration and byte equality"
      pattern: "physical-iPhone offline study"
---

<objective>
Preserve the completed fail-closed withdrawal, prove promotion retention across subprocess exit, and authorize at most one new physical run inside a durable commit-and-recheck transaction before the same narrow public support claim may return.

Purpose: Close the observed evidence-retention boundary without inventing a physical pass, reusing exhausted invocation authority, or accepting transient files as release truth.
Output: Either committed, fresh, source-bound physical evidence plus the prior narrow support row, or durable `verification required` support with no completion claim.
</objective>

<artifacts_this_phase_produces>
- Existing completed Task 1 commits: `3ef8a959` (RED) and `60efab5d` (GREEN); these are preserved and not rerun.
- `evidence promotion survives producing subprocess exit` in `test/crosswake/proof_lane/evidence_test.exs`.
- `test/crosswake/proof_lane/physical_iphone_evidence_transaction_test.exs`, exercising the transaction in isolated temporary Git repositories with SAFE fake executable boundaries.
- `run_promotion_subprocess!/2` and `run_transaction_fixture!/1`, private ExUnit helpers for the lifecycle and transaction regressions.
- A private, disabled-by-default host seam that writes the exact `approved_hashes` object returned inside `evidence_input/1` as a mode-0600 Erlang external term and pins `commit_ref` to the validated pre-attempt GREEN code commit supplied by the transaction; neither seam changes a public callback/API.
- Focused host tests in `examples/phoenix_host/test/crosswake_example/physical_iphone_proof_host_test.exs` proving exact-object serialization, pinned-commit refusal/success, default disablement, and private leaf permissions without a device callback.
- `script/retain_physical_iphone_evidence_transaction.sh`, the one-shot transaction that owns the sole newly authorized exact standard invocation, fixed-destination validation, evidence-only commit, and fresh-process Evidence.check/2.
- A durable empty Git commit with the exact stable subject `chore(162-14): consume physical retry`, created after all GREEN code commits but before device execution and used as the one-shot attempt ledger without becoming the evidence `commit_ref`.
- A fresh fixed two-file physical evidence record only if the new run and durable transaction pass.
- Regenerated `guides/support_matrix.md` only if durable admission passes; otherwise its completed fail-closed state remains unchanged.
- No new support status, evidence schema, Android behavior, runtime filesystem support probe, or widened platform claim.
</artifacts_this_phase_produces>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/ADR-FIRST-B2C-ADOPTER.md
@.planning/FIRST-B2C-ADOPTER-ADOPTION-BRIEF.md
@.planning/FIRST-B2C-ADOPTER-ROUTE-POLICY-MAP.md
@.planning/phases/162-physical-iphone-adoption-proof/162-CONTEXT.md
@.planning/phases/162-physical-iphone-adoption-proof/162-VERIFICATION.md
@.planning/phases/162-physical-iphone-adoption-proof/162-12-SUMMARY.md
@.planning/phases/162-physical-iphone-adoption-proof/162-13-SUMMARY.md
@lib/crosswake/proof_lane/evidence.ex
@lib/crosswake/proof_lane/native_promotion.ex
@priv/native/crosswake_evidence_promote.c
@lib/mix/tasks/crosswake.proof_lane.physical_iphone.ex
@examples/phoenix_host/lib/crosswake_example/physical_iphone_proof_host.ex
@lib/crosswake/support_matrix/renderer.ex
@test/crosswake/support_matrix/renderer_test.exs
@guides/support_matrix.md

<interfaces>
- `Evidence.promote/2` publishes through `NativePromotion.publish/2` and immediately performs source-bound `Evidence.check/2`; `Evidence.check/1` intentionally cannot validate physical canonical-source authority.
- `NativePromotion.publish/2` waits for its C helper's zero exit, while the helper fsyncs the artifact, completion marker, destination, and parent and deletes files only on a nonzero helper result. Current source has no intended successful-path deletion.
- `Mix.Tasks.Crosswake.ProofLane.PhysicalIphone.promote_if_requested/2` obtains the exact canonical safe bytes from `PhysicalIphoneProofHost.evidence_input/1`; the durable transaction must capture that same object, not rebuild it from result JSON or retained evidence.
- `PhysicalIphoneProofHost.commit_ref/0` currently resolves `HEAD`; because the ledger must precede execution, the transaction must record the clean GREEN code commit first and use a private, exact-hash-validated transaction input so `evidence_input/1` emits `git-<code-commit>`, not the later empty ledger commit.
- The sole newly authorized physical invocation remains exactly `(cd examples/phoenix_host &amp;&amp; MIX_ENV=test mix crosswake.proof_lane.physical_iphone --run --promote --json)` after an all-ready readiness result and absent fixed destination.
- Task 1 is already complete and committed. The first production invocation returned a passed result and observed its two immediate evidence files, but its authority is exhausted because the destination was absent after the producing execution context exited.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Preserve the completed fail-closed withdrawal</name>
  <read_first>
    - .planning/phases/162-physical-iphone-adoption-proof/162-14-PLAN.md
    - lib/crosswake/support_matrix/renderer.ex
    - test/crosswake/support_matrix/renderer_test.exs
    - guides/support_matrix.md
  </read_first>
  <files>lib/crosswake/support_matrix/renderer.ex, test/crosswake/support_matrix/renderer_test.exs, guides/support_matrix.md</files>
  <action>Do not repeat or amend the completed implementation. Confirm commits `3ef8a959` and `60efab5d` are ancestors of HEAD and record Task 1 as already complete in the Plan 14 summary. Preserve the current `verification required` renderer/guide state, the removal of stale evidence, every existing narrow non-claim, and Android freeze. Do not restore any prior record or use the exhausted first invocation as authority.</action>
  <acceptance_criteria>
    - RED `3ef8a959` and GREEN `60efab5d` remain the sole Task 1 implementation commits.
    - Public physical-study support remains `verification required` before Tasks 2 and 3.
    - The stale record remains absent and no Xcode signing/workspace/debug artifact is touched or staged.
  </acceptance_criteria>
  <verify><automated>git merge-base --is-ancestor 3ef8a959 HEAD &amp;&amp; git merge-base --is-ancestor 60efab5d HEAD &amp;&amp; test ! -e .planning/phases/162-physical-iphone-adoption-proof/evidence/physical_iphone &amp;&amp; rg -q 'verification required' guides/support_matrix.md</automated></verify>
  <done>Task 1 remains complete and fail closed; no new Task 1 commit or physical invocation is created.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prove subprocess retention, then perform one durable physical retry</name>
  <precondition>Task 1's two commits are present, Plans 162-12 and 162-13 are complete, the fixed evidence destination is absent, and the exact readiness command from `examples/phoenix_host` returns outcome `ready` with a nonempty all-ready checks array.</precondition>
  <reversibility rating="costly">D-05/D-06 make the newly committed no-replace record durable release truth; correction requires explicit invalidation and another separately authorized fully preflighted run.</reversibility>
  <read_first>
    - AGENTS.md
    - .planning/phases/162-physical-iphone-adoption-proof/162-CONTEXT.md
    - .planning/phases/162-physical-iphone-adoption-proof/162-12-SUMMARY.md
    - .planning/phases/162-physical-iphone-adoption-proof/162-13-SUMMARY.md
    - .planning/todos/TODO-002-first-b2c-adopter-route-inputs.md
    - lib/crosswake/proof_lane/evidence.ex
    - lib/crosswake/proof_lane/native_promotion.ex
    - priv/native/crosswake_evidence_promote.c
    - lib/mix/tasks/crosswake.proof_lane.physical_iphone.ex
    - examples/phoenix_host/lib/crosswake_example/physical_iphone_proof_host.ex
    - examples/phoenix_host/test/crosswake_example/physical_iphone_proof_host_test.exs
    - test/crosswake/proof_lane/evidence_test.exs
    - test/crosswake/proof_lane/physical_iphone_evidence_transaction_test.exs
  </read_first>
  <files>test/crosswake/proof_lane/evidence_test.exs, test/crosswake/proof_lane/physical_iphone_evidence_transaction_test.exs, examples/phoenix_host/lib/crosswake_example/physical_iphone_proof_host.ex, examples/phoenix_host/test/crosswake_example/physical_iphone_proof_host_test.exs, script/retain_physical_iphone_evidence_transaction.sh, .planning/phases/162-physical-iphone-adoption-proof/evidence/physical_iphone/proof-lane-evidence.json, .planning/phases/162-physical-iphone-adoption-proof/evidence/physical_iphone/.complete</files>
  <behavior>
    - "A child BEAM performs a successful physical-class, source-bound promotion from synthetic owner-disjoint passed reports and canonical safe bytes, exits, and the parent still finds exactly the immutable artifact and marker and passes Evidence.check/2."
    - "Synthetic destinations are private temporary paths, are always deleted, never use the fixed production destination, and cannot affect support truth."
    - "An isolated temporary Git repository and SAFE injected fake executables prove rejection of non-ready output, a pre-existing destination, a pre-existing retry ledger, a wrong result/assertion set, an extra or missing evidence file, dirty tracked non-evidence changes, committed-blob mismatch, and a fresh-process source-bound refusal; they also prove ledger-before-run, exactly one run invocation, evidence-only staging/commit, private capture/source cleanup, and the successful source-bound path through the real Evidence.check/2 verifier."
    - "The exact approved_hashes list produced by evidence_input/1 crosses to the fresh process only as a mode-0600 Erlang external term; safe decoding accepts exactly one map with exactly kind: :physical_iphone_run_contract and binary canonical_bytes and rejects every other shape."
    - "All lifecycle and transaction GREEN commits exist before the ledger and physical command; the evidence commit_ref resolves to the clean pre-attempt GREEN code commit, not the ledger."
  </behavior>
  <action>Implement this task in two independent TDD sequences, and do not execute readiness or the device command until both are GREEN and committed. Sequence A: add and commit the subprocess-lifetime regression RED using only synthetic owner-disjoint reports, the fixed assertion vocabulary, canonical safe `physical_iphone_run_contract` bytes, and a random private temporary destination; make `run_promotion_subprocess!/2` GREEN and commit it. Inspect `Evidence.promote/2`, `NativePromotion.publish/2`, and the C helper; leave them byte-unchanged if the regression proves current successful publication survives child exit, and change them only if the test demonstrates the real lifecycle defect. Sequence B: add `physical_iphone_evidence_transaction_test.exs` plus focused `physical_iphone_proof_host_test.exs` coverage RED. The transaction fixture must initialize an isolated temporary Git repository and invoke the real transaction script with SAFE injected readiness, run, and verification executable paths—not shell fragments, `eval`, or production commands. Injection is accepted only under an explicit test-only guard tied to the isolated fixture and is disabled/rejected by default; production always selects the literal standard boundaries. Fake outputs and destinations use random temporary paths and synthetic contract values, never the fixed production evidence path or a support-authorizing result. The successful verification helper must start a fresh project BEAM and execute the real strict decoder plus `Evidence.check/2` against synthetic isolated evidence; a separate fake-verifier failure proves refusal propagation. Cover every behavior case listed above, including ordering recorded by an append-only synthetic call ledger. Then implement the script and private host seam GREEN and commit the script, host source, and both test files before any attempt ledger.

For the source handoff, inside `evidence_input/1` bind the exact `approved_hashes` list once, return that same value in the input map, and optionally pass that identical value to a private `maybe_write_transaction_sources/1`. Enable the write only when the transaction supplies a private capture-file path and pinned code commit under the guarded standard run. Serialize with `:erlang.term_to_binary(approved_hashes)`, create the containing mode-0700 root and leaf mode 0600 without following/replacing an existing leaf, and never print the path or bytes. The fresh verifier must `File.read/1`, decode only with `:erlang.binary_to_term(bytes, [:safe])`, and accept the strict closed shape `[%{kind: :physical_iphone_run_contract, canonical_bytes: bytes}]` with exactly those two atom keys and a binary value before supplying that exact decoded object only to `Evidence.check(destination, approved_hashes)`. It must refuse malformed terms, added/missing keys or entries, wrong kinds/types, source-less checking, or reconstructed canonical bytes. Both the producing command and fresh verifier run inside an unconditional `after`/shell trap lifecycle that removes the private stdout/source root on success and every failure.

At transaction start, require the fixed no-replace destination and exact ledger subject to be absent, and run the literal readiness command from `examples/phoenix_host`; accept only one JSON object whose outcome is `ready` and whose nonempty checks are all ready. Require a clean tracked non-evidence tree before proceeding (`git diff`/`git diff --cached` against HEAD must find no tracked path outside the absent fixed evidence pair). Record the full current HEAD as `code_commit`, verify it is a commit containing both GREEN sequences, and pass it through the guarded private host seam; validate the supplied full hash resolves to that exact commit. Only then create the empty `chore(162-14): consume physical retry` ledger commit. The subsequent single run must be the literal `(cd examples/phoenix_host &amp;&amp; MIX_ENV=test mix crosswake.proof_lane.physical_iphone --run --promote --json)`, with the private capture path and pinned code commit supplied only to that process. Require zero status, exactly one passed physical result, the complete ordered 10 owner-disjoint D-01/D-02 assertions, no rule ID, and evidence `commit_ref == "git-#{code_commit}"`; explicitly reject the ledger hash.

After the run, require the destination to contain exactly regular `proof-lane-evidence.json` and `.complete` files. Recheck that no tracked non-evidence path is dirty, stage by exact pathspec only those two evidence files, assert the index path set is exactly that pair, and create the explicitly permitted evidence-only production commit. Verify the commit's parent is the ledger, its changed-path set is exactly the pair, `git show <evidence-commit>:<path>` equals each working-tree file byte-for-byte, and its embedded `commit_ref` still resolves to `code_commit`. Only then invoke the fresh source-bound verifier described above. Any rejection after the ledger consumes the sole retry and leaves support `verification required`; any rejection before it performs no run. Never stage or touch Xcode signing, workspace, derived-data, result-bundle, or debug artifacts.</action>
  <acceptance_criteria>
    - The subprocess-lifetime regression and isolated transaction-script contract each have distinct RED and GREEN commits, and all GREEN code commits precede the attempt ledger and any physical invocation.
    - If the regression passes against existing publication code, `evidence.ex`, `native_promotion.ex`, and `crosswake_evidence_promote.c` remain byte-unchanged.
    - The new physical retry occurs at most once, is the exact standard host-owned command, and follows all-ready D-04 preflight with an absent fixed destination.
    - The isolated transaction suite covers every required rejection and success/order/cleanup case without fixed production evidence paths or support authorization.
    - The stable empty attempt commit exists before device execution and remains after every post-ledger outcome; the evidence `commit_ref` is the preceding GREEN code commit actually run and is explicitly not the ledger.
    - Success means all 10 owner-disjoint assertions passed, exactly two immutable files remain after command exit, only those files form the evidence commit, their committed blobs equal working bytes, and a fresh process safely decodes the strict exact-object term then passes Evidence.check/2.
    - Failure consumes the retry authority, retains `verification required`, and never treats synthetic evidence, Evidence.check/1, private output, or transient files as proof.
  </acceptance_criteria>
  <verify><automated>mix test test/crosswake/proof_lane/evidence_test.exs test/crosswake/proof_lane/physical_iphone_evidence_transaction_test.exs --max-failures 1 &amp;&amp; (cd examples/phoenix_host &amp;&amp; MIX_ENV=test mix test test/crosswake_example/physical_iphone_proof_host_test.exs --max-failures 1) &amp;&amp; script/retain_physical_iphone_evidence_transaction.sh</automated></verify>
  <done>Either one fresh physical record is retained after exit, committed, and source-bound rechecked from a fresh process, or the sole retry closes fail-closed with public support still verification required.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Re-earn only the narrow public row from committed retained evidence</name>
  <precondition>Task 2 produced a passed transaction receipt proving the fresh fixed two-file record survived command exit, was committed, and passed fresh-process Evidence.check/2 with the exact same-run canonical sources.</precondition>
  <read_first>
    - AGENTS.md
    - .planning/phases/162-physical-iphone-adoption-proof/162-CONTEXT.md
    - .planning/phases/162-physical-iphone-adoption-proof/162-12-SUMMARY.md
    - .planning/phases/162-physical-iphone-adoption-proof/162-13-SUMMARY.md
    - lib/crosswake/support_matrix/renderer.ex
    - test/crosswake/support_matrix/renderer_test.exs
    - guides/support_matrix.md
    - .planning/phases/162-physical-iphone-adoption-proof/evidence/physical_iphone/proof-lane-evidence.json
  </read_first>
  <files>lib/crosswake/support_matrix/renderer.ex, test/crosswake/support_matrix/renderer_test.exs, guides/support_matrix.md</files>
  <behavior>
    - "Only Task 2's committed, post-exit retained, fresh-process source-bound record permits `device evidence`."
    - "Absent that complete transaction receipt, support stays `verification required` and Plan 14 closes without durable completion."
    - "The row remains one first-adopter flow on one recorded iOS runtime line and preserves every broader non-claim."
  </behavior>
  <action>D-04 through D-06: require all Task 2 transaction gates, not the exhausted first invocation, synthetic regression, old summaries, a transient destination, a direct XCUITest result, source-less Evidence.check/1, or record existence. If and only if Task 2 passed, change only the physical-study row to existing `device evidence` wording, update its regression, and regenerate the guide. Inspect only allowlisted evidence fields; never reconstruct or retain the captured canonical source bytes. D-07: keep learner-facing prose free of replay/session mechanics. Preserve Android freeze and every background/generic-storage/generic-sync/multiple-island/simulator/every-iPhone non-claim. If Task 2 failed at any point, make no support edit and record fail-closed closure in the summary.</action>
  <acceptance_criteria>
    - Re-promotion is conditional on committed post-exit evidence plus fresh-process source-bound Evidence.check/2 from the same run.
    - Success restores only the prior narrow physical-study row; failure leaves `verification required` without an evidence exception.
    - Renderer tests and generated guide remain byte-identical and contain no sensitive, identifying, host-private, or private-path values.
  </acceptance_criteria>
  <verify><automated>mix test test/crosswake/support_matrix/renderer_test.exs test/crosswake/support_matrix/support_matrix_test.exs --max-failures 1 &amp;&amp; mix run -e 'alias Crosswake.SupportMatrix; alias Crosswake.SupportMatrix.Renderer; true = File.read!("guides/support_matrix.md") == Renderer.render(SupportMatrix.canonical())'</automated></verify>
  <done>The public row is restored only from one committed, post-exit retained, fresh-process source-bound standard-run record; otherwise verification required remains the final truth.</done>
</task>

</tasks>

<source_coverage_audit>
SOURCE | ID | Feature/Requirement | Plan | Status | Notes
GOAL | — | One physical-iPhone offline-study proof with narrow support truth | 162-14 | COVERED | Durable retention and admission recovery only.
REQ | DEVICE-01..07 | Physical assertions, privacy-safe evidence, and narrow claim | 162-14 | COVERED | No requirement is widened or inferred.
RESEARCH | closed preflight + dual authority | Device execution begins only after complete prerequisites and joins independent device/backend outcomes | 162-14 Task 2 | COVERED | Readiness remains all-ready and assertions owner-disjoint.
RESEARCH | existing Evidence/NativePromotion | Reuse closed source-bound validation and atomic no-replace publication rather than hand-rolled evidence copying | 162-14 Task 2 | COVERED | Lifecycle regression decides whether existing publication needs any repair.
RESEARCH | one-shot transaction | Prove command ordering, retry exhaustion, evidence-only commit, exact source handoff, and code-commit provenance without spending the physical attempt | 162-14 Task 2 | COVERED | Isolated temporary Git repositories and SAFE fake executable boundaries cover both refusal and success paths before production execution.
RESEARCH | privacy + simulator boundary | Retain only allowlisted evidence; simulator and synthetic proof never promote | 162-14 Tasks 2-3 | COVERED | Ephemeral canonical-safe test bytes cannot reach support.
CONTEXT | D-01..D-03 | Sequential host-owned, owner-disjoint physical proof | 162-14 Task 2 | COVERED | Exact standard command and 10 assertions remain authoritative.
CONTEXT | D-04..D-06 | Closed preflight, no-replace source-bound promotion, redaction | 162-14 Tasks 1-3 | COVERED | Retry is one-shot and evidence must survive exit and commit.
CONTEXT | D-07..D-09 | Honest recovery/public UX and accessibility boundaries | 162-14 Task 3 | COVERED | Public prose stays narrow; no UI breadth is added.
</source_coverage_audit>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| producing BEAM/subprocess -> retained filesystem | A passed in-process promotion is insufficient until files remain after producer exit. |
| isolated transaction fixture -> production transaction | Test command injection must be impossible unless the explicit private test guard and isolated temporary repository are both active. |
| GREEN code commit -> retry ledger -> device process | The durable ledger must precede the run without replacing the code commit that evidence identifies as the executed source. |
| retained filesystem -> git commit | Only the exact two fixed evidence files may become durable release truth. |
| same-run approved_hashes object -> fresh BEAM recheck | The exact object crosses as a private external term, is decoded safely to a strict closed shape, and must never be reconstructed, retained, or printed. |
| committed evidence -> canonical renderer | Only the complete transaction may authorize narrow public support. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-162-77 | Spoofing | exhausted or synthetic invocation authority | high | mitigate | Task 2 authorizes one new exact command only after the RED/GREEN subprocess regression; synthetic paths cannot reach support. |
| T-162-78 | Tampering | evidence lost or changed across process exit/commit | high | mitigate | Parent-after-exit checks, exact two-file allowlist, evidence-only commit, blob equality, and fresh-process Evidence.check/2 all gate success. |
| T-162-79 | Repudiation | transient success represented as durable proof | high | mitigate | The transaction cannot pass before commit and post-commit source-bound recheck; Task 3 requires its receipt. |
| T-162-80 | Information Disclosure | stdout, canonical source channel, physical output | high | mitigate | Mode-0700 root, ephemeral same-run source handoff, safe aggregate assertions only, cleanup on every exit, and no private paths in artifacts/output. |
| T-162-81 | Elevation of Privilege | Evidence.check/1 or reconstructed bytes bypass | high | mitigate | Both are forbidden; the fresh process receives the exact approved_hashes object captured during the one standard run and calls Evidence.check/2. |
| T-162-82 | Denial of Service | device/signing/readiness or transaction failure | low | accept | Failure consumes the authorized attempt and leaves support verification required; no evidence exception or manual approval bypass exists. |
| T-162-83 | Tampering | unrelated Xcode or worktree artifacts staged | high | mitigate | Transaction stages and commits only the two explicit evidence files and asserts the commit path set before support admission. |
| T-162-84 | Elevation of Privilege | fake command injection reaches production | high | mitigate | SAFE executable-path injection is accepted only by the explicit isolated-fixture test guard; the default transaction hard-selects the literal readiness, run, and verifier boundaries. |
| T-162-85 | Spoofing | ledger hash presented as executed code provenance | high | mitigate | Record and validate clean GREEN HEAD before the ledger, pin evidence_input/1 to that exact commit, reject ledger equality, and assert the evidence commit_ref resolves to the pre-attempt code commit. |
| T-162-86 | Tampering | unsafe or widened external-term source object | high | mitigate | Mode-0600 no-replace serialization, `binary_to_term(..., [:safe])`, exact one-entry/two-key shape validation, Evidence.check/2-only consumption, and unconditional cleanup. |
</threat_model>

<verification>
- Do not rerun completed Task 1 tests or the physical command during planning.
- Executor proves the synthetic subprocess lifecycle RED then GREEN before the one newly authorized physical invocation.
- Executor proves the isolated temporary-Git transaction suite RED then GREEN before the one newly authorized physical invocation; it covers all rejection, ordering, staging, blob-equality, cleanup, and source-bound cases with SAFE fake boundaries.
- Executor commits all lifecycle and transaction/host/script GREEN code before recording the clean code commit and creating the retry ledger.
- Executor accepts physical success only after all-ready preflight, exact standard command, 10 owner-disjoint passed assertions, post-exit retention, exact evidence-only commit, committed-blob equality, pre-ledger GREEN commit_ref provenance, and fresh-process strict-term source-bound Evidence.check/2.
- Renderer/guide parity and every privacy, Android-freeze, and narrow non-claim boundary remain intact.
</verification>

<success_criteria>
- The first invocation is never reused as authority and at most one new physical run occurs.
- The transaction is independently proven before the attempt; no test fixture uses the production evidence destination or can authorize support.
- Transient evidence cannot promote support; committed post-exit source-bound evidence can.
- The durable ledger orders before the run while the record identifies the exact preceding GREEN code commit actually executed.
- No synthetic bytes, source reconstruction, Evidence.check/1, simulator result, or manual exception substitutes for physical proof.
- Final truth is either the prior narrow one-flow/one-runtime claim or `verification required`.
</success_criteria>

<output>
Create `.planning/phases/162-physical-iphone-adoption-proof/162-14-SUMMARY.md` when done. Keep Plan 162-15 next and do not alter completed Plan 01-13 history.
</output>
