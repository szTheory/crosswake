---
phase: 162-physical-iphone-adoption-proof
plan: "03"
type: execute
wave: 3
depends_on: [162-01, 162-02]
files_modified:
  - lib/mix/tasks/crosswake.proof_lane.physical_iphone.ex
  - priv/templates/crosswake/proof_lane/ios/ProofLaneDriver.swift.eex
  - priv/templates/crosswake/proof_lane/ios/CrosswakeProofLaneTests/ProofLaneContractTests.swift.eex
  - priv/templates/crosswake/proof_lane/ios/CrosswakeProofLaneUITests/ProofLaneUITests.swift.eex
  - test/mix/tasks/crosswake.proof_lane.physical_iphone_test.exs
autonomous: true
requirements: [DEVICE-01, DEVICE-02, DEVICE-03, DEVICE-04, DEVICE-05]
must_haves:
  truths:
    - "One host-owned sequential driver runs verified pack install, authorized entry, selected/free-form offline submissions, offline audio, terminate/relaunch without storage reset, reconnect, recovery cases, scope fences, and entry/replay disablement per D-01."
    - "XCTest/XCUITest reports only device-local lifecycle/audio/status assertions; the command joins them with the separately produced Phoenix authority report and requires the complete fixed manifest per D-02/D-05."
    - "Fixture resets occur only between independent cases; offline submission, process termination, relaunch, and replay share one uninterrupted persistence lifetime."
  artifacts:
    - path: "priv/templates/crosswake/proof_lane/ios/ProofLaneDriver.swift.eex"
      provides: "Sequential physical-only driver and typed host adapter callbacks."
    - path: "priv/templates/crosswake/proof_lane/ios/CrosswakeProofLaneUITests/ProofLaneUITests.swift.eex"
      provides: "Accessible real-process lifecycle/audio/local-status assertions."
    - path: "lib/mix/tasks/crosswake.proof_lane.physical_iphone.ex"
      provides: "Signed xcodebuild orchestration, ephemeral report parsing, and dual-authority join."
  key_links:
    - from: "Mix physical_iphone task"
      to: "signed xcodebuild test on selected physical destination"
      via: "sanitized preflight contract and private run directory"
      pattern: "xcodebuild|destination|PhysicalIphonePreflight"
    - from: "XCUITest closed report + Phoenix closed report"
      to: "PhysicalIphoneContract complete assertion manifest"
      via: "owner-specific canonical join"
      pattern: "device_local|backend_authority|assertions"
---

<objective>
Execute the ten-step exit test on one selected physical iPhone while preserving the device/backend authority split.

Purpose: Turn the existing pack, scoped replay, navigation shell, and generated proof seams into one rerunnable physical device sequence without a second harness.
Output: Generated sequential Swift driver/tests and production Mix orchestration with complete dual-authority results.
</objective>

<artifacts_this_phase_produces>
- Versioned `PhysicalIPhoneHostAdapter` operations embedded in the existing generated driver template.
- XCUITest cases for physical pack/audio, offline selected/free-form persistence, relaunch, and observable recovery/fenced states.
- Exact `mix crosswake.proof_lane.physical_iphone --run --json` orchestration contract using a private ephemeral run root; no raw test artifacts retained.
</artifacts_this_phase_produces>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/162-physical-iphone-adoption-proof/162-CONTEXT.md
@.planning/phases/162-physical-iphone-adoption-proof/162-UI-SPEC.md
@.planning/phases/161-ios-pronunciation-pack-seam/161-CONTEXT.md
@.planning/phases/161.1-first-adopter-ios-navigation-shell/161.1-CONTEXT.md
@priv/templates/crosswake/proof_lane/ios/ProofLaneDriver.swift.eex
@priv/templates/crosswake/proof_lane/ios/CrosswakeProofLaneTests/ProofLaneContractTests.swift.eex
@priv/templates/crosswake/proof_lane/ios/CrosswakeProofLaneUITests/ProofLaneUITests.swift.eex

<interfaces>
- Plan 01 preflight yields an immutable sanitized run contract and fixed owner-tagged assertions.
- Plan 02 Phoenix report provides backend-authority outcomes only; it is joined, never inferred from UI.
- Existing `ProofLaneHostAdapter` pack methods and Phase 161 exact-byte install/audio contract remain the media seam.
- `xcodebuild` output, `.xcresult`, destination details, and host responses are ephemeral parser inputs only.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend the generated driver with the uninterrupted physical sequence</name>
  <reversibility rating="costly">D-01 fixes the sequential host integration and test launch contract across generated Swift and Phoenix fixtures.</reversibility>
  <read_first>
    - .planning/phases/162-physical-iphone-adoption-proof/162-CONTEXT.md
    - lib/crosswake/proof_lane/physical_iphone_contract.ex
    - priv/templates/crosswake/proof_lane/ios/ProofLaneDriver.swift.eex
    - priv/templates/crosswake/proof_lane/ios/CrosswakeProofLaneTests/ProofLaneContractTests.swift.eex
    - priv/templates/crosswake/proof_lane/ios/CrosswakeProofLaneUITests/ProofLaneUITests.swift.eex
  </read_first>
  <files>priv/templates/crosswake/proof_lane/ios/ProofLaneDriver.swift.eex, priv/templates/crosswake/proof_lane/ios/CrosswakeProofLaneTests/ProofLaneContractTests.swift.eex, priv/templates/crosswake/proof_lane/ios/CrosswakeProofLaneUITests/ProofLaneUITests.swift.eex</files>
  <behavior>
    - "Contract tests reject missing/extra/reordered assertion IDs, simulator class, identifying runtime precision, and host adapter results outside closed outcomes."
    - "The main case installs and re-verifies one pack online, enters study, queues both answer categories offline, plays installed audio, terminates, relaunches without reset, then reconnects."
    - "Independent reject, conflict, logout, account-switch, entry-disabled, and replay-disabled cases reset only before their case and assert retained local work."
    - "The Swift report contains only fixed assertion IDs and closed outcomes; it cannot include values, paths, device details, payloads, output, or logs."
  </behavior>
  <action>Extend the existing driver and test targets rather than creating another Xcode target. Add a physical-run mode that is unavailable on simulator and requires the Plan 01 contract. Keep the current Phase 161 pack operations, but require actual host adapter observations for verified installed bytes and offline audio. Drive app termination with `XCUIApplication.terminate()` and relaunch without setting reset launch arguments. Queue one selected and one free-form mutation through visible controls, then observe the closed local state. For recovery/scope/gate cases, use explicit host test adapter controls and stable nonsensitive accessibility IDs; never add hidden client authority over Phoenix outcomes. Emit one ephemeral canonical Swift report containing device-local assertions only.</action>
  <acceptance_criteria>
    - The generated sequence follows D-01 exactly and cannot reset between offline submission and replay.
    - A simulator or default nil adapter is explicitly non-passing.
    - Device tests prove installed offline audio and local state across real terminate/relaunch.
    - Recovery/fence/gate observations retain work and never expose backend reasons or sensitive values.
  </acceptance_criteria>
  <verify><automated>mix test test/crosswake/proof_lane/template_contract_test.exs test/crosswake/proof_lane/ios_verifier_test.exs --max-failures 1 &amp;&amp; bash script/verify_generated_ios_shell.sh --proof-lane --reference-pack-adapter | jq -e '.outcome == "passed" and .scope == "pack_audio_prerequisite"' &gt;/dev/null</automated></verify>
  <done>The generated iOS lane can execute the full physical sequence while simulator/reference runs remain advisory and non-promoting.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Orchestrate signed device execution and join dual-authority reports</name>
  <precondition>A runnable generated host exists; actual signed-device execution additionally requires the D-04 route, physical destination, signing, media, fixture, and backend controls checked by Plan 01 preflight.</precondition>
  <reversibility rating="costly">D-02/D-05 make report ownership and the physical invocation contract host-consumed surfaces.</reversibility>
  <read_first>
    - lib/mix/tasks/crosswake.proof_lane.physical_iphone.ex
    - lib/crosswake/proof_lane/physical_iphone_preflight.ex
    - lib/crosswake/proof_lane/physical_iphone_contract.ex
    - lib/crosswake/proof_lane/evidence.ex
    - script/verify_generated_ios_shell.sh
  </read_first>
  <files>lib/mix/tasks/crosswake.proof_lane.physical_iphone.ex, test/mix/tasks/crosswake.proof_lane.physical_iphone_test.exs</files>
  <behavior>
    - "After ready preflight, the task creates one mode-0700 private run root and invokes the selected signed physical destination; it never accepts a simulator substitution."
    - "It parses device-local and Phoenix reports independently, rejects owner overlap/missing assertions, and succeeds only on one complete exact passed manifest."
    - "Any failure removes ephemeral output owned by this invocation and returns one stable rule; no dated destination is created by `--run`."
    - "Command output is one closed JSON result and contains no xcodebuild/server text or private values."
  </behavior>
  <action>Fill the runner seam established by Plan 01. Use normalized host configuration and preflight-returned opaque handles rather than destination/credential/endpoint CLI flags. Execute the generated Xcode scheme against the selected physical destination with signing enabled, store all xcodebuild/Phoenix/raw reports under one caller-owned private temporary root, parse fixed records, and join them strictly by Plan 01 owner. Require every assertion exactly once and passed. Treat malformed output, process failure, unexpected files, owner overlap, incomplete assertions, and cleanup failure as non-passing stable rules. `--run --json` returns a sanitized in-memory candidate only; promotion remains Plan 05.</action>
  <acceptance_criteria>
    - The runner is unreachable before preflight and a simulator cannot be selected.
    - Complete owner-disjoint reports join into one exact assertion manifest; UI cannot satisfy backend assertions.
    - Failure leaves no dated evidence and cleans invocation-owned ephemeral output.
    - The only stdout is a closed JSON outcome/rule or sanitized candidate without sensitive fields.
  </acceptance_criteria>
  <verify><automated>mix test test/mix/tasks/crosswake.proof_lane.physical_iphone_test.exs test/crosswake/proof_lane/physical_iphone_preflight_test.exs --max-failures 1 &amp;&amp; mix format --check-formatted lib/mix/tasks/crosswake.proof_lane.physical_iphone.ex test/mix/tasks/crosswake.proof_lane.physical_iphone_test.exs</automated></verify>
  <done>One command runs the signed device and backend authorities sequentially and yields a complete sanitized candidate without retaining raw artifacts.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Mix runner -> xcodebuild/physical device | Tool output and destination metadata are sensitive and untrusted as proof until parsed. |
| XCUITest -> Phoenix report | Device observation cannot grant backend/session/domain authority. |
| raw reports -> sanitized candidate | Raw output may contain identifiers, paths, screenshots, logs, responses, or payloads. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-162-11 | Spoofing | physical destination | high | mitigate | Preflight plus runner reject simulator/unknown and tests ensure selected physical class is required. |
| T-162-12 | Elevation of Privilege | dual-authority join | high | mitigate | Task 2 enforces disjoint assertion ownership and complete Phoenix backend report. |
| T-162-13 | Information Disclosure | raw test artifacts | high | mitigate | Private ephemeral run root, closed parsers, sanitized stdout, and mandatory owned cleanup. |
| T-162-14 | Tampering | sequence/reset | high | mitigate | Task 1 contract tests prove no reset between submission, terminate/relaunch, and replay. |
| T-162-15 | Repudiation | incomplete assertion report | medium | mitigate | Exact fixed ordered manifest rejects missing, duplicate, extra, or reordered assertions. |

ASVS L1: all high threats are mitigated before a sanitized candidate can leave the runner.
</threat_model>

<verification>
- Generated contract and advisory reference lanes pass without promotion authority.
- Runner unit tests use injected processes/reports to prove physical-only selection, cleanup, and dual-authority completeness.
- The actual physical command is automation-owned; any unavoidable connection/signing/credential action is only prerequisite setup and never replaces assertions.
</verification>

<success_criteria>
- DEVICE-01..05 are executable through one host-owned physical driver.
- The D-01 sequence and D-02 authority split are mechanically enforced.
- Raw `.xcresult`, screenshots, video, logs, output, identifiers, payloads, and media remain ephemeral and unretained.
</success_criteria>

<output>
Create `.planning/phases/162-physical-iphone-adoption-proof/162-03-SUMMARY.md` when done.
</output>
