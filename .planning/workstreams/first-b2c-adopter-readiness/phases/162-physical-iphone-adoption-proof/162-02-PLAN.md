---
phase: 162-physical-iphone-adoption-proof
plan: "02"
type: execute
wave: 2
depends_on: [162-01]
files_modified:
  - priv/templates/crosswake/proof_lane/test/crosswake_proof_lane_test.exs.eex
  - priv/templates/crosswake/proof_lane/e2e/support/proof_lane_host_adapter.ts.eex
  - test/crosswake/proof_lane/template_contract_test.exs
  - examples/phoenix_host/test/crosswake_example/local_first/physical_iphone_authority_test.exs
autonomous: true
requirements: [DEVICE-02, DEVICE-03, DEVICE-04, DEVICE-05]
must_haves:
  truths:
    - "Per D-02/D-03, Phoenix independently proves current replay admission, scoped transactional idempotency/effect, retained reject/conflict outcomes, scope isolation, and entry/replay feature denial; device/UI output is never backend authority."
    - "Accepted replay applies one domain effect exactly once; rejected/conflicted/blocked work remains retained, ordered draining halts, and no silent delete, last-write-wins, or automatic retry exists per D-08."
    - "Logout/account switch fences before a new scope activates, while host disablement independently blocks route entry and replay without erasing queued work."
  artifacts:
    - path: "priv/templates/crosswake/proof_lane/test/crosswake_proof_lane_test.exs.eex"
      provides: "Generated host-owned Phoenix authority fixture contract with closed DEVICE observations."
    - path: "priv/templates/crosswake/proof_lane/e2e/support/proof_lane_host_adapter.ts.eex"
      provides: "Typed host callback surface for closed local/outbox observations without payload or authority leakage."
    - path: "examples/phoenix_host/test/crosswake_example/local_first/physical_iphone_authority_test.exs"
      provides: "Executable first-party analog for the generated host authority matrix."
  key_links:
    - from: "generated host fixture callbacks"
      to: "ReplayAdmission.authorize/4 and Study.apply_one/3"
      via: "current request authority followed by one Ecto transaction"
      pattern: "current_session|matching_scope|feature_enabled|apply_one"
    - from: "Phoenix closed observation report"
      to: "PhysicalIphoneContract backend_authority assertions"
      via: "exact ordered assertion IDs and outcomes only"
      pattern: "backend_authority|accepted|rejected|conflict|blocked"
---

<objective>
Make Phoenix the independent authority half of the physical exit test through host-owned generated fixtures and closed observations.

Purpose: Prove exactly-once domain application, retained recovery, scope fencing, and kill-switch behavior without trusting the shell, WebView, or test driver.
Output: Generated Phoenix fixture contract, typed local observation callbacks, and an executable example-host authority matrix.
</objective>

<artifacts_this_phase_produces>
- Host-owned callbacks for isolated reset, accepted/duplicate, rejected, conflict, scoped-session change, route-entry gate, replay gate, retained-count, and outbox-empty observations.
- Closed Phoenix authority report keyed only by Plan 01 assertion IDs and outcomes.
- No new endpoint taxonomy, generic resolver, generic replication module, account model, or retained payload surface.
</artifacts_this_phase_produces>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/162-physical-iphone-adoption-proof/162-CONTEXT.md
@.planning/phases/162-physical-iphone-adoption-proof/162-RESEARCH.md
@.planning/phases/160-scoped-replay-and-auth-safety/160-CONTEXT.md
@examples/phoenix_host/lib/crosswake_example/local_first/replay_admission.ex
@examples/phoenix_host/lib/crosswake_example/local_first/study.ex
@examples/phoenix_host/lib/crosswake_example/local_first/sync_controller.ex
@priv/templates/crosswake/proof_lane/test/crosswake_proof_lane_test.exs.eex
@priv/templates/crosswake/proof_lane/e2e/support/proof_lane_host_adapter.ts.eex

<interfaces>
- `ReplayAdmission.authorize/4` derives session, matching scope, route, feature, Sigra, and domain authority immediately before each event.
- `Study.apply_one/3` owns scoped idempotency and domain effect in one transaction; persisted accepted/rejected rows decide duplicate outcomes.
- `SyncController.sync_events/4` drains serially, removes accepted only, retains rejected/blocked, and exposes one closed halt class.
- Plan 01 `PhysicalIphoneContract.assertions/0` is the only backend assertion vocabulary.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Generate a closed host authority and fixture contract</name>
  <reversibility rating="costly">D-02/D-03 make this generated callback shape the host integration seam; reversal requires coordinated host templates and driver changes.</reversibility>
  <read_first>
    - .planning/phases/162-physical-iphone-adoption-proof/162-CONTEXT.md
    - lib/crosswake/proof_lane/physical_iphone_contract.ex
    - examples/phoenix_host/lib/crosswake_example/local_first/replay_admission.ex
    - examples/phoenix_host/lib/crosswake_example/local_first/study.ex
    - priv/templates/crosswake/proof_lane/test/crosswake_proof_lane_test.exs.eex
    - test/crosswake/proof_lane/template_contract_test.exs
  </read_first>
  <files>priv/templates/crosswake/proof_lane/test/crosswake_proof_lane_test.exs.eex, test/crosswake/proof_lane/template_contract_test.exs</files>
  <behavior>
    - "Generated tests require host-owned callbacks for isolated fixture reset and each accepted/duplicate/rejected/conflict/scope/gate case; missing callbacks block rather than skip."
    - "The accepted path calls current request admission then commits idempotency plus effect once; lost-response duplicate returns accepted with no second effect."
    - "Rejected and conflict cases keep the record available and stop later work; blocked admission keeps all unaccepted work."
    - "Reports contain fixed assertion IDs/outcomes and aggregate counts only, never payloads, scopes, accounts, endpoints, flags, responses, errors, or identifiers."
  </behavior>
  <action>Extend the existing generated ExUnit template rather than adding a parallel harness. Define an idiomatic host-owned adapter contract exercised by generated tests: callbacks must control isolated cases but Phoenix must still execute its real Plug/session/route/feature/Sigra/domain admission and Ecto transaction paths. Reset may occur between independent cases only. The offline-submit/relaunch/replay case must remain one uninterrupted fixture lifetime per D-01. Require explicit accepted duplicate, retained rejected, retained conflict, wrong/new scope denial, logged-out denial, disabled-entry denial, and disabled-replay denial observations. Normalize callbacks to fixed results and fail closed on missing, thrown, malformed, or extra data. Keep all host domain schema and account/flag policy outside Crosswake.</action>
  <acceptance_criteria>
    - Generated host tests cannot pass without every backend-authority assertion.
    - Exactly one accepted effect and idempotency record are observed across initial plus duplicate replay.
    - Rejected/conflict/blocked cases retain queued work and cannot continue automatically.
    - Generated output contains no open metadata, payload, identity, route path, endpoint, or flag value.
  </acceptance_criteria>
  <verify><automated>mix test test/crosswake/proof_lane/template_contract_test.exs --max-failures 1 &amp;&amp; bash script/verify_phoenix_host_proof_lane.sh</automated></verify>
  <done>The generated proof lane has a mandatory, host-owned Phoenix authority matrix with closed safe observations.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prove the generated authority matrix against the example Phoenix host</name>
  <reversibility rating="reversible">The example fixture demonstrates the stable generated contract without becoming reusable product authority.</reversibility>
  <read_first>
    - examples/phoenix_host/test/crosswake_example/local_first/sync_controller_test.exs
    - examples/phoenix_host/test/crosswake_example/local_first/replay_auth_test.exs
    - examples/phoenix_host/lib/crosswake_example/local_first/replay_admission.ex
    - examples/phoenix_host/lib/crosswake_example/local_first/study.ex
    - priv/templates/crosswake/proof_lane/e2e/support/proof_lane_host_adapter.ts.eex
  </read_first>
  <files>examples/phoenix_host/test/crosswake_example/local_first/physical_iphone_authority_test.exs, priv/templates/crosswake/proof_lane/e2e/support/proof_lane_host_adapter.ts.eex</files>
  <behavior>
    - "Current request/session and exact scope are re-derived before each event, including after logout/account switch and gate changes."
    - "Selected and free-form fixture categories remain opaque to proof output while both reach one accepted transactional effect."
    - "Entry disabled and replay disabled are separate assertions; both preserve the same queued aggregate."
    - "The TypeScript adapter observes local queue/lifecycle/UI facts only and cannot manufacture Phoenix outcomes."
  </behavior>
  <action>Add a discriminating example-host test module that implements the generated callback shape using the existing request-bound test authority, `ReplayAdmission`, `SyncController`, `Study`, and Repo sandbox. Use synthetic private fixture values only inside the test process and assert output projections contain fixed IDs, outcomes, and aggregate counts. Exercise mid-sequence feature-state change and fence-first scope transition, including a stale completion unable to affect the new scope. Extend the generated TypeScript adapter only with typed local observation callbacks required by the physical driver; do not give it backend outcome setters, credential/session authority, payload-returning methods, or a generic recovery interface.</action>
  <acceptance_criteria>
    - The example host proves all backend DEVICE assertions through real request admission and Ecto effects.
    - The accepted duplicate is discriminating: the domain-effect count remains one.
    - Scope/gate denials occur before new persistence and retain queued aggregate counts.
    - TypeScript compile-time contracts keep browser observation separate from Phoenix authority.
  </acceptance_criteria>
  <verify><automated>(cd examples/phoenix_host &amp;&amp; MIX_ENV=test mix test test/crosswake_example/local_first/physical_iphone_authority_test.exs --max-failures 1 &amp;&amp; npx tsc --noEmit) &amp;&amp; bash script/verify_phoenix_host_proof_lane.sh</automated></verify>
  <done>Phoenix independently proves replay, idempotency, recovery retention, scope isolation, and feature gating for the generated physical contract.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| device/WebView -> Phoenix | Client-supplied scope/event/UI state is untrusted and cannot grant admission. |
| generated fixtures -> host domain | Test controls may select cases but cannot bypass real request and transaction authority. |
| Phoenix authority -> proof report | Sensitive records/results must project to fixed low-cardinality observations only. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-162-06 | Elevation of Privilege | replay admission | high | mitigate | Tasks require current session, scope, route, feature, Sigra, and domain checks per event before persistence. |
| T-162-07 | Tampering | idempotency/effect | high | mitigate | Task 2 proves one scoped transaction and one effect across lost-response duplicate replay. |
| T-162-08 | Information Disclosure | host proof report | high | mitigate | Both tasks allow only fixed IDs/outcomes/counts and inject forbidden values through negative tests. |
| T-162-09 | Spoofing | generated fixture callbacks | medium | mitigate | Task 1 treats malformed/missing callback output as blocked and never accepts callback-authored backend passes. |
| T-162-10 | Denial of Service | retained halted drain | low | accept | Ordered drain intentionally halts on reject/conflict/block so learner work remains available for host recovery. |

ASVS L1: high threats are mitigated at the primary client-to-backend and backend-to-evidence trust boundaries.
</threat_model>

<verification>
- Generated template contract, example-host authority tests, TypeScript compile, and existing Phoenix proof lane pass.
- Existing Phase 160 replay/auth tests remain green.
- No UI assertion is counted as backend authority.
</verification>

<success_criteria>
- DEVICE-02 through DEVICE-05 have independent Phoenix authority proof.
- Reject/conflict/scope/gate behavior is explicit, retained, and non-echoing.
- D-02/D-03/D-08 ownership and non-goals remain intact.
</success_criteria>

<output>
Create `.planning/phases/162-physical-iphone-adoption-proof/162-02-SUMMARY.md` when done.
</output>
