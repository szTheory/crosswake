---
phase: 159-host-reusable-proof-lane
plan: "28"
type: execute
wave: 18
depends_on: ["159-27"]
files_modified:
  - priv/native/crosswake_evidence_promote.c
  - test/crosswake/proof_lane/evidence_test.exs
  - .planning/phases/159-host-reusable-proof-lane/159-VALIDATION.md
  - .planning/phases/159-host-reusable-proof-lane/159-VERIFICATION.md
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - .planning/STATE.md
autonomous: true
gap_closure: true
requirements: [PROOF-01, PROOF-02, PROOF-03, PROOF-04]
must_haves:
  truths:
    - "Evidence publication pins the requested destination parent and its newly reserved destination with held no-follow directory descriptors, and reservation, leaf writes, verification, handoff, failure policy, and synchronization remain relative to those descriptors (D-15, D-17 through D-19, D-22, PROOF-04)."
    - "A deterministic ancestor-replacement regression pauses immediately after reservation, replaces the pathname ancestor, and proves that the publisher never creates retained evidence in the replacement target or follows the substituted route (D-15, D-22, PROOF-04)."
    - "Concurrent promotion preserves exactly one destination winner, interrupted publication remains visibly non-passing, and repeated checks/generation retain their observed idempotent and read-only behavior at the already declared scope (D-02, D-20 through D-22, PROOF-01, PROOF-02)."
    - "One fresh unchanged-tree automated gate re-proves the additive Phoenix browser path, backend confirmation, empty outbox, duplicate idempotency, native/iOS contracts, UI overflow contract, privacy boundary, and descriptor-pinned publication before phase status advances (D-01 through D-23, PROOF-01 through PROOF-04)."
    - "Validation, verification, requirements, roadmap, and state agree with the fresh result; Phase 160 becomes available only after complete success while TODO-002 and adopter-instance completeness remain unknown_blocking."
  artifacts:
    - path: priv/native/crosswake_evidence_promote.c
      provides: "Descriptor-pinned, no-replace retained-evidence publisher with descriptor-relative verification, cleanup policy, and durability synchronization."
    - path: test/crosswake/proof_lane/evidence_test.exs
      provides: "Deterministic post-reservation ancestor-replacement, collision, interruption, concurrency, and retained-evidence regressions."
    - path: .planning/phases/159-host-reusable-proof-lane/159-VALIDATION.md
      provides: "Fresh safe command/result ledger for the containment repair and complete same-tree gate."
    - path: .planning/phases/159-host-reusable-proof-lane/159-VERIFICATION.md
      provides: "Goal-backward verdict tracing the descriptor authority and all preserved Phase 159 contracts."
    - path: .planning/REQUIREMENTS.md
      provides: "PROOF-01 through PROOF-04 status synchronized from the final gate."
    - path: .planning/ROADMAP.md
      provides: "Twenty-eight-plan Phase 159 execution and verification truth."
    - path: .planning/STATE.md
      provides: "Current blocker or Phase 160 discussion readiness synchronized from the final gate."
  key_links:
    - from: priv/native/crosswake_evidence_promote.c
      to: requested destination parent and destination inode
      via: "held parent_fd and destination_fd authority with mkdirat/openat/fstatat/renameat2-or-renameatx_np/unlinkat/fsync"
      pattern: "mkdirat|openat|fstatat|renameat2|renameatx_np|unlinkat|fsync"
    - from: test/crosswake/proof_lane/evidence_test.exs
      to: native reservation window
      via: "test-only compile-time barrier after reserved destination descriptor acquisition"
      pattern: "ancestor|reserve|barrier|replacement"
    - from: fresh verification verdict
      to: .planning/REQUIREMENTS.md, .planning/ROADMAP.md, and .planning/STATE.md
      via: "all-or-blocked reconciliation after the complete same-tree command"
      pattern: "gaps_found|complete|current_phase"
  prohibitions:
    - statement: "No pathname-derived leaf operation may run after destination reservation, and no ancestor substitution may redirect retained evidence."
      category: containment
      verification: automated
    - statement: "No raw payload, answer, identity, account, credential, token, media, transcript, device identifier, endpoint, compiler/helper output, canary bytes, or absolute temporary path may enter retained evidence or planning ledgers."
      category: privacy
      verification: automated
    - statement: "No stale or partial pass, advisory accessibility runtime, or unclassified probe may promote Phase 159 or unblock Phase 160."
      category: evidence
      verification: automated
    - statement: "No Android, UI expansion, TODO-002 closure, external API, generic sync/storage, dashboard, support promotion, or Phase 160-162 implementation may be added."
      category: scope
      verification: artifact-inspection
flagged_assumptions:
  - "[UNRESOLVED-SPEC-PROBE] PROOF-01 concurrency: the acceptance authority is the existing concurrent-generator and new adversarial publication evidence; it does not imply a cross-host or distributed guarantee."
  - "[UNRESOLVED-SPEC-PROBE] PROOF-02 idempotency: repeated generation/check behavior is accepted only for the same normalized nine-key input and host tree exercised by the deterministic suite."
  - "[UNRESOLVED-SPEC-PROBE] PROOF-02 concurrency: observed same-host concurrent behavior is required, without inferring broader coordination semantics."
  - "[UNRESOLVED-SPEC-PROBE] PROOF-03 manual classification: planner classifies this as an automatable additive-corpus and generated-contract requirement; the existing Phoenix Playwright plus template/iOS commands are the gate, with no human UAT."
  - "[UNRESOLVED-SPEC-PROBE] PROOF-04 manual classification: planner classifies this as an automatable privacy, integrity, and filesystem-containment requirement; adversarial tests and artifact inspection are the gate, with no manual approval."
  - "[PRESERVE] `.planning/config.json` is unrelated dirty state and `COVERAGE.md` is the no-external-API declaration; both remain byte-identical."
  - "[PRESERVE] UI-SPEC overflow stays covered by deterministic template/iOS checks; accessibility-size runtime stays advisory and non-promoting."
---

<objective>
Close the ancestor-replacement publication race and reconcile Phase 159 only from one fresh complete automated gate.

Purpose: Make retained evidence publication derive all authority from pinned directory descriptors so a changed pathname ancestor cannot redirect later writes, then restore milestone truth from executable evidence.
Output: Descriptor-relative native publication, a discriminating adversarial regression, fresh validation/verification, and synchronized phase status.

Decision fidelity: implement D-15, D-17 through D-19, and D-22 for PROOF-04; re-prove D-01 through D-23 and PROOF-01 through PROOF-04. Phase 160 replay/auth, Phase 161 pack/audio, Phase 162 device execution, Android, TODO-002 inputs, generic sync/storage, dashboards, and support promotion remain excluded.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/ADR-FIRST-B2C-ADOPTER.md
@.planning/FIRST-B2C-ADOPTER-ADOPTION-BRIEF.md
@.planning/FIRST-B2C-ADOPTER-ROUTE-POLICY-MAP.md
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/159-host-reusable-proof-lane/159-CONTEXT.md
@.planning/phases/159-host-reusable-proof-lane/159-RESEARCH.md
@.planning/phases/159-host-reusable-proof-lane/159-PATTERNS.md
@.planning/phases/159-host-reusable-proof-lane/159-UI-SPEC.md
@.planning/phases/159-host-reusable-proof-lane/159-VALIDATION.md
@.planning/phases/159-host-reusable-proof-lane/159-VERIFICATION.md
@.planning/phases/159-host-reusable-proof-lane/COVERAGE.md
@.planning/phases/159-host-reusable-proof-lane/159-27-SUMMARY.md
@priv/native/crosswake_evidence_promote.c
@lib/crosswake/proof_lane/native_promotion.ex
@lib/crosswake/proof_lane/evidence.ex
@test/crosswake/proof_lane/evidence_test.exs
@script/verify_phoenix_host_proof_lane.sh
</context>

<assumption_delta_decision>
Primary noun: descriptor-pinned destination authority.
Decision: replace post-reservation pathname authority with one held no-follow parent descriptor and one held no-follow reserved-destination descriptor; keep the accepted visible non-passing reservation policy on mid-publication failure, cleaning only invocation-created leaves relative to the held destination descriptor.
Rationale: the re-verification found an execution miss against the already locked D-22 containment contract, not a new public format or product decision.
</assumption_delta_decision>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Pin native publication authority and defeat ancestor replacement</name>
  <reversibility rating="reversible">The repair is private native filesystem machinery behind the unchanged Evidence.promote/3 contract and retained schema.</reversibility>
  <read_first>
    - AGENTS.md
    - .planning/phases/159-host-reusable-proof-lane/159-CONTEXT.md
    - .planning/phases/159-host-reusable-proof-lane/159-VERIFICATION.md
    - .planning/phases/159-host-reusable-proof-lane/159-27-SUMMARY.md
    - priv/native/crosswake_evidence_promote.c
    - lib/crosswake/proof_lane/native_promotion.ex
    - lib/crosswake/proof_lane/evidence.ex
    - test/crosswake/proof_lane/evidence_test.exs
  </read_first>
  <files>priv/native/crosswake_evidence_promote.c, test/crosswake/proof_lane/evidence_test.exs</files>
  <behavior>
    - "RED: a test-only build pauses after the destination has been reserved and opened; the test moves/replaces the pathname ancestor, releases the helper, and observes no artifact or completion marker in the substituted target."
    - "The held parent and reserved-directory descriptors remain the only authority for artifact creation, byte verification, pending-marker creation, no-replace marker handoff, owned-leaf cleanup, and directory synchronization."
    - "Existing simultaneous promoters still produce one complete winner and one stable collision result; an interrupted writer never produces a passing completion marker."
    - "Failures expose only the existing closed native exit classification and cannot echo the destination, barrier paths, canary content, or helper output."
  </behavior>
  <action>Start with a deterministic regression that compiles `crosswake_evidence_promote.c` with a test-only `CROSSWAKE_EVIDENCE_TEST_BARRIER` branch. Use two test-owned marker paths passed only to that special executable: after `mkdirat` plus successful no-follow `openat` of the reserved directory, the helper signals readiness and waits with a bounded deadline; the test renames the original parent, installs a symlinked replacement ancestor aimed at a separate escape directory, and releases the helper. Assert the test fails against the current path-based implementation, then require the repaired helper to leave the escape directory empty, retain no passing artifact at the substituted path, terminate deterministically, and keep canary/barrier/path bytes out of returned errors. Keep the barrier absent from ordinary builds and runtime behavior.

Refactor the publisher to split the destination into parent path plus single basename, reject invalid basename/root cases without echo, open the parent with `O_RDONLY | O_DIRECTORY | O_NOFOLLOW | O_CLOEXEC`, reserve with `mkdirat(parent_fd, basename, 0700)`, and immediately acquire `destination_fd` using `openat(parent_fd, basename, O_RDONLY | O_DIRECTORY | O_NOFOLLOW | O_CLOEXEC)`. Replace `matches_file` and `rename_noreplace` with private `matches_file_at` and `rename_noreplace_at` helpers using directory descriptors. Create `proof-lane-evidence.json` and `.complete.pending` via `openat` with exclusive/no-follow/close-on-exec flags, validate leaf identity/type/size and exact bytes through held leaf descriptors or `fstatat(..., AT_SYMLINK_NOFOLLOW)`, and hand off `.complete.pending` to `.complete` with Linux `renameat2` or Darwin `renameatx_np` using `destination_fd` for both sides and the platform no-replace flag. Synchronize each leaf, the reserved destination after the marker handoff, and the parent reservation using held descriptors.

Centralize terminal handling so descriptors close on every branch. On a failure after reservation, use `unlinkat(destination_fd, ...)` only for leaves this invocation created; do not traverse the replacement pathname, remove an advertised destination by path, recursively clean, or disturb an existing/concurrent winner. Preserve exit 10 for a real destination/marker collision and exit 20 for every other sanitized failure. Retain the exact bounded stdin frame, canonical evidence filename, 64-byte marker, modes, platform support, and public Elixir contract per D-15, D-17 through D-19, and D-22.</action>
  <acceptance_criteria>
    - The adversarial barrier test deterministically replaces the ancestor after reservation and observes zero retained-evidence leaves in the attacker-selected escape directory.
    - Ordinary native builds contain no active barrier behavior, compile warning-clean on the supported platform, and preserve the existing bounded frame and exit contract.
    - Every operation following reservation is rooted in held directory descriptors; both final leaves are regular, exact, durable, and visible as complete only after no-replace handoff.
    - Collision, interruption, callback, privacy, digest-snapshot, concurrent-winner, and post-promotion read checks remain green with stable non-echoing errors.
  </acceptance_criteria>
  <verify>
    <automated>mix test test/crosswake/proof_lane/evidence_test.exs &amp;&amp; cc -std=c11 -O2 -Wall -Wextra -Werror -o /tmp/crosswake-evidence-promote-159-28 priv/native/crosswake_evidence_promote.c &amp;&amp; mix format --check-formatted test/crosswake/proof_lane/evidence_test.exs</automated>
  </verify>
  <done>The native publisher cannot be redirected through a post-reservation ancestor substitution and retains the established privacy, collision, interruption, and durability contracts.</done>
</task>

<task type="auto">
  <name>Task 2: Run the complete same-tree gate and reconcile phase truth</name>
  <reversibility rating="reversible">The task records fresh evidence and synchronizes planning status without changing product, retained-schema, or support contracts.</reversibility>
  <read_first>
    - AGENTS.md
    - .planning/ADR-FIRST-B2C-ADOPTER.md
    - .planning/FIRST-B2C-ADOPTER-ADOPTION-BRIEF.md
    - .planning/FIRST-B2C-ADOPTER-ROUTE-POLICY-MAP.md
    - .planning/REQUIREMENTS.md
    - .planning/ROADMAP.md
    - .planning/STATE.md
    - .planning/phases/159-host-reusable-proof-lane/159-CONTEXT.md
    - .planning/phases/159-host-reusable-proof-lane/159-UI-SPEC.md
    - .planning/phases/159-host-reusable-proof-lane/159-VALIDATION.md
    - .planning/phases/159-host-reusable-proof-lane/159-VERIFICATION.md
    - .planning/phases/159-host-reusable-proof-lane/COVERAGE.md
    - .planning/phases/159-host-reusable-proof-lane/159-27-SUMMARY.md
    - priv/native/crosswake_evidence_promote.c
    - test/crosswake/proof_lane/evidence_test.exs
    - script/verify_phoenix_host_proof_lane.sh
  </read_first>
  <files>.planning/phases/159-host-reusable-proof-lane/159-VALIDATION.md, .planning/phases/159-host-reusable-proof-lane/159-VERIFICATION.md, .planning/REQUIREMENTS.md, .planning/ROADMAP.md, .planning/STATE.md</files>
  <action>Hash-protect `.planning/config.json` and `COVERAGE.md`, require Task 1's focused test and warning-clean native build to pass, then run the complete final-tree command from Plan 159-27 with the new ancestor-replacement regression included. Re-prove missing-only host generation, private helper provenance, poisoned-cache rejection, closed configuration, concurrent and repeated generator behavior, descriptor-pinned evidence publication, collision winner, interruption policy, completion-digest snapshot for both check arities, privacy/source validation, typed generated browser proof, real UI mutation, IndexedDB observation, reconnect through app code, backend confirmation, empty outbox, duplicate idempotency, iOS/XCTest/XCUITest contracts, shell syntax, and formatting.

Keep the UI-SPEC overflow consideration executable through the existing deterministic template/iOS assertions: text wraps within 24pt insets without clipping, ellipsis, horizontal scrolling, or type-size reduction, and the retry target remains at least 44x44pt. Record accessibility-size runtime as advisory and non-promoting. For the five spec-less probes, record the precise planner classifications from frontmatter: PROOF-01/PROOF-02 concurrency and PROOF-02 idempotency are limited to observed same-host deterministic tests; PROOF-03 is classified as automated additive browser/iOS contract evidence; PROOF-04 is classified as automated privacy/integrity/containment evidence. Do not create a UAT or human-verification gate.

Rewrite `159-VALIDATION.md` with fresh commands, observed counts, platform class, stable rule IDs, safe relative paths, closed pass/fail results, and protected before/after hashes only. Rewrite `159-VERIFICATION.md` goal-backward from the final tree, explicitly tracing `Evidence.promote/3` through `NativePromotion.publish/2` into held parent/destination descriptors and the adversarial replacement result, plus all D-01 through D-23, PROOF-01 through PROOF-04, UI overflow/advisory handling, and the five probe classifications. Only after the whole gate succeeds, complete all four PROOF rows, record 28/28 plans executed and verified in `ROADMAP.md`, and advance `STATE.md` to Phase 160 discussion. On any failure, keep `gaps_found`, preserve incomplete requirement status, and record one consistent safe blocker while Phase 160 remains blocked. Preserve TODO-002, adopter-instance `unknown_blocking`, Android freeze, Phase 160-162 ownership, no-external-API truth, and all unrelated bytes.</action>
  <acceptance_criteria>
    - One fresh unchanged tree passes the focused descriptor repair and every prior complete Phase 159 automated contract.
    - Validation and verification trace the held descriptors and discriminating replacement result without recording raw/native output or sensitive values.
    - All five spec-less probes retain explicit bounded classifications and none becomes a human gate or an authority broader than its automated evidence.
    - Requirements, roadmap, and state advance together only on complete success; otherwise they consistently retain the Phase 159 blocker and Phase 160 remains blocked.
    - `.planning/config.json` and `COVERAGE.md` remain byte-identical, and TODO-002, unknown_blocking adopter completeness, Android, advisory runtime, and downstream ownership remain unchanged.
  </acceptance_criteria>
  <verify>
    <automated>mix test test/mix/tasks/crosswake_gen_proof_lane_test.exs test/crosswake/proof_lane/config_test.exs test/crosswake/proof_lane/template_contract_test.exs test/crosswake/proof_lane/ios_verifier_test.exs test/crosswake/proof_lane/evidence_test.exs &amp;&amp; cc -std=c11 -O2 -Wall -Wextra -Werror -o /tmp/crosswake-evidence-promote-159-28-final priv/native/crosswake_evidence_promote.c &amp;&amp; cc -std=c11 -O2 -Wall -Wextra -Werror -o /tmp/crosswake-proof-lane-fs-159-28 priv/native/crosswake_proof_lane_fs.c &amp;&amp; npm --prefix examples/phoenix_host run typecheck:offline-route-proof &amp;&amp; bash script/verify_phoenix_host_proof_lane.sh &amp;&amp; bash -n script/verify_generated_ios_shell.sh script/verify_phoenix_host_proof_lane.sh &amp;&amp; mix format --check-formatted lib/crosswake/proof_lane/*.ex test/crosswake/proof_lane/*.exs test/mix/tasks/crosswake_gen_proof_lane_test.exs</automated>
  </verify>
  <done>Phase 159 status derives from a fresh complete automated proof of descriptor-pinned publication and every preserved contract, with no stale, advisory, manual, or partial promotion.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| destination pathname -> native publisher | A mutable filesystem namespace supplies an untrusted path; held no-follow descriptors must become the sole authority before any retained leaf is created. |
| native helper -> retained evidence directory | Artifact bytes and the completion marker cross into durable storage and must be exact, exclusive, ordered, and synchronized. |
| test barrier -> containment claim | Test-only coordination must deterministically expose the vulnerable window without entering ordinary builds or production behavior. |
| generated browser/iOS evidence -> phase claim | Automated browser and contract checks prove only the bounded Phase 159 surface; advisory runtime cannot promote support. |
| command output -> planning ledgers | Unbounded/raw results cross into durable planning files and must reduce to safe closed evidence. |
| verification verdict -> milestone status | Requirements, roadmap, and state must advance from one complete result or remain blocked together. |

## STRIDE Threat Register — ASVS L1, block on high

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- | --- |
| T-159-G28-01 | Tampering / Elevation of Privilege | destination ancestor | critical | mitigate | Pin parent and reserved destination with no-follow descriptors; all subsequent leaf operations and synchronization use *at APIs relative to held descriptors. |
| T-159-G28-02 | Spoofing | completion marker | high | mitigate | Exclusive descriptor-relative pending marker, exact digest bytes, no-replace descriptor-relative rename, leaf verification, and directory fsync. |
| T-159-G28-03 | Denial of Service | interruption and collision handling | medium | mitigate | Stable collision classification, bounded barrier, descriptor-relative owned-leaf cleanup, visible non-passing reservation, and concurrent-winner regression. |
| T-159-G28-04 | Information Disclosure | helper errors and ledgers | high | mitigate | Closed exit/rule/path results plus anti-echo assertions and safe validation fields only. |
| T-159-G28-05 | Repudiation | phase completion | critical | mitigate | Fresh complete same-tree gate and goal-backward reconciliation before any status advance. |
| T-159-G28-06 | Elevation of Privilege | advisory/manual/downstream claims | high | mitigate | Explicit bounded probe classifications; keep accessibility runtime advisory and preserve TODO-002, Android, and Phase 160-162 boundaries. |
| T-159-G28-SC | Tampering | package supply chain | low | accept | No install or dependency change occurs; repository-owned C/Elixir/test sources and existing compiler/toolchain are reused. |
</threat_model>

<verification>
Task 1 must first demonstrate the formerly exploitable timing window and then close it using held descriptors. Task 2 reruns the complete same-tree gate, compares protected hashes, and inspects all five reconciled planning files for a single observed verdict and explicit bounded probe classifications.
</verification>

<success_criteria>
- Ancestor replacement after reservation cannot redirect artifact or marker creation into an attacker-selected directory.
- Publication, verification, no-replace completion, owned-leaf failure handling, and durability derive from held directory descriptors.
- PROOF-01/PROOF-02 concurrency and idempotency remain scoped to deterministic same-host observations; PROOF-03/PROOF-04 remain fully automated classifications.
- Every prior Phase 159 host-ownership, browser, config, iOS/UI, privacy, integrity, and evidence contract passes on the repaired final tree.
- Phase status advances only on complete success while TODO-002, unknown_blocking adopter completeness, Android freeze, advisory runtime, and downstream ownership remain intact.
</success_criteria>

## Multi-Source Coverage Audit

| Source | ID | Feature / requirement | Plan | Status | Notes |
| --- | --- | --- | --- | --- | --- |
| GOAL | — | Configurable host-owned browser, shell, offline-island, and device-proof scaffolding | 159-28 | COVERED | Repairs the final retained-evidence containment blocker and reruns the complete proof lane. |
| REQ | PROOF-01 | Missing-only host-owned scaffold | 159-28 | COVERED-PRESERVED | Full generator suite plus bounded same-host concurrency classification. |
| REQ | PROOF-02 | Closed host configuration contract | 159-28 | COVERED-PRESERVED | Full config/generator suite plus bounded rerun idempotency and concurrency classifications. |
| REQ | PROOF-03 | Additive primary browser/unit/fixture corpus | 159-28 | COVERED-PRESERVED | Planner classifies it as automated; generated Phoenix proof and iOS/template contracts rerun. |
| REQ | PROOF-04 | Privacy-safe retained evidence | 159-28 | COVERED | Descriptor-pinned native publisher, adversarial replacement regression, and full privacy/integrity suite. |
| RESEARCH | — | Existing C/Elixir/Jason/Playwright/XCTest stack; validate-scan-promote and no new packages | 159-28 | COVERED | Uses repository native helper and current test/toolchain seams only. |
| CONTEXT | D-01-D-14 | Additive host ownership, closed config/browser/device boundaries, advisory runtime | 159-28 | COVERED-PRESERVED | Complete final gate reruns these locked contracts without widening scope. |
| CONTEXT | D-15-D-22 | Allowlisted, non-echoing, atomic, fail-closed evidence lifecycle | 159-28 | COVERED | Descriptor authority repair plus adversarial and preserved privacy/integrity tests. |
| CONTEXT | D-23 | Three-day stop and prohibited expansion | 159-28 | COVERED-PRESERVED | One bounded repair plan; no framework, platform, dashboard, or downstream expansion. |
| UI | — | Covered overflow and accessibility-size backstop | 159-28 | COVERED-PRESERVED | Deterministic contract reruns; runtime stays advisory/non-promoting. |
| API | — | No external API integration | 159-28 | COVERED-PRESERVED | `COVERAGE.md` remains hash-protected and unchanged. |

Deferred Phase 160 scoped replay/auth, Phase 161 pack/audio, Phase 162 physical-device proof, Android, generic sync/storage, dashboards, support promotion, and TODO-002 adopter inputs remain excluded. All spec-less probes are explicitly classified or retained as bounded assumptions in frontmatter and Task 2; none is silently dropped.

## Artifacts this phase produces

- Modified `priv/native/crosswake_evidence_promote.c`, with private `matches_file_at` and `rename_noreplace_at` symbols; no new public API, wire format, or retained schema.
- Modified `test/crosswake/proof_lane/evidence_test.exs`, adding the named ancestor-replacement publication regression and test-local barrier coordination; no production test hook outside the compile-time native branch.
- Updated `.planning/phases/159-host-reusable-proof-lane/159-VALIDATION.md` and `159-VERIFICATION.md` from fresh safe evidence.
- Updated `.planning/REQUIREMENTS.md`, `.planning/ROADMAP.md`, and `.planning/STATE.md` only as warranted by the observed complete result.
- Executor-created `.planning/phases/159-host-reusable-proof-lane/159-28-SUMMARY.md`.
- No new dependency, package, external API, ORM/schema operation, AI integration, UI surface, Android artifact, UAT handoff, support claim, or product symbol.

<output>
Create `.planning/phases/159-host-reusable-proof-lane/159-28-SUMMARY.md` when done.
</output>
