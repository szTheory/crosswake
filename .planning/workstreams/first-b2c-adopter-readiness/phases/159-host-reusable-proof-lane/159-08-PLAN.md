---
phase: 159-host-reusable-proof-lane
plan: "08"
type: execute
wave: 4
depends_on: [159-05]
files_modified:
  - lib/crosswake/proof_lane/generator.ex
  - lib/crosswake/proof_lane/generator_fs.ex
  - priv/native/crosswake_proof_lane_fs.c
  - test/mix/tasks/crosswake_gen_proof_lane_test.exs
autonomous: true
gap_closure: true
requirements: [PROOF-01, PROOF-02]
must_haves:
  truths:
    - "Every generate, check, and diff filesystem operation remains beneath the descriptor-pinned normalized host root even if a destination ancestor is a symlink or changes between validation and final open (D-01, D-02, D-06, PROOF-01, PROOF-02)."
    - "Symlink, ancestor-swap, and destination-race failures return stable relative-path rules without exposing rejected host paths or mutating files outside the host root (D-09, D-20, D-21)."
    - "Valid reruns remain missing-only and preserve host-owned bytes, while interrupted and concurrent runs retain one complete no-clobber manifest (D-02, D-22)."
  artifacts:
    - path: "lib/crosswake/proof_lane/generator_fs.ex"
      provides: "Closed Elixir boundary for descriptor-relative, no-follow generator operations"
    - path: "priv/native/crosswake_proof_lane_fs.c"
      provides: "Linux/Darwin openat-style traversal, exclusive creation, safe reads, and manifest publication"
    - path: "test/mix/tasks/crosswake_gen_proof_lane_test.exs"
      provides: "Symlinked ancestor and deterministic ancestor-swap race regressions for all actions"
  key_links:
    - from: "Crosswake.ProofLane.Generator.generate/1, check/1, diff/1"
      to: "Crosswake.ProofLane.GeneratorFS"
      via: "relative desired paths only; the filesystem helper owns root pinning and component traversal"
      pattern: "GeneratorFS"
---

<objective>
Close the remaining host-root escape by making every generator filesystem action descriptor-relative and no-follow.

Purpose: Lexical containment cannot survive symlinked ancestors or a validation-to-open race.
Output: A repository-owned filesystem boundary and adversarial executable regressions.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/ADR-FIRST-B2C-ADOPTER.md
@.planning/phases/159-host-reusable-proof-lane/159-CONTEXT.md
@.planning/phases/159-host-reusable-proof-lane/159-VERIFICATION.md
@.planning/phases/159-host-reusable-proof-lane/159-05-SUMMARY.md
@lib/crosswake/proof_lane/config.ex
@lib/crosswake/proof_lane/generator.ex
@lib/crosswake/proof_lane/native_promotion.ex
@priv/native/crosswake_evidence_promote.c
@test/mix/tasks/crosswake_gen_proof_lane_test.exs

<interfaces>
Keep `Generator.generate/1`, `check/1`, and `diff/1` return contracts unchanged. Introduce a narrow `GeneratorFS` API that accepts the validated absolute host root plus safe relative paths, never caller-supplied absolute destinations. Model native invocation and closed error translation on `Crosswake.ProofLane.NativePromotion.rename_noreplace/2`.
</interfaces>
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Confine one generated file through a descriptor-pinned no-follow path</name>
  <reversibility rating="reversible">The filesystem implementation is internal behind unchanged generator action contracts.</reversibility>
  <read_first>
    - .planning/phases/159-host-reusable-proof-lane/159-VERIFICATION.md
    - lib/crosswake/proof_lane/generator.ex
    - lib/crosswake/proof_lane/native_promotion.ex
    - priv/native/crosswake_evidence_promote.c
    - test/mix/tasks/crosswake_gen_proof_lane_test.exs
  </read_first>
  <files>lib/crosswake/proof_lane/generator_fs.ex, priv/native/crosswake_proof_lane_fs.c, test/mix/tasks/crosswake_gen_proof_lane_test.exs</files>
  <behavior>
    - "A generated browser destination whose `e2e` ancestor is a symlink is rejected before any outside file is created."
    - "A test-controlled ancestor replacement immediately before final open cannot redirect the created file."
    - "The helper opens a canonicalized root through no-follow component traversal, then uses root-relative descriptors for child operations."
  </behavior>
  <action>Start RED with the verifier's symlinked-`e2e` reproduction and a deterministic race hook that swaps a real destination ancestor for a symlink after traversal begins but before the final create. Implement `GeneratorFS` and the packaged C helper with fixed argv, closed exit codes, and no propagation of native stdout/stderr. Canonicalize the declared root once, reopen it from `/` one component at a time with directory and no-follow flags, and perform child traversal with `openat`/`mkdirat`; create the final file with exclusive and no-follow flags. Reject symlinks, non-directories, `.`/`..`, absolute child paths, unsupported platforms, compiler failure, or changed ancestors with a stable `PL-GENERATE-DESTINATION`/`PL-GENERATE-WRITE` result containing only the manifest-relative path. Do not add a replacement-capable, lexical-containment, or ordinary-open fallback.</action>
  <verify>
    <automated>mix test test/mix/tasks/crosswake_gen_proof_lane_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - The exact symlink escape from `159-VERIFICATION.md` creates no file outside the temporary host.
    - The controlled ancestor swap cannot change the directory reached by final create.
    - Unsupported helper/toolchain paths fail closed and leave the temporary tree byte-identical outside operation-owned staging.
  </acceptance_criteria>
  <done>One production generator write is pinned to the validated host root across ancestor symlinks and races.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Route generate, check, diff, and manifest lifecycle through the safe boundary</name>
  <reversibility rating="reversible">All public generator actions and desired relative paths remain unchanged.</reversibility>
  <read_first>
    - lib/crosswake/proof_lane/generator.ex
    - lib/crosswake/proof_lane/generator_fs.ex
    - test/mix/tasks/crosswake_gen_proof_lane_test.exs
  </read_first>
  <files>lib/crosswake/proof_lane/generator.ex, lib/crosswake/proof_lane/generator_fs.ex, priv/native/crosswake_proof_lane_fs.c, test/mix/tasks/crosswake_gen_proof_lane_test.exs</files>
  <behavior>
    - "All scaffold reads, missing checks, directory creation, exclusive writes, manifest staging, and no-clobber publication use the descriptor-relative boundary."
    - "Check and diff remain read-only under symlink and race attempts."
    - "Existing idempotency, host-byte preservation, interruption, and two-writer tests remain green."
  </behavior>
  <action>Replace `destination/2`, `within?/2`, `File.mkdir_p/1`, `File.open/3`, `File.read/1`, and manifest link/publication authority in Generator with relative operations through `GeneratorFS`. Keep EEx rendering in Elixir, but pass bytes and safe manifest-relative names to the helper. Make check/diff use descriptor-relative no-follow reads and classify a symlink/change as a safe finding rather than following it. Exercise a symlink or ancestor swap separately at the ExUnit, browser, iOS, and `.crosswake` manifest parents, including swaps before the final open and manifest publication. Preserve every D-02 missing-only/no-clobber outcome and D-20/D-21 output shape.</action>
  <verify>
    <automated>mix test test/mix/tasks/crosswake_gen_proof_lane_test.exs &amp;&amp; mix format --check-formatted lib/crosswake/proof_lane/generator.ex lib/crosswake/proof_lane/generator_fs.ex test/mix/tasks/crosswake_gen_proof_lane_test.exs &amp;&amp; test -s priv/native/crosswake_proof_lane_fs.c</automated>
  </verify>
  <acceptance_criteria>
    - Every generator filesystem call is mediated by `GeneratorFS`; no authoritative lexical `within?` or direct File read/write/create path remains.
    - Symlink and swap tests cover all generated namespaces and the manifest, and check/diff make no writes.
    - Valid edited files remain reused, and parallel/interrupted generation cannot overwrite them or expose a partial manifest.
  </acceptance_criteria>
  <done>PROOF-01 and PROOF-02 are symlink-safe and race-safe for every generator action.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| validated host root → native filesystem helper | Host-controlled filesystem topology can change during traversal. |
| relative desired state → host-owned files | Missing-only creation and inspection cross a permanent host boundary. |

## STRIDE Threat Register — ASVS L1, block on high

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- | --- |
| T-159-G08-01 | Tampering / Elevation of Privilege | generator destination traversal | high | mitigate | Descriptor-pinned no-follow traversal plus symlink and ancestor-swap tests for every namespace. |
| T-159-G08-02 | Tampering | manifest promotion | high | mitigate | Root-relative exclusive staging/publication with no replacement fallback and concurrent-winner tests. |
| T-159-G08-03 | Information Disclosure | helper failures | medium | mitigate | Closed exits translate to stable rule and relative path only; native output is discarded. |
| T-159-G08-SC | Tampering | package supply chain | low | accept | No package install or external integration; the helper is repository-owned source compiled locally. |
</threat_model>

<verification>
Run the full generator suite and formatter from the final tree; inspect that generator filesystem authority flows only through `GeneratorFS`.
</verification>

<success_criteria>
- No symlink or ancestor race can redirect a generator action outside its declared host root.
- Normal generation, check, and diff retain their established contracts and privacy-safe output.
- No schema, external API, Android, generic storage/sync, device-service, or support-claim surface is added.
</success_criteria>

## Artifacts this phase produces

- New internal boundary: `Crosswake.ProofLane.GeneratorFS`.
- New packaged native source: `priv/native/crosswake_proof_lane_fs.c` for Linux/Darwin descriptor-relative operations.
- No dependency, schema, endpoint, Android artifact, or `COVERAGE.md` change.

<output>
Create `.planning/phases/159-host-reusable-proof-lane/159-08-SUMMARY.md` when done.
</output>
