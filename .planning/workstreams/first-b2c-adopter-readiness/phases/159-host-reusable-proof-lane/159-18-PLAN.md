---
phase: 159-host-reusable-proof-lane
plan: "18"
type: execute
wave: 11
depends_on: [159-17]
files_modified:
  - examples/phoenix_host/package.json
  - examples/phoenix_host/tsconfig.offline_route_proof.json
  - examples/phoenix_host/e2e/crosswake_proof_lane/proof_lane.spec.ts
  - script/verify_phoenix_host_proof_lane.sh
  - test/crosswake/proof_lane/template_contract_test.exs
autonomous: true
gap_closure: true
requirements: [PROOF-01, PROOF-03]
must_haves:
  truths:
    - "The configured Phoenix-host proof command fails when the generated host proof is absent and, when present, typechecks and executes it beside the existing primary browser corpus (D-01, D-03, D-20, PROOF-01, PROOF-03)."
    - "The generated host proof drives a real UI mutation through the generated support adapter, observes IndexedDB, reconnects through application code, confirms one backend row, drains the outbox, and proves duplicate replay remains exactly once (D-03, D-07, PROOF-03)."
    - "Repeated successful runs reuse the existing Playwright configuration, fixtures, Phoenix webServer, and test database lifecycle without creating retained browser output or a separate proof authority (D-01, D-02, D-22, D-23)."
  artifacts:
    - path: "examples/phoenix_host/e2e/crosswake_proof_lane/proof_lane.spec.ts"
      provides: "Host-owned generated-proof fixture wired to the real Phoenix adapter and replay assertions"
    - path: "examples/phoenix_host/package.json"
      provides: "Primary proof command that explicitly selects the generated host proof"
    - path: "examples/phoenix_host/tsconfig.offline_route_proof.json"
      provides: "Closed TypeScript scope containing the generated spec and generated support adapter"
    - path: "script/verify_phoenix_host_proof_lane.sh"
      provides: "Fail-closed existence, typecheck, and Playwright orchestration"
  key_links:
    - from: "script/verify_phoenix_host_proof_lane.sh"
      to: "examples/phoenix_host/e2e/crosswake_proof_lane/proof_lane.spec.ts"
      via: "explicit required-file preflight followed by the package proof command"
      pattern: "proof_lane.spec.ts"
    - from: "proof_lane.spec.ts"
      to: "Phoenix persistence and IndexedDB"
      via: "runOfflineIslandProof with host adapter callbacks for backend confirmation, empty outbox, and duplicate idempotency"
      pattern: "assertBackendConfirmation|assertOutboxEmpty|assertDuplicateIdempotency"
  prohibitions:
    - statement: "The existing offline_sync.spec.ts, browser fixtures, Playwright configuration, and webServer remain the primary browser corpus and lifecycle authority."
      category: architecture
      verification: "bash script/verify_phoenix_host_proof_lane.sh"
    - statement: "No browser report, trace, screenshot, raw output, payload, identity, endpoint value, token, media, or device identifier becomes retained evidence."
      category: privacy
      verification: flagged-unverified
    - statement: "No Android lane, new browser framework, parallel proof authority, generic test orchestrator, or permanent CI lane is added."
      category: scope
      verification: flagged-unverified
flagged_assumptions:
  - "[UNRESOLVED/NO-CLAIM] PROOF-01 concurrency probe: simultaneous independent invocations share the existing Phoenix webServer/test-database seam; this plan guarantees fail-closed non-zero outcomes and no retained partial artifact, but does not claim concurrent-run isolation that the host corpus does not define."
  - "[RESOLVED] PROOF-03 unclassified probe: the existing Phoenix browser corpus remains primary, while the generated spec is an additive member of that same corpus and uses the same real fixtures and backend authority."
---

<objective>
Make the configured Phoenix-host command prove the generated, host-owned browser lane rather than passing when that lane is absent.

Purpose: Close the current false-green path while preserving the adopter's existing browser corpus as the single proof identity.
Output: One real generated-proof fixture, explicit command/typecheck inclusion, and deletion-sensitive automated coverage.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/ADR-FIRST-B2C-ADOPTER.md
@.planning/phases/159-host-reusable-proof-lane/159-CONTEXT.md
@.planning/phases/159-host-reusable-proof-lane/159-VERIFICATION.md
@.planning/phases/159-host-reusable-proof-lane/159-10-SUMMARY.md
@examples/phoenix_host/playwright.config.ts
@examples/phoenix_host/e2e/offline_sync.spec.ts
@examples/phoenix_host/e2e/support/offline_route_proof.ts
@examples/phoenix_host/e2e/crosswake_proof_lane/support/proof_lane.ts
@priv/templates/crosswake/proof_lane/e2e/proof_lane.spec.ts.eex
@script/verify_phoenix_host_proof_lane.sh
</context>

<assumption_delta_decision>
Primary noun: Phoenix-host browser proof corpus.
Decision: no-change.
Rationale: The existing Phoenix-host Playwright corpus remains the primary identity and authority; the generated host proof is integrated into it as an additive host-owned spec, not installed beside it as a second test system.
</assumption_delta_decision>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Execute the generated host proof through the primary Phoenix corpus</name>
  <reversibility rating="reversible">The change adds one host-owned fixture and narrows local command selection without changing a published runtime or data contract.</reversibility>
  <read_first>
    - .planning/phases/159-host-reusable-proof-lane/159-VERIFICATION.md
    - .planning/phases/159-host-reusable-proof-lane/159-CONTEXT.md
    - examples/phoenix_host/package.json
    - examples/phoenix_host/tsconfig.offline_route_proof.json
    - examples/phoenix_host/playwright.config.ts
    - examples/phoenix_host/e2e/offline_sync.spec.ts
    - examples/phoenix_host/e2e/support/offline_route_proof.ts
    - examples/phoenix_host/e2e/crosswake_proof_lane/support/proof_lane.ts
    - priv/templates/crosswake/proof_lane/e2e/proof_lane.spec.ts.eex
    - script/verify_phoenix_host_proof_lane.sh
    - test/crosswake/proof_lane/template_contract_test.exs
  </read_first>
  <files>examples/phoenix_host/package.json, examples/phoenix_host/tsconfig.offline_route_proof.json, examples/phoenix_host/e2e/crosswake_proof_lane/proof_lane.spec.ts, script/verify_phoenix_host_proof_lane.sh, test/crosswake/proof_lane/template_contract_test.exs</files>
  <behavior>
    - "RED: the wrapper and a structural regression fail when e2e/crosswake_proof_lane/proof_lane.spec.ts is absent from disk, the TypeScript project, or the Playwright selection."
    - "GREEN: the new host-owned fixture calls the rendered generated runOfflineIslandProof helper with real Phoenix-host callbacks and proves UI mutation, one queued record, application reconnect, backend count one, empty outbox, and duplicate accepted_count zero/count one."
    - "The existing offline_sync.spec.ts and browser_online_restore.spec.ts continue to run in the same command, and Playwright's existing webServer remains the only Phoenix/test-database lifecycle owner."
  </behavior>
  <action>Start with a deletion-sensitive failing contract test for all three seams: required file, TypeScript file list, and Playwright command selection. Add the generated host proof fixture under the already generated namespace, importing the rendered support adapter and only host-local existing helper primitives needed to implement the real Phoenix example adapter. Drive the same semantic sequence required by D-03, including one real button mutation, one IndexedDB record, application-owned online-event reconnect, backend confirmation, empty outbox, and a duplicate request that creates no second row. Add the generated support and spec to the explicit TypeScript project. Extend `proof:offline-island` to select this spec alongside the existing primary and cleanup specs. Make the shell wrapper explicitly require the generated spec and support file before typecheck/Playwright execution so deleting either is unconditionally non-zero even when other selected specs pass. Keep Playwright config, its webServer, fixtures, the unfiltered test command, and existing specs intact; keep all output ephemeral and use no package installation or external API.</action>
  <acceptance_criteria>
    - Removing the generated proof spec or generated support file makes `bash script/verify_phoenix_host_proof_lane.sh` fail before a green browser result.
    - The TypeScript project includes and typechecks both generated surfaces.
    - The package proof command runs the existing primary spec, cleanup regression, and generated host spec in one corpus.
    - The generated host spec reaches real Phoenix backend confirmation, outbox drain, and duplicate replay assertions; an assertion failure exits non-zero.
    - No existing browser fixture, Playwright configuration, or webServer lifecycle is replaced.
  </acceptance_criteria>
  <verify>
    <automated>mix test test/crosswake/proof_lane/template_contract_test.exs &amp;&amp; npm --prefix examples/phoenix_host run typecheck:offline-route-proof &amp;&amp; bash script/verify_phoenix_host_proof_lane.sh</automated>
  </verify>
  <done>The advertised one-command Phoenix proof is deletion-sensitive and executes the real generated host adapter path inside the existing authoritative corpus.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| proof command selection -> durable proof claim | An omitted generated spec can otherwise yield a false green result. |
| host browser/IndexedDB -> Phoenix backend | Untrusted queued data crosses through generated adapter callbacks to backend assertions. |

## STRIDE Threat Register — ASVS L1, block on high

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- | --- |
| T-159-G18-01 | Spoofing / Repudiation | Phoenix proof command | high | mitigate | Explicit file preflight, TypeScript inclusion, Playwright selection, and deletion-sensitive regression. |
| T-159-G18-02 | Tampering | generated host adapter | high | mitigate | Real UI, IndexedDB, backend-count, empty-outbox, and duplicate-idempotency assertions through the existing host. |
| T-159-G18-03 | Information Disclosure | browser output | medium | mitigate | Retain no reports or raw output; assertions use opaque mutation references and closed outcomes. |
| T-159-G18-04 | Denial of Service | parallel local invocations | low | accept | The existing single-host Playwright lifecycle is preserved; simultaneous-run isolation is explicitly not claimed. |
| T-159-G18-SC | Tampering | package supply chain | low | accept | No install or dependency change; use the locked existing Node/Playwright toolchain. |
</threat_model>

<verification>
Run the structural contract, explicit TypeScript project, and one-command Phoenix proof; the proof must include all three specs and fail if the generated spec/support is missing.
</verification>

<success_criteria>
- The generated host proof is a required, typed, executed member of the primary browser corpus.
- Its adapter proves backend confirmation, empty outbox, and exactly-once duplicate behavior with the real host.
- Missing generated proof can no longer pass, and no new test authority or retained artifact is introduced.
</success_criteria>

## Artifacts this phase produces

- New host-owned fixture path: `examples/phoenix_host/e2e/crosswake_proof_lane/proof_lane.spec.ts`.
- Expanded `proof:offline-island` and `tsconfig.offline_route_proof.json` inclusion contracts.
- Strengthened required-file preflight in `script/verify_phoenix_host_proof_lane.sh`.
- No new symbol, dependency, API matrix, CI lane, evidence schema, Android artifact, or external service.

<output>
Create `.planning/phases/159-host-reusable-proof-lane/159-18-SUMMARY.md` when done.
</output>
