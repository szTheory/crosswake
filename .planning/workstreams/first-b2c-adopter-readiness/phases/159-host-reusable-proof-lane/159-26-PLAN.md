---
phase: 159-host-reusable-proof-lane
plan: "26"
type: execute
wave: 16
depends_on: ["159-24"]
files_modified:
  - lib/crosswake/proof_lane/evidence.ex
  - test/crosswake/proof_lane/evidence_test.exs
autonomous: true
gap_closure: true
requirements: [PROOF-04]
must_haves:
  truths:
    - "Evidence.check/1 and check/2 decode, validate closed schema/privacy rules, and validate approved sources from the exact artifact bytes whose digest matched the regular completion marker; neither arity reopens the artifact pathname (D-15 through D-19, D-22, PROOF-04)."
    - "Deterministic replacement after digest verification but before decode/source validation proves both check arities consume the captured digest-bound snapshot and that the next check rejects the now-mismatched retained path (D-15, D-18, D-19, D-22, PROOF-04)."
    - "scan_stage/1 remains read-only and validates the same single-byte flow while public return shapes, the twelve-field schema, approved canonical-source rules, and stable non-echoing errors remain unchanged (D-15 through D-20, PROOF-04)."
    - "No generator, browser, iOS UI, Android, downstream replay/pack/device behavior, support claim, or TODO-002 state changes (D-01 through D-14, D-21, D-23)."
  artifacts:
    - path: lib/crosswake/proof_lane/evidence.ex
      provides: "One digest-bound artifact snapshot carried through scan, decode, and source validation."
    - path: test/crosswake/proof_lane/evidence_test.exs
      provides: "Discriminating replacement-between-verification-and-consumption regressions for check/1 and check/2 plus subsequent-path rejection."
  key_links:
    - from: Evidence.check/1 and Evidence.check/2
      to: .complete and proof-lane-evidence.json
      via: "one verified snapshot result containing captured bytes and decoded Evidence struct"
      pattern: "verify_complete_marker|scan_bytes|string_map_to_evidence|verify_sources"
    - from: verified canonical bytes
      to: approved source validation
      via: "decoded approved_hashes from the same byte binary, without a second File.read"
      pattern: "require_sources|verify_sources"
  prohibitions:
    - statement: "No check path may reopen the evidence artifact after its completion digest has been verified."
      category: integrity
      verification: automated
    - statement: "No raw artifact bytes, replacement canary, source bytes, absolute path, payload, identity, credential, token, media, transcript, or stable device identifier may enter diagnostics or ledgers."
      category: privacy
      verification: automated
    - statement: "No schema field, public API arity, dependency, UI, Android surface, or downstream phase behavior may be added."
      category: scope
      verification: artifact-inspection
flagged_assumptions:
  - "[RESOLVED-BY-TEST] Evidence acceptance is snapshot-based: the bytes whose marker digest passed are the only bytes decoded and source-validated in that call; a later call re-reads and rejects a changed path."
  - "[UNRESOLVED-SPEC-PROBE] PROOF-04 unclassified manual review has no plain phase SPEC classification; retain it as an explicit non-promoting assumption while automated integrity/privacy gates remain authoritative."
  - "[VERIFIED] No package installation is needed; Jason and OTP crypto are existing locked dependencies."
---

<objective>
Close the evidence-read TOCTOU gap by carrying one digest-bound artifact snapshot through scanning, decoding, and approved-source validation.

Purpose: Ensure accepted evidence bytes are exactly the bytes matched by the completion digest for both public check arities.
Output: Single-read verified evidence flow plus deterministic replacement regressions for check/1 and check/2.

Decision fidelity: implement D-15, D-16, D-17, D-18, D-19, D-20, and D-22 for PROOF-04; preserve D-01 through D-14, D-21, and D-23. Deferred Phase 160 replay/auth, Phase 161 pack/audio, Phase 162 device evidence, Android work, generic sync/storage, dashboards, support promotion, and TODO-002 inputs remain excluded.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/ADR-FIRST-B2C-ADOPTER.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/159-host-reusable-proof-lane/159-CONTEXT.md
@.planning/phases/159-host-reusable-proof-lane/159-VERIFICATION.md
@.planning/phases/159-host-reusable-proof-lane/159-22-SUMMARY.md
@lib/crosswake/proof_lane/evidence.ex
@test/crosswake/proof_lane/evidence_test.exs
</context>

<assumption_delta_decision>
Primary noun: digest-bound evidence snapshot.
Decision: promote the verified artifact byte binary to the single source for scanning, decoding, and source checks; remove the pathname as a second post-verification data source.
Rationale: path identity can change between reads, while carrying the captured bytes preserves the completion marker's exact content binding.
</assumption_delta_decision>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Consume the exact completion-digest-bound evidence bytes</name>
  <reversibility rating="reversible">The change is an internal data-flow correction behind unchanged Evidence.check/1, check/2, and scan_stage/1 contracts.</reversibility>
  <read_first>
    - AGENTS.md
    - .planning/phases/159-host-reusable-proof-lane/159-CONTEXT.md
    - .planning/phases/159-host-reusable-proof-lane/159-VERIFICATION.md
    - .planning/phases/159-host-reusable-proof-lane/159-22-SUMMARY.md
    - lib/crosswake/proof_lane/evidence.ex
    - test/crosswake/proof_lane/evidence_test.exs
  </read_first>
  <files>lib/crosswake/proof_lane/evidence.ex, test/crosswake/proof_lane/evidence_test.exs</files>
  <behavior>
    - "check/1 reads the artifact once, verifies the exact bytes against `.complete`, scans and decodes those bytes, and applies `require_sources/1` to the decoded Evidence value without another artifact path read."
    - "check/2 performs the same one-read flow and applies `verify_sources/2` to approved hashes decoded from those exact verified bytes."
    - "A closed test-only barrier replaces the advertised artifact after marker verification and before decoding; each arity still returns the result implied by the captured original bytes, while a subsequent ordinary check rejects the replaced on-disk artifact."
    - "The replacement regressions are deterministic, contain no sleeps or polling, and fail against the current implementation that calls `read_evidence/1` after `scan_stage/1`."
    - "Malformed markers, malformed JSON, noncanonical JSON, sensitive values, unexpected entries, symlinks, approved-source absence/mismatch, and post-promotion mutation retain their stable non-echoing outcomes."
  </behavior>
  <action>Write two discriminating regressions first, one for `Evidence.check/1` and one for `Evidence.check/2`. Use a module-private, test-only, process-local zero-arity barrier invoked exactly after the captured artifact bytes match the completion marker and before scan/decode/source validation. At the barrier, atomically replace the artifact pathname with malformed canary bytes while retaining the original marker. Assert the in-flight check validates only the captured original snapshot, the canary never appears in errors/results, and a second ordinary check rejects the now-mismatched directory. For check/2, build an original evidence value with a real approved canonical source and prove source verification uses the original decoded approved hashes. Require hook cleanup in `after` blocks and normalize any malformed hook behavior to the existing stable non-echoing integrity failure.

Refactor the reader pipeline so one private function enumerates the directory, enforces the two regular approved leaves, reads `proof-lane-evidence.json` once, verifies `.complete` against that binary, runs the closed barrier, scans/canonicalizes the same binary, decodes it to `%Evidence{}`, and returns the verified snapshot/evidence. `scan_stage/1` maps that result to `:ok`; `check/1` feeds its Evidence value to `require_sources/1`; `check/2` feeds the same value to `verify_sources/2`. Delete the second artifact read from `read_evidence/1` or remove that helper entirely. Do not pass the pathname back into decode or approved-source validation.

Preserve the exact twelve-field JSON schema, marker shape, `scan_stage/1`/`check/1`/`check/2` public results, approved canonical-source semantics, D-17 sensitive exclusions, D-18 hash restrictions, D-19 stable rule/path-only errors, and D-20 read-only behavior. The test barrier must be inaccessible as a public API and must not add retained metadata, logs, raw output, sleeps, packages, or filesystem authority.</action>
  <verify>
    <automated>mix test test/crosswake/proof_lane/evidence_test.exs &amp;&amp; mix format --check-formatted lib/crosswake/proof_lane/evidence.ex test/crosswake/proof_lane/evidence_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - Both check arities have deterministic replacement-after-verification regressions that fail on the old two-read implementation and pass only when original digest-bound bytes are consumed.
    - No production check flow reopens `proof-lane-evidence.json` after marker verification; scanning, canonical decode, and source validation use one captured binary.
    - A subsequent check of the replaced path fails closed, proving snapshot acceptance does not promote mutable pathname state.
    - Existing privacy, schema, marker, concurrency, collision, lifecycle-hook, approved-source, and anti-echo contracts remain green with no public API or dependency change.
  </acceptance_criteria>
  <done>Every Evidence reader validates and consumes one exact digest-bound canonical byte snapshot, including both public check arities under deterministic replacement.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| retained pathname -> captured bytes | Path contents are untrusted and mutable; one read establishes the candidate snapshot for this check. |
| captured bytes -> completion marker | Digest equality binds the exact snapshot before any semantic acceptance. |
| verified snapshot -> JSON/privacy decoder | Decode and closed-schema/privacy validation must consume only the bound binary. |
| decoded evidence -> approved sources | Source hashes must come from the same decoded snapshot and compare only with caller-approved canonical bytes. |

## STRIDE Threat Register — ASVS L1, block on high

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- | --- |
| T-159-G26-01 | Tampering | check/1 artifact replacement | critical | mitigate | One captured digest-bound byte flow plus deterministic replacement regression. |
| T-159-G26-02 | Tampering | check/2 artifact/source replacement | critical | mitigate | Decode approved hashes and validate supplied sources from the same verified snapshot. |
| T-159-G26-03 | Information Disclosure | replacement/barrier failures | high | mitigate | Process-local closed hook, stable rule/path-only errors, and anti-echo canaries. |
| T-159-G26-04 | Repudiation | retained evidence acceptance | high | mitigate | Subsequent ordinary check rejects changed path; tests distinguish in-flight snapshot from current retained state. |
| T-159-G26-SC | Tampering | package supply chain | low | accept | No install occurs; existing Jason and OTP crypto are reused. |
</threat_model>

<verification>
Run the focused evidence suite and targeted formatter. Require discriminating no-sleep replacement coverage for both check arities, subsequent mismatch rejection, exact marker binding, canonical JSON, approved sources, privacy, lifecycle hooks, collision/concurrency, and non-echoing errors.
</verification>

<success_criteria>
- The completion digest binds the exact bytes used by all semantic and source validation in each check call.
- Both public check arities are protected by deterministic adversarial replacement tests.
- Later pathname mutation remains visible to the next reader and cannot inherit prior acceptance.
- Public evidence schema/API, generator/browser/iOS/UI contracts, Android freeze, downstream phases, and TODO-002 remain unchanged.
</success_criteria>

## Multi-Source Coverage Audit

| Source | ID | Feature / requirement | Plan | Status | Notes |
| --- | --- | --- | --- | --- | --- |
| GOAL | — | Privacy-safe host-reusable retained proof | 159-26 | COVERED | Closes the digest/read data-flow blocker. |
| REQ | PROOF-04 | Reject sensitive retained evidence | 159-26 | COVERED | Exact verified bytes drive scan, decode, and source checks. |
| REQ | PROOF-01/PROOF-02 | Safe scaffold and closed config | 159-26 | PRESERVED | No generator/config files change; Plan 159-27 reruns them. |
| REQ | PROOF-03 | Primary browser corpus | 159-26 | PRESERVED | No browser files change; Plan 159-27 reruns executable proof. |
| RESEARCH | — | Typed allowlist plus validate-scan-promote stack | 159-26 | COVERED | Uses existing Evidence/Jason/OTP contracts only. |
| CONTEXT | D-15-D-20, D-22 | Exact schema, approved hashes, scanning, non-echoing atomic evidence | 159-26 | COVERED | One verified snapshot carries all acceptance work. |
| CONTEXT | D-01-D-14, D-21, D-23 | Additive host/browser/iOS/read-only/time-box boundaries | 159-26 | PRESERVED | No deferred or forbidden surface changes. |
| UI | — | Existing overflow contract and accessibility-size backstop | 159-26 | PRESERVED | No UI artifact changes; Plan 159-27 reruns deterministic contracts. |
| API | — | No external API integration | 159-26 | PRESERVED | Local retained-evidence validation only; existing `COVERAGE.md` remains unchanged. |

The spec-less PROOF-04 unclassified manual-review probe remains explicit and non-promoting; automated integrity/privacy evidence is the gate. Deferred Phase 160-162 behavior, Android work, generic sync/storage, dashboards, support promotion, and TODO-002 inputs remain excluded.

## Artifacts this phase produces

- Modified verified-snapshot flow in `lib/crosswake/proof_lane/evidence.ex`.
- Modified deterministic race regressions in `test/crosswake/proof_lane/evidence_test.exs`.
- No newly created symbol/path, dependency, schema field, generated host path, UI, Android artifact, external API, or support claim.

<output>
Create `.planning/phases/159-host-reusable-proof-lane/159-26-SUMMARY.md` when done.
</output>
