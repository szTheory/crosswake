---
phase: 159-host-reusable-proof-lane
plan: "22"
type: execute
wave: 14
depends_on: ["159-21"]
files_modified:
  - lib/crosswake/proof_lane/evidence.ex
  - lib/crosswake/proof_lane/native_promotion.ex
  - priv/native/crosswake_evidence_promote.c
  - test/crosswake/proof_lane/evidence_test.exs
autonomous: true
gap_closure: true
requirements: [PROOF-04]
must_haves:
  truths:
    - "A retained evidence directory passes only while its regular 64-byte `.complete` marker equals the lowercase SHA-256 of the exact canonical evidence artifact; every reader recomputes this binding (D-15, D-17, D-18, D-22, PROOF-04)."
    - "Mutation through the advertised artifact pathname after native readback, before marker publication, after marker publication, or after promotion returns makes the directory non-passing; helper success is denied at both deterministic barriers (D-15, D-19, D-22, PROOF-04)."
    - "Promotion serializes the unchanged twelve-field JSON exactly once, validates and hashes those bytes with OTP `:crypto`, sends only a bounded private frame to the native helper, and never creates or reopens a caller-data staging pathname (D-15 through D-19, PROOF-04)."
    - "An interrupted or replaced reservation remains visibly incomplete and non-passing; Crosswake never automatically removes, moves, restores, quarantines, or repurposes the advertised final directory (D-02, D-19, D-22)."
    - "The browser corpus, closed nine-key configuration, generated iOS UI/contracts, Android freeze, TODO-002, and Phase 160-162 boundaries remain unchanged (D-01 through D-14, D-16, D-20, D-21, D-23)."
  artifacts:
    - path: lib/crosswake/proof_lane/evidence.ex
      provides: "Canonical-byte SHA-256 derivation plus digest-bound complete-only scan/check/read behavior."
    - path: lib/crosswake/proof_lane/native_promotion.ex
      provides: "Bounded non-echoing byte-and-digest Port protocol with deterministic closed barriers."
    - path: priv/native/crosswake_evidence_promote.c
      provides: "Exclusive incomplete reservation, repeated artifact readback, and atomic digest-marker publication."
    - path: test/crosswake/proof_lane/evidence_test.exs
      provides: "No-sleep exact-byte mutation, marker-shape, interruption, replacement, and anti-echo regressions."
  key_links:
    - from: Crosswake.ProofLane.Evidence.promote/3
      to: priv/native/crosswake_evidence_promote.c
      via: "one bounded frame carrying approved digest plus the exact canonical bytes"
      pattern: "crypto.hash|Port.open|Port.command|exit_status"
    - from: priv/native/crosswake_evidence_promote.c
      to: retained evidence directory
      via: "artifact readback before and after atomic `.complete` publication"
      pattern: "memcmp|fsync|renameat|complete"
    - from: Evidence.check/1, Evidence.check/2, and Evidence.scan_stage/1
      to: .complete
      via: "exact marker shape and recomputed artifact SHA-256 equality"
      pattern: "proof-lane-evidence.json|.complete|sha256"
  prohibitions:
    - statement: "No evidence publication fallback may consume caller bytes through a caller-controlled named source."
      category: spec-less-fallback
      verification: flagged-unverified
    - statement: "No Crosswake path may automatically remove or relocate the advertised evidence directory after reservation."
      category: host-ownership
      verification: automated
    - statement: "No marker digest, recovery metadata, or publication state may be added to the twelve-field JSON schema."
      category: schema
      verification: automated
    - statement: "No raw evidence bytes, child output, canary, absolute path, payload, identity, credential, token, media, transcript, or device identifier may enter diagnostics or ledgers."
      category: privacy
      verification: automated
    - statement: "No generic IPC/filesystem library, external dependency, UI change, Android work, or Phase 160-162 behavior may be introduced."
      category: scope
      verification: artifact-inspection
flagged_assumptions:
  - "[VERIFIED] OTP 28 supplies `:crypto.hash(:sha256, bytes)` and this repository already uses it; the repair adds no package."
  - "[BOUNDED] `.complete` has one closed representation: exactly 64 lowercase hexadecimal SHA-256 bytes, no newline or metadata; the digest is not a thirteenth JSON field."
  - "[BOUNDED] The private protocol accepts one canonical evidence payload no larger than 64 KiB plus one fixed 64-byte digest; declared and received lengths must match exactly."
  - "[FLAGGED-UNVERIFIED] No alternate caller-byte transport is part of the spec-less fallback contract; concrete source-path-absence and fail-closed tests enforce the implemented safety truth without claiming external human wiring."
  - "[RESOLVED] A valid marker is an acceptance predicate, not an immutability promise: every Evidence reader recomputes the artifact digest, so mutation before or after marker publication cannot remain passing."
  - "[RESOLVED] Recovery chooses a new absent destination or explicit operator-owned removal after inspection; the helper never cleans or repurposes an advertised reservation."
---

<objective>
Repair the Phase 159 evidence boundary so acceptance is content-bound, not timing-bound: the marker atomically carries the approved artifact digest and every reader verifies it.

Purpose: Close the same-inode mutation race while preserving the exact twelve-field privacy schema and fail-closed host ownership.
Output: Digest-bearing atomic acceptance, repeated deterministic mutation barriers, complete-only readers, and focused evidence regressions.

Decision fidelity: implement D-15, D-17, D-18, D-19, and D-22 directly; preserve D-01 through D-14, D-16, D-20, D-21, and D-23 unchanged. Deferred Phase 160 replay/auth, Phase 161 pack/audio, Phase 162 device evidence, Android work, generic sync/storage, dashboards, and TODO-002 inputs remain excluded.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/ADR-FIRST-B2C-ADOPTER.md
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/159-host-reusable-proof-lane/159-CONTEXT.md
@.planning/phases/159-host-reusable-proof-lane/159-RESEARCH.md
@.planning/phases/159-host-reusable-proof-lane/159-PATTERNS.md
@.planning/phases/159-host-reusable-proof-lane/159-UI-SPEC.md
@.planning/phases/159-host-reusable-proof-lane/159-VERIFICATION.md
@.planning/phases/159-host-reusable-proof-lane/159-13-SUMMARY.md
@.planning/phases/159-host-reusable-proof-lane/159-21-SUMMARY.md
</context>

<assumption_delta_decision>
Primary noun: retained evidence acceptance.
Decision: replace the empty completion sentinel with an atomic closed SHA-256 marker and require digest equality at every read; keep the public JSON unchanged.
Rationale: inode identity and one readback cannot prevent later writes through the advertised pathname, while a recomputed digest predicate makes every mutation observably non-passing.
</assumption_delta_decision>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Bind evidence acceptance to the approved canonical-byte digest</name>
  <reversibility rating="reversible">The marker and Port framing are private implementation details behind the existing Evidence API and unchanged JSON schema.</reversibility>
  <read_first>
    - AGENTS.md
    - .planning/phases/159-host-reusable-proof-lane/159-CONTEXT.md
    - .planning/phases/159-host-reusable-proof-lane/159-VERIFICATION.md
    - lib/crosswake/proof_lane/evidence.ex
    - lib/crosswake/proof_lane/native_promotion.ex
    - priv/native/crosswake_evidence_promote.c
    - test/crosswake/proof_lane/evidence_test.exs
  </read_first>
  <files>lib/crosswake/proof_lane/evidence.ex, lib/crosswake/proof_lane/native_promotion.ex, priv/native/crosswake_evidence_promote.c, test/crosswake/proof_lane/evidence_test.exs</files>
  <behavior>
    - "Promotion serializes `Jason.encode!(Evidence.to_map(evidence))` once, applies the closed schema/privacy/canonical-source checks to that binary, derives its lowercase SHA-256 with `:crypto`, and sends the fixed digest plus exact bytes through one bounded private frame."
    - "The helper reserves the absent final directory at mode 0700, writes only `proof-lane-evidence.json`, performs full-length byte comparison, fsyncs it, and atomically publishes `.complete` containing exactly the 64 approved digest bytes."
    - "At a fixed barrier after first readback and before marker publication, mutation of the same inode through the advertised path yields failure and no valid marker."
    - "At a fixed barrier after marker publication and before native success classification, mutation of the same inode yields failure; the marker remains digest-bound to the approved bytes and all readers reject the mismatch."
    - "After promotion returns, changing the artifact while retaining the marker makes `scan_stage/1`, `check/1`, and `check/2` return the stable incomplete/integrity failure without echoing bytes."
    - "Missing, non-regular, short, long, uppercase, newline-terminated, malformed, or artifact-mismatched markers are rejected; extra entries and symlinks remain rejected."
    - "Interruption and reservation replacement leave the final pathname untouched and non-passing; concurrent collision winners remain byte-identical."
  </behavior>
  <action>Write deterministic regressions first for every behavior above. Replace sleeps and marker polling with a closed test-only Port barrier protocol. The helper may emit only fixed tokens identifying `after_readback` and `after_marker`; the wrapper invokes only an installed zero-arity callback returning exactly `:ok`, then sends one fixed continuation token. Preserve malformed callback return/raise/throw/exit coverage and map unexpected Port/helper state to the existing generic non-echoing evidence failure.

Refactor `Evidence.promote/3` to normalize and serialize once per D-15/D-17/D-18, scan and approved-source-check that exact binary in memory, compute `Base.encode16(:crypto.hash(:sha256, bytes), case: :lower)`, and pass the digest plus bytes to `NativePromotion`. Remove stage-directory creation, `File.rm_rf`, and all source-path arguments. Keep the twelve map fields and their JSON representation byte-for-byte unchanged. After native success, run the same digest-bound retained check before returning; this is a current acceptance check, while later readers repeat the predicate.

Make `scan_stage/1`, `check/1`, and `check/2` accept only the two regular leaves `proof-lane-evidence.json` and `.complete`. Read the marker as exactly 64 lowercase hex bytes with no terminator, recompute SHA-256 over the artifact bytes, compare in constant-time using `:crypto.hash_equals/2`, then run canonical JSON/privacy/source checks. Use one stable rule/path-only result for missing or integrity-invalid acceptance state and keep check operations read-only per D-19/D-20/D-22.

Replace `NativePromotion.rename_noreplace/3` with the bounded publisher while retaining the public Evidence result surface. Use binary stdio plus exit status; bytes and digest never appear in argv, environment, stdout, stderr, exceptions, or returned errors. Compile the repository helper as today and remove only this process's random executable outside the advertised namespace.

In `crosswake_evidence_promote.c`, pin the destination parent no-follow, exclusively reserve the absent final leaf as a directory, and never delete or move that advertised directory. Receive the fixed digest and at most 64 KiB of bytes with exact framing. Create the artifact with `openat` exclusive/no-follow, write all bytes, rewind and compare exact length/content, fsync, and set read-only mode. Re-read and compare at `after_readback`. Create a helper-private pending marker with exclusive/no-follow open, write exactly the 64 digest bytes, fsync it, then atomically rename it no-replace to `.complete` and fsync the directory. At `after_marker`, re-open the artifact no-follow and repeat exact-length/content comparison before success. Any error leaves the reservation visibly non-passing and performs no advertised-directory cleanup. Preserve D-02, D-15, D-17, D-18, D-19, and D-22; do not log payloads, native output, canaries, or absolute paths.</action>
  <verify>
    <automated>mix test test/crosswake/proof_lane/evidence_test.exs &amp;&amp; cc -std=c11 -O2 -Wall -Wextra -Werror -o /tmp/crosswake-evidence-promote-159-22 priv/native/crosswake_evidence_promote.c</automated>
  </verify>
  <acceptance_criteria>
    - A directory passes only when canonical artifact SHA-256 equals an exact closed-shape regular `.complete` marker; the JSON remains exactly twelve fields.
    - Both deterministic same-inode mutation barriers reject success, and a mutation after return is rejected by every public reader.
    - Interrupted, malformed-marker, extra-entry, replacement, and collision cases are stable, non-echoing, and never automatically clean or relocate the advertised destination.
    - The focused suite has no sleeps and covers source-path absence, exact protocol bounds, lifecycle-hook totality, approved hashes, concurrency, and anti-echo behavior.
  </acceptance_criteria>
  <done>Evidence acceptance is continuously content-bound to the approved canonical bytes through an atomic digest marker, with no schema change or advertised-path cleanup.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| typed candidate -> canonical bytes | Untrusted candidate data must pass the exact twelve-field, privacy, canonical-source, and D-17/D-18 rules before hashing. |
| canonical bytes -> helper stdin | Digest and bytes cross one bounded private frame; diagnostics expose fixed statuses only. |
| helper -> advertised reservation | The final directory is incomplete until an atomic exact-shape digest marker exists. |
| retained directory -> Evidence readers | Every read recomputes artifact SHA-256 and rejects marker/content mismatch. |
| reservation -> host ownership | After reservation, Crosswake has no automatic removal or relocation authority. |

## STRIDE Threat Register — ASVS L1, block on high

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- | --- |
| T-159-G22-01 | Tampering | artifact after native readback | high | mitigate | Digest-bearing marker plus deterministic pre-marker and post-marker mutation barriers and repeated readback. |
| T-159-G22-02 | Tampering | retained artifact after return | high | mitigate | Every scan/check recomputes artifact SHA-256 and constant-time-compares the exact marker. |
| T-159-G22-03 | Tampering / DoS | marker publication | high | mitigate | Closed 64-byte shape, regular-file requirement, helper-private pending write, fsync, and atomic no-replace rename. |
| T-159-G22-04 | Information Disclosure | Port/errors/tests | high | mitigate | Bounded stdin only, fixed control tokens, sanitized errors, and anti-echo canaries. |
| T-159-G22-05 | Elevation of Privilege | reservation cleanup | high | mitigate | No automatic deletion/move/restore of the advertised directory on any branch. |
| T-159-G22-SC | Tampering | package supply chain | low | accept | No install occurs; OTP crypto, Jason, and repository-owned C are reused. |
</threat_model>

<verification>
Run the focused evidence suite and warning-clean native build. Require exact marker shape, digest equality, both mutation barriers, mutation-after-return rejection, interruption, replacement, collision, anti-echo, lifecycle-hook totality, and no source-path/no advertised-cleanup evidence.
</verification>

<success_criteria>
- Exact approved canonical bytes determine the marker digest and remain the only passing artifact content.
- Marker publication is atomic; all incomplete or mismatched states are non-passing.
- The final directory is never automatically removed or relocated after reservation.
- Public JSON, UI, browser/config/iOS contracts, Android freeze, and downstream phase boundaries remain unchanged.
</success_criteria>

## Multi-Source Coverage Audit

| Source | ID | Feature / requirement | Plan | Status | Notes |
| --- | --- | --- | --- | --- | --- |
| GOAL | — | Privacy-safe host-reusable retained proof | 159-22 | COVERED | Content-bound acceptance repairs the retained-evidence blocker. |
| REQ | PROOF-04 | Reject sensitive retained evidence | 159-22 | COVERED | Canonical bytes, digest marker, readers, races, and anti-echo are executable. |
| REQ | PROOF-01/02/03 | Generated scaffold, config, and primary browser corpus | 159-22 | PRESERVED | No generator/config/browser/UI files change; Plan 159-24 reruns their gates. |
| RESEARCH | — | Existing Elixir/C stack and validate-scan-promote boundary | 159-22 | COVERED | Uses OTP crypto and repository helper only. |
| CONTEXT | D-15-D-19, D-22 | Typed allowlist, approved hash, final scan, atomic failure | 159-22 | COVERED | Exact D-NN behavior is implemented and tested. |
| CONTEXT | D-01-D-14, D-16, D-20-D-21, D-23 | Host/browser/config/iOS/read-only/time-box constraints | 159-22 | PRESERVED | No public surface or deferred behavior changes. |
| UI | D-14 | Existing wrapped labels and advisory accessibility backstop | 159-22 | PRESERVED | No UI artifacts change; deterministic contract reruns in Plan 159-24. |
| API | — | No external API integration | 159-22 | PRESERVED | Local evidence publication only. |

Deferred Phase 160-162 behavior, Android work, generic sync/storage, dashboards, support promotion, and TODO-002 inputs are excluded.

## Artifacts this phase produces

- Modified evidence boundary in `evidence.ex`, `native_promotion.ex`, and `crosswake_evidence_promote.c`.
- Modified focused regressions in `evidence_test.exs`.
- No dependency, schema field, generated host path, UI, Android artifact, external API, or support claim.

<output>
Create `.planning/phases/159-host-reusable-proof-lane/159-22-SUMMARY.md` when done.
</output>
