---
phase: 159-host-reusable-proof-lane
plan: "23"
type: execute
wave: 14
depends_on: ["159-21"]
files_modified:
  - lib/crosswake/proof_lane/generator.ex
  - lib/crosswake/proof_lane/generator_fs.ex
  - priv/native/crosswake_proof_lane_fs.c
  - test/mix/tasks/crosswake_gen_proof_lane_test.exs
autonomous: true
gap_closure: true
requirements: [PROOF-01, PROOF-02]
must_haves:
  truths:
    - "Generated files and the desired-state manifest are rendered once and sent through one bounded private frame to a helper-owned anonymous/already-unlinked descriptor; no caller-data staging leaf exists (D-02, D-19, D-22, PROOF-01)."
    - "Ordinary unprivileged Linux publishes an approved `O_TMPFILE` fd through a verified `/proc/self/fd/<fd>` reference and `linkat(AT_FDCWD, proc_ref, parent_fd, leaf, AT_SYMLINK_FOLLOW)`; missing or mismatched procfs fails closed and `AT_EMPTY_PATH` is absent (D-02, D-22, PROOF-01)."
    - "Darwin uses an already-unlinked regular descriptor with `fclonefileat` only after warning-clean compile and real runtime capability proof; unsupported filesystems/platforms fail closed without a weaker publication path (D-02, D-22, PROOF-01)."
    - "Collision, fault, and retry paths never remove, relocate, capture, restore, or rewrite an advertised final name; existing winners remain at their original pathname and byte-identical (D-02, D-20, D-21, D-22, PROOF-01)."
    - "The closed configuration, fixed manifest schema/paths, browser authority, generated iOS UI/contracts, Android freeze, TODO-002, and Phase 160-162 boundaries remain unchanged (D-01 through D-14, D-16 through D-21, D-23, PROOF-02)."
  artifacts:
    - path: lib/crosswake/proof_lane/generator.ex
      provides: "Single-render manifest and generated-file calls with unchanged missing-only results."
    - path: lib/crosswake/proof_lane/generator_fs.ex
      provides: "Bounded local Port writer without named caller-data input or manifest publish action."
    - path: priv/native/crosswake_proof_lane_fs.c
      provides: "Unprivileged Linux procfs descriptor-reference publication and guarded Darwin descriptor cloning."
    - path: test/mix/tasks/crosswake_gen_proof_lane_test.exs
      provides: "No-sleep capability, source-absence, collision, fault, and final-name preservation regressions."
  key_links:
    - from: Crosswake.ProofLane.Generator
      to: Crosswake.ProofLane.GeneratorFS.write/4
      via: "one already-rendered binary for each ordinary file and the manifest"
      pattern: "manifest_desired|GeneratorFS.write"
    - from: GeneratorFS.write/4
      to: priv/native/crosswake_proof_lane_fs.c
      via: "bounded stdin Port protocol with fixed control/status tokens"
      pattern: "Port.open|Port.command|exit_status"
    - from: anonymous Linux fd
      to: absent final leaf
      via: "verified process-private procfs fd reference followed by atomic no-replace linkat"
      pattern: "/proc/self/fd|AT_SYMLINK_FOLLOW|linkat"
    - from: already-unlinked Darwin fd
      to: absent final leaf
      via: "runtime-proved fclonefileat"
      pattern: "unlinkat|fclonefileat"
  prohibitions:
    - statement: "No generated-file publication fallback may accept caller bytes from a caller-controlled named source."
      category: spec-less-fallback
      verification: flagged-unverified
    - statement: "No Linux implementation may use `AT_EMPTY_PATH`; no procfs reference may be trusted without held-fd device/inode verification."
      category: platform
      verification: automated
    - statement: "No unsupported Linux procfs or Darwin clone capability may fall back to copy, rename, reopen, or caller-named publication."
      category: spec-less-fallback
      verification: flagged-unverified
    - statement: "No post-publication branch may mutate an advertised final leaf."
      category: host-ownership
      verification: automated
    - statement: "No generic IPC/filesystem abstraction, package, external API, UI change, Android work, or downstream behavior may be introduced."
      category: scope
      verification: artifact-inspection
flagged_assumptions:
  - "[VERIFIED] Linux documents the ordinary-unprivileged `O_TMPFILE` publication alternative through `/proc/self/fd/<fd>` plus `linkat(..., AT_SYMLINK_FOLLOW)`; `AT_EMPTY_PATH` would require `CAP_DAC_READ_SEARCH` and is prohibited here."
  - "[ASSUMED/TEST-BEFORE-USE] Supported Linux CI/host filesystems provide `O_TMPFILE` and mounted readable `/proc/self/fd`; the runtime test must prove both under ordinary privileges, otherwise publication is sanitized non-passing."
  - "[ASSUMED/TEST-BEFORE-USE] Current Darwin accepts `fclonefileat` from the already-unlinked regular descriptor on the destination volume; the real runtime probe gates success and unsupported capability fails closed."
  - "[FLAGGED-UNVERIFIED] No alternate publication transport is part of the spec-less fallback contract; concrete source-path-absence, syscall, and fail-closed tests enforce implemented safety without claiming external human wiring."
  - "[BOUNDED] One generated payload is capped at a constant above the largest current rendered proof-lane artifact; overflow fails before native filesystem mutation and the chosen ceiling is documented beside the renderer."
  - "[RESOLVED] Existing destination collision remains host ownership and may return `:reused` without comparing or rewriting host bytes."
---

<objective>
Repair generated-file and manifest publication so exact rendered bytes move from Elixir into anonymous native descriptors and publish atomically on ordinary unprivileged Linux and guarded Darwin.

Purpose: Close the manifest/source-swap blocker without staging cleanup or privileged Linux assumptions.
Output: Bounded byte publication, verified procfs fd linking, guarded Darwin cloning, and focused generator regressions.

Decision fidelity: implement D-02, D-19, D-20, D-21, and D-22 directly; preserve D-01, D-03 through D-18, and D-23 unchanged. Deferred Phase 160-162 behavior, Android work, generic sync/storage, dashboards, and support promotion remain excluded.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/ADR-FIRST-B2C-ADOPTER.md
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/159-host-reusable-proof-lane/159-CONTEXT.md
@.planning/phases/159-host-reusable-proof-lane/159-RESEARCH.md
@.planning/phases/159-host-reusable-proof-lane/159-PATTERNS.md
@.planning/phases/159-host-reusable-proof-lane/159-UI-SPEC.md
@.planning/phases/159-host-reusable-proof-lane/159-VERIFICATION.md
@.planning/phases/159-host-reusable-proof-lane/159-13-SUMMARY.md
@.planning/phases/159-host-reusable-proof-lane/159-21-SUMMARY.md
</context>

<assumption_delta_decision>
Primary noun: generated-file publication.
Decision: Linux links an anonymous O_TMPFILE through a verified process-private procfs fd reference; Darwin retains the already-unlinked-fd clone path behind real proof.
Rationale: the procfs form is the documented ordinary-unprivileged Linux route, while AT_EMPTY_PATH requires capability the supported host/CI surface does not have.
</assumption_delta_decision>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Publish exact rendered files through supported anonymous-descriptor primitives</name>
  <reversibility rating="reversible">The native publication mechanics remain private behind existing created/reused/error results and unchanged generated artifacts.</reversibility>
  <read_first>
    - AGENTS.md
    - .planning/phases/159-host-reusable-proof-lane/159-CONTEXT.md
    - .planning/phases/159-host-reusable-proof-lane/159-VERIFICATION.md
    - lib/crosswake/proof_lane/generator.ex
    - lib/crosswake/proof_lane/generator_fs.ex
    - priv/native/crosswake_proof_lane_fs.c
    - test/mix/tasks/crosswake_gen_proof_lane_test.exs
    - mix.exs
    - .github/workflows/release-please.yml
  </read_first>
  <files>lib/crosswake/proof_lane/generator.ex, lib/crosswake/proof_lane/generator_fs.ex, priv/native/crosswake_proof_lane_fs.c, test/mix/tasks/crosswake_gen_proof_lane_test.exs</files>
  <behavior>
    - "Ordinary generated files and `.crosswake/proof_lane.json` each use `GeneratorFS.write/4` with one already-rendered bounded binary; no input file, staging manifest leaf, or separate publish action exists."
    - "On Linux the helper creates `O_TMPFILE|O_RDWR|O_CLOEXEC` in the pinned destination parent, writes and full-length readback-compares bytes, fsyncs, verifies `/proc/self/fd/<fd>` opens to the held device/inode, then atomically links it no-replace with `AT_SYMLINK_FOLLOW`."
    - "Linux missing procfs, unreadable/mismatched fd reference, unsupported O_TMPFILE, or link denial returns the stable sanitized non-passing result with no final leaf and no alternate publication path."
    - "On Darwin the helper creates an exclusive private regular leaf in the pinned parent, unlinks it before reading caller bytes or exposing barriers, writes/readback-compares/fsyncs the held fd, and uses `fclonefileat`; real runtime proof gates success."
    - "Read/write/fsync faults occur before publication and leave no final leaf; EEXIST preserves the original winner byte-for-byte and returns the existing reused outcome."
    - "A fixed barrier before atomic publication supports deterministic winner installation; a fixed barrier after publication supports forced failure while proving Crosswake leaves the final pathname and exact published bytes untouched."
    - "Former input/staging-name decoys cannot affect output and are never removed; errors contain only PL-GENERATE rules and safe relative paths."
  </behavior>
  <action>Start with no-sleep regressions for all behaviors. Replace marker files and `usleep` with a closed Port barrier that emits only `before_publish` or `after_publish`, invokes only a zero-arity test callback returning exactly `:ok`, and resumes on one fixed token. Add an ordinary-unprivileged Linux runtime test that proves O_TMPFILE, opens `/proc/self/fd/<held_fd>`, compares `st_dev`/`st_ino`, and publishes with the exact procfs `linkat` form. Explicitly assert the production source contains `/proc/self/fd` and `AT_SYMLINK_FOLLOW` but does not select the privileged empty-path form. Add current-Darwin compile/runtime proof for already-unlinked-fd `fclonefileat`. Unsupported capability tests must assert sanitized failure and absent destination.

In `GeneratorFS`, remove `input_file/1`, `invoke_publish/3`, `publish/3`, source-path argv, environment-driven hook files, and staging cleanup. Implement a local bounded single-message Port sender for `write/4`; preserve `read/2`, `status/2`, `regular?/2`, safe relative-path errors, and the existing result tuples. Accept only closed test callback/fault options and discard all unexpected child output. Keep this seam private to GeneratorFS.

In `Generator`, construct `manifest.contents` once and pass it directly to `GeneratorFS.write/4`. Remove `promote_manifest/4` and staging suffixes while preserving fixed manifest keys/values/template version/paths, missing-only created/reused behavior, host-edit preservation, read-only check/diff, and the pre-manifest interruption boundary. Do not alter configuration, templates, browser authority, iOS UI, or schema.

In `crosswake_proof_lane_fs.c`, retain safe-relative validation and no-follow root/parent pinning, but make `write` consume one bounded frame and publish from a helper-owned descriptor. On Linux use `_GNU_SOURCE`, `openat(parent_fd, ".", O_TMPFILE|O_RDWR|O_CLOEXEC, 0600)`, exact write/readback/fsync/mode handling, construct `/proc/self/fd/<fd>`, open that reference, and require `fstat` device/inode equality with the held fd before `linkat(AT_FDCWD, proc_ref, parent_fd, leaf, AT_SYMLINK_FOLLOW)`. Treat absent procfs and `EOPNOTSUPP`/`EINVAL`/`EPERM` as sanitized failure. Do not use the privileged empty-path operation.

On Darwin retain a random exclusive/no-follow source only long enough to obtain a regular fd, unlink its name before accepting bytes or barriers, then exact-write/readback/fsync and call `fclonefileat(fd, parent_fd, leaf, 0)`. Guard declarations and runtime errors. On both platforms, once link/clone succeeds no branch may unlink, rename, capture, restore, quarantine, or rewrite the destination; a post-publication fault returns non-passing and leaves exact bytes for inspection. Preserve D-02/D-19-D-22 and introduce no dependency or generalized filesystem layer.</action>
  <verify>
    <automated>mix test test/mix/tasks/crosswake_gen_proof_lane_test.exs &amp;&amp; cc -std=c11 -O2 -Wall -Wextra -Werror -o /tmp/crosswake-proof-lane-fs-159-23 priv/native/crosswake_proof_lane_fs.c</automated>
  </verify>
  <acceptance_criteria>
    - No generated or manifest caller-data staging pathname exists; exact framed bytes publish from anonymous/already-unlinked descriptors.
    - Linux runtime proves ordinary-unprivileged procfs fd-reference publication with held inode verification and no privileged empty-path operation.
    - Darwin success requires warning-clean compilation and real already-unlinked-fd clone capability; missing capabilities fail closed without fallback.
    - Faults before publication leave no destination; collision and post-publication failures preserve the original final pathname and bytes.
    - Rerun, concurrency, host edits, ancestor swaps, check/diff, configuration, manifest provenance, browser, and iOS contracts remain green.
  </acceptance_criteria>
  <done>Generated artifacts publish atomically from exact descriptor-bound bytes on supported ordinary hosts, with no caller-named staging source or final-name cleanup.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| renderer -> GeneratorFS | Host-local bytes are untrusted for diagnostics and remain one bounded binary. |
| GeneratorFS -> native stdin | Caller bytes cross a private Port frame, never argv/env or a named source. |
| anonymous fd -> procfs reference | Linux must prove the kernel-owned process-private reference resolves to the held device/inode. |
| descriptor -> final destination | One atomic no-replace link/clone transfers exact bytes to an absent leaf. |
| published leaf -> failure handling | Crosswake has no cleanup authority after the advertised name exists. |

## STRIDE Threat Register — ASVS L1, block on high

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- | --- |
| T-159-G23-01 | Tampering | generated source bytes | high | mitigate | Remove caller-data source path; exact frame, full readback, and anonymous/already-unlinked fd. |
| T-159-G23-02 | Spoofing / Tampering | Linux procfs fd reference | high | mitigate | Open reference and compare device/inode with held fd before atomic linkat. |
| T-159-G23-03 | Elevation of Privilege | Linux publication | high | mitigate | Use documented unprivileged procfs alternative and reject the capability-requiring empty-path form. |
| T-159-G23-04 | Tampering / DoS | unsupported platform capability | high | mitigate | Real runtime probes; sanitized fail-closed status with no alternate publication path. |
| T-159-G23-05 | Tampering | concurrent/final destination | high | mitigate | Atomic no-replace publication and absolute prohibition on post-publication mutation. |
| T-159-G23-06 | Information Disclosure | Port/native failures | high | mitigate | Fixed control/status only, safe relative paths, and anti-echo regressions. |
| T-159-G23-SC | Tampering | package supply chain | low | accept | No package install; repository C and platform syscalls only. |
</threat_model>

<verification>
Run the focused generator suite and warning-clean helper build. Require current-platform real capability proof, exact Linux procfs inode verification where applicable, source-path absence, pre/post-publication barriers, collision winner preservation, fault-before-publication behavior, anti-echo, and preserved generator contracts.
</verification>

<success_criteria>
- Exact rendered bytes publish without caller-controlled named staging inputs.
- Linux works under ordinary privileges using verified procfs fd linking; Darwin works only when the proven clone primitive is present.
- Capability absence and all failures are non-passing without weaker fallback.
- Final-name host ownership, configuration, manifest, browser, iOS/UI, Android freeze, and downstream boundaries remain unchanged.
</success_criteria>

## Multi-Source Coverage Audit

| Source | ID | Feature / requirement | Plan | Status | Notes |
| --- | --- | --- | --- | --- | --- |
| GOAL | — | Host-owned configurable proof scaffolding | 159-23 | COVERED | Repairs the generated publication blocker. |
| REQ | PROOF-01 | Non-overwriting generated scaffold | 159-23 | COVERED | Anonymous publication, capabilities, collisions, and faults are executable. |
| REQ | PROOF-02 | Closed configuration and desired-state manifest | 159-23 | COVERED-PRESERVED | Manifest bytes use the same schema and config-derived values. |
| REQ | PROOF-03/04 | Browser corpus and retained evidence | 159-23 | PRESERVED | No browser/evidence files change; Plans 159-22/24 own those gates. |
| RESEARCH | — | No-new-dependency Elixir/C stack and missing-only ownership | 159-23 | COVERED | Uses existing modules and platform primitives only. |
| CONTEXT | D-02, D-19-D-22 | Host ownership, non-echo, read-only inspection, atomic lifecycle | 159-23 | COVERED | Exact D-NN behavior is implemented and tested. |
| CONTEXT | D-01, D-03-D-18, D-23 | Namespace, browser, config, iOS, privacy, stop rule | 159-23 | PRESERVED | No public/deferred surface changes. |
| UI | D-14 | Existing wrapped labels and advisory native backstop | 159-23 | PRESERVED | No UI files change; Plan 159-24 reruns contract proof. |
| API | — | No external API integration | 159-23 | PRESERVED | Local filesystem helper only. |

Deferred Phase 160-162 behavior, Android work, generic sync/storage, dashboards, support promotion, and TODO-002 inputs are excluded.

## Artifacts this phase produces

- Modified generator boundary in `generator.ex`, `generator_fs.ex`, and `crosswake_proof_lane_fs.c`.
- Modified focused regressions in `crosswake_gen_proof_lane_test.exs`.
- No dependency, config/manifest schema change, generated path, UI, Android artifact, external API, or support claim.

<output>
Create `.planning/phases/159-host-reusable-proof-lane/159-23-SUMMARY.md` when done.
</output>
