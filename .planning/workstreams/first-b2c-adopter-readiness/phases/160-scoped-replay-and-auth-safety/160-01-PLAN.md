---
phase: 160-scoped-replay-and-auth-safety
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/crosswake/offline/journal.ex
  - lib/crosswake/offline/replay.ex
  - lib/crosswake/offline/runtime.ex
  - test/crosswake/offline/journal_test.exs
  - test/crosswake/offline/replay_test.exs
  - test/crosswake/offline/runtime_test.exs
  - examples/phoenix_host/priv/static/offline_study.js
  - examples/phoenix_host/e2e/offline_sync.spec.ts
autonomous: true
requirements: [SCOPE-01, SCOPE-02]
must_haves:
  truths:
    - "Every journal entry, replay request, local record, lease, and completion requires the same bounded versioned opaque scope reference; no production operation addresses every retained scope (D-01, D-02, D-05, SCOPE-01)."
    - "Retained partitions launch inert, account transitions fence before activation, and an old epoch can affect neither the current UI nor another scope (D-03, D-04, D-06, SCOPE-02)."
    - "One active scope is read serially in journal order; accepted entries alone are removed while rejected, conflict, blocked, and ambiguous entries remain visible and recoverable (D-07, D-10, D-11, D-12)."
  artifacts:
    - path: "lib/crosswake/offline/journal.ex"
      provides: "Required versioned opaque scope on transport-only journal entries"
    - path: "lib/crosswake/offline/runtime.ex"
      provides: "Inactive/active/stopping lifecycle fence and monotonic epoch lease"
    - path: "examples/phoenix_host/priv/static/offline_study.js"
      provides: "Compound-key scope partition and exact-scope browser storage API"
    - path: "examples/phoenix_host/e2e/offline_sync.spec.ts"
      provides: "Deterministic two-scope lifecycle and retained-queue proof"
  key_links:
    - from: "Journal.Entry.scope_ref"
      to: "Replay.Request.scope_ref"
      via: "request_for_entry/1 copies the required value without deriving meaning"
      pattern: "scope_ref"
    - from: "Runtime active scope and epoch"
      to: "offline_study.js read/send/completion operations"
      via: "an exact scope+epoch lease checked before each storage or UI mutation"
      pattern: "scope_ref|epoch"
  prohibitions:
    - statement: "MUST NOT decode, derive, hash, enumerate, log, or repurpose an opaque scope reference as account, route, provider, device, or correlation identity."
      category: privacy
      verification: flagged-unverified
    - statement: "MUST NOT provide an unscoped/default/all-partition read, drain, count, delete, lease, checkpoint, or conflict path, or treat current cookies as proof that an old partition is authorized."
      category: scope-safety
      verification: flagged-unverified
flagged_assumptions:
  - "[FLAGGED/UNVERIFIED] SCOPE-01 remains an external edge assumption: the complete deterministic edge is required versioned opaque scope construction plus compound-key/index access, while the real host-issued format and rotation policy remain unknown_blocking."
  - "[FLAGGED/UNVERIFIED] SCOPE-02 remains an external edge assumption: cold-launch inactivity, switch-before-send, switch-in-flight, monotonic epoch invalidation, retained queues, and inert stale completions are the required deterministic lifecycle edges."
---

<objective>
Establish the scope-bound transport, storage, and lifecycle contract that the host admission tracer consumes.

Purpose: Make cross-scope access mechanically unavailable before any request reaches Phoenix.
Output: Scope-required Entry/Request contracts, exact-scope IndexedDB access, lifecycle epoch fencing, and deterministic browser proof.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/ADR-FIRST-B2C-ADOPTER.md
@.planning/ROADMAP.md
@.planning/phases/160-scoped-replay-and-auth-safety/160-CONTEXT.md
@.planning/phases/160-scoped-replay-and-auth-safety/160-RESEARCH.md
@.planning/phases/160-scoped-replay-and-auth-safety/160-PATTERNS.md

<interfaces>
`Journal.new_entry/1` and `Replay.request_for_entry/1` must share one conservative, non-echoing opaque scope contract. The browser store uses that value only as a compound key/index boundary. Plan 02 consumes the resulting sensitive request and active scope+epoch lease; no observation surface consumes either directly.
</interfaces>
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Trace one exact-scope entry through transport and deterministic browser storage</name>
  <reversibility rating="one-way">D-01 changes persisted and wire contracts, so reversal after execution requires coordinated storage and envelope migration.</reversibility>
  <read_first>
    AGENTS.md
    .planning/phases/160-scoped-replay-and-auth-safety/160-CONTEXT.md
    lib/crosswake/offline/journal.ex
    lib/crosswake/offline/replay.ex
    examples/phoenix_host/priv/static/offline_study.js
    test/crosswake/offline/journal_test.exs
    test/crosswake/offline/replay_test.exs
  </read_first>
  <files>lib/crosswake/offline/journal.ex, lib/crosswake/offline/replay.ex, test/crosswake/offline/journal_test.exs, test/crosswake/offline/replay_test.exs, examples/phoenix_host/priv/static/offline_study.js, examples/phoenix_host/e2e/offline_sync.spec.ts</files>
  <behavior>
    - Test: every Entry and Request requires one matching versioned bounded opaque scope and malformed values fail with a stable rule/path that does not echo input (D-01, D-05).
    - Test: one synthetic active scope queues and reads one event through compound `(scope_ref, local_ref)` identity while a second retained partition is untouched (D-02).
    - Test: entries, checkpoints, conflicts, and leases expose exact-scope operations only; no production read/drain enumerates all partitions (D-02, D-06).
    - Test: replay wire serialization contains the sensitive scope needed by the host, while the value never appears in browser status, console, error, or retained proof bytes (D-01, D-15).
  </behavior>
  <action>Start with failing core contract tests and the thinnest browser storage assertion. Per D-01, add a required versioned opaque `scope_ref` to Journal.Entry and Replay.Request, validate only a conservative bounded transport shape and equality, copy it directly in `request_for_entry/1`, and keep transport serialization explicitly sensitive. Per D-02, migrate the existing single IndexedDB outbox to compound scope/local identity and scope indexes for records and related replay state; every public production operation must require one scope. Use two synthetic opaque fixtures without inferring the unknown host issuance or rotation policy. Preserve the existing browser proof seam and TODO-002 as unknown_blocking.</action>
  <acceptance_criteria>
    - Core construction and wire tests prove D-01 required/versioned/opaque/non-echoing behavior.
    - Exact-scope browser access reaches one partition and leaves a second partition byte-for-byte untouched.
    - No production browser API can read, count, drain, delete, checkpoint, conflict, or lease without one scope.
    - ASVS L1 V3 Session Management, V5 Input Validation, and V8 Data Protection are evidenced by focused contract and partition tests.
  </acceptance_criteria>
  <verify><automated>mix test test/crosswake/offline/journal_test.exs test/crosswake/offline/replay_test.exs</automated></verify>
  <done>One scope-required entry can be constructed, serialized for transport, stored, and retrieved only through its exact partition without revealing or interpreting the scope.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Fence relaunch, logout, account switching, and ordered exact-scope drain</name>
  <reversibility rating="costly">The persisted lifecycle/epoch contract coordinates retained browser state with later host and device proof consumers.</reversibility>
  <read_first>
    .planning/phases/160-scoped-replay-and-auth-safety/160-CONTEXT.md
    lib/crosswake/offline/runtime.ex
    test/crosswake/offline/runtime_test.exs
    examples/phoenix_host/priv/static/offline_study.js
    examples/phoenix_host/e2e/offline_sync.spec.ts
  </read_first>
  <files>lib/crosswake/offline/runtime.ex, test/crosswake/offline/runtime_test.exs, examples/phoenix_host/priv/static/offline_study.js, examples/phoenix_host/e2e/offline_sync.spec.ts</files>
  <behavior>
    - Test: new or restored storage is inactive, reveals no retained payload, sends nothing, and issues no lease until explicit host activation (D-03, D-05).
    - Test: logout/switch enters stopping, increments epoch, prevents reads/sends, cancels or awaits the worker, and only then permits a new scope; an old completion mutates neither storage nor UI (D-04, D-10).
    - Test: the active scope drains serially in journal order; accepted duplicates are removable, rejected/conflict remain, and blocked halts with the current and later entries retained without a hot retry (D-07, D-11, D-12).
    - Test: the learner sees text-semantic saved/syncing/attention/paused status with stable focus, an appropriate live region, and no backend vocabulary or progress spinner while blocked (D-21, D-23).
  </behavior>
  <action>Implement D-03's closed inactive/active/stopping lifecycle and monotonic epoch in Runtime and persist it beside the browser outbox. Per D-04 and D-06, provide scope-targeted activation, fencing, and lease equality primitives only; account mapping, encryption, retention, deletion, and recovery remain host-owned. Require the captured scope+epoch before every read, send, completion, deletion, checkpoint, conflict, and UI mutation. Preserve ambiguous old entries when a callback becomes stale. Implement the client half of D-07/D-11/D-12: serial order, accepted-only deletion, retained rejection/conflict, blocked halt, and no automatic authority fallback. Render D-21/D-23 learner status through existing tokens and accessible status semantics.</action>
  <acceptance_criteria>
    - Relaunch, switch-before-send, and switch-in-flight tests prove no cross-scope display, send, delete, or UI mutation.
    - Missing, malformed, inactive, mismatched, and old-epoch operations visibly block and preserve queued work.
    - Nth-event blocked behavior retains N and later entries in order and starts no hot retry.
    - ASVS L1 V3 Session Management and V4 Access Control are evidenced by epoch invalidation and exact-scope lease tests.
  </acceptance_criteria>
  <verify><automated>mix test test/crosswake/offline/runtime_test.exs &amp;&amp; (cd examples/phoenix_host &amp;&amp; npm run proof:offline-island -- --grep "inactive relaunch|switch before send|switch in flight|ordered blocked drain")</automated></verify>
  <done>Retained partitions start inert, account transitions fence before activation, and stale or blocked work cannot cross scopes or disappear.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| host activation -> retained partition | A host-issued sensitive scope may activate exactly one otherwise inert partition. |
| async request completion -> current browser state | A completion must present the same active scope and epoch before any mutation. |

## ASVS L1 Control Mapping

| ASVS L1 category | Required control | Task | Automated evidence |
|---|---|---|---|
| V3 Session Management | Inert relaunch, fence-first transition, and stale epoch rejection. | Task 2 | Runtime tests and focused switch/relaunch Playwright cases. |
| V4 Access Control | Exact-scope operations and accepted-only deletion. | Tasks 1-2 | Contract tests plus two-scope ordered-drain proof. |
| V5 Input Validation | Closed bounded scope shape with stable non-echoing failures. | Task 1 | Journal and replay tests. |
| V8 Data Protection | Scope remains sensitive and absent from browser diagnostics/proof bytes. | Tasks 1-2 | Contract and browser output assertions. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-160-01 | Spoofing/Elevation | scope contract and IndexedDB partition | high | mitigate | Task 1 requires exact scope equality and exact-scope storage APIs. |
| T-160-02 | Tampering | lifecycle and stale completion | high | mitigate | Task 2 fences first, increments epoch, and proves old callbacks inert. |
| T-160-06 | Denial of Service/Elevation | blocked drain | high | mitigate | Task 2 halts without hot retry and retains every unaccepted entry. |
| T-160-SC | Tampering | package installs | low | accept | No package-manager installation occurs; existing runtime and test dependencies are reused. |
</threat_model>

<verification>
Run both focused task commands; each is intended to return actionable core or browser feedback in under 30 seconds. Do not run the approximately 300-second phase gate in this plan.
</verification>

<success_criteria>
Scope-bound data is unreachable without explicit matching activation, and every account transition prevents stale access before a new scope becomes active.
</success_criteria>

<output>
Create `.planning/phases/160-scoped-replay-and-auth-safety/160-01-SUMMARY.md` when done.
</output>
