---
phase: 160-scoped-replay-and-auth-safety
plan: "13"
type: execute
wave: 10
depends_on: ["160-12"]
files_modified:
  - examples/phoenix_host/priv/static/offline_study.js
  - examples/phoenix_host/e2e/offline_sync.spec.ts
  - .planning/phases/160-scoped-replay-and-auth-safety/160-VALIDATION.md
autonomous: true
gap_closure: true
requirements: [SCOPE-01, SCOPE-02, SCOPE-03, SCOPE-04, SCOPE-05]
must_haves:
  truths:
    - "After the host successfully activates an authorized scope while the browser is already online, retained work for that exact scope enters the existing lease-guarded replay worker without waiting for another mutation or connectivity event (D-03, D-05, D-07; SCOPE-01, SCOPE-02, SCOPE-03)."
    - "An activation-triggered replay may read, send, delete, or render only while its captured scope-and-epoch lease remains current; a fence or account switch makes the old invocation inert and leaves retained work partitioned (D-04, D-05; SCOPE-01, SCOPE-02)."
    - "A 200 response with no halt is complete only when accepted records account for the entire submitted batch in the same order; a truncated, reordered, duplicated, unknown, or otherwise mismatched acknowledgement is blocked before deletion and renders the existing paused state (D-11, D-12; SCOPE-03)."
    - "The two browser regressions prove one activation-triggered request, exact-scope emptying after a full acknowledgement, full retention after a truncated acknowledgement, paused rendering, no hot retry, and no scope or payload disclosure (D-14, D-17, D-21, D-22; SCOPE-01 through SCOPE-05)."
    - "One fresh complete same-tree Phase 160 gate supersedes the post-160-12 ledger evidence while TODO-002, adopter-instance completeness, generated iOS/device proof, and independent security retain their explicit non-passing status."
  artifacts:
    - path: "examples/phoenix_host/priv/static/offline_study.js"
      provides: "Online activation-to-worker wiring and exact complete-acknowledgement classification through existing scoped lease guards."
    - path: "examples/phoenix_host/e2e/offline_sync.spec.ts"
      provides: "Deterministic Playwright regressions for retained online activation and truncated successful acknowledgement handling."
    - path: ".planning/phases/160-scoped-replay-and-auth-safety/160-VALIDATION.md"
      provides: "Fresh post-160-13 focused and complete same-tree evidence reconciliation."
  key_links:
    - from: "activateScope(scopeRef) successful lifecycle write"
      to: "flushOutbox()"
      via: "online-only dispatch through the captured current scope-and-epoch lease and existing single-worker guard"
      pattern: "activateScope|navigator.onLine|flushOutbox|leaseIsCurrent"
    - from: "submitted exact-scope records"
      to: "classifyReplayResponse(data, records)"
      via: "full cardinality and index-by-index client_mutation_id equality before complete classification"
      pattern: "acceptedRecords.length|records.length|client_mutation_id|blocked"
    - from: "blocked replay classification"
      to: "retained IndexedDB records and learner-visible paused state"
      via: "return before deleteAcceptedMutations followed by renderPausedStatus without retry scheduling"
      pattern: "kind === 'blocked'|renderPausedStatus|deleteAcceptedMutations"
  prohibitions:
    - statement: "Preserve Phoenix-first explicit route/runtime ownership, host backend authority, exact-scope partitions, and fence-first account transitions; do not create a generic or background replay scheduler."
      category: project-boundary
      verification: flagged-unverified
    - statement: "Preserve sensitive-payload isolation: no replay payload, scope reference, account/session/provider/device identity, credential, token, endpoint, flag name, or stable correlator may enter status, console, logs, aggregates, inspection, or retained evidence."
      category: privacy
      verification: flagged-unverified
    - statement: "Do not reopen repaired Phoenix admission, Ecto authority, Sigra, privacy projection, Android, native storage, dashboard, commerce, menu/action, or physical-device work; this plan is limited to the two current browser blockers and final evidence reconciliation."
      category: scope-boundary
      verification: flagged-unverified
flagged_assumptions:
  - "[UNRESOLVED-SPEC-PROBE][SCOPE-01] No phase SPEC exists and the deterministic probe classified SCOPE-01 unresolved/unclassified; this plan preserves the verified opaque exact-scope partition contract and binds only the demonstrated online-activation replay gap."
  - "[UNRESOLVED-SPEC-PROBE][SCOPE-02] No phase SPEC exists and the deterministic probe classified SCOPE-02 unresolved/unclassified; existing fence/switch behavior remains authoritative and the new activation dispatch must use the same lease guards."
  - "[UNRESOLVED-SPEC-PROBE][SCOPE-03] No phase SPEC exists and the deterministic probe classified SCOPE-03 unresolved/unclassified; host backend admission remains authoritative and this plan tightens only browser acknowledgement completeness."
  - "[UNRESOLVED-SPEC-PROBE][SCOPE-04] No phase SPEC exists and the deterministic probe classified SCOPE-04 unresolved/unclassified; existing privacy projections remain authoritative and both regressions assert absence of sensitive fixture bytes from learner/diagnostic output."
  - "[UNRESOLVED-SPEC-PROBE][SCOPE-05] No phase SPEC exists and the deterministic probe classified SCOPE-05 unresolved/unclassified; Sigra remains an unchanged backend-authority adapter and gains no browser, credential, token, provider, device, or account responsibility."
---

<objective>
Close the two current Phase 160 browser replay blockers without widening the offline-island contract.

Purpose: Ensure newly authorized retained work starts safely and ensure incomplete success acknowledgements can never strand mutations behind a false success state.
Output: Lease-guarded online activation replay, exact full-batch success classification, two deterministic Playwright regressions, and fresh complete same-tree Phase 160 evidence.
</objective>

<artifacts_this_phase_produces>
- No new files, public symbols, storage schema, endpoint, dependency, external API integration, or ORM migration.
- Existing `activateScope(scopeRef)` behavior that conditionally dispatches the existing scoped worker after the active lifecycle write succeeds and only while online.
- Existing `classifyReplayResponse(data, records)` behavior that returns complete only for an exact ordered full-batch accepted acknowledgement and otherwise returns the closed blocked classification.
- Named Playwright cases for retained online activation replay and truncated-200 fail-closed retention in `examples/phoenix_host/e2e/offline_sync.spec.ts`.
- Validation task rows `160-13-01` and `160-13-02` plus a dated post-160-13 same-tree gate section in `160-VALIDATION.md`.
</artifacts_this_phase_produces>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/ADR-FIRST-B2C-ADOPTER.md
@.planning/FIRST-B2C-ADOPTER-ADOPTION-BRIEF.md
@.planning/FIRST-B2C-ADOPTER-ROUTE-POLICY-MAP.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/160-scoped-replay-and-auth-safety/160-CONTEXT.md
@.planning/phases/160-scoped-replay-and-auth-safety/160-RESEARCH.md
@.planning/phases/160-scoped-replay-and-auth-safety/160-PATTERNS.md
@.planning/phases/160-scoped-replay-and-auth-safety/160-VERIFICATION.md
@.planning/phases/160-scoped-replay-and-auth-safety/160-REVIEW.md
@.planning/phases/160-scoped-replay-and-auth-safety/160-12-SUMMARY.md
@.planning/phases/160-scoped-replay-and-auth-safety/160-VALIDATION.md

<interfaces>
`activateScope(scopeRef)` is the host-authorized browser entry point. `flushOutbox()` already captures the current `{scopeRef, epoch}`, enforces one active worker, and delegates to `flushScopedOutbox(invocation)`, whose storage, fetch-completion, deletion, count, and status effects are guarded by `leaseIsCurrent(lease)`. `classifyReplayResponse(data, records)` is the sole response-shape decision before accepted deletion. A non-halted success is therefore safe only when every submitted record has one accepted record at the same index; halted-prefix semantics remain the separate existing partial-batch path.
</interfaces>
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Replay retained online work on activation and reject incomplete success</name>
  <reversibility rating="reversible">Both changes are private browser-worker behavior behind existing activation, lease, response, and status contracts; undo is local to two host proof files.</reversibility>
  <read_first>
    - AGENTS.md
    - .planning/ADR-FIRST-B2C-ADOPTER.md
    - .planning/phases/160-scoped-replay-and-auth-safety/160-CONTEXT.md
    - .planning/phases/160-scoped-replay-and-auth-safety/160-VERIFICATION.md
    - .planning/phases/160-scoped-replay-and-auth-safety/160-REVIEW.md
    - .planning/phases/160-scoped-replay-and-auth-safety/160-12-SUMMARY.md
    - examples/phoenix_host/priv/static/offline_study.js
    - examples/phoenix_host/e2e/offline_sync.spec.ts
    - examples/phoenix_host/e2e/support/offline_route_proof.ts
  </read_first>
  <files>examples/phoenix_host/priv/static/offline_study.js, examples/phoenix_host/e2e/offline_sync.spec.ts</files>
  <behavior>
    - "RED activation case: queue one synthetic mutation under scope A, reload while transport is online so retained work is inert, install a request counter before host activation, activate scope A, and observe exactly one `/study/sync` request, the exact scope-A outbox becoming empty, and no scope/payload fixture in page or console output."
    - "An activation-triggered invocation uses the same current scope-and-epoch lease and one-worker guard as online-event and post-review replay; fencing or switching during an await cannot delete or render for the old lease."
    - "RED acknowledgement case: submit two scope-A records and return HTTP 200 with a matching accepted prefix, empty rejected list, and no halt; classification is blocked, neither submitted record is deleted, the status is paused, and no second request is scheduled."
    - "A non-halted response is complete only when accepted_records length equals submitted records length, rejected is empty, and every accepted client_mutation_id equals the submitted ID at the same index; existing valid complete and valid halted-prefix cases remain green."
  </behavior>
  <action>First add the two named Playwright regressions and run them red against the current tree. For the activation tracer, retain a real scope-partitioned IndexedDB mutation across reload, restore online transport, attach the deterministic `/study/sync` observer before calling the host-authorized `activateScope`, and assert a single request and exact-scope emptying after a full ordered acknowledgement. Then, per D-03, D-04, D-05, and D-07, extend successful activation after `writeLifecycle` with an online-only dispatch through the existing replay entry point; do not introduce a second worker path, bypass `activeFlush`, capture authority before the lifecycle write settles, or make activation await network completion. Route unexpected worker rejection through the same current-lease paused handling used by `replayOnOnline` so the returned activation promise represents lifecycle activation rather than server replay. For the acknowledgement tracer, intercept one two-record request and return a matching one-record prefix with `halted` absent/null; assert the entire submitted batch remains and the learner sees the existing paused copy with one request total. Then, per D-11 and D-12, tighten `classifyReplayResponse` so the non-halted complete branch requires exact full cardinality, an empty rejected list, and index-ordered identity equality before it can return accepted IDs; every mismatch returns blocked before deletion. Preserve the existing allowed halted-prefix behavior, closed halt vocabulary, stale-lease guards, accepted-only deletion, accessible status surface, and non-echoing copy. Use opaque synthetic fixtures only, keep all assertions observation-only, and do not add retries, timers, background listeners, all-scope reads, server changes, or diagnostic metadata.</action>
  <acceptance_criteria>
    - Successful host activation while online triggers exactly one request for retained work in the activated exact scope and empties that partition after an exact full acknowledgement.
    - The activation path delegates to the existing single-worker lease-guarded replay entry point; existing switch-before-send, switch-in-flight, post-response fence, and inactive-online cases remain green.
    - A truncated 200 acknowledgement with no halt retains the complete submitted batch, renders `Sync is paused`, and produces exactly one request with no automatic hot retry.
    - Reordered, duplicate, unknown, overlong, rejected-plus-complete, and truncated non-halted acknowledgements share the closed blocked path; a full ordered acknowledgement and a valid halted accepted prefix preserve their existing semantics.
    - Page text, console capture, assertion messages, and durable test output do not expose either opaque scope fixture or replay payload values.
    - The complete offline-island Playwright suite passes and provides deterministic coverage for SCOPE-01 through SCOPE-05 without changing host authority or support claims.
  </acceptance_criteria>
  <verify><automated>cd examples/phoenix_host &amp;&amp; npm run proof:offline-island -- --grep "online activation replays retained exact-scope work|truncated successful acknowledgement fails closed" &amp;&amp; npm run proof:offline-island</automated></verify>
  <done>Authorized online activation drains retained exact-scope work through the existing lease guard, while every incomplete non-halted success retains the whole submitted batch in a visible paused state.</done>
</task>

<task type="auto">
  <name>Task 2: Reconcile one fresh complete Phase 160 same-tree gate</name>
  <reversibility rating="reversible">The validation ledger records reproducible evidence and can be superseded by a later fresh gate without changing runtime contracts.</reversibility>
  <read_first>
    - AGENTS.md
    - .planning/REQUIREMENTS.md
    - .planning/ROADMAP.md
    - .planning/STATE.md
    - .planning/phases/160-scoped-replay-and-auth-safety/160-VERIFICATION.md
    - .planning/phases/160-scoped-replay-and-auth-safety/160-SECURITY.md
    - .planning/phases/160-scoped-replay-and-auth-safety/160-12-PLAN.md
    - .planning/phases/160-scoped-replay-and-auth-safety/160-12-SUMMARY.md
    - .planning/phases/160-scoped-replay-and-auth-safety/160-VALIDATION.md
    - examples/phoenix_host/priv/static/offline_study.js
    - examples/phoenix_host/e2e/offline_sync.spec.ts
  </read_first>
  <files>.planning/phases/160-scoped-replay-and-auth-safety/160-VALIDATION.md</files>
  <behavior>
    - "The final-tree browser corpus includes and passes both new regressions plus every pre-existing scope, fence, halt, malformed-response, quarantine, recovery, and idempotency case."
    - "The complete established Phase 160 chain passes on one tree after the runtime and test changes, including core, Sigra, Phoenix, browser, generated-host, asserted non-passing iOS prerequisite, planning/adoption, and scoped formatting checks."
    - "Validation records only commands, aggregate counts, stable requirement/threat IDs, closed outcomes, and explicit non-passing boundaries; it retains no scope, payload, account, auth, token, device, endpoint, flag, or mutation fixture."
    - "The prior post-160-12 ledger remains historical and is explicitly superseded; 160-VERIFICATION.md and 160-SECURITY.md are not edited or self-approved."
  </behavior>
  <action>After Task 1 passes its focused and full browser commands, run the exact established Phase 160 current-tree chain on the final tree. Only after the entire chain succeeds, add task rows `160-13-01` and `160-13-02` and a dated post-160-13 gate section to `160-VALIDATION.md`. Record observed commands and safe aggregate results, bind the activation regression to SCOPE-01/SCOPE-02/SCOPE-03 and activation/race/denial threats, bind exact acknowledgement handling to SCOPE-03 plus tampering/denial threats, and record SCOPE-04/SCOPE-05 as preserved by the complete retained gate rather than claiming new implementation. Preserve the executed decision set explicitly: D-01: required opaque scope; D-02: one scope-indexed outbox; D-03: inert launch until host activation; D-04: fence-first epoch changes; D-05: mismatches retain work and block; D-06: host-owned account, retention, encryption, cleanup, and recovery policy; D-07: bounded ordered exact-scope drain; D-08: ordered current backend admission; D-09: one-event host transaction and idempotency; D-10: no new application after observed authority change; D-11: closed accepted/rejected/conflict/blocked outcomes; D-12: typed HTTP batch semantics; D-13: host-owned route gate and visible paused state; D-14: the existing proof system; D-15: separate sensitive wire and safe observation planes; D-16: closed surface-specific observation vocabulary; D-17: no sensitive/correlating observable values; D-18: narrow per-surface ceilings; D-19: allowlist-first output and final-byte scan; D-20: Sigra as a closed backend-authority adapter; D-21: calm non-echoing learner copy and stable operator rules; D-22: positive and negative egress proof; D-23: the existing accessible status model without a new visual system. Mark the post-160-12 gate superseded while retaining it as history. Keep the spec-less probe classification visible: SCOPE-01 through SCOPE-05 remain unresolved/unclassified by SPEC fallback and are accepted here only through explicit plan truths plus executable code evidence. Preserve TODO-002 and adopter-instance completeness as `unknown_blocking`, generated iOS/device proof as asserted non-passing, and independent Phase 160 security as blocked; do not edit `160-VERIFICATION.md`, `160-SECURITY.md`, requirements, roadmap, state, or configuration. The ledger must remain codename-only and contain no replay wire values, scope fixtures, sensitive payloads, authority facts, or identifying adopter information.</action>
  <acceptance_criteria>
    - Focused new Playwright cases and the full offline-island suite pass on the repaired final tree.
    - The complete established Phase 160 chain passes after both browser fixes without excluding or weakening any previously passing suite.
    - `160-VALIDATION.md` contains `160-13-01`, `160-13-02`, the exact observed post-160-13 command/result, all five SCOPE IDs, and the activation-race, acknowledgement-tampering, cross-scope, disclosure, and denial/hot-retry threat mappings.
    - Evidence reconciliation contains safe aggregates and closed identifiers only and explicitly supersedes, rather than deletes, the post-160-12 gate.
    - TODO-002, adopter-instance completeness, generated iOS/device proof, spec-less probe status, and independent security remain explicitly non-passing or unresolved as applicable.
    - No implementation or evidence artifact outside the three declared `files_modified` paths changes; the required execution summary is the only additional plan artifact, and the user's pre-existing `.planning/config.json` modification remains untouched.
  </acceptance_criteria>
  <verify><automated>mix test test/crosswake/offline test/crosswake/telemetry_test.exs test/crosswake/proof/phase160_scoped_replay_privacy_test.exs test/crosswake/proof_lane/evidence_test.exs test/crosswake/doctor/doctor_test.exs test/crosswake/operator_inspection/json_formatter_test.exs &amp;&amp; (cd packages/crosswake_sigra &amp;&amp; mix deps.get &amp;&amp; mix test test/crosswake/companions/sigra/contracts_test.exs) &amp;&amp; (cd examples/phoenix_host &amp;&amp; MIX_ENV=test mix test test/crosswake_example/local_first &amp;&amp; npm run proof:offline-island) &amp;&amp; bash script/verify_phoenix_host_proof_lane.sh &amp;&amp; (ios_output=$(bash script/verify_generated_ios_shell.sh --proof-lane 2&gt;&amp;1); ios_status=$?; test "$ios_status" -eq 2 -o "$ios_status" -eq 3; printf '%s\n' "$ios_output" | grep -Eq '"outcome":"(blocked|unavailable)"') &amp;&amp; mix test test/crosswake/planning/first_adopter_context_test.exs test/crosswake/adoption/route_inventory_test.exs &amp;&amp; mix format --check-formatted examples/phoenix_host/lib/crosswake_example/local_first/replay_admission.ex examples/phoenix_host/lib/crosswake_example/local_first/study.ex examples/phoenix_host/lib/crosswake_example/local_first/review_event.ex examples/phoenix_host/test/crosswake_example/local_first/replay_admission_test.exs examples/phoenix_host/test/crosswake_example/local_first/study_test.exs examples/phoenix_host/test/crosswake_example/local_first/sync_controller_test.exs &amp;&amp; git diff --check -- examples/phoenix_host/priv/static/offline_study.js examples/phoenix_host/e2e/offline_sync.spec.ts .planning/phases/160-scoped-replay-and-auth-safety/160-VALIDATION.md</automated></verify>
  <done>The two repaired browser contracts and every previously passing Phase 160 contract have fresh same-tree automated evidence without promoting any external, device, security, or broader sync claim.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| host activation -> retained browser partition | A successful host-authorized activation may start only the exact scope-and-epoch worker created by that activation. |
| async worker -> lifecycle/storage/UI | Every awaited completion must still present the same lease before deletion, counting, or learner-visible status mutation. |
| Phoenix 200 body -> browser deletion authority | An untrusted or corrupted acknowledgement cannot authorize deletion unless it accounts exactly and in order for the complete submitted non-halted batch. |
| sensitive replay plane -> proof/status output | Scope and mutation data are necessary for transport but prohibited from learner copy, console capture, diagnostics, and retained evidence. |

## ASVS L1 Control Mapping

| ASVS category | Required control | Automated evidence |
|---|---|---|
| V4 Access Control | Activation and every worker side effect retain exact current scope-and-epoch authority. | Online-activation tracer plus retained switch/fence/cross-scope Playwright corpus. |
| V5 Input Validation | A non-halted 200 body must exactly match the submitted batch before deletion authority is granted. | Truncated-success regression plus full/malformed/halted response corpus. |
| V7 Error Handling and Logging | Malformed acknowledgement and unexpected worker rejection produce a stable paused state without hot retry or echoed values. | Request-count, paused-status, console, and page-error assertions. |
| V8 Data Protection | Sensitive scope and mutation fixtures remain transport-only and absent from durable evidence. | Browser non-disclosure assertions plus the complete privacy/evidence gate. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-160-09 | Tampering / Elevation of Privilege | `activateScope` to `flushOutbox` lease handoff | high | mitigate | Task 1 delegates activation replay to the existing single-worker entry point and reruns switch-before-send, in-flight switch, and post-response fence regressions. |
| T-160-10 | Tampering | `classifyReplayResponse` acknowledgement parser | high | mitigate | Task 1 requires full cardinality, empty rejected results, and ordered identity equality before complete classification or deletion. |
| T-160-11 | Spoofing / Elevation of Privilege | scoped IndexedDB worker | high | mitigate | Task 1 proves only the activated exact scope drains and all old-lease effects remain inert; no all-scope access is introduced. |
| T-160-12 | Information Disclosure | browser status, console, Playwright, validation evidence | medium | mitigate | Tasks 1-2 assert non-echoing output and retain only closed IDs, commands, and aggregate results in evidence. |
| T-160-13 | Denial of Service | activation and blocked-response replay triggers | medium | mitigate | Task 1 uses the existing one-flight guard, makes malformed success terminally paused, schedules no retry, and proves one request only. |
| T-160-SC | Tampering | dependency supply chain | low | accept | No package is added, upgraded, or installed by this plan; existing lockfiles and established commands are reused. |
</threat_model>

<source_coverage_audit>
| Source | ID | Gap-relevant contract | Plan coverage | Status |
|---|---|---|---|---|
| GOAL | Phase 160 | Account-scoped replay, backend authority continuity, payload privacy, and visible fail-closed disablement work together end to end. | 160-13 Tasks 1-2 close the two current browser blockers; executed Plans 160-01 through 160-12 remain authoritative for all other clauses. | COVERED |
| REQ | SCOPE-01 | Opaque scope on envelopes and scope-partitioned outbox. | Task 1 activates and drains only the retained exact-scope partition; Task 2 reruns the full retained contract. | COVERED |
| REQ | SCOPE-02 | Logout/switch stops replay and cross-scope work fails closed. | Task 1 reuses the lease guard and retains all switch/fence regressions; Task 2 reruns them on the final tree. | COVERED |
| REQ | SCOPE-03 | Backend authority and feature state are rechecked before queued mutation application. | Existing host admission remains unchanged; Task 1 starts authorized retained work and denies incomplete deletion authority; Task 2 reruns host authority proof. | COVERED |
| REQ | SCOPE-04 | Sensitive payloads remain absent from operational and retained outputs. | Tasks 1-2 require non-disclosing regressions and the complete privacy/evidence gate. | COVERED |
| REQ | SCOPE-05 | Sigra remains a backend-authority adapter without token/identity authority. | No Sigra change; Task 2 reruns the typed companion and host contract on the final tree. | COVERED |
| RESEARCH | scoped activation/lifecycle lease | Retained partitions stay inert until exact host activation; all worker effects require the current lease. | Task 1 online activation tracer and retained fence corpus. | COVERED |
| RESEARCH | ordered serial replay and fail-closed response handling | Accepted deletion follows a closed ordered acknowledgement; malformed success retains work visibly. | Task 1 full-cardinality parser gate and truncated-success regression. | COVERED |
| RESEARCH | proof and privacy boundaries | Extend the existing Playwright/evidence system and retain no sensitive replay/auth facts. | Tasks 1-2. | COVERED |
| CONTEXT | D-01, D-02 | Required opaque scope and one scope-indexed outbox with no production all-scope drain. | Existing implementation preserved; Task 1 proves exact-scope activation replay. | COVERED |
| CONTEXT | D-03, D-04, D-05 | Inert launch, exact activation, monotonic lease, fence-first switch, stale completion safety. | Task 1 activation-to-worker tracer plus full fence/switch corpus. | COVERED |
| CONTEXT | D-06 | Host owns account mapping, retention, encryption, cleanup, and recovery authorization. | No ownership change; plan prohibits new account or cleanup authority. | COVERED |
| CONTEXT | D-07 | One bounded exact-scope batch drains serially in journal order. | Task 1 uses existing worker and exact ordered acknowledgement. | COVERED |
| CONTEXT | D-08, D-09, D-10 | Phoenix current authority and atomic host application remain authoritative. | No server change; Task 2 reruns the complete host chain from Plan 160-12. | COVERED |
| CONTEXT | D-11, D-12, D-13 | Closed outcomes, typed batch semantics, retained blocked work, and visible paused disablement. | Task 1 exact complete/halted distinction and blocked retention. | COVERED |
| CONTEXT | D-14 | Extend existing proof seams with activation, switching, halted, malformed, and privacy cases. | Task 1 adds the two missing Playwright cases and reruns the whole corpus. | COVERED |
| CONTEXT | D-15 through D-22 | Sensitive wire data remains separate from closed observations and every egress is non-echoing. | Tasks 1-2 add no observation fields and rerun privacy/evidence gates. | COVERED |
| CONTEXT | D-23 | Existing accessible host-owned paused status remains the learner surface. | Task 1 reuses `renderPausedStatus` and existing semantic status element. | COVERED |
| CONTEXT | Deferred/excluded | Generic/background sync, Android expansion, broad storage, provider identity, dashboards, commerce, menu breadth, and adopter reidentification. | Explicitly prohibited and absent from tasks. | EXCLUDED |
</source_coverage_audit>

<verification>
Run the two new Playwright cases first, then the complete offline-island corpus. After the repaired browser tree is green, run the exact complete Phase 160 same-tree chain and reconcile `160-VALIDATION.md` only from observed results. All verification is automated; no conversational UAT or human checkpoint is permitted.
</verification>

<success_criteria>
- Successful online activation starts one lease-guarded replay of retained exact-scope work.
- Complete success requires exact full-batch ordered acknowledgement; incomplete or mismatched 200 responses retain the whole batch and visibly pause.
- Cross-scope, stale-lease, privacy, host authority, Sigra, halted replay, and no-hot-retry contracts remain green.
- Fresh same-tree evidence covers all five SCOPE requirement IDs while preserving spec-less probe flags and every external/non-passing boundary.
- Only the browser runtime, its Playwright spec, and the Phase 160 validation ledger change.
</success_criteria>

<output>
Create `.planning/phases/160-scoped-replay-and-auth-safety/160-13-SUMMARY.md` when done.
</output>
