---
phase: 160-scoped-replay-and-auth-safety
plan: "06"
type: execute
wave: 4
depends_on: ["160-03"]
files_modified:
  - packages/crosswake_sigra/lib/crosswake/companions/sigra.ex
  - packages/crosswake_sigra/test/crosswake/companions/sigra/contracts_test.exs
  - examples/phoenix_host/lib/crosswake_example/local_first/replay_admission.ex
  - examples/phoenix_host/test/crosswake_example/local_first/replay_admission_test.exs
autonomous: true
gap_closure: true
requirements: [SCOPE-03, SCOPE-05]
must_haves:
  truths:
    - "Sigra replay admission accepts only a real `RouteEntry` plus validated backend `AuthContext`; nil, maps, missing evidence, incompatible values, exceptions, exits, and throws return only `{:deny, :sigra_denied}` (D-08, D-20, SCOPE-03, SCOPE-05)."
    - "The example host resolves and passes its current typed route/session evidence to Sigra immediately before every event, while the browser and Crosswake core receive no credential, token, account mapping, provider, device, or exact denial detail."
    - "The production default path has an executable denial case and cannot be made permissive by omitting callback overrides."
  artifacts:
    - path: "packages/crosswake_sigra/lib/crosswake/companions/sigra.ex"
      provides: "Type-guarded fail-closed replay decision projection."
    - path: "examples/phoenix_host/lib/crosswake_example/local_first/replay_admission.ex"
      provides: "Host-owned typed route/AuthContext construction and default replay admission call."
    - path: "examples/phoenix_host/test/crosswake_example/local_first/replay_admission_test.exs"
      provides: "Default-path typed allow/deny, missing evidence, and denial-detail privacy integration proof."
  key_links:
    - from: "ReplayAdmission resolved host route/session"
      to: "Crosswake.Companions.Sigra.replay_decision/3"
      via: "typed RouteEntry and validated AuthContext passed per event"
      pattern: "RouteEntry|AuthContext|replay_decision"
    - from: "Sigra evaluator result"
      to: "ReplayAdmission closed reason"
      via: "allow or sigra_denied projection only"
      pattern: "sigra_denied"
  prohibitions:
    - statement: "MUST NOT call Sigra replay admission with nil or untyped authority inputs, or treat absent/incompatible evidence as allow."
      status: unresolved
      verification: flagged-unverified
    - statement: "MUST NOT move credentials, tokens, account mapping, provider/device identity, exact session records, or Sigra findings into core, browser, errors, logs, or proof artifacts."
      status: unresolved
      verification: flagged-unverified
flagged_assumptions:
  - "[UNRESOLVED-SPEC-PROBE] SCOPE-03 and SCOPE-05 are classified as automated host/companion boundary edges; synthetic typed backend evidence proves the contract without inferring real adopter auth data."
  - "[PRESERVE] Sigra remains an optional backend authority adapter; no credential, refresh, provider, MFA ceremony, device identity, or account lookup authority is added."
---

<objective>
Replace the permissive nil-backed Sigra default with current typed backend route/session evidence and closed denial.

Purpose: Close T-160-03 at the production host/companion boundary without expanding Crosswake authority.
Output: Type-guarded `replay_decision/3`, typed host adapters, and default-path integration proof.
</objective>

<artifacts_this_phase_produces>
- Guarded `Crosswake.Companions.Sigra.replay_decision/3` clauses for `RouteEntry` and `AuthContext`.
- Private host `default_sigra_route/1` and `default_sigra_auth_context/1` construction helpers (or equivalently named private typed builders).
- Default-path Sigra denial integration cases in `replay_admission_test.exs`.
</artifacts_this_phase_produces>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/ADR-FIRST-B2C-ADOPTER.md
@.planning/phases/160-scoped-replay-and-auth-safety/160-CONTEXT.md
@.planning/phases/160-scoped-replay-and-auth-safety/160-PATTERNS.md
@.planning/phases/160-scoped-replay-and-auth-safety/160-VERIFICATION.md
@.planning/phases/160-scoped-replay-and-auth-safety/160-SECURITY.md
@packages/crosswake_sigra/lib/crosswake/companions/sigra.ex
@packages/crosswake_sigra/lib/crosswake/companions/sigra/contracts.ex
@packages/crosswake_sigra/lib/crosswake/companions/sigra/evaluator.ex
@lib/crosswake/manifest/types.ex
@examples/phoenix_host/lib/crosswake_example/local_first/replay_admission.ex
@examples/phoenix_host/test/crosswake_example/local_first/replay_admission_test.exs
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Make Sigra replay projection deny every non-typed authority input</name>
  <reversibility rating="reversible">The change narrows one companion helper behind its existing closed allow/deny return contract.</reversibility>
  <read_first>
    - AGENTS.md
    - .planning/phases/160-scoped-replay-and-auth-safety/160-CONTEXT.md
    - packages/crosswake_sigra/lib/crosswake/companions/sigra.ex
    - packages/crosswake_sigra/lib/crosswake/companions/sigra/contracts.ex
    - packages/crosswake_sigra/lib/crosswake/companions/sigra/evaluator.ex
    - lib/crosswake/manifest/types.ex
    - packages/crosswake_sigra/test/crosswake/companions/sigra/contracts_test.exs
  </read_first>
  <files>packages/crosswake_sigra/lib/crosswake/companions/sigra.ex, packages/crosswake_sigra/test/crosswake/companions/sigra/contracts_test.exs</files>
  <behavior>
    - "RED: `replay_decision(nil, nil, [])`, maps, malformed structs, and missing route/auth context all return `{:deny, :sigra_denied}`."
    - "A valid `RouteEntry` and validated `AuthContext` pass to `Evaluator.evaluate_route_auth/3`; allow returns `:allow` and every other result returns the same closed denial."
    - "Evaluator exceptions, exits, throws, invalid returns, or incompatible contract versions cannot escape or reveal finding/session details."
    - "The facade returns only `:allow` or `{:deny, :sigra_denied}` and does not log or serialize either typed input."
  </behavior>
  <action>Add a primary `replay_decision/3` clause that pattern-matches `%Crosswake.Manifest.Types.RouteEntry{}` and `%Crosswake.Companions.Sigra.Contracts.AuthContext{}`, validates the auth context with `Contracts.validate_auth_context/1`, and only then calls the existing evaluator. Add a catch-all clause returning `{:deny, :sigra_denied}` so the evaluator's nil-route allow behavior is unreachable through replay admission. Preserve rescue/catch collapse and the exact closed return type. Extend companion contract tests with nil, plain-map, malformed typed context, valid allow, predicated-route denial, exception/exit/throw, and captured-byte assertions that forbid authority facts per D-20.</action>
  <acceptance_criteria>
    - Nil and every untyped/malformed input deny through the replay facade without invoking an allow branch.
    - Valid typed inputs preserve evaluator behavior while projecting no facts or findings beyond the closed decision.
    - Exceptions and invalid evaluator outcomes collapse to `sigra_denied` with no input/detail bytes.
    - Existing Sigra contract suites remain green without new dependencies or authority surfaces.
  </acceptance_criteria>
  <verify><automated>cd packages/crosswake_sigra &amp;&amp; mix test test/crosswake/companions/sigra/contracts_test.exs</automated></verify>
  <done>The replay facade cannot allow nil, untyped, malformed, or exceptional backend evidence.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Feed current typed host authority into the production default path</name>
  <reversibility rating="reversible">The example host keeps route/session builders private and preserves the existing ReplayAdmission callback contract.</reversibility>
  <read_first>
    - .planning/phases/160-scoped-replay-and-auth-safety/160-CONTEXT.md
    - .planning/phases/160-scoped-replay-and-auth-safety/160-VERIFICATION.md
    - .planning/phases/160-scoped-replay-and-auth-safety/160-PATTERNS.md
    - packages/crosswake_sigra/lib/crosswake/companions/sigra.ex
    - packages/crosswake_sigra/lib/crosswake/companions/sigra/contracts.ex
    - lib/crosswake/manifest/types.ex
    - examples/phoenix_host/lib/crosswake_example/local_first/replay_admission.ex
    - examples/phoenix_host/test/crosswake_example/local_first/replay_admission_test.exs
  </read_first>
  <files>examples/phoenix_host/lib/crosswake_example/local_first/replay_admission.ex, examples/phoenix_host/test/crosswake_example/local_first/replay_admission_test.exs</files>
  <behavior>
    - "The default host session resolver supplies scope plus a validated backend `AuthContext`; the default route resolver supplies a real `RouteEntry` with current route/flag/auth declarations."
    - "`sigra_allows?/3` calls `replay_decision(route, auth_context, opts)` immediately before domain authorization for each event."
    - "Missing or malformed host evidence, a current predicated-route denial, or unavailable companion returns `:sigra_denied` before `Study.apply_one/3`."
    - "Callback-injected test seams still run in D-08 order but cannot make the no-callback production default silently skip typed authority."
  </behavior>
  <action>Change the example host's default resolution values from loose maps into a real `%RouteEntry{id: "offline-study", path: "/study", runtime: :offline_island, offline: :local_first, gated_by: :offline_study_replay, ...}` and a host session record containing the existing synthetic `scope_ref` plus a `%AuthContext{}` built and validated through `Contracts.new_auth_context/1`. Keep all synthetic fixture identity values server-configured and inside the host authority path. Update `sigra_allows?/3` so the default branch extracts the typed auth context and calls `Crosswake.Companions.Sigra.replay_decision(route, auth_context, [])`; absence or construction failure is `sigra_denied`, never a permissive substitute. Add an integration test that uses no callback overrides and a server-configured predicated route/context denial, asserts the domain callback/effect is not reached, and captures only the closed reason. Preserve session -> scope -> route -> feature -> Sigra -> domain ordering, host ownership, and non-echoing error semantics per D-08 and D-20.</action>
  <acceptance_criteria>
    - The no-callback production path passes non-nil typed route and validated auth context to Sigra for every event.
    - Missing/malformed/default-path denied evidence halts before the domain callback and database effect.
    - Tests prove both default allow and default deny without relying on injected Sigra callbacks.
    - Browser/core output receives no auth context, identity field, finding, credential, or token detail.
  </acceptance_criteria>
  <verify><automated>cd examples/phoenix_host &amp;&amp; MIX_ENV=test mix test test/crosswake_example/local_first/replay_admission_test.exs test/crosswake_example/local_first/sync_controller_test.exs</automated></verify>
  <done>Every default replay event reaches Sigra with current typed backend evidence, and unavailable or denied evidence stops closed before mutation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| host session/route resolution -> Sigra | Only current typed backend evidence may reach the companion evaluator. |
| Sigra finding/result -> replay admission | Exact authority facts collapse to allow or one safe denial class. |

## ASVS L1 Control Mapping

| ASVS category | Required control | Automated evidence |
|---|---|---|
| V2 Authentication | Validate current backend AuthContext before every event. | Companion typed-input and host default-path tests. |
| V3 Session Management | Use the current host session evidence, not client/cached authority. | Per-event default resolver tests. |
| V4 Access Control | Preserve route/feature/Sigra/domain order and stop before effect on denial. | Host integration order/denial cases. |
| V7 Error Handling | Collapse findings/exceptions to `sigra_denied` without detail. | Companion captured-byte cases. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-160-03 | Elevation | Host/Sigra authorization chain | high | mitigate | Tasks 1-2 require typed current route/AuthContext and deny every missing, malformed, or exceptional result. |
| T-160-04 | Information Disclosure | Sigra denial projection | high | mitigate | Both tasks retain only allow or `sigra_denied` outside the authority path. |
| T-160-SC | Tampering | Package installs | low | accept | No package-manager installation occurs; existing Sigra/Phoenix dependencies are reused. |
</threat_model>

<verification>
Run the focused companion and Phoenix host commands; both default-path allow/deny and all existing callback order cases must pass.
</verification>

<success_criteria>
Sigra evaluates real current backend evidence for every replay event and never becomes a credential, account, token, or client authority.
</success_criteria>

<output>
Create `.planning/phases/160-scoped-replay-and-auth-safety/160-06-SUMMARY.md` when done.
</output>
