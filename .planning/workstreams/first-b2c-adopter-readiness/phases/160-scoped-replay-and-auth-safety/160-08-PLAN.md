---
phase: 160-scoped-replay-and-auth-safety
plan: "08"
type: execute
wave: 6
depends_on: ["160-05", "160-06", "160-07"]
files_modified:
  - lib/crosswake/offline/safe_observation.ex
  - lib/crosswake/offline/telemetry.ex
  - lib/crosswake/telemetry.ex
  - lib/crosswake/doctor/doctor.ex
  - test/crosswake/offline/safe_observation_test.exs
  - test/crosswake/offline/telemetry_test.exs
  - test/crosswake/telemetry_test.exs
  - test/crosswake/doctor/doctor_test.exs
  - test/crosswake/proof/phase160_scoped_replay_privacy_test.exs
  - .planning/phases/160-scoped-replay-and-auth-safety/160-VALIDATION.md
autonomous: true
gap_closure: true
requirements: [SCOPE-01, SCOPE-02, SCOPE-03, SCOPE-04, SCOPE-05]
must_haves:
  truths:
    - "Every public SafeObservation projection revalidates the entire struct before returning bytes; direct or forged struct construction cannot reach telemetry, Logger, doctor, inspection, aggregates, or retained proof (D-15 through D-19, D-22, SCOPE-04)."
    - "Production egress callers act only on `{:ok, projection}` and return a stable closed error without callback invocation, telemetry execution, logging, or doctor serialization when validation fails."
    - "One fresh same-tree gate exercises all five requirements and all six security gaps after Plans 04-07; blocked native/device prerequisites remain non-passing and no security artifact is self-approved."
  artifacts:
    - path: "lib/crosswake/offline/safe_observation.ex"
      provides: "Public boundary revalidation and typed fail-closed projections."
    - path: "lib/crosswake/telemetry.ex"
      provides: "Root telemetry emission that cannot execute for forged observations."
    - path: "test/crosswake/proof/phase160_scoped_replay_privacy_test.exs"
      provides: "Forged-struct every-egress canary proof plus full gap regression binding."
    - path: ".planning/phases/160-scoped-replay-and-auth-safety/160-VALIDATION.md"
      provides: "Observed post-remediation commands and requirement/threat evidence ledger."
  key_links:
    - from: "SafeObservation public struct"
      to: "to_telemetry/1, to_logger/1, and to_doctor/1"
      via: "full field revalidation before projection"
      pattern: "validate|to_telemetry|to_logger|to_doctor"
    - from: "typed projection result"
      to: "Offline.Telemetry, Crosswake.Telemetry, Logger, and Doctor"
      via: "success-only egress with closed invalid-observation return"
      pattern: "SafeObservation|error"
    - from: "Plans 04-08 regression results"
      to: "160-VALIDATION.md"
      via: "one fresh same-tree final gate"
      pattern: "T-160-0[1-6]|SCOPE-0[1-5]"
  prohibitions:
    - statement: "MUST NOT trust `%SafeObservation{}` shape as proof of constructor validation or emit any projection before revalidation succeeds."
      status: unresolved
      verification: flagged-unverified
    - statement: "MUST NOT place raw answers, media, transcripts, credentials, account identifiers, tokens, stable device identifiers, scope references, exact auth facts, arbitrary metadata, or printable canaries into any operational or retained output."
      status: unresolved
      verification: flagged-unverified
    - statement: "MUST NOT mark 160-SECURITY.md verified from plan execution; `$gsd-secure-phase 160` must independently re-audit the implemented tree."
      status: unresolved
      verification: flagged-unverified
flagged_assumptions:
  - "[UNRESOLVED-SPEC-PROBE] SCOPE-01 through SCOPE-05 remain externally bounded by synthetic opaque fixtures; real adopter scope, route, flag, session, media, and device inputs are neither inferred nor retained."
  - "[PRESERVE] SafeObservation stays a small versioned allowlist rather than becoming a redactor or generic diagnostic envelope."
---

<objective>
Close the SafeObservation construction bypass and prove all six Phase 160 security remediations together.

Purpose: Make every production egress fail closed even when callers forge the public struct, then replace stale validation with fresh post-gap evidence.
Output: Revalidated typed projections, guarded egress consumers, forged-canary regressions, and a reconciled automated validation ledger.
</objective>

<artifacts_this_phase_produces>
- A public `SafeObservation.validate/1` boundary (or equivalent constructor round-trip) used by every projection.
- Success-only telemetry, Logger, and Doctor egress with one stable invalid-observation result.
- A forged-struct every-egress regression matrix and fresh Phase 160 validation ledger.
</artifacts_this_phase_produces>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/ADR-FIRST-B2C-ADOPTER.md
@.planning/phases/160-scoped-replay-and-auth-safety/160-CONTEXT.md
@.planning/phases/160-scoped-replay-and-auth-safety/160-PATTERNS.md
@.planning/phases/160-scoped-replay-and-auth-safety/160-REVIEW.md
@.planning/phases/160-scoped-replay-and-auth-safety/160-VERIFICATION.md
@.planning/phases/160-scoped-replay-and-auth-safety/160-SECURITY.md
@.planning/phases/160-scoped-replay-and-auth-safety/160-VALIDATION.md
@lib/crosswake/offline/safe_observation.ex
@lib/crosswake/offline/telemetry.ex
@lib/crosswake/telemetry.ex
@lib/crosswake/doctor/doctor.ex
@test/crosswake/proof/phase160_scoped_replay_privacy_test.exs
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Revalidate every SafeObservation projection at its public boundary</name>
  <reversibility rating="costly">The public projection return contract becomes explicitly typed and fail closed; later changes must preserve compatibility or add an adapter.</reversibility>
  <read_first>
    - AGENTS.md
    - .planning/phases/160-scoped-replay-and-auth-safety/160-CONTEXT.md
    - .planning/phases/160-scoped-replay-and-auth-safety/160-SECURITY.md
    - lib/crosswake/offline/safe_observation.ex
    - test/crosswake/offline/safe_observation_test.exs
    - test/crosswake/proof/phase160_scoped_replay_privacy_test.exs
  </read_first>
  <files>lib/crosswake/offline/safe_observation.ex, test/crosswake/offline/safe_observation_test.exs, test/crosswake/proof/phase160_scoped_replay_privacy_test.exs</files>
  <behavior>
    - "RED: a directly constructed `%SafeObservation{}` with an invalid route, enum, readiness, measurement, nested canary, or extra struct field value is rejected by each public projection."
    - "`validate/1` reconstructs the exact constructor input from declared fields and delegates to the same key/value/measurement/readiness rules as `new/1`."
    - "`to_telemetry/1`, `to_logger/1`, and `to_doctor/1` return `{:ok, exact_projection}` or `{:error, %Error{rule_id: ..., path: ...}}`; no invalid input value is echoed."
    - "Valid constructor-produced observations retain their exact surface-specific allowlists and bounded values."
  </behavior>
  <action>Add a public `validate/1` (or an equivalent private constructor round-trip used by all public projections) that copies only the declared struct fields into a plain map and applies the exact `new/1` validation rules. Change all three projection functions to validate first and return a typed success/error tuple. Do not inspect, stringify, hash, log, redact, or echo invalid field values. Add table-driven forged-struct cases spanning every field class and printable canaries; assert no projection map or forbidden byte is returned. Preserve the existing closed enum and exact-key vocabulary rather than adding a general metadata escape hatch.</action>
  <acceptance_criteria>
    - No public `%SafeObservation{}` can reach a projection solely by matching the struct shape.
    - Valid observations project only the existing telemetry/logger or doctor keys.
    - Invalid observations return stable rule/path data without their values or canary bytes.
    - Existing constructor validation remains the single semantic source of truth.
  </acceptance_criteria>
  <verify><automated>mix test test/crosswake/offline/safe_observation_test.exs test/crosswake/proof/phase160_scoped_replay_privacy_test.exs</automated></verify>
  <done>Every SafeObservation projection validates the complete value set before producing bytes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Guard every production telemetry, Logger, and Doctor egress</name>
  <reversibility rating="reversible">Consumers retain their small output schemas and can adapt locally to the typed projection result.</reversibility>
  <read_first>
    - .planning/phases/160-scoped-replay-and-auth-safety/160-SECURITY.md
    - lib/crosswake/offline/safe_observation.ex
    - lib/crosswake/offline/telemetry.ex
    - lib/crosswake/telemetry.ex
    - lib/crosswake/doctor/doctor.ex
    - test/crosswake/offline/telemetry_test.exs
    - test/crosswake/telemetry_test.exs
    - test/crosswake/doctor/doctor_test.exs
    - test/crosswake/proof/phase160_scoped_replay_privacy_test.exs
  </read_first>
  <files>lib/crosswake/offline/telemetry.ex, lib/crosswake/telemetry.ex, lib/crosswake/doctor/doctor.ex, test/crosswake/offline/telemetry_test.exs, test/crosswake/telemetry_test.exs, test/crosswake/doctor/doctor_test.exs, test/crosswake/proof/phase160_scoped_replay_privacy_test.exs</files>
  <behavior>
    - "Offline.Telemetry invokes its callback only after a valid telemetry projection; invalid observations return one stable closed error and invoke it zero times."
    - "Crosswake.Telemetry executes no telemetry event and the default Logger receives no entry for an invalid observation."
    - "Doctor returns no readiness map or serialized detail for an invalid observation."
    - "Captured callback, telemetry-handler, log-message, Logger-metadata, doctor, inspection, aggregate, and proof bytes exclude the forged canary and every D-17 class."
  </behavior>
  <action>Update each production consumer to pattern-match only the typed success projection from Task 1 and return the same stable closed invalid-observation result otherwise. Ensure root telemetry does not call `:telemetry.execute/3`, Offline.Telemetry does not invoke its callback, and Doctor does not return a readiness map on failure. Extend focused tests with counters/handlers/log capture and serialized-byte assertions proving zero egress for forged structs. Reuse the existing every-egress privacy matrix; do not create a second diagnostics, logging, or proof pipeline.</action>
  <acceptance_criteria>
    - Invalid observations cause zero callback, telemetry, Logger, and Doctor output.
    - Valid observations preserve current event names and exact allowlisted data.
    - The every-egress matrix fails if any production consumer bypasses validation.
    - No sensitive or forged value appears in error, log, test artifact, or serialized output bytes.
  </acceptance_criteria>
  <verify><automated>mix test test/crosswake/offline/safe_observation_test.exs test/crosswake/offline/telemetry_test.exs test/crosswake/telemetry_test.exs test/crosswake/doctor/doctor_test.exs test/crosswake/proof/phase160_scoped_replay_privacy_test.exs</automated></verify>
  <done>All named operational egresses act only on a freshly validated closed projection.</done>
</task>

<task type="auto">
  <name>Task 3: Run the complete post-gap Phase 160 gate and reconcile validation</name>
  <reversibility rating="reversible">The validation ledger records current-tree evidence and can be regenerated without changing runtime contracts.</reversibility>
  <read_first>
    - .planning/phases/160-scoped-replay-and-auth-safety/160-04-SUMMARY.md
    - .planning/phases/160-scoped-replay-and-auth-safety/160-05-SUMMARY.md
    - .planning/phases/160-scoped-replay-and-auth-safety/160-06-SUMMARY.md
    - .planning/phases/160-scoped-replay-and-auth-safety/160-07-SUMMARY.md
    - .planning/phases/160-scoped-replay-and-auth-safety/160-VALIDATION.md
    - .planning/phases/160-scoped-replay-and-auth-safety/160-SECURITY.md
    - test/crosswake/proof/phase160_scoped_replay_privacy_test.exs
    - script/verify_phoenix_host_proof_lane.sh
    - script/verify_generated_ios_shell.sh
  </read_first>
  <files>.planning/phases/160-scoped-replay-and-auth-safety/160-VALIDATION.md</files>
  <action>Run one fresh same-tree chain after Plans 04-08 that covers legacy quarantine/recovery, lifecycle races and partial halted responses, typed default Sigra evidence, legacy/scoped/concurrent idempotency, forged SafeObservation rejection, all existing scoped replay/privacy/evidence suites, host proof, asserted non-passing generated iOS prerequisite, planning/adoption truth, and scoped formatting. Update `160-VALIDATION.md` only from observed results: add Tasks 160-04 through 160-08 to the verification map, bind SCOPE-01 through SCOPE-05 and T-160-01 through T-160-06 to exact commands, and retain TODO-002 plus real adopter/device inputs as `unknown_blocking`. Do not edit `160-SECURITY.md`; its status belongs to a fresh independent `$gsd-secure-phase 160` audit after execution.</action>
  <acceptance_criteria>
    - One current-tree chain covers all five requirements and six high threat regressions.
    - Validation records observed commands/results rather than copying plan promises or stale summaries.
    - Blocked/unavailable generated iOS output remains explicitly non-passing and physical-iPhone proof remains external.
    - Security remains blocked until an independent secure-phase rerun closes the threat register.
  </acceptance_criteria>
  <verify><automated>mix test test/crosswake/offline test/crosswake/telemetry_test.exs test/crosswake/proof/phase160_scoped_replay_privacy_test.exs test/crosswake/proof_lane/evidence_test.exs test/crosswake/doctor/doctor_test.exs test/crosswake/operator_inspection/json_formatter_test.exs &amp;&amp; (cd packages/crosswake_sigra &amp;&amp; mix deps.get &amp;&amp; mix test test/crosswake/companions/sigra/contracts_test.exs) &amp;&amp; (cd examples/phoenix_host &amp;&amp; MIX_ENV=test mix test test/crosswake_example/local_first &amp;&amp; npm run proof:offline-island) &amp;&amp; bash script/verify_phoenix_host_proof_lane.sh &amp;&amp; (ios_output=$(bash script/verify_generated_ios_shell.sh --proof-lane 2&gt;&amp;1); ios_status=$?; test "$ios_status" -eq 2 -o "$ios_status" -eq 3; printf '%s\n' "$ios_output" | grep -Eq '"outcome":"(blocked|unavailable)"') &amp;&amp; mix test test/crosswake/planning/first_adopter_context_test.exs test/crosswake/adoption/route_inventory_test.exs &amp;&amp; mix format --check-formatted lib/crosswake/offline/safe_observation.ex lib/crosswake/offline/telemetry.ex lib/crosswake/telemetry.ex lib/crosswake/doctor/doctor.ex test/crosswake/offline/safe_observation_test.exs test/crosswake/offline/telemetry_test.exs test/crosswake/telemetry_test.exs test/crosswake/doctor/doctor_test.exs test/crosswake/proof/phase160_scoped_replay_privacy_test.exs packages/crosswake_sigra/lib/crosswake/companions/sigra.ex packages/crosswake_sigra/test/crosswake/companions/sigra/contracts_test.exs examples/phoenix_host/lib/crosswake_example/local_first/replay_admission.ex examples/phoenix_host/test/crosswake_example/local_first/replay_admission_test.exs examples/phoenix_host/lib/crosswake_example/local_first/study.ex examples/phoenix_host/test/crosswake_example/local_first/study_test.exs examples/phoenix_host/test/crosswake_example/local_first/sync_controller_test.exs examples/phoenix_host/priv/repo/migrations/20260802170000_restore_review_event_idempotency_guard.exs</automated></verify>
  <done>Fresh automated evidence covers every gap without sensitive output, manual UAT, false device promotion, or self-approved security status.</done>
</task>

</tasks>

## Multi-Source Coverage Audit

| SOURCE | ID | Feature/Requirement | Plan | Status | Notes |
|---|---|---|---|---|---|
| GOAL | — | Account-scoped outboxes, payload redaction, backend reauthorization, auth continuity, and server-side disablement | 04-08 | COVERED | Gap plans repair migration, lifecycle, authority, idempotency, and egress boundaries. |
| REQ | SCOPE-01 | Required opaque scoped envelopes and partitioned outbox | 04, 07, 08 | COVERED | Legacy data is quarantined, explicitly recovered into an exact active scope, and host effects stay globally idempotent. |
| REQ | SCOPE-02 | Logout/switch stop and cross-scope denial | 04-05, 08 | COVERED | Fence-first worker ownership and post-await lease checks make stale completions inert. |
| REQ | SCOPE-03 | Per-event session, route, and feature reauthorization | 06-08 | COVERED | Typed current backend evidence reaches Sigra before every effect. |
| REQ | SCOPE-04 | No raw answer egress | 08 | COVERED | Forged public observations are revalidated at every projection and production egress. |
| REQ | SCOPE-05 | Sigra backend-authority adapter only | 06, 08 | COVERED | Missing/untyped evidence denies; only a closed decision leaves the companion. |
| RESEARCH | — | Scoped storage, epoch fence, serial transaction, typed observation, reused proof | 04-08 | COVERED | Corrective plans preserve the chosen architecture and add no platform breadth. |
| CONTEXT | D-01..D-06 | Scope lifecycle and host ownership | 04-05 | COVERED | No legacy record is silently assigned; recovery requires an exact active host lease. |
| CONTEXT | D-07..D-13 | Serial replay, ordered admission, atomicity, outcomes, HTTP, disablement | 05-07 | COVERED | One worker, post-await fencing, typed admission, global tombstone, and halted UI. |
| CONTEXT | D-14 | Reuse proof seams and deterministic cases | 08 | COVERED | Same full gate and evidence seams; native/device prerequisites remain non-passing. |
| CONTEXT | D-15..D-19 | Separate safe plane and per-surface allowlists | 08 | COVERED | Constructor rules are enforced again at each public projection. |
| CONTEXT | D-20 | Sigra remains closed backend evidence adapter | 06 | COVERED | Typed host inputs stay backend-only and findings collapse to one safe denial. |
| CONTEXT | D-21..D-23 | Safe copy, canary proof, accessible learner status | 05, 08 | COVERED | Halted work is visibly paused/blocked and forged canaries reach no egress. |
| EDGE | SCOPE-01..05 | Five unresolved external edge assumptions | 04-08 | COVERED | Synthetic fixtures exercise boundaries without inferring adopter facts. |
| PROHIBITION | — | No scope/authority bypass, silent drop/fallback, sensitive egress, or false proof promotion | 04-08 | COVERED | Each gap plan retains explicit unresolved prohibitions for execution-time proof. |
| REVIEW | T-160-01..06 | Six demonstrated high-severity implementation gaps | 04-08 | COVERED | Each threat has an owning plan and focused regression; Plan 08 reruns all together. |
| VERIFICATION | — | Previously passing suites missed adversarial upgrade/race/default/forgery/migration/halt cases | 04-08 | COVERED | Each missed case receives positive and negative automated evidence. |
| SECURITY | T-160-01..06 | Independent security closure required | 04-08 | COVERED | Plans mitigate; only a later secure-phase rerun may mark threats closed. |

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| public SafeObservation struct -> projection | Struct shape is untrusted until the complete closed value set is revalidated. |
| projection -> operational egress | Only typed success projections may invoke callbacks, telemetry, Logger, or Doctor. |
| post-gap assertions -> validation/security status | Automated results may update validation; security status requires a separate audit. |

## ASVS L1 Control Mapping

| ASVS category | Required control | Automated evidence |
|---|---|---|
| V2 Authentication | Full gate proves current typed backend evidence before effects. | Sigra and host default-path cases. |
| V3 Session Management | Full gate proves stale scope epochs cannot mutate storage/UI. | Browser race and lifecycle cases. |
| V4 Access Control | Closed admission, cross-scope denial, and idempotent effects. | Host and migration suites. |
| V5 Input Validation | Revalidate every public observation field before projection. | Forged struct table. |
| V7 Error Handling | Invalid observations and partial halted batches remain closed and visible. | Zero-egress and halted UI assertions. |
| V8 Data Protection | No forged canary or D-17 class reaches final bytes. | Every-egress matrix and proof scan. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-160-01 | Spoofing / Elevation | legacy IndexedDB records | high | mitigate | Plan 04 quarantines and requires explicit exact-scope recovery; Task 3 reruns it. |
| T-160-02 | Tampering | async lifecycle effects | high | mitigate | Plan 05 owns worker cancellation and post-await leases; Task 3 reruns races. |
| T-160-03 | Elevation | Sigra default authority | high | mitigate | Plan 06 requires typed current inputs and closed denial; Task 3 reruns it. |
| T-160-04 | Information Disclosure | SafeObservation egress | high | mitigate | Tasks 1-2 revalidate and guard all named production egresses. |
| T-160-05 | Tampering / Repudiation | ReviewEvent idempotency | high | mitigate | Plan 07 restores the global guard and normalizes races; Task 3 reruns it. |
| T-160-06 | Elevation / Denial of Service | halted replay drain | high | mitigate | Plan 05 retains halted work and renders paused/blocked; Task 3 reruns it. |
| T-160-SC | Tampering | package installs | low | accept | No package-manager installation occurs; existing dependencies and proof tooling are reused. |
</threat_model>

<verification>
Tasks 1-2 provide focused fail-closed egress feedback. Task 3 alone owns the approximately 300-second post-gap same-tree gate and observed validation reconciliation.
</verification>

<success_criteria>
Forged observations reach no output, all six demonstrated security gaps have executable regressions, and release remains blocked until execution plus a fresh secure-phase audit close the threat register.
</success_criteria>

<output>
Create `.planning/phases/160-scoped-replay-and-auth-safety/160-08-SUMMARY.md` when done.
</output>
