---
phase: 160-scoped-replay-and-auth-safety
plan: "15"
type: execute
wave: 12
depends_on: ["160-14"]
files_modified:
  - examples/phoenix_host/lib/crosswake_example/router.ex
  - examples/phoenix_host/lib/crosswake_example/e2e/replay_authority.ex
  - examples/phoenix_host/lib/crosswake_example/e2e/replay_session_controller.ex
  - examples/phoenix_host/config/test.exs
  - examples/phoenix_host/priv/static/offline_study.js
  - examples/phoenix_host/e2e/offline_sync.spec.ts
autonomous: true
gap_closure: true
requirements: [SCOPE-01, SCOPE-02, SCOPE-03, SCOPE-04, SCOPE-05]
must_haves:
  truths:
    - "Decision fidelity: D-01: required opaque scope; D-02: one scope-indexed outbox; D-03: inert launch; D-04: fence-first epoch; D-05: mismatch blocks and retains; D-06: host-owned account/retention/encryption/recovery; D-07: ordered exact-scope drain; D-08: ordered current admission; D-09: atomic host event/idempotency; D-10: no new application after observed authority change; D-11: closed outcomes and retained unaccepted work; D-12: typed HTTP/batch semantics; D-13: host-owned gated_by; D-14: existing proof seams; D-15: separate wire/observation planes; D-16: closed per-surface vocabulary; D-17: no sensitive observable values; D-18: narrow egress ceilings; D-19: allowlist-first output/final-byte scan; D-20: Sigra closed backend adapter; D-21: non-echoing audience-safe errors; D-22: positive/negative privacy proof; D-23: existing accessible paused UI."
    - "The existing browser proof authenticates through a compile-time test-only host session seam before replay; production has no synthetic replay authority default (D-08, D-14, D-20; SCOPE-03, SCOPE-05)."
    - "An immediate-online review submission enters the existing `replayOnOnline/0` current-lease rejection guard rather than discarding `flushOutbox/0`'s promise (D-04, D-05, D-07; SCOPE-01, SCOPE-02)."
    - "A deterministic storage/worker rejection after the online local write renders the existing accessible paused state, retains the queued exact-scope mutation, emits no unhandled rejection/page error/console output, and schedules no hot retry (D-11, D-13, D-21, D-23; SCOPE-02, SCOPE-04)."
    - "The authenticated browser corpus still proves per-event request-bound host authority, full acknowledgement deletion, scope fencing, disablement retention, and privacy without retaining session, scope, payload, token, or account fixtures (D-14, D-17, D-22; SCOPE-01 through SCOPE-05)."
  artifacts:
    - path: "examples/phoenix_host/lib/crosswake_example/e2e/replay_authority.ex"
      provides: "Compile-time test/e2e-only host authority adapter backed by the signed test session."
    - path: "examples/phoenix_host/lib/crosswake_example/e2e/replay_session_controller.ex"
      provides: "Compile-time test/e2e-only authenticated-session fixture endpoint for browser proof."
    - path: "examples/phoenix_host/priv/static/offline_study.js"
      provides: "Immediate-online submit dispatch through the established current-lease worker guard."
    - path: "examples/phoenix_host/e2e/offline_sync.spec.ts"
      provides: "Authenticated replay setup and deterministic immediate-online failure containment regression."
  key_links:
    - from: "Playwright browser context"
      to: "host replay session"
      via: "reserved `/_e2e` compile-time-gated controller followed by the production replay route"
      pattern: "_e2e.*replay-session|study/sync|learnloop/sync"
    - from: "handleReview/1 after queueMutation"
      to: "replayOnOnline/0"
      via: "online dispatch through the existing active-scope/epoch lease capture and rejection catch"
      pattern: "navigator.onLine|replayOnOnline\(\)"
    - from: "worker/storage rejection"
      to: "retained queue plus paused status"
      via: "current-lease catch path with discarded failure detail and no retry scheduling"
      pattern: "renderPausedStatus|unhandledrejection|scoped_mutations"
  prohibitions:
    - statement: "The browser test fixture must not become a production session default, public authentication shortcut, bearer-token authority, or adopter-specific account model."
      category: safety
      verification: flagged-unverified
    - statement: "A replay failure must not delete queued work, retry in a hot loop, mutate a newer scope, or surface backend/storage details to the learner."
      category: safety
      verification: flagged-unverified
    - statement: "Browser status, console, page errors, Playwright output, and retained proof must not contain raw answers, scope references, session/account facts, credentials, tokens, device identifiers, endpoints, or host flag names."
      category: privacy
      verification: flagged-unverified
flagged_assumptions:
  - "[UNRESOLVED-SPEC-PROBE][SCOPE-02 unclassified] The demonstrated online-submit error is treated as the existing paused/retained lifecycle state; no broader retry UX is inferred."
  - "[UNRESOLVED-SPEC-PROBE][SCOPE-04 unclassified] Existing egress allowlists remain authoritative; this plan adds only negative browser canary assertions and no new observable field."
  - "[UNRESOLVED-SPEC-PROBE][SCOPE-05 unclassified] The test-only host authority supplies typed Sigra evidence but does not expand Sigra or core ownership."
---

<objective>
Keep authenticated browser proof runnable and contain immediate-online replay failure in the visible paused state.

Purpose: Exercise the repaired production auth boundary with an explicit test-only host session while ensuring online submission never creates an unhandled browser rejection.
Output: Compile-time-gated replay test authority, authenticated Playwright setup, lease-aware immediate-submit dispatch, and deterministic failure containment evidence.
</objective>

<artifacts_this_phase_produces>
- New file/module: `examples/phoenix_host/lib/crosswake_example/e2e/replay_authority.ex` / `CrosswakeExample.E2E.ReplayAuthority`.
- New file/module: `examples/phoenix_host/lib/crosswake_example/e2e/replay_session_controller.ex` / `CrosswakeExample.E2E.ReplaySessionController`.
- New test-only route: `POST /_e2e/replay-session`, inside the router's existing `Mix.env() in [:test, :e2e]` guard.
- New test-only configuration value selecting `CrosswakeExample.E2E.ReplayAuthority` for `:offline_study_replay_authority`.
- New Playwright regression name: `immediate online submit failure retains queued work without an unhandled rejection`.
- No production session fixture, new wire field, browser token, persisted account field, retry scheduler, or UI state vocabulary.
</artifacts_this_phase_produces>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/160-scoped-replay-and-auth-safety/160-CONTEXT.md
@.planning/phases/160-scoped-replay-and-auth-safety/160-VERIFICATION.md
@.planning/phases/160-scoped-replay-and-auth-safety/160-14-SUMMARY.md
@examples/phoenix_host/lib/crosswake_example/router.ex
@examples/phoenix_host/priv/static/offline_study.js
@examples/phoenix_host/e2e/offline_sync.spec.ts

<interfaces>
- `ReplayAuth` from Plan 160-14 requires the configured host adapter and a current signed host session; the E2E adapter must implement only that exact host contract.
- `replayOnOnline()` captures the active scope-and-epoch lease, calls `flushOutbox()`, catches failure, rechecks the lease, and renders the paused state without exposing the error.
- `handleReview(rating)` already persists the mutation before triggering online replay; the repair changes only the worker entry point after that durable write.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Authenticate the existing browser proof through an explicit test-only host adapter</name>
  <read_first>
    - AGENTS.md
    - .planning/phases/160-scoped-replay-and-auth-safety/160-CONTEXT.md
    - .planning/phases/160-scoped-replay-and-auth-safety/160-VERIFICATION.md
    - .planning/phases/160-scoped-replay-and-auth-safety/160-14-SUMMARY.md
    - examples/phoenix_host/lib/crosswake_example/router.ex
    - examples/phoenix_host/config/test.exs
    - examples/phoenix_host/e2e/offline_sync.spec.ts
  </read_first>
  <files>examples/phoenix_host/lib/crosswake_example/router.ex, examples/phoenix_host/lib/crosswake_example/e2e/replay_authority.ex, examples/phoenix_host/lib/crosswake_example/e2e/replay_session_controller.ex, examples/phoenix_host/config/test.exs, examples/phoenix_host/e2e/offline_sync.spec.ts</files>
  <behavior>
    - "The browser obtains a signed host session only through a `/_e2e` route compiled in test/e2e environments, then the normal public replay route derives current authority from that session."
    - "No session, an explicitly logged-out fixture, a switched fixture account, and a revoked fixture session remain non-success and persist nothing when driven through the browser/request context."
    - "The production router/configuration contains no fixture identity or permissive authority fallback."
  </behavior>
  <action>Start RED by updating the primary offline-island Playwright setup to establish a replay session before any authorized sync and prove that the same endpoint denies after the test session is cleared/revoked. Add `CrosswakeExample.E2E.ReplayAuthority` and `ReplaySessionController` inside the existing reserved E2E namespace and compile-time router guard. The controller may create, switch, revoke, or clear only synthetic opaque test session state; the authority adapter must read that signed state from `Plug.Conn`, return the exact host contract from Plan 160-14, and re-evaluate current state on every event. Configure it only in `config/test.exs`/the existing E2E environment, never as the production fallback. Update direct `page.request.post('/study/sync')` calls to retain cookies/session context and keep fixture values out of assertions/artifacts. Do not reuse the SaaS demo account as replay authority, expose adopter values, or add credentials/tokens to the browser.</action>
  <acceptance_criteria>
    - `POST /_e2e/replay-session` and its adapter are compiled only under the existing test/e2e guard.
    - The full offline-island browser corpus establishes an explicit signed replay session before authorized public replay.
    - Browser/request-level no-session, logout, switch, and revocation probes deny before persistence.
    - Production configuration has no synthetic scope/session/account/route/flag values and missing host authority remains fail closed.
    - Existing browser fixtures remain opaque, synthetic, and absent from retained proof output.
  </acceptance_criteria>
  <verify><automated>cd examples/phoenix_host &amp;&amp; MIX_ENV=test mix test test/crosswake_example/local_first test/crosswake_example/e2e &amp;&amp; npm run proof:offline-island -- --grep "fully authorized scoped Study event|anonymous|logged-out|account switch|revoked session"</automated></verify>
  <done>The real replay endpoints are exercised through an explicit signed test session and test-only current-authority adapter, with no production fixture fallback.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Contain immediate-online worker rejection with the existing lease guard</name>
  <read_first>
    - .planning/phases/160-scoped-replay-and-auth-safety/160-CONTEXT.md
    - .planning/phases/160-scoped-replay-and-auth-safety/160-VERIFICATION.md
    - examples/phoenix_host/priv/static/offline_study.js
    - examples/phoenix_host/e2e/offline_sync.spec.ts
  </read_first>
  <files>examples/phoenix_host/priv/static/offline_study.js, examples/phoenix_host/e2e/offline_sync.spec.ts</files>
  <behavior>
    - "Submitting a review while online queues the mutation, then dispatches replay through `replayOnOnline/0` rather than directly discarding `flushOutbox/0`."
    - "A deterministic scoped IndexedDB/worker failure after the local write leaves exactly one queued mutation, renders `Sync is paused`, and produces zero unhandled rejections, page errors, or console messages."
    - "The failure schedules no retry and a subsequent fence/account switch prevents stale status/storage effects."
  </behavior>
  <action>Start RED with the exact immediate-online submit path: authenticate and activate a synthetic scope, intercept the scoped worker/storage operation so the local `queueMutation` succeeds but the subsequent replay read/send/delete phase rejects deterministically, attach `unhandledrejection`, `pageerror`, console, and request counters before clicking the real rating button, and assert paused status plus retained exact-scope queue. Change `handleReview/1` to enter `replayOnOnline/0` after the durable local write instead of calling `flushOutbox/0` directly. Reuse its active-scope/epoch lease capture, catch, current-lease recheck, failure-detail discard, and paused rendering; do not add a second catch path, retry timer, new status label, or error logging. Keep the current-card progression and durable local save behavior unchanged.</action>
  <acceptance_criteria>
    - `handleReview/1` contains no direct fire-and-forget call to `flushOutbox/0`; online dispatch uses `replayOnOnline/0`.
    - The named immediate-online failure Playwright test clicks the real submit control after successful queueing and forces the replay worker to reject.
    - The test observes paused UI, exactly one retained exact-scope record, one replay attempt at most, zero unhandled rejections, zero page errors, and zero console output.
    - Existing active-online, inactive/fenced, activation replay, switch, halted, malformed acknowledgement, quarantine, recovery, duplicate, and privacy browser cases remain green.
    - No error object, scope reference, payload, session/account fact, endpoint, or token appears in UI or captured output.
  </acceptance_criteria>
  <verify><automated>cd examples/phoenix_host &amp;&amp; npm run proof:offline-island -- --grep "immediate online submit failure retains queued work without an unhandled rejection" &amp;&amp; npm run proof:offline-island</automated></verify>
  <done>Immediate-online replay failure is contained by the current lease-aware worker path, with visible paused state, retained work, and no unhandled browser rejection.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| test harness -> production replay route | Synthetic authority may cross only through a signed, compile-time-gated test session; the public route still uses the production auth pipeline. |
| local durable write -> online worker | The queued mutation exists before replay begins and must survive every worker failure. |
| rejected async worker -> lifecycle/UI | Only a still-current lease may render paused state; failures cannot become unhandled rejection or stale-account effects. |
| sensitive browser state -> test/evidence output | Scope, payload, session/account, endpoint, and exception details remain absent from console, page errors, assertions, and retained evidence. |

## ASVS L1 Control Mapping

| ASVS category | Required control | Automated evidence |
|---|---|---|
| V2 Authentication | Browser proof uses an explicit signed host test session and normal replay pipeline. | Authenticated Playwright and request-level denial cases. |
| V3 Session Management | Logout, account switch, and revocation change current server state and block replay. | Test-only session controller scenarios. |
| V4 Access Control | Public replay remains protected even when exercised by E2E fixtures. | Production-route requests through test authority adapter. |
| V7 Error Handling and Logging | Online worker rejection becomes paused state with no unhandled or logged detail. | Immediate-submit failure regression. |
| V8 Data Protection | Test/session/scope/payload values do not enter observable output. | Console/pageerror/body and retained-artifact assertions. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-160-15-01 | Spoofing / Elevation of Privilege | anonymous/test replay session | high | mitigate | Task 1 makes authentication explicit and confines the fixture issuer/adapter to the compile-time E2E guard. |
| T-160-15-02 | Spoofing | logged-out, switched, or revoked fixture session | high | mitigate | Task 1 mutates current host session state and proves subsequent public replay denial before persistence. |
| T-160-15-03 | Elevation of Privilege | permissive fixture fallback | critical | mitigate | Task 1 configures synthetic authority only in test/E2E and asserts production has no fallback. |
| T-160-15-04 | Tampering | scope mismatch/stale lease after account switch | high | mitigate | Tasks 1-2 retain exact server scope comparison and browser scope-plus-epoch checks. |
| T-160-15-05 | Denial of Service | unhandled immediate-online rejection/hot retry | high | mitigate | Task 2 routes submit through the guarded worker, asserts paused state, queue retention, zero unhandled rejection, and bounded request count. |
| T-160-15-06 | Information Disclosure | browser/test output | high | mitigate | Both tasks assert no sensitive fixture or failure details in UI, console, page errors, responses, or evidence. |
| T-160-SC | Tampering | dependency supply chain | low | accept | No package install or dependency change is planned; existing lockfiles and commands are reused. |
</threat_model>

<source_coverage_audit>
| Source | ID | Gap-relevant contract | Coverage | Status |
|---|---|---|---|---|
| GOAL | Phase 160 | Authenticated scoped replay remains visibly fail closed in the bounded study island. | Tasks 1-2 exercise production auth and contain online failure. | COVERED |
| REQ | SCOPE-01 | Exact-scope queued work remains partitioned. | Task 2 asserts retained exact-scope queue. | COVERED |
| REQ | SCOPE-02 | Logout/switch and worker failure stop replay safely. | Tasks 1-2. | COVERED |
| REQ | SCOPE-03 | Browser proof reaches current backend authority before mutation. | Task 1 routes authenticated E2E through Plan 160-14. | COVERED |
| REQ | SCOPE-04 | Sensitive values remain absent from outputs. | Both tasks require negative observable-output assertions. | COVERED |
| REQ | SCOPE-05 | Sigra remains server-side typed evidence only. | Test adapter supplies typed evidence server-side; browser receives none. | COVERED |
| RESEARCH | Existing proof seam | Extend Playwright instead of a second proof system. | Both tasks modify the primary offline-island corpus only. | COVERED |
| RESEARCH | Fence-first failure handling | Current lease gates storage/UI side effects and blocked work is retained. | Task 2 reuses `replayOnOnline/0`. | COVERED |
| CONTEXT | D-01 through D-13 | Exact scope, fence, per-event authority, closed outcomes, host flag. | Plan 160-14 plus Tasks 1-2 provide browser proof without changing ownership. | COVERED |
| CONTEXT | D-14 through D-23 | Existing proof/evidence system, privacy split, Sigra boundary, calm accessible UI. | Tasks 1-2 extend only the current test seam and paused status. | COVERED |
| CONTEXT | Deferred/excluded | TODO-002, adopter values, Android, packs, device claims, generic/background sync. | Explicitly absent. | EXCLUDED |
</source_coverage_audit>

<verification>
Run the authenticated fixture setup and denial subset first, then the immediate-online failure case, then the complete offline-island corpus. All checks are automated and use the real public replay routes; no manual UAT or physical-device claim is involved.
</verification>

<success_criteria>
- Browser proof authenticates explicitly through a compile-time test-only host adapter.
- Production retains no synthetic replay authority fallback.
- Immediate-online submission uses the established lease-aware worker guard.
- Deterministic worker failure visibly pauses, retains the queue, emits no unhandled rejection, and does not hot retry.
- The complete scoped replay/auth/privacy browser corpus remains green.
</success_criteria>

<output>
Create `.planning/phases/160-scoped-replay-and-auth-safety/160-15-SUMMARY.md` when done.
</output>
