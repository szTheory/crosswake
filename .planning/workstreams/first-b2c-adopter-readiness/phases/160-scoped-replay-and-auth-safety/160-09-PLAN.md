---
phase: 160-scoped-replay-and-auth-safety
plan: "09"
type: execute
wave: 7
depends_on: ["160-08"]
files_modified:
  - examples/phoenix_host/lib/crosswake_example/local_first/replay_admission.ex
  - examples/phoenix_host/lib/crosswake_example/local_first/study.ex
  - examples/phoenix_host/lib/crosswake_example/local_first/sync_controller.ex
  - examples/phoenix_host/test/crosswake_example/local_first/replay_admission_test.exs
  - examples/phoenix_host/test/crosswake_example/local_first/study_test.exs
  - examples/phoenix_host/test/crosswake_example/local_first/sync_controller_test.exs
autonomous: true
gap_closure: true
requirements: [SCOPE-01, SCOPE-03]
must_haves:
  truths:
    - "Phoenix host admission accepts exactly the same anchored `vN.<16-128 URL-safe bytes>` opaque scope grammar as core and browser, and every hostile non-opaque value denies before any authority callback or mutation (D-01, D-05, D-08, SCOPE-01)."
    - "A persisted same-scope or legacy null-scope ReviewEvent with status `accepted` remains an accepted idempotency tombstone, while status `rejected` remains an explicit rejected result in both ordinary duplicate and transaction-race recovery paths (D-09, D-11, SCOPE-03)."
    - "SyncController places only accepted outcomes in `accepted_records`; a persisted rejection is returned through the retained rejection path so the browser accepted-prefix deletion cannot remove it (D-07, D-11, D-12)."
  artifacts:
    - path: "examples/phoenix_host/lib/crosswake_example/local_first/replay_admission.ex"
      provides: "Anchored cross-layer opaque scope validation at the host trust boundary."
    - path: "examples/phoenix_host/lib/crosswake_example/local_first/study.ex"
      provides: "Status-aware persisted idempotency outcomes shared by normal and race recovery."
    - path: "examples/phoenix_host/lib/crosswake_example/local_first/sync_controller.ex"
      provides: "Accepted-only acknowledgement and retained rejected response routing."
    - path: "examples/phoenix_host/test/crosswake_example/local_first/replay_admission_test.exs"
      provides: "Positive boundary and hostile non-opaque admission regressions."
    - path: "examples/phoenix_host/test/crosswake_example/local_first/study_test.exs"
      provides: "Normal and race-recovery rejected-row regressions."
    - path: "examples/phoenix_host/test/crosswake_example/local_first/sync_controller_test.exs"
      provides: "Response-level proof that rejected records never enter the accepted prefix."
  key_links:
    - from: "untrusted replay scope_ref"
      to: "ReplayAdmission.authorize/4 authority callbacks"
      via: "`valid_scope/1` matches the full anchored URL-safe grammar before session resolution"
      pattern: "valid_scope|scope_ref_pattern"
    - from: "ReviewEvent.status"
      to: "Study.apply_one/3 and current_outcome/2"
      via: "one status-aware outcome mapper used by duplicate and race-recovery decisions"
      pattern: "accepted|rejected|current_outcome"
    - from: "Study rejected outcome"
      to: "SyncController rejected response"
      via: "outcome-specific branching excludes rejection from accepted_records"
      pattern: "accepted_records|rejected"
  prohibitions:
    - "MUST NOT accept whitespace, punctuation, account-like delimiters, non-URL-safe bytes, zero/leading-zero versions, or values outside the 16-128 byte payload bounds as opaque scopes."
    - "MUST NOT translate a persisted rejected row into accepted, deleteable, conflict, or free-form error output."
flagged_assumptions:
  - "[UNRESOLVED-SPEC-PROBE] SCOPE-01 remains unclassified: synthetic boundary values prove the transport grammar, while real host issuance, rotation, and account mapping remain unknown_blocking."
  - "[UNRESOLVED-SPEC-PROBE] SCOPE-03 remains unclassified: synthetic accepted/rejected rows prove truthful idempotency outcomes without inferring adopter domain reconciliation."
---

<objective>
Close the two Phoenix-host defects that can admit a non-opaque scope or falsely acknowledge retained rejected work.

Purpose: Restore the same fail-closed scope grammar at every layer and keep persisted recovery outcomes truthful through the browser response boundary.
Output: Anchored host admission, status-aware idempotency recovery, accepted-only controller acknowledgement, and hostile regressions.
</objective>

<artifacts_this_phase_produces>
- Module attribute `ReplayAdmission.@scope_ref_pattern` and the tightened private `valid_scope/1` behavior in `examples/phoenix_host/lib/crosswake_example/local_first/replay_admission.ex`.
- A private status-aware persisted-outcome mapper in `examples/phoenix_host/lib/crosswake_example/local_first/study.ex`, used by ordinary duplicates and `current_outcome/2` race recovery.
- Outcome-specific accepted/rejected routing in `SyncController.sync_events/4`.
- Named hostile-scope, persisted-rejection, race-recovery, and accepted-prefix regression cases in the three existing Phoenix local-first test files.
</artifacts_this_phase_produces>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/ADR-FIRST-B2C-ADOPTER.md
@.planning/phases/160-scoped-replay-and-auth-safety/160-CONTEXT.md
@.planning/phases/160-scoped-replay-and-auth-safety/160-RESEARCH.md
@.planning/phases/160-scoped-replay-and-auth-safety/160-PATTERNS.md
@.planning/phases/160-scoped-replay-and-auth-safety/160-VERIFICATION.md
@.planning/phases/160-scoped-replay-and-auth-safety/160-08-SUMMARY.md

<interfaces>
`ReplayAdmission.authorize/4` must deny malformed scope input before `resolve(:session, ...)`. `Study.apply_one/3` owns closed `accepted` versus `rejected` persisted outcomes; `SyncController.sync_events/4` owns whether those outcomes enter `accepted_records` or the retained `rejected` collection. Existing `scope_conflict`, blocked admission, atomic transaction, and non-echoing response contracts remain unchanged.
</interfaces>
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Trace one opaque scope through exact host admission and reject hostile variants before authority</name>
  <reversibility rating="reversible">The change replaces one private validator while preserving the persisted and wire grammar already enforced by core and browser.</reversibility>
  <read_first>
    - AGENTS.md
    - .planning/phases/160-scoped-replay-and-auth-safety/160-CONTEXT.md
    - .planning/phases/160-scoped-replay-and-auth-safety/160-VERIFICATION.md
    - lib/crosswake/offline/journal.ex
    - examples/phoenix_host/priv/static/offline_study.js
    - examples/phoenix_host/lib/crosswake_example/local_first/replay_admission.ex
    - examples/phoenix_host/test/crosswake_example/local_first/replay_admission_test.exs
  </read_first>
  <files>examples/phoenix_host/lib/crosswake_example/local_first/replay_admission.ex, examples/phoenix_host/test/crosswake_example/local_first/replay_admission_test.exs</files>
  <behavior>
    - "A positive-version scope with exactly 16 URL-safe payload bytes and one with exactly 128 bytes reaches the existing current-authority chain."
    - "Payloads of 15 or 129 bytes, whitespace, dot/slash/colon/at-sign punctuation, Unicode, account-shaped delimiters, version zero, leading-zero versions, missing prefix, and trailing newline return `{:deny, :invalid_envelope}`."
    - "Every rejected scope stops before session, route, feature, Sigra, domain, or Study callbacks and no rejected value appears in returned or captured bytes."
  </behavior>
  <action>Start with failing table-driven integration cases. Per D-01 and D-08, replace the prefix-plus-length clause with a module-level anchored regular expression equivalent to `v[1-9][0-9]*\.[A-Za-z0-9_-]{16,128}` over the complete binary, and make `valid_scope/1` return only `:ok` or `{:error, :invalid_envelope}`. Exercise both accepted length boundaries and the hostile classes listed above. Instrument the session callback in negative cases to prove validation happens before current authority resolution. Keep the scope opaque and non-echoing; do not decode, normalize, hash, log, or map it.</action>
  <acceptance_criteria>
    - Host, core, and browser enforce the same anchored positive-version URL-safe grammar and 16-128 byte payload bounds.
    - Hostile scope cases return only the closed invalid-envelope denial and invoke zero authority callbacks.
    - Rejected scope bytes appear in neither result nor captured log/error output.
    - SCOPE-01, D-01, D-05, D-08, ASVS L1 V4, V5, and V8 are covered by the focused integration suite.
  </acceptance_criteria>
  <verify><automated>cd examples/phoenix_host &amp;&amp; MIX_ENV=test mix test test/crosswake_example/local_first/replay_admission_test.exs</automated></verify>
  <done>Only the established bounded opaque scope grammar can cross host request validation into current authority resolution.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Preserve persisted rejected outcomes through duplicate, race, and controller response paths</name>
  <reversibility rating="reversible">The change corrects private outcome branching while retaining the existing ReviewEvent schema and typed response envelope.</reversibility>
  <read_first>
    - .planning/phases/160-scoped-replay-and-auth-safety/160-CONTEXT.md
    - .planning/phases/160-scoped-replay-and-auth-safety/160-VERIFICATION.md
    - examples/phoenix_host/lib/crosswake_example/local_first/review_event.ex
    - examples/phoenix_host/lib/crosswake_example/local_first/study.ex
    - examples/phoenix_host/lib/crosswake_example/local_first/sync_controller.ex
    - examples/phoenix_host/test/crosswake_example/local_first/study_test.exs
    - examples/phoenix_host/test/crosswake_example/local_first/sync_controller_test.exs
  </read_first>
  <files>examples/phoenix_host/lib/crosswake_example/local_first/study.ex, examples/phoenix_host/lib/crosswake_example/local_first/sync_controller.ex, examples/phoenix_host/test/crosswake_example/local_first/study_test.exs, examples/phoenix_host/test/crosswake_example/local_first/sync_controller_test.exs</files>
  <behavior>
    - "A pre-existing same-scope `accepted` row and a legacy null-scope `accepted` row still return accepted without adding a second effect."
    - "A pre-existing same-scope `rejected` row and a legacy null-scope `rejected` row return a closed rejected outcome in the ordinary duplicate path."
    - "A deterministic uniqueness-race recovery whose winning row is rejected returns the same rejected outcome through `current_outcome/2`; unknown persisted status fails closed rather than becoming accepted."
    - "SyncController places only accepted outcomes in `accepted_records`; a rejected outcome enters the closed retained rejection response, stops accepted-prefix advancement at that event, and leaves the database row unchanged."
  </behavior>
  <action>Start with persisted-row and response-envelope regressions. Per D-09 and D-11, retain status when the idempotency query finds a same-scope or legacy null-scope ReviewEvent. Add one private mapper from the schema's closed persisted statuses to result outcomes: accepted maps to the existing accepted acknowledgement, rejected maps to `{:ok, %{client_mutation_id: id, outcome: :rejected}}`, and any unexpected value fails closed. Use that mapper for both the ordinary duplicate result and `current_outcome/2`, so `race_outcome/3` cannot diverge. In `SyncController.sync_events/4`, append only `outcome: :accepted` to `accepted_records`; route `outcome: :rejected` into the existing low-cardinality `rejected` collection and stop the ordered drain without acknowledging that event or later entries. Add normal, legacy, deterministic race-recovery, and controller regressions that assert one unchanged ReviewEvent and no rejected ID in the accepted prefix.</action>
  <acceptance_criteria>
    - Normal and race-recovery paths report persisted accepted/rejected status identically for same-scope and null-scope tombstones.
    - Rejected rows remain present and are never represented in `accepted_records`.
    - Cross-scope conflict, rollback, and concurrent accepted-retry behavior remain green.
    - SCOPE-03, D-07, D-09, D-11, D-12, ASVS L1 V4, V7, and V8 are covered by focused Phoenix tests.
  </acceptance_criteria>
  <verify><automated>cd examples/phoenix_host &amp;&amp; MIX_ENV=test mix test test/crosswake_example/local_first/study_test.exs test/crosswake_example/local_first/sync_controller_test.exs</automated></verify>
  <done>Persisted rejection remains a truthful retained recovery outcome through normal duplicates, transaction races, and the browser-facing response envelope.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| browser replay envelope -> Phoenix authority chain | An untrusted scope must match the complete opaque transport grammar before host session or route authority is consulted. |
| persisted ReviewEvent -> replay response | Stored idempotency status controls whether browser work may be acknowledged and deleted. |

## ASVS L1 Control Mapping

| ASVS category | Required control | Automated evidence |
|---|---|---|
| V4 Access Control | Malformed scopes stop before authority callbacks; rejected status cannot gain accepted semantics. | ReplayAdmission and controller focused tests. |
| V5 Input Validation | Full anchored grammar and exact byte bounds at host admission. | Hostile scope table. |
| V7 Error Handling and Logging | Denials and persisted rejection use closed non-echoing classes. | Captured result/error assertions. |
| V8 Data Protection | Scope values and rejected record details never enter diagnostic output. | Non-echoing byte assertions. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-160-01 | Spoofing / Elevation | ReplayAdmission scope validation | high | mitigate | Task 1 enforces the shared anchored grammar before every authority callback. |
| T-160-05 | Tampering / Repudiation | ReviewEvent idempotency outcome | high | mitigate | Task 2 derives acknowledgement from persisted status in normal and race paths and excludes rejection from accepted_records. |
| T-160-SC | Tampering | package installs | low | accept | No package-manager installation occurs; existing Phoenix, Ecto, and ExUnit dependencies are reused. |
</threat_model>

<verification>
Run both focused Phoenix commands. They must prove hostile scope rejection, accepted/rejected persisted status parity, race recovery, accepted-only response routing, and existing conflict/rollback/idempotency behavior without manual UAT.
</verification>

<success_criteria>
The Phoenix host accepts only the established opaque scope grammar and can no longer turn retained rejected work into a deleteable accepted acknowledgement.
</success_criteria>

<output>
Create `.planning/phases/160-scoped-replay-and-auth-safety/160-09-SUMMARY.md` when done.
</output>
