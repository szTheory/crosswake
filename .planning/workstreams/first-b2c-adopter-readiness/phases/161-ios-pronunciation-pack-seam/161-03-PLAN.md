---
phase: 161-ios-pronunciation-pack-seam
plan: "03"
type: execute
wave: 3
depends_on: ["161-02"]
files_modified:
  - examples/ios_shell_host/CrosswakeShell/CrosswakeShellApp.swift
  - examples/ios_shell_host/CrosswakeShell/RequiredPackView.swift
  - examples/ios_shell_host/CrosswakeShellTests/RequiredPackViewTests.swift
  - examples/ios_shell_host/CrosswakeShell.xcodeproj/project.pbxproj
autonomous: true
requirements: [PACK-01, PACK-02, PACK-04, PACK-05]
must_haves:
  truths:
    - "The reference host explicitly constructs and injects its provider and private exact requirement configuration; missing injection remains provider_unavailable and no bundled fallback exists (D-01, D-02, D-03)."
    - "The reference UI exposes semantic state, one permitted foreground action, owning layer, stable reason/rule and accessibility identifiers while final learner copy/presentation remain host-owned (D-13, D-14, D-15)."
    - "Install, Update, Retry, and Invalidate-then-Install recovery are explicit; there is no background continuation, automatic retry loop, silent online-only downgrade, or second runtime owner (D-13; PACK-05)."
    - "Dynamic Type, text-plus-color status, accessible announcements/focus, reduced-motion-safe transitions, and system light/dark colors remain usable without exposing backend mechanics (D-14)."
  artifacts:
    - path: "examples/ios_shell_host/CrosswakeShell/CrosswakeShellApp.swift"
      provides: "Explicit reference-host provider construction and CrosswakeShellConfig injection using private requirement values."
    - path: "examples/ios_shell_host/CrosswakeShell/RequiredPackView.swift"
      provides: "Semantic foreground recovery UI with stable identifiers and closed safe copy."
    - path: "examples/ios_shell_host/CrosswakeShellTests/RequiredPackViewTests.swift"
      provides: "Deterministic state/action/copy/accessibility contract tests."
  key_links:
    - from: "PronunciationPackProvider"
      to: "CrosswakeShellConfig.packProvider"
      via: "example-host composition root only"
      pattern: "PronunciationPackProvider|packProvider"
    - from: "RequiredPackStatus"
      to: "RequiredPackView primary action"
      via: "closed state/reason-to-permitted-action mapping"
      pattern: "notInstalled|stale|failed|invalidating|available"
    - from: "RequiredPackView action"
      to: "ActivationCoordinator install/retry/invalidate"
      via: "existing shell coordinator callbacks"
      pattern: "onInstall|onRetry|onInvalidate"
  prohibitions:
    - requirement_id: PACK-02
      category: safety
      statement: "MUST NOT let requested, staged, acknowledged, old-version, malformed, revoked, or unreconciled media silently activate a route or downgrade it to a different runtime behavior."
      status: unresolved
      verification: flagged-unverified
    - requirement_id: PACK-04
      category: values
      statement: "MUST NOT turn the provider seam into Crosswake-owned distribution, generic storage, asset lookup, playback policy, or host media-layout authority."
      status: unresolved
      verification: flagged-unverified
    - requirement_id: PACK-05
      category: transparency
      statement: "MUST NOT present local fixture, simulator, package, or generated native results as physical-iPhone, adopter-instance, Android, background-transfer, or generic-storage proof."
      status: unresolved
      verification: flagged-unverified
flagged_assumptions:
  - "[UNRESOLVED-SPEC-PROBE][PACK-01 unclassified] Reference-host composition uses the exact Plan 01 seam and adds no separate download/storage API."
  - "[UNRESOLVED-SPEC-PROBE][PACK-02 unclassified] UI action availability derives exclusively from closed lifecycle/reason values and never overrides core activation truth."
  - "[UNRESOLVED-SPEC-PROBE][PACK-03 unclassified] Exact integrity values remain host-private inputs and are not rendered or retained."
  - "[UNRESOLVED-SPEC-PROBE][PACK-04 concurrency] UI disables transient operations while the serialized provider/store owns the action; repeated taps cannot create parallel authority."
  - "[UNRESOLVED-SPEC-PROBE][PACK-05 unclassified] Reference presentation demonstrates foreground iOS behavior only and adds no support claim or taxonomy."
---

<objective>
Expose the verified pack lifecycle through an explicit host composition root and one accessible, task-oriented foreground recovery surface.

Purpose: Make the host/Core ownership boundary and every non-available recovery state understandable without leaking storage mechanics or expanding product scope.
Output: Reference provider injection, semantic pack UI, stable accessibility IDs, and deterministic view-contract tests.
</objective>

<artifacts_this_phase_produces>
- Changed `CrosswakeShellApp`/`RootSceneWrapper` construction of a host-owned `PronunciationPackProvider` and optional `packProvider:` config argument.
- Changed `RequiredPackView` semantic mappings for the seven D-11 lifecycle states and closed reasons, including one primary permitted action and an explicit invalidate recovery action where allowed.
- New stable accessibility IDs: `required-pack-status`, `required-pack-primary-action`, `required-pack-invalidate-action`, and `required-pack-owner` (or equivalent exact names recorded by implementation tests).
- New private view helpers for safe learner message, developer rule/owner/remediation, permitted action, announcement, and focus behavior.
- New `RequiredPackViewTests.swift` and Xcode test-target membership.
- No new endpoint, CLI command/flag, bridge command, public asset lookup, media-player API, Android UI, background task, dashboard, or support label.
</artifacts_this_phase_produces>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/ADR-FIRST-B2C-ADOPTER.md
@.planning/FIRST-B2C-ADOPTER-ADOPTION-BRIEF.md
@.planning/phases/161-ios-pronunciation-pack-seam/161-CONTEXT.md
@.planning/phases/161-ios-pronunciation-pack-seam/161-PATTERNS.md
@.planning/phases/161-ios-pronunciation-pack-seam/161-02-SUMMARY.md
@examples/ios_shell_host/CrosswakeShell/CrosswakeShellApp.swift
@examples/ios_shell_host/CrosswakeShell/RequiredPackView.swift
@examples/ios_shell_host/CrosswakeShell/PronunciationPackProvider.swift

<interfaces>
- `CrosswakeShellConfig` now accepts an optional `PackProvider`; only the example composition root supplies the concrete reference provider.
- `RequiredPackView` receives `RequiredPackStatus` and three existing action closures; preserve this host-owned presentation seam.
- `ActivationCoordinator` and `PackStore` own state/allowed behavior; the view must not inspect filesystem/provider internals or manufacture lifecycle transitions.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Inject the reference provider and render closed foreground recovery through stable accessibility semantics</name>
  <reversibility rating="reversible">Reference composition, copy, accessibility IDs, and view helpers are host-owned and can evolve without changing the costly provider protocol.</reversibility>
  <read_first>
    - AGENTS.md
    - brandbook/BRAND-SPEC.md
    - .planning/phases/161-ios-pronunciation-pack-seam/161-CONTEXT.md
    - .planning/phases/161-ios-pronunciation-pack-seam/161-PATTERNS.md
    - .planning/phases/161-ios-pronunciation-pack-seam/161-02-SUMMARY.md
    - packages/crosswake-shell-core-ios/Sources/CrosswakeShellCore/PackProvider.swift
    - packages/crosswake-shell-core-ios/Sources/CrosswakeShellCore/PackStore.swift
    - examples/ios_shell_host/CrosswakeShell/PronunciationPackProvider.swift
    - examples/ios_shell_host/CrosswakeShell/CrosswakeShellApp.swift
    - examples/ios_shell_host/CrosswakeShell/RequiredPackView.swift
    - examples/ios_shell_host/CrosswakeShellTests/DiagnosticExportManagerTests.swift
    - examples/ios_shell_host/CrosswakeShell.xcodeproj/project.pbxproj
  </read_first>
  <files>examples/ios_shell_host/CrosswakeShell/CrosswakeShellApp.swift, examples/ios_shell_host/CrosswakeShell/RequiredPackView.swift, examples/ios_shell_host/CrosswakeShellTests/RequiredPackViewTests.swift, examples/ios_shell_host/CrosswakeShell.xcodeproj/project.pbxproj</files>
  <behavior>
    - "The example host alone constructs its provider and private exact requirement, then injects the provider through CrosswakeShellConfig; omitting it still yields provider_unavailable."
    - "Checking/installing/invalidating expose status but disable primary action; not-installed offers Install, stale offers Update, retryable failed offers Retry, corrupt/revoked flows expose Invalidate then Install, and available shows ready without implying generic storage."
    - "Learner copy is brief and task-oriented; developer detail contains only stable PACK-* rule, owner `host pack provider`, and safe remediation, never path/URL/digest/raw error/archive/media."
    - "At accessibility text sizes, every status/action remains fully labeled, text accompanies color, status changes announce without unstable focus, controls meet 44pt targets, reduced motion avoids decorative transitions, and system colors support light/dark."
  </behavior>
  <action>Start with view-contract tests over every lifecycle/reason/action combination and the reference composition source. Construct the `PronunciationPackProvider` only in `CrosswakeShellApp`/`RootSceneWrapper`, with exact fixture size and SHA-256 held in private native configuration; inject only the protocol through `CrosswakeShellConfig` per D-01 through D-04. Refactor `RequiredPackView` to derive permitted actions and messages solely from `RequiredPackStatus` per D-13 and D-14. Use one prominent action, stable accessibility identifiers, status announcements, stable focus, text plus semantic color, Dynamic Type-safe vertical flow, system light/dark colors, and reduced-motion-safe behavior. Keep final copy host-owned and use D-15's safe learner wording and stable developer rule/owner/remediation without backend mechanics. Never show digest, exact host path, URL, archive details, raw error, or media. Do not add automatic retry/background continuation, online-only mutation fallback, generic download controls, or a new visual system.</action>
  <acceptance_criteria>
    - The provider is instantiated only in example-host code and config omission remains a tested blocked state.
    - Each lifecycle/reason has exactly the permitted foreground action and no transient state permits duplicate mutation.
    - Stable identifiers and full labels are testable at accessibility text size; text, focus, announcements, reduced motion, and system colors meet D-14.
    - Learner and developer copy contain only safe semantic state, owner, stable rule, and remediation.
    - Source and tests contain no new Android, background, generic storage/distribution, scoring, capture, dashboard, or support-taxonomy behavior.
  </acceptance_criteria>
  <verify><automated>xcodebuild -project examples/ios_shell_host/CrosswakeShell.xcodeproj -scheme CrosswakeShell -destination 'platform=iOS Simulator,name=iPhone 16' -only-testing:CrosswakeShellTests/RequiredPackViewTests test CODE_SIGNING_ALLOWED=NO</automated></verify>
  <done>The reference host visibly owns provider construction and learner presentation, while every pack state offers one accessible, explicit foreground recovery action without leaking host mechanics.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| private host requirement/provider -> CrosswakeShellConfig | Exact integrity/storage configuration must stay host-private while the protocol injection remains explicit. |
| closed core status -> RequiredPackView | Host UI may present state/actions but cannot manufacture availability or bypass activation. |
| provider failures -> learner/developer copy | Raw host details must collapse to safe copy and stable rule/owner/remediation. |
| repeated user action -> serialized store/provider | UI must not start parallel authority while an operation is transient. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-161-11 | Information Disclosure | reference composition and UI copy | high | mitigate | Task 1 keeps exact configuration private and tests closed copy/identifiers only. |
| T-161-12 | Spoofing | RequiredPackView ready state | high | mitigate | View consumes PackStore state only and cannot set lifecycle/activation authority. |
| T-161-13 | Tampering / Denial of Service | repeated foreground actions | medium | mitigate | Transient states disable actions and underlying Plan 02 serialization remains authoritative. |
| T-161-14 | Repudiation | ambiguous ownership/recovery | medium | mitigate | Stable rule, owner, state, and permitted action make denial explicit without raw details. |
| T-161-SC | Tampering | dependency supply chain | low | accept | No dependency or package install is planned; existing SwiftUI/XCTest toolchain is reused. |
</threat_model>

<source_coverage_audit>
| Source | ID | Contract | Plan-set coverage | Status |
|---|---|---|---|---|
| GOAL | Phase 161 | Explicit host/Core boundary and fail-closed recovery. | Reference composition and semantic UI. | COVERED |
| REQ | PACK-01 | Host-supplied foreground provider. | Explicit composition injection. | COVERED |
| REQ | PACK-02 | Unconfigured/invalid paths never available. | Missing-provider and closed-action tests. | COVERED |
| REQ | PACK-04 | Host owns private media integration/UI. | Provider construction and all copy remain host-owned. | COVERED |
| REQ | PACK-05 | No background/generic/Android/scoring scope. | Source-contract assertions and artifacts list. | COVERED |
| CONTEXT | D-13, D-14, D-15 | Explicit foreground recovery, accessible semantic UI, safe copy. | Task 1. | COVERED |
| RESEARCH | UI ownership | Core exposes state; host owns presentation. | Task 1 preserves dependency direction. | COVERED |
| EDGE | PACK-01..PACK-05 | Spec-less probe rows remain unresolved. | All five flagged assumptions retained. | FLAGGED |
| PROHIBITION | PACK-02, PACK-04, PACK-05 | Bespoke safety/ownership/promotion constraints. | Descriptor-less flagged-unverified entries retained. | FLAGGED |
</source_coverage_audit>

<verification>
Build the example host and run the view-contract test target. Treat any exposed private value, UI-manufactured state, missing action mapping, duplicate transient action, or inaccessible state as a failed plan.
</verification>

<success_criteria>
- Reference provider injection is explicit and host-owned.
- Missing/unhealthy provider states remain visible and non-available.
- Recovery is foreground-only, semantic, accessible, and one-action-at-a-time.
- No host-private storage/media mechanics or widened support claim appears.
</success_criteria>

<output>
Create `.planning/phases/161-ios-pronunciation-pack-seam/161-03-SUMMARY.md` when done.
</output>
