---
phase: 161-ios-pronunciation-pack-seam
plan: "07"
type: execute
wave: 6
depends_on: ["161-06"]
files_modified:
  - packages/crosswake-shell-core-ios/Sources/CrosswakeShellCore/PackStore.swift
  - packages/crosswake-shell-core-ios/Tests/CrosswakeShellCoreTests/PackStoreTests.swift
  - packages/crosswake-shell-core-ios/Tests/CrosswakeShellCoreTests/ActivationConformanceTests.swift
autonomous: true
gap_closure: true
requirements: [PACK-02, PACK-04]
must_haves:
  truths:
    - "Every empty, malformed, unknown, unsupported-contract, or declaration-incompatible required-pack reference produces a closed blocking status before runtime selection (D-03, D-06, D-11; PACK-02)."
    - "A stale reconciliation result cannot apply after a newer per-pack mutation begins, and ordinary reconciliation never clears persisted revocation (D-05, D-09; PACK-02, PACK-04)."
    - "Only the originating successful invalidation followed by fresh absence, or the originating fully verified reinstall followed by fresh exact installed status, can clear revocation; failed replacement does not destroy known-good bytes (D-07, D-08, D-09)."
    - "Failed, cancelled, interrupted, or overlapping invalidation remains blocked across later reconciliation and relaunch without echoing provider details (D-09, D-12)."
  artifacts:
    - "path: packages/crosswake-shell-core-ios/Sources/CrosswakeShellCore/PackStore.swift | provides: Total required-reference validation and per-pack operation generation fencing."
    - "path: packages/crosswake-shell-core-ios/Tests/CrosswakeShellCoreTests/PackStoreTests.swift | provides: Deterministic malformed-reference and overlapping-reconciliation concurrency matrix."
    - "path: packages/crosswake-shell-core-ios/Tests/CrosswakeShellCoreTests/ActivationConformanceTests.swift | provides: Route-level proof that invalid required references cannot reach LiveView."
  key_links:
    - "from: ShellManifest.routes[].packs | to: PackStore.blockingStatus(for:) | via: total parse/lookup/version validation with no dropped entries | pattern: blockingStatus|parse|malformedProviderResult|versionMismatch"
    - "from: PackStore mutation operation | to: provider await completion | via: captured per-pack operation generation checked before every state write | pattern: operationGeneration|generation"
    - "from: persisted revocation ledger | to: clearRevocation | via: same-generation successful invalidation plus fresh absence or verified reinstall only | pattern: clearRevocation|notInstalled|installed"
  prohibitions:
    - "requirement_id: PACK-02 | category: safety | statement: MUST NOT let requested, staged, acknowledged, old-version, malformed, revoked, or unreconciled media silently activate a route or downgrade it to a different runtime behavior. | status: unresolved | verification: flagged-unverified"
    - "requirement_id: PACK-04 | category: values | statement: MUST NOT turn the provider seam into Crosswake-owned distribution, generic storage, asset lookup, playback policy, or host media-layout authority. | status: unresolved | verification: flagged-unverified"
    - "requirement_id: PACK-05 | category: transparency | statement: MUST NOT present local fixture, simulator, package, or generated native results as physical-iPhone, adopter-instance, Android, background-transfer, or generic-storage proof. | status: unresolved | verification: flagged-unverified"
flagged_assumptions:
  - "[UNRESOLVED-SPEC-PROBE][PACK-01 unclassified] The provider seam remains exactly status/install/invalidate; no transport or storage operation is inferred."
  - "[UNRESOLVED-SPEC-PROBE][PACK-02 unclassified] Every unrecognized availability input stays non-available under the closed D-11 reasons."
  - "[UNRESOLVED-SPEC-PROBE][PACK-03 unclassified] Exact size, pinned SHA-256, atomic promotion, persistence ordering, and fresh status are all mandatory."
  - "[UNRESOLVED-SPEC-PROBE][PACK-04 concurrency] Interrupted or overlapping work is governed by per-pack serialization and current-operation authority."
  - "[UNRESOLVED-SPEC-PROBE][PACK-05 unclassified] The iOS foreground-only stop list is exhaustive and no adjacent platform or product behavior is inferred."
---

<objective>
Make required-pack gating total and make revocation durable under every overlapping reconciliation/invalidation schedule.

Purpose: Close both fail-open paths in the same activation authority: discarded manifest references and stale async results that can erase a newer trust revocation.
Output: Total reference validation, per-pack operation generations, and deterministic route/concurrency XCTest coverage.
</objective>

## Artifacts this phase produces

- New private `PackReferenceResolution` enum in `PackStore.swift` for valid versus closed blocking reference outcomes.
- New private `operationGenerations: [String: UInt64]`, `beginMutation(for:)`, and `isCurrent(_:for:)` generation-fence symbols in `PackStore.swift`.
- New deterministic test actor `ControlledPackProvider` in `PackStoreTests.swift`.
- New XCTest methods covering empty/malformed/unknown/incompatible references, stale reconciliation during failed invalidation, overlapping invalidations, and verified reinstall revocation clearance.
- No new public provider case, lifecycle reason, storage format, bridge capability, background operation, or product surface.

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/ADR-FIRST-B2C-ADOPTER.md
@.planning/phases/161-ios-pronunciation-pack-seam/161-CONTEXT.md
@.planning/phases/161-ios-pronunciation-pack-seam/161-VERIFICATION.md
@.planning/phases/161-ios-pronunciation-pack-seam/161-06-SUMMARY.md
@packages/crosswake-shell-core-ios/Sources/CrosswakeShellCore/PackStore.swift
@packages/crosswake-shell-core-ios/Sources/CrosswakeShellCore/ActivationCoordinator.swift
@packages/crosswake-shell-core-ios/Tests/CrosswakeShellCoreTests/PackStoreTests.swift
@packages/crosswake-shell-core-ios/Tests/CrosswakeShellCoreTests/ActivationConformanceTests.swift

<interfaces>
- `ActivationCoordinator.resolve` treats a non-nil `PackStore.blockingStatus(for:)` as `.requiredPack`; nil continues runtime selection, so parsing must be total.
- `PackStore` is `@MainActor`, but each provider await yields actor execution and permits reconciliation/mutation overlap.
- `invalidatePack` already persists revocation before awaiting provider; the gap is authority to apply later completions and clear the ledger.
- The existing D-11 reasons are closed and costly; reuse `malformedProviderResult`, `versionMismatch`, and `providerUnavailable` deterministically rather than expanding the public enum.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Block every invalid required-pack reference before runtime selection</name>
  <read_first>
    - .planning/phases/161-ios-pronunciation-pack-seam/161-CONTEXT.md
    - .planning/phases/161-ios-pronunciation-pack-seam/161-VERIFICATION.md
    - packages/crosswake-shell-core-ios/Sources/CrosswakeShellCore/PackStore.swift
    - packages/crosswake-shell-core-ios/Sources/CrosswakeShellCore/ActivationCoordinator.swift
    - packages/crosswake-shell-core-ios/Tests/CrosswakeShellCoreTests/PackStoreTests.swift
    - packages/crosswake-shell-core-ios/Tests/CrosswakeShellCoreTests/ActivationConformanceTests.swift
  </read_first>
  <files>packages/crosswake-shell-core-ios/Sources/CrosswakeShellCore/PackStore.swift, packages/crosswake-shell-core-ios/Tests/CrosswakeShellCoreTests/PackStoreTests.swift, packages/crosswake-shell-core-ios/Tests/CrosswakeShellCoreTests/ActivationConformanceTests.swift</files>
  <behavior>
    - Empty strings, whitespace-only strings, missing separators, extra separators, empty ID/version, and invalid bounded syntax return a failed blocking status.
    - A syntactically valid but undeclared pack ID returns a closed provider-unavailable block; a declared ID with a nonexact version returns a closed version-mismatch block.
    - If any required reference is invalid, no later valid/available entry can mask it and no all-invalid list can produce nil.
    - Route-level tests prove each invalid class returns `.requiredPack` and never `.liveView`.
  </behavior>
  <action>Replace the `compactMap` parser pipeline with total ordered resolution through private `PackReferenceResolution`. Validate every original list entry, require exactly one separator plus nonempty bounded components, require a declared requirement, and require the referenced version and contract to match the declaration before consulting status. Map malformed syntax to the existing closed malformed-result reason, unknown IDs to provider-unavailable, and incompatible versions to version-mismatch without echoing the input. Return the first blocking result in route order, including invalid entries; do not silently discard any required reference. Write table-driven `PackStoreTests` and route-level `ActivationConformanceTests` before implementation per D-03, D-06, D-11, and D-12.</action>
  <acceptance_criteria>
    - Every malformed/empty/unknown/incompatible test case returns a deterministic non-available status and stable closed reason.
    - An all-invalid required list and a mixed invalid-plus-available list both block route activation.
    - Valid exact references retain existing available/non-available behavior and route order.
    - Failure output does not contain the rejected reference or other provider/private input.
  </acceptance_criteria>
  <verify><automated>swift test --package-path packages/crosswake-shell-core-ios --filter PackStoreTests &amp;&amp; swift test --package-path packages/crosswake-shell-core-ios --filter ActivationConformanceTests</automated></verify>
  <done>Required-pack parsing is total: every manifest entry either resolves to exact current authority or creates an explicit route block.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Fence stale reconciliation and preserve revocation through overlapping invalidation</name>
  <read_first>
    - .planning/phases/161-ios-pronunciation-pack-seam/161-CONTEXT.md
    - .planning/phases/161-ios-pronunciation-pack-seam/161-RESEARCH.md
    - .planning/phases/161-ios-pronunciation-pack-seam/161-VERIFICATION.md
    - packages/crosswake-shell-core-ios/Sources/CrosswakeShellCore/PackStore.swift
    - packages/crosswake-shell-core-ios/Tests/CrosswakeShellCoreTests/PackStoreTests.swift
  </read_first>
  <files>packages/crosswake-shell-core-ios/Sources/CrosswakeShellCore/PackStore.swift, packages/crosswake-shell-core-ios/Tests/CrosswakeShellCoreTests/PackStoreTests.swift</files>
  <behavior>
    - A reconciliation started before invalidation cannot write status or clear revocation after invalidation begins.
    - Ordinary reconciliation while revoked never clears revocation even when the provider reports the old installed record.
    - Failed/cancelled invalidation remains `.failed(.invalidationFailed)` and stays blocked after subsequent reconciliation and a newly constructed store.
    - Only a same-generation invalidation result plus same-generation fresh `.notInstalled`, or a same-generation verified reinstall plus fresh exact `.installed`, clears revocation.
    - Two overlapping invalidations have one newest generation; an older completion cannot overwrite or clear the newer operation's outcome.
  </behavior>
  <action>Add a monotonic per-pack operation generation inside the main-actor store. Every install or invalidate begins a new mutation generation; read-only reconciliation captures the current generation before awaiting and applies its result only if still current. Route all status writes through a generation check. Ordinary `reconcileAll` must respect a persisted revocation and never call `clearRevocation`. The originating invalidation may clear only after both its own result and a fresh same-generation status are `.notInstalled`; a verified reinstall may clear only after its own fresh exact installed reconciliation. Keep revocation persisted before provider I/O and retain it on every failure or stale completion per D-05, D-07, and D-09. Build `ControlledPackProvider` with explicit continuations/barriers so concurrency tests schedule each overlap deterministically without sleeps or timing assumptions.</action>
  <acceptance_criteria>
    - Deterministic tests pause reconciliation after provider entry, begin invalidation, resume the stale installed result, force invalidation failure, and prove the pack remains revoked/blocked through another reconciliation.
    - Deterministic overlapping-invalidation tests prove the older completion cannot clear or overwrite the newer generation.
    - Successful same-generation invalidation plus fresh absence clears revocation; successful verified reinstall plus fresh exact installed status also clears it.
    - No timed sleeps, random scheduling, raw provider errors, or private requirement values are used as concurrency evidence.
  </acceptance_criteria>
  <verify><automated>swift test --package-path packages/crosswake-shell-core-ios --filter PackStoreTests</automated></verify>
  <done>Per-pack generation authority prevents stale asynchronous work from re-enabling revoked media, including after failed or overlapping invalidation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| manifest pack references -> activation gate | Untrusted or incompatible references must not disappear before runtime selection. |
| provider await -> main-actor state | Async results may be stale when actor execution resumes. |
| revocation ledger -> reconciled inventory | Installed bytes cannot override newer trust revocation. |

## ASVS L1 / STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-161-30 | Elevation of Privilege | malformed/unknown pack reference | high | mitigate | Task 1 makes parsing total and route-tests every invalid class; any activation pass blocks completion. |
| T-161-31 | Tampering | stale reconciliation completion | high | mitigate | Task 2 checks captured generation before every post-await status write. |
| T-161-32 | Elevation of Privilege | persisted revocation | high | mitigate | Task 2 confines clearance to same-generation successful invalidation/fresh absence or verified reinstall. |
| T-161-33 | Denial of Service | overlapping operations | medium | mitigate | Deterministic barrier tests prove newest-operation authority and idempotent closed outcomes. |
| T-161-34 | Information Disclosure | invalid input/failure output | high | mitigate | Closed reasons only; tests assert no rejected reference or provider detail is echoed. |
| T-161-35 | Tampering | package supply chain | low | accept | No dependency or package-manager change is planned. |
</threat_model>

<verification>
Run the complete PackStore and activation conformance suites. High-severity malformed-reference, stale-result, revocation, or privacy failures block completion.
</verification>

<success_criteria>
- Invalid required-pack references can never disappear from the activation decision.
- Revocation survives failed/overlapping invalidation, stale reconciliation, later reconciliation, and relaunch.
- Only an explicitly authorized current operation can clear revocation.
- PACK-04 concurrency remains a visible unresolved probe assumption while its concrete required guarantees are deterministically tested.
</success_criteria>

<output>
Create `.planning/phases/161-ios-pronunciation-pack-seam/161-07-SUMMARY.md` when done.
</output>
