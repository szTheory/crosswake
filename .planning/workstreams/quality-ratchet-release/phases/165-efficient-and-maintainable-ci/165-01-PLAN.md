---
phase: 165-efficient-and-maintainable-ci
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/ci_monitor.cjs
  - test/crosswake/proof/phase165_evidence_test.exs
  - .planning/workstreams/quality-ratchet-release/phases/165-efficient-and-maintainable-ci/evidence/baseline.json
  - .planning/workstreams/quality-ratchet-release/phases/165-efficient-and-maintainable-ci/evidence/baseline.md
  - .planning/workstreams/quality-ratchet-release/phases/165-efficient-and-maintainable-ci/evidence/required-context-baseline.json
  - script/classify_ci_change.py
  - script/ci_docs_allowlist.json
  - script/ci_leaf_manifest.json
  - .github/workflows/crosswake-ci.yml
  - test/crosswake/proof/phase165_ci_policy_test.exs
autonomous: true
requirements: [CIP-05, CIP-06, CIP-07]
estimate:
  tokens: 42000
  raw_tokens: 42000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "A fresh pre-change current-main baseline exists before any PR trigger, runner, cache, or producer topology is changed, per D-25."
    - "Baseline evidence is canonical allowlisted JSON with generated Markdown, records sample count plus median/range, and represents exact per-job queue time as not_exposed, per D-26 through D-30."
    - "Before any topology mutation, a sanitized strict branch-protection snapshot freezes the exact sorted legacy required-context set and a source digest; every migration cohort must reject live drift from it per D-02 and D-10."
    - "A documentation-only comparison follows one production path from NUL-safe classification through one literal documentation-contract leaf to a checkout-free always-visible Crosswake CI umbrella, per D-08 and D-14 through D-18."
    - "[Edge CIP-05/adjacency] A change touching one allowlisted documentation path and one executable or otherwise unallowlisted path is full_proof."
    - "[Edge CIP-05/empty] Empty, null, malformed, or unavailable diff input is full_proof."
    - "[Edge CIP-05/ordering] Reordering the same validated diff records does not change classification, scheduled families, or explicit irrelevance."
    - "[Edge CIP-05/concurrency] Each PR run derives classification only from its validated base and synthetic merge SHA; concurrent runs share no mutable classifier state."
    - "[Edge CIP-06/boundary] One sample reports sample_count 1 with identical median/min/max; invalid or reversed timestamps are not_measured rather than coerced."
    - "[Edge CIP-06/adjacency] Equal valid timestamps produce a zero-duration observation; a completion timestamp earlier than start is not_measured."
    - "[Edge CIP-06/empty] An unavailable matched cohort or metric is retained as not_measured with a closed reason and is never replaced by an unmatched value."
    - "[Edge CIP-06/ordering] Reordering cohort samples produces byte-stable aggregate values and deterministic presentation order."
    - "[Edge CIP-06/precision] Durations use integer milliseconds without unsupported rounding claims, while exact job queue time remains not_exposed."
  assumptions:
    - statement: "[FLAGGED ASSUMPTION edge CIP-01/unclassified] The repository inventory used later for runner placement contains every PR proof job; execution must re-scan rather than trust a retained count."
      status: unresolved
  artifacts:
    - path: "scripts/ci_monitor.cjs"
      provides: "capture-evidence, validate-evidence, render-evidence, compare-evidence, and test-evidence commands"
    - path: ".planning/workstreams/quality-ratchet-release/phases/165-efficient-and-maintainable-ci/evidence/baseline.json"
      provides: "sanitized pre-mutation current-main and representative cohort evidence"
    - path: ".planning/workstreams/quality-ratchet-release/phases/165-efficient-and-maintainable-ci/evidence/required-context-baseline.json"
      provides: "sanitized pre-migration strictness, exact sorted required contexts, source SHA, and protection digest"
    - path: "script/classify_ci_change.py"
      provides: "first-party fail-closed documentation_only/full_proof classifier"
    - path: ".github/workflows/crosswake-ci.yml"
      provides: "initial pull_request-only classifier, documentation-contract leaf, and Crosswake CI umbrella tracer"
  key_links:
    - from: "scripts/ci_monitor.cjs"
      to: "evidence/baseline.json"
      via: "capture-evidence normalizes GitHub run/job/check data in memory before writing"
      pattern: "capture-evidence|baseline.json"
    - from: "evidence/required-context-baseline.json"
      to: "Plans 05-09 source-trigger migration"
      via: "verify-required-context-snapshot performs a fresh read-only strictness/set/digest comparison before each cohort removes a producer"
      pattern: "required_contexts|strict|source_digest"
    - from: "script/classify_ci_change.py"
      to: ".github/workflows/crosswake-ci.yml"
      via: "versioned classification output controls the literal documentation-contract leaf"
      pattern: "documentation_only|full_proof"
    - from: ".github/workflows/crosswake-ci.yml"
      to: "script/ci_leaf_manifest.json"
      via: "static needs and exact irrelevance reason for the tracer leaf"
      pattern: "merge-blocking-crosswake-ci|Crosswake CI"
  prohibitions:
    - requirement_id: CIP-05
      category: transparency
      statement: "MUST NOT describe a mixed, unvalidated, or unknown change as documentation-only or use classification to bypass an applicable first-adopter privacy check."
      status: unresolved
      flagged: true
    - requirement_id: CIP-06
      category: privacy
      statement: "MUST NOT retain raw API payloads, logs, actors, messages, runner identities, cache keys, credentials, adopter facts, account/device identifiers, or revealing links in canonical or rendered evidence."
      status: unresolved
      flagged: true
    - requirement_id: CIP-06
      category: transparency
      statement: "MUST NOT present workflow delay or dependency wait as exact job queue time, or call unmatched descriptive timing an improvement."
      status: unresolved
      flagged: true
    - requirement_id: CIP-07
      category: transparency
      statement: "MUST NOT erase literal proof names, logs, summaries, retained artifacts, or exact local remediation commands while consolidating orchestration."
      status: unresolved
      flagged: true
---

<objective>
Capture the reproducible pre-change efficiency baseline, then prove the smallest complete PR classification-to-leaf-to-umbrella path without disturbing Phase 164 authority.

Purpose: Preserve a truthful before side for CIP-06 and validate the end-to-end CI architecture before broad workflow mutation.
Output: Sanitized baseline evidence plus a production-quality documentation-only tracer in the new PR workflow.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/workstreams/quality-ratchet-release/STATE.md
@.planning/workstreams/quality-ratchet-release/ROADMAP.md
@.planning/workstreams/quality-ratchet-release/phases/165-efficient-and-maintainable-ci/165-CONTEXT.md
@.planning/workstreams/quality-ratchet-release/phases/165-efficient-and-maintainable-ci/165-RESEARCH.md
@.planning/workstreams/quality-ratchet-release/phases/165-efficient-and-maintainable-ci/165-VALIDATION.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Freeze and commit the sanitized pre-change baseline before topology mutation</name>
  <read_first>
    scripts/ci_monitor.cjs
    .planning/seeds/SEED-007-ci-cd-performance.md
    .planning/workstreams/quality-ratchet-release/phases/165-efficient-and-maintainable-ci/165-CONTEXT.md
    .planning/workstreams/quality-ratchet-release/phases/165-efficient-and-maintainable-ci/165-PATTERNS.md
    AGENTS.md
  </read_first>
  <precondition>`gh auth status` succeeds with read access to Actions and strict default-branch protection so the pre-mutation evidence and authority snapshot can be captured without changing remote state.</precondition>
  <files>scripts/ci_monitor.cjs, test/crosswake/proof/phase165_evidence_test.exs, .planning/workstreams/quality-ratchet-release/phases/165-efficient-and-maintainable-ci/evidence/baseline.json, .planning/workstreams/quality-ratchet-release/phases/165-efficient-and-maintainable-ci/evidence/baseline.md, .planning/workstreams/quality-ratchet-release/phases/165-efficient-and-maintainable-ci/evidence/required-context-baseline.json</files>
  <behavior>
    - Canonical records accept only schema_version, repository_sha, run_id, attempt, event, stable workflow/job/check identity, runner_class, timestamps, closed outcome/cache fields, cohort criteria, sample_count, and source_command per D-26 and D-30.
    - workflow_start_delay_ms is run_started_at minus created_at; job_execution_ms is completed_at minus started_at; exact job_queue_time is not_exposed per D-27.
    - Missing comparable cohorts and invalid timestamp pairs produce not_measured with a closed reason; input ordering does not alter aggregates.
    - Required-context snapshot accepts only schema_version, repository_sha, default_branch, strict, exact sorted required_contexts, captured_at, source_command, and source_digest; set order is canonical and any live drift fails.
  </behavior>
  <action>First write failing evidence-schema and metric tests, then extend the existing standard-library-only monitor dispatch with `capture-evidence`, `capture-required-context-snapshot`, `verify-required-context-snapshot`, `validate-evidence`, `render-evidence`, `compare-evidence`, and `test-evidence`. Normalize GitHub JSON in memory and never write the transport payload. Capture the fresh current-main inventory and any available matched documentation-only, full-proof executable, and main-bound pre-change cohorts before editing any workflow, classifier, cache, runner, or producer file. In the same pre-mutation commit, read strict default-branch protection and write `required-context-baseline.json` with the exact sorted required-context strings, strict boolean, repository SHA, default branch, source command, capture time, and a digest over the canonical strictness-plus-context-set payload; assert the observed set has 27 entries at this source snapshot but treat the stored set and digest, not the count, as authority. Store canonical JSON and generate Markdown from it; label SEED-007 historical only, exact job queue time `not_exposed`, unavailable comparisons `not_measured`, and timing descriptive per D-25 through D-30 and D-33.</action>
  <acceptance_criteria>
    - `baseline.json` exists before Task 2 changes `.github/workflows/crosswake-ci.yml` and validates through `node scripts/ci_monitor.cjs validate-evidence`.
    - `baseline.md` is generated solely from `baseline.json` and contains explicit sample counts, median/range, observed/not measured language, and exact source commands.
    - Schema fixtures containing an unknown field, actor, message, raw payload, log text, runner identity, cache key, token, account/device identifier, or revealing link are rejected without echoing their value.
    - Equal timestamps yield zero milliseconds; reversed or incomplete timestamp pairs yield not_measured; exact job queue time serializes as not_exposed.
    - `required-context-baseline.json` contains strict=true, 27 exact sorted unique context strings for the observed source state, and a reproducible digest; a one-string, strictness, order-normalization, or source-SHA negative fixture is rejected.
    - A fresh read-only `verify-required-context-snapshot --live` passes immediately after capture and performs no branch-protection write.
  </acceptance_criteria>
  <verify>
    <automated>node scripts/ci_monitor.cjs test-evidence &amp;&amp; mix test test/crosswake/proof/phase165_evidence_test.exs &amp;&amp; node scripts/ci_monitor.cjs validate-evidence .planning/workstreams/quality-ratchet-release/phases/165-efficient-and-maintainable-ci/evidence/baseline.json &amp;&amp; node scripts/ci_monitor.cjs verify-required-context-snapshot .planning/workstreams/quality-ratchet-release/phases/165-efficient-and-maintainable-ci/evidence/required-context-baseline.json --live</automated>
  </verify>
  <done>A privacy-safe, reproducible pre-change baseline and digest-bound exact legacy authority snapshot are committed before any topology mutation, and all five CIP-06 edge contracts are executable.</done>
</task>

<task type="tracer" tdd="true">
  <name>Task 2: Prove one documentation classification through one literal leaf and the checkout-free umbrella</name>
  <read_first>
    .github/workflows/contract-drift-gate.yml
    .github/workflows/phase69-proof.yml
    script/check_aggregator_result_semantics.py
    script/list_merge_blocking_checks.py
    lib/crosswake/planning/first_adopter_context.ex
    brandbook/BRAND-SPEC.md
  </read_first>
  <files>script/classify_ci_change.py, script/ci_docs_allowlist.json, script/ci_leaf_manifest.json, .github/workflows/crosswake-ci.yml, test/crosswake/proof/phase165_ci_policy_test.exs</files>
  <behavior>
    - A validated documentation-only diff emits versioned `documentation_only`; every invalid, mixed, empty, unknown, or unallowlisted case emits `full_proof` per D-14 through D-16.
    - The classifier job checks out the synthetic merge with `fetch-depth: 0`, verifies the event base and checked-out merge commit objects, and falls back to full_proof if full-history acquisition or validation fails.
    - One literal `documentation-contracts` leaf runs for documentation_only and reports its exact remediation command; executable proof is explicitly irrelevant for this tracer case per D-18 and D-32.
    - Job `merge-blocking-crosswake-ci`, displayed as `Crosswake CI`, runs with `if: always()`, static needs equal to `proof_leaves union required_control_nodes`, bounded timeout, no checkout/setup/install, and fails every missing, skipped, or non-success control node plus every missing or unexplained proof result per D-07 through D-09.
  </behavior>
  <action>After the baseline commit, add the first-party NUL-safe classifier using argv-based `git diff --name-status -z -M -C`, explicit rename/copy arity, both old/new paths, deleted paths, resolvable base and synthetic-merge SHAs, and the closed output values `documentation_only` and `full_proof`. The classifier job must use pinned checkout with `fetch-depth: 0`, validate `pull_request.base.sha` and checked-out synthetic merge `HEAD` as commit objects, and pass only those validated object IDs to the classifier; checkout/fetch/object failure visibly returns full_proof. Lock `ci_docs_allowlist.json` to the exact resolved research set: root `README.md`, `CONTRIBUTING.md`, `SETUP.md`, `examples/QUICK_START.md`, `guides/**/*.md`, `brandbook/**/*.md`, `.planning/**/*.md`, and `docs/**/*.md` except the publish runbook, port registry, and generated contract snippet; all other paths are full proof. Define manifest `proof_leaves` with `documentation-contracts`, `required_control_nodes` with `classify-change`, and an empty `legacy_compatibility_contexts`; enforce `umbrella static needs = proof_leaves union required_control_nodes`. Create `Crosswake CI` as a pull_request-only workflow with PR-number concurrency and `cancel-in-progress: false`; run classification and the one literal leaf, then conclude in `merge-blocking-crosswake-ci` using only runner-built-in facilities over `toJSON(needs)`. Add an isolated bare-remote integration fixture that creates base/head/synthetic-merge objects, begins from a shallow checkout, performs the workflow's full-history acquisition shape, and proves an allowlisted PR diff reaches documentation_only; omit the acquisition and prove the same fixture falls back to full_proof. Leave all Phase 164 workflows and required contexts unchanged so this tracer is additive and non-authoritative. Summaries must follow D-31 through D-33.</action>
  <acceptance_criteria>
    - Workflow triggers only `pull_request`, has no workflow-level path filter, and its concurrency group includes the repository identity plus pull-request number with `cancel-in-progress: false`.
    - Classifier fixtures cover additions, modifications, deletions, renames, copies, newline/NUL-adjacent path data, glob characters, record reordering, mixed paths, zero/unresolvable SHAs, shallow history, unknown statuses, truncated records, and empty output.
    - The classifier job uses `actions/checkout` with `fetch-depth: 0`; the bare-remote workflow-shape fixture proves an allowlisted base-to-synthetic-merge diff becomes documentation_only only after both objects are present, while acquisition/object failures become full_proof.
    - The umbrella has literal static needs exactly equal to `proof_leaves union required_control_nodes` (`documentation-contracts` plus control node `classify-change` in this tracer), uses `if: always()`, and contains no checkout, local action, repository script, dependency install, or PR-code execution.
    - A missing, skipped, failed, cancelled, timed-out, stale, action-required, or unknown `classify-change` control result fails the umbrella and can never be classifier-issued irrelevant.
    - Existing merge-blocking producer inventory and live required-context policy remain unchanged by the additive tracer.
  </acceptance_criteria>
  <verify>
    <automated>python3 script/classify_ci_change.py --self-test &amp;&amp; mix test test/crosswake/proof/phase165_ci_policy_test.exs &amp;&amp; actionlint .github/workflows/crosswake-ci.yml &amp;&amp; python3 script/list_merge_blocking_checks.py --emitters &gt;/dev/null &amp;&amp; script/check_required_checks_registered.sh --local-only</automated>
  </verify>
  <done>The committed baseline precedes a production-quality documentation-only PR tracer, and the new umbrella is visible but does not yet replace any Phase 164 authority.</done>
</task>

</tasks>

<threat_model>
Security posture: OWASP ASVS Level 1; every high or critical threat blocks execution until its listed mitigation passes.

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Git history → classifier | Untrusted path bytes, statuses, and SHAs enter first-party policy code. |
| GitHub REST → evidence files | Untrusted API JSON is reduced to an allowlisted aggregate record. |
| leaf results → umbrella | Missing or unexplained results must not become a passing required verdict. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-165-03 | Tampering / Elevation | `classify_ci_change.py` | high | mitigate | NUL-safe argv Git invocation, closed status arity, non-echoing full-proof fallback, adversarial fixtures. |
| T-165-04 | Tampering | `merge-blocking-crosswake-ci` | high | mitigate | Static manifest/needs parity and fail-closed missing, unknown, and unexplained-skip semantics. |
| T-165-07 | Information disclosure | baseline evidence | high | mitigate | Allowlisted schema, in-memory normalization, forbidden-field negative fixtures, no raw payload persistence. |
</threat_model>

## Artifacts this phase produces

This plan establishes the complete phase surface inventory. New files: `.github/workflows/crosswake-ci.yml`, `.github/workflows/cancel-obsolete-crosswake-ci.yml`, `.github/actions/setup-android-jvm/action.yml`, `script/classify_ci_change.py`, `script/ci_docs_allowlist.json`, `script/ci_leaf_manifest.json`, `script/check_ci_leaf_manifest.py`, `script/select_obsolete_ci_runs.py`, `script/required_check_policy.json`, `script/check_phase165_efficient_ci.sh`, `test/crosswake/proof/phase165_ci_policy_test.exs`, `test/crosswake/proof/phase165_ci_integrity_test.exs`, `test/crosswake/proof/phase165_evidence_test.exs`, `test/fixtures/ci/classifier/**`, `test/fixtures/ci/cancellation/**`, and `.planning/workstreams/quality-ratchet-release/phases/165-efficient-and-maintainable-ci/evidence/{baseline.json,baseline.md,required-context-baseline.json,remote-default-source.json,live-observation.json,required-context-retirement.json,after.json,comparison.md}`. New monitor subcommands are `capture-evidence`, `capture-required-context-snapshot`, `verify-required-context-snapshot`, `validate-evidence`, `render-evidence`, `compare-evidence`, `test-evidence`, `verify-remote-default-source`, and `probe-phase165`. Classifier surface values are `documentation_only` and `full_proof`; fields are `schema_version`, `classification`, `reason`, `scheduled_families`, and `irrelevant_leaves`. Manifest top-level sets are `proof_leaves`, `required_control_nodes`, and `legacy_compatibility_contexts`; umbrella static needs are exactly `proof_leaves union required_control_nodes`, while compatibility jobs remain outside that union. Evidence fields are the allowlisted fields named in Task 1. Workflow surfaces are `Crosswake CI`, job ID `merge-blocking-crosswake-ci`, literal manifest leaf IDs migrated in Plans 05-09, and the trusted controller in Plan 03. The Android action inputs are `working-directory`, `java-version`, and `gradle-version`; the Elixir composite retains and exposes only its existing safe cache-hit outputs. Required-check mutation flags are `--policy`, `--mode add|retire`, `--dry-run`, and `--apply`; the target required context is the observed display name for job ID `merge-blocking-crosswake-ci`, expected to be `Crosswake CI` and frozen only after the source-bound live emitted-name probe.

## Multi-Source Coverage Audit

| Source | IDs/items | Plans | Status |
|--------|-----------|-------|--------|
| GOAL | Faster, cheaper CI without losing named proof or authority | 01-12 | COVERED |
| REQ | CIP-01..CIP-07 | 01-12 | COVERED |
| RESEARCH | Baseline before mutation; policy contracts; trusted cancellation; runner/cache portability; one PR workflow; green-first cutover; matched evidence | 01-12 | COVERED |
| CONTEXT | D-01..D-33 | 01-12 | COVERED; each implementing task cites its decision IDs |

<verification>
Run both task commands in order and confirm `git log --format=%s -2` shows the baseline evidence commit before the tracer workflow commit.
</verification>

<success_criteria>
The baseline is immutable pre-change evidence, the docs-only tracer works end to end, and existing required producers/protection remain unchanged.
</success_criteria>

<output>
Create `.planning/workstreams/quality-ratchet-release/phases/165-efficient-and-maintainable-ci/165-01-SUMMARY.md` when done.
</output>
