---
phase: 165-efficient-and-maintainable-ci
plan: 10
type: execute
wave: 10
depends_on: [165-03, 165-09]
files_modified:
  - scripts/ci_monitor.cjs
  - .planning/workstreams/quality-ratchet-release/phases/165-efficient-and-maintainable-ci/evidence/live-observation.json
  - script/required_check_policy.json
  - script/check_required_checks_registered.sh
  - script/register_required_checks.sh
  - .planning/workstreams/quality-ratchet-release/phases/165-efficient-and-maintainable-ci/evidence/required-context-retirement.json
autonomous: true
requirements: [CIP-02, CIP-04, CIP-05, CIP-07]
estimate:
  tokens: 44000
  raw_tokens: 44000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "Live documentation-only and executable PR probes emit one always-visible Crosswake CI result, the exact expected leaf schedule, and no unexplained skipped/missing leaf."
    - "Live requested-event timing proves the trusted controller cancels a strict lower run ID for the same PR/workflow, never the newer run, and records observed timing before cancellation authority is claimed per D-05 and D-06."
    - "Crosswake CI is registered additively while strict branch protection retains every legacy compatibility context; both authorities are re-read and verified per D-10."
    - "The exact legacy-retirement diff is generated and validated without applying it; Plan 11 is the only approval boundary."
  artifacts:
    - path: "script/required_check_policy.json"
      provides: "exact additive and retirement context sets plus expected strictness"
    - path: "evidence/live-observation.json"
      provides: "sanitized docs/full/cancellation/check-name live probe outcomes"
    - path: "evidence/required-context-retirement.json"
      provides: "exact old/new strict branch-protection dry-run diff"
  key_links:
    - from: "script/required_check_policy.json"
      to: "script/register_required_checks.sh"
      via: "--mode add registers Crosswake CI without removing legacy contexts; --mode retire remains dry-run"
      pattern: "Crosswake CI|legacy_contexts|target_contexts"
  prohibitions:
    - requirement_id: CIP-02
      category: safety
      statement: "MUST NOT claim obsolete-run cancellation authority from static fixtures alone; the requested-event timing and inversion behavior must be observed live."
      status: unresolved
      flagged: true
    - requirement_id: CIP-07
      category: transparency
      statement: "MUST NOT retire any legacy required context in the same unobserved operation that first adds Crosswake CI authority."
      status: unresolved
      flagged: true
---

<objective>
Observe the complete topology live, prove monotonic cancellation timing, register Crosswake CI additively, and stop at an exact retirement diff.

Purpose: Establish green-first dual authority before the irreversible external trust decision.
Output: Sanitized live probes, additive branch-protection state, and a verified unapplied retirement proposal.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/quality-ratchet-release/phases/165-efficient-and-maintainable-ci/165-03-SUMMARY.md
@.planning/workstreams/quality-ratchet-release/phases/165-efficient-and-maintainable-ci/165-09-SUMMARY.md
@.planning/workstreams/quality-ratchet-release/phases/165-efficient-and-maintainable-ci/165-CONTEXT.md
@.github/workflows/required-checks-audit.yml
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Run live docs-only, full-proof, emitted-name, and monotonic cancellation probes</name>
  <precondition>The Crosswake CI and trusted requested-run controller workflow revisions are present on the repository default branch, `gh auth status` succeeds with read/write Actions access, and no release/publish/recovery run is selected by the probe.</precondition>
  <read_first>
    scripts/ci_monitor.cjs
    .github/workflows/crosswake-ci.yml
    .github/workflows/cancel-obsolete-crosswake-ci.yml
    script/ci_leaf_manifest.json
    script/select_obsolete_ci_runs.py
  </read_first>
  <files>scripts/ci_monitor.cjs, .planning/workstreams/quality-ratchet-release/phases/165-efficient-and-maintainable-ci/evidence/live-observation.json</files>
  <behavior>
    - Temporary docs-only and executable PR probes assert exact classifier, leaf, umbrella, check-name, runner, timeout, and summary behavior and clean up their branches/PRs after evidence capture.
    - Two same-PR Crosswake CI requests observe workflow_run: requested timing, cancel only the lower ID, and prove the greatest ID completes or remains authoritative.
  </behavior>
  <action>Add bounded `probe-phase165` monitor orchestration over existing `gh` transport, using sanitized in-memory records and cleanup traps. Create one docs-only probe and one executable probe whose changes are non-sensitive fixtures owned by this phase; assert the docs probe schedules only affected D-18 leaves plus umbrella and the executable probe schedules the complete manifest. Run a same-PR two-update cancellation sequence, record requested-controller start relative to runner consumption, strict lower-ID cancellation, newer-run survival, bounded cancel/poll/force behavior, and the exact emitted check display name for job ID `merge-blocking-crosswake-ci`. If requested timing, emitted name, or inversion cannot be proven, stop without claiming controller or required-context authority. Persist only the allowlisted `live-observation.json` per D-05, D-25 through D-30.</action>
  <acceptance_criteria>
    - Live docs-only probe observes Crosswake CI and exact bounded documentation/privacy/planning leaves, with zero root-suite/browser/Android/Apple/unrelated-package jobs scheduled.
    - Live executable probe observes every manifest leaf and a successful checkout-free umbrella.
    - Live cancellation probe records lower ID cancelled, newer ID not cancelled, same PR/workflow identity, requested-event timing, and bounded controller action.
    - Evidence records the exact emitted umbrella display/context name and contains no forbidden D-30 fields.
  </acceptance_criteria>
  <verify>
    <automated>node scripts/ci_monitor.cjs probe-phase165 --assert-cleanup --output .planning/workstreams/quality-ratchet-release/phases/165-efficient-and-maintainable-ci/evidence/live-observation.json &amp;&amp; node scripts/ci_monitor.cjs validate-evidence .planning/workstreams/quality-ratchet-release/phases/165-efficient-and-maintainable-ci/evidence/live-observation.json &amp;&amp; python3 script/select_obsolete_ci_runs.py --self-test</automated>
  </verify>
  <done>Live evidence, not static inference, proves docs-only visibility, full-proof completeness, the emitted context name, and monotonic requested-run cancellation timing.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Register the observed Crosswake CI context additively and verify dual authority</name>
  <precondition>Task 1 records a successful umbrella on representative PRs and an exact emitted context name; strict branch protection is currently readable and all legacy required contexts have exactly one compatibility producer.</precondition>
  <read_first>
    script/check_required_checks_registered.sh
    script/register_required_checks.sh
    script/list_merge_blocking_checks.py
    .planning/workstreams/quality-ratchet-release/phases/164-dependency-security-and-gate-authority/164-CONTEXT.md
    .planning/workstreams/quality-ratchet-release/phases/165-efficient-and-maintainable-ci/evidence/live-observation.json
  </read_first>
  <files>script/required_check_policy.json, script/check_required_checks_registered.sh, script/register_required_checks.sh</files>
  <reversibility rating="costly">D-07 makes the stable umbrella authority costly to change because any replacement requires another green-first producer/protection migration.</reversibility>
  <behavior>
    - `--mode add --apply` adds only the observed Crosswake CI context, preserves strict=true and every legacy required context, and refuses if local/live producer or green-first prerequisites fail.
    - Post-apply audit reads live protection and proves exact dual authority in both directions.
  </behavior>
  <action>Create an exact policy file containing observed umbrella context, legacy context set derived from current protection, target umbrella-only set, and strictness. Extend the existing audit/registrar rather than adding another writer. Support `--policy`, `--mode add|retire`, `--dry-run`, and `--apply`; default to dry-run, require explicit mode, reject unknown/duplicate contexts, preserve app_id when present, verify unique producers and green observed umbrella, and perform additive apply only in this task. Re-read protection after apply and fail unless strict remains true and the live set equals legacy union umbrella per D-07 and D-10.</action>
  <acceptance_criteria>
    - Additive dry-run diff contains exactly one added observed umbrella context and zero removals/strictness changes.
    - Apply refuses if umbrella observation is absent/red, producer count is not one, current protection differs from policy source, or credentials are unavailable.
    - Post-apply live audit proves strict=true, umbrella present, every legacy context present, and each context has one producer.
  </acceptance_criteria>
  <verify>
    <automated>script/register_required_checks.sh --policy script/required_check_policy.json --mode add --dry-run &amp;&amp; script/register_required_checks.sh --policy script/required_check_policy.json --mode add --apply &amp;&amp; script/check_required_checks_registered.sh --policy script/required_check_policy.json --state dual --live</automated>
  </verify>
  <done>Crosswake CI is required additively under strict protection while every legacy authority remains required and uniquely produced.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Generate the exact legacy-context retirement proposal without applying it</name>
  <precondition>Live audit proves the dual required-context state exactly.</precondition>
  <read_first>
    script/required_check_policy.json
    script/register_required_checks.sh
    script/check_required_checks_registered.sh
    .planning/workstreams/quality-ratchet-release/phases/165-efficient-and-maintainable-ci/evidence/live-observation.json
  </read_first>
  <files>script/required_check_policy.json, script/register_required_checks.sh, script/check_required_checks_registered.sh, .planning/workstreams/quality-ratchet-release/phases/165-efficient-and-maintainable-ci/evidence/required-context-retirement.json</files>
  <behavior>
    - Retirement dry-run preserves strict=true and the observed umbrella, removes exactly the legacy policy set, and applies no mutation.
    - Drift, unknown contexts, missing umbrella, missing producer, or non-strict protection blocks proposal generation.
  </behavior>
  <action>Run the registrar's retire mode in dry-run only and write a canonical allowlisted proposal with repository/default branch, source protection digest, strictness before/after, exact sorted added/removed/retained context names, producer verification result, live observation reference, generated timestamp, and apply command. Reject any diff that adds a context, removes Crosswake CI, changes strictness, includes an unknown context, or does not exactly match the policy's legacy set. Do not accept or apply the retirement in this plan; D-10 approval belongs to Plan 11.</action>
  <acceptance_criteria>
    - Proposal shows strict true before/after, Crosswake CI retained, zero additions, and removal set exactly equal to `legacy_contexts`.
    - `--mode retire --dry-run` leaves live branch protection byte/semantically unchanged as confirmed by before/after reads.
    - Proposal contains no credentials, tokens, actor, raw API payload, or revealing repository/adopter data beyond safe repository-relative authority identifiers.
  </acceptance_criteria>
  <verify>
    <automated>script/register_required_checks.sh --policy script/required_check_policy.json --mode retire --dry-run --output .planning/workstreams/quality-ratchet-release/phases/165-efficient-and-maintainable-ci/evidence/required-context-retirement.json &amp;&amp; script/check_required_checks_registered.sh --policy script/required_check_policy.json --state dual --live</automated>
  </verify>
  <done>An exact, drift-bound, unapplied retirement proposal is ready for the sole human trust decision.</done>
</task>

</tasks>

<threat_model>
Security posture: OWASP ASVS Level 1; every high or critical threat blocks execution until its listed mitigation passes.

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| live Actions/protection API → evidence/policy | External mutable state must be validated and normalized. |
| registrar → branch protection | A write can create a merge bypass or permanent pending gate. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-165-02 | Tampering / Denial of service | live cancellation | high | mitigate | Same-PR strict lower-ID live probe and newer-run survival assertion. |
| T-165-07 | Information disclosure | live evidence | high | mitigate | Allowlisted normalization and forbidden-field validation. |
| T-165-08 | Elevation of privilege | additive protection write | high | mitigate | Green-first one-context add, exact dual-state verification, retire dry-run only. |
</threat_model>

## Artifacts this phase produces

New policy file `script/required_check_policy.json` fields: `schema_version`, `strict`, `umbrella_context`, `legacy_contexts`, `dual_contexts`, `target_contexts`, and `source_digest`. Registrar/audit CLI flags: `--policy`, `--mode add|retire`, `--state dual|target`, `--dry-run`, `--apply`, `--live`, and `--output`. Monitor subcommand `probe-phase165` adds `--assert-cleanup` and `--output`. Evidence files are `live-observation.json` and `required-context-retirement.json` with only allowlisted aggregate/protection fields.

<verification>
Run all three commands in order; finish with live dual authority and an unapplied retirement proposal.
</verification>

<success_criteria>
Live behavior is proven, cancellation timing is observed, the umbrella is additively required, legacy contexts remain required, and retirement has not been applied.
</success_criteria>

<output>
Create `.planning/workstreams/quality-ratchet-release/phases/165-efficient-and-maintainable-ci/165-10-SUMMARY.md` when done.
</output>
