---
phase: 165-efficient-and-maintainable-ci
plan: 09
type: execute
wave: 9
depends_on: [165-08]
files_modified:
  - .github/workflows/crosswake-ci.yml
  - .github/workflows/brandbook-verify.yml
  - .github/workflows/collateral-guard.yml
  - .github/workflows/hex-page-proof.yml
  - .github/workflows/release-as-staleness-gate.yml
  - script/ci_leaf_manifest.json
  - script/check_ci_leaf_manifest.py
  - script/list_merge_blocking_checks.py
  - test/crosswake/proof/phase165_ci_integrity_test.exs
  - test/crosswake/proof/phase165_ci_policy_test.exs
  - script/check_phase165_efficient_ci.sh
  - .planning/workstreams/quality-ratchet-release/phases/165-efficient-and-maintainable-ci/165-VALIDATION.md
autonomous: true
requirements: [CIP-02, CIP-04, CIP-05, CIP-07]
estimate:
  tokens: 46000
  raw_tokens: 46000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "One pull_request-only Crosswake CI workflow now owns recurring product proof; no additional unowned event authority is added per D-03, and release, publish/recovery, scheduled audit, manual advisory, and justified main-bound workflows remain separate per D-01, D-02, D-11, and D-24."
    - "A documentation-only PR runs classification, affected privacy/public-doc/planning leaves, and the always-visible umbrella, but no unrelated executable family per D-17 and D-18."
    - "Brand structural proof remains a required leaf, while literal brand visual proof remains advisory and cannot enter proof_leaves, required_control_nodes, umbrella needs, or compatibility authority."
    - "Every summary states classification/reason, scheduled and irrelevant families, expected/observed leaf counts, and an exact remediation command using D-31 through D-33 voice."
  artifacts:
    - path: "script/check_phase165_efficient_ci.sh"
      provides: "one recurring aggregate policy/integrity/evidence gate"
    - path: ".github/workflows/crosswake-ci.yml"
      provides: "complete literal PR proof graph and Crosswake CI umbrella"
  key_links:
    - from: "script/check_phase165_efficient_ci.sh"
      to: "script/ci_leaf_manifest.json"
      via: "composes classifier, manifest, producer, runner, cache, timeout, cancellation, and evidence contracts"
      pattern: "check_ci_leaf_manifest|phase165_ci_integrity"
  prohibitions:
    - requirement_id: CIP-02
      category: safety
      statement: "MUST NOT duplicate complete PR proof on landed main or fold release/recovery/publish/scheduled trust into ordinary PR concurrency."
      status: unresolved
      flagged: true
    - requirement_id: CIP-05
      category: transparency
      statement: "MUST NOT make a required result disappear through workflow-level path filters or treat an unexplained skipped leaf as neutral."
      status: unresolved
      flagged: true
---

<objective>
Finish the single PR orchestration surface and create one recurring structural gate before live authority changes.

Purpose: Ensure every ordinary PR proof has one readable owner and every preserved exception is explicit.
Output: Complete Crosswake CI graph, removed trigger duplication, and one local/CI aggregate verifier.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/quality-ratchet-release/phases/165-efficient-and-maintainable-ci/165-08-SUMMARY.md
@.planning/workstreams/quality-ratchet-release/phases/165-efficient-and-maintainable-ci/165-CONTEXT.md
@.planning/workstreams/quality-ratchet-release/phases/165-efficient-and-maintainable-ci/165-VALIDATION.md
@.planning/workstreams/quality-ratchet-release/phases/165-efficient-and-maintainable-ci/evidence/required-context-baseline.json
@brandbook/BRAND-SPEC.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Move brand, collateral, Hex-page, and release-staleness PR proof</name>
  <precondition>A fresh read-only required-context snapshot verification exactly matches the Plan 01 strictness, sorted context set, source SHA, and digest.</precondition>
  <read_first>
    .github/workflows/brandbook-verify.yml
    .github/workflows/collateral-guard.yml
    .github/workflows/hex-page-proof.yml
    .github/workflows/release-as-staleness-gate.yml
    .github/workflows/crosswake-ci.yml
    .planning/workstreams/quality-ratchet-release/phases/165-efficient-and-maintainable-ci/evidence/required-context-baseline.json
  </read_first>
  <files>.github/workflows/crosswake-ci.yml, .github/workflows/brandbook-verify.yml, .github/workflows/collateral-guard.yml, .github/workflows/hex-page-proof.yml, .github/workflows/release-as-staleness-gate.yml</files>
  <behavior>
    - Required brand-structural/public-doc/collateral/release-staleness proof appears once under Crosswake CI with exact source commands and literal names.
    - Literal `brand-visual` remains an advisory sibling: it is absent from `proof_leaves`, `required_control_nodes`, umbrella `needs`, and every compatibility projection; a red visual result remains visible as red advisory evidence and is not converted to success.
    - Focused documentation-only scheduling uses affected-family ownership; release/publish automation itself remains untouched and separate.
  </behavior>
  <action>Before any source-trigger edit, run the live snapshot verifier and stop on drift. Move the required ordinary PR proof families into Crosswake CI as literal jobs `brand-structural`, `collateral-binaries-guard`, `hex-page-proof`, and `release-as-staleness-proof`; move `brand-visual` as a separate literal advisory sibling whose failure remains visibly red but cannot affect or masquerade as the required Crosswake CI conclusion. Keep `brand-visual` outside `proof_leaves`, `required_control_nodes`, umbrella `needs`, and all compatibility rows; do not use `continue-on-error` or a success-coercing wrapper. Remove equivalent pull_request/generic push execution after required-leaf parity and advisory-visibility checks pass. Add `compat-release-as-staleness` only for frozen snapshot context `merge-blocking-release-as-staleness`. Delete the four source workflow files because their push/manual variants repeat the same ordinary proof and own no distinct secret, permission, cancellation, or approval authority. Keep Release Please, Hex publish, iOS recovery/backfill, required-check audit, and native advisory workflows outside Crosswake CI with prior permissions/cancellation. Add no extra event authority per D-03. Wire affected public docs to focused ExDoc/guide/support/capability/brand checks; mark all unrelated executable leaves exactly irrelevant per D-02, D-10, D-11, D-17, D-18, D-24, and D-31 through D-33.</action>
  <acceptance_criteria>
    - All ordinary PR/push product-proof workflows from the live inventory have one PR producer in Crosswake CI or a documented distinct non-PR boundary.
    - No required workflow uses workflow-level paths/paths-ignore.
    - `brand-structural` is a manifest proof leaf and static umbrella need; `brand-visual` is literal and visible but absent from manifest authority, umbrella needs, and compatibility rows, and a failing visual run remains red advisory evidence.
    - Release Please and publish/recovery workflow triggers, permissions, secrets, concurrency, environments, and approvals are unchanged.
    - Frozen snapshot verification passes immediately before all four source trigger removals; live drift leaves the original producers intact.
  </acceptance_criteria>
  <verify>
    <automated>test ! -e .github/workflows/brandbook-verify.yml &amp;&amp; test ! -e .github/workflows/collateral-guard.yml &amp;&amp; test ! -e .github/workflows/hex-page-proof.yml &amp;&amp; test ! -e .github/workflows/release-as-staleness-gate.yml &amp;&amp; actionlint .github/workflows/crosswake-ci.yml &amp;&amp; mix test test/crosswake/proof/phase165_ci_integrity_test.exs --only triggers --only release_trust</automated>
  </verify>
  <done>Recurring PR proof has one owner and release/recovery/schedule/manual authority remains separate.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Seal complete manifest, static-needs, trigger, permission, and summary parity</name>
  <read_first>
    .github/workflows/crosswake-ci.yml
    script/ci_leaf_manifest.json
    script/check_ci_leaf_manifest.py
    script/list_merge_blocking_checks.py
    test/crosswake/proof/phase165_ci_integrity_test.exs
  </read_first>
  <files>.github/workflows/crosswake-ci.yml, script/ci_leaf_manifest.json, script/check_ci_leaf_manifest.py, script/list_merge_blocking_checks.py, test/crosswake/proof/phase165_ci_integrity_test.exs</files>
  <behavior>
    - Complete `proof_leaves union required_control_nodes` equals umbrella static needs; proof display names/remediation/irrelevance, control-node success requirements, and producers are exact.
    - Crosswake CI has read-only PR permissions, PR-number concurrency, cancel-in-progress false, bounded timeouts, and no workflow-level path filters.
  </behavior>
  <action>Regenerate the complete manifest from explicit reviewed rows, never from dynamic matrices. Keep `classify-change` as the sole `required_control_nodes` member; enumerate every required proof job under `proof_leaves`, including `brand-structural` but excluding advisory `brand-visual`; enumerate all and only exact strings from Plan 01 `required-context-baseline.json` under `legacy_compatibility_contexts`. Update umbrella static needs to equal `proof_leaves union required_control_nodes`, include exact proof irrelevance reasons, require success for the classifier control node, and use the closed result vocabulary. Extend structural tests to inventory every workflow and reject equivalent PR/push duplication, pure-work macOS, missing timeout, broad PR permissions, checkout in the umbrella, hidden remediation, unstable/generated names, protected trust-boundary collapse, a compatibility or advisory job in umbrella needs, a control node with irrelevance, or any compatibility projection of `brand-visual`. Assert `brand-visual` remains a literal advisory job with no success coercion and no path into required authority. Verify summary text includes D-32 fields and D-33 units/observed/not measured wording.</action>
  <acceptance_criteria>
    - `proof_leaves union required_control_nodes` equals umbrella static needs; `required_control_nodes` is exactly `classify-change`; `legacy_compatibility_contexts` equals the frozen exact required-context set, each needs the umbrella or one exact leaf, and direct one-item mutations fail.
    - `brand-structural` is required; `brand-visual` appears literally outside all three manifest authority sets, outside umbrella needs, outside compatibility dependencies, and without success coercion.
    - Workflow inventory reports one recurring PR entry, zero equivalent full-proof push/main copies, and explicit separate release/publish/recovery/schedule/manual/advisory workflows.
    - Umbrella performs no checkout, local action, dependency install, cache restore, or untrusted code execution.
  </acceptance_criteria>
  <verify>
    <automated>python3 script/check_ci_leaf_manifest.py --self-test &amp;&amp; python3 script/list_merge_blocking_checks.py --emitters &gt;/dev/null &amp;&amp; mix test test/crosswake/proof/phase165_ci_integrity_test.exs &amp;&amp; actionlint .github/workflows/*.yml</automated>
  </verify>
  <done>The complete PR graph is exact, least-privilege, bounded, readable, and fail closed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Publish one recurring Phase 165 contract gate</name>
  <read_first>
    script/check_phase164_dependency_security_and_gate_authority.sh
    test/crosswake/proof/phase165_ci_policy_test.exs
    test/crosswake/proof/phase165_ci_integrity_test.exs
    test/crosswake/proof/phase165_evidence_test.exs
    .planning/workstreams/quality-ratchet-release/phases/165-efficient-and-maintainable-ci/165-VALIDATION.md
  </read_first>
  <files>script/check_phase165_efficient_ci.sh, test/crosswake/proof/phase165_ci_policy_test.exs, test/crosswake/proof/phase165_ci_integrity_test.exs, test/crosswake/proof/phase165_evidence_test.exs, .planning/workstreams/quality-ratchet-release/phases/165-efficient-and-maintainable-ci/165-VALIDATION.md</files>
  <behavior>
    - One command composes stable classifier, manifest, producer, aggregator, cancellation, runner, cache, timeout, permission, trigger, and evidence-schema checks.
    - It excludes live timing thresholds and one-time branch-protection mutation from recurring CI.
  </behavior>
  <action>Create `script/check_phase165_efficient_ci.sh` in the Phase 164 aggregate-gate style with fail-fast actionable diagnostics, explicit tool checks, bounded commands, and no credential requirement for local mode. Compose only recurring contracts per D-28 and keep live cohort collection, timing comparisons, and branch-protection mutation as phase evidence/operations. Update validation frontmatter to Nyquist compliant only after every mapped command exists and passes.</action>
  <acceptance_criteria>
    - Aggregate script exits zero only when all Phase 165 self-tests, ExUnit contracts, actionlint, local producer, and local required-context audits pass.
    - It contains no hosted-runner timing threshold, new telemetry surface, live branch-protection write, or proof retry.
    - Validation map has an existing automated command for CIP-01 through CIP-07.
  </acceptance_criteria>
  <verify>
    <automated>script/check_phase165_efficient_ci.sh</automated>
  </verify>
  <done>One bounded recurring command protects the full optimized topology without promoting descriptive timing or one-time migration into CI.</done>
</task>

</tasks>

<threat_model>
Security posture: OWASP ASVS Level 1; every high or critical threat blocks execution until its listed mitigation passes.

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| untrusted PR → consolidated workflow | One workflow runs many proof families but must remain read-only. |
| required umbrella → branch protection | A missing/neutralized leaf could bypass all named proof. |
| release/recovery → optimization scope | Sensitive permissions must not be absorbed into ordinary CI. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-165-05 | Elevation of privilege | Crosswake CI | high | mitigate | Read-only PR permissions; no release/recovery secrets; controller isolated. |
| T-165-04 | Tampering | complete umbrella | high | mitigate | Exact static graph, closed results, no checkout/setup in conclusion. |
| T-165-14 | Elevation of privilege | release/recovery workflows | high | mitigate | Structural byte/semantic trust assertions and separate triggers/permissions. |
</threat_model>

## Artifacts this phase produces

New recurring CLI surface: `script/check_phase165_efficient_ci.sh`. Complete workflow surface: workflow `Crosswake CI`; required control node `classify-change`; job ID `merge-blocking-crosswake-ci` displayed `Crosswake CI`; required proof leaf job IDs `documentation-contracts`, `brand-structural`, `collateral-binaries-guard`, `guard-01-contract-drift-test`, `guard-02-generate-and-diff`, `proof-aggregator-negative-control`, `proof-dependency-security`, `proof-requires-example-host`, exact source-qualified Phase 5/10/18/23/34/41/43/45/48/52/58/67/69/70/71/73/74/75/79/96 leaves listed by Plans 05-08, Phase 130/132 core-hermetic and companion-engine leaves, `android-package-unit`, `ios-package-unit`, `android-generated-shell-unit`, `guard-01-e2e-honesty`, `guard-02-prod-route-absence`, `e2e-proof`, `route-tour-proof`, `ios-mirror-parity-proof`, `hex-page-proof`, and `release-as-staleness-proof`. Literal job `brand-visual` remains advisory and is intentionally outside `proof_leaves`, `required_control_nodes`, umbrella static needs, and compatibility projections; its red result stays visible and is never coerced to success. Umbrella static needs are exactly the required proof set union `classify-change`. The exact purpose display, family, remediation, and irrelevance for every required proof job is enumerated in `proof_leaves`; `classify-change` requires success and has no irrelevance. `legacy_compatibility_contexts` enumerates all 27 exact context strings captured by Plan 01 and emits each from a checkout-free compatibility job until Plan 12. Plan 09 deletes `brandbook-verify.yml`, `collateral-guard.yml`, `hex-page-proof.yml`, and `release-as-staleness-gate.yml` after parity; it introduces no new command beyond the existing brand structural/visual, collateral guard, Hex page, and release-staleness command sequences. Classifier outputs are `documentation_only|full_proof`; no workflow-level path filter, new telemetry surface, external API, release command, or product surface is added.

Scope rationale: this 12-file plan is atomic because it closes the last source-trigger cohort and immediately seals the complete workflow/manifest/control/compatibility graph plus its recurring aggregate gate. Splitting the source removal from the complete parity gate would leave an intermediate final topology without its recurring contract. Each task has a distinct five-file-or-smaller working set despite shared verifier files; with zero calibration samples, the task/file threshold is authoritative over the low-confidence token estimate.

<verification>
Run the aggregate script and confirm the full workflow inventory names each separate non-PR authority and no duplicate recurring product proof.
</verification>

<success_criteria>
One PR workflow owns all recurring proof, docs-only behavior stays visible and bounded, and all stable invariants pass one command.
</success_criteria>

<output>
Create `.planning/workstreams/quality-ratchet-release/phases/165-efficient-and-maintainable-ci/165-09-SUMMARY.md` when done.
</output>
