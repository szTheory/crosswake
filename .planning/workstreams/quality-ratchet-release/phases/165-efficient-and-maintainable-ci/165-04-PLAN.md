---
phase: 165-efficient-and-maintainable-ci
plan: 04
type: execute
wave: 4
depends_on: [165-02, 165-03]
files_modified:
  - script/verify_generated_android_shell.sh
  - .github/actions/setup-android-jvm/action.yml
  - .github/workflows/native-behavioral-proof-gate.yml
  - test/crosswake/proof/phase165_ci_integrity_test.exs
  - .github/actions/setup-elixir-cache/action.yml
  - .github/workflows/phase5-proof.yml
  - .github/workflows/phase18-proof.yml
  - .github/workflows/phase79-proof.yml
autonomous: true
requirements: [CIP-01, CIP-03, CIP-04]
estimate:
  tokens: 48000
  raw_tokens: 48000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "Pure Elixir and Android/JVM proof runs on Linux; every macOS job invokes a Swift/Xcode/signing/simulator Apple tool per D-19 and D-20."
    - "Compiled caches miss across OS, architecture, OTP, Elixir, MIX_ENV, dependency topology, lock, JDK, Gradle, Swift/Xcode, platform, architecture, or resolved-state incompatibility per D-21 and D-22."
    - "Every long-running job has a bounded job timeout and assertions are never retried per D-23."
  artifacts:
    - path: ".github/actions/setup-android-jvm/action.yml"
      provides: "official JDK 17 and Gradle wrapper/cache setup with explicit compatibility identity"
    - path: "script/verify_generated_android_shell.sh"
      provides: "portable generated Android package/JVM proof separated from optional macOS/device setup"
  key_links:
    - from: ".github/actions/setup-android-jvm/action.yml"
      to: "packages/crosswake-shell-core-android/gradle/wrapper/gradle-wrapper.properties"
      via: "wrapper/JDK/Gradle/OS/config identity"
      pattern: "java-version|setup-gradle|gradle-wrapper.properties"
  prohibitions:
    - requirement_id: CIP-01
      category: safety
      statement: "MUST NOT use runner migration to add Android generator, Maven, JVM, vector, emulator/device, template, parity, or product behavior."
      status: unresolved
      flagged: true
    - requirement_id: CIP-03
      category: privacy
      statement: "MUST NOT expose cache keys, runner identities, or toolchain internals in the primary Crosswake CI verdict or retained evidence."
      status: unresolved
      flagged: true
    - requirement_id: CIP-04
      category: safety
      statement: "MUST NOT alter Release Please, recovery, or publish cancellation and approval semantics while applying ordinary CI timeouts."
      status: unresolved
      flagged: true
---

<objective>
Make runner placement, cache identity, and timeouts correct before the full proof graph moves into the new workflow.

Purpose: Remove macOS waste without changing proof behavior or restoring incompatible artifacts.
Output: Portable Android/JVM proof, reusable setup contracts, exact cache dimensions, and structural runner/timeout tests.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/quality-ratchet-release/phases/165-efficient-and-maintainable-ci/165-02-SUMMARY.md
@.planning/workstreams/quality-ratchet-release/phases/165-efficient-and-maintainable-ci/165-03-SUMMARY.md
@.planning/workstreams/quality-ratchet-release/phases/165-efficient-and-maintainable-ci/165-CONTEXT.md
@.planning/workstreams/quality-ratchet-release/phases/165-efficient-and-maintainable-ci/165-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Separate portable generated Android/JVM proof from macOS-only provisioning</name>
  <read_first>
    script/verify_generated_android_shell.sh
    .github/workflows/native-behavioral-proof-gate.yml
    packages/crosswake-shell-core-android/gradle/wrapper/gradle-wrapper.properties
    packages/crosswake-shell-core-android/build.gradle
  </read_first>
  <files>script/verify_generated_android_shell.sh, .github/actions/setup-android-jvm/action.yml, .github/workflows/native-behavioral-proof-gate.yml, test/crosswake/proof/phase165_ci_integrity_test.exs</files>
  <behavior>
    - `CROSSWAKE_ANDROID_CONNECTED_TESTS=0` requires only checkout, explicit JDK 17, wrapper validation/setup-gradle, generation, and JVM/package proof on Ubuntu.
    - macOS-only downloads, Homebrew paths, DEVELOPER_DIR, SDK/emulator setup, and device proof are unreachable in the portable mode.
  </behavior>
  <action>Split the helper at the current platform seam so portable generation and JVM/package assertions accept an already configured Java/Gradle toolchain and optional connected-test provisioning remains separate. Add `setup-android-jvm` using pinned official setup-java and `gradle/actions/setup-gradle@v6` with inputs `working-directory`, `java-version` default `17`, and `gradle-version` derived from the wrapper; do not layer another Gradle User Home cache. Move `android-generated-shell-unit` to Ubuntu and preserve frozen Android outputs/commands exactly per D-19 and D-22.</action>
  <acceptance_criteria>
    - Portable-mode shell test runs without references to macOS disk images, Homebrew, `/Applications`, `Contents/Home`, DEVELOPER_DIR, simulator, emulator, or connected-device setup in the executed branch.
    - Android package and generated-shell JVM jobs use Ubuntu, explicit JDK 17, wrapper validation, official setup-gradle, and bounded timeouts.
    - Existing Android generator, Maven/JVM/vector fixtures and expected outputs are unchanged.
  </acceptance_criteria>
  <verify>
    <automated>bash -n script/verify_generated_android_shell.sh &amp;&amp; shellcheck script/verify_generated_android_shell.sh &amp;&amp; actionlint .github/workflows/native-behavioral-proof-gate.yml &amp;&amp; mix test test/crosswake/proof/phase165_ci_integrity_test.exs --only runner_placement</automated>
  </verify>
  <done>Generated Android JVM proof is Linux-portable with no Android breadth change.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Preserve complete Elixir, Gradle, and Swift cache compatibility identity</name>
  <read_first>
    .github/actions/setup-elixir-cache/action.yml
    .github/workflows/native-behavioral-proof-gate.yml
    .tool-versions
    packages/crosswake-shell-core-ios/Package.swift
    .planning/seeds/SEED-007-ci-cd-performance.md
  </read_first>
  <files>.github/actions/setup-elixir-cache/action.yml, .github/actions/setup-android-jvm/action.yml, .github/workflows/native-behavioral-proof-gate.yml, test/crosswake/proof/phase165_ci_integrity_test.exs</files>
  <behavior>
    - Elixir deps/_build identity includes dependency scope, OS, architecture, OTP, Elixir, MIX_ENV, lock hash, and workflow/job topology; dependency-present and hermetic scopes never overlap.
    - Gradle identity includes JDK, wrapper/Gradle, OS, and relevant config/lock inputs; Swift identity includes platform, architecture, Swift/Xcode, Package.swift, and explicit present/absent Package.resolved state.
  </behavior>
  <action>Extend the established Elixir composite only to expose safe closed exact/partial/miss outcomes while retaining every existing compiled-cache dimension, shared Hex-package tarball boundary, and fetch-only retry. Add fixture-driven structural assertions that one-step changes to each compatibility dimension miss. Use official Gradle setup rather than a second cache. Qualify Swift cache identity by runner platform/architecture, `swift --version`/Xcode identity, manifest hash, and resolved-state sentinel per D-21 and D-22; never retain raw cache keys in evidence or summaries.</action>
  <acceptance_criteria>
    - Tests mutate each listed cache dimension independently and prove the derived identity differs.
    - The shared Hex cache remains limited to inert package tarballs; registry ETS and compiled dependency/build trees remain scoped.
    - Swift absent-resolved-state and present-resolved-state fixtures cannot share an identity.
  </acceptance_criteria>
  <verify>
    <automated>mix test test/crosswake/proof/phase165_ci_integrity_test.exs --only cache_identity &amp;&amp; actionlint .github/workflows/native-behavioral-proof-gate.yml</automated>
  </verify>
  <done>Incompatible BEAM, Gradle, and Swift states miss by construction and cache observations reveal no keys.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Move mixed legacy proof to the correct runner and audit every timeout</name>
  <read_first>
    .github/workflows/phase5-proof.yml
    .github/workflows/phase18-proof.yml
    .github/workflows/phase79-proof.yml
    .github/workflows/release-please.yml
    test/crosswake/proof/phase165_ci_integrity_test.exs
  </read_first>
  <files>.github/workflows/phase5-proof.yml, .github/workflows/phase18-proof.yml, .github/workflows/phase79-proof.yml, test/crosswake/proof/phase165_ci_integrity_test.exs</files>
  <behavior>
    - Phase 5 pure proof uses Linux; Phase 18 and Phase 79 split Elixir/Android JVM work onto Linux and retain only actual Swift/Xcode work on macOS.
    - Every job in every workflow has a positive bounded timeout; no assertion command is wrapped in retry.
  </behavior>
  <action>Move Phase 5's explicitly non-native job to Ubuntu, split Phase 18 and Phase 79 at their actual Apple-tool boundary, remove dead DEVELOPER_DIR from Linux jobs, and retain literal proof names/artifacts. Add a repository-wide structural timeout/runner audit that classifies commands, not workflow filenames, and explicitly exempts no job from bounded timeout. Preserve Release Please exact `cancel-in-progress: false` and queue/approval semantics per D-20, D-23, and D-24.</action>
  <acceptance_criteria>
    - Structural inventory reports zero pure Elixir/Android-JVM jobs on macOS and zero macOS jobs without an Apple-tool invocation.
    - Structural inventory reports zero jobs without a positive bounded timeout.
    - No proof/test/assertion step is retried; the existing bounded dependency-fetch retry remains limited to fetch setup.
    - Release Please concurrency, permissions, environment/approval, and publish jobs are unchanged.
  </acceptance_criteria>
  <verify>
    <automated>actionlint .github/workflows/phase5-proof.yml .github/workflows/phase18-proof.yml .github/workflows/phase79-proof.yml &amp;&amp; mix test test/crosswake/proof/phase165_ci_integrity_test.exs --only runner_placement --only timeout --only release_trust</automated>
  </verify>
  <done>Runner placement and timeouts are derived per job, while Apple and release trust boundaries remain intact.</done>
</task>

</tasks>

<threat_model>
Security posture: OWASP ASVS Level 1; every high or critical threat blocks execution until its listed mitigation passes.

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| PR cache producer → later run | Untrusted or incompatible artifacts could be restored into proof. |
| portable proof → frozen Android surface | Runner work could accidentally broaden Android behavior. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-165-06 | Tampering | BEAM/Gradle/Swift caches | high | mitigate | Complete compatibility identity and one-dimension negative fixtures; no privileged PR cache consumer. |
| T-165-09 | Tampering | Android runner move | medium | mitigate | Existing fixture/output parity and explicit no-device/no-feature structural assertions. |
| T-165-10 | Denial of service | long-running jobs | medium | mitigate | Per-job bounded timeout and no assertion retry. |
</threat_model>

## Artifacts this phase produces

New action `.github/actions/setup-android-jvm/action.yml` has inputs `working-directory`, `java-version`, and `gradle-version`; it exports no sensitive cache key. Existing `setup-elixir-cache` retains `deps-cache-hit` and `build-cache-hit` closed outputs. No application schema, external API, Android feature, or release surface changes.

<verification>
Run all task commands plus `python3 script/check_ci_leaf_manifest.py --self-test`; record the exact current workflow/job inventory rather than a historical count.
</verification>

<success_criteria>
All pure work is Linux, all macOS jobs invoke Apple tools, all caches have complete compatibility identity, and all jobs are bounded without proof retries.
</success_criteria>

<output>
Create `.planning/workstreams/quality-ratchet-release/phases/165-efficient-and-maintainable-ci/165-04-SUMMARY.md` when done.
</output>
