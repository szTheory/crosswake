---
phase: 165-efficient-and-maintainable-ci
plan: 06
type: execute
wave: 6
depends_on: [165-05]
files_modified:
  - .github/workflows/crosswake-ci.yml
  - .github/workflows/phase41-proof.yml
  - .github/workflows/phase43-proof.yml
  - .github/workflows/phase45-proof.yml
  - .github/workflows/phase48-proof.yml
  - .github/workflows/phase52-proof.yml
  - .github/workflows/phase58-proof.yml
  - .github/workflows/phase69-proof.yml
  - .github/workflows/phase70-proof.yml
  - script/ci_leaf_manifest.json
  - script/check_ci_leaf_manifest.py
  - test/crosswake/proof/phase165_ci_integrity_test.exs
autonomous: true
requirements: [CIP-02, CIP-05, CIP-07]
estimate:
  tokens: 48000
  raw_tokens: 48000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Gating, Rulestead, Rindle, provider, operator, auth, closeout, and subscription proof retains literal identity while recurring PR execution is consolidated per D-07 and D-11."
    - "Legacy newline-oriented relevance logic is removed in favor of the central classifier, and unknown classification runs full proof per D-14 through D-17."
    - "After each cohort, umbrella static needs remains exactly `proof_leaves union required_control_nodes`, with `classify-change` required as a control node and never treated as irrelevant, per D-07 through D-09."
  artifacts:
    - path: ".github/workflows/crosswake-ci.yml"
      provides: "literal Phase 41/43/45/48/52/58/69/70 proof leaves"
  key_links:
    - from: "script/classify_ci_change.py"
      to: ".github/workflows/crosswake-ci.yml"
      via: "one closed classification output replaces Phase 69 inline newline/grep relevance"
      pattern: "full_proof|irrelevant_leaves"
  prohibitions:
    - requirement_id: CIP-07
      category: transparency
      statement: "MUST NOT hide domain proof failures behind a generic orchestration error; summaries must name the literal failing proof and exact local command."
      status: unresolved
      flagged: true
---

<objective>
Migrate the first domain-proof cohort into the single PR graph while preserving distinct scheduled advisories.

Purpose: Remove another large band of duplicated PR/push work without weakening domain-specific evidence.
Output: Eight literal domain leaves, central relevance, and updated exact manifest governance.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/quality-ratchet-release/phases/165-efficient-and-maintainable-ci/165-05-SUMMARY.md
@.planning/workstreams/quality-ratchet-release/phases/165-efficient-and-maintainable-ci/165-CONTEXT.md
@.planning/workstreams/quality-ratchet-release/phases/165-efficient-and-maintainable-ci/165-PATTERNS.md
@.planning/workstreams/quality-ratchet-release/phases/165-efficient-and-maintainable-ci/evidence/required-context-baseline.json
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Move gating, Rulestead, Rindle, and provider PR proof</name>
  <precondition>A fresh read-only required-context snapshot verification exactly matches the Plan 01 strictness, sorted context set, source SHA, and digest.</precondition>
  <read_first>
    .github/workflows/phase41-proof.yml
    .github/workflows/phase43-proof.yml
    .github/workflows/phase45-proof.yml
    .github/workflows/phase48-proof.yml
    .github/workflows/crosswake-ci.yml
    .planning/workstreams/quality-ratchet-release/phases/165-efficient-and-maintainable-ci/evidence/required-context-baseline.json
  </read_first>
  <files>.github/workflows/crosswake-ci.yml, .github/workflows/phase41-proof.yml, .github/workflows/phase43-proof.yml, .github/workflows/phase45-proof.yml, .github/workflows/phase48-proof.yml</files>
  <behavior>
    - The four merge-blocking jobs run literally once per full-proof PR on Linux with existing commands, timeouts, names, logs, artifacts, and summaries.
    - Scheduled/manual advisory jobs remain in their source files with no pull_request or generic push trigger.
  </behavior>
  <action>Before any source-trigger edit, run the live snapshot verifier and stop on drift. Move the four PR-owned merge-blocking jobs into Crosswake CI using the exact target IDs in the mapping table, exact local commands, and purpose names. Emit each matching frozen-snapshot display string from its listed checkout-free compatibility job outside umbrella proof leaves/control nodes, bound fail-closed to the new leaf/umbrella. Preserve any scheduled/manual advisory sibling under its existing permission/secret boundary with ordinary PR and push removed. Use the shared Elixir cache action, no arbitrary-command abstraction, and exact manifest-issued docs-only irrelevance per D-01, D-02, D-07, D-10 through D-12, D-18, and D-32.</action>
  <acceptance_criteria>
    - Each source merge-blocking display name has exactly one PR producer in Crosswake CI.
    - Each retained advisory job has only schedule/manual triggers and prior permissions/secrets.
    - Every moved leaf is a static umbrella need and reports one exact local remediation command.
    - Snapshot verification passes immediately before each source loses PR/push authority; drift leaves all four source producers intact.
  </acceptance_criteria>
  <verify>
    <automated>test ! -e .github/workflows/phase41-proof.yml &amp;&amp; actionlint .github/workflows/crosswake-ci.yml .github/workflows/phase43-proof.yml .github/workflows/phase45-proof.yml .github/workflows/phase48-proof.yml &amp;&amp; python3 script/list_merge_blocking_checks.py --emitters &gt;/dev/null</automated>
  </verify>
  <done>The first domain cohort is consolidated without losing advisory or named-proof boundaries.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Move operator, auth, closeout, and subscription PR proof</name>
  <precondition>A fresh read-only required-context snapshot verification still matches the frozen Plan 01 artifact after Task 1.</precondition>
  <read_first>
    .github/workflows/phase52-proof.yml
    .github/workflows/phase58-proof.yml
    .github/workflows/phase69-proof.yml
    .github/workflows/phase70-proof.yml
    .github/workflows/crosswake-ci.yml
    .planning/workstreams/quality-ratchet-release/phases/165-efficient-and-maintainable-ci/evidence/required-context-baseline.json
  </read_first>
  <files>.github/workflows/crosswake-ci.yml, .github/workflows/phase52-proof.yml, .github/workflows/phase58-proof.yml, .github/workflows/phase69-proof.yml, .github/workflows/phase70-proof.yml</files>
  <behavior>
    - The four domain proof jobs run once under Crosswake CI; Phase 69 no longer owns newline-oriented path classification.
    - Documentation-only scheduling is controlled only by the central classifier and exact leaf manifest.
  </behavior>
  <action>Re-run the live snapshot verifier before any source-trigger edit. Move the four PR merge-blocking jobs into Crosswake CI using the exact target IDs in the mapping table, add the listed exact-name legacy compatibility jobs only for contexts in the frozen snapshot, and remove their ordinary PR/generic push producers from the phase workflows after parity passes. Delete Phase 69's inline diff/grep relevance owner and consume only the central closed classifier output. Retain scheduled/manual provider/device advisories separately with no PR cancellation. Update `proof_leaves` and `legacy_compatibility_contexts` without changing required control node `classify-change`, preserving D-07, D-10, D-14 through D-18, D-23, and D-31 through D-33.</action>
  <acceptance_criteria>
    - Repository search and structural test find no phase-local newline/grep PR classifier in the migrated files.
    - Full-proof and documentation-only fixtures schedule the exact expected Plan 06 families and explicit irrelevance.
    - Manifest validator, producer audit, actionlint, runner audit, and timeout audit all pass.
    - Frozen snapshot verification passes immediately before all four source trigger edits; live drift stops without removing a producer.
  </acceptance_criteria>
  <verify>
    <automated>test ! -e .github/workflows/phase69-proof.yml &amp;&amp; actionlint .github/workflows/crosswake-ci.yml .github/workflows/phase52-proof.yml .github/workflows/phase58-proof.yml .github/workflows/phase70-proof.yml &amp;&amp; python3 script/check_ci_leaf_manifest.py --self-test &amp;&amp; mix test test/crosswake/proof/phase165_ci_integrity_test.exs --only triggers --only manifest</automated>
  </verify>
  <done>The second domain cohort is single-run, centrally classified, statically named, and timeout-bounded.</done>
</task>

</tasks>

<threat_model>
Security posture: OWASP ASVS Level 1; every high or critical threat blocks execution until its listed mitigation passes.

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| central classifier → domain leaves | An incorrect neutral decision could skip unrelated domain proof. |
| scheduled advisory → PR graph | Secret/device authority must not enter ordinary PR execution. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-165-04 | Tampering | domain leaf graph | high | mitigate | Exact manifest/static-needs parity and full-proof fallback. |
| T-165-12 | Elevation of privilege | advisory workflows | high | mitigate | Retain schedule/manual trust, permissions, and secrets outside PR workflow. |
</threat_model>

## Artifacts this phase produces

Deterministic exhaustive migration map:

| Source workflow/job | Crosswake CI proof leaf ID | Compatibility job ID → frozen display context | Source file disposition | Primary command surface |
|---|---|---|---|---|
| `phase41-proof.yml / merge-blocking-gating-proof` | `phase41-gating-proof` | `compat-phase41-gating-proof` → `merge-blocking gating doctor and support matrix proof (hermetic)` | delete after parity; workflow_dispatch is the same proof, not separate authority | Phase 41 gating doctor test plus hermetic suite |
| `phase43-proof.yml / merge-blocking-rulestead-proof` | `phase43-rulestead-proof` | `compat-phase43-rulestead-proof` → `merge-blocking rulestead proof (hermetic)` | retain schedule/manual advisory only | hermetic suite excluding advisory/example-host tests |
| `phase45-proof.yml / merge-blocking-rindle-proof` | `phase45-rindle-proof` | `compat-phase45-rindle-proof` → `merge-blocking rindle proof (hermetic)` | retain schedule/manual advisory only | hermetic suite excluding advisory/example-host tests |
| `phase48-proof.yml / merge-blocking-provider-adapter-proof` | `phase48-provider-adapter-proof` | `compat-phase48-provider-adapter-proof` → `merge-blocking provider adapter proof (hermetic)` | retain schedule/manual advisory only | Phase 48 provider-adapter focused proof |
| `phase52-proof.yml / merge-blocking-operator-proof` | `phase52-operator-proof` | `compat-phase52-operator-proof` → `merge-blocking operator proof (hermetic)` | retain schedule/manual advisory only | Phase 52 operator truth proof |
| `phase58-proof.yml / merge-blocking-auth-closeout-proof` | `phase58-auth-closeout-proof` | `compat-phase58-auth-closeout-proof` → `merge-blocking auth closeout proof (hermetic)` | retain schedule/manual advisory only | security closeout plus focused auth/support/doctor/companion proofs |
| `phase69-proof.yml / merge-blocking-closeout-proof` | `phase69-closeout-proof` | `compat-phase69-closeout-proof` → `merge-blocking-closeout-proof` | delete after parity; workflow_dispatch is the same proof | `mix closeout.verify` plus Phase 69 docs-contract and closeout tests; inline classifier removed |
| `phase70-proof.yml / merge-blocking-subscription-saas-proof` | `phase70-subscription-saas-proof` | `compat-phase70-subscription-saas-proof` → `merge-blocking subscription SaaS proof (hermetic)` | retain schedule/manual advisory only | Phase 70 subscription/SaaS commerce proof |

Every proof row uses the existing manifest fields; every compatibility row uses `job_id`, exact `display_context`, and `needs_target`. No new CLI, action input/output, schema, or external integration is added.

Scope rationale: this 12-file cohort is atomic because the two tasks each migrate four independently named leaves but share one workflow, manifest, validator, and integrity test; those shared files must be resealed after each half while the frozen eight-context compatibility set remains exact. Each task owns five source/workflow files plus shared test updates, and source-trigger removal cannot be separated from replacement producer creation. With no calibration samples, file/task thresholds—not the low-confidence token estimate—govern execution.

<verification>
Run both tasks then the aggregate manifest/producer/trigger checks from Plan 05.
</verification>

<success_criteria>
All eight domain proofs run once on PRs, advisories stay separated, and central classification cannot silently omit them.
</success_criteria>

<output>
Create `.planning/workstreams/quality-ratchet-release/phases/165-efficient-and-maintainable-ci/165-06-SUMMARY.md` when done.
</output>
