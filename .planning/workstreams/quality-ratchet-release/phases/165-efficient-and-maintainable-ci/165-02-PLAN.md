---
phase: 165-efficient-and-maintainable-ci
plan: 02
type: execute
wave: 2
depends_on: [165-01]
files_modified:
  - script/classify_ci_change.py
  - script/ci_docs_allowlist.json
  - test/fixtures/ci/classifier
  - test/crosswake/proof/phase165_ci_policy_test.exs
  - script/check_ci_leaf_manifest.py
  - script/ci_leaf_manifest.json
  - script/check_aggregator_result_semantics.py
  - .github/workflows/aggregator-negative-control.yml
  - script/list_merge_blocking_checks.py
autonomous: true
requirements: [CIP-05, CIP-07]
estimate:
  tokens: 36000
  raw_tokens: 36000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Classifier output is binary, versioned, NUL-safe, rename/copy-aware, and defaults every non-allowlisted or invalid case to full_proof per D-14 through D-18."
    - "Manifest leaf IDs, literal job declarations, display names, remediation commands, allowed irrelevance reasons, static umbrella needs, and unique producers are bijective per D-07 through D-09."
    - "[Edge CIP-07/adjacency] Exactly equal manifest/job/static-needs sets pass; one extra or missing member on any side fails with the member name."
    - "[Edge CIP-07/empty] Empty manifest, empty static needs, or a missing umbrella fails validation."
    - "[Edge CIP-07/ordering] Declaration order does not change set parity, while diagnostics and generated summaries remain deterministically ordered."
  assumptions:
    - statement: "[FLAGGED ASSUMPTION edge CIP-03/unclassified] Complete cache-compatibility identity can be derived from repository toolchain and lock/config files without introducing a new dependency; Plan 04 must verify the exact inventory."
      status: unresolved
  artifacts:
    - path: "script/check_ci_leaf_manifest.py"
      provides: "four-way manifest, workflow, static-needs, display-name, remediation, and producer parity"
    - path: "test/fixtures/ci/classifier"
      provides: "adversarial NUL/status/SHA/path classification corpus"
  key_links:
    - from: "script/ci_leaf_manifest.json"
      to: ".github/workflows/crosswake-ci.yml"
      via: "exact leaf IDs, literal names, remediation commands, and irrelevance reasons"
      pattern: "leaf_id|display_name|remediation_command|irrelevance_reason"
  prohibitions:
    - requirement_id: CIP-05
      category: transparency
      statement: "MUST NOT widen the documentation allowlist to hide executable, release, workflow, dependency, lockfile, generated-contract, or mixed changes from full proof."
      status: unresolved
      flagged: true
    - requirement_id: CIP-07
      category: transparency
      statement: "MUST NOT replace heterogeneous literal proof jobs with a dynamic arbitrary-command matrix whose absent cells or generated names evade governance."
      status: unresolved
      flagged: true
---

<objective>
Harden the tracer's classification and exact proof-inventory contracts before more leaves are moved.

Purpose: Make documentation-only skipping and umbrella completeness mechanically fail closed.
Output: Adversarial classifier fixtures and one exact leaf/needs/producer governance path.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/quality-ratchet-release/phases/165-efficient-and-maintainable-ci/165-01-SUMMARY.md
@.planning/workstreams/quality-ratchet-release/phases/165-efficient-and-maintainable-ci/165-CONTEXT.md
@.planning/workstreams/quality-ratchet-release/phases/165-efficient-and-maintainable-ci/165-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Close every classifier ambiguity with adversarial Git fixtures</name>
  <read_first>
    script/classify_ci_change.py
    script/ci_docs_allowlist.json
    test/crosswake/proof/phase165_ci_policy_test.exs
    lib/crosswake/planning/first_adopter_context.ex
    .github/workflows/phase75-closeout-gate.yml
  </read_first>
  <files>script/classify_ci_change.py, script/ci_docs_allowlist.json, test/fixtures/ci/classifier, test/crosswake/proof/phase165_ci_policy_test.exs</files>
  <behavior>
    - Both old and new paths of rename/copy records must be allowlisted; deletion remains classified by its deleted path.
    - Empty, malformed, shallow, zero-SHA, unresolved-SHA, unknown-status, mixed, and unallowlisted input is full_proof.
    - Same records in different order produce identical normalized output.
  </behavior>
  <action>Inventory the exact public-doc, planning, and guide roots and assign each allowed family its bounded D-18 owner; exclude release inputs and any family without an applicable focused privacy/public-doc/planning check. Expand table-driven self-tests and isolated temporary repositories so byte-oriented names, rename/copy status arity, both endpoints, deletions, truncated records, shallow history, zero/missing/unresolvable SHAs, mixed inputs, duplicates, and permutations are executable. Keep filenames as data, never shell expressions or file-content output, and retain the full-proof default per D-14, D-15, D-16, D-17, and D-18.</action>
  <acceptance_criteria>
    - The allowlist is a closed JSON data file whose every path family names a scheduled focused proof owner.
    - Every listed adversarial fixture asserts exactly `documentation_only` or `full_proof` plus a closed reason and scheduled/irrelevant families.
    - A mixed allowed/unallowed change, a rename crossing the allowlist boundary, and an empty diff all produce full_proof.
  </acceptance_criteria>
  <verify>
    <automated>python3 script/classify_ci_change.py --self-test &amp;&amp; mix test test/crosswake/proof/phase165_ci_policy_test.exs --only classifier</automated>
  </verify>
  <done>All four CIP-05 edge probes are explicit classifier contracts and unknown input cannot silently skip proof.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Enforce exact leaf, needs, irrelevance, and producer parity</name>
  <read_first>
    script/ci_leaf_manifest.json
    .github/workflows/crosswake-ci.yml
    script/list_merge_blocking_checks.py
    script/check_aggregator_result_semantics.py
    .github/workflows/aggregator-negative-control.yml
  </read_first>
  <files>script/check_ci_leaf_manifest.py, script/ci_leaf_manifest.json, script/check_aggregator_result_semantics.py, .github/workflows/aggregator-negative-control.yml, script/list_merge_blocking_checks.py</files>
  <behavior>
    - Manifest, jobs, umbrella static needs, literal display names, remediation commands, and producer records agree in both directions.
    - success passes; skipped passes only with that leaf's exact classifier-issued manifest reason; every missing/extra/unknown/failed/cancelled/timed_out/action_required/stale/unexplained-skipped case fails.
  </behavior>
  <action>Add `check_ci_leaf_manifest.py --self-test` using the existing stable parser/diagnostic style, extend `list_merge_blocking_checks.py` instead of adding a producer taxonomy, and extend the existing closed evaluator/negative-control workflow with missing, extra, empty, exact-irrelevance, and every closed result arm. Require literal static `needs` and literal display names; matrices remain limited to homogeneous compatibility axes per D-09 and D-13. The production umbrella stays checkout-free per D-08.</action>
  <acceptance_criteria>
    - Removing or adding one leaf independently in the manifest, workflow jobs, or umbrella needs makes the self-test fail and names the mismatch.
    - Empty manifest/needs and missing umbrella fixtures fail.
    - Exact irrelevant skip passes neutral; a different, missing, or unknown irrelevance reason fails.
    - Unique producer output remains stable TSV and reports exactly one target umbrella producer.
  </acceptance_criteria>
  <verify>
    <automated>python3 script/check_ci_leaf_manifest.py --self-test &amp;&amp; python3 script/check_aggregator_result_semantics.py --self-test &amp;&amp; python3 script/list_merge_blocking_checks.py --emitters &gt;/dev/null &amp;&amp; actionlint .github/workflows/aggregator-negative-control.yml</automated>
  </verify>
  <done>The proof graph cannot omit, invent, reorder away, or silently neutralize a leaf.</done>
</task>

</tasks>

<threat_model>
Security posture: OWASP ASVS Level 1; every high or critical threat blocks execution until its listed mitigation passes.

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Git diff bytes → classifier | Untrusted names/statuses can attempt command or policy injection. |
| manifest/workflow → required conclusion | A missing leaf or false irrelevance could bypass proof. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-165-03 | Tampering / Elevation | classifier | high | mitigate | Byte-safe argv parsing, closed statuses, full-proof fallback, adversarial fixtures. |
| T-165-04 | Tampering | manifest and umbrella | high | mitigate | Four-way bidirectional parity plus direct missing/extra/empty negative controls. |
</threat_model>

## Artifacts this phase produces

New surfaces in this plan: `script/check_ci_leaf_manifest.py` with `--self-test`; `test/fixtures/ci/classifier/**`; manifest fields `leaf_id`, `display_name`, `family`, `remediation_command`, and `irrelevance_reason`. No package, external API, schema, telemetry service, or product surface is added.

<verification>
Run both task gates and the Plan 01 tracer gate; all must finish in under 60 seconds locally except Mix compilation cache warm-up.
</verification>

<success_criteria>
The classifier and leaf graph fail closed for every stated ambiguity and the umbrella's authority is structurally complete.
</success_criteria>

<output>
Create `.planning/workstreams/quality-ratchet-release/phases/165-efficient-and-maintainable-ci/165-02-SUMMARY.md` when done.
</output>
