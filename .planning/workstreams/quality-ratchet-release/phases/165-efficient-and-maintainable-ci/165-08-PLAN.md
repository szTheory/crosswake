---
phase: 165-efficient-and-maintainable-ci
plan: 08
type: execute
wave: 8
depends_on: [165-07]
files_modified:
  - .github/workflows/crosswake-ci.yml
  - .github/workflows/native-behavioral-proof-gate.yml
  - .github/workflows/phase5-proof.yml
  - .github/workflows/phase18-proof.yml
  - .github/workflows/phase79-proof.yml
  - .github/workflows/offline-sync-e2e-gate.yml
  - .github/workflows/phase67-proof.yml
  - .github/workflows/phase68-proof.yml
  - .github/workflows/merge-blocking-ios-mirror-parity.yml
  - .github/workflows/phase10-proof.yml
  - .github/workflows/phase96-proof.yml
  - script/ci_leaf_manifest.json
  - test/crosswake/proof/phase165_ci_integrity_test.exs
autonomous: true
requirements: [CIP-01, CIP-02, CIP-05, CIP-07]
estimate:
  tokens: 52000
  raw_tokens: 52000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "Android/JVM, iOS/Swift, browser/example-host, iOS-mirror, and remaining package proof stays literal and uses the runner dictated by its actual tool invocation per D-19 and D-20."
    - "Native emulator/device advisories remain separate from required PR proof and frozen Android behavior is unchanged."
    - "Documentation-only PRs schedule none of the root-suite, Playwright, Android, Apple, or unrelated packaging leaves per D-18, while the umbrella remains visible."
    - "After every cross-runtime cohort, umbrella static needs remains exactly `proof_leaves union required_control_nodes`, with `classify-change` required as a control node and never treated as irrelevant, per D-07 through D-09."
  artifacts:
    - path: ".github/workflows/crosswake-ci.yml"
      provides: "literal native package, generated Android, offline E2E, Phase 5/10/18/67/79/96, and iOS mirror parity leaves"
  key_links:
    - from: ".github/actions/setup-android-jvm/action.yml"
      to: ".github/workflows/crosswake-ci.yml"
      via: "Linux Android package/generated JVM leaves"
      pattern: "setup-android-jvm|ubuntu"
  prohibitions:
    - requirement_id: CIP-01
      category: safety
      statement: "MUST NOT promote advisory emulator/device proof into required authority or expand frozen Android feature/parity scope during runner consolidation."
      status: unresolved
      flagged: true
---

<objective>
Move native, browser, package, and remaining cross-runtime proof into the consolidated PR graph while retaining their platform and advisory boundaries.

Purpose: Complete the expensive proof migration without turning CI work into product/mobile breadth.
Output: Literal cross-runtime leaves, Linux/Apple runner split, and no equivalent source-workflow PR/push execution.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/quality-ratchet-release/phases/165-efficient-and-maintainable-ci/165-07-SUMMARY.md
@.planning/workstreams/quality-ratchet-release/phases/165-efficient-and-maintainable-ci/165-04-SUMMARY.md
@.planning/workstreams/quality-ratchet-release/phases/165-efficient-and-maintainable-ci/165-CONTEXT.md
@.planning/ADR-FIRST-B2C-ADOPTER.md
@.planning/workstreams/quality-ratchet-release/phases/165-efficient-and-maintainable-ci/evidence/required-context-baseline.json
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Move native package and mixed Phase 5/18/79 proof with the verified runner split</name>
  <precondition>A fresh read-only required-context snapshot verification exactly matches the Plan 01 strictness, sorted context set, source SHA, and digest.</precondition>
  <read_first>
    .github/workflows/native-behavioral-proof-gate.yml
    .github/workflows/phase5-proof.yml
    .github/workflows/phase18-proof.yml
    .github/workflows/phase79-proof.yml
    .github/workflows/crosswake-ci.yml
    .planning/workstreams/quality-ratchet-release/phases/165-efficient-and-maintainable-ci/evidence/required-context-baseline.json
  </read_first>
  <files>.github/workflows/crosswake-ci.yml, .github/workflows/native-behavioral-proof-gate.yml, .github/workflows/phase5-proof.yml, .github/workflows/phase18-proof.yml, .github/workflows/phase79-proof.yml</files>
  <behavior>
    - Android package/generated JVM and pure Elixir work runs on Ubuntu; iOS/Swift/Xcode work alone remains on macOS.
    - The legacy native aggregator context remains as a closed compatibility conclusion until Plan 12, while literal subproof names and artifacts remain visible.
  </behavior>
  <action>Before any source-trigger edit, run the live snapshot verifier and stop on drift. Move the native-behavioral literal subjobs and Phase 5/18/79 PR proof into Crosswake CI using the exact target IDs in the mapping table, Plan 04's Linux Android/JVM setup, and macOS only for exact Apple commands. Preserve current commands, fixtures, outputs, artifacts, and display names; add `compat-native-behavioral-proof` only for the exact native aggregator display context in the frozen snapshot. Remove equivalent pull_request/generic push execution after leaf/compatibility parity passes; delete native/Phase 5/18/79 source files because their manual invocation repeats the same proof and owns no distinct trust boundary. Do not add emulator/device proof or Android behavior per D-07, D-18 through D-20, and the frozen project boundary.</action>
  <acceptance_criteria>
    - Runner audit reports every migrated pure/Android-JVM job on Ubuntu and every macOS job with an Apple invocation.
    - Native compatibility conclusion fails for any missing, failed, cancelled, timed-out, stale, action-required, unknown, or unexplained skipped subleaf.
    - Source workflows no longer duplicate ordinary PR/push proof; existing advisory-native authority remains non-required and separate.
    - Snapshot verification passes immediately before each source trigger removal; live drift leaves all source producers intact.
  </acceptance_criteria>
  <verify>
    <automated>actionlint .github/workflows/crosswake-ci.yml .github/workflows/native-behavioral-proof-gate.yml .github/workflows/phase5-proof.yml .github/workflows/phase18-proof.yml .github/workflows/phase79-proof.yml &amp;&amp; mix test test/crosswake/proof/phase165_ci_integrity_test.exs --only runner_placement --only triggers</automated>
  </verify>
  <done>Native and mixed proof is consolidated with exact platform ownership and no product breadth.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Move browser/offline and Android JVM leaves while isolating device advisory</name>
  <precondition>A fresh read-only required-context snapshot verification still matches the frozen Plan 01 artifact after Task 1.</precondition>
  <read_first>
    .github/workflows/offline-sync-e2e-gate.yml
    .github/workflows/phase67-proof.yml
    .github/workflows/phase68-proof.yml
    .github/workflows/crosswake-ci.yml
    AGENTS.md
    .planning/workstreams/quality-ratchet-release/phases/165-efficient-and-maintainable-ci/evidence/required-context-baseline.json
  </read_first>
  <files>.github/workflows/crosswake-ci.yml, .github/workflows/offline-sync-e2e-gate.yml, .github/workflows/phase67-proof.yml, .github/workflows/phase68-proof.yml</files>
  <behavior>
    - Offline/browser guard and E2E leaves remain named and literal; the legacy offline aggregator is a closed compatibility conclusion until retirement.
    - Phase 67 Android JVM proof runs on Ubuntu; Phase 68 emulator proof is manual advisory only and never a required PR leaf.
  </behavior>
  <action>Re-run the live snapshot verifier before any source-trigger edit. Move the offline-sync guard, Playwright, route-tour, and Android JVM PR jobs into Crosswake CI using the exact target IDs in the mapping table with existing example-host/browser fixtures and artifacts. Add `compat-offline-sync-e2e` only for the exact offline display context in the frozen snapshot. Delete the offline and Phase 67 source workflows after parity. Retain Phase 68 with workflow_dispatch only and remove its PR/push triggers; the emulator job remains advisory and outside every manifest authority set. Do not add device parity or treat it as proof authority. Docs-only mode must mark every browser/Android leaf explicitly irrelevant per D-08, D-18, D-19, and D-31.</action>
  <acceptance_criteria>
    - Full-proof schedules all browser/offline/Android JVM leaves; documentation-only schedules none of them and records exact irrelevance reasons.
    - Phase 68 has no pull_request or generic push trigger and no manifest required-leaf row.
    - Legacy offline context is still produced once and fails closed over all underlying leaves.
    - Snapshot verification passes immediately before offline, Phase 67, and Phase 68 trigger edits; drift stops with all original triggers intact.
  </acceptance_criteria>
  <verify>
    <automated>actionlint .github/workflows/crosswake-ci.yml .github/workflows/offline-sync-e2e-gate.yml .github/workflows/phase67-proof.yml .github/workflows/phase68-proof.yml &amp;&amp; python3 script/check_aggregator_result_semantics.py --self-test &amp;&amp; python3 script/check_ci_leaf_manifest.py --self-test</automated>
  </verify>
  <done>Browser/offline and Android JVM proof is single-run; device advisory remains non-authoritative.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Move iOS mirror, Phase 10, and Threadline documentation-contract proof</name>
  <precondition>A fresh read-only required-context snapshot verification still matches the frozen Plan 01 artifact after Task 2.</precondition>
  <read_first>
    .github/workflows/merge-blocking-ios-mirror-parity.yml
    .github/workflows/phase10-proof.yml
    .github/workflows/phase96-proof.yml
    .github/workflows/crosswake-ci.yml
    script/ci_leaf_manifest.json
    .planning/workstreams/quality-ratchet-release/phases/165-efficient-and-maintainable-ci/evidence/required-context-baseline.json
  </read_first>
  <files>.github/workflows/crosswake-ci.yml, .github/workflows/merge-blocking-ios-mirror-parity.yml, .github/workflows/phase10-proof.yml, .github/workflows/phase96-proof.yml, script/ci_leaf_manifest.json</files>
  <behavior>
    - Existing iOS mirror parity, Phase 10, and Threadline docs-contract proof runs literally once per applicable PR.
    - The focused public-doc contract may run in documentation_only only when the classifier records its affected family; unrelated executable leaves remain explicitly irrelevant.
  </behavior>
  <action>Re-run the live snapshot verifier before any source-trigger edit. Move the three PR jobs into Crosswake CI using the exact target IDs in the mapping table and remove their ordinary PR/generic push producers after parity. Add `compat-ios-mirror-parity` and `compat-phase96-threadline-docs-contract` only for their exact frozen-snapshot display contexts, outside umbrella proof leaves/control nodes. Delete the iOS-mirror and Phase 96 source files; delete Phase 10 because its workflow_dispatch repeats the same proof and owns no distinct authority. Assign iOS mirror parity to macOS only if its exact command invokes Apple tools; otherwise use Linux. Add manifest rows with exact family/remediation/irrelevance and ensure the documentation-only path schedules the focused docs-contract owner only for affected allowlisted public docs per D-10, D-18 through D-20, and D-32.</action>
  <acceptance_criteria>
    - Each moved display name has one PR producer and one exact manifest/static-needs row.
    - Documentation-only public-doc fixture schedules focused docs-contract proof and skips unrelated native/browser/root leaves explicitly.
    - Scheduled/manual Threadline advisory remains separate and non-required.
    - Snapshot verification passes immediately before all three source trigger removals; drift stops with the original producers intact.
  </acceptance_criteria>
  <verify>
    <automated>actionlint .github/workflows/crosswake-ci.yml .github/workflows/merge-blocking-ios-mirror-parity.yml .github/workflows/phase10-proof.yml .github/workflows/phase96-proof.yml &amp;&amp; python3 script/check_ci_leaf_manifest.py --self-test</automated>
  </verify>
  <done>The remaining package/mirror/docs-contract leaves are literal, single-run, and correctly classified.</done>
</task>

</tasks>

<threat_model>
Security posture: OWASP ASVS Level 1; every high or critical threat blocks execution until its listed mitigation passes.

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| PR code → native/browser runners | Untrusted code executes proof but must receive no privileged publication authority. |
| advisory native proof → required graph | Advisory device/emulator results must not promote support or merge authority. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-165-05 | Elevation of privilege | Crosswake CI leaves | high | mitigate | Read-only PR permissions, no publish/recovery secrets, trusted controller remains separate. |
| T-165-04 | Tampering | native/offline compatibility conclusions | high | mitigate | Closed results and exact manifest/static-needs parity. |
| T-165-13 | Spoofing | advisory-native results | medium | mitigate | Keep out of required manifest and branch-protection target; preserve advisory labels. |
</threat_model>

## Artifacts this phase produces

Deterministic exhaustive migration map:

| Source workflow/job(s) | Crosswake CI proof leaf ID(s) | Compatibility job ID → frozen display context | Source file disposition | Primary command surface |
|---|---|---|---|---|
| `native-behavioral-proof-gate.yml / android-package-unit, ios-package-unit, android-generated-shell-unit` | same three IDs | `compat-native-behavioral-proof` → `merge-blocking-native-behavioral-proof` | delete after parity | Gradle package tests; Swift package tests; generated Android shell verifier |
| `phase5-proof.yml / phase5-proof` | `phase5-proof` | none | delete after parity | `script/verify_phase5_example_hosts.sh` |
| `phase18-proof.yml / phase18-proof` split | `phase18-elixir-android-proof`, `phase18-ios-proof` | none | delete after parity | Phase 18 contract + generated Android proof; generated iOS shell proof |
| `phase79-proof.yml / merge-blocking-v5-proof` split | `phase79-android-proof`, `phase79-ios-proof` | none | delete after parity | generated Android and iOS shell commands on their owning OS |
| `offline-sync-e2e-gate.yml / guard-01-e2e-honesty, guard-02-prod-route-absence, e2e-proof, route-tour-proof` | same four IDs | `compat-offline-sync-e2e` → `merge-blocking-offline-sync-e2e` | delete after parity | structural honesty, prod-route absence, Playwright offline E2E, route-tour evidence |
| `phase67-proof.yml / android-jvm-proof` | `phase67-android-jvm-proof` | none | delete after parity | `./gradlew testDebugUnitTest` |
| `phase68-proof.yml / android-emulator-advisory` | none; advisory excluded | none | retain workflow_dispatch only | existing non-blocking emulator advisory |
| `merge-blocking-ios-mirror-parity.yml / merge-blocking-ios-mirror-parity` | `ios-mirror-parity-proof` | `compat-ios-mirror-parity` → `merge-blocking-ios-mirror-parity` | delete after parity | `script/check_ios_mirror_parity.sh` |
| `phase10-proof.yml / phase10-proof` | `phase10-proof` | none | delete after parity | SaaS profile, selective-native, and local-first verifiers |
| `phase96-proof.yml / merge-blocking-threadline-docs-contract-proof` | `phase96-threadline-docs-contract-proof` | `compat-phase96-threadline-docs-contract` → `merge-blocking Threadline docs-contract proof (hermetic)` | delete after parity | focused Threadline contract/support/doctor/generator test set |

Each proof row adds exact manifest fields; compatibility rows add exact `job_id`, `display_context`, and `needs_target` and perform no checkout/action setup. They are removed only after Plan 11 approval and Plan 12 retirement.

Scope rationale: this 13-file plan is atomic because every cross-runtime source is coupled to the same runner-placement audit, literal umbrella graph, frozen required-context compatibility set, and manifest. The three tasks are already separated by platform/authority cohort and each migration removes a source trigger only after its replacement passes. A cross-plan split would repeatedly mutate the same workflow/manifest and create extra producer-transition states; with zero calibration samples, the task/file boundaries override the low-confidence token estimate.

<verification>
Run all task gates plus `script/check_required_checks_registered.sh --local-only`; do not mutate live branch protection.
</verification>

<success_criteria>
Cross-runtime proof runs once on the right OS, docs-only PRs avoid it, advisories remain non-authoritative, and existing required contexts still report.
</success_criteria>

<output>
Create `.planning/workstreams/quality-ratchet-release/phases/165-efficient-and-maintainable-ci/165-08-SUMMARY.md` when done.
</output>
