---
phase: 167-documentation-and-pull-request-reconciliation
plan: 07
type: execute
wave: 7
depends_on: [167-06]
files_modified:
  - packages/crosswake-shell-core-ios/Tests/CrosswakeShellCoreTests/PackStoreTests.swift
  - .planning/workstreams/quality-ratchet-release/phases/167-documentation-and-pull-request-reconciliation/evidence/pr-105-resolution.json
autonomous: true
requirements: [DOC-03]
user_setup:
  - service: github
    why: "Refresh, squash/rebase, and merge PR #105"
    dashboard_config:
      - task: "Authenticate gh with repository branch-update, pull-request, and merge authority"
        location: "gh auth login or existing GitHub CLI credential"
estimate:
  tokens: 20000
  raw_tokens: 20000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "PR #105 is rebased after #110, squashed to one intent commit, retains only waiter-closure clarity, passes the current Swift package and Crosswake CI, and merges per D-24."
    - "Exact fresh head/base/check state gates every branch/merge write; the narrow cleanup does not widen native behavior or Android scope per D-27 and D-28."
    - "PRs #115 and #57 remain untouched release approval surfaces in this plan per D-25 and D-26."
    - "Executor/evidence commits remain on unprotected `agent-phase167-fixforward`; after #105 merges and its receipt commits, the freshly verified protected-default commit is merged into that phase branch so Plan 08 inherits #105's exact tree and all prior phase ancestry."
  artifacts:
    - path: "packages/crosswake-shell-core-ios/Tests/CrosswakeShellCoreTests/PackStoreTests.swift"
      provides: "actor-isolated deterministic PackStore waiter closure ownership"
    - path: ".planning/workstreams/quality-ratchet-release/phases/167-documentation-and-pull-request-reconciliation/evidence/pr-105-resolution.json"
      provides: "privacy-safe one-intent tested-candidate and final-reachability receipt for D-24"
  key_links:
    - from: "ControlledPackProvider waiter queues"
      to: "PackStore reconciliation/invalidation tests"
      via: "entry barrier then single continuation resume"
      pattern: "resumeStatusWaiters|resumeInvalidationWaiters"
    - from: "PR #105 exact refreshed head"
      to: "Crosswake CI"
      via: "same-head successful required check before merge"
      pattern: "Crosswake CI"
    - from: "fresh protected default after #105 merge"
      to: "Plan 08 phase-branch payload"
      via: "no-rewrite merge whose first parent is the receipt commit and second parent is the freshly fetched default"
      pattern: "git merge --no-ff --no-edit FETCH_HEAD"
  prohibitions:
    - requirement_id: DOC-03
      category: values
      statement: "MUST NOT use a narrow accepted cleanup as a vehicle for native feature breadth or leave it indefinitely ambiguous."
      status: unresolved
      flagged: true
---

<objective>
Resolve narrow PR #105 as one rebased/squashed intent commit with current Swift and umbrella proof per D-24, D-27, and D-28.

Purpose: Clear the accepted PackStore test-maintainability change without widening runtime behavior or carrying stale branch state into release readiness.
Output: One focused test commit on default, merged disposition, and bounded post-action facts.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/workstreams/quality-ratchet-release/phases/167-documentation-and-pull-request-reconciliation/167-CONTEXT.md
@.planning/workstreams/quality-ratchet-release/phases/167-documentation-and-pull-request-reconciliation/167-06-SUMMARY.md
@packages/crosswake-shell-core-ios/Tests/CrosswakeShellCoreTests/PackStoreTests.swift
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Rebase and squash the PackStore waiter-closure cleanup</name>
  <precondition>`agent-phase167-fixforward` is checked out and unprotected with no tracked/staged residue, `gh auth status` succeeds, Swift is available, and remote default contains the verified #110 disposition; otherwise halt before remote mutation.</precondition>
  <read_first>
    - packages/crosswake-shell-core-ios/Tests/CrosswakeShellCoreTests/PackStoreTests.swift
    - packages/crosswake-shell-core-ios/Sources/CrosswakeShellCore/PackStore.swift
    - packages/crosswake-shell-core-ios/Package.swift
    - .planning/workstreams/quality-ratchet-release/phases/167-documentation-and-pull-request-reconciliation/167-RESEARCH.md
  </read_first>
  <files>packages/crosswake-shell-core-ios/Tests/CrosswakeShellCoreTests/PackStoreTests.swift</files>
  <behavior>
    - Controlled provider waiter arrays retain actor isolation, resume each eligible continuation once, and remove resumed closures deterministically.
    - Reconciliation/invalidation tests wait on explicit entry counts rather than timing.
    - The branch contains one intent commit and no production/native behavior change.
  </behavior>
  <action>Keep the executor checkout on `agent-phase167-fixforward` and perform #105's branch rewrite in an isolated worktree. Refresh #105's exact head/base/check state and current default immediately before rebasing. Squash its three commits into one intent commit limited to `PackStoreTests.swift`. Preserve actor isolation, separate status/invalidation queues, explicit entry barriers, and deterministic continuation ownership; do not modify production PackStore behavior or add native/Android scope. Quote structured `gh`/git inputs, never commit on or check out local `main`, and stop on any head/base/check mismatch per D-21, D-24, and D-28.</action>
  <acceptance_criteria>
    - The rebased branch is based on the exact fresh default SHA and contains one intent commit.
    - Diff is limited to closure/waiter clarity in the one test file.
    - Swift tests preserve deterministic entry barriers and single-resume actor semantics.
    - Any remote state mismatch stops before push.
  </acceptance_criteria>
  <verify>
    <automated>swift test --package-path packages/crosswake-shell-core-ios</automated>
    <fails_when>non-zero exit, or the Swift test summary reports 0 tests executed</fails_when>
  </verify>
  <done>PR #105 carries one current, narrow, behavior-preserving intent commit with focused Swift proof.</done>
</task>

<task type="auto">
  <name>Task 2: Require current umbrella proof and merge PR #105</name>
  <reversibility rating="costly">D-24 already locks the merge after proof; undoing the remote merge requires a reviewed revert even though the code change is narrow, so no redundant decision checkpoint is added.</reversibility>
  <precondition>The isolated one-commit #105 branch passes the Swift package, `agent-phase167-fixforward` remains the executor branch, and `gh auth status` still succeeds.</precondition>
  <read_first>
    - packages/crosswake-shell-core-ios/Tests/CrosswakeShellCoreTests/PackStoreTests.swift
    - .github/workflows/crosswake-ci.yml
    - .planning/workstreams/quality-ratchet-release/phases/167-documentation-and-pull-request-reconciliation/167-CONTEXT.md
  </read_first>
  <files>.planning/workstreams/quality-ratchet-release/phases/167-documentation-and-pull-request-reconciliation/evidence/pr-105-resolution.json</files>
  <behavior>
    - After #105 merges and the validated receipt commits on `agent-phase167-fixforward`, run exactly `git fetch --no-tags origin main` and then `git merge --no-ff --no-edit FETCH_HEAD` while that unprotected phase branch remains checked out.
    - The integration merge's first parent is the receipt commit and its second parent is the freshly fetched protected-default commit; the latter is the live-validated #105 merge authority and supplies the exact protected-default `PackStoreTests.swift` blob to the phase branch.
    - The integrated phase branch retains `367f5b5491384594a652d137a03933fa3a89418a` and the receipt commit as ancestors, has a clean index/tracked worktree, and preserves exactly the three runtime files and their recorded hashes before Plan 08 or any optional local-`main` ref advance.
  </behavior>
  <action>Immediately refresh #105 head/base/checks, compare against the tested commit and current remote default, and require successful current `Crosswake CI` for that exact head. Capture a `packstore_waiter_clarity` receipt that binds the one intent commit and sole changed path to the full tested head/base OIDs. Merge #105, then re-read its final state, merge commit, and remote default and run the shared live resolution validator. Commit the receipt on `agent-phase167-fixforward`. With no other executor commit pending, fetch and validate fresh protected default again, then run exactly `git merge --no-ff --no-edit FETCH_HEAD` on the still-checked-out phase branch; never rewrite or squash its history. Prove the integration merge's parent order, fresh-default ancestry/tree authority, exact `PackStoreTests.swift` blob, prior phase/receipt ancestry, clean index/tracked state, and unchanged runtime hashes. Only after that proof may the local `main` ref optionally advance to the same fresh-default OID without checkout; remain on the integrated phase branch for the summary and Plan 08. Retain only allowlisted receipt facts. Do not touch #115/#57 or change `git.allow_default_branch_commits`.</action>
  <acceptance_criteria>
    - #105 is merged, not deferred, with the exact tested one-commit intent.
    - Current Crosswake CI succeeds on the exact merged head.
    - Merge commit is reachable from freshly re-read remote default.
    - The phase branch has a no-rewrite integration merge with receipt first parent and fresh protected default second parent, so its Plan 08 payload contains #105's merged tree and prior phase ancestry.
    - Tracked/index state is clean and the three runtime paths remain the only untracked files with hashes `05b25ad604490dba4c12df84c624d04b1c7bae454ecd78a262dd1b97b68c1a28`, `fd4c22c0f07449f02acc487c3100eed7a10edb1382d63807dd4003a54bfd2943`, and `6cf0413c5cc52eb4f9c10ba497f82614608a54659e10bd48172bad2d9765dff4`.
    - Retained facts contain no remote prose, log, URL, secret, or private value.
  </acceptance_criteria>
  <verify>
    <automated>swift test --package-path packages/crosswake-shell-core-ios &amp;&amp; python3 script/check_phase167_pr_dispositions.py --verify-resolution .planning/workstreams/quality-ratchet-release/phases/167-documentation-and-pull-request-reconciliation/evidence/pr-105-resolution.json --live &amp;&amp; git fetch --no-tags origin main &amp;&amp; sh -c 'receipt=.planning/workstreams/quality-ratchet-release/phases/167-documentation-and-pull-request-reconciliation/evidence/pr-105-resolution.json; if ! receipt_oid="$(git log -1 --format=%H -- "$receipt")"; then exit 1; fi; if ! branch="$(git branch --show-current)"; then exit 1; fi; if ! first_parent="$(git rev-parse HEAD^1)"; then exit 1; fi; if ! second_parent="$(git rev-parse HEAD^2)"; then exit 1; fi; if ! fresh_default="$(git rev-parse FETCH_HEAD)"; then exit 1; fi; if ! untracked="$(git ls-files --others --exclude-standard)"; then exit 1; fi; expected="$(printf "%s\n" .planning/workstreams/quality-ratchet-release/config.json .planning/workstreams/quality-ratchet-release/milestone.lock .planning/workstreams/quality-ratchet-release/state.json)"; test "$branch" = agent-phase167-fixforward &amp;&amp; test "$first_parent" = "$receipt_oid" &amp;&amp; test "$second_parent" = "$fresh_default" &amp;&amp; git merge-base --is-ancestor 367f5b5491384594a652d137a03933fa3a89418a HEAD &amp;&amp; git merge-base --is-ancestor "$receipt_oid" HEAD &amp;&amp; git diff --quiet FETCH_HEAD HEAD -- packages/crosswake-shell-core-ios/Tests/CrosswakeShellCoreTests/PackStoreTests.swift &amp;&amp; git diff --quiet &amp;&amp; git diff --cached --quiet &amp;&amp; test "$untracked" = "$expected" &amp;&amp; test "$(shasum -a 256 .planning/workstreams/quality-ratchet-release/config.json)" = "05b25ad604490dba4c12df84c624d04b1c7bae454ecd78a262dd1b97b68c1a28  .planning/workstreams/quality-ratchet-release/config.json" &amp;&amp; test "$(shasum -a 256 .planning/workstreams/quality-ratchet-release/milestone.lock)" = "fd4c22c0f07449f02acc487c3100eed7a10edb1382d63807dd4003a54bfd2943  .planning/workstreams/quality-ratchet-release/milestone.lock" &amp;&amp; test "$(shasum -a 256 .planning/workstreams/quality-ratchet-release/state.json)" = "6cf0413c5cc52eb4f9c10ba497f82614608a54659e10bd48172bad2d9765dff4  .planning/workstreams/quality-ratchet-release/state.json"'</automated>
    <fails_when>non-zero exit; executor branch is not agent-phase167-fixforward or is protected; the Swift test summary reports 0 tests; missing or mismatched full head/base/default/merge OIDs; Crosswake CI absent, non-successful, or attached to another head; commit count is not exactly one; changed scope is not exactly PackStoreTests.swift; #105 is not MERGED; integration merge absent or rewritten; parent one is not the receipt commit; parent two is not freshly fetched protected default; RED/prior phase or receipt ancestry is lost; phase-branch PackStoreTests.swift differs from fresh protected default; tracked/index/runtime/untracked state drifts; local main updates before integration proof or with a commit pending; any executor commit lands on main; or merge commit is not reachable from fresh default</fails_when>
  </verify>
  <done>PR #105 is merged and unambiguous on current default with bounded post-action facts ready for final evidence.</done>
</task>

</tasks>

<threat_model>
Security posture: OWASP ASVS Level 1; high/critical findings block execution.

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GitHub state → rebase/squash/merge | Mutable remote state could change the bytes or base being approved. |
| concurrent Swift tasks → continuations | Incorrect closure ownership could double-resume or hang proof. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-167-20 | Tampering | PR #105 mutation, phase integration, and protected-main execution | high | mitigate | Keep execution on the unprotected phase branch, isolate the guarded #105 rewrite, require same-head CI, then merge freshly fetched protected default after the receipt with exact parent/tree/ancestry/runtime proof before Plan 08 or an optional local-`main` ref advance. |
| T-167-21 | Denial of service | controlled waiter queues | medium | mitigate | Actor-isolated entry barriers and single-resume Swift package tests. |
| T-167-22 | Information disclosure | GitHub observations | medium | mitigate | Structured allowlisted fields only; no prose/log/URL retention. |
</threat_model>

## Artifacts this phase produces

- New phase-local bounded `evidence/pr-105-resolution.json` receipt; no new production source path, public API, native capability, or Android parity.
- One squashed intent commit changing only `PackStoreTests.swift`.
- Remote disposition for #105: `merged`; Plan 08 owns the aggregate final evidence.

<verification>
Require the full Swift package, exact-head current Crosswake CI, final MERGED state, and default-branch reachability.
</verification>

<success_criteria>
The accepted waiter-closure cleanup is merged as one current intent commit and no stale PR ambiguity or native breadth remains.
</success_criteria>

<output>
Create `.planning/workstreams/quality-ratchet-release/phases/167-documentation-and-pull-request-reconciliation/167-07-SUMMARY.md` when done.
</output>
