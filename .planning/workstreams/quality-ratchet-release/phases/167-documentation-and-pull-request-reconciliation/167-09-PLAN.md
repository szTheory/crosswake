---
phase: 167-documentation-and-pull-request-reconciliation
plan: "09"
type: execute
wave: 9
depends_on: [167-08]
files_modified:
  - lib/crosswake/capability_map.ex
  - test/crosswake/capability_map/capability_map_test.exs
autonomous: true
gap_closure: true
requirements: [DOC-01]
estimate:
  tokens: 9000
  raw_tokens: 9000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "The only valid complete adoption-authority tuples are the canonical `:available`, `:reference_evidence`, and `:blocked` claims; every other subject/binding/state/proof/date/runtime/promotion combination fails closed per D-16, D-17, and D-20 (DOC-01)."
    - "An `:available` claim cannot carry first-adopter subject authority, a missing or omitted source binding, a missing or omitted proof source, or `support_promotion: true` per D-18 and D-20 (DOC-01)."
    - "Both existing generated renderers still consume `first_adopter_claims/0`, documentation synchronization remains byte-clean, and the parked first-adopter lane remains private, non-transferable, blocked, and untouched per D-01, D-02, D-03, D-14, D-19, and D-21."
  artifacts:
    - path: "lib/crosswake/capability_map.ex"
      provides: "One closed complete-tuple validator for all three adoption authority states."
    - path: "test/crosswake/capability_map/capability_map_test.exs"
      provides: "Discriminating available-state cross-product and all-state mutation regressions."
  key_links:
    - from: "Crosswake.CapabilityMap.first_adopter_claims/0"
      to: "Crosswake.CapabilityMap.validate_adoption_claim!/1"
      via: "Every canonical claim is validated before either renderer can consume it."
      pattern: "Enum.map(&validate_adoption_claim!/1)"
    - from: "Crosswake.CapabilityMap.validate_adoption_claim!/1"
      to: "complete adoption-authority tuple set"
      via: "Exact membership across subject, binding, state, proof, recorded date, runtime line, and promotion."
      pattern: "validate_complete_authority_tuple"
    - from: "lib/crosswake/capability_map.ex"
      to: "guides/capability_map.md and guides/support_matrix.md"
      via: "Existing capability and support renderers consume the validated `first_adopter_claims/0` result; `mix crosswake.docs.sync --check` proves both projections remain current."
      pattern: "first_adopter_claims"
  prohibitions:
    - requirement_id: DOC-01
      category: safety
      statement: "MUST NOT represent an available or promoting claim when its exact subject, source binding, or proof authority is absent or incompatible."
      status: flagged_unverified
      flagged: true
    - requirement_id: DOC-02
      category: privacy
      statement: "MUST NOT transfer retained reference-host evidence to first-adopter activation or expose adopter identity, host-private facts, payloads, credentials, account/device identifiers, tokens, private URLs, host flags, or proprietary taxonomy."
      status: flagged_unverified
      flagged: true
flagged_assumptions:
  - id: FA-DOC-01
    requirement_id: DOC-01
    category: unclassified
    status: unresolved
    flagged: true
    probe: "Spec-less edge probe returned DOC-01 unclassified."
    handling: "Use the binding verification gap and D-20 as executable acceptance authority; preserve the flag rather than inventing another predicate."
  - id: FA-DOC-02
    requirement_id: DOC-02
    category: unclassified
    status: unresolved
    flagged: true
    probe: "Spec-less edge probe returned DOC-02 unclassified."
    handling: "Treat non-transfer and privacy as preservation constraints, run the existing privacy proof, and do not edit parked workstream state."
  - id: FA-DOC-03
    requirement_id: DOC-03
    category: adjacency_empty_ordering_concurrency
    status: unresolved
    flagged: true
    probe: "Spec-less edge probe left DOC-03 adjacency, empty, ordering, and concurrency unresolved."
    handling: "Do not translate these prompts into new acceptance predicates because this gap does not read or mutate PR state, ordering, evidence, or concurrency; Plan 167-08 and 167-VERIFICATION.md remain the verified authority."
---

<objective>
Close the Plan 167-01 execution miss by replacing partial state checks with one exact, complete adoption-authority tuple contract and by proving the previously accepted available/missing-authority/promoting cross-product cannot pass per D-16 through D-21.

Purpose: Make DOC-01's executable owner genuinely fail closed before its already-current public projections are trusted.
Output: A closed three-tuple validator, discriminating cross-product regressions, and fresh focused renderer/docs-sync/privacy proof without reopening DOC-02, DOC-03, or Phase 168.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/ADR-FIRST-B2C-ADOPTER.md
@.planning/FIRST-B2C-ADOPTER-ROUTE-POLICY-MAP.md
@.planning/workstreams/quality-ratchet-release/REQUIREMENTS.md
@.planning/workstreams/quality-ratchet-release/ROADMAP.md
@.planning/workstreams/quality-ratchet-release/STATE.md
@.planning/workstreams/first-b2c-adopter-readiness/STATE.md
@.planning/workstreams/quality-ratchet-release/phases/167-documentation-and-pull-request-reconciliation/167-CONTEXT.md
@.planning/workstreams/quality-ratchet-release/phases/167-documentation-and-pull-request-reconciliation/167-RESEARCH.md
@.planning/workstreams/quality-ratchet-release/phases/167-documentation-and-pull-request-reconciliation/167-PATTERNS.md
@.planning/workstreams/quality-ratchet-release/phases/167-documentation-and-pull-request-reconciliation/167-VERIFICATION.md
@.planning/workstreams/quality-ratchet-release/phases/167-documentation-and-pull-request-reconciliation/167-01-SUMMARY.md
@lib/crosswake/capability_map.ex
@test/crosswake/capability_map/capability_map_test.exs

<interfaces>
- `Crosswake.CapabilityMap.first_adopter_claims/0` returns three maps and validates each through `validate_adoption_claim!/1` before the capability and support renderers consume them.
- The exact reusable-contract authority tuple is `:crosswake_contract`, `:repository_bound`, `:available`, `:repository_contract`, no recorded date, no iOS runtime line, and `support_promotion: false`.
- The exact retained-reference authority tuple is `:reference_host`, `:source_bound`, `:reference_evidence`, `:physical_device`, `2026-08-27`, iOS runtime line `26.6`, and `support_promotion: false`.
- The exact first-adopter authority tuple is `:first_adopter`, `:required_missing`, `:blocked`, `:required_missing`, no recorded date, no iOS runtime line, and `support_promotion: false`.
- `validate_adoption_claim!/1` already validates the closed evidence-subject, source-binding, and activation-state vocabularies and raises a bounded `ArgumentError`; this plan tightens cross-field completeness without changing the public function or canonical claim bytes.
</interfaces>
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Trace available authority through the exact complete-tuple gate</name>
  <reversibility rating="reversible">The change tightens one internal validator and its focused tests without changing public claim labels, map keys, or renderer interfaces.</reversibility>
  <read_first>
    - AGENTS.md
    - .planning/workstreams/quality-ratchet-release/phases/167-documentation-and-pull-request-reconciliation/167-VERIFICATION.md
    - .planning/workstreams/quality-ratchet-release/phases/167-documentation-and-pull-request-reconciliation/167-01-SUMMARY.md
    - lib/crosswake/capability_map.ex
    - test/crosswake/capability_map/capability_map_test.exs
  </read_first>
  <files>lib/crosswake/capability_map.ex, test/crosswake/capability_map/capability_map_test.exs</files>
  <behavior>
    - The canonical reusable-contract `:available` claim remains accepted with its exact repository subject, binding, proof source, nil date/runtime, and non-promoting posture.
    - A cross-product over first-adopter/crosswake subjects, repository/missing/omitted bindings, repository/missing/omitted proof sources, and both promotion booleans rejects every `:available` tuple except the single canonical reusable-contract tuple.
    - Deleting `source_binding` or `proof_source`, assigning nil, or assigning `:required_missing` cannot be hidden by `activation_state: :available`; `support_promotion: true` is rejected even when every other available field is canonical.
    - Rejection uses a fixed rule-oriented error and never echoes claim fields, statements, boundaries, or supplied values per D-20 and D-21.
  </behavior>
  <action>Start with a failing ExUnit mutation matrix built from the canonical reusable-contract claim returned by `first_adopter_claims/0`; include the exact fail-open tuple reproduced by 167-VERIFICATION.md, explicit key deletion cases, and the available-state source/proof/promotion cross-product. Require the unmodified canonical available tuple to pass so the test distinguishes closure from blanket rejection. In `validate_adoption_claim!/1`, preserve the existing closed-vocabulary checks and add a private `validate_complete_authority_tuple/2` gate over exactly these seven authority fields: `evidence_subject`, `source_binding`, `activation_state`, `proof_source`, `recorded_on`, `ios_runtime_line`, and `support_promotion`. Admit the exact reusable-contract tuple documented in `<interfaces>` and reject every other `:available` tuple with a fixed rule identifier. Keep `first_adopter_claims/0`, public labels, renderer APIs, claim statements/boundaries, the pinned reference date/runtime, and all canonical claim maps unchanged per D-01, D-02, D-03, D-14, and D-16 through D-21.</action>
  <acceptance_criteria>
    - The verifier's available + first-adopter + missing-binding + missing-proof + promoting tuple raises `ArgumentError` after a discriminating RED failure under the old implementation.
    - Every noncanonical available tuple in the subject/binding/proof/promotion cross-product fails, including omitted and nil authority, while the one exact canonical available tuple succeeds.
    - Failure output is bounded to stable rule identifiers and contains no supplied claim values.
    - No renderer, guide, parked-state, PR, evidence, workflow, package, release, Android, or Phase 168 file is changed.
  </acceptance_criteria>
  <verify>
    <automated>mix test test/crosswake/capability_map/capability_map_test.exs</automated>
    <fails_when>the command exits non-zero, ExUnit reports zero tests, the reproduced fail-open tuple is accepted, any noncanonical available cross-product member is accepted, or the canonical available tuple is rejected</fails_when>
  </verify>
  <done>The single available happy path and its hostile missing-authority/promoting variants pass through the same production validator, with only the canonical reusable-contract authority accepted.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Close all three authority states and re-prove both projections</name>
  <read_first>
    - .planning/workstreams/quality-ratchet-release/phases/167-documentation-and-pull-request-reconciliation/167-CONTEXT.md
    - .planning/workstreams/quality-ratchet-release/phases/167-documentation-and-pull-request-reconciliation/167-VERIFICATION.md
    - lib/crosswake/capability_map.ex
    - test/crosswake/capability_map/capability_map_test.exs
    - test/crosswake/capability_map/renderer_test.exs
    - test/crosswake/support_matrix/renderer_test.exs
    - lib/mix/tasks/crosswake.docs.sync.ex
    - lib/crosswake/planning/first_adopter_context.ex
  </read_first>
  <files>lib/crosswake/capability_map.ex, test/crosswake/capability_map/capability_map_test.exs</files>
  <behavior>
    - The complete validator admits exactly the three canonical authority tuples documented in `<interfaces>` and no fourth tuple.
    - Table-driven mutations of every authority axis for `:reference_evidence` and `:blocked` reject changed, missing, or omitted subject/binding/proof/date/runtime/promotion values while preserving their canonical claims.
    - The partial `validate_reference_evidence` and `validate_blocked_promotion` behavior is consolidated behind the exact complete-tuple gate so no alternate validator path can accept an incomplete state.
    - Both existing renderer suites, no-write documentation synchronization, and destination-aware privacy scanning pass without downstream source or generated-byte edits.
  </behavior>
  <action>Extend the RED matrix so each of the three canonical claims first proves its exact tuple is accepted, then mutate each authority field independently to an incompatible closed value, nil, or omitted key and require rejection. Include the D-20 reference-host subject/binding/physical/date/runtime/non-transfer constraints and the blocked first-adopter missing-authority/non-promotion constraints; use neutral sentinels only to prove error non-echoing. Complete `validate_complete_authority_tuple/2` with exactly the retained-reference and blocked tuples documented in `<interfaces>`, then remove or fold the two partial state-specific validators so a claim cannot bypass complete validation through branch ordering. Keep validation after the closed vocabulary checks, return the original map unchanged on success, and retain fixed non-echoing rule identifiers on failure. Do not address the non-blocking canonical-row, Phase 41 remediation, or support-owner wording warnings; they are separable from this blocker and would broaden the gap. After focused tests pass, run both renderer suites, `mix crosswake.docs.sync --check`, and the existing adoption-context privacy scan without writing generated guides or parked state.</action>
  <acceptance_criteria>
    - Exactly three complete authority tuples are accepted: reusable available contracts, dated/source-bound reference evidence, and blocked first-adopter activation.
    - Every mutation of subject, binding, state, proof, date, runtime line, or promotion away from those tuples is rejected with non-echoing output.
    - Capability and support renderer tests pass from the same validated `first_adopter_claims/0` owner, and docs sync reports no generated drift.
    - The privacy scan passes; DOC-02/DOC-03 verified artifacts, parked state, PR dispositions, release-only deferrals, Android posture, and Phase 168 boundaries remain byte-untouched.
  </acceptance_criteria>
  <verify>
    <automated>mix test test/crosswake/capability_map test/crosswake/support_matrix/renderer_test.exs &amp;&amp; mix crosswake.docs.sync --check &amp;&amp; mix crosswake.adoption_context.scan &amp;&amp; git diff --check -- lib/crosswake/capability_map.ex test/crosswake/capability_map/capability_map_test.exs</automated>
    <fails_when>any command exits non-zero, an ExUnit run reports zero tests, any noncanonical authority tuple is accepted, either generated guide drifts, the privacy scanner reports a rule/path, or Git reports a whitespace error in the two modified files</fails_when>
  </verify>
  <done>DOC-01's executable claim owner is closed across all three states and its two generated projections plus privacy boundary are freshly re-proved without reopening already-verified DOC-02 or DOC-03 work.</done>
</task>

</tasks>

<threat_model>
Security posture: OWASP ASVS Level 1; high/critical findings block execution.

## Trust Boundaries

| Boundary | Description |
|---|---|
| caller-supplied claim map → `validate_adoption_claim!/1` | Map keys and values may attempt to claim support without complete subject, source, proof, date/runtime, or promotion authority. |
| retained reference evidence → public claim renderers | A dated reference-host result must never become present first-adopter activation or transferable support. |
| rejected claim → exception/test/CI output | Invalid data may contain sensitive values and must not be echoed into logs or proof. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-167-09-01 | Tampering / Elevation of Privilege | adoption authority validator | high | mitigate | Tasks 1-2 replace partial checks with membership in the exact three-tuple set and exercise available-state cross-products plus all-axis mutations. |
| T-167-09-02 | Spoofing / Repudiation | reference and first-adopter claim attribution | high | mitigate | Exact subject, binding, proof, date/runtime, and non-promotion values are required together before either renderer consumes a claim. |
| T-167-09-03 | Information Disclosure | validation errors and privacy proof | high | mitigate | Fixed rule identifiers never interpolate the claim; Task 2 runs the destination-aware privacy scanner after focused tests. |
| T-167-09-SC | Tampering | package supply chain | low | accept | No dependency, action, external service, or package installation is planned; only tracked Elixir source and its focused test are changed. |
</threat_model>

## Adversarial Prohibition Recall

The recall pass retained only the two bespoke value/safety constraints in `must_haves.prohibitions`: fail-closed complete authority and privacy-safe non-transfer. Both remain descriptor-less and `flagged_unverified` for the executor/verifier. Routine null, omission, ordering, and branch-bypass cases are concrete test behavior rather than additional prose prohibitions; no human checkpoint is created for this fully automatable gap.

## Artifacts this phase produces

- No new file or public API is created.
- New private production identifier: `Crosswake.CapabilityMap.validate_complete_authority_tuple/2`.
- New focused test: `D-20 available authority cross-product rejects missing source, missing proof, and promotion`.
- New focused test: `D-20 complete adoption-authority tuple set is closed for all activation states`.
- Existing files changed: `lib/crosswake/capability_map.ex` and `test/crosswake/capability_map/capability_map_test.exs` only.

<source_coverage_audit>
| Source | ID | Contract | Coverage | Status |
|---|---|---|---|---|
| GOAL | Phase 167 | Maintainers see one current supported-behavior account without disturbing parked adopter work. | Task 1 closes the executable source; Task 2 re-proves both projections and privacy while prohibiting parked/PR edits. | COVERED |
| REQ | DOC-01 | Public support/capability truth agrees with verified behavior. | Tasks 1-2 close the failed complete-tuple truth and rerun capability, renderer, docs-sync, and privacy proof. | COVERED |
| REQ | DOC-02 | Parked adopter work remains codename-only, resumable, and blocked on real authority. | Already verified by Plans 167-01/04/08; this plan modifies no parked artifact and Task 2 reruns privacy/non-transfer proof. | COVERED (preserved) |
| REQ | DOC-03 | Every open PR has an unambiguous disposition. | Already verified by Plans 167-05 through 167-08; this plan reads or mutates no PR, receipt, evidence, branch, or remote state. | COVERED (preserved) |
| RESEARCH | complete claim modeling | Subject, binding, activation, proof, date/runtime, and promotion must be validated together before rendering. | Tasks 1-2 implement the exact three-tuple set in the existing owner with no new dependency or truth store. | COVERED |
| RESEARCH | generated/public/privacy boundary | Generated bytes stay projections, authored/privacy truth stays semantic and non-echoing. | Task 2 uses existing renderer tests, no-write docs sync, and privacy scan without downstream edits. | COVERED |
| RESEARCH | warning and phase boundary | Do not conflate separable warnings, PR work, or Phase 168 release work with the blocker. | Task 2 explicitly excludes all three warnings, PR artifacts, release operations, and adjacent scope. | COVERED |
| CONTEXT | D-01, D-02, D-03, D-14 | One executable owner feeds deterministic projections with semantic—not archaeological—tests. | Task 1 preserves the owner/interface; Task 2 re-proves the existing projections. | COVERED |
| CONTEXT | D-16, D-17, D-18, D-19, D-20, D-21 | Closed internal dimensions, three separate claims, atomic consistency, public/durable terminology, impossible-state rejection, and privacy. | Tasks 1-2 enforce exact tuple completeness and preserve current downstream bytes/state with non-echoing proof. | COVERED |
| CONTEXT | D-04 through D-13, D-15, D-22 through D-32 | Authored docs, sync/CI, PR dispositions, reader quality, and docs-only routing. | Delivered and verified by Plans 167-02 through 167-08; no source in those concerns is changed by this gap. | COVERED (existing) |
| CONTEXT | Deferred Ideas | Phase 168 candidate/release work, new docs UI/taxonomy, Android breadth, adopter activation, and product/mobile breadth remain excluded. | No task or file enters those areas. | EXCLUDED (not a gap) |
| VERIFICATION | blocking gap | `:available` currently accepts first-adopter/missing-source/missing-proof/promoting authority. | Task 1 reproduces and closes it; Task 2 proves the complete three-state set and downstream wiring. | COVERED |
</source_coverage_audit>

<verification>
The Task 2 automated command is the complete gap gate: it reruns focused capability behavior, both renderers, no-write documentation synchronization, destination-aware privacy proof, and scoped diff hygiene. No manual UAT or human verification is required.
</verification>

<success_criteria>
- The exact fail-open tuple recorded in 167-VERIFICATION.md is rejected.
- Only the three canonical complete adoption-authority tuples are accepted; missing/omitted source or proof and any available-state promotion are impossible.
- Both generated projections remain byte-current from the same validated owner, and privacy scanning passes without editing parked or public artifacts.
- DOC-02 and DOC-03 remain verified, Phase 168 remains unopened, and no non-blocking warning cleanup is mixed into the blocker closure.
</success_criteria>

<output>
Create `.planning/workstreams/quality-ratchet-release/phases/167-documentation-and-pull-request-reconciliation/167-09-SUMMARY.md` when done, recording the RED reproduction, exact accepted-tuple count, cross-product mutation count, focused renderer/docs-sync/privacy results, and confirmation that only the two declared files changed.
</output>
