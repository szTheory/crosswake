---
phase: 167-documentation-and-pull-request-reconciliation
plan: 05
type: execute
wave: 5
depends_on: [167-04]
files_modified:
  - script/check_phase167_pr_dispositions.py
  - .planning/workstreams/quality-ratchet-release/phases/167-documentation-and-pull-request-reconciliation/evidence/pr-121-resolution.json
  - .github/actions/setup-android-jvm/action.yml
  - .github/workflows/crosswake-ci.yml
  - .github/workflows/phase68-proof.yml
  - .github/workflows/release-please.yml
  - test/crosswake/proof/phase165_ci_integrity_test.exs
autonomous: true
requirements: [DOC-03]
user_setup:
  - service: github
    why: "Refresh, update, and merge or supersede PR #121"
    dashboard_config:
      - task: "Authenticate gh with repository branch-update, pull-request, and merge authority"
        location: "gh auth login or existing GitHub CLI credential"
estimate:
  tokens: 28000
  raw_tokens: 28000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "PR #121 is rebased onto fresh default-branch authority, all seven setup-java uses share the execution-time official immutable v6 SHA, the assertion matches, current Crosswake CI passes, and the PR is merged per D-22."
    - "If and only if the Dependabot branch cannot carry the coherent seven-use change, one manual replacement is verified/merged and #121 is explicitly closed as superseded with its rationale preserved per D-22."
    - "Every remote write is preceded by an immediate exact head/base/check refresh and stops on mismatch; #115 and #57 receive no mutation in this plan per D-25 through D-28."
    - "PR work occurs in an isolated worktree/branch so the committed Plan 01-04 reconciliation is neither overwritten nor accidentally included in #121."
  artifacts:
    - path: "script/check_phase167_pr_dispositions.py"
      provides: "shared closed-schema exact-SHA PR resolution validator introduced before Plans 05-08 use it"
    - path: ".planning/workstreams/quality-ratchet-release/phases/167-documentation-and-pull-request-reconciliation/evidence/pr-121-resolution.json"
      provides: "privacy-safe tested-candidate and final-reachability receipt for D-22"
    - path: "test/crosswake/proof/phase165_ci_integrity_test.exs"
      provides: "repository-wide exact setup-java v6 pin/count/coherence assertion"
    - path: ".github/actions/setup-android-jvm/action.yml"
      provides: "one of seven coherently pinned live setup-java uses"
  key_links:
    - from: "test/crosswake/proof/phase165_ci_integrity_test.exs"
      to: ".github"
      via: "repository-wide seven-use full-SHA assertion"
      pattern: "actions/setup-java@"
    - from: "PR #121 exact refreshed head"
      to: "Crosswake CI"
      via: "same-head successful required check before merge"
      pattern: "Crosswake CI"
  prohibitions:
    - requirement_id: DOC-03
      category: safety
      statement: "MUST NOT mutate a pull request after its observed head, base, or required-check state has changed."
      status: unresolved
      flagged: true
---

<objective>
Resolve ordinary PR #121 under fresh optimistic-concurrency guards and one coherent immutable setup-java v6 contract per D-22, D-27, and D-28.

Purpose: Remove the dependency-maintenance ambiguity before package/docs and Swift changes rebase on the new default branch.
Output: Seven coherent immutable action uses, exact regression, merged/superseded disposition, and bounded post-action facts for final evidence.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/workstreams/quality-ratchet-release/phases/167-documentation-and-pull-request-reconciliation/167-CONTEXT.md
@.planning/workstreams/quality-ratchet-release/phases/167-documentation-and-pull-request-reconciliation/167-04-SUMMARY.md
@test/crosswake/proof/phase165_ci_integrity_test.exs
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Introduce the reusable exact-SHA PR resolution gate</name>
  <read_first>
    - script/check_phase166_ownership_ledger.py
    - .planning/workstreams/quality-ratchet-release/phases/165-efficient-and-maintainable-ci/evidence/live-observation.json
    - .planning/workstreams/quality-ratchet-release/phases/167-documentation-and-pull-request-reconciliation/167-CONTEXT.md
  </read_first>
  <files>script/check_phase167_pr_dispositions.py</files>
  <behavior>
    - `--self-test-resolution` exercises positive and negative synthetic receipts; `--verify-resolution &lt;path&gt; --live` validates a captured tested candidate against fresh structured GitHub/default-branch authority.
    - A resolution receipt permits only closed fields for original/candidate number, full tested head/base OIDs, merge commit, freshly read default OID, `Crosswake CI`, closed scope profile, intent-commit count, disposition, replacement relationship, and supersession receipt.
    - Live verification rejects a non-full OID, a candidate whose live head differs from the tested head, missing/pending/cancelled/unsuccessful `Crosswake CI` on that head, an unmerged candidate, an unreachable merge commit, an unauthorized replacement, an unresolved original, or scope/commit-count drift.
    - Scope profiles are closed to `setup_java_v6`, `package_reconciliation`, and `packstore_waiter_clarity`; each recomputes its exact allowed changed-path set from structured PR data, and the latter requires one intent commit.
  </behavior>
  <action>Following the Phase 166 dependency-free closed-schema/self-test pattern, create the shared validator before any remote disposition task uses it. Use fixed `gh` and `git` argv with structured JSON only; resolve the repository default branch and its full OID afresh, verify `Crosswake CI` belongs to the exact tested head, and prove the merge commit is reachable from that freshly fetched authority. For supersession, allow exactly one candidate distinct from #121/#110, require the original CLOSED-unmerged, the replacement MERGED/reachable with the same closed scope profile, and an exact fixed supersession-comment receipt verified live without storing or echoing its prose. For #105 forbid replacement. Fail closed and print only stable rule IDs plus PR numbers per D-21, D-22 through D-24, D-27, and D-28.</action>
  <acceptance_criteria>
    - The helper exists before Plans 05-08 invoke it and self-tests every success/failure dimension.
    - A merely CLOSED or MERGED state cannot pass without exact tested head/base identity, same-head successful Crosswake CI, authorized scope, and fresh-default reachability.
    - Supersession is accepted only for #121/#110 with one verified merged replacement and an exact privacy-safe live receipt.
    - No PR prose, logs, URLs, credentials, or arbitrary remote value enters receipts, commands, or diagnostics.
  </acceptance_criteria>
  <verify>
    <automated>python3 script/check_phase167_pr_dispositions.py --self-test-resolution</automated>
    <fails_when>non-zero exit, the positive self-test count is absent, or any negative exact-SHA/check/reachability/scope/replacement fixture is accepted</fails_when>
  </verify>
  <done>The phase has one reusable fail-closed validator for exact tested PR candidates and merged default-branch authority.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Rebase and complete all seven immutable setup-java v6 uses</name>
  <precondition>`gh auth status` succeeds, Plans 01-04 are committed on the phase branch, the repository-pinned Erlang/Elixir toolchain is selectable, and permissions allow updating PR #121 or creating one manual superseding branch; otherwise halt before remote mutation.</precondition>
  <read_first>
    - .github/actions/setup-android-jvm/action.yml
    - .github/workflows/crosswake-ci.yml
    - .github/workflows/phase68-proof.yml
    - .github/workflows/release-please.yml
    - test/crosswake/proof/phase165_ci_integrity_test.exs
    - .planning/workstreams/quality-ratchet-release/phases/167-documentation-and-pull-request-reconciliation/167-RESEARCH.md
  </read_first>
  <files>.github/actions/setup-android-jvm/action.yml, .github/workflows/crosswake-ci.yml, .github/workflows/phase68-proof.yml, .github/workflows/release-please.yml, test/crosswake/proof/phase165_ci_integrity_test.exs</files>
  <behavior>
    - Structured GitHub reads capture full current head/base OIDs, merge state, and named check conclusions immediately before any branch update.
    - The official setup-java v6 release is resolved to one full 40-hex immutable commit and every live use plus exact count assertion agrees.
    - Android remains at its existing generator/Maven/JVM/vector posture; only dependency setup is changed.
  </behavior>
  <action>Preserve the committed Plan 01-04 phase branch and perform #121 work in an isolated worktree/branch. Fetch current remote default and structured PR #121 state without consuming title/body text. Require the fresh head/base/check observation to remain unchanged at the mutation boundary. Attempt the locked Dependabot rebase and coherent expansion first; resolve the suitable official `actions/setup-java` v6 commit immediately before editing, require a full immutable SHA, update all seven live uses and the exact repository-wide assertion, and keep comments identifying v6. If branch protection prevents coherent completion, create one manual superseding PR containing exactly this change and preserve #121's rationale. Quote all `gh` arguments, use structured JSON fields only, and never interpolate PR prose into commands per D-21, D-22, and D-28.</action>
  <acceptance_criteria>
    - Exactly seven tracked setup-java uses share one official full v6 SHA and the test asserts exact count/value.
    - Action/workflow YAML remains valid and no Android feature/parity/device scope changes.
    - The update branch is based on the exact freshly observed default SHA and contains only the coherent dependency/assertion change.
    - Any changed remote head/base/check state causes a hard stop before push or merge.
    - The phase branch containing Plans 01-04 remains byte-identical and separate from the #121 candidate.
  </acceptance_criteria>
  <verify>
    <automated>actionlint .github/workflows/crosswake-ci.yml .github/workflows/phase68-proof.yml .github/workflows/release-please.yml &amp;&amp; mix test test/crosswake/proof/phase165_ci_integrity_test.exs</automated>
    <fails_when>non-zero exit, the ExUnit summary reports 0 tests, or setup-java count/SHA coherence is not exact</fails_when>
  </verify>
  <done>The PR branch or one explicit superseding branch contains the complete immutable seven-use v6 change on current authority.</done>
</task>

<task type="auto">
  <name>Task 3: Require current CI and close PR #121 with one final disposition</name>
  <reversibility rating="costly">D-22 already authorizes the merge or bounded supersession; undoing a landed remote disposition requires a coordinated revert and a new reviewed PR, so no duplicate decision checkpoint is added.</reversibility>
  <precondition>Task 1's candidate branch exists and `gh auth status` still succeeds.</precondition>
  <read_first>
    - .github/actions/setup-android-jvm/action.yml
    - .github/workflows/crosswake-ci.yml
    - .github/workflows/phase68-proof.yml
    - .github/workflows/release-please.yml
    - test/crosswake/proof/phase165_ci_integrity_test.exs
    - .planning/workstreams/quality-ratchet-release/phases/167-documentation-and-pull-request-reconciliation/167-CONTEXT.md
  </read_first>
  <files>.planning/workstreams/quality-ratchet-release/phases/167-documentation-and-pull-request-reconciliation/evidence/pr-121-resolution.json</files>
  <action>Immediately refresh candidate head/base and current named checks; require the tested full head SHA, current default base SHA, mergeable state, and successful `Crosswake CI` for that exact head. Merge the original PR when it carries the coherent change. On the authorized fallback only, merge the verified manual replacement and close #121 with the validator's fixed bounded supersession marker. Capture the closed resolution receipt only after re-reading original, candidate, checks, merge commit, and remote default; then run the shared live validator so the seven-use scope profile, exact tested identity, same-head CI, replacement relationship, and default reachability are one gate. Retain only closed receipt facts for Plan 08. Do not comment on or mutate #115/#57.</action>
  <acceptance_criteria>
    - #121 has exactly one final disposition: merged, or closed as superseded by one verified merged replacement.
    - The merged commit is reachable from the freshly re-read remote default branch.
    - Current Crosswake CI succeeded on the exact merged candidate head.
    - Retained observations contain no PR title/body/log/URL, credential, or private data.
  </acceptance_criteria>
  <verify>
    <automated>python3 script/check_phase167_pr_dispositions.py --verify-resolution .planning/workstreams/quality-ratchet-release/phases/167-documentation-and-pull-request-reconciliation/evidence/pr-121-resolution.json --live</automated>
    <fails_when>non-zero exit; missing or mismatched full head/base/default/merge OIDs; Crosswake CI absent, non-successful, or attached to another head; setup-java scope/count drift; unresolved original; unauthorized replacement; missing supersession receipt; or merge commit not reachable from fresh default</fails_when>
  </verify>
  <done>PR #121 is unambiguous, its coherent change is on current default, and bounded post-action facts are ready for final evidence.</done>
</task>

</tasks>

<threat_model>
Security posture: OWASP ASVS Level 1; high/critical findings block execution.

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GitHub PR JSON → local commands | Remote text/state is untrusted and mutable. |
| official action release → workflow pin | A mutable tag could introduce supply-chain code. |
| tested head → merge | TOCTOU could merge different bytes or stale checks. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-167-13 | Tampering / Elevation of privilege | setup-java pin | high | mitigate | Official release resolution, full immutable 40-hex SHA, exact seven-use assertion. |
| T-167-14 | Tampering / Spoofing | PR mutation | high | mitigate | Immediate exact head/base/check refresh before every push/merge and hard stop on mismatch. |
| T-167-15 | Information disclosure | gh input/output | high | mitigate | Structured fields and bounded facts only; never PR prose, logs, tokens, or URLs in commands/evidence. |
</threat_model>

## Artifacts this phase produces

- No new file, workflow, check context, Android feature, or public API.
- One exact repository assertion for seven `actions/setup-java` uses at the execution-time official v6 full SHA.
- Remote disposition for #121: `merged` or `superseded`; final evidence is deferred to Plan 08.

<verification>
Require local action/YAML tests, exact-head current Crosswake CI, final PR state, and merged-commit reachability from remote default.
</verification>

<success_criteria>
PR #121 no longer carries ambiguous dependency drift and its complete immutable change is on current authority.
</success_criteria>

<output>
Create `.planning/workstreams/quality-ratchet-release/phases/167-documentation-and-pull-request-reconciliation/167-05-SUMMARY.md` when done.
</output>
