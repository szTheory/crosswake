---
phase: 167-documentation-and-pull-request-reconciliation
plan: 03
type: execute
wave: 3
depends_on: [167-02]
files_modified:
  - .github/workflows/crosswake-ci.yml
  - script/ci_leaf_manifest.json
  - script/ci_docs_allowlist.json
  - test/crosswake/proof/phase165_ci_integrity_test.exs
  - test/crosswake/proof/phase166_repository_quality_test.exs
  - test/crosswake/proof/phase69_docs_contract_parity_test.exs
autonomous: true
requirements: [DOC-01, DOC-02]
estimate:
  tokens: 22000
  raw_tokens: 22000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Recurring generated parity, semantic support/adopter invariants, version markers, ExDoc topology, examples, package surface, privacy, links, and docs-only routing stay under existing owners per D-11 and D-12."
    - "Documentation-only changes still produce visible `Crosswake CI` authority without browser, Android, Apple, or product proof, while release-sensitive inputs remain excluded per D-32."
    - "CI invokes `mix crosswake.docs.sync --check` without writes/staging and never promotes network checks, generic prose lint, inventories, live PR facts, or render snapshots to merge authority per D-13 and D-15."
  artifacts:
    - path: ".github/workflows/crosswake-ci.yml"
      provides: "existing documentation/package proof owners extended with docs sync"
    - path: "script/ci_leaf_manifest.json"
      provides: "unchanged umbrella topology with updated leaf argv/ownership"
    - path: "test/crosswake/proof/phase165_ci_integrity_test.exs"
      provides: "docs-only scheduling and immutable-owner regression"
  key_links:
    - from: ".github/workflows/crosswake-ci.yml"
      to: "lib/mix/tasks/crosswake.docs.sync.ex"
      via: "literal no-write check argv"
      pattern: "mix crosswake.docs.sync --check"
    - from: "script/ci_leaf_manifest.json"
      to: ".github/workflows/crosswake-ci.yml"
      via: "existing documentation-contracts/package leaf ownership"
      pattern: "documentation-contracts"
    - from: "script/ci_docs_allowlist.json"
      to: ".github/workflows/crosswake-ci.yml"
      via: "public_docs classification with release inputs excluded"
      pattern: "public_docs"
  prohibitions:
    - requirement_id: DOC-01
      category: transparency
      statement: "MUST NOT create a second required check, hide named proof, or turn one-time review evidence into recurring merge authority."
      status: unresolved
      flagged: true
---

<objective>
Extend the existing documentation and package proof owners with the new no-write parity command and semantic current-truth coverage per D-11 through D-15 and D-32.

Purpose: Make the corrected documentation contract recurring without increasing CI topology or erasing the inexpensive docs-only path.
Output: Existing workflow/manifest/allowlist wiring and focused CI-policy regressions.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/quality-ratchet-release/phases/167-documentation-and-pull-request-reconciliation/167-02-SUMMARY.md
@.planning/workstreams/quality-ratchet-release/phases/165-efficient-and-maintainable-ci/165-CONTEXT.md
@.planning/workstreams/quality-ratchet-release/phases/166-clean-checkout-engineering-quality/166-CONTEXT.md
@.planning/workstreams/quality-ratchet-release/phases/167-documentation-and-pull-request-reconciliation/167-CONTEXT.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add docs-sync and semantic claim proof to existing CI owners</name>
  <precondition>The repository-pinned Erlang 27.3 / Elixir 1.19.5-otp-27 toolchain is selectable and `mix --version` succeeds.</precondition>
  <read_first>
    - .github/workflows/crosswake-ci.yml
    - script/ci_leaf_manifest.json
    - lib/mix/tasks/crosswake.docs.sync.ex
    - test/crosswake/proof/phase166_repository_quality_test.exs
    - test/crosswake/proof/phase69_docs_contract_parity_test.exs
    - test/crosswake/capability_map/capability_map_test.exs
    - test/crosswake/support_matrix/renderer_test.exs
  </read_first>
  <files>.github/workflows/crosswake-ci.yml, script/ci_leaf_manifest.json, test/crosswake/proof/phase166_repository_quality_test.exs, test/crosswake/proof/phase69_docs_contract_parity_test.exs</files>
  <behavior>
    - Existing `documentation-contracts` runs docs sync check, semantic claim/privacy/link/example coverage, and keeps package/ExDoc proof on its existing owner.
    - No job stages generated files or adds a workflow, leaf family, required context, or umbrella need.
    - Manifest-to-workflow parity names the existing correction owner/action on divergence.
  </behavior>
  <action>Add the literal no-write docs-sync command and focused claim tests to the existing `documentation-contracts` job; extend the existing package/ExDoc owner only where generated guide/public package inclusion requires it. Update the existing leaf manifest rather than adding a new owner. Retain every existing named proof, summary, artifact, and `Crosswake CI` need. Ensure no CI step invokes default write mode or `git add`, and exclude network availability, prose lint, complete inventories, live PR enumeration, and light/dark/system screenshots from recurring authority per D-11 through D-15.</action>
  <acceptance_criteria>
    - Exactly the existing documentation/package owners cover all D-12 recurring contracts.
    - No new workflow, required context, leaf family, or umbrella edge appears.
    - CI generated parity is observational and leaves tracked bytes/index untouched.
    - Existing ExDoc/package/public example checks remain named and actionable.
  </acceptance_criteria>
  <verify>
    <automated>actionlint .github/workflows/crosswake-ci.yml &amp;&amp; python3 script/check_ci_leaf_manifest.py --self-test &amp;&amp; mix test test/crosswake/proof/phase166_repository_quality_test.exs --only ci_parity --only generated_contracts test/crosswake/proof/phase69_docs_contract_parity_test.exs</automated>
    <fails_when>non-zero exit, an ExUnit summary reports 0 tests, or leaf/workflow parity reports an unknown owner</fails_when>
  </verify>
  <done>Existing CI owners enforce the corrected generated and semantic documentation contract with no new authority path.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Re-prove the visible inexpensive documentation-only route</name>
  <read_first>
    - script/ci_docs_allowlist.json
    - .github/workflows/crosswake-ci.yml
    - script/ci_leaf_manifest.json
    - test/crosswake/proof/phase165_ci_integrity_test.exs
    - docs/COMPANION-PUBLISH-RUNBOOK.md
  </read_first>
  <files>script/ci_docs_allowlist.json, .github/workflows/crosswake-ci.yml, script/ci_leaf_manifest.json, test/crosswake/proof/phase165_ci_integrity_test.exs</files>
  <behavior>
    - Planning/public-doc files select documentation proof and a visible successful umbrella without unrelated product/browser/native jobs.
    - Release runbooks/config remain release-sensitive and cannot use documentation-only irrelevance.
    - Unknown classifications run full proof and failed/cancelled/missing documentation work remains fail-closed.
  </behavior>
  <action>Extend Phase 165's exact classifier/manifest tests for the new task and generated guide paths. Preserve `planning` and `public_docs` families, the release-input exclusion, unknown-full-proof behavior, and fail-closed umbrella semantics. Do not classify `docs/COMPANION-PUBLISH-RUNBOOK.md` or release inputs as documentation-only merely because they are prose per D-12, D-13, and D-32.</action>
  <acceptance_criteria>
    - A synthetic generated-guide-only change schedules documentation proof and visible umbrella success only.
    - Browser, Android, Apple, and product leaves stay unscheduled for exact docs-only input.
    - Release-sensitive runbook/config input still schedules its fuller owner set.
    - Cancelled, failed, or missing documentation proof cannot yield umbrella success.
  </acceptance_criteria>
  <verify>
    <automated>mix test test/crosswake/proof/phase165_ci_integrity_test.exs --only classifier --only manifest --only triggers &amp;&amp; actionlint .github/workflows/crosswake-ci.yml</automated>
    <fails_when>non-zero exit, the ExUnit summary reports 0 tests, or the docs-only fixture schedules a forbidden proof family</fails_when>
  </verify>
  <done>The corrected documentation remains cheap to review and visible under the sole required umbrella.</done>
</task>

</tasks>

<threat_model>
Security posture: OWASP ASVS Level 1; high/critical findings block execution.

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| changed paths → CI classifier | Untrusted path sets must not bypass required documentation/release proof. |
| workflow/manifest → umbrella | A duplicate or missing owner could spoof green authority. |
| generated check → worktree | CI must not stage or rewrite tracked projections. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-167-07 | Spoofing | documentation leaf and umbrella | high | mitigate | Exact manifest/workflow owner parity and fail-closed required-result tests. |
| T-167-08 | Tampering | docs-only classification | high | mitigate | Closed allowlist, release-input exclusion, unknown-full-proof tests. |
| T-167-09 | Tampering | generated CI check | high | mitigate | Literal `--check` argv and no staging/default-write commands. |
</threat_model>

## Artifacts this phase produces

- No new workflow, job family, check context, public label, or CLI flag.
- Existing `documentation-contracts` and package/ExDoc leaf argv gain `mix crosswake.docs.sync --check` and focused semantic tests.
- Existing manifest/allowlist records are extended only for the tracked generated documentation paths.

<verification>
Run both task commands and the Phase 165 docs-only synthetic routing suite; inspect the static needs set remains unchanged.
</verification>

<success_criteria>
Recurring documentation truth is complete, no-write, fail-closed, and still inexpensive under one existing CI authority.
</success_criteria>

<output>
Create `.planning/workstreams/quality-ratchet-release/phases/167-documentation-and-pull-request-reconciliation/167-03-SUMMARY.md` when done.
</output>
