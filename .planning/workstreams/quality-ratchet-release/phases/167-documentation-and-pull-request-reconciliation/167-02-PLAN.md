---
phase: 167-documentation-and-pull-request-reconciliation
plan: 02
type: execute
wave: 2
depends_on: [167-01]
files_modified:
  - lib/mix/tasks/crosswake.docs.sync.ex
  - test/mix/tasks/crosswake.docs.sync_test.exs
  - script/repository_artifact_policy.json
  - script/verify_repository.mjs
  - test/js/repository_verification.test.mjs
  - test/crosswake/proof/phase166_repository_quality_test.exs
autonomous: true
requirements: [DOC-01]
estimate:
  tokens: 26000
  raw_tokens: 26000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "A maintainer runs `mix crosswake.docs.sync` to update both generated guides and `mix crosswake.docs.sync --check` to compare bytes without writing or staging anything per D-03, D-09, and D-13."
    - "Generated documentation joins the existing artifact registry with canonical sources, fixed argv, outputs, and one remediation; no second registry is introduced per D-10."
    - "Every artifact record is validated independently while the exact legacy contract-generator record and restoration behavior remain intact per D-10 and D-14."
  artifacts:
    - path: "lib/mix/tasks/crosswake.docs.sync.ex"
      provides: "fixed-purpose write/check maintainer command"
    - path: "test/mix/tasks/crosswake.docs.sync_test.exs"
      provides: "write, drift, no-write, argument, and remediation contract"
    - path: "script/repository_artifact_policy.json"
      provides: "generated documentation ownership record in the existing registry"
  key_links:
    - from: "lib/mix/tasks/crosswake.docs.sync.ex"
      to: "guides/support_matrix.md"
      via: "SupportMatrix renderer bytes"
      pattern: "crosswake.docs.sync"
    - from: "lib/mix/tasks/crosswake.docs.sync.ex"
      to: "guides/capability_map.md"
      via: "CapabilityMap renderer bytes"
      pattern: "crosswake.docs.sync"
    - from: "script/repository_artifact_policy.json"
      to: "script/verify_repository.mjs"
      via: "generated_contracts iteration and fixed argv validation"
      pattern: "generated_contracts"
  prohibitions:
    - requirement_id: DOC-01
      category: transparency
      statement: "MUST NOT make checked-in generated guides an independent truth source or conceal their canonical owner and correction command."
      status: unresolved
      flagged: true
---

<objective>
Create the fixed-purpose documentation synchronization boundary and register it with the existing generated-artifact authority per D-03 and D-09 through D-14.

Purpose: Give maintainers one reproducible correction command while proving CI check mode is genuinely no-write.
Output: Mix task and tests, generalized multi-record artifact validation, and the exact documentation registry record.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/quality-ratchet-release/phases/167-documentation-and-pull-request-reconciliation/167-01-SUMMARY.md
@.planning/workstreams/quality-ratchet-release/phases/167-documentation-and-pull-request-reconciliation/167-CONTEXT.md
@.planning/workstreams/quality-ratchet-release/phases/167-documentation-and-pull-request-reconciliation/167-PATTERNS.md
@script/repository_artifact_policy.json
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement deterministic write and no-write check modes</name>
  <precondition>The repository-pinned Erlang 27.3 / Elixir 1.19.5-otp-27 toolchain is selectable and `mix --version` succeeds.</precondition>
  <reversibility rating="costly">D-09 makes the documented command costly to rename after contributor, artifact-policy, CI, and remediation consumers adopt it.</reversibility>
  <read_first>
    - lib/mix/tasks/crosswake.docs.sync.ex
    - test/mix/tasks/crosswake.docs.sync_test.exs
    - lib/mix/tasks/crosswake.contract.gen.ex
    - lib/crosswake/capability_map/renderer.ex
    - lib/crosswake/support_matrix/renderer.ex
    - guides/capability_map.md
    - guides/support_matrix.md
    - test/mix/tasks/crosswake.gen.native_controls_ui_test.exs
    - test/crosswake/capability_map/renderer_test.exs
    - test/crosswake/support_matrix/renderer_test.exs
  </read_first>
  <files>lib/mix/tasks/crosswake.docs.sync.ex, test/mix/tasks/crosswake.docs.sync_test.exs</files>
  <behavior>
    - `run([])` renders both canonical projections and idempotently creates/updates/reuses their tracked targets.
    - `run(["--check"])` renders in memory, byte-compares both targets, and never calls a writer or changes file bytes, metadata, index, or worktree status.
    - Drift reports canonical source, generated target, and exactly `mix crosswake.docs.sync`; invalid/combined arguments fail closed without echoing contents per D-09, D-13, and D-30.
  </behavior>
  <action>Write the Mix task tests first using `Mix.Shell.Process` and invocation-owned temporary targets. Implement `Mix.Tasks.Crosswake.Docs.Sync` with accepted argv exactly `[]` and `["--check"]`. Compose the existing pure `render` APIs; default mode may call their idempotent writers, while check mode must only read and compare rendered bytes. Aggregate drift deterministically by target path and emit bounded owner/target/remediation facts, never document contents or environment values. Keep this separate from `crosswake.contract.gen` per D-09 and preserve generated owner headers established by Plan 01 per D-03.</action>
  <acceptance_criteria>
    - Default mode makes both tracked guides byte-identical to their canonical renderers and is idempotent.
    - Check mode passes on parity and fails on either drifted/missing target without modifying bytes, metadata, index, or worktree status.
    - Unknown, repeated, or combined options are rejected.
    - Failure output is calm, bounded, non-secret, and gives one exact correction command per D-30.
  </acceptance_criteria>
  <verify>
    <automated>mix test test/mix/tasks/crosswake.docs.sync_test.exs</automated>
    <fails_when>non-zero exit, or the ExUnit summary reports 0 tests</fails_when>
  </verify>
  <done>The stable docs-sync command writes both projections and proves a genuinely observational check path.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Generalize the existing artifact policy for documentation projections</name>
  <read_first>
    - script/repository_artifact_policy.json
    - script/verify_repository.mjs
    - test/js/repository_verification.test.mjs
    - test/crosswake/proof/phase166_repository_quality_test.exs
    - lib/mix/tasks/crosswake.docs.sync.ex
    - lib/mix/tasks/crosswake.contract.gen.ex
  </read_first>
  <files>script/repository_artifact_policy.json, script/verify_repository.mjs, test/js/repository_verification.test.mjs, test/crosswake/proof/phase166_repository_quality_test.exs</files>
  <behavior>
    - Every `generated_contracts` record has exact closed keys, a tracked canonical source, one-or-more nonempty fixed argv arrays, sorted nonempty safe outputs, and nonempty remediation.
    - Cross-record canonical sources and outputs are unique/non-overlapping; malformed, colliding, unsafe, empty, or unordered records fail closed.
    - Production generation preserves the index and restores original bytes on success and failure for both legacy and documentation records.
  </behavior>
  <action>Replace only the singleton/two-invocation assumption in the Phase 166 validator and tests. Register a second exact record with canonical source `lib/mix/tasks/crosswake.docs.sync.ex`, argv `["mix", "crosswake.docs.sync"]`, outputs `guides/capability_map.md` and `guides/support_matrix.md` in sorted order, and remediation `mix crosswake.docs.sync`. Preserve the old contract-generator record byte-for-byte and preserve safe-relative path, fixed argv, undeclared-output, symlink, restoration, and index guarantees. Add discriminating multi-record tests for empty argv, unsafe argv, duplicate source/output, overlapping output, unordered output, generation failure, and restoration per D-10 and D-13.</action>
  <acceptance_criteria>
    - The registry contains exactly the legacy record plus the docs-sync record, with no competing registry.
    - Each declared record is independently validated and executed; cross-record collisions are rejected.
    - The original contract generator regression stays exact.
    - Generated execution restores bytes/index on all success and failure paths.
  </acceptance_criteria>
  <verify>
    <automated>mix test test/crosswake/proof/phase166_repository_quality_test.exs --only artifact_policy --only generated_contracts &amp;&amp; sh -c 'out="$(mktemp "${TMPDIR:-/tmp}/crosswake-generated-contract-test.XXXXXX")"; trap '\''rm -f -- "$out"'\'' EXIT; node --test test/js/repository_verification.test.mjs --test-name-pattern="^generated-contract production runner preserves index and restores bytes$" | tee "$out"; grep -Eq "^# pass [1-9][0-9]*$" "$out"'</automated>
    <fails_when>non-zero exit, the TAP pass line is absent, or it reports zero passing tests</fails_when>
  </verify>
  <done>The one artifact registry safely owns both generator families and retains no-write/no-residue guarantees.</done>
</task>

</tasks>

<threat_model>
Security posture: OWASP ASVS Level 1; high/critical findings block execution.

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Mix argv/filesystem → generated targets | Arguments or paths could expand write authority or leak contents. |
| artifact policy → process spawn | Repository data becomes executable argv and must stay fixed and contained. |
| no-write check → Git worktree/index | A parity check must not mutate or stage user state. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-167-04 | Tampering / Elevation of privilege | generator argv and outputs | high | mitigate | Closed args, array spawn, safe-relative unique outputs, no shell evaluation, collision fixtures. |
| T-167-05 | Tampering | `--check` worktree behavior | high | mitigate | Render-in-memory tests snapshot bytes, metadata, index, and status without writer invocation. |
| T-167-06 | Information disclosure | failure output | medium | mitigate | Allowlisted owner/path/remediation facts only; no content or environment echo. |
</threat_model>

## Artifacts this phase produces

- Module `Mix.Tasks.Crosswake.Docs.Sync` with `run/1` and public CLI forms `mix crosswake.docs.sync` and `mix crosswake.docs.sync --check`.
- Artifact-policy record fields remain `canonical_source`, `regeneration_argv`, `output_paths`, and `remediation_command`; one new docs-sync record is added.
- No package install, second registry, staging command, or workflow/check context.

<verification>
Run both task commands, then run `mix crosswake.docs.sync --check` twice and confirm `git diff --check` plus unchanged index/worktree bytes.
</verification>

<success_criteria>
Both generated documentation projections have one stable correction command and one existing policy owner, with observational CI check semantics.
</success_criteria>

<output>
Create `.planning/workstreams/quality-ratchet-release/phases/167-documentation-and-pull-request-reconciliation/167-02-SUMMARY.md` when done.
</output>
