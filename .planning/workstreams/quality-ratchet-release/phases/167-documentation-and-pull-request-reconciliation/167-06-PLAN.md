---
phase: 167-documentation-and-pull-request-reconciliation
plan: 06
type: execute
wave: 6
depends_on: [167-05]
files_modified:
  - packages/crosswake_rulestead/mix.exs
  - packages/crosswake_rulestead/README.md
  - packages/crosswake_rindle/mix.exs
  - packages/crosswake_rindle/README.md
  - guides/companion_compatibility.md
  - guides/install.md
  - docs/COMPANION-PUBLISH-RUNBOOK.md
  - test/crosswake/proof/phase132_compat_matrix_drift_test.exs
  - test/crosswake/proof/phase142_release_integrity_test.exs
autonomous: true
requirements: [DOC-01, DOC-03]
user_setup:
  - service: github
    why: "Refresh, update, and merge or supersede PR #110"
    dashboard_config:
      - task: "Authenticate gh with repository branch-update, pull-request, and merge authority"
        location: "gh auth login or existing GitHub CLI credential"
estimate:
  tokens: 28000
  raw_tokens: 28000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "PR #110 is rebased after #121 and reconciles both companion manifests, both package READMEs, compatibility/install guidance, publish runbook, and drift fixtures to current executable/published truth per D-23."
    - "The known core `~> 0.1` compatibility footgun and stale unpublished statements are not deferred; all affected surfaces agree before current Crosswake CI and merge per D-02, D-04, and D-23."
    - "If the original branch cannot remain reviewable, one Phase 167 replacement preserves its rationale, is verified/merged, and #110 is explicitly superseded; exact SHA/check guards apply per D-27 and D-28."
    - "The #110 or Phase 167 replacement candidate also lands the already-committed Plans 01-04 reconciliation on top of post-#121 default without changing those reviewed bytes."
  artifacts:
    - path: "packages/crosswake_rulestead/mix.exs"
      provides: "current core compatibility floor for Rulestead"
    - path: "packages/crosswake_rindle/mix.exs"
      provides: "current core compatibility floor for Rindle"
    - path: "guides/companion_compatibility.md"
      provides: "current package-version/floor/publish-state matrix"
    - path: "test/crosswake/proof/phase132_compat_matrix_drift_test.exs"
      provides: "bidirectional manifest/document compatibility parity"
  key_links:
    - from: "packages/crosswake_rulestead/mix.exs"
      to: "guides/companion_compatibility.md"
      via: "phase132 bidirectional floor assertion"
      pattern: "crosswake_rulestead"
    - from: "packages/crosswake_rindle/mix.exs"
      to: "packages/crosswake_rindle/README.md"
      via: "matching install dependency floor"
      pattern: "crosswake"
    - from: "PR #110 exact refreshed head"
      to: "Crosswake CI"
      via: "same-head successful required check before merge"
      pattern: "Crosswake CI"
  prohibitions:
    - requirement_id: DOC-03
      category: transparency
      statement: "MUST NOT leave a known package compatibility footgun ambiguous or call a published package unpublished."
      status: unresolved
      flagged: true
---

<objective>
Resolve PR #110 as one complete package/documentation compatibility transaction on the post-#121 default branch per D-02, D-04, D-23, D-27, and D-28.

Purpose: Make executable dependency floors, package install copy, public compatibility truth, release operations, and drift proof agree before release PRs refresh.
Output: Coherent companion floors/statements, bidirectional regressions, merged/superseded disposition, and bounded post-action facts.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/workstreams/quality-ratchet-release/phases/167-documentation-and-pull-request-reconciliation/167-CONTEXT.md
@.planning/workstreams/quality-ratchet-release/phases/167-documentation-and-pull-request-reconciliation/167-05-SUMMARY.md
@mix.exs
@.release-please-manifest.json
@release-please-config.json
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Rebase #110 and align both companion package sources and READMEs</name>
  <precondition>`gh auth status` succeeds, the committed Plans 01-04 phase branch is intact, the repository-pinned Erlang/Elixir toolchain is selectable, and remote default contains the verified #121 disposition; otherwise halt before remote mutation.</precondition>
  <read_first>
    - packages/crosswake_rulestead/mix.exs
    - packages/crosswake_rulestead/README.md
    - packages/crosswake_rindle/mix.exs
    - packages/crosswake_rindle/README.md
    - mix.exs
    - .release-please-manifest.json
    - release-please-config.json
    - test/crosswake/proof/phase132_compat_matrix_drift_test.exs
  </read_first>
  <files>packages/crosswake_rulestead/mix.exs, packages/crosswake_rulestead/README.md, packages/crosswake_rindle/mix.exs, packages/crosswake_rindle/README.md, test/crosswake/proof/phase132_compat_matrix_drift_test.exs</files>
  <behavior>
    - Both executable manifests use the current supported core floor and both package READMEs show matching install truth.
    - Path-based development dependencies remain unchanged and independent companion versions are not moved into core lockstep.
    - A drift mutation in either manifest or README fails the same bidirectional contract.
  </behavior>
  <action>Refresh #110's exact head/base/check state and the new remote default immediately before rebasing in an isolated worktree. Attempt the original branch first, then replay the already-committed Plans 01-04 reconciliation without altering its bytes so this candidate lands all current Phase 167 local truth. Derive the core floor and package versions from current executable/release owners, update both manifests and both package READMEs together, and extend the existing Phase 132 test rather than creating a new matrix. Preserve independent companion versioning and path-dependency development behavior. If the original branch cannot carry this complete reviewable transaction, move the exact combined changes to one Phase 167 superseding PR and preserve #110's rationale per D-23.</action>
  <acceptance_criteria>
    - Both manifests and package READMEs state the same current core floor.
    - No stale core `~> 0.1` install footgun remains on the four current package surfaces.
    - Development path dependencies and independent versioning remain unchanged.
    - Fresh SHA mismatch halts before branch update.
    - The candidate contains the committed Plans 01-04 reconciliation exactly once on top of post-#121 default.
  </acceptance_criteria>
  <verify>
    <automated>mix test test/crosswake/proof/phase132_compat_matrix_drift_test.exs</automated>
    <fails_when>non-zero exit, or the ExUnit summary reports 0 tests</fails_when>
  </verify>
  <done>The executable companion sources and package-local installation copy agree on the supported core contract.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Align public compatibility, install, runbook, and drift truth</name>
  <read_first>
    - guides/companion_compatibility.md
    - guides/install.md
    - docs/COMPANION-PUBLISH-RUNBOOK.md
    - packages/crosswake_rulestead/mix.exs
    - packages/crosswake_rulestead/README.md
    - packages/crosswake_rindle/mix.exs
    - packages/crosswake_rindle/README.md
    - test/crosswake/proof/phase132_compat_matrix_drift_test.exs
    - test/crosswake/proof/phase142_release_integrity_test.exs
    - CHANGELOG.md
  </read_first>
  <files>guides/companion_compatibility.md, guides/install.md, docs/COMPANION-PUBLISH-RUNBOOK.md, test/crosswake/proof/phase132_compat_matrix_drift_test.exs, test/crosswake/proof/phase142_release_integrity_test.exs</files>
  <behavior>
    - Compatibility matrix states current published-package status/version and exact core floor for every affected companion.
    - Install guide and runbook use current executable package/version facts and retain the Phase 168 approval boundary.
    - Synthetic drift fixtures discriminate the old incomplete two-manifest-only change from the complete transaction.
  </behavior>
  <action>Expand #110's change across the compatibility matrix, current install guide, publish runbook, and existing Phase 132/142 drift fixtures. Derive values from the already-updated manifests and current release authority; do not rewrite released changelog history. Preserve the release-input docs-only exclusion and do not perform Phase 168 candidate parity, mirror, tag, registry, or publication work. Ensure the tests fail if any affected package manifest, README, guide, published-state statement, or runbook floor disagrees per D-03, D-04, D-05, D-12, and D-23.</action>
  <acceptance_criteria>
    - All affected current surfaces agree on published state, version, and core floor.
    - Drift fixtures fail on either stale manifest-only or prose-only changes.
    - Release operations remain verify-first and approval-gated.
    - CHANGELOG released history is unchanged.
  </acceptance_criteria>
  <verify>
    <automated>mix test test/crosswake/proof/phase132_compat_matrix_drift_test.exs test/crosswake/proof/phase142_release_integrity_test.exs test/crosswake/guides/release_boundaries_test.exs &amp;&amp; mix crosswake.docs.sync --check</automated>
    <fails_when>non-zero exit, an ExUnit summary reports 0 tests, or generated projection drift is reported</fails_when>
  </verify>
  <done>The full current package/document/runbook compatibility surface agrees and the known footgun is mechanically closed.</done>
</task>

<task type="auto">
  <name>Task 3: Require current CI and close PR #110 with one final disposition</name>
  <precondition>The complete #110 or superseding branch passes Tasks 1-2 locally and `gh auth status` still succeeds.</precondition>
  <read_first>
    - packages/crosswake_rulestead/mix.exs
    - packages/crosswake_rindle/mix.exs
    - guides/companion_compatibility.md
    - test/crosswake/proof/phase132_compat_matrix_drift_test.exs
    - .planning/workstreams/quality-ratchet-release/phases/167-documentation-and-pull-request-reconciliation/167-CONTEXT.md
  </read_first>
  <files>packages/crosswake_rulestead/mix.exs, packages/crosswake_rindle/mix.exs, guides/companion_compatibility.md, test/crosswake/proof/phase132_compat_matrix_drift_test.exs</files>
  <action>Refresh candidate head/base/checks again and require exact agreement with tested bytes plus successful current `Crosswake CI` on that head. Merge #110 when it carries the coherent transaction; otherwise merge the single verified replacement and close #110 as superseded with a bounded preserved-rationale reason. Re-read final state and remote default reachability. Retain only allowlisted number/SHA/check/disposition/reason/next-gate facts for Plan 08; do not mutate #115/#57.</action>
  <acceptance_criteria>
    - #110 is merged or explicitly superseded by one verified merged reconciliation PR.
    - Exact tested head passed current Crosswake CI and its merge commit is on fresh remote default.
    - No incomplete package-floor path remains after merge.
    - Remote default now contains the verified Plans 01-04 documentation/parked-state transaction as well as the #110 package reconciliation.
    - Retained facts exclude PR prose, logs, URLs, credentials, and private data.
  </acceptance_criteria>
  <verify>
    <automated>sh -c 'state="$(gh pr view 110 --json state,mergedAt,headRefOid,baseRefOid,statusCheckRollup)" &amp;&amp; printf "%s" "$state" | jq -e '\''(.state == "MERGED") or (.state == "CLOSED" and .mergedAt == null) | select(.)'\'' &gt;/dev/null'</automated>
    <fails_when>non-zero exit, missing full head/base OIDs, or #110 remains OPEN</fails_when>
  </verify>
  <done>PR #110 is unambiguous and the complete package/documentation reconciliation is on current default.</done>
</task>

</tasks>

<threat_model>
Security posture: OWASP ASVS Level 1; high/critical findings block execution.

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| package manifests → install/public guidance | Stale or partial floors can create unsafe dependency resolution claims. |
| GitHub state → branch/merge mutation | Remote state may change between observation and action. |
| release guidance → publication | Phase 167 must not cross immutable Phase 168 approval. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-167-16 | Tampering / Spoofing | compatibility truth | high | mitigate | Bidirectional manifest/README/guide/runbook drift tests sourced from executable owners. |
| T-167-17 | Tampering | PR #110 mutation | high | mitigate | Immediate exact head/base/check refresh and same-head CI before merge. |
| T-167-18 | Elevation of privilege | release runbook | high | mitigate | Explicit Phase 168 stop; no publish/tag/mirror/registry action. |
| T-167-19 | Information disclosure | GitHub fields | medium | mitigate | Structured low-cardinality fields only; no untrusted prose or logs retained. |
</threat_model>

## Artifacts this phase produces

- No new source path or public API.
- Existing manifest dependency floors, package README snippets, compatibility/install/runbook statements, and Phase 132/142 drift fixtures are reconciled.
- Remote disposition for #110: `merged` or `superseded`; final evidence is deferred to Plan 08.

<verification>
Run Tasks 1-2 locally, require exact-head current Crosswake CI, then verify final PR state and default-branch reachability.
</verification>

<success_criteria>
The known companion-core compatibility footgun is absent from every current owner/projection and PR #110 has one unambiguous final disposition.
</success_criteria>

<output>
Create `.planning/workstreams/quality-ratchet-release/phases/167-documentation-and-pull-request-reconciliation/167-06-SUMMARY.md` when done.
</output>
