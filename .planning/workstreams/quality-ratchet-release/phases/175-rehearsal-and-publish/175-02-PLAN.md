---
phase: 175-rehearsal-and-publish
plan: 02
type: execute
wave: 2
depends_on: ["175-01"]
files_modified:
  - .github/workflows/required-checks-audit.yml
  - .github/workflows/see-it-run-collateral.yml
  - .github/workflows/native-collateral-advisory.yml
  - .github/workflows/phase68-proof.yml
  - .github/workflows/phase45-proof.yml
  - .github/workflows/phase43-proof.yml
  - .github/workflows/phase132-proof.yml
  - .github/workflows/phase130-proof.yml
  - .github/workflows/phase34-proof.yml
  - .github/workflows/phase23-proof.yml
autonomous: true
requirements: [VAC-03]

estimate:
  tokens: 70000
  raw_tokens: 35000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Every third-party `uses:` reference in every `.github/workflows/*.yml` and `.github/actions/**/action.yml` file resolves to a 40-character commit SHA, so `check-actions` over its now-derived full scope reports zero mutable refs."
    - "Every new pin carries a trailing `# v<version>` comment naming the tag it was resolved from, matching the convention the four already-pinned publish workflows use, so a reader can tell which version a SHA stands for without a network call."
    - "A pin whose action+version is already pinned somewhere in this repository reuses that exact SHA rather than being re-resolved, so the same action at the same version cannot end up with two different SHAs in one repo."
    - "`required-checks-audit.yml`'s job that reads `secrets.BRANCH_PROTECTION_READ_TOKEN` no longer loads any third-party action at a mutable ref, closing the narrow credential-exfiltration path D-23 names."
    - "The four already-pinned publish workflows are untouched by this plan; the diff is confined to the ten workflow files that carried mutable refs (D-28 item 4)."
  prohibitions:
    - "Never resolve a failing scope-cardinality check by narrowing its expected scope, silencing it, or adding an exclusion; the defect is fixed, not the check."
    - "Never write a verification gate whose exit status is taken from the tail of a pipeline; a pipeline reports the last command's status and the gate would be green while asserting nothing."
    - "Never run `git add -A` or `git add .` under `.planning/`; stage `.planning/` paths explicitly by filename, and never stage `config.json`, `milestone.lock`, or `state.json` under the workstream directory."
    - "Never run `git stash` in any form, and never run `git reset --hard`, `git clean -fd`, or a checkout of tracked paths this plan did not create."
  artifacts:
    - .github/workflows/required-checks-audit.yml
    - .github/workflows/see-it-run-collateral.yml
    - .github/workflows/native-collateral-advisory.yml
    - .github/workflows/phase68-proof.yml
    - .github/workflows/phase45-proof.yml
    - .github/workflows/phase43-proof.yml
    - .github/workflows/phase132-proof.yml
    - .github/workflows/phase130-proof.yml
    - .github/workflows/phase34-proof.yml
    - .github/workflows/phase23-proof.yml
  key_links:
    - "`175-01`'s derived scope -> the 31 refs it now sees -> the pins this plan writes -> `mutable_refs=0` meaning the whole repo rather than three files."
    - "`.github/dependabot.yml`'s weekly `github-actions` ecosystem -> the update path that keeps these pins from becoming orphans (D-24); that file is cited, never modified."
---

<planner_notes>
**Edge-probe accounting.** No phase requirement ID from the spec-less probe report applies to this
plan (its requirement is VAC-03). **0 surfaced == 0 authored + 0 flagged.**

**Prohibition accounting.** The phase-wide recall is recorded once in `175-01`'s planner notes;
this plan carries the two of the four minted prohibitions that bear on it, plus the two standing
project prohibitions about pipeline exit status and `.planning/` staging.

**Why pinning is not deferred.** D-21 makes Wave 0 do the full job — discovery, gate, *and* the 31
pins — rather than discovery-and-gate with pinning deferred. D-23 is the deciding fact, not tidiness:
`required-checks-audit.yml:67` reads `secrets.BRANCH_PROTECTION_READ_TOKEN` in a job whose `uses:`
includes a mutable third-party ref. "These are all just proof and advisory workflows" undersells that.
D-24 answers the orphaned-pin objection: `.github/dependabot.yml` already runs the `github-actions`
ecosystem weekly and bump pull requests are already flowing.
</planner_notes>

<scope_boundary>
This plan edits `uses:` lines and nothing else. It does not change a job graph, a trigger, a step's
`run:` body, a permission block, or a secret reference. It does not touch
`.github/workflows/release-please.yml`, `hex-publish.yml`, `ios-mirror-backfill.yml`, or
`exact-public-proof.yml` — those four are already fully pinned and are outside this diff by decision
(D-28 item 4). It does not modify `.github/dependabot.yml`.

Auditing whether the pinned SHAs are themselves correct, latest, or uncompromised is explicitly out of
scope and belongs in a SEED (D-29).
</scope_boundary>

<objective>
Pin the 31 mutable third-party action references that `175-01` made visible, so the pin audit's green
result is a true statement about the repository.

Purpose: the audit was green over three files while ten workflow files carried mutable refs. `175-01`
fixed the measurement; this plan fixes the thing measured. Until both land, `mutable_refs=0` is either
a lie or unavailable.

Output: ten workflow files whose every third-party `uses:` is a 40-character SHA with a version
comment, and a `check-actions` run over the full derived scope reporting `mutable_refs=0`.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/quality-ratchet-release/phases/175-rehearsal-and-publish/175-CONTEXT.md
@.planning/workstreams/quality-ratchet-release/phases/175-rehearsal-and-publish/175-PATTERNS.md
@.github/dependabot.yml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pin the 23 mutable refs in the five heaviest workflow files</name>
  <files>.github/workflows/required-checks-audit.yml, .github/workflows/see-it-run-collateral.yml, .github/workflows/native-collateral-advisory.yml, .github/workflows/phase68-proof.yml, .github/workflows/phase45-proof.yml</files>
  <read_first>
    - `.github/workflows/hex-publish.yml` around lines 247, 261, 267 and 322 — the repo's pin convention, verbatim: `uses: <owner>/<repo>@<40-char-lowercase-sha> # v<version>`.
    - `.github/actions/setup-elixir-cache/action.yml` and `.github/actions/setup-android-jvm/action.yml` — the same convention in composite actions, and the source of several SHAs to reuse.
    - `.github/workflows/required-checks-audit.yml` in full, paying attention to the job around line 67 that reads `secrets.BRANCH_PROTECTION_READ_TOKEN` — this is the D-23 exposure and the reason this file is in the first task rather than the second.
    - `.github/workflows/see-it-run-collateral.yml` and `.github/workflows/native-collateral-advisory.yml` in full — these carry 11 and 6 mutable refs respectively, including four actions with no existing pin anywhere in the repo.
    - `.github/dependabot.yml` in full — read it to confirm the `github-actions` ecosystem runs weekly. Cite it in the SUMMARY as the update path; do not modify it.
    - `.planning/workstreams/quality-ratchet-release/phases/175-rehearsal-and-publish/175-PATTERNS.md`, the Wave 0 SHA-pin section.
  </read_first>
  <action>
Replace every mutable third-party `uses:` ref in these five files with a 40-character commit SHA plus a
trailing version comment. Touch nothing else on the line or in the file.

Resolution rule, in this order:

1. **Reuse an existing in-repo pin** when the same `owner/repo` at the same tag is already pinned
   somewhere in `.github/`. Copy the SHA and the comment verbatim. The pins already present and
   reusable here are: `actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7`,
   `actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7`,
   `actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1`. Note that
   `actions/checkout` also appears pinned at `9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0` for v6 — these
   files reference `@v7`, so the v7 SHA is the correct one; do not cross the two.
2. **Resolve a fresh SHA** for an action with no in-repo pin, using
   `gh api repos/<owner>/<repo>/git/ref/tags/<tag>` and, when the returned object type is `tag`,
   dereferencing it with `gh api repos/<owner>/<repo>/git/tags/<sha>` to get the commit SHA. An
   annotated tag's own object SHA is not the commit SHA and must not be pinned. The actions needing
   fresh resolution in this task are `charmbracelet/vhs-action@v2`,
   `peter-evans/create-pull-request@v8`, `actions/download-artifact@v8`, and
   `ReactiveCircus/android-emulator-runner@v2`.
3. **`erlef/setup-beam@v1` is ambiguous in-repo** — two distinct SHAs are currently pinned for tag
   `v1` (`54075bcc5e249e4758d363f27d099f55d843f124` and `fc68ffb90438ef2936bbb3251622353b3dcb2f93`).
   Do not guess. Resolve `v1` freshly per rule 2, use that SHA for the `phase68-proof.yml` and
   `phase45-proof.yml` occurrences, and record in the SUMMARY which of the two existing pins it
   matches (or that it matches neither, which is a finding to name, not to silently reconcile — this
   plan does not re-pin already-pinned files).

The specific lines in scope for this task, as they stand today:
`required-checks-audit.yml:72`; `see-it-run-collateral.yml:52,57,83,99,120,121,178,196,202,207,226`;
`native-collateral-advisory.yml:15,16,35,58,59,78`; `phase68-proof.yml:22,33,52`;
`phase45-proof.yml:21,23`. Re-derive the line numbers at execution time rather than trusting them —
`node scripts/ci_monitor.cjs check-actions <file>` prints `file:lineno:line` for every `uses:` it finds.

Local `./`-prefixed composite-action references are not third-party refs and are correctly ignored by
the checker; leave them alone.
  </action>
  <acceptance_criteria>
    - For each of the five files, `node scripts/ci_monitor.cjs check-actions <file>` reports `mutable_refs=0`.
    - Every changed `uses:` line matches `uses: [^ ]+@[0-9a-f]{40} # v` — a bare SHA with no version comment is not acceptable.
    - `git diff -U0 -- .github/workflows/required-checks-audit.yml .github/workflows/see-it-run-collateral.yml .github/workflows/native-collateral-advisory.yml .github/workflows/phase68-proof.yml .github/workflows/phase45-proof.yml | grep -E '^[+-]' | grep -v '^[+-][+-]' | grep -cv 'uses:'` prints 0 — every changed line is a `uses:` line.
    - `required-checks-audit.yml` contains zero mutable third-party refs, and its `secrets.BRANCH_PROTECTION_READ_TOKEN` reference is byte-identical to before.
    - Each freshly resolved SHA is recorded in the SUMMARY with the `gh api` output it came from, including whether the tag was annotated and therefore dereferenced.
    - The SUMMARY states which existing pin, if any, the freshly resolved `erlef/setup-beam@v1` SHA matches.
  </acceptance_criteria>
  <verify>
    <automated>node scripts/ci_monitor.cjs check-actions .github/workflows/required-checks-audit.yml .github/workflows/see-it-run-collateral.yml .github/workflows/native-collateral-advisory.yml .github/workflows/phase68-proof.yml .github/workflows/phase45-proof.yml</automated>
    <fails_when>non-zero exit, or the summary line reports `mutable_refs=` anything other than 0</fails_when>
    <automated>grep -hE 'uses: [^ .]' .github/workflows/required-checks-audit.yml .github/workflows/see-it-run-collateral.yml .github/workflows/native-collateral-advisory.yml .github/workflows/phase68-proof.yml .github/workflows/phase45-proof.yml | grep -cvE 'uses: [^ ]+@[0-9a-f]{40} # v'</automated>
    <fails_when>the printed count is anything other than 0</fails_when>
    <automated>git diff --name-only -- .github/workflows/release-please.yml .github/workflows/hex-publish.yml .github/workflows/ios-mirror-backfill.yml .github/workflows/exact-public-proof.yml</automated>
    <fails_when>the output is non-empty — an already-pinned publish workflow was modified, violating D-28 item 4</fails_when>
  </verify>
  <reversibility rating="reversible">Workflow `uses:` refs; a wrong pin is a one-line revert and Dependabot already maintains the update path (D-24).</reversibility>
  <done>The five heaviest files carry zero mutable third-party refs, including the job that loads a branch-protection read token.</done>
</task>

<task type="auto">
  <name>Task 2: Pin the remaining 8 mutable refs and prove the full scope is clean</name>
  <files>.github/workflows/phase43-proof.yml, .github/workflows/phase132-proof.yml, .github/workflows/phase130-proof.yml, .github/workflows/phase34-proof.yml, .github/workflows/phase23-proof.yml</files>
  <read_first>
    - The five files named above, in full.
    - This plan's Task 1 SUMMARY entries for the SHAs already resolved, so the same action at the same tag gets the same SHA in both tasks.
    - `scripts/ci_monitor.cjs` as changed by `175-01` — specifically the derived default scope, because this task's final check runs with no arguments.
  </read_first>
  <action>
Apply the same three resolution rules from Task 1 to the remaining five files. The lines in scope as
they stand today are `phase43-proof.yml:21,23`; `phase132-proof.yml:62,65`;
`phase130-proof.yml:66,69`; `phase34-proof.yml:22`; `phase23-proof.yml:22`. Re-derive them at
execution time with `node scripts/ci_monitor.cjs check-actions <file>`.

Every ref here is either `actions/checkout@v7` or `erlef/setup-beam@v1`, both of which Task 1 already
resolved — reuse those exact SHAs and comments rather than re-resolving, so a single action at a single
tag has exactly one SHA across this pull request.

Then prove the repository-wide result, not just these ten files. Run
`node scripts/ci_monitor.cjs check-actions` with no arguments and confirm three things together: it
exits 0, its `mutable_refs=` field reads 0, and its `files=` field equals a freshly computed count of
`.github/workflows/*.yml` plus `action.yml` files under `.github/actions/`. Write that run's full
output to
`.planning/workstreams/quality-ratchet-release/phases/175-rehearsal-and-publish/evidence/175-check-actions-full-scope.log`,
redirecting stdout and stderr into the file directly rather than through a pipeline so the recorded
exit status is the check's own, and append the observed exit status and the freshly computed expected
file count as final lines.

Stage the evidence file explicitly by filename.
  </action>
  <acceptance_criteria>
    - `node scripts/ci_monitor.cjs check-actions` with no arguments exits 0 and its final line reports `mutable_refs=0`.
    - That same line's `files=` value equals `ls .github/workflows/*.yml | wc -l` plus `find .github/actions -name action.yml | wc -l`, computed at check time.
    - `grep -rhE 'uses: [^ .]' .github/workflows/*.yml .github/actions/*/action.yml | grep -cvE '@[0-9a-f]{40}'` prints 0 across the whole repository.
    - `.../evidence/175-check-actions-full-scope.log` exists, is non-empty, records exit status 0, and records the freshly computed expected file count next to the observed one.
    - `git diff --name-only` for this plan lists only the five workflow files and the evidence log.
    - The same `owner/repo@tag` never resolves to two different SHAs in this pull request; state the check in the SUMMARY.
  </acceptance_criteria>
  <verify>
    <automated>node scripts/ci_monitor.cjs check-actions</automated>
    <fails_when>non-zero exit, or the summary line reports `mutable_refs=` anything other than 0</fails_when>
    <automated>EXPECTED=$(( $(ls .github/workflows/*.yml | wc -l) + $(find .github/actions -name action.yml | wc -l) )); ACTUAL=$(node scripts/ci_monitor.cjs check-actions 2>/dev/null | tail -1 | sed -E 's/^files=([0-9]+).*/\1/'); test "$EXPECTED" = "$ACTUAL"; echo "scope-parity-exit=$? expected=$EXPECTED actual=$ACTUAL"</automated>
    <fails_when>the printed `scope-parity-exit` is anything other than 0</fails_when>
    <automated>grep -rhE 'uses: [^ .]' .github/workflows/*.yml .github/actions/*/action.yml | grep -cvE '@[0-9a-f]{40}'</automated>
    <fails_when>the printed count is anything other than 0</fails_when>
    <automated>test -s .planning/workstreams/quality-ratchet-release/phases/175-rehearsal-and-publish/evidence/175-check-actions-full-scope.log</automated>
    <fails_when>non-zero exit — the evidence log is missing or empty</fails_when>
  </verify>
  <reversibility rating="reversible">Workflow `uses:` refs and a log file.</reversibility>
  <done>Zero mutable third-party action refs remain anywhere in `.github/`, proven by a no-argument run over the derived full scope with its output recorded.</done>
</task>

</tasks>

<artifacts_produced>
## Artifacts this phase produces (plan 175-02)

| Kind | Name |
|---|---|
| New file | `.planning/.../175-rehearsal-and-publish/evidence/175-check-actions-full-scope.log` |
| Modified file | ten `.github/workflows/*.yml` files, `uses:` lines only |
</artifacts_produced>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Third-party action code -> this repository's CI runners | A mutable tag lets an upstream owner change the code a job executes without any change in this repo |
| A mutable-ref job -> `secrets.BRANCH_PROTECTION_READ_TOKEN` | `required-checks-audit.yml` loads a repository secret in a job that also loads third-party code |
| Annotated git tag object -> the SHA written into a pin | An annotated tag's own object SHA is not the commit SHA; pinning the wrong one silently fails to pin |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-175-05 | Elevation of Privilege | `required-checks-audit.yml`'s branch-protection-token job loading a mutable third-party ref | high | mitigate | Task 1 pins that file first and its verification asserts zero mutable refs in it while requiring the secret reference to remain byte-identical. |
| T-175-06 | Tampering | Upstream mutation of a tag pointing at code this repo executes | high | mitigate | Task 1 and Task 2 convert all 31 refs to 40-character commit SHAs; Task 2's repo-wide grep proves none remain. |
| T-175-07 | Spoofing | A pin written from an annotated tag's object SHA rather than its commit SHA | medium | mitigate | Task 1's resolution rule 2 requires dereferencing a `tag`-type object via `git/tags/<sha>` and recording the `gh api` output in the SUMMARY. |
| T-175-08 | Tampering | The same action at the same tag ending up with two different SHAs, so one of them is unexplained | medium | mitigate | Task 1 rule 1 mandates reuse of existing in-repo pins; Task 2 reuses Task 1's resolutions; an acceptance criterion asserts single-SHA-per-tag across the pull request; the pre-existing `erlef/setup-beam@v1` divergence is named as a finding rather than silently reconciled. |
| T-175-09 | Tampering | An unrelated edit riding along in a workflow diff during the phase that performs irreversible publishes | high | mitigate | Task 1's acceptance criteria assert every changed line is a `uses:` line, and a verification asserts the four already-pinned publish workflows are untouched. |
| T-175-10 | Repudiation | Pins becoming orphans with no update path | low | accept | `.github/dependabot.yml` already runs the `github-actions` ecosystem weekly and bump pull requests are flowing (D-24); no further mitigation is needed in this phase. |
| T-175-SC | Tampering | npm/pip/cargo installs | high | mitigate | Not applicable to this plan — it adds no package-manager install step. If any task later requires one, a package-legitimacy gate plus a blocking human checkpoint is required first. |
</threat_model>

<verification>
- `node scripts/ci_monitor.cjs check-actions` with no arguments exits 0 and reports `mutable_refs=0`.
- Its `files=` field equals a freshly computed glob count of workflow plus composite-action files.
- `grep -rhE 'uses: [^ .]' .github/workflows/*.yml .github/actions/*/action.yml | grep -cvE '@[0-9a-f]{40}'` prints 0.
- Every changed line in the ten workflow files is a `uses:` line.
- `.github/workflows/release-please.yml`, `hex-publish.yml`, `ios-mirror-backfill.yml` and `exact-public-proof.yml` are absent from this plan's diff.
- `.../evidence/175-check-actions-full-scope.log` exists, is non-empty, and records exit status 0.

</verification>

<success_criteria>
`mutable_refs=0` is now a true statement about the whole repository rather than about three files, the
token-reading audit job no longer executes third-party code at a moving ref, and the diff that achieved
it contains nothing but `uses:` lines.
</success_criteria>

<output>
Create `.planning/workstreams/quality-ratchet-release/phases/175-rehearsal-and-publish/175-02-SUMMARY.md` when done.
</output>
