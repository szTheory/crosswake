---
phase: 175-rehearsal-and-publish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/ci_monitor.cjs
autonomous: true
requirements: [VAC-03]

estimate:
  tokens: 60000
  raw_tokens: 30000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "`node scripts/ci_monitor.cjs check-actions` with no arguments scans every `.github/workflows/*.yml` and `.github/actions/**/action.yml` file that exists at run time, discovered by walking the tree rather than from a literal path list, so a file added later is inside the audit by construction."
    - "The summary line prints the number of files scanned alongside the action and mutable-ref counts, so a truncated scope is visible in the CI log rather than inferable from a suspiciously small action count."
    - "The check refuses to report success when its scan scope is smaller than the scope it claims to cover; a narrowed scope produces a non-zero exit, not a green line about a smaller set."
    - "The scope assertion has itself been driven red against a deliberately narrowed scope and the red output recorded, so the gate is not vulnerable to the defect it exists to catch."
    - "The scope assertion is callable by a future check without copying it, rather than being welded into the one function that needed it first (D-25)."
  prohibitions:
    - "Never resolve a failing scope-cardinality check by narrowing its expected scope, silencing it, or adding an exclusion; the defect is fixed, not the check."
    - "Never write a verification gate whose exit status is taken from the tail of a pipeline; a pipeline reports the last command's status and the gate would be green while asserting nothing."
    - "Never run `git add -A` or `git add .` under `.planning/`; stage `.planning/` paths explicitly by filename, and never stage `config.json`, `milestone.lock`, or `state.json` under the workstream directory."
    - "Never run `git stash` in any form, and never run `git reset --hard`, `git clean -fd`, or a checkout of tracked paths this plan did not create."
  artifacts:
    - scripts/ci_monitor.cjs
  key_links:
    - "`script/check_phase165_efficient_ci.sh:63-64` invokes `check-actions` with no arguments -> the default scope is the only scope CI ever exercises -> fixing the default is what makes the existing green line mean something."
    - "the derived scope -> the scope assertion -> the `files=` summary field -> the CI log a reviewer reads."
---

<planner_notes>
**Phase tracer placement.** TRACER_MODE is on for this phase. The phase's single
`type="tracer"` slice is Task 1 of `175-05` — the Hex candidate rehearsal, which is the thinnest path
that runs the whole release machinery end-to-end (candidate identity -> dispatch -> artifact ->
committed evidence) while stopping at the last reversible step. Plans `175-01`, `175-02` and `175-03`
sit in front of it because D-22 is a locked user decision: the Wave 0 pull request lands, green and
merged, before any Wave 1 task begins. That ordering is not a horizontal-layer decomposition of the
release work; it is a prerequisite repair to the audit tool that scores the release work.

**Edge-probe accounting.** This plan carries no phase requirement ID from the spec-less probe report
(its requirement is VAC-03, which is a milestone-standing requirement and not in the probe's
`{REL-*, DOC-*}` set). **0 surfaced == 0 authored + 0 flagged.**

**Prohibition accounting (phase-wide, recalled once).** 13 recalled in-prompt; **4 authored
descriptor-less across this phase's plans + 3 canon-referred + 6 dropped as routine-engineering or
edge-probe-owned == 13.** Canon breadcrumbs, not minted here: pinning to a compromised action SHA is
canon supply-chain security (covered by `/gsd-secure-phase` + Dependabot + the D-29 SEED); credential
exfiltration through a mutable third-party ref is canon OWASP CI/CD (covered by
`/gsd-secure-phase`); secret leakage into workflow logs is canon (covered by `/gsd-secure-phase`).
</planner_notes>

<scope_boundary>
This plan changes exactly one file: `scripts/ci_monitor.cjs`. It does not edit a workflow file, does
not redesign CI, and does not touch the four already-pinned publish workflows. Pinning the mutable
refs this fix exposes is `175-02`'s job, deliberately separated so the diff that changes the *measuring
instrument* is reviewable apart from the diff that changes the *thing measured* (D-28 item 4).

The two-line stdout contract is consumed by `script/check_phase165_efficient_ci.sh` and by CI log
readers. Extend it; do not restructure it.
</scope_boundary>

<objective>
Make `check-actions` audit the scope it claims to audit, and make a truncated scope impossible to
report as success.

Purpose: WINDOWS entry 35. The check today prints `actions=114 mutable_refs=0` while reading a
hardcoded list of three files, so 31 mutable third-party action refs across ten other workflow files
are scored as absent. The publish workflows themselves are already fully SHA-pinned, so this does not
make publishing unsafe — it makes the *claim* that the pin audit is green worthless, in the phase that
performs the milestone's only irreversible operations (D-21, D-23, Pitfall 6).

Output: a `check-actions` whose default scope is derived from the tree, whose summary line names the
file count it scanned, and whose scope assertion has been demonstrated failing.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/quality-ratchet-release/ROADMAP.md
@.planning/workstreams/quality-ratchet-release/STATE.md
@.planning/workstreams/quality-ratchet-release/phases/175-rehearsal-and-publish/175-CONTEXT.md
@.planning/workstreams/quality-ratchet-release/phases/175-rehearsal-and-publish/175-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Derive the check-actions scan scope from the tree, and assert it</name>
  <files>scripts/ci_monitor.cjs</files>
  <read_first>
    - `scripts/ci_monitor.cjs` in full around `checkActions()` (currently lines 269-303) and the file's header help block (around line 20) and its command dispatch (around line 1500). Note the file's zero-dependency style: only `node:child_process`, `node:crypto`, `node:fs`, `node:path` are imported at the top (lines 3-6). There is no `glob` dependency and none is to be added.
    - `script/check_phase165_efficient_ci.sh` lines 60-70 — the only in-repo caller, which invokes `check-actions` with no arguments. This is why the default scope is the only scope CI exercises.
    - `script/check_absence_is_not_success.exs` — the shape to mirror for factoring independent checks as separately callable functions composed by a runner, rather than inlining an assertion into the one caller that needed it (D-25).
    - `script/check_release_workflow_integrity.exs` — its `@proof_lanes` "DECLARED, NEVER DERIVED" comment block. Read it so you can state, in a code comment here, why this one place inverts that convention: a hardcoded roster was the defect, so the file discovery must be derived (D-28 item 1) while the *expected scope* stays an assertion rather than a silent input.
    - `.planning/workstreams/quality-ratchet-release/phases/175-rehearsal-and-publish/175-PATTERNS.md`, the `scripts/ci_monitor.cjs` section — the exact current shape and the three required changes.
  </read_first>
  <action>
Replace `checkActions()`'s hardcoded three-element default path list with a scope derived from the
working tree at run time. Explicit path arguments keep working exactly as today; only the no-argument
default changes.

Concrete target state:

- Add a reusable, module-level helper — name it `discoverActionSources` — that returns the sorted list
  of files in scope: every `.yml` file directly under `.github/workflows/`, plus every `action.yml`
  found by a recursive walk of `.github/actions/`. Implement the walk with `fs.readdirSync(dir, { withFileTypes: true })` and `path.join`; add no dependency. Sort the result so output ordering is stable across filesystems.
- Add a second reusable, module-level helper — name it `assertFullScope` — taking the actual scanned
  file list and the expected file list, and failing when the actual list is a proper subset of the
  expected list or when the expected list is empty. It is written as a standalone function called by
  `checkActions()`, not inlined into it, so a later check inherits it by construction (D-25).
  `checkActions()` is currently the only function in this file with a hardcoded-default-path shape, so
  there is nothing else to reconcile today — the helper exists for the next one.
- Have `checkActions()` call `discoverActionSources()` for its default, then call `assertFullScope`
  with the paths it is about to read and a freshly re-derived expectation, so a scope that shrank
  between derivation and read is a failure rather than a smaller green.
- Extend the summary line to lead with the file count (D-27). The line becomes
  `files=<N> actions=<N> mutable_refs=<N>`. Keep the existing per-line `file:lineno:line` stdout that
  precedes it, keep the existing stderr message for mutable refs, and keep the existing non-zero
  `process.exitCode` on mutable refs. Do not restructure the two-line contract — `script/check_phase165_efficient_ci.sh` and CI log readers grep it.
- On scope failure, write a distinct stderr message naming both counts (expected vs scanned) and set a
  non-zero `process.exitCode`. A truncated scope must not be able to exit 0.
- Update the help text block near line 20 so `check-actions`' documented default describes the derived
  scope rather than three named files.
- Add a short comment above `discoverActionSources` explaining why discovery is derived here even
  though `check_release_workflow_integrity.exs`'s roster convention is declared-never-derived: the
  roster that was scanned-and-truncated is the defect being repaired, and a literal roster here would
  reproduce it.

Leave every other subcommand in the file untouched. Do not edit any workflow file in this task.
  </action>
  <acceptance_criteria>
    - `scripts/ci_monitor.cjs` contains `function discoverActionSources` and `function assertFullScope` at module level, and `checkActions` calls both.
    - `node scripts/ci_monitor.cjs check-actions` exits non-zero today (31 mutable refs are still present until `175-02` lands) and its final stdout line matches `^files=[0-9]+ actions=[0-9]+ mutable_refs=[0-9]+$`.
    - The `files=` value printed by the no-argument invocation equals `ls .github/workflows/*.yml | wc -l` plus the count of `action.yml` files found under `.github/actions/` — computed freshly at check time, never compared against a number written into the plan.
    - `node scripts/ci_monitor.cjs check-actions .github/workflows/crosswake-ci.yml` still accepts explicit arguments and reports a `files=1` scope.
    - `grep -c 'setup-android-jvm' scripts/ci_monitor.cjs` prints 0 — that path literal occurs today only inside the hardcoded default list (line 272), so a zero count proves the list is gone rather than commented out.
    - `grep -v '^ *//' scripts/ci_monitor.cjs | grep -c "require('glob')"` prints 0 — no dependency added.
  </acceptance_criteria>
  <verify>
    <automated>node --check scripts/ci_monitor.cjs</automated>
    <fails_when>non-zero exit — the file does not parse</fails_when>
    <automated>node scripts/ci_monitor.cjs check-actions 2>/dev/null | tail -1 | grep -Eq '^files=[0-9]+ actions=[0-9]+ mutable_refs=[0-9]+$'; echo "summary-line-shape-exit=$?"</automated>
    <fails_when>the printed `summary-line-shape-exit` is anything other than 0</fails_when>
    <automated>EXPECTED=$(( $(ls .github/workflows/*.yml | wc -l) + $(find .github/actions -name action.yml | wc -l) )); ACTUAL=$(node scripts/ci_monitor.cjs check-actions 2>/dev/null | tail -1 | sed -E 's/^files=([0-9]+).*/\1/'); test "$EXPECTED" = "$ACTUAL"; echo "scope-parity-exit=$? expected=$EXPECTED actual=$ACTUAL"</automated>
    <fails_when>the printed `scope-parity-exit` is anything other than 0</fails_when>
  </verify>
  <reversibility rating="reversible">One utility file; the change is a default-value and output-format extension behind an unchanged CLI surface.</reversibility>
  <done>`check-actions` with no arguments scans every workflow and composite-action file that exists, prints the count it scanned, and cannot exit 0 on a truncated scope.</done>
</task>

<task type="auto">
  <name>Task 2: Drive the scope assertion red against a deliberately narrowed scope</name>
  <files>scripts/ci_monitor.cjs, .planning/workstreams/quality-ratchet-release/phases/175-rehearsal-and-publish/evidence/175-scope-gate-regression.log</files>
  <read_first>
    - `scripts/ci_monitor.cjs` as changed by Task 1 — specifically `assertFullScope` and its failure path.
    - `scripts/ci_monitor.cjs`'s existing `test-evidence` subcommand and its dispatch entry, as the in-file convention for a self-test subcommand (this file has no external test harness; no `ci_monitor.cjs` test file exists in the repo).
    - `script/check_absence_is_not_success.exs` lines 1-19 — the doc-comment convention of naming the specific past incident a check exists to catch, before any code.
    - `.planning/workstreams/quality-ratchet-release/phases/174-clean-room-host-realism-adopter-fidelity/174-NON-VACUITY.md` — the milestone's standard for what counts as a *measured* non-vacuity fact: a named executed mutation, not a bare tick.
  </read_first>
  <action>
Prove the scope assertion has teeth, per D-28 item 3, and record the proof.

Add a `test-check-actions-scope` subcommand to `scripts/ci_monitor.cjs`, dispatched alongside the
existing `test-evidence` subcommand. It runs `assertFullScope` twice in-process against fixture inputs
and reports both outcomes:

1. A **known-bad** case: an actual list that is a proper subset of the expected list (for example, the
   three-file list that was the historical default, against the full derived expectation). This case
   MUST be observed failing. If it passes, the subcommand exits non-zero with a message saying the
   scope gate is vacuous.
2. A **known-clean control**: actual equals expected. This case MUST be observed passing, so the red in
   case 1 is proven to be caused by the narrowing rather than by the assertion always failing.

The subcommand prints one line per case naming the case, the two cardinalities, and the observed
outcome, then exits 0 only when case 1 was red and case 2 was green.

Add a doc comment above the subcommand naming the incident it guards: the audit reported
`mutable_refs=0` over a three-file default while 31 mutable refs sat in ten unscanned workflow files,
so the assertion that was correct was applied to a truncated set.

Then capture the real-world demonstration. Create the phase evidence directory if it does not exist and
write the full stdout and stderr of `node scripts/ci_monitor.cjs test-check-actions-scope` to
`.planning/workstreams/quality-ratchet-release/phases/175-rehearsal-and-publish/evidence/175-scope-gate-regression.log`,
redirecting both streams to the file directly rather than through a pipeline, so the recorded exit
status is the subcommand's own. Append the observed exit status to the log as a final line.

Stage the evidence file explicitly by filename.
  </action>
  <acceptance_criteria>
    - `node scripts/ci_monitor.cjs test-check-actions-scope` exits 0 and its output contains one line for the narrowed case reporting a red outcome and one line for the equal-scope control reporting a green outcome.
    - `scripts/ci_monitor.cjs` contains `test-check-actions-scope` in both its dispatch block and its help text.
    - `.planning/workstreams/quality-ratchet-release/phases/175-rehearsal-and-publish/evidence/175-scope-gate-regression.log` exists, is non-empty, and its final line records an exit status of 0.
    - The log names both cardinalities for the narrowed case, so a reader can see which scope was withheld.
    - The SUMMARY states the narrowed-case cardinalities as measured numbers, not as a tick.
  </acceptance_criteria>
  <verify>
    <automated>node scripts/ci_monitor.cjs test-check-actions-scope</automated>
    <fails_when>non-zero exit — either the narrowed case passed (the gate is vacuous) or the control case failed (the assertion always fails)</fails_when>
    <automated>test -s .planning/workstreams/quality-ratchet-release/phases/175-rehearsal-and-publish/evidence/175-scope-gate-regression.log</automated>
    <fails_when>non-zero exit — the evidence log is missing or empty</fails_when>
    <automated>node --check scripts/ci_monitor.cjs</automated>
    <fails_when>non-zero exit</fails_when>
  </verify>
  <reversibility rating="reversible">A self-test subcommand and a log file.</reversibility>
  <done>The scope assertion has been observed going red against a narrowed scope and green against an equal scope, and the demonstration is on disk.</done>
</task>

</tasks>

<artifacts_produced>
## Artifacts this phase produces (plan 175-01)

| Kind | Name |
|---|---|
| New function | `discoverActionSources` in `scripts/ci_monitor.cjs` |
| New function | `assertFullScope` in `scripts/ci_monitor.cjs` |
| New CLI subcommand | `node scripts/ci_monitor.cjs test-check-actions-scope` |
| New output field | `files=<N>` on the `check-actions` summary line |
| New file | `.planning/.../175-rehearsal-and-publish/evidence/175-scope-gate-regression.log` |
| New directory | `.planning/.../175-rehearsal-and-publish/evidence/` |
</artifacts_produced>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Working tree -> `check-actions` scan scope | Untrusted-in-the-sense-of-unbounded input: any file added to `.github/` later must land inside the audit without anyone remembering to widen a list |
| `check-actions` stdout -> `script/check_phase165_efficient_ci.sh` and CI log readers | A machine-parsed summary line that a reviewer treats as authority about the whole repo |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-175-01 | Repudiation | `checkActions()` truncated default scope | high | mitigate | Task 1 derives the scope from a tree walk and asserts actual-vs-expected cardinality; a proper-subset scope exits non-zero instead of printing a smaller green. |
| T-175-02 | Spoofing | The `mutable_refs=0` summary line read as a repo-wide claim | high | mitigate | Task 1 prints `files=<N>` so the claim's scope is legible in every CI log; Task 2's verification compares that count against a freshly globbed count rather than a written-down number. |
| T-175-03 | Tampering | The scope gate itself being vacuous — an assertion that never fires | high | mitigate | Task 2 adds a self-test that drives the assertion red against a narrowed scope and green against an equal-scope control, and records the run. |
| T-175-04 | Elevation of Privilege | A recursive `fs` walk following a symlink out of `.github/` | low | accept | `withFileTypes` directory entries are not followed as symlinks by `isDirectory()` on a symlink entry, and the repo contains no symlinks under `.github/`; the walk is read-only and its output is only ever used as a file list to read. |
| T-175-SC | Tampering | npm/pip/cargo installs | high | mitigate | Not applicable to this plan — it adds no package-manager install step and no dependency. If any task later requires one, a package-legitimacy gate plus a blocking human checkpoint is required first. |
</threat_model>

<verification>
- `node --check scripts/ci_monitor.cjs` exits 0.
- `node scripts/ci_monitor.cjs check-actions` prints a final line matching `files=<N> actions=<N> mutable_refs=<N>`, with `files=<N>` equal to a freshly computed glob count.
- `node scripts/ci_monitor.cjs test-check-actions-scope` exits 0 with a red narrowed case and a green control case.
- `.../evidence/175-scope-gate-regression.log` exists, is non-empty, and records exit status 0.
- `git diff --name-only` for this plan lists only `scripts/ci_monitor.cjs` and the evidence log.
</verification>

<success_criteria>
The pin audit now covers the repository it claims to cover, the size of that coverage is printed in
every run, and the assertion that guards the coverage has been demonstrated failing when the coverage
shrinks.
</success_criteria>

<output>
Create `.planning/workstreams/quality-ratchet-release/phases/175-rehearsal-and-publish/175-01-SUMMARY.md` when done.
</output>
