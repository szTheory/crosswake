---
phase: 175-rehearsal-and-publish
plan: 05
type: execute
wave: 5
depends_on: ["175-04"]
files_modified:
  - docs/COMPANION-PUBLISH-RUNBOOK.md
  - script/check_release_doc_version_literals.exs
  - .github/workflows/crosswake-ci.yml
  - .planning/workstreams/quality-ratchet-release/phases/175-rehearsal-and-publish/175-DOC-TRUTH.md
autonomous: true
requirements: [DOC-04, DOC-06]

estimate:
  tokens: 75000
  raw_tokens: 37500
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "`docs/COMPANION-PUBLISH-RUNBOOK.md` no longer contains the section warning that the pipeline publishes exactly one version; the section is removed outright rather than reworded, softened, or commented out (D-17)."
    - "No bare version literal appears outside a code fence in either `docs/COMPANION-PUBLISH-RUNBOOK.md` or `docs/RELEASE-INCIDENT-RESPONSE.md`; prose that previously bound a claim to a version now states the claim in terms of state — the approved version, the recorded baseline, the candidate public ref — rather than a number."
    - "Both documents state the invariant `This document makes no version-specific claims.` at the top, and a CI check asserts that sentence is present in each."
    - "A CI check fails the build when a bare version literal appears outside a code fence in either document, over a declared two-file roster, so a version written as though permanent cannot recur silently (D-19)."
    - "That check has been observed going red against a deliberately injected version literal and green on the clean documents, so it is not a check that passes while asserting nothing."
    - "`git subtree split` is documented by name as the durable mirror-split mechanism in both the ordinary-publication section and the iOS-mirror steps of the runbook (DOC-06)."
    - "Archived planning history is untouched; DOC-06's scope is live documentation only (D-18)."
  prohibitions:
    - "Never resolve a failing version-literal check by narrowing its declared roster, silencing it, or adding an exclusion; the defect is fixed, not the check."
    - "Never soften a documentation claim that has become false; remove it. A softened claim is indistinguishable from a claim that was always hedged."
    - "Never rewrite archived `.planning/` history to make a live documentation check pass."
    - "Never write a verification gate whose exit status is taken from the tail of a pipeline; a pipeline reports the last command's status and the gate would be green while asserting nothing."
    - "Never run `git add -A` or `git add .` under `.planning/`; stage `.planning/` paths explicitly by filename, and never stage `config.json`, `milestone.lock`, or `state.json` under the workstream directory."
  artifacts:
    - docs/COMPANION-PUBLISH-RUNBOOK.md
    - script/check_release_doc_version_literals.exs
    - .planning/workstreams/quality-ratchet-release/phases/175-rehearsal-and-publish/175-DOC-TRUTH.md
  key_links:
    - "the deleted section -> the check that makes its recurrence fail CI -> the invariant sentence asserted in both documents."
    - "`script/check_release_workflow_integrity.exs`'s exit contract and declared-roster convention -> the new check's shape, so a reviewer recognizes it immediately."
---

<planner_notes>
**Edge-probe accounting.** The spec-less probe surfaced 2 rows for this plan's requirements — DOC-04
and DOC-06, both `unclassified` and therefore **`unresolved`**. Neither is auto-resolved with a
backstop; both are surfaced below as flagged planner assumptions. **2 surfaced == 0 authored as
resolved truths + 2 flagged.**

**Planner judgment on ordering — surfaced, not silent.** RESEARCH.md's recommended wave structure puts
DOC-04 in a documentation wave *after* `0.2.2` is live, reading ROADMAP SC8's "deleted in the same
change window that makes it false" as "after the publish". This plan places it *before* the publish
waves instead, for two reasons. First, the claim is already false: Phase 171 removed the version gating
the section describes, so REQUIREMENTS.md's DOC-04 wording — deleted the moment it becomes false — is
better served earlier, and the whole phase is the change window either way. Second, and decisively, the
runbook is the document an operator reads *while* executing the publish sequence; leaving a bolded
warning in it that says this pipeline cannot publish anything but one specific version, during the very
window in which they publish a different one, is an active hazard. If the developer prefers the
RESEARCH ordering, this plan moves to a wave after `175-09` unchanged — only its `wave` and
`depends_on` change.

**Why the check's roster covers both documents from the start.** Authoring it against one file now and
widening it later would mean shipping a check whose scope is smaller than the scope it claims, which is
the exact defect Wave 0 just spent three plans repairing.
</planner_notes>

<flagged_assumptions>
Both of this plan's edge-coverage rows came back `unclassified` from the spec-less probe and remain
**unresolved**. Recorded rather than dropped:

- **DOC-04 (unclassified, unresolved).** The planner's assumption is that "deleted, not softened" is
  satisfied by removing the whole `##` section including its heading, and that the section's final
  paragraph — the still-true, not-version-bound observation about the post-publication proof not running
  through exact-ref recovery — is a separate fact whose disposition must be decided explicitly rather
  than deleted by adjacency. Task 1 requires that decision to be recorded.
- **DOC-06 (unclassified, unresolved).** The planner's assumption is that "residual references are
  removed" is already satisfied in live code (prior research found zero live workflow or script
  references outside one unrelated comment), so the remaining obligation is purely additive: name
  `git subtree split` explicitly in the runbook. If a live reference is found during execution that
  prior research missed, that is a finding to record, not a silent extra edit.
</flagged_assumptions>

<scope_boundary>
This plan edits one existing document, adds one check script, and wires that check into CI. It does not
touch `docs/RELEASE-INCIDENT-RESPONSE.md`'s content — `175-04` authored it and this plan only adds it to
the check's roster. It does not rewrite archived `.planning/` history (D-18). It dispatches nothing,
merges nothing, and touches no registry.

If the version-literal check goes red on a line this plan did not intend to change, fix the line — never
the roster.
</scope_boundary>

<objective>
Make the release documentation true, and make the specific way it went untrue structurally hard to
repeat.

Purpose: DOC-04 exists because a version was written into prose as though it were permanent, and the
sentence survived long after the condition it described was repaired. Deleting it is half the job. The
other half is that nothing stops the next writer from doing it again — which is what D-19's check is
for.

Output: a runbook with no false section and no bare version literal in prose, `git subtree split`
documented by name, and a CI check that has been seen failing.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/quality-ratchet-release/REQUIREMENTS.md
@.planning/workstreams/quality-ratchet-release/phases/175-rehearsal-and-publish/175-CONTEXT.md
@.planning/workstreams/quality-ratchet-release/phases/175-rehearsal-and-publish/175-PATTERNS.md
@prompts/crosswake-brand-book.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove the false section and de-version the runbook's prose (DOC-04)</name>
  <files>docs/COMPANION-PUBLISH-RUNBOOK.md</files>
  <read_first>
    - `docs/COMPANION-PUBLISH-RUNBOOK.md` in full, all 279 lines. Read it before editing so the de-versioning rewrites match the surrounding register rather than reading as patches.
    - `.planning/workstreams/quality-ratchet-release/phases/175-rehearsal-and-publish/175-PATTERNS.md`, the `docs/COMPANION-PUBLISH-RUNBOOK.md` section — it quotes the exact deletion target and flags the final-paragraph disposition question.
    - `.planning/workstreams/quality-ratchet-release/phases/175-rehearsal-and-publish/175-CONTEXT.md`, decisions D-17 and D-19.
    - `.github/workflows/release-please.yml`'s `approved-release-guard` job and its `approved_version` output — the state-shaped vocabulary the de-versioned prose should use in place of a number.
    - `docs/RELEASE-INCIDENT-RESPONSE.md` as created by `175-04` — its opening shape and its invariant sentence, which this file now adopts verbatim.
    - `prompts/crosswake-brand-book.md` — documentation voice.
  </read_first>
  <action>
Two changes to `docs/COMPANION-PUBLISH-RUNBOOK.md`.

**First, delete the false section outright.** It is the second `##` section of the file, currently
spanning lines 29 through 46 — heading, bolded callout, the paragraph about publish jobs being gated on
a version equality, the paragraph referencing `TODO-009` / `SEED-017`, and the closing `Related:`
paragraph. Remove the heading and the section body. Do not reword it, do not move it, do not comment it
out, and do not leave a stub heading. D-17 is explicit: deleted, not softened.

The final `Related:` paragraph of that section is a separate matter and needs an explicit decision
rather than deletion by adjacency. Its claim — that a release completing through exact-ref recovery does
not run the post-publication proof, because that job depends on the ordinary publish jobs — is not
version-bound and may still be true. Verify it against
`.github/workflows/release-please.yml`'s current `needs:` graph for the proof job. If it is still true,
preserve the sentence somewhere it belongs — `## Candidate-local versus exact-public proof` is the
natural home — with the version reference removed. If the graph has changed and it is no longer true,
drop it and say so. Either way, record the verdict and the evidence in the SUMMARY; do not leave it
undecided.

**Second, de-version the remaining prose.** Nine lines outside code fences currently bind a claim to a
version literal: the opening contract line, the three linked-coordinate bullets, the companion-floors
line, the mirror-rehearsal step's baseline-and-split line, the two status-surface lines about the mirror
baseline and the candidate public ref, and the rollback paragraph about an already-live core. Rewrite
each in terms of **state** rather than a number — the approved version, the recorded mirror baseline, the
candidate public ref, the already-published core — using the vocabulary `approved-release-guard` and
`mix crosswake.release.status` already use. Literals inside fenced command examples stay exactly as they
are; the check ignores fenced content and so should you.

Then add the invariant at the top of the file, on its own line and bolded, worded identically to the one
`175-04` put in the incident document: **This document makes no version-specific claims.** Place it
immediately after the existing opening paragraph, before the first `##` heading.

Do not renumber the seven-step sequence, do not change any heading text other than removing the deleted
one, and do not touch any fenced block.

<!-- planner-discipline-allow: TODO-009 -->
The deletion-witness identifier is named here so the executor knows which line proves the removal; the
acceptance criterion greps `docs/COMPANION-PUBLISH-RUNBOOK.md`, not this plan.
  </action>
  <acceptance_criteria>
    - `grep -c 'TODO-009' docs/COMPANION-PUBLISH-RUNBOOK.md` prints 0 — that identifier occurs today only inside the deleted section, so a zero count witnesses the deletion.
    - `grep -c 'This document makes no version-specific claims\.' docs/COMPANION-PUBLISH-RUNBOOK.md` prints 1.
    - `awk 'BEGIN{f=0} /^```/{f=!f; next} f==0 && /[0-9]+\.[0-9]+\.[0-9]+/{c++} END{print c+0}' docs/COMPANION-PUBLISH-RUNBOOK.md` prints 0.
    - The file still contains the headings `## Candidate authority`, `## Exact seven-step operator sequence`, `## Five states and one correction`, `## Ordinary publication and recovery`, and `## CI ownership` — the deletion removed one section, not several.
    - `git diff -U0 -- docs/COMPANION-PUBLISH-RUNBOOK.md` shows no change inside any fenced block; state in the SUMMARY how that was checked.
    - The SUMMARY records an explicit verdict on the `Related:` paragraph — preserved-and-where, or dropped-and-why — with the `needs:` graph evidence behind it.
  </acceptance_criteria>
  <verify>
    <automated>grep -c 'TODO-009' docs/COMPANION-PUBLISH-RUNBOOK.md</automated>
    <fails_when>the printed count is anything other than 0</fails_when>
    <automated>awk 'BEGIN{f=0} /^```/{f=!f; next} f==0 && /[0-9]+\.[0-9]+\.[0-9]+/{c++} END{print c+0}' docs/COMPANION-PUBLISH-RUNBOOK.md</automated>
    <fails_when>the printed count is anything other than 0</fails_when>
    <automated>grep -c 'This document makes no version-specific claims\.' docs/COMPANION-PUBLISH-RUNBOOK.md</automated>
    <fails_when>the printed count is anything other than 1</fails_when>
    <automated>grep -cE '^## (Candidate authority|Exact seven-step operator sequence|Five states and one correction|Ordinary publication and recovery|CI ownership)$' docs/COMPANION-PUBLISH-RUNBOOK.md</automated>
    <fails_when>the printed count is anything other than 5</fails_when>
  </verify>
  <reversibility rating="reversible">Documentation only; every change is a text revert away.</reversibility>
  <done>The false section is gone, the runbook's prose makes no version-specific claim, and the invariant sentence is at the top.</done>
</task>

<task type="auto">
  <name>Task 2: Document `git subtree split` as the durable mirror-split mechanism (DOC-06)</name>
  <files>docs/COMPANION-PUBLISH-RUNBOOK.md</files>
  <read_first>
    - `docs/COMPANION-PUBLISH-RUNBOOK.md` as changed by Task 1 — specifically `### 5. Run the trusted mirror rehearsal`, `## Ordinary publication and recovery`, and `### Scope of the iOS mirror recovery mode`, the three places the mirror split is described without naming its mechanism.
    - `.github/workflows/ios-mirror-backfill.yml` around lines 134-154 — the `candidate-rehearsal` operation's actual split invocation and its no-push guarantee.
    - `script/release_candidate/ios_mirror.sh` and `script/verify_ios_mirror_backfill.sh` — the live split implementation the documentation must describe accurately, including where the computed split SHA comes from.
    - `.planning/workstreams/quality-ratchet-release/phases/175-rehearsal-and-publish/175-RESEARCH.md`, the State of the Art table — it records that the live-code half of DOC-06 is already satisfied and only the documentation gap remains.
    - `.planning/workstreams/quality-ratchet-release/phases/175-rehearsal-and-publish/175-CONTEXT.md`, decisions D-06 and D-18.
  </read_first>
  <action>
Add explicit `git subtree split` documentation to `docs/COMPANION-PUBLISH-RUNBOOK.md` in the two places
the mirror split is currently described only by effect.

In `### 5. Run the trusted mirror rehearsal`, name the mechanism: the rehearsal computes the mirror
commit with `git subtree split` over the iOS package path and validates the push with a dry run, never
pushing a ref. State that the split SHA is deterministic for a given source tree, which is why the
rehearsal's SHA and the publish's SHA are comparable.

In `## Ordinary publication and recovery` (and its `### Scope of the iOS mirror recovery mode`
subsection), state that the publish path computes the mirror commit with the same `git subtree split`
invocation the rehearsal used, so the recovery path and the ordinary path cannot diverge in mechanism.

Add one line recording that this is the durable mechanism and that the previously-used external splitter
is not to be reinstalled or referenced going forward. Do not add a migration note, a history section, or
an explanation of why the old tool was dropped — the runbook is an operator contract, not a changelog.

Verify before writing, and record the result in the SUMMARY, that no live workflow or script still
invokes the previous splitter. Prior research found exactly one residual reference, in an unrelated
comment in `script/check_ios_mirror_parity.sh` around line 31. If that is still the only one, leave it
alone and say so; it is a comment, not an invocation. If a live invocation is found, that is a finding to
record — not an extra edit to make quietly here.

Do not touch archived `.planning/` history; DOC-06's scope is live documentation only (D-18).

Anything new added here obeys the no-version-literal invariant: prose names the mechanism, not a version.

<!-- planner-discipline-allow: splitsh -->
The previous splitter's name (`splitsh-lite`) is named here only so the executor can grep for it; the
acceptance criteria grep live source directories, not this plan.
  </action>
  <acceptance_criteria>
    - `grep -c 'git subtree split' docs/COMPANION-PUBLISH-RUNBOOK.md` prints at least 3 — the rehearsal step, the ordinary publication section, and the recovery-mode subsection each name the mechanism.
    - `grep -rl 'splitsh' .github/ script/ lib/ scripts/` lists at most `script/check_ios_mirror_parity.sh`, and the SUMMARY states the exact file list observed.
    - `grep -c 'splitsh' docs/COMPANION-PUBLISH-RUNBOOK.md` — the SUMMARY records the count and whether the runbook mentions the previous splitter by name at all; either answer is acceptable as long as it is a deliberate, recorded choice and not an accident.
    - `awk 'BEGIN{f=0} /^```/{f=!f; next} f==0 && /[0-9]+\.[0-9]+\.[0-9]+/{c++} END{print c+0}' docs/COMPANION-PUBLISH-RUNBOOK.md` still prints 0 after this task.
    - `git status --porcelain .planning/` shows no modification to any archived milestone path.
    - The SUMMARY quotes the live split invocation from `script/release_candidate/ios_mirror.sh` and confirms the documentation describes it accurately rather than plausibly.
  </acceptance_criteria>
  <verify>
    <automated>test "$(grep -c 'git subtree split' docs/COMPANION-PUBLISH-RUNBOOK.md)" -ge 3; echo "subtree-mentions-exit=$?"</automated>
    <fails_when>the printed `subtree-mentions-exit` is anything other than 0</fails_when>
    <automated>awk 'BEGIN{f=0} /^```/{f=!f; next} f==0 && /[0-9]+\.[0-9]+\.[0-9]+/{c++} END{print c+0}' docs/COMPANION-PUBLISH-RUNBOOK.md</automated>
    <fails_when>the printed count is anything other than 0</fails_when>
    <automated>git status --porcelain .planning/workstreams/quality-ratchet-release/milestones/</automated>
    <fails_when>the output is non-empty — archived history was modified</fails_when>
  </verify>
  <reversibility rating="reversible">Documentation only.</reversibility>
  <done>The durable mirror-split mechanism is named in the runbook wherever the split is described, and the live-code state was re-verified rather than assumed.</done>
</task>

<task type="auto">
  <name>Task 3: The version-literal CI check, driven red before it is trusted (D-19)</name>
  <files>script/check_release_doc_version_literals.exs, .github/workflows/crosswake-ci.yml, .planning/workstreams/quality-ratchet-release/phases/175-rehearsal-and-publish/175-DOC-TRUTH.md</files>
  <read_first>
    - `script/check_release_workflow_integrity.exs`, its first two lines and its `@proof_lanes` constant with the DECLARED, NEVER DERIVED comment block — the exit-contract header convention and the declared-roster convention to copy.
    - `script/check_absence_is_not_success.exs` in full — the multi-paragraph `#` rationale header naming the specific past incident, and the `run/1` shape: per-check functions returning findings, an aggregate, two `[crosswake] OK:` lines and `0` on empty, per-finding `[crosswake] FAIL:` blocks and `1` otherwise. Copy this shape exactly.
    - `.github/workflows/crosswake-ci.yml` around lines 150-170 and 215-230 — where `script/check_release_workflow_integrity.exs` is invoked, and the step-summary remediation-line convention next to it.
    - `.planning/workstreams/quality-ratchet-release/VERIFICATION-CONVENTIONS.md` and `.planning/workstreams/quality-ratchet-release/phases/170-vacuous-assertion-remediation/170-VACUITY-TAXONOMY.md` — a new check clears the six-shape taxonomy with a measured fact before it becomes merge-blocking (VAC-03).
    - `docs/COMPANION-PUBLISH-RUNBOOK.md` and `docs/RELEASE-INCIDENT-RESPONSE.md` as they now stand — both must be clean before this check is wired.
  </read_first>
  <action>
Write `script/check_release_doc_version_literals.exs` and wire it into CI.

Shape and conventions, copied from the two existing check scripts:

- `#!/usr/bin/env elixir` and an exit-contract comment on line 2: `0` clean, `1` defect found, `3` could
  not verify.
- A multi-paragraph `#` rationale header naming the specific incident this check exists to catch: a
  version was written into runbook prose as though permanent, the condition it described was repaired,
  and the sentence survived. Name that, before any code.
- A module-level roster constant holding the two file paths in scope —
  `docs/COMPANION-PUBLISH-RUNBOOK.md` and `docs/RELEASE-INCIDENT-RESPONSE.md` — with a comment stating
  it is declared, never derived, and that widening it is a deliberate edit rather than a scan result.
  Exit `3` if a rostered file is missing, so a renamed document cannot make the check silently pass.
- `run/1` composing two independently callable check functions and aggregating their findings, in
  `check_absence_is_not_success.exs`'s exact reporting shape.

The two checks:

1. **Bare version literals outside code fences.** Strip fenced blocks first — track an open/closed
   state across lines beginning with three backticks — then match the remaining text against a
   three-component numeric version pattern. Every match is a finding carrying the file, the line number,
   and the offending line.
2. **The invariant marker is present.** Each rostered file must contain the sentence
   `This document makes no version-specific claims.` Its absence is a finding. This is the half that
   keeps the check from being satisfiable by deleting prose: a document with no literals and no marker
   fails.

Then prove the check has teeth before trusting it, and record the proof:

- Copy a rostered document to a scratch path under the phase evidence directory, inject a bare version
  literal into a prose line, run the check against a roster pointing at the scratch copy (supply the
  roster override as a command-line argument the script accepts for this purpose only — an argument that
  *narrows* for testing is acceptable; an argument that narrows the default is not), and confirm it exits
  `1` and names the injected line.
- Run it again against the same scratch copy with the literal removed and confirm it exits `0`, so the
  red is proven to be caused by the literal rather than by the check always failing.
- Run the real, no-argument invocation over the declared roster and confirm it exits `0`.

Write all three runs' output to
`.planning/workstreams/quality-ratchet-release/phases/175-rehearsal-and-publish/evidence/175-version-literal-check.log`,
redirecting stdout and stderr into the file directly rather than through a pipeline so each recorded
exit status is the script's own.

Wire the check into `.github/workflows/crosswake-ci.yml` as a step named
`Check release doc version literals`, running `elixir script/check_release_doc_version_literals.exs`,
placed in the same job as the existing `Check candidate workflow structure` step and immediately after
it. Add its remediation line to that job's step summary in the existing style.

Finally, write `175-DOC-TRUTH.md` recording: the vacuity-taxonomy row for this check — check id, a shape
letter from the six shapes or the explicit escape form with a reason, and the measured non-vacuity fact
(the injected literal it was observed failing on, quoted) — plus the DOC-04 and DOC-06 dispositions from
Tasks 1 and 2 with the evidence behind each. Never a bare tick, never a blank shape.

Stage `.planning/` paths explicitly by filename.
  </action>
  <acceptance_criteria>
    - `elixir script/check_release_doc_version_literals.exs` exits 0 against the declared roster and prints the `[crosswake] OK:` lines.
    - The script's line 2 is an exit-contract comment naming exit codes 0, 1 and 3.
    - The roster constant names exactly two paths and carries a declared-never-derived comment; `grep -c 'RELEASE-INCIDENT-RESPONSE' script/check_release_doc_version_literals.exs` prints at least 1.
    - Removing the invariant sentence from either rostered document causes the check to exit 1 — demonstrated on the scratch copy, not on the real file.
    - Injecting a bare version literal into a prose line of the scratch copy causes the check to exit 1 and the output names the injected line number.
    - Removing that literal from the scratch copy causes the check to exit 0 — the control case.
    - `.../evidence/175-version-literal-check.log` exists, is non-empty, and records three exit statuses: 1, 0, and 0, each labeled with which run produced it.
    - `.github/workflows/crosswake-ci.yml` contains a step named `Check release doc version literals` invoking the script, and `node scripts/ci_monitor.cjs check-actions` still reports `mutable_refs=0` after the edit.
    - `175-DOC-TRUTH.md` carries a taxonomy row with a non-blank shape and a quoted injected literal as its measured fact, plus a recorded disposition for DOC-04 and DOC-06.
  </acceptance_criteria>
  <verify>
    <automated>elixir script/check_release_doc_version_literals.exs</automated>
    <fails_when>non-zero exit — a rostered document carries a bare version literal outside a fence, is missing the invariant marker, or is missing entirely</fails_when>
    <automated>test -s .planning/workstreams/quality-ratchet-release/phases/175-rehearsal-and-publish/evidence/175-version-literal-check.log</automated>
    <fails_when>non-zero exit — the fail-first demonstration was not recorded</fails_when>
    <automated>grep -c 'Check release doc version literals' .github/workflows/crosswake-ci.yml</automated>
    <fails_when>the printed count is 0 — the check is not wired into CI</fails_when>
    <automated>node scripts/ci_monitor.cjs check-actions</automated>
    <fails_when>non-zero exit, or the summary line reports `mutable_refs=` anything other than 0</fails_when>
    <automated>elixir script/check_release_workflow_integrity.exs</automated>
    <fails_when>non-zero exit — the CI workflow edit broke an existing release-surface invariant</fails_when>
  </verify>
  <reversibility rating="costly">A new merge-blocking CI check. Removing it later is a one-file revert plus a workflow edit, but any branch open at the time inherits the gate, so the undo is coordinated rather than local.</reversibility>
  <done>A declared-roster check fails the build on a bare version literal or a missing invariant marker in either release document, and it has been observed red on an injected literal and green on its removal.</done>
</task>

</tasks>

<artifacts_produced>
## Artifacts this phase produces (plan 175-05)

| Kind | Name |
|---|---|
| New file | `script/check_release_doc_version_literals.exs` |
| New function | `bare_version_literals/1` in that script |
| New function | `invariant_marker_present/1` in that script |
| New CI step | `Check release doc version literals` in `.github/workflows/crosswake-ci.yml` |
| New doc invariant sentence | `This document makes no version-specific claims.` in `docs/COMPANION-PUBLISH-RUNBOOK.md` |
| Deleted doc section | the runbook's second `##` section warning about single-version publication |
| New file | `.planning/.../175-rehearsal-and-publish/175-DOC-TRUTH.md` |
| New file | `.planning/.../175-rehearsal-and-publish/evidence/175-version-literal-check.log` |
</artifacts_produced>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Documentation prose -> an operator mid-publish | A false warning in the runbook is read as current fact under time pressure |
| The check's declared roster -> the claim "the release docs make no version claims" | A roster smaller than the claim is the Wave 0 defect in a new place |
| A new merge-blocking check -> every open branch | A check wired before it is proven can block work while asserting nothing |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-175-22 | Spoofing | A false runbook section read as current fact during the publish window | high | mitigate | Task 1 deletes the section outright and an acceptance criterion witnesses the deletion through an identifier that occurs only inside it. |
| T-175-23 | Repudiation | A claim softened rather than removed, so a hedged falsehood survives | high | mitigate | D-17 is stated in the action, a prohibition forbids softening, and the witness grep would still pass on a reworded section — so the criterion also asserts the surrounding heading set is intact, making a stub heading visible. |
| T-175-24 | Tampering | A new version literal reintroduced into prose later | medium | mitigate | Task 3's check fails the build on any three-component literal outside a fence across a declared two-file roster, wired into CI. |
| T-175-25 | Spoofing | The check satisfiable by deleting prose rather than by being correct | high | mitigate | The second check function requires the invariant marker to be present in each rostered file, so an emptied document fails rather than passes. |
| T-175-26 | Repudiation | A check wired before it was shown to fail — the milestone's named defect in a new place | high | mitigate | Task 3 requires a recorded red on an injected literal and a green on its removal, on a scratch copy, before the check is trusted; VAC-03's taxonomy row with a quoted measured fact is required in `175-DOC-TRUTH.md`. |
| T-175-27 | Tampering | The check's roster narrowed to make a red go away | high | mitigate | The roster is a declared constant with a never-derived comment, a missing rostered file exits 3 rather than passing, and a prohibition forbids narrowing the roster as a remedy. |
| T-175-28 | Tampering | Archived planning history rewritten to satisfy a live documentation check | medium | mitigate | A prohibition forbids it, D-18 scopes DOC-06 to live documentation, and Task 2's verification asserts an empty `git status --porcelain` under the archived milestones path. |
| T-175-SC | Tampering | npm/pip/cargo installs | high | mitigate | Not applicable to this plan — it adds no package-manager install step. If any task later requires one, a package-legitimacy gate plus a blocking human checkpoint is required first. |
</threat_model>

<verification>
- `elixir script/check_release_doc_version_literals.exs` exits 0 over the declared two-file roster.
- The outside-fence version-literal count is 0 in both release documents, and both carry the invariant sentence.
- `grep -c 'TODO-009' docs/COMPANION-PUBLISH-RUNBOOK.md` prints 0.
- `grep -c 'git subtree split' docs/COMPANION-PUBLISH-RUNBOOK.md` prints at least 3.
- `.../evidence/175-version-literal-check.log` records exit statuses 1, 0 and 0 for the injected, control and real runs.
- `.github/workflows/crosswake-ci.yml` invokes the new check, and `check-actions` still reports `mutable_refs=0`.
- `elixir script/check_release_workflow_integrity.exs` exits 0.
</verification>

<success_criteria>
The release documentation says only true things, the specific way it stopped being true is now a build
failure rather than a thing to remember, and that build failure has been watched happening.
</success_criteria>

<output>
Create `.planning/workstreams/quality-ratchet-release/phases/175-rehearsal-and-publish/175-05-SUMMARY.md` when done.
</output>
