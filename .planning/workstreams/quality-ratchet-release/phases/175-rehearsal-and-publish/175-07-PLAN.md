---
phase: 175-rehearsal-and-publish
plan: 07
type: execute
wave: 7
depends_on: ["175-06"]
files_modified:
  - .planning/workstreams/quality-ratchet-release/phases/175-rehearsal-and-publish/175-PUBLISH-LEG-1.md
autonomous: false
requirements: [REL-12]

estimate:
  tokens: 70000
  raw_tokens: 35000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "The lower-blast-radius held companion publishes first, as the fire drill for the repaired clean-room lane, and the justification for which companion that is — download counts as an acknowledged imperfect proxy, no reverse dependency found in-repo, both companion publish jobs structurally identical — is recorded as a planning decision rather than treated as self-evident (D-01, D-02, D-03)."
    - "'Blast radius' means impact-if-the-publish-is-wrong, not how much of the pipeline the drill exercises; the two are recorded as separate properties and are not conflated (D-02)."
    - "The companion's larger or smaller code surface is explicitly not a factor in the ordering, because both companion publish jobs are structurally identical — same trigger, same dependency graph, Hex-only (D-04)."
    - "No core-first adjustment is applied: both companions declare a floor already satisfied by the live core release, so neither depends on the new core version existing first (D-05)."
    - "This leg touches one registry only. It does not pass through the approved-release-guard identity gate and does not touch the iOS mirror or Maven, so its risk posture is deliberately lighter than the core leg's (D-06, D-09)."
    - "A human authorized this publish at a checkpoint immediately before it, by typing back the Hex candidate-rehearsal run identifier read from this phase's own rehearsal evidence — a value that cannot be satisfied from what was typed at any other gate (D-07, D-08)."
    - "The pull request head SHA was re-confirmed immediately before dispatching the rehearsal and again immediately before merging; if they differed the rehearsal was redone rather than reconciled (D-33)."
    - "The package is live on Hex, confirmed by a direct query against the registry's own API showing the new version, never by re-reading the workflow that published it (D-30)."
    - "'No reverse dependents found' is recorded as 'no signal found', not as 'definitively zero' (D-03)."
    - "The incident-response runbook's recorded commit is confirmed to be an ancestor of the commit this leg publishes from, checked at the gate and re-checked by an automated assertion at the merge — because this leg's merge is the phase's first publish command, and the ROADMAP's before-any-publish bar must be checked here rather than assumed from plan ordering."
  prohibitions:
    - "Never satisfy a one-way-door checkpoint by auto-approval, a shared yes/no, or a value reproduced from a different gate; each gate's typed-back value must be read from that leg's own evidence."
    - "Never record a publish or proof leg as verified using evidence produced for a different version or a different candidate ref; the evidence must come from the run that attests the exact version and ref being claimed."
    - "Never describe `mix hex.retire` as undoing this publish; retirement is a forward-only advisory flag and the version stays resolvable."
    - "Never write a verification gate whose exit status is taken from the tail of a pipeline; a pipeline reports the last command's status and the gate would be green while asserting nothing."
    - "Never run `git add -A` or `git add .` under `.planning/`; stage `.planning/` paths explicitly by filename, and never stage `config.json`, `milestone.lock`, or `state.json` under the workstream directory."
  artifacts:
    - .planning/workstreams/quality-ratchet-release/phases/175-rehearsal-and-publish/175-PUBLISH-LEG-1.md
  key_links:
    - "`175-REHEARSAL-EVIDENCE.md`'s Hex leg run identifier -> gate 1's resume signal, which the operator must read from it and type back (D-08)."
    - "`docs/RELEASE-INCIDENT-RESPONSE.md`'s Hex rows -> gate 1's recovery statement, quoted rather than re-derived (D-10)."
    - "`175-INCIDENT-DOC-COMMIT.md`'s recorded SHA -> gate 1's displayed evidence and the ancestor check that discharges the before-any-publish bar at the phase's first publish command."
    - "the pull request head at rehearsal time -> the head at merge time -> the published artifact's provenance."
---

<planner_notes>
**Edge-probe accounting.** The spec-less probe surfaced 1 row for REL-12, `unclassified` and therefore
**unresolved**. It is not auto-resolved with a backstop; it is surfaced below as a flagged planner
assumption. **1 surfaced == 0 authored as resolved truths + 1 flagged.**

**Gate weight.** This gate is deliberately light (D-09). It guards a single Hex publish with a real
forward recovery path. The heavy gate is `175-08`'s, which guards three registries at once. Making all
three gates identically weighty is how approval fatigue is manufactured; making them visibly different
documents is the mitigation the decisions asked for.

**Why a checkpoint at all, when the companion pick is already decided.** The reversibility gate is not
re-asking which companion goes first — D-01 settled that. It is confirming, against live evidence read
moments before, that the thing about to be published is the thing that was rehearsed. That is a fact
about the present, not a decision about direction, and it is why the resume signal is a value read from
evidence rather than a yes.
</planner_notes>

<flagged_assumptions>
REL-12's edge-coverage row came back `unclassified` from the spec-less probe and remains **unresolved**.
Recorded rather than dropped: the planner's assumption is that "the lower-blast-radius held companion
publishes live as the fire-drill for the repaired clean-room lane" is satisfied by the companion named in
D-01 reaching the Hex registry through its ordinary publish job, and that the clean-room lane counts as
exercised when that job's downstream clean-room proof runs against the freshly published package. If the
clean-room lane does not run for a companion publish in this repository's current graph, the fire-drill
half of REL-12 is not satisfied by the publish alone — that is a finding to record in Task 3, not a
detail to assume away.
</flagged_assumptions>

<scope_boundary>
This plan publishes exactly one companion package to exactly one registry. It does not merge the core
release pull request, does not touch the iOS mirror or Maven, and does not add companion state to the
linked-release rollup — that rollup structurally has no companion inputs and widening it would expand a
check's meaning without a requirement behind it (D-31).

If the publish fails part way, stop and open `docs/RELEASE-INCIDENT-RESPONSE.md`. Do not improvise a
recovery; that document exists precisely so this moment is not where recovery gets designed.
</scope_boundary>

<objective>
Publish the lower-blast-radius held companion as the fire drill for the repaired lane, behind a human
gate, and confirm it live against the registry itself.

Purpose: this is the first irreversible act of the milestone, chosen deliberately to be the cheapest one
to have been wrong about. If the repaired publish path has a defect, it surfaces here, on a package with
the smallest observable consumer surface, rather than on the core release that three registries depend
on.

Output: a merged companion pull request, a completed publish job, a live registry confirmation, and a
recorded ordering justification.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/quality-ratchet-release/ROADMAP.md
@.planning/workstreams/quality-ratchet-release/phases/175-rehearsal-and-publish/175-CONTEXT.md
@.planning/workstreams/quality-ratchet-release/phases/175-rehearsal-and-publish/175-REHEARSAL-EVIDENCE.md
@.planning/workstreams/quality-ratchet-release/phases/175-rehearsal-and-publish/175-INCIDENT-DOC-COMMIT.md
@docs/RELEASE-INCIDENT-RESPONSE.md
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking-human">
  <name>Task 1: Gate 1 of 3 — authorize the first companion Hex publish</name>
  <read_first>
    - `.planning/workstreams/quality-ratchet-release/phases/175-rehearsal-and-publish/175-REHEARSAL-EVIDENCE.md` — the Hex leg's run identifier, `package_count`, and `external_state_changed`, which this gate displays and whose run identifier is the resume signal.
    - `.planning/workstreams/quality-ratchet-release/phases/175-rehearsal-and-publish/175-INCIDENT-DOC-COMMIT.md` — the runbook's recorded commit SHA, which this gate displays, and the ancestor command it names. This leg's merge is the phase's first publish command, so the before-any-publish bar is checked here, not first at gate 2.
    - `docs/RELEASE-INCIDENT-RESPONSE.md`, the Hex rows of `## Mid-sequence partial failure` and the Hex subsection of `## Retire / backfill after a bad publish` — quote the recovery statement from there; do not re-derive it (D-10).
    - `.planning/workstreams/quality-ratchet-release/phases/175-rehearsal-and-publish/175-CONTEXT.md`, decisions D-01 through D-11.
    - `.planning/workstreams/quality-ratchet-release/STATE.md`'s open-release-PR triage table.
  </read_first>
  <action>
Re-fetch this leg's live evidence immediately before presenting, so nothing shown to the operator is
stale. Run `gh pr view <companion-pr> --json number,headRefOid,mergeable,mergeStateStatus,title` and
`gh run view <hex-rehearsal-run-id> --json conclusion,headSha`, query the package's current state on
the Hex API, and run the ancestor check naming the runbook's recorded SHA from
`175-INCIDENT-DOC-COMMIT.md`. Do not merge anything in this task and do not merge under any answer.

Present, in this order and no more than this:

1. The companion pull request number, its title, and its current head SHA — alongside the head SHA the
   Hex rehearsal ran against. State plainly whether they are equal. If they are not, the correct answer
   is to stop and redo the rehearsal, and say so.
2. The package name and the version about to become permanently listed, and the versions currently live
   on Hex for it.
3. The rehearsal evidence, by field: the Hex candidate-rehearsal run identifier, `package_count` as
   observed, and `external_state_changed` as observed. Name the fields; do not summarize them as "the
   rehearsal passed" (D-11).
4. The incident-response runbook's recorded commit SHA, read from `175-INCIDENT-DOC-COMMIT.md`, and the
   result of the ancestor check against the commit this leg would publish from. This merge is the
   phase's first publish command, so this is where the ROADMAP's before-any-publish bar is discharged.
   If the runbook commit is not an ancestor — which a squash or rebase can cause between the recording
   and now — the bar is unmet and the correct answer is to stop and re-derive the SHA.
5. The one-line recovery statement, quoted from `docs/RELEASE-INCIDENT-RESPONSE.md`'s Hex subsection:
   retirement is a forward-only advisory flag, the version stays resolvable, and existing lockfiles
   continue to work. Link the section rather than paraphrasing it.
6. One line on scope: this leg touches Hex only. It does not pass through the identity gate the core
   release uses, and it does not touch the iOS mirror or Maven (D-06).

Keep it to that — one evidence line for the runbook check, no more. This is still the light gate; the
heavy one is gate 2, and this addition does not promote gate 1 to gate 2's form (D-09).
  </action>
  <decision>Merge the lower-blast-radius companion pull request now, accepting that its Hex publish cannot be withdrawn — or stop here.</decision>
  <context>
A Hex publish is a one-way door. `mix hex.retire` sets an advisory flag; it does not remove the package
or the version, and existing lockfiles continue to resolve it. Recovery is retire-forward only.

This companion goes first because it is the cheaper one to have been wrong about, not because it
exercises more of the pipeline — those are separate properties (D-02). The signal behind the pick is
download count, which is an acknowledged imperfect proxy: no reverse dependency exists in this
repository for either companion, but that is "no signal found", not "definitively zero" (D-03).

Both companion publish jobs are structurally identical — same trigger, same dependency graph, Hex-only —
so the drill exercises the publish pipeline rather than the package's own code, and the larger-code-surface
companion is explicitly not the better fire drill for that reason (D-04).
  </context>
  <options>
    <option id="publish-companion-1">
      <name>Merge now and let the publish job run</name>
      <pros>The repaired lane is exercised on the cheapest package to have been wrong about, before the core release depends on it working. The rehearsal evidence is minutes old rather than days.</pros>
      <cons>The version becomes permanently listed on Hex. If a defect is found afterward, the only recovery is retire-forward — the version stays resolvable with an advisory flag.</cons>
    </option>
    <option id="stop-before-leg-1">
      <name>Stop here</name>
      <pros>Nothing is published. The phase can resume after re-rehearsing, or after whatever concern prompted the stop is resolved.</pros>
      <cons>The core release stays blocked; the phase's remaining requirements cannot be satisfied without this leg.</cons>
    </option>
  </options>
  <resume-signal>Reply `publish leg 1 <hex-rehearsal-run-id>` with the Hex candidate-rehearsal run identifier read from `175-REHEARSAL-EVIDENCE.md`, or `stop` to halt the phase here. A different gate's run identifier is not accepted.</resume-signal>
  <acceptance_criteria>
    - The gate displayed the runbook's recorded commit SHA from `175-INCIDENT-DOC-COMMIT.md` together with a freshly run ancestor-check result, and a not-an-ancestor result was presented as a stop rather than as a caveat.
  </acceptance_criteria>
  <reversibility rating="one-way">A Hex publish cannot be withdrawn; retirement flags the version but does not remove it, and existing lockfiles keep resolving it.</reversibility>
  <done>The maintainer has explicitly authorized or declined this publish, with the head-SHA comparison, the named rehearsal fields, the runbook SHA and its ancestor-check result, and the quoted Hex recovery statement in front of them.</done>
</task>

<task type="auto">
  <name>Task 2: Re-confirm the head, merge the companion pull request, and watch its publish job</name>
  <precondition>Gate 1 was answered with the Hex candidate-rehearsal run identifier from `175-REHEARSAL-EVIDENCE.md`; a `stop` answer halts this plan.</precondition>
  <files>.planning/workstreams/quality-ratchet-release/phases/175-rehearsal-and-publish/175-PUBLISH-LEG-1.md</files>
  <read_first>
    - `.github/workflows/release-please.yml` around lines 295-310 and 450-465 — the companion Hex publish jobs and their dependency graph. Confirm for yourself that the companion job depends on the release-please job only and never on the approved-release-guard job, so no approval receipt is produced or awaited for this merge (Pitfall 2).
    - `175-REHEARSAL-EVIDENCE.md` — the head SHA the rehearsal ran against.
    - `175-INCIDENT-DOC-COMMIT.md` — the runbook's recorded commit SHA and the `git merge-base --is-ancestor` command it names, re-run here because this merge is the phase's first publish command.
    - `docs/COMPANION-PUBLISH-RUNBOOK.md`'s `## Ordinary publication and recovery` section — the ordinary path this merge follows.
    - `docs/RELEASE-INCIDENT-RESPONSE.md`'s `## Mid-sequence partial failure` Hex rows — read before merging, so a partial failure is answered from the table rather than improvised.
  </read_first>
  <action>
Re-confirm, then merge, then watch.

**Re-confirm the head.** Run `gh pr view <companion-pr> --json headRefOid` again, immediately before
merging, and compare against the head recorded in `175-REHEARSAL-EVIDENCE.md`. If they differ, do not
merge: return to `175-06` and redo the Hex rehearsal against the new head, then re-run gate 1. This is
not a formality — the pull request reports a behind-base merge state and its head can be refreshed
between the gate and the merge (D-33, Pitfall 1).

**Re-check the runbook ancestor bar.** Immediately before merging, re-run
`git merge-base --is-ancestor <runbook-sha> HEAD` with the SHA recorded in `175-INCIDENT-DOC-COMMIT.md`,
taking the check's own exit status and never a pipeline tail's. A non-zero result means the recorded SHA
went stale — a squash or rebase rewrote it — and the bar is unmet: stop, re-derive the SHA in
`175-INCIDENT-DOC-COMMIT.md`, and re-run gate 1. Do not merge on a stale SHA.

**Merge.** Merge through the normal protected-branch path. An admin-bypass merge is refused in this
repository and must not be attempted. Record the merge commit SHA, and re-run the ancestor check against
it, recording the result.

**Watch the publish job.** The tag-creation signal from the release job triggers the companion Hex
publish job. Wait for it and record its run identifier, its job-level conclusion, and its duration. Do
not wait for an approval receipt or an approved-version binding; none is produced for a companion merge
and waiting for one is a known trap.

**If the job fails.** Stop. Open `docs/RELEASE-INCIDENT-RESPONSE.md`, find the matching Hex failure-mode
row, and follow it. Record which row matched and what it directed. Do not retry blindly — a retry after a
successful publish is a duplicate-publish attempt and has its own row.

Begin `175-PUBLISH-LEG-1.md` with: the re-confirmed head SHA and the rehearsal head SHA side by side with
an explicit equal / not-equal verdict; the merge commit SHA; the runbook ancestor-check result with the
SHA it was run against; the publish job's run identifier, URL and conclusion; and gate 1's answer
verbatim with its timestamp.

**Label the two commit SHAs on their own lines, exactly.** This document records more than one resolvable
40-hex commit, so a verification that took the last bare hex token in the file would re-assert the wrong
one and report green while proving nothing. Write each on a line of its own, starting at column one, with
these exact labels and nothing else before them:

- a line beginning `Merge commit SHA:` followed by the merge commit's 40-hex SHA
- a line beginning `Runbook SHA checked:` followed by the runbook SHA the ancestor check ran against

Both labels are load-bearing: the verifications below extract by label, not by position, and an absent or
mislabeled line fails the task closed. Each label appears exactly once.

Stage `.planning/` paths explicitly by filename.
  </action>
  <acceptance_criteria>
    - `175-PUBLISH-LEG-1.md` records the pre-merge head SHA and the rehearsal head SHA with an explicit equal / not-equal verdict, and the verdict is equal.
    - The file carries exactly one line beginning `Merge commit SHA:` whose 40-character SHA `git cat-file -e` resolves, and exactly one line beginning `Runbook SHA checked:`; the merge SHA is extracted by that label rather than by position.
    - `git merge-base --is-ancestor <runbook-sha> <merge-sha>` exits 0 for the SHA recorded in `175-INCIDENT-DOC-COMMIT.md`, and the file records that result beside the SHA it was run against on the `Runbook SHA checked:` line.
    - The file records a publish job run identifier whose `gh run view <id> --json conclusion` reports `success`.
    - The file records gate 1's answer verbatim, including the run identifier the operator typed, and that identifier equals the one in `175-REHEARSAL-EVIDENCE.md`.
    - No approval receipt or approved-version artifact was awaited; the SUMMARY states this explicitly with the job's dependency graph as evidence.
    - If the publish job failed, the file names the matched failure-mode row from `docs/RELEASE-INCIDENT-RESPONSE.md` and what it directed.
  </acceptance_criteria>
  <verify>
    <automated>RUNBOOK=$(grep -oE '\b[0-9a-f]{40}\b' .planning/workstreams/quality-ratchet-release/phases/175-rehearsal-and-publish/175-INCIDENT-DOC-COMMIT.md | head -1); git merge-base --is-ancestor "$RUNBOOK" HEAD; echo "runbook-ancestor-exit=$? runbook=$RUNBOOK"</automated>
    <fails_when>the printed `runbook-ancestor-exit` is anything other than 0, or `runbook` is empty — the before-any-publish bar is unmet at the phase's first publish command</fails_when>
    <automated>RUN=$(grep -oE 'run[ _-]?id[^0-9]*[0-9]{6,}' .planning/workstreams/quality-ratchet-release/phases/175-rehearsal-and-publish/175-PUBLISH-LEG-1.md | grep -oE '[0-9]{6,}' | tail -1); gh run view "$RUN" --json conclusion --jq '.conclusion' > /tmp/leg1-conclusion.txt 2>&1; grep -qx 'success' /tmp/leg1-conclusion.txt; echo "leg1-conclusion-exit=$? run=$RUN"</automated>
    <fails_when>the printed `leg1-conclusion-exit` is anything other than 0, or `run` is empty</fails_when>
    <automated>LINE=$(grep -m1 '^Merge commit SHA:' .planning/workstreams/quality-ratchet-release/phases/175-rehearsal-and-publish/175-PUBLISH-LEG-1.md); LABEL_EXIT=$?; SHA=$(printf '%s' "$LINE" | grep -oE '[0-9a-f]{40}'); git cat-file -e "$SHA"; echo "merge-sha-exit=$? label-exit=$LABEL_EXIT sha=$SHA"</automated>
    <fails_when>the printed `merge-sha-exit` is anything other than 0, or `label-exit` is anything other than 0, or `sha` is empty — the labeled merge-commit line is missing, so the check would otherwise have re-asserted some other 40-hex token in the file</fails_when>
  </verify>
  <reversibility rating="one-way">Merging this pull request triggers a Hex publish that cannot be withdrawn; the version stays permanently listed and resolvable even after retirement.</reversibility>
  <done>The companion pull request is merged from the exact head that was rehearsed, and its Hex publish job has completed with a recorded conclusion.</done>
</task>

<task type="auto">
  <name>Task 3: Confirm the companion is live by querying Hex directly</name>
  <precondition>`175-PUBLISH-LEG-1.md` records a completed publish job run identifier.</precondition>
  <files>.planning/workstreams/quality-ratchet-release/phases/175-rehearsal-and-publish/175-PUBLISH-LEG-1.md</files>
  <read_first>
    - `.planning/workstreams/quality-ratchet-release/phases/175-rehearsal-and-publish/175-RESEARCH.md`, the "Independent registry verification" code block — the exact Hex API query shape to use.
    - `175-PUBLISH-LEG-1.md` as written by Task 2.
    - `.github/workflows/release-please.yml`'s clean-room proof lanes for companion packages — to determine whether the clean-room lane actually ran for this publish, which is the open half of REL-12's flagged assumption.
  </read_first>
  <action>
Confirm the publish against the registry itself, not against the workflow that performed it (D-30).

Query the Hex API for the published package and record the full release list it returns and the
latest-stable version, together with the timestamp of the query. The new version must be present. Write
the raw response body to
`.planning/workstreams/quality-ratchet-release/phases/175-rehearsal-and-publish/evidence/175-leg1-hex-registry.json`,
redirecting output into the file directly rather than through a pipeline so the recorded exit status is
`curl`'s own. A response that does not parse, or an empty release list, is a failure — not an empty
success.

Then resolve REL-12's flagged assumption about the fire-drill half. Determine, from run evidence rather
than from reading the workflow, whether a clean-room proof lane executed against the freshly published
companion package. Record the answer as **exercised** or **not exercised**, with the run identifier or
the explicit absence of one. If it did not run, say so plainly and record what would make it run; do not
let REL-12 read as fully satisfied on the publish alone.

Close `175-PUBLISH-LEG-1.md` with two sections:

- **Registry confirmation.** The queried version list, the latest stable version, the query timestamp,
  and an explicit live / not-live verdict.
- **Ordering justification (D-03).** The recorded reason this companion went first: the download counts
  observed and when they were observed, the absence of any in-repo reverse dependency, and an explicit
  statement that this is "no signal found" rather than "definitively zero". Note that the dependents
  graph was not queried and that download count is an acknowledged imperfect proxy. Note also that
  "blast radius" here means impact-if-wrong and not pipeline coverage, and that the other companion's
  larger code surface was explicitly not a factor because both publish jobs are structurally identical.

Stage `.planning/` paths explicitly by filename.
  </action>
  <acceptance_criteria>
    - `.../evidence/175-leg1-hex-registry.json` exists, is non-empty, and parses as JSON containing a non-empty release list.
    - The new companion version is present in that release list; the SUMMARY quotes the matched entry.
    - `175-PUBLISH-LEG-1.md` records the query timestamp and an explicit live / not-live verdict, and the verdict is live.
    - The file records an explicit exercised / not-exercised verdict for the clean-room lane, backed by a run identifier or by a stated absence of one.
    - The ordering justification section records the observed download counts with their observation date, the in-repo reverse-dependency finding, and the words distinguishing "no signal found" from "definitively zero".
    - No fact in this task's record is sourced from reading a workflow definition; the SUMMARY states the source of each.
  </acceptance_criteria>
  <verify>
    <automated>test -s .planning/workstreams/quality-ratchet-release/phases/175-rehearsal-and-publish/evidence/175-leg1-hex-registry.json</automated>
    <fails_when>non-zero exit — the registry response was not captured</fails_when>
    <automated>python3 -c "import json,sys; d=json.load(open('.planning/workstreams/quality-ratchet-release/phases/175-rehearsal-and-publish/evidence/175-leg1-hex-registry.json')); rs=d.get('releases') or []; print('releases=%d latest=%s' % (len(rs), d.get('latest_stable_version'))); sys.exit(0 if rs else 1)"</automated>
    <fails_when>non-zero exit — the response does not parse or the release list is empty, which is an empty success rather than a confirmation</fails_when>
    <automated>test -s .planning/workstreams/quality-ratchet-release/phases/175-rehearsal-and-publish/175-PUBLISH-LEG-1.md</automated>
    <fails_when>non-zero exit — the leg record is missing or empty</fails_when>
  </verify>
  <reversibility rating="reversible">A read-only registry query and a record document.</reversibility>
  <done>The companion is confirmed live by the registry's own API, the clean-room lane's exercised state is decided rather than assumed, and the ordering justification is on disk as a recorded decision.</done>
</task>

</tasks>

<artifacts_produced>
## Artifacts this phase produces (plan 175-07)

| Kind | Name |
|---|---|
| New file | `.planning/.../175-rehearsal-and-publish/175-PUBLISH-LEG-1.md` |
| New file | `.planning/.../175-rehearsal-and-publish/evidence/175-leg1-hex-registry.json` |
| Document section | the pre-merge head comparison with its equal / not-equal verdict |
| Document section | registry confirmation with a live / not-live verdict |
| Document section | the D-03 ordering justification record |
| Recorded value | the companion merge commit SHA |
| Recorded value | the companion Hex publish job run identifier |
| Checkpoint | gate 1 of 3, resume signal `publish leg 1 <hex-rehearsal-run-id>` |
</artifacts_produced>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| A human approval -> an irreversible registry mutation | The checkpoint is the only thing between the plan and a permanently listed version |
| A moving pull-request head -> the tree that actually publishes | The head can refresh between the gate and the merge |
| The publish workflow's own report -> the claim that the package is live | A workflow reporting success is not the registry reporting the version |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-175-35 | Spoofing | Approval fatigue across three near-identical gates | high | mitigate | The resume signal is a leg-specific, non-fungible run identifier the operator must read from this phase's own rehearsal evidence; a value typed at another gate is not accepted (D-08). Gate weight is deliberately asymmetric (D-09). |
| T-175-36 | Tampering | Publishing a tree other than the one rehearsed | high | mitigate | Task 2 re-confirms the head immediately before merging and requires an explicit equal / not-equal verdict; a mismatch returns to the rehearsal plan rather than proceeding (D-33). |
| T-175-37 | Repudiation | "It published" claimed from the workflow's own conclusion | high | mitigate | Task 3 queries the registry API directly, captures the raw response to disk, and fails on an unparseable body or an empty release list. |
| T-175-38 | Elevation of Privilege | An admin-bypass merge skipping required checks on a publish-triggering merge | high | mitigate | Task 2 states the normal protected-branch path is required and that admin-bypass is refused in this repository. |
| T-175-39 | Repudiation | A partial failure answered by improvisation rather than by the response table | high | mitigate | Task 2 requires reading the Hex failure-mode rows before merging and, on failure, recording which row matched and what it directed; blind retry is called out as its own failure mode. |
| T-175-40 | Spoofing | REL-12 reading as satisfied when only half of it — the publish, not the fire drill — actually happened | high | mitigate | Task 3 requires an explicit exercised / not-exercised verdict for the clean-room lane from run evidence, and forbids letting the publish alone close the requirement. |
| T-175-40b | Repudiation | The before-any-publish runbook bar passing by plan ordering rather than by a check, at the phase's actual first publish command | high | mitigate | Gate 1 displays the runbook's recorded SHA and a freshly run ancestor-check result, and Task 2 re-runs `git merge-base --is-ancestor` with the check's own exit status immediately before merging; a stale SHA is defined as a stop, not a caveat. |
| T-175-SC | Tampering | npm/pip/cargo installs | high | mitigate | Not applicable to this plan — it adds no package-manager install step. The published package is a first-party, already-audited package whose next version this phase releases. |
</threat_model>

<verification>
- Gate 1 was answered with the Hex candidate-rehearsal run identifier, recorded verbatim, and it matches `175-REHEARSAL-EVIDENCE.md`.
- The pre-merge head SHA equals the rehearsal head SHA, with an explicit recorded verdict.
- `git merge-base --is-ancestor <runbook-sha> HEAD` exits 0 for the SHA recorded in `175-INCIDENT-DOC-COMMIT.md`, checked at gate 1 and again immediately before the merge.
- The merge commit SHA resolves and the companion Hex publish job's conclusion is `success`.
- A direct Hex API query returns a parseable body with a non-empty release list containing the new version, captured to disk with a timestamp.
- The clean-room lane carries an explicit exercised / not-exercised verdict backed by a run identifier or a stated absence.
- The ordering justification is recorded with observed download counts, their date, and the "no signal found" qualification.
</verification>

<success_criteria>
The first one-way door is walked through deliberately: a human authorized it against evidence minutes
old, the tree that published is provably the tree that was rehearsed, the registry itself confirms the
version is live, and the reason this package went first is written down rather than assumed.
</success_criteria>

<output>
Create `.planning/workstreams/quality-ratchet-release/phases/175-rehearsal-and-publish/175-07-SUMMARY.md` when done.
</output>
