---
phase: 175-rehearsal-and-publish
plan: 08
type: execute
wave: 8
depends_on: ["175-07"]
files_modified:
  - .planning/workstreams/quality-ratchet-release/phases/175-rehearsal-and-publish/175-PUBLISH-LEG-2.md
autonomous: false
requirements: [REL-13]

estimate:
  tokens: 85000
  raw_tokens: 42500
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "`crosswake 0.2.2` is live on Hex, on the iOS mirror, and on Maven, each confirmed by an independent query against that registry itself rather than by re-reading the workflow that published it (D-30)."
    - "This is the only leg that opens all three one-way doors at once; its gate carries the full evidence block and the three-registry irreversibility statement, and is deliberately heavier than the two companion gates (D-06, D-09)."
    - "The gate displays named evidence fields rather than a summary verdict: the iOS rehearsal's `state`, `authorization_result` and `external_state_changed`, the Maven fire drill's validated-then-dropped deployment, and the recorded commit SHA of the incident-response runbook (D-11)."
    - "The gate's recovery language for each registry is quoted from or linked to the response table rather than independently re-derived, so the two cannot drift apart (D-10)."
    - "The operator authorized this leg by typing back the iOS candidate-rehearsal run identifier — a different named value from the one gate 1 accepted, so a tired operator cannot satisfy this gate from what they typed at the previous one (D-08)."
    - "The pushed mirror tag lands on exactly the split SHA the iOS rehearsal computed; a different SHA means the published tree is not the rehearsed tree."
    - "The incident-response runbook's recorded commit is an ancestor of the commit this leg publishes from, so the ROADMAP's before-the-first-publish bar is checked rather than asserted."
    - "The pull request head SHA was re-confirmed immediately before merging and equals the head every rehearsal leg ran against (D-33)."
  prohibitions:
    - "Never satisfy a one-way-door checkpoint by auto-approval, a shared yes/no, or a value reproduced from a different gate; each gate's typed-back value must be read from that leg's own evidence."
    - "Never record a publish or proof leg as verified using evidence produced for a different version or a different candidate ref; the evidence must come from the run that attests the exact version and ref being claimed."
    - "Never describe a Maven re-upload, an iOS tag re-point, or a Hex retirement as undoing this publish; a published Maven coordinate is permanent, a re-pointed tag does not reach consumers who already resolved, and retirement is forward-only."
    - "Never write a verification gate whose exit status is taken from the tail of a pipeline; a pipeline reports the last command's status and the gate would be green while asserting nothing."
    - "Never run `git add -A` or `git add .` under `.planning/`; stage `.planning/` paths explicitly by filename, and never stage `config.json`, `milestone.lock`, or `state.json` under the workstream directory."
  artifacts:
    - .planning/workstreams/quality-ratchet-release/phases/175-rehearsal-and-publish/175-PUBLISH-LEG-2.md
  key_links:
    - "`175-REHEARSAL-EVIDENCE.md`'s iOS leg run identifier -> gate 2's resume signal (D-08)."
    - "`175-INCIDENT-DOC-COMMIT.md`'s recorded SHA -> gate 2's displayed evidence and the ancestor check that discharges SC1 (D-11)."
    - "the rehearsal's computed split SHA -> the mirror tag this leg pushes, which must land on it."
    - "`docs/RELEASE-INCIDENT-RESPONSE.md`'s three registry groups -> gate 2's three recovery statements, quoted not paraphrased (D-10)."
---

<planner_notes>
**Edge-probe accounting.** The spec-less probe surfaced 1 row for REL-13, `unclassified` and therefore
**unresolved**. Not auto-resolved with a backstop; surfaced below as a flagged planner assumption.
**1 surfaced == 0 authored as resolved truths + 1 flagged.**

**This phase cannot run unattended.** Three human gates are required, and
`--no-reversibility-gates` must not be used when planning or executing this phase (D-12). This plan
carries the heaviest of the three.

**Why gate 2 is not merged into gate 1.** A single up-front gate would authorize the Maven upload using
rehearsal evidence already stale by the time that leg fires, and would give the operator no opportunity
to react to what leg 1 actually showed (D-07). The gates are per-leg for that reason, not for ceremony.
</planner_notes>

<flagged_assumptions>
REL-13's edge-coverage row came back `unclassified` and remains **unresolved**. Recorded rather than
dropped: the planner's assumption is that "publishes through the version-generalized graph to Hex, the
iOS mirror, and Maven" is satisfied when all three ordinary publish jobs run to `success` for this
version and all three registries independently confirm the artifact — and specifically that a release
completing through an exact-ref recovery path would *not* satisfy it, because the ordinary jobs would not
have run. If this leg completes through recovery rather than through the ordinary path, that is a finding
to record in Task 3, and it also changes what `175-09` can claim.
</flagged_assumptions>

<scope_boundary>
This plan merges one pull request and confirms three registries. It does not touch a companion package.
It does not add companion state to the linked-release rollup (D-31). It does not author or amend
`docs/RELEASE-INCIDENT-RESPONSE.md` — it quotes it.

If any leg of the three-registry publish fails, stop and work the response table. Two of the three
registries succeeding is exactly the partial-failure case that document was written before this moment
to answer.
</scope_boundary>

<objective>
Publish `crosswake 0.2.2` through the repaired, version-generalized graph to all three registries, behind
the phase's heaviest gate, and confirm each registry independently.

Purpose: this is the milestone's thesis. The version gate was generalized without generalizing the
authority; this is the first real release exercising that for a version other than the one the gate used
to be welded to. It is also the only leg that opens three one-way doors simultaneously — Hex permanence,
an iOS mirror tag consumers can already have resolved, and a Maven coordinate that is permanent the
moment it is published.

Output: a merged core release, three registry confirmations from three independent queries, and a
recorded gate answer.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/quality-ratchet-release/ROADMAP.md
@.planning/workstreams/quality-ratchet-release/phases/175-rehearsal-and-publish/175-CONTEXT.md
@.planning/workstreams/quality-ratchet-release/phases/175-rehearsal-and-publish/175-REHEARSAL-EVIDENCE.md
@.planning/workstreams/quality-ratchet-release/phases/175-rehearsal-and-publish/175-INCIDENT-DOC-COMMIT.md
@docs/RELEASE-INCIDENT-RESPONSE.md
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking-human">
  <name>Task 1: Gate 2 of 3 — authorize the three-registry core publish</name>
  <read_first>
    - `175-REHEARSAL-EVIDENCE.md` in full — all three legs' run identifiers and asserted field values, and the computed split SHA. The iOS leg's run identifier is this gate's resume signal.
    - `175-INCIDENT-DOC-COMMIT.md` — the runbook's recorded commit SHA, which this gate displays, and the ancestor command it names.
    - `175-PUBLISH-LEG-1.md` — what leg 1 actually showed, including its clean-room verdict. The operator is entitled to react to it here; that is why this gate is not merged into gate 1.
    - `docs/RELEASE-INCIDENT-RESPONSE.md` — the Hex, iOS mirror and Maven rows of `## Mid-sequence partial failure` and all three subsections of `## Retire / backfill after a bad publish`. Quote from these; do not paraphrase (D-10).
    - `.planning/workstreams/quality-ratchet-release/phases/175-rehearsal-and-publish/175-CONTEXT.md`, decisions D-06 through D-12 and D-20.
  </read_first>
  <action>
Re-fetch every displayed fact immediately before presenting. Run
`gh pr view <core-pr> --json number,headRefOid,baseRefOid,mergeable,mergeStateStatus,title`, fetch
`origin/main` and `pull/<core-pr>/head`, require the fetched OIDs to equal those live base/head values,
and run `script/check_release_runbook_ancestry.sh` against each exact full OID. The helper rejects
symbolic local `HEAD`. Query all three registries for the target version's current absence. Do not merge
anything in this task and do not merge under any answer.

This is the heavy gate. Present the full evidence block:

1. **Identity.** The core pull request number and its current head SHA, beside the head SHA all three
   rehearsal legs ran against, with an explicit equal / not-equal statement. Not equal means stop and
   re-rehearse.
2. **Rehearsal evidence, by named field.** The Hex leg's run identifier with `package_count` and
   `external_state_changed` as observed; the iOS leg's run identifier with `state`,
   `authorization_result` and `external_state_changed` as observed, plus the computed split SHA; the
   Maven fire drill's deployment identifier, its observed validated state, and its drop confirmation.
   Name each field. Do not collapse any of this into "the rehearsals passed" (D-11).
3. **The runbook's recorded commit SHA**, and the results against the freshly fetched exact remote
   `baseRefOid` and `headRefOid` (D-11). If either remote object does not contain the runbook commit,
   the ROADMAP's bar is not met and the correct answer is to stop; local `HEAD` is not evidence.
4. **The three-registry irreversibility statement**, one line each, quoted from
   `docs/RELEASE-INCIDENT-RESPONSE.md`:
   - Hex: the version becomes permanently listed; retirement flags it but does not remove it and existing
     lockfiles keep resolving it.
   - Maven: free to drop from the validated state, **permanent once published**. There is no retire.
     Recovery is a superseding version only.
   - iOS mirror: the tag can be re-pointed, but consumers who already resolved the old commit are
     unaffected by the re-point — SwiftPM's resolved-package cache holds what it already fetched.
   Link each to its section anchor rather than restating it in your own words.
5. **Current registry state**, freshly observed: the versions live on Hex, whether the target mirror tag
   exists, and the HTTP status of the target Maven POM. All three must show the version absent; a
   version already present is a duplicate-publish case with its own row in the table.
6. **What leg 1 showed**, in one or two lines — its registry confirmation and its clean-room verdict —
   so the operator is deciding with the fire drill's actual outcome in front of them.

State plainly that this is the only leg in the phase that opens all three doors at once.
  </action>
  <decision>Merge the core release pull request now, accepting simultaneous, non-withdrawable publication to Hex, the iOS mirror, and Maven — or stop here.</decision>
  <context>
Three one-way doors open together on this merge, and they are not equally bad to be wrong about. Hex has
a real forward recovery path. The iOS mirror tag can be re-pointed, but that does not reach anyone who
already resolved it. Maven has no recovery at all once the coordinate is published: the only move is to
publish a superseding version and document the defective one.

Every rehearsal that could be run has been run, against this exact tree, and the incident-response
runbook is committed and is an ancestor of what this publishes. There is no further evidence available
short of publishing.
  </context>
  <options>
    <option id="publish-core">
      <name>Merge the core release and let the three publish jobs run</name>
      <pros>The milestone's thesis is exercised end to end: the generalized version gate publishes a version other than the one it used to be welded to, and the post-publication proof lane gets its first real opportunity to run. All three rehearsals are minutes-to-hours old and describe this exact tree.</pros>
      <cons>Three registries mutate at once. The Maven coordinate becomes permanent. The mirror tag becomes resolvable and cannot be un-resolved for anyone who fetches it. The Hex version becomes permanently listed.</cons>
    </option>
    <option id="stop-before-leg-2">
      <name>Stop here</name>
      <pros>Two of three doors stay shut. The already-published companion is unaffected and needs no action.</pros>
      <cons>The milestone's central requirement is unmet, the proof lane still has never executed at a real release, and the second companion leg has nothing to follow.</cons>
    </option>
  </options>
  <resume-signal>Reply `publish core <ios-rehearsal-run-id>` with the iOS candidate-rehearsal run identifier read from `175-REHEARSAL-EVIDENCE.md`, or `stop` to halt the phase here. Gate 1's identifier is not accepted.</resume-signal>
  <reversibility rating="one-way">Publishes to three registries at once: a Maven coordinate is permanent once published, a Hex version cannot be removed, and a re-pointed mirror tag does not un-resolve consumers who already fetched the old commit.</reversibility>
  <done>The maintainer has explicitly authorized or declined the three-registry publish, with the head comparison, all three rehearsals' named fields, the runbook's ancestor check, the three quoted irreversibility statements, and leg 1's outcome in front of them.</done>
</task>

<task type="auto">
  <name>Task 2: Re-confirm the head, merge the core release, and watch the publish graph</name>
  <precondition>Gate 2 was answered with the iOS candidate-rehearsal run identifier from `175-REHEARSAL-EVIDENCE.md`; a `stop` answer halts this plan.</precondition>
  <files>.planning/workstreams/quality-ratchet-release/phases/175-rehearsal-and-publish/175-PUBLISH-LEG-2.md</files>
  <read_first>
    - `.github/workflows/release-please.yml` in full for the ordinary release path: the release job, the approved-release-guard job and its exact head, tree, base and approved-version identity chain, the three core publish jobs, and the post-publication proof job's dispatch condition.
    - `docs/COMPANION-PUBLISH-RUNBOOK.md`'s `### 7. Approve one exact-head merge` and `## Five states and one correction` — the operator procedure this merge follows and the state vocabulary to record against.
    - `175-REHEARSAL-EVIDENCE.md` — the head SHA and the computed split SHA to compare against.
    - `docs/RELEASE-INCIDENT-RESPONSE.md`'s `## Mid-sequence partial failure` in full — read before merging, all three registry groups, so a partial failure is answered from the table.
  </read_first>
  <action>
**Re-confirm the remote graph.** Query `headRefOid` and `baseRefOid` immediately before merging and
compare the head against `175-REHEARSAL-EVIDENCE.md`. Fetch `origin/main` and
`pull/<core-pr>/head`, require the fetched OIDs to equal the live values, and run
`script/check_release_runbook_ancestry.sh` against each. A mismatch or failed ancestry check means
returning to `175-06`, landing the runbook remotely if needed, redoing all three rehearsals, and
re-running gate 2 — not merging anyway (D-33).

**Merge through the normal protected-branch path.** An admin-bypass merge is refused in this repository
and must not be attempted. Record GitHub's exact `mergeCommit.oid`, fetch `origin/main` so the object is
available, and run `script/check_release_runbook_ancestry.sh` against that full merge OID. Record the
result. The post-merge audit does not excuse a failed pre-merge remote-base/head check.

**Watch the publish graph, job by job.** Record the run identifier and, for each of the identity gate
job, the Hex publish job, the iOS core publish job and the Android core publish job, its name and its
conclusion. Do not summarize the set as "the release succeeded" — a per-job record is what makes a
partial failure visible.

Record which path the release took: the ordinary publish path, or an exact-ref recovery path. This
matters beyond bookkeeping — the post-publication proof job depends on the ordinary publish jobs, so a
release completing through recovery does not run it at all, and that would leave the next plan's
requirement unsatisfiable through this run.

**Compare the mirror tag to the rehearsal.** Once the iOS publish job completes, resolve the SHA the
pushed tag points at and compare it to the split SHA the iOS rehearsal computed. Record both and an
explicit equal / not-equal verdict.

**If any job fails.** Stop. Find the matching row in the response table for the registry that failed,
record which row matched and what it directed, and follow it. Never retry a leg that already succeeded —
that is a duplicate-publish attempt with its own row. Never roll back a leg that succeeded to make the
set look uniform.

Begin `175-PUBLISH-LEG-2.md` with: the head comparison verdict, a line exactly matching
`Merge commit SHA: <40-hex>`, the runbook
ancestor-check result, the per-job conclusions, the path taken, the tag-versus-split-SHA verdict, and
gate 2's answer verbatim with its timestamp.

Stage `.planning/` paths explicitly by filename.
  </action>
  <acceptance_criteria>
    - `175-PUBLISH-LEG-2.md` records the pre-merge head SHA and the rehearsal head SHA with an explicit equal / not-equal verdict, and the verdict is equal.
    - The file records a 40-character merge commit SHA that `git cat-file -e` resolves.
    - `bash script/check_release_runbook_ancestry.sh <runbook-sha> <merge-sha>` exits 0 for the exact GitHub merge OID, and the file records that result.
    - The file records a named conclusion for each of the four watched jobs — no job is omitted and none is summarized away.
    - The file records whether the release took the ordinary publish path or a recovery path, with the evidence that decided it.
    - The file records the pushed mirror tag's SHA beside the rehearsal's computed split SHA with an explicit verdict, and the verdict is equal.
    - The file records gate 2's answer verbatim, and the identifier typed equals the iOS leg's run identifier in `175-REHEARSAL-EVIDENCE.md`.
  </acceptance_criteria>
  <verify>
    <automated>RUNBOOK=$(grep -oE '\b[0-9a-f]{40}\b' .planning/workstreams/quality-ratchet-release/phases/175-rehearsal-and-publish/175-INCIDENT-DOC-COMMIT.md | head -1); MERGE=$(sed -n 's/^Merge commit SHA:[[:space:]]*\([0-9a-f]\{40\}\)$/\1/p' .planning/workstreams/quality-ratchet-release/phases/175-rehearsal-and-publish/175-PUBLISH-LEG-2.md); bash script/check_release_runbook_ancestry.sh "$RUNBOOK" "$MERGE"</automated>
    <fails_when>non-zero exit, or either exact SHA is empty — the one-way-door bar is unmet for the actual merge object</fails_when>
    <automated>test -s .planning/workstreams/quality-ratchet-release/phases/175-rehearsal-and-publish/175-PUBLISH-LEG-2.md</automated>
    <fails_when>non-zero exit — the leg record is missing or empty</fails_when>
    <automated>git ls-remote --tags https://github.com/szTheory/crosswake-shell-core-ios.git > /tmp/ios-tags-after.txt 2>&1; echo "ls-remote-exit=$?"; wc -l < /tmp/ios-tags-after.txt</automated>
    <fails_when>the printed `ls-remote-exit` is non-zero, or the line count is 0 — the mirror remote was unreachable, so any tag claim would be vacuous</fails_when>
  </verify>
  <reversibility rating="one-way">This merge publishes to three registries simultaneously; the Maven coordinate is permanent, the Hex version cannot be removed, and the mirror tag is resolvable by consumers the instant it lands.</reversibility>
  <done>The core release is merged from the exact rehearsed head, every publish job has a recorded conclusion, the path taken is recorded, and the pushed mirror tag matches the rehearsed split SHA.</done>
</task>

<task type="auto">
  <name>Task 3: Confirm 0.2.2 live on all three registries by independent query</name>
  <precondition>`175-PUBLISH-LEG-2.md` records conclusions for all four watched publish-graph jobs.</precondition>
  <files>.planning/workstreams/quality-ratchet-release/phases/175-rehearsal-and-publish/175-PUBLISH-LEG-2.md</files>
  <read_first>
    - `.planning/workstreams/quality-ratchet-release/phases/175-rehearsal-and-publish/175-RESEARCH.md`, the "Independent registry verification" code block — the exact Hex, mirror and Maven queries to use.
    - `175-PUBLISH-LEG-2.md` as written by Task 2, especially the recorded split SHA comparison.
    - `docs/RELEASE-INCIDENT-RESPONSE.md`'s `## Irreversibility summary` — so a not-live verdict on any registry routes straight to the right row rather than to improvisation.
  </read_first>
  <action>
Confirm each registry from the registry itself, and capture the raw responses to disk (D-30).

**Hex.** Query the package API and record the returned release list and the latest stable version, with a
timestamp. Write the raw body to `evidence/175-leg2-hex-registry.json`, redirecting into the file
directly rather than through a pipeline. An unparseable body or an empty release list is a failure, not
an empty success.

**iOS mirror.** Run `git ls-remote --tags` against the mirror repository and record the target tag and the
SHA it resolves to. Write the raw output to `evidence/175-leg2-ios-tags.txt` the same way. Assert two
things, not one: the tag exists, **and** it resolves to the split SHA recorded in Task 2. A tag that
exists at the wrong SHA is a failure.

**Maven.** Request the target POM URL from the public repository and record the HTTP status with a
timestamp. Write the status and headers to `evidence/175-leg2-maven-pom.txt`. A success status is the
confirmation; anything else is a not-live verdict routing to the Maven row of the response table.

Then close `175-PUBLISH-LEG-2.md` with a three-row registry table — one row per registry, each carrying
the query performed, the observed result, the timestamp, and an explicit live / not-live verdict. There
is no third verdict and no blank. Add one line stating whether the release took the ordinary path, since
the next plan's requirement depends on it.

If any registry reads not-live, do not proceed to the next plan. Record the matched response-table row
and stop.

Stage `.planning/` paths explicitly by filename.
  </action>
  <acceptance_criteria>
    - `evidence/175-leg2-hex-registry.json` exists, parses as JSON, and its release list contains the published version.
    - `evidence/175-leg2-ios-tags.txt` exists, is non-empty, contains the target tag, and the SHA beside that tag equals the split SHA recorded in Task 2.
    - `evidence/175-leg2-maven-pom.txt` exists and records a success HTTP status with a timestamp.
    - `175-PUBLISH-LEG-2.md`'s closing table has exactly three rows and zero blank verdicts, and all three read live.
    - The file states whether the release took the ordinary publish path.
    - Every recorded fact names the query it came from; the SUMMARY states that none came from reading a workflow definition.
  </acceptance_criteria>
  <verify>
    <automated>test -s .planning/workstreams/quality-ratchet-release/phases/175-rehearsal-and-publish/evidence/175-leg2-hex-registry.json</automated>
    <fails_when>non-zero exit — the Hex response was not captured</fails_when>
    <automated>python3 -c "import json,sys; d=json.load(open('.planning/workstreams/quality-ratchet-release/phases/175-rehearsal-and-publish/evidence/175-leg2-hex-registry.json')); rs=[r.get('version') for r in (d.get('releases') or [])]; print('releases=%d' % len(rs)); sys.exit(0 if rs else 1)"</automated>
    <fails_when>non-zero exit — the body does not parse or the release list is empty</fails_when>
    <automated>test -s .planning/workstreams/quality-ratchet-release/phases/175-rehearsal-and-publish/evidence/175-leg2-ios-tags.txt</automated>
    <fails_when>non-zero exit — the mirror tag listing was not captured</fails_when>
    <automated>test -s .planning/workstreams/quality-ratchet-release/phases/175-rehearsal-and-publish/evidence/175-leg2-maven-pom.txt</automated>
    <fails_when>non-zero exit — the Maven status was not captured</fails_when>
  </verify>
  <reversibility rating="reversible">Read-only registry queries and a record document.</reversibility>
  <done>All three registries independently confirm the core release is live, the mirror tag is confirmed at the rehearsed split SHA, and the raw responses are on disk.</done>
</task>

</tasks>

<artifacts_produced>
## Artifacts this phase produces (plan 175-08)

| Kind | Name |
|---|---|
| New file | `.planning/.../175-rehearsal-and-publish/175-PUBLISH-LEG-2.md` |
| New file | `.planning/.../175-rehearsal-and-publish/evidence/175-leg2-hex-registry.json` |
| New file | `.planning/.../175-rehearsal-and-publish/evidence/175-leg2-ios-tags.txt` |
| New file | `.planning/.../175-rehearsal-and-publish/evidence/175-leg2-maven-pom.txt` |
| Document section | the per-job publish-graph conclusion record |
| Document section | the pushed-tag versus rehearsed-split-SHA verdict |
| Document section | the three-row registry live / not-live table |
| Recorded value | the core release merge commit SHA |
| Checkpoint | gate 2 of 3, resume signal `publish core <ios-rehearsal-run-id>` |
</artifacts_produced>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| A human approval -> three simultaneous irreversible registry mutations | One answer opens three doors with three different recovery costs |
| The approved-release-guard identity chain -> what is actually published | The exact head, tree, base and approved-version binding is what keeps a mutated candidate from publishing |
| The rehearsed split SHA -> the SHA the mirror tag actually lands on | A tag at a different SHA means the published tree is not the rehearsed tree |
| A publish job's conclusion -> the claim the artifact is live | A green job is not a registry response |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-175-41 | Tampering | Publishing an unapproved or mutated candidate | high | mitigate | The approved-release-guard exact head, tree, base and approved-version chain is unchanged by this phase; Task 2 records the identity gate job's conclusion by name rather than folding it into a set verdict. |
| T-175-42 | Spoofing | Approval fatigue satisfying gate 2 from gate 1's answer | high | mitigate | The resume signal is the iOS rehearsal run identifier, deliberately a different named value from gate 1's, read from this phase's own evidence; gate 1's identifier is explicitly not accepted (D-08). |
| T-175-43 | Repudiation | A uniform risk posture hiding that this leg is three times the exposure | high | mitigate | Gate 2 carries the full evidence block and three separately quoted irreversibility statements, one per registry, rather than one generic warning (D-06, D-09, D-10). |
| T-175-44 | Spoofing | "It published" claimed from job conclusions | high | mitigate | Task 3 queries all three registries directly, captures raw responses to disk, and fails on an unparseable body or an empty release list. |
| T-175-45 | Tampering | A mirror tag landing on a SHA other than the rehearsed split | high | mitigate | Task 2 compares the pushed tag's SHA to the rehearsal's computed split SHA with an explicit verdict, and Task 3 re-asserts it from a live remote listing. |
| T-175-46 | Repudiation | A partial failure across three registries answered by improvisation | high | mitigate | Task 2 requires reading all three registry groups of the response table before merging and, on failure, recording the matched row; retrying a succeeded leg and rolling back a succeeded leg are both named as forbidden. |
| T-175-47 | Repudiation | The runbook bar passing against a divergent local checkout instead of what publishes | high | mitigate | Gate 2 fetches and verifies the exact remote base/head OIDs, the helper rejects symbolic `HEAD`, and Task 2 audits the exact GitHub merge OID; a failed pre-merge check is a stop. |
| T-175-48 | Information Disclosure | Publish credentials appearing in captured evidence | medium | mitigate | The captured artifacts are registry responses and remote listings only; no job log body or credential is written into the evidence files. |
| T-175-SC | Tampering | npm/pip/cargo installs | high | mitigate | Not applicable to this plan — it adds no package-manager install step. The published artifacts are first-party packages this repository already builds. |
</threat_model>

<verification>
- Gate 2 was answered with the iOS candidate-rehearsal run identifier, recorded verbatim and matching `175-REHEARSAL-EVIDENCE.md`.
- The pre-merge head SHA equals the rehearsed head SHA, with an explicit recorded verdict.
- `script/check_release_runbook_ancestry.sh` exits 0 for freshly fetched exact remote base and head OIDs before merge and for the exact GitHub merge OID afterward.
- All four watched publish-graph jobs carry a named recorded conclusion, and the path taken is recorded.
- The pushed mirror tag resolves to the rehearsal's computed split SHA.
- Hex, mirror and Maven each confirm the release live through a captured, parseable raw response.
- The closing registry table has three rows, zero blank verdicts, and all three read live.
</verification>

<success_criteria>
`crosswake 0.2.2` is live on all three registries, published from the exact tree that was rehearsed,
through a version gate that generalized without loosening its authority — and every one of those claims
is backed by a response from the registry itself rather than by the workflow that produced it.
</success_criteria>

<output>
Create `.planning/workstreams/quality-ratchet-release/phases/175-rehearsal-and-publish/175-08-SUMMARY.md` when done.
</output>
