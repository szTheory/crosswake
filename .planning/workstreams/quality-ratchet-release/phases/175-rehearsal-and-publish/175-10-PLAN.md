---
phase: 175-rehearsal-and-publish
plan: 10
type: execute
wave: 10
depends_on: ["175-09"]
files_modified:
  - .planning/workstreams/quality-ratchet-release/phases/175-rehearsal-and-publish/175-PUBLISH-LEG-3.md
  - .planning/workstreams/quality-ratchet-release/REQUIREMENTS.md
autonomous: false
requirements: [REL-15]

estimate:
  tokens: 70000
  raw_tokens: 35000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "The second held companion pull request is merged and its package is live on Hex, confirmed by a direct query against the registry's own API showing the new version (D-30)."
    - "Success Criterion 7 is discharged as TWO independent facts stated together, not as one gate the companion publish triggers: the rollup's COMPLETE was established once from the core release's own run, and the second companion's Hex listing is its own separate confirmation (D-31)."
    - "No companion state was threaded into the linked-release rollup; its source file and its job are unmodified, because it structurally has no companion inputs (D-31)."
    - "A human authorized this publish at a checkpoint immediately before it, by typing back the live post-publication proof run identifier — a third named value, distinct from the two earlier gates' (D-07, D-08)."
    - "This gate is light, matching the first companion gate rather than the core gate, because it guards a single Hex publish with a real forward recovery path (D-09)."
    - "The pull request head SHA was re-confirmed immediately before merging and matches the head the Hex rehearsal ran against (D-33)."
    - "Every phase requirement carries an explicit satisfied or not-satisfied disposition pointing at the artifact or run that decides it; a requirement left open is visible rather than inferred."
  prohibitions:
    - "Never satisfy a one-way-door checkpoint by auto-approval, a shared yes/no, or a value reproduced from a different gate; each gate's typed-back value must be read from that leg's own evidence."
    - "Never treat the linked-release rollup's COMPLETE as gated on a companion publish; it has no companion inputs, and adding some would silently widen what COMPLETE means."
    - "Never record a publish or proof leg as verified using evidence produced for a different version or a different candidate ref; the evidence must come from the run that attests the exact version and ref being claimed."
    - "Never mark a requirement satisfied without naming the artifact or run that decides it, and never reword a requirement to make it tickable."
    - "Never write a verification gate whose exit status is taken from the tail of a pipeline; a pipeline reports the last command's status and the gate would be green while asserting nothing."
    - "Never run `git add -A` or `git add .` under `.planning/`; stage `.planning/` paths explicitly by filename, and never stage `config.json`, `milestone.lock`, or `state.json` under the workstream directory."
  artifacts:
    - .planning/workstreams/quality-ratchet-release/phases/175-rehearsal-and-publish/175-PUBLISH-LEG-3.md
  key_links:
    - "`175-PROOF-EVIDENCE.md`'s proof run identifier -> gate 3's resume signal, the third distinct named value (D-08)."
    - "`175-PROOF-EVIDENCE.md`'s rollup COMPLETE -> SC7's first fact; the companion's Hex listing -> SC7's second, independent fact (D-31)."
    - "the nine per-requirement dispositions -> REQUIREMENTS.md's checkbox state, row for row."
---

<planner_notes>
**Edge-probe accounting.** The spec-less probe surfaced 1 row for REL-15, `unclassified` and therefore
**unresolved**. Not auto-resolved with a backstop; surfaced below as a flagged planner assumption.
**1 surfaced == 0 authored as resolved truths + 1 flagged.**

**Phase-wide probe reconciliation.** Across all ten plans the spec-less probe surfaced **11 items**:
3 classified rows for REL-11 and 8 unclassified rows for REL-10, REL-12, REL-13, REL-14, REL-15, REL-16,
DOC-04 and DOC-06. **11 surfaced == 3 authored into `must_haves.truths` (2 explicit + 1 backstop, in
`175-06`) + 8 surfaced as flagged assumptions (2 in `175-04`, 2 in `175-05`, and 1 each in `175-07`,
`175-08`, `175-09` and this plan) == 11.** No silent drops.

**Gate weight.** Light, matching gate 1 (D-09). One registry, one package, a real forward recovery path.
</planner_notes>

<assumption_delta_decision>
The assumption-delta detector fired on this phase with a `pluralization` signal — the phrase "second held
companion" in the phase scope.

- **The noun that is now primary:** unchanged. The release model's primary identity is the **linked core
  release** — the core package plus its two native coordinates — and companions have been independently
  versioned plural members of the family since before this phase. The rollup's six children are all
  linked-release children by construction.
- **Decision:** `no-change`. This is not a generalization event. "Second companion" here names an
  *ordering position within one phase's publish sequence*, not a second instance of something previously
  singular. Nothing in the identity model is being asked to accommodate a new variant.
- **Consequence:** the correct move is explicitly *not* to add companion state alongside the existing
  rollup children. D-31 already records this, and `175-09` asserts the rollup's source file is
  unmodified. Adding alongside here would be the failure mode, not the fix.
- **Suggested companion invariant test (not required, not added by this plan):** an assertion that the
  rollup's child set is exactly the six linked-release children and contains no companion-derived state,
  so a future phase that quietly threads a companion in goes red. Recorded as a suggestion; wiring it is
  outside this phase's requirements.
</assumption_delta_decision>

<flagged_assumptions>
REL-15's edge-coverage row came back `unclassified` and remains **unresolved**. Recorded rather than
dropped: the planner's assumption is that "the second held companion PR is merged and published, and the
linked-release rollup reports COMPLETE" is discharged by two separately established facts — the
companion's own Hex listing, and the rollup verdict already recorded in `175-09` from the core release's
run — and specifically that re-reading the rollup after the companion merges proves nothing additional,
because the companion is not one of its inputs. If the rollup is re-read here and has *changed*, that is
a finding worth investigating, not a reassurance.
</flagged_assumptions>

<scope_boundary>
This plan publishes one companion package to one registry and then closes the phase's requirement
accounting. It does not modify the rollup, does not re-publish anything, and does not edit release
documentation — that landed in `175-05`.

If the companion publish fails, stop and work the Hex rows of `docs/RELEASE-INCIDENT-RESPONSE.md`. The
core release is already live and is not rolled back to make the set look uniform.
</scope_boundary>

<objective>
Publish the second held companion behind the phase's third gate, confirm it live on Hex, and give every
phase requirement an explicit disposition.

Purpose: the phase's last one-way door, and the point at which the milestone's central claim becomes
checkable end to end — six packages, three registries, a proof lane that actually ran, and a record that
says which requirement each of those facts closes.

Output: a merged companion, a live registry confirmation, SC7's two facts stated separately, and a
nine-row requirement disposition table.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/quality-ratchet-release/ROADMAP.md
@.planning/workstreams/quality-ratchet-release/REQUIREMENTS.md
@.planning/workstreams/quality-ratchet-release/phases/175-rehearsal-and-publish/175-CONTEXT.md
@.planning/workstreams/quality-ratchet-release/phases/175-rehearsal-and-publish/175-PROOF-EVIDENCE.md
@docs/RELEASE-INCIDENT-RESPONSE.md
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking-human">
  <name>Task 1: Gate 3 of 3 — authorize the second companion Hex publish</name>
  <read_first>
    - `175-PROOF-EVIDENCE.md` — the live proof run identifier, which is this gate's resume signal, and the rollup verdict this gate reports.
    - `175-PUBLISH-LEG-2.md` — the three-registry confirmation with its timestamps, which this gate displays.
    - `175-PUBLISH-LEG-1.md` — what the first companion leg showed.
    - `docs/RELEASE-INCIDENT-RESPONSE.md`'s Hex rows and Hex retire subsection — quote the recovery statement from there (D-10).
    - `.planning/workstreams/quality-ratchet-release/phases/175-rehearsal-and-publish/175-CONTEXT.md`, decisions D-07 through D-11 and D-31.
  </read_first>
  <action>
Re-fetch this leg's live evidence immediately before presenting. Run
`gh pr view <second-companion-pr> --json number,headRefOid,mergeable,mergeStateStatus,title` and query
the package's current state on the Hex API. Do not merge anything in this task and do not merge under any
answer.

Present, in this order:

1. The second companion pull request number, its title, and its current head SHA beside the head the Hex
   rehearsal ran against, with an explicit equal / not-equal statement.
2. The package name and the version about to become permanently listed, and the versions currently live
   for it.
3. **The confirmed-live status of the core release across all three registries, with timestamps** — the
   Hex version list, the mirror tag and the SHA it resolves to, and the Maven POM status. This is what
   makes gate 3 a different document from gate 1: the operator is being asked to finish a sequence whose
   middle leg is now observable, not to repeat an earlier decision.
4. The post-publication proof run identifier, its status and its conclusion, and the rollup verdict.
5. The one-line Hex recovery statement, quoted from `docs/RELEASE-INCIDENT-RESPONSE.md`: retirement is a
   forward-only advisory flag, the version stays resolvable, and existing lockfiles continue to work.
6. One line on scope: this leg touches Hex only, and merging it does not change the rollup's inputs — the
   rollup was verified once from the core release's own run and is not re-gated here (D-31).

Keep it to that. This is a light gate.
  </action>
  <decision>Merge the second held companion pull request now, accepting that its Hex publish cannot be withdrawn — or stop here with the core release live and one companion unpublished.</decision>
  <context>
A Hex publish is a one-way door. Retirement sets an advisory flag; it does not remove the package or the
version, and existing lockfiles continue to resolve it. Recovery is retire-forward only.

Unlike gate 2, this leg touches one registry. Unlike gate 1, the core release is now live on three
registries and the post-publication proof has actually executed and passed — so this decision is being
made with the whole sequence's outcome visible rather than predicted.

Stopping here is a coherent end state, not a broken one: the core release and the first companion stay
live and correct, and the second companion simply remains at its previous version.
  </context>
  <options>
    <option id="publish-companion-2">
      <name>Merge the second companion and let its publish job run</name>
      <pros>The family is consistent at one release, and the milestone's requirement accounting closes with every planned coordinate live. The preceding legs have already exercised the exact same job shape successfully.</pros>
      <cons>One more permanently listed Hex version. If a defect is found afterward, the only recovery is retire-forward.</cons>
    </option>
    <option id="stop-before-leg-3">
      <name>Stop here</name>
      <pros>Nothing further is published. The core release and the first companion are unaffected and need no action.</pros>
      <cons>The phase's last requirement is unmet and the companion family is split across two releases.</cons>
    </option>
  </options>
  <resume-signal>Reply `publish leg 3 <exact-public-proof-run-id>` with the live post-publication proof run identifier read from `175-PROOF-EVIDENCE.md`, or `stop` to halt the phase here. Neither earlier gate's identifier is accepted.</resume-signal>
  <reversibility rating="one-way">A Hex publish cannot be withdrawn; retirement flags the version but does not remove it, and existing lockfiles keep resolving it.</reversibility>
  <done>The maintainer has explicitly authorized or declined the final publish, with the head comparison, the three-registry confirmed-live status with timestamps, the proof run's outcome, and the quoted Hex recovery statement in front of them.</done>
</task>

<task type="auto">
  <name>Task 2: Merge the second companion and confirm it live on Hex</name>
  <precondition>Gate 3 was answered with the post-publication proof run identifier from `175-PROOF-EVIDENCE.md`; a `stop` answer halts this plan.</precondition>
  <files>.planning/workstreams/quality-ratchet-release/phases/175-rehearsal-and-publish/175-PUBLISH-LEG-3.md</files>
  <read_first>
    - `.github/workflows/release-please.yml`'s second companion Hex publish job and its dependency graph — confirm for yourself it depends on the release job only, so no approval receipt is produced or awaited (Pitfall 2).
    - `175-REHEARSAL-EVIDENCE.md` — the head SHA the Hex rehearsal ran against.
    - `175-PUBLISH-LEG-1.md` — the first companion leg's record, whose shape this one mirrors.
    - `.planning/workstreams/quality-ratchet-release/phases/175-rehearsal-and-publish/175-RESEARCH.md`, the "Independent registry verification" code block.
    - `docs/RELEASE-INCIDENT-RESPONSE.md`'s Hex failure-mode rows — read before merging.
  </read_first>
  <action>
**Re-confirm the head.** `gh pr view <second-companion-pr> --json headRefOid` immediately before merging,
compared against the head recorded in `175-REHEARSAL-EVIDENCE.md`. A mismatch means redoing the Hex
rehearsal and re-running gate 3, not merging anyway (D-33).

**Merge** through the normal protected-branch path; admin-bypass is refused here. Record the merge commit
SHA.

**Watch the publish job** and record its run identifier, URL and conclusion. Do not wait for an approval
receipt; none is produced for a companion merge.

**Confirm live from the registry.** Query the Hex API for the package and write the raw body to
`.planning/workstreams/quality-ratchet-release/phases/175-rehearsal-and-publish/evidence/175-leg3-hex-registry.json`,
redirecting into the file directly rather than through a pipeline. Record the release list, the latest
stable version and the query timestamp. An unparseable body or an empty release list is a failure, not an
empty success.

**Discharge Success Criterion 7 as two separate facts.** Write them as two rows, each with its own
evidence:

- Fact one — the linked-release rollup reports COMPLETE. Cite `175-PROOF-EVIDENCE.md`'s rollup section
  and the core release run it came from. Do **not** re-derive it from this companion's run; the companion
  is not one of the rollup's inputs.
- Fact two — the second companion is live on Hex. Cite this task's own registry query.

Add one line stating why these are independent, per D-31, and record the rollup's verdict as re-read now
only as a change-detection observation: if it differs from what `175-09` recorded, that is a finding to
investigate, not a reassurance.

**If the publish job fails.** Stop, find the matching Hex failure-mode row, record which row matched and
what it directed, and follow it. Never retry a leg that already succeeded. Never roll back the core
release to make the family look uniform.

Write `175-PUBLISH-LEG-3.md` with the head comparison verdict, the merge commit SHA, the publish job's
conclusion, the registry confirmation with its timestamp and a live / not-live verdict, gate 3's answer
verbatim, and the two SC7 facts.

**Label the merge commit SHA on its own line, exactly.** This document records several resolvable 40-hex
commits — the pre-merge head, the rehearsal head, and whatever the SC7 citations carry — so a
verification that took the last bare hex token in the file could resolve a different commit and report
green while proving nothing about the merge. Write a line of its own, starting at column one, beginning
`Merge commit SHA:` and followed by the merge commit's 40-hex SHA. That label is load-bearing: the
verification below extracts by label, not by position, and an absent or mislabeled line fails the task
closed. The label appears exactly once.

Stage `.planning/` paths explicitly by filename.
  </action>
  <acceptance_criteria>
    - `175-PUBLISH-LEG-3.md` records the pre-merge head SHA and the rehearsal head SHA with an explicit equal / not-equal verdict, and the verdict is equal.
    - The file carries exactly one line beginning `Merge commit SHA:` whose 40-character SHA `git cat-file -e` resolves; the merge SHA is extracted by that label rather than by position.
    - The file records a publish job run identifier whose conclusion is `success`.
    - `evidence/175-leg3-hex-registry.json` exists, parses as JSON, and its release list contains the new version.
    - The file records the registry query timestamp and a live / not-live verdict, and the verdict is live.
    - The file records SC7 as two separately evidenced rows, the first citing `175-PROOF-EVIDENCE.md` rather than this companion's run.
    - The file records gate 3's answer verbatim, and the identifier typed equals the proof run identifier in `175-PROOF-EVIDENCE.md`.
    - `git status --porcelain lib/crosswake/release_candidate/workflow.ex` is empty.
  </acceptance_criteria>
  <verify>
    <automated>test -s .planning/workstreams/quality-ratchet-release/phases/175-rehearsal-and-publish/evidence/175-leg3-hex-registry.json</automated>
    <fails_when>non-zero exit — the registry response was not captured</fails_when>
    <automated>python3 -c "import json,sys; d=json.load(open('.planning/workstreams/quality-ratchet-release/phases/175-rehearsal-and-publish/evidence/175-leg3-hex-registry.json')); rs=d.get('releases') or []; print('releases=%d latest=%s' % (len(rs), d.get('latest_stable_version'))); sys.exit(0 if rs else 1)"</automated>
    <fails_when>non-zero exit — the body does not parse or the release list is empty</fails_when>
    <automated>git status --porcelain lib/crosswake/release_candidate/workflow.ex</automated>
    <fails_when>the output is non-empty — the rollup's definition was modified</fails_when>
    <automated>LINE=$(grep -m1 '^Merge commit SHA:' .planning/workstreams/quality-ratchet-release/phases/175-rehearsal-and-publish/175-PUBLISH-LEG-3.md); LABEL_EXIT=$?; SHA=$(printf '%s' "$LINE" | grep -oE '[0-9a-f]{40}'); git cat-file -e "$SHA"; echo "merge-sha-exit=$? label-exit=$LABEL_EXIT sha=$SHA"</automated>
    <fails_when>the printed `merge-sha-exit` is anything other than 0, or `label-exit` is anything other than 0, or `sha` is empty — the labeled merge-commit line is missing, so the check would otherwise have re-asserted some other 40-hex token in the file</fails_when>
  </verify>
  <reversibility rating="one-way">Merging this pull request triggers a Hex publish that cannot be withdrawn; the version stays permanently listed and resolvable even after retirement.</reversibility>
  <done>The second companion is merged from the rehearsed head, its publish job has completed, the registry confirms it live, and Success Criterion 7 is recorded as two independently evidenced facts.</done>
</task>

<task type="auto">
  <name>Task 3: Give all nine phase requirements an explicit disposition</name>
  <precondition>`175-PUBLISH-LEG-3.md` records the second companion live on Hex, or records an explicit stop with its reason.</precondition>
  <files>.planning/workstreams/quality-ratchet-release/phases/175-rehearsal-and-publish/175-PUBLISH-LEG-3.md, .planning/workstreams/quality-ratchet-release/REQUIREMENTS.md</files>
  <read_first>
    - `.planning/workstreams/quality-ratchet-release/REQUIREMENTS.md` — the exact wording of REL-10 through REL-16, DOC-04 and DOC-06, and the traceability table's row format at the bottom of the file.
    - `.planning/workstreams/quality-ratchet-release/ROADMAP.md`'s Phase 175 section — the eight Success Criteria, which are the concrete form of these requirements.
    - Every record this phase produced: `175-WAVE0-EXIT.md`, `175-INCIDENT-DOC-COMMIT.md`, `175-DOC-TRUTH.md`, `175-REHEARSAL-EVIDENCE.md`, `175-PUBLISH-LEG-1.md`, `175-PUBLISH-LEG-2.md`, `175-PROOF-EVIDENCE.md`, and this plan's own record.
    - `.planning/workstreams/quality-ratchet-release/phases/174-clean-room-host-realism-adopter-fidelity/174-NON-VACUITY.md` — the house standard for a per-requirement disposition table.
  </read_first>
  <action>
Add a per-requirement disposition table to `175-PUBLISH-LEG-3.md`: one row each for REL-10, REL-11,
REL-12, REL-13, REL-14, REL-15, REL-16, DOC-04 and DOC-06. Each row carries the requirement id, an
explicit **satisfied** or **not satisfied** verdict, and the artifact, run identifier or registry
response that decides it.

There is no third verdict and no blank. A requirement whose evidence is partial — REL-12 if the
clean-room lane was recorded as not exercised — is **not satisfied**, stated as such, with the exact
command or run that would satisfy it. A criterion left unstated must never be readable as satisfied.

Then reconcile the eight ROADMAP Success Criteria against those rows, noting explicitly where a success
criterion is narrower or broader than the requirement it implements — SC7 in particular is two facts and
is verdicted against both, not one.

Add a short closing section recording this phase's deliberate non-actions with reasons:

- Companion state was not threaded into the linked-release rollup (D-31).
- Archived planning history was not rewritten to remove residual references to the previous mirror
  splitter (D-18).
- The pinned action SHAs were not audited for correctness or provenance; that is the SEED filed in
  `175-WAVE0-EXIT.md` (D-29).

Then update `REQUIREMENTS.md`: tick each of the nine checkboxes that is genuinely satisfied and update its
traceability row; leave unticked any that is not, and do not soften any requirement text to make it
tickable. Stage `.planning/` paths explicitly by filename.
  </action>
  <acceptance_criteria>
    - `175-PUBLISH-LEG-3.md` carries a nine-row disposition table; the count of rows is exactly 9 and the count of rows with a blank verdict is 0.
    - Every satisfied row names a specific artifact, run identifier or registry response; no row claims satisfaction from prose alone.
    - Every not-satisfied row names what would satisfy it, as a command, a run, or an artifact.
    - The eight ROADMAP Success Criteria are reconciled against the rows, with SC7 verdicted against both of its facts.
    - The closing section records all three deliberate non-actions with reasons.
    - `REQUIREMENTS.md`'s checkbox state for REL-10 through REL-16, DOC-04 and DOC-06 agrees with the disposition table row for row; state the measured agreement in the SUMMARY.
    - `git diff --unified=0 .planning/workstreams/quality-ratchet-release/REQUIREMENTS.md` changes only checkbox state and traceability rows, never a requirement's descriptive text.
  </acceptance_criteria>
  <verify>
    <automated>grep -cE 'REL-1[0-6]|DOC-0[46]' .planning/workstreams/quality-ratchet-release/phases/175-rehearsal-and-publish/175-PUBLISH-LEG-3.md</automated>
    <fails_when>the printed count is fewer than 9 — a requirement is missing from the disposition table</fails_when>
    <automated>git diff --unified=0 .planning/workstreams/quality-ratchet-release/REQUIREMENTS.md > /tmp/reqs-diff.txt 2>&1; echo "diff-exit=$?"; grep -cE '^[+-][^+-]' /tmp/reqs-diff.txt</automated>
    <fails_when>the diff changes any requirement's descriptive text rather than only its checkbox state and traceability row</fails_when>
    <automated>elixir script/check_release_doc_version_literals.exs</automated>
    <fails_when>non-zero exit — a release document regressed a version literal or lost its invariant marker during the phase</fails_when>
    <automated>node scripts/ci_monitor.cjs check-actions</automated>
    <fails_when>non-zero exit, or the summary line reports `mutable_refs=` anything other than 0</fails_when>
  </verify>
  <reversibility rating="reversible">Documentation and checkbox state only.</reversibility>
  <done>All nine phase requirements carry an explicit verdict backed by a named artifact or run, the eight success criteria are reconciled against them, the deliberate non-actions are recorded, and `REQUIREMENTS.md` agrees row for row.</done>
</task>

</tasks>

<artifacts_produced>
## Artifacts this phase produces (plan 175-10)

| Kind | Name |
|---|---|
| New file | `.planning/.../175-rehearsal-and-publish/175-PUBLISH-LEG-3.md` |
| New file | `.planning/.../175-rehearsal-and-publish/evidence/175-leg3-hex-registry.json` |
| Document section | the two-row Success Criterion 7 record, one fact per row |
| Document section | the nine-row per-requirement disposition table |
| Document section | the ROADMAP success-criteria reconciliation |
| Document section | the deliberate non-actions record |
| Recorded value | the second companion's merge commit SHA and publish job run identifier |
| Checkpoint | gate 3 of 3, resume signal `publish leg 3 <exact-public-proof-run-id>` |
| Updated file | `.planning/workstreams/quality-ratchet-release/REQUIREMENTS.md` (checkbox state and traceability rows only) |
</artifacts_produced>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| A human approval -> a final irreversible registry mutation | The third near-identical prompt is where approval fatigue is most likely |
| A rollup verdict -> the claim that a companion publish succeeded | Two unrelated facts stated in one sentence invite one being read as evidence for the other |
| Self-reported phase records -> the milestone's requirement accounting | These dispositions are what the milestone audit reads instead of re-deriving the facts |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-175-55 | Spoofing | Approval fatigue satisfying gate 3 from an earlier gate's answer | high | mitigate | The resume signal is the live proof run identifier — a third distinct named value — and neither earlier gate's identifier is accepted; the prompt's content is materially different, leading with the three-registry confirmed-live status the earlier gates could not show (D-08, D-09). |
| T-175-56 | Spoofing | The rollup's COMPLETE read as confirmation that the companion published | high | mitigate | Task 2 requires SC7 to be written as two separately evidenced rows, the first citing the core release's own run and never this companion's; a prohibition forbids treating the rollup as companion-gated (D-31). |
| T-175-57 | Tampering | Widening the rollup so a companion merge can influence COMPLETE | high | mitigate | A prohibition forbids it, the assumption-delta decision records `no-change` with its reasoning, and a verification asserts the rollup's source file is unmodified. |
| T-175-58 | Repudiation | A partially-satisfied requirement reading as satisfied at phase close | high | mitigate | Task 3 requires nine explicit verdicts with no third value and no blank, every satisfied row naming its deciding artifact, and every not-satisfied row naming what would satisfy it. |
| T-175-59 | Tampering | Requirement text softened to make a checkbox tickable | high | mitigate | A prohibition forbids rewording and a verification diffs `REQUIREMENTS.md` for descriptive-text changes rather than checkbox-state changes. |
| T-175-60 | Repudiation | Documentation or pin hygiene regressing silently during the publish waves | medium | mitigate | Task 3's verification re-runs both hygiene checks — the version-literal check and the full-scope action audit — at phase close, so a regression during the publish window surfaces before the phase seals. |
| T-175-SC | Tampering | npm/pip/cargo installs | high | mitigate | Not applicable to this plan — it adds no package-manager install step. The published package is a first-party, already-audited package whose next version this phase releases. |
</threat_model>

<verification>
- Gate 3 was answered with the post-publication proof run identifier, recorded verbatim and matching `175-PROOF-EVIDENCE.md`.
- The pre-merge head SHA equals the rehearsed head SHA, with an explicit recorded verdict.
- The second companion's publish job conclusion is `success` and a direct Hex API query returns a parseable body whose release list contains the new version.
- Success Criterion 7 is recorded as two separately evidenced rows.
- `lib/crosswake/release_candidate/workflow.ex` is unmodified.
- The nine-row disposition table has zero blank verdicts and every satisfied row names its deciding artifact.
- `REQUIREMENTS.md`'s checkbox state agrees with the table row for row, with no requirement text reworded.
- `elixir script/check_release_doc_version_literals.exs` and `node scripts/ci_monitor.cjs check-actions` both still pass at phase close.
</verification>

<success_criteria>
The last one-way door is walked through with the whole sequence's outcome visible, the second companion is
confirmed live by the registry itself, the two facts Success Criterion 7 states together are recorded
separately with separate evidence, and every requirement this phase owns carries a verdict an auditor can
act on without opening a diff.
</success_criteria>

<output>
Create `.planning/workstreams/quality-ratchet-release/phases/175-rehearsal-and-publish/175-10-SUMMARY.md` when done.
</output>
