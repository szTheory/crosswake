---
phase: 175-rehearsal-and-publish
plan: 04
type: execute
wave: 4
depends_on: ["175-03"]
files_modified:
  - docs/RELEASE-INCIDENT-RESPONSE.md
  - .planning/workstreams/quality-ratchet-release/phases/175-rehearsal-and-publish/175-INCIDENT-DOC-COMMIT.md
autonomous: true
requirements: [REL-10, REL-16]

estimate:
  tokens: 70000
  raw_tokens: 35000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "A single document, `docs/RELEASE-INCIDENT-RESPONSE.md`, answers 'a publish just went wrong, what do I do right now' for Hex, the iOS mirror, and Maven without a cross-file jump mid-incident (D-13)."
    - "The incident-response content lives in its own file, `docs/RELEASE-INCIDENT-RESPONSE.md`, and is not added to `docs/COMPANION-PUBLISH-RUNBOOK.md`, which stays pre-flight prose for someone preparing a release rather than someone already mid-failure (D-14)."
    - "An irreversibility summary table sits above the fold, so the reader learns which of the three registries can be walked back before reading any procedure (D-15)."
    - "The partial-failure matrix is grouped by registry, each group carrying the same five failure-mode rows, so the asymmetric reversibility is block-separated rather than interleaved (D-16)."
    - "Every matrix row carries a non-omitted, non-hedging Irreversibility cell — including where the honest answer is that the step is reversible (D-16)."
    - "The Hex section states that retirement is a forward-only advisory flag that does not remove the package or version and that existing lockfiles continue to resolve it, and names `--unretire` as its reversal."
    - "The Maven section states that a `PUBLISHED` coordinate is permanent and that only a `VALIDATED` deployment is droppable, so the rehearsal's droppability is never read as a real publish being droppable."
    - "The iOS mirror section states that re-pointing a tag does not un-resolve consumers who already fetched the old commit through SwiftPM's resolved-package cache — a fact currently documented nowhere in this repository (D-20)."
    - "The document states, at the top, that it makes no version-specific claims, and contains no bare version literal outside a code fence (D-19)."
    - "The document's commit SHA is recorded in-repo before any publish step in this phase executes, so the ROADMAP one-way-door bar is checkable rather than asserted (SC1)."
    - "The `## Mid-sequence partial failure` and `## Retire / backfill after a bad publish` heading texts are fixed, because the phase's checkpoint prompts cite the GitHub-generated anchors derived from them (D-15)."
  prohibitions:
    - "Never describe `mix hex.retire`, an iOS mirror re-tag, or a superseding Maven upload as undoing a publish, and never hedge an irreversibility statement with 'may' or 'should consider'; retirement is forward-only advisory and a published Maven coordinate is permanent."
    - "Never record a publish or proof leg as verified using evidence produced for a different version or a different candidate ref; the evidence must come from the run that attests the exact version and ref being claimed."
    - "Never write a verification gate whose exit status is taken from the tail of a pipeline; a pipeline reports the last command's status and the gate would be green while asserting nothing."
    - "Never name the early-adopter SaaS in any artifact; the codename is First B2C Adopter."
    - "Never run `git add -A` or `git add .` under `.planning/`; stage `.planning/` paths explicitly by filename, and never stage `config.json`, `milestone.lock`, or `state.json` under the workstream directory."
  artifacts:
    - docs/RELEASE-INCIDENT-RESPONSE.md
    - .planning/workstreams/quality-ratchet-release/phases/175-rehearsal-and-publish/175-INCIDENT-DOC-COMMIT.md
  key_links:
    - "`docs/RELEASE-INCIDENT-RESPONSE.md`'s stable heading anchors -> the three `checkpoint:decision` prompts in `175-07`, `175-08` and `175-10`, which cite them rather than re-deriving recovery language (D-10)."
    - "the recorded commit SHA -> gate 2's evidence block in `175-08` and SC1's before-the-first-publish bar."
    - "`.github/workflows/release-please.yml`'s own `VALIDATED`/`PUBLISHED` comment -> the Maven Irreversibility cells, so the doc and the workflow cannot drift apart."
---

<planner_notes>
**Edge-probe accounting.** The spec-less probe surfaced 2 rows for this plan's requirements — REL-10
and REL-16, both `unclassified` and therefore **`unresolved`**. Neither is auto-resolved with a
backstop; both are surfaced below as flagged planner assumptions. **2 surfaced == 0 authored as
resolved truths + 2 flagged.**

**Prohibition accounting.** The phase-wide recall is recorded once in `175-01`'s planner notes. This
plan carries the transparency prohibition minted for it (recovery language must not overstate
recoverability or hedge) plus the evidence-provenance prohibition and three standing project
prohibitions.

**Why one document and not two.** REL-10 and REL-16 are different jobs but share one reader at one
moment (D-13). Splitting them would force a cross-file jump mid-incident. They do not go inside
`docs/COMPANION-PUBLISH-RUNBOOK.md` (D-14): that file is pre-flight prose for someone preparing a
release, not someone already mid-failure.
</planner_notes>

<flagged_assumptions>
Both of this plan's edge-coverage rows came back `unclassified` from the spec-less probe and remain
**unresolved**. They are recorded here rather than silently dropped, and neither is treated as covered:

- **REL-10 (unclassified, unresolved).** The probe could not classify the requirement's edge shape.
  The planner's assumption is that "a retire/backfill runbook covering Hex, the iOS mirror, and Maven
  is committed before the first publish" is satisfied by one document covering three registries with a
  recorded commit SHA, and that "before" means the commit SHA is an ancestor of the first publish
  commit — not merely an earlier wall-clock timestamp. Task 3 is where that assumption is discharged
  against a real `git merge-base --is-ancestor` check.
- **REL-16 (unclassified, unresolved).** The planner's assumption is that "a multi-registry
  partial-failure response table exists before the first publish, not derived during an incident" is
  satisfied by the five failure-mode rows in each of three registry groups authored here, and that
  completeness means five rows per group with no blank Irreversibility cell. If a sixth failure mode is
  discovered during the publish waves, adding it then is exactly the "derived during an incident"
  outcome the requirement forbids — record it as a finding instead.
</flagged_assumptions>

<scope_boundary>
This plan creates one new document and one record. It does not edit
`docs/COMPANION-PUBLISH-RUNBOOK.md` — DOC-04 and DOC-06 are `175-05`'s job, deliberately separated so
the new incident document and the surgery on the existing runbook are reviewable apart.

It does not dispatch a rehearsal, does not merge a pull request, and does not touch a registry.

If writing the matrix reveals that a recovery path this repo assumes does not actually exist, record it
as an unmet row with a named reason rather than inventing a plausible command. A runbook that overstates
recoverability is worse than no runbook, because it is read under time pressure.
</scope_boundary>

<objective>
Put the incident-response document on disk, with a recorded commit SHA, before any one-way door in this
phase opens.

Purpose: the ROADMAP's one-way-door note makes this a hard bar — nothing in this phase is merged or run
without the retire/backfill runbook already committed. REL-16 exists for the same reason: a
per-registry partial-failure matrix derived live, mid-incident, under time pressure, is how a bad
publish becomes two bad publishes.

Output: `docs/RELEASE-INCIDENT-RESPONSE.md` with an irreversibility summary above the fold, a
registry-grouped failure matrix, retire/backfill procedures for all three registries, and its commit
SHA recorded for gate 2 and SC1.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/quality-ratchet-release/ROADMAP.md
@.planning/workstreams/quality-ratchet-release/REQUIREMENTS.md
@.planning/workstreams/quality-ratchet-release/phases/175-rehearsal-and-publish/175-CONTEXT.md
@.planning/workstreams/quality-ratchet-release/phases/175-rehearsal-and-publish/175-RESEARCH.md
@.planning/workstreams/quality-ratchet-release/phases/175-rehearsal-and-publish/175-PATTERNS.md
@prompts/crosswake-brand-book.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: The irreversibility summary and the registry-grouped partial-failure matrix (REL-16)</name>
  <files>docs/RELEASE-INCIDENT-RESPONSE.md</files>
  <read_first>
    - `docs/COMPANION-PUBLISH-RUNBOOK.md` in full — for voice and register only. Note its opening shape: a short declarative "this is the operator contract for X" line immediately followed by a read-only/mutation boundary statement. Copy that register. Note also that its opening line binds the document to one version; that is the mistake this document must not repeat.
    - `docs/MILESTONE-BOUNDARY-HYGIENE.md` — the table-first, no-narrative-prose runbook shape: a bold scope-boundary statement up top, then numbered sections each opening with a fenced command block, no narrative paragraphs between commands.
    - `.github/workflows/release-please.yml` around lines 996-1161, especially the `android-publish-fire-drill` job and its comment near lines 1156-1157 about immutability being scoped to one deployment state. Quote that comment's substance in the Maven Irreversibility cells so the document and the workflow cannot drift.
    - `.github/workflows/hex-publish.yml` — the `candidate-rehearsal` and `publish` operations and the `recovery-*` jobs, so the Hex rows name commands that exist.
    - `.github/workflows/ios-mirror-backfill.yml` — the `candidate-rehearsal`, `publish-ios-mirror` and `recover-ios-mirror` jobs, so the iOS rows name jobs that exist.
    - `.planning/workstreams/quality-ratchet-release/phases/175-rehearsal-and-publish/175-RESEARCH.md`, Pitfalls 4 and 5 and the "Independent registry verification" code block.
    - `.planning/workstreams/quality-ratchet-release/phases/175-rehearsal-and-publish/175-CONTEXT.md`, decisions D-13 through D-20.
    - `prompts/crosswake-brand-book.md` — documentation voice; it supersedes older voice references.
  </read_first>
  <action>
Create `docs/RELEASE-INCIDENT-RESPONSE.md`, table-first, with no narrative prose sections.

**Top of file, above everything else.** An `# Release Incident Response` title, then a short
declarative opening in the register of the existing runbook — this is the operator contract for
recovering a release that has already gone wrong — immediately followed by the mutation boundary (which
of the commands below change public state and which do not). Then, on its own line and bolded, the
invariant: **This document makes no version-specific claims.** That sentence is asserted by a CI check
`175-05` adds; write it verbatim so the check's marker assertion finds it.

**Then, above the fold, an `## Irreversibility summary` table.** Three rows, one per registry, with
columns naming the mutation, whether it can be walked back, and what the real recovery is. Short
declarative sentences. Bold the fact that must not be missed. No hedging words — never "may", never
"should consider". Use the plain-English gloss borrowed from yank/deprecation vocabulary for Hex
retirement: not selected by default resolution, but existing lockfiles still work.

<!-- planner-discipline-allow: should consider -->
The hedging phrases named above are quoted here only to forbid them; the acceptance criterion greps
`docs/RELEASE-INCIDENT-RESPONSE.md`, not this plan.

**Then `## Mid-sequence partial failure`** — REL-16's matrix. Keep this heading text exactly as
written; the phase's checkpoint prompts cite the GitHub-generated anchor derived from it.

Organize as three row-groups **by registry** — Hex, then iOS mirror, then Maven — so the asymmetric
reversibility is block-separated rather than interleaved (D-16). Each group carries the same five
failure-mode rows, in the same order:

1. auth or credential failure before any write
2. partial upload or job failure after a partial state change
3. published successfully but found broken after the fact
4. version already taken, or a duplicate-publish attempt
5. the post-publication proof lane failed after the underlying publish succeeded

Columns, in this order: **Detect | Decide | Command | Irreversibility**. Cells cap at one line;
a multi-line command goes in a fenced snippet immediately beneath the row it belongs to, never inside
the cell. The Irreversibility column is never omitted and never blank, even when the honest answer is
that the step is reversible, and it never hedges.

Ground each Detect cell in an independent registry or run-evidence observation — a `curl` against the
registry, a `git ls-remote`, a `gh run view` on a job conclusion — never "re-read the workflow to see
whether it would have run". That is this milestone's named recurring defect.

Do not write a Maven "retire" command; none exists. Do not describe a re-tag as un-publishing.
  </action>
  <acceptance_criteria>
    - `docs/RELEASE-INCIDENT-RESPONSE.md` exists and its first 40 lines contain the exact sentence `This document makes no version-specific claims.`
    - The file contains the headings `## Irreversibility summary` and `## Mid-sequence partial failure`, spelled exactly so.
    - The `## Irreversibility summary` table has exactly three data rows, one each naming Hex, the iOS mirror, and Maven.
    - The `## Mid-sequence partial failure` section contains exactly 15 data rows — three registry groups of five failure modes — and the count of rows with an empty final column is 0.
    - The table header row reads `Detect | Decide | Command | Irreversibility` in that order.
    - `grep -c 'should consider' docs/RELEASE-INCIDENT-RESPONSE.md` prints 0 — no hedging in the irreversibility register.
    - No Maven row names a retire or delete command; the Maven recovery is stated as publishing a superseding version.
    - `awk 'BEGIN{f=0} /^```/{f=!f; next} f==0 && /[0-9]+\.[0-9]+\.[0-9]+/{c++} END{print c+0}' docs/RELEASE-INCIDENT-RESPONSE.md` prints 0 — no bare version literal outside a code fence.
    - Every Detect cell names a registry response, a run conclusion, or a remote ref listing — the SUMMARY states how many of the 15 do so, and the number is 15.
  </acceptance_criteria>
  <verify>
    <automated>grep -c 'This document makes no version-specific claims\.' docs/RELEASE-INCIDENT-RESPONSE.md</automated>
    <fails_when>the printed count is 0</fails_when>
    <automated>awk 'BEGIN{f=0} /^```/{f=!f; next} f==0 && /[0-9]+\.[0-9]+\.[0-9]+/{c++} END{print c+0}' docs/RELEASE-INCIDENT-RESPONSE.md</automated>
    <fails_when>the printed count is anything other than 0</fails_when>
    <automated>grep -cE '^## (Irreversibility summary|Mid-sequence partial failure)$' docs/RELEASE-INCIDENT-RESPONSE.md</automated>
    <fails_when>the printed count is anything other than 2</fails_when>
    <automated>awk '/^## Mid-sequence partial failure$/,/^## Retire/' docs/RELEASE-INCIDENT-RESPONSE.md | grep -E '^\|' | grep -vE '^\|[ -]*\|' | grep -vc 'Detect'</automated>
    <fails_when>the printed count is anything other than 15</fails_when>
  </verify>
  <reversibility rating="reversible">A new documentation file; nothing executable changes and no registry is touched.</reversibility>
  <done>The incident document exists with an irreversibility summary above the fold and a fifteen-row, registry-grouped partial-failure matrix whose Irreversibility column is complete and unhedged.</done>
</task>

<task type="auto">
  <name>Task 2: The retire / backfill procedures for all three registries (REL-10)</name>
  <files>docs/RELEASE-INCIDENT-RESPONSE.md</files>
  <read_first>
    - `docs/RELEASE-INCIDENT-RESPONSE.md` as written by Task 1 — the matrix rows this section's procedures are referenced from.
    - `.planning/workstreams/quality-ratchet-release/phases/175-rehearsal-and-publish/175-RESEARCH.md`, Pitfall 4 (Hex retirement semantics, including the exact reason vocabulary and the reversal flag) and Pitfall 5 (Maven immutability).
    - `docs/COMPANION-PUBLISH-RUNBOOK.md`'s "Ordinary publication and recovery" and "Five states and one correction" sections — the existing principles this section extends rather than replaces: never retry an immutable success, recover the one missing coordinate, never roll back a proven success.
    - `.github/workflows/ios-mirror-backfill.yml`'s `recover-ios-mirror` job and `script/release_candidate/ios_mirror.sh` — the real mechanism behind an iOS mirror correction.
    - `.planning/workstreams/quality-ratchet-release/phases/175-rehearsal-and-publish/175-CONTEXT.md`, decisions D-15, D-16, D-19 and D-20.
  </read_first>
  <action>
Append `## Retire / backfill after a bad publish` to `docs/RELEASE-INCIDENT-RESPONSE.md`. Keep this
heading text exactly as written; the phase's checkpoint prompts cite the GitHub-generated anchor derived
from it.

Three subsections, one per registry, each opening with a fenced command block and carrying one-line
statements beneath it — no narrative paragraphs.

**Hex.** `mix hex.retire PACKAGE VERSION <reason> --message "<text>"` with the exact reason vocabulary
the tool accepts — `renamed`, `deprecated`, `security`, `invalid`, `other` — each requiring a message,
and `mix hex.retire PACKAGE VERSION --unretire` as the reversal. State plainly that retirement is an
advisory flag: the package and version remain resolvable and usable, and existing lockfiles continue to
resolve them. Retirement is not a rollback of the publish and must never be written as one.

**iOS mirror.** The re-tag / correction path through `ios-mirror-backfill.yml`'s recovery operation,
naming the operation and its inputs. Then state D-20's fact explicitly, because it is documented nowhere
else in this repository: re-pointing a mirror tag does not un-resolve consumers who already fetched the
old commit, because SwiftPM's resolved-package cache holds the previously resolved revision. Moving the
tag changes what a *new* resolution gets; it does not reach anyone who already resolved.

**Maven.** State that a coordinate is permanent once it reaches the published state and that the only
recovery is forward: publish a new superseding version and document the defective one. Name the
`VALIDATED`-to-drop path explicitly as the *rehearsal's* affordance and say in the same breath that it
does not apply to a real publish — this distinction is exactly where a reader under pressure will make
the expensive mistake. Quote the substance of `release-please.yml`'s own comment on that boundary so the
two cannot drift.

Close the section with a short cross-link line pointing back to
`docs/COMPANION-PUBLISH-RUNBOOK.md` for pre-flight procedure, and add the reciprocal link from that file
in `175-05` rather than here.

Every command in this section is written so it can be copied and run without editing anything except the
package name and version, and neither of those appears as a literal outside a fence.
  </action>
  <acceptance_criteria>
    - `docs/RELEASE-INCIDENT-RESPONSE.md` contains the heading `## Retire / backfill after a bad publish`, spelled exactly so.
    - That section contains three registry subsections and each opens with a fenced command block.
    - The Hex subsection names all five retirement reasons and the reversal flag, and states that the version remains resolvable.
    - `grep -c 'un-resolve' docs/RELEASE-INCIDENT-RESPONSE.md` prints at least 1 — D-20's fact is stated, not implied.
    - The Maven subsection contains no retire or delete command and states the superseding-version recovery.
    - `awk 'BEGIN{f=0} /^```/{f=!f; next} f==0 && /[0-9]+\.[0-9]+\.[0-9]+/{c++} END{print c+0}' docs/RELEASE-INCIDENT-RESPONSE.md` still prints 0 after this task.
    - `grep -c 'COMPANION-PUBLISH-RUNBOOK.md' docs/RELEASE-INCIDENT-RESPONSE.md` prints at least 1.
  </acceptance_criteria>
  <verify>
    <automated>grep -cE '^## Retire / backfill after a bad publish$' docs/RELEASE-INCIDENT-RESPONSE.md</automated>
    <fails_when>the printed count is anything other than 1</fails_when>
    <automated>awk 'BEGIN{f=0} /^```/{f=!f; next} f==0 && /[0-9]+\.[0-9]+\.[0-9]+/{c++} END{print c+0}' docs/RELEASE-INCIDENT-RESPONSE.md</automated>
    <fails_when>the printed count is anything other than 0</fails_when>
    <automated>grep -c 'unretire' docs/RELEASE-INCIDENT-RESPONSE.md</automated>
    <fails_when>the printed count is 0 — the reversal flag is not documented</fails_when>
    <automated>grep -c 'un-resolve' docs/RELEASE-INCIDENT-RESPONSE.md</automated>
    <fails_when>the printed count is 0 — D-20's SwiftPM cache fact is missing</fails_when>
  </verify>
  <reversibility rating="reversible">Documentation only.</reversibility>
  <done>All three registries have a copy-runnable recovery procedure whose language matches what the tools actually do, with the iOS cache fact stated for the first time in this repository.</done>
</task>

<task type="auto">
  <name>Task 3: Commit the document and record its SHA against the one-way-door bar</name>
  <files>.planning/workstreams/quality-ratchet-release/phases/175-rehearsal-and-publish/175-INCIDENT-DOC-COMMIT.md</files>
  <read_first>
    - `.planning/workstreams/quality-ratchet-release/ROADMAP.md`, the Phase 175 one-way-door note and Success Criterion 1 — the bar this record discharges.
    - `.planning/workstreams/quality-ratchet-release/phases/175-rehearsal-and-publish/175-CONTEXT.md`, decisions D-10 and D-11 — the record is displayed at gate 2, and the checkpoint prompts' recovery language is generated from or linked to this document rather than re-derived.
    - `docs/RELEASE-INCIDENT-RESPONSE.md` as written by Tasks 1 and 2, for the heading anchors to record.
  </read_first>
  <action>
Commit `docs/RELEASE-INCIDENT-RESPONSE.md` and record the resulting commit identity so SC1 is checkable
rather than asserted.

Write `175-INCIDENT-DOC-COMMIT.md` containing:

- The full 40-character commit SHA that introduced `docs/RELEASE-INCIDENT-RESPONSE.md`, obtained with
  `git log --format=%H -1 -- docs/RELEASE-INCIDENT-RESPONSE.md`, together with its author date.
- The two heading anchors the phase's checkpoint prompts will cite —
  `docs/RELEASE-INCIDENT-RESPONSE.md#mid-sequence-partial-failure` and
  `docs/RELEASE-INCIDENT-RESPONSE.md#retire--backfill-after-a-bad-publish` — each confirmed against the
  heading text actually in the file. If a heading was reworded, the anchors change and every citing
  checkpoint must be updated; record that as a finding rather than leaving the anchors stale.
- A one-line statement of the bar: no publish command in this phase executes until this SHA is an
  ancestor of `HEAD` on the branch the publish runs from. Record the exact command a later plan uses to
  check it, in the form `git merge-base --is-ancestor <sha> HEAD`.
- The three per-registry recovery one-liners that gates 1, 2 and 3 will quote, each written as a pointer
  into this document's own rows rather than as independently re-derived prose (D-10). The gates quote;
  they do not paraphrase.

Note that the SHA recorded here is the SHA on the branch this plan commits to. If the work later lands on
`main` through a squash or rebase, the branch SHA is no longer an ancestor of `main` — re-derive and
re-record the post-merge SHA before the first publish, and say in this file that the recorded SHA must be
re-derived after any history-rewriting merge. A stale SHA that fails the ancestor check is correct
behavior; a stale SHA nobody re-checks is the defect.

Stage `.planning/` paths explicitly by filename.
  </action>
  <acceptance_criteria>
    - `175-INCIDENT-DOC-COMMIT.md` exists and contains a 40-character lowercase hexadecimal SHA.
    - `git cat-file -e <recorded-sha>` exits 0 — the SHA resolves in this repository.
    - `git show --stat <recorded-sha>` lists `docs/RELEASE-INCIDENT-RESPONSE.md`.
    - `git merge-base --is-ancestor <recorded-sha> HEAD` exits 0 at the time of recording.
    - Both recorded anchors match headings actually present in `docs/RELEASE-INCIDENT-RESPONSE.md`; state the check in the SUMMARY.
    - The file states the re-derivation rule for a post-squash or post-rebase SHA.
    - The three per-registry recovery one-liners are present and each points at a row or subsection of `docs/RELEASE-INCIDENT-RESPONSE.md`.
  </acceptance_criteria>
  <verify>
    <automated>test -s .planning/workstreams/quality-ratchet-release/phases/175-rehearsal-and-publish/175-INCIDENT-DOC-COMMIT.md</automated>
    <fails_when>non-zero exit — the record is missing or empty</fails_when>
    <automated>SHA=$(grep -oE '\b[0-9a-f]{40}\b' .planning/workstreams/quality-ratchet-release/phases/175-rehearsal-and-publish/175-INCIDENT-DOC-COMMIT.md | head -1); git merge-base --is-ancestor "$SHA" HEAD; echo "ancestor-exit=$? sha=$SHA"</automated>
    <fails_when>the printed `ancestor-exit` is anything other than 0, or `sha` is empty</fails_when>
    <automated>SHA=$(grep -oE '\b[0-9a-f]{40}\b' .planning/workstreams/quality-ratchet-release/phases/175-rehearsal-and-publish/175-INCIDENT-DOC-COMMIT.md | head -1); git show --stat --format= "$SHA" | grep -q 'docs/RELEASE-INCIDENT-RESPONSE.md'; echo "touches-doc-exit=$?"</automated>
    <fails_when>the printed `touches-doc-exit` is anything other than 0</fails_when>
  </verify>
  <reversibility rating="reversible">A record document.</reversibility>
  <done>The incident document is committed, its SHA is on disk, and the ancestor check that discharges the one-way-door bar is named and passing.</done>
</task>

</tasks>

<artifacts_produced>
## Artifacts this phase produces (plan 175-04)

| Kind | Name |
|---|---|
| New file | `docs/RELEASE-INCIDENT-RESPONSE.md` |
| New doc section | `## Irreversibility summary` |
| New doc section | `## Mid-sequence partial failure` |
| New doc section | `## Retire / backfill after a bad publish` |
| New doc invariant sentence | `This document makes no version-specific claims.` |
| New anchor | `docs/RELEASE-INCIDENT-RESPONSE.md#mid-sequence-partial-failure` |
| New anchor | `docs/RELEASE-INCIDENT-RESPONSE.md#retire--backfill-after-a-bad-publish` |
| New file | `.planning/.../175-rehearsal-and-publish/175-INCIDENT-DOC-COMMIT.md` |
</artifacts_produced>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| The runbook's claims -> an operator acting mid-incident | Text read under time pressure becomes a command run against a public registry |
| The document's heading text -> the checkpoint prompts citing its anchors | A reworded heading silently breaks the link a one-way-door gate depends on |
| `release-please.yml`'s immutability comment -> the doc's Maven cells | Two copies of one fact that can drift apart |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-175-16 | Spoofing | A recovery procedure that overstates recoverability — retirement written as a rollback, or Maven written as droppable | high | mitigate | Task 2 requires the real semantics with the reversal flag and the resolvability statement, forbids a Maven retire command, and requires the rehearsal-only `VALIDATED` affordance to be named as not applying to a real publish; an acceptance criterion greps for the reversal flag and for D-20's cache fact. |
| T-175-17 | Repudiation | A Detect cell that says "check the workflow" rather than naming an independent observation | high | mitigate | Task 1 requires every Detect cell to name a registry response, a run conclusion, or a remote ref listing, and the SUMMARY to state how many of the 15 do so. |
| T-175-18 | Tampering | A blank or hedged Irreversibility cell letting a one-way step read as recoverable | high | mitigate | Task 1's acceptance criteria assert zero rows with an empty final column and zero hedging phrases. |
| T-175-19 | Repudiation | A version literal in the doc becoming a permanent-sounding claim, as happened before | medium | mitigate | The invariant sentence plus the outside-fence literal count asserted at 0 in both Task 1 and Task 2; `175-05` makes it a CI check. |
| T-175-20 | Tampering | The recorded commit SHA going stale after a squash or rebase, so the one-way-door bar passes on a SHA nobody rechecks | high | mitigate | Task 3 records the re-derivation rule explicitly and names the `git merge-base --is-ancestor` command that later plans run; a stale SHA fails that check rather than being ignored. |
| T-175-21 | Spoofing | A reworded heading breaking a checkpoint's anchor without anyone noticing | medium | mitigate | Task 3 confirms both anchors against the headings actually present and requires a reword to be recorded as a finding that updates every citing checkpoint. |
| T-175-SC | Tampering | npm/pip/cargo installs | high | mitigate | Not applicable to this plan — it adds no package-manager install step. If any task later requires one, a package-legitimacy gate plus a blocking human checkpoint is required first. |
</threat_model>

<verification>
- `docs/RELEASE-INCIDENT-RESPONSE.md` exists with all three required headings spelled exactly as specified.
- The invariant sentence is present in the first 40 lines and the outside-fence version-literal count is 0.
- The irreversibility summary has three rows; the partial-failure matrix has 15 rows with zero empty Irreversibility cells.
- The Hex reversal flag and D-20's SwiftPM cache fact are both present; no Maven retire command exists.
- `175-INCIDENT-DOC-COMMIT.md` records a resolvable 40-character SHA that touches the document and passes `git merge-base --is-ancestor <sha> HEAD`.
</verification>

<success_criteria>
An operator whose publish has just gone wrong opens one file, reads which of the three registries can be
walked back before reading any procedure, finds their exact failure mode in a registry-grouped matrix,
and runs a command that matches what the tool actually does — and the phase can prove that file existed
before the first publish, by SHA.
</success_criteria>

<output>
Create `.planning/workstreams/quality-ratchet-release/phases/175-rehearsal-and-publish/175-04-SUMMARY.md` when done.
</output>
