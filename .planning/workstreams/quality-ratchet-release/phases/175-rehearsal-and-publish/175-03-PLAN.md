---
phase: 175-rehearsal-and-publish
plan: 03
type: execute
wave: 3
depends_on: ["175-01", "175-02"]
files_modified:
  - .planning/workstreams/quality-ratchet-release/phases/175-rehearsal-and-publish/175-WAVE0-EXIT.md
autonomous: false
requirements: [VAC-03]

estimate:
  tokens: 45000
  raw_tokens: 22500
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "All five of D-28's exit criteria carry an independently checkable verdict on disk — derived scope parity, zero mutable refs over the full scope, a demonstrated red on a narrowed scope, a diff confined to `scripts/ci_monitor.cjs` plus the ten mutable-ref workflow files, and a merged pull request — with no sixth verdict and no blank."
    - "The scope-cardinality gate carries a vacuity-taxonomy shape and a measured non-vacuity fact — the cardinalities of the narrowed case it was observed failing against — before it is treated as authority (VAC-03)."
    - "The two items D-29 places outside Wave 0 — auditing whether the pinned SHAs are themselves correct or uncompromised, and extending the cardinality gate beyond `scripts/ci_monitor.cjs` — are recorded as SEED candidates rather than silently absorbed or silently dropped."
    - "No Wave 1 task begins until the Wave 0 pull request is merged into `main` with CI green (D-22); the record names the merge commit."
  prohibitions:
    - "Never resolve a failing scope-cardinality check by narrowing its expected scope, silencing it, or adding an exclusion; the defect is fixed, not the check."
    - "Never write a verification gate whose exit status is taken from the tail of a pipeline; a pipeline reports the last command's status and the gate would be green while asserting nothing."
    - "Never run `git add -A` or `git add .` under `.planning/`; stage `.planning/` paths explicitly by filename, and never stage `config.json`, `milestone.lock`, or `state.json` under the workstream directory."
    - "Never run `git stash` in any form, and never run `git reset --hard`, `git clean -fd`, or a checkout of tracked paths this plan did not create."
  artifacts:
    - .planning/workstreams/quality-ratchet-release/phases/175-rehearsal-and-publish/175-WAVE0-EXIT.md
  key_links:
    - "`175-01` and `175-02`'s evidence logs -> the five-row exit record -> the human gate that releases Wave 1."
    - "the new scope-cardinality gate -> the six-shape vacuity taxonomy -> VAC-03's bar for a check becoming authority."
---

<planner_notes>
**Edge-probe accounting.** No phase requirement ID from the spec-less probe report applies to this
plan (its requirement is VAC-03). **0 surfaced == 0 authored + 0 flagged.**

**Why a separate plan for the exit.** D-28 enumerates five independently checkable criteria. Folding
them into `175-02`'s last task would let a partially-satisfied exit read as a finished plan. They are a
gate, and a gate deserves a verdict of its own.

**Why the merge is a blocking human checkpoint.** D-22 requires Wave 0 to land as its own pull
request, merged and fully green, before any Wave 1 task begins. Merging into protected `main` is not
something the executor can do unattended in this repository — an admin-bypass merge is refused here —
so the release of Wave 1 is a human act, and the checkpoint is where it is recorded.
</planner_notes>

<scope_boundary>
This plan records and gates. It changes no executable file. If writing the record reveals that one of
D-28's five criteria is unmet, do **not** repair it here and do **not** soften the criterion — record
the unmet verdict with the exact command that would satisfy it, and stop at the checkpoint. A record
that quietly fixes what it was supposed to measure is not a record.
</scope_boundary>

<objective>
Give Wave 0 an explicit, five-part exit verdict, and hold Wave 1 behind a human confirmation that the
Wave 0 pull request actually merged green.

Purpose: D-28 names five criteria precisely because "the triage is done" is the kind of claim this
milestone exists to stop accepting. D-22 makes the merge the hard bar: a bad pin breaks a non-publish
proof workflow well outside the blast radius of the publish waves, and that has to surface on its own
pull request rather than inside the change window that publishes `0.2.2`.

Output: `175-WAVE0-EXIT.md` with five verdicts, a vacuity-taxonomy row for the new gate, two recorded
SEED candidates, and a named merge commit.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/quality-ratchet-release/VERIFICATION-CONVENTIONS.md
@.planning/workstreams/quality-ratchet-release/phases/175-rehearsal-and-publish/175-CONTEXT.md
@.planning/workstreams/quality-ratchet-release/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: The five-row Wave 0 exit record, the gate's vacuity row, and the two SEED candidates</name>
  <files>.planning/workstreams/quality-ratchet-release/phases/175-rehearsal-and-publish/175-WAVE0-EXIT.md</files>
  <read_first>
    - `.planning/workstreams/quality-ratchet-release/phases/175-rehearsal-and-publish/175-CONTEXT.md`, decisions D-21 through D-29 — the five exit criteria and the two explicit out-of-scope items.
    - `.planning/workstreams/quality-ratchet-release/VERIFICATION-CONVENTIONS.md` — the `vacuity_taxonomy` record's required keys, the explicit escape form, and the "never a bare tick" rule.
    - `.planning/workstreams/quality-ratchet-release/phases/170-vacuous-assertion-remediation/170-VACUITY-TAXONOMY.md` — the six shapes.
    - `.planning/workstreams/quality-ratchet-release/phases/174-clean-room-host-realism-adopter-fidelity/174-NON-VACUITY.md` — the house standard for a measured non-vacuity fact.
    - This phase's `175-01-SUMMARY.md` and `175-02-SUMMARY.md`, and both evidence logs under `.../175-rehearsal-and-publish/evidence/`.
  </read_first>
  <action>
Write `175-WAVE0-EXIT.md` with three sections.

**Section 1 — the five exit verdicts.** One row per D-28 criterion, each carrying an explicit
**met** or **not met** verdict and the command or artifact that decides it. There is no third verdict
and no blank.

1. Derived scope parity — the no-argument `check-actions` run reports a `files=` count equal to a
   freshly globbed count of `.github/workflows/*.yml` plus `action.yml` files under `.github/actions/`.
   Record both numbers as observed, and record how the expected number was computed. Never compare
   against a number written into a plan.
2. Zero mutable refs over the full scope — that same no-argument invocation reports `mutable_refs=0`
   and exits 0. Cite `evidence/175-check-actions-full-scope.log`.
3. The cardinality assertion has been exercised against a deliberate regression — cite
   `evidence/175-scope-gate-regression.log` and quote the two cardinalities of the narrowed case and
   its observed red, plus the equal-scope control's green.
4. Diff confinement — `git diff --name-only` against the pull request base lists only
   `scripts/ci_monitor.cjs`, the ten workflow files that carried mutable refs, this phase's evidence
   logs, and this record. No already-pinned publish workflow appears. No CI redesign appears.
5. Landed as its own pull request, CI fully green, merged — left as **not met** by this task; Task 2
   is where it closes.

**Section 2 — the vacuity-taxonomy row for the new gate (VAC-03).** The scope-cardinality assertion is
a check this milestone landed, so it clears the taxonomy bar before it is treated as authority. Give it
a check id as it appears in its own source, a shape letter from the six shapes (or the explicit escape
form with its reason), and a measured non-vacuity fact: the specific narrowed-scope case it was
observed turning red against, with its cardinalities. Never a bare tick and never a blank shape.

State here, with D-26's reasoning in one line, why this gate stays separate from PR #173's
`check_absence_is_not_success.exs` rather than being folded into it: #173's family is "the assertion is
a no-op on the value it looked at"; this is "the assertion is correct but the set of things it looked
at was truncated" — same pathology, structurally different failure mode, and #173 verifiably would not
have caught this defect.

**Section 3 — the two SEED candidates (D-29).** Record both, each with a one-line statement of what
would be audited and why it is outside Wave 0:

- Auditing whether the SHAs pinned by `175-02` are themselves correct, latest, or uncompromised.
- Extending the scope-cardinality gate beyond `scripts/ci_monitor.cjs` to other checks in the repo.

File them through the project's normal capture path so they exist as SEEDs and not only as prose in
this file; record their SEED identifiers in the row.

Stage `.planning/` paths explicitly by filename.
  </action>
  <acceptance_criteria>
    - `175-WAVE0-EXIT.md` exists and its Section 1 contains exactly five verdict rows; the count of rows with a blank verdict is 0.
    - Row 1 records two numbers — observed `files=` and freshly computed expected — and they are equal.
    - Row 2 cites `evidence/175-check-actions-full-scope.log` and records `mutable_refs=0` with an exit status of 0.
    - Row 3 quotes the narrowed case's two cardinalities and its observed red, and the control's green.
    - Row 4 names every path in the pull request diff, and the count of already-pinned publish workflows appearing in it is 0.
    - Row 5 reads **not met** at the end of this task, naming Task 2's checkpoint as what closes it.
    - Section 2 carries a shape letter or the explicit escape form with a reason — never blank — and a measured fact with cardinalities, plus the one-line D-26 separation rationale.
    - Section 3 carries two rows, each with a filed SEED identifier, not prose alone.
  </acceptance_criteria>
  <verify>
    <automated>test -s .planning/workstreams/quality-ratchet-release/phases/175-rehearsal-and-publish/175-WAVE0-EXIT.md</automated>
    <fails_when>non-zero exit — the record is missing or empty</fails_when>
    <automated>node scripts/ci_monitor.cjs check-actions</automated>
    <fails_when>non-zero exit, or the summary line reports `mutable_refs=` anything other than 0</fails_when>
    <automated>node scripts/ci_monitor.cjs test-check-actions-scope</automated>
    <fails_when>non-zero exit — the narrowed case passed or the control failed</fails_when>
    <automated>git diff --name-only origin/main...HEAD</automated>
    <fails_when>the output contains `.github/workflows/release-please.yml`, `hex-publish.yml`, `ios-mirror-backfill.yml`, or `exact-public-proof.yml`</fails_when>
  </verify>
  <reversibility rating="reversible">A record document; nothing executable changes.</reversibility>
  <done>Four of D-28's five criteria carry a decided verdict backed by a named artifact, the new gate has a taxonomy row with a measured fact, and both out-of-scope items are filed as SEEDs.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Confirm the Wave 0 pull request is merged and green before Wave 1 opens</name>
  <what-built>
`scripts/ci_monitor.cjs` now derives its `check-actions` scan scope from the tree, prints the file
count it scanned, and refuses to exit 0 on a truncated scope. The 31 mutable third-party action refs
across ten workflow files are pinned to 40-character commit SHAs with version comments. A
`test-check-actions-scope` subcommand demonstrates the scope assertion going red on a narrowed scope
and green on an equal-scope control. `175-WAVE0-EXIT.md` records four of D-28's five criteria as met.
  </what-built>
  <how-to-verify>
1. Open the Wave 0 pull request and confirm its diff contains only `scripts/ci_monitor.cjs`, the ten
   workflow files, this phase's two evidence logs, and `175-WAVE0-EXIT.md`. No already-pinned publish
   workflow. No CI redesign.
2. Confirm every required check on that pull request is green — in particular the proof workflows whose
   `uses:` refs changed (`phase23`, `phase34`, `phase43`, `phase68`, `phase130`, `phase132`), the
   see-it-run collateral lane, and the native collateral advisory lane. A bad pin breaks one of these,
   and containing that failure here rather than inside the publish window is the entire reason D-22
   puts this on its own pull request.
3. Merge the pull request into `main`. This repository refuses an admin-bypass merge, so it must go
   through the normal protected-branch path.
4. After the merge, confirm on `main` that `node scripts/ci_monitor.cjs check-actions` exits 0 and
   reports `mutable_refs=0`.
5. Record the merge commit SHA — Row 5 of `175-WAVE0-EXIT.md` is updated to **met** with that SHA.

If any required check is red, do not merge and do not proceed. Report which check and stop; a bad pin
is repaired on this pull request, not worked around.
  </how-to-verify>
  <resume-signal>Reply `wave0 merged <merge-commit-sha>` with the actual merge commit SHA to release Wave 1, or describe the failing check to stop here.</resume-signal>
  <done>The Wave 0 pull request is merged into `main` with CI green, the merge commit SHA is recorded in Row 5 of `175-WAVE0-EXIT.md`, and Wave 1 is released.</done>
</task>

</tasks>

<artifacts_produced>
## Artifacts this phase produces (plan 175-03)

| Kind | Name |
|---|---|
| New file | `.planning/.../175-rehearsal-and-publish/175-WAVE0-EXIT.md` |
| Document section | the five-row D-28 exit verdict table |
| Document section | the vacuity-taxonomy row for the scope-cardinality gate |
| Document section | the two D-29 SEED candidate rows |
| New SEED | audit of the SHAs pinned by `175-02` |
| New SEED | extension of the scope-cardinality gate beyond `scripts/ci_monitor.cjs` |
</artifacts_produced>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Wave 0's self-reported completion -> the release of Wave 1 | The record is what authorizes the phase's irreversible half to begin |
| A Wave 0 pull request -> protected `main` | A bad pin reaches every workflow in the repo on merge |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-175-11 | Repudiation | A partially-satisfied Wave 0 exit reading as complete | high | mitigate | Task 1 requires five explicit verdicts with no third value and no blank, each naming the command or artifact that decides it; Row 5 starts as not met by construction. |
| T-175-12 | Spoofing | A scope-parity claim compared against a number copied from the plan rather than computed | high | mitigate | Row 1 requires the expected count to be freshly globbed at record time and both numbers recorded; the plan deliberately writes down no expected count. |
| T-175-13 | Tampering | A bad pin breaking a non-publish proof workflow discovered inside the publish window | high | mitigate | D-22's own-pull-request sequencing plus Task 2's checkpoint, which enumerates the specific lanes whose refs changed and forbids merging on a red check. |
| T-175-14 | Elevation of Privilege | An admin-bypass merge skipping required checks | high | mitigate | Task 2 states the normal protected-branch path is required and that admin-bypass is refused in this repository; the resume signal demands the real merge commit SHA. |
| T-175-15 | Repudiation | The out-of-scope items silently absorbed or silently dropped | medium | mitigate | Section 3 requires both D-29 items filed as SEEDs with identifiers, not prose. |
| T-175-SC | Tampering | npm/pip/cargo installs | high | mitigate | Not applicable to this plan — it adds no package-manager install step. If any task later requires one, a package-legitimacy gate plus a blocking human checkpoint is required first. |
</threat_model>

<verification>
- `175-WAVE0-EXIT.md` exists with exactly five verdict rows and zero blank verdicts.
- `node scripts/ci_monitor.cjs check-actions` exits 0 with `mutable_refs=0` and a `files=` count equal to a freshly globbed count.
- `node scripts/ci_monitor.cjs test-check-actions-scope` exits 0.
- The pull request diff contains no already-pinned publish workflow.
- The Wave 0 pull request is merged into `main` with CI green and its merge commit SHA is recorded.
</verification>

<success_criteria>
Wave 0 is closed on evidence rather than on assertion: five decided verdicts, a gate that has been
seen failing, two out-of-scope items filed rather than forgotten, and a merged, green pull request whose
commit SHA is on disk. Only then does Wave 1 open.
</success_criteria>

<output>
Create `.planning/workstreams/quality-ratchet-release/phases/175-rehearsal-and-publish/175-03-SUMMARY.md` when done.
</output>
