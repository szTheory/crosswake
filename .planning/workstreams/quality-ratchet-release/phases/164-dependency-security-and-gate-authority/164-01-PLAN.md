---
phase: 164-dependency-security-and-gate-authority
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - mix.lock
  - examples/phoenix_host/mix.lock
  - script/check_dependency_security.sh
  - test/fixtures/security/advisory-bearing.lock
  - test/crosswake/proof/phase164_dependency_security_and_gate_authority_test.exs
  - .github/workflows/dependency-security.yml
autonomous: true
requirements: [SEC-01, SEC-02, SEC-03]
estimate:
  tokens: 26000
  raw_tokens: 26000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "The root and example-host locks resolve together to the exact patched current-minor target set while their public Phoenix, LiveView, and Plug ranges remain unchanged (D-01, D-02; SEC-01, SEC-02)."
    - "Each canonical project passes its own credential-free mix hex.audit invocation, and an inactive advisory-bearing lock is rejected with only the lock path, package, and corrective command disclosed (D-03, D-04; SEC-01, SEC-03)."
    - "One literal merge-blocking-dependency-security job is the sole producer of the stable pull-request result; branch-protection registration remains post-main work through the existing registrar (D-05, D-06, D-07, D-13; SEC-03)."
  artifacts:
    - path: "script/check_dependency_security.sh"
      provides: "Fail-closed independent audit runner and deterministic inactive-lock negative-control seam."
    - path: ".github/workflows/dependency-security.yml"
      provides: "Exactly one literal dependency-security required-result producer."
    - path: "test/crosswake/proof/phase164_dependency_security_and_gate_authority_test.exs"
      provides: "Hermetic lock/range/audit/producer contract proof."
  key_links:
    - from: ".github/workflows/dependency-security.yml"
      to: "script/check_dependency_security.sh"
      via: "one direct job step, without a same-named sibling or aggregator"
      pattern: "merge-blocking-dependency-security|check_dependency_security"
    - from: "script/check_dependency_security.sh"
      to: "mix.lock and examples/phoenix_host/mix.lock"
      via: "two independent mix hex.audit working directories plus a separate fixture assertion"
      pattern: "hex.audit|examples/phoenix_host|advisory-bearing.lock"
  prohibitions:
    - "requirement_id: SEC-02 | category: values | statement: MUST NOT broaden compatibility floors, cross a minor line while a fixed current-minor release exists, or retain unrelated dependency refresh. | status: unresolved | verification: flagged-unverified"
    - "requirement_id: SEC-03 | category: authority | statement: MUST NOT add a second security producer or register its context before one green main-branch result exists. | status: unresolved | verification: flagged-unverified"
    - "requirement_id: SEC-03 | category: privacy | statement: MUST NOT print credentials, environment dumps, adopter data, payloads, tokens, account/device identifiers, or unrelated audit state. | status: unresolved | verification: flagged-unverified"
flagged_assumptions:
  - "[UNRESOLVED-SPEC-PROBE][SEC-01 unclassified] The two canonical lockfiles are the complete supported Hex audit surface; no additional project or advisory allowlist is inferred."
  - "[UNRESOLVED-SPEC-PROBE][SEC-02 adjacency] Only resolver-required transitive entries adjacent to Phoenix 1.8.13, Phoenix LiveView 1.1.33, Plug 1.19.5, Bandit 1.12.5, and hpax 1.0.4 may move."
  - "[UNRESOLVED-SPEC-PROBE][SEC-02 empty] An absent lock, absent target tuple, or empty audit result is a failure, not a clean result."
  - "[UNRESOLVED-SPEC-PROBE][SEC-02 ordering] Lock tuple ordering and audit report ordering carry no semantic priority; assertions compare resolved identities and deterministic sanitized records."
  - "[UNRESOLVED-SPEC-PROBE][SEC-03 concurrency] Concurrent workflow runs do not create additional authority: the literal context has one producer per run, while trigger/concurrency redesign remains Phase 165."
---

<objective>
Establish the production tracer for Phase 164: both supported locks pass through one fail-closed dependency-security command and one stable pull-request producer.

Purpose: Remove known dependency advisories without changing public compatibility policy, then prove the retained security result cannot pass vacuously.
Output: Exact constrained lock updates, a sanitized dual-audit command, inactive vulnerable fixture, phase proof, and one stable workflow producer.
</objective>

<artifacts_this_phase_produces>
- New executable `script/check_dependency_security.sh` with canonical dual-audit mode and an explicit inactive-lock fixture mode.
- New inactive `test/fixtures/security/advisory-bearing.lock`; it is never copied over a canonical lock.
- New ExUnit entry point `test/crosswake/proof/phase164_dependency_security_and_gate_authority_test.exs` covering exact targets, unchanged ranges, audit non-vacuity, and producer cardinality.
- New workflow/job `.github/workflows/dependency-security.yml` / `merge-blocking-dependency-security`.
- Updated root and example-host lock tuples for Phoenix 1.8.13, Phoenix LiveView 1.1.33, Plug 1.19.5, Bandit 1.12.5, hpax 1.0.4, and only resolver-required transitives; no new package or public manifest symbol.
</artifacts_this_phase_produces>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/workstreams/quality-ratchet-release/phases/164-dependency-security-and-gate-authority/164-CONTEXT.md
@.planning/workstreams/quality-ratchet-release/phases/164-dependency-security-and-gate-authority/164-RESEARCH.md
@.planning/workstreams/quality-ratchet-release/phases/164-dependency-security-and-gate-authority/164-PATTERNS.md
@mix.exs
@examples/phoenix_host/mix.exs
@script/check_required_checks_registered.sh
@.github/workflows/requires-example-host-gate.yml

<interfaces>
- `mix hex.audit` is the advisory authority and must execute separately in repository root and `examples/phoenix_host` without publishing credentials.
- The committed manifests are public compatibility authority and remain byte-for-byte unchanged under D-02.
- `merge-blocking-dependency-security` is a literal governance contract; registration is delegated to the unchanged green-first `script/register_required_checks.sh` only after main has produced it green.
</interfaces>
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Trace both patched locks through one fail-closed audit command</name>
  <reversibility rating="reversible">D-01 updates generated locks within existing public ranges; reverting the lock diff and command restores the prior repository contract without migration.</reversibility>
  <read_first>
    - AGENTS.md
    - .planning/workstreams/quality-ratchet-release/phases/164-dependency-security-and-gate-authority/164-CONTEXT.md
    - .planning/workstreams/quality-ratchet-release/phases/164-dependency-security-and-gate-authority/164-RESEARCH.md
    - .planning/workstreams/quality-ratchet-release/phases/164-dependency-security-and-gate-authority/164-PATTERNS.md
    - mix.exs
    - mix.lock
    - examples/phoenix_host/mix.exs
    - examples/phoenix_host/mix.lock
    - script/check_required_checks_registered.sh
  </read_first>
  <files>mix.lock, examples/phoenix_host/mix.lock, script/check_dependency_security.sh, test/fixtures/security/advisory-bearing.lock, test/crosswake/proof/phase164_dependency_security_and_gate_authority_test.exs</files>
  <behavior>
    - "RED: the current advisory-bearing canonical locks fail, and the inactive known-bad lock is rejected with its fixture path, affected package, and one corrective command."
    - "GREEN: root and example locks independently audit clean at Phoenix 1.8.13, LiveView 1.1.33, and Plug 1.19.5; the example also resolves Bandit 1.12.5 and hpax 1.0.4."
    - "The manifests' Phoenix, LiveView, Plug, and Bandit declarations remain byte-for-byte unchanged, and the full lock diff contains only named targets plus resolver-required coherent transitives."
    - "A missing lock, missing audit executable/result, or audit tool error returns nonzero and cannot be mistaken for zero advisories."
  </behavior>
  <action>Write the failing phase proof first. In disposable copies, temporarily constrain the five D-01 targets exactly, resolve each lock with Mix, inspect the complete before/after package table, and transfer only resolver-produced coherent tuples into the canonical locks; reject LiveView 1.2.x, Plug 1.20.x, manifest edits, and unrelated refresh per D-01/D-02. Implement a bash-3.2-compatible `script/check_dependency_security.sh` rooted from its own path. Its canonical mode must run `mix hex.audit` independently in both projects, accumulate both statuses, reject missing/empty inputs, and print only a bounded project/lock/package/corrective-command summary per D-03. Add a separate explicit fixture mode for the inactive D-04 lock snapshot so an expected negative result can never mask either real audit. The fixture contains package metadata only and never replaces a canonical lock.</action>
  <acceptance_criteria>
    - Both canonical audits run even when the first fails, and the command exits nonzero for any advisory, missing input/result, or tool error.
    - Exact targets are Phoenix 1.8.13, LiveView 1.1.33, Plug 1.19.5, Bandit 1.12.5, and hpax 1.0.4; any collateral tuple is resolver-required and recorded by the test.
    - `mix.exs` and `examples/phoenix_host/mix.exs` are unchanged, and `mix deps.get --check-locked` succeeds in both projects.
    - The inactive known-bad fixture fails with actionable bounded output and contains no credentials, sensitive identifiers, payloads, or adopter facts.
  </acceptance_criteria>
  <verify><automated>script/check_dependency_security.sh &amp;&amp; script/check_dependency_security.sh --assert-vulnerable-fixture test/fixtures/security/advisory-bearing.lock &amp;&amp; mix deps.get --check-locked &amp;&amp; (cd examples/phoenix_host &amp;&amp; mix deps.get --check-locked) &amp;&amp; mix test test/crosswake/proof/phase164_dependency_security_and_gate_authority_test.exs -x</automated></verify>
  <done>The two canonical lock authorities are patched inside existing ranges, audit independently, and retain a deterministic non-vacuous negative control.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Publish exactly one stable dependency-security result</name>
  <reversibility rating="costly">D-07 makes the literal check name a future branch-protection contract; renaming it after registration requires coordinated main evidence and governance administration.</reversibility>
  <read_first>
    - .planning/workstreams/quality-ratchet-release/phases/164-dependency-security-and-gate-authority/164-CONTEXT.md
    - .github/workflows/requires-example-host-gate.yml
    - .github/workflows/required-checks-audit.yml
    - script/list_merge_blocking_checks.py
    - script/register_required_checks.sh
    - script/check_dependency_security.sh
    - test/crosswake/proof/phase164_dependency_security_and_gate_authority_test.exs
  </read_first>
  <files>.github/workflows/dependency-security.yml, test/crosswake/proof/phase164_dependency_security_and_gate_authority_test.exs</files>
  <behavior>
    - "Exactly one literal workflow job display name equals merge-blocking-dependency-security."
    - "The job directly runs the canonical audits and inactive-lock negative control; no job-level continue-on-error can turn either path green."
    - "The step summary is bounded and actionable, while all raw environment, credentials, and unrelated state remain absent."
    - "Branch-protection registration is absent from this change and remains a post-main handoff through the existing registrar."
  </behavior>
  <action>Add `.github/workflows/dependency-security.yml` using the existing checkout/setup-beam pin and current trigger posture. Define one job id and literal display name `merge-blocking-dependency-security`; call the script directly for both canonical audits and its deterministic D-04 negative control. Keep failure authority in the script/job, with `if: always()` used only for a sanitized summary and no workflow-level suppression. Extend the phase proof to parse both `.yml` and `.yaml`, assert exact producer cardinality/provenance, reject dynamic or duplicate security names, and assert the workflow does not invoke `script/register_required_checks.sh`. Preserve D-06/D-07: do not mutate branch protection; record the post-main command in the summary handoff only if administration authority is absent.</action>
  <acceptance_criteria>
    - Repository-local proof finds one and only one `merge-blocking-dependency-security` producer at the new workflow/job.
    - Introducing an advisory-bearing fixture result makes the job fail; successful canonical audits alone cannot mask it.
    - No cache, runner migration, trigger deduplication, workflow consolidation, Android change, adopter artifact, or registration write is introduced per D-13.
    - The only governance follow-up is the existing green-first registrar after the context has appeared green on main.
  </acceptance_criteria>
  <verify><automated>mix test test/crosswake/proof/phase164_dependency_security_and_gate_authority_test.exs -x &amp;&amp; python3 script/list_merge_blocking_checks.py --emitters | awk -F '\t' '$1 == "merge-blocking-dependency-security" {n++} END {exit n == 1 ? 0 : 1}'</automated></verify>
  <done>Pull requests expose one actionable dependency-security authority whose real and negative-control paths both fail closed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Hex advisory data -> canonical locks/audit process | Third-party advisory results and resolver output determine whether repository dependencies are safe. |
| repository audit output -> workflow summary | Potentially sensitive process output is reduced to bounded package/path/remediation data. |
| workflow job name -> protected-branch policy | A literal context becomes governance authority only after a green main result and explicit registrar action. |

## STRIDE Threat Register — ASVS L1, block on high

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-164-04 | Denial of Service | Phoenix/Plug/Bandit/hpax dependency graph | high | mitigate | Task 1 resolves exact fixed current-minor targets, audits both locks independently, and proves an advisory-bearing fixture fails. |
| T-164-05 | Information Disclosure | audit command and workflow summary | high | mitigate | Tasks 1-2 emit only project/lock/package/corrective-command records and test absence of environment, credential, token, adopter, account, device, and payload data. |
| T-164-07 | Spoofing / Tampering | dependency-security context | high | mitigate | Task 2 proves one literal producer and leaves registration behind the existing green-first main-branch handoff. |
| T-164-SC | Tampering | package supply chain | low | accept | No new package is installed; existing Hex packages are resolver-produced and independently audited against Hex authority. |
</threat_model>

<source_coverage_audit>
| Source | ID | Contract | Coverage | Status |
|---|---|---|---|---|
| GOAL | Phase 164 | Patched dependencies fail closed through one authoritative path. | Tasks 1-2 establish the vertical security path. | COVERED |
| REQ | SEC-01, SEC-02, SEC-03 | Clean audits, constrained patched versions, stable negative-controlled result. | Tasks 1-2. | COVERED |
| RESEARCH | dependency/security producer | Exact five targets, dual audit, inactive fixture, one job. | Tasks 1-2 follow the responsibility map. | COVERED |
| CONTEXT | D-01..D-07, D-13 | Constrained remediation and existing governance seam. | Cited in truths/actions/criteria. | COVERED |
| EDGE | 5 rows | SEC-01 unclassified; SEC-02 adjacency/empty/ordering; SEC-03 concurrency. | Five applicable rows equal five flagged assumptions. | FLAGGED |
| PROHIBITION | 3 bespoke constraints | Compatibility scope, authority timing, audit privacy. | Descriptor-less flagged-unverified entries retained. | FLAGGED |
</source_coverage_audit>

<verification>
Run the tracer command, both locked-dependency checks, the phase proof, and exact-producer count. Live branch-protection administration is not a Phase 164 execution gate; unavailable authority must remain an explicit post-main handoff rather than a pass.
</verification>

<success_criteria>
- Both supported projects return zero known advisories inside unchanged public compatibility ranges.
- The inactive advisory fixture is rejected deterministically and safely.
- One literal pull-request result owns dependency security, with no registration or Phase-165 topology work mixed in.
</success_criteria>

<output>
Create `.planning/workstreams/quality-ratchet-release/phases/164-dependency-security-and-gate-authority/164-01-SUMMARY.md` when done.
</output>
