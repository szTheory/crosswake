---
phase: 164-dependency-security-and-gate-authority
plan: "03"
type: execute
wave: 2
depends_on: [164-01]
files_modified:
  - test/support/example_host.ex
  - test/crosswake/proof/phase164_example_host_isolation_test.exs
  - script/check_example_host_isolation.sh
  - .github/workflows/requires-example-host-gate.yml
autonomous: true
requirements: [CIG-03]
estimate: {tokens: 24000, raw_tokens: 24000, tasks: 2, confidence: low}
must_haves:
  truths:
    - "Example-host setup snapshots exact prior Application presence/value and owns every added BEAM path, started PID, temporary file, and database; cleanup restores or deletes only resources created by that test (D-09, D-10; CIG-03)."
    - "An already-started process and a pre-existing config/path are explicitly unowned and survive cleanup unchanged, while every owned resource is absent after on_exit (D-09, D-10; CIG-03)."
    - "The tagged class and complete root suite pass a bounded fixed-seed matrix without residue before --max-cases 1 is removed; the dedicated lane/name and dev/test compile split remain intact (D-09, D-13; CIG-03)."
  artifacts:
    - path: "test/support/example_host.ex"
      provides: "Exact prior-state snapshots and scoped ownership-token cleanup."
    - path: "test/crosswake/proof/phase164_example_host_isolation_test.exs"
      provides: "Pre-existing-versus-owned env/path/PID/file/database lifecycle proof."
    - path: "script/check_example_host_isolation.sh"
      provides: "Bounded fixed-seed tagged/full-suite and residue evidence command."
  key_links:
    - from: "Crosswake.TestSupport.ExampleHost setup"
      to: "ExUnit on_exit"
      via: "fetch_env snapshots and ownership tokens registered at each mutation boundary"
      pattern: "fetch_env|put_env|delete_env|delete_path|on_exit"
    - from: ".github/workflows/requires-example-host-gate.yml"
      to: "script/check_example_host_isolation.sh"
      via: "evidence-gated removal of serial max-cases after dev host compile"
      pattern: "requires_example_host|check_example_host_isolation"
  prohibitions:
    - "requirement_id: CIG-03 | category: safety | statement: MUST NOT stop, delete, reset, or overwrite a process, config key, path, file, or database the current test does not own. | status: unresolved | verification: flagged-unverified"
    - "requirement_id: CIG-03 | category: values | statement: MUST NOT conceal isolation defects with retries, exclusions, weaker async coverage, global reset, or permanent serialization. | status: unresolved | verification: flagged-unverified"
    - "requirement_id: CIG-03 | category: scope | statement: MUST NOT alter the dedicated job identity, runner, cache, triggers, workflow topology, Android proof, or parked adopter lane. | status: unresolved | verification: flagged-unverified"
flagged_assumptions:
  - "[UNRESOLVED-SPEC-PROBE][CIG-03 concurrency] Three fixed seeds (17, 101, 1009), each running the tagged class and complete suite without --max-cases 1 and with residue checks, are the bounded parallel evidence; scheduler optimization remains Phase 165."
---

<objective>
Replace detached global example-host test state with exact owned-resource lifecycle and retain bounded evidence that the tagged and complete suites coexist safely.

Purpose: Make state-mutating tests independent of ordering luck before removing the documented serialization stopgap.
Output: Hardened ExampleHost support, lifecycle proof, deterministic isolation matrix, and evidence-gated dedicated lane update.
</objective>

<artifacts_this_phase_produces>
- New internal ExampleHost ownership-token/state-snapshot helpers for Application keys, added code paths, owned processes, and unique database/files; the public test helper call shape remains compatible unless a compile-proven caller change is required.
- New `test/crosswake/proof/phase164_example_host_isolation_test.exs` and executable `script/check_example_host_isolation.sh` using fixed seeds 17, 101, and 1009.
- Updated requires-example-host job command and live-derived summary after evidence passes; no product-runtime, Android, adopter, or generic cleanup API.
</artifacts_this_phase_produces>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/workstreams/quality-ratchet-release/phases/164-dependency-security-and-gate-authority/164-CONTEXT.md
@.planning/workstreams/quality-ratchet-release/phases/164-dependency-security-and-gate-authority/164-RESEARCH.md
@.planning/workstreams/quality-ratchet-release/phases/164-dependency-security-and-gate-authority/164-PATTERNS.md
@test/support/example_host.ex
@.github/workflows/requires-example-host-gate.yml
@test/test_helper.exs

<interfaces>
- `Application.fetch_env/2` preserves present-versus-absent state; cleanup restores `{:ok, value}` with put_env and `:error` with delete_env.
- `{:already_started, pid}` is an unowned resource token and must never be stopped by this helper.
- Only paths for which `Code.prepend_path/1` added ownership and only unique test-created files/databases may be removed.
</interfaces>
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Trace one example-host setup through exact cleanup</name>
  <reversibility rating="costly">D-09 changes the shared lifecycle seam used by multiple tagged proof tests, so reversal requires coordinated helper and caller behavior.</reversibility>
  <read_first>
    - AGENTS.md
    - .planning/workstreams/quality-ratchet-release/phases/164-dependency-security-and-gate-authority/164-CONTEXT.md
    - .planning/workstreams/quality-ratchet-release/phases/164-dependency-security-and-gate-authority/164-RESEARCH.md
    - .planning/workstreams/quality-ratchet-release/phases/164-dependency-security-and-gate-authority/164-PATTERNS.md
    - test/support/example_host.ex
    - test/test_helper.exs
    - test/crosswake/proof/phase7_saas_lane_test.exs
  </read_first>
  <files>test/support/example_host.ex, test/crosswake/proof/phase164_example_host_isolation_test.exs</files>
  <behavior>
    - "A present Application value and an absent key are restored exactly after the same setup path."
    - "Newly added paths, owned PIDs, and the unique SQLite file disappear after cleanup; pre-existing paths and already-started PIDs remain untouched."
    - "Cleanup is registered immediately after each mutation and still runs when setup fails after a later boundary."
    - "Two setups use distinct database/file resources and cannot delete or overwrite one another."
  </behavior>
  <action>Write the failing lifecycle proof first. Refactor the central D-09 helper so every Application mutation captures `Application.fetch_env/2` and immediately registers exact restoration; every successful code-path insertion, process start, and unique file/database creation returns an ownership token registered for cleanup. Remove the detached `Process.unlink/1` lifetime strategy. Stop only PIDs returned by a successful start, treat `already_started` as unowned, call `Code.delete_path/1` only for paths this setup added, and remove only the exact unique path created by this test. Preserve existing helper call behavior where possible; do not globally reset applications or broaden edits to callers unless the focused compile proves the shared seam requires it. Add failure-in-the-middle, pre-existing-state, already-started, unique-resource, and idempotent-cleanup tests with no real adopter data.</action>
  <acceptance_criteria>
    - Present and absent Application states round-trip exactly under success and mid-setup failure.
    - Owned resources are absent after cleanup; unowned process/path/config/file state is byte-for-byte or identity-equal to before.
    - Concurrent setup resources are unique and cleanup of one leaves the other intact.
    - No `Process.unlink` detached-global ownership, global reset, retry, exclusion, or product runtime change remains in the helper path.
  </acceptance_criteria>
  <verify><automated>mix test test/crosswake/proof/phase164_example_host_isolation_test.exs</automated></verify>
  <done>One real example-host setup/teardown path proves exact owned-resource cleanup without harming pre-existing state.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Remove serial execution only after bounded tagged/full-suite evidence</name>
  <reversibility rating="reversible">The lane command can restore max-cases serialization if deterministic evidence exposes a remaining defect; the stable job contract is unchanged.</reversibility>
  <read_first>
    - .planning/workstreams/quality-ratchet-release/phases/164-dependency-security-and-gate-authority/164-CONTEXT.md
    - test/support/example_host.ex
    - test/crosswake/proof/phase164_example_host_isolation_test.exs
    - .github/workflows/requires-example-host-gate.yml
    - test/test_helper.exs
  </read_first>
  <files>script/check_example_host_isolation.sh, .github/workflows/requires-example-host-gate.yml</files>
  <behavior>
    - "After the example host is fetched and compiled in MIX_ENV=dev, seeds 17, 101, and 1009 each run `mix test --only requires_example_host --seed N` and `CROSSWAKE_INCLUDE_EXAMPLE_HOST=1 mix test --seed N`, without serial max-cases."
    - "Every seed records pre/post owned temp-resource state and repository diff; any residue or changed application/code-path state fails."
    - "The lane keeps its job id/name and example dev compile plus root test compile split."
    - "The lane removes --max-cases 1 and replaces stale fixed counts only after the complete matrix passes."
  </behavior>
  <action>Create the bounded D-09 evidence script using the fixed seeds 17, 101, and 1009. Its unfiltered canonical invocation must resolve the repository root, run `MIX_ENV=dev mix deps.get` and `MIX_ENV=dev mix compile` from `examples/phoenix_host`, then run `MIX_ENV=test mix compile` from the root before the matrix. For each N in 17, 101, and 1009, execute exactly `mix test --only requires_example_host --seed N` and `CROSSWAKE_INCLUDE_EXAMPLE_HOST=1 mix test --seed N`; the include environment variable is mandatory because `test/test_helper.exs` excludes that class by default, and all six commands run without `--max-cases 1`. Per-run filtering may exist only as a developer feedback option and cannot satisfy the task or phase gate. Compare only the owned temp-prefix/resource and Git diff snapshots needed to detect phase-scoped residue. Fail immediately with seed/class/path remediation while keeping output bounded. Execute the entire matrix before editing the workflow. Only on green evidence, update the dedicated job to call the corrected non-serial path, preserve its literal name and dev/test compile split, and derive summary counts/paths live rather than retaining fixed numbers. Do not add retries or Phase-165 runner/cache/trigger/concurrency changes.</action>
  <acceptance_criteria>
    - All six exact commands pass: three `--only requires_example_host` runs and three complete-suite runs with `CROSSWAKE_INCLUDE_EXAMPLE_HOST=1`; every run leaves no owned temp database/file, Application state, code path, process, or tracked-file change.
    - The workflow no longer contains the serial stopgap and still has the same job id/name, runner, and dev/test build split.
    - A seeded failure or residue returns nonzero with the failing seed, class, and actionable owned resource/path.
    - No unrelated repository residue cleanup, caller rewrite, Android/adopter work, or workflow consolidation is introduced.
  </acceptance_criteria>
  <verify><automated>script/check_example_host_isolation.sh &amp;&amp; mix test test/crosswake/proof/phase164_example_host_isolation_test.exs</automated></verify>
  <done>The serialization flag is removed only behind reproducible evidence that tagged and complete suites restore all state.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| test setup -> global BEAM/Application state | Shared process/config/code-path state must be acquired and restored by exact ownership. |
| test process -> temp filesystem/database | Concurrent tests may delete only unique paths they created. |
| bounded evidence -> CI lane command | Serialization removal is admitted only after deterministic tagged/full-suite proof. |

## STRIDE Threat Register — ASVS L1, block on high

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|---|
| T-164-06 | Tampering / Denial of Service | ExampleHost cleanup | high | mitigate | Task 1 uses exact prior-state snapshots, owned PID/path/file/database tokens, and adversarial pre-existing/concurrent tests. |
| T-164-09 | Repudiation | serialization-removal evidence | medium | mitigate | Task 2 retains fixed seed/class results and fails on any residue before editing the stable lane. |
| T-164-SC | Tampering | package supply chain | low | accept | No install is planned; existing ExUnit, Mix, and repository scripts are reused. |
</threat_model>

<source_coverage_audit>
| Source | ID | Contract | Coverage | Status |
|---|---|---|---|---|
| GOAL | Phase 164 | State-mutating merge proof is trustworthy. | Tasks 1-2 repair and exercise exact state ownership. | COVERED |
| REQ | CIG-03 | Isolation alone/full suite with no residue. | Tasks 1-2. | COVERED |
| RESEARCH | exact resource ownership | fetch_env, owned paths/PIDs/files/database, bounded seeds. | Tasks 1-2. | COVERED |
| CONTEXT | D-09, D-10, D-13 | Dedicated lane, restoration, evidence before deserialization, no topology optimization. | Cited in actions/criteria. | COVERED |
| EDGE | 1 row | CIG-03 concurrency. | One applicable row equals one flagged assumption. | FLAGGED |
| PROHIBITION | 3 bespoke constraints | Unowned-resource safety, no concealment, frozen adjacent scope. | Descriptor-less flagged-unverified entries retained. | FLAGGED |
</source_coverage_audit>

<verification>
Run the focused lifecycle proof, then execute `script/check_example_host_isolation.sh` with no seed filter. That canonical command must await and assert all six tagged/full combinations for seeds 17, 101, and 1009 before it returns success.
</verification>

<success_criteria>
- Exact cleanup preserves every pre-existing resource and removes every owned one.
- Tagged and full suites pass at seeds 17, 101, and 1009 without residue.
- The dedicated lane loses only the proven serialization stopgap and stale fixed-count wording.
</success_criteria>

<output>
Create `.planning/workstreams/quality-ratchet-release/phases/164-dependency-security-and-gate-authority/164-03-SUMMARY.md` when done.
</output>
