---
phase: 164-dependency-security-and-gate-authority
plan: "02"
type: execute
wave: 3
depends_on: [164-01, 164-03, 164-04]
files_modified:
  - script/list_merge_blocking_checks.py
  - script/check_required_checks_registered.sh
  - test/crosswake/proof/phase153_1_gate_integrity_test.exs
  - script/check_exunit_ownership.exs
  - test/crosswake/proof/phase164_exunit_ownership_test.exs
  - script/check_phase164_dependency_security_and_gate_authority.sh
autonomous: true
requirements: [CIG-01, CIG-02]
estimate: {tokens: 32000, raw_tokens: 32000, tasks: 3, confidence: low}
must_haves:
  truths:
    - "One local inventory reports every literal workflow producer with exact path/job provenance and rejects duplicate, malformed, unnamed, dynamic, or missing required authority before any credentialed GitHub read (D-05, D-06; CIG-01)."
    - "The live audit compares registered contexts to the full literal producer map and local merge-blocking candidates to registration; unavailable administration authority remains exit 3 UNVERIFIED (D-06, D-07; CIG-01)."
    - "Every intended root *_test.exs file is classified from executable Elixir tags into at least one merge-blocking execution class, including the dedicated requires_example_host class, without fixed counts or lexical false positives (D-08, D-13; CIG-02)."
    - "One credential-free phase entry point runs security, inventory, local audit, ExUnit ownership, example-host isolation, and aggregator proof in dependency-safe order (D-03, D-05, D-08, D-09, D-11, D-12; SEC-03, CIG-01, CIG-02, CIG-03, CIG-04)."
  artifacts:
    - path: "script/list_merge_blocking_checks.py"
      provides: "Sole strict workflow producer inventory with machine-readable provenance."
    - path: "script/check_exunit_ownership.exs"
      provides: "AST-based test-file-to-merge-blocking-class ownership detector."
    - path: "test/crosswake/proof/phase164_exunit_ownership_test.exs"
      provides: "Owned/unowned/comment-only/string-only detector negative controls."
    - path: "script/check_phase164_dependency_security_and_gate_authority.sh"
      provides: "Single credential-free Phase 164 aggregate verification entry point."
  key_links:
    - from: "script/check_required_checks_registered.sh"
      to: "script/list_merge_blocking_checks.py"
      via: "local strict full-producer records before gh branch-protection input"
      pattern: "emitters|producer|UNVERIFIED|exit 3"
    - from: "script/check_exunit_ownership.exs"
      to: "test/test_helper.exs and .github/workflows/requires-example-host-gate.yml"
      via: "executable tag AST plus explicit merge-blocking execution-class manifest"
      pattern: "requires_example_host|Code.string_to_quoted|unowned"
    - from: "script/check_phase164_dependency_security_and_gate_authority.sh"
      to: "all Phase 164 focused detectors and proof files"
      via: "one credential-free, fail-fast, dependency-safe aggregate command"
      pattern: "dependency_security|emitters|local-only|exunit_ownership|example_host_isolation|aggregator_result"
  prohibitions:
    - "requirement_id: CIG-01 | category: authority | statement: MUST NOT introduce a second producer inventory, substitute job ids for authority names, auto-register branch protection, or treat unavailable gh authority as success. | status: unresolved | verification: flagged-unverified"
    - "requirement_id: CIG-02 | category: values | statement: MUST NOT classify test ownership from substrings, comments, strings, copied file counts, retries, or weakened exclusions. | status: unresolved | verification: flagged-unverified"
    - "requirement_id: CIG-02 | category: scope | statement: MUST NOT change Android jobs, adopter work, runners, caches, triggers, or workflow consolidation while proving shared CI ownership. | status: unresolved | verification: flagged-unverified"
flagged_assumptions:
  - "[UNRESOLVED-SPEC-PROBE][CIG-01 adjacency] Authority adjacency means exact workflow path plus job id for one literal display name; no proximity or naming-family inference is accepted."
  - "[UNRESOLVED-SPEC-PROBE][CIG-01 empty] Empty/missing workflow documents, jobs maps, producer names, registered-context input, or producer sets fail closed or remain explicit UNVERIFIED at the credential boundary."
  - "[UNRESOLVED-SPEC-PROBE][CIG-01 ordering] Workflow file, job, and registered-context order carries no precedence; cardinality is computed over deterministic normalized records."
  - "[UNRESOLVED-SPEC-PROBE][CIG-02 adjacency] Module-level and per-test executable tags apply only through Elixir AST scope; comments, strings, and neighboring modules do not transfer ownership."
  - "[UNRESOLVED-SPEC-PROBE][CIG-02 empty] A test file with no runnable owned tests or an execution class with no merge-blocking lane is unowned and fails with its path/remediation."
  - "[UNRESOLVED-SPEC-PROBE][CIG-02 ordering] File/test/tag ordering has no ownership meaning; classification is set-based and diagnostics sort paths deterministically."
  - "[UNRESOLVED-SPEC-PROBE][CIG-02 concurrency] Ownership proves at least one required execution path, not scheduler order or runner concurrency; Phase 165 retains concurrency topology authority."
---

<objective>
Make repository-local required-context and ExUnit ownership inventory strict, complete, and non-vacuous.

Purpose: Prevent a green duplicate, silently skipped workflow, stale registered context, or unexecuted test file from masquerading as merge authority.
Output: One strict producer detector/auditor, one executable-syntax test ownership detector with hermetic negative controls, and one credential-free aggregate phase command.
</objective>

<artifacts_this_phase_produces>
- Extended `script/list_merge_blocking_checks.py` full-producer/emitters contract and strict diagnostic vocabulary for malformed-workflow, non-map-jobs, unnamed-authority, dynamic-authority, duplicate-producer, and missing-producer cases.
- Extended bidirectional `script/check_required_checks_registered.sh` audit; the existing registrar remains unchanged.
- New executable `script/check_exunit_ownership.exs` and its execution-class manifest/diagnostic records.
- New `test/crosswake/proof/phase164_exunit_ownership_test.exs`; no new workflow, package, Android surface, or branch-protection writer.
- New executable `script/check_phase164_dependency_security_and_gate_authority.sh` as the one credential-free phase-level verification entry point.
</artifacts_this_phase_produces>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/workstreams/quality-ratchet-release/phases/164-dependency-security-and-gate-authority/164-CONTEXT.md
@.planning/workstreams/quality-ratchet-release/phases/164-dependency-security-and-gate-authority/164-RESEARCH.md
@.planning/workstreams/quality-ratchet-release/phases/164-dependency-security-and-gate-authority/164-PATTERNS.md
@script/list_merge_blocking_checks.py
@script/check_required_checks_registered.sh
@script/register_required_checks.sh
@test/test_helper.exs
@.github/workflows/requires-example-host-gate.yml
@.github/workflows/aggregator-negative-control.yml

<interfaces>
- The detector's full producer records are tab-separated `name`, `workflow path`, and `job id` for local consumers; diagnostics go to stderr and nonzero status.
- Registered contexts are external policy input, not the definition of local producers: registered -> exactly one full producer, merge-blocking candidate -> registered.
- ExUnit ownership derives from executable module/test tags and the explicit default/hermetic versus requires-example-host execution classes.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Make one producer inventory authoritative in both directions</name>
  <reversibility rating="costly">D-05 makes the detector output a governance interface consumed by local tests and the branch-protection auditor; incompatible changes require coordinated script/test migration.</reversibility>
  <read_first>
    - AGENTS.md
    - .planning/workstreams/quality-ratchet-release/phases/164-dependency-security-and-gate-authority/164-CONTEXT.md
    - .planning/workstreams/quality-ratchet-release/phases/164-dependency-security-and-gate-authority/164-RESEARCH.md
    - .planning/workstreams/quality-ratchet-release/phases/164-dependency-security-and-gate-authority/164-PATTERNS.md
    - script/list_merge_blocking_checks.py
    - script/check_required_checks_registered.sh
    - script/register_required_checks.sh
    - test/crosswake/proof/phase153_1_gate_integrity_test.exs
    - .github/workflows/requires-example-host-gate.yml
    - .github/workflows/aggregator-negative-control.yml
  </read_first>
  <files>script/list_merge_blocking_checks.py, script/check_required_checks_registered.sh, test/crosswake/proof/phase153_1_gate_integrity_test.exs</files>
  <behavior>
    - "Malformed YAML, a non-map document/jobs node, unnamed required-style job, and dynamic required name each return nonzero with exact workflow/job provenance and remediation."
    - "Duplicate literal names retain both producer records and fail before any gh request; default presentation must not deduplicate the evidence used for cardinality."
    - "A registered context without exactly one literal local producer fails, while every local merge-blocking candidate absent from registration also fails."
    - "A `--local-only` audit mode runs the complete local syntax/type/name/cardinality checks without invoking gh or requiring branch-protection credentials."
    - "Without readable branch protection, all local validation still runs and the final outcome is the existing explicit exit 3 UNVERIFIED."
  </behavior>
  <action>After dependent Plans 164-03 and 164-04 have finalized their workflow edits, extend the existing D-05 Python detector rather than creating another inventory. Preserve PyYAML safe loading, `.yml`/`.yaml` discovery, sorted records, and exact workflow/job provenance, but replace every silent parse/type/name recovery with stable nonzero diagnostics. Keep a full literal-producer mode distinct from the merge-blocking candidate view so duplicate records are never collapsed. The authoritative real-tree run must scan the final workflow tree, including `requires-example-host-gate.yml` and `aggregator-negative-control.yml`, rather than a pre-change snapshot. In the bash-3.2-compatible D-06 checker, run all local syntax/type/name/cardinality validation before `gh`; expose those same complete checks as `--local-only`, which exits without invoking gh, then in normal mode compare each registered context against the full producer map exactly once and every local merge-blocking candidate against registration. Permit injected fixture registration JSON for tests, never credentials. Add hermetic fixtures for malformed YAML, non-map jobs, unnamed/dynamic required authority, duplicate authority, registered-without-producer, candidate-without-registration, and local-only credential independence; assert status, diagnostic id, source, and one remediation while preserving the bash construct guard.</action>
  <acceptance_criteria>
    - Every authority-bearing local input is either a valid literal producer record or an actionable failure; no malformed/dynamic input disappears.
    - Duplicate and missing authority negative controls fail without network or GitHub credentials.
    - `script/check_required_checks_registered.sh --local-only` proves the final workflow tree locally and never invokes gh.
    - Real branch-protection read failure is exit 3 UNVERIFIED after local checks, never success and never a suppression of local defects.
    - The unchanged green-first registrar remains the only branch-protection writer per D-06/D-07.
  </acceptance_criteria>
  <verify><automated>mix test test/crosswake/proof/phase153_1_gate_integrity_test.exs &amp;&amp; python3 script/list_merge_blocking_checks.py --emitters &gt;/dev/null &amp;&amp; script/check_required_checks_registered.sh --local-only</automated></verify>
  <done>Every registered or merge-blocking context has local, literal, exactly-one producer authority before governance credentials enter the path.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prove every intended ExUnit file belongs to a merge-blocking class</name>
  <reversibility rating="reversible">D-08 adds a repository-local detector and tests without changing ExUnit runtime contracts or public behavior.</reversibility>
  <read_first>
    - .planning/workstreams/quality-ratchet-release/phases/164-dependency-security-and-gate-authority/164-CONTEXT.md
    - test/test_helper.exs
    - .github/workflows/requires-example-host-gate.yml
    - script/list_merge_blocking_checks.py
    - test/crosswake/proof/phase153_1_gate_integrity_test.exs
  </read_first>
  <files>script/check_exunit_ownership.exs, test/crosswake/proof/phase164_exunit_ownership_test.exs</files>
  <behavior>
    - "Every live test/**/*_test.exs path with runnable tests maps to default/hermetic or requires_example_host merge-blocking ownership from executable tags."
    - "Module-level and per-test tag scopes are recognized; comments and string literals containing tag names do not create ownership."
    - "A synthetic unowned file fails with its path, effective exclusion/class, and concrete remove/move-exclusion or add-merge-lane remediation."
    - "Ownership is derived live with no retained test counts and no changes to current exclusion authority."
  </behavior>
  <action>Create `script/check_exunit_ownership.exs` under D-08. Parse each root test file with `Code.string_to_quoted` and traverse executable ExUnit module/test/tag AST; never use raw substring matching. Encode the current execution-class manifest explicitly: default/hermetic lanes cover tests not excluded by `test/test_helper.exs`, while the dedicated literal merge-blocking requires-example-host job covers executable `:requires_example_host` tests. Fail malformed source and every unowned runnable file with deterministic path, effective class/exclusion, and one remediation. Add temporary-tree positive/negative tests for default ownership, module/test tags, unowned exclusions, comment-only and string-only false positives, and empty/no-runnable-test input. The proof file itself runs in the normal root merge-blocking suite, making the live ownership command recurring without adding another workflow per D-13.</action>
  <acceptance_criteria>
    - The live repository returns zero unowned intended test files, including the example-host-tagged class.
    - Comment/string fixtures do not satisfy tag ownership, and malformed or empty executable ownership fails closed.
    - Adding a synthetic unowned `*_test.exs` produces nonzero output naming that path and its concrete remediation.
    - No test count, retry, new exclusion, weakened async posture, workflow consolidation, or Android/adopter scope is introduced.
  </acceptance_criteria>
  <verify><automated>elixir script/check_exunit_ownership.exs &amp;&amp; mix test test/crosswake/proof/phase164_exunit_ownership_test.exs</automated></verify>
  <done>Every intended ExUnit file has executable-syntax evidence of at least one merge-blocking execution owner.</done>
</task>

<task type="auto">
  <name>Task 3: Publish one credential-free aggregate Phase 164 verification command</name>
  <reversibility rating="reversible">The D-03/D-05/D-08/D-09/D-11/D-12 aggregate is orchestration over focused checks and can be adjusted without changing their authority contracts.</reversibility>
  <read_first>
    - .planning/workstreams/quality-ratchet-release/phases/164-dependency-security-and-gate-authority/164-CONTEXT.md
    - script/check_dependency_security.sh
    - script/list_merge_blocking_checks.py
    - script/check_required_checks_registered.sh
    - script/check_exunit_ownership.exs
    - script/check_example_host_isolation.sh
    - script/check_aggregator_result_semantics.py
  </read_first>
  <files>script/check_phase164_dependency_security_and_gate_authority.sh</files>
  <action>Create a bash-3.2-compatible, repository-rooted phase entry point with `set -euo pipefail` semantics and bounded section labels. Run the focused contracts in dependency-safe order: dependency security script plus its phase proof; strict `--emitters` inventory plus `check_required_checks_registered.sh --local-only` and the phase153_1 audit proof; live ExUnit ownership plus its negative-control proof; focused example-host lifecycle proof followed by the unfiltered six-run isolation matrix; then exact aggregator structural proof and the complete aggregator semantics self-test. Invoke no `gh` command, branch-protection write, credentials, adopter data, environment dump, Android work, or Phase-165 topology operation. Await every command and return nonzero at the first failed contract with the responsible section and exact corrective command.</action>
  <acceptance_criteria>
    - One command covers focused security, inventory, local audit, ownership, isolation, and aggregator checks without credentials.
    - The aggregate directly invokes every specialized authority in dependency-safe order and cannot replace or weaken its negative controls.
    - Any child failure makes the aggregate nonzero with a bounded section name and corrective command; success means every child completed.
    - The entry point performs no remote governance read/write and emits no sensitive environment or adopter information.
  </acceptance_criteria>
  <verify><automated>script/check_phase164_dependency_security_and_gate_authority.sh</automated></verify>
  <done>Phase 164 has one credential-free aggregate entry point that proves every split security and CI-authority responsibility.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| workflow YAML -> local producer records | Semi-structured authority input must parse strictly and retain provenance/cardinality. |
| branch-protection JSON -> local policy comparison | Credentialed external state is compared bidirectionally without overriding local truth. |
| test source AST -> execution-class ownership | Comments/strings are untrusted lexical content and cannot confer merge-blocking ownership. |

## STRIDE Threat Register — ASVS L1, block on high

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|---|
| T-164-01 | Spoofing / Tampering | required-check display names | high | mitigate | Task 1 retains every literal producer with provenance and rejects duplicate, unnamed, dynamic, and registered-without-producer cases. |
| T-164-03 | Tampering | malformed workflow and executable-tag input | high | mitigate | Tasks 1-2 fail parse/type errors and use YAML/Elixir parsers rather than silent recovery or lexical tag matching. |
| T-164-08 | Repudiation | unavailable governance read | medium | mitigate | Task 1 completes local proof first and preserves explicit exit 3 UNVERIFIED with the existing admin remediation. |
| T-164-SC | Tampering | package supply chain | low | accept | No package install is planned; existing PyYAML, Elixir, bash, gh, and jq tooling is reused. |
</threat_model>

<source_coverage_audit>
| Source | ID | Contract | Coverage | Status |
|---|---|---|---|---|
| GOAL | Phase 164 | Every required result has one authoritative path. | Tasks 1-2 cover check contexts and test execution owners; Task 3 composes all focused phase authorities into one credential-free entry point. | COVERED |
| REQ | CIG-01, CIG-02 | Exact producer cardinality and complete ExUnit execution ownership. | Tasks 1-2 implement the requirements; Task 3 verifies them with dependent SEC/CIG contracts. | COVERED |
| RESEARCH | strict inventory/AST ownership | Bidirectional full-producer audit, executable tag classification, and one retained phase entry point. | Tasks 1-3. | COVERED |
| CONTEXT | D-03, D-05..D-09, D-11..D-13 | One detector/governance seam, complete live-derived ownership, and dependency-safe aggregate proof. | Cited throughout executable actions. | COVERED |
| EDGE | 7 rows | CIG-01 adjacency/empty/ordering; CIG-02 adjacency/empty/ordering/concurrency. | Seven applicable rows equal seven flagged assumptions. | FLAGGED |
| PROHIBITION | 3 bespoke constraints | Single authority, syntax-based ownership, frozen adjacent scope. | Descriptor-less flagged-unverified entries retained. | FLAGGED |
</source_coverage_audit>

<verification>
Run `script/check_phase164_dependency_security_and_gate_authority.sh` as the phase-level entry point; it includes both hermetic proof files, the live strict producer emitter, local-only required-context audit, ExUnit ownership, example-host isolation matrix, and aggregator proofs. The separate credentialed comparison remains visibly UNVERIFIED when gh authority is unavailable.
</verification>

<success_criteria>
- Duplicate, missing, malformed, unnamed, and dynamic required authority fails with exact provenance.
- Registered contexts and local candidates are compared in the correct direction against the full producer map.
- Every intended test file is owned by a merge-blocking class using executable syntax, with no fixed counts.
- The one credential-free phase aggregate completes every focused security and CI-authority proof.
</success_criteria>

<output>
Create `.planning/workstreams/quality-ratchet-release/phases/164-dependency-security-and-gate-authority/164-02-SUMMARY.md` when done.
</output>
