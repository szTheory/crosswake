---
phase: 170-vacuous-assertion-remediation
plan: 03
type: execute
wave: 3
depends_on: ["170-02"]
files_modified:
  - test/crosswake/doctor/doctor_test.exs
  - test/crosswake/support_matrix/support_matrix_test.exs
  - test/crosswake/guides/evidence_manifest_test.exs
  - test/crosswake/guides/release_boundaries_test.exs
  - test/mix/tasks/crosswake_release_status_test.exs
  - test/crosswake/release_candidate/coordinate_test.exs
  - test/crosswake/release_candidate/mirror_test.exs
  - script/collection_assertion_ledger.json
autonomous: true
requirements: [VAC-02]

estimate:
  tokens: 60000
  raw_tokens: 60000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "At each covered high-blast-radius site, driving the real code path to an empty collection makes the inserted guard raise `ExUnit.AssertionError` — demonstrated by a test that asserts the raise, not by inspection (VAC-02 empty edge, D-12.2)"
    - "Each empty-input regression test also asserts that the collection under the empty-input scenario is genuinely empty, so the raise cannot be attributed to an unrelated failure"
    - "Each empty-input regression drives the real production call path (`Doctor.run/1`, `SupportMatrix.canonical/0`, the manifest reader, `Coordinate`/`Mirror`/`ReleaseStatus`) rather than a fabricated struct that no longer exercises the code under test (D-12.2)"
    - "Any named D-12.2 site that cannot be forced empty without a `lib/` change is recorded by name with a stated reason and its structural-test-only coverage, never silently dropped"
    - "`test/crosswake/release_candidate/cleanroom_test.exs` and every other release-graph test file still passes after the additions"
    - "Regenerating `script/collection_assertion_ledger.json` after these additions produces no unclassified rows"
  artifacts:
    - path: "test/crosswake/doctor/doctor_test.exs"
      provides: "Empty-input regression for the commerce-corridor findings filter"
      contains: "Enum.empty?"
    - path: "test/crosswake/support_matrix/support_matrix_test.exs"
      provides: "Empty-input regression for the release-boundary target filter"
      contains: "Enum.empty?"
    - path: "test/crosswake/release_candidate/mirror_test.exs"
      provides: "Empty-input regression for the mirror push-argument refutation"
      contains: "Enum.empty?"
  key_links:
    - from: "test/crosswake/doctor/doctor_test.exs"
      to: "lib/crosswake/doctor.ex"
      via: "Doctor.run/1 called with a route source declaring zero commerce-corridor routes"
      pattern: "Doctor\\.run"
    - from: "test/crosswake/release_candidate/mirror_test.exs"
      to: "lib/crosswake/release_candidate/mirror.ex"
      via: "the real Mirror computation driven to an empty push-argument list"
      pattern: "Mirror\\."
  prohibitions:
    - statement: "An empty-input regression must not be built on a fabricated harness that no longer exercises the real code path — a test that asserts a property of its own fixture must not be presented as proof the guard works"
      category: transparency
    - statement: "A site that cannot be forced empty without changing runtime code must not be quietly omitted from the record; the phase must state which sites are covered only structurally and why"
      category: transparency
---

<objective>
Add genuine empty-input regression tests at the runtime-derived, highest-blast-radius sites, so the
"empty now fails" property is demonstrated by execution and not only by source structure.

Purpose: D-12's proof is a hybrid matched to measured risk. The itemized structural test (plan
170-02) covers every remediated site; this plan adds real runtime proof where a site's collection is
genuinely derived from a filter, a file on disk, or a live release computation — including the three
`refute Enum.any?` sites inside Phase 175's one-way-door blast radius.

Output: empty-input regression tests inside the seven named files, a regenerated ledger, and an
explicit written record for any named site that proved unreachable without a `lib/` change.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/quality-ratchet-release/phases/170-vacuous-assertion-remediation/170-CONTEXT.md
@.planning/workstreams/quality-ratchet-release/phases/170-vacuous-assertion-remediation/170-PATTERNS.md
@.planning/workstreams/quality-ratchet-release/phases/170-vacuous-assertion-remediation/170-02-SUMMARY.md
@script/collection_assertion_remediation.json
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Empty-input regressions at the four filtered / file-derived sites</name>
  <files>test/crosswake/doctor/doctor_test.exs, test/crosswake/support_matrix/support_matrix_test.exs, test/crosswake/guides/evidence_manifest_test.exs, test/crosswake/guides/release_boundaries_test.exs</files>
  <precondition>The non-emptiness guards from plan 170-02 Task 2 are present in
  `test/crosswake/doctor/doctor_test.exs`, `test/crosswake/support_matrix/support_matrix_test.exs`,
  `test/crosswake/guides/evidence_manifest_test.exs` and
  `test/crosswake/guides/release_boundaries_test.exs`; these regressions assert those guards raise.</precondition>
  <behavior>
    - doctor: calling `Doctor.run/1` with a route source that declares zero `commerce.corridor.*`
      routes yields an empty filtered findings list, and the guard expression raises
      `ExUnit.AssertionError` on it.
    - support_matrix: filtering `matrix.release_boundaries` on a target value matched by no entry
      yields an empty list, and the guard expression raises on it.
    - evidence_manifest: calling the manifest value reader against an in-test manifest map whose
      relevant key holds an empty list yields an empty value list, and the guard expression raises
      on it.
    - release_boundaries: driving `Crosswake.ReleaseStatus.build/…` to an empty companion list
      yields an empty list and the guard raises — OR, if no override hook exists and adding one
      would require a `lib/` change, the site is recorded as structural-test-only with that reason.
  </behavior>
  <read_first>
    - test/crosswake/doctor/doctor_test.exs — the test at line ~745 that calls `Doctor.run(route_source:
      …, install_manifest_path: …, cwd: …)` (option shape at lines ~705-711), and the sibling test at
      line ~541 ("commerce_summary returns the not_applicable freshness baseline when no commerce
      routes exist") which already establishes a zero-commerce-route scenario and the router module
      that produces it. Reuse that existing router rather than fabricating a new one.
    - test/crosswake/support_matrix/support_matrix_test.exs lines 160-195 — `SupportMatrix.canonical/0`
      and the flagged filter at line ~187.
    - test/crosswake/guides/evidence_manifest_test.exs lines 100-130 — the `@example_manifest`
      attribute, `read_manifest!/1`, `manifest_values/2`, the flagged sites at lines 110-118, and the
      sibling synthetic-regression test at line ~125 showing the in-test manifest-map idiom.
    - test/crosswake/guides/release_boundaries_test.exs lines 75-110 — the test calling
      `Crosswake.ReleaseStatus.build()` and the flagged site at line ~101. Check whether a
      keyword-override arity already exists before assuming one must be added.
    - .planning/workstreams/quality-ratchet-release/phases/170-vacuous-assertion-remediation/170-PATTERNS.md
      §"Empty-input regression tests (7 existing files)" — the per-file input-construction idiom and
      the flagged scope note for release_boundaries.
    - .planning/workstreams/quality-ratchet-release/phases/170-vacuous-assertion-remediation/170-CONTEXT.md
      D-12, D-14, and the phase boundary forbidding `lib/` changes.
  </read_first>
  <action>
In each of the four named files, add one new `test` beside the flagged site — inside the same
`describe` block where one exists — named for the property, e.g. `"phase 170: an empty
commerce-corridor findings list now fails instead of passing vacuously"`.

Each regression test has the same two-part shape:
1. Build the empty-input scenario through the file's own existing input-construction idiom, driving
   the real production call path. Bind the collection using the exact expression the flagged
   assertion consumes.
2. Assert the collection is empty (so the raise cannot be mistaken for an unrelated failure), then
   assert that evaluating the guard on it raises `ExUnit.AssertionError`, using
   `assert_raise ExUnit.AssertionError, fn -> refute Enum.empty?(collection) end`.

Per-site input construction:
- `doctor_test.exs`: reuse the zero-commerce-route router module already present in this file (see
  the sibling test at line ~541) as `route_source:`, keeping the other `Doctor.run/1` options the
  flagged test uses.
- `support_matrix_test.exs`: `SupportMatrix.canonical/0` returns a fixed matrix with no injectable
  empty variant, so force the empty case by filtering `matrix.release_boundaries` on a target value
  that matches zero entries (a target string not present in the canonical matrix). Do not stub or
  replace `SupportMatrix.canonical/0`.
- `evidence_manifest_test.exs`: build the manifest as an in-test map whose relevant key holds an
  empty list and pass it to `manifest_values/2` directly, rather than reading `@example_manifest`
  from disk — the committed fixture is guaranteed non-empty by its own sibling test.
- `release_boundaries_test.exs`: first determine whether `Crosswake.ReleaseStatus.build` already
  accepts a keyword override that can drive the companion list empty. If it does, use it. If it does
  NOT, STOP at this site: this phase must not change `lib/`, so do not add an override hook and do
  not fabricate a bypass. Instead record the site in the plan SUMMARY as structural-test-only,
  naming the exact reason (no injectable override exists on the live release-status computation and
  adding one is outside this phase's boundary) and citing plan 170-02's structural test as the
  coverage that does apply. This is an explicit, reasoned disposition — never a silent omission.

Write the new tests using explicit comparisons and `Enum.empty?` rather than the four
collection-assertion shapes this phase audits, so the additions contribute no new rows to the
ledger.
  </action>
  <verify>
    <automated>mix test test/crosswake/doctor/doctor_test.exs test/crosswake/support_matrix/support_matrix_test.exs</automated>
    <fails_when>non-zero exit, or the summary line reports 0 tests</fails_when>
    <automated>mix test test/crosswake/guides/evidence_manifest_test.exs test/crosswake/guides/release_boundaries_test.exs</automated>
    <fails_when>non-zero exit, or the summary line reports 0 tests</fails_when>
    <automated>grep -c 'assert_raise ExUnit.AssertionError' test/crosswake/doctor/doctor_test.exs test/crosswake/support_matrix/support_matrix_test.exs test/crosswake/guides/evidence_manifest_test.exs</automated>
    <fails_when>non-zero exit, or any listed file reports a count of 0</fails_when>
  </verify>
  <acceptance_criteria>
    - `test/crosswake/doctor/doctor_test.exs`, `test/crosswake/support_matrix/support_matrix_test.exs`
      and `test/crosswake/guides/evidence_manifest_test.exs` each contain at least one new test whose
      name contains `phase 170` and whose body contains
      `assert_raise ExUnit.AssertionError`.
    - Each new test binds its collection from a real production call (`Doctor.run`,
      `SupportMatrix.canonical`, the manifest value reader) — `grep -n` on the new test body shows
      the production function name, not only literal data.
    - Each new test asserts the collection is empty before asserting the raise.
    - For `test/crosswake/guides/release_boundaries_test.exs`: either a new `phase 170` test exists,
      or the plan SUMMARY contains a named disposition paragraph for that site stating the reason and
      the structural-test coverage that applies. Exactly one of the two, never neither.
    - `git diff --name-only 817102b0744080d12338c26d9c488e90f6cc3dc2..HEAD -- lib` produces no output.
    - `mix format --check-formatted` exits 0.
  </acceptance_criteria>
  <done>Three or four of the named filtered/file-derived sites have executing empty-input
  regressions, and any site not reachable without a `lib/` change is recorded by name with its
  reason.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Empty-input regressions at the three release-graph refutation sites</name>
  <files>test/mix/tasks/crosswake_release_status_test.exs, test/crosswake/release_candidate/coordinate_test.exs, test/crosswake/release_candidate/mirror_test.exs</files>
  <precondition>The non-emptiness guards from plan 170-02 Task 2 are present in
  `test/mix/tasks/crosswake_release_status_test.exs`, `test/crosswake/release_candidate/coordinate_test.exs`
  and `test/crosswake/release_candidate/mirror_test.exs`.</precondition>
  <behavior>
    - release_status: driving the status computation to an empty collection at the flagged
      refutation makes the guard raise `ExUnit.AssertionError`.
    - coordinate: driving the coordinate computation to an empty companion/argument list makes the
      guard raise.
    - mirror: driving the mirror computation to an empty `push_arguments` list makes the guard raise.
  </behavior>
  <read_first>
    - test/mix/tasks/crosswake_release_status_test.exs — the flagged refutation site and the
      surrounding fixture-construction idiom (module attributes feeding the real computation).
    - test/crosswake/release_candidate/coordinate_test.exs — the `@candidate` / `@companions` module
      attributes and the `Coordinate` call they feed.
    - test/crosswake/release_candidate/mirror_test.exs lines 138-148 — the flagged
      `result.push_arguments` refutation and how `result` is produced.
    - script/collection_assertion_remediation.json — the exact frozen rows for these three files.
    - .planning/workstreams/quality-ratchet-release/phases/170-vacuous-assertion-remediation/170-CONTEXT.md
      §`<verified_ground_truth>` "Blast-radius finding", D-01, D-12.
  </read_first>
  <action>
These three files carry the release-graph refutation sites that sit inside Phase 175's one-way-door
blast radius — the reason D-01 widened the audited population beyond SEED-018's grep. Add one
empty-input regression test per file, following the same two-part shape as Task 1: build the empty
scenario through the file's own existing fixture-construction idiom driving the real
`ReleaseStatus` / `Coordinate` / `Mirror` computation, assert the collection is genuinely empty, then
assert the guard raises `ExUnit.AssertionError`.

Prefer driving the computation with an empty input (an empty companion list, an empty candidate set)
over post-hoc struct surgery: the point of the test is that the real code path can produce an empty
collection, which is exactly the condition under which the pre-fix refutation passed while asserting
nothing.

If a given site's real computation cannot produce an empty collection through any input the test
file can construct without a `lib/` change, apply the same rule as Task 1: stop, and record the site
by name in the SUMMARY with its reason and its structural-test-only coverage. Do not fabricate a
bypass, and do not substitute a struct literal that no longer exercises the computation.

Name each test with a `phase 170:` prefix so the additions are greppable as one set. Use explicit
comparisons and `Enum.empty?` rather than the four audited collection-assertion shapes.
  </action>
  <verify>
    <automated>mix test test/mix/tasks/crosswake_release_status_test.exs test/crosswake/release_candidate/coordinate_test.exs test/crosswake/release_candidate/mirror_test.exs</automated>
    <fails_when>non-zero exit, or the summary line reports 0 tests</fails_when>
    <automated>mix test test/crosswake/release_candidate/cleanroom_test.exs test/crosswake/proof/phase142_release_integrity_test.exs</automated>
    <fails_when>non-zero exit, or the summary line reports 0 tests</fails_when>
    <automated>grep -rc 'phase 170' test/mix/tasks/crosswake_release_status_test.exs test/crosswake/release_candidate/coordinate_test.exs test/crosswake/release_candidate/mirror_test.exs</automated>
    <fails_when>non-zero exit, or every listed file reports a count of 0</fails_when>
  </verify>
  <acceptance_criteria>
    - Each of the three files either contains a new test named with a `phase 170:` prefix that
      contains `assert_raise ExUnit.AssertionError`, or is named in the SUMMARY with a stated reason
      for structural-test-only coverage. Exactly one of the two per file.
    - Each new test drives the real computation — `grep -n` on the new test body shows the
      `ReleaseStatus`, `Coordinate` or `Mirror` call, not only a literal struct.
    - `test/crosswake/release_candidate/cleanroom_test.exs` passes unchanged
      (`git diff --name-only 817102b0744080d12338c26d9c488e90f6cc3dc2..HEAD -- test/crosswake/release_candidate/cleanroom_test.exs` is empty for
      this task).
    - `git diff --name-only 817102b0744080d12338c26d9c488e90f6cc3dc2..HEAD -- lib .github` produces no output.
  </acceptance_criteria>
  <done>The three release-graph refutation sites inside Phase 175's blast radius have executing
  empty-input regressions, or a named written reason why one of them does not.</done>
</task>

<task type="auto">
  <name>Task 3: Reconcile the ledger and record the measured empty-now-fails facts</name>
  <files>script/collection_assertion_ledger.json</files>
  <read_first>
    - script/collection_assertion_ledger.json — the ledger as regenerated by plan 170-02.
    - script/inventory_collection_assertions.exs — `--check` and `--emit-snapshot` behavior.
    - .planning/workstreams/quality-ratchet-release/phases/170-vacuous-assertion-remediation/170-CONTEXT.md
      D-14 and the Phase 169 D-23 precedent it cites.
  </read_first>
  <action>
Regenerate and commit `script/collection_assertion_ledger.json` so it reflects the tree after the
regression tests landed. If the additions introduced any new matching call site, classify each new
row with a site-specific rationale in the same commit — a stale ledger after this plan would mean
the phase shipped an incomplete record of its own changes.

Then run the three proof tests together and record in the plan SUMMARY the D-14 measured facts for
this plan, taken from the actual runs and never from an estimate:
- the number of empty-input regression tests added, per file;
- the number of D-12.2-named sites covered by an executing regression versus the number covered only
  by the structural test, each of the latter named with its reason;
- the total ledger row count and per-bucket composition after this plan, alongside the count
  recorded at the end of plan 170-01, with any delta explained;
- confirmation that the regenerated ledger contains zero rows with bucket `needs-fix`.

Also confirm and record that no merge-blocking guard was registered: neither
`script/check_absence_is_not_success.exs` nor `script/required_check_policy.json` nor any file under
`.github/workflows/` was modified by this phase. This is ROADMAP success criterion #3 satisfied by
construction (D-13), and the SUMMARY is where the evidence is recorded.
  </action>
  <verify>
    <automated>elixir script/inventory_collection_assertions.exs --check</automated>
    <fails_when>non-zero exit, or stdout contains "[crosswake] FAIL:"</fails_when>
    <automated>mix test test/crosswake/proof/phase170_vacuous_assertion_ledger_test.exs test/crosswake/proof/phase170_guard_expression_match_test.exs</automated>
    <fails_when>non-zero exit, or the summary line reports fewer than 11 tests, or it reports 0 tests</fails_when>
    <automated>git diff --stat 817102b0744080d12338c26d9c488e90f6cc3dc2..HEAD -- script/check_absence_is_not_success.exs script/required_check_policy.json .github lib</automated>
    <fails_when>non-zero exit, or any output line at all naming one of those paths</fails_when>
  </verify>
  <acceptance_criteria>
    - `script/collection_assertion_ledger.json` regenerates byte-identically
      (`elixir script/inventory_collection_assertions.exs --emit-snapshot | diff - script/collection_assertion_ledger.json`
      prints nothing).
    - The regenerated ledger contains zero rows with bucket `needs-fix`.
    - Every row in the regenerated ledger whose bucket is not `safe-by-construction` has a non-empty
      `rationale`.
    - The SUMMARY states, as integers taken from real runs: regressions added per file, sites covered
      by execution, sites covered only structurally (each named with a reason), ledger row total, and
      per-bucket composition.
    - The SUMMARY states explicitly that no required-check registration and no merge-blocking guard
      was added, citing the empty pinned-range git diff over the check registry, workflows and `lib/`.
  </acceptance_criteria>
  <done>The ledger is current and clean, and the phase's empty-now-fails evidence is recorded as
  measured integers with every uncovered site named.</done>
</task>

</tasks>

## Artifacts this phase produces

Created or extended by this plan:
- Empty-input regression tests named with a `phase 170:` prefix inside
  `test/crosswake/doctor/doctor_test.exs`, `test/crosswake/support_matrix/support_matrix_test.exs`,
  `test/crosswake/guides/evidence_manifest_test.exs`,
  `test/crosswake/guides/release_boundaries_test.exs`,
  `test/mix/tasks/crosswake_release_status_test.exs`,
  `test/crosswake/release_candidate/coordinate_test.exs`,
  `test/crosswake/release_candidate/mirror_test.exs`.
- `script/collection_assertion_ledger.json` — regenerated and reconciled after the additions.

Produced by sibling plans in this phase: `script/inventory_collection_assertions.exs` (module
`Crosswake.CollectionAssertionInventory`; flags `--root`, `--scope`, `--ledger`, `--emit-snapshot`,
`--emit-remediation`, `--check`); `script/collection_assertion_remediation.json`;
`test/crosswake/proof/phase170_vacuous_assertion_ledger_test.exs`;
`test/crosswake/proof/phase170_guard_expression_match_test.exs`;
`test/crosswake/proof/phase170_vacuity_taxonomy_convention_test.exs`;
`.planning/workstreams/quality-ratchet-release/VERIFICATION-CONVENTIONS.md`; the `vacuity_taxonomy`
VERIFICATION.md frontmatter field and `## Vacuity Taxonomy` body section; `169-VACUITY-TAXONOMY.md`;
`170-VACUITY-TAXONOMY.md`; `COVERAGE.md`.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test fixture → production computation | Each regression must cross into real `Doctor` / `SupportMatrix` / `ReleaseStatus` / `Coordinate` / `Mirror` code; a fixture that stops short proves nothing |
| release-graph tests → Phase 175 one-way door | These three files gate an irreversible publish; a regression that weakens them is higher-consequence than elsewhere |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-170-09 | Spoofing | empty-input regression harness | high | mitigate | Each regression must bind its collection from a named production call, asserted by a `grep -n` acceptance criterion; struct-literal substitution that bypasses the computation is explicitly forbidden and a site that cannot be driven empty is recorded rather than faked. |
| T-170-10 | Tampering | release-graph test files | high | mitigate | The three release-graph files and `cleanroom_test.exs` are run as an explicit verify step; additions are purely additive new `test` blocks and the existing assertions are left intact. |
| T-170-11 | Repudiation | uncovered sites | medium | mitigate | Any D-12.2 site not reachable without a `lib/` change must be named in the SUMMARY with its reason; the acceptance criteria require exactly one of {regression exists, named disposition} per site. |
| T-170-12 | Denial of service | suite runtime | low | accept | Seven additional fast unit tests; no network, no subprocess. |
| T-170-SC | Tampering | npm/pip/cargo installs | low | accept | This plan installs no packages and adds no dependency. |

ASVS level 1; blocking threshold `high`. Both `high` threats are dispositioned `mitigate` with
executable acceptance criteria.
</threat_model>

<verification>
- The seven named test files pass, and `cleanroom_test.exs` and
  `phase142_release_integrity_test.exs` pass unchanged.
- `elixir script/inventory_collection_assertions.exs --check` exits 0 with zero `needs-fix` rows.
- Both phase-170 proof tests pass together with 11+ tests and 0 failures.
- No file under `lib/` or `.github/` was modified by this phase.
</verification>

<success_criteria>
The "empty collection now fails" property is demonstrated by executing tests at the
runtime-derived, highest-blast-radius sites; every named D-12.2 site is either covered by an
executing regression or recorded by name with the reason it is covered structurally only; and the
ledger is current, clean, and free of `needs-fix` rows.
</success_criteria>

<output>
Create `.planning/workstreams/quality-ratchet-release/phases/170-vacuous-assertion-remediation/170-03-SUMMARY.md` when done
</output>
