---
phase: 170-vacuous-assertion-remediation
plan: 05
type: execute
wave: 4
depends_on: ["170-03", "170-04"]
files_modified:
  - .planning/workstreams/quality-ratchet-release/phases/170-vacuous-assertion-remediation/170-VACUITY-TAXONOMY.md
  - test/crosswake/proof/phase170_vacuity_taxonomy_convention_test.exs
autonomous: true
requirements: [VAC-03]

estimate:
  tokens: 40000
  raw_tokens: 40000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Phase 170's own new checks are recorded against the six-shape taxonomy with a measured non-vacuity fact per check, taken from the 170-01, 170-02 and 170-03 SUMMARYs (D-15)"
    - "Every phase VERIFICATION.md under this workstream either carries a `## Vacuity Taxonomy` section or has a sibling `NNN-VACUITY-TAXONOMY.md` addendum — a decidable, greppable answer per phase (D-16)"
    - "The convention check is a pure predicate proven capable of returning false: against a synthetic fixture directory holding a VERIFICATION.md with neither a section nor an addendum, the predicate returns false (D-14, D-16)"
    - "The convention test records the real number of phase VERIFICATION.md files it examined, and fails loudly rather than passing green if that number is zero (D-14)"
    - "No entry in any vacuity_taxonomy record is a bare yes/no tick; every entry carries a shape letter or the explicit 'matches none of A-F, because ___' form plus a measured fact (D-15)"
    - "The convention test asserts presence of the record only; it asserts nothing about collection assertions anywhere in the tree, so it is not VACG-01 in disguise (D-13, D-21, ROADMAP SC#3)"
    - "The convention check is applied at phase close through the VERIFICATION.md artifact, matching where D-17 places the obligation"
  artifacts:
    - path: ".planning/workstreams/quality-ratchet-release/phases/170-vacuous-assertion-remediation/170-VACUITY-TAXONOMY.md"
      provides: "Phase 170's own vacuity-taxonomy record"
      contains: "vacuity_taxonomy"
    - path: "test/crosswake/proof/phase170_vacuity_taxonomy_convention_test.exs"
      provides: "Decidable phase-close convention presence check with its non-vacuity proof"
      contains: "defmodule Crosswake.Proof.Phase170VacuityTaxonomyConventionTest"
  key_links:
    - from: "test/crosswake/proof/phase170_vacuity_taxonomy_convention_test.exs"
      to: ".planning/workstreams/quality-ratchet-release/VERIFICATION-CONVENTIONS.md"
      via: "asserts the convention document exists and links the taxonomy source"
      pattern: "VERIFICATION-CONVENTIONS"
    - from: "test/crosswake/proof/phase170_vacuity_taxonomy_convention_test.exs"
      to: ".planning/workstreams/quality-ratchet-release/phases"
      via: "Path.wildcard over the phase directories, then the section-or-addendum predicate per phase"
      pattern: "VERIFICATION"
  prohibitions:
    - statement: "A vacuity_taxonomy entry must not be a bare yes/no tick; a boolean asserted without inspecting what is inside it is the target defect restated at the process layer"
      category: transparency
    - statement: "The convention check must not be allowed to report success while examining zero phase artifacts — a check whose finding count is zero must fail loudly rather than pass green"
      category: transparency
---

<objective>
Record Phase 170's own checks against the six-shape taxonomy, and turn the convention from plan
170-04 into a decidable, executing check that has been proven capable of going red.

Purpose: D-16's ROSTER/DONE property — completeness is a positive assertion, never an inference from
absence. An auditor must get a decidable answer per phase, and the check that answers must itself be
proven non-vacuous, or this phase ships an instance of its own target defect.

Output: `170-VACUITY-TAXONOMY.md` and
`test/crosswake/proof/phase170_vacuity_taxonomy_convention_test.exs`.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/quality-ratchet-release/phases/170-vacuous-assertion-remediation/170-CONTEXT.md
@.planning/workstreams/quality-ratchet-release/VERIFICATION-CONVENTIONS.md
@.planning/workstreams/quality-ratchet-release/phases/169-diagnostic-legibility/169-VACUITY-TAXONOMY.md
@.planning/workstreams/quality-ratchet-release/phases/170-vacuous-assertion-remediation/170-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Record Phase 170's own checks against the six-shape taxonomy</name>
  <precondition>`.planning/workstreams/quality-ratchet-release/VERIFICATION-CONVENTIONS.md` exists
  (plan 170-04 Task 1), and the 170-01, 170-02 and 170-03 SUMMARYs carry the measured counts this
  record cites.</precondition>
  <files>.planning/workstreams/quality-ratchet-release/phases/170-vacuous-assertion-remediation/170-VACUITY-TAXONOMY.md</files>
  <read_first>
    - .planning/workstreams/quality-ratchet-release/VERIFICATION-CONVENTIONS.md — the field and
      section specification this record must satisfy exactly.
    - .planning/workstreams/quality-ratchet-release/phases/169-diagnostic-legibility/169-VACUITY-TAXONOMY.md
      — the sibling record's shape, written one plan earlier; match it.
    - .planning/workstreams/quality-ratchet-release/phases/170-vacuous-assertion-remediation/170-01-SUMMARY.md,
      170-02-SUMMARY.md and 170-03-SUMMARY.md — the measured counts. Take every number from these
      files, never from an estimate written in a PLAN.
    - .planning/research/v23/PITFALLS.md §"Pitfall 4" — to map each check to a shape letter.
    - .planning/workstreams/quality-ratchet-release/phases/170-vacuous-assertion-remediation/170-CONTEXT.md
      D-15, D-16, D-17, D-18, D-21.
  </read_first>
  <action>
Create
`.planning/workstreams/quality-ratchet-release/phases/170-vacuous-assertion-remediation/170-VACUITY-TAXONOMY.md`,
following `169-VACUITY-TAXONOMY.md`'s shape exactly: frontmatter carrying `phase`,
`addendum_to: 170-VERIFICATION.md`, `convention: VERIFICATION-CONVENTIONS.md` and the
`vacuity_taxonomy:` list, then a `## Vacuity Taxonomy` body section with entries in check-ID lexical
order.

Record one entry per check this phase landed. At minimum:
- the ledger-completeness check in `test/crosswake/proof/phase170_vacuous_assertion_ledger_test.exs`;
- the guard-presence-and-expression-match check in
  `test/crosswake/proof/phase170_guard_expression_match_test.exs`;
- the convention-presence check added in Task 2 of this plan;
- the empty-input regression set added by plan 170-03, recorded as one entry naming the count of
  sites it covers.

For each entry, `non_vacuity_evidence` must be a measured fact taken from the corresponding SUMMARY:
the audited row count and per-bucket composition for the ledger check; the frozen remediation row
count plus the two demonstrated RED paths (missing guard, wrong-variable guard) for the structural
check; the number of phase artifacts examined plus the synthetic-fixture RED demonstration for the
convention check; the number of executing regressions and the number of sites covered structurally
only for the regression set.

Per D-15, never a bare yes/no tick — a boolean asserted without inspecting what is inside it is
Shape A restated at the process layer. Per D-17, state that this record is the phase-close artifact
the verifier reads, not a plan-time or PR-time judgment. Per D-18, link
`.planning/research/v23/PITFALLS.md` §"Pitfall 4" by relative path and do not restate any shape's
definition. Per D-21, close with a short paragraph noting that when VACG-01 lands it supersedes
Shape A only, so the field narrows to the five remaining shapes rather than being retired.

If any check genuinely maps to none of Shapes A-F, use the explicit escape form
`matches none of A-F, because <reason>` rather than forcing a letter.
  </action>
  <verify>
    <automated>grep -c 'non_vacuity_evidence' .planning/workstreams/quality-ratchet-release/phases/170-vacuous-assertion-remediation/170-VACUITY-TAXONOMY.md</automated>
    <fails_when>non-zero exit, or the printed count is below 4 (one entry per check this phase landed)</fails_when>
    <automated>grep -c 'PITFALLS.md' .planning/workstreams/quality-ratchet-release/phases/170-vacuous-assertion-remediation/170-VACUITY-TAXONOMY.md</automated>
    <fails_when>non-zero exit, or the printed count is 0 (the taxonomy must be linked, not restated)</fails_when>
    <automated>grep -nE '[0-9]+' .planning/workstreams/quality-ratchet-release/phases/170-vacuous-assertion-remediation/170-VACUITY-TAXONOMY.md</automated>
    <fails_when>non-zero exit, or no line printed (a record with no numeral carries no measured fact)</fails_when>
  </verify>
  <acceptance_criteria>
    - The file's frontmatter contains `addendum_to: 170-VERIFICATION.md` and a `vacuity_taxonomy:`
      list with at least 4 entries.
    - Every entry has `check_id`, `shape` (a single letter A-F or the literal escape form
      `matches none of A-F, because`), and `non_vacuity_evidence`.
    - Every `non_vacuity_evidence` value contains at least one numeral, and each numeral is traceable
      to a stated figure in `170-01-SUMMARY.md`, `170-02-SUMMARY.md` or `170-03-SUMMARY.md` — the
      SUMMARY it came from is named in the entry.
    - No entry's `non_vacuity_evidence` is `yes`, `no`, `true`, `false`, `checked` or `n/a`.
    - Every `check_id` resolves to a real test module or test name
      (`grep -rn "<check_id>" test` returns at least one match per ID).
    - The file contains no definition of any of Shapes A through F and links
      `.planning/research/v23/PITFALLS.md`.
    - The closing paragraph states the VACG-01 narrowing rule.
  </acceptance_criteria>
  <done>Phase 170's own checks each carry a shape mapping and a measured non-vacuity fact traced to
  a named SUMMARY, in a record shaped exactly like Phase 169's.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Make the convention decidable with an executing, proven-red presence check</name>
  <files>test/crosswake/proof/phase170_vacuity_taxonomy_convention_test.exs</files>
  <precondition>`.planning/workstreams/quality-ratchet-release/VERIFICATION-CONVENTIONS.md`,
  `169-VACUITY-TAXONOMY.md` and `170-VACUITY-TAXONOMY.md` all exist.</precondition>
  <behavior>
    - Given a phase directory whose `NNN-VERIFICATION.md` contains a `## Vacuity Taxonomy` section →
      the predicate returns true.
    - Given a phase directory whose `NNN-VERIFICATION.md` lacks the section but whose directory holds
      a sibling `NNN-VACUITY-TAXONOMY.md` → the predicate returns true.
    - Given a synthetic fixture phase directory with a `NNN-VERIFICATION.md` and neither the section
      nor an addendum → the predicate returns false and the phase is named in the failure message.
    - Given zero phase directories containing a VERIFICATION.md → the test fails loudly rather than
      reporting success over an empty world.
    - Given a missing `VERIFICATION-CONVENTIONS.md` → the convention-document test fails.
  </behavior>
  <read_first>
    - test/crosswake/proof/phase169_check_name_uniqueness_test.exs lines 220-260 — the
      pure-predicate-extraction plus paired synthetic-fixture non-vacuity pattern to mirror.
    - .planning/workstreams/quality-ratchet-release/VERIFICATION-CONVENTIONS.md — the rule this test
      makes decidable, including the exact heading text and the verbatim null statement.
    - .planning/workstreams/quality-ratchet-release/phases/169-diagnostic-legibility/169-VERIFICATION.md
      and .../169-VACUITY-TAXONOMY.md — the one real phase artifact pair this check examines today.
    - .planning/workstreams/quality-ratchet-release/phases/170-vacuous-assertion-remediation/170-CONTEXT.md
      D-13, D-14, D-16, D-17, D-20, D-21.
    - script/collection_assertion_ledger.json — to confirm afterwards that this new test contributes
      no unclassified rows.
  </read_first>
  <action>
Create `test/crosswake/proof/phase170_vacuity_taxonomy_convention_test.exs`, module
`Crosswake.Proof.Phase170VacuityTaxonomyConventionTest`.

Extract the rule as a pure predicate over a directory listing plus file contents —
`taxonomy_recorded?(phase_dir_entries, verification_source)` — so a synthetic fixture can prove it
capable of returning false rather than resting on "it currently passes against a currently-compliant
tree". The predicate returns true when the verification source contains the `## Vacuity Taxonomy`
heading, OR the directory entries include a file matching `*-VACUITY-TAXONOMY.md`.

Tests to write:
1. **Convention document exists and single-sources the taxonomy** — assert
   `.planning/workstreams/quality-ratchet-release/VERIFICATION-CONVENTIONS.md` exists, contains
   `vacuity_taxonomy`, contains the verbatim null statement `This phase landed no new checks.`, and
   links `.planning/research/v23/PITFALLS.md`.
2. **Every phase artifact is compliant** — walk
   `.planning/workstreams/quality-ratchet-release/phases/*/` with `Path.wildcard/1 |> Enum.sort/1`,
   select the directories containing a `*-VERIFICATION.md`, apply the predicate to each, collect the
   non-compliant directory names, and assert the collected list is empty with a message naming each
   offender and pointing the reader at `VERIFICATION-CONVENTIONS.md`.
3. **Findings floor (D-14)** — assert the number of phase directories examined in test 2 is at least
   1, in a separate test with its own message, so a wildcard that silently stops matching cannot turn
   this check into a green no-op. Record the real examined count in the plan SUMMARY.
4. **Non-vacuity control (D-14)** — build a `@tag :tmp_dir` fixture directory containing a
   `999-VERIFICATION.md` with neither the heading nor a sibling addendum, apply the predicate, and
   assert it returns false.
5. **Positive control for the addendum branch** — build a second fixture directory containing a
   `998-VERIFICATION.md` without the heading plus a `998-VACUITY-TAXONOMY.md`, and assert the
   predicate returns true, so the two branches are each exercised rather than one covering for the
   other.

Per D-13 and D-21, this check asserts presence of the phase-close record only. It must not scan for
collection assertions, must not evaluate the quality of an entry's evidence, and must not be added
to `script/check_absence_is_not_success.exs` or to any required-check registry — it is an ordinary
`mix test` entry. Per D-20, change no file under `lib/`, `script/` or `.github/workflows/`.

Write this module's own assertions with explicit comparisons and per-item `Enum.each` asserts rather
than the four collection-assertion shapes this phase audits, so the file contributes no rows to the
ledger. After the file lands, regenerate the ledger and confirm it is unchanged.
  </action>
  <verify>
    <automated>mix test test/crosswake/proof/phase170_vacuity_taxonomy_convention_test.exs</automated>
    <fails_when>non-zero exit, or the summary line reports fewer than 5 tests, or it reports 0 tests</fails_when>
    <automated>elixir script/inventory_collection_assertions.exs --emit-snapshot | diff - script/collection_assertion_ledger.json</automated>
    <fails_when>non-zero exit, or any diff hunk printed on stdout (the new test file must contribute no unclassified rows)</fails_when>
    <automated>mix test test/crosswake/proof/phase170_vacuous_assertion_ledger_test.exs test/crosswake/proof/phase170_guard_expression_match_test.exs test/crosswake/proof/phase170_vacuity_taxonomy_convention_test.exs</automated>
    <fails_when>non-zero exit, or the summary line reports fewer than 16 tests, or it reports 0 tests</fails_when>
    <automated>git diff --name-only 817102b0744080d12338c26d9c488e90f6cc3dc2..HEAD -- lib .github</automated>
    <fails_when>non-zero exit, or any output line at all</fails_when>
  </verify>
  <acceptance_criteria>
    - `mix test test/crosswake/proof/phase170_vacuity_taxonomy_convention_test.exs` reports 5 or more
      tests and 0 failures.
    - The module contains a `defp taxonomy_recorded?` predicate, at least one test asserting it
      returns false for a synthetic non-compliant fixture, and at least one asserting it returns true
      for a synthetic addendum-only fixture.
    - The module contains a separate findings-floor test asserting the examined phase-directory count
      is at least 1, and the SUMMARY records the real count observed.
    - `grep -c 'check_absence_is_not_success\|required_check_policy' test/crosswake/proof/phase170_vacuity_taxonomy_convention_test.exs`
      is 0 — this check is not wired into the merge-blocking registry.
    - Regenerating the ledger produces a byte-identical file.
    - `git diff --name-only 817102b0744080d12338c26d9c488e90f6cc3dc2..HEAD -- lib .github` produces no output.
  </acceptance_criteria>
  <done>The phase-close vacuity-taxonomy convention is enforced by an executing check that has been
  demonstrated returning false for a non-compliant phase and true for the addendum branch, with its
  examined-artifact count recorded.</done>
</task>

</tasks>

## Artifacts this phase produces

Created or extended by this plan:
- `.planning/workstreams/quality-ratchet-release/phases/170-vacuous-assertion-remediation/170-VACUITY-TAXONOMY.md`
  — Phase 170's own vacuity-taxonomy record.
- `test/crosswake/proof/phase170_vacuity_taxonomy_convention_test.exs` — module
  `Crosswake.Proof.Phase170VacuityTaxonomyConventionTest`; predicate `taxonomy_recorded?/2`.

Produced by sibling plans in this phase: `script/inventory_collection_assertions.exs` (module
`Crosswake.CollectionAssertionInventory`; functions `run/2`, `rows/2`, `classify/2`,
`normalize_expression/1`, `row_key/3`, `render_snapshot/2`, `render_remediation/1`; CLI flags
`--root`, `--scope`, `--ledger`, `--emit-snapshot`, `--emit-remediation`, `--check`);
`script/collection_assertion_ledger.json`; `script/collection_assertion_remediation.json`;
`test/crosswake/proof/phase170_vacuous_assertion_ledger_test.exs`;
`test/crosswake/proof/phase170_guard_expression_match_test.exs`; the empty-input regression tests
prefixed `phase 170:` in the seven remediated test files;
`.planning/workstreams/quality-ratchet-release/VERIFICATION-CONVENTIONS.md`; the `vacuity_taxonomy`
VERIFICATION.md frontmatter key (`check_id`, `shape`, `also_shapes`, `non_vacuity_evidence`) and the
`## Vacuity Taxonomy` body section;
`.planning/workstreams/quality-ratchet-release/phases/169-diagnostic-legibility/169-VACUITY-TAXONOMY.md`;
`.planning/workstreams/quality-ratchet-release/phases/170-vacuous-assertion-remediation/COVERAGE.md`;
the ROADMAP `## Milestone Conventions` section; the shape vocabulary `assert_all`, `assert_any`,
`refute_any`, `refute_all`; the bucket vocabulary `safe-by-construction`,
`safe-compile-time-literal`, `safe-cardinality-pinned`, `safe-guarded`, `needs-fix`.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| planning artifact tree → convention check | The check's input is a glob over `.planning/` phase directories; a glob that stops matching turns the check into a silent no-op |
| convention check → merge-blocking registry | The check must stay an ordinary test and must not become a registered required check in this milestone |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-170-17 | Tampering | convention-check glob | high | mitigate | A separate findings-floor test asserts the examined phase-directory count is at least 1, and the real count is recorded in the SUMMARY, so a silently non-matching wildcard fails instead of passing green. |
| T-170-18 | Spoofing | `vacuity_taxonomy` entries | medium | mitigate | Acceptance criteria reject `yes`/`no`/`true`/`false`/`checked`/`n/a` as evidence values and require every numeral to be traceable to a named SUMMARY. |
| T-170-19 | Elevation of privilege | check registration | medium | mitigate | A grep acceptance criterion asserts the new test references neither the absence scanner nor the required-check policy, and the pinned-range git diff over `lib` and `.github` is asserted empty. |
| T-170-20 | Repudiation | Phase 170 self-record | low | accept | The record cites the SUMMARY each figure came from; deeper provenance is carried by git history. |
| T-170-SC | Tampering | npm/pip/cargo installs | low | accept | This plan installs no packages and adds no dependency. |

ASVS level 1; blocking threshold `high`. The single `high` threat is dispositioned `mitigate` with a
dedicated executing test.
</threat_model>

<verification>
- `mix test test/crosswake/proof/phase170_vacuity_taxonomy_convention_test.exs` runs 5+ tests,
  0 failures, including both a proven-false and a proven-true synthetic fixture.
- All three phase-170 proof test files run together with 16+ tests and 0 failures.
- The ledger regenerates byte-identically after the new test file lands.
- `git diff --name-only 817102b0744080d12338c26d9c488e90f6cc3dc2..HEAD -- lib .github` is empty.
- The SUMMARY records the number of phase directories the convention check examined.
</verification>

<success_criteria>
Every phase artifact in this workstream gives a decidable answer to "was each new check measured
against the six-shape vacuity taxonomy", Phase 170's own checks carry shape mappings and measured
non-vacuity facts, and the check that enforces this has been demonstrated going red — with no
merge-blocking guard and no required-check registration added anywhere in this milestone.
</success_criteria>

<output>
Create `.planning/workstreams/quality-ratchet-release/phases/170-vacuous-assertion-remediation/170-05-SUMMARY.md` when done
</output>
