---
phase: 166-clean-checkout-engineering-quality
plan: 05
type: execute
wave: 4
depends_on: [166-03, 166-04]
files_modified:
  - script/repository_verification_stages.json
  - script/ci_leaf_manifest.json
  - script/check_ci_leaf_manifest.py
  - .github/workflows/crosswake-ci.yml
  - test/crosswake/proof/phase166_repository_quality_test.exs
  - script/check_phase166_clean_checkout_engineering_quality.sh
autonomous: true
requirements: [ENG-01, ENG-03, ENG-04]
estimate:
  tokens: 30000
  raw_tokens: 30000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "Every supported local stage has one literal CI owner invoking the same behavioral command, and missing, extra, copied, or divergent ownership fails with an exact correction per D-06."
    - "The existing 44 proof leaves, `classify-change`, static umbrella needs, and `Crosswake CI` authority remain literal and fail closed per D-23."
    - "Generated drift no longer stages the worktree, and browser owners select the explicit zero-retry/fresh-server repository mode per D-18 and D-19."
    - "One credential-free recurring command protects Phase 166 structure, behavior, artifact, ownership, and output contracts without becoming a second CI engine."
  artifacts:
    - path: "script/check_phase166_clean_checkout_engineering_quality.sh"
      provides: "recurring credential-free Phase 166 contract gate"
    - path: "script/check_ci_leaf_manifest.py"
      provides: "bidirectional local-stage/CI-owner parity validation"
    - path: ".github/workflows/crosswake-ci.yml"
      provides: "existing literal owners wired to shared repository stage commands"
  key_links:
    - from: "script/check_ci_leaf_manifest.py"
      to: "script/repository_verification_stages.json"
      via: "closed stage owner and argv parity"
      pattern: "repository_verification_stages"
    - from: "script/repository_verification_stages.json"
      to: ".github/workflows/crosswake-ci.yml"
      via: "literal owner IDs invoke the same fixed stage facade"
      pattern: "verify_repository.sh --stage"
    - from: "script/check_phase166_clean_checkout_engineering_quality.sh"
      to: "test/crosswake/proof/phase166_repository_quality_test.exs"
      via: "composes recurring structural and behavioral contracts"
      pattern: "phase166_repository_quality_test"
---

<objective>
Wire the supported stage inventory into the existing literal CI authority and publish one recurring Phase 166 contract gate.

Purpose: Prevent local/CI command drift while preserving Phase 165's fail-closed ownership graph.
Output: Atomic stage-owner parity, non-staging generated proof, deterministic browser owners, and aggregate gate.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/quality-ratchet-release/phases/166-clean-checkout-engineering-quality/166-03-SUMMARY.md
@.planning/workstreams/quality-ratchet-release/phases/166-clean-checkout-engineering-quality/166-04-SUMMARY.md
@.planning/workstreams/quality-ratchet-release/phases/165-efficient-and-maintainable-ci/165-13-SUMMARY.md
@script/ci_leaf_manifest.json
@script/check_ci_leaf_manifest.py
@.github/workflows/crosswake-ci.yml
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend the existing authority validator with bidirectional stage parity</name>
  <read_first>
    - script/repository_verification_stages.json
    - script/ci_leaf_manifest.json
    - script/check_ci_leaf_manifest.py
    - .github/workflows/crosswake-ci.yml
    - test/crosswake/proof/phase165_ci_policy_test.exs
    - test/crosswake/proof/phase166_repository_quality_test.exs
  </read_first>
  <files>script/repository_verification_stages.json, script/ci_leaf_manifest.json, script/check_ci_leaf_manifest.py, test/crosswake/proof/phase166_repository_quality_test.exs</files>
  <behavior>
    - Every stage resolves to a literal tracked workflow owner and the exact fixed facade invocation; every declared owner resolves back to one supported stage.
    - A missing owner, extra owner, changed argv/cwd/env, duplicate stage, or command copied outside the shared facade fails with stage ID and one correction.
    - Existing proof/control/static-needs cardinality and `Crosswake CI` display authority remain exact.
  </behavior>
  <action>Start RED with mutations of the production manifests/workflow fixture, then extend `check_ci_leaf_manifest.py` rather than create a second authority validator. Validate the closed stage manifest, map each stage to literal `ci_owners`, and require owners to invoke the same `script/verify_repository.sh --stage <id>` behavioral entrypoint with matching required environment. Keep the existing `ci_leaf_manifest.json` top-level identity schema and all 44 leaves plus `classify-change` unchanged unless a genuinely new literal owner is required; if a new owner is required, add its leaf, workflow producer, static umbrella need, remediation, and tests atomically. Reject copied raw stage commands so local and CI cannot drift. Preserve the `Crosswake CI` name and no-checkout umbrella per D-06 and D-23.</action>
  <acceptance_criteria>
    - Stage-to-owner and owner-to-stage parity is exact and non-vacuous.
    - Direct mutations for missing/extra/divergent/copied commands fail through the production validator.
    - The existing 44 leaves and one classifier control remain unchanged unless the atomic new-owner rule is exercised and justified.
    - No CI permission, branch protection, release workflow, advisory ownership, or Android product scope changes.
  </acceptance_criteria>
  <verify>
    <automated>python3 script/check_ci_leaf_manifest.py --self-test &amp;&amp; mix test test/crosswake/proof/phase166_repository_quality_test.exs --only ci_parity</automated>
    <fails_when>non-zero exit, zero selected ExUnit tests, or manifest self-test output omits its pass marker</fails_when>
  </verify>
  <reversibility rating="reversible">Structural parity fields can be reverted without changing live branch protection or published interfaces.</reversibility>
  <done>One existing validator proves the fixed local stage contract and literal CI ownership in both directions.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Switch generated and browser CI owners to shared stage commands atomically</name>
  <read_first>
    - .github/workflows/crosswake-ci.yml
    - script/repository_verification_stages.json
    - script/ci_leaf_manifest.json
    - script/check_ci_leaf_manifest.py
    - examples/phoenix_host/playwright.config.ts
    - script/repository_artifact_policy.json
  </read_first>
  <files>.github/workflows/crosswake-ci.yml, script/repository_verification_stages.json, script/ci_leaf_manifest.json, script/check_ci_leaf_manifest.py, test/crosswake/proof/phase166_repository_quality_test.exs</files>
  <behavior>
    - `guard-02-generate-and-diff` invokes the shared generated-contract stage and contains no staging/index mutation in behavior or remediation.
    - `e2e-proof` and `route-tour-proof` invoke shared browser stage behavior with explicit repository mode and zero retries.
    - Current leaf IDs/display names, evidence paths/schemas, and umbrella authority remain byte/semantically compatible.
  </behavior>
  <action>Replace the generated-drift job's inline regeneration/staging sequence and remediation with the shared non-staging generated-contract stage while keeping literal identity `guard-02-generate-and-diff`. Switch existing browser owners to the shared stage command/environment that selects `CROSSWAKE_REPOSITORY_VERIFY=1`; preserve their distinct selectors/evidence responsibilities instead of collapsing them. Reuse stage command records for root, example host, iOS, Android, format, warnings, and cleanliness wherever existing literal owners already match, without building a dynamic CI scheduler. Retain Phase 165 runner placement, cache identity, timeouts, leaf IDs/names, artifacts, static needs, and `Crosswake CI`. Update active comments invariant-first; historical phase paths remain provenance per D-23 and D-24.</action>
  <acceptance_criteria>
    - No active generated-drift behavior or remediation stages the Git index.
    - Browser owners run first-attempt fresh-server repository behavior and retain existing selectors/evidence.
    - Every supported stage passes bidirectional parity with its literal owner(s).
    - Workflow syntax, 44-leaf authority (or an explicitly justified atomic addition), one classifier control, and static umbrella parity pass.
  </acceptance_criteria>
  <verify>
    <automated>actionlint .github/workflows/crosswake-ci.yml &amp;&amp; python3 script/check_ci_leaf_manifest.py --self-test &amp;&amp; mix test test/crosswake/proof/phase166_repository_quality_test.exs --only ci_parity --only generated_contracts</automated>
    <fails_when>non-zero exit, zero selected ExUnit tests, or actionlint emits any diagnostic</fails_when>
  </verify>
  <reversibility rating="reversible">Existing protected identities stay fixed; only their shared behavioral invocation changes.</reversibility>
  <done>CI and local verification consume one behavioral command source without index mutation or authority drift.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Publish the recurring Phase 166 quality gate</name>
  <read_first>
    - script/check_phase165_efficient_ci.sh
    - script/verify_repository.sh
    - test/js/repository_verification.test.mjs
    - test/js/playwright_repository_mode.test.mjs
    - test/crosswake/proof/phase166_repository_quality_test.exs
    - .planning/workstreams/quality-ratchet-release/phases/166-clean-checkout-engineering-quality/166-VALIDATION.md
  </read_first>
  <files>script/check_phase166_clean_checkout_engineering_quality.sh, test/crosswake/proof/phase166_repository_quality_test.exs</files>
  <behavior>
    - One credential-free command composes runner self-tests, Playwright config tests, repository-quality ExUnit, CI parity, Phase 165 authority, and actionlint.
    - It reports stable purpose sections and one exact remediation without stale phase labels in active user-facing headings.
    - It performs no external trust write, live branch-protection operation, package install, or complete dirty-tree claim.
  </behavior>
  <action>Create the recurring gate in the strict root-resolution and bounded failure style of `script/check_phase165_efficient_ci.sh`, using purpose-led active headings and Phase 166 only as secondary provenance per D-24. Compose `script/verify_repository.sh --self-test`, both Node test files, the Phase 166 ExUnit test, `python3 script/check_ci_leaf_manifest.py --self-test`, `script/check_phase165_efficient_ci.sh`, and actionlint for the touched workflow. Keep canonical full-run evidence out of this in-place gate; Plan 06 owns the isolated exact-commit run. Emit one safe root-relative correction per section and no contents, credentials, adopter facts, or environment dump.</action>
  <acceptance_criteria>
    - The aggregate checks every recurring ENG-01 through ENG-04 contract without credentials or global installs.
    - Phase 165 authority and changed-workflow syntax remain green.
    - Output is purpose-led, bounded, color-independent, and names one correction per failure.
    - No complete-run PASS is emitted from the existing dirty checkout.
  </acceptance_criteria>
  <verify>
    <automated>script/check_phase166_clean_checkout_engineering_quality.sh</automated>
    <fails_when>non-zero exit, missing final PASS marker, or any section reports FAIL/BLOCKED</fails_when>
  </verify>
  <reversibility rating="reversible">The aggregate is a credential-free composition of existing checks.</reversibility>
  <done>One recurring command protects stage, artifact, ownership, CI, and failure-output contracts.</done>
</task>

</tasks>

<threat_model>
Security posture: OWASP ASVS Level 1; every high or critical threat blocks execution until its listed mitigation passes.

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local stage records → tracked CI workflow | Divergent ownership could spoof parity or bypass proof. |
| PR workflow → required umbrella | Leaf identity and result semantics are merge authority. |
| generated/browser commands → Git/process state | Inline commands can stage unrelated files or reuse uncontrolled services. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-166-17 | Spoofing | local/CI stage owners | high | mitigate | Bidirectional literal parity and mutation self-tests. |
| T-166-18 | Tampering | generated drift CI owner | high | mitigate | Shared explicit-path byte comparison; no index writes. |
| T-166-19 | Elevation of privilege | repository/CI authority | high | mitigate | Read-only PR permissions, preserved umbrella, no branch-protection or release write. |
| T-166-20 | Repudiation | divergent CI ownership | high | mitigate | Exact manifest/workflow/static-needs/producer checks with one correction. |
</threat_model>

## Artifacts this phase produces

- `script/check_phase166_clean_checkout_engineering_quality.sh`: recurring credential-free repository-quality gate and purpose section IDs.
- `script/check_ci_leaf_manifest.py`: local-stage schema/parity validation and mutation self-tests.
- `script/repository_verification_stages.json`: finalized literal `ci_owners` and shared CI invocation records.
- `.github/workflows/crosswake-ci.yml`: shared stage invocations under preserved `guard-02-generate-and-diff`, `e2e-proof`, `route-tour-proof`, native/root/host/format/warnings/cleanliness owners and unchanged `Crosswake CI` authority.
- `test/crosswake/proof/phase166_repository_quality_test.exs`: tag `:ci_parity` and protected-identity/non-staging/browser-owner assertions.

<verification>
Run the recurring Phase 166 gate and the proven Phase 165 aggregate. Every workflow and manifest mutation must remain local/read-only with respect to GitHub authority.
</verification>

<success_criteria>
Supported stage behavior cannot drift between local and CI owners, and one recurring command reports any regression concisely without weakening existing authority.
</success_criteria>

<output>
Create `.planning/workstreams/quality-ratchet-release/phases/166-clean-checkout-engineering-quality/166-05-SUMMARY.md` when done.
</output>
