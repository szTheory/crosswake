---
phase: 166-clean-checkout-engineering-quality
plan: 04
type: execute
wave: 3
depends_on: [166-02]
files_modified:
  - .planning/workstreams/quality-ratchet-release/phases/166-clean-checkout-engineering-quality/166-ownership-ledger.md
  - examples/phoenix_host/playwright.config.ts
  - test/js/playwright_repository_mode.test.mjs
autonomous: true
requirements: [ENG-02, ENG-04]
estimate:
  tokens: 22000
  raw_tokens: 22000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Every non-planning path from the mechanical v22 base through final Phase 166 has an evidence-backed owner and disposition, and expansion stops at the last direct ownership edge per D-07 and D-08."
    - "No branch is removed without supported-entrypoint, workflow/config, dependency, dynamic-dispatch, focused regression, and full-gate evidence per D-09."
    - "Literal proof identity, platform implementations, fixtures, validators, and sentinels are not mislabeled duplicate, while unproven fallbacks are retained per D-10 and D-11."
    - "Browser repository mode is first-attempt and fresh-server deterministic without changing ordinary local behavior or widening Android per D-12 and D-19."
  artifacts:
    - path: ".planning/workstreams/quality-ratchet-release/phases/166-clean-checkout-engineering-quality/166-ownership-ledger.md"
      provides: "mechanical v22 candidate and direct-edge disposition ledger"
    - path: "test/js/playwright_repository_mode.test.mjs"
      provides: "behavioral repository-mode browser configuration proof"
  key_links:
    - from: "166-ownership-ledger.md"
      to: "git diff --name-only -z 8383aaea2a2b2e10bbe61dd843b51f4129a5d447"
      via: "reproducible candidate enumeration through final tree"
      pattern: "8383aaea2a2b2e10bbe61dd843b51f4129a5d447"
    - from: "examples/phoenix_host/playwright.config.ts"
      to: "script/repository_verification_stages.json"
      via: "explicit repository verification environment selects zero retries and no server reuse"
      pattern: "CROSSWAKE_REPOSITORY_VERIFY"
---

<objective>
Audit the bounded v22 ownership cone and remove only demonstrated misleading behavior, including nondeterministic repository-mode browser fallback.

Purpose: Satisfy engineering-quality cleanup without turning Phase 166 into broad historical refactoring.
Output: Complete ownership ledger and behavioral Playwright repository-mode contract.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/quality-ratchet-release/phases/166-clean-checkout-engineering-quality/166-CONTEXT.md
@.planning/workstreams/quality-ratchet-release/phases/166-clean-checkout-engineering-quality/166-RESEARCH.md
@.planning/workstreams/quality-ratchet-release/phases/165-efficient-and-maintainable-ci/165-13-SUMMARY.md
@examples/phoenix_host/playwright.config.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Record the complete evidence-bounded v22 ownership cone</name>
  <read_first>
    - AGENTS.md
    - .planning/workstreams/quality-ratchet-release/phases/166-clean-checkout-engineering-quality/166-CONTEXT.md
    - .planning/workstreams/quality-ratchet-release/phases/166-clean-checkout-engineering-quality/166-RESEARCH.md
    - .planning/workstreams/quality-ratchet-release/phases/164-dependency-security-and-gate-authority/164-05-SUMMARY.md
    - .planning/workstreams/quality-ratchet-release/phases/165-efficient-and-maintainable-ci/165-13-SUMMARY.md
  </read_first>
  <files>.planning/workstreams/quality-ratchet-release/phases/166-clean-checkout-engineering-quality/166-ownership-ledger.md</files>
  <action>Regenerate the NUL-safe non-planning candidate set from base `8383aaea2a2b2e10bbe61dd843b51f4129a5d447` through the current final tree rather than copying the research count. Write one row per candidate with exact columns `candidate | evidence | owner | disposition`. For each direct caller/import/include/generator/mutator/test/shared-authority edge, add a separate expansion row containing the evidence and owner, then stop when no direct edge remains per D-07 and D-08. Allowed dispositions are `retained`, `changed`, `removed-with-proof`, and `unproven-retained`. Apply D-09's full removal evidence, D-10's same-invariant/same-I/O/same-authority/same-failure test, D-11's migration-only supported-caller/current-policy proof, and D-12's responsibility/side-effect/trust-boundary extraction test. Do not edit candidate source in this task, infer adopter facts, or treat phase labels/file length/text similarity as evidence. Carry FA-ENG-02 as unresolved in the ledger header.</action>
  <acceptance_criteria>
    - Candidate enumeration is mechanically reproducible from the exact base and final commit/tree.
    - Every candidate and every direct expansion has evidence, owner, and one closed disposition.
    - No row is removed/changed solely for style, age, file length, textual similarity, or static reachability.
    - Every uncertain dead/duplicate/fallback candidate is `unproven-retained`, not silently deleted.
  </acceptance_criteria>
  <verify>
    <automated>python3 -c 'from pathlib import Path; p=Path(".planning/workstreams/quality-ratchet-release/phases/166-clean-checkout-engineering-quality/166-ownership-ledger.md"); s=p.read_text(); assert "candidate | evidence | owner | disposition" in s; assert "8383aaea2a2b2e10bbe61dd843b51f4129a5d447" in s; assert "FA-ENG-02" in s'</automated>
    <fails_when>non-zero exit or any required ledger header/base/flag assertion is absent</fails_when>
  </verify>
  <reversibility rating="reversible">The ledger records evidence and makes no source deletion.</reversibility>
  <done>The entire bounded ownership cone is explicit, evidence-backed, and closed without broad cleanup.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Make Playwright repository proof first-attempt and fresh-server deterministic</name>
  <read_first>
    - examples/phoenix_host/playwright.config.ts
    - examples/phoenix_host/package.json
    - examples/phoenix_host/package-lock.json
    - .github/workflows/crosswake-ci.yml
    - script/repository_verification_stages.json
    - .planning/workstreams/quality-ratchet-release/phases/166-clean-checkout-engineering-quality/166-ownership-ledger.md
  </read_first>
  <files>examples/phoenix_host/playwright.config.ts, test/js/playwright_repository_mode.test.mjs, .planning/workstreams/quality-ratchet-release/phases/166-clean-checkout-engineering-quality/166-ownership-ledger.md</files>
  <behavior>
    - Explicit repository-verification mode forces zero retries, no existing-server reuse, and deterministic invocation-owned output directories.
    - Existing ordinary local and CI configuration semantics remain unchanged until the CI owner is atomically switched in Plan 05.
    - Configuration tests import/evaluate the real config and assert outcomes rather than search source text.
  </behavior>
  <action>Start RED with a dependency-free Node test that evaluates the real Playwright config under repository mode, normal local mode, and current CI mode. Add explicit `CROSSWAKE_REPOSITORY_VERIFY=1` behavior that selects zero retries, `reuseExistingServer: false`, and invocation-owned report/result/artifact roots supplied by the runner. Preserve sequential workers, service-worker blocking, projects, tests, database/server ownership, and ordinary local behavior. Do not use `CI=1` as the repository determinism switch because it currently enables retry semantics. Record the config path as `changed` with behavioral evidence in the ownership ledger, and retain all protected `e2e-proof`/`route-tour-proof` identities per D-23.</action>
  <acceptance_criteria>
    - Repository mode is always zero-retry and fresh-server, independent of generic CI truthiness.
    - Normal local behavior remains compatible and current CI behavior is not silently changed before Plan 05.
    - Output paths are explicit and invocation-owned; no global browser state is deleted.
    - The ledger records the exact evidence, owner, and changed disposition.
  </acceptance_criteria>
  <verify>
    <automated>node --test test/js/playwright_repository_mode.test.mjs</automated>
    <fails_when>non-zero exit or the TAP summary reports zero tests</fails_when>
  </verify>
  <reversibility rating="reversible">The explicit environment mode is additive and leaves default behavior intact.</reversibility>
  <done>Browser proof has a deterministic repository mode backed by behavioral tests and bounded ownership evidence.</done>
</task>

</tasks>

<threat_model>
Security posture: OWASP ASVS Level 1; every high or critical threat blocks execution until its listed mitigation passes.

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Git history/tree → cleanup scope | A broad or spoofed base could turn unrelated code into phase scope. |
| dynamic dispatch → dead-code decision | Runtime entrypoints can evade static search. |
| browser environment → server/process ownership | Reused services or global artifacts can falsify determinism. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-166-13 | Spoofing / Tampering | ownership ledger base and paths | high | mitigate | Exact immutable base, NUL-safe enumeration, one evidence row per path, no source edit in audit task. |
| T-166-14 | Tampering | dead/fallback removal | high | mitigate | Dynamic-dispatch and supported-caller review plus focused and full regression; uncertainty retained. |
| T-166-15 | Denial of service / Spoofing | Playwright reused server and retries | medium | mitigate | Explicit zero-retry/fresh-server mode and real-config tests. |
| T-166-16 | Information disclosure | ledger evidence | medium | mitigate | Paths and low-cardinality disposition only; no content, secret, payload, or adopter fact. |
</threat_model>

## Artifacts this phase produces

- `166-ownership-ledger.md`: exact columns `candidate | evidence | owner | disposition`, immutable base field, final-tree field, direct-edge rows, and unresolved flag `FA-ENG-02`.
- `examples/phoenix_host/playwright.config.ts`: environment flag `CROSSWAKE_REPOSITORY_VERIFY`; repository-mode zero retries, fresh server, and owned output roots.
- `test/js/playwright_repository_mode.test.mjs`: real-config behavioral tests for repository, local, and CI modes.

<verification>
Regenerate the ledger candidate list from the exact base and run the real Playwright configuration test. Do not stage or rewrite the user's pre-existing dirty paths.
</verification>

<success_criteria>
Every v22-touched ownership candidate is disposed with evidence, uncertain code is retained, and the demonstrated browser fallback is deterministic in repository mode.
</success_criteria>

<output>
Create `.planning/workstreams/quality-ratchet-release/phases/166-clean-checkout-engineering-quality/166-04-SUMMARY.md` when done.
</output>
