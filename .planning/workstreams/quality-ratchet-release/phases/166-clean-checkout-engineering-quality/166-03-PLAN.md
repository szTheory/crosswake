---
phase: 166-clean-checkout-engineering-quality
plan: 03
type: execute
wave: 3
depends_on: [166-02]
files_modified:
  - script/repository_artifact_policy.json
  - script/repository_verification_stages.json
  - script/verify_repository.mjs
  - test/crosswake/proof/phase166_repository_quality_test.exs
  - test/fixtures/repository_quality/artifact-cases.json
autonomous: true
requirements: [ENG-03, ENG-04]
estimate:
  tokens: 26000
  raw_tokens: 26000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Every relevant repository artifact belongs to one of the three explicit classes in D-13, with narrow safe fixtures rather than broad exceptions."
    - "Every intentionally tracked generated contract names its canonical source, regeneration argv, exact outputs, and byte-for-byte non-staging drift check per D-14 and D-16."
    - "Artifact failures expose only stable category, repository-relative path, and one correction; invocation cleanup never deletes unowned state per D-17 and D-18."
  artifacts:
    - path: "script/repository_artifact_policy.json"
      provides: "closed three-class artifact and generated-contract policy"
    - path: "test/crosswake/proof/phase166_repository_quality_test.exs"
      provides: "structural artifact-policy and generated-registry proof"
  key_links:
    - from: "script/repository_artifact_policy.json"
      to: "lib/mix/tasks/crosswake.contract.gen.ex"
      via: "canonical source and exact default/dev output registry"
      pattern: "crosswake\\.contract\\.gen"
    - from: "script/verify_repository.mjs"
      to: "script/repository_artifact_policy.json"
      via: "repository-cleanliness stage validates tracked/ignored/forbidden intent"
      pattern: "repository_artifact_policy"
---

<objective>
Make artifact intent and generated-contract drift executable without staging or exposing suspicious content.

Purpose: Ensure clean verification has a policy stronger than `.gitignore` and leaves only intentional repository state.
Output: Three-class policy, artifact fixtures, structural tests, and runner integration.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/quality-ratchet-release/phases/166-clean-checkout-engineering-quality/166-02-SUMMARY.md
@.planning/workstreams/quality-ratchet-release/phases/166-clean-checkout-engineering-quality/166-CONTEXT.md
@.gitignore
@mix.exs
@lib/mix/tasks/crosswake.contract.gen.ex
@test/crosswake/contract/contract_drift_test.exs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define and prove the closed three-class artifact policy</name>
  <read_first>
    - .gitignore
    - script/ci_leaf_manifest.json
    - mix.exs
    - examples/phoenix_host/.env
    - examples/phoenix_host/package.json
    - examples/phoenix_host/playwright.config.ts
    - packages/crosswake-shell-core-ios/Package.swift
    - packages/crosswake-shell-core-android/gradlew
    - lib/mix/tasks/crosswake.contract.gen.ex
    - test/crosswake/contract/contract_drift_test.exs
    - test/crosswake/proof/phase165_ci_policy_test.exs
  </read_first>
  <files>script/repository_artifact_policy.json, test/crosswake/proof/phase166_repository_quality_test.exs, test/fixtures/repository_quality/artifact-cases.json</files>
  <behavior>
    - The policy has exactly ignored transient, intentionally tracked, and forbidden tracked classes; unknown keys/classes and overlaps fail closed.
    - `examples/phoenix_host/.env` is the sole explicit safe fixture for its exact non-secret role, not a wildcard exception.
    - Forbidden secret/editor/crash/credential/local-only path families are detected without reading or printing contents.
    - The unresolved FA-ENG-03 assumption remains flagged while the policy supplies a mechanical backstop.
  </behavior>
  <action>Start RED with a closed synthetic artifact matrix, then create the small JSON policy per D-13 and D-14. Follow `script/ci_leaf_manifest.json` for a closed top-level schema and deterministic record ordering, and follow `test/crosswake/proof/phase165_ci_policy_test.exs` for decoded-JSON key, ordering, uniqueness, and allowed-value assertions. Enumerate repository-owned transient families by intent, not every downloaded/tool-version-dependent file. Represent intentionally tracked source separately from generated contracts. Represent forbidden tracked secret-bearing, editor, crash, credential, and local-only families with narrowly named safe fixtures; the checked-in example `.env` exception is exact-path and purpose-specific. Add ExUnit decoding/schema/overlap/order tests and negative fixtures for a forbidden path, ignored source concealment, safe fixture, unknown class/key, and ambiguous overlap. The checker may classify paths but must never print file contents, environment values, tokens, payloads, or cache keys per D-17.</action>
  <acceptance_criteria>
    - Exactly three policy classes parse; unknown, duplicate, unordered, overlapping, or empty records fail.
    - Existing `.gitignore`, package allowlists, native/browser output roots, and the exact safe `.env` fixture are reconciled without broadening exclusions.
    - Forbidden diagnostics include one stable category/path/remediation tuple and no file body.
    - No Android feature, template, generator, device, vector, Maven, JVM, or parity behavior changes.
  </acceptance_criteria>
  <verify>
    <automated>mix test test/crosswake/proof/phase166_repository_quality_test.exs --only artifact_policy</automated>
    <fails_when>non-zero exit, zero selected tests, or any fixture secret sentinel appears in output</fails_when>
  </verify>
  <reversibility rating="reversible">The policy and synthetic fixtures are additive repository metadata.</reversibility>
  <done>Artifact intent is explicit, closed, narrow, privacy-safe, and mechanically testable.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Regenerate every tracked contract without mutating the index</name>
  <read_first>
    - script/repository_artifact_policy.json
    - script/repository_verification_stages.json
    - script/verify_repository.mjs
    - lib/mix/tasks/crosswake.contract.gen.ex
    - test/crosswake/contract/contract_drift_test.exs
    - .github/workflows/crosswake-ci.yml
  </read_first>
  <files>script/repository_artifact_policy.json, script/repository_verification_stages.json, script/verify_repository.mjs, test/crosswake/proof/phase166_repository_quality_test.exs, test/fixtures/repository_quality/artifact-cases.json</files>
  <behavior>
    - The generated registry names the canonical generator and all eight default/dev outputs exactly.
    - Drift proof snapshots exact bytes, runs default and `--dev` generation, compares explicit paths, restores only snapshot-owned copies in focused dirty mode, and never stages files.
    - A missing, changed, extra, or unregistered generated output fails with one regeneration command.
  </behavior>
  <action>Extend the policy and production runner with the eight explicit outputs owned by `Crosswake.Contract` generation: both iOS and Android route-activation default/dev fixtures, the root/native bridge vector copies, and `docs/_contract_snippet.md`. Use fixed regeneration argv for default and `--dev`. In complete isolated mode compare against committed bytes; in focused dirty mode snapshot only registered outputs, regenerate, compare byte-for-byte, and restore only those snapshots. Never run `git add`, change index state, or use a repository-wide diff as ownership. Keep the existing semantic contract-drift test as a distinct fast responsibility unless the D-10 identical-invariant test proves true; correct only demonstrably contradictory comments, invariant-first per D-24.</action>
  <acceptance_criteria>
    - All eight outputs, canonical source, two generation invocations, and one correction command are exact and tested.
    - Deliberate generated drift fails; unchanged generation passes; focused dirty mode restores the exact prior bytes.
    - Git index checksum and staged records are identical before and after success and failure.
    - Existing `guard-02-generate-and-diff` identity remains unchanged for the later atomic CI migration per D-23.
  </acceptance_criteria>
  <verify>
    <automated>mix test test/crosswake/proof/phase166_repository_quality_test.exs --only generated_contracts &amp;&amp; node --test test/js/repository_verification.test.mjs --test-name-pattern='generated|index'</automated>
    <fails_when>non-zero exit, zero selected tests in either runner, or fixture index bytes differ after execution</fails_when>
  </verify>
  <reversibility rating="reversible">The non-staging checker replaces no public contract and preserves all generated outputs.</reversibility>
  <done>Generated-contract drift is exact, non-staging, and safe in both isolated and focused modes.</done>
</task>

</tasks>

<threat_model>
Security posture: OWASP ASVS Level 1; every high or critical threat blocks execution until its listed mitigation passes.

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repository paths → artifact policy | Untrusted filenames must not escape classification or become command text. |
| generator → tracked outputs | Regeneration may alter worktree bytes but must never alter the index or unrelated files. |
| suspicious artifact → diagnostics | Detection must not disclose contents. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-166-09 | Information disclosure | forbidden-artifact diagnostics | high | mitigate | Category/path/remediation allowlist only; no body/environment output. |
| T-166-10 | Tampering | generated-contract drift | high | mitigate | Explicit paths and byte snapshots; no staging or index mutation. |
| T-166-11 | Tampering / Elevation of privilege | artifact path matching | high | mitigate | Repository-relative normalized paths, closed safe fixtures, hostile-path tests. |
| T-166-12 | Spoofing | ignored artifact intent | medium | mitigate | Three-class policy independent of `.gitignore`; overlap/unknown rejection. |
</threat_model>

## Artifacts this phase produces

- `script/repository_artifact_policy.json`: fields `schema_version`, `ignored_transient`, `intentionally_tracked`, `generated_contracts`, `forbidden_tracked`, and `safe_fixtures`; generated records name `canonical_source`, `regeneration_argv`, `output_paths`, and `remediation_command`.
- `test/fixtures/repository_quality/artifact-cases.json`: synthetic policy, generated-drift, safe-fixture, index-preservation, and forbidden-artifact cases.
- `test/crosswake/proof/phase166_repository_quality_test.exs`: tags `:artifact_policy` and `:generated_contracts`.
- `script/verify_repository.mjs`: artifact-policy validator and explicit byte-regeneration checker consumed by `repository-cleanliness`.

<verification>
Run both focused test commands and confirm no `git add`/`git clean` behavior exists in the production remediation path.
</verification>

<success_criteria>
Every relevant artifact has explicit intent, generated outputs are reproducible without staging, and diagnostics stay narrow and non-secret.
</success_criteria>

<output>
Create `.planning/workstreams/quality-ratchet-release/phases/166-clean-checkout-engineering-quality/166-03-SUMMARY.md` when done.
</output>
