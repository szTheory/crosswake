---
phase: 166-clean-checkout-engineering-quality
plan: 07
type: execute
wave: 7
depends_on: [166-06]
files_modified:
  - script/capture_repository_verification_evidence.sh
  - script/run_repository_evidence_environment.sh
  - script/repository_evidence_toolchain.json
  - script/repository_verification_stages.json
  - test/js/repository_verification.test.mjs
autonomous: true
requirements: [ENG-01, ENG-03, ENG-04]
estimate:
  tokens: 24000
  raw_tokens: 24000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Capture tooling is committed before evidence is produced, so later evidence binds to a non-self-referential supported-code commit."
    - "A dirty source checkout can name an explicit tracked commit; only an isolated checkout of that commit becomes evidence input per D-15."
    - "The Apple-capable host has an executable invocation-local route to declared BEAM, Node, and Java tools without secrets or global/system installation per D-04 and D-05."
  artifacts:
    - path: "script/capture_repository_verification_evidence.sh"
      provides: "exact-commit isolated capture and bounded evidence verification"
    - path: "script/run_repository_evidence_environment.sh"
      provides: "invocation-local declared-tool environment route on an Apple-capable host"
    - path: "script/repository_evidence_toolchain.json"
      provides: "closed Darwin/arm64 artifact URLs, versions, release authorities, and SHA-256 pins"
  key_links:
    - from: "script/run_repository_evidence_environment.sh"
      to: "script/capture_repository_verification_evidence.sh"
      via: "validated invocation-local PATH and explicit commit"
      pattern: "--commit"
---

<objective>
Commit the exact-commit capture tool and a qualified invocation-local tool-environment route before canonical evidence exists.

Purpose: Make evidence reproducible from the dirty development checkout without making evidence identity self-referential.
Output: Tested capture/environment scripts and no canonical evidence files.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/quality-ratchet-release/phases/166-clean-checkout-engineering-quality/166-06-SUMMARY.md
@.planning/workstreams/quality-ratchet-release/phases/166-clean-checkout-engineering-quality/166-CONTEXT.md
@script/verify_repository.sh
@script/verify_repository.mjs
@script/repository_verification_stages.json
@.tool-versions
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement commit-addressed isolated capture without evidence output</name>
  <read_first>
    - script/verify_repository.sh
    - script/verify_repository.mjs
    - script/repository_verification_stages.json
    - script/repository_artifact_policy.json
    - .tool-versions
    - script/check_example_host_isolation.sh
    - script/retain_physical_iphone_evidence_transaction.sh
  </read_first>
  <files>script/capture_repository_verification_evidence.sh, test/js/repository_verification.test.mjs</files>
  <behavior>
    - Capture accepts an explicit source repository and commit SHA even when the source worktree is dirty, resolves the SHA as a tracked commit, and copies none of its working-tree/index state.
    - It creates an isolated checkout with an empty baseline, runs `script/verify_repository.sh --all`, validates closed results and repository/index cleanup, and projects only allowlisted evidence fields.
    - Self-tests cover wrong SHA, untracked object, failing child, altered index, residue, forbidden evidence key, symlink escape, and dirty-source success.
  </behavior>
  <action>First ground every existing input with `git ls-files --error-unmatch -- script/verify_repository.sh script/verify_repository.mjs script/repository_verification_stages.json script/repository_artifact_policy.json .tool-versions script/check_example_host_isolation.sh script/retain_physical_iphone_evidence_transaction.sh`; halt if any is not tracked. Start RED with capture tests, then implement the strict script with public `--source-repository`, `--commit`, `--output-dir`, `--verify`, and `--self-test` flags. Follow `script/check_example_host_isolation.sh` for exact temporary ownership, status capture, and before/after comparison, and `script/retain_physical_iphone_evidence_transaction.sh` for commit-bound evidence validation and closed output projection; do not copy that analog's staging behavior. Resolve the commit from the source repository object database and create an invocation-owned isolated checkout without requiring or copying a clean source worktree. Require an empty isolated baseline and exact preflight, invoke the complete facade, retain full logs only under the run root, validate the evidence schema, and clean the run root on every outcome per D-15 through D-22. In this plan run only self-tests and temporary fixture captures; canonical phase evidence is forbidden until Plan 08, after this tool is committed.</action>
  <acceptance_criteria>
    - A dirty source fixture succeeds when its explicit commit is valid, and none of the dirty bytes enter the isolated checkout.
    - Every negative fixture creates no canonical evidence and cleans its exact owned roots.
    - The committed tool contains all logic later used to produce and verify evidence.
  </acceptance_criteria>
  <verify>
    <automated>script/capture_repository_verification_evidence.sh --self-test &amp;&amp; node --test test/js/repository_verification.test.mjs --test-name-pattern='capture|dirty source|evidence'</automated>
    <fails_when>non-zero exit, zero selected tests, or any fixture leaks dirty-source bytes or retained output</fails_when>
  </verify>
  <reversibility rating="reversible">The tool creates only invocation-owned temporary state and is committed before use.</reversibility>
  <done>The committed capture tool can verify any explicit tracked commit from a dirty source without producing canonical evidence yet.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Provide the qualified invocation-local evidence environment</name>
  <read_first>
    - .tool-versions
    - script/verify_generated_android_shell.sh
    - script/check_example_host_isolation.sh
    - script/ci_leaf_manifest.json
    - script/repository_verification_stages.json
    - .github/actions/setup-elixir-cache/action.yml
    - .github/actions/setup-android-jvm/action.yml
    - packages/crosswake-shell-core-ios/Package.swift
  </read_first>
  <files>script/run_repository_evidence_environment.sh, script/repository_evidence_toolchain.json, script/repository_verification_stages.json, test/js/repository_verification.test.mjs</files>
  <behavior>
    - On `Darwin/arm64`, the route validates Xcode/Swift in place and materializes Erlang 27.3, Elixir 1.19.5 built for OTP 27, Node 22.14.0, and Eclipse Temurin JDK 17.0.20.1+1 only beneath an invocation-owned tool root.
    - It consumes `script/repository_evidence_toolchain.json`, whose closed records pin immutable release URLs, source authority, archive layout, version probe, and literal SHA-256; it rejects floating URLs, unsupported platform/architecture, and any checksum mismatch before extraction.
    - It requires no credential, signing material, global install, package-manager mutation, or clean source checkout.
  </behavior>
  <action>First ground every existing input with `git ls-files --error-unmatch -- .tool-versions script/repository_verification_stages.json .github/actions/setup-elixir-cache/action.yml .github/actions/setup-android-jvm/action.yml packages/crosswake-shell-core-ios/Package.swift script/verify_generated_android_shell.sh script/check_example_host_isolation.sh script/ci_leaf_manifest.json`; halt if any is not tracked. Start RED with a fake artifact server/materialization fixture that proves exact version selection, checksum/source pin rejection, archive-entry escape rejection, PATH containment, failure cleanup, and forbidden global write detection. Create the closed lock input `script/repository_evidence_toolchain.json` for `os=Darwin` and `arch=arm64` only, following the tracked `script/ci_leaf_manifest.json` analog for closed keys, ordered records, and literal authority fields. Pin these immutable authorities and literal archive SHA-256 values: Erlang Ecosystem Foundation `erlef/otp_builds` release `OTP-27.3`, asset `OTP-27.3-macos-arm64.tar.gz`, digest `a76eb513202c7131bcd62ee516f8498098b8adadd417bd90e30ae3a2e3f6762d`; Elixir upstream release `v1.19.5`, asset `elixir-otp-27.zip`, digest `1ab3154ec19adcd4b764cf96badecbe44df5ec6f358dc0fbe29c3749ff6c08de`; Node upstream `v22.14.0`, asset `node-v22.14.0-darwin-arm64.tar.gz`, digest `e9404633bc02a5162c5c573b1e2490f5fb44648345d64a958b17e325729a5e42`; and Eclipse Temurin release `jdk-17.0.20.1+1`, asset `OpenJDK17U-jdk_aarch64_mac_hotspot_17.0.20.1_1.tar.gz`, digest `196d13ba5f10414bef7f6a05a9b3f00edacb18ebacef2b99485db9e2ee18f0e8`. Record the immutable HTTPS URL, authority repository/tag, archive root, executable-relative path, and exact version probe for every artifact; no `latest` URL or plugin resolution is allowed. Follow `script/verify_generated_android_shell.sh` only for Darwin architecture selection, download retry, archive extraction, and Java-home discovery, replacing its user-home, floating Adoptium endpoint, and Homebrew fallback with the invocation-owned locked records; follow `script/check_example_host_isolation.sh` for exact temporary-root cleanup. Implement `script/run_repository_evidence_environment.sh` with a validated `crosswake-repository-evidence-tools.*` root, `curl --proto '=https' --tlsv1.2 --fail --location`, SHA-256 verification before extraction, archive-entry containment checks, and an allowlisted PATH made from the four extracted prefixes plus the inherited system directories needed for Xcode. Place Erlang first, then add the OTP-27 Elixir archive's `bin` directory as its own prefix and validate `erl` reports 27.3 and `elixir --version` reports 1.19.5 with OTP 27; set `JAVA_HOME` to the extracted Temurin bundle and validate Java major 17; validate Node exactly 22.14.0. Validate host `xcodebuild` and `swift` in place without installing Apple tooling. Add `--prepare-and-preflight` to perform a real download, checksum, extraction, composition, exact version probes, and `script/verify_repository.sh --stage repository-preflight` against an explicit tracked commit, then clean the tool root without producing canonical evidence. The normal path invokes the committed capture script with explicit source repository and commit. Do not use asdf, Homebrew, sudo, secrets, signing, system package-manager writes, user-global caches, or paths outside the owned roots per D-04 and D-05.</action>
  <acceptance_criteria>
    - Synthetic provisioning tests reject wrong version, checksum, source, global path, missing Apple tool, and cleanup escape.
    - The real Darwin/arm64 route materializes all four locked archives, composes OTP 27 with Elixir 1.19.5, and passes the production repository preflight before canonical capture.
    - The script can target an explicit commit from a dirty source and delegates all proof to the committed capture/facade commands.
  </acceptance_criteria>
  <verify>
    <automated>script/run_repository_evidence_environment.sh --self-test &amp;&amp; script/run_repository_evidence_environment.sh --prepare-and-preflight --source-repository . --commit "$(git rev-parse HEAD)" &amp;&amp; script/capture_repository_verification_evidence.sh --self-test &amp;&amp; script/check_phase166_clean_checkout_engineering_quality.sh</automated>
    <fails_when>non-zero exit, missing self-test or real-preflight PASS, a floating/unpinned source is accepted, an archive/checksum/version/composition probe differs, any path escapes its owned root, or stage/CI parity regresses</fails_when>
  </verify>
  <reversibility rating="reversible">Invocation-local tools and temporary checkouts are deleted; tracked pins and scripts can be reverted.</reversibility>
  <done>A credential-free executable route has materially prepared all pinned tools and passed exact production preflight on Darwin/arm64 before Plan 08 capture.</done>
</task>

</tasks>

<threat_model>
Security posture: OWASP ASVS Level 1; every high or critical threat blocks execution until its listed mitigation passes.

## Trust Boundaries

| Boundary | Description |
|---|---|
| dirty source repository → isolated commit checkout | Working-tree bytes must not enter evidence. |
| pinned tool material → executed binaries | Tool artifacts become executable authority. |
| full logs → bounded evidence | Sensitive child output must remain temporary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-166-27 | Spoofing / Tampering | commit-addressed checkout | high | mitigate | Resolve an explicit commit object, isolated empty baseline, and dirty-source exclusion tests. |
| T-166-28 | Tampering / Elevation of privilege | tool provisioning | high | mitigate | Tracked source/checksum pins, invocation-local root, no sudo/global package-manager writes, hostile fixtures. |
| T-166-29 | Information disclosure | logs/evidence projection | high | mitigate | Closed evidence schema and temporary full logs removed on every exit. |
</threat_model>

<verification>
Run both self-test suites. This plan must end with committed tooling and no canonical evidence files.
</verification>

<success_criteria>
Capture and qualified environment tooling are committed, tested, source-worktree-independent, and ready to define the supported-code commit for Plan 08.
</success_criteria>

<output>
Create `.planning/workstreams/quality-ratchet-release/phases/166-clean-checkout-engineering-quality/166-07-SUMMARY.md` when done.
</output>
