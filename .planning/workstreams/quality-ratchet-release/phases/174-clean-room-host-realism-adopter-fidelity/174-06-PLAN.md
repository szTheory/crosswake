---
phase: 174-clean-room-host-realism-adopter-fidelity
plan: 06
type: execute
wave: 4
depends_on: ["174-01", "174-02", "174-03", "174-04", "174-05"]
files_modified:
  - .planning/workstreams/quality-ratchet-release/phases/174-clean-room-host-realism-adopter-fidelity/174-NON-VACUITY.md
  - .planning/workstreams/quality-ratchet-release/REQUIREMENTS.md
autonomous: true
requirements: [ROOM-01, ROOM-02, ROOM-03, ROOM-04, ROOM-05, ROOM-06, FID-01]

estimate:
  tokens: 60000
  raw_tokens: 30000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Every check this phase landed is recorded against the six-shape vacuity taxonomy with a measured non-vacuity fact — a count found on a real run, or a named executed mutation — never a bare tick and never a blank shape (milestone `vacuity_taxonomy` convention)."
    - "A check this phase landed that was not demonstrated against a mutation is named with a decidable reason rather than omitted; omission is defined as non-compliant by the convention."
    - "Each of the seven phase requirements carries an explicit satisfied / not-satisfied disposition pointing at the artifact that decides it, so a requirement left open is visible rather than inferred."
    - "The phase's deliberate non-actions — the matrix path's install step, the untouched release lanes, the untouched `doctor` contract — are recorded with reasons rather than left as gaps a later reader must reconstruct."
  prohibitions:
    - "Never record a check in the vacuity taxonomy with a blank shape; use a shape letter or the explicit escape form with a reason, never a third option."
    - "Never omit a landed check from the record; a check without an executed mutation is named with a decidable reason, because omission and 'nothing needed checking' must never look the same on disk."
    - "Never mark a requirement satisfied without naming the artifact or run that decides it."
    - "Never write a verification gate whose exit status is taken from the tail of a pipeline; a pipeline reports the last command's status and the gate would be green while asserting nothing."
    - "Never run `git add -A` or `git add .` under `.planning/`; stage `.planning/` paths explicitly by filename, and never stage `config.json`, `milestone.lock`, or `state.json` under the workstream directory."
    - "Never run `git stash` in any form, and never run `git reset --hard`, `git clean -fd`, or a checkout of tracked paths this plan did not create."
    - "Never name the early-adopter SaaS in any artifact; the codename is First B2C Adopter."
  artifacts:
    - .planning/workstreams/quality-ratchet-release/phases/174-clean-room-host-realism-adopter-fidelity/174-NON-VACUITY.md
  key_links:
    - "the five sibling SUMMARYs -> the enumerated check set -> 174-NON-VACUITY.md's rows -> the `## Vacuity Taxonomy` section VERIFICATION.md carries at phase close."
    - "174-CLEANROOM-EVIDENCE.md, 174-FID-01-DISPOSITION.md and the two companion findings -> the per-requirement disposition table."
---

<planner_notes>
**Nyquist Dimension 8 is not applicable to this phase** — the ROADMAP's Phase 174 **Research** line
records the decision to skip phase-level research, so there is no `174-RESEARCH.md` and therefore no
`174-VALIDATION.md`. Recorded, not fabricated around.

**Edge-probe accounting.** 7 probe rows surfaced, all `unclassified` and therefore `unresolved`.
**7 surfaced == 0 authored as resolved truths + 7 flagged as unresolved assumptions** (ROOM-01
through ROOM-06 and FID-01). This plan's Task 2 is where each of those seven gets an explicit
disposition against real evidence — which is how they stop being planner assumptions.

**Prohibition accounting.** 10 recalled in-prompt; **7 authored here descriptor-less + 3
canon-referred** (OWASP Top Ten, path traversal, injection — existing project canon, not specific to
this plan) **== 10**.
</planner_notes>

<scope_boundary>
This plan records. It changes no executable file, no workflow, and no contract. If writing the record
reveals a check that is vacuous, do **not** repair it here — name it in the record with its shape and
the measured fact that exposes it, and say plainly that it is unproven. A record that quietly fixes
what it was supposed to measure is not a record.
</scope_boundary>

<objective>
Close the phase with the two things a later reader needs and cannot reconstruct: what every check
this phase landed actually proves, and where each of the seven requirements stands.

Purpose: milestone v23.0's standing `vacuity_taxonomy` convention exists because a check that is
green while asserting nothing is this project's named central defect. Every phase in this milestone
carries the record; a phase that skipped it would be indistinguishable from a phase that had nothing
to record.

Output: `174-NON-VACUITY.md`, and a per-requirement disposition an auditor can read without opening a
diff.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/workstreams/quality-ratchet-release/ROADMAP.md
@.planning/workstreams/quality-ratchet-release/VERIFICATION-CONVENTIONS.md
@.planning/workstreams/quality-ratchet-release/REQUIREMENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: The phase's vacuity-taxonomy and non-vacuity record</name>
  <files>.planning/workstreams/quality-ratchet-release/phases/174-clean-room-host-realism-adopter-fidelity/174-NON-VACUITY.md</files>
  <read_first>
    - `.planning/workstreams/quality-ratchet-release/VERIFICATION-CONVENTIONS.md` in full — the `vacuity_taxonomy` record's required keys, the lexical ordering rule, the explicit escape form, the null statement rule, and the "never a bare tick" rule. Follow it exactly.
    - `.planning/workstreams/quality-ratchet-release/phases/173-recovery-path-proof-convergence/173-NON-VACUITY.md` — the house standard for what counts as a measured non-vacuity fact, and for recording a real-run observation that did not go as expected.
    - `.planning/workstreams/quality-ratchet-release/phases/172-per-package-proof-scope/172-NON-VACUITY.md` — the precedent that only checks with an actually-executed mutation get full rows, and every other landed check is named with a decidable reason.
    - `.planning/workstreams/quality-ratchet-release/phases/170-vacuous-assertion-remediation/170-VACUITY-TAXONOMY.md` — the six shapes.
    - All five sibling SUMMARYs for this phase (174-01 through 174-05), which record each check's measured mutation.
  </read_first>
  <action>
Write `174-NON-VACUITY.md` covering **every** check this phase landed across all six plans, in
check-id lexical order.

Enumerate the check set from the five sibling SUMMARYs rather than from memory. It includes, at
minimum: the `step=` marker-roster check, the `manifest_contract` byte-identity guard with its four
distinct failure modes, the clean-room lane-roster parity check, the companion-findings check, and
the physical-iPhone exit-status classifier's cases. If Task 1 of plan 174-03 selected `close-now`,
the vocabulary-completeness, ordering, owner and stale-envelope cases belong in the set too.

Each row carries: the check id as it appears in its own source, a shape letter from the six-shape
taxonomy or the explicit escape form with its reason, and a **measured** non-vacuity fact — the count
the check found on a real run, or the specific mutation it was demonstrated turning red against.
Never "looks correct", never a blank shape.

Where a check landed but was **not** demonstrated against a mutation, say so in its row with a
decidable reason. Omission is defined as non-compliant by the convention, in every case, with no
exception: "there was nothing to check" and "we forgot" must never look the same on disk.

Add a closing section with the real-run observations this phase made — the rindle rehearsal run and
its job-level conclusion, the threadline run, the sigra run — pointing at `174-CLEANROOM-EVIDENCE.md`
and the two findings rather than duplicating them.

Then record the three things this phase deliberately did **not** do, each with its reason:

1. The matrix path was not given a `mix crosswake.install` step — ROADMAP SC#2 names the legacy path,
   and the matrix path's ten profile runs sit on the pull-request critical path. This leaves ROOM-02's
   broader wording partially open; say so rather than reading the narrower SC as the whole
   requirement.
2. `release-please.yml`'s `clean-room-proof-*` lanes were not modified; the rehearsal is a second door
   onto the same script and the lanes keep their `needs: publish-hex-*` ordering guarantee.
3. `doctor` was not touched; the byte-identity guard is the evidence, not the assertion.

Note that the `## Vacuity Taxonomy` section of this phase's `VERIFICATION.md` is authored at phase
close by the verification step, per the convention; this file is its evidence, not its replacement.
  </action>
  <acceptance_criteria>
    - `174-NON-VACUITY.md` exists and contains one row per check landed by plans 174-01 through 174-05, in check-id lexical order; state the row count and how you enumerated the set in the SUMMARY.
    - Every row carries a shape letter or the explicit escape form with a reason; the count of rows with a blank shape is zero.
    - Every row carries either a measured count, a named executed mutation, or an explicit decidable reason why no mutation was executed.
    - The closing section names the rindle run id and its job-level conclusion and links both companion findings.
    - All three deliberate non-actions are recorded with reasons, including the partially-open state of ROOM-02's broader wording.
    - `bash script/assert_manifest_contract_unchanged.sh` exits 0, which is the evidence behind the third non-action.
  </acceptance_criteria>
  <verify>
    <automated>bash script/assert_manifest_contract_unchanged.sh</automated>
    <fails_when>non-zero exit</fails_when>
    <automated>test -s .planning/workstreams/quality-ratchet-release/phases/174-clean-room-host-realism-adopter-fidelity/174-NON-VACUITY.md</automated>
    <fails_when>non-zero exit — the file is missing or empty</fails_when>
    <automated>mix test test/crosswake/proof --max-cases 1</automated>
    <fails_when>non-zero exit, or the summary line reports any number of failures other than 0</fails_when>
  </verify>
  <reversibility rating="reversible">A record document; nothing executable changes.</reversibility>
  <done>Every check this phase landed is recorded against the vacuity taxonomy with a measured fact, and the phase's real-run observations and deliberate non-actions are on disk.</done>
</task>

<task type="auto">
  <name>Task 2: Each of the seven requirements gets an explicit disposition</name>
  <files>.planning/workstreams/quality-ratchet-release/phases/174-clean-room-host-realism-adopter-fidelity/174-NON-VACUITY.md, .planning/workstreams/quality-ratchet-release/REQUIREMENTS.md</files>
  <read_first>
    - `.planning/workstreams/quality-ratchet-release/REQUIREMENTS.md` — the ROOM-01..ROOM-06 and FID-01 wording, and the traceability table's row format at the bottom of the file.
    - `.planning/workstreams/quality-ratchet-release/ROADMAP.md` Phase 174 section — the seven Success Criteria, which are the concrete form of these requirements.
    - `174-CLEANROOM-EVIDENCE.md`, `174-FID-01-DISPOSITION.md`, `174-FINDING-THREADLINE.md`, `174-FINDING-SIGRA.md`, and the five sibling SUMMARYs — the evidence each disposition points at.
  </read_first>
  <action>
Add a per-requirement disposition table to `174-NON-VACUITY.md`: one row for each of ROOM-01,
ROOM-02, ROOM-03, ROOM-04, ROOM-05, ROOM-06 and FID-01, each carrying the requirement id, an explicit
**satisfied** or **not satisfied** verdict, and the artifact, run id or check that decides it.

There is no third verdict and no blank. A requirement whose evidence is pending — ROOM-03 if plan
174-04 recorded the rindle observation as post-merge — is **not satisfied**, stated as such, with the
exact command that would satisfy it. This is the whole point: a criterion left unstated must never be
readable as satisfied.

Note explicitly in the table's preamble that ROOM-02's requirement text is broader than ROADMAP SC#2
(the requirement says "the clean-room host", the success criterion says the legacy path), and that
only the legacy path was changed. Verdict it honestly against the wording you are claiming, and say
which wording that is.

Then update `REQUIREMENTS.md`: tick each ROOM/FID checkbox that is genuinely satisfied and update its
traceability row; leave unticked any that is not, and do not soften the requirement text to make it
tickable. If nothing can be ticked yet, say that in the SUMMARY rather than ticking optimistically.
Stage `.planning/` paths explicitly by filename.
  </action>
  <acceptance_criteria>
    - `174-NON-VACUITY.md` carries a seven-row disposition table; the count of rows is exactly 7 and the count of rows with a blank verdict is 0.
    - Every satisfied row names a specific artifact, run id, or check; no row claims satisfaction from prose alone.
    - Every not-satisfied row names what would satisfy it, as a command or an artifact.
    - The ROOM-02 preamble note is present and states which wording the verdict is against.
    - `REQUIREMENTS.md`'s checkboxes and traceability rows for ROOM-01..ROOM-06 and FID-01 agree with the disposition table row-for-row; state the measured agreement in the SUMMARY.
    - No requirement text in `REQUIREMENTS.md` was reworded.
  </acceptance_criteria>
  <verify>
    <automated>grep -c 'ROOM-0' .planning/workstreams/quality-ratchet-release/phases/174-clean-room-host-realism-adopter-fidelity/174-NON-VACUITY.md</automated>
    <fails_when>the printed count is fewer than 6</fails_when>
    <automated>git diff --unified=0 .planning/workstreams/quality-ratchet-release/REQUIREMENTS.md</automated>
    <fails_when>the diff changes any requirement's descriptive text rather than only its checkbox state and traceability row</fails_when>
    <automated>mix test test/crosswake/proof --max-cases 1</automated>
    <fails_when>non-zero exit, or the summary line reports any number of failures other than 0</fails_when>
  </verify>
  <reversibility rating="reversible">Documentation and checkbox state only.</reversibility>
  <done>All seven phase requirements carry an explicit verdict backed by a named artifact or run, and `REQUIREMENTS.md` agrees with it row-for-row.</done>
</task>

</tasks>

<artifacts_produced>
## Artifacts this phase produces (plan 174-06)

| Kind | Name |
|---|---|
| New file | `.planning/.../174-clean-room-host-realism-adopter-fidelity/174-NON-VACUITY.md` |
| Document section | the phase's vacuity-taxonomy rows, in check-id lexical order |
| Document section | the seven-row per-requirement disposition table |
| Updated file | `.planning/workstreams/quality-ratchet-release/REQUIREMENTS.md` (checkbox state and traceability rows only) |
</artifacts_produced>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Sibling SUMMARYs -> the phase record | Self-reported task outcomes become the phase's audited evidence |
| The phase record -> milestone closeout | This document is what Phase 175 and the milestone audit read instead of re-deriving the facts |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-174-25 | Repudiation | A landed check silently omitted from the record, so absence reads as "nothing to check" | high | mitigate | Task 1 enumerates the set from the five sibling SUMMARYs, requires a row per check, and requires a decidable reason for any check without an executed mutation; the convention defines omission as non-compliant. |
| T-174-26 | Spoofing | A requirement marked satisfied on prose rather than evidence | high | mitigate | Task 2 requires every satisfied row to name an artifact, run id or check, and forbids a third verdict or a blank; pending evidence verdicts as not satisfied. |
| T-174-27 | Tampering | Requirement text softened to make a checkbox tickable | high | mitigate | Task 2 forbids rewording and its verification diffs `REQUIREMENTS.md` for descriptive-text changes rather than checkbox-state changes. |
| T-174-28 | Repudiation | A check found vacuous while writing the record being quietly repaired instead of reported | medium | mitigate | The scope boundary forbids repair in this plan and requires the finding to be named as unproven in the record. |
| T-174-SC | Tampering | npm/pip/cargo installs | high | mitigate | Not applicable to this plan — it adds no package-manager install step. If any task later requires one, a package-legitimacy gate plus a blocking human checkpoint is required first. |
</threat_model>

<verification>
- `174-NON-VACUITY.md` exists, has zero rows with a blank shape, and carries a seven-row requirement disposition table with zero blank verdicts.
- `bash script/assert_manifest_contract_unchanged.sh` exits 0.
- `mix test test/crosswake/proof --max-cases 1` exits 0 with 0 failures.
- `REQUIREMENTS.md`'s ROOM/FID checkbox state agrees with the disposition table row-for-row, with no requirement text reworded.
</verification>

<success_criteria>
The phase closes with a record an auditor can act on: every check it landed named with a shape and a
measured fact, every one of its seven requirements carrying an explicit verdict backed by a named
artifact or run, and every deliberate non-action written down with its reason.
</success_criteria>

<output>
Create `.planning/workstreams/quality-ratchet-release/phases/174-clean-room-host-realism-adopter-fidelity/174-06-SUMMARY.md` when done.
</output>
