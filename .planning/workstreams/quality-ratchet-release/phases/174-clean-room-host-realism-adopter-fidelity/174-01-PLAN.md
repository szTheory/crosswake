---
phase: 174-clean-room-host-realism-adopter-fidelity
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - script/verify_companion_cleanroom.sh
  - test/crosswake/proof/phase174_cleanroom_host_realism_test.exs
  - .planning/workstreams/quality-ratchet-release/phases/174-clean-room-host-realism-adopter-fidelity/evidence/174-legacy-rindle-local-run.log
autonomous: true
requirements: [ROOM-01, ROOM-02, ROOM-04]

estimate:
  tokens: 120000
  raw_tokens: 60000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "The legacy positional clean-room path's generated host declares a real Crosswake route carrying capability metadata before `doctor` runs — observed in the host generated during an actual run, not read out of the script (ROADMAP SC#1, ROOM-01)."
    - "The legacy positional path runs `mix crosswake.install` before `doctor`, and the install step is observed executing in a real run's captured log (ROADMAP SC#2, ROOM-02)."
    - "The legacy path emits a `step=` marker for every stage it performs, at the same granularity the matrix path already emits, and every marker in the declared roster is found in a real run's captured log (ROADMAP SC#4, ROOM-04)."
    - "`bash script/verify_companion_cleanroom.sh crosswake_rindle 0.1.0 rindle Rindle` completes with `mix crosswake.doctor` exiting zero against the live published package (ROADMAP SC#1/SC#2, ROOM-01/ROOM-02)."
    - "The captured log of that run is committed in-repo, so the evidence for SC#1, SC#2 and SC#4 outlives the shell session that produced it."
  prohibitions:
    - "Never weaken, narrow, or scope-down `doctor`'s `manifest_contract` check, or any other `doctor` contract, to make this harness pass. The harness is the thing being fixed."
    - "Never mark a clean-room lane non-blocking, delete a failing lane job, or add a failure-swallowing construct to the harness to turn it green."
    - "Never derive the `step=` marker roster from `script/verify_companion_cleanroom.sh` itself — a roster read out of the artifact being policed silently excludes every marker the script forgot to emit."
    - "Never write a verification gate whose exit status is taken from the tail of a pipeline; a pipeline reports the last command's status and the gate would be green while asserting nothing."
    - "Never run `git add -A` or `git add .` under `.planning/`; stage `.planning/` paths explicitly by filename, and never stage `config.json`, `milestone.lock`, or `state.json` under the workstream directory."
    - "Never run `git stash` in any form, and never run `git reset --hard`, `git clean -fd`, or a checkout of tracked paths this plan did not create."
    - "Never name the early-adopter SaaS in any artifact; the codename is First B2C Adopter."
  artifacts:
    - script/verify_companion_cleanroom.sh
    - test/crosswake/proof/phase174_cleanroom_host_realism_test.exs
    - .planning/workstreams/quality-ratchet-release/phases/174-clean-room-host-realism-adopter-fidelity/evidence/174-legacy-rindle-local-run.log
  key_links:
    - "legacy Step 4 router stub -> route capability metadata -> Manifest.compile -> doctor's manifest_contract check reaching a populated :routes section."
    - "legacy Step 6 runtime config -> new legacy install step (`mix crosswake.install --router lib/clean_room_host/router.ex`) -> priv/crosswake/install_manifest.json -> doctor's load_install_manifest advisory."
    - "legacy `step=` emissions -> the committed run log -> phase174_cleanroom_host_realism_test.exs's independently-declared marker roster."
---

<planner_notes>
**Nyquist Dimension 8 is not applicable to this phase.** The ROADMAP's own Phase 174 **Research**
line records the decision to skip phase-level research (`"Standard pattern — pure backport of an
already-working, already-tested function (matrix_write_host) into a sibling code path in the same
file. Skip phase-level research."`). There is therefore no `174-RESEARCH.md` and no
`174-VALIDATION.md` this run. That absence is recorded, not fabricated around.

**Edge-probe accounting (no SPEC exists for this phase).** The deterministic edge probe returned
7 rows, all `unclassified`. Per the specless fallback, an `unclassified` row stays `unresolved` and
is never auto-resolved with a backstop. **7 surfaced == 0 authored as resolved truths + 7 flagged as
unresolved assumptions.** The flagged rows are ROOM-01, ROOM-02, ROOM-03, ROOM-04, ROOM-05, ROOM-06,
FID-01.

The `must_haves.truths` above are **roadmap-sourced**, derived from the seven Success Criteria in
the ROADMAP's Phase 174 section — which are already written as concrete, checkable acceptance
criteria. They are *not* probe-resolved rows. Do not read a truth in this file as evidence that any
probe row was resolved; all 7 remain unresolved planner assumptions.

**Prohibition accounting.** 10 prohibitions were recalled in-prompt. 3 were dropped by the
canon-referral rule with a breadcrumb rather than authored: OWASP Top Ten web-application
prohibitions, path-traversal prohibitions, and injection prohibitions — all are covered by existing
project canon and none is specific to this phase's shell-harness and Mix-task scope. **7 authored
here (descriptor-less, each disposing flagged-unverified) + 3 canon-referred == 10 recalled.**
</planner_notes>

<assumption_delta_decision>
- **Primary noun:** clean-room host generation.
- **Decision:** `add-alongside`, accepted as recorded debt.
- **Rationale:** this phase teaches the legacy positional path what the matrix path already knows,
  rather than promoting the matrix path to be the only host generator. Promotion is already tracked
  as **CONS-01** in `REQUIREMENTS.md` ("Delete the legacy positional clean-room path once the
  backport has proven stable across a release cycle"), and deleting the path before it has run green
  against a live release once would destroy the very evidence CONS-01 asks for.
- **What would force a later promote:** a third divergence between the two paths, or one release
  cycle of green legacy runs — whichever comes first.
</assumption_delta_decision>

<scope_boundary>
This plan changes **only** the legacy positional code path inside
`script/verify_companion_cleanroom.sh` (everything from the `SCRIPT_DIR=` assignment onward, Steps
1 through 7) plus one new test file and one committed run log.

Explicitly NOT in scope:

- **The matrix path is byte-unchanged.** `matrix_write_host`, `matrix_write_smoke`,
  `matrix_registration_check`, and the profile loop are read as the reference implementation and are
  not edited. The matrix path today also does not run `mix crosswake.install`; extending it is
  deliberately deferred, because those 10 invocations sit on the pull-request critical path while
  the legacy path runs post-publish only. ROADMAP SC#2 names the legacy path alone. Record this
  deferral in the SUMMARY so it is a decision on disk rather than an omission.
- **`lib/crosswake/doctor/doctor.ex` is not touched by this plan at all.** Plan 174-02 owns the
  guard that proves it.
- Workflow files. Plan 174-03 owns the CI rehearsal lane and the CI-captured evidence.
- Threadline and sigra diagnosis. Plan 174-04 owns those, one finding each.
</scope_boundary>

<objective>
Wire one clean-room profile end to end through every layer the legacy path touches — host
generation, Hex dependency resolution, a route carrying capability metadata, the adopter's own
`mix crosswake.install`, `doctor`, and grep-able `step=` logging — and prove it by running it for
real against the live published `crosswake_rindle 0.1.0`.

Purpose: `TODO-011` records that the companion clean-room lane has never been green, and that the
rindle failure is the harness's own fault: a routeless router stub produces a manifest with no
`:routes` section, so `manifest_contract` blocks. The two available repairs are not equivalent, and
`TODO-011` recommends the stronger one — make the host realistic — precisely so that a contract is
not weakened to make a harness pass.

Output: a legacy path whose generated host does what an adopter does, and a committed log of a real
run proving it, rather than a script that looks right on inspection.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/workstreams/quality-ratchet-release/ROADMAP.md
@.planning/workstreams/quality-ratchet-release/VERIFICATION-CONVENTIONS.md
@.planning/todos/TODO-011-companion-cleanroom-lane-has-never-been-green.md
</context>

<tasks>

<task type="tracer">
  <name>Task 1: One legacy profile, end to end — real route, real install, real doctor, real run</name>
  <precondition>Network access to hex.pm is available, an `asdf`-managed Erlang/Elixir matching `.tool-versions` is installed, and `crosswake_rindle 0.1.0` is live on Hex. All three are required because this task's verification is an actual execution of the harness, not an inspection of it.</precondition>
  <files>script/verify_companion_cleanroom.sh, .planning/workstreams/quality-ratchet-release/phases/174-clean-room-host-realism-adopter-fidelity/evidence/174-legacy-rindle-local-run.log</files>
  <read_first>
    - `script/verify_companion_cleanroom.sh` lines 295-419 — `matrix_write_host`, the reference implementation. Read the third `python3` heredoc in particular: it is the block that rewrites the generated router's `get "/"` route to carry a `metadata: %{crosswake: [...]}` keyword list. Note the exact four metadata keys and their values.
    - `script/verify_companion_cleanroom.sh` lines 1037-1200 — legacy Steps 1 through 4, including the `mix new "$APP_NAME" --sup` host generation, the two `mix.exs` deps-patching heredocs, and the Step 4 router-stub heredoc that is the defect `TODO-011` names.
    - `script/verify_companion_cleanroom.sh` lines 1386-1456 — legacy Steps 5, 6 and 7, so the new install step lands in the right place relative to the runtime-config append and the `mix crosswake.doctor --router CleanRoomHost.Router` invocation.
    - `script/verify_companion_cleanroom.sh` lines 777-800 — the `log`, `ok` and `fail` helpers and the `[crosswake]` message convention every new emission must follow.
    - `lib/mix/tasks/crosswake.install.ex` lines 25-140 and 178-248 — the switch list, and `infer_router_path!/1`, `infer_web_module!/1`, `infer_endpoint_path/1`, `patch_endpoint/2`. These decide whether the installer can run against a `mix new --sup` host at all, and which flags must be passed explicitly.
    - `lib/crosswake/doctor/doctor.ex` lines 234-277 — `load_install_manifest/1`, the advisory warning the install step exists to satisfy. Read it; do not edit it.
  </read_first>
  <action>
Make the legacy positional path generate the host an adopter actually has, and prove it with one
real run of one profile.

**1a. Give the generated router a real Crosswake route (ROOM-01).**
Replace the Step 4 router-stub heredoc so the emitted `CleanRoomHost.Router` declares one route
carrying `metadata: %{crosswake: [...]}` with the same four keys `matrix_write_host` already uses —
`id`, `runtime`, `offline`, `security`. Use a distinct route id for this path so a log or manifest
read later cannot confuse the two hosts; `legacy-clean-room-home` is the id to use. Keep the
existing `use Phoenix.Router` line and keep the module name `CleanRoomHost.Router`, because Step 7's
`--router CleanRoomHost.Router` argument and the installer's web-module inference both depend on it.
The route's controller and action name only need to be resolvable at compile time, exactly as in the
matrix host.

The comment currently sitting in that heredoc records the assumption `TODO-011` disproved. It goes
away with the stub it annotates; replace it with a comment naming `TODO-011` and ROOM-01 as the
reason this route exists.

**1b. Run the adopter's installer before `doctor` (ROOM-02).**
Add a new step between the existing Step 6 (runtime config) and Step 7 (`doctor`), numbered Step 6.5
or renumbered coherently — pick one and be consistent, and say which in the SUMMARY. It runs
`mix crosswake.install` against the generated host with an explicit `--router` path
(`lib/clean_room_host/router.ex`), because the installer's own `infer_router_path!/1` globs
`lib/*_web/router.ex` and this host has no `_web` directory. Pass `--web-module` and
`--policy-module` explicitly as well if, after reading `infer_web_module!/1`, inference from
`CleanRoomHost.Router` does not produce a usable value — decide from the source you read, not from a
guess, and record which flags you passed and why in the SUMMARY.

This host has no endpoint file; `patch_endpoint/2`'s `nil` clause prints a notice rather than
failing, which is the correct adopter-facing behavior and must not be worked around.

Recompile after the installer patches the router, then let Step 7 run `doctor` unchanged. `doctor`'s
invocation, its flags, and the assertion that it exits zero are all unchanged — the whole point of
this phase is that the harness moved and the contract did not.

The installer must not be invoked with any failure-swallowing construct: if it fails, the run fails.

**1c. Emit `step=` markers at matrix granularity (ROOM-04).**
Every stage of the legacy path emits one machine-grep-able line in addition to whatever human line
it already prints. The line shape mirrors the matrix path's:
`[crosswake] source_mode=legacy-positional package=$PACKAGE version=$VERSION step=<name>`.

The marker roster is exactly these nine names, in this order:
`metadata`, `generate`, `deps`, `compile`, `router`, `smoke`, `register`, `install`, `doctor`.
Emit each exactly once per run. Do not remove or reword any existing human-readable `Step N:` line —
this is additive logging, and the existing lines are what a human reads.

Declare the roster once, near the top of the legacy section, as a single shell array constant named
`LEGACY_STEP_MARKERS`, and emit through one small helper that takes a step name. A helper plus a
declared roster is what makes drift between the two paths a change to one line rather than a silent
omission.

**1d. Run it for real, and keep the log.**
Execute `bash script/verify_companion_cleanroom.sh crosswake_rindle 0.1.0 rindle Rindle` with
`ASDF_ERLANG_VERSION` and `ASDF_ELIXIR_VERSION` pinned from `.tool-versions`, redirecting combined
stdout and stderr into
`.planning/workstreams/quality-ratchet-release/phases/174-clean-room-host-realism-adopter-fidelity/evidence/174-legacy-rindle-local-run.log`
and reading the script's own exit status — not a pipeline's. Commit that log.

If the run fails, read the failure and fix the **harness**. If the failure is genuinely in the
published package rather than the harness, stop and report it rather than editing the harness around
it; that is a ROOM-05-shaped finding and plan 174-04 owns findings.

Stage the log by its explicit path. Do not stage anything else under `.planning/`.
  </action>
  <acceptance_criteria>
    - The committed log contains a line matching `step=install` and a line matching `step=doctor`, and the `step=install` line appears before the `step=doctor` line (measured by line number, not by eye).
    - The committed log contains all nine declared marker names, each at least once; the measured count of distinct markers found is 9 and is stated in the SUMMARY.
    - The committed log contains the installer's own output, demonstrating the install step executed rather than merely being present in the script.
    - The committed log's final line is the script's `state=passed` completion line, and the recorded exit status of the run is 0.
    - `grep -v '^[[:space:]]*#' script/verify_companion_cleanroom.sh | grep -c 'metadata: %{'` returns 2 — one occurrence in the matrix reference implementation, one newly in the legacy router stub.
    - `grep -v '^[[:space:]]*#' script/verify_companion_cleanroom.sh | grep -c 'mix crosswake.install'` returns 1.
    - `git diff --stat` for this task lists `script/verify_companion_cleanroom.sh` and the evidence log and no other source file; in particular `lib/crosswake/doctor/doctor.ex` is absent from the diff.
    - The matrix path region of the script is unchanged: `git diff` shows no hunk inside `matrix_write_host`, `matrix_write_smoke`, `matrix_registration_check`, or the profile loop.
    - `shellcheck script/verify_companion_cleanroom.sh` reports no error-level finding.
  </acceptance_criteria>
  <verify>
    <automated>shellcheck -S error script/verify_companion_cleanroom.sh</automated>
    <fails_when>non-zero exit</fails_when>
    <automated>bash -n script/verify_companion_cleanroom.sh</automated>
    <fails_when>non-zero exit</fails_when>
    <automated>grep -c 'step=doctor' .planning/workstreams/quality-ratchet-release/phases/174-clean-room-host-realism-adopter-fidelity/evidence/174-legacy-rindle-local-run.log</automated>
    <fails_when>the printed count is 0, or the file does not exist</fails_when>
    <automated>grep -c 'step=install' .planning/workstreams/quality-ratchet-release/phases/174-clean-room-host-realism-adopter-fidelity/evidence/174-legacy-rindle-local-run.log</automated>
    <fails_when>the printed count is 0</fails_when>
  </verify>
  <reversibility rating="reversible">All changes are contained to one shell script's legacy section plus a committed log; reverting is a single-file revert and touches no published artifact and no live workflow.</reversibility>
  <done>A real run of the legacy path against live `crosswake_rindle 0.1.0` generates a host with a metadata-bearing route, runs the adopter's installer, reaches `doctor`, exits zero, and its log is committed.</done>
</task>

<task type="auto">
  <name>Task 2: The marker roster is asserted against the real log, from an independent source</name>
  <files>test/crosswake/proof/phase174_cleanroom_host_realism_test.exs</files>
  <read_first>
    - `test/crosswake/proof/phase172_per_package_ref_test.exs` — the conventions for a phase-proof test in this directory: module naming, how it locates repository-relative paths, and how it asserts on measured counts rather than on prose.
    - `test/crosswake/proof/phase170_vacuous_assertion_ledger_test.exs` — the house pattern for a roster-completeness assertion that itself fails on an empty roster.
    - The `LEGACY_STEP_MARKERS` constant introduced by Task 1, and the committed evidence log, so the test asserts against what was actually emitted.
  </read_first>
  <behavior>
    - Given the committed run log, every one of the nine declared markers is found at least once.
    - The roster the test checks is a literal list declared inside the test module; the test fails if that list has any length other than nine, so a future edit that empties it cannot make the check vacuous.
    - The install marker's first occurrence precedes the doctor marker's first occurrence in the log.
    - Given a fixture log with one marker removed, the check fails and names the missing marker.
    - Given an empty fixture log, the check fails rather than passing over an empty collection.
  </behavior>
  <action>
Add `Crosswake.Proof.Phase174CleanRoomHostRealismTest` under `test/crosswake/proof/`.

Declare the nine-name marker roster as a module attribute **in the test file**, authored
independently. Do not parse it out of `script/verify_companion_cleanroom.sh`: a roster derived from
the artifact under test would silently shrink to whatever the script happens to emit, which is the
scope-selector defect recorded as SEED-019, and the check would then pass by construction for every
marker the script forgot.

Assert, against the committed evidence log:

- the roster length is exactly nine (an assertion on the roster itself, so an emptied roster is a
  red test rather than a vacuous green);
- every roster entry appears in the log at least once, with the failure message naming the specific
  missing entries rather than reporting a bare boolean;
- the ordering property between the install and doctor markers, by first-occurrence line index.

Then prove the check is non-vacuous by two fixture cases written into a temp directory: a log with
one marker deleted must fail and must name that marker, and an empty log must fail. Record in the
SUMMARY which marker you deleted for the mutation and that the empty case was exercised — a measured
mutation, not a claim that the check "looks correct".

Also assert the parity claim ROADMAP SC#4 actually makes: extract the distinct `step=` names emitted
by the matrix path region of the script and assert the legacy roster is not a strict subset of a
smaller set — specifically, assert the legacy roster's cardinality is greater than or equal to the
matrix path's distinct marker count. Granularity parity is the claim; an equal-or-finer legacy
roster is what satisfies it.
  </action>
  <acceptance_criteria>
    - `mix test test/crosswake/proof/phase174_cleanroom_host_realism_test.exs` exits 0 with a summary line reporting 0 failures.
    - The test file contains a literal nine-entry roster and an assertion on its length; deleting an entry from that literal makes the test red (state in the SUMMARY that this was exercised).
    - The one-marker-removed fixture case and the empty-log fixture case both exist as named tests and both assert a failure, not a truthy result.
    - The test reads the committed evidence log by repository-relative path and fails with a named message if that file is absent — absence is never a pass.
    - No assertion in the file greps `script/verify_companion_cleanroom.sh` for the roster.
    - `mix format --check-formatted` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>mix test test/crosswake/proof/phase174_cleanroom_host_realism_test.exs --max-cases 1</automated>
    <fails_when>non-zero exit, or the summary line reports any number of failures other than 0, or the summary line reports 0 tests</fails_when>
    <automated>mix format --check-formatted</automated>
    <fails_when>non-zero exit</fails_when>
  </verify>
  <reversibility rating="reversible">A new test file only; removing it restores the prior state exactly.</reversibility>
  <done>The nine-marker roster is asserted against a real run's log from an independently authored list, and the assertion has been shown to go red under a deliberate mutation and against an empty log.</done>
</task>

</tasks>

<artifacts_produced>
## Artifacts this phase produces (plan 174-01)

| Kind | Name |
|---|---|
| Shell array constant | `LEGACY_STEP_MARKERS` in `script/verify_companion_cleanroom.sh` |
| Shell helper function | the single `step=` marker emitter introduced by Task 1c |
| Log marker names | `step=metadata`, `step=generate`, `step=deps`, `step=compile`, `step=router`, `step=smoke`, `step=register`, `step=install`, `step=doctor` |
| Log field | `source_mode=legacy-positional` |
| Route id | `legacy-clean-room-home` (the legacy host's Crosswake route) |
| New command invocation | `mix crosswake.install --router lib/clean_room_host/router.ex` inside the legacy path |
| New file | `test/crosswake/proof/phase174_cleanroom_host_realism_test.exs` |
| New module | `Crosswake.Proof.Phase174CleanRoomHostRealismTest` |
| New file | `.planning/.../174-clean-room-host-realism-adopter-fidelity/evidence/174-legacy-rindle-local-run.log` |
</artifacts_produced>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| hex.pm -> clean-room host | Package bytes fetched from a public registry are compiled and executed inside the harness |
| Harness-generated host -> `doctor` | The harness authors the very host whose contract `doctor` then grades |
| Run log -> phase evidence | A locally produced log becomes committed evidence for three success criteria |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-174-01 | Tampering | `doctor`'s `manifest_contract` contract weakened to make the harness pass | high | mitigate | This plan's scope boundary forbids touching `lib/crosswake/doctor/doctor.ex`, acceptance requires that file's absence from the diff, and plan 174-02 lands a byte-identity guard over the check's source. |
| T-174-02 | Repudiation | A committed evidence log that does not correspond to a real run | high | mitigate | Task 1 requires the log to be the redirected output of the actual invocation and to end with the script's own completion line; Task 2 asserts nine independently declared markers are present in it, which a hand-written log would not reproduce by accident. |
| T-174-03 | Spoofing | A marker roster derived from the script it polices, silently excluding forgotten markers | high | mitigate | Task 2 declares the roster literally inside the test, asserts its cardinality, and forbids grepping the script for it. |
| T-174-04 | Denial of Service | The installer failing inside the harness and being worked around rather than reported | medium | mitigate | Task 1b forbids failure-swallowing constructs around the installer and routes package-side failures to plan 174-04 as findings instead of harness edits. |
| T-174-05 | Information Disclosure | Adopter identity leaking into committed evidence | medium | mitigate | A standing prohibition in `must_haves`; the codename First B2C Adopter is the only permitted reference, and the clean-room log contains no adopter-derived input. |
| T-174-SC | Tampering | npm/pip/cargo installs | high | mitigate | Not applicable to this plan — it adds no package-manager install step. `mix deps.get` and `mix archive.install` invocations in this file are pre-existing and unchanged. If any task later requires a new package-manager install, a package-legitimacy gate plus a blocking human checkpoint is required first. |
</threat_model>

<verification>
- `bash -n script/verify_companion_cleanroom.sh` exits 0.
- `shellcheck -S error script/verify_companion_cleanroom.sh` reports no error-level finding.
- `mix test test/crosswake/proof/phase174_cleanroom_host_realism_test.exs --max-cases 1` exits 0 with 0 failures and a non-zero test count.
- `mix format --check-formatted` exits 0.
- The committed evidence log exists, ends in the script's completion line, and contains all nine markers.
</verification>

<success_criteria>
The legacy positional clean-room path generates a host with a real metadata-bearing Crosswake route,
runs `mix crosswake.install`, reaches `doctor`, and exits zero against the live published
`crosswake_rindle 0.1.0` — proven by a committed log of an actual run, with `doctor`'s contract
untouched and the log's `step=` markers asserted against an independently authored roster.
</success_criteria>

<output>
Create `.planning/workstreams/quality-ratchet-release/phases/174-clean-room-host-realism-adopter-fidelity/174-01-SUMMARY.md` when done.
</output>
