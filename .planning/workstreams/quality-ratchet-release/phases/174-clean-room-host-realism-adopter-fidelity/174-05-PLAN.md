---
phase: 174-clean-room-host-realism-adopter-fidelity
plan: 05
type: execute
wave: 3
depends_on: ["174-01", "174-04"]
files_modified:
  - .planning/workstreams/quality-ratchet-release/phases/174-clean-room-host-realism-adopter-fidelity/174-FINDING-THREADLINE.md
  - .planning/workstreams/quality-ratchet-release/phases/174-clean-room-host-realism-adopter-fidelity/174-FINDING-SIGRA.md
  - .planning/workstreams/quality-ratchet-release/phases/174-clean-room-host-realism-adopter-fidelity/evidence/174-threadline-run.log
  - .planning/workstreams/quality-ratchet-release/phases/174-clean-room-host-realism-adopter-fidelity/evidence/174-sigra-run.log
  - test/crosswake/proof/phase174_companion_findings_test.exs
  - .planning/todos/TODO-011-companion-cleanroom-lane-has-never-been-green.md
autonomous: true
requirements: [ROOM-05]

estimate:
  tokens: 120000
  raw_tokens: 60000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "The threadline clean-room failure has its own separately recorded root-cause finding, derived from a run taken in this phase rather than assumed fixed by the backport (ROADMAP SC#5, ROOM-05)."
    - "The sigra clean-room failure has its own separately recorded root-cause finding, distinct from threadline's — two findings, never one covering both (ROADMAP SC#5, ROOM-05)."
    - "Each finding records a named root cause and states whether the run is now green or still red; the criterion is satisfied by two distinct written findings in either outcome (ROADMAP SC#5, ROOM-05)."
    - "The requirement that both findings exist and name distinct root causes is checked mechanically, with the companion roster derived from TODO-011's recorded failure table rather than from the findings directory (ROOM-05)."
  prohibitions:
    - "Never write one finding covering both threadline and sigra; the lumping is the specific failure ROADMAP SC#5 forbids."
    - "Never record a companion as fixed because the backport plausibly addressed it; a finding rests on a run taken in this phase or on an explicitly stated inability to take one."
    - "Never weaken, narrow, or disconnect `doctor`'s `manifest_contract` check, or any other contract, to turn a companion run green."
    - "Never derive the finding roster from the findings directory; derive it from TODO-011's recorded failure table, an independent source."
    - "Never write a verification gate whose exit status is taken from the tail of a pipeline; a pipeline reports the last command's status and the gate would be green while asserting nothing."
    - "Never run `git add -A` or `git add .` under `.planning/`; stage `.planning/` paths explicitly by filename, and never stage `config.json`, `milestone.lock`, or `state.json` under the workstream directory."
    - "Never run `git stash` in any form, and never run `git reset --hard`, `git clean -fd`, or a checkout of tracked paths this plan did not create."
    - "Never name the early-adopter SaaS in any artifact; the codename is First B2C Adopter."
  artifacts:
    - .planning/workstreams/quality-ratchet-release/phases/174-clean-room-host-realism-adopter-fidelity/174-FINDING-THREADLINE.md
    - .planning/workstreams/quality-ratchet-release/phases/174-clean-room-host-realism-adopter-fidelity/174-FINDING-SIGRA.md
    - test/crosswake/proof/phase174_companion_findings_test.exs
  key_links:
    - "TODO-011's dated failure table -> phase174_companion_findings_test.exs's roster -> the two finding files and their recorded root causes."
    - "the clean-room rehearsal workflow (plan 174-04) -> threadline and sigra runs -> the two captured logs -> the two findings."
---

<planner_notes>
**Nyquist Dimension 8 is not applicable to this phase** — the ROADMAP's Phase 174 **Research** line
records the decision to skip phase-level research, so there is no `174-RESEARCH.md` and therefore no
`174-VALIDATION.md`. Recorded, not fabricated around.

**Edge-probe accounting.** 7 probe rows surfaced, all `unclassified` and therefore `unresolved`.
**7 surfaced == 0 authored as resolved truths + 7 flagged as unresolved assumptions** (ROOM-01
through ROOM-06 and FID-01). The truths above are **roadmap-sourced** from Success Criterion 5, not
probe-resolved.

**Prohibition accounting.** 11 recalled in-prompt; **8 authored here descriptor-less + 3
canon-referred** (OWASP Top Ten, path traversal, injection — existing project canon, not specific to
this plan) **== 11**.
</planner_notes>

<scope_boundary>
This plan diagnoses and records. It does not repair threadline or sigra packages: a package-side root
cause is written down, and whether to fix it is a decision for a later phase with its own publish
path. If the diagnosis surfaces a *harness* defect, repairing it here is in scope and must be stated
as such in the finding — but `doctor`'s contract remains off-limits, and plan 174-02's guard is
re-run at the end of this plan to prove it.
</scope_boundary>

<objective>
Give the threadline and sigra clean-room failures each their own root-cause finding, taken from runs
made in this phase.

Purpose: `TODO-011` is explicit — "the threadline and sigra failures above are separate causes and
must be diagnosed on their own; fixing the rindle cause will not turn the lane green by itself."
ROADMAP SC#5 turns that into a structural requirement: two distinct written findings, satisfied
whether the runs end red or green. The failure mode being guarded against is the comfortable
assumption that a backport fixed three problems because it fixed one.

Output: two findings, two captured logs, and one mechanical check that neither can go missing.
The phase's vacuity-taxonomy record is plan 174-06's job.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/workstreams/quality-ratchet-release/ROADMAP.md
@.planning/workstreams/quality-ratchet-release/VERIFICATION-CONVENTIONS.md
@.planning/todos/TODO-011-companion-cleanroom-lane-has-never-been-green.md
@.planning/workstreams/quality-ratchet-release/phases/174-clean-room-host-realism-adopter-fidelity/174-CLEANROOM-EVIDENCE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Threadline — its own run, its own root cause, its own finding</name>
  <precondition>`crosswake_threadline` is live on hex.pm at the version under diagnosis, and either `gh` is authenticated to dispatch the rehearsal workflow or the local runtime can execute the harness against hex.pm directly. The 2026-07-03 failing run (28714131245) is older than the default log retention window, so its original log may no longer be readable; the diagnosis rests on a run taken now.</precondition>
  <files>.planning/workstreams/quality-ratchet-release/phases/174-clean-room-host-realism-adopter-fidelity/174-FINDING-THREADLINE.md, .planning/workstreams/quality-ratchet-release/phases/174-clean-room-host-realism-adopter-fidelity/evidence/174-threadline-run.log</files>
  <read_first>
    - `.planning/todos/TODO-011-companion-cleanroom-lane-has-never-been-green.md` — the failure table, and specifically the threadline row: 2026-07-03, described as a module-shipment variant and explicitly **not diagnosed** there.
    - `script/verify_companion_cleanroom.sh` legacy Steps 5 and 6 — the threadline branches. Threadline is not a `Crosswake.Companion` implementor: the harness suppresses the companion-behaviour assertions and substitutes three module-shipment canaries, and it deliberately does not register threadline under `:companions`. That divergence is where a module-shipment failure would land.
    - `script/verify_companion_cleanroom.sh` matrix `matrix_write_smoke`'s threadline branch — the matrix path's equivalent canaries, so you can tell a harness divergence from a package defect.
    - `.planning/workstreams/quality-ratchet-release/phases/173-recovery-path-proof-convergence/173-NON-VACUITY.md` — the house standard for recording a real-run observation.
  </read_first>
  <action>
Determine the published `crosswake_threadline` versions from the registry, then take a run: the
version that failed on 2026-07-03 if it is still resolvable, and the current latest. Say which
versions you ran and why in the finding — "the version that failed" and "the version an adopter would
get today" are different questions and both matter.

Run it through the clean-room harness (the plan 174-04 rehearsal workflow, or the local invocation if
dispatch is unavailable), capture the combined output into `evidence/174-threadline-run.log` by
redirecting to the file and reading the command's own exit status, and commit the log.

Write `174-FINDING-THREADLINE.md` with, at minimum: the versions run, how they were run (run id and
URL if CI, or the local invocation), the observed outcome, the **named root cause**, whether the
cause is harness-side or package-side, and the current status — green, or still red with the cause
identified.

If the run is now green, say so and say **why** the backport made it green, naming the specific
mechanism. "It passes now" without a mechanism is an assumption dressed as a finding, and the next
person to hit a red threadline run would have nothing to work from.

If the run cannot be taken at all, that is itself the finding: state what blocked it, what would
unblock it, and mark the root cause explicitly as undetermined. Undetermined-and-said-so is a valid
finding; silence is not.

Do not mention sigra in this file beyond, at most, a breadcrumb line pointing at the sibling finding.
</action>
  <acceptance_criteria>
    - `174-FINDING-THREADLINE.md` exists and names: the versions run, the invocation (run id/URL or local command), the observed outcome, a named root cause (or an explicit undetermined marker with the blocker), the harness-side/package-side classification, and the current status.
    - `evidence/174-threadline-run.log` exists, is non-empty, and corresponds to the run the finding describes.
    - The finding contains no claim that threadline was fixed by the rindle backport unless it names the mechanism and cites the run that shows it.
    - The finding's body concerns threadline only; sigra appears at most as a breadcrumb link.
  </acceptance_criteria>
  <verify>
    <automated>test -s .planning/workstreams/quality-ratchet-release/phases/174-clean-room-host-realism-adopter-fidelity/evidence/174-threadline-run.log</automated>
    <fails_when>non-zero exit — the log is missing or empty</fails_when>
    <automated>grep -c 'root cause' .planning/workstreams/quality-ratchet-release/phases/174-clean-room-host-realism-adopter-fidelity/174-FINDING-THREADLINE.md</automated>
    <fails_when>the printed count is 0, or the file does not exist</fails_when>
  </verify>
  <reversibility rating="reversible">A diagnosis and two documents; no package, contract or workflow is changed.</reversibility>
  <done>Threadline has a root-cause finding of its own, resting on a run taken in this phase.</done>
</task>

<task type="auto">
  <name>Task 2: Sigra — its own run, its own root cause, its own finding</name>
  <precondition>`crosswake_sigra` is live on hex.pm at the version under diagnosis. The 2026-08-09 failing run (31325689640) predates the default log retention window, so its original log may no longer be readable; the diagnosis rests on a run taken now.</precondition>
  <files>.planning/workstreams/quality-ratchet-release/phases/174-clean-room-host-realism-adopter-fidelity/174-FINDING-SIGRA.md, .planning/workstreams/quality-ratchet-release/phases/174-clean-room-host-realism-adopter-fidelity/evidence/174-sigra-run.log</files>
  <read_first>
    - `.planning/todos/TODO-011-companion-cleanroom-lane-has-never-been-green.md` — the sigra row: 2026-08-09, failing in the smoke test at `test/smoke_test.exs:21`. That is a *different layer* from the rindle failure, which was at `doctor`.
    - `script/verify_companion_cleanroom.sh` legacy Step 5 — the no-engine smoke-test branch the sigra profile takes, and specifically which assertion sits around line 21 of the generated smoke test. The generated file is the subject; count lines in the emitted test, not in the script.
    - `script/verify_companion_cleanroom.sh` matrix `matrix_write_smoke`'s sigra branch — the matrix path asserts an allow projection and a closed denial. Compare the two paths' sigra assertions; a divergence between them is a strong candidate root cause.
    - `.planning/seeds/SEED-014-proof-lane-and-doctor-fidelity.md` "Loose end — CLOSED 2026-09-15" — records that `crosswake_sigra 0.1.3` published and resolves from the registry, which bears on which version to run.
  </read_first>
  <action>
Determine the published `crosswake_sigra` versions, then take a run at the version that failed on
2026-08-09 if still resolvable and at the current latest, capturing the combined output into
`evidence/174-sigra-run.log` by redirecting to the file and reading the command's own exit status.

Diagnose the smoke-test failure specifically. The legacy path's generated smoke test and the matrix
path's sigra smoke test assert different things; establish which assertion failed, in which generated
file, at which line, and whether the cause is a harness assertion that does not match the package's
real public seam (harness-side) or a genuine regression in the package (package-side).

Write `174-FINDING-SIGRA.md` in the same shape as the threadline finding: versions run, invocation,
observed outcome, named root cause, harness-side/package-side classification, current status.

The sigra root cause must be stated independently of threadline's. If the two turn out to share a
cause, say so explicitly in both files and name the shared cause in each — SC#5 forbids one finding
covering both, not two findings that agree.

Do not mention threadline in this file beyond, at most, a breadcrumb line pointing at the sibling
finding.
  </action>
  <acceptance_criteria>
    - `174-FINDING-SIGRA.md` exists and names: the versions run, the invocation, the observed outcome, a named root cause (or an explicit undetermined marker with the blocker), the harness-side/package-side classification, and the current status.
    - The finding identifies the specific failing assertion — file, line in the generated test, and what it asserted — or states explicitly why that could not be determined.
    - `evidence/174-sigra-run.log` exists, is non-empty, and corresponds to the run the finding describes.
    - The finding's body concerns sigra only; threadline appears at most as a breadcrumb link.
  </acceptance_criteria>
  <verify>
    <automated>test -s .planning/workstreams/quality-ratchet-release/phases/174-clean-room-host-realism-adopter-fidelity/evidence/174-sigra-run.log</automated>
    <fails_when>non-zero exit — the log is missing or empty</fails_when>
    <automated>grep -c 'root cause' .planning/workstreams/quality-ratchet-release/phases/174-clean-room-host-realism-adopter-fidelity/174-FINDING-SIGRA.md</automated>
    <fails_when>the printed count is 0, or the file does not exist</fails_when>
  </verify>
  <reversibility rating="reversible">A diagnosis and two documents; no package, contract or workflow is changed.</reversibility>
  <done>Sigra has a root-cause finding of its own, naming the failing assertion and resting on a run taken in this phase.</done>
</task>

<task type="auto">
  <name>Task 3: The two findings cannot silently become one, or zero</name>
  <files>test/crosswake/proof/phase174_companion_findings_test.exs, .planning/todos/TODO-011-companion-cleanroom-lane-has-never-been-green.md</files>
  <read_first>
    - `.planning/todos/TODO-011-companion-cleanroom-lane-has-never-been-green.md` — the dated failure table. Its companion column is the independent roster source for this check.
    - `test/crosswake/proof/phase170_vacuous_assertion_ledger_test.exs` — the house pattern for a ledger-completeness assertion that fails on an empty roster.
    - The two finding files as written by Tasks 1 and 2.
  </read_first>
  <behavior>
    - The roster of companions requiring a finding is discovered from TODO-011's failure table and has at least the two members threadline and sigra; a discovered roster of zero fails.
    - Every roster member has a finding file of its own, and the check names any member that does not.
    - The findings' recorded root-cause statements are compared; the check fails if the two files are byte-identical, which is what a single lumped finding copied twice would look like.
    - Given a fixture with one finding file removed, the check fails and names the missing companion.
  </behavior>
  <action>
Add `Crosswake.Proof.Phase174CompanionFindingsTest` under `test/crosswake/proof/`.

Discover the roster from TODO-011's failure table. That table is the independent source: deriving the
roster from the findings directory would make the check pass for exactly the findings that exist,
which is the scope-selector defect recorded as SEED-019 and would score the absence of a finding as
success — this project's named central defect.

Assert the discovered roster is non-empty and contains at least threadline and sigra before checking
anything else. Then assert each member has its own finding file, and that the two files are not
byte-identical.

Prove non-vacuity with a fixture run: point the discovery at a temp copy of the phase directory with
one finding removed, and assert the check fails naming that companion. Record which one you removed
and the observed message in the SUMMARY.

Then update `TODO-011` itself: add a dated line under each of the threadline and sigra rows pointing
at its finding, and record the todo's current status. Do not delete the original table — it is the
roster source this check depends on, and it is the historical record.
  </action>
  <acceptance_criteria>
    - `mix test test/crosswake/proof/phase174_companion_findings_test.exs` exits 0 with 0 failures and at least 3 tests.
    - The roster is discovered from TODO-011, not from the findings directory; a non-emptiness assertion runs before the per-member checks.
    - The byte-identity comparison between the two findings exists as a named test.
    - The one-finding-removed fixture case exists as a named test and names the missing companion in its failure.
    - TODO-011 carries a dated pointer line for each of threadline and sigra, and its original failure table is intact.
    - `mix format --check-formatted` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>mix test test/crosswake/proof/phase174_companion_findings_test.exs --max-cases 1</automated>
    <fails_when>non-zero exit, or the summary line reports any number of failures other than 0, or fewer than 3 tests ran</fails_when>
    <automated>mix format --check-formatted</automated>
    <fails_when>non-zero exit</fails_when>
  </verify>
  <reversibility rating="reversible">A new test file and an append to a todo; nothing executable changes.</reversibility>
  <done>A missing or lumped companion finding is a red test, checked against a roster that comes from outside the thing being checked.</done>
</task>

</tasks>

<artifacts_produced>
## Artifacts this phase produces (plan 174-05)

| Kind | Name |
|---|---|
| New file | `.planning/.../174-FINDING-THREADLINE.md` |
| New file | `.planning/.../174-FINDING-SIGRA.md` |
| New files | `.planning/.../evidence/174-threadline-run.log`, `.planning/.../evidence/174-sigra-run.log` |
| New file | `test/crosswake/proof/phase174_companion_findings_test.exs` |
| New module | `Crosswake.Proof.Phase174CompanionFindingsTest` |
</artifacts_produced>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| hex.pm -> clean-room host | Published companion bytes are compiled and executed inside the harness |
| Diagnostic runs -> committed findings | A run's output becomes the in-repo root-cause record two later phases will rely on |
| TODO-011's table -> the findings check roster | A human-maintained table is the independent source a mechanical check depends on |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-174-20 | Repudiation | A companion recorded as fixed by assumption rather than by a run | high | mitigate | Tasks 1 and 2 each require a run taken in this phase with its log committed, and forbid a fixed claim that does not name the mechanism and cite the run. |
| T-174-21 | Spoofing | One lumped finding presented as two | high | mitigate | Task 3 asserts the two files are not byte-identical and checks each roster member has its own file; Tasks 1 and 2 forbid cross-mentions beyond a breadcrumb. |
| T-174-22 | Tampering | The findings roster derived from the findings directory, so a missing finding excludes itself | high | mitigate | Task 3 derives the roster from TODO-011's failure table and asserts non-emptiness before any per-member check, with a fixture proving the removed-finding case red. |
| T-174-23 | Tampering | A red companion run resolved by weakening a contract | high | mitigate | The scope boundary forbids contract edits, and Task 4's acceptance re-runs plan 174-02's byte-identity guard as the closing claim's evidence. |
| T-174-24 | Information Disclosure | Adopter identity entering a finding or a captured log | medium | mitigate | A standing prohibition in `must_haves`; the codename First B2C Adopter is the only permitted reference, and the clean-room logs contain no adopter-derived input. |
| T-174-SC | Tampering | npm/pip/cargo installs | high | mitigate | Not applicable to this plan — it adds no package-manager install step. If any task later requires one, a package-legitimacy gate plus a blocking human checkpoint is required first. |
</threat_model>

<verification>
- `mix test test/crosswake/proof --max-cases 1` exits 0 with 0 failures.
- `bash script/assert_manifest_contract_unchanged.sh` exits 0.
- Both finding files exist, are non-identical, and each names a root cause and a harness-side/package-side classification.
- Both captured run logs exist and are non-empty.
- `mix format --check-formatted` exits 0.
</verification>

<success_criteria>
Threadline and sigra each have their own separately recorded root-cause finding resting on a run
taken in this phase — whether those runs ended red or green — and the absence of either is a red
test checked against a roster from outside the findings themselves.
</success_criteria>

<output>
Create `.planning/workstreams/quality-ratchet-release/phases/174-clean-room-host-realism-adopter-fidelity/174-05-SUMMARY.md` when done.
</output>
