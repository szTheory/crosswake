---
phase: 168-0-2-1-release-candidate-readiness
plan: "03"
type: execute
wave: 3
depends_on: [168-02]
files_modified:
  - lib/crosswake/release_candidate/artifact.ex
  - lib/crosswake/release_candidate/coordinate.ex
  - script/release_candidate/hex_artifacts.sh
  - script/verify_hex_publish_dry_run.sh
  - test/crosswake/release_candidate/artifact_test.exs
  - test/crosswake/release_candidate/coordinate_test.exs
autonomous: true
requirements: [REL-03, REL-04]
estimate:
  tokens: 26000
  raw_tokens: 26000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Before approval, core and all five companions pass Hex audit/dry-run/build, official unpack, canonical file/metadata inspection, and digest capture from the exact candidate ref per D-08."
    - "Candidate consumers receive only invocation-local unpacked package roots, while independent metadata checks prove files, versions, dependency requirements, compatibility floors, and candidate-core `Version.match?/2` per D-09."
    - "Core Hex, iOS, and Android coordinates are linked at `0.2.1`, while companion versions and PRs #115/#146/#147 remain independent and outside this approval per D-15 and D-16."
  artifacts:
    - path: "lib/crosswake/release_candidate/artifact.ex"
      provides: "Closed normalized Hex artifact observations and payload/metadata digests."
    - path: "lib/crosswake/release_candidate/coordinate.ex"
      provides: "Linked core/native tuple and independent companion floor validation."
    - path: "script/release_candidate/hex_artifacts.sh"
      provides: "Narrow isolated Hex dry-run/build/unpack adapter."
  key_links:
    - from: "script/release_candidate/hex_artifacts.sh"
      to: "lib/crosswake/release_candidate/artifact.ex"
      via: "bounded normalized manifest containing package/version/checksum/metadata/file digest/unpacked root"
      pattern: "payload_digest|metadata_digest"
    - from: "lib/crosswake/release_candidate/coordinate.ex"
      to: "release-please-config.json"
      via: "core/iOS/Android linked group plus independently versioned companions"
      pattern: "crosswake-shell-core"
  flagged_assumptions:
    - id: EDGE-REL-03-UNCLASSIFIED
      requirement: REL-03
      status: flagged_unverified
      flagged: true
      assumption: "Manual edge review is covered by mutation fixtures for each coordinate, absent/extra package files, dependency floor boundary versions, malformed requirements, and companion/core version equality; no additional REL-03 edge class is presently known."
---

<objective>
Build and inspect the exact candidate package family, then prove coordinate and compatibility consistency from package metadata.

Purpose: Ensure clean-room proof consumes what would ship rather than repository source.
Output: Six unpacked candidate payloads, normalized digests, and linked-coordinate/floor validators.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/workstreams/quality-ratchet-release/REQUIREMENTS.md
@.planning/workstreams/quality-ratchet-release/phases/168-0-2-1-release-candidate-readiness/168-CONTEXT.md
@.planning/workstreams/quality-ratchet-release/phases/168-0-2-1-release-candidate-readiness/168-RESEARCH.md
@.planning/workstreams/quality-ratchet-release/phases/168-0-2-1-release-candidate-readiness/168-02-SUMMARY.md
@mix.exs
@release-please-config.json
@.release-please-manifest.json
@packages/crosswake-shell-core-ios/Package.swift
@packages/crosswake-shell-core-android/build.gradle.kts

<interfaces>
- The adapter may invoke official Mix/Hex package tasks but returns only normalized observations; `Crosswake.ReleaseCandidate.Artifact` decides acceptance.
- Package roots are execution-time, invocation-local directories and never durable receipt identities; SHA-256 payload/metadata digests are durable.
- `phx_new` is an approved isolated build tool from the research legitimacy audit, not a new project dependency; no npm, pip, or cargo install is introduced.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Produce and inspect six exact candidate Hex payloads</name>
  <read_first>
    - script/verify_hex_publish_dry_run.sh
    - mix.exs
    - packages/crosswake_rulestead/mix.exs
    - packages/crosswake_rindle/mix.exs
    - packages/crosswake_sigra/mix.exs
    - packages/crosswake_chimeway/mix.exs
    - packages/crosswake_threadline/mix.exs
    - lib/crosswake/release_candidate/receipt.ex
  </read_first>
  <files>lib/crosswake/release_candidate/artifact.ex, script/release_candidate/hex_artifacts.sh, script/verify_hex_publish_dry_run.sh, test/crosswake/release_candidate/artifact_test.exs</files>
  <behavior>
    - Exactly `crosswake`, `crosswake_rulestead`, `crosswake_rindle`, `crosswake_sigra`, `crosswake_chimeway`, and `crosswake_threadline` run package audit/publish dry-run/build and official unpack under isolated non-authorizing Hex/Mix homes.
    - Each observation binds candidate ref, package/version, outer checksum, normalized metadata digest, normalized payload/file manifest digest, declared requirements, and an exclusively created unpacked root.
    - Missing/extra/path-escaping/symlink package entries, changed metadata, absent output, zero files, credentials, public fetch fallback, repository-root fallback, or package-set drift blocks readiness.
  </behavior>
  <action>Add hostile artifact fixtures before implementing the adapter. Extend the existing dry-run seam rather than replacing it: run official `mix hex.publish --dry-run`, `mix hex.build --output`, and Hex unpack semantics for core plus five companions, each from the exact candidate worktree with isolated homes and output roots. Normalize rather than archive raw output, compute SHA-256 metadata and payload manifests, and pass only allowlisted observations into the evaluator. Do not create a signed/local Hex repository, use a staging registry, authorize publication, or treat a source/path tree as package evidence per D-07/D-08/D-10.</action>
  <acceptance_criteria>
    - Six and only six candidate packages produce non-empty unpacked payloads and stable normalized digests.
    - Every payload originates from its built tarball and no repository or public-registry fallback can pass.
    - Fixture mutations independently detect file-list, metadata, checksum, path escape, empty package, wrong version, and missing package errors.
  </acceptance_criteria>
  <verify>
    <automated>mix test test/crosswake/release_candidate/artifact_test.exs &amp;&amp; bash script/verify_hex_publish_dry_run.sh</automated>
    <fails_when>non-zero exit, 0 tests pass, package count differs from six, an artifact is empty, or a normalized digest/source assertion is absent</fails_when>
  </verify>
  <done>The exact candidate ref yields six inspected, unpacked, content-addressed Hex payloads without publishing or path-source substitution.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Enforce linked core coordinates and independent companion floors</name>
  <read_first>
    - release-please-config.json
    - .release-please-manifest.json
    - mix.exs
    - packages/crosswake-shell-core-ios/Package.swift
    - packages/crosswake-shell-core-android/build.gradle.kts
    - lib/crosswake/release_status.ex
    - lib/crosswake/release_candidate/artifact.ex
  </read_first>
  <files>lib/crosswake/release_candidate/coordinate.ex, test/crosswake/release_candidate/coordinate_test.exs</files>
  <behavior>
    - Root Hex, iOS package, Android artifact, Release Please manifest, and linked-component config resolve to `0.2.1` for the candidate.
    - Every companion requirement and supported compatibility floor accepts its boundary version and candidate core via `Version.match?/2`; immediately below-floor and malformed requirements fail.
    - Independent companion versions may differ from `0.2.1`; folding PRs #115/#146/#147 or companion release intent into the linked tuple fails.
  </behavior>
  <action>Implement metadata-first coordinate validation using the six unpacked artifact observations plus tracked Release Please, Mix, SwiftPM, and Gradle truth. Assert package file membership, requirements, compatibility floors, and boundary behavior separately so unpacked path dependencies cannot hide bad public metadata per D-09. Encode only Hex/iOS/Android core as the linked `0.2.1` unit per D-15; preserve companion independence and current open proposals per D-16, without editing Android behavior or creating parity work.</action>
  <acceptance_criteria>
    - The linked tuple is exactly Hex core/iOS core/Android core at `0.2.1`.
    - All five companion requirements and floors accept the candidate and reject discriminating below-floor controls.
    - Companion releases, versions, and PRs never become candidate approval children.
  </acceptance_criteria>
  <verify>
    <automated>mix test test/crosswake/release_candidate/coordinate_test.exs &amp;&amp; elixir script/check_release_workflow_integrity.exs</automated>
    <fails_when>non-zero exit, 0 tests pass, any linked coordinate drifts, a floor mutation passes, or a companion is classified into the linked release unit</fails_when>
  </verify>
  <done>The exact distributable metadata proves linked core/native 0.2.1 coordinates and independent compatible companions.</done>
</task>

</tasks>

<threat_model>
Security posture: OWASP ASVS Level 1; high/critical findings block execution.

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-168-07 | Tampering | Hex tarball unpack | high | mitigate | Official unpack semantics, exclusive roots, path/symlink rejection, canonical manifests, and digest fixtures. |
| T-168-08 | Spoofing | package source | high | mitigate | Candidate worktree-to-tarball provenance and explicit rejection of repository/public/path fallback. |
| T-168-09 | Tampering | coordinate graph | high | mitigate | Cross-format linked tuple plus boundary-version mutation tests and companion exclusion checks. |
</threat_model>

## Adversarial Prohibition Recall

Package-source honesty is projected as the bespoke flagged prohibition in Plan 04, where the actual consumer boundary is enforced. Generic archive and dependency-security canon is covered by the STRIDE register and later `$gsd-secure-phase` review.

## Artifacts this phase produces

- Modules `Crosswake.ReleaseCandidate.Artifact` and `Crosswake.ReleaseCandidate.Coordinate`.
- Adapter `script/release_candidate/hex_artifacts.sh` with candidate-local build/unpack mode.
- Observation fields `package`, `version`, `candidate_ref`, `outer_checksum`, `metadata_digest`, `payload_digest`, `files`, `requirements`, and ephemeral `unpacked_root`.
- New tests `artifact_test.exs` and `coordinate_test.exs` with six-package and floor/coordinate mutation fixtures.

## Source Coverage

| SOURCE | ID | Feature / requirement | Plan | Status | Notes |
|--------|----|-----------------------|------|--------|-------|
| REQ | REL-03 | Coordinate/floor consistency | 168-03 | COVERED | Linked tuple and companion boundaries. |
| REQ | REL-04 | Package audit/build proof | 168-03 | COVERED | Six exact candidate tarballs. |
| RESEARCH | Artifact pipeline | Official Hex build/unpack and normalized digests | 168-03 | COVERED | No custom package format. |
| CONTEXT | D-07 through D-10 | Candidate-local artifact proof | 168-03 | COVERED | Build/unpack/source/floor boundaries. |
| CONTEXT | D-15, D-16 | Linked unit and companion independence | 168-03 | COVERED | Exact approval scope enforced. |

<verification>
Run both task commands under Plan 01's pinned runtime, then run `git diff --check` and compare two fresh builds from the same candidate ref after normalization.
</verification>

<success_criteria>
Six exact candidate artifacts are content-addressed and internally consistent, with linked core/native coordinates and no companion release expansion.
</success_criteria>

<output>
Create `.planning/workstreams/quality-ratchet-release/phases/168-0-2-1-release-candidate-readiness/168-03-SUMMARY.md` when done.
</output>
