---
phase: 168-0-2-1-release-candidate-readiness
plan: "04"
type: execute
wave: 4
depends_on: [168-03]
files_modified:
  - lib/crosswake/release_candidate/cleanroom.ex
  - script/verify_companion_cleanroom.sh
  - test/crosswake/release_candidate/cleanroom_test.exs
autonomous: true
requirements: [REL-01, REL-04]
estimate:
  tokens: 30000
  raw_tokens: 30000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "A real minimal Phoenix host generated by exactly `phx_new 1.8.13` consumes only the six unpacked candidate payloads under invocation-unique isolated Mix/Hex/deps/build/lock state and installs twice per D-11."
    - "The closed five-lane matrix proves Rulestead, Rindle, Sigra, Chimeway, and Threadline registration/absence and non-vacuous public behavior with a negative control in every lane per D-12."
    - "The same harness has a distinct post-publication mode that fetches exact public versions, rejects `path` lock entries, compares approved digests, reruns all lanes, and finishes with fail-closed live status per D-07/D-14."
  artifacts:
    - path: "lib/crosswake/release_candidate/cleanroom.ex"
      provides: "Closed five-profile clean-room observation and non-vacuity policy."
    - path: "script/verify_companion_cleanroom.sh"
      provides: "Candidate-local and exact-public generated-Phoenix consumer modes."
    - path: "test/crosswake/release_candidate/cleanroom_test.exs"
      provides: "Five-lane, twice-install, source-origin, equality/empty/order, and negative-control regressions."
  key_links:
    - from: "lib/crosswake/release_candidate/artifact.ex"
      to: "script/verify_companion_cleanroom.sh"
      via: "six allowlisted unpacked roots plus approved normalized digests"
      pattern: "candidate-local|post-publication"
    - from: "script/verify_companion_cleanroom.sh"
      to: "mix crosswake.release.status --live"
      via: "post-publication exact registry proof only"
      pattern: "release.status --live"
  flagged_assumptions:
    - id: EDGE-REL-01-ADJACENCY
      requirement: REL-01
      status: flagged_unverified
      flagged: true
      assumption: "When a dependency version exactly equals a supported floor it is accepted; immediately lower versions are rejected, and no package/profile identities merge merely because constraints touch."
    - id: EDGE-REL-01-EMPTY
      requirement: REL-01
      status: flagged_unverified
      flagged: true
      assumption: "An empty package set, empty profile result, absent public seam, or null observation is `BLOCKED`; a one-package observation cannot satisfy the six-package/five-profile matrix."
    - id: EDGE-REL-01-ORDERING
      requirement: REL-01
      status: flagged_unverified
      flagged: true
      assumption: "Input discovery order is irrelevant; normalized package/profile results use the fixed documented order, with duplicate/equal identities rejected rather than stably coalesced."
  prohibitions:
    - id: PROHIBITION-PACKAGE-PROOF-HONESTY
      statement: "Repository paths, cached dependencies, source-tree tests, and published-current packages must never be reported as candidate-package or exact-public 0.2.1 proof; partial public success must never be reported as complete."
      status: flagged_unverified
      flagged: true
---

<objective>
Prove the six candidate packages through a real generated Phoenix consumer and prepare the distinct exact-public verification path.

Purpose: Establish host-reusable, non-vacuous package-family confidence without confusing source paths with shipped packages.
Output: Isolated twice-install harness, five closed companion profiles, and exact-public comparison mode.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/workstreams/quality-ratchet-release/REQUIREMENTS.md
@.planning/workstreams/quality-ratchet-release/phases/168-0-2-1-release-candidate-readiness/168-CONTEXT.md
@.planning/workstreams/quality-ratchet-release/phases/168-0-2-1-release-candidate-readiness/168-RESEARCH.md
@.planning/workstreams/quality-ratchet-release/phases/168-0-2-1-release-candidate-readiness/168-03-SUMMARY.md
@script/verify_companion_cleanroom.sh
@lib/crosswake/release_status.ex

<interfaces>
- Candidate-local mode receives only Plan 03 unpacked roots and metadata; public mode receives package/version/digest authority and performs exact registry fetch itself.
- Scratch roots are created exclusively per invocation and only those roots may be removed.
- `phx_new 1.8.13` is installed into invocation-local homes; it does not enter `mix.exs` or the repository lock.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Run five non-vacuous profiles from unpacked candidate payloads</name>
  <read_first>
    - script/verify_companion_cleanroom.sh
    - lib/crosswake/release_candidate/artifact.ex
    - lib/crosswake/release_candidate/coordinate.ex
    - prompts/crosswake-integrations-and-companions.md
    - prompts/crosswake-elixir-oss-dna.md
  </read_first>
  <files>lib/crosswake/release_candidate/cleanroom.ex, script/verify_companion_cleanroom.sh, test/crosswake/release_candidate/cleanroom_test.exs</files>
  <behavior>
    - Candidate-local mode generates a minimal Phoenix app with exact `phx_new 1.8.13`, uses only the six unpacked payload roots, and performs two fresh installs with isolated `MIX_HOME`, `HEX_HOME`, deps, build, and lock state.
    - Every lane compiles with warnings-as-errors, loads runtime configuration, checks router/config output and public seams, runs smoke behavior, registration assertions, one negative profile/registration control, and `mix crosswake.doctor`.
    - Rulestead/Rindle prove engines plus dependency behavior; Sigra returns non-vacuous auth; Chimeway proves telemetry with Sigra absent; Threadline stays unregistered with siblings absent while proving Plug/Telemetry/Ledger/templates.
  </behavior>
  <action>Replace predictable shared scratch handling and handwritten `mix new` host construction inside the existing harness with exclusive temporary roots and the pinned real generator. Add explicit `--source-mode candidate-local` inputs for the closed six-package root/digest manifest and reject missing, duplicate, extra, repository-root, public-fetch, or outside-root sources. Preserve and strengthen the five D-12 profiles with engine/auth/event/contracts/absence assertions and per-lane negative controls; run each install twice from clean isolated state. Return bounded profile counts/results/digests, never application payloads or verbose logs.</action>
  <acceptance_criteria>
    - Exactly five profiles execute from exactly six candidate tarball payloads in a genuine generated Phoenix host.
    - Both isolated install passes compile and all runtime/router/public/smoke/registration/doctor checks are non-empty.
    - Removing each profile's required engine/auth/event/contracts/absence behavior makes its negative fixture fail.
  </acceptance_criteria>
  <verify>
    <automated>mix test test/crosswake/release_candidate/cleanroom_test.exs</automated>
    <fails_when>non-zero exit, 0 tests pass, fewer than five profiles or two installs execute, any lane lacks a negative control, or a repository/public fallback passes candidate mode</fails_when>
  </verify>
  <done>The candidate-local package family passes a five-lane, twice-installed, real-Phoenix, non-vacuous clean room.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add exact-public post-publication comparison mode</name>
  <precondition>Fixture tests may run immediately; a live `0.2.1` public check is valid only after the linked packages were actually published and the approved receipt exists.</precondition>
  <read_first>
    - script/verify_companion_cleanroom.sh
    - lib/crosswake/release_candidate/cleanroom.ex
    - lib/crosswake/release_candidate/receipt.ex
    - lib/mix/tasks/crosswake.release.status.ex
  </read_first>
  <files>lib/crosswake/release_candidate/cleanroom.ex, script/verify_companion_cleanroom.sh, test/crosswake/release_candidate/cleanroom_test.exs</files>
  <behavior>
    - Public mode fetches each package by exact package/version with official Hex fetch/unpack, proves lock entries are registry-backed rather than `path`, and compares normalized metadata/payload digests against the approved receipt.
    - It reruns all five profiles with the same twice-install and negative-control contract, then requires fail-closed `mix crosswake.release.status --live`.
    - Missing packages, registry lag, path/cache fallback, digest mismatch, partial availability, or live-status ambiguity returns `PARTIAL` or `BLOCKED`, never readiness or completeness.
  </behavior>
  <action>Add `--source-mode exact-public` as a separate D-07/D-14 execution path. Fetch/unpack exact versions into new isolated roots, inspect lock SCM/source, compare the same normalized digests captured before approval, and reuse rather than fork the five-profile assertions. Fixture the public fetch seam so development never depends on unpublished `0.2.1`; keep the real post-publication invocation dormant until its precondition is true. Never calibrate candidate readiness from published-current packages or silently reuse caches.</action>
  <acceptance_criteria>
    - Fixture-backed exact-public mode discriminates registry source, exact version, digest equality, five lanes, and live status.
    - Partial registry success remains visible as `PARTIAL` with exact succeeded/failed children.
    - The pre-approval candidate command cannot invoke or count exact-public mode as reversible proof.
  </acceptance_criteria>
  <verify>
    <automated>mix test test/crosswake/release_candidate/cleanroom_test.exs --only post_publication</automated>
    <fails_when>non-zero exit, 0 tests pass, a path/cache source passes, a digest mutation passes, or partial fixture publication becomes `COMPLETE`</fails_when>
  </verify>
  <done>The same clean-room contract can prove exact public 0.2.1 payloads after publication without overstating pre-publication readiness.</done>
</task>

</tasks>

<threat_model>
Security posture: OWASP ASVS Level 1; high/critical findings block execution.

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-168-10 | Tampering / DoS | clean-room scratch roots | high | mitigate | Exclusive creation, ownership marker, bounded cleanup, and outside-root rejection. |
| T-168-11 | Spoofing | package source | high | mitigate | Explicit source modes, lock/source inspection, exact versions, and digest comparison. |
| T-168-12 | Repudiation | companion profile success | high | mitigate | Closed five-lane results, two independent installs, required public behavior, and per-lane negative controls. |
</threat_model>

## Adversarial Prohibition Recall

Kept bespoke transparency prohibition `PROHIBITION-PACKAGE-PROOF-HONESTY` as flagged-unverified. Generic shell, dependency, and filesystem canon is omitted with this breadcrumb and remains covered by the STRIDE register and later `$gsd-secure-phase` review.

## Artifacts this phase produces

- Module `Crosswake.ReleaseCandidate.Cleanroom`.
- Harness flags `--source-mode candidate-local` and `--source-mode exact-public` plus closed package-root/digest input.
- Result fields `generator_version`, `install_count`, `package_count`, `profile_count`, `source_mode`, `path_lock_count`, `profile_results`, and `live_status`.
- New `cleanroom_test.exs` with five profile controls, two-install controls, exact-floor adjacency, empty/single input, normalized ordering, source-origin, and digest mutations.

## Source Coverage

| SOURCE | ID | Feature / requirement | Plan | Status | Notes |
|--------|----|-----------------------|------|--------|-------|
| REQ | REL-01 | Five companion clean-room profiles | 168-04 | COVERED | Real Phoenix, twice-install, negative controls. |
| REQ | REL-04 | Candidate and post-public package proof | 168-04 | COVERED | Separate source modes and digest comparison. |
| RESEARCH | Clean-room architecture | Pinned generator and isolated consumer | 168-04 | COVERED | Existing seam extended. |
| CONTEXT | D-07, D-11 through D-14 | Two-stage clean-room contract | 168-04 | COVERED | Candidate/local and exact-public paths remain distinct. |

<verification>
Run both task commands under the pinned runtime. Then execute candidate-local mode twice against independently built Plan 03 artifacts and require equivalent normalized results with different scratch roots.
</verification>

<success_criteria>
Every supported companion works non-vacuously from candidate packages before approval, and the exact-public verification path is ready but cannot masquerade as pre-approval proof.
</success_criteria>

<output>
Create `.planning/workstreams/quality-ratchet-release/phases/168-0-2-1-release-candidate-readiness/168-04-SUMMARY.md` when done.
</output>
