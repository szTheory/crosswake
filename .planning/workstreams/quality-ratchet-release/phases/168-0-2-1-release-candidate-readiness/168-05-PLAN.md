---
phase: 168-0-2-1-release-candidate-readiness
plan: "05"
type: execute
wave: 5
depends_on: [168-04]
files_modified:
  - lib/crosswake/release_candidate/mirror.ex
  - script/release_candidate/ios_mirror.sh
  - script/verify_ios_mirror_backfill.sh
  - test/crosswake/release_candidate/mirror_test.exs
autonomous: true
requirements: [REL-02, REL-04]
estimate:
  tokens: 24000
  raw_tokens: 24000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Credential-free baseline proves public mirror `main` and tag `0.2.0` resolve to the already recorded source tree; conflict, absence, replacement, or unreachable state is `BLOCKED` per D-21."
    - "Candidate rehearsal validates an exact 40-SHA `0.2.1` subtree split and performs credentialed real-mirror porcelain dry-run only; local no-credential output says `WRITE AUTHORITY NOT CHECKED` per D-22/D-23."
    - "Normal mirror publication is ancestor-only fast-forward with immutable tag and atomic main+tag where supported, while force-with-lease exists only in separately approved exact-ref recovery per D-24/D-25."
  artifacts:
    - path: "lib/crosswake/release_candidate/mirror.ex"
      provides: "Closed baseline/candidate/publish/recovery mirror-mode policy."
    - path: "script/release_candidate/ios_mirror.sh"
      provides: "Narrow Git/subtree/remote adapter with bounded porcelain observations."
    - path: "test/crosswake/release_candidate/mirror_test.exs"
      provides: "Race, conflict, auth, ancestry, atomicity, and mode-separation fixtures."
  key_links:
    - from: "script/release_candidate/ios_mirror.sh"
      to: "lib/crosswake/release_candidate/mirror.ex"
      via: "normalized local/remote main/tag/split/auth/dry-run observations"
      pattern: "WRITE AUTHORITY NOT CHECKED"
    - from: "lib/crosswake/release_candidate/mirror.ex"
      to: "lib/crosswake/release_candidate/receipt.ex"
      via: "baseline and rehearsal check IDs with external-state/credential flags"
      pattern: "baseline|candidate|publish|recovery"
  flagged_assumptions:
    - id: EDGE-REL-02-UNCLASSIFIED
      requirement: REL-02
      status: flagged_unverified
      flagged: true
      assumption: "Manual edge review is represented by fixtures for absent credentials, authorization denial, unreachable remotes, equal/already-current refs, diverged main, conflicting tags, non-atomic capability, remote races, and mode confusion; no additional REL-02 edge class is presently known."
---

<objective>
Separate credential-free mirror truth, credentialed candidate rehearsal, ordinary publication, and recovery into four explicit modes.

Purpose: Prove iOS mirror write authority before approval without mutating refs or normalizing destructive recovery.
Output: Deterministic mirror policy, narrow adapter, and hostile Git/ref/auth fixtures.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/workstreams/quality-ratchet-release/REQUIREMENTS.md
@.planning/workstreams/quality-ratchet-release/phases/168-0-2-1-release-candidate-readiness/168-CONTEXT.md
@.planning/workstreams/quality-ratchet-release/phases/168-0-2-1-release-candidate-readiness/168-RESEARCH.md
@.planning/workstreams/quality-ratchet-release/phases/168-0-2-1-release-candidate-readiness/168-04-SUMMARY.md
@script/verify_ios_mirror_backfill.sh
@.github/workflows/ios-mirror-backfill.yml

<interfaces>
- Mirror remote refs, authorization, capabilities, dry-run porcelain, and subtree split are execution-time observations refreshed within each mode.
- The adapter never decides candidate state and never writes secrets/remote URLs into the receipt.
- Baseline and candidate rehearsal are reversible checks; publish and recovery implementation remains unreachable from the Phase 168 approval command.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Prove immutable baseline and credentialed candidate dry-run authority</name>
  <read_first>
    - script/verify_ios_mirror_backfill.sh
    - .github/workflows/ios-mirror-backfill.yml
    - packages/crosswake-shell-core-ios/Package.swift
    - lib/crosswake/release_candidate/receipt.ex
  </read_first>
  <files>lib/crosswake/release_candidate/mirror.ex, script/release_candidate/ios_mirror.sh, script/verify_ios_mirror_backfill.sh, test/crosswake/release_candidate/mirror_test.exs</files>
  <behavior>
    - Baseline mode uses no write credential and proves public mirror main/tag `0.2.0` exist, agree with their recorded source subtree/tree, and have not been replaced.
    - Candidate mode accepts only exact version `0.2.1` and a full 40-SHA, computes the iOS subtree split, re-fetches remote refs, checks authorization with the deploy key, and performs a real remote dry-run with porcelain proving no ref changed.
    - No credential locally yields a successful baseline plus explicit `WRITE AUTHORITY NOT CHECKED`; bad/denied credential, conflict, race, unreachable remote, changed candidate, or unparseable porcelain is `BLOCKED`.
  </behavior>
  <action>Write local bare-remote fixtures and authorization/dry-run seam fixtures before extending the existing mirror script. Implement distinct `baseline` and `candidate` modes: baseline reads immutable D-21 truth without credentials; candidate requires fresh exact ref/version/tree, freshly computed subtree split, deploy-key authorization, and real-mirror porcelain dry-run with before/after ref equality per D-22/D-23. Emit only mode, bounded ref/tree digests, capability, authorization checked/result, dry-run result, external-state-changed=false, and one correction. Never expose remote URLs, key material, command logs, or use credentials for baseline.</action>
  <acceptance_criteria>
    - Baseline passes only for exact recorded public `main`/`0.2.0` truth and never needs a credential.
    - Candidate rehearsal proves exact split, authorization, remote dry-run, and zero ref mutation for current observed refs.
    - Missing credentials are honest locally; denied credentials, races, conflicts, replacements, and unreachable remotes block.
  </acceptance_criteria>
  <verify>
    <automated>mix test test/crosswake/release_candidate/mirror_test.exs</automated>
    <fails_when>non-zero exit, 0 tests pass, a conflicting/replaced ref passes, absent credentials claim write authority, or a dry-run fixture changes a ref</fails_when>
  </verify>
  <done>Mirror baseline is credential-free, and candidate rehearsal proves write authority without changing main or tags.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Constrain normal publication and exact-ref recovery</name>
  <read_first>
    - lib/crosswake/release_candidate/mirror.ex
    - script/release_candidate/ios_mirror.sh
    - script/verify_ios_mirror_backfill.sh
    - .github/workflows/ios-mirror-backfill.yml
  </read_first>
  <files>lib/crosswake/release_candidate/mirror.ex, script/release_candidate/ios_mirror.sh, script/verify_ios_mirror_backfill.sh, test/crosswake/release_candidate/mirror_test.exs</files>
  <behavior>
    - Publish mode re-fetches immediately before action, requires remote main ancestor/equal to candidate split, rejects any existing different `0.2.1` tag, and updates main+tag atomically when the remote supports atomic push.
    - If atomic push is unsupported, the policy stops for explicit workflow handling rather than silently splitting an asserted atomic operation.
    - Recovery mode requires a separately supplied approval receipt and exact expected old/new refs before force-with-lease; normal modes cannot form force/move/delete arguments.
  </behavior>
  <action>Refactor the inherited normal mirror path so publish mode is fast-forward/equal only, tag creation is immutable, and atomic capability is observed and enforced per D-24. Move all force-with-lease construction behind a distinct `recovery` mode requiring separate approval and exact expected refs per D-25. Keep this task fixture/local-remote only: do not publish `0.2.1`, push the real mirror, move/delete any tag, or make recovery callable from readiness evaluation.</action>
  <acceptance_criteria>
    - Normal publish fixtures accept fast-forward/equal refs and reject divergence, tag conflict, races, and unsupported atomic operation.
    - Force-with-lease appears only in recovery mode with separate approval plus exact expected refs.
    - Candidate rehearsal and the final Phase 168 approval surface cannot invoke publish or recovery.
  </acceptance_criteria>
  <verify>
    <automated>mix test test/crosswake/release_candidate/mirror_test.exs &amp;&amp; bash -n script/release_candidate/ios_mirror.sh script/verify_ios_mirror_backfill.sh</automated>
    <fails_when>non-zero exit, 0 tests pass, normal mode accepts divergence or force, recovery lacks exact approval/ref binding, or shell syntax fails</fails_when>
  </verify>
  <done>Ordinary publication is fast-forward/immutable/atomic, and destructive concurrency control is isolated behind separately approved recovery.</done>
</task>

</tasks>

<threat_model>
Security posture: OWASP ASVS Level 1; high/critical findings block execution.

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-168-13 | Spoofing | mirror authorization | high | mitigate | Trusted deploy-key rehearsal, explicit credential flag, bounded porcelain, and denied-auth fixtures. |
| T-168-14 | Tampering | mirror main/tag | high | mitigate | Immediate ref fetch, ancestry/equality guard, immutable tag, atomic push requirement, before/after dry-run equality. |
| T-168-15 | Elevation of privilege | recovery force path | high | mitigate | Separate mode, separate approval receipt, exact expected refs, and no reachability from readiness. |
</threat_model>

## Adversarial Prohibition Recall

No additional values prohibition survives deduplication: silent ref mutation is already covered by the phase-wide explicit-approval prohibition in Plan 08. Generic SSH/Git credential canon remains covered by this STRIDE register and later `$gsd-secure-phase` review.

## Artifacts this phase produces

- Module `Crosswake.ReleaseCandidate.Mirror`.
- Adapter `script/release_candidate/ios_mirror.sh` with modes `baseline`, `candidate`, `publish`, and `recovery`.
- Mirror fields `mode`, `source_ref`, `split_sha`, `remote_main`, `remote_tag`, `atomic_supported`, `authorization_checked`, `authorization_result`, `dry_run_result`, and `external_state_changed`.
- Exact local diagnostic `WRITE AUTHORITY NOT CHECKED`.

## Source Coverage

| SOURCE | ID | Feature / requirement | Plan | Status | Notes |
|--------|----|-----------------------|------|--------|-------|
| REQ | REL-02 | Immutable and reproducible mirror refs | 168-05 | COVERED | Four closed modes and hostile fixtures. |
| REQ | REL-04 | Candidate mirror rehearsal | 168-05 | COVERED | Exact split/auth/dry-run authority. |
| RESEARCH | Mirror architecture | Separate baseline/candidate/publish/recovery | 168-05 | COVERED | Narrow adapter and deterministic policy. |
| CONTEXT | D-21 through D-25 | Mirror baseline, rehearsal, credentials, publication, recovery | 168-05 | COVERED | Each boundary is explicit. |

<verification>
Run both task commands, then exercise baseline against the public mirror without credentials and require a candidate-mode local run without credentials to emit exactly `WRITE AUTHORITY NOT CHECKED` and no remote change.
</verification>

<success_criteria>
Maintainers can distinguish immutable public baseline from credentialed dry-run authority, and neither normal readiness nor publication can silently use destructive recovery semantics.
</success_criteria>

<output>
Create `.planning/workstreams/quality-ratchet-release/phases/168-0-2-1-release-candidate-readiness/168-05-SUMMARY.md` when done.
</output>
