---
phase: 168-0-2-1-release-candidate-readiness
plan: "02"
type: execute
wave: 2
depends_on: [168-01]
files_modified:
  - lib/crosswake/release_candidate.ex
  - lib/crosswake/release_candidate/identity.ex
  - lib/crosswake/release_candidate/receipt.ex
  - lib/crosswake/release_candidate/projection.ex
  - lib/mix/tasks/crosswake.release.candidate.ex
  - test/crosswake/release_candidate/receipt_test.exs
  - test/mix/tasks/crosswake_release_candidate_test.exs
autonomous: true
requirements: [REL-04, REL-05]
estimate:
  tokens: 26000
  raw_tokens: 26000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "The exact invocation `mix crosswake.release.candidate --version 0.2.1 --ref <40sha> --output-dir <dir>` evaluates fresh inputs and never treats the current PR #57 head as candidate authority per D-03 and D-26."
    - "A candidate receipt has exactly `READY FOR APPROVAL`, `BLOCKED`, `STALE`, `PARTIAL`, or `COMPLETE`; any identity, config, package, workflow, proof, or run drift becomes `STALE` per D-04 and D-29."
    - "One allowlisted JSON receipt is authoritative while Markdown, GitHub Summary, and calm `NO_COLOR` terminal output are deterministic projections with one next action; no raw payload or SLSA claim enters evidence per D-28, D-29, D-30, and D-31."
  artifacts:
    - path: "lib/crosswake/release_candidate.ex"
      provides: "Deterministic candidate evaluator and five-state transition authority."
    - path: "lib/crosswake/release_candidate/identity.ex"
      provides: "Exact ref/head/tree/base/version/config/workflow/artifact/proof/run identity validation."
    - path: "lib/crosswake/release_candidate/receipt.ex"
      provides: "Closed privacy-safe JSON receipt schema."
    - path: "lib/mix/tasks/crosswake.release.candidate.ex"
      provides: "Thin exact CLI boundary."
  key_links:
    - from: "lib/mix/tasks/crosswake.release.candidate.ex"
      to: "lib/crosswake/release_candidate.ex"
      via: "validated version/ref/output-dir options passed to `Crosswake.ReleaseCandidate.run!/1`"
      pattern: "Crosswake.ReleaseCandidate.run!"
    - from: "lib/crosswake/release_candidate/receipt.ex"
      to: "lib/crosswake/release_candidate/projection.ex"
      via: "the same normalized receipt map drives every human projection"
      pattern: "READY FOR APPROVAL|BLOCKED|STALE|PARTIAL|COMPLETE"
  flagged_assumptions:
    - id: EDGE-REL-04-UNCLASSIFIED
      requirement: REL-04
      status: flagged_unverified
      flagged: true
      assumption: "Manual edge review is represented by closed-schema rejection tests for every unknown, missing, malformed, or internally inconsistent candidate-identity field; no additional REL-04 edge class is presently known."
  prohibitions:
    - id: PROHIBITION-PRIVACY
      statement: "Receipts and projections must not contain credentials, adopter/customer facts, private URLs, raw package payloads, logs, actors, tokens, stable device identifiers, answers, media, or transcripts."
      status: flagged_unverified
      flagged: true
---

<objective>
Create the deterministic candidate identity, receipt, projection, and exact Mix command boundary.

Purpose: Give every later package, mirror, workflow, and approval check one closed authority model.
Output: Tested five-state evaluator, privacy-safe receipt schema, calm projections, and thin CLI.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/ADR-FIRST-B2C-ADOPTER.md
@.planning/workstreams/quality-ratchet-release/ROADMAP.md
@.planning/workstreams/quality-ratchet-release/REQUIREMENTS.md
@.planning/workstreams/quality-ratchet-release/phases/168-0-2-1-release-candidate-readiness/168-CONTEXT.md
@.planning/workstreams/quality-ratchet-release/phases/168-0-2-1-release-candidate-readiness/168-RESEARCH.md
@.planning/workstreams/quality-ratchet-release/phases/168-0-2-1-release-candidate-readiness/168-01-SUMMARY.md

<interfaces>
- Entry authority is the validated Phase 168 landing receipt from Plan 01; all current release-PR/ref/run identities remain execution-time observations.
- The domain layer owns state and serialization; shell/GitHub adapters may only return normalized observations and must not decide readiness.
- There is no application database, Ecto repository, release branch, RC tag, or second candidate authority per D-06 and D-27.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define exact identity and the closed five-state receipt</name>
  <read_first>
    - .planning/workstreams/quality-ratchet-release/phases/168-0-2-1-release-candidate-readiness/168-CONTEXT.md
    - .planning/workstreams/quality-ratchet-release/phases/168-0-2-1-release-candidate-readiness/168-RESEARCH.md
    - .planning/workstreams/quality-ratchet-release/phases/168-0-2-1-release-candidate-readiness/evidence/phase168-entry-landing.json
    - lib/crosswake/release_status.ex
  </read_first>
  <files>lib/crosswake/release_candidate.ex, lib/crosswake/release_candidate/identity.ex, lib/crosswake/release_candidate/receipt.ex, lib/crosswake/release_candidate/projection.ex, test/crosswake/release_candidate/receipt_test.exs</files>
  <behavior>
    - Exact 40-hex ref, head, tree, protected-default base, version, Release Please config/manifests, workflow blobs, package digests, proof results, and CI run/head/conclusion are required and mutually consistent.
    - Fresh complete reversible proof yields `READY FOR APPROVAL`; changed bound input yields `STALE`; missing/failed/ambiguous proof yields `BLOCKED`; externally observed partial publication yields `PARTIAL`; all linked public coordinates and mirrors proven yields `COMPLETE`.
    - Unknown keys, extra states, private fields, non-40-hex refs, stale run heads, zero proof counts, and contradictory flags fail closed; JSON key/list ordering and every projection are deterministic.
  </behavior>
  <action>Write receipt and transition tests before implementation. Define explicit structs/types and allowlists for the D-03/D-04 identity tuple, proof observations, external-state/credential flags, state, and exactly one correction/next action. Derive state solely from normalized inputs; never probe the network in these modules. Serialize the canonical JSON with stable ordering and project it to Markdown, GitHub Summary, and terse terminal text, honoring `NO_COLOR`, text-only state markers, and bounded diagnostics per D-28 through D-31. Add privacy canaries and non-vacuity mutation tests for every identity field. Keep the parked adopter lane, offline payloads, Android breadth, companion release proposals, and generic release-platform concepts outside the schema.</action>
  <acceptance_criteria>
    - The receipt schema accepts only the documented fields and exactly five states.
    - Each mutated bound identity independently changes a previously ready receipt to `STALE`.
    - Credential absence and unresolved evidence are explicit flags and never silently inferred as success.
    - JSON, Markdown, GitHub Summary, and terminal projections agree on state and one next action without exposing prohibited data.
  </acceptance_criteria>
  <verify>
    <automated>mix test test/crosswake/release_candidate/receipt_test.exs</automated>
    <fails_when>non-zero exit, 0 tests pass, any mutation remains ready, an unknown field is accepted, or a privacy canary appears in output</fails_when>
  </verify>
  <done>The candidate identity and receipt are deterministic, five-state, privacy-safe, drift-sensitive, and independent of mutable external discovery.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Expose the exact thin candidate command</name>
  <read_first>
    - lib/crosswake/release_candidate.ex
    - lib/crosswake/release_candidate/receipt.ex
    - lib/mix/tasks/crosswake.release.status.ex
    - test/mix/tasks/crosswake_release_status_test.exs
  </read_first>
  <files>lib/mix/tasks/crosswake.release.candidate.ex, test/mix/tasks/crosswake_release_candidate_test.exs</files>
  <behavior>
    - Only `--version`, `--ref`, and `--output-dir` are accepted; version is exactly `0.2.1`, ref is full 40-hex, and output is written beneath the requested directory.
    - The task delegates to `Crosswake.ReleaseCandidate.run!/1`, writes authoritative JSON plus projections, returns non-zero for `BLOCKED` or `STALE`, and never merges, publishes, tags, pushes, or edits a database.
    - Unknown/missing/duplicate options, short SHAs, dirty or changed observed identity, and unwritable output directories fail with one actionable error.
  </behavior>
  <action>Following D-26, implement only `mix crosswake.release.candidate --version 0.2.1 --ref &lt;40sha&gt; --output-dir &lt;dir&gt;` as a thin task around the deterministic evaluator. Inject narrow adapters from later plans rather than embedding shell orchestration. Treat selecting this long-lived CLI seam as a costly but reversible interface choice; do not introduce Ecto, an app process, a release branch, an RC tag, interactive prompts, or implicit remote mutations per D-06/D-27.</action>
  <acceptance_criteria>
    - The exact command produces the canonical JSON and matching projections for a closed fixture.
    - Invalid inputs fail before any adapter or filesystem side effect beyond the requested output directory.
    - The task contains no GitHub, registry, mirror, merge, publish, push, or database authority.
  </acceptance_criteria>
  <verify>
    <automated>mix test test/crosswake/release_candidate test/mix/tasks/crosswake_release_candidate_test.exs</automated>
    <fails_when>non-zero exit, 0 tests pass, the exact CLI contract is not accepted, malformed inputs reach an adapter, or the command performs an unrequested remote mutation</fails_when>
  </verify>
  <reversibility rating="costly">D-26 makes the exact Mix command a durable maintainer interface; it remains changeable in source but later automation will depend on its spelling and receipt contract.</reversibility>
  <done>The exact candidate command produces one authoritative receipt and performs evaluation only.</done>
</task>

</tasks>

<threat_model>
Security posture: OWASP ASVS Level 1; high/critical findings block execution.

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-168-04 | Spoofing / Tampering | candidate identity | high | mitigate | Full-SHA and tree/base/config/workflow/package/run binding with independent mutation tests. |
| T-168-05 | Information disclosure | receipt and projections | high | mitigate | Closed allowlist, privacy canaries, bounded diagnostics, and no raw adapter payload retention. |
| T-168-06 | Elevation of privilege | Mix task | high | mitigate | Evaluation-only boundary with no merge/publish/push/tag/database operations. |
</threat_model>

## Adversarial Prohibition Recall

Kept bespoke privacy prohibition `PROHIBITION-PRIVACY` as flagged-unverified. Generic injection, filesystem, and compliance canon is intentionally omitted here and remains covered by the STRIDE register and later `$gsd-secure-phase` review.

## Artifacts this phase produces

- Modules `Crosswake.ReleaseCandidate`, `Crosswake.ReleaseCandidate.Identity`, `Crosswake.ReleaseCandidate.Receipt`, and `Crosswake.ReleaseCandidate.Projection`.
- Mix task `Mix.Tasks.Crosswake.Release.Candidate` and exact flags `--version`, `--ref`, `--output-dir`.
- Receipt field families `identity`, `checks`, `external_state`, `credentials`, `state`, and `next_action` with states `READY FOR APPROVAL`, `BLOCKED`, `STALE`, `PARTIAL`, `COMPLETE`.
- New test paths `test/crosswake/release_candidate/receipt_test.exs` and `test/mix/tasks/crosswake_release_candidate_test.exs`.

## Source Coverage

| SOURCE | ID | Feature / requirement | Plan | Status | Notes |
|--------|----|-----------------------|------|--------|-------|
| GOAL | Phase 168 | Exact candidate approval authority | 168-02 | COVERED | Identity, state, receipt, and CLI authority. |
| REQ | REL-04 | Candidate identity and stale detection | 168-02 | COVERED | Every bound input is closed and mutation-tested. |
| REQ | REL-05 | Deterministic operator surface | 168-02 | COVERED | Exact command and projections. |
| RESEARCH | Receipt architecture | Pure domain evaluator and thin adapters | 168-02 | COVERED | Responsibility map is preserved. |
| CONTEXT | D-03, D-04, D-26 through D-31 | Exact identity, CLI, states, schema, privacy, projections | 168-02 | COVERED | Each decision is explicit in must-haves/tasks. |

<verification>
Run both task commands under the pinned runtime from Plan 01, then run `mix format --check-formatted` and require deterministic byte-identical JSON from two identical fixture evaluations.
</verification>

<success_criteria>
The exact Mix command emits one privacy-safe, drift-sensitive, five-state receipt and cannot itself change remote release state.
</success_criteria>

<output>
Create `.planning/workstreams/quality-ratchet-release/phases/168-0-2-1-release-candidate-readiness/168-02-SUMMARY.md` when done.
</output>
