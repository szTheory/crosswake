---
phase: 168-0-2-1-release-candidate-readiness
plan: "11"
type: execute
wave: 11
depends_on: [168-10]
gap_closure: true
files_modified:
  - CHANGELOG.md
  - guides/install.md
  - test/mix/tasks/crosswake_install_test.exs
  - test/crosswake/proof/phase48_provider_adapter_proof_test.exs
autonomous: true
requirements: [REL-04, REL-05]
estimate:
  tokens: 30000
  raw_tokens: 30000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "The changelog's published-release claims state `0.2.1`, matching what is actually installable, so a public reader is not told the newest release is a version that has been superseded (168-VERIFICATION gap 3, REL-04)."
    - "The entries describing work that shipped in the published `0.2.1` are recorded under a published-version heading rather than under unreleased planning continuity, preserving the changelog's own stated separation between planning milestones and published Hex releases."
    - "Every merge-blocking proof assertion that pins a published-version literal is updated in lockstep, and no assertion is weakened or deleted to make the change pass (REL-05)."
  artifacts:
    - path: "CHANGELOG.md"
      provides: "Published Hex truth at `0.2.1` plus a published-version section for the entries that shipped in it."
    - path: "test/crosswake/proof/phase48_provider_adapter_proof_test.exs"
      provides: "Merge-blocking literals realigned to the corrected published-truth sentences."
  key_links:
    - from: "CHANGELOG.md"
      to: "test/crosswake/proof/phase48_provider_adapter_proof_test.exs"
      via: "exact-contains assertions on published-truth sentences"
      pattern: "published Hex release"
  prohibitions:
    - id: PROHIBITION-NO-CLAIM-INFLATION
      statement: "Reconciling published truth must not promote any deferred, advisory, or verification-required surface into a supported claim. Only the published version number and the placement of already-written entries change."
      status: flagged_unverified
      flagged: true
---

<objective>
Bring the changelog's published-release truth in line with what is actually published, and land the pending anonymization edits that share those files.

Purpose: `CHANGELOG.md` still tells public readers that the current published Hex release is `0.2.0` and that no support claims have been cut after `0.2.0`, while `crosswake 0.2.1` has been installable since 2026-09-14. That is the public-facing half of verification gap 3, and it is guarded by merge-blocking exact-literal assertions that must move with it.
Output: Corrected published-version truth in the changelog, the shipped entries filed under a published-version heading, matching proof literals, and a clean working tree.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/workstreams/quality-ratchet-release/phases/168-0-2-1-release-candidate-readiness/168-CONTEXT.md
@.planning/workstreams/quality-ratchet-release/phases/168-0-2-1-release-candidate-readiness/168-VERIFICATION.md

<interfaces>
- `release-please-config.json` sets `skip-changelog: true` for the root component, so `CHANGELOG.md` is hand-maintained. There is no generated `0.2.1` section to recover and none was ever written; the corrected content is authored here.
- `CHANGELOG.md` states its own contract near the top: semantic-version headings are published Hex releases, while planning milestone labels live in `.planning/MILESTONE-ARC.md` and are not a second installable version axis. Preserve that separation exactly.
- Everything currently under the unreleased heading landed after the `0.2.0` tag and is therefore contained in the published `0.2.1`. The publication timestamp recorded by the Hex registry is 2026-09-14.
- `test/crosswake/proof/phase48_provider_adapter_proof_test.exs` asserts three exact sentences from `CHANGELOG.md` with merge-blocking posture, two of which name the published version. Other proof tests may assert further changelog literals; the full suite is the authority on which.
- The working tree arrives carrying three uncommitted edits from an earlier anonymization pass — in `CHANGELOG.md`, `guides/install.md`, and `test/mix/tasks/crosswake_install_test.exs` — which replace a real customer's product name with a neutral example name. The repository is public and must not name that adopter, so these edits are correct and land first, before this plan edits the same changelog file.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Land the pending adopter-anonymization edits</name>
  <read_first>
    - guides/install.md
    - test/mix/tasks/crosswake_install_test.exs
  </read_first>
  <files>CHANGELOG.md, guides/install.md, test/mix/tasks/crosswake_install_test.exs</files>
  <action>Run `git diff` on the three files and confirm the only change is substituting a neutral example application name for a real adopter's product name in prose, fixture module names, and fixture paths. Confirm the substitution is complete and internally consistent across all three files, including the derived web-module, policy-module, and OTP-application spellings, and grep those three files for any remaining occurrence of the previous name reported by that diff. Do not alter behavior, assertions, or coverage; this is a naming substitution only. Commit as `docs(168-11): use a neutral example application name` and push. Leave `.planning/` state files to the harness.</action>
  <acceptance_criteria>
    - The three files contain no occurrence of the previous adopter product name in any spelling shown by the diff.
    - The installer test still exercises the same assertions with the same strength, only renamed.
    - The working tree is clean of source and documentation changes before Task 2 begins.
  </acceptance_criteria>
  <verify>
    <automated>mix test test/mix/tasks/crosswake_install_test.exs &amp;&amp; test -z "$(git status --porcelain CHANGELOG.md guides/install.md test/mix/tasks/crosswake_install_test.exs)"</automated>
    <fails_when>non-zero exit; the installer test reports 0 tests or any failure; any of the three files remains modified in the working tree after the commit</fails_when>
  </verify>
  <reversibility rating="reversible">A documentation and fixture rename with no behavior change.</reversibility>
  <done>The anonymization edits are committed, and the changelog is clean and safe for Task 2 to edit.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Reconcile changelog published truth to the released 0.2.1</name>
  <read_first>
    - CHANGELOG.md
    - test/crosswake/proof/phase48_provider_adapter_proof_test.exs
    - .planning/workstreams/quality-ratchet-release/phases/168-0-2-1-release-candidate-readiness/168-VERIFICATION.md
  </read_first>
  <files>CHANGELOG.md, test/crosswake/proof/phase48_provider_adapter_proof_test.exs</files>
  <behavior>
    - The changelog's published-truth statement names the version that is actually installable from Hex, and a reader is not told a superseded version is current.
    - The entries that shipped inside the published patch sit under a semantic-version heading dated to the publication date, and a fresh empty unreleased heading sits above them for future work.
    - The merge-blocking exact-literal assertions in the phase 48 proof test match the corrected sentences, with the same posture and the same number of assertions.
    - No deferred, advisory, or verification-required surface is promoted to a supported claim by the move; section membership for those surfaces is preserved.
  </behavior>
  <action>Update the phase 48 proof literals FIRST so the suite fails, then correct the changelog. Rename the unreleased heading to a semantic-version heading for the published patch, dated to its Hex publication date, and open a new empty unreleased heading above it. Rewrite the published-truth sentence and the published-family sentence to name the version and family that are actually live, keeping their existing sentence shape and their distinction between published package truth and planning continuity. Rewrite the support-claims sentence so it describes the published patch honestly instead of asserting that nothing has been cut since the older version. Leave the advisory, verification-required, and deferred sections exactly where they are and word-for-word intact apart from any version number they state. Then run the full Elixir suite and reconcile any other proof assertion that pins a stale published-version literal, without weakening or removing an assertion; if an assertion cannot be satisfied without weakening it, stop and report rather than editing it. Commit the failing literals as `test(168-11): pin corrected published release truth`, then the changelog as `docs(168-11): state 0.2.1 as the published Hex release`.</action>
  <acceptance_criteria>
    - The changelog names the live published version as current and files the shipped entries under a published-version heading.
    - The phase 48 proof test still makes three exact-contains assertions with merge-blocking posture.
    - The full Elixir and JavaScript suites pass with no assertion weakened or deleted.
  </acceptance_criteria>
  <verify>
    <automated>mix test test/crosswake/proof/phase48_provider_adapter_proof_test.exs &amp;&amp; mix test test/mix/tasks/crosswake.docs.sync_test.exs &amp;&amp; elixir script/check_release_version_truth.exs &amp;&amp; mix test &amp;&amp; npm test</automated>
    <fails_when>non-zero exit; the phase 48 proof test reports 0 tests or any failure; the docs-sync task test reports any failure; the version-truth guard reports FAIL or BLOCKED; `mix test` reports any failure or runs 0 tests; `npm test` reports any failure or runs 0 tests</fails_when>
  </verify>
  <reversibility rating="reversible">Documentation and test-literal edits only; no public artifact changes.</reversibility>
  <done>Public-facing published-release truth matches the registry, and the guards that pin it agree.</done>
</task>

</tasks>

<threat_model>
Security posture: OWASP ASVS Level 1; high/critical findings block execution.

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repository documentation → public reader | Changelog claims are consumed as support and version truth by people who never read the code. |
| planning artifacts → public repository | Adopter identity must not cross into public files. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-168-39 | Information disclosure | example application naming | high | mitigate | Task 1 lands the neutral-name substitution and greps the three files for residual occurrences before any further edit. |
| T-168-40 | Repudiation | published-release claim | medium | mitigate | The published-truth sentence names the live version and is pinned by a merge-blocking exact literal. |
| T-168-41 | Tampering | proof assertions | high | mitigate | Literals are updated, never weakened or removed; the task stops and reports instead of relaxing an assertion. |
| T-168-42 | Spoofing | support-claim inflation | medium | mitigate | Advisory, verification-required, and deferred sections keep their membership and wording. |
</threat_model>

## Multi-Source Coverage Audit

| SOURCE | ID | Item | Plan(s) | Status | Notes |
|--------|----|------|---------|--------|-------|
| GAP | VERIFICATION gap 3 | Repository version truth agrees with what is published | 168-09, 168-10, 168-11 | COVERED | Coordinates, guard, and the public-facing changelog claim. |
| REQ | REL-04 | Exact `0.2.1` truth across the repository | 168-11 | COVERED | Published-release claim corrected. |
| REQ | REL-05 | Reversible release hygiene automated, guards intact | 168-11 | COVERED | Merge-blocking literals move with the truth instead of being dropped. |
| CONTEXT | D-30 | Evidence and public claims stay honest, privacy-bounded, and never overstated | 168-11 | COVERED | Anonymization lands first; no claim inflation. |
| CONTEXT | D-29 | Answer-first, honest state; never report a superseded fact as current | 168-11 | COVERED | Published-truth sentence names the live version. |
| CONTEXT | Deferred ideas | Companion releases, GitHub App, SLSA, dashboard | — | EXCLUDED | Not touched; deferred sections preserved verbatim. |

<verification>
The changelog names the live published version, files shipped entries under a published-version heading, keeps advisory and deferred sections intact, and both suites pass with the merge-blocking literals realigned rather than relaxed.
</verification>

<success_criteria>
A public reader of the changelog is told the truth about what is installable, and the guards that enforce that truth agree with it.
</success_criteria>

<output>
Create `.planning/workstreams/quality-ratchet-release/phases/168-0-2-1-release-candidate-readiness/168-11-SUMMARY.md` when done, recording which sentences changed, which proof literals moved with them, and confirming no assertion was weakened.
</output>
