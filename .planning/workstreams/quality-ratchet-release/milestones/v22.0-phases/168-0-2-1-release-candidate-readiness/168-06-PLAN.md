---
phase: 168-0-2-1-release-candidate-readiness
plan: "06"
type: execute
wave: 6
depends_on: [168-05]
files_modified:
  - .github/workflows/release-please.yml
  - .github/workflows/hex-publish.yml
  - .github/workflows/ios-mirror-backfill.yml
  - script/guarded_hex_publish.sh
  - script/release_candidate/android_publication.sh
  - script/check_release_workflow_integrity.exs
  - test/crosswake/release_candidate/workflow_test.exs
autonomous: true
requirements: [REL-02, REL-03, REL-04, REL-05]
estimate:
  tokens: 30000
  raw_tokens: 30000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "Trusted candidate rehearsal loads the mirror deploy key only for dry-run authority, binds the result to the exact current candidate head/tree/run, and changes no package or ref per D-22/D-23."
    - "After the one later approved merge, workflow guards require the approved head as a merge parent and identical approved tree before linked tags or publications; there is no release branch, RC tag, multi-PR controller, or second approval per D-05/D-06/D-17/D-20."
    - "The postapproval graph preserves exact child outcomes, stops dependents on failure, and reports immutable partial public success as `PARTIAL` with exact-ref forward recovery and no tag movement per D-18/D-19."
  artifacts:
    - path: ".github/workflows/release-please.yml"
      provides: "Approved-head/tree guard and fixed linked core/native postapproval graph."
    - path: ".github/workflows/hex-publish.yml"
      provides: "Exact-ref guarded Hex child publication and public artifact proof."
    - path: ".github/workflows/ios-mirror-backfill.yml"
      provides: "Credentialed candidate rehearsal plus normal immutable mirror publication."
    - path: "script/check_release_workflow_integrity.exs"
      provides: "Structural enforcement for approval, identity, graph, and recovery boundaries."
  key_links:
    - from: ".github/workflows/release-please.yml"
      to: ".github/workflows/hex-publish.yml"
      via: "approved release head/tree/version/receipt identity propagated to the existing publish workflow"
      pattern: "approved_head|approved_tree|candidate_receipt"
    - from: ".github/workflows/release-please.yml"
      to: ".github/workflows/ios-mirror-backfill.yml"
      via: "linked tag and exact source ref after merge/tree guard"
      pattern: "ios-core-v0.2.1"
---

<objective>
Wire trusted rehearsal and the one-approval postapproval release graph into existing workflow families.

Purpose: Ensure a later exact-head merge can release the linked unit without erasing partial success or widening approval scope.
Output: Trusted rehearsal, merge/tree guard, linked child graph, structural scanner, and exact-ref recovery truth.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/ADR-FIRST-B2C-ADOPTER.md
@.planning/workstreams/quality-ratchet-release/REQUIREMENTS.md
@.planning/workstreams/quality-ratchet-release/phases/168-0-2-1-release-candidate-readiness/168-CONTEXT.md
@.planning/workstreams/quality-ratchet-release/phases/168-0-2-1-release-candidate-readiness/168-RESEARCH.md
@.planning/workstreams/quality-ratchet-release/phases/168-0-2-1-release-candidate-readiness/168-05-SUMMARY.md
@.github/workflows/release-please.yml
@.github/workflows/hex-publish.yml
@.github/workflows/ios-mirror-backfill.yml
@script/check_release_workflow_integrity.exs

<interfaces>
- The only approval identity is the later refreshed Release Please PR #57 exact head/tree/base and candidate receipt; PR number alone is never authority.
- Workflow secrets remain in GitHub trusted jobs and are never serialized into artifacts, logs, summaries, or receipts.
- Companion PRs #115/#146/#147 remain open independent proposals and cannot be merged/dispatched by this graph.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Run credentialed candidate rehearsal in trusted existing workflows</name>
  <read_first>
    - .github/workflows/ios-mirror-backfill.yml
    - .github/workflows/hex-publish.yml
    - script/release_candidate/ios_mirror.sh
    - script/release_candidate/hex_artifacts.sh
    - lib/crosswake/release_candidate/receipt.ex
  </read_first>
  <files>.github/workflows/hex-publish.yml, .github/workflows/ios-mirror-backfill.yml, script/check_release_workflow_integrity.exs, test/crosswake/release_candidate/workflow_test.exs</files>
  <behavior>
    - A release-sensitive or exact refreshed Release Please candidate can run the six-package dry-run/build and credentialed real-mirror candidate dry-run without publication.
    - Trusted output binds requested/refreshed head, observed tree/base, run ID/head/conclusion, package digests, mirror split/remote refs/auth/dry-run, workflow blobs, and `external_state_changed: false`.
    - Secret absence/denial, stale head, workflow drift, any ref delta, any package publication, or incomplete artifact upload blocks `READY FOR APPROVAL`.
  </behavior>
  <action>Add fixture/structural tests, then extend the existing Hex and iOS workflows with candidate-rehearsal inputs/jobs rather than creating a workflow family. Pin permissions, load the mirror deploy key only in its rehearsal step, run the exact candidate command against freshly observed head/tree/base, and upload the canonical receipt/projections with exact run identity. Preserve all existing normal/recovery entry points but keep them unreachable during rehearsal. Mask credentials and bound diagnostics; assert no tags, refs, packages, or companion proposals changed per D-22/D-23.</action>
  <acceptance_criteria>
    - Trusted rehearsal covers package artifacts and real-mirror write authority for one exact head without external mutation.
    - Receipt workflow/run/head/tree and workflow-blob fields match the executing trusted job.
    - Missing/denied credentials, stale identity, or any remote-state delta is blocking and privacy-safe.
  </acceptance_criteria>
  <verify>
    <automated>actionlint .github/workflows/hex-publish.yml .github/workflows/ios-mirror-backfill.yml &amp;&amp; mix test test/crosswake/release_candidate/workflow_test.exs</automated>
    <fails_when>non-zero exit, 0 tests pass, actionlint reports an error, rehearsal can reach publication, or a secret/state-change fixture passes</fails_when>
  </verify>
  <done>The trusted workflow can prove exact-head package and mirror authority without changing external state.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Guard the linked postapproval graph by approved head and tree</name>
  <read_first>
    - .github/workflows/release-please.yml
    - .github/workflows/hex-publish.yml
    - .github/workflows/ios-mirror-backfill.yml
    - script/guarded_hex_publish.sh
    - release-please-config.json
    - .release-please-manifest.json
  </read_first>
  <files>.github/workflows/release-please.yml, .github/workflows/hex-publish.yml, .github/workflows/ios-mirror-backfill.yml, script/guarded_hex_publish.sh, script/release_candidate/android_publication.sh</files>
  <behavior>
    - A merged release runs only when the merge commit contains the approved head as a parent and its tree equals the approved candidate tree; squash/rebase/changed-tree/stale receipt fails before tags or publications.
    - The fixed graph is merge/tree guard, linked Release Please tags, Hex/Android/iOS-mirror child publications, exact public artifact proof, then fail-closed linked rollup; failed children stop only dependent jobs.
    - Exactly Hex core, iOS core, and Android core `0.2.1` participate; companion proposals and any release branch/RC tag/multi-PR merge controller are structurally absent.
  </behavior>
  <action>Extend the existing Release Please and publication workflows so the later D-20 merge triggers one fixed linked graph. Require approved-head parentage and identical tree immediately before any one-way action per D-05; pass exact version/ref/receipt digests to existing Hex and mirror guards plus a narrow Android publication adapter. Preserve child boundaries and `needs` dependencies per D-18 instead of claiming a transaction. Do not add an environment reviewer, second approval, generic controller, release branch, RC tag, companion merge, new workflow family, or Android feature/parity change per D-06/D-15/D-17/D-20.</action>
  <acceptance_criteria>
    - Changed-tree, missing-parent, stale receipt, wrong version, or companion-expanded fixtures fail before any publish/tag step.
    - The graph exposes exact independent Hex, Android, mirror, public-proof, and rollup child states.
    - No second reviewer gate exists; merge of the fully bound refreshed #57 head remains the single irreversible approval.
  </acceptance_criteria>
  <verify>
    <automated>actionlint .github/workflows/release-please.yml .github/workflows/hex-publish.yml .github/workflows/ios-mirror-backfill.yml &amp;&amp; elixir script/check_release_workflow_integrity.exs</automated>
    <fails_when>non-zero exit, actionlint reports an error, a bad merge/tree fixture reaches publication, the graph loses a linked child, or a second approval/companion/release-branch path is detected</fails_when>
  </verify>
  <done>The existing release workflows encode one exact-head approval and a guarded linked publication graph.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Preserve partial public truth and exact-ref recovery</name>
  <read_first>
    - lib/crosswake/release_candidate/receipt.ex
    - .github/workflows/release-please.yml
    - .github/workflows/hex-publish.yml
    - .github/workflows/ios-mirror-backfill.yml
    - script/check_release_workflow_integrity.exs
  </read_first>
  <files>.github/workflows/release-please.yml, script/guarded_hex_publish.sh, script/release_candidate/android_publication.sh, script/check_release_workflow_integrity.exs, test/crosswake/release_candidate/workflow_test.exs</files>
  <behavior>
    - Any successful public coordinate remains recorded when a sibling/dependent fails; the rollup is `PARTIAL` with exact successful coordinates, exact failed step/ref, and one safe next action.
    - Ordinary recovery retries idempotent exact refs or uses a forward fix/new version; immutable public package versions and semantic tags are never moved or silently replaced.
    - Exceptional force/replacement is unreachable without the separately approved recovery contract from Plan 05.
  </behavior>
  <action>Add failure-at-each-child and resume fixtures. Persist bounded child observations even when downstream jobs are skipped, feed them into the receipt's D-19 `PARTIAL` state, and make retries exact-ref/idempotent. Ensure public Hex/Android/tag/mirror success is never denied by a later failure and no rollback fiction appears. Structural checks must reject tag movement, version replacement, blind force, lost successful coordinates, or a retry from a mutable branch. This task implements future one-way handling only; do not execute publication during the plan.</action>
  <acceptance_criteria>
    - Failure fixtures at every child preserve prior successes and exact failed identity as `PARTIAL`.
    - Recovery uses exact refs and forward correction; normal code cannot move a public tag or replace a package version.
    - `COMPLETE` requires all linked children, exact-public proof, and live rollup success.
  </acceptance_criteria>
  <verify>
    <automated>mix test test/crosswake/release_candidate/workflow_test.exs &amp;&amp; elixir script/check_release_workflow_integrity.exs</automated>
    <fails_when>non-zero exit, 0 tests pass, partial success is lost or marked complete, mutable-ref recovery passes, or normal logic can move/replace public coordinates</fails_when>
  </verify>
  <done>The release graph reports irreversible partial reality exactly and supports safe forward/exact-ref recovery without tag movement.</done>
</task>

</tasks>

<threat_model>
Security posture: OWASP ASVS Level 1; high/critical findings block execution.

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-168-16 | Spoofing / Tampering | approval-to-merge identity | critical | mitigate | Approved head parentage, identical tree, exact receipt/workflow/run binding immediately before publication. |
| T-168-17 | Elevation of privilege | trusted credentials | high | mitigate | Least-permission existing jobs, step-local secret loading, dry-run reachability tests, and masked bounded outputs. |
| T-168-18 | Repudiation | partial publication | high | mitigate | Exact child-state persistence, immutable success retention, PARTIAL rollup, and exact-ref recovery fixtures. |
</threat_model>

## Adversarial Prohibition Recall

No new bespoke prohibition survives deduplication: silent publication and widened approval are expressed once as the flagged phase-wide trust/autonomy prohibition in Plan 08. Generic CI and secret-handling canon remains in this STRIDE register and later `$gsd-secure-phase` review.

## Artifacts this phase produces

- Existing-workflow candidate-rehearsal inputs/jobs and approved-head/tree guard fields.
- New adapter `script/release_candidate/android_publication.sh` for exact linked Android coordinate observation/publication only.
- Workflow result fields `approved_head`, `approved_tree`, `merge_oid`, `merge_parents`, `merge_tree`, `child_states`, `successful_coordinates`, `failed_step`, and `failed_ref`.
- New structural/fixture test path `test/crosswake/release_candidate/workflow_test.exs`.

## Source Coverage

| SOURCE | ID | Feature / requirement | Plan | Status | Notes |
|--------|----|-----------------------|------|--------|-------|
| REQ | REL-02 | Mirror publication boundary | 168-06 | COVERED | Existing trusted workflow uses Plan 05 policy. |
| REQ | REL-03 | Linked release tuple | 168-06 | COVERED | Hex/iOS/Android only. |
| REQ | REL-04 | Exact-head trusted proof | 168-06 | COVERED | Run/head/tree/workflow binding. |
| REQ | REL-05 | Safe postapproval graph | 168-06 | COVERED | Exact identity and partial truth. |
| CONTEXT | D-05, D-06, D-15 through D-20, D-23 through D-25 | Approval, graph, partial, credentials, mirror publication | 168-06 | COVERED | One fixed guarded graph in existing workflows. |

<verification>
Run all three task commands, then prove every one-way step has the approved-head/tree guard as a transitive dependency and every child-failure fixture retains prior public success.
</verification>

<success_criteria>
The existing workflow family can rehearse credentials reversibly and, only after the one later merge approval, execute a guarded linked graph with honest partial outcomes.
</success_criteria>

<output>
Create `.planning/workstreams/quality-ratchet-release/phases/168-0-2-1-release-candidate-readiness/168-06-SUMMARY.md` when done.
</output>
