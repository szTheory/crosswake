---
phase: 168-0-2-1-release-candidate-readiness
plan: "09"
type: execute
wave: 9
depends_on: [168-08]
gap_closure: true
files_modified:
  - mix.exs
  - .release-please-manifest.json
  - README.md
  - examples/android_shell_host/app/build.gradle
  - examples/android_shell_host/app/src/main/assets/crosswake_manifest.json
  - examples/ios_shell_host/Fixtures/crosswake_manifest.json
  - examples/native_evidence/evidence-manifest.example.json
  - examples/phoenix_host/evidence/evidence-manifest.example.json
  - examples/phoenix_host/priv/crosswake/install_manifest.json
  - guides/android_uat.md
  - packages/crosswake-shell-core-android/build.gradle.kts
autonomous: false
requirements: [REL-03, REL-04]
estimate:
  tokens: 26000
  raw_tokens: 26000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "The repository's declared core/native version truth on protected default equals the version that is actually published and tagged (`0.2.1`), closing 168-VERIFICATION gap 3 (REL-03, REL-04)."
    - "Release Please can no longer re-form an already-published `0.2.1` candidate off a `0.2.0` manifest, and PR #158 is closed unmerged so no duplicate tag or duplicate Hex publish can be triggered (D-19: never move a public tag or silently replace a package)."
    - "Restoring version truth changes no public package, no semantic tag, and no mirror ref; `refs/tags/hex-v0.2.1` still resolves to `b780a198` afterwards (D-18, D-19)."
  artifacts:
    - path: "mix.exs"
      provides: "Root Hex version truth `0.2.1` matching the published Hex release."
    - path: ".release-please-manifest.json"
      provides: "Linked core/iOS/Android manifest truth `0.2.1` matching the three published release tags."
  key_links:
    - from: ".release-please-manifest.json"
      to: "refs/tags/hex-v0.2.1"
      via: "manifest core version equals the newest published release tag version"
      pattern: "0\\.2\\.1"
    - from: "protected default version truth"
      to: "PR #158"
      via: "restore lands first, then the duplicate-release proposal is closed unmerged"
      pattern: "release-please--branches--main"
  prohibitions:
    - id: PROHIBITION-NO-REPUBLISH
      statement: "This plan must never merge PR #158, create or move a semantic tag, publish a package, push a mirror ref, or dispatch any publication workflow. It edits repository files and closes one pull request."
      status: flagged_unverified
      flagged: true
---

<objective>
Restore the repository's declared `0.2.1` version truth on protected default, then close the duplicate-release proposal PR #158 unmerged.

Purpose: `origin/main` currently declares `0.2.0` while `crosswake 0.2.1` is published on Hex, Maven Central, and the iOS mirror and tagged at `b780a198`. That understatement keeps an open Release Please PR armed to re-tag and re-publish an already-public version. This is the only live inconsistency among the three verification gaps.
Output: Eleven coordinate files back at `0.2.1` on protected default, and PR #158 closed without merge.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/workstreams/quality-ratchet-release/STATE.md
@.planning/workstreams/quality-ratchet-release/REQUIREMENTS.md
@.planning/workstreams/quality-ratchet-release/phases/168-0-2-1-release-candidate-readiness/168-CONTEXT.md
@.planning/workstreams/quality-ratchet-release/phases/168-0-2-1-release-candidate-readiness/168-VERIFICATION.md

<interfaces>
- Branching strategy is `none`; work commits directly to protected `main`. Use the repository's Phase 168 commit style (`fix(168-09): ...`).
- `c7edcd78` ("fix(168-08): restore untagged candidate state") is the exact rollback being undone. It reverted precisely eleven files from `0.2.1` to `0.2.0`. Its reverse patch was confirmed to apply cleanly against the current tree at planning time.
- `b780a198` is the published, tagged `0.2.1` merge commit and is an ancestor of `origin/main`. The eleven files at that commit are the target state; no other file differs between the two for version reasons.
- `refs/tags/{hex,ios-core,android-core}-v0.2.1` already exist on origin and are immutable. Nothing in this plan may touch them.
- PR #158 ("chore: release main", branch `release-please--branches--main`) proposes `"."` = `0.2.1` — a version already published. It is closed, never merged.
- The working tree carries unrelated in-flight edits to `CHANGELOG.md`, `guides/install.md`, and `test/mix/tasks/crosswake_install_test.exs`. None of those files belong to this plan; leave them uncommitted here. Plan 168-11 lands them.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Reverse the post-publication version rollback across the eleven coordinate files</name>
  <read_first>
    - .planning/workstreams/quality-ratchet-release/phases/168-0-2-1-release-candidate-readiness/168-VERIFICATION.md
    - .release-please-manifest.json
    - mix.exs
  </read_first>
  <files>mix.exs, .release-please-manifest.json, README.md, examples/android_shell_host/app/build.gradle, examples/android_shell_host/app/src/main/assets/crosswake_manifest.json, examples/ios_shell_host/Fixtures/crosswake_manifest.json, examples/native_evidence/evidence-manifest.example.json, examples/phoenix_host/evidence/evidence-manifest.example.json, examples/phoenix_host/priv/crosswake/install_manifest.json, guides/android_uat.md, packages/crosswake-shell-core-android/build.gradle.kts</files>
  <action>Inspect `git show c7edcd78` to read the exact eleven-file rollback, then reverse-apply it with `git show c7edcd78 | git apply -R`. Confirm `git status --porcelain` lists exactly those eleven paths as modified by this step and that the three pre-existing unrelated edits named in the interfaces block are untouched. Confirm every one of the eleven now matches its state at `b780a198`. Run the full Elixir and JavaScript suites; if a test asserts the stale `0.2.0` repository version truth (rather than an intentional `0.2.0` baseline such as the mirror baseline constant or a historical fixture), update that assertion in the same commit and say so in the commit body — an assertion that pins the repo to an understated version is part of the defect, but `Crosswake.ReleaseStatus`'s `@mirror_baseline_version` and the phase 153 mirror-parity fixtures are legitimately about the `0.2.0` baseline and must not be changed. Commit as `fix(168-09): restore 0.2.1 version truth on protected default` and push to `main`. Create no tag, publish nothing, dispatch no workflow, and do not touch PR #158 in this task (D-19: recovery is a forward fix; public tags and packages are never moved or replaced).</action>
  <verify>
    <automated>test -z "$(git diff --name-only b780a198 -- mix.exs .release-please-manifest.json README.md examples/android_shell_host/app/build.gradle examples/android_shell_host/app/src/main/assets/crosswake_manifest.json examples/ios_shell_host/Fixtures/crosswake_manifest.json examples/native_evidence/evidence-manifest.example.json examples/phoenix_host/evidence/evidence-manifest.example.json examples/phoenix_host/priv/crosswake/install_manifest.json guides/android_uat.md packages/crosswake-shell-core-android/build.gradle.kts)" &amp;&amp; git fetch --quiet --tags origin &amp;&amp; test "$(git rev-parse 'refs/tags/hex-v0.2.1^{commit}')" = b780a19863936619394087f1ffd384f1dca17c93 &amp;&amp; elixir script/check_release_workflow_integrity.exs &amp;&amp; mix crosswake.release.status &amp;&amp; mix test &amp;&amp; npm test</automated>
    <fails_when>non-zero exit; any of the eleven paths still differs from its `b780a198` content; `refs/tags/hex-v0.2.1` resolves to anything other than `b780a198`; the integrity scanner prints any FAIL line or fewer than its full check set; `mix crosswake.release.status` reports a lockstep manifest or lockstep config failure; `mix test` reports any failure or runs 0 tests; `npm test` reports any failure or runs 0 tests</fails_when>
  </verify>
  <reversibility rating="reversible">This edits repository files only. No public package, semantic tag, or mirror ref changes, so an ordinary revert commit undoes it.</reversibility>
  <done>Protected default declares `0.2.1` across all eleven coordinate files, both suites are green, and the published tags are untouched.</done>
</task>

<task type="checkpoint:decision" gate="blocking-human">
  <name>Task 2: Authorize closing the duplicate-release proposal PR #158</name>
  <read_first>
    - .planning/workstreams/quality-ratchet-release/phases/168-0-2-1-release-candidate-readiness/168-VERIFICATION.md
  </read_first>
  <action>Re-read PR #158's current head and proposed diff with `gh pr view 158` and `gh pr diff 158` immediately before presenting, and re-confirm that protected default now declares `0.2.1`. Present the exact PR number, head ref, proposed version tuple, the already-published coordinates it would duplicate, and the fact that `refs/tags/hex-v0.2.1` already exists at `b780a198`. Do not close the PR in this task and do not merge it under any answer.</action>
  <decision>Close PR #158 unmerged so an already-published `0.2.1` cannot be re-tagged and re-published, accepting that the next genuine release is `0.2.2`; or leave it open and stop.</decision>
  <context>The maintainer already made this call on 2026-09-15 during the forensic pass: restore version truth to `0.2.1` and close #158 unmerged. This checkpoint exists because closing an open pull request is an outward-facing act, not because the direction is unsettled. Merging #158 is prohibited under every option — it would attempt to tag over an immutable public tag and re-publish a live Hex version, which D-19 forbids.</context>
  <options>
    <option id="close-158-unmerged">
      <name>Close PR #158 unmerged (matches the recorded maintainer decision)</name>
      <pros>Disarms the duplicate tag-and-publish hazard; version truth and published truth agree; the next release forms cleanly as `0.2.2`.</pros>
      <cons>The `0.2.1` release notes Release Please staged in that PR are discarded and must be re-derived if wanted later.</cons>
    </option>
    <option id="leave-open">
      <name>Leave PR #158 open and stop</name>
      <pros>Changes nothing outward-facing right now.</pros>
      <cons>Leaves an open, mergeable proposal whose merge would attempt to move a published tag and republish a live version.</cons>
    </option>
  </options>
  <resume-signal>Reply `close 158` to authorize the close, or `leave open` to stop this plan here.</resume-signal>
  <reversibility rating="reversible">A closed GitHub pull request can be reopened, and Release Please will re-propose a candidate on the next release-worthy commit.</reversibility>
  <done>The maintainer has explicitly authorized or declined the close, with the current PR state and the published coordinates in front of them.</done>
</task>

<task type="auto">
  <name>Task 3: Close PR #158 unmerged and record the disarmed hazard</name>
  <precondition>Task 2 returned `close 158`. If it returned `leave open`, stop without touching PR #158.</precondition>
  <read_first>
    - .planning/workstreams/quality-ratchet-release/phases/168-0-2-1-release-candidate-readiness/168-VERIFICATION.md
  </read_first>
  <files>(no repository files; this task acts on GitHub pull request state only)</files>
  <action>Close PR #158 with `gh pr close 158` and a comment stating that `crosswake 0.2.1` is already published and tagged at `b780a198`, that protected default has been restored to `0.2.1` version truth, and that the next genuine release is `0.2.2`. Verify the PR is `CLOSED` and was not merged. Re-assert that all three `0.2.1` release tags still point at `b780a198`. Do not delete the `release-please--branches--main` branch, do not dispatch any workflow, and do not create a replacement release PR by hand — Release Please forms the next candidate on its own from the now-correct manifest.</action>
  <verify>
    <automated>test "$(gh pr view 158 --json state --jq '.state')" = CLOSED &amp;&amp; test "$(gh pr view 158 --json mergedAt --jq '.mergedAt // "null"')" = null &amp;&amp; git fetch --quiet --tags origin &amp;&amp; test "$(git rev-parse 'refs/tags/hex-v0.2.1^{commit}')" = b780a19863936619394087f1ffd384f1dca17c93 &amp;&amp; test "$(git rev-parse 'refs/tags/ios-core-v0.2.1^{commit}')" = b780a19863936619394087f1ffd384f1dca17c93 &amp;&amp; test "$(git rev-parse 'refs/tags/android-core-v0.2.1^{commit}')" = b780a19863936619394087f1ffd384f1dca17c93</automated>
    <fails_when>non-zero exit; PR 158 reports any state other than CLOSED; `mergedAt` is a timestamp rather than null; any of the three `0.2.1` tags resolves to a commit other than `b780a198`</fails_when>
  </verify>
  <reversibility rating="costly">Closing a pull request is reopenable, but the staged release-note content and its review history stop being the live proposal, so restoring the same candidate means re-deriving it.</reversibility>
  <done>PR #158 is closed unmerged, no tag moved, and no package was published.</done>
</task>

</tasks>

<threat_model>
Security posture: OWASP ASVS Level 1; high/critical findings block execution.

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repository default branch → public registries | A merge of a release proposal crosses from repository state into immutable public package and tag state. |
| local agent → GitHub API | Pull request state mutation via `gh`. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-168-31 | Tampering | `refs/tags/{hex,ios-core,android-core}-v0.2.1` | critical | mitigate | Every task re-asserts the three tags still resolve to `b780a198`; no task has tag-write authority. |
| T-168-32 | Elevation of privilege | PR #158 merge path | critical | mitigate | Merging is prohibited under every checkpoint option; the plan only closes, behind a `blocking-human` gate. |
| T-168-33 | Repudiation | version truth restore | medium | mitigate | One scoped commit whose body names `c7edcd78` as the rollback being reversed and the published coordinates it now matches. |
| T-168-34 | Tampering | unrelated in-flight working-tree edits | low | mitigate | The commit is scoped to the eleven named paths; the three unrelated dirty files are explicitly out of scope and deferred to 168-11. |
</threat_model>

## Multi-Source Coverage Audit

| SOURCE | ID | Item | Plan(s) | Status | Notes |
|--------|----|------|---------|--------|-------|
| GAP | VERIFICATION gap 3 | Re-apply `0.2.1` across the eleven reverted files | 168-09 | COVERED | Task 1. |
| GAP | VERIFICATION gap 3 | Close PR #158 unmerged after the restore lands | 168-09 | COVERED | Tasks 2–3, in that order. |
| GAP | VERIFICATION gap 3 | Regression check that the manifest is never behind the newest published tag | 168-10 | COVERED | Split out so the guard is authored against a correct manifest. |
| REQ | REL-03 | Coordinate and floor consistency | 168-09, 168-10 | COVERED | Manifest, `mix.exs`, and Android Gradle coordinates realigned. |
| REQ | REL-04 | Exact `0.2.1` candidate truth | 168-09 | COVERED | Declared version equals published version. |
| CONTEXT | D-18, D-19 | Fail-closed graph; never move a public tag or replace a package | 168-09 | COVERED | Prohibition plus per-task tag re-assertion. |
| CONTEXT | D-16, D-17 | Companion release PRs stay independent; no multi-PR controller | 168-09 | COVERED | Only PR #158 is acted on; companion proposals are untouched. |
| CONTEXT | Deferred ideas | Companion releases, GitHub App, SLSA, dashboard | — | EXCLUDED | Out of scope for gap closure. |

<verification>
Protected default declares `0.2.1` across the eleven coordinate files and matches `b780a198` on each; both regression suites are green; PR #158 is CLOSED and unmerged; all three `0.2.1` tags still resolve to `b780a198`.
</verification>

<success_criteria>
Repository version truth and published truth agree, and no open proposal can re-publish an already-live version — achieved without publishing, tagging, or moving anything public.
</success_criteria>

<output>
Create `.planning/workstreams/quality-ratchet-release/phases/168-0-2-1-release-candidate-readiness/168-09-SUMMARY.md` when done, recording the restore commit SHA, the PR #158 close outcome, and explicit confirmation that no tag or package changed.
</output>
