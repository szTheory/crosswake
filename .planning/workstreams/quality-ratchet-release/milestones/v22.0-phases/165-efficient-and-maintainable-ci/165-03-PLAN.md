---
phase: 165-efficient-and-maintainable-ci
plan: 03
type: execute
wave: 3
depends_on: [165-02]
files_modified:
  - script/select_obsolete_ci_runs.py
  - test/fixtures/ci/cancellation
  - test/crosswake/proof/phase165_ci_policy_test.exs
  - .github/workflows/cancel-obsolete-crosswake-ci.yml
  - test/crosswake/proof/phase165_ci_integrity_test.exs
autonomous: true
requirements: [CIP-02, CIP-04]
estimate:
  tokens: 32000
  raw_tokens: 32000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Ordinary PR concurrency is keyed by repository plus PR number with cancel-in-progress false; the trusted controller alone decides cancellation per D-04 and D-05."
    - "The controller runs from default-branch workflow_run: requested code, checks out no PR code, and holds only actions: write per D-06."
    - "[Edge CIP-02/adjacency] For the same PR/workflow, a candidate with run ID current-1 is cancellable, while equal and current+1 are not."
    - "[Edge CIP-02/empty] Empty candidate sets, missing/multiple PRs, missing workflow identity, malformed IDs, incomplete pagination, or ambiguous API state select no cancellation."
    - "[Edge CIP-02/ordering] Candidate input order does not affect the deterministically sorted strict-lower-ID selection."
    - "[Edge CIP-02/concurrency] Concurrent controller invocations can cancel only IDs lower than their own requested run and never cancel the greatest authoritative run."
  assumptions:
    - statement: "[FLAGGED ASSUMPTION edge CIP-04/unclassified] GitHub workflow_run: requested reaches the trusted controller early enough to bound obsolete runner use; Plan 10 must retain a live timing probe before authority is claimed."
      status: unresolved
  artifacts:
    - path: "script/select_obsolete_ci_runs.py"
      provides: "pure strict-lower-run-ID selection policy"
    - path: ".github/workflows/cancel-obsolete-crosswake-ci.yml"
      provides: "trusted default-branch cancellation controller"
  key_links:
    - from: ".github/workflows/cancel-obsolete-crosswake-ci.yml"
      to: "script/select_obsolete_ci_runs.py"
      via: "validated workflow-run JSON is selected before any cancel API mutation"
      pattern: "workflow_run|requested|actions: write"
  prohibitions:
    - requirement_id: CIP-02
      category: safety
      statement: "MUST NOT cancel main, release, scheduled-audit, manual-recovery, publish, or future merge-queue authority while pruning superseded PR proof."
      status: unresolved
      flagged: true
    - requirement_id: CIP-04
      category: safety
      statement: "MUST NOT retry assertions or proof commands under the label of bounded cancellation or dependency-fetch resilience."
      status: unresolved
      flagged: true
---

<objective>
Build and statically prove monotonic PR cancellation without granting untrusted code privileged authority.

Purpose: Remove superseded cycles while making cancellation inversion and fork collisions impossible by contract.
Output: Pure selector, negative fixtures, least-privilege trusted controller, and deferred live-timing acceptance hook.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/quality-ratchet-release/phases/165-efficient-and-maintainable-ci/165-02-SUMMARY.md
@.planning/workstreams/quality-ratchet-release/phases/165-efficient-and-maintainable-ci/165-CONTEXT.md
@.planning/workstreams/quality-ratchet-release/phases/165-efficient-and-maintainable-ci/165-RESEARCH.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Prove strict lower-ID selection as a pure closed policy</name>
  <read_first>
    script/check_aggregator_result_semantics.py
    .github/workflows/crosswake-ci.yml
    .planning/workstreams/quality-ratchet-release/phases/165-efficient-and-maintainable-ci/165-PATTERNS.md
  </read_first>
  <files>script/select_obsolete_ci_runs.py, test/fixtures/ci/cancellation, test/crosswake/proof/phase165_ci_policy_test.exs</files>
  <behavior>
    - Select only positive candidate IDs strictly below current for one identical workflow and exactly one identical PR.
    - Return a deterministic empty selection for equal/newer/mismatch/missing/malformed/multiple-PR/incomplete-pagination/ambiguous inputs.
    - Permutations and concurrent current-run perspectives never select the maximum run ID.
  </behavior>
  <action>Implement a standard-library-only pure selector with closed validation of repository, workflow identity, event, exactly one PR number, positive integer run IDs, and complete pagination. Separate selection from network mutation. Add table-driven fixtures for lower/equal/greater, same-named fork branches, PR/workflow/repository mismatch, missing/multiple PRs, malformed/overflow-like IDs, duplicate candidates, permutations, incomplete pages, and inversion sequences per D-04 and D-05.</action>
  <acceptance_criteria>
    - Only strict lower IDs for the same repository, workflow, and PR are returned, sorted once and without duplicates.
    - Every ambiguous or incomplete fixture returns an empty selection and closed reason without coercion.
    - A two-controller inversion fixture proves the older requested run cannot select the newer run.
  </acceptance_criteria>
  <verify>
    <automated>python3 script/select_obsolete_ci_runs.py --self-test &amp;&amp; mix test test/crosswake/proof/phase165_ci_policy_test.exs --only cancellation</automated>
  </verify>
  <done>All four CIP-02 edge probes and cancellation inversion are executable without GitHub mutation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Isolate the trusted requested-run controller at minimum permission</name>
  <read_first>
    .github/workflows/required-checks-audit.yml
    .github/workflows/release-please.yml
    .github/workflows/crosswake-ci.yml
    script/select_obsolete_ci_runs.py
  </read_first>
  <files>.github/workflows/cancel-obsolete-crosswake-ci.yml, test/crosswake/proof/phase165_ci_integrity_test.exs</files>
  <behavior>
    - Controller trigger is default-branch `workflow_run` activity `requested` for Crosswake CI only.
    - It has actions: write and no broader write permission, performs no PR checkout, and validates event/workflow/repository/PR/run identities before mutation.
    - Ordinary cancel precedes bounded polling and force-cancel; malformed/ambiguous/no-selection exits neutral and records a closed summary.
  </behavior>
  <action>Create the trusted controller from default-branch code with `workflow_run: {workflows: [Crosswake CI], types: [requested]}` and minimum `actions: write`; omit checkout entirely by embedding the small validated selector invocation or fetching only trusted default-branch policy bytes by immutable repository authority, never PR code. Keep Crosswake CI `cancel-in-progress: false`. Validate complete candidate pagination, request ordinary cancel, poll for a bounded interval, and force-cancel only a still-running selected lower ID. Mark the controller non-authoritative until Plan 10's live requested-event timing/inversion probe passes. Preserve D-24 release/recovery non-cancellation.</action>
  <acceptance_criteria>
    - Structural test rejects pull_request_target, PR checkout, contents write, issues write, secrets, broad write-all, non-requested activity, or built-in cancel-in-progress true.
    - Controller mutation is unreachable unless the pure policy returns one or more strict lower IDs.
    - Release Please retains its existing non-cancelling queue/approval semantics byte-for-byte in this plan.
  </acceptance_criteria>
  <verify>
    <automated>actionlint .github/workflows/cancel-obsolete-crosswake-ci.yml .github/workflows/crosswake-ci.yml &amp;&amp; python3 script/select_obsolete_ci_runs.py --self-test &amp;&amp; mix test test/crosswake/proof/phase165_ci_integrity_test.exs --only cancellation_controller</automated>
  </verify>
  <done>The controller is least-privilege, default-branch trusted, strict-lower only, and explicitly awaits live timing proof before cancellation authority is claimed.</done>
</task>

</tasks>

<threat_model>
Security posture: OWASP ASVS Level 1; every high or critical threat blocks execution until its listed mitigation passes.

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| workflow_run payload → controller | Untrusted event/API metadata reaches an actions-write workflow. |
| controller → GitHub cancel API | A privileged mutation can deny authoritative CI if selection inverts. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-165-01 | Denial of service | concurrency identity | high | mitigate | Repository plus PR-number identity and exact workflow validation. |
| T-165-02 | Tampering / Denial of service | selector/controller | high | mitigate | Strict lower-ID pure policy, complete pagination, ambiguity no-op, live inversion probe. |
| T-165-05 | Elevation of privilege | controller | high | mitigate | Default-branch requested event, no PR checkout, minimum actions write, no secrets. |
</threat_model>

## Artifacts this phase produces

New surfaces: `script/select_obsolete_ci_runs.py` (`--self-test`, `--current-run`, `--candidates`), `test/fixtures/ci/cancellation/**`, and workflow `cancel-obsolete-crosswake-ci.yml` triggered by `workflow_run: requested`. It produces only the closed dispositions `cancel_lower`, `no_op`, and `invalid`; it adds no app API or schema.

<verification>
Run both task commands; live authority remains deliberately unclaimed until Plan 10.
</verification>

<success_criteria>
Static and fixture evidence proves cancellation monotonicity, fork isolation, and privileged-controller isolation.
</success_criteria>

<output>
Create `.planning/workstreams/quality-ratchet-release/phases/165-efficient-and-maintainable-ci/165-03-SUMMARY.md` when done.
</output>
