---
phase: 165-efficient-and-maintainable-ci
plan: 05
type: execute
wave: 5
depends_on: [165-02, 165-04]
files_modified:
  - .github/workflows/crosswake-ci.yml
  - .github/workflows/aggregator-negative-control.yml
  - .github/workflows/contract-drift-gate.yml
  - .github/workflows/dependency-security.yml
  - .github/workflows/requires-example-host-gate.yml
  - .github/workflows/phase130-proof.yml
  - .github/workflows/phase132-proof.yml
  - .github/workflows/phase23-proof.yml
  - .github/workflows/phase34-proof.yml
  - script/ci_leaf_manifest.json
  - script/check_ci_leaf_manifest.py
  - script/list_merge_blocking_checks.py
  - test/crosswake/proof/phase165_ci_integrity_test.exs
autonomous: true
requirements: [CIP-02, CIP-05, CIP-07]
estimate:
  tokens: 50000
  raw_tokens: 50000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "Core security, contract, example-host, hermetic, engine, and commerce proof appears as literal purpose-named Crosswake CI leaves; each live legacy required display name is emitted separately by a checkout-free compatibility conclusion until Plan 12 per D-07, D-10, D-11, and D-12."
    - "Moved PR proof no longer responds to generic branch push or landed main; distinct schedule/manual advisory jobs remain in their original non-PR trust boundary per D-01 and D-02."
    - "Documentation-only classification explicitly marks these executable families irrelevant while still scheduling the D-18 bounded documentation/privacy/planning contract."
  artifacts:
    - path: ".github/workflows/crosswake-ci.yml"
      provides: "literal jobs guard-01-contract-drift-test, guard-02-generate-and-diff, merge-blocking-dependency-security, merge-blocking-requires-example-host, phase130/132 hermetic and engine proof, and Phase 23/34 commerce proof"
  key_links:
    - from: "script/ci_leaf_manifest.json"
      to: ".github/workflows/crosswake-ci.yml"
      via: "each migrated core leaf carries its exact display name, family, remediation command, and static umbrella need"
      pattern: "contract-drift|dependency-security|requires-example-host|hermetic|commerce"
  prohibitions:
    - requirement_id: CIP-07
      category: transparency
      statement: "MUST NOT collapse core, example-host, engine, security, and commerce evidence into one opaque command or remove their purpose-named logs and artifacts."
      status: unresolved
      flagged: true
---

<objective>
Expand the proven PR workflow across the core infrastructure and hermetic proof families without changing scheduled/advisory trust.

Purpose: Begin eliminating duplicated PR/push execution while retaining exact proof evidence and legacy required contexts during migration.
Output: Core/infra literal leaves in Crosswake CI, stripped duplicate PR triggers, and exact manifest parity.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/quality-ratchet-release/phases/165-efficient-and-maintainable-ci/165-02-SUMMARY.md
@.planning/workstreams/quality-ratchet-release/phases/165-efficient-and-maintainable-ci/165-04-SUMMARY.md
@.planning/workstreams/quality-ratchet-release/phases/165-efficient-and-maintainable-ci/165-CONTEXT.md
@.planning/workstreams/quality-ratchet-release/phases/165-efficient-and-maintainable-ci/evidence/required-context-baseline.json
@.planning/workstreams/quality-ratchet-release/phases/164-dependency-security-and-gate-authority/164-05-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Move core gate-integrity leaves into Crosswake CI</name>
  <precondition>The maximum-shape gate from Plan 02 passes, and a fresh read-only `verify-required-context-snapshot --live` exactly matches the Plan 01 strictness, sorted required-context set, source SHA, and protection digest.</precondition>
  <read_first>
    .github/workflows/crosswake-ci.yml
    .github/workflows/aggregator-negative-control.yml
    .github/workflows/contract-drift-gate.yml
    .github/workflows/dependency-security.yml
    .github/workflows/requires-example-host-gate.yml
    .planning/workstreams/quality-ratchet-release/phases/165-efficient-and-maintainable-ci/evidence/required-context-baseline.json
  </read_first>
  <files>.github/workflows/crosswake-ci.yml, .github/workflows/aggregator-negative-control.yml, .github/workflows/contract-drift-gate.yml, .github/workflows/dependency-security.yml, .github/workflows/requires-example-host-gate.yml</files>
  <behavior>
    - Exact existing proof commands and display names remain literal jobs with per-job timeout and remediation summary.
    - The Crosswake CI umbrella statically needs every moved job; the legacy contract-drift context remains produced and fail-closed until Plan 12 retirement.
  </behavior>
  <action>Before editing or removing any source trigger, run the live required-context snapshot verifier and stop on any strictness, set, source, or digest drift. Move aggregator negative-control, contract-drift guards, dependency-security, and requires-example-host PR proof into the exact target IDs listed in this plan's mapping table. For every matching context in the frozen Plan 01 snapshot, add the exact listed checkout-free compatibility job with the snapshot's literal display string and a closed dependency on the corresponding new leaf or umbrella; record it under `legacy_compatibility_contexts`, outside umbrella `proof_leaves` and `required_control_nodes`, so current protection remains satisfied and Plan 12 can delete it without deleting meaningful proof. Remove pull_request and generic push product-proof triggers only after the new leaf, compatibility producer, and snapshot parity all pass; delete the four source workflow files because none owns a distinct retained authority. Preserve Phase 164 commands/artifacts per D-01, D-02, D-07 through D-12, and D-17.</action>
  <acceptance_criteria>
    - Every moved proof exists exactly once on pull_request with its literal purpose phrase, command, timeout, summary, artifact behavior, and local remediation command; every live legacy required display name has exactly one separate compatibility producer.
    - Source workflows no longer schedule equivalent PR or generic push work; any retained workflow has only a justified non-PR trigger.
    - Compatibility contract-drift conclusion fails when either guard is failed, cancelled, missing, timed out, stale, action-required, unknown, or unexplained skipped.
    - Immediately before each source-trigger removal, live strictness/set/digest equals `required-context-baseline.json`; any drift leaves the source producer intact and stops the task.
  </acceptance_criteria>
  <verify>
    <automated>test ! -e .github/workflows/aggregator-negative-control.yml &amp;&amp; test ! -e .github/workflows/contract-drift-gate.yml &amp;&amp; test ! -e .github/workflows/dependency-security.yml &amp;&amp; test ! -e .github/workflows/requires-example-host-gate.yml &amp;&amp; actionlint .github/workflows/crosswake-ci.yml &amp;&amp; python3 script/check_aggregator_result_semantics.py --self-test &amp;&amp; python3 script/list_merge_blocking_checks.py --emitters &gt;/dev/null</automated>
  </verify>
  <done>Core integrity leaves run once per PR inside Crosswake CI and current required contexts remain live.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Move hermetic engine and commerce PR proof while retaining scheduled advisories</name>
  <precondition>A fresh read-only required-context snapshot verification still matches the Plan 01 authority artifact after Task 1.</precondition>
  <read_first>
    .github/workflows/phase130-proof.yml
    .github/workflows/phase132-proof.yml
    .github/workflows/phase23-proof.yml
    .github/workflows/phase34-proof.yml
    .github/workflows/crosswake-ci.yml
    .planning/workstreams/quality-ratchet-release/phases/165-efficient-and-maintainable-ci/evidence/required-context-baseline.json
  </read_first>
  <files>.github/workflows/crosswake-ci.yml, .github/workflows/phase130-proof.yml, .github/workflows/phase132-proof.yml, .github/workflows/phase23-proof.yml, .github/workflows/phase34-proof.yml</files>
  <behavior>
    - Phase 130/132 core-hermetic and companion-engine proof and Phase 23/34 merge-blocking commerce proof run as distinct literal PR jobs.
    - Existing scheduled/manual engine-present and commerce advisory jobs remain separate and non-cancelling; no schedule is transferred into PR concurrency.
  </behavior>
  <action>Re-run the live required-context snapshot verifier before removing triggers. Move the PR-owned jobs from the four phase workflows into Crosswake CI using the exact source-qualified target IDs in the mapping table. For each context in the frozen Plan 01 snapshot, emit its listed checkout-free compatibility job with the exact old display string; keep compatibility jobs outside umbrella proof leaves/control nodes and bind them fail-closed to the new leaf/umbrella. Preserve both Phase 130 and Phase 132 proof evidence where commands/fixtures differ; do not merge them merely because job IDs were historically similar. Keep scheduled/manual advisory jobs in source workflows with PR and generic push removed, preserving permissions and secrets. Use the existing Elixir setup composite, Linux runner, bounded timeout, full-proof condition, and exact D-32 remediation summary. Do not create heterogeneous matrices per D-11 through D-13.</action>
  <acceptance_criteria>
    - Producer audit reports one PR producer for each legacy required display name and no duplicate job/context.
    - Schedule/manual advisory jobs remain runnable under their prior permission/secret posture and are absent from ordinary PR concurrency.
    - Documentation-only mode explicitly marks the migrated executable leaves irrelevant with manifest-authorized reasons.
    - Frozen-snapshot verification passes immediately before all four source trigger edits; live drift stops without removing a producer.
  </acceptance_criteria>
  <verify>
    <automated>actionlint .github/workflows/crosswake-ci.yml .github/workflows/phase130-proof.yml .github/workflows/phase132-proof.yml .github/workflows/phase23-proof.yml .github/workflows/phase34-proof.yml &amp;&amp; python3 script/list_merge_blocking_checks.py --emitters &gt;/dev/null</automated>
  </verify>
  <done>Hermetic, engine, and commerce PR proof is single-run and named, while scheduled advisory authority stays separate.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Freeze exact parity for the first migrated leaf cohort</name>
  <read_first>
    script/ci_leaf_manifest.json
    script/check_ci_leaf_manifest.py
    script/list_merge_blocking_checks.py
    .github/workflows/crosswake-ci.yml
    test/crosswake/proof/phase165_ci_integrity_test.exs
  </read_first>
  <files>script/ci_leaf_manifest.json, script/check_ci_leaf_manifest.py, script/list_merge_blocking_checks.py, test/crosswake/proof/phase165_ci_integrity_test.exs</files>
  <behavior>
    - Migrated leaf inventory equals workflow declarations and umbrella static needs with unique literal names and exact remediation commands.
    - Product proof in this cohort has pull_request authority only; retained scheduled/manual workflows contain no PR or generic branch push trigger.
  </behavior>
  <action>Populate manifest arrays `proof_leaves`, `required_control_nodes`, and `legacy_compatibility_contexts`: the umbrella statically needs exactly `proof_leaves union required_control_nodes`; `classify-change` remains the required control node; no compatibility conclusion is in that union. Every compatibility conclusion statically needs the umbrella or its exact proof result and emits one frozen-snapshot display name. Mark scheduled/advisory jobs outside all three arrays, and extend structural tests for one authoritative PR trigger and no equivalent push duplication. Validate timeout, runner, permission, needs direction, acyclic graph, maximum-shape budget, and producer invariants without relying on retained counts per D-01, D-02, D-07, D-09, D-10, and D-23.</action>
  <acceptance_criteria>
    - Manifest validator and producer audit pass on the migrated cohort and fail direct missing/duplicate fixtures.
    - Trigger audit finds no moved job scheduled by both Crosswake CI and a source workflow.
    - Crosswake CI umbrella remains checkout-free and statically needs exactly `proof_leaves union required_control_nodes`; compatibility contexts remain outside the union.
  </acceptance_criteria>
  <verify>
    <automated>python3 script/check_ci_leaf_manifest.py --self-test &amp;&amp; python3 script/list_merge_blocking_checks.py --emitters &gt;/dev/null &amp;&amp; mix test test/crosswake/proof/phase165_ci_integrity_test.exs --only triggers --only manifest</automated>
  </verify>
  <done>The first migration cohort has exact, machine-checked trigger and proof-graph authority.</done>
</task>

</tasks>

<threat_model>
Security posture: OWASP ASVS Level 1; every high or critical threat blocks execution until its listed mitigation passes.

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| classifier → executable leaves | False irrelevance could skip security or core proof. |
| migrated leaves → current branch protection | A missing legacy context could deadlock or bypass the staged migration. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-165-04 | Tampering | expanded umbrella | high | mitigate | Exact manifest/static-needs parity and compatibility conclusions over closed results. |
| T-165-08 | Elevation of privilege | legacy required contexts | high | mitigate | Preserve one producer per live context; no protection mutation in this plan. |
| T-165-11 | Denial of service | trigger migration | medium | mitigate | Structural one-PR-producer/no-push-duplicate audit and actionlint. |
</threat_model>

## Artifacts this phase produces

Deterministic exhaustive migration map:

| Source workflow/job(s) | Crosswake CI proof leaf ID(s) | Compatibility job ID → frozen display context | Source file disposition | Primary command surface |
|---|---|---|---|---|
| `aggregator-negative-control.yml / merge-blocking-aggregator-negative-control` | `proof-aggregator-negative-control` | `compat-aggregator-negative-control` → `merge-blocking-aggregator-negative-control` | delete after parity | `python3 script/check_aggregator_result_semantics.py --assert-outcomes` |
| `contract-drift-gate.yml / guard-01-contract-drift-test, guard-02-generate-and-diff` | same two guard IDs | `compat-contract-drift` → `merge-blocking-contract-drift` | delete after parity | focused contract drift ExUnit; `mix crosswake.contract.gen` plus staged diff |
| `dependency-security.yml / merge-blocking-dependency-security` | `proof-dependency-security` | `compat-dependency-security` → `merge-blocking-dependency-security` | delete after parity | `script/check_dependency_security.sh` and advisory-bearing negative fixture |
| `requires-example-host-gate.yml / merge-blocking-requires-example-host` | `proof-requires-example-host` | `compat-requires-example-host` → `merge-blocking-requires-example-host` | delete after parity | `script/check_example_host_isolation.sh --matrix-only` plus tagged-path inventory |
| `phase130-proof.yml / core-hermetic-proof, companion-engine-absent-proof` | `phase130-core-hermetic-proof`, `phase130-companion-engine-absent-proof` | `compat-phase130-core-hermetic-proof` → `core hermetic proof (merge-blocking)`; `compat-phase130-companion-engine-absent-proof` → `companion engine-absent proof (merge-blocking)` | retain schedule/manual advisory only | Phase 130 focused guards/full hermetic suite; companion tests and Rulestead package verification |
| `phase132-proof.yml / core-hermetic-proof, companion-engine-absent-proof` | `phase132-core-hermetic-proof`, `phase132-companion-engine-absent-proof` | `compat-phase132-core-hermetic-proof` → `phase132 core hermetic proof (merge-blocking)`; `compat-phase132-companion-engine-absent-proof` → `phase132 companion engine-absent proof (merge-blocking)` | retain schedule/manual advisory only | Phase 132 compatibility/full hermetic suite; companion tests and Rindle release-package verification |
| `phase23-proof.yml / merge-blocking-commerce-proof` | `phase23-commerce-proof` | `compat-phase23-commerce-proof` → `merge-blocking commerce support proof (hermetic)` | retain schedule/manual advisory only | Phase 23 commerce/support/doctor/guide/planning proofs |
| `phase34-proof.yml / merge-blocking-commerce-proof` | `phase34-commerce-proof` | `compat-phase34-commerce-proof` → `merge-blocking phase34 commerce support proof (hermetic)` | retain schedule/manual advisory only | full hermetic suite excluding example-host tests |

All proof rows add exact `leaf_id`, literal `display_name`, `family`, `remediation_command`, and `irrelevance_reason`; all compatibility rows add exact `job_id`, `display_context`, and `needs_target`. No new action input/output, API, schema, or product symbol is introduced.

Scope rationale: this 13-file cohort is retained as one atomic plan because trigger removal is safe only when `crosswake-ci.yml`, the manifest/validator/producer audit, every source producer, and its frozen compatibility replacement change and verify together. Each task stays at five owned files or fewer; splitting the shared authority transition across plans would create an observable duplicate-or-missing producer window. The estimate remains low-confidence with zero calibration samples, so task/file ownership—not the token projection—is the sizing authority.

<verification>
Run all task commands and `script/check_required_checks_registered.sh --local-only`; no live branch-protection write occurs.
</verification>

<success_criteria>
Core and hermetic PR proof runs once through Crosswake CI, scheduled/advisory trust remains separate, and every migrated leaf is statically governed.
</success_criteria>

<output>
Create `.planning/workstreams/quality-ratchet-release/phases/165-efficient-and-maintainable-ci/165-05-SUMMARY.md` when done.
</output>
