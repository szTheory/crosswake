---
phase: 164-dependency-security-and-gate-authority
plan: "05"
type: execute
wave: 4
depends_on: [164-02, 164-03, 164-04]
files_modified:
  - test/support/example_host.ex
  - test/crosswake/proof/phase164_example_host_isolation_test.exs
  - script/check_exunit_ownership.exs
  - test/crosswake/proof/phase164_exunit_ownership_test.exs
  - .planning/workstreams/quality-ratchet-release/phases/164-dependency-security-and-gate-authority/164-VALIDATION.md
autonomous: true
gap_closure: true
requirements: [CIG-02, CIG-03]
estimate: {tokens: 14000, raw_tokens: 14000, tasks: 2, confidence: low}
must_haves:
  truths:
    - "After an owned Repo stops, cleanup removes exactly its owned primary SQLite path and the derived <path>-wal and <path>-shm sidecars; unowned files and processes remain unchanged (CIG-03, D-09, D-10)."
    - "Behavioral proof creates and observes the primary database plus both SQLite sidecars before cleanup, then proves all three paths absent afterward (CIG-03, D-09, D-10)."
    - "The exact tagged/complete seed matrix for 17, 101, and 1009 and the Phase 164 aggregate pass with no crosswake-example-host-* residue (CIG-03, D-09, D-10)."
    - "Default/hermetic ownership is backed by an explicit current broad-lane manifest, and removing that lane from a synthetic tree makes the detector fail closed without changing workflow topology (CIG-02, D-08, D-13)."
  artifacts:
    - path: "test/support/example_host.ex"
      provides: "Owned SQLite primary/WAL/SHM cleanup after owned Repo shutdown."
    - path: "test/crosswake/proof/phase164_example_host_isolation_test.exs"
      provides: "Behavioral sidecar lifecycle and unowned-resource regression proof."
    - path: "script/check_exunit_ownership.exs"
      provides: "Explicit default/hermetic and requires-example-host execution-lane manifests."
    - path: "test/crosswake/proof/phase164_exunit_ownership_test.exs"
      provides: "Missing-default-lane negative control and live-tree ownership proof."
    - path: ".planning/workstreams/quality-ratchet-release/phases/164-dependency-security-and-gate-authority/164-VALIDATION.md"
      provides: "Pending-to-green gap-closure verification mapping for Plan 164-05."
  key_links:
    - from: "Crosswake.TestSupport.ExampleHost.start_saas_repo!/0"
      to: "owned SQLite cleanup token"
      via: "Repo ownership is registered after file ownership so reverse on_exit cleanup stops the Repo before deleting the exact primary/WAL/SHM paths"
      pattern: "start_owned!|own_file!|on_exit|cleanup!"
    - from: "test/crosswake/proof/phase164_example_host_isolation_test.exs"
      to: "test/support/example_host.ex"
      via: "real WAL-mode Repo activity observes primary, -wal, and -shm paths before explicit owned cleanup"
      pattern: "journal_mode|sqlite3-wal|sqlite3-shm|cleanup!"
    - from: "script/check_exunit_ownership.exs"
      to: ".github/workflows/phase130-proof.yml"
      via: "literal default/hermetic lane manifest validates the existing core-hermetic-proof job and broad suite selector"
      pattern: "core-hermetic-proof|mix test --exclude requires_example_host --exclude advisory_only"
---

<objective>
Close the Phase 164 verification gap by making owned SQLite cleanup include the real WAL/SHM resources and by making the current default/hermetic ownership assumption executable.

Purpose: Restore CIG-03's exact no-residue truth and close the verifier's non-blocking CIG-02 robustness warning without altering CI topology.
Output: Sidecar-aware owned cleanup, behavioral sidecar proof, explicit default/hermetic lane authority, negative controls, and current validation metadata.
</objective>

<artifacts_this_phase_produces>
- Sidecar-aware cleanup in `test/support/example_host.ex` for only the owned primary SQLite path plus its exact `-wal` and `-shm` companions after the owned Repo stops.
- Behavioral regression coverage in `test/crosswake/proof/phase164_example_host_isolation_test.exs` that creates and observes all three SQLite resources, proves their removal, and proves unrelated/pre-existing resources survive.
- An explicit default/hermetic lane manifest and missing-lane negative control in the existing ExUnit ownership detector/test files; no workflow file changes.
- Updated `164-VALIDATION.md` rows and a `164-05-SUMMARY.md` recording the exact focused, six-run, detector, and Phase 164 aggregate evidence.
</artifacts_this_phase_produces>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/workstreams/quality-ratchet-release/phases/164-dependency-security-and-gate-authority/164-VERIFICATION.md
@.planning/workstreams/quality-ratchet-release/phases/164-dependency-security-and-gate-authority/164-CONTEXT.md
@.planning/workstreams/quality-ratchet-release/phases/164-dependency-security-and-gate-authority/164-RESEARCH.md
@.planning/workstreams/quality-ratchet-release/phases/164-dependency-security-and-gate-authority/164-03-SUMMARY.md
@.planning/workstreams/quality-ratchet-release/phases/164-dependency-security-and-gate-authority/164-02-SUMMARY.md
@test/support/example_host.ex
@test/crosswake/proof/phase164_example_host_isolation_test.exs
@script/check_example_host_isolation.sh
@script/check_exunit_ownership.exs
@test/crosswake/proof/phase164_exunit_ownership_test.exs
@.github/workflows/phase130-proof.yml

<interfaces>
- `ExampleHost.Ownership` cleanup tokens are atomic and idempotent; explicit cleanup and ExUnit `on_exit` may both invoke the same token safely.
- `start_owned!/1` returns `{:owned, pid, token}` only for a process this setup started; `{:unowned, pid}` from `already_started` must never be stopped.
- The owned SQLite resource set is closed and path-derived: the unique primary path, primary path plus `-wal`, and primary path plus `-shm`; cleanup must not glob the temp directory.
- ExUnit `on_exit` runs registered callbacks in reverse order, so the later Repo token must stop the owned Repo before the earlier file token removes database resources.
- The current broad default/hermetic authority is `.github/workflows/phase130-proof.yml`, job `core-hermetic-proof`, literal name `core hermetic proof (merge-blocking)`, executing `mix test --exclude requires_example_host --exclude advisory_only`.
</interfaces>
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Trace a real WAL-mode Repo through owned primary and sidecar cleanup</name>
  <reversibility rating="reversible">D-09/D-10 extend one internal owned-file cleanup token and its focused proof; reverting is local to the helper and test.</reversibility>
  <read_first>
    - AGENTS.md
    - .planning/workstreams/quality-ratchet-release/phases/164-dependency-security-and-gate-authority/164-VERIFICATION.md
    - .planning/workstreams/quality-ratchet-release/phases/164-dependency-security-and-gate-authority/164-CONTEXT.md
    - .planning/workstreams/quality-ratchet-release/phases/164-dependency-security-and-gate-authority/164-RESEARCH.md
    - .planning/workstreams/quality-ratchet-release/phases/164-dependency-security-and-gate-authority/164-03-SUMMARY.md
    - test/support/example_host.ex
    - test/crosswake/proof/phase164_example_host_isolation_test.exs
    - script/check_example_host_isolation.sh
  </read_first>
  <files>test/support/example_host.ex, test/crosswake/proof/phase164_example_host_isolation_test.exs</files>
  <behavior>
    - "A real owned example-host Repo is placed into WAL mode, performs database activity, and exposes the primary SQLite path, <path>-wal, and <path>-shm while the Repo is alive."
    - "Explicitly stopping the owned Repo before claiming the file token removes all three owned paths; registered on_exit cleanup remains idempotent."
    - "A pre-existing/already-started Repo remains unowned, and exact unrelated files (including similarly prefixed files and sidecars for another path) survive cleanup byte-for-byte."
    - "Cleanup attempts all three exact owned paths and treats already-absent paths as successful without scanning or deleting other temp resources."
  </behavior>
  <action>Write the failing CIG-03 behavioral regression first. Exercise a real unique example-host SQLite database with WAL journaling and a write/read boundary that deterministically creates both sidecars; assert the primary path, the exact `-wal` companion, and the exact `-shm` companion are observable while the owned Repo is running. Extend the existing D-09/D-10 owned-file token so its cleanup removes only those three path-derived resources, with the Repo ownership callback registered later and therefore stopped first by reverse on_exit order. Make explicit proof cleanup stop the owned Repo before invoking the file token, then assert all three paths are absent and a second cleanup call is harmless. Add adversarial files for another primary path and similarly prefixed names and prove their bytes remain unchanged. Preserve the current `:already_started` unowned process behavior, exact Application restoration, code-path ownership, unique path generation, and public helper call shapes. Do not add glob cleanup, broad temp reconciliation, retries, suite serialization, workflow edits, product runtime changes, Android work, adopter work, or Phase-165 topology changes. The existing matrix script already detects all `crosswake-example-host-*` sidecars and needs no source edit unless the new test proves its exact primary/WAL/SHM diagnostic is insufficient.</action>
  <acceptance_criteria>
    - The regression test observes the owned primary SQLite file and both exact sidecars before cleanup, then observes all three absent after the owned Repo is stopped and cleanup completes.
    - Cleanup targets exactly the owned primary, WAL, and SHM paths; unrelated and pre-existing files/processes remain present and identity- or byte-equal.
    - Repeated cleanup is idempotent, and already-absent owned paths do not turn successful teardown into failure.
    - CIG-03, D-09, and D-10 remain satisfied without serialization, retries, exclusions, glob deletion, or broadened runtime/workflow scope.
  </acceptance_criteria>
  <verify><automated>mix test test/crosswake/proof/phase164_example_host_isolation_test.exs</automated></verify>
  <done>A real owned WAL-mode Repo tears down in the safe order and leaves none of its primary/WAL/SHM resources while every unowned resource survives.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Make the current default/hermetic execution lane explicit and close the gap evidence</name>
  <reversibility rating="reversible">D-08 adds a local manifest check and synthetic negative control around an existing lane without changing any workflow producer or topology.</reversibility>
  <read_first>
    - AGENTS.md
    - .planning/workstreams/quality-ratchet-release/phases/164-dependency-security-and-gate-authority/164-VERIFICATION.md
    - .planning/workstreams/quality-ratchet-release/phases/164-dependency-security-and-gate-authority/164-CONTEXT.md
    - .planning/workstreams/quality-ratchet-release/phases/164-dependency-security-and-gate-authority/164-02-SUMMARY.md
    - script/check_exunit_ownership.exs
    - test/crosswake/proof/phase164_exunit_ownership_test.exs
    - .github/workflows/phase130-proof.yml
    - .planning/workstreams/quality-ratchet-release/phases/164-dependency-security-and-gate-authority/164-VALIDATION.md
  </read_first>
  <files>script/check_exunit_ownership.exs, test/crosswake/proof/phase164_exunit_ownership_test.exs, .planning/workstreams/quality-ratchet-release/phases/164-dependency-security-and-gate-authority/164-VALIDATION.md</files>
  <behavior>
    - "An ordinary runnable test is owned only when the explicit default/hermetic manifest resolves an existing workflow, job id, literal merge-blocking name, and broad selector."
    - "A synthetic tree with an ordinary runnable test but no manifested default/hermetic lane fails with `missing-execution-class`, the manifest path/job, and an actionable restoration message."
    - "The existing requires-example-host lane manifest and missing-lane negative control continue to pass unchanged."
    - "The live detector, focused ownership tests, exact six seed/class matrix, and Phase 164 aggregate all pass before validation rows are marked green."
  </behavior>
  <action>Write the missing-default-lane negative control first. Add one explicit D-08 default/hermetic lane manifest to the existing detector for `.github/workflows/phase130-proof.yml`, job `core-hermetic-proof`, literal display name `core hermetic proof (merge-blocking)`, and the current broad hermetic selector. Validate this lane only when at least one analyzed test classifies as default/hermetic, exactly as the dedicated lane is conditional on an effective `requires_example_host` class. Keep the two class authorities separate so one live lane cannot confer ownership on the other. Update synthetic fixture construction to include both manifested lanes by default, then add a default/hermetic missing-lane case that fails with path, job/name, and remediation; preserve all AST/tag/comment/string/malformed/exclusion controls. Do not edit `.github/workflows/phase130-proof.yml` or any other workflow. After both focused task gates pass, run the unfiltered six-run isolation matrix and the full Phase 164 aggregate, confirm the temp-prefix snapshot contains no primary/WAL/SHM residue, and change only Plan 164-05 validation rows from pending to green with the exact commands/results. Keep CIG-02 subordinate to the blocking CIG-03 closure: if the manifest cannot be expressed in these two existing files, leave its validation row pending and record a Phase 165 follow-up instead of changing CI topology.</action>
  <acceptance_criteria>
    - Default/hermetic ownership requires the explicit existing broad-lane manifest; its missing-lane negative control fails independently from the dedicated example-host lane control.
    - Live ownership and all focused detector tests pass without any workflow, producer, runner, trigger, cache, concurrency, Android, adopter, release, or Phase-165 topology edit.
    - `script/check_example_host_isolation.sh` passes tagged and complete classes for seeds 17, 101, and 1009 and leaves no primary, WAL, or SHM residue.
    - `script/check_phase164_dependency_security_and_gate_authority.sh` passes every child and Plan 164-05 validation rows record the exact green evidence without rewriting earlier historical results.
  </acceptance_criteria>
  <verify><automated>elixir script/check_exunit_ownership.exs &amp;&amp; mix test test/crosswake/proof/phase164_exunit_ownership_test.exs</automated></verify>
  <done>CIG-02 lane ownership is non-vacuous and the repaired CIG-03 sidecar lifecycle passes the exact six-run matrix plus the complete Phase 164 aggregate with clean validation metadata.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| owned Repo -> shared temp filesystem | Cleanup may delete only the unique database resources created by the owned Repo after that Repo has stopped. |
| ExUnit lifecycle -> global process/config/path state | Existing-state ownership semantics must keep already-started processes and pre-existing state unowned. |
| parsed ExUnit classes -> workflow execution lanes | A test class is merge-blocking only when its explicit lane manifest is present and executable. |

## STRIDE Threat Register — ASVS L1, block on high

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-164-06 | Tampering / Denial of Service | ExampleHost SQLite cleanup | high | mitigate | Task 1 stops only the owned Repo, removes only the exact primary/WAL/SHM paths, and proves adversarial unowned files/processes survive. |
| T-164-11 | Repudiation | sidecar lifecycle evidence | medium | mitigate | Task 1 must observe both sidecars before cleanup and Task 2 retains the exact six-run matrix plus the aggregate and residue diagnostics. |
| T-164-03 | Spoofing / Tampering | ExUnit execution ownership | medium | mitigate | Task 2 binds default/hermetic classification to an explicit current workflow/job/name/selector manifest and exercises a missing-lane negative control. |
| T-164-SC | Tampering | package supply chain | low | accept | No dependency or action installation is planned; existing Elixir, ExUnit, Ecto/SQLite, and repository scripts are reused. |
</threat_model>

<source_coverage_audit>
| Source | ID | Contract | Coverage | Status |
|---|---|---|---|---|
| GOAL | Phase 164 | Required merge results fail closed through one authoritative path. | Tasks 1-2 restore the state-isolation child and retain the aggregate authority. | COVERED |
| REQ | CIG-03 | State-mutating tests pass alone/full with exact restoration and no residue. | Task 1 behavior plus Task 2 matrix/aggregate acceptance. | COVERED |
| REQ | CIG-02 | Every intended ExUnit file has a merge-blocking execution path. | Task 2 explicit default/hermetic manifest and missing-lane control. | COVERED |
| RESEARCH | exact resource ownership | Delete only unique owned database/files after owned process cleanup. | Task 1 exact primary/WAL/SHM ownership and adversarial preservation proof. | COVERED |
| CONTEXT | D-08 | Detector understands default/hermetic and dedicated classes. | Task 2 cites and implements D-08 without workflow edits. | COVERED |
| CONTEXT | D-09, D-10 | Exact restoration, per-test unique resources, bounded tagged/full evidence, no concealment. | Task 1 and Task 2 cite D-09/D-10 in behavior, action, and acceptance. | COVERED |
| CONTEXT | D-13 | No Phase-165 topology optimization in Phase 164. | Both tasks prohibit workflow/topology edits. | COVERED |
| VERIFICATION | blocking gap | Primary cleanup left `-wal` and `-shm`; matrix/aggregate red. | Task 1 repairs the root cause; Task 2 reruns exact retained authorities. | COVERED |
| VERIFICATION | CIG-02 warning | Default/hermetic ownership assumed a broad lane without validating one. | Task 2 closes it in existing detector/test files only. | COVERED |
| EXISTING | SEC-01..03, CIG-01, CIG-04 and D-01..07/D-11..13 | Previously completed Phase 164 contracts. | Plans 164-01, 164-02, and 164-04 remain authoritative; this gap plan does not reopen them. | COVERED |
</source_coverage_audit>

<verification>
Run in order:

1. `mix test test/crosswake/proof/phase164_example_host_isolation_test.exs`
2. `elixir script/check_exunit_ownership.exs && mix test test/crosswake/proof/phase164_exunit_ownership_test.exs`
3. `script/check_example_host_isolation.sh`
4. `script/check_phase164_dependency_security_and_gate_authority.sh`

The third command must pass the exact six combinations: tagged and complete classes at seeds 17, 101, and 1009. The fourth must complete every child successfully. A final `find "${TMPDIR:-/tmp}" -maxdepth 1 -name 'crosswake-example-host-*' -print` must emit no resource created by these runs; pre-existing unowned paths are compared to the before snapshot rather than deleted.
</verification>

<success_criteria>
- The owned SQLite primary, WAL, and SHM resources are observable before cleanup and absent after the owned Repo stops.
- Unowned processes, pre-existing state, unrelated files, and other database paths remain unchanged.
- Default/hermetic and requires-example-host classes each require an explicit existing merge-blocking lane manifest, with independent missing-lane controls.
- The exact six seed/class matrix and the complete Phase 164 aggregate pass with no new residue and no adjacent-scope changes.
</success_criteria>

<output>
Create `.planning/workstreams/quality-ratchet-release/phases/164-dependency-security-and-gate-authority/164-05-SUMMARY.md` when done, including focused test results, the six seed/class outcomes, the Phase 164 aggregate result, the before/after owned-temp snapshot, and the final CIG-03/D-09/D-10 disposition.
</output>
