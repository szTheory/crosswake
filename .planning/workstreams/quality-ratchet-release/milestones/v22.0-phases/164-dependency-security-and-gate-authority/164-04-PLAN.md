---
phase: 164-dependency-security-and-gate-authority
plan: "04"
type: execute
wave: 2
depends_on: [164-01]
files_modified:
  - test/crosswake/proof/phase134_native_gate_blocking_proof_test.exs
  - script/check_aggregator_result_semantics.py
  - .github/workflows/aggregator-negative-control.yml
autonomous: true
requirements: [CIG-04]
estimate: {tokens: 18000, raw_tokens: 18000, tasks: 2, confidence: low}
must_haves:
  truths:
    - "Each named required aggregator declares exactly its expected leaves; both missing and unexpected leaves fail before result values are evaluated (D-11, D-12; CIG-04)."
    - "Success passes, only an explicitly declared irrelevant skipped leaf is neutral, and failure/cancelled/disallowed skipped/timed_out/action_required/stale/unknown/empty/missing input fails closed (D-11, D-12; CIG-04)."
    - "The existing if: always() + toJSON(needs) + alls-green convention and sibling job identities remain intact; no runner/cache/trigger/consolidation or Android proof change enters Phase 164 (D-12, D-13; CIG-04)."
  artifacts:
    - path: "test/crosswake/proof/phase134_native_gate_blocking_proof_test.exs"
      provides: "Exact expected-versus-declared aggregator leaf parity and missing/unexpected fixtures."
    - path: ".github/workflows/aggregator-negative-control.yml"
      provides: "Executable closed result-vocabulary matrix with one explicit-neutral arm."
    - path: "script/check_aggregator_result_semantics.py"
      provides: "Credential-free closed-vocabulary evaluator, complete matrix self-test, and final workflow-outcome assertion."
  key_links:
    - from: "named aggregator needs lists"
      to: "phase134 exact parity proof"
      via: "scoped single-line needs set equality"
      pattern: "missing|unexpected|needs:"
    - from: "aggregator-negative-control synthetic jobs"
      to: "script/check_aggregator_result_semantics.py"
      via: "step-local action outcomes passed to the same closed policy that the local full-matrix self-test executes"
      pattern: "allowed-skips|continue-on-error|outcome|assert-outcomes"
  prohibitions:
    - "requirement_id: CIG-04 | category: authority | statement: MUST NOT authorize failed work through allowed-failures, treat undeclared skipped/unknown/empty/missing work as neutral, or validate only a subset of required leaves. | status: unresolved | verification: flagged-unverified"
    - "requirement_id: CIG-04 | category: scope | statement: MUST NOT replace the established aggregator convention, rename sibling gates, alter Android proof, or perform Phase-165 runner/cache/trigger/workflow consolidation. | status: unresolved | verification: flagged-unverified"
flagged_assumptions:
  - "[UNRESOLVED-SPEC-PROBE][CIG-04 unclassified] D-11 is the exhaustive result policy: GitHub needs supplies success/failure/cancelled/skipped, while timed_out/action_required/stale/unknown remain fail-closed synthetic or API-boundary conclusions; no additional neutral class is inferred."
---

<objective>
Prove required aggregators cannot lose a leaf or reinterpret any non-success state as green.

Purpose: Close both halves of rollup authority: exact input membership and an explicit result vocabulary with one narrow irrelevant-work neutral case.
Output: Exact structural parity proof and expanded executable aggregator negative-control matrix.
</objective>

<artifacts_this_phase_produces>
- Extended phase134 helper returning deterministic `missing` and `unexpected` leaf diagnostics from exact set equality.
- New direct missing-leaf and unexpected-leaf fixtures scoped to named aggregator blocks.
- New credential-free `script/check_aggregator_result_semantics.py` whose default self-test executes the complete closed result matrix and proves missing/inverted outcome records fail.
- Expanded aggregator step ids/outcome assertions for success, failure, cancelled, disallowed skipped, explicitly irrelevant skipped, timed_out, action_required, stale, unknown, and empty/missing synthetic input.
- No new action, result convention, workflow producer, Android artifact, or Phase-165 orchestration surface.
</artifacts_this_phase_produces>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/workstreams/quality-ratchet-release/phases/164-dependency-security-and-gate-authority/164-CONTEXT.md
@.planning/workstreams/quality-ratchet-release/phases/164-dependency-security-and-gate-authority/164-RESEARCH.md
@.planning/workstreams/quality-ratchet-release/phases/164-dependency-security-and-gate-authority/164-PATTERNS.md
@test/crosswake/proof/phase134_native_gate_blocking_proof_test.exs
@.github/workflows/aggregator-negative-control.yml
@.github/workflows/contract-drift-gate.yml
@.github/workflows/native-behavioral-proof-gate.yml
@.github/workflows/offline-sync-e2e-gate.yml

<interfaces>
- Repository aggregators retain `if: always()`, exact single-line `needs: [..]`, and `jobs: ${{ toJSON(needs) }}` through `re-actors/alls-green@release/v1`.
- `allowed-skips` may name only a leaf explicitly classified irrelevant; `allowed-failures` is outside the contract.
- A leaf omitted from `needs` cannot reach alls-green, so exact structural parity is a separate mandatory precondition.
</interfaces>
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Trace one removed leaf into a structural gate failure</name>
  <reversibility rating="reversible">D-12 strengthens a test helper from subset to exact equality without changing workflow runtime behavior.</reversibility>
  <read_first>
    - AGENTS.md
    - .planning/workstreams/quality-ratchet-release/phases/164-dependency-security-and-gate-authority/164-CONTEXT.md
    - .planning/workstreams/quality-ratchet-release/phases/164-dependency-security-and-gate-authority/164-RESEARCH.md
    - .planning/workstreams/quality-ratchet-release/phases/164-dependency-security-and-gate-authority/164-PATTERNS.md
    - test/crosswake/proof/phase134_native_gate_blocking_proof_test.exs
    - .github/workflows/contract-drift-gate.yml
    - .github/workflows/native-behavioral-proof-gate.yml
    - .github/workflows/offline-sync-e2e-gate.yml
  </read_first>
  <files>test/crosswake/proof/phase134_native_gate_blocking_proof_test.exs</files>
  <behavior>
    - "Removing one expected leaf from a named aggregator fails with that leaf under missing and the exact declared set."
    - "Adding one undeclared leaf fails with that leaf under unexpected; reordering the same set passes."
    - "Renamed/missing aggregator jobs, absent if: always(), absent alls-green, or absent toJSON(needs) remain non-vacuous failures."
    - "Checks stay scoped to the named aggregator block and its supported single-line needs syntax."
  </behavior>
  <action>Write direct D-12 missing-leaf and unexpected-leaf fixtures first. Replace the current subset helper with exact set equality over the named aggregator's single-line `needs` list, normalize only ordering, and emit both missing and unexpected lists with aggregator/workflow provenance. Preserve existing non-vacuity assertions for missing/renamed job, `if: always()`, alls-green, and `toJSON(needs)`. Do not silently broaden the parser to unsupported multiline YAML and do not edit any Android job, runner, vector, or proof claim while reading the shared native workflow.</action>
  <acceptance_criteria>
    - Exact equality accepts reordering but rejects either a missing or unexpected leaf with stable named diagnostics.
    - A directly removed required leaf fails even though all remaining synthetic results could be success.
    - Every existing sibling aggregator still satisfies exact parity and the established runtime convention.
    - The test remains scoped/non-vacuous and introduces no workflow or Android changes.
  </acceptance_criteria>
  <verify><automated>mix test test/crosswake/proof/phase134_native_gate_blocking_proof_test.exs</automated></verify>
  <done>A required leaf can no longer disappear before result aggregation without a deterministic structural failure.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Execute the complete closed aggregator result vocabulary locally and in CI</name>
  <reversibility rating="reversible">D-11 expands the existing synthetic negative-control workflow without changing production aggregator names or leaf topology.</reversibility>
  <read_first>
    - .planning/workstreams/quality-ratchet-release/phases/164-dependency-security-and-gate-authority/164-CONTEXT.md
    - .planning/workstreams/quality-ratchet-release/phases/164-dependency-security-and-gate-authority/164-PATTERNS.md
    - .github/workflows/aggregator-negative-control.yml
    - test/crosswake/proof/phase134_native_gate_blocking_proof_test.exs
    - .github/workflows/contract-drift-gate.yml
  </read_first>
  <files>script/check_aggregator_result_semantics.py, .github/workflows/aggregator-negative-control.yml</files>
  <behavior>
    - "The credential-free local harness executes success, failure, cancelled, disallowed skipped, explicit-irrelevant skipped, timed_out, action_required, stale, unknown, empty, and missing arms through one closed evaluator."
    - "Success and explicitly irrelevant skipped pass only when the exact irrelevant leaf is declared; every other arm returns fail."
    - "The harness's own negative controls prove that deleting an outcome or inverting an expected result makes the final assertion fail."
    - "Every intentional CI action failure is step-local via continue-on-error, and the workflow awaits one harness-backed final assertion over every captured outcome."
    - "No allowed-failures input exists anywhere in the negative-control or named required aggregators."
  </behavior>
  <action>Build a Python-standard-library, credential-free D-11 policy harness before changing the workflow. Give it one closed evaluator over a map of leaf results plus an explicit irrelevant-leaf set, and make its canonical self-test execute every required class: success, failure, cancelled, disallowed skipped, explicit-irrelevant skipped, timed_out, action_required, stale, unknown, empty, and missing. The canonical self-test must also remove one required outcome and invert one expectation, await both validations, and fail unless each mutation is detected with the exact class/remediation. Add a second mode that accepts the workflow's complete named step-outcome map and rejects absent, extra, empty, or inverted records. Then extend the existing D-11/D-12 workflow in place: add deterministic action arms for the same classes, use `allowed-skips` only on the explicitly irrelevant named leaf, contain intentional action failures on their individual steps, and invoke the harness in the final awaited step with every captured outcome. The local harness is the primary executable semantics gate; the workflow proves the retained action matches it. Treat GitHub-needs versus API-boundary vocabulary explicitly without inventing a second production rollup convention.</action>
  <acceptance_criteria>
    - The local canonical command executes every D-11 result class exactly once and proves both missing and inverted outcome records are rejected.
    - Only success and the exact explicit-irrelevant skipped arm succeed; every other arm fails in the local evaluator and the workflow action matrix.
    - Empty/missing input and unknown values are executable local and workflow negative controls, not prose inference.
    - Existing required aggregators, stable job names, runners, caches, triggers, Android proof, and workflow count remain unchanged.
  </acceptance_criteria>
  <verify><automated>python3 script/check_aggregator_result_semantics.py --self-test &amp;&amp; mix test test/crosswake/proof/phase134_native_gate_blocking_proof_test.exs</automated></verify>
  <done>The retained aggregator convention has executable closed semantics for exact membership, success, explicit irrelevance, and every non-success/missing class.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| workflow expected leaves -> declared needs | Missing work can vanish before runtime aggregation unless exact membership is proven. |
| GitHub/API result strings -> alls-green policy | Untrusted result values must map through one closed pass/neutral/fail vocabulary. |

## STRIDE Threat Register — ASVS L1, block on high

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|---|
| T-164-02 | Tampering / Spoofing | aggregator leaves and results | high | mitigate | Task 1 proves exact leaf parity; Task 2 executes every success/neutral/non-success/unknown/empty/missing arm through one credential-free policy and the retained action. |
| T-164-10 | Repudiation | expected synthetic failures | medium | mitigate | Task 2 makes missing/inverted records executable local negative controls and requires one awaited harness-backed CI assertion over every step outcome. |
| T-164-SC | Tampering | action supply chain | low | accept | No package/action is added; the repository's existing pinned alls-green release contract is exercised. |
</threat_model>

<source_coverage_audit>
| Source | ID | Contract | Coverage | Status |
|---|---|---|---|---|
| GOAL | Phase 164 | Required merge results fail closed through one path. | Tasks 1-2 cover exact aggregator membership and values. | COVERED |
| REQ | CIG-04 | Missing/cancelled/failed fail; explicit irrelevance neutral. | Tasks 1-2. | COVERED |
| RESEARCH | closed aggregation/exact parity | Direct missing leaf plus credential-free and action-backed complete synthetic result matrices. | Tasks 1-2. | COVERED |
| CONTEXT | D-11, D-12, D-13 | Closed vocabulary, existing convention, no topology optimization. | Cited in actions/criteria. | COVERED |
| EDGE | 1 row | CIG-04 unclassified. | One applicable row equals one flagged assumption. | FLAGGED |
| PROHIBITION | 2 bespoke constraints | No non-success neutralization and no adjacent-scope topology change. | Descriptor-less flagged-unverified entries retained. | FLAGGED |
</source_coverage_audit>

<verification>
Run the structural proof and `python3 script/check_aggregator_result_semantics.py --self-test` locally. The local command must execute and await every closed-vocabulary arm plus missing/inverted negative controls. The retained aggregator-negative-control producer must pass every action outcome to the same harness mode and await its final assertion; YAML presence alone is never behavioral proof.
</verification>

<success_criteria>
- Exact leaf parity rejects missing and unexpected inputs.
- Only success and explicitly irrelevant skipped are non-failing in both the credential-free complete matrix and retained action-backed matrix.
- The existing aggregator convention remains the single authority with no Android or Phase-165 topology changes.
</success_criteria>

<output>
Create `.planning/workstreams/quality-ratchet-release/phases/164-dependency-security-and-gate-authority/164-04-SUMMARY.md` when done.
</output>
