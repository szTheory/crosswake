---
phase: 166-clean-checkout-engineering-quality
plan: 02
type: execute
wave: 2
depends_on: [166-01]
files_modified:
  - script/verify_repository.mjs
  - script/repository_verification_stages.json
  - test/js/repository_verification.test.mjs
  - test/fixtures/repository_quality/stage-cases.json
autonomous: true
requirements: [ENG-01, ENG-03, ENG-04]
estimate:
  tokens: 24000
  raw_tokens: 24000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Independent proof families continue after another independent family fails, while dependency failures produce BLOCKED and every FAIL/BLOCKED makes the run nonzero per D-19 and D-20."
    - "Success, failure, interruption, and blocked runs always execute exact invocation-owned cleanup and final Git inspection per D-15, D-16, and D-18."
    - "The final plain-text summary is bounded, deterministic, color-independent, and gives one safe root-relative remediation per failed or blocked purpose ID per D-21 and D-22."
  artifacts:
    - path: "script/verify_repository.mjs"
      provides: "dependency-aware hybrid runner, finalizer, and result renderer"
    - path: "test/js/repository_verification.test.mjs"
      provides: "behavioral proof of execution, cleanup, and output semantics"
  key_links:
    - from: "script/verify_repository.mjs"
      to: "git status --porcelain=v1 -z --untracked-files=all"
      via: "byte-preserving before/final snapshots stored outside shell variables"
      pattern: "porcelain=v1"
    - from: "script/verify_repository.mjs"
      to: "script/repository_verification_stages.json"
      via: "dependencies and owned_outputs drive scheduling and cleanup"
      pattern: "owned_outputs"
---

<objective>
Expand the tracer into the complete dependency-aware runner and deterministic terminal contract.

Purpose: Let one run reveal independent defects while still failing closed and always restoring invocation-owned state.
Output: Full scheduler, finalizer, NUL-safe status logic, bounded logs, and negative execution tests.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/workstreams/quality-ratchet-release/phases/166-clean-checkout-engineering-quality/166-01-SUMMARY.md
@.planning/workstreams/quality-ratchet-release/phases/166-clean-checkout-engineering-quality/166-CONTEXT.md
@script/check_example_host_isolation.sh
@script/repository_verification_stages.json
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Execute dependency chains while continuing independent stages</name>
  <read_first>
    - script/verify_repository.mjs
    - script/repository_verification_stages.json
    - test/js/repository_verification.test.mjs
    - test/fixtures/repository_quality/stage-cases.json
    - examples/phoenix_host/mix.exs
    - examples/phoenix_host/package.json
    - packages/crosswake-shell-core-ios/Package.swift
    - packages/crosswake-shell-core-android/gradlew
  </read_first>
  <files>script/verify_repository.mjs, script/repository_verification_stages.json, test/js/repository_verification.test.mjs, test/fixtures/repository_quality/stage-cases.json</files>
  <behavior>
    - Prerequisite/repository preflight fails fast; a failed stage recursively blocks dependents but does not suppress an independent ready stage.
    - Every stage uses fixed argv, cwd, environment, and a bounded timeout; full child output remains in an invocation-owned log.
    - Any `FAIL` or `BLOCKED` yields a nonzero final status after finalization.
  </behavior>
  <action>Start with fixture tests for one failed prerequisite, two dependent stages, and two independent stages. Implement a deterministic topological scheduler that records only `PASS`, `FAIL`, or `BLOCKED`, preserves manifest order for ties, and never conflates blocked work with skipped/irrelevant work. Execute fixed argv directly with explicit cwd and a minimal additive env map. Store complete stdout/stderr per purpose ID beneath the validated run root; terminal output may stream child logs, but the final summary remains bounded. Apply D-19 through D-22 exactly and retain D-23 protected CI identities as owner metadata rather than renaming them.</action>
  <acceptance_criteria>
    - A failed prerequisite prevents every dependent child from starting and labels each `BLOCKED`.
    - Independent eligible stages run and report their own results after a sibling failure.
    - Timeout, spawn error, signal, ordinary nonzero, and malformed result all become closed failures.
    - Final status is nonzero for any FAIL/BLOCKED combination and zero only when every selected stage passes.
  </acceptance_criteria>
  <verify>
    <automated>node --test test/js/repository_verification.test.mjs --test-name-pattern='dependency|blocked|independent|timeout'</automated>
    <fails_when>non-zero exit, or the TAP summary reports zero matching tests</fails_when>
  </verify>
  <reversibility rating="reversible">Scheduler behavior is isolated behind fixtures and has no persistent external side effect.</reversibility>
  <done>The full scheduler exposes independent defects and blocked chains without weakening final failure semantics.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Make cleanup, Git snapshots, and the final summary unconditional</name>
  <read_first>
    - script/verify_repository.mjs
    - test/js/repository_verification.test.mjs
    - test/fixtures/repository_quality/stage-cases.json
    - script/check_example_host_isolation.sh
    - .gitignore
    - .planning/workstreams/quality-ratchet-release/phases/166-clean-checkout-engineering-quality/166-CONTEXT.md
  </read_first>
  <files>script/verify_repository.mjs, script/repository_verification_stages.json, test/js/repository_verification.test.mjs, test/fixtures/repository_quality/stage-cases.json</files>
  <behavior>
    - Complete mode rejects a dirty initial baseline; focused mode may start dirty but final comparison reports only newly introduced non-ignored state.
    - Porcelain bytes remain NUL-delimited in files and are never stored in shell variables or printed as file contents.
    - Cleanup deletes only ledgered outputs created by this invocation beneath exact validated prefixes, including on child failure or interruption.
    - The summary has stable purpose IDs, literal result words, no color dependency, bounded detail, and exactly one remediation for each non-pass result.
  </behavior>
  <action>Start RED with temporary Git repositories containing staged, unstaged, untracked, ignored, unusual-byte, whitespace, newline, leading-dash, and symlink/prefix-escape cases. Implement pre/final `git status --porcelain=v1 -z --untracked-files=all` snapshots as files beneath the run root, never shell variables. Require an empty complete-run baseline per D-15; focused mode records its initial state but refuses to call it clean. Track only exact paths the invocation created, validate their parents and `crosswake-repository-verify.*` ownership before removal, refuse symlink/prefix escape, and preserve pre-existing caches byte-for-byte per D-16 and D-18. Finalize snapshot and cleanup in `finally`/signal handling while preserving the originating exit status. Render per D-17 and D-21 through D-24; plain output is authoritative and `NO_COLOR` is honored if color is ever added.</action>
  <acceptance_criteria>
    - Dirty complete mode exits before proof work and does not label the baseline clean.
    - Cleanup-on-success, cleanup-on-failure, interruption, invalid prefix, symlink escape, and pre-existing-cache preservation tests pass.
    - Index bytes and staged state are unchanged; no command invokes `git add`, `git clean`, or `.git/info/exclude` as policy.
    - Summary ordering and remediation count are deterministic, output remains bounded, and injected sensitive contents never appear.
  </acceptance_criteria>
  <verify>
    <automated>node --test test/js/repository_verification.test.mjs --test-name-pattern='git|cleanup|summary|remediation|secret'</automated>
    <fails_when>non-zero exit, zero matching tests, changed fixture index bytes, or a secret sentinel appears in captured output</fails_when>
  </verify>
  <reversibility rating="reversible">Cleanup is restricted to synthetic/invocation-owned paths and the implementation is removable.</reversibility>
  <done>Every exit path finalizes safely and yields a bounded, accessible, actionable result without residue.</done>
</task>

</tasks>

<threat_model>
Security posture: OWASP ASVS Level 1; every high or critical threat blocks execution until its listed mitigation passes.

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| manifest → scheduler | Dependency data controls which child processes start. |
| child processes → filesystem | Proof commands may create outputs, logs, and caches. |
| Git bytes → diagnostics | Hostile paths must be classified without evaluation or content disclosure. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-166-05 | Tampering / Elevation of privilege | filenames and fixed argv | high | mitigate | Direct argv spawn, NUL-safe parsing, repository-contained cwd, hostile filename fixtures. |
| T-166-06 | Tampering / Denial of service | cleanup finalizer | high | mitigate | Exact creation ledger, validated prefix/parent, symlink escape refusal, no glob/global cleanup. |
| T-166-07 | Spoofing / Repudiation | clean baseline | high | mitigate | Empty-baseline requirement plus byte-preserving staged/unstaged/untracked snapshots. |
| T-166-08 | Information disclosure | logs and final summary | high | mitigate | Private invocation logs, allowlisted bounded summary, category/path/remediation only. |
</threat_model>

## Artifacts this phase produces

- `script/verify_repository.mjs`: closed result enum `PASS|FAIL|BLOCKED`, dependency-aware scheduler, invocation creation ledger, NUL-safe snapshot/finalizer, timeout and signal handling, and bounded summary renderer.
- `script/repository_verification_stages.json`: finalized dependency edges, timeouts, and exact owned-output declarations for all nine stages.
- `test/js/repository_verification.test.mjs`: execution, hostile-path, dirty-baseline, cleanup, summary, and disclosure regressions.
- `test/fixtures/repository_quality/stage-cases.json`: synthetic dependency, process, Git, and cleanup cases.

<verification>
Run the focused Node patterns and the full `script/verify_repository.sh --self-test`; no exact host toolchain is required for synthetic negative controls.
</verification>

<success_criteria>
The runner keeps independent proof useful, blocks invalid chains, always finalizes, and never mistakes dirty or unsafe repository state for clean success.
</success_criteria>

<output>
Create `.planning/workstreams/quality-ratchet-release/phases/166-clean-checkout-engineering-quality/166-02-SUMMARY.md` when done.
</output>
