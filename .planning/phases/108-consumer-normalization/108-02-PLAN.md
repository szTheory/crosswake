---
phase: 108-consumer-normalization
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - priv/static/crosswake/offline.css
  - priv/templates/crosswake/offline_ui/offline_root.html.heex.eex
  - priv/templates/crosswake/offline_ui/offline_page.html.heex.eex
  - lib/mix/tasks/crosswake.gen.offline_ui.ex
autonomous: true
requirements: [NORM-02]
must_haves:
  truths:
    - "The generator emits a vendored offline.css component stylesheet copied into the host's priv/static/assets/offline.css via the existing ensure_file no-clobber guard, mirroring the Phase 107 tokens.css vendor-by-copy mechanism (D-01, D-03)"
    - "offline.css references semantic --cw-* tokens only and NEVER redefines any --cw-* value; dark mode flows automatically (D-03, D-04)"
    - "offline.css carries :root { color-scheme: light dark; } and a :focus-visible outline rule (outline, not box-shadow), plus a non-color status cue (D-07)"
    - "offline_root.html.heex.eex links offline.css after tokens.css and app.css, and the <body> Tailwind classes are retired (D-02)"
    - "offline_page.html.heex.eex contains zero Tailwind utility/color classes — all styling is .cw-offline-* semantic classes — and adds a11y markup: role=status, <ul role=list>/<li>, <h2> Status heading (D-02, D-08)"
    - "The stale tailwind.config.js (legacy blue #699cc9 / amber #e1b982) and esbuild-CSS-bundling Next-steps block is retired and replaced with honest phx.gen.auth-style guidance (D-11)"
    - "No --cw-primitive-* references and no var() fallbacks in any generator output; microcopy preserved (D-04, D-09, D-10)"
  artifacts:
    - path: "priv/static/crosswake/offline.css"
      provides: "Vendored .cw-offline-* component stylesheet consuming semantic tokens"
      contains: "var(--cw-surface-default)"
    - path: "priv/templates/crosswake/offline_ui/offline_page.html.heex.eex"
      provides: "Token-backed offline page markup with a11y semantics"
      contains: "role=\"status\""
    - path: "lib/mix/tasks/crosswake.gen.offline_ui.ex"
      provides: "Generator that vendors offline.css (no-clobber) and prints honest guidance"
      contains: "offline.css"
  key_links:
    - from: "lib/mix/tasks/crosswake.gen.offline_ui.ex"
      to: "priv/static/crosswake/offline.css"
      via: "get_offline_css_path/0 + ensure_file → priv/static/assets/offline.css"
      pattern: "offline\\.css"
    - from: "priv/templates/crosswake/offline_ui/offline_root.html.heex.eex"
      to: "offline.css"
      via: "<link> placed after tokens.css and app.css"
      pattern: "offline\\.css"
    - from: "priv/static/crosswake/offline.css"
      to: "tokens.css semantic tier"
      via: "consumes --cw-* custom properties, never redefines them"
      pattern: "var\\(--cw-"
---

<objective>
Convert the offline_ui generator off Tailwind onto the semantic token tier (NORM-02). The actual color source today is a STALE hardcoded Tailwind theme emitted by `crosswake.gen.offline_ui.ex` (legacy blue `#699cc9` / amber `#e1b982` — not even canonical) backing the templates' utility classes. Retiring the templates' Tailwind classes without retiring this emitted theme would leave dead drift behind, so both go together.

Deliverables: (1) a NEW vendored `priv/static/crosswake/offline.css` component stylesheet of `.cw-offline-*` rules referencing semantic tokens (the new color+layout source, D-01); (2) the generator copies it into the host's `priv/static/assets/offline.css` via the existing no-clobber `ensure_file`, mirroring Phase 107's tokens.css mechanism; (3) `offline_root.html.heex.eex` links offline.css after tokens.css/app.css and drops the `<body>` Tailwind classes; (4) `offline_page.html.heex.eex` replaces ALL Tailwind classes with `.cw-offline-*` semantic classes plus a11y markup; (5) the stale `tailwind.config.js`/esbuild Next-steps block is retired for honest phx.gen.auth-style guidance.

Purpose: A generated offline page that renders correctly in light AND dark with zero Tailwind dependency in the host, consuming the one distributed token source.
Output: New offline.css, two rewritten templates, an updated generator. Wiring only — no new tokens.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/108-consumer-normalization/108-CONTEXT.md
@.planning/phases/108-consumer-normalization/108-PATTERNS.md
@.planning/phases/107-token-source-distribution/107-CONTEXT.md

<interfaces>
Semantic token names available (COMPLETE public contract — read priv/static/crosswake/tokens.css; primitive refs forbidden):
Surfaces: --cw-surface-default/-inset/-raised/-inverse
Text: --cw-text-default, --cw-text-muted (secondary text; NOT --cw-text-subtle), --cw-text-inverse, --cw-text-code
Borders: --cw-border-default/-strong/-subtle
Actions: --cw-action-bg/-hover/-fg/-focus-ring
Status: --cw-status-success/-warning/-error/-info
Runtime: --cw-runtime-offline, --cw-runtime-native
Fonts: --cw-font-display/-body/-mono
Sizes: --cw-text-scale-xs(12)/-sm(14)/-md(16)/-lg(18)/-xl(20)
Radii: --cw-radius-sm(6)/-md(10)/-lg(14)/-xl(20); --cw-spacing-base(4); --cw-focus-ring-width(2); --cw-tracking-tight

Generator reuse patterns (lib/mix/tasks/crosswake.gen.offline_ui.ex):
- ensure_file/2 (lines ~129-145): no-clobber writer returning :reused / :created — reuse verbatim for offline.css
- get_tokens_css_path/0 (lines ~113-119): Application.app_dir(:crosswake, "priv/static/crosswake/tokens.css") with File.cwd! fallback — mirror as get_offline_css_path/0
- tokens_css_dest (line ~50): Path.join([dir, "priv", "static", "assets", "tokens.css"]) — mirror as offline_css_dest with "offline.css"
- The stale Mix.shell().info heredoc spans lines ~58-103; the Tailwind/esbuild block is ~65-101

Dark-mode FLIPPING tokens (D-06): --cw-status-success → wake-500 (white text fails AA), --cw-status-warning, --cw-runtime-offline flip; --cw-runtime-native and --cw-status-error do NOT flip.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author the vendored priv/static/crosswake/offline.css component stylesheet</name>
  <files>priv/static/crosswake/offline.css</files>
  <read_first>
    - priv/static/crosswake/tokens.css (SOURCE OF TRUTH — the only --cw-* names allowed; read the semantic tier + forbidden-pairing comments; do NOT modify)
    - .planning/phases/108-consumer-normalization/108-PATTERNS.md § "1. priv/static/crosswake/offline.css (NEW)" (header convention, available names, required D-07 rules, class naming) and § "3. ...offline_page" (the target .cw-offline-* structure)
    - .planning/phases/108-consumer-normalization/108-CONTEXT.md (D-01, D-03, D-04, D-05, D-06, D-07, D-10)
  </read_first>
  <action>
    Create a static vendored CSS file. Start with the header convention (parallel to tokens.css but host-owned-editable):
    `/* GENERATED by mix crosswake.gen.offline_ui — host-owned, editable */`
    `/* To pick up upstream changes, delete this file and re-run the generator */`

    Author `.cw-offline-*` rules covering BOTH color and layout (D-01) for the offline_page structure: page wrapper, card, heading, status card, status label/value, session list, session item, session title/subtitle. Suggested names (Claude's discretion per CONTEXT.md): `.cw-offline-page`, `.cw-offline-card`, `.cw-offline-heading`, `.cw-offline-status`, `.cw-offline-status-label`, `.cw-offline-status-value`, `.cw-offline-session-list`, `.cw-offline-session-item`, `.cw-offline-session-title`, `.cw-offline-session-subtitle`.

    Reference SEMANTIC tokens ONLY (D-04 — zero `--cw-primitive-*`, zero hex literals, zero `var()` fallbacks per D-10). Apply the canonical mapping: card bg → var(--cw-surface-inset); card border → 1px solid var(--cw-border-default); card radius → var(--cw-radius-lg); page bg → var(--cw-surface-default); body/heading text → var(--cw-text-default); session-subtitle/secondary text → var(--cw-text-muted) (D-05, NOT text-subtle); fonts → var(--cw-font-display) for the heading, var(--cw-font-body) for body; sizes → var(--cw-text-scale-*); spacing → multiples of var(--cw-spacing-base).

    NEVER redefine any --cw-* value in this file (D-03) — it consumes tokens, it does not declare them. Dark mode then flows automatically.

    Include the D-07 required rules in this file:
    `:root { color-scheme: light dark; }`
    `:focus-visible { outline: var(--cw-focus-ring-width) solid var(--cw-action-focus-ring); outline-offset: 2px; }` (outline, NOT box-shadow-only).
    Status-not-by-color-alone: give `.cw-offline-status` a non-color shape cue (a thick `border-left` using var(--cw-border-strong) or a status token as `color:`/border only), in addition to its text label — never status-color-as-fill-behind-light-text (D-06/D-07). Sync-status-style indicators, if any, use status tokens as `color:` only on a neutral surface, never as backgrounds.
  </action>
  <verify>
    <automated>cd /Users/jon/projects/crosswake && f=priv/static/crosswake/offline.css && test -f "$f" && grep -q 'cw-offline-' "$f" && grep -q 'var(--cw-surface-default)' "$f" && grep -q 'var(--cw-text-default)' "$f" && grep -q 'var(--cw-text-muted)' "$f" && grep -q 'color-scheme: light dark' "$f" && grep -q ':focus-visible' "$f" && grep -q 'outline:' "$f" && test "$(grep -c -- '--cw-primitive-' "$f")" = "0" && test "$(grep -cE '#[0-9A-Fa-f]{3,6}' "$f")" = "0" && ! grep -qE 'var\(--cw-[a-z-]+,' "$f" && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - `priv/static/crosswake/offline.css` exists with the host-owned GENERATED header
    - Contains `.cw-offline-` class rules; contains `var(--cw-surface-default)`, `var(--cw-text-default)`, `var(--cw-text-muted)`, `var(--cw-radius-lg)`
    - Contains `:root { color-scheme: light dark; }` and a `:focus-visible { outline: ... }` rule (literal `outline:`, not box-shadow)
    - `grep -c -- '--cw-primitive-' offline.css` == 0; `grep -cE '#[0-9A-Fa-f]{3,6}' offline.css` == 0; no `var(--cw-x, fallback)` patterns
    - File contains NO `--cw-...:` DECLARATION line of any --cw-* token (it consumes, never redefines)
    - `.cw-offline-status` carries a non-color cue (border-left or border treatment), not a status-color fill behind light text
    - Does not use `--cw-text-subtle`
  </acceptance_criteria>
  <done>A vendored semantic-only component stylesheet exists, carries the D-07 a11y rules, and never redefines tokens.</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite offline_root + offline_page templates onto .cw-offline-* classes with a11y markup</name>
  <files>priv/templates/crosswake/offline_ui/offline_root.html.heex.eex, priv/templates/crosswake/offline_ui/offline_page.html.heex.eex</files>
  <read_first>
    - priv/templates/crosswake/offline_ui/offline_root.html.heex.eex (current — links tokens.css then app.css; `<body class="bg-cw-foam-50 text-cw-current-950 antialiased">`)
    - priv/templates/crosswake/offline_ui/offline_page.html.heex.eex (current — 22 lines of Tailwind utility classes)
    - priv/static/crosswake/offline.css (the file authored in Task 1 — the class names you wire to must match it)
    - .planning/phases/108-consumer-normalization/108-PATTERNS.md § "2. offline_root" (link-order change) and § "3. offline_page" (full Tailwind retirement list, a11y additions, target structure)
    - .planning/phases/108-consumer-normalization/108-CONTEXT.md (D-02, D-08, D-09)
  </read_first>
  <action>
    offline_root.html.heex.eex:
    Add a third stylesheet `<link phx-track-static rel="stylesheet" href={~p"/assets/offline.css"} />` AFTER the existing tokens.css and app.css links, so the order is tokens.css → app.css → offline.css (D-02).
    Replace `<body class="bg-cw-foam-50 text-cw-current-950 antialiased">` with `<body>` (or a single `.cw-offline-body` class if offline.css defines body styling) — retire ALL Tailwind body classes; body styling lives in offline.css.

    offline_page.html.heex.eex:
    Replace EVERY Tailwind utility/color class (min-h-screen, flex, flex-col, items-center, justify-center, p-4, max-w-md, w-full, bg-white, rounded-lg, shadow-md, p-6, border-t-4, border-cw-wake-700, text-2xl, font-bold, mb-*, text-cw-current-950, bg-cw-foam-50, rounded, border, border-cw-brass-500, text-sm, font-medium, text-cw-brass-500, text-lg, space-y-4, p-3, border-gray-200, font-semibold, text-gray-500) with the `.cw-offline-*` semantic classes that match Task 1's offline.css. No Tailwind classes and no `text-cw-*`/`bg-cw-*`/`border-cw-*` color references remain.

    Add the D-08 a11y markup during the rewrite (no redesign, structure otherwise unchanged):
    - Status card gets `role="status"` and must exist in the initial DOM.
    - The two-session block becomes `<ul role="list">` with `<li>` items (was `<div class="space-y-4">` + `<div>`s).
    - The "Status" label becomes an `<h2>` heading (was a `<p>`).
    Preserve the exact microcopy (D-09): "Offline Workspace", "Status", "Available offline", "Study Session A", "Pending server confirmation", "Study Session B", "Draft only". An optional HTML comment may flag the placeholder copy as proto-copy; do not change wording.
  </action>
  <verify>
    <automated>cd /Users/jon/projects/crosswake && r=priv/templates/crosswake/offline_ui/offline_root.html.heex.eex && p=priv/templates/crosswake/offline_ui/offline_page.html.heex.eex && grep -q 'offline.css' "$r" && grep -q 'cw-offline-' "$p" && grep -q 'role="status"' "$p" && grep -q 'role="list"' "$p" && grep -q '<h2' "$p" && test "$(cat "$r" "$p" | grep -coE 'min-h-screen|flex|bg-white|bg-cw-foam-50|text-cw-current-950|border-cw-|border-gray-|space-y-4|max-w-md')" = "0" && t=$(grep -bo 'tokens.css' "$r" | head -1 | cut -d: -f1) && a=$(grep -bo 'app.css' "$r" | head -1 | cut -d: -f1) && o=$(grep -bo 'offline.css' "$r" | head -1 | cut -d: -f1) && [ "$t" -lt "$a" ] && [ "$a" -lt "$o" ] && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - offline_root links offline.css AFTER tokens.css and app.css (byte-position order tokens < app < offline)
    - offline_root `<body>` has no Tailwind classes (no `bg-cw-`, `text-cw-`, `antialiased`)
    - offline_page contains `cw-offline-` classes and contains `role="status"`, `role="list"`, and an `<h2>` Status heading
    - `cat root page | grep -coE 'min-h-screen|flex|bg-white|bg-cw-foam-50|text-cw-current-950|border-cw-|border-gray-|space-y-4|max-w-md'` == 0
    - Microcopy strings "Available offline", "Pending server confirmation", "Draft only" preserved verbatim
  </acceptance_criteria>
  <done>Both templates are Tailwind-free, wired to .cw-offline-* classes, correctly link-ordered, and a11y-upgraded with preserved copy.</done>
</task>

<task type="auto">
  <name>Task 3: Vendor offline.css in the generator (no-clobber) and replace the stale Next-steps block</name>
  <files>lib/mix/tasks/crosswake.gen.offline_ui.ex</files>
  <read_first>
    - lib/mix/tasks/crosswake.gen.offline_ui.ex (the file being modified — ensure_file ~129-145, get_tokens_css_path ~113-119, tokens_css_dest ~50, the Mix.shell().info heredoc ~58-103)
    - priv/static/crosswake/offline.css (the source file the generator must copy)
    - .planning/phases/108-consumer-normalization/108-PATTERNS.md § "4. lib/mix/tasks/crosswake.gen.offline_ui.ex" (exact patterns to mirror + the D-11 replacement text)
    - .planning/phases/108-consumer-normalization/108-CONTEXT.md (D-11)
  </read_first>
  <action>
    Add an offline.css path resolver mirroring get_tokens_css_path/0: a private get_offline_css_path/0 using Application.app_dir(:crosswake, "priv/static/crosswake/offline.css") with the File.cwd! dev fallback (same shape, swap "tokens.css" → "offline.css").

    Add the destination alongside tokens_css_dest: `offline_css_dest = Path.join([dir, "priv", "static", "assets", "offline.css"])`.

    Add the copy via the EXISTING no-clobber writer right after the tokens.css copy: `ensure_file(offline_css_dest, File.read!(get_offline_css_path()))`. Reuse ensure_file/2 verbatim — do not change its behavior (its no-clobber is already tested).

    Replace the stale Mix.shell().info heredoc (retire step 2 the `tailwind.config.js` legacy blue #699cc9 / amber #e1b982 theme, and step 3 esbuild CSS bundling) with honest, concise phx.gen.auth-style guidance per D-11 and PATTERNS.md §4 replacement text: keep the router-mount step (`get "/offline", #{...}Web.OfflineController, :index`); state that tokens.css AND offline.css were copied into priv/static/assets/, are host-owned and editable, and re-running will NOT update them (no-clobber) — to pick up upstream changes, delete and re-run; keep legitimate offline.js esbuild guidance for hosts that bundle JS but explicitly note NO CSS build step is required (offline.css is served static). No Tailwind references anywhere. Keep the "Offline UI components generated successfully!" and "generated successfully" neutral strings (the test keeps those).
  </action>
  <verify>
    <automated>cd /Users/jon/projects/crosswake && f=lib/mix/tasks/crosswake.gen.offline_ui.ex && grep -q 'get_offline_css_path' "$f" && grep -q 'offline_css_dest' "$f" && grep -q 'priv/static/crosswake/offline.css' "$f" && test "$(grep -coE 'tailwind.config.js|699cc9|e1b982|Configure esbuild to bundle offline.js' "$f")" = "0" && grep -q 'Offline UI components generated successfully' "$f" && mix compile --warnings-as-errors 2>&1 | grep -vq 'error:' && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - File contains `get_offline_css_path`, `offline_css_dest`, and `ensure_file(offline_css_dest, ...)`
    - File resolves `priv/static/crosswake/offline.css`
    - `grep -coE 'tailwind.config.js|699cc9|e1b982|Configure esbuild to bundle offline.js' <file>` == 0 (stale block retired)
    - File still contains `Offline UI components generated successfully!` and `get "/offline"` guidance
    - `mix compile` succeeds with no new errors
  </acceptance_criteria>
  <done>The generator vendors offline.css with no-clobber and prints honest, Tailwind-free, build-free guidance.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| generator → host filesystem | The generator writes files into a host project dir; templates are EEx-evaluated with `web_module`/`app_module` substitution only. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-108-03 | Tampering | EEx template interpolation in offline_root/offline_page | accept | Only `web_module`/`app_module` (derived from the `--app` CLI arg, a module name) are interpolated; offline.css is a STATIC vendored file with no interpolation. No user-controlled content reaches CSS/markup output. Existing generator surface, unchanged trust model. |
| T-108-04 | Tampering | offline.css clobbering host customizations | mitigate | Copy uses the existing tested no-clobber `ensure_file/2` (returns :reused without overwrite) — host edits are preserved. |
| T-108-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs in this plan; nothing to verify. |
</threat_model>

<verification>
- `priv/static/crosswake/offline.css` exists, semantic-only, never redefines --cw-*, carries D-07 rules
- offline_root links tokens.css → app.css → offline.css in that order; body Tailwind-free
- offline_page Tailwind-free with role=status / role=list / `<h2>` and preserved microcopy
- generator vendors offline.css no-clobber; stale Tailwind/esbuild block gone; `mix compile` clean
- (Generator-output assertions and the full test contract are Plan 03; visual light/dark confirmation is Plan 04)
</verification>

<success_criteria>
NORM-02 satisfied: the offline_ui generator produces token-backed markup (semantic .cw-offline-* classes referencing the semantic tier) with no Tailwind utility classes and no Tailwind dependency required in the host, the stale hardcoded Tailwind color theme is retired, and a vendored offline.css consuming the distributed tokens is copied (no-clobber) and linked in correct order.
</success_criteria>

<output>
Create `.planning/phases/108-consumer-normalization/108-02-SUMMARY.md` when done. Record the final `.cw-offline-*` class names and whether btn/status treatments are solid-fill (with --cw-text-inverse) or outlined (note that the final call is confirmed by 108-04 render-verify).
</output>
