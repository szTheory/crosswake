---
phase: 108-consumer-normalization
plan: 03
type: execute
wave: 2
depends_on: ["108-02"]
files_modified:
  - test/mix/tasks/crosswake.gen.offline_ui_test.exs
autonomous: true
requirements: [NORM-04]
must_haves:
  truths:
    - "The generator test asserts the NEW token-backed contract: generated output contains semantic token references and contains NO retired Tailwind class names (D-11, NORM-04)"
    - "Retired assertions are removed: cw-wake-700, cw-brass-500, tailwind.config.js, 'Configure esbuild to bundle offline.js' (NORM-04)"
    - "Neutral assertions are kept: 'Offline UI components generated successfully!', 'get \"/offline\"', the controller module name (NORM-04)"
    - "A no-clobber test for offline.css mirrors the existing tokens.css no-clobber test (D-01)"
    - "A link-order assertion verifies offline_root links offline.css after tokens.css and app.css (D-02)"
    - "mix test on the file exits 0 against the Plan 02 generator output"
  artifacts:
    - path: "test/mix/tasks/crosswake.gen.offline_ui_test.exs"
      provides: "Test pinning the semantic-token contract and absence of Tailwind"
      contains: "var(--cw-surface-default)"
  key_links:
    - from: "test/mix/tasks/crosswake.gen.offline_ui_test.exs"
      to: "lib/mix/tasks/crosswake.gen.offline_ui.ex generated output"
      via: "run([--dir, --app]) then read generated files"
      pattern: "run\\(\\[\"--dir\""
---

<objective>
Rewrite the generator test to assert the new token-backed contract instead of the retired Tailwind class names (NORM-04). The test must fail loudly if the generator ever regresses to Tailwind classes, primitive tokens, or the stale color theme.

This plan depends on Plan 02 (the generator now emits semantic .cw-offline-* markup and vendors offline.css with honest guidance). The assertions here pin THAT contract: semantic token references present, Tailwind/primitive absent, offline.css vendored with no-clobber, link order correct.

Purpose: A green test suite that guarantees the normalized generator contract and guards against silent re-drift.
Output: An updated `crosswake.gen.offline_ui_test.exs` that exits 0.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/108-consumer-normalization/108-CONTEXT.md
@.planning/phases/108-consumer-normalization/108-PATTERNS.md
@.planning/phases/108-consumer-normalization/108-02-SUMMARY.md

<interfaces>
Generated-output destination paths (from the generator, used by the test):
- controller:   <dir>/lib/test_app_web/controllers/offline_controller.ex
- offline_root: <dir>/lib/test_app_web/components/layouts/offline_root.html.heex
- offline_page: <dir>/lib/test_app_web/controllers/offline_html/offline_page.html.heex
- offline.css vendored: <dir>/priv/static/assets/offline.css
- tokens.css vendored:  <dir>/priv/static/assets/tokens.css
Test harness: import Mix.Tasks.Crosswake.Gen.OfflineUi, only: [run: 1]; import ExUnit.CaptureIO; @tmp_dir "tmp_offline_ui_test"; run(["--dir", @tmp_dir, "--app", "TestApp"]).
NOTE: actual class names live in offline.css; the templates carry .cw-offline-* class attributes. Assert on "cw-offline-" in the templates; assert on "var(--cw-...)" in offline.css.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite the generator test to the semantic-token contract</name>
  <files>test/mix/tasks/crosswake.gen.offline_ui_test.exs</files>
  <read_first>
    - test/mix/tasks/crosswake.gen.offline_ui_test.exs (current — keep setup, the generate test, and the existing tokens.css no-clobber test as the mirror pattern; the "outputs standard instructions" test needs the four retired assertions removed)
    - lib/mix/tasks/crosswake.gen.offline_ui.ex (the generator under test — confirm destination paths and Mix.shell output strings after Plan 02)
    - priv/static/crosswake/offline.css (confirm it contains var(--cw-surface-default), var(--cw-text-default), --cw-action-bg so the assertions match reality)
    - .planning/phases/108-consumer-normalization/108-PATTERNS.md § "9. ...offline_ui_test.exs" (exact assertions to remove/keep/add, the no-clobber mirror, the link-order test)
    - .planning/phases/108-consumer-normalization/108-CONTEXT.md (NORM-04 test rewrite contract) and ROADMAP success criterion #4
  </read_first>
  <action>
    In the existing "outputs standard instructions" test, REMOVE the four retired assertions: `output =~ "cw-wake-700"`, `output =~ "cw-brass-500"`, `output =~ "tailwind.config.js"`, `output =~ "Configure esbuild to bundle offline.js"`. KEEP the neutral ones: `"Offline UI components generated successfully!"`, `"get \"/offline\""`, `"TestAppWeb.OfflineController"`.

    ADD a test asserting generated templates contain semantic class refs and NO Tailwind: read the generated offline_page.html.heex and offline_root.html.heex, combine, assert `=~ "cw-offline-"`, and `refute` each of: "flex", "bg-white", "bg-cw-foam-50", "text-cw-current-950", "min-h-screen", the regex ~r/border-cw-/, "border-gray-", and "--cw-primitive-".

    ADD a test asserting the vendored offline.css contains semantic token refs and no primitives: read `<dir>/priv/static/assets/offline.css`, assert it exists, assert `=~ "var(--cw-surface-default)"`, `=~ "var(--cw-text-default)"`, `=~ "--cw-action-bg"`, and `refute =~ "--cw-primitive-"`.

    ADD an offline.css no-clobber test mirroring the existing tokens.css no-clobber test verbatim (swap path to `priv/static/assets/offline.css`, write custom content, re-run, assert the custom content is preserved and the output `=~ "reused"`).

    ADD a link-order test: read the generated offline_root.html.heex, find byte positions of "tokens.css", "app.css", "offline.css" via :binary.match, assert tokens_pos < app_pos < offline_pos.

    Do not weaken or delete the existing "generates offline UI templates" test or the existing tokens.css no-clobber / link-order tests.
  </action>
  <verify>
    <automated>cd /Users/jon/projects/crosswake && f=test/mix/tasks/crosswake.gen.offline_ui_test.exs && test "$(grep -coE 'cw-wake-700|cw-brass-500|tailwind.config.js|Configure esbuild to bundle offline.js' "$f")" = "0" && grep -q 'cw-offline-' "$f" && grep -q 'var(--cw-surface-default)' "$f" && grep -q 'offline.css' "$f" && grep -q 'refute' "$f" && mix test test/mix/tasks/crosswake.gen.offline_ui_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - `grep -coE 'cw-wake-700|cw-brass-500|tailwind.config.js|Configure esbuild to bundle offline.js' <file>` == 0 (retired assertions gone)
    - File still asserts "Offline UI components generated successfully!", "get \"/offline\"", "TestAppWeb.OfflineController"
    - File contains a `cw-offline-` assertion, a `var(--cw-surface-default)` assertion, and `refute` assertions including `--cw-primitive-` and `min-h-screen`
    - File contains an offline.css no-clobber test (`priv/static/assets/offline.css` preserved + "reused")
    - File contains a tokens < app < offline link-order assertion
    - `mix test test/mix/tasks/crosswake.gen.offline_ui_test.exs` exits 0
  </acceptance_criteria>
  <done>The generator test pins the semantic-token contract, rejects Tailwind/primitive regressions, and passes green against the Plan 02 generator.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none meaningful) | Test-only change. No runtime surface, no auth, no network, no user input. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-108-05 | Tampering | test asserting generated content | accept | Test reads generator output in a tmp dir; no external input, no secrets. Low-surface reality. |
| T-108-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs in this plan; nothing to verify. |
</threat_model>

<verification>
- Retired Tailwind assertions removed; neutral assertions kept
- New semantic-token / no-Tailwind / no-primitive assertions added
- offline.css no-clobber + link-order tests added
- `mix test test/mix/tasks/crosswake.gen.offline_ui_test.exs` exits 0
</verification>

<success_criteria>
NORM-04 satisfied: the generator test asserts the presence of semantic token references and the absence of retired Tailwind class names in the generated output, and the suite passes against the normalized generator.
</success_criteria>

<output>
Create `.planning/phases/108-consumer-normalization/108-03-SUMMARY.md` when done.
</output>
