---
phase: 108-consumer-normalization
plan: 04
type: execute
wave: 3
depends_on: ["108-01", "108-02", "108-03"]
files_modified:
  - examples/phoenix_host/lib/crosswake_example_web/controllers/offline_html/index.html.heex
  - priv/static/crosswake/offline.css
autonomous: false
requirements: [NORM-01, NORM-02]
must_haves:
  truths:
    - "The example host deck page renders correct colors in BOTH light and dark mode, driven only by tokens.css + the normalized app.css (D-13)"
    - "A generated offline page renders correctly in BOTH light and dark mode via tokens.css + offline.css (D-13)"
    - "Status buttons (success/danger) pass AA contrast in BOTH modes; if a solid fill fails in either mode, it is switched to an outlined treatment (D-06, D-13)"
    - "Focus rings are visible (outline-based) on interactive elements; no hover/focus weirdness; no layout shift (D-07, D-13)"
    - "Secondary text is legible (text-muted/stone-600), confirming the D-05 AA remediation visually"
  artifacts:
    - path: ".planning/phases/108-consumer-normalization/108-RENDER-VERIFY.md"
      provides: "Render-verify evidence log with the four screenshot states and the AA/contrast verdicts"
      contains: "light"
  key_links:
    - from: "rendered example host deck page"
      to: "priv/static/css/tokens.css + priv/static/css/app.css"
      via: "served <link> tags; dark mode via prefers-color-scheme + [data-theme]"
      pattern: "data-theme"
---

<objective>
Execute the D-13 RELEASE GATE: this phase is NOT done until the example host AND a generated offline page are browser-rendered and visually inspected in BOTH light and dark mode (system preference AND a `[data-theme="dark"]` toggle). Verification must LOOK at the rendered output — not grep for `var(--cw-`.

The render-verify confirms what greps cannot: correct colors, working dark mode, AA contrast on text and especially the D-06 status buttons (success flips to wake-500 in dark where white text fails AA), visible outline-based focus rings, no hover/focus weirdness, no layout shift. If a status button's solid fill fails contrast in either mode, this plan switches it to an outlined treatment in offline.css and the example index.html.heex, then re-verifies.

Purpose: Guarantee the normalization is correct in the eye, not just in the grep — the project's render-verify rule is mandatory for brand-facing surfaces.
Output: A render-verify evidence log; any D-06 contrast fix applied and re-verified.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/108-consumer-normalization/108-CONTEXT.md
@.planning/phases/108-consumer-normalization/108-01-SUMMARY.md
@.planning/phases/108-consumer-normalization/108-02-SUMMARY.md

<interfaces>
Dark-mode triggers (from tokens.css): @media (prefers-color-scheme: dark) :root:not([data-theme]) AND [data-theme="dark"].
So both system-preference dark AND an explicit [data-theme="dark"] attribute on <html>/<body> must be exercised.
D-06 flip facts: --cw-status-success → wake-500 in dark (white text ~2.9:1 fails AA); --cw-status-warning, --cw-runtime-offline flip; --cw-runtime-native and --cw-status-error do NOT flip.
Served example paths: deck page links /css/tokens.css then /css/app.css; the example offline page is controllers/offline_html/index.html.heex linking /css/tokens.css.
A generated offline page can be produced by running `mix crosswake.gen.offline_ui` into a tmp/host dir (the generator vendors tokens.css + offline.css and emits offline_root/offline_page); render the generated offline_page with both stylesheets linked.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Produce a renderable generated offline page and capture all four render states</name>
  <files>.planning/phases/108-consumer-normalization/108-RENDER-VERIFY.md</files>
  <read_first>
    - .planning/phases/108-consumer-normalization/108-01-SUMMARY.md and 108-02-SUMMARY.md (what was built; the final class names and any solid-vs-outlined decision noted)
    - priv/static/crosswake/offline.css (the stylesheet under visual test)
    - examples/phoenix_host/lib/crosswake_example_web/controllers/offline_html/index.html.heex (the example offline page under visual test)
    - .planning/phases/108-consumer-normalization/108-CONTEXT.md (D-06, D-07, D-13) and MEMORY visual render-verify rule (browser-render + LOOK; never present token wiring as done on grep alone)
  </read_first>
  <action>
    Generate a host offline page to render: run `mix crosswake.gen.offline_ui --dir <tmp> --app DemoApp` (or reuse the example host), producing offline_root + offline_page and the vendored tokens.css + offline.css under the host's priv/static/assets.

    Browser-render (headless Chromium/Playwright/puppeteer, or the project's established render tool) FOUR states and save screenshots:
      (a) generated offline page — light (no dark preference, no data-theme)
      (b) generated offline page — dark (emulate prefers-color-scheme: dark AND set [data-theme="dark"]; capture both if they differ)
      (c) example host deck/offline surface — light
      (d) example host deck/offline surface — dark
    Ensure tokens.css is linked BEFORE the consuming stylesheet in each rendered page (otherwise custom properties are undefined and the test is invalid).

    Write `.planning/phases/108-consumer-normalization/108-RENDER-VERIFY.md` logging: the command used, the four screenshot file paths, and a per-state verdict on: correct colors, dark mode actually flipping, status-button contrast (estimate or measure foreground/background AA — flag any < 4.5:1 normal / < 3:1 large), focus-ring visibility, hover/focus behavior, layout shift. Explicitly note the D-06 success-button result in dark mode.
  </action>
  <verify>
    <automated>cd /Users/jon/projects/crosswake && test -f .planning/phases/108-consumer-normalization/108-RENDER-VERIFY.md && grep -qi 'light' .planning/phases/108-consumer-normalization/108-RENDER-VERIFY.md && grep -qi 'dark' .planning/phases/108-consumer-normalization/108-RENDER-VERIFY.md && grep -qi 'contrast\|AA\|status' .planning/phases/108-consumer-normalization/108-RENDER-VERIFY.md && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - Four screenshots captured (generated offline light+dark, example host light+dark) with file paths recorded in 108-RENDER-VERIFY.md
    - The log records a per-state verdict covering colors, dark-mode flip, status-button AA, focus rings, hover/focus, layout shift
    - The D-06 dark-mode success-button result is explicitly stated (pass / failed → outlined)
    - tokens.css confirmed linked before the consuming stylesheet in each rendered page
  </acceptance_criteria>
  <done>Four render states are captured and a structured render-verify log exists with AA/contrast verdicts.</done>
</task>

<task type="auto">
  <name>Task 2: Apply any D-06 contrast remediation surfaced by render-verify and re-render</name>
  <files>priv/static/crosswake/offline.css, examples/phoenix_host/lib/crosswake_example_web/controllers/offline_html/index.html.heex</files>
  <read_first>
    - .planning/phases/108-consumer-normalization/108-RENDER-VERIFY.md (Task 1 verdicts — the source of what, if anything, must change)
    - priv/static/crosswake/offline.css and examples/phoenix_host/lib/crosswake_example_web/controllers/offline_html/index.html.heex (the files to remediate)
    - .planning/phases/108-consumer-normalization/108-CONTEXT.md (D-06: outlined-treatment fallback; --cw-text-inverse theme-aware foreground; --cw-status-warning never a fill behind light text)
  </read_first>
  <action>
    If Task 1 found any status-button (success/danger) or status indicator failing AA in EITHER mode: switch that element from solid-fill to the D-06 outlined treatment — status color applied as `color:` and `border-color:` on a neutral `--cw-surface-*` background, no light text on a flipping fill. Keep `--cw-status-warning` as never a fill behind light text. Re-render the affected state(s) and append the re-verification result to 108-RENDER-VERIFY.md.

    If Task 1 found everything passing AA in both modes (solid fill with `--cw-text-inverse` foreground holds), make NO code change — record "no remediation required" in the log. Do not restyle anything that already passes; this is a fix-only-if-needed task, not a redesign.

    Stay semantic-tier-only (D-04), no var() fallbacks (D-10), no new tokens.
  </action>
  <verify>
    <automated>cd /Users/jon/projects/crosswake && grep -qi 'remediation\|outlined\|no remediation required\|re-verif' .planning/phases/108-consumer-normalization/108-RENDER-VERIFY.md && test "$(grep -c -- '--cw-primitive-' priv/static/crosswake/offline.css)" = "0" && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - 108-RENDER-VERIFY.md records either the applied outlined-treatment remediation + re-verification, OR an explicit "no remediation required" verdict
    - If remediation applied: the affected element uses status color as color/border on a neutral surface (no light text on a flipping fill), and re-render confirms AA pass in both modes
    - offline.css still has zero `--cw-primitive-` and zero var() fallbacks after any edit
  </acceptance_criteria>
  <done>Any contrast failure is fixed with an outlined treatment and re-verified, or the surfaces are confirmed AA-passing as-is.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human render-verify sign-off (D-13 release gate)</name>
  <what-built>
    Both drifted consumers are normalized onto the semantic token tier: the example host served app.css and example offline page (Plan 01), and the offline_ui generator templates + vendored offline.css (Plan 02), with the test contract updated (Plan 03). Task 1/2 produced four rendered screenshots (generated offline + example host, each light and dark) and applied any D-06 contrast remediation, logged in 108-RENDER-VERIFY.md.
  </what-built>
  <how-to-verify>
    1. Open `.planning/phases/108-consumer-normalization/108-RENDER-VERIFY.md` and view the four screenshots.
    2. Confirm in LIGHT mode: brand colors look correct (teal/brass on foam), text is legible, secondary text (text-muted) is readable, buttons/badges look right.
    3. Confirm in DARK mode: the page actually flips to dark surfaces with legible text — no white-on-white or black-on-black, no unstyled flash.
    4. Confirm the success/danger status buttons are readable in BOTH modes (the D-06 footgun: success → wake-500 in dark; white text must not be muddy). If outlined treatment was applied, confirm it reads as a status affordance.
    5. Confirm focus rings are visible (tab through interactive elements) and there is no layout shift / hover weirdness between modes.
    6. To exercise the toggle path yourself: serve the example host and toggle `[data-theme="dark"]` on the root element, and/or flip your OS appearance to dark, and reload.
  </how-to-verify>
  <resume-signal>Type "approved" if all four states render correctly with AA-legible text and status buttons in both modes; otherwise describe the specific state/element that looks wrong and it will be remediated.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none meaningful) | Render-verification + an optional CSS contrast tweak. No auth, no network, no user input surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-108-06 | Information Disclosure | render screenshots in .planning | accept | Screenshots show brand styling of a demo offline page; no secrets, no PII. |
| T-108-SC | Tampering | npm/pip/cargo installs | mitigate | If a headless-browser tool must be installed to render (e.g. playwright/puppeteer), prefer an already-present tool; if an install is genuinely required, verify the package on npmjs.com before installing and note it in the SUMMARY. No new runtime/app dependency is added — render tooling is dev-only. |
</threat_model>

<verification>
- 108-RENDER-VERIFY.md exists with four render states and per-state AA verdicts
- D-06 success-button dark-mode result explicitly recorded (pass or outlined-remediated)
- Human checkpoint approved (D-13 release gate)
</verification>

<success_criteria>
D-13 satisfied: the example host and a generated offline page are browser-rendered and visually confirmed correct in BOTH light and dark mode — colors, dark-mode flip, AA-legible text and status buttons (D-05/D-06), visible focus rings (D-07), no layout shift — with any contrast failure remediated via outlined treatment and re-verified, and human sign-off recorded.
</success_criteria>

<output>
Create `.planning/phases/108-consumer-normalization/108-04-SUMMARY.md` when done, linking 108-RENDER-VERIFY.md and recording the final status-button treatment (solid with text-inverse vs outlined) and the human sign-off verdict.
</output>
