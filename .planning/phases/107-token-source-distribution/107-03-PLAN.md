---
phase: 107-token-source-distribution
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - guides/tokens.md
  - mix.exs
autonomous: true
requirements: [NORM-03]
must_haves:
  truths:
    - "A single distribution guide documents: source = crosswake.tokens.json; generate with one Node command; outputs = brandbook + priv copies; consumers receive by verbatim copy + link"
    - "The guide states the contract: never hand-edit a palette in any consumer; both generated copies are byte-identical"
    - "The guide is registered in mix.exs extras so it ships in the Hex package and appears in ExDoc"
  artifacts:
    - path: "guides/tokens.md"
      provides: "The one documented distribution mechanism (NORM-03)"
      contains: "node brandbook/tools/compile-tokens.js"
      min_lines: 30
    - path: "mix.exs"
      provides: "ExDoc extras registration for the guide"
      contains: "guides/tokens.md"
  key_links:
    - from: "mix.exs extras"
      to: "guides/tokens.md"
      via: "extras list entry"
      pattern: "guides/tokens.md"
---

<objective>
Write the ONE documented distribution mechanism (NORM-03) as `guides/tokens.md` and register it in `mix.exs` `extras:` so it ships in the Hex package and appears in ExDoc under the Guides group. The doc states: the source is `crosswake.tokens.json`; you regenerate with one Node command; outputs are `brandbook/tokens/tokens.css` and the packaged `priv/static/crosswake/tokens.css`; consumers receive tokens.css by verbatim copy + `<link>` (never a hand-edited palette); both generated copies are byte-identical.

Purpose: Satisfy NORM-03's "exactly one documented distribution mechanism" and success criterion #4 (a dev verifies the source is current with one command, no toolchain beyond Node).
Output: `guides/tokens.md` + a one-line `mix.exs` extras registration.

This plan is documentation + config only — it has no code-file overlap with Plans 01/02, so it runs in Wave 1 in parallel with Plan 01.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/107-token-source-distribution/107-CONTEXT.md
@.planning/phases/107-token-source-distribution/107-RESEARCH.md
@.planning/phases/107-token-source-distribution/107-PATTERNS.md

<interfaces>
<!-- From PATTERNS.md lines 410-522 + RESEARCH.md lines 557-594. Use directly. -->

Analog: guides/offline.md (same placement + capability naming + ExDoc registration).

mix.exs extras list (line 101 has "guides/offline.md"): add `"guides/tokens.md"` as one new entry, naturally after "guides/offline.md". The `groups_for_extras: [Guides: ~r/guides\//]` catch-all auto-groups it — no change needed there. The `files:` whitelist already includes `guides` — no change needed.

Recommended guide content outline is in PATTERNS.md lines 421-482 and RESEARCH.md lines 565-594.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write guides/tokens.md and register it in mix.exs extras</name>
  <files>guides/tokens.md, mix.exs</files>
  <read_first>
    - guides/offline.md (analog — style and structure reference)
    - mix.exs (the extras list being extended — read the docs/extras block around lines 90-125)
    - .planning/phases/107-token-source-distribution/107-PATTERNS.md lines 410-522 (guide content outline + mix.exs extras instructions)
    - .planning/phases/107-token-source-distribution/107-RESEARCH.md lines 557-594 (documentation placement rationale + content outline)
  </read_first>
  <action>
    Per D-10. Create `guides/tokens.md` titled "Crosswake Token Distribution" following the content outline in PATTERNS.md lines 421-482: a Source section (`crosswake.tokens.json`, DTCG 2025.10, frozen v9.0 contract); a Generate section (the single command `node brandbook/tools/compile-tokens.js` and its two outputs — `brandbook/tokens/tokens.css` and `priv/static/crosswake/tokens.css` — plus the `diff` parity command); a "What ships in the package" section (priv is whitelisted, brandbook is excluded); a "How consumers receive tokens.css" section covering both paths — via `mix crosswake.gen.offline_ui` (no-clobber copy to `priv/static/assets/tokens.css` + link before app.css) and direct Phoenix host (copy to static dir + `<link>` before app.css, with the load-order rationale that `--cw-*` must be defined first); and a Contract section (never hand-edit a consumer palette — edit the JSON and regenerate; both copies byte-identical; Phase 109 will add the CI diff gate). In `mix.exs`, add `"guides/tokens.md"` to the `extras:` list after the `"guides/offline.md"` entry. Do not modify `groups_for_extras:` or the `files:` whitelist (both already cover it). Do NOT place the doc under `brandbook/` (excluded from the package).
  </action>
  <verify>
    <automated>test -f guides/tokens.md && grep -q 'node brandbook/tools/compile-tokens.js' guides/tokens.md && grep -q 'priv/static/crosswake/tokens.css' guides/tokens.md && grep -q 'guides/tokens.md' mix.exs && mix compile --warnings-as-errors 2>/dev/null; mix docs 2>&1 | grep -vi warning | grep -qi error && exit 1 || exit 0</automated>
  </verify>
  <acceptance_criteria>
    - `guides/tokens.md` exists and contains `node brandbook/tools/compile-tokens.js`, `priv/static/crosswake/tokens.css`, a no-hand-edit contract statement, and the byte-identical/diff parity statement
    - `mix.exs` `extras:` list includes `"guides/tokens.md"`
    - `guides/tokens.md` is at least 30 lines and documents both consumer-receive paths (generator + direct host) with the tokens-before-app.css load-order note
    - The doc is in `guides/` (not `brandbook/`), so it ships in the package and appears in ExDoc
  </acceptance_criteria>
  <done>The single distribution mechanism is documented in guides/tokens.md and registered in mix.exs extras; a developer can regenerate and verify the source with one Node command following the doc.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none — documentation + config only) | No new code paths or filesystem writes beyond a Markdown file and a config list entry |

Documentation + ExDoc config. No runtime, build-execution, or external-input surface.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-107-05 | Information Disclosure | guides/tokens.md content | accept | Documents only public brand-token distribution mechanics; no secrets, credentials, or internal-only data |
| T-107-SC | Tampering | mix/hex installs | accept | No packages installed; doc-only change plus one existing-list config entry (RESEARCH.md lines 111-117) |
</threat_model>

<verification>
- `guides/tokens.md` exists with the single-command generate instruction and both consumer paths
- `mix.exs` extras includes `guides/tokens.md`
- `mix docs` builds without error (guide picked up under Guides group)
</verification>

<success_criteria>
- NORM-03: Exactly one distribution mechanism is documented end-to-end (source → generate → outputs → consumer copy + link → contract).
- Success criterion #4: the doc shows a developer verifying the token source is current with one Node command and no other toolchain.
- The guide ships in the Hex package and appears in ExDoc.
</success_criteria>

<output>
Create `.planning/phases/107-token-source-distribution/107-03-SUMMARY.md` when done
</output>
