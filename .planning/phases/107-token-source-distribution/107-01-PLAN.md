---
phase: 107-token-source-distribution
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - brandbook/tools/compile-tokens.js
  - brandbook/tools/compile-tokens.test.mjs
  - priv/static/crosswake/tokens.css
autonomous: true
requirements: [TOKN-04, TOKN-05]
must_haves:
  truths:
    - "Running `node brandbook/tools/compile-tokens.js` emits --cw-font-display, --cw-font-body, --cw-font-mono into tokens.css"
    - "Running the generator emits all 24 dimension tokens (--cw-text-scale-*, --cw-display-scale-*, --cw-radius-*, etc.) into tokens.css"
    - "Font families serialize as comma-joined stacks with multi-word names quoted (not raw arrays)"
    - "Dimension tokens emit their raw $value string (e.g. 16px, -0.02em)"
    - "A packaged mirror priv/static/crosswake/tokens.css is written, byte-identical to brandbook/tokens/tokens.css"
  artifacts:
    - path: "brandbook/tools/compile-tokens.js"
      provides: "Non-color token emit + second writeFileSync to priv mirror"
      contains: "serializeNonColor"
    - path: "priv/static/crosswake/tokens.css"
      provides: "Packaged distributable mirror of the full token set"
      contains: "--cw-font-display"
    - path: "brandbook/tools/compile-tokens.test.mjs"
      provides: "Node tests for font/dimension emit + priv mirror parity"
      contains: "byte-identical"
  key_links:
    - from: "brandbook/tools/compile-tokens.js"
      to: "priv/static/crosswake/tokens.css"
      via: "second writeFileSync after mkdirSync recursive"
      pattern: "PRIV_CSS_PATH"
    - from: "compile-tokens.js fontFamily tokens"
      to: "serializeNonColor"
      via: "non-color emit path bypassing resolveAlias"
      pattern: "serializeNonColor"
---

<objective>
Extend `brandbook/tools/compile-tokens.js` so the generated `tokens.css` carries the complete token set — the 3 `font.*` family tokens and all 24 `dimension.*` tokens — not just colors, all derived from `crosswake.tokens.json`. Also emit a packaged mirror at `priv/static/crosswake/tokens.css` that ships in the Hex package (because `brandbook/` is excluded from the package). Both copies are produced from one generator run and are byte-identical.

Purpose: Make `tokens.css` the genuine single source for typography and dimensions (TOKN-04/TOKN-05), and create the packaged bridge that downstream distribution (Plan 02) copies into consumers.
Output: Extended `compile-tokens.js`, new Node tests, and the new generated file `priv/static/crosswake/tokens.css`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/107-token-source-distribution/107-CONTEXT.md
@.planning/phases/107-token-source-distribution/107-RESEARCH.md
@.planning/phases/107-token-source-distribution/107-PATTERNS.md

<interfaces>
<!-- Extracted from compile-tokens.js (PATTERNS.md lines 32-107). Use these directly; no exploration needed. -->

compile-tokens.js exports `{ flattenTokens, resolveAlias }`. Key existing structures:
- `ROOT = path.resolve(__dirname, '../..')` (repo root anchor)
- `CSS_PATH = path.join(ROOT, 'brandbook/tokens/tokens.css')` (existing single output)
- `flattenTokens()` already captures ALL tokens including font/dimension; they are dropped by the `groups` allow-list filter only
- `props(flat, paths, dark, isPrim)` calls `resolveAlias(t.$value)` — CANNOT handle fontFamily arrays (returns the raw Array → broken CSS)
- `main()` builds the `out` array then `fs.writeFileSync(CSS_PATH, out, 'utf8')` once

Complete non-color token inventory (27 tokens) and the exact expected `:root` block output are enumerated in RESEARCH.md lines 423-493 and PATTERNS.md lines 249-277. Treat that block as the verified output spec.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add font/dimension serialization helpers and the non-color :root block</name>
  <files>brandbook/tools/compile-tokens.js</files>
  <read_first>
    - brandbook/tools/compile-tokens.js (the file being modified — read in full)
    - .planning/phases/107-token-source-distribution/107-PATTERNS.md lines 109-157 (three insertion points + serializeNonColor/propsNonColor helpers)
    - .planning/phases/107-token-source-distribution/107-RESEARCH.md lines 175-206, 299-304 (Pattern 1 + Pitfall 1: why props() cannot serialize fontFamily arrays)
    - .planning/phases/107-token-source-distribution/107-RESEARCH.md lines 337-389 (exact expected output block + per-family quoting rules)
  </read_first>
  <behavior>
    - serializeNonColor for a fontFamily token returns a comma-space-joined stack with multi-word names double-quoted: font.display → `"Space Grotesk", ui-sans-serif, system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif`
    - serializeNonColor for a dimension token returns the raw $value string unchanged: text-scale.md → `16px`; tracking.tight → `-0.02em`
    - The new `:root` block contains all 27 non-color properties (3 font + 24 dimension), alphabetically sorted, two-space indented, each terminated with `;`
    - fontFamily tokens never pass through resolveAlias (no `[object Array]` or unquoted multi-word names in output)
    - The block is labeled `/* ─── Fonts & dimensions ─── */` and appended AFTER the existing color tiers; it has no dark-mode variant block
  </behavior>
  <action>
    Implement per D-01/D-02/D-03/D-04 and PATTERNS.md insertion points 1-3 (excluding the second writeFileSync, which is Task 2). Add a `NON_COLOR_GROUPS` array (`font`, `text-scale`, `display-scale`, `line-height`, `spacing`, `radius`, `focus`, `tracking`). Add `serializeNonColor(token)` that branches on `token['$type']`: for `fontFamily`, map each family name (wrap in double quotes if it `.includes(' ')`) and join with `', '`; otherwise return `token['$value']` verbatim. Add `propsNonColor(flat, paths)` mirroring the existing `props()` declaration format (`'  --cw-' + p.replace(/\./g, '-') + ': ' + serializeNonColor(flat[p]) + ';'`). In `main()`, after the `sem` filter, add `const nonColor = all.filter(p => NON_COLOR_GROUPS.some(g => p.startsWith(g + '.')))`. Append a labeled `/* ─── Fonts & dimensions ─── */` `:root { ... }` block to the `out` array using `propsNonColor(flat, nonColor)`, positioned after the Forbidden-pairings comment block and before the final trailing `''`. Do NOT route non-color tokens through `props()` or `resolveAlias`. Do NOT add these tokens to any dark-mode block (no `$dark` values exist).
  </action>
  <verify>
    <automated>node brandbook/tools/compile-tokens.js && grep -q '\-\-cw-font-display: "Space Grotesk"' brandbook/tokens/tokens.css && grep -q '\-\-cw-text-scale-md: 16px' brandbook/tokens/tokens.css && grep -q '\-\-cw-radius-lg: 14px' brandbook/tokens/tokens.css && grep -q '\-\-cw-tracking-tight: -0.02em' brandbook/tokens/tokens.css && ! grep -q 'object Array' brandbook/tokens/tokens.css</automated>
  </verify>
  <acceptance_criteria>
    - `brandbook/tokens/tokens.css` contains `--cw-font-display: "Space Grotesk"` (quoted multi-word family) and `--cw-font-body`, `--cw-font-mono`
    - `brandbook/tokens/tokens.css` contains `--cw-text-scale-md: 16px`, `--cw-display-scale-sm: 28px`, `--cw-radius-lg: 14px`, `--cw-tracking-tight: -0.02em`
    - The file contains a `/* ─── Fonts & dimensions ─── */` comment immediately preceding the new `:root` block
    - No occurrence of `[object Array]` or an unquoted multi-word font name anywhere in the file
    - `node brandbook/tools/compile-tokens.js` exits 0
  </acceptance_criteria>
  <done>tokens.css contains all 27 non-color properties correctly serialized; fontFamily multi-word names quoted; dimension values raw; the generator runs clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Emit the packaged priv mirror and add Node tests</name>
  <files>brandbook/tools/compile-tokens.js, brandbook/tools/compile-tokens.test.mjs, priv/static/crosswake/tokens.css</files>
  <read_first>
    - brandbook/tools/compile-tokens.js (after Task 1 changes)
    - brandbook/tools/compile-tokens.test.mjs (the test file being extended — read in full)
    - .planning/phases/107-token-source-distribution/107-PATTERNS.md lines 161-214 (test import/setup pattern + new test bodies)
    - .planning/phases/107-token-source-distribution/107-RESEARCH.md lines 208-225, 306-316 (Pattern 2 + Pitfalls 2 & 3: mkdirSync recursive, ROOT-anchored path)
  </read_first>
  <behavior>
    - After running the script, `priv/static/crosswake/tokens.css` exists
    - `priv/static/crosswake/tokens.css` is byte-identical to `brandbook/tokens/tokens.css` (assert.strictEqual on file contents)
    - The priv mirror carries the same `/* GENERATED from crosswake.tokens.json — do not edit */` header
    - New tests positively assert presence of `--cw-font-display` (quoted), `--cw-font-body`, `--cw-font-mono`, `--cw-text-scale-md: 16px`, `--cw-radius-lg: 14px`, `--cw-display-scale-sm: 28px`
    - All pre-existing tests still pass (semantic/primitive tiers unchanged)
  </behavior>
  <action>
    In `compile-tokens.js`, add `const PRIV_CSS_PATH = path.join(ROOT, 'priv/static/crosswake/tokens.css')` alongside `CSS_PATH` (ROOT-anchored, never relative — per Pitfall 3). In `main()`, after the existing `fs.writeFileSync(CSS_PATH, out, 'utf8')`, add `fs.mkdirSync(path.dirname(PRIV_CSS_PATH), { recursive: true })` then `fs.writeFileSync(PRIV_CSS_PATH, out, 'utf8')` (same `out` string → byte-identical) and a `process.stdout.write` confirmation line. The `{ recursive: true }` is mandatory because `priv/static/` does not exist yet (per D-06, Pitfall 2). In `compile-tokens.test.mjs`, add `const PRIV_TOKENS_CSS = resolve(ROOT, 'priv/static/crosswake/tokens.css')` and the eight Wave-0 tests enumerated in PATTERNS.md lines 206-214 (three font-stack presence tests, three dimension presence tests, one priv-mirror-exists test, one byte-identical-parity test) using the existing `execSync(node COMPILE_SCRIPT)` + `readFileSync` + `assert` structure. Commit the generated `priv/static/crosswake/tokens.css` to git so it ships in the Hex package.
  </action>
  <verify>
    <automated>node --test brandbook/tools/compile-tokens.test.mjs && diff brandbook/tokens/tokens.css priv/static/crosswake/tokens.css && grep -q 'GENERATED from crosswake.tokens.json' priv/static/crosswake/tokens.css</automated>
  </verify>
  <acceptance_criteria>
    - `node --test brandbook/tools/compile-tokens.test.mjs` reports all tests passing (existing + 8 new)
    - `priv/static/crosswake/tokens.css` exists and `diff brandbook/tokens/tokens.css priv/static/crosswake/tokens.css` exits 0 (no output)
    - `priv/static/crosswake/tokens.css` line 1 is the `/* GENERATED from crosswake.tokens.json — do not edit */` header
    - `compile-tokens.js` declares `PRIV_CSS_PATH` via `path.join(ROOT, ...)` and calls `fs.mkdirSync(..., { recursive: true })` before the second write
    - The generated `priv/static/crosswake/tokens.css` is staged/committed (not gitignored)
  </acceptance_criteria>
  <done>Both output files are produced from one run and are byte-identical; the priv mirror is committed; all Node tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| build-tool → filesystem | `compile-tokens.js` writes generated CSS to two repo paths |

This phase is build-tooling / static-asset generation. No runtime auth, session, or external-input surface (per RESEARCH.md Security Domain, lines 720-724).

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-107-01 | Tampering | generated `tokens.css` files | mitigate | `/* GENERATED … do not edit */` header on both outputs (asserted by existing test) signals reviewers not to hand-edit |
| T-107-02 | Tampering | `PRIV_CSS_PATH` write target | mitigate | ROOT-anchored `path.join(ROOT, ...)` — no path traversal from cwd; deterministic fixed relative subpath, not derived from external input |
| T-107-SC | Tampering | npm/node installs | accept | No packages installed; `compile-tokens.js` imports only Node stdlib (`fs`, `path`); tests use `node:test`/`node:assert` (RESEARCH.md Package Legitimacy Audit, lines 111-117) |
</threat_model>

<verification>
- `node brandbook/tools/compile-tokens.js` exits 0 and writes both files
- `node --test brandbook/tools/compile-tokens.test.mjs` all green
- `diff brandbook/tokens/tokens.css priv/static/crosswake/tokens.css` exits 0
- No `[object Array]` or unquoted multi-word font names in either output
</verification>

<success_criteria>
- TOKN-04: `tokens.css` contains `--cw-font-display`, `--cw-font-body`, `--cw-font-mono` as valid quoted font stacks
- TOKN-05: `tokens.css` contains all 24 dimension tokens (`--cw-text-scale-*`, `--cw-display-scale-*`, `--cw-radius-*`, `--cw-line-height-*`, `--cw-spacing-base`, `--cw-focus-ring-width`, `--cw-tracking-tight`) as raw value strings
- The packaged mirror `priv/static/crosswake/tokens.css` exists, is byte-identical to the brand book copy, and is committed for the Hex package
</success_criteria>

<output>
Create `.planning/phases/107-token-source-distribution/107-01-SUMMARY.md` when done
</output>
