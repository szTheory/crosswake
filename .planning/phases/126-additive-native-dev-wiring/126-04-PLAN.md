---
phase: 126-additive-native-dev-wiring
plan: 04
type: execute
wave: 3
depends_on: [126-01, 126-02, 126-03]
files_modified:
  - test/crosswake/guides/native_dev_wiring_test.exs
  - examples/QUICK_START.md
autonomous: true
requirements: [NDEV-03]

must_haves:
  truths:
    - "A source-derived guard proves the proof posture is untouched (prod plist/manifest/fixtures), the dev wiring files exist, the dev fixtures point at the source-derived port, and the dev fixtures are honestly tagged"
    - "The guard is non-vacuous: synthetic wrong-value cases prove each assertion actually fires"
    - "QUICK_START has one additive Run-Against-the-Local-Backend section with copy-paste iOS (-scheme Dev) and Android (installDevDebug) commands"
    - "The new section carries the honest advisory-native label and both native proof labels required by the existing drift scanner; existing QUICK_START required content is unchanged"
  artifacts:
    - path: "test/crosswake/guides/native_dev_wiring_test.exs"
      provides: "Crosswake.Guides.NativeDevWiringTest — proof-untouched + dev-exists + dev-correct + dev-tagged + anti-vacuity"
      contains: "NativeDevWiringTest"
    - path: "examples/QUICK_START.md"
      provides: "Run Against the Local Backend (Dev Wiring) section with iOS + Android launch commands"
      contains: "Run Against the Local Backend"
  key_links:
    - from: "test/crosswake/guides/native_dev_wiring_test.exs"
      to: "examples/phoenix_host/config/runtime.exs"
      via: "source-derived port via the System.get_env PORT regex (never hardcoded 4700)"
      pattern: "get_env"
    - from: "test/crosswake/guides/native_dev_wiring_test.exs"
      to: "examples/ios_shell_host/Fixtures/route_activation-dev.json"
      via: "Jason.decode! key lookup on origin/url against the source-derived localhost:<port>"
      pattern: "route_activation-dev\\.json"
    - from: "examples/QUICK_START.md"
      to: "test/crosswake/guides/quick_start_adoption_drift_test.exs"
      via: "new section satisfies the existing native-label + source-derived-port drift checks"
      pattern: "published-coordinate"
---

<objective>
Close the phase with the D-14 proof-posture guard test (source-derived, JSON via
Jason.decode!, with synthetic anti-vacuity regression cases) and the single additive
D-15 QUICK_START dev-wiring section carrying the honest advisory-native voice (D-16) —
satisfying NDEV-03. This guard is the automated coverage that proves the phase's reason
for existing: the checked-in proof artifacts stay byte-untouched while the dev overlay
correctly reaches the local backend.

Purpose: Doc-contract / drift tests guard every port/route/command claim in this repo;
the guard also closes a real coverage gap (the existing QUICK_START scanner does not
match 10.0.2.2:4700). The QUICK_START section is the copy-paste NDEV-03 launch surface.

Output: test/crosswake/guides/native_dev_wiring_test.exs; one additive QUICK_START
"Run Against the Local Backend (Dev Wiring)" section.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/126-additive-native-dev-wiring/126-CONTEXT.md
@.planning/phases/126-additive-native-dev-wiring/126-RESEARCH.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write the source-derived proof-posture guard with anti-vacuity cases (D-14)</name>
  <files>test/crosswake/guides/native_dev_wiring_test.exs</files>
  <read_first>
    - test/crosswake/guides/port_registry_test.exs (verbatim committed_port/0 + source_port!/3 helpers and the PORT regex)
    - test/crosswake/guides/native_evidence_drift_test.exs (the synthetic anti-vacuity regression idiom — feed a deliberately-wrong in-memory value to the same comparison and assert the failure category fires)
    - test/crosswake/contract/contract_drift_test.exs (JSON key-lookup via Jason.decode!, never text-grep on JSON)
    - examples/ios_shell_host/CrosswakeShell/Info.plist and Info-Dev.plist (proof-untouched + dev-exists assertions)
    - examples/android_shell_host/app/src/main/AndroidManifest.xml and app/src/dev/res/xml/network_security_config_dev.xml (proof-untouched + dev-correct assertions)
    - .planning/phases/126-additive-native-dev-wiring/126-RESEARCH.md §"D-14 Guard Test Grounding" (the exact assertion blocks: proof-untouched / dev-exists / dev-correct / dev-honestly-tagged)
  </read_first>
  <behavior>
    - Test: prod Info.plist contains none of NSAllowsArbitraryLoads / NSExceptionDomains / localhost; prod iOS + Android fixtures origin equals the proof invalid-domain; prod AndroidManifest has usesCleartextTraffic=false and does not reference network_security_config_dev.
    - Test: all four dev wiring files exist (Info-Dev.plist, route_activation-dev.json, dev assets route_activation.json, network_security_config_dev.xml).
    - Test: dev fixture origin/url contain the source-derived localhost:<port> (iOS) and 10.0.2.2:<port> (Android); Info-Dev.plist contains the localhost cleartext exception; network_security_config_dev.xml contains 10.0.2.2 + cleartextTrafficPermitted.
    - Test: dev _generated_by starts with the prod fixture's _generated_by value (tolerates the dev suffix).
    - Test (anti-vacuity): a synthetic prod-plist-with-NSExceptionDomains produces a failure; a synthetic dev fixture whose origin still points at the proof domain produces a failure.
  </behavior>
  <action>
Create test/crosswake/guides/native_dev_wiring_test.exs defining module Crosswake.Guides.NativeDevWiringTest. Copy the committed_port/0 and source_port!/3 helpers (and the PORT regex against examples/phoenix_host/config/runtime.exs) verbatim from port_registry_test.exs — derive the port from source, never hardcode 4700 (this also closes the 10.0.2.2 coverage gap). Read all JSON via File.read! |> Jason.decode! and assert with key lookup, never String.contains? on raw JSON for fixture values. Implement the four assertion blocks from RESEARCH §"D-14 Guard Test Grounding": (a) proof-untouched — prod Info.plist contains none of NSAllowsArbitraryLoads / NSExceptionDomains / the localhost token; prod iOS + Android fixtures origin equals the proof invalid-domain; prod AndroidManifest contains usesCleartextTraffic="false" and does not reference network_security_config_dev. (b) dev-exists — assert File.exists? for the four dev files. (c) dev-correct — using the source-derived port, assert the iOS dev fixture origin+url contain localhost:<port> and the Android dev fixture origin+url contain 10.0.2.2:<port>; assert Info-Dev.plist contains the localhost cleartext exception tokens (NSExceptionDomains, the localhost host, NSExceptionAllowsInsecureHTTPLoads) and network_security_config_dev.xml contains 10.0.2.2 and cleartextTrafficPermitted. (d) dev-honestly-tagged — assert the dev fixture _generated_by starts with the prod fixture _generated_by. Add synthetic anti-vacuity regression cases (house idiom): feed a deliberately-wrong in-memory value (a prod-plist string CONTAINING NSExceptionDomains; a dev-fixture map whose origin still points at the proof domain) to the same predicate/helper and assert the expected failure is produced, proving the positive assertions are not vacuous.
  </action>
  <verify>
    <automated>cd /Users/jon/projects/crosswake && mix test test/crosswake/guides/native_dev_wiring_test.exs 2>&1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - The file defines `Crosswake.Guides.NativeDevWiringTest`.
    - grep shows the PORT regex `System\.get_env\("PORT"\)` (source-derived, no literal `4700` used as the asserted port).
    - The test reads dev fixtures via `Jason.decode!` and asserts on the `origin` / `url` keys.
    - It includes at least two synthetic anti-vacuity regression cases.
    - `mix test test/crosswake/guides/native_dev_wiring_test.exs` passes with 0 failures.
  </acceptance_criteria>
  <done>A non-vacuous, source-derived guard proves proof-untouched + dev-exists + dev-correct + dev-tagged and is green.</done>
</task>

<task type="auto">
  <name>Task 2: Add the additive QUICK_START dev-wiring section with honest voice (D-15/D-16)</name>
  <files>examples/QUICK_START.md</files>
  <read_first>
    - examples/QUICK_START.md (the "Android Local-Development Walkthrough" section the new section follows, and the existing native-label block; do not remove existing required content)
    - test/crosswake/guides/quick_start_adoption_drift_test.exs (wrong_port_failures regex, documented_path_failures, native_label_failures 1500-char window requiring both "checked-in public-coordinate proof" and "published-coordinate mode", require_contains for existing required content)
    - .planning/phases/126-additive-native-dev-wiring/126-RESEARCH.md §"QUICK_START.md — D-15 New Section Structure" (the exact section content) and §"QUICK_START.md Drift Scanner Compatibility" (label + path rules)
    - .planning/phases/126-additive-native-dev-wiring/126-CONTEXT.md D-16 (advisory-native voice; caveat next to the command; JAVA_HOME 17 note right after the gradle command)
  </read_first>
  <action>
Add exactly ONE new section titled "Run Against the Local Backend (Dev Wiring)" after the existing "Android Local-Development Walkthrough" and before "Troubleshooting Quick Checks". Do NOT create a new guides/ file (Phase 128 owns guides/see_it_run.md) and do NOT edit the host READMEs. The section: (1) opens with the honest advisory-native sentence — a successful simulator/emulator run confirms the dev wiring reaches the local backend but does not prove physical-device support (D-16) — and includes BOTH drift-scanner-required labels "checked-in public-coordinate proof" and "published-coordinate mode" within the section so native_label_failures is satisfied; (2) shows starting the shared backend (PORT=4700 mix phx.server, or docker compose up); (3) iOS — the xcodebuild -scheme Dev -configuration Debug-Dev command (destination iPhone 16) plus the open-in-Xcode-pick-Dev path; (4) Android — installDevDebug with the JAVA_HOME=/opt/homebrew/opt/openjdk@17 note placed right before/with the gradle command (caveat next to the command — D-16), then adb shell am start -n dev.crosswake.shell.dev/.MainActivity. Reference hosts at the directory level (examples/ios_shell_host, examples/android_shell_host) — do not cite deeper file paths in prose (documented_path_failures fragility). Do not remove or alter any existing required content (PORT=4700 mix phx.server, localhost:4700, /offline, /bridge-proof, mix setup, etc.).
  </action>
  <verify>
    <automated>cd /Users/jon/projects/crosswake && grep -q 'Run Against the Local Backend' examples/QUICK_START.md && grep -q 'scheme Dev' examples/QUICK_START.md && grep -q 'installDevDebug' examples/QUICK_START.md && mix test test/crosswake/guides/quick_start_adoption_drift_test.exs 2>&1 | tail -6</automated>
  </verify>
  <acceptance_criteria>
    - QUICK_START contains a single new "Run Against the Local Backend (Dev Wiring)" section after the Android walkthrough.
    - The section contains `-scheme Dev`, `installDevDebug`, `adb shell am start -n dev.crosswake.shell.dev/.MainActivity`, and the `JAVA_HOME=/opt/homebrew/opt/openjdk@17` note adjacent to the gradle command.
    - The section contains both `checked-in public-coordinate proof` and `published-coordinate mode`.
    - `mix test test/crosswake/guides/quick_start_adoption_drift_test.exs` passes (no wrong-port / documented-path / native-label failures).
    - No new file under `guides/` is created and the host READMEs are unchanged.
  </acceptance_criteria>
  <done>QUICK_START surfaces the copy-paste iOS + Android dev-wiring commands with honest advisory-native voice, and all existing QUICK_START drift checks stay green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| documented commands → reader | The launch commands must be honest about what dev wiring proves (advisory native, not device) |
| guard test → proof posture | The guard is the automated boundary that fails the build if any future change mutates the proof artifacts or weakens the dev scoping |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-126-01 | Repudiation | future change silently mutating prod proof posture or leaking cleartext to prod | mitigate | The D-14 guard asserts prod plist/manifest/fixtures are untouched and dev cleartext stays host-pinned; anti-vacuity cases prove the assertions actually fire — the build fails on regression |
| T-126-06 | Information disclosure | docs overclaiming native support (dishonest evidence) | mitigate | D-16 advisory-native voice + the required native labels; quick_start_adoption_drift_test enforces the labels and source-derived port |
| T-126-SC | Tampering | npm/pip/cargo installs | accept | No package-manager installs in this plan — ExUnit + Markdown only (Jason already present) |
</threat_model>

<verification>
- `mix test test/crosswake/guides/native_dev_wiring_test.exs` green (source-derived, non-vacuous).
- `mix test test/crosswake/guides/quick_start_adoption_drift_test.exs` green after the new section.
- Full `mix test` green before `/gsd-verify-work` (phase gate).
- No `guides/` file created; host READMEs unchanged (`git diff --quiet`).
</verification>

<success_criteria>
- The proof-posture guard proves the byte-untouched invariant + correct dev wiring + honest tagging, and cannot pass vacuously.
- QUICK_START carries the single additive NDEV-03 launch section with honest advisory-native voice and passes all existing drift checks.
</success_criteria>

<output>
Create `.planning/phases/126-additive-native-dev-wiring/126-04-SUMMARY.md` when done
</output>
