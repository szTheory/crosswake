---
phase: 46-sigra-auth-contract-only-slice
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/crosswake/companions/sigra/contracts.ex
  - test/crosswake/companions/sigra/contracts_test.exs
autonomous: true
requirements: [AUTH-01]

must_haves:
  truths:
    - "A Phoenix team can construct a typed backend-owned AuthContext with actor_id, org_id, mfa_level, and auth_age normalized for route evaluation."
    - "D-09: SessionAuthorityLane is backend-set only; device/client signals are accepted only as evidence and validators reject evidence fields `authority_state`, `mfa_level`, `auth_level`, `session_authority`, and `access_granted`."
    - "StepUpChallenge is contract state only and does not imply handoff, OAuth, passkey, refresh-token, or ceremony execution."
    - "D-30: server-side session authority is the durable truth; client session material is only lookup/transport evidence, never independent route authority."
    - "D-31: bearer/token presence is insufficient for high-risk route activation; route requirements check auth level and recency."
  artifacts:
    - path: "lib/crosswake/companions/sigra/contracts.ex"
      provides: "Sigra auth contract structs, closed vocabularies, constructors, and validators"
      contains: "defmodule Crosswake.Companions.Sigra.Contracts"
    - path: "test/crosswake/companions/sigra/contracts_test.exs"
      provides: "Wave 0 AUTH-01 unit coverage for contract shape and authority/evidence boundaries"
      contains: "Crosswake.Companions.Sigra.ContractsTest"
  key_links:
    - from: "new_auth_context/1"
      to: "validate_auth_context/1"
      via: "constructor validation result"
      pattern: "validate_auth_context"
    - from: "validate_evidence_lane/1"
      to: "reject_evidence_authority_lane/2"
      via: "authority-field rejection"
      pattern: "authority_state|session_authority|access_granted"
---

<objective>
Create the Phase 46 contract foundation: `Crosswake.Companions.Sigra.Contracts` with typed
`AuthContext`, `SessionAuthorityLane`, `StepUpChallenge`, and evidence-only state, plus the Wave 0
AUTH-01 unit tests that lock the backend-authority boundary before any RouteGate wiring starts.

Purpose: AUTH-01 is the security boundary for the rest of the phase. The route DSL and fail-closed
gate checks in later plans are only credible if the contract layer already rejects client/device
authority claims and exposes mechanically comparable MFA/recency helpers.

Output: `lib/crosswake/companions/sigra/contracts.ex` and
`test/crosswake/companions/sigra/contracts_test.exs`.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/MILESTONE-ARC.md
@.planning/phases/46-sigra-auth-contract-only-slice/46-CONTEXT.md
@.planning/phases/46-sigra-auth-contract-only-slice/46-RESEARCH.md
@.planning/phases/46-sigra-auth-contract-only-slice/46-VALIDATION.md
@.planning/phases/46-sigra-auth-contract-only-slice/46-PATTERNS.md

<interfaces>
From `lib/crosswake/companions/rindle/contracts.ex` (contract seam to mirror per D-01):

```elixir
@spec new_capture_evidence(map() | keyword()) :: {:ok, CaptureEvidence.t()} | {:error, keyword()}
def new_capture_evidence(attrs), do: build_and_validate(attrs, CaptureEvidence, &validate_capture_evidence/1, :capture_evidence)

def validate_capture_evidence(%CaptureEvidence{} = evidence) do
  []
  |> validate_required_string(:grant_id, evidence.grant_id)
  |> validate_evidence_source(evidence.source)
  |> reject_trace_authority_lane(evidence.trace_metadata)
  |> to_validation_result()
end
```

From `lib/crosswake/companions/rindle/contracts.ex` (forbidden authority-field rejection pattern):

```elixir
defp reject_trace_authority_lane(errors, trace_metadata) when is_map(trace_metadata) do
  errors
  |> reject_trace_key(trace_metadata, :authority_state)
  |> reject_trace_key(trace_metadata, :availability_state)
end
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create the AUTH-01 unit test scaffold for Sigra contracts</name>
  <files>test/crosswake/companions/sigra/contracts_test.exs</files>
  <read_first>
    - test/crosswake/companions/sigra/contracts_test.exs (new file to create; keep open while adding cases)
    - test/crosswake/companions/rindle/contracts_test.exs (contract test shape: vocabulary locks, constructor happy path, invalid vocabulary, forbidden authority-field cases)
    - lib/crosswake/companions/rindle/contracts.ex (nested struct and validator naming pattern)
    - .planning/phases/46-sigra-auth-contract-only-slice/46-CONTEXT.md (D-01 through D-11, D-29 through D-34 are the contract boundary)
    - .planning/phases/46-sigra-auth-contract-only-slice/46-VALIDATION.md (Wave 0 AUTH-01 test file requirement)
  </read_first>
  <behavior>
    - Test 1: valid AuthContext constructor returns a typed struct with actor_id, org_id, mfa_level, and auth_age normalized for route comparison.
    - Test 2: invalid mfa vocabulary is rejected for both AuthContext and SessionAuthorityLane.
    - Test 3: evidence payloads carrying authority fields such as authority_state, mfa_level, auth_level, session_authority, or access_granted are rejected.
    - Test 4: StepUpChallenge remains reference/state only and does not require or expose ceremony/token/passkey/OAuth execution fields.
  </behavior>
  <action>
    Create `Crosswake.Companions.Sigra.ContractsTest` as the Wave 0 AUTH-01 test file per
    `46-VALIDATION.md`. Mirror the Rindle contract test layout with `describe` blocks for
    `AuthContext`, `SessionAuthorityLane`, `StepUpChallenge`, and the evidence lane/fields. Lock the
    contract decisions from D-01 through D-11 directly in test names and assertions: layered plain
    structs (D-01/D-02), minimum `AuthContext` fields `actor_id`, `org_id`, `mfa_level`, `auth_age`
    (D-04), a closed comparable MFA vocabulary (D-05), numeric-seconds auth-age normalization
    suitable for later RouteGate checks (D-06), `StepUpChallenge` as state only (D-07), and
    backend-only authority lanes with explicit rejection of evidence-carried authority fields
    (D-08 through D-11). Keep the tests hermetic and `async: true`. Include explicit assertions that
    no contract constructor accepts passkey material, OAuth/PKCE artifacts, bearer tokens, or
    refresh-token semantics in this phase (D-07, D-32, D-33, D-34).
  </action>
  <verify>
    <automated>cd /Users/jon/projects/crosswake && test -f test/crosswake/companions/sigra/contracts_test.exs && rg -n "authority_state|session_authority|access_granted|StepUpChallenge|AuthContext" test/crosswake/companions/sigra/contracts_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - `test/crosswake/companions/sigra/contracts_test.exs` exists and defines `Crosswake.Companions.Sigra.ContractsTest`
    - The file has dedicated `describe` coverage for `AuthContext`, `SessionAuthorityLane`, `StepUpChallenge`, and the evidence surface
    - The file asserts rejection of evidence-carried authority keys `authority_state`, `mfa_level`, `auth_level`, `session_authority`, and `access_granted`
    - The file contains at least one assertion locking the MFA vocabulary and one assertion locking numeric auth-age normalization for route checks
    - The file contains an explicit non-goal assertion that no passkey/OAuth/refresh-token machinery is modeled in Phase 46
  </acceptance_criteria>
  <done>The AUTH-01 Wave 0 contract test file exists with explicit coverage for type shape, vocabulary, backend-only authority, and Sigra non-goals.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement Crosswake.Companions.Sigra.Contracts to satisfy AUTH-01</name>
  <files>lib/crosswake/companions/sigra/contracts.ex</files>
  <read_first>
    - lib/crosswake/companions/sigra/contracts.ex (new file to create; keep open while implementing)
    - lib/crosswake/companions/rindle/contracts.ex (constructor/validator helpers and authority-lane rejection pattern)
    - lib/crosswake/commerce/contracts.ex (closed vocabulary + helper-accessor style)
    - .planning/phases/46-sigra-auth-contract-only-slice/46-CONTEXT.md (D-01 through D-11, D-29 through D-34)
    - .planning/phases/46-sigra-auth-contract-only-slice/46-RESEARCH.md (Pitfall 2: evidence field creep into authority)
  </read_first>
  <behavior>
    - Test 1: `new_auth_context/1`, `new_session_authority_lane/1`, and `new_step_up_challenge/1` return `{:ok, struct}` for valid attrs and `{:error, keyword()}` for invalid attrs.
    - Test 2: `mfa_level_vocabulary/0` and the comparison helper preserve a closed ordering suitable for `auth_min_level` checks.
    - Test 3: evidence validation rejects authority-field smuggling even when the payload is otherwise well-formed.
  </behavior>
  <action>
    Create `Crosswake.Companions.Sigra.Contracts` per D-01 through D-11 and the ecosystem posture in
    D-29 through D-34. Use layered plain structs under this module (D-01/D-02): at minimum
    `AuthContext`, `SessionAuthorityLane`, `StepUpChallenge`, and either `EvidenceLane` or explicit
    evidence structs/fields with the same backend-vs-evidence split (D-03). `AuthContext` must carry
    `actor_id`, `org_id`, `mfa_level`, and `auth_age` (D-04); implement auth-age as numeric seconds
    and expose a helper such as `auth_age_seconds/1` so later RouteGate code compares normalized
    seconds per D-06. Lock a mechanically comparable MFA vocabulary and expose helper functions such
    as `mfa_level_vocabulary/0` and `mfa_level_meets?/2` or equivalent so Plan 46-02 does not
    duplicate ordering logic (D-05). Keep `StepUpChallenge` reference/state only with fields like
    `challenge_id`, `required_mfa_level`, `max_auth_age_seconds`, `reason`, `issued_at`, and
    `expires_at`; do not imply or model ceremony execution, passkeys, OAuth, PKCE, or refresh-token
    rotation (D-07, D-32, D-33, D-34). Implement `new_*` constructors and `validate_*` functions
    returning `:ok | {:error, keyword()}`. Enforce the backend authority boundary by validating
    `SessionAuthorityLane` as backend-set only and adding a dedicated rejection helper that refuses
    evidence payload keys such as `authority_state`, `mfa_level`, `auth_level`, `session_authority`,
    and `access_granted` (D-08 through D-11, especially D-09). Preserve the D-30 Phoenix session
    lesson that backend session authority is durable truth and the D-31 Guardian/JWT lesson that
    token presence alone is not enough for high-risk activation; the contracts must model level and
    recency as first-class checks. Do not introduce Ecto, changesets, or any real Sigra dependency.
  </action>
  <verify>
    <automated>cd /Users/jon/projects/crosswake && mix test test/crosswake/companions/sigra/contracts_test.exs -x</automated>
  </verify>
  <acceptance_criteria>
    - `lib/crosswake/companions/sigra/contracts.ex` defines `Crosswake.Companions.Sigra.Contracts`
    - The module exposes plain-struct contracts plus `new_*` and `validate_*` functions returning `{:ok, struct}` or `{:error, keyword()}`
    - The module exposes a closed MFA vocabulary helper and a comparison helper that later RouteGate code can call directly
    - Evidence validation rejects the authority keys named in D-09
    - The implementation does not use `Ecto.Schema`, changesets, or a real Sigra dependency
    - `mix test test/crosswake/companions/sigra/contracts_test.exs -x` exits 0
  </acceptance_criteria>
  <done>AUTH-01 is satisfied: the Sigra contract layer exists, authority/evidence boundaries are enforced in code, and focused unit tests are green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| device/client auth signal -> evidence lane | Untrusted client/device observations may be supplied as evidence but cannot mutate backend authority |
| backend projection -> AuthContext / SessionAuthorityLane | Trusted backend-owned route-activation facts cross into Crosswake's typed contract layer |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-46-01 | Spoofing / Elevation | `Crosswake.Companions.Sigra.Contracts` evidence surface | mitigate | Reject `authority_state`, `mfa_level`, `auth_level`, `session_authority`, and `access_granted` in evidence validators per D-08 through D-11; green AUTH-01 tests prove device/client signals stay evidence-only. |
| T-46-02 | Information Disclosure | `StepUpChallenge` and auth structs | mitigate | Keep `StepUpChallenge` reference-only and forbid bearer tokens, passkey material, OAuth artifacts, refresh tokens, or PII-heavy actor metadata in the contract surface per D-07, D-21, D-32, and D-33. |
| T-46-03 | Tampering | MFA/recency comparison logic | mitigate | Centralize closed vocabulary helpers (`mfa_level_vocabulary/0`, comparison helper, auth-age normalization helper) in the contract module so later plans do not reimplement ordering ad hoc. |
| T-46-SC | Tampering | npm/pip/cargo installs | accept | No package-manager installs in Phase 46; pure in-tree Elixir changes only. |
</threat_model>

<verification>
- `cd /Users/jon/projects/crosswake && mix test test/crosswake/companions/sigra/contracts_test.exs -x` exits 0
- `cd /Users/jon/projects/crosswake && mix compile --warnings-as-errors` exits 0 after the contract module lands
</verification>

<success_criteria>
- `Crosswake.Companions.Sigra.Contracts` exists with typed `AuthContext`, `SessionAuthorityLane`, `StepUpChallenge`, and evidence-only state
- MFA ordering and auth-age normalization are centralized in the contract module for downstream RouteGate checks
- Evidence cannot set authority fields at the contract boundary
- AUTH-01 unit coverage is present and green
</success_criteria>

<output>
Create `.planning/phases/46-sigra-auth-contract-only-slice/46-01-SUMMARY.md` when done
</output>
