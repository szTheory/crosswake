---
phase: 46-sigra-auth-contract-only-slice
plan: 04
type: execute
wave: 4
depends_on: ["46-03"]
files_modified:
  - lib/crosswake/doctor/doctor.ex
  - lib/crosswake/support_matrix/support_matrix.ex
  - test/crosswake/doctor/doctor_test.exs
  - test/crosswake/support_matrix/support_matrix_test.exs
  - test/crosswake/proof/phase46_sigra_auth_contract_test.exs
autonomous: true
requirements: [AUTH-02]

must_haves:
  truths:
    - "`mix crosswake.doctor` reports auth-predicated routes with exact route id, `auth_min_level`, and `requires_recent_auth`."
    - "Doctor output makes the contract-only posture explicit without claiming Sigra handoff, step-up execution, passkeys, OAuth, or refresh-token machinery."
    - "SupportMatrix exposes a canonical Sigra auth contract truth row with owner backend_seam, package_class companion, proof_class merge_blocking, and fallback :step_up_required."
    - "Doctor and support truth surfaces never emit token/PII/passkey/OAuth artifacts."
    - "D-27: the hermetic Phase 46 proof covers constructors/validators, evidence authority rejection, route-policy validation, manifest serialization, RouteGate absent/weak/stale `:step_up_required`, and doctor/support auth route truth."
  artifacts:
    - path: "lib/crosswake/doctor/doctor.ex"
      provides: "Phase 46 auth findings wired into Doctor.run/1"
      contains: "phase_46_auth_findings"
    - path: "lib/crosswake/support_matrix/support_matrix.ex"
      provides: "canonical Sigra auth support-truth accessor"
      contains: "auth_contract_truth"
    - path: "test/crosswake/proof/phase46_sigra_auth_contract_test.exs"
      provides: "Hermetic proof assertions for doctor and support truth"
      contains: "auth.route_predicated"
  key_links:
    - from: "phase_46_auth_findings/1"
      to: "manifest.routes"
      via: "auth predicate inspection"
      pattern: "auth_min_level|requires_recent_auth"
    - from: "SupportMatrix.auth_contract_truth/0"
      to: "Phase46 proof and Phase47 docs-contract vocabulary"
      via: "stable canonical row keys and fallback code"
      pattern: "step_up_required"
---

<objective>
Close Phase 46 by surfacing auth truth to operators: doctor findings for auth-predicated routes and
a canonical support-matrix row for the Sigra auth contract surface, both honest about the
contract-only scope and both covered by hermetic proof.

Purpose: Crosswake treats diagnostics and support truth as product surface. AUTH-02 is not complete
until `mix crosswake.doctor` and `Crosswake.SupportMatrix` make the auth posture visible without
implying machinery that has been deferred to v3.6.

Output: auth doctor findings, canonical support-matrix auth truth, focused doctor/support tests, and
final phase46 proof coverage.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/MILESTONE-ARC.md
@.planning/phases/46-sigra-auth-contract-only-slice/46-CONTEXT.md
@.planning/phases/46-sigra-auth-contract-only-slice/46-RESEARCH.md
@.planning/phases/46-sigra-auth-contract-only-slice/46-VALIDATION.md
@.planning/phases/46-sigra-auth-contract-only-slice/46-PATTERNS.md
@.planning/phases/46-sigra-auth-contract-only-slice/46-03-SUMMARY.md

<interfaces>
From `lib/crosswake/doctor/doctor.ex` and `lib/crosswake/support_matrix/support_matrix.ex`:

```elixir
def run(opts \\ []) do
  phase_38_findings = phase_38_companion_seam_findings()
  phase_41_findings = phase_41_gating_findings(manifest)
  ...
end

@spec gating_truth_label() :: String.t()
def gating_truth_label, do: "Runtime Gate State — not build-proof posture"

@spec gating_truth() :: [map()]
def gating_truth do
  ...
end
```

Use the same additive pattern for a Phase 46 auth finding function and a canonical
support-matrix auth accessor. Keep these surfaces operator-facing and data-only.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add auth doctor findings and prove human/JSON parity</name>
  <files>lib/crosswake/doctor/doctor.ex, test/crosswake/doctor/doctor_test.exs, test/crosswake/proof/phase46_sigra_auth_contract_test.exs</files>
  <read_first>
    - lib/crosswake/doctor/doctor.ex (run/1 accumulator, `phase_41_gating_findings/1`, and `check/6` helper pattern)
    - test/crosswake/doctor/doctor_test.exs (route-scoped doctor assertion style and formatter parity checks)
    - test/crosswake/proof/phase41_gating_doctor_test.exs (per-route product-truth proof pattern)
    - .planning/phases/46-sigra-auth-contract-only-slice/46-CONTEXT.md (D-23 through D-28)
    - .planning/phases/46-sigra-auth-contract-only-slice/46-RESEARCH.md (Pitfall 3: doctor/support truth lagging code)
  </read_first>
  <behavior>
    - Test 1: every route declaring auth predicates emits an `auth.route_predicated` advisory with route_id and exact predicate values.
    - Test 2: doctor output includes an explicit contract-only note such as `auth.step_up_required_contract` without claiming Sigra machinery.
    - Test 3: human and JSON doctor outputs omit tokens, PII, passkeys, and OAuth artifacts.
  </behavior>
  <action>
    Extend `Crosswake.Doctor` per D-23 through D-28. Add a private
    `phase_46_auth_findings/1` function following the Phase 41 pattern and wire it into `run/1`
    after the existing phase-specific finding families. For every manifest route where either
    `auth_min_level` or `requires_recent_auth` is set, emit an `:advisory` finding with code
    `auth.route_predicated` and route-scoped check name containing the exact route id plus the
    declared predicate values (D-23, D-24). When any auth-predicated routes exist, also emit a
    stable auth-specific contract-only finding code such as `auth.step_up_required_contract` that
    states Crosswake enforces fail-closed route denial only and does not ship handoff, ceremony,
    passkey, OAuth, or refresh-token machinery in Phase 46 (D-24, D-26, D-28). Keep details
    minimal and non-sensitive: route id, `auth_min_level`, `requires_recent_auth`, and fallback
    `:step_up_required` vocabulary only. Do not surface actor ids, session ids, challenge contents,
    token refs, passkey artifacts, or OAuth data in doctor findings (D-21). Extend
    `test/crosswake/doctor/doctor_test.exs` first to prove both human and JSON formatters expose the
    auth findings correctly, then update `phase46_sigra_auth_contract_test.exs` with D-27 hermetic
    proof assertions that the codes exist in a compiled manifest run and that doctor truth lists
    auth-predicated routes.
  </action>
  <verify>
    <automated>cd /Users/jon/projects/crosswake && mix test test/crosswake/doctor/doctor_test.exs test/crosswake/proof/phase46_sigra_auth_contract_test.exs -x</automated>
  </verify>
  <acceptance_criteria>
    - `doctor.ex` defines and wires `phase_46_auth_findings/1`
    - Per-route `auth.route_predicated` findings include route id and declared predicate values
    - Doctor output includes a stable contract-only auth finding code such as `auth.step_up_required_contract`
    - No new doctor finding details include tokens, passkeys, OAuth artifacts, or PII-heavy actor/session data
    - `test/crosswake/doctor/doctor_test.exs` proves both human and JSON parity for the new auth findings
    - `mix test test/crosswake/doctor/doctor_test.exs test/crosswake/proof/phase46_sigra_auth_contract_test.exs -x` exits 0
  </acceptance_criteria>
  <done>Doctor truth is complete: auth-predicated routes are visible, contract-only posture is explicit, and formatter parity is proven.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add canonical support-matrix auth truth and close the phase proof</name>
  <files>lib/crosswake/support_matrix/support_matrix.ex, test/crosswake/support_matrix/support_matrix_test.exs, test/crosswake/proof/phase46_sigra_auth_contract_test.exs</files>
  <read_first>
    - lib/crosswake/support_matrix/support_matrix.ex (accessor style for `gating_truth/0`, `gating_truth_label/0`, and commerce support truth)
    - test/crosswake/support_matrix/support_matrix_test.exs (canonical truth + renderer parity assertion style)
    - test/crosswake/proof/phase46_sigra_auth_contract_test.exs (extend the existing proof file rather than creating a second phase proof)
    - .planning/phases/46-sigra-auth-contract-only-slice/46-CONTEXT.md (D-25 through D-28)
    - .planning/phases/46-sigra-auth-contract-only-slice/46-RESEARCH.md (product-truth parity requirement)
  </read_first>
  <behavior>
    - Test 1: `SupportMatrix.auth_contract_truth/0` (or equivalent named accessor) returns a canonical row with owner backend_seam, package_class companion, proof_class merge_blocking, and fallback step_up_required.
    - Test 2: the row explicitly states contract-only posture and excludes Sigra machinery claims.
    - Test 3: the phase proof asserts doctor codes, support truth, and denial vocabulary share stable auth terms for the Phase 47 docs-contract handoff.
  </behavior>
  <action>
    Add canonical support truth per D-25 through D-28. In `Crosswake.SupportMatrix`, introduce a
    dedicated auth accessor such as `auth_contract_truth/0` returning one or more canonical rows/maps
    for the Sigra auth contract surface. The canonical row must carry owner `:backend_seam`,
    package class `:companion`, proof class `:merge_blocking`, and fallback `:step_up_required`
    (D-25). Include explicit contract-only wording that the shipped surface is typed auth context +
    route predicate enforcement only; do not add a broader `auth_summary` operational surface in
    this phase (D-26). Choose stable field names and strings because Phase 47 will docs-contract
    them (D-28). Extend `test/crosswake/support_matrix/support_matrix_test.exs` with exact-key and
    exact-value assertions for the auth row, including the non-goal wording. Then finish
    `phase46_sigra_auth_contract_test.exs` with a final proof that doctor findings, support truth,
    and `:step_up_required` denial vocabulary align on the stable auth terms `auth_min_level`,
    `requires_recent_auth`, `AuthContext`, `SessionAuthorityLane`, and `step_up_required` as phase
    handoff truth. This final proof must close the D-27 list across all Phase 46 surfaces: contract
    constructors/validators, evidence authority rejection, route-policy validation, manifest
    serialization, absent/weak/stale RouteGate denial, and doctor/support truth. Do not claim
    handoff, OAuth, passkeys, or refresh rotation anywhere in the row or proof outputs.
  </action>
  <verify>
    <automated>cd /Users/jon/projects/crosswake && mix test test/crosswake/support_matrix/support_matrix_test.exs test/crosswake/proof/phase46_sigra_auth_contract_test.exs -x</automated>
  </verify>
  <acceptance_criteria>
    - `support_matrix.ex` exposes a canonical auth support accessor such as `auth_contract_truth/0`
    - The auth row includes owner `:backend_seam`, package class `:companion`, proof class `:merge_blocking`, and fallback `:step_up_required`
    - The row explicitly states contract-only posture and excludes claims about handoff/passkey/OAuth/refresh-token machinery
    - `support_matrix_test.exs` asserts exact keys/values for the auth truth row
    - `phase46_sigra_auth_contract_test.exs` proves stable auth vocabulary alignment across doctor/support/denial surfaces
    - `mix test test/crosswake/support_matrix/support_matrix_test.exs test/crosswake/proof/phase46_sigra_auth_contract_test.exs -x` exits 0
  </acceptance_criteria>
  <done>Support truth is complete: Crosswake exposes a canonical Sigra auth contract row, the phase proof covers it, and the vocabulary is stable for Phase 47 docs-contract work.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| manifest route auth predicates -> doctor findings | Compiled route metadata is rendered into operator-facing diagnostic output |
| canonical auth support truth -> tests/docs handoff | Stable support-matrix vocabulary becomes public truth consumed by later docs-contract checks |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-46-08 | Information Disclosure | `Doctor` auth findings | mitigate | Restrict findings to route id, declared predicates, and contract-only posture; never include tokens, passkeys, OAuth artifacts, or PII-heavy actor/session data per D-21. |
| T-46-09 | Repudiation / Integrity | `SupportMatrix` auth truth overstating shipped capability | mitigate | Canonical auth row must explicitly say contract-only/fail-closed route gate posture and must not claim handoff, ceremony, passkey, OAuth, or refresh-token machinery per D-25 and D-26. |
| T-46-10 | Tampering | vocabulary drift across doctor, support truth, and future docs | mitigate | Lock stable codes/accessor names (`auth.route_predicated`, `auth.step_up_required_contract`, `auth_contract_truth/0`, `:step_up_required`) in tests and phase proof so Phase 47 docs-contract has fixed anchors. |
| T-46-SC | Tampering | npm/pip/cargo installs | accept | No package-manager installs in Phase 46; pure in-tree Elixir changes only. |
</threat_model>

<verification>
- `cd /Users/jon/projects/crosswake && mix test test/crosswake/doctor/doctor_test.exs test/crosswake/support_matrix/support_matrix_test.exs test/crosswake/proof/phase46_sigra_auth_contract_test.exs -x` exits 0
- `cd /Users/jon/projects/crosswake && mix compile --warnings-as-errors` exits 0
- `cd /Users/jon/projects/crosswake && mix test` is the phase gate before verify-work
</verification>

<success_criteria>
- `mix crosswake.doctor` surfaces auth-predicated routes and contract-only auth posture with stable codes
- `Crosswake.SupportMatrix` exposes canonical Sigra auth truth with backend_seam ownership and `:step_up_required` fallback
- No doctor/support output claims unshipped Sigra machinery or leaks sensitive auth artifacts
- The phase46 proof covers D-27 end to end: contract validators, evidence authority rejection, policy validation, manifest serialization, RouteGate denials, and doctor/support truth
</success_criteria>

<output>
Create `.planning/phases/46-sigra-auth-contract-only-slice/46-04-SUMMARY.md` when done
</output>
