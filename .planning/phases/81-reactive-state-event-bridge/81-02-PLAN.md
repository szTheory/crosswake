---
phase: 81-reactive-state-event-bridge
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/crosswake-shell-core-ios/Sources/CrosswakeShellCore/CrosswakeShell.swift
  - packages/crosswake-shell-core-ios/Sources/CrosswakeShellCore/BridgeChannel.swift
  - packages/crosswake-shell-core-ios/Sources/CrosswakeShellCore/ConnectionState.swift
  - packages/crosswake-shell-core-ios/Sources/CrosswakeShellCore/ServerEvent.swift
  - examples/ios_shell_host/CrosswakeShell/CrosswakeShellApp.swift
autonomous: true
requirements: [STATE-01, STATE-02]
must_haves:
  truths:
    - "Native UI updates connection status (e.g., 'Connected') automatically when socket connects"
    - "Server-pushed toast event is displayed in native UI without manual polling"
    - "Native observers are correctly disposed of on view lifecycle changes"
  artifacts:
    - path: "packages/crosswake-shell-core-ios/Sources/CrosswakeShellCore/CrosswakeShell.swift"
      provides: "Reactive Combine publishers for iOS shell"
    - path: "examples/ios_shell_host/CrosswakeShell/CrosswakeShellApp.swift"
      provides: "SwiftUI bindings observing the new publishers"
  key_links:
    - from: "examples/ios_shell_host/CrosswakeShell/CrosswakeShellApp.swift"
      to: "packages/crosswake-shell-core-ios/Sources/CrosswakeShellCore/CrosswakeShell.swift"
      via: "Combine publishers"
      pattern: "onReceive"
---

<objective>
Implement the iOS reactive UI observers for shell state and server events, proving the new reactive observer pattern using Combine.

Purpose: Eliminate legacy callbacks and manual polling, adopting modern SwiftUI and Combine for the iOS bridge.
Output: Core iOS shell exposes connection state and server events via Combine, and the iOS demo app renders them cleanly.
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/81-reactive-state-event-bridge/81-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define Reactive Models & Combine Publishers in iOS Core</name>
  <files>packages/crosswake-shell-core-ios/Sources/CrosswakeShellCore/CrosswakeShell.swift, packages/crosswake-shell-core-ios/Sources/CrosswakeShellCore/BridgeChannel.swift, packages/crosswake-shell-core-ios/Sources/CrosswakeShellCore/ConnectionState.swift, packages/crosswake-shell-core-ios/Sources/CrosswakeShellCore/ServerEvent.swift</files>
  <action>
    Create `ConnectionState` (enum representing Connecting, Connected, Disconnected, Retrying) and `ServerEvent` struct.
    Update `CrosswakeShell` (which is an `ObservableObject`) to expose `@Published var connectionState` and `let serverEvents = PassthroughSubject&lt;ServerEvent, Never&gt;()`.
    Implement logic in `BridgeChannel` to emit server pushed events into `serverEvents` and update `connectionState` (e.g., tracking socket status or handling a server event push bridge command).
  </action>
  <verify>
    <automated>xcodebuild -scheme CrosswakeShell -project examples/ios_shell_host/CrosswakeShell.xcodeproj -destination 'platform=iOS Simulator,name=iPhone 15' build</automated>
  </verify>
  <done>Core iOS shell provides reactive publishers for connection state and server events</done>
</task>

<task type="auto">
  <name>Task 2: Update iOS Demo App UI to Observe Publishers</name>
  <files>examples/ios_shell_host/CrosswakeShell/CrosswakeShellApp.swift</files>
  <action>
    In `CrosswakeShellApp.swift` (inside `RootSceneView`), observe `shell.connectionState`.
    Render a subtle native indicator (e.g., a small banner above the `LiveViewContainerView` that hides when Connected) per D-01.
    Observe `shell.serverEvents` using `.onReceive(shell.serverEvents)` and render a Toast-equivalent native overlay per D-02.
    Ensure observer disposal is inherently tied to the SwiftUI view lifecycle (SwiftUI's `.onReceive` handles this automatically, or use `.onDisappear` if manual subscription is used) per D-03.
  </action>
  <verify>
    <automated>xcodebuild -scheme CrosswakeShell -project examples/ios_shell_host/CrosswakeShell.xcodeproj -destination 'platform=iOS Simulator,name=iPhone 15' build</automated>
  </verify>
  <done>Demo app successfully displays connection state and Toast overlays without manual polling</done>
</task>

</tasks>

<success_criteria>
- iOS app builds and runs.
- `CrosswakeShell` exposes `connectionState` and `serverEvents` natively via `Combine`.
- Native UI overlays the connection status and handles server-pushed toast messages correctly.
</success_criteria>

<output>
Create `.planning/phases/81-reactive-state-event-bridge/81-02-SUMMARY.md` when done
</output>
