---
phase: 81-reactive-state-event-bridge
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/crosswake-shell-core-android/src/main/java/dev/crosswake/shell/core/CrosswakeShell.kt
  - packages/crosswake-shell-core-android/src/main/java/dev/crosswake/shell/core/BridgeChannel.kt
  - packages/crosswake-shell-core-android/src/main/java/dev/crosswake/shell/core/ConnectionState.kt
  - packages/crosswake-shell-core-android/src/main/java/dev/crosswake/shell/core/ServerEvent.kt
  - examples/android_shell_host/app/src/main/java/dev/crosswake/shell/MainActivity.kt
autonomous: true
requirements: [STATE-01, STATE-02]
must_haves:
  truths:
    - "Native UI updates connection status (e.g., 'Connected') automatically when socket connects"
    - "Server-pushed toast event is displayed in native UI without manual polling"
    - "Native observers are correctly disposed of on view lifecycle changes"
  artifacts:
    - path: "packages/crosswake-shell-core-android/src/main/java/dev/crosswake/shell/core/CrosswakeShell.kt"
      provides: "Reactive state and event flows for Android shell"
    - path: "examples/android_shell_host/app/src/main/java/dev/crosswake/shell/MainActivity.kt"
      provides: "Compose UI observing the new flows"
  key_links:
    - from: "examples/android_shell_host/app/src/main/java/dev/crosswake/shell/MainActivity.kt"
      to: "packages/crosswake-shell-core-android/src/main/java/dev/crosswake/shell/core/CrosswakeShell.kt"
      via: "StateFlow collection"
      pattern: "collectAsState"
---

<objective>
Implement the Android reactive UI observers for shell state and server events, proving the new reactive observer pattern using StateFlow.

Purpose: Eliminate legacy callbacks and manual polling, adopting modern Compose and StateFlow for the Android bridge.
Output: Core Android shell exposes connection state and server events, and the Android demo app renders them cleanly.
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/81-reactive-state-event-bridge/81-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define Reactive Models & StateFlows in Android Core</name>
  <files>packages/crosswake-shell-core-android/src/main/java/dev/crosswake/shell/core/CrosswakeShell.kt, packages/crosswake-shell-core-android/src/main/java/dev/crosswake/shell/core/BridgeChannel.kt, packages/crosswake-shell-core-android/src/main/java/dev/crosswake/shell/core/ConnectionState.kt, packages/crosswake-shell-core-android/src/main/java/dev/crosswake/shell/core/ServerEvent.kt</files>
  <action>
    Create `ConnectionState` (enum or sealed class representing Connecting, Connected, Disconnected, Retrying) and `ServerEvent` data class.
    Update `CrosswakeShell` to expose `connectionState: StateFlow&lt;ConnectionState&gt;` and `serverEvents: SharedFlow&lt;ServerEvent&gt;`.
    Implement logic in `BridgeChannel` to emit server pushed events into `serverEvents` and update `connectionState` (e.g. tracking socket status or evaluating a server.event.push bridge command).
  </action>
  <verify>
    <automated>cd packages/crosswake-shell-core-android && ./gradlew compileDebugKotlin</automated>
  </verify>
  <done>Core Android shell provides reactive flows for connection state and server events</done>
</task>

<task type="auto">
  <name>Task 2: Update Android Demo App UI to Observe Flows</name>
  <files>examples/android_shell_host/app/src/main/java/dev/crosswake/shell/MainActivity.kt</files>
  <action>
    In `MainActivity.kt`, inside the `RenderPresentation` or `LiveViewScreen` Compose block, observe `shell.connectionState` using `collectAsState()`.
    Render a subtle native indicator (e.g., a small banner above the `LiveViewScreen` that hides when Connected) per D-01.
    Observe `shell.serverEvents` using a `LaunchedEffect` and render a standard native Snackbar using `SnackbarHostState` per D-02.
    Ensure observer disposal is strictly tied to the View lifecycle (`onDispose` via Compose effect APIs) per D-03.
  </action>
  <verify>
    <automated>cd examples/android_shell_host && ./gradlew app:assembleDebug</automated>
  </verify>
  <done>Demo app successfully displays connection state and Snackbar toasts without manual polling</done>
</task>

</tasks>

<success_criteria>
- Android app builds and runs.
- `CrosswakeShell` exposes `connectionState` and `serverEvents` natively via `StateFlow` and `SharedFlow`.
- Native UI overlays the connection status and handles server-pushed toast messages.
</success_criteria>

<output>
Create `.planning/phases/81-reactive-state-event-bridge/81-01-SUMMARY.md` when done
</output>
