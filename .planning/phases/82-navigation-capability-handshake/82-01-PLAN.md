---
phase: 82
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [
  "packages/crosswake-shell-core-ios/Sources/CrosswakeShellCore/CrosswakeShellConfig.swift",
  "packages/crosswake-shell-core-ios/Sources/CrosswakeShellCore/CrosswakeDelegates.swift",
  "packages/crosswake-shell-core-ios/Sources/CrosswakeShellCore/ActivationCoordinator.swift",
  "packages/crosswake-shell-core-ios/Sources/CrosswakeShellCore/RouteUnavailableView.swift",
  "packages/crosswake-shell-core-android/src/main/java/dev/crosswake/shell/core/CrosswakeShellConfig.kt",
  "packages/crosswake-shell-core-android/src/main/java/dev/crosswake/shell/core/CrosswakeDelegates.kt",
  "packages/crosswake-shell-core-android/src/main/java/dev/crosswake/shell/core/ActivationCoordinator.kt",
  "packages/crosswake-shell-core-android/src/main/java/dev/crosswake/shell/core/RouteUnavailableView.kt",
  "packages/crosswake-shell-core-android/src/test/java/dev/crosswake/shell/core/CrosswakeShellConfigTest.kt"
]
autonomous: true
requirements: [NAV-01, NAV-02]
must_haves:
  truths:
    - "System successfully fails-closed on invalid native routes"
    - "App initializes with native routes available"
  artifacts:
    - path: "packages/crosswake-shell-core-ios/Sources/CrosswakeShellCore/CrosswakeDelegates.swift"
      provides: "RouteDelegate definition"
    - path: "packages/crosswake-shell-core-ios/Sources/CrosswakeShellCore/CrosswakeShellConfig.swift"
      provides: "Route registry property"
  key_links:
    - from: "CrosswakeShellConfig"
      to: "RouteDelegate"
---

<objective>
Implement the Registry/Delegate pattern for route mapping in the shell core, and expose capability lists.

Purpose: Per D-01, host apps need a way to register native route mappings without embedding host-specific screens in the standalone shell. Per D-02, the shell needs a mechanism to expose these capabilities.
Output: RouteDelegate protocols and config registry in both Swift and Kotlin cores.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/82-navigation-capability-handshake/82-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add RouteDelegate and Registry to iOS Core</name>
  <files>
    packages/crosswake-shell-core-ios/Sources/CrosswakeShellCore/CrosswakeDelegates.swift,
    packages/crosswake-shell-core-ios/Sources/CrosswakeShellCore/CrosswakeShellConfig.swift,
    packages/crosswake-shell-core-ios/Sources/CrosswakeShellCore/ActivationCoordinator.swift,
    packages/crosswake-shell-core-ios/Sources/CrosswakeShellCore/RouteUnavailableView.swift
  </files>
  <action>
    Define `RouteDelegate` protocol in `CrosswakeDelegates.swift` with a method to handle a routeID.
    Update `CrosswakeShellConfig` to accept a `routeDelegate` property or a registry closure mapping route IDs to actions.
    Add explicit instructions to generate and expose the capability list within `CrosswakeShellConfig`.
    Update `ActivationCoordinator.swift` to invoke `RouteDelegate` when resolving a native route, and to return a `RouteUnavailableView` (defining it in `RouteUnavailableView.swift`) when a native route cannot be resolved (satisfies D-01 fail-closed UI).
    Per D-01, this ensures native screens aren't compiled into the core.
  </action>
  <verify>
    <automated>cd packages/crosswake-shell-core-ios && swift build</automated>
  </verify>
  <done>iOS core config can accept a route delegate mapping.</done>
</task>

<task type="auto">
  <name>Task 2: Add RouteDelegate and Registry to Android Core</name>
  <files>
    packages/crosswake-shell-core-android/src/main/java/dev/crosswake/shell/core/CrosswakeDelegates.kt,
    packages/crosswake-shell-core-android/src/main/java/dev/crosswake/shell/core/CrosswakeShellConfig.kt,
    packages/crosswake-shell-core-android/src/main/java/dev/crosswake/shell/core/ActivationCoordinator.kt,
    packages/crosswake-shell-core-android/src/test/java/dev/crosswake/shell/core/CrosswakeShellConfigTest.kt,
    packages/crosswake-shell-core-android/src/main/java/dev/crosswake/shell/core/RouteUnavailableView.kt
  </files>
  <action>
    Define `RouteDelegate` interface in `CrosswakeDelegates.kt` with a method to handle native routes.
    Update `CrosswakeShellConfig` to accept `routeDelegate`.
    Add explicit instructions to generate and expose the capability list within `CrosswakeShellConfig`.
    Update `ActivationCoordinator.kt` to invoke `RouteDelegate` when resolving a native route, and to return a `RouteUnavailableView` (defining it in `RouteUnavailableView.kt`) when a native route cannot be resolved (satisfies D-01 fail-closed UI).
    Write unit tests for the Android RouteDelegates and shell config in `CrosswakeShellConfigTest.kt`.
  </action>
  <verify>
    <automated>cd packages/crosswake-shell-core-android && ./gradlew test</automated>
  </verify>
  <done>Android core config can accept a route delegate mapping.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Shell -> Native UI | Route registry invokes unverified host code |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-82-01 | Tampering | Route Registry | mitigate | Validate route IDs against manifest before invoking delegate. |
</threat_model>

<verification>
Unit tests for config initialization with delegates pass.
</verification>

<success_criteria>
Core shell packages build with route delegate properties.
</success_criteria>

<output>
Create `.planning/phases/82-navigation-capability-handshake/82-01-SUMMARY.md` when done
</output>