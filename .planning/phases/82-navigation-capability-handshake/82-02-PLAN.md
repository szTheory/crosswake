---
phase: 82
plan: 02
type: execute
wave: 2
depends_on: ["82-01"]
files_modified: [
  "examples/ios_shell_host/CrosswakeShell/CrosswakeShellApp.swift",
  "examples/android_shell_host/app/src/main/java/dev/crosswake/shell/MainActivity.kt",
  "examples/ios_shell_host/CrosswakeShell/LiveViewContainerViewController.swift",
  "examples/android_shell_host/app/src/main/java/dev/crosswake/shell/LiveViewFragment.kt",
  "examples/phoenix_host/lib/crosswake_example/router.ex",
  "examples/phoenix_host/lib/crosswake_example/selective_native/on_mount.ex",
  "examples/ios_shell_host/CrosswakeShell/NativeCaptureView.swift",
  "examples/android_shell_host/app/src/main/java/dev/crosswake/shell/NativeCaptureActivity.kt",
  "examples/phoenix_host/assets/js/app.js",
  "examples/phoenix_host/test/crosswake_example/selective_native/claim_capture_live_test.exs"
]
autonomous: false
requirements: [NAV-01, NAV-02]
must_haves:
  truths:
    - "Native shell sends its capability list to the backend upon connection."
    - "User can navigate from a LiveView route to a native escape hatch screen."
  artifacts:
    - path: "examples/ios_shell_host/CrosswakeShell/CrosswakeShellApp.swift"
      provides: "Host configuration with route mapping"
  key_links:
    - from: "native capabilities"
      to: "LiveSocket params"
    - from: "LiveSocket params"
      to: "on_mount.ex"
---

<objective>
Wire the host apps to use the new route delegate, present the native capture route, and prove capability handshake.

Purpose: Fulfill NAV-01 and NAV-02 by demonstrating the full cycle: capabilities sent to Phoenix on connect (D-02), and navigating to a native screen which dismisses explicitly (D-03).
Output: Working capability detection in Phoenix and native routing in iOS/Android demo apps.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/82-navigation-capability-handshake/82-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Inject Capabilities into Web Context and Parse in Elixir</name>
  <files>
    examples/ios_shell_host/CrosswakeShell/LiveViewContainerViewController.swift,
    examples/android_shell_host/app/src/main/java/dev/crosswake/shell/LiveViewFragment.kt,
    examples/phoenix_host/lib/crosswake_example/router.ex,
    examples/phoenix_host/lib/crosswake_example/selective_native/on_mount.ex,
    examples/phoenix_host/assets/js/app.js
  </files>
  <action>
    In `LiveViewContainerViewController.swift` and `LiveViewFragment.kt`, inject the `capabilities` dictionary into the JavaScript environment so `window.crosswakeBridge` can pass it to the LiveSocket params.
    Modify `examples/phoenix_host/assets/js/app.js` to ensure the `capabilities` from `window.crosswakeBridge` are passed as params to `LiveSocket` initialization.
    In `on_mount.ex`, use `get_connect_params(socket)["capabilities"]` to read them and log/assign them (per D-02).
    In `router.ex`, wire the `on_mount` hook so that it is globally attached to LiveView routes.
  </action>
  <verify>
    <automated>cd examples/phoenix_host && mix test</automated>
  </verify>
  <done>Capabilities passed across the socket boundary.</done>
</task>

<task type="auto">
  <name>Task 2: Wire Native Escape Hatch in Host Apps</name>
  <files>
    examples/ios_shell_host/CrosswakeShell/CrosswakeShellApp.swift,
    examples/android_shell_host/app/src/main/java/dev/crosswake/shell/MainActivity.kt,
    examples/ios_shell_host/CrosswakeShell/NativeCaptureView.swift,
    examples/android_shell_host/app/src/main/java/dev/crosswake/shell/NativeCaptureActivity.kt,
    examples/phoenix_host/test/crosswake_example/selective_native/claim_capture_live_test.exs
  </files>
  <action>
    Implement the `RouteDelegate` in the iOS and Android host demo apps.
    Map the `capture` route ID to present the existing `NativeCaptureView` / `NativeCaptureActivity`.
    Implement native dismissal + explicit bridge event sending "route_return" (per D-03).
    Modify LiveView state to handle the `route_return` bridge event, proving the return flow.
  </action>
  <verify>
    <automated>cd examples/phoenix_host && mix test</automated>
  </verify>
  <done>Native routes wired up with explicit return events.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human Verification of End-to-End Handshake</name>
  <files>N/A</files>
  <action>Please manually verify the capability handshake and routing.</action>
  <what-built>Capability Handshake and Native Navigation wired end-to-end.</what-built>
  <how-to-verify>
    1. Start the Phoenix backend and native demo app.
    2. Verify backend console logs the registered native capabilities on socket connect.
    3. Tap a LiveView route configured to trigger the native capture escape hatch.
    4. Verify the native screen opens and dismisses correctly.
  </how-to-verify>
  <verify>
    <automated>echo "Manual verification task"</automated>
  </verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
  <done>Verification complete.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Web -> Shell | Bridge events triggered by LiveView |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-82-02 | Information Disclosure | Capability Handshake | mitigate | Validate capability list format in Elixir mount. |
</threat_model>

<verification>
End-to-end verification of capability printing and navigation.
</verification>

<success_criteria>
Phoenix detects capabilities and native route transitions flawlessly.
</success_criteria>

<output>
Create `.planning/phases/82-navigation-capability-handshake/82-02-SUMMARY.md` when done
</output>