---
phase: 106-collateral-integration-closeout
plan: 02
type: execute
wave: 2
depends_on: ["106-01"]
files_modified:
  - README.md
  - mix.exs
  - .github/workflows/brandbook-verify.yml
  - brandbook/collateral/README.md
autonomous: true
requirements: [COLL-02, COLL-03, COLL-04]
must_haves:
  truths:
    - "README opens with a GitHub `<picture>` block that swaps light/dark hero via absolute raw.githubusercontent.com URLs; the existing title/content is preserved below — D-05"
    - "ExDoc renders the production mark as its logo and `mix compile` still passes after the mix.exs edit — D-06, D-07"
    - "`mix hex.build` produces a tarball containing NO brandbook/ paths, with `:exclude_patterns` belt-and-suspenders in place — D-06"
    - "An advisory (non-merge-blocking) CI lane validates brandbook size budget, SVG structure, and token JSON on brandbook/** changes — D-08"
    - "Committed brandbook total size is <= 1MB including the new PNGs — D-09"
    - "The manual GitHub social-preview upload step is documented in a collateral README — D-10 (deferred-idea documentation)"
  artifacts:
    - path: "README.md"
      provides: "Picture header with absolute raw URLs, existing content intact"
      contains: "raw.githubusercontent.com/szTheory/crosswake/main/brandbook/collateral/"
    - path: "mix.exs"
      provides: "ExDoc logo + hex exclude_patterns"
      contains: "exclude_patterns"
    - path: ".github/workflows/brandbook-verify.yml"
      provides: "Advisory brandbook validation lane"
      contains: "brandbook/**"
    - path: "brandbook/collateral/README.md"
      provides: "Collateral index + manual social-preview upload instructions"
      contains: "Social preview"
  key_links:
    - from: "README.md"
      to: "brandbook/collateral/readme-header.svg"
      via: "absolute raw.githubusercontent.com <img>/<source> URL"
      pattern: "raw\\.githubusercontent\\.com/szTheory/crosswake/main/brandbook/collateral/readme-header"
    - from: "mix.exs docs()"
      to: "brandbook/logo/crosswake-mark.svg"
      via: "logo: key"
      pattern: "logo:"
    - from: ".github/workflows/brandbook-verify.yml"
      to: "brandbook/tools/check-production.mjs"
      via: "node step"
      pattern: "check-production"
---

<objective>
Wire the v9.0 brand into the repo surfaces and stand up the advisory CI lane and size gate: a `<picture>` README header (absolute raw URLs, light/dark), ExDoc `logo:` + hex `:exclude_patterns` in mix.exs (verified by a tarball assertion + green compile), an advisory `brandbook-verify.yml` lane, the final <=1MB brandbook size gate, and a collateral README documenting the manual GitHub social-preview upload.

Purpose: COLL-02, COLL-03, COLL-04 — close out v9.0 integration. Milestone UAT/closeout is handled by the orchestrator, not this plan (D-10).
Output: edited README.md + mix.exs, new brandbook-verify.yml, new brandbook/collateral/README.md.
</objective>

<executor_discipline>
To avoid context exhaustion / crashes:
- Commit after EACH task.
- For README.md and mix.exs, use the Edit tool with surgical edits — do NOT rewrite whole files and do NOT echo full file contents back.
- Run `mix hex.build` and `mix compile` and report only pass/fail + the relevant grep result, not full build logs.
- Keep `tar -tf` output filtered (grep), do not dump the entire tarball listing.
</executor_discipline>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/106-collateral-integration-closeout/106-CONTEXT.md
@.planning/phases/106-collateral-integration-closeout/106-01-SUMMARY.md

<read_first>
- README.md (first ~10 lines) — insert the <picture> block ABOVE the `# Crosswake` H1; keep everything below unchanged.
- mix.exs lines 69-122 — `package()` has `files: ~w(lib priv mix.exs README.md LICENSE CHANGELOG.md guides)` (strict allowlist, already excludes brandbook); `docs()` is the multi-key keyword list to add `logo:` to.
- .github/workflows/phase96-proof-advisory.yml — house advisory-lane conventions: `permissions: contents: read`, advisory framing comment, `actions/checkout` pinned by SHA. Match this style.
- brandbook/tools/check-production.mjs and check-candidates.mjs — the SVG structural validators the CI lane runs.
- brandbook/tools/compile-tokens.js + compile-tokens.test.mjs — token round-trip the CI lane runs (`node --test`).
</read_first>

<interfaces>
Constants (from mix.exs):
- @source_url = "https://github.com/szTheory/crosswake"
- Raw URL base for collateral: https://raw.githubusercontent.com/szTheory/crosswake/main/brandbook/collateral/

ExDoc logo target (committed production mark): brandbook/logo/crosswake-mark.svg

hex `:files` is ALREADY a strict allowlist (no brandbook). `:exclude_patterns` is belt-and-suspenders insurance, not the primary exclusion — both are required by D-06.

.gitignore ALREADY contains `/brandbook/tools/node_modules/` and `/brandbook/tools/fonts/` (lines 34-36) — VERIFY these are present; do NOT duplicate. COLL-04's ".gitignore additions" are largely already satisfied — confirm rather than re-add.

Committed brandbook size before this milestone's PNGs was 614KB; budget ceiling is 1048576 bytes (1MB).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire README picture header + ExDoc logo + hex exclude_patterns (with tarball + compile assertions)</name>
  <files>README.md, mix.exs</files>
  <read_first>README.md (first 10 lines), mix.exs lines 69-100</read_first>
  <action>
    README.md (D-05, COLL-02): Insert a GitHub `<picture>` block at the very top of README.md, ABOVE the `# Crosswake` heading. Structure:
    - `<source media="(prefers-color-scheme: dark)" srcset="https://raw.githubusercontent.com/szTheory/crosswake/main/brandbook/collateral/readme-header-dark.svg">`
    - `<img src="https://raw.githubusercontent.com/szTheory/crosswake/main/brandbook/collateral/readme-header.svg" alt="Crosswake">`
    Use ABSOLUTE raw.githubusercontent.com URLs on the `main` branch (works on GitHub AND hexdocs). Wrap in a centering `<p align="center">` if desired. Do NOT rewrite or reflow the rest of README — only prepend the header. Keep the existing `# Crosswake` title and all content below intact.

    mix.exs (D-06, D-07, COLL-03):
    - In `docs()`, add `logo: "brandbook/logo/crosswake-mark.svg"` (ExDoc ~0.38 supports SVG logos).
    - In `package()`, add `exclude_patterns: ["brandbook"]` as belt-and-suspenders alongside the existing strict `:files` allowlist (do NOT remove or weaken `:files`).

    Then PROVE the integration:
    - Run `mix compile` (or `mix compile --warnings-as-errors` if it currently passes) — must be green.
    - Run `mix hex.build`, locate the produced `crosswake-<version>.tar`, and assert via `tar -tf` that NO entry path contains `brandbook/`. (The contents are inside `contents.tar.gz` within the hex tarball — extract/inspect that inner archive: `tar -xOf crosswake-*.tar contents.tar.gz | tar -tzf - | grep -c brandbook` MUST be 0.)

    Do NOT echo full build logs — report compile pass/fail and the brandbook-count (must be 0).
  </action>
  <verify>
    <automated>grep -q 'raw.githubusercontent.com/szTheory/crosswake/main/brandbook/collateral/readme-header.svg' README.md && grep -q 'readme-header-dark.svg' README.md && grep -q 'prefers-color-scheme: dark' README.md && grep -q 'logo:' mix.exs && grep -q 'exclude_patterns' mix.exs && mix compile >/dev/null 2>&1 && rm -f crosswake-*.tar && mix hex.build >/dev/null 2>&1 && test "$(tar -xOf crosswake-*.tar contents.tar.gz | tar -tzf - | grep -c 'brandbook')" -eq 0 && echo OK</automated>
  </verify>
  <done>README starts with the picture block (absolute raw URLs, dark source + light img); mix.exs has logo: and exclude_patterns; mix compile green; hex tarball contains zero brandbook/ paths.</done>
  <acceptance_criteria>
    - README.md contains BOTH the dark `srcset` and the light `src` with the exact absolute raw.githubusercontent.com/szTheory/crosswake/main/brandbook/collateral/ prefix.
    - `prefers-color-scheme: dark` media present on the `<source>`.
    - Existing `# Crosswake` H1 and body content preserved (header prepended, not replacing).
    - mix.exs docs() has `logo: "brandbook/logo/crosswake-mark.svg"`; package() has `exclude_patterns` AND retains the strict `:files` allowlist.
    - `mix compile` exits 0.
    - `mix hex.build` succeeds and the inner contents archive lists ZERO paths containing `brandbook`.
  </acceptance_criteria>
  <commit>feat(106-02): wire README picture header + ExDoc logo + hex exclude (D-05/D-06/D-07)</commit>
</task>

<task type="auto">
  <name>Task 2: Add advisory brandbook-verify CI lane + collateral README + final size gate</name>
  <files>.github/workflows/brandbook-verify.yml, brandbook/collateral/README.md</files>
  <read_first>.github/workflows/phase96-proof-advisory.yml (advisory conventions), brandbook/tools/check-production.mjs, brandbook/tools/compile-tokens.test.mjs</read_first>
  <action>
    Create `.github/workflows/brandbook-verify.yml` (D-08, COLL-04) — an ADVISORY (non-merge-blocking) lane matching the house advisory style in phase96-proof-advisory.yml:
    - `permissions: contents: read`.
    - Triggers: `pull_request` and `push` filtered by `paths: ['brandbook/**']` (advisory feedback on brandbook changes) — but include the leading advisory framing comment that this lane is NOT a required merge gate. Do NOT add this workflow to any branch-protection required-contexts (it stays advisory by virtue of not being a required check). Pin `actions/checkout` and `actions/setup-node` by commit SHA like the sibling workflow pins checkout.
    - Jobs/steps:
      1. Size budget: `git ls-files brandbook | xargs ... ` sum of committed file bytes MUST be <= 1048576; fail the job if exceeded. (Use a portable byte-sum; on ubuntu-latest use `stat -c%s`.)
      2. SVG structural validation: `node brandbook/tools/check-production.mjs` and `node brandbook/tools/check-candidates.mjs`.
      3. Token JSON + round-trip: `node --test brandbook/tools/compile-tokens.test.mjs` (and contrast/gen-wordmark tests if cheap) — validates token JSON parses and compile round-trips.
    - Node version: pin a current LTS (e.g. 22.x).

    Create `brandbook/collateral/README.md` (D-10) — a short index of the collateral assets (one line each: what + dimensions) AND a "Social preview" section documenting the MANUAL GitHub step that cannot be automated from the repo: upload `social-card.png` at GitHub → repo Settings → General → Social preview. This documents the deferred manual step.

    FINAL size gate (D-09, COLL-04): run the same committed-size sum locally and assert <= 1048576 bytes including the new PNGs from 106-01.
  </action>
  <verify>
    <automated>test -f .github/workflows/brandbook-verify.yml && grep -q 'brandbook/\*\*' .github/workflows/brandbook-verify.yml && grep -q 'check-production' .github/workflows/brandbook-verify.yml && grep -qi 'contents: read' .github/workflows/brandbook-verify.yml && test -f brandbook/collateral/README.md && grep -qi 'social preview' brandbook/collateral/README.md && git add -A brandbook && test "$(git ls-files brandbook | xargs stat -f%z | awk '{s+=$1} END{print s}')" -le 1048576 && echo OK</automated>
  </verify>
  <done>brandbook-verify.yml exists (advisory, paths-filtered, runs size+SVG+token jobs); collateral README documents the manual social-preview upload; committed brandbook <= 1MB.</done>
  <acceptance_criteria>
    - brandbook-verify.yml has `permissions: contents: read`, a `paths: ['brandbook/**']` filter, and is framed as advisory (comment + not a required check).
    - Lane runs size-budget (<=1048576), check-production.mjs + check-candidates.mjs, and `node --test` on the token round-trip.
    - actions are SHA-pinned consistent with sibling workflows.
    - brandbook/collateral/README.md lists the assets and documents the manual GitHub Settings → Social preview upload of social-card.png.
    - Total committed brandbook size (incl. new PNGs) <= 1048576 bytes, asserted locally.
  </acceptance_criteria>
  <commit>ci(106-02): advisory brandbook-verify lane + collateral README + size gate (D-08/D-09/D-10)</commit>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo edit → hex package contents | mix.exs `:files`/`:exclude_patterns` control what ships to hex.pm consumers; a mistake could leak brandbook/ or drop required lib files |
| GitHub CI runner → repo | brandbook-verify.yml runs on PR/push; advisory only, read-only permissions |
| README raw URLs → GitHub/hexdocs renderers | absolute raw.githubusercontent URLs are fetched by remote renderers (no secrets, public assets) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-106-03 | Tampering | mix.exs edit breaking the hex package (dropping lib/ or shipping brandbook/) | mitigate | Task 1 asserts `mix compile` green AND `tar -tf` of the hex tarball inner archive has ZERO brandbook paths; `:files` allowlist retained, exclude_patterns added as insurance only |
| T-106-04 | Elevation of Privilege | brandbook-verify.yml gaining write/merge-gate power | mitigate | `permissions: contents: read`; advisory framing; not added to required status checks; paths-filtered to brandbook/** |
| T-106-05 | Tampering | unpinned third-party GitHub Actions | mitigate | Pin actions/checkout + actions/setup-node by commit SHA, matching phase96-proof-advisory.yml house style |
| T-106-SC | Tampering | npm/pip/cargo installs | mitigate | This plan adds ZERO new packages; CI lane runs existing brandbook/tools scripts via node only. No install tasks → no package legitimacy gate required |
</threat_model>

<verification>
- README.md opens with the `<picture>` block; both absolute raw URLs + dark media present; body preserved.
- mix.exs has `logo:` and `exclude_patterns`; `mix compile` green; hex tarball lists no brandbook/ paths.
- brandbook-verify.yml exists, advisory, paths-filtered, runs size + SVG + token jobs, SHA-pinned actions.
- brandbook/collateral/README.md documents the manual social-preview upload.
- Committed brandbook <= 1048576 bytes.
</verification>

<success_criteria>
- COLL-02: README header wired via absolute raw.githubusercontent URL with `<picture>` dark-mode handling.
- COLL-03: ExDoc `logo:` configured; hex tarball verified to exclude brandbook/ with `:exclude_patterns` belt-and-suspenders.
- COLL-04: advisory CI lane checks size budget + SVG validity + token JSON validity; committed brandbook < 1MB; .gitignore additions confirmed present.
</success_criteria>

<output>
Create `.planning/phases/106-collateral-integration-closeout/106-02-SUMMARY.md` when done. Record: README header wired (yes/no), mix compile result, hex tarball brandbook-count (0), CI lane created, final committed brandbook byte total. Keep it short; do not paste file contents.
</output>
