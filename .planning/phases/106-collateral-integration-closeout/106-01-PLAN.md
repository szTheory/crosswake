---
phase: 106-collateral-integration-closeout
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - brandbook/tools/export-raster.mjs
  - brandbook/collateral/readme-header.svg
  - brandbook/collateral/readme-header-dark.svg
  - brandbook/collateral/social-card.svg
  - brandbook/collateral/social-card.png
  - brandbook/collateral/favicon-32.png
  - brandbook/collateral/apple-touch-icon.png
autonomous: true
requirements: [COLL-01]
must_haves:
  truths:
    - "Maintainer has a light + dark README hero SVG pair that renders identically on GitHub (no external refs, literal hex fills) — D-01"
    - "A 1200x630 social card PNG exists under 150KB, exported from an SVG source, with key content inside the 1080x600 safe zone — D-02"
    - "favicon-32.png (32x32) and apple-touch-icon.png (180x180, opaque Foam 50 bg) are derived from the production favicon mark — D-03"
    - "Every collateral raster was browser-rendered and visually inspected before commit (PNGs READ) — D-04"
  artifacts:
    - path: "brandbook/collateral/readme-header.svg"
      provides: "Light-mode README hero (Current 950 #09141A art, transparent bg, path-only)"
      contains: "#09141A"
    - path: "brandbook/collateral/readme-header-dark.svg"
      provides: "Dark-mode README hero (Foam 50 #F7F1E6 art, transparent bg, path-only)"
      contains: "#F7F1E6"
    - path: "brandbook/collateral/social-card.svg"
      provides: "1200x630 OG/social card source"
      contains: "viewBox"
    - path: "brandbook/collateral/social-card.png"
      provides: "Exported 1200x630 PNG, <150KB"
    - path: "brandbook/collateral/favicon-32.png"
      provides: "32x32 favicon raster"
    - path: "brandbook/collateral/apple-touch-icon.png"
      provides: "180x180 opaque apple touch icon"
    - path: "brandbook/tools/export-raster.mjs"
      provides: "Exact-dimension Playwright PNG exporter (clip + deviceScaleFactor 1)"
      exports: ["(CLI script)"]
  key_links:
    - from: "brandbook/tools/export-raster.mjs"
      to: "examples/phoenix_host/node_modules/playwright"
      via: "createRequire resolve (same pattern as render-verify.mjs)"
      pattern: "playwright"
    - from: "brandbook/collateral/social-card.png"
      to: "brandbook/collateral/social-card.svg"
      via: "export-raster.mjs clip 1200x630"
      pattern: "social-card"
---

<objective>
Produce the v9.0 derivative collateral set in `brandbook/collateral/`: two README hero SVGs (light + dark), a social card (SVG source + 1200x630 PNG export), and two favicon rasters (32x32, 180x180). All rasters are exported via a small Playwright clip-screenshot tool at exact dimensions and visually inspected (READ) before commit.

Purpose: COLL-01 — ship the collateral that the integration plan (106-02) wires into README, ExDoc, and social previews.
Output: `brandbook/collateral/{readme-header.svg, readme-header-dark.svg, social-card.svg, social-card.png, favicon-32.png, apple-touch-icon.png}` + `brandbook/tools/export-raster.mjs`.
</objective>

<executor_discipline>
This is a long asset-authoring plan. To avoid context exhaustion / crashes:
- Commit after EACH task (the commit boundaries below are mandatory checkpoints).
- Do NOT echo full SVG/PNG file contents back into the conversation. Write files with the Write tool; confirm with `ls -l` and `magick identify`, not by printing.
- When you READ a rendered PNG for visual verification, READ it once, state pass/fail in one sentence, then move on. Do not re-Read.
- Keep tool calls small. Never paste base64 or binary into the conversation.
- If a render looks wrong, fix the SVG and re-export — do not narrate every coordinate.
</executor_discipline>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/106-collateral-integration-closeout/106-CONTEXT.md

<read_first>
- brandbook/logo/crosswake-lockup-horizontal.svg — light lockup, LITERAL #09141A fills, path-only wordmark + mark group. Copy this art into readme-header.svg / social-card.svg.
- brandbook/logo/crosswake-lockup-horizontal-dark.svg — dark lockup (Foam 50 art). Copy into readme-header-dark.svg.
- brandbook/logo/favicon.svg — 16-grid 2-line mark; uses an internal `@media (prefers-color-scheme)` style. For raster export you MUST force a single literal color (do NOT rely on the media query in the exported PNG).
- brandbook/tools/render-verify.mjs — Playwright loader pattern to copy: resolves chromium from `examples/phoenix_host/node_modules`, wraps SVG in HTML, screenshots. render-verify is fullPage + 300px box, so it is NOT suitable for exact-dimension export — write a dedicated clip exporter instead.
</read_first>

<interfaces>
Brand colors (from the lockup/favicon sources, use these literal hex values — `magick` cannot resolve `currentColor`):
- Current 950 (dark ink): #09141A
- Foam 50 (light/paper): #F7F1E6

Playwright resolution pattern (from render-verify.mjs):
```
const PW_MODULES = resolve(__dirname, '../../examples/phoenix_host/node_modules');
const { chromium } = require(resolve(PW_MODULES, 'playwright'));
```
Exact-dimension screenshot API:
```
const page = await browser.newPage({ viewport: { width, height }, deviceScaleFactor: 1 });
await page.screenshot({ path, clip: { x: 0, y: 0, width, height } });
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write the exact-dimension raster export tool</name>
  <files>brandbook/tools/export-raster.mjs</files>
  <read_first>brandbook/tools/render-verify.mjs (copy the Playwright resolution + SVG-to-HTML wrapping pattern)</read_first>
  <action>
    Create `brandbook/tools/export-raster.mjs`, a CLI that exports an SVG to a PNG at exact pixel dimensions. Reuse render-verify.mjs's Playwright-from-examples-host resolution. CLI signature: `node brandbook/tools/export-raster.mjs <input.svg> <output.png> <width> <height> [bgColor]`.

    Behavior:
    - Wrap the input SVG in a minimal HTML page where the SVG fills a div sized to exactly width x height (`<svg width=W height=H>` or CSS `width:Wpx;height:Hpx`), `margin:0`, no scrollbars.
    - If `bgColor` arg is provided, set it as the page/body background (opaque) — used for apple-touch-icon (Foam 50). If omitted, leave the page background transparent and pass `omitBackground: true` to the screenshot so transparent SVGs export with alpha.
    - Launch headless chromium, `newPage({ viewport: { width, height }, deviceScaleFactor: 1 })`, goto the file URL, screenshot with `clip: { x:0, y:0, width, height }`.
    - Print the output path and exit 0 on success, non-zero on error.

    Do NOT use `magick` for export (it cannot resolve currentColor/media-query fills reliably) — this Playwright tool is the canonical export path for the whole plan.
  </action>
  <verify>
    <automated>node brandbook/tools/export-raster.mjs brandbook/logo/crosswake-mark.svg /tmp/exp-smoke.png 64 64 "#F7F1E6" && magick identify -format "%wx%h" /tmp/exp-smoke.png | grep -qx "64x64" && echo OK</automated>
  </verify>
  <done>export-raster.mjs exists; smoke export produces a 64x64 PNG; tool exits 0.</done>
  <acceptance_criteria>
    - Output PNG dimensions exactly match the requested width x height (verified via `magick identify -format "%wx%h"`).
    - With a bgColor arg the PNG is opaque; without it the PNG preserves transparency (omitBackground).
    - Playwright is loaded from examples/phoenix_host/node_modules — zero new npm installs.
  </acceptance_criteria>
  <commit>chore(106-01): add exact-dimension raster export tool</commit>
</task>

<task type="auto">
  <name>Task 2: Author README hero SVGs (light + dark)</name>
  <files>brandbook/collateral/readme-header.svg, brandbook/collateral/readme-header-dark.svg</files>
  <read_first>brandbook/logo/crosswake-lockup-horizontal.svg, brandbook/logo/crosswake-lockup-horizontal-dark.svg</read_first>
  <action>
    Create `brandbook/collateral/readme-header.svg` (light: Current 950 #09141A art) and `brandbook/collateral/readme-header-dark.svg` (dark: Foam 50 #F7F1E6 art). Per D-01: a wide hero composition — horizontal lockup centered, with a whisper of wake-seam motif (a few sparse parallel seam lines echoing the mark's 20-degree wake slope), TRANSPARENT background. Understated (per the phase "specifics": lockup + a whisper of wake seams, NOT a billboard).

    Hard constraints (D-01, GitHub SVG sanitization per PITFALLS.md):
    - Path/line-only art reusing the lockup geometry from the source files; copy the inlined wordmark + mark paths directly (do not re-derive coordinates).
    - LITERAL hex fills only (#09141A for light, #F7F1E6 for dark) — NO `currentColor`, NO `<style>`, NO `@media` queries (GitHub strips them; light/dark is handled by the two-file `<picture>` pattern in 106-02, not in-SVG).
    - No external references (no `<image href>`, no remote fonts). Self-contained.
    - Transparent background (no full-bleed background rect).
    - Pick a wide viewBox suited to a README banner (e.g. ~1280x320 or proportional to the lockup) and center the lockup.

    Render-verify BOTH files before commit: export each to PNG on a contrasting backdrop and READ the PNG to confirm the lockup is centered, legible, and the wake seams read as a whisper not a billboard.
  </action>
  <verify>
    <automated>test -f brandbook/collateral/readme-header.svg && test -f brandbook/collateral/readme-header-dark.svg && grep -q "#09141A" brandbook/collateral/readme-header.svg && grep -q "#F7F1E6" brandbook/collateral/readme-header-dark.svg && ! grep -qi "@media\|<style\|currentColor\|href=\"http" brandbook/collateral/readme-header.svg && ! grep -qi "@media\|<style\|currentColor\|href=\"http" brandbook/collateral/readme-header-dark.svg && echo OK</automated>
    <human-check>Both header PNGs READ: lockup centered + legible, wake seams understated, correct color per theme.</human-check>
  </verify>
  <done>Both header SVGs exist, literal-hex, no media/style/currentColor/external refs, transparent bg; renders inspected.</done>
  <acceptance_criteria>
    - readme-header.svg contains #09141A and NOT #F7F1E6 art; readme-header-dark.svg contains #F7F1E6.
    - Neither file contains `@media`, `<style`, `currentColor`, or an `href="http` external reference.
    - No background rect spanning the full viewBox (transparent).
    - Export PNG of each READ and confirmed legible/centered before commit.
  </acceptance_criteria>
  <commit>feat(106-01): add light + dark README hero SVGs (D-01)</commit>
</task>

<task type="auto">
  <name>Task 3: Author social card + export all rasters (social card, favicons)</name>
  <files>brandbook/collateral/social-card.svg, brandbook/collateral/social-card.png, brandbook/collateral/favicon-32.png, brandbook/collateral/apple-touch-icon.png</files>
  <read_first>brandbook/logo/crosswake-lockup-horizontal-dark.svg, brandbook/logo/favicon.svg</read_first>
  <action>
    Author `brandbook/collateral/social-card.svg` at a 1200x630 viewBox (D-02): dark Current 950 (#09141A) full-bleed background, the Foam 50 (#F7F1E6) horizontal lockup, and a short tagline (use the README tagline voice, e.g. "Declare the crossing."). All key content — lockup + tagline — must sit inside the 1080x600 centered safe zone (60px horizontal / 15px vertical margins). LITERAL hex fills only, path-only, self-contained.

    Then export the three PNGs with `export-raster.mjs` (Task 1):
    - `social-card.png` = export social-card.svg at 1200x630 (no bgColor arg needed; the SVG itself paints the dark bg). Target < 150KB; hard ceiling 300KB (D-02/COLL-01). If over 150KB, the SVG is the source of truth — a flat-color card screenshots tiny; investigate before accepting.
    - `favicon-32.png` = export brandbook/logo/favicon.svg at 32x32. The favicon source uses a prefers-color-scheme media query; force the default dark-ink rendering (no bgColor → transparent, dark #09141A strokes). Confirm 32x32 + transparent.
    - `apple-touch-icon.png` = export brandbook/logo/favicon.svg at 180x180 WITH bgColor "#F7F1E6" (opaque Foam 50 — iOS does not composite transparency well, D-03).

    READ all three PNGs to visually confirm: social card lockup+tagline legible inside safe zone; favicon mark readable at 32px; apple-touch icon has an opaque foam background with the dark mark.
  </action>
  <verify>
    <automated>magick identify -format "%wx%h" brandbook/collateral/social-card.png | grep -qx "1200x630" && magick identify -format "%wx%h" brandbook/collateral/favicon-32.png | grep -qx "32x32" && magick identify -format "%wx%h" brandbook/collateral/apple-touch-icon.png | grep -qx "180x180" && test "$(stat -f%z brandbook/collateral/social-card.png)" -lt 153600 && echo OK</automated>
    <human-check>All three PNGs READ: social card content inside safe zone + legible; favicon readable at 32px; apple-touch icon opaque Foam 50 bg.</human-check>
  </verify>
  <done>social-card.svg + 3 PNGs exist at exact dims; social-card.png < 150KB; all visually verified.</done>
  <acceptance_criteria>
    - social-card.png is exactly 1200x630 and < 153600 bytes (150KB).
    - favicon-32.png is exactly 32x32; apple-touch-icon.png is exactly 180x180.
    - apple-touch-icon.png is opaque (Foam 50 background); favicon-32.png preserves transparency.
    - social-card.svg uses literal hex fills, path-only, no external refs; key content within 1080x600 safe zone.
    - All three PNGs READ and confirmed before commit.
  </acceptance_criteria>
  <commit>feat(106-01): add social card + favicon rasters (D-02, D-03)</commit>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local rasterizer (Playwright/Chromium) → committed PNG | Headless browser renders local file:// SVGs; output bytes are committed to the repo |
| brandbook tools → examples host node_modules | export-raster.mjs imports Playwright from an existing local tree (no new installs) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-106-01 | Tampering | SVG sources containing `<script>`/external `href` rendered by Chromium | mitigate | SVGs are path-only, literal-hex, self-contained; grep gate in Task 2 rejects `href="http"` / `<style` / `@media`; render is local file:// only, no network |
| T-106-02 | Information Disclosure | export-raster.mjs loading remote resources during render | accept | Inputs are local self-contained SVGs with no external refs; Chromium runs headless on local files; no secrets in scope |
| T-106-SC | Tampering | npm/pip/cargo installs | mitigate | This plan adds ZERO new packages — Playwright reused from examples/phoenix_host/node_modules (render-verify pattern). No install tasks → no package legitimacy gate required |
</threat_model>

<verification>
- All six collateral files + export-raster.mjs exist under their committed paths.
- `magick identify` confirms exact dimensions for all three PNGs (1200x630, 32x32, 180x180).
- social-card.png < 150KB.
- README hero SVGs pass the no-media/no-style/no-currentColor/no-external grep gate with correct theme colors.
- Each PNG was READ and visually confirmed.
</verification>

<success_criteria>
- COLL-01 collateral set complete in brandbook/collateral/: readme-header.svg, readme-header-dark.svg, social-card.svg, social-card.png (1200x630, <150KB), favicon-32.png (32x32), apple-touch-icon.png (180x180 opaque).
- export-raster.mjs is the reusable exact-dimension export path for downstream needs.
- Every raster visually verified (D-04).
</success_criteria>

<output>
Create `.planning/phases/106-collateral-integration-closeout/106-01-SUMMARY.md` when done. Keep it short: list the files created with their verified dimensions/sizes and one line per visual-verification result. Do NOT paste file contents into the summary.
</output>
