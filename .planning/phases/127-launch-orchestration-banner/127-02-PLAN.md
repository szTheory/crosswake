---
phase: 127-launch-orchestration-banner
plan: 02
type: execute
wave: 2
depends_on: ["127-01"]
files_modified:
  - lib/mix/tasks/crosswake.demo.ex
  - test/crosswake/guides/see_it_run_banner_test.exs
autonomous: true
requirements: [LAUNCH-01]
must_haves:
  truths:
    - "mix crosswake.demo shells out to bin/see-it-run.sh, streams its live output, forwards args verbatim, and propagates the script's exit status (D-01)"
    - "The banner drift test reads bin/see-it-run.sh as text, derives PORT from runtime.exs via the documented regex, and asserts the derived http://localhost:#{port}, the three routes, the verbatim native command literals, and the posture words appear in the script (D-21)"
    - "The drift test's synthetic anti-vacuity cases FAIL when the port is corrupted or a route is removed — proving the assertions are not vacuous (D-21)"
  artifacts:
    - path: "lib/mix/tasks/crosswake.demo.ex"
      provides: "Thin Mix.Task alias that System.cmds into bin/see-it-run.sh (no banner/boot logic)"
      contains: "use Mix.Task"
    - path: "test/crosswake/guides/see_it_run_banner_test.exs"
      provides: "Source-derived drift guard for the bin/see-it-run.sh banner string"
      contains: "Crosswake.Guides.SeeItRunBannerTest"
  key_links:
    - from: "lib/mix/tasks/crosswake.demo.ex"
      to: "bin/see-it-run.sh"
      via: "System.cmd into the canonical script, args forwarded verbatim (D-01)"
      pattern: "see-it-run.sh"
    - from: "test/crosswake/guides/see_it_run_banner_test.exs"
      to: "examples/phoenix_host/config/runtime.exs"
      via: "PORT derived via ~r/System\\.get_env\\(\"PORT\"\\)\\s*\\|\\|\\s*\"(\\d+)\"/ regex (D-21)"
      pattern: "System\\\\.get_env"
  prohibitions:
    - "Mix.Tasks.Crosswake.Demo MUST NOT contain any banner/boot/printf orchestration logic — it is a thin System.cmd alias only, carrying a '# logic lives in bin/see-it-run.sh' note (D-01)"
    - "The drift test MUST NOT hardcode 4700 — the port is derived from runtime.exs (D-21)"
    - "This test MUST NOT duplicate the Phase-128 guide-markdown assertions — it guards the banner string only (D-21 scope boundary)"
    - "MUST NOT call or extend Crosswake.Doctor (D-18 — scopes do not overlap)"
---

<objective>
Add the two thin Elixir companions to the canonical `bin/see-it-run.sh` launcher
(produced by plan 127-01): a `mix crosswake.demo` alias that shells out to the script,
and the `see_it_run_banner_test.exs` drift guard that keeps the banner's facts honest
and source-derived — the load-bearing validation artifact for this phase.

Purpose: satisfy LAUNCH-01's "optional `mix crosswake.demo` alias" and lock the banner
to its sources (port/routes/commands/posture) so it can never silently drift, mirroring
the house drift-test culture.

Output: `lib/mix/tasks/crosswake.demo.ex`, `test/crosswake/guides/see_it_run_banner_test.exs`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/127-launch-orchestration-banner/127-CONTEXT.md
@.planning/phases/127-launch-orchestration-banner/127-PATTERNS.md
@bin/see-it-run.sh
</context>

<artifacts_this_phase_produces>
This plan (127-02) produces:
- `Mix.Tasks.Crosswake.Demo` (`lib/mix/tasks/crosswake.demo.ex`) — thin alias
- `Crosswake.Guides.SeeItRunBannerTest` (`test/crosswake/guides/see_it_run_banner_test.exs`) — banner drift guard

Consumes (created by plan 127-01, do NOT create here): `bin/see-it-run.sh` and the `bin/` directory.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Thin mix crosswake.demo alias (System.cmd passthrough)</name>
  <files>lib/mix/tasks/crosswake.demo.ex</files>
  <read_first>
    - lib/mix/tasks/crosswake.contract.gen.ex (lines 1-6: `use Mix.Task`, `@shortdoc`, `@moduledoc`, `@impl Mix.Task def run(args)` scaffold)
    - lib/mix/tasks/crosswake.doctor.ex (lines 8-13: calm declarative `@shortdoc`/`@moduledoc` voice to mirror — but do NOT call Crosswake.Doctor)
    - bin/see-it-run.sh (the script this task shells into; flag surface it forwards)
  </read_first>
  <action>
Create `Mix.Tasks.Crosswake.Demo` with `use Mix.Task`, a calm `@shortdoc` (e.g. "Boot the shared demo backend and print the See It Run banner") and a `@moduledoc` that MUST carry the note `# logic lives in bin/see-it-run.sh` and state that this task is a thin alias adding no banner/boot logic (D-01).

`@impl Mix.Task def run(args)`: resolve the repo-root path to `bin/see-it-run.sh` (derive from `File.cwd!`/`__DIR__` so it works when invoked from the umbrella root; the script lives at repo-root `bin/see-it-run.sh`), then `System.cmd(script_path, args, into: IO.stream(:stdio, :line))` so the script's live output (the progress dots + banner) streams to the user's terminal. Forward `args` VERBATIM — do NOT `OptionParser.parse` (the shell owns all flag parsing; `--web-only`/`--build`/`--no-open`/`--ios`/`--android`/`--all`/`--help` pass straight through). Propagate the exit status: if the returned status is non-zero, `exit({:shutdown, status})` so a backend boot failure (exit 1) surfaces through `mix`.

No banner string, no docker logic, no curl, no toolchain checks live here — all of that is in `bin/see-it-run.sh`. Do NOT call or extend `Crosswake.Doctor` (D-18).
  </action>
  <verify>
    <automated>mix compile --warnings-as-errors 2>&1 | grep -vi warning; grep -q 'use Mix.Task' lib/mix/tasks/crosswake.demo.ex && grep -q 'see-it-run.sh' lib/mix/tasks/crosswake.demo.ex && grep -q 'System.cmd' lib/mix/tasks/crosswake.demo.ex && grep -q 'logic lives in bin/see-it-run.sh' lib/mix/tasks/crosswake.demo.ex && ! grep -q 'Crosswake.Doctor' lib/mix/tasks/crosswake.demo.ex</automated>
  </verify>
  <acceptance_criteria>
    - Module is `Mix.Tasks.Crosswake.Demo` with `use Mix.Task`, `@shortdoc`, `@moduledoc`
    - `@moduledoc` (or an adjacent comment) carries `# logic lives in bin/see-it-run.sh`
    - `run/1` calls `System.cmd(<resolved bin/see-it-run.sh path>, args, into: IO.stream(:stdio, :line))` and forwards `args` verbatim with no `OptionParser.parse`
    - Non-zero script status is propagated via `exit({:shutdown, status})`
    - Body contains NO printf/banner/docker/curl/toolchain logic; does NOT reference `Crosswake.Doctor`
    - `mix compile --warnings-as-errors` succeeds
  </acceptance_criteria>
  <done>`mix crosswake.demo [flags]` runs `bin/see-it-run.sh` with the same flags, streams its output, and exits with the script's status — a pure pass-through alias.</done>
</task>

<task type="auto">
  <name>Task 2: see_it_run_banner_test.exs source-derived banner drift guard with anti-vacuity cases</name>
  <files>test/crosswake/guides/see_it_run_banner_test.exs</files>
  <read_first>
    - test/crosswake/guides/quick_start_adoption_drift_test.exs (lines 1-7: module + `@..._path` + `use ExUnit.Case`; 36-80: the main scan test + the three synthetic anti-vacuity cases via `String.replace` + `assert_failure_category`; 388-435: `failure/3`, `format_failures/1`, `assert_no_drift_failures/1`, `assert_failure_category/2`, `source_port!/3` helpers; the `phoenix_host_port/0` + PORT regex `~r/System\.get_env\("PORT"\)\s*\|\|\s*"(\d+)"/`)
    - test/crosswake/guides/native_evidence_drift_test.exs (the replace-a-known-good-token-and-assert-it-fails anti-vacuity idiom)
    - bin/see-it-run.sh (the banner text under test — the file this test reads as a string)
    - examples/phoenix_host/config/runtime.exs (line 12: the canonical PORT source the test derives from)
  </read_first>
  <action>
Create `Crosswake.Guides.SeeItRunBannerTest` with `use ExUnit.Case, async: true`. Define `@banner_script_path "bin/see-it-run.sh"` and `@phoenix_config_path "examples/phoenix_host/config/runtime.exs"`. Copy the drift-test scaffolding from `quick_start_adoption_drift_test.exs` nearly verbatim, retargeting the read from QUICK_START.md to `bin/see-it-run.sh`. This proves banner-in-shell is fully drift-testable with zero Elixir runtime dependency (D-21).

Copy verbatim from the analog: `phoenix_host_port/0` (deriving PORT via `~r/System\.get_env\("PORT"\)\s*\|\|\s*"(\d+)"/` against `@phoenix_config_path`), `source_port!/3`, `require_contains/5`, `failure/3`, `format_failures/1`, `assert_no_drift_failures/1`, `assert_failure_category/2`.

Build a `scan_banner({path, contents})` (mirroring `scan_quick_start/1`) that returns a list of failures unless ALL of these appear in the banner contents:
- the derived URL `http://localhost:#{port}` (NEVER hardcode 4700 — port comes from `phoenix_host_port/0`)
- the three route paths: `/offline`, `/bridge-proof` (and the bare `/` home — assert via a `/` route line, not a substring that matches everything)
- iOS command literals: `-scheme Dev` and `Debug-Dev`
- Android command literals: `installDevDebug`, `adb shell am start -n dev.crosswake.shell.dev/.MainActivity`, `JAVA_HOME=/opt/homebrew/opt/openjdk@17`
- posture words: `advisory`, `simulator`, `emulator`, and a "proven"/"native build" phrase

Main test: `assert_no_drift_failures(scan_banner({@banner_script_path, File.read!(@banner_script_path)}))`.

Synthetic anti-vacuity cases (D-21 — the house idiom; these MUST fail to prove non-vacuity):
- (a) wrong port: `String.replace(banner, port, "4000")` -> `assert_failure_category(scan_banner({"synthetic/banner_wrong_port.sh", mutated}), :wrong_port)`
- (b) missing route: `String.replace(banner, "/bridge-proof", "/nope")` -> `assert_failure_category(..., :missing_route)`
- (c) missing posture: strip a posture word (e.g. `String.replace(~r/advisory/i, "optional")`) -> `assert_failure_category(..., :missing_native_label)`
Use distinct failure categories (`:wrong_port`, `:missing_route`, `:missing_command`, `:missing_native_label`) so each synthetic case asserts on a specific category.

Add a readability sanity test mirroring the analog's "source-derived facts are readable": assert `phoenix_host_port()` derives a `~r/^\d+$/` port and `File.exists?(@banner_script_path)`.

Scope boundary (D-21): this test guards the BANNER STRING ONLY. Do NOT assert anything about a `guides/see_it_run.md` markdown file — that is Phase 128's `see_it_run_test.exs`. No banner-string duplication across the two files.
  </action>
  <verify>
    <automated>mix test test/crosswake/guides/see_it_run_banner_test.exs 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - Module is `Crosswake.Guides.SeeItRunBannerTest` using `ExUnit.Case`
    - PORT is derived via `~r/System\.get_env\("PORT"\)\s*\|\|\s*"(\d+)"/` against `examples/phoenix_host/config/runtime.exs`; `4700` does NOT appear as a hardcoded literal in any assertion
    - Main test asserts the banner in `bin/see-it-run.sh` contains the derived `http://localhost:#{port}`, the routes `/offline` and `/bridge-proof`, the literals `-scheme Dev`, `Debug-Dev`, `installDevDebug`, `adb shell am start -n dev.crosswake.shell.dev/.MainActivity`, `JAVA_HOME=/opt/homebrew/opt/openjdk@17`, and the posture words `advisory`/`simulator`/`emulator` + a proven/native-build phrase
    - At least three synthetic anti-vacuity cases (wrong port, missing route, missing posture word) each assert a specific failure category and would FAIL the scan
    - No assertion references a `guides/see_it_run.md` markdown file (Phase-128 scope)
    - `mix test test/crosswake/guides/see_it_run_banner_test.exs` passes
  </acceptance_criteria>
  <done>The banner drift test passes green against the real `bin/see-it-run.sh`, and its synthetic cases prove a corrupted port or missing route would be caught — the banner can no longer silently drift from its sources.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| mix task -> shell | `mix crosswake.demo` shells out via `System.cmd` to `bin/see-it-run.sh` with developer-local privileges |
| test -> filesystem | the drift test reads `bin/see-it-run.sh` and `runtime.exs` as text (read-only) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-127-06 | Tampering/Elevation | `mix crosswake.demo` System.cmd | mitigate | invoke a fixed repo-relative `bin/see-it-run.sh` path (not a user-supplied program); forward args to the script as an args list (no shell string interpolation, no `eval`); the script itself owns its `set -euo pipefail` hardening (plan 127-01) |
| T-127-07 | Spoofing/Tampering | banner fact drift | mitigate | the drift test derives PORT from source and asserts routes/commands/posture against the actual file, with anti-vacuity synthetic cases — prevents a banner that silently misstates the port/routes/commands |
| T-127-08 | Repudiation | test vacuity | mitigate | synthetic wrong-port / missing-route / missing-posture cases assert specific failure categories, proving the guard is not a no-op |
| T-127-SC | Tampering | npm/pip/cargo installs | n/a | no package-manager installs in this plan; no legitimacy gate required |
</threat_model>

<verification>
- `mix compile --warnings-as-errors` succeeds with the new task present.
- `mix test test/crosswake/guides/see_it_run_banner_test.exs` passes (main + readability + anti-vacuity cases).
- `git status` shows only the two new files added — no change to bin/see-it-run.sh, runtime.exs, or any proof fixture.
</verification>

<success_criteria>
- LAUNCH-01's "optional `mix crosswake.demo` alias" is satisfied by a thin pass-through task.
- The banner is locked to its sources by a passing, non-vacuous drift test — the validation backbone called out for this phase.
</success_criteria>

<output>
Create `.planning/phases/127-launch-orchestration-banner/127-02-SUMMARY.md` when done.
</output>
