---
phase: 128-collateral-see-it-run-guide
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - guides/see_it_run.md
  - test/crosswake/guides/see_it_run_test.exs
  - mix.exs
autonomous: true
requirements: [DOCS-01, DOCS-03]
must_haves:
  truths:
    - "A reader opening guides/see_it_run.md meets a top-of-page gameplan blockquote with the single hero command bin/see-it-run.sh"
    - "The guide has the exact eight JTBD sections in D-08 order and links forward to examples/QUICK_START.md without duplicating its commands"
    - "The guide appears as item 2 (after README) in both the ExDoc extras list and the Start group"
    - "mix test test/crosswake/guides/see_it_run_test.exs passes on delivery (green, no File.exists? assertions on human-captured binaries)"
    - "The drift test derives port from runtime.exs and routes from router.ex, asserts the guide carries them, and rejects three synthetic mutations"
  artifacts:
    - path: "guides/see_it_run.md"
      provides: "The See It Run orientation guide (DOCS-01)"
      contains: "# See It Run"
      min_lines: 60
    - path: "test/crosswake/guides/see_it_run_test.exs"
      provides: "Source-derived guide drift test (DOCS-03)"
      contains: "Crosswake.Guides.SeeItRunTest"
      min_lines: 120
    - path: "mix.exs"
      provides: "ExDoc Start-group + extras registration of the guide (D-10)"
      contains: "guides/see_it_run.md"
  key_links:
    - from: "test/crosswake/guides/see_it_run_test.exs"
      to: "examples/phoenix_host/config/runtime.exs"
      via: "PORT regex derivation (shared with banner test, D-18 correct duplication)"
      pattern: "System\\.get_env\\(\"PORT\"\\)"
    - from: "test/crosswake/guides/see_it_run_test.exs"
      to: "guides/see_it_run.md"
      via: "@target reads + scans the guide markdown only"
      pattern: "guides/see_it_run\\.md"
    - from: "mix.exs"
      to: "guides/see_it_run.md"
      via: "extras + groups_for_extras Start group item 2"
      pattern: "guides/see_it_run\\.md"
---

<objective>
Author the `guides/see_it_run.md` orientation guide (DOCS-01), register it in the ExDoc
"Start" group as item 2 after README (D-10), and add the source-derived drift test
`test/crosswake/guides/see_it_run_test.exs` (DOCS-03) that stays GREEN on delivery (D-19).

Purpose: The guide is the reader-empathy gateway from README → "can I see it work?". The
test guards its truth (port, routes, hero command, links) against drift, mirroring the
house drift-test culture, with zero overlap against the Phase-127 banner test (D-18).

Output: `guides/see_it_run.md`, `test/crosswake/guides/see_it_run_test.exs`, mix.exs ExDoc
extras + Start-group edits.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/128-collateral-see-it-run-guide/128-CONTEXT.md
@.planning/phases/128-collateral-see-it-run-guide/128-UI-SPEC.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add the source-derived guide drift test (test-first, green-on-delivery)</name>
  <files>test/crosswake/guides/see_it_run_test.exs</files>
  <read_first>
    - test/crosswake/guides/quick_start_adoption_drift_test.exs (THE template — copy its per-file helpers: require_contains, require_regex, wrong_port_failures, documented_path_failures, failure, format_failures, assert_no_drift_failures, assert_failure_category, source_port!, lines, line_number)
    - test/crosswake/guides/see_it_run_banner_test.exs (the Phase-127 sibling to stay NON-overlapping with — D-18; note its @banner_script_path target and its banner-string literals like "-scheme Dev", "installDevDebug", "JAVA_HOME=/opt/homebrew/opt/openjdk@17", "proven native build", "    /              ")
    - examples/phoenix_host/config/runtime.exs (canonical PORT source — derive 4700 via the shared regex, never hardcode)
    - examples/phoenix_host/lib/crosswake_example/router.ex (canonical route source for /offline, /bridge-proof)
  </read_first>
  <behavior>
    - Test "source-derived facts are readable": phoenix_host_port() matches a numeric regex; File.exists?("guides/see_it_run.md") is true; File.exists?("examples/QUICK_START.md") is true; File.exists?("bin/see-it-run.sh") is true.
    - Test "guide contains source-derived facts": scan_guide returns [] (no drift). Asserts the guide contains: "http://localhost:#{port}" (port derived from runtime.exs); routes "/offline" and "/bridge-proof"; hero command "bin/see-it-run.sh"; "docker compose"; a link to "examples/QUICK_START.md"; native posture words "advisory", "simulator", "emulator"; and the canonical term "emulator evidence".
    - Test "scanner rejects wrong port" (:wrong_port): mutating localhost:#{port} -> localhost:4000 triggers wrong_port_failures.
    - Test "scanner rejects missing route" (:missing_route): replacing "/bridge-proof" -> "/nope" triggers a missing_route failure.
    - Test "scanner rejects missing native posture label" (:missing_native_label): replacing all "advisory" -> "optional" (and "emulator evidence" -> "emulator only") triggers a missing_native_label failure.
    - documented_path_failures (examples/, bin/, guides/ prefixes) catches broken internal links; wrong_port_failures line-scans every localhost:/PORT= occurrence.
  </behavior>
  <action>
    Create `test/crosswake/guides/see_it_run_test.exs` defining module `Crosswake.Guides.SeeItRunTest` with `use ExUnit.Case, async: true`. Set `@target_path "guides/see_it_run.md"`, `@quick_start_path "examples/QUICK_START.md"`, `@hero_command_path "bin/see-it-run.sh"`, `@phoenix_config_path "examples/phoenix_host/config/runtime.exs"`, `@router_path "examples/phoenix_host/lib/crosswake_example/router.ex"`.
    Replicate the per-file helper set from quick_start_adoption_drift_test.exs verbatim in idiom (the house style copies helpers per file — do NOT extract a shared module): `source_port!/3`, `phoenix_host_port/0` using regex `~r/System\.get_env\("PORT"\)\s*\|\|\s*"(\d+)"/`, `require_contains/5`, `require_regex/5`, `failure/3`, `format_failures/1`, `assert_no_drift_failures/1`, `assert_failure_category/2`, `lines/1`, `line_number/2`, `documented_path_failures/2`, `documented_paths/1` (extend the prefix regex to also match `bin/` and `guides/` in addition to `examples|script`), and `wrong_port_failures/3`.
    Implement `scan_guide/1` taking `{path, contents}` and returning a flat list of failures. Required assertions (each via require_contains with a distinct category): URL `http://localhost:#{port}` (:wrong_port); routes `/offline` and `/bridge-proof` (:missing_route); hero command `bin/see-it-run.sh` (:missing_command); `docker compose` (:missing_command); the QUICK_START link string `examples/QUICK_START.md` (:missing_path); native posture words `advisory`, `simulator`, `emulator` (:missing_native_label); the canonical term `emulator evidence` (:missing_native_label). Append `documented_path_failures(path, contents)` and `wrong_port_failures(path, contents, port)`.
    Write the four tests per the behavior block. D-19 HARD RULE: do NOT add any `File.exists?` assertion against the human-captured binaries (`ios-simulator.png`, `android-emulator.png`, `three-runtime-montage.png`, `see-it-run.gif`) — that guard is the DEFERRED sibling `see_it_run_collateral_test.exs`, out of this plan. The only `File.exists?` calls allowed here are for the guide, QUICK_START, and bin/see-it-run.sh (all exist or are created in this phase).
    D-18 HARD RULE: do NOT assert any banner-string literal that see_it_run_banner_test.exs owns: `-scheme Dev`, `Debug-Dev`, `installDevDebug`, `adb shell am start -n dev.crosswake.shell.dev/.MainActivity`, `JAVA_HOME=/opt/homebrew/opt/openjdk@17`, `proven native build`, or the whitespace-padded home-route line `    /              `. Deriving the port from runtime.exs is shared-source and correct (not duplication). Sharing the words `advisory`/`simulator`/`emulator` is also fine; the forbidden duplication is asserting the same banner COMMAND-LITERAL strings in both files.
    Comment-text discipline: this test negative-greps nothing literal that must be absent from the guide, so no allowlist markers needed. Keep helper docstrings free of stale port numbers.
  </action>
  <verify>
    <automated>mix test test/crosswake/guides/see_it_run_test.exs 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'Crosswake.Guides.SeeItRunTest' test/crosswake/guides/see_it_run_test.exs` returns 1
    - The test file does NOT contain `ios-simulator.png`, `android-emulator.png`, `three-runtime-montage.png`, or `see-it-run.gif` (D-19): `grep -E 'ios-simulator\.png|android-emulator\.png|three-runtime-montage\.png|see-it-run\.gif' test/crosswake/guides/see_it_run_test.exs` returns nothing
    - The test file does NOT contain banner-only literals (D-18): `grep -E '\-scheme Dev|installDevDebug|JAVA_HOME=/opt/homebrew|proven native build' test/crosswake/guides/see_it_run_test.exs` returns nothing
    - The port is derived, not hardcoded: `grep -c 'System.get_env("PORT")' test/crosswake/guides/see_it_run_test.exs` returns at least 1
    - After Task 2 completes, `mix test test/crosswake/guides/see_it_run_test.exs` exits 0 (green on delivery)
  </acceptance_criteria>
  <done>Test module exists with four tests (readable-facts, no-drift, plus three synthetic anti-vacuity cases for :wrong_port, :missing_route, :missing_native_label); no binary File.exists? assertions; no banner-literal overlap; port derived from runtime.exs. Test goes green once the guide exists (Task 2).</done>
</task>

<task type="auto">
  <name>Task 2: Author guides/see_it_run.md (gameplan blockquote + 8 JTBD sections)</name>
  <files>guides/see_it_run.md</files>
  <read_first>
    - .planning/phases/128-collateral-see-it-run-guide/128-UI-SPEC.md (Document Structure Contract — the exact 8-section order; Copywriting Contract — verbatim title/blockquote/section/label strings; Collateral Layout Contract — the raw.githubusercontent.com image URL pattern)
    - examples/QUICK_START.md (the doc this guide LINKS to, never duplicates — note section anchors: "First Run", "### Option A: One Command (Docker)" (renamed in plan 128-02), "## See The Route Owners", "### Run Against the Local Backend (Dev Wiring)")
    - guides/route_policy.md and guides/install.md and guides/adopter_profiles.md (house guide voice; the Go Deeper forward-nav targets)
    - guides/support_matrix.md (the #support-truth-label-legend anchor and the canonical "emulator evidence" term for D-06/D-15)
    - bin/see-it-run.sh (confirm the hero command path and that the banner footer already points here)
    - test/crosswake/guides/see_it_run_test.exs (the contract this guide must satisfy — every required string must be present)
  </read_first>
  <action>
    Create `guides/see_it_run.md` with H1 `# See It Run`, then the gameplan blockquote (D-07) using the UI-SPEC verbatim tagline: a `>` block declaring the one-sentence journey promise with honest scope ("…advisorily, without claiming device support"), the zero-toolchain caveat ("Works from Docker with no local Elixir toolchain."), the single hero command `bin/see-it-run.sh` in a fenced code block, "the banner prints your next steps", and the forward link `See [examples/QUICK_START.md](../examples/QUICK_START.md) for the full proof command reference.`
    Then the eight sections in EXACT D-08 / UI-SPEC order, each a clean exit point:
    1. `## What You'll See` — embed the montage + recording via `https://raw.githubusercontent.com/szTheory/crosswake/main/brandbook/collateral/see-it-run/three-runtime-montage.png` and `.../see-it-run.gif` using HTML `<img>` with the UI-SPEC alt text carrying `emulator evidence`; add the caption line `Advisory native evidence — simulator/emulator, not a physical device.` and a `> Advisory native collateral` blockquote BEFORE/ADJACENT to the native imagery (never after).
    2. `## Run It Now (Zero Toolchain)` — JTBD-A: the Docker one-command path to web; link forward to QUICK_START "Option A: One Command (Docker)"; mention `mix crosswake.demo` only as the note `If you have Elixir installed locally, mix crosswake.demo does the same thing.`
    3. `## Browse the Route Owners` — name `/`, `/offline`, `/bridge-proof` by owner label; include `http://localhost:4700` style URLs written with the port (the test scans for `http://localhost:<port>`); link forward to QUICK_START "See The Route Owners".
    4. `## Compare All Three Runtimes (Web, iOS, Android)` — the advisory-labeled comparison.
    5. `## Wire a Native Runtime to the Local Backend` — JTBD-C: reference Phase-126 dev wiring; quote the D-15 honest-label sentence VERBATIM ("A successful simulator or emulator run confirms the dev wiring reaches the local backend, but does not prove physical-device support. This is `emulator evidence` per the [support-truth label legend](support_matrix.md#support-truth-label-legend)."); link forward to QUICK_START "Run Against the Local Backend (Dev Wiring)" — do NOT copy the xcodebuild/gradlew commands.
    6. `## What This Proves (And What It Doesn't)` — echo (do not duplicate banner string literals) the proven-web / needs-native-build posture; "proven" reserved for web; native is `emulator evidence`.
    7. `## Go Deeper` — forward-only nav links to `examples/QUICK_START.md`, `install.md`, `route_policy.md`, `adopter_profiles.md`. Never link back to README (D-14 non-circular).
    Markdown authoring rules (UI-SPEC): `#` for title only; `##` for sections; `###` for sub-items; fenced code blocks for all commands; `>` blockquotes for gameplan + advisory callouts. Brand voice: calm, explicit, honest; no hype. The guide must contain every literal the test in Task 1 requires: `http://localhost:4700`, `/offline`, `/bridge-proof`, `bin/see-it-run.sh`, `docker compose`, `examples/QUICK_START.md`, `advisory`, `simulator`, `emulator`, `emulator evidence`.
    Use relative links to sibling guides (e.g. `support_matrix.md#...`) and `../examples/QUICK_START.md` for the cross-tree link, matching how ExDoc resolves guide-to-guide links.
  </action>
  <verify>
    <automated>mix test test/crosswake/guides/see_it_run_test.exs 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `head -1 guides/see_it_run.md` is `# See It Run`
    - All eight section headings present in order: `grep -n '^## ' guides/see_it_run.md` lists What You'll See, Run It Now (Zero Toolchain), Browse the Route Owners, Compare All Three Runtimes (Web, iOS, Android), Wire a Native Runtime to the Local Backend, What This Proves (And What It Doesn't), Go Deeper
    - Hero command present: `grep -c 'bin/see-it-run.sh' guides/see_it_run.md` returns at least 1
    - Honest labels present: `grep -c 'emulator evidence' guides/see_it_run.md` returns at least 1
    - Forward link present, no back-link to README: `grep -c 'examples/QUICK_START.md' guides/see_it_run.md` >= 1 AND `grep -c 'README.md' guides/see_it_run.md` returns 0
    - `mix test test/crosswake/guides/see_it_run_test.exs` exits 0
  </acceptance_criteria>
  <done>guides/see_it_run.md exists with the gameplan blockquote and all eight D-08 sections in order, satisfies every required string in the Task-1 drift test (which now passes), embeds collateral via raw.githubusercontent.com URLs with honest alt text, quotes the D-15 sentence verbatim, and links forward (never back to README).</done>
</task>

<task type="auto">
  <name>Task 3: Register the guide in ExDoc extras + Start group (item 2, after README)</name>
  <files>mix.exs</files>
  <read_first>
    - mix.exs (the `docs` function: `extras:` list starting "README.md" then "CHANGELOG.md"; `groups_for_extras:` `Start:` group = ["README.md", "guides/route_policy.md", "guides/install.md"])
    - .planning/phases/128-collateral-see-it-run-guide/128-UI-SPEC.md (ExDoc Integration Contract — guide goes at index 2 in both lists, right after README)
  </read_first>
  <action>
    In `mix.exs`, insert `"guides/see_it_run.md"` as item 2 of the `extras:` list — immediately after `"README.md"` and before `"CHANGELOG.md"` (D-10). Insert `"guides/see_it_run.md"` as item 2 of the `Start:` list inside `groups_for_extras:` — immediately after `"README.md"` and before `"guides/route_policy.md"`. Do NOT touch the package `files:`/`exclude_patterns:` lines (collateral stays in brandbook/, D-01) and do NOT alter any other extras entry or group.
  </action>
  <verify>
    <automated>mix docs 2>&1 | tail -15 && grep -n 'guides/see_it_run.md' mix.exs</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'guides/see_it_run.md' mix.exs` returns exactly 2 (extras + Start group)
    - In extras, the guide is the 2nd entry: the line after `"README.md"` in the extras list is `"guides/see_it_run.md"`
    - In the Start group, the guide is the 2nd entry: the line after `"README.md"` inside `Start:` is `"guides/see_it_run.md"`
    - `files:` and `exclude_patterns:` lines are unchanged: `grep -c 'exclude_patterns: \["brandbook"\]' mix.exs` returns 1
    - `mix docs` exits 0 and emits doc/see_it_run.html (the guide renders)
  </acceptance_criteria>
  <done>guides/see_it_run.md is item 2 in both the ExDoc extras list and the Start group; mix docs builds the guide page; the Hex package files/exclude lines are untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| docs → downstream `mix deps.get` | What ships in the Hex tarball; binaries must never enter `guides/` (D-01) |
| guide text → reader trust | Honest-label discipline; no native overclaim crossing into the guide |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-128-01 | Tampering | guides/see_it_run.md drift vs source (port/routes) | mitigate | source-derived drift test (Task 1) derives port from runtime.exs, scans every localhost:/PORT= line |
| T-128-02 | Information disclosure | native overclaim in guide prose | mitigate | test asserts `advisory`/`simulator`/`emulator`/`emulator evidence`; D-15 sentence quoted verbatim with legend anchor |
| T-128-03 | Tampering | binary leaks into Hex tarball | accept | Task 3 leaves `exclude_patterns: ["brandbook"]` and `files:` untouched; no binary touched in this plan |
| T-128-SC | Tampering | npm/pip/cargo installs | accept | no package installs in this plan (docs + Elixir test only); no audit table required |
</threat_model>

<verification>
- `mix test test/crosswake/guides/see_it_run_test.exs` exits 0 (green on delivery, D-19)
- `mix docs` builds and emits the see_it_run guide page in the Start group
- guides/see_it_run.md contains all eight D-08 sections and every test-required literal
- No banner-literal overlap with see_it_run_banner_test.exs (D-18)
</verification>

<success_criteria>
DOCS-01 satisfied (guide with gameplan + JTBD sections, in Start group after README, linking not duplicating QUICK_START) and DOCS-03 satisfied (source-derived drift test, green on delivery, three anti-vacuity cases, no binary-existence assertions).
</success_criteria>

<output>
Create `.planning/phases/128-collateral-see-it-run-guide/128-01-SUMMARY.md` when done
</output>
