---
status: complete
phase: 128-collateral-see-it-run-guide
source: [128-01-SUMMARY.md, 128-02-SUMMARY.md, 128-03-SUMMARY.md]
started: 2026-06-22T21:31:25Z
updated: 2026-06-22T21:55:00Z
verification: automated
---

## Current Test

[testing complete]

<!--
All six UAT checks were converted to automated tests (zero human verification).
Each check below names its automated owner. The drift tests follow the repo's
per-file house idiom (source-derived port, anti-vacuity mutation cases) and run
in the default `mix test` suite (executed by hex-page-proof.yml CI). Two CI
additions exercise the render + capture paths end-to-end. The native binary
capture stays human-gated (D-03) behind a tag-excluded deferred guard.

A real bug was found while automating Test 3: the README `<img>` montage tag used
smart quotes (src=”…”), which silently breaks image rendering. Fixed to straight
quotes; readme_see_it_run_test.exs now guards against the regression (the
:broken_image category).
-->

## Tests

### 1. See It Run guide renders in docs
expected: Guide is item 2 in ExDoc Start group (after README), gameplan blockquote, 7 ordered JTBD sections, native refs labeled "emulator evidence".
result: pass
automated_by: test/crosswake/guides/see_it_run_test.exs (structure + ExDoc-registration tests) and hex-page-proof.yml (`mix docs` + `test -f doc/see_it_run.html`)

### 2. Guide drift test passes
expected: see_it_run_test.exs reports 0 failures; port derived from runtime.exs (not hardcoded); guards port/route/label drift.
result: pass
automated_by: test/crosswake/guides/see_it_run_test.exs (10 tests, already in CI full suite)

### 3. README "See it run" section
expected: `## See it run` section ordered between "What this is not" and "Choose your path"; hero command; well-formed montage `<img>`; three route owners; source-derived port; advisory "emulator evidence" blockquote + legend anchor; forward-links to guide and QUICK_START.
result: pass
automated_by: test/crosswake/guides/readme_see_it_run_test.exs (8 tests). Found + fixed smart-quote img bug; :broken_image anti-vacuity case guards it.

### 4. QUICK_START routing + Option A rename
expected: "New here?" pointer to the guide; Option A renamed "One Command (Docker)" leading with bin/see-it-run.sh; docker compose demoted to fallback; "What This Does Not Prove" intact.
result: pass
automated_by: test/crosswake/guides/quick_start_adoption_drift_test.exs (5 tests, already in CI full suite)

### 5. Collateral capture harness runs
expected: bin/capture-collateral.sh exists/executable, passes bash -n; --web-only flag; seven filenames + rename mapping; native commands PRINTED not executed; "emulator evidence" labels. End-to-end web capture proven against live backend.
result: pass
automated_by: test/crosswake/guides/capture_collateral_test.exs (9 tests, static scan + bash -n) and offline-sync-e2e-gate.yml route-tour-proof job (runs `bin/capture-collateral.sh --web-only`, asserts renamed web PNGs)

### 6. Collateral asset table is honest
expected: seven-row table; web rows "Web proof"; native/montage rows "emulator evidence — advisory, not physical device"; raw.githubusercontent.com reference pattern.
result: pass
automated_by: test/crosswake/guides/collateral_table_test.exs (6 tests)

## Summary

total: 6
passed: 6
issues: 0
pending: 0
skipped: 0
blocked: 0

## Automation Notes

- New/extended drift tests run in the default `mix test` suite (hermetic, already
  in CI via hex-page-proof.yml): see_it_run_test.exs (+5), readme_see_it_run_test.exs,
  capture_collateral_test.exs, collateral_table_test.exs.
- CI additions: `test -f doc/see_it_run.html` in hex-page-proof.yml; a
  capture-collateral.sh `--web-only` end-to-end step in the route-tour-proof job.
- Deferred native guard: test/crosswake/guides/see_it_run_collateral_test.exs
  (`@tag :collateral_binaries`, excluded by default via test_helper.exs). Run once
  binaries land: `mix test --include collateral_binaries`.
- Native screenshot capture (iOS sim + Android emulator) stays human-gated (D-03)
  — the only check that genuinely needs a human; everything verifying the docs and
  harness are correct is now zero-human.
- Pre-existing, seed-dependent flaky failures in unrelated proof/docs-debt tests
  (HexPage, Phase48/52/69, ReleaseBoundaries, NativeEvidenceDrift, Manifest.Validator)
  are NOT introduced by this work — the failing set is identical with changes
  stashed and varies by run seed. Out of scope here.

## Gaps

[none — all six checks pass with automated owners]
