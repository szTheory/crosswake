---
phase: 21-reconciliation-example
plan: 02
type: execute
wave: 2
depends_on:
  - 21-01
files_modified:
  - guides/commerce.md
  - test/crosswake/guides/commerce_test.exs
  - examples/phoenix_host/README.md
autonomous: true
requirements:
  - RECN-01
  - RECN-02
  - RECN-03
user_setup: []
must_haves:
  truths:
    - "Guide language presents a minimal Phoenix-owned reconciliation inbox example for purchase, restore, webhook, and support evidence."
    - "Idempotency guidance is explicit about provider-aware dual keys (`event_key` and `subject_key`) and explicitly rejects transient device correlation IDs as identity authority."
    - "Projection guidance documents one authoritative snapshot and deterministic top-level state precedence (`stale`, `pending`, `denied`, `granted`) with monotonic ordering."
    - "Documentation keeps boundaries explicit: backend-owned authority, evidence non-authoritative, provider-neutral vocabulary, and example/docs-only posture."
  artifacts:
    - path: guides/commerce.md
      provides: "Canonical reconciliation example narrative, dual-key idempotency contract, and projection precedence guidance"
      contains: "## Minimal Reconciliation Inbox Example"
    - path: test/crosswake/guides/commerce_test.exs
      provides: "Docs-contract assertions for reconciliation example semantics and boundary fences"
      contains: "event_key"
    - path: examples/phoenix_host/README.md
      provides: "Example-host discoverability pointer to reconciliation example modules with explicit scope statement"
      contains: "reconciliation example"
  key_links:
    - from: guides/commerce.md
      to: test/crosswake/guides/commerce_test.exs
      via: "Literal guide wording for RECN semantics is locked by docs tests"
      pattern: "Minimal Reconciliation Inbox Example"
    - from: guides/commerce.md
      to: examples/phoenix_host/README.md
      via: "Example-host README references the same example/docs-only boundaries and module entry points"
      pattern: "example/docs-only"
---

<objective>
Publish Phase 21 reconciliation example guidance and lock it with docs-contract tests.

Purpose: Ensure host teams can follow the runnable example semantics from Plan 21-01 without ambiguity or boundary drift, satisfying RECN-01/02/03 in docs and testable copy.
Output: Updated commerce guide and example-host README plus expanded guide tests that lock source coverage, dual-key idempotency, projection precedence, and scope fences.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/21-reconciliation-example/21-CONTEXT.md
@.planning/phases/21-reconciliation-example/21-RESEARCH.md
@.planning/phases/21-reconciliation-example/21-PATTERNS.md
@.planning/phases/21-reconciliation-example/21-01-PLAN.md
@guides/commerce.md
@test/crosswake/guides/commerce_test.exs
@examples/phoenix_host/README.md
@examples/phoenix_host/lib/crosswake_example/commerce/reconciliation_keys.ex
@examples/phoenix_host/lib/crosswake_example/commerce/reconciliation_inbox.ex
@examples/phoenix_host/lib/crosswake_example/commerce/entitlement_projection.ex
</context>

<tasks>
<task type="auto">
  <name>Task 1: Add a minimal reconciliation inbox guide section tied to concrete evidence sources</name>
  <files>guides/commerce.md</files>
  <read_first>guides/commerce.md, .planning/phases/21-reconciliation-example/21-CONTEXT.md, .planning/phases/21-reconciliation-example/21-RESEARCH.md, examples/phoenix_host/lib/crosswake_example/commerce/reconciliation_inbox.ex</read_first>
  <action>Add `## Minimal Reconciliation Inbox Example` to `guides/commerce.md` with a concrete sequence for `purchase`, `restore`, `webhook`, and `support` evidence ingestion. Include exact ownership statements: Phoenix persists append-only evidence events and canonical attempts; host-owned verification workers/processes run provider checks; one backend projection updates `entitlement_snapshot`; ingestion outcomes remain non-authoritative. Include explicit mention that the example is example/docs-only and companion-ready, not a required persistence schema or job framework contract.</action>
  <verify>mix test test/crosswake/guides/commerce_test.exs</verify>
  <acceptance_criteria>
    - `guides/commerce.md` contains heading `## Minimal Reconciliation Inbox Example`
    - `guides/commerce.md` contains all four evidence source words `purchase`, `restore`, `webhook`, and `support` in the example flow
    - `guides/commerce.md` contains explicit non-authoritative wording for ingestion outcomes and backend-owned authority wording for projection
    - `guides/commerce.md` contains explicit `example/docs-only` posture text that rejects required persistence/job framework mandates
  </acceptance_criteria>
  <done>RECN-01 guidance is explicit, source-complete, and scoped correctly for Phoenix-owned backend reconciliation.</done>
</task>

<task type="auto">
  <name>Task 2: Document dual-key idempotency and deterministic projection precedence</name>
  <files>guides/commerce.md</files>
  <read_first>guides/commerce.md, examples/phoenix_host/lib/crosswake_example/commerce/reconciliation_keys.ex, examples/phoenix_host/lib/crosswake_example/commerce/entitlement_projection.ex, .planning/REQUIREMENTS.md</read_first>
  <action>Add a dedicated subsection that names and defines `event_key` and `subject_key` with provider-aware identity fields, plus a negative statement that device `correlation_id` is trace metadata only. Add a projection precedence table that maps freshness/reconciliation/authority conditions to exactly one top-level output (`stale`, `pending`, `denied`, `granted`) and calls out monotonic `as_of` ordering guards for out-of-order evidence.</action>
  <verify>mix test test/crosswake/guides/commerce_test.exs</verify>
  <acceptance_criteria>
    - `guides/commerce.md` contains both `event_key` and `subject_key`
    - `guides/commerce.md` contains wording that transient `correlation_id` does not define idempotency identity
    - `guides/commerce.md` contains all four top-level states `stale`, `pending`, `denied`, and `granted` in one precedence-oriented section or table
    - `guides/commerce.md` contains `as_of` monotonic ordering guidance for projection updates
  </acceptance_criteria>
  <done>RECN-02 and RECN-03 narrative contracts are explicit, deterministic, and directly mappable to executable example modules.</done>
</task>

<task type="auto">
  <name>Task 3: Expand docs tests and example-host README to lock boundary-safe guidance</name>
  <files>test/crosswake/guides/commerce_test.exs, examples/phoenix_host/README.md</files>
  <read_first>test/crosswake/guides/commerce_test.exs, examples/phoenix_host/README.md, guides/commerce.md, .planning/phases/21-reconciliation-example/21-PATTERNS.md</read_first>
  <action>Extend `test/crosswake/guides/commerce_test.exs` with literal assertions for the new section heading, source coverage terms, `event_key`/`subject_key` language, and the `stale/pending/denied/granted` precedence contract. Add or tighten negative checks in reconciliation sections to reject provider-specific terms (`storekit`, `play_billing`, `revenuecat`) and any phrasing that implies evidence can directly grant authority. Update `examples/phoenix_host/README.md` with a concise pointer to new `CrosswakeExample.Commerce.*` modules and restate that reconciliation example code is supportable example surface, not a core billing engine or adapter implementation.</action>
  <verify>mix test test/crosswake/guides/commerce_test.exs</verify>
  <acceptance_criteria>
    - `test/crosswake/guides/commerce_test.exs` contains assertions for `Minimal Reconciliation Inbox Example`, `event_key`, `subject_key`, and `stale/pending/denied/granted`
    - `test/crosswake/guides/commerce_test.exs` contains `refute` checks for `storekit`, `play_billing`, and `revenuecat` in reconciliation-related sections
    - `examples/phoenix_host/README.md` contains a reconciliation example pointer referencing `CrosswakeExample.Commerce` modules
    - `rg "Minimal Reconciliation Inbox Example|event_key|subject_key|storekit|play_billing|revenuecat" guides/commerce.md test/crosswake/guides/commerce_test.exs examples/phoenix_host/README.md` returns expected positive matches for required terms and test-enforced negative vocabulary checks
  </acceptance_criteria>
  <done>Docs and example-host discoverability remain synchronized and boundary-safe, with automated regression checks against ambiguity and provider leakage.</done>
</task>
</tasks>

<threat_model>
## Security Gate

- standard: ASVS L1
- block_on: high

## Trust Boundaries

- Public guidance influences host implementation choices and can accidentally widen scope if not constrained.
- Reconciliation example wording crosses from docs into production host logic through copy-forward adoption.
- Example-host README wording shapes support expectations and must not imply shipped provider adapters.

## STRIDE Threat Register

| threat_id | category | component | disposition | mitigation_plan |
| --- | --- | --- | --- | --- |
| P21-02-T01 | spoofing | Guide authority/evidence semantics | mitigated | Lock literal docs wording that evidence is non-authoritative and backend projection owns authority updates. |
| P21-02-T02 | tampering | Docs drift from executable example behavior | mitigated | Tie guide assertions to exact terminology verified by `test/crosswake/guides/commerce_test.exs`. |
| P21-02-T03 | repudiation | Idempotency ambiguity | mitigated | Require explicit `event_key`/`subject_key` wording and explicit correlation-id exclusion statement. |
| P21-02-T04 | information_disclosure | Provider-specific vocabulary leakage | mitigated | Add negative vocabulary checks for `storekit`, `play_billing`, and `revenuecat` in reconciliation guide sections. |
| P21-02-T05 | elevation_of_privilege | Docs implying evidence can grant access | mitigated | Enforce precedence copy that keeps pending/verification states non-granting and authority projection backend-owned. |
</threat_model>

<verification>
Before declaring plan complete:
- [ ] mix test test/crosswake/guides/commerce_test.exs
- [ ] rg "Minimal Reconciliation Inbox Example|event_key|subject_key|stale|pending|denied|granted|as_of|example/docs-only" guides/commerce.md test/crosswake/guides/commerce_test.exs
- [ ] rg "reconciliation example|CrosswakeExample.Commerce|core billing engine|adapter" examples/phoenix_host/README.md
</verification>

<success_criteria>
- All tasks completed
- RECN-01 guide content shows minimal Phoenix-owned inbox flow for purchase/restore/webhook/support evidence
- RECN-02 guide and tests lock provider-aware dual-key idempotency semantics and correlation-id exclusion
- RECN-03 guide and tests lock one authoritative projection with deterministic stale/pending/denied/granted outcomes
- Boundary posture stays explicit and unchanged: backend-owned authority, non-authoritative evidence, provider-neutral vocabulary, example/docs-only delivery
</success_criteria>

<output>
After completion, create `.planning/phases/21-reconciliation-example/21-02-SUMMARY.md`
</output>
