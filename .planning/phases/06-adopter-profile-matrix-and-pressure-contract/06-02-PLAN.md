---
phase: 06-adopter-profile-matrix-and-pressure-contract
plan: 02
type: execute
wave: 2
depends_on:
  - 06-01
files_modified:
  - examples/phoenix_host/README.md
  - script/verify_adopter_profile_contract.sh
  - test/crosswake/proof/adopter_profile_contract_test.exs
  - .planning/phases/06-adopter-profile-matrix-and-pressure-contract/06-PATTERNS.md
autonomous: true
requirements:
  - PROF-02
must_haves:
  truths:
    - "Contributors can see how the three adopter profiles partition future work inside one shared example host instead of drifting into three starter apps."
    - "The shared Phoenix host, paired native hosts, and proof lane extension points are documented as one artifact class that later phases must extend rather than replace."
    - "A deterministic verification scaffold exists for the profile contract so later phases can prove lane separation and doc alignment without inventing a new proof system."
  artifacts:
    - path: "examples/phoenix_host/README.md"
      provides: "Shared example-host lane contract for the three adopter profiles"
    - path: "script/verify_adopter_profile_contract.sh"
      provides: "Deterministic verification entrypoint for lane-boundary and doc-alignment checks"
    - path: "test/crosswake/proof/adopter_profile_contract_test.exs"
      provides: "Automated proof scaffold that later exemplar phases can extend"
  key_links:
    - from: "examples/phoenix_host/README.md"
      to: "guides/adopter_profiles.md"
      via: "host lane documentation uses the same profile names, route classes, and non-goals as the public guide"
      pattern: "Phoenix SaaS Portal|Selective Native Flow|Local-First Study Flow"
    - from: "script/verify_adopter_profile_contract.sh"
      to: "script/verify_phase5_example_hosts.sh"
      via: "the new scaffold extends the existing example-host proof posture instead of replacing it"
      pattern: "verify_phase5_example_hosts"
---

<objective>
Lock the shared example-host lane boundaries and proof-scaffold contract that later exemplar phases will extend.

Purpose: Fulfill the shared-host and downstream-pressure parts of PROF-02 without implementing the Phase 7-10 exemplars early.
Output: A host-local lane contract, a reusable profile-contract verification script, a proof scaffold test, and a phase-local patterns file that tells later executors how to extend the shared artifact class cleanly.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-adopter-profile-matrix-and-pressure-contract/06-CONTEXT.md
@.planning/phases/06-adopter-profile-matrix-and-pressure-contract/06-RESEARCH.md
@.planning/phases/05-packs-native-escape-and-proof-lanes/05-01-PLAN.md
@.planning/phases/05-packs-native-escape-and-proof-lanes/05-08-PLAN.md
@.planning/phases/02-manifest-truth-and-compatibility/02-04-PLAN.md
@guides/adopter_profiles.md
@examples/phoenix_host/lib/crosswake_example/router.ex
@script/verify_phase5_example_hosts.sh

<interfaces>
From examples/phoenix_host/lib/crosswake_example/router.ex:
```elixir
crosswake_defaults runtime: :live_view, offline: :cached_read_only, security: :standard
live "/library", CrosswakeExample.LibraryLive, crosswake: [...]
live "/camera", CrosswakeExample.CameraLive, :capture, crosswake: [runtime: :native_screen, ...]
```

From script/verify_phase5_example_hosts.sh:
```bash
mix test test/mix/tasks/crosswake_install_test.exs \
  test/crosswake/proof/phase5_proof_lane_test.exs
CROSSWAKE_IOS_PROJECT_ROOT="examples/ios_shell_host" bash script/verify_generated_ios_shell.sh
CROSSWAKE_ANDROID_PROJECT_ROOT="examples/android_shell_host" bash script/verify_generated_android_shell.sh
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define the shared example-host lane contract for the three adopter profiles</name>
  <files>examples/phoenix_host/README.md, .planning/phases/06-adopter-profile-matrix-and-pressure-contract/06-PATTERNS.md, test/crosswake/proof/adopter_profile_contract_test.exs</files>
  <behavior>
    - Test 1: The host-local contract states that later exemplars extend one shared Phoenix host plus the paired iOS and Android hosts rather than creating three standalone sample apps per D-12 through D-16.
    - Test 2: Each profile is assigned a believable job-to-be-done, a 4-8 route budget, its primary route classes, route/module/fixture boundaries, and one primary failure vocabulary focus per D-17 through D-29.
    - Test 3: The contract uses the same locked names and seam vocabulary as `guides/adopter_profiles.md`, and the proof test fails if those drift apart.
  </behavior>
  <action>Create `examples/phoenix_host/README.md` as the host-local lane contract for downstream exemplar work. Document one shared example-host artifact class, explicit lane names, allowed route classes, expected route/module/fixture separation, and the rule that the iOS and Android example hosts stay paired public proof artifacts instead of multiplying per profile, per D-12 through D-16. Use the same locked names and pressure meanings from D-01 through D-06 and D-17 through D-23, and capture the realism guardrails from D-24 through D-29 so later phases extend the shared host without turning it into a kitchen-sink demo. Add `.planning/phases/06-adopter-profile-matrix-and-pressure-contract/06-PATTERNS.md` as a concise execution aid summarizing the shared-host, cross-link, and proof-extension patterns from the research so later plans can reuse them without re-reading the whole research file.</action>
  <verify>
    <automated>mix test test/crosswake/proof/adopter_profile_contract_test.exs</automated>
  </verify>
  <done>The repository has an explicit host-local lane contract and a matching patterns file that lock how later exemplar phases must extend the shared artifact class.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prepare the reusable profile-contract verification scaffold for later exemplar proof lanes</name>
  <files>script/verify_adopter_profile_contract.sh, test/crosswake/proof/adopter_profile_contract_test.exs</files>
  <behavior>
    - Test 1: The new verification entrypoint checks that the public profile guide and shared-host lane contract agree on profile names, lane boundaries, route classes, and explicit non-goals.
    - Test 2: The scaffold asserts that later proof lanes extend the checked-in example-host artifact class and the existing Phase 5 proof posture instead of replacing it with a separate proof system, per D-13 through D-16.
    - Test 3: The scaffold remains documentation-and-boundary focused; it does not require Phase 7-10 exemplar routes to exist yet and fails only on contract drift or forbidden starter-app/plugin-bus language per D-24 through D-35.
  </behavior>
  <action>Add `script/verify_adopter_profile_contract.sh` and a focused ExUnit wrapper test that mechanically verifies the shared profile contract before later phases implement exemplar routes. The script should inspect `guides/adopter_profiles.md`, `examples/phoenix_host/README.md`, and `script/verify_phase5_example_hosts.sh` to prove that Phase 6 preserves one shared example-host artifact class, keeps the paired native hosts as proof artifacts, and leaves support/proof status in the existing docs and proof hooks rather than inventing a new system. Keep this scaffold extendable by later phases for route/module/proof-lane assertions, but do not implement the actual exemplar lanes here.</action>
  <verify>
    <automated>bash script/verify_adopter_profile_contract.sh</automated>
    <automated>mix test test/crosswake/proof/adopter_profile_contract_test.exs</automated>
  </verify>
  <done>A deterministic profile-contract verification scaffold exists and is ready for Phases 7-10 to extend without redefining the artifact class or proof posture.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| public profile guide -> shared example-host execution | If these drift, later exemplar phases can silently pressure the wrong seams |
| new verification scaffold -> existing proof posture | A parallel proof system would fragment support truth and weaken install credibility |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-04 | T | `examples/phoenix_host/README.md` | mitigate | Mechanically compare the host-local lane contract to `guides/adopter_profiles.md` so names, route classes, and non-goals cannot diverge silently |
| T-06-05 | R | `script/verify_adopter_profile_contract.sh` | mitigate | Require the scaffold to reference the existing Phase 5 example-host proof entrypoint so later phases extend one proof posture instead of creating an unverifiable parallel story |
| T-06-06 | E | future exemplar lane boundaries | mitigate | Lock the shared-host and no-template rules in both docs and tests so later work cannot escalate into starter-app or plugin-bus scope without breaking automation |
</threat_model>

<verification>
Run `bash script/verify_adopter_profile_contract.sh` and `mix test test/crosswake/proof/adopter_profile_contract_test.exs` to prove the public profile guide, shared-host lane contract, and existing example-host proof posture stay aligned.
</verification>

<success_criteria>
- Contributors have one explicit shared-host lane contract to follow for Phases 7-10.
- The paired native hosts remain documented as proof artifacts of the same shared example-host class.
- A deterministic verification scaffold exists for lane-boundary and doc-alignment drift without implementing exemplar functionality early.
- Phase 6 closes with a handoff that is executable, falsifiable, and still scoped strictly to docs, planning, and proof scaffolding.
</success_criteria>

<output>
After completion, create `.planning/phases/06-adopter-profile-matrix-and-pressure-contract/06-adopter-profile-matrix-and-pressure-contract-02-SUMMARY.md`
</output>
