---
phase: 52
plan: "02"
type: implementation
wave: 2
depends_on:
  - 52-01
files_modified:
  - .github/workflows/phase52-proof.yml
autonomous: true
requirements:
  - PROOF-01
  - PROOF-02
must_haves:
  truths:
    - "CI exposes one clearly named merge-blocking hermetic operator-proof job and one clearly named advisory-only visibility job."
    - "The workflow runs the same focused local proof command maintainers use by hand and never smuggles advisory env vars into the merge-blocking lane."
    - "Decision coverage: D-01 through D-05 and D-15 through D-27 are reflected in workflow topology, naming, comments, and non-claim notices."
  artifacts:
    - path: .github/workflows/phase52-proof.yml
      provides: "Dedicated Phase 52 merge-blocking versus advisory workflow"
      contains: "merge-blocking-operator-proof"
  key_links:
    - from: .github/workflows/phase52-proof.yml
      to: test/crosswake/proof/phase52_operator_truth_test.exs
      via: "focused hermetic mix test command"
      pattern: "mix test test/crosswake/proof/phase52_operator_truth_test\\.exs"
    - from: .github/workflows/phase52-proof.yml
      to: test/crosswake/proof/phase43_rulestead_advisory_test.exs
      via: "step-level advisory env gating"
      pattern: "MIX_INCLUDE_RULESTEAD|phase43_rulestead_advisory_test"
    - from: .github/workflows/phase52-proof.yml
      to: test/crosswake/proof/phase45_rindle_advisory_test.exs
      via: "step-level advisory env gating"
      pattern: "MIX_INCLUDE_RINDLE|phase45_rindle_advisory_test"
---

<objective>
Wire the Phase 52 proof contract into dedicated CI topology with an explicit merge-blocking versus advisory boundary.

Purpose: keep the new hermetic proof durable in CI while making environment-sensitive visibility explicit, non-blocking, and impossible to misread as shipped support.
Output: `.github/workflows/phase52-proof.yml` with a required hermetic operator-proof job and an advisory-only job for current optional dependency visibility.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/52-operator-proof-and-docs/52-CONTEXT.md
@.planning/phases/52-operator-proof-and-docs/52-RESEARCH.md
@.planning/phases/52-operator-proof-and-docs/52-PATTERNS.md
@.planning/phases/52-operator-proof-and-docs/52-01-PLAN.md
@.planning/phases/52-operator-proof-and-docs/52-01-SUMMARY.md
@.github/workflows/phase43-proof.yml
@.github/workflows/phase45-proof.yml
@test/crosswake/proof/phase52_operator_truth_test.exs
@test/crosswake/proof/phase43_rulestead_advisory_test.exs
@test/crosswake/proof/phase45_rindle_advisory_test.exs

<interfaces>
From `test/crosswake/proof/phase52_operator_truth_test.exs` after Plan 52-01:
- The hermetic contract is the exact local command `mix test test/crosswake/proof/phase52_operator_truth_test.exs`.
- The proof file must stay untagged and env-independent so it is safe for the merge-blocking lane.

From `.github/workflows/phase43-proof.yml` and `.github/workflows/phase45-proof.yml`:
- Required jobs run on `pull_request`, `push` to `main`, and `workflow_dispatch`.
- Advisory jobs run only on `schedule` and `workflow_dispatch` with `continue-on-error: true`.
- Optional dependency env vars are set at step level only.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the merge-blocking Phase 52 operator-proof workflow job</name>
  <files>.github/workflows/phase52-proof.yml</files>
  <read_first>
    - `.github/workflows/phase52-proof.yml` (create in this task if absent)
    - `.planning/phases/52-operator-proof-and-docs/52-CONTEXT.md`
    - `.planning/phases/52-operator-proof-and-docs/52-RESEARCH.md`
    - `.github/workflows/phase43-proof.yml`
    - `.github/workflows/phase45-proof.yml`
    - `test/crosswake/proof/phase52_operator_truth_test.exs`
  </read_first>
  <action>Create `.github/workflows/phase52-proof.yml` per D-01 through D-05 and D-17 through D-20 with the exact job key `merge-blocking-operator-proof` and a human-facing `name` that says `merge-blocking operator proof (hermetic)`. Use the existing repo pattern: `pull_request`, `push` to `main`, `workflow_dispatch`, and `schedule` at the workflow level; gate the required job so it runs only on `pull_request`, `push`, and `workflow_dispatch`. Use `actions/checkout@v6`, `erlef/setup-beam@v1`, `elixir-version: \"1.19.5\"`, and `otp-version: \"27.3\"` to match existing proof workflows. Compile with warnings as errors, then run the exact local contract `mix test test/crosswake/proof/phase52_operator_truth_test.exs`. Keep the lane hermetic: no `MIX_INCLUDE_*` variables, no example-host-only test paths, and no widened full-suite command that could pull advisory or unrelated historical proofs into Phase 52.</action>
  <acceptance_criteria>
    - `.github/workflows/phase52-proof.yml` defines `merge-blocking-operator-proof` with the exact run command `mix test test/crosswake/proof/phase52_operator_truth_test.exs`.
    - The required job is gated away from the scheduled trigger and contains no workflow-level or job-level `MIX_INCLUDE_*` env vars.
    - Workflow comments explain that the merge-blocking lane locks v3.6 operator truth only and is the same command maintainers run locally.
  </acceptance_criteria>
  <verify>
    <automated>rg -n "merge-blocking-operator-proof|mix test test/crosswake/proof/phase52_operator_truth_test.exs|workflow_dispatch|pull_request|push:|schedule:" .github/workflows/phase52-proof.yml && mix test test/crosswake/proof/phase52_operator_truth_test.exs</automated>
  </verify>
  <done>The repo has a dedicated hermetic Phase 52 proof workflow job that mirrors the local proof command and stays free of advisory env state.</done>
</task>

<task type="auto">
  <name>Task 2: Add advisory-only visibility without implying deferred features shipped</name>
  <files>.github/workflows/phase52-proof.yml</files>
  <read_first>
    - `.github/workflows/phase52-proof.yml`
    - `test/crosswake/proof/phase43_rulestead_advisory_test.exs`
    - `test/crosswake/proof/phase45_rindle_advisory_test.exs`
    - `.planning/phases/52-operator-proof-and-docs/52-CONTEXT.md`
    - `.planning/phases/52-operator-proof-and-docs/52-RESEARCH.md`
    - `.github/workflows/phase43-proof.yml`
    - `.github/workflows/phase45-proof.yml`
  </read_first>
  <action>In the same workflow add an `advisory-operator-proof` job per D-02 through D-05, D-15, and D-26. Gate it to `schedule` and `workflow_dispatch` only, set `continue-on-error: true`, and use step-level env only: `MIX_INCLUDE_RULESTEAD=1` for `mix test test/crosswake/proof/phase43_rulestead_advisory_test.exs` and `MIX_INCLUDE_RINDLE=1` for `mix test test/crosswake/proof/phase45_rindle_advisory_test.exs`. Add explicit notices and comments that failures are advisory, cannot gate merge, and do not imply StoreKit, Play Billing, Chimeway delivery, full Sigra machinery, or standalone public shell packages have shipped per D-04 and D-10. Keep this job as a presentation layer derived from existing advisory proofs, not a second canonical contract per D-15. Do not invent new provider, device, or auth advisory jobs whose underlying support claims are still deferred.</action>
  <acceptance_criteria>
    - The advisory job runs only on `schedule` and `workflow_dispatch` and has `continue-on-error: true`.
    - `MIX_INCLUDE_RULESTEAD` and `MIX_INCLUDE_RINDLE` appear only on the exact advisory steps that need them.
    - Workflow notices and comments explicitly say the advisory lane does not gate merge and does not promote deferred provider, auth, notification-delivery, or shell-package claims into shipped support.
  </acceptance_criteria>
  <verify>
    <automated>rg -n "advisory-operator-proof|continue-on-error: true|phase43_rulestead_advisory_test.exs|phase45_rindle_advisory_test.exs|MIX_INCLUDE_RULESTEAD|MIX_INCLUDE_RINDLE|cannot gate merge|StoreKit|Play Billing|Chimeway|standalone public shell packages" .github/workflows/phase52-proof.yml && mix test test/crosswake/proof/phase52_operator_truth_test.exs</automated>
  </verify>
  <done>The dedicated workflow shows advisory visibility for current optional dependency proofs without widening or mislabeling shipped support.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| hermetic proof contract -> CI branch gate | The merge-blocking job must execute only deterministic local truth so advisory env requirements cannot silently block merges. |
| advisory proof visibility -> public support interpretation | CI job names and notices can be misread as shipped support if the advisory boundary is not explicit. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-52-04 | Spoofing | `.github/workflows/phase52-proof.yml` advisory job names and notices | mitigate | Label the non-blocking job as advisory-only, restrict it to schedule or manual dispatch, and state the exact deferred non-claims per D-02, D-04, and D-26. |
| T-52-05 | Tampering | `.github/workflows/phase52-proof.yml` merge-blocking job env and commands | mitigate | Keep the required lane pinned to the exact hermetic Phase 52 test command with no `MIX_INCLUDE_*` env bleed and no advisory-tagged proof inclusion per D-01 through D-05. |
| T-52-06 | Repudiation | CI proof topology | mitigate | Use explicit comments, local-command parity, and visible advisory notices so reviewers can audit which job gates merge and which jobs remain informational. |
| T-52-SC | Tampering | npm/pip/cargo installs | accept | No new package-manager installs are allowed in this plan. |
</threat_model>

<verification>
- `rg -n "merge-blocking-operator-proof|advisory-operator-proof|phase52_operator_truth_test|phase43_rulestead_advisory_test|phase45_rindle_advisory_test|continue-on-error: true" .github/workflows/phase52-proof.yml`
- `mix test test/crosswake/proof/phase52_operator_truth_test.exs`
</verification>

<success_criteria>
- The repo has a dedicated Phase 52 proof workflow instead of scattering operator proof across older lanes.
- Merge-blocking and advisory jobs are visually and behaviorally distinct.
- Current advisory visibility stays honest about deferred provider, auth, notification-delivery, and shell-package scope.
</success_criteria>

<output>
Create `.planning/phases/52-operator-proof-and-docs/52-02-SUMMARY.md` when done.
</output>
