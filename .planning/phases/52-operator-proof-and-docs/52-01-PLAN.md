---
phase: 52
plan: "01"
type: implementation
wave: 1
depends_on: []
files_modified:
  - test/support/proof_assertions.ex
  - test/crosswake/proof/phase52_operator_truth_test.exs
  - test/fixtures/proof/phase52_operator_inspection.json
  - test/fixtures/proof/phase52_publish_readiness.json
autonomous: true
requirements:
  - PROOF-01
  - PROOF-02
must_haves:
  truths:
    - "Maintainers can run one hermetic Phase 52 proof and catch drift in inspect JSON, doctor publish-readiness JSON, support rows, denial vocabulary, rebuild/action classes, promotion rules, and public non-claims."
    - "Proof failures identify stable proof ids, live source, observed drift, file or module path, remediation hint, and whether the drift affects merge-blocking truth."
    - "Decision coverage: D-03 is enforced by full deterministic local truth coverage for inspection output, doctor publish-readiness findings, support matrix rows, docs-contract parity, denial vocabulary, rebuild/action classes, promotion rules, and public non-claims."
    - "Decision coverage: D-07 and D-08 are enforced by byte-identical generated support-matrix checks plus live-code parity assertions for support statuses, proof classes, denial reasons, rebuild/change/action classes, promotion rule ids, doctor readiness categories/codes, and guide anchors."
    - "Decision coverage: D-18 and D-19 are enforced by a requirement-mapped selective rollup that does not aggregate every historical proof and references historical dependencies only when they protect live v3.6 operator truth."
    - "Decision coverage: D-23 and D-25 are enforced by stable-id helper assertions with severity/posture/object/hint/docs-link semantics while preserving raw support, proof, rebuild, provider, auth, notification, and denial axes."
    - "Decision coverage: D-06 through D-27 are enforced through a requirement-mapped v3.6 operator-truth rollup instead of a mega historical lane."
  artifacts:
    - path: test/support/proof_assertions.ex
      provides: "Stable-id helper assertions, JSON normalization, and hermetic-lane guard helpers"
      contains: "defmodule Crosswake.TestSupport.ProofAssertions"
    - path: test/crosswake/proof/phase52_operator_truth_test.exs
      provides: "Focused Phase 52 hermetic proof for operator truth and docs-contract parity"
      contains: "defmodule Crosswake.Proof.Phase52OperatorTruthTest"
    - path: test/fixtures/proof/phase52_operator_inspection.json
      provides: "Normalized golden contract for operator inspection JSON"
      contains: "\"schema_version\": \"1.0.0\""
    - path: test/fixtures/proof/phase52_publish_readiness.json
      provides: "Normalized golden contract for publish-readiness JSON"
      contains: "\"schema_version\": \"1.0.0\""
  key_links:
    - from: test/crosswake/proof/phase52_operator_truth_test.exs
      to: test/support/proof_assertions.ex
      via: "stable-id proof helpers"
      pattern: "proof\\.(operator|readiness|docs|denial|rebuild)"
    - from: test/crosswake/proof/phase52_operator_truth_test.exs
      to: lib/crosswake/operator_inspection/json_formatter.ex
      via: "normalized JSON fixture comparison"
      pattern: "Crosswake\\.OperatorInspection\\.JSONFormatter\\.render|mix crosswake\\.inspect"
    - from: test/crosswake/proof/phase52_operator_truth_test.exs
      to: lib/crosswake/doctor/publish_readiness.ex
      via: "normalized JSON fixture comparison and stable readiness metadata assertions"
      pattern: "Crosswake\\.Doctor\\.PublishReadiness\\.(run|to_map)|mix crosswake\\.doctor"
---

<objective>
Build the hermetic proof contract that makes v3.6 operator truth mechanically durable.

Purpose: lock the live operator, doctor, support, denial, rebuild, promotion, and docs-contract surface with actionable drift failures before wiring CI around it.
Output: a focused Phase 52 proof module, stable proof assertion helpers, and normalized JSON golden fixtures for operator inspection and publish readiness.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/MILESTONE-ARC.md
@.planning/phases/52-operator-proof-and-docs/52-CONTEXT.md
@.planning/phases/52-operator-proof-and-docs/52-RESEARCH.md
@.planning/phases/52-operator-proof-and-docs/52-PATTERNS.md
@.planning/phases/52-operator-proof-and-docs/52-VALIDATION.md
@.planning/phases/49-operator-inspection-contract/49-01-SUMMARY.md
@.planning/phases/50-doctor-publish-and-readiness-checks/50-02-SUMMARY.md
@.planning/phases/51-support-matrix-and-native-rebuild-truth/51-03-SUMMARY.md
@lib/crosswake/operator_inspection/types.ex
@lib/crosswake/operator_inspection/json_formatter.ex
@lib/crosswake/doctor/publish_readiness.ex
@lib/crosswake/support_matrix/support_matrix.ex
@lib/crosswake/support_matrix/renderer.ex
@lib/crosswake/shell/denial.ex
@test/crosswake/proof/phase47_companion_arc_test.exs
@test/crosswake/support_matrix/renderer_test.exs
@test/crosswake/guides/release_boundaries_test.exs
@test/mix/tasks/crosswake_inspect_test.exs
@test/mix/tasks/crosswake_doctor_test.exs
@test/support/router_fixtures.ex

<interfaces>
From `lib/crosswake/operator_inspection/types.ex`:
- `schema_version/0` returns `"1.0.0"`.
- `document/1`, `route/1`, and `condition/1` construct the versioned operator document.
- `to_map/1` is the canonical JSON-safe projection and keeps booleans intact.

From `lib/crosswake/operator_inspection/json_formatter.ex`:
- `render/1` accepts `Crosswake.OperatorInspection.Types.Document` and emits stable JSON.

From `lib/crosswake/doctor/publish_readiness.ex`:
- `run/1` returns a `%Report{schema_version, status, summary, checks}`.
- `ReadinessCheck` already carries `category`, `code`, `severity`, `proof_class`, `rebuild_requirement`, and `details`.
- `to_map/1` is the machine-readable projection the proof should normalize and compare.

From `lib/crosswake/support_matrix/support_matrix.ex` and `renderer.ex`:
- `canonical/1` remains the single source of support truth.
- `render/1` in `Crosswake.SupportMatrix.Renderer` is the generated guide contract for `guides/support_matrix.md`.

From `lib/crosswake/shell/denial.ex`:
- `reasons/0` is the canonical denial vocabulary list that docs and operator truth must stay aligned to.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add failing Phase 52 proof contracts and normalized golden fixtures</name>
  <files>test/crosswake/proof/phase52_operator_truth_test.exs, test/fixtures/proof/phase52_operator_inspection.json, test/fixtures/proof/phase52_publish_readiness.json</files>
  <read_first>
    - `test/crosswake/proof/phase52_operator_truth_test.exs` (create in this task if absent)
    - `test/fixtures/proof/phase52_operator_inspection.json` (create in this task if absent)
    - `test/fixtures/proof/phase52_publish_readiness.json` (create in this task if absent)
    - `.planning/phases/52-operator-proof-and-docs/52-CONTEXT.md`
    - `.planning/phases/52-operator-proof-and-docs/52-RESEARCH.md`
    - `.planning/phases/52-operator-proof-and-docs/52-PATTERNS.md`
    - `test/crosswake/proof/phase47_companion_arc_test.exs`
    - `test/crosswake/support_matrix/renderer_test.exs`
    - `test/mix/tasks/crosswake_inspect_test.exs`
    - `test/mix/tasks/crosswake_doctor_test.exs`
    - `lib/crosswake/operator_inspection/json_formatter.ex`
    - `lib/crosswake/doctor/publish_readiness.ex`
  </read_first>
  <behavior>
    - Test 1: normalized `mix crosswake.inspect --format json` output matches a golden fixture with stable proof ids and no volatile-field drift.
    - Test 2: normalized `mix crosswake.doctor --check-publish --format json` output matches a golden fixture and preserves canonical readiness categories, codes, proof classes, rebuild metadata, and deferred-scope messaging.
    - Test 3: docs-contract coverage proves `guides/support_matrix.md` is byte-identical to the renderer while authored guides preserve exact operator non-claims, denial vocabulary, promotion ids, and rebuild wording.
    - Test 4: the proof file exposes at least one `@tag :phase52_smoke` fast assertion subset while the full file stays hermetic and untagged at module level so the merge-blocking lane never depends on advisory env vars, example-host modules, or provider/device state.
  </behavior>
  <action>Expand `test/crosswake/proof/phase52_operator_truth_test.exs` first per D-03, D-06 through D-11, and D-17 through D-21. Add at least one `@tag :phase52_smoke` test that verifies the stable proof-id/helper contract quickly for per-task feedback, while keeping the full proof file as the wave/CI contract. Add a focused router fixture inside the proof module or reuse existing in-repo fixtures so the full test covers exact grouped proof ids `proof.operator.inspect.*`, `proof.readiness.publish.*`, `proof.docs.*`, `proof.denial.*`, and `proof.rebuild.*` per D-12 through D-14, D-23, and D-25. Create two normalized golden fixtures named `test/fixtures/proof/phase52_operator_inspection.json` and `test/fixtures/proof/phase52_publish_readiness.json`; the expected JSON must omit volatile fields such as `evaluated_at`, timestamps, and nondeterministic ordering before comparison per D-09. Add source assertions for the exact public non-claims from D-10 and D-19: StoreKit and Play Billing adapters are not shipped in v3.6, Sigra remains contract-only, notification-token readiness is provider-snapshot only, Chimeway delivery is deferred, standalone public shell packages are deferred, compatibility-window narrowing is distinct from a native rebuild, `:step_up_required` remains contract-only auth truth, and commerce provider non-claims stay scoped. Keep the proof requirement-mapped to v3.6 operator truth only per D-17 through D-20; do not widen into a mega historical lane or provider/device workflows.</action>
  <acceptance_criteria>
    - `test/crosswake/proof/phase52_operator_truth_test.exs` contains failing assertions for inspect JSON, publish-readiness JSON, support-matrix byte parity, authored guide non-claims, denial vocabulary, and a hermetic lane guard.
    - `test/crosswake/proof/phase52_operator_truth_test.exs` contains `@tag :phase52_smoke` on at least one fast proof-id/helper assertion for the per-task feedback loop.
    - `test/fixtures/proof/phase52_operator_inspection.json` and `test/fixtures/proof/phase52_publish_readiness.json` exist with normalized `"schema_version": "1.0.0"` payloads and no timestamp-like fields.
    - The new proof test fails before the shared proof-assertion helper exists.
  </acceptance_criteria>
  <verify>
    <automated>mix test test/crosswake/proof/phase52_operator_truth_test.exs --only phase52_smoke</automated>
  </verify>
  <done>The failing proof contract describes the exact Phase 52 operator-truth surface, drift ids, and docs-contract boundaries.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement stable-id proof helpers and make the hermetic operator proof pass</name>
  <files>test/support/proof_assertions.ex, test/crosswake/proof/phase52_operator_truth_test.exs, test/fixtures/proof/phase52_operator_inspection.json, test/fixtures/proof/phase52_publish_readiness.json</files>
  <read_first>
    - `test/support/proof_assertions.ex` (create in this task if absent)
    - `test/crosswake/proof/phase52_operator_truth_test.exs`
    - `test/fixtures/proof/phase52_operator_inspection.json`
    - `test/fixtures/proof/phase52_publish_readiness.json`
    - `test/support/router_fixtures.ex`
    - `lib/crosswake/operator_inspection/types.ex`
    - `lib/crosswake/operator_inspection/json_formatter.ex`
    - `lib/crosswake/doctor/publish_readiness.ex`
    - `lib/crosswake/support_matrix/support_matrix.ex`
    - `lib/crosswake/support_matrix/renderer.ex`
    - `lib/crosswake/shell/denial.ex`
    - `.planning/phases/52-operator-proof-and-docs/52-CONTEXT.md`
  </read_first>
  <behavior>
    - Test 1: proof helpers emit actionable failures containing stable id, subject, expected live source, observed drift, path, remediation hint, and merge-blocking or advisory posture.
    - Test 2: JSON fixture comparison normalizes maps, lists, and volatile fields before asserting equality so golden files lock contract shape rather than timestamp noise.
    - Test 3: the Phase 52 proof passes while asserting semantic meaning alongside fixture parity, so accepting fixture updates never becomes the only remediation path.
  </behavior>
  <action>Create `Crosswake.TestSupport.ProofAssertions` per D-12 through D-16 and D-22 through D-27 with helper entry points that cover normalized JSON fixture comparison, guide-claim parity, vocabulary sync, and hermetic-lane guards. The helper messages must include a stable proof id, subject, expected live source, observed drift, guide or module path, remediation hint, and whether the drift affects merge-blocking or advisory truth per D-13. Normalize JSON by decoding through `Jason.decode!/1`, recursively sorting collections by stable keys, dropping `evaluated_at` and other volatile timestamps, and re-encoding before equality checks per D-09 and D-24. Then wire `phase52_operator_truth_test.exs` to prove `mix crosswake.inspect` JSON, `mix crosswake.doctor --check-publish` JSON, `guides/support_matrix.md` renderer bytes, `guides/install.md`, `guides/native_shell.md`, `guides/compatibility.md`, `guides/companions.md`, `guides/commerce.md`, and `guides/capabilities.md` authored non-claims, `Crosswake.Shell.Denial.reasons/0`, canonical support statuses, proof classes, action classes, promotion rule ids, and `:step_up_required` posture all remain synchronized. Keep snapshot-style fixtures as evidence only; pair every fixture assertion with semantic checks that explain what changed so D-16 holds.</action>
  <acceptance_criteria>
    - `test/support/proof_assertions.ex` exists and exports stable-id helper functions grouped under `proof.operator.*`, `proof.readiness.*`, `proof.docs.*`, `proof.denial.*`, and `proof.rebuild.*`.
    - `test/crosswake/proof/phase52_operator_truth_test.exs` uses the helper instead of raw equality-only failures and asserts semantic non-claims alongside fixture parity.
    - `mix test test/crosswake/proof/phase52_operator_truth_test.exs` passes.
  </acceptance_criteria>
  <verify>
    <automated>mix test test/crosswake/proof/phase52_operator_truth_test.exs</automated>
  </verify>
  <done>The Phase 52 proof is hermetic, requirement-mapped, and produces actionable drift output instead of generic assertion noise.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| canonical operator/support truth -> proof fixtures and guide assertions | The proof must lock live support claims without letting stale fixtures or prose drift hide a semantic change. |
| proof failure output -> maintainer remediation | Failure messages become part of the operator surface because they determine whether support-truth drift can be diagnosed quickly and correctly. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-52-01 | Tampering | `test/crosswake/proof/phase52_operator_truth_test.exs` and JSON fixtures | mitigate | Use normalized golden fixtures plus semantic assertions per D-06 through D-11 and D-16 so fixture refresh alone never masks a support-truth change. |
| T-52-02 | Spoofing | authored guides and deferred-scope claims | mitigate | Assert exact non-claim wording and canonical support metadata for StoreKit, Play Billing, Sigra, notification delivery, Chimeway, and shell-package defers per D-10 and D-19. |
| T-52-03 | Repudiation | proof failure ergonomics | mitigate | Emit stable proof ids, source paths, and remediation hints from shared helper assertions per D-12 through D-15 so drift failures are auditable and actionable. |
| T-52-SC | Tampering | npm/pip/cargo installs | accept | No new package-manager installs are allowed in this plan. |
</threat_model>

<verification>
- `mix test test/crosswake/proof/phase52_operator_truth_test.exs --only phase52_smoke` after each task commit.
- `mix test test/crosswake/proof/phase52_operator_truth_test.exs`
</verification>

<success_criteria>
- One focused hermetic proof locks the v3.6 operator surface without re-homing every historical proof.
- Golden JSON contracts and guide assertions point to live canonical truth rather than prose guesses.
- Drift failures tell maintainers exactly what changed and where to fix it.
</success_criteria>

<output>
Create `.planning/phases/52-operator-proof-and-docs/52-01-SUMMARY.md` when done.
</output>
