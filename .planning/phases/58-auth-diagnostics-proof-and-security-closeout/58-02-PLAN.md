---
phase: 58-auth-diagnostics-proof-and-security-closeout
plan: "02"
title: "Security Ledger And Closeout Verifier Hardening"
type: execute
wave: 2
depends_on: ["58-01-PLAN.md"]
status: pending
requirements: [DIAG-02, DIAG-03, PROOF-01]
requirements_addressed: [PROOF-01]
must_haves:
  truths:
    - "D-22: Security closeout is bounded adversarial STRIDE plus remediation ledger"
    - "D-23: Required Phase 58 security section set is preserved"
    - "D-24: Security surfaces use compact evidence-backed ledger rows"
    - "D-25: Evidence channels cannot set SessionAuthorityLane"
    - "D-26: Replay, double-consume, expiry, revocation, and mismatch controls are reviewed"
    - "D-27: Phoenix session renewal, CSRF posture, and LiveView invalidation stay host-owned"
    - "D-28: Plug/controller and LiveView paths share auth semantics"
    - "D-29: Denial and telemetry sanitization excludes secret and identity fields"
    - "D-30: Findings disposition blocks unresolved high/critical issues"
    - "D-31: Phoenix/Plug host-owned session mutation lesson is preserved"
    - "D-32: Ecto consume/audit/projection transaction lesson is preserved"
    - "D-33: LiveView and HTTP shared auth semantics lesson is preserved"
    - "D-34: Telemetry/Phoenix stable observability API lesson is preserved"
    - "D-35: OWASP/OAuth/WebAuthn sanitized diagnostic lesson is preserved"
    - "D-36: Native/mobile evidence-not-authority lesson is preserved"
    - "D-37: Proof, diagnostics, support truth, and security constraints remain product surface"
autonomous: true
files_modified:
  - .planning/phases/58-auth-diagnostics-proof-and-security-closeout/58-SECURITY.md
  - lib/crosswake/planning/closeout_verifier.ex
  - lib/mix/tasks/closeout.verify.ex
  - test/mix/tasks/closeout_verify_test.exs
  - test/crosswake/proof/phase58_auth_diagnostics_closeout_test.exs
---

# Plan 58-02: Security Ledger And Closeout Verifier Hardening

<objective>
Close PROOF-01 security posture by strengthening `58-SECURITY.md` into an evidence-backed STRIDE ledger and keeping `mix closeout.verify --security-only` deterministic, merge-blocking, and scoped to machine-checkable Phase 58 guarantees.
</objective>

<context>
The current security artifact has the required headings but is too terse for the locked Phase 58 security model. Strengthen the artifact and verifier tests without editing implementation authority paths. The verifier should remain deterministic; avoid broad editorial scoring that would be brittle.
</context>

<tasks>
  <task id="58-02-01" type="execute">
    <name>Rewrite the security closeout as bounded STRIDE evidence ledger</name>
    <read_first>
      AGENTS.md
      .planning/PROJECT.md
      .planning/REQUIREMENTS.md
      .planning/ROADMAP.md
      .planning/STATE.md
      .planning/phases/58-auth-diagnostics-proof-and-security-closeout/58-CONTEXT.md
      .planning/phases/58-auth-diagnostics-proof-and-security-closeout/58-RESEARCH.md
      .planning/phases/58-auth-diagnostics-proof-and-security-closeout/58-VALIDATION.md
      .planning/phases/58-auth-diagnostics-proof-and-security-closeout/58-PATTERNS.md
      .planning/phases/58-auth-diagnostics-proof-and-security-closeout/58-SECURITY.md
      lib/crosswake/companions/sigra/contracts.ex
      lib/crosswake/companions/sigra/evaluator.ex
      lib/crosswake/companions/sigra/handoff.ex
      lib/crosswake/companions/sigra/step_up.ex
      lib/crosswake/companions/sigra/step_up_ceremony.ex
      lib/crosswake/companions/sigra/auth_return.ex
      lib/crosswake/companions/sigra/telemetry.ex
      lib/crosswake/companions/sigra/denial_codes.ex
      lib/crosswake/compatibility/route_gate.ex
      lib/crosswake/support_matrix/support_matrix.ex
      lib/crosswake/operator_inspection.ex
      examples/phoenix_host/lib/crosswake_example/saas_portal/auth.ex
      examples/phoenix_host/lib/crosswake_example/saas_portal/handoff.ex
      examples/phoenix_host/lib/crosswake_example/saas_portal/auth_return_attempt.ex
      examples/phoenix_host/lib/crosswake_example/saas_portal/auth_return_audit_event.ex
    </read_first>
    <action>Rewrite `.planning/phases/58-auth-diagnostics-proof-and-security-closeout/58-SECURITY.md` while preserving these exact headings: `## Token And Locator Handling`, `## Handoff Tickets`, `## Step-Up Ceremony`, `## Auth Return Boundaries`, `## Telemetry And Diagnostics`, `## Denial Sanitization`, `## Session Renewal And LiveView Invalidation`, `## Doctor Support Operator And Docs Truth`, `## Proof And Non-Claims`, and `## Findings Disposition`. Under each surface heading, add a compact Markdown table with columns `Surface`, `STRIDE`, `Adversarial scenario`, `Control`, `Evidence`, `Residual risk`, and `Disposition`. Include rows that directly challenge: handoff envelopes, step-up locators, auth-return envelopes, deep links, bridge events, provider payloads, and telemetry events setting `SessionAuthorityLane`; replay/double-consume/expiry/revocation/route mismatch/session mismatch for handoff, step-up, and auth-return attempts; Plug/controller versus LiveView semantic bypass; Phoenix session renewal/CSRF/LiveView invalidation ownership; denial and telemetry leaks of raw tokens/codes/nonces/PKCE/credentials/session/actor/org/device/IP/user-agent/provider payload/raw `return_to`; and provider/device overclaims. Target state: all high/critical scenarios are `Closed` or `Mitigated`, medium provider/device uncertainty is `Mitigated` or `Advisory`, and evidence cites concrete modules, tests, workflows, guides, or example-host files.</action>
    <acceptance_criteria>
      - `.planning/phases/58-auth-diagnostics-proof-and-security-closeout/58-SECURITY.md` contains all ten required headings exactly.
      - Every required heading has at least one ledger table row with the columns `Surface`, `STRIDE`, `Adversarial scenario`, `Control`, `Evidence`, `Residual risk`, and `Disposition`.
      - The ledger explicitly states that telemetry, handoff envelopes, step-up locators, auth-return envelopes, deep links, bridge events, and provider payloads cannot set `SessionAuthorityLane`.
      - The ledger explicitly names `configure_session(conn, renew: true)` or host-owned Phoenix session renewal posture as host-owned and post-validation only.
      - The ledger contains no unresolved `Critical` or `High` finding in `## Findings Disposition`.
    </acceptance_criteria>
  </task>

  <task id="58-02-02" type="execute">
    <name>Harden security-only closeout verification without broad editorial scoring</name>
    <read_first>
      .planning/phases/58-auth-diagnostics-proof-and-security-closeout/58-CONTEXT.md
      .planning/phases/58-auth-diagnostics-proof-and-security-closeout/58-SECURITY.md
      lib/crosswake/planning/closeout_verifier.ex
      lib/mix/tasks/closeout.verify.ex
      test/mix/tasks/closeout_verify_test.exs
      test/crosswake/proof/phase58_auth_diagnostics_closeout_test.exs
    </read_first>
    <action>Audit `Crosswake.Planning.CloseoutVerifier.security_closeout_check/1` in `lib/crosswake/planning/closeout_verifier.ex` and `Mix.Tasks.Closeout.Verify` in `lib/mix/tasks/closeout.verify.ex`. Preserve existing deterministic behavior: `--security-only --security-closeout .planning/phases/58-auth-diagnostics-proof-and-security-closeout/58-SECURITY.md` skips v3.6 ledger checks, requires all ten Phase 58 security headings, fails on missing files, and fails on unresolved `Critical` or `High` findings whose status is not `Closed` or `Mitigated`. Add only bounded structural checks if needed: table headers include `STRIDE`, `Adversarial scenario`, `Control`, `Evidence`, `Residual risk`, and `Disposition`; content includes `SessionAuthorityLane`, `diagnostic evidence only`, `provider/device proof remains advisory`, and `direct shell/WebView token authority`. Update `test/mix/tasks/closeout_verify_test.exs` with fixtures that prove missing table structure or unresolved `High` rows fail, and mitigated `High` rows pass. Target state: verifier catches machine-checkable security-closeout drift without pretending to judge full STRIDE quality.</action>
    <acceptance_criteria>
      - `mix closeout.verify --security-only --security-closeout .planning/phases/58-auth-diagnostics-proof-and-security-closeout/58-SECURITY.md` passes.
      - `mix test test/mix/tasks/closeout_verify_test.exs test/crosswake/proof/phase58_auth_diagnostics_closeout_test.exs --trace` passes.
      - A test fixture with an unresolved `High` finding status such as `Open` fails with `closeout.security.phase58`.
      - A test fixture missing one required Phase 58 heading fails with `closeout.security.phase58`.
      - A test fixture that has required headings, required table headers, and no unresolved high/critical findings passes in `--security-only` mode.
    </acceptance_criteria>
  </task>

  <task id="58-02-03" type="execute">
    <name>Lock denial sanitization and security non-claims into closeout proof</name>
    <read_first>
      .planning/phases/58-auth-diagnostics-proof-and-security-closeout/58-CONTEXT.md
      lib/crosswake/companions/sigra/denial_codes.ex
      lib/crosswake/shell/denial.ex
      lib/crosswake/companions/sigra/telemetry.ex
      test/crosswake/proof/phase58_auth_diagnostics_closeout_test.exs
      test/crosswake/companions/sigra/telemetry_test.exs
      guides/companions.md
      guides/support_matrix.md
      guides/native_shell.md
    </read_first>
    <action>Strengthen `test/crosswake/proof/phase58_auth_diagnostics_closeout_test.exs` so Phase 58 proof rejects secret-bearing denial detail keys and public overclaims. Assert `Crosswake.Companions.Sigra.DenialCodes.allowed_detail_keys/0` excludes `access_token`, `refresh_token`, `id_token`, `authorization_code`, `nonce`, `pkce_verifier`, `credential_id`, `passkey_credential_id`, `provider_payload`, `return_to`, `raw_return_to`, `session_ref`, `subject_ref`, `actor_id`, `org_id`, `device_id`, `email`, `ip`, and `user_agent`. Assert guide/security content preserves strings for `telemetry is diagnostic evidence only`, `provider/device proof remains advisory`, `direct shell/WebView token authority`, `refresh-token helpers`, `provider templates`, `passkey SDK wrappers`, and `native auth UI` as non-claims. Target state: denial sanitization and security-sensitive non-claims are merge-blocking proof, not manual review only.</action>
    <acceptance_criteria>
      - `mix test test/crosswake/proof/phase58_auth_diagnostics_closeout_test.exs test/crosswake/companions/sigra/telemetry_test.exs --trace` passes.
      - The proof fails if `allowed_detail_keys/0` includes any token/code/nonce/PKCE/credential/session/actor/org/device/IP/user-agent/provider payload/raw return key.
      - The proof fails if docs/security wording omits advisory provider/device proof or diagnostic-evidence-only telemetry posture.
      - Public shell denial reason `:step_up_required` remains compact; rich auth subcodes stay in Sigra denial vocabulary and safe detail maps.
    </acceptance_criteria>
  </task>
</tasks>

<threat_model>
| ID | STRIDE | Task | Asset | Threat | Control |
|----|--------|------|-------|--------|---------|
| T-58-05 | Elevation of Privilege | 58-02-01 | Session authority | Envelopes, locators, deep links, bridge events, provider payloads, or telemetry directly set authority. | STRIDE ledger rows cite `SessionAuthorityLane`, host-owned records, evaluator/route-gate proof, and all evidence-authority values false. |
| T-58-06 | Tampering | 58-02-01 | Handoff/step-up/auth-return records | Replay, double-consume, expiry bypass, revocation bypass, route mismatch, or session mismatch mutates authority. | Ledger cites host-owned records, atomic consume/audit/projection, and Phase 55-57 proof files. |
| T-58-07 | Information Disclosure | 58-02-02, 58-02-03 | Denials and telemetry | Security closeout passes while secret-bearing telemetry or denial keys are allowed. | Closeout verifier structural checks plus denial/telemetry proof secret bans. |
| T-58-08 | Repudiation | 58-02-02 | Closeout evidence | A shallow artifact with headings but no adversarial evidence passes. | Bounded table-header/content checks and proof tests require STRIDE rows, evidence refs, and no unresolved High/Critical findings. |
| T-58-09 | Spoofing | 58-02-03 | Support claims | Provider/device or native auth support is implied by closeout wording. | Security and docs proof preserve advisory/deferred non-claim strings. |
</threat_model>

<verification>
Run:

```sh
mix closeout.verify --security-only --security-closeout .planning/phases/58-auth-diagnostics-proof-and-security-closeout/58-SECURITY.md
mix test test/mix/tasks/closeout_verify_test.exs test/crosswake/proof/phase58_auth_diagnostics_closeout_test.exs test/crosswake/companions/sigra/telemetry_test.exs --trace
```
</verification>

<must_haves>
<truths>
- D-22: `58-SECURITY.md` is a bounded adversarial STRIDE review plus remediation ledger.
- D-23: Existing required security section set is preserved.
- D-24: Security surfaces use compact rows with adversarial scenario, control, evidence refs, residual risk, and disposition.
- D-25: Security review challenges whether evidence channels can set `SessionAuthorityLane`; answer remains no.
- D-26: Security review challenges replay/double-consume/expiry/revocation/route/session mismatch for host-owned records.
- D-27: Phoenix session renewal, CSRF posture, and LiveView invalidation stay host-owned and post-validation.
- D-28: Plug/controller and LiveView paths share auth semantics.
- D-29: Denial and telemetry sanitization excludes tokens, codes, credential/session/actor/org/device/IP/user-agent/provider payload/raw return target fields.
- D-30: Findings disposition blocks unresolved high/critical findings and keeps provider/device residual risk advisory when not claimed.
- D-31: Phoenix/Plug host-owned session mutation lesson is preserved.
- D-32: Ecto consume/audit/projection transaction lesson is preserved.
- D-33: LiveView and HTTP shared auth semantics lesson is preserved.
- D-34: Telemetry/Phoenix stable observability API lesson is preserved.
- D-35: OWASP/OAuth/WebAuthn generic user failure and sanitized operator detail lesson is preserved.
- D-36: Hotwire Native/AppAuth/mobile evidence-not-authority lesson is preserved.
- D-37: Crosswake prompt-corpus proof/diagnostics/support truth as product surface lesson is preserved.
</truths>
- Security closeout is a bounded STRIDE ledger plus findings disposition, not only checklist headings.
- High/Critical unresolved findings block closeout.
- Telemetry is diagnostic evidence only.
- Backend `SessionAuthorityLane` and host-owned records remain authority.
- Provider/device proof remains advisory unless explicitly promoted later.
- No raw tokens/codes/nonces/PKCE/credential/session/actor/org/device/IP/user-agent/provider payload/raw `return_to` values appear in telemetry or denials.
</must_haves>
