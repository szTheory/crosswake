---
phase: 79-v5-0-closeout-hermetic-verification
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/crosswake/planning/closeout_verifier.ex
  - .github/workflows/phase79-proof.yml
autonomous: true
requirements:
  - PROOF-01
must_haves:
  truths:
    - "Phase 79 correctly closes PROOF-01 via mix closeout.verify"
    - "CI runner executes deterministic proof lane using --local generated shells (D-05, D-06)"
    - "Android tests run without emulators (JVM only) (D-03, D-04)"
    - "iOS tests run without simulators (build only)"
    - "Dedicated phase79-proof.yml workflow for v5.0 E2E tests (D-01, D-02)"
  artifacts:
    - path: "lib/crosswake/planning/closeout_verifier.ex"
      provides: "Updated regex check for PROOF-01"
      min_lines: 40
    - path: ".github/workflows/phase79-proof.yml"
      provides: "New workflow for v5.0 verification"
      contains: "mix crosswake.gen.shell ios"
  key_links:
    - from: ".github/workflows/phase79-proof.yml"
      to: "script/verify_generated_ios_shell.sh"
      via: "shell execution"
      pattern: "./script/verify_generated_ios_shell.sh"
    - from: ".github/workflows/phase79-proof.yml"
      to: "script/verify_generated_android_shell.sh"
      via: "shell execution"
      pattern: "./script/verify_generated_android_shell.sh"
---

<objective>
Update closeout verification tooling and implement the CI proof lane for v5.0 standalone shell generation to ensure hermetic builds and `--local` dependency resolution.

Purpose: Completes Phase 79 and the v5.0 milestone by establishing strict CI safety for the new SPM and Maven extraction architectures without introducing emulator flakiness.
Output: An updated verifier and a new GitHub Actions workflow file for Phase 79.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@lib/crosswake/planning/closeout_verifier.ex
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update closeout verifier to support Phase 79</name>
  <files>lib/crosswake/planning/closeout_verifier.ex</files>
  <action>
    Update the `requirements_state_check/2` function. Find the regex checking for PROOF-01 and Phase 75: `~r/\|\s*(PROOF-01)\s*\|\s*Phase (75)\s*\|\s*Pending\s*\|/` and modify the phase group to `(75|79)` to correctly support PROOF-01 resolution for Phase 79.
  </action>
  <verify>
    <automated>grep -E -c "\((75\|79)\)" lib/crosswake/planning/closeout_verifier.ex</automated>
  </verify>
  <done>The regex pattern is updated to include 79.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Phase 79 proof workflow</name>
  <files>.github/workflows/phase79-proof.yml</files>
  <action>
    Create a new CI workflow `name: Phase 79 Proof` triggered on `pull_request` and `push` to `main`.
    Define a job `merge-blocking-v5-proof` running on `macos-15`.
    Include steps:
    1. Check out code (`actions/checkout@v4`).
    2. Set up Elixir (1.19.5 / OTP 27.3 via `erlef/setup-beam@v1`).
    3. Run `mix deps.get`.
    4. Run `mix crosswake.gen.shell ios --target tmp/ios_shell --local`.
    5. Execute `./script/verify_generated_ios_shell.sh` with environment variables: `CROSSWAKE_IOS_PROJECT_ROOT=tmp/ios_shell/native/ios/crosswake_shell`, `CROSSWAKE_IOS_LAUNCH_SIMULATOR=0`, and `CROSSWAKE_IOS_BUILD_FOR_TESTING=1`.
    6. Run `mix crosswake.gen.shell android --target tmp/android_shell --local`.
    7. Execute `./script/verify_generated_android_shell.sh` with environment variables: `CROSSWAKE_ANDROID_PROJECT_ROOT=tmp/android_shell/native/android/crosswake_shell` and `CROSSWAKE_ANDROID_CONNECTED_TESTS=0`.
  </action>
  <verify>
    <automated>test -f .github/workflows/phase79-proof.yml</automated>
  </verify>
  <done>The new GitHub Actions workflow correctly encapsulates hermetic verification of iOS and Android targets using --local overrides.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI Pipeline | Executes potentially untrusted PR code, isolated via GitHub Actions VM |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-79-01 | Tampering | `.github/workflows/phase79-proof.yml` | accept | Standard GitHub PR branch protections prevent unauthorized code from merging; isolated runner VM limits impact of malicious tests. |
</threat_model>

<verification>
- `mix test` passes (if any unit tests exist for closeout_verifier, they still pass).
- `lib/crosswake/planning/closeout_verifier.ex` supports Phase 79 for PROOF-01.
- `.github/workflows/phase79-proof.yml` exists with iOS and Android steps correctly configured for headless verification.
</verification>

<success_criteria>
- PROOF-01 verifier is correctly updated.
- Hermetic CI proof workflow accurately wires `--local` shells using existing bash scripts without introducing emulator wait times.
</success_criteria>

<output>
Create `.planning/phases/79-v5-0-closeout-hermetic-verification/79-01-SUMMARY.md` when done
</output>