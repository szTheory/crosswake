---
phase: 129-stable-companion-contract-surface
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - test/crosswake/proof/phase129_companion_contract_freeze_test.exs
autonomous: true
requirements: [SEAM-01, SEAM-03]
nyquist_compliant: true

must_haves:
  truths:
    - "A merge-blocking proof test asserts the Crosswake.Companion callback set is frozen at exactly 6 callbacks (MapSet equality, not membership)"
    - "The freeze test derives the contract module list from a single source of truth (mix.exs groups_for_modules), not a third hardcoded copy"
    - "The freeze test asserts each contract module carries a non-hidden @moduledoc and each of the 4 struct types carries @typedoc on t/0"
    - "The freeze test guards both edges of the Shell.Denial boundary: Denial absent from the contract group, Compatibility.Finding present"
  artifacts:
    - "test/crosswake/proof/phase129_companion_contract_freeze_test.exs (new, untagged, async: true)"
  key_links:
    - "Test reads Mix.Project.config()[:docs][:groups_for_modules] keyword :\"Companion Contract\" — wires to mix.exs config landed in plan 129-02"
    - "Reuses Crosswake.TestSupport.ProofAssertions.stable_id_message/7 for every assertion failure message"
---

<objective>
Write the merge-blocking freeze proof test FIRST (write-test-first forcing function, D-18). This test encodes the entire Phase 129 contract: the frozen 6-callback shape, non-hidden moduledocs + typedocs on the 5 contract modules, and the Shell.Denial boundary (Denial absent / Finding present).

This is intentionally written before the source promotions and mix.exs config (which land in plan 129-02). Until 129-02 lands, this test FAILS — that failure is the intended driver, not a defect (D-18). The test is untagged so the existing PR-gating proof lane auto-picks it; no new CI workflow is created.

Purpose: Encode SEAM-01 (moduledoc/typedoc/callback freeze) and SEAM-03 (Denial boundary) as executable CI gates before any source changes.
Output: test/crosswake/proof/phase129_companion_contract_freeze_test.exs
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/129-stable-companion-contract-surface/129-CONTEXT.md
@.planning/phases/129-stable-companion-contract-surface/129-RESEARCH.md
@.planning/phases/129-stable-companion-contract-surface/129-PATTERNS.md
@.planning/phases/129-stable-companion-contract-surface/129-VALIDATION.md

# Analogs to mirror (read before writing)
@test/crosswake/proof/phase65_diagnostic_export_seam_test.exs
@test/crosswake/proof/phase38_companion_contract_test.exs
@test/support/proof_assertions.ex
@test/crosswake/hex_page_test.exs
@lib/crosswake/companion.ex
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write the Phase 129 companion-contract freeze proof test</name>
  <files>test/crosswake/proof/phase129_companion_contract_freeze_test.exs</files>
  <read_first>
129-PATTERNS.md section "test/crosswake/proof/phase129_companion_contract_freeze_test.exs (CREATE)" — contains the verbatim module structure, all 6 tests, the @expected_callbacks attribute, the contract_modules/0 helper, and the @struct_contract_modules list. RESEARCH.md Findings 7, 8, 9, 10 (stable_id_message/7 signature, MapSet.equal? rationale, Code.fetch_docs/1 EEP-48 shape, Mix.Project.config derivation). test/crosswake/proof/phase38_companion_contract_test.exs for async/untagged structure. test/support/proof_assertions.ex for the stable_id_message/7 positional-arg contract.
  </read_first>
  <behavior>
    - Test "callbacks frozen": MapSet.new(Crosswake.Companion.behaviour_info(:callbacks)) MapSet.equal? the @expected_callbacks set of exactly 6 {name, arity} tuples (companion_id/0, enabled?/1, route_gated?/2, kill_switch_active?/1, validate_dependency/0, report_state/0). Both additions AND removals fail (equality, not membership).
    - Test "moduledoc non-hidden": for every module in contract_modules(), Code.fetch_docs/1 returns a docs_v1 tuple whose moduledoc element is_map (rejects :none and :hidden).
    - Test "typedoc on t/0": for each of the 4 struct modules (State, Finding, Target, RouteEntry), the docs list from Code.fetch_docs/1 contains a {:type, :t, 0} entry whose doc is a map.
    - Test "Denial absent": Crosswake.Shell.Denial is NOT in contract_modules().
    - Test "Finding present": Crosswake.Compatibility.Finding IS in contract_modules().
    - Test "guide exists": guides/companion_contract.md exists on disk.
    - Until plan 129-02 lands, contract_modules() returns [] (the "Companion Contract" group does not yet exist), so the moduledoc/typedoc loops pass vacuously, "Finding present" FAILS, and "guide exists" FAILS. That red state is the intended write-test-first driver (D-18).
  </behavior>
  <action>
Create test/crosswake/proof/phase129_companion_contract_freeze_test.exs as module Crosswake.Proof.Phase129CompanionContractFreezeTest using ExUnit.Case with async: true (read-only reflection — no Application.put_env, unlike phase38/phase65 which are async: false). Leave the test UNTAGGED so the existing PR-gating proof lane auto-includes it; do NOT add @tag decorators and do NOT create any CI workflow file. alias Crosswake.TestSupport.ProofAssertions.

Define @expected_callbacks as a MapSet of the 6 frozen {name, arity} tuples confirmed against lib/crosswake/companion.ex (D-13, RESEARCH Finding 1). Add a code comment above it explaining that equality (MapSet.equal?) catches both additions AND removals, and that any intentional shape change must edit @expected_callbacks here AND the @callback defs in companion.ex in the SAME PR (D-12, D-17).

Define a private contract_modules/0 helper that reads Mix.Project.config()[:docs][:groups_for_modules] and Keyword.get(:"Companion Contract", []) — single source of truth per D-15, precedent at hex_page_test.exs line ~120. Define @struct_contract_modules as the explicit list of the 4 struct-bearing modules for the typedoc test.

Implement the 6 tests exactly per 129-PATTERNS.md: (1) callback freeze via MapSet.equal?; (2) moduledoc non-hidden via match? on is_map(moduledoc) over contract_modules(); (3) @typedoc present on t/0 via Enum.find_value over the Code.fetch_docs docs list filtering {:type, :t, 0}; (4) refute Shell.Denial in contract_modules(); (5) assert Compatibility.Finding in contract_modules(); (6) File.exists? on guides/companion_contract.md.

Every assertion's failure message MUST use ProofAssertions.stable_id_message/7 with all 7 positional string args and posture :merge_blocking. Use stable-id slugs of the form proof.seam_01.* and proof.seam_03.* per the slugs in 129-PATTERNS.md. On callback drift the hint must instruct editing @expected_callbacks AND companion.ex @callback defs in the SAME PR (D-17). On hidden/missing moduledoc the hint must point at guides/companion_contract.md and SEAM-01.

Do NOT use Code.Typespec.fetch_callbacks/1 full-AST comparison — name/arity via behaviour_info is the breaking-change boundary (D-14).
  </action>
  <verify>
    <automated>mix compile 2>&1 | tail -5 && mix test test/crosswake/proof/phase129_companion_contract_freeze_test.exs 2>&1 | tail -25</automated>
  </verify>
  <acceptance_criteria>
The test file compiles cleanly. When run BEFORE plan 129-02 lands, the suite executes (no compile/load errors) and reports failures on the "Finding present" and "guide exists" tests with merge-blocking stable-id messages — confirming the write-test-first forcing function is wired. No @tag decorators present (untagged). async: true is set. @expected_callbacks contains exactly the 6 frozen tuples. contract_modules/0 reads from Mix.Project.config, not a hardcoded list.
  </acceptance_criteria>
  <done>
phase129_companion_contract_freeze_test.exs exists, compiles, is untagged + async: true, contains all 6 assertions with stable_id_message/7 failure messages, derives its module list from Mix.Project.config()[:docs][:groups_for_modules], and fails (as intended) on Finding-present + guide-exists until 129-02 lands.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | This plan adds a read-only ExUnit reflection test. No user input, no network, no auth, no crypto, no data storage, no runtime surface change. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-129-01 | Tampering | Contract freeze test | low | accept | Honest assessment per RESEARCH "Security Domain: Not applicable" — the test is the integrity control, not a target. No L1-relevant threat above the `high` blocking threshold. No package installs in this plan (no T-129-SC). |
</threat_model>

<verification>
- `mix test test/crosswake/proof/phase129_companion_contract_freeze_test.exs` runs and reports the expected intentional failures (Finding-present, guide-exists) before 129-02.
- After 129-02 lands, the same command is GREEN (verified in 129-02).
- File is untagged (no `@tag` lines) and `async: true`.
</verification>

<success_criteria>
- Freeze test encodes SEAM-01 (callback freeze + moduledoc/typedoc) and SEAM-03 (Denial boundary) as merge-blocking assertions.
- Module list derived from single source of truth (D-15).
- Test compiles and is auto-picked by the existing proof lane (untagged).
</success_criteria>

<output>
Create `.planning/phases/129-stable-companion-contract-surface/129-01-SUMMARY.md` when done.
</output>
