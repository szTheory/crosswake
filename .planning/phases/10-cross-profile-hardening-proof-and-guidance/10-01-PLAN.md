---
phase: 10-cross-profile-hardening-proof-and-guidance
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/crosswake/doctor/doctor.ex
  - test/crosswake/doctor/doctor_test.exs
autonomous: true
requirements:
  - HARD-01
must_haves:
  truths:
    - "Doctor catches unsupported capability combinations and warns or fails."
    - "Adopters are informed of boundaries at compile/check time rather than native crash time."
  artifacts:
    - path: "lib/crosswake/doctor/doctor.ex"
      provides: "New diagnostic checks for exemplar boundaries"
    - path: "test/crosswake/doctor/doctor_test.exs"
      provides: "Tests for the boundary checks"
  key_links:
    - from: "lib/crosswake/doctor/doctor.ex"
      to: "test/crosswake/doctor/doctor_test.exs"
      via: "ExUnit tests"
---

<objective>
Upgrade `mix crosswake.doctor` to enforce boundary constraints surfaced by the exemplars without expanding the Elixir API.

Purpose: Contract Locking Phase requires enforcing current boundaries rather than inflating `RoutePolicy` to accommodate rough edges. Adopters should fail-fast at compile/check time if they attempt unsupported feature combinations.
Output: Upgraded Doctor module and passing ExUnit tests.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-cross-profile-hardening-proof-and-guidance/10-CONTEXT.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add boundary validation to the Doctor</name>
  <files>lib/crosswake/doctor/doctor.ex, test/crosswake/doctor/doctor_test.exs</files>
  <behavior>
    - Test 1: Doctor correctly catches an `:offline_island` route requesting `background_sync` capability and issues a warning/error.
    - Test 2: Doctor catches other relevant cross-profile unsupported capability boundary attempts (e.g., SaaS portal mixing with unsupported generic plugin bus patterns if applicable).
  </behavior>
  <action>
    Implement validation checks within `lib/crosswake/doctor/doctor.ex` to identify known feature gaps exposed by the Phase 7, 8, and 9 exemplars. For instance, catch an offline island route requesting `background_sync` capability, and emit a `check()` with severity `:error` or `:warning` specifying the unsupported capability and advising users to check the Boundary Warnings in the offline guide. Add the corresponding tests in `test/crosswake/doctor/doctor_test.exs`. As per PATTERNS.md, use the existing `check/6` or similar diagnostic loop pattern within `doctor.ex`.
  </action>
  <verify>
    <automated>mix test test/crosswake/doctor/doctor_test.exs</automated>
  </verify>
  <done>Doctor correctly identifies bounded capabilities and fails/warns with appropriate hints. Tests pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Policy Compiler -> Doctor | The `mix crosswake.doctor` task consumes the declarative `RoutePolicy`. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-01 | Information Disclosure | `lib/crosswake/doctor/doctor.ex` | mitigate | Explicitly fail and warn if policies combine sensitive capabilities with insecure execution modes, preventing hidden security leaks at runtime. |
</threat_model>

<verification>
mix test test/crosswake/doctor/doctor_test.exs
</verification>

<success_criteria>
`mix crosswake.doctor` successfully raises warnings/errors on defined capability boundary violations, and these are covered by ExUnit tests.
</success_criteria>

<output>
After completion, create `.planning/phases/10-cross-profile-hardening-proof-and-guidance/10-cross-profile-hardening-proof-and-guidance-01-SUMMARY.md`
</output>
