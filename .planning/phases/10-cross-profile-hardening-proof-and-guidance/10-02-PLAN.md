---
phase: 10-cross-profile-hardening-proof-and-guidance
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - script/verify_saas_profile.sh
  - script/verify_selective_native.sh
  - script/verify_local_first.sh
  - .github/workflows/phase10-proof.yml
autonomous: true
requirements:
  - HARD-02
must_haves:
  truths:
    - "Proof lanes are isolated into per-profile bash scripts."
    - "Native compilation happens only against the single flagship host."
    - "CI does not suffer from combinatorial explosion."
  artifacts:
    - path: "script/verify_saas_profile.sh"
      provides: "Hermetic proof lane for SaaS profile"
    - path: "script/verify_selective_native.sh"
      provides: "Hermetic proof lane for Selective Native profile"
    - path: "script/verify_local_first.sh"
      provides: "Hermetic proof lane for Local-First profile"
    - path: ".github/workflows/phase10-proof.yml"
      provides: "CI workflow that orchestrates the hermetic scripts"
  key_links:
    - from: ".github/workflows/phase10-proof.yml"
      to: "script/verify_*.sh"
      via: "bash execution"
---

<objective>
Establish isolated, per-profile verification scripts for the exemplar lanes, and wire them up in a new Phase 10 CI workflow.

Purpose: To avoid monolithic kitchen-sink testing and combinatorial explosion in native build CI. ExUnit is used to test generation for all profiles, while native build is restricted to the flagship example host.
Output: Three hermetic profile-verification bash scripts and one CI workflow.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-cross-profile-hardening-proof-and-guidance/10-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create per-profile hermetic bash scripts</name>
  <files>script/verify_saas_profile.sh, script/verify_selective_native.sh, script/verify_local_first.sh</files>
  <action>
    Create three distinct verification scripts: `script/verify_saas_profile.sh`, `script/verify_selective_native.sh`, and `script/verify_local_first.sh`. 
    Each script must use the boilerplate `set -euo pipefail`, switch to the project root, and execute the relevant ExUnit test lane (e.g., `mix test test/crosswake/proof/phase7_saas_lane_test.exs` or equivalent for each). Ensure scripts are marked executable (`chmod +x`). Do NOT include `xcodebuild` or `gradlew` inside these profile scripts. As per PATTERNS.md, use `script/verify_phase5_example_hosts.sh` as the analog for constructing these hermetic scripts.
  </action>
  <verify>
    <automated>bash script/verify_saas_profile.sh &amp;&amp; bash script/verify_selective_native.sh &amp;&amp; bash script/verify_local_first.sh</automated>
  </verify>
  <done>Scripts execute successfully and pass their respective ExUnit tests.</done>
</task>

<task type="auto">
  <name>Task 2: Configure Phase 10 CI workflow</name>
  <files>.github/workflows/phase10-proof.yml</files>
  <action>
    Create `.github/workflows/phase10-proof.yml` (based on `phase5-proof.yml` patterns). The workflow should set up Elixir/Java, get dependencies, and then execute the three new profile bash scripts (`script/verify_saas_profile.sh`, `script/verify_selective_native.sh`, `script/verify_local_first.sh`) in addition to the flagship host tests (`script/verify_generated_ios_shell.sh` and `script/verify_generated_android_shell.sh`).
  </action>
  <verify>
    <automated>yamlfmt .github/workflows/phase10-proof.yml || true</automated>
  </verify>
  <done>Workflow file is created, valid YAML, and contains the correct execution steps.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI -> Test Suite | GitHub actions executes code within a controlled runner environment. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-02 | Elevation of Privilege | `.github/workflows/phase10-proof.yml` | accept | Tests run in sandboxed CI without elevated host privileges. |
</threat_model>

<verification>
Ensure all `verify_*.sh` scripts run successfully locally via Bash.
</verification>

<success_criteria>
Profile testing is decoupled into isolated scripts. CI matrix avoids compiling native shells for every profile layout.
</success_criteria>

<output>
After completion, create `.planning/phases/10-cross-profile-hardening-proof-and-guidance/10-cross-profile-hardening-proof-and-guidance-02-SUMMARY.md`
</output>
