---
phase: 64-runtime-line-policy-contract-support-truth-taxonomy
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/crosswake/runtime_line/rebuild_policy.ex
autonomous: true
requirements: [RLINE-01, RLINE-02]
nyquist_compliant: true

must_haves:
  truths:
    - "An adopter can classify any of the 8 change classes as OTA-safe or rebuild-required"
    - "Capability-axis classes derive their verdict from Capability.rebuild, never from the change-class label"
    - "System-level classes (sdk_floor_bump, privacy_manifest_entry) map via a locked closed set"
    - ":companion_required is never treated as OTA-safe"
  artifacts:
    - path: "lib/crosswake/runtime_line/rebuild_policy.ex"
      provides: "classify/2, diff/2, rebuild_required?/1 — the rebuild/OTA policy contract"
      contains: "defmodule Crosswake.RuntimeLine.RebuildPolicy"
  key_links:
    - from: "RebuildPolicy.classify/2"
      to: "Capability.rebuild"
      via: "pattern match on capability.rebuild for capability-axis classes"
      pattern: "capability.rebuild|rebuild_required\\?"
    - from: "RebuildPolicy"
      to: "@system_rebuild_classes"
      via: "closed-set module attribute for system classes"
      pattern: "@system_rebuild_classes"
---

<objective>
Implement the new public module `Crosswake.RuntimeLine.RebuildPolicy` (D-01, D-02, D-03, D-04, D-05, D-06): the pure-Elixir contract that classifies any of the 8 change classes as `:ota_safe` or `{:rebuild_required, :native_shell | :companion_shell}`, deriving capability-axis verdicts ENTIRELY from `Capability.rebuild` and mapping system-level classes via a locked `@system_rebuild_classes` closed set. This is the RLINE-01 engine and the co-truth anchor the RLINE-02 proof asserts against `SupportMatrix.action_classes()`.

Purpose: Locks the rebuild/OTA boundary in one narrow, derived, additive module — no static rebuild registry (would drift from `Capability.rebuild`), no manifest schema field.
Output: One new module file with `classify/2`, `diff/2`, `rebuild_required?/1`, the `change_class()`/`verdict()` types, and the `@system_rebuild_classes` attribute.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/64-runtime-line-policy-contract-support-truth-taxonomy/64-CONTEXT.md
@.planning/phases/64-runtime-line-policy-contract-support-truth-taxonomy/64-RESEARCH.md
@.planning/phases/64-runtime-line-policy-contract-support-truth-taxonomy/64-PATTERNS.md

<interfaces>
<!-- D-04 locked public API (signatures are discretion; preserve intent). -->
@type change_class :: :bridge_schema_change | :capability_family_add | :permission_add | :entitlement_add | :sdk_floor_bump | :privacy_manifest_entry | :push_capability_change | :url_scheme_change
@type verdict :: :ota_safe | {:rebuild_required, :native_shell | :companion_shell}
@system_rebuild_classes [:sdk_floor_bump, :privacy_manifest_entry]   # closed set; system classes -> {:rebuild_required, :native_shell}

@spec classify(change_class(), Capability.t() | nil) :: verdict()
@spec diff(Root.t(), Root.t()) :: [{change_class(), verdict()}]
@spec rebuild_required?(Capability.rebuild()) :: boolean()

Capability.rebuild :: :none | :native_required | :companion_required   (existing field — the source of truth)
  :native_required    -> {:rebuild_required, :native_shell}
  :companion_required -> {:rebuild_required, :companion_shell}
  :none               -> :ota_safe
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement Crosswake.RuntimeLine.RebuildPolicy (classify/2, diff/2, rebuild_required?/1)</name>
  <files>lib/crosswake/runtime_line/rebuild_policy.ex</files>
  <behavior>
    - `rebuild_required?(:none) == false`; `rebuild_required?(:native_required) == true`; `rebuild_required?(:companion_required) == true`.
    - For a capability-axis change class (e.g. `:capability_family_add`, `:bridge_schema_change`, `:permission_add`, `:entitlement_add`, `:push_capability_change`, `:url_scheme_change`) with `%Capability{rebuild: :native_required}` → `{:rebuild_required, :native_shell}`; with `:companion_required` → `{:rebuild_required, :companion_shell}`; with `:none` → `:ota_safe`.
    - For system classes `:sdk_floor_bump` and `:privacy_manifest_entry` (capability arg `nil`) → `{:rebuild_required, :native_shell}`.
    - `classify/2` never returns `:ota_safe` for `:companion_required` (D-06b).
    - `diff/2` returns a list of `{change_class, verdict}` and does NOT assert whether a binary already shipped (D-06c) — it classifies only.
  </behavior>
  <read_first>
    - lib/crosswake/native_escape/contract.ex (lines 1-12 module header + module-attribute constants pattern; lines ~126-130 the `unless ... in @enum raise ArgumentError` guard pattern)
    - lib/crosswake/bridge/contract.ex (lines 1-22 module-constant + typed `@spec` public API style; ~98-108 the small public-predicate functions)
    - lib/crosswake/manifest/types.ex — the `Capability` struct definition + its `rebuild` field type (`:none | :native_required | :companion_required`) and the `Root` struct type (for the `diff/2` spec)
    - .planning/phases/64-runtime-line-policy-contract-support-truth-taxonomy/64-PATTERNS.md (Pattern 7 RebuildPolicy contract-module style + Pitfall 5: classify must key off Capability.rebuild, never the label)
  </read_first>
  <action>
    Create `lib/crosswake/runtime_line/rebuild_policy.ex` with `defmodule Crosswake.RuntimeLine.RebuildPolicy`, an honest `@moduledoc` describing derivation from the existing `native_runtime_version` axis (no runtime config; new system classes go through a phase), `alias Crosswake.Manifest.Types.Capability` and `alias Crosswake.Manifest.Types.Root`, the `@system_rebuild_classes [:sdk_floor_bump, :privacy_manifest_entry]` closed-set attribute, and the `@type change_class` / `@type verdict` types per the interface block.

    Implement `rebuild_required?/1` as a public predicate over `Capability.rebuild()`: `:none → false`, `:native_required → true`, `:companion_required → true`.

    Implement `classify/2`: when `change_class in @system_rebuild_classes`, return `{:rebuild_required, :native_shell}` (capability arg may be nil). Otherwise (the 6 capability-axis classes) branch on `capability.rebuild`: `:native_required → {:rebuild_required, :native_shell}`, `:companion_required → {:rebuild_required, :companion_shell}`, `:none → :ota_safe`. Add a guard that raises `ArgumentError` (mirror native_escape contract) when a capability-axis class is passed with `capability == nil` — the source of truth (`Capability.rebuild`) is required and must never be inferred from the label alone (Pitfall 5 / D-06a).

    Implement `diff/2` over two `Root.t()` manifests: detect the change classes present between them and return `[{change_class(), verdict()}]` by feeding each detected change through `classify/2`. Document (in `@doc`/`@moduledoc`) that `diff/2` is tooling/doctor input, NOT a release-gate oracle, and does not know whether a binary with the new `native_runtime_version` already shipped (D-06c).

    Keep the public surface to exactly these three functions plus any necessary private helpers.
  </action>
  <verify>
    <automated>mix compile --warnings-as-errors 2>&1 | tail -5; grep -q "defmodule Crosswake.RuntimeLine.RebuildPolicy" lib/crosswake/runtime_line/rebuild_policy.ex && grep -q "@system_rebuild_classes \[:sdk_floor_bump, :privacy_manifest_entry\]" lib/crosswake/runtime_line/rebuild_policy.ex</automated>
  </verify>
  <acceptance_criteria>
    - `lib/crosswake/runtime_line/rebuild_policy.ex` defines `Crosswake.RuntimeLine.RebuildPolicy`
    - Module defines `@spec classify(change_class(), Capability.t() | nil) :: verdict()`, `@spec diff(Root.t(), Root.t())`, `@spec rebuild_required?(Capability.rebuild()) :: boolean()`
    - `@system_rebuild_classes` is `[:sdk_floor_bump, :privacy_manifest_entry]`
    - `classify/2` for `:companion_required` returns `{:rebuild_required, :companion_shell}` (never `:ota_safe`)
    - `classify/2` for a capability-axis class with a `:none`-rebuild capability returns `:ota_safe`
    - `classify/2` for `:sdk_floor_bump`/`:privacy_manifest_entry` returns `{:rebuild_required, :native_shell}`
    - `classify/2` raises `ArgumentError` when a capability-axis class is given a nil capability (no label-only classification)
    - `mix test test/crosswake/proof/phase64_runtime_line_policy_test.exs --only rline_01` passes (the RLINE-01 classify/predicate assertions)
    - `mix compile --warnings-as-errors` exits 0
  </acceptance_criteria>
  <done>RebuildPolicy classifies all 8 change classes correctly — capability-axis off `Capability.rebuild`, system-level off the closed set — never label-only, never companion-as-OTA-safe; the `:rline_01` proof segment passes and `diff/2` is documented as non-oracle.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| change description → rebuild verdict | A wrong verdict (OTA-safe when a rebuild is required) is the silent-native-break footgun |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-64-08 | Spoofing | classify/2 capability-axis path | mitigate | Keys off `Capability.rebuild`, raises on nil capability for capability-axis classes; proof `:rline_01` covers all 8 classes |
| T-64-09 | Spoofing | classify/2 companion handling | mitigate | `:companion_required → {:rebuild_required, :companion_shell}`, never `:ota_safe` (D-06b); proof asserts |
| T-64-10 | Repudiation/Elevation | diff/2 misuse as release gate | mitigate | `@moduledoc`/`@doc` explicitly states diff/2 is tooling input, not a release-gate oracle (D-06c) |
| T-64-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this phase (pure Elixir) |
</threat_model>

<verification>
`mix compile --warnings-as-errors` exits 0; `mix test test/crosswake/proof/phase64_runtime_line_policy_test.exs --only rline_01` passes. The co-truth parity (`:rline_02`) goes green only once SupportMatrix data lands (plan 04) since it cross-checks `action_classes()`.
</verification>

<success_criteria>
The rebuild/OTA boundary is locked in a narrow derived module: 8 change classes classified, capability-axis off `Capability.rebuild`, system-level off a closed set, no label-only classification, no companion-as-OTA-safe, diff documented as non-oracle.
</success_criteria>

<output>
Create `.planning/phases/64-runtime-line-policy-contract-support-truth-taxonomy/64-03-SUMMARY.md` when done
</output>
