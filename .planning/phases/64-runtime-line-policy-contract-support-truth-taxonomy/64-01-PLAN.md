---
phase: 64-runtime-line-policy-contract-support-truth-taxonomy
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - test/crosswake/proof/phase64_runtime_line_policy_test.exs
autonomous: true
requirements: [RLINE-01, RLINE-02, RLINE-03, RLINE-04, RLINE-05]
nyquist_compliant: true
wave_0: true

must_haves:
  truths:
    - "A hermetic proof lane file exists tagging every RLINE-01..05 assertion"
    - "The proof lane RED-fails (compile or assertion) until the contract/data/render plans land"
  artifacts:
    - path: "test/crosswake/proof/phase64_runtime_line_policy_test.exs"
      provides: "Wave-0 failing proof scaffold for the whole phase"
      contains: "Crosswake.Proof.Phase64RuntimeLinePolicyTest"
  key_links:
    - from: "test/crosswake/proof/phase64_runtime_line_policy_test.exs"
      to: "Crosswake.RuntimeLine.RebuildPolicy / SupportMatrix / Types"
      via: "alias + assertion calls"
      pattern: "RebuildPolicy|rebuild_matrix|verification_method"
---

<objective>
Create the Wave-0 hermetic proof lane scaffold `test/crosswake/proof/phase64_runtime_line_policy_test.exs` that encodes every RLINE-01..05 assertion as a tagged test. This is the Nyquist Wave-0 gap: the file MUST exist (and RED-fail) before the contract/data/render plans implement the modules it asserts against. It is the single automated proof artifact all downstream tasks make green.

Purpose: Tests-first. Every downstream plan task points its `<verify>` at this file; the proof lane is the canonical green-up target and the security-invariant guard for the phase.
Output: One new hermetic ExUnit proof file with tagged tests (`:rline_01` … `:rline_05`) covering the full Validation Per-Task map.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/64-runtime-line-policy-contract-support-truth-taxonomy/64-CONTEXT.md
@.planning/phases/64-runtime-line-policy-contract-support-truth-taxonomy/64-PATTERNS.md
@.planning/phases/64-runtime-line-policy-contract-support-truth-taxonomy/64-VALIDATION.md

<interfaces>
<!-- Contracts the proof lane asserts against. These land in plans 02-05; the scaffold references them and RED-fails until then. -->

Crosswake.RuntimeLine.RebuildPolicy (plan 03):
  @spec classify(change_class(), Capability.t() | nil) :: :ota_safe | {:rebuild_required, :native_shell | :companion_shell}
  @spec rebuild_required?(Capability.rebuild()) :: boolean()
  @spec diff(Root.t(), Root.t()) :: [{change_class(), verdict()}]
  change_class() = :bridge_schema_change | :capability_family_add | :permission_add | :entitlement_add | :sdk_floor_bump | :privacy_manifest_entry | :push_capability_change | :url_scheme_change

Crosswake.SupportMatrix (plan 04):
  action_classes/0 -> [%ActionClassEntry{action_class, rebuild_required: boolean}]  (co-truth source; "native_shell"/"companion_native"/"provider_adapter" = true; "docs_only"/"route_manifest"/"compatibility" = false)
  rebuild_matrix/1 -> [Types.RuntimeLineRow.t()]
  promotion_rules/0 (existing) gains rows claim_id "shell.android.jvm_hermetic" + "shell.android.device_verified"
  validate/1 -> [] | [errors]  (rejects :device_verified on a CI-only CapabilitySupportEntry)

Crosswake.Manifest.Types (plan 02):
  %Compatibility{} keys EXACTLY: manifest_schema_version, bridge_protocol_version, native_runtime_version, supported_manifest_sources, remote_updates  (manifest_schema_version == "1.0.0")
  %CapabilitySupportEntry{verification_method: :none}  (new defaulted field)
  %PromotionRuleEntry{required_verification_method: :none}  (new defaulted field)
  %RuntimeLineRow{runtime_line, capability_surface, change_class, ota_safe, rebuild_required, evidence_tier}  (new struct)

Doctor (plan 05): `mix crosswake.doctor` human output contains "rebuild & compatibility matrix:" + "evidence posture:" lines; `--format json` output has "rebuild_matrix" + "evidence_posture" keys.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold the hermetic phase-64 proof lane covering all RLINE-01..05 assertions</name>
  <files>test/crosswake/proof/phase64_runtime_line_policy_test.exs</files>
  <read_first>
    - test/crosswake/proof/phase52_operator_truth_test.exs (the exact hermetic-lane template: `use ExUnit.Case, async: false`, `import ExUnit.CaptureIO`, `alias Crosswake.TestSupport.ProofAssertions`, NO `@moduletag :requires_example_host`)
    - test/support/proof_assertions.ex (available helpers: stable_id_message/7, assert_normalized_json_fixture/4, assert_contains_exact/4)
    - .planning/phases/64-runtime-line-policy-contract-support-truth-taxonomy/64-VALIDATION.md (the Per-Task Verification Map — every row below becomes a tagged test)
    - .planning/phases/64-runtime-line-policy-contract-support-truth-taxonomy/64-PATTERNS.md (the phase64 proof-lane section, lines ~573-676, with the RLINE-02 struct-key assertion sketch and CaptureIO-over-doctor pattern)
  </read_first>
  <action>
    Create module `Crosswake.Proof.Phase64RuntimeLinePolicyTest` with `use ExUnit.Case, async: false`, `import ExUnit.CaptureIO`, and aliases `Crosswake.TestSupport.ProofAssertions`, `Crosswake.RuntimeLine.RebuildPolicy`, `Crosswake.SupportMatrix`, `Crosswake.Manifest.Types`. Do NOT add `@moduletag :requires_example_host` — this lane is hermetic.

    Author one or more tagged tests per VALIDATION row, using `@tag :rline_01` … `@tag :rline_05`:
    - `:rline_01` — assert `RebuildPolicy.rebuild_required?/1` returns false for `:none` and true for `:native_required` and `:companion_required`; assert `RebuildPolicy.classify/2` returns `{:rebuild_required, :native_shell}` for a capability-axis class with a `Capability{rebuild: :native_required}`, `{:rebuild_required, :companion_shell}` for `:companion_required`, `:ota_safe` for `:none`, and `{:rebuild_required, :native_shell}` for system classes `:sdk_floor_bump` and `:privacy_manifest_entry` (capability arg nil). Cover all 8 change-class atoms.
    - `:rline_02` — assert the `%Types.Compatibility{}` struct key set equals exactly `[:manifest_schema_version, :bridge_protocol_version, :native_runtime_version, :supported_manifest_sources, :remote_updates]` (sort both sides; subtract `:__struct__`); assert a canonical manifest's `compatibility.manifest_schema_version == "1.0.0"`; assert co-truth parity: for each `SupportMatrix.action_classes()` entry, when `rebuild_required` is true the classification path yields `{:rebuild_required, _}` and when false yields `:ota_safe`.
    - `:rline_03` — assert `SupportMatrix.rebuild_matrix(SupportMatrix.canonical())` returns a non-empty `[%Types.RuntimeLineRow{}]` whose `runtime_line` values include `"1.x"`; capture `mix crosswake.doctor` human output and assert it contains `"rebuild & compatibility matrix:"`; capture `--format json` output, `Jason.decode!` it, and assert a `"rebuild_matrix"` key holding a list; assert structural parity (every row's `runtime_line` present in both human and JSON output).
    - `:rline_04` — assert a `CapabilitySupportEntry` with `verification_method: :device_verified` renders the literal `"device-verified"` and one with `:jvm_hermetic` renders `"jvm-hermetic (CI only)"` via the doctor formatter path; assert `SupportMatrix.validate/1` returns a non-empty error list when an otherwise-valid matrix carries a CI-only entry claiming `:device_verified`; capture doctor output and assert the `evidence posture:` line contains `ios=device-verified` and `android=jvm-hermetic (CI only)`.
    - `:rline_05` — assert `SupportMatrix.promotion_rules()` contains a row with `claim_id == "shell.android.jvm_hermetic"`, `minimum_consecutive_passes == 3`, `required_verification_method == :jvm_hermetic`; a row `claim_id == "shell.android.device_verified"` with `required_verification_method == :device_verified` and a `demotion_trigger` string mentioning Phases 67/68 and that jvm_hermetic must not be read as device_verified; assert the canonical Android `SupportEntry.status == :verification_required` (Android NOT promoted in this phase).

    Use `capture_io(fn -> Mix.Task.reenable("crosswake.doctor"); Mix.Task.run("crosswake.doctor", [...]) end)` for the doctor-render assertions (mirror phase52). Where a stable-id message aids drift reporting, use `ProofAssertions.stable_id_message/7` with posture `:merge_blocking`.

    This file WILL fail to compile / RED-fail now because `RebuildPolicy`, the new fields, `RuntimeLineRow`, `rebuild_matrix/1`, the Android rows, and the doctor render do not exist yet. That is the intended Wave-0 RED state — do NOT stub the production modules from here.
  </action>
  <verify>
    <automated>test -f test/crosswake/proof/phase64_runtime_line_policy_test.exs && grep -q "Phase64RuntimeLinePolicyTest" test/crosswake/proof/phase64_runtime_line_policy_test.exs && for t in rline_01 rline_02 rline_03 rline_04 rline_05; do grep -q ":$t" test/crosswake/proof/phase64_runtime_line_policy_test.exs || exit 1; done</automated>
  </verify>
  <acceptance_criteria>
    - test/crosswake/proof/phase64_runtime_line_policy_test.exs exists and defines `Crosswake.Proof.Phase64RuntimeLinePolicyTest`
    - File contains `@tag :rline_01`, `:rline_02`, `:rline_03`, `:rline_04`, `:rline_05`
    - File does NOT contain `@moduletag :requires_example_host`
    - File aliases `Crosswake.RuntimeLine.RebuildPolicy`, `Crosswake.SupportMatrix`, `Crosswake.Manifest.Types`, `Crosswake.TestSupport.ProofAssertions`
    - Running `mix test test/crosswake/proof/phase64_runtime_line_policy_test.exs` RED-fails (compile error or assertion failure) — this is the intended Wave-0 state
    - File asserts the exact 5-key `Compatibility` enumeration and `manifest_schema_version == "1.0.0"`
    - File asserts canonical Android `SupportEntry.status == :verification_required`
  </acceptance_criteria>
  <done>The phase-64 proof lane file exists, encodes all 16 VALIDATION rows as `:rline_0N`-tagged tests, is hermetic (no host tag), and RED-fails because the production contracts it references are not yet implemented.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| contract claim → operator-facing render | Evidence labels and rebuild verdicts cross from data into doctor output an operator trusts |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-64-01 | Spoofing/Elevation | verification_method labeling | mitigate | Proof lane (`:rline_04`) asserts `validate/1` rejects `:device_verified` on a CI-only entry and that the formatter never promotes `:jvm_hermetic` to `device-verified` |
| T-64-02 | Spoofing | RebuildPolicy.classify/2 | mitigate | Proof lane (`:rline_01`,`:rline_02`) asserts classification keys off `Capability.rebuild` + co-truth parity with `action_classes()` |
| T-64-03 | Elevation | Android SupportEntry.status | mitigate | Proof lane (`:rline_05`) asserts Android stays `:verification_required` (D-20) |
| T-64-04 | Information Disclosure | human vs JSON doctor | mitigate | Proof lane (`:rline_03`) asserts both formatters render the same `rebuild_matrix` row set |
| T-64-SC | Tampering | npm/pip/cargo installs | accept | No package-manager installs in this phase (pure Elixir, no new hex deps per RESEARCH Package Legitimacy Audit) |
</threat_model>

<verification>
`mix test test/crosswake/proof/phase64_runtime_line_policy_test.exs` RED-fails now (Wave-0). The full suite goes green only after plans 02-05 land. Do not weaken assertions to make it pass early.
</verification>

<success_criteria>
The proof lane exists, is hermetic, tags every RLINE requirement, and is the single green-up target for the phase. RED state is expected and correct at end of this plan.
</success_criteria>

<output>
Create `.planning/phases/64-runtime-line-policy-contract-support-truth-taxonomy/64-01-SUMMARY.md` when done
</output>
