---
phase: 64-runtime-line-policy-contract-support-truth-taxonomy
plan: 04
type: execute
wave: 2
depends_on: ["64-02", "64-03"]
files_modified:
  - lib/crosswake/support_matrix/support_matrix.ex
autonomous: true
requirements: [RLINE-02, RLINE-03, RLINE-04, RLINE-05]
nyquist_compliant: true

must_haves:
  truths:
    - "SupportMatrix exposes a rebuild & compatibility matrix as a typed RuntimeLineRow list via rebuild_matrix/1"
    - "The matrix has one row per major runtime-line band (1.x, 2.x), not per patch or capability family"
    - "iOS capability entries carry verification_method :device_verified; Android carry :jvm_hermetic"
    - "validate/1 rejects :device_verified on a CI-only entry"
    - "Two Android promotion rows exist as criteria-as-data; Android SupportEntry.status stays :verification_required"
  artifacts:
    - path: "lib/crosswake/support_matrix/support_matrix.ex"
      provides: "@rebuild_matrix_rows + rebuild_matrix/1 + verification_method validation + 2 Android promotion rows"
      contains: "def rebuild_matrix"
  key_links:
    - from: "@rebuild_matrix_rows"
      to: "Types.new_runtime_line_row/1"
      via: "module attribute building typed rows"
      pattern: "new_runtime_line_row"
    - from: "canonical/1"
      to: "rebuild_matrix field + :device_verified iOS / :jvm_hermetic Android entries"
      via: "populating the matrix + verification_method on capability families"
      pattern: "rebuild_matrix:|verification_method:"
    - from: "validate/1"
      to: "verification_method CI-only invariant"
      via: "validate_verification_method_invariant clause"
      pattern: "validate_verification_method"
---

<objective>
Wire the support-truth data layer in `lib/crosswake/support_matrix/support_matrix.ex` (D-10, D-13, D-14, D-15, D-16, D-17, D-18, D-19): add `@rebuild_matrix_rows` + the 1-arity `rebuild_matrix/1` accessor mirroring `capability_families/1`; populate the `rebuild_matrix` field in `canonical/1`; set `verification_method` on canonical capability families (iOS `:device_verified`, Android `:jvm_hermetic`); add the CI-only-never-device validation clause to `validate/1`; and append the two Android `PromotionRuleEntry` rows (`shell.android.jvm_hermetic`, `shell.android.device_verified`) — criteria-as-data only, with Android `SupportEntry.status` staying `:verification_required` (D-20).

Purpose: Turns the type-layer (plan 02) and policy engine (plan 03) into operator-visible support truth and the gated promotion criteria, while mechanically forbidding evidence laundering.
Output: One modified file: a new module attribute, a new accessor, a new validation clause, two new promotion rows, and `verification_method` population on existing canonical capability entries.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/64-runtime-line-policy-contract-support-truth-taxonomy/64-CONTEXT.md
@.planning/phases/64-runtime-line-policy-contract-support-truth-taxonomy/64-RESEARCH.md
@.planning/phases/64-runtime-line-policy-contract-support-truth-taxonomy/64-PATTERNS.md
@.planning/phases/64-runtime-line-policy-contract-support-truth-taxonomy/64-02-SUMMARY.md
@.planning/phases/64-runtime-line-policy-contract-support-truth-taxonomy/64-03-SUMMARY.md

<interfaces>
<!-- From plan 02 (types.ex): -->
Types.new_runtime_line_row(runtime_line:, capability_surface:, change_class:, ota_safe:, rebuild_required:, evidence_tier:)
%RuntimeLineRow{} evidence_tier :: :none | :provider_advisory | :jvm_hermetic | :emulator_advisory | :device_verified
CapabilitySupportEntry now accepts `verification_method:` (default :none)
PromotionRuleEntry now accepts `required_verification_method:` (default :none)
SupportMatrix now has a `rebuild_matrix: []` field (constructor Keyword.get'd)

<!-- Existing in this file (verified): -->
def canonical(opts \\ [])                 # ~line 272 — builds the SupportMatrix; populate rebuild_matrix here
def validate(%SupportMatrix{})            # ~line 347 — pipe of validate_* clauses; add a new clause
def action_classes                        # ~line 366 — co-truth source (native_shell/companion_native/provider_adapter => rebuild_required true)
def capability_families(%SupportMatrix{}) # ~line 388 — accessor template to mirror for rebuild_matrix/1
defp validate_promotion_rule_rows(errors) # ~line 774 — Enum.reduce error-accumulation shape to mirror
defp promotion_rule_entries               # ~line 882 — append two new rows here
existing Android promo row: claim_id "shell.android.generated_project" (~line 903) — the exact row template

<!-- D-18/D-19 locked values: -->
shell.android.jvm_hermetic: required_verification_method: :jvm_hermetic, promotes_to: :merge_blocking,
  minimum_consecutive_passes: 3, freshness_window: "current release branch, within 30 CI runs",
  failure_budget: "zero consecutive failures; single failure resets counter", required_platforms: ["android"]
shell.android.device_verified: required_verification_method: :device_verified, GATED;
  demotion_trigger prose states device/emulator proof unavailable until Phases 67/68 and that
  jvm_hermetic promotion MUST NOT be read as device_verified
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add @rebuild_matrix_rows + rebuild_matrix/1 accessor, populate canonical/1, and set capability verification_method</name>
  <files>lib/crosswake/support_matrix/support_matrix.ex</files>
  <behavior>
    - `SupportMatrix.rebuild_matrix(SupportMatrix.canonical())` returns a non-empty `[%Types.RuntimeLineRow{}]` with `runtime_line` values including `"1.x"` (and `"2.x"` per D-14), one row per major band.
    - Each row's `evidence_tier` is a member of the D-09 enum; `ota_safe`/`rebuild_required` are booleans consistent with `RebuildPolicy` (rebuild bands are not OTA-safe).
    - Canonical iOS capability families carry `verification_method: :device_verified`; Android carry `:jvm_hermetic`.
    - `rebuild_matrix/1` is 1-arity, no options, returns the stored field (mirrors `capability_families/1`).
  </behavior>
  <read_first>
    - lib/crosswake/support_matrix/support_matrix.ex — `canonical/1` (~272), `capability_families/1` accessor (~388), `@commerce_corridor_entries` module attribute (~25) for the static-data pattern, `action_classes`/`action_class_entries` (~366/~818) for the co-truth bands, the canonical capability-family construction inside `canonical/1` (where iOS/Android entries are built)
    - lib/crosswake/manifest/types.ex — `new_runtime_line_row/1` and the `RuntimeLineRow` struct (from plan 02), `new_capability_support_entry/1` (to pass `verification_method:`)
    - .planning/phases/64-runtime-line-policy-contract-support-truth-taxonomy/64-PATTERNS.md (the support_matrix MODIFY section: Analog 1 module-attribute + accessor; the `@rebuild_matrix_rows` sketch; the `<specifics>` doctor sketch showing the 1.x row)
    - .planning/phases/64-runtime-line-policy-contract-support-truth-taxonomy/64-CONTEXT.md (D-14 granularity: one row per major band; columns)
  </read_first>
  <action>
    Add a `@rebuild_matrix_rows` module attribute (mirror `@commerce_corridor_entries` placement/lifecycle) built from `Types.new_runtime_line_row/1`, with one row per major runtime-line band per D-14 (at minimum `"1.x"`; include `"2.x"` to demonstrate the band model). Columns per D-14: `runtime_line`, `capability_surface` (`[String.t()]` of the canonical capability family names already in the matrix), `change_class` (string label, e.g. the existing "native or companion rebuild required" prose), `ota_safe` (bool), `rebuild_required` (bool), `evidence_tier` (atom from D-09 — reuse the verification_method enum per D-15; do NOT invent a separate 3-value enum). The matrix DISPLAYS the rebuild + evidence seams — derive `ota_safe`/`rebuild_required` consistently with `RebuildPolicy`/`action_classes()`, do not re-decide them.

    Add the accessor `@spec rebuild_matrix(SupportMatrix.t()) :: [Types.RuntimeLineRow.t()]` / `def rebuild_matrix(%SupportMatrix{} = support_matrix), do: support_matrix.rebuild_matrix` alongside `capability_families/1`.

    In `canonical/1`, populate the new `rebuild_matrix:` field from `@rebuild_matrix_rows` (same lifecycle as the other typed-row lists). Also set `verification_method:` on the canonical capability-family entries: iOS-owned/device-backed entries → `:device_verified`; Android-owned entries → `:jvm_hermetic`. Do NOT set `:device_verified` on any Android or CI-only entry (D-10).
  </action>
  <verify>
    <automated>mix compile --warnings-as-errors 2>&1 | tail -5; grep -q "def rebuild_matrix" lib/crosswake/support_matrix/support_matrix.ex && grep -q "@rebuild_matrix_rows" lib/crosswake/support_matrix/support_matrix.ex; mix test test/crosswake/proof/phase64_runtime_line_policy_test.exs --only rline_03 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `@rebuild_matrix_rows` exists and is built via `Types.new_runtime_line_row/1`
    - `rebuild_matrix/1` is a 1-arity accessor returning `support_matrix.rebuild_matrix` (mirrors `capability_families/1`)
    - `canonical/1` populates `rebuild_matrix:` from `@rebuild_matrix_rows`
    - Matrix rows are per major band (`"1.x"`, `"2.x"`), NOT per patch or per capability family
    - `row.evidence_tier` values are members of the D-09 enum (no separate 3-value enum)
    - Canonical iOS capability entries set `verification_method: :device_verified`; Android set `:jvm_hermetic`; no Android/CI-only entry set to `:device_verified`
    - `mix test test/crosswake/proof/phase64_runtime_line_policy_test.exs --only rline_03` passes
    - `mix compile --warnings-as-errors` exits 0
  </acceptance_criteria>
  <done>SupportMatrix exposes the per-band rebuild & compatibility matrix as typed rows via `rebuild_matrix/1`, canonical capability families carry honest evidence tiers (iOS device-verified, Android jvm-hermetic), and the `:rline_03` proof segment passes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add CI-only-never-device validation + the two gated Android promotion rows</name>
  <files>lib/crosswake/support_matrix/support_matrix.ex</files>
  <behavior>
    - `SupportMatrix.validate/1` returns a non-empty error list when a `CapabilitySupportEntry` whose evidence corpus is CI-only carries `verification_method: :device_verified`; returns `[]` for the canonical matrix (Android jvm_hermetic, iOS device_verified are both valid).
    - `SupportMatrix.promotion_rules()` contains `shell.android.jvm_hermetic` with `minimum_consecutive_passes: 3`, `required_verification_method: :jvm_hermetic`, `promotes_to: :merge_blocking`, `freshness_window: "current release branch, within 30 CI runs"`, `failure_budget: "zero consecutive failures; single failure resets counter"`.
    - `promotion_rules()` contains `shell.android.device_verified` with `required_verification_method: :device_verified` and a `demotion_trigger` mentioning Phases 67/68 and that jvm_hermetic must not be read as device_verified.
    - Both new rows pass `validate_promotion_rule_rows/1` (non-empty required_evidence / required_docs_anchors / check_ids; non-empty demotion_trigger).
    - Android baseline `SupportEntry.status` stays `:verification_required` (unchanged).
  </behavior>
  <read_first>
    - lib/crosswake/support_matrix/support_matrix.ex — `validate/1` pipe (~347), `validate_promotion_rule_rows/1` (~774) for the `Enum.reduce` cond-based error shape to mirror, `promotion_rule_entries/0` (~882) and the `shell.android.generated_project` row (~903-922) as the exact row template, the canonical Android `SupportEntry` construction (~305) to confirm status stays `:verification_required`
    - lib/crosswake/manifest/types.ex — `new_promotion_rule_entry/1` now accepting `required_verification_method:` (from plan 02)
    - .planning/phases/64-runtime-line-policy-contract-support-truth-taxonomy/64-PATTERNS.md (Analog 2 promotion-row template, Analog 3 validate clause; Pitfall 3 non-empty list fields)
    - .planning/phases/64-runtime-line-policy-contract-support-truth-taxonomy/64-CONTEXT.md (D-18/D-19 exact values + gating prose; D-20 do-not-promote)
  </read_first>
  <action>
    Add a private `validate_verification_method_invariant/1` clause and insert it into the `validate/1` pipe (after the existing `validate_*` clauses), following the `Enum.reduce(errors, fn entry, acc -> cond do ... end end)` shape of `validate_promotion_rule_rows/1`. It must append an error when an entry's `verification_method == :device_verified` but its evidence corpus is CI-only (no genuine real-device artifact). Determine "CI-only" structurally from the entry data available (e.g. Android-owned / lacking a device-evidence marker) — preserve the structural guarantee that `:device_verified` requires real-device evidence (D-10a). Constructor default `:none` is always safe.

    Append two `promotion_rule(...)` rows to `promotion_rule_entries/0`, mirroring `shell.android.generated_project` exactly and adding the new `required_verification_method:` field:
    - `shell.android.jvm_hermetic` — `required_verification_method: :jvm_hermetic`, `current_proof_class: :advisory`, `promotes_to: :merge_blocking`, `minimum_consecutive_passes: 3`, `freshness_window: "current release branch, within 30 CI runs"`, `failure_budget: "zero consecutive failures; single failure resets counter"`, `required_platforms: ["android"]`, non-empty `required_evidence` (verify script + JVM BridgeChannel proof + support-matrix parity + docs-contract parity), non-empty `required_docs_anchors`, non-empty `check_ids`, `change_class`/`action_class` consistent with the existing Android row, and an explicit `demotion_trigger`.
    - `shell.android.device_verified` — `required_verification_method: :device_verified`, GATED; `demotion_trigger` prose explicitly states device/emulator proof is unavailable until Phases 67/68 and that jvm_hermetic promotion MUST NOT be read as device_verified (D-19); non-empty required_evidence/required_docs_anchors/check_ids so it passes `validate_promotion_rule_rows/1`.

    Do NOT change the Android baseline `SupportEntry.status` — it stays `:verification_required` (D-20). This phase authors criteria as data only.
  </action>
  <verify>
    <automated>mix compile --warnings-as-errors 2>&1 | tail -5; grep -q "validate_verification_method" lib/crosswake/support_matrix/support_matrix.ex && grep -q "shell.android.jvm_hermetic" lib/crosswake/support_matrix/support_matrix.ex && grep -q "shell.android.device_verified" lib/crosswake/support_matrix/support_matrix.ex; mix test test/crosswake/proof/phase64_runtime_line_policy_test.exs --only rline_04 --only rline_05 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `validate/1` includes a `validate_verification_method_invariant/1` clause; `validate/1` returns errors for a CI-only entry claiming `:device_verified` and `[]` for the canonical matrix
    - `promotion_rules()` contains `shell.android.jvm_hermetic` with `minimum_consecutive_passes: 3` and `required_verification_method: :jvm_hermetic`
    - `promotion_rules()` contains `shell.android.device_verified` with `required_verification_method: :device_verified` and a `demotion_trigger` referencing Phases 67/68 + the no-promotion-to-device note
    - Both new rows satisfy `validate_promotion_rule_rows/1` (non-empty required_evidence, required_docs_anchors, check_ids; non-empty demotion_trigger)
    - Android baseline `SupportEntry.status` is `:verification_required` (unchanged — D-20)
    - `mix test test/crosswake/proof/phase64_runtime_line_policy_test.exs --only rline_04 --only rline_05` passes
    - `mix compile --warnings-as-errors` exits 0
  </acceptance_criteria>
  <done>The CI-only-never-device invariant is enforced in validate/1, the two gated Android promotion rows exist as criteria-as-data, Android stays `:verification_required`, and the `:rline_04`/`:rline_05` proof segments pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| evidence corpus → claimed verification_method | A CI-only corpus claiming :device_verified is the core evidence-laundering threat |
| advisory CI evidence → merge-blocking promotion | Promotion criteria must gate, not auto-promote, Android |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-64-11 | Spoofing/Elevation | validate_verification_method_invariant | mitigate | `validate/1` rejects `:device_verified` on a CI-only entry (D-10a); proof `:rline_04` asserts |
| T-64-12 | Elevation | Android promotion rows | mitigate | device_verified row is GATED with Phase 67/68 prose; Android status stays `:verification_required` (D-20); proof `:rline_05` asserts |
| T-64-13 | Spoofing | evidence_tier reuse | mitigate | Matrix `evidence_tier` reuses the D-09 verification_method enum (D-15) — no parallel enum to drift |
| T-64-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this phase (pure Elixir) |
</threat_model>

<verification>
`mix compile --warnings-as-errors` exits 0; `mix test test/crosswake/proof/phase64_runtime_line_policy_test.exs --only rline_03 --only rline_04 --only rline_05` passes (the doctor-render parity in `:rline_03`/`:rline_04` fully greens only after plan 05 wires the formatters). `mix test` (full suite) must stay green.
</verification>

<success_criteria>
SupportMatrix exposes the per-band rebuild matrix, carries honest per-platform evidence tiers, mechanically rejects evidence laundering, and ships the two gated Android promotion rows as data — with Android still `:verification_required`.
</success_criteria>

<output>
Create `.planning/phases/64-runtime-line-policy-contract-support-truth-taxonomy/64-04-SUMMARY.md` when done
</output>
