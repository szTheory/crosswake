---
phase: 64-runtime-line-policy-contract-support-truth-taxonomy
plan: 05
type: execute
wave: 3
depends_on: ["64-01", "64-04"]
files_modified:
  - lib/crosswake/doctor/doctor.ex
  - lib/crosswake/doctor/formatter.ex
  - lib/crosswake/doctor/json_formatter.ex
autonomous: true
requirements: [RLINE-03, RLINE-04]
nyquist_compliant: true

must_haves:
  truths:
    - "An operator running mix crosswake.doctor sees a 'rebuild & compatibility matrix:' block under 'release policy:'"
    - "Doctor prints an 'evidence posture:' line showing ios=device-verified android=jvm-hermetic (CI only)"
    - "Doctor --format json carries rebuild_matrix and evidence_posture keys"
    - "Human and JSON formatters render the same RuntimeLineRow data (structural parity, no independent serialization)"
    - ":jvm_hermetic renders as 'jvm-hermetic (CI only)' and 'device-verified' prints only for genuine :device_verified"
  artifacts:
    - path: "lib/crosswake/doctor/formatter.ex"
      provides: "format_rebuild_matrix/1 + evidence posture line + format_evidence_tier/1"
      contains: "rebuild & compatibility matrix:"
    - path: "lib/crosswake/doctor/json_formatter.ex"
      provides: "rebuild_matrix + evidence_posture JSON keys"
      contains: "rebuild_matrix"
    - path: "lib/crosswake/doctor/doctor.ex"
      provides: "release_policy_snapshot/1 carrying rebuild_matrix + evidence_posture"
      contains: "rebuild_matrix"
  key_links:
    - from: "doctor.ex release_policy_snapshot/1"
      to: "SupportMatrix.rebuild_matrix/1"
      via: "accessor call into the snapshot map"
      pattern: "SupportMatrix.rebuild_matrix"
    - from: "formatter.ex + json_formatter.ex"
      to: "the same [RuntimeLineRow.t()] list"
      via: "shared traversal from release_policy.rebuild_matrix"
      pattern: "rebuild_matrix"
---

<objective>
Render the rebuild & compatibility matrix and evidence posture in `mix crosswake.doctor` (D-16, RLINE-03 / RLINE-04): extend `release_policy_snapshot/1` to carry `rebuild_matrix` + `evidence_posture`, add `format_rebuild_matrix/1` + a `format_evidence_tier/1` honesty helper + the `evidence posture:` line to the human `Doctor.Formatter`, and add the matching `rebuild_matrix` + `evidence_posture` keys to `Doctor.JSONFormatter` — both formatters traversing the SAME `[RuntimeLineRow.t()]` list (structural parity, no independent serialization). The formatter renders `:jvm_hermetic` as `"jvm-hermetic (CI only)"` and `"device-verified"` only for a genuine `:device_verified` (no implicit promotion). This is the final green-up of the phase-64 proof lane.

Purpose: Makes the locked support truth operator-visible without any path that could overclaim CI evidence as device-verified.
Output: Three modified doctor files; the full phase64 proof lane goes green; full `mix test` stays green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/64-runtime-line-policy-contract-support-truth-taxonomy/64-CONTEXT.md
@.planning/phases/64-runtime-line-policy-contract-support-truth-taxonomy/64-RESEARCH.md
@.planning/phases/64-runtime-line-policy-contract-support-truth-taxonomy/64-PATTERNS.md
@.planning/phases/64-runtime-line-policy-contract-support-truth-taxonomy/64-04-SUMMARY.md

<interfaces>
<!-- From plan 04: -->
SupportMatrix.rebuild_matrix(support_matrix) :: [Types.RuntimeLineRow.t()]
%RuntimeLineRow{runtime_line, capability_surface, change_class, ota_safe, rebuild_required, evidence_tier}

<!-- Existing (verified): -->
doctor.ex  release_policy_snapshot/1 (~1186-1203) returns map with keys: crosswake_version, manifest_schema_version,
  bridge_protocol_version, native_runtime_version, package_version_truth, companion_requirement,
  capability_families, package_surfaces, release_boundaries, change_classes
formatter.ex  format_release_policy/1 (~60-108) FULL clause (gets the new lines) + minimal clause (unchanged);
  format_change_classes/1 (~141-148) is the template for format_rebuild_matrix/1
json_formatter.ex  format_release_policy/1 (~103-116) returns a field-subset map (add the two new keys)

<!-- Render contract (D-10b / D-16 / <specifics> sketch): -->
release policy:
  native_runtime_version=1.0.0
  evidence posture: ios=device-verified  android=jvm-hermetic (CI only)
  rebuild & compatibility matrix:
    1.x  ota_safe=...  rebuild_required=...  evidence_tier=...  capability_surface=[...]
format_evidence_tier(:jvm_hermetic) => "jvm-hermetic (CI only)"; (:device_verified) => "device-verified"; (other) => Atom.to_string/1
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Carry rebuild_matrix + evidence_posture in release_policy_snapshot/1</name>
  <files>lib/crosswake/doctor/doctor.ex</files>
  <behavior>
    - `release_policy_snapshot/1` returns a map that additionally contains `rebuild_matrix:` (the `[RuntimeLineRow.t()]` from `SupportMatrix.rebuild_matrix/1`) and `evidence_posture:` (a map like `%{ios: :device_verified, android: :jvm_hermetic}` derived from the support matrix / matrix rows).
    - The evidence posture is derived from real support-matrix data, never hardcoded device-verified for a CI-only platform.
  </behavior>
  <read_first>
    - lib/crosswake/doctor/doctor.ex — `release_policy_snapshot/1` (~1186-1203) full current map shape
    - lib/crosswake/support_matrix/support_matrix.ex — `rebuild_matrix/1` accessor (from plan 04) and how capability families carry `verification_method` per platform
    - .planning/phases/64-runtime-line-policy-contract-support-truth-taxonomy/64-PATTERNS.md (the doctor.ex MODIFY section + Open Question 1: evidence_posture_snapshot/1 is planner discretion — a private helper in doctor.ex is sufficient)
  </read_first>
  <action>
    Extend the map returned by `release_policy_snapshot/1` with `rebuild_matrix: SupportMatrix.rebuild_matrix(support_matrix)` and `evidence_posture: evidence_posture_snapshot(support_matrix)`. Add a private `evidence_posture_snapshot/1` that derives the per-platform evidence tier from the support matrix (group canonical capability `verification_method` by owner platform, or read the matrix rows) producing a map such as `%{ios: :device_verified, android: :jvm_hermetic}`. Derive it from live data — do not hardcode `:device_verified` for any CI-only platform (D-10).
  </action>
  <verify>
    <automated>mix compile --warnings-as-errors 2>&1 | tail -5; grep -q "rebuild_matrix: SupportMatrix.rebuild_matrix" lib/crosswake/doctor/doctor.ex && grep -q "evidence_posture" lib/crosswake/doctor/doctor.ex</automated>
  </verify>
  <acceptance_criteria>
    - `release_policy_snapshot/1` returned map includes `rebuild_matrix:` sourced from `SupportMatrix.rebuild_matrix/1`
    - returned map includes `evidence_posture:` derived from a private `evidence_posture_snapshot/1` reading live support-matrix data
    - evidence posture for Android is `:jvm_hermetic` (not device-verified) for the canonical matrix
    - `mix compile --warnings-as-errors` exits 0
  </acceptance_criteria>
  <done>The doctor release-policy snapshot carries the rebuild matrix and a data-derived evidence posture, ready for both formatters to render.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Render the matrix + evidence posture in both formatters with shared traversal</name>
  <files>lib/crosswake/doctor/formatter.ex, lib/crosswake/doctor/json_formatter.ex</files>
  <behavior>
    - `mix crosswake.doctor` human output contains a `"  rebuild & compatibility matrix:"` block listing each runtime-line band and a `"  evidence posture: "` line containing `ios=device-verified` and `android=jvm-hermetic (CI only)`.
    - `mix crosswake.doctor --format json` output (`Jason.decode!`) has a `"rebuild_matrix"` list (each row carrying runtime_line/ota_safe/rebuild_required/evidence_tier/capability_surface) and an `"evidence_posture"` map.
    - Human and JSON render the same set of runtime_line bands (structural parity) traversing the same `release_policy.rebuild_matrix` list.
    - A row/entry with `:jvm_hermetic` renders the literal `"jvm-hermetic (CI only)"`; `:device_verified` renders `"device-verified"`; the formatter never promotes `:jvm_hermetic` to device-verified.
  </behavior>
  <read_first>
    - lib/crosswake/doctor/formatter.ex — `format_release_policy/1` FULL clause (~60-87) + minimal clause (~89-107), `format_change_classes/1` (~141-148) as the exact `format_rebuild_matrix/1` template
    - lib/crosswake/doctor/json_formatter.ex — `format_release_policy/1` (~103-116) field-subset map
    - .planning/phases/64-runtime-line-policy-contract-support-truth-taxonomy/64-PATTERNS.md (formatter + json_formatter MODIFY sections with the format_rebuild_matrix/1 + format_evidence_tier/1 sketch; Pitfall 4 human/JSON divergence; the shared-traversal rule)
    - .planning/phases/64-runtime-line-policy-contract-support-truth-taxonomy/64-VALIDATION.md (the `:rline_03`/`:rline_04` render assertions this must satisfy)
  </read_first>
  <action>
    In `formatter.ex`: add `format_rebuild_matrix/1` mirroring `format_change_classes/1` (section header `"  rebuild & compatibility matrix:"` then `Enum.map` over rows emitting runtime_line, ota_safe, rebuild_required, evidence_tier via `format_evidence_tier/1`, and capability_surface). Add the honesty helper `format_evidence_tier/1`: `:jvm_hermetic -> "jvm-hermetic (CI only)"`, `:device_verified -> "device-verified"`, other -> `Atom.to_string/1`. Add a `format_evidence_posture/1` that renders the posture map as `ios=<tier>  android=<tier>` using the same `format_evidence_tier/1`. Extend ONLY the FULL `format_release_policy/1` clause: add `rebuild_matrix` + `evidence_posture` to its pattern match, add the `"  evidence posture: #{format_evidence_posture(evidence_posture)}"` line alongside the `native_runtime_version=` lines, and append `format_rebuild_matrix(rebuild_matrix)` alongside the `format_change_classes(change_classes)` call. Leave the minimal clause unchanged.

    In `json_formatter.ex`: add `rebuild_matrix:` and `evidence_posture:` keys to the `format_release_policy/1` map, sourced from `release_policy.rebuild_matrix` and `release_policy.evidence_posture` — the SAME list/map the human formatter consumes (Pitfall 4: no independent serialization). Confirm the JSON path serializes the rows consistently with how it already serializes other struct lists (e.g. `package_surfaces`) — reuse that path so the evidence_tier value is not silently atom-promoted.
  </action>
  <verify>
    <automated>mix compile --warnings-as-errors 2>&1 | tail -5; grep -q "rebuild & compatibility matrix:" lib/crosswake/doctor/formatter.ex && grep -q "jvm-hermetic (CI only)" lib/crosswake/doctor/formatter.ex && grep -q "rebuild_matrix" lib/crosswake/doctor/json_formatter.ex; mix test test/crosswake/proof/phase64_runtime_line_policy_test.exs 2>&1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - `formatter.ex` defines `format_rebuild_matrix/1` and `format_evidence_tier/1` with `:jvm_hermetic -> "jvm-hermetic (CI only)"` and `:device_verified -> "device-verified"`
    - the FULL `format_release_policy/1` clause emits the `"  evidence posture: "` line and the `"  rebuild & compatibility matrix:"` block; the minimal clause is unchanged
    - `json_formatter.ex` `format_release_policy/1` adds `rebuild_matrix` + `evidence_posture` keys from `release_policy.rebuild_matrix`/`.evidence_posture`
    - human and JSON outputs render the same runtime_line band set (structural parity)
    - `mix test test/crosswake/proof/phase64_runtime_line_policy_test.exs` exits 0 (ALL of rline_01..05 green)
    - `mix test` (full suite) exits 0
    - `mix compile --warnings-as-errors` exits 0
  </acceptance_criteria>
  <done>Doctor renders the rebuild & compatibility matrix and evidence posture in both human and JSON form via shared traversal, never promoting jvm-hermetic to device-verified; the full phase-64 proof lane and the full suite are green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| evidence_tier atom → operator-readable string | The render must not silently upgrade CI evidence to device-verified |
| human formatter ↔ JSON formatter | Independent serialization would let the two outputs disagree (information disclosure) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-64-14 | Spoofing/Elevation | format_evidence_tier/1 | mitigate | Explicit string mapping renders `:jvm_hermetic` as "jvm-hermetic (CI only)", "device-verified" only for `:device_verified` (D-10b); proof `:rline_04` asserts |
| T-64-15 | Information Disclosure | human vs JSON formatter | mitigate | Both formatters traverse the same `release_policy.rebuild_matrix` list (D-16, Pitfall 4); proof `:rline_03` compares both outputs |
| T-64-16 | Tampering | minimal format_release_policy clause | mitigate | New keys added only to the FULL clause; minimal clause left intact so no partial-data render path can crash or misreport |
| T-64-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this phase (pure Elixir) |
</threat_model>

<verification>
`mix test test/crosswake/proof/phase64_runtime_line_policy_test.exs` exits 0 (full RLINE-01..05 green); `mix test` full suite exits 0; `mix compile --warnings-as-errors` exits 0; `mix crosswake.doctor` human output shows the matrix + evidence posture and `--format json` carries `rebuild_matrix`/`evidence_posture`.
</verification>

<success_criteria>
The rebuild & compatibility matrix and evidence posture are operator-visible in both doctor formats via shared traversal, with no path that promotes CI-only evidence to device-verified; the phase-64 proof lane is fully green.
</success_criteria>

<output>
Create `.planning/phases/64-runtime-line-policy-contract-support-truth-taxonomy/64-05-SUMMARY.md` when done
</output>
