---
phase: 64-runtime-line-policy-contract-support-truth-taxonomy
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/crosswake/manifest/types.ex
autonomous: true
requirements: [RLINE-02, RLINE-03, RLINE-04, RLINE-05]
nyquist_compliant: true

must_haves:
  truths:
    - "CapabilitySupportEntry can carry a typed verification_method (default :none) without breaking existing constructors"
    - "PromotionRuleEntry can carry a typed required_verification_method (default :none)"
    - "A typed RuntimeLineRow struct exists with the 6 locked columns and JSON-encodes"
    - "SupportMatrix can hold a rebuild_matrix list (default []) without breaking existing constructors"
    - "Compatibility struct is unchanged — still exactly 5 fields, manifest_schema_version still 1.0.0"
  artifacts:
    - path: "lib/crosswake/manifest/types.ex"
      provides: "RuntimeLineRow struct + verification_method + required_verification_method + SupportMatrix.rebuild_matrix field + constructors + to_map clauses"
      contains: "defmodule RuntimeLineRow"
  key_links:
    - from: "Crosswake.Manifest.Types.RuntimeLineRow"
      to: "Jason.Encoder + to_map/1"
      via: "@derive Jason.Encoder + to_map clause"
      pattern: "@derive Jason.Encoder"
    - from: "CapabilitySupportEntry.verification_method"
      to: "default :none (not in @enforce_keys)"
      via: "defstruct default + Keyword.get"
      pattern: "verification_method: :none"
---

<objective>
Add the additive type-layer foundation in `lib/crosswake/manifest/types.ex` that every other plan builds on: the new `RuntimeLineRow` struct, the new `verification_method` field on `CapabilitySupportEntry`, the new `required_verification_method` field on `PromotionRuleEntry`, and the new `rebuild_matrix` field on `SupportMatrix` — all additive, defaulted, back-compat, with matching constructors and `to_map/1` clauses. Implements D-07 (verification_method is a NEW orthogonal axis, kept separate from `proof_class`), D-08, D-09, D-12, D-13, D-14, D-15. Crucially: the `Compatibility` struct is NOT touched (RLINE-02 / D-02, D-03, D-04, D-05 — no new manifest field, no schema bump).

Purpose: This is the contracts-first foundation. `RebuildPolicy` (plan 03 — independent), `SupportMatrix` data (plan 04), and doctor render (plan 05) all consume these types.
Output: One modified file with one new struct, three new defaulted struct fields, three constructor updates, three `to_map/1` updates, and the new struct's `to_map/1` clause.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/64-runtime-line-policy-contract-support-truth-taxonomy/64-CONTEXT.md
@.planning/phases/64-runtime-line-policy-contract-support-truth-taxonomy/64-RESEARCH.md
@.planning/phases/64-runtime-line-policy-contract-support-truth-taxonomy/64-PATTERNS.md

<interfaces>
<!-- The locked shapes this plan must produce. Verified against types.ex in RESEARCH/PATTERNS. -->
RuntimeLineRow:
  @enforce_keys [:runtime_line, :capability_surface, :change_class, :ota_safe, :rebuild_required, :evidence_tier]
  @type evidence_tier :: :none | :provider_advisory | :jvm_hermetic | :emulator_advisory | :device_verified
  fields: runtime_line :: String.t(), capability_surface :: [String.t()], change_class :: String.t(),
          ota_safe :: boolean(), rebuild_required :: boolean(), evidence_tier :: evidence_tier()

CapabilitySupportEntry: add `verification_method: :none` to defstruct only (NOT @enforce_keys);
  @type verification_method :: :none | :provider_advisory | :jvm_hermetic | :emulator_advisory | :device_verified

PromotionRuleEntry: add `required_verification_method: :none` to defstruct only (NOT @enforce_keys)

SupportMatrix: add `rebuild_matrix: []` to defstruct only (NOT @enforce_keys);
  @type t gains `rebuild_matrix: [Crosswake.Manifest.Types.RuntimeLineRow.t()]`
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add RuntimeLineRow struct + verification_method + required_verification_method fields</name>
  <files>lib/crosswake/manifest/types.ex</files>
  <behavior>
    - `%Crosswake.Manifest.Types.RuntimeLineRow{}` requires all 6 enforced keys and JSON-encodes via Jason.
    - `Types.new_runtime_line_row(runtime_line: "1.x", capability_surface: [...], change_class: "...", ota_safe: false, rebuild_required: true, evidence_tier: :jvm_hermetic)` returns a valid struct.
    - `Types.new_capability_support_entry(...)` without `:verification_method` yields `verification_method: :none` (back-compat); passing `verification_method: :device_verified` is preserved.
    - `Types.new_promotion_rule_entry(...)` without `:required_verification_method` yields `:none`.
    - `Types.to_map(%RuntimeLineRow{})` includes `"evidence_tier"` as a string; `to_map(%CapabilitySupportEntry{})` includes `"verification_method"`; `to_map(%PromotionRuleEntry{})` includes `"required_verification_method"`.
  </behavior>
  <read_first>
    - lib/crosswake/manifest/types.ex — `ReleaseBoundaryEntry` (~lines 478-496) as the struct template; `CapabilitySupportEntry` (~426-460) + `new_capability_support_entry/1` (~823-839) + its `to_map/1` (~1093-1111); `PromotionRuleEntry` (~557-614) + `new_promotion_rule_entry/1` (~885-904) + its `to_map/1` (~1153-1171); `new_release_boundary_entry/1` (~852-860) + `to_map(%ReleaseBoundaryEntry{})` (~1123-1130)
    - .planning/phases/64-runtime-line-policy-contract-support-truth-taxonomy/64-PATTERNS.md (the four `types.ex` MODIFY sections + the RuntimeLineRow NEW section, with exact field lists and the atom_label/1 vs Atom.to_string/1 serialization rule)
  </read_first>
  <action>
    Mirror `ReleaseBoundaryEntry` to add `defmodule RuntimeLineRow` inside `Crosswake.Manifest.Types`: `@moduledoc false`, `@derive Jason.Encoder`, `@enforce_keys [:runtime_line, :capability_surface, :change_class, :ota_safe, :rebuild_required, :evidence_tier]`, matching `defstruct`, an `@type evidence_tier :: :none | :provider_advisory | :jvm_hermetic | :emulator_advisory | :device_verified` (D-09 verbatim), and `@type t` per the interface block. Add a `new_runtime_line_row/1` constructor mirroring `new_release_boundary_entry/1` (all 6 via `Keyword.fetch!`). Add a `to_map(%RuntimeLineRow{} = row)` clause mirroring `to_map(%ReleaseBoundaryEntry{})`, using `Atom.to_string(row.evidence_tier)` for the atom column.

    On `CapabilitySupportEntry`: append `verification_method: :none` to `defstruct` (NOT to `@enforce_keys`); add `@type verification_method` matching the D-09 enum; in `new_capability_support_entry/1` add `verification_method: Keyword.get(attrs, :verification_method, :none)`; in its `to_map/1` add `"verification_method" => Atom.to_string(support_entry.verification_method)` (use `Atom.to_string/1`, consistent with the existing `"owner"` serialization in that clause; `:none` is never nil so it survives the existing `Enum.reject(is_nil)` pipeline).

    On `PromotionRuleEntry`: append `required_verification_method: :none` to `defstruct` (NOT to `@enforce_keys`); in `new_promotion_rule_entry/1` add `required_verification_method: Keyword.get(attrs, :required_verification_method, :none)`; in its `to_map/1` add `"required_verification_method" => atom_label(entry.required_verification_method)` (use `atom_label/1` — that clause's convention for atom fields like `promotes_to`).

    Do NOT modify `Compatibility` in any way. Do NOT add any of these fields to the per-platform baseline `SupportEntry` struct (D-11).
  </action>
  <verify>
    <automated>mix compile --warnings-as-errors 2>&1 | tail -5; grep -q "defmodule RuntimeLineRow" lib/crosswake/manifest/types.ex && grep -q "verification_method: :none" lib/crosswake/manifest/types.ex && grep -q "required_verification_method: :none" lib/crosswake/manifest/types.ex</automated>
  </verify>
  <acceptance_criteria>
    - `lib/crosswake/manifest/types.ex` defines `defmodule RuntimeLineRow` with `@derive Jason.Encoder` and the 6-key `@enforce_keys`
    - `Types.new_runtime_line_row/1` exists and constructs a `RuntimeLineRow` from the 6 keys
    - `CapabilitySupportEntry` defstruct includes `verification_method: :none` and `verification_method` is NOT in its `@enforce_keys`
    - `PromotionRuleEntry` defstruct includes `required_verification_method: :none` and it is NOT in its `@enforce_keys`
    - `Compatibility` struct is byte-for-byte unchanged (still exactly 5 fields; `manifest_schema_version` default/value unchanged)
    - `SupportEntry` (per-platform baseline) gains NO new field
    - `mix compile --warnings-as-errors` exits 0
  </acceptance_criteria>
  <done>RuntimeLineRow exists and JSON-encodes; CapabilitySupportEntry and PromotionRuleEntry carry their new defaulted atom fields with constructor + to_map support; Compatibility and SupportEntry untouched; project compiles clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add SupportMatrix.rebuild_matrix field with constructor and to_map support</name>
  <files>lib/crosswake/manifest/types.ex</files>
  <behavior>
    - `%SupportMatrix{}` constructed without `:rebuild_matrix` defaults to `rebuild_matrix: []` (back-compat — no existing call site breaks).
    - `Types.new_support_matrix(rebuild_matrix: [row])` preserves the list.
    - `Types.to_map(%SupportMatrix{rebuild_matrix: [row]})` includes a `"rebuild_matrix"` key mapping each row through `to_map/1`.
  </behavior>
  <read_first>
    - lib/crosswake/manifest/types.ex — `SupportMatrix` struct (~362-397), `new_support_matrix/1` (~792-805), `to_map(%SupportMatrix{})` (~1063-1075)
    - .planning/phases/64-runtime-line-policy-contract-support-truth-taxonomy/64-PATTERNS.md (the `SupportMatrix.rebuild_matrix` MODIFY section + Pitfall 2: do NOT add to @enforce_keys)
  </read_first>
  <action>
    Append `rebuild_matrix: []` to the `SupportMatrix` `defstruct` (do NOT add to `@enforce_keys` — Pitfall 2). Extend `@type t` with `rebuild_matrix: [Crosswake.Manifest.Types.RuntimeLineRow.t()]`. In `new_support_matrix/1` add `rebuild_matrix: Keyword.get(attrs, :rebuild_matrix, [])`. In `to_map(%SupportMatrix{})` add `"rebuild_matrix" => Enum.map(support_matrix.rebuild_matrix, &to_map/1)` (mirrors the existing `"change_classes"` line).
  </action>
  <verify>
    <automated>mix compile --warnings-as-errors 2>&1 | tail -5; grep -q "rebuild_matrix: \[\]" lib/crosswake/manifest/types.ex</automated>
  </verify>
  <acceptance_criteria>
    - `SupportMatrix` defstruct includes `rebuild_matrix: []` and it is NOT in `@enforce_keys`
    - `@type t` for `SupportMatrix` includes `rebuild_matrix: [Crosswake.Manifest.Types.RuntimeLineRow.t()]`
    - `new_support_matrix/1` resolves `rebuild_matrix` via `Keyword.get(attrs, :rebuild_matrix, [])`
    - `to_map(%SupportMatrix{})` emits a `"rebuild_matrix"` key built by mapping rows through `to_map/1`
    - `mix compile --warnings-as-errors` exits 0 and no existing `new_support_matrix/1` call site raises `KeyError`
  </acceptance_criteria>
  <done>SupportMatrix carries an additive defaulted `rebuild_matrix` list with constructor and serialization support; all existing call sites still compile and construct.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| struct construction → serialized output | New atom fields cross into JSON/to_map; wrong defaults would silently mislabel evidence |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-64-05 | Spoofing | verification_method default | mitigate | Default is `:none` (D-09 / Pitfall 1) — never a stronger tier; iOS sets `:device_verified` explicitly in plan 04, asserted by proof lane `:rline_04` |
| T-64-06 | Tampering | Compatibility struct | mitigate | This plan modifies NOTHING on `Compatibility`; proof lane `:rline_02` asserts the exact 5-field set and unchanged `manifest_schema_version` |
| T-64-07 | Elevation | SupportEntry baseline rows | mitigate | D-11: no `verification_method` added to baseline `SupportEntry`; field lives only on `CapabilitySupportEntry`/`PromotionRuleEntry` |
| T-64-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this phase (pure Elixir) |
</threat_model>

<verification>
`mix compile --warnings-as-errors` exits 0. The phase64 proof lane (plan 01) advances toward green for the struct-shape assertions (`:rline_02` field enumeration) once this lands; it stays RED on RebuildPolicy/SupportMatrix-data/doctor until plans 03-05.
</verification>

<success_criteria>
RuntimeLineRow exists and JSON-encodes; the three new defaulted fields are present, constructor- and to_map-wired, back-compat; Compatibility and SupportEntry are untouched.
</success_criteria>

<output>
Create `.planning/phases/64-runtime-line-policy-contract-support-truth-taxonomy/64-02-SUMMARY.md` when done
</output>
