---
phase: 64-runtime-line-policy-contract-support-truth-taxonomy
plan: 06
type: execute
wave: 1
depends_on: [64-01, 64-04, 64-05]
files_modified:
  - lib/crosswake/support_matrix/support_matrix.ex
  - lib/crosswake/doctor/doctor.ex
  - lib/crosswake/doctor/finding_policy.ex
  - test/crosswake/proof/phase64_runtime_line_policy_test.exs
  - test/crosswake/doctor/doctor_test.exs
autonomous: true
gap_closure: true
requirements: [RLINE-04]
user_setup: []

must_haves:
  truths:
    - "Support truth reports :jvm_hermetic distinctly from :device_verified and NEVER labels CI-only evidence as device-verified — including through the rebuild_matrix surface"
    - "Unverified hosts produce status: :error from mix crosswake.doctor (honest blocking posture restored)"
  artifacts:
    - path: "lib/crosswake/support_matrix/support_matrix.ex"
      provides: "rebuild_matrix with no unbacked :device_verified row + validate_rebuild_matrix_evidence/2 gate in validate/1 pipe"
      contains: "validate_rebuild_matrix_evidence"
    - path: "lib/crosswake/doctor/finding_policy.ex"
      provides: ":error severity restored for :verification_required shell_proof and support_claim findings"
      contains: ":error"
    - path: "test/crosswake/proof/phase64_runtime_line_policy_test.exs"
      provides: ":rline_04 assertion that rebuild_matrix carries no unbacked :device_verified row; doctor proof tests rescue Mix.Error"
      contains: "rebuild_matrix"
    - path: "test/crosswake/doctor/doctor_test.exs"
      provides: "verification_required host asserts status :error; :failed shell asserts status :error"
      contains: "report.status == :error"
  key_links:
    - from: "lib/crosswake/support_matrix/support_matrix.ex validate/1"
      to: "validate_rebuild_matrix_evidence/2"
      via: "pipe append after validate_verification_method_invariant/2"
      pattern: "validate_rebuild_matrix_evidence"
    - from: "lib/crosswake/doctor/finding_policy.ex shell_proof(:verification_required)"
      to: "doctor overall status"
      via: ":error severity drives report.status == :error"
      pattern: "shell_proof\\(:verification_required"
---

<objective>
Close the two BLOCKER gaps found by the Phase 64 verifier (status: gaps_found, 4/5 must-haves). Both gaps stem from work shipped in plan 04 and are honesty-posture violations of RLINE-04 and the phase goal ("so no downstream surface can overclaim").

- **Gap 1 (CR-01 / RLINE-04):** Evidence laundering via the unvalidated `rebuild_matrix` surface. The `@rebuild_matrix_rows` "2.x" row hardcodes `evidence_tier: :device_verified` for a band that does not exist (native_runtime_version is "1.0.0"; device-verified proof is gated until Phases 67/68). `validate/1` never inspects rebuild_matrix rows, so `mix crosswake.doctor` publicly renders `evidence_tier=device-verified` with zero backing proof.
- **Gap 2 (WR-04):** `finding_policy.ex` downgraded `:verification_required` findings from `:error` to `:warning` as an unplanned auto-fix, flipping `mix crosswake.doctor` to `status: :ok` for unverified hosts — an overclaim vector with no D-number mandate.

Purpose: Restore the honesty taxonomy so neither the rebuild_matrix nor the doctor overall-status surface can overclaim, and make the laundering route structurally impossible to re-introduce (not merely remove the current instance).

Output: Validated rebuild_matrix with a structural evidence gate; reverted finding severities; corrected proof + unit tests that lock the honest posture; corrected (non-lying) evidence_posture_snapshot comment/derivation.

This plan does NOT replan or modify the contracts shipped in 64-01..05 beyond the two specific honesty fixes above. Build on the existing code.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/64-runtime-line-policy-contract-support-truth-taxonomy/64-CONTEXT.md
@.planning/phases/64-runtime-line-policy-contract-support-truth-taxonomy/64-VERIFICATION.md
@.planning/phases/64-runtime-line-policy-contract-support-truth-taxonomy/64-04-SUMMARY.md

<interfaces>
<!-- Key contracts the executor needs. Extracted from the codebase — no exploration required. -->

From lib/crosswake/support_matrix/support_matrix.ex (validate/1 pipe, ~line 393-402):
```elixir
def validate(%SupportMatrix{} = support_matrix) do
  []
  |> validate_categories_present(support_matrix)
  |> validate_exact_statuses(support_matrix)
  |> validate_narrow_baseline(support_matrix)
  |> validate_capability_families_present(support_matrix)
  |> validate_phase51_support_truth()
  |> validate_verification_method_invariant(support_matrix)
end
```

From lib/crosswake/support_matrix/support_matrix.ex (the ANALOG to mirror, ~line 875-895):
```elixir
# D-10a: A CapabilitySupportEntry with proof_class :advisory has a CI-only evidence
# corpus. Claiming :device_verified on a CI-only entry is evidence laundering.
defp validate_verification_method_invariant(errors, %SupportMatrix{} = support_matrix) do
  support_matrix.capability_families
  |> Enum.reduce(errors, fn entry, acc ->
    cond do
      entry.verification_method == :device_verified and entry.proof_class == :advisory ->
        [ %{key: :capability_families, message: "...", hint: "..."} | acc ]
      true -> acc
    end
  end)
end
```
Each error map shape is: `%{key: atom(), message: String.t(), hint: String.t()}`.
A RuntimeLineRow row exposes: `runtime_line` (String), `evidence_tier` (atom from D-09 enum), `ota_safe`, `rebuild_required`, `capability_surface`, `change_class`.

From lib/crosswake/doctor/finding_policy.ex (the two clauses to revert, lines 26-29 and 38-41):
```elixir
def shell_proof(:verification_required, label, script_path) do
  {:warning, "proof_hook_verification_required", "..."}   # <- :warning must become :error
end
def support_claim(:verification_required) do
  {:warning, "support_claim_verification_required", "...", "..."}  # <- :warning must become :error
end
```

From lib/mix/tasks/crosswake.doctor.ex (line 54-55) — WHY the proof tests must rescue:
```elixir
if report.status == :error do
  Mix.raise("Crosswake doctor found blocking issues")
end
```
After reverting findings to :error, running `mix crosswake.doctor` against the unverified ManagedRouter host (as the :rline_04 doctor proof tests do at ~lines 375, 410, 472) will raise Mix.Error. The capture_io blocks must rescue Mix.Error so the hermetic lane runs without severity relaxation.

From lib/crosswake/doctor/doctor.ex (~line 1210-1215) — WR-05 lying comment:
```elixir
# D-16: Evidence posture summary keyed by platform — derived from the rebuild_matrix
# evidence_tier values. iOS uses :device_verified; Android uses :jvm_hermetic (CI only).
defp evidence_posture_snapshot(_support_matrix) do
  %{ios: :device_verified, android: :jvm_hermetic}   # discards arg, comment lies
end
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Remove unbacked 2.x row and add structural rebuild_matrix evidence gate (Gap 1, RLINE-04)</name>
  <files>lib/crosswake/support_matrix/support_matrix.ex, lib/crosswake/doctor/doctor.ex</files>
  <read_first>
    - lib/crosswake/support_matrix/support_matrix.ex — @rebuild_matrix_rows (~lines 276-315, the "1.x" and "2.x" rows), validate/1 pipe (~lines 393-402), and validate_verification_method_invariant/2 (~lines 871-895, THE ANALOG for the new clause — same reduce-over-rows + error-map shape).
    - lib/crosswake/doctor/doctor.ex — evidence_posture_snapshot/1 (~lines 1210-1215) and its call site in release_policy_snapshot (~line 1206).
    - .planning/phases/64-runtime-line-policy-contract-support-truth-taxonomy/64-CONTEXT.md — D-10a (evidence-laundering invariant), D-14 (one row per band), D-15 (evidence_tier reuses D-09 enum, matrix displays not re-decides).
  </read_first>
  <behavior>
    - SupportMatrix.canonical() |> SupportMatrix.rebuild_matrix/1 returns rows where the only runtime_line is "1.x"; no row has runtime_line "2.x".
    - SupportMatrix.validate(SupportMatrix.canonical()) returns [] (canonical is clean).
    - SupportMatrix.validate/1 on a matrix whose rebuild_matrix contains a row with evidence_tier: :device_verified (with no backing device-verified promotion evidence) returns a NON-empty error list whose message references evidence laundering / the row's runtime_line.
    - SupportMatrix.validate/1 on a matrix whose rebuild_matrix rows are all :jvm_hermetic / :none returns [] (no false positive).
  </behavior>
  <action>
    1. In @rebuild_matrix_rows (~lines 296-314), DELETE the entire second Types.new_runtime_line_row(...) entry — the one with runtime_line: "2.x" and evidence_tier: :device_verified. Keep ONLY the "1.x" row (evidence_tier: :jvm_hermetic). The 2.x band does not exist (native_runtime_version is "1.0.0") and device-verified proof is gated until Phases 67/68 per the shell.android.device_verified promotion rule. Leave the trailing comma/list syntax valid.
    2. Add a private clause validate_rebuild_matrix_evidence/2 mirroring validate_verification_method_invariant/2 exactly in shape: reduce over support_matrix.rebuild_matrix, and for any row whose evidence_tier == :device_verified where no corresponding passed device-verified promotion evidence exists, append an error map %{key: :rebuild_matrix, message: ..., hint: ...}. The "no backing evidence" condition for this phase: there is NO promotion_rules() entry of required_verification_method: :device_verified whose status is promoted/passed (the shell.android.device_verified rule is explicitly GATED, so device-verified is never backed in Phase 64). Concretely: reject any :device_verified rebuild_matrix row unconditionally in Phase 64 since the device-verified corpus does not exist yet. The message MUST reference evidence laundering (D-10a) and name the offending runtime_line; the hint MUST state that :device_verified requires real-device proof gated until Phases 67/68 and that CI-only bands use :jvm_hermetic.
    3. Append `|> validate_rebuild_matrix_evidence(support_matrix)` to the validate/1 pipe, after validate_verification_method_invariant(support_matrix) (~line 401).
    4. Fix the WR-05 lying derivation in doctor.ex evidence_posture_snapshot/1 (~line 1213): the comment claims it is "derived from the rebuild_matrix" but the function discards its argument and returns a hardcoded map. Either (a) derive ios/android tiers from the now-1.x-only rebuild_matrix evidence_tier values (read the support_matrix.rebuild_matrix rows), OR (b) at minimum correct the comment so it does not claim derivation it does not perform — state plainly it is a fixed Phase-64 posture map (ios=:device_verified from iOS native capability entries, android=:jvm_hermetic CI-only). The comment MUST NOT lie about its source. Prefer (a) if the 1.x row's evidence_tier suffices; otherwise (b) with an honest comment.
  </action>
  <verify>
    <automated>mix compile --warnings-as-errors 2>&1 | tail -5; mix test test/crosswake/support_matrix/ 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - support_matrix.ex: @rebuild_matrix_rows contains no row with `runtime_line: "2.x"` (grep -c '"2.x"' on the @rebuild_matrix_rows block returns 0).
    - support_matrix.ex: @rebuild_matrix_rows contains no row with `evidence_tier: :device_verified` (grep -v '^#' lib/crosswake/support_matrix/support_matrix.ex within the rows block shows no `evidence_tier: :device_verified`).
    - support_matrix.ex: validate/1 pipe includes `validate_rebuild_matrix_evidence`.
    - support_matrix.ex: defp validate_rebuild_matrix_evidence/2 exists and iterates support_matrix.rebuild_matrix.
    - doctor.ex: evidence_posture_snapshot comment no longer asserts a derivation the function does not perform (either it now reads support_matrix.rebuild_matrix, or the comment is corrected to describe a fixed posture map).
    - `mix compile --warnings-as-errors` exits 0.
  </acceptance_criteria>
  <done>The unbacked :device_verified rebuild_matrix row is removed AND a validation gate structurally rejects any future :device_verified rebuild_matrix row lacking backing proof; the evidence_posture_snapshot comment is honest.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Close the rebuild_matrix proof blind spot and rescue Mix.Error in doctor proof tests (Gap 1 test coverage)</name>
  <files>test/crosswake/proof/phase64_runtime_line_policy_test.exs</files>
  <read_first>
    - test/crosswake/proof/phase64_runtime_line_policy_test.exs — the :rline_04 block (~lines 347-510): note the existing tests assert only against capability_families entries and the validate/1 CI-only rejection; the rebuild_matrix surface is NOT asserted. Note the capture_io + Mix.Task.run pattern at ~lines 237, 261, 297, 375, 410, 472.
    - lib/mix/tasks/crosswake.doctor.ex (~line 54-55) — Mix.raise("Crosswake doctor found blocking issues") when report.status == :error. After Task 3 reverts findings to :error, the unverified ManagedRouter host will raise here inside capture_io.
    - 64-01-PLAN.md test comment acknowledging the Phase 52 capture_io pattern (the rescue Mix.Error approach).
  </read_first>
  <behavior>
    - New :rline_04 test: SupportMatrix.rebuild_matrix(SupportMatrix.canonical()) contains NO row with evidence_tier == :device_verified — asserts the rebuild_matrix surface directly (closing the blind spot), not just capability_families.
    - New (or extended) :rline_04 test: SupportMatrix.validate/1 returns a non-empty error list when a :device_verified rebuild_matrix row is injected into an otherwise-canonical matrix.
    - Existing :rline_04 doctor-output tests (the capture_io + Mix.Task.run ManagedRouter ones) still run green even though the host is unverified and the doctor task now raises Mix.Error.
  </behavior>
  <action>
    1. Add a :rline_04 tagged test asserting rebuild_matrix honesty directly: fetch SupportMatrix.rebuild_matrix(SupportMatrix.canonical()), assert Enum.all? rows have evidence_tier in [:none, :provider_advisory, :jvm_hermetic, :emulator_advisory] (i.e. never :device_verified) — use ProofAssertions.stable_id_message with a new stable id e.g. "proof.rline_04.rebuild_matrix.no_unbacked_device_verified" and severity :merge_blocking. This closes the test blind spot where green tests asserted only against capability_families.
    2. Add (or extend) a :rline_04 tagged test that injects a row with evidence_tier: :device_verified into a clone of canonical's rebuild_matrix (mirror the existing capability_families injection test at ~lines 432-466) and asserts SupportMatrix.validate/1 returns a non-empty list — locking the validate_rebuild_matrix_evidence/2 gate added in Task 1.
    3. In every :rline_04 (and any other) doctor-output test that wraps Mix.Task.run(@doctor_task, ["--router", ...ManagedRouter...]) in capture_io against the UNVERIFIED host, wrap the Mix.Task.run call in `try ... rescue Mix.Error -> :ok end` (or `try/rescue` inside the capture_io fn) so the captured output is still produced and the raised Mix.Error (now expected after Task 3 restores :error severity) does not fail the test. The output assertion stays the same — only the raise is rescued. Do NOT relax any severity to avoid the raise; the rescue is the correct hermetic pattern (Phase 52 precedent).
  </action>
  <verify>
    <automated>mix test test/crosswake/proof/phase64_runtime_line_policy_test.exs 2>&1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - phase64_runtime_line_policy_test.exs contains a :rline_04 test referencing `rebuild_matrix` that asserts no `:device_verified` row (new stable id present, e.g. "proof.rline_04.rebuild_matrix.no_unbacked_device_verified").
    - phase64_runtime_line_policy_test.exs contains a :rline_04 test injecting a `:device_verified` rebuild_matrix row and asserting validate/1 returns non-empty.
    - Every capture_io block running mix crosswake.doctor against ManagedRouter rescues Mix.Error (grep shows `rescue Mix.Error` inside the affected test bodies).
    - `mix test test/crosswake/proof/phase64_runtime_line_policy_test.exs` exits 0.
  </acceptance_criteria>
  <done>The :rline_04 proof lane asserts rebuild_matrix honesty directly (blind spot closed) and the doctor-output proof tests survive the restored :error posture via rescue Mix.Error rather than severity relaxation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Revert finding severities to :error and lock honest doctor posture in unit tests (Gap 2, WR-04)</name>
  <files>lib/crosswake/doctor/finding_policy.ex, test/crosswake/doctor/doctor_test.exs</files>
  <read_first>
    - lib/crosswake/doctor/finding_policy.ex — shell_proof(:verification_required, ...) (~lines 26-29) and support_claim(:verification_required) (~lines 38-41), both currently returning :warning. Note shell_proof(:failed, ...) (~lines 16-19) already returns :error — the honest reference.
    - test/crosswake/doctor/doctor_test.exs — the verification_required test (~lines 72-104, line 84 asserts report.status == :ok with a Phase-64 comment), the write_proof_hook!/4 helper (~lines 1082-1100, exit_status arg controls pass/fail), and the existing :error assertion at ~line 657 for reference.
  </read_first>
  <behavior>
    - FindingPolicy.shell_proof(:verification_required, label, path) returns a tuple whose first element is :error.
    - FindingPolicy.support_claim(:verification_required) returns a tuple whose first element is :error.
    - Doctor.run against the unverified ManagedRouter host (proof status :verification_required) yields report.status == :error.
    - Doctor.run against a host whose shell proof hook exits non-zero (:failed) also yields report.status == :error (compensating assertion locking the honest posture).
  </behavior>
  <action>
    1. In finding_policy.ex, revert shell_proof(:verification_required, label, script_path) (~line 27) first tuple element from :warning to :error. Keep the "proof_hook_verification_required" key and message string unchanged.
    2. In finding_policy.ex, revert support_claim(:verification_required) (~line 39) first tuple element from :warning to :error. Keep the "support_claim_verification_required" key, message, and remediation strings unchanged.
    3. In doctor_test.exs (~line 84), revert `assert report.status == :ok` to `assert report.status == :error` for the verification_required state. Update the misleading Phase-64 comment (~lines 81-83) to state the honest posture: unverified shell proofs are blocking (:error) until --native-checks runs them; the test must catch the regression, not confirm it. Leave the other assertions in that test (manifest, support.status == :verification_required, shells.*.proof.status == :verification_required, release_policy fields) unchanged — only report.status flips.
    4. Add a compensating test (or extend an existing setup) that writes a FAILING shell proof hook via write_proof_hook!(target, "android", 1, "android proof failed") (exit_status 1 = :failed), runs Doctor.run against ManagedRouter, and asserts report.status == :error AND report.shells.android.proof.status reflects the failure. This locks the honest posture so a :failed shell can never silently pass.
  </action>
  <verify>
    <automated>mix test test/crosswake/doctor/doctor_test.exs 2>&1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - finding_policy.ex: shell_proof(:verification_required, ...) returns a tuple with first element :error (grep shows `{:error, "proof_hook_verification_required"`).
    - finding_policy.ex: support_claim(:verification_required) returns a tuple with first element :error (grep shows `{:error, "support_claim_verification_required"`).
    - finding_policy.ex: no `:warning` remains on the :verification_required clauses.
    - doctor_test.exs: the verification_required test asserts `report.status == :error` (the `== :ok` assertion is gone for that state).
    - doctor_test.exs: a test asserts a :failed shell (write_proof_hook! exit_status 1) produces `report.status == :error`.
    - `mix test test/crosswake/doctor/doctor_test.exs` exits 0.
  </acceptance_criteria>
  <done>mix crosswake.doctor reports status: :error for an unverified host (honest blocking posture restored), the unit test catches rather than confirms the regression, and a :failed shell is proven to also block.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

This is an internal policy/taxonomy module with no new external attack surface. The relevant boundary is the **support-truth publication boundary**: where Crosswake's internal evidence state is rendered into operator-facing `mix crosswake.doctor` output and machine-consumable JSON / CI gates. The "untrusted" direction here is outward overclaim — surfacing stronger evidence than exists.

| Boundary | Description |
|----------|-------------|
| internal evidence state → doctor human/JSON output | A surface may render evidence stronger than the backing proof corpus (integrity/overclaim) |
| internal evidence state → CI status gate | A green/:ok overall status may signal "verified" for an unverified host (integrity/overclaim) |

## STRIDE Threat Register

The dominant threat class is **integrity / overclaim**, not typical web threats (no new I/O, auth, or network surface in this plan).

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-64-01 | Tampering (integrity) | @rebuild_matrix_rows :device_verified row rendered by doctor formatters | mitigate | Task 1: remove unbacked :device_verified row AND add validate_rebuild_matrix_evidence/2 gate so re-introduction is structurally rejected, not just deleted |
| T-64-02 | Spoofing (evidence provenance) | rebuild_matrix surface claiming device-verified provenance without device proof | mitigate | Task 1 validation gate rejects :device_verified absent a passed device-verified promotion rule (gated until Phases 67/68); Task 2 proof asserts the property directly, closing the test blind spot |
| T-64-03 | Repudiation / false-negative gate | finding_policy.ex :verification_required severity → doctor overall :ok for unverified host | mitigate | Task 3: revert to :error so CI gates on status see the unverified state; Task 3 compensating test locks :failed → :error |
| T-64-04 | Information disclosure (misleading) | doctor.ex evidence_posture_snapshot/1 comment claims rebuild_matrix derivation it does not perform | mitigate | Task 1: derive from rebuild_matrix or correct the comment so it does not lie |
| T-64-SC | Tampering | npm/pip/cargo installs | accept | No package-manager installs in this plan; no dependency surface changes |
</threat_model>

<verification>
Run the full affected test surface and confirm both honesty postures from the verifier's behavioral spot-checks:

1. `mix test test/crosswake/proof/phase64_runtime_line_policy_test.exs` — exits 0 (18+ tests, including new rebuild_matrix :rline_04 assertions).
2. `mix test test/crosswake/support_matrix/ test/crosswake/doctor/doctor_test.exs` — exits 0.
3. `mix compile --warnings-as-errors` — exits 0.
4. Doctor must NOT render device-verified for an unbacked band:
   `MIX_ENV=test mix crosswake.doctor --router Elixir.Crosswake.TestSupport.RouterFixtures.ManagedRouter 2>&1 | grep -c 'evidence_tier=device-verified'` returns 0 (note: the doctor task now exits non-zero with Mix.Error — that is the correct WR-04 posture; capture output before checking, e.g. append `|| true`).
5. Doctor reports :error for an unverified host:
   `MIX_ENV=test mix crosswake.doctor --router Elixir.Crosswake.TestSupport.RouterFixtures.ManagedRouter; echo "exit=$?"` — exit code is non-zero (Mix.raise on status: :error).
6. Full suite regression: `mix test 2>&1 | tail -5` — no new failures introduced.
</verification>

<success_criteria>
- All five observable truths in 64-VERIFICATION.md verify (the two failed truths — rebuild_matrix honesty and unverified-host :error posture — now pass; the three already-passing truths are unaffected).
- `@rebuild_matrix_rows` carries no `:device_verified` row and a validation gate rejects any future one.
- `mix crosswake.doctor` renders no `evidence_tier=device-verified` for an unbacked band.
- `mix crosswake.doctor` reports `status: :error` (non-zero exit) for an unverified host.
- `evidence_posture_snapshot/1` comment is internally consistent (does not lie about its source).
- The :rline_04 proof lane asserts rebuild_matrix honesty directly (blind spot closed).
- doctor_test.exs catches the WR-04 regression rather than confirming it, and a :failed shell is proven to block.
- RLINE-04 moves from BLOCKED to SATISFIED.
</success_criteria>

<output>
Create `.planning/phases/64-runtime-line-policy-contract-support-truth-taxonomy/64-06-SUMMARY.md` when done.
</output>
