---
phase: 02-manifest-truth-and-compatibility
plan: 04
type: execute
wave: 4
depends_on:
  - 02-01
  - 02-03
files_modified:
  - lib/crosswake/support_matrix/renderer.ex
  - test/crosswake/support_matrix/renderer_test.exs
  - guides/install.md
  - guides/compatibility.md
  - guides/support_matrix.md
autonomous: true
requirements:
  - DX-04
  - MANI-04
must_haves:
  truths:
    - "Adopters can read compatibility and support docs generated or mechanically checked from the same canonical truth as doctor and manifest validation."
    - "Published support claims stay narrow, proof-oriented, and explicitly classified as supported, expected but unproven, or unsupported."
    - "Install guidance points to the doctor task and compatibility boundaries without implying Phase 3 shell-runtime proof already exists."
  artifacts:
    - path: "lib/crosswake/support_matrix/renderer.ex"
      provides: "Renderer that turns canonical support truth into mechanically checked Markdown output"
    - path: "guides/compatibility.md"
      provides: "Public compatibility-boundary, rough-edge, and non-goal guidance for adopters"
    - path: "guides/support_matrix.md"
      provides: "Checked support-matrix document derived from canonical support truth"
  key_links:
    - from: "lib/crosswake/support_matrix/renderer.ex"
      to: "guides/support_matrix.md"
      via: "generated or mechanically checked docs derive from canonical support truth"
      pattern: "supported|expected but unproven|unsupported"
    - from: "guides/install.md"
      to: "lib/mix/tasks/crosswake.doctor.ex"
      via: "install guidance points adopters to the primary doctor command"
      pattern: "mix crosswake.doctor"
---

<objective>
Ship the adopter-facing documentation surface for Phase 2 as a distinct follow-on slice so doctor implementation stays focused and public support claims remain mechanically tied to canonical truth.

Purpose: Fulfill DX-04 and MANI-04 without overloading the doctor implementation plan or letting support docs drift from the contract source.
Output: Support-matrix renderer, compatibility guide, support matrix guide, and install-guide updates aligned to Phase 2's honest boundary posture.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-manifest-truth-and-compatibility/02-CONTEXT.md
@.planning/phases/02-manifest-truth-and-compatibility/02-RESEARCH.md
@.planning/phases/02-manifest-truth-and-compatibility/02-PATTERNS.md
@.planning/phases/02-manifest-truth-and-compatibility/02-01-PLAN.md
@.planning/phases/02-manifest-truth-and-compatibility/02-03-PLAN.md
@guides/install.md

<interfaces>
From lib/crosswake/support_matrix/support_matrix.ex:
```elixir
# Plan 02-01 establishes the canonical support truth consumed here.
```

From lib/mix/tasks/crosswake.doctor.ex:
```elixir
# Plan 02-03 establishes the doctor command referenced by install docs here.
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Render support truth into a checked support-matrix guide</name>
  <read_first>.planning/phases/02-manifest-truth-and-compatibility/02-CONTEXT.md, .planning/phases/02-manifest-truth-and-compatibility/02-RESEARCH.md, .planning/phases/02-manifest-truth-and-compatibility/02-PATTERNS.md, .planning/phases/02-manifest-truth-and-compatibility/02-01-PLAN.md, lib/crosswake/support_matrix/support_matrix.ex, lib/crosswake/install/manifest.ex</read_first>
  <files>lib/crosswake/support_matrix/renderer.ex, test/crosswake/support_matrix/renderer_test.exs, guides/support_matrix.md</files>
  <behavior>
    - Test 1: The renderer emits deterministic Markdown from canonical support truth and preserves the `:created | :reused | :updated` artifact-writer semantics.
    - Test 2: The generated guide renders the exact public statuses `supported`, `expected but unproven`, and `unsupported` from canonical data per D-16.
    - Test 3: A generated-doc drift check fails when `guides/support_matrix.md` diverges from the current renderer output, satisfying D-26 with mechanical proof instead of prose trust.
  </behavior>
  <action>Create `Crosswake.SupportMatrix.Renderer` to render canonical support truth into Markdown using the deterministic artifact-writer pattern mapped in `02-PATTERNS.md` from `lib/crosswake/install/manifest.ex`. Add dedicated renderer tests in `test/crosswake/support_matrix/renderer_test.exs` that assert deterministic output, exact status rendering, and idempotent write behavior. Use the renderer to generate or mechanically verify `guides/support_matrix.md`, keeping claims narrow per D-15 and statuses rendered literally as supported, expected but unproven, or unsupported per D-16. Include a direct generated-doc consistency check in the test or verification path so drift between canonical truth and `guides/support_matrix.md` fails automation immediately per D-26. Do not introduce ad hoc prose-only baselines outside the canonical support data from Plan 02-01.</action>
  <verify>
    <automated>mix test test/crosswake/support_matrix/renderer_test.exs</automated>
  </verify>
  <acceptance_criteria>
    <automated>test -f lib/crosswake/support_matrix/renderer.ex && test -f test/crosswake/support_matrix/renderer_test.exs && test -f guides/support_matrix.md</automated>
    <automated>mix test test/crosswake/support_matrix/renderer_test.exs</automated>
    <automated>rg -n 'supported|expected but unproven|unsupported' guides/support_matrix.md test/crosswake/support_matrix/renderer_test.exs</automated>
  </acceptance_criteria>
  <done>`guides/support_matrix.md` is generated or mechanically checked from the same support truth used by doctor and manifest validation, and the renderer has its own direct automated proof.</done>
</task>

<task type="auto">
  <name>Task 2: Publish compatibility-boundary guidance and wire install docs to doctor</name>
  <read_first>.planning/phases/02-manifest-truth-and-compatibility/02-CONTEXT.md, .planning/phases/02-manifest-truth-and-compatibility/02-RESEARCH.md, .planning/phases/02-manifest-truth-and-compatibility/02-03-PLAN.md, guides/install.md</read_first>
  <files>guides/install.md, guides/compatibility.md</files>
  <action>Add `guides/compatibility.md` that explains runtime ownership boundaries, layered compatibility axes, bundled/cached/remote manifest posture, replacement-versus-companion remote update boundaries, fail-closed activation, rough edges, and explicit non-goals per D-24 and D-25. Update `guides/install.md` to point at `mix crosswake.doctor`, `guides/compatibility.md`, and `guides/support_matrix.md` while preserving the existing honest-boundary tone. Add mechanically checked content assertions in the verification path so the docs literally state the Phase 2 compatibility boundaries, the exact support statuses, and the install-guide doctor links required by D-26, MANI-04, and DX-04. Keep the docs explicit that native shell generation exists but shell boot/runtime proof remains Phase 3, so Phase 2 does not overclaim solved runtime support per D-25 and D-26.</action>
  <verify>
    <automated>mix test test/crosswake/support_matrix/renderer_test.exs && rg -n 'supported|expected but unproven|unsupported|Phoenix|LiveView|iOS|Android' guides/support_matrix.md && rg -n 'runtime ownership|manifest_schema_version|bridge_protocol_version|native_runtime_version|bundled|cached|remote|fail-closed|Phase 3' guides/compatibility.md && rg -n 'mix crosswake\\.doctor|guides/compatibility\\.md|guides/support_matrix\\.md' guides/install.md</automated>
  </verify>
  <acceptance_criteria>
    <automated>test -f guides/install.md && test -f guides/compatibility.md && test -f guides/support_matrix.md</automated>
    <automated>rg -n 'mix crosswake\\.doctor|guides/compatibility\\.md|guides/support_matrix\\.md' guides/install.md</automated>
    <automated>rg -n 'runtime ownership|manifest_schema_version|bridge_protocol_version|native_runtime_version|bundled|cached|remote|fail-closed|Phase 3' guides/compatibility.md</automated>
    <automated>rg -n 'supported|expected but unproven|unsupported' guides/support_matrix.md</automated>
  </acceptance_criteria>
  <done>Adopter docs describe support and compatibility truth honestly, prove the exact support statuses and compatibility boundaries in automation, point to the doctor workflow, and avoid implying Phase 3 runtime proof already exists.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Canonical support truth -> published docs | Prose drift can overstate support or hide rough edges |
| Doctor command -> install and compatibility guides | Docs can point to the wrong verification surface and mislead adopters |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-16 | S | `guides/support_matrix.md` | mitigate | Generate or mechanically verify support claims from canonical support truth so docs cannot spoof broader compatibility than the contract proves |
| T-02-17 | R | `guides/compatibility.md` | mitigate | Document explicit runtime boundaries, rough edges, and non-goals so adopter expectations are anchored to the real Phase 2 contract |
| T-02-18 | T | `lib/crosswake/support_matrix/renderer.ex` | mitigate | Keep rendering deterministic and source-of-truth-driven so checked docs cannot drift silently from manifest and doctor data |
</threat_model>

<verification>
Run `mix test test/crosswake/support_matrix/renderer_test.exs` and the grep assertions in Task 2 to prove `guides/support_matrix.md`, `guides/compatibility.md`, and `guides/install.md` literally contain the generated support statuses, compatibility boundaries, and doctor links promised by Phase 2.
</verification>

<success_criteria>
- Support docs are generated or mechanically checked from canonical truth.
- Install docs point to `mix crosswake.doctor` and the new compatibility/support guides.
- Phase 2 docs remain explicit about rough edges and do not imply broader shell-runtime proof than the roadmap allows.
- Renderer tests and docs drift/content assertions directly prove the docs slice rather than relying on doctor tests as proxy coverage.
</success_criteria>

<output>
After completion, create `.planning/phases/02-manifest-truth-and-compatibility/02-manifest-truth-and-compatibility-04-SUMMARY.md`
</output>
