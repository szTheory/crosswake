---
phase: 152-capability-map-collateral-and-v20-handoff
plan: 03
type: execute
wave: 1
depends_on:
  - 152-01
files_modified:
  - examples/phoenix_host/e2e/route_tour.spec.ts
  - examples/phoenix_host/e2e/support/evidence_manifest.ts
  - examples/phoenix_host/evidence/evidence-manifest.example.json
  - test/crosswake/guides/evidence_manifest_test.exs
  - examples/phoenix_host/test/crosswake_example/showcase/reset_test.exs
  - .github/workflows/offline-sync-e2e-gate.yml
autonomous: true
requirements:
  - PROOF-01
  - PROOF-02
  - PROOF-03
  - PROOF-04
  - CAPMAP-01
  - CAPMAP-03
must_haves:
  truths:
    - "D-14: Route-tour proof remains semantic-first; screenshots are generated only after assertions pass."
    - "D-15/D-16: Evidence manifest covers showcase hub, AdminPilot, Fieldserv, LearnLoop, bridge, offline study, native fallback, and capability pressure rows."
    - "D-19: Server reset proof remains explicit that browser-owned IndexedDB and outbox state are not reset by server fixtures."
  artifacts:
    - path: "examples/phoenix_host/e2e/support/evidence_manifest.ts"
      provides: "Generalized v19 route-tour evidence manifest writer"
    - path: "examples/phoenix_host/evidence/evidence-manifest.example.json"
      provides: "Committed manifest fixture for schema tests"
    - path: "test/crosswake/guides/evidence_manifest_test.exs"
      provides: "Manifest schema, posture, limitations, retention, and artifact validation"
    - path: ".github/workflows/offline-sync-e2e-gate.yml"
      provides: "CI artifact checks and honest route-tour evidence summary"
  key_links:
    - from: "examples/phoenix_host/e2e/route_tour.spec.ts"
      to: "examples/phoenix_host/e2e/support/evidence_manifest.ts"
      via: "writeRouteTourEvidenceManifest(screenshotDir, routeTourCommand)"
      pattern: "writeRouteTourEvidenceManifest\\("
    - from: "test/crosswake/guides/evidence_manifest_test.exs"
      to: "examples/phoenix_host/evidence/evidence-manifest.example.json"
      via: "read_manifest! fixture validation"
      pattern: "evidence-manifest\\.example\\.json"
    - from: "examples/phoenix_host/test/crosswake_example/showcase/reset_test.exs"
      to: "examples/phoenix_host/lib/crosswake_example/showcase/reset.ex"
      via: "Reset.reset!/0 idempotence and browser_state_reset assertions"
      pattern: "Reset\\.reset!\\("
---

<objective>
Generalize v19 proof evidence so route-tour behavior, manifest metadata, reset determinism, and CI artifact summaries support the capability map without overclaiming native/offline/commerce behavior.

Purpose: Satisfy PROOF-01 through PROOF-04 by preserving semantic-first browser proof, explicit reset boundaries, and honest evidence/collateral metadata.
Output: Cross-lane evidence manifest rows, committed fixture JSON, schema tests, reset assertions, and CI summary/artifact checks.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/152-capability-map-collateral-and-v20-handoff/152-CONTEXT.md
@.planning/phases/152-capability-map-collateral-and-v20-handoff/152-RESEARCH.md
@.planning/phases/152-capability-map-collateral-and-v20-handoff/152-PATTERNS.md
@.planning/phases/152-capability-map-collateral-and-v20-handoff/152-VALIDATION.md
@.planning/phases/149-saas-admin-showcase/149-VERIFICATION.md
@.planning/phases/150-field-service-showcase/150-07-SUMMARY.md
@.planning/phases/151-subscription-learning-showcase/151-VERIFICATION.md
@examples/phoenix_host/e2e/route_tour.spec.ts
@examples/phoenix_host/e2e/learnloop_route_tour.spec.ts
@examples/phoenix_host/e2e/support/offline_route_proof.ts
@examples/phoenix_host/e2e/support/evidence_manifest.ts
@examples/phoenix_host/evidence/evidence-manifest.example.json
@test/crosswake/guides/evidence_manifest_test.exs
@examples/phoenix_host/lib/crosswake_example/showcase/reset.ex
@examples/phoenix_host/test/crosswake_example/showcase/reset_test.exs
@.github/workflows/offline-sync-e2e-gate.yml
</context>

## Artifacts this phase produces

- A route-tour screenshot artifact `screenshots/showcase-hub.png` captured after showcase hub semantic assertions.
- Generalized `routeTourEntries/1` in `examples/phoenix_host/e2e/support/evidence_manifest.ts`.
- Generalized `EvidenceRoute` metadata fields for support label, proof class, capability posture, package owner, retention, limitations, and unavailable reasons.
- Updated `examples/phoenix_host/evidence/evidence-manifest.example.json` fixture with v19 lane and pressure rows.
- Expanded `test/crosswake/guides/evidence_manifest_test.exs` schema and non-claim validation.
- Reset proof assertions for deterministic lane counts/digest and browser-state boundary.
- CI route-tour artifact checks and step summary text that names semantic proof before screenshots.

<tasks>

<task type="auto">
  <name>Task 1: Capture showcase hub evidence after semantic assertions</name>
  <files>examples/phoenix_host/e2e/route_tour.spec.ts</files>
  <read_first>
    - `examples/phoenix_host/e2e/route_tour.spec.ts` target file to modify.
    - `examples/phoenix_host/e2e/support/evidence_manifest.ts` to keep artifact paths synchronized.
    - `.planning/phases/149-saas-admin-showcase/149-07-SUMMARY.md`, `.planning/phases/150-field-service-showcase/150-07-SUMMARY.md`, and `.planning/phases/151-subscription-learning-showcase/151-07-SUMMARY.md` for screenshot-after-assertion patterns.
    - `.planning/phases/152-capability-map-collateral-and-v20-handoff/152-PATTERNS.md` section for `examples/phoenix_host/e2e/route_tour.spec.ts`.
  </read_first>
  <action>Update the first `@learnloop` route-tour test so it calls `captureRouteScreenshot(page, "showcase-hub.png")` immediately after `await proveShowcaseHub(page)` and before leaving the hub. Keep all existing AdminPilot, Fieldserv, LearnLoop, library, bridge, offline, and native-fallback assertions before their screenshot captures per D-14/D-18/D-23. Do not weaken any existing `ownerMessage` assertions, unsupported-claim checks, overflow checks, or LiveView connection waits.</action>
  <verify>
    <automated>cd examples/phoenix_host && npx playwright test e2e/route_tour.spec.ts --grep @learnloop</automated>
  </verify>
  <acceptance_criteria>
    - `route_tour.spec.ts` captures `showcase-hub.png` only after `proveShowcaseHub(page)`.
    - `writeRouteTourEvidenceManifest(screenshotDir, routeTourCommand)` still runs after all semantic assertions and screenshot captures in the first test.
    - Existing screenshot names remain unchanged: `adminpilot-dashboard.png`, `adminpilot-approvals.png`, `adminpilot-approval-approved.png`, `adminpilot-diagnostics.png`, `fieldserv-jobs.png`, `fieldserv-job-detail.png`, `fieldserv-inspection.png`, `fieldserv-capture-handoff.png`, `fieldserv-evidence-review.png`, `learnloop-dashboard.png`, `learnloop-course.png`, `learnloop-subscription.png`, `learnloop-pack.png`, `learnloop-history-diagnostics.png`, `learnloop-route-tour.png`, `library.png`, `bridge-proof.png`, `offline-study-replayed.png`, and `selective-native-claim-capture-unavailable.png`.
  </acceptance_criteria>
  <done>Showcase hub screenshot collateral is captured after semantic hub assertions and before manifest generation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Generalize evidence manifest rows and fixture</name>
  <files>examples/phoenix_host/e2e/support/evidence_manifest.ts, examples/phoenix_host/evidence/evidence-manifest.example.json, test/crosswake/guides/evidence_manifest_test.exs</files>
  <read_first>
    - `examples/phoenix_host/e2e/support/evidence_manifest.ts` target file to modify.
    - `examples/phoenix_host/evidence/evidence-manifest.example.json` target fixture to modify.
    - `test/crosswake/guides/evidence_manifest_test.exs` target test to satisfy from Plan 152-01.
    - `examples/phoenix_host/e2e/route_tour.spec.ts` for exact screenshot artifact names.
    - `examples/phoenix_host/e2e/support/offline_route_proof.ts` for LearnLoop route ids and screenshot names.
    - `.planning/phases/152-capability-map-collateral-and-v20-handoff/152-CONTEXT.md` D-15 through D-18 and D-23.
  </read_first>
  <action>Update `EvidenceRoute` and `routeTourEntries/1` so the manifest covers `showcase-hub`, AdminPilot approval flow rows, Fieldserv rows, LearnLoop rows, `library`, `bridge-proof`, `offline-study`, `selective-native-claim-capture`, and named capability-pressure rows. Use exact support labels from D-02 and exact collateral labels from D-18. Keep `proof_class` separate from `support_label` and `capability_posture`; proof posture must use merge-blocking/advisory/not-yet-proven/unsupported semantics, and screenshots must not be a proof posture per D-05/D-14/D-23. Update `evidence-manifest.example.json` to match the generalized route set and current Crosswake version. Update `evidence_manifest_test.exs` so expected route ids, allowed labels, optional fields, required limitations, unavailable reasons, and artifact existence checks all match the new manifest. Retain non-claim limitation text for native share, physical device, camera, media upload, provider authority, backend entitlement, offline mutation, and native rebuild boundaries per D-16/D-24.</action>
  <verify>
    <automated>mix test test/crosswake/guides/evidence_manifest_test.exs -x</automated>
  </verify>
  <acceptance_criteria>
    - `routeTourEntries/1` includes captured rows for showcase hub, AdminPilot, Fieldserv, LearnLoop, bridge, offline study, and native fallback.
    - Capability-pressure rows use `status: "unavailable"` with concrete `unavailable_reason`, `known_limitations`, `retention_label`, `support_label`, `capability_posture`, and `package_owner`.
    - The committed example manifest validates without generated Playwright artifacts by using the test fixture artifact root.
    - Synthetic regressions still reject missing root fields, route fields, retention labels, limitations, unavailable reasons, unsupported labels, and missing required artifacts.
  </acceptance_criteria>
  <done>The generalized evidence manifest and schema tests pass.</done>
</task>

<task type="auto">
  <name>Task 3: Preserve reset proof and CI evidence summary honesty</name>
  <files>examples/phoenix_host/test/crosswake_example/showcase/reset_test.exs, .github/workflows/offline-sync-e2e-gate.yml</files>
  <read_first>
    - `examples/phoenix_host/test/crosswake_example/showcase/reset_test.exs` target test file to modify.
    - `examples/phoenix_host/lib/crosswake_example/showcase/reset.ex` source under test.
    - `.github/workflows/offline-sync-e2e-gate.yml` target workflow to modify.
    - `examples/phoenix_host/e2e/support/evidence_manifest.ts` for the final required artifact paths.
    - `.planning/phases/152-capability-map-collateral-and-v20-handoff/152-VALIDATION.md` PROOF-01 and PROOF-02 rows.
  </read_first>
  <action>Extend reset tests only if needed so PROOF-01 explicitly proves two server resets produce stable lane counts and digest, `browser_state_reset` remains `false`, and lane count assertions include `:saas_admin`, `:field_service`, and `:learning_training`. Do not change `Showcase.Reset` to clear browser-owned IndexedDB or outboxes per D-19. Update `.github/workflows/offline-sync-e2e-gate.yml` route-tour evidence checks so required screenshot paths match the generalized manifest, including `showcase-hub.png`, AdminPilot, Fieldserv, LearnLoop, library, bridge, offline study, and native fallback artifacts. Update the step summary to say semantic Playwright assertions covered the v19 route ids before screenshots, screenshots are collateral, and native/device/provider/offline-mutation/commerce authority are not claimed.</action>
  <verify>
    <automated>cd examples/phoenix_host && mix test --warnings-as-errors test/crosswake_example/showcase/reset_test.exs</automated>
    <automated>mix test test/crosswake/guides/evidence_manifest_test.exs -x</automated>
  </verify>
  <acceptance_criteria>
    - Reset tests still prove stable digest and `browser_state_reset: false`.
    - Workflow artifact checks require every captured screenshot referenced by merge-blocking captured manifest rows.
    - Workflow summary preserves the required non-claims and 14-day retention unless an existing project constant says otherwise.
  </acceptance_criteria>
  <done>Fixture reset proof, evidence manifest validation, and CI artifact summary are aligned and honest.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser route tour -> evidence manifest | Playwright proof generates metadata consumed by ExUnit and CI artifact checks. |
| server fixture reset -> browser proof | Server reset state is combined with browser-owned IndexedDB/outbox cleanup in route tours. |
| CI artifact summary -> maintainer trust | Workflow summaries describe what route-tour artifacts prove and do not prove. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-152-09 | Repudiation | `route_tour.spec.ts` screenshots | mitigate | Capture screenshots only after semantic assertions and encode that ordering in assertion messages and manifest limitations. |
| T-152-10 | Tampering | `evidence_manifest.ts` | mitigate | Validate schema, route ids, support labels, proof posture, known limitations, unavailable reasons, package owner, and required artifacts. |
| T-152-11 | Information Integrity | `Showcase.Reset` proof | mitigate | Keep browser-state reset false and prove server fixture determinism separately from browser IndexedDB/outbox cleanup. |
| T-152-12 | Spoofing | `.github/workflows/offline-sync-e2e-gate.yml` summary | mitigate | Summary must state semantic assertions are proof and screenshots are collateral, with native/device/provider/offline/commerce non-claims. |
| T-152-SC | Tampering | npm/pip/cargo installs | accept | No package-manager installs are planned; existing Playwright dependency is already present. |
</threat_model>

<verification>
- `mix test test/crosswake/guides/evidence_manifest_test.exs -x`
- `cd examples/phoenix_host && mix test --warnings-as-errors test/crosswake_example/showcase/reset_test.exs`
- `cd examples/phoenix_host && npx playwright test e2e/route_tour.spec.ts e2e/learnloop_route_tour.spec.ts`
</verification>

<success_criteria>
Plan 152-03 is complete when PROOF-01 and PROOF-02 are executable, route-tour evidence covers the v19 showcase lanes and capability pressure, manifest schema tests pass, CI summary/artifact checks are honest, and no new native control, provider, storage/sync productization, schema, or dashboard work is introduced.
</success_criteria>

<output>
Create `.planning/phases/152-capability-map-collateral-and-v20-handoff/152-03-SUMMARY.md` when done.
</output>
