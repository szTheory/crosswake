---
phase: 123-native-package-behavioral-proof
plan: 02
type: execute
wave: 2
depends_on: ["123-01"]
files_modified:
  - packages/crosswake-shell-core-ios/Tests/CrosswakeShellCoreTests/CrosswakeShellCoreTests.swift
  - packages/crosswake-shell-core-ios/Package.swift
  - packages/crosswake-shell-core-ios/Tests/CrosswakeShellCoreTests/BridgeConformanceTests.swift
  - packages/crosswake-shell-core-ios/Tests/CrosswakeShellCoreTests/ActivationConformanceTests.swift
autonomous: true
requirements: [NTEST-02]
must_haves:
  truths:
    - "swift test compiles and runs in packages/crosswake-shell-core-ios/ (the corrupted CrosswakeShellCoreTests.swift is replaced with a valid XCTestCase)"
    - "The Swift test target loads bridge_contract_vectors.json from the test bundle via Bundle.module"
    - "A data-driven XCTest iterates all bridge vectors and asserts each reply's status and denial reason string against the vector's expected values"
    - "All six behaviors are covered: activation success/failure, bridge version-mismatch denial, capability allowlist, active-route, pack-version, delegate/escape-hatch (present->ok, absent->deny)"
    - "No test hardcodes a bridge protocol version literal — every version is read from the loaded vectors file"
  artifacts:
    - path: "packages/crosswake-shell-core-ios/Package.swift"
      provides: "testTarget resources declaration so Bundle.module exposes the vectors file"
      contains: ".copy(\"Resources/\")"
    - path: "packages/crosswake-shell-core-ios/Tests/CrosswakeShellCoreTests/BridgeConformanceTests.swift"
      provides: "Data-driven bridge evaluate() vector tests + delegate escape-hatch tests"
      contains: "Bundle.module"
    - path: "packages/crosswake-shell-core-ios/Tests/CrosswakeShellCoreTests/ActivationConformanceTests.swift"
      provides: "ActivationCoordinator resolve()/activate() success + denial tests, version-anchored to the vectors file"
      contains: "bridge_protocol_version"
    - path: "packages/crosswake-shell-core-ios/Tests/CrosswakeShellCoreTests/CrosswakeShellCoreTests.swift"
      provides: "Minimal valid XCTestCase replacing the corrupted file"
      contains: "XCTestCase"
  key_links:
    - from: "packages/crosswake-shell-core-ios/Package.swift"
      to: "packages/crosswake-shell-core-ios/Tests/CrosswakeShellCoreTests/Resources/bridge_contract_vectors.json"
      via: "testTarget resources: [.copy(\"Resources/\")] makes the file available via Bundle.module"
      pattern: "resources:\\s*\\["
    - from: "packages/crosswake-shell-core-ios/Tests/CrosswakeShellCoreTests/BridgeConformanceTests.swift"
      to: "packages/crosswake-shell-core-ios/Sources/CrosswakeShellCore/BridgeChannel.swift"
      via: "BridgeChannel.evaluate(_:completion:) driven per vector; reply.denial?.denial.reason asserted"
      pattern: "\\.evaluate\\("
---

<objective>
Replace the corrupted iOS test file so swift test compiles, declare the gen-emitted vectors file as a test-target resource, and write the XCTest behavioral suites covering all six native behaviors — bridge vectors driven data-driven from bridge_contract_vectors.json (NTEST-02), activation paths code-level but version-anchored to the same file (D-01).

Purpose: The published crosswake-shell-core-ios package self-certifies its contract behavior on macOS with no simulator, parametrized from the shared vectors (SC-2).
Output: Replaced corrupted file, Package.swift resources, two new XCTest files.

Depends on Plan 01: the iOS copy of bridge_contract_vectors.json is emitted by the gen task into Tests/CrosswakeShellCoreTests/Resources/ (do not author it here).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/123-native-package-behavioral-proof/123-CONTEXT.md
@.planning/phases/123-native-package-behavioral-proof/123-RESEARCH.md
@.planning/phases/123-native-package-behavioral-proof/123-PATTERNS.md

# iOS seams to drive (READ before writing tests — signatures, denial-reason strings, reply paths)
@packages/crosswake-shell-core-ios/Sources/CrosswakeShellCore/BridgeChannel.swift
@packages/crosswake-shell-core-ios/Sources/CrosswakeShellCore/ActivationCoordinator.swift
@packages/crosswake-shell-core-ios/Sources/CrosswakeShellCore/PackStore.swift
@packages/crosswake-shell-core-ios/Sources/CrosswakeShellCore/CrosswakeDelegates.swift
@packages/crosswake-shell-core-ios/Package.swift

# Full working XCTest examples (§3) and seam inventory (§6) — superseded only on §9 (native constants, rejected D-10) and §10 (iOS blocking, advisory per D-07)
@.planning/research/NATIVE-TESTING.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace the corrupted iOS test file and declare test-target resources</name>
  <files>packages/crosswake-shell-core-ios/Tests/CrosswakeShellCoreTests/CrosswakeShellCoreTests.swift, packages/crosswake-shell-core-ios/Package.swift</files>
  <read_first>
    - packages/crosswake-shell-core-ios/Package.swift (the testTarget block ~lines 23-25 currently has no resources: declaration)
    - .planning/phases/123-native-package-behavioral-proof/123-PATTERNS.md §"CrosswakeShellCoreTests.swift (REPLACE)" and §"Package.swift (EDIT)" (the exact replacement body + resources rule)
    - .planning/phases/123-native-package-behavioral-proof/123-RESEARCH.md §"Common Pitfalls" Pitfall 3 (corrupted file blocks compile) and §"Swift Package.swift Resource Declaration" (.copy vs .process)
  </read_first>
  <action>
This MUST be the first iOS task (Pitfall 3): the existing CrosswakeShellCoreTests.swift contains literal backslash-n escape bytes on a single line and will not compile, so swift test fails before any test runs.

Overwrite packages/crosswake-shell-core-ios/Tests/CrosswakeShellCoreTests/CrosswakeShellCoreTests.swift with a minimal valid XCTestCase subclass: import XCTest, a testable import of CrosswakeShellCore, and an empty final class CrosswakeShellCoreTests: XCTestCase {} with a one-line comment pointing to where the real behavioral tests live (BridgeConformanceTests.swift / ActivationConformanceTests.swift).

Edit Package.swift's testTarget to add resources: [.copy("Resources/")] per D-06. Use .copy (not .process) — correct for JSON, simpler, no transformation (RESEARCH §Swift resource declaration). The Resources/ directory and its bridge_contract_vectors.json are created by Plan 01's gen task; this task only declares the rule.

Run swift test to confirm the package compiles and the empty suite runs green before writing real test content.
  </action>
  <verify>
    <automated>cd packages/crosswake-shell-core-ios && swift test 2>&1 | tail -20</automated>
  </verify>
  <done>swift test compiles and runs (no compile error from the previously-corrupted file); Package.swift declares the testTarget resources via .copy("Resources/").</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Write the data-driven bridge conformance XCTest suite</name>
  <files>packages/crosswake-shell-core-ios/Tests/CrosswakeShellCoreTests/BridgeConformanceTests.swift</files>
  <read_first>
    - packages/crosswake-shell-core-ios/Sources/CrosswakeShellCore/BridgeChannel.swift (evaluate(_:completion:) ~line 180 is synchronous for test paths; init ~line 148; denial-reason strings at lines 184/189/193/203/218/228; reply path BridgeReplyEnvelope.denial -> BridgeDenialEnvelope.denial -> RouteDenialPayload.reason)
    - packages/crosswake-shell-core-ios/Sources/CrosswakeShellCore/CrosswakeDelegates.swift (AppInfoDelegate and the other delegate protocols for the escape-hatch tests)
    - .planning/phases/123-native-package-behavioral-proof/123-RESEARCH.md §"Code Examples" Swift section (the BridgeVectorsFile/BridgeVector Codable models, setUp Bundle.module load, synchronous evaluate pattern) and §"Common Pitfalls" Pitfall 1 (apply session_override) + Pitfall 6 (ok vector needs appInfoDelegate)
    - .planning/phases/123-native-package-behavioral-proof/123-PATTERNS.md §"BridgeConformanceTests.swift (NEW)"
    - .planning/research/NATIVE-TESTING.md §3 (the fuller XCTest example + makeSession/makeRequest/makeConfig helper shapes)
  </read_first>
  <behavior>
    - setUp loads bridge_contract_vectors.json via Bundle.module and decodes BridgeVectorsFile (snake_case JSON -> camelCase via CodingKeys)
    - One data-driven test iterates vectorsFile.vectors; for each it builds a session (applying session_override), a request (applying request_override), and a config, constructs BridgeChannel, calls evaluate synchronously, and asserts reply.status == vector.expectedOutcome and (when present) reply.denial?.denial.reason == vector.expectedDenialReason — with the vector id in the failure message
    - The ok vector (vec-003) configures an appInfoDelegate so it does not deny undeclared_capability (Pitfall 6)
    - Two symmetric delegate escape-hatch tests for a delegate-backed command: delegate present -> ok; delegate absent -> deny (undeclared_capability/unavailable_capability)
    - The bridge protocol version used to build sessions/requests is read from vectorsFile.bridgeProtocolVersion, never a literal (D-10)
  </behavior>
  <action>
Create BridgeConformanceTests.swift per RESEARCH §Code Examples (Swift) and NATIVE-TESTING §3, driving the real seams in BridgeChannel.swift.

Define Codable models BridgeVectorsFile (bridgeProtocolVersion + vectors, with CodingKeys mapping bridge_protocol_version) and BridgeVector (id, description, requestOverride, sessionOverride, expectedOutcome, expectedDenialReason with CodingKeys for the snake_case keys). For the override dictionaries use a small AnyCodable-style decoder or decode them as [String: String]/nested dictionaries sufficient to carry the fields the session/request builders need (version, command, capability, route_id, active_route_id, origin, capabilities, route_required_packs, installed_packs).

In setUp, load via Bundle.module.url(forResource: "bridge_contract_vectors", withExtension: "json") and JSONDecoder().decode(BridgeVectorsFile.self, ...). Bundle.module is generated because Package.swift now declares resources (Task 1). macOS-only is acceptable (advisory lane, no Linux guard).

Write makeSession(bridgeProtocolVersion:overrides:), makeRequest(version:overrides:), and makeConfig(for:) helpers that START from a permissive baseline (everything passes) and APPLY each vector's overrides before constructing BridgeChannel (Pitfall 1 — actually apply session_override/request_override; a test that ignores them is not data-driven). The ok-path config must include an appInfoDelegate stub (Pitfall 6).

Write the data-driven test iterating vectorsFile.vectors. Use the synchronous evaluate pattern: var reply: BridgeReplyEnvelope?; channel.evaluate(request) { reply = $0 } — no XCTestExpectation (RESEARCH confirms evaluate's completion is synchronous for test paths). Assert reply?.status == vector.expectedOutcome and, when expectedDenialReason is non-nil, reply?.denial?.denial.reason == that string. Include vector.id in every assertion message.

Add the two delegate escape-hatch tests (present->ok, absent->deny) using anonymous delegate stubs from CrosswakeDelegates.swift.

Assert on the reply's reason string/JSON only — never on an internal guard firing (D-14). Never declare a BridgeChannel.protocolVersion constant (D-10).
  </action>
  <verify>
    <automated>cd packages/crosswake-shell-core-ios && swift test --filter BridgeConformanceTests 2>&1 | tail -20</automated>
  </verify>
  <done>BridgeConformanceTests loads the vectors via Bundle.module, drives BridgeChannel.evaluate for every vector, and passes with status + denial-reason assertions per vector plus the two delegate escape-hatch tests; no version literal is hardcoded.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Write the activation conformance XCTest suite (success + denial paths)</name>
  <files>packages/crosswake-shell-core-ios/Tests/CrosswakeShellCoreTests/ActivationConformanceTests.swift</files>
  <read_first>
    - packages/crosswake-shell-core-ios/Sources/CrosswakeShellCore/ActivationCoordinator.swift (resolve(request:manifest:) and activate(_:) public seams; RouteDenialReason enum ~lines 17-25; the injected manifestLoader/requestLoader/packStore/config init)
    - packages/crosswake-shell-core-ios/Sources/CrosswakeShellCore/PackStore.swift (PackStore(requiredVersions:inventory:) no-Bundle constructor; blockingStatus(for:))
    - .planning/research/NATIVE-TESTING.md §6.2 (ActivationCoordinator injected-closure construction) and §3 (activation test examples)
    - .planning/phases/123-native-package-behavioral-proof/123-PATTERNS.md §"ActivationConformanceTests.swift (NEW)" (setUp loads bridge_protocol_version for version-anchoring)
  </read_first>
  <behavior>
    - setUp loads bridge_protocol_version from the vectors file (JSONSerialization or the Codable model) for version-anchoring per D-01
    - Activation success: ActivationCoordinator with a permissive injected manifest/request/packStore resolves/activates to a LiveView presentation (not a Denied/RequiredPack)
    - Activation failure: an inactive-route or required-pack scenario resolves to a Denied/RequiredPack with the expected RouteDenialReason
    - No hardcoded version literal (version comes from the loaded file)
  </behavior>
  <action>
Create ActivationConformanceTests.swift covering the activation half of the six behaviors (activation success/failure) at the code level, version-anchored to the vectors file per D-01 (load bridge_protocol_version in setUp; use it when constructing manifests/requests so a version bump in the regenerated copy flows through).

Construct ActivationCoordinator with injected manifestLoader/requestLoader/packStore/config closures and PackStore(requiredVersions:inventory:) per NATIVE-TESTING §6.2 — no Bundle, no simulator. Write at least: one success test (LiveView presentation), one inactive-route denial test, and one required-pack denial test, asserting on the resolved presentation / RouteDenialReason. Use anonymous delegate/loader stubs (no mocking library).

Assert on the resolved outcome value, not an internal guard (D-14). Do not add a native version constant (D-10).
  </action>
  <verify>
    <automated>cd packages/crosswake-shell-core-ios && swift test 2>&1 | tail -25</automated>
  </verify>
  <done>ActivationConformanceTests passes; activation success, inactive-route denial, and required-pack denial are covered; the full swift test suite (bridge + activation + minimal) is green on macOS; version is read from the vectors file.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test bundle → vectors resource | Tests read the gen-emitted DO-NOT-EDIT copy via Bundle.module; no external file load |
| test code → CrosswakeShellCore seams | Public test seams only; no production code change |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-123-04 | Tampering | A native version constant reintroduced in Swift would create a new drift surface | mitigate | D-10 enforced: tests read bridgeProtocolVersion from the loaded vectors file; no BridgeChannel.protocolVersion constant added |
| T-123-05 | Spoofing of proof | Tests could assert on an internal guard firing rather than the observable reply ("test the fake") | mitigate | D-14 enforced: assertions are on reply.status and reply.denial?.denial.reason strings only |
| T-123-06 | DoS / environment | swift test claiming simulator/device support it does not have | accept | macOS-only, no simulator; the lane is advisory (D-07); tests never launch a simulator |
| T-123-SC | Tampering | npm/pip/cargo installs | accept | No package-manager installs; SwiftPM resolves only the existing package graph — legitimacy gate N/A |
</threat_model>

<verification>
- `cd packages/crosswake-shell-core-ios && swift test` (full suite green on macOS; no compile error)
- Bridge suite asserts status + denial reason per vector, loaded via Bundle.module
- Activation suite covers success + inactive-route + required-pack denial
- `grep` confirms no hardcoded bridge version literal in the Swift test files (version comes from the vectors file)
</verification>

<success_criteria>
- swift test compiles and passes in packages/crosswake-shell-core-ios/ with no simulator (NTEST-02 / SC-2).
- All six behaviors covered; bridge behaviors data-driven from the gen-emitted vectors copy.
- No version literal hardcoded in any iOS test file (D-10).
- Corrupted CrosswakeShellCoreTests.swift replaced with a valid XCTestCase (D-12).
</success_criteria>

<output>
Create `.planning/phases/123-native-package-behavioral-proof/123-02-SUMMARY.md` when done. Record: the new Swift test class names (CrosswakeShellCoreTests, BridgeConformanceTests, ActivationConformanceTests), the Package.swift resources rule used (.copy), and which behaviors each file covers.
</output>
