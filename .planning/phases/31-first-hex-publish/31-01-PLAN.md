---
phase: "31"
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: false
requirements: [REL-07, REL-08, PRF-02]
user_setup:
  - service: github
    why: "release-please workflow needs permissions to open PRs"
    dashboard_config:
      - task: "Set workflow permissions to Read and write permissions"
        location: "Repo Settings -> Actions -> General"
      - task: "Allow GitHub Actions to create and approve pull requests"
        location: "Repo Settings -> Actions -> General"
  - service: hex.pm
    why: "Publish hex package"
    env_vars:
      - name: HEX_API_KEY
        source: "https://hex.pm/dashboard/keys (scope: package:crosswake)"
    dashboard_config:
      - task: "Add HEX_API_KEY as a repository secret"
        location: "Repo Settings -> Secrets and variables -> Actions"

must_haves:
  truths:
    - "default_workflow_permissions is write"
    - "HEX_API_KEY is present in repo secrets"
    - "Release PR is created by release-please"
    - "Maintainer can merge the PR"
    - "hex.pm and hexdocs.pm host version 0.1.0"
  artifacts: []
  key_links:
    - from: "GitHub Actions"
      to: "hex.pm"
      via: "HEX_API_KEY"
      pattern: "HEX_API_KEY"
---

<objective>
Complete the irreversible human-gated handoffs (GitHub permissions, `HEX_API_KEY`, Release PR merge) so `crosswake 0.1.0` is live on hex.pm.

Purpose: This marks the official release of `crosswake 0.1.0`. All prior steps were automated preparation; this phase requires human validation and execution to push the package to hex.pm.
Output: Published hex.pm package, Github Release PR merged.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Pre-publish Permissions and Secrets Verification</name>
  <what-built>GitHub Actions workflow permissions and hex.pm API keys</what-built>
  <how-to-verify>
    1. Confirm `gh api repos/szTheory/crosswake/actions/permissions/workflow` returns `default_workflow_permissions: "write"` and `can_approve_pull_request_reviews: true`. (If missing, configure in Repo Settings -> Actions -> General).
    2. Confirm `gh secret list --repo szTheory/crosswake` shows `HEX_API_KEY`. The key must be generated at https://hex.pm/dashboard/keys with `package:crosswake` scope (NOT `api:write`).
    3. Confirm `grep -n "HEX_API_KEY" .github/workflows/release-please.yml` shows no occurrence in any `with:` block.
    4. Confirm you are ready to push the accumulated commits to `main` (which will trigger `release-please`).
  </how-to-verify>
  <resume-signal>Type "approved" when configurations are confirmed and ready for push.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Push to main</name>
  <files>None</files>
  <action>Run `git push origin main` to push the accumulated commits and trigger the `release-please` workflow. This action assumes all commits from phases 26-30 have been made locally.</action>
  <verify>
    <automated>git status</automated>
  </verify>
  <done>Commits are pushed to origin main</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Release PR Merge and Publish Verification</name>
  <what-built>Release PR and hex.pm package</what-built>
  <how-to-verify>
    1. Verify `release-please` has opened a PR titled `chore(main): release 0.1.0` (within ~2 minutes of the push).
    2. Maintainer must manually review and merge the Release PR.
    3. Wait for the `publish-hex` GitHub Actions workflow to run and turn green.
    4. Run `curl -fsS https://hex.pm/api/packages/crosswake/releases/0.1.0` and verify it returns a JSON body with `"version": "0.1.0"`.
    5. Visit `https://hex.pm/packages/crosswake/0.1.0` and verify it renders with working GitHub, Docs, and Changelog links.
    6. Visit `https://hexdocs.pm/crosswake/0.1.0/` and verify it renders the README as the landing page.
  </how-to-verify>
  <resume-signal>Type "approved" when the package is successfully verified on hex.pm and hexdocs.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Local -> GitHub -> Hex.pm | Maintainer triggers automated workflow that pushes package artifacts to external registry. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-31-01 | Elevation of Privilege | HEX_API_KEY | mitigate | Key scoped only to `package:crosswake` instead of full `api:write` |
| T-31-02 | Information Disclosure | release-please.yml | mitigate | Verify `HEX_API_KEY` is only used in `env:` block, not passed to external actions via `with:` |
</threat_model>

<verification>
- GitHub permissions verified.
- HEX_API_KEY verified and scoped correctly.
- Release PR merged successfully.
- Package verified live on hex.pm.
- Docs verified live on hexdocs.pm.
</verification>

<success_criteria>
`crosswake 0.1.0` is published to hex.pm, and all required metadata and documentation are correctly rendered.
</success_criteria>

<output>
After completion, create `.planning/phases/31-first-hex-publish/31-01-SUMMARY.md`
</output>
