---
phase: 163.1-close-gap-v21-physical-adopter-composition
plan: 01
type: tracer
wave: 2
depends_on: ["163.1-02"]
files_modified:
  - examples/phoenix_host/lib/crosswake_example/adopter_handoff.ex
  - examples/phoenix_host/lib/crosswake_example/physical_iphone_proof_host.ex
  - lib/crosswake/proof_lane/physical_iphone_preflight.ex
  - test/crosswake/proof_lane/physical_iphone_preflight_test.exs
  - examples/phoenix_host/test/crosswake_example/physical_iphone_proof_host_test.exs
  - examples/phoenix_host/native/ios/CrosswakeProofLane/ProofLaneDriver.swift
  - examples/phoenix_host/native/ios/CrosswakeProofLaneTests/ProofLaneContractTests.swift
autonomous: true
requirements: []
must_haves:
  truths:
    - "D-01/D-02: A host-private sanitized handoff traverses RouteInventory and NavigationTopology in the same current-run physical preflight."
    - "D-03/D-04: Missing, stale, contradictory, or fixture input returns a stable blocked result without echoing supplied values."
    - "D-21: The existing physical command exposes the shared eligibility chain before any device or evidence callback."
  artifacts:
    - path: "examples/phoenix_host/lib/crosswake_example/adopter_handoff.ex"
      provides: "Host-private JSON handoff loading and existing-validator composition"
      exports: ["load/1", "validate/1"]
    - path: "lib/crosswake/proof_lane/physical_iphone_preflight.ex"
      provides: "Current-run inventory and topology eligibility gate"
    - path: "examples/phoenix_host/native/ios/CrosswakeProofLane/ProofLaneDriver.swift"
      provides: "Exact current-run-bound topology envelope decoder and closed native configuration result"
  key_links:
    - from: "examples/phoenix_host/lib/crosswake_example/adopter_handoff.ex"
      to: "Crosswake.Adoption.RouteInventory and Crosswake.Adoption.NavigationTopology"
      via: "validate/1"
      pattern: "RouteInventory.*NavigationTopology"
    - from: "examples/phoenix_host/lib/crosswake_example/physical_iphone_proof_host.ex"
      to: "lib/crosswake/proof_lane/physical_iphone_preflight.ex"
      via: "inventory/topology preflight options"
      pattern: "adopter_handoff|topology"
    - from: "examples/phoenix_host/lib/crosswake_example/physical_iphone_proof_host.ex"
      to: "examples/phoenix_host/native/ios/CrosswakeProofLane/ProofLaneDriver.swift"
      via: "private xctestrun environment carrying a current-run-bound closed topology envelope"
      pattern: "CROSSWAKE_REFERENCE_HOST_NAVIGATION_TOPOLOGY|CROSSWAKE_REFERENCE_HOST_PHYSICAL_PROOF_NONCE"
  prohibitions:
    - "FLAGGED-UNVERIFIED: Fixture rows cannot become eligible-host, physical-promotion, or support evidence."
    - "FLAGGED-UNVERIFIED: Raw handoff bytes and rejected values cannot enter git, output, logs, or retained evidence."
---

<objective>
Trace one production-quality host-private handoff from source loading through closed route validation, ready-topology compilation, and the physical preflight boundary without claiming adopter/device promotion.

Purpose: Prove the phase's shared eligibility architecture before expanding the physical host.
Output: A fail-closed handoff loader and a current-run preflight slice that distinguishes fixture from adopter-bound authority.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/163.1-close-gap-v21-physical-adopter-composition/163.1-CONTEXT.md
@.planning/phases/163.1-close-gap-v21-physical-adopter-composition/163.1-RESEARCH.md
@.planning/v21.0-MILESTONE-AUDIT.md
@.planning/todos/TODO-002-first-b2c-adopter-route-inputs.md
@.planning/phases/163-first-b2c-adopter-reference-host-integration/163-01-SUMMARY.md

Planning assumptions: the spec-less probe visibly skipped because Phase 163.1 has no requirement IDs. Frontmatter remains `requirements: []`; Plan 02 creates the audit-derived ledger per D-22. No external API is integrated; COVERAGE.md records the deterministic detector false-positive. TODO-002 remains open, so this tracer proves the chain with test-only sanitized input and proves production remains blocked.
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Trace the current sanitized handoff through the physical eligibility gate</name>
  <reversibility rating="costly">D-02 makes this the shared eligibility seam consumed by host composition, physical execution, and promotion.</reversibility>
  <read_first>AGENTS.md, .planning/todos/TODO-002-first-b2c-adopter-route-inputs.md, .planning/phases/163.1-close-gap-v21-physical-adopter-composition/163.1-PATTERNS.md, lib/crosswake/adoption/route_inventory.ex, lib/crosswake/adoption/navigation_topology.ex, lib/crosswake/proof_lane/physical_iphone_preflight.ex, examples/phoenix_host/lib/crosswake_example/physical_iphone_proof_host.ex, examples/phoenix_host/native/ios/CrosswakeProofLane/ProofLaneDriver.swift, packages/crosswake-shell-core-ios/Sources/CrosswakeShellCore/NavigationTopology.swift, test/crosswake/proof_lane/physical_iphone_preflight_test.exs, examples/phoenix_host/test/crosswake_example/physical_iphone_proof_host_test.exs, examples/phoenix_host/native/ios/CrosswakeProofLaneTests/ProofLaneContractTests.swift</read_first>
  <files>examples/phoenix_host/lib/crosswake_example/adopter_handoff.ex, examples/phoenix_host/lib/crosswake_example/physical_iphone_proof_host.ex, lib/crosswake/proof_lane/physical_iphone_preflight.ex, test/crosswake/proof_lane/physical_iphone_preflight_test.exs, examples/phoenix_host/test/crosswake_example/physical_iphone_proof_host_test.exs, examples/phoenix_host/native/ios/CrosswakeProofLane/ProofLaneDriver.swift, examples/phoenix_host/native/ios/CrosswakeProofLaneTests/ProofLaneContractTests.swift</files>
  <behavior>
    - Test 1: a test-owned sanitized handoff with exact top-level keys validates through RouteInventory and compiles a `ready` NavigationTopology before preflight reports ready.
    - Test 2: absent, malformed, duplicated, contradictory, unknown-blocking, or fixture-class input returns only `PI-PREFLIGHT-ADOPTER-HANDOFF` and no rejected value.
    - Test 3: the device report and evidence destination callbacks are never invoked when the handoff/topology check blocks.
    - Test 4: reusing a previously validated row without current-run source loading cannot satisfy preflight.
    - Test 5: the Elixir host serializes only `schema_version`, `run_binding`, and `topology`; the Swift consumer accepts it only when schema version is 1, run binding equals the separately injected current invocation nonce, topology is `ready`, and topology/manifest schema versions match.
    - Test 6: absent, malformed, extra-field, stale-run, mismatched-schema, unknown-blocking, or empty topology is denied before `NavigationCoordinator` or any leaf factory is created, with no payload or binding echoed.
  </behavior>
  <action>Create host-private `CrosswakeExample.AdopterHandoff` with `load/1` and `validate/1`; read JSON only from a caller-supplied path, accept exact top-level `schema_version`, `rows`, and `navigation_entries`, pass rows to `RouteInventory.validate_inventory/1`, require `RouteInventory.promotion_status/1` eligibility, compile `%{rows: rows, entries: navigation_entries}` through `NavigationTopology.compile/1`, and require `NavigationTopology.promotion_status/1` ready. Follow the `Closed handoff and compiled topology`, `Physical preflight and one-command task`, and `Host physical-run hygiene` analogs in `163.1-PATTERNS.md`. Return a closed source classification and topology, never raw bytes or field values. Extend `PhysicalIphonePreflight` with an ordered `adopter_handoff` callback checked before config, signing, device, report, destination, or promotion work. Change `PhysicalIphoneProofHost.preflight_options/0` to load the host-private path from `CROSSWAKE_FIRST_ADOPTER_HANDOFF_PATH`; keep its existing synthetic inventory available only through an explicitly fixture-class test seam. At the existing `inject_ui_test_environment/3` transport seam, encode exactly `%{"schema_version" =&gt; 1, "run_binding" =&gt; run.nonce, "topology" =&gt; validated_topology}` into `CROSSWAKE_REFERENCE_HOST_NAVIGATION_TOPOLOGY` and inject it beside the separately supplied `CROSSWAKE_REFERENCE_HOST_PHYSICAL_PROOF_NONCE`; never write it outside the invocation-private `.xctestrun` environment and existing cleanup root. In `ProofLaneDriver.swift`, add the sole host-private decoder/configuration owner: require the envelope's exact three keys, schema 1, equality with the separately injected nonce, `NavigationTopology.status == .ready`, nonempty entries, and matching topology/manifest schema versions, returning only a closed denial. Expose a validated configuration value for Plan 05; do not create a coordinator or leaf on any decode, staleness, binding, schema, or topology denial. Add Elixir producer tests plus Swift consumer and negative tests. Use stable `PI-PREFLIGHT-ADOPTER-HANDOFF` remediation with no echo per D-01 through D-04 and D-21. Do not add a Swift route declaration, a second route authority, or persisted/diagnostic handoff material.</action>
  <verify>
    <automated>set -euo pipefail; blocked_json=$(mktemp); trap 'rm -f -- "$blocked_json"' EXIT; mix test test/crosswake/proof_lane/physical_iphone_preflight_test.exs --max-failures 1; (cd examples/phoenix_host &amp;&amp; MIX_ENV=test mix test test/crosswake_example/physical_iphone_proof_host_test.exs --max-failures 1); xcodebuild test -project examples/phoenix_host/native/ios/CrosswakeProofLane.xcodeproj -scheme CrosswakeProofLane -destination 'platform=iOS Simulator,name=iPhone 17' -only-testing:CrosswakeProofLaneTests/ProofLaneContractTests; set +e; mix crosswake.proof_lane.physical_iphone --readiness --json &gt;"$blocked_json" 2&gt;/dev/null; readiness_status=$?; set -e; test "$readiness_status" -eq 2; jq -e '.outcome == "blocked" and any(.checks[]; .id == "PI-PREFLIGHT-ADOPTER-HANDOFF" and .state == "blocked")' "$blocked_json" &gt;/dev/null</automated>
  </verify>
  <acceptance_criteria>`AdopterHandoff.validate/1` delegates route and topology truth to the existing modules; a valid test handoff reaches a ready preflight contract and crosses the private `.xctestrun` seam as the exact nonce-bound envelope; Swift rejects malformed, stale, mismatched, or unknown-blocking input before coordinator/leaf creation; production without TODO-002 is blocked before report/evidence callbacks; emitted failure data contains only the stable rule and closed outcome.</acceptance_criteria>
  <done>The thinnest real handoff-to-preflight path works end to end, while fixture and missing-input paths remain non-promotable.</done>
</task>

</tasks>

## Artifacts this phase produces

- New path `examples/phoenix_host/lib/crosswake_example/adopter_handoff.ex`.
- New functions `CrosswakeExample.AdopterHandoff.load/1` and `validate/1`.
- New physical-preflight callback key `:adopter_handoff` and stable rule `PI-PREFLIGHT-ADOPTER-HANDOFF`.
- New host-private environment input `CROSSWAKE_FIRST_ADOPTER_HANDOFF_PATH`.
- New ephemeral xctestrun-only `CROSSWAKE_REFERENCE_HOST_NAVIGATION_TOPOLOGY` envelope, bound to the existing invocation nonce and consumed only by the physical iOS process.
- No schema, migration, external API, or retained handoff artifact.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| host-private file → Elixir validators | Untrusted sanitized handoff enters closed route/topology authority. |
| preflight → device/evidence callbacks | Eligibility decides whether side effects may begin. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-163.1-01 | Tampering | AdopterHandoff/preflight | high | mitigate | Exact keys, RouteInventory validation, ready topology, current-run recheck, stable block before callbacks. |
| T-163.1-02 | Elevation of Privilege | fixture/adopter mode | high | mitigate | Closed source classification prevents fixture output from entering promotion. |
| T-163.1-03 | Information Disclosure | handoff errors | high | mitigate | Never echo bytes or values; emit stable rule only. |
| T-163.1-SC | Tampering | package installs | high | mitigate | No package-manager install occurs in this plan. |
</threat_model>

<verification>
The focused core and Phoenix-host tests pass, and a normal readiness invocation without TODO-002 remains blocked without side effects.
</verification>

<success_criteria>
The shared eligibility chain is executable and fail-closed; it proves architecture, not adopter or device promotion.
</success_criteria>

<output>
Create `.planning/phases/163.1-close-gap-v21-physical-adopter-composition/163.1-01-SUMMARY.md` when done.
</output>
