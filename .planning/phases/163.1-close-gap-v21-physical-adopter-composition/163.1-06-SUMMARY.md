---
phase: 163.1-close-gap-v21-physical-adopter-composition
plan: 06
subsystem: phoenix-replay-authority
tags: [elixir, phoenix, ecto, scoped-replay, physical-iphone, test-gating]
requires:
  - phase: 160-scoped-replay-and-auth-safety
    provides: request-time replay admission and scope-qualified ReviewEvent persistence
  - phase: 163-first-b2c-adopter-reference-host-integration
    provides: physical iPhone run provenance and host proof contract
provides:
  - Database-authoritative scoped replay idempotency without preflight acceptance lookup
  - Nonce-bound, single-use backend verification for device-caused physical cases
  - Compile-gated fixture setup and verification routes with closed output
affects: [physical-iphone-proof, device-caused-recovery, scoped-replay]
tech-stack:
  added: []
  patterns: [insert-then-resolve-idempotency, nonce-bound-case-verification, normal-replay-only-observation]
key-files:
  created: []
  modified:
    - examples/phoenix_host/lib/crosswake_example/local_first/study.ex
    - examples/phoenix_host/lib/crosswake_example/local_first/physical_iphone_authority.ex
    - examples/phoenix_host/lib/crosswake_example/local_first/sync_controller.ex
    - examples/phoenix_host/lib/crosswake_example/router.ex
key-decisions:
  - "The database, not a read-before-insert lookup, decides scoped replay idempotency."
  - "Physical fixture controls prepare or verify closed cases; only normal replay can record the correlated device outcome."
patterns-established:
  - "Failed unique inserts resolve from persisted rows after the transaction, preserving global legacy tombstones."
  - "Physical case verification consumes a nonce/case binding so a replayed report cannot pass twice."
requirements-completed: []
coverage:
  - id: D1
    description: Scoped replay uses an authoritative insert transaction and rejects denied authority before persistence.
    verification:
      - kind: integration
        ref: cd examples/phoenix_host && MIX_ENV=test mix test test/crosswake_example/local_first/study_test.exs test/crosswake_example/local_first/replay_auth_test.exs --max-failures 1
        status: pass
    human_judgment: false
  - id: D2
    description: Test-gated physical-case controls require a prepared nonce binding and verify one correlated normal replay outcome exactly once.
    verification:
      - kind: integration
        ref: cd examples/phoenix_host && MIX_ENV=test mix test test/crosswake_example/local_first/physical_iphone_authority_test.exs test/crosswake_example/router_test.exs --max-failures 1
        status: pass
    human_judgment: false
duration: 22min
completed: 2026-08-28
status: complete
---

# Phase 163.1 Plan 06: Device-Caused Replay Authority Summary

**Phoenix replay now uses constraint-backed scoped idempotency, while physical-case reports can pass only after a nonce-bound normal replay records the matching closed outcome.**

## Performance

- **Tasks:** 2/2
- **Files modified:** 8
- **Verification:** 34 focused Phoenix tests passed across replay, authority, router, and physical-host coverage.

## Accomplishments

- Removed the race-prone read-before-insert idempotency decision from `Study.apply_one/3`; accepted persistence remains in the Ecto transaction and constraint failures resolve from the authoritative persisted row.
- Added fail-closed authority denial before persistence, preserving rejected and scope-conflict work without acknowledgement.
- Added nonce-bound `prepare_case/2` and single-use `verify_case/2`, with normal `SyncController` replay as the sole result recorder.
- Added compile-gated `/_e2e/physical-case` controls returning only closed outcomes, and removed backend report code that manufactured negative cases.

## Task Commits

1. **Task 1: Make scoped uniqueness and accepted domain effect one authoritative transaction** — `ef73cc87` (TDD RED), `f8364cea` (GREEN)
2. **Task 2: Expose host-private case setup and device-result verification** — `64f27e1b` (TDD RED), `8c55e69e` (GREEN), `b4c9193e` (regression)

## Files Created/Modified

- `examples/phoenix_host/lib/crosswake_example/local_first/study.ex` — constraint-backed replay insertion and post-failure outcome resolution.
- `examples/phoenix_host/lib/crosswake_example/local_first/physical_iphone_authority.ex` — closed case setup, normal-replay correlation, and single-use verification.
- `examples/phoenix_host/lib/crosswake_example/local_first/sync_controller.ex` — records physical case outcomes only after replay admission.
- `examples/phoenix_host/lib/crosswake_example/router.ex` — test/e2e-only physical fixture-control routes.
- `examples/phoenix_host/test/crosswake_example/local_first/study_test.exs` — authority-denial regression coverage.
- `examples/phoenix_host/test/crosswake_example/local_first/physical_iphone_authority_test.exs` — nonce correlation and replayed-verification coverage.
- `examples/phoenix_host/test/crosswake_example/router_test.exs` — compile-gated control-route coverage.
- `examples/phoenix_host/test/crosswake_example/physical_iphone_proof_host_test.exs` — uncorrelated rows remain non-passing.

## Decisions Made

- Legacy global mutation-ID tombstones remain conflicts for every current scope.
- A fixture control cannot mark a case passed; verification consumes a result recorded from normal replay admission.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 2 - Missing critical functionality] Recorded case outcomes only from normal replay**
- **Found during:** Task 2
- **Issue:** The new verifier needed an authoritative request-time observation path; setup or report endpoints could otherwise manufacture a pass.
- **Fix:** Added closed outcome recording inside `SyncController` after normal replay admission and result handling.
- **Files modified:** `examples/phoenix_host/lib/crosswake_example/local_first/sync_controller.ex`
- **Verification:** Focused replay and physical-authority tests passed.
- **Committed in:** `8c55e69e`

**2. [Rule 1 - Bug] Rejected stale backend report expectations**
- **Found during:** Task 2 regression verification
- **Issue:** A physical-host test still expected a report to pass from an uncorrelated persisted row after backend manufacturing was removed.
- **Fix:** Updated the regression to require the closed unavailable result.
- **Files modified:** `examples/phoenix_host/test/crosswake_example/physical_iphone_proof_host_test.exs`
- **Verification:** Combined focused suite passed (34 tests).
- **Committed in:** `b4c9193e`

**Total deviations:** 2 auto-fixed (Rule 1: 1, Rule 2: 1). **Impact:** Required to preserve the plan’s device-caused and fail-closed authority boundary; no new platform, storage, or sync scope.

## Issues Encountered

- The initial wider concurrency probe was already green, so the RED gate instead pinned the previously missing authority-denial behavior before persistence.

## User Setup Required

None.

## Next Phase Readiness

- Plan 07 can drive the six physical cases through the new test-only setup and verification seam.
- TODO-002 validation and a source-bound signed-device run remain required before physical or support promotion.

## Self-Check: PASSED

- Confirmed all eight modified files exist and task commits `ef73cc87`, `f8364cea`, `64f27e1b`, `8c55e69e`, and `b4c9193e` are present.
- Confirmed the focused five-file Phoenix verification command passed with 34 tests.
