---
phase: 163.1-close-gap-v21-physical-adopter-composition
plan: 08
type: execute
wave: 6
depends_on: ["163.1-02", "163.1-03", "163.1-07"]
files_modified:
  - lib/crosswake/proof_lane/physical_iphone_contract.ex
  - lib/mix/tasks/crosswake.proof_lane.physical_iphone.ex
  - lib/crosswake/proof_lane/evidence.ex
  - test/crosswake/proof_lane/physical_iphone_evidence_transaction_test.exs
  - .planning/phases/163.1-close-gap-v21-physical-adopter-composition/163.1-VALIDATION.md
autonomous: true
requirements: []
user_setup:
  - service: first_b2c_adopter_private_handoff
    why: "Required source authority for adopter binding and promotion"
    env_vars:
      - name: CROSSWAKE_FIRST_ADOPTER_HANDOFF_PATH
        source: "Host-private sanitized TODO-002 handoff outside the repository"
  - service: signed_physical_iphone
    why: "Required physical execution target"
    env_vars:
      - name: CROSSWAKE_PHYSICAL_IPHONE_UDID
        source: "Connected signed iPhone selected by the host maintainer"
      - name: CROSSWAKE_PHYSICAL_IPHONE_HOST_BASE_URL
        source: "Private Phoenix proof host for the run"
must_haves:
  truths:
    - "D-18/D-19: Exact ordered device/backend assertions join all-or-nothing and publish only the redacted source-bound run contract plus digest marker."
    - "D-21/D-23: One proof command performs fresh preflight, physical cases, independent checks, and redacted promotion."
    - "Completion truth changes only after TODO-002 validates and the signed physical run passes."
  artifacts:
    - path: "lib/crosswake/proof_lane/physical_iphone_contract.ex"
      provides: "Closed ordered composed-device assertion contract"
    - path: ".planning/phases/163.1-close-gap-v21-physical-adopter-composition/163.1-VALIDATION.md"
      provides: "Fresh aggregate-only closure evidence"
  key_links:
    - from: "mix crosswake.proof_lane.physical_iphone --run --promote --json"
      to: "Evidence.promote and Evidence.check/2"
      via: "exact joined report and approved canonical source bytes"
      pattern: "join_reports|Evidence"
  prohibitions:
    - "FLAGGED-UNVERIFIED: Run nonce, handoff, payload, scope/account, flags, endpoints, media, private digests, xcresult, screenshots, logs, and backend rows cannot be retained."
    - "FLAGGED-UNVERIFIED: Simulator/reference-fixture output cannot substitute for the source-bound physical transaction."
---
<objective>Seal the composed assertion/evidence contract, execute the sole source-bound physical transaction, and reconcile milestone truth only from fresh passing evidence.</objective>
<execution_context>@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md</execution_context>
<context>@.planning/phases/163.1-close-gap-v21-physical-adopter-composition/163.1-CONTEXT.md
@.planning/phases/163.1-close-gap-v21-physical-adopter-composition/163.1-VALIDATION.md
@.planning/v21.0-MILESTONE-AUDIT.md
@.planning/todos/TODO-002-first-b2c-adopter-route-inputs.md</context>
<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Version the exact device/backend evidence transaction</name>
  <reversibility rating="costly">D-18/D-19 change a closed retained schema and all exact decoders/fixtures must move together.</reversibility>
  <read_first>lib/crosswake/proof_lane/physical_iphone_contract.ex, lib/mix/tasks/crosswake.proof_lane.physical_iphone.ex, lib/crosswake/proof_lane/evidence.ex, test/crosswake/proof_lane/physical_iphone_evidence_transaction_test.exs, examples/phoenix_host/lib/crosswake_example/physical_iphone_proof_host.ex</read_first>
  <files>lib/crosswake/proof_lane/physical_iphone_contract.ex, lib/mix/tasks/crosswake.proof_lane.physical_iphone.ex, lib/crosswake/proof_lane/evidence.ex, test/crosswake/proof_lane/physical_iphone_evidence_transaction_test.exs</files>
  <behavior>- Exact schema version and ordered owner-disjoint assertion list accept only complete passed device/backend pairs.
- Missing, reordered, duplicated, wrong-owner, blocked, unavailable, stale-nonce, replayed-report, or mismatched-source input fails closed.
- Promotion uses the completion-digest-bound source snapshot and Evidence.check/2.
- Retained directory contains only canonical redacted JSON and a regular 64-byte completion marker.</behavior>
  <action>Bump `PhysicalIphoneContract`/Evidence schema only as required for the new device-origin assertion set. Update exact parser, join order, decoder, canonical fixture migration, and negative regressions together per D-18/D-19. Require the ephemeral nonce/case/mutation binding during host verification but omit it from the joined candidate and evidence. Keep device and backend owners disjoint, make report correlation single-use, and preserve no-replace digest-bound promotion. Run the API coverage gate against the reasoned no-external-API declaration.</action>
  <verify><automated>mix test test/crosswake/proof_lane/physical_iphone_evidence_transaction_test.exs test/mix/tasks/crosswake.proof_lane.physical_iphone_test.exs test/crosswake/proof_lane/evidence_test.exs --max-failures 1 &amp;&amp; node /Users/jon/.codex/gsd-core/bin/gsd-tools.cjs check api-coverage.verify-pre .planning/phases/163.1-close-gap-v21-physical-adopter-composition</automated></verify>
  <acceptance_criteria>Only the exact ordered complete paired report produces a candidate; every correlation/replay/owner/order mutation fails; Evidence.check/2 accepts the canonical source-bound fixture; coverage gate passes with no fabricated matrix.</acceptance_criteria>
  <done>The composed evidence contract is exact, redacted, source-bound, and replay-resistant.</done>
</task>
<task type="auto">
  <name>Task 2: Execute the source-bound physical proof and record observed truth</name>
  <precondition>TODO-002 is closed with a host-private sanitized handoff that `AdopterHandoff.validate/1` accepts, the configured Phoenix host is reachable, a signed physical iPhone is connected, all Phase 163.1 implementation summaries exist, and the fixed Phase 163.1 evidence destination is absent.</precondition>
  <read_first>.planning/todos/TODO-002-first-b2c-adopter-route-inputs.md, .planning/phases/163.1-close-gap-v21-physical-adopter-composition/163.1-VALIDATION.md, .planning/REQUIREMENTS.md, .planning/ROADMAP.md, .planning/STATE.md, .planning/v21.0-MILESTONE-AUDIT.md, lib/mix/tasks/crosswake.proof_lane.physical_iphone.ex, lib/crosswake/proof_lane/evidence.ex</read_first>
  <files>.planning/phases/163.1-close-gap-v21-physical-adopter-composition/163.1-VALIDATION.md</files>
  <action>Per D-21/D-23, use one invocation-owned mode-0700 run root and one production invocation of `(cd examples/phoenix_host &amp;&amp; MIX_ENV=test mix crosswake.proof_lane.physical_iphone --run --promote --json)` after readiness reports a nonempty all-ready check set and the destination is absent. Automation evaluates every device/backend assertion and Evidence.check/2; delete private run artifacts on every exit. Run the full core, host, composed iOS contract, privacy/adoption-context, and guide drift checks, then record aggregate counts, stable IDs, closed outcomes, the source-bound check, and the narrow non-claims only in 163.1-VALIDATION.md. Leave requirement/support/milestone status for Plans 09-10 after independent validation and audit. Do not retain or summarize prohibited run material.</action>
  <verify><automated>set -euo pipefail; run_root=$(mktemp -d); trap 'rm -rf -- "$run_root"' EXIT; chmod 700 "$run_root"; (cd examples/phoenix_host &amp;&amp; MIX_ENV=test mix crosswake.proof_lane.physical_iphone --readiness --json) &gt; "$run_root/readiness.json"; jq -e '.outcome == "ready" and (.checks | type == "array" and length &gt; 0) and all(.checks[]; .state == "ready")' "$run_root/readiness.json" &gt;/dev/null; test ! -e .planning/phases/163.1-close-gap-v21-physical-adopter-composition/evidence/physical_iphone; (cd examples/phoenix_host &amp;&amp; MIX_ENV=test mix crosswake.proof_lane.physical_iphone --run --promote --json) &gt; "$run_root/result.json"; jq -e '.outcome == "passed" and .device_class == "physical_iphone" and all(.assertions[]; .outcome == "passed")' "$run_root/result.json" &gt;/dev/null; mix run -e 'alias Crosswake.ProofLane.Evidence; :ok = Evidence.check(".planning/phases/163.1-close-gap-v21-physical-adopter-composition/evidence/physical_iphone", :source_bound)'; mix test; (cd examples/phoenix_host &amp;&amp; MIX_ENV=test mix test); mix crosswake.adoption_context.scan; mix crosswake.capability_map.gen --check; mix crosswake.support_matrix.gen --check; test "$(rg -c '^- \[ \] \*\*COMPOSE-0[1-6]:' .planning/REQUIREMENTS.md)" = "6"; test "$(rg -c '^\| COMPOSE-0[1-6] \| Phase 163\.1 \| Pending \|' .planning/REQUIREMENTS.md)" = "6"</automated></verify>
  <acceptance_criteria>Exactly one eligible physical invocation passes against validated adopter input; the exact two-file evidence pair passes source-bound checking; suites and scans pass; aggregate-only Phase 163.1 validation records the fresh run while all six COMPOSE rows remain unchecked/Pending for Plan 10's post-audit reconciliation.</acceptance_criteria>
  <done>The source-bound evidence prerequisite for independent validation and audit exists.</done>
</task>
</tasks>
## Artifacts this phase produces
- Updated `PhysicalIphoneContract.schema_version/0` and exact ordered assertion entries if required.
- Updated exact Evidence decoder/canonical fixture version if required.
- Existing CLI flags `--readiness --json` and `--run --promote --json`; no new command.
- New path `.planning/phases/163.1-close-gap-v21-physical-adopter-composition/evidence/physical_iphone/` containing only canonical evidence JSON and `.complete`.
- Final validated `163.1-VALIDATION.md`; no ORM schema push and no new Ecto migration.
<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| device report + backend report → join | Independently owned untrusted reports enter exact contract. |
| ephemeral run → retained evidence | Sensitive run state is projected to redacted durable truth. |
## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-163.1-22 | Spoofing | report correlation | high | mitigate | Ephemeral binding, owner split, exact assertion order, single-use join. |
| T-163.1-23 | Information Disclosure | evidence publication | high | mitigate | Exact allowlist, final-byte scan, private-root cleanup, two-file output. |
| T-163.1-24 | Tampering | report replay/replacement | high | mitigate | Completion-digest-bound snapshot and no-replace promotion. |
| T-163.1-25 | Elevation of Privilege | optimistic artifact classification | high | mitigate | Depend on Plan 03 exact-path classifier and full repository scan. |
| T-163.1-SC | Tampering | package installs | high | mitigate | No installs. |
</threat_model>
<verification>Run the exact contract suite first, then the single source-bound transaction and full fresh closure gate.</verification>
<success_criteria>All D-01 through D-23 and every audit gap are evidenced; the milestone audit passes without broadening claims.</success_criteria>
<output>Create `.planning/phases/163.1-close-gap-v21-physical-adopter-composition/163.1-08-SUMMARY.md` when done.</output>
