---
phase: 158-adoption-reset-and-route-map
plan: 03
type: execute
wave: 2
depends_on: [158-01]
files_modified:
  - lib/crosswake/planning/first_adopter_context.ex
  - test/crosswake/planning/first_adopter_context_test.exs
autonomous: true
requirements: [RESET-01, RESET-03, RESET-04]
must_haves:
  truths:
    - "One centralized matrix classifies every active first-adopter path into exactly one durable, public, fast-changing, host-private, secret-only, or forbidden destination (D-14, D-15)."
    - "An empty required path category or an unclassified/duplicate active path fails the drift check instead of making the scan vacuously green (RESET-01 empty/adjacency probes)."
    - "Path and rule iteration is deterministic and stable when classifications compare equal (RESET-01 ordering probe)."
    - "v20 remains stopped/partial with no completion tag claim, and Phases 156-157 remain outside active v21 scope (RESET-03)."
    - "Public CI uses generic patterns plus a synthetic canary; privileged terms are optional secret input and are never printed, stored, uploaded, or embedded in failure text (D-16, D-17)."
    - "The durable codename/public phrase split is enforced mechanically across the registered scope (RESET-04)."
  artifacts:
    - path: "lib/crosswake/planning/first_adopter_context.ex"
      provides: "Central routing matrix, scan rules, and stable non-echoing violations"
      exports: ["routing_matrix/0", "active_paths/0", "scan/1", "scan_private_terms/2"]
    - path: "test/crosswake/planning/first_adopter_context_test.exs"
      provides: "Discoverability, v20 truth, drift, public phrase, and private-term canary proof"
  key_links:
    - from: "lib/crosswake/planning/first_adopter_context.ex"
      to: "test/crosswake/planning/first_adopter_context_test.exs"
      via: "Tests consume one path/rule matrix rather than duplicating lists"
      pattern: "routing_matrix"
    - from: ".planning/milestones/v20.0-ROADMAP.md"
      to: "test/crosswake/planning/first_adopter_context_test.exs"
      via: "Stopped/partial and abandoned-evidence assertions lock archive truth"
      pattern: "STOPPED / PARTIAL"
  prohibitions:
    - statement: "MUST NOT echo a configured private term or matched secret content in errors, logs, test names, persisted artifacts, or CI output."
      category: privacy
    - statement: "MUST NOT search git history or external sources to discover, verify, or infer the adopter identity."
      category: privacy
    - statement: "MUST NOT recast abandoned Phase 156-157 planning as shipped evidence or active v21 work."
      category: transparency
flagged_assumptions:
  - "RESET-03's deterministic edge category was unclassified. This plan resolves the behavior with explicit archive/scope assertions, but the classifier result remains flagged for verifier judgment."
  - "RESET-04's deterministic edge category was unclassified. This plan resolves the behavior with synthetic-canary and routing assertions, but the classifier result remains flagged for verifier judgment."
---

<objective>
Centralize first-adopter context routing and turn privacy, discoverability, and stopped-milestone
truth into deterministic browser-free proof.

Purpose: A new maintainer can find the governing boundary without private knowledge, and protected
promotion cannot leak secret terms or revive abandoned scope.
Output: `Crosswake.Planning.FirstAdopterContext` and expanded ExUnit enforcement.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/ADR-FIRST-B2C-ADOPTER.md
@.planning/FIRST-B2C-ADOPTER-ADOPTION-BRIEF.md
@.planning/FIRST-B2C-ADOPTER-LINEAR-ISSUE-DRAFTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/MILESTONES.md
@.planning/milestones/v20.0-ROADMAP.md
@.planning/milestones/v20.0-REQUIREMENTS.md
@.planning/phases/158-adoption-reset-and-route-map/158-CONTEXT.md
@test/crosswake/planning/first_adopter_context_test.exs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Centralize path routing and non-echoing privacy rules</name>
  <read_first>
    - AGENTS.md
    - .planning/phases/158-adoption-reset-and-route-map/158-CONTEXT.md
    - .planning/FIRST-B2C-ADOPTER-ADOPTION-BRIEF.md
    - .planning/FIRST-B2C-ADOPTER-LINEAR-ISSUE-DRAFTS.md
    - test/crosswake/planning/first_adopter_context_test.exs
  </read_first>
  <files>lib/crosswake/planning/first_adopter_context.ex, test/crosswake/planning/first_adopter_context_test.exs</files>
  <behavior>
    - "Every active path is classified exactly once and required destination categories are non-empty."
    - "Public paths require `first adopter`; durable paths may use the codename; host-private/secret/forbidden values never become scanned repository content."
    - "A synthetic configured private term produces only `privacy.private_term` plus the affected path."
  </behavior>
  <action>Create `Crosswake.Planning.FirstAdopterContext` per D-14 and D-20 with one `routing_matrix/0`, stable destination atoms, explicit included/excluded paths, generic prohibited field/commercial patterns, and stable rule IDs. Follow the assigned `test/crosswake/planning/first_adopter_context_test.exs` analog for centralized scanned-path registration, the environment-provided private-term scan, non-echoing failures, and browser-free ExUnit enforcement. Include active governing docs, Phase 158 context/discussion/plan/summary artifacts, TODO-002, Linear drafts, `AGENTS.md`, capability source/guide, and support source/guide per D-15; explicitly exclude historical archives from identity scanning, prompt lineage, superseded brand seeds, raw fixtures/evidence, and git history. Implement pure scan functions that return sorted `%{rule_id: ..., path: ...}` violations only. Parse `CROSSWAKE_PRIVATE_ADOPTER_TERMS` in the caller/test seam case-insensitively and never include terms or matching lines in results per D-16 through D-18. Refactor tests to consume the module rather than retain a second path list.</action>
  <verify>
    <automated>CROSSWAKE_PRIVATE_ADOPTER_TERMS=synthetic-private-term mix test test/crosswake/planning/first_adopter_context_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - `routing_matrix/0` classifies every registered active path exactly once and tests fail on duplicates, unclassified additions, or empty required categories.
    - Private-term canary failure data contains `privacy.private_term` and a path but not the configured term.
    - No network call, git-history scan, scanner service, dependency, or secret persistence is added.
  </acceptance_criteria>
  <done>All active adoption context is routed through one non-vacuous, non-echoing matrix.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Lock discoverability, v20 stopped truth, and active-scope exclusion</name>
  <read_first>
    - .planning/ADR-FIRST-B2C-ADOPTER.md
    - .planning/PROJECT.md
    - .planning/ROADMAP.md
    - .planning/STATE.md
    - .planning/MILESTONES.md
    - .planning/milestones/v20.0-ROADMAP.md
    - .planning/milestones/v20.0-REQUIREMENTS.md
    - .planning/phases/158-adoption-reset-and-route-map/158-CONTEXT.md
    - lib/crosswake/planning/first_adopter_context.ex
    - test/crosswake/planning/first_adopter_context_test.exs
  </read_first>
  <files>test/crosswake/planning/first_adopter_context_test.exs</files>
  <behavior>
    - "Governing docs expose GET-6, reversal conditions, Alpha/public-v1 split, stop list, privacy boundary, current phase, and stop date."
    - "v20 is stopped/partial, has no shipped/completion-tag claim, and Phase 156-157 are absent from active v21 phase listings."
    - "TODO-002 remains open while sanitized route rows are unavailable."
  </behavior>
  <action>Extend `Crosswake.Planning.FirstAdopterContextTest` with explicit assertions for RESET-01 and RESET-03 across the existing canonical docs. Follow the assigned `test/crosswake/planning/first_adopter_context_test.exs` analog for centralized scanned-path registration and browser-free ExUnit enforcement; preserve its environment-provided private-term seam and non-echoing failure discipline while adding discoverability assertions. Verify D-01/D-03 policy-versus-instance completion, D-04 Alpha pause behavior, the accepted GET-6 reversal condition, Android/stop-list freezes, v20 stopped/partial wording, abandoned Phase 156-157 evidence, absence from active v21 phases, and TODO-002 open status. Keep assertions semantic and path-specific; do not mutate the already-correct governing documents solely to satisfy a string count. Treat the unclassified RESET-03/RESET-04 edge results as flagged verifier judgments, not silent dismissals.</action>
  <verify>
    <automated>mix test test/crosswake/planning/first_adopter_context_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - Focused tests prove all RESET-01 discoverability anchors and RESET-03 stopped/partial conditions.
    - No assertion reads git history, external sources, release tags, or private adopter facts.
    - Test failure messages remain calm, operational, and limited to stable rule/path information per D-19.
  </acceptance_criteria>
  <done>Durable strategy and stopped-milestone truth are mechanically discoverable without identity knowledge.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Secret environment -> local scan | Privileged terms enter memory for comparison and must never reach output or disk. |
| Repository path -> public/durable destination | Misclassification can expose a codename or hide an active path from scanning. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-158-01 | Information Disclosure | active adoption files | high | mitigate | Central path matrix, generic scans, privileged canary, and no term echo. |
| T-158-02 | Tampering / Elevation of Privilege | proof-promotion posture | high | mitigate | Tests bind open TODO/unknown route status to no host/device promotion. |
| T-158-03 | Spoofing / Tampering | route safety ownership | high | mitigate | Matrix and Plan 01 contract require route-local fields; defaults are never evidence. |
| T-158-04 | Information Disclosure | public guide paths | high | mitigate | Public destination scan rejects durable codename and requires public phrase. |
| T-158-05 | Information Disclosure | scan errors | high | mitigate | Violation shape is stable rule ID plus affected path only; canary proves term absence. |

ASVS Level 1: every high-severity threat is covered by the focused ExUnit gate.
</threat_model>

<verification>
Run the privileged synthetic-canary command after Task 1 and the focused context test after Task 2.
Run the Phase 158 quick command at plan completion; all are browser-free and under 120 seconds.
</verification>

<success_criteria>
- RESET-01, RESET-03, and RESET-04 are mechanically enforced.
- D-14 through D-20 have one deterministic implementation surface.
- Missing real private-term secrets do not fail ordinary fork PRs, while protected promotion can run the privileged scan.
</success_criteria>

## Artifacts this phase produces

- `lib/crosswake/planning/first_adopter_context.ex`
  - module `Crosswake.Planning.FirstAdopterContext`
  - functions `routing_matrix/0`, `active_paths/0`, `scan/1`, `scan_private_terms/2`
  - destination atoms and stable privacy rule IDs
- `test/crosswake/planning/first_adopter_context_test.exs`
  - centralized routing, drift, canary, discoverability, and v20 assertions

<output>
Create `.planning/phases/158-adoption-reset-and-route-map/158-03-SUMMARY.md` when done.
</output>
