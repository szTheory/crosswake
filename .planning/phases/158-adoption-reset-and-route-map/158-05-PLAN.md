---
phase: 158-adoption-reset-and-route-map
plan: 05
type: tdd
wave: 4
depends_on: [158-04]
files_modified:
  - lib/crosswake/adoption/route_inventory.ex
  - test/crosswake/adoption/route_inventory_test.exs
  - .planning/FIRST-B2C-ADOPTER-ROUTE-POLICY-MAP.md
autonomous: true
gap_closure: true
requirements: [RESET-02]
must_haves:
  truths:
    - "A concrete route row cannot become eligible when any route-local safety field uses `known_default` (D-05, D-06, D-07, RESET-02 boundary probe)."
    - "A local-mutation route cannot become eligible without coherent offline-island ownership, mutation, scope, fallback, disablement, queued-data retention, and auth posture (D-06, D-10, RESET-02)."
    - "Recent-auth requirements and auth authority agree in both directions before promotion; `not_applicable` is accepted only when route semantics prove the field does not apply (D-06, D-07)."
    - "One fully explicit local-mutation row, including a coherent recent-auth variant, remains eligible after the fail-closed checks are added."
    - "Existing collision, empty-inventory, declaration-order, closed-vocabulary, privacy, and precision behaviors remain green; RESET-01 and RESET-03 truth is not reopened."
  artifacts:
    - path: "lib/crosswake/adoption/route_inventory.ex"
      provides: "Fail-closed safety-status and route-state invariant validation before promotion"
      exports: ["validate/1", "validate!/1", "validate_inventory/1", "promotion_status/1"]
    - path: "test/crosswake/adoption/route_inventory_test.exs"
      provides: "Known-default, not-applicable, cross-field, and positive eligibility regressions"
    - path: ".planning/FIRST-B2C-ADOPTER-ROUTE-POLICY-MAP.md"
      provides: "Documented concrete-route invariant and eligibility contract"
  key_links:
    - from: "lib/crosswake/adoption/route_inventory.ex"
      to: "test/crosswake/adoption/route_inventory_test.exs"
      via: "Every blocked status/state combination and positive eligible row is exercised through the public validate-to-promotion path"
      pattern: "promotion_status"
    - from: ".planning/FIRST-B2C-ADOPTER-ROUTE-POLICY-MAP.md"
      to: "lib/crosswake/adoption/route_inventory.ex"
      via: "The documented route-local safety rules match the executable validators"
      pattern: "known_default"
  prohibitions:
    - statement: "MUST NOT allow product-surface defaults to become concrete-route safety authority."
      category: safety
      verification: flagged-unverified
    - statement: "MUST NOT infer adopter-specific scope, auth, fallback, disablement, retention, mutation, media, or route values."
      category: privacy
      verification: flagged-unverified
    - statement: "MUST NOT widen the route inventory into replay, proof-lane, pack, device, Android, generic sync, generic storage, or feature-flag implementation."
      category: values
      verification: flagged-unverified
---

<objective>
Close the executable RESET-02 promotion gaps by making route safety status and cross-field
coherence fail closed from sanitized input through eligibility.

Purpose: Prevent a defaults-only or semantically incomplete concrete route from being promoted as
ready for host or physical-device proof.
Output: Hardened route inventory validation, exhaustive focused regressions, and aligned route-map
truth.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/ADR-FIRST-B2C-ADOPTER.md
@.planning/FIRST-B2C-ADOPTER-ROUTE-POLICY-MAP.md
@.planning/phases/158-adoption-reset-and-route-map/158-CONTEXT.md
@.planning/phases/158-adoption-reset-and-route-map/158-VERIFICATION.md
@.planning/phases/158-adoption-reset-and-route-map/158-01-SUMMARY.md
@lib/crosswake/adoption/route_inventory.ex
@test/crosswake/adoption/route_inventory_test.exs

<interfaces>
Preserve the existing `Crosswake.Adoption.RouteInventory` public API. Add private, named validation
seams for concrete-route safety status and normalized route-state invariants; all failures continue
to use `ValidationError` with stable rule ID, safe route reference, and field only.
</interfaces>
</context>

<assumption_delta_decision>
Detected delta: `fallback` pluralization.
Primary noun: route-local runtime, authority, and fallback posture.
Decision: no-change.
Rationale: D-06 and the route-policy map already make explicit route-local ownership primary; the
pluralized `fallbacks` field is the closed set of online, offline, denied, corrupt-pack, and
disabled outcomes, not a competing ownership model.
</assumption_delta_decision>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Block inherited safety posture through the validate-to-promotion path</name>
  <read_first>
    - AGENTS.md
    - .planning/phases/158-adoption-reset-and-route-map/158-CONTEXT.md
    - .planning/phases/158-adoption-reset-and-route-map/158-VERIFICATION.md
    - .planning/FIRST-B2C-ADOPTER-ROUTE-POLICY-MAP.md
    - lib/crosswake/adoption/route_inventory.ex
    - test/crosswake/adoption/route_inventory_test.exs
  </read_first>
  <files>lib/crosswake/adoption/route_inventory.ex, test/crosswake/adoption/route_inventory_test.exs</files>
  <behavior>
    - "For each member of the module's concrete-route safety-field set, replacing an explicit posture with status `known_default` returns a stable fail-closed validation error before promotion."
    - "The error contains only a rule ID, the sanitized route reference, the field, and remediation; it does not echo supplied posture content."
    - "A row whose safety values are all `confirmed_sanitized` continues through `validate/1` and `promotion_status/1` to `{:eligible, row}`."
  </behavior>
  <action>Start RED with a table-driven regression that iterates the actual concrete-route safety fields and proves a `known_default` status cannot cross `RouteInventory.validate/1`; include the verifier's defaults-only row as an end-to-end negative control. Implement one private safety-status validator in `lib/crosswake/adoption/route_inventory.ex` and invoke it before value acceptance, per D-05 through D-07. Keep `status_values/0` unchanged because the vocabulary still describes the layered discovery model; the concrete-row boundary, not the vocabulary getter, rejects inheritance. Use a dedicated stable rule ID and preserve non-echoing `ValidationError` output per D-09, D-10, D-17, and D-19. Do not encode product-surface defaults or adopter facts in the module.</action>
  <verify>
    <automated>mix test test/crosswake/adoption/route_inventory_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - Every concrete-route safety field has a regression proving `known_default` is rejected before eligibility.
    - The defaults-only verifier reproduction no longer returns an eligible result.
    - The existing fully confirmed synthetic row remains eligible and all pre-existing inventory tests remain green.
    - Failure output contains no supplied posture value or matched sensitive content.
  </acceptance_criteria>
  <done>Concrete route promotion can no longer inherit any safety-critical value from the product-surface discovery table.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Enforce local-mutation and recent-auth cross-field invariants</name>
  <read_first>
    - .planning/phases/158-adoption-reset-and-route-map/158-CONTEXT.md
    - .planning/phases/158-adoption-reset-and-route-map/158-VERIFICATION.md
    - .planning/FIRST-B2C-ADOPTER-ROUTE-POLICY-MAP.md
    - lib/crosswake/adoption/route_inventory.ex
    - test/crosswake/adoption/route_inventory_test.exs
  </read_first>
  <files>lib/crosswake/adoption/route_inventory.ex, test/crosswake/adoption/route_inventory_test.exs, .planning/FIRST-B2C-ADOPTER-ROUTE-POLICY-MAP.md</files>
  <behavior>
    - "A `local_first` route requires `offline_island` ownership, a non-`none` mutation category, opaque partitioned scope with logout/account-switch replay stops, queue-local and retain-and-block fallbacks, entry/replay disablement, and retain-until-resolution posture."
    - "For those required local-mutation fields, `not_applicable` is rejected with a stable field-specific rule rather than treated as eligible."
    - "An auth posture of `recent_auth` requires recent auth to be `required`, and a `required` recent-auth posture requires recent-auth authority rather than a weaker auth level."
    - "A coherent recent-auth local-mutation row validates and promotes, while each single-field contradiction is rejected."
  </behavior>
  <action>Start RED with one negative test per invariant axis and the verifier's incoherent local-mutation reproduction, plus one positive recent-auth local-mutation row. After the closed per-field postures are normalized, call private `validate_route_invariants/1` helpers before constructing the final struct. Enforce local-first ownership, actionable mutation, scope/logout/account-switch isolation, offline/disabled fallback, entry/replay disablement, queued-data retention, and recent-auth authority exactly as stated by the route-policy map and D-06 through D-10. Treat `not_applicable` as valid only when the normalized route semantics prove the field is irrelevant; never accept it unconditionally. Return stable field/route errors without supplied values. Update the route-policy map with a concise invariant table that matches the executable rules and retains TODO-002 as open; do not add a concrete adopter row.</action>
  <verify>
    <automated>mix test test/crosswake/adoption/route_inventory_test.exs &amp;&amp; mix test test/crosswake/planning/first_adopter_context_test.exs test/crosswake/adoption/route_inventory_test.exs test/crosswake/capability_map test/crosswake/support_matrix</automated>
  </verify>
  <acceptance_criteria>
    - The incoherent local-mutation verifier reproduction is blocked by validation and cannot reach an eligible promotion result.
    - Focused tests isolate every required scope, fallback, disablement, retention, ownership, mutation, and recent-auth contradiction.
    - At least one coherent recent-auth local-mutation row returns `{:eligible, row}` through the public API.
    - Existing RESET-02 adjacency, empty, ordering, and precision regressions remain green.
    - The route-policy map and implementation express the same invariant set without adding host-private or adopter-specific facts.
  </acceptance_criteria>
  <done>Local mutation and recent-auth routes promote only when their complete route-local authority and failure posture is coherent.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Product-surface default -> concrete route row | Discovery guidance must not become executable safety authority. |
| Sanitized route row -> host/device proof eligibility | Contradictory scope, auth, fallback, disablement, or retention posture could falsely promote a route. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-158-G05-01 | Spoofing / Tampering | `validate_posture/3` safety status | high | mitigate | Reject `known_default` for every concrete-route safety field and cover the defaults-only reproduction with table-driven tests. |
| T-158-G05-02 | Tampering / Elevation of Privilege | route-state promotion invariants | high | mitigate | `validate_route_invariants/1` blocks incoherent local mutation and recent-auth combinations before struct creation or eligibility. |
| T-158-G05-03 | Information Disclosure | validation failure output | high | mitigate | Stable rule ID, sanitized route reference, and field-only errors; negative tests assert supplied values are absent. |
| T-158-G05-04 | Denial of Service | overly broad invariant rejection | medium | mitigate | Positive eligible fixtures cover authenticated and recent-auth local-mutation rows while existing read-only/not-applicable cases remain explicit. |

ASVS Level 1: all high-severity threats are blocking focused-test failures.
</threat_model>

<verification>
Run the focused route inventory file after each RED/GREEN cycle. At plan completion run the Phase
158 quick suite so route hardening cannot regress capability, support, or planning-context truth.
</verification>

<success_criteria>
- RESET-02 no longer permits defaults-only or incoherent local-mutation/recent-auth eligibility.
- All status and cross-field denials are deterministic, field-specific, and non-echoing.
- The positive eligible row proves the fix is fail-closed rather than universally blocking.
- Prior satisfied RESET-01 and RESET-03 truth remains unchanged.
</success_criteria>

## Artifacts this phase produces

- `lib/crosswake/adoption/route_inventory.ex`
  - private concrete-route safety-status validator
  - private `validate_route_invariants/1` orchestration and named local-mutation/recent-auth invariant helpers
  - stable rule IDs for inherited posture and incoherent route state
- `test/crosswake/adoption/route_inventory_test.exs`
  - defaults-only tracer regression
  - table-driven per-safety-field `known_default` regressions
  - local-mutation and recent-auth negative matrix
  - positive eligible recent-auth local-mutation row
- `.planning/FIRST-B2C-ADOPTER-ROUTE-POLICY-MAP.md`
  - executable invariant table aligned with `RouteInventory`

<output>
Create `.planning/phases/158-adoption-reset-and-route-map/158-05-SUMMARY.md` when done.
</output>
