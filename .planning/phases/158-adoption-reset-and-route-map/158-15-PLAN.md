---
phase: 158-adoption-reset-and-route-map
plan: "15"
type: execute
wave: 11
depends_on: [158-14]
files_modified:
  - lib/crosswake/planning/first_adopter_context.ex
  - test/crosswake/planning/first_adopter_context_test.exs
  - test/mix/tasks/crosswake_adoption_context_scan_test.exs
autonomous: true
gap_closure: true
requirements: [RESET-04]
must_haves:
  truths:
    - "Every tracked or non-ignored repository candidate is classified before the protected scan can pass; the fixed artifact glob list is no longer the discovery boundary (D-14, D-15, D-16, RESET-04)."
    - "An unregistered guide, a Phase 159-162 artifact, a workflow, a source file, and a test file all enter the same private-term scan used by the Mix task without a per-file registry edit (D-15, D-17)."
    - "Repository enumeration failure, an unsafe candidate path, or a text artifact outside the closed classification rules returns a stable routing rule and relative path instead of silently dropping the file (D-15, D-20)."
    - "Private-term failures expose only `privacy.private_term` and repository-relative paths; configured terms, matched text, Git output, and file contents never enter test, Mix-task, or CI diagnostics (D-17, RESET-04)."
    - "Historical archives, prompt lineage, superseded brand seeds, and raw fixture/evidence paths retain explicit non-scanned classifications rather than being reached through history or external discovery (D-15, D-18)."
  artifacts:
    - path: "lib/crosswake/planning/first_adopter_context.ex"
      provides: "Fail-closed repository candidate enumeration, classification, and non-echoing content scanning"
      exports: ["artifact_globs/0", "routing_matrix/0", "active_paths/0", "discover_paths/1", "scan_filesystem/2"]
    - path: "test/crosswake/planning/first_adopter_context_test.exs"
      provides: "Repository-boundary tracer plus unregistered guide/later-phase/workflow/source/test regressions"
    - path: "test/mix/tasks/crosswake_adoption_context_scan_test.exs"
      provides: "End-to-end Mix-task blocking and non-echoing diagnostics over unregistered candidates"
  key_links:
    - from: "lib/crosswake/planning/first_adopter_context.ex"
      to: "Git repository index and non-ignored worktree candidates"
      via: "`git -C ROOT ls-files --cached --others --exclude-standard -z` feeds deterministic repository candidate classification"
      pattern: "ls-files|--exclude-standard"
    - from: "lib/crosswake/planning/first_adopter_context.ex"
      to: "guides/, .planning/phases/159-162, .github/workflows/, lib/, test/"
      via: "closed path rules classify repository-facing text artifacts as scanned without exact-file registration"
      pattern: "classify_repository_path|repository_candidates"
    - from: "test/mix/tasks/crosswake_adoption_context_scan_test.exs"
      to: "Mix.Tasks.Crosswake.AdoptionContext.Scan.run/1"
      via: "temporary Git repository candidates flow through the production Mix task and `scan_filesystem/2`"
      pattern: "Scan.run|privacy.private_term"
  prohibitions:
    - statement: "MUST NOT print, persist, upload, or return configured private terms, matched lines, Git command output, or surrounding file content."
      category: privacy
      verification: automated
    - statement: "MUST NOT search git history, external sources, or ignored raw evidence to infer adopter identity."
      category: values
      verification: automated
    - statement: "MUST NOT retain the fixed allowlist as the sole discovery path or silently classify every unknown candidate with an unexamined catch-all."
      category: safety
      verification: automated
---

<objective>
Close the remaining RESET-04 bypass by making repository enumeration and path classification the
fail-closed front door to private-term scanning.

Purpose: Ensure an unregistered repository-facing artifact cannot evade protected CI merely
because its exact path is absent from `@artifact_globs`.
Output: A tracked/non-ignored repository classification boundary plus direct and Mix-task
regressions for unregistered guide, later-phase, workflow, source, and test artifacts.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/ADR-FIRST-B2C-ADOPTER.md
@.planning/phases/158-adoption-reset-and-route-map/158-CONTEXT.md
@.planning/phases/158-adoption-reset-and-route-map/158-VERIFICATION.md
@.planning/phases/158-adoption-reset-and-route-map/158-06-SUMMARY.md
@.planning/phases/158-adoption-reset-and-route-map/158-12-SUMMARY.md
@.planning/phases/158-adoption-reset-and-route-map/158-14-SUMMARY.md
@lib/crosswake/planning/first_adopter_context.ex
@lib/mix/tasks/crosswake.adoption_context.scan.ex
@test/crosswake/planning/first_adopter_context_test.exs
@test/mix/tasks/crosswake_adoption_context_scan_test.exs

<interfaces>
Preserve `Crosswake.Planning.FirstAdopterContext.scan/1`, `scan_private_terms/2`,
`scan_filesystem/2`, `routing_matrix/0`, `active_paths/0`, `artifact_globs/0`, and
`discover_paths/1`, plus `mix crosswake.adoption_context.scan [--root PATH]
[--require-private-terms]`. `scan_filesystem/2` remains the production boundary and returns only
sorted `%{rule_id: binary(), path: binary()}` violations. The implementation may add private
`repository_candidates/1`, `classify_repository_path/1`, and `scannable_text_path?/1` helpers;
none becomes a new public API.
</interfaces>
</context>

<assumption_delta_decision>
Primary noun: repository artifact classification.
Decision: no-change — the deterministic detector fired on the roadmap's existing
"authority/fallback story" wording, not on a second identity model, optional anchor, or newly
chosen primary key. This gap broadens discovery coverage while retaining repository-relative path
identity and the existing destination model.
</assumption_delta_decision>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Trace unregistered repository artifacts through fail-closed classification</name>
  <reversibility rating="costly">Changing discovery from a fixed registry to repository-wide classification alters which checked-in artifacts can block CI, so undo requires coordinated scanner-policy and regression changes.</reversibility>
  <read_first>
    - AGENTS.md
    - .planning/ADR-FIRST-B2C-ADOPTER.md
    - .planning/phases/158-adoption-reset-and-route-map/158-CONTEXT.md
    - .planning/phases/158-adoption-reset-and-route-map/158-VERIFICATION.md
    - .planning/phases/158-adoption-reset-and-route-map/158-06-SUMMARY.md
    - lib/crosswake/planning/first_adopter_context.ex
    - test/crosswake/planning/first_adopter_context_test.exs
  </read_first>
  <files>lib/crosswake/planning/first_adopter_context.ex, test/crosswake/planning/first_adopter_context_test.exs</files>
  <behavior>
    - "A temporary Git repository containing `guides/unregistered-adoption-note.md` and `.planning/phases/159-host-reusable-proof-lane/159-NOTES.md` is enumerated without adding either exact path to `@artifact_globs`; a process-assembled private term in either path yields `privacy.private_term` for that relative path."
    - "Equivalent tracked/non-ignored candidates under `.github/workflows/`, `lib/`, and `test/` are classified as scanned repository text and cannot bypass the private-term rule."
    - "Ignored candidates and explicit historical/prompt/raw-evidence exclusions remain classified outside the scanned set, while unknown text path shapes and repository-enumeration failure yield stable routing violations rather than an empty success."
    - "No returned or raised value contains the process-assembled term, matched content, or raw Git stderr/stdout."
    - "The real repository scan still includes the active governing files, Phase 158 artifacts, all Phase 159-162 artifacts already present, guides, workflows, source, and tests while preserving named public destinations for public-phrase enforcement."
  </behavior>
  <action>Start RED with a temporary repository helper that initializes Git locally, writes candidates, and constructs the private canary only at runtime from neutral fragments. Add table-driven cases for an unregistered guide, a Phase 159 artifact, one workflow, one Elixir source, and one ExUnit test; assert the same `scan_filesystem/2` entry point returns only stable rule/path pairs. Replace `discovered_entries/1`'s `@artifact_globs` expansion as the sole input with deterministic `git -C ROOT ls-files --cached --others --exclude-standard -z` enumeration. Parse NUL-delimited relative paths, reject traversal/symlink/non-regular candidates before reads, and convert Git failure to a stable repository-enumeration routing violation without including command output. Classify each candidate through ordered rules: retain exact named public and fast-changing destinations; scan active governing files, guides, Phase 158 and Phase 159-162 planning artifacts, workflows, source, tests, and other supported repository text under the durable destination; explicitly classify historical milestone/older-phase archives, prompt lineage, superseded brand seeds, binaries, and raw fixture/evidence trees as non-scanned forbidden material per D-15; return `routing.unclassified_path` for any remaining text candidate instead of defaulting it away. Keep `artifact_globs/0` as compatibility/policy metadata, not discovery authority. Preserve destination-specific public phrase rules only for the named public adoption surfaces, apply generic and private-term checks to every scanned entry, sort/deduplicate violations, and expose no sensitive data per D-14 through D-18.</action>
  <verify>
    <automated>mix test test/crosswake/planning/first_adopter_context_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - The unregistered guide and Phase 159 artifact both fail with `privacy.private_term` and their repository-relative paths through `scan_filesystem/2`.
    - Workflow, source, and test candidates are discovered and scanned without exact-file registration.
    - Candidate enumeration uses the Git index plus non-ignored worktree files; it does not inspect commit history, remotes, or external sources.
    - Git failure, traversal/symlink candidates, and unclassified text paths cannot produce a clean result.
    - Explicit exclusions remain destination-classified and non-scanned; ignored/build/dependency files are not enumerated.
    - The fixed artifact glob collection remains available to callers but is not the privacy boundary.
    - Every assertion inspects stable rule/path data only; neither the complete canary nor matched content is written to a checked-in file or failure message.
  </acceptance_criteria>
  <done>Every repository-facing text candidate is classified before scanning can pass, and the representative previously unregistered surfaces are blocked end to end.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prove the production Mix task blocks unregistered guide and later-phase canaries</name>
  <read_first>
    - .planning/phases/158-adoption-reset-and-route-map/158-VERIFICATION.md
    - lib/crosswake/planning/first_adopter_context.ex
    - lib/mix/tasks/crosswake.adoption_context.scan.ex
    - test/crosswake/planning/first_adopter_context_test.exs
    - test/mix/tasks/crosswake_adoption_context_scan_test.exs
    - .github/workflows/hex-page-proof.yml
  </read_first>
  <files>test/mix/tasks/crosswake_adoption_context_scan_test.exs</files>
  <behavior>
    - "With a process-assembled private term configured, `Scan.run([\"--root\", root])` raises for an unregistered guide and a Phase 159 artifact discovered from the temporary Git repository."
    - "The Mix error contains sorted `privacy.private_term PATH` lines only and omits the configured term, matching content, and Git output."
    - "A clean temporary repository and the live repository both pass through the same production task after all candidates are classified."
    - "The existing protected workflow remains wired to `mix crosswake.adoption_context.scan --require-private-terms`; no CI, secret, service, or dependency expansion is introduced."
  </behavior>
  <action>Extend the existing Mix-task test helper so every supplied root is a local Git repository and its test files are visible to the same cached/non-ignored enumeration used by production. Add one end-to-end failing case containing both an unregistered guide and a Phase 159 artifact with a term assembled inside the process from neutral fragments. Invoke the real `Scan.run/1`, assert deterministic rule/path lines for both files, and refute disclosure of the term and surrounding content. Retain the clean-root, generic rule, public-spelling, identifying-field, missing-required-secret, and trusted-workflow cases. Do not change the Mix task or workflow unless the implementation contract proves an actual wiring defect; this task verifies the existing caller now inherits the repository-wide boundary from Task 1.</action>
  <verify>
    <automated>mix test test/crosswake/planning/first_adopter_context_test.exs test/mix/tasks/crosswake_adoption_context_scan_test.exs &amp;&amp; mix crosswake.adoption_context.scan &amp;&amp; mix format --check-formatted lib/crosswake/planning/first_adopter_context.ex test/crosswake/planning/first_adopter_context_test.exs test/mix/tasks/crosswake_adoption_context_scan_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - The production Mix task returns non-zero for both unregistered canary paths and identifies only stable rule IDs plus relative paths.
    - The Mix-task test fails against the pre-Plan-15 fixed allowlist and passes only after repository enumeration is wired through `scan_filesystem/2`.
    - The clean live-repository scan exits zero after classifying guides, Phase 159-162 artifacts, workflows, source, and tests.
    - Protected workflow assertions remain green and no secret value, result artifact, new action, package, network request, or external scanner is added.
    - The three changed Elixir source/test files pass the explicit formatter gate.
  </acceptance_criteria>
  <done>The existing merge-blocking Mix task proves that unregistered repository-facing artifacts cannot bypass private-term enforcement.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| Git index/non-ignored worktree -> repository candidate set | Candidate omission or enumeration failure could silently bypass protected scanning. |
| Candidate path -> classifier/file read | Traversal, symlink, binary, historical, or unknown paths require explicit safe handling before content is read. |
| Secret environment -> scanner diagnostics | Configured private terms must remain memory-only and absent from all output. |
| Scanner library -> Mix task/CI status | Repository classification must reach the already merge-blocking command without a second discovery seam. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- | --- |
| T-158-G15-01 | Tampering / Elevation of Privilege | repository candidate discovery | high | mitigate | Enumerate cached plus non-ignored candidates with NUL-delimited Git output and fail closed on command failure. |
| T-158-G15-02 | Tampering | path classification | high | mitigate | Ordered explicit scan/exclusion rules plus `routing.unclassified_path` prevent silent unknown-text fallback. |
| T-158-G15-03 | Information Disclosure | private-term comparison and errors | high | mitigate | Process-only canaries and rule/path-only results; focused and Mix-task tests reject term/content/Git-output echo. |
| T-158-G15-04 | Elevation of Privilege | unregistered guide and later-phase artifact | high | mitigate | Direct and production-task regressions prove both paths block without exact registration. |
| T-158-G15-05 | Tampering | workflow/source/test coverage | high | mitigate | Table-driven candidate cases demonstrate these repository-facing trees enter the same scanner. |
| T-158-G15-06 | Information Disclosure | explicit historical/raw exclusions | medium | mitigate | Classify excluded paths without reading them and retain the D-15 prohibition on history/external identity discovery. |
| T-158-G15-07 | Denial of Service | binary/generated/dependency inputs | medium | mitigate | Git ignore semantics plus closed text/binary classification bound reads and avoid invalid-text processing. |

ASVS Level 1: every high-severity discovery, bypass, and disclosure threat has a blocking automated mitigation.
</threat_model>

<verification>
- Focused module tests prove candidate enumeration, classification, direct private-term blocking, exclusions, and stable non-echoing failures.
- Mix-task tests prove the production caller inherits the same discovery boundary for an unregistered guide and later-phase artifact.
- The live repository task and explicit formatter command must pass before Plan 158-16 may reconcile evidence.
</verification>

<success_criteria>
- The fixed allowlist no longer controls whether a repository-facing file is scanned.
- Unregistered guide, later-phase, workflow, source, and test paths are mechanically covered.
- Enumeration/classification failures are blocking and non-echoing.
- RESET-04 remains the only requirement changed by this gap plan.
</success_criteria>

## Deterministic Planner Gate Evaluation

- **API coverage:** Not applicable. This gap changes an internal Elixir module and existing Mix-task data flow; it adds no HTTP, GraphQL, bridge, or external service endpoint. Public module/Mix-task compatibility is explicitly preserved and tested.
- **Assumption delta:** The deterministic detector returned `detected: true` only for the pre-existing roadmap phrase `authority/fallback story`. The recorded decision is `no-change`: repository-relative paths remain the identity anchor, while candidate discovery is promoted from exact registration to repository classification. Separately, the prior completeness assumption around `@artifact_globs` is invalidated by `158-VERIFICATION.md` and replaced with directly testable Git enumeration.
- **Schema push:** Not applicable. The scoped files contain no supported ORM schema, migration, or database model change.

## Phase-wide Multi-Source Coverage Audit

| SOURCE | ID | Feature / constraint | Covering plans | Status | Notes |
| --- | --- | --- | --- | --- | --- |
| GOAL | — | Privacy-safe context routing for the first-adopter adoption reset | 158-03, 06, 08, 12, 15, 16 | COVERED | Plan 15 closes the sole verified discovery bypass; Plan 16 reconciles fresh evidence. |
| REQ | RESET-01 | Durable infrastructure framing and discoverability | 158-02 through 04, 09, 10, 13, 14 | COVERED | Already satisfied; unchanged by this gap pass. |
| REQ | RESET-02 | Explicit route ownership and fail-closed posture | 158-01, 04, 05, 07, 11, 14 | COVERED | Already satisfied; unchanged by this gap pass. |
| REQ | RESET-03 | Honest v20 stopped/partial truth | 158-03, 04, 07, 10, 14 | COVERED | Already satisfied; unchanged by this gap pass. |
| REQ | RESET-04 | No prohibited identity/private terms in repository-facing adoption artifacts | 158-03, 06, 08, 12, 15, 16 | COVERED | Repository enumeration, non-echoing regressions, and fresh reconciliation close the remaining blocker. |
| RESEARCH | — | Centralized browser-free privacy scan with no new scanner service/dependency | 158-15 | COVERED | Existing module, Git, ExUnit, and Mix task only. |
| RESEARCH | — | Explicit scanning must not be vacuous when new active files appear | 158-15 | COVERED | Tracked/non-ignored candidate enumeration supersedes fixed discovery. |
| CONTEXT | D-01 through D-13 | Completeness, route, and capability contracts | 158-01 through 14 | COVERED | Already implemented; Plan 15 does not alter route/support/capability semantics. |
| CONTEXT | D-14 | Centralized destination routing and privacy boundary | 158-03, 06, 15 | COVERED | Every repository candidate receives a scan/exclusion classification. |
| CONTEXT | D-15 | Explicit drift-checked scope and prohibited discovery sources | 158-15 | COVERED | Candidate rules cover active surfaces and explicitly retain historical/prompt/raw exclusions. |
| CONTEXT | D-16 | Always-on public rules plus supplied synthetic/private checks | 158-08, 12, 15 | COVERED | Existing rules now operate over the complete repository-facing candidate set. |
| CONTEXT | D-17 | Secret-backed, case-insensitive, non-echoing private-term gate | 158-08, 12, 15 | COVERED | Direct and Mix-task regressions expose only rule/path pairs. |
| CONTEXT | D-18 | No new scanner service/dependency | 158-15 | COVERED | Uses existing Git/Mix/ExUnit environment only. |
| CONTEXT | D-19, D-20 | Calm deterministic maintainer workflow, no UI/dashboard/taxonomy | 158-15, 16 | COVERED | Stable diagnostics and reproducible local commands only. |
| CONTEXT | Deferred/stop list | No proof-lane, replay, pack, device, Android, generic sync/storage, UI, or identity discovery | — | EXCLUDED | Outside Phase 158 and not part of the verified gap. |

## Artifacts this phase produces

- Modified `lib/crosswake/planning/first_adopter_context.ex` with private `repository_candidates/1`, `classify_repository_path/1`, and `scannable_text_path?/1` helpers; no new public symbol.
- Modified `test/crosswake/planning/first_adopter_context_test.exs` with temporary-Git tracer and table-driven unregistered guide/later-phase/workflow/source/test coverage.
- Modified `test/mix/tasks/crosswake_adoption_context_scan_test.exs` with production Mix-task regressions for the unregistered guide and later-phase paths.
- No new file, dependency, API endpoint, schema, route, UI, service, or adopter-instance fact.

<output>
Create `.planning/phases/158-adoption-reset-and-route-map/158-15-SUMMARY.md` when done.
</output>
