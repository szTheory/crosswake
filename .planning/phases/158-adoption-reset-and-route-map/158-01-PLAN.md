---
phase: 158-adoption-reset-and-route-map
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/crosswake/adoption/route_inventory.ex
  - test/crosswake/adoption/route_inventory_test.exs
  - .planning/FIRST-B2C-ADOPTER-ROUTE-POLICY-MAP.md
autonomous: true
requirements: [RESET-02, RESET-04]
must_haves:
  truths:
    - "A sanitized concrete Phoenix route pattern can be validated end to end without recording a resource instance or adopter-specific fact (D-01, D-05, RESET-02)."
    - "Missing adopter-supplied values are represented by `unknown_blocking`; empty, nil, or omitted safety fields are rejected rather than treated as confirmation (D-02, D-07)."
    - "Two route rows with the same opaque route ID or sanitized path pattern are rejected as collisions; adjacency never silently merges ownership records (RESET-01 adjacency probe)."
    - "An empty route inventory is policy-contract complete but adopter-instance incomplete and returns a blocked promotion result rather than a vacuous pass (RESET-01 empty probe)."
    - "Validated route rows retain declaration order when values compare equal, making renderer/test output stable (RESET-01 ordering probe)."
    - "The promotion boundary is exact: all route-local safety fields explicitly confirmed can be eligible, while any `unknown_blocking` field blocks promotion (RESET-02 boundary probe)."
    - "Size posture uses closed size bands and codec families; exact byte counts, digests, URLs, endpoints, and host flag names are rejected from durable rows (D-08, D-09, RESET-02 precision probe)."
  artifacts:
    - path: "lib/crosswake/adoption/route_inventory.ex"
      provides: "Closed sanitized route-row contract and promotion-readiness evaluation"
      exports: ["validate/1", "validate!/1", "validate_inventory/1", "promotion_status/1", "status_values/0"]
    - path: "test/crosswake/adoption/route_inventory_test.exs"
      provides: "Fail-closed route inventory, collision, privacy, and unknown-blocking proof"
    - path: ".planning/FIRST-B2C-ADOPTER-ROUTE-POLICY-MAP.md"
      provides: "Layered surface defaults plus concrete-route contract with explicit unresolved input"
  key_links:
    - from: ".planning/FIRST-B2C-ADOPTER-ROUTE-POLICY-MAP.md"
      to: "lib/crosswake/adoption/route_inventory.ex"
      via: "Documented field/status vocabulary matches the closed validator"
      pattern: "unknown_blocking"
    - from: "lib/crosswake/adoption/route_inventory.ex"
      to: "test/crosswake/adoption/route_inventory_test.exs"
      via: "Synthetic, sanitized rows prove accepted and blocked paths without adopter facts"
      pattern: "promotion_status"
  prohibitions:
    - statement: "MUST NOT fabricate adopter-specific route IDs, paths, mutations, media values, fallbacks, endpoints, flags, or authority facts when sanitized inputs are unavailable."
      category: transparency
    - statement: "MUST NOT let a product-surface default silently supply route-local auth, recent-auth, scope, mutation, media, fallback, disablement, or queued-data-retention posture."
      category: safety
    - statement: "MUST NOT store raw answers, media, transcripts, credentials, account or device identifiers, proprietary taxonomy, commercial details, or revealing links in the route inventory."
      category: privacy
flagged_assumptions:
  - "RESET-03 edge probe is unclassified and remains unresolved for manual verification in Plan 03; it is not auto-dismissed."
  - "RESET-04 edge probe is unclassified and remains unresolved for manual verification in Plan 03; it is not auto-dismissed."
---

<objective>
Deliver the production tracer for Phase 158: one synthetic sanitized route row crosses the public
inventory contract, closed Elixir validation, fail-closed promotion status, and deterministic
ExUnit proof without inventing any adopter route fact.

Purpose: Establish an executable ownership contract before capability, privacy, and support-truth
expansion.
Output: `Crosswake.Adoption.RouteInventory`, its tests, and the layered route-policy map.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/ADR-FIRST-B2C-ADOPTER.md
@.planning/FIRST-B2C-ADOPTER-ROUTE-POLICY-MAP.md
@.planning/todos/TODO-002-first-b2c-adopter-route-inputs.md
@.planning/phases/158-adoption-reset-and-route-map/158-CONTEXT.md
@.planning/phases/158-adoption-reset-and-route-map/158-RESEARCH.md
@.planning/phases/158-adoption-reset-and-route-map/158-PATTERNS.md
@lib/crosswake/policy/schema.ex
@lib/crosswake/policy/route.ex

<interfaces>
Create `Crosswake.Adoption.RouteInventory` with `validate/1`, `validate!/1`,
`validate_inventory/1`, `promotion_status/1`, and `status_values/0`. Validation accepts only the
fields named by D-08, returns field-specific safe errors, and uses the four D-07 statuses.
</interfaces>
</context>

<assumption_delta_decision>
Primary noun: route policy ownership
Decision: no-change
Rationale: This phase makes existing per-route ownership explicit rather than adding a second identity representation.
</assumption_delta_decision>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Validate one sanitized route from declaration to blocked promotion</name>
  <read_first>
    - AGENTS.md
    - .planning/phases/158-adoption-reset-and-route-map/158-CONTEXT.md
    - .planning/FIRST-B2C-ADOPTER-ROUTE-POLICY-MAP.md
    - .planning/todos/TODO-002-first-b2c-adopter-route-inputs.md
    - lib/crosswake/policy/schema.ex
    - lib/crosswake/policy/route.ex
  </read_first>
  <files>lib/crosswake/adoption/route_inventory.ex, test/crosswake/adoption/route_inventory_test.exs</files>
  <behavior>
    - "A synthetic row using an opaque ID, sanitized Phoenix path pattern, and explicit route-local fields validates without any adopter-specific value."
    - "The same row with one safety field set to `unknown_blocking` validates as recorded uncertainty but `promotion_status/1` returns an explicit blocked result."
    - "Omitted, nil, blank, unknown-key, forbidden-key, duplicate-ID, and duplicate-path cases fail with a stable rule ID and safe field/route reference only."
  </behavior>
  <action>Create `Crosswake.Adoption.RouteInventory` using the existing NimbleOptions discipline, not a runtime database or generic public inventory framework, per D-05, D-07, D-08, D-10, and D-20. Follow the assigned `lib/crosswake/policy/schema.ex` analog for closed named-field and enum validation, and the assigned `lib/crosswake/policy/route.ex` analog for normalized validated structs plus named cross-field checks. Define closed vocabularies for statuses, runtime owners, offline posture, mutation categories, staleness classes, auth levels, scope/logout/account-switch posture, media size bands, codec families, integrity posture, five fallback classes, entry/replay disablement, and queued-data retention. Make auth, recent-auth, scope, mutation, media, every fallback, disablement, and retention explicit on every concrete row per D-06; defaults may exist only for non-sensitive discovery posture. Implement `validate_inventory/1` collision detection and stable declaration-order output, and `promotion_status/1` so `unknown_blocking` returns a typed blocked result per D-01 through D-03. Use only synthetic opaque fixtures. Error tuples/messages must name a stable rule ID, safe route reference, field, and remediation without echoing rejected input per D-09, D-17, and D-19.</action>
  <verify>
    <automated>mix test test/crosswake/adoption/route_inventory_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - `Crosswake.Adoption.RouteInventory.status_values/0` returns exactly `[:confirmed_sanitized, :known_default, :unknown_blocking, :not_applicable]`.
    - The focused ExUnit file proves a valid synthetic row, blocked promotion, empty inventory, stable order, collisions, unknown keys, forbidden keys, and safe non-echoing errors.
    - No new dependency, database, external service, Android file, runtime replay code, or adopter-specific row is introduced.
  </acceptance_criteria>
  <done>One production-quality sanitized route path is validated end to end and remains visibly blocked whenever required adopter input is unknown.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Freeze the layered inventory contract and preserve the open input blocker</name>
  <read_first>
    - .planning/FIRST-B2C-ADOPTER-ROUTE-POLICY-MAP.md
    - .planning/todos/TODO-002-first-b2c-adopter-route-inputs.md
    - .planning/phases/158-adoption-reset-and-route-map/158-CONTEXT.md
    - lib/crosswake/adoption/route_inventory.ex
    - test/crosswake/adoption/route_inventory_test.exs
  </read_first>
  <files>.planning/FIRST-B2C-ADOPTER-ROUTE-POLICY-MAP.md, test/crosswake/adoption/route_inventory_test.exs</files>
  <behavior>
    - "The map distinguishes known product-surface defaults from the absent concrete-route inventory."
    - "Every required concrete-route safety field is explicitly route-local and the unresolved set is `unknown_blocking`."
    - "TODO-002 remains open because sanitized concrete route rows are not available."
  </behavior>
  <action>Update the route-policy map to describe the D-05 layered model, the D-07 status vocabulary, the D-08 allowlist, the D-09 exclusions, and the D-10 validator entry points. Add a concrete-route inventory section that truthfully states adopter-instance completeness is blocked, without adding a guessed row. State that D-03 prevents host-proof or physical-device promotion until required rows validate. Preserve the compact known-surface ownership table and D-04 web-only Alpha pause rule. Extend the test to assert the document and validator share the required vocabulary and that TODO-002's frontmatter still says `status: open`.</action>
  <verify>
    <automated>mix test test/crosswake/adoption/route_inventory_test.exs test/crosswake/planning/first_adopter_context_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - The route-policy map contains the four closed statuses and identifies each safety-critical field as route-local.
    - The map contains no fabricated concrete adopter route row and explicitly records adopter-instance completeness as blocked.
    - `.planning/todos/TODO-002-first-b2c-adopter-route-inputs.md` remains open and unchanged except for no required edit.
  </acceptance_criteria>
  <done>The policy contract is complete, the instance inventory is honestly blocked, and later proof cannot mistake defaults for confirmed route facts.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Planning input -> durable route row | Potentially sensitive or unconfirmed product facts cross into repository truth. |
| Surface default -> concrete Phoenix route | Discovery defaults could be mistaken for route-local safety authority. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-158-01 | Information Disclosure | durable route inventory | high | mitigate | D-08/D-09 allowlist, forbidden-key rejection, synthetic fixtures, and non-echoing errors block private-term leakage. |
| T-158-02 | Tampering / Elevation of Privilege | promotion status | high | mitigate | Any `unknown_blocking` safety field returns blocked promotion; no optimistic fallback exists. |
| T-158-03 | Spoofing / Tampering | family defaults | high | mitigate | D-06 route-local fields are required and cannot inherit from product-surface defaults. |
| T-158-04 | Information Disclosure | public-guide naming | high | mitigate | Public phrase enforcement is completed in Plan 03 before phase completion. |
| T-158-05 | Information Disclosure | validation errors | high | mitigate | Errors contain stable rule IDs plus safe paths/fields only and tests use a synthetic secret canary. |

ASVS Level 1: all high-severity rows are blocking and are tied to automated tests.
</threat_model>

<verification>
Run `mix test test/crosswake/adoption/route_inventory_test.exs` after each task and the Phase 158
quick command after the plan; feedback remains under the 120-second validation budget.
</verification>

<success_criteria>
- RESET-02 has a closed executable contract and an honest `unknown_blocking` instance state.
- RESET-04 exclusions are enforced at the route-row boundary without echoing rejected content.
- TODO-002 remains open and no out-of-scope runtime feature is implemented.
</success_criteria>

## Artifacts this phase produces

- `lib/crosswake/adoption/route_inventory.ex`
  - module `Crosswake.Adoption.RouteInventory`
  - functions `validate/1`, `validate!/1`, `validate_inventory/1`, `promotion_status/1`, `status_values/0`
  - closed row fields and enum vocabularies required by D-07/D-08
- `test/crosswake/adoption/route_inventory_test.exs`
  - module `Crosswake.Adoption.RouteInventoryTest`
- `.planning/FIRST-B2C-ADOPTER-ROUTE-POLICY-MAP.md`
  - layered default/concrete-route contract and explicit instance blocker

<output>
Create `.planning/phases/158-adoption-reset-and-route-map/158-01-SUMMARY.md` when done.
</output>
