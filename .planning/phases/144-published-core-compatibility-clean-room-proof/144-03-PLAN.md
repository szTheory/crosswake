---
phase: 144-published-core-compatibility-clean-room-proof
plan: 03
type: execute
wave: 3
depends_on:
  - 144-01
  - 144-02
files_modified:
  - script/check_release_workflow_integrity.exs
  - test/crosswake/proof/phase142_release_integrity_test.exs
autonomous: true
requirements:
  - PREF-03
user_setup: []
must_haves:
  truths:
    - "PREF-03: merge-blocking static proof fails aggregate behavioral gates, proof-before-publish order, native proof cascades, missing mirror-token preflight, missing queue max, and cancel-in-progress true (D-10, D-13)."
    - "PREF-03: static proof fails stale or locally derived clean-room dependency floors, missing exact pin, missing lockfile postconditions, and incomplete package matrix coverage (D-10, D-13, D-15, D-16)."
    - "PREF-03: `script/check_release_workflow_integrity.exs` plus ExUnit remain authoritative; no YAML parser or actionlint replacement is introduced (D-10, D-11, D-12)."
    - "PREF-03: failure IDs and messages are stable, actionable, and fixture-proven against real workflow/script text mutations (D-14)."
  artifacts:
    - path: "script/check_release_workflow_integrity.exs"
      provides: "Consolidated Phase 144 PREF-03 semantic scanner"
      exports: ["release.workflow.aggregate_gate.behavioral_jobs_absent", "release.workflow.proof_after_publish", "release.workflow.native_proof_decoupled", "release.workflow.mirror_token_preflight", "release.workflow.concurrency_queue_max", "release.workflow.no_cancel_in_progress_true", "release.cleanroom.package_matrix_complete", "release.workflow.companion_floors_honest", "release.workflow.doctor_proof_unmasked"]
    - path: "test/crosswake/proof/phase142_release_integrity_test.exs"
      provides: "Adversarial Phase 144 release-integrity fixtures"
      contains: "phase144_release_integrity"
  key_links:
    - from: "script/check_release_workflow_integrity.exs"
      to: ".github/workflows/release-please.yml"
      via: "scanner validates release workflow gates, proof order, native decoupling, mirror preflight, and concurrency"
      pattern: "release\\.workflow\\."
    - from: "test/crosswake/proof/phase142_release_integrity_test.exs"
      to: "script/check_release_workflow_integrity.exs"
      via: "ExUnit mutates real text and asserts stable scanner IDs fail"
      pattern: "phase144_release_integrity"
---

<objective>
Close Phase 144 with a merge-blocking static proof that fails on the complete PREF-03 regression set.

Purpose: PREF-03 should not rely on reviewers remembering release footguns. This plan consolidates the clean-room, doctor, workflow, package-matrix, mirror-preflight, and concurrency invariants into stable scanner IDs with adversarial ExUnit fixtures.
Output: extended `script/check_release_workflow_integrity.exs` and `test/crosswake/proof/phase142_release_integrity_test.exs` coverage. Covers D-10 through D-16 plus D-13's full regression list; it depends on Plan 01 clean-room contract decisions and Plan 02 doctor proof decisions.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/144-published-core-compatibility-clean-room-proof/144-CONTEXT.md
@.planning/phases/144-published-core-compatibility-clean-room-proof/144-RESEARCH.md
@.planning/phases/144-published-core-compatibility-clean-room-proof/144-VALIDATION.md
@.planning/phases/144-published-core-compatibility-clean-room-proof/144-PATTERNS.md
@.planning/phases/142-release-graph-governance-contract/142-02-SUMMARY.md
@.planning/phases/143-guarded-auto-publish-train/143-03-SUMMARY.md
@.planning/phases/144-published-core-compatibility-clean-room-proof/144-01-SUMMARY.md
@.planning/phases/144-published-core-compatibility-clean-room-proof/144-02-SUMMARY.md
@script/check_release_workflow_integrity.exs
@test/crosswake/proof/phase142_release_integrity_test.exs
@script/verify_companion_cleanroom.sh
@lib/mix/tasks/crosswake.doctor.ex
@.github/workflows/release-please.yml
@guides/companion_compatibility.md
@packages/crosswake_rulestead/mix.exs
@packages/crosswake_rindle/mix.exs
@packages/crosswake_sigra/mix.exs
@packages/crosswake_chimeway/mix.exs
@packages/crosswake_threadline/mix.exs
</context>

## Artifacts This Phase Produces

- Scanner check IDs:
  - `release.workflow.aggregate_gate.behavioral_jobs_absent`
  - `release.workflow.proof_after_publish`
  - `release.workflow.native_proof_decoupled`
  - `release.workflow.mirror_token_preflight`
  - `release.workflow.concurrency_queue_max`
  - `release.workflow.no_cancel_in_progress_true`
  - `release.cleanroom.package_matrix_complete`
  - `release.workflow.companion_floors_honest`
  - `release.workflow.doctor_proof_unmasked`
- ExUnit tag: `:phase144_release_integrity`.
- Fixture env overrides used by tests: `RELEASE_WORKFLOW_PATH`, `CLEANROOM_SCRIPT_PATH`, `DOCTOR_TASK_PATH`, `COMPANION_MIX_ROOT`.

<source_audit>
SOURCE | ID | Feature/Requirement | Plan | Status | Notes
GOAL | phase-144 | Repair clean-room harness, use exact companion versions and derived core floors, and prove doctor can load fresh routers | 01,02,03 | COVERED | Plan 03 adds the final merge-blocking static proof over prior decisions.
REQ | PREF-01 | Exact companion version and Hex-derived core floor | 01,03 | COVERED | Plan 01 implements; Plan 03 guards regressions.
REQ | PREF-02 | Doctor fresh-router load proof | 02,03 | COVERED | Plan 02 implements; Plan 03 guards masking.
REQ | PREF-03 | Merge-blocking static test fails aggregate gates, stale floors, proof cascades, missing mirror preflight | 03 | COVERED | This plan owns full PREF-03.
RESEARCH | scanner-authority | Keep script/check_release_workflow_integrity.exs plus ExUnit authoritative | 03 | COVERED | No parser/actionlint replacement.
RESEARCH | negative-fixtures | Mutate real workflow/script text and assert stable IDs | 03 | COVERED | Task 2 adds fixture suite.
RESEARCH | package-matrix | All five packages covered, including mixed floors and observer profile | 01,03 | COVERED | Plan 03 asserts complete matrix.
CONTEXT | D-01..D-05 | Hex metadata floor, exact pin, fail-closed states, lockfile postconditions, mixed floors | 01,03 | COVERED | Plan 01 implements; Plan 03 prevents regression.
CONTEXT | D-06..D-09 | Doctor-owned readiness and router diagnostics | 02,03 | COVERED | Plan 02 implements; Plan 03 prevents masking.
CONTEXT | D-10 | Existing scanner plus ExUnit authoritative | 03 | COVERED | Task 1/2 extend same files.
CONTEXT | D-11 | No YAML parser unless current scanner cannot express invariants | 03 | COVERED | Task 1 keeps dependency-free scanner.
CONTEXT | D-12 | Actionlint advisory only, not semantic replacement | 03 | COVERED | Task 1 keeps scanner authoritative.
CONTEXT | D-13 | Fail on aggregate gates, stale/local floors, proof cascades/order, package matrix, mirror preflight, doctor masking, queue max, cancel true | 03 | COVERED | Task 1/2 cover the full list.
CONTEXT | D-14 | Stable IDs and actionable messages | 03 | COVERED | Task 1 IDs and Task 2 fixtures.
CONTEXT | D-15..D-18 | All five packages and absence semantics | 01,03 | COVERED | Plan 03 matrix check plus Plan 01 profiles.
CONTEXT | D-19..D-21 | Calm logs, useful failure copy, boring proof path | 01,02,03 | COVERED | Prior plans shape output; Plan 03 verifies non-masked proof path.
</source_audit>

<must_haves>
- Existing Phase 142/143 IDs stay green; Phase 144 adds stable IDs rather than replacing history.
- PREF-03 scanner checks are based on non-comment semantic source text and cannot pass from comments or step-label decoys.
- Negative fixtures cover aggregate gate, stale/local floors, proof order/cascade, package matrix, mirror preflight, doctor proof masking, `queue: max`, and `cancel-in-progress: true`.
- No schema push, native backfill, release-status completion, dashboard UI, or new parser dependency enters this phase.
</must_haves>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend scanner with consolidated PREF-03 IDs</name>
  <files>script/check_release_workflow_integrity.exs</files>
  <read_first>
    - `script/check_release_workflow_integrity.exs`
    - `script/verify_companion_cleanroom.sh`
    - `lib/mix/tasks/crosswake.doctor.ex`
    - `.github/workflows/release-please.yml`
    - `guides/companion_compatibility.md`
    - `packages/crosswake_rulestead/mix.exs`
    - `packages/crosswake_rindle/mix.exs`
    - `packages/crosswake_sigra/mix.exs`
    - `packages/crosswake_chimeway/mix.exs`
    - `packages/crosswake_threadline/mix.exs`
  </read_first>
  <behavior>
    - Scanner passes on the current source and prints OK for every Phase 144 PREF-03 ID listed in this plan's artifact section.
    - Scanner preserves existing Phase 142/143 IDs while adding Phase 144 aliases/umbrella checks for workflow gate, proof order, native decoupling, mirror preflight, concurrency, package matrix, floor truth, and doctor proof masking.
    - Scanner ignores full-line comments for required and forbidden invariants.
  </behavior>
  <action>
    Extend `script/check_release_workflow_integrity.exs` in the existing dependency-free style. Do not add a YAML parser or actionlint dependency per D-10, D-11, and D-12. Continue using source reads, job-block extraction, comment stripping, and stable `check/3` tuples.

    Add exact Phase 144 IDs: `release.workflow.aggregate_gate.behavioral_jobs_absent`, `release.workflow.proof_after_publish`, `release.workflow.native_proof_decoupled`, `release.workflow.mirror_token_preflight`, `release.workflow.concurrency_queue_max`, `release.workflow.no_cancel_in_progress_true`, `release.cleanroom.package_matrix_complete`, `release.workflow.companion_floors_honest`, and `release.workflow.doctor_proof_unmasked`. These may wrap existing checks where the existing check already proves the same behavior, but they must emit their own OK/FAIL lines and actionable details.

    `release.cleanroom.package_matrix_complete` must require all five clean-room proof jobs in `.github/workflows/release-please.yml`, all five package names in `script/verify_companion_cleanroom.sh`, correct package/version arguments from Release Please outputs, chimeway's no-Sigra boundary, threadline's no-Sigra/no-Chimeway observer boundary, and threadline not registered under `:companions`. `release.workflow.companion_floors_honest` must preserve rulestead/rindle `~> 0.1` and sigra/chimeway/threadline `~> 0.2` by reading package `mix.exs` files; the proof must not accept a single normalized floor. `release.workflow.doctor_proof_unmasked` must combine Plan 02 doctor task and clean-room script checks so the static scanner fails if the doctor loses app config readiness or the script preloads the router before doctor.
  </action>
  <acceptance_criteria>
    - `elixir script/check_release_workflow_integrity.exs` prints OK for every Phase 144 PREF-03 ID listed in this plan.
    - Existing IDs including `release.concurrency.queue_max`, `release.concurrency.no_true_cancellation`, `release.aggregate_gate.behavioral_jobs_absent`, `release.cleanup.after_publish_and_proof`, `release.mirror_token.preflight`, and `release.version_graph.companion_floors_honest` still print OK.
    - Scanner code remains dependency-free and does not introduce YAML parser package usage.
    - Scanner details name the file/command to inspect or rerun.
  </acceptance_criteria>
  <verify>
    <automated>elixir script/check_release_workflow_integrity.exs</automated>
  </verify>
  <done>Scanner emits stable Phase 144 PREF-03 IDs covering the full release-integrity regression surface while preserving prior release-governance checks.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add adversarial PREF-03 ExUnit fixtures</name>
  <files>test/crosswake/proof/phase142_release_integrity_test.exs</files>
  <read_first>
    - `test/crosswake/proof/phase142_release_integrity_test.exs`
    - `script/check_release_workflow_integrity.exs`
    - `script/verify_companion_cleanroom.sh`
    - `lib/mix/tasks/crosswake.doctor.ex`
    - `.github/workflows/release-please.yml`
    - `guides/companion_compatibility.md`
  </read_first>
  <behavior>
    - Positive proof asserts scanner output includes all Phase 144 PREF-03 IDs.
    - Negative fixtures mutate real source text and fail on aggregate behavioral gates, local/stale dependency floors, missing exact pin, missing lockfile assertion, proof-before-publish, native proof cascades, missing package member, missing mirror token preflight, doctor proof masking, missing `queue: max`, and `cancel-in-progress: true`.
    - Comment-only and step-text decoys cannot satisfy required invariants.
  </behavior>
  <action>
    Extend `test/crosswake/proof/phase142_release_integrity_test.exs` with `@phase144_release_integrity_ids` and `@tag :phase144_release_integrity`. Reuse `run_fixture_set/1` and existing fixture env helpers; add any missing env names from Plans 01 and 02 (`CLEANROOM_SCRIPT_PATH`, `DOCTOR_TASK_PATH`, `COMPANION_MIX_ROOT`) without duplicating scanner logic in the tests.

    Add one positive test asserting every Phase 144 PREF-03 ID reports `[crosswake] OK:`. Add negative fixture tests for these exact regression classes per D-13 and D-14: aggregate `releases_created` used by a behavioral job; local package source or docs used as clean-room floor authority; exact companion pin weakened from `== ${VERSION}`; lockfile postcondition removed; clean-room proof job allowed to run without its matching `publish-hex-*` need; iOS proof made dependent on Android proof or Android proof made dependent on iOS proof; mirror-token preflight removed from `publish-ios-core`; one of the five package proof jobs or script package profiles removed; `Code.ensure_loaded?(CleanRoomHost.Router)` preloaded before doctor; `queue: max` removed; `cancel-in-progress: true` introduced. Keep fixtures as temp files and assert the intended check ID for each failure.
  </action>
  <acceptance_criteria>
    - `mix test test/crosswake/proof/phase142_release_integrity_test.exs --only phase144_release_integrity` passes.
    - Full `mix test test/crosswake/proof/phase142_release_integrity_test.exs` passes with Phase 142, 143, and 144 proof together.
    - Every PREF-03 regression class named in the behavior block has a negative fixture asserting a stable check ID.
    - Tests do not rely on live Hex or GitHub network access.
  </acceptance_criteria>
  <verify>
    <automated>bash -lc 'mix test test/crosswake/proof/phase142_release_integrity_test.exs --only phase144_release_integrity && mix test test/crosswake/proof/phase142_release_integrity_test.exs'</automated>
  </verify>
  <done>ExUnit fixtures prove PREF-03 guardrails against real text mutations and keep release integrity merge-blocking without live network dependency.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Workflow source to scanner | Release DAG semantics are inferred from repository text before GitHub executes it. |
| Clean-room script source to scanner | Dependency proof authority and router proof masking are validated from script text. |
| Package source/docs to scanner | Compatibility floors and package-family membership are checked against known package surfaces. |
| Scanner output to CI/operator | Stable IDs decide whether release-integrity proof passes or blocks. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-144-11 | Elevation of Privilege | aggregate Release Please gate | mitigate | Phase 144 scanner ID fails any behavioral job that uses aggregate release authority per D-13. |
| T-144-12 | Tampering | proof job order | mitigate | Scanner and fixtures require every companion proof to need matching publish job success per D-13 and D-16. |
| T-144-13 | Denial of Service | release workflow concurrency | mitigate | Scanner fails missing `queue: max` and any `cancel-in-progress: true` regression per D-13. |
| T-144-14 | Tampering | clean-room dependency floors | mitigate | Scanner fails local/stale floor authority and requires Hex metadata plus lockfile postconditions from Plan 01 per D-01 through D-05. |
| T-144-15 | Tampering | package matrix | mitigate | Scanner requires all five companion/observer packages and their absence/profile rules per D-15 through D-18. |
| T-144-16 | Information Disclosure | mirror-token preflight | mitigate | Scanner preserves preflight structure without printing token values; Phase 145 owns live mirror recovery. |
| T-144-17 | Spoofing | doctor proof masking | mitigate | Scanner fails script-side router preloads and requires doctor-owned readiness from Plan 02 per D-07. |
| T-144-SC | Tampering | package-manager installs | accept | No npm/pip/cargo installs are added; scanner/test work uses Elixir stdlib and ExUnit. |
</threat_model>

<verification>
Run the final Phase 144 proof:
- `elixir script/check_release_workflow_integrity.exs`
- `mix test test/crosswake/proof/phase142_release_integrity_test.exs --only phase144_release_integrity`
- `mix test test/crosswake/proof/phase142_release_integrity_test.exs`
- `mix test test/mix/tasks/crosswake_doctor_router_test.exs`

Recommended final gate:
- `mix verify`
</verification>

<success_criteria>
- PREF-03 has a deterministic merge-blocking proof with stable IDs for the complete regression list.
- Phase 144 does not broaden into Phase 145 mirror/backfill execution, Phase 146 release-status DX, schema push work, or product breadth.
- All PREF-01, PREF-02, and PREF-03 requirement IDs are covered across the three plans.
</success_criteria>

<output>
Create `.planning/phases/144-published-core-compatibility-clean-room-proof/144-03-SUMMARY.md` when done.
</output>
