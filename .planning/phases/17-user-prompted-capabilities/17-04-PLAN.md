---
phase: "17-user-prompted-capabilities"
plan: "04"
type: execute
wave: 3
depends_on:
  - "17-01"
  - "17-02"
files_modified:
  - "examples/android_shell_host/app/src/main/java/dev/crosswake/shell/BridgeChannel.kt"
  - "examples/android_shell_host/app/src/main/java/dev/crosswake/shell/LiveViewFragment.kt"
  - "examples/android_shell_host/app/src/main/java/dev/crosswake/shell/MainActivity.kt"
  - "examples/android_shell_host/app/src/main/java/dev/crosswake/shell/NotificationTokenProvider.kt"
  - "examples/android_shell_host/app/src/main/java/dev/crosswake/shell/FilePickerCoordinator.kt"
  - "examples/android_shell_host/app/src/main/java/dev/crosswake/shell/transfer/TransferCoordinator.kt"
  - "examples/android_shell_host/app/src/test/java/dev/crosswake/shell/BridgeChannelTest.kt"
autonomous: true
requirements:
  - "CAP-NOTIFTOKEN"
  - "CAP-FILEPICKER"
must_haves:
  truths:
    - "The Android shell fails closed on `notification_token` when provider or authorization prerequisites are missing, instead of inventing a generic platform token per D-01 through D-09."
    - "The Android shell runs `files.pick` only through declared `native_picker` transfer seams and stages copy-first content before Phoenix sees it per D-10 through D-21."
    - "Android tests exist for both capabilities even though local verification is currently blocked by missing Java in this environment, so final proof must run in CI or a Java-enabled workstation."
  artifacts:
    - path: "examples/android_shell_host/app/src/main/java/dev/crosswake/shell/NotificationTokenProvider.kt"
      provides: "Android token provider seam with explicit provider-prerequisite denial posture"
    - path: "examples/android_shell_host/app/src/main/java/dev/crosswake/shell/FilePickerCoordinator.kt"
      provides: "Android picker coordinator that stages import-copy handles for transfer-bound flows"
    - path: "examples/android_shell_host/app/src/test/java/dev/crosswake/shell/BridgeChannelTest.kt"
      provides: "Android bridge tests for prompt-free token and transfer-bound picker behavior"
  key_links:
    - from: "examples/android_shell_host/app/src/main/java/dev/crosswake/shell/LiveViewFragment.kt"
      to: "examples/android_shell_host/app/src/main/java/dev/crosswake/shell/NotificationTokenProvider.kt"
      via: "bridge injection resolves token evidence through the provider seam"
      pattern: "notification_token|provider|status"
    - from: "examples/android_shell_host/app/src/main/java/dev/crosswake/shell/MainActivity.kt"
      to: "examples/android_shell_host/app/src/main/java/dev/crosswake/shell/FilePickerCoordinator.kt"
      via: "activity result launches picker staging and returns typed results to the bridge"
      pattern: "GET_CONTENT|transfer_id|items|cancel"
---

<objective>
Implement the Android shell handlers and tests for `notification_token` and `file_picker`.

Purpose: Android carries the hardest support-truth edge in Phase 17 because token retrieval is provider-sensitive and local verification is blocked by missing Java, so the plan has to keep both execution scope and proof posture explicit.
Output: Android provider and picker coordinators, bridge wiring, staged-handle transfer integration, and test coverage with an explicit local verification constraint.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/17-user-prompted-capabilities/17-CONTEXT.md
@.planning/phases/17-user-prompted-capabilities/17-RESEARCH.md
@.planning/phases/17-user-prompted-capabilities/17-PATTERNS.md
@.planning/phases/17-user-prompted-capabilities/17-01-PLAN.md
@.planning/phases/17-user-prompted-capabilities/17-02-PLAN.md
@examples/android_shell_host/app/src/main/java/dev/crosswake/shell/BridgeChannel.kt
@examples/android_shell_host/app/src/main/java/dev/crosswake/shell/LiveViewFragment.kt
@examples/android_shell_host/app/src/main/java/dev/crosswake/shell/transfer/TransferCoordinator.kt

<interfaces>
From examples/android_shell_host/app/src/main/java/dev/crosswake/shell/BridgeChannel.kt:
```kotlin
private val permissionStatusProvider: (String) -> Map<String, String>?
private val filesPickHandler: (Map<String, String>) -> Map<String, String>
```

From examples/android_shell_host/app/src/main/java/dev/crosswake/shell/transfer/TransferCoordinator.kt:
```kotlin
fun execute(command: String, payload: Map<String, String>, correlationId: String): Map<String, String>?
```

From examples/android_shell_host/app/src/main/java/dev/crosswake/shell/LiveViewFragment.kt:
```kotlin
filesPickHandler = { payload -> payload }
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add Android notification-token provider and fail-closed bridge behavior</name>
  <files>examples/android_shell_host/app/src/main/java/dev/crosswake/shell/BridgeChannel.kt, examples/android_shell_host/app/src/main/java/dev/crosswake/shell/LiveViewFragment.kt, examples/android_shell_host/app/src/main/java/dev/crosswake/shell/NotificationTokenProvider.kt, examples/android_shell_host/app/src/test/java/dev/crosswake/shell/BridgeChannelTest.kt</files>
  <behavior>
    - Test 1: `notification_token` returns a typed success payload only when the configured provider snapshot is available and authorization prerequisites are satisfied per D-01 through D-06.
    - Test 2: Missing provider setup or token availability denies explicitly instead of synthesizing a fake platform-neutral Android token per D-04 and research guidance.
    - Test 3: Success remains evidence-only and does not imply backend registration or delivery readiness per D-07 and D-09.
  </behavior>
  <action>Add an Android `NotificationTokenProvider` seam that makes provider-sensitive token retrieval explicit. Use the current support truth from research: do not invent a generic Android push token surface in core, and deny when the configured provider prerequisites are absent. Reuse the existing `permissions.status` notifications signal for normalized status, wire the provider into bridge dispatch, and keep the command prompt-free and one-shot per D-01 through D-09. The success payload must include explicit provider identity so the Android companion-backed FCM seam remains honest and distinct from the iOS APNs floor.</action>
  <verify>
    <automated>java -version && (cd examples/android_shell_host && ./gradlew test --tests dev.crosswake.shell.BridgeChannelTest)</automated>
  </verify>
  <done>The Android shell exposes `notification_token` only through an explicit provider-aware seam and returns typed denials when prerequisites are missing.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement Android transfer-bound picker staging and record the local verification constraint</name>
  <files>examples/android_shell_host/app/src/main/java/dev/crosswake/shell/BridgeChannel.kt, examples/android_shell_host/app/src/main/java/dev/crosswake/shell/LiveViewFragment.kt, examples/android_shell_host/app/src/main/java/dev/crosswake/shell/MainActivity.kt, examples/android_shell_host/app/src/main/java/dev/crosswake/shell/FilePickerCoordinator.kt, examples/android_shell_host/app/src/main/java/dev/crosswake/shell/transfer/TransferCoordinator.kt, examples/android_shell_host/app/src/test/java/dev/crosswake/shell/BridgeChannelTest.kt</files>
  <behavior>
    - Test 1: `files.pick` launches `ACTION_GET_CONTENT` for declared inbound `native_picker` transfer seams and stages app-controlled copies before returning handles per D-10 through D-15.
    - Test 2: The public reply uses `transfer_id` plus `items` and a distinct cancel outcome, not raw URIs or empty-success cancellation per D-16 through D-20.
    - Test 3: Plan execution records that local Android verification is blocked until Java is installed, while keeping the automated Gradle command as the canonical proof step.
  </behavior>
  <action>Create an Android `FilePickerCoordinator` that uses `ACTION_GET_CONTENT` for the first import-copy flow and immediately stages content into app-controlled storage before returning opaque handles. Wire the activity result path through `MainActivity`, `LiveViewFragment`, `BridgeChannel`, and `TransferCoordinator` so picker execution stays transfer-bound and copy-first per D-10 through D-21. Do not use `ACTION_OPEN_DOCUMENT`, persistable grants, raw content URIs as public handles, or durable reopen-later semantics. Keep the plan and summary explicit that `java -version` currently fails in this local environment, so Android verification must be completed in a Java-enabled environment or CI after implementation.</action>
  <verify>
    <automated>java -version && (cd examples/android_shell_host && ./gradlew test --tests dev.crosswake.shell.BridgeChannelTest)</automated>
  </verify>
  <done>The Android shell enforces transfer-bound copy-first picker behavior, and the execution record preserves the current local Java blocker instead of pretending verification already passed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Android provider/config -> notification-token reply | Provider-specific token state crosses into a core bridge reply here and must stay explicitly scoped. |
| Android content URI -> staged handle | External document-provider content crosses into app staging here and must not leak raw durable authority. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-10 | S | Android token provider seam | mitigate | Deny when provider prerequisites are absent instead of spoofing a platform-neutral token surface. |
| T-17-11 | T | picker staging path | mitigate | Use `ACTION_GET_CONTENT` plus immediate app-controlled copy staging and never expose raw URIs as stable public handles. |
| T-17-12 | R | verification posture | mitigate | Keep the canonical Gradle test command in the plan and explicitly record the missing-Java local blocker so proof claims stay honest. |
</threat_model>

<verification>
Canonical Android proof command: `java -version && (cd examples/android_shell_host && ./gradlew test --tests dev.crosswake.shell.BridgeChannelTest)`. In this local environment the `java -version` preflight is currently expected to fail because Java is missing, so execution must preserve that blocker and complete the Gradle verification in CI or a Java-enabled workstation before Phase 17 can be treated as fully verified.
</verification>

<success_criteria>
The checked-in Android shell has explicit provider-aware token handling, copy-first transfer-bound picker staging, and test coverage whose local verification blocker is recorded honestly.
</success_criteria>

<output>
After completion, create `.planning/phases/17-user-prompted-capabilities/17-04-SUMMARY.md`
</output>
