---
phase: "17-user-prompted-capabilities"
plan: "02"
type: execute
wave: 2
depends_on:
  - "17-01"
files_modified:
  - "lib/crosswake/bridge/commands/file_picker.ex"
  - "lib/crosswake/bridge/registry.ex"
  - "lib/crosswake/transfer/contracts.ex"
  - "lib/crosswake/manifest/builder.ex"
  - "guides/capabilities.md"
  - "guides/support_matrix.md"
  - "test/support/router_fixtures.ex"
  - "test/crosswake/bridge/registry_test.exs"
  - "test/crosswake/manifest/manifest_test.exs"
  - "test/crosswake/transfer/contracts_test.exs"
autonomous: true
requirements:
  - "CAP-FILEPICKER"
must_haves:
  truths:
    - "Public `file_picker` invocation is bound to a declared route-local `transfer_id`, not ambient filesystem authority, per D-10 through D-15."
    - "Successful picker replies return `transfer_id` plus `items` with normalized metadata fields and typed cancellation behavior per D-16 through D-21."
    - "Manifest and public support truth describe the picker as copy-first, transfer-verified, and low-frequency rather than durable access authority."
  artifacts:
    - path: "lib/crosswake/bridge/commands/file_picker.ex"
      provides: "Typed request and response structs for the file-picker bridge contract"
    - path: "lib/crosswake/transfer/contracts.ex"
      provides: "Transfer-bound validation rules that gate picker-backed flows"
    - path: "test/support/router_fixtures.ex"
      provides: "Route fixtures proving `source: :native_picker` transfer declarations for file-picker flows"
  key_links:
    - from: "lib/crosswake/bridge/registry.ex"
      to: "lib/crosswake/transfer/contracts.ex"
      via: "files.pick resolves only through transfer-bound native_picker declarations"
      pattern: "files.pick|native_picker|transfer_id"
    - from: "lib/crosswake/manifest/builder.ex"
      to: "guides/support_matrix.md"
      via: "public picker support row matches the staged-handle and verification posture"
      pattern: "file_picker|copy|verification"
---

<objective>
Define the Elixir-side `file_picker` bridge contract and bind it to transfer seams instead of ambient file authority.

Purpose: Phase 17 only works if `files.pick` stays subordinate to declared import or upload-preparation seams; otherwise Crosswake would silently publish persistent file authority it does not own.
Output: Typed picker payloads, transfer-bound registry rules, route fixtures, manifest/support truth, and tests that enforce copy-first bounded behavior.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/17-user-prompted-capabilities/17-CONTEXT.md
@.planning/phases/17-user-prompted-capabilities/17-RESEARCH.md
@.planning/phases/17-user-prompted-capabilities/17-PATTERNS.md
@.planning/phases/17-user-prompted-capabilities/17-01-PLAN.md
@lib/crosswake/bridge/registry.ex
@lib/crosswake/transfer/contracts.ex
@lib/crosswake/manifest/builder.ex
@test/support/router_fixtures.ex

<interfaces>
From lib/crosswake/transfer/contracts.ex:
```elixir
defmodule Declaration do
  @enforce_keys [:protocol, :version, :id, :intent, :direction, :verification]
  defstruct [:protocol, :version, :id, :intent, :direction, :source, :destination, :verification, media_types: []]
end
```

From lib/crosswake/bridge/registry.ex:
```elixir
@transfer_commands %{
  "transfer.import" => :import,
  "transfer.export" => :export,
  "transfer.download" => :download,
  "transfer.upload.prepare" => :upload
}

defp transfer_entry(route, command, transfer_intent) do
  case Enum.find(route.transfers, &match_transfer_command?(&1, transfer_intent)) do
```

From test/support/router_fixtures.ex:
```elixir
[
  id: :lesson_import,
  intent: :import,
  source: :native_picker,
  verification: :required,
  media_types: ["application/pdf"]
]
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add the transfer-bound file-picker contract and registry gate</name>
  <files>lib/crosswake/bridge/commands/file_picker.ex, lib/crosswake/bridge/registry.ex, lib/crosswake/transfer/contracts.ex, test/crosswake/bridge/registry_test.exs, test/crosswake/transfer/contracts_test.exs</files>
  <behavior>
    - Test 1: `files.pick` requires a declared `transfer_id` whose transfer seam is inbound and `source: :native_picker` per D-10 and D-11.
    - Test 2: Successful replies use one stable top-level shape with `transfer_id` and `items`, while cancellation is modeled as a distinct typed outcome per D-16 through D-20.
    - Test 3: MIME filters remain advisory at request time and real validation stays on the transfer seam per D-14.
  </behavior>
  <action>Create a dedicated `Crosswake.Bridge.Commands.FilePicker` module for the public `file_picker` semantics while keeping the wire command as `files.pick`. Update registry lookup so the command resolves through a declared `transfer_id` and an inbound `source: :native_picker` transfer seam, not a free-standing route capability, per D-10 through D-15. Extend transfer validation only as far as needed to encode this bounded seam and typed picker outcomes; do not add persistent-access, directory, or reopen-later semantics per D-12 and D-21.</action>
  <verify>
    <automated>mix test test/crosswake/bridge/registry_test.exs test/crosswake/transfer/contracts_test.exs</automated>
  </verify>
  <done>`files.pick` is governed by a typed Elixir contract and transfer-bound registry gate that rejects ambient or out-of-scope file authority.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Align route fixtures, manifest truth, and support wording to the copy-first picker seam</name>
  <files>lib/crosswake/manifest/builder.ex, guides/capabilities.md, guides/support_matrix.md, test/support/router_fixtures.ex, test/crosswake/manifest/manifest_test.exs</files>
  <behavior>
    - Test 1: Manifest output and route fixtures expose picker-backed transfers through `source: :native_picker` seams, not a broad filesystem capability.
    - Test 2: Public docs describe picker output as staged-handle evidence with nullable metadata and transfer verification, not durable access.
    - Test 3: Rebuild and fallback language stays explicit while broader doctor/support expansion remains deferred to Phase 18.
  </behavior>
  <action>Update route fixtures, manifest metadata, and only the directly coupled guide/support rows needed to keep `file_picker` truthful once the Elixir contract ships. Preserve the copy-first, transfer-verified posture per D-13 through D-21, including stable `items` shape, nullable metadata truth, and explicit cancellation semantics. Do not widen into generic browsing, directory selection, or persistent authority language, and do not absorb Phase 18 doctor or broad support-matrix expansion work into this plan.</action>
  <verify>
    <automated>mix test test/crosswake/manifest/manifest_test.exs && rg -n "file_picker|native_picker|copy|verification|items" guides/capabilities.md guides/support_matrix.md lib/crosswake/manifest/builder.ex test/support/router_fixtures.ex</automated>
  </verify>
  <done>The picker seam is reflected consistently across fixtures, manifest truth, and public support wording without implying ambient file access or persistent grants.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Phoenix route -> picker command | Route-owned code can request a picker here, so transfer-bound gating must prevent ambient file browsing authority. |
| Native handle metadata -> Phoenix transfer seam | Unverified MIME/type and provider metadata crosses here and must stay advisory until transfer verification completes. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-04 | E | files.pick registry path | mitigate | Resolve picker requests only through declared inbound `native_picker` transfer seams with explicit `transfer_id`. |
| T-17-05 | T | picker metadata contract | mitigate | Keep metadata normalized and nullable, and preserve transfer-side verification instead of trusting picker filters or provider MIME claims. |
| T-17-06 | I | support wording | mitigate | Update manifest/support wording to describe copy-first staged handles and avoid durable-access or persistent-authority claims. |
</threat_model>

<verification>
Run `mix test test/crosswake/bridge/registry_test.exs test/crosswake/transfer/contracts_test.exs test/crosswake/manifest/manifest_test.exs`. Confirm fixture and guide text keeps `file_picker` transfer-bound, copy-first, and verification-backed.
</verification>

<success_criteria>
Crosswake exposes `file_picker` as a typed transfer-bound bridge seam whose manifest truth, fixtures, and public wording all reject ambient file authority.
</success_criteria>

<output>
After completion, create `.planning/phases/17-user-prompted-capabilities/17-02-SUMMARY.md`
</output>
