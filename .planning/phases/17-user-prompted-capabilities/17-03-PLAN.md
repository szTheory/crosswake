---
phase: "17-user-prompted-capabilities"
plan: "03"
type: execute
wave: 3
depends_on:
  - "17-01"
  - "17-02"
files_modified:
  - "examples/ios_shell_host/CrosswakeShell/BridgeChannel.swift"
  - "examples/ios_shell_host/CrosswakeShell/LiveViewContainerViewController.swift"
  - "examples/ios_shell_host/CrosswakeShell/CrosswakeShellApp.swift"
  - "examples/ios_shell_host/CrosswakeShell/NotificationTokenProvider.swift"
  - "examples/ios_shell_host/CrosswakeShell/FilePickerCoordinator.swift"
  - "examples/ios_shell_host/CrosswakeShell/TransferCoordinator.swift"
  - "examples/ios_shell_host/CrosswakeShellTests/BridgeChannelTests.swift"
autonomous: true
requirements:
  - "CAP-NOTIFTOKEN"
  - "CAP-FILEPICKER"
must_haves:
  truths:
    - "The iOS shell can answer `notification_token` without prompting, returning provider-explicit token evidence plus normalized notification status per D-01 through D-09."
    - "The iOS shell can execute `files.pick` only for declared picker-backed transfers and stage copy-first handles before Phoenix sees the result per D-10 through D-21."
    - "Bridge tests cover denial, cancel, and success paths so route-local fail-closed behavior remains explicit."
  artifacts:
    - path: "examples/ios_shell_host/CrosswakeShell/NotificationTokenProvider.swift"
      provides: "iOS APNs token snapshot provider used by the bounded bridge"
    - path: "examples/ios_shell_host/CrosswakeShell/FilePickerCoordinator.swift"
      provides: "iOS copy-first picker coordinator that stages selected files for transfer seams"
    - path: "examples/ios_shell_host/CrosswakeShellTests/BridgeChannelTests.swift"
      provides: "iOS bridge tests for both user-prompted capabilities"
  key_links:
    - from: "examples/ios_shell_host/CrosswakeShell/CrosswakeShellApp.swift"
      to: "examples/ios_shell_host/CrosswakeShell/NotificationTokenProvider.swift"
      via: "app lifecycle registers and refreshes the shell-local token snapshot"
      pattern: "remoteNotifications|token|snapshot"
    - from: "examples/ios_shell_host/CrosswakeShell/BridgeChannel.swift"
      to: "examples/ios_shell_host/CrosswakeShell/FilePickerCoordinator.swift"
      via: "files.pick dispatch stages copy-first handles and returns typed results"
      pattern: "files.pick|transfer_id|items|canceled"
---

<objective>
Implement the iOS shell handlers and tests for `notification_token` and `file_picker`.

Purpose: The Phase 17 Elixir contracts only become credible once the checked-in iOS shell proves the same route-local, fail-closed semantics at the native boundary.
Output: iOS token snapshot plumbing, copy-first picker staging, bridge dispatch updates, and test coverage for success and denial paths.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/17-user-prompted-capabilities/17-CONTEXT.md
@.planning/phases/17-user-prompted-capabilities/17-RESEARCH.md
@.planning/phases/17-user-prompted-capabilities/17-PATTERNS.md
@.planning/phases/17-user-prompted-capabilities/17-01-PLAN.md
@.planning/phases/17-user-prompted-capabilities/17-02-PLAN.md
@examples/ios_shell_host/CrosswakeShell/BridgeChannel.swift
@examples/ios_shell_host/CrosswakeShell/LiveViewContainerViewController.swift
@examples/ios_shell_host/CrosswakeShell/TransferCoordinator.swift

<interfaces>
From examples/ios_shell_host/CrosswakeShell/BridgeChannel.swift:
```swift
private let permissionStatusProvider: (String) -> [String: String]?
private let filesPickHandler: ([String: String]) -> [String: String]
private let transferCoordinator: TransferCoordinator?
```

From examples/ios_shell_host/CrosswakeShell/TransferCoordinator.swift:
```swift
func execute(command: String, payload: [String: String], correlationID: String) -> [String: String]?
```

From examples/ios_shell_host/CrosswakeShell/CrosswakeShellApp.swift:
```swift
@main
struct CrosswakeShellApp: App
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add prompt-free iOS notification-token dispatch and token snapshot plumbing</name>
  <files>examples/ios_shell_host/CrosswakeShell/BridgeChannel.swift, examples/ios_shell_host/CrosswakeShell/LiveViewContainerViewController.swift, examples/ios_shell_host/CrosswakeShell/CrosswakeShellApp.swift, examples/ios_shell_host/CrosswakeShell/NotificationTokenProvider.swift, examples/ios_shell_host/CrosswakeShellTests/BridgeChannelTests.swift</files>
  <behavior>
    - Test 1: `notification_token` returns a typed success payload only when an APNs token snapshot is available and notification status permits it, without prompting per D-01 through D-06.
    - Test 2: Missing authorization, missing token, or missing shell setup returns explicit typed denial reasons rather than empty success per D-04.
    - Test 3: Success remains evidence-only and never implies backend registration or delivery readiness per D-07 and D-09.
  </behavior>
  <action>Add an iOS `NotificationTokenProvider` that keeps a shell-local APNs token snapshot refreshed from app lifecycle callbacks and exposed as a bounded request/reply helper. Update `CrosswakeShellApp.swift` and `LiveViewContainerViewController.swift` only as needed to wire registration callbacks and inject the provider into `BridgeChannel`. Keep the bridge command prompt-free per D-01 through D-04: use the existing `permissions.status` path for authorization truth, do not call authorization prompts here, and return typed denial payloads when authorization or token prerequisites are missing. The success payload must identify the provider explicitly as the iOS core APNs floor so later companion-backed variants do not require a breaking contract change.</action>
  <verify>
    <automated>xcodebuild test -project examples/ios_shell_host/CrosswakeShell.xcodeproj -scheme CrosswakeShell -destination 'platform=iOS Simulator,name=iPhone 16'</automated>
  </verify>
  <done>The iOS shell can answer `notification_token` from a shell-maintained token snapshot with explicit denial reasons and no prompt-owning behavior.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement copy-first iOS file-picker staging and bridge tests</name>
  <files>examples/ios_shell_host/CrosswakeShell/BridgeChannel.swift, examples/ios_shell_host/CrosswakeShell/LiveViewContainerViewController.swift, examples/ios_shell_host/CrosswakeShell/FilePickerCoordinator.swift, examples/ios_shell_host/CrosswakeShell/TransferCoordinator.swift, examples/ios_shell_host/CrosswakeShellTests/BridgeChannelTests.swift</files>
  <behavior>
    - Test 1: `files.pick` requires a declared `transfer_id` and returns `transfer_id` plus `items`, not a raw URL or a shape that varies by multiplicity, per D-10 and D-16 through D-19.
    - Test 2: The picker stages app-sandbox copies or staged import handles before replying, preserving copy-first posture per D-13.
    - Test 3: User cancellation returns a distinct typed outcome and transfer verification remains the authority on MIME/type truth per D-14 and D-20.
  </behavior>
  <action>Create an iOS `FilePickerCoordinator` that uses `UIDocumentPickerViewController(..., asCopy: true)` and stages selected content into app-controlled storage before returning opaque handles. Update bridge dispatch and transfer coordination so `files.pick` is transfer-bound per D-10 through D-15, with stable `transfer_id` and `items` reply shape per D-16 through D-21. Do not expose raw security-scoped URLs, persistent bookmarks, in-place editing, or reopen-later semantics.</action>
  <verify>
    <automated>xcodebuild test -project examples/ios_shell_host/CrosswakeShell.xcodeproj -scheme CrosswakeShell -destination 'platform=iOS Simulator,name=iPhone 16'</automated>
  </verify>
  <done>The iOS shell enforces transfer-bound picker execution, stages copy-first handles, and returns typed success or cancel outcomes that match the Elixir contract.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| iOS app lifecycle -> notification-token bridge reply | APNs token state crosses into Phoenix-owned route logic here and must stay evidence-only. |
| iOS document picker -> transfer staging | Provider-backed files cross into app-controlled staging here and must not become durable authority. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-07 | E | iOS bridge dispatch | mitigate | Keep `notification_token` and `files.pick` behind the existing bridge capability/version checks and transfer gating. |
| T-17-08 | I | APNs token handling | mitigate | Return only token evidence plus normalized status and deny when setup or authorization prerequisites are missing. |
| T-17-09 | T | document provider results | mitigate | Stage app-sandbox copies and expose opaque handles instead of raw URLs or persistent access tokens. |
</threat_model>

<verification>
Run `xcodebuild test -project examples/ios_shell_host/CrosswakeShell.xcodeproj -scheme CrosswakeShell -destination 'platform=iOS Simulator,name=iPhone 16'`. Confirm bridge tests cover prompt-free token success/denial and transfer-bound picker success/cancel semantics.
</verification>

<success_criteria>
The checked-in iOS shell executes both Phase 17 capabilities with prompt-free token evidence, copy-first picker staging, and fail-closed tests aligned to the Elixir contracts.
</success_criteria>

<output>
After completion, create `.planning/phases/17-user-prompted-capabilities/17-03-SUMMARY.md`
</output>
