---
phase: "17-user-prompted-capabilities"
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - "lib/crosswake/bridge/contract.ex"
  - "lib/crosswake/bridge/registry.ex"
  - "lib/crosswake/bridge/commands/notification_token.ex"
  - "lib/crosswake/manifest/builder.ex"
  - "guides/capabilities.md"
  - "guides/support_matrix.md"
  - "test/crosswake/bridge/contract_test.exs"
  - "test/crosswake/bridge/registry_test.exs"
  - "test/crosswake/manifest/manifest_test.exs"
autonomous: true
requirements:
  - "CAP-NOTIFTOKEN"
must_haves:
  truths:
    - "Phoenix routes can issue a typed `notification_token` request that never triggers an authorization prompt per D-01 through D-04."
    - "Successful replies carry provider-explicit token evidence plus normalized `notification_status`, while missing prerequisites fail closed through the existing denial envelope per D-05 through D-09."
    - "Manifest and public support truth describe `notification_token` as evidence-only and companion-sensitive instead of implying delivery or backend registration truth."
  artifacts:
    - path: "lib/crosswake/bridge/commands/notification_token.ex"
      provides: "Typed request and response structs for the notification-token bridge contract"
    - path: "lib/crosswake/bridge/registry.ex"
      provides: "Manifest-backed command resolution for notification_token"
    - path: "guides/support_matrix.md"
      provides: "Published prerequisite, denial, fallback, and rebuild truth for notification_token"
  key_links:
    - from: "lib/crosswake/bridge/registry.ex"
      to: "lib/crosswake/bridge/commands/notification_token.ex"
      via: "allowlisted command lookup uses the typed notification-token contract"
      pattern: "notification_token|notifications.token|get"
    - from: "lib/crosswake/manifest/builder.ex"
      to: "guides/support_matrix.md"
      via: "capability catalog metadata renders the public support row"
      pattern: "notification_token|evidence|companion"
---

<objective>
Define the Elixir-side `notification_token` bridge contract, registry lookup, and public truth surfaces.

Purpose: Phase 17 has to lock the non-prompting, evidence-only semantics before native token snapshots land, otherwise the shells will drift into prompt-owning or backend-truth behavior.
Output: Typed bridge payloads, registry wiring, manifest/support guidance, and tests that encode the fail-closed prerequisite posture.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/17-user-prompted-capabilities/17-CONTEXT.md
@.planning/phases/17-user-prompted-capabilities/17-RESEARCH.md
@.planning/phases/17-user-prompted-capabilities/17-PATTERNS.md
@lib/crosswake/bridge/contract.ex
@lib/crosswake/bridge/registry.ex
@lib/crosswake/manifest/builder.ex
@guides/capabilities.md
@guides/support_matrix.md

<interfaces>
From lib/crosswake/bridge/contract.ex:
```elixir
@commands ~w(
  app.info.get
  haptics.impact
  permissions.status
  share.invoke
  files.pick
  transfer.download
  transfer.export
  transfer.import
  transfer.upload.prepare
)

def ok_reply(%Request{} = request, payload \\ %{}) when is_map(payload)
def deny_reply(%Request{} = request, %Denial{} = denial)
```

From lib/crosswake/bridge/commands/permissions_status.ex:
```elixir
@supported_aliases ["notifications"]
@supported_statuses [:granted, :denied, :restricted]
```

From lib/crosswake/bridge/registry.ex:
```elixir
@capability_commands %{
  "app.info.get" => "app.info.get",
  "haptics.impact" => "haptics.impact",
  "permissions.status" => "permissions.status",
  "files.pick" => "files.pick",
  "share.invoke" => "share.invoke"
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 0: Reconcile overlapping in-progress files before Phase 17 execution</name>
  <files>lib/crosswake/bridge/contract.ex, lib/crosswake/bridge/registry.ex, lib/crosswake/manifest/builder.ex, guides/capabilities.md, guides/support_matrix.md, examples/ios_shell_host/CrosswakeShell/BridgeChannel.swift, examples/android_shell_host/app/src/main/java/dev/crosswake/shell/BridgeChannel.kt, test/crosswake/bridge/contract_test.exs, test/crosswake/bridge/registry_test.exs, test/crosswake/manifest/manifest_test.exs</files>
  <action>Before editing any Phase 17 target file, classify the current overlapping worktree changes and choose an execution-safe path: isolated worktree, branch-local reconciliation, or explicit preservation of the in-progress Phase 15 and 16 edits already present in this workspace. Do not overwrite or revert unrelated local work. Record the chosen isolation or reconciliation path in the execution summary so Phase 17 proof is tied to a known file baseline.</action>
  <verify>
    <automated>git status --short lib/crosswake/bridge/contract.ex lib/crosswake/bridge/registry.ex lib/crosswake/manifest/builder.ex guides/capabilities.md guides/support_matrix.md examples/ios_shell_host/CrosswakeShell/BridgeChannel.swift examples/android_shell_host/app/src/main/java/dev/crosswake/shell/BridgeChannel.kt test/crosswake/bridge/contract_test.exs test/crosswake/bridge/registry_test.exs test/crosswake/manifest/manifest_test.exs</automated>
  </verify>
  <done>Phase 17 execution has an explicit worktree-isolation or reconciliation path for every overlapping target file, so implementation does not trample existing local changes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 1: Add the prompt-free notification-token request and response contract</name>
  <files>lib/crosswake/bridge/contract.ex, lib/crosswake/bridge/commands/notification_token.ex, lib/crosswake/bridge/registry.ex, test/crosswake/bridge/contract_test.exs, test/crosswake/bridge/registry_test.exs</files>
  <behavior>
    - Test 1: `notification_token` is a typed one-shot bridge command and does not share the `permissions.status` request shape per D-01 through D-04.
    - Test 2: A successful payload includes an explicit `provider` field plus `token`, `notification_status`, and optional `detail`, with the normalized status vocabulary reused from `permissions.status` per D-05 and D-06.
    - Test 3: Registry lookup stays manifest-backed and route-local, denying undeclared or unsupported use through the existing bridge path per D-08.
  </behavior>
  <action>Add a dedicated `Crosswake.Bridge.Commands.NotificationToken` module and register a new low-level bridge command for it in the contract and registry. Keep the command semantics prompt-free and deterministic per D-01 through D-04: no authorization request helpers, no implicit retries, no empty-success fallback. Reuse the same normalized notification-status vocabulary as `permissions.status` per D-05 and D-06, but model token retrieval as evidence only per D-07 and D-09. Make provider truth explicit in the success payload, using a stable normalized field such as `provider` so APNs and companion-backed FCM do not collapse into a fake platform-neutral token story. The registry should continue using manifest-backed capability/version checks instead of bypassing route-local capability declaration.</action>
  <verify>
    <automated>mix test test/crosswake/bridge/contract_test.exs test/crosswake/bridge/registry_test.exs</automated>
  </verify>
  <done>The Elixir contract exposes a prompt-free, typed `notification_token` command with fail-closed registry behavior and provider-explicit, normalized reply semantics.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Align manifest and public support truth with evidence-only notification-token semantics</name>
  <files>lib/crosswake/manifest/builder.ex, guides/capabilities.md, guides/support_matrix.md, test/crosswake/manifest/manifest_test.exs</files>
  <behavior>
    - Test 1: Capability metadata names the prerequisite and denial posture without implying backend registration or delivery readiness per D-07 and D-09.
    - Test 2: Public docs and support matrix describe notification-token success as evidence-only and companion-sensitive, not a broad push integration guarantee.
    - Test 3: Rebuild guidance stays explicit when token-provider or shell configuration changes are required.
  </behavior>
  <action>Update manifest catalog metadata and the directly coupled capability/support guides so `notification_token` truth matches the shipped command. Preserve the companion-sensitive posture from research, but tighten the wording around prompt-free prerequisites, evidence-only replies, and fail-closed denial reasons per D-04 through D-09. Only change docs/support rows that are required to keep implementation truth aligned now; leave broader doctor, allowlist, and operational expansion for Phase 18.</action>
  <verify>
    <automated>mix test test/crosswake/manifest/manifest_test.exs && rg -n "notification_token|evidence|companion|required" guides/capabilities.md guides/support_matrix.md lib/crosswake/manifest/builder.ex</automated>
  </verify>
  <done>The manifest catalog and public truth surfaces accurately describe the shipped notification-token contract without overclaiming permission, provider, or backend authority, and they stay aligned with the provider-explicit payload semantics.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Phoenix route -> bridge registry | Route-owned code can ask for a token snapshot here, so undeclared or prompting behavior would widen runtime authority. |
| Manifest/catalog -> public support docs | Overstated notification support here would mislead adopters into treating token evidence as backend or delivery truth. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-01 | E | bridge registry | mitigate | Require manifest-backed declaration and version checks before `notification_token` resolves. |
| T-17-02 | T | notification-token payload contract | mitigate | Reuse the narrow normalized status vocabulary, require explicit provider tagging, and forbid prompt-owning or backend-registration fields per D-01 through D-09. |
| T-17-03 | I | support truth | mitigate | Update manifest metadata and directly coupled docs in the same plan so evidence-only and rebuild-sensitive posture stays explicit. |
</threat_model>

<verification>
Run `mix test test/crosswake/bridge/contract_test.exs test/crosswake/bridge/registry_test.exs test/crosswake/manifest/manifest_test.exs`. Confirm `guides/capabilities.md` and `guides/support_matrix.md` describe `notification_token` as prompt-free, provider-explicit, evidence-only, and fail-closed on missing prerequisites.
</verification>

<success_criteria>
Crosswake has a typed Elixir `notification_token` contract whose registry behavior, manifest metadata, and public support wording all preserve prompt-free, provider-explicit, evidence-only semantics.
</success_criteria>

<output>
After completion, create `.planning/phases/17-user-prompted-capabilities/17-01-SUMMARY.md`
</output>
