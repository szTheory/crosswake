---
phase: 61-notification-open-resolver-and-route-policy
plan: 1
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/crosswake/shell/denial.ex
  - lib/crosswake/companions/chimeway/contracts.ex
  - lib/crosswake/companions/chimeway/denial_codes.ex
autonomous: true
requirements:
  - OPEN-01
  - OPEN-03

must_haves:
  truths:
    - "System recognizes :notification_open_denied as a core denial reason"
    - "Notification open events are modeled with bounds (action_ref, open_ref)"
    - "Notification denial details are sanitized to prevent PII leakage"
  artifacts:
    - path: "lib/crosswake/shell/denial.ex"
      provides: "Core denial reason addition"
    - path: "lib/crosswake/companions/chimeway/contracts.ex"
      provides: "NotificationOpenEvidence struct"
    - path: "lib/crosswake/companions/chimeway/denial_codes.ex"
      provides: "Denial subcode configuration and sanitization"
  key_links:
    - from: "lib/crosswake/companions/chimeway/denial_codes.ex"
      to: "lib/crosswake/shell/denial.ex"
      via: "subcode mapping"
---

<objective>
Define the Chimeway notification-open evidence contracts and the core denial boundaries.

Purpose: Provide the struct definitions that the host registry and resolver will use, and register `:notification_open_denied` with RouteGate so the resolver can fail closed with clear, stable error subcodes that do not leak PII.
Output: `NotificationOpenEvidence` and `OpenResolution` structs, `Chimeway.DenialCodes`, and updated `Shell.Denial` reasons.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add core denial reason</name>
  <files>lib/crosswake/shell/denial.ex</files>
  <action>
    Add `:notification_open_denied` to the `@reasons` list and its corresponding `@type reason` spec in `Shell.Denial`.
    This ensures that discrete notification failures (e.g. expired, revoked) have their own reason atom rather than being buried under `:gate_denied`. (Decision D-18)
  </action>
  <verify>
    <automated>mix compile --warnings-as-errors</automated>
  </verify>
  <done>The `:notification_open_denied` atom is present in the `Denial.reason` type and `@reasons` list.</done>
</task>

<task type="auto">
  <name>Task 2: Define open evidence contracts</name>
  <files>lib/crosswake/companions/chimeway/contracts.ex</files>
  <action>
    Add `NotificationOpenEvidence` and `OpenResolution` structs to the Chimeway contracts.
    `NotificationOpenEvidence` must require at least: `route_id`, `open_ref`, `binding_ref`, `provider`, `action_ref`, and `auth_context`. Mirror the existing struct creation pattern (e.g. `new_token_binding/1` -> `new_notification_open_evidence/1`). (Decision D-09)
  </action>
  <verify>
    <automated>mix test test/crosswake/companions/chimeway/contracts_test.exs</automated>
  </verify>
  <done>Both structs are defined, enforce their keys, and provide constructor functions.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Implement denial subcodes and sanitizer</name>
  <files>lib/crosswake/companions/chimeway/denial_codes.ex, test/crosswake/companions/chimeway/denial_codes_test.exs</files>
  <behavior>
    - Detail sanitization removes raw push tokens, PII, and unregistered keys.
    - Safe keys like `open_ref`, `binding_ref`, `action_kind`, `evaluated_at` pass through.
  </behavior>
  <action>
    Create `Crosswake.Companions.Chimeway.DenialCodes` mirroring `Sigra.DenialCodes`.
    Define constants for canonical subcodes (`notification.open.expired`, `notification.open.replayed`, `notification.open.binding_revoked`, `notification.open.route_mismatch`, `notification.open.binding_mismatch`, `notification.open.unsupported_action`, `notification.open.policy_denied`).
    Implement `sanitize_details/1` with an `@allowed_detail_keys` list allowing only safe diagnostic identifiers (no PII, no raw tokens, no title/body). (Decisions D-19, D-21)
  </action>
  <verify>
    <automated>mix test test/crosswake/companions/chimeway/denial_codes_test.exs</automated>
  </verify>
  <done>Subcodes are available and the sanitizer strictly filters forbidden keys.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Core → Companion Error Output | Notification errors must not leak PII or push tokens to unverified telemetry or clients. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-61-01 | Information Disclosure | Chimeway.DenialCodes | mitigate | Strict allowlist inside `sanitize_details/1` rejecting anything not explicitly marked safe (preventing token/PII leaks). |
</threat_model>

<verification>
- Types compile and struct construction is valid.
- Sanitizer correctly strips unknown details.
</verification>

<success_criteria>
Core denial reason is established and Chimeway contracts for open evidence are defined, providing the necessary data structures for subsequent schema and resolver tasks.
</success_criteria>
