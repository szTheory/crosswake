---
phase: 61-notification-open-resolver-and-route-policy
plan: 3
type: execute
wave: 1
depends_on: []
files_modified:
  - examples/phoenix_host/lib/crosswake_example/chimeway/notification_open_intent.ex
  - examples/phoenix_host/lib/crosswake_example/chimeway/notification_open_intent_event.ex
  - examples/phoenix_host/test/crosswake_example/chimeway/notification_open_intent_test.exs
  - examples/phoenix_host/lib/crosswake_example/chimeway/registry.ex
  - examples/phoenix_host/priv/repo/migrations/20260603000000_create_chimeway_notification_open_intents.exs
autonomous: true
requirements:
  - OPEN-01

must_haves:
  truths:
    - "Notification intents are one-time consumable via database transaction"
    - "Intent consumption leaves an append-only audit trail"
    - "Replayed or expired intents are deterministically rejected"
  artifacts:
    - path: "examples/phoenix_host/lib/crosswake_example/chimeway/notification_open_intent.ex"
      provides: "Ecto schema for one-time intent"
    - path: "examples/phoenix_host/lib/crosswake_example/chimeway/registry.ex"
      provides: "Ecto.Multi issue/consume operations"
  key_links:
    - from: "examples/phoenix_host/lib/crosswake_example/chimeway/registry.ex"
      to: "examples/phoenix_host/lib/crosswake_example/chimeway/notification_open_intent.ex"
      via: "repo transaction"
---

<objective>
Implement the server-side, host-owned notification open-intent record and its lifecycle.

Purpose: Provide hermetic, provable anti-replay and expiry protection for notification opens without depending on provider (APNs/FCM) delivery guarantees, following the existing Sigra handoff/step-up pattern.
Output: Intent Ecto schema, audit event schema, database migration, and registry transactional issue/consume flows.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Generate database migration</name>
  <files>examples/phoenix_host/priv/repo/migrations/20260603000000_create_chimeway_notification_open_intents.exs</files>
  <action>
    Create a new Ecto migration under `examples/phoenix_host/priv/repo/migrations/` to create two tables: `chimeway_notification_open_intents` and `chimeway_notification_open_intent_events` (audit log).
    The intents table needs: `open_ref` (string, unique), `binding_ref` (string), `route_id` (string), `action_ref` (string, nullable), `scope` (string, nullable), `metadata` (jsonb, nullable), `state` (string, default "issued"), `expires_at` (utc_datetime), `consumed_at` (utc_datetime, nullable), and standard timestamps.
    The events table needs: `open_intent_id` (fkey), `event_type`, `occurred_at`, `details` (jsonb).
    (Decision D-02)
  </action>
  <verify>
    <automated>cd examples/phoenix_host && mix ecto.migrate</automated>
  </verify>
  <done>Database tables exist and are properly constrained, including `scope` and `metadata`.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Define Ecto schemas</name>
  <files>
    examples/phoenix_host/lib/crosswake_example/chimeway/notification_open_intent.ex,
    examples/phoenix_host/lib/crosswake_example/chimeway/notification_open_intent_event.ex,
    examples/phoenix_host/test/crosswake_example/chimeway/notification_open_intent_test.exs
  </files>
  <behavior>
    - The schema handles casting and validation of all fields including `scope` and `metadata`.
    - The `state` field is enforced to valid lifecycle states (e.g. "issued", "consumed", "revoked").
  </behavior>
  <action>
    Create the corresponding Ecto schemas for `NotificationOpenIntent` and `NotificationOpenIntentEvent`.
    Include strict validations (changesets) mirroring `HandoffTicket`. Ensure the `state` field is enforced to valid lifecycle states (e.g. "issued", "consumed", "revoked"). Ensure `scope` and `metadata` are cast and validated.
  </action>
  <verify>
    <automated>cd examples/phoenix_host && mix test test/crosswake_example/chimeway/notification_open_intent_test.exs</automated>
  </verify>
  <done>Schemas compile, map accurately to the migration (including scope/metadata), and pass unit tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Implement Issue/Consume Multi flows</name>
  <files>examples/phoenix_host/lib/crosswake_example/chimeway/registry.ex, examples/phoenix_host/test/crosswake_example/chimeway/registry_notification_open_test.exs</files>
  <behavior>
    - `issue_notification_open_intent` inserts a new intent with "issued" state and logs an event.
    - `consume_notification_open_intent` validates that the intent's `scope` matches the envelope scope, and that the referenced Chimeway binding is not revoked.
    - If valid, it locks the row, validates expiry, sets state to "consumed", records `consumed_at`, and logs an event.
    - Attempting to consume an already consumed intent returns an error.
  </behavior>
  <action>
    Add `issue_notification_open_intent/2` and `consume_notification_open_intent/2` to `CrosswakeExample.Chimeway.Registry`.
    Use `Ecto.Multi` to handle the query, update, and event insertion atomically, mirroring the logic in `saas_portal/handoff.ex`. 
    The consume flow must structurally reject expired rows, validate that the intent's `scope` matches the envelope, ensure the referenced Chimeway binding is not revoked, and log an audit event on state transition. Emit telemetry upon successful `Ecto.Multi` commit in the consume flow. (Decision D-03)
  </action>
  <verify>
    <automated>cd examples/phoenix_host && mix test test/crosswake_example/chimeway/registry_notification_open_test.exs</automated>
  </verify>
  <done>Registry exposes hermetic one-time consumption logic backed by Ecto.Multi with scope and binding validation, plus telemetry on success.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Network → Host | Client notification open requests are untrusted; state is enforced by backend DB. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-61-03 | Tampering | Registry Ecto.Multi | mitigate | Concurrent consume requests blocked by DB row locks/Ecto.Multi transactions. |
</threat_model>

<verification>
- Migration runs successfully.
- Tests prove that intents cannot be consumed twice (anti-replay) and expire correctly.
</verification>

<success_criteria>
The one-time consumptive record is functional, giving the resolver the backend authority to deterministically detect replayed or expired notification opens.
</success_criteria>