---
phase: 160-scoped-replay-and-auth-safety
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/crosswake/offline/journal.ex
  - lib/crosswake/offline/replay.ex
  - lib/crosswake/offline/runtime.ex
  - test/crosswake/offline/journal_test.exs
  - test/crosswake/offline/replay_test.exs
  - test/crosswake/offline/runtime_test.exs
  - examples/phoenix_host/priv/static/offline_study.js
  - examples/phoenix_host/lib/crosswake_example/local_first/replay_admission.ex
  - examples/phoenix_host/lib/crosswake_example/local_first/sync_controller.ex
  - examples/phoenix_host/lib/crosswake_example/local_first/study.ex
  - examples/phoenix_host/lib/crosswake_example/local_first/review_event.ex
  - examples/phoenix_host/priv/repo/migrations/20260802160000_scope_review_events.exs
  - examples/phoenix_host/test/crosswake_example/local_first/replay_admission_test.exs
  - examples/phoenix_host/test/crosswake_example/local_first/sync_controller_test.exs
  - examples/phoenix_host/test/crosswake_example/local_first/study_test.exs
  - examples/phoenix_host/e2e/offline_sync.spec.ts
autonomous: true
requirements: [SCOPE-01, SCOPE-02, SCOPE-03, SCOPE-05]
must_haves:
  truths:
    - "One production Study event travels from an active exact-scope partition through the complete per-event host admission order and one atomic idempotency-plus-domain transaction; feature or Sigra denial produces zero effect (D-01 through D-10, SCOPE-01 through SCOPE-03, SCOPE-05)."
    - "Cold launch is inert, logout/account switch fences before new activation, stale completions cannot affect another scope, and every unaccepted entry remains available without an unscoped drain (D-02 through D-07, D-10 through D-13)."
    - "The host re-resolves session, mapped scope, route, existing gated_by state, Sigra evidence, and domain authorization immediately before every event; missing, malformed, exceptional, or timed-out decisions deny without echo (D-08, D-12, D-13, D-20)."
    - "Accepted duplicates after a lost response return accepted without repeating the effect; rejected, conflict, and blocked remain explicit and no silent last-write-wins path exists (D-09 through D-12)."
  artifacts:
    - path: "examples/phoenix_host/e2e/offline_sync.spec.ts"
      provides: "Runnable authorized one-event tracer plus exact-scope lifecycle and halt proof"
    - path: "examples/phoenix_host/lib/crosswake_example/local_first/replay_admission.ex"
      provides: "Ordered fail-closed host admission pipeline"
    - path: "examples/phoenix_host/lib/crosswake_example/local_first/study.ex"
      provides: "One-event atomic idempotency and domain effect"
    - path: "examples/phoenix_host/priv/static/offline_study.js"
      provides: "Scope-partitioned outbox and persisted lifecycle fence"
  key_links:
    - from: "offline_study.js exact-scope ordered cursor"
      to: "SyncController.sync/2"
      via: "one active scope+epoch lease and one bounded scoped event"
      pattern: "scope_ref|epoch"
    - from: "SyncController.sync/2"
      to: "ReplayAdmission.authorize/3"
      via: "closed request validation followed by current per-event authority"
      pattern: "ReplayAdmission"
    - from: "ReplayAdmission.authorize/3"
      to: "Study.apply_one/3"
      via: "session -> scope -> route -> gated_by -> Sigra -> host-domain allow chain"
      pattern: "evaluate|authorize|apply_one"
    - from: "Study.apply_one/3"
      to: "Repo.transaction"
      via: "one Ecto.Multi containing idempotency decision and domain mutation"
      pattern: "Ecto.Multi"
  prohibitions:
    - statement: "MUST NOT decode, derive, hash, enumerate, log, or repurpose an opaque scope reference as account, route, provider, device, or correlation identity."
      category: privacy
      verification: flagged-unverified
    - statement: "MUST NOT provide an unscoped/default/all-partition read, drain, count, delete, lease, checkpoint, or conflict path, or treat current cookies as proof that an old partition is authorized."
      category: scope-safety
      verification: flagged-unverified
    - statement: "MUST NOT skip, reorder, cache batch-wide, or fail open the session, scope, route, feature, Sigra, or host-domain admission layers."
      category: authority-safety
      verification: flagged-unverified
    - statement: "MUST NOT make Crosswake, the shell, WebView, or crosswake_sigra a credential, token, account-mapping, provider, device-identity, MFA-ceremony, or session-refresh authority."
      category: authority-safety
      verification: flagged-unverified
flagged_assumptions:
  - "[FLAGGED/UNVERIFIED] SCOPE-01 remains an external edge assumption: the complete deterministic edge is required versioned opaque scope construction plus compound-key/index access, while the real host-issued format and rotation policy remain unknown_blocking."
  - "[FLAGGED/UNVERIFIED] SCOPE-02 remains an external edge assumption: cold-launch inactivity, switch-before-send, switch-in-flight, monotonic epoch invalidation, retained queues, and inert stale completions are the required deterministic lifecycle edges."
  - "[FLAGGED/UNVERIFIED] SCOPE-03 remains an external edge assumption: entry and replay must visibly retain and pause on every closed denial, and a host authority transition is required before another drain attempt."
  - "[FLAGGED/UNVERIFIED] SCOPE-05 remains an external edge assumption: the existing optional Sigra boundary must collapse missing, incompatible, exceptional, timed-out, or malformed evidence to one closed denial without exposing authority facts."
---

<objective>
Deliver the complete production tracer and the essential account/auth/idempotency edge proof for one Study event.

Purpose: Prove the full security architecture on the first real effect, rather than deferring admission or atomicity behind an optimistic slice.
Output: Scope-required core contracts, partitioned local storage, lifecycle fence, complete host admission, atomic one-event replay, and deterministic browser/host proof.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/ADR-FIRST-B2C-ADOPTER.md
@.planning/ROADMAP.md
@.planning/phases/160-scoped-replay-and-auth-safety/160-CONTEXT.md
@.planning/phases/160-scoped-replay-and-auth-safety/160-RESEARCH.md
@.planning/phases/160-scoped-replay-and-auth-safety/160-PATTERNS.md

<interfaces>
Extend `Journal.new_entry/1`, `Replay.request_for_entry/1`, and the browser outbox with the same required versioned opaque scope contract. The host owns conn/session resolution, scope mapping, current manifest route lookup, the existing `gated_by` source, Sigra evidence, domain authorization, Repo, schema, and changesets. Reuse the existing Sigra route/session evaluator and reduce its result inside the host admission adapter to closed allow/deny; do not add credential or account authority to core.
</interfaces>
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Trace one fully authorized Study event through every safety boundary</name>
  <read_first>
    AGENTS.md
    .planning/phases/160-scoped-replay-and-auth-safety/160-CONTEXT.md
    lib/crosswake/offline/journal.ex
    lib/crosswake/offline/replay.ex
    examples/phoenix_host/priv/static/offline_study.js
    examples/phoenix_host/lib/crosswake_example/local_first/sync_controller.ex
    examples/phoenix_host/lib/crosswake_example/local_first/study.ex
    examples/phoenix_host/lib/crosswake_example/local_first/review_event.ex
    examples/phoenix_host/e2e/offline_sync.spec.ts
    lib/crosswake/compatibility/route_gate.ex
    packages/crosswake_sigra/lib/crosswake/companions/sigra.ex
  </read_first>
  <files>lib/crosswake/offline/journal.ex, lib/crosswake/offline/replay.ex, test/crosswake/offline/journal_test.exs, test/crosswake/offline/replay_test.exs, examples/phoenix_host/priv/static/offline_study.js, examples/phoenix_host/lib/crosswake_example/local_first/replay_admission.ex, examples/phoenix_host/lib/crosswake_example/local_first/sync_controller.ex, examples/phoenix_host/lib/crosswake_example/local_first/study.ex, examples/phoenix_host/lib/crosswake_example/local_first/review_event.ex, examples/phoenix_host/priv/repo/migrations/20260802160000_scope_review_events.exs, examples/phoenix_host/e2e/offline_sync.spec.ts</files>
  <behavior>
    - Test: an autonomous source preflight asserts D-01 is locked, every Entry/Request requires matching versioned opaque scope fields, and rejected values never appear in errors.
    - Test: one active synthetic scope queues one Study event under a compound scope/local key, sends it in journal order, and leaves a second retained partition untouched (D-01, D-02, D-07).
    - Test: immediately before the event, the host enforces bounded closed input, current backend session, host-mapped scope equality, declared route, dependency and existing gated_by state, current Sigra route/session evidence, and host domain authorization in D-08 order.
    - Test: the allowed event records the idempotency decision and Study effect in one transaction; feature-disabled and Sigra-denied variants create neither record (D-09, D-13, D-20).
  </behavior>
  <action>Start with a failing browser-host tracer and an autonomous preflight that parses the D-01 paragraph from 160-CONTEXT.md and asserts the required/versioned/opaque/host-issued/sensitive migration contract; this replaces the superseded human checkpoint and does not reopen the choice. Add the same conservative non-echoing scope validator to Journal and Replay, migrate the one IndexedDB outbox to compound scope/local identity, and admit exactly one active synthetic fixture scope. Create host-owned ReplayAdmission and make SyncController pass the one event through D-08's complete order, including the current existing `gated_by` source and the existing Sigra route/session evaluator. Only after all layers allow, run one host-owned Ecto.Multi/Repo transaction that records a scope-qualified idempotency outcome with the Study domain effect. Return a typed accepted outcome; return a typed blocked class on feature or Sigra denial. Keep TODO-002 and real route/flag/session inputs unknown_blocking and make no physical-device claim.</action>
  <acceptance_criteria>
    - The preflight is autonomous and proves D-01 source fidelity before any migration; there is no checkpoint or user decision.
    - The accepted tracer crosses every D-08 layer, including current feature state and Sigra evidence, before one D-09 transaction commits.
    - Feature-disabled and Sigra-denied tracer variants return a closed blocked response, preserve the local record, and commit zero idempotency/domain rows.
    - A second partition is untouched and no production outbox operation can address every scope.
    - ASVS L1 V2 Authentication, V3 Session Management, V4 Access Control, and V5 Input Validation are evidenced by the tracer's session, exact-scope, route/feature/Sigra/domain, and bounded-envelope assertions.
  </acceptance_criteria>
  <verify><automated>mix test test/crosswake/offline/journal_test.exs test/crosswake/offline/replay_test.exs &amp;&amp; (cd examples/phoenix_host &amp;&amp; MIX_ENV=test mix test test/crosswake_example/local_first/replay_admission_test.exs test/crosswake_example/local_first/study_test.exs &amp;&amp; npm run proof:offline-island -- --grep "fully authorized scoped Study event")</automated></verify>
  <done>One Study event succeeds through partitioned storage, complete current host authority, and atomic idempotency+effect; feature and Sigra denials fail closed before any effect.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Fence relaunch, logout, account switching, and ordered drain</name>
  <reversibility rating="costly">The persisted lifecycle/epoch contract coordinates retained browser state with later device proof and requires synchronized consumers if changed.</reversibility>
  <read_first>
    .planning/phases/160-scoped-replay-and-auth-safety/160-CONTEXT.md
    lib/crosswake/offline/runtime.ex
    test/crosswake/offline/runtime_test.exs
    examples/phoenix_host/priv/static/offline_study.js
    examples/phoenix_host/e2e/offline_sync.spec.ts
  </read_first>
  <files>lib/crosswake/offline/runtime.ex, test/crosswake/offline/runtime_test.exs, examples/phoenix_host/priv/static/offline_study.js, examples/phoenix_host/e2e/offline_sync.spec.ts</files>
  <behavior>
    - Test: new/reloaded storage is inactive, reveals no retained payload, sends nothing, and cannot issue a scope+epoch lease until explicit host activation (D-03, D-05).
    - Test: logout/switch moves active to stopping, increments epoch, stops the scoped worker, and only then activates another scope; an old completion changes neither storage nor UI (D-04, D-10).
    - Test: one active scope drains serially in journal order; accepted duplicates are removed, rejected/conflict remain recoverable, and blocked halts with the current and later entries retained (D-07, D-11, D-12).
    - Test: the learner sees a calm accessible saved/syncing/attention/paused state with stable focus, text semantics, and no backend details or blocked progress spinner (D-21, D-23).
  </behavior>
  <action>Implement the closed inactive/active/stopping fence and monotonic epoch in Runtime and persist the browser fence beside the outbox. Every read, count, send, completion, delete, checkpoint, conflict, and lease operation must require an exact active scope+epoch; a cold or restored store stays inert. Fence before switch, await or cancel the single scoped worker, and make every stale completion inert while preserving ambiguous entries. Per D-06, Crosswake supplies only scope-targeted lifecycle primitives and tests; host account mapping, encryption choice, retention, cleanup/deletion, and retained-partition recovery stay outside core, which neither enumerates identities nor derives scope from account data. Drain serially and recheck the lease before every send and completion mutation. Delete only accepted outcomes, including an accepted duplicate; retain rejected/conflict, and halt on blocked until an explicit authority/activation trigger. Render D-21/D-23 closed learner states through the existing status region using host-owned copy and existing tokens.</action>
  <acceptance_criteria>
    - Cold/relaunch, switch-before-send, and switch-in-flight tests prove no cross-scope display, send, delete, or UI mutation.
    - Missing, malformed, inactive, mismatched, and old-epoch operations block non-echoingly and preserve the queue.
    - Nth-event block leaves N and later entries in order, issues no hot retry, and never converts the route to online-only.
    - ASVS L1 V3 Session Management and V4 Access Control are evidenced by epoch invalidation, exact-scope leases, retained blocked data, and stale-completion tests.
  </acceptance_criteria>
  <verify><automated>mix test test/crosswake/offline/runtime_test.exs &amp;&amp; (cd examples/phoenix_host &amp;&amp; npm run proof:offline-island -- --grep "inactive relaunch|switch before send|switch in flight|ordered blocked drain")</automated></verify>
  <done>Retained partitions start inert, account transitions fence before activation, and one exact scope drains in order without stale mutation, silent drop, or loss of unaccepted work.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Prove every admission denial and atomic retry edge</name>
  <reversibility rating="costly">The host admission order and scope-qualified idempotency outcomes are security and recovery contracts shared by endpoint, browser, and host persistence.</reversibility>
  <read_first>
    .planning/phases/160-scoped-replay-and-auth-safety/160-CONTEXT.md
    examples/phoenix_host/lib/crosswake_example/local_first/replay_admission.ex
    examples/phoenix_host/lib/crosswake_example/local_first/sync_controller.ex
    examples/phoenix_host/lib/crosswake_example/local_first/study.ex
    examples/phoenix_host/priv/repo/migrations/20260802160000_scope_review_events.exs
    examples/phoenix_host/test/crosswake_example/local_first/replay_admission_test.exs
    examples/phoenix_host/test/crosswake_example/local_first/sync_controller_test.exs
    examples/phoenix_host/test/crosswake_example/local_first/study_test.exs
  </read_first>
  <files>examples/phoenix_host/lib/crosswake_example/local_first/replay_admission.ex, examples/phoenix_host/lib/crosswake_example/local_first/sync_controller.ex, examples/phoenix_host/lib/crosswake_example/local_first/study.ex, examples/phoenix_host/lib/crosswake_example/local_first/review_event.ex, examples/phoenix_host/priv/repo/migrations/20260802160000_scope_review_events.exs, examples/phoenix_host/test/crosswake_example/local_first/replay_admission_test.exs, examples/phoenix_host/test/crosswake_example/local_first/sync_controller_test.exs, examples/phoenix_host/test/crosswake_example/local_first/study_test.exs</files>
  <behavior>
    - Test: missing session, scope mismatch, unknown route, disabled feature, missing/incompatible/denied Sigra, host authorization denial, exception, timeout, and malformed callback each deny with a closed class and zero effect (D-08, D-20).
    - Test: route entry and replay use the same existing gated_by source; changing it before event N prevents N from beginning while earlier commits remain (D-10, D-13).
    - Test: rollback leaves neither idempotency nor domain record; retry after a lost response returns the prior accepted result and leaves exactly one effect (D-09, D-11).
    - Test: wholly denied requests use conventional typed non-success responses, while an earlier accepted event plus a later halt returns a successful typed envelope; no free-form error or multi-status response exists (D-12).
  </behavior>
  <action>Complete the host integration matrix around the tracer without adding a new flag service, session system, or generic sync API. Re-resolve every admission layer immediately before every event, convert all non-allow/exceptional adapter outcomes to closed non-echoing denial classes, and preserve current host ownership. Replace any bulk application path with serial Study.apply_one/3. Make scope-qualified idempotency and domain mutation one transaction, persist the closed result needed for retry, and add deterministic rollback/lost-response hooks. Keep accepted, rejected, conflict, and blocked distinct; blocked begins no effect and halts later dispatch.</action>
  <acceptance_criteria>
    - Test doubles prove the exact D-08 call order for each event and no batch-long authority snapshot exists.
    - Missing/incompatible Sigra denies without returning session, finding, credential, token, provider, device, or exact auth detail.
    - Rollback creates zero records; lost-response retry creates one idempotency record and one domain effect and returns accepted.
    - ASVS L1 V2 Authentication, V3 Session Management, V4 Access Control, V5 Input Validation, and V7 Error Handling/Logging are evidenced by exact denial cases, conventional typed responses, and non-echoing failure assertions.
  </acceptance_criteria>
  <verify><automated>cd examples/phoenix_host &amp;&amp; MIX_ENV=test mix test test/crosswake_example/local_first/replay_admission_test.exs test/crosswake_example/local_first/sync_controller_test.exs test/crosswake_example/local_first/study_test.exs</automated></verify>
  <done>Every event is reauthorized through the complete fail-closed host chain, and transaction rollback, duplicates, and lost responses cannot repeat or silently drop a Study effect.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| host authority -> local partition | A host-issued sensitive scope activates otherwise inert retained data. |
| browser -> Phoenix endpoint | Untrusted bounded envelopes cannot establish session, scope, route, or feature authority. |
| RouteGate/Sigra/host callbacks -> mutation | Every dynamic allow decision must be current, ordered, and fail closed. |
| transaction result -> retry | Lost responses and duplicate events must not repeat a domain effect. |

## ASVS L1 Control Mapping

| ASVS L1 category | Required control | Task | Automated evidence |
|---|---|---|---|
| V2 Authentication | Resolve current backend session and reduce Sigra evidence to closed allow/deny before each event. | Tasks 1, 3 | Task 1 tracer command; Task 3 replay-admission suite. |
| V3 Session Management | Exact host-mapped scope, inert relaunch, fence-first logout/switch, and epoch-invalidated stale callbacks. | Tasks 1, 2, 3 | Core runtime tests plus scoped Playwright switch/relaunch cases. |
| V4 Access Control | Resolve route, existing gated_by state, Sigra, and host-domain authorization in locked order before every effect. | Tasks 1, 3 | Tracer feature/Sigra denials and full admission-order integration suite. |
| V5 Input Validation | Enforce body/batch/closed-key/size/scope grammar before authority work and reject without echo. | Tasks 1, 3 | Journal/replay contract tests and controller invalid-envelope cases. |
| V7 Error Handling and Logging | Emit conventional typed blocked/outcome bodies with stable safe classes only. | Task 3 | Controller tests for every denial, partial halt, and non-echoing response. |
| V8 Data Protection | Keep scope/payload/identity values out of status, errors, and test artifacts; Plan 02 closes all operational egress. | Tasks 1, 2 | Browser/host non-echo assertions; Plan 02 is the full egress gate. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-160-01 | Spoofing/Elevation | scope admission and IndexedDB partition | high | mitigate | Task 1 requires exact scope equality and exact-scope storage; Task 2 proves relaunch/switch isolation with its Playwright command. |
| T-160-02 | Tampering | lifecycle and stale completion | high | mitigate | Task 2 fences first, increments epoch, and proves old callbacks inert with runtime and browser tests. |
| T-160-03 | Elevation | route/feature/Sigra/domain chain | high | mitigate | Tasks 1 and 3 run the locked per-event order and deny every missing, exceptional, or malformed decision. |
| T-160-05 | Tampering/Repudiation | idempotency and domain effect | high | mitigate | Tasks 1 and 3 use one transaction and prove rollback, duplicate, and lost-response behavior. |
| T-160-06 | Denial of Service/Elevation | disabled route drain | high | mitigate | Tasks 2 and 3 halt before the next event, retain queued data, and prove no hot retry. |
| T-160-SC | Tampering | package installs | low | accept | No package-manager installation occurs; the plan uses existing Phoenix, Ecto, IndexedDB, Playwright, and Sigra dependencies. |
</threat_model>

<verification>
Run every task command, then run `mix test test/crosswake/offline/{journal,replay,runtime}_test.exs &amp;&amp; (cd examples/phoenix_host &amp;&amp; MIX_ENV=test mix test test/crosswake_example/local_first &amp;&amp; npm run proof:offline-island)`.
</verification>

<success_criteria>
The first accepted Study event already has complete scope, lifecycle, feature, Sigra, domain-authorization, and atomic-idempotency protection; all denial/race/retry edges retain data and fail visibly closed.
</success_criteria>

<output>
Create `.planning/phases/160-scoped-replay-and-auth-safety/160-01-SUMMARY.md` when done.
</output>
