---
phase: 160-scoped-replay-and-auth-safety
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/crosswake/offline/journal.ex
  - lib/crosswake/offline/replay.ex
  - lib/crosswake/offline/safe_observation.ex
  - test/crosswake/offline/journal_test.exs
  - test/crosswake/offline/replay_test.exs
  - examples/phoenix_host/priv/static/offline_study.js
  - examples/phoenix_host/lib/crosswake_example/local_first/sync_controller.ex
  - examples/phoenix_host/lib/crosswake_example/local_first/study.ex
  - examples/phoenix_host/e2e/offline_sync.spec.ts
autonomous: false
requirements: [SCOPE-01, SCOPE-02, SCOPE-03, SCOPE-04, SCOPE-05]
must_haves:
  truths:
    - "One synthetic host-issued scope travels through the exact-scope IndexedDB partition and scoped Phoenix request to one authorized host mutation; no unscoped drain is reachable (D-01, D-02, D-07, SCOPE-01)."
    - "The server rejects a mismatched current scope before domain application, and a safe blocked observation contains neither the scope, payload, nor mutation references (D-05, D-08, D-15, D-17, SCOPE-02 through SCOPE-05)."
    - "The tracer remains a Phoenix-host contract for one study island and does not claim adopter-instance or physical-device completion while TODO-002 is unknown_blocking."
  artifacts:
    - path: "lib/crosswake/offline/safe_observation.ex"
      provides: "Transport-independent allowlisted observation type"
    - path: "examples/phoenix_host/priv/static/offline_study.js"
      provides: "One production exact-scope IndexedDB and replay path"
    - path: "examples/phoenix_host/e2e/offline_sync.spec.ts"
      provides: "Runnable end-to-end scoped replay tracer"
  key_links:
    - from: "offline_study.js exact-scope cursor"
      to: "SyncController.sync/2"
      via: "bounded JSON envelope carrying one scope and one ordered event"
      pattern: "scope_ref|by_scope"
    - from: "SyncController.sync/2"
      to: "Study.apply_one/3"
      via: "host current-scope comparison before transactional mutation"
      pattern: "current_scope|apply_one"
    - from: "blocked replay result"
      to: "SafeObservation.to_map/1"
      via: "closed denial class projection, never transport-map redaction"
      pattern: "SafeObservation"
  prohibitions:
    - statement: "MUST NOT decode, derive, hash, enumerate, log, or repurpose an opaque scope reference as account, route, provider, device, or correlation identity."
      category: privacy
      verification: flagged-unverified
flagged_assumptions:
  - "[FLAGGED/UNVERIFIED] SCOPE-01 was unclassified by the spec-less edge probe; this plan assumes its complete edge is required versioned scope construction plus exact compound-key/index access, with malformed, absent, and mismatched values denied non-echoingly."
---

<objective>
Prove the thinnest production-quality scoped replay path across core envelope, browser partition,
Phoenix admission, atomic host effect, and privacy-safe observation.

Purpose: Front-load the phase's hardest constraint: a real accepted replay works while the adjacent
cross-scope path is impossible and non-observable.
Output: Required scoped contracts, one scoped IndexedDB path, one host endpoint path, and one E2E tracer.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/ADR-FIRST-B2C-ADOPTER.md
@.planning/phases/160-scoped-replay-and-auth-safety/160-CONTEXT.md
@.planning/phases/160-scoped-replay-and-auth-safety/160-RESEARCH.md
@.planning/phases/160-scoped-replay-and-auth-safety/160-PATTERNS.md
@.planning/phases/159-host-reusable-proof-lane/159-01-SUMMARY.md

<interfaces>
Existing seams: `Journal.new_entry/1`, `Replay.request_for_entry/1`, `Replay.to_map/1`,
`SyncController.sync/2`, `Study.sync_events/1`, and browser `flushOutbox()`.
Create: `SafeObservation.new/1`, `SafeObservation.to_map/1`, `Study.apply_one/3`, and a scoped
browser storage API whose production reads/counts/deletes require an active scope.
</interfaces>
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 0: Confirm the locked persisted-and-wire migration door</name>
  <read_first>
    .planning/phases/160-scoped-replay-and-auth-safety/160-CONTEXT.md
    lib/crosswake/offline/journal.ex
    lib/crosswake/offline/replay.ex
    examples/phoenix_host/priv/static/offline_study.js
  </read_first>
  <decision>Confirm execution of D-01's already-locked required scope_ref storage/wire migration.</decision>
  <context>D-01's semantics are not reopened: scope_ref remains required, versioned, opaque, host-issued, and sensitive. This checkpoint exists because generated clients will persist the field and endpoints will accept it; undo later requires storage and wire migration.</context>
  <files>None (decision checkpoint only)</files>
  <options>
    <option id="proceed-locked-d01">
      <name>Proceed with locked D-01</name>
      <pros>Delivers the required account-isolation boundary and keeps the agreed host/core ownership.</pros>
      <cons>Future incompatible scope shape changes require explicit storage and wire migration.</cons>
    </option>
    <option id="halt-phase">
      <name>Halt Phase 160</name>
      <pros>Avoids crossing the persisted/wire door in this execution.</pros>
      <cons>Leaves SCOPE-01 through SCOPE-05 and downstream device proof blocked; it does not authorize an alternative implementation.</cons>
    </option>
  </options>
  <action>Review only the migration timing and irreversible cost. Do not reinterpret D-01, select another scope representation, infer adopter identity, or implement any code during this checkpoint.</action>
  <acceptance_criteria>
    - The selected signal is exactly proceed-locked-d01 or halt-phase.
    - Proceeding preserves every D-01 semantic; halting performs no implementation or partial migration.
  </acceptance_criteria>
  <verify><automated>rg -n "D-01:.*scope_ref.*required, versioned, opaque host-issued" .planning/phases/160-scoped-replay-and-auth-safety/160-CONTEXT.md &amp;&amp; test ! -e examples/phoenix_host/priv/repo/migrations/20260802160000_scope_review_events.exs</automated></verify>
  <done>The executor has an explicit proceed or halt signal before creating persisted/wire scope data.</done>
  <resume-signal>Select: proceed-locked-d01 or halt-phase</resume-signal>
</task>

<task type="tracer" tdd="true">
  <name>Task 1: Trace one authorized scoped mutation end to end</name>
  <reversibility rating="one-way">D-01 fixes a persisted and wire-visible scope_ref contract; the user already locked this choice, so record the migration cost without re-asking.</reversibility>
  <read_first>
    AGENTS.md
    .planning/phases/160-scoped-replay-and-auth-safety/160-CONTEXT.md
    .planning/phases/160-scoped-replay-and-auth-safety/160-RESEARCH.md
    lib/crosswake/offline/safe_observation.ex (new; confirm absent before creating)
    lib/crosswake/offline/telemetry.ex
    examples/phoenix_host/priv/static/offline_study.js
    examples/phoenix_host/lib/crosswake_example/local_first/sync_controller.ex
    examples/phoenix_host/lib/crosswake_example/local_first/study.ex
    examples/phoenix_host/e2e/offline_sync.spec.ts
  </read_first>
  <files>lib/crosswake/offline/safe_observation.ex, examples/phoenix_host/priv/static/offline_study.js, examples/phoenix_host/lib/crosswake_example/local_first/sync_controller.ex, examples/phoenix_host/lib/crosswake_example/local_first/study.ex, examples/phoenix_host/e2e/offline_sync.spec.ts</files>
  <behavior>
    - Test: browser/server admission rejects missing, malformed, or unversioned scope_ref without echoing it (D-01, D-05).
    - Test: one synthetic scope queues, reads, posts, applies, and deletes exactly its accepted record; a second scope remains untouched (D-02, D-07).
    - Test: host current-scope mismatch returns a typed blocked response and produces zero ReviewEvent rows (D-08, D-12).
    - Test: the blocked observation serializes only schema_version, route_id, lifecycle, outcome, denial_class, and bounded measurements; injected payload/scope/reference keys are rejected non-echoingly (D-15 through D-19).
  </behavior>
  <action>Start RED with the existing Playwright offline spec. Per D-01, require `scope_ref` and `scope_version` in the browser/server envelope, validate a conservative opaque transport grammar without decoding it, and migrate the browser database in place to a compound `[scope_ref, local_ref]` key plus scope/order index with no production all-scope read/count/delete call. Create `Crosswake.Offline.SafeObservation` as an allowlist constructor/serializer that cannot accept arbitrary metadata. For the tracer only, activate one deterministic synthetic test scope from host-rendered test input, send one event, require the host test session to resolve the same scope, and apply the existing host mutation through Study. Use typed non-echoing blocked JSON projected through SafeObservation. Preserve the original browser fixture/UI path. Core Journal/Replay contracts and full transactional idempotency expand in Plans 02/04 against this proven wire path. The host-issued real scope and adopter route remain external unknown_blocking inputs, so use synthetic opaque fixtures and make no device/adopter claim.</action>
  <acceptance_criteria>
    - Browser/server envelopes require validated versioned scope fields, and no admission error contains the rejected value.
    - IndexedDB schema has a scope-qualified key/index and the application path contains no unscoped mutation-store getAll/count/delete operation.
    - The accepted tracer commits one row and removes only the accepted same-scope entry; mismatch commits zero rows and retains the queue.
    - SafeObservation has no scope_ref, payload, client_mutation_id, idempotency_key, checkpoint, account/session/token, endpoint, flag, or free metadata field.
    - Existing `offline_sync.spec.ts` remains the primary browser corpus and passes with two synthetic scopes in its fixture setup.
  </acceptance_criteria>
  <verify><automated>cd examples/phoenix_host &amp;&amp; npm run proof:offline-island -- --grep "authorized scoped mutation"</automated></verify>
  <done>One real scoped mutation succeeds end to end, the adjacent mismatched scope is blocked before effect, the other partition remains intact, and neither observation nor visible output contains transport-sensitive values.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Bind the core journal and replay contracts to the proven scope</name>
  <reversibility rating="one-way">D-01 adds persisted and wire-visible fields after the immediately preceding checkpoint confirms the locked migration door.</reversibility>
  <read_first>
    .planning/phases/160-scoped-replay-and-auth-safety/160-CONTEXT.md
    lib/crosswake/offline/journal.ex
    lib/crosswake/offline/replay.ex
    lib/crosswake/offline/safe_observation.ex
    test/crosswake/offline/journal_test.exs
    test/crosswake/offline/replay_test.exs
  </read_first>
  <files>lib/crosswake/offline/journal.ex, lib/crosswake/offline/replay.ex, test/crosswake/offline/journal_test.exs, test/crosswake/offline/replay_test.exs</files>
  <behavior>
    - Test: Journal.Entry and Replay.Request require valid matching scope_ref/scope_version and request_for_entry copies both exactly (D-01).
    - Test: missing/malformed/mismatched scope returns stable non-echoing errors; no default/current-cookie scope fallback exists (D-05).
    - Test: Replay outcomes are exactly accepted/rejected/conflict/blocked; blocked carries only a closed safe class and halt semantics (D-11, D-12).
    - Test: transport serializers remain sensitive and SafeObservation rejects Entry, Request, and Outcome rather than redacting them (D-15, D-19).
  </behavior>
  <action>Extend the existing keyword-only typed contracts with required `scope_ref` and `scope_version`, using the same conservative validator proven by the tracer and no decode/hash/identity mapping. Copy scope fields exactly from Entry to Request. Add `Replay.blocked/2`, include `:blocked` in Outcome.status, and define blocked reason as a closed safe admission class rather than free-form response data. Keep Journal.to_map/1 and Replay.to_map/1 transport-only and make SafeObservation reject those structs. Do not add account enumeration, unscoped constructors, or observable serializers for transport structures.</action>
  <acceptance_criteria>
    - Every Entry/Request construction path requires the two scope fields and emits no rejected value.
    - request_for_entry preserves scope byte-for-byte and no inference/default helper exists.
    - Outcome exposes all four closed states; blocked has no authoritative map or raw reason.
    - Tests prove transport maps/structs cannot pass SafeObservation construction.
  </acceptance_criteria>
  <verify><automated>mix test test/crosswake/offline/journal_test.exs test/crosswake/offline/replay_test.exs</automated></verify>
  <done>All core retained and wire envelopes are scope-required, all replay outcomes are closed, and sensitive transport values cannot masquerade as observations.</done>
</task>

</tasks>

## Artifacts this phase produces

- `Crosswake.Offline.SafeObservation`, `SafeObservation.new/1`, and `SafeObservation.to_map/1`.
- `Journal.Entry.scope_ref`, `Journal.Entry.scope_version`, `Replay.Request.scope_ref`, `Replay.Request.scope_version`, and `Replay.blocked/2`.
- Exact-scope IndexedDB helpers and the production-quality Playwright tracer in `offline_sync.spec.ts`.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| host session -> browser outbox | A host-issued sensitive scope activates one otherwise inert local partition. |
| browser -> Phoenix replay endpoint | Untrusted scoped envelopes cross into backend authority. |
| replay transport -> observation | Sensitive operational data must be structurally absent from safe output. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-160-01 | Spoofing/Elevation | scope admission and IndexedDB partition | high | mitigate | Required opaque scope equality, exact-scope index APIs, mismatch E2E denial, and zero-effect assertion in Task 1. |
| T-160-02 | Tampering | replay endpoint | high | mitigate | Resolve current host scope before `Study.apply_one/3`; reject before transaction on mismatch. |
| T-160-04 | Information Disclosure | observation/status/error surfaces | high | mitigate | Typed SafeObservation allowlist plus negative canary and non-echo tests in Task 1. |
| T-160-SC | Tampering | package installation | low | accept | No package-manager install occurs; Phase 160 uses the existing dependency set. |
</threat_model>

<verification>
Run the focused core contract suite and the one scoped Playwright tracer from a reset Phoenix test database. The tracer must prove both the accepted and mismatch paths without inspecting or retaining scope bytes outside test-local state.
</verification>

<success_criteria>
The first slice demonstrates SCOPE-01 through SCOPE-05 architecture end to end: required scope, partitioned storage, backend comparison, atomic accepted effect, typed blocked denial, and privacy-safe observation.
</success_criteria>

<output>
Create `.planning/phases/160-scoped-replay-and-auth-safety/160-01-SUMMARY.md` when done.
</output>
