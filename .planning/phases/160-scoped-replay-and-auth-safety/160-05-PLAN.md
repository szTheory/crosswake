---
phase: 160-scoped-replay-and-auth-safety
plan: "05"
type: execute
wave: 5
depends_on: ["160-04"]
files_modified:
  - examples/phoenix_host/priv/static/offline_study.js
  - examples/phoenix_host/e2e/offline_sync.spec.ts
autonomous: true
gap_closure: true
requirements: [SCOPE-02, SCOPE-03]
must_haves:
  truths:
    - "Fence-first logout/account switch revokes the active lease, aborts or awaits the scoped worker, and prevents every later storage or UI side effect from the old epoch (D-03 through D-06, SCOPE-02)."
    - "A successful or non-success response delayed across a scope transition cannot delete old work or overwrite status for the newly active scope."
    - "A successful partial batch with `halted` retains the current and later entries, stops the drain, and renders the calm paused state rather than ordinary sync progress (D-07, D-10 through D-13, D-21, D-23, SCOPE-03)."
  artifacts:
    - path: "examples/phoenix_host/priv/static/offline_study.js"
      provides: "Abortable/awaited flush lifecycle, post-await lease guards, and closed halted-result handling."
    - path: "examples/phoenix_host/e2e/offline_sync.spec.ts"
      provides: "Deterministic post-response race, non-OK race, and mid-batch disablement regressions."
  key_links:
    - from: "fenceScope"
      to: "flushOutbox"
      via: "lease revocation plus AbortController/active flush completion ownership"
      pattern: "AbortController|flushPromise|leaseIsCurrent"
    - from: "SyncController data.halted"
      to: "learner paused status"
      via: "closed response parsing before accepted-only deletion and status rendering"
      pattern: "halted|Sync is paused"
  prohibitions:
    - statement: "MUST NOT mutate storage or UI from an old epoch after any await, including successful deletion/count and non-OK response paths."
      status: unresolved
      verification: flagged-unverified
    - statement: "MUST NOT render a halted or blocked drain as progress, delete its unaccepted records, or start a hot retry."
      status: unresolved
      verification: flagged-unverified
flagged_assumptions:
  - "[UNRESOLVED-SPEC-PROBE] SCOPE-02 is classified as an automated lifecycle race edge; deterministic delayed response/storage cases are the acceptance authority."
  - "[PRESERVE] The browser remains one foreground study island; no background replay, generic sync engine, or new visual system is introduced."
---

<objective>
Make fence-first lifecycle and server disablement hold across every asynchronous browser completion.

Purpose: Close T-160-02 and T-160-06 so old work cannot affect a new scope and blocked work cannot masquerade as progress.
Output: Abortable/awaited scoped flushes, post-await lease guards, halted-result UI handling, and deterministic Playwright races.
</objective>

<artifacts_this_phase_produces>
- Private active flush ownership (`flushPromise` and `AbortController` lifecycle).
- Private closed response classifier for accepted/rejected/halted outcomes.
- Playwright delayed-success, delayed-non-OK, and partial-halt regression cases.
</artifacts_this_phase_produces>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/ADR-FIRST-B2C-ADOPTER.md
@.planning/phases/160-scoped-replay-and-auth-safety/160-CONTEXT.md
@.planning/phases/160-scoped-replay-and-auth-safety/160-PATTERNS.md
@.planning/phases/160-scoped-replay-and-auth-safety/160-VERIFICATION.md
@.planning/phases/160-scoped-replay-and-auth-safety/160-SECURITY.md
@.planning/phases/160-scoped-replay-and-auth-safety/160-04-SUMMARY.md
@examples/phoenix_host/priv/static/offline_study.js
@examples/phoenix_host/e2e/offline_sync.spec.ts
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Revoke and settle an in-flight flush before activating another scope</name>
  <reversibility rating="reversible">The change is private browser lifecycle machinery behind the existing host activation/fence methods.</reversibility>
  <read_first>
    - AGENTS.md
    - .planning/phases/160-scoped-replay-and-auth-safety/160-CONTEXT.md
    - .planning/phases/160-scoped-replay-and-auth-safety/160-VERIFICATION.md
    - .planning/phases/160-scoped-replay-and-auth-safety/160-04-SUMMARY.md
    - examples/phoenix_host/priv/static/offline_study.js
    - examples/phoenix_host/e2e/offline_sync.spec.ts
  </read_first>
  <files>examples/phoenix_host/priv/static/offline_study.js, examples/phoenix_host/e2e/offline_sync.spec.ts</files>
  <behavior>
    - "RED: a delayed successful response reaches the current lease check, then the test fences during an awaited storage operation; the old completion cannot delete or render status."
    - "A delayed forbidden/non-OK response crossing the same fence cannot overwrite the new scope's status."
    - "fenceScope revokes the in-memory lease first, persists stopping, aborts the active request where possible, awaits the owned flush to settle, then persists inactive."
    - "activateScope cannot proceed while stopping/flush settlement is incomplete; a stale finally block cannot clear ownership for a newer worker."
  </behavior>
  <action>Replace the bare `flushing` boolean with invocation-owned active flush state containing the captured scope-plus-epoch lease, an `AbortController`, and a `flushPromise`. `fenceScope` must clear active scope authority before awaiting anything, persist `stopping`, abort the matching fetch, await that invocation's completion, and only then persist `inactive`; `activateScope` continues to require inactive state. In `flushOutbox`, pass the abort signal to fetch and call `leaseIsCurrent(lease)` immediately before every post-await delete, count, style, and text mutation on success, network-error, parse-error, and non-OK branches. Keep old entries on abort/stale completion. Make finally clear only its own active invocation. Add deterministic Playwright barriers around response delivery and IndexedDB completion so success and non-OK tests fence after the old check but before the later side effect, then activate a second scope and assert its status/storage remain unchanged per D-03 through D-06.</action>
  <acceptance_criteria>
    - Fence completes only after the old scoped worker settles and no replacement scope activates during stopping.
    - Old success, abort, parse, network, and non-OK paths perform zero post-fence storage or UI mutations.
    - Ambiguous old records remain retained for idempotent resolution; another scope remains empty and unaffected.
    - Errors and status expose only existing calm copy and stable rules, never scope, payload, endpoint, or backend detail.
  </acceptance_criteria>
  <verify><automated>npm --prefix examples/phoenix_host run proof:offline-island -- --grep "post-response fence blocks old success side effects|post-response fence blocks old denial status"</automated></verify>
  <done>A logout or account switch owns the worker shutdown boundary, and no old epoch can mutate storage or learner status afterward.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Treat a partial halted batch as retained paused work</name>
  <reversibility rating="reversible">The browser consumes an already-defined typed host response without changing the wire schema.</reversibility>
  <read_first>
    - .planning/phases/160-scoped-replay-and-auth-safety/160-CONTEXT.md
    - examples/phoenix_host/priv/static/offline_study.js
    - examples/phoenix_host/lib/crosswake_example/local_first/sync_controller.ex
    - examples/phoenix_host/e2e/offline_sync.spec.ts
  </read_first>
  <files>examples/phoenix_host/priv/static/offline_study.js, examples/phoenix_host/e2e/offline_sync.spec.ts</files>
  <behavior>
    - "A 200 response containing accepted records plus `data.halted` deletes only the accepted prefix and retains the halted current/later records in order."
    - "The client stops the drain with no reconnect timer or immediate retry and renders the existing task-oriented paused copy with error semantics."
    - "Unknown, malformed, or contradictory halted/result shapes fail closed and retain all records not already proven accepted."
    - "A later host authority transition permits a new explicit drain; blocked state alone never changes route mode or queue ownership."
  </behavior>
  <action>Add a closed response classifier before mutation that accepts only arrays of accepted/rejected records plus an absent or declared `halted` class from the Phoenix envelope. When `halted` is present, validate the accepted prefix, delete only those accepted IDs under the current lease, retain the halted and later records, set error semantics, and render exactly the existing calm paused copy from D-21. Return without another flush attempt. Treat malformed/unknown/contradictory response fields as blocked with no deletion. Extend the existing ordered-drain Playwright seam with a mid-batch feature-disable response, assert the first accepted record is removed, N and later records remain ordered, status is paused rather than `Synced`, and no second request occurs until an explicit later authority/reconnect action per D-07 and D-10 through D-13.</action>
  <acceptance_criteria>
    - Partial success removes only server-confirmed accepted entries and never the halted suffix.
    - Learner status is visibly paused/blocked with text semantics and no progress spinner or backend vocabulary.
    - No hot retry, silent online-only fallback, route change, or cross-scope mutation occurs.
    - Unknown response shapes preserve queued work and fail through a stable non-echoing path.
  </acceptance_criteria>
  <verify><automated>npm --prefix examples/phoenix_host run proof:offline-island -- --grep "mid-batch disablement retains halted suffix|malformed halted response fails closed"</automated></verify>
  <done>Server-side disablement is a visible, retained, non-retrying paused state rather than ordinary sync progress.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| async completion -> current browser state | Every storage/UI side effect must re-present the current scope-plus-epoch authority after awaits. |
| typed host batch -> local drain state | A halted result may remove only the accepted prefix and must leave the rest visibly blocked. |

## ASVS L1 Control Mapping

| ASVS category | Required control | Automated evidence |
|---|---|---|
| V3 Session Management | Fence revokes and settles the old worker before replacement activation. | Delayed success/non-OK race cases. |
| V4 Access Control | Every post-await side effect requires the captured current lease. | Storage/UI second-scope assertions. |
| V7 Error Handling | Halted and malformed responses map to closed calm status without detail. | Partial/malformed response cases. |
| V8 Data Protection | Retained status and errors contain no sensitive replay/auth values. | Browser final-byte assertions. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-160-02 | Tampering | Lifecycle and stale async completion | high | mitigate | Task 1 revokes/settles the worker and guards every post-await effect with the captured lease. |
| T-160-06 | Elevation / Denial of Service | Halted replay drain | high | mitigate | Task 2 retains the suffix, stops retry, and renders paused state. |
| T-160-SC | Tampering | Package installs | low | accept | No package-manager installation is required; existing browser proof dependencies are reused. |
</threat_model>

<verification>
Run the two focused Playwright commands and preserve the existing inactive relaunch, switch-before-send, switch-in-flight, and ordered blocked-drain cases.
</verification>

<success_criteria>
All old-epoch completions are inert, and host disablement retains work in a truthful paused state without retry or fallback.
</success_criteria>

<output>
Create `.planning/phases/160-scoped-replay-and-auth-safety/160-05-SUMMARY.md` when done.
</output>
