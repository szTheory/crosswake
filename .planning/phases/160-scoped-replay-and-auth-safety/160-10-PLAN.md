---
phase: 160-scoped-replay-and-auth-safety
plan: "10"
type: execute
wave: 7
depends_on: ["160-08"]
files_modified:
  - examples/phoenix_host/priv/static/offline_study.js
  - examples/phoenix_host/e2e/offline_sync.spec.ts
autonomous: true
gap_closure: true
requirements: [SCOPE-02]
must_haves:
  truths:
    - "An `online` event while the retained lifecycle is inactive creates no replay invocation, request, storage/UI mutation, console output, page error, or unhandled promise rejection (D-03, D-05, SCOPE-02)."
    - "Unexpected online-listener replay failures are caught at the event boundary and may render paused status only while the captured scope-plus-epoch lease remains current (D-04, D-10, D-21)."
    - "Active reconnect replay, fence-first cancellation, stale-completion guards, and retained halted suffix behavior remain unchanged."
  artifacts:
    - path: "examples/phoenix_host/priv/static/offline_study.js"
      provides: "Inactive replay no-op and caught lease-aware online event adapter."
    - path: "examples/phoenix_host/e2e/offline_sync.spec.ts"
      provides: "Inactive-online zero-work/zero-error regression and active reconnect preservation."
  key_links:
    - from: "window online event"
      to: "flushOutbox"
      via: "a private event adapter checks lifecycle authority and catches the returned promise"
      pattern: "online|flushOutbox|catch"
    - from: "unexpected active replay failure"
      to: "renderPausedStatus"
      via: "captured scope-plus-epoch lease equality before UI mutation"
      pattern: "leaseIsCurrent|renderPausedStatus"
  prohibitions:
    - "MUST NOT call `requireActiveLease` or create `activeFlush` from an inactive online event."
    - "MUST NOT log replay errors or let a rejected promise expose scope, payload, endpoint, or backend details."
flagged_assumptions:
  - "[UNRESOLVED-SPEC-PROBE] SCOPE-02 remains unclassified: deterministic inactive reconnect and current-lease failure cases are the automated authority; real host account transitions remain unknown_blocking."
---

<objective>
Make browser reconnect while inactive an explicitly inert, fully caught lifecycle path.

Purpose: Preserve inactive-by-default replay without normal network changes producing unhandled promise failures.
Output: Lease-aware online event handling and deterministic inactive/active reconnect regressions.
</objective>

<artifacts_this_phase_produces>
- A private online replay event adapter in `examples/phoenix_host/priv/static/offline_study.js` that performs the inactive no-op and owns promise rejection handling.
- A Playwright case named for inactive online replay in `examples/phoenix_host/e2e/offline_sync.spec.ts`, plus assertions preserving active reconnect behavior.
</artifacts_this_phase_produces>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/ADR-FIRST-B2C-ADOPTER.md
@.planning/phases/160-scoped-replay-and-auth-safety/160-CONTEXT.md
@.planning/phases/160-scoped-replay-and-auth-safety/160-PATTERNS.md
@.planning/phases/160-scoped-replay-and-auth-safety/160-VERIFICATION.md
@.planning/phases/160-scoped-replay-and-auth-safety/160-08-SUMMARY.md
@examples/phoenix_host/priv/static/offline_study.js
@examples/phoenix_host/e2e/offline_sync.spec.ts

<interfaces>
`flushOutbox/0` owns invocation creation for a valid active lease. The browser event listener is fire-and-forget and therefore must own promise rejection handling. `fenceScope/0`, `leaseIsCurrent/1`, and the current `activeFlush` ownership remain the authority for cancellation and post-await effects.
</interfaces>
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Trace an inactive online event to a zero-work no-op and catch active listener failures</name>
  <reversibility rating="reversible">The change is private browser event wiring behind the existing host lifecycle and replay methods.</reversibility>
  <read_first>
    - AGENTS.md
    - .planning/phases/160-scoped-replay-and-auth-safety/160-CONTEXT.md
    - .planning/phases/160-scoped-replay-and-auth-safety/160-VERIFICATION.md
    - examples/phoenix_host/priv/static/offline_study.js
    - examples/phoenix_host/e2e/offline_sync.spec.ts
  </read_first>
  <files>examples/phoenix_host/priv/static/offline_study.js, examples/phoenix_host/e2e/offline_sync.spec.ts</files>
  <behavior>
    - "RED: after cold inactive launch, dispatching `online` records zero `/study/sync` requests, leaves lifecycle inactive, keeps `activeFlush` absent, preserves paused copy, and emits no `pageerror` or `unhandledrejection`."
    - "The same zero-work behavior holds immediately after `fenceScope/0` completes and before any replacement scope activates."
    - "An unexpected failure during an active listener-triggered flush is caught; paused status is rendered only if the captured lease is still current and no sensitive detail is logged or rendered."
    - "An ordinary active reconnect still sends the exact-scope queue and existing switch-in-flight, post-response fence, and halted-suffix cases remain green."
  </behavior>
  <action>Start with a Playwright regression that installs page-error and unhandled-rejection capture before dispatching an online event in the inactive lifecycle. Add an explicit inactive predicate at the start of `flushOutbox/0`, before `requireActiveLease/0`, invocation construction, or `activeFlush` assignment, and return a resolved no-op when scope or epoch authority is absent. Replace the direct listener reference with a private fire-and-forget online adapter that captures the current lease only for an active scope, invokes `flushOutbox/0`, catches unexpected rejection, and calls `renderPausedStatus/0` only if that captured lease is still current. The catch must discard details without logging or interpolation. Add post-fence inactive and active reconnect assertions while preserving D-03 through D-05 and the existing cancellation ownership.</action>
  <acceptance_criteria>
    - Cold-launch and post-fence online events create no replay work, network request, UI/storage mutation, or rejected-promise signal.
    - The listener owns every returned promise; unexpected active failures cannot become unhandled and stale failures cannot affect replacement-scope status.
    - Existing active reconnect, switch, delayed response, and halted-suffix tests remain green.
    - SCOPE-02, D-03, D-04, D-05, D-10, D-21, ASVS L1 V3, V7, and V8 are covered by browser automation.
  </acceptance_criteria>
  <verify><automated>npm --prefix examples/phoenix_host run proof:offline-island -- --grep "inactive online replay is inert|switch in flight|post-response fence|mid-batch disablement"</automated></verify>
  <done>Network changes while replay is inactive are silent no-ops, while active listener failures are caught without stale or sensitive side effects.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| browser network event -> scoped replay worker | A connectivity signal has no authority to activate a retained scope or create replay work. |
| rejected async replay -> learner UI | Failure handling may affect status only under the same current scope-plus-epoch lease and may expose no sensitive detail. |

## ASVS L1 Control Mapping

| ASVS category | Required control | Automated evidence |
|---|---|---|
| V3 Session Management | Inactive and fenced lifecycle states cannot acquire a replay lease from an online event. | Cold-launch and post-fence Playwright cases. |
| V7 Error Handling and Logging | Event-boundary promise failures are caught without detail or console output. | Page-error, unhandled-rejection, and console capture. |
| V8 Data Protection | Error handling renders only existing calm copy and no scope/payload/endpoint bytes. | Browser byte assertions. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-160-02 | Tampering / Denial of Service | inactive online replay listener | medium | mitigate | Task 1 makes inactive dispatch a zero-work no-op and catches active promise failure under the current lease. |
| T-160-SC | Tampering | package installs | low | accept | No package-manager installation occurs; existing browser and Playwright dependencies are reused. |
</threat_model>

<verification>
Run the focused browser command, then preserve the complete existing offline-island corpus for Plan 160-11's same-tree gate. No manual UAT is required.
</verification>

<success_criteria>
Inactive reconnects are inert and error-free, and active listener failures are contained without weakening scope/epoch fencing.
</success_criteria>

<output>
Create `.planning/phases/160-scoped-replay-and-auth-safety/160-10-SUMMARY.md` when done.
</output>
