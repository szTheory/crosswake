---
phase: 160-scoped-replay-and-auth-safety
plan: "12"
type: execute
wave: 9
depends_on: ["160-11"]
files_modified:
  - examples/phoenix_host/lib/crosswake_example/local_first/replay_admission.ex
  - examples/phoenix_host/lib/crosswake_example/local_first/study.ex
  - examples/phoenix_host/lib/crosswake_example/local_first/review_event.ex
  - examples/phoenix_host/test/crosswake_example/local_first/replay_admission_test.exs
  - examples/phoenix_host/test/crosswake_example/local_first/study_test.exs
  - examples/phoenix_host/test/crosswake_example/local_first/sync_controller_test.exs
  - .planning/phases/160-scoped-replay-and-auth-safety/160-VALIDATION.md
autonomous: true
gap_closure: true
requirements: [SCOPE-03]
must_haves:
  truths:
    - "Phoenix admits a replay event only when its string-key set is exactly `client_mutation_id`, `card_id`, and `rating`; an extra outcome, status, authority, or arbitrary field denies before session, route, feature, Sigra, domain, or persistence work (D-08, SCOPE-03)."
    - "Study persistence reconstructs attributes from the three admitted replay fields plus the host-resolved scope, and ReviewEvent assigns a new row's accepted status server-side; browser input cannot mass-assign persisted outcome/status even if admission is bypassed internally (D-09, D-11, SCOPE-03)."
    - "A hostile extra-field request produces only a closed non-echoing denial and no row, while a direct defense-in-depth persistence probe cannot change the server-owned accepted outcome; existing rejected tombstones remain rejected and retained (D-11, D-12)."
    - "One fresh complete same-tree gate supersedes the post-160-09/10 evidence for this defect while TODO-002, generated iOS/device proof, and independent security remain non-passing or unknown_blocking."
  artifacts:
    - path: "examples/phoenix_host/lib/crosswake_example/local_first/replay_admission.ex"
      provides: "Exact three-key replay-wire admission before current authority callbacks."
    - path: "examples/phoenix_host/lib/crosswake_example/local_first/study.ex"
      provides: "Server-constructed ReviewEvent persistence attributes."
    - path: "examples/phoenix_host/lib/crosswake_example/local_first/review_event.ex"
      provides: "Changeset boundary that cannot cast a caller-selected persisted status."
    - path: "examples/phoenix_host/test/crosswake_example/local_first/sync_controller_test.exs"
      provides: "Hostile extra-field controller-to-persistence regression."
    - path: ".planning/phases/160-scoped-replay-and-auth-safety/160-VALIDATION.md"
      provides: "Fresh post-160-12 focused and complete same-tree evidence reconciliation."
  key_links:
    - from: "untrusted browser event map"
      to: "ReplayAdmission.authorize/4 current-authority chain"
      via: "exact string-key equality plus existing value validation before `resolve(:session, ...)`"
      pattern: "valid_event|client_mutation_id|card_id|rating"
    - from: "admitted replay event"
      to: "ReviewEvent.changeset/2"
      via: "Study-owned persistence attribute constructor that copies only the three wire fields and adds host scope"
      pattern: "Map.take|scope_ref|changeset"
    - from: "ReviewEvent changeset"
      to: "persisted replay outcome"
      via: "server-owned accepted assignment rather than status casting"
      pattern: "accepted|status|put_change"
  prohibitions:
    - statement: "MUST NOT accept unknown replay-wire keys, atomize untrusted keys, forward the original event map to Ecto, or permit caller-controlled status/outcome persistence."
      category: authority-safety
      verification: flagged-unverified
    - statement: "MUST NOT echo hostile field names or values, raw answers, scope references, credentials, tokens, account identifiers, stable device identifiers, or database details in response, log, telemetry, doctor, inspection, aggregate, validation, or proof output."
      category: privacy
      verification: flagged-unverified
    - statement: "MUST NOT pull WR-02 or WR-03 into this bounded closure, infer TODO-002 adopter inputs, promote generated iOS/device or independent security status, or add generic sync, background replay, native storage, Android work, dashboard, commerce, or menu breadth."
      category: scope-boundary
      verification: flagged-unverified
flagged_assumptions:
  - "[UNRESOLVED-SPEC-PROBE] SCOPE-03's deterministic admission edge is an exact three-string-key replay map with the existing closed value domains; real adopter domain fields and reconciliation remain unknown_blocking and are not inferred."
  - "[BOUNDARY ASSUMPTION] A newly applied ReviewEvent has server-owned `accepted` status; persisted `rejected` rows are host-owned tombstones created outside the browser replay-wire changeset and must remain truthful through duplicate/recovery handling."
  - "[PRESERVE] Direct internal callers of Study.apply_one/3 are not a second public wire contract, but persistence still reconstructs an allowlisted map so an admission regression cannot become mass assignment."
---

<objective>
Close the remaining client-controlled replay-status defect at both the Phoenix admission and Ecto persistence boundaries.

Purpose: Restore SCOPE-03 server authority by making the replay wire exact and making persisted outcome assignment exclusively host-owned.
Output: Exact replay-event admission, defense-in-depth persistence allowlisting, hostile extra-field regressions, and fresh focused plus complete same-tree evidence.
</objective>

<artifacts_this_phase_produces>
- Private `ReplayAdmission.@event_keys` (or an equivalently exact private key-set contract) and tightened `valid_event/1` behavior.
- Private `Study.persistence_attrs/2` (or an equivalently named allowlist constructor) that returns only `scope_ref`, `client_mutation_id`, `card_id`, and `rating` for new persistence.
- `ReviewEvent.changeset/2` behavior that does not cast caller-supplied `status` and assigns the new-row `accepted` state server-side.
- Named hostile-extra-field integration cases in `sync_controller_test.exs` and focused admission/persistence regressions in `replay_admission_test.exs` and `study_test.exs`.
- Task rows `160-12-01` and `160-12-02` plus a new dated post-160-12 gate section in `160-VALIDATION.md`.
</artifacts_this_phase_produces>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/ADR-FIRST-B2C-ADOPTER.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/160-scoped-replay-and-auth-safety/160-CONTEXT.md
@.planning/phases/160-scoped-replay-and-auth-safety/160-RESEARCH.md
@.planning/phases/160-scoped-replay-and-auth-safety/160-PATTERNS.md
@.planning/phases/160-scoped-replay-and-auth-safety/160-VERIFICATION.md
@.planning/phases/160-scoped-replay-and-auth-safety/160-REVIEW.md
@.planning/phases/160-scoped-replay-and-auth-safety/160-09-SUMMARY.md
@.planning/phases/160-scoped-replay-and-auth-safety/160-11-SUMMARY.md
@.planning/phases/160-scoped-replay-and-auth-safety/160-VALIDATION.md

<interfaces>
`SyncController.sync_events/4` calls `ReplayAdmission.authorize/4` before `Study.apply_one/3`. Admission owns the exact untrusted wire schema and current authority ordering. Study owns construction of the persistence attributes passed to `ReviewEvent.changeset/2`; the changeset owns field casting/validation but must not grant an external attrs map control of persisted status. Existing accepted/rejected duplicate semantics, scope-conflict handling, and accepted-only response routing remain unchanged.
</interfaces>
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Trace a hostile extra-field replay map to a closed denial and zero persistence</name>
  <reversibility rating="reversible">This tightens private example-host admission and persistence seams without changing the published replay envelope or database schema.</reversibility>
  <read_first>
    - AGENTS.md
    - .planning/phases/160-scoped-replay-and-auth-safety/160-CONTEXT.md
    - .planning/phases/160-scoped-replay-and-auth-safety/160-VERIFICATION.md
    - examples/phoenix_host/lib/crosswake_example/local_first/replay_admission.ex
    - examples/phoenix_host/lib/crosswake_example/local_first/study.ex
    - examples/phoenix_host/lib/crosswake_example/local_first/review_event.ex
    - examples/phoenix_host/lib/crosswake_example/local_first/sync_controller.ex
    - examples/phoenix_host/test/crosswake_example/local_first/sync_controller_test.exs
  </read_first>
  <files>examples/phoenix_host/lib/crosswake_example/local_first/replay_admission.ex, examples/phoenix_host/lib/crosswake_example/local_first/study.ex, examples/phoenix_host/lib/crosswake_example/local_first/review_event.ex, examples/phoenix_host/test/crosswake_example/local_first/sync_controller_test.exs</files>
  <behavior>
    - "RED: a valid three-field event extended with `status: rejected`, `outcome: rejected`, an authority-shaped key, or an arbitrary nested key is blocked as `invalid_envelope` before any authority callback and leaves `review_events` empty."
    - "The unchanged exact three-field event reaches all D-08 authority callbacks in order and persists one accepted row."
    - "Study constructs persistence attributes from the three admitted fields plus the host-resolved scope; the original request map is never passed to Ecto."
    - "ReviewEvent.changeset/2 does not cast caller status and assigns `accepted` for a new authorized event; unique constraints, rating validation, and existing persisted rejected tombstone behavior remain intact."
  </behavior>
  <action>Start with a failing `SyncController.sync_events/4` integration regression that sends otherwise valid events containing hostile extra keys, instruments session/route/feature/Sigra/domain callbacks, and asserts `{:blocked, :invalid_envelope}`, zero callback messages, and zero ReviewEvent rows. Per D-08, define the replay event's exact three string keys once inside `ReplayAdmission` and require exact key-set equality before applying the existing id/card/rating value checks; compare strings directly and do not call atom-converting APIs. Per D-09, replace `Map.put(event, "scope_ref", scope_ref)` in `Study.apply_one/3` with a private allowlist constructor that copies only `client_mutation_id`, `card_id`, and `rating`, then adds the host-resolved `scope_ref`. In `ReviewEvent.changeset/2`, remove `status` from caller-controlled casting and force a new authorized row's status to `accepted` through server-owned changeset logic. Keep D-11 persisted accepted/rejected duplicate mapping and D-12 controller result routing byte-for-byte compatible.</action>
  <acceptance_criteria>
    - Every replay event with any fourth key returns the closed invalid-envelope result before the first authority callback and inserts zero rows.
    - The exact three-key event still completes the existing authority chain and persists one row with status `accepted`.
    - No original untrusted event map is passed to `ReviewEvent.changeset/2`, and the changeset cannot cast a supplied status.
    - Denial/result inspection contains none of the hostile field values, scope fixture, authority details, or database errors.
    - SCOPE-03, D-08, D-09, D-11, D-12, ASVS L1 V4, V5, V7, and V8 are exercised by the focused host integration suite.
  </acceptance_criteria>
  <verify><automated>cd examples/phoenix_host &amp;&amp; MIX_ENV=test mix test test/crosswake_example/local_first/sync_controller_test.exs</automated></verify>
  <done>The browser-to-host tracer accepts only the exact replay wire and cannot persist a browser-selected status or outcome.</done>
</task>

<task type="auto">
  <name>Task 2: Pin both authority layers and reconcile fresh complete same-tree evidence</name>
  <reversibility rating="reversible">The focused regressions and validation ledger can be regenerated without changing runtime or persisted contracts.</reversibility>
  <read_first>
    - .planning/phases/160-scoped-replay-and-auth-safety/160-VERIFICATION.md
    - .planning/phases/160-scoped-replay-and-auth-safety/160-VALIDATION.md
    - examples/phoenix_host/lib/crosswake_example/local_first/replay_admission.ex
    - examples/phoenix_host/lib/crosswake_example/local_first/study.ex
    - examples/phoenix_host/lib/crosswake_example/local_first/review_event.ex
    - examples/phoenix_host/test/crosswake_example/local_first/replay_admission_test.exs
    - examples/phoenix_host/test/crosswake_example/local_first/study_test.exs
    - examples/phoenix_host/test/crosswake_example/local_first/sync_controller_test.exs
  </read_first>
  <files>examples/phoenix_host/test/crosswake_example/local_first/replay_admission_test.exs, examples/phoenix_host/test/crosswake_example/local_first/study_test.exs, examples/phoenix_host/test/crosswake_example/local_first/sync_controller_test.exs, .planning/phases/160-scoped-replay-and-auth-safety/160-VALIDATION.md</files>
  <behavior>
    - "ReplayAdmission table coverage rejects each hostile fourth-key class before session resolution, while the exact key set and existing value boundaries remain accepted."
    - "A direct defense-in-depth call to Study.apply_one/3 with caller `status`, `outcome`, and unrelated fields still persists exactly one accepted row containing only the approved replay fields plus scope."
    - "Pre-existing accepted and rejected tombstones retain their current truthful duplicate/race outcomes; cross-scope conflict, rollback, and concurrent retry tests remain green."
    - "The complete current-tree Phase 160 chain passes after the repair, and validation records aggregate counts/closed IDs only while preserving every external non-passing boundary."
  </behavior>
  <action>Extend `replay_admission_test.exs` with table-driven exact-key coverage and zero-callback assertions for status, outcome, authority-shaped, atom-key, and arbitrary nested extras. Extend `study_test.exs` with a direct defense-in-depth case that bypasses admission, supplies hostile extra fields, and proves one accepted row whose scope/id/card/rating match the approved values and whose status cannot be selected by the caller. Preserve the existing rejected-tombstone fixtures, which use the explicit internal `Ecto.Changeset.change/2` seam rather than the replay changeset. Run the focused complete Phoenix local-first suite, then rerun the exact established Plan 160 same-tree chain on the final tree. Only after both pass, append `160-12-01` and `160-12-02` evidence plus a dated post-160-12 gate to `160-VALIDATION.md`; record safe aggregate counts and closed requirement/threat IDs only. Mark the prior post-160-09/10 gate superseded but retain it as history. Keep TODO-002, adopter-instance completeness, generated iOS/device proof, and independent security explicitly non-passing; do not edit `160-SECURITY.md` or broaden into WR-02/WR-03.</action>
  <acceptance_criteria>
    - Focused ReplayAdmission, Study, and SyncController suites prove exact-key denial, persistence allowlisting, server-owned accepted status, and unchanged accepted/rejected/conflict semantics.
    - The full Phase 160 current-tree chain passes after the production and regression changes, including core, Sigra, Phoenix, browser, generated-host, asserted non-passing iOS prerequisite, planning/adoption, and scoped formatting checks.
    - `160-VALIDATION.md` records observed post-160-12 commands/results and maps SCOPE-03 plus T-160-07 to both focused and complete evidence without raw replay/auth data.
    - The validation ledger explicitly retains TODO-002 as unknown_blocking and generated iOS/device plus independent security as non-passing.
    - No implementation or evidence change addresses WR-02, WR-03, Android, generic/background sync, native storage, dashboard, commerce, or menu breadth.
  </acceptance_criteria>
  <verify><automated>mix test test/crosswake/offline test/crosswake/telemetry_test.exs test/crosswake/proof/phase160_scoped_replay_privacy_test.exs test/crosswake/proof_lane/evidence_test.exs test/crosswake/doctor/doctor_test.exs test/crosswake/operator_inspection/json_formatter_test.exs &amp;&amp; (cd packages/crosswake_sigra &amp;&amp; mix deps.get &amp;&amp; mix test test/crosswake/companions/sigra/contracts_test.exs) &amp;&amp; (cd examples/phoenix_host &amp;&amp; MIX_ENV=test mix test test/crosswake_example/local_first &amp;&amp; npm run proof:offline-island) &amp;&amp; bash script/verify_phoenix_host_proof_lane.sh &amp;&amp; (ios_output=$(bash script/verify_generated_ios_shell.sh --proof-lane 2&gt;&amp;1); ios_status=$?; test "$ios_status" -eq 2 -o "$ios_status" -eq 3; printf '%s\n' "$ios_output" | grep -Eq '"outcome":"(blocked|unavailable)"') &amp;&amp; mix test test/crosswake/planning/first_adopter_context_test.exs test/crosswake/adoption/route_inventory_test.exs &amp;&amp; mix format --check-formatted examples/phoenix_host/lib/crosswake_example/local_first/replay_admission.ex examples/phoenix_host/lib/crosswake_example/local_first/study.ex examples/phoenix_host/lib/crosswake_example/local_first/review_event.ex examples/phoenix_host/test/crosswake_example/local_first/replay_admission_test.exs examples/phoenix_host/test/crosswake_example/local_first/study_test.exs examples/phoenix_host/test/crosswake_example/local_first/sync_controller_test.exs</automated></verify>
  <done>The repaired admission/persistence boundary and every previously passing Phase 160 contract have fresh same-tree automated evidence with no broadened support claim.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| browser replay map -> Phoenix admission | An untrusted map must match the exact three-field wire contract before any current authority callback executes. |
| admitted map -> Study/ReviewEvent persistence | Persistence must reconstruct server-owned attributes and outcome rather than mass-assign the untrusted map. |
| test output -> validation/proof artifacts | Hostile values and sensitive replay/auth facts must not enter durable evidence. |

## ASVS L1 Control Mapping

| ASVS category | Required control | Automated evidence |
|---|---|---|
| V4 Access Control | Client input cannot select stored replay authority state after backend authorization. | Hostile controller-to-database regression and direct Study defense case. |
| V5 Input Validation | Replay admission enforces exact key equality and existing closed value domains. | ReplayAdmission hostile-key table and SyncController integration case. |
| V7 Error Handling and Logging | Unknown fields and persistence defenses return only closed non-echoing results. | Captured result/output assertions and same-tree privacy suite. |
| V8 Data Protection | Hostile payloads, scopes, authority facts, and database details remain absent from evidence and operational egress. | Existing privacy gate plus safe validation reconciliation. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-160-07 | Tampering / Elevation of Privilege | ReplayAdmission, Study, ReviewEvent | high | mitigate | Tasks 1-2 require exact wire keys, reconstruct persistence attributes, force server-owned accepted status, and prove hostile extras cannot alter storage. |
| T-160-08 | Information Disclosure | closed denial and validation evidence | medium | mitigate | Tasks 1-2 assert non-echoing failures and record only safe aggregate same-tree evidence. |
| T-160-SC | Tampering | package installs | low | accept | No dependency is added or changed; the established lockfiles and test commands are reused. |
</threat_model>

<source_coverage_audit>
| Source | ID | Gap-relevant contract | Plan coverage | Status |
|---|---|---|---|---|
| GOAL | Phase 160 | Backend reauthorization remains authoritative through application, not only admission. | 160-12 Tasks 1-2; existing 160-01 through 160-11 retain the other goal clauses. | COVERED |
| REQ | SCOPE-03 | Re-check backend session, route, and server feature state before applying queued mutations. | 160-12 Tasks 1-2 close client-controlled persisted state after those checks. | COVERED |
| REQ | SCOPE-01, SCOPE-02, SCOPE-04, SCOPE-05 | Existing phase requirements unaffected by this blocker. | Existing plans 160-01 through 160-11; complete same-tree regression in Task 2. | COVERED |
| RESEARCH | per-event host admission and one-event Ecto transaction | Sensitive transport input reaches one host-authorized atomic effect only through closed boundaries. | 160-12 Tasks 1-2. | COVERED |
| CONTEXT | D-08 | Closed envelope shape precedes every authority callback. | 160-12 Task 1 exact-key admission. | COVERED |
| CONTEXT | D-09 | Host-owned atomic idempotency/domain application. | 160-12 Tasks 1-2 persistence allowlist and server-owned status. | COVERED |
| CONTEXT | D-11, D-12 | Accepted/rejected outcomes remain explicit and typed through response handling. | 160-12 Tasks 1-2 preserve existing tombstone/controller behavior. | COVERED |
| CONTEXT | D-01 through D-07, D-10, D-13 through D-23 | No new verified blocker; existing executed plans remain authoritative. | Existing 160-01 through 160-11 plus Task 2 regression gate. | COVERED |
| CONTEXT | Deferred ideas | Generic sync/storage, background replay, flag infrastructure, dashboards, Android, auth-provider breadth, and broader UI. | Excluded by user decision and plan prohibitions. | EXCLUDED |
</source_coverage_audit>

<verification>
First run the focused SyncController tracer command. Then run the complete Phoenix local-first suite after the expanded admission and Study tests. Finally run the exact complete same-tree chain in Task 2 and reconcile `160-VALIDATION.md` only from observed final-tree results. No manual UAT or human verification is permitted for this automatable defect.
</verification>

<success_criteria>
- Extra replay-wire keys fail closed before authority resolution and persistence.
- The persistence layer independently prevents client-selected status/outcome through allowlisted attributes and server-owned accepted assignment.
- Hostile-input failures and retained evidence remain privacy-safe and non-echoing.
- Focused and complete same-tree Phase 160 gates pass, with only SCOPE-03 claimed by this gap plan.
- TODO-002, generated iOS/device proof, and independent security remain explicitly non-passing or unknown_blocking.
</success_criteria>

<output>
Create `.planning/phases/160-scoped-replay-and-auth-safety/160-12-SUMMARY.md` when done.
</output>
