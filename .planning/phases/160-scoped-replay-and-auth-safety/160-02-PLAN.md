---
phase: 160-scoped-replay-and-auth-safety
plan: "02"
type: execute
wave: 2
depends_on: [160-01]
files_modified:
  - lib/crosswake/offline/runtime.ex
  - test/crosswake/offline/runtime_test.exs
  - packages/crosswake_sigra/lib/crosswake/companions/sigra/contracts.ex
  - packages/crosswake_sigra/lib/crosswake/companions/sigra/evaluator.ex
  - packages/crosswake_sigra/lib/crosswake/companions/sigra.ex
  - packages/crosswake_sigra/test/crosswake/companions/sigra/contracts_test.exs
  - packages/crosswake_sigra/test/crosswake/companions/sigra/replay_authority_test.exs
autonomous: true
requirements: [SCOPE-01, SCOPE-02, SCOPE-04, SCOPE-05]
must_haves:
  truths:
    - "Retained scopes are inactive after construction/relaunch; only exact host activation creates a scope+epoch lease, and fence-before-switch makes every stale lease inert (D-03 through D-06, SCOPE-02)."
    - "Journal.Entry and Replay.Request require the same versioned opaque scope_ref, and Replay exposes closed accepted/rejected/conflict/blocked transport outcomes without making them observable data (D-01, D-05, D-11, D-15, SCOPE-01/SCOPE-04)."
    - "Sigra replay evaluation consumes backend authority evidence and returns only allow or deny with a closed safe class; absent, incompatible, raised, or malformed adapters deny (D-20, SCOPE-05)."
  artifacts:
    - path: "lib/crosswake/offline/runtime.ex"
      provides: "Pure scoped lifecycle fence and epoch lease contract"
    - path: "packages/crosswake_sigra/lib/crosswake/companions/sigra/evaluator.ex"
      provides: "Closed replay authority projection from backend session evidence"
  key_links:
    - from: "Runtime.activate_scope/2"
      to: "Runtime.lease/1"
      via: "active exact scope and monotonic epoch"
      pattern: "active|epoch|scope_ref"
    - from: "Journal.request_for_entry/1"
      to: "Replay.Request.scope_ref"
      via: "required opaque value copied byte-for-byte without interpretation"
      pattern: "scope_ref"
    - from: "Sigra.evaluate_replay/3"
      to: "Evaluator.evaluate_route_auth/3"
      via: "closed allow/deny projection with sanitized denial class"
      pattern: "evaluate_replay"
  prohibitions:
    - statement: "MUST NOT silently expose, delete, merge, or reactivate a retained old-scope partition during relaunch, logout, or account switching."
      category: product-safety
      verification: flagged-unverified
    - statement: "MUST NOT make Sigra, the shell, or a WebView into credential, token, account-mapping, provider, device-identity, MFA-ceremony, or session-refresh authority."
      category: authority-safety
      verification: flagged-unverified
flagged_assumptions:
  - "[FLAGGED/UNVERIFIED] SCOPE-02 was unclassified by the spec-less edge probe; this plan assumes the required lifecycle edges are cold-launch inactivity, switch-before-send, switch-in-flight, monotonic epoch invalidation, retained queue, and no stale UI/storage mutation."
  - "[FLAGGED/UNVERIFIED] SCOPE-05 was unclassified by the spec-less edge probe; this plan assumes every missing/incompatible/exceptional Sigra adapter result must collapse to one closed denial and never reveal the authority evidence."
---

<objective>
Expand the scoped tracer with a core lifecycle fence and replay-only Sigra adapter.

Purpose: Make account switching and auth projection fail closed before browser/server expansion relies on them.
Output: Pure lifecycle primitives and backend-authority-only Sigra replay decisions consuming Plan 01's scoped contracts.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/160-scoped-replay-and-auth-safety/160-CONTEXT.md
@.planning/phases/160-scoped-replay-and-auth-safety/160-PATTERNS.md
@.planning/phases/160-scoped-replay-and-auth-safety/160-VALIDATION.md
@.planning/phases/160-scoped-replay-and-auth-safety/160-01-SUMMARY.md

<interfaces>
Consume `Journal.Entry.scope_ref`, `SafeObservation.new/1`, and `SafeObservation.to_map/1` from Plan 01.
Existing Sigra seams are `Contracts.AuthContext`, `Evaluator.evaluate_route_auth/3`, and
`Crosswake.Companions.Sigra.evaluate_auth/3`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement the inactive-active-stopping epoch fence</name>
  <reversibility rating="costly">The persisted lifecycle/epoch contract coordinates browser, shell, and later device proof; changing it requires synchronized consumers but no data rewrite beyond the host-owned fence record.</reversibility>
  <read_first>
    .planning/phases/160-scoped-replay-and-auth-safety/160-CONTEXT.md
    .planning/phases/160-scoped-replay-and-auth-safety/160-01-SUMMARY.md
    lib/crosswake/offline/runtime.ex
    lib/crosswake/offline/journal.ex
    lib/crosswake/offline/replay.ex
    test/crosswake/offline/runtime_test.exs
  </read_first>
  <files>lib/crosswake/offline/runtime.ex, test/crosswake/offline/runtime_test.exs</files>
  <behavior>
    - Test: new/reloaded fence is inactive with no readable/displayable scope and cannot issue a lease (D-03).
    - Test: activate_scope accepts only an already host-authorized exact scope and increments/retains monotonic epoch rules without identity derivation (D-01, D-06).
    - Test: begin_stop transitions active to stopping and increments epoch before another scope can activate; old lease equality fails (D-04).
    - Test: missing, malformed, inactive, mismatched, and old-epoch operations return blocked while entries remain unchanged (D-05).
  </behavior>
  <action>Add a pure `Runtime.ScopeFence` struct with closed lifecycle `inactive | active | stopping`, optional sensitive `scope_ref`, and non-negative monotonic `epoch`. Implement exact, non-echoing `new_fence/0`, `activate_scope/2`, `begin_stop/1`, `finish_stop/1`, `lease/1`, and `lease_current?/2`. Per D-03/D-04, a new or restored instance is inactive; stopping increments epoch before any new activation, and no stale completion can mutate a current session. Per D-06, keep account mapping, cleanup/deletion, encryption, retention, and recovery callbacks outside core. Do not add enumeration or all-scope APIs.</action>
  <acceptance_criteria>
    - ScopeFence has only the three closed lifecycle values and a monotonic epoch.
    - Every operation that needs a scope requires an active exact scope+epoch lease.
    - A switch-in-flight unit test proves the old completion cannot update the fence or current entry state.
    - Source contains no account-derived scope, scope hashing, retained-partition enumeration, or cleanup policy.
  </acceptance_criteria>
  <verify><automated>mix test test/crosswake/offline/runtime_test.exs test/crosswake/offline/replay_test.exs</automated></verify>
  <done>The core supplies scope-targeted lifecycle primitives that start inert, fence before switch, and make stale completions mechanically unusable while preserving data.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Close Sigra to safe replay decisions</name>
  <reversibility rating="costly">The replay projection is a shared companion/host contract; incompatible changes require coordinated package and host updates.</reversibility>
  <read_first>
    .planning/phases/160-scoped-replay-and-auth-safety/160-CONTEXT.md
    packages/crosswake_sigra/lib/crosswake/companions/sigra/contracts.ex
    packages/crosswake_sigra/lib/crosswake/companions/sigra/evaluator.ex
    packages/crosswake_sigra/lib/crosswake/companions/sigra.ex
    packages/crosswake_sigra/test/crosswake/companions/sigra/contracts_test.exs
    packages/crosswake_sigra/test/crosswake/companions/sigra/replay_authority_test.exs (new; confirm absent before creating)
    packages/crosswake_sigra/test/crosswake/compatibility/route_gate_test.exs
  </read_first>
  <files>packages/crosswake_sigra/lib/crosswake/companions/sigra/contracts.ex, packages/crosswake_sigra/lib/crosswake/companions/sigra/evaluator.ex, packages/crosswake_sigra/lib/crosswake/companions/sigra.ex, packages/crosswake_sigra/test/crosswake/companions/sigra/contracts_test.exs, packages/crosswake_sigra/test/crosswake/companions/sigra/replay_authority_test.exs</files>
  <behavior>
    - Test: evaluate_replay returns only {:allow, :authorized} or {:deny, closed_class}; missing context, invalid/incompatible results, exceptions, and timeouts deny (D-20).
  </behavior>
  <action>In Sigra contracts add a replay decision type with only `allow` or `deny` and closed safe classes; expose `evaluate_replay/3` through the companion facade by projecting existing backend AuthContext/RouteEntry evaluation. Catch missing modules, malformed returns, exceptions, and bounded adapter timeouts as explicit denial without returning Finding details. Do not add tokens, credentials, account mapping, provider/device fields, auth ceremonies, or session renewal.</action>
  <acceptance_criteria>
    - Sigra replay results contain no AuthContext, SessionAuthorityLane, Finding, details, token, identity, or exact auth-age data.
    - Missing and incompatible Sigra tests return the same closed deny shape without raising.
  </acceptance_criteria>
  <verify><automated>cd packages/crosswake_sigra &amp;&amp; mix test test/crosswake/companions/sigra/contracts_test.exs test/crosswake/companions/sigra/replay_authority_test.exs</automated></verify>
  <done>Sigra provides a fail-closed replay authorization projection without acquiring identity, credential, token, or provider authority.</done>
</task>

</tasks>

## Artifacts this phase produces

- `Crosswake.Offline.Runtime.ScopeFence`, `ScopeLease`, `new_fence/0`, `activate_scope/2`, `begin_stop/1`, `finish_stop/1`, `lease/1`, and `lease_current?/2`.
- `Crosswake.Companions.Sigra.Contracts.ReplayDecision`, `Evaluator.evaluate_replay/3`, and `Crosswake.Companions.Sigra.evaluate_replay/3`.
- `packages/crosswake_sigra/test/crosswake/companions/sigra/replay_authority_test.exs`.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| host lifecycle -> local runtime | Backend-authorized scope activation controls access to retained state. |
| backend session evidence -> Sigra -> core | Sensitive authority evidence is reduced to a closed replay decision. |
| sensitive journal entry -> replay transport | Required scope must remain exact and non-observable. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-160-02 | Spoofing/Elevation | ScopeFence and stale leases | high | mitigate | Inactive default, fence-first epoch increment, exact lease equality, and switch-in-flight tests in Task 1. |
| T-160-03 | Elevation | Sigra replay projection | high | mitigate | Closed projection and fail-closed missing/incompatible/exception tests in Task 2. |
| T-160-04 | Information Disclosure | journal/replay serializers | high | mitigate | Transport-only boundary and SafeObservation rejection tests in Task 2. |
| T-160-SC | Tampering | package installs | low | accept | No new dependency is installed. |
</threat_model>

<verification>
Run focused core and companion suites. Source inspection must confirm no production all-scope operation, wire-to-telemetry serializer, or Sigra identity/token authority was introduced.
</verification>

<success_criteria>
Cold launch is inert, account changes invalidate old work before new activation, every core envelope is scope-required, and Sigra is a closed backend-authority allow/deny adapter only.
</success_criteria>

<output>
Create `.planning/phases/160-scoped-replay-and-auth-safety/160-02-SUMMARY.md` when done.
</output>
