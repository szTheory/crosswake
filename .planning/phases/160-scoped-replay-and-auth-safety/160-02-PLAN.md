---
phase: 160-scoped-replay-and-auth-safety
plan: "02"
type: execute
wave: 2
depends_on: [160-01]
files_modified:
  - lib/crosswake/offline/safe_observation.ex
  - lib/crosswake/offline/telemetry.ex
  - test/crosswake/offline/safe_observation_test.exs
  - test/crosswake/offline/telemetry_test.exs
  - lib/crosswake/proof_lane/evidence.ex
  - test/crosswake/proof_lane/evidence_test.exs
  - test/crosswake/doctor/doctor_test.exs
  - test/crosswake/operator_inspection/json_formatter_test.exs
  - test/crosswake/proof/phase160_scoped_replay_privacy_test.exs
  - .planning/phases/160-scoped-replay-and-auth-safety/160-VALIDATION.md
autonomous: true
requirements: [SCOPE-01, SCOPE-02, SCOPE-03, SCOPE-04, SCOPE-05]
must_haves:
  truths:
    - "Telemetry, Logger, doctor, inspection, aggregates, generated artifacts, and final retained evidence accept only surface-specific closed safe observations; raw answer, scope, event/idempotency/checkpoint, auth/identity, endpoint/flag, media, and correlating hashes are structurally absent (D-15 through D-19, D-22, SCOPE-04)."
    - "The exact Phase 159 twelve-field evidence schema remains intact and carries only named closed Phase 160 assertion IDs; final-byte scanning gates promotion (D-14, D-18, D-19)."
    - "The existing Phase 159 ExUnit and Playwright host seams prove the complete scoped replay cases, while the existing XCTest/XCUITest seam remains deterministically blocked/non-passing without real host adapters; no generated cross-runtime expansion or Phase 161/162 claim is introduced (D-14)."
    - "Learner output is calm, accessible, and task-oriented while operator output uses stable safe rules and owning layers without echoing rejected input (D-21, D-23)."
  artifacts:
    - path: "lib/crosswake/offline/safe_observation.ex"
      provides: "Versioned closed observation type separated from sensitive replay transport"
    - path: "test/crosswake/proof/phase160_scoped_replay_privacy_test.exs"
      provides: "Positive-schema, negative-canary, alias/nesting, and non-vacuity matrix for every egress"
    - path: "lib/crosswake/proof_lane/evidence.ex"
      provides: "Closed Phase 160 assertion vocabulary on the unchanged Phase 159 schema"
    - path: ".planning/phases/160-scoped-replay-and-auth-safety/160-VALIDATION.md"
      provides: "Observed requirement, ASVS, and threat evidence mapping"
  key_links:
    - from: "Replay accepted/rejected/conflict/blocked state"
      to: "SafeObservation.new/1"
      via: "explicit closed projection, never transport-map redaction"
      pattern: "SafeObservation"
    - from: "SafeObservation"
      to: "telemetry/Logger/doctor/inspection/aggregate"
      via: "surface-specific exact allowlists"
      pattern: "to_map|metadata"
    - from: "Phase 160 assertion results"
      to: "Evidence.promote/3"
      via: "unchanged exact schema, completion-digest snapshot, and final-byte scan"
      pattern: "assertion_ids|promote"
  prohibitions:
    - statement: "MUST NOT emit, attach, hash, correlate, or retain raw/selected/free-form answers, scopes, mutation references, checkpoints, identities, credentials, tokens, exact auth facts, endpoints, flags, media, or host authority maps for debugging or proof convenience."
      category: privacy
      verification: flagged-unverified
    - statement: "MUST NOT delete rejected, conflict, blocked, or ambiguous entries; silently retry or drop them; silently change the route to online-only; or present a blocked drain as progress."
      category: product-safety
      verification: flagged-unverified
    - statement: "MUST NOT convert missing host authority, blocked/unavailable native execution, unknown adopter inputs, or absent physical-device evidence into passing, skipped, or support-promoting proof."
      category: transparency
      verification: flagged-unverified
flagged_assumptions:
  - "[FLAGGED/UNVERIFIED] SCOPE-04 remains an external edge assumption: every named egress, alias, nested value, printable canary, opaque identifier, and stable hash requires positive allowlist proof plus negative final-byte exclusion; real adopter payloads remain forbidden test inputs."
---

<objective>
Close the scoped replay tracer with the essential privacy-safe observation and retained-evidence proof.

Purpose: Make the production replay path operable without allowing sensitive wire/auth data into any diagnostic or proof plane.
Output: Closed observation contract, all-egress canary proof, unchanged-schema evidence assertions, and a fresh automated Phase 160 validation gate.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/ADR-FIRST-B2C-ADOPTER.md
@.planning/phases/160-scoped-replay-and-auth-safety/160-CONTEXT.md
@.planning/phases/160-scoped-replay-and-auth-safety/160-RESEARCH.md
@.planning/phases/160-scoped-replay-and-auth-safety/160-VALIDATION.md
@.planning/phases/159-host-reusable-proof-lane/159-28-SUMMARY.md
@.planning/phases/160-scoped-replay-and-auth-safety/160-01-SUMMARY.md

<interfaces>
Consume Plan 01's required scoped Entry/Request, closed replay outcomes, host admission classes, browser lifecycle states, and atomic result. Preserve Phase 159 Evidence's exact twelve keys and its existing scan/check/promote lifecycle. The generated native seam stays at its existing blocked/unavailable non-passing boundary until later real host/device inputs exist.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Make closed SafeObservation the only operational egress input</name>
  <reversibility rating="costly">D-16 establishes a versioned public observation vocabulary; incompatible changes require explicit schema/version adapters.</reversibility>
  <read_first>
    .planning/phases/160-scoped-replay-and-auth-safety/160-CONTEXT.md
    .planning/phases/160-scoped-replay-and-auth-safety/160-01-SUMMARY.md
    lib/crosswake/offline/telemetry.ex
    lib/crosswake/offline/safe_observation.ex (new; confirm absent before creating)
    test/crosswake/offline/telemetry_test.exs
    test/crosswake/doctor/doctor_test.exs
    test/crosswake/operator_inspection/json_formatter_test.exs
    test/crosswake/proof/phase160_scoped_replay_privacy_test.exs (new; confirm absent before creating)
  </read_first>
  <files>lib/crosswake/offline/safe_observation.ex, lib/crosswake/offline/telemetry.ex, test/crosswake/offline/safe_observation_test.exs, test/crosswake/offline/telemetry_test.exs, test/crosswake/doctor/doctor_test.exs, test/crosswake/operator_inspection/json_formatter_test.exs, test/crosswake/proof/phase160_scoped_replay_privacy_test.exs</files>
  <behavior>
    - Test: the versioned constructor accepts only declared route/runtime/lifecycle/outcome/denial enums and bounded measurements; unknown keys/values fail with stable rule/path output (D-15, D-16).
    - Test: scope, payload, selected/free-form answers, event/idempotency/correlation/checkpoint refs, authority maps, rejection reasons, identities, credentials/tokens, exact auth ages, Sigra ceremony refs, endpoints, flags, media, aliases, nested values, and stable hashes fail before serialization (D-17, D-19, D-22).
    - Test: telemetry/Logger receives route plus only its closed lifecycle/outcome/denial/measurements; doctor remains static adapter/config readiness; inspection reports route/contract readiness; aggregates expose no content or active scope (D-18).
    - Test: deleting any named egress assertion makes the privacy matrix fail, proving non-vacuous coverage (D-22).
  </behavior>
  <action>Create a transport-independent SafeObservation struct whose constructor accepts only explicit closed fields, never Entry, Request, Outcome, authority records, or arbitrary maps. Make Offline.Telemetry consume this type and project an exact smaller map. Extend the existing tests, not the public surfaces, so doctor remains static, inspection remains route/contract-only, and Logger/aggregates receive only their declared ceilings. Build a shared egress×canary matrix with positive schemas, forbidden aliases/nesting, printable canaries, opaque identifiers, and stable digests; capture final serialized bytes and stable non-echoing errors. Add a deletion control for every egress row. Keep host user copy and operator rules separate per D-21.</action>
  <acceptance_criteria>
    - No operational egress accepts a replay transport struct or free-form metadata/details map.
    - Every egress has an exact positive schema, negative primary/alias/nested/hash canaries, and a non-vacuity deletion control.
    - Failure output contains only stable rule, owning layer, and safe path/class; it never contains rejected bytes.
    - ASVS L1 V7 Error Handling/Logging and V8 Data Protection are evidenced across telemetry, Logger, doctor, inspection, and aggregate serialized bytes.
  </acceptance_criteria>
  <verify><automated>mix test test/crosswake/offline/safe_observation_test.exs test/crosswake/offline/telemetry_test.exs test/crosswake/doctor/doctor_test.exs test/crosswake/operator_inspection/json_formatter_test.exs test/crosswake/proof/phase160_scoped_replay_privacy_test.exs</automated></verify>
  <done>Every operational surface is allowlisted by construction, non-echoing, and non-vacuously proven free of replay, payload, identity, credential, and correlation data.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Carry only closed scoped-replay assertions through existing proof evidence</name>
  <reversibility rating="costly">Closed assertion identifiers are retained-evidence vocabulary and incompatible removal requires a version adapter.</reversibility>
  <read_first>
    .planning/phases/159-host-reusable-proof-lane/159-28-SUMMARY.md
    .planning/phases/160-scoped-replay-and-auth-safety/160-CONTEXT.md
    .planning/phases/160-scoped-replay-and-auth-safety/160-01-SUMMARY.md
    lib/crosswake/proof_lane/evidence.ex
    test/crosswake/proof_lane/evidence_test.exs
    test/crosswake/proof/phase160_scoped_replay_privacy_test.exs
    script/verify_phoenix_host_proof_lane.sh
    script/verify_generated_ios_shell.sh
  </read_first>
  <files>lib/crosswake/proof_lane/evidence.ex, test/crosswake/proof_lane/evidence_test.exs, test/crosswake/proof/phase160_scoped_replay_privacy_test.exs</files>
  <behavior>
    - Test: Evidence keys remain the exact Phase 159 twelve-field set and accept only closed assertion IDs for scope partition, logout/switch fence, per-event reauthorization, atomic idempotency, safe observation, and feature disablement (D-14, D-18).
    - Test: missing protected assertions, raw/alias/nested canaries, opaque identifiers, and correlating hashes fail build/check/promote without echo; final retained bytes contain none of the injected values (D-17, D-19, D-22).
    - Test: Phase 159's existing generated ExUnit/Playwright host path consumes the real Plan 01 assertions; unchanged XCTest/XCUITest execution without later host adapters remains blocked/unavailable and cannot promote evidence (D-14).
  </behavior>
  <action>Add only the six closed Phase 160 assertion IDs to Evidence and preserve its exact keys, approved canonical-source hashing, completion-digest snapshot, final-byte scanner, and atomic promotion. Bind the IDs to the real Plan 01 host/Playwright result and Task 1 egress matrix; do not attach raw test output. Per D-14, extend the existing concrete ExUnit/Playwright/evidence seam for the required deterministic cases and run the existing generated iOS verifier to assert that missing scoped host/device adapters stay blocked or unavailable. Do not add generated cross-runtime template breadth or claim Phase 161/162 outcomes.</action>
  <acceptance_criteria>
    - Evidence Map.keys is unchanged and only assertion_ids vocabulary expands.
    - Every closed ID is backed by the actual scoped host/browser/privacy command, not a self-attested boolean.
    - The final-byte scanner rejects every forbidden canary class and protected assertion omission is non-passing.
    - Existing generated native proof remains non-passing without real host adapters and cannot promote adopter or device support.
    - ASVS L1 V4 Access Control, V7 Error Handling/Logging, and V8 Data Protection are evidenced by content-bound closed assertions and the final-byte promotion scan.
  </acceptance_criteria>
  <verify><automated>mix test test/crosswake/proof_lane/evidence_test.exs test/crosswake/proof/phase160_scoped_replay_privacy_test.exs &amp;&amp; bash script/verify_phoenix_host_proof_lane.sh &amp;&amp; bash script/verify_generated_ios_shell.sh</automated></verify>
  <done>The existing proof lane retains only content-bound closed Phase 160 results on the unchanged schema, and missing native/adopter prerequisites remain deterministically non-passing.</done>
</task>

<task type="auto">
  <name>Task 3: Run one fresh complete Phase 160 gate and record observed evidence</name>
  <reversibility rating="reversible">The validation ledger records current-tree evidence and can be regenerated without changing runtime contracts.</reversibility>
  <read_first>
    .planning/phases/160-scoped-replay-and-auth-safety/160-VALIDATION.md
    .planning/phases/160-scoped-replay-and-auth-safety/160-01-SUMMARY.md
    test/crosswake/proof/phase160_scoped_replay_privacy_test.exs
    script/verify_phoenix_host_proof_lane.sh
    script/verify_generated_ios_shell.sh
  </read_first>
  <files>.planning/phases/160-scoped-replay-and-auth-safety/160-VALIDATION.md</files>
  <action>Run one same-tree gate covering the scoped core contracts and lifecycle, complete Phoenix admission/idempotency suite, primary Playwright offline-island corpus, SafeObservation and all-egress matrix, exact-schema Evidence build/check/promote scan, existing generated host proof, existing generated iOS non-passing prerequisite behavior, the repository privacy check, and formatting. Update 160-VALIDATION.md only from observed commands, mapping SCOPE-01 through SCOPE-05, ASVS L1 V2/V3/V4/V5/V7/V8, and T-160-01 through T-160-06. Preserve the five flagged external edge assumptions, TODO-002, real scope/route/flag/session inputs, and physical-iPhone proof as unknown_blocking rather than fabricating fixtures into support claims.</action>
  <acceptance_criteria>
    - One fresh current-tree command chain covers all five requirements, six threat IDs, and all six applicable ASVS L1 categories.
    - Validation records exact commands and observed pass/fail state; stale summaries or blocked/unavailable native output cannot close the phase.
    - TODO-002 and real adopter/device prerequisites remain unknown_blocking and no Phase 161/162 outcome is claimed.
  </acceptance_criteria>
  <verify><automated>mix test test/crosswake/offline test/crosswake/proof/phase160_scoped_replay_privacy_test.exs test/crosswake/proof_lane/evidence_test.exs test/crosswake/doctor/doctor_test.exs test/crosswake/operator_inspection/json_formatter_test.exs &amp;&amp; (cd examples/phoenix_host &amp;&amp; MIX_ENV=test mix test test/crosswake_example/local_first &amp;&amp; npm run proof:offline-island) &amp;&amp; bash script/verify_phoenix_host_proof_lane.sh &amp;&amp; bash script/verify_generated_ios_shell.sh &amp;&amp; mix crosswake.first_b2c_adopter.check &amp;&amp; mix format --check-formatted</automated></verify>
  <done>Fresh automated evidence covers SCOPE-01 through SCOPE-05 and every high threat without sensitive output, manual UAT, generated breadth, or downstream support promotion.</done>
</task>

</tasks>

## Multi-Source Coverage Audit

| SOURCE | ID | Feature/Requirement | Plan | Status | Notes |
|---|---|---|---|---|---|
| GOAL | — | Account-scoped outboxes, payload redaction, backend reauthorization, auth continuity, and server-side disablement | 01-02 | COVERED | Complete Study-event tracer plus essential privacy proof. |
| REQ | SCOPE-01 | Required opaque scoped envelopes and partitioned outbox | 01 | COVERED | Core, browser, and host tracer. |
| REQ | SCOPE-02 | Logout/switch stop and cross-scope denial | 01 | COVERED | Persisted epoch fence and race proof. |
| REQ | SCOPE-03 | Per-event session, route, and feature reauthorization | 01 | COVERED | Complete D-08 admission order before first effect. |
| REQ | SCOPE-04 | No raw answer egress | 02 | COVERED | Typed observation plus every-egress/final-byte matrix. |
| REQ | SCOPE-05 | Sigra backend-authority adapter only | 01 | COVERED | Existing optional adapter reduced to closed host admission result. |
| RESEARCH | — | One DB with scoped indexes, inactive epoch fence, serial event transaction, typed observation, and reused Phase 159 proof | 01-02 | COVERED | No new dependency, sync engine, flag service, or proof pipeline. |
| CONTEXT | D-01..D-06 | Scope lifecycle and host ownership | 01 | COVERED | Tracer and lifecycle task cite every decision. |
| CONTEXT | D-07..D-13 | Serial replay, ordered admission, atomicity, outcomes, HTTP, and disablement | 01 | COVERED | First effect is fully authorized and atomic. |
| CONTEXT | D-14 | Reuse Phase 159 proof seams and required deterministic cases | 01-02 | COVERED | Host ExUnit/Playwright/evidence carry assertions; existing native seam stays non-passing without later adapters. |
| CONTEXT | D-15..D-19 | Separate safe plane and surface-specific allowlists | 02 | COVERED | SafeObservation and exact egress/evidence schemas. |
| CONTEXT | D-20 | Sigra remains closed backend evidence adapter | 01 | COVERED | No credential/token/account authority added. |
| CONTEXT | D-21..D-23 | Safe copy, canary proof, accessible state | 01-02 | COVERED | Learner state in Plan 01; all-egress proof in Plan 02. |
| EDGE | SCOPE-01..05 | Five unresolved external edge assumptions | 01-02 | COVERED | Exactly five flagged assumptions retained; none inferred or dismissed. |
| PROHIBITION | — | No silent drain/drop/fallback, authority bypass, sensitive egress, or false proof promotion | 01-02 | COVERED | Seven explicit flagged-unverified prohibitions retained. |

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| sensitive replay/auth transport -> operational egress | Wire/domain data must be structurally unavailable to telemetry, logs, diagnostics, inspection, and aggregates. |
| automated assertion -> retained evidence | Only content-bound closed IDs may cross the exact Phase 159 schema. |
| generated proof -> support claim | Blocked/unavailable or unknown external inputs cannot become passing support evidence. |

## ASVS L1 Control Mapping

| ASVS L1 category | Required control | Task | Automated evidence |
|---|---|---|---|
| V2 Authentication | Carry only the closed result of current backend/Sigra authentication evidence; retain no session details. | Tasks 1, 3 | All-egress matrix and complete Phase 160 gate. |
| V3 Session Management | Retain only closed lifecycle results from Plan 01; never expose active scope or auth age. | Tasks 1, 3 | SafeObservation schema and full lifecycle/privacy command. |
| V4 Access Control | Bind evidence IDs to real per-event route/feature/Sigra/domain denial tests. | Tasks 2, 3 | Evidence content-binding tests and complete host gate. |
| V5 Input Validation | Reject unknown observation/evidence keys, values, aliases, nesting, and protected-path omission. | Tasks 1, 2 | SafeObservation and Evidence negative/property suites. |
| V7 Error Handling and Logging | Stable safe rule/layer/path output only; telemetry and Logger accept closed observation input. | Tasks 1, 3 | Logger/telemetry captured-byte and non-echo tests. |
| V8 Data Protection | Exclude payload, scope, identity, credentials, references, media, and correlating hashes through final retained bytes. | Tasks 1, 2, 3 | Egress×canary matrix, final-byte Evidence scan, and repository privacy check. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-160-01 | Spoofing | cross-scope evidence claim | high | mitigate | Task 2 accepts only content-bound closed IDs backed by Plan 01's real exact-scope command. |
| T-160-02 | Tampering | lifecycle assertion | high | mitigate | Task 2 binds logout/switch evidence to Plan 01 runtime/browser results; Task 3 reruns them. |
| T-160-03 | Elevation | auth/feature assertion | high | mitigate | Tasks 2-3 bind and rerun complete per-event host denial proof. |
| T-160-04 | Information Disclosure | telemetry/Logger/doctor/inspection/aggregate/evidence | high | mitigate | Task 1 owns the operational allowlists and canary matrix; Task 2 owns exact evidence and final-byte scanning; Task 3 reruns both. |
| T-160-05 | Tampering/Repudiation | atomicity evidence | high | mitigate | Task 2 binds the ID to Plan 01 rollback/lost-response/duplicate tests; Task 3 reruns the host suite. |
| T-160-06 | Elevation/Denial of Service | disabled-path evidence | high | mitigate | Task 2 binds queue-retained blocked evidence; Task 3 reruns entry/replay disablement and no-hot-retry proof. |
| T-160-SC | Tampering | package installs | low | accept | No package-manager installation occurs; this plan uses existing test and proof infrastructure. |
</threat_model>

<verification>
Run the three task commands. Confirm the validation ledger names every requirement, ASVS category, and threat, and that retained bytes contain only the unchanged evidence schema plus closed assertion IDs.
</verification>

<success_criteria>
All operational and retained surfaces are allowlisted and canary-free, the real complete tracer is content-bound to evidence, and unknown adopter/native/device prerequisites remain non-passing.
</success_criteria>

<output>
Create `.planning/phases/160-scoped-replay-and-auth-safety/160-02-SUMMARY.md` when done.
</output>
