---
phase: 160-scoped-replay-and-auth-safety
plan: "07"
type: execute
wave: 4
depends_on: ["160-03"]
files_modified:
  - examples/phoenix_host/priv/repo/migrations/20260802170000_restore_review_event_idempotency_guard.exs
  - examples/phoenix_host/lib/crosswake_example/local_first/review_event.ex
  - examples/phoenix_host/lib/crosswake_example/local_first/study.ex
  - examples/phoenix_host/test/crosswake_example/local_first/study_test.exs
autonomous: true
gap_closure: true
requirements: [SCOPE-01, SCOPE-03]
must_haves:
  truths:
    - "A legacy `review_events` row with null scope remains a global idempotency tombstone: replaying its client mutation ID returns the prior accepted outcome and cannot repeat the domain effect (D-09, SCOPE-01, SCOPE-03)."
    - "New rows retain required scope-qualified uniqueness, while an ID already owned by another non-null scope fails closed as conflict without exposing or applying that other scope's record."
    - "The corrective migration is additive and safe for databases that already ran the original Phase 160 migration; it restores protection without guessing a legacy row's account scope."
  artifacts:
    - path: "examples/phoenix_host/priv/repo/migrations/20260802170000_restore_review_event_idempotency_guard.exs"
      provides: "Additive restoration of the global client mutation unique guard."
    - path: "examples/phoenix_host/lib/crosswake_example/local_first/study.ex"
      provides: "Legacy-null, same-scope duplicate, cross-scope conflict, and new-event atomic decision handling."
    - path: "examples/phoenix_host/test/crosswake_example/local_first/study_test.exs"
      provides: "Old-row duplicate, cross-scope collision, rollback, and concurrent retry regressions."
  key_links:
    - from: "ReviewEvent client_mutation_id"
      to: "Study.apply_one/3 idempotency decision"
      via: "global lookup inside the same Ecto.Multi transaction before effect"
      pattern: "client_mutation_id|Ecto.Multi"
    - from: "database unique constraints"
      to: "closed duplicate/conflict result"
      via: "both global and scope-qualified constraint mapping without changeset detail leakage"
      pattern: "unique_constraint|scope_ref"
  prohibitions:
    - statement: "MUST NOT backfill, infer, or assign a legacy null scope from the replaying account or mutation contents."
      status: unresolved
      verification: flagged-unverified
    - statement: "MUST NOT treat a cross-scope ID collision as a new event, repeat its effect, or reveal the existing record/scope."
      status: unresolved
      verification: flagged-unverified
flagged_assumptions:
  - "[UNRESOLVED-SPEC-PROBE] SCOPE-01/SCOPE-03 idempotency is classified as an automated database edge; a global legacy tombstone plus exact-scope uniqueness is the fail-closed authority."
  - "[PRESERVE] No real account mapping is available for old rows, so the repair keeps null as unassigned and never fabricates adopter data."
---

<objective>
Restore legacy idempotency protection without inventing scope assignments for existing rows.

Purpose: Close T-160-05 so the scope migration cannot repeat an already committed Study effect.
Output: Additive database guard, atomic decision handling, and migration regression tests.
</objective>

<artifacts_this_phase_produces>
- Migration module `CrosswakeExample.Repo.Migrations.RestoreReviewEventIdempotencyGuard`.
- Private `Study` idempotency states for `:new`, legacy/same-scope duplicate, and cross-scope conflict.
- Legacy-row and cross-scope regression fixtures in `study_test.exs`.
</artifacts_this_phase_produces>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/ADR-FIRST-B2C-ADOPTER.md
@.planning/phases/160-scoped-replay-and-auth-safety/160-CONTEXT.md
@.planning/phases/160-scoped-replay-and-auth-safety/160-PATTERNS.md
@.planning/phases/160-scoped-replay-and-auth-safety/160-VERIFICATION.md
@.planning/phases/160-scoped-replay-and-auth-safety/160-SECURITY.md
@examples/phoenix_host/priv/repo/migrations/20260518213507_create_review_events.exs
@examples/phoenix_host/priv/repo/migrations/20260802160000_scope_review_events.exs
@examples/phoenix_host/lib/crosswake_example/local_first/review_event.ex
@examples/phoenix_host/lib/crosswake_example/local_first/study.ex
@examples/phoenix_host/test/crosswake_example/local_first/study_test.exs
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Keep one legacy accepted effect idempotent after scope migration</name>
  <reversibility rating="costly">Adding/restoring a database uniqueness contract affects persisted host rows and later reversal requires a migration.</reversibility>
  <read_first>
    - AGENTS.md
    - .planning/phases/160-scoped-replay-and-auth-safety/160-CONTEXT.md
    - .planning/phases/160-scoped-replay-and-auth-safety/160-VERIFICATION.md
    - examples/phoenix_host/priv/repo/migrations/20260518213507_create_review_events.exs
    - examples/phoenix_host/priv/repo/migrations/20260802160000_scope_review_events.exs
    - examples/phoenix_host/lib/crosswake_example/local_first/review_event.ex
    - examples/phoenix_host/lib/crosswake_example/local_first/study.ex
    - examples/phoenix_host/test/crosswake_example/local_first/study_test.exs
  </read_first>
  <files>examples/phoenix_host/priv/repo/migrations/20260802170000_restore_review_event_idempotency_guard.exs, examples/phoenix_host/lib/crosswake_example/local_first/review_event.ex, examples/phoenix_host/lib/crosswake_example/local_first/study.ex, examples/phoenix_host/test/crosswake_example/local_first/study_test.exs</files>
  <behavior>
    - "RED: insert a pre-scope accepted row with `scope_ref: nil`, replay the same mutation ID under a valid scope, and observe accepted with one row/effect."
    - "An additive migration recreates the global unique index on `client_mutation_id` without modifying legacy scope bytes and preserves the scoped unique index."
    - "Inside one Ecto.Multi transaction, a global lookup treats null-scope and same-scope rows as accepted duplicates; no new effect begins."
    - "Migration up/down and a fresh database both retain a coherent index set with non-echoing constraint handling."
  </behavior>
  <action>Create the timestamped corrective migration rather than rewriting the already-runnable original migration. Recreate the canonical global unique index on `review_events.client_mutation_id` while retaining the `(scope_ref, client_mutation_id)` index; do not update null scope values. In `ReviewEvent.changeset/2`, map both constraint names to stable closed validation. In `Study.apply_one/3`, perform the idempotency lookup by global client mutation ID inside the existing `Ecto.Multi`: no row is `:new`; a row with null scope or matching scope is an accepted duplicate returned without insert/effect; a row with a different non-null scope is a closed scope conflict. Add direct legacy-row fixture insertion that bypasses the new required changeset, run the migration-shaped database, and prove accepted retry has one row/effect per D-09.</action>
  <acceptance_criteria>
    - Existing null-scope rows remain byte-identical and globally unique after the corrective migration.
    - Replaying a legacy accepted ID returns accepted and performs no insert or domain effect.
    - New scoped rows are protected by both global and scope-qualified uniqueness.
    - Constraint and conflict results expose no mutation ID, scope, row, or database detail.
  </acceptance_criteria>
  <verify><automated>cd examples/phoenix_host &amp;&amp; MIX_ENV=test mix test test/crosswake_example/local_first/study_test.exs</automated></verify>
  <done>A legacy committed mutation cannot be applied again after the scope schema change, without fabricating account ownership.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prove cross-scope and concurrent retries remain closed and atomic</name>
  <reversibility rating="reversible">The task expands deterministic tests around the established database contract.</reversibility>
  <read_first>
    - .planning/phases/160-scoped-replay-and-auth-safety/160-CONTEXT.md
    - examples/phoenix_host/lib/crosswake_example/local_first/review_event.ex
    - examples/phoenix_host/lib/crosswake_example/local_first/study.ex
    - examples/phoenix_host/test/crosswake_example/local_first/study_test.exs
  </read_first>
  <files>examples/phoenix_host/lib/crosswake_example/local_first/study.ex, examples/phoenix_host/test/crosswake_example/local_first/study_test.exs</files>
  <behavior>
    - "The same ID already owned by another non-null scope returns conflict/blocked and never applies or exposes that record."
    - "Two concurrent same-scope attempts produce one row/effect and two closed accepted results or one accepted plus a normalized retry result, never a leaked constraint error."
    - "Forced rollback still leaves neither idempotency row nor domain effect; the next valid retry may succeed exactly once."
    - "All results remain compatible with accepted-only browser deletion and retained conflict/blocked semantics."
  </behavior>
  <action>Normalize global-constraint races after `Repo.transaction/1`: when a concurrent insert loses, re-read by client mutation ID and return the same legacy/same-scope duplicate or cross-scope conflict decision instead of `transaction_failed` detail. Keep the lookup and effect inside host-owned transaction semantics; do not return the existing row. Add cross-scope, concurrent same-scope, rollback-then-retry, and lost-response retry cases with independently generated opaque IDs. Assert exact row/effect counts and closed outcomes consumed by `SyncController`, preserving D-09 through D-12.</action>
  <acceptance_criteria>
    - Cross-scope collision performs zero new effects and returns only a closed retained outcome.
    - Concurrent same-scope retries produce exactly one committed row/effect and no raw constraint error.
    - Rollback and later retry remain atomic and idempotent.
    - Accepted duplicate remains deleteable; conflict/blocked remains retained by the client contract.
  </acceptance_criteria>
  <verify><automated>cd examples/phoenix_host &amp;&amp; MIX_ENV=test mix test test/crosswake_example/local_first/study_test.exs test/crosswake_example/local_first/sync_controller_test.exs</automated></verify>
  <done>Legacy, same-scope, cross-scope, concurrent, rollback, and retry paths preserve one atomic effect and closed outcomes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| legacy database row -> scoped replay | A pre-scope accepted row must remain authoritative without being assigned to a current account. |
| transaction constraint -> replay result | Database races/details must collapse to an accepted duplicate or closed retained conflict. |

## ASVS L1 Control Mapping

| ASVS category | Required control | Automated evidence |
|---|---|---|
| V4 Access Control | Cross-scope collisions never become effects or reveal rows. | Cross-scope fixture. |
| V5 Input Validation | Required new scopes and opaque IDs remain changeset/contract validated. | Changeset and Study cases. |
| V7 Error Handling | Constraint races collapse to closed outcomes. | Concurrent retry cases. |
| V8 Data Protection | No legacy/current scope, ID, or row detail leaves the host transaction. | Captured outcome assertions. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-160-05 | Tampering / Repudiation | ReviewEvent migration and Study effect | high | mitigate | Tasks 1-2 restore the global tombstone, preserve scoped uniqueness, and normalize races atomically. |
| T-160-SC | Tampering | Package installs | low | accept | No package-manager installation occurs; existing Ecto/Phoenix dependencies are reused. |
</threat_model>

<verification>
Run both focused host commands against the migrated test database and preserve all existing admission/controller tests.
</verification>

<success_criteria>
No pre-scope, same-scope, cross-scope, concurrent, rollback, or lost-response retry can repeat a committed Study effect.
</success_criteria>

<output>
Create `.planning/phases/160-scoped-replay-and-auth-safety/160-07-SUMMARY.md` when done.
</output>
