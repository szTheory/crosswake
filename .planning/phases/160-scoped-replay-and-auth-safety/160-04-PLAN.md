---
phase: 160-scoped-replay-and-auth-safety
plan: "04"
type: execute
wave: 4
depends_on: ["160-03"]
files_modified:
  - examples/phoenix_host/priv/static/offline_study.js
  - examples/phoenix_host/e2e/offline_sync.spec.ts
autonomous: true
gap_closure: true
requirements: [SCOPE-01, SCOPE-02]
must_haves:
  truths:
    - "Opening a legacy offline-study database atomically quarantines every unscoped mutation without assigning it to the current account, rendering its payload, or making it eligible for replay (D-01 through D-06, SCOPE-01, SCOPE-02)."
    - "Only an explicit host call made after backend-authorized scope activation can move quarantined mutations into that exact scope; relaunch, cookies, or a different active account cannot claim them (D-03 through D-06)."
    - "Interrupted upgrade or recovery leaves one complete source of truth: the legacy/quarantine record or the exact-scope record, never a silently missing or multiply replayable mutation."
  artifacts:
    - path: "examples/phoenix_host/priv/static/offline_study.js"
      provides: "Version-4 IndexedDB quarantine and explicit host-authorized exact-scope recovery."
    - path: "examples/phoenix_host/e2e/offline_sync.spec.ts"
      provides: "Deterministic legacy-upgrade, inert-quarantine, authorized-recovery, relaunch, and second-scope regressions."
  key_links:
    - from: "IndexedDB legacy mutations store"
      to: "legacy_mutations_quarantine"
      via: "one atomic onupgradeneeded cursor copy/delete transaction without scope assignment"
      pattern: "onupgradeneeded|legacy_mutations_quarantine|openCursor"
    - from: "window.crosswakeOfflineStudy.recoverLegacyMutations"
      to: "scoped_mutations"
      via: "explicit matching active scope-plus-epoch lease and one atomic quarantine-to-scoped transaction"
      pattern: "recoverLegacyMutations|scope_ref|local_ref"
  prohibitions:
    - statement: "MUST NOT infer a scope for legacy records from cookies, the currently active account, a digest, route state, or record contents."
      status: unresolved
      verification: flagged-unverified
    - statement: "MUST NOT render, log, inspect, retain as proof, or return legacy mutation payloads or opaque identifiers during migration or recovery."
      status: unresolved
      verification: flagged-unverified
flagged_assumptions:
  - "[UNRESOLVED-SPEC-PROBE] SCOPE-01 is classified as an automated storage-upgrade edge: the example host proves quarantine and explicit recovery using synthetic records; real host issuance and recovery UX remain host-owned."
  - "[PRESERVE] TODO-002 and real adopter route/account inputs remain unknown_blocking; synthetic recovery fixtures do not promote adopter-instance completeness."
---

<objective>
Make legacy offline mutations retained, visible as blocked work, and recoverable only through explicit host reauthorization.

Purpose: Close T-160-01 without assigning pre-scope work to whichever account happens to launch after upgrade.
Output: Atomic IndexedDB quarantine/recovery mechanics and deterministic browser proof.
</objective>

<artifacts_this_phase_produces>
- IndexedDB schema version 4 and `legacy_mutations_quarantine` object store.
- Private `quarantineLegacyMutations` and `countLegacyQuarantine` helpers.
- Host semantic bridge method `window.crosswakeOfflineStudy.recoverLegacyMutations(scopeRef)`.
- Playwright legacy-upgrade and authorized-recovery fixtures in `offline_sync.spec.ts`.
</artifacts_this_phase_produces>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/ADR-FIRST-B2C-ADOPTER.md
@.planning/phases/160-scoped-replay-and-auth-safety/160-CONTEXT.md
@.planning/phases/160-scoped-replay-and-auth-safety/160-PATTERNS.md
@.planning/phases/160-scoped-replay-and-auth-safety/160-VERIFICATION.md
@.planning/phases/160-scoped-replay-and-auth-safety/160-SECURITY.md
@.planning/phases/160-scoped-replay-and-auth-safety/160-03-SUMMARY.md
@examples/phoenix_host/priv/static/offline_study.js
@examples/phoenix_host/e2e/offline_sync.spec.ts

<interfaces>
Preserve the existing compound `(scope_ref, local_ref)` store and inactive-by-default lifecycle. Automatic upgrade may quarantine unscoped bytes but cannot derive scope. Recovery is a low-frequency host call that requires the same explicit active scope and epoch used by normal replay; it returns only a closed success/blocked result and never record content.
</interfaces>
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Quarantine one legacy mutation during upgrade and keep replay inert</name>
  <reversibility rating="costly">The IndexedDB version/store addition is persisted host state and later reversal requires another storage migration.</reversibility>
  <read_first>
    - AGENTS.md
    - .planning/phases/160-scoped-replay-and-auth-safety/160-CONTEXT.md
    - .planning/phases/160-scoped-replay-and-auth-safety/160-VERIFICATION.md
    - examples/phoenix_host/priv/static/offline_study.js
    - examples/phoenix_host/e2e/offline_sync.spec.ts
  </read_first>
  <files>examples/phoenix_host/priv/static/offline_study.js, examples/phoenix_host/e2e/offline_sync.spec.ts</files>
  <behavior>
    - "RED: a Playwright fixture creates the prior database version with an unscoped `mutations` record, then loads the current app and proves the record is neither visible nor present in `scoped_mutations`."
    - "The version-4 upgrade copies each legacy record by cursor into `legacy_mutations_quarantine` and deletes its legacy source only inside the same upgrade transaction; abort preserves the legacy store unchanged."
    - "Quarantine presence produces the existing calm needs-attention/paused semantics without record count, payload, identifier, scope, or backend vocabulary and sends no request."
    - "Cold relaunch preserves quarantine and inactive lifecycle state; activating another synthetic scope does not move, display, delete, or send quarantined bytes."
  </behavior>
  <action>Raise `DB_VERSION` to 4 and add constants for the prior `mutations` store and new `legacy_mutations_quarantine` store. In `onupgradeneeded`, create the quarantine store if absent and, only when the prior unscoped store exists, walk it with `openCursor`; copy each cursor value under an internal migration key without adding `scope_ref`, then delete that cursor only after the quarantine add succeeds. Keep all operations in the upgrade transaction so any error aborts the whole migration. After open, call `countLegacyQuarantine` and retain a closed recovery-required flag. Do not enumerate quarantine through the normal scoped read/count/drain functions, do not pass its values to status/console/errors, and do not start replay from it. Add a deterministic version-1 fixture before navigation and assertions for atomic retention, inert launch, generic status copy, no network send, relaunch persistence, and a second untouched scope per D-01 through D-06.</action>
  <acceptance_criteria>
    - A legacy record exists in exactly one non-replayable location after successful upgrade and remains in the legacy store if upgrade aborts.
    - The scoped store remains empty until explicit recovery; no automatic current-scope assignment occurs.
    - Browser-visible, console, error, and request bytes contain no legacy record, identifier, payload, or scope fixture.
    - Relaunch and unrelated scope activation leave the quarantine record byte-for-byte retained and inert.
  </acceptance_criteria>
  <verify><automated>npm --prefix examples/phoenix_host run proof:offline-island -- --grep "legacy upgrade quarantines unscoped work|legacy quarantine remains inert across relaunch"</automated></verify>
  <done>Pre-scope learner work survives upgrade in a non-replayable quarantine and cannot silently attach to the next account.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Recover quarantined work only under an explicit matching host lease</name>
  <reversibility rating="costly">The host recovery call moves persisted records into the scoped contract and must remain compatible with deployed storage.</reversibility>
  <read_first>
    - .planning/phases/160-scoped-replay-and-auth-safety/160-CONTEXT.md
    - examples/phoenix_host/priv/static/offline_study.js
    - examples/phoenix_host/e2e/offline_sync.spec.ts
  </read_first>
  <files>examples/phoenix_host/priv/static/offline_study.js, examples/phoenix_host/e2e/offline_sync.spec.ts</files>
  <behavior>
    - "Missing, malformed, inactive, mismatched, or stale recovery scope rejects with a stable non-echoing rule and leaves quarantine unchanged."
    - "A matching active host scope moves every quarantined mutation into `(scope_ref, local_ref)` identity and removes quarantine only in the same transaction."
    - "A recovered record follows the ordinary serial replay path; accepted deletion touches only that exact scope while a retained second partition is unchanged."
    - "Interrupted or duplicate recovery is idempotent and cannot create two scoped copies or erase the last retained copy."
  </behavior>
  <action>Add `recoverLegacyMutations(scopeRef)` to the frozen `window.crosswakeOfflineStudy` host surface. Require a valid scope equal to the current active lease, capture its epoch, and recheck that lease before opening and before committing a single readwrite transaction over quarantine plus `scoped_mutations`. For each quarantined record, require the existing bounded client mutation identifier needed for `local_ref`; add the unchanged mutation fields plus the explicit authorized `scope_ref`/`local_ref`, and delete the quarantine source only after the scoped add succeeds. Any malformed record, key collision, stale lease, transaction error, or interruption aborts and returns one stable blocked classification without echo. Extend the fixture to prove wrong-scope denial, successful exact-scope recovery, duplicate-call idempotency, ordinary accepted replay, and preservation of an independently seeded second scope. Keep deletion/retention policy host-owned and do not add generic storage or all-scope APIs.</action>
  <acceptance_criteria>
    - Recovery cannot run without a current matching scope-plus-epoch lease and never consults cookies or record contents for authority.
    - Successful recovery leaves zero quarantine records and one scoped record per legacy mutation; failure leaves quarantine complete.
    - The recovered mutation replays through the existing endpoint and accepted-only deletion path without exposing its scope or payload.
    - The second scoped partition and all unrelated host-owned bytes remain unchanged.
  </acceptance_criteria>
  <verify><automated>npm --prefix examples/phoenix_host run proof:offline-island -- --grep "explicit host recovery scopes quarantined work|wrong scope cannot recover legacy work"</automated></verify>
  <done>Legacy work becomes replayable only after explicit host reauthorization for one exact scope, with atomic and non-echoing recovery.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| legacy unscoped storage -> quarantine | Persisted sensitive work crosses schema versions without gaining account authority. |
| host recovery call -> scoped outbox | Only a current explicit host scope and epoch may assign quarantined work to a replay partition. |

## ASVS L1 Control Mapping

| ASVS category | Required control | Automated evidence |
|---|---|---|
| V3 Session Management | Recovery requires a current matching scope-plus-epoch lease. | Wrong-scope, inactive, stale, and relaunch Playwright cases. |
| V4 Access Control | Automatic migration never assigns scope; host recovery moves only into the exact authorized partition. | Upgrade/recovery and second-partition cases. |
| V5 Input Validation | Legacy records and recovery scope are bounded and fail with stable non-echoing rules. | Malformed-record and scope cases. |
| V8 Data Protection | Quarantined payloads never enter UI, console, errors, requests, or proof output. | Browser byte assertions. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-160-01 | Spoofing / Elevation | Legacy IndexedDB upgrade and scope recovery | high | mitigate | Tasks 1-2 quarantine without scope and require explicit matching host authority before atomic recovery. |
| T-160-SC | Tampering | Package installs | low | accept | No package-manager installation is required; existing Playwright infrastructure is reused. |
</threat_model>

<verification>
Run both focused Playwright grep commands; they must prove automatic non-assignment, inert retention, exact-scope recovery, and second-partition preservation without new manual UAT.
</verification>

<success_criteria>
Legacy offline work is never stranded or silently assigned: it remains inert until an explicit authorized scope recovers it atomically.
</success_criteria>

<output>
Create `.planning/phases/160-scoped-replay-and-auth-safety/160-04-SUMMARY.md` when done.
</output>
