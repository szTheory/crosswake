---
phase: 130-extraction-mechanics-footgun-guards
plan: 05
type: execute
wave: 3
depends_on: ["130-04"]
files_modified:
  - test/crosswake/proof/phase130_extraction_guards_test.exs
  - .github/workflows/phase130-proof.yml
autonomous: true
requirements: [EXTRACT-03]
must_haves:
  truths:
    - "A merge-blocking guard fails the build if any lib/ file statically names an extracted companion module — asserted GREEN against the real core lib/ now that the rulestead adapter has been extracted (EXTRACT-03, SC#3)"
    - "Companions.Sigra / Companions.Chimeway static refs stay LEGAL (the guard does not flag them) (D-14)"
    - "The companion lane CI job runs the engine-absent (merge-blocking) and engine-present (advisory) lanes with mix clean between them (D-33)"
  artifacts:
    - test/crosswake/proof/phase130_extraction_guards_test.exs
    - .github/workflows/phase130-proof.yml
  key_links:
    - "EXTRACT-03 assert-green depends on Plan 04 having removed the adapter from core lib/ — the guard turning green is the signal the extraction is complete (D-14)"
    - "CI companion job runs mix companions.test (engine-absent, blocking) + the :engine_present advisory lane with mix clean front (D-33)"
---

<objective>
Close out EXTRACT-03: now that Plan 04 has removed the Crosswake.Companions.Rulestead adapter from core lib/,
un-skip and assert GREEN the EXTRACT-03 static-ref guard against the real codebase (the rulestead alias no
longer exists in lib/; Sigra/Chimeway stay legal, D-14). Wire the companion-lane CI workflow so the engine-absent
(merge-blocking) and engine-present (advisory) lanes run with mix clean between them (D-33). Run the full proof
suite + the dress-rehearsal verify one final time as the phase exit gate.

Purpose: EXTRACT-03's green state is only honest after extraction — sequencing this assertion last avoids a
guard that passes vacuously (the adapter would still be in lib/ if asserted earlier). The CI job makes the
whole guard suite + companion lane merge-blocking going forward.
Output: green EXTRACT-03 real-lib assertion, companion-lane CI, full-suite exit-gate pass.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/130-extraction-mechanics-footgun-guards/130-CONTEXT.md
@.planning/phases/130-extraction-mechanics-footgun-guards/130-PATTERNS.md
@.planning/phases/130-extraction-mechanics-footgun-guards/130-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Un-skip and assert EXTRACT-03 static-ref guard GREEN against real core lib/ (post-extraction)</name>
  <files>test/crosswake/proof/phase130_extraction_guards_test.exs</files>
  <read_first>
    - test/crosswake/proof/phase130_extraction_guards_test.exs (the EXTRACT-03 describe block — the assert-green-against-real-lib test was marked @tag :skip / pending in Plan 03 with a Plan 05 pointer)
    - 130-CONTEXT.md § D-14 (legitimate Sigra/Chimeway core static refs that MUST stay legal: route_gate.ex:9, support_matrix.ex, doctor.ex:790, policy/schema.ex:7)
    - 130-RESEARCH.md § SC#3 (the assert_no_static_refs!/0 entrypoint that globs lib/**/*.ex)
  </read_first>
  <action>
    Remove the @tag :skip / pending marker from the EXTRACT-03 assert-green-against-real-lib test and make it
    call CompanionGuard.assert_no_static_refs!/0 against the real core lib/ — it now PASSES because Plan 04
    removed lib/crosswake/companions/rulestead.ex (no Crosswake.Companions.Rulestead alias node survives in
    core lib/). Add an explicit positive assertion that the known legitimate Sigra/Chimeway static refs
    (route_gate.ex, support_matrix.ex, doctor.ex, policy/schema.ex) are NOT flagged — read one such file and
    assert check_source/1 on it returns :ok (D-14, the guard scope is the frozen MapSet, not a blanket
    Companions.* ban). Confirm the whole phase130_extraction_guards_test.exs file is fully GREEN now (EXTRACT-01,
    EXTRACT-03 real-lib + non-vacuity, EXTRACT-04, D-27, self-match-avoidance) with no skips remaining.
  </action>
  <verify>
    <automated>mix test test/crosswake/proof/phase130_extraction_guards_test.exs 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - EXTRACT-03 assert_no_static_refs!/0 against real core lib/ is GREEN (adapter extracted; no Rulestead alias node remains)
    - A positive assertion confirms a real Sigra/Chimeway-referencing core file is NOT flagged (D-14)
    - No @tag :skip / pending tests remain in phase130_extraction_guards_test.exs; the whole file is green
  </acceptance_criteria>
  <done>EXTRACT-03 guard merge-blocking and green against the real codebase; Sigra/Chimeway stay legal.</done>
</task>

<task type="auto">
  <name>Task 2: Wire companion-lane CI workflow + run the full-suite phase exit gate (D-33)</name>
  <files>.github/workflows/phase130-proof.yml</files>
  <read_first>
    - .github/workflows/phase43-proof.yml (the existing advisory/hermetic CI lane structure to mirror — the analog for the engine-present/absent split)
    - 130-RESEARCH.md § "Proof Lane Definitions" (the 4 lanes: core hermetic untagged blocking; companion engine-absent blocking; engine-present advisory; the mix clean discipline) and § "Wave 0 Gaps" (.github/workflows/phase130-proof.yml)
    - 130-VALIDATION.md § "Sampling Rate" (before /gsd-verify-work: full suite green AND script/verify_companion_package.sh green)
  </read_first>
  <action>
    Create .github/workflows/phase130-proof.yml (or fold a companion job into existing CI — pick the cleaner per
    the repo's convention). It must run: (1) the core hermetic proof lane (the two phase130 proof files +
    mix test --exclude requires_example_host) as merge-blocking; (2) a companion job that runs mix companions.test
    in the engine-ABSENT default state (merge-blocking) AND, as a separate advisory step, the :engine_present lane
    — with mix clean BETWEEN the two states so a stale engine-present stub .beam cannot leak into the absent lane
    (D-33); (3) script/verify_companion_package.sh crosswake_rulestead as a blocking step (tarball excludes test/,
    --dry-run exit 0, --warnings-as-errors engine-absent). Mark the :engine_present step advisory (non-blocking).
    Then run the full phase exit gate locally: mix test --exclude requires_example_host --exclude advisory_only,
    the two phase130 proof files, mix companions.test, and bash script/verify_companion_package.sh — all green.
    Record the phase exit-gate result in the SUMMARY.
  </action>
  <verify>
    <automated>mix test --exclude requires_example_host --exclude advisory_only 2>&1 | tail -12</automated>
  </verify>
  <acceptance_criteria>
    - .github/workflows/phase130-proof.yml exists; core hermetic lane + companion engine-absent lane are merge-blocking; engine-present lane is advisory; mix clean runs between engine states (D-33)
    - CI runs script/verify_companion_package.sh as a blocking step
    - Full local exit gate green: core suite + both phase130 proof files + mix companions.test + verify script all pass
  </acceptance_criteria>
  <done>Companion-lane CI wired with correct blocking/advisory split; full phase exit gate green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| guard assert-green timing -> extraction completeness | Asserting EXTRACT-03 green before extraction would pass vacuously (adapter still in lib/) — sequencing it post-extraction keeps it honest |
| CI merge gate -> engine-present/absent state | The advisory engine-present lane must not contaminate the merge-blocking engine-absent lane |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-130-12 | Tampering | EXTRACT-03 guard asserted green while the adapter is still in lib/ (vacuous pass) | high | mitigate | Assertion sequenced to Wave 3, AFTER Plan 04 extraction removes the adapter; the guard turning green is the explicit signal the extraction is complete (D-14) |
| T-130-13 | Tampering | Stale engine-present .beam leaks into the merge-blocking engine-absent CI lane | medium | mitigate | CI runs mix clean between engine states; engine-present step is advisory/non-blocking (D-33) |
| T-130-14 | Denial of Service | Sigra/Chimeway static refs falsely flagged -> build bricked | high | mitigate | Positive assertion that a real Sigra/Chimeway-referencing file returns :ok from check_source/1 (frozen MapSet scope, D-14) |
| T-130-SC | Tampering | npm/pip/cargo installs | low | accept | No new packages (RESEARCH.md Package Legitimacy Audit: zero new deps); CI uses existing mix tooling only |
</threat_model>

<verification>
- phase130_extraction_guards_test.exs fully green (no skips); EXTRACT-03 real-lib assertion green; Sigra/Chimeway not flagged.
- .github/workflows/phase130-proof.yml: blocking core + companion-engine-absent lanes, advisory engine-present, blocking verify script.
- Full local exit gate green: core suite + both phase130 proof files + mix companions.test + verify script.
</verification>

<success_criteria>
EXTRACT-03 is merge-blocking and green against the real post-extraction codebase (Sigra/Chimeway stay
legal); the companion-lane CI enforces the engine-absent (blocking) / engine-present (advisory) split with
mix clean discipline; the full phase exit gate is green. All 5 success criteria are satisfied and proven.
</success_criteria>

<output>
Create `.planning/phases/130-extraction-mechanics-footgun-guards/130-05-SUMMARY.md` when done
</output>
