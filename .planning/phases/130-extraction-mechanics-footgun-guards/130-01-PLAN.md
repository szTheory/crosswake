---
phase: 130-extraction-mechanics-footgun-guards
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - test/crosswake/proof/phase130_extraction_guards_test.exs
  - test/crosswake/proof/phase130_fail_closed_contract_test.exs
  - lib/crosswake/companion_guard.ex
  - script/verify_companion_package.sh
  - packages/crosswake_rulestead/mix.exs
  - packages/crosswake_rulestead/README.md
autonomous: true
requirements: [EXTRACT-01, EXTRACT-02, EXTRACT-03, EXTRACT-04, COMPAT-01]
must_haves:
  truths:
    - "Both proof test files exist and are RED (failing meaningfully, not erroring on missing modules) so feedback sampling has red→green targets for every success criterion"
    - "Crosswake.CompanionGuard module loads and exposes the public helper surface the guard tests call"
    - "packages/crosswake_rulestead/ skeleton exists and `cd packages/crosswake_rulestead && mix compile` succeeds before any source is moved"
  artifacts:
    - test/crosswake/proof/phase130_extraction_guards_test.exs
    - test/crosswake/proof/phase130_fail_closed_contract_test.exs
    - lib/crosswake/companion_guard.ex
    - script/verify_companion_package.sh
    - packages/crosswake_rulestead/mix.exs
  key_links:
    - "Guard tests call Crosswake.CompanionGuard public helpers — signatures must match between stub and test"
    - "Package skeleton mix.exs declares {:crosswake, path: \"../..\"} with NO runtime: false (D-19)"
---

<objective>
Wave 0 scaffolding: stand up the two merge-blocking proof test files, the `Crosswake.CompanionGuard`
support module (signatures only + frozen MapSet), the parameterized verify script, and the
`packages/crosswake_rulestead/` package skeleton — so every later task has a RED test to turn GREEN
and feedback sampling works from task 1.

Purpose: Nyquist requires the test scaffold to exist before implementation. The guards' green state
depends on extraction (Wave 2), so the guard tests must be authored (red) early. This plan ships NO
real AST logic and NO moved source — only the skeleton each downstream plan fills in.
Output: Two proof `.exs` files (red), `companion_guard.ex` (compiles, public API stubbed), the verify
script, and a self-contained `packages/crosswake_rulestead/` skeleton that compiles standalone.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/130-extraction-mechanics-footgun-guards/130-CONTEXT.md
@.planning/phases/130-extraction-mechanics-footgun-guards/130-PATTERNS.md
@.planning/phases/130-extraction-mechanics-footgun-guards/130-RESEARCH.md
@.planning/phases/130-extraction-mechanics-footgun-guards/130-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Crosswake.CompanionGuard support module (frozen MapSet + stubbed public API)</name>
  <files>lib/crosswake/companion_guard.ex</files>
  <read_first>
    - test/crosswake/proof/phase129_companion_contract_freeze_test.exs (the `@expected_callbacks` MapSet + `Macro.prewalk/3` idiom this module mirrors — D-13/D-12)
    - 130-PATTERNS.md § `lib/crosswake/companion_guard.ex` (frozen MapSet pattern, check_source/1 signature, check_ensure_loaded_placement/1 signature)
    - test/support/proof_assertions.ex:8 (stable_id_message/7 signature the guard's failure messages will use)
  </read_first>
  <action>
    Create `Crosswake.CompanionGuard` as a plain module (NO `use ExUnit.Case`) callable from proof tests
    and — post-publish — from each companion package's own test (D-17, the guard travels with the code).
    Declare a module attribute `@extracted_companions` as a frozen `MapSet.new([Crosswake.Companions.Rulestead])`
    with a comment naming Phase 130 as the extraction phase, per D-13 (DO NOT derive from path: deps — core
    names no companion, so there is nothing to derive from). Expose a `extracted_companions/0` reader.
    Stub the public API with minimal-but-honest bodies that downstream Plan 03 fills in:
    `check_source/1` (returns `{:violation, list}` or `:ok` for EXTRACT-03 static-ref detection),
    `check_ensure_loaded_placement/1` (returns `{:violation, list}` or `:ok` for EXTRACT-04 placement),
    `assert_no_static_refs!/0` and `assert_ensure_loaded_in_function_bodies!/0` (the test entrypoints that
    glob `Path.wildcard(Path.join(File.cwd!(), "lib/**/*.ex"))` and raise on violation).
    In THIS plan the two `check_*` functions may return `:ok` unconditionally as a placeholder — but add a
    `@moduledoc` and inline marker noting "Plan 03 implements AST logic" so the executor of Plan 03 knows
    where to wire it. Do NOT implement the AST walk here. The module MUST compile clean with
    `mix compile --warnings-as-errors`.
  </action>
  <verify>
    <automated>mix compile --warnings-as-errors 2>&1 | tail -5 && elixir -e "Code.ensure_loaded?(Crosswake.CompanionGuard) || (IO.puts(:stderr, \"FAIL: module missing\"); System.halt(1))" -S mix run -e ":ok" 2>/dev/null; mix run -e "true = function_exported?(Crosswake.CompanionGuard, :check_source, 1); true = function_exported?(Crosswake.CompanionGuard, :check_ensure_loaded_placement, 1); IO.puts(\"OK\")"</automated>
  </verify>
  <acceptance_criteria>
    - `Crosswake.CompanionGuard` compiles clean under `--warnings-as-errors`
    - `@extracted_companions` is a frozen MapSet containing exactly `Crosswake.Companions.Rulestead`, each entry commented with its extraction phase
    - Public functions exported: `check_source/1`, `check_ensure_loaded_placement/1`, `assert_no_static_refs!/0`, `assert_ensure_loaded_in_function_bodies!/0`, `extracted_companions/0`
    - No AST logic implemented yet (placeholder bodies); moduledoc notes Plan 03 fills it in
  </acceptance_criteria>
  <done>Module compiles, public API present, frozen MapSet correct, no AST logic.</done>
</task>

<task type="auto">
  <name>Task 2: Stub phase130_extraction_guards_test.exs and phase130_fail_closed_contract_test.exs (RED)</name>
  <files>test/crosswake/proof/phase130_extraction_guards_test.exs, test/crosswake/proof/phase130_fail_closed_contract_test.exs</files>
  <read_first>
    - test/crosswake/proof/phase129_companion_contract_freeze_test.exs (header/module + async:true + untagged hermetic-lane idiom; self-match-avoidance test at bottom)
    - test/crosswake/proof/phase42_rulestead_companion_test.exs (Application.put_env / on_exit setup + RouteGate.evaluate assertion shape for the fail-closed test)
    - lib/crosswake/companion.ex:59-128 (the 6 @callback signatures the stub companion must implement)
    - 130-RESEARCH.md § SC#3/SC#4/SC#5 (the exact assertions each test must make) and § "Code Examples" (stub companion shape)
    - 130-PATTERNS.md § phase130_extraction_guards_test.exs and § phase130_fail_closed_contract_test.exs
  </read_first>
  <action>
    Create `Crosswake.Proof.Phase130ExtractionGuardsTest` (`use ExUnit.Case, async: true`, UNTAGGED — must
    NOT carry `:requires_example_host` or `:engine_present`, per D-18). Structure with `describe` blocks for
    EXTRACT-01 (mix.exs source assertion), EXTRACT-03 (static-ref AST guard), EXTRACT-04 (ensure_loaded
    placement), and the D-27 runtime:false guard. Write the REAL assertions now (they will be RED until later
    plans): EXTRACT-01 `refute String.contains?(source, "MIX_INCLUDE_RULESTEAD")` and the rindle variant;
    EXTRACT-03 calls `CompanionGuard.assert_no_static_refs!/0` PLUS non-vacuity pair — `check_source/1` on a
    synthetic string containing the `Crosswake.Companions.Rulestead` alias returns `{:violation, _}`, and on a
    `Crosswake.Companions.Sigra.Evaluator` alias returns `:ok` (Sigra stays legal, D-14); EXTRACT-04 calls
    `assert_ensure_loaded_in_function_bodies!/0` plus a synthetic-regression non-vacuity check; the runtime:false
    guard reads `packages/crosswake_rulestead/mix.exs` and refutes the `runtime: false` substring (D-27). Include
    the self-match-avoidance test at the bottom (this proof file carries no `@moduletag`, no `MIX_INCLUDE_*` ref —
    build the literal from concatenated fragments as phase65 does, so the guard does not flag its own file).
    Create `Crosswake.Proof.Phase130FailClosedContractTest` (`use ExUnit.Case, async: false` — `Application.put_env`
    on `:companions` is a shared global key, D-07 no cache). Define `Crosswake.TestSupport.StubDepMissingCompanion`
    (a plain module implementing `@behaviour Crosswake.Companion`, NOT an alias to the moved source — avoids
    tripping EXTRACT-03, D-20) whose `validate_dependency/0` returns `{:error, [SomeAbsentModule]}`. Write the
    SC#5 assertions: RouteGate denies a gated route with `reason: :dependency_missing` and
    `details["missing_kind"] == "engine_unvalidated"`; the D-02 precedence test (dep-missing beats kill-switch);
    the D-08 raise test (a stub whose `validate_dependency/0` raises still yields `:dependency_missing`). Use
    `Application.put_env`/`on_exit` save-restore for `:companions` and the stub's config key.
    Both files MUST be RED (assertions fail because the implementation is absent) — NOT erroring on a missing
    module. Run them to confirm they FAIL with assertion failures, not compile/load errors.
  </action>
  <verify>
    <automated>mix test test/crosswake/proof/phase130_extraction_guards_test.exs test/crosswake/proof/phase130_fail_closed_contract_test.exs 2>&1 | tail -15; echo "EXIT=$?"</automated>
  </verify>
  <acceptance_criteria>
    - Both files compile and load (no `UndefinedFunctionError` / missing-module errors)
    - Tests are RED via assertion failures (the implementation they target does not exist yet), not load errors
    - Extraction-guards test is `async: true`, untagged, has the self-match-avoidance test, and references `Crosswake.CompanionGuard`
    - Fail-closed test is `async: false`, defines `StubDepMissingCompanion` implementing all 6 `@behaviour Crosswake.Companion` callbacks, and includes the D-02 precedence + D-08 raise assertions
    - Non-vacuity pairs present (Rulestead alias detected, Sigra alias NOT detected)
  </acceptance_criteria>
  <done>Two proof files exist, compile, and fail by assertion (red), with non-vacuity controls.</done>
</task>

<task type="auto">
  <name>Task 3: Scaffold packages/crosswake_rulestead/ skeleton + verify script</name>
  <files>packages/crosswake_rulestead/mix.exs, packages/crosswake_rulestead/README.md, script/verify_companion_package.sh</files>
  <read_first>
    - mix.exs (core):1-90 (the `elixirc_paths`, `files:` allowlist, `package/0`, `deps/0` structure to mirror — NOTE core's files: includes `priv guides` which the companion has NEITHER)
    - 130-PATTERNS.md § `packages/crosswake_rulestead/mix.exs` (critical D-19/D-22/D-28/D-29 differences) and § `script/verify_companion_package.sh` (the three-step verify)
    - 130-RESEARCH.md § "Dress-Rehearsal Verify Commands" (exact hex.build --unpack / --dry-run / --warnings-as-errors invocations)
  </read_first>
  <action>
    Create `packages/crosswake_rulestead/mix.exs` defining `CrosswakeRulestead.MixProject` with
    `app: :crosswake_rulestead`, `@version "0.1.0"` carrying a `# x-release-please-version` marker (D-22 —
    separate from core 0.1.2; do NOT touch release-please config/manifest), `elixir: "~> 1.19"`, and
    `elixirc_paths(:test) -> ["lib", "test/support"]` / `elixirc_paths(_) -> ["lib"]` mirroring core. `deps/0`
    declares `{:crosswake, path: "../.."}` with NO `runtime: false` (D-19 — core is a RUNTIME dep) and
    `{:rulestead, "~> 0.1", optional: true}` (D-28). `package/0` sets `name: "crosswake_rulestead"`,
    `licenses: ["Apache-2.0"]`, and a `files:` allowlist of `~w(lib mix.exs README.md)` that EXCLUDES `test/`,
    `priv`, and `guides` (the companion ships none of those). Create a placeholder `lib/` directory via a tiny
    `.gitkeep` is unnecessary — instead create `packages/crosswake_rulestead/README.md` with a one-paragraph
    package description in brand voice. The moved adapter source and tests arrive in Plan 04; this task ships
    only the skeleton. The skeleton MUST satisfy `cd packages/crosswake_rulestead && mix deps.get && mix compile`
    with an empty `lib/` (no source yet) — create an empty `lib/.gitkeep` if Mix requires the directory.
    Create `script/verify_companion_package.sh` (executable, `set -euo pipefail`) parameterized on `$1`
    (default `crosswake_rulestead`) implementing the three D-24 checks: (1) `mix hex.build --unpack -o
    /tmp/${PACKAGE}_unpack` then FAIL if `$UNPACK_DIR/test` exists or `$UNPACK_DIR/lib/crosswake/companions/rulestead.ex`
    is absent; (2) `mix hex.publish --dry-run`; (3) `mix compile --warnings-as-errors`. Failure messages lead with
    `[crosswake]`. In Wave 0 the script will not pass end-to-end (no source yet) — that is expected; the script's
    own shape (shellcheck-clean, executable bit, correct assertions) is what this task delivers. `chmod +x` it.
  </action>
  <verify>
    <automated>test -x script/verify_companion_package.sh && bash -n script/verify_companion_package.sh && (cd packages/crosswake_rulestead && mix deps.get >/dev/null 2>&1 && mix compile 2>&1 | tail -3) && echo "SKELETON-OK"</automated>
  </verify>
  <acceptance_criteria>
    - `packages/crosswake_rulestead/mix.exs` declares `{:crosswake, path: "../.."}` with NO `runtime: false` and `{:rulestead, "~> 0.1", optional: true}`
    - `@version "0.1.0"` with `# x-release-please-version` marker; release-please config/manifest NOT touched
    - `files:` allowlist excludes `test/`, `priv`, `guides`
    - `cd packages/crosswake_rulestead && mix compile` succeeds with empty lib/ skeleton
    - `script/verify_companion_package.sh` is executable, `bash -n` clean, parameterized on `$1`, asserts test/ excluded + lib/ source present, runs the three D-24 verify steps
  </acceptance_criteria>
  <done>Package skeleton compiles standalone; verify script shape complete and executable.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| core lib/ → extracted companion | A static reference from core into an extracted module re-couples the poncho — the guard is the boundary enforcement |
| companion package → external engine (Rulestead) | Optional dependency probed at runtime; absence must fail closed, never fail open |
| proof test → its own assertions | A vacuously-passing guard is a security-relevant false negative |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-130-01 | Tampering | Proof test vacuity (guard passes when it should fail) | high | mitigate | Wave 0 ships RED tests with non-vacuity controls (Rulestead alias detected / Sigra alias not) before any implementation makes them green |
| T-130-02 | Repudiation | Stub companion masquerading as moved source | medium | mitigate | `StubDepMissingCompanion` is a plain test module, NOT an alias to the moved adapter — verifies the seam, not the code (D-20) |
| T-130-SC | Tampering | npm/pip/cargo installs | low | accept | No new packages introduced this phase (RESEARCH.md Package Legitimacy Audit: zero new deps); skeleton uses path: dep + stdlib only |
</threat_model>

<verification>
- Both proof files load and fail by assertion (red), not by missing-module errors.
- `Crosswake.CompanionGuard` compiles and exposes the public API the tests call.
- `packages/crosswake_rulestead/` compiles standalone as a `path:` dep skeleton.
- `script/verify_companion_package.sh` passes `bash -n` and is executable.
</verification>

<success_criteria>
Wave 0 scaffold complete: red proof targets exist for all 5 success criteria; CompanionGuard API
present (logic stubbed); package skeleton + verify script in place. Sampling can run from task 1.
</success_criteria>

<output>
Create `.planning/phases/130-extraction-mechanics-footgun-guards/130-01-SUMMARY.md` when done
</output>
