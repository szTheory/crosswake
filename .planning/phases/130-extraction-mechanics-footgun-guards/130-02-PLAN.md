---
phase: 130-extraction-mechanics-footgun-guards
plan: 02
type: execute
wave: 1
depends_on: ["130-01"]
files_modified:
  - lib/crosswake/shell/denial.ex
  - lib/crosswake/compatibility/route_gate.ex
  - lib/crosswake/doctor/doctor.ex
  - test/crosswake/doctor/doctor_test.exs
  - test/fixtures/bridge_contract_vectors.json
  - packages/crosswake-shell-core-ios/Tests/CrosswakeShellCoreTests/Resources/bridge_contract_vectors.json
  - packages/crosswake-shell-core-android/src/test/resources/bridge_contract_vectors.json
  - test/crosswake/proof/phase130_fail_closed_contract_test.exs
autonomous: true
requirements: [COMPAT-01]
must_haves:
  truths:
    - "With a companion registered+enabled but its dependency absent, RouteGate denies the gated route with reason :dependency_missing — never silently allows, never crashes (COMPAT-01, SC#5, D-01)"
    - "Precedence holds: dependency_missing fires BEFORE kill_switch_active and gate_denied (D-02)"
    - "A companion whose validate_dependency/0 raises still produces :dependency_missing via try/rescue (D-08, fail-closed even on a raise)"
    - "details.missing_kind distinguishes :engine_unvalidated from :adapter_unloadable (D-06)"
    - ":dependency_missing is the 13th reason in the closed Denial.@reasons enum and the generated bridge contract fixtures list it (D-04)"
  artifacts:
    - lib/crosswake/compatibility/route_gate.ex
    - lib/crosswake/shell/denial.ex
    - test/crosswake/proof/phase130_fail_closed_contract_test.exs
  key_links:
    - "RouteGate.check_dependencies/2 synthesizes the Denial INLINE in prepend_gate_evaluation_findings/3 (D-03) — NOT via finding_to_denial/2, NOT through on_unavailable (D-10 RESOLVED)"
    - "Doctor and RouteGate share the code string companion.dependency_missing (D-05)"
    - "mix crosswake.contract.gen regenerates the 3 bridge_contract_vectors.json fixtures from Denial.reasons() (D-04)"
---

<objective>
Wire the RouteGate hot-path fail-closed enforcement (COMPAT-01 / SC#5): a registered+enabled companion
whose dependency is absent must DENY its gated route with reason :dependency_missing, ahead of kill-switch
and gate checks (D-02), failing closed even when the dependency check itself raises (D-08). Add
:dependency_missing as the 13th Denial reason (D-04), branch the doctor remediation on missing_kind (D-06),
and regenerate the contract fixtures. Turns phase130_fail_closed_contract_test.exs GREEN.

Purpose: This is the security spine of the phase — fail-OPEN here is the defect. The doctor (cold path)
already emits companion.dependency_missing (D-32); this plan adds the hot path and unifies the surfaces on
one code string (D-05). Independent of the extraction (uses a stub companion, not the moved source).
Output: RouteGate enforcement, extended Denial enum, doctor missing_kind branch, regenerated fixtures,
green contract test.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/130-extraction-mechanics-footgun-guards/130-CONTEXT.md
@.planning/phases/130-extraction-mechanics-footgun-guards/130-PATTERNS.md
@.planning/phases/130-extraction-mechanics-footgun-guards/130-RESEARCH.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add :dependency_missing (13th reason) to Denial enum + sweep exhaustive sites + regenerate fixtures</name>
  <files>lib/crosswake/shell/denial.ex, test/crosswake/doctor/doctor_test.exs, test/fixtures/bridge_contract_vectors.json, packages/crosswake-shell-core-ios/Tests/CrosswakeShellCoreTests/Resources/bridge_contract_vectors.json, packages/crosswake-shell-core-android/src/test/resources/bridge_contract_vectors.json</files>
  <behavior>
    - Test 1: Denial.reasons() includes :dependency_missing (13 reasons total, was 12)
    - Test 2: doctor_test.exs denial_reasons assertion passes with "dependency_missing" added
    - Test 3: bridge_contract_vectors.json denial_reasons array contains "dependency_missing" in all 3 locations
    - Edge: a Denial built with reason :dependency_missing serializes to JSON via Atom.to_string (no exhaustive-case crash in shell / JSON formatter / operator inspection)
  </behavior>
  <read_first>
    - lib/crosswake/shell/denial.ex:14-44 (the @reasons list — 12 entries verified — and @type reason union to extend)
    - test/crosswake/doctor/doctor_test.exs:107-121 (the HARDCODED exhaustive Enum.sort list — add "dependency_missing")
    - 130-RESEARCH.md § "Open Investigation #2 — D-04" (the 2 hardcoded sites + the safe dynamic sites; A2: iOS/Android Swift/Kotlin tests do NOT hardcode the list, they read per-vector from JSON, so only the regenerated fixtures change)
    - lib/mix/tasks/crosswake.contract.gen.ex (the generator that derives fixtures from Denial.reasons())
  </read_first>
  <action>
    Add :dependency_missing as the 13th entry of @reasons in lib/crosswake/shell/denial.ex (D-04) with an
    inline comment naming Phase 130 / COMPAT-01, and add the new branch to the @type reason union. Add
    "dependency_missing" to the hardcoded Enum.sort list at test/crosswake/doctor/doctor_test.exs:107-121.
    Run mix crosswake.contract.gen to regenerate the 3 bridge_contract_vectors.json fixtures (core + ios
    Resources + android resources) so their denial_reasons arrays include the new reason; commit the
    regenerated files. Do a final grep sweep for any OTHER exhaustive case-reason match or hardcoded reason
    list (per A1) — research confirms the dynamic sites (operator_inspection.ex, publish_readiness.ex,
    doctor.ex:1399, activation.ex, denial.ex commerce catch-all) auto-extend via Enum.map(Denial.reasons(), ...)
    or carry catch-all clauses; verify no new clause is needed and record the sweep result. Verify A2 explicitly:
    grep the ios Tests/ and android src/test/ trees for a hardcoded denial-reason count or list — record it as
    a verified no-op (the Swift/Kotlin tests read expected_denial_reason per-vector from the regenerated JSON,
    so no source edits there).
  </action>
  <verify>
    <automated>mix test test/crosswake/doctor/doctor_test.exs 2>&1 | tail -5 && mix run -e "true = :dependency_missing in Crosswake.Shell.Denial.reasons(); 13 = length(Crosswake.Shell.Denial.reasons()); IO.puts(\"OK 13 reasons\")"</automated>
  </verify>
  <acceptance_criteria>
    - Denial.reasons() returns 13 reasons including :dependency_missing; @type reason union extended
    - doctor_test.exs:107-121 hardcoded list includes "dependency_missing" and the doctor test passes
    - All 3 bridge_contract_vectors.json files regenerated and list "dependency_missing" in denial_reasons
    - Sweep recorded: no additional exhaustive case clause needed (A1); iOS/Android source no-op confirmed (A2)
  </acceptance_criteria>
  <done>13-reason enum, hardcoded list updated, fixtures regenerated, sweep confirmed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: RouteGate fail-closed enforcement (check_dependencies/2) + doctor missing_kind branch</name>
  <files>lib/crosswake/compatibility/route_gate.ex, lib/crosswake/doctor/doctor.ex</files>
  <behavior>
    - Test 1: companion validate_dependency/0 returns {:error, _} -> RouteGate denies gated route, reason :dependency_missing, missing_kind "engine_unvalidated"
    - Test 2: companion ALSO kill_switch_active?/1 true -> still :dependency_missing (D-02 precedence beats kill_switch)
    - Test 3: companion validate_dependency/0 raises -> still :dependency_missing, missing_kind "adapter_unloadable" (D-08 try/rescue/catch)
    - Test 4: companion validate_dependency/0 returns :ok -> no dependency denial; normal kill_switch/gate flow proceeds
    - Edge: a DISABLED companion with a missing dep does NOT deny (the enabled+gated filter runs ahead of the dep check, D-02/D-09)
  </behavior>
  <read_first>
    - lib/crosswake/compatibility/route_gate.ex:95-170 (prepend_gate_evaluation_findings/3 injection site at 101-119; the inline kill-switch Denial.new synthesis at 121-147 is the idiom to mirror — D-03; the enabled+gated filter at 103-107)
    - lib/crosswake/doctor/doctor.ex:564-618 (phase_38_companion_seam_findings — the existing companion.dependency_missing :error finding at :589 to extend with missing_kind, D-06; the :validate_dependency telemetry span at ~572-580 to mirror for the gate span)
    - lib/crosswake/companion.ex:115 (the validate_dependency/0 callback contract: :ok | {:error, [module()]})
    - 130-RESEARCH.md § "Open Investigation #1 — D-10" (on_unavailable is a POST-denial transition field — do NOT route the dep check through it; synthesize INLINE in prepend_gate_evaluation_findings/3 only) and § "Architecture Patterns" (the check_dependencies/2 shape, the inline Denial.new fields, the telemetry span name [:crosswake, :companion, :dependency_check])
    - 130-PATTERNS.md § route_gate.ex (modify) and § doctor.ex (modify) — the exact reduce_while + try/rescue/catch structure and the doctor missing_kind branch
  </read_first>
  <action>
    Add a private check_dependencies/2 to lib/crosswake/compatibility/route_gate.ex that takes the already-filtered
    (enabled + gated) companions list plus the route, and Enum.reduce_while over them. For each companion, wrap
    companion.validate_dependency() in try/rescue/catch (D-08): :ok -> continue; {:error, _mods} -> halt with
    missing_kind :engine_unvalidated; any raise/throw -> halt with missing_kind :adapter_unloadable. On halt,
    synthesize the Denial INLINE via Denial.new (mirroring the kill-switch synthesis at lines 121-147, D-03) with
    reason: :dependency_missing, a brand-voice message naming the companion and ending "The gate fails closed."
    (calm/present-tense, [crosswake] prefix), route_id: route.id, and details %{"companion_id" => Atom.to_string(...),
    "missing_kind" => Atom.to_string(missing_kind)} (missing_kind serialized to string at the boundary). Wire
    check_dependencies AHEAD of check_kill_switches in prepend_gate_evaluation_findings/3 so precedence is
    dependency_missing -> kill_switch_active -> gate_denied (D-02). Do NOT touch transition_for_non_notification_denial/2
    and do NOT route through on_unavailable (D-10 RESOLVED). Wrap the check in a :telemetry.span as
    [:crosswake, :companion, :dependency_check], carrying :exception metadata on the rescue path (D-08). Do NOT add
    a cache / :persistent_term — live-compute on the gate path (D-07). In lib/crosswake/doctor/doctor.ex, extend the
    existing {true, {:error, mods}} branch at ~582-597 to carry missing_kind: :engine_unvalidated in details (the
    cold path always calls validate_dependency/0 directly, so it is always :engine_unvalidated here, D-06) and keep
    the shared code string companion.dependency_missing (D-05). Refine the doctor remediation microcopy in brand
    voice (name what happened + the one fix: "companion dependencies are not pulled transitively; the host app
    declares them").
  </action>
  <verify>
    <automated>mix test test/crosswake/proof/phase130_fail_closed_contract_test.exs 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - RouteGate denies a gated route with reason :dependency_missing when a registered+enabled companion's validate_dependency/0 returns {:error, _} (SC#5)
    - missing_kind is "engine_unvalidated" for {:error,_} and "adapter_unloadable" for a raise (D-06/D-08)
    - Precedence: dependency_missing beats kill_switch_active and gate_denied (D-02) — verified by the precedence test
    - A raise in validate_dependency/0 still yields :dependency_missing (try/rescue/catch, D-08)
    - No cache / :persistent_term introduced; live Application.get_env read preserved (D-07)
    - transition_for_non_notification_denial/2 unchanged; no on_unavailable routing (D-10)
    - Doctor finding carries missing_kind: :engine_unvalidated and shares the companion.dependency_missing code string (D-05/D-06)
    - phase130_fail_closed_contract_test.exs is GREEN
  </acceptance_criteria>
  <done>RouteGate fails closed with correct precedence and missing_kind; doctor branch extended; contract test green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| adopter config (:companions registry) -> RouteGate | A registered companion whose dependency is absent must not be trusted to answer gate questions |
| companion.validate_dependency/0 -> RouteGate denial envelope | Core synthesizes the Denial; the companion never authors it (D-01) |
| dependency check execution -> denial decision | An exception in the check itself must fail closed, not crash or fall through to allow (D-08) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-130-03 | Elevation of Privilege | RouteGate gated route with companion dep absent silently allowed | critical | mitigate | check_dependencies/2 fires FIRST (D-02) and fail-CLOSES with reason :dependency_missing before route_gated?/2 can fail-OPEN to :pass; SC#5 contract test asserts :deny |
| T-130-04 | Denial of Service | validate_dependency/0 raises and crashes the gate evaluation | high | mitigate | try/rescue/catch wraps the check (D-08); a raise yields :dependency_missing + missing_kind :adapter_unloadable + :exception telemetry, never a crash |
| T-130-05 | Tampering | doctor(live) vs RouteGate(cached) TOCTOU drift | medium | mitigate | No cache — live-compute on the gate path (D-07); both surfaces read Application.get_env live |
| T-130-SC | Tampering | npm/pip/cargo installs | low | accept | No new packages (RESEARCH.md Package Legitimacy Audit: zero new deps); stdlib + existing telemetry only |
</threat_model>

<verification>
- phase130_fail_closed_contract_test.exs green (SC#5 + D-02 precedence + D-08 raise).
- Denial.reasons() returns 13 including :dependency_missing; doctor_test green; fixtures regenerated.
- mix test --exclude requires_example_host passes (no exhaustive-case regression from the new reason).
- No :persistent_term / cache introduced in route_gate.ex.
</verification>

<success_criteria>
COMPAT-01 hot path complete: RouteGate fails closed with correct precedence and missing_kind; the doctor
cold path shares the code string and carries missing_kind; the 13th reason is wired through the enum and
all generated fixtures; the core hermetic contract test is green and merge-blocking.
</success_criteria>

<output>
Create `.planning/phases/130-extraction-mechanics-footgun-guards/130-02-SUMMARY.md` when done
</output>
