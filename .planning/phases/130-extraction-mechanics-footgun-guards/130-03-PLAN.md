---
phase: 130-extraction-mechanics-footgun-guards
plan: 03
type: execute
wave: 1
depends_on: ["130-01"]
files_modified:
  - lib/crosswake/companion_guard.ex
  - test/crosswake/proof/phase130_extraction_guards_test.exs
autonomous: true
requirements: [EXTRACT-04, EXTRACT-03]
must_haves:
  truths:
    - "A guard verifies every Code.ensure_loaded? call occurs inside a def/defp/defmacro body, never at module-evaluation time (EXTRACT-04, SC#4)"
    - "The EXTRACT-04 guard is non-vacuous: a synthetic source with a module-eval-level Code.ensure_loaded? is caught as a violation"
    - "CompanionGuard.check_source/1 detects a Crosswake.Companions.Rulestead alias AST node and does NOT flag Crosswake.Companions.Sigra (EXTRACT-03 logic is implemented and non-vacuous; D-14/D-15)"
    - "No companion path: dep carries runtime: false (D-27 guard green against the package skeleton)"
  artifacts:
    - lib/crosswake/companion_guard.ex
    - test/crosswake/proof/phase130_extraction_guards_test.exs
  key_links:
    - "AST mechanism is Code.string_to_quoted/2 + Macro.prewalk/3 over Path.wildcard(lib/**/*.ex) — stdlib only, no mix xref, no boundary lib (D-11/D-12)"
    - "EXTRACT-03 matches the namespaced {:__aliases__, _, [:Crosswake, :Companions, :Rulestead]} node; EXTRACT-04 matches the Code.ensure_loaded? call node (any arg) — they key on different node shapes (D-15)"
    - "EXTRACT-03 assert-green against the real lib/ is DEFERRED to Plan 05 (after extraction removes the adapter source) — this plan implements + proves the LOGIC via synthetic controls"
---

<objective>
Implement the real AST logic inside `Crosswake.CompanionGuard` for both guards and turn the EXTRACT-04
placement assertions GREEN (EXTRACT-04 is already clean in core today — all 13 Code.ensure_loaded? sites
are inside function bodies, D-16/D-32), plus the D-27 runtime:false guard. Implement and PROVE the
EXTRACT-03 static-ref detection logic via synthetic non-vacuity controls — but do NOT yet assert it green
against the real `lib/` (the rulestead adapter source still lives in core until Plan 04 extracts it; that
assert-green step is Plan 05's job, sequenced after extraction).

Purpose: Authors the fitness-function suite (one bespoke AST proof, stdlib-only, D-11) and makes EXTRACT-04
merge-blocking now. EXTRACT-03 logic is correct and non-vacuously proven here; its green state against the
codebase is gated on the extraction, so the guard turning red↔green is the signal the extraction is complete.
Output: real AST walk in CompanionGuard; green EXTRACT-04 + D-27 + EXTRACT-03 non-vacuity controls.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/130-extraction-mechanics-footgun-guards/130-CONTEXT.md
@.planning/phases/130-extraction-mechanics-footgun-guards/130-PATTERNS.md
@.planning/phases/130-extraction-mechanics-footgun-guards/130-RESEARCH.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement CompanionGuard AST logic — check_source/1 (EXTRACT-03) + check_ensure_loaded_placement/1 (EXTRACT-04)</name>
  <files>lib/crosswake/companion_guard.ex</files>
  <behavior>
    - Test 1: check_source/1 on a string containing a Crosswake.Companions.Rulestead alias node -> {:violation, [_|_]}
    - Test 2: check_source/1 on a string containing a Crosswake.Companions.Sigra.Evaluator alias node -> :ok (Sigra stays legal, D-14)
    - Test 3: check_source/1 on a moduledoc string literal mentioning Crosswake.Companions.Rulestead (as text, not an alias node) -> :ok (string literals are not {:__aliases__} nodes, D-12)
    - Test 4: check_ensure_loaded_placement/1 on a source with Code.ensure_loaded? inside a def body -> :ok
    - Test 5: check_ensure_loaded_placement/1 on a source with Code.ensure_loaded? at module-eval / @attr position -> {:violation, [_|_]}
  </behavior>
  <read_first>
    - test/crosswake/proof/phase129_companion_contract_freeze_test.exs (the Code.string_to_quoted/2 + Macro.prewalk/3 idiom + @expected_callbacks MapSet shape)
    - test/crosswake/proof/phase65_diagnostic_export_seam_test.exs (File.read! + Path.wildcard glob + the self-match-avoidance guard at the file bottom)
    - test/support/proof_assertions.ex:8 (stable_id_message/7 — the failure-message helper to embed in assert_*! raises)
    - 130-PATTERNS.md § lib/crosswake/companion_guard.ex (check_source/1 prewalk matcher; check_ensure_loaded_placement/1 prune-then-walk + belt regex; the stable-id slug examples like proof.extract_03.static_ref.<file>)
    - 130-RESEARCH.md § SC#3 + SC#4 (the exact AST node shapes: {:__aliases__, _, [:Crosswake, :Companions, :Rulestead]} for EXTRACT-03; {:., _, [{:__aliases__, _, [:Code]}, :ensure_loaded?]} for EXTRACT-04; the belt regex ~r/^[^#\n]*Code\.ensure_loaded\?/m)
  </read_first>
  <action>
    Implement check_source/1 (EXTRACT-03): Code.string_to_quoted/2 the source, Macro.prewalk/3 collecting any
    {:__aliases__, _meta, parts} node whose parts equals [:Crosswake, :Companions, :Rulestead] — derive the
    banned alias-part lists from @extracted_companions (Module.split each to atoms) so the matcher stays in sync
    with the frozen MapSet, NOT a blanket Crosswake.Companions.* ban (D-14). Return :ok or {:violation, nodes}.
    String literals in moduledocs are not alias nodes so they cannot false-positive (D-12) — no special-casing.
    Implement check_ensure_loaded_placement/1 (EXTRACT-04) via AST prune-then-walk (D-16): collect the body
    subtrees of every def/defp/defmacro, then walk the FULL ast for Code.ensure_loaded? call nodes
    ({:., _, [{:__aliases__, _, [:Code]}, :ensure_loaded?]} or its sugar form); assert each such node is reachable
    only inside a collected function body. Any ensure_loaded? node at module-eval / @attr position is a violation.
    Add the cheap textual belt: Regex.scan(~r/^[^#\n]*Code\.ensure_loaded\?/m, source) flagging a non-indented
    (module-body) occurrence as an escalation signal (AST stays authoritative; belt only escalates for
    macro/unquote-injected edge cases). Implement assert_no_static_refs!/0 and
    assert_ensure_loaded_in_function_bodies!/0 to glob Path.wildcard(Path.join(File.cwd!(), "lib/**/*.ex")),
    run the respective check per file, and on any violation raise with ProofAssertions.stable_id_message/7
    (stable-id slugs proof.extract_03.static_ref.<file> / proof.extract_04.ensure_loaded_placement.<file>;
    failure message in brand voice leading with [crosswake], naming the boundary crossed, the one fix, and the
    compile-vs-runtime/stale-recompile footgun). The module's own AST walk must NOT false-positive on
    companion_guard.ex itself (the frozen MapSet holds module atoms in a MapSet literal — those are not
    Crosswake.Companions.Rulestead alias nodes, but verify this file passes its own check_source/1).
  </action>
  <verify>
    <automated>mix compile --warnings-as-errors 2>&1 | tail -3 && mix run -e "{:violation,_} = Crosswake.CompanionGuard.check_source(\"alias Crosswake.Companions.Rulestead\"); :ok = Crosswake.CompanionGuard.check_source(\"alias Crosswake.Companions.Sigra.Evaluator\"); :ok = Crosswake.CompanionGuard.check_ensure_loaded_placement(\"defmodule X do\\n def f, do: Code.ensure_loaded?(Y)\\nend\"); IO.puts(\"AST-OK\")"</automated>
  </verify>
  <acceptance_criteria>
    - check_source/1 returns {:violation,_} for a Rulestead alias node, :ok for a Sigra alias node, :ok for a moduledoc string literal mentioning Rulestead (D-12/D-14/D-15)
    - check_ensure_loaded_placement/1 returns :ok for in-body ensure_loaded?, {:violation,_} for module-eval-level
    - Banned-alias list derived from @extracted_companions (stays in sync; no blanket Companions.* ban)
    - assert_*! helpers glob lib/**/*.ex and raise with stable_id_message/7 in brand voice
    - companion_guard.ex passes its own check_source/1 (no self-false-positive)
    - Compiles clean under --warnings-as-errors
  </acceptance_criteria>
  <done>Both AST checks implemented, non-vacuously correct, stdlib-only, brand-voice failure messages.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Turn EXTRACT-04 + D-27 + EXTRACT-03 non-vacuity controls GREEN in phase130_extraction_guards_test.exs</name>
  <files>test/crosswake/proof/phase130_extraction_guards_test.exs</files>
  <behavior>
    - Test 1: EXTRACT-04 describe block — assert_ensure_loaded_in_function_bodies!/0 passes against real lib/ (all 13 sites are in-body, D-32) GREEN
    - Test 2: EXTRACT-04 non-vacuity — a synthetic module-eval-level Code.ensure_loaded? source is caught GREEN
    - Test 3: EXTRACT-03 non-vacuity — check_source/1 detects Rulestead alias / ignores Sigra alias GREEN
    - Test 4: D-27 — packages/crosswake_rulestead/mix.exs contains no "runtime: false" substring GREEN
    - Edge: the EXTRACT-03 assert-green-against-real-lib test stays SKIPPED/pending here with a marker pointing to Plan 05 (adapter source still in core until extraction)
  </behavior>
  <read_first>
    - test/crosswake/proof/phase130_extraction_guards_test.exs (the Plan 01 stub with describe blocks already structured)
    - 130-PATTERNS.md § phase130_extraction_guards_test.exs (non-vacuity pair pattern, self-match-avoidance pattern, D-27 guard pattern)
    - 130-RESEARCH.md § SC#4 non-vacuity (the synthetic temp-file / in-memory source injection approach for the module-eval-level catch)
  </read_first>
  <action>
    Fill in the EXTRACT-04 describe block to call CompanionGuard.assert_ensure_loaded_in_function_bodies!/0
    against the real lib/ (passes today — all 13 sites are in def/defp bodies, D-32) plus the non-vacuity
    synthetic-regression test (build an in-memory or temp-file source with a module-eval-level
    Code.ensure_loaded? and assert check_ensure_loaded_placement/1 returns {:violation,_}). Fill in the
    EXTRACT-03 NON-VACUITY controls (Rulestead alias -> violation; Sigra alias -> :ok) and the D-27 guard
    (read packages/crosswake_rulestead/mix.exs, refute "runtime: false" substring with a stable_id_message).
    Keep the EXTRACT-03 assert-green-against-real-lib test as a clearly-marked pending/skip (@tag :skip with a
    comment: "asserted green in Plan 05 after extraction removes the adapter source from core lib/") — do NOT
    assert it green here, because the adapter still lives in core lib/ until Plan 04. Ensure the self-match-avoidance
    test (no @moduletag, no MIX_INCLUDE_ ref built from concatenated fragments) is present and passes. Confirm
    the whole file is async: true and untagged (no :requires_example_host / :engine_present, D-18).
  </action>
  <verify>
    <automated>mix test test/crosswake/proof/phase130_extraction_guards_test.exs 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - EXTRACT-04 assert-against-real-lib + non-vacuity tests are GREEN
    - EXTRACT-03 non-vacuity controls (Rulestead detected / Sigra ignored) GREEN
    - D-27 runtime:false guard GREEN against the package skeleton
    - EXTRACT-03 assert-green-against-real-lib test is explicitly skipped/pending with a Plan 05 pointer
    - Self-match-avoidance test passes; file is async: true and untagged
  </acceptance_criteria>
  <done>EXTRACT-04 + D-27 + EXTRACT-03 controls green; EXTRACT-03 real-lib assertion deferred to Plan 05.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| guard logic -> the codebase it scans | A guard that passes vacuously is a security-relevant false negative — every guard carries a non-vacuity control |
| core lib/ -> extracted companion module | The static-ref guard is the merge-blocking enforcement of the poncho boundary |
| module-eval time -> runtime | A compile-time Code.ensure_loaded? bakes engine presence into the .beam (stale-recompile footgun) — EXTRACT-04 forbids it |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-130-06 | Tampering | EXTRACT-04 guard passes vacuously (never catches a real module-eval placement) | high | mitigate | Synthetic-regression non-vacuity test injects a module-eval-level Code.ensure_loaded? and asserts {:violation}; guard runs against the real lib/**/*.ex glob (D-17, travels with the code) |
| T-130-07 | Denial of Service | Blanket Companions.* ban bricks the build (Sigra/Chimeway legitimately aliased) | high | mitigate | Banned-alias list derived from the frozen @extracted_companions MapSet (rulestead only); Sigra non-flag test proves the scope (D-14) |
| T-130-08 | Tampering | Module-eval Code.ensure_loaded? -> stale .beam confuses engine-present/absent test state | medium | mitigate | EXTRACT-04 AST prune-walk + belt regex forbids module-eval placement; runtime-probe-only invariant enforced |
| T-130-SC | Tampering | npm/pip/cargo installs | low | accept | No new packages (RESEARCH.md Package Legitimacy Audit: zero new deps); Code.string_to_quoted/2 + Macro.prewalk/3 + Path.wildcard stdlib only |
</threat_model>

<verification>
- phase130_extraction_guards_test.exs: EXTRACT-04 + D-27 + EXTRACT-03 non-vacuity controls green; EXTRACT-03 real-lib assertion skipped with Plan 05 pointer.
- CompanionGuard compiles --warnings-as-errors and passes its own check_source/1.
- check_source/1 and check_ensure_loaded_placement/1 behave correctly on synthetic controls.
</verification>

<success_criteria>
EXTRACT-04 guard is merge-blocking and green; the EXTRACT-03 detection logic is implemented and
non-vacuously proven (Rulestead detected, Sigra exempt); the D-27 runtime:false guard is green. The
EXTRACT-03 real-codebase assertion is correctly deferred to Plan 05 (post-extraction).
</success_criteria>

<output>
Create `.planning/phases/130-extraction-mechanics-footgun-guards/130-03-SUMMARY.md` when done
</output>
