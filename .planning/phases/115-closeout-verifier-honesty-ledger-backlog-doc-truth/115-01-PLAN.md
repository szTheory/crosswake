---
phase: 115-closeout-verifier-honesty-ledger-backlog-doc-truth
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/crosswake/planning/closeout_verifier.ex
  - test/crosswake/planning/closeout_verifier_test.exs
  - test/mix/tasks/closeout_verify_test.exs
autonomous: true
requirements: [GATE-02]
must_haves:
  truths:
    - "GATE-02: malformed, missing, empty, block-list, or junk expected_phases frontmatter produces blocking check id closeout.expected_phases."
    - "D-01/D-05: expected phases come only from a strict non-empty inline array parser; no guessed phase set is used."
    - "D-02: mix closeout.verify still prints the full CloseoutVerifier report before raising Mix.Error."
    - "D-03: closeout.expected_phases is relaxed when no active CLOSEOUT.md exists."
    - "D-04: phase-dependent checks do not fabricate phase scans after an invalid expected_phases contract."
    - "D-09/D-10/D-11: real validation ledgers require concrete tested_by/evidence frontmatter, while active validation-ledger-finalization deferrals remain narrow and resolved gaps do not reopen the escape hatch."
  artifacts:
    - path: "lib/crosswake/planning/closeout_verifier.ex"
      provides: "strict expected phase contract, stable closeout.expected_phases check, ledger evidence validation"
      contains: "closeout.expected_phases"
    - path: "test/crosswake/planning/closeout_verifier_test.exs"
      provides: "expected_phases and ledger-evidence regression coverage"
      contains: "closeout.expected_phases"
    - path: "test/mix/tasks/closeout_verify_test.exs"
      provides: "rendered report plus Mix.Error coverage for malformed expected phases"
      contains: "closeout.expected_phases"
  key_links:
    - from: "lib/crosswake/planning/closeout_verifier.ex"
      to: "test/crosswake/planning/closeout_verifier_test.exs"
      via: "private parser/check behavior exercised through CloseoutVerifier.run/1 fixtures"
      pattern: "closeout.expected_phases"
    - from: "lib/mix/tasks/closeout.verify.ex"
      to: "lib/crosswake/planning/closeout_verifier.ex"
      via: "Mix task renders CloseoutVerifier.render(report), then raises on failed status"
      pattern: "CloseoutVerifier.render"
---

## Artifacts This Phase Produces

| Artifact | Plan | Kind |
|----------|------|------|
| `closeout.expected_phases` | 115-01 | stable verifier check id |
| private helper `parse_expected_phases/1` | 115-01 | strict inline-array expected-phases parser |
| private helper `expected_phases_check/2` | 115-01 | report-first closeout-contract check |
| private helper `validation_ledger_evidence?/2` | 115-01 | concrete evidence validator for real ledgers |
| private helper `validation_exception_satisfied?/3` | 115-01 | accepted-exception validator for historically non-reconstructable ledgers |
| `test/crosswake/planning/closeout_verifier_test.exs` expected-phase and evidence cases | 115-01 | ExUnit regression coverage |
| `test/mix/tasks/closeout_verify_test.exs` malformed expected-phases CLI case | 115-01 | Mix task regression coverage |
| `.planning/milestones/v3.6-VALIDATION-EXCEPTION.md` | 115-02 | accepted exception artifact |
| normalized `tested_by:` and structured `evidence:` frontmatter on v3.8/v3.9 real target ledgers | 115-02 | historical validation ledger contract |
| DEBT-01 wording corrections in `.planning/REQUIREMENTS.md` and `.planning/ROADMAP.md` | 115-02 | planning-scope truth |
| `test/crosswake/planning/doc_truth_test.exs` | 115-03 | DOC-01 source-contract test |
| precedence rule in `.planning/MILESTONES.md` | 115-03 | canonical doc-truth rule |
| v8.0 shipped-state entry in `.planning/MILESTONES.md` | 115-03 | curated milestone truth |
| append-only note in `.planning/v1.0-MILESTONE-AUDIT.md` | 115-03 | audit annotation |

<objective>
Harden `Crosswake.Planning.CloseoutVerifier` so Phase 115 starts by making the closeout gate fail closed on invalid closeout contracts and bare ledger attestations.

Purpose: DEBT-01 depends on a trustworthy verifier. The executor must first remove the vacuous expected-phases behavior, add the stable `closeout.expected_phases` report check, and make ledger compliance require concrete evidence before historical artifacts are normalized.

Output: `CloseoutVerifier` report-first fail-closed behavior and focused ExUnit/Mix task tests for GATE-02.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/115-closeout-verifier-honesty-ledger-backlog-doc-truth/115-CONTEXT.md
@.planning/phases/115-closeout-verifier-honesty-ledger-backlog-doc-truth/115-RESEARCH.md
@.planning/phases/115-closeout-verifier-honesty-ledger-backlog-doc-truth/115-VALIDATION.md
@lib/crosswake/planning/closeout_verifier.ex
@lib/mix/tasks/closeout.verify.ex
@test/crosswake/planning/closeout_verifier_test.exs
@test/mix/tasks/closeout_verify_test.exs
@.planning/quick/260603-nzr-tighten-validation-ledger-closeout-gate/260603-nzr-SUMMARY.md
</context>

<source_coverage_audit>
| Source | Item | Coverage |
|--------|------|----------|
| GOAL | Closeout verifier fails closed instead of passing vacuously | Plan 115-01 tasks 1-2 |
| GOAL | Every flagged ledger has real evidence or accepted exception | Plan 115-02 tasks 1-3, enabled by 115-01 |
| GOAL | v8.0 planning documents converge | Plan 115-03 tasks 1-2 |
| REQ | GATE-02 | Plan 115-01 frontmatter and tasks |
| REQ | DEBT-01 | Plan 115-02 frontmatter and tasks |
| REQ | DOC-01 | Plan 115-03 frontmatter and tasks |
| RESEARCH | Strict inline expected_phases parser, no YAML dependency, report-first Mix behavior, evidence validation | Plan 115-01 tasks 1-2 |
| RESEARCH | v3.6 exception, v3.8/v3.9 evidence normalization | Plan 115-02 tasks 1-3 |
| RESEARCH | MILESTONES precedence and v1.0 audit immutability | Plan 115-03 tasks 1-2 |
| CONTEXT | D-01 through D-05 | Plan 115-01 tasks 1-2 |
| CONTEXT | D-06 through D-11 | Plan 115-02 tasks 1-3 plus 115-01 evidence helper |
| CONTEXT | D-12 through D-16 | Plan 115-03 tasks 1-2 |
| EXCLUDED | Deferred YAML dependency, live GitHub validation, separate doc-truth ADR, broad closeout CI modernization | Explicit prohibitions across plans |
</source_coverage_audit>

<must_haves>
Truths:
- GATE-02, D-01, D-05: current closeout frontmatter must contain a non-empty inline `expected_phases: ["..."]` array, top-level or under `phase_verification_coverage`; missing, empty, malformed, block-list, or junk forms are blocking.
- GATE-02, D-02: the new failure is a `Check` entry with id `closeout.expected_phases`; the Mix task prints the report before raising `Mix.Error`.
- GATE-02, D-03: no active closeout remains non-blocking for closeout-artifact checks, including the new expected-phases check.
- GATE-02, D-04: invalid expected phases must not cause dependent checks to scan a guessed phase list or create misleading missing-ledger findings.
- GATE-02, D-09, D-10: a real ledger with only `nyquist_compliant: true` is not compliant unless it also has concrete `tested_by:` and structured `evidence:` frontmatter.
- GATE-02, D-11: only an active `deferred_with_reason` entry with scope `validation-ledger-finalization` and status other than `resolved`/`closed` remains an escape hatch.

Prohibitions:
- Do not add a YAML dependency or general frontmatter parser.
- Do not restructure `.github/workflows/phase75-closeout-gate.yml` in this plan.
- Do not modify v3.8/v3.9 ledger files or v8.0 doc-truth files in this plan.
- Do not create synthetic v3.6 per-phase ledgers.
</must_haves>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add failing verifier and Mix task coverage for strict closeout contracts</name>
  <files>test/crosswake/planning/closeout_verifier_test.exs, test/mix/tasks/closeout_verify_test.exs</files>
  <read_first>
    - test/crosswake/planning/closeout_verifier_test.exs
    - test/mix/tasks/closeout_verify_test.exs
    - lib/crosswake/planning/closeout_verifier.ex
    - lib/mix/tasks/closeout.verify.ex
    - .planning/phases/115-closeout-verifier-honesty-ledger-backlog-doc-truth/115-CONTEXT.md
    - .planning/phases/115-closeout-verifier-honesty-ledger-backlog-doc-truth/115-VALIDATION.md
  </read_first>
  <behavior>
    - Missing `expected_phases:` in an active closeout yields blocking id `closeout.expected_phases` and no guessed phase-set observations.
    - Empty inline array, non-array junk, and block-list syntax yield blocking `closeout.expected_phases`.
    - Inline arrays under `phase_verification_coverage` and top-level inline arrays are accepted when non-empty.
    - Inactive closeout keeps `closeout.expected_phases` present but non-blocking.
    - A phase that resolves to zero `*-VALIDATION.md` files blocks when no active validation-ledger-finalization deferral or accepted exception exists.
    - A ledger with `nyquist_compliant: true` but no concrete `tested_by:` and structured `evidence:` frontmatter blocks.
    - Valid fixture ledgers with existing `type: test_file` refs, allowed local `type: command` refs, or non-empty `type: ci_run`/`type: artifact` refs pass.
    - `mix closeout.verify` renders the new check id before raising `Mix.Error` for malformed expected phases.
  </behavior>
  <action>
    Add RED tests before implementation per GATE-02 and D-01 through D-05. In `closeout_verifier_test.exs`, extend the stable-id assertion to include `closeout.expected_phases`; extend the inactive-closeout relaxation test to assert this id is present and non-blocking; add active-closeout fixtures for missing, empty, junk, block-list, valid top-level inline, and valid nested inline expected phases. Assert invalid contracts produce a blocking `closeout.expected_phases` check and that `closeout.verification.coverage`, `closeout.summaries.frontmatter`, and `closeout.validation.ledger` observe an explicit invalid-contract skip rather than phase-specific fabricated findings. Add fixture ledger tests for D-09 through D-11: bare compliance blocks, nonexistent `type: test_file` blocks, unsupported command text blocks, valid local test_file/command passes, and accepted exception fixtures satisfy a zero-ledger historical phase. In `closeout_verify_test.exs`, add a malformed expected-phases case that captures rendered output containing `closeout.expected_phases` and then raises through the existing `closeout verification found blocking issues` path. Do not change production files in this task.
  </action>
  <acceptance_criteria>
    - `test/crosswake/planning/closeout_verifier_test.exs` has cases naming `closeout.expected_phases`, missing expected phases, malformed expected phases, inactive closeout, zero ledger files, bare evidence, invalid evidence, valid evidence, and accepted exception behavior.
    - `test/mix/tasks/closeout_verify_test.exs` asserts rendered output contains `closeout.expected_phases` before `Mix.Error`.
    - Running the automated command before Task 2 fails for the new behavior, proving the tests are RED.
  </acceptance_criteria>
  <verify>
    <automated>mix test test/crosswake/planning/closeout_verifier_test.exs test/mix/tasks/closeout_verify_test.exs</automated>
  </verify>
  <done>Focused tests describe the GATE-02 contract before implementation and fail on the current permissive verifier.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement report-first fail-closed expected phases and evidence validation</name>
  <files>lib/crosswake/planning/closeout_verifier.ex, test/crosswake/planning/closeout_verifier_test.exs, test/mix/tasks/closeout_verify_test.exs</files>
  <read_first>
    - lib/crosswake/planning/closeout_verifier.ex
    - lib/mix/tasks/closeout.verify.ex
    - test/crosswake/planning/closeout_verifier_test.exs
    - test/mix/tasks/closeout_verify_test.exs
    - .planning/phases/115-closeout-verifier-honesty-ledger-backlog-doc-truth/115-RESEARCH.md
    - .planning/quick/260603-nzr-tighten-validation-ledger-closeout-gate/260603-nzr-SUMMARY.md
  </read_first>
  <action>
    In `CloseoutVerifier`, add stable check id `closeout.expected_phases` and include it in the inactive-closeout relaxation list per D-02 and D-03. Replace the permissive expected-phase helper with private `parse_expected_phases/1`, returning `{:ok, phases}` only for a non-empty inline array with phase numbers and `{:error, reason}` for missing, empty, malformed, block-list, or junk input per D-01 and D-05. Add private `expected_phases_check/2` that reports the parse result through the normal `Check` struct. Feed one shared parse result into `phase_verification_check`, `summary_frontmatter_check`, and `validation_ledger_check`; on parse error, those checks must produce non-blocking skipped observations such as `skipped: invalid expected_phases contract` and must not inspect a guessed phase list per D-04. Update `prior_validation_debt_check/2` so malformed prior closeouts never use a guessed phase list; invalid prior contracts should be listed as unsatisfied prior debt with the offending closeout path or milestone.

    Add private `validation_ledger_evidence?/2` and use it from `validation_ledger_check/2` and `prior_validation_debt_check/2` for real ledgers per D-09 and D-10. Compliance requires `nyquist_compliant: true`, at least one non-empty `tested_by:` entry, and at least one structured `evidence:` item with `type:` and `ref:`. Validate `type: test_file` by checking the referenced file exists under `cwd`; validate `type: command` only when the ref starts with an executable local command shape such as `mix test`, `mix compile`, or `mix closeout.verify`; allow `type: ci_run` and `type: artifact` only when ref is non-empty. Add private `validation_exception_satisfied?/3` for the D-07 artifact shape so historical zero-ledger phases can pass only when `.planning/milestones/<milestone>-VALIDATION-EXCEPTION.md` has `status: accepted_exception`, `scope: validation-ledger-finalization`, `not_reconstructable: true`, the phase in `affected_phases`, and concrete evidence refs. Preserve resolved/closed deferral semantics so D-11 remains true and `resolved_gaps` still cannot reopen the escape hatch. Keep `Mix.Tasks.Closeout.Verify` report-first behavior unchanged unless tests require only assertion updates.
  </action>
  <acceptance_criteria>
    - `CloseoutVerifier.run/1` reports `closeout.expected_phases` in every standard report.
    - Invalid expected phases make the report failed through `closeout.expected_phases`, while dependent phase checks do not name phases from a guessed list.
    - `mix closeout.verify --cwd <fixture>` prints `closeout.expected_phases` and then raises `Mix.Error` for malformed expected phases.
    - Real ledger compliance requires `nyquist_compliant: true`, `tested_by:`, and structured concrete `evidence:`.
    - `type: test_file` evidence fails when the file does not exist; unsupported command evidence fails; local `mix test`, `mix compile`, and `mix closeout.verify` command refs are accepted.
    - `mix closeout.verify` in the live repo still passes in the no-active-closeout state after the new id is relaxed.
  </acceptance_criteria>
  <verify>
    <automated>mix test test/crosswake/planning/closeout_verifier_test.exs test/mix/tasks/closeout_verify_test.exs && mix closeout.verify</automated>
  </verify>
  <done>GATE-02 passes: the verifier fails closed on invalid contracts and bare ledger evidence while the Mix task keeps report-first failure semantics.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| planning artifact -> verifier | `CLOSEOUT.md` and `*-VALIDATION.md` are untrusted text inputs parsed by the verifier. |
| verifier report -> CI/Mix task | The report determines whether `mix closeout.verify` exits successfully or raises. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-115-01 | Tampering | expected-phases parsing | mitigate | Fail closed through `closeout.expected_phases`; reject missing/malformed contracts and do not substitute a guessed phase set. |
| T-115-02 | Repudiation | validation ledger evidence | mitigate | Require `tested_by:` plus concrete structured `evidence:` refs; local file refs must exist and local commands must use approved executable shapes. |
| T-115-03 | Denial of Service | inactive closeout gate | mitigate | Include `closeout.expected_phases` in inactive-closeout relaxation so ordinary mid-milestone planning edits do not fail while no current CLOSEOUT.md exists. |
| T-115-SC | Tampering | package installs | mitigate | No package-manager installs in this plan; no YAML parser dependency is introduced. |
</threat_model>

<verification>
- `mix test test/crosswake/planning/closeout_verifier_test.exs test/mix/tasks/closeout_verify_test.exs`
- `mix closeout.verify`
</verification>

<success_criteria>
GATE-02 is complete when malformed or absent expected phases are blocking via `closeout.expected_phases`, zero-ledger phases with no active deferral or accepted exception block, bare ledger attestations block, and the Mix task prints diagnostics before raising.
</success_criteria>

<output>
Create `.planning/phases/115-closeout-verifier-honesty-ledger-backlog-doc-truth/115-01-SUMMARY.md` when done.
</output>
