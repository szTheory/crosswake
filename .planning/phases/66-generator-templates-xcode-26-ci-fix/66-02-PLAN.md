---
phase: 66-generator-templates-xcode-26-ci-fix
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/mix/tasks/crosswake.gen.shell.ex
  - priv/templates/crosswake/shell/android/app/src/main/AndroidManifest.xml.eex
  - priv/templates/crosswake/shell/ios/Info.plist.eex
  - priv/templates/crosswake/shell/ios/CrosswakeShell.entitlements.eex
  - priv/templates/crosswake/shell/ios/PrivacyInfo.xcprivacy.eex
autonomous: true
requirements:
  - TMPL-01
  - TMPL-02
  - TMPL-03
must_haves:
  truths:
    - "mix crosswake.gen.shell emits iOS permission, entitlement, and privacy scaffolds"
    - "mix crosswake.gen.shell emits Android manifest permission scaffolds"
    - "Scaffolds contain ADOPT: markers and do not inject real native values without host action"
    - "Generated scaffolds cover all capability families that require permissions/entitlements"
  artifacts:
    - path: "lib/mix/tasks/crosswake.gen.shell.ex"
      provides: "Generator logic mapping capabilities to template emission"
    - path: "priv/templates/crosswake/shell/ios/CrosswakeShell.entitlements.eex"
      provides: "iOS entitlements template"
    - path: "priv/templates/crosswake/shell/ios/PrivacyInfo.xcprivacy.eex"
      provides: "iOS privacy info template"
  key_links: []
---

<objective>
Update the generator to emit iOS and Android permission/entitlement scaffolds with ADOPT: markers, parity-locked to declared capabilities.

Purpose: Fulfill TMPL-01, TMPL-02, and TMPL-03 by surfacing necessary permissions and entitlements during shell generation while strictly requiring the host to take ownership of the final values via ADOPT: markers.
Output: Updated generator task and template files with scaffolded ADOPT: markers.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@lib/crosswake/support_matrix/support_matrix.ex
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create iOS entitlements and privacy info templates</name>
  <files>
    priv/templates/crosswake/shell/ios/CrosswakeShell.entitlements.eex,
    priv/templates/crosswake/shell/ios/PrivacyInfo.xcprivacy.eex,
    lib/mix/tasks/crosswake.gen.shell.ex
  </files>
  <action>
    Create EEx templates for `CrosswakeShell.entitlements` and `PrivacyInfo.xcprivacy` in `priv/templates/crosswake/shell/ios/`. Add them to `@ios_templates` in `lib/mix/tasks/crosswake.gen.shell.ex`.
    Inject `ADOPT: CROSSWAKE` comment markers in these files where real values would normally be provided, outlining the entitlements (e.g., Push Notifications, Domains) required by capability families.
  </action>
  <verify>
    <automated>ls priv/templates/crosswake/shell/ios/ | grep -E "entitlements|xcprivacy" | wc -l | grep -v '^0$'</automated>
  </verify>
  <done>iOS entitlement and privacy templates exist and are integrated into the generator</done>
</task>

<task type="auto">
  <name>Task 2: Inject capability-parity ADOPT: markers into manifest templates</name>
  <files>
    priv/templates/crosswake/shell/android/app/src/main/AndroidManifest.xml.eex,
    priv/templates/crosswake/shell/ios/Info.plist.eex,
    lib/mix/tasks/crosswake.gen.shell.ex
  </files>
  <action>
    Update `AndroidManifest.xml.eex` and `Info.plist.eex` to conditionally emit permission declarations (`uses-permission`, `NSCameraUsageDescription`, etc.) based on the capabilities present in the manifest/project.
    Use `ADOPT: CROSSWAKE` comment markers directly adjacent to or inside the scaffolded elements so the host clearly sees what they must claim ownership of.
    Ensure every capability requiring a permission/entitlement (e.g., `notification_token`, `media_capture`, `haptics`) has a corresponding scaffold block.
  </action>
  <verify>
    <automated>grep -r "ADOPT:" priv/templates/crosswake/shell/ | wc -l | grep -v '^0$'</automated>
  </verify>
  <done>Templates emit capability-specific scaffolds with ADOPT: markers</done>
</task>

</tasks>

<verification>
Templates contain `ADOPT:` markers. `crosswake.gen.shell` task processes the new `.eex` templates.
</verification>

<success_criteria>
Adopters can generate permission/entitlement scaffolds that are parity-locked to declared capabilities with explicit `ADOPT:` markers, fulfilling TMPL-01, TMPL-02, and TMPL-03.
</success_criteria>

<output>
Create `.planning/phases/66-generator-templates-xcode-26-ci-fix/66-02-SUMMARY.md` when done
</output>
