---
phase: 66-generator-templates-xcode-26-ci-fix
plan: 03
type: execute
wave: 2
depends_on: ["66-02"]
files_modified:
  - lib/crosswake/doctor/doctor.ex
autonomous: true
requirements:
  - TMPL-04
must_haves:
  truths:
    - "mix crosswake.doctor flags un-replaced ADOPT: markers in generated native files"
    - "mix crosswake.doctor checks for capability vs permission drift in native files"
  artifacts:
    - path: "lib/crosswake/doctor/doctor.ex"
      provides: "Template placeholder and drift checks"
  key_links: []
---

<objective>
Update the Doctor to flag un-replaced template placeholders (`ADOPT:`) and detect manifest↔shell↔docs permission drift.

Purpose: Fulfill TMPL-04 by enforcing that hosts replace the `ADOPT:` scaffolds and keep their native permissions synchronized with the declared capabilities.
Output: Updated `Crosswake.Doctor` module with zero-dependency string scanning.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@lib/crosswake/doctor/doctor.ex
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add placeholder drift check to Doctor</name>
  <files>lib/crosswake/doctor/doctor.ex</files>
  <action>
    Add a new finding generator (e.g., `phase_66_generator_drift_findings/2`) called from `Crosswake.Doctor.run/1`.
    For iOS, read `Info.plist`, `CrosswakeShell.entitlements`, and `PrivacyInfo.xcprivacy` using `File.read!/1` (if they exist). For Android, read `AndroidManifest.xml`.
    Use zero-dependency string scanning (`String.contains?(content, "ADOPT:")`) to find unresolved placeholders.
    If found, emit a `:error` severity finding (e.g., code `shell_unreplaced_placeholders`) instructing the user to replace the `ADOPT:` markers with their host-owned values.
  </action>
  <verify>
    <automated>grep -q "ADOPT:" lib/crosswake/doctor/doctor.ex || grep -q "shell_unreplaced_placeholders" lib/crosswake/doctor/doctor.ex</automated>
  </verify>
  <done>Doctor flags ADOPT: markers as errors</done>
</task>

<task type="auto">
  <name>Task 2: Add capability-permission drift check</name>
  <files>lib/crosswake/doctor/doctor.ex</files>
  <action>
    Extend the drift check logic to also scan the raw native file strings for expected capability permission declarations (e.g. `<uses-permission android:name="android.permission.INTERNET"`).
    Compare the declared capabilities in `manifest` (e.g. `notification_token`, `media_capture`) against the presence of their required XML/plist strings in the native files.
    Emit a `:error` severity finding (e.g., code `shell_permission_drift`) if a declared capability lacks its corresponding permission string in the native manifest, ensuring manifest↔shell↔docs parity.
  </action>
  <verify>
    <automated>grep -q "shell_permission_drift" lib/crosswake/doctor/doctor.ex</automated>
  </verify>
  <done>Doctor checks for capability vs permission drift</done>
</task>

</tasks>

<verification>
The `Crosswake.Doctor` module successfully executes zero-dependency string scans for `ADOPT:` and expected permission mappings.
</verification>

<success_criteria>
Drift and un-replaced placeholders are correctly flagged by `mix crosswake.doctor`, fulfilling TMPL-04.
</success_criteria>

<output>
Create `.planning/phases/66-generator-templates-xcode-26-ci-fix/66-03-SUMMARY.md` when done
</output>
