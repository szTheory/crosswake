---
phase: 66-generator-templates-xcode-26-ci-fix
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .github/workflows/phase5-proof.yml
  - .github/workflows/phase18-proof.yml
  - .github/workflows/phase23-proof.yml
  - .github/workflows/phase34-proof.yml
  - .github/workflows/phase41-proof.yml
  - .github/workflows/phase43-proof.yml
  - .github/workflows/phase45-proof.yml
  - .github/workflows/phase48-proof.yml
  - .github/workflows/phase52-proof.yml
  - .github/workflows/phase58-proof.yml
autonomous: true
requirements:
  - AVER-05
must_haves:
  truths:
    - "iOS CI builds target the Xcode 26 SDK for App Store-submittable proof"
  artifacts:
    - path: ".github/workflows/phase5-proof.yml"
      provides: "Xcode 26 SDK target CI config"
      contains: "DEVELOPER_DIR"
  key_links: []
---

<objective>
Update iOS CI workflows to target the Xcode 26 SDK so iOS shell proofs remain App Store-submittable.

Purpose: AVER-05 requires iOS CI to use the correct Xcode SDK version.
Output: Updated CI workflow files with DEVELOPER_DIR correctly configured for Xcode 26.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Enforce Xcode 26 SDK in CI</name>
  <files>.github/workflows/*.yml</files>
  <action>
    Add or update the `DEVELOPER_DIR` environment variable to `/Applications/Xcode_26.0.app/Contents/Developer` (or the equivalent correct path for Xcode 26 on GitHub Actions `macos-15` runners) in all `.github/workflows/phase*-proof.yml` files that execute iOS native proofs or `xcodebuild` steps.
    Do not touch Android-only workflows, but any workflow invoking `script/verify_generated_ios_shell.sh` or `xcodebuild` must have this environment variable set.
  </action>
  <verify>
    <automated>grep -r "DEVELOPER_DIR.*Xcode_26" .github/workflows/ | grep -v '^#' | wc -l | grep -v '^0$'</automated>
  </verify>
  <done>DEVELOPER_DIR points to Xcode 26 across iOS CI proofs</done>
</task>

</tasks>

<verification>
grep across `.github/workflows/` confirms the Xcode 26 SDK targeting.
</verification>

<success_criteria>
All iOS shell proof workflows target Xcode 26, fulfilling AVER-05.
</success_criteria>

<output>
Create `.planning/phases/66-generator-templates-xcode-26-ci-fix/66-01-SUMMARY.md` when done
</output>
