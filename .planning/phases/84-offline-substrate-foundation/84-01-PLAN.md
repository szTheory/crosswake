---
phase: 84-offline-substrate-foundation
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - lib/crosswake/offline/content_pack.ex
  - test/crosswake/offline/content_pack_test.exs
  - lib/crosswake/policy/schema.ex
  - lib/crosswake/manifest/builder.ex
  - test/crosswake/policy/route_test.exs
  - test/crosswake/manifest/builder_test.exs
autonomous: true
requirements:
  - OFF-01
must_haves:
  truths:
    - "Crosswake provides a structured data type for defining offline content packs that the manifest builder can utilize"
    - "Developers can configure routes to explicitly require specific offline asset bundles"
    - "Client applications can discover required offline asset bundles from the generated manifest"
  artifacts:
    - path: "lib/crosswake/offline/content_pack.ex"
      provides: "Elixir data structure for server-side generation of offline asset/data bundles"
      min_lines: 10
    - path: "test/crosswake/offline/content_pack_test.exs"
      provides: "Verification of ContentPack behavior and encoding"
  key_links:
    - from: "Crosswake.Policy.Schema"
      to: "Crosswake.Offline.ContentPack"
      via: "Schema casting converts pack maps into ContentPack structs"
      pattern: "Crosswake.Offline.ContentPack"
    - from: "Crosswake.Manifest.Builder"
      to: "Crosswake.Offline.ContentPack"
      via: "Manifest compiler pattern matches on ContentPack structs to build the pack_registry"
      pattern: "%ContentPack{"
---

<objective>
Define `Crosswake.Offline.ContentPack` as the standard struct for Offline Island bundles, and integrate it into `Crosswake.Manifest.Builder` and route policies to link content packs with manifest generation.

Purpose: Provides the foundation for OFF-01, allowing the Elixir backend to describe and validate asset bundles required by offline experiences using a typed struct rather than raw maps.
Output: A new `ContentPack` struct, updates to route schema and manifest builder to utilize it, and comprehensive test coverage ensuring route policies properly integrate with offline packs.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@lib/crosswake/policy/schema.ex
@lib/crosswake/policy/route.ex
@lib/crosswake/manifest/builder.ex
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create ContentPack Struct</name>
  <files>lib/crosswake/offline/content_pack.ex, test/crosswake/offline/content_pack_test.exs</files>
  <behavior>
    - Test 1: Given valid keys (id, version, kind), a ContentPack struct is created successfully.
    - Test 2: Given a ContentPack struct, it JSON encodes correctly matching the expected keys (including assets and data_payloads).
  </behavior>
  <action>
    Define the `Crosswake.Offline.ContentPack` module. It must:
    - Define `@enforce_keys [:id, :version, :kind]`.
    - Define `defstruct [:id, :version, :kind, :integrity, assets: [], data_payloads: []]`.
    - Derive `Jason.Encoder`.
    - Provide a complete `@type t` definition covering all fields (`id: String.t()`, `version: String.t()`, `kind: atom()`, `integrity: map() | nil`, `assets: [String.t()]`, `data_payloads: [String.t()]`).
  </action>
  <verify>
    <automated>mix test test/crosswake/offline/content_pack_test.exs</automated>
  </verify>
  <done>Struct is defined, enforces required keys, properly encodes to JSON, and is covered by unit tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Integrate ContentPack into Route Policy and Manifest Compilation</name>
  <files>lib/crosswake/policy/schema.ex, lib/crosswake/manifest/builder.ex, test/crosswake/policy/route_test.exs, test/crosswake/manifest/builder_test.exs</files>
  <behavior>
    - Test 1: `RouteTest` validates an `offline: :local_first` route declaration with explicit `packs` automatically casting into `ContentPack` structs.
    - Test 2: `BuilderTest` verifies that a route containing `packs` as `ContentPack` structs results in a manifest where the `pack_registry` is populated and the corresponding route entry contains the pack reference `id@version`.
  </behavior>
  <action>
    Integrate `Crosswake.Offline.ContentPack` so it is actively used in the routing system and manifest builder.
    - In `lib/crosswake/policy/schema.ex`, update the `packs` NimbleOptions definition. Instead of just validating maps, use a custom validator/caster (e.g., `{:custom, ...}`) or `map` type to cast the raw maps into `%Crosswake.Offline.ContentPack{}` structs so `route.packs` contains a list of structs.
    - In `lib/crosswake/manifest/builder.ex`, update `pack_registry/1` and `pack_reference/1` (and any other relevant private functions) to pattern match on `%Crosswake.Offline.ContentPack{}` instead of bare maps. Ensure `alias Crosswake.Offline.ContentPack` is used.
    - In `route_test.exs`, add a test case under `describe "new/1"` named `test "accepts pack bindings for local_first routes"` which creates an offline_island + local_first route with a pack map, asserting it is cast to a `ContentPack` struct on the created Route.
    - In `builder_test.exs`, add a test that generates a manifest from a route with `packs: [%Crosswake.Offline.ContentPack{id: "data_core", version: "1.0.0", kind: :content}]` and asserts that `manifest.pack_registry` contains `"data_core@1.0.0"` and the route's `packs` list includes `"data_core@1.0.0"`.
  </action>
  <verify>
    <automated>mix test test/crosswake/policy/route_test.exs test/crosswake/manifest/builder_test.exs</automated>
  </verify>
  <done>Route policies securely parse pack configurations into ContentPack structs, and the manifest builder successfully processes these structs into the root pack registry.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client → API | Offline bundles downloaded by clients must be verified against integrity checksums provided by Manifest packs. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-84-01 | Tampering | `ContentPack` / Manifest | mitigate | Define the `integrity` map attribute on ContentPack and pack declarations to support SHA-256 (or similar) client-side validation against tampering. |
| T-84-02 | Spoofing | Route Policy | mitigate | Restrict route policy definition via `NimbleOptions` strict validation so malicious manifests cannot inject arbitrary data. |
</threat_model>

<verification>
- `mix test test/crosswake/offline/content_pack_test.exs` passes
- `mix test test/crosswake/policy/route_test.exs test/crosswake/manifest/builder_test.exs` passes
</verification>

<success_criteria>
`ContentPack` structure is formally defined, can serialize to JSON, and is actively used by the routing policy schema and manifest builder to process offline route packs.
</success_criteria>

<output>
Create `.planning/phases/84-offline-substrate-foundation/84-01-SUMMARY.md` when done
</output>
