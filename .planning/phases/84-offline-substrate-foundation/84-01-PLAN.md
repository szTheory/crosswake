---
phase: 84-offline-substrate-foundation
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - lib/crosswake/offline/content_pack.ex
  - test/crosswake/offline/content_pack_test.exs
  - test/crosswake/policy/route_test.exs
  - test/crosswake/manifest/builder_test.exs
autonomous: true
requirements:
  - OFF-01
must_haves:
  truths:
    - "Server can construct and JSON-encode a ContentPack representing an Offline Island bundle"
    - "Route policy allows local_first routes to bind to explicit ContentPacks via packs attribute"
    - "Manifest compilation accurately exposes pack requirements in both the route entry and pack_registry"
  artifacts:
    - path: "lib/crosswake/offline/content_pack.ex"
      provides: "Elixir data structure for server-side generation of offline asset/data bundles"
      min_lines: 10
    - path: "test/crosswake/offline/content_pack_test.exs"
      provides: "Verification of ContentPack behavior and encoding"
  key_links:
    - from: "Crosswake.Policy.Route"
      to: "Crosswake.Manifest.Builder"
      via: "Manifest compiler maps route packs into a top-level pack_registry"
      pattern: "Types.new_pack_entry"
---

<objective>
Define `Crosswake.Offline.ContentPack` as the standard struct for Offline Island bundles, and verify that `local_first` route policies correctly pass these bindings through to the Manifest `pack_registry`.

Purpose: Provides the foundation for OFF-01, allowing the Elixir backend to describe and validate asset bundles required by offline experiences.
Output: A new `ContentPack` struct, tests for it, and comprehensive test coverage ensuring route policies and the manifest builder properly integrate with offline packs.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@lib/crosswake/policy/schema.ex
@lib/crosswake/policy/route.ex
@lib/crosswake/manifest/builder.ex
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create ContentPack Struct</name>
  <files>lib/crosswake/offline/content_pack.ex, test/crosswake/offline/content_pack_test.exs</files>
  <behavior>
    - Test 1: Given valid keys (id, version, kind), a ContentPack struct is created successfully.
    - Test 2: Given a ContentPack struct, it JSON encodes correctly matching the expected keys (including assets and data_payloads).
  </behavior>
  <action>
    Define the `Crosswake.Offline.ContentPack` module. It must:
    - Define `@enforce_keys [:id, :version, :kind]`.
    - Define `defstruct [:id, :version, :kind, :integrity, assets: [], data_payloads: []]`.
    - Derive `Jason.Encoder`.
    - Provide a complete `@type t` definition covering all fields (`id: String.t()`, `version: String.t()`, `kind: atom()`, `integrity: map() | nil`, `assets: [String.t()]`, `data_payloads: [String.t()]`).
  </action>
  <verify>
    <automated>mix test test/crosswake/offline/content_pack_test.exs</automated>
  </verify>
  <done>Struct is defined, enforces required keys, properly encodes to JSON, and is covered by unit tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Test Route Policy and Manifest Compilation for Packs</name>
  <files>test/crosswake/policy/route_test.exs, test/crosswake/manifest/builder_test.exs</files>
  <behavior>
    - Test 1: `RouteTest` validates an `offline: :local_first` route declaration with explicit `packs` (verifying local_first supports packs).
    - Test 2: `BuilderTest` verifies that a route containing `packs` results in a manifest where the `pack_registry` is populated and the corresponding route entry contains the pack reference `id@version`.
  </behavior>
  <action>
    Since `Crosswake.Policy.Schema`, `Crosswake.Policy.Route`, and `Crosswake.Manifest.Builder` already logically support the `packs` attribute (as `pack_requirement`), our goal here is to explicitly codify their usage with `offline: :local_first` and ensure test coverage for manifest builder.
    - In `route_test.exs`, add a test case under `describe "new/1"` named `test "accepts pack bindings for local_first routes"` which creates an offline_island + local_first route with a pack.
    - In `builder_test.exs`, add a test that generates a manifest from a route with `packs: [%{id: "data_core", version: "1.0.0", kind: :content, integrity: nil}]` and asserts that `manifest.pack_registry` contains `"data_core@1.0.0"` and the route's `packs` list includes `"data_core@1.0.0"`.
  </action>
  <verify>
    <automated>mix test test/crosswake/policy/route_test.exs test/crosswake/manifest/builder_test.exs</automated>
  </verify>
  <done>Test coverage confirms local_first routes accept pack declarations and that the manifest builder correctly extracts these packs into the root pack registry.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client → API | Offline bundles downloaded by clients must be verified against integrity checksums provided by Manifest packs. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-84-01 | Tampering | `ContentPack` / Manifest | mitigate | Define the `integrity` map attribute on ContentPack and pack declarations to support SHA-256 (or similar) client-side validation against tampering. |
| T-84-02 | Spoofing | Route Policy | mitigate | Restrict route policy definition via `NimbleOptions` strict validation so malicious manifests cannot inject arbitrary data. |
</threat_model>

<verification>
- `mix test test/crosswake/offline/content_pack_test.exs` passes
- `mix test test/crosswake/policy/route_test.exs test/crosswake/manifest/builder_test.exs` passes
</verification>

<success_criteria>
`ContentPack` structure is formally defined, can serialize to JSON, and the integration linking offline route packs to the generated manifest's pack registry is fully covered by tests.
</success_criteria>

<output>
Create `.planning/phases/84-offline-substrate-foundation/84-01-SUMMARY.md` when done
</output>