---
phase: 154-the-control-contract-seam
plan: 08
type: execute
wave: 7
depends_on: [154-07]
autonomous: false
requirements: [CTRL-01, CTRL-02, CTRL-05, HRDN-01]
pr_boundary: "PR #3 (tail) — honest-claims documentation sweep and the phase's human verification gate"
files_modified:
  - guides/compatibility.md
  - guides/adopter_profiles.md
  - guides/web_to_mobile_migration.md
  - guides/bridge.md
  - guides/support_matrix.md

must_haves:
  truths:
    - "Every guide that describes the seam shows a SINGLE push and a SINGLE reply clause — no example branches on availability before pushing, because a pre-check is a three-way branch by the back door (D-09, D-21)."
    - "The guides state plainly that an in-flight ask does NOT survive a LiveView reconnect, that a fresh epoch is minted at mount, and that the recovery path is fallback UI rebuilt from assigns (D-24)."
    - "The guides state which parts of CTRL-04 and CTRL-05 are structurally impossible and which are CI-caught, rather than claiming the stronger form for both (D-45, D-52)."
    - "The compatibility guide states that the 14th denial reason has no companion-Finding axis and is minted only by core, so companion authors are not misled into returning it (D-15)."
    - "The support matrix states that iOS native reply delivery reaches adopters with the Phase 156 native release rather than claiming uniform completeness (D-03)."
    - "A developer running the showcase in a desktop browser sees the fail-closed thesis render itself, in brand voice, with correct light and dark rendering (D-65, D-69)."
  prohibitions:
    - statement: "MUST NOT document the seam in a way that suggests an adopter should check availability before pushing — every guide example shows one push and one reply clause, because an availability pre-check reintroduces exactly the three-way branch the one-denial contract exists to collapse (D-09, D-21)."
    - statement: "MUST NOT document in-flight asks as surviving a reconnect — a reader who assumes durability will build a UI that silently loses a user's answer, which is the exact silently-wrong failure this project exists to prevent (D-24)."
    - statement: "MUST NOT claim the structural-impossibility form for the parts of CTRL-04 and CTRL-05 that are only CI-caught — name which is which, or the requirement quietly overclaims (D-45, D-52)."
  artifacts:
    - path: "guides/bridge.md"
      provides: "the adopter-facing seam guide: one push, one reply clause, resolve, reconnect semantics"
      contains: "handle_info"
    - path: "guides/compatibility.md"
      provides: "the 14th denial reason documented as core-minted with no companion Finding axis"
      contains: "shell_unreachable"
    - path: "guides/web_to_mobile_migration.md"
      provides: "the migration path from a hand-rolled inline script to the seam"
      contains: "Crosswake.Bridge.push"
  key_links:
    - from: "guides/bridge.md"
      to: "lib/crosswake/bridge.ex"
      via: "documented function signatures must match the shipped ones"
      pattern: "push/3"
    - from: "guides/support_matrix.md"
      to: "lib/crosswake/manifest/builder.ex"
      via: "the matrix's rebuild and interaction columns derive from the capability catalog"
      pattern: "interaction"
---

<objective>
Close the phase with the documentation pass that keeps its claims honest, then put a human in front
of the one thing no automated harness can judge: whether the desktop-browser evidence panel actually
reads as the fail-closed thesis rather than as a broken demo.

D-77 named scope honesty as this phase's biggest risk. The corresponding documentation risk is
claim inflation: three of this phase's requirements have a strong form and a weaker true form, and
the guides are where the difference either gets stated or gets lost. CTRL-04 closes the mechanical
plugin-catalog road completely but does not stop a maintainer adding forty controls one string at a
time (D-45). CTRL-05's enforce-keys change is the only part that earns "structurally impossible";
everything else is CI-caught (D-52). CTRL-01's reply half is satisfied on Android natively, on every
platform by server-synthesized denials, and on iOS only in-repo until the Phase 156 release (D-03).

Purpose: this project's brand is compatibility honesty. A guide that overclaims on the milestone's
headline seam costs more than the feature earns.

Output: five guides that state the true form of each claim, and a human sign-off on the showcase's
default desktop experience.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/154-the-control-contract-seam/154-CONTEXT.md
@.planning/phases/154-the-control-contract-seam/154-RESEARCH.md
@.planning/phases/154-the-control-contract-seam/154-07-SUMMARY.md
@brandbook/BRAND-SPEC.md

Decisions this plan implements: D-03, D-09, D-15, D-16 (as dispositioned by Plan 05's checkpoint),
D-21, D-24, D-45, D-52, D-65, D-69, D-77.
</context>

<tasks>

<task type="auto">
  <name>Task 1: The honest-claims documentation sweep</name>
  <files>guides/bridge.md, guides/compatibility.md, guides/adopter_profiles.md, guides/web_to_mobile_migration.md, guides/support_matrix.md</files>
  <read_first>
    - `guides/bridge.md` — as left by Plans 01 and 06, so this pass tightens rather than duplicates
    - `guides/compatibility.md` — the companion Finding-versus-core-Denial boundary, which the 14th reason must be documented as sitting OUTSIDE of
    - `guides/adopter_profiles.md` — the per-profile guidance that must now mention the seam and the attach requirement
    - `guides/web_to_mobile_migration.md` — the migration narrative that should now show the inline-script-to-seam path the showcase just walked
    - `guides/support_matrix.md` — as left by Plans 02 and 06
    - `lib/crosswake/bridge.ex` — the shipped signatures, so documented forms match exactly
    - `lib/crosswake/bridge/catalog_guard.ex` — the six-criteria labelling in its moduledoc, which the guides must not contradict
    - `.planning/phases/154-the-control-contract-seam/154-05-SUMMARY.md` — the D-16 disposition selected at Plan 05's checkpoint, which determines what the compatibility guide says about native reason strings
    - `brandbook/BRAND-SPEC.md` §2 pillar 3 and §6 — the no-hidden-bridge-magic pillar and the voice rules
  </read_first>
  <action>
    Sweep `guides/bridge.md`, `guides/compatibility.md`, `guides/adopter_profiles.md`,
    `guides/web_to_mobile_migration.md`, and `guides/support_matrix.md` so each states the TRUE form
    of this phase's claims.

    Every seam example shows one push and one reply clause. No example checks availability first
    (D-09), and the fire-and-forget case is shown needing no reply clause at all (D-21). If a reader
    can copy an example into a three-way branch, the example is wrong.

    State plainly in `guides/bridge.md` that an in-flight ask does not survive a reconnect, that a
    fresh epoch is minted at mount, that late replies drop as foreign-epoch, and that the recovery
    path is fallback UI rebuilt from assigns, which Phase 155 ships (D-24). Document `resolve/2` and
    when the fallback handler calls it.

    In `guides/compatibility.md`, document the 14th denial reason as core-minted with no companion
    Finding axis (D-15), so a companion author does not try to return it. Record the D-16 disposition
    selected at Plan 05's checkpoint: what the shipped natives emit today, what the server does with
    an unrecognized reason string, and — if the deferral option was chosen — the seed that tracks the
    remaining work.

    In `guides/support_matrix.md`, keep the four locked change-class strings byte-identical and
    confirm the Phase 156 statement about iOS reply delivery survives (D-03).

    In `guides/adopter_profiles.md`, add the attach requirement per profile — a socket that never
    attached raises a named error on first push, which is a brand-new install-time failure surface
    every adopter hits exactly once.

    In `guides/web_to_mobile_migration.md`, show the inline-script-to-seam migration the showcase
    just walked, framed as the CSP hardening it is.

    Across all five: where a claim has a strong form and a weaker true form, write the weaker true
    form and name the boundary. The enforce-keys change is the only part of CTRL-04 and CTRL-05 that
    is structurally impossible; everything else is CI-caught (D-45, D-52). Do not soften this into
    marketing.
  </action>
  <verify>
    <automated>mix test test/crosswake/guides/ test/crosswake/proof/phase69_docs_contract_parity_test.exs test/crosswake/hex_page_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'shell_unreachable' guides/compatibility.md` returns at least 1
    - `grep -ci 'reconnect' guides/bridge.md` returns at least 1
    - `grep -c 'resolve' guides/bridge.md` returns at least 1
    - `grep -ci 'attach' guides/adopter_profiles.md` returns at least 1
    - `grep -c 'Crosswake.Bridge.push' guides/web_to_mobile_migration.md` returns at least 1
    - `grep -ci 'available?\|connected?' guides/bridge.md guides/adopter_profiles.md guides/web_to_mobile_migration.md` returns 0 across all three (no example teaches a pre-check)
    - `grep -c 'compatibility-bump only' guides/support_matrix.md` returns at least 1 (the locked legend strings are still byte-identical)
    - `grep -ci 'CI-caught\|caught in CI' guides/support_matrix.md guides/bridge.md` returns at least 1 (the strong-versus-true-form distinction is stated, not implied)
    - `mix test test/crosswake/guides/` exits 0
    - `mix docs` exits 0
  </acceptance_criteria>
  <done>Five guides state the true form of every claim this phase makes, teach one push and one reply clause, and name the reconnect semantics rather than leaving a reader to assume durability.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verification of the showcase evidence panel, doctor output, and the guard's failure message</name>
  <what-built>
    The full Phase 154 seam is in place and every automated gate is green: `Crosswake.Bridge.push/3`
    with a correlated typed reply, the one-shape denial across no-shell / old-shell / unwired-hook /
    timeout / refusal, the loud raise for an undeclared capability, the merge-blocking catalog guard,
    the shipped JS hook, the iOS return leg, and both showcase LiveViews migrated off the hand-rolled
    inline script.

    What automation CANNOT judge is whether the desktop-browser evidence panel actually reads as the
    fail-closed thesis. D-65 makes that panel the strongest demo in the project — a skeptical Phoenix
    developer with no shell should come away convinced, not confused. A Playwright assertion can
    confirm the reason string is present; it cannot tell you the copy lands, that the two identity
    labels teach the distinction, or that a reader does not read "declined" as "broken".
  </what-built>
  <how-to-verify>
    1. `cd examples/phoenix_host && mix phx.server`, then open the AdminPilot approval route in a
       desktop browser. There is deliberately no shell here — that is the case under test.
    2. BEFORE approving: confirm the panel shows the idle copy and no dispatch or reply rows. It
       should read as honest and non-apologetic — no "coming soon", no "nothing here yet".
    3. Approve a request. Confirm the panel now shows: the capability as declared in ROUTE POLICY
       (the family form), the command as sent on the WIRE (the dotted form), the route, the
       correlation, and a reply row reading as a shell decline with the unreachable reason and a
       sentence making clear the approval itself stands.
    4. Judge the copy: does a skeptical reader come away understanding that Crosswake did nothing
       rather than faking a haptic tap, and that this is correct behavior — not a failure?
    5. Toggle the OS between light and dark mode with the page open. Confirm the panel inherits both
       correctly and that the reply row's text remains readable in each. Nothing here should be
       interactive.
    6. With a screen reader or the browser's accessibility inspector, confirm the reply row announces
       as a polite live region when it appears, and announces atomically rather than word by word.
    7. Run `mix crosswake.doctor` at the repo root. Confirm the new capability-rebuild guidance and
       the legacy-capability-id advisory read clearly and actionably, and that neither changes
       doctor's exit status.
    8. Open the failure message for the catalog guard by running
       `mix test test/crosswake/proof/phase154_catalog_guard_test.exs` against a deliberately broken
       fixture, or read the message text in `lib/crosswake/bridge/catalog_guard.ex`. Confirm the
       six-step recipe for adding the next control is present and genuinely followable — a gate with
       no documented path to yes gets deleted.
  </how-to-verify>
  <resume-signal>Type "approved" to close the phase, or describe what reads wrong in the panel copy, the labels, the theming, the live region, the doctor output, or the guard's failure message.</resume-signal>
</task>

</tasks>

## Artifacts this phase produces

See `154-01-PLAN.md` → "Artifacts this phase produces" for the full phase-wide list. This plan
creates no new symbols; it updates `guides/compatibility.md`, `guides/adopter_profiles.md`,
`guides/web_to_mobile_migration.md`, `guides/bridge.md`, and `guides/support_matrix.md`.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| published guide → adopter implementation | A guide example is copied verbatim into adopter code; an unsafe or misleading example propagates at scale. |
| published claim → adopter risk assessment | An overclaimed structural guarantee causes an adopter to skip a control they would otherwise have added. |

## STRIDE Threat Register

ASVS L1; blocking threshold `high`. No new code surface is introduced by this plan.

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-154-34 | Spoofing (of guarantee strength) | published CTRL-04 and CTRL-05 claims | high | mitigate | Task 1 requires the weaker true form be written wherever a claim has both, and the acceptance criteria mechanically require the structural-versus-CI-caught distinction to appear in the guides — not merely in the guard's moduledoc where no adopter reads it (D-45, D-52). |
| T-154-35 | Tampering | guide examples teaching an availability pre-check | high | mitigate | Task 1's acceptance criteria negative-check all three adopter-facing guides for pre-check predicates, so a copied example cannot reintroduce the three-way branch CTRL-02 collapses (D-09). |
| T-154-36 | Repudiation | undocumented reconnect semantics | medium | mitigate | The reconnect behavior is stated plainly with its recovery path, so an adopter cannot assume durability and build a UI that silently loses an answer (D-24). |
| T-154-37 | Information Disclosure | showcase evidence panel content | low | accept | The panel is capped at four semantic fields plus a verdict, all of which the adopter's own page already renders; the human checkpoint additionally reviews it visually. |
| T-154-SC | Tampering | npm/pip/cargo installs | low | accept | Documentation-only plan; zero package-manager operations, and zero new external packages this phase per RESEARCH.md's Package Legitimacy Audit. |
</threat_model>

<verification>
- `mix test --exclude requires_example_host --exclude advisory_only` exits 0.
- `cd examples/phoenix_host && npx playwright test route_tour.spec.ts` exits 0.
- `mix docs` exits 0.
- The human checkpoint above is approved.
</verification>

<success_criteria>
- Five guides state the true form of every Phase 154 claim, with the structural-versus-CI-caught
  distinction written where adopters read it.
- No guide example teaches an availability pre-check, and the fire-and-forget case is shown needing
  no reply clause.
- Reconnect semantics and the recovery path are documented plainly.
- A human has confirmed the desktop-browser evidence panel reads as the fail-closed thesis in both
  themes, announces politely, and that the doctor output and the guard's failure message are
  actionable.
- Both the core hermetic suite and the Playwright route tour are green — the phase's final gate.
</success_criteria>

<output>
Create `.planning/phases/154-the-control-contract-seam/154-08-SUMMARY.md` when done.
</output>
