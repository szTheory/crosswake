---
phase: 48-strategic-signal-and-milestone-memory
plan: "03"
type: execute
wave: 2
depends_on:
  - 48-01
  - 48-02
files_modified:
  - test/crosswake/planning/milestone_arc_closeout_parity_test.exs
autonomous: true
requirements:
  - STRAT-01
  - STRAT-02
must_haves:
  truths:
    - "Strategic-arc drift and malformed closeout exceptions fail fast in deterministic tests instead of surfacing only at milestone close."
    - "The v3.6 arc and closeout contract stay machine-checkable without a heavyweight schema framework."
    - "Phase 53 has an exact merge-blocking enforcement target anchored in source artifacts that tests can already inspect."
  artifacts:
    - path: test/crosswake/planning/milestone_arc_closeout_parity_test.exs
      provides: "ExUnit parity guards for Phase 48 strategic-memory and closeout contracts"
      contains: "defmodule Crosswake.Planning.MilestoneArcCloseoutParityTest"
  key_links:
    - from: test/crosswake/planning/milestone_arc_closeout_parity_test.exs
      to: .planning/MILESTONE-ARC.md
      via: "queue field and milestone presence assertions"
      pattern: "MILESTONE-ARC"
    - from: test/crosswake/planning/milestone_arc_closeout_parity_test.exs
      to: .planning/milestones/v3.6-CLOSEOUT.md
      via: "closeout ledger and exception-shape assertions"
      pattern: "v3\\.6-CLOSEOUT"
    - from: test/crosswake/planning/milestone_arc_closeout_parity_test.exs
      to: test/crosswake/planning/summary_frontmatter_test.exs
      via: "fail-loud planning parity helper pattern"
      pattern: "parse_frontmatter|Regex"
---

<objective>
Add lightweight deterministic parity guards for the strategic arc and closeout contract so Phase 48 ships more than prose and Phase 53 has an exact merge-blocking target to promote later.

Purpose: satisfy D-05 now with simple ExUnit checks, keep STRAT-01 and STRAT-02 mechanically durable, and avoid inventing a new schema framework for planning artifacts.
Output: `test/crosswake/planning/milestone_arc_closeout_parity_test.exs`.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/48-strategic-signal-and-milestone-memory/48-CONTEXT.md
@.planning/phases/48-strategic-signal-and-milestone-memory/48-RESEARCH.md
@.planning/phases/48-strategic-signal-and-milestone-memory/48-PATTERNS.md
@.planning/phases/48-strategic-signal-and-milestone-memory/48-VALIDATION.md
@.planning/MILESTONE-ARC.md
@.planning/milestones/v3.6-CLOSEOUT.md
@.planning/milestones/v3.5-MILESTONE-AUDIT.md
@test/crosswake/planning/summary_frontmatter_test.exs

<interfaces>
From `test/crosswake/planning/summary_frontmatter_test.exs`:
`parse_frontmatter/1` uses a fail-loud regex match for the first YAML block.
`scan_ids/1` extracts canonical IDs with `~r/\\b([A-Z]+-\\d+)\\b/`.
Existing parity tests prefer plain ExUnit plus `Regex` and explicit assertion messages over external schema tooling.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add strategic-arc contract parity assertions</name>
  <files>test/crosswake/planning/milestone_arc_closeout_parity_test.exs</files>
  <read_first>
test/crosswake/planning/milestone_arc_closeout_parity_test.exs
test/crosswake/planning/summary_frontmatter_test.exs
.planning/MILESTONE-ARC.md
.planning/PROJECT.md
.planning/phases/48-strategic-signal-and-milestone-memory/48-CONTEXT.md
.planning/phases/48-strategic-signal-and-milestone-memory/48-RESEARCH.md
.planning/phases/48-strategic-signal-and-milestone-memory/48-PATTERNS.md
  </read_first>
  <acceptance_criteria>
- The new test file asserts the shipped milestone list is complete through `v3.5`, `v3.6` is active, and each queued milestone in `.planning/MILESTONE-ARC.md` contains `Objective`, `Why now`, `Depends on`, `Risk tags`, `Key outputs`, `Non-goals`, and `Proof required` per D-02.
- The test also asserts `.planning/PROJECT.md` points readers back to `MILESTONE-ARC.md` for queue truth per D-03 and that the future queue names remain limited to the approved v3.7/v3.8/v3.9/v4.0/v4.1/Threadline set from D-14 through D-18.
- Parser/assertion logic fails loudly on malformed strategic-queue sections instead of silently passing on missing headings, mirroring the `summary_frontmatter` pattern.
  </acceptance_criteria>
  <action>Create `test/crosswake/planning/milestone_arc_closeout_parity_test.exs` using the same fail-loud parsing posture as `summary_frontmatter_test.exs`. Add deterministic STRAT-01 tests that read `.planning/MILESTONE-ARC.md` and assert the shipped milestone list is complete through `v3.5`, `v3.6` is active, and the queued milestones are exactly the approved v3.7/v3.8/v3.9/v4.0/v4.1/Threadline set from D-14 through D-18. For each queued milestone section, assert presence of the D-02 field headings `Objective`, `Why now`, `Depends on`, `Risk tags`, `Key outputs`, `Non-goals`, and `Proof required`. Add a source assertion against `.planning/PROJECT.md` so the project-level queue summary references `.planning/MILESTONE-ARC.md` per D-03. Fail loudly when a queue section exists without the required headings rather than returning a false green.</action>
  <verify>
    <automated>mix test test/crosswake/planning/milestone_arc_closeout_parity_test.exs -x</automated>
  </verify>
  <done>The new planning parity test fails whenever the strategic arc or project-level queue reference drifts away from the locked Phase 48 contract.</done>
</task>

<task type="auto">
  <name>Task 2: Add closeout-ledger and enforcement-target parity assertions</name>
  <files>test/crosswake/planning/milestone_arc_closeout_parity_test.exs</files>
  <read_first>
test/crosswake/planning/milestone_arc_closeout_parity_test.exs
test/crosswake/planning/summary_frontmatter_test.exs
.planning/milestones/v3.6-CLOSEOUT.md
.planning/MILESTONE-ARC.md
.planning/phases/48-strategic-signal-and-milestone-memory/48-CONTEXT.md
.planning/phases/48-strategic-signal-and-milestone-memory/48-RESEARCH.md
.planning/milestones/v3.5-MILESTONE-AUDIT.md
  </read_first>
  <acceptance_criteria>
- The test file asserts `.planning/milestones/v3.6-CLOSEOUT.md` contains the D-08 ledger keys, D-09 `deferred_with_reason` exception shape, and checklist coverage for roadmap/requirements/state/verification/validation/thread-seed/release continuity.
- The test file asserts an explicit `Phase 53 enforcement target` section mentions `closeout.verify` and the D-05/D-11 fail conditions instead of vague future cleanup.
- `mix test test/crosswake/planning/milestone_arc_closeout_parity_test.exs -x` passes alongside `mix test test/crosswake/planning/summary_frontmatter_test.exs -x` with no new dependencies.
  </acceptance_criteria>
  <action>Extend `test/crosswake/planning/milestone_arc_closeout_parity_test.exs` for STRAT-02 by reading `.planning/milestones/v3.6-CLOSEOUT.md` and asserting the D-08 ledger contract keys, the D-09 `deferred_with_reason` shape, and the checklist references to roadmap parity, requirements state, phase verification coverage, SUMMARY frontmatter coverage, validation ledger status, thread/seed disposition, release/changelog continuity, public support-claim changes, deferred items, and explicit exceptions. Add a dedicated assertion for the exact `Phase 53 enforcement target` section so D-05 and D-11 cannot regress into vague prose; the test should fail if `closeout.verify` or its required failure conditions disappear. Keep the test dependency-free ExUnit plus `Regex` parsing only, mirroring the existing `summary_frontmatter_test.exs` helpers instead of introducing YAML libraries or schema tooling.</action>
  <verify>
    <automated>mix test test/crosswake/planning/milestone_arc_closeout_parity_test.exs -x &amp;&amp; mix test test/crosswake/planning/summary_frontmatter_test.exs -x</automated>
  </verify>
  <done>The Phase 48 planning artifacts are guarded by deterministic tests that catch closeout-ledger drift and vague Phase 53 enforcement text before milestone close.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| planning tests -> strategic/closeout artifacts | If the tests are too weak, stale arc fields and malformed exceptions will survive until milestone close. |
| current parity checks -> future merge-blocking closeout gate | Phase 53 needs an exact promotion path from lightweight parity checks to `closeout.verify`. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-48-07 | Tampering | `test/crosswake/planning/milestone_arc_closeout_parity_test.exs` | mitigate | Use fail-loud ExUnit assertions for the D-02 strategic-queue fields, approved milestone set, and project-level canonical reference so arc drift cannot silently pass. |
| T-48-08 | Repudiation | closeout exception and evidence coverage | mitigate | Assert D-08 ledger keys and D-09 `deferred_with_reason` fields directly from `v3.6-CLOSEOUT.md` so missing evidence is treated as a test failure. |
| T-48-09 | Tampering | Phase 53 enforcement handoff | mitigate | Require explicit `Phase 53 enforcement target` and `closeout.verify` wording in the closeout artifact so the merge-blocking gate cannot later be reinterpreted as vague prose. |
| T-48-SC | Tampering | npm/pip/cargo installs | accept | No package-manager installs or new dependencies are permitted in this test-only plan. |
</threat_model>

<verification>
- `mix test test/crosswake/planning/milestone_arc_closeout_parity_test.exs -x`
- `mix test test/crosswake/planning/summary_frontmatter_test.exs -x`
- `gsd-sdk query roadmap.analyze`
</verification>

<success_criteria>
- A new ExUnit planning parity test guards both the strategic arc and the v3.6 closeout contract.
- The test fails loudly on missing D-02 queue fields, missing D-08 ledger keys, malformed `deferred_with_reason` exceptions, or absent Phase 53 enforcement text.
- Phase 48 ships a real lightweight parity check now while leaving an exact `closeout.verify` promotion target for Phase 53.
</success_criteria>

<output>
Create `.planning/phases/48-strategic-signal-and-milestone-memory/48-03-SUMMARY.md` when done
</output>
