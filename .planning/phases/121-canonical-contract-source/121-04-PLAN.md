---
phase: 121-canonical-contract-source
plan: 04
type: execute
wave: 1
depends_on: ["121-01", "121-02", "121-03"]
gap_closure: true
autonomous: true
requirements: [CANON-02, CANON-04, CANON-05]
files_modified:
  - examples/ios_shell_host/Fixtures/crosswake_manifest.json
  - examples/android_shell_host/app/src/main/assets/crosswake_manifest.json
  - lib/mix/tasks/crosswake.contract.gen.ex
  - .planning/REQUIREMENTS.md

must_haves:
  truths:
    - "The SC-2 grep, scoped to git-tracked files and excluding the deferred phase52 proof fixture, returns a single bridge_protocol_version value (1.1.0) across lib/ test/ packages/ examples/ (CANON-02, SC-2)"
    - "Both committed crosswake_manifest.json files carry compatibility.bridge_protocol_version = 1.1.0, matching their sibling route_activation.json and the canonical Crosswake.Bridge.Contract.version() (CANON-02, CANON-04)"
    - "Each example app's asset bundle is internally consistent: manifest bridge axis == activation bridge axis == 1.1.0 (CANON-04)"
    - "native_runtime_version and manifest_schema_version in both manifests remain 1.0.0 — only the bridge axis moves (D-03)"
    - "The crosswake.contract.gen.ex key-ordering comment no longer asserts a false unconditional Map.new ordering guarantee (WR-01)"
    - "REQUIREMENTS.md shows CANON-05 checked and its traceability row Complete, matching the verified codebase state (commit a05bb62)"
  artifacts:
    - path: "examples/ios_shell_host/Fixtures/crosswake_manifest.json"
      provides: "iOS example-host manifest with compatibility.bridge_protocol_version aligned to 1.1.0"
      contains: "1.1.0"
    - path: "examples/android_shell_host/app/src/main/assets/crosswake_manifest.json"
      provides: "Android example-host manifest with compatibility.bridge_protocol_version aligned to 1.1.0"
      contains: "1.1.0"
    - path: "lib/mix/tasks/crosswake.contract.gen.ex"
      provides: "Gen task with corrected, accurate key-ordering comment (<32-key dependency stated explicitly)"
      exports: ["run/1"]
      min_lines: 40
    - path: ".planning/REQUIREMENTS.md"
      provides: "Tracking sync — CANON-05 checked and traceability row Complete"
      contains: "CANON-05"
  key_links:
    - from: "examples/ios_shell_host/Fixtures/crosswake_manifest.json"
      to: "examples/ios_shell_host/Fixtures/route_activation.json"
      via: "both declare bridge_protocol_version 1.1.0 — within-bundle manifest/activation agreement"
      pattern: "\"bridge_protocol_version\": \"1.1.0\""
    - from: "examples/android_shell_host/app/src/main/assets/crosswake_manifest.json"
      to: "examples/android_shell_host/app/src/main/assets/route_activation.json"
      via: "both declare bridge_protocol_version 1.1.0 — within-bundle manifest/activation agreement"
      pattern: "\"bridge_protocol_version\": \"1.1.0\""
  prohibitions:
    - "MUST NOT touch the existing 121-01/02/03 PLAN.md or any SUMMARY.md — those plans are COMPLETE."
    - "MUST NOT change manifest_schema_version or native_runtime_version in either manifest — they stay 1.0.0 (D-03)."
    - "MUST NOT add crosswake_manifest.json to mix crosswake.contract.gen's emitted set — these files are owned by the gen.shell scaffold path, not contract.gen; folding them in would split ownership of one file across two generators. Drift-coverage of crosswake_manifest.json is a Phase 122 generate-and-diff guard-scoping concern (see handoff note below)."
    - "MUST NOT regenerate the manifests via `mix crosswake.gen.shell` — that task writes to a --target scaffold dir (native/ios/crosswake_shell) using scaffold-once ensure_file/2 (it returns :reused on existing files). It cannot and will not overwrite the committed examples/ manifests. The fix is an in-place targeted edit of the bridge axis only."
    - "MUST NOT plan or modify test/fixtures/proof/phase52_operator_inspection.json — it is a deferred, known-stale proof snapshot (deferred-items.md), failing for unrelated phase52 reasons. It is explicitly excluded from SC-2 acceptance."
    - "MUST NOT depend on or modify examples/*/build/ copies — those are gitignored build artifacts (confirmed via git check-ignore), not tracked source, and are out of SC-2 scope on tracked files."
---

<objective>
Close the single Phase 121 BLOCKER (SC-2 / CR-01): the two committed `crosswake_manifest.json` example fixtures carry `compatibility.bridge_protocol_version: "1.0.0"` while their sibling `route_activation.json` files (regenerated by `mix crosswake.contract.gen`) carry `"1.1.0"`. This within-bundle drift is exactly what Phase 121 exists to eliminate. Align both manifests' bridge axis to `1.1.0` (the canonical value from `Crosswake.Bridge.Contract.version()`), leaving the schema and native-runtime axes at `1.0.0` per D-03.

Also resolve the two WARNINGS: correct the misleading `Map.new/1` key-ordering comment in `crosswake.contract.gen.ex` (WR-01), and sync REQUIREMENTS.md so CANON-05 reflects its verified-complete state.

Purpose: Make SC-2 pass — `grep -rn '"bridge_protocol_version"'` returns a single value (1.1.0) across tracked lib/ test/ packages/ examples/, with the deferred phase52 proof fixture explicitly excluded. This fully satisfies CANON-02 (was PARTIALLY SATISFIED) and removes the example-app internal inconsistency noted under CANON-04.

Output: Two manifests with bridge axis = 1.1.0, an accurate gen-task comment, and a synced REQUIREMENTS.md tracking state.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/121-canonical-contract-source/121-CONTEXT.md
@.planning/phases/121-canonical-contract-source/121-VERIFICATION.md
@lib/crosswake/bridge/contract.ex
@lib/crosswake/shell/fixtures.ex
@lib/mix/tasks/crosswake.gen.shell.ex
</context>

<critical_finding>
The orchestrator's first-pass recommendation ("re-run `mix crosswake.gen.shell` to regenerate the committed manifests") was traced against the code and found NOT VIABLE. The chain is:

- `mix crosswake.gen.shell ios|android --target PATH` writes fixtures into a scaffold root (`native/ios/crosswake_shell` / `native/android/crosswake_shell`), NOT into the committed `examples/ios_shell_host/Fixtures/` or `examples/android_shell_host/app/src/main/assets/` paths (`crosswake.gen.shell.ex` lines 82-83, 95, 103-104, 117).
- `gen.shell` writes every fixture through `ensure_file/2` (line 261), which is scaffold-once: if the file already exists it returns `:reused` and does NOT overwrite (lines 264-266). So even pointed at an existing directory it would never update a committed manifest.

Therefore the committed `examples/` `crosswake_manifest.json` files are NOT live generator output into `examples/` — they are hand-maintained tracked fixtures whose bridge literal was never updated when Plan 01 moved `Manifest.Types.@bridge_protocol_version` to `Contract.version()`. Confirmed: neither manifest carries a `_generated_by` marker, and the two files are byte-identical.

The correct, minimal fix is a TARGETED in-place edit of the one drifted field (`compatibility.bridge_protocol_version` 1.0.0 → 1.1.0) in both files. This respects gen.shell's existing ownership boundary (do NOT fold manifest emission into contract.gen — see prohibitions and the Phase 122 handoff note) and changes only the field that is actually wrong.
</critical_finding>

<artifacts_this_phase_produces>
This is a gap-closure plan; it modifies existing tracked files (no net-new code files):

1. `examples/ios_shell_host/Fixtures/crosswake_manifest.json` — `compatibility.bridge_protocol_version` edited 1.0.0 → 1.1.0 (line 350). Schema + native-runtime axes unchanged (lines 351-352).
2. `examples/android_shell_host/app/src/main/assets/crosswake_manifest.json` — same edit (line 350). The two files remain byte-identical after the edit.
3. `lib/mix/tasks/crosswake.contract.gen.ex` — key-ordering comment block (lines ~174-189) reworded to state the <32-key dependency explicitly (no behavior/logic change).
4. `.planning/REQUIREMENTS.md` — CANON-05 checkbox `[ ]` → `[x]`; traceability row `CANON-05 | Phase 121 | Pending` → `Complete`.

No regenerated outputs from a generator are committed by this plan (the manifests are hand-maintained fixtures edited in place; `mix crosswake.contract.gen`'s four owned surfaces are already correct and untouched).
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Align both committed crosswake_manifest.json bridge axis to 1.1.0 (closes SC-2 BLOCKER / CANON-02)</name>
  <files>examples/ios_shell_host/Fixtures/crosswake_manifest.json, examples/android_shell_host/app/src/main/assets/crosswake_manifest.json</files>
  <read_first>
    - examples/ios_shell_host/Fixtures/crosswake_manifest.json (the drifted file; bridge axis at line 350; the `compatibility` block is lines 349-362)
    - examples/android_shell_host/app/src/main/assets/crosswake_manifest.json (byte-identical sibling; same line 350)
    - examples/ios_shell_host/Fixtures/route_activation.json (sibling already at 1.1.0 — the target value to match)
    - lib/crosswake/bridge/contract.ex (the canonical authority; `@version "1.1.0"` line 10, `version/0`)
    - lib/crosswake/shell/fixtures.ex (manifest_json/0 line 110 → manifest/0 line 43 → Types.new_compatibility() line 49 — confirms the canonical compatibility block already derives bridge = Contract.version() = 1.1.0; the committed files are simply stale)
  </read_first>
  <action>
    In BOTH manifest files, edit the single field `compatibility.bridge_protocol_version` from `"1.0.0"` to `"1.1.0"` (line 350 in each). This is the ONLY value change.

    Do NOT change `manifest_schema_version` (line 351, stays "1.0.0") or `native_runtime_version` (line 352, stays "1.0.0") — per D-03 only the bridge axis moves; the schema and native-runtime axes did not change.

    The two files are byte-identical before the edit; apply the identical edit to both so they remain byte-identical after. Do NOT reformat, re-sort, or otherwise touch any other line in either file. Do NOT add a `_generated_by` marker — these remain hand-maintained tracked fixtures (do NOT bring them under `mix crosswake.contract.gen`; see prohibitions and the Phase 122 handoff note).

    The target value 1.1.0 is the canonical `Crosswake.Bridge.Contract.version()` and matches the sibling `route_activation.json` files already at 1.1.0 — restoring within-bundle consistency.
  </action>
  <verify>
    <automated>test "$(grep -c '"bridge_protocol_version": "1.0.0"' examples/ios_shell_host/Fixtures/crosswake_manifest.json examples/android_shell_host/app/src/main/assets/crosswake_manifest.json | grep -v ':0$' | wc -l | tr -d ' ')" = "0" && grep -q '"bridge_protocol_version": "1.1.0"' examples/ios_shell_host/Fixtures/crosswake_manifest.json && grep -q '"bridge_protocol_version": "1.1.0"' examples/android_shell_host/app/src/main/assets/crosswake_manifest.json && grep -q '"native_runtime_version": "1.0.0"' examples/ios_shell_host/Fixtures/crosswake_manifest.json && diff examples/ios_shell_host/Fixtures/crosswake_manifest.json examples/android_shell_host/app/src/main/assets/crosswake_manifest.json && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - `grep '"bridge_protocol_version"' examples/ios_shell_host/Fixtures/crosswake_manifest.json` returns `"1.1.0"` (was `"1.0.0"`).
    - Same for the Android manifest.
    - `grep '"native_runtime_version"'` and `grep '"manifest_schema_version"'` in both manifests still return `"1.0.0"` (unchanged).
    - `diff` of the two manifest files returns empty (still byte-identical).
    - SC-2 grep scoped to tracked files excluding the deferred fixture returns ONLY 1.1.0: `git ls-files -z lib test packages examples | xargs -0 grep -h '"bridge_protocol_version"' 2>/dev/null | grep -o '"1\.[0-9]\.[0-9]"' | sort -u` — and after excluding the known phase52 proof fixture, the only version literal present in JSON value position is `"1.1.0"`. (The phase52 proof fixture `test/fixtures/proof/phase52_operator_inspection.json:1828` is the single explicitly-deferred exception; it carries "1.0.0" for unrelated phase52 reasons and is left as-is per deferred-items.md.)
    - No other line in either manifest changed (git diff shows exactly one changed line per file).
  </acceptance_criteria>
  <done>SC-2's example-manifest drift is eliminated: both committed manifests declare bridge_protocol_version 1.1.0, matching their sibling route_activation.json and the canonical Contract.version(). CANON-02 moves from PARTIALLY SATISFIED to SATISFIED; the CANON-04 example-app internal inconsistency is resolved.</done>
</task>

<task type="auto">
  <name>Task 2: Correct the misleading Map.new key-ordering comment in contract.gen.ex (WR-01)</name>
  <files>lib/mix/tasks/crosswake.contract.gen.ex</files>
  <read_first>
    - lib/mix/tasks/crosswake.contract.gen.ex (the comment block at lines ~174-189 above `pairs_to_map/1`; the `encode_doc/1` and `pairs_to_map/1` functions lines 170-194)
    - lib/crosswake/shell/fixtures.ex (json!/1 line 118 — sibling JSON-encoding helper for context on existing repo encoding conventions)
  </read_first>
  <action>
    Rewrite the comment block above `pairs_to_map/1` (currently lines ~174-189) so it no longer asserts that `Map.new/1` on a sorted keyword list unconditionally preserves sort order for JSON encoding. That claim is false for BEAM maps with more than 32 keys (small-map literal ordering is an implementation detail that does not hold once a map promotes to a hash-array-mapped trie).

    State accurately: idempotency of the emitted JSON relies on every generated object having FEWER THAN 32 keys, where BEAM small-map key ordering happens to be stable; this is a documented dependency, not a general guarantee. Note that if any generated object grows to 32+ keys, key order becomes non-deterministic and `write_if_changed` would churn — at which point encoding must move to an explicitly-ordered structure (e.g. encoding the sorted pairs list directly rather than via `Map.new/1`).

    Keep this change comment-only and low-risk: do NOT alter `pairs_to_map/1`, `encode_doc/1`, `convert_value/1`, `pairs_list?/1`, or any encoding logic — the verified idempotency must not be destabilized. Per comment-text discipline, do NOT introduce any literal string in the comment that a negative grep elsewhere keys on; this is prose describing the <32-key dependency, not code.
  </action>
  <verify>
    <automated>mix compile --warnings-as-errors 2>&1 | tail -5; grep -n 'iterates it in' lib/mix/tasks/crosswake.contract.gen.ex; mix crosswake.contract.gen && git diff --exit-code -- examples/ios_shell_host/Fixtures/route_activation.json examples/android_shell_host/app/src/main/assets/route_activation.json test/fixtures/bridge_contract_vectors.json docs/_contract_snippet.md && echo IDEMPOTENT</automated>
  </verify>
  <acceptance_criteria>
    - The comment no longer claims `Map.new/1` preserves sort order on OTP 26+ as a general guarantee; it explicitly states the <32-key dependency.
    - `grep -c 'iterates it in that order on OTP 26+' lib/mix/tasks/crosswake.contract.gen.ex` returns 0 (the false claim is gone).
    - `mix compile --warnings-as-errors` succeeds (no new warnings introduced).
    - The four gen-task surfaces remain idempotent: `mix crosswake.contract.gen && git diff --exit-code` on the four generated files is clean (encoding logic unchanged).
    - No change to any function body in the file (git diff shows comment lines only).
  </acceptance_criteria>
  <done>The WR-01 warning is resolved: the key-ordering comment accurately documents the <32-key dependency instead of asserting a false unconditional guarantee, with no change to the verified-idempotent encoding behavior.</done>
</task>

<task type="auto">
  <name>Task 3: Sync REQUIREMENTS.md CANON-05 tracking to verified-complete state</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md (CANON-05 checkbox line 20; traceability row `CANON-05 | Phase 121 | Pending` at line 82; note CANON-02 line 17 is already `[x]` and its traceability row line 79 is already `Complete` — leave those as-is)
    - .planning/phases/121-canonical-contract-source/121-VERIFICATION.md (SC-5 VERIFIED, commit a05bb62 — the evidence CANON-05 is complete)
  </read_first>
  <action>
    In `.planning/REQUIREMENTS.md`, change the CANON-05 checkbox from `- [ ] **CANON-05**` to `- [x] **CANON-05**` (line 20). The implementation is verified complete (SC-5 VERIFIED, `?: "1.0.0"` removed, fail-closed `error()` present, commit a05bb62).

    In the traceability table, change the CANON-05 row status from `Pending` to `Complete` (line 82): `| CANON-05 | Phase 121 | Complete |`.

    Do NOT change any other requirement's checkbox or status (CANON-01..04 and CANON-02's row are already Complete; GUARD-01 and later-phase rows stay Pending). This is a documentation-sync edit only — no code.
  </action>
  <verify>
    <automated>grep -q '^\- \[x\] \*\*CANON-05\*\*' .planning/REQUIREMENTS.md && grep -qE '\| CANON-05 \| Phase 121 \| Complete \|' .planning/REQUIREMENTS.md && ! grep -qE '\| CANON-05 \| Phase 121 \| Pending \|' .planning/REQUIREMENTS.md && echo PASS</automated>
  </verify>
  <acceptance_criteria>
    - `grep '\[x\] \*\*CANON-05\*\*' .planning/REQUIREMENTS.md` matches (checkbox checked).
    - The traceability table row for CANON-05 reads `Complete`, not `Pending`.
    - No other requirement checkbox or traceability status changed.
  </acceptance_criteria>
  <done>REQUIREMENTS.md tracking matches the verified codebase state: CANON-05 is checked and Complete, removing the documentation discrepancy noted in VERIFICATION.md.</done>
</task>

</tasks>

<phase_122_handoff>
**Drift-coverage of `crosswake_manifest.json` is a Phase 122 concern — do NOT fold it into Phase 121.**

This plan aligns the manifest bridge axis by an in-place edit, but it does NOT bring `crosswake_manifest.json` under any generate-and-diff guard. Reason: `crosswake_manifest.json` content is produced by `Crosswake.Shell.Fixtures` (`fixtures.ex` `export/1` → `manifest_json/0` → `manifest/0` → `Types.new_compatibility()`), which is wired into the `mix crosswake.gen.shell` scaffold task — NOT into `mix crosswake.contract.gen`. The two committed `examples/` manifests are hand-maintained snapshots of that fixture content.

Phase 122's generate-and-diff CI guard must therefore decide how to cover these manifests WITHOUT splitting ownership of a single file across two generators. The correct Phase 122 options are one of:
  (a) extend the drift guard to assert the committed `examples/` manifests' `compatibility.bridge_protocol_version` equals `Crosswake.Bridge.Contract.version()` (a parity assertion, not a regeneration), or
  (b) give `gen.shell` (or a thin manifest-refresh path) authority to re-emit the committed `examples/` manifests so the guard can diff them — keeping manifest ownership with the Fixtures/gen.shell lineage.

VERIFICATION.md's second "missing" item ("remove the crosswake_manifest.json MUST NOT prohibition from the gen task scope") is intentionally NOT actioned here, because adding manifest emission to `contract.gen` would make `contract.gen` and `gen.shell`/`Fixtures` both own the same file and fight each other. This handoff note surfaces the item explicitly rather than dropping it.
</phase_122_handoff>

<verification>
Phase-level checks after all three tasks:

1. SC-2 single-value (tracked files, deferred fixture excluded):
   `git ls-files -z lib test packages examples | xargs -0 grep -hn '"bridge_protocol_version"' 2>/dev/null | grep -o '"1\.[0-9]\.[0-9]"' | sort -u`
   → only `"1.1.0"` once the one phase52 proof fixture (deferred) is set aside. The phase52 proof fixture is the single explicitly-deferred exception.

2. Within-bundle consistency: for each example app, manifest bridge axis == activation bridge axis == 1.1.0.

3. Manifests byte-identical: `diff` of the two `crosswake_manifest.json` files is empty.

4. Non-bridge axes unchanged: `native_runtime_version` and `manifest_schema_version` still 1.0.0 in both manifests.

5. Gen task idempotent and compiling: `mix compile --warnings-as-errors` clean; `mix crosswake.contract.gen && git diff --exit-code` clean on the four generated surfaces.

6. REQUIREMENTS.md: CANON-05 checked and Complete.
</verification>

<success_criteria>
- SC-2 passes: the bridge_protocol_version literal across tracked lib/ test/ packages/ examples/ is a single value (1.1.0), with the deferred phase52 proof fixture explicitly excluded.
- Both committed crosswake_manifest.json files carry bridge_protocol_version 1.1.0 derived from (and matching) the canonical Crosswake.Bridge.Contract.version().
- native_runtime_version and manifest_schema_version remain 1.0.0 in both manifests (D-03).
- The contract.gen.ex key-ordering comment accurately states the <32-key dependency (WR-01 resolved); gen-task idempotency unchanged.
- REQUIREMENTS.md CANON-05 is checked and Complete.
- No existing 121-01/02/03 PLAN.md or SUMMARY.md modified.
</success_criteria>

<output>
Create `.planning/phases/121-canonical-contract-source/121-04-SUMMARY.md` when done.
</output>
