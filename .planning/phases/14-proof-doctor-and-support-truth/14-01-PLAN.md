---
phase: "14-proof-doctor-and-support-truth"
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - "lib/crosswake/doctor/doctor.ex"
  - "lib/crosswake/doctor/formatter.ex"
  - "test/crosswake/doctor/formatter_test.exs"
autonomous: true
requirements:
  - "SUPP-01"
must_haves:
  truths:
    - "Doctor explicitly renders package surfaces, capability families, and release boundaries."
    - "Prerequisites, denial behaviors, and fallback information for capabilities are visible in the CLI text output."
  artifacts:
    - path: "lib/crosswake/doctor/doctor.ex"
      provides: "Struct pass-through for support matrix data"
    - path: "lib/crosswake/doctor/formatter.ex"
      provides: "CLI string formatting for capabilities and boundaries"
  key_links:
    - from: "lib/crosswake/doctor/doctor.ex"
      to: "lib/crosswake/support_matrix/support_matrix.ex"
      via: "release_policy_snapshot"
      pattern: "support_matrix.capability_families"
---

<objective>
Extend doctor outputs for capability-family, package-boundary, and commerce-seam prerequisites and denials.

Purpose: Maintainers and adopters need visibility into the full typed contract truth (including prerequisites and denials for capabilities and commerce seams) directly from the `mix crosswake.doctor` command, not just the static markdown documentation.
Output: Updated `doctor.ex` exporting struct details and `formatter.ex` formatting them into CLI text sections.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/14-proof-doctor-and-support-truth/14-CONTEXT.md
@lib/crosswake/doctor/doctor.ex
@lib/crosswake/doctor/formatter.ex
@lib/crosswake/support_matrix/support_matrix.ex
</context>

<tasks>

<task type="auto">
  <name>Task 1: Expose full struct sets in release policy snapshot</name>
  <files>lib/crosswake/doctor/doctor.ex</files>
  <action>Update `release_policy_snapshot/1` in `lib/crosswake/doctor/doctor.ex` to pass the full structs for `capability_families`, `package_surfaces`, `release_boundaries`, and `change_classes` from the `support_matrix`, rather than mapping them to strings.</action>
  <verify>
    <automated>grep -q "capability_families: support_matrix.capability_families" lib/crosswake/doctor/doctor.ex</automated>
  </verify>
  <done>The snapshot returns the structs intact.</done>
</task>

<task type="auto">
  <name>Task 2: Format detailed capability and boundary sections in doctor CLI</name>
  <files>lib/crosswake/doctor/formatter.ex, test/crosswake/doctor/formatter_test.exs</files>
  <action>Update `format_release_policy/1` (or add new format functions) in `lib/crosswake/doctor/formatter.ex` to handle the structured data. Render text blocks for "capability families" (showing prerequisites, denial, and fallback), "package boundaries" (showing why and burden), and "change classes". Use string-formatting patterns similar to `Crosswake.SupportMatrix.Renderer` but suited for CLI text layout. If `test/crosswake/doctor/formatter_test.exs` does not exist, create it to verify the text rendering.</action>
  <verify>
    <automated>mix test test/crosswake/doctor/formatter_test.exs</automated>
  </verify>
  <done>Doctor outputs human-readable text blocks for prerequisites, denial, and fallback for capabilities.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI Output -> Operator | Provides accurate representation of system support boundaries without masking risks. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-01-1 | Information Disclosure | `formatter.ex` | accept | Doctor output only reads static manifest/support matrix definitions; no runtime secrets are exposed. |
| T-14-01-2 | Repudiation | `doctor.ex` | mitigate | Ensure output accurately reflects the truth of the system support matrix. |
</threat_model>

<verification>
1. Run `mix crosswake.doctor` and manually inspect output to ensure capabilities and their prerequisites are listed.
</verification>

<success_criteria>
Doctor explicitly renders package surfaces, capability families, and release boundaries including commerce seams.
</success_criteria>

<output>
After completion, create `.planning/phases/14-proof-doctor-and-support-truth/14-01-SUMMARY.md`
</output>