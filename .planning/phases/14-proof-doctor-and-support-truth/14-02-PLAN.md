---
phase: "14-proof-doctor-and-support-truth"
plan: "02"
type: execute
wave: 2
depends_on: ["14-01"]
files_modified:
  - "lib/crosswake/doctor/doctor.ex"
  - "lib/crosswake/doctor/formatter.ex"
  - "test/crosswake/doctor/doctor_test.exs"
autonomous: true
requirements:
  - "SUPP-02"
must_haves:
  truths:
    - "Doctor explicitly reports merge-blocking capability proof requirements separate from advisory proof requirements."
    - "Environment-sensitive capabilities do not cause CI doctor failures natively but issue advisory warnings."
  artifacts:
    - path: "lib/crosswake/doctor/doctor.ex"
      provides: "Check separation for merge-blocking vs advisory capabilities"
    - path: "lib/crosswake/doctor/formatter.ex"
      provides: "Formatting for new advisory capability checks"
  key_links:
    - from: "lib/crosswake/doctor/doctor.ex"
      to: "lib/crosswake/doctor/doctor.ex"
      via: "capability_proof_findings"
      pattern: "proof_class"
---

<objective>
Split merge-blocking proof from advisory environment-sensitive proof for future capability and commerce claims.

Purpose: Maintainers must be able to distinguish merge-blocking proof from advisory proof before widening public support claims. Adding real-device or storefront credentials to proofs should not break standard CI pipelines.
Output: Updated `doctor.ex` generating categorized findings for capability `proof_classes` and `formatter.ex` updated to handle the new check severities.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/14-proof-doctor-and-support-truth/14-CONTEXT.md
@lib/crosswake/doctor/doctor.ex
@lib/crosswake/doctor/formatter.ex
</context>

<tasks>

<task type="auto">
  <name>Task 1: Generate distinct capability findings based on proof class</name>
  <files>lib/crosswake/doctor/doctor.ex</files>
  <action>Add a function like `capability_posture_findings/1` that iterates over `support_matrix.capability_families`. Split them into `merge_blocking` and `advisory`. Generate a `check/6` with severity `:error` for missing/failing `merge_blocking` proofs (even if abstract for now) and a `check/6` with severity `:advisory` for capabilities classified as `advisory`. Append these findings to the overall `support_posture` findings.</action>
  <verify>
    <automated>grep -q "capability_posture_findings" lib/crosswake/doctor/doctor.ex</automated>
  </verify>
  <done>Findings cleanly separate `merge_blocking` capabilities from `advisory` capabilities.</done>
</task>

<task type="auto">
  <name>Task 2: Format advisory and merge-blocking capability findings</name>
  <files>lib/crosswake/doctor/formatter.ex, test/crosswake/doctor/doctor_test.exs</files>
  <action>Ensure the formatter cleanly renders the new capability proof posture checks. Verify that any new codes generated in Task 1 are properly formatted so users understand why an advisory check didn't fail the build. If `test/crosswake/doctor/doctor_test.exs` does not exist, create it to cover the new behavior.</action>
  <verify>
    <automated>mix test test/crosswake/doctor/doctor_test.exs</automated>
  </verify>
  <done>The CLI explicitly distinguishes between advisory and merge-blocking capability requirements in the output.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI Pipeline -> Doctor | Doctor must accurately return non-zero exit codes ONLY for merge-blocking failures. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-02-1 | Elevation of Privilege | `doctor.ex` | accept | Test execution environments don't gain privilege; they just change exit behavior. |
| T-14-02-2 | Tampering | `doctor.ex` | mitigate | Ensure explicit `proof_class` enum values prevent arbitrary bypassing of checks. |
</threat_model>

<verification>
1. Run `mix crosswake.doctor` and verify exit codes behavior reflects only merge-blocking checks.
</verification>

<success_criteria>
Maintainers can distinguish merge-blocking proof from advisory proof before widening public support claims.
</success_criteria>

<output>
After completion, create `.planning/phases/14-proof-doctor-and-support-truth/14-02-SUMMARY.md`
</output>