---
phase: 34-mockstorefront-and-idempotency-invariants
plan: 02
type: execute
wave: 2
depends_on: ["34-01"]
files_modified:
  - test/crosswake/proof/phase34_mock_storefront_test.exs
autonomous: true
requirements: [WIRE-03, MOCK-01, MOCK-02, MOCK-03]
must_haves:
  truths:
    - "Running mix test test/crosswake/proof/phase34_mock_storefront_test.exs passes with all cases green"
    - "The replay invariant holds: same entry_id with different correlation_id yields identical event_key and replay?: true via ReconciliationInbox.ingest_evidence/2 (WIRE-03, D-01..D-04)"
    - "The negative case holds: two different entry_id values yield distinct event_keys and replay?: false (WIRE-03 negative)"
    - "Restore evidence shares the same subject_key as a purchase of the canonical subscription product (D-06, restore re-grants prior purchase)"
    - "MockStorefront source contains none of the forbidden tokens storekit, play_billing, play billing, revenuecat (Success Criterion #5, WIRE-03 fence)"
    - "The test is hermetic and merge-blocking: Code.require_file at module scope, async: false, NO @moduletag :requires_example_host, so it runs under mix test --exclude requires_example_host"
  artifacts:
    - path: "test/crosswake/proof/phase34_mock_storefront_test.exs"
      provides: "Hermetic untagged proof test: struct-shape cases, replay/idempotency invariant, restore-shares-subject-key, provider-vocabulary fence"
      contains: "defmodule Crosswake.Proof.Phase34MockStorefrontTest"
      min_lines: 60
  key_links:
    - from: "test/crosswake/proof/phase34_mock_storefront_test.exs"
      to: "MockStorefront.simulate_purchase + ReconciliationInbox.ingest_evidence/2"
      via: "two ingest calls on same entry_id / different correlation_id, asserting replay?: true"
      pattern: "ingest_evidence"
    - from: "test/crosswake/proof/phase34_mock_storefront_test.exs"
      to: "examples/phoenix_host/lib/crosswake_example/commerce/mock_storefront.ex"
      via: "Code.require_file at module scope + File.read! source fence"
      pattern: "Code\\.require_file"
---

<objective>
Create `test/crosswake/proof/phase34_mock_storefront_test.exs` — the single hermetic, untagged, merge-blocking proof for Phase 34. It loads `reconciliation_keys.ex`, `reconciliation_inbox.ex`, and the new `mock_storefront.ex` via `Code.require_file` at module scope, then proves:

1. **Struct shape** (MOCK-01/02): `simulate_purchase/1` and `simulate_restore/1` return correctly-shaped `ReconciliationEvidence`.
2. **Swap-target doc** (MOCK-03): the `@moduledoc` (or source) names both functions.
3. **Replay invariant** (WIRE-03 positive): same `entry_id` + different `correlation_id` → identical `event_key` → `replay?: true` from `ingest_evidence/2`.
4. **Replay negative** (WIRE-03): different `entry_id` → distinct `event_key` → `replay?: false`.
5. **Restore shares identity** (D-06): restore `subject_key` == purchase `subject_key` for the canonical product.
6. **Vocabulary fence** (Success Criterion #5): `mock_storefront.ex` source contains none of `storekit`, `play_billing`, `play billing`, `revenuecat`.

Purpose: This is the merge-blocking artifact that makes the idempotency invariant and provider-neutrality boundary real. It proves the keying is on stable provider identity (via `ReconciliationKeys`), not transient `correlation_id`.

Output: One new test file. The test is UNtagged (no `@moduletag :requires_example_host`) so it runs in the `phase34-proof.yml` merge-blocking `mix test --exclude requires_example_host` lane.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/34-mockstorefront-and-idempotency-invariants/34-CONTEXT.md
@.planning/phases/34-mockstorefront-and-idempotency-invariants/34-RESEARCH.md
@.planning/phases/34-mockstorefront-and-idempotency-invariants/34-PATTERNS.md
@.planning/phases/34-mockstorefront-and-idempotency-invariants/34-VALIDATION.md

@examples/phoenix_host/lib/crosswake_example/commerce/mock_storefront.ex
</context>

<constraints>
HARD constraints — non-negotiable:
- This test MUST be hermetic and merge-blocking: `Code.require_file` at module scope (top of file, BEFORE `defmodule`, using `__DIR__` as the second arg), `use ExUnit.Case, async: false`, and NO `@moduletag :requires_example_host` (Pitfall 2 + 3). Tagging it `:requires_example_host` would exclude it from the merge-blocking lane and break WIRE-03's contract.
- The vocabulary-fence file read MUST use a bare project-root-relative string path passed to `File.read!` — `File.read!("examples/phoenix_host/lib/crosswake_example/commerce/mock_storefront.ex")` — NOT `Path.join(__DIR__, ...)` (Pitfall 5, verified project convention).
- Forbidden tokens MUST be written with `<>` concatenation in this test (`"store" <> "kit"`, `"play" <> "_billing"`, `"play" <> " " <> "billing"`, `"revenue" <> "cat"`) so the test's own source does not trip a fence (Pitfall 6, established convention from phase21/phase23).
- Identity assertions MUST prove keying is on `entry_id`/stable identity, NOT `correlation_id` (the entire WIRE-03 invariant).
- NO changes to `mock_storefront.ex` (created in Plan 01), `lib/crosswake/`, or any existing example-host commerce module. This plan creates exactly ONE file: the test.
- This plan depends on 34-01 (the module must exist for `Code.require_file` to load it and for the fence to read it).
</constraints>

<tasks>

<task type="auto">
  <name>Task 1: Replay invariant + struct-shape + swap-doc + restore-shares-subject-key cases</name>
  <files>test/crosswake/proof/phase34_mock_storefront_test.exs</files>
  <read_first>
    - `test/crosswake/proof/phase21_reconciliation_example_test.exs` (lines 1-16 for the `Code.require_file`-at-module-scope idiom and `use ExUnit.Case, async: false` + alias block — but note line 11 `@moduletag :requires_example_host` is the ONE line Phase 34 must NOT copy; lines 35-49 for the replay assertion shape `{:ok, first} = ingest_evidence(...)` then `ingest_evidence(..., seen_event_keys: [first.event_key])` asserting `replay?` and `event_key ==`; lines 52-73 for the correlation_id-variation + `subject_key` stability shape; lines 165-178 for the `struct!`/`sample_evidence` fixture helper idiom if needed)
    - `examples/phoenix_host/lib/crosswake_example/commerce/mock_storefront.ex` (full — the module under test; confirm exact `provider_reference`/`evidence_ref` string forms, the `@subscription_entry_id` value, and the `captured_at` opts seam so assertions match exactly)
    - `examples/phoenix_host/lib/crosswake_example/commerce/reconciliation_inbox.ex` (lines ~14-37 for `ingest_evidence/2` signature, the `seen_event_keys` membership mechanic, the `{:ok, map()}` return with keys `event_key`/`subject_key`/`status`/`replay?`/`trace_metadata`, and that `"purchase"`/`"restore"` map to `status: :awaiting_verification`)
    - `examples/phoenix_host/lib/crosswake_example/commerce/reconciliation_keys.ex` (lines ~14-43 to confirm `event_key/1` excludes `correlation_id` and `subject_key/2` is `subject::provider::provider_reference`, so purchase+restore on the same `@subscription_entry_id` share `subject_key`)
    - `test/crosswake/proof/phase33_commerce_corridor_routes_test.exs` (the untagged-hermetic test shape — confirms an untagged proof test is the correct model for the merge-blocking lane)
  </read_first>
  <action>
    Put the three `Code.require_file` calls at the very TOP of the file, BEFORE `defmodule`, each using `__DIR__` as the second argument and the relative path `"../../../examples/phoenix_host/lib/crosswake_example/commerce/<file>.ex"`: load `reconciliation_keys.ex`, then `reconciliation_inbox.ex`, then `mock_storefront.ex` (load keys and inbox before the mock, mirroring phase21 ordering).

    Open `defmodule Crosswake.Proof.Phase34MockStorefrontTest do` with `use ExUnit.Case, async: false`. Add a short comment noting the test is intentionally UNtagged (hermetic via Code.require_file + pure calls) so it lands in the merge-blocking lane — do NOT add `@moduletag :requires_example_host`. Add aliases: `Crosswake.Commerce.Contracts`, `CrosswakeExample.Commerce.MockStorefront`, `CrosswakeExample.Commerce.ReconciliationInbox`, `CrosswakeExample.Commerce.ReconciliationKeys`.

    Write these test cases (mirror phase21 assertion shapes):

    - "simulate_purchase returns storefront/mock purchase evidence" (MOCK-01): build `%Contracts.PurchaseIntent{entry_id: "sub_pro_monthly", correlation_id: "c1"}`, call `MockStorefront.simulate_purchase(intent)`, assert the result is a `%Contracts.ReconciliationEvidence{}` with `source: :storefront`, `provider: "mock"`, `event_kind: "purchase"`, and a non-nil `captured_at`. Assert `provider_reference` and `evidence_ref` are the deterministic entry_id-derived values defined in `mock_storefront.ex` (read the module to confirm exact strings).

    - "simulate_restore returns storefront/mock restore evidence" (MOCK-02): build `%Contracts.RestoreIntent{correlation_id: "anything"}`, call `MockStorefront.simulate_restore(intent)`, assert `source: :storefront`, `provider: "mock"`, `event_kind: "restore"`, and that `provider_reference` equals the canonical-product value (anchored on `@subscription_entry_id`, not on `correlation_id`).

    - "moduledoc names both swap-target functions" (MOCK-03): read the module source via `File.read!("examples/phoenix_host/lib/crosswake_example/commerce/mock_storefront.ex")` and assert it contains both `"simulate_purchase/1"` and `"simulate_restore/1"`. (Asserting against source text is sufficient and avoids depending on doc-compilation flags.)

    - "same entry_id, different correlation_id yields replay?: true" (WIRE-03 positive): build two PurchaseIntents with the SAME `entry_id: "sub_pro_monthly"` but `correlation_id: "c1"` vs `"c2"`. Call `simulate_purchase` on each. `assert {:ok, first} = ReconciliationInbox.ingest_evidence(evidence1, correlation_id: "c1")`. `assert {:ok, replay} = ReconciliationInbox.ingest_evidence(evidence2, correlation_id: "c2", seen_event_keys: [first.event_key])`. Assert `replay.replay? == true` and `replay.event_key == first.event_key`. This proves identity is keyed on `entry_id`, not `correlation_id`.

    - "different entry_id yields distinct event_key and replay?: false" (WIRE-03 negative): build PurchaseIntents with `entry_id: "entry_a"` and `entry_id: "entry_b"`. `simulate_purchase` each. `assert {:ok, a} = ingest_evidence(evidence_a)`. `assert {:ok, b} = ingest_evidence(evidence_b, seen_event_keys: [a.event_key])`. Assert `refute b.replay?` and `refute a.event_key == b.event_key`.

    - "restore shares subject_key with purchase of the canonical product" (D-06): `simulate_purchase(%PurchaseIntent{entry_id: "sub_pro_monthly", correlation_id: "c1"})` and `simulate_restore(%RestoreIntent{correlation_id: "c2"})`. `assert {:ok, p} = ingest_evidence(purchase_evidence)` and `assert {:ok, r} = ingest_evidence(restore_evidence)`. Assert `p.subject_key == r.subject_key` (both anchor on the same `provider_reference` derived from `@subscription_entry_id`).

    All `ingest_evidence` calls must be wrapped in `assert {:ok, _} = ...` matches (a `{:error, _}` would correctly blow up the test). Do NOT place full code blocks in this action — copy exact assertion syntax from the phase21 read_first analog.
  </action>
  <verify>
    <automated>cd /Users/jon/projects/crosswake && mix test test/crosswake/proof/phase34_mock_storefront_test.exs 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - `mix test test/crosswake/proof/phase34_mock_storefront_test.exs` passes with 0 failures.
    - Source assertion: the three `Code.require_file(...)` calls appear at module scope BEFORE `defmodule`, using `__DIR__` and the `"../../../examples/phoenix_host/..."` relative paths.
    - Source assertion: `use ExUnit.Case, async: false` is present and `@moduletag :requires_example_host` is ABSENT: `grep -c 'requires_example_host' test/crosswake/proof/phase34_mock_storefront_test.exs` returns 0.
    - Behavior: the replay-positive case asserts both `replay.replay? == true` AND `replay.event_key == first.event_key`, using two DIFFERENT correlation_ids on the SAME entry_id.
    - Behavior: the replay-negative case asserts `refute b.replay?` and distinct event_keys for two different entry_ids.
    - Behavior: the restore case asserts `p.subject_key == r.subject_key`.
    - Behavior: the MOCK-03 case asserts the source contains both `"simulate_purchase/1"` and `"simulate_restore/1"`.
  </acceptance_criteria>
  <done>
    The proof test file exists, is hermetic and untagged, and `mix test test/crosswake/proof/phase34_mock_storefront_test.exs` passes — proving struct shapes (MOCK-01/02), the swap-target moduledoc (MOCK-03), the replay invariant both positive and negative (WIRE-03), and restore-shares-subject-key (D-06).
  </done>
</task>

<task type="auto">
  <name>Task 2: Provider-vocabulary fence case + merge-blocking lane confirmation</name>
  <files>test/crosswake/proof/phase34_mock_storefront_test.exs</files>
  <read_first>
    - `test/crosswake/proof/phase21_reconciliation_example_test.exs` (lines 141-163 for the exact fence shape: forbidden-token list built with `<>` concatenation, `File.read!(file) |> String.downcase()`, `refute String.contains?(content, forbidden), "<msg>"`, with the file path as a bare project-root-relative string)
    - `test/crosswake/proof/phase23_commerce_support_proof_test.exs` (lines 33-38 for the `@forbidden_provider_tokens` module-attribute variant — either inline or module-attribute style is acceptable for a single-file fence)
    - `examples/phoenix_host/lib/crosswake_example/commerce/mock_storefront.ex` (full — confirm the `@moduledoc` prose contains none of the four forbidden tokens before asserting; if Plan 01's moduledoc accidentally wrote a banned token, this fence will catch it)
  </read_first>
  <action>
    Add one more test to the same module: "MockStorefront source contains no forbidden provider tokens" (Success Criterion #5, WIRE-03 fence). Build the forbidden-token list using `<>` concatenation exactly as in phase21/phase23: `["store" <> "kit", "play" <> "_billing", "play" <> " " <> "billing", "revenue" <> "cat"]`. Read the module source with the bare project-root-relative path `File.read!("examples/phoenix_host/lib/crosswake_example/commerce/mock_storefront.ex")` piped to `String.downcase()`. Iterate the tokens and `refute String.contains?(content, forbidden), "mock_storefront.ex leaked provider token #{forbidden}"`.

    Do NOT add a `Code.require_file` self-guard or a router-import guard — the test is already purely functional and legitimately uses `Code.require_file` for the example-host modules (RESEARCH Open Question 2: no self-guard needed for this hermetic functional test). Do NOT use `Path.join(__DIR__, ...)` for the fence read (Pitfall 5).

    After writing, confirm the whole file runs in the merge-blocking lane by running the full hermetic suite locally.
  </action>
  <verify>
    <automated>cd /Users/jon/projects/crosswake && mix test --exclude requires_example_host 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - The fence test passes: `mix test test/crosswake/proof/phase34_mock_storefront_test.exs` green including the new fence case.
    - The full merge-blocking lane is green: `mix test --exclude requires_example_host` passes with 0 failures, AND the Phase 34 test was actually executed in that run (it is untagged, so it must appear in the count — confirm by also running with `--only` is not needed; the untagged file runs by default).
    - Source assertion: forbidden tokens are written with `<>` concatenation, not as literals: `grep -E '"store" <> "kit"' test/crosswake/proof/phase34_mock_storefront_test.exs` matches.
    - Source assertion: the fence reads `File.read!("examples/phoenix_host/lib/crosswake_example/commerce/mock_storefront.ex")` with a bare string path (no `Path.join`, no `__DIR__` in the fence read).
    - Negative-confidence check: if `mock_storefront.ex` contained any forbidden token, this test would fail — confirmed by the test logic refuting `String.contains?` for each of the four tokens against the downcased source.
  </acceptance_criteria>
  <done>
    The vocabulary-fence case asserts `mock_storefront.ex` contains none of `storekit`/`play_billing`/`play billing`/`revenuecat` (case-insensitive), and `mix test --exclude requires_example_host` runs the full Phase 34 proof file green in the merge-blocking lane.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none at runtime) | This is a test file. It performs no network I/O and no persistence. It reads two source files from the repo working tree (`Code.require_file` to load modules, `File.read!` to fence-scan source) — both trusted, version-controlled, in-repo paths. No untrusted input crosses any boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-34-SC | Tampering | npm/pip/cargo installs | n/a | This phase installs ZERO external packages (RESEARCH Package Legitimacy Audit: not applicable). No supply-chain surface introduced by this plan. |
| T-34-04 | Information Disclosure | `mock_storefront.ex` source provider-token leakage | mitigate | THIS plan is the mitigation: the vocabulary-fence test (Task 2) refutes presence of the four forbidden provider tokens in the mock source. This is the formal enforcement of the boundary pre-checked in Plan 01. |
| T-34-05 | Tampering | merge-blocking lane bypass | mitigate | The test is untagged and `async: false`, so it runs under `mix test --exclude requires_example_host` (the merge-blocking `phase34-proof.yml` job). The acceptance criteria assert the absence of `@moduletag :requires_example_host`, preventing accidental exclusion from the gate (Pitfall 2). |

**No `high`-severity threats.** This is a hermetic, in-process test reading only trusted in-repo source. The phase's single meaningful security invariant — provider-neutrality of the mock source — is actively enforced by Task 2's fence. There is no auth, no persistence (AF-02), no network, and no authority mutation. The mock-boundary authority invariant (`authority_mutation_allowed_from_evidence?/1 → false`) is asserted in Phase 36, not here, and nothing in this plan weakens it.
</threat_model>

<verification>
- `mix test test/crosswake/proof/phase34_mock_storefront_test.exs` green (all cases).
- `mix test --exclude requires_example_host` green AND includes the Phase 34 file (untagged → runs in merge-blocking lane).
- `grep -c 'requires_example_host' test/crosswake/proof/phase34_mock_storefront_test.exs` returns 0 (test is untagged).
- No file under `lib/crosswake/`, `mock_storefront.ex`, or existing example-host commerce modules changed by this plan.
</verification>

<success_criteria>
- ROADMAP Success Criterion #4 (replay invariant, WIRE-03): same `provider_reference`/different `correlation_id` → `replay?: true`, proving idempotency keyed on stable provider identity via `ReconciliationKeys`. ✓
- ROADMAP Success Criterion #5 (vocabulary fence): `MockStorefront` source contains no `storekit`/`play_billing`/`play billing`/`revenuecat`. ✓
- MOCK-01/02/03 re-confirmed at the proof layer (struct-shape + swap-doc cases).
- Test is hermetic, untagged, `async: false`, merge-blocking — D-08 isolation discipline honored.
</success_criteria>

<output>
Create `.planning/phases/34-mockstorefront-and-idempotency-invariants/34-02-SUMMARY.md` when done.
</output>
