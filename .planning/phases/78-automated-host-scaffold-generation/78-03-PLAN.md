---
phase: 78
plan: 03
type: execute
wave: 2
depends_on: ["78-01"]
files_modified:
  - priv/templates/crosswake/shell/android/app/src/main/java/dev/crosswake/shell/CrosswakeViewModel.kt.eex
  - priv/templates/crosswake/shell/android/app/src/main/java/dev/crosswake/shell/MainActivity.kt.eex
  - priv/templates/crosswake/shell/android/app/src/main/AndroidManifest.xml.eex
  - priv/templates/crosswake/shell/android/app/build.gradle.eex
  - priv/templates/crosswake/shell/android/settings.gradle.eex
autonomous: true
requirements: [GEN-01, GEN-02, GEN-03]
must_haves:
  truths:
    - "Android generator produces a thin app driven by a remote or local Maven AAR dependency."
    - "Android MainActivity delegates state observation to CrosswakeViewModel."
    - "AndroidManifest contains no bloated capability comments."
  artifacts:
    - path: "priv/templates/crosswake/shell/android/app/src/main/java/dev/crosswake/shell/CrosswakeViewModel.kt.eex"
      provides: "Glue layer initializing SDK"
      contains: "CrosswakeShell.initialize"
    - path: "priv/templates/crosswake/shell/android/app/src/main/java/dev/crosswake/shell/MainActivity.kt.eex"
      provides: "Dumb root activity"
      contains: "viewModels<CrosswakeViewModel>()"
    - path: "priv/templates/crosswake/shell/android/app/build.gradle.eex"
      provides: "Maven AAR Dependency"
      contains: "<%= if @local do %>"
  key_links:
    - from: "priv/templates/crosswake/shell/android/app/src/main/java/dev/crosswake/shell/MainActivity.kt.eex"
      to: "priv/templates/crosswake/shell/android/app/src/main/java/dev/crosswake/shell/CrosswakeViewModel.kt.eex"
      via: "ViewModel instantiation"
      pattern: "CrosswakeViewModel"
---

<objective>
Overhaul Android EEx templates to output a thin scaffold that consumes `crosswake-shell-core-android` via Maven and uses a dedicated ViewModel glue layer.

Purpose: Implement D-01, D-03, D-05, D-06, and D-07 for Android native clients.
Output: Updated EEx templates for the Android project.
</objective>

<context>
@.planning/phases/78-automated-host-scaffold-generation/78-CONTEXT.md
@.planning/phases/78-automated-host-scaffold-generation/78-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create `CrosswakeViewModel.kt.eex`</name>
  <files>priv/templates/crosswake/shell/android/app/src/main/java/dev/crosswake/shell/CrosswakeViewModel.kt.eex</files>
  <action>
    Implement the glue layer template (D-06, D-07).
    - Must be a subclass of `ViewModel`.
    - Expose `val state: StateFlow<CrosswakeState>`.
    - `init` block calls `CrosswakeShell.initialize()` and collects its state updates into the `StateFlow`.
  </action>
  <verify>
    <automated>grep "CrosswakeShell.initialize" priv/templates/crosswake/shell/android/app/src/main/java/dev/crosswake/shell/CrosswakeViewModel.kt.eex</automated>
  </verify>
  <done>ViewModel template correctly initializes the shell and observes state.</done>
</task>

<task type="auto">
  <name>Task 2: Update `MainActivity.kt.eex` and `AndroidManifest.xml.eex`</name>
  <files>
    priv/templates/crosswake/shell/android/app/src/main/java/dev/crosswake/shell/MainActivity.kt.eex,
    priv/templates/crosswake/shell/android/app/src/main/AndroidManifest.xml.eex
  </files>
  <action>
    Update Activity (D-05) and Manifest (D-03, D-04):
    1. In `MainActivity.kt.eex`, remove legacy routing/channel handling. Retrieve `CrosswakeViewModel` via `viewModels()`. Observe its state to render simple UI status.
    2. In `AndroidManifest.xml.eex`, remove large commented permission blocks. Add a single standard comment (`<!-- For adding capabilities, see: https://docs.crosswake.dev/capabilities -->`) and only include essential boot permissions (internet).
  </action>
  <verify>
    <automated>grep "CrosswakeViewModel" priv/templates/crosswake/shell/android/app/src/main/java/dev/crosswake/shell/MainActivity.kt.eex</automated>
  </verify>
  <done>Activity is simplified and Manifest is minimal.</done>
</task>

<task type="auto">
  <name>Task 3: Inject Maven Dependency into Build Files</name>
  <files>
    priv/templates/crosswake/shell/android/app/build.gradle.eex,
    priv/templates/crosswake/shell/android/settings.gradle.eex
  </files>
  <action>
    Update Gradle templates (GEN-01).
    1. In `app/build.gradle.eex`, use `<%= if @local do %>` to conditionally inject `implementation project(':crosswake-shell-core-android')` instead of the remote maven artifact `implementation 'dev.crosswake:shell-core-android:X.Y.Z'`.
    2. In `settings.gradle.eex`, use `<%= if @local do %>` to conditionally include the local project and set its project directory to `../../../packages/crosswake-shell-core-android`.
  </action>
  <verify>
    <automated>grep "crosswake-shell-core-android" priv/templates/crosswake/shell/android/app/build.gradle.eex</automated>
  </verify>
  <done>Gradle build correctly references local or remote artifact based on flag.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Generated Code → OS | Minimal manifest guarantees fewer default permissions are granted to the generated app. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-78-03 | Elevation of Privilege | `AndroidManifest.xml.eex` | mitigate | Strictly minimal permissions template prevents accidental deployment with excessive unused capabilities. |
</threat_model>

<verification>
Android templates are correctly formulated with valid EEx tags.
</verification>

<success_criteria>
Android scaffolding creates a thin client architecture.
</success_criteria>

<output>
Create `.planning/phases/78-automated-host-scaffold-generation/78-03-SUMMARY.md` when done
</output>
