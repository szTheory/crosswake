---
phase: 78
plan: 02
type: execute
wave: 2
depends_on: ["78-01"]
files_modified:
  - priv/templates/crosswake/shell/ios/CrosswakeCoordinator.swift.eex
  - priv/templates/crosswake/shell/ios/CrosswakeShellApp.swift.eex
  - priv/templates/crosswake/shell/ios/Info.plist.eex
  - priv/templates/crosswake/shell/ios/CrosswakeShell.xcodeproj/project.pbxproj.eex
autonomous: true
requirements: [GEN-01, GEN-02, GEN-03]
must_haves:
  truths:
    - "iOS generator produces a thin app driven by a remote or local SPM dependency."
    - "iOS root app delegates state observation to CrosswakeCoordinator."
    - "iOS Info.plist contains no bloated capability comments."
  artifacts:
    - path: "priv/templates/crosswake/shell/ios/CrosswakeCoordinator.swift.eex"
      provides: "Glue layer initializing SDK"
      contains: "CrosswakeShell.initialize"
    - path: "priv/templates/crosswake/shell/ios/CrosswakeShellApp.swift.eex"
      provides: "Dumb root app"
      contains: "@StateObject"
    - path: "priv/templates/crosswake/shell/ios/CrosswakeShell.xcodeproj/project.pbxproj.eex"
      provides: "SPM Dependency"
      contains: "<%= if @local do %>"
  key_links:
    - from: "priv/templates/crosswake/shell/ios/CrosswakeShellApp.swift.eex"
      to: "priv/templates/crosswake/shell/ios/CrosswakeCoordinator.swift.eex"
      via: "StateObject instantiation"
      pattern: "CrosswakeCoordinator"
---

<objective>
Overhaul iOS EEx templates to output a thin scaffold that consumes `crosswake-shell-core-ios` via SPM and uses a dedicated glue layer.

Purpose: Implement D-01, D-03, D-05, D-06, and D-07 for iOS native clients.
Output: Updated EEx templates for the iOS project.
</objective>

<context>
@.planning/phases/78-automated-host-scaffold-generation/78-CONTEXT.md
@.planning/phases/78-automated-host-scaffold-generation/78-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create `CrosswakeCoordinator.swift.eex`</name>
  <files>priv/templates/crosswake/shell/ios/CrosswakeCoordinator.swift.eex</files>
  <action>
    Implement the glue layer template (D-06, D-07).
    - Must be an `ObservableObject`.
    - Expose `@Published var state: CrosswakeState = .initializing`.
    - `init()` calls `CrosswakeShell.initialize()` and subscribes to state updates via publisher, assigning it to `self.state`.
  </action>
  <verify>
    <automated>grep "CrosswakeShell.initialize" priv/templates/crosswake/shell/ios/CrosswakeCoordinator.swift.eex</automated>
  </verify>
  <done>Coordinator template correctly initializes the shell and observes state.</done>
</task>

<task type="auto">
  <name>Task 2: Update `CrosswakeShellApp.swift.eex` and `Info.plist.eex`</name>
  <files>
    priv/templates/crosswake/shell/ios/CrosswakeShellApp.swift.eex,
    priv/templates/crosswake/shell/ios/Info.plist.eex
  </files>
  <action>
    Update App root (D-05) and Plist (D-03, D-04):
    1. In `CrosswakeShellApp.swift.eex`, remove legacy routing/URL handling and make it a dumb view that uses `@StateObject var coordinator = CrosswakeCoordinator()`. Render basic text based on `coordinator.state`.
    2. In `Info.plist.eex`, remove any large commented blocks of camera/bluetooth/etc permissions. Add a single standard comment (e.g. `<!-- For adding capabilities, see: https://docs.crosswake.dev/capabilities -->`) and only include essential boot permissions (internet).
  </action>
  <verify>
    <automated>grep "@StateObject" priv/templates/crosswake/shell/ios/CrosswakeShellApp.swift.eex</automated>
  </verify>
  <done>App root is simplified and Info.plist is minimal.</done>
</task>

<task type="auto">
  <name>Task 3: Inject SPM Dependency into `project.pbxproj.eex`</name>
  <files>priv/templates/crosswake/shell/ios/CrosswakeShell.xcodeproj/project.pbxproj.eex</files>
  <action>
    Update Xcode project template (GEN-01).
    - Add `CrosswakeShellCore` Swift Package dependency.
    - Use `<%= if @local do %>` to conditionally inject a local path reference (`../../../packages/crosswake-shell-core-ios` relative to the generated project root) instead of the remote Git repository URL.
    - Ensure `PBXFrameworksBuildPhase` links the package product.
  </action>
  <verify>
    <automated>grep "crosswake-shell-core" priv/templates/crosswake/shell/ios/CrosswakeShell.xcodeproj/project.pbxproj.eex</automated>
  </verify>
  <done>Xcode project properly references the Swift package based on the --local flag.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Generated Code → OS | Minimal plist guarantees fewer default permissions are granted to the generated app. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-78-02 | Elevation of Privilege | `Info.plist.eex` | mitigate | Strictly minimal permissions template prevents accidental deployment with excessive unused capabilities. |
</threat_model>

<verification>
iOS templates are correctly formulated with valid EEx tags.
</verification>

<success_criteria>
iOS scaffolding creates a thin client architecture.
</success_criteria>

<output>
Create `.planning/phases/78-automated-host-scaffold-generation/78-02-SUMMARY.md` when done
</output>
