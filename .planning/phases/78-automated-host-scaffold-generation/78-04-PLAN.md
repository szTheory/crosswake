---
phase: 78
plan: 04
type: execute
wave: 3
depends_on: ["78-02", "78-03"]
files_modified:
  - test/mix/tasks/crosswake_gen_shell_test.exs
autonomous: true
requirements: [GEN-01, GEN-02, GEN-03]
must_haves:
  truths:
    - "ExUnit generator tests pass."
    - "ExUnit generator tests validate the new thin scaffold structure."
    - "ExUnit generator tests validate the `--local` flag."
  artifacts:
    - path: "test/mix/tasks/crosswake_gen_shell_test.exs"
      provides: "Generator validations"
      contains: "CrosswakeCoordinator"
  key_links:
    - from: "test/mix/tasks/crosswake_gen_shell_test.exs"
      to: "lib/mix/tasks/crosswake.gen.shell.ex"
      via: "mix task invocation"
      pattern: "Mix.Task.run"
---

<objective>
Refactor the generator ExUnit test suite to assert the new thin scaffold structure and remove legacy file validations.

Purpose: Avoid false-positive CI failures caused by the test asserting the existence of removed "fat" source files.
Output: Updated ExUnit tests.
</objective>

<context>
@.planning/phases/78-automated-host-scaffold-generation/78-CONTEXT.md
@.planning/phases/78-automated-host-scaffold-generation/78-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite Generator Assertions</name>
  <files>test/mix/tasks/crosswake_gen_shell_test.exs</files>
  <action>
    Refactor `crosswake_gen_shell_test.exs` completely to match the thin architecture:
    1. Remove all `assert File.read!(...)` checks for `ActivationCoordinator`, `BridgeChannel`, `PackStore`, etc.
    2. Add assertions that `CrosswakeCoordinator.swift` and `CrosswakeViewModel.kt` exist and contain expected initialization strings.
    3. Add assertions that `Info.plist` and `AndroidManifest.xml` exist, and verify they lack large permission blocks while containing the documentation URL.
    4. Ensure the test runs the generator twice: once without `--local` (asserting remote dependencies) and once with `--local` (asserting local paths in `project.pbxproj` and `build.gradle`).
  </action>
  <verify>
    <automated>mix test test/mix/tasks/crosswake_gen_shell_test.exs</automated>
  </verify>
  <done>ExUnit tests pass and correctly validate both remote and local dependencies.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test Suite → Generator | Ensure generator regressions are caught by CI. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-78-04 | Repudiation | `crosswake_gen_shell_test.exs` | mitigate | Comprehensive testing of output artifacts ensures the CLI cannot silently fail to scaffold the correct dependency links. |
</threat_model>

<verification>
`mix test test/mix/tasks/crosswake_gen_shell_test.exs` runs green.
</verification>

<success_criteria>
Generator test suite properly enforces the new thin scaffold structure and validates the local dependency capability.
</success_criteria>

<output>
Create `.planning/phases/78-automated-host-scaffold-generation/78-04-SUMMARY.md` when done
</output>
