---
phase: 78
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/mix/tasks/crosswake.gen.shell.ex
  - priv/templates/crosswake/shell/ios/CrosswakeCoordinator.swift.eex
  - priv/templates/crosswake/shell/android/app/src/main/java/dev/crosswake/shell/CrosswakeViewModel.kt.eex
  - priv/templates/crosswake/shell/ios/ActivationCoordinator.swift.eex
  - priv/templates/crosswake/shell/ios/BridgeChannel.swift.eex
  - priv/templates/crosswake/shell/ios/LiveViewContainerViewController.swift.eex
  - priv/templates/crosswake/shell/ios/NativeCaptureView.swift.eex
  - priv/templates/crosswake/shell/ios/OfflineStore.swift.eex
  - priv/templates/crosswake/shell/ios/PackStore.swift.eex
  - priv/templates/crosswake/shell/ios/RequiredPackView.swift.eex
  - priv/templates/crosswake/shell/ios/RouteUnavailableView.swift.eex
  - priv/templates/crosswake/shell/ios/StudySessionIsland.swift.eex
  - priv/templates/crosswake/shell/ios/TransferCoordinator.swift.eex
  - priv/templates/crosswake/shell/ios/CrosswakeShellTests/ActivationCoordinatorTests.swift.eex
  - priv/templates/crosswake/shell/android/app/src/main/java/dev/crosswake/shell/ActivationCoordinator.kt.eex
  - priv/templates/crosswake/shell/android/app/src/main/java/dev/crosswake/shell/BridgeChannel.kt.eex
  - priv/templates/crosswake/shell/android/app/src/main/java/dev/crosswake/shell/LiveViewFragment.kt.eex
  - priv/templates/crosswake/shell/android/app/src/main/java/dev/crosswake/shell/NativeCaptureActivity.kt.eex
  - priv/templates/crosswake/shell/android/app/src/main/java/dev/crosswake/shell/RouteUnavailableActivity.kt.eex
  - priv/templates/crosswake/shell/android/app/src/main/java/dev/crosswake/shell/offline/OfflineStore.kt.eex
  - priv/templates/crosswake/shell/android/app/src/main/java/dev/crosswake/shell/offline/StudySessionIsland.kt.eex
  - priv/templates/crosswake/shell/android/app/src/main/java/dev/crosswake/shell/packs/PackStore.kt.eex
  - priv/templates/crosswake/shell/android/app/src/main/java/dev/crosswake/shell/packs/RequiredPackActivity.kt.eex
  - priv/templates/crosswake/shell/android/app/src/main/java/dev/crosswake/shell/transfer/TransferCoordinator.kt.eex
  - priv/templates/crosswake/shell/android/app/src/test/java/dev/crosswake/shell/ActivationCoordinatorTest.kt.eex
  - priv/templates/crosswake/shell/android/app/src/androidTest/java/dev/crosswake/shell/LiveViewBootInstrumentedTest.kt.eex
autonomous: true
requirements: [GEN-01]
must_haves:
  truths:
    - "CLI task crosswake.gen.shell accepts --local flag."
    - "Legacy fat templates are completely removed from the project structure."
  artifacts:
    - path: "lib/mix/tasks/crosswake.gen.shell.ex"
      provides: "Option parsing and template rendering updates"
      contains: "--local"
    - path: "priv/templates/crosswake/shell/ios/CrosswakeCoordinator.swift.eex"
      provides: "Empty placeholder for iOS glue layer"
      min_lines: 1
    - path: "priv/templates/crosswake/shell/android/app/src/main/java/dev/crosswake/shell/CrosswakeViewModel.kt.eex"
      provides: "Empty placeholder for Android glue layer"
      min_lines: 1
  key_links:
    - from: "lib/mix/tasks/crosswake.gen.shell.ex"
      to: "priv/templates/crosswake/shell"
      via: "template mapping lists"
      pattern: "@ios_templates"
---

<objective>
Update the `crosswake.gen.shell` Elixir task to support generating thin dependency-driven host projects and purge legacy fat template files.

Purpose: Elixir CLI tooling must support a `--local` flag for testing and must stop referring to massive duplicated source files that constitute the "eject trap."
Output: Updated `mix` task and a cleaned `priv/templates/` directory.
</objective>

<context>
@.planning/phases/78-automated-host-scaffold-generation/78-CONTEXT.md
@.planning/phases/78-automated-host-scaffold-generation/78-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add `--local` flag to Generator Task</name>
  <files>lib/mix/tasks/crosswake.gen.shell.ex</files>
  <action>
    Modify the generator task to support local dependency paths.
    1. Add `local: :boolean` to `@switches`.
    2. Extract `local` from `opts` in `run/1`.
    3. Update `render_ios_templates` and `render_android_templates` to accept `local` and pass it to `render_template`.
    4. In `render_template`, update `EEx.eval_file` assigns to include `local: local`.
    5. Clean up `@android_templates` and `@ios_templates` lists. Remove all legacy "fat" files (e.g. `ActivationCoordinator`, `BridgeChannel`, `LiveViewContainerViewController`, `PackStore`, `OfflineStore`, etc.).
    6. Add `{"CrosswakeShell/CrosswakeCoordinator.swift", "ios/CrosswakeCoordinator.swift.eex"}` to `@ios_templates`.
    7. Add `{"app/src/main/java/dev/crosswake/shell/CrosswakeViewModel.kt", "android/app/src/main/java/dev/crosswake/shell/CrosswakeViewModel.kt.eex"}` to `@android_templates`.
  </action>
  <verify>
    <automated>grep "local: :boolean" lib/mix/tasks/crosswake.gen.shell.ex</automated>
  </verify>
  <done>Task accepts --local and template lists are updated.</done>
</task>

<task type="auto">
  <name>Task 2: Purge Legacy Templates & Create Placeholders</name>
  <files>
    priv/templates/crosswake/shell/ios/*,
    priv/templates/crosswake/shell/android/app/src/main/java/dev/crosswake/shell/*,
    priv/templates/crosswake/shell/ios/CrosswakeCoordinator.swift.eex,
    priv/templates/crosswake/shell/android/app/src/main/java/dev/crosswake/shell/CrosswakeViewModel.kt.eex
  </files>
  <action>
    Delete all legacy `.eex` template files removed from the lists in Task 1. 
    This includes `ActivationCoordinator`, `BridgeChannel`, `LiveViewFragment`, `NativeCapture*`, `OfflineStore`, `PackStore`, `StudySessionIsland`, and the entire `transfer`, `packs`, `offline` subdirectories. Delete associated test templates (`ActivationCoordinatorTests.swift.eex`, `ActivationCoordinatorTest.kt.eex`, `LiveViewBootInstrumentedTest.kt.eex`).

    Create `CrosswakeCoordinator.swift.eex` and `CrosswakeViewModel.kt.eex` as empty files (or simple placeholders with a comment) so that the generator lists updated in Task 1 are structurally valid.
  </action>
  <verify>
    <automated>test ! -f priv/templates/crosswake/shell/ios/ActivationCoordinator.swift.eex</automated>
  </verify>
  <done>Legacy files are deleted and placeholders exist.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI Input → Generator | `--local` flag input must be validated as a safe boolean. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-78-01 | Tampering | `crosswake.gen.shell.ex` | mitigate | Use Elixir's strict `OptionParser` to ensure `--local` is cleanly parsed as a boolean, avoiding injection. |
</threat_model>

<verification>
`mix compile` successfully compiles the updated task.
</verification>

<success_criteria>
`crosswake.gen.shell` task code is updated and the `priv/templates/` directory is pruned of legacy files.
</success_criteria>

<output>
Create `.planning/phases/78-automated-host-scaffold-generation/78-01-SUMMARY.md` when done
</output>
