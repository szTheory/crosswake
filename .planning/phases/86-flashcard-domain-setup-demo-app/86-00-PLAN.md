---
phase: 86-flashcard-domain-setup-demo-app
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - examples/phoenix_host/test/support/flashcards_fixtures.ex
  - examples/phoenix_host/test/crosswake_example/flashcards_test.exs
autonomous: true
requirements:
  - DEMO-01
must_haves:
  truths:
    - "Flashcard context operations are validated"
  artifacts:
    - path: "examples/phoenix_host/test/crosswake_example/flashcards_test.exs"
      provides: "Context unit tests"
      contains: "defmodule CrosswakeExample.FlashcardsTest"
  key_links: []
---

<objective>
Implement test fixtures and context tests for the Flashcard domain as a prerequisite for implementation.

Purpose: Ensure a test-driven approach by defining expected behavior before creating schemas and contexts.
Output: Test files that will be satisfied in Wave 1.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/86-flashcard-domain-setup-demo-app/86-RESEARCH.md
</context>

<tasks>
<task type="auto">
  <name>Task 1: Fixtures and Context Tests</name>
  <files>
    examples/phoenix_host/test/support/flashcards_fixtures.ex
    examples/phoenix_host/test/crosswake_example/flashcards_test.exs
  </files>
  <read_first>
    - examples/phoenix_host/test/test_helper.exs
  </read_first>
  <action>
    Create test fixtures in `examples/phoenix_host/test/support/flashcards_fixtures.ex` defining `deck_fixture/1`, `card_fixture/1`, and `progress_fixture/1` within module `CrosswakeExample.FlashcardsFixtures`.
    Create context tests in `examples/phoenix_host/test/crosswake_example/flashcards_test.exs` covering basic operations in the `Flashcards` context (using `ExUnit.Case`).
    Include tests for `list_decks/0`, `create_deck/1`, `create_card/1`, and `list_deck_cards/1`.
    Since implementations don't exist yet, these tests will fail initially. That is expected.
  </action>
  <verify>
    <automated>cd examples/phoenix_host && grep -q "defmodule CrosswakeExample.FlashcardsFixtures" test/support/flashcards_fixtures.ex</automated>
  </verify>
  <done>Test files and fixtures are created</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Testing | No production trust boundary for tests. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-86-00 | Information Disclosure | Tests | accept | Tests contain non-sensitive language learning data. |
</threat_model>
