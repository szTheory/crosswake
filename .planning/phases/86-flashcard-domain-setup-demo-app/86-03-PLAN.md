---
phase: 86-flashcard-domain-setup-demo-app
plan: 03
type: execute
wave: 3
depends_on: ["86-01", "86-02"]
files_modified:
  - examples/phoenix_host/mix.exs
  - examples/phoenix_host/test/crosswake_example/flashcards_test.exs
  - examples/phoenix_host/test/support/flashcards_fixtures.ex
autonomous: true
requirements:
  - DEMO-01
must_haves:
  truths:
    - "Flashcard context operations are validated"
    - "Test suite compiles correctly without missing modules"
  artifacts:
    - path: "examples/phoenix_host/mix.exs"
      provides: "Compiler paths configuration"
      contains: "elixirc_paths"
  key_links: []
gap_closure: true
---

<objective>
Fix verification gaps: enable compilation of Phoenix test fixtures by adding `elixirc_paths` to `mix.exs`, and update tests and fixtures to match actual Ecto schema fields (`front_text`, `back_text`, `ease`, `interval`) and context functions (`upsert_progress`).

Purpose: Ensure the test suite compiles and tests pass against the actual implementation.
Output: A working, compiling, passing test suite for the Flashcards context.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/86-flashcard-domain-setup-demo-app/86-VERIFICATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add elixirc_paths to mix.exs</name>
  <files>examples/phoenix_host/mix.exs</files>
  <read_first>
    examples/phoenix_host/mix.exs
  </read_first>
  <action>
    Add `elixirc_paths` configuration to `examples/phoenix_host/mix.exs` so that the `:test` environment includes the `test/support` directory.
    - Define a private function `elixirc_paths(:test), do: ["lib", "test/support"]`
    - Define a fallback `elixirc_paths(_), do: ["lib"]`
    - Add `elixirc_paths: elixirc_paths(Mix.env())` to the `project/0` returned keyword list.
    Gap reason: Test suite fails to compile due to missing compiler paths (`module CrosswakeExample.FlashcardsFixtures is not loaded`).
  </action>
  <verify>
    <automated>cd examples/phoenix_host && mix compile</automated>
  </verify>
  <done>
    `mix.exs` correctly defines `elixirc_paths` to include `test/support` in test environment, fixing the compilation error.
  </done>
</task>

<task type="auto">
  <name>Task 2: Fix schema fields and function calls in tests and fixtures</name>
  <files>
    examples/phoenix_host/test/crosswake_example/flashcards_test.exs
    examples/phoenix_host/test/support/flashcards_fixtures.ex
  </files>
  <read_first>
    examples/phoenix_host/test/crosswake_example/flashcards_test.exs
    examples/phoenix_host/test/support/flashcards_fixtures.ex
    examples/phoenix_host/lib/crosswake_example/flashcards.ex
  </read_first>
  <action>
    Update both the test cases and the fixtures to use the correct schema fields and context functions implemented in Wave 1:
    1. For `Card`, replace `front` and `back` with `front_text` and `back_text`.
    2. For `Progress`, replace `status` with `ease` and `interval` (provide valid numeric/float default values for both in fixtures).
    3. In `flashcards_fixtures.ex` and `flashcards_test.exs`, replace any call to `create_progress` with `upsert_progress`.
    4. Ensure the `flashcards_test.exs` assertions match the actual schema fields.
    Gap reason: Tests and fixtures use incorrect field names (`front`/`back` vs `front_text`/`back_text`, `status` vs `ease`/`interval`) and non-existent `create_progress`.
  </action>
  <verify>
    <automated>cd examples/phoenix_host && mix test test/crosswake_example/flashcards_test.exs</automated>
  </verify>
  <done>
    `flashcards_test.exs` compiles, runs, and passes, validating against the correct schema fields and `upsert_progress` function.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Testing | No production trust boundary for tests. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-86-03 | Information Disclosure | Tests | accept | Tests contain non-sensitive language learning data. |
</threat_model>

<verification>
Run `cd examples/phoenix_host && mix test test/crosswake_example/flashcards_test.exs` and ensure it passes successfully.
</verification>

<success_criteria>
- The test suite compiles without missing `CrosswakeExample.FlashcardsFixtures` module errors.
- The tests run and pass against the actual Ecto schemas.
- `front_text`, `back_text`, `ease`, `interval`, and `upsert_progress` are correctly utilized in tests and fixtures.
</success_criteria>