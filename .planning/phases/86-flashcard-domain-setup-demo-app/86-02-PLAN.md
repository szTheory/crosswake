---
phase: 86-flashcard-domain-setup-demo-app
plan: 02
type: execute
wave: 2
depends_on: ["86-01"]
files_modified:
  - examples/phoenix_host/priv/repo/seeds.exs
autonomous: true
requirements:
  - DEMO-01
must_haves:
  truths:
    - "Demo data can be loaded via mix run priv/repo/seeds.exs"
  artifacts:
    - path: "examples/phoenix_host/priv/repo/seeds.exs"
      provides: "Demo database seeds"
      contains: "Repo.insert"
  key_links:
    - from: "examples/phoenix_host/priv/repo/seeds.exs"
      to: "examples/phoenix_host/lib/crosswake_example/flashcards.ex"
      via: "context calls or direct repo inserts"
      pattern: "Repo\\.insert|Flashcards\\."
---

<objective>
Implement initial database seeds for the Flashcard domain.

Purpose: Provide a demo data baseline for LiveView and offline development.
Output: A repeatable seed script that populates decks and cards.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/86-flashcard-domain-setup-demo-app/86-RESEARCH.md
</context>

<tasks>
<task type="auto">
  <name>Task 1: Demo Seeding</name>
  <files>examples/phoenix_host/priv/repo/seeds.exs</files>
  <read_first>
    - examples/phoenix_host/lib/crosswake_example/flashcards.ex
  </read_first>
  <action>
    Update or create `examples/phoenix_host/priv/repo/seeds.exs` to insert demo data for the flashcard app.
    Alias `CrosswakeExample.Repo` and `CrosswakeExample.Flashcards.{Deck, Card}`.
    Insert at least one deck (e.g., title: "Elixir Basics", description: "Core concepts of Elixir").
    Insert at least 3 cards for that deck (e.g., front: "What is OTP?", front: "What is a GenServer?", front: "What is Ecto?").
    Use `Repo.insert!` or the `Flashcards` context to create them, ensuring they are only created if they don't already exist or the script clears them first (idempotent is best, or standard Phoenix seeds which assume clean DB).
  </action>
  <verify>
    <automated>cd examples/phoenix_host && mix run priv/repo/seeds.exs</automated>
  </verify>
  <done>Seeds script executed successfully</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Testing | No production trust boundary for seeds and tests. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-86-02 | Information Disclosure | Demo Seeds | accept | Seeds contain non-sensitive language learning data. |
</threat_model>
