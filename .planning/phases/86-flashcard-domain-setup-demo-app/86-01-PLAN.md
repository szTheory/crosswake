---
phase: 86-flashcard-domain-setup-demo-app
plan: 01
type: execute
wave: 1
depends_on: ["86-00"]
files_modified:
  - examples/phoenix_host/priv/repo/migrations/*_create_flashcard_decks.exs
  - examples/phoenix_host/priv/repo/migrations/*_create_flashcard_cards.exs
  - examples/phoenix_host/priv/repo/migrations/*_create_flashcard_progress.exs
  - examples/phoenix_host/lib/crosswake_example/flashcards/deck.ex
  - examples/phoenix_host/lib/crosswake_example/flashcards/card.ex
  - examples/phoenix_host/lib/crosswake_example/flashcards/progress.ex
  - examples/phoenix_host/lib/crosswake_example/flashcards.ex
autonomous: true
requirements:
  - DEMO-01
must_haves:
  truths:
    - "Flashcard data models can be persisted to the database"
    - "Flashcard context provides CRUD operations for Decks and Cards"
  artifacts:
    - path: "examples/phoenix_host/lib/crosswake_example/flashcards/deck.ex"
      provides: "Ecto schema for Decks"
      contains: "@primary_key {:id, :binary_id, autogenerate: true}"
    - path: "examples/phoenix_host/lib/crosswake_example/flashcards.ex"
      provides: "Phoenix Context boundary for Flashcards"
      exports: ["list_decks", "list_deck_cards"]
  key_links:
    - from: "examples/phoenix_host/lib/crosswake_example/flashcards/card.ex"
      to: "examples/phoenix_host/lib/crosswake_example/flashcards/deck.ex"
      via: "belongs_to"
      pattern: "belongs_to :deck"
---

<objective>
Generate the Ecto migrations, schemas, and Phoenix context for the Flashcard domain using `binary_id` to support offline synchronization.

Purpose: Provide the backend data structure for the language learning demo app (Decks, Cards, Progress) and make the Wave 0 tests pass.
Output: Migrations, schema files, and the `CrosswakeExample.Flashcards` context module.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/86-flashcard-domain-setup-demo-app/86-RESEARCH.md
</context>

<tasks>
<task type="auto">
  <name>Task 1: Generate Migrations</name>
  <files>examples/phoenix_host/priv/repo/migrations/*</files>
  <read_first>
    - examples/phoenix_host/mix.exs
  </read_first>
  <action>
    Create three migration files in `examples/phoenix_host/priv/repo/migrations/` (you may use `mix ecto.gen.migration` within `examples/phoenix_host`).
    - Migration 1: `create_flashcard_decks` with `:id` as `:binary_id` primary key, `title` (:string), `description` (:string), and timestamps.
    - Migration 2: `create_flashcard_cards` with `:id` as `:binary_id` primary key, `deck_id` (references `:flashcard_decks`, type: `:binary_id`, on_delete: `:delete_all`), `front_text` (:string), `back_text` (:string), and timestamps. Create an index on `deck_id`.
    - Migration 3: `create_flashcard_progress` with `:id` as `:binary_id` primary key, `card_id` (references `:flashcard_cards`, type: `:binary_id`, on_delete: `:delete_all`), `user_id` (:string), `ease` (:float), `interval` (:integer), `next_review_at` (:utc_datetime), and timestamps. Create indexes on `card_id` and `user_id`, and a unique index on `[:card_id, :user_id]`.
  </action>
  <verify>
    <automated>cd examples/phoenix_host && mix ecto.migrate</automated>
  </verify>
  <done>Migrations created and applied</done>
</task>

<task type="auto">
  <name>Task 2: Create Ecto Schemas</name>
  <files>
    examples/phoenix_host/lib/crosswake_example/flashcards/deck.ex
    examples/phoenix_host/lib/crosswake_example/flashcards/card.ex
    examples/phoenix_host/lib/crosswake_example/flashcards/progress.ex
  </files>
  <read_first>
    - examples/phoenix_host/mix.exs
  </read_first>
  <action>
    Create the corresponding Ecto schemas in `examples/phoenix_host/lib/crosswake_example/flashcards/`.
    - `Deck`: module `CrosswakeExample.Flashcards.Deck`. Use `@primary_key {:id, :binary_id, autogenerate: true}` and `@foreign_key_type :binary_id`. schema "flashcard_decks", fields: `title` (:string), `description` (:string), `has_many :cards, CrosswakeExample.Flashcards.Card`.
    - `Card`: module `CrosswakeExample.Flashcards.Card`. Use `:binary_id` settings. schema "flashcard_cards", fields: `front_text` (:string), `back_text` (:string), `belongs_to :deck, CrosswakeExample.Flashcards.Deck`.
    - `Progress`: module `CrosswakeExample.Flashcards.Progress`. Use `:binary_id` settings. schema "flashcard_progress", fields: `user_id` (:string), `ease` (:float), `interval` (:integer), `next_review_at` (:utc_datetime), `belongs_to :card, CrosswakeExample.Flashcards.Card`.
    Each module should include a `changeset(struct, attrs)` function for casting and validating its fields using `Ecto.Changeset`.
  </action>
  <verify>
    <automated>cd examples/phoenix_host && mix compile</automated>
  </verify>
  <done>Ecto schemas created with binary_id</done>
</task>

<task type="auto">
  <name>Task 3: Create Phoenix Context</name>
  <files>examples/phoenix_host/lib/crosswake_example/flashcards.ex</files>
  <read_first>
    - examples/phoenix_host/lib/crosswake_example/flashcards/deck.ex
    - examples/phoenix_host/lib/crosswake_example/flashcards/card.ex
    - examples/phoenix_host/lib/crosswake_example/flashcards/progress.ex
  </read_first>
  <action>
    Create the Context boundary `examples/phoenix_host/lib/crosswake_example/flashcards.ex`.
    Implement module `CrosswakeExample.Flashcards`.
    Add standard CRUD functions for Decks: `list_decks/0`, `get_deck!/1`, `create_deck/1`, `update_deck/2`, `delete_deck/1`, `change_deck/1`.
    Add functions for Cards: `list_cards/0`, `get_card!/1`, `create_card/1`, `update_card/2`, `delete_card/1`, `change_card/1`.
    Add a function `list_deck_cards/1` which takes a `deck_id` and returns all cards for that deck (ordered by insertion or ID).
    Add functions for Progress: `get_progress/2` (takes `card_id` and `user_id`), `upsert_progress/1`.
  </action>
  <verify>
    <automated>cd examples/phoenix_host && mix test test/crosswake_example/flashcards_test.exs</automated>
  </verify>
  <done>Flashcards context boundary created and tests pass</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| API -> DB | Flashcard progress data is updated by users, requiring basic sanitization. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-86-01 | Tampering | Flashcards.Progress | mitigate | Validate inputs via `Ecto.Changeset` using explicit `cast/3` lists to prevent over-posting. |
</threat_model>
