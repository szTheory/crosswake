---
phase: 37-guides-walkthrough-and-docs-contract-lock
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - guides/commerce.md
  - test/crosswake/guides/commerce_test.exs
autonomous: true
requirements: [DOCS-01, DOCS-02]

must_haves:
  truths:
    # SC#1 (DOCS-01) — anchored walkthrough exists
    - "guides/commerce.md contains a `### Paywall Corridor Walkthrough` H3 inside Layer 1, after `### Minimal Reconciliation Inbox Example` and before `### Backend Idempotency`"
    - "Each of the six D-04 steps anchors a named example-host module/function with its relative file path (route declaration → MockStorefront purchase/restore → ingest_evidence/2 → project_snapshot/2 → derived_state/1 → PaywallLive)"
    # SC#2 (DOCS-01) — mock-vs-real callout
    - "The walkthrough opens with an explicit mock-vs-real callout naming `provider: \"mock\"`, no StoreKit, no Play Billing (D-05)"
    # SC#3 (DOCS-02) — named module + canonical field names + live lock
    - "`CrosswakeExample.Commerce.MockStorefront` is named exactly in the guide, and canonical field names `provider_reference` and `evidence_ref` appear (not invented aliases)"
    - "All six anchored functions resolve via `function_exported?/3` after module-scope `Code.require_file` of the pure commerce modules (live-code guard, D-06.2)"
    # D-08 — proof citation
    - "The walkthrough cites `test/crosswake/proof/phase34_paywall_corridor_proof_test.exs` as the hermetic merge-blocking proof, and the test asserts that path string"
    # SC#4 / SC#5 (DOCS-02) — regression fences survive
    - "The phase23 `commerce guide publishes three explicit layer headings` test still passes (three H2 layers unchanged, no 4th H2)"
    - "The phase23 four/five non-claims test still passes (StoreKit, Play Billing, Device-local authority, Offline purchase replay, Storefront purchase UI all `not shipped`)"
  artifacts:
    - path: "guides/commerce.md"
      provides: "Paywall Corridor Walkthrough H3 (anchor-only, mock-vs-real callout, six anchored steps, proof citation)"
      contains: "### Paywall Corridor Walkthrough"
    - path: "test/crosswake/guides/commerce_test.exs"
      provides: "Docs-contract assertions: string-presence (D-06.1) + live-code guard (D-06.2) + module-scope require_file + async: false"
      contains: "function_exported?(CrosswakeExample.Commerce.MockStorefront, :simulate_purchase, 2)"
  key_links:
    - from: "test/crosswake/guides/commerce_test.exs"
      to: "guides/commerce.md"
      via: "content =~ assertions against walkthrough strings"
      pattern: "### Paywall Corridor Walkthrough"
    - from: "test/crosswake/guides/commerce_test.exs"
      to: "examples/phoenix_host/lib/crosswake_example/commerce/*.ex"
      via: "module-scope Code.require_file + function_exported?/3"
      pattern: "function_exported\\?\\(CrosswakeExample.Commerce"
---

<objective>
Add an end-to-end **Paywall Corridor Walkthrough** to `guides/commerce.md` written against the final shipped Phase 33–36 example-host code, and extend `test/crosswake/guides/commerce_test.exs` with hybrid docs-contract assertions that lock the walkthrough's module/function references, canonical field names, mock-vs-real callout, and proof citation against the shipped example — without weakening the phase23 three-layer or non-claims regression fences.

Purpose: Make `guides/commerce.md` a genuinely merge-blocking artifact (DOCS-01, DOCS-02). After this phase, renaming or removing any anchored example function (or dropping the walkthrough heading, callout, or proof citation) breaks the guide test immediately — the guide can no longer drift from the shipped corridor.

Output: Two edited files — `guides/commerce.md` (one new H3 inside Layer 1) and `test/crosswake/guides/commerce_test.exs` (module-scope `Code.require_file`, `async: false`, and one new `describe` block). No `lib/crosswake/**` or example-host module changes (all shipped/locked, Phases 21/33/34/35/36).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/37-guides-walkthrough-and-docs-contract-lock/37-CONTEXT.md
@.planning/phases/37-guides-walkthrough-and-docs-contract-lock/37-RESEARCH.md
@.planning/phases/37-guides-walkthrough-and-docs-contract-lock/37-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Insert the Paywall Corridor Walkthrough H3 into guides/commerce.md (DOCS-01)</name>
  <files>guides/commerce.md</files>
  <read_first>
    - guides/commerce.md — read fully. Confirm the insertion point: the new H3 goes AFTER the `### Minimal Reconciliation Inbox Example` section (which ends at the paragraph "This reconciliation walkthrough is `example/docs-only` and companion-ready...") and BEFORE `### Backend Idempotency`. Confirm the three H2 layers are exactly `## Commerce Support Truth` / `## Reviewer And Storefront Playbooks` / `## Rough Edges And Non-Claims`.
    - guides/commerce.md `## Rough Edges And Non-Claims` (Layer 3) — read the canonical "X adapter is not shipped" non-claim phrasing so the new callout REFERENCES the mock-vs-real boundary WITHOUT copying the exact Layer 3 "not shipped" sentences into Layer 1 (Pitfall 5 in RESEARCH).
    - .planning/phases/37-guides-walkthrough-and-docs-contract-lock/37-RESEARCH.md "Architecture Patterns → Guide Insertion Pattern" — the six anchor steps in order, and "Research Finding 5" for the canonical field names.
  </read_first>
  <action>
    Insert exactly ONE new H3 section titled `### Paywall Corridor Walkthrough` (H3, three hashes — NOT a 4th H2, per D-02) between `### Minimal Reconciliation Inbox Example` and `### Backend Idempotency`, inside Layer 1 `## Commerce Support Truth` (D-01).

    Anchor-only — NO fenced code blocks anywhere in the section (D-03). Each step renders prose + the named module/function + its relative file path (inline backticks for identifiers, the path in parentheses).

    OPEN the section with an explicit mock-vs-real callout (D-05, SC#2) that:
    - names the literal value `provider: "mock"` verbatim,
    - states that no StoreKit code is shipped and no Play Billing code is shipped (use mock-vs-real positioning phrasing such as "no StoreKit or Play Billing code is shipped"; do NOT copy the Layer 3 sentences `StoreKit adapter is not shipped` / `Play Billing adapter is not shipped` verbatim — those stay in Layer 3 per Pitfall 5),
    - points the reader to `## Rough Edges And Non-Claims` for the explicit non-claims.

    Then narrate the six D-04 steps IN THIS ORDER, each anchoring the named symbol(s) + relative path:
    1. Route declaration — `commerce: [corridor: :subscription_default, role: :paywall_entry]`, in `examples/phoenix_host/lib/crosswake_example/router.ex`.
    2. Mock storefront — `CrosswakeExample.Commerce.MockStorefront.simulate_purchase/2` and `CrosswakeExample.Commerce.MockStorefront.simulate_restore/2`, in `examples/phoenix_host/lib/crosswake_example/commerce/mock_storefront.ex`. Name the canonical evidence field names `provider_reference` and `evidence_ref` (SC#3, Research Finding 5) when describing the emitted `ReconciliationEvidence`.
    3. Evidence ingestion — `CrosswakeExample.Commerce.ReconciliationInbox.ingest_evidence/2`, in `examples/phoenix_host/lib/crosswake_example/commerce/reconciliation_inbox.ex`.
    4. Snapshot projection — `CrosswakeExample.Commerce.EntitlementProjection.project_snapshot/2`, in `examples/phoenix_host/lib/crosswake_example/commerce/entitlement_projection.ex`. Note the verified-snapshot construction helper `CrosswakeExample.Commerce.MockBackend.build_verified_snapshot/2` in `examples/phoenix_host/lib/crosswake_example/commerce/mock_backend.ex` feeds the `:granted` path.
    5. Derived state — `CrosswakeExample.Commerce.EntitlementProjection.derived_state/1`, producing the four states `:stale | :pending | :denied | :granted`.
    6. LiveView rendering — `CrosswakeExample.PaywallEntryLive`, in `examples/phoenix_host/lib/crosswake_example/paywall_entry_live.ex`, rendering the four derived states. (Anchor by name in prose only — this is a runtime module; the test never loads it.)

    CLOSE the section with the D-08 proof citation: state that the full lane runs end-to-end in the hermetic, merge-blocking proof `test/crosswake/proof/phase34_paywall_corridor_proof_test.exs` (cite the path string verbatim), optionally noting the `.github/workflows/phase34-proof.yml` hermetic CI job runs it.

    Do NOT touch the three H2 headings, Layer 3 non-claims, or any other section (D-02, D-07). Run `mix format` if it touches markdown — it does not, but do not introduce trailing whitespace.
  </action>
  <verify>
    <automated>mix test test/crosswake/guides/commerce_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - `guides/commerce.md` contains the literal string `### Paywall Corridor Walkthrough`.
    - The new H3 appears after `### Minimal Reconciliation Inbox Example` and before `### Backend Idempotency` (grep line ordering confirms).
    - `guides/commerce.md` contains `CrosswakeExample.Commerce.MockStorefront`, `simulate_purchase/2`, `simulate_restore/2`, `ReconciliationInbox.ingest_evidence/2`, `EntitlementProjection.project_snapshot/2`, `derived_state/1`, `MockBackend.build_verified_snapshot/2`, and `CrosswakeExample.PaywallEntryLive`.
    - `guides/commerce.md` contains the literal `provider: "mock"`, the words `StoreKit` and `Play Billing` in the callout, and the literal `provider_reference` and `evidence_ref`.
    - `guides/commerce.md` contains the literal path string `test/crosswake/proof/phase34_paywall_corridor_proof_test.exs`.
    - `grep -c '^## ' guides/commerce.md` returns exactly 3 (no 4th H2 added).
    - No fenced code blocks (```) inside the new walkthrough section.
    - `mix test test/crosswake/guides/commerce_test.exs` exits 0 (the existing phase23 three-layer + non-claims fences still pass — Task 1 alone must not regress them).
  </acceptance_criteria>
  <done>The walkthrough H3 is present with all six anchored steps, mock-vs-real callout, canonical field names, and proof citation; the existing commerce_test.exs (pre-Task-2) still passes green, proving no regression to the phase23 fences.</done>
</task>

<task type="auto">
  <name>Task 2: Append hybrid docs-contract assertions to commerce_test.exs (DOCS-02)</name>
  <files>test/crosswake/guides/commerce_test.exs</files>
  <read_first>
    - test/crosswake/guides/commerce_test.exs — read fully. Confirm current module header `use ExUnit.Case, async: true`, the `@guide_path` + `setup_all` reading `content`, the `content =~` idiom, and the phase23 regression fences: `commerce guide publishes three explicit layer headings` and `non-claims section explicitly names StoreKit, Play Billing, device-local authority, and offline replay`. These MUST remain unchanged and passing (D-07).
    - test/crosswake/proof/phase34_mock_storefront_test.exs — read the file header (lines 1-30): the module-scope `Code.require_file(...)` calls BEFORE `defmodule`, the relative path form `"../../../examples/phoenix_host/lib/crosswake_example/commerce/<module>.ex"`, and `use ExUnit.Case, async: false`. Copy this idiom exactly.
    - .planning/phases/37-guides-walkthrough-and-docs-contract-lock/37-RESEARCH.md "Research Finding 3" (load order + async: false rationale) and "Research Finding 4" (the complete six-assertion function_exported? list; NEVER assert verify_and_broadcast/2).
    - guides/commerce.md — after Task 1, to confirm the walkthrough strings the new string-presence assertions reference actually exist.
  </read_first>
  <action>
    Make three edits to `test/crosswake/guides/commerce_test.exs`:

    (1) At the TOP of the file, BEFORE `defmodule Crosswake.Guides.CommerceTest do`, add five module-scope `Code.require_file` calls in dependency load order (keys → inbox → storefront → projection → backend), using the relative path form verified in RESEARCH (identical to the proof path since `guides/` and `proof/` are at the same depth):
      - `reconciliation_keys.ex`
      - `reconciliation_inbox.ex`
      - `mock_storefront.ex`
      - `entitlement_projection.ex`
      - `mock_backend.ex`
    Path form: `"../../../examples/phoenix_host/lib/crosswake_example/commerce/<module>.ex"`, second arg `__DIR__`. This order matters because `ReconciliationInbox` aliases `ReconciliationKeys` at compile time (Pitfall 2).

    (2) Change the module header from `use ExUnit.Case, async: true` to `use ExUnit.Case, async: false` (Discretion (a) resolved YES — module-scope require_file collision-avoidance, matches Phase 34/36 precedent).

    (3) Append ONE new `describe "paywall corridor walkthrough (DOCS-01 / DOCS-02)"` block at the end of the module (after the last existing test, before the final `end`) containing:

    String-presence assertions (D-06.1), using the existing `content =~ ...` idiom (these tests take `%{content: content}`):
      - heading exists: `content =~ "### Paywall Corridor Walkthrough"` (SC#1).
      - module named exactly: `content =~ "CrosswakeExample.Commerce.MockStorefront"` (SC#3).
      - canonical field names present: `content =~ "provider_reference"` and `content =~ "evidence_ref"` (SC#3).
      - mock-vs-real callout present: `content =~ ~s(provider: "mock")`, and presence of `StoreKit` and `Play Billing` in callout context (SC#2). Use the exact strings the walkthrough uses from Task 1.
      - proof file cited (D-08): `content =~ "test/crosswake/proof/phase34_paywall_corridor_proof_test.exs"`.

    Live-code guard (D-06.2), ONE test that does NOT need `content` (use `_context` or no context arg) asserting all SIX `function_exported?/3` (Research Finding 4):
      - `function_exported?(CrosswakeExample.Commerce.MockStorefront, :simulate_purchase, 2)`
      - `function_exported?(CrosswakeExample.Commerce.MockStorefront, :simulate_restore, 2)`
      - `function_exported?(CrosswakeExample.Commerce.ReconciliationInbox, :ingest_evidence, 2)`
      - `function_exported?(CrosswakeExample.Commerce.EntitlementProjection, :project_snapshot, 2)`
      - `function_exported?(CrosswakeExample.Commerce.EntitlementProjection, :derived_state, 1)`
      - `function_exported?(CrosswakeExample.Commerce.MockBackend, :build_verified_snapshot, 2)`

    NEVER assert `function_exported?(MockBackend, :verify_and_broadcast, 2)` and NEVER invoke any of these functions — `function_exported?` checks only (Pitfall 4: verify_and_broadcast touches Phoenix.PubSub at runtime; build_verified_snapshot is pure/safe and only `function_exported?`-checked). Do NOT `Code.require_file` `paywall_entry_live.ex` or any router/endpoint runtime module (Pitfall 4 / 6).

    Give each assertion a failure message string. Do NOT modify, rename, or weaken the existing phase23 fences (D-07). Run `mix format` on the file after editing.
  </action>
  <verify>
    <automated>mix test test/crosswake/guides/commerce_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - First non-blank line under `defmodule Crosswake.Guides.CommerceTest do` is `use ExUnit.Case, async: false` (no longer `async: true`).
    - The five `Code.require_file(...)` lines appear at file scope BEFORE `defmodule`, in order keys → inbox → storefront → projection → backend.
    - `commerce_test.exs` contains `function_exported?(CrosswakeExample.Commerce.MockStorefront, :simulate_purchase, 2)` and the other five `function_exported?/3` assertions; contains NO `verify_and_broadcast` reference.
    - `commerce_test.exs` contains `content =~ "### Paywall Corridor Walkthrough"`, `content =~ "CrosswakeExample.Commerce.MockStorefront"`, `provider_reference`, `evidence_ref`, `provider: "mock"`, and `test/crosswake/proof/phase34_paywall_corridor_proof_test.exs`.
    - The existing tests `commerce guide publishes three explicit layer headings` and `non-claims section explicitly names StoreKit, Play Billing, device-local authority, and offline replay` are present verbatim and unmodified.
    - `mix test test/crosswake/guides/commerce_test.exs` exits 0 (all new assertions + all phase23 fences green).
    - `mix format --check-formatted test/crosswake/guides/commerce_test.exs` exits 0.
  </acceptance_criteria>
  <done>commerce_test.exs is `async: false` with five module-scope require_file calls and a new describe block containing five string-presence assertions plus one six-assertion live-code guard; the full file passes green and the phase23 regression fences are untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| guide prose ↔ shipped example code | Documentation can drift from the code it describes — the only meaningful boundary in this docs+test phase. |

This phase adds NO runtime code, NO inputs, NO network I/O, NO auth, NO data persistence, and NO new dependencies. The example modules are loaded into the test via `Code.require_file` and only `function_exported?`-checked (never invoked), so no PubSub/runtime path executes. No new attack surface is introduced.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-37-01 | Tampering (documentation drift) | `guides/commerce.md` walkthrough claims a function/module that does not exist or is later renamed | mitigate | D-06.2 live-code guard: six `function_exported?/3` assertions against require_file'd pure modules break the test the moment an anchored symbol is renamed/removed. |
| T-37-02 | Information disclosure (false capability claim) | Guide implies a StoreKit/Play Billing adapter is shipped | mitigate | D-05 mock-vs-real callout (`provider: "mock"`, no StoreKit/Play Billing) + D-07 four/five non-claims regression fence asserting all "not shipped" statements survive. |
| T-37-03 | Repudiation (proof citation rot) | Guide cites a proof file that is renamed/deleted, leaving "merge-blocking" claim unfounded | mitigate | D-08 string-presence assertion locks the literal path `test/crosswake/proof/phase34_paywall_corridor_proof_test.exs`. |
| T-37-04 | Denial (hermeticity break) | A future edit `require_file`s a runtime module (PaywallLive) or invokes `verify_and_broadcast/2`, breaking the hermetic merge-blocking lane | mitigate | Plan explicitly forbids loading runtime modules and invoking PubSub functions; only pure commerce modules are loaded and only `function_exported?`-checked. |

No high-severity threats — no install tasks, no `[ASSUMED]`/`[SUS]` packages (zero new dependencies), no network/auth surface. No blocking human checkpoint required.
</threat_model>

<verification>
Single Nyquist command for every task (37-VALIDATION.md is the validation contract):

```
mix test test/crosswake/guides/commerce_test.exs
```

After the wave completes, run the full suite to confirm no cross-file regression:

```
mix test
```

SC → assertion map (from 37-VALIDATION.md):
- SC#1 (DOCS-01) → `content =~ "### Paywall Corridor Walkthrough"` + anchored-module string assertions.
- SC#2 (DOCS-01) → `provider: "mock"` / StoreKit / Play Billing callout assertions.
- SC#3 (DOCS-02) → exact `CrosswakeExample.Commerce.MockStorefront` + `provider_reference`/`evidence_ref` + six `function_exported?/3` live guards.
- SC#4 (DOCS-02) → existing `commerce guide publishes three explicit layer headings` fence still green.
- SC#5 (DOCS-02) → existing four/five non-claims fence still green.
- D-08 → `content =~ "test/crosswake/proof/phase34_paywall_corridor_proof_test.exs"`.
</verification>

<success_criteria>
- `guides/commerce.md` has a `### Paywall Corridor Walkthrough` H3 inside Layer 1 with six anchored steps, mock-vs-real callout, canonical field names, and the proof citation (DOCS-01, SC#1/SC#2).
- `test/crosswake/guides/commerce_test.exs` is `async: false`, loads the five pure commerce modules at module scope, and asserts both string-presence (D-06.1) and six `function_exported?/3` live guards (D-06.2) (DOCS-02, SC#3).
- The phase23 three-layer heading fence and the four/five non-claims fence both still pass (SC#4, SC#5, D-07).
- `mix test test/crosswake/guides/commerce_test.exs` and `mix test` both exit 0.
- Exactly TWO files changed (`guides/commerce.md`, `test/crosswake/guides/commerce_test.exs`); zero changes to `lib/crosswake/**` or any example-host module.
</success_criteria>

<output>
Create `.planning/phases/37-guides-walkthrough-and-docs-contract-lock/37-01-SUMMARY.md` when done.
</output>
