---
phase: 48-commerce-provider-adapter-context
plan: "04"
type: execute
wave: 2
depends_on:
  - "48-01"
  - "48-02"
files_modified:
  - lib/crosswake/support_matrix/support_matrix.ex
  - lib/crosswake/operator_inspection.ex
  - lib/crosswake/doctor/publish_readiness.ex
  - test/crosswake/support_matrix_test.exs
  - test/crosswake/doctor/publish_readiness_test.exs
  - test/crosswake/proof/phase48_provider_adapter_proof_test.exs
autonomous: true
requirements:
  - ADPT-01
  - ADPT-02
  - ADPT-03
must_haves:
  truths:
    - "Support truth distinguishes shipped provider seams from advisory provider/device proof."
    - "Promotion rules remain criteria-as-code and demotable."
    - "Doctor/readiness check IDs become provider-specific enough to avoid stale `adapters_not_shipped` wording once seams ship."
---

<objective>
Update canonical support, operator inspection, and doctor readiness so v3.7 provider adapter seams are visible without overstating proof posture.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/48-commerce-provider-adapter-context/48-CONTEXT.md
@.planning/phases/48-commerce-provider-adapter-context/48-RESEARCH.md
@lib/crosswake/support_matrix/support_matrix.ex
@lib/crosswake/operator_inspection.ex
@lib/crosswake/doctor/publish_readiness.ex
@lib/crosswake/manifest/types.ex
@test/crosswake/proof/phase52_operator_truth_test.exs
@test/crosswake/doctor/publish_readiness_test.exs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Lock support and promotion semantics for shipped seams plus advisory proof</name>
  <read_first>
    lib/crosswake/support_matrix/support_matrix.ex, lib/crosswake/manifest/types.ex, test/crosswake/proof/phase52_operator_truth_test.exs, .planning/phases/48-commerce-provider-adapter-context/48-CONTEXT.md
  </read_first>
  <files>test/crosswake/support_matrix_test.exs, test/crosswake/proof/phase48_provider_adapter_proof_test.exs</files>
  <action>Add failing tests asserting `Crosswake.SupportMatrix.promotion_rules/0` still contains the four claim IDs `purchase_intent.provider.storekit`, `restore_intent.provider.storekit`, `purchase_intent.provider.play_billing`, and `restore_intent.provider.play_billing`, but their `required_evidence` includes deterministic adapter contract proof, backend reconciliation proof, docs parity, and advisory storefront lane evidence. Assert each rule has `action_class: "provider_adapter"`, `current_proof_class: :advisory`, `promotes_to: :merge_blocking`, non-empty `check_ids`, and a demotion trigger. Add support assertions that provider adapter seams are described as shipped while sandbox/device proof remains advisory.</action>
  <acceptance_criteria>
    - Tests fail if provider support is represented as a single boolean ready flag.
    - Tests fail if promotion rules lack docs anchors, check IDs, minimum consecutive passes, freshness windows, or demotion triggers.
    - Tests preserve support status, proof class, action class, rebuild class, and promotion rule as separate axes.
  </acceptance_criteria>
  <verify><automated>mix test test/crosswake/support_matrix_test.exs test/crosswake/proof/phase48_provider_adapter_proof_test.exs</automated></verify>
  <done>Support and promotion tests capture the shipped-seam/advisory-proof split.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement support, operator, and doctor readiness updates</name>
  <read_first>
    lib/crosswake/support_matrix/support_matrix.ex, lib/crosswake/operator_inspection.ex, lib/crosswake/doctor/publish_readiness.ex, test/crosswake/doctor/publish_readiness_test.exs, test/crosswake/proof/phase48_provider_adapter_proof_test.exs
  </read_first>
  <files>lib/crosswake/support_matrix/support_matrix.ex, lib/crosswake/operator_inspection.ex, lib/crosswake/doctor/publish_readiness.ex</files>
  <action>Update `Crosswake.SupportMatrix` provider adapter rows and promotion rules so v3.7 says StoreKit and Play Billing evidence adapter seams are shipped but environment-sensitive provider proof stays advisory until the existing criteria pass. Replace or supplement `diag.provider.adapters_not_shipped` with provider-specific readiness IDs such as `diag.provider.storekit.advisory_proof` and `diag.provider.play_billing.advisory_proof` while preserving backwards-compatible details where tests require them. Update `Crosswake.OperatorInspection` only if needed so `purchase_intent` and `restore_intent` routes expose the four provider promotion IDs and provider-adapter action class. Update `Crosswake.Doctor.PublishReadiness` so JSON and human findings distinguish `shipped_seams?: true`, `advisory_provider_proof?: true`, required setup, and rebuild requirement.</action>
  <acceptance_criteria>
    - `mix crosswake.doctor --check-publish --format json` includes provider adapter readiness details with shipped seam truth separate from advisory proof.
    - Operator inspection still derives provider promotion IDs from commerce route roles.
    - Provider adapter action class remains rebuild-required.
    - Existing Phase 52 operator proof still passes after fixture updates are made intentionally.
  </acceptance_criteria>
  <verify><automated>mix test test/crosswake/doctor/publish_readiness_test.exs test/crosswake/proof/phase52_operator_truth_test.exs test/crosswake/proof/phase48_provider_adapter_proof_test.exs</automated></verify>
  <done>Runtime/support diagnostics report provider adapter seams honestly and machine-readably.</done>
</task>

</tasks>

<threat_model>
## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-48-07 | Repudiation | Support/promotion truth | mitigate | Criteria-as-code promotion rows with check IDs, docs anchors, freshness, failure budgets, and demotion triggers. |
| T-48-08 | Tampering | Doctor readiness claims | mitigate | Tests assert shipped seam truth and advisory proof posture are separate machine-readable fields. |
</threat_model>

<verification>
Run `mix test test/crosswake/support_matrix_test.exs test/crosswake/doctor/publish_readiness_test.exs test/crosswake/proof/phase52_operator_truth_test.exs test/crosswake/proof/phase48_provider_adapter_proof_test.exs`.
</verification>

<success_criteria>
- SupportMatrix provider rules and action classes are current for v3.7.
- Doctor/readiness reports shipped seams without implying provider certification.
- Operator inspection keeps provider promotion IDs attached to commerce routes.
</success_criteria>

<output>
Create `.planning/phases/48-commerce-provider-adapter-context/48-04-SUMMARY.md` when done.
</output>
