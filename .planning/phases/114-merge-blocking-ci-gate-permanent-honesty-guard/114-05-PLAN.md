---
phase: 114-merge-blocking-ci-gate-permanent-honesty-guard
plan: 05
type: execute
wave: 2
depends_on: ["114-01"]
files_modified:
  - script/register-e2e-gate.sh
  - .github/workflows/offline-sync-e2e-gate.yml
autonomous: true
requirements: [GATE-01]
must_haves:
  truths:
    - "script/register-e2e-gate.sh GETs the live required_status_checks and replaces (never hardcodes) the checks array"
    - "It merges in merge-blocking-offline-sync-e2e (app_id 15368), drops the old e2e-offline-sync context, unique_by(.context), preserves the two existing required checks and strict: true"
    - "It supports DRY_RUN=1 (prints desired JSON, writes nothing) and is idempotent"
    - "It REFUSES (exit 2) to register until merge-blocking-offline-sync-e2e has a successful check-run on main (green-first preflight)"
    - "The workflow header carries the exact gh api ... PATCH one-liner as a comment, plus the correct ordering runbook"
  artifacts:
    - path: "script/register-e2e-gate.sh"
      provides: "GET-then-replace branch-protection registration with green-first preflight"
      contains: "required_status_checks"
  key_links:
    - from: "script/register-e2e-gate.sh"
      to: ".github/workflows/offline-sync-e2e-gate.yml"
      via: "registers the aggregator job name merge-blocking-offline-sync-e2e as the required check"
      pattern: "merge-blocking-offline-sync-e2e"
---

<objective>
Ship the GATE-01 registration deliverable (D-06): a committed, idempotent
script/register-e2e-gate.sh that GET-then-replaces main's required_status_checks to add
merge-blocking-offline-sync-e2e while preserving every existing check, and REFUSES to run
until that check has gone green on main at least once (green-first preflight) to avoid the
"Expected — Waiting for status" repo-wide deadlock. Mirror the exact gh api PATCH one-liner
+ ordering runbook as a comment in the workflow header.

IMPORTANT (sequencing reality): the branch-protection MUTATION is run OUT-OF-BAND by the
maintainer AFTER the renamed aggregator goes green on main; branch-protection writes are
harness-blocked. This plan delivers the SCRIPT + preflight + workflow comment — NOT the
executed registration. GATE-01's acceptance is "script + green-first preflight + workflow
comment exist and are correct," not "branch protection is mutated."

Output: script/register-e2e-gate.sh + the gh api PATCH comment block in the workflow.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/114-merge-blocking-ci-gate-permanent-honesty-guard/114-CONTEXT.md
@.planning/phases/114-merge-blocking-ci-gate-permanent-honesty-guard/114-RESEARCH-SYNTHESIS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author register-e2e-gate.sh (GET-then-replace + green-first preflight)</name>
  <files>script/register-e2e-gate.sh</files>
  <read_first>
    - .planning/phases/114-merge-blocking-ci-gate-permanent-honesty-guard/114-RESEARCH-SYNTHESIS.md §4 (the ready-to-lift bash script with the exact jq merge + preflight probe — lift it)
    - script/verify_adopter_profile_contract.sh (house style: #!/usr/bin/env bash, set -euo pipefail, ROOT_DIR resolution)
  </read_first>
  <action>
    Lift the script from RESEARCH-SYNTHESIS §4. It must: `set -euo pipefail`; parameterize
    REPO (default szTheory/crosswake), BRANCH (default main), NEW_CHECK
    (merge-blocking-offline-sync-e2e), ACTIONS_APP_ID (default 15368), OLD_CHECK (default
    e2e-offline-sync), DRY_RUN (default 0). GET the live required_status_checks endpoint,
    then build the desired payload with jq: keep strict from the current value; map the
    current checks dropping the OLD_CHECK context, append {context:NEW_CHECK, app_id:APP},
    and `unique_by(.context)`. NEVER hardcode the checks array (a hardcode silently drops
    checks added later — this preserves the two existing required checks
    "merge-blocking rulestead proof (hermetic)" and "brand-structural"). Print the desired
    JSON; if DRY_RUN=1, exit 0 without writing. Otherwise run the green-first preflight:
    query repos/REPO/commits/BRANCH/check-runs and `jq -e` that some check-run with
    name==NEW_CHECK has conclusion=="success"; if not, print a REFUSING message instructing
    "merge the rename, let it go green on main, then re-run" and exit 2. Only then
    `gh api -X PATCH` the endpoint with the desired payload and re-print the resulting
    {strict, checks}. Make it idempotent (re-running after registration is a no-op via
    unique_by). chmod +x the script.
  </action>
  <verify>
    <automated>cd /Users/jon/projects/crosswake && bash -n script/register-e2e-gate.sh && test -x script/register-e2e-gate.sh && grep -q 'required_status_checks' script/register-e2e-gate.sh && grep -q 'unique_by(.context)' script/register-e2e-gate.sh && grep -q 'exit 2' script/register-e2e-gate.sh</automated>
  </verify>
  <acceptance_criteria>
    - `bash -n script/register-e2e-gate.sh` parses clean and the file is executable (source + behavior assertion)
    - The script GETs required_status_checks and builds the payload from the live value with unique_by(.context) — no hardcoded checks array literal (source assertion)
    - It drops OLD_CHECK e2e-offline-sync and appends NEW_CHECK merge-blocking-offline-sync-e2e with app_id 15368 (source assertion)
    - DRY_RUN=1 path prints the desired JSON and exits 0 without a PATCH (source assertion: PATCH guarded behind the DRY_RUN check)
    - The green-first preflight exits 2 when no successful NEW_CHECK run exists on main (source assertion: check-runs probe + exit 2)
  </acceptance_criteria>
  <done>register-e2e-gate.sh is an idempotent GET-then-replace with DRY_RUN support and a green-first preflight that exits 2 before any deadlock-inducing PATCH.</done>
</task>

<task type="auto">
  <name>Task 2: Add the gh api PATCH comment + ordering runbook to the workflow header</name>
  <files>.github/workflows/offline-sync-e2e-gate.yml</files>
  <read_first>
    - .github/workflows/offline-sync-e2e-gate.yml (the placeholder comment slot left by plan 114-01)
    - .planning/phases/114-merge-blocking-ci-gate-permanent-honesty-guard/114-RESEARCH-SYNTHESIS.md §4 (the exact gh api PATCH one-liner + ordering: merge the rename → green on main → run the script)
    - .github/workflows/phase96-proof-advisory.yml (house pattern for an in-workflow runbook comment block)
  </read_first>
  <action>
    Replace the placeholder comment slot in the workflow header (left by plan 114-01) with a
    comment block that mirrors the exact gh api ... PATCH one-liner from RESEARCH-SYNTHESIS §4
    for registering merge-blocking-offline-sync-e2e as the required check, and documents the
    correct ordering: (1) merge the rename PR, (2) let merge-blocking-offline-sync-e2e go
    green on main at least once, (3) run script/register-e2e-gate.sh (maintainer, out-of-band
    — branch-protection writes are harness-blocked). Note it preserves the two existing
    required checks and strict:true. This is a YAML COMMENT only — do not add a job or step.
  </action>
  <verify>
    <automated>cd /Users/jon/projects/crosswake && grep -q 'register-e2e-gate.sh' .github/workflows/offline-sync-e2e-gate.yml && grep -q 'required_status_checks' .github/workflows/offline-sync-e2e-gate.yml</automated>
  </verify>
  <acceptance_criteria>
    - The workflow header comment references script/register-e2e-gate.sh and the gh api required_status_checks PATCH (source assertion)
    - The comment states the merge → green-on-main → run-script ordering (source assertion)
    - The addition is a comment only — `grep -cE '^  [a-z].*:$'` job-count is unchanged from plan 114-01's four jobs (behavior assertion: no new job/step added)
  </acceptance_criteria>
  <done>The workflow header carries the exact PATCH one-liner and the ordering runbook as a comment, with no new jobs/steps.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| maintainer → branch-protection API | The registration mutates main's required checks; a hardcoded payload or premature register would either drop existing checks or deadlock every PR |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-114-12 | Denial of Service | premature required-check rename → "Expected — Waiting for status" deadlock | mitigate | green-first preflight (check-runs success probe → exit 2) refuses to register until the new check has reported green on main |
| T-114-13 | Tampering | hardcoded checks array silently dropping existing required checks | mitigate | GET-then-replace with unique_by(.context); the two existing checks + strict are read from live state, never literal-ized |
| T-114-14 | Elevation of Privilege | granular vs full-protection PUT clobbering enforce_admins/reviews | mitigate | target the granular required_status_checks endpoint only — leaves enforce_admins/reviews untouched |
| T-114-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs in this plan (shell script + YAML comment only) |
</threat_model>

<verification>
- bash -n parses; script is executable; GET-then-replace with unique_by; drops OLD, adds NEW with app_id 15368
- DRY_RUN=1 writes nothing; preflight exits 2 absent a green run
- Workflow header carries the PATCH one-liner + ordering runbook (comment only)
</verification>

<success_criteria>
The registration script + green-first preflight + workflow comment exist and are correct; the branch-protection mutation itself is left for the maintainer to run out-of-band after the aggregator goes green on main (harness-blocked).
</success_criteria>

<output>
Create `.planning/phases/114-merge-blocking-ci-gate-permanent-honesty-guard/114-05-SUMMARY.md` when done
</output>
