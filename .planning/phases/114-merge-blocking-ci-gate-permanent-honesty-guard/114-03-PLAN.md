---
phase: 114-merge-blocking-ci-gate-permanent-honesty-guard
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - examples/phoenix_host/test/crosswake_example/router_test.exs
  - examples/phoenix_host/test/crosswake_example/e2e/sync_state_controller_test.exs
  - examples/phoenix_host/lib/crosswake_example/router.ex
autonomous: true
requirements: [GUARD-02]
must_haves:
  truths:
    - "router_test.exs asserts /_e2e/sync-state/:client_mutation_id is compiled under :test, wired to SyncStateController :show, verb :get"
    - "The controller scoping test inserts >=2 ReviewEvent rows with distinct client_mutation_id and asserts count == 1 for one id (guards the whole-table aggregate footgun)"
    - "Neither test references the route via ~p verified routes (conditionally-compiled routes break ~p)"
    - "router.ex carries a one-line comment above the Mix.env() if documenting /_e2e as the reserved test-harness namespace"
    - "The Mix.env() in [:test, :e2e] compile-time gating is unchanged (no Application.compile_env, no runtime guard)"
  artifacts:
    - path: "examples/phoenix_host/test/crosswake_example/router_test.exs"
      provides: "Positive route-presence assertion under :test via Phoenix.Router.routes/1"
      contains: "Phoenix.Router.routes"
    - path: "examples/phoenix_host/test/crosswake_example/e2e/sync_state_controller_test.exs"
      provides: "client_mutation_id scoping proof for the count aggregate"
      contains: "client_mutation_id"
  key_links:
    - from: "examples/phoenix_host/test/crosswake_example/router_test.exs"
      to: "examples/phoenix_host/lib/crosswake_example/router.ex"
      via: "Phoenix.Router.routes(CrosswakeExample.Router) finds the /_e2e route"
      pattern: "CrosswakeExample.Router"
    - from: "examples/phoenix_host/test/crosswake_example/e2e/sync_state_controller_test.exs"
      to: "examples/phoenix_host/lib/crosswake_example/e2e/sync_state_controller.ex"
      via: "exercises SyncStateController.show scoping via Repo"
      pattern: "SyncStateController"
---

<objective>
Add the GUARD-02 in-suite behavioral backstop (D-08, D-09): an ExUnit route-presence
assertion that the /_e2e endpoint is compiled under :test wired to the scoping
controller, plus a controller test proving `count` is scoped to client_mutation_id (not
a whole-table aggregate). Document /_e2e as the reserved test-harness namespace above the
existing compile-time Mix.env() gate without changing the gating mechanism.

Purpose: the CI prod-absence grep (plan 114-01) proves the route is ABSENT in prod; these
in-suite assertions prove it is PRESENT and correctly wired in :test, and that the count
the E2E relies on is per-mutation-scoped. Together they form GUARD-02's two halves.

Output: router_test.exs, sync_state_controller_test.exs, and a one-line router comment.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/114-merge-blocking-ci-gate-permanent-honesty-guard/114-CONTEXT.md
@.planning/phases/114-merge-blocking-ci-gate-permanent-honesty-guard/114-RESEARCH-SYNTHESIS.md
@examples/phoenix_host/lib/crosswake_example/router.ex
@examples/phoenix_host/lib/crosswake_example/e2e/sync_state_controller.ex
@examples/phoenix_host/lib/crosswake_example/local_first/review_event.ex
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Route-presence ExUnit assertion</name>
  <files>examples/phoenix_host/test/crosswake_example/router_test.exs</files>
  <behavior>
    - Phoenix.Router.routes(CrosswakeExample.Router) contains a route whose path is /_e2e/sync-state/:client_mutation_id
    - That route's verb == :get, plug == CrosswakeExample.E2E.SyncStateController, plug_opts == :show
    - The test runs under :test (where the route is compiled in) and passes
  </behavior>
  <read_first>
    - .planning/phases/114-merge-blocking-ci-gate-permanent-honesty-guard/114-RESEARCH-SYNTHESIS.md §5 (the ready-to-lift router_test.exs sketch)
    - examples/phoenix_host/lib/crosswake_example/router.ex (the /_e2e scope ~line 378: path, verb, controller, action)
    - examples/phoenix_host/test/crosswake_example/flashcards_test.exs (house pattern: plain `use ExUnit.Case`, no DataCase/ConnCase exists)
  </read_first>
  <action>
    Lift the router_test.exs from RESEARCH-SYNTHESIS §5: a plain `use ExUnit.Case, async: true`
    module that finds the /_e2e/sync-state/:client_mutation_id route via
    `CrosswakeExample.Router |> Phoenix.Router.routes() |> Enum.find(...)` and asserts it
    exists with verb :get, plug CrosswakeExample.E2E.SyncStateController, plug_opts :show.
    Do NOT use `~p` verified routes — conditionally-compiled routes break `~p` verification
    (D-08). Place the file at test/crosswake_example/router_test.exs.
  </action>
  <verify>
    <automated>cd /Users/jon/projects/crosswake/examples/phoenix_host && mix test test/crosswake_example/router_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - `mix test test/crosswake_example/router_test.exs` passes under MIX_ENV=test (behavior assertion)
    - The test asserts verb :get, plug CrosswakeExample.E2E.SyncStateController, plug_opts :show (source assertion)
    - The file contains no `~p` sigil reference to the /_e2e path (source assertion)
  </acceptance_criteria>
  <done>The route-presence test passes under :test and asserts the full verb/plug/plug_opts wiring without ~p.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Controller count-scoping test</name>
  <files>examples/phoenix_host/test/crosswake_example/e2e/sync_state_controller_test.exs</files>
  <behavior>
    - Insert two ReviewEvent rows with distinct client_mutation_id values (each with valid card_id, rating, status)
    - Querying sync-state for one of the ids yields count == 1 (NOT 2) — proving the aggregate is scoped to client_mutation_id, not the whole table
    - Querying for a non-existent id yields synced: false, count: 0
  </behavior>
  <read_first>
    - .planning/phases/114-merge-blocking-ci-gate-permanent-honesty-guard/114-RESEARCH-SYNTHESIS.md §5 (controller scoping test guidance: >=2 distinct rows, assert count == 1)
    - examples/phoenix_host/lib/crosswake_example/e2e/sync_state_controller.ex (the scoped-count footgun the test protects; show/2 returns %{synced, status, count})
    - examples/phoenix_host/lib/crosswake_example/local_first/review_event.ex (schema fields + changeset required: client_mutation_id, card_id, rating)
    - examples/phoenix_host/test/test_helper.exs (just ExUnit.start(); no Sandbox configured — SQLite, no async sandbox)
  </read_first>
  <action>
    Add the lowest-footprint controller scoping test (D-08, planner's discretion: no
    DataCase/ConnCase exists). Use plain `use ExUnit.Case` (async: false — SQLite has no
    async sandbox and rows are shared). Insert >=2 ReviewEvent rows with DISTINCT
    client_mutation_id via Repo.insert!(ReviewEvent.changeset(...)) (valid card_id, rating
    in ["good","hard"], status in ["accepted","rejected"]). Exercise the scoping path —
    either call SyncStateController.show/2 with a built Plug.Conn and decode the JSON body,
    or directly assert the controller's scoped `from(r in ReviewEvent, where: r.client_mutation_id == ^id) |> Repo.aggregate(:count, :id)` returns 1 for one id while the table holds >=2 rows. Prefer the controller-through path (build conn via Phoenix.ConnTest.build_conn or Plug.Test) so the test guards the real show/2 code; fall back to the query-level assertion only if conn construction adds disproportionate footprint. Clean up inserted rows (on_exit Repo.delete_all or unique ids per run) so repeated runs stay deterministic. Place at test/crosswake_example/e2e/sync_state_controller_test.exs (note: plan 114-01's e2e-proof job runs exactly this path).
  </action>
  <verify>
    <automated>cd /Users/jon/projects/crosswake/examples/phoenix_host && mix test test/crosswake_example/e2e/sync_state_controller_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - `mix test test/crosswake_example/e2e/sync_state_controller_test.exs` passes (behavior assertion)
    - The test inserts at least two rows with distinct client_mutation_id and asserts count == 1 for a single id (source + behavior assertion — proves whole-table aggregate would fail)
    - Re-running the test twice in succession both pass (behavior assertion — deterministic cleanup)
  </acceptance_criteria>
  <done>The controller test proves count is scoped to client_mutation_id (count == 1 with >=2 rows present) and is deterministic across reruns.</done>
</task>

<task type="auto">
  <name>Task 3: Document the reserved /_e2e namespace above the Mix.env() gate</name>
  <files>examples/phoenix_host/lib/crosswake_example/router.ex</files>
  <read_first>
    - examples/phoenix_host/lib/crosswake_example/router.ex (the `if Mix.env() in [:test, :e2e] do` block ~line 378)
    - .planning/phases/114-merge-blocking-ci-gate-permanent-honesty-guard/114-CONTEXT.md D-09 (keep Mix.env() compile-time gating; no Application.compile_env, no runtime guard; add a one-line documenting comment)
  </read_first>
  <action>
    Add a single one-line comment immediately above the `if Mix.env() in [:test, :e2e] do`
    block documenting /_e2e as the reserved test-harness namespace that compiles OUT of prod
    (the branch is erased from prod beams). Do NOT change the gating mechanism: keep
    `Mix.env() in [:test, :e2e]` (compile-time-safe; the "Mix unavailable in releases" rule
    only bites runtime calls). Do NOT switch to Application.compile_env and do NOT add a
    runtime refusal guard (compile-out is strictly stronger than exists-but-guarded; a
    redundant guard wrongly implies the route ships to prod — D-09).
  </action>
  <verify>
    <automated>cd /Users/jon/projects/crosswake/examples/phoenix_host && grep -B1 'if Mix.env() in \[:test, :e2e\]' lib/crosswake_example/router.ex | grep -q '#' && MIX_ENV=test mix compile --warnings-as-errors</automated>
  </verify>
  <acceptance_criteria>
    - A comment line referencing /_e2e as the reserved test-harness namespace sits directly above the `if Mix.env() in [:test, :e2e]` line (source assertion)
    - The gating expression is still exactly `Mix.env() in [:test, :e2e]` — no Application.compile_env, no runtime guard added (source assertion)
    - `MIX_ENV=test mix compile --warnings-as-errors` succeeds (behavior assertion)
  </acceptance_criteria>
  <done>A one-line namespace comment documents /_e2e; the compile-time Mix.env() gate is unchanged and still compiles cleanly.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| :test/:e2e compile → :prod compile | The /_e2e proof route must exist in :test (this plan asserts presence) and be absent in :prod (plan 114-01 asserts absence); a regression here is a production data-leak path |
| client_mutation_id → Ecto count aggregate | The E2E's "exactly one row" claim depends on the count being scoped per mutation, not whole-table |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-114-07 | Information Disclosure | /_e2e/sync-state endpoint leaking into prod | mitigate | compile-time Mix.env() gate (kept, documented) erases the route from prod beams; router_test proves :test presence, GUARD-02 CI grep proves prod absence |
| T-114-08 | Spoofing | whole-table count masquerading as per-mutation count | mitigate | controller scoping test inserts >=2 distinct rows and asserts count == 1, catching a bare Repo.aggregate(:count) regression |
| T-114-09 | Tampering | switching to a weaker runtime guard | accept | D-09 locks compile-out as strictly stronger; no runtime guard added by design |
| T-114-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs in this plan (Elixir tests only; mix deps unchanged) |
</threat_model>

<verification>
- router_test.exs passes under :test, asserts full wiring, no ~p
- controller scoping test proves count == 1 with >=2 rows, deterministic
- router.ex carries the namespace comment; compile clean; gate unchanged
</verification>

<success_criteria>
GUARD-02's in-suite half exists: route-presence asserted under :test, count proven scoped to client_mutation_id, /_e2e documented as the reserved namespace, and the compile-time gate untouched.
</success_criteria>

<output>
Create `.planning/phases/114-merge-blocking-ci-gate-permanent-honesty-guard/114-03-SUMMARY.md` when done
</output>
