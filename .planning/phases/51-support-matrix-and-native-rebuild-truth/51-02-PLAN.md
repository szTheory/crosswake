---
phase: 51
plan: "02"
type: implementation
wave: 2
depends_on:
  - 51-01
files_modified:
  - lib/crosswake/support_matrix/renderer.ex
  - guides/support_matrix.md
  - guides/install.md
  - guides/native_shell.md
  - guides/compatibility.md
  - test/crosswake/support_matrix/renderer_test.exs
  - test/crosswake/guides/release_boundaries_test.exs
autonomous: true
requirements:
  - SUPP-01
  - SUPP-02
must_haves:
  truths:
    - "The public support-matrix guide renders action classes, promotion rules, rebuild posture, and non-claims from canonical truth instead of prose-only tables."
    - "Install, compatibility, and native-shell guidance answer 'Do I need to rebuild?' directly while preserving explicit deferred-scope language."
    - "Public docs never imply StoreKit, Play Billing, full Sigra machinery, Chimeway delivery, or standalone shell packages have shipped."
    - "Decision coverage: D-12, D-13, D-15, D-20, D-23, D-24, D-33, and D-34 are implemented in generated support docs and linked rebuild/promotion guidance."
  artifacts:
    - path: lib/crosswake/support_matrix/renderer.ex
      provides: "Deterministic markdown rendering for action classes, promotion rules, and non-claims"
      contains: "defp action_class_section"
    - path: guides/support_matrix.md
      provides: "Generated support-truth guide with new rebuild and promotion sections"
      contains: "## Promotion Rules"
    - path: test/crosswake/guides/release_boundaries_test.exs
      provides: "Regression coverage for rebuild-first guidance and deferred-scope copy"
      contains: "promotion rules"
  key_links:
    - from: lib/crosswake/support_matrix/renderer.ex
      to: lib/crosswake/support_matrix/support_matrix.ex
      via: "canonical accessors for action classes and promotion rules"
      pattern: "action_classes|promotion_rules|notification_support_truth|companion_support_truth"
    - from: guides/install.md
      to: guides/support_matrix.md
      via: "rebuild and promotion guidance cross-links"
      pattern: "support_matrix\\.md#action-classes|support_matrix\\.md#promotion-rules"
---

<objective>
Render the Phase 51 support-truth contract into public docs without introducing a second source of truth.

Purpose: make the user-facing guidance match the widened v3.6 diagnostic surface and preserve explicit non-claims across rebuild, proof, and promotion language.
Output: updated support-matrix renderer output plus linked install/native-shell/compatibility guidance and regression tests that lock the public copy.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phases/51-support-matrix-and-native-rebuild-truth/51-CONTEXT.md
@.planning/phases/51-support-matrix-and-native-rebuild-truth/51-RESEARCH.md
@.planning/phases/51-support-matrix-and-native-rebuild-truth/51-PATTERNS.md
@.planning/phases/51-support-matrix-and-native-rebuild-truth/51-01-PLAN.md
@lib/crosswake/support_matrix/support_matrix.ex
@lib/crosswake/support_matrix/renderer.ex
@guides/support_matrix.md
@guides/install.md
@guides/native_shell.md
@guides/compatibility.md
@test/crosswake/support_matrix/renderer_test.exs
@test/crosswake/guides/release_boundaries_test.exs

<interfaces>
From `lib/crosswake/support_matrix/renderer.ex`:
- `render/1` already owns the generated markdown for `guides/support_matrix.md`.
- Existing sections are deterministic and table-based; Phase 51 should extend that pattern rather than hand-authoring a second support matrix elsewhere.

From `lib/crosswake/support_matrix/support_matrix.ex` after Plan 51-01:
- `change_classes/1`, `action_classes/0`, `promotion_rules/0`, `notification_support_truth/0`, and `companion_support_truth/0` are the only allowed sources for rebuild and deferred-scope truth.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add failing docs-contract tests for action classes, promotion rules, and non-claims</name>
  <files>test/crosswake/support_matrix/renderer_test.exs, test/crosswake/guides/release_boundaries_test.exs</files>
  <read_first>
    - `test/crosswake/support_matrix/renderer_test.exs`
    - `test/crosswake/guides/release_boundaries_test.exs`
    - `lib/crosswake/support_matrix/renderer.ex`
    - `guides/support_matrix.md`
    - `guides/install.md`
    - `guides/native_shell.md`
    - `guides/compatibility.md`
    - `.planning/phases/51-support-matrix-and-native-rebuild-truth/51-CONTEXT.md`
  </read_first>
  <behavior>
    - Test 1: the generated support-matrix guide contains `## Action Classes`, `## Promotion Rules`, and `## Public Non-Claims And Rough Edges` sections and renders exact machine labels from Plan 51-01.
    - Test 2: `guides/install.md`, `guides/native_shell.md`, and `guides/compatibility.md` all answer the rebuild question directly and link back to the canonical support-matrix action/promotion sections.
    - Test 3: the public copy explicitly names the locked defers from D-22 through D-25 and keeps advisory proof visible per D-26.
  </behavior>
  <action>Extend `test/crosswake/support_matrix/renderer_test.exs` and `test/crosswake/guides/release_boundaries_test.exs` first. In the renderer tests, assert exact section headings `## Action Classes`, `## Promotion Rules`, and `## Public Non-Claims And Rough Edges`; exact action-class labels `docs_only`, `route_manifest`, `compatibility`, `native_shell`, `companion_native`, and `provider_adapter`; and exact promotion-rule claim ids `shell.ios.generated_project`, `shell.android.generated_project`, `notification_token.provider_snapshot`, `purchase_intent.provider.storekit`, and `purchase_intent.provider.play_billing`. In the release-boundaries guide test, assert that `guides/install.md`, `guides/native_shell.md`, and `guides/compatibility.md` contain `Do I need to rebuild?`, `Promotion rules`, and explicit statements that StoreKit/Play Billing adapters are not shipped in v3.6, Sigra remains contract-only, notification-token readiness is provider-snapshot only, and standalone public shell packages are deferred. Keep the tests source-based; do not call `mix crosswake.inspect` or `mix crosswake.doctor` in this plan.</action>
  <acceptance_criteria>
    - `renderer_test.exs` asserts the three new support-matrix headings plus exact action-class and promotion-rule labels.
    - `release_boundaries_test.exs` asserts exact deferred-scope phrases for StoreKit/Play Billing, Sigra, notification delivery, and shell packages.
    - The new tests fail before any renderer or guide changes are made.
  </acceptance_criteria>
  <verify>
    <automated>mix test test/crosswake/support_matrix/renderer_test.exs test/crosswake/guides/release_boundaries_test.exs</automated>
  </verify>
  <done>Docs-contract tests define the exact public wording and guide sections Phase 51 must ship.</done>
</task>

<task type="auto">
  <name>Task 2: Render canonical rebuild and promotion truth into support guides</name>
  <files>lib/crosswake/support_matrix/renderer.ex, guides/support_matrix.md, guides/install.md, guides/native_shell.md, guides/compatibility.md, test/crosswake/support_matrix/renderer_test.exs, test/crosswake/guides/release_boundaries_test.exs</files>
  <read_first>
    - `lib/crosswake/support_matrix/renderer.ex`
    - `guides/support_matrix.md`
    - `guides/install.md`
    - `guides/native_shell.md`
    - `guides/compatibility.md`
    - `test/crosswake/support_matrix/renderer_test.exs`
    - `test/crosswake/guides/release_boundaries_test.exs`
    - `lib/crosswake/support_matrix/support_matrix.ex`
  </read_first>
  <action>Update `lib/crosswake/support_matrix/renderer.ex` per D-09 through D-27 by appending three deterministic sections after `change_class_section/1`: `action_class_section(Crosswake.SupportMatrix.action_classes())`, `promotion_rule_section(Crosswake.SupportMatrix.promotion_rules())`, and `public_non_claims_section/0`. Render the Action Classes table with exact columns `action_class`, `subject`, `required_action`, `rebuild_required`, `reason`, and `guide_anchor`. Render the Promotion Rules table with exact columns `claim_id`, `current_state`, `promotes_to`, `evidence_class`, `required_evidence`, `minimum_consecutive_passes`, `freshness_window`, `failure_budget`, `required_platforms`, `required_docs_anchors`, `change_class`, `action_class`, `check_ids`, and `demotion_trigger`. Make `guides/support_matrix.md` generated output include an explicit bullet section naming the Phase 51 non-claims: StoreKit and Play Billing adapters are not shipped in v3.6; Sigra is contract-only for route predicates and `:step_up_required`; notification-token readiness is provider-snapshot only and not delivery support; standalone public shell packages are deferred. Then update `guides/install.md`, `guides/native_shell.md`, and `guides/compatibility.md` so each contains a short `Do I need to rebuild?` or `Promotion rules` subsection that links to `guides/support_matrix.md#action-classes` and `guides/support_matrix.md#promotion-rules`, repeats the four change classes from D-10, and explains that compatibility-window narrowing is distinct from a native rebuild per D-14. Keep all claims narrow and do not invent 'production-ready', 'healthy', or similar synonym labels.</action>
  <acceptance_criteria>
    - `guides/support_matrix.md` is regenerated through `Crosswake.SupportMatrix.Renderer.render/1` and contains Action Classes, Promotion Rules, and Public Non-Claims sections.
    - `guides/install.md`, `guides/native_shell.md`, and `guides/compatibility.md` each contain cross-links to the canonical action/promotion sections.
    - `mix test test/crosswake/support_matrix/renderer_test.exs test/crosswake/guides/release_boundaries_test.exs` passes without manual guide edits outside the listed files.
  </acceptance_criteria>
  <verify>
    <automated>mix test test/crosswake/support_matrix/renderer_test.exs test/crosswake/guides/release_boundaries_test.exs</automated>
  </verify>
  <done>The public support guides render canonical rebuild and promotion truth and explicitly preserve all locked deferred-scope language.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| canonical support truth -> public guides | Generated docs can accidentally overclaim support if they diverge from the typed support contract. |
| guide cross-links -> rebuild decisions | Rebuild and promotion copy influences operator and adopter behavior even when they never inspect JSON output. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-51-04 | Tampering | `lib/crosswake/support_matrix/renderer.ex` and `guides/*.md` | mitigate | Keep `guides/support_matrix.md` generated from canonical accessors and lock exact headings, labels, and deferred-scope phrases with renderer and release-boundary tests. |
| T-51-05 | Information Disclosure | public support docs | mitigate | Add an explicit non-claims section naming StoreKit/Play Billing, full Sigra machinery, notification delivery, and shell-package defers so omission cannot read as support. |
| T-51-06 | Repudiation | rebuild and promotion guidance | mitigate | Link install/native-shell/compatibility guidance back to canonical action and promotion sections with stable headings and exact change-class wording. |
| T-51-SC | Tampering | npm/pip/cargo installs | accept | No new package-manager installs are allowed in this plan. |
</threat_model>

<verification>
- `mix test test/crosswake/support_matrix/renderer_test.exs test/crosswake/guides/release_boundaries_test.exs`
</verification>

<success_criteria>
- Public docs surface rebuild and promotion truth from the same canonical data model used by runtime diagnostics.
- Rebuild guidance is explicit and cross-linked instead of implied.
- Deferred support claims remain visible and impossible to misread as shipped breadth.
</success_criteria>

<output>
Create `.planning/phases/51-support-matrix-and-native-rebuild-truth/51-02-SUMMARY.md` when done.
</output>
