---
phase: 42-rulestead-in-tree-companion-and-mock-example
plan: 02
type: execute
wave: 2
depends_on: ["42-01"]
files_modified:
  - examples/phoenix_host/lib/crosswake_example/beta_feature_live.ex
  - examples/phoenix_host/lib/crosswake_example/router.ex
  - examples/phoenix_host/lib/crosswake_example/application.ex
  - examples/phoenix_host/config/config.exs
autonomous: true
requirements: [GATE-01, COMP-03]

must_haves:
  truths:
    - "examples/phoenix_host has a /gating scope with one route declaring gated_by: :rulestead and on_unavailable: :deny"
    - "The phoenix_host Application starts Crosswake.Companions.Rulestead.MockFlagSource in its supervisor tree"
    - "phoenix_host config registers Crosswake.Companions.Rulestead in :crosswake, :companions and enables it"
    - "A developer can drive the route through gated/rolling_out/killed by calling MockFlagSource.set_flag/2 at runtime"
  artifacts:
    - path: "examples/phoenix_host/lib/crosswake_example/beta_feature_live.ex"
      provides: "Minimal LiveView reached only when the gate passes"
      contains: "use Phoenix.LiveView"
      min_lines: 10
    - path: "examples/phoenix_host/lib/crosswake_example/router.ex"
      provides: "/gating scope with the gated route"
      contains: "gated_by: :rulestead"
    - path: "examples/phoenix_host/lib/crosswake_example/application.ex"
      provides: "MockFlagSource started in supervisor tree"
      contains: "MockFlagSource"
    - path: "examples/phoenix_host/config/config.exs"
      provides: "companion registration + enablement"
      contains: "Crosswake.Companions.Rulestead"
  key_links:
    - from: "examples/phoenix_host/lib/crosswake_example/router.ex"
      to: "Crosswake.Companions.Rulestead"
      via: "gated_by: :rulestead matching companion_id/0"
      pattern: "gated_by: :rulestead"
    - from: "examples/phoenix_host/lib/crosswake_example/application.ex"
      to: "Crosswake.Companions.Rulestead.MockFlagSource"
      via: "supervisor child spec"
      pattern: "MockFlagSource"
    - from: "examples/phoenix_host/config/config.exs"
      to: "Crosswake.Companions.Rulestead"
      via: "config :crosswake, :companions"
      pattern: "config :crosswake, :companions"
---

<objective>
Wire the Phase 42 rulestead companion into the `examples/phoenix_host` demo app: add a `/gating`
scope with one gated route (`gated_by: :rulestead`, `on_unavailable: :deny`), a minimal
LiveView the route serves when the gate passes, start the MockFlagSource Agent in the host
supervisor, and register + enable the companion in host config.

Purpose: SC#2 requires a real Phoenix host route that exercises the companion across all three
gate states, driven by mutating MockFlagSource at runtime (`set_flag/2`). This is the
developer-facing demonstration that complements the hermetic library proof from Plan 01. It also
exercises GATE-01 (route declares `gated_by` in the DSL) and the manifest binding for that route.

Output:
- `examples/phoenix_host/lib/crosswake_example/beta_feature_live.ex` (new minimal LiveView)
- `examples/phoenix_host/lib/crosswake_example/router.ex` (modified — new `/gating` scope)
- `examples/phoenix_host/lib/crosswake_example/application.ex` (modified — MockFlagSource child)
- `examples/phoenix_host/config/config.exs` (modified — companion registration + enablement)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/42-rulestead-in-tree-companion-and-mock-example/42-CONTEXT.md
@.planning/phases/42-rulestead-in-tree-companion-and-mock-example/42-RESEARCH.md
@.planning/phases/42-rulestead-in-tree-companion-and-mock-example/42-PATTERNS.md
@.planning/phases/42-rulestead-in-tree-companion-and-mock-example/42-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add BetaFeatureLive and the /gating route</name>
  <files>examples/phoenix_host/lib/crosswake_example/beta_feature_live.ex, examples/phoenix_host/lib/crosswake_example/router.ex</files>
  <read_first>
    - examples/phoenix_host/lib/crosswake_example/router.ex (the `/commerce` scope block around line 219 — exact `scope/pipe_through/crosswake_defaults/live` structure to mirror; note flat `CrosswakeExample` namespace, not a sub-namespace)
    - examples/phoenix_host/lib/crosswake_example/paywall_entry_live.ex (minimal LiveView shape: `use Phoenix.LiveView`, `mount/3`, `render/1`)
    - .planning/phases/42-rulestead-in-tree-companion-and-mock-example/42-CONTEXT.md (D-07, D-08, D-09 — single route, `:deny`, `/gating` scope as sibling)
    - .planning/phases/42-rulestead-in-tree-companion-and-mock-example/42-PATTERNS.md (sections "beta_feature_live.ex" and "router.ex (MODIFIED)")
    - .planning/phases/42-rulestead-in-tree-companion-and-mock-example/42-RESEARCH.md (Pattern 6, Pitfall 1, Anti-Patterns — `gated_by` must equal `companion_id`)
  </read_first>
  <action>
    Create `CrosswakeExample.BetaFeatureLive` in `examples/phoenix_host/lib/crosswake_example/beta_feature_live.ex`. Use the flat `CrosswakeExample` namespace to match the existing `PaywallEntryLive` convention (the actual router uses `scope "/commerce", CrosswakeExample do` with top-level view modules — do NOT introduce a `CrosswakeExample.Gating` sub-namespace despite the research draft; match the live codebase). Minimal shell: `use Phoenix.LiveView`, an `@impl true def mount(_params, _session, socket)` returning `{:ok, socket}` (optionally assign `:flag_key, :rulestead`), and an `@impl true def render(assigns)` returning a small `~H` template (heading "Beta Feature" and a short body). No `handle_event`, no PubSub — this view is reached only when the gate passes; gate denial is handled by RouteGate before mount.

    In `router.ex`, add a new `scope "/gating", CrosswakeExample do` block as a sibling to the existing `/commerce` scope (place it just before the final `end` of the router module). Inside: `pipe_through [:browser]` then a `crosswake_defaults runtime: :live_view, offline: :unavailable, security: :standard do` block declaring exactly one route: `live "/beta-feature", BetaFeatureLive, crosswake: [id: "gating-beta-feature", gated_by: :rulestead, on_unavailable: :deny]`.

    CRITICAL (RESEARCH Pitfall 1 / Anti-Patterns): `gated_by:` MUST be `:rulestead` — the atom equals `companion_id/0` from Plan 01, NOT a flag name like `:my_flag`. The doctor resolves `gated_by` against the set of registered companion IDs; using `:my_flag` would emit `gating.flag_reference_unknown :error`. `on_unavailable: :deny` per D-08 (no `:fallback_phoenix` posture this phase).
  </action>
  <verify>
    <automated>cd /Users/jon/projects/crosswake/examples/phoenix_host && mix compile --warnings-as-errors 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `examples/phoenix_host/lib/crosswake_example/beta_feature_live.ex` defines `CrosswakeExample.BetaFeatureLive` with `use Phoenix.LiveView` and a `render/1`
    - `router.ex` contains `scope "/gating", CrosswakeExample do`
    - The new route declares `gated_by: :rulestead` and `on_unavailable: :deny` and `id: "gating-beta-feature"`
    - No occurrence of `gated_by: :my_flag` in the router
    - `cd examples/phoenix_host && mix compile --warnings-as-errors` exits 0
  </acceptance_criteria>
  <done>phoenix_host compiles clean with the new /gating route and BetaFeatureLive; gated_by atom equals the companion_id :rulestead.</done>
</task>

<task type="auto">
  <name>Task 2: Start MockFlagSource and register the companion in host config</name>
  <files>examples/phoenix_host/lib/crosswake_example/application.ex, examples/phoenix_host/config/config.exs</files>
  <read_first>
    - examples/phoenix_host/lib/crosswake_example/application.ex (current supervisor children list to extend)
    - examples/phoenix_host/config/config.exs (current config structure to append to)
    - lib/crosswake/companions/rulestead/mock_flag_source.ex (Plan 01 — confirm the module name and that the bare module form works as a child spec via `use Agent`)
    - .planning/phases/42-rulestead-in-tree-companion-and-mock-example/42-CONTEXT.md (D-05, D-06 — MockFlagSource shipped with library, host starts it; companion registration)
    - .planning/phases/42-rulestead-in-tree-companion-and-mock-example/42-PATTERNS.md (sections "application.ex (MODIFIED)" and "config.exs (MODIFIED)")
    - .planning/phases/42-rulestead-in-tree-companion-and-mock-example/42-RESEARCH.md (Pattern 5, Pitfall 2 — MockFlagSource must start before any child that triggers gate evaluation; Verified doctor registration)
  </read_first>
  <action>
    In `application.ex`, add `Crosswake.Companions.Rulestead.MockFlagSource` as the FIRST entry in the supervisor `children` list (before `{Phoenix.PubSub, ...}` and `CrosswakeExample.Repo`). It must start before any child that could trigger Crosswake route/gate evaluation during init (RESEARCH Pitfall 2). Use the bare module form `Crosswake.Companions.Rulestead.MockFlagSource` (its `use Agent` generates a `child_spec/1`); if compilation/runtime requires explicit args use `{Crosswake.Companions.Rulestead.MockFlagSource, []}`.

    In `config.exs`, append two config blocks: `config :crosswake, :companions, [Crosswake.Companions.Rulestead]` (registers the module in the doctor and RouteGate dispatch loops) and `config :crosswake, :rulestead, %{enabled: true}` (the config map passed to `enabled?/1`; `Application.get_env(:crosswake, :rulestead, %{})` is how the doctor and RouteGate read it — verified in RESEARCH). Both blocks are required: `:companions` registers the module, `:rulestead` enables it.

    Add a brief comment near the config blocks documenting the developer-facing local-dev workflow (D-06): to drive gate states, call `Crosswake.Companions.Rulestead.MockFlagSource.set_flag(:rulestead, :gated)` (or `{:rolling_out, 50}` or `:killed`) in IEx, then visit `/gating/beta-feature`; `set_flag(:rulestead, nil)` or `reset/0` clears.
  </action>
  <verify>
    <automated>cd /Users/jon/projects/crosswake/examples/phoenix_host && mix compile --warnings-as-errors 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `application.ex` `children` list contains `Crosswake.Companions.Rulestead.MockFlagSource` as the first element
    - `config.exs` contains `config :crosswake, :companions, [Crosswake.Companions.Rulestead]`
    - `config.exs` contains `config :crosswake, :rulestead, %{enabled: true}`
    - `cd examples/phoenix_host && mix compile --warnings-as-errors` exits 0
    - Behavior: starting the app supervisor leaves `Process.whereis(Crosswake.Companions.Rulestead.MockFlagSource)` non-nil
  </acceptance_criteria>
  <done>phoenix_host starts MockFlagSource in its supervisor and registers + enables the rulestead companion in config; a developer can drive gate states via set_flag/2 at runtime.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser request → /gating route | external request hits a gated route; RouteGate must fail-closed when the flag denies |
| host config → companion registration | config decides which companions are active in this host; misregistration could leave the route ungated |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-42-02 | Spoofing / mis-binding | route `gated_by` atom | mitigate | `gated_by: :rulestead` must equal `companion_id/0`; verified against the doctor `gating.flag_reference_unknown` resolution (RESEARCH Pitfall 1). Compile + Plan 01 proof confirm the binding resolves |
| T-42-06 | Elevation of Privilege | `/gating` route reachable when companion not started | mitigate | `on_unavailable: :deny` and MockFlagSource started as the FIRST supervisor child guarantee the snapshot exists before gate evaluation (RESEARCH Pitfall 2); fail-closed when source is absent |
| T-42-SC | Tampering | npm/pip/cargo installs | accept | No package-manager installs in this phase — zero new dependencies added to phoenix_host mix.exs (RESEARCH Environment Availability: rulestead intentionally absent) |
</threat_model>

<verification>
- `cd examples/phoenix_host && mix compile --warnings-as-errors` exits 0
- `cd /Users/jon/projects/crosswake && mix test --exclude requires_example_host` exits 0 (no library regression from the Plan 01 proof)
- Manual (per VALIDATION.md): `iex -S mix phx.server` in phoenix_host, `MockFlagSource.set_flag(:rulestead, :gated)` then `:killed` then `{:rolling_out, 50}`, observe `/gating/beta-feature` response across states
</verification>

<success_criteria>
- `examples/phoenix_host` declares one gated route `gated_by: :rulestead`, `on_unavailable: :deny` under a `/gating` scope (GATE-01, SC#2)
- The route's `gated_by` binding is recorded in the compiled manifest at build time (GATE-01/GATE-02 via existing builder)
- MockFlagSource is started by the host Application; companion registered + enabled in config (SC#2)
- All three gate states are reachable by mutating MockFlagSource at runtime (SC#2)
</success_criteria>

<output>
Create `.planning/phases/42-rulestead-in-tree-companion-and-mock-example/42-02-SUMMARY.md` when done
</output>
