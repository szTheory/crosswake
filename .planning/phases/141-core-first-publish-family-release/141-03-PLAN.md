---
phase: 141-core-first-publish-family-release
plan: 03
type: execute
wave: 2
depends_on: ["141-01", "141-02"]
files_modified: []
autonomous: false
requirements: [FAMILY-05, FAMILY-04]
must_haves:
  truths:
    - "The Wave 1 prep (release-as 0.2.0 nudge + ~> 0.2 floors + matrix) is landed on origin/main with all 22 merge-blocking lanes green."
    - "New v17.0 merge-blocking-* lanes are registered required via register_required_checks.sh green-first, with publish-hex-* / clean-room-proof-* deliberately excluded."
    - "Core crosswake 0.2.0 is live on Hex (mix hex.info shows 0.2.0) and hexdocs.pm/crosswake/0.2.0 resolves — BEFORE any companion publishes."
  artifacts: []
  key_links:
    - "boundary PR green → release-please recomputes #25 to 0.2.0 → merging #25 triggers publish-hex → core 0.2.0 on Hex."
    - "core 0.2.0 live → the resolvable target every companion's ~> 0.2 seam needs (enforced strictly by depends_on for 141-04)."
---

<objective>
Land the Wave 1 prep to origin, run the carried ship-gate, then publish **core 0.2.0 first** — the strictly-ordered prerequisite that makes the companion publishes safe (D-141-B, D-141-E). Every publish step here is a human go/no-go gate (autonomous: false); the classifier already enforces this discipline.

Purpose: A companion publish before core 0.2.0 is live fails at `mix compile` (the exact 2026-07-03 sigra `KeyError :code` incident). Core-first ordering is the whole point of this phase. This plan gets the prep onto protected main via a boundary PR, arms branch protection, and drives the root Release PR (#25) to take core live at 0.2.0 — and stops until hexdocs confirms it.

Output: core `crosswake 0.2.0` live on Hex + hexdocs, ship-gate registered.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/141-core-first-publish-family-release/141-CONTEXT.md
@docs/COMPANION-PUBLISH-RUNBOOK.md
@docs/MILESTONE-BOUNDARY-HYGIENE.md
@.github/workflows/release-please.yml
@release-please-config.json
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Land Wave 1 prep to origin/main via boundary PR (no publish)</name>
  <action>Blocking human checkpoint — this is a gate, not automated work. Land the Wave 1 prep commits onto protected main via a boundary PR and get all 22 merge-blocking lanes green. Do NOT publish to Hex and do NOT merge any Release PR in this step. Follow the how-to-verify steps.</action>
  <what-built>Wave 1 prep commits (141-01 release-as 0.2.0 nudge + runbook core-first; 141-02 `~> 0.2` seams + matrix) are ready on local main. Because main is protected and local main runs ahead of origin, they must land via a boundary PR with all 22 merge-blocking lanes green (per docs/MILESTONE-BOUNDARY-HYGIENE.md — this step does NOT publish to Hex; it only lands the prep and gets CI green).</what-built>
  <how-to-verify>
    1. Open a `sync/main-141-prep` PR carrying the Wave 1 commits (or fold into the existing boundary flow). Do NOT merge any Release PR yet.
    2. Confirm all 22 merge-blocking lanes are GREEN on the PR, including `phase132_compat_matrix_drift` (proves the `~> 0.2` mix.exs ⇔ matrix agreement) and `merge-blocking-release-as-staleness` (should be green: the 0.2.0 pin is a fresh one-shot, not stale).
    3. If origin/main advanced mid-CI, bring the PR up to date (strict:true) and re-green.
    4. Merge the boundary PR. Confirm `gh run list --branch main --limit 15` shows the post-merge lanes green on main.
  </how-to-verify>
  <resume-signal>Type "prep-landed" once the boundary PR is merged and main is green, or describe the failing lane.</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Run register_required_checks.sh ship-gate green-first (exclude publish/proof jobs)</name>
  <action>Blocking human checkpoint — admin-only, run after the new lanes are green on main. Preview with DRY_RUN=1, confirm no publish-hex-*/clean-room-proof-* appear, then apply with DRY_RUN=0. Follow the how-to-verify steps.</action>
  <what-built>The carried v16.0/v17.0 merge-blocking lane-registration ship-gate (register_required_checks.sh, FAMILY-04 / D-9). Run green-first AFTER new lanes have gone green on main at least once — never before (a never-green required check freezes every open PR).</what-built>
  <how-to-verify>
    1. Preview: `DRY_RUN=1 script/register_required_checks.sh` — read the desired required_status_checks list. CONFIRM it does NOT contain any `publish-hex-*` or `clean-room-proof-*` job (those are post-merge publish jobs; requiring them is a permanent PR deadlock — hard rule in the runbook).
    2. Apply (repo-admin gh, owner token repo scope): `DRY_RUN=0 script/register_required_checks.sh`. It appends idempotently and leaves enforce_admins/review requirements untouched.
    3. Confirm registration (optionally let required-checks-audit.yml verify). Spot-check via `gh api repos/:owner/:repo/branches/main/protection/required_status_checks` that publish/proof jobs are absent.
  </how-to-verify>
  <resume-signal>Type "ship-gate-registered" once DRY_RUN=0 applied and publish/proof jobs confirmed excluded, or report the issue.</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Publish CORE crosswake 0.2.0 FIRST (irreversible) via root Release PR #25</name>
  <action>Blocking human go/no-go — hex.publish is irreversible after the ~60-minute grace window (D-141-E). Confirm #25 recomputed to 0.2.0, merge it, watch publish-hex to green, and STOP until hexdocs.pm/crosswake/0.2.0 resolves. Do NOT merge any companion Release PR here. Follow the how-to-verify steps.</action>
  <what-built>CORE PUBLISH FIRST (irreversible, D-141-B/E). Merging the root release-please Release PR (#25 — "chore: release main") triggers the `publish-hex` job (release-please.yml ~L92), which checks out the `hex-v0.2.0` tag, verifies `@version "0.2.0"` in mix.exs, runs the test bar, dry-runs, then `mix hex.publish --yes`, and polls hex.pm for 0.2.0.</what-built>
  <how-to-verify>
    1. Confirm release-please has recomputed #25 to **0.2.0** (the 141-01 release-as pin forces this over the pre-major 0.1.3 default). If #25 still shows 0.1.3, the pin did not take — STOP and re-check 141-01 landed. Do not proceed until #25 targets 0.2.0.
    2. Confirm #25 is green on all 22 lanes and up to date with main.
    3. Merge #25. Watch the `publish-hex` job to green (compile → tests → dry-run → publish → hex.pm verify). Hex is irreversible after ~60 min (retire, not delete).
    4. Verify core is live: `mix hex.info crosswake` shows 0.2.0, AND open `https://hexdocs.pm/crosswake/0.2.0` and confirm it resolves (hexdocs lags a minute). If hexdocs never resolves, treat as failure and investigate before any companion.
    5. Merge the auto-opened **core** `release-as-cleanup` PR (the `release-as-cleanup` job — release-please.yml ~L1202 — fires for the `.` component too, stripping the `release-as: "0.2.0"` pin added in 141-01). Confirm `merge-blocking-release-as-staleness` goes green on main before proceeding to 141-04. This is the removal vehicle for the core one-shot pin — do not hand-edit the config.
    6. Do NOT merge ANY companion Release PR in this plan — that is 141-04, gated on this being live.
    7. If `release-failure-alert` fires, STOP and investigate the named job before continuing.
  </how-to-verify>
  <resume-signal>Type "core-live-0.2.0" once `mix hex.info crosswake` shows 0.2.0, hexdocs.pm/crosswake/0.2.0 resolves, and the core release-as-cleanup PR is merged (staleness gate green), or report the failure.</resume-signal>
</task>

</tasks>

<verification>
- Boundary PR merged; main green on all 22 lanes.
- `required_status_checks` includes new v17.0 merge-blocking-* lanes and EXCLUDES publish-hex-*/clean-room-proof-*.
- `mix hex.info crosswake` → 0.2.0; `https://hexdocs.pm/crosswake/0.2.0` resolves.
- No companion Release PR merged in this plan.
</verification>

<success_criteria>
Core crosswake 0.2.0 is live and resolvable on Hex + hexdocs, the ship-gate is armed (publish/proof jobs excluded), and the family is now unblocked for the strictly-ordered companion publishes in 141-04.
</success_criteria>

<output>
Create `.planning/phases/141-core-first-publish-family-release/141-03-SUMMARY.md` when done.
</output>
