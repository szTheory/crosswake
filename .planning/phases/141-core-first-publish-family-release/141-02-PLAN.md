---
phase: 141-core-first-publish-family-release
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/crosswake_sigra/mix.exs
  - packages/crosswake_chimeway/mix.exs
  - packages/crosswake_threadline/mix.exs
  - guides/companion_compatibility.md
autonomous: true
requirements: [FAMILY-05]
must_haves:
  truths:
    - "Each v17.0 companion's publish-seam Hex requirement is `~> 0.2`, so a clean-room resolve compiles it against core 0.2.0, not 0.1.2."
    - "The compat-matrix 'Requires crosswake' cell for sigra/chimeway/threadline reads `~> 0.2` and the drift test stays green."
    - "rulestead and rindle remain at `~> 0.1` (out of scope, D-141-D)."
  artifacts:
    - packages/crosswake_sigra/mix.exs
    - packages/crosswake_chimeway/mix.exs
    - packages/crosswake_threadline/mix.exs
    - guides/companion_compatibility.md
  key_links:
    - "crosswake_dep/0 `~> 0.2` literal → the exact string the phase132 drift test AST-extracts and matches against the doc cell."
    - "compat matrix Requires-crosswake cell ⇔ mix.exs literal (bidirectional drift test, merge-blocking)."
---

<objective>
Raise the three v17.0 companion `crosswake_dep()` publish-seam floors `~> 0.1` → `~> 0.2` (D-141-C) and update the compat-matrix "Requires crosswake" cells to match, so the "Requires crosswake >= 0.2.0" floor is honest and a downstream resolve can never compile a companion against a too-old core. rulestead/rindle stay `~> 0.1` (D-141-D — they use only 0.1.2-era `Finding` fields and are out of scope).

Purpose: This is the code change that reconciles the publish seam with core 0.2.0. It also forces release-please to open fresh companion Release PRs (a source change on each companion package), which is exactly what Wave 2 needs to re-cut after the failed 0.1.0 attempt.

Output: three bumped mix.exs seams + three updated matrix rows, with the merge-blocking drift test (`phase132_compat_matrix_drift_test.exs`) green.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/141-core-first-publish-family-release/141-CONTEXT.md
@packages/crosswake_sigra/mix.exs
@guides/companion_compatibility.md
@test/crosswake/proof/phase132_compat_matrix_drift_test.exs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Bump the three companion crosswake_dep() seams to ~> 0.2</name>
  <files>packages/crosswake_sigra/mix.exs, packages/crosswake_chimeway/mix.exs, packages/crosswake_threadline/mix.exs</files>
  <action>In each of the three companion `mix.exs` files, change the `crosswake_dep/0` publish branch from `{:crosswake, "~> 0.1"}` to `{:crosswake, "~> 0.2"}` (the `do:` branch taken when `CROSSWAKE_RELEASE == "1"`). Leave the `else:` path-dep branch (`{:crosswake, path: "../.."}`) untouched — local/in-tree CI keeps testing against local core. Do NOT touch `crosswake_rulestead/mix.exs` or `crosswake_rindle/mix.exs` (D-141-D: v16.0 companions use only 0.1.2-era Finding fields, stay `~> 0.1`, publish separately if at all). Do NOT bump the companion `@version` (that is release-please's job via its manifest/release-as, not a manual edit). The AST extractor in the drift test reads the `do:` binary literal of the `if` inside `crosswake_dep/0`, so the exact `~> 0.2` string here is what the doc cell must match in Task 2.</action>
  <verify>
    <automated>for p in sigra chimeway threadline; do grep -q 'do: {:crosswake, "~> 0.2"}' "packages/crosswake_$p/mix.exs" || { echo "FAIL: $p not ~> 0.2"; exit 1; }; done; for p in rulestead rindle; do grep -q 'crosswake, "~> 0.1"' "packages/crosswake_$p/mix.exs" || { echo "FAIL: $p floor changed"; exit 1; }; done; echo OK</automated>
  </verify>
  <done>sigra/chimeway/threadline `crosswake_dep/0` publish branch is `{:crosswake, "~> 0.2"}`; rulestead/rindle remain `~> 0.1`; path-dep branches and @version untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Update compat matrix cells + reconcile the requirement-syntax prose</name>
  <files>guides/companion_compatibility.md</files>
  <action>In the matrix table, change the "Requires `crosswake`" cell from `~> 0.1` to `~> 0.2` for exactly the three rows: `crosswake_sigra`, `crosswake_chimeway`, `crosswake_threadline`. Leave the `crosswake_rulestead` and `crosswake_rindle` rows at `~> 0.1` (D-141-D). Do NOT change the `Current Version` cells (they stay `unpublished` until each package's first Hex release lands — the human writes the confirmed number back post-publish; the drift test's format guard only accepts `unpublished` or a bare backticked semver). Then reconcile the now-inaccurate explanatory prose so the guide is honest that TWO floors coexist: the "Reading the Requirement Syntax" section currently says `~> 0.1` "is the only requirement form in the matrix today" and predicts `~> 0.2` as hypothetical — update it to state both `~> 0.1` (rulestead/rindle) and `~> 0.2` (v17.0 companions) are now live forms, and that `~> 0.2` means `>= 0.2.0 and < 1.0.0`; and adjust the "Independent Versioning" example prose if it implies every companion accepts core 0.1.2. Keep the HTML-comment column contract and header row exactly as-is (the drift test keys column indices off them).</action>
  <verify>
    <automated>MIX_ENV=test mix test test/crosswake/proof/phase132_compat_matrix_drift_test.exs --exclude requires_example_host 2>&1 | grep -qE '0 failures' && echo OK</automated>
  </verify>
  <done>The three v17.0 companion rows read `~> 0.2`, rulestead/rindle read `~> 0.1`, prose reflects the two coexisting floors, and the merge-blocking `phase132_compat_matrix_drift_test.exs` passes (0 failures) — proving mix.exs literals and doc cells agree bidirectionally.</done>
</task>

</tasks>

<verification>
- `grep 'do: {:crosswake, "~> 0.2"}' packages/crosswake_{sigra,chimeway,threadline}/mix.exs` → 3 hits.
- rulestead/rindle seams still `~> 0.1`.
- `mix test test/crosswake/proof/phase132_compat_matrix_drift_test.exs` → 0 failures (drift green in both directions).
- No workflow YAML, no release-please config, no @version touched.
</verification>

<success_criteria>
The publish seam and the compat matrix both declare `>= 0.2.0` for the three v17.0 companions, drift-tested green — so a clean-room install resolves them against core 0.2.0.
</success_criteria>

<output>
Create `.planning/phases/141-core-first-publish-family-release/141-02-SUMMARY.md` when done.
</output>
