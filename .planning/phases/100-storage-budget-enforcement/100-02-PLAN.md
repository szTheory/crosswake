---
phase: 100
plan: 02
type: execute
wave: 2
depends_on: ["100-01"]
files_modified:
  - examples/phoenix_host/priv/static/offline_study.js
  - examples/phoenix_host/e2e/offline_storage.spec.ts
autonomous: true
requirements: [BDGT-02, BDGT-03]
must_haves:
  truths:
    - "The web client explicitly checks `navigator.storage.estimate()` before allowing operations."
    - "A brand-aligned warning banner is displayed if remaining storage is less than 5MB."
    - "The UI gracefully handles `QuotaExceededError` from IndexedDB by displaying a read-only error state instead of crashing."
  artifacts:
    - path: "examples/phoenix_host/priv/static/offline_study.js"
      provides: "Storage limit checking and error handling"
    - path: "examples/phoenix_host/e2e/offline_storage.spec.ts"
      provides: "E2E verification of quota errors"
  key_links:
    - from: "examples/phoenix_host/priv/static/offline_study.js"
      to: "navigator.storage"
      via: "estimate() API call"
      pattern: "navigator\\.storage\\.estimate"
---

<objective>
Implement proactive storage limits checks and graceful `QuotaExceededError` handling in the offline JavaScript client to avoid silent browser evictions.

Purpose: Prevent users from entering an offline session they cannot save to due to strict browser storage quotas. If quotas are hit mid-session, degrade gracefully.
Output: A hardened offline study JS controller and Playwright E2E tests validating the behavior.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/100-storage-budget-enforcement/100-RESEARCH.md
@.planning/phases/100-storage-budget-enforcement/100-CONTEXT.md
@.planning/phases/100-storage-budget-enforcement/100-UI-SPEC.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add E2E Tests for Storage Limits</name>
  <files>examples/phoenix_host/e2e/offline_storage.spec.ts</files>
  <behavior>
    - Test 1: "shows warning banner when storage is near limit" - mocks `navigator.storage.estimate` to return low remaining bytes and verifies banner presence with specific text from UI spec.
    - Test 2: "handles QuotaExceededError gracefully" - mocks IndexedDB to throw `QuotaExceededError` during `queueMutation`, verifying UI transitions to a read-only state.
  </behavior>
  <action>
    Create the Playwright test file `examples/phoenix_host/e2e/offline_storage.spec.ts`.
    Write tests that visit the offline study page (`/offline_html` or equivalent mocked route) and use `page.addInitScript` to mock `navigator.storage.estimate` and the `indexedDB` APIs to simulate quota constraints.
  </action>
  <verify>
    <automated>cd examples/phoenix_host && npx playwright test e2e/offline_storage.spec.ts || true</automated>
  </verify>
  <done>Failing tests exist defining the expected UI behavior for storage limits.</done>
</task>

<task type="auto">
  <name>Task 2: Proactive Storage Limit Checking</name>
  <files>examples/phoenix_host/priv/static/offline_study.js</files>
  <action>
    Implement a `checkStorageLimits()` function that calls `navigator.storage.estimate()`. 
    Check if the remaining space is less than 5_000_000 bytes (5MB). 
    If so, inject a warning banner into the top of the `#flashcard-container` or `body`.
    - Text: "Storage near limit. Clear old sessions to download more." (from UI spec)
    - Style: Use the Accent color `#C98A2E` and a non-intrusive layout.
    Call `checkStorageLimits()` during `DOMContentLoaded` initialization. Add feature detection `if (navigator.storage && navigator.storage.estimate)`.
  </action>
  <verify>
    <automated>cd examples/phoenix_host && npx playwright test e2e/offline_storage.spec.ts -g "shows warning banner"</automated>
  </verify>
  <done>Warning banner shows when space is strictly limited.</done>
</task>

<task type="auto">
  <name>Task 3: Graceful IndexedDB Error Handling</name>
  <files>examples/phoenix_host/priv/static/offline_study.js</files>
  <action>
    Update `queueMutation` and `seedDummyCards` to cleanly catch quota errors.
    1. Attach an `onabort` handler to the transaction (`tx.onabort`).
    2. Check if `tx.error` or `tx.error.name === 'QuotaExceededError'`. Reject the promise with this error.
    3. In `handleReview()`, catch this error. If it is a quota error, update the `#status` element or inject an error badge with:
       - Text: "Storage full. Progress won't be saved." (from UI spec)
       - Style: Use the Destructive color `#9A4D35` (e.g., text color or background badge).
       - Disable further modifications (disable buttons or stop advancing cards) to establish a "Read-only" mode.
  </action>
  <verify>
    <automated>cd examples/phoenix_host && npx playwright test e2e/offline_storage.spec.ts</automated>
  </verify>
  <done>IndexedDB failures correctly transition the app into a read-only visual state rather than crashing silently.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| IndexedDB -> Application | OS-level quota limits restrict application behavior, untrusted environment limits |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-100-03 | Denial of Service | Local Storage | mitigate | Application gracefully degrades to a read-only mode to prevent a hard crash or infinite retry loops when storage is exhausted. |
| T-100-04 | Tampering | DOM Manipulation | mitigate | `textContent` is used (or equivalent safe manipulation) when displaying status messages rather than `innerHTML` of untrusted inputs. |
</threat_model>

<verification>
- `offline_storage.spec.ts` tests pass.
- No unhandled promise rejections occur during a simulated `QuotaExceededError`.
</verification>

<success_criteria>
- The JS client explicitly estimates storage limits and acts on them.
- Full storage yields a branded, read-only UI experience.
</success_criteria>

<output>
Create `.planning/phases/100-storage-budget-enforcement/100-02-SUMMARY.md` when done
</output>