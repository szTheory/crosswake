---
phase: 100
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/crosswake/offline/contracts.ex
  - test/crosswake/offline/contracts_test.exs
  - examples/phoenix_host/lib/crosswake_example_web/controllers/offline_controller.ex
  - examples/phoenix_host/lib/crosswake_example_web/controllers/offline_html/index.html.heex
autonomous: true
requirements: [BDGT-01]
must_haves:
  truths:
    - "Elixir developers can configure offline storage budgets using ergonomic tuples like `{:mb, 50}`."
    - "The `StudySessionIsland` contract stores `:storage_budget` strictly in bytes for native web API alignment."
    - "The web client receives the storage budget via an HTML dataset attribute."
  artifacts:
    - path: "lib/crosswake/offline/contracts.ex"
      provides: "Budget parsing and storage"
    - path: "examples/phoenix_host/lib/crosswake_example_web/controllers/offline_html/index.html.heex"
      provides: "Budget dataset attribute"
  key_links:
    - from: "examples/phoenix_host/lib/crosswake_example_web/controllers/offline_html/index.html.heex"
      to: "offline_study.js"
      via: "data-storage-budget attribute on body or container"
      pattern: "data-storage-budget="
---

<objective>
Implement the `:storage_budget` attribute on the `StudySessionIsland` contract in Elixir and inject it into the offline HTML layout for the frontend to consume.

Purpose: We need an unambiguous source of truth for the offline storage budget (in bytes) that aligns perfectly with browser `navigator.storage` APIs while keeping the Elixir developer experience ergonomic.
Output: `StudySessionIsland` structurally validates budgets and the Phoenix example host passes this configuration to the JS client.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/100-storage-budget-enforcement/100-RESEARCH.md
@.planning/phases/100-storage-budget-enforcement/100-CONTEXT.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add `:storage_budget` to `StudySessionIsland`</name>
  <files>lib/crosswake/offline/contracts.ex, test/crosswake/offline/contracts_test.exs</files>
  <behavior>
    - Test 1: `new_study_session_island/2` parses `{:mb, 50}` into `50_000_000` (bytes).
    - Test 2: `new_study_session_island/2` parses `{:gb, 1}` into `1_000_000_000` (bytes).
    - Test 3: `new_study_session_island/2` accepts raw integer bytes directly.
    - Test 4: Passing an invalid tuple or negative integer raises an error.
  </behavior>
  <action>
    Update `StudySessionIsland` in `lib/crosswake/offline/contracts.ex`:
    1. Add `:storage_budget` to `@enforce_keys` and `defstruct`.
    2. Add private helper `parse_storage_budget/1` that pattern matches:
       - `bytes` when `is_integer(bytes) and bytes >= 0` -> `bytes`
       - `{:mb, mb}` when `is_integer(mb) and mb >= 0` -> `mb * 1_000_000`
       - `{:gb, gb}` when `is_integer(gb) and gb >= 0` -> `gb * 1_000_000_000`
       - anything else should raise or fall back to an error (or use `Keyword.fetch!` naturally if required).
    3. Use the helper inside `new_study_session_island/2` to populate `:storage_budget`.
  </action>
  <verify>
    <automated>mix test test/crosswake/offline/contracts_test.exs</automated>
  </verify>
  <done>`:storage_budget` is an enforced struct attribute parsing tuples to bytes.</done>
</task>

<task type="auto">
  <name>Task 2: Inject `:storage_budget` into Offline Layout</name>
  <files>
    examples/phoenix_host/lib/crosswake_example_web/controllers/offline_controller.ex,
    examples/phoenix_host/lib/crosswake_example_web/controllers/offline_html/index.html.heex
  </files>
  <action>
    1. In `offline_controller.ex`'s `index/2` function, generate a dummy `StudySessionIsland` contract with a budget of `{:mb, 50}` and pass it into the template assigns (e.g., `[island: contract]`). Ensure you include the other required fields like `route_id` and `sync_seam`.
    2. In `index.html.heex`, attach the budget to the DOM so `offline_study.js` can read it. Add `data-storage-budget="<%= @island.storage_budget %>"` to the `body` or a main wrapper `div` like `<div id="offline-island" data-storage-budget="...">`.
  </action>
  <verify>
    <automated>grep -q "data-storage-budget" examples/phoenix_host/lib/crosswake_example_web/controllers/offline_html/index.html.heex</automated>
  </verify>
  <done>The offline HTML layout includes a dataset attribute containing the storage budget in bytes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Elixir -> DOM | Storage budget limits originate from the server and are injected into untrusted browser environments |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-100-01 | Tampering | `:storage_budget` validation | mitigate | Elixir pattern matching ensures only valid integers are accepted into the struct |
| T-100-02 | Information Disclosure | Offline Layout | accept | The storage budget is not sensitive data |
</threat_model>

<verification>
- `contracts_test.exs` fully passes.
- The `index.html.heex` successfully receives and interpolates the budget.
</verification>

<success_criteria>
- The backend enforces budget declarations in bytes.
- The generated HTML carries the constraint payload.
</success_criteria>

<output>
Create `.planning/phases/100-storage-budget-enforcement/100-01-SUMMARY.md` when done
</output>