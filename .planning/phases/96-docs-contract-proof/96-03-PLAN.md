---
phase: 96-docs-contract-proof
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - examples/phoenix_host/lib/crosswake_example/audit/ledger.ex
  - examples/phoenix_host/priv/repo/migrations/20260611000000_create_crosswake_audit_events.exs
  - examples/phoenix_host/test/crosswake_example/threadline/phase96_example_host_ledger_proof_test.exs
  - examples/phoenix_host/mix.exs
  - .github/workflows/phase96-proof-advisory.yml
autonomous: true
requirements: [PROOF-02]
must_haves:
  truths:
    - "examples/phoenix_host commits the mix crosswake.gen.audit output: a CrosswakeExample.Audit.Ledger schema + timestamped migration with all 15 LEDG-02 columns plus a host-optional tier column"
    - "An async:false ExUnit test seeds the ledger via record_in_multi/3 + Repo.transaction, then runs mix crosswake.threadline and asserts the Mix.Shell.Process mailbox shows Posture: Durable and the seeded event"
    - "Cleanup runs via Repo.delete_all in setup (before the body), not on_exit (SQLite serialized-write race)"
    - "Mix.Task.reenable is called before every Mix.Task.run (avoids vacuous :noop pass)"
    - "An advisory workflow runs only on schedule/workflow_dispatch (never on PR/push) and uses no continue-on-error"
  artifacts:
    - path: "examples/phoenix_host/lib/crosswake_example/audit/ledger.ex"
      provides: "Committed gen.audit schema — canonical durable-posture adopter example"
      contains: "defmodule CrosswakeExample.Audit.Ledger"
    - path: "examples/phoenix_host/test/crosswake_example/threadline/phase96_example_host_ledger_proof_test.exs"
      provides: "PROOF-02 Ecto-backed durable-posture proof"
      contains: "Posture: Durable"
    - path: ".github/workflows/phase96-proof-advisory.yml"
      provides: "Advisory example-host lane (schedule/dispatch gated)"
      contains: "advisory"
  key_links:
    - from: "examples/phoenix_host/test/.../phase96_example_host_ledger_proof_test.exs"
      to: "CrosswakeExample.Audit.Ledger.record_in_multi/3"
      via: "Ecto.Multi seed + Repo.transaction"
      pattern: "record_in_multi\\("
    - from: ".github/workflows/phase96-proof-advisory.yml"
      to: "examples/phoenix_host test"
      via: "mix test working-directory examples/phoenix_host"
      pattern: "working-directory: examples/phoenix_host"
---

<objective>
Ship the advisory example-host proof lane (PROOF-02): commit the `mix crosswake.gen.audit` output into `examples/phoenix_host`, write an Ecto-backed ExUnit proof that real `record_in_multi/3` persistence plus `mix crosswake.threadline` reconstruction yields `durable` posture against a seeded SQLite ledger, and gate it in a schedule/dispatch-only advisory workflow.

Purpose: Prove the durable path end-to-end against a real Ecto + SQLite backend, without making it a merge gate (the example host carries pre-existing failures and SQLite has no async sandbox). The committed schema doubles as the canonical durable-posture adopter example (D-04).

Output: a committed schema + migration (gen.audit output with a host-optional `tier` column), the PROOF-02 test, an example-host `test` alias that creates+migrates the SQLite DB, and `.github/workflows/phase96-proof-advisory.yml`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/96-docs-contract-proof/96-CONTEXT.md
@.planning/phases/96-docs-contract-proof/96-RESEARCH.md
@.planning/phases/96-docs-contract-proof/96-PATTERNS.md

<interfaces>
<!-- record_in_multi is arity 3 (RESEARCH A1 — CONTEXT.md said /2 once; the generated template is /3). -->
CrosswakeExample.Audit.Ledger.record_in_multi(multi, name, attrs)  # Ecto.Multi.insert(multi, name, changeset(...))

<!-- The mix crosswake.threadline task groups by `tier` read from event[:tier]/event["tier"]. -->
<!-- tier is NOT a LEDG-02 column (RESEARCH A2 / Open Question 1). Add field :tier, :string as a -->
<!-- host-optional extension column so seeded events land under Native/Bridge/Phoenix, not "Other". -->
@tier_order ["native", "bridge", "phoenix"]   (from lib/mix/tasks/crosswake.threadline.ex)
Durable header printed by the task: "Posture: Durable"

<!-- 15 LEDG-02 columns the generated schema/migration must contain. -->
thread_id, correlation_id, route_id, actor_ref, actor_kind, event_class, event_type,
outcome, provenance (Ecto.Enum :device_claimed|:backend_accepted), occurred_at, recorded_at,
idempotency_key (unique index), metadata, row_hash, prev_hash

<!-- Config keys the threadline task reads (set in test setup, restore on_exit). -->
:crosswake, :audit_repo   => CrosswakeExample.Repo
:crosswake, :audit_ledger => CrosswakeExample.Audit.Ledger

<!-- Pinned-SHA actions (same as merge-blocking lane). -->
actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6
erlef/setup-beam@fc68ffb90438ef2936bbb3251622353b3dcb2f93 # v1
elixir-version: "1.19.5"   otp-version: "27.3"
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Commit gen.audit output (schema + migration) with a host-optional tier column</name>
  <files>examples/phoenix_host/lib/crosswake_example/audit/ledger.ex, examples/phoenix_host/priv/repo/migrations/20260611000000_create_crosswake_audit_events.exs</files>
  <read_first>
    - priv/templates/crosswake/audit/ledger.ex.eex (the exact template — render with app_module=CrosswakeExample; copy the 15-column schema, @forbidden_keys 8-key guard, changeset, record/1, record_in_multi/3 verbatim)
    - priv/templates/crosswake/audit/migration.exs.eex (the exact migration template — copy column defs + unique_index on idempotency_key)
    - examples/phoenix_host/lib/crosswake_example/saas_portal/handoff_audit_event.ex (secondary analog for example-host Ecto schema module shape)
    - examples/phoenix_host/priv/repo/migrations/20260609020457_create_flashcard_cards.exs (existing migration format + the latest committed timestamp 20260609020457 — the new migration must sort AFTER it)
    - lib/mix/tasks/crosswake.gen.audit.ex (confirm app_module derivation: get_app_module camelizes :crosswake_example to CrosswakeExample)
  </read_first>
  <action>
    Produce the committed output of `mix crosswake.gen.audit` for the CrosswakeExample app. Prefer running the generator from examples/phoenix_host (the executor may run `mix crosswake.gen.audit` there and then edit the result); if running it is not clean, hand-render the two templates with app_module=CrosswakeExample.

    Schema file examples/phoenix_host/lib/crosswake_example/audit/ledger.ex: defmodule CrosswakeExample.Audit.Ledger, rendered from ledger.ex.eex with all 15 LEDG-02 fields, provenance as Ecto.Enum values [:device_claimed, :backend_accepted], the 8-key @forbidden_keys ledger PII guard, the changeset/2 with reject_pii_in_metadata/1, record/1, and record_in_multi/3 (multi, name, attrs) — verbatim from the template. THEN add one host-optional extension field: field :tier, :string (RESEARCH A2 / Open Question 1 — the threadline task groups by tier; without this column seeded events fall into the "Other (unrecognized tier)" bucket). Add :tier to the changeset cast list so it is persisted. Add a comment marking :tier as a host extension beyond the LEDG-02 contract, not a canonical column.

    Migration file examples/phoenix_host/priv/repo/migrations/20260611000000_create_crosswake_audit_events.exs: defmodule CrosswakeExample.Repo.Migrations.CreateCrosswakeAuditEvents rendered from migration.exs.eex — create table(:crosswake_audit_events) with all 15 columns (the LEDG-02 columns null: false, metadata/row_hash/prev_hash nullable per template) plus add :tier, :string (nullable — optional host extension), and create unique_index(:crosswake_audit_events, [:idempotency_key]). Use timestamp 20260611000000 (sorts after the latest committed migration 20260609020457).
  </action>
  <verify>
    <automated>cd examples/phoenix_host && mix compile --warnings-as-errors 2>&1 | tail -5; test -f lib/crosswake_example/audit/ledger.ex && test -f priv/repo/migrations/20260611000000_create_crosswake_audit_events.exs && grep -q "field :tier" lib/crosswake_example/audit/ledger.ex && grep -q "def record_in_multi(multi, name, attrs)" lib/crosswake_example/audit/ledger.ex</automated>
  </verify>
  <acceptance_criteria>
    - examples/phoenix_host/lib/crosswake_example/audit/ledger.ex contains defmodule CrosswakeExample.Audit.Ledger and all 15 LEDG-02 field names
    - The schema contains field :provenance, Ecto.Enum, values: [:device_claimed, :backend_accepted]
    - The schema contains def record_in_multi(multi, name, attrs) (arity 3) and def record(attrs) and the 8-key @forbidden_keys guard
    - The schema contains field :tier, :string with :tier in the changeset cast list and a comment marking it a host extension
    - The migration file 20260611000000_create_crosswake_audit_events.exs creates table :crosswake_audit_events with all 15 columns + tier and a unique_index on [:idempotency_key]
    - `cd examples/phoenix_host && mix compile --warnings-as-errors` succeeds
  </acceptance_criteria>
  <done>The gen.audit schema and migration are committed into examples/phoenix_host with the verbatim 15-column LEDG-02 contract plus a host-optional tier column, record_in_multi/3 present, and the example host compiles warnings-clean.</done>
</task>

<task type="auto">
  <name>Task 2: Add a test alias that provisions the SQLite DB, then write the PROOF-02 Ecto-backed test</name>
  <files>examples/phoenix_host/mix.exs, examples/phoenix_host/test/crosswake_example/threadline/phase96_example_host_ledger_proof_test.exs</files>
  <read_first>
    - examples/phoenix_host/mix.exs (no aliases block currently — add one with a test alias so CI provisions the crosswake_audit_events table; the committed crosswake_example.db does not contain the new table)
    - examples/phoenix_host/test/crosswake_example/flashcards_test.exs (Repo.delete_all in setup do — NOT on_exit; async default; alias CrosswakeExample.Repo)
    - test/mix/tasks/crosswake.threadline_test.exs (Application.put_env :audit_repo/:audit_ledger setup + on_exit restore; Mix.Task.reenable before Mix.Task.run)
    - lib/mix/tasks/crosswake.threadline.ex (confirm it prints "Posture: Durable" and groups by tier; confirm it reads :crosswake :audit_repo / :audit_ledger config)
    - examples/phoenix_host/test/test_helper.exs (single line ExUnit.start() — no migrator; provisioning must come from the mix alias or a setup_all)
    - examples/phoenix_host/lib/crosswake_example/audit/ledger.ex (the schema from Task 1 — the test seeds via its record_in_multi/3)
  </read_first>
  <action>
    First, add an aliases/0 to examples/phoenix_host/mix.exs and wire aliases: aliases() into project/0. Add test: ["ecto.create --quiet", "ecto.migrate --quiet", "test"] so a fresh CI checkout creates the SQLite DB and applies all migrations (including the new crosswake_audit_events migration) before tests run. This makes the table exist in CI where the committed .db is absent or stale. Keep ecto_sql in deps (already present) — ecto.create/ecto.migrate are provided by it.

    Then create examples/phoenix_host/test/crosswake_example/threadline/phase96_example_host_ledger_proof_test.exs: defmodule CrosswakeExample.Threadline.Phase96ExampleHostLedgerProofTest using ExUnit.Case, async: false (ecto_sqlite3 has no async sandbox — D-05). alias CrosswakeExample.Repo.

    setup: FIRST line Repo.delete_all(CrosswakeExample.Audit.Ledger) (before the body, not on_exit — SQLite serialized-write race, Pitfall 5). Then capture prev_repo = Application.get_env(:crosswake, :audit_repo) and prev_ledger = Application.get_env(:crosswake, :audit_ledger); Application.put_env(:crosswake, :audit_repo, CrosswakeExample.Repo) and Application.put_env(:crosswake, :audit_ledger, CrosswakeExample.Audit.Ledger). Register on_exit to restore both (put_env back if prev existed, else delete_env). Return :ok.

    Test body: build thread_id = "proof-96-#{System.unique_integer([:positive])}". Seed via Ecto.Multi.new() |> CrosswakeExample.Audit.Ledger.record_in_multi(:audit_event, %{...}) |> Repo.transaction(). The attrs map MUST set every null:false column (thread_id, correlation_id, route_id, actor_ref, actor_kind, event_class, event_type, outcome, provenance: :backend_accepted, occurred_at: DateTime.utc_now(), recorded_at: DateTime.utc_now(), idempotency_key: unique string) AND tier: "phoenix" explicitly (Pitfall 4 — without tier the event lands in the Other bucket). Assert the transaction returns {:ok, _}.

    Then capture the task output via Mix.Shell.Process (NOT capture_io — subtask stdout bleeds through, D-05): Mix.Shell.Process.flush(); original_shell = Mix.shell(); Mix.shell(Mix.Shell.Process); Mix.Task.reenable("crosswake.threadline") (Pitfall 3 — before EVERY run); Mix.Task.run("crosswake.threadline", ["--thread-id", thread_id]); Mix.shell(original_shell).

    Assert the shell mailbox: drain {:mix_shell, :info, [msg]} messages and assert at least one msg =~ "Posture: Durable" AND that the seeded event is reconstructed (assert some msg references the thread's tier label "Phoenix" or the event_type "auth.step_up" / the actor_ref — assert the seeded event is visible, not just the posture header). Use assert_received or a receive loop collecting all :mix_shell messages.
  </action>
  <verify>
    <automated>cd examples/phoenix_host && mix ecto.create --quiet && mix ecto.migrate --quiet && mix test test/crosswake_example/threadline/phase96_example_host_ledger_proof_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - examples/phoenix_host/mix.exs contains an aliases function with test: ["ecto.create --quiet", "ecto.migrate --quiet", "test"] wired into project/0
    - The test file contains defmodule CrosswakeExample.Threadline.Phase96ExampleHostLedgerProofTest using ExUnit.Case, async: false
    - setup calls Repo.delete_all(CrosswakeExample.Audit.Ledger) before the body (not in on_exit)
    - The test seeds via CrosswakeExample.Audit.Ledger.record_in_multi(:audit_event, attrs) inside Ecto.Multi + Repo.transaction, with tier: "phoenix" set explicitly
    - The test calls Mix.Task.reenable("crosswake.threadline") immediately before Mix.Task.run and uses Mix.Shell.Process (not capture_io)
    - The test asserts a shell message contains "Posture: Durable" AND a message referencing the seeded event
    - `cd examples/phoenix_host && mix test test/crosswake_example/threadline/phase96_example_host_ledger_proof_test.exs` exits 0
  </acceptance_criteria>
  <done>The example host has a test alias that provisions the SQLite schema, and the PROOF-02 test seeds a durable event via record_in_multi/3, runs mix crosswake.threadline, and asserts Posture: Durable plus the reconstructed event — green locally.</done>
</task>

<task type="auto">
  <name>Task 3: Create the advisory (schedule/dispatch-gated) workflow</name>
  <files>.github/workflows/phase96-proof-advisory.yml</files>
  <read_first>
    - .github/workflows/phase23-proof.yml (THE advisory-lane analog: schedule + workflow_dispatch, ::notice annotation; NOTE phase23 uses continue-on-error: true which D-06 PROHIBITS — replace it with the job-level if: guard)
    - .github/workflows/phase75-closeout-gate.yml (pinned-SHA setup-beam + checkout + permissions: contents: read)
    - examples/phoenix_host/mix.exs (the test alias from Task 2 — the workflow runs the example host with working-directory)
  </read_first>
  <action>
    Create .github/workflows/phase96-proof-advisory.yml with name: "Phase 96 Proof - Threadline Docs Contract (Advisory)". permissions: contents: read. Triggers: ONLY workflow_dispatch and schedule with cron "0 6 * * 1" (weekly Monday 06:00 UTC). Do NOT add push or pull_request triggers (D-06 — the workflow-level trigger scope is the primary gate; the job-level if: is redundant insurance).

    Single job id advisory-threadline-example-host-ledger-proof. runs-on: ubuntu-latest; timeout-minutes: 15. Add if: ${{ github.event_name == 'schedule' || github.event_name == 'workflow_dispatch' }}. Do NOT use continue-on-error: true (D-06 — it renders failures green in the PR UI; the if: guard prevents PR/push runs entirely instead).

    Steps: (1) Checkout actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6; (2) Setup BEAM erlef/setup-beam@fc68ffb90438ef2936bbb3251622353b3dcb2f93 # v1 elixir 1.19.5 / otp 27.3; (3) mix deps.get with working-directory: examples/phoenix_host; (4) "Run example-host ledger proof" running mix test test/crosswake_example/threadline/phase96_example_host_ledger_proof_test.exs with working-directory: examples/phoenix_host (the test alias provisions ecto.create + ecto.migrate); (5) a final "Advisory lane notice" step with if: always() emitting echo "::notice title=Advisory lane::Example-host Ecto-backed ledger proof is advisory only — failures here do NOT gate merge."
  </action>
  <verify>
    <automated>test -f .github/workflows/phase96-proof-advisory.yml && grep -q "schedule" .github/workflows/phase96-proof-advisory.yml && grep -q "workflow_dispatch" .github/workflows/phase96-proof-advisory.yml && ! grep -q "continue-on-error" .github/workflows/phase96-proof-advisory.yml && grep -q "working-directory: examples/phoenix_host" .github/workflows/phase96-proof-advisory.yml && ! grep -qE "^\s*(push|pull_request):" .github/workflows/phase96-proof-advisory.yml</automated>
  </verify>
  <acceptance_criteria>
    - .github/workflows/phase96-proof-advisory.yml exists with name "Phase 96 Proof - Threadline Docs Contract (Advisory)"
    - Triggers are ONLY workflow_dispatch and schedule (cron "0 6 * * 1"); no push/pull_request triggers
    - The job has if: ${{ github.event_name == 'schedule' || github.event_name == 'workflow_dispatch' }} and does NOT use continue-on-error
    - The workflow pins actions/checkout and erlef/setup-beam to the same SHAs as the merge-blocking lane (elixir 1.19.5 / otp 27.3)
    - The example-host test step uses working-directory: examples/phoenix_host
    - A final ::notice step marks the lane advisory-only
  </acceptance_criteria>
  <done>The advisory workflow runs only on schedule/dispatch, never on PR/push, has no continue-on-error, runs the example-host PROOF-02 test via the provisioning test alias, and emits an advisory ::notice — matching D-06.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| example-host seed → SQLite ledger | Test writes to a real on-disk DB; must not seed PII and must isolate from other example-host tests |
| advisory lane → PR UI | A mis-gated advisory job could render green-on-fail and weaken trust in the merge-blocking lane |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-96-06 | Information disclosure | seeded ledger event attrs | mitigate | Seed uses synthetic opaque values (actor_ref "proof-actor", no email/name/token); the schema's reject_pii_in_metadata/1 guard fails closed on forbidden metadata keys |
| T-96-07 | Tampering | advisory lane false-green | mitigate | No continue-on-error (D-06); job gated to schedule/workflow_dispatch so it never runs on PR/push and cannot render a misleading green check on a PR |
| T-96-08 | Denial of service | SQLite write-lock race in test | mitigate | Repo.delete_all in setup (not on_exit) + async: false avoids the serialized-write race (Pitfall 5) |
| T-96-SC | Tampering | example-host dependency installs | accept | No new packages — ecto_sql/ecto_sqlite3 already in examples/phoenix_host/mix.exs; workflow runs mix deps.get of locked deps only |
</threat_model>

<verification>
- `cd examples/phoenix_host && mix compile --warnings-as-errors` succeeds (schema + migration)
- `cd examples/phoenix_host && mix test test/crosswake_example/threadline/phase96_example_host_ledger_proof_test.exs` exits 0 (durable posture + reconstructed event)
- `.github/workflows/phase96-proof-advisory.yml` runs only on schedule/dispatch, no continue-on-error, no push/pull_request triggers
</verification>

<success_criteria>
- PROOF-02 advisory lane proves real Ecto-backed record_in_multi/3 persistence and durable-posture reconstruction
- The committed schema is a valid gen.audit output usable as the durable-posture adopter example
- The advisory workflow cannot render a false-green check on a PR
</success_criteria>

<output>
Create `.planning/phases/96-docs-contract-proof/96-03-SUMMARY.md` when done
</output>
