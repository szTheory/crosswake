---
phase: 96-docs-contract-proof
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - guides/threadline.md
autonomous: true
requirements: [DOCS-01, DOCS-02, DOCS-03]
must_haves:
  truths:
    - "guides/threadline.md opens with the existing verbatim anchor sentence (unchanged)"
    - "The guide contains a 'What Threadline Is NOT' H2 anti-scope section anchored to REQUIREMENTS.md Out of Scope language"
    - "The guide presents the 15 LEDG-02 ledger columns as a verbatim field/type/meaning table"
    - "The guide presents BOTH forbidden-key lists (20-key telemetry denylist + 8-key ledger PII guard) in separate sections without conflation"
    - "The guide documents the header name, telemetry module/event/metadata names, both task names, and the 'terminal critical events only' scope verbatim"
    - "The guide documents honest limitations: WebView gap, hash-chaining-detects-not-prevents, OTel zero-dependency coexistence"
  artifacts:
    - path: "guides/threadline.md"
      provides: "Restructured contract-first Threadline guide with 10 H2 sections per D-07"
      contains: "What Threadline Is NOT"
      min_lines: 140
  key_links:
    - from: "guides/threadline.md"
      to: "Crosswake.Threadline.Telemetry forbidden_metadata_keys/0"
      via: "verbatim atom names present in PII-Free section"
      pattern: "access_token|refresh_token|session_ref"
    - from: "guides/threadline.md"
      to: "lib/crosswake/audit/ledger.ex defstruct"
      via: "15-column ledger table"
      pattern: "row_hash"
---

<objective>
Restructure the existing 70-line `guides/threadline.md` into the contract-first 10-section guide locked in D-07, so it becomes the honest, mechanically-checkable public contract for Threadline (DOCS-01/02/03). No runtime code changes — this plan only edits one Markdown file.

Purpose: The guide is the single source of truth that the Plan 96-02 hermetic parity test asserts against verbatim. Every contract string this plan writes (header name, telemetry keys, 15 ledger columns, both forbidden lists, task names, anti-scope items, locked microcopy) is a string the parity test will `assert guide =~ "..."` against. Missing or paraphrased strings make the merge-blocking lane red.

Output: A rewritten `guides/threadline.md` (~140-200 lines) with the D-07 H2 outline, verbatim contract content per D-08, the D-09 anti-scope section, and the D-10 locked microcopy.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/96-docs-contract-proof/96-CONTEXT.md
@.planning/phases/96-docs-contract-proof/96-RESEARCH.md
@.planning/phases/96-docs-contract-proof/96-PATTERNS.md

<interfaces>
<!-- Verbatim contract values the guide MUST contain (extracted from shipped source). -->
<!-- Source of each is named so the executor reproduces exact strings, not paraphrases. -->

Header name (from lib/crosswake/plug/threadline.ex NimbleOptions default):
  HTTP form to write in the guide: X-Crosswake-Thread-Id
  (lowercase wire form is "x-crosswake-thread-id"; the parity test derives it via init([]) and matches case-insensitively — but write the human-readable X-Crosswake-Thread-Id form)

Telemetry event names (Crosswake.Threadline.Telemetry.event_names/0):
  [:crosswake, :threadline, :request, :start]
  [:crosswake, :threadline, :request, :stop]
  [:crosswake, :threadline, :request, :exception]

Telemetry metadata keys — allowlist (Crosswake.Threadline.Telemetry.metadata_keys/0), 4 keys:
  thread_id, correlation_id, route_id, source

Telemetry forbidden metadata keys — DENYLIST (Crosswake.Threadline.Telemetry.forbidden_metadata_keys/0), 20 keys.
  Write each prose-form (no leading colon) in the PII-Free section:
  access_token, actor_id, actor_ref, authorization_code, credential_id, device_id,
  email, id_token, ip, nonce, org_id, passkey_credential_id, pkce_verifier,
  provider_payload, raw_return_to, refresh_token, return_to, session_ref,
  subject_ref, user_agent

Ledger PII guard — SEPARATE 8-key list (priv/templates/crosswake/audit/ledger.ex.eex @forbidden_keys):
  email, phone, ip_address, ssn, name, first_name, last_name, address

15 LEDG-02 ledger columns (lib/crosswake/audit/ledger.ex defstruct; frozen contract):
  thread_id, correlation_id, route_id, actor_ref, actor_kind, event_class, event_type,
  outcome, provenance, occurred_at, recorded_at, idempotency_key, metadata, row_hash, prev_hash
  provenance type: Ecto.Enum, values :device_claimed | :backend_accepted
  idempotency_key: unique index on crosswake_audit_events

Mix task names: mix crosswake.gen.audit, mix crosswake.threadline

Doctor finding codes (keep the existing Code|Severity|Meaning table verbatim — lines 55-61 of current guide):
  threadline.plug_missing (advisory), threadline.ledger_not_configured (advisory),
  threadline.pii_forbidden_field_present (error), threadline.ledger_schema_drift (warning),
  threadline.ledger_schema_invalid (advisory)

Vocabulary required: host-owned, actor_ref, append-only, device-claimed, backend-accepted
Banned words for the record_in_multi path narrative: magic, seamless, universal, automatically
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restructure guides/threadline.md to the D-07 contract-first outline</name>
  <files>guides/threadline.md</files>
  <read_first>
    - guides/threadline.md (current 70-line content — opening sentence is a keep-verbatim anchor; the Doctor findings table at lines 55-61 must survive intact)
    - guides/companions.md (house-style precedent: "Core Contract First" ordering, plain-section voice, "What This Guide Does Not Claim" mid-guide H2)
    - .planning/REQUIREMENTS.md lines 55-69 (the Out of Scope table — the anti-scope section item language must anchor to these exact phrasings: "APM", "OpenTelemetry replacement", "Logging framework", "Generic plugin / event bus", "PII in the audit ledger", "Full-session replay")
    - lib/crosswake/threadline/telemetry.ex (confirm metadata_keys/0, forbidden_metadata_keys/0, event_names/0 values match the interfaces block)
    - priv/templates/crosswake/audit/ledger.ex.eex (confirm the 8-key @forbidden_keys ledger PII guard and the 15-column schema)
  </read_first>
  <action>
    Rewrite guides/threadline.md as ten H2 sections in this exact order (D-07): (1) "What Threadline Is", (2) "What Threadline Is NOT", (3) "The Propagation Contract", (4) "Posture: Ephemeral vs Durable", (5) "The Audit Ledger Schema (LEDG-02)", (6) "PII-Free by Construction", (7) "Operations", (8) "Doctor Findings", (9) "Honest Limitations", (10) "Deferred Non-Claims".

    Keep the current opening sentence VERBATIM as the first prose under "What Threadline Is": "Threadline is Crosswake's honest, PII-free correlation thread across the three tiers of a Crosswake application: Native -> Bridge -> Phoenix." (it is a contains-exact anchor used by support_matrix_test.exs). Do NOT rename the file.

    Section 3 "The Propagation Contract" must contain: the header name X-Crosswake-Thread-Id; the three telemetry event names from the interfaces block; the four allowlisted telemetry metadata keys (thread_id, correlation_id, route_id, source); the module names Crosswake.Plug.Threadline and Crosswake.Live.Threadline; and the _crosswake_thread_id LiveView connect-param path. Use vocabulary host-owned and actor_ref; do not write "user id".

    Section 5 "The Audit Ledger Schema (LEDG-02)" must present the 15 columns as a verbatim `| field | type | meaning |` Markdown table (D-08), with provenance typed as Ecto.Enum :device_claimed | :backend_accepted and idempotency_key noted as a unique index. Use the exact 15 column names from the interfaces block. Mention mix crosswake.gen.audit as the scaffold task and that the ledger is host-owned and append-only (no update/delete).

    Section 6 "PII-Free by Construction" must present TWO forbidden lists in separate subsections WITHOUT conflating them (D-08, Pitfall 6): first the 20-key telemetry denylist (the forbidden_metadata_keys/0 list — what Plug strips from telemetry metadata), then the 8-key ledger PII guard (email, phone, ip_address, ssn, name, first_name, last_name, address — what the generated reject_pii_in_metadata/1 changeset blocks). Label clearly which list belongs to telemetry and which to the ledger changeset. Write each key prose-form (no leading colon).

    Section 7 "Operations" must document both task names verbatim: mix crosswake.threadline (with --thread-id and --actor-ref) and mix crosswake.gen.audit. Include the terminal-critical scope microcopy verbatim (D-10): "terminal critical events — auth handoffs, commerce receipts, step-up resolutions, route denials — not high-frequency request logging." Banned in the record_in_multi narrative: magic, seamless, universal, automatically.

    Section 8 "Doctor Findings" must keep the existing Code|Severity|Meaning table intact with all five codes (threadline.plug_missing, threadline.ledger_not_configured, threadline.pii_forbidden_field_present, threadline.ledger_schema_drift, threadline.ledger_schema_invalid).

    Section 9 "Honest Limitations" must contain the D-10 locked microcopy VERBATIM: the hash-chain sentence "`row_hash` and `prev_hash` detect gaps and overwrites after the fact. Hash-chaining does not prevent tampering — it reports it." (never use the adjective "secure"); the WebView gap framed as a deliberate scoping truth (WebView WebSocket / fetch / XHR header gap, not a bug, with the _crosswake_thread_id connect-param path for LiveView mounts); and OTel coexistence (standard :telemetry events, zero OTel dependency, host may attach an OTel handler to bridge spans).

    Section 2 "What Threadline Is NOT" (D-09) is a real H2 framed "non-goals by design, not deferred features." Item language must anchor to REQUIREMENTS.md Out of Scope: not an APM, not an OpenTelemetry replacement, not a logging framework (sets Logger.metadata, never emits log lines), not a plugin / event bus, no PII, no session replay. These exact phrasings feed the contains-exact test.

    House style (D-10): plain sections only — no "Note:" or "Tip:" callout boxes.
  </action>
  <verify>
    <automated>test -f guides/threadline.md && grep -q "What Threadline Is NOT" guides/threadline.md && grep -q "row_hash and prev_hash detect gaps" guides/threadline.md && grep -q "terminal critical events" guides/threadline.md && grep -cv '^#' guides/threadline.md | awk '{exit ($1>=120)?0:1}'</automated>
  </verify>
  <acceptance_criteria>
    - guides/threadline.md first prose line under the first H2 is verbatim "Threadline is Crosswake's honest, PII-free correlation thread across the three tiers of a Crosswake application: Native -> Bridge -> Phoenix."
    - `grep -c '^## ' guides/threadline.md` returns 10 (ten H2 sections)
    - guides/threadline.md contains the literal string "What Threadline Is NOT"
    - guides/threadline.md contains all 15 column names: thread_id, correlation_id, route_id, actor_ref, actor_kind, event_class, event_type, outcome, provenance, occurred_at, recorded_at, idempotency_key, metadata, row_hash, prev_hash
    - guides/threadline.md contains all 20 telemetry forbidden keys (access_token … user_agent) AND the 8 ledger PII keys (email, phone, ip_address, ssn, name, first_name, last_name, address) in distinct sections
    - guides/threadline.md contains the verbatim hash-chain sentence "Hash-chaining does not prevent tampering — it reports it." and does NOT contain the word "secure" describing the hash chain
    - guides/threadline.md contains "X-Crosswake-Thread-Id", "mix crosswake.threadline", "mix crosswake.gen.audit", "_crosswake_thread_id", and the terminal-critical microcopy verbatim
    - guides/threadline.md retains all five doctor finding codes in a Code|Severity|Meaning table
    - guides/threadline.md does NOT contain "Note:" or "Tip:" callout boxes, nor the banned words magic/seamless/universal/automatically in the record_in_multi narrative
  </acceptance_criteria>
  <done>The guide is restructured to the 10-section D-07 outline with all verbatim contract content (15 columns, both forbidden lists, header/task/module names, anti-scope items, locked microcopy) present, the opening anchor sentence and doctor table preserved, and the file still named guides/threadline.md.</done>
</task>

<task type="auto">
  <name>Task 2: Re-verify downstream contains-exact anchors did not break</name>
  <files>guides/threadline.md</files>
  <read_first>
    - test/crosswake/support_matrix/support_matrix_test.exs (the docs_anchor assertions ~lines 254-273 assert guides/threadline.md exists and is referenced — restructuring must not rename or remove the file)
    - test/crosswake/doctor/doctor_threadline_test.exs (doctor hint strings cite guides/threadline.md)
  </read_first>
  <action>
    Run the two existing merge-blocking tests that reference guides/threadline.md and confirm the restructure did not break their contains-exact references. These tests are NOT in the Plan 96-02 hermetic file list but they run in other lanes; breaking them would break a different merge gate (Pitfall 7). If either fails because of the restructure (e.g. the docs_anchor sentence anchor or a doctor hint substring moved), adjust guides/threadline.md to restore the broken anchor string — do not edit the tests.
  </action>
  <verify>
    <automated>mix test test/crosswake/support_matrix/support_matrix_test.exs test/crosswake/doctor/doctor_threadline_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - `mix test test/crosswake/support_matrix/support_matrix_test.exs test/crosswake/doctor/doctor_threadline_test.exs` exits 0
    - The "entry docs_anchor file exists in the repository" assertion in support_matrix_test.exs passes
    - No edits were made to either test file (only guides/threadline.md may be adjusted)
  </acceptance_criteria>
  <done>Both pre-existing merge-blocking tests that reference guides/threadline.md pass against the restructured guide; the opening sentence and doctor table anchors remain intact.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| docs → public contract | The guide is read by adopters as the authoritative PII-free posture; inaccurate text could mislead operators about what is persisted |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-96-01 | Information disclosure | guides/threadline.md PII examples | mitigate | Guide lists forbidden-key NAMES only (email, ssn, etc.) as denylist entries — never real PII values or sample tokens; no example payloads containing secrets |
| T-96-02 | Tampering | docs drift vs shipped code | mitigate | Plan 96-02 hermetic parity test asserts every contract string against the shipped public API; drift becomes a merge-blocking CI failure, not a silent doc error |
| T-96-03 | Repudiation | hash-chain overclaim | accept | Guide explicitly states "Hash-chaining does not prevent tampering — it reports it" and never uses "secure"; honesty is the mitigation, no code change |
</threat_model>

<verification>
- `mix test test/crosswake/support_matrix/support_matrix_test.exs test/crosswake/doctor/doctor_threadline_test.exs` passes (anchors preserved)
- `grep -c '^## ' guides/threadline.md` == 10
- All verbatim contract strings from the interfaces block are present (verified exhaustively by the Plan 96-02 parity test in Wave 2)
</verification>

<success_criteria>
- guides/threadline.md follows the D-07 10-section outline with the verbatim opening anchor and intact doctor table
- All DOCS-01 contract strings, the DOCS-02 anti-scope section, and the DOCS-03 honest-limitations microcopy are present verbatim
- No existing merge-blocking test that references the guide is broken
</success_criteria>

<output>
Create `.planning/phases/96-docs-contract-proof/96-01-SUMMARY.md` when done
</output>
