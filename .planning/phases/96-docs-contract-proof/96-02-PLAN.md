---
phase: 96-docs-contract-proof
plan: 02
type: execute
wave: 2
depends_on: ["96-01"]
files_modified:
  - test/crosswake/proof/phase96_threadline_docs_contract_test.exs
  - .github/workflows/phase96-proof.yml
autonomous: true
requirements: [DOCS-01, DOCS-02, DOCS-03, PROOF-01]
must_haves:
  truths:
    - "A hermetic ExUnit proof test asserts every Threadline contract string appears in guides/threadline.md, with a named custom failure message per assertion"
    - "Contract values are code-derived where a public accessor exists (header name, telemetry keys); the 15 ledger columns are hardcoded with a co-location comment"
    - "The proof module carries no @moduletag and no example-host dependency (hermetic lane self-guard)"
    - "A merge-blocking workflow runs the curated hermetic Threadline test file list and fails closed if any file is missing"
    - "The workflow job id is exactly merge-blocking-threadline-docs-contract-proof for branch protection"
  artifacts:
    - path: "test/crosswake/proof/phase96_threadline_docs_contract_test.exs"
      provides: "DOCS-01/02/03 contains-exact parity assertions + hermetic self-guard"
      contains: "Phase96ThreadlineDocsContractTest"
      min_lines: 80
    - path: ".github/workflows/phase96-proof.yml"
      provides: "Merge-blocking hermetic proof lane"
      contains: "merge-blocking-threadline-docs-contract-proof"
  key_links:
    - from: "test/crosswake/proof/phase96_threadline_docs_contract_test.exs"
      to: "guides/threadline.md"
      via: "File.read! + assert guide =~"
      pattern: "File.read!\\(\"guides/threadline.md\"\\)"
    - from: ".github/workflows/phase96-proof.yml"
      to: "test/crosswake/proof/phase96_threadline_docs_contract_test.exs"
      via: "mix test explicit file list"
      pattern: "phase96_threadline_docs_contract_test.exs"
---

<objective>
Ship the hermetic docs-contract parity test (DOCS-01/02/03) and the merge-blocking CI workflow that runs the curated Threadline proof lane (PROOF-01). The test mechanically asserts every contract string appears in the restructured `guides/threadline.md`; the workflow makes that assertion (plus the existing Plug/telemetry/doctor/gen.audit tests) a merge gate — no Ecto, network, or device required.

Purpose: Make guide drift a CI failure instead of a review concern. A renamed telemetry key or a dropped ledger column produces a NAMED test failure telling the maintainer which contract element is missing and which file to fix.

Output: `test/crosswake/proof/phase96_threadline_docs_contract_test.exs` (new) and `.github/workflows/phase96-proof.yml` (new), landing in the same commit (Pitfall 2 — `mix test <nonexistent path>` fails closed).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/96-docs-contract-proof/96-CONTEXT.md
@.planning/phases/96-docs-contract-proof/96-RESEARCH.md
@.planning/phases/96-docs-contract-proof/96-PATTERNS.md
@.planning/phases/96-docs-contract-proof/96-01-SUMMARY.md

<interfaces>
<!-- Public accessors the test calls (code-derived assertions, D-01). -->
Crosswake.Plug.Threadline.init([])[:header_name]   # => "x-crosswake-thread-id"
Crosswake.Threadline.Telemetry.metadata_keys/0      # => [:thread_id, :correlation_id, :route_id, :source]
Crosswake.Threadline.Telemetry.forbidden_metadata_keys/0  # 20 atoms
Crosswake.Threadline.Telemetry.event_names/0        # 3 event lists

<!-- Hardcoded list (no public accessor; LEDG-02 frozen contract, D-01). -->
@canonical_ledger_columns (string form, 15):
  "thread_id", "correlation_id", "route_id", "actor_ref", "actor_kind",
  "event_class", "event_type", "outcome", "provenance", "occurred_at",
  "recorded_at", "idempotency_key", "metadata", "row_hash", "prev_hash"

<!-- Ledger PII guard (8, separate from telemetry denylist) — assert these too. -->
"email", "phone", "ip_address", "ssn", "name", "first_name", "last_name", "address"

<!-- Pinned-SHA actions (phase73/75 pattern, D-02). -->
actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6
erlef/setup-beam@fc68ffb90438ef2936bbb3251622353b3dcb2f93 # v1
elixir-version: "1.19.5"   otp-version: "27.3"
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write the hermetic docs-contract parity test</name>
  <files>test/crosswake/proof/phase96_threadline_docs_contract_test.exs</files>
  <behavior>
    - Hermetic self-guard: reading __ENV__.file, the source contains no @moduletag and no "CrosswakeExample." reference (built via string concatenation so the guard does not match itself)
    - Header name: guides/threadline.md contains the value of Crosswake.Plug.Threadline.init([])[:header_name] (case-insensitive match acceptable since guide writes X-Crosswake-Thread-Id)
    - Telemetry metadata keys: for each key in Telemetry.metadata_keys/0, the guide contains Atom.to_string(key) — one assertion per key with a named failure message
    - Telemetry forbidden keys: for each key in Telemetry.forbidden_metadata_keys/0, the guide contains Atom.to_string(key) — one assertion per key
    - Telemetry event names: the guide contains "crosswake", "threadline", "request" event-name segments (assert the event domain appears)
    - Ledger columns: for each of the 15 hardcoded @canonical_ledger_columns strings, the guide contains it — one assertion per column
    - Ledger PII guard: for each of the 8 ledger PII keys, the guide contains it
    - Anti-scope (DOCS-02): the guide contains "What Threadline Is NOT" and the anti-scope items (APM, OpenTelemetry, logging framework, plugin, session replay)
    - Honest limitations (DOCS-03): the guide contains the verbatim hash-chain microcopy and OTel-coexistence + WebView-gap strings
    - Task/scope names: the guide contains "mix crosswake.threadline", "mix crosswake.gen.audit", and "terminal critical events"
  </behavior>
  <read_first>
    - test/crosswake/proof/phase91_threadline_contract_closeout_test.exs (module structure, async: false, aliasing public API — copy this shape)
    - test/crosswake/proof/phase52_operator_truth_test.exs lines 215-221 (hermetic lane self-guard: refute @moduletag, refute "Crosswake"<>"Example." — keep ONLY these two refutes, drop the phase52-specific MIX_INCLUDE_* checks)
    - test/crosswake/guides/release_boundaries_test.exs (File.read! repo-root-relative path + assert guide =~ pattern)
    - test/crosswake/support_matrix/support_matrix_test.exs lines 267-303 (custom failure message pattern + code-derived parity loop)
    - lib/crosswake/threadline/telemetry.ex (confirm metadata_keys/0, forbidden_metadata_keys/0, event_names/0 are public)
    - lib/crosswake/plug/threadline.ex (confirm init/1 returns a keyword list with :header_name)
    - guides/threadline.md (the restructured guide from Plan 96-01 — the assertion target)
  </read_first>
  <action>
    Create test/crosswake/proof/phase96_threadline_docs_contract_test.exs as defmodule Crosswake.Proof.Phase96ThreadlineDocsContractTest using ExUnit.Case, async: false (proof-lane convention; the test mutates no Application env so async would also work, but match the proof-lane default).

    Read the guide once per test via guide = File.read!("guides/threadline.md") (repo-root-relative — ExUnit runs from repo root; do NOT use Path.join(__DIR__, ...)).

    Implement the hermetic self-guard test FIRST: source = File.read!(__ENV__.file); refute Regex.match?(~r/^\s*@moduletag\s+:/m, source) with a named message; refute String.contains?(source, "Crosswake" <> "Example.") with a named message. Build the "CrosswakeExample." literal via string concatenation so the guard does not flag its own source.

    Header name: header = Crosswake.Plug.Threadline.init([])[:header_name]; assert String.downcase(guide) =~ String.downcase(header) with a message naming the header and the file to update. (Downcase both sides because the guide writes X-Crosswake-Thread-Id and the default is lowercase.)

    Telemetry metadata keys: for key <- Crosswake.Threadline.Telemetry.metadata_keys(), assert guide =~ Atom.to_string(key) with message "guides/threadline.md must document telemetry metadata key '<key>' — add it to the Propagation Contract section".

    Telemetry forbidden keys: for key <- Crosswake.Threadline.Telemetry.forbidden_metadata_keys(), assert guide =~ Atom.to_string(key) with message naming the key and the PII-Free by Construction section.

    Telemetry event names: assert the guide contains "threadline" and "request" (event-name segments). Iterate Telemetry.event_names/0 and assert each segment list's stringified parts appear; at minimum assert guide =~ "threadline" and guide =~ "request".

    Ledger columns: hardcode @canonical_ledger_columns as the 15 strings from the interfaces block with the D-01 co-location comment block (cross-reference @canonical_ledger_columns in lib/crosswake/doctor/doctor.ex; note the deferred canonical_columns/0 extraction). for col <- @canonical_ledger_columns, assert guide =~ col with message naming the column and the "Audit Ledger Schema (LEDG-02)" table.

    Ledger PII guard: hardcode the 8 ledger PII keys ("email","phone","ip_address","ssn","name","first_name","last_name","address") and assert each appears, with a message distinguishing it from the telemetry denylist (Pitfall 6).

    Anti-scope (DOCS-02): assert guide =~ "What Threadline Is NOT" and assert the guide contains anti-scope items "APM", "OpenTelemetry", "logging framework", "plugin", "session replay" — each a separate assertion with a named message.

    Honest limitations (DOCS-03): assert the verbatim hash-chain sentence "Hash-chaining does not prevent tampering — it reports it." appears; assert "zero OTel dependency" coexistence wording (assert "OTel" or "OpenTelemetry" plus "zero" present); assert the WebView gap is mentioned (assert guide =~ "WebView"); assert "_crosswake_thread_id" connect-param path appears.

    Task/scope: assert guide =~ "mix crosswake.threadline", guide =~ "mix crosswake.gen.audit", guide =~ "terminal critical events".

    Every assertion MUST carry a custom failure message naming the missing contract element and the file to update (guides/threadline.md) — this is the primary DX surface of the phase.
  </action>
  <verify>
    <automated>mix test test/crosswake/proof/phase96_threadline_docs_contract_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - `mix test test/crosswake/proof/phase96_threadline_docs_contract_test.exs` exits 0
    - The file contains defmodule Crosswake.Proof.Phase96ThreadlineDocsContractTest
    - The file contains a hermetic self-guard test asserting refute on @moduletag and on the example-host literal
    - The file contains `File.read!("guides/threadline.md")` and `Crosswake.Plug.Threadline.init([])[:header_name]` and `Crosswake.Threadline.Telemetry.metadata_keys()` and `Crosswake.Threadline.Telemetry.forbidden_metadata_keys()`
    - The file hardcodes a 15-element @canonical_ledger_columns list with the co-location comment referencing lib/crosswake/doctor/doctor.ex
    - The file itself carries no @moduletag (`grep -v '^#' test/crosswake/proof/phase96_threadline_docs_contract_test.exs | grep -c '@moduletag'` == 0)
    - Every assert/refute in the file includes a custom failure message string
  </acceptance_criteria>
  <done>The hermetic parity test passes against the restructured guide, asserts every contract element with a named failure message, derives header/telemetry values from public accessors, hardcodes the 15 ledger columns with a co-location comment, and self-guards against @moduletag and example-host dependency.</done>
</task>

<task type="auto">
  <name>Task 2: Create the merge-blocking hermetic proof workflow</name>
  <files>.github/workflows/phase96-proof.yml</files>
  <read_first>
    - .github/workflows/phase75-closeout-gate.yml (cleanest hermetic Elixir-only workflow: permissions contents:read, pinned-SHA checkout + setup-beam, mix deps.get, separate compile-warnings-as-errors step, then run step)
    - .github/workflows/phase73-proof.yml (pinned SHA values, multi-line mix test list shape)
    - .github/workflows/phase90-proof.yml (D-02 trigger shape: push branches ['**'] + pull_request)
    - test/crosswake/proof/phase96_threadline_docs_contract_test.exs (the new test from Task 1 — must be referenced in the mix test file list and exist in the same commit)
  </read_first>
  <action>
    Create .github/workflows/phase96-proof.yml with name: "Phase 96 Proof - Threadline Docs Contract". Set permissions: contents: read at the top. Trigger: push: branches: ['**'] plus pull_request (phase90 shape, D-02).

    Single job with id EXACTLY merge-blocking-threadline-docs-contract-proof (this string is registered in branch protection — do not abbreviate or rename). runs-on: ubuntu-latest; set timeout-minutes (use 15). name: a human-readable display string is fine but the job KEY must be the exact id above.

    Steps in order: (1) Checkout via actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6; (2) Setup BEAM via erlef/setup-beam@fc68ffb90438ef2936bbb3251622353b3dcb2f93 # v1 with elixir-version "1.19.5" and otp-version "27.3"; (3) mix deps.get; (4) a SEPARATE step "Compile (warnings as errors)" running mix compile --warnings-as-errors (do NOT fold --warnings-as-errors into mix test — repo convention, D-02); (5) "Run hermetic threadline docs contract proof" running mix test with the explicit file list below.

    The mix test step lists these files (D-03 baseline set), preceded by a YAML comment block mapping each file to the contract it proves:
      test/crosswake/proof/phase91_threadline_contract_closeout_test.exs
      test/crosswake/proof/phase92_server_propagation_closeout_test.exs
      test/crosswake/proof/phase96_threadline_docs_contract_test.exs
      test/crosswake/plug/threadline_test.exs
      test/crosswake/live/threadline_test.exs
      test/crosswake/threadline/telemetry_test.exs
      test/crosswake/threadline/id_test.exs
      test/crosswake/doctor/doctor_threadline_test.exs
      test/mix/tasks/crosswake.gen.audit_test.exs

    Do NOT include support_matrix_test.exs in the file list UNLESS Task 3 verifies it runs clean in this lane; if Task 3 confirms a clean scoped run, add the scoped path:LINE form there. No tag-based --only selection (phase52/64/65 anti-tag guard, D-03).
  </action>
  <verify>
    <automated>test -f .github/workflows/phase96-proof.yml && grep -q "merge-blocking-threadline-docs-contract-proof" .github/workflows/phase96-proof.yml && grep -q "phase96_threadline_docs_contract_test.exs" .github/workflows/phase96-proof.yml && grep -q "de0fac2e4500dabe0009e67214ff5f5447ce83dd" .github/workflows/phase96-proof.yml</automated>
  </verify>
  <acceptance_criteria>
    - .github/workflows/phase96-proof.yml exists with name "Phase 96 Proof - Threadline Docs Contract"
    - The workflow contains a job whose YAML key is exactly merge-blocking-threadline-docs-contract-proof
    - The workflow triggers on push branches ['**'] and pull_request
    - The workflow sets permissions: contents: read
    - The workflow pins actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd and erlef/setup-beam@fc68ffb90438ef2936bbb3251622353b3dcb2f93 with elixir 1.19.5 / otp 27.3
    - The workflow has a separate "mix compile --warnings-as-errors" step before the mix test step
    - The mix test step lists all 9 baseline files including phase96_threadline_docs_contract_test.exs
    - The mix test step has a comment mapping each file to the contract it proves
  </acceptance_criteria>
  <done>The merge-blocking workflow exists with the exact branch-protection job id, the phase90 trigger shape, pinned-SHA actions, a separate compile-warnings step, and the curated 9-file hermetic test list including the new proof test.</done>
</task>

<task type="auto">
  <name>Task 3: Verify the curated lane runs clean and decide support_matrix inclusion</name>
  <files>.github/workflows/phase96-proof.yml</files>
  <read_first>
    - .github/workflows/phase96-proof.yml (the workflow from Task 2 — the file list to validate)
    - test/crosswake/support_matrix/support_matrix_test.exs (Open Question 2 — the threadline describe block starts ~line 254; D-03 allows path:LINE scoping)
  </read_first>
  <action>
    Run the exact curated file list from the workflow locally to confirm the lane is green end-to-end (the workflow runs what the executor runs here). Then resolve Open Question 2: run the threadline-relevant portion of support_matrix_test.exs scoped by path:LINE (the docs_anchor describe block, ~line 254). If the scoped run is clean (exit 0), append the scoped form test/crosswake/support_matrix/support_matrix_test.exs:254 to the workflow's mix test file list with a mapping comment; if it pulls in pre-existing failures or cannot be cleanly scoped, leave it OUT and add a YAML comment in the workflow explaining the omission (the docs_anchor assertions already run as a merge gate in their own lane). Do not add the full unscoped file — there are ~28 known pre-existing full-suite failures and the curated lane must stay green.
  </action>
  <verify>
    <automated>mix test test/crosswake/proof/phase91_threadline_contract_closeout_test.exs test/crosswake/proof/phase92_server_propagation_closeout_test.exs test/crosswake/proof/phase96_threadline_docs_contract_test.exs test/crosswake/plug/threadline_test.exs test/crosswake/live/threadline_test.exs test/crosswake/threadline/telemetry_test.exs test/crosswake/threadline/id_test.exs test/crosswake/doctor/doctor_threadline_test.exs test/mix/tasks/crosswake.gen.audit_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - The full curated 9-file mix test command exits 0 (lane is green)
    - The scoped run `mix test test/crosswake/support_matrix/support_matrix_test.exs:254` was attempted; its result (clean → added to workflow with comment, or not-clean → omitted with explanatory YAML comment) is recorded in the SUMMARY
    - If support_matrix was added, .github/workflows/phase96-proof.yml contains the path:LINE scoped form (not the bare file path)
  </acceptance_criteria>
  <done>The curated hermetic lane passes locally with all 9 files green, and the support_matrix inclusion decision is resolved with the workflow updated (added scoped, or omitted with a documented reason).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI lane → merge gate | The proof lane is the authority that blocks merge; a weakened or vacuous lane lets drift through |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-96-04 | Tampering | proof lane weakened to false-green | mitigate | Explicit file list fails closed when a file is renamed/missing (Pitfall 2); per-key loops produce named failures, not vacuous passes; hermetic self-guard test forbids @moduletag escape hatches |
| T-96-05 | Elevation of privilege | CI action supply chain | mitigate | All GitHub Actions pinned to commit SHAs (not tags); permissions: contents: read minimizes token scope |
| T-96-SC | Tampering | npm/pip/cargo installs | accept | No package-manager installs in this plan — only mix deps.get of already-locked deps; no new dependencies introduced |
</threat_model>

<verification>
- `mix test` over the curated 9-file list exits 0
- `.github/workflows/phase96-proof.yml` has the exact job id, pinned SHAs, separate compile step, and the new test in its file list
- The new proof test and the workflow are committed together (Pitfall 2)
</verification>

<success_criteria>
- DOCS-01/02/03 are mechanically asserted by a hermetic test that passes
- PROOF-01 merge-blocking lane exists, runs the curated file list, and fails closed on missing files
- support_matrix inclusion is resolved (scoped-in or documented-out)
</success_criteria>

<output>
Create `.planning/phases/96-docs-contract-proof/96-02-SUMMARY.md` when done.
NOTE for the operator: register the check `merge-blocking-threadline-docs-contract-proof` in GitHub branch protection only AFTER this workflow's first completed run — GitHub lists job ids post-run (Pitfall 1).
</output>
