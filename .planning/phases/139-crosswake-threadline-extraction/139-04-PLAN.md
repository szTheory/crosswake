---
phase: 139-crosswake-threadline-extraction
plan: 04
type: execute
wave: 4
depends_on: [139-03]
files_modified: []
autonomous: false
requirements: [THREAD-03]
user_setup:
  - service: hex-release-pr
    why: "Merging the crosswake_threadline Release PR IS the auditable human go/no-go before the irreversible Hex publish of crosswake_threadline (D-8, D-9). threadline is the FINAL companion — it publishes LAST, only AFTER sigra + chimeway are live. Per the milestone plan this irreversible publish is DEFERRED to the batched family publish (at/after Phase 140 close)."
    dashboard_config:
      - task: "Merge the crosswake_threadline Release PR only after dress-rehearsal + hex.publish --dry-run (files: incl priv/, excl test/) + clean-room lane (BOTH siblings absent) are green AND sigra + chimeway are already live on Hex"
        location: "GitHub PR (release-please-generated)"
      - task: "Merge the auto-generated release-as-cleanup PR after the first crosswake_threadline Release PR merges"
        location: "GitHub PR (release-as-cleanup-*)"
must_haves:
  truths:
    - "the full pre-publish gate chain is green: path-dep dress rehearsal → hex.publish --dry-run (files: incl priv/, excl test/) → clean-room lane (crosswake + crosswake_threadline, NEITHER crosswake_sigra NOR crosswake_chimeway)"
    - "the crosswake_threadline Release PR merge triggers the auto publish + post-publish clean-room + release-as cleanup"
    - "crosswake_threadline 0.1.0 is live on Hex as an independent component, published LAST (after sigra AND chimeway — sequential family order); this irreversible publish is DEFERRED to the batched family publish"
    - "the hex.publish --dry-run tarball file list INCLUDES priv/ (the EEX templates) and EXCLUDES test/ (no test artifacts ship)"
  artifacts: []
  prohibitions:
    - "the irreversible hex.publish MUST NOT fire until sigra AND chimeway are live on Hex (threadline is the FINAL companion — sequential family order, D-9)"
    - "MUST NOT auto-advance past either human gate — both merges (Release PR, cleanup PR) are blocking-human go/no-go points"
    - "the dry-run tarball MUST NOT omit priv/ (would silently break mix crosswake.gen.audit for adopters) and MUST NOT include test/"
  key_links:
    - "ordered gate: dry-run (priv/ in, test/ out) → clean-room (both siblings absent) → HUMAN merge Release PR → auto publish → post-publish clean-room → auto cleanup PR → HUMAN merge cleanup PR"
    - "the clean-room lane is the zero-sibling-dep vacuity guard: it installs crosswake + crosswake_threadline with sigra AND chimeway deliberately absent and asserts real module-shipment canaries (Telemetry.event_names/0 == 3, Plug.init/1, Ledger.actor_ref/2) — Plan 03"
    - "DEFERRED: per the milestone plan the irreversible publish waits for the batched family publish (at/after Phase 140 close); this plan verifies the automated gates now and holds the human merge for the batch"
---

<objective>
Execute the human-gated publish gate chain (THREAD-03). This plan is deliberately NON-autonomous: the
two human actions (merge the Release PR, merge the cleanup PR) are the auditable go/no-go points. The
automated verification (dry-run with priv/ included and test/ excluded, clean-room with BOTH siblings
absent) is confirmed here; the irreversible Hex publish fires only on the human Release-PR merge. This
mirrors Phase 138 Plan 04 exactly, with two threadline specifics: (a) the dry-run must confirm the
tarball INCLUDES priv/ (the EEX templates — omitting them silently breaks mix crosswake.gen.audit for
adopters) and EXCLUDES test/, and (b) threadline is the FINAL companion, so it publishes LAST — only
AFTER sigra AND chimeway are live.

The ordered gate sequence: ① path-dep dress rehearsal (Plans 01-02, already green) → ②
`hex.publish --dry-run` from the package with CROSSWAKE_RELEASE=1 (confirm priv/ in, test/ out) → ③
clean-room lane (crosswake + crosswake_threadline, NEITHER sigra NOR chimeway — the zero-sibling
vacuity guard) → ④ HUMAN merge Release PR → ⑤ auto publish → ⑥ post-publish clean-room from Hex →
⑦ auto failure-alert on any step 5-6 failure → ⑧ auto release-as cleanup PR → ⑨ HUMAN merge cleanup PR.

Purpose: threadline is the THIRD and FINAL irreversible Hex publish of v17.0 (after sigra, then
chimeway). The merge is the human decision; the dry-run + clean-room (with both siblings deliberately
absent) cover the irreversible risk AND prove the no-sibling-dep invariant at publish time.
DEFERRED: per the milestone plan the irreversible publish is held for the batched family publish
(at/after Phase 140 close). This plan verifies the automated gates now; the human merge fires in the
batch, only after sigra + chimeway are live.
Output: crosswake_threadline 0.1.0 live on Hex as an independent component (published LAST); the
one-shot release-as 0.1.0 pin stripped.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/139-crosswake-threadline-extraction/139-CONTEXT.md
@.planning/phases/139-crosswake-threadline-extraction/139-RESEARCH.md
@.planning/phases/139-crosswake-threadline-extraction/139-VALIDATION.md
@.planning/phases/139-crosswake-threadline-extraction/139-03-SUMMARY.md
</context>

<artifacts_this_phase_produces>
This plan (04) produces no source artifacts — it executes and verifies the publish gate chain and
performs the two human go/no-go actions. Outcome: crosswake_threadline 0.1.0 published to Hex as an
independent component (LAST in the sequential family order), and the one-shot release-as 0.1.0 pin
stripped via the merged cleanup PR. Per the milestone plan the irreversible publish is DEFERRED to the
batched family publish (at/after Phase 140 close) — this plan verifies the automated gates now and
holds the human merge for the batch.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Verify the automated pre-publish gates (dress rehearsal + hex.publish --dry-run with priv/ in, test/ out + clean-room readiness)</name>
  <files>packages/crosswake_threadline/mix.exs</files>
  <read_first>
    - 139-03-SUMMARY.md (the CI lanes wired: publish-hex-threadline, clean-room-proof-threadline; the threadline no-engine/suppress-companion smoke fix)
    - 139-02-SUMMARY.md (the path-dep dress rehearsal already green; the vacuity-safe cleanroom proof)
    - 139-CONTEXT.md §A (files: incl priv; verify with mix hex.build --unpack) + §E (vacuity-safe clean-room; DEFERRED human publish)
    - 139-RESEARCH.md § Package `files:` (priv required; hex.build --unpack verification) + § Environment (HEX_API_KEY, CROSSWAKE_RELEASE=1 on all mix steps)
    - packages/crosswake_threadline/mix.exs (the files: allowlist — confirm priv present, test absent)
  </read_first>
  <action>
    Confirm the path-dep dress rehearsal from Plans 01-02 is still green (core suite excluding host tags + the threadline package lane). Then run the Hex dry-run from the package with the release env so hex.build sees the Hex dep (not the path dep): `cd packages/crosswake_threadline && CROSSWAKE_RELEASE=1 mix hex.publish --dry-run --yes`. Confirm it reports the correct package name (crosswake_threadline), version (0.1.0), and a files: allowlist that INCLUDES priv/ (the priv/templates/crosswake/audit/*.eex EEX templates — the generator's primary artifact) and EXCLUDES test/. Additionally run `cd packages/crosswake_threadline && CROSSWAKE_RELEASE=1 mix hex.build --unpack` (or inspect the dry-run file list) to prove the two EEX templates are in the tarball — omitting priv/ yields a valid tarball that silently breaks mix crosswake.gen.audit at adopter runtime. Do NOT run a real publish here — publishing fires only on the human Release-PR merge (Task 2), and is DEFERRED to the batched family publish. Record the dry-run/unpack output summary in the SUMMARY, explicitly listing the priv/templates/crosswake/audit/*.eex entries and confirming test/ is absent.
  </action>
  <verify>
    <automated>cd packages/crosswake_threadline && mix test</automated>
    <automated>mix test --exclude requires_example_host</automated>
    <automated>cd packages/crosswake_threadline && CROSSWAKE_RELEASE=1 mix hex.build --unpack 2>&1 | grep -q "priv/templates/crosswake/audit" && echo priv-in-tarball</automated>
  </verify>
  <acceptance_criteria>
    - dress rehearsal (core + threadline package lanes) green
    - hex.publish --dry-run succeeds with the right name/version and a files: allowlist that INCLUDES priv/ (the EEX templates) and EXCLUDES test/ (no real publish)
    - the tarball unpack confirms priv/templates/crosswake/audit/{ledger.ex.eex,migration.exs.eex} are present
  </acceptance_criteria>
  <done>The automated pre-publish gates (dress rehearsal + dry-run + priv/-in-tarball confirmation) are green and the tarball includes priv/ and excludes test/; the package is ready for the human go/no-go (held for the family batch).</done>
</task>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 2 (HUMAN): Merge the threadline Release PR (irreversible publish go/no-go — DEFERRED to the family batch, LAST after sigra + chimeway)</name>
  <action>HUMAN go/no-go (non-autonomous): after all pre-publish gates are green — dress rehearsal, hex.publish --dry-run (priv/ in, test/ out), and clean-room-proof-threadline (crosswake + crosswake_threadline, BOTH siblings absent) on main — AND after sigra + chimeway are already live on Hex, merge the release-please-generated crosswake_threadline 0.1.0 Release PR. This fires the irreversible Hex publish + post-publish clean-room. Per the milestone plan this is DEFERRED to the batched family publish (at/after Phase 140 close). See how-to-verify for the exact ordered steps and the live-on-Hex confirmation.</action>
  <what-built>release-please generated the crosswake_threadline Release PR (version 0.1.0). The dry-run (priv/ in, test/ out) + clean-room (both siblings deliberately absent; Telemetry/Plug/Ledger module-shipment canaries) + dress rehearsal are green; merging this PR is the auditable go/no-go that triggers the irreversible Hex publish. threadline is the FINAL companion — it publishes LAST.</what-built>
  <how-to-verify>
    1. Confirm the pre-publish gates are all green: dress rehearsal (Task 1), hex.publish --dry-run with priv/ included and test/ excluded (Task 1), and clean-room-proof-threadline on main (installs crosswake + crosswake_threadline, NEITHER crosswake_sigra NOR crosswake_chimeway — the zero-sibling vacuity guard).
    2. Confirm sigra AND chimeway are ALREADY live on Hex (threadline is the final companion — sequential family order, D-9): `curl -s https://hex.pm/api/packages/crosswake_sigra` and `.../crosswake_chimeway` both return published releases.
    3. Open the release-please-generated Release PR for crosswake_threadline and confirm it targets version 0.1.0 and the CHANGELOG is correct.
    4. MERGE the Release PR. This fires: auto publish-hex-threadline (⑤) → post-publish clean-room from Hex (⑥) → and, on any step 5-6 failure, the auto failure-alert issue (⑦).
    5. After merge, confirm crosswake_threadline 0.1.0 is live: `curl -s https://hex.pm/api/packages/crosswake_threadline | python3 -c "import sys,json; d=json.load(sys.stdin); print([r['version'] for r in d['releases']])"` shows 0.1.0, and the post-publish clean-room-proof-threadline job is green.
    Why human: this merge is the single irreversible go/no-go before the crosswake_threadline Hex name/version coordinate is claimed (D-9). threadline publishes LAST — only after sigra AND chimeway are live. DEFERRED to the batched family publish per the milestone plan.
  </how-to-verify>
  <resume-signal>Type "published" once crosswake_threadline 0.1.0 is live on Hex and the post-publish clean-room is green, or "deferred" to hold for the family batch, or describe any publish/clean-room failure (check the auto failure-alert issue).</resume-signal>
</task>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 3 (HUMAN): Merge the release-as-cleanup PR (strip the one-shot 0.1.0 pin)</name>
  <action>HUMAN confirmation (non-autonomous): merge the auto-generated release-as-cleanup PR that strips the one-shot release-as "0.1.0" pin from the crosswake_threadline block, after confirming it is scoped to threadline only. See how-to-verify for the exact steps.</action>
  <what-built>The first threadline Release PR merged, so the release-as-cleanup job auto-opened a PR stripping the one-shot release-as "0.1.0" pin from the crosswake_threadline block in release-please-config.json (else every future threadline release re-targets 0.1.0 forever).</what-built>
  <how-to-verify>
    1. Find the auto-generated release-as-cleanup-* PR: `gh pr list --search "release-as-cleanup"`.
    2. Confirm it removes the release-as (and _TODO_release_as) keys from the crosswake_threadline block in release-please-config.json and touches nothing else (NOT the sigra, chimeway, or core blocks).
    3. MERGE the cleanup PR.
    4. Confirm the release-as-staleness-gate (if present) is green and the crosswake_threadline block no longer contains release-as.
    Why human: confirmation that the one-shot strip is correct and scoped to threadline only.
  </how-to-verify>
  <resume-signal>Type "cleanup-merged" once the release-as-cleanup PR is merged and the threadline block no longer pins release-as, or describe any staleness-gate issue.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| human → Hex.pm (Release PR merge) | the single irreversible go/no-go; upstream gates cover the risk; DEFERRED to the family batch |
| clean-room lane → both-siblings absence | the post-publish clean-room installs crosswake + crosswake_threadline from Hex WITHOUT sigra AND WITHOUT chimeway — the zero-sibling invariant proven against the real published tarball |
| tarball → priv/ inclusion | the dry-run/unpack must confirm priv/ ships (the EEX templates) — omitting it silently breaks mix crosswake.gen.audit for adopters |
| sequential family order | threadline must publish LAST, only after sigra + chimeway are live (D-9) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-139-13 | Tampering (supply chain) | irreversible Hex publish | high | mitigate | The publish fires only on the human Release-PR merge, after dry-run (priv/ in, test/ out) + clean-room (both siblings absent) + dress rehearsal are green AND sigra + chimeway are live; a bad version is patch-recoverable, only the name/version coordinate is irreversible and the gate chain protects it (D-9). DEFERRED to the family batch. |
| T-139-14 | Elevation of Privilege (inter-companion dep at publish) | tarball contents | high | mitigate | The dry-run confirms the files: allowlist excludes test/ (threadline has NO test-only sibling dep to begin with) and includes priv/; the post-publish clean-room installs crosswake + crosswake_threadline from Hex WITHOUT sigra AND chimeway and passes — proving the zero-sibling-dep invariant against the real tarball (THREAD-02). |
| T-139-19 | Denial of Service (generator broken for adopters) | priv/ tarball inclusion | high | mitigate | The dry-run/unpack confirms priv/templates/crosswake/audit/*.eex ship; omitting priv/ yields a valid tarball that silently breaks mix crosswake.gen.audit at adopter runtime (Task 1). This is the threadline-unique publish risk. |
| T-139-20 | Tampering (out-of-order family publish) | sequential publish order | medium | mitigate | The human go/no-go (Task 2) explicitly checks sigra + chimeway are already live before merging the threadline Release PR — threadline is the final companion (D-9). |
| T-139-15b | Tampering (stale version) | release-as one-shot | high | mitigate | The auto release-as-cleanup PR strips the 0.1.0 pin after first merge; human confirms scope-to-threadline and merges (Task 3). Staleness gate catches a missed strip. |
| T-139-21 | Repudiation | Release PR = audit record | low | accept | The Release-PR merge is the auditable human decision of record; no extra environment-protection approval is added (0-recurring-intervention principle). |
| T-139-SC | Tampering | npm/pip/cargo installs | low | accept | No package installs; this plan runs the publish of the first-party crosswake_threadline package only (RESEARCH Package Legitimacy Audit: first-party). |
</threat_model>

<verification>
- Task 1: dress rehearsal (core + threadline package) green; hex.publish --dry-run succeeds with a files: allowlist that includes priv/ and excludes test/; unpack confirms the EEX templates ship (no real publish).
- Task 2 (human): Release PR merged (after sigra + chimeway live) → crosswake_threadline 0.1.0 live on Hex; post-publish clean-room-proof-threadline green (both siblings absent). DEFERRED to the family batch.
- Task 3 (human): release-as-cleanup PR merged → the one-shot 0.1.0 pin stripped from the threadline block.
</verification>

<success_criteria>
- The full ordered gate chain executed: dry-run (priv/ in, test/ out) + clean-room (both siblings absent) green, Release PR merged, publish + post-publish clean-room green, cleanup PR merged.
- crosswake_threadline 0.1.0 is live on Hex as an independent component, published LAST (after sigra AND chimeway — sequential family order, THREAD-03).
- The zero-sibling-dep invariant is proven against the real published tarball by the post-publish clean-room; priv/ ships (the generator works for adopters).
- The one-shot release-as pin is stripped. The irreversible publish is DEFERRED to the batched family publish (at/after Phase 140 close).
</success_criteria>

<output>
Create `.planning/phases/139-crosswake-threadline-extraction/139-04-SUMMARY.md` when done
</output>
