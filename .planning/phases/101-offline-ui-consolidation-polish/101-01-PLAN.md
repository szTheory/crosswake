---
phase: 101-offline-ui-consolidation-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - priv/templates/crosswake/offline_ui/offline_root.html.heex.eex
  - priv/templates/crosswake/offline_ui/offline_page.html.heex.eex
  - priv/templates/crosswake/offline_ui/offline_controller.ex.eex
  - priv/templates/crosswake/offline_ui/offline.js.eex
autonomous: true
requirements:
  - OFFC-02
  - BRND-01
  - BRND-02

must_haves:
  truths:
    - "The generated offline UI functions correctly without any LiveView websocket connection attempts"
    - "The offline UI renders with Crosswake Brand Book tokens natively via Tailwind"
    - "The UI displays explicit microcopy"
  artifacts:
    - path: "priv/templates/crosswake/offline_ui/offline_root.html.heex.eex"
      provides: "Layout omitting LiveView JS"
      contains: "<%= @inner_content %>"
    - path: "priv/templates/crosswake/offline_ui/offline_page.html.heex.eex"
      provides: "Branded UI template"
      contains: "cw-wake-700"
    - path: "priv/templates/crosswake/offline_ui/offline_controller.ex.eex"
      provides: "Host offline controller template"
      contains: "defmodule <%= inspect(web_module) %>.OfflineController"
    - path: "priv/templates/crosswake/offline_ui/offline.js.eex"
      provides: "Vanilla JS script for offline logic"
      contains: "navigator.storage.estimate"
  key_links:
    - from: "priv/templates/crosswake/offline_ui/offline_root.html.heex.eex"
      to: "priv/templates/crosswake/offline_ui/offline.js.eex"
      via: "script tag inclusion"
      pattern: "offline\\.js"
    - from: "priv/templates/crosswake/offline_ui/offline_controller.ex.eex"
      to: "priv/templates/crosswake/offline_ui/offline_page.html.heex.eex"
      via: "Phoenix render action"
      pattern: "render\\(.*:index" 
---

<objective>
Scaffold the EEx templates that will be generated by the `mix crosswake.gen.offline_ui` Mix task.
Purpose: We need brand-aligned, host-owned vanilla JS and UI components that run entirely independently of the LiveView socket ecosystem.
Output: Four EEx templates representing the root layout, offline page, controller, and JavaScript logic.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/101-offline-ui-consolidation-polish/101-CONTEXT.md
@.planning/phases/101-offline-ui-consolidation-polish/101-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold Offline UI Layout and Page Templates</name>
  <files>priv/templates/crosswake/offline_ui/offline_root.html.heex.eex, priv/templates/crosswake/offline_ui/offline_page.html.heex.eex</files>
  <read_first>prompts/crosswake-brand-book.md, .planning/phases/101-offline-ui-consolidation-polish/101-CONTEXT.md</read_first>
  <action>Create EEx templates for the host-owned offline UI.
For `offline_root.html.heex.eex`: Provide a minimal HTML document that omits `app.js`, `Phoenix.LiveView.Socket`, and any live-reloading logic to prevent offline websocket errors. Include a script tag pointing to your offline JS asset (e.g., `/assets/offline.js`). Include `<%= @inner_content %>` so the page view renders correctly within this layout.
For `offline_page.html.heex.eex`: Provide the actual offline UI island. It must rely strictly on standard Tailwind utility classes referencing Crosswake Brand Book colors (`cw-wake-700`, `cw-brass-500`, `cw-foam-50`, `cw-current-950`). Include explicit text microcopy exactly as requested (e.g., "Available offline", "Pending server confirmation", "Draft only"). Do not use Gettext, React, Alpine, or any JS frameworks.</action>
  <verify>
    <automated>grep -q "inner_content" priv/templates/crosswake/offline_ui/offline_root.html.heex.eex</automated>
  </verify>
  <done>Templates are created with appropriate Tailwind classes and honest microcopy, with absolutely no LiveView references.</done>
  <acceptance_criteria>The layout includes @inner_content but completely lacks LiveView socket wiring or standard app.js. The page template heavily utilizes cw-wake-700 and cw-brass-500 classes and explicit textual microcopy without depending on gettext.</acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Scaffold Controller and Vanilla JS Logic</name>
  <files>priv/templates/crosswake/offline_ui/offline_controller.ex.eex, priv/templates/crosswake/offline_ui/offline.js.eex</files>
  <read_first>.planning/phases/101-offline-ui-consolidation-polish/101-RESEARCH.md, .planning/phases/101-offline-ui-consolidation-polish/101-CONTEXT.md</read_first>
  <action>Create the controller and JS EEx templates.
For `offline_controller.ex.eex`: Define a standard Phoenix controller (`defmodule <%= inspect(web_module) %>.OfflineController`) that renders the `:index` action and uses the `offline_root` layout if applicable, or assumes the router specifies the layout. Provide a dummy module setup expected from a standard Phoenix controller.
For `offline.js.eex`: Define the vanilla JS logic. It must explicitly check `navigator.storage.estimate()` and handle potential `QuotaExceededError` via try/catch when performing IndexedDB operations. Ensure it does not rely on any complex bundler setups beyond standard Phoenix esbuild (use plain ES6 module or script semantics).</action>
  <verify>
    <automated>grep -q "navigator.storage.estimate" priv/templates/crosswake/offline_ui/offline.js.eex</automated>
  </verify>
  <done>Controller template defined and vanilla JS script ready to manage offline storage budgets.</done>
  <acceptance_criteria>The controller template outputs a valid Phoenix controller. The JS template uses `navigator.storage.estimate()` and contains a try/catch block guarding IDB inserts specifically checking for `QuotaExceededError`.</acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Server → Offline Client | The offline UI will operate locally without a connection. Data rendered here relies on local cache. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-101-01 | Information Disclosure | offline.js | accept | Client-side cache holds only user-scoped data. Assume physical device security. |
| T-101-02 | Denial of Service | offline.js | mitigate | IndexedDB operations are explicitly guarded with try/catch and QuotaExceededError handling. |
</threat_model>

<verification>
Automated greps on the newly created template files assert that essential identifiers (e.g., `navigator.storage.estimate`, `@inner_content`, `cw-wake-700`) are properly formatted within the templates.
</verification>

<success_criteria>
The four required EEx templates are authored, omitting complex JS frameworks and satisfying the specific Crosswake Brand visual constraints and budget enforcement logic.
</success_criteria>

<output>
Create `.planning/phases/101-offline-ui-consolidation-polish/101-01-SUMMARY.md` when done
</output>