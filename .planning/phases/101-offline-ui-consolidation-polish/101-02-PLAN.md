---
phase: 101-offline-ui-consolidation-polish
plan: 02
type: execute
wave: 2
depends_on: [101-01]
files_modified:
  - lib/mix/tasks/crosswake.gen.offline_ui.ex
  - test/mix/tasks/crosswake.gen.offline_ui_test.exs
autonomous: true
requirements:
  - OFFC-01

must_haves:
  truths:
    - "Developers can successfully scaffold the UI using the generator"
    - "Generator instructions explicitly tell users to configure a separate esbuild entry point for offline.js"
  artifacts:
    - path: "lib/mix/tasks/crosswake.gen.offline_ui.ex"
      provides: "Generator Mix task logic"
      contains: "def run"
    - path: "test/mix/tasks/crosswake.gen.offline_ui_test.exs"
      provides: "Generator test suite"
      contains: "defmodule Mix.Tasks.Crosswake.Gen.OfflineUiTest"
  key_links:
    - from: "lib/mix/tasks/crosswake.gen.offline_ui.ex"
      to: "priv/templates/crosswake/offline_ui/offline_root.html.heex.eex"
      via: "template path resolution"
      pattern: "offline_root.html.heex.eex"
---

<objective>
Implement the `mix crosswake.gen.offline_ui` task using a TDD workflow.
Purpose: Provide developers a zero-friction path to generate and own the offline sync UI in their host Phoenix application.
Output: A test suite verifying generator execution and the generator task itself.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/101-offline-ui-consolidation-polish/101-RESEARCH.md
@lib/mix/tasks/crosswake.gen.sync.ex
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement the Generator Task via TDD</name>
  <files>test/mix/tasks/crosswake.gen.offline_ui_test.exs, lib/mix/tasks/crosswake.gen.offline_ui.ex</files>
  <read_first>.planning/phases/101-offline-ui-consolidation-polish/101-RESEARCH.md, lib/mix/tasks/crosswake.gen.sync.ex</read_first>
  <behavior>
    - Test 1: Given standard arguments, the generator writes `offline_controller.ex`, `offline_root.html.heex`, `offline_page.html.heex`, and `offline.js` to the target standard Phoenix application directories (e.g. `lib/my_app_web/controllers`, `lib/my_app_web/components/layouts`).
    - Test 2: The generator outputs standard instructions to `Mix.shell().info` containing router instructions, `tailwind.config.js` color extensions (`cw-wake-700`, `cw-brass-500`, etc.), and esbuild configuration instructions. It strictly adheres to D-01 by not modifying AST. The instructions must explicitly tell the user to configure an esbuild entry point for `offline.js` in `config/config.exs` rather than importing it into `app.js`.
  </behavior>
  <action>Create the `Mix.Tasks.Crosswake.Gen.OfflineUi` generator and its corresponding ExUnit test suite.
Use `OptionParser` to parse arguments similarly to `crosswake.gen.sync.ex`. Calculate host destinations dynamically (controllers, layouts, HTML components, and assets). Evaluate the four EEx templates created in Plan 01 and write them out. Finally, build and print the post-install instructions. The ExUnit test suite must assert the generated files exist in a temporary directory and that the terminal instructions contain the required routing, Tailwind guidance, and esbuild configuration instructions. Specifically, the instructions must tell users to add `offline.js` as an entry point in `config/config.exs` instead of importing it into `app.js`.</action>
  <verify>
    <automated>mix test test/mix/tasks/crosswake.gen.offline_ui_test.exs</automated>
  </verify>
  <done>The TDD cycle is complete, tests are green, and the task correctly outputs instructions (including esbuild config) and files.</done>
  <acceptance_criteria>A comprehensive test suite exists. The generator scaffolds the 4 files in correct Phoenix paths without errors. It relies on terminal instructions rather than AST modifications for host router, tailwind config, and esbuild config for the separate `offline.js` entry point.</acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Generator → Host Project | The generator writes code to the host project. It must not overwrite critical user code unexpectedly without warning. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-101-03 | Tampering | mix task | mitigate | Generator outputs to dedicated `offline` directory scopes where possible and relies on host's standard git VCS to revert errors. |
</threat_model>

<verification>
`mix test test/mix/tasks/crosswake.gen.offline_ui_test.exs` runs successfully.
</verification>

<success_criteria>
The generator task is complete, fully tested, and ready to scaffold the consolidated UI for any adopting app.
</success_criteria>

<output>
Create `.planning/phases/101-offline-ui-consolidation-polish/101-02-SUMMARY.md` when done
</output>