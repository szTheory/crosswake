---
phase: 116-proof-debt-and-release-truth
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
  - CHANGELOG.md
  - examples/phoenix_host/mix.exs
  - guides/install.md
  - guides/native_shell.md
  - guides/compatibility.md
  - guides/support_matrix.md
  - examples/QUICK_START.md
  - guides/adoption.md
  - examples/phoenix_host/priv/crosswake/install_manifest.json
  - examples/ios_shell_host/Fixtures/crosswake_manifest.json
  - examples/android_shell_host/app/src/main/assets/crosswake_manifest.json
autonomous: true
requirements:
  - REL-TRUTH-01
must_haves:
  truths:
    - "D-06/D-07: Public release docs state exact current trust facts for crosswake 0.1.2 and use derived package-version wording in reusable generator/support prose."
    - "D-08/D-09: Historical 0.1.0 remains only as labeled historical release truth; CHANGELOG [Unreleased] is future-oriented and [0.1.2] is released."
    - "D-10: Narrative truth is guarded by docs/tests, not broad prose release-please markers."
    - "D-08/D-19: Example manifests carry current `crosswake_version` package truth and embedded package-version wording without presenting checked-in native hosts as published-coordinate proof."
    - "D-16/D-17/D-18: Front-door docs stop routing readers into known-bad commands while leaving the full quick-start/adoption rewrite to Phase 118."
    - "D-19/D-21/D-22: Proof labels are candid for skeptical Phoenix adopters and do not overclaim checked-in native hosts."
    - "D-20: BRAND-SPEC.md remains the current brand truth; this plan makes no brand-token or historical prompt changes."
  artifacts:
    - path: "README.md"
      provides: "Current package baseline and public proof-entry wording"
      contains: "0.1.2"
    - path: "CHANGELOG.md"
      provides: "Conventional release semantics for [Unreleased] and [0.1.2]"
      contains: "## [0.1.2] — 2026-06-17"
    - path: "guides/install.md"
      provides: "Install/rebuild release truth with shipped standalone native shell core packages"
    - path: "guides/native_shell.md"
      provides: "Native shell release truth without checked-in-host overclaim"
    - path: "guides/compatibility.md"
      provides: "Compatibility release truth without stale standalone-package deferral"
    - path: "guides/support_matrix.md"
      provides: "Support matrix package-version and evidence-label truth"
    - path: "examples/QUICK_START.md"
      provides: "Phase 116 safety entry that avoids bad setup/port/path claims"
    - path: "guides/adoption.md"
      provides: "Phase 116 safety entry that avoids bridge-owned offline mutation claims"
    - path: "examples/phoenix_host/mix.exs"
      provides: "Example host metadata no longer reads as stale Crosswake proof truth"
    - path: "examples/phoenix_host/priv/crosswake/install_manifest.json"
      provides: "Phoenix host install manifest uses current Crosswake package version truth"
      contains: "\"crosswake_version\""
    - path: "examples/ios_shell_host/Fixtures/crosswake_manifest.json"
      provides: "iOS example manifest fixture uses current Crosswake package version truth and current shell-core release wording"
      contains: "\"crosswake_version\""
    - path: "examples/android_shell_host/app/src/main/assets/crosswake_manifest.json"
      provides: "Android example manifest fixture uses current Crosswake package version truth and current shell-core release wording"
      contains: "\"crosswake_version\""
  key_links:
    - from: "mix.exs"
      to: "README.md"
      via: "README current baseline names the root package version"
      pattern: "0\\.1\\.2"
    - from: "CHANGELOG.md"
      to: "lib/crosswake/doctor/publish_readiness.ex"
      via: "PublishReadiness publish parity reads CHANGELOG.md and current project version"
      pattern: "\\[0\\.1\\.2\\]"
    - from: "guides/install.md"
      to: "guides/support_matrix.md"
      via: "Install guide links support matrix action classes and package-version proof labels"
      pattern: "support_matrix"
    - from: "mix.exs"
      to: "examples/phoenix_host/priv/crosswake/install_manifest.json, examples/ios_shell_host/Fixtures/crosswake_manifest.json, examples/android_shell_host/app/src/main/assets/crosswake_manifest.json"
      via: "example manifests expose current Crosswake package version in top-level crosswake_version"
      pattern: "crosswake_version"
---

<objective>
Reconcile public release truth so README, CHANGELOG, ExDoc extras, guide surfaces, support references, example metadata, and example manifests describe `crosswake 0.1.2` and package-version native coordinates accurately.

Purpose: REL-TRUTH-01 requires adopters to see current release facts instead of stale latest-version, pending-release, deferred-shell, wrong-command, or bridge-owned offline claims. This plan implements D-06, D-07, D-08, D-09, D-10, D-16, D-17, D-18, D-19, D-20, D-21, and D-22.
Output: Public docs, example metadata, and example manifest JSON that state current release truth, label proof classes honestly, and avoid Phase 118/119/120 scope absorption.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/116-proof-debt-and-release-truth/116-CONTEXT.md
@.planning/phases/116-proof-debt-and-release-truth/116-RESEARCH.md
@.planning/phases/116-proof-debt-and-release-truth/116-VALIDATION.md
@README.md
@CHANGELOG.md
@examples/QUICK_START.md
@guides/adoption.md
@guides/install.md
@guides/native_shell.md
@guides/compatibility.md
@guides/support_matrix.md
@mix.exs
@examples/phoenix_host/mix.exs
@examples/phoenix_host/priv/crosswake/install_manifest.json
@examples/ios_shell_host/Fixtures/crosswake_manifest.json
@examples/android_shell_host/app/src/main/assets/crosswake_manifest.json
@examples/phoenix_host/config/config.exs
@examples/phoenix_host/playwright.config.ts
@lib/crosswake/doctor/publish_readiness.ex
@test/crosswake/doctor/publish_readiness_test.exs
</context>

## Artifacts this phase produces

- Changed docs: README current baseline; CHANGELOG release sections and links; install/native/compatibility/support guide release-truth paragraphs; quick-start/adoption safety labels.
- Changed metadata: example host `mix.exs` wording/version metadata so it does not read as stale Crosswake package proof.
- Changed manifests: Phoenix install manifest plus checked-in iOS/Android example manifests carry current top-level `crosswake_version` package truth and current embedded shell-core release wording.
- Changed tests: none in this plan; Plan 116-03 adds the guard after this baseline exists.

<tasks>

<task type="auto">
  <name>Task 1: Correct README, CHANGELOG, example metadata, and manifest version truth</name>
  <read_first>
    .planning/phases/116-proof-debt-and-release-truth/116-CONTEXT.md
    .planning/phases/116-proof-debt-and-release-truth/116-RESEARCH.md
    README.md
    CHANGELOG.md
    mix.exs
    examples/phoenix_host/mix.exs
    examples/phoenix_host/priv/crosswake/install_manifest.json
    examples/ios_shell_host/Fixtures/crosswake_manifest.json
    examples/android_shell_host/app/src/main/assets/crosswake_manifest.json
    lib/crosswake/doctor/publish_readiness.ex
    test/crosswake/doctor/publish_readiness_test.exs
  </read_first>
  <files>README.md, CHANGELOG.md, examples/phoenix_host/mix.exs, examples/phoenix_host/priv/crosswake/install_manifest.json, examples/ios_shell_host/Fixtures/crosswake_manifest.json, examples/android_shell_host/app/src/main/assets/crosswake_manifest.json</files>
  <action>Per D-06, D-08, D-09, D-10, D-19, D-21, and D-22, change README's current baseline to `Crosswake version 0.1.2`; rewrite CHANGELOG so `[Unreleased]` describes only unpublished work after the current release, `[0.1.2]` is a released entry dated `2026-06-17`, and `0.1.0` is only historical release truth. Update CHANGELOG comparison links so `[Unreleased]` compares from `v0.1.2` to `HEAD`, `[0.1.2]` compares `v0.1.0...v0.1.2` or links to the `v0.1.2` release, and `[0.1.0]` remains the historical release tag. Update `examples/phoenix_host/mix.exs` so the example host metadata no longer reads as a stale Crosswake package baseline; use `0.1.2` or an explicit comment that separates example app metadata from root package truth. Update the top-level `crosswake_version` in `examples/phoenix_host/priv/crosswake/install_manifest.json`, `examples/ios_shell_host/Fixtures/crosswake_manifest.json`, and `examples/android_shell_host/app/src/main/assets/crosswake_manifest.json` to the current package version. For the two native example manifests, also update embedded `support_matrix.package_surfaces` / `support_matrix.shells` release wording that still describes standalone shell core packages as deferred or version `0.1.0`, using current package-version or Hex-matched wording. Do not change route, capability, pack, compatibility-axis, or runtime-owner semantics, and do not present checked-in native hosts as published-coordinate proof.</action>
  <acceptance_criteria>
    - README current baseline names Crosswake `0.1.2`.
    - CHANGELOG `[Unreleased]` is future-oriented and no longer says the current Hex release is `0.1.0`.
    - CHANGELOG `[0.1.2]` is dated `2026-06-17` and no longer says that release is pending or not cut on Hex.
    - `0.1.0` remains only in historical release context, comparison links, or explicitly labeled baseline truth.
    - No broad release automation markers are added to prose beyond the existing narrow root `mix.exs` version marker.
    - All three example manifests have top-level `crosswake_version` equal to the current Crosswake package version.
    - The iOS and Android example manifests no longer carry stale embedded shell-core deferral wording or `0.1.0` shell artifact versions as current proof.
    - Manifest edits do not alter route/capability/pack semantics or claim checked-in native hosts are published-coordinate proof.
  </acceptance_criteria>
  <verify>
    <automated>mix test test/crosswake/doctor/publish_readiness_test.exs &amp;&amp; mix run -e 'current = Application.spec(:crosswake, :vsn) |&gt; to_string(); Enum.each(~w(examples/phoenix_host/priv/crosswake/install_manifest.json examples/ios_shell_host/Fixtures/crosswake_manifest.json examples/android_shell_host/app/src/main/assets/crosswake_manifest.json), fn path -&gt; body = File.read!(path); unless body =~ ~s("crosswake_version": "#{current}"), do: raise("#{path} missing current crosswake_version #{current}") end); for {path, checks} &lt;- [{"README.md", ["0.1.2"]}, {"CHANGELOG.md", ["## [Unreleased]", "## [0.1.2]"]}], do: for check &lt;- checks, do: unless(File.read!(path) =~ check, do: raise("#{path} missing #{check}"))'</automated>
  </verify>
  <done>README, CHANGELOG, example host metadata, and example manifests present current `0.1.2` release truth without stale pending/latest or unlabelled manifest-version claims.</done>
</task>

<task type="auto">
  <name>Task 2: Replace stale standalone-shell deferral in install, native, compatibility, and support surfaces</name>
  <read_first>
    .planning/phases/116-proof-debt-and-release-truth/116-CONTEXT.md
    .planning/phases/116-proof-debt-and-release-truth/116-RESEARCH.md
    guides/install.md
    guides/native_shell.md
    guides/compatibility.md
    guides/support_matrix.md
    mix.exs
  </read_first>
  <files>guides/install.md, guides/native_shell.md, guides/compatibility.md, guides/support_matrix.md</files>
  <action>Per D-06, D-07, D-08, D-18, D-19, D-21, and D-22, replace the stale promotion-rule wording in `guides/install.md`, `guides/native_shell.md`, and `guides/compatibility.md` with current package-version truth: standalone native shell core packages ship through SwiftPM and Maven Central, generated non-local coordinates resolve at the Crosswake package version, provider/device/native UI evidence remains advisory or verification-required unless promoted, and checked-in native hosts are not described as published-coordinate proof. In `guides/support_matrix.md`, keep Hex-matched shell artifact rows, make the current package-version wording clear, and keep device/emulator/broader native-runtime evidence labeled advisory or separately proven.</action>
  <acceptance_criteria>
    - Install, native shell, and compatibility guides no longer describe standalone public shell packages as unavailable after Milestone 11 release truth.
    - Generator/support prose uses derived wording such as the Crosswake package version or Hex-matched version where repeatable release prose would otherwise churn.
    - Support matrix keeps generated SwiftPM/Maven coordinates tied to the Crosswake package version.
    - Checked-in iOS/Android hosts are not presented as published-coordinate proof in this phase.
  </acceptance_criteria>
  <verify>
    <automated>elixir -e 'paths = ~w(guides/install.md guides/native_shell.md guides/compatibility.md guides/support_matrix.md); for path &lt;- paths, do: if(File.read!(path) =~ "standalone public shell packages are deferred", do: raise("#{path} has stale standalone-shell deferral")); support = File.read!("guides/support_matrix.md"); unless support =~ "Crosswake package version", do: raise("support matrix missing package-version shell wording")'</automated>
  </verify>
  <done>Guide and support surfaces describe shipped standalone native shell core packages without overclaiming native evidence classes owned by Phase 119.</done>
</task>

<task type="auto">
  <name>Task 3: Add Phase 116 safety labels to quick-start and adoption front doors</name>
  <read_first>
    .planning/phases/116-proof-debt-and-release-truth/116-CONTEXT.md
    .planning/phases/116-proof-debt-and-release-truth/116-RESEARCH.md
    examples/QUICK_START.md
    guides/adoption.md
    examples/phoenix_host/config/config.exs
    examples/phoenix_host/playwright.config.ts
  </read_first>
  <files>examples/QUICK_START.md, guides/adoption.md</files>
  <action>Per D-16, D-17, D-18, D-19, D-21, and D-22, make narrow safety edits only. In `examples/QUICK_START.md`, remove the bad `mix setup` instruction, stop naming port `4000`, name `4002` as the current example-host port if a port is named, and label native simulator/device walkthrough content as advisory until Phase 119 classifies checked-in native evidence. In `guides/adoption.md`, remove the bridge-owned offline mutation example and replace it with a short current-verified-proof label: the current proof is app-owned IndexedDB outbox plus reconnect-triggered Phoenix/Ecto reconciliation; the full adoption rewrite belongs to Phase 118. Keep the maintainer voice precise and candid.</action>
  <acceptance_criteria>
    - Quick start no longer routes readers into `mix setup`.
    - Quick start no longer names `http://localhost:4000` as the current example-host server.
    - Adoption guide no longer presents `Crosswake.mutate` or a bridge-owned sync engine as current proof.
    - Both files tell skeptical adopters where current verified proof lives without attempting the complete Phase 118 rewrite.
    - Neither file claims checked-in iOS/Android hosts are published-coordinate proof.
  </acceptance_criteria>
  <verify>
    <automated>elixir -e 'bad = [{"examples/QUICK_START.md", ["mix setup", "localhost:4000"]}, {"guides/adoption.md", ["Crosswake.mutate", "Sync Engine (Bridge)"]}]; for {path, terms} &lt;- bad, term &lt;- terms, do: if(File.read!(path) =~ term, do: raise("#{path} still contains #{term}")); unless File.read!("examples/QUICK_START.md") =~ "4002", do: raise("quick start must name current 4002 port or remove server port instructions")'</automated>
  </verify>
  <done>Front-door safety labels prevent public readers from following known-bad commands or fictional offline authority while Phase 118 remains the owner of the full rewrite.</done>
</task>

</tasks>

## Scope Fences

- D-17: Do not perform the full Phase 118 quick-start/adoption rewrite in this plan.
- D-19: Do not classify checked-in iOS/Android hosts as published-coordinate proof; Phase 119 owns that evidence decision.
- D-20: Do not edit brandbook token truth or historical prompt seed files.
- Phase 120 screenshot/video/artifact collateral is not part of this release-truth repair.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| public docs -> adopter decisions | Public prose affects install, proof, native evidence, and offline authority expectations. |
| release metadata -> ExDoc/HexDocs | README, CHANGELOG, and guides are packaged as ExDoc extras and become public docs. |
| example manifests -> native/example proof readers | Checked-in manifest JSON communicates package-version and support truth to example-shell readers and tests. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-116-03 | Spoofing / Repudiation | README, CHANGELOG, guides | mitigate | State `0.1.2` current truth where trust depends on exact release facts and keep old versions historical. |
| T-116-04 | Elevation of Privilege / Tampering | adoption guide offline authority prose | mitigate | Remove bridge-owned mutation authority language and name app-owned outbox plus backend reconciliation as the current proof. |
| T-116-05 | Information Disclosure | native proof labels | mitigate | Label simulator/device/native evidence as advisory or separately proven; do not imply checked-in hosts prove published coordinates. |
| T-116-09 | Repudiation | example manifest JSON | mitigate | Align top-level `crosswake_version` and embedded shell-core support wording to current package truth while preserving Phase 119 native evidence classification. |
| T-116-SC | Tampering | package installs | accept | No npm, pip, or cargo package installs are planned in this plan. |
</threat_model>

<verification>
Overall plan checks:

<automated>mix test test/crosswake/doctor/publish_readiness_test.exs</automated>
<automated>elixir -e 'checks = [{"README.md", "0.1.2"}, {"CHANGELOG.md", "## [0.1.2]"}, {"guides/support_matrix.md", "Crosswake package version"}]; for {path, term} &lt;- checks, do: unless(File.read!(path) =~ term, do: raise("#{path} missing #{term}"))'</automated>
<automated>mix run -e 'current = Application.spec(:crosswake, :vsn) |&gt; to_string(); manifests = ~w(examples/phoenix_host/priv/crosswake/install_manifest.json examples/ios_shell_host/Fixtures/crosswake_manifest.json examples/android_shell_host/app/src/main/assets/crosswake_manifest.json); Enum.each(manifests, fn path -&gt; body = File.read!(path); unless body =~ ~s("crosswake_version": "#{current}"), do: raise("#{path} missing current crosswake_version #{current}") end); Enum.each(tl(manifests), fn path -&gt; body = File.read!(path); if body =~ "Separately versioned shell artifacts stay deferred", do: raise("#{path} still carries stale shell-core deferral"); if body =~ ~s("version": "0.1.0"), do: raise("#{path} still carries stale shell artifact version") end)'</automated>

Do not run or commit dirty example-host SQLite database artifacts.
</verification>

<success_criteria>
REL-TRUTH-01 is satisfied: public docs, example metadata, and example manifests agree with the current `0.1.2` release truth or explicitly label old fixture truth, and front-door surfaces no longer route readers into known-bad instructions.
</success_criteria>

<output>
Create `.planning/phases/116-proof-debt-and-release-truth/116-02-SUMMARY.md` when done.
</output>
