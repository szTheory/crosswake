---
phase: 09-local-first-content-flow-exemplar
plan: 03
type: execute
wave: 3
depends_on: [02]
files_modified:
  - examples/phoenix_host/lib/crosswake_example/local_first/study_session_live.ex
  - examples/phoenix_host/lib/crosswake_example/local_first/study_history_live.ex
autonomous: true
requirements:
  - LOCAL-01
  - LOCAL-02
must_haves:
  truths:
    - "User can simulate studying cards within an offline island."
    - "User can view past review history and rejections on a cached route."
  artifacts:
    - path: "examples/phoenix_host/lib/crosswake_example/local_first/study_session_live.ex"
      provides: "LiveView for island mode"
    - path: "examples/phoenix_host/lib/crosswake_example/local_first/study_history_live.ex"
      provides: "LiveView for history mode"
  key_links:
    - from: "examples/phoenix_host/lib/crosswake_example/local_first/study_history_live.ex"
      to: "examples/phoenix_host/lib/crosswake_example/local_first/study.ex"
      via: "Fetches review events"
      pattern: "Study\\.list_events"
---

<objective>
Implement the Local-First LiveViews to represent the Offline Island and Cached Read-Only states.

Purpose: To prove the local-first product shape, we need the frontend routes that act as the offline island (`/study/session`) and the history view (`/study/history`). The session view will simulate a flashcard study interface, demonstrating what happens offline. The history view will display the sync outcomes, exposing explicit conflicts to the user.
Output: Two functional LiveView modules.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-local-first-content-flow-exemplar/09-CONTEXT.md
@.planning/phases/09-local-first-content-flow-exemplar/09-PATTERNS.md
@.planning/phases/09-local-first-content-flow-exemplar/09-RESEARCH.md

<interfaces>
From examples/phoenix_host/lib/crosswake_example/local_first/study.ex:
```elixir
defmodule CrosswakeExample.LocalFirst.Study do
  def list_events() # returns [%ReviewEvent{}, ...]
end
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement Study Session LiveView (Island)</name>
  <files>examples/phoenix_host/lib/crosswake_example/local_first/study_session_live.ex</files>
  <action>
    Create `CrosswakeExample.LocalFirst.StudySessionLive`.
    - Follow the LiveView structural pattern established in `examples/phoenix_host/lib/crosswake_example/saas_portal/approvals_live.ex` per PATTERNS.md.
    - Build a simple flashcard UI (e.g., displaying a card, buttons for 'Good', 'Hard').
    - Since it is an `offline: :island` route, it should rely on client-side state for progression.
    - Provide a button to artificially simulate pushing to the outbox (to demonstrate the flow if connected).
    per D-01, D-02, and D-07.
  </action>
  <verify>
    <automated>mix test --color</automated>
  </verify>
  <done>LiveView compiles and renders a basic flashcard UI.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Study History LiveView (Cached Read-Only)</name>
  <files>examples/phoenix_host/lib/crosswake_example/local_first/study_history_live.ex</files>
  <action>
    Create `CrosswakeExample.LocalFirst.StudyHistoryLive`.
    - Follow the explicit review/conflict UI pattern from `examples/phoenix_host/lib/crosswake_example/selective_native/submission_review_live.ex` per PATTERNS.md.
    - Fetch and display the list of review events using `Study.list_events/0`.
    - Display explicit statuses for each event (e.g., 'accepted', 'rejected' with reason).
    - As a cached read-only route, it should display the state without offering mutation actions.
    per D-07, D-08, and D-09.
  </action>
  <verify>
    <automated>mix test --color</automated>
  </verify>
  <done>LiveView fetches events from context and renders them with explicit status.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| UI | Displaying user-generated data |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-04 | Spoofing | `StudyHistoryLive` | accept | Safe for exemplar scope, assuming correct escaping in LiveView rendering templates. |
</threat_model>

<verification>
Ensure both LiveViews compile and can be accessed via tests or browser when Phoenix is running.
</verification>

<success_criteria>
- The UI exposes both island capabilities and cached history explicitly.
- The history UI renders different visual states based on the sync outcome (accepted vs rejected).
</success_criteria>

<output>
After completion, create `.planning/phases/09-local-first-content-flow-exemplar/09-03-SUMMARY.md`
</output>
