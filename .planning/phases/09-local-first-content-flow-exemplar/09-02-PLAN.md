---
phase: 09-local-first-content-flow-exemplar
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified:
  - examples/phoenix_host/lib/crosswake_example/local_first/sync_controller.ex
  - examples/phoenix_host/lib/crosswake_example/router.ex
autonomous: true
requirements:
  - LOCAL-01
  - LOCAL-02
must_haves:
  truths:
    - "Client outbox payloads can be pushed to a reconciliation endpoint."
    - "The /study lanes are explicitly declared with local-first crosswake policy."
  artifacts:
    - path: "examples/phoenix_host/lib/crosswake_example/local_first/sync_controller.ex"
      provides: "API endpoint for outbox sync"
      exports: ["sync/2"]
    - path: "examples/phoenix_host/lib/crosswake_example/router.ex"
      provides: "Route policy definitions for study lanes"
      contains: "offline: :island"
  key_links:
    - from: "examples/phoenix_host/lib/crosswake_example/local_first/sync_controller.ex"
      to: "examples/phoenix_host/lib/crosswake_example/local_first/study.ex"
      via: "Delegates payload sync"
      pattern: "Study\\.sync_events"
---

<objective>
Expose the synchronization API endpoint and define the explicit Crosswake route policy for the Local-First lanes.

Purpose: The offline island needs a designated endpoint to push its outbox journal upon regaining connectivity. We also need to declare the local-first routes in the router so the Native Shell manifest compilation recognizes them.
Output: A new `SyncController` and updated `router.ex` containing the `/study` scope with correct Crosswake metadata.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-local-first-content-flow-exemplar/09-CONTEXT.md
@.planning/phases/09-local-first-content-flow-exemplar/09-PATTERNS.md
@.planning/phases/09-local-first-content-flow-exemplar/09-RESEARCH.md

<interfaces>
From examples/phoenix_host/lib/crosswake_example/local_first/study.ex:
```elixir
defmodule CrosswakeExample.LocalFirst.Study do
  # Returns {:ok, explicit_results} where explicit_results maps client_mutation_id to success/rejection
  def sync_events(events_payload)
end
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement Sync API Endpoint</name>
  <files>examples/phoenix_host/lib/crosswake_example/local_first/sync_controller.ex</files>
  <action>
    Create `CrosswakeExample.LocalFirst.SyncController`.
    - Implement the `sync/2` action to receive a JSON payload of review events from the client outbox.
    - Validate the payload shape.
    - Delegate processing to `Study.sync_events/1` created in Plan 01.
    - Return a JSON response with explicit accepted/rejected statuses for each `client_mutation_id` per D-08.
  </action>
  <verify>
    <automated>mix test --color</automated>
  </verify>
  <done>Controller parses incoming events, triggers context, and returns correctly shaped JSON.</done>
</task>

<task type="auto">
  <name>Task 2: Define Route Policy for Local-First Lanes</name>
  <files>examples/phoenix_host/lib/crosswake_example/router.ex</files>
  <action>
    Update `router.ex` to establish the `/study` scope under the `CrosswakeExample.LocalFirst` module namespace.
    - Define a `live` route for `/study/session` pointing to `StudySessionLive` (to be created next) with Crosswake metadata: `runtime: :live_view`, `offline: :island`, `packs: [[id: :daily_study, version: "1.0.0", kind: :content]]`, `security: :standard`.
    - Define a `live` route for `/study/history` pointing to `StudyHistoryLive` with Crosswake metadata: `runtime: :live_view`, `offline: :cached_read_only`, `security: :standard`.
    - Define a `post` route for `/study/sync` pointing to `SyncController, :sync`.
    per D-04, D-05, and D-07.
  </action>
  <verify>
    <automated>mix test --color</automated>
  </verify>
  <done>Routes are declared and `mix compile` passes without Crosswake policy validation errors.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client → Server | Unauthenticated or user-authenticated outbox synchronization |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-03 | Elevation of Privilege | `router.ex` | mitigate | Ensure proper pipeline placement (e.g. CSRF/auth where applicable) for API endpoint. |
</threat_model>

<verification>
`mix phx.routes` displays the new `/study/session`, `/study/history`, and `/study/sync` endpoints.
</verification>

<success_criteria>
- `/study/sync` accepts HTTP POST.
- The router correctly assigns `:island` and `:cached_read_only` policy logic to the new routes.
</success_criteria>

<output>
After completion, create `.planning/phases/09-local-first-content-flow-exemplar/09-02-SUMMARY.md`
</output>
