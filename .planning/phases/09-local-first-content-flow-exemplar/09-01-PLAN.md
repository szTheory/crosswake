---
phase: 09-local-first-content-flow-exemplar
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - examples/phoenix_host/priv/repo/migrations/*_create_review_events.exs
  - examples/phoenix_host/lib/crosswake_example/local_first/review_event.ex
  - examples/phoenix_host/lib/crosswake_example/local_first/study.ex
autonomous: true
requirements:
  - LOCAL-01
must_haves:
  truths:
    - "Review events persist as append-only journal entries."
    - "Sync operations are explicit and idempotent."
  artifacts:
    - path: "examples/phoenix_host/lib/crosswake_example/local_first/review_event.ex"
      provides: "Ecto schema for ReviewEvent"
      contains: "client_mutation_id"
    - path: "examples/phoenix_host/lib/crosswake_example/local_first/study.ex"
      provides: "Context handling Ecto.Multi sync logic"
      exports: ["sync_events/1", "list_events/0"]
  key_links:
    - from: "examples/phoenix_host/lib/crosswake_example/local_first/study.ex"
      to: "examples/phoenix_host/lib/crosswake_example/local_first/review_event.ex"
      via: "Ecto.Multi.insert_all"
      pattern: "Ecto\\.Multi\\.insert_all"
---

<objective>
Establish the core Ecto schema and context for the Local-First study flow.

Purpose: To prove a local-first product shape, we need a robust backend that handles offline synchronization explicitly. This plan sets up the server authoritative truth: an append-only journal of review events pushed from the offline island's outbox.
Output: Migration for `review_events`, Ecto schema, and `Study` context handling `Ecto.Multi` synchronization.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-local-first-content-flow-exemplar/09-CONTEXT.md
@.planning/phases/09-local-first-content-flow-exemplar/09-PATTERNS.md
@.planning/phases/09-local-first-content-flow-exemplar/09-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Generate ReviewEvent Migration and Schema</name>
  <files>examples/phoenix_host/priv/repo/migrations/*_create_review_events.exs, examples/phoenix_host/lib/crosswake_example/local_first/review_event.ex</files>
  <action>
    Create a migration to add the `review_events` table and the corresponding `CrosswakeExample.LocalFirst.ReviewEvent` schema.
    - Follow the Ecto Schema pattern from `examples/phoenix_host/lib/crosswake_example/selective_native/claim.ex` per PATTERNS.md.
    - Fields: `client_mutation_id` (string, unique constraint for idempotency), `card_id` (integer), `rating` (string, e.g., 'good', 'hard'), `status` (string, e.g., 'accepted', 'rejected', default: 'accepted').
    - Include standard Ecto timestamps.
    - Implement a `changeset/2` function for basic validation.
    per D-06 and D-08.
  </action>
  <verify>
    <automated>mix test --color</automated>
  </verify>
  <done>Migration and schema are present, `mix ecto.migrate` runs without errors.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Study Context for Sync Reconciliation</name>
  <files>examples/phoenix_host/lib/crosswake_example/local_first/study.ex</files>
  <action>
    Create `CrosswakeExample.LocalFirst.Study` context.
    - Follow the Ecto Repository pattern from `examples/phoenix_host/lib/crosswake_example/selective_native/claims.ex` per PATTERNS.md.
    - Implement `sync_events(events_payload)` using `Ecto.Multi`.
    - It should accept an array of event maps from the client.
    - Use `Ecto.Multi.insert_all` to insert valid events into `review_events`, handling conflicts by ignoring (`on_conflict: :nothing`) based on `client_mutation_id`.
    - Return an explicit result containing successful inserts and any specific rejections, so the client outbox knows the exact outcome.
    - Add `list_events/0` to retrieve history for the UI.
    per D-08 and Pitfall 1/2 from RESEARCH.
  </action>
  <verify>
    <automated>mix test --color</automated>
  </verify>
  <done>Context handles Ecto.Multi logic cleanly, isolating client mutation duplicates.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client Outbox → API | Untrusted payload containing client-generated IDs |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-01 | Tampering | `Study.sync_events` | mitigate | Use `client_mutation_id` uniqueness to prevent replay and duplicate submissions. |
| T-09-02 | Elevation of Privilege | `ReviewEvent` | mitigate | Ensure schema requires explicit validation before Ecto insertion. |
</threat_model>

<verification>
Ensure `mix ecto.migrate` works in the Phoenix host.
Ensure context can process a batch of events with duplicate mutation IDs gracefully.
</verification>

<success_criteria>
- Schema has idempotency keys (`client_mutation_id`).
- `Ecto.Multi` transaction handles batch inserts safely.
- No silent failures: errors are bubbled up explicitly.
</success_criteria>

<output>
After completion, create `.planning/phases/09-local-first-content-flow-exemplar/09-01-SUMMARY.md`
</output>
