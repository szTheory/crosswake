---
phase: 76-core-shell-extraction-packaging
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/crosswake-shell-core-android/build.gradle.kts
  - packages/crosswake-shell-core-android/settings.gradle.kts
  - packages/crosswake-shell-core-android/src/main/AndroidManifest.xml
  - packages/crosswake-shell-core-android/src/main/java/dev/crosswake/shell/core/ActivationCoordinator.kt
  - packages/crosswake-shell-core-android/src/main/java/dev/crosswake/shell/core/BridgeChannel.kt
  - packages/crosswake-shell-core-android/src/main/java/dev/crosswake/shell/core/transfer/TransferCoordinator.kt
  - packages/crosswake-shell-core-android/src/main/java/dev/crosswake/shell/core/packs/PackStore.kt
  - packages/crosswake-shell-core-android/src/main/java/dev/crosswake/shell/core/DiagnosticExportManager.kt
autonomous: true
requirements:
  - CORE-02
  - CORE-03

must_haves:
  truths:
    - "Android core logic exists as a standalone Maven (Gradle) AAR project"
    - "The project compiles independently without a host app"
  artifacts:
    - path: "packages/crosswake-shell-core-android/build.gradle.kts"
      provides: "Gradle Library Build Definition"
    - path: "packages/crosswake-shell-core-android/src/main/java/dev/crosswake/shell/core/ActivationCoordinator.kt"
      provides: "Core Activation Logic"
  key_links:
    - from: "packages/crosswake-shell-core-android"
      to: "Android SDK"
      via: "gradlew build"
      pattern: "import android.content.Context"
---

<objective>
Extract the Android Crosswake core logic into a standalone Maven Central AAR artifact (Gradle library project).

Purpose: Isolate the shell's policy, routing, and bridge validation into an independent library, preventing host-app duplication and setting up for future standardizations.
Output: A compiling Gradle Android Library project in `packages/crosswake-shell-core-android`.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/76-core-shell-extraction-packaging/RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Initialize Gradle Library Project</name>
  <files>
    packages/crosswake-shell-core-android/build.gradle.kts
    packages/crosswake-shell-core-android/settings.gradle.kts
    packages/crosswake-shell-core-android/src/main/AndroidManifest.xml
  </files>
  <action>
    Create the `packages/crosswake-shell-core-android` directory structure.
    Create `build.gradle.kts` defining it as a `com.android.library` with standard Android target SDK (e.g., 34).
    Create `settings.gradle.kts` and a minimal `src/main/AndroidManifest.xml` (with package `dev.crosswake.shell.core`).
    Ensure the folder structure `src/main/java/dev/crosswake/shell/core` is ready. You can initialize Gradle using gradle wrapper locally or assume typical structure. To make it build independently, copy the root `gradle` wrapper files from `examples/android_shell_host` or rely on the system `gradle` if needed, but preferably setup a complete local build.
  </action>
  <verify>
    <automated>test -f packages/crosswake-shell-core-android/build.gradle.kts</automated>
  </verify>
  <done>Gradle build files and directory structure exist.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate and Expose Android Core Classes</name>
  <files>
    packages/crosswake-shell-core-android/src/main/java/dev/crosswake/shell/core/ActivationCoordinator.kt
    packages/crosswake-shell-core-android/src/main/java/dev/crosswake/shell/core/BridgeChannel.kt
    packages/crosswake-shell-core-android/src/main/java/dev/crosswake/shell/core/transfer/TransferCoordinator.kt
    packages/crosswake-shell-core-android/src/main/java/dev/crosswake/shell/core/packs/PackStore.kt
    packages/crosswake-shell-core-android/src/main/java/dev/crosswake/shell/core/DiagnosticExportManager.kt
  </files>
  <action>
    Copy the core Android classes (`ActivationCoordinator.kt`, `BridgeChannel.kt`, `TransferCoordinator.kt`, `PackStore.kt`, `DiagnosticExportManager.kt` and their corresponding data models) from `examples/android_shell_host/app/src/main/java/dev/crosswake/shell/` to `packages/crosswake-shell-core-android/src/main/java/dev/crosswake/shell/core/`.
    Refactor the copied Kotlin files by ensuring classes, functions, properties, and constructors have `public` (or default open) visibility as needed. Update their package declarations to `dev.crosswake.shell.core`.
    Extract necessary capability lambda/delegate types into public definitions so the host can inject implementations (e.g., Permissions, File Picker) instead of tightly coupling to host UI fragments.
  </action>
  <verify>
    <automated>cd packages/crosswake-shell-core-android && gradle build || ./gradlew build</automated>
  </verify>
  <done>The Gradle library project builds successfully in isolation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Host App -> Maven Core | Host injects capabilities, core executes them |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-76-02 | Tampering | BridgeChannel | mitigate | Ensure core bridge validation logic is closed for modification by host inheritance (keep critical classes or methods `final`). |
</threat_model>

<verification>
- Verify `packages/crosswake-shell-core-android` builds via `gradle build`.
- Verify core logic (Activation, Bridge) exists in the package.
</verification>

<success_criteria>
- `crosswake-shell-core-android` exists as a standalone Maven AAR project.
- The library compiles independently of any specific host app.
</success_criteria>

<output>
Create `.planning/phases/76-core-shell-extraction-packaging/76-02-SUMMARY.md` when done
</output>
