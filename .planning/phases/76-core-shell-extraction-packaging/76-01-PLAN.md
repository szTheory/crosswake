---
phase: 76-core-shell-extraction-packaging
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/crosswake-shell-core-ios/Package.swift
  - packages/crosswake-shell-core-ios/Sources/CrosswakeShellCore/ActivationCoordinator.swift
  - packages/crosswake-shell-core-ios/Sources/CrosswakeShellCore/BridgeChannel.swift
  - packages/crosswake-shell-core-ios/Sources/CrosswakeShellCore/TransferCoordinator.swift
  - packages/crosswake-shell-core-ios/Sources/CrosswakeShellCore/PackStore.swift
  - packages/crosswake-shell-core-ios/Sources/CrosswakeShellCore/DiagnosticExportManager.swift
autonomous: true
requirements:
  - CORE-01
  - CORE-03

must_haves:
  truths:
    - "iOS core logic exists as a standalone Swift Package"
    - "The package compiles independently without a host app"
  artifacts:
    - path: "packages/crosswake-shell-core-ios/Package.swift"
      provides: "SPM Package Definition"
    - path: "packages/crosswake-shell-core-ios/Sources/CrosswakeShellCore/ActivationCoordinator.swift"
      provides: "Core Activation Logic"
  key_links:
    - from: "packages/crosswake-shell-core-ios"
      to: "Foundation"
      via: "swift build"
      pattern: "import Foundation"
---

<objective>
Extract the iOS Crosswake core logic into a standalone Swift Package Manager (SPM) dependency.

Purpose: Isolate the shell's policy, routing, and bridge validation into an independent library, preventing host-app duplication and setting up for future standardizations.
Output: A compiling Swift Package in `packages/crosswake-shell-core-ios`.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/76-core-shell-extraction-packaging/RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Initialize SPM Package</name>
  <files>packages/crosswake-shell-core-ios/Package.swift</files>
  <action>
    Create the `packages/crosswake-shell-core-ios` directory structure.
    Create a `Package.swift` file defining the `CrosswakeShellCore` library, supporting iOS 15+.
    Ensure the folder structure `Sources/CrosswakeShellCore` is ready.
  </action>
  <verify>
    <automated>test -f packages/crosswake-shell-core-ios/Package.swift</automated>
  </verify>
  <done>Package.swift is defined and structure exists.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate and Expose iOS Core Classes</name>
  <files>
    packages/crosswake-shell-core-ios/Sources/CrosswakeShellCore/ActivationCoordinator.swift
    packages/crosswake-shell-core-ios/Sources/CrosswakeShellCore/BridgeChannel.swift
    packages/crosswake-shell-core-ios/Sources/CrosswakeShellCore/TransferCoordinator.swift
    packages/crosswake-shell-core-ios/Sources/CrosswakeShellCore/PackStore.swift
    packages/crosswake-shell-core-ios/Sources/CrosswakeShellCore/DiagnosticExportManager.swift
  </files>
  <action>
    Copy the core iOS classes (`ActivationCoordinator.swift`, `BridgeChannel.swift`, `TransferCoordinator.swift`, `PackStore.swift`, `DiagnosticExportManager.swift`, and their corresponding data models) from `examples/ios_shell_host/CrosswakeShell/` to `packages/crosswake-shell-core-ios/Sources/CrosswakeShellCore/`.
    Refactor these files by adding `public` access modifiers to classes, structs, properties, and initializers so they can be consumed by a host app.
    Extract necessary capability closure/delegate types into public definitions so the host can inject implementations (e.g., File Picker, Permissions) instead of tightly coupling to host UI classes.
  </action>
  <verify>
    <automated>cd packages/crosswake-shell-core-ios && swift build</automated>
  </verify>
  <done>The SPM package builds successfully in isolation without warnings or errors.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Host App -> SPM Core | Host injects capabilities, core executes them |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-76-01 | Tampering | BridgeChannel | mitigate | Ensure core bridge validation logic is public and unmodifiable by host inheritance (use final classes or structs). |
</threat_model>

<verification>
- Verify `packages/crosswake-shell-core-ios` builds via `swift build`.
- Verify core logic (Activation, Bridge) exists in the package and is marked `public`.
</verification>

<success_criteria>
- `crosswake-shell-core-ios` exists as a standalone Swift Package.
- The library compiles independently of any specific host app.
</success_criteria>

<output>
Create `.planning/phases/76-core-shell-extraction-packaging/76-01-SUMMARY.md` when done
</output>
