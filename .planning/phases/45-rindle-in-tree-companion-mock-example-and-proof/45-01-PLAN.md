---
phase: 45-rindle-in-tree-companion-mock-example-and-proof
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/crosswake/companions/rindle.ex
  - mix.exs
  - test/crosswake/proof/phase45_rindle_companion_test.exs
autonomous: true
requirements: [MEDIA-03, PROOF-01]

must_haves:
  truths:
    - "D-01: core owns only the companion contract and invariant primitives; example-host owns the mock upload/verify workflow"
    - "D-02: no new generic upload framework is added under Crosswake.Companions.Rindle"
    - "D-14: Crosswake.Companions.Rindle implements all six Crosswake.Companion callbacks with companion_id :rindle"
    - "D-15: validate_dependency/0 checks Code.ensure_loaded?(Rindle) and returns {:error, [Rindle]} when the optional dependency is absent"
    - "D-16: route_gated?/2 returns :pass and kill_switch_active?/1 returns false; Phase 45 does not invent a Rindle route-gating DSL"
    - "D-17: report_state/0 returns Crosswake.Companion.State with media details and no route-gate claims"
    - "D-18: no compile-time references to optional Rindle APIs are introduced outside Code.ensure_loaded?/1 checks"
    - "D-27: mix.exs includes rindle only when MIX_INCLUDE_RINDLE=1 and fails clearly when enabled but missing"
  artifacts:
    - path: "lib/crosswake/companions/rindle.ex"
      provides: "Second concrete in-tree companion implementation"
      contains: "def companion_id, do: :rindle"
    - path: "mix.exs"
      provides: "Conditional optional Rindle dependency"
      contains: "MIX_INCLUDE_RINDLE"
    - path: "test/crosswake/proof/phase45_rindle_companion_test.exs"
      provides: "Hermetic companion fail-closed proof"
      contains: "companion.dependency_missing"
---

<objective>
Add the concrete `Crosswake.Companions.Rindle` companion and the optional
dependency gate that lets Phase 45 prove fail-closed behavior without the
`rindle` dependency present.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/45-rindle-in-tree-companion-mock-example-and-proof/45-CONTEXT.md
@.planning/phases/45-rindle-in-tree-companion-mock-example-and-proof/45-RESEARCH.md
@.planning/phases/45-rindle-in-tree-companion-mock-example-and-proof/45-PATTERNS.md
@.planning/phases/45-rindle-in-tree-companion-mock-example-and-proof/45-VALIDATION.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add the Rindle companion module</name>
  <files>lib/crosswake/companions/rindle.ex, test/crosswake/proof/phase45_rindle_companion_test.exs</files>
  <read_first>
    - lib/crosswake/companion.ex
    - lib/crosswake/companion/state.ex
    - lib/crosswake/companions/rulestead.ex
    - .planning/phases/45-rindle-in-tree-companion-mock-example-and-proof/45-CONTEXT.md
  </read_first>
  <action>
    Create `lib/crosswake/companions/rindle.ex` defining `Crosswake.Companions.Rindle`.
    Implement `@behaviour Crosswake.Companion`.

    Required callback behavior:
    - `companion_id/0` returns `:rindle`.
    - `enabled?/1` reads `:enabled` from the supplied config map and defaults false.
    - `route_gated?/2` returns `:pass`.
    - `kill_switch_active?/1` returns `false`.
    - `validate_dependency/0` returns `:ok` only when `Code.ensure_loaded?(Rindle)` is true, otherwise `{:error, [Rindle]}`.
    - `report_state/0` reads `Application.get_env(:crosswake, :rindle, %{})`, returns `%Crosswake.Companion.State{companion_id: :rindle, enabled: enabled, dependency_status: :present | {:missing, [Rindle]}, gate_status: :unconfigured, kill_switch_status: :inactive, checked_at: System.monotonic_time(:millisecond), details: %{surface: :media, mode: :contract_only}}`.

    Add tests in `test/crosswake/proof/phase45_rindle_companion_test.exs` asserting these callbacks directly with the optional dependency absent. Use `async: false` because Application env is shared.
  </action>
  <verify>
    <automated>mix test test/crosswake/proof/phase45_rindle_companion_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - `lib/crosswake/companions/rindle.ex` defines `Crosswake.Companions.Rindle`
    - Source contains `@behaviour Crosswake.Companion`
    - `Crosswake.Companions.Rindle.companion_id()` returns `:rindle`
    - `Crosswake.Companions.Rindle.validate_dependency()` returns `{:error, [:"Elixir.Rindle"]}` when env var `MIX_INCLUDE_RINDLE` is unset and no Rindle dep is loaded
    - `route_gated?/2` returns `:pass`
    - `kill_switch_active?/1` returns `false`
    - `report_state/0` returns a `Crosswake.Companion.State` with `details.surface == :media`
    - Focused test command exits 0
  </acceptance_criteria>
  <done>The Rindle companion exists, compiles without the optional dependency, and fails closed when the dependency is absent.</done>
</task>

<task type="auto">
  <name>Task 2: Add MIX_INCLUDE_RINDLE conditional dependency wiring</name>
  <files>mix.exs</files>
  <read_first>
    - mix.exs
    - .planning/phases/43-rulestead-hermetic-advisory-proof-and-guide/43-01-PLAN.md
    - .planning/phases/45-rindle-in-tree-companion-mock-example-and-proof/45-PATTERNS.md
  </read_first>
  <action>
    Update `mix.exs` `deps/0` so it keeps the existing `base` list and existing
    `rulestead` conditional unchanged, then adds a parallel `rindle` conditional:
    `if System.get_env("MIX_INCLUDE_RINDLE") == "1", do: [{:rindle, "~> 0.1"}], else: []`.
    Return `base ++ rulestead ++ rindle`.

    Do not use `optional: true`. Do not set `MIX_INCLUDE_RINDLE` in the hermetic path.
    Do not commit a `mix.lock` entry for `rindle` after hermetic work.
  </action>
  <verify>
    <automated>mix compile --warnings-as-errors && grep -q "MIX_INCLUDE_RINDLE" mix.exs && test "$(grep -c rindle mix.lock 2>/dev/null || true)" = "0"</automated>
  </verify>
  <acceptance_criteria>
    - `mix.exs` contains `MIX_INCLUDE_RINDLE`
    - `mix.exs` contains a `{:rindle, "~> 0.1"}` conditional dependency
    - `mix.exs` still contains `MIX_INCLUDE_RULESTEAD`
    - `mix.exs` does not add `optional: true` for Rindle
    - Hermetic `mix compile --warnings-as-errors` exits 0 with env var unset
    - `mix.lock` contains no `rindle` line after hermetic work
  </acceptance_criteria>
  <done>Rindle can be included only in advisory lanes without changing the hermetic dep tree.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Prove doctor fail-closed behavior for enabled-missing Rindle</name>
  <files>test/crosswake/proof/phase45_rindle_companion_test.exs</files>
  <read_first>
    - test/crosswake/proof/phase42_rulestead_companion_test.exs
    - lib/crosswake/doctor.ex
    - lib/crosswake/companions/rindle.ex
  </read_first>
  <action>
    Extend `phase45_rindle_companion_test.exs` with a hermetic doctor fixture
    following `phase42_rulestead_companion_test.exs`: register
    `[Crosswake.Companions.Rindle]` in `Application.put_env(:crosswake, :companions, ...)`,
    set `Application.put_env(:crosswake, :rindle, %{enabled: true})`, create a temp
    install manifest, run `Crosswake.Doctor.run/1`, and assert an error finding with
    `code == "companion.dependency_missing"` and `check == "companion.rindle"`.

    Add a disabled-companion assertion showing `%{enabled: false}` does not emit
    `companion.dependency_missing`.
  </action>
  <verify>
    <automated>mix test test/crosswake/proof/phase45_rindle_companion_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - Test asserts enabled Rindle with missing optional dependency emits `companion.dependency_missing`
    - Test asserts finding severity is `:error`
    - Test asserts finding check is `companion.rindle`
    - Test asserts disabled Rindle emits no `companion.dependency_missing` finding
    - Focused test command exits 0
  </acceptance_criteria>
  <done>Doctor fail-closed behavior is proven for the second in-tree companion.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Host config -> companion registry | Host can enable Rindle in Application env |
| Companion -> optional dependency | `Code.ensure_loaded?(Rindle)` is the only dependency presence check |
| Doctor -> operator truth | Missing enabled dependency must become an explicit error |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-45-01 | Tampering | Optional dependency boundary | mitigate | Keep all Rindle dependency checks behind `Code.ensure_loaded?(Rindle)` and prove missing dep returns `{:error, [Rindle]}` |
| T-45-02 | Information disclosure | CI dependency state | mitigate | Do not commit `rindle` to `mix.lock`; advisory inclusion is env-gated only |
| T-45-03 | Elevation of privilege | Rindle route behavior | mitigate | `route_gated?/2` returns `:pass`; no new route authority or gate DSL ships in this phase |
</threat_model>

<verification>
Run:
- `mix test test/crosswake/proof/phase45_rindle_companion_test.exs`
- `mix compile --warnings-as-errors`
- `mix test --exclude requires_example_host --exclude advisory_only`
</verification>

<success_criteria>
- MEDIA-03 has a concrete in-tree Rindle companion.
- PROOF-01 fail-closed behavior is proven with the optional dependency absent.
- No generic upload framework or route-gating expansion is added.
</success_criteria>
