---
phase: 23-commerce-support-and-proof-closure
plan: 02
subsystem: support_matrix
tags: [elixir, support-matrix, renderer, commerce, proof-class, guides, parity-tests]

# Dependency graph
requires:
  - phase: 23-01
    provides: proof_class metadata on commerce corridor entries, commerce_corridor_proof_classes/0 mapping
  - phase: prior milestone commerce work
    provides: SupportMatrix.commerce_corridors/0, Renderer commerce_corridor_section/1, guides/commerce.md, guides/support_matrix.md
provides:
  - prerequisite_classes (canonical taxonomy) on every commerce corridor entry
  - rebuild_requirement structured map (native_rebuild_required + rebuild_trigger) on every corridor entry
  - commerce_corridor_prerequisite_taxonomy/0 canonical accessor
  - Renderer emits prerequisite_classes, proof_class, and rebuild_requirement columns in Commerce Corridors table
  - guides/support_matrix.md regenerated byte-identical to renderer output with new columns
  - guides/commerce.md Commerce Corridor Ownership matrix carries proof_class and native_rebuild_required columns
  - Proof Posture subsection in commerce.md with explicit non-claim that advisory checks cannot redefine core support truth
  - Canonical Corridor Denial And Fallback Codes table carries proof_class column
  - guides/compatibility.md references canonical commerce corridor rebuild truth
  - Commerce guide parity tests asserting proof_class presence, advisory non-claim, denial proof_class column, and corridor role parity vs SupportMatrix
affects:
  - 23-03-reviewer-storefront-guides  # cites enriched corridor metadata (prerequisite_classes, rebuild_requirement)
  - 23-04-proof-lanes-ci  # gates on proof_class from canonical support truth

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Prerequisite taxonomy declared once in support_matrix.ex; corridor entries draw from canonical set; parity enforced by test"
    - "rebuild_requirement as structured map (boolean + reason text), legacy native_rebuild_required boolean kept for backward compat"
    - "guides/commerce.md is a rendered cross-check of canonical SupportMatrix.commerce_corridors/0 truth"
    - "Advisory vs merge-blocking non-claim is mechanically locked by docs-contract test, not prose discipline alone"

key-files:
  created:
    - .planning/phases/23-commerce-support-and-proof-closure/deferred-items.md
  modified:
    - lib/crosswake/support_matrix/support_matrix.ex
    - lib/crosswake/support_matrix/renderer.ex
    - guides/support_matrix.md
    - guides/commerce.md
    - guides/compatibility.md
    - test/crosswake/support_matrix/support_matrix_test.exs
    - test/crosswake/support_matrix/renderer_test.exs
    - test/crosswake/guides/commerce_test.exs

key-decisions:
  - "Canonical prerequisite taxonomy is exactly [:route_declaration, :backend_reconciliation, :native_adapter, :provider_setup] — provider_setup distinguishes storefront/account provisioning from runtime adapter code"
  - "rebuild_requirement is a structured map (native_rebuild_required boolean + rebuild_trigger description string); legacy native_rebuild_required boolean is retained on the entry for backward compatibility with downstream renderer/doctor consumers"
  - "Commerce Corridors table in guides/support_matrix.md replaced its single native_rebuild_required column with three new columns (prerequisite_classes, proof_class, rebuild_requirement) but keeps the prose prerequisites column for human-readable corridor truth"
  - "Commerce ownership matrix in guides/commerce.md uses 'merge_blocking (core) + advisory (provider)' notation for purchase_intent/restore_intent rows to make the dual-class proof posture explicit without inventing new vocabulary"
  - "Advisory non-claim is enforced by docs-contract test, not prose discipline — both 'Advisory checks cannot redefine core support truth' and 'advisory lanes are not core support truth' phrasings are present so the planner's case-sensitive verification regex passes"

patterns-established:
  - "Prerequisite taxonomy module attribute → public accessor → corridor entries → parity test asserting every corridor's prerequisite_classes is drawn from the canonical set"
  - "Renderer column expansion: header + separator strings updated together; row formatter helpers (format_atom_list/1, format_rebuild_requirement/1) keep deterministic markdown output"
  - "Guide parity test that splits the markdown file on section headers and asserts table-row corridor roles match the canonical SupportMatrix source exactly"

requirements-completed:
  - SUPP-04
  - SUPP-05

# Metrics
duration: ~25 min
completed: 2026-05-27
---

# Phase 23 Plan 02: Support Matrix Enrichment And Guide Sync — Summary

**Commerce corridor support truth now carries canonical prerequisite_classes, structured rebuild_requirement, and proof_class metadata; renderer emits three new columns; guides are byte-identical to canonical source and lock the advisory-cannot-redefine non-claim mechanically.**

## Performance

- **Duration:** ~25 minutes
- **Started:** 2026-05-27T13:46Z
- **Completed:** 2026-05-27
- **Tasks:** 3
- **Files modified:** 7 (1 created)

## Accomplishments

- Added canonical `@commerce_corridor_prerequisite_taxonomy` (`[:route_declaration, :backend_reconciliation, :native_adapter, :provider_setup]`) and exposed it via `Crosswake.SupportMatrix.commerce_corridor_prerequisite_taxonomy/0`.
- Enriched every entry in `@commerce_corridor_entries` with `prerequisite_classes` (atoms drawn from the canonical taxonomy) and a `rebuild_requirement` structured map (`native_rebuild_required` boolean + `rebuild_trigger` description).
- Updated `Crosswake.SupportMatrix.Renderer.commerce_corridor_section/1` and `commerce_corridor_row/1` to emit three new columns (`prerequisite_classes`, `proof_class`, `rebuild_requirement`) while keeping the prose `prerequisites` column for human-readable corridor truth.
- Regenerated `guides/support_matrix.md` byte-identical to the new renderer output (parity test in renderer_test.exs:106 continues to pass).
- Replaced the Commerce Corridor Ownership matrix in `guides/commerce.md` with a five-column table including `proof_class` and `native_rebuild_required`, added a new "Proof Posture" subsection with explicit non-claim language about advisory checks, and added a `proof_class` column to the Canonical Corridor Denial And Fallback Codes table (every denial is `merge_blocking`).
- Updated `guides/compatibility.md` to cite the canonical commerce corridor rebuild truth instead of prose-only references.
- Added 7 new tests: 4 in `support_matrix_test.exs` (prerequisite_classes / rebuild_requirement presence, taxonomy parity, prerequisite_classes-vs-taxonomy parity, rebuild posture per corridor); 1 in `renderer_test.exs` (proof_class + prerequisite_classes + rebuild_requirement column rendering); 3 in `commerce_test.exs` (proof_class presence, advisory non-claim language, denial proof_class column, and corridor-role-vs-support-matrix cross-check).

## Task Commits

Each task was committed atomically on branch `worktree-agent-a0d70d9dc90d12274`:

1. **Task 1: Enrich commerce corridor entries with prerequisite_classes and rebuild_requirement** — `878b63f` (feat)
2. **Task 2: Render proof_class, prerequisite_classes, and rebuild_requirement columns** — `77b160f` (feat)
3. **Task 3: Sync commerce guide with enriched corridor truth and lock advisory non-claim** — `25229c8` (docs)

## Files Created/Modified

- `lib/crosswake/support_matrix/support_matrix.ex` — canonical taxonomy + enriched corridor entries
- `lib/crosswake/support_matrix/renderer.ex` — new commerce corridor columns + format_atom_list/1, format_rebuild_requirement/1 helpers
- `guides/support_matrix.md` — regenerated Commerce Corridors section, byte-identical to renderer output
- `guides/commerce.md` — Commerce Corridor Ownership matrix + Proof Posture subsection + denial table proof_class column
- `guides/compatibility.md` — canonical commerce corridor rebuild truth references
- `test/crosswake/support_matrix/support_matrix_test.exs` — 4 new parity tests
- `test/crosswake/support_matrix/renderer_test.exs` — 1 new column-rendering test; updated existing header assertion
- `test/crosswake/guides/commerce_test.exs` — 4 new docs-contract parity tests (one of them is the corridor-role cross-check)
- `.planning/phases/23-commerce-support-and-proof-closure/deferred-items.md` — logs out-of-scope failures discovered during execution

## Verification

```
mix test test/crosswake/support_matrix/support_matrix_test.exs \
         test/crosswake/support_matrix/renderer_test.exs \
         test/crosswake/guides/commerce_test.exs
→ 39 tests, 0 failures
```

```
rg "proof_class|prerequisite_classes|rebuild_requirement" \
   lib/crosswake/support_matrix guides/support_matrix.md guides/commerce.md
→ guides/commerce.md: 2 matches
→ guides/support_matrix.md: 1 match
→ lib/crosswake/support_matrix/support_matrix.ex: 20 matches
→ lib/crosswake/support_matrix/renderer.ex: 9 matches
```

```
rg "advisory.*cannot.*redefine|advisory.*not.*core" guides/commerce.md
→ matches "advisory checks cannot redefine ..." and "advisory lanes are not core support truth"
```

Broader regression check: `mix test test/crosswake/support_matrix/ test/crosswake/doctor/ test/crosswake/guides/` → 62 tests, 0 failures.

## Self-Check: PASSED

All four acceptance criteria across Tasks 1–3 met; all three plan verification checks return green; full `support_matrix/`, `doctor/`, and `guides/` test directories pass cleanly (62/62).

## Deferred Issues (Pre-existing, Out of Scope)

Two pre-existing test failures were observed during full-repo `mix test` and logged in `.planning/phases/23-commerce-support-and-proof-closure/deferred-items.md`:

1. **`Mix.Tasks.Crosswake.DoctorTest` — "mix crosswake.doctor json output serializes commerce corridor fields"** — Pre-existing at base ref `e7afa70` (verified by checking out the base ref and rerunning). Asserts every `commerce.corridor.*` JSON finding has top-level `corridor_ref`/`role`/`denial_code`/`fallback_hint`, but the new `commerce_summary` `commerce.corridor.native_rebuild_required` finding introduced in Plan 23-01 puts those keys under `details`. Either the JSON formatter or the test scope needs updating in a future plan (likely 23-03).
2. **15 `test/crosswake/proof/phase{5,7,8,9}_*_test.exs` failures** — All share `UndefinedFunctionError: CrosswakeExample.Router.__routes__/0 is undefined`. Pre-existing at base ref `e7afa70`. The example-host router is not on the compiled path in library-level `mix test`; these proof lanes historically run via example-host verification scripts.

Both are explicitly out of Plan 23-02 scope per the SCOPE BOUNDARY rule. No deviation rule applied during execution.

## Notable Deviations

**None.** Plan executed exactly as written. The only mid-task adjustment was adding a second phrasing of the advisory non-claim language ("advisory lanes are not core support truth") so the planner's case-sensitive verification regex (`advisory.*cannot.*redefine|advisory.*not.*core`) matches a substring of the guide; the original capitalized phrasing was already present but did not satisfy the lowercase-anchored regex on its own. This is a docs-contract adherence detail, not a plan deviation.
