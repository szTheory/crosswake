---
phase: 147-arc-fixture-and-showcase-foundation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - examples/phoenix_host/lib/crosswake_example/showcase/catalog.ex
  - examples/phoenix_host/test/crosswake_example/showcase/catalog_test.exs
autonomous: true
requirements:
  - SHOW-01
  - SHOW-03
requirements_addressed:
  - SHOW-01
  - SHOW-03
user_setup: []
must_haves:
  truths:
    - "Hub catalog exposes SaaS/admin, field-service, and learning/training lanes for the first screen per D-03."
    - "Route-owner/support labels use visible text and allowed vocabulary, not color-only or broad support claims per D-16 and D-17."
    - "Catalog route IDs and posture are verified against compiled route metadata per D-13, D-14, and D-15."
  artifacts:
    - path: "examples/phoenix_host/lib/crosswake_example/showcase/catalog.ex"
      provides: "Product-facing lane catalog, allowed labels, route IDs, CTA copy, pressure notes"
      exports: ["lanes/0", "cards/0", "route_ids/0", "allowed_support_labels/0"]
    - path: "examples/phoenix_host/test/crosswake_example/showcase/catalog_test.exs"
      provides: "Compiled route metadata and label vocabulary proof"
  key_links:
    - from: "examples/phoenix_host/lib/crosswake_example/showcase/catalog.ex"
      to: "examples/phoenix_host/lib/crosswake_example/router.ex"
      via: "route_id/path/runtime/offline/security verified in catalog_test"
      pattern: "Crosswake.Policy.RouterMetadata.fetch"
---

<objective>
Create the curated showcase route-card catalog and the drift tests that keep product labels aligned to compiled route truth.

Purpose: SHOW-03 depends on visible support labels that are useful to users without becoming a second route-policy source.
Output: `CrosswakeExample.Showcase.Catalog` plus route metadata and vocabulary tests.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/147-arc-fixture-and-showcase-foundation/147-CONTEXT.md
@.planning/phases/147-arc-fixture-and-showcase-foundation/147-RESEARCH.md
@.planning/phases/147-arc-fixture-and-showcase-foundation/147-PATTERNS.md
@.planning/phases/147-arc-fixture-and-showcase-foundation/147-UI-SPEC.md
@examples/phoenix_host/lib/crosswake_example/router.ex
@examples/phoenix_host/lib/crosswake_example/saas_portal/fixtures.ex
@guides/route_policy.md
@guides/support_matrix.md
@lib/crosswake/policy/router_metadata.ex
@examples/phoenix_host/lib/crosswake_example/crosswake/policy.ex
</context>

## Artifacts this phase produces

- `CrosswakeExample.Showcase.Catalog`
- `CrosswakeExample.Showcase.Catalog.lanes/0`
- `CrosswakeExample.Showcase.Catalog.cards/0`
- `CrosswakeExample.Showcase.Catalog.route_ids/0`
- `CrosswakeExample.Showcase.Catalog.allowed_support_labels/0`
- `examples/phoenix_host/test/crosswake_example/showcase/catalog_test.exs`
- Summary file `.planning/phases/147-arc-fixture-and-showcase-foundation/147-02-SUMMARY.md`

<must_haves>
- Three lane cards are represented as data: SaaS/admin, field-service, learning/training.
- Allowed support labels include only: Available today, Proof-backed example, Demo pressure, Advisory evidence, Future gap, Next-pack candidate.
- Runtime/offline labels include visible text such as LiveView route, Cached read-only, Offline island, Native screen, Requires native runtime, Demo pressure, Future native-control candidate.
- Catalog does not duplicate the full Crosswake route-policy DSL.
</must_haves>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write catalog metadata tests before the catalog implementation</name>
  <files>examples/phoenix_host/test/crosswake_example/showcase/catalog_test.exs</files>
  <read_first>
    - examples/phoenix_host/test/crosswake_example/router_test.exs
    - examples/phoenix_host/test/crosswake_example/bridge_proof_live_test.exs
    - lib/crosswake/policy/router_metadata.ex
    - examples/phoenix_host/lib/crosswake_example/crosswake/policy.ex
    - examples/phoenix_host/lib/crosswake_example/router.ex
    - .planning/phases/147-arc-fixture-and-showcase-foundation/147-VALIDATION.md
  </read_first>
  <behavior>
    - Test 1: `Catalog.lanes/0` returns exactly SaaS/admin, field-service, and learning/training lane identifiers.
    - Test 2: every catalog card has a route ID/path that exists in compiled `CrosswakeExample.Router` metadata.
    - Test 3: card runtime/offline/security/capability posture matches compiled metadata when that route already exists.
    - Test 4: all support labels come from the allowed vocabulary and no broad unqualified support wording is accepted.
  </behavior>
  <action>Create `catalog_test.exs` first. Use `Phoenix.Router.routes(CrosswakeExample.Router)` or `CrosswakeExample.Router.__routes__()` and `Crosswake.Policy.RouterMetadata.fetch/1` to build the compiled route map. Tests must cite D-13, D-14, D-15, D-16, and D-17 in comments or assertion messages for traceability. Use source assertions, not screenshots. Do not use regex-only parsing over `router.ex` as the authority.</action>
  <verify>
    <automated>cd examples/phoenix_host && mix test test/crosswake_example/showcase/catalog_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - The first run fails because `CrosswakeExample.Showcase.Catalog` does not exist or lacks required functions.
    - Test failure messages name route IDs and label categories.
    - Tests include all three lane identifiers and the allowed support-label vocabulary.
  </acceptance_criteria>
  <done>Catalog behavior is specified before implementation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement the small product-facing showcase catalog</name>
  <files>examples/phoenix_host/lib/crosswake_example/showcase/catalog.ex, examples/phoenix_host/test/crosswake_example/showcase/catalog_test.exs</files>
  <read_first>
    - examples/phoenix_host/lib/crosswake_example/saas_portal/fixtures.ex
    - guides/route_policy.md
    - guides/support_matrix.md
    - .planning/phases/147-arc-fixture-and-showcase-foundation/147-UI-SPEC.md
    - examples/phoenix_host/lib/crosswake_example/router.ex
  </read_first>
  <action>Create `CrosswakeExample.Showcase.Catalog` with module-attribute data and public accessors `lanes/0`, `cards/0`, `route_ids/0`, and `allowed_support_labels/0`. Per D-03, include SaaS/admin, field-service, and learning/training. Per D-04 and D-05, expose diagnostics/proof routes only as secondary links, not the lane-card center. Per D-13 and D-14, store curated product copy, route IDs, paths, visible labels, capability chips, CTA labels, and v20 pressure notes without duplicating the full route-policy DSL. Per D-16 and D-17, use visible text labels and honest support labels only. Per D-18, do not add full capability-map classification.</action>
  <verify>
    <automated>cd examples/phoenix_host && mix test test/crosswake_example/showcase/catalog_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - `Catalog.lanes/0` returns exactly three lane records in a stable order: SaaS/admin, field-service, learning/training.
    - Each lane has `id`, `heading`, `body`, `primary_path`, `primary_route_id`, `primary_cta`, `runtime_labels`, `support_labels`, `capability_chips`, and `boundary_note` fields or equivalent documented keys.
    - Existing route-backed cards match compiled metadata for path/runtime/offline/security.
    - Field-service native-pressure copy uses labels such as Demo pressure, Future gap, or Next-pack candidate instead of claiming shipped scanner/capture support.
  </acceptance_criteria>
  <done>Catalog tests pass and the catalog is ready for `HubLive` rendering.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| catalog data -> rendered hub | Static server-side copy becomes user-facing support truth. |
| compiled route metadata -> curated labels | Route policy is authoritative; product copy must not drift. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-147-04 | Spoofing | `Showcase.Catalog` support labels | mitigate | Allowed support-label vocabulary and compiled route metadata tests. |
| T-147-05 | Tampering | route ID/path mapping | mitigate | `catalog_test.exs` fails when catalog route IDs or paths drift from compiled router truth. |
| T-147-06 | Information Disclosure | hub copy | mitigate | Keep raw diagnostics and implementation guts secondary per D-04. |
| T-147-SC | Tampering | package installs | accept | No new npm/pip/cargo packages; use existing Phoenix/ExUnit stack. |
</threat_model>

<verification>
- `cd examples/phoenix_host && mix test test/crosswake_example/showcase/catalog_test.exs`
- No schema push gate applies: files modified do not match the provided schema-relevant patterns.
</verification>

<success_criteria>
- SHOW-03 has a tested catalog foundation.
- SHOW-01 has lane metadata ready for the root hub.
- Catalog labels stay honest and route-policy-backed.
</success_criteria>

<output>
Create `.planning/phases/147-arc-fixture-and-showcase-foundation/147-02-SUMMARY.md` when done.
</output>
