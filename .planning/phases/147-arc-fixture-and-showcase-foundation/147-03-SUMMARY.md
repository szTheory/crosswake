---
phase: 147-arc-fixture-and-showcase-foundation
plan: 03
subsystem: example-host fixtures
tags: [phoenix, ecto, mix, fixtures, showcase-reset]

requires:
  - phase: 147-arc-fixture-and-showcase-foundation
    provides: v19 showcase foundation planning and lane boundaries
provides:
  - Deterministic server-side showcase reset contract for SaaS/admin, field-service/native-pressure, and learning/training lanes
  - Reset-safe selective-native fixture seeding
  - Shared seed script and local Mix CLI reset delegation
affects: [phase-147, phase-148, phase-149, phase-150, phase-151, showcase-fixtures]

tech-stack:
  added: []
  patterns:
    - Example-host-local reset orchestrator delegates to lane-owned fixture/context helpers
    - Stable reset digest derived from deterministic lane records and counts
    - Server reset explicitly reports browser_state_reset: false

key-files:
  created:
    - examples/phoenix_host/lib/crosswake_example/showcase/fixtures.ex
    - examples/phoenix_host/lib/crosswake_example/showcase/reset.ex
    - examples/phoenix_host/test/crosswake_example/showcase/reset_test.exs
  modified:
    - examples/phoenix_host/lib/crosswake_example/flashcards.ex
    - examples/phoenix_host/lib/crosswake_example/selective_native/fixtures.ex
    - examples/phoenix_host/priv/repo/seeds.exs
    - examples/phoenix_host/mix.exs

key-decisions:
  - "Reset remains example-host-local and delegates to SaaSPortal, SelectiveNative, and Flashcards ownership instead of becoming a core fixture engine."
  - "The local Mix reset alias starts only the Repo it needs, avoiding a Phoenix endpoint port bind for a data reset command."
  - "Browser-owned IndexedDB/outbox state remains outside server reset and is reported as browser_state_reset: false."

patterns-established:
  - "Showcase.Reset.reset!/0 returns %{counts, digest, browser_state_reset} as the shared reset contract."
  - "SelectiveNative.Fixtures.seed/0 is reset-safe and child-before-parent deletes submissions before claims."
  - "Flashcards.reset_seed!/0 owns deterministic deck/card server data and clears server-side learning sync/progress rows."

requirements-completed: [SHOW-02]

duration: 7m 19s
completed: 2026-07-09
status: complete
---

# Phase 147 Plan 03: Deterministic Showcase Reset Summary

**Deterministic Phoenix example-host showcase reset with stable counts, digest output, reset-safe lane fixtures, and shared seed/CLI delegation.**

## Performance

- **Duration:** 7m 19s
- **Started:** 2026-07-09T19:46:50Z
- **Completed:** 2026-07-09T19:54:09Z
- **Tasks:** 3
- **Files modified:** 8

## Accomplishments

- Added `CrosswakeExample.Showcase.Reset.reset!/0` returning stable `counts`, a SHA-256 `digest`, and `browser_state_reset: false`.
- Added deterministic fixture/reset helpers for SaaS/admin counts, selective-native persisted claims, and Flashcards learning/training records.
- Replaced inline `priv/repo/seeds.exs` seeding with the shared reset contract.
- Added `mix showcase.reset`, which delegates to the same contract and prints counts, digest, and the browser-state non-claim.

## Task Commits

1. **Task 1: Write reset idempotency and non-claim tests** - `804d48c4` (test)
2. **Task 2: Implement lane-owned deterministic reset orchestration** - `70fe0d0c` (feat)
3. **Task 3: Delegate seeds and local CLI reset to the same contract** - `41d1c332` (feat)

## Files Created/Modified

- `examples/phoenix_host/lib/crosswake_example/showcase/fixtures.ex` - static SaaS/admin foundation counts and digest components.
- `examples/phoenix_host/lib/crosswake_example/showcase/reset.ex` - server-side reset orchestrator and stable digest contract.
- `examples/phoenix_host/lib/crosswake_example/flashcards.ex` - deterministic learning/training reset helper and digest components.
- `examples/phoenix_host/lib/crosswake_example/selective_native/fixtures.ex` - reset-safe selective-native claim seeding.
- `examples/phoenix_host/priv/repo/seeds.exs` - seed script delegates to `CrosswakeExample.Showcase.Reset.reset!/0`.
- `examples/phoenix_host/mix.exs` - adds `mix showcase.reset` alias.
- `examples/phoenix_host/test/crosswake_example/showcase/reset_test.exs` - idempotency, digest, duplicate-row, non-claim, and lane-count tests.

## Decisions Made

- Kept reset orchestration in the example host, not Crosswake core.
- Used existing lane contexts and schemas only; no future-domain Ecto schema modeling was added.
- Built the digest from deterministic counts and fixed lane records, not autogenerated database IDs or timestamps.
- Kept browser offline state explicitly out of scope with `browser_state_reset: false` in results and CLI/seed output.

## Deviations from Plan

None - plan executed as written.

## Known Stubs

None.

## Threat Flags

None. The reset mutation surface remains local to seeds/Mix and uses fixed context functions with no user-supplied table names or scopes.

## Issues Encountered

None. The expected TDD RED failure occurred because `CrosswakeExample.Showcase.Reset.reset!/0` did not exist before implementation.

## Verification

- `cd examples/phoenix_host && mix test test/crosswake_example/showcase/reset_test.exs` - PASS, 4 tests, 0 failures.
- `cd examples/phoenix_host && mix showcase.reset` - PASS, exited 0 and printed counts, digest `be19c567f40a2a41bba8632bc333f5ef731248f6703397320edc2ec4c18897f8`, and `browser_state_reset=false`.
- Schema push detector - PASS, no Payload/Prisma/Drizzle/Supabase/TypeORM schema files changed.
- Stub scan - PASS, no TODO/FIXME/placeholder/coming-soon/not-available stubs in created or modified plan files.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

Wave 2 can call `CrosswakeExample.Showcase.Reset.reset!/0` from a gated local/test reset endpoint without duplicating fixture logic. Later domain-lane phases can extend lane-owned fixture helpers while keeping the showcase reset as an orchestrator only.

## Self-Check: PASSED

- Created files exist on disk.
- Task commits `804d48c4`, `70fe0d0c`, and `41d1c332` exist in git history.
- Required verification commands passed.
- Shared `.planning/STATE.md`, `.planning/ROADMAP.md`, and `.planning/REQUIREMENTS.md` were not updated because the Wave 1 orchestrator owns shared tracking.

---
*Phase: 147-arc-fixture-and-showcase-foundation*
*Completed: 2026-07-09*
