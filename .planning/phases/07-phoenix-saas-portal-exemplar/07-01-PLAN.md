---
phase: 07-phoenix-saas-portal-exemplar
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - examples/phoenix_host/lib/crosswake_example/router.ex
  - examples/phoenix_host/lib/crosswake_example/saas_portal/auth.ex
  - examples/phoenix_host/lib/crosswake_example/saas_portal/on_mount.ex
  - examples/phoenix_host/lib/crosswake_example/saas_portal/fixtures.ex
  - examples/phoenix_host/lib/crosswake_example/saas_portal/accounts.ex
  - examples/phoenix_host/lib/crosswake_example/saas_portal/approvals.ex
  - test/crosswake/proof/phase7_saas_lane_test.exs
autonomous: true
requirements:
  - SAAS-01
must_haves:
  truths:
    - "The shared example host carries one authenticated `/saas` lane that stays mostly `:live_view` and feels like ordinary Phoenix app code."
    - "Auth remains host-owned example code with one lightweight role split and explicit authorization boundaries."
    - "The SaaS lane stays within the locked route budget and does not drift into offline, native-screen, or transfer-first pressure."
  artifacts:
    - path: "examples/phoenix_host/lib/crosswake_example/router.ex"
      provides: "Shared-host `/saas` route declarations and Crosswake route metadata"
    - path: "examples/phoenix_host/lib/crosswake_example/saas_portal/auth.ex"
      provides: "Host-owned session and authorization boundary for the SaaS lane"
    - path: "examples/phoenix_host/lib/crosswake_example/saas_portal/on_mount.ex"
      provides: "LiveView `on_mount` boundary for signed-in user and role enforcement"
    - path: "examples/phoenix_host/lib/crosswake_example/saas_portal/fixtures.ex"
      provides: "Minimal-realistic seeded users, account, and approval queue"
    - path: "test/crosswake/proof/phase7_saas_lane_test.exs"
      provides: "Executable proof of route presence, route count, and lane boundaries"
  key_links:
    - from: "examples/phoenix_host/lib/crosswake_example/router.ex"
      to: "examples/phoenix_host/README.md"
      via: "implemented route set matches the locked SaaS lane contract"
      pattern: "/saas/dashboard|/saas/accounts/:id|/saas/approvals|/saas/approvals/:id|/saas/settings/profile"
    - from: "examples/phoenix_host/lib/crosswake_example/saas_portal/auth.ex"
      to: ".planning/phases/07-phoenix-saas-portal-exemplar/07-CONTEXT.md"
      via: "auth remains host-owned and limited to one lightweight role split"
      pattern: "member|approver|host-owned"
---

<objective>
Establish the shared-host SaaS lane scaffolding and authenticated route boundary without widening product scope.

Purpose: Fulfill the host-lane and auth-boundary foundations of SAAS-01 by adding a believable Phoenix-owned SaaS slice inside the shared example host.
Output: A `/saas` route group, host-owned auth scaffolding, minimal fixtures, and initial proof assertions that lock the lane budget and boundary.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-phoenix-saas-portal-exemplar/07-CONTEXT.md
@.planning/phases/07-phoenix-saas-portal-exemplar/07-RESEARCH.md
@.planning/phases/07-phoenix-saas-portal-exemplar/07-PATTERNS.md
@.planning/phases/06-adopter-profile-matrix-and-pressure-contract/06-CONTEXT.md
@guides/adopter_profiles.md
@guides/native_shell.md
@examples/phoenix_host/README.md
@examples/phoenix_host/lib/crosswake_example/router.ex
@examples/phoenix_host/lib/crosswake_example/crosswake/policy.ex
@test/crosswake/proof/adopter_profile_contract_test.exs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add the shared-host `/saas` lane and route-local Crosswake declarations</name>
  <files>examples/phoenix_host/lib/crosswake_example/router.ex, test/crosswake/proof/phase7_saas_lane_test.exs</files>
  <behavior>
    - Test 1: The shared example host exposes exactly the five locked SaaS routes under `/saas`.
    - Test 2: All Phase 7 SaaS routes compile as `:live_view` routes and stay inside one shared `crosswake_defaults` scope.
    - Test 3: The router does not introduce packs, transfers, offline-island declarations, or `:native_screen` ownership for the SaaS lane.
  </behavior>
  <action>Extend `examples/phoenix_host/lib/crosswake_example/router.ex` with one `/saas` route group that keeps shared defaults at the scope level and declares the locked route set from Phase 7 context. Use route ids and module boundaries that clearly map to `CrosswakeExample.SaaSPortal.*`. Make the authenticated boundary explicit in the router with ordinary Phoenix and LiveView wiring: plug-based session loading where appropriate, one `live_session` for signed-in SaaS routes, and a lane-specific `on_mount` hook for signed-in and role-aware assigns. Keep capability declarations absent for now except where the later approval-detail route will need them, and fail the proof if the route set exceeds the locked budget or drifts into the wrong runtime classes.</action>
  <verify>
    <automated>mix test test/crosswake/proof/phase7_saas_lane_test.exs</automated>
  </verify>
  <done>The repo has a falsifiable `/saas` lane in the shared example host that matches the locked route budget and stays clearly LiveView-owned.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add host-owned auth and minimal SaaS fixture scaffolding</name>
  <files>examples/phoenix_host/lib/crosswake_example/saas_portal/auth.ex, examples/phoenix_host/lib/crosswake_example/saas_portal/on_mount.ex, examples/phoenix_host/lib/crosswake_example/saas_portal/fixtures.ex, examples/phoenix_host/lib/crosswake_example/saas_portal/accounts.ex, examples/phoenix_host/lib/crosswake_example/saas_portal/approvals.ex, examples/phoenix_host/lib/crosswake_example/router.ex, test/crosswake/proof/phase7_saas_lane_test.exs</files>
  <behavior>
    - Test 1: The SaaS lane exposes one signed-in boundary with one lightweight role split only.
    - Test 2: Authorization is enforced in ordinary Phoenix and LiveView places rather than by route policy alone, specifically through router/live-session setup, `on_mount`, and the guarded approval action path.
    - Test 3: Fixture realism stays minimal: one account, two seeded users, and a small approval queue.
  </behavior>
  <action>Create the host-owned SaaS support modules that the later LiveViews will use: auth/session helpers, one lane-specific `on_mount` module, role checks, and minimal domain fixtures for account health and approvals. Wire those modules into the router and signed-in LiveView lane so the auth boundary is concretely enforced in the ordinary Phoenix places locked by context: plug or session setup, `live_session` or equivalent route boundary, `on_mount`, and later the guarded approval action itself. Keep the implementation example-app-local and explicit that it is not a Crosswake auth API. Wire proof assertions that lock the one-account, two-user, approval-centric posture so later work does not widen into a full auth product or backoffice system.</action>
  <verify>
    <automated>mix test test/crosswake/proof/phase7_saas_lane_test.exs</automated>
    <automated>rg -n 'member|approver|account|approval' examples/phoenix_host/lib/crosswake_example/saas_portal</automated>
  </verify>
  <done>The shared example host now has believable, host-owned SaaS auth and data scaffolding that later plans can build on without reopening Phase 7 scope decisions.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| shared host lane -> public SaaS contract | If route count, runtime class, or lane naming drifts, the implementation stops proving the locked profile |
| auth example code -> public product surface | If auth scaffolding looks like a Crosswake API, the example host will over-promise project scope |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01 | T | shared router lane | mitigate | Lock the exact `/saas` route set and runtime posture in proof tests so later changes cannot silently widen scope |
| T-07-02 | E | auth boundary | mitigate | Keep auth host-owned, role-limited, and re-checked in ordinary Phoenix boundaries rather than encoded in Crosswake abstractions |
| T-07-03 | R | exemplar realism | mitigate | Freeze the minimal-realistic fixture shape in tests so the shared host does not drift into starter-app breadth |
</threat_model>

<verification>
Run `mix test test/crosswake/proof/phase7_saas_lane_test.exs` and confirm the `/saas` route set, runtime ownership, role split, and fixture boundaries stay within the Phase 7 contract.
</verification>

<success_criteria>
- The shared example host contains one `/saas` lane with the locked route budget and LiveView-first runtime ownership.
- Host-owned auth scaffolding exists with one lightweight role split and explicit authorization boundaries.
- Automation fails if the SaaS lane drifts into broader runtime classes, capability breadth, or auth-system scope.
</success_criteria>

<output>
After completion, create `.planning/phases/07-phoenix-saas-portal-exemplar/07-phoenix-saas-portal-exemplar-01-SUMMARY.md`
</output>
