---
phase: 24-reconciliation-traceability-hardening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/21-reconciliation-example/21-01-SUMMARY.md
  - .planning/phases/21-reconciliation-example/21-02-SUMMARY.md
  - .planning/REQUIREMENTS.md
autonomous: true
requirements:
  - RECN-01
  - RECN-02
  - RECN-03
user_setup: []
must_haves:
  truths:
    - "Phase 21's 21-01-SUMMARY.md and 21-02-SUMMARY.md frontmatter use the canonical `requirements-completed:` key (not bare `requirements:`)"
    - "Both Phase 21 SUMMARY files still list RECN-01, RECN-02, RECN-03 under the renamed key — list contents unchanged"
    - "REQUIREMENTS.md milestone bullets show `- [x] **RECN-01**`, `- [x] **RECN-02**`, `- [x] **RECN-03**`"
    - "REQUIREMENTS.md traceability table rows for RECN-01/02/03 contain BOTH literal substrings `Phase 21` and `Phase 24` in the Phase column cell, and `Complete` in the Status column"
    - "No `lib/`, `examples/`, or commerce-adjacent files mutated (planning-artifact-only scope per Footgun 4)"
  artifacts:
    - path: ".planning/phases/21-reconciliation-example/21-01-SUMMARY.md"
      provides: "Canonical `requirements-completed:` frontmatter listing RECN-01/02/03"
      contains: "requirements-completed:"
    - path: ".planning/phases/21-reconciliation-example/21-02-SUMMARY.md"
      provides: "Canonical `requirements-completed:` frontmatter listing RECN-01/02/03"
      contains: "requirements-completed:"
    - path: ".planning/REQUIREMENTS.md"
      provides: "Flipped RECN bullets and updated traceability table cells"
      contains: "- [x] **RECN-01**"
  key_links:
    - from: ".planning/phases/21-reconciliation-example/21-01-SUMMARY.md"
      to: ".planning/REQUIREMENTS.md"
      via: "Every ID under requirements-completed: must exist as a bullet in REQUIREMENTS.md"
      pattern: "requirements-completed:[\\s\\S]*?RECN-0[123]"
    - from: ".planning/REQUIREMENTS.md traceability table cell"
      to: "future audit tool substring scan"
      via: "Both `Phase 21` and `Phase 24` literal substrings present in same cell"
      pattern: "Phase 21 \\(validated\\); Phase 24 \\(traceability normalized\\)"
---

<objective>
Normalize the Phase 21 SUMMARY frontmatter key from bare `requirements:` to the canonical `requirements-completed:` and synchronize `.planning/REQUIREMENTS.md` (milestone bullets + bottom traceability table) so the three RECN requirements reflect satisfied/complete artifact shape across all three sources the milestone audit cross-checks.

Purpose: Phase 21 already verified RECN-01/02/03 (`21-VERIFICATION.md` passes); the v3.2 milestone audit reports `partial` ONLY because the SUMMARY frontmatter uses the wrong key. Renaming the key and flipping REQUIREMENTS.md state turns three of the audit's `partial` rows into `satisfied`.

Output: Two SUMMARY files with canonical frontmatter key, REQUIREMENTS.md with `[x]` checkboxes on RECN bullets and traceability cells containing both `Phase 21` and `Phase 24` literal substrings.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/24-reconciliation-traceability-hardening/24-CONTEXT.md
@.planning/phases/24-reconciliation-traceability-hardening/24-RESEARCH.md
@.planning/phases/24-reconciliation-traceability-hardening/24-PATTERNS.md
@.planning/phases/21-reconciliation-example/21-01-SUMMARY.md
@.planning/phases/21-reconciliation-example/21-02-SUMMARY.md
@.planning/phases/23-commerce-support-and-proof-closure/23-03-SUMMARY.md

<interfaces>
<!-- The canonical SUMMARY frontmatter shape (target). Multi-line list shape is canonical and matches Phase 21's existing list shape — preserve it. -->

From .planning/phases/23-commerce-support-and-proof-closure/23-03-SUMMARY.md (lines 53–55):

```yaml
requirements-completed:
  - SUPP-05
```

Phase 19/20 use the inline alternative `requirements-completed: [COMM-04, COMM-05]` — also canonical. Phase 21 currently uses multi-line under the wrong key; preserve multi-line shape on rename.

Target after rename in 21-01-SUMMARY.md (lines 6–9) and 21-02-SUMMARY.md (lines 6–9):

```yaml
requirements-completed:
  - RECN-01
  - RECN-02
  - RECN-03
```

REQUIREMENTS.md target bullet shape (analog: existing `- [x] **COMM-04**` line):

```markdown
- [x] **RECN-01**: Host apps can follow a minimal Phoenix-owned reconciliation inbox example for purchase, restore, webhook, and support evidence.
- [x] **RECN-02**: Host apps can follow idempotency guidance that uses provider-aware identity rather than transient device correlation IDs.
- [x] **RECN-03**: Host apps can project one authoritative entitlement snapshot from verified evidence and expose stale, pending, denied, and granted states clearly.
```

REQUIREMENTS.md target traceability table cells (analog: existing `Completed (19-01, 19-03)` cell shows multi-value cells are native shape):

```markdown
| RECN-01 | Phase 21 (validated); Phase 24 (traceability normalized) | Complete |
| RECN-02 | Phase 21 (validated); Phase 24 (traceability normalized) | Complete |
| RECN-03 | Phase 21 (validated); Phase 24 (traceability normalized) | Complete |
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rename `requirements:` to `requirements-completed:` in both Phase 21 SUMMARY files</name>
  <read_first>
    - .planning/phases/21-reconciliation-example/21-01-SUMMARY.md (the file to modify — read fully first)
    - .planning/phases/21-reconciliation-example/21-02-SUMMARY.md (the file to modify — read fully first)
    - .planning/phases/23-commerce-support-and-proof-closure/23-03-SUMMARY.md (canonical shape reference — line 53 shows `requirements-completed:` followed by multi-line list)
    - .planning/phases/24-reconciliation-traceability-hardening/24-CONTEXT.md (D-02, D-03: rename key only; preserve list contents exactly; do NOT touch other phase summaries)
  </read_first>
  <files>
    - .planning/phases/21-reconciliation-example/21-01-SUMMARY.md
    - .planning/phases/21-reconciliation-example/21-02-SUMMARY.md
  </files>
  <action>
    Per D-02: In both files, change exactly one line — the frontmatter key on line 6 — from `requirements:` to `requirements-completed:`. Lines 7-9 (`  - RECN-01`, `  - RECN-02`, `  - RECN-03`) MUST be preserved byte-identically. No other lines change. No body content changes. No other frontmatter keys change. The multi-line YAML list shape is canonical (matches `23-03-SUMMARY.md` line 53-55 form); do NOT collapse to inline form.

    Per D-03 (scope): Do NOT scan or modify any other SUMMARY files in this task or this plan. RESEARCH Finding 1 confirmed the D-11 survey — only these two files use the bare `requirements:` key. The parity test in Plan 24-02 will mechanically enforce this going forward.

    Per Footgun 4: Do NOT touch `lib/crosswake/commerce/reconciliation.ex`, `lib/crosswake/commerce/contracts.ex`, `examples/phoenix_host/lib/crosswake_example/commerce/`, or any other RECN-adjacent code path. This task mutates exactly two planning-artifact files.
  </action>
  <verify>
    <automated>head -10 .planning/phases/21-reconciliation-example/21-01-SUMMARY.md | grep -q '^requirements-completed:$' &amp;&amp; head -10 .planning/phases/21-reconciliation-example/21-02-SUMMARY.md | grep -q '^requirements-completed:$' &amp;&amp; ! grep -E '^requirements:$' .planning/phases/21-reconciliation-example/21-01-SUMMARY.md &amp;&amp; ! grep -E '^requirements:$' .planning/phases/21-reconciliation-example/21-02-SUMMARY.md &amp;&amp; grep -q '  - RECN-01' .planning/phases/21-reconciliation-example/21-01-SUMMARY.md &amp;&amp; grep -q '  - RECN-02' .planning/phases/21-reconciliation-example/21-01-SUMMARY.md &amp;&amp; grep -q '  - RECN-03' .planning/phases/21-reconciliation-example/21-01-SUMMARY.md &amp;&amp; grep -q '  - RECN-01' .planning/phases/21-reconciliation-example/21-02-SUMMARY.md &amp;&amp; grep -q '  - RECN-02' .planning/phases/21-reconciliation-example/21-02-SUMMARY.md &amp;&amp; grep -q '  - RECN-03' .planning/phases/21-reconciliation-example/21-02-SUMMARY.md</automated>
  </verify>
  <acceptance_criteria>
    - `head -10 .planning/phases/21-reconciliation-example/21-01-SUMMARY.md | grep -q '^requirements-completed:$'` exits 0
    - `head -10 .planning/phases/21-reconciliation-example/21-02-SUMMARY.md | grep -q '^requirements-completed:$'` exits 0
    - `! grep -E '^requirements:$' .planning/phases/21-reconciliation-example/21-01-SUMMARY.md` (no bare `requirements:` key remains)
    - `! grep -E '^requirements:$' .planning/phases/21-reconciliation-example/21-02-SUMMARY.md` (no bare `requirements:` key remains)
    - `grep -q '  - RECN-01' .planning/phases/21-reconciliation-example/21-01-SUMMARY.md` AND `grep -q '  - RECN-02' .planning/phases/21-reconciliation-example/21-01-SUMMARY.md` AND `grep -q '  - RECN-03' .planning/phases/21-reconciliation-example/21-01-SUMMARY.md` (list contents preserved)
    - Same three greps pass for 21-02-SUMMARY.md
    - `git diff --stat .planning/phases/21-reconciliation-example/21-01-SUMMARY.md` reports exactly 1 line changed (key rename only); same for 21-02-SUMMARY.md
    - No other files modified: `git status --porcelain | grep -E '^\s*M' | wc -l` reports exactly 2
  </acceptance_criteria>
  <done>
    Both 21-01-SUMMARY.md and 21-02-SUMMARY.md carry the canonical `requirements-completed:` key listing RECN-01/02/03 in the same multi-line shape. No other lines in either file changed. No other files in the repository touched.
  </done>
</task>

<task type="auto">
  <name>Task 2: Flip RECN bullets and update traceability table cells in REQUIREMENTS.md</name>
  <read_first>
    - .planning/REQUIREMENTS.md (the file to modify — read fully first; bullets at lines 73-75, table at lines 122-124)
    - .planning/phases/24-reconciliation-traceability-hardening/24-CONTEXT.md (D-04: flip bullets in place, NO `### Validated` subsection; D-05: both `Phase 21` AND `Phase 24` literal substrings in same cell; D-06: NO narrative prose)
    - .planning/phases/24-reconciliation-traceability-hardening/24-PATTERNS.md (Mutation 1 and Mutation 2 sections show exact before/after text)
    - .planning/phases/23-commerce-support-and-proof-closure/23-03-SUMMARY.md (canonical-shape reference for cross-checking that key is `requirements-completed:` — used by parity test in Plan 24-02; not modified here)
  </read_first>
  <files>
    - .planning/REQUIREMENTS.md
  </files>
  <action>
    Two mechanical edits to `.planning/REQUIREMENTS.md`. Do them as separate sequential edits to keep diffs reviewable.

    **Edit A (Bullets — per D-04):** On lines 73, 74, 75 of `.planning/REQUIREMENTS.md`, change `- [ ] **RECN-01**:` → `- [x] **RECN-01**:`, `- [ ] **RECN-02**:` → `- [x] **RECN-02**:`, and `- [ ] **RECN-03**:` → `- [x] **RECN-03**:`. The bullet text after the requirement ID (`:` onwards) MUST remain byte-identical. Bullets stay in the existing `### Reconciliation Example (\`example/docs-only\`, companion-ready)` subsection. Do NOT introduce a `### Validated` subsection (D-04 explicit) — REQUIREMENTS.md has no precedent for one (confirmed by RESEARCH Finding 5).

    **Edit B (Traceability table — per D-05):** On lines 122, 123, 124 (the three RECN rows in the bottom traceability table), replace each row's Phase column cell and Status column cell:

    Before:
    ```
    | RECN-01 | Phase 24 | Pending |
    | RECN-02 | Phase 24 | Pending |
    | RECN-03 | Phase 24 | Pending |
    ```

    After:
    ```
    | RECN-01 | Phase 21 (validated); Phase 24 (traceability normalized) | Complete |
    | RECN-02 | Phase 21 (validated); Phase 24 (traceability normalized) | Complete |
    | RECN-03 | Phase 21 (validated); Phase 24 (traceability normalized) | Complete |
    ```

    The exact cell string `Phase 21 (validated); Phase 24 (traceability normalized)` is the only acceptable form. Do NOT abbreviate to `Phase 21/24`, `Phases 21+24`, `Phase 21 and 24`, or any variant (D-05, Footgun 3). The substring scanner the future audit tool runs requires BOTH `Phase 21` AND `Phase 24` as literal substrings in the same cell.

    Per D-06: Do NOT add narrative prose explaining the validated-in-21-normalized-in-24 distinction. The traceability cell text carries the meaning.

    Per Footgun 4: This task mutates one planning-artifact file. Do NOT touch any other file.
  </action>
  <verify>
    <automated>grep -q -- '- \[x\] \*\*RECN-01\*\*' .planning/REQUIREMENTS.md &amp;&amp; grep -q -- '- \[x\] \*\*RECN-02\*\*' .planning/REQUIREMENTS.md &amp;&amp; grep -q -- '- \[x\] \*\*RECN-03\*\*' .planning/REQUIREMENTS.md &amp;&amp; ! grep -q -- '- \[ \] \*\*RECN-0' .planning/REQUIREMENTS.md &amp;&amp; awk -F '|' '/^\| RECN-0[123] \|/ { phase=$3; status=$4; if (index(phase, "Phase 21") == 0) exit 1; if (index(phase, "Phase 24") == 0) exit 1; if (index(status, "Complete") == 0) exit 1 } END { print "ok" }' .planning/REQUIREMENTS.md | grep -q ok</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q -- '- \[x\] \*\*RECN-01\*\*' .planning/REQUIREMENTS.md` exits 0
    - `grep -q -- '- \[x\] \*\*RECN-02\*\*' .planning/REQUIREMENTS.md` exits 0
    - `grep -q -- '- \[x\] \*\*RECN-03\*\*' .planning/REQUIREMENTS.md` exits 0
    - `! grep -q -- '- \[ \] \*\*RECN-0' .planning/REQUIREMENTS.md` (no unchecked RECN bullets remain)
    - For each of RECN-01, RECN-02, RECN-03: the traceability table row contains BOTH literal substrings `Phase 21` AND `Phase 24` in the same Phase column cell — verified by `awk -F '|' '/^\| RECN-0[123] \|/ { if (index($3, "Phase 21") == 0 || index($3, "Phase 24") == 0) exit 1 } END { exit 0 }' .planning/REQUIREMENTS.md`
    - For each RECN row, the Status column cell contains `Complete` — verified by the same awk script checking `index($4, "Complete")`
    - No `### Validated` subsection introduced: `! grep -q '^### Validated' .planning/REQUIREMENTS.md`
    - No narrative prose added: `git diff --stat .planning/REQUIREMENTS.md` shows changes only on the bullet lines and the three traceability table rows (no new paragraphs)
    - No other files modified by this task
  </acceptance_criteria>
  <done>
    REQUIREMENTS.md shows `- [x]` on all three RECN bullets in the existing milestone subsection (no `### Validated` subsection added). Traceability table rows for RECN-01/02/03 each contain both `Phase 21` and `Phase 24` literal substrings in the Phase column cell and `Complete` in the Status column.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo → CI substring scanner | The future milestone-audit tool scans REQUIREMENTS.md and SUMMARY frontmatter for literal substrings (`Phase 21`, `Phase 24`, `requirements-completed:`). Any abbreviation or key drift defeats the scanner. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-01 | Tampering | REQUIREMENTS.md traceability cell text | mitigate | Acceptance criterion requires BOTH `Phase 21` and `Phase 24` literal substrings in same cell via `awk -F '|'` per-row check; abbreviations rejected. |
| T-24-02 | Information disclosure | Phase 21 SUMMARY content | accept | The rename preserves list contents byte-identically; no new information disclosed. Rationale: this is a key rename of a public planning artifact already in git history. |
| T-24-03 | Tampering | Bypassing `[ ]` → `[x]` checkbox state | mitigate | Acceptance criterion `! grep -q -- '- \[ \] \*\*RECN-0'` ensures no unchecked RECN bullet remains. |
| T-24-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan. Rationale: planning-artifact edits only; no dependency surface. |

**Severity:** low / none — this plan edits only `.planning/` markdown. No auth, session, crypto, input validation, or API surface. Security domain is non-applicable per RESEARCH "Security Domain" section, but the gate requires the block present.
</threat_model>

<verification>
After both tasks complete:

1. `git diff --stat` shows changes confined to exactly three files: `.planning/phases/21-reconciliation-example/21-01-SUMMARY.md`, `.planning/phases/21-reconciliation-example/21-02-SUMMARY.md`, `.planning/REQUIREMENTS.md`.
2. `grep -c '^requirements-completed:$' .planning/phases/21-reconciliation-example/21-01-SUMMARY.md` returns 1; same for 21-02-SUMMARY.md.
3. `! grep -E '^requirements:$' .planning/phases/21-reconciliation-example/21-01-SUMMARY.md` AND `! grep -E '^requirements:$' .planning/phases/21-reconciliation-example/21-02-SUMMARY.md` (no bare key remains).
4. `grep -c -- '- \[x\] \*\*RECN-0' .planning/REQUIREMENTS.md` returns 3.
5. `grep -c 'Phase 21 (validated); Phase 24 (traceability normalized)' .planning/REQUIREMENTS.md` returns 3.
6. No `lib/`, `examples/`, `test/`, or `.github/` files mutated by this plan (Plans 24-02 and 24-03 own those surfaces).
</verification>

<success_criteria>
- Both Phase 21 SUMMARY files use canonical `requirements-completed:` frontmatter key listing RECN-01/02/03 (multi-line shape preserved).
- `.planning/REQUIREMENTS.md` milestone bullets show `[x]` for RECN-01/02/03 in their existing subsection (no `### Validated` subsection introduced).
- `.planning/REQUIREMENTS.md` traceability table rows for RECN-01/02/03 contain both `Phase 21` and `Phase 24` literal substrings in the Phase column cell and `Complete` in the Status column.
- Plan touches only the three files declared in `files_modified`; no `lib/`, `examples/`, or commerce-adjacent file modified.
</success_criteria>

<output>
Create `.planning/phases/24-reconciliation-traceability-hardening/24-01-SUMMARY.md` when done. Frontmatter MUST use `requirements-completed:` (NOT bare `requirements:`) listing RECN-01, RECN-02, RECN-03 — this is the canonical SUMMARY shape Plan 24-02's parity test will enforce.
</output>
