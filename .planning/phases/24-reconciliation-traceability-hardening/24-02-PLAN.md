---
phase: 24-reconciliation-traceability-hardening
plan: 02
type: execute
wave: 2
depends_on:
  - 24-01
files_modified:
  - test/crosswake/planning/summary_frontmatter_test.exs
  - .github/workflows/phase23-proof.yml
autonomous: true
requirements:
  - RECN-01
  - RECN-02
  - RECN-03
user_setup: []
must_haves:
  truths:
    - "A deterministic ExUnit parity test exists at `test/crosswake/planning/summary_frontmatter_test.exs` that asserts (a) no SUMMARY uses bare `requirements:` key and (b) every ID listed under `requirements-completed:` exists as a bullet in REQUIREMENTS.md"
    - "The parity test passes against ALL 13 existing `.planning/phases/*/*-SUMMARY.md` files (D-11 survey confirmed clean post-24-01 rename)"
    - "The merge-blocking CI workflow lists the new test path so regressions block PR merges, not just local `mix test` runs"
    - "No `lib/`, `examples/`, or commerce-adjacent files mutated (planning + test + CI scope only per Footgun 4)"
  artifacts:
    - path: "test/crosswake/planning/summary_frontmatter_test.exs"
      provides: "Merge-blocking parity test enforcing D-10(a) and D-10(b)"
      contains: "defmodule Crosswake.Planning.SummaryFrontmatterTest"
    - path: ".github/workflows/phase23-proof.yml"
      provides: "CI merge gate includes parity test path"
      contains: "test/crosswake/planning/summary_frontmatter_test.exs"
  key_links:
    - from: "test/crosswake/planning/summary_frontmatter_test.exs"
      to: ".planning/phases/*/*-SUMMARY.md"
      via: "Path.wildcard glob + frontmatter regex parse"
      pattern: "Path.wildcard.*\\.planning/phases"
    - from: "test/crosswake/planning/summary_frontmatter_test.exs"
      to: ".planning/REQUIREMENTS.md"
      via: "Regex scan of `- \\[[x ]\\] \\*\\*([A-Z]+-\\d+)\\*\\*` bullet IDs"
      pattern: "REQUIREMENTS.md"
    - from: ".github/workflows/phase23-proof.yml merge-blocking-commerce-proof job"
      to: "the parity test path"
      via: "explicit `mix test` arg (CI does not auto-discover; RESEARCH Finding 6)"
      pattern: "test/crosswake/planning/summary_frontmatter_test.exs"
---

<objective>
Ship the deterministic merge-blocking ExUnit parity test that locks the SUMMARY frontmatter convention (`requirements-completed:` not bare `requirements:`) and amend the CI merge gate so the test runs on every PR/push, not just locally.

Purpose: Without a mechanical parity test, the artifact-shape gap that Phase 21 hit can recur. Without amending `phase23-proof.yml`, `mix test` auto-discovers the new test locally but CI never runs it (RESEARCH Finding 6: all four CI workflows enumerate explicit file paths). Both edits are required for D-09 (merge-blocking lane).

Output: One new ExUnit test file (with two test cases covering D-10a and D-10b) and one amended CI workflow step explicitly listing the new test path.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/24-reconciliation-traceability-hardening/24-CONTEXT.md
@.planning/phases/24-reconciliation-traceability-hardening/24-RESEARCH.md
@.planning/phases/24-reconciliation-traceability-hardening/24-PATTERNS.md
@.planning/phases/24-reconciliation-traceability-hardening/24-01-SUMMARY.md
@test/crosswake/guides/commerce_test.exs
@test/crosswake/support_matrix/renderer_test.exs
@.github/workflows/phase23-proof.yml

<interfaces>
<!-- Canonical ExUnit parity-test idiom from existing tests (commerce_test.exs, renderer_test.exs). -->
<!-- Use these patterns directly — no codebase exploration needed. -->

Module declaration convention (from test/crosswake/guides/commerce_test.exs:1-2 and test/crosswake/support_matrix/renderer_test.exs:1-2):

```elixir
defmodule Crosswake.Planning.SummaryFrontmatterTest do
  use ExUnit.Case, async: true
```

The module name MUST be `Crosswake.Planning.SummaryFrontmatterTest` (matches directory path convention `test/crosswake/planning/summary_frontmatter_test.exs`).

File-path-base convention (from test/crosswake/guides/commerce_test.exs:4):

```elixir
@guide_path Path.join([File.cwd!(), "guides", "commerce.md"])
```

For this test:

```elixir
@summary_glob Path.join(File.cwd!(), ".planning/phases/*/*-SUMMARY.md")
```

Loop-with-actionable-failure-message convention (from test/crosswake/guides/commerce_test.exs ~line 190-193 — loop with assertion that names the missing/wrong item):

```elixir
for path <- summaries do
  refute has_bare_requirements_key?(fm),
         "#{path} uses bare `requirements:` key — rename to `requirements-completed:`"
end
```

Frontmatter parse regex (handles both `\n` and `\r\n` line endings — see RESEARCH Finding 2 Code Examples):

```elixir
~r/\A---\r?\n(.*?)\r?\n---\r?\n/ms
```

Bare-key detector regex (matches `requirements:` followed by space+`[` or newline+indented-`-`; does NOT match `requirements-completed:` because the next char is `-`, not space-bracket or newline-dash):

```elixir
~r/^requirements:[ \t]*(?:\[|\r?\n[ \t]+-)/m
```

ID extraction regex (handles BOTH inline `[A, B]` AND multi-line `\n  - A` shapes — corpus has both per RESEARCH Finding 1):

```elixir
~r/^requirements-completed:[ \t]*(.*)/ms   # capture rest of frontmatter
~r/\b([A-Z]+-\d+)\b/                       # scan IDs from captured tail
```

REQUIREMENTS.md bullet ID scan regex (matches both `[ ]` and `[x]` bullets):

```elixir
~r/- \[[x ]\] \*\*([A-Z]+-\d+)\*\*/
```

CI workflow step pattern to extend (from .github/workflows/phase23-proof.yml:80-90 — current step) — the new step lives in the `merge-blocking-commerce-proof` job (lines 46-90), NOT the `advisory-commerce-proof` job (lines 92+, gated to schedule/dispatch only):

```yaml
- name: Run commerce contract tests (doctor + support matrix + renderer + guide)
  run: |
    mix test \
      test/crosswake/doctor/doctor_test.exs \
      test/crosswake/support_matrix/support_matrix_test.exs \
      test/crosswake/support_matrix/renderer_test.exs \
      test/crosswake/guides/commerce_test.exs
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create ExUnit parity test at `test/crosswake/planning/summary_frontmatter_test.exs`</name>
  <read_first>
    - test/crosswake/guides/commerce_test.exs (canonical loop-with-message parity-test idiom — read fully; the new test mirrors this style)
    - test/crosswake/support_matrix/renderer_test.exs (canonical failure-message clarity pattern — read fully)
    - .planning/phases/24-reconciliation-traceability-hardening/24-RESEARCH.md (Finding 1: D-11 survey shows 11 canonical + 2 Phase-21 files = 13 total; both inline and multi-line YAML list shapes are canonical; Finding 2: regex-only parsing approach; Findings 3 and Code Examples sections show exact regex patterns)
    - .planning/phases/24-reconciliation-traceability-hardening/24-PATTERNS.md (Pattern Assignment for `test/crosswake/planning/summary_frontmatter_test.exs` shows the four required private helpers and their exact regex patterns)
    - .planning/phases/24-reconciliation-traceability-hardening/24-CONTEXT.md (D-09: test path is `test/crosswake/planning/summary_frontmatter_test.exs`; D-10a: bare `requirements:` is forbidden in summaries; D-10b: every ID under `requirements-completed:` must exist in REQUIREMENTS.md; D-11: must pass against ALL existing summaries — survey already verified clean post-24-01)
    - .planning/phases/21-reconciliation-example/21-01-SUMMARY.md (must have been renamed by Plan 24-01 — read to confirm renamed shape before running test)
  </read_first>
  <behavior>
    - Test 1 (covers D-10a): "all phase summaries use requirements-completed: not bare requirements:" — walks `Path.wildcard(@summary_glob)`, parses each file's frontmatter via `parse_frontmatter/1`, calls `has_bare_requirements_key?/1`, and uses `refute` with a message that names the offending file path. MUST also assert the glob returned a non-empty list (guards against the test silently passing when the glob is broken).
    - Test 2 (covers D-10b): "all requirement IDs in requirements-completed: exist in REQUIREMENTS.md" — loads the set of known requirement IDs from `.planning/REQUIREMENTS.md` once via `parse_requirement_ids_from_requirements_md/0`, walks summaries, extracts IDs via `extract_completed_ids/1`, and asserts each ID is in the known set. Failure message MUST name BOTH the missing ID AND the file path that listed it.
    - Both tests use `async: true` (safe — reads only static committed files; no shared state).
    - Test runs as part of `mix test` and individually as `mix test test/crosswake/planning/summary_frontmatter_test.exs`.
    - The test does NOT assert verification status, commit SHAs, completion dates, or plan-number consistency (D-10 explicitly scopes the assertions narrowly — see Deferred Ideas).
  </behavior>
  <files>
    - test/crosswake/planning/summary_frontmatter_test.exs
  </files>
  <action>
    Per D-09: Create new file at `test/crosswake/planning/summary_frontmatter_test.exs` (the `test/crosswake/planning/` directory does not exist yet — Elixir tooling creates it implicitly when the file is written; no explicit `mkdir` needed in `mix test` discovery flow).

    **Module shape (per PATTERNS.md and RESEARCH Finding 3):**

    Module name: `Crosswake.Planning.SummaryFrontmatterTest` (matches directory path; mirrors `Crosswake.Guides.CommerceTest` / `Crosswake.SupportMatrix.RendererTest` / `Crosswake.DoctorTest` convention).

    Module attributes:
    - `use ExUnit.Case, async: true` (matches all three analog tests; safe because the test reads only static committed files).
    - `@summary_glob Path.join(File.cwd!(), ".planning/phases/*/*-SUMMARY.md")` — base path via `File.cwd!()` matches `commerce_test.exs` convention.

    **Two test cases (per D-10a and D-10b):**

    Test 1 (D-10a — no bare `requirements:` key in any SUMMARY):

    ```elixir
    test "all phase summaries use requirements-completed: not bare requirements:" do
      summaries = Path.wildcard(@summary_glob)
      assert summaries != [], "expected SUMMARY.md files at #{@summary_glob}"

      for path <- summaries do
        fm = parse_frontmatter(File.read!(path))
        refute has_bare_requirements_key?(fm),
               "#{path} uses bare `requirements:` key — rename to `requirements-completed:`"
      end
    end
    ```

    Test 2 (D-10b — every ID listed exists in REQUIREMENTS.md):

    ```elixir
    test "all requirement IDs in requirements-completed: exist in REQUIREMENTS.md" do
      known_ids = parse_requirement_ids_from_requirements_md()
      summaries = Path.wildcard(@summary_glob)
      assert summaries != [], "expected SUMMARY.md files at #{@summary_glob}"

      for path <- summaries do
        fm = parse_frontmatter(File.read!(path))
        ids = extract_completed_ids(fm)

        for id <- ids do
          assert id in known_ids,
                 "#{path} lists `#{id}` under requirements-completed: but `#{id}` is not in .planning/REQUIREMENTS.md"
        end
      end
    end
    ```

    **Four private helper functions (per PATTERNS.md and RESEARCH Code Examples):**

    `parse_frontmatter/1` — extract YAML block between first pair of `---\n` (or `---\r\n`) anchors using `~r/\A---\r?\n(.*?)\r?\n---\r?\n/ms`; return captured string or `""` if no frontmatter.

    `has_bare_requirements_key?/1` — `Regex.match?(~r/^requirements:[ \t]*(?:\[|\r?\n[ \t]+-)/m, frontmatter)`. The pattern only matches `requirements:` followed by space+`[` (inline list) or newline+indented-`-` (multi-line list); it does NOT match `requirements-completed:` (because the character after `requirements` in that key is `-`, not `:`).

    `extract_completed_ids/1` — `Regex.run(~r/^requirements-completed:[ \t]*(.*)/ms, frontmatter, capture: :all_but_first)` to capture the value tail (handles both inline `[A, B]` and multi-line `\n  - A` shapes since `.*` with `m` and `s` flags spans newlines), then `Regex.scan(~r/\b([A-Z]+-\d+)\b/, captured) |> Enum.map(fn [_, id] -> id end) |> Enum.uniq()`. Return `[]` if no key found.

    `parse_requirement_ids_from_requirements_md/0` — read `.planning/REQUIREMENTS.md` via `Path.join(File.cwd!(), ".planning/REQUIREMENTS.md") |> File.read!()`, scan with `~r/- \[[x ]\] \*\*([A-Z]+-\d+)\*\*/` (matches both checked and unchecked bullets so the test does not couple to status), return list of IDs.

    **Pre-commit verification (per D-11 and Pitfall 1):** Run `mix test test/crosswake/planning/summary_frontmatter_test.exs` after writing the file. MUST exit 0. If it fails citing `21-01-SUMMARY.md` or `21-02-SUMMARY.md`, Plan 24-01 did not complete the key rename — stop and surface the dependency violation rather than committing a red test.

    **Anti-patterns (do NOT do these):**
    - Do NOT add `assert_raise` or exception-based testing — none of the analog tests use it.
    - Do NOT add `setup_all` — file-walker reads inline (per PATTERNS.md analog idiom).
    - Do NOT import external helpers — all four helpers stay private inside the test module (matches all three analog tests).
    - Do NOT extend the test to assert verification status, commit SHAs, completion dates, or plan-number consistency (D-10 explicitly excludes these — see CONTEXT.md Deferred Ideas).
    - Do NOT add a phase-number floor or allowlist (RESEARCH Finding 1 D-11 verdict: no third key shape exists; not needed; CONTEXT D-11 also says "Do NOT add allowlist scaffolding `just in case`" — restated as the first Footgun in `<phase_specific_constraints>`).

    Per Footgun 4: This task creates exactly one test file. Do NOT modify `lib/`, `examples/`, or any commerce module.
  </action>
  <verify>
    <automated>mix test test/crosswake/planning/summary_frontmatter_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - `test/crosswake/planning/summary_frontmatter_test.exs` exists and is a valid Elixir source file
    - `grep -q '^defmodule Crosswake.Planning.SummaryFrontmatterTest do$' test/crosswake/planning/summary_frontmatter_test.exs` exits 0
    - `grep -q 'use ExUnit.Case, async: true' test/crosswake/planning/summary_frontmatter_test.exs` exits 0
    - `grep -q '@summary_glob' test/crosswake/planning/summary_frontmatter_test.exs` exits 0
    - `grep -q 'all phase summaries use requirements-completed' test/crosswake/planning/summary_frontmatter_test.exs` exits 0 (Test 1 description)
    - `grep -q 'all requirement IDs in requirements-completed' test/crosswake/planning/summary_frontmatter_test.exs` exits 0 (Test 2 description)
    - `grep -q 'parse_frontmatter' test/crosswake/planning/summary_frontmatter_test.exs` AND `grep -q 'has_bare_requirements_key' test/crosswake/planning/summary_frontmatter_test.exs` AND `grep -q 'extract_completed_ids' test/crosswake/planning/summary_frontmatter_test.exs` AND `grep -q 'parse_requirement_ids_from_requirements_md' test/crosswake/planning/summary_frontmatter_test.exs` (all four helpers present)
    - `mix test test/crosswake/planning/summary_frontmatter_test.exs` exits 0 with 2 tests passing, 0 failures
    - No file outside `test/crosswake/planning/` is modified by this task
    - No allowlist or phase-floor scaffolding in the test: `! grep -qiE 'allowlist|phase_floor|min_phase' test/crosswake/planning/summary_frontmatter_test.exs`
  </acceptance_criteria>
  <done>
    `test/crosswake/planning/summary_frontmatter_test.exs` exists with `Crosswake.Planning.SummaryFrontmatterTest` module, 2 tests (D-10a, D-10b), 4 private helpers, and passes locally via `mix test test/crosswake/planning/summary_frontmatter_test.exs` (2 tests, 0 failures).
  </done>
</task>

<task type="auto">
  <name>Task 2: Amend `.github/workflows/phase23-proof.yml` to add parity test to merge-blocking job</name>
  <read_first>
    - .github/workflows/phase23-proof.yml (the file to modify — read fully; merge-blocking job lines 46-90, advisory job lines 92+)
    - .planning/phases/24-reconciliation-traceability-hardening/24-RESEARCH.md (Finding 6: CI uses explicit paths, not `mix test` (all); local `mix test` will auto-discover but CI will NOT until the workflow is amended)
    - .planning/phases/24-reconciliation-traceability-hardening/24-PATTERNS.md (CI step recommendation: separate named step "Run planning corpus parity test" mirrors how `phase23_commerce_support_proof_test.exs` has its own dedicated step)
    - .planning/phases/24-reconciliation-traceability-hardening/24-CONTEXT.md (D-09: parity test is merge-blocking lane; D-01 prior art is Phase 23 D-11/D-12 split — new test belongs in merge-blocking, NOT advisory)
  </read_first>
  <files>
    - .github/workflows/phase23-proof.yml
  </files>
  <action>
    Per D-09 and RESEARCH Finding 6: Add the new parity test path to the `merge-blocking-commerce-proof` job in `.github/workflows/phase23-proof.yml` so CI runs it on every PR and push to main.

    Use a separate named step (Option A from PATTERNS.md — mirrors the pattern set by `phase23_commerce_support_proof_test.exs` step at line 77-78). Insert it AFTER the existing "Run commerce contract tests (doctor + support matrix + renderer + guide)" step (lines 80-90), still inside the `merge-blocking-commerce-proof` job's `steps:` list.

    Exact step to insert (preserve the indentation already used by surrounding steps — 6 spaces before `- name:` based on existing file shape):

    ```yaml
          - name: Run planning corpus parity test
            # Phase 24: merge-blocking parity test asserting (a) no SUMMARY file
            # uses bare `requirements:` key and (b) every requirements-completed:
            # ID exists in .planning/REQUIREMENTS.md. Hermetic — reads only static
            # planning artifact files committed to git.
            run: mix test test/crosswake/planning/summary_frontmatter_test.exs
    ```

    **Critical placement constraints:**
    - Step MUST live inside the `merge-blocking-commerce-proof` job (job name on line 46), NOT inside `advisory-commerce-proof` (line 92+). The advisory job is gated by `if: ${{ github.event_name == 'schedule' || github.event_name == 'workflow_dispatch' }}` and runs weekly only — placing the test there would defeat the merge-blocking requirement (D-09).
    - Step MUST come AFTER the existing commerce contract test step (the parity test depends conceptually on the same hermetic-test posture; separate step keeps each test's failure cause discoverable in CI logs).
    - Do NOT collapse the new test path into the existing step's `mix test \` command list (PATTERNS.md Option A vs B — Option A is recommended for discoverability and matches the existing pattern of `phase23_commerce_support_proof_test.exs` having its own named step).

    **Do not change:**
    - The advisory job
    - The merge-blocking job's `name:`, `runs-on:`, `timeout-minutes:`, `if:` guard, or any setup step (Checkout, Setup BEAM, Install Elixir dependencies, Compile)
    - Any other workflow file in `.github/workflows/` — only `phase23-proof.yml` is in scope
    - The on/triggers block

    Per Footgun 4: This task modifies exactly one CI workflow file. Do NOT touch `lib/`, `examples/`, or any commerce module. Do NOT modify other workflow files (phase5-proof.yml, phase10-proof.yml, phase18-proof.yml).

    **Verification approach:** After editing, run `grep -q 'summary_frontmatter_test.exs' .github/workflows/phase23-proof.yml` (the path appears in the file at all) AND verify via an awk script that the path appears within the merge-blocking job's section (between the `merge-blocking-commerce-proof:` line and the `advisory-commerce-proof:` line), not in the advisory section.
  </action>
  <verify>
    <automated>grep -q 'test/crosswake/planning/summary_frontmatter_test.exs' .github/workflows/phase23-proof.yml &amp;&amp; awk '/^  merge-blocking-commerce-proof:/{flag=1; next} /^  advisory-commerce-proof:/{flag=0} flag &amp;&amp; /summary_frontmatter_test\.exs/{found=1} END{exit !found}' .github/workflows/phase23-proof.yml &amp;&amp; grep -q '^      - name: Run planning corpus parity test$' .github/workflows/phase23-proof.yml</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q 'test/crosswake/planning/summary_frontmatter_test.exs' .github/workflows/phase23-proof.yml` exits 0 (path present)
    - `grep -q '^      - name: Run planning corpus parity test$' .github/workflows/phase23-proof.yml` exits 0 (new named step exists with exact indentation)
    - The path lives inside the merge-blocking job, not the advisory job: `awk '/^  merge-blocking-commerce-proof:/{flag=1; next} /^  advisory-commerce-proof:/{flag=0} flag && /summary_frontmatter_test\.exs/{found=1} END{exit !found}' .github/workflows/phase23-proof.yml` exits 0
    - No other workflow file changed: `git diff --name-only .github/workflows/ | wc -l` reports 1 (only phase23-proof.yml)
    - The existing "Run commerce contract tests" step is UNCHANGED: `grep -q '^      - name: Run commerce contract tests (doctor + support matrix + renderer + guide)$' .github/workflows/phase23-proof.yml` exits 0
    - The advisory-commerce-proof job is unchanged: `grep -q '^  advisory-commerce-proof:$' .github/workflows/phase23-proof.yml` exits 0 AND `! awk '/^  advisory-commerce-proof:/{flag=1} flag && /summary_frontmatter_test\.exs/{print "leaked"; exit 1}' .github/workflows/phase23-proof.yml`
    - YAML is still parseable (CI lint or a `yq`/`python -c "import yaml; yaml.safe_load(open('.github/workflows/phase23-proof.yml'))"` if available) — if no YAML linter, rely on the structural greps above
  </acceptance_criteria>
  <done>
    `.github/workflows/phase23-proof.yml` includes a new `Run planning corpus parity test` step inside the `merge-blocking-commerce-proof` job that runs `mix test test/crosswake/planning/summary_frontmatter_test.exs`. The path is NOT in the advisory job. No other workflow file modified.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo → CI runner | The merge-blocking CI runner executes `mix test` against committed test files; a test that reads `.planning/` paths trusts that those files are committed in the same revision. |
| CI workflow YAML → CI scheduler | The `if:` guard and job name in the workflow YAML determine which events trigger which job — misplacement into the advisory job would silently demote the parity test to non-blocking. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-04 | Tampering | Misplacement of parity test into advisory CI job | mitigate | Acceptance criterion uses `awk` flag scan bounded by `merge-blocking-commerce-proof:` and `advisory-commerce-proof:` job headers to confirm the path is inside the merge-blocking section. |
| T-24-05 | Denial of service | False-negative parity test (test silently returns no files and passes) | mitigate | Test 1 and Test 2 both `assert summaries != []` before the `for` loop, so an empty glob fails loudly. |
| T-24-06 | Spoofing | Regex false-positive matching `requirements-completed:` as bare `requirements:` | mitigate | `has_bare_requirements_key?/1` regex requires a character AFTER the colon (`[ \t]*(?:\[|\r?\n[ \t]+-)`); `requirements-completed:` does not have `:` after the literal `requirements` substring (it has `-completed:`), so the anchor `^requirements:` cannot match. |
| T-24-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan. ExUnit is Elixir stdlib; no new Hex dep added (RESEARCH Finding 7). Rationale: planning-corpus regex tests need only stdlib `File`, `Path`, `Regex`. |

**Severity:** low — test is hermetic (reads static files), no auth/session/crypto/API surface, no new dependencies, no `lib/` changes.
</threat_model>

<verification>
After both tasks complete:

1. `mix test test/crosswake/planning/summary_frontmatter_test.exs` exits 0 with 2 tests passing.
2. `mix test` (full suite) exits 0 — the new test discovered automatically by ExUnit and passes alongside existing tests.
3. `grep -q 'test/crosswake/planning/summary_frontmatter_test.exs' .github/workflows/phase23-proof.yml` confirms CI path inclusion.
4. The path appears inside `merge-blocking-commerce-proof` job section (verified via awk-flag bounded by job headers — see Task 2 acceptance criteria).
5. No `lib/`, `examples/`, or commerce-adjacent file mutated by this plan.
6. `git diff --stat` reports exactly two files changed: `test/crosswake/planning/summary_frontmatter_test.exs` (new) and `.github/workflows/phase23-proof.yml`.
</verification>

<success_criteria>
- Deterministic parity test exists at `test/crosswake/planning/summary_frontmatter_test.exs` covering D-10a (no bare `requirements:` key) and D-10b (every listed ID exists in REQUIREMENTS.md).
- Test passes via both `mix test test/crosswake/planning/summary_frontmatter_test.exs` and `mix test` (full suite).
- `.github/workflows/phase23-proof.yml` merge-blocking job includes the parity test path as a separate named step.
- No allowlist or phase-floor scaffolding in the test (D-11 survey confirmed clean post-24-01).
- Plan touches only the two files declared in `files_modified`; no `lib/`, `examples/`, or commerce-adjacent file modified.
</success_criteria>

<output>
Create `.planning/phases/24-reconciliation-traceability-hardening/24-02-SUMMARY.md` when done. Frontmatter MUST use `requirements-completed:` listing RECN-01, RECN-02, RECN-03 — the parity test now enforces this and will fail in CI if the SUMMARY uses bare `requirements:`.
</output>
