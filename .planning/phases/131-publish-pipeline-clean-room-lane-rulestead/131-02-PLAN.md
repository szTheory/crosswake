---
phase: 131-publish-pipeline-clean-room-lane-rulestead
plan: 02
type: execute
wave: 2
depends_on: ["131-01"]
files_modified:
  - .github/workflows/release-please.yml
  - script/verify_companion_cleanroom.sh
autonomous: true
requirements: [EXTRACT-06, PROOF-01]
must_haves:
  truths:
    - "publish-hex-rulestead job runs deps.get -> compile --warnings-as-errors -> mix test -> hex.publish --dry-run -> hex.publish in sequence (SC#2, D-09)"
    - "the publish job is gated on per-component output rulestead_release_created, NEVER the aggregate releases_created (SC#2, D-07)"
    - "the publish job sets CROSSWAKE_RELEASE=1 on all mix steps so the resolver emits the Hex dep (D-13)"
    - "script/verify_companion_cleanroom.sh polls Hex propagation, builds a throwaway Phoenix host OUTSIDE the monorepo, installs published crosswake + companion + engine, compiles --warnings-as-errors, runs an inline public-seam smoke test, and runs mix crosswake.doctor --router (exit 0) (SC#3, D-16/17/18/19/20)"
  artifacts:
    - ".github/workflows/release-please.yml publish-hex-rulestead job"
    - "script/verify_companion_cleanroom.sh (parameterized, [crosswake]-prefixed, set -euo pipefail)"
  key_links:
    - "needs.release-please.outputs.rulestead_release_created == 'true' -> publish-hex-rulestead runs (D-07)"
    - "CROSSWAKE_RELEASE=1 + checkout ref rulestead_tag_name -> honest tarball at the right commit (D-09/13)"
    - "hex.publish --dry-run gate precedes hex.publish in the same job (SC#2, SC#4)"
    - "verify_companion_cleanroom.sh installs {:rulestead, \"~> 0.1\"} -> Code.ensure_loaded?(Rulestead) true -> doctor exit 0 (D-19/20)"
---

<objective>
Add the gated per-companion publish job (`publish-hex-rulestead`) to the existing
`release-please.yml`, and author the parameterized `script/verify_companion_cleanroom.sh`
that proves the published artifact resolves and works OUTSIDE the monorepo. The publish job
executes the SC#2 sequence verbatim; the clean-room script holds the PROOF-01 logic (CI wires
it in Plan 03). The `hex.publish --dry-run` gate inside the publish job and the merge-blocking
in-monorepo `phase130-proof.yml` lanes are the PRE-publish gates (PROOF-02).

Purpose: EXTRACT-06 (a per-companion publish job keyed on the release-please output) and
PROOF-01 (the clean-room script + inline smoke test). The single most important correctness
point is D-07 — gate on `rulestead_release_created`, never the aggregate `releases_created`,
or a core-only release would publish the companion.

Output: the `publish-hex-rulestead` job and `script/verify_companion_cleanroom.sh`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/131-publish-pipeline-clean-room-lane-rulestead/131-CONTEXT.md
@.planning/phases/131-publish-pipeline-clean-room-lane-rulestead/131-RESEARCH.md
@.planning/phases/131-publish-pipeline-clean-room-lane-rulestead/131-01-SUMMARY.md
@brandbook/BRAND-SPEC.md
</context>

<artifacts_this_phase_produces>
This plan (02) creates:
- `publish-hex-rulestead` job in `.github/workflows/release-please.yml`
- `script/verify_companion_cleanroom.sh` (parameterized: PACKAGE, VERSION, ENGINE_PACKAGE, ENGINE_MODULE)
- the inline `test/smoke_test.exs` authored BY the clean-room script at runtime in `$RUNNER_TEMP`
- the minimal `use Phoenix.Router` stub authored BY the clean-room script in the throwaway app

Already created in Plan 01 (consumed here, NOT re-created): `crosswake_dep/0`, `CROSSWAKE_RELEASE`,
the release-please config/manifest entries, the `rulestead_release_created`/`rulestead_tag_name`/
`rulestead_version` output aliases.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Add gated publish-hex-rulestead job to release-please.yml (D-06/07/08/09/10/13)</name>
  <files>.github/workflows/release-please.yml</files>
  <read_first>
    - .github/workflows/release-please.yml lines 58-130 (the core `publish-hex` job — the exact step sequence to mirror: checkout@ref -> setup-beam -> cache -> install hex+rebar -> deps.get -> compile --warnings-as-errors -> verify @version grep -> test -> dry-run -> publish -> propagation poll) and lines 32-42 (the output aliases added in Plan 01)
    - .github/workflows/phase130-proof.yml lines 82-121 (`companion-engine-absent-proof` — the engine-absent `working-directory: packages/crosswake_rulestead` + plain test template; the PRE-publish merge gate that this job does NOT modify)
    - .planning/phases/131-publish-pipeline-clean-room-lane-rulestead/131-RESEARCH.md §"publish-hex Core Job — Template to Mirror" + "Companion job differences" (exact deltas: ref/working-directory/VERSION/version-grep target/test step/poll URL/CROSSWAKE_RELEASE env/if condition) and Pitfalls 1-3 + Assumption A3 (the --dry-run step DOES need HEX_API_KEY — match the core job)
    - .planning/phases/131-publish-pipeline-clean-room-lane-rulestead/131-01-SUMMARY.md (confirm the alias output names landed)
    - brandbook/BRAND-SPEC.md §6 (CI failure microcopy: `[crosswake]` prefix)
  </read_first>
  <action>
    Add a new `publish-hex-rulestead` job to `.github/workflows/release-please.yml`, mirroring the core `publish-hex` job (D-09) with these companion deltas: `needs: release-please`; `if: ${{ needs.release-please.outputs.rulestead_release_created == 'true' }}` (D-07 — per-component, NEVER `releases_created`; add a comment citing the aggregate footgun); checkout `ref: ${{ needs.release-please.outputs.rulestead_tag_name }}` (D-09 — publish from the companion tag, not the core tag); a job-level `env: { CROSSWAKE_RELEASE: "1" }` so every mix step activates the resolver (D-13, Pitfall 3); `working-directory: packages/crosswake_rulestead` on every mix step; setup-beam via `.tool-versions`; install hex+rebar; `mix deps.get`; `mix compile --warnings-as-errors`; a version-grep guard `grep -n "@version \"${{ needs.release-please.outputs.rulestead_version }}\"" packages/crosswake_rulestead/mix.exs`; test step = plain `mix test` (D-10 — engine-absent hermetic lane; do NOT add `--exclude requires_example_host`, that is a core-only tag); `mix hex.publish --dry-run --yes` with `HEX_API_KEY` in env (Assumption A3 — match the core dry-run step which sets the secret); `mix hex.publish --yes` with `HEX_API_KEY`; then a propagation poll of `https://hex.pm/api/packages/crosswake_rulestead/releases/${{ needs.release-please.outputs.rulestead_version }}` with MAX_ATTEMPTS=36 / DELAY=10 (mirror of the core poll). Use `[crosswake]`-prefixed messages for any custom failure echo. Do NOT modify the core `publish-hex`, `clean-room-proof-ios`, or `phase130-proof.yml` jobs.
  </action>
  <verify>
    <automated>python3 -c "import yaml,sys; d=yaml.safe_load(open('.github/workflows/release-please.yml')); j=d['jobs']['publish-hex-rulestead']; assert j['needs']=='release-please' or 'release-please' in j['needs']; assert 'rulestead_release_created' in j['if'] and 'releases_created' not in j['if']; env=j.get('env',{}); assert str(env.get('CROSSWAKE_RELEASE'))=='1'; steps=' '.join(str(s) for s in j['steps']); assert 'hex.publish --dry-run' in steps and 'hex.publish --yes' in steps and 'crosswake_rulestead/releases' in steps; assert 'packages/crosswake_rulestead' in steps; print('publish-hex-rulestead OK')"</automated>
  </verify>
  <acceptance_criteria>
    - The python3+yaml assertion prints `publish-hex-rulestead OK`
    - `j['if']` references `rulestead_release_created` and does NOT reference `releases_created` (D-07)
    - Job-level `env.CROSSWAKE_RELEASE == "1"` (D-13)
    - The step body contains, in order, `mix deps.get`, `mix compile --warnings-as-errors`, `mix test` (no `--exclude requires_example_host`), `mix hex.publish --dry-run --yes`, `mix hex.publish --yes` (SC#2 sequence)
    - The propagation poll targets `hex.pm/api/packages/crosswake_rulestead/releases/`
    - `git diff` shows the core `publish-hex` job and `phase130-proof.yml` are unchanged
  </acceptance_criteria>
  <done>publish-hex-rulestead is wired, gated on the per-component output, runs the SC#2 sequence under CROSSWAKE_RELEASE=1, and publishes from the companion tag.</done>
</task>

<task type="auto">
  <name>Task 2: Author script/verify_companion_cleanroom.sh — post-publish resolvability + doctor proof (D-16/17/18/19/20)</name>
  <files>script/verify_companion_cleanroom.sh</files>
  <read_first>
    - script/verify_companion_package.sh (house style: `set -euo pipefail`, `$1`/`$2` params, `[crosswake]`-prefixed failures, `UNPACK_DIR` temp idiom)
    - lib/mix/tasks/crosswake.doctor.ex lines 82-94 (`router_module!/1` does ONLY `Code.ensure_loaded?(module)` — a minimal `use Phoenix.Router` with NO routes suffices; resolves Open Question 1 toward the minimal stub) and lines 54-56 (doctor `Mix.raise` -> non-zero exit when report.status == :error)
    - packages/crosswake_rulestead/lib/crosswake/companions/rulestead.ex (public seam the smoke test asserts: `companion_id/0 == :rulestead` line 34, `enabled?/1` line 42, `validate_dependency/0` lines 122-138 with `Code.ensure_loaded?(Rulestead)` line 127; MockFlagSource is NOT shipped — use public callbacks only, D-18)
    - .planning/phases/131-publish-pipeline-clean-room-lane-rulestead/131-RESEARCH.md §"script/verify_companion_cleanroom.sh — Body Design" (signature, 5 steps, exact deps, engine `{:rulestead, "~> 0.1"}` resolves to 0.1.7 per D-20, propagation poll constants MAX_ATTEMPTS=36/DELAY=10) and §"Open Questions" #1 (minimal router stub default; add `scope "/"` only if doctor route-introspects) and §Security (validate $VERSION looks like semver before curl)
    - brandbook/BRAND-SPEC.md §6 (failure microcopy)
  </read_first>
  <action>
    Create `script/verify_companion_cleanroom.sh` with `set -euo pipefail` and signature `PACKAGE="${1:-crosswake_rulestead}"`, `VERSION="${2:?[crosswake] FAIL: VERSION required as \$2}"`, `ENGINE_PACKAGE="${3:-rulestead}"`, `ENGINE_MODULE="${4:-Rulestead}"` (D-16). Validate `$VERSION` matches a semver pattern (e.g. `^[0-9]+\.[0-9]+\.[0-9]+`) before any curl, failing with a `[crosswake]`-prefixed message (Security: prevent URL injection). Body (D-17): (1) Poll `https://hex.pm/api/packages/${PACKAGE}/releases/${VERSION}` for `"version"` with MAX_ATTEMPTS=36 / DELAY=10 (mirror the core poll); fail after timeout with `[crosswake]` microcopy naming the propagation timeout + the `mix hex.retire` recovery hint. (2) Create a throwaway app in `${RUNNER_TEMP:-/tmp}/clean-room-${PACKAGE}` (rm -rf first), OUTSIDE the monorepo, via `mix new clean_room_host --sup`. (3) Patch its `mix.exs` deps to include `{:phoenix, "~> 1.8"}`, `{:phoenix_live_view, "~> 1.2"}`, `{:crosswake, "~> 0.1"}`, `{:#{PACKAGE}, "~> 0.1"}` (atom-interpolated), `{:#{ENGINE_PACKAGE}, "~> 0.1"}` (D-19/D-20 — installing the real engine flips `Code.ensure_loaded?(#{ENGINE_MODULE})` true so doctor exits 0; NOT the engine-absent fail-closed path). (4) `mix deps.get`; `mix compile --warnings-as-errors`. (5) Write a minimal `lib/clean_room_host/router.ex` defining `defmodule CleanRoomHost.Router do use Phoenix.Router end` (minimal stub per Open Question 1 default). (6) Write an inline `test/smoke_test.exs` ExUnit module (D-18 — the tarball ships no `test/`) asserting the PUBLIC seam via `Crosswake.Companions.Rulestead`: `validate_dependency() == :ok` (engine present), `companion_id() == :rulestead`, and `enabled?/1` honoring config (`refute enabled?(%{})`, `assert enabled?(%{enabled: true})`) — public callbacks only, NO MockFlagSource. Run `mix test test/smoke_test.exs`. (7) Append companion registration to `config/runtime.exs` (`config :crosswake, :companions, [Crosswake.Companions.Rulestead]` + enable). (8) Run `mix crosswake.doctor --router CleanRoomHost.Router` and assert exit 0. Echo a `[crosswake]`-prefixed success line at the end. Make the file executable-ready (it is invoked via `bash script/...` from CI, so a shebang `#!/usr/bin/env bash` + `set -euo pipefail` is sufficient).
  </action>
  <verify>
    <automated>bash -n script/verify_companion_cleanroom.sh && grep -q 'set -euo pipefail' script/verify_companion_cleanroom.sh && grep -q 'crosswake.doctor --router' script/verify_companion_cleanroom.sh && grep -q 'use Phoenix.Router' script/verify_companion_cleanroom.sh && grep -q 'validate_dependency' script/verify_companion_cleanroom.sh && grep -q 'RUNNER_TEMP' script/verify_companion_cleanroom.sh && grep -q 'MAX_ATTEMPTS' script/verify_companion_cleanroom.sh && grep -Eq 'VERSION.*\?:' script/verify_companion_cleanroom.sh</automated>
  </verify>
  <acceptance_criteria>
    - `bash -n script/verify_companion_cleanroom.sh` exits 0 (valid bash)
    - `grep -q 'set -euo pipefail' script/verify_companion_cleanroom.sh` exits 0
    - `grep -q 'crosswake.doctor --router' script/verify_companion_cleanroom.sh` exits 0 (doctor smoke with --router)
    - `grep -q 'use Phoenix.Router' script/verify_companion_cleanroom.sh` exits 0 (minimal router stub authored)
    - `grep -q 'validate_dependency' script/verify_companion_cleanroom.sh` exits 0 (public-seam smoke assertion present)
    - `grep -q 'RUNNER_TEMP' script/verify_companion_cleanroom.sh` and `grep -q 'MAX_ATTEMPTS' script/verify_companion_cleanroom.sh` both exit 0 (out-of-monorepo + propagation poll)
    - The script defaults PACKAGE=crosswake_rulestead and requires VERSION as $2 (`shellcheck` clean if available)
    - NOTE: full end-to-end execution is Manual-Only / CI-only (PROOF-01) — it requires the published artifact live on Hex, which does not exist until the Plan 02 publish job runs post-merge (irreversible). Static + `bash -n` + grep verification only at author time.
  </acceptance_criteria>
  <done>verify_companion_cleanroom.sh exists, is valid bash, parameterized for rindle reuse, polls propagation, builds an out-of-monorepo Phoenix host, installs the published trio + engine, runs the inline public-seam smoke test, and runs the doctor --router smoke.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI job -> Hex.pm publish | HEX_API_KEY authorizes an IRREVERSIBLE publish; the dry-run gate + per-component if-gate guard it |
| release-please output -> publish trigger | gating on the wrong output publishes the companion on a core-only release |
| script $VERSION arg -> curl URL | an unvalidated version string flows into a Hex API URL |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-131-04 | Elevation of privilege | publish-hex-rulestead if-gate (D-07) | high | mitigate | gate strictly on `needs.release-please.outputs.rulestead_release_created == 'true'`; never the aggregate `releases_created` (a core-only release must NOT publish the companion) |
| T-131-05 | Tampering / runtime break | tarball missing crosswake dep (D-12) | high | mitigate | job-level `CROSSWAKE_RELEASE=1` forces the resolver to emit the Hex dep; `hex.publish --dry-run` precedes the real publish; Plan 01 Step-2 grep is the belt |
| T-131-06 | Information disclosure | HEX_API_KEY in publish job | medium | mitigate | reuse the existing repo secret; reference only via `${{ secrets.HEX_API_KEY }}` in `env:` of the dry-run + publish steps (no echo, no logging); scope to this job only |
| T-131-07 | Injection | $VERSION -> curl URL in cleanroom script | low | mitigate | validate `$VERSION` against a semver regex before constructing the Hex API URL; `set -euo pipefail` |
| T-131-SC | Tampering | npm/pip/cargo installs | low | accept | no package-manager installs added; the clean-room `mix deps.get` resolves only published `crosswake`/`crosswake_rulestead`/`rulestead` (the phase's own + an already-on-Hex engine at 0.1.7) — no new third-party packages |
</threat_model>

<verification>
- `python3 -c "import yaml; yaml.safe_load(open('.github/workflows/release-please.yml'))"` — workflow is valid YAML with the new job
- `bash -n script/verify_companion_cleanroom.sh` — script syntactically valid
- `cd packages/crosswake_rulestead && mix test` — engine-absent companion tests still green (the publish-job test step mirror)
- Manual-Only / CI-only: full `bash script/verify_companion_cleanroom.sh crosswake_rulestead <VERSION>` runs only post-publish (PROOF-01 irreversible)
</verification>

<success_criteria>
- publish-hex-rulestead runs the SC#2 sequence, gated on rulestead_release_created, under CROSSWAKE_RELEASE=1, publishing from the companion tag (EXTRACT-06)
- the dry-run gate precedes the real publish (SC#4 / PROOF-02 in-job belt)
- verify_companion_cleanroom.sh holds the PROOF-01 logic: propagation poll, out-of-monorepo Phoenix host, published-trio install, inline public-seam smoke, doctor --router exit 0 (SC#3)
- the script is parameterized for rindle reuse (D-16)
</success_criteria>

<output>
Create `.planning/phases/131-publish-pipeline-clean-room-lane-rulestead/131-02-SUMMARY.md` when done
</output>
