---
phase: 132-generalization-proof-rindle-compat-matrix
plan: 03
type: execute
wave: 2
depends_on:
  - 132-01
files_modified:
  - lib/crosswake/companions/rindle.ex
  - lib/crosswake/companions/rindle/contracts.ex
  - lib/crosswake/companions/rindle/reconciliation.ex
  - lib/crosswake/companion_guard.ex
  - packages/crosswake_rindle/lib/crosswake/companions/rindle.ex
  - packages/crosswake_rindle/lib/crosswake/companions/rindle/contracts.ex
  - packages/crosswake_rindle/lib/crosswake/companions/rindle/reconciliation.ex
  - packages/crosswake_rindle/test/crosswake/proof/phase45_rindle_companion_test.exs
  - packages/crosswake_rindle/test/crosswake/proof/phase45_rindle_mock_media_test.exs
  - packages/crosswake_rindle/test/crosswake/proof/phase45_rindle_advisory_test.exs
  - packages/crosswake_rindle/test/crosswake/proof/phase45_rindle_live_test.exs
  - packages/crosswake_rindle/test/crosswake/proof/phase72_media_evidence_workflow_proof_test.exs
  - packages/crosswake_rindle/test/crosswake/companions/rindle/contracts_test.exs
  - packages/crosswake_rindle/test/crosswake/companions/rindle/reconciliation_test.exs
  - packages/crosswake_rindle/test/support/study_session_live.ex
  - packages/crosswake_rindle/test/support/example_host/reconciliation_keys.ex
  - packages/crosswake_rindle/test/support/example_host/reconciliation_inbox.ex
  - packages/crosswake_rindle/test/support/example_host/mock_capture.ex
  - packages/crosswake_rindle/test/support/example_host/media_projection.ex
  - packages/crosswake_rindle/test/support/example_host/media_lane_live.ex
  - test/crosswake/guides/companions_test.exs
  - test/crosswake/proof/phase47_companion_arc_test.exs
  - mix.exs
  - script/verify_companion_package.sh
  - .github/workflows/phase132-proof.yml
  - .github/workflows/phase72-proof.yml
autonomous: true
requirements:
  - EXTRACT-07
  - SEAM-05
user_setup: []

must_haves:
  truths:
    - "No file under core lib/ statically names Crosswake.Companions.Rindle (or its .Contracts/.Reconciliation children) after the move — the EXTRACT-03 guard (CompanionGuard MapSet gains Crosswake.Companions.Rindle) is green and core discovers rindle only via the @behaviour seam + registry (SEAM-05)"
    - "packages/crosswake_rindle/ contains the moved rindle adapter + Contracts + Reconciliation (module names preserved) and its moved domain/adapter tests pass engine-absent via mix companions.test (EXTRACT-07)"
    - "The core seam tests (phase47 tests 1-2, companions guide test) exercise rindle through StubRindleAbsentCompanion via the registry, never aliasing the moved adapter, and stay green in core"
    - "CROSSWAKE_RELEASE=1 bash script/verify_companion_package.sh crosswake_rindle passes (the line-53 rulestead hardcode is parameterized from $PACKAGE)"
  artifacts:
    - packages/crosswake_rindle/lib/crosswake/companions/rindle.ex
    - packages/crosswake_rindle/lib/crosswake/companions/rindle/contracts.ex
    - packages/crosswake_rindle/lib/crosswake/companions/rindle/reconciliation.ex
    - .github/workflows/phase132-proof.yml
  key_links:
    - "lib/crosswake/companion_guard.ex @extracted_companion_names gains Crosswake.Companions.Rindle in the SAME commit that deletes lib/crosswake/companions/rindle.ex and its subdirectory (the guard comment's same-PR rule); the guard turning red is the signal a core alias survived"
    - "mix.exs companions.test alias chains the crosswake_rindle lane so mix verify and phase132-proof.yml exercise the moved tests engine-absent"
    - "the moved phase45/phase72 tests Code.require_file the 5 media helpers from the companion's own test/support/example_host/ (copied, not relative-pathed back to examples/) so the package is standalone-installable"
---

<objective>
Run `script/extract_companion.md` Steps 1-3, 8-11 on rindle: move the adapter +
owned `Contracts` + `Reconciliation` (module names preserved = non-breaking) into
`packages/crosswake_rindle/`, move the domain/adapter coupling tests to the companion
lane, rewrite the seam-only core tests to use `StubRindleAbsentCompanion` instead of
aliasing the moved adapter, add `Crosswake.Companions.Rindle` to the CompanionGuard
frozen MapSet, chain the `companions.test` alias, parameterize
`verify_companion_package.sh`, and stand up the `phase132-proof.yml` companion lane.

This is the EXTRACT-07 + SEAM-05 core: prove the recipe generalizes to a second
companion with ZERO rindle-specific branch added to core (D-02). It depends on 132-01
(package skeleton) but touches no doc/drift-test files, so it runs parallel with 132-02
in Wave 2. Publish-pipeline wiring (release-please component, clean-room) is 132-04.

Purpose: Generalization proof — the same checklist, substituted, with the EXTRACT-03
guard as the structural witness that core stayed seam-clean.
Output: rindle source + tests live in `packages/crosswake_rindle/`; core lib/ is
rindle-free; the companion lane is wired and merge-blocking.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/132-generalization-proof-rindle-compat-matrix/132-PATTERNS.md
@.planning/phases/132-generalization-proof-rindle-compat-matrix/132-RESEARCH.md
@script/extract_companion.md

# Source to move + its couplings:
@lib/crosswake/companions/rindle.ex
@lib/crosswake/companion_guard.ex
@test/crosswake/guides/companions_test.exs
@test/crosswake/proof/phase47_companion_arc_test.exs
@mix.exs

# Copy-templates / analogs:
@packages/crosswake_rulestead/lib/crosswake/companions/rulestead.ex
@.github/workflows/phase130-proof.yml
@script/verify_companion_package.sh
</context>

<artifacts_this_phase_produces>
This plan creates/moves these symbols (downstream verification must not flag them as
unexpected new core symbols): the moved `Crosswake.Companions.Rindle`,
`Crosswake.Companions.Rindle.Contracts` (+ `UploadGrant`/`CaptureEvidence`/`MediaObject`),
`Crosswake.Companions.Rindle.Reconciliation` (+ `Attempt`/`IdempotencyKey`/`EvidenceResult`)
now under `packages/crosswake_rindle/`; the engine-present fake `Rindle` (from 132-01);
the `phase132-proof.yml` workflow and its `core-hermetic-proof` /
`companion-engine-absent-proof` / `companion-engine-present-proof` jobs. The
CompanionGuard MapSet entry `Crosswake.Companions.Rindle` is the one core edit.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Move rindle source to the package + add Rindle to the CompanionGuard MapSet (same commit)</name>
  <read_first>
    - lib/crosswake/companions/rindle.ex (the adapter — companion_id/0 :rindle, validate_dependency/0 probing Code.ensure_loaded?(Rindle) at function-body runtime lines 27/41, enabled?/1, report_state/0)
    - lib/crosswake/companions/rindle/contracts.ex (Contracts + nested UploadGrant/CaptureEvidence/MediaObject; media_state_vocabulary/0 at line 124 — the 132-04 canary; new_media_object/1 at line 157)
    - lib/crosswake/companions/rindle/reconciliation.ex (Reconciliation + nested Attempt/IdempotencyKey/EvidenceResult; aliases .Contracts at line 10 — moves with the file)
    - packages/crosswake_rulestead/lib/crosswake/companions/rulestead.ex:9 (the @compile {:no_warn_undefined, Rulestead} the moved adapter needs — the rindle adapter in core does NOT have it yet because rindle modules currently exist in core)
    - lib/crosswake/companion_guard.ex:35 (@extracted_companion_names currently lists only Crosswake.Companions.Rulestead; PATTERNS.md §companion_guard reproduces the before/after)
  </read_first>
  <files>lib/crosswake/companions/rindle.ex, lib/crosswake/companions/rindle/contracts.ex, lib/crosswake/companions/rindle/reconciliation.ex, packages/crosswake_rindle/lib/crosswake/companions/rindle.ex, packages/crosswake_rindle/lib/crosswake/companions/rindle/contracts.ex, packages/crosswake_rindle/lib/crosswake/companions/rindle/reconciliation.ex, lib/crosswake/companion_guard.ex</files>
  <action>
    Move the three core source files into the package, module names PRESERVED
    (Crosswake.Companions.Rindle stays Crosswake.Companions.Rindle — non-breaking, per
    recipe Step 1 and the REQUIREMENTS.md naming-convention reminder: package
    crosswake_rindle != module Crosswake.Companions.Rindle):
    - lib/crosswake/companions/rindle.ex -> packages/crosswake_rindle/lib/crosswake/companions/rindle.ex
    - lib/crosswake/companions/rindle/contracts.ex -> packages/crosswake_rindle/lib/crosswake/companions/rindle/contracts.ex
    - lib/crosswake/companions/rindle/reconciliation.ex -> packages/crosswake_rindle/lib/crosswake/companions/rindle/reconciliation.ex
    Delete the originals (and the now-empty lib/crosswake/companions/rindle/ directory).

    On the moved rindle.ex, add @compile {:no_warn_undefined, Rindle} at the top of the
    module (recipe Step 1 mod 1 / D-29) — optional: true alone does NOT silence the
    undefined-module warning in engine-absent builds. Keep the Code.ensure_loaded?(Rindle)
    probes INSIDE function bodies verbatim (EXTRACT-04-clean). contracts.ex and
    reconciliation.ex move with no source edits (the .Contracts alias in reconciliation.ex
    moves with the file).

    In the SAME commit, add "Crosswake.Companions.Rindle" to @extracted_companion_names in
    lib/crosswake/companion_guard.ex (after the existing Rulestead entry). The guard comment
    mandates changing this attribute AND removing the source in the same PR. The MapSet
    prefix naturally covers the .Contracts/.Reconciliation children (D-02). Do NOT introduce
    any blanket Companions.* ban — Sigra/Chimeway stay legal (D-14, deferred milestone).

    After the move, verify core lib/ is clean:
    grep -r "Crosswake.Companions.Rindle" lib/ must return nothing (recipe Step 1 verify).
  </action>
  <verify>
    <automated>! grep -rq 'Crosswake.Companions.Rindle' lib/ && grep -q 'Crosswake.Companions.Rindle' lib/crosswake/companion_guard.ex && test -f packages/crosswake_rindle/lib/crosswake/companions/rindle.ex && grep -q 'no_warn_undefined, Rindle' packages/crosswake_rindle/lib/crosswake/companions/rindle.ex && mix test test/crosswake/proof/phase130_extraction_guards_test.exs 2>&1 | tail -8</automated>
  </verify>
  <done>
    The three rindle source files live under packages/crosswake_rindle/lib/ with module
    names preserved and @compile {:no_warn_undefined, Rindle} on the adapter; core lib/
    contains no Crosswake.Companions.Rindle reference except the companion_guard.ex MapSet
    entry; the EXTRACT-03 guard (phase130_extraction_guards_test.exs) is green.
  </done>
</task>

<task type="auto">
  <name>Task 2: Move the rindle coupling tests to the companion lane + copy the 5 media helpers</name>
  <read_first>
    - .planning/phases/132-generalization-proof-rindle-compat-matrix/132-RESEARCH.md §"DRIFT DISCOVERED" + §"Common Pitfalls" Pitfall 4/7 (the SEVEN coupling files, the media_lane_live.ex 5th helper, the Code.require_file rewrite, the advisory tag change)
    - .planning/phases/132-generalization-proof-rindle-compat-matrix/132-PATTERNS.md §"Moved Files — No New Pattern Needed" (the per-file move table + the 5 media helper source paths)
    - test/crosswake/proof/phase45_rindle_advisory_test.exs (the @moduletag :advisory_only -> :engine_present change + moduledoc env-var update — Pitfall 7)
    - packages/crosswake_rulestead/test/support/study_session_live.ex (the StudySessionLive stub to copy, D-23)
    - examples/phoenix_host/lib/crosswake_example/media/ (the 5 helper sources: reconciliation_keys, reconciliation_inbox, mock_capture, media_projection, media_lane_live)
  </read_first>
  <files>packages/crosswake_rindle/test/crosswake/proof/phase45_rindle_companion_test.exs, packages/crosswake_rindle/test/crosswake/proof/phase45_rindle_mock_media_test.exs, packages/crosswake_rindle/test/crosswake/proof/phase45_rindle_advisory_test.exs, packages/crosswake_rindle/test/crosswake/proof/phase45_rindle_live_test.exs, packages/crosswake_rindle/test/crosswake/proof/phase72_media_evidence_workflow_proof_test.exs, packages/crosswake_rindle/test/crosswake/companions/rindle/contracts_test.exs, packages/crosswake_rindle/test/crosswake/companions/rindle/reconciliation_test.exs, packages/crosswake_rindle/test/support/study_session_live.ex, packages/crosswake_rindle/test/support/example_host/reconciliation_keys.ex, packages/crosswake_rindle/test/support/example_host/reconciliation_inbox.ex, packages/crosswake_rindle/test/support/example_host/mock_capture.ex, packages/crosswake_rindle/test/support/example_host/media_projection.ex, packages/crosswake_rindle/test/support/example_host/media_lane_live.ex</files>
  <action>
    Move the five rindle DOMAIN/adapter proof tests and the two unit tests from core to
    the companion lane (delete the core originals):
    - phase45_rindle_companion_test.exs, phase45_rindle_mock_media_test.exs,
      phase45_rindle_advisory_test.exs, phase45_rindle_live_test.exs,
      phase72_media_evidence_workflow_proof_test.exs ->
      packages/crosswake_rindle/test/crosswake/proof/
    - test/crosswake/companions/rindle/{contracts_test,reconciliation_test}.exs ->
      packages/crosswake_rindle/test/crosswake/companions/rindle/ (verbatim — zero
      test/support deps).

    Copy the StudySessionLive stub (D-23) to
    packages/crosswake_rindle/test/support/study_session_live.ex (3-line stub, no drift
    risk). Copy the FIVE media helpers (RESEARCH.md drift: media_lane_live.ex is the 5th,
    added beyond CONTEXT.md's four) from examples/phoenix_host/lib/crosswake_example/media/
    into packages/crosswake_rindle/test/support/example_host/: reconciliation_keys.ex,
    reconciliation_inbox.ex, mock_capture.ex, media_projection.ex, media_lane_live.ex. Copy
    (do NOT relative-path back to examples/) so the package is standalone-installable; the
    hermeticity self-scan matches on basename so the copy is safe (D-04).

    In the moved phase45_mock_media, phase45_live, and phase72 tests, rewrite each
    Code.require_file call to reference the copied helpers via
    Path.join([__DIR__, "../../support/example_host", "<file>.ex"]) — the directory depth
    changed, so the original relative paths break (Pitfall 4).

    In the moved phase45_rindle_advisory_test.exs, change @moduletag :advisory_only to
    @moduletag :engine_present (mirroring rulestead's phase43 advisory test) and update the
    moduledoc that references MIX_INCLUDE_RINDLE=1 to reference the ENGINE_PRESENT_LANE=1
    pattern (Pitfall 7) — the env var no longer exists post-extraction.

    Do NOT move phase47_companion_arc_test.exs or test/crosswake/guides/companions_test.exs
    — those stay in core and are seam-rewritten in Task 3.
  </action>
  <verify>
    <automated>test -f packages/crosswake_rindle/test/crosswake/proof/phase72_media_evidence_workflow_proof_test.exs && test -f packages/crosswake_rindle/test/support/example_host/media_lane_live.ex && ! test -f test/crosswake/proof/phase72_media_evidence_workflow_proof_test.exs && ! test -d test/crosswake/companions/rindle && grep -q 'engine_present' packages/crosswake_rindle/test/crosswake/proof/phase45_rindle_advisory_test.exs && echo TESTS_MOVED_OK</automated>
  </verify>
  <done>
    The five proof tests + two unit tests live under packages/crosswake_rindle/test/ and
    are gone from core; the StudySessionLive stub and all five media helpers are copied
    into the companion test/support/; the moved tests' Code.require_file paths resolve
    locally; the advisory test is tagged :engine_present with an updated moduledoc.
  </done>
</task>

<task type="auto">
  <name>Task 3: Seam-rewrite the two core tests, chain companions.test, parameterize verify script, add phase132-proof.yml</name>
  <read_first>
    - .planning/phases/132-generalization-proof-rindle-compat-matrix/132-RESEARCH.md §"Resolved Investigation Items" (phase47 tests 1-2 stub rewrite; companions_test.exs lines 122/131 remove + 150/228 substitute — exact dispositions)
    - .planning/phases/132-generalization-proof-rindle-compat-matrix/132-PATTERNS.md §companions_test.exs + §phase47 + §mix.exs + §phase132-proof.yml + §verify_companion_package.sh (the surgical edits + the three-job CI structure)
    - test/crosswake/guides/companions_test.exs:116 (the rulestead extraction comment explaining why lines 122/131 are removed and 150/228 substituted)
    - test/crosswake/proof/phase47_companion_arc_test.exs:4 (alias Crosswake.Companions.Rindle used only by tests 1-2; tests 3-6 need no change)
    - mix.exs:59 (companions.test alias currently only rulestead)
    - script/verify_companion_package.sh:53 (the rulestead.ex hardcode to parameterize from $PACKAGE)
    - .github/workflows/phase130-proof.yml (the three-job companion-lane template + on:/setup-beam blocks)
    - .github/workflows/phase72-proof.yml (the existing macos-15 proof to retire/redirect)
  </read_first>
  <files>test/crosswake/guides/companions_test.exs, test/crosswake/proof/phase47_companion_arc_test.exs, mix.exs, script/verify_companion_package.sh, .github/workflows/phase132-proof.yml, .github/workflows/phase72-proof.yml</files>
  <action>
    Seam-rewrite test/crosswake/guides/companions_test.exs (stays in core): add the alias
    Crosswake.TestSupport.StubRindleAbsentCompanion alongside the existing
    StubRulesteadAbsentCompanion alias; REMOVE line 122
    (Code.ensure_loaded!(Crosswake.Companions.Rindle)) and line 131
    (function_exported?(Crosswake.Companions.Rindle, :validate_dependency, 0)) — the API
    guard now lives in the companion lane, mirroring the rulestead extraction precedent
    documented at line 116-118; SUBSTITUTE Crosswake.Companions.Rindle with
    StubRindleAbsentCompanion at the two companions-list sites (lines 150 and 228). Verify
    no alias to the moved adapter remains in the file.

    Partial-rewrite test/crosswake/proof/phase47_companion_arc_test.exs (stays in core):
    replace alias Crosswake.Companions.Rindle (line 4) with
    alias Crosswake.TestSupport.StubRindleAbsentCompanion, as: Rindle — this drives tests
    1-2 (Doctor.run through the @behaviour/registry seam) via the stub. Tests 3-6 use no
    rindle internals and need no changes. If test 5 (the hermetic-lane self-scan) checks a
    MIX_INCLUDE_RINDLE string, update it to reflect the post-extraction stub pattern (the
    self-scan checks what the file contains).

    Extend the core mix.exs companions.test alias (line 59) to chain both lanes:
    ["cmd --cd packages/crosswake_rulestead mix test", "cmd --cd packages/crosswake_rindle mix test"].

    Parameterize script/verify_companion_package.sh line 53: derive
    COMPANION_NAME=$(echo "$PACKAGE" | sed 's/^crosswake_//') and check
    lib/crosswake/companions/${COMPANION_NAME}.ex in the unpacked tarball instead of the
    hardcoded rulestead.ex; keep the [crosswake] FAIL message format, naming the derived
    path. Line 81 is comment-only — no change.

    Create .github/workflows/phase132-proof.yml as a copy-substitute of phase130-proof.yml
    (rulestead->rindle, 130->132, phase42/43->phase45/72) with three jobs: (1)
    core-hermetic-proof (merge-blocking, PR+push) running phase130_extraction_guards_test.exs,
    phase132_compat_matrix_drift_test.exs, and the broad hermetic suite (--exclude
    requires_example_host --exclude advisory_only); (2) companion-engine-absent-proof
    (merge-blocking) running mix companions.test for the rindle lane plus
    CROSSWAKE_RELEASE=1 bash script/verify_companion_package.sh crosswake_rindle; (3)
    companion-engine-present-proof (advisory, schedule + workflow_dispatch, continue-on-error)
    running mix clean then ENGINE_PRESENT_LANE=1 mix test --only engine_present in the rindle
    package. Use ubuntu-latest and the pinned setup-beam (elixir 1.19.5 / OTP 27.3 from
    .tool-versions). Retire or redirect .github/workflows/phase72-proof.yml — since phase72
    moved to the companion lane and runs inside job (2)/(3), delete the standalone
    macos-15 phase72-proof.yml (its proof now runs in the rindle companion lane).
  </action>
  <verify>
    <automated>! grep -q 'alias Crosswake.Companions.Rindle$' test/crosswake/proof/phase47_companion_arc_test.exs && ! grep -q 'function_exported?(Crosswake.Companions.Rindle' test/crosswake/guides/companions_test.exs && grep -q 'crosswake_rindle' mix.exs && grep -q 'COMPANION_NAME' script/verify_companion_package.sh && test -f .github/workflows/phase132-proof.yml && mix test test/crosswake/guides/companions_test.exs test/crosswake/proof/phase47_companion_arc_test.exs 2>&1 | tail -8</automated>
  </verify>
  <done>
    companions_test.exs and phase47_companion_arc_test.exs stay in core, exercise rindle
    only through StubRindleAbsentCompanion via the registry, and pass green; mix.exs
    companions.test chains the rindle lane; verify_companion_package.sh is parameterized
    from $PACKAGE; phase132-proof.yml has the three-job structure on ubuntu-latest and the
    standalone phase72-proof.yml is retired.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| core lib/ -> companion package | The `@behaviour` seam + `:companions` registry is the only sanctioned crossing; a static alias to the moved adapter would re-couple |
| Hex registry -> companion engine dep | `{:rindle, "~> 0.1", optional: true}` resolves an external author-controlled package at build time |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-132-05 | Tampering | re-coupling via a surviving core alias to the moved adapter | high | mitigate | CompanionGuard MapSet gains `Crosswake.Companions.Rindle`; the EXTRACT-03 guard (phase130_extraction_guards_test.exs) is merge-blocking and fails the build on any static alias under lib/ (Task 1 verify) |
| T-132-02 | Tampering | engine-present stub `.beam` leaking into the engine-absent lane | low | mitigate | `phase132-proof.yml` job (3) runs `mix clean` before the engine-present build; `:engine_present` excluded by default in the companion test_helper (D-33) |
| T-132-06 | Spoofing | parameterized `verify_companion_package.sh` `$PACKAGE` shaping a tarball path | low | accept | `$PACKAGE` is a CI-controlled literal (`crosswake_rindle`) passed by the workflow, not untrusted input; the derived path is checked for existence only |
| T-132-SC | Tampering | npm/pip/cargo installs | low | accept | No package-manager installs in this plan — source moves, test moves, CI YAML; no new external packages (RESEARCH.md §Package Legitimacy Audit: none) |
</threat_model>

<verification>
- `grep -r "Crosswake.Companions.Rindle" lib/` returns only the `companion_guard.ex`
  MapSet entry (SEAM-05 structural witness).
- `mix test test/crosswake/proof/phase130_extraction_guards_test.exs` green (EXTRACT-03).
- `mix companions.test` runs both lanes; the rindle lane passes engine-absent.
- `CROSSWAKE_RELEASE=1 bash script/verify_companion_package.sh crosswake_rindle` passes.
- `mix verify` green (companions.test + hermetic core suite).
</verification>

<success_criteria>
- EXTRACT-07: rindle adapter + Contracts + Reconciliation live in
  `packages/crosswake_rindle/` (module names preserved), tests pass engine-absent.
- SEAM-05: no rindle-specific branch in core lib/; the EXTRACT-03 guard is green; the
  recipe applied with the CompanionGuard MapSet as the only core edit.
</success_criteria>

<output>
Create `.planning/phases/132-generalization-proof-rindle-compat-matrix/132-03-SUMMARY.md` when done
</output>
