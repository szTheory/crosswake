---
phase: 109-drift-prevention-gate
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - brandbook/tools/check-consumer-drift.mjs
autonomous: true
requirements: [PROOF-01]
nyquist_compliant: true

must_haves:
  truths:
    - "Running `node brandbook/tools/check-consumer-drift.mjs` exits 0 on the current clean tree (green baseline)"
    - "The script scans exactly the 5 curated normalized consumers and excludes the deferred offenders"
    - "A bare hex color literal in a scanned file produces a non-zero exit with a file:line — rule report"
    - "A `var(--cw-primitive-` reference in any scanned file produces a non-zero exit"
    - "A CSS consumer with zero `var(--cw-` references produces a non-zero exit"
    - "A HEEX/template consumer with a retired Tailwind class in a class=\"...\" attribute produces a non-zero exit"
    - "CSS `#id` selectors, `rgba()` shadows, inline `display:flex`, and `[scrollbar-gutter:stable]` are NOT flagged"
    - "The per-file check functions are exported so the contract test can import them without running the scan"
  artifacts:
    - path: "brandbook/tools/check-consumer-drift.mjs"
      provides: "Curated-manifest consumer drift detection script (plain node, exit 1 on violation) with exported check functions"
      min_lines: 90
      contains: "MANIFEST"
  key_links:
    - from: "brandbook/tools/check-consumer-drift.mjs"
      to: "brandbook/tools/contrast.mjs"
      via: "named import of parseHex + PALETTE for human-readable violation messages"
      pattern: "import.*PALETTE.*from.*contrast"
    - from: "brandbook/tools/check-consumer-drift.mjs MANIFEST"
      to: "the 5 normalized consumer files on disk"
      via: "resolve(ROOT, entry.path) where ROOT = resolve(__dirname, '../..')"
      pattern: "resolve\\(ROOT"
---

<objective>
Create the deterministic, browser-free consumer-drift detection script `brandbook/tools/check-consumer-drift.mjs`. It scans a curated manifest of the 5 normalized consumer files and exits non-zero when any reintroduces brand-color drift (bare hex, primitive-tier token reference, or lost `var(--cw-` coverage), or — for templates — a retired Tailwind utility class. This is the core deliverable PROOF-01 is built on and the one-command local runner (success criterion #4).

Purpose: Codify the Phase 108 clean state so it cannot silently regress. The tree is zero-hex with live `var(--cw-` references TODAY (verified), so the gate must land GREEN — it is a confirmatory gate, not a remediation.
Output: `brandbook/tools/check-consumer-drift.mjs` — plain node script, exit 0 on the current tree, exit 1 + `file:line — rule` report on any violation, with exported per-file check functions for the contract test (Plan 02) to import.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/109-drift-prevention-gate/109-CONTEXT.md
@.planning/phases/109-drift-prevention-gate/109-RESEARCH.md
@.planning/phases/109-drift-prevention-gate/109-PATTERNS.md

<interfaces>
<!-- Reusable exports from brandbook/tools/contrast.mjs. Use directly — no codebase exploration needed. -->

From brandbook/tools/contrast.mjs (named exports on the final export line):
  export { linearize, luminance, parseHex, contrast, PALETTE };

  parseHex(hex): validates 6-digit hex ONLY (regex /^[0-9a-fA-F]{6}$/), throws on 3/8-digit.
    Use ONLY for PALETTE name-lookup AFTER confirming a 6-digit match. Never use for scanning.
  PALETTE: 17-color brand-hex object, keys like 'wake-700', values like '#2B756A'.
    Use only for human-readable violation messages (name-lookup).

ROOT derivation pattern (from compile-tokens.test.mjs:12-13, check-candidates.mjs:23-24):
  const __dirname = dirname(fileURLToPath(import.meta.url));
  const ROOT = resolve(__dirname, '../..');   // brandbook/tools/ → repo root

IS_MAIN dual-use guard (from check-candidates.mjs:110):
  const IS_MAIN = process.argv[1] === fileURLToPath(import.meta.url);
  if (IS_MAIN) { /* main scan loop */ }
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Detection module — manifest, check functions, and exported API</name>
  <files>brandbook/tools/check-consumer-drift.mjs</files>
  <read_first>
    - brandbook/tools/check-consumer-drift.mjs (does not yet exist — confirm before creating)
    - brandbook/tools/contrast.mjs (lines 1-60 — exact parseHex signature + PALETTE values to import)
    - brandbook/tools/check-production.mjs (the plain-node header/shebang/JSDoc + per-file-loop + exit-0/exit-1 convention to mirror)
    - brandbook/tools/check-candidates.mjs (the IS_MAIN guard at ~line 110 for module/script dual use)
    - .planning/phases/109-drift-prevention-gate/109-PATTERNS.md (lines 21-245 — exact regexes, manifest shape, function bodies)
    - .planning/phases/109-drift-prevention-gate/109-RESEARCH.md (Detection Regex Architecture section + Common Pitfalls)
  </read_first>
  <behavior>
    - findHexColors(content): given `color: #2B756A;` returns one violation whose text includes `#2B756A`; given `#status { color: var(--cw-text-muted); }` returns zero (the `#id` selector is not a value-context hex); given `box-shadow: 0 4px 6px rgba(9,20,26,0.06);` returns zero (rgba is not `#`-hex). For a 6-digit match equal to a PALETTE value, the violation text includes the palette name (e.g. `#2B756A (wake-700)`).
    - findPrimitiveRefs(content): given `color: var(--cw-primitive-foam-50);` returns ≥1; given `color: var(--cw-surface-default);` returns zero.
    - checkCssSemanticCoverage(content): given `body { color: red; }` returns `{ ok: false }`; given any content containing `var(--cw-` returns `{ ok: true }`.
    - findRetiredTailwindInClassAttrs(content): given `<div class="flex items-center">` returns a violation with text `flex`; given `<style>body { display: flex; }</style><div class="btn-primary">` returns zero (CSS property, not a class attr); given `<html lang="en" class="[scrollbar-gutter:stable]">` returns zero (intentional layout utility, not in blocklist).
  </behavior>
  <action>
    Create `brandbook/tools/check-consumer-drift.mjs` with a `#!/usr/bin/env node` shebang and a JSDoc header (mirror check-production.mjs style) describing the 4 drift rules and the usage line `node brandbook/tools/check-consumer-drift.mjs [--verbose]`.

    Imports: `readFileSync` from `node:fs`; `resolve`, `dirname` from `node:path`; `fileURLToPath` from `node:url`; named import `parseHex, PALETTE` from `./contrast.mjs`. Derive `const ROOT = resolve(dirname(fileURLToPath(import.meta.url)), '../..')`.

    Define `const MANIFEST` as an inline JS array (D-02 curated manifest, NOT a glob) of exactly these 5 entries, each `{ path, type }`:
      - `examples/phoenix_host/priv/static/css/app.css` → type `css`
      - `priv/static/crosswake/offline.css` → type `css`
      - `priv/templates/crosswake/offline_ui/offline_page.html.heex.eex` → type `heex`
      - `priv/templates/crosswake/offline_ui/offline_root.html.heex.eex` → type `heex`
      - `examples/phoenix_host/lib/crosswake_example_web/controllers/offline_html/index.html.heex` → type `heex`
    Above MANIFEST, place a header comment documenting (a) D-02 "add new normalized consumers here", (b) the two EXCLUDED deferred offenders with the CORRECT path for the second one: `examples/phoenix_host/priv/static/offline_study.js` and `examples/phoenix_host/lib/crosswake_example/saas_portal/step_up_challenge_live.ex` (NOT `crosswake_example_web/live/saas_portal/...` — that path does not exist), and (c) that `priv/static/crosswake/tokens.css` is excluded because byte-parity is covered by compile-tokens.test.mjs:222 (D-04).

    Implement and EXPORT these pure functions (named exports so Plan 02 can import them):
      - `findHexColors(content)`: line-by-line scan using the lookbehind hex regex `/(?<=[:,(\s])#([0-9a-fA-F]{6}|[0-9a-fA-F]{8}|[0-9a-fA-F]{3})\b/g` (value-context lookbehind excludes `#id` selectors; valid CSS hex lengths 3/6/8 only). Return array of `{ line, text, rule: 'hex-color-forbidden' }`. For 6-digit matches, look up the hex in PALETTE (case-insensitive) and append ` (paletteName)` to text when found. Do NOT call parseHex during scanning (it throws on 3/8-digit) — PALETTE lookup is a plain `Object.entries(PALETTE).find(...)`.
      - `findPrimitiveRefs(content)`: substring/regex match `var(--cw-primitive-`, return array of `{ line, text, rule: 'primitive-token-forbidden' }`.
      - `checkCssSemanticCoverage(content)`: return `{ ok: /var\(--cw-/.test(content) }`.
      - `findRetiredTailwindInClassAttrs(content)`: extract only `class="([^"]*)"` attribute values via regex; for each, flag any blocklist token found as a substring; return array of `{ line, text, rule: 'retired-tailwind-class-forbidden' }`. The blocklist `RETIRED_TAILWIND` is exactly: `flex`, `bg-white`, `bg-cw-`, `text-cw-`, `min-h-screen`, `border-cw-`, `border-gray-`, `space-y-`, `max-w-md` (derived from the NORM-04 test contract). Scope to class attributes ONLY so `display: flex` in an inline `<style>` is never flagged; `[scrollbar-gutter:stable]` is intentionally absent from the blocklist and must not be flagged.

    Implement `checkFile(entry, content)` dispatch: every entry → findHexColors + findPrimitiveRefs; `css` entries also → checkCssSemanticCoverage (push a `semantic-coverage-lost` violation if `!ok`); `heex` entries also → findRetiredTailwindInClassAttrs. Export `checkFile` and `MANIFEST` too.

    No fenced code, no full implementation here — name the identifiers, regexes, exit codes, and message shapes; the executor writes the bodies guided by the exact excerpts in 109-PATTERNS.md lines 95-173.
  </action>
  <verify>
    <automated>node -e "import('./brandbook/tools/check-consumer-drift.mjs').then(m => { const h = m.findHexColors('color: #2B756A;'); const idg = m.findHexColors('#status { color: var(--cw-text-muted); }'); const rgba = m.findHexColors('box-shadow: 0 4px 6px rgba(9,20,26,0.06);'); const flex = m.findRetiredTailwindInClassAttrs('<div class=\"flex\">'); const styleFlex = m.findRetiredTailwindInClassAttrs('<style>body{display:flex}</style><div class=\"btn-primary\">'); const sg = m.findRetiredTailwindInClassAttrs('<html class=\"[scrollbar-gutter:stable]\">'); const cov = m.checkCssSemanticCoverage('body{color:red}'); if (h.length===1 && idg.length===0 && rgba.length===0 && flex.length===1 && styleFlex.length===0 && sg.length===0 && cov.ok===false) { console.log('PASS'); } else { console.error('FAIL', JSON.stringify({h:h.length,idg:idg.length,rgba:rgba.length,flex:flex.length,styleFlex:styleFlex.length,sg:sg.length,cov:cov.ok})); process.exit(1); } })"</automated>
  </verify>
  <acceptance_criteria>
    - `node -e` import smoke test (above) prints `PASS`: hex detected, `#id` not flagged, rgba not flagged, class-attr `flex` flagged, inline-style `display:flex` not flagged, `[scrollbar-gutter:stable]` not flagged, zero-`var` coverage fails.
    - `brandbook/tools/check-consumer-drift.mjs` exports (at minimum) `findHexColors`, `findPrimitiveRefs`, `checkCssSemanticCoverage`, `findRetiredTailwindInClassAttrs`, `checkFile`, `MANIFEST`.
    - MANIFEST contains exactly the 5 paths above and none of the deferred offenders or `tokens.css`.
    - The deferred-offender header comment uses `crosswake_example/saas_portal/step_up_challenge_live.ex` (corrected path), not `crosswake_example_web/live/saas_portal/`.
  </acceptance_criteria>
  <done>The detection module exists with the curated 5-entry manifest, all four exported check functions behave per the smoke test, and no fenced implementation was placed in the plan action.</done>
</task>

<task type="auto">
  <name>Task 2: Main scan loop, file:line reporting, and green-baseline exit</name>
  <files>brandbook/tools/check-consumer-drift.mjs</files>
  <read_first>
    - brandbook/tools/check-consumer-drift.mjs (the module from Task 1 — extend it, do not rewrite)
    - brandbook/tools/check-production.mjs (lines ~73-126 — the per-file loop, console.error reporting, exit-0/exit-1 convention, and file-read try/catch to copy)
    - .planning/phases/109-drift-prevention-gate/109-PATTERNS.md (lines 175-245 — core scan loop + IS_MAIN guard + GitHub ::error annotation format)
  </read_first>
  <action>
    Add an `IS_MAIN` guard (`process.argv[1] === fileURLToPath(import.meta.url)`) so the module can be imported by Plan 02's test without running the scan. Inside `if (IS_MAIN) { ... }`:

    Iterate MANIFEST. For each entry, `resolve(ROOT, entry.path)` and `readFileSync(abs, 'utf8')` inside a try/catch that records `Cannot read file: <msg>` as a violation and continues (copy check-production.mjs error handling). Run `checkFile(entry, content)` and collect violations.

    Report each violation as `entry.path:LINE — RULE — TEXT` to `console.error`, grouped per file with a `FAIL: <path>` header (mirror check-production.mjs output style). Also emit a GitHub annotation line per violation: `::error file=<path>,line=<n>::<rule> <text>` (discretionary annotation per CONTEXT; semantic-coverage-lost violations may use line 1).

    Exit convention: if total violations === 0, `console.log` an `All N consumer file(s) passed drift check.` summary and `process.exit(0)`; otherwise print an `N file(s) failed drift check. Fix the above before proceeding.` footer and `process.exit(1)`. Support an optional `--verbose` flag that also logs `OK: <path>` for clean files (mirror check-production.mjs).

    The script must exit 0 on the current tree (verified zero-hex, ≥1 `var(--cw-` per CSS file, no retired Tailwind in class attrs) — this is the green baseline. No fenced code in this action.
  </action>
  <verify>
    <automated>node brandbook/tools/check-consumer-drift.mjs; echo "exit=$?"</automated>
  </verify>
  <acceptance_criteria>
    - `node brandbook/tools/check-consumer-drift.mjs` exits 0 on the current clean tree and prints an all-passed summary (green baseline — PROOF-01 SC #4 one-command local runner).
    - Temporarily appending `color: #2B756A;` to a COPY of any manifested CSS file and pointing the script at it produces exit 1 with a `file:line — rule` report (validated by the Plan 02 contract test; do not mutate the real files here).
    - The script is importable as a module without executing the scan (IS_MAIN guard present), enabling Plan 02 imports.
    - Uses only Node built-ins + the `contrast.mjs` sibling import — no new npm dependency, no browser, no Playwright (PROOF-01 SC #3).
  </acceptance_criteria>
  <done>`node brandbook/tools/check-consumer-drift.mjs` runs in under a second, exits 0 on the current tree with a clear summary, exits 1 with per-violation file:line reporting when drift is present, and is import-safe via the IS_MAIN guard.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| committed source files → drift script | The script reads only committed, in-repo files via a hardcoded curated manifest. No untrusted/user-supplied input, no network, no external services. Read-only. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-109-01 | Tampering (gate correctness) | check-consumer-drift.mjs detection logic | mitigate | The realistic risk is the gate silently PASSING when it should fail (false-negative coverage gap). Mitigated by exporting pure check functions exercised with synthetic positive/negative fixtures in Plan 02's contract test, plus the Task 1 import smoke test asserting each guard. |
| T-109-02 | Information Disclosure | file reads | accept | Reads only committed repo files via a fixed manifest; no PII, no secrets, no network egress. Low-value, read-only. |
| T-109-03 | Denial of Service | regex scan | accept | Inputs are small committed files; regexes are linear (no catastrophic backtracking — lookbehind + bounded `{3,8}` quantifiers). Runtime < 1s. |
| T-109-SC | Tampering | npm/pip/cargo installs | accept | No package installs introduced — script uses only Node built-ins + the existing `contrast.mjs` sibling module. No `[ASSUMED]`/`[SUS]` packages; package legitimacy gate N/A. |
</threat_model>

<verification>
- `node brandbook/tools/check-consumer-drift.mjs` exits 0 on the current tree (green baseline).
- The Task 1 `node -e` import smoke test prints `PASS` (all four guards behave correctly).
- Detection of hex, primitive refs, lost coverage, and retired Tailwind classes is proven by the smoke test; full negative-fixture proof lands in Plan 02.
</verification>

<success_criteria>
- `brandbook/tools/check-consumer-drift.mjs` exists with the curated 5-entry manifest and exported check functions.
- Exits 0 on the current clean tree; exits 1 with `file:line — rule` reporting on any drift.
- Browser-free, OS-deterministic, zero new npm dependencies (PROOF-01 SC #3, #4).
- False-positive guards in place: `#id` selectors, `rgba()` shadows, inline `display:flex`, `[scrollbar-gutter:stable]`.
</success_criteria>

<output>
Create `.planning/phases/109-drift-prevention-gate/109-01-SUMMARY.md` when done.
</output>
