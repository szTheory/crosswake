---
phase: 53-release-continuity-and-closeout-hardening
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - CHANGELOG.md
  - lib/crosswake/planning/closeout_verifier.ex
  - lib/crosswake/doctor/publish_readiness.ex
  - test/crosswake/planning/closeout_verifier_test.exs
  - test/crosswake/doctor/publish_readiness_test.exs
  - test/crosswake/hex_page_test.exs
autonomous: true
requirements:
  - REL-01
must_haves:
  truths:
    - "CHANGELOG.md distinguishes unreleased v3.6 support claims from the published Hex release 0.1.0."
    - "Closeout drift fails closed through a shared Elixir validator instead of a checklist-only process."
    - "Deferred provider, auth, notification, and shell claims remain explicitly non-shipped in public release truth."
    - "D-08: ExUnit tests under `test/crosswake/planning` back the closeout validator so CI and local `mix test` exercise the same checks as the operator command."
    - "D-11: release prose and durable lessons remain human judgment, while artifact existence, frontmatter shape, traceability, exception shape, and public-claim parity are deterministic closeout checks."
  artifacts:
    - path: "CHANGELOG.md"
      provides: "Canonical public release history with richer [Unreleased] truth"
      contains: "## [Unreleased]"
    - path: "lib/crosswake/planning/closeout_verifier.ex"
      provides: "Shared closeout validation contract and actionable stable-id failures"
      exports: ["run/1", "render/1"]
    - path: "test/crosswake/planning/closeout_verifier_test.exs"
      provides: "Fail-closed REL-01 regression coverage for milestone closeout truth"
      contains: "closeout."
  key_links:
    - from: "lib/crosswake/planning/closeout_verifier.ex"
      to: ".planning/milestones/v3.6-CLOSEOUT.md"
      via: "file-backed closeout ledger parsing"
      pattern: "v3\\.6-CLOSEOUT"
    - from: "lib/crosswake/doctor/publish_readiness.ex"
      to: "CHANGELOG.md"
      via: "publish parity check"
      pattern: "Unreleased"
---

<objective>
Build the shared closeout-verification core and lock release/changelog truth before any CLI or CI wiring.

Purpose: enforce REL-01 at the two canonical truth surfaces the user locked in D-01 through D-12: `CHANGELOG.md` for public release history and a shared Elixir validator for v3.6 closeout evidence.
Output: a tested `Crosswake.Planning.CloseoutVerifier`, richer `[Unreleased]` changelog structure, and publish-readiness tests that fail closed on false shipped language or missing public claim boundaries.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/MILESTONE-ARC.md
@.planning/milestones/v3.6-CLOSEOUT.md
@.planning/phases/53-release-continuity-and-closeout-hardening/53-CONTEXT.md
@.planning/phases/53-release-continuity-and-closeout-hardening/53-RESEARCH.md
@.planning/phases/53-release-continuity-and-closeout-hardening/53-PATTERNS.md
@.planning/phases/53-release-continuity-and-closeout-hardening/53-VALIDATION.md
@.planning/phases/48-strategic-signal-and-milestone-memory/48-03-SUMMARY.md
@.planning/phases/50-doctor-publish-and-readiness-checks/50-01-SUMMARY.md
@.planning/phases/52-operator-proof-and-docs/52-01-SUMMARY.md
@CHANGELOG.md
@lib/crosswake/doctor/publish_readiness.ex
@test/crosswake/planning/milestone_arc_closeout_parity_test.exs
@test/crosswake/doctor/publish_readiness_test.exs
@test/crosswake/hex_page_test.exs
@test/support/proof_assertions.ex

<interfaces>
From `lib/crosswake/doctor/publish_readiness.ex`:
```elixir
def run(opts \\ [])
def findings(%Report{} = report)
def to_map(%Report{} = report)
```

From `test/support/proof_assertions.ex`:
```elixir
def stable_id_message(id, subject, source, observed, path, hint, posture)
def assert_normalized_json_fixture(id, json_payload, fixture_path, opts \\ [])
def assert_file_exact(id, path, expected, opts \\ [])
def assert_contains_exact(id, path, needle, opts \\ [])
```

From `.planning/milestones/v3.6-CLOSEOUT.md` the validator must understand these ledger keys:
`requirements_state`, `roadmap_parity`, `phase_verification_coverage`, `summary_frontmatter_coverage`, `validation_ledger_status`, `thread_seed_disposition`, `release_changelog_continuity`, `public_support_claim_changes`, `deferred_with_reason`, `exceptions`, `resolved_gaps`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Lock failing REL-01 contracts for closeout evidence and public release truth</name>
  <read_first>
    test/crosswake/planning/closeout_verifier_test.exs (create this file), test/crosswake/planning/milestone_arc_closeout_parity_test.exs, test/crosswake/doctor/publish_readiness_test.exs, test/crosswake/hex_page_test.exs, CHANGELOG.md, .planning/milestones/v3.6-CLOSEOUT.md, .planning/phases/53-release-continuity-and-closeout-hardening/53-CONTEXT.md
  </read_first>
  <files>test/crosswake/planning/closeout_verifier_test.exs, test/crosswake/doctor/publish_readiness_test.exs, test/crosswake/hex_page_test.exs</files>
  <behavior>
    - Test 1: missing verification, malformed `deferred_with_reason`, or missing `requirements-completed:` surfaces a blocking `closeout.*` failure with source, observed drift, and hint.
    - Test 2: release truth fails when `CHANGELOG.md` lacks the richer `[Unreleased]` split or confuses v3.6 planning work with published Hex `0.1.0`.
    - Test 3: deferred provider/auth/notification/shell claims remain explicitly non-shipped in public release wording and readiness checks.
  </behavior>
  <acceptance_criteria>
    - `test/crosswake/planning/closeout_verifier_test.exs` covers D-07 through D-12 fail-closed cases for roadmap parity, requirements state, phase verification, summary frontmatter, validation ledger drift, thread/seed status, release continuity, and malformed `deferred_with_reason`.
    - `test/crosswake/doctor/publish_readiness_test.exs` or `test/crosswake/hex_page_test.exs` asserts `[Unreleased]` has separate sections for unpublished support claims, verification-required or advisory surfaces, deferred non-shipped claims, and last published Hex truth `0.1.0` per D-02 and D-05.
    - The new failing tests use stable `closeout.*` or existing `diag.publish.*` ids instead of anonymous assertion text, per D-10 and D-20.
  </acceptance_criteria>
  <action>Create failing regression coverage for D-01 through D-12 and D-18 through D-24 before production edits. Add `test/crosswake/planning/closeout_verifier_test.exs` with file-backed fixtures against `.planning/milestones/v3.6-CLOSEOUT.md`, `.planning/STATE.md`, `.planning/REQUIREMENTS.md`, `.planning/ROADMAP.md`, and the existing v3.6 phase directories. Extend the publish-readiness and Hex page tests so they assert the exact public truth split required by D-02, reject shipped-language drift called out in D-03 and D-23, and keep deferred provider/auth/notification/shell claims explicitly non-shipped per D-24. Do not add shell scripts or SDK-only checks here.</action>
  <verify>
    <automated>mix test test/crosswake/planning/closeout_verifier_test.exs test/crosswake/doctor/publish_readiness_test.exs test/crosswake/hex_page_test.exs</automated>
  </verify>
  <done>The REL-01 regression suite exists, fails before implementation, and names the exact closeout and release-truth gaps Phase 53 must close.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement the shared closeout validator and changelog/publish-readiness enforcement</name>
  <read_first>
    lib/crosswake/planning/closeout_verifier.ex (create this file), lib/crosswake/doctor/publish_readiness.ex, test/crosswake/planning/closeout_verifier_test.exs, test/support/proof_assertions.ex, CHANGELOG.md, .planning/milestones/v3.6-CLOSEOUT.md, .planning/phases/53-release-continuity-and-closeout-hardening/53-RESEARCH.md
  </read_first>
  <files>lib/crosswake/planning/closeout_verifier.ex, lib/crosswake/doctor/publish_readiness.ex, CHANGELOG.md</files>
  <behavior>
    - Test 1: `Crosswake.Planning.CloseoutVerifier.run/1` returns a stable report/check structure and `render/1` exposes actionable failure lines.
    - Test 2: the validator treats missing or malformed closeout evidence as blocking, but keeps editorial wording outside the hard gate.
    - Test 3: publish-readiness treats richer `[Unreleased]` structure and published-versus-unreleased wording as deterministic truth checks without generating the full changelog.
  </behavior>
  <acceptance_criteria>
    - `lib/crosswake/planning/closeout_verifier.ex` defines a single source of truth for D-07 through D-12, with grouped stable ids such as `closeout.roadmap.*`, `closeout.requirements.*`, `closeout.state.*`, `closeout.validation.*`, `closeout.release.*`, and `closeout.handoff.*`.
    - Every blocking failure includes subject, source artifact, observed drift, expected command or shape, remediation hint, and a posture marker that distinguishes merge-blocking truth from human-review guidance per D-10 and D-21.
    - `CHANGELOG.md` stays the canonical release surface per D-01, keeps milestone labels distinct from Hex releases per D-05, and adds the richer `[Unreleased]` split from D-02 without introducing a second release ledger.
  </acceptance_criteria>
  <action>Implement `Crosswake.Planning.CloseoutVerifier` as the shared Elixir validator named in D-07, with nested report/check structs or an equivalent typed result shape that mirrors the stable-id discipline from Phase 50 and Phase 52. Parse `.planning/milestones/v3.6-CLOSEOUT.md`, `ROADMAP.md`, `REQUIREMENTS.md`, `STATE.md`, phase verification/validation files, SUMMARY frontmatter, and thread/seed directories so the deterministic failures listed in D-09 are enforced mechanically. Reuse the stable message style from `Crosswake.TestSupport.ProofAssertions`; do not hand-roll a second diagnostics vocabulary. Extend `Crosswake.Doctor.PublishReadiness` so its changelog parity check understands the D-02 subsection split and the D-05 published-versus-planning distinction, and rewrite `CHANGELOG.md` to point release readers at the correct planning archive surfaces instead of implying milestone labels are installable Hex releases. Keep prose human-authored per D-03 and D-04.</action>
  <verify>
    <automated>mix test test/crosswake/planning/closeout_verifier_test.exs test/crosswake/doctor/publish_readiness_test.exs test/crosswake/hex_page_test.exs</automated>
  </verify>
  <done>`Crosswake.Planning.CloseoutVerifier` and the changelog/publish-readiness truth checks pass together, with no second release ledger and no fail-open closeout gaps.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `CHANGELOG.md` -> public support claims | Human-authored release prose can overstate what Hex `0.1.0` actually published. |
| planning docs -> closeout verifier | YAML/frontmatter and planning-status files are untrusted inputs to the fail-closed validator. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-53-01 | Tampering | `CHANGELOG.md`, `lib/crosswake/doctor/publish_readiness.ex` | mitigate | Enforce D-01 through D-06 with deterministic `[Unreleased]` shape checks, explicit `0.1.0` published anchor, and deferred-claim wording tests. |
| T-53-02 | Repudiation | `lib/crosswake/planning/closeout_verifier.ex` | mitigate | Use stable `closeout.*` ids, subject/source/hint fields, and blocking posture for every D-09 failure so missing evidence never passes silently. |
| T-53-SC | Tampering | npm/pip/cargo installs | accept | No package-manager installs occur in this plan. |
</threat_model>

<verification>
Run the focused REL-01 regression suite after each task:
- `mix test test/crosswake/planning/closeout_verifier_test.exs test/crosswake/doctor/publish_readiness_test.exs test/crosswake/hex_page_test.exs`
</verification>

<success_criteria>
- `CHANGELOG.md` has a richer `[Unreleased]` structure that clearly separates unreleased support truth from published Hex `0.1.0`.
- `Crosswake.Planning.CloseoutVerifier` is the single source of truth for deterministic closeout checks and exposes stable, actionable failure ids.
- Publish-readiness and Hex page tests fail on false shipped language or missing deferred-claim boundaries.
</success_criteria>

<output>
Create `.planning/phases/53-release-continuity-and-closeout-hardening/53-01-SUMMARY.md` when done
</output>
