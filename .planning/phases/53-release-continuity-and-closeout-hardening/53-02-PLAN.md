---
phase: 53-release-continuity-and-closeout-hardening
plan: "02"
type: execute
wave: 2
depends_on:
  - "53-01"
files_modified:
  - lib/mix/tasks/closeout.verify.ex
  - test/mix/tasks/closeout_verify_test.exs
  - test/crosswake/planning/closeout_ci_parity_test.exs
  - .github/workflows/phase52-proof.yml
autonomous: true
requirements:
  - REL-01
must_haves:
  truths:
    - "`mix closeout.verify` reuses the shared validator and exits non-zero on blocking closeout drift."
    - "The merge-blocking v3.6 proof lane runs closeout verification without promoting advisory/provider/device checks."
    - "Maintainers get one boring local command and one merge-blocking CI path for closeout truth."
  artifacts:
    - path: "lib/mix/tasks/closeout.verify.ex"
      provides: "Thin Mix wrapper over `Crosswake.Planning.CloseoutVerifier`"
      exports: ["run/1"]
    - path: ".github/workflows/phase52-proof.yml"
      provides: "Merge-blocking workflow step that invokes `mix closeout.verify`"
      contains: "mix closeout.verify"
    - path: "test/mix/tasks/closeout_verify_test.exs"
      provides: "Command-level failure and output regression coverage"
      contains: "Mix.Tasks.Closeout.Verify"
  key_links:
    - from: "lib/mix/tasks/closeout.verify.ex"
      to: "lib/crosswake/planning/closeout_verifier.ex"
      via: "delegated verifier run/render call"
      pattern: "CloseoutVerifier\\.(run|render)"
    - from: ".github/workflows/phase52-proof.yml"
      to: "mix closeout.verify"
      via: "merge-blocking proof job step"
      pattern: "mix closeout\\.verify"
---

<objective>
Expose the shared closeout validator through an idiomatic Mix task and wire the same command into the merge-blocking proof lane.

Purpose: satisfy D-06, D-07, D-12, D-19, D-21, and D-24 by making closeout truth runnable through one local command and one CI path, without inventing a second release workflow or promoting advisory signals.
Output: `mix closeout.verify`, focused CLI/workflow regression tests, and phase-proof wiring that calls the validator in CI.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/milestones/v3.6-CLOSEOUT.md
@.planning/phases/53-release-continuity-and-closeout-hardening/53-CONTEXT.md
@.planning/phases/53-release-continuity-and-closeout-hardening/53-RESEARCH.md
@.planning/phases/53-release-continuity-and-closeout-hardening/53-PATTERNS.md
@.planning/phases/53-release-continuity-and-closeout-hardening/53-VALIDATION.md
@.planning/phases/50-doctor-publish-and-readiness-checks/50-02-SUMMARY.md
@.planning/phases/52-operator-proof-and-docs/52-02-SUMMARY.md
@lib/crosswake/planning/closeout_verifier.ex
@lib/mix/tasks/crosswake.doctor.ex
@lib/mix/tasks/crosswake.inspect.ex
@.github/workflows/phase52-proof.yml
@test/mix/tasks/crosswake_doctor_test.exs

<interfaces>
From `lib/mix/tasks/crosswake.doctor.ex` and `lib/mix/tasks/crosswake.inspect.ex`:
```elixir
def run(args)
```
Both tasks use strict option parsing, render one human/JSON surface, and call `Mix.raise(...)` for blocking failures.

From `lib/crosswake/planning/closeout_verifier.ex` (created by Plan 53-01):
```elixir
run/1
render/1
```
The Mix wrapper must delegate to these functions rather than duplicating validation logic.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Lock failing Mix-task and CI parity tests for closeout verification</name>
  <read_first>
    test/mix/tasks/closeout_verify_test.exs (create this file), test/crosswake/planning/closeout_ci_parity_test.exs (create this file), lib/crosswake/planning/closeout_verifier.ex, lib/mix/tasks/crosswake.doctor.ex, lib/mix/tasks/crosswake.inspect.ex, .github/workflows/phase52-proof.yml, .planning/phases/53-release-continuity-and-closeout-hardening/53-CONTEXT.md
  </read_first>
  <files>test/mix/tasks/closeout_verify_test.exs, test/crosswake/planning/closeout_ci_parity_test.exs</files>
  <behavior>
    - Test 1: `mix closeout.verify` prints the verifier report and raises on blocking checks instead of silently succeeding.
    - Test 2: the merge-blocking proof workflow contains a dedicated `mix closeout.verify` step, while the advisory lane remains `continue-on-error` and non-promotional.
  </behavior>
  <acceptance_criteria>
    - `test/mix/tasks/closeout_verify_test.exs` covers success and blocking-failure behavior for `Mix.Tasks.Closeout.Verify`, using the shared verifier contract from Plan 53-01.
    - `test/crosswake/planning/closeout_ci_parity_test.exs` asserts `.github/workflows/phase52-proof.yml` runs `mix closeout.verify` in the merge-blocking job and keeps the advisory lane explicitly non-blocking per D-24.
    - The failing tests reference D-06, D-07, D-12, and D-19 by asserting the thin Mix wrapper pattern rather than a shell script or parallel command path.
  </acceptance_criteria>
  <action>Add focused failing coverage before wiring the task. Mirror the existing `test/mix/tasks/crosswake_doctor_test.exs` style for command tests, but target `Mix.Tasks.Closeout.Verify` and the blocking exit behavior required by D-07 and D-10. Add a planning-parity test that reads `.github/workflows/phase52-proof.yml` and asserts the merge-blocking job invokes `mix closeout.verify` while the advisory lane keeps `continue-on-error: true` and does not add new shipped-claim language. Do not broaden Phase 52 proof scope beyond closeout invocation.</action>
  <verify>
    <automated>mix test test/mix/tasks/closeout_verify_test.exs test/crosswake/planning/closeout_ci_parity_test.exs</automated>
  </verify>
  <done>Command and workflow regressions fail until the closeout Mix task exists and the proof lane invokes it with the correct blocking posture.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement `mix closeout.verify` and merge-blocking proof-lane wiring</name>
  <read_first>
    lib/mix/tasks/closeout.verify.ex (create this file), lib/crosswake/planning/closeout_verifier.ex, test/mix/tasks/closeout_verify_test.exs, test/crosswake/planning/closeout_ci_parity_test.exs, lib/mix/tasks/crosswake.doctor.ex, .github/workflows/phase52-proof.yml, .planning/phases/53-release-continuity-and-closeout-hardening/53-RESEARCH.md
  </read_first>
  <files>lib/mix/tasks/closeout.verify.ex, .github/workflows/phase52-proof.yml</files>
  <behavior>
    - Test 1: the Mix task is a thin maintainer-DX wrapper over the shared validator and exposes the stable failure text from Plan 53-01.
    - Test 2: CI runs the same local command in a merge-blocking job, preserving the hermetic-versus-advisory split already established in Phase 52.
  </behavior>
  <acceptance_criteria>
    - `lib/mix/tasks/closeout.verify.ex` uses `Mix.Task`, has a discoverable shortdoc/moduledoc, delegates to `Crosswake.Planning.CloseoutVerifier.run/1`, renders the report, and `Mix.raise`s on blocking status per D-07, D-10, and D-19.
    - `.github/workflows/phase52-proof.yml` adds a named merge-blocking step that runs `mix closeout.verify` after dependency install and compile, without changing advisory-lane triggers or `continue-on-error: true`.
    - No second workflow or shell wrapper is introduced for release continuity; the existing v3.6 proof topology is extended in place per D-06 and D-24.
  </acceptance_criteria>
  <action>Create `Mix.Tasks.Closeout.Verify` as the thin wrapper locked by D-07 and D-19. Keep the task surface intentionally small: parse only the arguments needed to delegate to `Crosswake.Planning.CloseoutVerifier`, emit the shared report text, and raise on blocking failures so CI gets a reliable non-zero exit. Then update `.github/workflows/phase52-proof.yml` by adding a merge-blocking step named for milestone closeout verification that runs `mix closeout.verify`; keep the advisory operator-proof lane scheduled/manual and explicitly non-promotional, as established in Phase 52 and required by D-24. Do not create a new workflow file or a second command path.</action>
  <verify>
    <automated>mix test test/mix/tasks/closeout_verify_test.exs test/crosswake/planning/closeout_ci_parity_test.exs</automated>
  </verify>
  <done>Maintainers can run `mix closeout.verify` locally, CI runs the same command in the merge-blocking proof lane, and advisory proofs remain visible but non-gating.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| developer -> Mix task | Command-line execution must surface the validator result honestly and fail non-zero on blocking drift. |
| repository workflow -> merge gate | CI configuration is a source of truth for whether closeout checks actually gate merges. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-53-03 | Repudiation | `lib/mix/tasks/closeout.verify.ex` | mitigate | Wrap the shared validator directly and `Mix.raise` on blocking status so a failing closeout cannot be hidden behind exit-code drift. |
| T-53-04 | Tampering | `.github/workflows/phase52-proof.yml` | mitigate | Add a parity test that asserts `mix closeout.verify` exists in the merge-blocking job and keeps the advisory lane non-blocking and non-promotional. |
| T-53-SC | Tampering | npm/pip/cargo installs | accept | No package-manager installs occur in this plan. |
</threat_model>

<verification>
Run the command and workflow regressions after each task:
- `mix test test/mix/tasks/closeout_verify_test.exs test/crosswake/planning/closeout_ci_parity_test.exs`
</verification>

<success_criteria>
- `mix closeout.verify` exists, is discoverable through Mix, and delegates to the shared validator instead of reimplementing checks.
- The merge-blocking proof lane runs `mix closeout.verify`.
- Advisory optional-dependency proof remains advisory-only and does not imply new shipped provider/auth/notification/shell support.
</success_criteria>

<output>
Create `.planning/phases/53-release-continuity-and-closeout-hardening/53-02-SUMMARY.md` when done
</output>
