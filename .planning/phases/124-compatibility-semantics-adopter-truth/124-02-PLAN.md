---
phase: 124-compatibility-semantics-adopter-truth
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/crosswake/support_matrix/support_matrix.ex
  - lib/crosswake/support_matrix/renderer.ex
  - guides/support_matrix.md
  - test/crosswake/proof/phase52_operator_truth_test.exs
autonomous: true
requirements: [COMPAT-02]
must_haves:
  truths:
    - "There is exactly ONE canonical source mapping each version-axis change-kind to a rebuild class (no parallel taxonomy)"
    - "The rebuild class strings are the 4 canonical change-class strings VERBATIM (no minted synonyms)"
    - "native_runtime_version has NO additive-without-rebuild row — every native_runtime move maps to native-rebuild"
    - "guides/support_matrix.md gains a rendered '## Rebuild Decision Table' section right after '## Change Classes'"
    - "The phase52 byte-parity guard asserts the rendered support_matrix.md equals Renderer.render(canonical())"
  artifacts:
    - path: "lib/crosswake/support_matrix/support_matrix.ex"
      provides: "Canonical rebuild_decision_table/0 + RebuildDecisionEntry rows"
      contains: "rebuild_decision_table"
    - path: "lib/crosswake/support_matrix/renderer.ex"
      provides: "## Rebuild Decision Table section renderer"
      contains: "Rebuild Decision Table"
    - path: "guides/support_matrix.md"
      provides: "Rendered rebuild decision table section"
      contains: "## Rebuild Decision Table"
  key_links:
    - from: "lib/crosswake/support_matrix/renderer.ex"
      to: "lib/crosswake/support_matrix/support_matrix.ex"
      via: "render/1 calls rebuild_decision_table/0 and emits its rows"
      pattern: "rebuild_decision_table"
    - from: "test/crosswake/proof/phase52_operator_truth_test.exs"
      to: "guides/support_matrix.md"
      via: "byte-parity assert_file_exact against Renderer.render(canonical())"
      pattern: "support_matrix.byte_parity"
  prohibitions:
    - "MUST NOT add, rename, or mint new change-class names — the 4-class taxonomy already ships in change_class_entries/0 (D-07)"
    - "MUST NOT hand-author the rebuild table markdown directly in support_matrix.md (instantly breaks the byte-parity guard, D-08)"
    - "MUST NOT generate the rebuild table via mix crosswake.contract.gen (wrong generator boundary — that is bridge-version fixtures, D-08)"
    - "MUST NOT introduce a native_runtime additive-without-rebuild row (D-09 asymmetry)"
    - "MUST NOT use support-status column headers (Target/Version/Status) — this is a rebuild-class table, not a second support matrix"
prohibitions_are_blocking: true
---

<objective>
Create the single canonical source that maps each version-axis change-kind to its rebuild class — `Crosswake.SupportMatrix.rebuild_decision_table/0` — and render it into a new `## Rebuild Decision Table` section of `guides/support_matrix.md`, auto-covered by the existing phase52 byte-parity merge-blocking guard. This is the source-of-truth that COMPAT-03 (guide), COMPAT-04 (doctor), and COMPAT-05 (changelog) all consume; it MUST be built before/with those consumers.

Purpose: The milestone's drift-elimination thesis applied to change-class semantics. The 4-class taxonomy already ships in `change_class_entries/0`; the gap is the axis→change-kind→rebuild-class MAPPING and a rendered table. One source, generated surface, guarded by byte-parity — no parallel taxonomies, no minted synonyms.

Output: `rebuild_decision_table/0` + a `RebuildDecisionEntry` struct, a `rebuild_decision_table_section/1` renderer, the regenerated `guides/support_matrix.md`, and a phase52 assertion that the new section is present.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/124-compatibility-semantics-adopter-truth/124-CONTEXT.md
@.planning/phases/124-compatibility-semantics-adopter-truth/124-PATTERNS.md
</context>

<artifacts_this_phase_produces>
NEW symbols/sections created here (drift pass: treat as newly-minted):
- `Crosswake.SupportMatrix.rebuild_decision_table/0` (public function) in support_matrix.ex
- `RebuildDecisionEntry` struct (or a Types constructor like `new_rebuild_decision_entry/1`) with fields `axis`, `change_kind`, `rebuild_class`, `adopter_action`, `denial_signal`, `guide_anchor` (exact field names = Claude's discretion provided phase52 byte-parity holds)
- `rebuild_decision_table_section/1` (+ a `rebuild_decision_table_row/1` helper) in renderer.ex
- The `## Rebuild Decision Table` section in guides/support_matrix.md (rendered output)
The 4 change-class STRINGS are pre-existing (from change_class_entries/0) and reused verbatim — NOT new.
</artifacts_this_phase_produces>

<context>
@lib/crosswake/support_matrix/support_matrix.ex
@lib/crosswake/support_matrix/renderer.ex
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the canonical rebuild_decision_table/0 + RebuildDecisionEntry to SupportMatrix</name>
  <files>lib/crosswake/support_matrix/support_matrix.ex</files>
  <read_first>
    - lib/crosswake/support_matrix/support_matrix.ex — `change_class_entries/0` (lines 771-812): the analog shape (list of structs built via a Types constructor) AND the source of the 4 canonical change-class strings used VERBATIM as rebuild_class values. Also read `canonical/0` and the `SupportMatrix.t()` struct to decide whether the renderer reads the table via the struct or calls `rebuild_decision_table/0` directly (PATTERNS.md Observation 3).
    - .planning/phases/124-compatibility-semantics-adopter-truth/124-PATTERNS.md — "rebuild_decision_table/0 + RebuildDecisionEntry" section, including the full D-09 axis-mapping rows table and the 4 verbatim strings.
    - .planning/phases/124-compatibility-semantics-adopter-truth/124-CONTEXT.md — D-08 (field list), D-09 (the load-bearing mapping; the asymmetry note), D-18 (4 verbatim strings).
  </read_first>
  <action>
    Add a public `rebuild_decision_table/0` near `change_class_entries/0`, returning a list of `RebuildDecisionEntry` structs (define the struct, or add a `Types.new_rebuild_decision_entry/1` constructor mirroring `new_change_class_entry`). Fields per D-08: `axis`, `change_kind` (`:additive` | `:breaking`), `rebuild_class` (one of the 4 verbatim strings), `adopter_action`, `denial_signal` (the concrete fail-closed denial code: `compatibility_mismatch`, `inactive_route`, `undeclared_capability`, `pack_incompatible`, or "n/a"), `guide_anchor`. Encode EXACTLY the D-09 rows: manifest_schema additive→`compatibility-bump only`, breaking→`native or companion rebuild required`; bridge_protocol additive→`compatibility-bump only`, breaking→`native or companion rebuild required`; native_runtime additive→`native or companion rebuild required` (NO additive-without-rebuild row), breaking→`native or companion rebuild required`; capability core-owned additive→`compatibility-bump only`, capability native/companion additive→`native or companion rebuild required`, capability breaking→`native or companion rebuild required`; docs/wording→`docs-only`; core Elixir behavior→`core-only/no native rebuild`. Pull rebuild_class strings by referencing the same source as `change_class_entries/0` (verbatim, no minting). State the manifest_schema/bridge_protocol asymmetry-vs-native_runtime rationale in the row data (adopter_action or a doc note) so it survives into the rendered doc per D-09. Wire `rebuild_decision_table/0` into `canonical/0`/the struct if the renderer consumes it via the struct, otherwise leave it callable directly.
  </action>
  <acceptance_criteria>
    - `Crosswake.SupportMatrix.rebuild_decision_table()` returns a non-empty list of structs with the six D-08 fields.
    - Every `rebuild_class` value is one of the 4 verbatim strings: `docs-only`, `core-only/no native rebuild`, `compatibility-bump only`, `native or companion rebuild required` — assert via `iex`/test: `Enum.all?(rebuild_decision_table(), & &1.rebuild_class in ["docs-only","core-only/no native rebuild","compatibility-bump only","native or companion rebuild required"])`.
    - There is exactly ONE native_runtime_version row with `change_kind: :additive`, and its `rebuild_class` is `native or companion rebuild required` (asymmetry locked) — no native_runtime row maps to compat-bump-only or no-rebuild.
    - manifest_schema_version `:additive` and bridge_protocol_version `:additive` rows map to `compatibility-bump only`.
    - `denial_signal` values are drawn only from the Denial vocabulary (`compatibility_mismatch`/`inactive_route`/`undeclared_capability`/`pack_incompatible`) or `n/a`.
    - `mix compile --warnings-as-errors` passes (assuming repo baseline is warning-clean for this module).
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/jon/projects/crosswake && mix run -e 'rows = Crosswake.SupportMatrix.rebuild_decision_table(); allowed = ["docs-only","core-only/no native rebuild","compatibility-bump only","native or companion rebuild required"]; true = Enum.all?(rows, fn r -> r.rebuild_class in allowed end); IO.puts(length(rows))'</automated>
  </verify>
  <done>rebuild_decision_table/0 is the single canonical axis→change-kind→rebuild-class source; all rebuild_class values are the 4 verbatim strings; the native_runtime additive→native-rebuild asymmetry is encoded; compiles clean.</done>
</task>

<task type="auto">
  <name>Task 2: Render the ## Rebuild Decision Table section and lock it with phase52</name>
  <files>lib/crosswake/support_matrix/renderer.ex, guides/support_matrix.md, test/crosswake/proof/phase52_operator_truth_test.exs</files>
  <read_first>
    - lib/crosswake/support_matrix/renderer.ex — `render/1` (~line 57) and `change_class_section/1` (~lines 155-163): the analog section builder and where in the render list to insert the new section.
    - test/crosswake/proof/phase52_operator_truth_test.exs lines 151-159 — the byte-parity `assert_file_exact` guard that auto-covers the rendered output; the place to add a section-presence assertion.
    - .planning/phases/124-compatibility-semantics-adopter-truth/124-PATTERNS.md — "renderer.ex — new rebuild_decision_table_section/1" section (table header columns + insertion point).
    - .planning/phases/124-compatibility-semantics-adopter-truth/124-CONTEXT.md — D-08 (section placed right after `## Change Classes`), D-11 (extend phase52 to assert the new section present).
  </read_first>
  <action>
    Add `rebuild_decision_table_section/1` to renderer.ex mirroring `change_class_section/1`: emit a `## Rebuild Decision Table` heading then a markdown table with columns `Axis | Change Kind | Rebuild Class | Adopter Action | Denial Signal | Guide Anchor`, one row per `rebuild_decision_table/0` entry (add a `rebuild_decision_table_row/1` helper). Insert the section call in `render/1` IMMEDIATELY AFTER the `## Change Classes` section and before the next section, so it lands right after Change Classes in the rendered doc. Then regenerate `guides/support_matrix.md` by running the renderer (use the project's existing regenerate path — e.g. `mix run -e 'File.write!("guides/support_matrix.md", Crosswake.SupportMatrix.Renderer.render(Crosswake.SupportMatrix.canonical()))'` or the documented regen task) so the file byte-matches `Renderer.render(canonical())`. Do NOT hand-edit the markdown table. Finally, extend phase52 with an assertion that the rendered output contains the literal heading `## Rebuild Decision Table` (in addition to the existing byte-parity assert which already covers drift).
  </action>
  <acceptance_criteria>
    - `guides/support_matrix.md` contains `## Rebuild Decision Table` positioned after `## Change Classes`: verify `grep -n '## Change Classes\|## Rebuild Decision Table' guides/support_matrix.md` shows Change Classes line number < Rebuild Decision Table line number.
    - The rendered section's table header is `| Axis | Change Kind | Rebuild Class | Adopter Action | Denial Signal | Guide Anchor |`.
    - Byte-parity holds: `mix test test/crosswake/proof/phase52_operator_truth_test.exs` passes (the merge-blocking `support_matrix.byte_parity` assert).
    - The new phase52 assertion verifies `Renderer.render(canonical())` includes `## Rebuild Decision Table`.
    - All 4 verbatim rebuild-class strings appear in `guides/support_matrix.md`.
    - Regen is idempotent: running the regen command twice leaves `guides/support_matrix.md` unchanged (`git diff --exit-code guides/support_matrix.md`).
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/jon/projects/crosswake && mix test test/crosswake/proof/phase52_operator_truth_test.exs 2>&1 | tail -5</automated>
  </verify>
  <done>guides/support_matrix.md carries a rendered `## Rebuild Decision Table` right after `## Change Classes`, byte-parity guard passes, and phase52 explicitly asserts the section's presence.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| canonical source → generated doc | The rendered `guides/support_matrix.md` is adopter-facing truth; drift between canonical table and committed doc would publish a false rebuild-cost claim |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-124-04 | Tampering / Repudiation | guides/support_matrix.md (generated doc) | mitigate | The existing phase52 byte-parity merge-blocking guard (`support_matrix.byte_parity`) auto-covers the new section; a hand-edit or drift fails CI. No new guard needed (D-08). |
| T-124-05 | Information Disclosure (false claim) | rebuild_class vocabulary | mitigate | rebuild_class values are pulled verbatim from the single change_class taxonomy (D-18); no minted synonyms means no second vocabulary to desync. Acceptance criterion asserts every value is in the locked 4-string set. |
| T-124-SC | Tampering | dependency surface | accept | No package-manager installs in this plan (pure Elixir + markdown regen). No legitimacy checkpoint required. |
</threat_model>

<verification>
- `Crosswake.SupportMatrix.rebuild_decision_table()` returns the locked D-09 rows with only the 4 verbatim rebuild-class strings.
- `mix test test/crosswake/proof/phase52_operator_truth_test.exs` passes (byte-parity + new section-presence assert).
- `guides/support_matrix.md` shows `## Rebuild Decision Table` after `## Change Classes`, regen idempotent.
</verification>

<success_criteria>
COMPAT-02 satisfied: each version axis is mapped to a rebuild class with documented additive-vs-breaking rules in ONE committed source (`rebuild_decision_table/0`), rendered into `guides/support_matrix.md`, and guarded by a docs-contract (byte-parity) test asserting the table is present. The native_runtime asymmetry is explicit. No new or renamed change classes.
</success_criteria>

<output>
Create `.planning/phases/124-compatibility-semantics-adopter-truth/124-02-SUMMARY.md` when done. Record the chosen RebuildDecisionEntry field names and how the renderer consumes the table (struct field vs direct call).
</output>