---
phase: 161-ios-pronunciation-pack-seam
plan: "06"
type: execute
wave: 5
depends_on: ["161-05"]
files_modified:
  - packages/crosswake-shell-core-ios/Sources/CrosswakeShellCore/PackStore.swift
  - packages/crosswake-shell-core-ios/Tests/CrosswakeShellCoreTests/PackStoreTests.swift
  - examples/ios_shell_host/Fixtures/declared_pack_requirements.json
  - examples/ios_shell_host/CrosswakeShell/CrosswakeShellApp.swift
  - examples/ios_shell_host/CrosswakeShell/PronunciationPackProvider.swift
  - examples/ios_shell_host/CrosswakeShell/RequiredPackView.swift
  - examples/ios_shell_host/CrosswakeShellTests/PronunciationPackProviderTests.swift
  - examples/ios_shell_host/CrosswakeShellTests/RequiredPackViewTests.swift
autonomous: true
gap_closure: true
requirements: [PACK-01, PACK-02, PACK-03, PACK-04, PACK-05]
must_haves:
  truths:
    - "The reference host's real bundled construction path supplies a positive exact byte count and pinned SHA-256 to PackStore and reaches available only after the existing provider verifies and promotes those exact bytes (D-02, D-03, D-04, D-07; PACK-01, PACK-02, PACK-03)."
    - "The example host acquires bytes but does not derive its expected integrity declaration from those acquired bytes at install time; exact authority stays in host-private bundled configuration (D-02, D-04; PACK-04)."
    - "The concrete host provider incrementally hashes the staged file in bounded reads off MainActor, compares the accumulated staged-file byte count and digest exactly before atomic promotion, and emits only privacy-safe closed results (D-02, D-03, D-04; PACK-03)."
    - "Absent pronunciation media renders an explicit route-blocking not_installed state with an Install action; the recovery surface is never blank (D-13, D-14)."
    - "Checking, installing, and invalidating render explicit foreground progress and expose no action while work is in flight (D-13, D-14)."
    - "Closed failures expose only Retry or Invalidate according to the approved recovery mapping and privacy-safe copy (D-11 through D-15)."
    - "Reconciled available renders the ready state while route activation, not the view, consumes availability (D-07, D-14)."
    - "The recovery surface scrolls rather than clipping or truncating growing content (D-14)."
    - "The combined lifecycle status wraps inside the scrollable recovery surface (D-14)."
    - "Intentional action absence is always explained by explicit lifecycle text (D-13, D-14)."
    - "No button is shown during a foreground pack operation (D-13, D-14)."
    - "Each error state exposes exactly its permitted recovery action (D-13, D-14)."
    - "Ready state exposes no install or update action (D-13, D-14)."
    - "Foreground controls remain reachable in the scrollable content column (D-14)."
    - "Safe developer context wraps and scrolls without expanding into private details (D-12, D-14)."
    - "statement: A held-out large-Dynamic-Type UI-state test verifies that recovery copy wraps readably and every required control remains reachable. | verification: backstop"
    - "statement: A held-out large-Dynamic-Type UI-state test verifies that the combined accessible lifecycle status remains readable and announces the full state without displacing VoiceOver focus. | verification: backstop"
    - "statement: A held-out large-Dynamic-Type UI-state test verifies that action labels wrap or reflow without truncation and preserve a reachable minimum 44pt target. | verification: backstop"
    - "statement: A held-out large-Dynamic-Type UI-state test verifies that safe owner, rule, route, runtime, and remediation text wraps without leaking excluded diagnostic data. | verification: backstop"
  artifacts:
    - "path: examples/ios_shell_host/Fixtures/declared_pack_requirements.json | provides: Host-private exact requirement declaration for the bundled pronunciation fixture."
    - "path: packages/crosswake-shell-core-ios/Sources/CrosswakeShellCore/PackStore.swift | provides: Validated decoding of exact bundled PackRequirement metadata."
    - "path: examples/ios_shell_host/CrosswakeShell/PronunciationPackProvider.swift | provides: Chunked staged-file SHA-256 and exact byte-count verification off MainActor before atomic promotion."
    - "path: examples/ios_shell_host/CrosswakeShellTests/PronunciationPackProviderTests.swift | provides: Deterministic XCTest through PackStore.bundled and the concrete host provider, including proof that staged-file streaming is used."
    - "path: examples/ios_shell_host/CrosswakeShell/RequiredPackView.swift | provides: Approved 8pt diagnostic and 24pt top-level spacing with unchanged semantic recovery behavior."
  key_links:
    - "from: declared_pack_requirements.json | to: PackStore.bundled | via: validated exact requirement decoding | pattern: expected_byte_count|expected_sha256|PackRequirement"
    - "from: PackStore.bundled | to: PronunciationPackProvider.install | via: the same exact PackRequirement passed by the production composition path and checked against the streamed staged file before promotion | pattern: bundled|packProvider|install"
    - "from: RequiredPackView.Presentation | to: SwiftUI recovery controls | via: closed state/action mapping and stable accessibility identifiers | pattern: required-pack-status|required-pack-primary-action|required-pack-invalidate-action"
  prohibitions:
    - "requirement_id: PACK-02 | category: safety | statement: MUST NOT let requested, staged, acknowledged, old-version, malformed, revoked, or unreconciled media silently activate a route or downgrade it to a different runtime behavior. | status: unresolved | verification: flagged-unverified"
    - "requirement_id: PACK-04 | category: values | statement: MUST NOT turn the provider seam into Crosswake-owned distribution, generic storage, asset lookup, playback policy, or host media-layout authority. | status: unresolved | verification: flagged-unverified"
    - "requirement_id: PACK-05 | category: transparency | statement: MUST NOT present local fixture, simulator, package, or generated native results as physical-iPhone, adopter-instance, Android, background-transfer, or generic-storage proof. | status: unresolved | verification: flagged-unverified"
flagged_assumptions:
  - "[UNRESOLVED-SPEC-PROBE][PACK-01 unclassified] The provider seam remains exactly status/install/invalidate; no transport or storage operation is inferred."
  - "[UNRESOLVED-SPEC-PROBE][PACK-02 unclassified] Every unrecognized availability input stays non-available under the closed D-11 reasons."
  - "[UNRESOLVED-SPEC-PROBE][PACK-03 unclassified] Exact size, pinned SHA-256, atomic promotion, persistence ordering, and fresh status are all mandatory."
  - "[UNRESOLVED-SPEC-PROBE][PACK-04 concurrency] Interrupted or overlapping work is governed by per-pack serialization and current-operation authority."
  - "[UNRESOLVED-SPEC-PROBE][PACK-05 unclassified] The iOS foreground-only stop list is exhaustive and no adjacent platform or product behavior is inferred."
---

<objective>
Repair the smallest real reference-host path from a bundled exact declaration through the concrete provider to reconciled route availability, then align the already-approved recovery view spacing without changing its ownership or behavior.

Purpose: Close the production construction disconnect first so the example host proves the same exact requirement contract as the package tracer, while carrying every approved UI consideration into executable phase truth.
Output: Validated bundled exact-requirement decoding, one concrete-host XCTest on that path, and the 8pt/24pt spacing correction.
</objective>

## Artifacts this phase produces

- New private `BundledPackRequirementDeclaration` decoding symbol in `PackStore.swift`.
- New XCTest `testBundledConstructionPropagatesExactRequirementThroughConcreteProvider` in `PronunciationPackProviderTests.swift`.
- Changed host-private `declared_pack_requirements.json` record shape with contract marker, pack ID, required version, positive exact byte count, and pinned SHA-256; the private values are not copied into planning or retained evidence.
- No new public Swift symbol, command, schema, external API, evidence field, support label, Android path, or product surface.

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/ADR-FIRST-B2C-ADOPTER.md
@.planning/phases/161-ios-pronunciation-pack-seam/161-CONTEXT.md
@.planning/phases/161-ios-pronunciation-pack-seam/161-VERIFICATION.md
@.planning/phases/161-ios-pronunciation-pack-seam/161-UI-SPEC.md
@.planning/phases/161-ios-pronunciation-pack-seam/161-02-SUMMARY.md
@.planning/phases/161-ios-pronunciation-pack-seam/161-03-SUMMARY.md
@packages/crosswake-shell-core-ios/Sources/CrosswakeShellCore/PackProvider.swift
@packages/crosswake-shell-core-ios/Sources/CrosswakeShellCore/PackStore.swift
@examples/ios_shell_host/CrosswakeShell/PronunciationPackProvider.swift

<interfaces>
- `PackRequirement` is Codable and carries `contractVersion`, `packID`, `requiredVersion`, `expectedByteCount`, and `expectedSHA256`.
- `PackStore.bundled(bundle:provider:)` currently decodes `[String:String]` and loses the integrity fields; this is the production constructor under repair.
- `RootSceneWrapper` injects `HostPronunciationPackConfiguration.provider()` through `CrosswakeShellConfig`; `ActivationCoordinator.bundled` supplies that provider to `PackStore.bundled`.
- `PronunciationPackProvider.install` already performs byte-count/digest checks, atomic destination promotion, and post-commit inventory persistence.
</interfaces>
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Carry the exact bundled requirement through the real reference-host constructor</name>
  <reversibility rating="costly">The bundled requirement wire shape is host-consumed configuration; changing it again requires coordinated fixture and host migration even though it stays private to native hosts.</reversibility>
  <read_first>
    - AGENTS.md
    - .planning/ADR-FIRST-B2C-ADOPTER.md
    - .planning/phases/161-ios-pronunciation-pack-seam/161-CONTEXT.md
    - .planning/phases/161-ios-pronunciation-pack-seam/161-VERIFICATION.md
    - packages/crosswake-shell-core-ios/Sources/CrosswakeShellCore/PackProvider.swift
    - packages/crosswake-shell-core-ios/Sources/CrosswakeShellCore/PackStore.swift
    - packages/crosswake-shell-core-ios/Sources/CrosswakeShellCore/ActivationCoordinator.swift
    - examples/ios_shell_host/Fixtures/declared_pack_requirements.json
    - examples/ios_shell_host/CrosswakeShell/CrosswakeShellApp.swift
    - examples/ios_shell_host/CrosswakeShell/PronunciationPackProvider.swift
    - examples/ios_shell_host/CrosswakeShellTests/PronunciationPackProviderTests.swift
  </read_first>
  <files>packages/crosswake-shell-core-ios/Sources/CrosswakeShellCore/PackStore.swift, packages/crosswake-shell-core-ios/Tests/CrosswakeShellCoreTests/PackStoreTests.swift, examples/ios_shell_host/Fixtures/declared_pack_requirements.json, examples/ios_shell_host/CrosswakeShell/CrosswakeShellApp.swift, examples/ios_shell_host/CrosswakeShell/PronunciationPackProvider.swift, examples/ios_shell_host/CrosswakeShellTests/PronunciationPackProviderTests.swift</files>
  <behavior>
    - The example bundle decodes one current-contract requirement with nonempty opaque ID/version, positive exact byte count, and a syntactically valid pinned SHA-256.
    - The actual `PackStore.bundled(bundle:provider:)` path passes those exact fields to `PronunciationPackProvider`, then requires fresh provider status before becoming available.
    - Zero, negative, missing, malformed, unsupported-contract, or duplicate requirement declarations fail closed and never manufacture a zero/empty `PackRequirement`.
    - The provider's source closure acquires fixture bytes only; expected integrity is not calculated from the same acquired bytes as a substitute for pinned configuration (D-02, D-04).
    - Provider verification reads the staged file in bounded chunks off MainActor, compares the accumulated byte count and SHA-256 exactly to the pinned requirement, and performs no destination promotion or inventory write before both checks pass (D-02, D-03, D-04).
  </behavior>
  <action>Write the failing example-host XCTest cases first and make the end-to-end test call `PackStore.bundled(bundle: Bundle.main, provider: PronunciationPackProvider(...))`, not a directly constructed store or package fake. Define private `BundledPackRequirementDeclaration` decoding in `PackStore.swift`, decode the host-private exact declaration, validate the current contract marker, nonempty bounded ID/version, positive count, and canonical SHA-256 syntax, and construct the exact `PackRequirement` values. Replace the example declaration with the exact pinned metadata for the committed fixture without copying those values to diagnostics, output, or planning evidence. Remove `HostPronunciationPackConfiguration.privateRequirement()` and its preverification closure so the provider receives authority only through the bundled requirement. In `PronunciationPackProvider.swift`, replace the in-memory `SHA256.hash(data: bytes)` verification with a staged-file verifier that opens the staged URL, repeatedly reads a bounded chunk, incrementally updates `SHA256`, accumulates the actual staged-file byte count, and runs outside MainActor before returning only the count and digest needed for exact comparison. Compare both results to the requirement before `replaceItemAt`/`moveItem` and before inventory persistence; mismatches retain the existing closed failure reasons and deferred staging cleanup, while successful verification preserves atomic promotion followed by inventory commit. Give the focused XCTest target a deterministic seam: `testStreamedVerifierUsesMultipleBoundedReads` drives a deliberately small test chunk size, while `testInstallStreamsStagedFileOffMainActorBeforePromotion` proves `install` invokes verification on the staged file URL off MainActor rather than hashing the source `Data`; also assert count/digest mismatches never promote, known-good replacement remains intact, relaunch status remains exact, and logs/output contain no bytes, digest, path, URL, or raw error. Add focused package decoding rejection tests. Keep legacy generated declarations fail-closed; do not treat bundled inventory or an absent declaration as availability. This implements D-02 through D-04 without widening provider ownership.</action>
  <acceptance_criteria>
    - `testBundledConstructionPropagatesExactRequirementThroughConcreteProvider` exercises the production bundle constructor and concrete provider, observes checking/non-available first, installs, reconciles fresh status, and observes available.
    - The provider receives the same positive exact byte count and pinned SHA-256 decoded from the bundle; neither value is replaced with zero/empty or derived from provider output.
    - Deterministic provider tests prove the staged file is SHA-256 hashed in more than one bounded read off MainActor and that exact staged count/digest comparison completes before atomic destination promotion and inventory persistence.
    - Missing, malformed, unsupported, duplicate, or non-positive declarations produce only blocked state in deterministic tests.
    - Test and application output contain no fixture bytes, digest values, paths, URLs, or raw errors.
  </acceptance_criteria>
  <verify><automated>swift test --package-path packages/crosswake-shell-core-ios --filter PackStoreTests &amp;&amp; xcodebuild -project examples/ios_shell_host/CrosswakeShell.xcodeproj -scheme CrosswakeShell -destination 'platform=iOS Simulator,name=iPhone 17' -only-testing:CrosswakeShellTests/PronunciationPackProviderTests test CODE_SIGNING_ALLOWED=NO ONLY_ACTIVE_ARCH=YES</automated></verify>
  <done>The real bundled reference-host construction path carries exact integrity authority into the existing concrete provider and can reach available only through verified promotion plus fresh status.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Align the approved recovery surface spacing and preserve every UI state contract</name>
  <read_first>
    - .planning/phases/161-ios-pronunciation-pack-seam/161-UI-SPEC.md
    - .planning/phases/161-ios-pronunciation-pack-seam/161-CONTEXT.md
    - examples/ios_shell_host/CrosswakeShell/RequiredPackView.swift
    - examples/ios_shell_host/CrosswakeShellTests/RequiredPackViewTests.swift
  </read_first>
  <files>examples/ios_shell_host/CrosswakeShell/RequiredPackView.swift, examples/ios_shell_host/CrosswakeShellTests/RequiredPackViewTests.swift</files>
  <behavior>
    - Top-level recovery groups use 24pt spacing and developer diagnostic rows use 8pt spacing.
    - Existing stable accessibility IDs, state/action mapping, 44pt targets, Dynamic Type wrapping, scrolling, text-plus-color semantics, and privacy-safe copy remain unchanged (D-12 through D-15).
    - The four UI-SPEC held-out backstops remain explicitly represented in this plan's structured must-haves and are not claimed as mechanically closed by the spacing edit.
  </behavior>
  <action>Change the two known spacing-debt constants in `RequiredPackView` from 20 to 24 and from 6 to 8. Add a deterministic source or view-inspection XCTest that pins those two layout values while rerunning the full presentation contract. Preserve the approved SwiftUI system-only surface, one-action semantics, stable identifiers, scroll behavior, 44pt minimum targets, Dynamic Type, VoiceOver announcement behavior, and safe host-owned copy per D-13 through D-15. Do not add a design system, modal, automatic reinstall, new surface, or new product copy.</action>
  <acceptance_criteria>
    - The recovery surface uses exactly 24pt top-level spacing and 8pt diagnostic spacing.
    - All existing lifecycle/action/accessibility/privacy tests remain green.
    - No UI consideration marked covered regresses, and all four held-out backstops remain structured in `must_haves.truths` with `verification: backstop`.
  </acceptance_criteria>
  <verify><automated>xcodebuild -project examples/ios_shell_host/CrosswakeShell.xcodeproj -scheme CrosswakeShell -destination 'platform=iOS Simulator,name=iPhone 17' -only-testing:CrosswakeShellTests/RequiredPackViewTests test CODE_SIGNING_ALLOWED=NO ONLY_ACTIVE_ARCH=YES</automated></verify>
  <done>The reference recovery view conforms to the approved 8pt/24pt rhythm while preserving its complete fail-closed, accessible, privacy-safe state contract.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| host-private bundle -> PackStore | Exact integrity authority crosses decoding and validation before core constructs requirements. |
| PackStore -> concrete host provider | The production constructor must preserve every exact field without substitution. |
| lifecycle status -> recovery view | Only closed semantic state/action data may reach learner and developer UI. |

## ASVS L1 / STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-161-25 | Tampering | bundled requirement metadata | high | mitigate | Task 1 validates current contract, positive count, canonical SHA-256, uniqueness, and exact propagation through the concrete provider test; failure blocks task completion. |
| T-161-26 | Spoofing | host source closure | high | mitigate | Task 1 removes source-derived expected integrity and proves authority originates only from pinned host configuration. |
| T-161-27 | Information Disclosure | tests/UI/output | high | mitigate | Both tasks retain closed state/rule output only and prohibit private integrity values, paths, URLs, media, and raw errors. |
| T-161-28 | Denial of Service | Dynamic Type recovery view | medium | mitigate | Task 2 preserves scroll/wrap/44pt behavior and pins approved spacing with XCTest. |
| T-161-29 | Tampering | package supply chain | low | accept | No dependency install or version change occurs; existing Swift/Apple tooling is used. |
</threat_model>

<verification>
Run the focused package decoder suite, the concrete example-host bundled-construction XCTest, and the complete RequiredPackView XCTest target. Treat any high-severity threat failure as blocking.
</verification>

<success_criteria>
- The real host path no longer manufactures zero/empty integrity declarations.
- The exact host-private declaration reaches verified atomic provider installation and fresh reconciliation.
- The approved UI spacing debt is closed without changing semantic ownership, actions, copy, or accessibility.
- All structured UI backstops and all five unresolved spec-less assumptions remain visible and non-promoting.
</success_criteria>

<output>
Create `.planning/phases/161-ios-pronunciation-pack-seam/161-06-SUMMARY.md` when done.
</output>
