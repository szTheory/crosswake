---
phase: 161-ios-pronunciation-pack-seam
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/crosswake-shell-core-ios/Sources/CrosswakeShellCore/PackProvider.swift
  - packages/crosswake-shell-core-ios/Sources/CrosswakeShellCore/PackStore.swift
  - packages/crosswake-shell-core-ios/Sources/CrosswakeShellCore/CrosswakeShellConfig.swift
  - packages/crosswake-shell-core-ios/Sources/CrosswakeShellCore/ActivationCoordinator.swift
  - packages/crosswake-shell-core-ios/Tests/CrosswakeShellCoreTests/PackStoreTests.swift
  - packages/crosswake-shell-core-ios/Tests/CrosswakeShellCoreTests/PackProviderFixtureTests.swift
  - packages/crosswake-shell-core-ios/Tests/CrosswakeShellCoreTests/Resources/pronunciation-pack-fixture.bin
autonomous: true
requirements: [PACK-01, PACK-02, PACK-03, PACK-04]
must_haves:
  truths:
    - "A host can implement one versioned async PackProvider for exact requirement-bound status, install, and invalidate operations; nil, malformed, cancelled, and failed results remain blocked (D-01, D-02, D-03; PACK-01, PACK-02)."
    - "One immutable local fixture reaches route activation only after exact byte-count and SHA-256 verification, atomic-promotion attestation, and a second fresh status reconciliation (D-04, D-05, D-07; PACK-03)."
    - "Every cold start begins at checking and activation consumes only current reconciled inventory; timestamps, staged bytes, or an install acknowledgement never grant authority (D-06, D-07, D-10)."
    - "Crosswake core owns the requirement, lifecycle, reconciled inventory, and activation denial while the provider API exposes no URL, credential, filesystem path, archive layout, codec, retention, download UI, playback, or raw Error (D-02, D-03, D-12; PACK-04)."
  artifacts:
    - path: "packages/crosswake-shell-core-ios/Sources/CrosswakeShellCore/PackProvider.swift"
      provides: "Versioned PackRequirement, PackInstalledRecord, closed PackProviderResult/PackFailureReason, and async PackProvider contract."
    - path: "packages/crosswake-shell-core-ios/Sources/CrosswakeShellCore/PackStore.swift"
      provides: "Checking-first lifecycle, strict provider-result binding, fresh reconciliation, and activation-facing inventory."
    - path: "packages/crosswake-shell-core-ios/Tests/CrosswakeShellCoreTests/PackProviderFixtureTests.swift"
      provides: "Production-shaped real-byte tracer from immutable fixture through provider attestation, reconciliation, and route activation."
    - path: "packages/crosswake-shell-core-ios/Tests/CrosswakeShellCoreTests/Resources/pronunciation-pack-fixture.bin"
      provides: "Non-sensitive immutable pronunciation-pack fixture bytes used for deterministic integrity proof."
  key_links:
    - from: "PackRequirement"
      to: "PackProvider.status(for:)"
      via: "exact opaque ID, version, byte count, and SHA-256 declaration"
      pattern: "PackRequirement|status\(for:"
    - from: "PackProvider.install(_:)"
      to: "PackStore.reconcile(_:)"
      via: "install result validation followed by mandatory fresh status"
      pattern: "install|required|reconcile|status"
    - from: "PackStore.statuses"
      to: "ActivationCoordinator.resolve"
      via: "existing blockingStatus gate where only available unblocks"
      pattern: "blockingStatus|requiredPack"
  prohibitions:
    - requirement_id: PACK-02
      category: safety
      statement: "MUST NOT let requested, staged, acknowledged, old-version, malformed, revoked, or unreconciled media silently activate a route or downgrade it to a different runtime behavior."
      status: unresolved
      verification: flagged-unverified
    - requirement_id: PACK-04
      category: values
      statement: "MUST NOT turn the provider seam into Crosswake-owned distribution, generic storage, asset lookup, playback policy, or host media-layout authority."
      status: unresolved
      verification: flagged-unverified
    - requirement_id: PACK-05
      category: transparency
      statement: "MUST NOT present local fixture, simulator, package, or generated native results as physical-iPhone, adopter-instance, Android, background-transfer, or generic-storage proof."
      status: unresolved
      verification: flagged-unverified
flagged_assumptions:
  - "[UNRESOLVED-SPEC-PROBE][PACK-01 unclassified] CONTEXT D-01 through D-03 and REQUIREMENTS.md are the authoritative provider-seam contract; no additional provider operation or transport field is inferred."
  - "[UNRESOLVED-SPEC-PROBE][PACK-02 unclassified] CONTEXT D-03 and D-06 through D-13 define every availability denial; unknown provider behavior maps to the closed provider_failed path."
  - "[UNRESOLVED-SPEC-PROBE][PACK-03 unclassified] Exact size/SHA-256, same-volume promotion, persistence ordering, and fresh status are all required; the research assumptions about the exact Foundation primitive remain implementation choices behind tests."
  - "[UNRESOLVED-SPEC-PROBE][PACK-04 concurrency] Per-pack mutation serialization, idempotency, side-effect-free status, and current-inventory route reads are mandatory under interruption or parallel calls (D-05)."
  - "[UNRESOLVED-SPEC-PROBE][PACK-05 unclassified] The explicit iOS-only foreground scope and stop list are exhaustive; no adjacent platform, storage, distribution, capture, scoring, or support-taxonomy behavior is inferred."
---

<objective>
Trace one exact pronunciation-pack requirement through a host-supplied provider, real immutable bytes, fresh reconciliation, and the existing route activation gate.

Purpose: Prove the hardest architectural constraint first: no operation acknowledgement can become availability without exact requirement binding and a fresh provider status.
Output: The costly versioned provider contract, checking-first reconciled store, host injection seam, immutable fixture, and end-to-end XCTest tracer.
</objective>

<artifacts_this_phase_produces>
- New public `PackProviderContract.version`, `PackRequirement`, `PackInstalledRecord`, `PackProviderResult`, `PackFailureReason`, and `PackProvider` symbols in `PackProvider.swift`.
- Changed `RequiredPackStatus.failureReason` from an open string to the closed reason type while retaining the seven D-11 lifecycle states.
- New `PackStore` provider initializer, `reconcileAll()`, and requirement-bound internal reconciliation path; `blockingStatus(for:)` remains the activation-facing query.
- New optional `packProvider` field and initializer argument on `CrosswakeShellConfig`; no URL, path, credential, archive, codec, or playback field is added.
- Changed `ActivationCoordinator.bundled` and bootstrap flow to construct checking-first requirements and begin reconciliation without bypassing the existing pack gate.
- New `PackStoreTests.swift`, `PackProviderFixtureTests.swift`, and immutable `Resources/pronunciation-pack-fixture.bin`.
- No new command, CLI flag, dependency, Android path, backend API, schema, evidence field, or support label.
</artifacts_this_phase_produces>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/ADR-FIRST-B2C-ADOPTER.md
@.planning/FIRST-B2C-ADOPTER-ADOPTION-BRIEF.md
@.planning/FIRST-B2C-ADOPTER-ROUTE-POLICY-MAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/161-ios-pronunciation-pack-seam/161-CONTEXT.md
@.planning/phases/161-ios-pronunciation-pack-seam/161-RESEARCH.md
@.planning/phases/161-ios-pronunciation-pack-seam/161-PATTERNS.md
@packages/crosswake-shell-core-ios/Sources/CrosswakeShellCore/PackStore.swift
@packages/crosswake-shell-core-ios/Sources/CrosswakeShellCore/ActivationCoordinator.swift
@packages/crosswake-shell-core-ios/Sources/CrosswakeShellCore/CrosswakeShellConfig.swift

<interfaces>
- `PackStore` is `@MainActor`, publishes private-set `[String: RequiredPackStatus]`, and exposes `blockingStatus(for:)`; preserve this activation-facing shape while replacing bundled inventory/timed success authority.
- `ActivationCoordinator.resolve(request:manifest:)` calls `packStore.blockingStatus(for: route.packs)` before runtime resolution and returns `.requiredPack` for every non-available state.
- `CrosswakeShellConfig` carries optional host delegates through its initializer; the new provider follows this explicit optional-injection pattern, while a nil provider is a blocked state.
- The package test target already copies `Tests/CrosswakeShellCoreTests/Resources/` through `Package.swift`.
</interfaces>
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Trace one verified fixture from exact requirement through fresh status to route activation</name>
  <reversibility rating="costly">D-01 fixes an adopter-facing versioned provider callback/result contract whose later change requires coordinated host compatibility work.</reversibility>
  <read_first>
    - AGENTS.md
    - .planning/ADR-FIRST-B2C-ADOPTER.md
    - .planning/phases/161-ios-pronunciation-pack-seam/161-CONTEXT.md
    - .planning/phases/161-ios-pronunciation-pack-seam/161-RESEARCH.md
    - .planning/phases/161-ios-pronunciation-pack-seam/161-PATTERNS.md
    - packages/crosswake-shell-core-ios/Package.swift
    - packages/crosswake-shell-core-ios/Sources/CrosswakeShellCore/PackStore.swift
    - packages/crosswake-shell-core-ios/Sources/CrosswakeShellCore/ActivationCoordinator.swift
    - packages/crosswake-shell-core-ios/Tests/CrosswakeShellCoreTests/ActivationConformanceTests.swift
  </read_first>
  <files>packages/crosswake-shell-core-ios/Sources/CrosswakeShellCore/PackProvider.swift, packages/crosswake-shell-core-ios/Sources/CrosswakeShellCore/PackStore.swift, packages/crosswake-shell-core-ios/Tests/CrosswakeShellCoreTests/PackProviderFixtureTests.swift, packages/crosswake-shell-core-ios/Tests/CrosswakeShellCoreTests/Resources/pronunciation-pack-fixture.bin, packages/crosswake-shell-core-ios/Tests/CrosswakeShellCoreTests/ActivationConformanceTests.swift</files>
  <behavior>
    - "RED: a route requiring the fixture pack remains `.requiredPack(checking)` before reconciliation and `.requiredPack` after an install acknowledgement if the subsequent status is non-installed or malformed."
    - "RED: a provider that streams the committed fixture bytes, verifies the exact required count and SHA-256, atomically promotes them, persists its installed record, and returns the same exact record from a fresh status lets the existing coordinator resolve the route."
    - "Wrong ID, required version, byte count, integrity attestation, atomic-promotion attestation, or contract version never writes available."
    - "The public requirement/result API contains no URL, filesystem location, Error, credential, archive layout, codec, retention, playback, or free-form metadata member (D-01, D-02, D-03, D-12)."
  </behavior>
  <action>Start with the end-to-end XCTest using a deterministic fixture provider and the existing `ActivationCoordinator.resolve` gate. Add `PackProvider.swift` with explicit contract version 1, immutable `Sendable` requirement and installed-record values, the exact D-11 closed reason enum, and one `Sendable` async protocol whose three methods are `status(for:)`, `install(_:)`, and `invalidate(_:)` per D-01 and D-02. All methods return the same closed result family; they do not throw provider details across the seam. Rework `PackStore` so required declarations are full `PackRequirement` values, initial statuses are checking, and only a fresh `status(for:)` result whose record exactly matches ID/version/byte count plus verified-integrity and completed-atomic-promotion attestations can become available per D-03, D-06, D-07, and D-10. The install path may publish installing stages, but after the provider call it must always reconcile through status; cancellation and every unexpected result map to a closed non-available state. Build the fixture provider in the test target around genuine committed bytes and a private temporary same-volume destination so the tracer proves byte counting, CryptoKit SHA-256, promotion, inventory commit, fresh provider instantiation, and activation. Keep fixture values synthetic and never print its digest, bytes, or temporary location.</action>
  <acceptance_criteria>
    - The committed fixture test starts with route activation blocked as checking, installs real bytes, performs a second provider status call, and only then resolves the route.
    - A successful install result followed by missing, malformed, mismatched, or failed status remains blocked.
    - Exact ID, version, size, integrity verification, atomic promotion, and provider contract version are all mechanically asserted before available.
    - The public contract exposes exactly status/install/invalidate over requirement/result values and contains no host storage, transport, playback, or raw error authority.
    - Captured test output contains only test names and closed states/reasons, never fixture bytes, digest values, URLs, paths, or raw failures.
  </acceptance_criteria>
  <verify><automated>swift test --package-path packages/crosswake-shell-core-ios --filter PackProviderFixtureTests/testVerifiedFixturePromotesThenFreshStatusUnblocksActivation</automated></verify>
  <done>One production-quality happy path proves real immutable bytes cannot unblock a route until exact verification, atomic promotion, persistence, and fresh status reconciliation all agree.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire optional host injection and checking-first cold-launch denial</name>
  <reversibility rating="costly">The D-11 lifecycle and reason values are observable by host UI, diagnostics, tests, and retained low-cardinality proof, so incompatible renames require a contract-version migration.</reversibility>
  <read_first>
    - .planning/phases/161-ios-pronunciation-pack-seam/161-CONTEXT.md
    - packages/crosswake-shell-core-ios/Sources/CrosswakeShellCore/PackProvider.swift
    - packages/crosswake-shell-core-ios/Sources/CrosswakeShellCore/PackStore.swift
    - packages/crosswake-shell-core-ios/Sources/CrosswakeShellCore/CrosswakeShellConfig.swift
    - packages/crosswake-shell-core-ios/Sources/CrosswakeShellCore/ActivationCoordinator.swift
    - packages/crosswake-shell-core-ios/Tests/CrosswakeShellCoreTests/ActivationConformanceTests.swift
  </read_first>
  <files>packages/crosswake-shell-core-ios/Sources/CrosswakeShellCore/CrosswakeShellConfig.swift, packages/crosswake-shell-core-ios/Sources/CrosswakeShellCore/ActivationCoordinator.swift, packages/crosswake-shell-core-ios/Sources/CrosswakeShellCore/PackStore.swift, packages/crosswake-shell-core-ios/Tests/CrosswakeShellCoreTests/PackStoreTests.swift</files>
  <behavior>
    - "RED: bundled shell construction with no provider displays provider_unavailable and never trusts the legacy bundled pack_inventory.json as availability."
    - "RED: a newly constructed store with a provider publishes checking until async reconciliation completes; activation during that window stays blocked."
    - "RED: fresh reconciliation is performed once at cold bootstrap and route reads consume the current in-memory reconciled inventory rather than hashing or calling status synchronously on every navigation."
    - "All eleven D-11 failure reasons and seven lifecycle states round-trip as closed raw values; unknown bounded failures become provider_failed without echoing details."
  </behavior>
  <action>Add optional `packProvider` injection to `CrosswakeShellConfig` without registering it as a bridge capability or exposing its configuration. Update `ActivationCoordinator.bundled` to build the store from immutable declarations and the optional provider, remove bundled inventory as an authority source, and arrange cold bootstrap so the first visible route decision consumes checking while one async reconciliation updates the store and reactivates the last request. Preserve the existing synchronous `resolve` gate and do not make navigation await filesystem work. Add focused `PackStoreTests` for nil provider, cold checking, one reconciliation per bootstrap, malformed/unknown closed mapping, and status side-effect expectations per D-03, D-05, D-06, D-07, D-11, and D-12. Ensure existing callers that omit the provider compile and fail closed rather than simulating success.</action>
  <acceptance_criteria>
    - `CrosswakeShellConfig()` remains source-compatible and means explicitly unconfigured provider, not bundled availability.
    - A missing provider produces a closed provider_unavailable blocked status and all install/retry/invalidate actions remain non-available.
    - Activation before reconciliation is blocked as checking; completion reuses current inventory and reactivates without synchronously calling the provider from `resolve`.
    - Legacy `pack_inventory.json` content and timed sleeps can no longer produce available.
    - Every closed lifecycle/reason value is tested and unknown provider failure details are discarded.
  </acceptance_criteria>
  <verify><automated>swift test --package-path packages/crosswake-shell-core-ios --filter PackStoreTests &amp;&amp; swift test --package-path packages/crosswake-shell-core-ios --filter ActivationConformanceTests</automated></verify>
  <done>The shell's real construction path is checking-first, optionally provider-backed, and fail-closed when unconfigured, while the existing activation gate remains the sole route authority.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| host PackProvider -> Crosswake PackStore | Trusted in-process host code returns facts that core must still bind exactly to the current requirement. |
| staged fixture bytes -> provider installed record | Real bytes cross integrity and atomic-promotion checks before inventory may be persisted. |
| reconciled PackStore inventory -> ActivationCoordinator | Only current exact available state may unblock route activation. |
| provider failures -> UI/tests/diagnostics | Raw errors and host-private values must collapse to closed non-echoing reasons. |

## ASVS L1 Control Mapping

| ASVS category | Required control | Automated evidence |
|---|---|---|
| V4 Access Control | Route activation requires exact current reconciled pack authority. | End-to-end activation tracer and nil/malformed tests. |
| V5 Input Validation | Every provider record binds exact contract version, ID, version, size, verification, and promotion. | PackStore result-matrix XCTest. |
| V6 Cryptography | Fixture provider uses CryptoKit SHA-256 and exact byte count before promotion. | Real-byte integration fixture test. |
| V7 Error Handling | Unknown/cancelled/raw provider failures map to closed reasons only. | Failure mapping and captured-output tests. |
| V8 Data Protection | Digests, bytes, paths, URLs, and errors do not enter observable output. | Public API shape and output privacy assertions. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-161-01 | Tampering | staged pronunciation bytes | high | mitigate | Task 1 requires exact byte count plus CryptoKit SHA-256 before atomic promotion and proves mismatch denial. |
| T-161-02 | Spoofing / Elevation of Privilege | provider installed result | high | mitigate | Tasks 1-2 validate every attestation against the current requirement and require a fresh status before availability. |
| T-161-03 | Tampering / Repudiation | checking and reconciled inventory | high | mitigate | Task 2 removes bundled/timed authority and makes cold starts checking-first. |
| T-161-04 | Information Disclosure | provider failure/output surface | high | mitigate | Closed results discard raw errors and tests assert no host-private values escape. |
| T-161-SC | Tampering | dependency supply chain | low | accept | No package install is planned; Swift Concurrency, Foundation, CryptoKit, and XCTest come from the pinned Apple toolchain already audited by research. |
</threat_model>

<source_coverage_audit>
| Source | ID | Contract | Plan-set coverage | Status |
|---|---|---|---|---|
| GOAL | Phase 161 | Replace simulated pack success with verified atomic foreground provider truth. | Tracer proves requirement -> real bytes -> provider -> reconciliation -> activation. | COVERED |
| REQ | PACK-01 | Host status/install/invalidate seam. | Tasks 1-2. | COVERED |
| REQ | PACK-02 | Invalid/unconfigured conditions never available. | Tasks 1-2 plus later expansion. | COVERED |
| REQ | PACK-03 | Exact size/SHA-256 then atomic install. | Task 1 real-byte tracer. | COVERED |
| REQ | PACK-04 | Explicit core/host ownership. | Public API shape and injection seam in Tasks 1-2. | COVERED |
| RESEARCH | Fresh reconciliation | Operation acknowledgement is never availability authority. | Task 1 makes second status the tracer hinge. | COVERED |
| RESEARCH | No new packages | Use Apple SDK concurrency/Foundation/CryptoKit/XCTest. | Task 1 uses existing SDK only. | COVERED |
| CONTEXT | D-01, D-02, D-03 | Versioned closed requirement/provider seam. | Task 1. | COVERED |
| CONTEXT | D-04, D-05, D-06, D-07, D-10 | Exact verification, serialization posture, checking restart, fresh status, diagnostic-only timestamps. | Tasks 1-2; host mutation expansion is Plan 02. | COVERED |
| CONTEXT | D-11, D-12 | Closed lifecycle/reasons and non-echoing mapping. | Task 2. | COVERED |
| EDGE | PACK-01..PACK-05 | Spec-less probe rows remain unresolved. | All five are preserved as flagged assumptions; no semantics are invented. | FLAGGED |
| PROHIBITION | PACK-02, PACK-04, PACK-05 | Bespoke safety/ownership/promotion constraints. | Descriptor-less flagged-unverified entries are retained without fabricated checks. | FLAGGED |
</source_coverage_audit>

<verification>
Run the real-byte activation tracer first, then the complete focused store and activation suites. The phase cannot proceed to horizontal expansion unless install acknowledgement alone is proven insufficient and the exact fresh-status path is green.
</verification>

<success_criteria>
- The public provider API is narrow, versioned, closed, and host-implemented.
- Real fixture bytes reach available only after exact verification, atomic promotion, persistence, and fresh reconciliation.
- Nil, malformed, cancelled, unknown, and legacy bundled paths stay blocked.
- The existing route gate remains the only activation authority.
</success_criteria>

<output>
Create `.planning/phases/161-ios-pronunciation-pack-seam/161-01-SUMMARY.md` when done.
</output>
