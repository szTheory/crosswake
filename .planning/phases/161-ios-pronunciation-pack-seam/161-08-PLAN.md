---
phase: 161-ios-pronunciation-pack-seam
plan: "08"
type: execute
wave: 5
depends_on: ["161-05"]
files_modified:
  - priv/templates/crosswake/proof_lane/ios/ProofLaneDriver.swift.eex
  - priv/templates/crosswake/proof_lane/ios/ProofLaneApp.swift.eex
  - priv/templates/crosswake/proof_lane/ios/Resources/pronunciation-pack-fixture.bin.eex
  - priv/templates/crosswake/proof_lane/ios/CrosswakeProofLaneTests/ProofLaneContractTests.swift.eex
  - priv/templates/crosswake/proof_lane/ios/CrosswakeProofLaneUITests/ProofLaneUITests.swift.eex
  - script/verify_generated_ios_shell.sh
  - test/crosswake/proof_lane/template_contract_test.exs
  - test/crosswake/proof_lane/ios_verifier_test.exs
autonomous: true
gap_closure: true
requirements: [PACK-01, PACK-03, PACK-04, PACK-05]
must_haves:
  truths:
    - "The generated reference adapter acquires the committed immutable fixture, verifies its pinned exact byte count and SHA-256, atomically promotes it, and derives readiness from the installed artifact rather than a Boolean marker (D-16, D-17; PACK-03)."
    - "A newly constructed adapter after process relaunch reopens and exactly verifies the promoted artifact before reporting ready (D-06, D-07, D-17)."
    - "The host-owned pronunciation operation requires an explicit networking-disabled condition and performs a deterministic read of the installed artifact; missing, corrupt, or network-dependent operation remains blocked (D-18, D-19)."
    - "The verifier passes pack_audio_prerequisite only after consuming an exact structured allowlisted evidence document for acquisition, integrity, atomic promotion, relaunch readback, networking-disabled execution, and installed-audio read (D-20, D-21)."
    - "Default/nil adapter remains non-passing and every generated native result remains advisory; Phase 162 alone owns physical-iPhone and adopter promotion (D-20, D-21; PACK-05)."
  artifacts:
    - path: "priv/templates/crosswake/proof_lane/ios/ProofLaneDriver.swift.eex"
      provides: "Real reference fixture install, artifact-backed relaunch status, and networking-disabled installed-byte operation."
    - path: "priv/templates/crosswake/proof_lane/ios/CrosswakeProofLaneTests/ProofLaneContractTests.swift.eex"
      provides: "Deterministic structured evidence producer from actual reference-adapter operations."
    - path: "script/verify_generated_ios_shell.sh"
      provides: "Exact structured evidence parser and advisory pack-audio gate."
    - path: "test/crosswake/proof_lane/ios_verifier_test.exs"
      provides: "Malformed/missing/partial/forged transcript rejection and exact structured pass regression."
  key_links:
    - from: "pronunciation-pack-fixture.bin"
      to: "ProofLaneReferencePackAdapter.installPronunciationPackForeground"
      via: "pinned exact integrity, same-volume staging, atomic promotion, and artifact-backed state"
      pattern: "SHA256|replaceItemAt|moveItem|atomic"
    - from: "relaunch adapter"
      to: "installed artifact"
      via: "fresh byte-count and SHA-256 readback"
      pattern: "observe|readback|installed"
    - from: "ProofLaneContractTests"
      to: "verify_generated_ios_shell.sh"
      via: "temporary exact-schema structured evidence file, never console markers"
      pattern: "CROSSWAKE_PROOF_LANE_EVIDENCE_PATH|assertion_ids|schema_version"
  prohibitions:
    - requirement_id: PACK-02
      category: safety
      statement: "MUST NOT let requested, staged, acknowledged, old-version, malformed, revoked, or unreconciled media silently activate a route or downgrade it to a different runtime behavior."
      status: unresolved
      verification: flagged-unverified
    - requirement_id: PACK-04
      category: values
      statement: "MUST NOT turn the provider seam into Crosswake-owned distribution, generic storage, asset lookup, playback policy, or host media-layout authority."
      status: unresolved
      verification: flagged-unverified
    - requirement_id: PACK-05
      category: transparency
      statement: "MUST NOT present local fixture, simulator, package, or generated native results as physical-iPhone, adopter-instance, Android, background-transfer, or generic-storage proof."
      status: unresolved
      verification: flagged-unverified
flagged_assumptions:
  - "[UNRESOLVED-SPEC-PROBE][PACK-01 unclassified] The provider seam remains exactly status/install/invalidate; no transport or storage operation is inferred."
  - "[UNRESOLVED-SPEC-PROBE][PACK-02 unclassified] Every unrecognized availability input stays non-available under the closed D-11 reasons."
  - "[UNRESOLVED-SPEC-PROBE][PACK-03 unclassified] Exact size, pinned SHA-256, atomic promotion, persistence ordering, and fresh status are all mandatory."
  - "[UNRESOLVED-SPEC-PROBE][PACK-04 concurrency] Interrupted or overlapping work is governed by per-pack serialization and current-operation authority."
  - "[UNRESOLVED-SPEC-PROBE][PACK-05 unclassified] The iOS foreground-only stop list is exhaustive and no adjacent platform or product behavior is inferred."
---

<objective>
Replace the generated reference adapter's Boolean/marker success with real verified fixture installation, artifact-backed relaunch, deterministic networking-disabled pronunciation read, and structured verifier evidence.

Purpose: Make the existing Phase 159 lane capable of honest advisory pack/audio proof without inventing a second harness or broadening the public provider contract.
Output: Production-quality generated reference adapter operations, exact structured evidence, and fail-closed verifier tests.
</objective>

## Artifacts this phase produces

- New internal `ProofLanePackRequirement`, `ProofLaneInstalledRecord`, and `ProofLanePackEvidence` symbols in `ProofLaneDriver.swift.eex`.
- New internal `ProofLaneEvidenceDocument` Codable symbol and `writeStructuredEvidenceIfRequested(_:)` helper in `ProofLaneContractTests.swift.eex`.
- New process-scoped `CROSSWAKE_PROOF_LANE_EVIDENCE_PATH` test/verifier handoff; it carries only schema version, closed outcome, and approved assertion IDs.
- New approved assertion IDs: `fixture_acquired`, `exact_integrity_verified`, `atomic_promotion_completed`, `relaunch_artifact_readback`, `networking_disabled`, and `installed_audio_read`.
- Changed generated fixture bytes to a deterministic synthetic pronunciation-read fixture with pinned private integrity metadata in the generated reference adapter.
- No public PackProvider playback/asset API, external service, retained path/digest/media field, device-support claim, Android path, background transfer, or product surface.

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/ADR-FIRST-B2C-ADOPTER.md
@.planning/phases/161-ios-pronunciation-pack-seam/161-CONTEXT.md
@.planning/phases/161-ios-pronunciation-pack-seam/161-VERIFICATION.md
@.planning/phases/161-ios-pronunciation-pack-seam/161-04-SUMMARY.md
@priv/templates/crosswake/proof_lane/ios/ProofLaneDriver.swift.eex
@priv/templates/crosswake/proof_lane/ios/ProofLaneApp.swift.eex
@priv/templates/crosswake/proof_lane/ios/CrosswakeProofLaneTests/ProofLaneContractTests.swift.eex
@priv/templates/crosswake/proof_lane/ios/CrosswakeProofLaneUITests/ProofLaneUITests.swift.eex
@script/verify_generated_ios_shell.sh
@test/crosswake/proof_lane/ios_verifier_test.exs
@test/crosswake/proof_lane/template_contract_test.exs

<interfaces>
- `ProofLaneHostAdapter` already separates `installPronunciationPackForeground()` from `exerciseInstalledPronunciationAudioOffline()` and returns closed `ProofLaneSnapshot` values.
- `ProofLaneReferencePackAdapter` currently persists only a UserDefaults Boolean and must retain its nil/default factory behavior while replacing its internals.
- `verify_generated_ios_shell.sh` owns temporary directories and already emits only `{outcome,rule_id,scope}`; keep that public output shape.
- `Crosswake.ProofLane.Evidence` already owns retained evidence and must not receive the temporary structured operation detail.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement real fixture installation, relaunch readback, and offline pronunciation read in the generated adapter</name>
  <reversibility rating="costly">Generated sources become host-owned after copying; changing their operation contract later requires coordinated template and adopter proof updates even though the reference implementation remains internal.</reversibility>
  <read_first>
    - AGENTS.md
    - .planning/ADR-FIRST-B2C-ADOPTER.md
    - .planning/phases/161-ios-pronunciation-pack-seam/161-CONTEXT.md
    - .planning/phases/161-ios-pronunciation-pack-seam/161-VERIFICATION.md
    - priv/templates/crosswake/proof_lane/ios/ProofLaneDriver.swift.eex
    - priv/templates/crosswake/proof_lane/ios/ProofLaneApp.swift.eex
    - priv/templates/crosswake/proof_lane/ios/Resources/pronunciation-pack-fixture.bin.eex
    - priv/templates/crosswake/proof_lane/ios/CrosswakeProofLaneTests/ProofLaneContractTests.swift.eex
    - priv/templates/crosswake/proof_lane/ios/CrosswakeProofLaneUITests/ProofLaneUITests.swift.eex
  </read_first>
  <files>priv/templates/crosswake/proof_lane/ios/ProofLaneDriver.swift.eex, priv/templates/crosswake/proof_lane/ios/ProofLaneApp.swift.eex, priv/templates/crosswake/proof_lane/ios/Resources/pronunciation-pack-fixture.bin.eex, priv/templates/crosswake/proof_lane/ios/CrosswakeProofLaneTests/ProofLaneContractTests.swift.eex, priv/templates/crosswake/proof_lane/ios/CrosswakeProofLaneUITests/ProofLaneUITests.swift.eex</files>
  <behavior>
    - Empty, missing, wrong-size, wrong-digest, interrupted, or promotion-failed fixture acquisition returns blocked and leaves no artifact-backed ready state.
    - Successful foreground installation validates pinned exact count/SHA-256 before same-volume atomic promotion and reports passed only after reopening and validating the destination.
    - A new adapter instance derives relaunch readiness by reading/verifying the installed artifact; UserDefaults Boolean/marker state cannot grant readiness.
    - The audio/read operation requires the networking-disabled launch condition, revalidates the installed artifact, and performs a deterministic read operation over the synthetic pronunciation payload.
    - XCUITest drives missing provider, install, terminate/relaunch, networking-disabled relaunch, and the audio/read action only through stable accessibility IDs.
  </behavior>
  <action>Write failing generated contract tests for wrong size/digest, interrupted acquisition, atomic promotion failure, post-install artifact mutation, fresh adapter readback, and the explicit networking-disabled installed-byte operation. Replace UserDefaults authority in `ProofLaneReferencePackAdapter` with Application Support storage, unique same-volume staging, CryptoKit incremental SHA-256, exact pinned requirement comparison, atomic promotion, and artifact-backed `observe()`. Keep the reference fixture synthetic and immutable; its exact private metadata lives only in generated host code. Make `exerciseInstalledPronunciationAudioOffline()` require the networking-disabled environment, freshly verify the installed artifact, and execute a deterministic bounded read of the pronunciation payload before returning passed. Update app/UI tests so networking-disabled state is present before the audio process launch and no hidden filesystem or adapter internals cross the XCUITest boundary per D-16 through D-20.</action>
  <acceptance_criteria>
    - No UserDefaults Boolean, install acknowledgement, or nonempty-fixture check can produce passed.
    - Exact fixture integrity, atomic promotion, destination readback, new-instance relaunch, and networking-disabled installed-byte read each have a deterministic positive and negative XCTest.
    - XCUITest uses only stable IDs and closed labels; it does not inspect paths, digest values, fixture bytes, or adapter internals.
    - The public Crosswake `PackProvider` API remains unchanged and gains no asset or playback operation.
  </acceptance_criteria>
  <verify><automated>mix test test/crosswake/proof_lane/template_contract_test.exs &amp;&amp; env -u CROSSWAKE_IOS_XCODEBUILD_BIN -u CROSSWAKE_IOS_PROJECT_ROOT -u CROSSWAKE_IOS_SHIM_MODE CROSSWAKE_IOS_USE_LOCAL_CORE=1 CROSSWAKE_IOS_LAUNCH_SIMULATOR=1 bash script/verify_generated_ios_shell.sh --proof-lane --reference-pack-adapter</automated></verify>
  <done>The generated reference adapter earns advisory readiness from exact installed bytes across relaunch and an explicit networking-disabled pronunciation read, never from Boolean/marker theater.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Gate advisory pack audio on exact structured operation evidence</name>
  <read_first>
    - .planning/phases/161-ios-pronunciation-pack-seam/161-CONTEXT.md
    - .planning/phases/161-ios-pronunciation-pack-seam/161-VERIFICATION.md
    - priv/templates/crosswake/proof_lane/ios/CrosswakeProofLaneTests/ProofLaneContractTests.swift.eex
    - script/verify_generated_ios_shell.sh
    - test/crosswake/proof_lane/ios_verifier_test.exs
    - test/crosswake/proof_lane/template_contract_test.exs
  </read_first>
  <files>priv/templates/crosswake/proof_lane/ios/CrosswakeProofLaneTests/ProofLaneContractTests.swift.eex, script/verify_generated_ios_shell.sh, test/crosswake/proof_lane/ios_verifier_test.exs, test/crosswake/proof_lane/template_contract_test.exs</files>
  <behavior>
    - A successful generated XCTest writes one atomic exact-schema document to the verifier-owned temporary path with only schema version, closed outcome, and the six approved assertion IDs.
    - The shell verifier requires a regular file, exact keys/types/outcome, exact assertion-ID set with no duplicates/extras, and successful XCTest execution before advisory pass.
    - Printed marker strings without the structured document, partial/extra/malformed documents, failed tests, symlink/nonregular evidence, or reference-adapter mode absence remain blocked.
    - The temporary document is deleted on every exit and never becomes retained Crosswake Evidence or validation content.
  </behavior>
  <action>Introduce `ProofLaneEvidenceDocument` in the generated contract-test target and write it atomically only after the same test has executed real acquisition, exact integrity, atomic promotion, fresh-adapter readback, networking-disabled enforcement, and installed-audio read. Have `verify_generated_ios_shell.sh` create a restrictive temporary evidence path, pass it only to the XCTest process, and parse it structurally with an existing system tool after tests succeed. Require exact schema, closed passed outcome, and exact approved assertion-ID set; reject symlinks, nonregular files, absent/partial/extra/duplicate/wrong-type data, and console-marker-only transcripts. Keep public verifier output at the existing closed three-field JSON and cleanup the temporary file on every exit. Rewrite `ios_verifier_test.exs` shims to create structured fixtures through the handed-off path and add negative cases for every malformed class; remove marker strings as authority per D-20 and D-21.</action>
  <acceptance_criteria>
    - `pack_audio_prerequisite` passes only when XCTest succeeds and the exact structured document proves all six operations.
    - Console text containing prior marker words without a valid document stays blocked.
    - Missing, partial, extra, duplicate, malformed, wrong-outcome, symlink, and nonregular documents each have a deterministic blocked verifier test.
    - Verifier stdout remains only the closed three-field outcome and contains no XCTest output, digest, path, URL, media, account/device identifier, token, or raw error.
  </acceptance_criteria>
  <verify><automated>mix test test/crosswake/proof_lane/ios_verifier_test.exs test/crosswake/proof_lane/template_contract_test.exs &amp;&amp; env -u CROSSWAKE_IOS_XCODEBUILD_BIN -u CROSSWAKE_IOS_PROJECT_ROOT -u CROSSWAKE_IOS_SHIM_MODE CROSSWAKE_IOS_USE_LOCAL_CORE=1 CROSSWAKE_IOS_LAUNCH_SIMULATOR=1 bash script/verify_generated_ios_shell.sh --proof-lane --reference-pack-adapter</automated></verify>
  <done>The advisory verifier consumes exact structured behavioral evidence and cannot be promoted by printed markers or incomplete adapter operations.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| bundled fixture -> generated adapter | Bytes must cross exact integrity and atomic promotion before readiness. |
| installed artifact -> relaunch/audio operation | Fresh local readback, not remembered Boolean state, establishes authority. |
| XCTest process -> shell verifier | Untrusted output crosses an exact structured temporary-file contract. |
| simulator advisory -> retained/support truth | Local native success must remain non-promoting. |

## ASVS L1 / STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-161-36 | Spoofing | generated reference adapter | high | mitigate | Task 1 removes Boolean authority and requires artifact-backed exact verification for every passed snapshot. |
| T-161-37 | Tampering | staged/generated fixture | high | mitigate | Task 1 verifies pinned exact count/SHA-256 before atomic promotion and revalidates destination bytes. |
| T-161-38 | Spoofing / Repudiation | verifier evidence | high | mitigate | Task 2 requires exact structured operation evidence plus successful XCTest; marker-only output is rejected. |
| T-161-39 | Information Disclosure | temporary/retained evidence | high | mitigate | Task 2 allowlists only closed assertion IDs/outcome, uses restrictive temporary cleanup, and preserves three-field stdout. |
| T-161-40 | Elevation of Privilege | simulator promotion | high | mitigate | Both tasks preserve advisory rule ID and Phase 162-only device promotion. |
| T-161-41 | Tampering | package supply chain | low | accept | No new package or dependency is installed. |
</threat_model>

<verification>
Run focused rendered-template and verifier tests, then execute the real generated reference-adapter XCTest/XCUITest lane. All high-severity integrity, evidence, privacy, and promotion threats are blocking.
</verification>

<success_criteria>
- Generated reference readiness is derived from exact atomically promoted bytes and fresh relaunch readback.
- Networking-disabled pronunciation read is a real adapter operation with deterministic negative coverage.
- Structured exact-schema evidence, not console markers, is the sole advisory pass authority.
- No private proof values are retained and Phase 162/device/support boundaries do not advance.
</success_criteria>

<output>
Create `.planning/phases/161-ios-pronunciation-pack-seam/161-08-SUMMARY.md` when done.
</output>
