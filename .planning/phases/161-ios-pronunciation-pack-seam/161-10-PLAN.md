---
phase: 161-ios-pronunciation-pack-seam
plan: "10"
type: execute
wave: 8
depends_on: ["161-09"]
files_modified:
  - examples/ios_shell_host/CrosswakeShell/PronunciationPackProvider.swift
  - examples/ios_shell_host/CrosswakeShellTests/PronunciationPackProviderTests.swift
  - examples/ios_shell_host/CrosswakeShell.xcodeproj/project.pbxproj
autonomous: true
gap_closure: true
requirements: [PACK-01, PACK-02, PACK-03, PACK-04]
must_haves:
  truths:
    - "Every provider status and cold-launch reconciliation checks the current installed artifact against the exact PackRequirement; absent, unreadable, wrong-size, and wrong-digest bytes stay non-available (D-02, D-03, D-06, D-07, D-10; PACK-02, PACK-03)."
    - "A post-promotion inventory-persistence failure restores the complete prior artifact-plus-inventory pair, or removes an uncommitted first install, so no durable record can attest to different bytes (D-04, D-05, D-08; PACK-02, PACK-03)."
    - "The reference host keeps URL, filesystem, bytes, hashing, inventory, and rollback mechanics private behind the unchanged status/install/invalidate seam (D-01, D-04, D-12; PACK-01, PACK-04)."
    - "The focused concrete-provider XCTest suite passes from a clean DerivedData directory on the available iPhone 17 simulator without ONLY_ACTIVE_ARCH or another architecture-masking override (D-16, D-17; PACK-01)."
  artifacts:
    - path: "examples/ios_shell_host/CrosswakeShell/PronunciationPackProvider.swift"
      provides: "Byte-backed status reconciliation and recoverable artifact-plus-inventory publication inside the host-owned provider actor."
    - path: "examples/ios_shell_host/CrosswakeShellTests/PronunciationPackProviderTests.swift"
      provides: "Deterministic deletion, corruption, read-failure, post-promotion inventory-failure, relaunch, and known-good rollback regressions."
    - path: "examples/ios_shell_host/CrosswakeShell.xcodeproj/project.pbxproj"
      provides: "Destination-correct simulator architecture settings for the app, test target, and local Swift package product."
  key_links:
    - from: "PronunciationPackProvider.status(for:)"
      to: "PackStore fresh reconciliation"
      via: "inventory lookup plus current artifact open, bounded byte count, and SHA-256 comparison against the supplied requirement"
      pattern: "status\(for:|verify|expectedByteCount|expectedSHA256"
    - from: "PronunciationPackProvider.install(_:)"
      to: "private durable inventory"
      via: "recoverable promotion boundary that restores the prior artifact when saveInventory fails"
      pattern: "replaceItemAt|saveInventory|rollback|restore"
    - from: "CrosswakeShellTests"
      to: "CrosswakeShellCore local package product"
      via: "matching destination-derived iOS Simulator SDK and standard architecture settings"
      pattern: "SUPPORTED_PLATFORMS|SDKROOT|ARCHS"
---

<objective>
Close the remaining reference-host integrity, publication, and simulator-execution gaps without widening the provider contract.

Purpose: Make fresh status a real byte attestation, make replacement recoverable across durable-inventory failure, and prove the concrete host path in its clean intended XCTest environment per D-01 through D-12 and D-16 through D-17.
Output: A corrected host provider, deterministic storage/relaunch regressions, and destination-correct Xcode architecture configuration.
</objective>

<gap_root_cause>
This is an execution miss against the already-correct Plans 161-02/06 contract, not a changed product assumption: the concrete provider persisted the requested ordering but omitted rollback and treated metadata as status authority. The architecture gap was masked by prior `ONLY_ACTIVE_ARCH=YES` verification, so this plan repairs and proves the clean test substrate first, then keeps that escape hatch absent from both production TDD gates.
</gap_root_cause>

## Artifacts this phase produces

- Changed `PronunciationPackProvider.status(for:)` to attest the current artifact on every reconciliation instead of trusting inventory metadata alone.
- Changed `PronunciationPackProvider.install(_:)` publication so every inventory failure after promotion restores the preceding artifact/inventory pair or removes an uncommitted first install.
- New private injectable artifact-read and inventory-write fault seams used only to make all closed failure boundaries deterministic in XCTest.
- New concrete-provider XCTest cases for deleted, unreadable, same-size corrupted, wrong-size, and transaction-rollback relaunch paths.
- Corrected `CrosswakeShell.xcodeproj` simulator SDK/architecture inheritance for the app and test target; no new dependency, package, public API, schema, Android path, background work, storage product, or evidence field.

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/ADR-FIRST-B2C-ADOPTER.md
@.planning/FIRST-B2C-ADOPTER-ADOPTION-BRIEF.md
@.planning/FIRST-B2C-ADOPTER-ROUTE-POLICY-MAP.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/161-ios-pronunciation-pack-seam/161-CONTEXT.md
@.planning/phases/161-ios-pronunciation-pack-seam/161-RESEARCH.md
@.planning/phases/161-ios-pronunciation-pack-seam/161-PATTERNS.md
@.planning/phases/161-ios-pronunciation-pack-seam/161-VERIFICATION.md
@.planning/phases/161-ios-pronunciation-pack-seam/161-02-SUMMARY.md
@.planning/phases/161-ios-pronunciation-pack-seam/161-06-SUMMARY.md
@.planning/phases/161-ios-pronunciation-pack-seam/161-09-SUMMARY.md
@examples/ios_shell_host/CrosswakeShell/PronunciationPackProvider.swift
@examples/ios_shell_host/CrosswakeShellTests/PronunciationPackProviderTests.swift
@examples/ios_shell_host/CrosswakeShell.xcodeproj/project.pbxproj
@packages/crosswake-shell-core-ios/Sources/CrosswakeShellCore/PackProvider.swift
@packages/crosswake-shell-core-ios/Sources/CrosswakeShellCore/PackStore.swift

<interfaces>
- `PackProvider` remains the D-01 async `status(for:)`, `install(_:)`, and `invalidate(_:)` protocol over `PackRequirement` and closed `PackProviderResult`; do not add a URL, path, playback, storage, or transport member.
- `PackFailureReason` already supplies `sizeMismatch`, `digestMismatch`, `inventoryPersistenceFailed`, and `providerFailed`; artifact absence maps to `notInstalled`, while deterministic open/read failure maps to the existing non-echoing `providerFailed` reason.
- `PackStore.status(for:requirement:)` grants available only for an exact installed record and maps old installed versions to stale; the host provider must ensure the returned record is backed by bytes checked during that same status call.
- `PronunciationPackProvider` is an actor and `verifyStagedFile(_:chunkSize:)` already performs bounded CryptoKit hashing in a detached utility task; reuse one byte-verification primitive for staging and installed-artifact reconciliation.
- The Xcode project uses a local `CrosswakeShellCore` Swift package product and a hosted `CrosswakeShellTests` target; simulator destination settings must agree across project, app, test bundle, and package dependency without command-line architecture overrides.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Repair and prove clean simulator architecture compatibility</name>
  <reversibility rating="reversible">Xcode destination and architecture inheritance can be changed locally without migrating a public or persisted contract.</reversibility>
  <read_first>
    - .planning/phases/161-ios-pronunciation-pack-seam/161-VERIFICATION.md
    - .planning/phases/161-ios-pronunciation-pack-seam/161-09-SUMMARY.md
    - examples/ios_shell_host/CrosswakeShell.xcodeproj/project.pbxproj
    - examples/ios_shell_host/CrosswakeShell.xcodeproj/xcshareddata/xcschemes/CrosswakeShell.xcscheme
    - packages/crosswake-shell-core-ios/Package.swift
    - examples/ios_shell_host/CrosswakeShellTests/PronunciationPackProviderTests.swift
  </read_first>
  <files>examples/ios_shell_host/CrosswakeShell.xcodeproj/project.pbxproj</files>
  <behavior>
    - "The CrosswakeShell app target, hosted CrosswakeShellTests bundle, and local CrosswakeShellCore product resolve the selected iPhone simulator SDK and a compatible standard simulator architecture from the destination."
    - "A clean focused test run compiles and executes the existing PronunciationPackProviderTests without ONLY_ACTIVE_ARCH, EXCLUDED_ARCHS, a prewarmed DerivedData module, or manual Xcode intervention (D-16, D-17)."
    - "Device support remains present for Phase 162; the repair does not freeze the project to one simulator CPU or add an Android/platform claim."
  </behavior>
  <action>Use `xcodebuild -showBuildSettings` for the app and hosted test target at the available iPhone 17 simulator destination to identify the exact SDK/architecture divergence reported in `161-VERIFICATION.md`. Correct `project.pbxproj` so project, app, and test configurations inherit the destination-selected `iphonesimulator` SDK and standard architectures consistently with the local Swift package product. Remove or replace the hard-coded device-SDK/architecture setting responsible for compiling CrosswakeShell and CrosswakeShellCore for different simulator architectures; do not solve the gap by masking an architecture, pinning one host CPU, deleting device support, or changing package source. Run the clean command below with a dedicated DerivedData path and inspect its build settings/output only to confirm the host module and package module share the selected simulator architecture. Retain no raw build output or `.xcresult` as phase evidence. This establishes the executable test substrate before either production TDD loop and closes the executable-host portion of D-16 and D-17 while D-18 through D-21 remain advisory/non-promoting.</action>
  <acceptance_criteria>
    - Destination-resolved app and test settings use the iOS Simulator SDK and compatible standard architecture inheritance.
    - The clean focused XCTest run passes with no command-line architecture masking or excluded-architecture workaround.
    - The existing iPhoneOS platform remains supported for Phase 162 and no physical-device claim is made.
    - No dependency, package, public provider symbol, Android file, or test-only production branch is added.
  </acceptance_criteria>
  <verify><automated>xcodebuild clean test -project examples/ios_shell_host/CrosswakeShell.xcodeproj -scheme CrosswakeShell -derivedDataPath .build/phase161-reference-host-architecture -destination 'platform=iOS Simulator,name=iPhone 17' -only-testing:CrosswakeShellTests/PronunciationPackProviderTests CODE_SIGNING_ALLOWED=NO</automated></verify>
  <done>The concrete reference-host provider suite compiles and passes from clean simulator state under honest destination-derived architecture configuration before production behavior changes begin.</done>
</task>

<task type="tracer" tdd="true">
  <name>Task 2: Reconcile every current artifact byte before route availability</name>
  <reversibility rating="reversible">Installed-byte verification is private host-provider behavior behind the already-settled versioned D-01 protocol.</reversibility>
  <read_first>
    - AGENTS.md
    - .planning/phases/161-ios-pronunciation-pack-seam/161-CONTEXT.md
    - .planning/phases/161-ios-pronunciation-pack-seam/161-VERIFICATION.md
    - examples/ios_shell_host/CrosswakeShell/PronunciationPackProvider.swift
    - examples/ios_shell_host/CrosswakeShellTests/PronunciationPackProviderTests.swift
    - examples/ios_shell_host/CrosswakeShell.xcodeproj/project.pbxproj
    - packages/crosswake-shell-core-ios/Sources/CrosswakeShellCore/PackProvider.swift
    - packages/crosswake-shell-core-ios/Sources/CrosswakeShellCore/PackStore.swift
  </read_first>
  <files>examples/ios_shell_host/CrosswakeShell/PronunciationPackProvider.swift, examples/ios_shell_host/CrosswakeShellTests/PronunciationPackProviderTests.swift</files>
  <behavior>
    - "A newly constructed provider returns installed only after inventory and the current destination artifact agree with the supplied contract version, pack ID, required version, exact byte count, and SHA-256 (D-02, D-03, D-06, D-07)."
    - "Deleting the installed artifact after a successful install makes both immediate status and a newly constructed provider's relaunch status notInstalled; PackStore activation remains requiredPack."
    - "Replacing installed bytes with a same-size digest mismatch returns digestMismatch; changing their length returns sizeMismatch; an injected artifact open/read failure returns providerFailed, with every case remaining route-blocking and non-echoing (D-11, D-12)."
    - "Status remains side-effect-free: it does not repair, promote, rewrite inventory, remove a destination, or treat the prior installed record as current authority (D-05, D-10)."
  </behavior>
  <action>With Task 1's clean simulator substrate already passing, write the deletion, same-size corruption, wrong-size, and injected read-failure tests first, and make each test create a new `PronunciationPackProvider` and drive fresh `status(for:)`; for deletion and corruption also pass the result through a newly constructed/reconciled `PackStore` and the existing activation gate so this production tracer proves route denial end to end. Refactor the existing off-main bounded verifier into a private installed-artifact verification path used by both `install(_:)` and `status(for:)`. In `status(for:)`, load the exact inventory record, locate only the private destination derived by the host, and attest the current regular artifact against the supplied `PackRequirement` before returning `.installed(record)`. Return `.notInstalled` for absence, `.failure(.sizeMismatch)` or `.failure(.digestMismatch)` for exact integrity mismatches, and `.failure(.providerFailed)` for injected or real open/read failure. Preserve version-stale behavior only when the returned record is still backed by bytes that satisfy the current size/digest requirement; otherwise the exact mismatch reason wins. Never log or return a digest, byte, path, URL, inventory body, or raw error. This closes the first verification gap per D-02, D-03, D-06, D-07, D-10, D-11, D-12, D-16, and D-17 without moving storage authority into core.</action>
  <acceptance_criteria>
    - Every status call opens and verifies the installed artifact currently at the host-private destination before it may return installed.
    - Delete, injected read failure, wrong count, and wrong digest each produce their specified closed result on immediate status and after provider relaunch.
    - Delete and corruption paths reconcile through PackStore and leave ActivationCoordinator at requiredPack; persisted metadata alone never unblocks the route.
    - Status performs no writes or cleanup and observable output contains only closed result/state identifiers.
  </acceptance_criteria>
  <verify><automated>xcodebuild clean test -project examples/ios_shell_host/CrosswakeShell.xcodeproj -scheme CrosswakeShell -derivedDataPath .build/phase161-reference-host-attestation -destination 'platform=iOS Simulator,name=iPhone 17' -only-testing:CrosswakeShellTests/PronunciationPackProviderTests CODE_SIGNING_ALLOWED=NO</automated></verify>
  <done>Fresh status and relaunch reconciliation derive availability from the current exact artifact bytes, never from inventory metadata alone, under the repaired clean simulator configuration.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Roll back every post-promotion inventory failure as one recoverable publication</name>
  <reversibility rating="reversible">The rollback and injected-writer mechanisms are private host implementation choices behind the stable provider result contract.</reversibility>
  <read_first>
    - .planning/phases/161-ios-pronunciation-pack-seam/161-CONTEXT.md
    - .planning/phases/161-ios-pronunciation-pack-seam/161-VERIFICATION.md
    - .planning/phases/161-ios-pronunciation-pack-seam/161-02-PLAN.md
    - examples/ios_shell_host/CrosswakeShell/PronunciationPackProvider.swift
    - examples/ios_shell_host/CrosswakeShellTests/PronunciationPackProviderTests.swift
    - examples/ios_shell_host/CrosswakeShell.xcodeproj/project.pbxproj
  </read_first>
  <files>examples/ios_shell_host/CrosswakeShell/PronunciationPackProvider.swift, examples/ios_shell_host/CrosswakeShellTests/PronunciationPackProviderTests.swift</files>
  <behavior>
    - "An injected inventory write failure after replacing a known-good artifact returns inventoryPersistenceFailed and restores the exact prior artifact plus its prior inventory record (D-04, D-08)."
    - "An injected inventory write failure after a first installation removes the uncommitted promoted artifact and leaves inventory absent; immediate and relaunched status remain non-available."
    - "After replacement rollback, a fresh provider reports the old installed record only when its restored bytes pass current status verification; the attempted new requirement cannot become available."
    - "Rollback failure cannot return installed or leave newly promoted bytes paired with old metadata; it collapses to a closed failure without exposing filesystem details (D-11, D-12)."
  </behavior>
  <action>With Task 1's architecture repair still proven by Task 2's clean no-mask gate, add a deterministic private inventory-writer failure seam and RED tests that fail exactly after artifact promotion for both first install and replacement. Rework `install(_:)` into a recoverable publication boundary: retain a private same-volume handle/name for the prior destination during replacement, promote only verified staged bytes, persist the matching inventory, and discard the retained prior artifact only after durable metadata commit. If persistence fails, restore the prior destination and leave the prior inventory byte-for-byte authoritative; for a first install, remove the uncommitted destination and keep inventory absent. Ensure deferred staging cleanup cannot remove a restored winner. Return `.failure(.inventoryPersistenceFailed)` for the injected persistence boundary, and fail closed if rollback itself fails; never claim the attempted record installed. After each failure, instantiate a fresh provider and assert status/relaunch truth against both old and attempted requirements. Keep all transaction files inside the host-owned storage root, retain actor serialization, and do not add a generic transaction/storage API, background recovery worker, or evidence field. This implements D-04, D-05, D-08, D-11, D-12, D-16, and D-17.</action>
  <acceptance_criteria>
    - The deterministic fault fires after verified promotion and before durable matching inventory completion.
    - Replacement failure restores the prior artifact and prior inventory; first-install failure leaves neither visible artifact nor installed record.
    - Fresh status and a newly constructed provider prove the restored pair and deny the attempted replacement.
    - No failure branch can leave old metadata paired with new bytes or emit private filesystem/integrity detail.
  </acceptance_criteria>
  <verify><automated>xcodebuild clean test -project examples/ios_shell_host/CrosswakeShell.xcodeproj -scheme CrosswakeShell -derivedDataPath .build/phase161-reference-host-rollback -destination 'platform=iOS Simulator,name=iPhone 17' -only-testing:CrosswakeShellTests/PronunciationPackProviderTests CODE_SIGNING_ALLOWED=NO</automated></verify>
  <done>Artifact and inventory publication is recoverable as one host-private transaction across every injected post-promotion persistence failure, proven on a clean repaired simulator substrate.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| installed host artifact -> provider status | Mutable filesystem state must be re-attested before core can receive installed authority. |
| staged replacement -> live artifact and inventory | Two durable resources must publish or roll back as one observable state. |
| host provider result -> PackStore activation | Closed results may unblock only after exact current-byte verification. |
| Xcode destination -> app/test/package modules | All modules must compile for the same selected simulator SDK and architecture. |

## ASVS L1 Control Mapping

| ASVS category | Required control | Automated evidence |
|---|---|---|
| V4 Access Control | Route availability requires current exact artifact authority. | Delete/corrupt/read-failure activation tracer. |
| V5 Validation | Status validates count and SHA-256 against the complete PackRequirement. | Focused provider result matrix. |
| V6 Cryptography | Bounded CryptoKit SHA-256 re-attests current bytes. | Concrete fixture XCTest. |
| V7 Error Handling | Read, persistence, and rollback failures collapse to closed non-echoing reasons. | Injected fault tests. |
| V8 Data Protection | No bytes, digests, paths, URLs, inventory bodies, or raw errors enter output/evidence. | Semantic assertions and final privacy gate. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-161-48 | Tampering | installed pronunciation artifact | high | mitigate | Task 2 hashes and counts current bytes on every status/relaunch before installed can be returned. |
| T-161-49 | Elevation of Privilege | persisted installed record | high | mitigate | Task 2 proves metadata alone cannot grant PackStore/ActivationCoordinator availability. |
| T-161-50 | Tampering / Repudiation | replacement publication | high | mitigate | Task 3 injects post-promotion persistence failure and requires complete prior-state restoration. |
| T-161-51 | Information Disclosure | provider failures and tests | medium | mitigate | Tasks 2-3 retain only closed reasons and prohibit raw filesystem/integrity values. |
| T-161-52 | Denial of Service | simulator architecture mismatch | medium | mitigate | Task 1 aligns destination SDK/architectures and proves a clean executable XCTest run before production TDD begins. |
| T-161-SC | Tampering | package supply chain | low | accept | No package-manager install or dependency change occurs; the existing local Swift package is reused. |
</threat_model>

<verification>
Run the three focused task gates in order: establish the clean no-mask simulator substrate first, then rerun each production TDD slice from its own clean dedicated DerivedData path. Inspect the diff to confirm only the host provider, its tests, and destination architecture configuration changed.
</verification>

<success_criteria>
- Deleted, unreadable, wrong-size, and wrong-digest current artifacts are non-available on immediate status and fresh relaunch reconciliation.
- Every injected post-promotion inventory failure restores the exact prior known-good pair or leaves a first install wholly unpublished.
- The concrete provider XCTest suite passes from clean DerivedData without architecture-masking command-line flags.
- The public provider seam, Phoenix-first ownership, privacy exclusions, iOS foreground scope, and Phase 162-only physical-device boundary remain unchanged.
</success_criteria>

<output>
Create `.planning/phases/161-ios-pronunciation-pack-seam/161-10-SUMMARY.md` when done.
</output>
