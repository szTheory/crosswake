---
phase: 161-ios-pronunciation-pack-seam
plan: "02"
type: execute
wave: 2
depends_on: ["161-01"]
files_modified:
  - packages/crosswake-shell-core-ios/Sources/CrosswakeShellCore/PackStore.swift
  - packages/crosswake-shell-core-ios/Tests/CrosswakeShellCoreTests/PackStoreTests.swift
  - packages/crosswake-shell-core-ios/Tests/CrosswakeShellCoreTests/PackProviderFixtureTests.swift
  - examples/ios_shell_host/CrosswakeShell/PronunciationPackProvider.swift
  - examples/ios_shell_host/CrosswakeShellTests/PronunciationPackProviderTests.swift
  - examples/ios_shell_host/CrosswakeShell/Resources/pronunciation-pack-fixture.bin
  - examples/ios_shell_host/CrosswakeShell.xcodeproj/project.pbxproj
autonomous: true
requirements: [PACK-02, PACK-03, PACK-04]
must_haves:
  truths:
    - "The host reference provider performs foreground same-volume staging, off-main-actor streaming size/SHA-256 verification, atomic promotion, and inventory persistence only after commit; Crosswake never becomes the archive store (D-03, D-04; PACK-03, PACK-04)."
    - "Per-pack install and invalidate operations are serialized and idempotent, status is side-effect-free, and parallel/interrupted work never fabricates availability or destroys a committed winner (D-05; PACK-02)."
    - "A failed replacement preserves last-known-good bytes; when the declaration is newer, those bytes are stale and route-blocking rather than a fallback (D-08)."
    - "Invalidation revokes availability before provider deletion, survives relaunch, remains blocked on deletion failure, and clears only after confirmed invalidation or a fully verified reinstall (D-09)."
  artifacts:
    - path: "examples/ios_shell_host/CrosswakeShell/PronunciationPackProvider.swift"
      provides: "Host-owned foreground reference actor for real staged verification, atomic promotion, private inventory, and deterministic injected failures."
    - path: "examples/ios_shell_host/CrosswakeShellTests/PronunciationPackProviderTests.swift"
      provides: "Example-host XCTest coverage that compiles and exercises the concrete PronunciationPackProvider against the real fixture, filesystem, inventory, relaunch, invalidation, and serialized-race matrix."
    - path: "packages/crosswake-shell-core-ios/Sources/CrosswakeShellCore/PackStore.swift"
      provides: "Persistent revocation intent, stale/known-good semantics, closed recovery, and serialized store coordination."
    - path: "packages/crosswake-shell-core-ios/Tests/CrosswakeShellCoreTests/PackProviderFixtureTests.swift"
      provides: "Fault matrix for corrupt/substituted bytes, atomic replacement, persistence, relaunch, invalidation, and concurrency."
  key_links:
    - from: "CrosswakeShellTests/PronunciationPackProviderTests.swift"
      to: "CrosswakeShell/PronunciationPackProvider.swift"
      via: "@testable import of the example-host module plus direct concrete-provider construction in the shared Xcode test target"
      pattern: "PronunciationPackProviderTests|PronunciationPackProvider"
    - from: "PronunciationPackProvider.install(_:)"
      to: "private provider inventory"
      via: "stage -> stream size/hash -> atomic promotion -> persist ordering"
      pattern: "SHA256|replaceItemAt|inventory"
    - from: "PackStore.invalidatePack"
      to: "persistent revocation ledger"
      via: "revoke-before-provider ordering and fresh status confirmation"
      pattern: "invalidate|revok|invalidationFailed"
    - from: "failed update"
      to: "last-known-good artifact"
      via: "separate staging and no delete-first path"
      pattern: "staging|lastKnown|stale"
  prohibitions:
    - requirement_id: PACK-02
      category: safety
      statement: "MUST NOT let requested, staged, acknowledged, old-version, malformed, revoked, or unreconciled media silently activate a route or downgrade it to a different runtime behavior."
      status: unresolved
      verification: flagged-unverified
    - requirement_id: PACK-04
      category: values
      statement: "MUST NOT turn the provider seam into Crosswake-owned distribution, generic storage, asset lookup, playback policy, or host media-layout authority."
      status: unresolved
      verification: flagged-unverified
    - requirement_id: PACK-05
      category: transparency
      statement: "MUST NOT present local fixture, simulator, package, or generated native results as physical-iPhone, adopter-instance, Android, background-transfer, or generic-storage proof."
      status: unresolved
      verification: flagged-unverified
flagged_assumptions:
  - "[UNRESOLVED-SPEC-PROBE][PACK-01 unclassified] The Plan 01 provider protocol is the complete public seam; this plan adds no provider operation."
  - "[UNRESOLVED-SPEC-PROBE][PACK-02 unclassified] The locked closed reason vocabulary is exhaustive; injected host failures map to it without new free-form cases."
  - "[UNRESOLVED-SPEC-PROBE][PACK-03 unclassified] Foundation replacement is used only if it satisfies the tested same-volume preservation contract; a private POSIX primitive is allowed behind the same tests if the deployment target requires it."
  - "[UNRESOLVED-SPEC-PROBE][PACK-04 concurrency] The reference actor serializes the one-pack host path, and tests prove overlapping install/invalidate calls cannot reorder revocation, promotion, or inventory."
  - "[UNRESOLVED-SPEC-PROBE][PACK-05 unclassified] Host reference storage remains an example integration, not a generic Crosswake storage claim."
---

<objective>
Expand the tracer into the complete host-owned atomic installer, replacement, invalidation, restart, and concurrency contract.

Purpose: Make every corrupt, interrupted, insufficient-storage, replacement, persistence, revocation, and race path preserve an explicit blocked state and known-good bytes.
Output: Reference host provider, real local fixture, persistent core revocation, and exhaustive deterministic fault-injection XCTest.
</objective>

<artifacts_this_phase_produces>
- New host-owned `PronunciationPackProvider` actor with async `status(for:)`, `install(_:)`, and `invalidate(_:)` conformance.
- New private reference-provider helpers for Application Support root creation, unique staging, bounded streaming SHA-256, atomic promote/replace, and the smallest injectable host-private Codable file-backed inventory store with closed persistence failure injection.
- New example-host `Resources/pronunciation-pack-fixture.bin` and Xcode resource membership used only for deterministic local acceptance.
- New example-host `CrosswakeShellTests/PronunciationPackProviderTests.swift` in the existing shared-scheme XCTest target, compiling and exercising the concrete host provider rather than a package-only fake.
- New private persisted `PackRevocationLedger` (or equivalently named internal type) consumed by `PackStore`; it stores opaque pack revocation state only, not digests, bytes, URLs, paths, credentials, or media.
- Expanded `PackProviderFixtureTests` and `PackStoreTests` covering every D-17 storage/lifecycle case.
- No public asset resolver, downloader implementation in Crosswake core, background task, retry loop, storage-budget/eviction API, endpoint, command, flag, Android file, or evidence schema field.
</artifacts_this_phase_produces>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/ADR-FIRST-B2C-ADOPTER.md
@.planning/FIRST-B2C-ADOPTER-ADOPTION-BRIEF.md
@.planning/phases/161-ios-pronunciation-pack-seam/161-CONTEXT.md
@.planning/phases/161-ios-pronunciation-pack-seam/161-RESEARCH.md
@.planning/phases/161-ios-pronunciation-pack-seam/161-PATTERNS.md
@.planning/phases/161-ios-pronunciation-pack-seam/161-01-SUMMARY.md
@packages/crosswake-shell-core-ios/Sources/CrosswakeShellCore/PackProvider.swift
@packages/crosswake-shell-core-ios/Sources/CrosswakeShellCore/PackStore.swift
@examples/ios_shell_host/CrosswakeShell/PermissionStatusProvider.swift

<interfaces>
- Plan 01 creates `PackProvider`, `PackRequirement`, `PackInstalledRecord`, `PackProviderResult`, and `PackFailureReason`; implement these directly and do not widen them for reference-provider internals.
- `PackStore` grants `.available` only from a fresh exact provider status and exposes all other states through the existing `blockingStatus(for:)` activation gate.
- The example host is the correct owner for Application Support URLs, CryptoKit hashing, FileManager/POSIX operations, private inventory, fixture source, and playback policy.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement the host-owned real-byte atomic provider and known-good replacement contract</name>
  <reversibility rating="reversible">The reference provider's private actor, inventory encoding, and filesystem primitive can change behind the already-versioned D-01 protocol.</reversibility>
  <read_first>
    - AGENTS.md
    - .planning/ADR-FIRST-B2C-ADOPTER.md
    - .planning/phases/161-ios-pronunciation-pack-seam/161-CONTEXT.md
    - .planning/phases/161-ios-pronunciation-pack-seam/161-RESEARCH.md
    - .planning/phases/161-ios-pronunciation-pack-seam/161-PATTERNS.md
    - .planning/phases/161-ios-pronunciation-pack-seam/161-01-SUMMARY.md
    - packages/crosswake-shell-core-ios/Sources/CrosswakeShellCore/PackProvider.swift
    - packages/crosswake-shell-core-ios/Tests/CrosswakeShellCoreTests/PackProviderFixtureTests.swift
    - examples/ios_shell_host/CrosswakeShell/PermissionStatusProvider.swift
    - examples/ios_shell_host/CrosswakeShell.xcodeproj/project.pbxproj
    - examples/ios_shell_host/CrosswakeShell.xcodeproj/xcshareddata/xcschemes/CrosswakeShell.xcscheme
  </read_first>
  <files>examples/ios_shell_host/CrosswakeShell/PronunciationPackProvider.swift, examples/ios_shell_host/CrosswakeShellTests/PronunciationPackProviderTests.swift, examples/ios_shell_host/CrosswakeShell/Resources/pronunciation-pack-fixture.bin, examples/ios_shell_host/CrosswakeShell.xcodeproj/project.pbxproj, packages/crosswake-shell-core-ios/Tests/CrosswakeShellCoreTests/PackProviderFixtureTests.swift</files>
  <behavior>
    - "Real fixture install creates a unique staging artifact under the final Application Support volume, streams bytes and CryptoKit SHA-256 off MainActor, compares exact requirement values, atomically promotes, then writes the injectable host-private Codable file-backed inventory."
    - "Wrong size, digest, or version; interrupted transfer; injected insufficient storage, write, promotion, or inventory failure; and substituted bytes return only the matching D-11 reason and never report installed."
    - "A replacement failure at every pre-commit boundary leaves the prior committed artifact and inventory readable; a newer requirement sees it only as stale."
    - "Status performs no write, cleanup, hashing side effect visible to core, or promotion; install and invalidate are idempotent for the same requirement."
  </behavior>
  <action>Implement `PronunciationPackProvider` as a host-owned actor conforming to Plan 01's protocol. Keep all Application Support destinations, fixture/download source, archive bytes, staging names, CryptoKit digest work, atomic FileManager or private POSIX operation, and inventory implementation inside the example host per D-03 and D-04. Implement the resolved research choice as the smallest private `Codable` file-backed inventory store injected into the provider through private load/save dependencies; inject its file location and writer/failure seam for deterministic tests, and store only exact installed-record facts needed for reconciliation. Stage a unique sibling on the destination volume, stream/count/hash without `@MainActor`, compare exact private requirement values without logging, atomically replace or promote only verified bytes, and call the inventory save only after the filesystem commit. Preserve a committed artifact until replacement succeeds; on failure, clean only invocation-owned staging and never delete the known-good destination. Add injectable private seams for interruption, storage/write, promotion, and inventory failures. Create `CrosswakeShellTests/PronunciationPackProviderTests.swift` with `@testable import CrosswakeShell` so the tests instantiate and exercise the actual `examples/ios_shell_host/CrosswakeShell/PronunciationPackProvider.swift`, never a separate provider fake. Use the committed example-host fixture to prove successful exact byte-count/SHA-256 acceptance, rejection of wrong size/digest/substituted bytes, same-volume atomic promotion, inventory persistence followed by a newly constructed provider relaunch, and last-known-good preservation at every failed replacement boundary per D-08 and D-17. Add the test file to the existing `CrosswakeShellTests` PBXSourcesBuildPhase and the fixture to the required resource phase in `project.pbxproj`; preserve the shared `CrosswakeShell` scheme's existing test-target reference so the focused `xcodebuild test` command is executable without manual Xcode membership changes. Keep all assertions and failure output semantic and closed: never print fixture bytes, digest values, URLs, paths, media, staging names, inventory contents, or raw errors. Keep status side-effect-free and the reference actor serialized for this one pack per D-05 and D-08.</action>
  <acceptance_criteria>
    - The reference provider compiles as non-MainActor host code and uses incremental CryptoKit SHA-256 over bounded chunks.
    - The host-private Codable inventory file is absent until atomic promotion completes, is written only after that commit, supports deterministic write failure, and reloads through a newly constructed store/provider.
    - Every injected failure returns its closed reason, removes only owned staging, and leaves prior committed bytes/inventory intact.
    - An old installed version is reported as exact installed fact to core, which maps it stale for a newer requirement; it is never silently used.
    - `CrosswakeShellTests/PronunciationPackProviderTests.swift` compiles the concrete example-host provider and proves the real immutable fixture's exact byte-count/SHA-256 acceptance and rejection, same-volume promotion, new-provider relaunch persistence, and known-good replacement preservation.
    - Xcode project source/resource membership and the shared `CrosswakeShell` scheme run the focused provider XCTest without any manual target-selection step.
    - No provider result, assertion message, log, or UI-facing value contains path, URL, digest, bytes, archive layout, or raw Error.
  </acceptance_criteria>
  <verify><automated>swift test --package-path packages/crosswake-shell-core-ios --filter PackProviderFixtureTests &amp;&amp; xcodebuild -project examples/ios_shell_host/CrosswakeShell.xcodeproj -scheme CrosswakeShell -destination 'platform=iOS Simulator,name=iPhone 16' -only-testing:CrosswakeShellTests/PronunciationPackProviderTests test CODE_SIGNING_ALLOWED=NO</automated></verify>
  <done>The reference host can install or replace one real immutable archive with exact integrity and no delete-first data loss while keeping all transport/storage details outside core.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Make invalidation durable, race-safe, and fail-closed across relaunch</name>
  <reversibility rating="reversible">The private revocation ledger can migrate behind PackStore; the observable D-09 blocked semantics remain fixed.</reversibility>
  <read_first>
    - .planning/phases/161-ios-pronunciation-pack-seam/161-CONTEXT.md
    - packages/crosswake-shell-core-ios/Sources/CrosswakeShellCore/PackProvider.swift
    - packages/crosswake-shell-core-ios/Sources/CrosswakeShellCore/PackStore.swift
    - packages/crosswake-shell-core-ios/Tests/CrosswakeShellCoreTests/PackStoreTests.swift
    - packages/crosswake-shell-core-ios/Tests/CrosswakeShellCoreTests/PackProviderFixtureTests.swift
    - examples/ios_shell_host/CrosswakeShell/PronunciationPackProvider.swift
    - examples/ios_shell_host/CrosswakeShellTests/PronunciationPackProviderTests.swift
    - examples/ios_shell_host/CrosswakeShell.xcodeproj/project.pbxproj
  </read_first>
  <files>packages/crosswake-shell-core-ios/Sources/CrosswakeShellCore/PackStore.swift, packages/crosswake-shell-core-ios/Tests/CrosswakeShellCoreTests/PackStoreTests.swift, packages/crosswake-shell-core-ios/Tests/CrosswakeShellCoreTests/PackProviderFixtureTests.swift, examples/ios_shell_host/CrosswakeShell/PronunciationPackProvider.swift, examples/ios_shell_host/CrosswakeShellTests/PronunciationPackProviderTests.swift, examples/ios_shell_host/CrosswakeShell.xcodeproj/project.pbxproj</files>
  <behavior>
    - "Crosswake persists revocation before invoking provider invalidation and immediately blocks activation even if installed bytes remain."
    - "Provider invalidation success plus fresh status not-installed clears revocation; deletion failure, cancellation, malformed success, or installed status maps invalidation_failed and remains blocked across a new store/provider instance."
    - "A later fully verified install plus fresh matching status clears revocation; an install acknowledgement, old bytes, or partial persistence cannot clear it."
    - "Overlapping same-pack install/install, invalidate/invalidate, and install/invalidate calls execute deterministically, remain idempotent, and never reactivate a revoked pack from an older completion."
  </behavior>
  <action>Add a private persisted revocation ledger to `PackStore` that records only opaque pack ID plus pending/failed revocation state. Follow D-09 ordering exactly: revoke availability and persist intent, call provider invalidate, reconcile fresh status, then clear only on confirmed not-installed; keep `invalidation_failed` blocked on every other result. Permit a later exact verified reinstall to clear revocation only after its fresh status succeeds. Use one per-pack task/actor serialization mechanism in the store so stale async completions cannot overwrite newer install/invalidate intent, and keep operations idempotent per D-05. Extend the host reference provider, package tests, and the actual example-host `PronunciationPackProviderTests` for successful invalidation, failed deletion with bytes remaining, a new provider/store instance after both successful and failed revocation, reinstall recovery, duplicate install/install and invalidate/invalidate calls, serialized install/invalidate races in both enqueue orders, stale-completion fencing, and old-version stale behavior per D-05, D-09, and D-17. The example-host tests must continue to construct `PronunciationPackProvider` directly and use only semantic closed assertions, with no host-private data or raw evidence in test output. Keep the Xcode source membership executable from the shared scheme. Do not add automatic retries, background continuation, generic retention/eviction, or synchronous navigation hashing.</action>
  <acceptance_criteria>
    - Availability is revoked before provider invalidation begins and stays revoked after process reconstruction.
    - Failed invalidation cannot reactivate retained bytes; only confirmed not-installed or a fully verified/reconciled reinstall clears the revocation ledger.
    - Same-requirement repeated mutations are idempotent and parallel opposite operations have a deterministic final blocked/available result consistent with the latest serialized intent.
    - The concrete example-host provider XCTest proves successful and failed invalidation/revocation across newly constructed instances and serialized install/invalidate races without using a test fake.
    - Last-known-good replacement and stale route-blocking tests remain green.
    - No raw provider failure or private storage value escapes the closed status/reason surface.
  </acceptance_criteria>
  <verify><automated>swift test --package-path packages/crosswake-shell-core-ios --filter PackStoreTests &amp;&amp; swift test --package-path packages/crosswake-shell-core-ios --filter PackProviderFixtureTests &amp;&amp; xcodebuild -project examples/ios_shell_host/CrosswakeShell.xcodeproj -scheme CrosswakeShell -destination 'platform=iOS Simulator,name=iPhone 16' -only-testing:CrosswakeShellTests/PronunciationPackProviderTests test CODE_SIGNING_ALLOWED=NO</automated></verify>
  <done>Revocation is durable authority rather than best-effort deletion, and per-pack concurrency cannot resurrect invalid or stale media.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| fixture/download source -> host staging | Untrusted or interrupted bytes enter host-private storage. |
| verified staging -> committed destination | Atomic promotion must preserve the known-good artifact under every failure. |
| provider inventory -> core reconciliation | Inventory persistence ordering must not assert a filesystem commit that did not occur. |
| invalidation intent -> provider deletion/status | Trust revocation must survive failed deletion, cancellation, and relaunch. |
| concurrent host/core operations -> lifecycle | Older completions must not overwrite the latest serialized authority. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-161-05 | Tampering | staged/downloaded bytes | high | mitigate | Task 1 streams exact size and SHA-256 and rejects substituted/corrupt bytes before promotion. |
| T-161-06 | Denial of Service | known-good replacement | high | mitigate | Task 1 uses separate staging and atomic replacement with fault tests preserving the prior commit. |
| T-161-07 | Spoofing / Repudiation | private provider inventory | high | mitigate | Task 1 persists only after promotion and Task 2 requires fresh status before core authority. |
| T-161-08 | Elevation of Privilege | failed invalidation/reactivation | high | mitigate | Task 2 persists revoke-first intent and blocks until confirmed invalidation or verified reinstall. |
| T-161-09 | Tampering / Denial of Service | concurrent install/invalidate | high | mitigate | Task 2 serializes per pack and proves idempotency plus stale-completion fencing. |
| T-161-10 | Information Disclosure | provider failures and filesystem details | high | mitigate | Both tasks return only closed reasons and assert output excludes private values. |
| T-161-SC | Tampering | dependency supply chain | low | accept | No package install or dependency change is planned; only existing Apple SDK frameworks are used. |
</threat_model>

<source_coverage_audit>
| Source | ID | Contract | Plan-set coverage | Status |
|---|---|---|---|---|
| GOAL | Phase 161 | Verified atomic host provider with fail-closed invalid paths. | Tasks 1-2 complete host mutation and revocation behavior. | COVERED |
| REQ | PACK-02 | Every invalid path stays non-available. | Full fault and concurrency matrix. | COVERED |
| REQ | PACK-03 | Immutable archive exact verification and atomic install. | Real host provider plus fixture tests. | COVERED |
| REQ | PACK-04 | Host owns bytes/files/inventory implementation. | Provider remains in example host; core sees closed facts only. | COVERED |
| RESEARCH | Atomic preservation | Stage separately, verify, promote, then persist. | Task 1 ordering and injected faults. | COVERED |
| RESEARCH | Invalidation/concurrency | Revoke first and serialize per pack. | Task 2. | COVERED |
| CONTEXT | D-03, D-04, D-05 | Trusted host boundary, host verification/promotion, serialization/idempotency. | Tasks 1-2. | COVERED |
| CONTEXT | D-08, D-09 | Known-good preservation, stale denial, durable revocation. | Tasks 1-2. | COVERED |
| CONTEXT | D-17 | Deterministic real-byte/fault/relaunch/concurrency XCTest matrix. | Both tasks. | COVERED |
| EDGE | PACK-01..PACK-05 | Spec-less probe rows remain unresolved. | All five flagged assumptions retained. | FLAGGED |
| PROHIBITION | PACK-02, PACK-04, PACK-05 | Bespoke safety/ownership/promotion constraints. | Descriptor-less flagged-unverified entries retained. | FLAGGED |
</source_coverage_audit>

<verification>
Run the full real-byte provider fault matrix and core lifecycle suite. Treat any known-good destruction, inventory-before-promotion path, failed-invalidation reactivation, or nondeterministic concurrent result as a blocking high-severity failure.
</verification>

<success_criteria>
- Real host bytes are verified and atomically promoted off the main actor.
- All storage, integrity, persistence, invalidation, and race failures remain blocked.
- Known-good bytes survive failed update and stale bytes never activate a newer requirement.
- Revocation survives relaunch and clears only through confirmed safe paths.
</success_criteria>

<output>
Create `.planning/phases/161-ios-pronunciation-pack-seam/161-02-SUMMARY.md` when done.
</output>
