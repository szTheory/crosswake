---
phase: 161-ios-pronunciation-pack-seam
plan: "12"
type: execute
wave: 10
depends_on: ["161-11"]
files_modified:
  - examples/ios_shell_host/CrosswakeShell/PronunciationPackProvider.swift
  - examples/ios_shell_host/CrosswakeShellTests/PronunciationPackProviderTests.swift
autonomous: true
gap_closure: true
requirements: [PACK-02, PACK-03, PACK-04]
must_haves:
  truths:
    - "A replacement failure after the committed artifact has been retained restores the exact prior artifact and inventory before returning atomicInstallFailed (D-04, D-08, D-11; PACK-02, PACK-03)."
    - "Immediate status and a newly constructed provider both attest the restored old bytes/record; the attempted requirement remains non-available (D-06, D-07, D-10, D-17; PACK-02, PACK-03)."
    - "The rollback transaction remains serialized and private to the host provider; the public status/install/invalidate seam and Crosswake/host ownership do not widen (D-01, D-05, D-12; PACK-04)."
  artifacts:
    - "path: examples/ios_shell_host/CrosswakeShell/PronunciationPackProvider.swift | provides: rollback-protected publication state covering both promotion moves and inventory persistence."
    - "path: examples/ios_shell_host/CrosswakeShellTests/PronunciationPackProviderTests.swift | provides: deterministic second-move failure plus immediate/relaunch known-good preservation XCTest."
  key_links:
    - "from: PronunciationPackProvider.install(_:) | to: rollbackPublication | via: publication state captured before destination retention and handled by every subsequent thrown move/persistence branch | pattern: publication|retainedArtifact|rollbackPublication"
    - "from: injected staged-to-destination failure | to: fresh provider status | via: restored destination bytes and unchanged prior inventory | pattern: move|atomicInstallFailed|status"
  prohibitions:
    - "requirement_id: PACK-02 | category: safety | statement: MUST NOT let requested, staged, acknowledged, old-version, malformed, revoked, or unreconciled media silently activate a route or downgrade it to a different runtime behavior. | status: unresolved | verification: flagged-unverified"
    - "requirement_id: PACK-04 | category: values | statement: MUST NOT turn the provider seam into Crosswake-owned distribution, generic storage, asset lookup, playback policy, or host media-layout authority. | status: unresolved | verification: flagged-unverified"
    - "requirement_id: PACK-05 | category: transparency | statement: MUST NOT present local fixture, simulator, package, or generated native results as physical-iPhone, adopter-instance, Android, background-transfer, or generic-storage proof. | status: unresolved | verification: flagged-unverified"
flagged_assumptions:
  - "[UNRESOLVED-SPEC-PROBE][PACK-01 unclassified] The provider seam remains exactly status/install/invalidate; this repair adds no provider operation."
  - "[UNRESOLVED-SPEC-PROBE][PACK-02 unclassified] Every publication failure remains non-available under the existing closed D-11 reasons."
  - "[UNRESOLVED-SPEC-PROBE][PACK-03 unclassified] Exact size, pinned SHA-256, same-volume promotion, persistence ordering, rollback, and fresh status are all mandatory."
  - "[UNRESOLVED-SPEC-PROBE][PACK-04 concurrency] The actor owns one serialized publication state; interrupted or overlapping work cannot observe a retained artifact as committed inventory."
  - "[UNRESOLVED-SPEC-PROBE][PACK-05 unclassified] The iOS foreground-only stop list remains exhaustive; no adjacent storage or device behavior is inferred."
---

<objective>
Close WR-01 with a production end-to-end replacement transaction that preserves the last-known-good artifact and inventory when staged publication fails.

Purpose: Make D-08 true at the exact untested second-move boundary and prove the restored pair through immediate and relaunched status.
Output: A private rollback-protected publication state and deterministic XCTest fault coverage.
</objective>

<gap_root_cause>
WR-01 is an execution miss against D-04/D-08/D-17: Plan 161-10 protected inventory-write failure but left the staged-to-live move in the outer catch, after the prior artifact had moved to the retained location. Deferred cleanup then deleted the only known-good bytes. The fix must bring every publication step under one state-aware rollback boundary rather than add another symptom-specific catch.
</gap_root_cause>

## Artifacts this phase produces

- New private `PublicationMover` closure type and injected `publicationMover` initializer field for deterministic move-failure XCTest; the default delegates to the provider's `FileManager.moveItem`.
- New private publication-state tracking inside `install(_:)` that distinguishes no prior artifact, prior artifact retained, replacement promoted, and inventory committed.
- New XCTest `testReplacementPromotionMoveFailureRestoresKnownGoodArtifactAndInventoryAfterRelaunch` (exact final name may follow the existing XCTest naming convention while preserving this behavior).
- New files, public symbols, evidence fields, fixtures, APIs, packages, schemas, UI behavior, Android work, physical-device claims, and external integrations: none.

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/ADR-FIRST-B2C-ADOPTER.md
@.planning/REQUIREMENTS.md
@.planning/phases/161-ios-pronunciation-pack-seam/161-CONTEXT.md
@.planning/phases/161-ios-pronunciation-pack-seam/161-VERIFICATION.md
@.planning/phases/161-ios-pronunciation-pack-seam/161-10-SUMMARY.md
@examples/ios_shell_host/CrosswakeShell/PronunciationPackProvider.swift
@examples/ios_shell_host/CrosswakeShellTests/PronunciationPackProviderTests.swift

<interfaces>
- `PronunciationPackProvider` is an actor implementing the unchanged async `PackProvider` status/install/invalidate contract.
- `install(_:)` currently stages verified bytes, moves the old destination to a unique retained path, moves staging to destination, and then persists inventory.
- `rollbackPublication(destination:retainedArtifact:priorInventoryData:)` already restores a promoted replacement after inventory-write failure; extend the transaction's call coverage rather than moving rollback authority into core.
- `PackProviderResult.failure(.atomicInstallFailed)` is the existing closed result for publication failure; raw filesystem errors and paths must not escape.
</interfaces>
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Preserve known-good publication across the staged-to-live move failure</name>
  <reversibility rating="reversible">The injected mover and publication-state implementation are private host details behind the settled provider protocol and inventory encoding.</reversibility>
  <read_first>
    - AGENTS.md
    - .planning/phases/161-ios-pronunciation-pack-seam/161-CONTEXT.md
    - .planning/phases/161-ios-pronunciation-pack-seam/161-VERIFICATION.md
    - .planning/phases/161-ios-pronunciation-pack-seam/161-10-SUMMARY.md
    - examples/ios_shell_host/CrosswakeShell/PronunciationPackProvider.swift
    - examples/ios_shell_host/CrosswakeShellTests/PronunciationPackProviderTests.swift
  </read_first>
  <files>examples/ios_shell_host/CrosswakeShell/PronunciationPackProvider.swift, examples/ios_shell_host/CrosswakeShellTests/PronunciationPackProviderTests.swift</files>
  <behavior>
    - "RED: install old requirement, inject failure only for the replacement's staging-to-destination move after destination-to-retained succeeds, and observe atomicInstallFailed."
    - "GREEN: the original destination bytes and inventory record survive immediately; status for the old requirement is installed and status for the attempted requirement is non-available."
    - "GREEN after relaunch: a newly constructed provider re-attests the same original bytes/record and denies the attempted requirement."
    - "Existing first-install, successful replacement, digest/size failure, and inventory-persistence rollback tests remain green."
  </behavior>
  <action>Add the deterministic private publication-move seam and write the failing XCTest before changing production behavior. The injected mover must delegate the first destination-to-retained move, throw only on the replacement staging-to-destination move, and allow rollback's retained-to-destination move so the test proves the exact WR-01 sequence rather than a pre-publication rejection. Refactor `install(_:)` so the prior inventory bytes, presence of a prior artifact, and completed publication steps are captured before mutation; once the old destination is retained, every thrown promotion or inventory branch must invoke the same rollback routine before returning a closed result. Make cleanup ownership state-aware so `defer` removes only uncommitted staging/debris and never a retained artifact still required for rollback. On promotion-move failure return `.failure(.atomicInstallFailed)` only after restoration succeeds; if restoration itself fails, preserve any remaining retained bytes for recovery and still fail closed without echoing details. Assert exact prior bytes and inventory through immediate and new-provider `status(for:)` calls. Keep actor serialization, same-volume private paths, exact verification-before-promotion, and post-promotion inventory ordering per D-04, D-05, D-08, D-11, D-12, and D-17. Do not add a generic filesystem transaction API or modify the public PackProvider surface.</action>
  <acceptance_criteria>
    - The fault is injected after the old destination has moved to retained storage and before staging becomes live.
    - The operation returns atomicInstallFailed and the old artifact/inventory pair is restored exactly.
    - Immediate and relaunched status report the old requirement installed only after byte attestation; the attempted requirement is non-available.
    - All existing provider tests pass and no raw error, path, byte, digest, inventory content, or private identifier reaches output/evidence.
  </acceptance_criteria>
  <verify><automated>xcodebuild clean test -project examples/ios_shell_host/CrosswakeShell.xcodeproj -scheme CrosswakeShell -derivedDataPath .build/phase161-publication-move-rollback -destination 'platform=iOS Simulator,name=iPhone 17' -only-testing:CrosswakeShellTests/PronunciationPackProviderTests CODE_SIGNING_ALLOWED=NO</automated></verify>
  <done>WR-01 is closed by deterministic XCTest proving that the exact second publication move failure preserves and re-attests the committed prior artifact/inventory pair across relaunch.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| staged verified bytes -> live destination | A filesystem throw after retaining the winner must not destroy or replace committed authority. |
| live artifact -> durable inventory | Bytes and metadata must roll forward or back as one host-private pair. |
| host filesystem failures -> provider result | Private paths/errors collapse to the closed atomicInstallFailed reason. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-161-58 | Tampering | replacement publication | high | mitigate | Task 1 makes every post-retention move/persistence failure enter one tested rollback boundary. |
| T-161-59 | Denial of Service | known-good artifact | high | mitigate | Task 1 proves exact prior bytes/inventory survive immediate and relaunched status after second-move failure. |
| T-161-60 | Information Disclosure | filesystem failure handling | medium | mitigate | Task 1 returns closed results only and retains no raw path/error/integrity data. |
| T-161-61 | Elevation of Privilege | attempted replacement inventory | medium | mitigate | Fresh status must re-attest restored old bytes; the attempted requirement never becomes available. |
| T-161-SC | Tampering | package supply chain | low | accept | No package-manager install or dependency change occurs. |
</threat_model>

<verification>
Run the clean focused host XCTest suite. Inspect the diff to confirm the only production authority change is private publication rollback and that the public PackProvider contract is byte-identical.
</verification>

<success_criteria>
- WR-01's exact second-move failure is deterministic and red-before-green under XCTest.
- Every publication failure after prior-artifact retention restores or preserves the last-known-good pair and remains non-available.
- Immediate and relaunched provider status prove restored bytes rather than trusting inventory alone.
- Privacy, foreground-only ownership, Android freeze, and Phase 162 device boundary remain intact.
</success_criteria>

<output>
Create `.planning/phases/161-ios-pronunciation-pack-seam/161-12-SUMMARY.md` when done.
</output>
