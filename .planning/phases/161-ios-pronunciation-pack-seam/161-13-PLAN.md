---
phase: 161-ios-pronunciation-pack-seam
plan: "13"
type: execute
wave: 10
depends_on: ["161-11"]
files_modified:
  - priv/templates/crosswake/proof_lane/ios/ProofLaneDriver.swift.eex
  - priv/templates/crosswake/proof_lane/ios/CrosswakeProofLaneTests/ProofLaneContractTests.swift.eex
  - priv/templates/crosswake/proof_lane/ios/CrosswakeProofLaneUITests/ProofLaneUITests.swift.eex
  - script/verify_generated_ios_shell.sh
  - test/crosswake/proof_lane/template_contract_test.exs
  - test/crosswake/proof_lane/ios_verifier_test.exs
autonomous: true
gap_closure: true
requirements: [PACK-01, PACK-03, PACK-04, PACK-05]
must_haves:
  truths:
    - "Generated advisory proof performs an actual URLSession pronunciation request through a deny-only URLProtocol, observes notConnectedToInternet, and only then proves installed local audio bytes (D-16, D-18, D-19; PACK-03, PACK-04)."
    - "The structured XCTest evidence is emitted only from the observed denied-network plus installed-byte operation; neither a Boolean nor an unconsumed environment marker can satisfy it (D-17, D-18, D-20)."
    - "The shell verifier requires the exact honest schema and rejects the old unconditional networking_disabled document, missing denial evidence, reordered/extra fields, and generic build/test success (D-20, D-21; PACK-05)."
    - "Default generated iOS remains blocked/unavailable and reference-adapter success remains simulator-advisory; Phase 162 alone owns physical-iPhone promotion (D-20, D-21; PACK-05)."
  artifacts:
    - "path: priv/templates/crosswake/proof_lane/ios/ProofLaneDriver.swift.eex | provides: deterministic deny-only URLSession transport and host-owned installed-audio operation."
    - "path: priv/templates/crosswake/proof_lane/ios/CrosswakeProofLaneTests/ProofLaneContractTests.swift.eex | provides: observation-bound structured evidence producer and negative network-dependent regression."
    - "path: script/verify_generated_ios_shell.sh | provides: exact schema gate for honest denied-network/local-audio advisory evidence."
    - "path: test/crosswake/proof_lane/ios_verifier_test.exs | provides: real generation/verifier schema rejection and advisory pass coverage."
  key_links:
    - "from: ProofLaneReferencePackAdapter.exerciseInstalledPronunciationAudioOffline() | to: deny-only URLSession | via: an actual data request whose URLProtocol completes with URLError.notConnectedToInternet | pattern: URLSession|URLProtocol|notConnectedToInternet"
    - "from: observed operation result | to: ProofLaneEvidenceDocument | via: assertion IDs returned by the exercised adapter path rather than appended unconditionally | pattern: network_operation_denied|installed_audio_read"
    - "from: generated XCTest transcript | to: pack_audio_prerequisite | via: exact schema comparison in verify_generated_ios_shell.sh | pattern: schema_version|assertion_ids|PL-IOS-PACK-AUDIO-ADVISORY"
  prohibitions:
    - "requirement_id: PACK-02 | category: safety | statement: MUST NOT let requested, staged, acknowledged, old-version, malformed, revoked, or unreconciled media silently activate a route or downgrade it to a different runtime behavior. | status: unresolved | verification: flagged-unverified"
    - "requirement_id: PACK-04 | category: values | statement: MUST NOT turn the provider seam into Crosswake-owned distribution, generic storage, asset lookup, playback policy, or host media-layout authority. | status: unresolved | verification: flagged-unverified"
    - "requirement_id: PACK-05 | category: transparency | statement: MUST NOT present local fixture, simulator, package, or generated native results as physical-iPhone, adopter-instance, Android, background-transfer, or generic-storage proof. | status: unresolved | verification: flagged-unverified"
flagged_assumptions:
  - "[UNRESOLVED-SPEC-PROBE][PACK-01 unclassified] Generated proof consumes the existing host-owned proof operation and adds no public provider transport member."
  - "[UNRESOLVED-SPEC-PROBE][PACK-02 unclassified] Failed or malformed denial/audio operations remain blocked under existing closed outcomes."
  - "[UNRESOLVED-SPEC-PROBE][PACK-03 unclassified] Exact verified installed bytes and an observed denied request are both mandatory for advisory pass."
  - "[UNRESOLVED-SPEC-PROBE][PACK-04 concurrency] Generated foreground install/relaunch/audio operations stay serial and process-scoped; the deny-only session cannot create background authority."
  - "[UNRESOLVED-SPEC-PROBE][PACK-05 unclassified] Simulator proof remains advisory and the complete iOS-only foreground stop list remains unclaimed."
---

<objective>
Close CR-01 by replacing fabricated network-disabled evidence with an actual denied-network pronunciation operation and an exact observation-bound verifier schema.

Purpose: Make generated advisory proof honest without widening the public PackProvider or claiming physical-device evidence.
Output: Deny-only generated transport, operation-derived XCTest evidence, exact verifier regressions, and an advisory simulator run.
</objective>

<gap_root_cause>
CR-01 is an execution miss against D-18/D-20/D-21: Plan 161-08 introduced a Boolean and environment marker, then Plan 161-11 removed the marker consumption while leaving `networking_disabled` unconditionally emitted and accepted. The repair must make the denial an observed result of the same host-owned audio operation and bind the schema to that result.
</gap_root_cause>

## Artifacts this phase produces

- New internal `ProofLaneDenyingURLProtocol` URL-loading interceptor that services no request and completes with `URLError.notConnectedToInternet`.
- New internal `ProofLaneAudioNetworkObservation` closed enum (`denied` / `unexpectedSuccess` / `failed`) and injected/private URLSession used by the reference adapter.
- Changed `exerciseInstalledPronunciationAudioOffline()` evidence path: actual denied request observed first, exact installed bytes read second.
- Structured evidence schema version `2`, replacing `networking_disabled` with `network_operation_denied`; the exact allowlisted IDs remain process-scoped transcript evidence.
- New generated XCTest negative case proving an unexpected transport success or non-denial failure blocks audio proof and emits no passing evidence.
- New ExUnit verifier fixtures for legacy-schema rejection, missing/extra/reordered denial evidence rejection, exact schema acceptance, and real generated-template assertions.
- New files, public provider operations, external APIs/services, retained native artifacts, evidence values beyond stable IDs/closed outcomes, Android work, background transfer, and physical-device claims: none.

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/ADR-FIRST-B2C-ADOPTER.md
@.planning/REQUIREMENTS.md
@.planning/phases/161-ios-pronunciation-pack-seam/161-CONTEXT.md
@.planning/phases/161-ios-pronunciation-pack-seam/161-VERIFICATION.md
@.planning/phases/161-ios-pronunciation-pack-seam/161-08-SUMMARY.md
@.planning/phases/161-ios-pronunciation-pack-seam/161-11-SUMMARY.md
@priv/templates/crosswake/proof_lane/ios/ProofLaneDriver.swift.eex
@priv/templates/crosswake/proof_lane/ios/CrosswakeProofLaneTests/ProofLaneContractTests.swift.eex
@priv/templates/crosswake/proof_lane/ios/CrosswakeProofLaneUITests/ProofLaneUITests.swift.eex
@script/verify_generated_ios_shell.sh
@test/crosswake/proof_lane/template_contract_test.exs
@test/crosswake/proof_lane/ios_verifier_test.exs

<interfaces>
- `ProofLaneHostAdapter.exerciseInstalledPronunciationAudioOffline()` is the narrow D-19 host-owned operation and remains the only generated audio seam.
- `ProofLaneReferencePackAdapter` already installs and re-attests exact Application Support fixture bytes; it must not expose asset paths or layout.
- `ProofLaneContractTests.emitStructuredEvidence()` currently prints schema version 1 with six IDs, including an unconditional `networking_disabled` ID.
- `verify_generated_ios_shell.sh` captures the XCTest transcript and accepts one exact JSON document before emitting the closed advisory `pack_audio_prerequisite` result.
- Generated default adapter selection remains nil; only explicit `--reference-pack-adapter` may exercise this simulator-advisory path.
</interfaces>
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Observe denied network transport before installed-audio advisory proof</name>
  <reversibility rating="reversible">The deny-only URL-loading test transport and evidence schema are generated proof internals; changing them does not migrate public provider or persisted host data.</reversibility>
  <read_first>
    - AGENTS.md
    - .planning/phases/161-ios-pronunciation-pack-seam/161-CONTEXT.md
    - .planning/phases/161-ios-pronunciation-pack-seam/161-VERIFICATION.md
    - .planning/phases/161-ios-pronunciation-pack-seam/161-08-SUMMARY.md
    - .planning/phases/161-ios-pronunciation-pack-seam/161-11-SUMMARY.md
    - priv/templates/crosswake/proof_lane/ios/ProofLaneDriver.swift.eex
    - priv/templates/crosswake/proof_lane/ios/CrosswakeProofLaneTests/ProofLaneContractTests.swift.eex
    - priv/templates/crosswake/proof_lane/ios/CrosswakeProofLaneUITests/ProofLaneUITests.swift.eex
    - script/verify_generated_ios_shell.sh
    - test/crosswake/proof_lane/template_contract_test.exs
    - test/crosswake/proof_lane/ios_verifier_test.exs
  </read_first>
  <files>priv/templates/crosswake/proof_lane/ios/ProofLaneDriver.swift.eex, priv/templates/crosswake/proof_lane/ios/CrosswakeProofLaneTests/ProofLaneContractTests.swift.eex, script/verify_generated_ios_shell.sh, test/crosswake/proof_lane/template_contract_test.exs, test/crosswake/proof_lane/ios_verifier_test.exs</files>
  <behavior>
    - "RED: the old six-ID schema and an adapter whose request unexpectedly succeeds are rejected; neither can produce advisory pass."
    - "GREEN: the reference adapter issues one URLSession request intercepted by ProofLaneDenyingURLProtocol, observes notConnectedToInternet, then reads/re-attests installed audio bytes and returns passed."
    - "GREEN: structured evidence schema version 2 contains exact operation-derived IDs including network_operation_denied and installed_audio_read; missing, extra, duplicate, reordered, or legacy IDs fail closed."
    - "GREEN: actual generation/template tests and the explicit reference-adapter simulator verifier pass, while untouched/default generation remains blocked or unavailable."
  </behavior>
  <action>Start by changing ExUnit and generated XCTest expectations so the current unconditional document fails. In `ProofLaneDriver.swift.eex`, remove the unused `ProofLanePackEvidence.networkingDisabled` Boolean and build an internal ephemeral URLSession whose only protocol class is `ProofLaneDenyingURLProtocol`; the interceptor must reject every request locally with `URLError.notConnectedToInternet`, never contact a host, and expose no requested URL. Make the host-owned offline-audio operation issue a bounded pronunciation request through that session, require the exact denial code, and only then reopen/re-attest the installed fixture and perform its bounded audio-byte read. Inject a session/observation seam into tests so an unexpected success and a different failure deterministically remain blocked. Return operation evidence from the exercised path (or an equivalent private result consumed immediately by XCTest) so `emitStructuredEvidence` cannot append a denial ID independently of behavior. Change the exact schema to version 2 and replace the old assertion with `network_operation_denied`; update the verifier's exact document and shim fixtures to reject the legacy schema, missing denial, extra/duplicate/reordered IDs, generic test success, and any evidence not produced by the named contract test. Remove `CROSSWAKE_PROOF_LANE_NETWORK_DISABLED` from the shell launch because it is no longer authority; template tests must require the deny-only URLSession flow and prohibit the obsolete Boolean/marker path. Run real generator/template tests and the explicit simulator command below. Keep all emitted/retained data limited to stable assertion IDs and closed outcomes per D-12/D-21, retain default non-pass, and preserve D-19's no-public-asset-resolution boundary.</action>
  <acceptance_criteria>
    - A real URLSession data operation runs and is deterministically denied locally with notConnectedToInternet before installed audio can pass.
    - Unexpected network success, another transport error, absent installed bytes, or corrupt bytes each remain blocked and produce no passing structured evidence.
    - The verifier requires exact schema version 2 and operation-derived denial/audio IDs; the old networking_disabled document is rejected.
    - Real template/generation tests and the reference-adapter simulator command pass; default generated proof remains non-passing and advisory/device boundaries are unchanged.
    - No network request escapes the deny-only URLProtocol and no URL, path, digest, media, raw transcript, credential, account/token, or stable device identifier enters retained evidence.
  </acceptance_criteria>
  <verify><automated>mix test test/crosswake/proof_lane/template_contract_test.exs test/crosswake/proof_lane/ios_verifier_test.exs test/crosswake/proof_lane/config_test.exs &amp;&amp; (default_output=$(bash script/verify_generated_ios_shell.sh --proof-lane 2&gt;&amp;1); default_status=$?; test "$default_status" -eq 2 -o "$default_status" -eq 3; printf '%s\n' "$default_output" | grep -Eq '"outcome":"(blocked|unavailable)"') &amp;&amp; reference_output=$(env -u CROSSWAKE_IOS_XCODEBUILD_BIN -u CROSSWAKE_IOS_PROJECT_ROOT -u CROSSWAKE_IOS_SHIM_MODE CROSSWAKE_IOS_USE_LOCAL_CORE=1 CROSSWAKE_IOS_LAUNCH_SIMULATOR=1 bash script/verify_generated_ios_shell.sh --proof-lane --reference-pack-adapter) &amp;&amp; printf '%s\n' "$reference_output" | grep -Eq '"outcome":"passed"' &amp;&amp; printf '%s\n' "$reference_output" | grep -Eq '"scope":"pack_audio_prerequisite"'</automated></verify>
  <done>CR-01 is closed by generated proof that observes an actual locally denied URLSession request and exact installed audio bytes before the advisory schema can pass.</done>
</task>

<task type="auto">
  <name>Task 2: Remove the obsolete UI launch marker and retain accessibility-only process proof</name>
  <reversibility rating="reversible">The generated UI-test launch environment and source-contract assertion are local proof scaffolding with no public or persisted contract.</reversibility>
  <read_first>
    - .planning/phases/161-ios-pronunciation-pack-seam/161-CONTEXT.md
    - .planning/phases/161-ios-pronunciation-pack-seam/161-UI-SPEC.md
    - priv/templates/crosswake/proof_lane/ios/CrosswakeProofLaneUITests/ProofLaneUITests.swift.eex
    - test/crosswake/proof_lane/template_contract_test.exs
  </read_first>
  <files>priv/templates/crosswake/proof_lane/ios/CrosswakeProofLaneUITests/ProofLaneUITests.swift.eex, test/crosswake/proof_lane/template_contract_test.exs</files>
  <action>Remove the obsolete network-disabled launch environment assignment from the generated XCUITest. Keep XCUITest limited to D-18's accessibility-visible install, terminate/relaunch, and audio action; the denial observation remains inside Task 1's host-owned operation and XCTest evidence. Strengthen `template_contract_test.exs` so generated UI source cannot reintroduce the obsolete environment marker and still retains the reference-adapter selection, stable accessibility identifiers, relaunch, Dynamic Type, scrolling, and 44-point target checks. Do not add filesystem, network, transcript, or hidden test-authority inspection to XCUITest.</action>
  <acceptance_criteria>
    - Generated XCUITest no longer sets or claims a network-disabled environment marker.
    - XCUITest retains the existing install/relaunch/audio interaction through stable accessibility identifiers only.
    - Template tests enforce marker absence and all approved UI-SPEC accessibility/backstop contracts remain covered.
  </acceptance_criteria>
  <verify><automated>mix test test/crosswake/proof_lane/template_contract_test.exs</automated></verify>
  <done>The generated UI proof no longer carries a non-authoritative marker and remains an accessibility-only consumer of the operation proved by Task 1.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| generated audio operation -> URL-loading system | The proof must observe deterministic denial without allowing external egress. |
| adapter operation -> XCTest evidence | Assertion IDs must derive from executed behavior, not test-authored claims. |
| XCTest transcript -> shell verifier | Only one exact privacy-safe schema may promote the advisory prerequisite. |
| simulator -> Phase 162 truth | Local advisory success cannot become physical-device/adopter promotion. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-161-62 | Spoofing | networking-denied evidence | high | mitigate | Task 1 binds the stable denial ID to an actual URLSession operation completing with notConnectedToInternet. |
| T-161-63 | Information Disclosure | denied request/transcript | high | mitigate | Deny-only URLProtocol prevents egress; verifier retains only stable IDs and closed outcome. |
| T-161-64 | Tampering | structured evidence schema | high | mitigate | Exact schema/version/order validation rejects legacy, missing, extra, duplicate, and generic-success documents. |
| T-161-65 | Repudiation | simulator advisory claim | high | mitigate | Task 1 reruns default non-pass and labels reference success only as advisory pack_audio_prerequisite. |
| T-161-66 | Denial of Service | network-dependent audio path | medium | mitigate | Negative XCTest proves unexpected success/non-denial cannot substitute for installed local bytes. |
| T-161-SC | Tampering | package supply chain | low | accept | No package-manager install or dependency change occurs. |
</threat_model>

<verification>
Run focused ExUnit generation/verifier tests, then run both the default closed non-pass and explicit reference-adapter advisory simulator command. Inspect generated Swift to confirm URLProtocol is the sole session transport and the public provider seam is unchanged.
</verification>

<success_criteria>
- CR-01's claimed condition is observed by a real denied URLSession operation, not a Boolean, environment marker, or unconditional test string.
- Exact schema tests fail closed for every dishonest or malformed evidence shape.
- Real generation and simulator-advisory proof pass with default generation remaining non-passing.
- No external egress/API, raw evidence leakage, physical-device claim, Android work, or provider/public asset API is introduced.
</success_criteria>

<output>
Create `.planning/phases/161-ios-pronunciation-pack-seam/161-13-SUMMARY.md` when done.
</output>
