---
phase: 161-ios-pronunciation-pack-seam
plan: "16"
type: execute
wave: 13
depends_on: ["161-15"]
files_modified:
  - examples/ios_shell_host/CrosswakeShell/RequiredPackView.swift
  - examples/ios_shell_host/CrosswakeShellTests/RequiredPackViewTests.swift
  - script/verify_generated_ios_shell.sh
  - test/crosswake/proof_lane/ios_verifier_test.exs
  - .planning/phases/161-ios-pronunciation-pack-seam/161-VALIDATION.md
autonomous: true
gap_closure: true
requirements: [PACK-01, PACK-02, PACK-03, PACK-04, PACK-05]
must_haves:
  truths:
    - "One fresh same-tree gate executes construction-started retained-old, promoted-new/inventory-pending, and inventory-committed recovery; in-flight status awaiting; repeated-status snapshot invariance; and memoized bootstrap failure/no-mutation tests before PACK-03 closure (D-04 through D-08, D-17; PACK-03)."
    - "All Swift core, clean reference-host XCTest, approved UI, proof-lane/evidence, Phase 160 privacy/authority, Sigra, Phoenix, and browser preservation gates pass on the post-161-15 tree (PACK-01 through PACK-05)."
    - "Retained validation contains only stable IDs, current HEAD, the unchanged exact six-file Plan 15/16 subject digest, aggregate counts, and closed outcomes—including construction bootstrap/status purity, storage-root volume-anchor shapes, recovery-before-invalidate, and durability ordering; it contains no raw media, answers, transcripts, credentials, identifiers, filesystem locations, digests, logs, screenshots, or native artifacts (D-12, D-21)."
    - "The exact subject set—PronunciationPackProvider.swift, PronunciationPackProviderTests.swift, RequiredPackView.swift, RequiredPackViewTests.swift, verify_generated_ios_shell.sh, and ios_verifier_test.exs—has the same content digest before execution and after validation publication; unrelated repository changes, including .planning/config.json, do not enter the digest or block the gate."
    - "Every generated-shell xcodebuild invocation, including -list and -showdestinations discovery as well as build/test execution, receives the same caller-supplied private DerivedData path under one run root; all raw discovery/test captures and temporary files stay under that trap-cleaned root, and automated success/failure tests prove cleanup."
    - "Default generated iOS remains non-passing, reference-adapter output remains simulator-advisory, TODO-002 remains unknown_blocking, and Phase 162 alone owns physical-iPhone promotion (D-18 through D-21; PACK-05)."
    - "The UI recovery surface still renders explicit empty/loading/error/ready states, one permitted foreground action, scrollable wrapping content, privacy-safe developer context, stable accessibility identifiers, reduced-motion-safe transitions, and no unexplained blank state (D-13 through D-15)."
    - "A hosted accessibility-XXXL XCTest verifies recovery copy wraps inside the scrollable viewport and every required control can be scrolled into view with a 44pt minimum target."
    - "A hosted accessibility-XXXL XCTest verifies the combined lifecycle status remains untruncated, announces its full label/message, and issues an explicit preserve-focus directive rather than moving VoiceOver focus."
    - "A hosted accessibility-XXXL XCTest verifies Install, Update, Retry, and Invalidate labels wrap or reflow without ellipsis and remain reachable at 44pt minimum size."
    - "A hosted accessibility-XXXL XCTest verifies owner, stable rule, route, runtime, and remediation accessibility text remains complete/wrapped while an exclusion scan rejects sensitive diagnostic fields."
    - "The existing no-external-API declaration remains unchanged and no API matrix, ORM/schema work, Android/background/delta/eviction/generic-storage/scoring/capture/dashboard/support work is added (PACK-04, PACK-05)."
  artifacts:
    - "path: examples/ios_shell_host/CrosswakeShell/RequiredPackView.swift | provides: testable full-state lifecycle announcement with an explicit preserve-focus effect and unchanged stable accessibility surface."
    - "path: examples/ios_shell_host/CrosswakeShellTests/RequiredPackViewTests.swift | provides: hosted accessibility-XXXL geometry, wrapping, reachability, minimum-target, announcement/focus, and developer-context backstops for UI-SPEC E1-E4."
    - "path: script/verify_generated_ios_shell.sh | provides: caller-supplied/run-scoped private DerivedData, SourcePackages, TMPDIR, and raw-capture ownership for every discovery/build/test xcodebuild invocation with one EXIT cleanup trap."
    - "path: test/crosswake/proof_lane/ios_verifier_test.exs | provides: invocation-argument and success/failure cleanup tests for private -list/-showdestinations/build/test execution."
    - "path: .planning/phases/161-ios-pronunciation-pack-seam/161-VALIDATION.md | provides: fresh post-161-15 aggregate-only schema-3 evidence seal with crash-recovery, UI, privacy, API, and non-promotion outcomes."
    - "path: .planning/phases/161-ios-pronunciation-pack-seam/COVERAGE.md | provides: preserved no-external-API declaration; verified but not modified."
    - "ephemeral: private mktemp run directory | provides: trap-cleaned Xcode DerivedData, raw suite logs, and allowlisted final-run manifest retained only until validation-to-manifest equality succeeds."
  key_links:
    - "from: 161-15 restart-state XCTest | to: PACK-03 validation | via: clean reference-host test execution on the same final tree | pattern: retained|inventoryPending|inventoryCommitted|recovery"
    - "from: generated reference-adapter proof | to: validation | via: exact advisory schema and observed denied-network plus installed-byte assertions | pattern: network_operation_denied|installed_audio_read|pack_audio_prerequisite"
    - "from: hosted accessibility-XXXL UI backstops | to: 161-UI-SPEC E1-E4 | via: RequiredPackViewTests layout/accessibility-tree probes and stable identifiers | pattern: required-pack-status|required-pack-primary-action|required-pack-invalidate-action|required-pack-owner"
    - "from: RequiredPackView lifecycle change | to: VoiceOver | via: full combined announcement plus explicit preserve-focus effect | pattern: LifecycleAccessibilityEffect|announcement|preserveFocus"
    - "from: fresh gate observations | to: 161-VALIDATION.md | via: numeric schema 3 plus stable IDs, aggregate counts, and closed outcomes only; the parsed retained payload must equal the private schema-3 manifest | pattern: schema|passed|blocked|unavailable|cmp"
    - "from: named provider XCTest markers | to: retained validation equality | via: exact `invalidate_recovery_outcomes` and `durability_outcomes` key sets whose values enter the same manifest and extracted payload comparison | pattern: invalidate_recovery_outcomes|durability_outcomes|cmp"
    - "from: construction/bootstrap and volume-anchor XCTest markers | to: retained validation equality | via: exact `bootstrap_outcomes` and `volume_anchor_outcomes` objects proving D-05 status purity plus retained-old/first-install absent-destination handling | pattern: bootstrap_outcomes|volume_anchor_outcomes|cmp"
    - "from: generated-shell verifier entry point | to: every xcodebuild discovery/build/test invocation | via: one caller-supplied run root whose DerivedData path is passed to -list, -showdestinations, build-for-testing, and test-without-building and whose raw capture paths are trap-cleaned | pattern: CROSSWAKE_IOS_RUN_ROOT|derivedDataPath|cleanup"
    - "from: exact unchanged six-file subject set | to: manifest and retained validation | via: path-delimited Git blob hashes folded into one SHA-256 before execution and rechecked after publication; no composition-root edit is needed | pattern: subject_tree_sha256|phase161-plans15-16-v3"
  prohibitions:
    - "requirement_id: PACK-02 | category: safety | statement: MUST NOT let requested, staged, acknowledged, old-version, malformed, revoked, crash-stranded, or unreconciled media silently activate a route or downgrade it to a different runtime behavior. | status: unresolved | verification: flagged-unverified"
    - "requirement_id: PACK-04 | category: values | statement: MUST NOT turn the provider seam or its recovery journal into Crosswake-owned distribution, generic storage, asset lookup, playback policy, or host media-layout authority. | status: unresolved | verification: flagged-unverified"
    - "requirement_id: PACK-05 | category: transparency | statement: MUST NOT present local fixture, simulator, package, or generated native results as physical-iPhone, adopter-instance, Android, background-transfer, or generic-storage proof. | status: unresolved | verification: flagged-unverified"
flagged_assumptions:
  - "[UNRESOLVED-SPEC-PROBE][PACK-01 unclassified] Final closure verifies only the established foreground status/install/invalidate seam and existing proof operation."
  - "[UNRESOLVED-SPEC-PROBE][PACK-02 unclassified] All unrecognized journal, publication, provider, and generated-operation states remain closed and non-available."
  - "[UNRESOLVED-SPEC-PROBE][PACK-03 unclassified] Exact integrity, fsynced transaction ordering, recoverable inventory commit, fresh status, and real-byte proof are jointly mandatory."
  - "[UNRESOLVED-SPEC-PROBE][PACK-04 concurrency] Final gates prove one construction-time recovery task, side-effect-free status awaiting that existing barrier, memoized failure, and actor serialization after successful bootstrap."
  - "[UNRESOLVED-SPEC-PROBE][PACK-05 unclassified] The foreground iOS stop list is exhaustive and no local validation promotes an adjacent platform, product, or device claim."
---

<objective>
Run a fresh same-tree Phase 161 gate after crash-recovery repair and retain only privacy-safe aggregate evidence.

Purpose: Replace the invalid pre-recovery seal with direct proof that publication survives process interruption while every settled provider, UI, privacy, ownership, advisory, and no-external-API boundary remains intact.
Output: Executable RequiredPackView accessibility-XXXL backstops plus an updated `161-VALIDATION.md`; no support-truth, requirement-status, Phase 162, product-surface, or API declaration change.
</objective>

<gap_root_cause>
`161-VERIFICATION.md` remains `gaps_found` because the previous final gate covered thrown failures but not durable restart recovery. Plan 161-15 changes the transaction authority and test surface, so Phase 161 cannot close from the earlier 161-14 counts or validation text; the repaired tree must execute the exact seeded interruption states and all preservation gates together.
</gap_root_cause>

## Artifacts this phase produces

- Updated `.planning/phases/161-ios-pronunciation-pack-seam/161-VALIDATION.md` with a fresh date, aggregate counts, crash-recovery outcomes, PACK-01 through PACK-05 coverage, T-161-72 through T-161-81 disposition, UI consideration coverage/backstops, and unchanged non-promotion boundaries.
- Expanded `examples/ios_shell_host/CrosswakeShellTests/RequiredPackViewTests.swift` with four named hosted accessibility-XXXL tests covering UI-SPEC E1-E4, backed by a narrow internal lifecycle-announcement effect in `RequiredPackView.swift`.
- Preserved `.planning/phases/161-ios-pronunciation-pack-seam/COVERAGE.md` byte-for-byte as the accepted no-external-API declaration.
- Updated generated-shell verifier and ExUnit contract tests proving one caller-supplied/run-scoped private DerivedData/TMPDIR/raw-capture root is passed through discovery, build, and test paths and removed on success or failure.
- One ephemeral exact-schema manifest publishing numeric `schema: 3` inside the private mktemp run directory, deleted only after the retained validation payload is parsed, required to carry schema 3, and proved field-for-field equal; all raw logs and DerivedData are removed by the same EXIT trap.
- New public symbols, fixtures, evidence fields, APIs, packages, schemas, product states/actions/copy, Android artifacts, support claims, physical-device evidence, and retained raw native artifacts: none.

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/ADR-FIRST-B2C-ADOPTER.md
@.planning/FIRST-B2C-ADOPTER-ADOPTION-BRIEF.md
@.planning/FIRST-B2C-ADOPTER-ROUTE-POLICY-MAP.md
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/161-ios-pronunciation-pack-seam/161-CONTEXT.md
@.planning/phases/161-ios-pronunciation-pack-seam/161-UI-SPEC.md
@.planning/phases/161-ios-pronunciation-pack-seam/161-VALIDATION.md
@.planning/phases/161-ios-pronunciation-pack-seam/161-VERIFICATION.md
@.planning/phases/161-ios-pronunciation-pack-seam/161-14-SUMMARY.md
@.planning/phases/161-ios-pronunciation-pack-seam/161-15-SUMMARY.md
@.planning/phases/161-ios-pronunciation-pack-seam/COVERAGE.md
@examples/ios_shell_host/CrosswakeShell/RequiredPackView.swift
@examples/ios_shell_host/CrosswakeShellTests/RequiredPackViewTests.swift
@script/verify_generated_ios_shell.sh
@test/crosswake/proof_lane/ios_verifier_test.exs

<interfaces>
- `161-VERIFICATION.md` is the authoritative one-gap source and requires durable transaction/recovery plus three deterministic restart states.
- `161-15-SUMMARY.md` must identify the concrete journal ordering, recovery policy, and exact retained-old/promoted-new/inventory-committed tests.
- `COVERAGE.md` remains the accepted one-line no-external-API declaration and must not be rewritten as a matrix.
- The UI-SPEC has 12 covered, 4 automated-backstop, 2 dismissed, and 0 unresolved considerations; the four backstops require new hosted accessibility-XXXL XCTest rather than inference from presentation-only tests.
- `verify_generated_ios_shell.sh` currently gives build/test a private DerivedData path but omits it from `-list` and both `-showdestinations` calls, and its discovery/transcript captures can be allocated outside the final gate's run root.
</interfaces>
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Make all four UI-SPEC large-Dynamic-Type backstops executable</name>
  <reversibility rating="reversible">The internal announcement effect and hosted test helpers preserve the settled host UI contract and can change without a public API migration.</reversibility>
  <read_first>
    - AGENTS.md
    - .planning/phases/161-ios-pronunciation-pack-seam/161-CONTEXT.md
    - .planning/phases/161-ios-pronunciation-pack-seam/161-UI-SPEC.md
    - examples/ios_shell_host/CrosswakeShell/RequiredPackView.swift
    - examples/ios_shell_host/CrosswakeShellTests/RequiredPackViewTests.swift
  </read_first>
  <files>examples/ios_shell_host/CrosswakeShell/RequiredPackView.swift, examples/ios_shell_host/CrosswakeShellTests/RequiredPackViewTests.swift</files>
  <behavior>
    - "RED E1: `testAccessibilityXXXLRecoveryCopyWrapsAndControlsRemainReachable` hosts a route-blocking recovery state at accessibilityExtraExtraExtraLarge in a 320x568 viewport, proves the full learner copy is untruncated, proves a UIScrollView owns overflow, scrolls the required control into the visible bounds, and measures at least 44pt in both hit-target dimensions."
    - "RED E2: `testAccessibilityXXXLLifecycleAnnouncementPreservesFocus` changes lifecycle state in a hosted view, captures the full combined status announcement, and proves the lifecycle effect explicitly preserves the existing VoiceOver focus target rather than assigning a new one."
    - "RED E3: `testAccessibilityXXXLActionLabelsWrapWithoutTruncation` exercises Install, Update, Retry, and Invalidate states, requires each full visible/accessibility label without an ellipsis, scrolls each into view, and measures a minimum 44pt target."
    - "RED E4: `testAccessibilityXXXLDeveloperContextWrapsWithoutSensitiveData` supplies deliberately long safe route/runtime values, requires complete owner/rule/route/runtime/remediation accessibility text within the scrollable column, and rejects URL, path, digest, credential, token, account/device identifier, raw error, transcript, and media fields."
    - "GREEN preservation: all existing closed presentation/action, stable identifier, composition-root, spacing, reduced-motion, and privacy tests remain green."
  </behavior>
  <action>Build a deterministic `UIHostingController` test helper inside `RequiredPackViewTests` that renders the real `RequiredPackView` at `UIContentSizeCategory.accessibilityExtraExtraExtraLarge` in a fixed 320x568 viewport, forces layout, walks the UIKit accessibility hierarchy by the existing stable identifiers, finds the owning `UIScrollView`, scrolls off-screen elements into its visible bounds, and measures actual element/control frames. Assert full expected labels and multiline height/containment instead of source-string presence or presentation-model inference; an accessibility label equal to expected copy is necessary but does not alone prove wrapping, reachability, or 44pt geometry. Add the four named E1-E4 tests from `<behavior>`. In `RequiredPackView`, replace the direct lifecycle post with a narrow internal injectable announcement sink and a testable `LifecycleAccessibilityEffect` carrying the full combined state label plus learner message and an explicit `preserveFocus` directive; production still calls `UIAccessibility.post(.announcement, ...)`, retains `@AccessibilityFocusState`, and never changes that binding during a lifecycle announcement. Keep the initializer source-compatible with a production default. Preserve D-13 through D-15 and D-18: the same one-action state machine, exact host copy, identifiers, scrolling, system styles, disabled animation, and privacy exclusions. Do not add a third-party inspection/snapshot library, UI test target, new learner state/action, modal, automatic retry, public API, or sensitive diagnostic input.</action>
  <acceptance_criteria>
    - The focused XCTest executes four named hosted accessibility-XXXL backstops, one for each UI-SPEC E1-E4 row, rather than claiming coverage from existing presentation-only tests.
    - E1 proves actual scroll ownership, multiline untruncated recovery copy, reachable visible control geometry, and a 44x44pt-or-larger target in the constrained viewport.
    - E2 captures the full lifecycle state/message announcement and proves the accessibility effect preserves the prior VoiceOver focus target.
    - E3 covers all four permitted action labels with full text, wrapping/reflow, scroll reachability, and 44pt minimum targets.
    - E4 covers owner, rule, route, runtime, and remediation wrapping and fails on every D-12/D-21 excluded diagnostic category.
    - Existing state/action semantics, stable identifiers, spacing, reduced-motion behavior, system styling, host-owned copy, and privacy tests remain green.
    - The focused Xcode run stores DerivedData and raw output only in one private mktemp directory removed by a single EXIT trap, with no named Phase 161 native artifact retained under `.build`.
  </acceptance_criteria>
  <verify><automated>bash -o errexit -o nounset -o pipefail -c 'rm -rf .build/phase161-ui-backstops; run_root=$(mktemp -d "${TMPDIR:-/tmp}/crosswake-phase161-16-ui.XXXXXX"); ( cleanup() { rm -rf "$run_root"; }; trap cleanup EXIT; xcodebuild clean test -project examples/ios_shell_host/CrosswakeShell.xcodeproj -scheme CrosswakeShell -derivedDataPath "$run_root/DerivedData" -destination '"'"'platform=iOS Simulator,name=iPhone 17'"'"' -only-testing:CrosswakeShellTests/RequiredPackViewTests CODE_SIGNING_ALLOWED=NO &gt;"$run_root/xcode.log" 2&gt;&amp;1; for marker in testAccessibilityXXXLRecoveryCopyWrapsAndControlsRemainReachable testAccessibilityXXXLLifecycleAnnouncementPreservesFocus testAccessibilityXXXLActionLabelsWrapWithoutTruncation testAccessibilityXXXLDeveloperContextWrapsWithoutSensitiveData; do grep -F "$marker" "$run_root/xcode.log" &gt;/dev/null; done; ); test ! -e "$run_root"; test ! -e .build/phase161-ui-backstops; ! find .build -maxdepth 2 \( -name '"'"'phase161-*.xcresult'"'"' -o -name '"'"'phase161-*.log'"'"' -o -name '"'"'phase161-*-manifest.json'"'"' \) -print -quit | grep -q .'</automated></verify>
  <done>All four UI-SPEC backstops are direct hosted accessibility-XXXL XCTest with real wrapping, scroll/reachability, 44pt geometry, full announcement/preserved focus, and privacy-safe developer-context assertions.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Execute and retain the fresh crash-recovery final-tree gate</name>
  <reversibility rating="reversible">The validation record is replaceable aggregate evidence; it changes no runtime, public contract, persisted product data, or support claim.</reversibility>
  <read_first>
    - AGENTS.md
    - .planning/ADR-FIRST-B2C-ADOPTER.md
    - .planning/FIRST-B2C-ADOPTER-ADOPTION-BRIEF.md
    - .planning/FIRST-B2C-ADOPTER-ROUTE-POLICY-MAP.md
    - .planning/PROJECT.md
    - .planning/REQUIREMENTS.md
    - .planning/ROADMAP.md
    - .planning/STATE.md
    - .planning/phases/161-ios-pronunciation-pack-seam/161-CONTEXT.md
    - .planning/phases/161-ios-pronunciation-pack-seam/161-UI-SPEC.md
    - .planning/phases/161-ios-pronunciation-pack-seam/161-VALIDATION.md
    - .planning/phases/161-ios-pronunciation-pack-seam/161-VERIFICATION.md
    - .planning/phases/161-ios-pronunciation-pack-seam/161-14-SUMMARY.md
    - .planning/phases/161-ios-pronunciation-pack-seam/161-15-SUMMARY.md
    - .planning/phases/161-ios-pronunciation-pack-seam/COVERAGE.md
    - examples/ios_shell_host/CrosswakeShell/RequiredPackView.swift
    - examples/ios_shell_host/CrosswakeShellTests/RequiredPackViewTests.swift
    - script/verify_generated_ios_shell.sh
    - test/crosswake/proof_lane/ios_verifier_test.exs
  </read_first>
  <files>script/verify_generated_ios_shell.sh, test/crosswake/proof_lane/ios_verifier_test.exs, .planning/phases/161-ios-pronunciation-pack-seam/161-VALIDATION.md</files>
  <behavior>
    - "The clean host XCTest executes all three construction-started restart states, in-flight status awaiting exactly one existing barrier, repeated-status storage snapshot invariance, memoized bootstrap failure with zero mutation, idempotence, unsafe-journal denial, existing rollback/invalidate paths, and approved RequiredPackView behavior."
    - "The complete Swift core and proof-lane suites preserve requirement binding, exact integrity, fresh reconciliation, observed denied networking, installed-byte audio proof, and closed evidence."
    - "Phase 160 privacy/authority, Sigra, Phoenix, and browser proof preservation all pass on the same final tree."
    - "Default generated iOS remains blocked/unavailable; only the explicit reference adapter passes pack_audio_prerequisite and remains simulator-advisory."
    - "The validation update records current aggregate-only outcomes and explicitly leaves TODO-002, adopter-instance completeness, and physical-iPhone promotion unresolved/outside Phase 161."
    - "The retained run binding is parsed back from validation and equals the private manifest for every run ID, HEAD revision, subject digest/count, suite count, restart/UI/requirement/threat/generated/API outcome, and boundary outcome before ephemeral evidence is removed."
    - "The private manifest publishes numeric `schema: 3`; both manifest privacy/schema validation and the parsed retained `161-VALIDATION.md` payload require `.schema == 3` before canonical manifest-to-validation equality succeeds."
    - "`testVerifierUsesPrivateDerivedDataForEveryXcodebuildInvocation` records the shim arguments and proves -list, both -showdestinations branches, build-for-testing, and test-without-building all receive one caller-supplied run-root DerivedData path; every raw capture path and effective TMPDIR is beneath the same root."
    - "`testVerifierCleansCallerRunRootAfterSuccessAndFailure` runs structured success plus list/build/test failure modes and proves the caller-scoped verifier root is absent after each exit while structured closed output remains non-echoing."
    - "The host log contains construction/bootstrap D-05 tests, retained-old and first-install storage-root-anchor tests, mismatched-existing-leaf denial, both recovery-before-invalidate tests, and all four durability-order tests; equality includes exact `bootstrap_outcomes`, `volume_anchor_outcomes`, `invalidate_recovery_outcomes`, and `durability_outcomes` objects."
  </behavior>
  <action>Write the two generated-verifier ExUnit tests from `<behavior>` first. Refactor `script/verify_generated_ios_shell.sh` around one owned run root selected from `CROSSWAKE_IOS_RUN_ROOT` or a private `mktemp -d` fallback. Reject an empty/root/broad caller value, create the scoped root with owner-only permissions, and install one EXIT trap that removes that exact root on every success or failure. Put DerivedData, cloned SourcePackages, generated-project temporary state, `TMPDIR`, target-list capture, destination capture, build/test output, and test transcript beneath this root; do not allocate additional native capture roots through ambient `/tmp`. Pass `-derivedDataPath "$RUN_ROOT/DerivedData"` and the scoped cloned-package path to every xcodebuild invocation without exception, including `-list`, both proof and non-proof `-showdestinations`, `build-for-testing`, and `test-without-building`. Redirect discovery output to named files inside the run root, parse from those files, and emit only the existing closed proof JSON on proof failures. Extend `ios_verifier_test.exs` so the xcodebuild shim appends NUL-safe or one-invocation-per-line arguments to a test-owned trace outside the script-owned root; assert each expected discovery/build/test invocation contains the exact scoped DerivedData argument, assert capture/TMPDIR paths reported by the test hook remain beneath the scoped root, and assert the script-owned root is absent after structured success, list failure, build failure, and test failure. Preserve operation-scoped Git/SwiftPM behavior and existing closed outcomes.

Run the complete command chain in `<verify>` once after `161-15-SUMMARY.md` exists and Task 1 plus verifier tests are green; do not reuse 161-14 evidence. Require the exact Plan 15 construction/bootstrap, D-05 status-purity, volume-anchor, invalidate, durability, topology, and UI marker names listed by the updated verify command—these replace all legacy recovery-on-status marker wording. The synchronous composition root remains unchanged, so the non-self-referential subject allowlist remains exactly the same six files and `file_count: 6`; `CrosswakeShellApp.swift` is intentionally absent. Retain exact closed `bootstrap_outcomes` keys `construction_started`, `in_flight_status_awaited_existing`, `repeated_status_unchanged`, and `failure_memoized_no_mutation`, plus `volume_anchor_outcomes` keys `retained_old_absent_destination`, `first_install_absent_destination`, and `mismatched_existing_leaf_denied`. Keep the existing restart, invalidate, durability, UI, requirement, threat, generated, API, boundary, privacy-safe manifest equality, DerivedData cleanup, and stop-list requirements. Publish numeric `schema: 3` in the private manifest, require `.schema == 3` in both the manifest privacy/schema gate and the parsed retained `161-VALIDATION.md` gate, validate exact recursive key sets, publish only the exact allowlisted JSON between the existing sentinels, parse it back, require canonical field-for-field equality with the schema-3 manifest and unchanged subject digest, then remove ephemeral evidence.</action>
  <acceptance_criteria>
    - Fresh clean XCTest executes all construction-started restart states, in-flight status/single-task behavior, repeated-status byte-and-entry snapshot invariance, memoized bootstrap failure/no mutation, both invalidate states, all four durability sequences, and fail-closed recovery on the same final tree.
    - Retained-old and first-install absent-destination shapes validate existing leaves against the canonical existing-directory storageRoot volume anchor; a mismatched injected existing leaf denies with zero sibling/inventory/journal/directory mutation.
    - Core, host/UI, proof/evidence, Phase 160 privacy/authority, Sigra, Phoenix, and browser gates all pass.
    - The same-run host log contains all four named hosted accessibility-XXXL UI tests, so 12 covered/4 backstop/2 dismissed/0 unresolved is sealed only after direct E1-E4 execution.
    - Default generated iOS is closed non-passing; explicit reference-adapter success is simulator-advisory only.
    - Every generated-shell `xcodebuild` invocation—including target and destination discovery—uses the caller-supplied private run-root DerivedData path, every raw capture/TMPDIR path is under that trap-cleaned root, and ExUnit proves cleanup after success and failure.
    - The allowlisted current-run manifest binds its run ID and HEAD to a non-self-referential digest of exactly the six Plan 15/16 implementation/test subject files; the same digest is unchanged after tests and validation publication.
    - The private manifest publishes numeric `schema: 3`, and its exact-key privacy/schema validation rejects any value other than 3.
    - Parsing retained validation requires schema 3 and yields canonical JSON equality with the private manifest for every schema/revision/binding field, count, bootstrap/status-purity outcome, volume-anchor outcome, restart/topology/UI/requirement/threat/generated/API outcome, both invalidate outcomes, all durability outcomes, UI-SPEC count, and non-promotion boundary before cleanup.
    - API coverage accepts the unchanged declaration and retained validation contains only stable IDs, revision/run binding, aggregate counts, and closed outcomes.
    - Phase 162, TODO-002, adopter completeness, Android, background/delta/eviction, generic storage/distribution, scoring/capture, dashboard, and support promotion remain untouched and unclaimed.
  </acceptance_criteria>
  <verify><automated>bash -o errexit -o nounset -o pipefail -c 'rm -rf .build/phase161-crash-recovery .build/phase161-ui-backstops .build/phase161-final-crash-recovery .build/phase161-final-run-manifest.json; subject_files=(examples/ios_shell_host/CrosswakeShell/PronunciationPackProvider.swift examples/ios_shell_host/CrosswakeShellTests/PronunciationPackProviderTests.swift examples/ios_shell_host/CrosswakeShell/RequiredPackView.swift examples/ios_shell_host/CrosswakeShellTests/RequiredPackViewTests.swift script/verify_generated_ios_shell.sh test/crosswake/proof_lane/ios_verifier_test.exs); subject_digest() { for subject in "${subject_files[@]}"; do test -f "$subject"; printf "%s\0" "$subject"; git hash-object "$subject"; done | shasum -a 256 | awk '\''{print $1}'\''; }; subject_before=$(subject_digest); head_revision=$(git rev-parse HEAD); run_root=$(mktemp -d "${TMPDIR:-/tmp}/crosswake-phase161-16-final.XXXXXX"); ( cleanup() { rm -rf "$run_root"; }; trap cleanup EXIT; manifest="$run_root/phase161-final-run-manifest.json"; validation=.planning/phases/161-ios-pronunciation-pack-seam/161-VALIDATION.md; swift test --package-path packages/crosswake-shell-core-ios &gt;"$run_root/core.log" 2&gt;&amp;1; xcodebuild clean test -project examples/ios_shell_host/CrosswakeShell.xcodeproj -scheme CrosswakeShell -derivedDataPath "$run_root/DerivedData" -destination '\''platform=iOS Simulator,name=iPhone 17'\'' -only-testing:CrosswakeShellTests/PronunciationPackProviderTests -only-testing:CrosswakeShellTests/RequiredPackViewTests CODE_SIGNING_ALLOWED=NO &gt;"$run_root/host.log" 2&gt;&amp;1; for marker in testConstructionBootstrapRecoversRetainedKnownGoodPublication testConstructionBootstrapRollsBackPromotedReplacementBeforeInventoryCommit testConstructionBootstrapFinalizesCommittedReplacement testConstructionBootstrapRecoveryIsIdempotent testStatusDuringInFlightBootstrapAwaitsExistingRecoveryWithoutStartingAnother testRepeatedStatusAfterBootstrapLeavesStorageSnapshotUnchanged testConstructionBootstrapFailureIsMemoizedAndClosesAllOperations testInvalidateAwaitsRetainedKnownGoodBootstrapBeforeRevocation testInvalidateAwaitsPromotedReplacementBootstrapBeforeRevocation testDurabilityOrderingRecordsJournalSyncBeforeRetentionRename testDurabilityOrderingRecordsJournalSyncBeforePromotionRename testDurabilityOrderingRecordsInventorySyncBeforeCommittedCleanup testDurabilityOrderingRecordsCommittedCleanupDirectorySync testRestartRecoveryRejectsMalformedSchemaAndPhase testRestartRecoveryRejectsSiblingPackDestination testRestartRecoveryRejectsRecordIdentityOrVersionMismatch testRestartRecoveryRejectsCollidingTransactionLeaves testRestartRecoveryRejectsNonRegularDescendantWithoutMutation testRetainedOldRecoveryUsesStorageRootVolumeAnchorWhenDestinationIsAbsent testFirstInstallRecoveryUsesStorageRootVolumeAnchorWhenDestinationIsAbsent testRestartRecoveryRejectsInjectedMismatchedVolumeExistingLeafWithoutMutation testAccessibilityXXXLRecoveryCopyWrapsAndControlsRemainReachable testAccessibilityXXXLLifecycleAnnouncementPreservesFocus testAccessibilityXXXLActionLabelsWrapWithoutTruncation testAccessibilityXXXLDeveloperContextWrapsWithoutSensitiveData; do grep -F "$marker" "$run_root/host.log" &gt;/dev/null; done; mix test test/crosswake/proof_lane test/crosswake/offline/proof_lane_test.exs test/crosswake/proof_lane/ios_verifier_test.exs &gt;"$run_root/proof.log" 2&gt;&amp;1; mix test test/crosswake/offline test/crosswake/telemetry_test.exs test/crosswake/proof/phase160_scoped_replay_privacy_test.exs test/crosswake/proof_lane/evidence_test.exs test/crosswake/doctor/doctor_test.exs test/crosswake/operator_inspection/json_formatter_test.exs &gt;"$run_root/privacy.log" 2&gt;&amp;1; (cd packages/crosswake_sigra &amp;&amp; mix deps.get &amp;&amp; mix test test/crosswake/companions/sigra/contracts_test.exs) &gt;"$run_root/sigra.log" 2&gt;&amp;1; (cd examples/phoenix_host &amp;&amp; MIX_ENV=test mix test test/crosswake_example/local_first test/crosswake_example/e2e) &gt;"$run_root/phoenix.log" 2&gt;&amp;1; (cd examples/phoenix_host &amp;&amp; npm run proof:offline-island) &gt;"$run_root/browser.log" 2&gt;&amp;1; default_ios_run="$run_root/generated-default"; set +e; TMPDIR="$run_root" CROSSWAKE_IOS_RUN_ROOT="$default_ios_run" bash script/verify_generated_ios_shell.sh --proof-lane &gt;"$run_root/default-ios.log" 2&gt;&amp;1; default_status=$?; set -e; test ! -e "$default_ios_run"; test "$default_status" -eq 2 -o "$default_status" -eq 3; grep -Eq '\''"outcome":"(blocked|unavailable)"'\'' "$run_root/default-ios.log"; reference_ios_run="$run_root/generated-reference"; env -u CROSSWAKE_IOS_XCODEBUILD_BIN -u CROSSWAKE_IOS_PROJECT_ROOT -u CROSSWAKE_IOS_SHIM_MODE TMPDIR="$run_root" CROSSWAKE_IOS_RUN_ROOT="$reference_ios_run" CROSSWAKE_IOS_USE_LOCAL_CORE=1 CROSSWAKE_IOS_LAUNCH_SIMULATOR=1 bash script/verify_generated_ios_shell.sh --proof-lane --reference-pack-adapter &gt;"$run_root/reference-ios.log" 2&gt;&amp;1; grep -Eq '\''"outcome":"passed"'\'' "$run_root/reference-ios.log"; grep -Eq '\''"scope":"pack_audio_prerequisite"'\'' "$run_root/reference-ios.log"; test ! -e "$reference_ios_run"; node /Users/jon/.codex/gsd-core/bin/gsd-tools.cjs check api-coverage.verify-pre .planning/phases/161-ios-pronunciation-pack-seam &gt;"$run_root/api.log" 2&gt;&amp;1; grep -Eq '\''"passed": true'\'' "$run_root/api.log"; git diff --exit-code HEAD -- .planning/phases/161-ios-pronunciation-pack-seam/COVERAGE.md; core_count=$(grep -Eo '\''Executed [0-9]+ tests'\'' "$run_root/core.log" | tail -1 | grep -Eo '\''[0-9]+'\''); host_count=$(grep -c '\''Test Case.*passed'\'' "$run_root/host.log"); proof_count=$(grep -Eo '\''[0-9]+ tests'\'' "$run_root/proof.log" | awk '\''{sum += $1} END {print sum}'\''); privacy_count=$(grep -Eo '\''[0-9]+ tests'\'' "$run_root/privacy.log" | awk '\''{sum += $1} END {print sum}'\''); sigra_count=$(grep -Eo '\''[0-9]+ tests'\'' "$run_root/sigra.log" | awk '\''{sum += $1} END {print sum}'\''); phoenix_count=$(grep -Eo '\''[0-9]+ tests'\'' "$run_root/phoenix.log" | awk '\''{sum += $1} END {print sum}'\''); browser_count=$(grep -Eo '\''[0-9]+ passed'\'' "$run_root/browser.log" | tail -1 | grep -Eo '\''[0-9]+'\''); for count in "$core_count" "$host_count" "$proof_count" "$privacy_count" "$sigra_count" "$phoenix_count" "$browser_count"; do test "$count" -gt 0; done; jq -n --arg run_id "$(uuidgen)" --arg head_revision "$head_revision" --arg subject_tree_sha256 "$subject_before" --argjson core "$core_count" --argjson host "$host_count" --argjson proof "$proof_count" --argjson privacy "$privacy_count" --argjson sigra "$sigra_count" --argjson phoenix "$phoenix_count" --argjson browser "$browser_count" '\''{schema:3,run_id:$run_id,head_revision:$head_revision,subject:{set:"phase161-plans15-16-v3",file_count:6,tree_sha256:$subject_tree_sha256},suite_counts:{swift_core:$core,host_xctest:$host,proof_lane:$proof,privacy:$privacy,sigra:$sigra,phoenix:$phoenix,browser:$browser},restart_outcomes:{retained_old:"passed",promoted_inventory_pending:"passed",inventory_committed:"passed",idempotence:"passed",malformed_schema_phase_denied:"passed",sibling_destination_denied:"passed",record_identity_version_denied:"passed",colliding_leaves_denied:"passed",non_regular_denied:"passed"},bootstrap_outcomes:{construction_started:"passed",in_flight_status_awaited_existing:"passed",repeated_status_unchanged:"passed",failure_memoized_no_mutation:"passed"},volume_anchor_outcomes:{retained_old_absent_destination:"passed",first_install_absent_destination:"passed",mismatched_existing_leaf_denied:"passed"},invalidate_recovery_outcomes:{retained_old_before_revocation:"passed",promoted_inventory_pending_before_revocation:"passed"},durability_outcomes:{retention_order:"passed",promotion_order:"passed",inventory_before_committed_cleanup:"passed",committed_cleanup_order:"passed"},ui_backstops:{recovery_wrap_reachability:"passed",announcement_focus:"passed",action_wrap_targets:"passed",developer_context:"passed"},requirements:{PACK_01:"passed",PACK_02:"passed",PACK_03:"passed",PACK_04:"passed",PACK_05:"passed_non_claim"},threats:{T_161_72:"passed",T_161_73:"passed",T_161_74:"passed",T_161_75:"passed",T_161_76:"passed",T_161_77:"passed",T_161_78:"passed",T_161_79:"passed",T_161_80:"passed",T_161_81:"passed"},generated:{default:"closed_non_passing",reference:"passed_simulator_advisory"},api_coverage:"passed",ui_spec:{covered:12,backstop:4,dismissed:2,unresolved:0},boundaries:{todo_002:"unknown_blocking",adopter_instance:"unknown_blocking",physical_iphone_promotion:"phase_162_only"}}'\'' &gt;"$manifest"; jq -e '\''keys == ["api_coverage","bootstrap_outcomes","boundaries","durability_outcomes","generated","head_revision","invalidate_recovery_outcomes","requirements","restart_outcomes","run_id","schema","subject","suite_counts","threats","ui_backstops","ui_spec","volume_anchor_outcomes"] and .schema == 3 and (.subject | keys == ["file_count","set","tree_sha256"]) and .subject.set == "phase161-plans15-16-v3" and .subject.file_count == 6 and (.suite_counts | keys == ["browser","host_xctest","phoenix","privacy","proof_lane","sigra","swift_core"]) and ([.suite_counts[]] | all(. &gt; 0)) and (.restart_outcomes | keys == ["colliding_leaves_denied","idempotence","inventory_committed","malformed_schema_phase_denied","non_regular_denied","promoted_inventory_pending","record_identity_version_denied","retained_old","sibling_destination_denied"]) and (.bootstrap_outcomes | keys == ["construction_started","failure_memoized_no_mutation","in_flight_status_awaited_existing","repeated_status_unchanged"]) and (.volume_anchor_outcomes | keys == ["first_install_absent_destination","mismatched_existing_leaf_denied","retained_old_absent_destination"]) and (.invalidate_recovery_outcomes | keys == ["promoted_inventory_pending_before_revocation","retained_old_before_revocation"]) and (.durability_outcomes | keys == ["committed_cleanup_order","inventory_before_committed_cleanup","promotion_order","retention_order"]) and ([.restart_outcomes[],.bootstrap_outcomes[],.volume_anchor_outcomes[],.invalidate_recovery_outcomes[],.durability_outcomes[],.ui_backstops[],.threats[]] | all(. == "passed")) and .requirements == {PACK_01:"passed",PACK_02:"passed",PACK_03:"passed",PACK_04:"passed",PACK_05:"passed_non_claim"} and .generated == {default:"closed_non_passing",reference:"passed_simulator_advisory"} and .api_coverage == "passed" and .ui_spec == {covered:12,backstop:4,dismissed:2,unresolved:0} and .boundaries == {todo_002:"unknown_blocking",adopter_instance:"unknown_blocking",physical_iphone_promotion:"phase_162_only"}'\'' "$manifest" &gt;/dev/null; ! rg -i '\''raw_answer|transcript|credential|account_identifier|device_identifier|stable_device|token|https?://|/Users/|/private/|expected_digest|media_path|journal_path|inventory_path|xcresult|screenshot|raw_output'\'' "$manifest"; node -e '\''const fs=require("fs"); const [validation,manifest]=process.argv.slice(1); const payload=JSON.stringify(JSON.parse(fs.readFileSync(manifest,"utf8")),null,2); let source=fs.readFileSync(validation,"utf8"); const start="&lt;!-- phase161-final-run-manifest:start --&gt;"; const finish="&lt;!-- phase161-final-run-manifest:end --&gt;"; const block=start+"\n"+payload+"\n"+finish; if(source.includes(start)){ source=source.replace(/&lt;!-- phase161-final-run-manifest:start --&gt;[\s\S]*?&lt;!-- phase161-final-run-manifest:end --&gt;/,block); } else { const section="## Fresh Post-Crash-Recovery Final-Tree Gate\n\nRetained evidence is the allowlisted machine-readable object below. Dynamic run facts appear only in this object so they can be compared mechanically; raw native output and private values are excluded.\n\n"+block+"\n\n### Boundaries Retained\n\nThe object above is simulator-advisory evidence only. Adopter-instance and physical-device promotion remain outside Phase 161, and the foreground iOS stop list remains unchanged.\n"; const replaced=source.replace(/\n## Fresh Post-WR-01\/CR-01 Final-Tree Gate[\s\S]*$/, "\n"+section); if(replaced===source) throw new Error("stale validation section not found"); source=replaced; } fs.writeFileSync(validation,source);'\'' "$validation" "$manifest"; awk '\''/&lt;!-- phase161-final-run-manifest:start --&gt;/{capture=1;next}/&lt;!-- phase161-final-run-manifest:end --&gt;/{capture=0}capture'\'' "$validation" &gt;"$run_root/retained.json"; jq -e . "$run_root/retained.json" &gt;/dev/null; jq -e '\''keys == ["api_coverage","bootstrap_outcomes","boundaries","durability_outcomes","generated","head_revision","invalidate_recovery_outcomes","requirements","restart_outcomes","run_id","schema","subject","suite_counts","threats","ui_backstops","ui_spec","volume_anchor_outcomes"] and .schema == 3 and (.subject | keys == ["file_count","set","tree_sha256"]) and .subject.set == "phase161-plans15-16-v3" and .subject.file_count == 6 and (.bootstrap_outcomes | keys == ["construction_started","failure_memoized_no_mutation","in_flight_status_awaited_existing","repeated_status_unchanged"]) and (.volume_anchor_outcomes | keys == ["first_install_absent_destination","mismatched_existing_leaf_denied","retained_old_absent_destination"])'\'' "$run_root/retained.json" &gt;/dev/null; ! rg -i '\''raw_answer|transcript|credential|account_identifier|device_identifier|stable_device|token|https?://|/Users/|/private/|expected_digest|media_path|journal_path|inventory_path|xcresult|screenshot|raw_output'\'' "$run_root/retained.json"; jq -S . "$manifest" &gt;"$run_root/manifest.sorted"; jq -S . "$run_root/retained.json" &gt;"$run_root/retained.sorted"; cmp "$run_root/manifest.sorted" "$run_root/retained.sorted"; subject_after=$(subject_digest); test "$subject_after" = "$subject_before"; rm "$manifest"; test ! -e "$manifest"; git diff --check -- "$validation"; ); test ! -e "$run_root"; test ! -e .build/phase161-crash-recovery; test ! -e .build/phase161-ui-backstops; test ! -e .build/phase161-final-crash-recovery; test ! -e .build/phase161-final-run-manifest.json; ! find .build -maxdepth 3 \( -name '\''phase161-*.xcresult'\'' -o -name '\''phase161-*.log'\'' -o -name '\''phase161-*-manifest.json'\'' \) -print -quit | grep -q .'</automated></verify>
  <done>Fresh same-tree evidence publishes numeric schema 3, binds the unchanged exact six-file subject set, and canonically retains a schema-3 payload equal to the private manifest for construction/bootstrap status-purity, storage-root volume-anchor, invalidate, durability, UI, privacy, and cleanup outcomes without any physical-device, external-API, UI-change, or adjacent product claim.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| repaired final tree -> Phase 161 validation claim | Only fresh execution of all crash-recovery states may supersede `gaps_found`. |
| XCTest/shell output -> retained evidence | Raw transaction, filesystem, media, and identity data must collapse to stable aggregates. |
| simulator/reference adapter -> device/support truth | Advisory results cannot promote Phase 162, adopter instance, or support. |
| Phase 161 repair -> Phase 160 authority and approved UI | Pack recovery must preserve scoped replay, backend authorization, privacy, browser behavior, and accessible recovery semantics. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-161-77 | Repudiation | final Phase 161 closure | high | mitigate | Task 2 derives every named construction/bootstrap, D-05 status-purity, volume-anchor, recovery/topology/invalidate/durability/UI/requirement/threat/generated/API outcome and seven positive suite counts from one run, binds them to a fresh run ID/current HEAD plus the unchanged exact six-file non-self-referential digest, publishes numeric schema 3, then requires `.schema == 3` and canonical retained-manifest equality. |
| T-161-78 | Information Disclosure | retained validation evidence and native build output | high | mitigate | Task 2 stores every raw log, direct-Xcode DerivedData tree, generated-verifier discovery/test capture, extracted payload, and allowlisted manifest beneath trap-cleaned private run roots; passes private DerivedData to every xcodebuild invocation; privacy-scans manifest and retained payload; and asserts cleanup on success and failure. |
| T-161-79 | Spoofing | advisory native result | high | mitigate | Task 2 requires both default non-pass and explicit simulator-advisory reference success while Phase 162 stays sole promotion owner. |
| T-161-80 | Tampering | adjacent replay/auth/UI and generated-verifier contracts | high | mitigate | Tasks 1-2 add direct E1-E4 hosted UI backstops, enforce private DerivedData/capture ownership for discovery through test, and rerun Phase 160, Sigra, Phoenix, browser, and RequiredPackView gates instead of inferring preservation. |
| T-161-81 | Repudiation | no-external-API classification | medium | mitigate | Task 2 verifies the exact unchanged COVERAGE.md declaration and creates no API matrix. |
| T-161-SC | Tampering | package supply chain | low | accept | No package install or dependency change is planned; existing locked dependencies are exercised. |
</threat_model>

<source_coverage_audit>
| Source | ID | Feature/constraint | Plan | Status | Notes |
|---|---|---|---|---|---|
| GOAL | Phase 161 | Verified foreground media installation survives interruption with explicit ownership and honest proof. | 15-16 | COVERED | Durable recovery plus fresh same-tree seal. |
| REQ | PACK-01 | Existing host foreground status/install/invalidate seam remains runnable. | 16 | COVERED | Public seam unchanged and full provider/proof gates rerun. |
| REQ | PACK-02 | Interrupted or invalid publication never reports available. | 15-16 | COVERED | Construction-time bootstrap converges once; status only awaits/projects, mutation waits for success, and fresh denial/attestation tests seal behavior. |
| REQ | PACK-03 | Exact integrity plus crash-atomic/recoverable installation precedes availability. | 15-16 | COVERED | Recorder-proved durable ordering, convergent recovery, invalidate ordering, and three restart states. |
| REQ | PACK-04 | Crosswake/host ownership remains explicit. | 15-16 | COVERED | Journal/filesystem authority stays host-private; core API is unchanged. |
| REQ | PACK-05 | Adjacent platform/product claims remain excluded. | 16 | COVERED | Full stop list and simulator/device boundary are retained. |
| RESEARCH | Atomic replacement | Preserve last-known-good bytes and persist inventory only after promotion. | 15 | COVERED | Durable state extends the existing same-volume transaction across process death. |
| RESEARCH | Fresh reconciliation | Availability follows exact byte-backed provider status. | 15-16 | COVERED | Construction starts recovery independently; operations await its memoized result, and status remains side-effect-free while rerunning exact attestation. |
| RESEARCH | Privacy/evidence | Closed results and aggregate-only retained proof. | 15-16 | COVERED | Journal is non-sensitive; validation retains aggregates only. |
| CONTEXT | D-01 through D-05 | Narrow versioned provider, off-main verified same-volume publication, per-pack serialization, and side-effect-free status. | 15-16 | COVERED | One detached construction-time barrier uses immutable dependencies; status only awaits/projects and repeated snapshots remain unchanged behind the public seam. |
| CONTEXT | D-06 through D-10 | Cold-start reconciliation, known-good preservation, revocation, and non-authoritative timestamps. | 15-16 | COVERED | Bootstrap begins at provider construction, is memoized, and converges before mutation while status initiates no recovery. |
| CONTEXT | D-11 through D-15 | Closed non-echoing failure and accessible foreground recovery UI. | 15-16 | COVERED | Existing reasons/UI remain unchanged and automated UI gates rerun. |
| CONTEXT | D-16 through D-21 | Real-byte tests, advisory generated proof, privacy-safe evidence, and Phase 162 boundary. | 15-16 | COVERED | Restart fixtures plus complete proof/evidence seal. |
| CONTEXT | Deferred ideas | Signed receipts, asset resolution, background/resumable/delta/eviction/layout, Android, capture/scoring, physical-iPhone promotion. | — | EXCLUDED | Explicitly deferred or Phase 162-owned. |
| UI | covered E1-E4 | Empty/loading/error/ready states, action rules, scroll/wrap, safe developer context, accessibility/reduced motion. | 16 | COVERED | Existing RequiredPackViewTests rerun; no UI change. |
| UI | backstop E1-E4 | Four large-Dynamic-Type readability/reachability/focus/privacy assertions. | 16 | COVERED | Four named hosted accessibility-XXXL tests exercise real layout/accessibility geometry and lifecycle effects. |
| UI | dismissed | Partial form/list and zero-one-many collection behavior. | — | EXCLUDED | The surface is exactly one required pack, not a form or collection. |
| API | COVERAGE.md | No external API integration. | 16 | COVERED | Exact declaration preserved and verified; no matrix invented. |
</source_coverage_audit>

<gap_coverage_audit>
| Verification blocker | Closing plan/task | Automated evidence |
|---|---|---|
| Replacement publication has no durable state or recovery across process termination. | 161-15 Task 1 | Fsynced journal ordering and one detached construction-time startup barrier independent of status. |
| D-05 status purity conflicts with mutating recovery. | 161-15 Task 1 | In-flight status awaits the existing barrier, repeated status preserves byte/entry snapshots, and failure is memoized without retry/mutation. |
| Retained-old restart is untested. | 161-15 Task 1 | Seeded retained artifact plus old inventory; provider construction restores it while status only re-attests. |
| Promoted-new/inventory-pending restart is untested. | 161-15 Task 1 | Seeded promoted/retained files plus old inventory; new provider rolls back and denies attempted requirement. |
| Inventory-commit recovery is untested. | 161-15 Task 1 | Seeded committed new pair plus cleanup journal; new provider finalizes and re-attests new requirement. |
| Public invalidate can mutate revocation state before crash recovery. | 161-15 Task 1 | Seeded retained-old and promoted-new/inventory-pending journal states call invalidate directly and prove convergence precedes the first revocation removal/inventory write. |
| Fsync ordering is prose-only. | 161-15 Task 1 | Host-private production-backed operation dependency plus four named recorder XCTest prove exact retention, promotion, inventory-commit, and committed-cleanup sequences. |
| Retained-old/first-install have an absent live destination, so it cannot anchor a volume check. | 161-15 Task 1 | Canonical existing-directory storageRoot anchors volume identity; named absent-destination tests validate only existing canonical non-symlink regular leaves, and one injected mismatched leaf proves zero sibling/inventory/journal/directory mutation. |
| Generated verifier discovery escapes private DerivedData/run-root ownership. | 161-16 Task 2 | Script passes one caller-scoped DerivedData to every list/destination/build/test invocation, stores captures under the same trap-cleaned root, and ExUnit asserts success/failure cleanup. |
| UI-SPEC E1-E4 backstops were claimed without executable layout/accessibility coverage. | 161-16 Task 1 | Four named hosted accessibility-XXXL wrapping/reachability/44pt/announcement-focus/privacy tests. |
| Fresh post-repair evidence seal is required. | 161-16 Task 2 | Same-run chain, unchanged exact six-file digest/count, bootstrap and volume-anchor outcomes, invalidate/durability outcomes, allowlisted manifest equality, and trap-cleaned native output. |
</gap_coverage_audit>

<verification>
Execute the four focused UI backstops, generated-verifier private-run-root tests, then the complete chain after Plan 161-15. Require numeric schema 3 in both the private manifest and parsed retained validation, then canonically compare them: construction-started recovery, in-flight status awaiting one existing barrier, repeated-status snapshot invariance, memoized failure/no mutation, retained-old/first-install storage-root-anchor shapes, mismatched-existing-leaf denial, all restart/topology/invalidate/durability/UI outcomes, aggregate counts, HEAD, and unchanged exact six-file digest/count must match; private native state must be gone and all non-promotion/privacy/API boundaries unchanged.
</verification>

<success_criteria>
- PACK-03 has fresh direct construction-started crash-recovery evidence for retained-old, promoted-new/inventory-pending, and inventory-committed states without status-triggered mutation.
- D-05 has direct in-flight, repeated-status snapshot, and memoized-failure evidence; storageRoot anchors volume identity for absent-destination recovery shapes.
- Public invalidate has fresh direct evidence that recovery converges before revocation mutation, and each durability transition has a named operation-order outcome.
- Generated-shell target/destination discovery, build, and test all use one caller-scoped private DerivedData/capture root whose cleanup is tested on success and failure.
- PACK-01 through PACK-05 and every high-severity T-161-72 through T-161-80 mitigation are accounted for.
- Validation is privacy-safe and aggregate-only, publishes numeric schema 3 equal to the private manifest, and reuses no stale result.
- The approved UI, no-external-API declaration, Phase 160 authority, Android freeze, and Phase 162-only physical-device boundary remain intact.
</success_criteria>

<output>
Create `.planning/phases/161-ios-pronunciation-pack-seam/161-16-SUMMARY.md` when done.
</output>
