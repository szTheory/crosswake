---
phase: 91-identity-telemetry-contract
plan: 02
type: execute
wave: 2
depends_on: ["91-01"]
files_modified:
  - lib/crosswake/bridge/contract.ex
  - lib/crosswake/bridge/denial.ex
  - lib/crosswake/shell/activation.ex
  - mix.exs
  - test/crosswake/bridge/contract_test.exs
  - test/crosswake/shell/activation_test.exs
  - test/crosswake/proof/phase91_threadline_contract_closeout_test.exs
autonomous: true
requirements: [PROP-04, PROP-02]
must_haves:
  truths:
    - "thread_id is a declared optional field (default nil, not enforced) on Bridge.Contract.Request, Bridge.Contract.Reply, Bridge.Denial, and Shell.Activation.Request"
    - "ok_reply/2, deny_reply/2, and Denial.from_request/2 copy request.thread_id onto the response, exactly like correlation_id"
    - "Request.to_map/1 omits thread_id when nil (footgun fix); Denial.to_map/1 omits thread_id when nil (Types.to_map does not nil-filter)"
    - "Contract.@version is 1.1.0; the compatibility gate still passes"
    - "Hex package version is 0.1.1"
    - "A closeout proof test asserts the full published Threadline telemetry allowlist AND @version 1.1.0"
  artifacts:
    - path: "test/crosswake/proof/phase91_threadline_contract_closeout_test.exs"
      provides: "Published-allowlist + version closeout proof"
      contains: "Crosswake.Threadline.Telemetry"
    - path: "lib/crosswake/bridge/contract.ex"
      provides: "thread_id on Request/Reply, @version 1.1.0, Request.to_map nil-filter"
      contains: "thread_id"
    - path: "lib/crosswake/bridge/denial.ex"
      provides: "thread_id on Denial + from_request + to_map nil-filter"
      contains: "thread_id"
    - path: "lib/crosswake/shell/activation.ex"
      provides: "thread_id on Activation.Request"
      contains: "thread_id"
  key_links:
    - from: "lib/crosswake/bridge/contract.ex ok_reply/2,deny_reply/2"
      to: "Reply.thread_id"
      via: "request.thread_id propagation"
      pattern: "thread_id: request\\.thread_id"
    - from: "lib/crosswake/bridge/denial.ex from_request/2"
      to: "Denial.thread_id"
      via: "request.thread_id propagation"
      pattern: "thread_id: request\\.thread_id"
---

<objective>
Add `thread_id` as a first-class **optional** field (default nil, NOT enforced) to the bridge and activation contract envelopes — `Bridge.Contract.Request`/`Reply`, `Bridge.Denial`, and `Shell.Activation.Request` — propagate it through the from-request helpers exactly like `correlation_id`, fix the two `to_map/1` nil-serialization footguns, bump the bridge envelope `@version` to `1.1.0` and the Hex package to `0.1.1`, and lock the full published Threadline contract (telemetry allowlist + version) with a closeout proof test.

Purpose: PROP-04 (thread_id first-class on contracts) plus the version half of PROP-02's published-contract proof. This is the wire-envelope foundation Phases 92/94 consume; presence is never enforced here (D-01) because no caller can populate thread_id until Phase 92/93. The type spec is `thread_id: String.t() | nil` (D-02) — an opaque string mirroring correlation_id, with no format validation in the contract; UUID minting lives in the Phase 92 Plug.
Output: modifications to the four envelope modules + mix.exs + two existing tests, and one new closeout proof test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/91-identity-telemetry-contract/91-CONTEXT.md
@.planning/phases/91-identity-telemetry-contract/91-RESEARCH.md
@.planning/phases/91-identity-telemetry-contract/91-VALIDATION.md
@.planning/phases/91-identity-telemetry-contract/91-01-SUMMARY.md

<interfaces>
<!-- Exact current shapes (verified). Modify in place; do not re-derive. -->

lib/crosswake/bridge/contract.ex:
  @version "1.0.0"                       # -> bump to "1.1.0" (D-04). version/0 accessor returns it.
  Request @enforce_keys: [:protocol, :version, :command, :capability, :route_id,
                          :active_route_id, :origin, :native_runtime_version, :correlation_id]
          defstruct adds capabilities/installed_packs/payload defaults — thread_id joins here as `thread_id: nil`
  Reply   @enforce_keys: [:protocol, :version, :command, :route_id, :correlation_id, :status]
          defstruct has payload: %{}, denial: nil — thread_id joins as `thread_id: nil`
  new_request/1, new_reply/1: struct! maps via Keyword.get/fetch! — add thread_id: Keyword.get(attrs, :thread_id)
  ok_reply/2, deny_reply/2: build new_reply(...) copying correlation_id — add thread_id: request.thread_id
  to_map(%Request{}): plain map literal, NO nil-filter (footgun D-05). Add "thread_id" => request.thread_id then `|> Enum.reject(fn {_k, v} -> is_nil(v) end) |> Map.new()`
  to_map(%Reply{}): ALREADY ends with Enum.reject nil-filter — just add "thread_id" => reply.thread_id to the literal

lib/crosswake/bridge/denial.ex:
  @enforce_keys [:command, :route_id, :correlation_id, :denial]   # thread_id NOT added here
  defstruct [:command, :route_id, :correlation_id, :denial]       # add :thread_id
  new/1: struct! map via Keyword.fetch! — add thread_id: Keyword.get(attrs, :thread_id)
  from_request/2: copies correlation_id — add thread_id: request.thread_id
  to_map/1: builds literal then `|> Types.to_map()`. Types.to_map/1 only stringifies keys + recurses — it does NOT nil-filter (verified lib/crosswake/manifest/types.ex). Add "thread_id" => denial.thread_id AND insert `|> Enum.reject(fn {_k, v} -> is_nil(v) end) |> Map.new()` BEFORE the `|> Types.to_map()` pipe so a nil thread_id is dropped locally. Do NOT modify the shared Types.to_map/1.

lib/crosswake/shell/activation.ex (Request submodule):
  @enforce_keys includes :correlation_id but NOT thread_id — leave unchanged
  defstruct [:route_id, :url, :source, :origin, :manifest_source, :bridge_protocol_version,
             :native_runtime_version, :correlation_id, declared_pack_requirements: %{}, ...] — add :thread_id
  new_request/1: struct! map — add thread_id: Keyword.get(attrs, :thread_id)
  to_map(%Request{}): ALREADY ends with `|> Enum.reject(... is_nil ...) |> Map.new()` — just add "thread_id" => request.thread_id to the literal; no footgun fix needed

mix.exs: @version "0.1.0"  ->  "0.1.1" (D-06, patch). version: @version is used at line 10.

Crosswake.Threadline.Telemetry (from plan 91-01): event_names/0, metadata_keys/0, forbidden_metadata_keys/0 accessors — consumed by the closeout proof test.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add thread_id to all four envelopes + propagation + nil-filter fixes</name>
  <files>lib/crosswake/bridge/contract.ex, lib/crosswake/bridge/denial.ex, lib/crosswake/shell/activation.ex, test/crosswake/bridge/contract_test.exs, test/crosswake/shell/activation_test.exs</files>
  <read_first>
    - lib/crosswake/bridge/contract.ex (Request/Reply structs, new_request/1, new_reply/1, ok_reply/2, deny_reply/2, to_map/1 clauses, @version)
    - lib/crosswake/bridge/denial.ex (Denial struct, new/1, from_request/2, to_map/1 delegating to Types.to_map/1)
    - lib/crosswake/shell/activation.ex (Activation.Request struct, new_request/1, to_map/1 — already nil-filters)
    - lib/crosswake/manifest/types.ex (Types.to_map/1 — confirm it does NOT nil-filter, so the Denial fix is local)
    - test/crosswake/bridge/contract_test.exs (the to_map equality assertion at ~line 79 asserts "version" => "1.0.0" — must update to "1.1.0"; all to_map equality assertions must stay green)
    - test/crosswake/shell/activation_test.exs (new_request round-trip + to_map nil-filter assertions)
  </read_first>
  <behavior>
    - %Bridge.Contract.Request{}, %Bridge.Contract.Reply{}, %Bridge.Denial{}, %Shell.Activation.Request{} each construct with thread_id defaulting to nil and thread_id is NOT in any @enforce_keys
    - Contract.new_request(thread_id: "t-1") round-trips: returned struct's thread_id == "t-1"
    - ok_reply(request_with_thread, %{}).thread_id == request.thread_id
    - deny_reply(request_with_thread, denial).thread_id == request.thread_id
    - Denial.from_request(request_with_thread, shell_denial).thread_id == request.thread_id
    - Contract.to_map(request_with_nil_thread) does NOT contain the "thread_id" key
    - Contract.to_map(request_with_thread "t-1") contains "thread_id" => "t-1"
    - Bridge.Denial.to_map(denial_with_nil_thread) does NOT contain "thread_id"; with a value it contains "thread_id" => value
    - Activation.Request.to_map(request_with_nil_thread) does NOT contain "thread_id"; with value present it does
    - Existing contract_test to_map equality assertion now expects "version" => "1.1.0"
  </behavior>
  <action>Add `thread_id: nil` to the `defstruct` of Bridge.Contract.Request, Bridge.Contract.Reply, Bridge.Denial, and Shell.Activation.Request — do NOT add thread_id to any @enforce_keys (D-01). Add `thread_id: String.t() | nil` to each `@type t`. In Contract.new_request/1 and new_reply/1, Denial.new/1, and Activation.new_request/1, add `thread_id: Keyword.get(attrs, :thread_id)` to the struct! map (Pitfall 2: struct! does not auto-map kwargs). In Contract.ok_reply/2 and deny_reply/2 add `thread_id: request.thread_id` to the new_reply call (D-03). In Denial.from_request/2 add `thread_id: request.thread_id` to the new(...) call (D-03). In Contract.to_map(%Request{}) add `"thread_id" => request.thread_id` to the literal AND append `|> Enum.reject(fn {_k, v} -> is_nil(v) end) |> Map.new()` (D-05 footgun fix — the Request clause currently has no nil-filter). In Contract.to_map(%Reply{}) add `"thread_id" => reply.thread_id` to the literal only (it already nil-filters). In Denial.to_map/1 add `"thread_id" => denial.thread_id` to the literal AND insert `|> Enum.reject(fn {_k, v} -> is_nil(v) end) |> Map.new()` BEFORE the existing `|> Types.to_map()` pipe (EXTRA FINDING: Types.to_map/1 does NOT nil-filter — verified at lib/crosswake/manifest/types.ex; the fix must be local to denial.ex; do NOT touch Types.to_map/1). In Activation.to_map(%Request{}) add `"thread_id" => request.thread_id` to the literal only (it already nil-filters). Update test/crosswake/bridge/contract_test.exs: change the to_map equality assertion that reads `"version" => "1.0.0"` to `"1.1.0"` (this depends on the @version bump in Task 2 — order Task 2 to land the bump, OR set the assertion now and run after Task 2; mark the assertion update here and confirm green after Task 2). Add to contract_test.exs: thread_id default-nil field assertions for Request/Reply/Denial, the ok_reply/deny_reply/from_request propagation assertions, and the Request/Denial to_map nil-absent + value-present assertions. Add to test/crosswake/shell/activation_test.exs: thread_id round-trip via new_request and the to_map nil-absent + value-present assertions. Do NOT modify the @version attribute in this task — that is Task 2 (kept separate so the version-coupled assertion flips in one place).</action>
  <verify>
    <automated>mix test test/crosswake/bridge/contract_test.exs test/crosswake/shell/activation_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "thread_id" lib/crosswake/bridge/contract.ex` >= 6 (Request struct, Reply struct, new_request, new_reply, ok_reply, deny_reply, both to_map clauses)
    - thread_id appears in defstruct but NOT in @enforce_keys of Request/Reply/Denial/Activation.Request (verified by reading each @enforce_keys)
    - lib/crosswake/bridge/contract.ex Request to_map clause contains `Enum.reject(fn {_k, v} -> is_nil(v) end)` (footgun fixed)
    - lib/crosswake/bridge/denial.ex to_map/1 contains an `Enum.reject(... is_nil ...)` BEFORE `Types.to_map()` and Types.to_map/1 in manifest/types.ex is unchanged
    - ok_reply/2, deny_reply/2, and Denial.from_request/2 each contain `thread_id: request.thread_id`
    - test/crosswake/bridge/contract_test.exs and test/crosswake/shell/activation_test.exs assert thread_id propagation + nil-absent serialization
    - `mix test test/crosswake/bridge/contract_test.exs test/crosswake/shell/activation_test.exs` exits 0 after Task 2 lands the version bump (the "1.1.0" assertion is the only one that needs Task 2)
  </acceptance_criteria>
  <done>All four envelopes carry an optional thread_id, the from-request helpers propagate it, both to_map footguns (Request literal + Denial via Types.to_map) are fixed locally so nil thread_id serializes as absent, and the existing + new contract/activation tests pass once the version bump lands in Task 2.</done>
</task>

<task type="auto">
  <name>Task 2: Bump envelope @version to 1.1.0, Hex to 0.1.1, write closeout proof</name>
  <files>lib/crosswake/bridge/contract.ex, mix.exs, test/crosswake/proof/phase91_threadline_contract_closeout_test.exs</files>
  <read_first>
    - lib/crosswake/bridge/contract.ex (@version "1.0.0" at line 10; version/0 accessor at line 102; new_request/new_reply default to @version)
    - mix.exs (@version "0.1.0" at line 4; used as version: @version and source_ref: "v#{@version}")
    - test/crosswake/proof/phase58_auth_diagnostics_closeout_test.exs (the published-allowlist proof pattern — exact-list equality on event_names + forbidden-key membership)
    - lib/crosswake/threadline/telemetry.ex (the module whose published lists the proof asserts — from plan 91-01)
    - lib/crosswake/compatibility/compatibility.ex (confirm bridge_target uses request.version and semver >= logic, so 1.1.0 satisfies a 1.0.0 requirement — D-04, no gate break)
  </read_first>
  <action>Bump `@version "1.0.0"` to `"1.1.0"` in lib/crosswake/bridge/contract.ex (D-04 — additive/minor, informational; NOT a 2.0.0 bump because no breaking change occurred). Bump `@version "0.1.0"` to `"0.1.1"` in mix.exs (D-06 — patch, purely additive, no `feat!`, no `~> 0.1` adopter break). Create test/crosswake/proof/phase91_threadline_contract_closeout_test.exs mirroring the phase58 closeout pattern: assert Crosswake.Threadline.Telemetry.event_names() equals EXACTLY the three request-span names (full-list equality), assert metadata_keys() equals EXACTLY [:thread_id, :correlation_id, :route_id, :source], assert :actor_ref and :access_token and :user_agent are in forbidden_metadata_keys() and that forbidden_metadata_keys() is disjoint from metadata_keys(), assert Telemetry.execute([:crosswake, :threadline, :request, :start], %{}, %{access_token: "secret", thread_id: "t-1"}) returns :ok and (via an attached handler) the received metadata contains :thread_id but not :access_token, assert Crosswake.Bridge.Contract.version() == "1.1.0", and assert each of the four envelope structs constructs with a thread_id field defaulting to nil. Before finalizing, run `grep -rn "\"1.0.0\"" test/crosswake/compatibility/compatibility_test.exs` to confirm no compat test asserts the bridge envelope default version string equals "1.0.0" (the compat tests construct Target structs with explicit bridge_protocol_version, so the bump is safe — but verify).</action>
  <verify>
    <automated>mix test test/crosswake/proof/phase91_threadline_contract_closeout_test.exs test/crosswake/bridge/contract_test.exs test/crosswake/compatibility/</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '@version "1.1.0"' lib/crosswake/bridge/contract.ex` == 1
    - `grep -c '@version "0.1.1"' mix.exs` == 1
    - test/crosswake/proof/phase91_threadline_contract_closeout_test.exs asserts exact-list equality for event_names/0 and metadata_keys/0
    - the proof test asserts Crosswake.Bridge.Contract.version() == "1.1.0"
    - the proof test asserts execute/3 with a forbidden key returns :ok and the forbidden key is absent from handler-received metadata
    - `mix test test/crosswake/proof/phase91_threadline_contract_closeout_test.exs` exits 0
    - `mix test test/crosswake/compatibility/` exits 0 (the @version bump did not break the compatibility gate — D-04)
  </acceptance_criteria>
  <done>@version is 1.1.0, Hex is 0.1.1, the closeout proof locks the full published Threadline telemetry contract plus the version, the compatibility gate still passes, and the previously version-coupled contract_test assertion is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| native shell / Plug → bridge & activation envelopes | thread_id is opaque caller-supplied input; it rides the wire but is never enforced or format-validated at the envelope (validation lives at the Phase 92 Plug / Phase 94 ledger) |
| envelope struct → to_map/1 wire serialization | nil thread_id must serialize as absent, never as an explicit null key, to avoid wire-key drift for native JSON decoders |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-91-WIRE | Tampering | to_map/1 (Request + Denial) | mitigate | nil-filter on both clauses so nil thread_id is dropped (no `"thread_id" => null` wire drift); verified by the to_map nil-absent assertions |
| T-91-ENF | Denial of Service (self-imposed deadlock) | @enforce_keys | mitigate | thread_id is optional (default nil, not enforced) per D-01 — no caller can populate it until Phase 92/93, so enforcing it would break the full suite; verified by default-nil construction assertions |
| T-91-VER | Repudiation (dishonest version signal) | @version bump | accept | additive-minor 1.1.0 (not 2.0.0) honestly signals "envelope can now carry thread_id"; informational, not gate-wired (D-04); compatibility gate uses semver >= so 1.1.0 satisfies 1.0.0 — verified by `mix test test/crosswake/compatibility/` |
| T-91-PII | Information Disclosure | closeout proof (Threadline.Telemetry) | mitigate | proof locks the published forbidden-key denylist + execute/3 rejection so PII keys cannot silently re-enter the contract; verified by the closeout proof's forbidden-key + execute/3 assertions |
</threat_model>

<verification>
- `mix test test/crosswake/bridge/contract_test.exs test/crosswake/shell/activation_test.exs test/crosswake/proof/phase91_threadline_contract_closeout_test.exs` exits 0.
- `mix test test/crosswake/compatibility/` exits 0 (D-04: version bump does not break the gate).
- `mix test` (full suite) green — no `"thread_id" => nil` and no `"version" => "1.0.0"` assertion failures remain.
- `grep -rn "thread_id.*nil" lib/crosswake/manifest/types.ex` returns nothing (shared Types.to_map/1 was NOT modified).
</verification>

<success_criteria>
- thread_id is a declared optional field on all four envelopes alongside the unchanged correlation_id (PROP-04).
- from-request helpers propagate thread_id; both to_map footguns are fixed locally; nil thread_id serializes as absent.
- @version is 1.1.0, Hex is 0.1.1; compatibility gate unaffected.
- The closeout proof locks the full published Threadline telemetry allowlist + version (PROP-02 published-contract half).
</success_criteria>

<output>
Create `.planning/phases/91-identity-telemetry-contract/91-02-SUMMARY.md` when done.
</output>
