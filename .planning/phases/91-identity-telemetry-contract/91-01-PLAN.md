---
phase: 91-identity-telemetry-contract
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/crosswake/threadline/telemetry.ex
  - test/crosswake/threadline/telemetry_test.exs
autonomous: true
requirements: [PROP-02]
must_haves:
  truths:
    - "Crosswake.Threadline.Telemetry exists and compiles using only :telemetry"
    - "event_names/0 returns exactly the three request-span names"
    - "metadata_keys/0 returns exactly [:thread_id, :correlation_id, :route_id, :source]"
    - "metadata/1 drops forbidden PII keys silently without raising"
    - "execute/3 routes metadata through metadata/1 and never raises on forbidden keys"
    - "safe_value?/1 rejects binaries longer than 128 chars"
  artifacts:
    - path: "lib/crosswake/threadline/telemetry.ex"
      provides: "Threadline telemetry allowlist contract module"
      contains: "defmodule Crosswake.Threadline.Telemetry"
      min_lines: 80
    - path: "test/crosswake/threadline/telemetry_test.exs"
      provides: "Hermetic unit coverage for the telemetry allowlist"
      contains: "Crosswake.Threadline.Telemetry"
  key_links:
    - from: "lib/crosswake/threadline/telemetry.ex"
      to: ":telemetry.execute/3"
      via: "execute/3 wrapper"
      pattern: ":telemetry\\.execute"
    - from: "lib/crosswake/threadline/telemetry.ex"
      to: "metadata/1 reduce-filter"
      via: "execute/3 passes metadata through metadata/1"
      pattern: "metadata\\(metadata\\)"
---

<objective>
Ship `Crosswake.Threadline.Telemetry` — the low-cardinality `:telemetry` event-name + metadata allowlist guard — mirroring `Crosswake.Companions.Sigra.Telemetry` exactly in shape, narrowed to the PROP-02 four-key allowlist and the request-span event triplet. Zero runtime emission; this defines the contract the Phase 92 Plug will emit through.

Purpose: PROP-02. Establishes the PII-free, cardinality-bounded telemetry contract before any emitter exists, so the Phase 92 Plug can emit through `execute/3` immediately without re-deriving the guard.
Output: `lib/crosswake/threadline/telemetry.ex` and `test/crosswake/threadline/telemetry_test.exs`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/91-identity-telemetry-contract/91-CONTEXT.md
@.planning/phases/91-identity-telemetry-contract/91-RESEARCH.md
@.planning/phases/91-identity-telemetry-contract/91-VALIDATION.md

<interfaces>
<!-- The exact pattern to mirror. Executor copies this module's shape; do not explore further. -->

Crosswake.Companions.Sigra.Telemetry (lib/crosswake/companions/sigra/telemetry.ex) exposes:
  @event_names :: [[atom()]]            # list of dot-path atom lists
  @metadata_keys :: [atom()]            # allowlist
  @forbidden_metadata_keys :: [atom()]  # PII denylist
  defmodule Event do @enforce_keys [:name]; defstruct [:name | metadata_keys] end
  event_names/0, metadata_keys/0, forbidden_metadata_keys/0   # public accessors
  valid_event_name?/1 :: boolean()      # name in @event_names
  new_event/1 :: Event.t()              # raises ArgumentError on unknown name
  metadata/1 :: map()                   # accepts Event.t() | map() | keyword(); reduce-filter, no raise
  to_map/1 :: map()                     # Event.t() -> string-keyed map
  execute/3 :: :ok                      # :telemetry.execute(name, measurements, metadata(metadata))
  # private: normalize_key/1, safe_value?/1, normalize_value/1
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write failing hermetic unit tests for Threadline.Telemetry</name>
  <files>test/crosswake/threadline/telemetry_test.exs</files>
  <read_first>
    - test/crosswake/companions/sigra/telemetry_test.exs (the test pattern to mirror — allowlist assertions, drop-secrets assertion, no-raise execute assertion)
    - lib/crosswake/companions/sigra/telemetry.ex (the module shape under test, so test names/behaviors match)
  </read_first>
  <behavior>
    - event_names/0 returns exactly [[:crosswake, :threadline, :request, :start], [:crosswake, :threadline, :request, :stop], [:crosswake, :threadline, :request, :exception]]
    - metadata_keys/0 returns exactly [:thread_id, :correlation_id, :route_id, :source]
    - forbidden_metadata_keys/0 includes :actor_ref, :access_token, :email, :ip, :user_agent, :session_ref, :subject_ref; and is disjoint from metadata_keys/0
    - metadata/1 keeps only allowlisted keys with safe values and silently drops forbidden keys (e.g. :access_token, :actor_ref, :email) and unknown keys — asserted via map equality, no raise
    - metadata/1 drops a >128-char binary value for an allowlisted key (cardinality bound); keeps a binary ≤128 chars
    - metadata/1 drops nil-valued allowlisted keys (safe_value?(nil) is false)
    - valid_event_name?/1 returns true for a declared name, false for an undeclared name
    - execute/3 with forbidden metadata returns :ok and does not raise (attach a :telemetry handler asserting the forbidden key is absent from received metadata)
  </behavior>
  <action>Create test/crosswake/threadline/telemetry_test.exs mirroring test/crosswake/companions/sigra/telemetry_test.exs. Use `use ExUnit.Case, async: true`, alias Crosswake.Threadline.Telemetry. Assert exact-list equality for event_names/0 and metadata_keys/0 (not just membership) since these are the published contract. Add the drop-secrets map-equality test passing a mix of allowlisted-safe, forbidden (:access_token, :actor_ref, :email, :user_agent), nil-valued, and unknown high-cardinality keys, asserting the result map contains only the allowlisted-safe entries. Add a 129-char binary rejection test and a 128-char acceptance test. For execute/3, attach a :telemetry handler via :telemetry.attach/4 to one declared event name, call Telemetry.execute/3 with forbidden metadata, assert_receive the event, and assert the forbidden key is absent from the handler-received metadata. Detach the handler in an on_exit. Tests MUST fail now because the module does not yet exist.</action>
  <verify>
    <automated>mix test test/crosswake/threadline/telemetry_test.exs 2>&1 | grep -qE "Crosswake.Threadline.Telemetry.*(undefined|is not available|UndefinedFunctionError|CompileError|module .* is not loaded)" && echo RED_OK</automated>
  </verify>
  <acceptance_criteria>
    - test/crosswake/threadline/telemetry_test.exs exists and references Crosswake.Threadline.Telemetry
    - Running the test fails because Crosswake.Threadline.Telemetry is undefined (RED state confirmed)
    - Tests assert exact-list equality for event_names/0 and metadata_keys/0 (not membership-only)
    - A test asserts metadata/1 drops :access_token and :actor_ref silently (no raise)
    - A test asserts a 129-char binary value is rejected and a 128-char value is kept
    - An execute/3 test attaches a :telemetry handler and asserts forbidden keys are absent
  </acceptance_criteria>
  <done>Test file exists, asserts the full published contract + PII rejection + cardinality bound, and currently fails because the module is not yet implemented.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement Crosswake.Threadline.Telemetry mirroring Sigra</name>
  <files>lib/crosswake/threadline/telemetry.ex</files>
  <read_first>
    - lib/crosswake/companions/sigra/telemetry.ex (source-of-truth template — copy safe_value?/1, metadata/1 reduce-filter, execute/3, normalize_key/1, normalize_value/1, the Event inner struct, and all accessors verbatim in shape)
    - test/crosswake/threadline/telemetry_test.exs (the failing tests this task must turn green)
  </read_first>
  <action>Create lib/crosswake/threadline/telemetry.ex defining `Crosswake.Threadline.Telemetry`. Declare `@event_names` with EXACTLY three names: [:crosswake, :threadline, :request, :start], [:crosswake, :threadline, :request, :stop], [:crosswake, :threadline, :request, :exception] (D-07; do NOT pre-declare bridge/activation events — D-08). Declare `@metadata_keys [:thread_id, :correlation_id, :route_id, :source]` (D-11, PROP-02 fixed set). Declare `@forbidden_metadata_keys` as Sigra's verified 19-key list (:access_token, :actor_id, :authorization_code, :credential_id, :device_id, :email, :id_token, :ip, :nonce, :org_id, :passkey_credential_id, :pkce_verifier, :provider_payload, :raw_return_to, :refresh_token, :return_to, :session_ref, :subject_ref, :user_agent) PLUS :actor_ref (RESEARCH A1 — Phase 94's PII-adjacent field must never appear in telemetry). Include the inner `defmodule Event` with `@enforce_keys [:name]` and `defstruct [:name | @metadata_keys]` (Claude's discretion D-11/A2 — include it for consistency with the established Sigra/Chimeway pattern). Provide public accessors event_names/0, metadata_keys/0, forbidden_metadata_keys/0; valid_event_name?/1 (`name in @event_names`); new_event/1 (raises ArgumentError on an unknown name, identical to Sigra). Copy metadata/1 (the three-clause Event/keyword/map reduce-filter that drops forbidden keys, keeps allowlisted keys with safe values, drops everything else — no raise), execute/3 (`:telemetry.execute(name, measurements, metadata(metadata))`), and the private safe_value?/1 (nil->false, atom->true, non-neg integer->true, binary->`String.length(value) <= 128`, else false), normalize_key/1, normalize_value/1 — all verbatim in shape from Sigra. Add a narrow-scope @moduledoc stating this is diagnostic-only correlation telemetry, PII-free, and not an APM/OTel replacement. Zero new dependencies — only :telemetry (already a dep).</action>
  <verify>
    <automated>mix test test/crosswake/threadline/telemetry_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - lib/crosswake/threadline/telemetry.ex contains `@metadata_keys [:thread_id, :correlation_id, :route_id, :source]`
    - @event_names contains EXACTLY the three request-span names and no others
    - @forbidden_metadata_keys contains :actor_ref AND all 19 Sigra keys (20 total)
    - safe_value?/1 implements the nil->false / atom->true / non-neg-int->true / binary ≤128 / else->false rule
    - execute/3 calls :telemetry.execute/3 with metadata routed through metadata/1
    - `mix test test/crosswake/threadline/telemetry_test.exs` exits 0 (all GREEN)
    - `grep -c "otel\|opentelemetry\|OpenTelemetry" lib/crosswake/threadline/telemetry.ex` returns 0 (no OTel dependency)
  </acceptance_criteria>
  <done>Threadline.Telemetry compiles, all unit tests pass, the allowlist is the four PROP-02 keys, the forbidden list includes :actor_ref plus Sigra's 19, and no OTel reference exists.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caller → Threadline.Telemetry.metadata/1 | Arbitrary caller-supplied metadata map crosses into the telemetry bus; untrusted keys/values must be filtered before emission |
| Threadline.Telemetry.execute/3 → :telemetry bus | Only allowlisted, safe-valued, non-forbidden metadata may reach attached handlers |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-91-PII | Information Disclosure | metadata/1 reduce-filter | mitigate | @forbidden_metadata_keys denylist (incl. :actor_ref) drops PII keys silently; verified by the "drops :access_token/:actor_ref" map-equality test and the execute/3 handler-absence test |
| T-91-CARD | Denial of Service (time-series cardinality explosion) | safe_value?/1 | mitigate | binary values bounded to ≤128 chars; verified by the 129-char rejection test |
| T-91-UNK | Information Disclosure | metadata/1 allowlist | mitigate | all-or-nothing allowlist: only @metadata_keys with safe values pass, unknown keys dropped; verified by the unknown-key drop assertion |
| T-91-SC | Tampering | dependency surface | accept | zero new packages installed this phase; only the existing :telemetry dep is used (verified by no-OTel grep) |
</threat_model>

<verification>
- `mix test test/crosswake/threadline/telemetry_test.exs` exits 0.
- `mix test` (full suite) stays green — this plan adds a new module and test only; it touches no existing file.
- `grep -rn "otel\|opentelemetry\|OpenTelemetry" lib/crosswake/threadline/` returns nothing.
</verification>

<success_criteria>
- Crosswake.Threadline.Telemetry exists, compiles, and is covered by hermetic unit tests (PROP-02).
- event_names/0 = exactly three request-span names; metadata_keys/0 = the four PROP-02 keys; forbidden_metadata_keys/0 includes :actor_ref + Sigra's 19.
- Forbidden-key rejection and >128-char rejection are proven without raising.
- No OTel dependency introduced.
</success_criteria>

<output>
Create `.planning/phases/91-identity-telemetry-contract/91-01-SUMMARY.md` when done.
</output>
