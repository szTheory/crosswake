---
phase: "93"
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/crosswake-shell-core-ios/Sources/CrosswakeShellCore/ActivationCoordinator.swift
  - examples/ios_shell_host/CrosswakeShell/LiveViewContainerViewController.swift
autonomous: true
requirements:
  - PROP-05
must_haves:
  truths:
    - "D-01: iOS cold start mints a UUIDv4 thread_id"
    - "D-02: thread_id is scoped to the shell instance"
    - "D-05: Incoming deep links with thread_id override the active thread_id"
    - "The initial WebView URLRequest includes the X-Crosswake-Thread-Id header"
    - "D-03: window.crosswakeBridge.threadId is available to JS before the page loads using WKUserScript atDocumentStart"
  artifacts:
    - path: "packages/crosswake-shell-core-ios/Sources/CrosswakeShellCore/ActivationCoordinator.swift"
      provides: "Thread ID tracking and routing"
    - path: "examples/ios_shell_host/CrosswakeShell/LiveViewContainerViewController.swift"
      provides: "WebView header and script injection"
  key_links:
    - from: "packages/crosswake-shell-core-ios/Sources/CrosswakeShellCore/ActivationCoordinator.swift"
      to: "examples/ios_shell_host/CrosswakeShell/LiveViewContainerViewController.swift"
      via: "LiveViewSession.threadID"
---

<objective>
Update the iOS native shell to propagate `thread_id` across the activation sequence. This ensures full Native → Bridge → Phoenix correlation.
Purpose: Correlate telemetry and audit events deterministically from the native app start.
Output: Core structs accept `threadID`, coordinator mints/tracks it, and the WebView propagates it via HTTP headers and JS injection.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/93-native-shell-propagation/93-CONTEXT.md
@.planning/phases/93-native-shell-propagation/RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: iOS Core Shell Updates</name>
  <files>packages/crosswake-shell-core-ios/Sources/CrosswakeShellCore/ActivationCoordinator.swift</files>
  <action>
    - In `ActivationRequest`: add `public let threadID: String?`, update `CodingKeys` (with `"thread_id"`), and `init`.
    - In `ActivationRequest.forIncomingURL(...)`: parse `thread_id` from the URL query params (`URLComponents(url: url, resolvingAgainstBaseURL: false)?.queryItems?.first(where: { $0.name == "thread_id" })?.value`), falling back to `baseline.threadID`.
    - In `LiveViewSession`: add `public let threadID: String`.
    - In `ActivationCoordinator`: add `public private(set) var currentThreadID: String = Foundation.UUID().uuidString`.
    - In `ActivationCoordinator.activate(_ request: ActivationRequest)`: if `request.threadID` is non-nil, update `self.currentThreadID = request.threadID!`.
    - In `ActivationCoordinator.resolve(...)`: pass `threadID: currentThreadID` when constructing the returned `LiveViewSession`.
    - In `ActivationCoordinator.loadAndFilterRequest()`: pass `threadID: request.threadID` in the returned `ActivationRequest`.
  </action>
  <verify>
    <automated>swift build --package-path packages/crosswake-shell-core-ios</automated>
  </verify>
  <done>Core iOS shell structs support `threadID` and `ActivationCoordinator` maintains the active instance.</done>
</task>

<task type="auto">
  <name>Task 2: iOS Shell Host Updates</name>
  <files>examples/ios_shell_host/CrosswakeShell/LiveViewContainerViewController.swift</files>
  <action>
    - In `LiveViewContainerViewController.loadRouteIfNeeded()`: mutate the `URLRequest` by adding `request.setValue(session.threadID, forHTTPHeaderField: "X-Crosswake-Thread-Id")` before calling `webView.load(request)`.
    - In the `lazy var webView` initialization: add `window.crosswakeBridge.threadId = "\(session.threadID)";` to the injected `scriptSource` inside the `WKUserScript`.
    - In `LiveViewContainerViewController.update(...)`: to avoid script stacking (Pitfall 1), execute `webView.configuration.userContentController.removeAllUserScripts()`, recompute `capabilitiesString`, and re-add the `WKUserScript` with the new capabilities string and `session.threadID` via `addUserScript`. Do NOT remove message handlers (`bridgeChannel`), only user scripts.
  </action>
  <verify>
    <automated>xcodebuild build -project examples/ios_shell_host/CrosswakeShell.xcodeproj -scheme CrosswakeShell -sdk iphonesimulator</automated>
  </verify>
  <done>WebView requests include the `X-Crosswake-Thread-Id` header and `threadId` is injected deterministically at document start.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Native → Web | Injected JavaScript variables must not contain executable code payloads |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-93-01   | Tampering | `WKUserScript` | mitigate | ThreadID is locally minted or parsed from URL. Using standard string interpolation is safe for UUID/URL components, but ensure it is properly quoted. |
</threat_model>

<verification>
Automated builds pass for both the core shell package and the example iOS host app.
</verification>

<success_criteria>
`thread_id` is propagated correctly in the iOS shell via header and `atDocumentStart` JS injection.
</success_criteria>

<output>
Create `.planning/phases/93-native-shell-propagation/93-01-SUMMARY.md` when done
</output>