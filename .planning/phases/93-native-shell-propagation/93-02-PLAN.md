---
phase: "93"
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/crosswake-shell-core-android/src/main/java/dev/crosswake/shell/core/ActivationCoordinator.kt
  - examples/android_shell_host/app/src/main/java/dev/crosswake/shell/LiveViewFragment.kt
  - examples/android_shell_host/app/src/test/java/dev/crosswake/shell/ActivationCoordinatorTest.kt
  - examples/android_shell_host/app/src/test/java/dev/crosswake/shell/BridgeChannelTest.kt
autonomous: true
requirements:
  - PROP-05
must_haves:
  truths:
    - "D-01: Android cold start mints a UUIDv4 thread_id"
    - "D-02: thread_id is scoped to the shell instance"
    - "D-05: Incoming deep links with thread_id override the active thread_id"
    - "The initial WebView loadUrl includes the X-Crosswake-Thread-Id header"
    - "D-04: window.crosswakeBridge.threadId is available to JS before the page loads using WebViewCompat.addDocumentStartJavaScript"
  artifacts:
    - path: "packages/crosswake-shell-core-android/src/main/java/dev/crosswake/shell/core/ActivationCoordinator.kt"
      provides: "Thread ID tracking and routing"
    - path: "examples/android_shell_host/app/src/main/java/dev/crosswake/shell/LiveViewFragment.kt"
      provides: "WebView header and document-start script injection"
  key_links:
    - from: "packages/crosswake-shell-core-android/src/main/java/dev/crosswake/shell/core/ActivationCoordinator.kt"
      to: "examples/android_shell_host/app/src/main/java/dev/crosswake/shell/LiveViewFragment.kt"
      via: "LiveViewSession.threadId"
---

<objective>
Update the Android native shell to propagate `thread_id` across the activation sequence.
Purpose: Correlate telemetry and audit events deterministically from the native app start.
Output: Core structs accept `threadId`, coordinator mints/tracks it, and the WebView propagates it via HTTP headers and `WebViewCompat.addDocumentStartJavaScript` to eliminate race conditions.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/93-native-shell-propagation/93-CONTEXT.md
@.planning/phases/93-native-shell-propagation/RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Android Core Shell Updates</name>
  <files>packages/crosswake-shell-core-android/src/main/java/dev/crosswake/shell/core/ActivationCoordinator.kt</files>
  <action>
    - In `ActivationRequest`: add `val threadId: String? = null`.
    - In `ActivationRequest.forIncomingUrl(...)`: parse `thread_id` from the URL query params (`android.net.Uri.parse(url).getQueryParameter("thread_id")`), falling back to `baseline.threadId`.
    - In `LiveViewSession`: add `val threadId: String`.
    - In `ActivationCoordinator`: add `var currentThreadId: String = java.util.UUID.randomUUID().toString()` (with `private set`).
    - In `ActivationCoordinator.activate(request: ActivationRequest)`: if `request.threadId != null`, update `currentThreadId = request.threadId`.
    - In `ActivationCoordinator.resolve(...)`: pass `threadId = currentThreadId` when constructing the returned `LiveViewSession`.
    - In `ActivationCoordinator.loadActivationRequest(...)`: pass `threadId = json.optString("thread_id").takeIf { it.isNotBlank() }` in the returned `ActivationRequest`.
  </action>
  <verify>
    <automated>./gradlew :packages:crosswake-shell-core-android:compileDebugKotlin</automated>
  </verify>
  <done>Core Android shell structs support `threadId` and `ActivationCoordinator` maintains the active instance.</done>
</task>

<task type="auto">
  <name>Task 2: Android Shell Host Updates</name>
  <files>examples/android_shell_host/app/src/main/java/dev/crosswake/shell/LiveViewFragment.kt, examples/android_shell_host/app/src/test/java/dev/crosswake/shell/ActivationCoordinatorTest.kt, examples/android_shell_host/app/src/test/java/dev/crosswake/shell/BridgeChannelTest.kt</files>
  <action>
    - In `LiveViewFragment.kt`:
      - Add `ARG_THREAD_ID` to companion object constants.
      - Update `LiveViewSession` creation inside `get()` and `newInstance()` to pass `threadId` via `requireArguments().getString(ARG_THREAD_ID).orEmpty()` / `putString`.
      - In `loadRouteIfNeeded`, pass headers when calling `webView.loadUrl(session.url, mapOf("X-Crosswake-Thread-Id" to session.threadId))`.
      - In `configureWebView`, remove the `evaluateJavascript` call from `onPageStarted`.
      - Add `WebViewCompat.addDocumentStartJavaScript(webView, "window.crosswakeBridge = window.crosswakeBridge || {}; window.crosswakeBridge.capabilities = $capabilitiesJson; window.crosswakeBridge.threadId = \"${session.threadId}\";", setOf("*"))` inside `configureWebView` (wrapped in `WebViewFeature.isFeatureSupported(WebViewFeature.DOCUMENT_START_SCRIPT)`). Import `androidx.webkit.WebViewCompat` and `androidx.webkit.WebViewFeature`.
    - In `ActivationCoordinatorTest.kt` and `BridgeChannelTest.kt`: update any explicit constructions of `LiveViewSession(...)` to include `threadId = "test-thread-id"`.
  </action>
  <verify>
    <automated>./gradlew :examples:android_shell_host:app:testDebugUnitTest</automated>
  </verify>
  <done>WebView requests include the `X-Crosswake-Thread-Id` header, `threadId` is injected deterministically at document start, and tests pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Native → Web | Injected JavaScript variables must not contain executable code payloads |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-93-02   | Tampering | `WebViewCompat` | mitigate | ThreadId is locally minted or parsed from URL. Using standard string interpolation is safe for UUID/URL components, but ensure it is properly quoted. |
</threat_model>

<verification>
Automated builds and tests pass for both the core shell package and the example Android host app.
</verification>

<success_criteria>
`thread_id` is propagated correctly in the Android shell via header and document-start JS injection, fixing the previous race condition.
</success_criteria>

<output>
Create `.planning/phases/93-native-shell-propagation/93-02-SUMMARY.md` when done
</output>