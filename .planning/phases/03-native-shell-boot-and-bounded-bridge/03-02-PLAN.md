---
phase: 03-native-shell-boot-and-bounded-bridge
plan: 02
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - lib/mix/tasks/crosswake.gen.shell.ex
  - lib/crosswake/shell/fixtures.ex
  - priv/templates/crosswake/shell/android/settings.gradle.eex
  - priv/templates/crosswake/shell/android/build.gradle.eex
  - priv/templates/crosswake/shell/android/gradle.properties.eex
  - priv/templates/crosswake/shell/android/gradlew.eex
  - priv/templates/crosswake/shell/android/gradlew.bat.eex
  - priv/templates/crosswake/shell/android/gradle/wrapper/gradle-wrapper.properties.eex
  - priv/templates/crosswake/shell/android/app/build.gradle.eex
  - priv/templates/crosswake/shell/android/app/src/main/AndroidManifest.xml.eex
  - test/mix/tasks/crosswake_gen_shell_test.exs
autonomous: true
requirements:
  - SHELL-01
  - SHELL-02
  - MANI-03
must_haves:
  truths:
    - "The shell generator produces canonical shell fixtures and real host-owned generated native baseline assets instead of placeholder source stubs."
    - "Generated projects bundle the manifest-backed activation fixture, declared-pack requirements, and denial contract needed for later boot logic."
    - "Generated output is scaffold-once and host-owned, not a regeneratable pseudo-owned native directory."
  artifacts:
    - path: "lib/mix/tasks/crosswake.gen.shell.ex"
      provides: "Real shell project generation entrypoint"
    - path: "lib/crosswake/shell/fixtures.ex"
      provides: "Canonical manifest and activation fixture export for generated shells"
    - path: "priv/templates/crosswake/shell/android/app/src/main/AndroidManifest.xml.eex"
      provides: "Real Android app manifest and entry wiring template"
  key_links:
    - from: "lib/mix/tasks/crosswake.gen.shell.ex"
      to: "lib/crosswake/shell/fixtures.ex"
      via: "generator exports the same manifest-backed fixture shape both platforms boot from"
      pattern: "fixtures|manifest|activation|pack"
    - from: "lib/mix/tasks/crosswake.gen.shell.ex"
      to: "priv/templates/crosswake/shell/android/gradlew.eex"
      via: "Android generation renders a host-owned Gradle project rather than placeholder Kotlin files"
      pattern: "settings.gradle|build.gradle|gradlew|AndroidManifest"
---

<objective>
Upgrade shell generation from placeholder scaffolds to canonical fixture export plus real generator-owned native baseline assets that carry the shared activation contract and exported manifest truth forward.

Purpose: Implement D-23 through D-27 without claiming runtime behavior that the platform slices have not yet wired.
Output: Shell fixture export helpers, upgraded generator behavior, Android baseline project templates, and tests that assert host-owned scaffold output.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-native-shell-boot-and-bounded-bridge/03-CONTEXT.md
@.planning/phases/03-native-shell-boot-and-bounded-bridge/03-RESEARCH.md
@.planning/phases/03-native-shell-boot-and-bounded-bridge/03-PATTERNS.md
@.planning/phases/03-native-shell-boot-and-bounded-bridge/03-01-PLAN.md
@lib/mix/tasks/crosswake.gen.shell.ex
@test/mix/tasks/crosswake_gen_shell_test.exs

<interfaces>
From lib/mix/tasks/crosswake.gen.shell.ex:
```elixir
@switches [target: :string]
def run(args) do
  {opts, argv, invalid} = OptionParser.parse(args, strict: @switches)
end
```

From lib/crosswake/manifest/manifest.ex:
```elixir
@spec render(Types.Root.t()) :: String.t()
```

From lib/crosswake/shell/activation.ex:
```elixir
# Plan 03-01 establishes the activation request and denial contract consumed here.
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Export canonical shell fixtures and upgrade the generator contract</name>
  <read_first>.planning/phases/03-native-shell-boot-and-bounded-bridge/03-CONTEXT.md, .planning/phases/03-native-shell-boot-and-bounded-bridge/03-RESEARCH.md, .planning/phases/03-native-shell-boot-and-bounded-bridge/03-PATTERNS.md, .planning/phases/03-native-shell-boot-and-bounded-bridge/03-01-PLAN.md, lib/mix/tasks/crosswake.gen.shell.ex, lib/crosswake/manifest/manifest.ex, test/mix/tasks/crosswake_gen_shell_test.exs</read_first>
  <files>lib/mix/tasks/crosswake.gen.shell.ex, lib/crosswake/shell/fixtures.ex, test/mix/tasks/crosswake_gen_shell_test.exs</files>
  <behavior>
    - Test 1: `mix crosswake.gen.shell ios|android` produces a real project layout plus bundled manifest and activation fixtures instead of Phase 1 placeholder notes per D-23.
    - Test 2: The generator summary output states the shell is host-owned, scaffold-once, and not safely regeneratable over host edits per D-26.
    - Test 3: Fixture export derives from canonical manifest truth and the shared activation contract rather than an ad hoc shell-only JSON shape per D-05 and D-27.
  </behavior>
  <action>Create `Crosswake.Shell.Fixtures` to render bundled manifest, route activation, declared-pack requirement, installed-pack inventory fixture, and denial fixtures from existing manifest and compatibility truth so later iOS and Android templates can boot the same shape. Upgrade `mix crosswake.gen.shell` to call that export layer and to generate real project roots for both platforms, while preserving the strict CLI parse and explicit summary pattern already used in this task module. Implement D-23 through D-27 directly: generate host-owned projects that match what adopters ship, say clearly that Crosswake scaffolds once and does not re-own host-edited native apps, and remove the current Phase 1 placeholder scaffold language from generated artifacts.</action>
  <verify>
    <automated>mix test test/mix/tasks/crosswake_gen_shell_test.exs</automated>
  </verify>
  <acceptance_criteria>
    <automated>test -f lib/crosswake/shell/fixtures.ex && test -f test/mix/tasks/crosswake_gen_shell_test.exs</automated>
    <automated>rg -n 'host-owned|scaffold once|manifest|activation|denial|not regenerate|real project' lib/mix/tasks/crosswake.gen.shell.ex lib/crosswake/shell/fixtures.ex test/mix/tasks/crosswake_gen_shell_test.exs</automated>
    <automated>mix test test/mix/tasks/crosswake_gen_shell_test.exs</automated>
  </acceptance_criteria>
  <done>The generator contract now exports real shell boot fixtures and states the native ownership model honestly.</done>
</task>

<task type="auto">
  <name>Task 2: Replace placeholder Android templates with a real host-owned baseline</name>
  <read_first>.planning/phases/03-native-shell-boot-and-bounded-bridge/03-CONTEXT.md, .planning/phases/03-native-shell-boot-and-bounded-bridge/03-RESEARCH.md, priv/templates/crosswake/shell/ios/README.md.eex, priv/templates/crosswake/shell/android/README.md.eex, lib/mix/tasks/crosswake.gen.shell.ex</read_first>
  <files>priv/templates/crosswake/shell/android/settings.gradle.eex, priv/templates/crosswake/shell/android/build.gradle.eex, priv/templates/crosswake/shell/android/gradle.properties.eex, priv/templates/crosswake/shell/android/gradlew.eex, priv/templates/crosswake/shell/android/gradlew.bat.eex, priv/templates/crosswake/shell/android/gradle/wrapper/gradle-wrapper.properties.eex, priv/templates/crosswake/shell/android/app/build.gradle.eex, priv/templates/crosswake/shell/android/app/src/main/AndroidManifest.xml.eex, test/mix/tasks/crosswake_gen_shell_test.exs</files>
  <action>Add the minimum real Android Studio baseline needed before runtime wiring in Plan 03-04: root Gradle files, Gradle wrapper files, app build file, and manifest wiring per D-23 and D-25. Wire the generated Android scaffold to bundle the exported manifest, activation, denial, and pack-compatibility fixtures from Task 1, while keeping the template intentionally thin per D-01 through D-03: app boot seam, manifest fixture loading, navigation root, and route-unavailable surface seams only, with no offline hosts, pack managers, or broad plugin registries. Update `test/mix/tasks/crosswake_gen_shell_test.exs` so the generated artifact inventory asserts these are real Android project files and no longer accepts the old placeholder `Sources/AppShell.swift` or Phase 1-only fixture wording.</action>
  <verify>
    <automated>mix test test/mix/tasks/crosswake_gen_shell_test.exs</automated>
  </verify>
  <acceptance_criteria>
    <automated>test -f priv/templates/crosswake/shell/android/settings.gradle.eex && test -f priv/templates/crosswake/shell/android/build.gradle.eex && test -f priv/templates/crosswake/shell/android/gradle.properties.eex && test -f priv/templates/crosswake/shell/android/gradlew.eex && test -f priv/templates/crosswake/shell/android/gradle/wrapper/gradle-wrapper.properties.eex && test -f priv/templates/crosswake/shell/android/app/build.gradle.eex && test -f priv/templates/crosswake/shell/android/app/src/main/AndroidManifest.xml.eex</automated>
    <automated>rg -n 'applicationId|usesCleartextTraffic|gradlew|gradle-wrapper|host-owned' priv/templates/crosswake/shell/android/settings.gradle.eex priv/templates/crosswake/shell/android/build.gradle.eex priv/templates/crosswake/shell/android/gradle.properties.eex priv/templates/crosswake/shell/android/gradlew.eex priv/templates/crosswake/shell/android/gradle/wrapper/gradle-wrapper.properties.eex priv/templates/crosswake/shell/android/app/build.gradle.eex priv/templates/crosswake/shell/android/app/src/main/AndroidManifest.xml.eex</automated>
    <automated>mix test test/mix/tasks/crosswake_gen_shell_test.exs</automated>
  </acceptance_criteria>
  <done>The repo contains a real host-owned Android project baseline plus the shared generator output that later platform plans can wire for runtime behavior.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| generator inputs -> generated native projects | Incorrect scaffolding can encode dishonest ownership or unsafe defaults into host-owned apps |
| canonical manifest truth -> bundled shell fixtures | Drift here would let generated shells boot from parallel truth |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-05 | T | `lib/crosswake/shell/fixtures.ex` | mitigate | Export bundled shell fixtures from canonical manifest and activation truth so templates cannot drift into shell-only config |
| T-03-06 | R | `lib/mix/tasks/crosswake.gen.shell.ex` | mitigate | Print scaffold-once and host-owned ownership language per D-26 so generated apps do not imply lifecycle re-ownership |
| T-03-07 | E | native app templates | mitigate | Keep templates thin and fail-closed, carrying only app boot, manifest load, and denial seams required for Phase 3 |
| T-03-08 | D | generated project layout | mitigate | Replace Phase 1 placeholders with real project baselines per D-23 through D-25 so adopters and proof lanes exercise the same artifact class |
</threat_model>

<verification>
Run `mix test test/mix/tasks/crosswake_gen_shell_test.exs` and confirm generated shells now produce real project files, bundled manifest fixtures, and explicit host-owned ownership language.
</verification>

<success_criteria>
- The shell generator emits real iOS and Android app project baselines.
- Bundled shell fixtures derive from canonical manifest and activation truth.
- Generated output is honest about one-time scaffolding and host ownership.
</success_criteria>

<output>
After completion, create `.planning/phases/03-native-shell-boot-and-bounded-bridge/03-native-shell-boot-and-bounded-bridge-02-SUMMARY.md`
</output>
