---
phase: 03-native-shell-boot-and-bounded-bridge
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/crosswake/shell/activation.ex
  - lib/crosswake/shell/denial.ex
  - lib/crosswake/compatibility/compatibility.ex
  - lib/crosswake/compatibility/route_gate.ex
  - test/crosswake/shell/activation_test.exs
  - test/crosswake/compatibility/compatibility_test.exs
autonomous: true
requirements:
  - MANI-03
  - SHELL-03
  - BRDG-03
must_haves:
  truths:
    - "Every app entry path is normalized into one typed activation request before any web container loads."
    - "Route activation fails closed on manifest, capability, bridge, and declared pack-compatibility mismatches instead of silently falling back to a generic container."
    - "Route and bridge enforcement share one denial vocabulary that native shells, doctor output, and tests can assert directly."
  artifacts:
    - path: "lib/crosswake/shell/activation.ex"
      provides: "Typed activation request and manifest-first route resolution"
    - path: "lib/crosswake/shell/denial.ex"
      provides: "Stable denial codes and machine-readable denial payloads"
    - path: "lib/crosswake/compatibility/route_gate.ex"
      provides: "Fail-closed route decisions that reuse the shared denial vocabulary"
  key_links:
    - from: "lib/crosswake/shell/activation.ex"
      to: "lib/crosswake/compatibility/route_gate.ex"
      via: "activation resolves route requests through the existing fail-closed gate"
      pattern: "ActivationRequest|RouteGate|deny|allow"
    - from: "lib/crosswake/compatibility/compatibility.ex"
      to: "lib/crosswake/shell/denial.ex"
      via: "compatibility findings map into stable denial reasons"
      pattern: "inactive_route|origin_denied|compatibility_mismatch|undeclared_capability|pack_incompatible"
---

<objective>
Define the shared activation and denial contract first so every later shell and bridge slice consumes one manifest-first, native-first enforcement surface.

Purpose: Implement D-05 through D-10 and D-17 through D-22 before any platform-specific runtime work starts.
Output: Shared activation structs, shared denial vocabulary, fail-closed route-gate wiring, and tests that prove the contract.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-native-shell-boot-and-bounded-bridge/03-CONTEXT.md
@.planning/phases/03-native-shell-boot-and-bounded-bridge/03-RESEARCH.md
@.planning/phases/03-native-shell-boot-and-bounded-bridge/03-PATTERNS.md
@lib/crosswake/manifest/types.ex
@lib/crosswake/compatibility/compatibility.ex
@lib/crosswake/compatibility/route_gate.ex

<interfaces>
From lib/crosswake/compatibility/compatibility.ex:
```elixir
defmodule Target do
  defstruct [
    :manifest_schema_version,
    :bridge_protocol_version,
    :native_runtime_version,
    :origin,
    packs: %{},
    manifest_source: :bundled,
    capabilities: %{}
  ]
end
```

From lib/crosswake/compatibility/route_gate.ex:
```elixir
@spec evaluate(Root.t(), String.t(), Target.t()) :: Decision.t()
```

From lib/crosswake/manifest/types.ex:
```elixir
defmodule RouteEntry do
  @enforce_keys [:id, :path, :runtime]
  defstruct [..., capabilities: [], allowlisted_origins: []]
end
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create the typed native activation contract and denial model</name>
  <read_first>.planning/phases/03-native-shell-boot-and-bounded-bridge/03-CONTEXT.md, .planning/phases/03-native-shell-boot-and-bounded-bridge/03-RESEARCH.md, .planning/phases/03-native-shell-boot-and-bounded-bridge/03-PATTERNS.md, lib/crosswake/manifest/types.ex, lib/crosswake/compatibility/compatibility.ex, lib/crosswake/doctor/check.ex</read_first>
  <files>lib/crosswake/shell/activation.ex, lib/crosswake/shell/denial.ex, test/crosswake/shell/activation_test.exs</files>
  <behavior>
    - Test 1: Cold start, deep link, notification, and in-app navigation all normalize into one `ActivationRequest` shape before runtime mount per D-05.
    - Test 2: Activation results are explicit `allow` or `deny`, never "load the web container and figure it out later" per D-06, D-08, and D-17.
    - Test 3: Denials expose stable product-level reasons including compatibility mismatch, undeclared or unavailable capability, origin denied, inactive route, and declared-pack incompatibility per D-18 and D-19.
  </behavior>
  <action>Create `Crosswake.Shell.Activation` and `Crosswake.Shell.Denial` as the shared Phase 3 contract modules, following the typed-struct and `to_map/1` style already used in `Crosswake.Manifest.Types`. Encode D-05, D-06, and D-15 by making `ActivationRequest` carry route id or URL, source, origin, manifest source, bridge version, native runtime version, declared-pack requirements, installed-pack inventory, capabilities, and correlation identity before any container load. Encode D-10 and D-18 through D-20 by giving `Crosswake.Shell.Denial` one stable reason vocabulary and machine-readable details that both native route-unavailable screens and bridge denial replies can reuse, including a specific `pack_incompatible` denial for MANI-03. Do not add Phase 4 or Phase 5 concepts such as pack UI, offline journals, or broad native-screen registries per D-03.</action>
  <verify>
    <automated>mix test test/crosswake/shell/activation_test.exs</automated>
  </verify>
  <acceptance_criteria>
    <automated>test -f lib/crosswake/shell/activation.ex && test -f lib/crosswake/shell/denial.ex && test -f test/crosswake/shell/activation_test.exs</automated>
    <automated>rg -n 'ActivationRequest|correlation|origin|manifest_source|compatibility_mismatch|undeclared_capability|origin_denied|inactive_route|pack_incompatible' lib/crosswake/shell/activation.ex lib/crosswake/shell/denial.ex test/crosswake/shell/activation_test.exs</automated>
    <automated>mix test test/crosswake/shell/activation_test.exs</automated>
  </acceptance_criteria>
  <done>One typed activation request and one denial envelope exist as the canonical Phase 3 entry contract.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Rewire compatibility and route gating onto the shared fail-closed denial surface</name>
  <read_first>.planning/phases/03-native-shell-boot-and-bounded-bridge/03-CONTEXT.md, .planning/phases/03-native-shell-boot-and-bounded-bridge/03-PATTERNS.md, lib/crosswake/shell/activation.ex, lib/crosswake/shell/denial.ex, lib/crosswake/compatibility/compatibility.ex, lib/crosswake/compatibility/route_gate.ex, test/crosswake/compatibility/compatibility_test.exs</read_first>
  <files>lib/crosswake/compatibility/compatibility.ex, lib/crosswake/compatibility/route_gate.ex, test/crosswake/compatibility/compatibility_test.exs</files>
  <behavior>
    - Test 1: Route-gate evaluation returns denial codes mapped from compatibility findings instead of free-form strings per D-18 and D-22.
    - Test 2: Deep-link activation denial happens before runtime boot and exposes only safe recovery metadata per D-20.
    - Test 3: In-app navigation denial keeps the current route stable and reports an interrupting denial reason instead of partial navigation per D-21, including declared-pack incompatibility when the shell lacks a required compatible pack per MANI-03.
  </behavior>
  <action>Extend `Crosswake.Compatibility` and `Crosswake.Compatibility.RouteGate` to consume the shared denial vocabulary from Task 1 and to distinguish activation context such as deep link versus in-app navigation per D-20 and D-21. Preserve the existing route-first, fail-closed posture from Phase 2, but replace the current string-only reasons with typed denial payloads that native shells can render directly and bridge enforcement can reuse later. Implement MANI-03 explicitly by checking each route's declared pack requirements against the shell target's installed-pack inventory and compatibility versions before activation; if a required pack is missing or incompatible, deny activation with the shared `pack_incompatible` reason and recovery metadata instead of attempting route boot. Implement D-17 and D-22 explicitly: denied routes do not return partial success state and bridge-side denial mapping must remain side-effect-free and directly assertable in tests.</action>
  <verify>
    <automated>mix test test/crosswake/compatibility/compatibility_test.exs test/crosswake/shell/activation_test.exs</automated>
  </verify>
  <acceptance_criteria>
    <automated>rg -n 'denial|inactive_route|origin_denied|compatibility_mismatch|pack_incompatible|safe_fallback|deep_link|in_app' lib/crosswake/compatibility/compatibility.ex lib/crosswake/compatibility/route_gate.ex test/crosswake/compatibility/compatibility_test.exs</automated>
    <automated>mix test test/crosswake/compatibility/compatibility_test.exs test/crosswake/shell/activation_test.exs</automated>
  </acceptance_criteria>
  <done>Compatibility and route-gate logic now produce one stable, fail-closed denial surface for all Phase 3 activation paths.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| app entrypoints -> activation contract | Untrusted URLs, route ids, and origins cross into native activation logic here |
| compatibility findings -> native denial UI | If denial reasons drift, shells can present misleading or permissive recovery paths |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | S | `lib/crosswake/shell/activation.ex` | mitigate | Normalize every app entry into one typed request carrying origin, route identity, and manifest source per D-05 and D-15 before any runtime loads |
| T-03-02 | T | `lib/crosswake/compatibility/route_gate.ex` | mitigate | Reuse shared denial codes and explicit allow or deny decisions so later shells cannot reinterpret failed checks as safe fallbacks |
| T-03-03 | R | `lib/crosswake/shell/denial.ex` | mitigate | Give denials stable reason codes and details so doctor, tests, and native surfaces can prove why activation was blocked |
| T-03-04 | E | route activation pipeline | mitigate | Keep activation fail-closed and pre-container per D-06, D-08, and D-17 so web content never gains authority to escalate routing |
</threat_model>

<verification>
Run `mix test test/crosswake/shell/activation_test.exs test/crosswake/compatibility/compatibility_test.exs` and confirm every activation path is typed, pre-container, and deny-first.
</verification>

<success_criteria>
- Phase 3 has one typed activation request and one typed denial envelope.
- Route-gate results are machine-readable and reusable across shells and bridge denials.
- No activation path implies generic-container fallback on failure.
</success_criteria>

<output>
After completion, create `.planning/phases/03-native-shell-boot-and-bounded-bridge/03-native-shell-boot-and-bounded-bridge-01-SUMMARY.md`
</output>
