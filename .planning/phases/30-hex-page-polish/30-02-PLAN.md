---
phase: 30-hex-page-polish
plan: 02
type: execute
wave: 2
depends_on: ["01"]
files_modified: ["script/verify_hex_tarball.sh"]
autonomous: true
requirements: [HEX-02, PRF-01]

must_haves:
  truths:
    - "Hex tarball contains only allowlisted files"
    - "Internal/development directories are excluded from the tarball"
    - "mix hex.publish dry run succeeds without warnings"
  artifacts:
    - path: "script/verify_hex_tarball.sh"
      provides: "Automated test for tarball contents"
      min_lines: 10
  key_links:
    - from: "script/verify_hex_tarball.sh"
      to: "mix hex.build"
      via: "shell execution"
      pattern: "mix hex\\.build --unpack"
---

<objective>
Validate the contents of the Hex distribution tarball and verify the publish pre-flight checks pass.

Purpose: Guarantee that no internal or private project files (e.g. `.planning`, `examples`) leak into the published Hex package, and that `mix hex.publish` will succeed.
Output: A new validation script and confirmation of a clean dry-run.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/30-hex-page-polish/30-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Hex tarball validation script</name>
  <files>script/verify_hex_tarball.sh</files>
  <action>Create an automated bash script `script/verify_hex_tarball.sh` that validates the `.tar` contents (per D-05, D-06).
1. The script should run `mix hex.build --unpack`.
2. It must explicitly assert that banned paths (`.planning`, `prompts`, `test`, `examples`, `AGENTS.md`, `script`, `.github`, `native`) DO NOT exist inside the unpacked `crosswake-*/` directory.
3. It must verify that expected paths (`lib/`, `priv/`, `guides/`, `mix.exs`, `README.md`) DO exist.
4. Exit 1 if validation fails, Exit 0 if successful.
Ensure the script is marked executable (`chmod +x`).</action>
  <verify>
    <automated>./script/verify_hex_tarball.sh</automated>
  </verify>
  <done>Script successfully runs hex.build --unpack and verifies no leaked directories exist.</done>
</task>

<task type="auto">
  <name>Task 2: Run dry-run publish validation</name>
  <files>None</files>
  <action>Execute `mix hex.publish --dry-run --yes` to validate PRF-01. This ensures the package passes metadata, license, and preflight checks without actually publishing.</action>
  <verify>
    <automated>mix hex.publish --dry-run --yes</automated>
  </verify>
  <done>The dry-run completes with a 0 exit code and reports no errors.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Build Output -> Distribution | Building the tarball packages local files for public distribution. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-30-02 | Information Disclosure | `mix hex.build` | mitigate | Explicit `:files` allowlist in `mix.exs` prevents accidental leakage of `.planning` or `prompts` directories. Assessed locally by `verify_hex_tarball.sh`. |
</threat_model>

<verification>
`./script/verify_hex_tarball.sh` exits 0 and `mix hex.publish --dry-run --yes` exits 0.
</verification>

<success_criteria>
Tarball strictly matches the allowlist and dry-run publish proves readiness for the final publish phase.
</success_criteria>

<output>
After completion, create `.planning/phases/30-hex-page-polish/30-02-SUMMARY.md`
</output>
