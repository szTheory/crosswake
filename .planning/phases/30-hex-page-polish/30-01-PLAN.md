---
phase: 30-hex-page-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: ["README.md", "mix.exs"]
autonomous: true
requirements: [HEX-01, HEX-03]

must_haves:
  truths:
    - "README.md uses absolute GitHub URLs for repository-only files"
    - "README.md uses relative URLs for Hex-packaged guides"
    - "Mix docs sidebar is logically grouped into modules and extras"
  artifacts:
    - path: "README.md"
      provides: "Landing page for HexDocs"
      min_lines: 10
    - path: "mix.exs"
      provides: "ex_doc configuration with groups_for_modules and groups_for_extras"
      contains: "groups_for_modules:"
  key_links:
    - from: "README.md"
      to: "GitHub"
      via: "absolute URL for examples"
      pattern: "https://github.com/szTheory/crosswake"
---

<objective>
Polish the documentation configuration for HexDocs to provide an adopter-ready landing page and sidebar.

Purpose: Guarantee that HexDocs viewers see cleanly grouped sidebars and don't encounter dead links when clicking references to repo-only files.
Output: Rewritten `README.md` and grouped `mix.exs` docs configuration.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/30-hex-page-polish/30-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite non-package links in README.md</name>
  <files>README.md</files>
  <action>Rewrite all markdown links in `README.md` that point to non-packaged paths (e.g., `examples/...`, `AGENTS.md`, `prompts/...`, `script/...`) to be absolute URLs pointing to `https://github.com/szTheory/crosswake/blob/main/path` (per D-02). 

Reference the `README.md` analog pattern from 30-PATTERNS.md ("Absolute links to internal/excluded files pattern"):
```markdown
- [examples/phoenix_host/README.md](https://github.com/szTheory/crosswake/blob/main/examples/phoenix_host/README.md)
```

CRITICAL: Strictly keep links to `guides/` as relative (e.g., `[Install](guides/install.md)`), because `ex_doc` natively resolves these (per D-01).

Reference the `README.md` analog pattern from 30-PATTERNS.md ("Relative links to guides pattern"):
```markdown
- [guides/user_flows.md](guides/user_flows.md) for the fastest JTBD and user-flow ramp-up
```</action>
  <verify>
    <automated>grep -E '\[.*\]\((examples/|AGENTS.md|prompts/|script/)' README.md ; test $? -eq 1</automated>
  </verify>
  <done>No relative links to repository-only files remain in README.md, but guides/ remain relative.</done>
</task>

<task type="auto">
  <name>Task 2: Configure ex_doc grouping in mix.exs</name>
  <files>mix.exs</files>
  <action>Update `mix.exs` `docs/0` configuration to logically organize the sidebar (per D-03, D-04). 
1. Add `groups_for_modules` and group modules logically (e.g., `:Policy`, `:Bridge`, `:Manifest`, `:Capabilities`).
2. Subdivide the existing `groups_for_extras` to cluster `guides/` cleanly (e.g., `:Setup` for install/support_matrix, `:Capabilities` for capabilities/commerce/offline/packs, `:Reference` for others). Ensure no guides are omitted from being matched.

Reference the `mix.exs` analog pattern from 30-PATTERNS.md ("Docs configuration pattern"):
```elixir
      groups_for_extras: [
        Guides: ~r/guides\//
      ]
```
Expand this pattern for Phase 30 by replacing it with grouped extras and adding `groups_for_modules` to group the sidebar elements.</action>
  <verify>
    <automated>mix docs 2>&1 | grep -q "warning:" ; test $? -eq 1</automated>
  </verify>
  <done>mix docs compiles cleanly with zero warnings, and groups_for_modules exists in mix.exs.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Public Docs -> Repo | Docs are rendered externally; references to internal files cross this boundary via HTTP links. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-30-01 | Information Disclosure | `README.md` | accept | The README publicizes paths to repo-only files (examples, scripts), which are fully public on GitHub anyway. No secrets exposed. |
</threat_model>

<verification>
`mix docs` completes cleanly, and grep confirms no 404-prone relative links remain in `README.md`.
</verification>

<success_criteria>
`mix.exs` has robust groups for modules and extras. `README.md` links are safe for HexDocs.
</success_criteria>

<output>
After completion, create `.planning/phases/30-hex-page-polish/30-01-SUMMARY.md`
</output>
