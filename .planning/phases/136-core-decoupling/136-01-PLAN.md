---
phase: 136-core-decoupling
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/crosswake/companion.ex
  - test/crosswake/proof/phase129_companion_contract_freeze_test.exs
  - test/crosswake/proof/phase136_decouple_proof_test.exs
autonomous: true
requirements: [DECOUPLE-01, DECOUPLE-02, DECOUPLE-03, DECOUPLE-04, DECOUPLE-05, DECOUPLE-06]
must_haves:
  truths:
    - "The Companion behaviour declares four new optional callbacks (forbidden_metadata_keys/0, denial_codes/0, evaluate_auth/3, auth_authority?/0) alongside the existing telemetry_events/0."
    - "The Phase-129 companion-contract freeze test passes with the new 11-callback expected set."
    - "The five backstop tests exist and encode fail-closed + baseline-PII + attach-time-capture behavior (RED until Wave 1/2 land)."
  artifacts:
    - "lib/crosswake/companion.ex (four new @callback + expanded @optional_callbacks)"
    - "test/crosswake/proof/phase136_decouple_proof_test.exs (five backstop tests)"
    - "test/crosswake/proof/phase129_companion_contract_freeze_test.exs (updated @expected_callbacks)"
  key_links:
    - "companion.ex @optional_callbacks keyword list -> function_exported?/3 dispatch in every inversion site."
    - "@expected_callbacks MapSet in freeze test -> the exact callback shape the behaviour freezes."
---

<objective>
Extend the `Crosswake.Companion` behaviour with the four optional callbacks the inversion
sites (Wave 1/2) dispatch through, update the Phase-129 freeze test to match the new shape in
the same commit, and scaffold the five Nyquist backstop tests that pin the phase's
not-yet-inferable behaviors.

Purpose: The behaviour additions are the foundation every downstream inversion depends on
(`function_exported?/3` targets these arities). The freeze test asserts `MapSet.equal?` on the
exact callback set and breaks the moment the behaviour changes, so both files must move together
(D-12/D-17 "reviewer sees intentional shape change"). Backstop tests belong in Wave 0/1 per
Nyquist so later waves turn them GREEN rather than authoring verification after the fact.

Output: A compiling behaviour with 11 callbacks (7 existing + 4 new, all four new optional), a
passing freeze test, and a RED backstop test file whose assertions the Wave 1/2 plans satisfy.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/136-core-decoupling/136-CONTEXT.md
@.planning/phases/136-core-decoupling/136-RESEARCH.md
</context>

<artifacts_produced>
## Artifacts this phase produces (Plan 01 subset)

New `@callback` declarations in `lib/crosswake/companion.ex`:
- `forbidden_metadata_keys() :: [atom()]`
- `denial_codes() :: [String.t()]`
- `evaluate_auth(route :: RouteEntry.t(), auth_context :: map(), opts :: keyword()) :: {:allow, map()} | {:deny, Crosswake.Shell.Denial.t()}`
- `auth_authority?() :: boolean()`

Expanded `@optional_callbacks` keyword list: `telemetry_events: 0, forbidden_metadata_keys: 0, denial_codes: 0, evaluate_auth: 3, auth_authority?: 0`.

New test file `test/crosswake/proof/phase136_decouple_proof_test.exs` (five backstop tests; the
Wave 1/2 plans make them GREEN).

Updated `@expected_callbacks` MapSet in `phase129_companion_contract_freeze_test.exs` (7 -> 11).
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: Extend Companion behaviour with four optional callbacks</name>
  <files>lib/crosswake/companion.ex</files>
  <read_first>
    - lib/crosswake/companion.ex (the whole behaviour; the existing `@callback telemetry_events() :: [Crosswake.Telemetry.event_doc()]` at ~L141 and `@optional_callbacks telemetry_events: 0` at ~L143 are the exact template to copy)
    - .planning/phases/136-core-decoupling/136-RESEARCH.md ("`companion.ex` — Callback Shape Ground Truth" section: exact signatures for the four callbacks)
  </read_first>
  <action>
    Add four `@callback` declarations directly above the `@optional_callbacks` line, each with a
    short moduledoc-style `@doc` following the tone of the existing `telemetry_events/0` doc.
    Names and arities exactly: `forbidden_metadata_keys/0` returning `[atom()]`;
    `denial_codes/0` returning `[String.t()]`; `evaluate_auth/3` taking
    `(route :: Crosswake.Manifest.Types.RouteEntry.t(), auth_context :: map(), opts :: keyword())`
    and returning `{:allow, map()} | {:deny, Crosswake.Shell.Denial.t()}`; `auth_authority?/0`
    returning `boolean()`. Replace the single-entry `@optional_callbacks telemetry_events: 0`
    with the full keyword list `telemetry_events: 0, forbidden_metadata_keys: 0, denial_codes: 0,
    evaluate_auth: 3, auth_authority?: 0`. Add the RouteEntry alias/reference if the type is not
    already resolvable in this module. Do NOT overload `route_gated?/2`; these are dedicated
    callbacks (D-136-B). Do NOT add any implementation or default — behaviour declarations only.
  </action>
  <verify>
    <automated>mix compile --warnings-as-errors 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - Source: `lib/crosswake/companion.ex` contains `@callback forbidden_metadata_keys()`, `@callback denial_codes()`, `@callback evaluate_auth(`, and `@callback auth_authority?()`.
    - Source: `@optional_callbacks` line lists all five callback arities (telemetry_events, forbidden_metadata_keys, denial_codes, evaluate_auth, auth_authority?).
    - `mix compile --warnings-as-errors` exits 0.
  </acceptance_criteria>
  <done>Behaviour compiles clean with 11 declared callbacks, four of them newly optional.</done>
</task>

<task type="auto">
  <name>Task 2: Update Phase-129 freeze test to the 11-callback expected set</name>
  <files>test/crosswake/proof/phase129_companion_contract_freeze_test.exs</files>
  <read_first>
    - test/crosswake/proof/phase129_companion_contract_freeze_test.exs (the `@expected_callbacks MapSet.new([...])` at ~L21-29, currently exactly 7 entries)
    - .planning/phases/136-core-decoupling/136-RESEARCH.md ("The Phase-129 Freeze Test to Update" — the exact four tuples to add)
  </read_first>
  <action>
    Add the four new callback tuples to `@expected_callbacks`: `{:forbidden_metadata_keys, 0}`,
    `{:denial_codes, 0}`, `{:evaluate_auth, 3}`, `{:auth_authority?, 0}`. Keep the existing seven
    tuples unchanged. Add an inline comment marking the four as "Added in Phase 136 (DECOUPLE)".
    Do not weaken the `MapSet.equal?` assertion — the freeze is intentional; it must now freeze
    at 11 callbacks.
  </action>
  <verify>
    <automated>mix test test/crosswake/proof/phase129_companion_contract_freeze_test.exs 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - Source: `@expected_callbacks` contains all four new tuples and still contains the original seven.
    - Behavior: `mix test test/crosswake/proof/phase129_companion_contract_freeze_test.exs` passes (0 failures).
  </acceptance_criteria>
  <done>Freeze test asserts the exact 11-callback contract and passes.</done>
</task>

<task type="auto">
  <name>Task 3: Scaffold the five Nyquist backstop tests</name>
  <files>test/crosswake/proof/phase136_decouple_proof_test.exs</files>
  <read_first>
    - .planning/phases/136-core-decoupling/136-RESEARCH.md ("Backstop Tests Required (Non-Inferable from Existing Suite)" — the five behaviors, and "Validation Architecture" test map)
    - .planning/phases/136-core-decoupling/136-VALIDATION.md ("Wave 0 Requirements" — the five backstops enumerated)
    - test/crosswake/proof/phase130_fail_closed_contract_test.exs (existing stub-companion registration + Application.put_env(:crosswake, :companions, [...]) setup pattern to reuse for stubs)
    - test/crosswake/proof/phase133_telemetry_contract_test.exs (existing stub-seeded companion merge test pattern)
  </read_first>
  <action>
    Create `test/crosswake/proof/phase136_decouple_proof_test.exs` with five tests, one per
    backstop behavior. Reuse the existing `Application.put_env(:crosswake, :companions, [...])`
    stub-registration pattern (with `on_exit` reset) from the phase130/phase133 proof tests —
    do NOT invent a new registry harness. The five tests:
    (1) DECOUPLE-04 — a stub companion whose `evaluate_auth/3` raises, registered as the auth
        authority; evaluating an auth-predicated route yields a `:deny` decision (denial reason
        `:dependency_missing`; the executor may also accept a dedicated `:auth_evaluator_error`
        atom — pin whichever Plan 03 implements).
    (2) DECOUPLE-01 — with `companions: []`, `Crosswake.Telemetry.events/0` returns a non-empty
        list AND the `:reserved`-tier subset is empty.
    (3) DECOUPLE-04 — two stub companions both returning `auth_authority?/0 == true`; the
        first-registered one's `evaluate_auth/3` is used, and a doctor/telemetry conflict signal
        is observable (assert the first-registered decision wins; pin the exact signal to Plan 03).
    (4) DECOUPLE-05 — `Crosswake.Telemetry.baseline_forbidden_metadata_keys/0` is callable and
        returns exactly the 10-atom set `:access_token, :refresh_token, :id_token,
        :authorization_code, :token, :session_ref, :subject_ref, :actor_id, :ip, :email`
        (assert `MapSet.new(...)` equality — order-independent).
    (5) DECOUPLE-05 — a stub companion contributing a known forbidden key via
        `forbidden_metadata_keys/0`; after `attach_default_logger/1`, resetting the companion
        registry to `[]` still scrubs that key on a subsequently emitted event (proving the set
        was captured in the handler closure at attach time, not re-aggregated per event).
    These tests are expected to be RED until Plans 02/03 land — mark the file with a
    module-level `@moduledoc` note that Wave 1 turns them green. Every test uses `@tag`s
    consistent with the existing proof suite. No fenced implementation of production code here —
    tests reference the public functions by name.
  </action>
  <verify>
    <automated>mix compile --warnings-as-errors 2>&1 | tail -3 && test -f test/crosswake/proof/phase136_decouple_proof_test.exs && echo FILE_EXISTS</automated>
  </verify>
  <acceptance_criteria>
    - Source: `test/crosswake/proof/phase136_decouple_proof_test.exs` exists and defines five distinct `test` blocks covering behaviors (1)-(5) above.
    - Source: the baseline-keys test asserts exactly the 10 named atoms via set equality.
    - `mix compile --warnings-as-errors` exits 0 (test file compiles even while assertions are RED).
    - The file compiles as a valid ExUnit module (referenced production functions may be undefined until Wave 1 — that is expected RED, not a compile error, because they are called not aliased).
  </acceptance_criteria>
  <done>Five backstop tests authored, compiling, and encoding the fail-closed + baseline-PII + attach-capture contracts; RED pending Wave 1.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| route activation -> companion auth evaluator | Core dispatches auth decision to a runtime-registered companion; a missing/misbehaving companion must never yield allow. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-136-01 | Elevation of Privilege | Companion behaviour contract (evaluate_auth/3, auth_authority?/0) | high | mitigate | Declare `evaluate_auth/3` + `auth_authority?/0` as the dedicated fail-closed auth seam and pin the fail-open regression with backstop tests (1) and (3) in this plan; Plan 03 implements the deny-on-absent/deny-on-raise behavior the tests assert. |
| T-136-05 | Information Disclosure | baseline PII contract | high | mitigate | Backstop test (4) freezes the 10-atom baseline as a public, auditable contract so a future edit that removes a key is caught (semver: removal = major). |

Note: internal Elixir library refactor — no new network/web attack surface. Threats are the
fail-open (T-136-01) and PII-baseline-erosion (T-136-05) regressions only.
</threat_model>

<verification>
- `mix compile --warnings-as-errors` passes.
- `mix test test/crosswake/proof/phase129_companion_contract_freeze_test.exs` passes.
- `test/crosswake/proof/phase136_decouple_proof_test.exs` exists and compiles (assertions RED until Wave 1).
</verification>

<success_criteria>
- Companion behaviour declares 11 callbacks, four newly optional, compiles clean.
- Phase-129 freeze test green at the 11-callback shape.
- Five backstop tests authored and compiling.
</success_criteria>

<output>
Create `.planning/phases/136-core-decoupling/136-01-SUMMARY.md` when done
</output>
