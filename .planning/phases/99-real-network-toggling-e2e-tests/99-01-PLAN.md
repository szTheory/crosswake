---
phase: 99-real-network-toggling-e2e-tests
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - examples/phoenix_host/playwright.config.ts
  - examples/phoenix_host/lib/crosswake_example/e2e/sync_state_controller.ex
  - examples/phoenix_host/lib/crosswake_example/router.ex
autonomous: true
requirements:
  - SYNC-02
  - SYNC-03
must_haves:
  truths:
    - "D-01: Playwright tests and config are kept at the root of examples/phoenix_host, strictly outside the OSS library root"
    - "Test environment is deterministic and isolated from external state"
    - "Test environment lifecycle is fully managed by the test runner"
    - "Database state is verifiable without Node-to-Postgres coupling"
  artifacts:
    - path: "examples/phoenix_host/playwright.config.ts"
      provides: "Playwright configuration"
    - path: "examples/phoenix_host/lib/crosswake_example/e2e/sync_state_controller.ex"
      provides: "Backend sync state verification API"
  key_links:
    - from: "examples/phoenix_host/lib/crosswake_example/router.ex"
      to: "examples/phoenix_host/lib/crosswake_example/e2e/sync_state_controller.ex"
      via: "/_e2e route scope"
---

<objective>
Setup E2E harness and backend verification API for network-toggling tests.

Purpose: To establish a deterministic testing environment that manages the Phoenix application lifecycle and securely exposes database state for Playwright assertions without leaking Postgres credentials.
Output: Playwright configuration file and Elixir E2E controller endpoint.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/99-real-network-toggling-e2e-tests/99-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Playwright configuration</name>
  <files>examples/phoenix_host/playwright.config.ts</files>
  <action>
    Create `playwright.config.ts` at the root of `examples/phoenix_host` per decision D-03 and requirement SYNC-03.
    Include `use: { serviceWorkers: 'block' }` to prevent service worker caching from masking test results.
    Include `webServer: { command: 'MIX_ENV=test mix do ecto.drop, ecto.setup, phx.server', port: 4002, reuseExistingServer: !process.env.CI }` to manage Phoenix.
    Set `workers: 1` to ensure tests run sequentially and avoid database locks.
  </action>
  <verify>
    <automated>cat examples/phoenix_host/playwright.config.ts | grep serviceWorkers</automated>
  </verify>
  <done>Playwright is configured with a webServer block and blocked service workers.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Backend Sync Verifier API</name>
  <files>
    examples/phoenix_host/lib/crosswake_example/e2e/sync_state_controller.ex
    examples/phoenix_host/lib/crosswake_example/router.ex
  </files>
  <action>
    Implement `CrosswakeExample.E2E.SyncStateController` in `examples/phoenix_host/lib/crosswake_example/e2e/sync_state_controller.ex` with a `show/2` function.
    It should take `client_mutation_id` from params, query `CrosswakeExample.LocalFirst.ReviewEvent` using `CrosswakeExample.Repo.get_by(client_mutation_id: id)`, and return JSON `%{"synced" => true, "status" => record.status}` or `%{synced: false}` if nil.
    
    Update `examples/phoenix_host/lib/crosswake_example/router.ex` per decision D-02:
    At the bottom of the file (or an appropriate place), wrap a new route in `if Mix.env() in [:test, :e2e] do`.
    Add a scope for `/_e2e` pointing to `CrosswakeExample.E2E` namespace, piping through `:api`.
    Add `get "/sync-state/:client_mutation_id", SyncStateController, :show`.
  </action>
  <verify>
    <automated>grep -q "_e2e" examples/phoenix_host/lib/crosswake_example/router.ex</automated>
  </verify>
  <done>A test-only API endpoint exists for verifying Ecto database state.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test API → Production | The test verification API exposes sensitive internal database state |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-99-01 | Information Disclosure | `router.ex` E2E Scope | mitigate | The `/_e2e` scope is strictly gated behind `if Mix.env() in [:test, :e2e] do`, physically preventing compilation of this route into production builds. |
</threat_model>

<verification>
- `playwright.config.ts` correctly blocks service workers and manages the `webServer`
- The `SyncStateController` returns the correct JSON format based on database presence
- `router.ex` isolates the `/_e2e` routes from the production build
</verification>

<success_criteria>
1. E2E execution runs against a fresh, isolated Elixir testing environment automatically
2. Service worker caching cannot mask offline failure testing
3. Database states can be verified via API without Node to Postgres connections
</success_criteria>

<output>
Create `.planning/phases/99-real-network-toggling-e2e-tests/99-01-SUMMARY.md` when done
</output>
