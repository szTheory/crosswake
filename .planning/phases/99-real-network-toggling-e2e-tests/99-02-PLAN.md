---
phase: 99-real-network-toggling-e2e-tests
plan: 02
type: execute
wave: 2
depends_on:
  - 99-01
files_modified:
  - examples/phoenix_host/e2e/offline_sync.spec.ts
autonomous: true
requirements:
  - SYNC-01
  - SYNC-02
must_haves:
  truths:
    - "E2E tests verify true network disconnection from the user\'s perspective"
    - "Application accurately records user actions while offline"
    - "Tests complete deterministically with background sync processing upon reconnection"
  artifacts:
    - path: "examples/phoenix_host/e2e/offline_sync.spec.ts"
      provides: "Network-toggling offline sync test"
  key_links:
    - from: "examples/phoenix_host/e2e/offline_sync.spec.ts"
      to: "/_e2e/sync-state"
      via: "Playwright API request"
---

<objective>
Implement real network-toggling E2E tests using Playwright.

Purpose: To verify the complete offline sync reconciliation loop (storing records offline, coming online, syncing to backend) by physically simulating browser network disconnections instead of mocking JavaScript APIs.
Output: An updated E2E test file using native Playwright offline capabilities.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/99-real-network-toggling-e2e-tests/99-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite Offline Sync E2E Test</name>
  <files>examples/phoenix_host/e2e/offline_sync.spec.ts</files>
  <action>
    Rewrite the test `simulates offline study actions and verifies Ecto sync on reconnect` in `examples/phoenix_host/e2e/offline_sync.spec.ts` to fulfill SYNC-01 and SYNC-02.
    
    Remove the `localStorage` mocking. Instead:
    1. Navigate to `/study/session`.
    2. Toggle the network offline using `await context.setOffline(true)`.
    3. Generate a `mutationId` (UUID) within the test or extract one from the UI/DOM/Network (e.g. by intercepting requests or evaluating a JS script to write to the mock offline store if the actual UI is not completely implemented, but ensure it behaves as if a user did it). Since the mocked UI has "Good" and "Hard" buttons, you can click "Good". To retrieve the generated `client_mutation_id`, you can either intercept the `/study/sync` request after reconnecting or extract it from the DOM/JS state.
    4. To keep it robust, you can intercept the sync POST when coming back online:
       `const syncRequestPromise = page.waitForRequest(req => req.url().includes('/study/sync') && req.method() === 'POST');`
    5. Toggle the network online using `await context.setOffline(false)`.
    6. If the UI requires a manual trigger to sync in the mocked state, click `button:has-text("Simulate Network Sync")`.
    7. Await the `syncRequestPromise` and parse the POST payload to extract the `client_mutation_id`.
    8. Assert the Ecto state using Playwright's `expect.poll` hitting the new backend verification API: `page.request.get(\`/_e2e/sync-state/\${mutationId}\`)`. Poll until the API returns `{ "synced": true }` (use a 5000ms timeout).
  </action>
  <verify>
    <automated>cat examples/phoenix_host/e2e/offline_sync.spec.ts | grep "context.setOffline(true)"</automated>
  </verify>
  <done>E2E test accurately uses context.setOffline to verify the synchronization loop.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Network | Simulating realistic browser network failures |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-99-02 | Denial of Service | E2E Tests | accept | Playwright testing environment, offline network toggling is constrained to the test context. |
</threat_model>

<verification>
- `offline_sync.spec.ts` correctly drops and restores the browser network
- Playwright assertions read from the `/_e2e/sync-state` API rather than DOM UI hacks
- Polling mechanism avoids flakiness from background sync delays
</verification>

<success_criteria>
1. E2E tests faithfully simulate the browser's lack of network access
2. Background synchronization accurately updates Elixir Ecto state
3. Test suite completes deterministically with passing assertions
</success_criteria>

<output>
Create `.planning/phases/99-real-network-toggling-e2e-tests/99-02-SUMMARY.md` when done
</output>
