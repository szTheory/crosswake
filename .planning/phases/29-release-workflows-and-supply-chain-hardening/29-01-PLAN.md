---
phase: 29
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .github/workflows/release-please.yml
  - .github/workflows/hex-publish.yml
  - .github/dependabot.yml
autonomous: true
requirements:
  - REL-03
  - REL-04
  - REL-05
  - REL-06
user_setup:
  - service: github
    why: "Publishing to Hex and optionally bypassing branch protections"
    env_vars:
      - name: HEX_API_KEY
        source: "Hex Dashboard -> Keys"
      - name: RELEASE_PLEASE_TOKEN
        source: "GitHub Settings -> Developer Settings -> Fine-grained PATs"
    dashboard_config:
      - task: "Add HEX_API_KEY as an Action secret"
        location: "GitHub Repository Settings -> Secrets and variables -> Actions"

must_haves:
  truths:
    - "Release-please automated workflow exists and creates Release PRs on main pushes"
    - "Hex publish manual recovery workflow exists for one-off retries"
    - "All external GitHub Actions are pinned to full commit SHAs"
    - "Dependabot surfaces updates for GitHub Actions weekly"
  artifacts:
    - path: ".github/workflows/release-please.yml"
      provides: "Automated release and publish pipeline"
      contains: "publish-hex"
    - path: ".github/workflows/hex-publish.yml"
      provides: "Manual hex publish recovery pipeline"
      contains: "workflow_dispatch"
    - path: ".github/dependabot.yml"
      provides: "Supply-chain dependency updates for GitHub Actions"
      contains: "package-ecosystem: \"github-actions\""
  key_links:
    - from: ".github/workflows/release-please.yml"
      to: "publish-hex job"
      via: "needs.release-please.outputs.release_created == 'true'"
      pattern: "needs\\.release-please\\.outputs\\.release_created"
---

<objective>
Land both release workflow files (Release PR creation + manual recovery) and bake in supply-chain hardening so post-CVE tag-move attacks cannot affect the release pipeline.

Purpose: Automate Hex releases based on conventional commits while hardening the supply chain using SHA-pinned external GitHub Actions and Dependabot.
Output: Two GitHub Actions workflow files and a Dependabot configuration.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/milestones/v3.3-phases/29-release-workflows-and-supply-chain-hardening/29-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add automated release-please pipeline</name>
  <files>.github/workflows/release-please.yml</files>
  <action>
Create `.github/workflows/release-please.yml` based on `~/projects/oarlock/.github/workflows/release-please.yml`.
- Replace any references to "oarlock" with "crosswake" (e.g., in the Hex API verification loop `https://hex.pm/api/packages/crosswake/releases/${VERSION}`).
- Ensure all `uses:` steps have a full commit SHA and a version comment.
- Specifically pin `googleapis/release-please-action` to `5c625bfb5d1ff62eadeeb3772007f7f66fdcf071 # v4.1.3`.
- Retain the exact logic where `publish-hex` only runs `if: ${{ needs.release-please.outputs.release_created == 'true' }}`.
  </action>
  <verify>
    <automated>grep "5c625bfb5d1ff62eadeeb3772007f7f66fdcf071" .github/workflows/release-please.yml</automated>
  </verify>
  <done>release-please.yml created with SHA pinned actions and proper job dependencies</done>
</task>

<task type="auto">
  <name>Task 2: Add manual hex-publish recovery pipeline</name>
  <files>.github/workflows/hex-publish.yml</files>
  <action>
Create `.github/workflows/hex-publish.yml` based on `~/projects/oarlock/.github/workflows/hex-publish.yml`.
- Replace any references to "oarlock" with "crosswake" if they exist.
- Ensure all `uses:` steps use exact commit SHAs with version comments (matching the SHAs used in Task 1 for checkout, setup-beam, and cache).
- No `@vN`-tag references should remain.
  </action>
  <verify>
    <automated>grep -E "uses: .+@[a-f0-9]{40}" .github/workflows/hex-publish.yml | wc -l | awk '{if ($1 >= 3) exit 0; else exit 1}'</automated>
  </verify>
  <done>hex-publish.yml created with fully SHA-pinned actions and workflow_dispatch configuration</done>
</task>

<task type="auto">
  <name>Task 3: Add Dependabot for Actions and verify workflows</name>
  <files>.github/dependabot.yml</files>
  <action>
Create `.github/dependabot.yml` with `version: 2` and a weekly update schedule for the `github-actions` package-ecosystem targeting the `/` directory.
Then, run `actionlint` to ensure both workflow files have valid syntax.
  </action>
  <verify>
    <automated>actionlint .github/workflows/release-please.yml .github/workflows/hex-publish.yml</automated>
  </verify>
  <done>dependabot.yml created and actionlint passes on the workflow files</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Actions workflow execution | Third-party GitHub Actions have access to workflow secrets and repository write permissions. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-29-01 | Tampering | External Actions | mitigate | Pin all external Actions to explicit commit SHAs so tag-moving attacks cannot alter the executed code. |
| T-29-02 | Elevation of Privilege | Actions Runner | mitigate | Dependabot will provide PRs for Action updates so we stay ahead of known vulnerabilities in Action dependencies. |
</threat_model>

<verification>
- `cat .github/workflows/release-please.yml | grep "googleapis/release-please-action@5c625bfb5d1ff62eadeeb3772007f7f66fdcf071"`
- `actionlint .github/workflows/*.yml` runs without error
- No `@vN` tags remain in `uses:` blocks
</verification>

<success_criteria>
- Both `.github/workflows/release-please.yml` and `.github/workflows/hex-publish.yml` exist, parse cleanly via `actionlint`, and have NO unpinned action versions.
- `.github/dependabot.yml` is correctly set up for GitHub Actions.
</success_criteria>

<output>
After completion, create `.planning/milestones/v3.3-phases/29-release-workflows-and-supply-chain-hardening/29-01-SUMMARY.md`
</output>