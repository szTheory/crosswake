---
phase: 134-shell-lifecycle-native-uat-promotion
plan: 03
type: execute
wave: 3
# REVIEW FIX (finding 8 [LOW — Codex]): ROADMAP.md:278 sequences Wave 3 as blocked on Wave 2. The
# real code dependency is on Plan 01 (Wave 1: the .crosswake/shell.json manifest + template_version/0
# accessor that --diff re-renders from). To keep frontmatter consistent with the roadmap's conservative
# wave ordering, depends_on now lists BOTH 134-01 (true data dependency) and 134-02 (roadmap Wave-2
# sequencing). This keeps wave=3 correct (max(dep waves)+1 = max(1,2)+1 = 3) and removes the
# frontmatter-vs-roadmap divergence the reviewer flagged.
depends_on: ["134-01", "134-02"]
files_modified:
  - lib/mix/tasks/crosswake.gen.shell.ex
  - test/mix/tasks/crosswake_gen_shell_diff_test.exs
autonomous: true
requirements: [LIFE-02b]
must_haves:
  truths:
    - "`mix crosswake.gen.shell <platform> --diff` prints a unified diff of on-disk files vs freshly re-rendered templates and writes NOTHING (D-13)"
    - "Diffs re-render using the manifest's saved params so app-name/router assigns are not noise (D-14)"
    - "Each changed file carries an advisory RebuildPolicy-vocabulary verdict; ONLY project.pbxproj is excluded from --diff (gradlew/gradlew.bat ARE diffed — REVIEW FIX finding 6; D-02's gradlew exclusion is stamp-only) (D-15/D-16)"
  artifacts:
    - "--diff switch + diff branch in crosswake.gen.shell.ex"
    - "file→change-class advisory lookup helper (reuses RebuildPolicy verdict vocabulary, NOT RebuildPolicy.diff/2)"
  key_links:
    - "the --diff branch MUST fork before any File.write/ensure_file call (structural non-destructiveness, D-13)"
    - "re-render assigns come from the manifest params; local-path-missing → warn + fall back to Hex, never silent fail (D-14)"
    - "annotation uses a file→change-class map, NOT RebuildPolicy.diff/2 which diffs Root.t() manifests not files (D-16)"
---

<objective>
Add `mix crosswake.gen.shell <platform> --diff` (LIFE-02b, D-13..D-16): a non-destructive unified diff between the host's on-disk generated files and the templates freshly re-rendered in memory, with a trust banner, per-file advisory RebuildPolicy-vocabulary annotations, summary stats, TTY colorization, and `project.pbxproj` excluded. Non-destructiveness is structural — the diff path forks before any write call.

Purpose: let hosts preview exactly what changed between their generated shell and the current templates without ever overwriting their host-owned files.
Output: the `--diff` switch + branch in gen.shell.ex, the advisory file→change-class lookup, and the now-GREEN diff test from Plan 00.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/134-shell-lifecycle-native-uat-promotion/134-CONTEXT.md
@.planning/phases/134-shell-lifecycle-native-uat-promotion/134-PATTERNS.md
@lib/mix/tasks/crosswake.gen.shell.ex
@lib/crosswake/runtime_line/rebuild_policy.ex
</context>

<artifacts_this_phase_produces>
This plan creates: the `--diff` switch on `mix crosswake.gen.shell`, the in-memory re-render + `List.myers_difference/2` unified-diff renderer, and the advisory file→change-class lookup helper. (Full phase inventory in 134-04-PLAN.md.)

It CONSUMES (does not create): the public `Mix.Tasks.Crosswake.Gen.Shell.template_version/0` accessor (Plan 01) for the re-render's `template_version` assign, and the `.crosswake/shell.json` manifest at the per-platform root (Plan 01).
</artifacts_this_phase_produces>

<review_feedback_addressed>
Cross-AI review (134-REVIEWS.md) resolution for this plan:
- **Finding 6 [MEDIUM — Codex] — wrappers excluded from --diff.** INCORPORATED (Task 1 action +
  acceptance + must_haves): re-read D-02 (134-CONTEXT.md:50-56) — its `gradlew`/`gradlew.bat` exclusion is
  scoped to STAMPS only. D-15 excludes ONLY `project.pbxproj` from the diff. So `gradlew`/`gradlew.bat` are
  now DIFFED (with an `:ota_safe` advisory verdict); `@diff_excluded_templates = ["...project.pbxproj"]` is
  the sole diff exclusion. No conflict with D-02 — the prior over-exclusion was the defect.
- **Finding 8 [LOW — Codex] — depends_on vs roadmap.** INCORPORATED (frontmatter): depends_on now lists
  `["134-01", "134-02"]`, reconciling with ROADMAP.md:278's Wave-3-blocked-on-Wave-2 sequencing while
  preserving the true data dependency on Plan 01. wave=3 unchanged and still correct.
- **Finding 3 [MEDIUM — both] — shared version source.** INCORPORATED (Task 1 re-render): the re-render's
  `template_version` assign comes from the public `template_version/0` accessor (Plan 01), not a duplicated
  constant — so a stale on-disk stamp surfaces as a diff line.
- **Finding 1 [HIGH — both] — manifest path.** ALREADY CONSISTENT here: the re-render reads the manifest at
  the per-platform root `<target>/native/<platform>/crosswake_shell/.crosswake/shell.json`.
</review_feedback_addressed>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add --diff branch — in-memory re-render, unified diff, advisory annotation, pbxproj exclusion</name>
  <read_first>
    - lib/mix/tasks/crosswake.gen.shell.ex (the file being extended — @switches ~14, render_template ~148-160, the per-platform template lists, the generation flow)
    - lib/crosswake/runtime_line/rebuild_policy.ex (the verdict VOCABULARY to reuse — :ota_safe / {:rebuild_required, :native_shell}; do NOT call diff/2 which diffs Root.t() manifests per D-16)
    - 134-PATTERNS.md "crosswake.gen.shell.ex (MODIFY)" --diff section (lines 315-341)
    - 134-RESEARCH.md RQ3 "--diff Rendering Mechanics" (lines 361-411) — file→change-class table, myers_difference, pbxproj exclusion + RQ2 re-render assigns + local-path fallback
  </read_first>
  <files>lib/mix/tasks/crosswake.gen.shell.ex</files>
  <behavior>
    - `mix crosswake.gen.shell android --target DIR --diff` over an unchanged generated shell prints a 0-changed summary and writes nothing.
    - When a template differs from the on-disk file, output contains a git-style ---/+++/@@ unified diff for that file plus an advisory verdict annotation.
    - The diff output never contains project.pbxproj content.
    - No file under DIR is created, modified, or deleted by a --diff run.
  </behavior>
  <action>
    Add `diff: :boolean` to @switches. Fork the run early (D-13): if opts[:diff], print the trust banner `[crosswake] diff — read-only, no files changed` and call a `run_diff/...` path that NEVER reaches any `ensure_file/2` or `File.write!/2` call — the fork happens BEFORE any write, so non-destructiveness is a code guarantee, not a convention.

    Re-render assigns (D-14): read the target's `.crosswake/shell.json` at the per-platform root `<target>/native/<platform>/crosswake_shell/.crosswake/shell.json` (finding 1 canonical location); re-render each template in memory with `EEx.eval_file` using the manifest's saved `params` (router, local, target) so app-name/bundle/router assigns match the original generation and don't show as spurious diffs. The `template_version` assign for the re-render comes from the live `Mix.Tasks.Crosswake.Gen.Shell.template_version/0` accessor (Plan 01, finding 3) so the re-rendered stamp reflects the CURRENT epoch — this is exactly what makes a stale on-disk stamp show up as a diff line. Re-fetch capabilities from the manifest's `router` param (or fall back to the public-route capability ids if router is null, per RESEARCH RQ2). Pre-manifest shells: accept a one-time `--router` override and offer to write the manifest after a successful diff. If `params.local == true` but the local package path no longer resolves: emit `Mix.shell().info("[crosswake] local package path not found: ... — falling back to Hex package")` and re-render with local: false — never silently fail (D-14).

    Diff computation (D-15): REVIEW FIX (finding 6 [MEDIUM — Codex]) — D-02 excludes `gradlew`/`gradlew.bat`
    only from STAMPS; D-15 excludes ONLY `CrosswakeShell.xcodeproj/project.pbxproj` from the DIFF. The prior
    revision wrongly also skipped `gradlew`/`gradlew.bat` from `--diff`, which would hide wrapper-template
    changes from hosts (re-read D-02 in 134-CONTEXT.md:50-56: the gradlew exclusion is scoped to "NO stamp",
    not "no diff"). Therefore `@diff_excluded_templates = ["CrosswakeShell.xcodeproj/project.pbxproj"]` is the
    ONLY diff exclusion — `gradlew` and `gradlew.bat` ARE diffed (they are real generated files a host may
    want to see change). For each template EXCEPT `project.pbxproj`, split the on-disk content and the
    re-rendered content into lines and run `List.myers_difference/2`; render a git-style unified diff
    (`--- <file> (on disk)` / `+++ <file> (current template)` / `@@` hunks) with `-`/`+`/space prefixes.
    Colorize with IO.ANSI red/green gated by `IO.ANSI.enabled?()`. Emit a summary stat line (ota-safe /
    rebuild-required / unchanged counts).

    Per-file advisory annotation (D-16 — CORRECTNESS CATCH): do NOT route through `RebuildPolicy.diff/2`. Add a small private `file_change_class/1` (or `advisory_verdict/1`) lookup that maps file basenames/patterns to a change-class atom and an advisory verdict, REUSING RebuildPolicy's verdict vocabulary: Info.plist/PrivacyInfo.xcprivacy/entitlements/AndroidManifest.xml/app build.gradle/root build.gradle/gradle-wrapper.properties → {:rebuild_required, :native_shell}; *.swift / *.kt / settings.gradle / gradle.properties / themes.xml → :ota_safe. Because `gradlew`/`gradlew.bat` are now diffed (REVIEW FIX finding 6), give them a verdict too: map `gradlew`/`gradlew.bat` → :ota_safe (wrapper script changes do not by themselves force a native rebuild; they remain advisory). The lookup must have a safe default verdict for any unmapped basename so no diffed file is left unannotated. Print this verdict per changed file as ADVISORY (classify/2 is documented tooling input, not a release-gate oracle — say so in the annotation).
  </action>
  <verify>
    <automated>cd /tmp && rm -rf cw_diff && (cd /Users/jon/projects/crosswake && mix crosswake.gen.shell android --target /tmp/cw_diff) && find /tmp/cw_diff -type f | sort > /tmp/cw_diff_before.txt && (cd /Users/jon/projects/crosswake && mix crosswake.gen.shell android --target /tmp/cw_diff --diff > /tmp/cw_diff_out.txt 2>&1) && find /tmp/cw_diff -type f | sort > /tmp/cw_diff_after.txt && diff /tmp/cw_diff_before.txt /tmp/cw_diff_after.txt && ! grep -q "project.pbxproj" /tmp/cw_diff_out.txt && grep -q "read-only" /tmp/cw_diff_out.txt && echo "diff non-destructive, pbxproj excluded, banner present"</automated>
  </verify>
  <acceptance_criteria>
    - A `--diff` run over a freshly generated shell leaves the file set byte-identical (no create/modify/delete).
    - The `--diff` stdout contains the trust banner text "read-only".
    - The `--diff` stdout never contains "project.pbxproj".
    - When a template genuinely differs, the output shows a `---`/`+++` unified-diff header and an advisory verdict line for that file.
    - `@diff_excluded_templates` contains ONLY `project.pbxproj` — `gradlew`/`gradlew.bat` are NOT in the diff-exclusion list (REVIEW FIX finding 6); a forced change to a generated `gradlew` surfaces in `--diff` output with an `:ota_safe` advisory verdict.
    - `mix compile --warnings-as-errors` clean.
  </acceptance_criteria>
  <done>The `--diff` branch re-renders from the manifest params, prints a non-destructive unified diff with advisory annotations, excludes project.pbxproj, and writes nothing.</done>
</task>

<task type="auto">
  <name>Task 2: Activate the gen.shell --diff test (no-write / stdout / pbxproj-excluded) — turn GREEN</name>
  <read_first>
    - test/mix/tasks/crosswake_gen_shell_diff_test.exs (the Plan 00 scaffold to un-skip)
    - 134-VALIDATION.md Per-Task Verification Map (diff no-write / stdout / pbxproj rows)
  </read_first>
  <files>test/mix/tasks/crosswake_gen_shell_diff_test.exs</files>
  <action>
    Remove the Plan-00 pending-skip guards now that the `--diff` branch exists. Implement three tests in a tmp dir: (1) no-write — generate a shell, snapshot the recursive file list + per-file content hash, run `--diff`, re-snapshot, assert byte-identical; (2) stdout — capture the `--diff` output (ExUnit.CaptureIO or Mix.shell capture) and assert it contains a unified-diff `---` header when a template is forced to differ (e.g. by editing the on-disk generated file, NOT the template, then diffing); (3) pbxproj-excluded — assert the captured output does not contain "project.pbxproj". Keep each test in its own tmp dir for async safety.
  </action>
  <verify>
    <automated>cd /Users/jon/projects/crosswake && mix test test/mix/tasks/crosswake_gen_shell_diff_test.exs 2>&1 | grep -qE "0 failures" && echo "gen.shell --diff test GREEN"</automated>
  </verify>
  <acceptance_criteria>
    - `mix test test/mix/tasks/crosswake_gen_shell_diff_test.exs` → 0 failures.
    - The no-write test proves the file set is byte-identical before/after `--diff`.
    - The pbxproj-excluded test proves the output omits project.pbxproj.
    - No pending-skip guard remains in the diff test file.
  </acceptance_criteria>
  <done>The diff test is fully GREEN and proves non-destructiveness, unified-diff stdout, and pbxproj exclusion.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| host shell.json → re-render assigns | manifest params drive in-memory EEx render; host could have hand-edited |
| local package path → filesystem | params.local path may no longer resolve |

## STRIDE Threat Register (ASVS L1, block-on: high)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-134-03-01 | Tampering | tampered manifest params drive re-render | medium | mitigate | Re-render is in-memory only; output is a printed diff, never written — a poisoned param can mislead the diff text but cannot modify host files (D-13 structural fork) |
| T-134-03-02 | Denial of Service | local package path missing → crash | medium | mitigate | Explicit warn + fall back to Hex (local: false), never silent-fail or crash (D-14) |
| T-134-03-03 | Information Disclosure | diff prints file contents | low | accept | Host-owned files printed back to the host's own terminal; no new exposure |
| T-134-03-04 | Tampering | accidental write during diff | high | mitigate | Diff path forks BEFORE any File.write!/ensure_file call; no-write test asserts byte-identical file set (D-13) |
</threat_model>

<verification>
- `--diff` over an unchanged shell leaves the file set byte-identical and prints the read-only banner.
- `--diff` output excludes project.pbxproj.
- `mix test test/mix/tasks/crosswake_gen_shell_diff_test.exs` → 0 failures.
- `mix compile --warnings-as-errors` clean.
</verification>

<success_criteria>
LIFE-02b (diff half) satisfied: `mix crosswake.gen.shell --diff` prints a non-destructive unified diff with advisory RebuildPolicy-vocabulary annotations, excludes project.pbxproj, and never overwrites host files.
</success_criteria>

<output>
Create `.planning/phases/134-shell-lifecycle-native-uat-promotion/134-03-SUMMARY.md` when done.
</output>
