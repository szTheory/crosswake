---
status: testing
phase: 134-shell-lifecycle-native-uat-promotion
source: [134-VERIFICATION.md]
started: 2026-06-29T21:21:06Z
updated: 2026-06-29T21:21:06Z
---

## Current Test

number: 1
name: Android generated-shell lane actually blocks a real PR merge
expected: |
  On the first run of the `native-behavioral-proof-gate` aggregator against a live PR,
  `needs.android-generated-shell-unit.result` appears in the `toJSON(needs)` alls-green input;
  the aggregator gates on both `android-package-unit` and `android-generated-shell-unit`; and
  a PR whose `android-generated-shell-unit` job fails is blocked from merging by branch protection.
awaiting: user response

## Tests

### 1. Android generated-shell lane actually blocks a real PR merge
expected: needs.android-generated-shell-unit.result appears in the aggregator's toJSON(needs) input; the merge-blocking-native-behavioral-proof aggregator fails (blocking the PR) when android-generated-shell-unit fails. Structural wiring (job on macos-latest, SHA-pinned setup-beam, Temurin 17, aggregator `needs:` + `if: always()`) is already code-verified; only the live runtime blocking behavior remains.
why_human: Merge-blocking behavior is only observable in GitHub Actions on a real PR against origin. The new job has never run against a live PR because local main is ~100 commits ahead of origin; per project state this syncs in one catch-up PR at the milestone boundary, which is when this confirmation naturally fires (134-04 SUMMARY "footgun 4" runbook).
result: [pending]

## Summary

total: 1
passed: 0
issues: 0
pending: 1
skipped: 0
blocked: 0

## Gaps
