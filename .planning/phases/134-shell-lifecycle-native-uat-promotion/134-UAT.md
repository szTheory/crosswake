---
status: complete
phase: 134-shell-lifecycle-native-uat-promotion
source: [134-VERIFICATION.md]
started: 2026-06-29T21:21:06Z
updated: 2026-06-29T21:21:06Z
---

## Current Test

[testing complete]

## Tests

### 1. Android generated-shell lane actually blocks a real PR merge
expected: needs.android-generated-shell-unit.result appears in the aggregator's toJSON(needs) input; the merge-blocking-native-behavioral-proof aggregator fails (blocking the PR) when android-generated-shell-unit fails. Structural wiring (job on macos-latest, SHA-pinned setup-beam, Temurin 17, aggregator `needs:` + `if: always()`) is already code-verified; only the live runtime blocking behavior remains.
result: pass
source: automated
discharged_by: |
  Shifted left into CI — no human verification required, forever. The blocking claim was
  decomposed into three independently-automated parts (none needs a live PR against origin):

  - claim 1 (WIRING): test/crosswake/proof/phase134_native_gate_blocking_proof_test.exs
    (stable ids proof.life_01a.gate.*). Runs every PR in the untagged merge-blocking ExUnit lane.
    Asserts if:always(), needs:{android-package-unit,android-generated-shell-unit}, and
    alls-green+toJSON(needs) — structurally via script/assert_aggregator_wiring.py (PyYAML; exit 2
    on a renamed aggregator → non-vacuity). Also locks the two sibling aggregators by parity.

  - claim 2 (ROLLUP SEMANTICS / footgun 1): .github/workflows/aggregator-negative-control.yml,
    lane merge-blocking-aggregator-negative-control. Hermetically proves the shared
    re-actors/alls-green pattern FAILS on a RED leaf AND on a SKIPPED leaf, and PASSES on green —
    on ubuntu-latest, ~30s, no Android/macOS/live-PR. The skipped-leaf arm is the discriminating
    case (a naive needs:-only rollup would let a skipped leaf pass). The mechanism is byte-identical
    across all three real aggregators (locked by the claim-1 sibling-parity proof), so proving it
    once discharges footgun 1 for all three. Equivalence — not the literal real-Android aggregator
    failing on a live PR — is the load-bearing claim, and is a stronger, permanent proof.

  - claim 3 (REGISTRATION): .github/workflows/required-checks-audit.yml runs the existing
    check_required_checks_registered.sh daily (cron + workflow_dispatch) with an admin read-PAT;
    opens/auto-closes a single tracking issue and fails on GAP (exit 1) / UNVERIFIED (exit 3).
    The one-time admin apply (DRY_RUN=0 script/register_required_checks.sh) and the
    BRANCH_PROTECTION_READ_TOKEN secret are milestone-gated config (origin-sync), NOT recurring
    verification — the auditor proves-forever once they exist.

## Summary

total: 1
passed: 1
issues: 0
pending: 0
skipped: 0
blocked: 0

## Gaps

[none]
