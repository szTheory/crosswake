---
phase: 134-shell-lifecycle-native-uat-promotion
plan: 02
type: execute
wave: 2
depends_on: ["134-01"]
files_modified:
  - lib/mix/tasks/crosswake.shell.status.ex
  - test/mix/tasks/crosswake_shell_status_test.exs
autonomous: true
requirements: [LIFE-02b]
must_haves:
  truths:
    - "A host runs `mix crosswake.shell.status` and learns whether each generated shell is up-to-date or N template versions behind (D-09/D-10)"
    - "Exit code is 0 (up-to-date or not-a-shell), 2 (at least one behind), or 1 (error) (D-12)"
    - "`--format json` emits a machine-readable status payload (D-11)"
  artifacts:
    - "mix crosswake.shell.status task with --target and --format json switches"
  key_links:
    - "status reads the .crosswake/shell.json manifest written by Plan 01 (at the per-platform root native/<platform>/crosswake_shell/.crosswake/shell.json — finding 1) and compares stamped template_version vs the live epoch from Mix.Tasks.Crosswake.Gen.Shell.template_version/0 (finding 3)"
    - "exit 2 uses exit({:shutdown, 2}); not-a-shell returns 0 so non-adopter CI never breaks (D-12)"
---

<objective>
Ship `mix crosswake.shell.status` (LIFE-02b, D-09..D-12): a host-facing read-only diagnostic that locates generated shells via `.crosswake/shell.json`, compares the stamped `template_version` against the live `@template_version`, reports up-to-date / N-behind / not-a-shell / mixed in calm `[crosswake]` prose, supports `--format json` for scripts, and uses the three-way exit-code convention (0/2/1).

Purpose: give hosts a one-command staleness check that is safe to run in their CI (not-a-shell returns 0).
Output: new `crosswake.shell.status.ex` task + the now-GREEN status test from Plan 00.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/134-shell-lifecycle-native-uat-promotion/134-CONTEXT.md
@.planning/phases/134-shell-lifecycle-native-uat-promotion/134-PATTERNS.md
@lib/mix/tasks/crosswake.doctor.ex
@lib/mix/tasks/crosswake.demo.ex
@lib/crosswake/doctor/json_formatter.ex
</context>

<artifacts_this_phase_produces>
This plan creates: `mix crosswake.shell.status` task with switches `--target PATH` and `--format json`. (Full phase inventory in 134-04-PLAN.md.)

It CONSUMES (does not create) the public `Mix.Tasks.Crosswake.Gen.Shell.template_version/0` accessor added by Plan 01.
</artifacts_this_phase_produces>

<review_feedback_addressed>
Cross-AI review (134-REVIEWS.md) resolution for this plan:
- **Finding 3 [MEDIUM — both] — shared template-version source.** INCORPORATED (Task 1 action/read_first/
  key_links): `shell.status` reads the live epoch via the public `Mix.Tasks.Crosswake.Gen.Shell.template_version/0`
  accessor (added in Plan 01), never parsing source or duplicating the constant.
- **Finding 1 [HIGH — both] — manifest path.** ALREADY CONSISTENT on this plan's side: Task 1 already probes
  the per-platform roots `native/ios/crosswake_shell/.crosswake/shell.json` and
  `native/android/crosswake_shell/.crosswake/shell.json` (the canonical location Plan 01 now writes). No change
  needed here beyond the key_links note; Plan 01 was the side corrected.
</review_feedback_addressed>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement crosswake.shell.status — manifest read, version compare, three-way exit</name>
  <read_first>
    - lib/mix/tasks/crosswake.doctor.ex (task skeleton, OptionParser strict, --format json branch, Mix.shell().info house style)
    - lib/mix/tasks/crosswake.demo.ex (exit({:shutdown, N}) pattern, lines 41-43)
    - 134-PATTERNS.md "crosswake.shell.status.ex" section (lines 111-207)
    - 134-RESEARCH.md RQ4 "shell.status Exit Code Wiring" (lines 413-474) + Open Question 2 (manifest location per platform)
    - lib/mix/tasks/crosswake.gen.shell.ex (call the PUBLIC `template_version/0` accessor Plan 01 adds for the live current epoch — REVIEW FIX finding 3; do not parse `@template_version`. Also note the per-platform output roots native/ios/crosswake_shell, native/android/crosswake_shell which this task probes)
  </read_first>
  <files>lib/mix/tasks/crosswake.shell.status.ex</files>
  <behavior>
    - Up-to-date shell (stamped == live) → calm "[crosswake] generated shells are up to date." → exit 0.
    - Behind shell (stamped < live) → "[crosswake] N template versions behind ..." with stamped → current both shown (D-10) → exit 2.
    - No manifest found anywhere → "[crosswake] no .crosswake/shell.json found — nothing to check." → exit 0 (D-12 non-adopter safe).
    - Bad/unreadable/malformed manifest → Mix.raise → exit 1.
    - `--format json` → Jason-encoded payload with per-platform status, stamped_version, current_version, versions_behind, highest_severity.
  </behavior>
  <action>
    Create Mix.Tasks.Crosswake.Shell.Status (`use Mix.Task`, @shortdoc, @moduledoc documenting the exit codes 0/2/1 — pitfall 6). @switches `[target: :string, format: :string]`, parsed with OptionParser strict + invalid guard (Mix.raise on invalid).

    Locate manifests (D-09): with `--target PATH`, check `PATH/.crosswake/shell.json`. With no arg, probe BOTH platform roots relative to cwd — `native/ios/crosswake_shell/.crosswake/shell.json` and `native/android/crosswake_shell/.crosswake/shell.json` (per RESEARCH Open Question 2 recommendation: one manifest per platform generation run). Report per platform: present+up-to-date, present+behind, or not-found.

    For each present manifest: read + Jason.decode; on decode error or missing required keys → {:error, reason}. Compare the manifest's `template_version` against the live current epoch. REVIEW FIX (finding 3 [MEDIUM — both reviewers]): read the live epoch by calling the PUBLIC accessor `Mix.Tasks.Crosswake.Gen.Shell.template_version/0` that Plan 01 adds — do NOT parse gen.shell.ex source for `@template_version`, and do NOT duplicate the integer constant in this task (Plan 02's files_modified deliberately excludes gen.shell.ex; the accessor is the one sanctioned cross-plan source of truth). "N behind" = current − stamped (D-10). Surface the highest-severity RebuildPolicy-vocabulary verdict spanned by those versions using the upgrade-guide change-class mapping (advisory; if Plan 04's lookup helper exists reuse it, otherwise a local severity derivation keyed off the guide is acceptable — keep it advisory). Show both numbers stamped → current, hex.outdated-style.

    Output: `[crosswake]` prefix + calm prose for the human format; `--format json` builds the per-platform map (status/stamped_version/current_version/versions_behind/highest_severity) and emits Jason.encode!(payload, pretty: true) via Mix.shell().info (reuse the doctor JSON house style, no need to reuse the JSONFormatter module — RESEARCH RQ4).

    Exit wiring (D-12): up_to_date → :ok (exit 0); not_a_shell → :ok (exit 0); {:behind, _} → exit({:shutdown, 2}); {:error, msg} → Mix.raise("[crosswake] shell.status: #{msg}") (exit 1). Mixed (one behind, one up-to-date) is :behind overall → exit 2.
  </action>
  <verify>
    <automated>cd /Users/jon/projects/crosswake && mix compile --warnings-as-errors 2>&1 | tail -1 && mix help crosswake.shell.status >/dev/null 2>&1 && (cd /tmp && rm -rf cw_status_none && mkdir cw_status_none && cd cw_status_none && mix crosswake.shell.status >/dev/null 2>&1; test $? -eq 0) && echo "status task registered; not-a-shell exits 0"</automated>
  </verify>
  <acceptance_criteria>
    - `mix help crosswake.shell.status` shows the @shortdoc.
    - Running `mix crosswake.shell.status` in a dir with no manifest prints the not-a-shell message and exits 0.
    - With a `--target` pointing at a freshly generated shell (template_version == live), exits 0 with the up-to-date message.
    - `mix compile --warnings-as-errors` clean.
  </acceptance_criteria>
  <done>crosswake.shell.status reads manifests, compares versions, reports calm prose + --format json, and exits 0/2/1 per D-12.</done>
</task>

<task type="auto">
  <name>Task 2: Activate the shell.status test (exit 0 / 2 / 1) — turn GREEN</name>
  <read_first>
    - test/mix/tasks/crosswake_shell_status_test.exs (the Plan 00 scaffold to un-skip)
    - 134-VALIDATION.md Per-Task Verification Map (status exit 0/2/1 rows)
  </read_first>
  <files>test/mix/tasks/crosswake_shell_status_test.exs</files>
  <action>
    Remove the Plan-00 pending-skip guards now that Mix.Tasks.Crosswake.Shell.Status loads. Implement the three exit-code tests by driving the task in a tmp dir: write a `.crosswake/shell.json` with a stamped template_version equal to live → assert exit 0 (or a non-raising run); write one with a stamped version LESS than live → assert the `exit({:shutdown, 2})` is raised (catch with `catch_exit/1` or `assert catch_exit(...) == {:shutdown, 2}`); write a malformed JSON manifest → assert Mix.raise (exit 1) via `assert_raise Mix.Error`. Also assert the `--format json` output decodes to a map with the per-platform keys. Keep tests async-safe (each uses its own tmp dir).
  </action>
  <verify>
    <automated>cd /Users/jon/projects/crosswake && mix test test/mix/tasks/crosswake_shell_status_test.exs 2>&1 | grep -qE "0 failures" && echo "shell.status test GREEN"</automated>
  </verify>
  <acceptance_criteria>
    - `mix test test/mix/tasks/crosswake_shell_status_test.exs` → 0 failures.
    - The behind-case test proves exit code 2 via catch_exit.
    - The error-case test proves exit 1 via assert_raise.
    - No pending-skip guard remains in the status test file.
  </acceptance_criteria>
  <done>The shell.status test is fully GREEN and proves the 0/2/1 exit contract plus the JSON payload shape.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| host filesystem → status task | reads `.crosswake/shell.json` which a host could have hand-edited |
| CLI `--target` → file path | user-supplied path crosses into File.read |

## STRIDE Threat Register (ASVS L1, block-on: high)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-134-02-01 | Tampering | hand-edited shell.json | medium | mitigate | Decode defensively; missing/invalid required keys → {:error} → exit 1, never a crash; manifest treated as advisory input |
| T-134-02-02 | Tampering | `--target` path traversal | low | mitigate | Path.expand/1 on target before joining `.crosswake/shell.json`; read-only, no write |
| T-134-02-03 | Denial of Service | exit-2 breaks non-adopter CI | medium | mitigate | not-a-shell returns exit 0 (D-12) — only an actually-behind shell returns 2 |
</threat_model>

<verification>
- `mix help crosswake.shell.status` registered.
- not-a-shell dir → exit 0; behind manifest → exit 2; malformed manifest → exit 1.
- `mix test test/mix/tasks/crosswake_shell_status_test.exs` → 0 failures.
- `mix compile --warnings-as-errors` clean.
</verification>

<success_criteria>
LIFE-02b (status half) satisfied: `mix crosswake.shell.status` reports up-to-date / N-behind / not-a-shell / mixed with calm prose + `--format json`, and exits 0/2/1 per D-12.
</success_criteria>

<output>
Create `.planning/phases/134-shell-lifecycle-native-uat-promotion/134-02-SUMMARY.md` when done.
</output>
