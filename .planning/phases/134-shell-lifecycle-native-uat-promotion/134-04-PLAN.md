---
phase: 134-shell-lifecycle-native-uat-promotion
plan: 04
type: execute
wave: 4
depends_on: ["134-01", "134-03"]
files_modified:
  - guides/native_shell_upgrade.md
  - lib/mix/tasks/crosswake.gen.shell.ex
  - test/crosswake/guides/native_shell_upgrade_test.exs
  - .github/workflows/native-behavioral-proof-gate.yml
  - guides/support_matrix.md
autonomous: true
requirements: [LIFE-02c, LIFE-01a, LIFE-01b]
must_haves:
  truths:
    - "guides/native_shell_upgrade.md provides a per-template-version changelog leading with the RebuildPolicy verdict and referencing classify/2 (D-06/D-08)"
    - "The gen.shell.ex line-254 placeholder is replaced with a real pointer to the upgrade guide (D-07)"
    - "The hermetic Android JVM generated-shell lane (android-generated-shell-unit) is merge-blocking via the aggregator's needs: (D-17/D-18/D-19)"
    - "iOS stays advisory; support_matrix.md labels are honest with the verbatim JVM-hermetic caveat (D-20)"
  artifacts:
    - guides/native_shell_upgrade.md
    - "android-generated-shell-unit job in native-behavioral-proof-gate.yml"
    - "updated guides/support_matrix.md iOS/Android shell rows"
  key_links:
    - "the new CI job needs erlef/setup-beam (Elixir, the script calls mix crosswake.gen.shell) AND actions/setup-java Temurin 17 (D-21 footgun 2)"
    - "the job sets the EXTERNAL env CROSSWAKE_ANDROID_CONNECTED_TESTS: \"0\" — NOT the internal RUN_CONNECTED_TESTS (RESEARCH correction)"
    - "aggregator if: always() stays; only the aggregator is registered, never the individual lane (D-21 footguns 1 & 3)"
---

<objective>
Finish Phase 134 (LIFE-02c, LIFE-01a, LIFE-01b): ship the per-template-version upgrade changelog and close the dangling gen.shell.ex placeholder (D-06/07/08); promote the hermetic Android JVM generated-shell lane to merge-blocking by adding a new aggregator-fed job (D-17/18/19/21); and keep iOS advisory with honest support_matrix labels (D-20).

Purpose: give hosts an actionable upgrade changelog, make the Android generated-shell proof actually gate merges (with zero new required-check registration), and label support truthfully.
Output: native_shell_upgrade.md, the line-254 replacement, the android-generated-shell-unit CI job + aggregator needs edit, and the support_matrix edits.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/134-shell-lifecycle-native-uat-promotion/134-CONTEXT.md
@.planning/phases/134-shell-lifecycle-native-uat-promotion/134-PATTERNS.md
@.github/workflows/native-behavioral-proof-gate.yml
@guides/support_matrix.md
@guides/native_shell.md
@lib/crosswake/runtime_line/rebuild_policy.ex
</context>

<artifacts_this_phase_produces>
FULL PHASE 134 ARTIFACT INVENTORY (every symbol this phase creates, across all plans):
- `@template_version` module attribute (integer epoch, starts at 1) — Plan 01
- 2-line stamp comment header in 17 stamp-eligible `.eex` templates — Plan 01
- `.crosswake/shell.json` manifest + fields `crosswake_version`, `template_version`, `platform`, `generated_at`, `params{router,local,target}`, do-not-hand-edit marker — Plan 01
- `mix crosswake.bump_template_version` task — Plan 01
- `test/crosswake/proof/phase134_template_version_drift_test.exs` (drift + non-vacuity, stable ids `proof.life_02a.*`) — Plan 00/01
- `mix crosswake.shell.status` task with switches `--target PATH`, `--format json` — Plan 02
- `mix crosswake.gen.shell --diff` switch — Plan 03
- file→change-class advisory lookup helper — Plan 03
- `guides/native_shell_upgrade.md` — Plan 04 (this plan)
- the line-254 placeholder replacement in `crosswake.gen.shell.ex` — Plan 04
- `android-generated-shell-unit` CI job in `native-behavioral-proof-gate.yml` — Plan 04
- updated `guides/support_matrix.md` iOS/Android shell rows — Plan 04
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Write native_shell_upgrade.md + replace the gen.shell.ex line-254 placeholder (LIFE-02c)</name>
  <read_first>
    - guides/native_shell.md (voice: calm/specific/actionable; host-owned contract; no "DO NOT EDIT"; banned words)
    - guides/android_uat.md (label vocabulary)
    - lib/crosswake/runtime_line/rebuild_policy.ex (classify/2 verdict vocabulary the guide derives from — D-08)
    - lib/mix/tasks/crosswake.gen.shell.ex line 254 (the placeholder inside shell_readme/1)
    - 134-PATTERNS.md "native_shell_upgrade.md" section (lines 380-430) + "Line 254 replacement" (lines 329-336)
  </read_first>
  <files>guides/native_shell_upgrade.md, lib/mix/tasks/crosswake.gen.shell.ex</files>
  <action>
    Create guides/native_shell_upgrade.md as a per-template-version changelog, newest-first (D-06). Each version section leads with the RebuildPolicy verdict (`ota_safe` or `{:rebuild_required, :native_shell|:companion_shell}`) and the named change-class atoms, then three subsections: "What changed", "Should you rebuild?" (quoting the actual `RuntimeLine.RebuildPolicy.classify/2` call + return — the verdict is DERIVED from the policy vocabulary, not free prose, D-08), and "Files changed in this template version". Even OTA-safe entries document what changed (host files are host-owned → every upgrade is elective). Include a "Template Version 1 (initial)" baseline section, and a section for the phase's shipped epoch (whatever Plan 01 set — match the @template_version value). Calm BRAND-SPEC voice; no banned words (magic/seamless/powerful/universal); code identifiers in backticks. The guide MUST literally reference `RebuildPolicy.classify/2` so the doc-presence test passes.

    Replace the dangling placeholder line inside `shell_readme/1` (the bullet that currently promises "patch-or-doc guidance") with a real pointer: a bullet directing hosts to follow `guides/native_shell_upgrade.md` for the per-version changelog and rebuild guidance (D-07). After the edit, the file must no longer contain the old placeholder phrase. <!-- planner-discipline-allow: patch-or-doc guidance -->
  </action>
  <verify>
    <automated>cd /Users/jon/projects/crosswake && test -f guides/native_shell_upgrade.md && grep -q "RebuildPolicy.classify/2" guides/native_shell_upgrade.md && grep -vq "patch-or-doc guidance" lib/mix/tasks/crosswake.gen.shell.ex && mix test test/crosswake/guides/native_shell_upgrade_test.exs 2>&1 | grep -qE "0 failures" && echo "upgrade guide present + placeholder gone + doc test GREEN"</automated>
  </verify>
  <acceptance_criteria>
    - guides/native_shell_upgrade.md exists and references `RebuildPolicy.classify/2`.
    - The guide has at least one version section leading with a RebuildPolicy verdict.
    - `grep "patch-or-doc guidance" lib/mix/tasks/crosswake.gen.shell.ex` returns nothing (placeholder replaced).
    - `mix test test/crosswake/guides/native_shell_upgrade_test.exs` → 0 failures.
    - No banned words (magic/seamless/powerful/universal) in the guide.
  </acceptance_criteria>
  <done>The upgrade changelog exists, derives verdicts from RebuildPolicy, the line-254 placeholder is replaced with a real pointer, and the doc-presence test is GREEN.</done>
</task>

<task type="auto">
  <name>Task 2: Add the merge-blocking android-generated-shell-unit CI job + wire the aggregator (LIFE-01a)</name>
  <read_first>
    - .github/workflows/native-behavioral-proof-gate.yml (the android-package-unit job block lines 54-68, the ios-package-unit advisory job, the merge-blocking-native-behavioral-proof aggregator lines 86-96 — needs:/if: always()/alls-green)
    - script/verify_generated_android_shell.sh (line 9: RUN_CONNECTED_TESTS="${CROSSWAKE_ANDROID_CONNECTED_TESTS:-1}" — the external env var to set; JAVA_HOME check)
    - 134-PATTERNS.md "native-behavioral-proof-gate.yml (MODIFY)" section (lines 434-504)
    - 134-RESEARCH.md RQ5 "CI Lane YAML Shape" (lines 476-557) — env var correction, four footguns
    - .github/workflows/phase73-proof.yml (the project-standard setup-beam pin to reuse — SEE action below)
  </read_first>
  <files>.github/workflows/native-behavioral-proof-gate.yml</files>
  <action>
    Add a NEW job `android-generated-shell-unit` to native-behavioral-proof-gate.yml (D-17). Do NOT extend the existing `android-package-unit` job (it tests the package/library, not a generated shell — conflation makes the jvm_hermetic claim dishonest). The new job: `runs-on: ubuntu-latest`; `env: CROSSWAKE_ANDROID_CONNECTED_TESTS: "0"` (the EXTERNAL env var the script reads — NOT the internal RUN_CONNECTED_TESTS; verified at script line 9); steps: actions/checkout@v4, then `erlef/setup-beam` (REQUIRED — the script calls `mix crosswake.gen.shell android`, D-21 footgun 2) pinned to the SAME SHA + versions the rest of the repo uses — use `erlef/setup-beam@fc68ffb90438ef2936bbb3251622353b3dcb2f93 # v1` with `elixir-version: "1.19.5"` and `otp-version: "27.3"` (the project standard verified in phase73-proof.yml; do NOT use the RESEARCH placeholder 1.18/27); then `actions/setup-java@v4` with `distribution: temurin` / `java-version: '17'` (satisfies the script's JAVA_HOME check and skips its slow self-install); then `run: script/verify_generated_android_shell.sh`; then a step-summary echo.

    Wire it into the aggregator (D-18): change `needs: [android-package-unit]` to `needs: [android-package-unit, android-generated-shell-unit]`. KEEP `if: always()` (D-21 footgun 1 — removing it lets a skipped red dependency pass) and the `re-actors/alls-green` rollup with `jobs: ${{ toJSON(needs) }}` UNCHANGED. Do NOT add android-generated-shell-unit to branch protection directly (D-21 footgun 3 / D-19 — register ONLY the aggregator; it is already the registered required check, so fanning into needs: propagates merge-blocking with zero new registration). Do NOT call register_required_checks.sh in this phase.
  </action>
  <verify>
    <automated>cd /Users/jon/projects/crosswake && grep -q "android-generated-shell-unit" .github/workflows/native-behavioral-proof-gate.yml && grep -q 'CROSSWAKE_ANDROID_CONNECTED_TESTS: "0"' .github/workflows/native-behavioral-proof-gate.yml && grep -q "erlef/setup-beam" .github/workflows/native-behavioral-proof-gate.yml && grep -q "needs: \[android-package-unit, android-generated-shell-unit\]" .github/workflows/native-behavioral-proof-gate.yml && grep -q "if: always()" .github/workflows/native-behavioral-proof-gate.yml && python3 -c "import yaml,sys; yaml.safe_load(open('.github/workflows/native-behavioral-proof-gate.yml'))" && echo "CI job added, aggregator wired, if:always intact, YAML valid"</automated>
  </verify>
  <acceptance_criteria>
    - The workflow contains a job `android-generated-shell-unit` with `CROSSWAKE_ANDROID_CONNECTED_TESTS: "0"`, `erlef/setup-beam` (SHA-pinned, elixir 1.19.5 / otp 27.3), and `actions/setup-java` Temurin 17.
    - The aggregator `needs:` lists BOTH `android-package-unit` and `android-generated-shell-unit`.
    - `if: always()` and `re-actors/alls-green` are unchanged on the aggregator.
    - The job does NOT extend android-package-unit and is NOT added to branch protection directly.
    - The YAML parses cleanly (`python3 -c yaml.safe_load`).
  </acceptance_criteria>
  <done>The hermetic Android generated-shell JVM lane is merge-blocking via the aggregator needs:, with the correct env var, Elixir+JDK setup, and all four D-21 footguns honored.</done>
</task>

<task type="auto">
  <name>Task 3: Honest iOS-advisory / Android-merge-blocking labels in support_matrix.md (LIFE-01b)</name>
  <read_first>
    - guides/support_matrix.md (the label legend ~12-28, the iOS/Android shell rows ~69-70, the Phase 134 promotion table ~164-175)
    - 134-PATTERNS.md "support_matrix.md (MODIFY)" section (lines 508-526) — the exact target row labels and verbatim caveat
  </read_first>
  <files>guides/support_matrix.md</files>
  <action>
    Update the Shell Artifacts rows (D-20). android_shell row → status `merge-blocking proof (JVM hermetic)` with proof source `native-behavioral-proof-gate / android-generated-shell-unit` and the VERBATIM caveat in the notes column: "JVM hermetic proof is not emulator evidence or physical-device proof." ios_shell row → keep `verification required` with an explicit advisory note: "advisory — not wired as a required CI lane; macOS/Xcode toolchain not guaranteed in CI." No "iOS supported/verified in CI" over-promise (BRAND-SPEC support-truth discipline). If a Phase 134 promotion table exists (~164-175) referencing `shell.android.jvm_hermetic`, update its status to reflect the now-merge-blocking lane consistent with the row edit. Reuse the existing label legend labels (`merge-blocking proof`, `JVM hermetic proof`, `verification required`) — do not invent new label vocabulary.
  </action>
  <verify>
    <automated>cd /Users/jon/projects/crosswake && grep -q "JVM hermetic proof is not emulator evidence or physical-device proof" guides/support_matrix.md && grep -q "advisory — not wired as a required CI lane" guides/support_matrix.md && grep -q "android-generated-shell-unit" guides/support_matrix.md && echo "support_matrix labels honest (android merge-blocking, ios advisory)"</automated>
  </verify>
  <acceptance_criteria>
    - The android_shell row reads `merge-blocking proof (JVM hermetic)` and carries the verbatim caveat "JVM hermetic proof is not emulator evidence or physical-device proof."
    - The ios_shell row keeps `verification required` and carries the verbatim advisory note "advisory — not wired as a required CI lane; macOS/Xcode toolchain not guaranteed in CI."
    - No "iOS supported in CI" / "iOS verified" over-promise appears.
    - The proof source references `android-generated-shell-unit`.
  </acceptance_criteria>
  <done>support_matrix.md labels the Android shell as merge-blocking JVM-hermetic (with the honest caveat) and iOS as advisory, with no over-promise.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| PR author → CI gate | a PR must not merge if the hermetic Android generated-shell proof fails |
| docs → host expectations | support_matrix labels set the host's trust in CI coverage |

## STRIDE Threat Register (ASVS L1, block-on: high)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-134-04-01 | Elevation of Privilege | red lane merges anyway (skipped==success) | high | mitigate | Keep `if: always()` + re-actors/alls-green on the aggregator so a skipped/failed dependency blocks the merge (D-21 footgun 1) |
| T-134-04-02 | Tampering | CI job silently no-ops (missing Elixir) | medium | mitigate | erlef/setup-beam present so `mix crosswake.gen.shell` actually runs (D-21 footgun 2); SHA-pinned action prevents tag-move supply-chain swap |
| T-134-04-03 | Spoofing | over-promised iOS support label | medium | mitigate | Honest advisory label + verbatim caveat; no "verified in CI" claim (D-20, BRAND-SPEC support-truth) |
| T-134-04-04 | Tampering | double-registration of individual lane | low | mitigate | Register ONLY the aggregator; the new lane fans in via needs: (D-21 footgun 3 / D-19) |
</threat_model>

<verification>
- guides/native_shell_upgrade.md exists, references classify/2; placeholder gone; doc test GREEN.
- android-generated-shell-unit job present with correct env/setup; aggregator needs both jobs; if: always() intact; YAML valid.
- support_matrix.md carries the honest Android merge-blocking + iOS advisory labels with verbatim caveats.
- `mix verify` green for the phase gate.
</verification>

<success_criteria>
LIFE-02c, LIFE-01a, LIFE-01b satisfied: a real upgrade changelog replaces the dangling placeholder; the hermetic Android JVM generated-shell lane is merge-blocking via the aggregator (zero new registration, all footguns honored); iOS stays advisory with honest labels.
</success_criteria>

<output>
Create `.planning/phases/134-shell-lifecycle-native-uat-promotion/134-04-SUMMARY.md` when done. Record the post-merge follow-up (D-21 footgun 4): on the first aggregator run after merge, confirm `needs.android-generated-shell-unit.result` appears in the alls-green toJSON(needs) input.
</output>
