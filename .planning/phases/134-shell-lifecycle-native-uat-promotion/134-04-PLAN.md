---
phase: 134-shell-lifecycle-native-uat-promotion
plan: 04
type: execute
wave: 4
depends_on: ["134-01", "134-03"]
files_modified:
  - guides/native_shell_upgrade.md
  - lib/mix/tasks/crosswake.gen.shell.ex
  - test/crosswake/guides/native_shell_upgrade_test.exs
  - .github/workflows/native-behavioral-proof-gate.yml
  - lib/crosswake/support_matrix/support_matrix.ex
  - guides/support_matrix.md
  - mix.exs
autonomous: true
requirements: [LIFE-02c, LIFE-01a, LIFE-01b]
must_haves:
  truths:
    - "guides/native_shell_upgrade.md provides a per-template-version changelog leading with the RebuildPolicy verdict and referencing classify/2 (D-06/D-08)"
    - "The gen.shell.ex line-254 placeholder is replaced with a real pointer to the upgrade guide (D-07)"
    - "The hermetic Android JVM generated-shell lane (android-generated-shell-unit) is merge-blocking via the aggregator's needs: (D-17/D-18/D-19)"
    - "iOS stays advisory; CANONICAL support_matrix.ex AND the rendered support_matrix.md labels are honest and in parity with the verbatim JVM-hermetic caveat (D-20; REVIEW FIX finding 5)"
    - "guides/native_shell_upgrade.md is registered in mix.exs docs extras so it publishes to HexDocs (REVIEW FIX finding 5 / Codex)"
  artifacts:
    - guides/native_shell_upgrade.md
    - "android-generated-shell-unit job on macos-latest in native-behavioral-proof-gate.yml (REVIEW FIX finding 2)"
    - "updated CANONICAL lib/crosswake/support_matrix/support_matrix.ex shell rows + rendered guides/support_matrix.md in parity (REVIEW FIX finding 5)"
    - "guides/native_shell_upgrade.md entry in mix.exs docs extras (REVIEW FIX finding 5)"
  key_links:
    - "the new CI job runs on macos-latest (REVIEW FIX finding 2 — the verify script is mac-specific: arch() exit-1s off-mac, commandlinetools-mac zip, adoptium/mac JDK); ubuntu-latest would download the mac cmdline-tools zip and die"
    - "the new CI job needs erlef/setup-beam (Elixir, the script calls mix crosswake.gen.shell) AND actions/setup-java Temurin 17 (D-21 footgun 2)"
    - "the job sets the EXTERNAL env CROSSWAKE_ANDROID_CONNECTED_TESTS: \"0\" — NOT the internal RUN_CONNECTED_TESTS (RESEARCH correction)"
    - "aggregator if: always() stays; only the aggregator is registered, never the individual lane (D-21 footguns 1 & 3)"
    - "support truth is CANONICAL in support_matrix.ex and RENDERED into guides/support_matrix.md; the phase69 docs-contract parity test enforces they move together — edit source + regenerate guide (REVIEW FIX finding 5)"
    - "guides/native_shell_upgrade.md must be added to mix.exs docs extras or it will not publish to HexDocs (REVIEW FIX finding 5)"
---

<objective>
Finish Phase 134 (LIFE-02c, LIFE-01a, LIFE-01b): ship the per-template-version upgrade changelog and close the dangling gen.shell.ex placeholder (D-06/07/08); promote the hermetic Android JVM generated-shell lane to merge-blocking by adding a new aggregator-fed job (D-17/18/19/21); and keep iOS advisory with honest support_matrix labels (D-20).

Purpose: give hosts an actionable upgrade changelog, make the Android generated-shell proof actually gate merges (with zero new required-check registration), and label support truthfully.
Output: native_shell_upgrade.md, the line-254 replacement, the android-generated-shell-unit CI job + aggregator needs edit, and the support_matrix edits.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/134-shell-lifecycle-native-uat-promotion/134-CONTEXT.md
@.planning/phases/134-shell-lifecycle-native-uat-promotion/134-PATTERNS.md
@.github/workflows/native-behavioral-proof-gate.yml
@guides/support_matrix.md
@guides/native_shell.md
@lib/crosswake/runtime_line/rebuild_policy.ex
</context>

<artifacts_this_phase_produces>
FULL PHASE 134 ARTIFACT INVENTORY (every symbol this phase creates, across all plans):
- `@template_version` module attribute (integer epoch, starts at 1) — Plan 01
- public `Mix.Tasks.Crosswake.Gen.Shell.template_version/0` accessor (single source of truth for Plans 02/03 — REVIEW FIX finding 3) — Plan 01
- 2-line stamp comment header in the **15** stamp-eligible `.eex` templates (derived from `@ios_templates`/`@android_templates`, incl. `gradle.properties`, excl. `project.pbxproj`/`gradlew`/`gradlew.bat` and the never-rendered README `.eex` — REVIEW FIX finding 4) — Plan 01
- generated-output stamp test `test/mix/tasks/crosswake_gen_shell_stamp_test.exs` (REVIEW FIX finding 4) — Plan 01
- `.crosswake/shell.json` manifest at the **per-platform generated root** (`<target>/native/<platform>/crosswake_shell/.crosswake/shell.json` — REVIEW FIX finding 1) + fields `crosswake_version`, `template_version`, `platform`, `generated_at`, `params{router,local,target}`, do-not-hand-edit marker — Plan 01
- `mix crosswake.bump_template_version` task — Plan 01
- `test/crosswake/proof/phase134_template_version_drift_test.exs` (drift + non-vacuity, stable ids `proof.life_02a.*`) — Plan 00/01
- `mix crosswake.shell.status` task with switches `--target PATH`, `--format json` (consumes `template_version/0`) — Plan 02
- `mix crosswake.gen.shell --diff` switch (diffs ALL templates except `project.pbxproj`; `gradlew`/`gradlew.bat` ARE diffed — REVIEW FIX finding 6) — Plan 03
- file→change-class advisory lookup helper — Plan 03
- `guides/native_shell_upgrade.md` — Plan 04 (this plan)
- the line-254 placeholder replacement in `crosswake.gen.shell.ex` — Plan 04
- `android-generated-shell-unit` CI job on **macos-latest** in `native-behavioral-proof-gate.yml` (REVIEW FIX finding 2) — Plan 04
- updated CANONICAL `lib/crosswake/support_matrix/support_matrix.ex` shell rows + rendered `guides/support_matrix.md` in parity (REVIEW FIX finding 5) — Plan 04
- `guides/native_shell_upgrade.md` entry in `mix.exs` docs extras (REVIEW FIX finding 5) — Plan 04
</artifacts_this_phase_produces>

<review_feedback_addressed>
Cross-AI review (134-REVIEWS.md) resolution for this plan:
- **Finding 2 [HIGH — both] — Android CI runner ↔ verify-script env mismatch.** INCORPORATED (Task 2):
  `android-generated-shell-unit` now runs on `macos-latest` (the runner the mac-specific script targets:
  arch() exit-1s off-mac, commandlinetools-mac zip, adoptium/mac JDK). Justified in the action: ubuntu would
  download the mac cmdline-tools zip and die; provisioning Linux cmdline-tools does not satisfy the script's
  `cmdline-tools/latest/` path check. Lane stays hermetic+fast (CONNECTED_TESTS=0 → JVM-only, no emulator).
  `if: always()` + `re-actors/alls-green` aggregator topology preserved; only the aggregator is registered
  (no brittle new individual required check).
- **Finding 5 [MEDIUM — Codex] — edits rendered docs but not canonical support truth.** INCORPORATED
  (Task 3 + new Task 4): Task 3 now edits CANONICAL `support_matrix.ex` (shell `support_entry` rows +
  `shell.android.jvm_hermetic` promotion narrative) AND regenerates `guides/support_matrix.md` in parity,
  verified by the phase69 docs-contract parity test plus renderer/support-matrix tests (editing only the
  .md would have FAILED parity). The promotion-table row (`shell.android.jvm_hermetic`) is updated explicitly
  alongside the shell-artifacts row. New Task 4 adds `guides/native_shell_upgrade.md` to `mix.exs` docs extras
  so the upgrade changelog publishes to HexDocs.
</review_feedback_addressed>

<tasks>

<task type="auto">
  <name>Task 1: Write native_shell_upgrade.md + replace the gen.shell.ex line-254 placeholder (LIFE-02c)</name>
  <read_first>
    - guides/native_shell.md (voice: calm/specific/actionable; host-owned contract; no "DO NOT EDIT"; banned words)
    - guides/android_uat.md (label vocabulary)
    - lib/crosswake/runtime_line/rebuild_policy.ex (classify/2 verdict vocabulary the guide derives from — D-08)
    - lib/mix/tasks/crosswake.gen.shell.ex line 254 (the placeholder inside shell_readme/1)
    - 134-PATTERNS.md "native_shell_upgrade.md" section (lines 380-430) + "Line 254 replacement" (lines 329-336)
  </read_first>
  <files>guides/native_shell_upgrade.md, lib/mix/tasks/crosswake.gen.shell.ex</files>
  <action>
    Create guides/native_shell_upgrade.md as a per-template-version changelog, newest-first (D-06). Each version section leads with the RebuildPolicy verdict (`ota_safe` or `{:rebuild_required, :native_shell|:companion_shell}`) and the named change-class atoms, then three subsections: "What changed", "Should you rebuild?" (quoting the actual `RuntimeLine.RebuildPolicy.classify/2` call + return — the verdict is DERIVED from the policy vocabulary, not free prose, D-08), and "Files changed in this template version". Even OTA-safe entries document what changed (host files are host-owned → every upgrade is elective). Include a "Template Version 1 (initial)" baseline section, and a section for the phase's shipped epoch (whatever Plan 01 set — match the @template_version value). Calm BRAND-SPEC voice; no banned words (magic/seamless/powerful/universal); code identifiers in backticks. The guide MUST literally reference `RebuildPolicy.classify/2` so the doc-presence test passes.

    Replace the dangling placeholder line inside `shell_readme/1` (the bullet that currently promises "patch-or-doc guidance") with a real pointer: a bullet directing hosts to follow `guides/native_shell_upgrade.md` for the per-version changelog and rebuild guidance (D-07). After the edit, the file must no longer contain the old placeholder phrase. <!-- planner-discipline-allow: patch-or-doc guidance -->
  </action>
  <verify>
    <automated>cd /Users/jon/projects/crosswake && test -f guides/native_shell_upgrade.md && grep -q "RebuildPolicy.classify/2" guides/native_shell_upgrade.md && grep -vq "patch-or-doc guidance" lib/mix/tasks/crosswake.gen.shell.ex && mix test test/crosswake/guides/native_shell_upgrade_test.exs 2>&1 | grep -qE "0 failures" && echo "upgrade guide present + placeholder gone + doc test GREEN"</automated>
  </verify>
  <acceptance_criteria>
    - guides/native_shell_upgrade.md exists and references `RebuildPolicy.classify/2`.
    - The guide has at least one version section leading with a RebuildPolicy verdict.
    - `grep "patch-or-doc guidance" lib/mix/tasks/crosswake.gen.shell.ex` returns nothing (placeholder replaced).
    - `mix test test/crosswake/guides/native_shell_upgrade_test.exs` → 0 failures.
    - No banned words (magic/seamless/powerful/universal) in the guide.
  </acceptance_criteria>
  <done>The upgrade changelog exists, derives verdicts from RebuildPolicy, the line-254 placeholder is replaced with a real pointer, and the doc-presence test is GREEN.</done>
</task>

<task type="auto">
  <name>Task 2: Add the merge-blocking android-generated-shell-unit CI job + wire the aggregator (LIFE-01a)</name>
  <read_first>
    - .github/workflows/native-behavioral-proof-gate.yml (the android-package-unit job block lines 54-68, the ios-package-unit advisory job, the merge-blocking-native-behavioral-proof aggregator lines 86-96 — needs:/if: always()/alls-green)
    - script/verify_generated_android_shell.sh (line 9: RUN_CONNECTED_TESTS="${CROSSWAKE_ANDROID_CONNECTED_TESTS:-1}" — the external env var to set; JAVA_HOME check)
    - 134-PATTERNS.md "native-behavioral-proof-gate.yml (MODIFY)" section (lines 434-504)
    - 134-RESEARCH.md RQ5 "CI Lane YAML Shape" (lines 476-557) — env var correction, four footguns
    - .github/workflows/phase73-proof.yml (the project-standard setup-beam pin to reuse — SEE action below)
  </read_first>
  <files>.github/workflows/native-behavioral-proof-gate.yml</files>
    REVIEW FIX (finding 2 [HIGH — both reviewers] — runner ↔ script environment must agree): the prior
    revision put this job on `ubuntu-latest`, but `script/verify_generated_android_shell.sh` is
    macOS-specific in ways that are NOT optional fallbacks — `arch()` (script:24-33) hard-`exit 1`s on any
    non-macOS uname path it reaches, the cmdline-tools download is `commandlinetools-mac-...zip` (script:10),
    and the JDK self-install targets adoptium `.../mac/...` (script:85). On Ubuntu, `actions/setup-java`
    satisfies the JDK short-circuit (script:58-62), but `install_android_tools_if_needed` (script:102) STILL
    runs and, when `sdkmanager` is absent at the exact `${SDK_ROOT}/cmdline-tools/latest/bin/sdkmanager`
    path (script:105), downloads the MAC cmdline-tools zip — dead-on-arrival on Linux. Provisioning Linux
    cmdline-tools via `android-actions/setup-android` does NOT reliably satisfy the script's `latest/`
    subdir check (that action installs under a versioned `cmdline-tools/<ver>/bin`), so it would still fall
    through to the mac download.

    DECISION (justified): run `android-generated-shell-unit` on **`macos-latest`** — the runner the script
    was actually written for — so runner and script agree with ZERO edits to the mac-specific verification
    script. This is one of the two resolutions both reviewers offered ("run on a macOS runner if the script
    is truly mac-only"). The lane stays hermetic and FAST: `CROSSWAKE_ANDROID_CONNECTED_TESTS: "0"` means
    JVM-only (`testDebugUnitTest`), no emulator, no AVD, no connected-test branch (script:244-247 returns
    early) — so the macOS cost is a single JVM gradle run, not an emulator boot. Do NOT rewrite the script
    to be Linux-aware in this phase (out of scope; the script is shared with other mac-only verification
    paths and a Linux port is its own change). If a future phase wants Linux, that is a separate, explicit
    script-portability task.

  <action>
    Add a NEW job `android-generated-shell-unit` to native-behavioral-proof-gate.yml (D-17). Do NOT extend the existing `android-package-unit` job (it tests the package/library, not a generated shell — conflation makes the jvm_hermetic claim dishonest). The new job: `runs-on: macos-latest` (REVIEW FIX finding 2 — matches the mac-specific verify script; NOT ubuntu-latest); `env: CROSSWAKE_ANDROID_CONNECTED_TESTS: "0"` (the EXTERNAL env var the script reads — NOT the internal RUN_CONNECTED_TESTS; verified at script line 9); steps: actions/checkout@v4, then `erlef/setup-beam` (REQUIRED — the script calls `mix crosswake.gen.shell android`, D-21 footgun 2) pinned to the SAME SHA + versions the rest of the repo uses — use `erlef/setup-beam@fc68ffb90438ef2936bbb3251622353b3dcb2f93 # v1` with `elixir-version: "1.19.5"` and `otp-version: "27.3"` (the project standard verified in phase73-proof.yml; do NOT use the RESEARCH placeholder 1.18/27); then `actions/setup-java@v4` with `distribution: temurin` / `java-version: '17'` (satisfies the script's JAVA_HOME check and skips its slow self-install); then `run: script/verify_generated_android_shell.sh`; then a step-summary echo. NOTE: `erlef/setup-beam` supports macOS runners; if the executor finds the pinned setup-beam SHA does not resolve Elixir/OTP on `macos-latest`, install via the project's macOS-supported path (e.g. asdf/Homebrew) at the SAME elixir 1.19.5 / otp 27.3 versions — but try setup-beam first.

    Wire it into the aggregator (D-18): change `needs: [android-package-unit]` to `needs: [android-package-unit, android-generated-shell-unit]`. KEEP `if: always()` (D-21 footgun 1 — removing it lets a skipped red dependency pass) and the `re-actors/alls-green` rollup with `jobs: ${{ toJSON(needs) }}` UNCHANGED. Do NOT add android-generated-shell-unit to branch protection directly (D-21 footgun 3 / D-19 — register ONLY the aggregator; it is already the registered required check, so fanning into needs: propagates merge-blocking with zero new registration). Do NOT call register_required_checks.sh in this phase.
  </action>
  <verify>
    <automated>cd /Users/jon/projects/crosswake && grep -q "android-generated-shell-unit" .github/workflows/native-behavioral-proof-gate.yml && grep -q 'CROSSWAKE_ANDROID_CONNECTED_TESTS: "0"' .github/workflows/native-behavioral-proof-gate.yml && grep -q "erlef/setup-beam" .github/workflows/native-behavioral-proof-gate.yml && grep -q "needs: \[android-package-unit, android-generated-shell-unit\]" .github/workflows/native-behavioral-proof-gate.yml && grep -q "if: always()" .github/workflows/native-behavioral-proof-gate.yml && python3 -c "import yaml; d=yaml.safe_load(open('.github/workflows/native-behavioral-proof-gate.yml')); assert d['jobs']['android-generated-shell-unit']['runs-on']=='macos-latest', 'android-generated-shell-unit must run on macos-latest (finding 2)'" && echo "CI job added on macos-latest, aggregator wired, if:always intact, YAML valid"</automated>
  </verify>
  <acceptance_criteria>
    - The workflow contains a job `android-generated-shell-unit` on `runs-on: macos-latest` (REVIEW FIX finding 2 — matches the mac-specific verify script) with `CROSSWAKE_ANDROID_CONNECTED_TESTS: "0"`, `erlef/setup-beam` (SHA-pinned, elixir 1.19.5 / otp 27.3), and `actions/setup-java` Temurin 17.
    - The aggregator `needs:` lists BOTH `android-package-unit` and `android-generated-shell-unit`.
    - `if: always()` and `re-actors/alls-green` are unchanged on the aggregator.
    - The job does NOT extend android-package-unit and is NOT added to branch protection directly.
    - The YAML parses cleanly (`python3 -c yaml.safe_load`).
  </acceptance_criteria>
  <done>The hermetic Android generated-shell JVM lane is merge-blocking via the aggregator needs:, with the correct env var, Elixir+JDK setup, and all four D-21 footguns honored.</done>
</task>

<task type="auto">
  <name>Task 3: Honest iOS-advisory / Android-merge-blocking labels in CANONICAL support truth + rendered guide (LIFE-01b)</name>
  <read_first>
    - lib/crosswake/support_matrix/support_matrix.ex (CANONICAL TRUTH — the shell support_entry rows at ~416-439: `ios_shell` and `android_shell`, each with proof_status/proof/notes; the `shell.android.jvm_hermetic` promotion_rule at ~1388-1417; the validators validate_verification_method_invariant / validate_rebuild_matrix_evidence at ~456-457 that must still pass)
    - lib/crosswake/support_matrix/renderer.ex (the guide is RENDERED from canonical truth — render/1 at ~18, "Shell Artifacts" section at ~48)
    - guides/support_matrix.md (the RENDERED artifact: label legend ~12-27 already carries the "JVM hermetic proof is not emulator evidence..." caveat at line 23; iOS/Android shell rows ~69-70; promotion table ~164-175)
    - test/crosswake/proof/phase69_docs_contract_parity_test.exs (reads guides/support_matrix.md AND SupportMatrix.canonical() and asserts PARITY — editing only the .md would FAIL this test; source + rendered guide MUST move together)
    - test/crosswake/support_matrix/renderer_test.exs and support_matrix_test.exs (the renderer/canonical tests that must stay green)
    - 134-PATTERNS.md "support_matrix.md (MODIFY)" section (lines 508-526) — the exact target row labels and verbatim caveat
  </read_first>
  <files>lib/crosswake/support_matrix/support_matrix.ex, guides/support_matrix.md</files>
  <action>
    REVIEW FIX (finding 5 [MEDIUM — Codex] — edit canonical truth, not just the rendered doc): `Crosswake.SupportMatrix`
    is the canonical source shared by manifest, doctor, and docs; `guides/support_matrix.md` is RENDERED from it
    (renderer.ex:18) and `phase69_docs_contract_parity_test.exs` asserts the two stay in PARITY. Editing only the
    markdown (as the prior revision did) leaves doctor/manifest support truth stale AND breaks the parity test.
    So edit the SOURCE first, then bring the rendered guide into parity.

    1. In `lib/crosswake/support_matrix/support_matrix.ex`, update the `android_shell` shell `support_entry`
       (~428-438) so its `notes` reflect the now-merge-blocking hermetic Android JVM generated-shell lane and
       carry the VERBATIM caveat "JVM hermetic proof is not emulator evidence or physical-device proof." and a
       reference to the `android-generated-shell-unit` lane / `native-behavioral-proof-gate`. Update the
       `ios_shell` `support_entry` (~417-427) notes to keep iOS advisory with the VERBATIM note "advisory — not
       wired as a required CI lane; macOS/Xcode toolchain not guaranteed in CI." Keep `proof_status` values
       consistent with the validators: the `shell.android.jvm_hermetic` promotion_rule (~1388) already encodes
       `current_proof_class: :advisory, promotes_to: :merge_blocking` with `required_verification_method:
       :jvm_hermetic` — this phase makes that promotion REAL via the CI lane, so update the promotion_rule's
       narrative/demotion text only as needed to reflect that the lane now exists, WITHOUT claiming device_verified
       (the validators at ~1069-1112 reject any device_verified laundering — do not touch device_verified rows).
       Make the MINIMUM source change that (a) records the Android shell's merge-blocking JVM-hermetic posture with
       the honest caveat, (b) keeps iOS advisory, and (c) keeps `validate/1`, `validate_verification_method_invariant`,
       and `validate_rebuild_matrix_evidence` GREEN. Do NOT invent new status atoms (allowed: :supported,
       :verification_required, :unsupported; proof_classes: :merge_blocking, :advisory, :not_applicable).
    2. Regenerate / update `guides/support_matrix.md` so it is in PARITY with the edited canonical source (run the
       project's support-matrix render path — the renderer produces the guide; reproduce its output, do not
       hand-diverge). Confirm the rendered Android shell row carries the verbatim caveat and the iOS row the
       verbatim advisory note, and that the Phase 134 promotion table (~164-175, the `shell.android.jvm_hermetic`
       row at ~174) reflects the now-merge-blocking lane.

    Reuse existing label vocabulary (`merge-blocking proof`, `JVM hermetic proof`, `verification required`) — do
    not invent new labels. The legend caveat string already exists at guides/support_matrix.md:23; ensure the
    Android shell ROW also surfaces it per D-20.
  </action>
  <verify>
    <automated>cd /Users/jon/projects/crosswake && grep -q "JVM hermetic proof is not emulator evidence or physical-device proof" guides/support_matrix.md && grep -q "advisory — not wired as a required CI lane" guides/support_matrix.md && grep -q "android-generated-shell-unit" guides/support_matrix.md && grep -q "android-generated-shell-unit\|JVM hermetic" lib/crosswake/support_matrix/support_matrix.ex && mix test test/crosswake/proof/phase69_docs_contract_parity_test.exs test/crosswake/support_matrix/renderer_test.exs test/crosswake/support_matrix/support_matrix_test.exs 2>&1 | grep -qE "0 failures" && echo "canonical support truth + rendered guide in parity; android merge-blocking, ios advisory"</automated>
  </verify>
  <acceptance_criteria>
    - `lib/crosswake/support_matrix/support_matrix.ex` (CANONICAL) is edited so the Android shell support truth reflects the merge-blocking JVM-hermetic lane with the verbatim caveat, and iOS stays advisory (REVIEW FIX finding 5 — not just the rendered doc).
    - The rendered `guides/support_matrix.md` is in PARITY with the source: android_shell row carries the verbatim caveat "JVM hermetic proof is not emulator evidence or physical-device proof." and references `android-generated-shell-unit`; ios_shell row carries the verbatim advisory note.
    - `mix test test/crosswake/proof/phase69_docs_contract_parity_test.exs` is GREEN (source ↔ guide parity holds).
    - `mix test test/crosswake/support_matrix/renderer_test.exs test/crosswake/support_matrix/support_matrix_test.exs` is GREEN (`validate/1` and the verification-method / rebuild-matrix invariants still pass; no device_verified laundering).
    - No "iOS supported in CI" / "iOS verified" over-promise appears; no new label vocabulary invented.
  </acceptance_criteria>
  <done>Canonical `support_matrix.ex` AND the rendered `guides/support_matrix.md` both label the Android shell as merge-blocking JVM-hermetic (honest caveat) and iOS as advisory, in parity, with the docs-contract + renderer + support-matrix tests GREEN.</done>
</task>

<task type="auto">
  <name>Task 4: Register guides/native_shell_upgrade.md in mix.exs docs extras (LIFE-02c)</name>
  <read_first>
    - mix.exs (the `extras:` list inside `docs` ~101-129 — guide extras end at `guides/telemetry.md`; finding 5 / Codex suggestion)
    - guides/native_shell_upgrade.md (the guide created in Task 1 — must exist before being listed)
  </read_first>
  <files>mix.exs</files>
  <action>
    REVIEW FIX (finding 5 / Codex suggestion — add the new guide to docs extras): add
    `"guides/native_shell_upgrade.md"` to the `extras:` list in `mix.exs` `docs` (the guide extras list
    currently ends at `guides/telemetry.md`, ~127). Place it near the other native-shell guides
    (`guides/native_shell.md`, `guides/android_uat.md`) for a sensible ExDoc sidebar grouping. This is what
    publishes the new upgrade changelog into the generated HexDocs alongside the rest of the guides; without
    it, the per-template-version changelog (Task 1) would not appear in docs.
  </action>
  <verify>
    <automated>cd /Users/jon/projects/crosswake && grep -q '"guides/native_shell_upgrade.md"' mix.exs && test -f guides/native_shell_upgrade.md && (mix compile --warnings-as-errors 2>&1 | tail -1) && echo "native_shell_upgrade.md registered in docs extras"</automated>
  </verify>
  <acceptance_criteria>
    - `mix.exs` `docs` `extras:` includes `"guides/native_shell_upgrade.md"`.
    - The referenced file exists (Task 1 created it).
    - `mix compile --warnings-as-errors` clean (no broken docs config).
  </acceptance_criteria>
  <done>The new upgrade guide is registered in mix.exs docs extras so it publishes to HexDocs with the other guides.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| PR author → CI gate | a PR must not merge if the hermetic Android generated-shell proof fails |
| docs → host expectations | support_matrix labels set the host's trust in CI coverage |

## STRIDE Threat Register (ASVS L1, block-on: high)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-134-04-01 | Elevation of Privilege | red lane merges anyway (skipped==success) | high | mitigate | Keep `if: always()` + re-actors/alls-green on the aggregator so a skipped/failed dependency blocks the merge (D-21 footgun 1) |
| T-134-04-02 | Tampering | CI job silently no-ops (missing Elixir) | medium | mitigate | erlef/setup-beam present so `mix crosswake.gen.shell` actually runs (D-21 footgun 2); SHA-pinned action prevents tag-move supply-chain swap |
| T-134-04-03 | Spoofing | over-promised iOS support label | medium | mitigate | Honest advisory label + verbatim caveat; no "verified in CI" claim (D-20, BRAND-SPEC support-truth) |
| T-134-04-04 | Tampering | double-registration of individual lane | low | mitigate | Register ONLY the aggregator; the new lane fans in via needs: (D-21 footgun 3 / D-19) |
</threat_model>

<verification>
- guides/native_shell_upgrade.md exists, references classify/2; placeholder gone; doc test GREEN.
- guides/native_shell_upgrade.md is listed in mix.exs docs extras (REVIEW FIX finding 5); mix compile clean.
- android-generated-shell-unit job present on macos-latest (REVIEW FIX finding 2) with correct env/setup; aggregator needs both jobs; if: always() intact; YAML valid.
- CANONICAL support_matrix.ex AND rendered support_matrix.md carry the honest Android merge-blocking + iOS advisory labels with verbatim caveats, in PARITY (REVIEW FIX finding 5); phase69 docs-contract parity + renderer + support-matrix tests GREEN.
- `mix verify` green for the phase gate.
</verification>

<success_criteria>
LIFE-02c, LIFE-01a, LIFE-01b satisfied: a real upgrade changelog replaces the dangling placeholder; the hermetic Android JVM generated-shell lane is merge-blocking via the aggregator (zero new registration, all footguns honored); iOS stays advisory with honest labels.
</success_criteria>

<output>
Create `.planning/phases/134-shell-lifecycle-native-uat-promotion/134-04-SUMMARY.md` when done. Record the post-merge follow-up (D-21 footgun 4): on the first aggregator run after merge, confirm `needs.android-generated-shell-unit.result` appears in the alls-green toJSON(needs) input.
</output>
