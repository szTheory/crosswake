---
phase: 134-shell-lifecycle-native-uat-promotion
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - test/crosswake/proof/phase134_template_version_drift_test.exs
  - test/mix/tasks/crosswake_shell_status_test.exs
  - test/mix/tasks/crosswake_gen_shell_diff_test.exs
  - test/crosswake/guides/native_shell_upgrade_test.exs
autonomous: true
requirements: [LIFE-02a, LIFE-02b, LIFE-02c]
must_haves:
  truths:
    - "Four red test files exist so every downstream task has an automated verify target (Nyquist)"
    - "The drift test asserts live-hash == checked-in-hash and is RED until Plan 01 stamps the templates and bumps the hash"
  artifacts:
    - test/crosswake/proof/phase134_template_version_drift_test.exs
    - test/mix/tasks/crosswake_shell_status_test.exs
    - test/mix/tasks/crosswake_gen_shell_diff_test.exs
    - test/crosswake/guides/native_shell_upgrade_test.exs
  key_links:
    - "Drift test @checked_in_hash line must match the exact regex shape that crosswake.bump_template_version (Plan 01) rewrites"
    - "Stable IDs proof.life_02a.template_version_drift / .non_vacuity.ios / .non_vacuity.android are the contract the verifier greps for"
---

<objective>
Create the four NEW test files (Wave 0 scaffold) that every downstream Plan 01–05 task verifies against, so no code-producing task lands without an automated verify target. Per 134-VALIDATION.md, all four test files are new — no existing infrastructure covers LIFE-02a/b/c.

Purpose: Nyquist sampling — establish the RED tests first so Plan 01 (stamp+manifest+drift) turns the drift test GREEN, Plan 02 (shell.status) turns the status test GREEN, Plan 03 (--diff) turns the diff test GREEN, and Plan 04 (guide) turns the doc-presence test GREEN.
Output: 4 test files, each compiling and runnable, expected RED until their producing plan lands.
</objective>

<review_feedback_addressed>
Cross-AI review (134-REVIEWS.md) resolution for this plan:
- **Finding 7 [MEDIUM — OpenCode] — broken "expected RED" verify command.** INCORPORATED in Task 1's
  `<verify>`: the drift test is expected to fail first, so the old `mix test ... && grep ...` chain
  never reached the grep. Now the run is `|| true`-guarded into `/tmp/cw_drift_red.log`, then we grep
  the saved log for the contract stable id `proof.life_02a.template_version_drift` AND a `N failure`
  line — positively asserting the intended RED state (preserves the Nyquist guard).
</review_feedback_addressed>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/134-shell-lifecycle-native-uat-promotion/134-VALIDATION.md
@.planning/phases/134-shell-lifecycle-native-uat-promotion/134-PATTERNS.md
@test/crosswake/proof/phase132_compat_matrix_drift_test.exs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Drift test scaffold (LIFE-02a) + non-vacuity guards</name>
  <read_first>
    - test/crosswake/proof/phase132_compat_matrix_drift_test.exs (exact analog — module header, async: true, File.cwd!() anchor, ProofAssertions.stable_id_message/7 call shape, non-vacuity guard)
    - 134-PATTERNS.md "phase134_template_version_drift_test.exs" section (lines 24-108)
    - 134-RESEARCH.md "Drift Test Structure" code example (lines 700-752)
  </read_first>
  <files>test/crosswake/proof/phase134_template_version_drift_test.exs</files>
  <action>
    Create the drift test module Crosswake.Proof.Phase134TemplateVersionDriftTest mirroring the phase132 analog: `use ExUnit.Case, async: true`, `alias Crosswake.TestSupport.ProofAssertions`, and a `@template_dir` module attribute anchored with `Path.join([File.cwd!(), "priv", "templates", "crosswake", "shell"])` (NOT Application.app_dir/2 — pitfall 2 in RESEARCH).

    Add a module attribute `@checked_in_hash` on its own dedicated line, initialized to the all-zero 64-char placeholder string. This exact line shape is the rewrite target for crosswake.bump_template_version in Plan 01 — keep it a bare `@checked_in_hash "..."` literal so a regex can find it.

    Add a private helper `live_template_hash/0` that globs `@template_dir/**/*.eex`, sorts with Enum.sort/1 (deterministic bytewise order — pitfall 8), reads each file, joins the bytes, hashes with `:crypto.hash(:sha256, blob)`, and Base.encode16(case: :lower).

    Test 1 ("template hash matches checked-in hash (drift guard)"): assert `live_template_hash() == @checked_in_hash`, failing with ProofAssertions.stable_id_message/7 using stable id `proof.life_02a.template_version_drift`, interpolating both the live and checked-in hash into the observed arg, hint "run `mix crosswake.bump_template_version`", posture `:merge_blocking`.

    Test 2 ("at least 1 iOS and 1 Android template present (non-vacuity guard)"): count `ios/**/*.eex` and `android/**/*.eex` separately; two asserts, each `>= 1`, with distinct stable ids `proof.life_02a.non_vacuity.ios` and `proof.life_02a.non_vacuity.android`, posture `:merge_blocking`.

    This test is EXPECTED RED on the all-zero placeholder hash — Plan 01's bump turns it GREEN. The non-vacuity tests pass immediately (templates already exist).
  </action>
  <verify>
    <!-- REVIEW FIX (finding 7, OpenCode MEDIUM): the drift test is EXPECTED to FAIL on the
         all-zero placeholder hash. A `mix test ... && grep ...` chain short-circuits at the
         failing `mix test`, so the grep never runs and the RED state is never asserted (a lost
         Nyquist guard). Capture the failing output with `|| true`, then grep the saved log for
         the stable id so we positively confirm the INTENDED red state (test ran, failed, and
         emitted the contract stable id) rather than failing for some unrelated reason. -->
    <automated>cd /Users/jon/projects/crosswake && (mix test test/crosswake/proof/phase134_template_version_drift_test.exs > /tmp/cw_drift_red.log 2>&1 || true) && grep -q "proof.life_02a.template_version_drift" /tmp/cw_drift_red.log && grep -qE "[0-9]+ failure" /tmp/cw_drift_red.log && echo "drift test compiles, runs, and is RED with the contract stable id (expected on placeholder hash)"</automated>
  </verify>
  <acceptance_criteria>
    - File compiles under `mix test` (no CompileError).
    - The drift-guard test FAILS with a message containing `proof.life_02a.template_version_drift` (RED expected — placeholder hash).
    - The non-vacuity test PASSES (templates exist).
    - `grep -c '@checked_in_hash "' test/crosswake/proof/phase134_template_version_drift_test.exs` returns exactly 1.
  </acceptance_criteria>
  <done>Drift test file exists, compiles, drift-guard test is RED with the stable id, non-vacuity guards GREEN, and the `@checked_in_hash` line is regex-rewritable.</done>
</task>

<task type="auto">
  <name>Task 2: shell.status + gen.shell --diff + upgrade-guide test scaffolds</name>
  <read_first>
    - test/crosswake/proof/phase132_compat_matrix_drift_test.exs (ExUnit doc-presence + File.cwd!() anchor shape)
    - 134-VALIDATION.md Per-Task Verification Map (the exact behaviors each test must cover)
    - 134-RESEARCH.md "shell.status Exit Code Pattern" (lines 755-785) for the exit-code behaviors to assert
  </read_first>
  <files>test/mix/tasks/crosswake_shell_status_test.exs, test/mix/tasks/crosswake_gen_shell_diff_test.exs, test/crosswake/guides/native_shell_upgrade_test.exs</files>
  <action>
    Create three RED test files, each compiling, each tagged so the suite does not break before the producing plan lands. Use a module tag `@moduletag :phase134_pending` on the status and diff modules and skip-guard each test that invokes not-yet-existing tasks behind an existence check (e.g. `if Code.ensure_loaded?(Mix.Tasks.Crosswake.Shell.Status)`), so the file compiles GREEN-skipped rather than CompileError before Plan 02/03. The verifier in Plan 02/03 removes the skip guard.

    crosswake_shell_status_test.exs (LIFE-02b status, Plan 02 target): test stubs for the three exit-code behaviors — exit 0 when up-to-date or not-a-shell, exit 2 when at least one behind, exit 1 on bad/unreadable manifest. Drive via `Mix.Task.rerun("crosswake.shell.status", args)` inside a tmp dir with a written `.crosswake/shell.json`, capturing the catch of `exit({:shutdown, N})`. Each test pending-skipped until the task module loads.

    crosswake_gen_shell_diff_test.exs (LIFE-02b diff, Plan 03 target): test stubs for diff-no-write (snapshot file mtimes/contents before and after a `--diff` run, assert byte-identical), diff-stdout (captured output contains a unified-diff `---`/`+++` header), and pbxproj-excluded (captured output does NOT contain `project.pbxproj`). Pending-skipped until the `--diff` switch exists.

    native_shell_upgrade_test.exs (LIFE-02c, Plan 04 target): doc-presence test — assert `guides/native_shell_upgrade.md` exists via File.exists? and its contents reference `RebuildPolicy.classify/2`; and a structural test that `lib/mix/tasks/crosswake.gen.shell.ex` no longer contains the literal placeholder phrase. <!-- planner-discipline-allow: patch-or-doc guidance -->  (Express the negative grep by reading the gen.shell.ex file and refute String.contains? on the placeholder phrase; do NOT paste the placeholder phrase as a bare comment in the test body.) Use File.cwd!() anchored paths.
  </action>
  <verify>
    <automated>mix test test/mix/tasks/crosswake_shell_status_test.exs test/mix/tasks/crosswake_gen_shell_diff_test.exs test/crosswake/guides/native_shell_upgrade_test.exs 2>&1 | grep -qE "[0-9]+ tests?" && echo "three scaffolds compile and run (pending-skipped)"</automated>
  </verify>
  <acceptance_criteria>
    - All three files compile under `mix test` (no CompileError).
    - Tests for not-yet-built capabilities are skipped (not errored) via the `Code.ensure_loaded?` / existence guards.
    - native_shell_upgrade_test.exs FAILS on the doc-presence assertion (RED — guide not yet created) OR is pending-skipped; either is acceptable as long as it compiles.
    - No test in these three files turns the broader `mix test` suite red with a CompileError.
  </acceptance_criteria>
  <done>Three scaffold files exist, compile, and their not-yet-implemented assertions are pending-skipped or cleanly RED — ready for Plans 03/04/05 to turn GREEN.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test harness → filesystem | Tests read templates and write tmp dirs; no untrusted external input |

## STRIDE Threat Register (ASVS L1, block-on: high)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-134-00-01 | Tampering | drift-test glob path | low | accept | File.cwd!()-anchored glob over in-repo templates only; no user input crosses this boundary |
| T-134-00-02 | Denial of Service | vacuous-pass (empty glob) | medium | mitigate | Non-vacuity guards (≥1 iOS AND ≥1 Android) fail :merge_blocking if templates vanish (pitfall 4) |
</threat_model>

<verification>
- `mix test test/crosswake/proof/phase134_template_version_drift_test.exs` runs; drift-guard RED, non-vacuity GREEN.
- `mix test test/mix/tasks/crosswake_shell_status_test.exs test/mix/tasks/crosswake_gen_shell_diff_test.exs test/crosswake/guides/native_shell_upgrade_test.exs` runs with no CompileError.
- `mix test` whole suite does not regress to a CompileError from these new files.
</verification>

<success_criteria>
All four NEW test files exist, compile, and provide an automated verify target for every downstream Plan 01–05 code-producing task. The drift-guard test is RED (placeholder hash), non-vacuity guards GREEN.
</success_criteria>

<output>
Create `.planning/phases/134-shell-lifecycle-native-uat-promotion/134-00-SUMMARY.md` when done.
</output>
