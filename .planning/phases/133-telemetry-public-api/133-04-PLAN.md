---
phase: 133-telemetry-public-api
plan: 04
type: execute
wave: 2
depends_on: ["133-02"]
files_modified:
  - guides/telemetry.md
  - mix.exs
  - test/crosswake/proof/phase133_telemetry_contract_test.exs
autonomous: true
requirements: [TELEM-02]
must_haves:
  truths:
    - "A reader can open guides/telemetry.md and find every :active event's measurements and metadata, the Keathley naming convention, the stop ⊇ start metadata rule (with the honest threadline :exception caveat), the semver contract (D-03), failure modes, security/PII, and a minimal attach_default_logger/1 example — in brandbook §14 concept order."
    - "guides/telemetry.md is linked from hexdocs under a new 'Telemetry' group; Crosswake.Telemetry + the per-subsystem *.Telemetry modules appear under a 'Telemetry' modules group."
  artifacts:
    - "guides/telemetry.md"
    - "mix.exs (extras + groups_for_extras + groups_for_modules Telemetry entries)"
  key_links:
    - "The guide's event table matches events/0 exactly (measurements/metadata per the Grounded Seed Set) — a proof-test assertion ties the doc surface to the runtime catalog so they cannot drift."
    - "mix.exs Telemetry module group includes Crosswake.Offline.Telemetry for hexdocs discoverability ONLY (it is NOT in the events/0 aggregation — it has no event_names/0)."
---

<objective>
Wave 2 — publish the public docs. Author `guides/telemetry.md` (brandbook §14 concept order, D-19) and wire the new "Telemetry" groups into `mix.exs` (D-18). Add the lightweight proof-test assertions that the guide exists and the mix.exs group is present (TELEM-02). Runs in parallel with plan 03 (no shared files).

Purpose: TELEM-02 — a reader can find one authoritative, calm, PII-aware telemetry guide and reach it from hexdocs. The guide is the human face of the same contract events/0 enforces mechanically.

Output: `guides/telemetry.md`, modified `mix.exs`, and two added assertions in the existing proof test.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/133-telemetry-public-api/133-CONTEXT.md
@.planning/phases/133-telemetry-public-api/133-RESEARCH.md
@.planning/phases/133-telemetry-public-api/133-PATTERNS.md
@.planning/phases/133-telemetry-public-api/133-02-SUMMARY.md

# Analogs + brand rules (read first)
@guides/threadline.md
@lib/crosswake/threadline/telemetry.ex
@brandbook/BRAND-SPEC.md
@mix.exs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author guides/telemetry.md</name>
  <files>guides/telemetry.md</files>
  <read_first>
    - guides/threadline.md (the "What X Is" / "What X Is NOT" structure to mirror verbatim in tone)
    - lib/crosswake/threadline/telemetry.ex (moduledoc framing sentences to adapt)
    - brandbook/BRAND-SPEC.md (§4 forbidden words; §6 API "boring on purpose" + failure-modes-first; §14 concept-page order; §20 declarative naming; §22 describe companions by specific value)
    - .planning/phases/133-telemetry-public-api/133-PATTERNS.md (§guides/telemetry.md — required section order, forbidden words, event-name formatting, log prefix)
    - .planning/phases/133-telemetry-public-api/133-RESEARCH.md (§Grounded Seed Set — exact measurements/metadata per event for the events table; Pitfall 4 — threadline :exception metadata caveat)
  </read_first>
  <action>Create guides/telemetry.md following the brandbook §14 concept order (D-19): "# Telemetry"; "What Crosswake Telemetry Is" (definition); "What Crosswake Telemetry Is NOT" (not an APM/observability platform, not distributed tracing, not a generic event bus, not a source of PII — mirror guides/threadline.md structure); "Semver Contract" (the D-03 statement — additions are non-breaking minors, removals/renames are breaking majors); "Events" (a table of every :active event from events/0 with its measurements and metadata, event names in backtick code spans / JetBrains Mono per D-19, and an explicit note that for spans stop metadata ⊇ start metadata BUT the threadline `:exception` event may carry empty metadata because the PII guard drops kind/reason — per RESEARCH Pitfall 4, document this honestly, do not blanket-claim stop ⊇ start for the exception case); "Reserved Events" (a brief note that Sigra/Chimeway events are declared but not yet emitted — the :reserved tier); "Attaching the Default Logger" (a minimal `attach_default_logger/1` example, noting core never auto-attaches and the host calls it from its supervision tree); "Failure Modes" (no companions configured → core+in-tree only; double-attach → {:error, :already_exists}; per brandbook §6 failure-modes-first); "Security and PII" (forbidden_metadata_keys denylist + safe_value? guard; D-15); "Testing" (how host suites use `:telemetry_test.attach_event_handlers/2`); "Related" (links to companion_contract.md and threadline.md). Use the `[crosswake]` log/CLI prefix (D-20). Do NOT use any of the forbidden words: seamless, magic, plugin, powerful, universal, auto_* (brandbook §4/§20). Keep prose calm, specific, declarative (§6/§20).</action>
  <verify>
    <automated>test -f guides/telemetry.md && grep -qi "is not" guides/telemetry.md && grep -q "attach_default_logger" guides/telemetry.md && ! grep -riE 'seamless|magic|plugin|powerful|universal' guides/telemetry.md && echo "GUIDE_OK"</automated>
  </verify>
  <done>guides/telemetry.md exists, contains the required sections (What Is / What Is NOT / Semver / Events / Reserved / Attaching / Failure Modes / Security / Testing / Related), documents per-event measurements+metadata with the threadline :exception caveat, and contains zero forbidden words.</done>
  <acceptance_criteria>
    - File exists with all 10 sections in brandbook §14 order.
    - The Events section lists every :active event with measurements and metadata matching the Grounded Seed Set.
    - The threadline `:exception` empty-metadata caveat is stated (does NOT claim thread_id present on exception).
    - `grep -riE 'seamless|magic|plugin|powerful|universal'` over the guide returns nothing.
    - The D-03 semver statement is present.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Wire the Telemetry docs groups into mix.exs</name>
  <files>mix.exs</files>
  <read_first>
    - mix.exs lines 95-181 (extras list ending at "guides/threadline.md"; groups_for_modules with "Companion Contract" last; groups_for_extras with Truth then "Extension Authors")
    - .planning/phases/133-telemetry-public-api/133-PATTERNS.md (§mix.exs — exact additions for extras, groups_for_modules, groups_for_extras)
    - .planning/phases/133-telemetry-public-api/133-RESEARCH.md (§mix.exs Docs Wiring — grounded insertion points)
  </read_first>
  <action>Edit mix.exs: (1) add `"guides/telemetry.md"` to the `extras:` list (after `"guides/threadline.md"`). (2) Add a `"Telemetry"` entry to `groups_for_modules:` after the `"Companion Contract"` group, listing `Crosswake.Telemetry, Crosswake.Threadline.Telemetry, Crosswake.Companions.Sigra.Telemetry, Crosswake.Companions.Chimeway.Telemetry, Crosswake.Offline.Telemetry` (Offline.Telemetry is included for hexdocs discoverability ONLY — it is a telemetry contract module — even though it is not in the events/0 aggregation). (3) Add a `"Telemetry"` entry to `groups_for_extras:` after the `Truth` group, containing `"guides/telemetry.md"`. Use scoped Edits — do NOT rewrite the whole file. Preserve existing ordering and syntax exactly.</action>
  <verify>
    <automated>mix compile --warnings-as-errors 2>&1 | tail -3; grep -c "guides/telemetry.md" mix.exs; grep -c "Crosswake.Telemetry" mix.exs</automated>
  </verify>
  <done>mix.exs lists guides/telemetry.md in extras, a Telemetry group in groups_for_extras, and a Telemetry module group in groups_for_modules; the project compiles cleanly.</done>
  <acceptance_criteria>
    - `grep -c "guides/telemetry.md" mix.exs` ≥ 2 (extras + groups_for_extras).
    - `groups_for_modules` contains a `"Telemetry"` group including `Crosswake.Telemetry` and the four `*.Telemetry` modules (incl. `Crosswake.Offline.Telemetry`).
    - `mix compile --warnings-as-errors` succeeds.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add TELEM-02 doc-presence assertions to the proof test</name>
  <files>test/crosswake/proof/phase133_telemetry_contract_test.exs</files>
  <read_first>
    - test/crosswake/proof/phase133_telemetry_contract_test.exs (the proof test from plan 01 — ADD two tests; do not disturb the contract tests)
    - .planning/phases/133-telemetry-public-api/133-PATTERNS.md (§ProofAssertions.stable_id_message/7 usage)
  </read_first>
  <behavior>
    - Test "TELEM-02 guide exists": asserts `File.exists?("guides/telemetry.md")` and that it contains the required section headings and a Semver Contract statement.
    - Test "TELEM-02 mix.exs Telemetry group present": reads mix.exs source and asserts a "Telemetry" group string is present in groups_for_modules and that "guides/telemetry.md" appears in extras.
    - Both use ProofAssertions.stable_id_message/7 for failure detail; the hermetic-lane guard already present must still pass.
  </behavior>
  <action>Append two tests to test/crosswake/proof/phase133_telemetry_contract_test.exs: one asserting guides/telemetry.md exists and contains the "What Crosswake Telemetry Is NOT", "Semver Contract", and "Events" headings; one reading mix.exs via File.read! and asserting both `"guides/telemetry.md"` and a `"Telemetry"` group token are present. Use `ProofAssertions.stable_id_message/7` for the failure messages with stable_ids like `proof.telem_02.guide.exists` and `proof.telem_02.mix_group`. Do NOT modify the existing contract/merge/reserved/fail-closed/hermetic tests.</action>
  <verify>
    <automated>mix test test/crosswake/proof/phase133_telemetry_contract_test.exs 2>&1 | tail -10</automated>
  </verify>
  <done>The two TELEM-02 doc-presence tests pass GREEN alongside the existing contract tests; the hermetic-lane guard still passes.</done>
  <acceptance_criteria>
    - Two new tests present, both GREEN.
    - The full phase133 proof file passes (all contract + doc + hermetic tests).
    - No `@moduletag` introduced (hermetic-lane test stays green).
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| guide prose → adopter expectations | Inaccurate metadata claims (e.g. "thread_id always present on exception") would mislead operators; the guide must be honest about the PII-guard caveat. |
| mix.exs docs config → published hexdocs | Misconfigured groups would hide or mis-place the telemetry module/guide. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-133-12 | Information Disclosure | guide encouraging logging of PII | high | mitigate | The "Security and PII" section documents the forbidden_metadata_keys denylist + safe_value? guard and instructs hosts never to add PII to event metadata (D-15); no example logs a PII value. |
| T-133-13 | Repudiation | guide misstating event metadata (exception caveat) | medium | mitigate | Guide states the threadline `:exception` empty-metadata caveat honestly (RESEARCH Pitfall 4); the events table matches events/0 (Task 3 ties doc surface to runtime catalog). |
| T-133-14 | Tampering | docs group misconfiguration hiding the module | low | accept | `mix compile --warnings-as-errors` + proof-test group assertion catch syntactic/grouping errors; full render verified in the manual-only `mix docs` check (133-VALIDATION.md). |
</threat_model>

<verification>
- `mix compile --warnings-as-errors` clean.
- `mix test test/crosswake/proof/phase133_telemetry_contract_test.exs` — all GREEN (contract + doc + hermetic).
- Guide contains no forbidden words; all §14 sections present.
- Manual-only (per 133-VALIDATION.md): `mix docs` then inspect that the "Telemetry" group renders with guides/telemetry.md linked.
</verification>

<success_criteria>
- A discoverable, accurate, PII-aware telemetry guide under a hexdocs "Telemetry" group, with measurements/metadata per event and the honest exception caveat (TELEM-02).
</success_criteria>

<artifacts_this_phase_produces>
This plan produces:
- `guides/telemetry.md` — the public telemetry guide.
- `mix.exs` additions — `"guides/telemetry.md"` in extras; a "Telemetry" group in `groups_for_extras`; a "Telemetry" module group in `groups_for_modules` (incl. `Crosswake.Offline.Telemetry` for discoverability only).
- Two TELEM-02 doc-presence assertions added to `test/crosswake/proof/phase133_telemetry_contract_test.exs`.
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/133-telemetry-public-api/133-04-SUMMARY.md` when done.
</output>
