---
phase: 133-telemetry-public-api
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - test/crosswake/proof/phase133_telemetry_contract_test.exs
  - test/crosswake/telemetry_test.exs
  - test/support/stub_companion.ex
autonomous: true
requirements: [TELEM-01, TELEM-03, TELEM-04]
must_haves:
  truths:
    - "A bidirectional contract test file exists and fails RED (the facade module does not yet exist) — proving the test drives real behavior, not a stub."
    - "A stub companion implementing the optional telemetry_events/0 callback exists in test/support so the merge mechanism can be proven without core naming a real companion (D-17)."
    - "A unit test file for attach_default_logger/1 + detach_default_logger/0 exists and fails RED."
  artifacts:
    - "test/crosswake/proof/phase133_telemetry_contract_test.exs"
    - "test/crosswake/telemetry_test.exs"
    - "test/support/stub_companion.ex (StubTelemetryCompanion module added)"
  key_links:
    - "Proof test derives event lists from Crosswake.Telemetry.events/0 at runtime (D-05) — not hardcoded — so it stays honest as the catalog grows."
    - "Stub companion is registered via Application.put_env(:crosswake, :companions, ...) with save/restore in setup/on_exit (async: false)."
---

<objective>
Wave 0 — write the failing tests first. Scaffold the merge-blocking bidirectional contract proof, the attach/detach logger unit test, and the `StubTelemetryCompanion` fixture. These tests MUST fail RED at the end of this plan because the production module `Crosswake.Telemetry` does not exist yet — that RED state is the forcing function for Waves 1–2.

Purpose: Establish the Nyquist test seams (TELEM-01/03/04) before any implementation, so the facade, callback, logger, guide, and docs are all built against a concrete, executable contract rather than interpreted prose.

Output: Three test/fixture files. The proof test and logger test compile but fail (RED) until the facade and logger land. The stub companion compiles standalone (it only depends on `Crosswake.Companion`, which already exists).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/133-telemetry-public-api/133-CONTEXT.md
@.planning/phases/133-telemetry-public-api/133-RESEARCH.md
@.planning/phases/133-telemetry-public-api/133-PATTERNS.md

# Analog test files to mirror (read these first)
@test/crosswake/proof/phase129_companion_contract_freeze_test.exs
@test/crosswake/proof/phase130_fail_closed_contract_test.exs
@test/support/stub_companion.ex

# Emission sites the proof test must drive (read for setup shape)
@lib/crosswake/compatibility/route_gate.ex
@lib/crosswake/doctor/doctor.ex
@lib/crosswake/plug/threadline.ex
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add StubTelemetryCompanion fixture (D-17 merge proof)</name>
  <files>test/support/stub_companion.ex</files>
  <read_first>
    - test/support/stub_companion.ex (existing StubCompanion module — mirror the 6 required @impl callbacks and @moduledoc false convention; ADD a new module at end of file, do NOT modify existing stubs)
    - lib/crosswake/companion.ex (the 6 required callbacks: companion_id/0, enabled?/1, route_gated?/2, kill_switch_active?/1, validate_dependency/0, report_state/0)
    - .planning/phases/133-telemetry-public-api/133-PATTERNS.md (§test/support/stub_companion.ex — exact stub shape, lines 313-355)
  </read_first>
  <behavior>
    - StubTelemetryCompanion implements all 6 required Crosswake.Companion callbacks with @impl true (sensible stubs; companion_id is :stub_telemetry — NOT an alias to any extracted companion, EXTRACT-03 safe).
    - It additionally implements the OPTIONAL telemetry_events/0 callback (NO @impl true annotation — optional callbacks carry no @impl) returning a single event_doc map for prefix [:crosswake, :stub_telemetry, :example] with tier :active, a description, measurements [:duration], metadata [:companion_id].
    - The module compiles cleanly today (it only depends on Crosswake.Companion, which exists). The event_doc map keys (event/tier/description/measurements/metadata) match the typespec to be defined in plan 02.
  </behavior>
  <action>Append a new module `Crosswake.TestSupport.StubTelemetryCompanion` to test/support/stub_companion.ex, mirroring the existing StubCompanion stub shape per 133-PATTERNS.md. Implement the 6 required callbacks with @impl true. Use `companion_id` value `:stub_telemetry`. Build `report_state/0` returning a `%Crosswake.Companion.State{}` with `:unconfigured` gate/kill-switch statuses (mirror the existing stub). Add the optional `telemetry_events/0` (no @impl annotation) returning a list with one event_doc map: keys exactly `event`, `tier`, `description`, `measurements`, `metadata`; event prefix `[:crosswake, :stub_telemetry, :example]`, tier `:active`, measurements `[:duration]`, metadata `[:companion_id]`. Do NOT modify the existing StubCompanion or any other stub in the file.</action>
  <verify>
    <automated>mix compile --warnings-as-errors 2>&1 | tail -5; mix test test/support/stub_companion.ex 2>&1 | tail -3 || true</automated>
  </verify>
  <done>StubTelemetryCompanion compiles with no warnings; it implements 6 required callbacks plus the optional telemetry_events/0; existing stubs unchanged.</done>
  <acceptance_criteria>
    - `function_exported?(Crosswake.TestSupport.StubTelemetryCompanion, :telemetry_events, 0)` returns true after compile.
    - The module passes `@behaviour Crosswake.Companion` (all 6 required callbacks present).
    - `companion_id/0` returns `:stub_telemetry` (not a frozen-companion alias).
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Write the bidirectional contract proof test (RED)</name>
  <files>test/crosswake/proof/phase133_telemetry_contract_test.exs</files>
  <read_first>
    - test/crosswake/proof/phase130_fail_closed_contract_test.exs (async: false; Application.put_env save/restore in setup/on_exit; hermetic-lane self-assertion at bottom — copy verbatim)
    - test/crosswake/proof/phase129_companion_contract_freeze_test.exs (ProofAssertions.stable_id_message/7 usage; moduledoc structure)
    - .planning/phases/133-telemetry-public-api/133-RESEARCH.md (§Test Seams; §Pattern 3 :telemetry_test.attach_event_handlers/2 message shape {event_name, ref, measurements, metadata}; §Grounded Seed Set for exact measurement/metadata keys per event)
    - .planning/phases/133-telemetry-public-api/133-PATTERNS.md (§phase133 proof test — declared⇒emitted, emitted⇒declared, companion merge, reserved-tier exclusion, hermetic-lane test blocks)
    - lib/crosswake/compatibility/route_gate.ex, lib/crosswake/doctor/doctor.ex, lib/crosswake/plug/threadline.ex (to construct minimal inputs that drive the real emission paths)
  </read_first>
  <behavior>
    - Test "TELEM-04 Side A declared⇒emitted": derives the :active event prefixes from Crosswake.Telemetry.events/0 at runtime, expands each to start/stop/exception, attaches via :telemetry_test.attach_event_handlers(self(), names), drives the REAL code paths (RouteGate dependency_check/kill_switch/route_gate spans, Doctor validate_dependency span, Plug.Threadline request triplet) using the StubTelemetryCompanion (or a stub that triggers each span), and assert_received each declared :active event with the declared measurement/metadata keys present (subset assertion: declared keys ⊆ emitted keys).
    - Test "TELEM-04 Side B emitted⇒declared": attaches handlers for ALL declared names (:active + :reserved), drives all paths, collects every [:crosswake,...] event seen, asserts captured -- declared == [].
    - Test "TELEM-01 companion merge": registers StubTelemetryCompanion via Application.put_env, asserts its declared events appear in events/0; deregisters in on_exit.
    - Test "TELEM-04 reserved tier excluded": asserts the :reserved tier has ≥24 entries (Sigra 14 + Chimeway 10), and no :reserved event prefix also appears in the :active tier.
    - Test "fail-closed (D-10)": with :companions set to [], events/0 returns only core+in-tree events and does NOT raise.
    - Hermetic-lane self-assertion test (no @moduletag): copy verbatim from phase130.
    - async: false; setup saves/restores Application.get_env(:crosswake, :companions, []).
  </behavior>
  <action>Create test/crosswake/proof/phase133_telemetry_contract_test.exs as `Crosswake.Proof.Phase133TelemetryContractTest` using `ExUnit.Case, async: false`, alias `Crosswake.TestSupport.ProofAssertions`. Implement the five behavior tests above plus the hermetic-lane guard, deriving every event list from `Crosswake.Telemetry.events/0` at runtime per D-05 (never hardcode the catalog). Drive each real emission path: register a stub companion in `:companions`, call `RouteGate` evaluation to fire dependency_check/kill_switch/route_gate spans, call `Doctor` to fire validate_dependency, and call `Crosswake.Plug.Threadline.call/2` with a minimal `%Plug.Conn{}` to fire the threadline triplet — consult the three emission-site files for exact call signatures. Use `:telemetry_test.attach_event_handlers/2` and `assert_received {name, ^ref, measurements, metadata}`. For Side A assert declared measurement/metadata keys are a subset of the emitted maps (per the Grounded Seed Set: dependency_check/kill_switch/route_gate measurements include companion_id/route_id; threadline metadata includes thread_id/correlation_id/route_id/source; threadline :exception may carry EMPTY metadata — assert only duration there, NOT thread_id). Use `ProofAssertions.stable_id_message/7` for failure detail. Copy the hermetic-lane self-assertion verbatim from phase130. This test MUST fail RED now (Crosswake.Telemetry does not exist). Do NOT add any @moduletag.</action>
  <verify>
    <automated>mix test test/crosswake/proof/phase133_telemetry_contract_test.exs 2>&1 | tail -15</automated>
  </verify>
  <done>The proof test compiles and FAILS (RED) with errors referencing the missing Crosswake.Telemetry module (UndefinedFunctionError or compile error on the alias) — confirming the test is wired to real behavior, not a passing stub.</done>
  <acceptance_criteria>
    - File defines exactly the five contract tests + the hermetic-lane guard test.
    - `async: false` is set; setup save/restores `:companions`.
    - Event lists are derived from `Crosswake.Telemetry.events/0` at runtime (grep: no literal `[:crosswake, :companion, :dependency_check]` list embedded as the source-of-truth catalog — driving inputs may name events, but the assertion baseline derives from events/0).
    - Running the file produces a RED result citing the missing facade.
    - No `@moduletag` line present (the hermetic-lane test enforces this).
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Write the attach_default_logger/1 unit test (RED)</name>
  <files>test/crosswake/telemetry_test.exs</files>
  <read_first>
    - .planning/phases/133-telemetry-public-api/133-RESEARCH.md (§Pattern 6 attach_default_logger/1 signature + behaviors; D-13/D-14)
    - .planning/phases/133-telemetry-public-api/133-PATTERNS.md (§attach_default_logger/1 — handler id "crosswake-default-logger", encode:false default, :exception forced to :error)
    - test/crosswake/proof/phase130_fail_closed_contract_test.exs (async: false + on_exit cleanup idiom)
  </read_first>
  <behavior>
    - Test: attach_default_logger/0 (default opts) registers a handler with id "crosswake-default-logger" and returns :ok; on_exit detaches.
    - Test: a second attach_default_logger call returns {:error, :already_exists} (relies on :telemetry, no custom guard — D-13).
    - Test: detach_default_logger/0 removes the handler and returns :ok; a second detach returns {:error, :not_found}.
    - Test: when an :exception event is emitted, the logger logs at :error level even when configured level is :info (D-14) — assert via ExUnit's `import ExUnit.CaptureLog` and `assert capture_log(...) =~` an [crosswake] error line.
    - Test: PII-safety — a forbidden metadata key (e.g. :access_token) present on an emitted event is NOT present in the captured log output (D-15).
    - async: false (telemetry handler table is global); detach in on_exit so tests are isolated.
  </behavior>
  <action>Create test/crosswake/telemetry_test.exs as `Crosswake.TelemetryTest` using `ExUnit.Case, async: false` and `import ExUnit.CaptureLog`. Write the five behavior tests above against `Crosswake.Telemetry.attach_default_logger/1` and `detach_default_logger/0`. Use handler id `"crosswake-default-logger"` (assert via `:telemetry.list_handlers/1` if convenient). For the exception-level test, emit a real `[:crosswake, ...]` exception event via `:telemetry.execute/3` after attaching with `level: :info`, and assert the captured log contains an error-level `[crosswake]` line. For the PII test, emit an event carrying `:access_token` in metadata and assert it is absent from the log. Always `on_exit(fn -> Crosswake.Telemetry.detach_default_logger() end)`. This test MUST fail RED now.</action>
  <verify>
    <automated>mix test test/crosswake/telemetry_test.exs 2>&1 | tail -15</automated>
  </verify>
  <done>The logger unit test compiles and FAILS (RED), citing the missing Crosswake.Telemetry.attach_default_logger/1.</done>
  <acceptance_criteria>
    - Five tests present: attach returns :ok, double-attach {:error, :already_exists}, detach + double-detach, exception forced to :error, PII key scrubbed.
    - Every test detaches in on_exit.
    - `async: false` set.
    - Running the file produces a RED result citing the missing logger functions.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| emitted telemetry metadata → test assertions | Untrusted/PII-bearing values could appear in event metadata; the test must prove they are scrubbed before logging. |
| test config (:companions) → global app env | Tests mutate a shared global key; must save/restore to avoid cross-test contamination. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-133-01 | Information Disclosure | logger test PII assertion | high | mitigate | Task 3 asserts a forbidden_metadata_key (`:access_token`) emitted on an event is ABSENT from captured log output (D-15) — the test is the regression guard for the scrubbing behavior built in plan 03. |
| T-133-02 | Tampering | global :companions app env mutated by tests | medium | mitigate | All tests use `async: false` and save/restore `Application.get_env(:crosswake, :companions, [])` in setup/on_exit (phase130 precedent). |
| T-133-03 | Tampering | telemetry handler table double-registration | low | accept | Test asserts `{:error, :already_exists}` is surfaced (not swallowed), relying on `:telemetry`'s built-in guard (D-13) — no custom guard. |
</threat_model>

<verification>
- `mix compile --warnings-as-errors` succeeds (stub fixture clean).
- `mix test test/crosswake/proof/phase133_telemetry_contract_test.exs` → RED citing missing `Crosswake.Telemetry`.
- `mix test test/crosswake/telemetry_test.exs` → RED citing missing logger functions.
- The RED state is the EXPECTED, correct outcome of this plan.
</verification>

<success_criteria>
- Three files created; stub fixture compiles clean; both test files fail RED for the right reason (missing production module), proving non-vacuous seams for TELEM-01/03/04.
</success_criteria>

<artifacts_this_phase_produces>
This plan produces (test-side only):
- `test/crosswake/proof/phase133_telemetry_contract_test.exs` — the new bidirectional contract test module (TELEM-04 + TELEM-01 merge proof + fail-closed).
- `test/crosswake/telemetry_test.exs` — attach/detach + exception-level + PII unit tests (TELEM-03).
- `Crosswake.TestSupport.StubTelemetryCompanion` (added to `test/support/stub_companion.ex`) — the stub companion implementing the optional `telemetry_events/0`.
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/133-telemetry-public-api/133-01-SUMMARY.md` when done.
</output>
