---
phase: 133-telemetry-public-api
plan: 02
type: execute
wave: 1
depends_on: ["133-01"]
files_modified:
  - lib/crosswake/telemetry.ex
  - lib/crosswake/companion.ex
autonomous: true
requirements: [TELEM-01, TELEM-04]
must_haves:
  truths:
    - "A developer can call Crosswake.Telemetry.events/0 and get the runtime-aggregated, self-describing catalog of every emitted [:crosswake,...] event across companion/RouteGate, doctor, threadline plus the reserved Sigra/Chimeway surface, merged with configured-companion telemetry_events/0 at runtime (D-05/D-07)."
    - "events/0 fails closed: with no companions configured it returns only core+in-tree events and never raises (D-10)."
    - "Crosswake.Companion declares an OPTIONAL telemetry_events/0 callback whose return type is [Crosswake.Telemetry.event_doc()] (D-07); the Phase 129 freeze test stays green (optional callbacks are invisible to behaviour_info(:callbacks))."
  artifacts:
    - "lib/crosswake/telemetry.ex (events/0, event_doc typespec)"
    - "lib/crosswake/companion.ex (@callback telemetry_events/0 + @optional_callbacks)"
  key_links:
    - "events/0 walks Application.get_env(:crosswake, :companions, []) and merges via function_exported?(mod, :telemetry_events, 0) — NEVER Code.ensure_loaded?, NEVER a static alias to a frozen companion (CompanionGuard / EXTRACT-03/04)."
    - "events/0 statically references ONLY in-tree *.Telemetry modules (Threadline, Sigra, Chimeway) — and NEVER Offline.Telemetry (no event_names/0; would raise)."
    - "events/0 is built by private functions called at call time — NO @core_events module attribute (stale-.beam footgun)."
---

<objective>
Wave 1 — build the aggregating facade. Create `Crosswake.Telemetry` with the `event_doc()` typespec and the runtime-aggregating `events/0`, and add the optional `telemetry_events/0` callback to `Crosswake.Companion`. This is the heart of TELEM-01 and the declared half of TELEM-04. After this plan, the Side B (emitted⇒declared), companion-merge, reserved-tier, and fail-closed tests from plan 01 turn GREEN. (Side A declared⇒emitted may also pass here if the driven paths are exercised; the logger tests stay RED until plan 03.)

Purpose: One canonical, runtime-true, self-describing, low-cardinality, PII-free catalog — the contract the DASH-01 dashboard and the contract test both consume. Runtime aggregation (D-05) avoids the v7.0 stale-.beam drift class.

Output: `lib/crosswake/telemetry.ex` (facade) and the modified `lib/crosswake/companion.ex` (optional callback).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/133-telemetry-public-api/133-CONTEXT.md
@.planning/phases/133-telemetry-public-api/133-RESEARCH.md
@.planning/phases/133-telemetry-public-api/133-PATTERNS.md
@.planning/phases/133-telemetry-public-api/133-01-SUMMARY.md

# Analog + constraint files (read first)
@lib/crosswake/threadline/telemetry.ex
@lib/crosswake/companions/sigra/telemetry.ex
@lib/crosswake/companions/chimeway/telemetry.ex
@lib/crosswake/companion.ex
@lib/crosswake/companion_guard.ex

# Grounded measurement/metadata sources
@lib/crosswake/compatibility/route_gate.ex
@lib/crosswake/doctor/doctor.ex
@lib/crosswake/plug/threadline.ex
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add optional telemetry_events/0 callback to Crosswake.Companion</name>
  <files>lib/crosswake/companion.ex</files>
  <read_first>
    - lib/crosswake/companion.ex (the 6 existing @callback declarations; add after the last one, report_state/0, near line 128)
    - .planning/phases/133-telemetry-public-api/133-PATTERNS.md (§lib/crosswake/companion.ex — exact addition block, lines 156-172; the Phase-129-safe note)
    - .planning/phases/133-telemetry-public-api/133-RESEARCH.md (§Pitfall 1 — @optional_callbacks is invisible to behaviour_info(:callbacks); Phase 129 freeze test is SAFE, do NOT modify it)
  </read_first>
  <behavior>
    - Crosswake.Companion declares `@callback telemetry_events() :: [Crosswake.Telemetry.event_doc()]` with a @doc explaining it is optional and merged into events/0 at call time.
    - `@optional_callbacks telemetry_events: 0` is declared after all @callback declarations.
    - `behaviour_info(:callbacks)` is UNCHANGED (still the 6 required callbacks); `behaviour_info(:optional_callbacks)` now contains `{:telemetry_events, 0}`.
    - The Phase 129 freeze test (test/crosswake/proof/phase129_companion_contract_freeze_test.exs) remains GREEN with NO modification.
  </behavior>
  <action>In lib/crosswake/companion.ex, after the `@callback report_state/0` declaration, add a `@doc` block and `@callback telemetry_events() :: [Crosswake.Telemetry.event_doc()]`, then `@optional_callbacks telemetry_events: 0`, exactly as specified in 133-PATTERNS.md §companion.ex. The @doc must state the callback is optional and that returned events are merged into `Crosswake.Telemetry.events/0` at call time. Do NOT touch any existing callback. Do NOT modify the Phase 129 freeze test — per D-07 research grounding, optional callbacks do not appear in `behaviour_info(:callbacks)` so that test stays green by design.</action>
  <verify>
    <automated>mix test test/crosswake/proof/phase129_companion_contract_freeze_test.exs 2>&1 | tail -5</automated>
  </verify>
  <done>Companion declares the optional telemetry_events/0 callback; the Phase 129 freeze test passes unchanged.</done>
  <acceptance_criteria>
    - `Crosswake.Companion.behaviour_info(:optional_callbacks)` includes `{:telemetry_events, 0}`.
    - `Crosswake.Companion.behaviour_info(:callbacks)` still equals the pre-existing 6-callback set.
    - phase129_companion_contract_freeze_test.exs passes with zero source changes to that file.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Build Crosswake.Telemetry facade — event_doc typespec + runtime events/0</name>
  <files>lib/crosswake/telemetry.ex</files>
  <read_first>
    - lib/crosswake/threadline/telemetry.ex (moduledoc framing "NOT an APM / tracing / event bus"; event_names/0; forbidden_metadata_keys/0)
    - lib/crosswake/companions/sigra/telemetry.ex + lib/crosswake/companions/chimeway/telemetry.ex (event_names/0 returning the reserved-tier prefixes — Sigra 14, Chimeway 10)
    - lib/crosswake/companion_guard.ex (the {:__aliases__} ban — confirms iterating :companions via function_exported?/3 is legal; static refs to in-tree *.Telemetry are legal)
    - .planning/phases/133-telemetry-public-api/133-PATTERNS.md (§lib/crosswake/telemetry.ex — moduledoc, event_doc typespec, events/0 aggregation, build_reserved_events, core active events table; the "DO NOT @core_events module attribute" and "DO NOT include Offline.Telemetry" gotchas)
    - .planning/phases/133-telemetry-public-api/133-RESEARCH.md (§Grounded Seed Set — exact measurements/metadata per event; §Pitfall 5 — span measurement semantics; D-03 semver statement; D-04 shape; D-06 uniq+sort)
    - lib/crosswake/compatibility/route_gate.ex:139,191,219 + lib/crosswake/doctor/doctor.ex:573 + lib/crosswake/plug/threadline.ex:50-76 (confirm exact measurement/metadata keys)
  </read_first>
  <behavior>
    - `@type event_doc :: %{event: [atom()], tier: :active | :reserved, description: String.t(), measurements: [atom()], metadata: [atom()]}` (D-04, keys named exactly event/tier/description/measurements/metadata).
    - `events/0` returns `[event_doc()]` built ENTIRELY at call time (no module attribute holding the catalog — D-05). It concatenates: (a) the core :active span/event docs for the 5 confirmed emitting prefixes; (b) the :reserved docs built from `Sigra.Telemetry.event_names/0 ++ Chimeway.Telemetry.event_names/0`; (c) configured-companion docs from `Application.get_env(:crosswake, :companions, []) |> Enum.flat_map(...)` merging via `function_exported?(mod, :telemetry_events, 0)`. Then `Enum.uniq_by(& &1.event) |> Enum.sort_by(& &1.event)` (D-06).
    - Core :active docs cover exactly: [:crosswake, :companion, :dependency_check], [:crosswake, :companion, :kill_switch], [:crosswake, :companion, :route_gate] (measurements [:system_time, :companion_id, :route_id]; stop adds :duration), [:crosswake, :companion, :validate_dependency] (stop adds :result), and [:crosswake, :threadline, :request] (start measurements [:system_time], stop [:duration]; metadata [:thread_id, :correlation_id, :route_id, :source]). Each carries a calm, specific description (brandbook §6, no forbidden words).
    - Fail-closed: with :companions == [], events/0 returns core+reserved only and never raises (D-10).
    - NEVER calls Offline.Telemetry (no event_names/0 → would raise). NEVER aliases a frozen/extracted companion module. NEVER uses Code.ensure_loaded? to probe companion support.
    - The moduledoc carries the D-03 semver statement ("telemetry events are public API — additions are minor; removals/renames are major") and the "Diagnostic-only / NOT an APM / NOT distributed tracing / NOT a generic event bus" framing.
  </behavior>
  <action>Create lib/crosswake/telemetry.ex as `Crosswake.Telemetry`. Write the moduledoc per 133-PATTERNS.md §telemetry.ex (semver statement D-03; diagnostic-only framing; zero-new-deps note) — obey brandbook §4 forbidden-words (no seamless/magic/plugin/powerful/universal) and §20 (no auto_*/magic). Define the `event_doc` typespec (D-04). Implement `events/0` (@spec `events() :: [event_doc()]`) building the catalog at runtime via private helpers `build_active_events/0`, `build_reserved_events/0`, and the companion merge — NO module attribute holds the catalog (D-05). Populate `build_active_events/0` from the Grounded Seed Set (read the three emission-site files to confirm keys; for the 4 companion spans companion_id/route_id are MEASUREMENTS not metadata per Pitfall 5; validate_dependency stop adds :result; threadline metadata is the 4 PROP-02 keys). Build `build_reserved_events/0` by mapping `Crosswake.Companions.Sigra.Telemetry.event_names() ++ Crosswake.Companions.Chimeway.Telemetry.event_names()` to event_doc maps with tier :reserved, empty description/measurements/metadata. Merge configured companions via `Application.get_env(:crosswake, :companions, []) |> Enum.flat_map(fn mod -> if function_exported?(mod, :telemetry_events, 0), do: mod.telemetry_events(), else: [] end)`. Finish with `Enum.uniq_by(& &1.event) |> Enum.sort_by(& &1.event)`. Do NOT reference Offline.Telemetry. Do NOT use Code.ensure_loaded?. Do NOT statically alias any extracted companion module. (attach_default_logger/1 is NOT in this plan — it lands in plan 03.)</action>
  <verify>
    <automated>mix compile --warnings-as-errors 2>&1 | tail -5; mix test test/crosswake/proof/phase133_telemetry_contract_test.exs 2>&1 | tail -15</automated>
  </verify>
  <done>events/0 returns the runtime-aggregated self-describing catalog; the proof test's Side B (emitted⇒declared), companion-merge, reserved-tier, and fail-closed tests pass GREEN. (Side A passes if its driven paths are exercised in plan 01; logger tests remain RED until plan 03.)</done>
  <acceptance_criteria>
    - `Crosswake.Telemetry.events/0` returns a sorted, de-duplicated list of event_doc maps with keys event/tier/description/measurements/metadata.
    - With `Application.put_env(:crosswake, :companions, [])`, events/0 returns core+reserved and does not raise.
    - With StubTelemetryCompanion registered, its `[:crosswake, :stub_telemetry, :example]` doc appears in events/0.
    - grep confirms NO `@core_events`-style module attribute holding the catalog; NO `Code.ensure_loaded?`; NO `Crosswake.Offline.Telemetry` reference; NO static alias to an extracted companion.
    - The CompanionGuard merge-blocking guard test stays GREEN (no {:__aliases__} ref to a frozen companion).
    - The Phase 133 proof test's Side B + merge + reserved + fail-closed assertions pass.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| configured companion module → events/0 | A host-registered companion's telemetry_events/0 is called at runtime; an absent/incompatible module must not crash the catalog. |
| in-tree *.Telemetry modules → events/0 | Static calls to event_names/0; Offline.Telemetry has no such function and must never be called. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-133-04 | Denial of Service | events/0 calling a missing companion function | high | mitigate | `function_exported?(mod, :telemetry_events, 0)` guards every companion call; missing/absent companions yield `[]`, never a raise (D-10 fail-closed). Proof test's fail-closed case asserts this. |
| T-133-05 | Denial of Service | events/0 calling Offline.Telemetry.event_names/0 (undefined) | high | mitigate | Offline.Telemetry is explicitly NOT in the aggregation chain (RESEARCH Pitfall 2); acceptance grep forbids the reference. |
| T-133-06 | Tampering / coupling | static alias to an extracted companion (re-coupling core) | high | mitigate | Iterate the runtime `:companions` registry via `function_exported?/3` only; CompanionGuard merge-blocking guard test enforces no `{:__aliases__}` ref (EXTRACT-03/04). |
| T-133-07 | Information Disclosure | high-cardinality / PII keys leaking into the published catalog | medium | mitigate | event_doc `metadata` lists are atom KEY NAMES only (no values); keys are the existing low-cardinality PROP-02 allowlist; no PII values traverse events/0. |
</threat_model>

<verification>
- `mix compile --warnings-as-errors` clean.
- `mix test test/crosswake/proof/phase133_telemetry_contract_test.exs` — Side B, merge, reserved-tier, fail-closed GREEN.
- `mix test test/crosswake/proof/phase129_companion_contract_freeze_test.exs` — GREEN (unchanged).
- CompanionGuard guard lane GREEN.
</verification>

<success_criteria>
- events/0 is the canonical runtime-aggregated self-describing catalog (TELEM-01); the declared-surface and reverse-direction contract halves pass (TELEM-04); Companion exposes the optional callback (D-07) without tripping the Phase 129 freeze (TELEM-01 federation seam).
</success_criteria>

<artifacts_this_phase_produces>
This plan produces:
- `Crosswake.Telemetry.events/0` — runtime-aggregated self-describing catalog.
- `Crosswake.Telemetry.event_doc()` typespec (`%{event, tier, description, measurements, metadata}`).
- `@callback telemetry_events/0` + `@optional_callbacks telemetry_events: 0` on `Crosswake.Companion`.
- Private helpers `build_active_events/0` and `build_reserved_events/0` (runtime catalog builders).
NOTE: `spannable_events/0` is intentionally NOT shipped in v1 (D-06 discretion — deferred to the DASH-01 dashboard build per RESEARCH Open Question 2).
</artifacts_this_phase_produces>

<output>
Create `.planning/phases/133-telemetry-public-api/133-02-SUMMARY.md` when done.
</output>
