---
phase: 92-server-propagation-plug-liveview
plan: 03
type: execute
wave: 2
depends_on: [92-01, 92-02]
files_modified:
  - test/crosswake/proof/phase92_server_propagation_closeout_test.exs
  - mix.exs
autonomous: true
requirements: [PROP-01, PROP-03]
must_haves:
  truths:
    - "A hermetic merge-blocking proof test asserts the full PROP-01 Plug contract (inbound vs minted source, Logger.metadata, response echo, :start/:stop/:exception triplet via the Phase 91 guard)"
    - "The same proof test asserts the full PROP-03 on_mount contract (sets metadata when connected with param; no-op on disconnected and absent param)"
    - "The proof test is hermetic — no example host, no network, no simulator"
    - "mix.exs @version is bumped 0.1.1 → 0.1.2 (patch under bump-patch-for-minor-pre-major)"
  artifacts:
    - path: "test/crosswake/proof/phase92_server_propagation_closeout_test.exs"
      provides: "Merge-blocking PROP-01 + PROP-03 closeout proof lane"
      contains: "Phase92ServerPropagationCloseoutTest"
    - path: "mix.exs"
      provides: "Hex version bump"
      contains: "0.1.2"
  key_links:
    - from: "test/crosswake/proof/phase92_server_propagation_closeout_test.exs"
      to: "Crosswake.Plug.Threadline and Crosswake.Live.Threadline"
      via: "contract assertions"
      pattern: "Plug.Threadline|Live.Threadline"
---

<objective>
Close Phase 92 with a hermetic, merge-blocking proof lane that asserts the full PROP-01 (Plug) and PROP-03 (on_mount) contracts end-to-end without any example host, network, or simulator — and bump the Hex `@version` from `0.1.1` to `0.1.2`.

Purpose: The proof lane is the merge-blocking honesty gate (feeds Phase 96 PROOF-01). The version bump ships the three additive public modules as a patch release per the project's `bump-patch-for-minor-pre-major: true` release-please config.
Output: `test/crosswake/proof/phase92_server_propagation_closeout_test.exs` and a one-line `mix.exs` change.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/92-server-propagation-plug-liveview/92-CONTEXT.md
@.planning/phases/92-server-propagation-plug-liveview/92-PATTERNS.md
@.planning/phases/92-server-propagation-plug-liveview/92-01-SUMMARY.md
@.planning/phases/92-server-propagation-plug-liveview/92-02-SUMMARY.md

<interfaces>
<!-- Modules under proof — shipped by plans 92-01 and 92-02. -->

Crosswake.Plug.Threadline — `@behaviour Plug`; `init/1` (NimbleOptions-validated keyword), `call/2`.
  Reads/mints `x-crosswake-thread-id`, sets `Logger.metadata`, echoes response header,
  emits [:crosswake, :threadline, :request, :start|:stop|:exception] via Crosswake.Threadline.Telemetry.execute/3.
Crosswake.Live.Threadline — `on_mount(:default, _params, _session, socket)` → {:cont, socket};
  sets Logger.metadata(crosswake_thread_id: id) only when connected with a valid `_crosswake_thread_id` connect param.
Crosswake.Threadline.Id — `generate/0` → 36-char RFC-4122 v4 UUID.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Hermetic Phase 92 closeout proof lane</name>
  <files>test/crosswake/proof/phase92_server_propagation_closeout_test.exs</files>
  <read_first>
    - test/crosswake/proof/phase39_route_policy_gating_test.exs (proof-test analog: `@moduledoc`, `use ExUnit.Case, async: true`, hermeticity self-assertion `refute Code.ensure_loaded?(CrosswakeExample)`)
    - lib/crosswake/plug/threadline.ex (the contract under proof — from plan 92-01)
    - lib/crosswake/live/threadline.ex (the contract under proof — from plan 92-02)
    - test/crosswake/plug/threadline_test.exs (reuse the Plug.Test + telemetry-handler assertion style)
    - test/crosswake/live/threadline_test.exs (reuse the mock-socket stub helpers)
  </read_first>
  <action>
    Create `Crosswake.Proof.Phase92ServerPropagationCloseoutTest`, `use ExUnit.Case, async: true`, `import Plug.Test`, `import Plug.Conn`. The `@moduledoc` states it proves PROP-01 (Plug: inbound verbatim `source: :inbound`; minted UUID `source: :minted`; `Logger.metadata`; response echo; the three-event telemetry triplet via `Threadline.Telemetry.execute/3`) and PROP-03 (on_mount sets metadata when connected with param; no-op on disconnected and absent param), fully hermetic.

    Include, at minimum, these proof assertions: (1) inbound header → id used verbatim + `:inbound` source in emitted `:start` metadata; (2) absent header → minted v4-format id + `:minted` source; (3) `Logger.metadata()[:crosswake_thread_id]` set after `call/2`; (4) response header echoes the id; (5) `:start` fires on `call/2` and `:stop` fires after `send_resp/3` with a `:duration` measurement; (6) a forbidden/PII metadata key is dropped (route a forbidden key and assert it never reaches the handler); (7) on_mount connected+param sets metadata; (8) on_mount disconnected → `{:cont, socket}` no-op, no metadata; (9) on_mount connected+absent-param → no-op. Attach telemetry handlers with `:telemetry.attach` + `on_exit` detach (unique handler ids via `System.unique_integer()`). Add the hermeticity self-assertion `refute Code.ensure_loaded?(CrosswakeExample)` mirroring phase39. Use `async: true` — no shared state, no `Application.put_env`.
  </action>
  <verify>
    <automated>mix test test/crosswake/proof/phase92_server_propagation_closeout_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - File defines `Crosswake.Proof.Phase92ServerPropagationCloseoutTest` with `use ExUnit.Case, async: true`
    - Contains a hermeticity self-assertion `refute Code.ensure_loaded?(CrosswakeExample)`
    - References both `Crosswake.Plug.Threadline` and `Crosswake.Live.Threadline`
    - Asserts inbound `:inbound` source, minted `:minted` source, `Logger.metadata` set, response echo, `:start` + `:stop` (with `:duration`) emission, and forbidden-key drop for the Plug; metadata-set + two no-op cases for on_mount
    - `mix test test/crosswake/proof/phase92_server_propagation_closeout_test.exs` passes
  </acceptance_criteria>
  <done>The hermetic proof lane is green and asserts the complete PROP-01 + PROP-03 contracts.</done>
</task>

<task type="auto">
  <name>Task 2: Hex version bump 0.1.1 → 0.1.2</name>
  <files>mix.exs</files>
  <read_first>
    - mix.exs (line 4: `@version "0.1.1"` — the single line to change)
    - .planning/phases/92-server-propagation-plug-liveview/92-RESEARCH.md (Pitfall 5: `bump-patch-for-minor-pre-major: true` means `feat:` in 0.x → patch bump, so 0.1.1 → 0.1.2, NOT 0.2.0)
  </read_first>
  <action>
    Edit `mix.exs` line 4, changing `@version "0.1.1"` to `@version "0.1.2"`. This is the ONLY change. Do NOT bump to `0.2.0` — the three new public modules are additive `feat:` work and under the project's `bump-patch-for-minor-pre-major: true` release-please config a `feat:` commit in the 0.x regime lands as a patch (D-14, RESEARCH Pitfall 5). Per RESEARCH Open Question 2, do NOT add retroactive CHANGELOG entries for the unpublished 0.1.1 bump; CHANGELOG edits are out of scope for this task.
  </action>
  <verify>
    <automated>grep -q '@version "0.1.2"' mix.exs && mix compile --warnings-as-errors</automated>
  </verify>
  <acceptance_criteria>
    - `mix.exs` line 4 reads `@version "0.1.2"`
    - No other line in `mix.exs` changed
    - `grep -c '@version "0.1.2"' mix.exs` returns 1
    - `mix compile --warnings-as-errors` succeeds
  </acceptance_criteria>
  <done>`@version` is `0.1.2`; the project compiles clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI proof lane → merge gate | The proof test is the merge-blocking honesty boundary for the shipped Plug/on_mount contracts |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-92P-01 | Repudiation | Contract drift between shipped modules and documented behavior | mitigate | The hermetic proof lane mechanically asserts inbound/minted source, metadata, echo, telemetry triplet, forbidden-key drop, and on_mount behavior — drift fails the merge gate (Task 1). |
| T-92P-02 | Tampering | Proof lane silently depending on a non-hermetic example host | mitigate | `refute Code.ensure_loaded?(CrosswakeExample)` self-assertion guarantees hermeticity (Task 1). |
| T-92P-SC | Tampering | npm/pip/cargo installs | n/a | Phase 92 introduces zero new packages; the version bump is a literal edit, no install. |
</threat_model>

<verification>
- `mix test test/crosswake/proof/phase92_server_propagation_closeout_test.exs` is green
- `grep -c '@version "0.1.2"' mix.exs` returns 1
- `mix test` (full suite) is green
</verification>

<success_criteria>
- A hermetic, merge-blocking proof lane asserts the full PROP-01 and PROP-03 contracts (feeds Phase 96 PROOF-01)
- `mix.exs` ships the three additive modules as version `0.1.2` (D-14)
</success_criteria>

<output>
Create `.planning/phases/92-server-propagation-plug-liveview/92-03-SUMMARY.md` when done
</output>
