---
phase: 95
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/crosswake/support_matrix/support_matrix.ex
  - test/crosswake/support_matrix/support_matrix_test.exs
  - lib/crosswake/doctor/doctor.ex
  - test/crosswake/doctor/doctor_test.exs
autonomous: true
requirements:
  - OPER-02
  - OPER-03
must_haves:
  truths:
    - "Support matrix exposes Threadline audit ledger truth"
    - "mix crosswake.doctor reports threadline findings"
  artifacts:
    - path: "lib/crosswake/support_matrix/support_matrix.ex"
      provides: "Audit ledger support truth definition"
    - path: "lib/crosswake/doctor/doctor.ex"
      provides: "Threadline posture checks"
  key_links:
    - from: "lib/crosswake/doctor/doctor.ex"
      to: "Ecto schema"
      via: "reflection"
      pattern: "__schema__\\(:fields\\)"
---

<objective>
Introduce the Threadline Support Matrix Truth and Doctor Validation Findings.
Purpose: Enforce correct usage and explicitly document Threadline posture support (OPER-02, OPER-03).
Output: SupportMatrix attributes and new Doctor hooks.
</objective>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/95-operator-surface/95-CONTEXT.md
@.planning/phases/95-operator-surface/95-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Support Matrix Truth for Audit Ledger</name>
  <files>
    - lib/crosswake/support_matrix/support_matrix.ex
    - test/crosswake/support_matrix/support_matrix_test.exs
  </files>
  <read_first>
    - lib/crosswake/support_matrix/support_matrix.ex
    - lib/crosswake/threadline/telemetry.ex
  </read_first>
  <action>
    Reference pattern analog: `lib/crosswake/support_matrix/support_matrix.ex (@notification_support_truth)` from PATTERNS.md.
    
    Define a new module attribute `@audit_ledger_support_truth` in `Crosswake.SupportMatrix` containing a single map with the following data:
    - `surface`: `"threadline audit ledger"`
    - `proof_class`: `:advisory`
    - `action_class`: `"operator_surface"`
    - `docs_anchor`: `"guides/threadline.md"`
    - `ephemeral_posture`: `:supported`
    - `durable_posture`: `:supported`
    - `telemetry`: A map setting `status: :shipped`, `event_names: Crosswake.Threadline.Telemetry.event_names()`, `metadata_keys: Crosswake.Threadline.Telemetry.metadata_keys()`, `forbidden_metadata_keys: Crosswake.Threadline.Telemetry.forbidden_metadata_keys()`
    - `deferred`: `[:crosswake_dashboard, :hash_chain_verify_task]`
    - `posture`: `"Threadline provides an honest, PII-free correlation across Native -> Bridge -> Phoenix via X-Crosswake-Thread-Id. It is an append-only sequence reconstruction tool, not an APM or full replay system."`

    Export it via `def audit_ledger_support_truth, do: @audit_ledger_support_truth`.
    In `test/crosswake/support_matrix/support_matrix_test.exs`, add a test verifying `Crosswake.SupportMatrix.audit_ledger_support_truth/0` returns the map and that its telemetry forbidden keys match `Crosswake.Threadline.Telemetry`.
  </action>
  <acceptance_criteria>
    - `lib/crosswake/support_matrix/support_matrix.ex` exports `audit_ledger_support_truth/0`
    - `mix test test/crosswake/support_matrix/support_matrix_test.exs` exits 0
  </acceptance_criteria>
  <verify>
    <automated>mix test test/crosswake/support_matrix/support_matrix_test.exs</automated>
  </verify>
  <done>Changes implemented and tested</done>
</task>

<task type="auto">
  <name>Task 2: Implement Doctor Findings for Threadline</name>
  <files>
    - lib/crosswake/doctor/doctor.ex
    - test/crosswake/doctor/doctor_test.exs
  </files>
  <read_first>
    - lib/crosswake/doctor/doctor.ex
    - lib/crosswake/support_matrix/support_matrix.ex
  </read_first>
  <action>
    Reference pattern analog: `lib/crosswake/doctor/doctor.ex (phase_62_notification_findings)` from PATTERNS.md.

    In `lib/crosswake/doctor/doctor.ex`, modify `run/1` to invoke a new private function `phase_95_threadline_findings(install_manifest, cwd)` and append its results to the `findings` list.
    
    Implement `phase_95_threadline_findings/2`:
    - Read the router file at the `router_path` (resolved from `install_manifest` relative to `cwd`). If the string `"plug Crosswake.Plug.Threadline"` is absent, emit a `threadline.plug_missing` finding (severity `:advisory`).
    - Retrieve `Application.get_env(:crosswake, :audit_ledger)`. If `nil`, emit a `threadline.ledger_not_configured` finding (severity `:advisory`).
    - If `audit_ledger` is configured and `Code.ensure_loaded?(schema)` returns true, reflect on its schema via `schema.__schema__(:fields)`:
      - Intersect the schema fields with `Crosswake.SupportMatrix.audit_ledger_support_truth() |> hd() |> get_in([:telemetry, :forbidden_metadata_keys])`. If any match, emit a `threadline.pii_forbidden_field_present` finding (severity `:error`) specifying the exact offending keys and the Ecto schema/module name (per D-03).
      - Check for the presence of all 15 canonical columns defined in LEDG-02: `[:thread_id, :correlation_id, :route_id, :actor_ref, :actor_kind, :event_class, :event_type, :outcome, :provenance, :occurred_at, :recorded_at, :idempotency_key, :metadata, :row_hash, :prev_hash]`. If any are missing, emit a `threadline.ledger_schema_drift` finding (severity `:warning`).
      
    Add tests in `test/crosswake/doctor/doctor_test.exs` for these four assertions (plug missing, unconfigured, schema drift, PII error).
  </action>
  <acceptance_criteria>
    - `lib/crosswake/doctor/doctor.ex` contains `defp phase_95_threadline_findings`
    - `lib/crosswake/doctor/doctor.ex` emits `threadline.pii_forbidden_field_present` as an `:error` when PII fields are detected
    - `mix test test/crosswake/doctor/doctor_test.exs` exits 0
  </acceptance_criteria>
  <verify>
    <automated>mix test test/crosswake/doctor/doctor_test.exs</automated>
  </verify>
  <done>Changes implemented and tested</done>
</task>

</tasks>