---
phase: 95-operator-surface
plan: "03"
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/crosswake/doctor/doctor.ex
  - test/crosswake/doctor/doctor_test.exs
autonomous: true
gap_closure: true
requirements: [OPER-02]
tags: [doctor, threadline, pii-safety, fail-closed, audit-ledger]

must_haves:
  truths:
    - "mix crosswake.doctor runs the threadline schema checks (PII + drift) when audit_ledger is the documented bare-atom config (audit_ledger: MyApp.Audit.Ledger)"
    - "The canonical 15-column ledger schema produces ZERO threadline.pii_forbidden_field_present findings (no false :error on :actor_ref)"
    - "The canonical 15-column ledger schema produces ZERO threadline.ledger_schema_drift findings"
    - "A schema with a genuine PII field (e.g. :email) still produces threadline.pii_forbidden_field_present :error"
    - "A keyword-list audit_ledger config without a :schema key does not crash the doctor"
  artifacts:
    - path: "lib/crosswake/doctor/doctor.ex"
      provides: "ledger_schema/1 normalization helper + corrected PII intersection"
      contains: "defp ledger_schema"
    - path: "test/crosswake/doctor/doctor_test.exs"
      provides: "regression tests for canonical schema, bare-atom config, CR-04 partial keyword config"
      contains: "CanonicalLedgerSchema"
  key_links:
    - from: "lib/crosswake/doctor/doctor.ex check_pii_fields/3"
      to: "@canonical_ledger_columns"
      via: "MapSet.difference of forbidden_keys minus canonical columns"
      pattern: "MapSet.difference"
    - from: "lib/crosswake/doctor/doctor.ex phase_95_threadline_findings/2"
      to: "ledger_schema/1"
      via: "config normalization for bare atom / keyword list / map"
      pattern: "ledger_schema\\("
---

<objective>
Close the OPER-02 gaps in `mix crosswake.doctor` threadline posture checks. Three interconnected defects make the fail-closed PII check fire falsely on every compliant host (CR-01), silently skip all schema checks under the documented bare-atom config shape (CR-02), and crash on a partial keyword config (CR-04).

Purpose: Restore the honest, actionable, fail-closed PII safety guarantee that OPER-02 promises. A silently-disabled PII check (CR-02) is itself a security defect — the doctor gives a clean bill of health to a host that may be leaking PII.

Output: Corrected `lib/crosswake/doctor/doctor.ex` (shared `ledger_schema/1` normalization helper + canonical-column exclusion in PII intersection) and regression tests in `test/crosswake/doctor/doctor_test.exs`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/95-operator-surface/95-VERIFICATION.md
@.planning/phases/95-operator-surface/95-01-SUMMARY.md

<interfaces>
<!-- Current state of the defective code. Executor uses these directly — no codebase exploration needed beyond read_first. -->

From lib/crosswake/doctor/doctor.ex (phase_95_threadline_findings/2, lines 886-911):
- `audit_ledger_config = Application.get_env(:crosswake, :audit_ledger)`
- schema-check branch ONLY entered when config `is_map` or `is_list`; bare atom falls to `_ -> []` (CR-02)
- inside branch: `schema = config[:schema] || config["schema"]` — string-key access crashes on keyword list without :schema (CR-04)

From lib/crosswake/doctor/doctor.ex @canonical_ledger_columns (lines 952-968) — the 15 LEDG-02 columns:
[:thread_id, :correlation_id, :route_id, :actor_ref, :actor_kind, :event_class, :event_type, :outcome, :provenance, :occurred_at, :recorded_at, :idempotency_key, :metadata, :row_hash, :prev_hash]

From lib/crosswake/doctor/doctor.ex check_pii_fields/3 (lines 989-1008):
- `offending = MapSet.intersection(MapSet.new(schema_fields), MapSet.new(forbidden_keys))` — does NOT exclude canonical columns (CR-01)

From lib/crosswake/threadline/telemetry.ex forbidden_metadata_keys/0:
- Returns 20 atoms INCLUDING `:actor_ref` (line 44) — which is also a required canonical column.
- Other genuine PII keys in the list: :email, :ip, :device_id, :user_agent, :session_ref, etc.

From priv/templates/crosswake/audit/ledger.ex.eex (the generator output that every compliant host runs):
- schema "crosswake_audit_events" declares exactly the 15 canonical columns, INCLUDING `field :actor_ref, :string` (line 17).
- This is the schema that `mix crosswake.gen.audit` scaffolds. Its `__schema__(:fields)` returns the 15 canonical columns and nothing else.

Doctor config shapes that MUST all work (per moduledoc and gen.audit output):
- bare atom:        `audit_ledger: MyApp.Audit.Ledger`   (documented canonical shape)
- keyword list:     `audit_ledger: [schema: MyApp.Audit.Ledger]`  (current test shape)
- map:              `audit_ledger: %{schema: MyApp.Audit.Ledger}`
- keyword w/o :schema: `audit_ledger: [foo: :bar]`  (must NOT crash — CR-04)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add ledger_schema/1 normalization helper and fix config-shape branch (CR-02, CR-04)</name>
  <files>lib/crosswake/doctor/doctor.ex, test/crosswake/doctor/doctor_test.exs</files>
  <read_first>
    - lib/crosswake/doctor/doctor.ex (lines 884-1030 — current phase_95_threadline_findings/2, check_ledger_schema/1, check_pii_fields/3, check_schema_drift/1, @canonical_ledger_columns)
    - test/crosswake/doctor/doctor_test.exs (lines 1-20 for module header/async setting; lines 1280-1372 for existing threadline doctor tests and PiiLedgerSchema/DriftLedgerSchema fixtures)
    - priv/templates/crosswake/audit/ledger.ex.eex (the canonical 15-column schema the doctor must approve)
    - lib/crosswake/threadline/telemetry.ex (lines 41-80 — forbidden_metadata_keys/0 contents)
  </read_first>
  <behavior>
    - Bare-atom config `audit_ledger: SomeSchemaModule` → schema checks RUN against SomeSchemaModule (currently skipped — CR-02).
    - Keyword config `audit_ledger: [schema: SomeSchemaModule]` → schema checks RUN (unchanged).
    - Map config `audit_ledger: %{schema: SomeSchemaModule}` → schema checks RUN (unchanged).
    - Keyword config `audit_ledger: [foo: :bar]` (no :schema key) → returns no schema findings, does NOT raise ArgumentError (CR-04).
    - `audit_ledger: nil` → no schema findings (unchanged).
    - A bare atom that does not resolve to a loaded module → no schema findings, no crash.
  </behavior>
  <action>
    Add a private helper `ledger_schema/1` to `Crosswake.Doctor` that normalizes the four `:audit_ledger` config shapes to a single schema module (or nil). Resolution rules, in this exact order:
    - `nil` → `nil`
    - a bare atom that is NOT a keyword list and NOT a map (i.e. a module) → return that atom as the schema
    - a list where `Keyword.keyword?(list)` is true → `Keyword.get(list, :schema)` (NEVER use string-key access on a keyword list — this is the CR-04 fix; guard with `Keyword.keyword?/1` before any keyword access)
    - a map → `Map.get(config, :schema) || Map.get(config, "schema")`
    - anything else → `nil`
    Note: in Elixir `is_atom(nil)` and `is_atom(true)` are true, so order the `nil` clause and a Keyword/map check before the bare-atom clause, OR use `is_atom(config) and not is_nil(config)` plus distinguish keyword lists (which are lists, not atoms). A bare module atom is detected via `is_atom(config) and not is_boolean(config)` after the nil clause.

    Rewrite the `schema_findings` block in `phase_95_threadline_findings/2` (currently lines 893-908) to call `schema = ledger_schema(audit_ledger_config)` and then `if schema && Code.ensure_loaded?(schema), do: check_ledger_schema(schema), else: []`. Delete the `is_map(config) or is_list(config)` guard branch and the `config[:schema] || config["schema"]` line — they are replaced by `ledger_schema/1`.

    Add the new doctor tests in `test/crosswake/doctor/doctor_test.exs` inside the existing threadline describe block (near lines 1310-1370). Add a module-level fixture `CanonicalLedgerSchema` whose `__schema__(:fields)` returns exactly the 15 canonical columns from `@canonical_ledger_columns` (thread_id, correlation_id, route_id, actor_ref, actor_kind, event_class, event_type, outcome, provenance, occurred_at, recorded_at, idempotency_key, metadata, row_hash, prev_hash). Tests to add:
    - "runs threadline schema checks with bare-atom audit_ledger config" — set `Application.put_env(:crosswake, :audit_ledger, CanonicalLedgerSchema)` (bare atom, the documented shape), run Doctor, assert there is NO `threadline.pii_forbidden_field_present` finding AND NO `threadline.ledger_schema_drift` finding (proves checks ran AND passed under bare-atom config — closes CR-02 + CR-01 jointly).
    - "does not crash on keyword audit_ledger config missing :schema key" — set `Application.put_env(:crosswake, :audit_ledger, foo: :bar)`, run Doctor, assert it returns a report without raising (CR-04 regression).
    Use `on_exit(fn -> Application.delete_env(:crosswake, :audit_ledger) end)` in each new test (matching the existing pattern at line 1326).
  </action>
  <verify>
    <automated>mix test test/crosswake/doctor/doctor_test.exs --only-failures 2>/dev/null || mix test test/crosswake/doctor/doctor_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - Source: `grep -n "defp ledger_schema" lib/crosswake/doctor/doctor.ex` returns a match.
    - Source: `grep -n "Keyword.keyword?" lib/crosswake/doctor/doctor.ex` returns a match (CR-04 guard present).
    - Source: `config\[:schema\] || config\["schema"\]` no longer appears inside phase_95_threadline_findings/2 (string-key keyword access removed).
    - Behavior: bare-atom config `audit_ledger: CanonicalLedgerSchema` causes check_ledger_schema/1 to run (proven by the canonical-schema test producing zero PII and zero drift findings — only possible if the branch executed).
    - Behavior: `audit_ledger: [foo: :bar]` does not raise ArgumentError; Doctor.run returns a report.
    - Test command: the two new tests pass; all previously-passing doctor threadline tests still pass.
  </acceptance_criteria>
  <done>The doctor enters schema checks for the documented bare-atom config; a partial keyword config no longer crashes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Exclude canonical columns from PII intersection (CR-01) + canonical-schema regression</name>
  <files>lib/crosswake/doctor/doctor.ex, test/crosswake/doctor/doctor_test.exs</files>
  <read_first>
    - lib/crosswake/doctor/doctor.ex (lines 970-1008 — check_ledger_schema/1, check_pii_fields/3, @canonical_ledger_columns)
    - lib/crosswake/threadline/telemetry.ex (lines 41-62 — forbidden_metadata_keys contents; confirm :actor_ref is present and is the ONLY canonical column in that list)
    - test/crosswake/doctor/doctor_test.exs (lines 1310-1372 — PiiLedgerSchema fixture and the existing :email PII test)
  </read_first>
  <behavior>
    - The canonical 15-column schema (which includes :actor_ref) produces ZERO threadline.pii_forbidden_field_present findings.
    - A schema containing :actor_ref PLUS a genuine PII field :email still fires :error, and :email IS in offending_keys while :actor_ref is NOT in offending_keys.
    - The existing PiiLedgerSchema test (which appends :email to the 15 canonical columns) still passes and now must assert :actor_ref is NOT reported.
  </behavior>
  <action>
    In `check_pii_fields/3` (lines 989-1008), before computing the intersection, subtract the canonical ledger columns from the forbidden key set: compute `ledger_forbidden = MapSet.difference(MapSet.new(forbidden_keys), MapSet.new(@canonical_ledger_columns))` and intersect `MapSet.new(schema_fields)` with `ledger_forbidden` instead of with `MapSet.new(forbidden_keys)`. This removes `:actor_ref` (the only forbidden key that is also a canonical column) from the PII denylist scope while preserving all genuine PII keys (:email, :ip, :device_id, :user_agent, etc.).

    Strengthen the existing PII test "emits threadline.pii_forbidden_field_present :error when schema has PII forbidden fields" (lines 1322-1340): keep the assertion that `:email in finding.details.offending_keys`, and ADD `refute :actor_ref in finding.details.offending_keys` (proves CR-01 is fixed — the canonical column is no longer falsely flagged even when other PII is present).

    Confirm the Task-1 canonical-schema test ("runs threadline schema checks with bare-atom audit_ledger config") asserts zero `threadline.pii_forbidden_field_present` — this is the primary CR-01 regression and already lands here once the intersection is corrected. If Task 1 and Task 2 are executed in one pass, this is a single test; do not duplicate.
  </action>
  <verify>
    <automated>mix test test/crosswake/doctor/doctor_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - Source: `grep -n "MapSet.difference" lib/crosswake/doctor/doctor.ex` shows a difference against `@canonical_ledger_columns` inside check_pii_fields.
    - Behavior: a schema returning exactly the 15 canonical columns yields no `threadline.pii_forbidden_field_present` finding.
    - Behavior: a schema returning the 15 canonical columns + `:email` yields a `threadline.pii_forbidden_field_present` :error whose `offending_keys` contains `:email` and does NOT contain `:actor_ref`.
    - Test command: `mix test test/crosswake/doctor/doctor_test.exs` — all threadline doctor tests pass; the strengthened PII test includes the `refute :actor_ref` assertion.
  </acceptance_criteria>
  <done>The fail-closed PII check no longer false-positives on the canonical :actor_ref column while still catching genuine PII fields.</done>
</task>

<task type="auto">
  <name>Task 3: Make env-mutating doctor tests race-safe (WARNING)</name>
  <files>test/crosswake/doctor/doctor_test.exs</files>
  <read_first>
    - test/crosswake/doctor/doctor_test.exs (line 2 — `use ExUnit.Case, async: true`; lines 1292-1370 — the threadline describe block using Application.put_env/delete_env)
  </read_first>
  <behavior>
    - The ledger_not_configured test (line 1292-1308) saves and restores any pre-existing :audit_ledger value via on_exit rather than calling delete_env with no restore.
    - Tests that mutate `:audit_ledger` Application env cannot race with other async tests reading the same key.
  </behavior>
  <action>
    Address the async env-mutation race in the threadline describe block. Apply the cheapest correct fix: wrap the `:audit_ledger` mutations in save/restore via `on_exit`. In the "emits threadline.ledger_not_configured" test (line 1292-1308), capture `prev = Application.get_env(:crosswake, :audit_ledger)` before `Application.delete_env`, and register `on_exit(fn -> if prev, do: Application.put_env(:crosswake, :audit_ledger, prev), else: Application.delete_env(:crosswake, :audit_ledger) end)`. Apply the same save/restore pattern to any new env-mutating test added in Tasks 1-2 that does not already restore prior state. If save/restore cannot fully isolate the env-mutating tests (e.g. concurrent reads remain possible), move ONLY the env-mutating threadline describe block into its own module declared with `use ExUnit.Case, async: false` rather than flipping the entire 1300-line file to synchronous. Do not change the async setting of unrelated describe blocks.
  </action>
  <verify>
    <automated>mix test test/crosswake/doctor/doctor_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - Source: the ledger_not_configured test captures prior `:audit_ledger` value and restores it in `on_exit` (no bare `delete_env` without restore).
    - Source: every threadline doctor test that calls `Application.put_env(:crosswake, :audit_ledger, ...)` has a corresponding `on_exit` restore.
    - Test command: `mix test test/crosswake/doctor/doctor_test.exs` passes deterministically across two consecutive runs (`mix test test/crosswake/doctor/doctor_test.exs && mix test test/crosswake/doctor/doctor_test.exs`).
  </acceptance_criteria>
  <done>Env-mutating threadline doctor tests restore prior state and no longer risk cross-test races.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| host audit ledger schema → doctor reflection | The doctor reflects on a host-supplied Ecto schema module to assert PII-free posture. A wrong verdict (false clean) is a security failure. |
| doctor findings → operator CLI output | Finding messages must name fields/modules but must not echo PII values (only field names from schema reflection, never row data). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-95-01 | Information Disclosure | doctor PII check silently disabled (CR-02) | mitigate | `ledger_schema/1` normalization ensures the bare-atom documented config path executes the PII check; regression test proves a misconfigured/PII schema is detected, not silently passed. |
| T-95-02 | Repudiation | false :error PII finding on canonical schema (CR-01) | mitigate | Exclude `@canonical_ledger_columns` from the forbidden set so the fail-closed error is meaningful (operators cannot dismiss it as known noise). |
| T-95-03 | Denial of Service | doctor crash on partial keyword config (CR-04) | mitigate | `Keyword.keyword?/1` guard before keyword access; regression test asserts no ArgumentError. |
| T-95-04 | Information Disclosure | doctor finding messages | accept | Messages reference schema field NAMES and module names only (from `__schema__(:fields)` reflection), never row data or PII values — no change to existing message construction needed. |
</threat_model>

<verification>
- `mix test test/crosswake/doctor/doctor_test.exs` passes, including the new canonical-schema, bare-atom-config, partial-keyword-config, and strengthened-PII tests.
- The two pre-existing unrelated doctor failures noted in 95-01-SUMMARY (crosswake_version "0.1.0" vs "0.1.2", bridge posture format) are out of scope — do not fix them, but do not let your changes introduce new failures beyond those two.
- `grep -n "defp ledger_schema" lib/crosswake/doctor/doctor.ex` returns a match.
- `grep -n "MapSet.difference" lib/crosswake/doctor/doctor.ex` shows canonical-column exclusion in check_pii_fields.
</verification>

<success_criteria>
- VERIFICATION truth "mix crosswake.doctor emits threadline.pii_forbidden_field_present (error, fail-closed) correctly" flips from FAILED to passing.
- Canonical 15-column schema → zero PII findings, zero drift findings.
- Bare-atom config → schema checks run.
- Genuine PII field (:email) → still fires :error.
- Partial keyword config → no crash.
- OPER-02 unblocked.
</success_criteria>

<output>
Create `.planning/phases/95-operator-surface/95-03-SUMMARY.md` when done.
</output>
