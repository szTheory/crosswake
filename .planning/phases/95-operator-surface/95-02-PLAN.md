---
phase: 95
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/mix/tasks/crosswake.threadline.ex
  - test/mix/tasks/crosswake.threadline_test.exs
autonomous: true
requirements:
  - OPER-01
must_haves:
  truths:
    - "mix crosswake.threadline prints a Native->Bridge->Phoenix timeline with posture"
  artifacts:
    - path: "lib/mix/tasks/crosswake.threadline.ex"
      provides: "CLI visualization of the timeline"
  key_links:
    - from: "lib/mix/tasks/crosswake.threadline.ex"
      to: "Ecto Repo"
      via: "query"
      pattern: "Application.get_env\\(:crosswake, :audit_repo\\)"
---

<objective>
Implement the `mix crosswake.threadline` CLI Task.
Purpose: Give operators a text-tree based Native -> Bridge -> Phoenix chronological timeline visualization (OPER-01).
Output: Standalone Mix Task and tests.
</objective>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/95-operator-surface/95-CONTEXT.md
@.planning/phases/95-operator-surface/95-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build Threadline CLI Task</name>
  <files>
    - lib/mix/tasks/crosswake.threadline.ex
  </files>
  <read_first>
    - lib/crosswake/threadline/telemetry.ex
  </read_first>
  <action>
    Create `Mix.Tasks.Crosswake.Threadline` utilizing `use Mix.Task`.
    - Define `@shortdoc "Inspects Threadline Native->Bridge->Phoenix events via text tree"`.
    - Parse arguments using `OptionParser.parse(args, strict: [thread_id: :string, actor_ref: :string])`. Ensure either `--thread-id` or `--actor-ref` is specified; if not, invoke `Mix.raise/1` with a usage error message.
    - Check the environment for `:audit_repo` and `:audit_ledger` under the `:crosswake` application space.
    - If either is `nil`, output exactly `"Posture: Ephemeral. No ledger configured."` utilizing `Mix.shell().info/1` and exit `0`. This reflects a valid posture (D-02).
    - If configured (durable posture), execute `Mix.Task.run("app.start")` and dynamically query the ledger for events matching the provided ID.
    - Print `"Posture: Durable"` followed by events formatted chronologically as a Unicode text tree (e.g., using `├── ` and `└── ` connectors).
  </action>
  <acceptance_criteria>
    - `lib/mix/tasks/crosswake.threadline.ex` defines `Mix.Tasks.Crosswake.Threadline`
    - Command `mix crosswake.threadline` prints `"Posture: Ephemeral. No ledger configured."` (when no config is present) and gracefully returns 0
    - Contains strict OptionParser declaration for `--thread-id` and `--actor-ref`
  </acceptance_criteria>
  <verify>mix test</verify>
  <done>Changes implemented and tested</done>
</task>

<task type="auto">
  <name>Task 2: Test Threadline CLI Task</name>
  <files>
    - test/mix/tasks/crosswake.threadline_test.exs
  </files>
  <read_first>
    - lib/mix/tasks/crosswake.threadline.ex
  </read_first>
  <action>
    Create `Mix.Tasks.Crosswake.ThreadlineTest`.
    - Utilize `ExUnit.CaptureIO` to intercept console output.
    - Assert that failing to provide valid arguments raises a `Mix.Error`.
    - Assert that under default (missing) configuration, the task generates the `"Posture: Ephemeral. No ledger configured."` informational output.
    - Implement a test for the durable posture path that mocks the repository/schema and asserts the existence of tree characters (`├──`, `└──`) in the rendered output.
  </action>
  <acceptance_criteria>
    - `mix test test/mix/tasks/crosswake.threadline_test.exs` exits 0
    - `ExUnit.CaptureIO` successfully intercepts the ephemeral posture message in at least one test case
  </acceptance_criteria>
  <verify>mix test</verify>
  <done>Changes implemented and tested</done>
</task>

</tasks>
