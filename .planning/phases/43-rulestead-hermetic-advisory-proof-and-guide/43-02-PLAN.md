---
phase: 43-rulestead-hermetic-advisory-proof-and-guide
plan: 02
type: execute
wave: 2
depends_on: [43-01]
files_modified:
  - guides/companions.md
  - test/crosswake/guides/companions_test.exs
  - mix.exs
autonomous: true
requirements: [PROOF-02]

must_haves:
  truths:
    - "guides/companions.md exists with a short companion-pattern intro plus a complete rulestead section covering gated_by DSL, gate-state semantics, kill-switch behavior, and MockFlagSource as the mock swap target"
    - "The rulestead section uses exact live-code vocabulary: gated_by, on_unavailable, kill_switch, MockFlagSource, fail-closed"
    - "A docs-contract ExUnit test asserts each anchor string is present in the guide and that referenced live-code symbols actually export"
    - "guides/companions.md is registered in mix.exs docs extras so ExDoc publishes it"
  artifacts:
    - path: "guides/companions.md"
      provides: "Companion-pattern intro + rulestead section"
      contains: "gated_by"
    - path: "test/crosswake/guides/companions_test.exs"
      provides: "Docs-contract test locking guide anchors to live code"
      contains: "function_exported?"
    - path: "mix.exs"
      provides: "guides/companions.md added to docs extras list"
      contains: "guides/companions.md"
  key_links:
    - from: "test/crosswake/guides/companions_test.exs"
      to: "guides/companions.md"
      via: "File.read! in setup_all + assert content =~ anchors"
      pattern: "companions.md"
    - from: "test/crosswake/guides/companions_test.exs"
      to: "Crosswake.Companions.Rulestead / MockFlagSource / SupportMatrix"
      via: "function_exported?/3 live-code guards"
      pattern: "function_exported\\?"
---

<objective>
Ship the first section of `guides/companions.md` — a short companion-pattern intro plus a complete rulestead section — locked to live code by a docs-contract test, and register the guide in ExDoc extras.

Purpose: PROOF-02 requires `guides/companions.md` to document the companion-seam pattern and the rulestead surfaces, locked to doctor/support-matrix truth by a docs-contract test. This is the first slice of the companion arc guide; Phase 47 expands it.

Output: `guides/companions.md` (new), `test/crosswake/guides/companions_test.exs` (new docs-contract test), and a one-line `mix.exs` docs extras addition.

Wave note: this plan runs in Wave 2 (after 43-01) because both plans edit `mix.exs` — 43-01 edits `deps/0`, this plan edits `docs/0` extras. Sequencing avoids a same-file conflict. The two edits are in different functions; apply this plan's change to the `docs/0` extras list only and leave 43-01's `deps/0` change untouched.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/43-rulestead-hermetic-advisory-proof-and-guide/43-CONTEXT.md
@.planning/phases/43-rulestead-hermetic-advisory-proof-and-guide/43-RESEARCH.md

<interfaces>
<!-- Live-code symbols the guide references and the docs-contract test must guard. Verified to exist. -->

Crosswake.Companions.Rulestead.validate_dependency/0   — lib/crosswake/companions/rulestead.ex
Crosswake.Companions.Rulestead.MockFlagSource.set_flag/2 — lib/crosswake/companions/rulestead/mock_flag_source.ex
  # set_flag(flag_key :: atom, gate_state) where gate_state is :gated | {:rolling_out, n} | :killed
Crosswake.Companions.Rulestead.MockFlagSource.get_flag/1 — same module
Crosswake.SupportMatrix.gating_truth/0                  — lib/crosswake/support_matrix/support_matrix.ex

Gate-state semantics (from phase42 proof + 40-CONTEXT, exact vocabulary):
  :gated            → deny with denial.reason :gate_denied
  {:rolling_out, n} → deny with denial.reason :gate_denied
  :killed           → deny with denial.reason :kill_switch_active (short-circuits route_gated?/2)
  on_unavailable: :deny → transition :halt (fail-closed default)

From test/crosswake/guides/commerce_test.exs — the docs-contract PATTERN to mirror:
  use ExUnit.Case, async: false
  @guide_path Path.join([File.cwd!(), "guides", "<name>.md"])
  setup_all do: content = File.read!(@guide_path); %{content: content}
  test "...", %{content: content} do: assert content =~ "anchor"
  # plus a test calling function_exported?(Module, :fun, arity) for live-code liveness
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write guides/companions.md (intro + rulestead section) and register in ExDoc extras</name>
  <files>guides/companions.md, mix.exs</files>
  <read_first>
    - guides/commerce.md (reference for guide voice, depth, and quality level the companion guide must match)
    - lib/crosswake/companions/rulestead/mock_flag_source.ex (MockFlagSource API: set_flag/2 with :gated | {:rolling_out, n} | :killed; get_flag/1)
    - lib/crosswake/companions/rulestead.ex (route_gated?/2, kill_switch_active?/1, validate_dependency/0 behavior)
    - .planning/phases/43-rulestead-hermetic-advisory-proof-and-guide/43-CONTEXT.md (D-04 guide structure, D-05 no rindle/sigra placeholders, D-06 exact DSL vocabulary, specifics: include a MockFlagSource.set_flag snippet)
    - mix.exs (docs/0 extras list — guides/companions.md must be added; note 43-01 modified deps/0 — leave that change intact)
  </read_first>
  <action>
    Create `guides/companions.md` with this structure (D-04):

    1. A short companion-pattern intro (2-4 sentences) that is universal (NOT rulestead-specific, so Phase 47 expansion wastes nothing): companions gate routes via the `Crosswake.Companion` behaviour; they wrap an optional dependency and fail closed (via `validate_dependency/0`) when that dependency is absent; each companion carries a hermetic+advisory proof posture (hermetic merge-blocking lane passes with the optional dep absent, advisory lane exercises it present). Use the literal phrase `fail-closed` in this intro.

    2. A complete rulestead section covering, with EXACT live-code vocabulary (D-06 — these are docs-contract anchors, must appear verbatim):
       - `gated_by` DSL declaration: a route declares `gated_by: :my_flag` with `on_unavailable: :deny` (the fail-closed default). Show a short route-policy snippet using `gated_by` and `on_unavailable: :deny`.
       - Gate-state semantics: `:gated` → deny (`:gate_denied`); `{:rolling_out, n}` → deny (`:gate_denied`) at the rollout percentage; `:killed` → kill-switch short-circuit producing `kill_switch` denial (`:kill_switch_active`), evaluated ahead of all other gate checks. Use the literal token `kill_switch` (not "kill switch" prose) so the anchor matches.
       - Kill-switch behavior and the fail-closed guarantee: when the flag snapshot is unavailable the posture is deny; the only carve-out is an explicit `on_unavailable: :fallback_phoenix` to a fully-owned route.
       - `MockFlagSource` as the dev/test mock swap target: include a short code snippet showing how to drive flag state in a test, e.g. `Crosswake.Companions.Rulestead.MockFlagSource.set_flag(:my_flag, :killed)` (per CONTEXT.md specifics). Note that the real flag-reading adapter (`Rulestead.Snapshot`) is the production swap target, deferred to a future phase.

    Do NOT add any rindle or sigra headings or placeholder sections (D-05) — scope is strictly the intro + rulestead. Match the voice and structural quality of `guides/commerce.md`.

    Then add `"guides/companions.md"` to the `extras:` list in `mix.exs` `docs/0` (alongside the other `guides/*.md` entries). The existing `groups_for_extras: [Guides: ~r/guides\//]` catch-all groups it automatically — no other docs/0 change needed. Do NOT touch `deps/0` (43-01 owns that change).
  </action>
  <verify>
    <automated>test -f guides/companions.md && grep -q "guides/companions.md" mix.exs && for a in gated_by on_unavailable kill_switch MockFlagSource fail-closed; do grep -q "$a" guides/companions.md || { echo "MISSING anchor: $a"; exit 1; }; done && echo GUIDE_OK</automated>
  </verify>
  <acceptance_criteria>
    - guides/companions.md exists
    - Contains all five anchors verbatim: `gated_by`, `on_unavailable`, `kill_switch`, `MockFlagSource`, `fail-closed`
    - Contains a `set_flag` snippet referencing `Crosswake.Companions.Rulestead.MockFlagSource`
    - Does NOT contain `rindle` or `sigra` (case-insensitive `grep -ci "rindle\|sigra" guides/companions.md` returns 0)
    - mix.exs docs/0 extras list contains `"guides/companions.md"`
    - mix.exs deps/0 still contains the MIX_INCLUDE_RULESTEAD conditional from 43-01 (this task did not revert it)
    - Guide includes a `gated_by:` route-policy snippet and the three gate-state outcomes (:gated, :rolling_out, :killed)
  </acceptance_criteria>
  <done>guides/companions.md exists with intro + rulestead section using exact anchors, no rindle/sigra content, and is registered in ExDoc extras without disturbing the deps/0 change.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create the docs-contract test (anchor presence + live-code guards)</name>
  <files>test/crosswake/guides/companions_test.exs</files>
  <read_first>
    - test/crosswake/guides/commerce_test.exs (PRIMARY pattern: File.read! @guide_path, setup_all returning content, assert content =~ anchor, function_exported?/3 live guard)
    - guides/companions.md (the guide written in Task 1 — anchors must match what was written)
    - .planning/phases/43-rulestead-hermetic-advisory-proof-and-guide/43-CONTEXT.md (D-07 test file/pattern, D-08 minimum anchor set + at least one function_exported? check)
    - .planning/phases/43-rulestead-hermetic-advisory-proof-and-guide/43-RESEARCH.md (Pattern 4: full docs-contract test example with the three function_exported? guards)
  </read_first>
  <behavior>
    - Test 1: guide content contains `gated_by`.
    - Test 2: guide content contains `kill_switch`.
    - Test 3: guide content contains `MockFlagSource`.
    - Test 4: guide content contains `on_unavailable`.
    - Test 5: guide content contains `fail-closed`.
    - Test 6: live-code guard — `function_exported?(Crosswake.Companions.Rulestead, :validate_dependency, 0)`, `function_exported?(Crosswake.Companions.Rulestead.MockFlagSource, :set_flag, 2)`, and `function_exported?(Crosswake.SupportMatrix, :gating_truth, 0)` all return true.
  </behavior>
  <action>
    Create `test/crosswake/guides/companions_test.exs` defining module `Crosswake.Guides.CompanionsTest` with `use ExUnit.Case, async: false` (D-07). Set `@guide_path Path.join([File.cwd!(), "guides", "companions.md"])`. `setup_all` reads the file via `File.read!(@guide_path)` and returns `%{content: content}`.

    Add one test per anchor asserting `content =~ "<anchor>"` for the D-08 minimum set: `"gated_by"`, `"kill_switch"`, `"MockFlagSource"`, `"on_unavailable"`, `"fail-closed"`. Each failing assertion should carry a message noting the guide anchor is missing/stale.

    Add one live-code guard test (no `content` needed) asserting all three `function_exported?/3` checks return true, each with a message stating the symbol is not exported so the guide anchor is stale:
      - `Crosswake.Companions.Rulestead.validate_dependency/0`
      - `Crosswake.Companions.Rulestead.MockFlagSource.set_flag/2`
      - `Crosswake.SupportMatrix.gating_truth/0`

    Do NOT use Code.require_file for any example-host module — this test is fully hermetic (reads a static file + checks library exports), so it runs in the standard `mix test --exclude requires_example_host` lane with no tag.
  </action>
  <verify>
    <automated>mix test test/crosswake/guides/companions_test.exs 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - File test/crosswake/guides/companions_test.exs exists
    - Contains `defmodule Crosswake.Guides.CompanionsTest`
    - Contains `use ExUnit.Case, async: false`
    - Contains `File.read!` of the companions.md guide path
    - Asserts presence of all five anchors: `gated_by`, `kill_switch`, `MockFlagSource`, `on_unavailable`, `fail-closed`
    - Contains `function_exported?(Crosswake.Companions.Rulestead, :validate_dependency, 0)`, `function_exported?(Crosswake.Companions.Rulestead.MockFlagSource, :set_flag, 2)`, and `function_exported?(Crosswake.SupportMatrix, :gating_truth, 0)`
    - `mix test test/crosswake/guides/companions_test.exs` exits 0
  </acceptance_criteria>
  <done>Docs-contract test exists, mirrors commerce_test.exs, asserts all five anchors plus three live-code guards, and passes against the guide from Task 1.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Guide prose → live code | Documentation can drift from the shipped DSL/symbols; docs-contract test is the only thing binding them |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-43-04 | Repudiation/Tampering | Guide anchors drifting from live code (stale docs presenting false contract) | mitigate | Docs-contract test asserts exact anchor strings present AND three `function_exported?/3` live-code guards; any renamed/removed symbol fails the test (research Pitfall 5) |
| T-43-05 | Information disclosure | Guide implying unshipped surface area (rindle/sigra placeholders) | mitigate | D-05 prohibits placeholders; Task 1 acceptance criterion asserts `grep -ci "rindle\|sigra" == 0` |
| T-43-SC | Tampering | npm/pip/cargo installs | n/a | No package installs in this plan; pure docs + ExUnit test using built-in `File.read!` and `function_exported?/3` |
</threat_model>

<verification>
- Docs-contract green: `mix test test/crosswake/guides/companions_test.exs` exits 0.
- Anchor presence: all five anchors (`gated_by`, `on_unavailable`, `kill_switch`, `MockFlagSource`, `fail-closed`) present verbatim in the guide.
- No scope creep: guide contains no rindle/sigra content.
- ExDoc registration: `grep -q "guides/companions.md" mix.exs` succeeds.
</verification>

<success_criteria>
- PROOF-02: `guides/companions.md` exists with a companion-pattern intro and a complete rulestead section (gated_by DSL, gate-state semantics, kill-switch behavior, MockFlagSource swap target); a docs-contract ExUnit test asserts the section's anchor strings are present and that the live-code symbols they reference actually export.
</success_criteria>

<output>
Create `.planning/phases/43-rulestead-hermetic-advisory-proof-and-guide/43-02-SUMMARY.md` when done
</output>
