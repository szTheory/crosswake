---
phase: 08-selective-native-flow-exemplar
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - examples/phoenix_host/mix.exs
  - examples/phoenix_host/config/config.exs
  - examples/phoenix_host/lib/crosswake_example/application.ex
  - examples/phoenix_host/lib/crosswake_example/repo.ex
  - examples/phoenix_host/lib/crosswake_example/router.ex
  - examples/phoenix_host/lib/crosswake_example/selective_native/on_mount.ex
  - examples/phoenix_host/lib/crosswake_example/selective_native/fixtures.ex
  - examples/phoenix_host/lib/crosswake_example/selective_native/claim.ex
  - examples/phoenix_host/lib/crosswake_example/selective_native/submission.ex
  - examples/phoenix_host/lib/crosswake_example/selective_native/claims.ex
  - examples/phoenix_host/lib/crosswake_example/selective_native/submissions.ex
  - examples/phoenix_host/priv/repo/migrations/*_create_selective_native_claims_and_submissions.exs
  - test/crosswake/proof/phase8_selective_native_lane_test.exs
autonomous: true
requirements:
  - NATIVE-01
must_haves:
  truths:
    - "The shared example host exposes one `/native` claims-evidence lane with exactly four public routes and exactly one `:native_screen` route."
    - "Phoenix remains authoritative for queue, detail, and review surfaces, while the native route owns only the capture step."
    - "The locked D-26 persistence requirement is satisfied through an example-host-only Ecto and SQLite slice rather than optional fixtures or Crosswake-core persistence."
  artifacts:
    - path: "examples/phoenix_host/lib/crosswake_example/router.ex"
      provides: "Shared-host `/native` route declarations, route ids, runtime ownership, and security defaults"
    - path: "examples/phoenix_host/lib/crosswake_example/selective_native/on_mount.ex"
      provides: "Lane-local LiveView mount boundary for the selective-native corridor"
    - path: "examples/phoenix_host/lib/crosswake_example/selective_native/claims.ex"
      provides: "Phoenix-owned claim queue and detail access boundary"
    - path: "examples/phoenix_host/lib/crosswake_example/selective_native/submissions.ex"
      provides: "Phoenix-owned review-state boundary for staged capture handoff"
    - path: "examples/phoenix_host/lib/crosswake_example/repo.ex"
      provides: "Example-host-only Ecto repo boundary for the selective-native lane"
    - path: "test/crosswake/proof/phase8_selective_native_lane_test.exs"
      provides: "Executable route-map proof for the four-route lane and single-native-route contract"
  key_links:
    - from: "examples/phoenix_host/lib/crosswake_example/router.ex"
      to: ".planning/phases/08-selective-native-flow-exemplar/08-CONTEXT.md"
      via: "implemented route map matches the locked `/native` claims-evidence lane"
      pattern: "/native/claims|/native/claims/:id|/native/claims/:id/capture|/native/submissions/:id/review"
    - from: "examples/phoenix_host/lib/crosswake_example/repo.ex"
      to: ".planning/phases/08-selective-native-flow-exemplar/08-RESEARCH.md"
      via: "example-host-only Ecto decision is implemented narrowly instead of left optional"
      pattern: "example-host-only Ecto-backed claim and submission slice"
---

<objective>
Establish the selective-native lane skeleton and Phoenix-owned corridor inside the shared example host.

Purpose: Fulfill the structural part of NATIVE-01 by giving Phase 8 a falsifiable `/native` route map, lane-local modules, and a narrow example-host-only Ecto state boundary without changing Crosswake core ownership.
Output: A `CrosswakeExample.SelectiveNative.*` lane with the locked four routes, one `live_session :selective_native`, and proof that the lane shape and persistence boundary cannot silently drift.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-selective-native-flow-exemplar/08-CONTEXT.md
@.planning/phases/08-selective-native-flow-exemplar/08-RESEARCH.md
@.planning/phases/08-selective-native-flow-exemplar/08-PATTERNS.md
@.planning/phases/07-phoenix-saas-portal-exemplar/07-PATTERNS.md
@guides/adopter_profiles.md
@examples/phoenix_host/README.md
@examples/phoenix_host/lib/crosswake_example/router.ex
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add the `/native` lane and lock its route map in proof</name>
  <files>examples/phoenix_host/lib/crosswake_example/router.ex, test/crosswake/proof/phase8_selective_native_lane_test.exs</files>
  <behavior>
    - Test 1: The shared example host exposes exactly `/native/claims`, `/native/claims/:id`, `/native/claims/:id/capture`, and `/native/submissions/:id/review`.
    - Test 2: Exactly one Phase 8 route compiles as `:native_screen`, and it is `/native/claims/:id/capture`.
    - Test 3: The surrounding Phase 8 routes remain `:live_view`, use one lane-local `live_session`, and do not drift into a flat public `/camera` or `/native/capture` story.
  </behavior>
  <action>Extend `examples/phoenix_host/lib/crosswake_example/router.ex` with one dedicated `/native` scope under `CrosswakeExample.SelectiveNative` that mirrors the SaaS lane structure: one shared `crosswake_defaults` block, one `live_session :selective_native`, and route-local overrides only where the contract changes. Keep the sensitive corridor narrow by leaving `/native/claims` and `/native/claims/:id` at `security: :standard`, then overriding the capture and review routes to `security: :sensitive`. Add proof that compiles the example-host route truth and fails if the route count, path shape, runtime ownership, or sensitive-corridor posture drifts.</action>
  <verify>
    <automated>mix test test/crosswake/proof/phase8_selective_native_lane_test.exs</automated>
  </verify>
  <done>The repo has a falsifiable Phase 8 route map in the shared example host, and the public selective-native lane can no longer silently drift back into a generic capture demo.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add example-host-only Ecto-backed claim and submission scaffolding</name>
  <files>examples/phoenix_host/mix.exs, examples/phoenix_host/config/config.exs, examples/phoenix_host/lib/crosswake_example/application.ex, examples/phoenix_host/lib/crosswake_example/repo.ex, examples/phoenix_host/lib/crosswake_example/selective_native/on_mount.ex, examples/phoenix_host/lib/crosswake_example/selective_native/fixtures.ex, examples/phoenix_host/lib/crosswake_example/selective_native/claim.ex, examples/phoenix_host/lib/crosswake_example/selective_native/submission.ex, examples/phoenix_host/lib/crosswake_example/selective_native/claims.ex, examples/phoenix_host/lib/crosswake_example/selective_native/submissions.ex, examples/phoenix_host/priv/repo/migrations/*_create_selective_native_claims_and_submissions.exs, test/crosswake/proof/phase8_selective_native_lane_test.exs</files>
  <behavior>
    - Test 1: The example host has a narrow Ecto-backed claim and submission slice that stays product-shaped without adding vendor semantics.
    - Test 2: Repo, schema, migration, and application wiring stay inside `examples/phoenix_host` and do not leak into Crosswake library code.
    - Test 3: The data boundary keeps `captured locally`, `staged`, `uploaded`, and `submitted` as distinct persisted states instead of collapsing them into one success label.
  </behavior>
  <action>Add the minimal example-host-only Ecto slice required by locked decision D-26: declare the example-host dependencies, configure a local SQLite-backed repo, wire an example-host `Application` and `Repo`, add narrow claim and submission schemas plus a small migration, and keep the lane-local services and fixtures bounded to generic claims-evidence data only. Use that persisted state boundary to preserve explicit staged-versus-uploaded vocabulary for the later capture and review flow. Add proof assertions or targeted structure checks that fail if persistence leaks into Crosswake core, if the lane grows beyond the bounded four-route corridor, or if the state model stops distinguishing local capture from upload and submission.</action>
  <verify>
    <automated>mix test test/crosswake/proof/phase8_selective_native_lane_test.exs</automated>
    <automated>rg -n 'Ecto|Repo|schema|captured locally|staged|uploaded|submitted' examples/phoenix_host/lib/crosswake_example examples/phoenix_host/config</automated>
  </verify>
  <done>The shared example host has the narrow Ecto-backed claim and submission slice that Phase 8 requires, and the persistence boundary stays outside Crosswake core.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| shared host lane -> public selective-native contract | If the route map drifts or a second native route appears, the exemplar stops proving the locked profile |
| example-host state -> Crosswake core | If claim/review persistence leaks into library code, the phase widens core scope instead of proving an exemplar |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01 | T | shared router lane | mitigate | Lock the exact four-route map, single-native-route contract, and sensitive corridor in proof tests |
| T-08-02 | E | route ownership boundary | mitigate | Keep the queue, detail, and review surfaces Phoenix-owned and make the native route explicit in both router metadata and proof |
| T-08-03 | T | example-host persistence boundary | mitigate | Keep repo, schema, migration, and state additions inside `examples/phoenix_host` and reject core-library scope growth |
</threat_model>

<verification>
Run `mix test test/crosswake/proof/phase8_selective_native_lane_test.exs` and confirm the locked route map, native-route count, and example-host-only Ecto state vocabulary all hold.
</verification>

<success_criteria>
- The shared example host contains the locked four-route `/native` claims-evidence lane.
- Exactly one Phase 8 route is `:native_screen`, and the surrounding routes remain Phoenix-owned.
- The host-side Ecto-backed claim/submission boundary is bounded to the example host and keeps staged, uploaded, and submitted states distinct.
</success_criteria>

<output>
After completion, create `.planning/phases/08-selective-native-flow-exemplar/08-selective-native-flow-exemplar-01-SUMMARY.md`
</output>
