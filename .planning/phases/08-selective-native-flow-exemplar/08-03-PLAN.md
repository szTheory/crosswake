---
phase: 08-selective-native-flow-exemplar
plan: 03
type: execute
wave: 3
depends_on:
  - 08-01
  - 08-02
files_modified:
  - examples/ios_shell_host/Fixtures/route_activation.json
  - examples/ios_shell_host/Fixtures/crosswake_manifest.json
  - examples/ios_shell_host/CrosswakeShellTests/ActivationCoordinatorTests.swift
  - examples/android_shell_host/app/src/main/assets/route_activation.json
  - examples/android_shell_host/app/src/main/assets/crosswake_manifest.json
  - examples/android_shell_host/app/src/androidTest/java/dev/crosswake/shell/LiveViewBootInstrumentedTest.kt
  - script/verify_phase5_example_hosts.sh
  - test/crosswake/proof/phase5_proof_lane_test.exs
  - test/crosswake/proof/phase8_selective_native_lane_test.exs
autonomous: true
requirements:
  - NATIVE-01
  - NATIVE-02
must_haves:
  truths:
    - "The checked-in Phoenix, iOS, and Android example hosts remain the public proof artifact class for the selective-native lane."
    - "Proof must cover both pack-ready and pack-blocked capture activation paths."
    - "The proof posture rejects second-native-route drift, whole-lane pack gating, and capture-equals-upload wording."
  artifacts:
    - path: "script/verify_phase5_example_hosts.sh"
      provides: "Base checked-in proof entrypoint extended to run selective-native assertions"
    - path: "test/crosswake/proof/phase8_selective_native_lane_test.exs"
      provides: "Lane-specific manifest, route-map, and staged-state assertions"
    - path: "examples/ios_shell_host/Fixtures/route_activation.json"
      provides: "Checked-in iOS shell activation fixture aligned to the selective-native lane"
    - path: "examples/android_shell_host/app/src/main/assets/route_activation.json"
      provides: "Checked-in Android shell activation fixture aligned to the selective-native lane"
  key_links:
    - from: "script/verify_phase5_example_hosts.sh"
      to: "test/crosswake/proof/phase8_selective_native_lane_test.exs"
      via: "checked-in proof posture runs Phase 8 lane assertions as part of the shared host artifact class"
      pattern: "phase8_selective_native_lane_test"
    - from: "examples/ios_shell_host/Fixtures/route_activation.json"
      to: "examples/android_shell_host/app/src/main/assets/route_activation.json"
      via: "paired shell fixtures exercise the same pack-ready and pack-blocked capture truth"
      pattern: "pack_incompatible|native-claim-capture"
---

<objective>
Extend the checked-in proof posture and shell fixtures so the selective-native lane becomes a public supported artifact instead of a Phoenix-only code path.

Purpose: Fulfill the proof-backed parts of NATIVE-01 and NATIVE-02 by making the paired shell fixtures, scripts, and ExUnit suite agree on the locked Phase 8 lane.
Output: Updated shell activation fixtures, manifest assertions, proof scripts, and lane tests that cover pack-ready and pack-blocked capture behavior.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/08-selective-native-flow-exemplar/08-CONTEXT.md
@.planning/phases/08-selective-native-flow-exemplar/08-RESEARCH.md
@.planning/phases/08-selective-native-flow-exemplar/08-PATTERNS.md
@.planning/phases/08-selective-native-flow-exemplar/08-01-PLAN.md
@.planning/phases/08-selective-native-flow-exemplar/08-02-PLAN.md
@examples/phoenix_host/README.md
@script/verify_phase5_example_hosts.sh
@test/crosswake/proof/phase5_proof_lane_test.exs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add lane-specific proof for manifest truth and staged-state honesty</name>
  <files>test/crosswake/proof/phase8_selective_native_lane_test.exs, test/crosswake/proof/phase5_proof_lane_test.exs</files>
  <behavior>
    - Test 1: The compiled manifest exposes exactly one Phase 8 `:native_screen` route and the locked four-route `/native` map.
    - Test 2: Proof fails if pack gating spreads beyond the capture route or if the review route loses its explicit review-before-upload posture.
    - Test 3: Proof fails if the capture route loses its explicit capability, pack, or transfer declarations.
  </behavior>
  <action>Introduce a dedicated `phase8_selective_native_lane_test.exs` and any supporting proof updates needed to make the lane falsifiable before the doc refresh. Reuse the existing proof style from Phase 5 and Phase 7: compile manifest truth from the shared example host, assert the `/native` route map, assert the one-native-route contract, assert route-local capability, pack, and transfer declarations, and assert the persisted staged-versus-uploaded state split that Phase 8 code must maintain. Keep README and guide contract enforcement out of this slice so proof order stays behind the later doc refresh.</action>
  <verify>
    <automated>mix test test/crosswake/proof/phase5_proof_lane_test.exs test/crosswake/proof/phase8_selective_native_lane_test.exs</automated>
  </verify>
  <done>The repo has lane-specific proof that turns the selective-native route story into executable contract instead of aspirational docs.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Align checked-in iOS and Android shell fixtures to the Phase 8 lane</name>
  <files>examples/ios_shell_host/Fixtures/route_activation.json, examples/ios_shell_host/Fixtures/crosswake_manifest.json, examples/ios_shell_host/CrosswakeShellTests/ActivationCoordinatorTests.swift, examples/android_shell_host/app/src/main/assets/route_activation.json, examples/android_shell_host/app/src/main/assets/crosswake_manifest.json, examples/android_shell_host/app/src/androidTest/java/dev/crosswake/shell/LiveViewBootInstrumentedTest.kt, script/verify_phase5_example_hosts.sh, test/crosswake/proof/phase8_selective_native_lane_test.exs</files>
  <behavior>
    - Test 1: The checked-in iOS and Android hosts exercise the selective-native capture route as part of the shared example-host artifact class.
    - Test 2: Both hosts expose pack-ready and pack-blocked activation truth for the capture route.
    - Test 3: Proof still runs from the existing base entrypoint instead of a Phase 8-only harness.
    - Test 4: The slice stays executable in the current repo by keeping a repo-local minimum verification path separate from full provisioned native-host verification.
  </behavior>
  <action>Update the checked-in shell activation and manifest fixtures so both platforms know about the new `/native` lane and can explicitly exercise the capture route. Add or refresh shell-side tests so the capture route opens only when the declared capability and pack are compatible and otherwise denies with `pack_incompatible`. Extend `script/verify_phase5_example_hosts.sh` to include the Phase 8 lane assertions rather than forking a new proof path, and keep the route fixtures and shell tests aligned to the same route ids and denial vocabulary on both platforms. Document inside the plan and resulting summary that full shell verification requires provisioned native host tooling, while repo-local fixture and ExUnit verification remains the minimum runnable proof in the current workspace.</action>
  <verify>
    <automated>mix test test/crosswake/proof/phase8_selective_native_lane_test.exs</automated>
    <automated>rg -n '/native/claims|native-claim-capture|pack_incompatible' examples/ios_shell_host/Fixtures examples/android_shell_host/app/src/main/assets</automated>
  </verify>
  <done>The checked-in shell artifacts now prove the selective-native lane and its pack-gated activation posture end to end.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Phoenix host truth -> checked-in shell artifacts | If shell fixtures lag the route declarations, the public proof posture becomes dishonest |
| proof scripts -> public support posture | If Phase 8 proof forks a new harness, the shared artifact-class contract weakens |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-07 | T | shell fixture alignment | mitigate | Update iOS and Android fixtures together and assert the same route ids, pack checks, and denial vocabulary |
| T-08-08 | R | proof entrypoint sprawl | mitigate | Extend `script/verify_phase5_example_hosts.sh` instead of building a second Phase 8 verification flow |
| T-08-09 | T | execution-environment drift | mitigate | Keep repo-local fixture and ExUnit verification runnable even before full provisioned native-host verification succeeds |
</threat_model>

<verification>
Run `mix test test/crosswake/proof/phase5_proof_lane_test.exs test/crosswake/proof/phase8_selective_native_lane_test.exs` and validate fixture content with `rg` as the repo-local minimum proof. Then run `bash script/verify_phase5_example_hosts.sh` in a provisioned native-host environment to confirm shell fixtures and route truth agree.
</verification>

<success_criteria>
- The checked-in example hosts exercise the selective-native capture route on both platforms.
- Proof covers pack-ready and pack-blocked activation paths.
- The shared proof entrypoint remains the public artifact-class contract for Phase 8.
</success_criteria>

<output>
After completion, create `.planning/phases/08-selective-native-flow-exemplar/08-selective-native-flow-exemplar-03-SUMMARY.md`
</output>
