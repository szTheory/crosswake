---
phase: 08-selective-native-flow-exemplar
plan: 02
type: execute
wave: 2
depends_on:
  - 08-01
files_modified:
  - examples/phoenix_host/lib/crosswake_example/router.ex
  - examples/phoenix_host/lib/crosswake_example/selective_native/claims_live.ex
  - examples/phoenix_host/lib/crosswake_example/selective_native/claim_live.ex
  - examples/phoenix_host/lib/crosswake_example/selective_native/capture_live.ex
  - examples/phoenix_host/lib/crosswake_example/selective_native/review_live.ex
  - examples/phoenix_host/lib/crosswake_example/selective_native/submissions.ex
  - test/crosswake/proof/phase8_selective_native_lane_test.exs
autonomous: true
requirements:
  - NATIVE-01
  - NATIVE-02
must_haves:
  truths:
    - "The public selective-native story is claim detail -> native capture -> Phoenix review -> explicit `transfer.upload.prepare`."
    - "Only the capture route declares the required pack, explicit `:camera` capability, and native ownership; surrounding routes stay Phoenix-owned and usable without the pack."
    - "Captured-local, staged, uploaded, and submitted remain visibly distinct states in code, copy, and proof."
  artifacts:
    - path: "examples/phoenix_host/lib/crosswake_example/selective_native/capture_live.ex"
      provides: "One explicit `:native_screen` capture step with route-local `:camera` capability, pack, transfer, and sensitive-route truth"
    - path: "examples/phoenix_host/lib/crosswake_example/selective_native/review_live.ex"
      provides: "Phoenix-owned review-before-upload boundary with explicit staged-state copy"
    - path: "examples/phoenix_host/lib/crosswake_example/router.ex"
      provides: "Route-local `:camera` capability, pack, and transfer declarations on the capture route only"
    - path: "test/crosswake/proof/phase8_selective_native_lane_test.exs"
      provides: "Executable proof that the lane uses declared pack and transfer seams instead of generic container behavior"
  key_links:
    - from: "examples/phoenix_host/lib/crosswake_example/router.ex"
      to: "guides/packs.md"
      via: "implemented pack and transfer posture matches the documented staged-before-upload contract"
      pattern: "pack_incompatible|transfer.upload.prepare|native_capture|camera"
    - from: "examples/phoenix_host/lib/crosswake_example/selective_native/review_live.ex"
      to: ".planning/phases/08-selective-native-flow-exemplar/08-CONTEXT.md"
      via: "review happens before upload and keeps Phoenix authoritative"
      pattern: "review|uploaded|submitted|captured locally"
---

<objective>
Implement the product-shaped selective-native flow on top of the Phase 8 lane skeleton.

Purpose: Fulfill the main behavioral parts of NATIVE-01 and NATIVE-02 by moving the public native-capture story onto the nested claim capture route while keeping review and upload intent in Phoenix.
Output: LiveView queue/detail/review surfaces, one explicit native capture route, route-local `:camera` capability, pack, and transfer declarations, and proof that the lane stays bounded.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phases/08-selective-native-flow-exemplar/08-CONTEXT.md
@.planning/phases/08-selective-native-flow-exemplar/08-RESEARCH.md
@.planning/phases/08-selective-native-flow-exemplar/08-PATTERNS.md
@.planning/phases/08-selective-native-flow-exemplar/08-01-PLAN.md
@guides/adopter_profiles.md
@guides/packs.md
@guides/native_shell.md
@guides/bridge.md
@examples/phoenix_host/lib/crosswake_example/router.ex
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build the Phoenix-owned queue, detail, and review surfaces</name>
  <files>examples/phoenix_host/lib/crosswake_example/selective_native/claims_live.ex, examples/phoenix_host/lib/crosswake_example/selective_native/claim_live.ex, examples/phoenix_host/lib/crosswake_example/selective_native/review_live.ex, examples/phoenix_host/lib/crosswake_example/selective_native/submissions.ex, test/crosswake/proof/phase8_selective_native_lane_test.exs</files>
  <behavior>
    - Test 1: The queue, detail, and review routes all render through Phoenix-owned LiveViews and feel like one coherent claims-evidence corridor.
    - Test 2: Review remains the only place where upload intent becomes actionable, and the UI copy keeps local capture distinct from transfer completion.
    - Test 3: The lane stays generic and fixture-light instead of drifting into insurance, billing, OCR, or vendor-specific semantics.
  </behavior>
  <action>Implement the three Phoenix-owned LiveView surfaces for claim listing, claim detail, and submission review using the bounded state boundary from Plan 08-01. Keep the capture step product-shaped by making the claim detail route the explicit jump-off to native capture and the review route the explicit place where staged evidence is inspected and confirmed. Preserve the locked state vocabulary in both assigns and rendered copy so the route set never implies that capture success equals upload success or submission completion.</action>
  <verify>
    <automated>mix test test/crosswake/proof/phase8_selective_native_lane_test.exs</automated>
  </verify>
  <done>The Phase 8 lane now reads as a believable Phoenix-owned corridor around one native step instead of a generic media demo.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Move native capture onto the nested route with route-local pack and transfer seams</name>
  <files>examples/phoenix_host/lib/crosswake_example/router.ex, examples/phoenix_host/lib/crosswake_example/selective_native/capture_live.ex, examples/phoenix_host/lib/crosswake_example/selective_native/review_live.ex, test/crosswake/proof/phase8_selective_native_lane_test.exs</files>
  <behavior>
    - Test 1: Only `/native/claims/:id/capture` declares the required pack, explicit `:camera` capability, native ownership, and upload transfer seam.
    - Test 2: The primary degraded-path vocabulary for blocked capture activation is `pack_incompatible`.
    - Test 3: The native route returns one staged local artifact and never implies background upload, hidden retry, or generic bridge authority.
    - Test 4: The review route must explicitly trigger `transfer.upload.prepare`; there is no auto-upload shortcut.
  </behavior>
  <action>Recontextualize the existing native-capture substrate onto the nested claim capture route. Update router metadata so the capture route alone declares the existing `:camera` capability, one required pack, one explicit `transfer.upload.prepare` seam, and one sensitive native boundary; keep the surrounding `/native` routes free of capability overreach, pack gating, and native ownership. Implement the capture and review surfaces so the native route yields a single staged local artifact, Phoenix review remains authoritative, and the only upload handoff is the typed, route-local transfer seam already documented in Crosswake. Add or extend proof so the lane fails if a second native route appears, if `:camera` or pack declarations spread to the whole lane, or if capture semantics collapse into generic upload behavior.</action>
  <verify>
    <automated>mix test test/crosswake/proof/phase8_selective_native_lane_test.exs</automated>
    <automated>rg -n 'capabilities|:camera|camera|pack_incompatible|transfer.upload.prepare|captured locally|staged' examples/phoenix_host/lib/crosswake_example/selective_native examples/phoenix_host/lib/crosswake_example/router.ex</automated>
  </verify>
  <done>The selective-native lane now proves one explicit native capture route with route-local `:camera` capability, pack, and transfer seams and a Phoenix-owned review boundary.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| native capture route -> surrounding Phoenix routes | The native step must not expand into a second runtime authority or turn the bridge into workflow control |
| capture result -> upload path | If staged capture is treated as uploaded or submitted, the exemplar becomes dishonest about transfer ownership |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-04 | E | capture route ownership | mitigate | Keep native ownership on one route only and fail proof if broader native drift appears |
| T-08-05 | T | capability, pack, and transfer contract | mitigate | Bind the explicit `:camera` capability, required pack, and `transfer.upload.prepare` seam to the capture route only and assert `pack_incompatible` denials |
| T-08-06 | I | staged-versus-uploaded truth | mitigate | Keep staged, uploaded, and submitted states distinct in services, copy, and tests |
</threat_model>

<verification>
Run `mix test test/crosswake/proof/phase8_selective_native_lane_test.exs` and confirm the lane stays bounded to one native route, one route-local `:camera` capability and pack gate, and one explicit review-before-upload handoff.
</verification>

<success_criteria>
- The public selective-native route story is the locked nested claims-evidence flow.
- Only the capture route declares the explicit `:camera` capability, required pack, and native ownership.
- Review happens before `transfer.upload.prepare`, and no surface implies that capture equals upload or submission.
</success_criteria>

<output>
After completion, create `.planning/phases/08-selective-native-flow-exemplar/08-selective-native-flow-exemplar-02-SUMMARY.md`
</output>
