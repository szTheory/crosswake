---
phase: "73"
plan: "02"
type: execute
wave: 2
depends_on: ["73-01"]
files_modified:
  - examples/phoenix_host/lib/crosswake_example/saas_portal/step_up_challenge_live.ex
  - examples/phoenix_host/lib/crosswake_example/router.ex
  - test/crosswake/proof/phase73_auth_sensitive_admin_workflow_proof_test.exs
  - .github/workflows/phase73-proof.yml
autonomous: true
requirements:
  - ADM-01
  - ADM-02
must_haves:
  truths:
    - "Step-up UI appears exactly as specified in 73-UI-SPEC.md when an admin route is accessed without sufficient auth."
    - "Archetype proof lane passes deterministically in CI."
  artifacts:
    - path: examples/phoenix_host/lib/crosswake_example/saas_portal/step_up_challenge_live.ex
      provides: "Challenge UI component"
    - path: test/crosswake/proof/phase73_auth_sensitive_admin_workflow_proof_test.exs
      provides: "Proof test file"
    - path: .github/workflows/phase73-proof.yml
      provides: "Proof CI workflow"
  key_links:
    - from: examples/phoenix_host/lib/crosswake_example/router.ex
      to: examples/phoenix_host/lib/crosswake_example/saas_portal/step_up_challenge_live.ex
      via: "route definition"
      pattern: "live.*StepUpChallengeLive"
    - from: examples/phoenix_host/lib/crosswake_example/saas_portal/step_up_challenge_live.ex
      to: examples/phoenix_host/lib/crosswake_example/saas_portal/step_up.ex
      via: "submission to consume/1"
      pattern: "StepUp.consume"
---

<objective>
Build the visual step-up challenge UI per 73-UI-SPEC.md, finalize the targeted CI proof test, and ensure the workflow runs green in CI.

Purpose: Deliver the visible product experience and ensure CI automation for the proof lane per D-09, D-10, and D-11.
Output: Working challenge UI, wired routes, CI workflow, and passing proof test.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/73-auth-sensitive-admin-workflow-proof/73-CONTEXT.md
@.planning/phases/73-auth-sensitive-admin-workflow-proof/73-UI-SPEC.md
@.planning/phases/73-auth-sensitive-admin-workflow-proof/73-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build Step-Up Challenge UI and Wire Routes</name>
  <files>examples/phoenix_host/lib/crosswake_example/saas_portal/step_up_challenge_live.ex, examples/phoenix_host/lib/crosswake_example/router.ex</files>
  <action>Create the LiveView UI component strictly matching `73-UI-SPEC.md` constraints (Secondary #F8FAFC card, Accent #2563EB CTA, "Verify Admin Identity" button text, "Admin Access Restricted" header). Add the route to `router.ex` to serve the challenge path. Wire the UI form submission to call `StepUp.consume/1` (or equivalent) in the state machine to satisfy the challenge and redirect the user.</action>
  <verify>
    <automated>mix compile</automated>
  </verify>
  <done>UI component complies with UX contract and router is successfully wired.</done>
</task>

<task type="auto">
  <name>Task 2: Finalize Targeted Proof Test and CI Workflow</name>
  <files>test/crosswake/proof/phase73_auth_sensitive_admin_workflow_proof_test.exs, .github/workflows/phase73-proof.yml</files>
  <action>Ensure `phase73_auth_sensitive_admin_workflow_proof_test.exs` is complete and asserts all requirements matching D-10 (route gates, intent invariants). Define the GitHub Actions YAML `phase73-proof.yml` mimicking other archetype proofs per D-11, isolating tests to `test/crosswake/proof/phase73_auth_sensitive_admin_workflow_proof_test.exs`. Reference the analog patterns identified in `73-PATTERNS.md` for Archetype CI & Test Hermeticity using RouteGate to verify explicit transitions without mocking HTTP requests.</action>
  <verify>
    <automated>mix test test/crosswake/proof/phase73_auth_sensitive_admin_workflow_proof_test.exs</automated>
  </verify>
  <done>Test passes hermetically and workflow definition is syntactically valid.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client → UI | Untrusted client rendering step-up completion |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-73-03 | Spoofing | Challenge UI | mitigate | Keep intent parsing server-side and never trust client completion states directly. |
</threat_model>

<verification>
mix test test/crosswake/proof/phase73_auth_sensitive_admin_workflow_proof_test.exs passes.
</verification>

<success_criteria>
Challenge UI conforms exactly to the design spec, tests cover all invariant cases, and CI is configured.
</success_criteria>

<output>
Create `.planning/phases/73-auth-sensitive-admin-workflow-proof/73-02-SUMMARY.md` when done
</output>
