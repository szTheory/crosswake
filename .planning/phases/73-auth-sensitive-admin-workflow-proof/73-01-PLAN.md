---
phase: "73"
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - examples/phoenix_host/lib/crosswake_example/saas_portal/step_up_intent.ex
  - examples/phoenix_host/lib/crosswake_example/saas_portal/step_up.ex
  - examples/phoenix_host/lib/crosswake_example/saas_portal/step_up_plug.ex
  - examples/phoenix_host/lib/crosswake_example/saas_portal/step_up_on_mount.ex
autonomous: true
requirements:
  - ADM-01
  - ADM-02
must_haves:
  truths:
    - "Unauthenticated admin access attempts trigger a secure step-up ceremony."
    - "Plug and LiveView evaluation accurately halt and redirect for missing auth levels."
  artifacts:
    - path: examples/phoenix_host/lib/crosswake_example/saas_portal/step_up.ex
      provides: "State machine logic"
    - path: examples/phoenix_host/lib/crosswake_example/saas_portal/step_up_on_mount.ex
      provides: "LiveView hook"
  key_links:
    - from: examples/phoenix_host/lib/crosswake_example/saas_portal/step_up_on_mount.ex
      to: examples/phoenix_host/lib/crosswake_example/saas_portal/step_up.ex
      via: "evaluate_or_issue call"
      pattern: "StepUpCeremony.evaluate_or_issue"
---

<objective>
Finalize the StepUpIntent Ecto schema and the shared route gating logic.

Purpose: Backend authority strictly controls step-up intent generation to protect administrative routes from native cache replay.
Output: Working core logic for the challenge state machine and `on_mount`/Plug route protections.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/73-auth-sensitive-admin-workflow-proof/73-CONTEXT.md
@.planning/phases/73-auth-sensitive-admin-workflow-proof/73-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Formalize Step-Up Intent Schema and State Machine</name>
  <files>examples/phoenix_host/lib/crosswake_example/saas_portal/step_up_intent.ex, examples/phoenix_host/lib/crosswake_example/saas_portal/step_up.ex</files>
  <action>Ensure `StepUpIntent` schema accurately enforces the states `pending -> challenged -> satisfied -> consumed` including explicit `expired` and `canceled` failure edges (D-03). Implement the server-issued `StepUpIntent` contract containing canonical return route, expiry, and required MFA level (D-01). Enforce that step-up evidence is valid against the issued intent before unlocking the admin route (D-06). Ensure `step_up.ex` properly wraps intent generation and consumption within Ecto Multi to safely rotate session IDs and CSRF material (e.g. `Plug.CSRFProtection.delete_csrf_token/0`) and generate telemetry audit logs per D-04 and D-08. Reference the analog patterns identified in `73-PATTERNS.md` for Ecto Intent State Machine and Telemetry Event Emission.</action>
  <verify>
    <automated>mix test test/crosswake/proof/phase73_auth_sensitive_admin_workflow_proof_test.exs</automated>
  </verify>
  <done>Core domain logic is clean and compiles without warnings.</done>
</task>

<task type="auto">
  <name>Task 2: Finalize Shared Plug and LiveView Gating</name>
  <files>examples/phoenix_host/lib/crosswake_example/saas_portal/step_up_plug.ex, examples/phoenix_host/lib/crosswake_example/saas_portal/step_up_on_mount.ex</files>
  <action>Verify and formalize the route evaluation gating that redirects unauthorized requests to the step-up challenge route using `decision()` and maps strict generic denial messages without leaking sensitive context per D-05 and D-07. Do not accept arbitrary absolute return URLs; the return target must resolve to a known manifest route ID/path (D-02). Reference the analog pattern for LiveView on_mount and Plug Pipeline route gating defined in `73-PATTERNS.md` using `evaluate_or_issue/3`.</action>
  <verify>
    <automated>mix test test/crosswake/proof/phase73_auth_sensitive_admin_workflow_proof_test.exs</automated>
  </verify>
  <done>Plug and on_mount logic cleanly map decisions to allow, halt, or redirect transitions.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client → API | Untrusted tokens and session persistence attempting admin actions |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-73-01 | Tampering | Session Gating | mitigate | Enforce step-up ceremony logic centrally and delete CSRF tokens upon success. |
| T-73-02 | Elevation | Native Auth | mitigate | Reject implicit local cache validity for admin routes; force fresh intent challenge. |
</threat_model>

<verification>
mix compile runs green.
</verification>

<success_criteria>
Step-Up gating logic and state machine are correctly finalized.
</success_criteria>

<output>
Create `.planning/phases/73-auth-sensitive-admin-workflow-proof/73-01-SUMMARY.md` when done
</output>
