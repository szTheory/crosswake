---
phase: 111-generator-rewire-clean-room-proof-release
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/crosswake/doctor/publish_readiness.ex
  - guides/adoption.md
  - guides/install.md
  - guides/support_matrix.md
  - CHANGELOG.md
autonomous: true
requirements: [DOCS-01]
must_haves:
  truths:
    - "`guides/adoption.md` is in PublishReadiness.@allowed_docs (whitelist consistent with mix.exs extras)"
    - "adoption.md §1 reframes the contradictory 'avoid generating host-owned shell code' sentence to the thin-wrapper / published-deps framing — without deleting the section"
    - "adoption.md and install.md cross-link each other (header note → install.md; back-reference → adoption.md)"
    - "support_matrix.md and CHANGELOG.md reconcile to published truth; CHANGELOG.md has a ## [0.1.2] section"
    - "No false-shipped-claims strings appear in CHANGELOG.md; `mix crosswake.doctor --check-publish` docs/parity checks pass"
  artifacts:
    - path: "lib/crosswake/doctor/publish_readiness.ex"
      provides: "guides/adoption.md added to @allowed_docs"
      contains: "guides/adoption.md"
    - path: "guides/adoption.md"
      provides: "reframed §1 + cross-link header to install.md"
      contains: "install.md"
    - path: "guides/install.md"
      provides: "back-reference to adoption.md"
      contains: "adoption.md"
    - path: "CHANGELOG.md"
      provides: "## [0.1.2] section reconciled to published truth"
      contains: "## [0.1.2]"
  key_links:
    - from: "guides/adoption.md"
      to: "guides/install.md"
      via: "header note pointing to the definitive install sequence"
      pattern: "install.md"
    - from: "PublishReadiness.@allowed_docs"
      to: "guides/adoption.md"
      via: "whitelist entry checked by docs_support_parity_check"
      pattern: "guides/adoption.md"
---

<objective>
Reconcile the public install story to the published truth: fix the documentation contradiction in `adoption.md`, cross-link it with the canonical `install.md`, add the missing whitelist entry, and reconcile `support_matrix.md` + `CHANGELOG.md` to reality — zero 404 / unresolvable install routes.

Purpose: `adoption.md` §1 currently says "Avoid generating host-owned shell code," which directly contradicts the product thesis (`mix crosswake.gen.shell` IS the install path). It was written before the v5.0 standalone-package architecture was complete. This plan makes the standalone-deps claim and the gen.shell thesis simultaneously true, and fixes a whitelist inconsistency where `adoption.md` is a `mix.exs` extra but not in `@allowed_docs`.

Output: `adoption.md` reframed §1 + cross-links, `install.md` back-reference, `@allowed_docs` whitelist fix, `support_matrix.md` + `CHANGELOG.md` reconciled with a `## [0.1.2]` entry.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/111-generator-rewire-clean-room-proof-release/111-CONTEXT.md
@.planning/phases/111-generator-rewire-clean-room-proof-release/111-RESEARCH.md

<interfaces>
<!-- Extracted from publish_readiness.ex — use directly. -->

@allowed_docs (lines 16-25) current list:
  ["guides/support_matrix.md", "guides/compatibility.md", "guides/companions.md",
   "guides/commerce.md", "guides/native_shell.md", "guides/capabilities.md",
   "guides/install.md", "CHANGELOG.md"]
  → ADD "guides/adoption.md" (already a mix.exs extra; D-03a fixes the inconsistency).

docs_support_parity_check/2 (lines 471-500) already uses @allowed_docs correctly — no logic change.

published_hex_truth?/1 (lines 668-672): requires "## [0.1.0]" and a "## [Unreleased]"
  section with 4 required subsections.

publish_parity_check (lines 207-209): verifies `changelog =~ "[#{expected_version}]"`
  → with @version "0.1.2" set, CHANGELOG must contain "## [0.1.2]".

no_false_shipped_claims?/1 (lines 674-685): CHANGELOG must NOT contain any of:
  "StoreKit provider adapter shipped", "Play Billing provider adapter shipped",
  "Chimeway delivery is supported", "standalone native shell packages are shipped",
  "full Sigra machinery is shipped".
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add adoption.md to @allowed_docs and reframe the contradictory §1 + cross-links</name>
  <files>lib/crosswake/doctor/publish_readiness.ex, guides/adoption.md, guides/install.md</files>
  <read_first>
    - lib/crosswake/doctor/publish_readiness.ex lines 16-25 (@allowed_docs) and lines 471-500 (docs_support_parity_check — confirm it reads the whitelist; no logic change needed)
    - guides/adoption.md §1 "Integrate the Crosswake Shell" (the contradictory "Avoid generating host-owned shell code" sentence)
    - guides/install.md top section (where the back-reference one-liner goes)
    - .planning/phases/111-generator-rewire-clean-room-proof-release/111-RESEARCH.md § Doc Reconciliation (the D-03b reframe language + D-03c cross-link copy, both within Claude's Discretion on exact wording)
  </read_first>
  <action>
    Add `"guides/adoption.md"` to the `@allowed_docs` list in `publish_readiness.ex` (D-03a — no other change to docs_support_parity_check/2). In `adoption.md` §1, REFRAME (do not delete) the "Avoid generating host-owned shell code" sentence to the thin-wrapper framing per D-03b: the generated shell is a thin, host-owned wrapper whose native deps (SPM `github.com/szTheory/crosswake-shell-core-ios`, Maven `io.github.sztheory:crosswake-shell-core-android`) resolve from published registries, not vendored/monorepo paths — the eject trap is eliminated by the published-core architecture, not by avoiding generation. Add a header note in adoption.md pointing to `install.md` for the definitive install sequence (D-03c). Add a one-line back-reference in `install.md` pointing to `adoption.md` for offline-sync architecture context (D-03c). Do NOT overstate (Pitfall 5): do not claim deps resolve without adding the Hex dep, and do not claim the shell never needs regeneration. Exact wording is Claude's Discretion within D-03b/D-03c intent.
  </action>
  <verify>
    <automated>cd /Users/jon/projects/crosswake && mix crosswake.doctor --check-publish</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "guides/adoption.md" lib/crosswake/doctor/publish_readiness.ex` matches inside @allowed_docs
    - `grep -c "Avoid generating host-owned shell code" guides/adoption.md` returns 0 (sentence reframed away)
    - `grep -l "install.md" guides/adoption.md` and `grep -l "adoption.md" guides/install.md` both match (bidirectional cross-link)
    - `mix crosswake.doctor --check-publish` does not regress (docs parity check still green)
  </acceptance_criteria>
  <done>adoption.md whitelisted + reframed + cross-linked with install.md; no contradiction remains; doctor docs parity green.</done>
</task>

<task type="auto">
  <name>Task 2: Reconcile support_matrix.md and CHANGELOG.md to published truth</name>
  <files>guides/support_matrix.md, CHANGELOG.md</files>
  <read_first>
    - CHANGELOG.md (full file — confirm the existing `## [0.1.0]` and `## [Unreleased]` sections and their 4 required subsections)
    - guides/support_matrix.md (full file — reconcile any stale claims to published truth)
    - lib/crosswake/doctor/publish_readiness.ex lines 207-209 (publish_parity_check version assertion), lines 668-685 (published_hex_truth?/1 + no_false_shipped_claims?/1)
    - .planning/phases/111-generator-rewire-clean-room-proof-release/111-RESEARCH.md § Doc Reconciliation (CHANGELOG requirements)
  </read_first>
  <action>
    Add a `## [0.1.2]` section to `CHANGELOG.md` describing the coordinated release (template rewire to published coordinates, clean-room proof, parity guard) so `publish_parity_check`'s `changelog =~ "[0.1.2]"` assertion passes. Keep the existing `## [0.1.0]` section and the `## [Unreleased]` section with its 4 required subsections intact (published_hex_truth?/1). Do NOT introduce any of the 5 forbidden false-shipped-claim strings (no_false_shipped_claims?/1): "StoreKit provider adapter shipped", "Play Billing provider adapter shipped", "Chimeway delivery is supported", "standalone native shell packages are shipped", "full Sigra machinery is shipped". Reconcile `support_matrix.md` to current published truth — correct any iOS/Android coordinate or version references to `github.com/szTheory/crosswake-shell-core-ios` / `io.github.sztheory:crosswake-shell-core-android` and the published install route; ensure zero 404 / unresolvable install URLs.
  </action>
  <verify>
    <automated>cd /Users/jon/projects/crosswake && mix crosswake.doctor --check-publish</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "## \[0.1.2\]" CHANGELOG.md` returns 1
    - `grep -c "## \[0.1.0\]" CHANGELOG.md` still returns at least 1 and `grep -c "## \[Unreleased\]" CHANGELOG.md` returns 1
    - `grep -E "StoreKit provider adapter shipped|Play Billing provider adapter shipped|Chimeway delivery is supported|standalone native shell packages are shipped|full Sigra machinery is shipped" CHANGELOG.md` returns no match
    - `mix crosswake.doctor --check-publish` green (publish_parity + no_false_shipped_claims + docs parity all pass)
    - no 404 install route in support_matrix.md (all referenced coordinates use szTheory / io.github.sztheory)
  </acceptance_criteria>
  <done>CHANGELOG has a [0.1.2] section with no false claims; support_matrix reflects published coordinates; doctor publish parity green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| published docs → external adopter | adopter reads adoption.md / install.md / support_matrix.md to install; a 404 or false claim breaks the install JTBD |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-111-08 | Information Disclosure | docs assert an install route that does not resolve (404 / monorepo-only) | mitigate | reconcile all coordinates to szTheory / io.github.sztheory; doctor docs-parity + no-false-claims checks gate |
| T-111-09 | Repudiation | reframe accidentally introduces a false shipped-claim or overstates the resolve-without-setup story | mitigate | no_false_shipped_claims?/1 enforces 5 forbidden strings; Pitfall-5 guidance forbids overstatement; `--check-publish` run after edit |
</threat_model>

<verification>
- `mix crosswake.doctor --check-publish` green (docs parity, publish parity, no-false-claims)
- Manual: confirm every install URL in adoption.md / install.md / support_matrix.md resolves (szTheory mirror + Maven Central + hex.pm) — no `github.com/crosswake/...` or `dev.crosswake` left
</verification>

<success_criteria>
- `guides/adoption.md` documents the actual published install path with no 404 URL and no monorepo-only install route; `guides/support_matrix.md` reflects current published truth (ROADMAP success criterion 4 + DOCS-01)
- adoption.md and install.md cross-link; `adoption.md` is whitelisted in `@allowed_docs`
- `CHANGELOG.md` has `## [0.1.2]` and no false-shipped-claims
</success_criteria>

<output>
Create `.planning/phases/111-generator-rewire-clean-room-proof-release/111-03-SUMMARY.md` when done
</output>
