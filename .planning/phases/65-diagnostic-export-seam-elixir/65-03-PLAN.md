---
phase: 65-diagnostic-export-seam-elixir
plan: 03
type: execute
wave: 3
depends_on: [65-01, 65-02]
files_modified:
  - test/fixtures/diagnostic/native_ios_crash.json
  - test/fixtures/diagnostic/native_ios_metrickit_hang.json
  - test/fixtures/diagnostic/native_android_anr.json
  - test/fixtures/diagnostic/native_android_low_memory.json
  - test/fixtures/diagnostic/web_liveview_fault.json
  - test/fixtures/diagnostic/bridge_command_fault.json
  - test/crosswake/proof/phase65_diagnostic_export_seam_test.exs
autonomous: true
requirements: [DIAG-01, DIAG-02, DIAG-03, DIAG-04]
must_haves:
  truths:
    - "Six fixtures under test/fixtures/diagnostic/ are GENERATED from to_map/1 output (one per layer × exit-reason axis) and normalize via assert_normalized_json_fixture"
    - "The proof lane asserts no diagnostics.* entry exists in Bridge.Contract.commands/0 (D-04 proof assertion (a))"
    - "The proof lane asserts no Req/Finch/HTTPoison/Mint/:httpc dep in mix.exs and no HTTP-sending reference in the DiagnosticExport module source (D-03 no-HTTP-dep; D-04 proof assertion (b))"
    - "The proof lane asserts forbidden_keys/0 ⟂ allowed_keys/0 over all 19 forbidden keys plus a sanitize/1 fail-closed + round-trip"
    - "The proof lane asserts the SupportMatrix truth + advisory doctor finding are present and non-overclaiming"
    - "The proof lane is hermetic (no @moduletag, no example-host refs, no MIX_INCLUDE_ flags)"
  artifacts:
    - path: "test/crosswake/proof/phase65_diagnostic_export_seam_test.exs"
      provides: "merge-blocking hermetic proof lane covering DIAG-01..04"
      contains: "Phase65"
    - path: "test/fixtures/diagnostic/native_ios_crash.json"
      provides: "canonical normalized Envelope fixture (native iOS crash axis)"
      contains: "native"
  key_links:
    - from: "test/crosswake/proof/phase65_diagnostic_export_seam_test.exs"
      to: "Crosswake.Bridge.Contract.commands/0"
      via: "refute Enum.any?(commands, &String.starts_with?(&1, \"diagnostics\"))"
      pattern: "Bridge.Contract.commands"
    - from: "test/crosswake/proof/phase65_diagnostic_export_seam_test.exs"
      to: "test/fixtures/diagnostic/*.json"
      via: "assert_normalized_json_fixture per axis"
      pattern: "assert_normalized_json_fixture"
---

<objective>
Generate the six canonical diagnostic envelope fixtures from running code and build the merge-blocking, hermetic phase-65 proof lane that asserts all four DIAG requirements in one file.

Purpose: This is the keystone that makes Phase 65 merge-blocking. It proves DIAG-01 (no bridge vocabulary + no HTTP dep), DIAG-02 (typed versioned envelope + fixtures normalize), DIAG-03 (forbidden ⟂ allowed + fail-closed sanitize round-trip), and DIAG-04 (support-truth + advisory doctor present + non-overclaiming). Fixtures become the shapes Phase 67 native shells parity-lock against.
Output: `test/fixtures/diagnostic/*.json` (6 files) + `test/crosswake/proof/phase65_diagnostic_export_seam_test.exs`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/65-diagnostic-export-seam-elixir/65-CONTEXT.md
@.planning/phases/65-diagnostic-export-seam-elixir/65-RESEARCH.md
@.planning/phases/65-diagnostic-export-seam-elixir/65-PATTERNS.md

<interfaces>
<!-- Proof-lane assertion targets (all spellings locked by Plans 01 + 02). -->

Crosswake.Shell.DiagnosticExport:
  new_envelope/1, new_envelope!/1, new_native_diagnostic/1, to_map/1, sanitize/1
  layers/0, platforms/0, kinds/0, sources/0, exit_reasons/0
  forbidden_keys/0 (19), allowed_keys/0
Crosswake.Bridge.Contract.commands/0  -> the 10-command list (proof anchor; must gain no "diagnostics.*")
Crosswake.SupportMatrix.diagnostic_export_support_truth/0 -> [entry]; entry.delivery_supported == false
Crosswake.Doctor.run/1 -> %Report{findings: [...]}; one finding code "diagnostic_export.contract_shipped" severity :advisory
Crosswake.TestSupport.ProofAssertions.stable_id_message/7, assert_normalized_json_fixture/4

Fixture axes (D-10), one JSON per file under test/fixtures/diagnostic/:
  native_ios_crash          (layer :native, platform :ios,     kind :crash,       source :metrickit,     exit_reason :crash)
  native_ios_metrickit_hang (layer :native, platform :ios,     kind :hang,        source :metrickit,     exit_reason :hang)
  native_android_anr        (layer :native, platform :android, kind :hang,        source :app_exit_info, exit_reason :anr)
  native_android_low_memory (layer :native, platform :android, kind :termination, source :app_exit_info, exit_reason :low_memory)
  web_liveview_fault        (layer :web,    platform :web,     kind :web_fault,   native_diagnostic nil)
  bridge_command_fault      (layer :bridge, platform :ios,     kind :bridge_fault, native_diagnostic nil)

Canonical 19 forbidden keys to iterate in the allowlist proof:
  :token, :raw_token, :device_token, :registration_token, :apns_token, :fcm_token,
  :provider_payload, :raw_payload, :notification_title, :notification_body,
  :route_params, :actor_id, :subject_ref, :session_ref, :device_id, :ip,
  :user_agent, :email, :provider_response_body
</interfaces>

# Analogs (read in full before writing)
@test/crosswake/proof/phase64_runtime_line_policy_test.exs
@test/crosswake/proof/phase58_auth_diagnostics_closeout_test.exs
@test/support/proof_assertions.ex
@test/fixtures/proof/phase52_operator_inspection.json
@lib/crosswake/shell/diagnostic_export.ex
@lib/crosswake/bridge/contract.ex
@lib/crosswake/support_matrix/support_matrix.ex
@lib/crosswake/doctor/doctor.ex
</context>

<tasks>

<task type="auto">
  <name>Task 1: Generate the six canonical diagnostic fixtures from to_map/1 output</name>
  <files>test/fixtures/diagnostic/native_ios_crash.json, test/fixtures/diagnostic/native_ios_metrickit_hang.json, test/fixtures/diagnostic/native_android_anr.json, test/fixtures/diagnostic/native_android_low_memory.json, test/fixtures/diagnostic/web_liveview_fault.json, test/fixtures/diagnostic/bridge_command_fault.json</files>
  <read_first>
    - lib/crosswake/shell/diagnostic_export.ex (new_envelope!/1 + new_native_diagnostic/1 + to_map/1 — fixtures must be GENERATED from this, never hand-authored, per D-10 / pitfall 5)
    - test/fixtures/proof/phase52_operator_inspection.json (existing fixture JSON shape/formatting convention)
    - test/support/proof_assertions.ex (assert_normalized_json_fixture/4 — which volatile keys it strips, lines 15-37, so fixtures match normalized form)
  </read_first>
  <action>
    Create the `test/fixtures/diagnostic/` directory (new sibling of `test/fixtures/proof/`). Generate each of the six fixtures by building the Envelope via `Crosswake.Shell.DiagnosticExport.new_envelope!/1` with the locked attrs for its axis (see the <interfaces> axis table), using deterministic non-volatile placeholder values: `correlation_id` like "phase65-fixture-native-ios-crash", `observed_at` "2026-06-04T00:00:00Z", `native_runtime_version` "1.0.0". For the four `:native` axes, build a `%NativeDiagnostic{}` via `new_native_diagnostic/1` with the axis's `source`/`exit_reason` and attach it to the envelope; for `web_liveview_fault` and `bridge_command_fault`, leave native_diagnostic nil (dropped by to_map/1). Then `DiagnosticExport.to_map/1 |> Jason.encode!(pretty: true)` and write each to its file. Do NOT hand-author the JSON — run a one-off `mix run`/`iex -S mix` snippet calling the real constructor + to_map/1 so fixtures exactly match what the proof regenerates. Each file must be valid JSON with stringified atom values ("native" not ":native") and no nil fields.
  </action>
  <verify>
    <automated>test $(ls test/fixtures/diagnostic/*.json | wc -l) -eq 6 && mix run -e 'Enum.each(Path.wildcard("test/fixtures/diagnostic/*.json"), fn f -> Jason.decode!(File.read!(f)) end); IO.puts("all-valid-json")'</automated>
  </verify>
  <acceptance_criteria>
    - Exactly six files exist under `test/fixtures/diagnostic/` matching the six axis names in <interfaces>.
    - Each file is valid JSON (decodes via `Jason.decode!`).
    - Atom values are stringified (e.g. `"layer": "native"`, not `:native`); no nil fields present.
    - Each fixture's content equals `DiagnosticExport.to_map/1` output for its axis (i.e. regenerating produces byte-equivalent normalized content — verified by the Task 2 proof round-trip).
    - Fixtures were generated from running code, not hand-authored.
  </acceptance_criteria>
  <done>
    Six axis fixtures exist under test/fixtures/diagnostic/, all valid JSON generated from to_map/1, stringified atoms, no nils.
  </done>
</task>

<task type="auto">
  <name>Task 2: Write the merge-blocking hermetic phase-65 proof lane</name>
  <files>test/crosswake/proof/phase65_diagnostic_export_seam_test.exs</files>
  <read_first>
    - test/crosswake/proof/phase64_runtime_line_policy_test.exs (module header `use ExUnit.Case, async: false`, @tag per-requirement, stable_id_message usage, and the hermetic-lane guard test at the bottom — copy verbatim, rename to phase65)
    - test/crosswake/proof/phase58_auth_diagnostics_closeout_test.exs (the `assert forbidden in forbidden_keys(); refute forbidden in allowed_keys()` idiom)
    - test/support/proof_assertions.ex (stable_id_message/7 signature lines 8-13; assert_normalized_json_fixture/4 lines 15-37)
    - lib/crosswake/bridge/contract.ex (commands/0 — the 10-command anchor; assert no "diagnostics.*" added)
    - lib/crosswake/shell/diagnostic_export.ex (the module under proof)
    - lib/crosswake/support_matrix/support_matrix.ex (diagnostic_export_support_truth/0)
    - lib/crosswake/doctor/doctor.ex (Doctor.run/1 + the "diagnostic_export.contract_shipped" finding)
    - mix.exs (deps list — proof reads its source to assert no HTTP-client dep)
  </read_first>
  <action>
    Create `test/crosswake/proof/phase65_diagnostic_export_seam_test.exs` as `Crosswake.Proof.Phase65DiagnosticExportSeamTest` using `use ExUnit.Case, async: false`, NO `@moduletag`, aliasing `ProofAssertions`, `Crosswake.Shell.DiagnosticExport`, `Crosswake.Bridge.Contract`, `Crosswake.SupportMatrix`, `Crosswake.Doctor`. Every `assert`/`refute` carries a `ProofAssertions.stable_id_message/7` failure message with id `"proof.diag_NN.<desc>"` and `posture: :merge_blocking`. Group by `@tag :diag_01`..`:diag_04`.
    (a) `@tag :diag_01`: `refute Enum.any?(Bridge.Contract.commands(), &String.starts_with?(&1, "diagnostics"))`; read `mix.exs` source via `File.read!` and `refute` it contains any of `Req`/`Finch`/`HTTPoison`/`Mint`/`:httpc`; read `lib/crosswake/shell/diagnostic_export.ex` source and `refute` it references those HTTP clients; assert `DiagnosticExport.behaviour_info(:callbacks)` includes `{:export, 1}`.
    (b) `@tag :diag_02`: assert the Envelope/NativeDiagnostic enforce-key sets and closed-enum accessors (`layers/0` == `[:native, :web, :bridge]`, etc.); assert NativeDiagnostic has no `:raw_payload` key. Then for each of the six fixtures, build the Envelope from its axis attrs, `to_map/1 |> Jason.encode!`, and call `ProofAssertions.assert_normalized_json_fixture("proof.diag_02.fixture.<axis>", json, "test/fixtures/diagnostic/<axis>.json", source:, hint:, posture: :merge_blocking)`.
    (c) `@tag :diag_03`: iterate the 19 forbidden keys asserting `key in DiagnosticExport.forbidden_keys()` and `refute key in DiagnosticExport.allowed_keys()`; assert a valid native-iOS-crash attrs map round-trips `sanitize/1` to `{:ok, %Envelope{}}`; assert injecting any forbidden key yields `{:error, :redaction_failed}`; assert an out-of-enum layer yields `{:error, :redaction_failed}`; assert a non-map input yields `{:error, :redaction_failed}`.
    (d) `@tag :diag_04`: assert `SupportMatrix.diagnostic_export_support_truth/0` is non-empty; assert `delivery_supported == false`, `deferred` contains the 3 atoms, `telemetry.authority_source == :host_configured_endpoint`; assert `entry.posture` contains "not a crash-reporting service"; assert `Doctor.run([])` findings include exactly one `code == "diagnostic_export.contract_shipped"` with `severity == :advisory`; assert that finding's `message` does NOT contain "crash-reporting service".
    (f) Copy the hermetic-lane guard test verbatim from phase64 (the final test): assert source has no `@moduletag`, no `Crosswake.Example.` reference, no `MIX_INCLUDE_` flag — rename messages to "phase65".
  </action>
  <verify>
    <automated>mix test test/crosswake/proof/phase65_diagnostic_export_seam_test.exs 2>&1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - `mix test test/crosswake/proof/phase65_diagnostic_export_seam_test.exs` passes (all DIAG-01..04 groups + hermetic guard green).
    - DIAG-01 group: `Bridge.Contract.commands()` has no "diagnostics.*" entry; mix.exs + module source contain no Req/Finch/HTTPoison/Mint/:httpc; `{:export, 1}` present.
    - DIAG-02 group: closed-enum accessors return locked sets; NativeDiagnostic has no `:raw_payload`; all six fixtures pass `assert_normalized_json_fixture`.
    - DIAG-03 group: all 19 forbidden keys in `forbidden_keys()` and none in `allowed_keys()`; sanitize round-trip `{:ok, _}`; forbidden-key / out-of-enum / non-map all `{:error, :redaction_failed}`.
    - DIAG-04 group: support-truth non-overclaiming + one `:advisory` `"diagnostic_export.contract_shipped"` finding whose message excludes "crash-reporting service".
    - Hermetic guard test passes: no `@moduletag`, no example-host ref, no `MIX_INCLUDE_` in the file.
    - `mix test` (full suite) stays green.
  </acceptance_criteria>
  <done>
    The phase65 proof lane exists, is hermetic, and asserts all four DIAG requirements (no-bridge-vocab + no-HTTP-dep, typed envelope + 6 fixtures normalize, forbidden ⟂ allowed + fail-closed sanitize, support-truth + advisory doctor non-overclaim); full suite green.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| proof lane → merge gate | The proof lane is the enforcement boundary: it converts the locked DIAG decisions into merge-blocking assertions. A weak/missing assertion is the threat (regression slips through), not data leakage. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-65-07 | Tampering | Bridge.Contract.commands/0 vocabulary | mitigate | Proof asserts `refute Enum.any?(commands, &String.starts_with?(&1, "diagnostics"))` — a future diagnostics.* bridge command can never merge silently (DIAG-01 / D-04). |
| T-65-08 | Elevation of Privilege | mix.exs deps + DiagnosticExport source | mitigate | Proof greps mix.exs and the module source for Req/Finch/HTTPoison/Mint/:httpc — an added HTTP-client dep (first-party-service creep) fails the merge gate (D-03/D-04). |
| T-65-09 | Information Disclosure | forbidden_keys ⟂ allowed_keys + sanitize/1 | mitigate | The merge-blocking allowlist test over all 19 forbidden keys + the fail-closed sanitize round-trip proves no token/PII key can enter an Envelope (DIAG-03 / D-15). |
| T-65-10 | Spoofing (misrepresentation) | SupportMatrix truth + doctor finding | mitigate | Proof asserts delivery_supported false, deferred atoms present, host_configured_endpoint authority, posture "not a crash-reporting service", advisory severity, non-overclaim message (DIAG-04 / D-16..D-18). |
| T-65-11 | Tampering | fixture drift | mitigate | Fixtures are generated from to_map/1 and locked by `assert_normalized_json_fixture`; any drift between code and fixture fails the proof (D-10 / pitfall 5). |
| T-65-SC | Tampering | hex/npm/pip installs | mitigate | This plan adds NO dependency and the proof itself asserts no HTTP-client dep was added. No package-install task exists. |
</threat_model>

<verification>
- `mix test test/crosswake/proof/phase65_diagnostic_export_seam_test.exs` green (DIAG-01..04 + hermetic guard).
- Six fixtures generated from to_map/1, all normalize.
- No diagnostics.* in Bridge.Contract.commands/0; no HTTP-client dep in mix.exs or module source.
- forbidden_keys/0 ⟂ allowed_keys/0 over 19 keys; sanitize fail-closed on forbidden/out-of-enum/non-map.
- support-truth + advisory doctor present and non-overclaiming.
- Full `mix test` suite green.
</verification>

<success_criteria>
- DIAG-01: proof asserts fire-and-forget HTTP seam shape with no bridge vocabulary and no HTTP dep.
- DIAG-02: typed versioned envelope + six layer×exit-reason fixtures normalize.
- DIAG-03: merge-blocking allowlist (forbidden ⟂ allowed) + fail-closed sanitize round-trip proven.
- DIAG-04: support-truth + advisory doctor readiness present and non-overclaiming, asserted by proof.
</success_criteria>

<output>
Create `.planning/phases/65-diagnostic-export-seam-elixir/65-03-SUMMARY.md` when done.
</output>
