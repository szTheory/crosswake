---
phase: 65-diagnostic-export-seam-elixir
plan: 02
type: execute
wave: 2
depends_on: [65-01]
files_modified:
  - lib/crosswake/support_matrix/support_matrix.ex
  - lib/crosswake/doctor/doctor.ex
autonomous: true
requirements: [DIAG-04]
must_haves:
  truths:
    - "SupportMatrix.diagnostic_export_support_truth/0 returns a non-empty list mirroring @notification_support_truth shape"
    - "The truth entry sets delivery_supported: false and deferred: [:native_diagnostic_export, :metrickit_capture, :application_exit_info_capture]"
    - "The posture string separates shipped-contract / deferred-native-transport / host-owns-data-not-a-crash-reporting-service"
    - "mix crosswake.doctor emits one :advisory finding code \"diagnostic_export.contract_shipped\" that fires unconditionally"
    - "The doctor finding message does NOT overclaim (no 'crash-reporting service' implication in the message)"
  artifacts:
    - path: "lib/crosswake/support_matrix/support_matrix.ex"
      provides: "@diagnostic_export_support_truth attribute + diagnostic_export_support_truth/0 accessor"
      contains: "diagnostic_export_support_truth"
    - path: "lib/crosswake/doctor/doctor.ex"
      provides: "phase_65_diagnostic_export_findings/0 + run/1 pipeline wiring"
      contains: "diagnostic_export.contract_shipped"
  key_links:
    - from: "lib/crosswake/doctor/doctor.ex"
      to: "SupportMatrix.diagnostic_export_support_truth/0"
      via: "the doctor finding reads posture/deferred/authority_source from the truth entry"
      pattern: "diagnostic_export_support_truth"
---

<objective>
Add the diagnostics-export readiness truth and the unconditional advisory doctor finding — both additive, both non-overclaiming.

Purpose: Satisfies DIAG-04 — `mix crosswake.doctor` and `SupportMatrix` report diagnostics-export readiness while explicitly NOT implying a first-party crash-reporting service. Mirrors `@notification_support_truth` (D-16/D-17) and `phase_62_notification_findings` (D-18) exactly.
Output: additive edits to `lib/crosswake/support_matrix/support_matrix.ex` and `lib/crosswake/doctor/doctor.ex`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/65-diagnostic-export-seam-elixir/65-CONTEXT.md
@.planning/phases/65-diagnostic-export-seam-elixir/65-PATTERNS.md

<interfaces>
<!-- @notification_support_truth shape to mirror (support_matrix.ex lines 251-270 + accessor 404-405).
     Phase-65 entry diffs are in the action; spellings below are locked for the Plan 03 proof. -->

Add to SupportMatrix:
  @diagnostic_export_support_truth [ %{ ...mirror @notification_support_truth... } ]
  @spec diagnostic_export_support_truth() :: [map()]
  def diagnostic_export_support_truth, do: @diagnostic_export_support_truth

  Entry fields (D-16):
    surface:            "diagnostic export envelope contract"
    proof_class:        :merge_blocking
    action_class:       (mirror notification entry convention, e.g. "shell_native")
    docs_anchor:        (a guides anchor formatted like the existing entries)
    delivery_supported: false
    telemetry: %{
      status: :shipped,
      event_names: [],
      metadata_keys: Crosswake.Shell.DiagnosticExport.allowed_keys(),
      forbidden_metadata_keys: Crosswake.Shell.DiagnosticExport.forbidden_keys(),
      authority_source: :host_configured_endpoint,
      proof_class: :merge_blocking
    }
    deferred: [:native_diagnostic_export, :metrickit_capture, :application_exit_info_capture]
    posture:  (D-17 three-way string — see action)

Doctor finding (D-18):
  code "diagnostic_export.contract_shipped", severity :advisory, fires unconditionally (no manifest gate)
</interfaces>

# Self-analog + structural analog (read in full before editing)
@lib/crosswake/support_matrix/support_matrix.ex
@lib/crosswake/doctor/doctor.ex
@lib/crosswake/doctor/check.ex
@lib/crosswake/doctor/formatter.ex
@lib/crosswake/doctor/json_formatter.ex
@lib/crosswake/shell/diagnostic_export.ex
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add @diagnostic_export_support_truth + accessor to SupportMatrix</name>
  <files>lib/crosswake/support_matrix/support_matrix.ex</files>
  <read_first>
    - lib/crosswake/support_matrix/support_matrix.ex (the @notification_support_truth block lines 251-270 and notification_support_truth/0 accessor lines 404-405 — the exact shape to mirror)
    - lib/crosswake/shell/diagnostic_export.ex (source of allowed_keys/0 + forbidden_keys/0 referenced by the telemetry sub-map)
  </read_first>
  <behavior>
    - Test: `SupportMatrix.diagnostic_export_support_truth/0` returns a non-empty list of maps.
    - Test: the entry has `delivery_supported: false`.
    - Test: `entry.deferred` contains exactly `:native_diagnostic_export`, `:metrickit_capture`, `:application_exit_info_capture`.
    - Test: `entry.telemetry.authority_source == :host_configured_endpoint` and `entry.telemetry.proof_class == :merge_blocking`.
    - Test: `entry.proof_class == :merge_blocking`.
    - Test: `entry.posture` contains "not a crash-reporting service", "Phase 67", and "host".
  </behavior>
  <action>
    Add `@diagnostic_export_support_truth` immediately after the `@notification_support_truth` block, mirroring its shape exactly (D-16). Populate one entry with: `surface: "diagnostic export envelope contract"`; `proof_class: :merge_blocking`; `action_class:` consistent with the notification entry convention; `docs_anchor:` formatted like the existing `docs_anchor` strings (e.g. a capabilities/diagnostics guide anchor); `delivery_supported: false`; a `telemetry:` sub-map with `status: :shipped`, `event_names: []`, `metadata_keys: Crosswake.Shell.DiagnosticExport.allowed_keys()`, `forbidden_metadata_keys: Crosswake.Shell.DiagnosticExport.forbidden_keys()`, `authority_source: :host_configured_endpoint`, `proof_class: :merge_blocking`; `deferred: [:native_diagnostic_export, :metrickit_capture, :application_exit_info_capture]`; and a `posture:` string (D-17) drawing all three separations: "Diagnostics-export envelope and sanitize contract are shipped and merge-blocking allowlist proof is enforced; native MetricKit/ApplicationExitInfo transport is not shipped until Phase 67; the host owns the endpoint and the data — Crosswake is not a crash-reporting service." Add the `@spec diagnostic_export_support_truth() :: [map()]` + `def diagnostic_export_support_truth, do: @diagnostic_export_support_truth` accessor immediately after `notification_support_truth/0` (line 405). Do NOT wire it into `canonical/1` — it is a standalone projection used by doctor + proof.
  </action>
  <verify>
    <automated>mix compile --warnings-as-errors && mix test test/crosswake/support_matrix/ 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `mix compile --warnings-as-errors` passes (the telemetry sub-map references to `Crosswake.Shell.DiagnosticExport.allowed_keys/0` and `forbidden_keys/0` resolve).
    - `SupportMatrix.diagnostic_export_support_truth/0` returns a non-empty list; `List.first(...).delivery_supported == false`.
    - The entry's `deferred` list equals `[:native_diagnostic_export, :metrickit_capture, :application_exit_info_capture]`.
    - `entry.telemetry.authority_source == :host_configured_endpoint`; `entry.telemetry.forbidden_metadata_keys` equals `DiagnosticExport.forbidden_keys()`.
    - `entry.posture` contains the substring "not a crash-reporting service".
    - The accessor follows the exact `@spec [map()]` + single-expression style of `notification_support_truth/0`.
  </acceptance_criteria>
  <done>
    `@diagnostic_export_support_truth` + `diagnostic_export_support_truth/0` exist mirroring the notification truth shape; delivery_supported false; three deferred atoms; host_configured_endpoint authority; non-overclaiming posture; compiles clean.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add the unconditional :advisory doctor finding and wire it into run/1</name>
  <files>lib/crosswake/doctor/doctor.ex</files>
  <read_first>
    - lib/crosswake/doctor/doctor.ex (phase_62_notification_findings/1 structural model lines 800-844; the run/1 findings accumulation lines 149-170; the check/6 private helper lines 1702-1711)
    - lib/crosswake/doctor/check.ex (the Check struct + severity type :error | :warning | :advisory)
    - lib/crosswake/support_matrix/support_matrix.ex (the diagnostic_export_support_truth/0 accessor added in Task 1 — source of the finding's posture/deferred/authority detail)
    - lib/crosswake/doctor/formatter.ex and lib/crosswake/doctor/json_formatter.ex (confirm advisory findings render through the generic findings list — verify parity, do not add bespoke rendering unless an existing finding required it)
  </read_first>
  <behavior>
    - Test: `Crosswake.Doctor.run/1` report findings include exactly one finding with `code == "diagnostic_export.contract_shipped"`.
    - Test: that finding has `severity == :advisory`.
    - Test: the finding fires even when no manifest/notifications are present (unconditional) — run/1 with a minimal/empty opts still includes it.
    - Test: the finding `message` does NOT contain "crash-reporting service" (non-overclaim — the seam-drawing language lives in the SupportMatrix posture, not the doctor headline message).
    - Test: the finding `details` carries `delivery_supported: false`, the 3-atom `deferred` list, and `authority_source: :host_configured_endpoint`.
  </behavior>
  <action>
    Add a private `phase_65_diagnostic_export_findings/0` mirroring `phase_62_notification_findings/1` (D-18) but with NO manifest argument and NO conditional gate — it fires unconditionally because the contract is core. Read the truth via `SupportMatrix.diagnostic_export_support_truth() |> List.first(%{})` and its `:telemetry` sub-map. Emit ONE finding via the `check/6` helper: `check(:advisory, "diagnostic_export.contract_shipped", "diagnostic_export_posture", message, hint, details)`. The `message` describes that the diagnostics-export envelope + sanitize contract is shipped and the allowlist proof is merge-blocking — but MUST NOT contain "crash-reporting service" (keep that seam language in the SupportMatrix posture). The `hint` points the operator at the host-owned endpoint responsibility and the Phase 67 native-transport deferral. The `details` map carries `delivery_supported: false`, `deferred:` (the 3-atom list from the truth), `authority_source: :host_configured_endpoint`, `proof_class: :merge_blocking`, and `forbidden_metadata_keys:` from the telemetry sub-map. Wire it into `run/1`: add `phase_65_findings = phase_65_diagnostic_export_findings()` next to `phase_62_findings` and append `phase_65_findings` to the `findings` accumulation list (after `phase_62_findings`). Because it returns a list unconditionally and takes no manifest, it needs no nil-guard clause. Confirm `Formatter` and `JSONFormatter` render it through the generic findings path (advisory severity already supported — `format_evidence_tier`/posture lines show advisory findings flow through existing rendering); only add formatter code if an existing finding of the same severity required bespoke rendering.
  </action>
  <verify>
    <automated>mix compile --warnings-as-errors && mix test test/crosswake/doctor/ 2>&1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - `Crosswake.Doctor.run/1` (with empty/minimal opts) returns a report whose findings include exactly one `code == "diagnostic_export.contract_shipped"`.
    - That finding's `severity == :advisory`.
    - The finding fires unconditionally (present even with no manifest / no notification routes).
    - The finding `message` does NOT contain the substring "crash-reporting service".
    - The finding `details` includes `delivery_supported: false`, the 3-atom `deferred` list, and `authority_source: :host_configured_endpoint`.
    - `mix test test/crosswake/doctor/` stays green (Formatter + JSONFormatter render the advisory finding without errors).
  </acceptance_criteria>
  <done>
    `phase_65_diagnostic_export_findings/0` exists, fires unconditionally, emits one `:advisory` `"diagnostic_export.contract_shipped"` finding with non-overclaiming message + host-owned details; wired into run/1; doctor tests green.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Crosswake readiness reporting → operator | The operator reads SupportMatrix truth + `mix crosswake.doctor` output and must form an accurate mental model of what ships vs. what is deferred. The risk here is misrepresentation, not data leakage. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-65-05 | Spoofing (misrepresentation) | SupportMatrix @diagnostic_export_support_truth | mitigate | `delivery_supported: false`, `authority_source: :host_configured_endpoint`, all 3 deferred atoms, and the posture's "not a crash-reporting service" clause prevent overclaiming a first-party reporter (D-16/D-17). Proof lane (Plan 03) asserts these. |
| T-65-06 | Spoofing (misrepresentation) | Doctor "diagnostic_export.contract_shipped" finding | mitigate | Severity `:advisory` (informational, not a remediation demand) and a message that excludes "crash-reporting service" keep the doctor headline honest (D-18); the seam-drawing language stays in the SupportMatrix posture. |
| T-65-SC | Tampering | hex/npm/pip installs | mitigate | This plan adds NO dependency. No package-install task exists; no-HTTP-dep is enforced by the Plan 03 proof lane. |
</threat_model>

<verification>
- `mix compile --warnings-as-errors` clean.
- `SupportMatrix.diagnostic_export_support_truth/0` mirrors the notification truth shape; delivery_supported false; 3 deferred atoms; host_configured_endpoint authority; non-overclaim posture.
- `Doctor.run/1` includes exactly one `:advisory` `"diagnostic_export.contract_shipped"` finding firing unconditionally, message free of "crash-reporting service".
- `mix test test/crosswake/support_matrix/` and `test/crosswake/doctor/` green.
</verification>

<success_criteria>
- DIAG-04: SupportMatrix + doctor report diagnostics-export readiness without implying a first-party crash-reporting service (delivery_supported false, deferred native transport, host-owns-data posture, advisory finding).
</success_criteria>

<output>
Create `.planning/phases/65-diagnostic-export-seam-elixir/65-02-SUMMARY.md` when done.
</output>
