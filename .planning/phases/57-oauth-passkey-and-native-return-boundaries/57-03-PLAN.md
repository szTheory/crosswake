---
phase: 57-oauth-passkey-and-native-return-boundaries
plan: "03"
type: execute
wave: 3
depends_on: ["57-02"]
files_modified:
  - examples/phoenix_host/lib/crosswake_example/saas_portal/auth_return_attempt.ex
  - examples/phoenix_host/lib/crosswake_example/saas_portal/auth_return_audit_event.ex
  - examples/phoenix_host/priv/repo/migrations/20260602080000_create_sigra_auth_return_attempts.exs
  - examples/phoenix_host/priv/repo/migrations/20260602080100_create_sigra_auth_return_audit_events.exs
  - test/crosswake/proof/phase57_auth_return_boundaries_test.exs
autonomous: true
requirements: [RETN-02, RETN-03]
---

# Plan 57-03: Host-Owned Attempt, Audit, And Promotion Proof

<objective>
Prove the host-owned server record boundary for auth returns: replay, expiry, binding, audit, and backend session authority projection are owned by the Phoenix host, not by OAuth callbacks, passkey assertions, native links, shell bridge events, or Crosswake envelopes.
Decision coverage: D-11, D-14, D-15, D-16, D-17, D-18, D-19, D-39, D-40, D-42.
</objective>

<context>
Phase 57 should follow the Phase 55 handoff and Phase 56 step-up pattern. Core Crosswake exposes pure contracts; the example host demonstrates Ecto-shaped attempt and audit records. Work with the already-dirty example-host files and avoid adding provider SDKs, native auth UI, refresh-token helpers, or real device proof.
</context>

<tasks>
  <task id="57-03-01" type="execute">
    <name>Lock example-host attempt and audit persistence shape</name>
    <read_first>
      AGENTS.md
      .planning/phases/57-oauth-passkey-and-native-return-boundaries/57-CONTEXT.md
      .planning/phases/57-oauth-passkey-and-native-return-boundaries/57-RESEARCH.md
      .planning/phases/57-oauth-passkey-and-native-return-boundaries/57-PATTERNS.md
      .planning/phases/55-session-handoff-tickets-and-authority-projection/55-CONTEXT.md
      .planning/phases/56-step-up-intent-and-plug-liveview-ceremony/56-CONTEXT.md
      lib/crosswake/companions/sigra/auth_return.ex
      examples/phoenix_host/lib/crosswake_example/saas_portal/auth_return_attempt.ex
      examples/phoenix_host/lib/crosswake_example/saas_portal/auth_return_audit_event.ex
      examples/phoenix_host/priv/repo/migrations/20260602080000_create_sigra_auth_return_attempts.exs
      examples/phoenix_host/priv/repo/migrations/20260602080100_create_sigra_auth_return_audit_events.exs
      test/crosswake/proof/phase57_auth_return_boundaries_test.exs
    </read_first>
    <action>Complete the example-host `AuthReturnAttempt` and `AuthReturnAuditEvent` schemas and migrations as host-owned proof artifacts. Attempt records must include attempt ref/digest, kind, lifecycle state, subject/org/source-session refs, expected session version, route id, return route id, transport, link verification posture, state/nonce/PKCE/challenge digests or validation refs, expected callback, provider audience or issuer refs where safe, issued/expires timestamps, consumed/revoked timestamps, audit correlation ref, return params, projected session ref/version, and projected authority map. Audit rows must be append-only by shape, with event id uniqueness and fields for event type, outcome, denial code, route/kind/transport, request ref, binding result, projection facts, and safe metadata. Migrations must add unique indexes for refs/digests/event ids and indexes for lifecycle state, expiry, route ids, source session, and audit correlation.</action>
    <verify>
      <automated>mix test test/crosswake/proof/phase57_auth_return_boundaries_test.exs --trace</automated>
      <automated>cd examples/phoenix_host && mix compile --warnings-as-errors</automated>
    </verify>
    <acceptance_criteria>
      - `AuthReturnAttempt` defines persisted fields for `attempt_ref`, `attempt_digest`, `kind`, `state`, `route_id`, `return_route_id`, `transport`, `link_verification`, `expected_callback`, `expires_at`, `consumed_at`, `revoked_at`, `audit_correlation_ref`, `projected_session_ref`, `projected_session_version`, and `projected_authority`.
      - `AuthReturnAuditEvent` defines persisted fields for `event_id`, `event_type`, `outcome`, `denial_code`, `route_id`, `return_route_id`, `kind`, `transport`, `request_ref`, `binding_result`, and `metadata`.
      - The attempt migration creates unique indexes for `attempt_ref` and `attempt_digest`.
      - The audit migration creates a unique index for `event_id`.
      - The proof asserts these schemas and migrations exist and expose replay, expiry, lifecycle, binding, projection, and audit fields.
    </acceptance_criteria>
  </task>

  <task id="57-03-02" type="execute">
    <name>Prove backend promotion requirements without provider or shell authority</name>
    <read_first>
      .planning/phases/57-oauth-passkey-and-native-return-boundaries/57-CONTEXT.md
      .planning/phases/57-oauth-passkey-and-native-return-boundaries/57-RESEARCH.md
      .planning/phases/57-oauth-passkey-and-native-return-boundaries/57-PATTERNS.md
      lib/crosswake/companions/sigra/auth_return.ex
      lib/crosswake/companions/sigra/contracts.ex
      lib/crosswake/companions/sigra/handoff.ex
      examples/phoenix_host/lib/crosswake_example/saas_portal/auth_return_attempt.ex
      examples/phoenix_host/lib/crosswake_example/saas_portal/auth_return_audit_event.ex
      test/crosswake/proof/phase57_auth_return_boundaries_test.exs
    </read_first>
    <action>Add or complete proof assertions and any minimal pure/example helper needed to demonstrate the host-owned promotion transaction shape. Promotion must be described or implemented as one atomic host transaction: fetch by attempt ref/digest, compare request/envelope/attempt/provider-verifier facts, conditionally consume a row where state is issued, `consumed_at` and `revoked_at` are nil, and `expires_at` is greater than evaluated time; append audit evidence; project a fresh `SessionAuthorityLane`; and return explicit renewal instructions. Shell bridge events and native deep-link delivery must be modeled as evidence inputs only. Do not add provider token exchange, WebAuthn cryptographic verification, native SDK code, bridge event authority, or core Plug.Conn mutation.</action>
    <verify>
      <automated>mix test test/crosswake/proof/phase57_auth_return_boundaries_test.exs --trace</automated>
      <automated>cd examples/phoenix_host && mix compile --warnings-as-errors</automated>
    </verify>
    <acceptance_criteria>
      - The proof asserts OAuth callback evidence alone cannot construct `AuthReturn.Completion`.
      - The proof asserts passkey assertion evidence alone cannot construct `AuthReturn.Completion`.
      - The proof asserts native deep-link or bridge-event evidence alone cannot construct `AuthReturn.Completion`.
      - The proof asserts completion requires a fresh `%Crosswake.Companions.Sigra.Contracts.SessionAuthorityLane{}` and explicit session renewal instructions.
      - The proof asserts replayed, expired, revoked, route-mismatched, return-route-mismatched, callback-mismatched, and link-unverified attempt postures deny before authority projection.
      - No core file in this plan imports `Plug.Conn`, `Ecto.Repo`, provider SDK modules, WebAuthn SDK modules, or native shell modules.
    </acceptance_criteria>
  </task>
</tasks>

<threat_model>
| ID | STRIDE | Asset | Threat | Mitigation |
|----|--------|-------|--------|------------|
| T-57-09 | Replay | Host attempt row | The same return attempt is consumed twice or after expiry/revocation. | Host-owned conditional consume on state, timestamps, and affected-row count. |
| T-57-10 | Tampering | Binding facts | Evidence is promoted against the wrong route, return route, callback, session version, or transport. | Compare validation request, envelope, attempt row, and verifier facts before consume. |
| T-57-11 | Repudiation | Audit trail | Promotion or denial has no durable audit evidence. | Append audit event in the same host-owned transaction shape. |
| T-57-12 | Elevation of Privilege | Shell/native evidence | Bridge event, native link delivery, OAuth callback, or passkey assertion directly mutates authority. | Treat all client/shell/provider returns as evidence until backend projection produces `SessionAuthorityLane`. |
</threat_model>

<verification>
Run `mix test test/crosswake/proof/phase57_auth_return_boundaries_test.exs --trace`, then `cd examples/phoenix_host && mix compile --warnings-as-errors`.
</verification>

<success_criteria>
- Host-owned attempt records are the replay, expiry, revocation, binding, audit, and authority-promotion source of truth.
- Promotion requires backend validation plus session authority projection and explicit renewal instructions.
- OAuth callbacks, passkey assertions, native deep links, and bridge events remain evidence-only.
</success_criteria>

<must_haves>
- Keep offline and native claims honest: no device/provider proof promotion in Phase 57.
- Keep session mutation at the Phoenix host boundary.
- Preserve a typed adapter seam without provider-specific core templates or shell authority.
- D-11: covered by host-owned attempt boundary proof.
- D-14: covered by attempt and audit schema shape.
- D-15: covered by lifecycle-state and expiry enforcement proof.
- D-16: covered by locator-only return artifact posture.
- D-17: covered by atomic consume, audit, projection, and renewal proof.
- D-18: covered by idiomatic Phoenix/Ecto example-host ownership.
- D-19: covered by evidence-only callback/deep-link/passkey proof.
- D-39: covered by host-boundary session renewal instructions.
- D-40: covered by transaction-shaped consume/audit/projection requirements.
- D-42: covered by passkey evidence remaining server-validated evidence only.
</must_haves>
