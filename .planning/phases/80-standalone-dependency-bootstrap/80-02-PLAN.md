---
phase: 80-standalone-dependency-bootstrap
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - examples/ios_shell_host/CrosswakeShell.xcodeproj/project.pbxproj
  - examples/ios_shell_host/CrosswakeShell/CrosswakeShellApp.swift
  - examples/ios_shell_host/CrosswakeShell/ActivationCoordinator.swift
  - examples/ios_shell_host/CrosswakeShell/BridgeChannel.swift
autonomous: true
requirements: [SETUP-01, SETUP-02]

must_haves:
  truths:
    - "iOS host app uses the standalone Crosswake SPM dependency"
    - "Generated shell code is removed from the iOS host"
    - "iOS host leverages modern UI (SwiftUI)"
  artifacts:
    - path: "examples/ios_shell_host/CrosswakeShell.xcodeproj/project.pbxproj"
      provides: "SPM dependency resolution"
      contains: "crosswake-shell-core-ios"
  key_links:
    - from: "examples/ios_shell_host/CrosswakeShell.xcodeproj/project.pbxproj"
      to: "GitHub"
      via: "SPM dependency"
      pattern: "crosswake-shell-core-ios"
---

<objective>
Update the iOS demo host to use standalone Crosswake dependencies instead of locally generated sources, and ensure the host leverages SwiftUI.

Purpose: To provide adoption evidence that standard iOS users can integrate the Crosswake `crosswake-shell-core-ios` SPM package effortlessly.
Output: Refactored iOS project relying purely on the SPM package.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/80-standalone-dependency-bootstrap/80-CONTEXT.md
@.planning/phases/80-standalone-dependency-bootstrap/80-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add SPM Dependency and Scrub Local Sources</name>
  <files>examples/ios_shell_host/CrosswakeShell.xcodeproj/project.pbxproj, examples/ios_shell_host/CrosswakeShell/ActivationCoordinator.swift, examples/ios_shell_host/CrosswakeShell/BridgeChannel.swift</files>
  <action>
    Per D-01 and D-02, update `project.pbxproj` to link the SPM remote package `https://github.com/crosswake/crosswake-shell-core-ios.git` version `0.1.0`. You may adapt `fix_pbxproj.py` to cleanly inject the SPM package instead of local files.
    Per D-03, remove `ActivationCoordinator.swift` and `BridgeChannel.swift` from `examples/ios_shell_host/CrosswakeShell/` and scrub their references from `project.pbxproj`. The native host must not contain generated boilerplate.
  </action>
  <verify>
    <automated>grep -i "crosswake-shell-core-ios" examples/ios_shell_host/CrosswakeShell.xcodeproj/project.pbxproj && test ! -f examples/ios_shell_host/CrosswakeShell/ActivationCoordinator.swift && test ! -f examples/ios_shell_host/CrosswakeShell/BridgeChannel.swift</automated>
  </verify>
  <done>SPM dependency added and old local sources are removed.</done>
</task>

<task type="auto">
  <name>Task 2: Refactor Host App to SwiftUI</name>
  <files>examples/ios_shell_host/CrosswakeShell/CrosswakeShellApp.swift</files>
  <action>
    Per D-04, ensure `CrosswakeShellApp.swift` (and any associated primary view) leverages SwiftUI as the modern native UI stack. Refactor away from UIKit wrappers if possible, directly instantiating and rendering the standalone `CrosswakeShell` dependency using SwiftUI.
  </action>
  <verify>
    <automated>grep -i "SwiftUI" examples/ios_shell_host/CrosswakeShell/CrosswakeShellApp.swift && xcodebuild -project examples/ios_shell_host/CrosswakeShell.xcodeproj build</automated>
  </verify>
  <done>iOS Host app updated to use SwiftUI and the standalone dependency.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Native App -> Dependency Registry | Fetching iOS library from GitHub SPM |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-80-02-1 | Tampering | project.pbxproj | accept | Trusting official SPM repository for `crosswake-shell-core-ios`. |
| T-80-02-SC | Tampering | npm/pip/cargo/spm installs | mitigate | Verified `[ASSUMED]` via research, using standard dependency resolution. |
</threat_model>

<verification>
- `project.pbxproj` contains SPM configuration for `crosswake-shell-core-ios`.
- `ActivationCoordinator.swift` and `BridgeChannel.swift` do not exist.
- `CrosswakeShellApp.swift` explicitly utilizes SwiftUI per modern standards.
</verification>

<success_criteria>
iOS demo project updated successfully and cleanly relies on `crosswake-shell-core-ios` via SPM without any generated boilerplate.
</success_criteria>

<output>
Create `.planning/phases/80-standalone-dependency-bootstrap/80-02-SUMMARY.md` when done
</output>