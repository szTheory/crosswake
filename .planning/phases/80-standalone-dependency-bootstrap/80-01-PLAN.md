---
phase: 80-standalone-dependency-bootstrap
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - examples/android_shell_host/app/build.gradle
  - examples/android_shell_host/app/src/main/java/dev/crosswake/shell/MainActivity.kt
  - examples/android_shell_host/app/src/main/java/dev/crosswake/shell/ActivationCoordinator.kt
  - examples/android_shell_host/app/src/main/java/dev/crosswake/shell/BridgeChannel.kt
autonomous: true
requirements: [SETUP-01, SETUP-02]

must_haves:
  truths:
    - "Android host app uses the standalone Crosswake Maven dependency"
    - "Generated shell code is removed from the Android host"
    - "Android host leverages modern UI (Jetpack Compose)"
  artifacts:
    - path: "examples/android_shell_host/app/build.gradle"
      provides: "Maven dependency resolution"
      contains: "dev.crosswake:shell-core-android:0.1.0"
  key_links:
    - from: "examples/android_shell_host/app/build.gradle"
      to: "Maven Central"
      via: "Gradle dependency"
      pattern: "shell-core-android"
---

<objective>
Update the Android demo host to use standalone Crosswake dependencies instead of locally generated sources, while modernizing its UI.

Purpose: To provide adoption evidence that standard Android users can integrate the Crosswake `shell-core-android` library effectively.
Output: Refactored Android project relying purely on the Maven package.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/80-standalone-dependency-bootstrap/80-CONTEXT.md
@.planning/phases/80-standalone-dependency-bootstrap/80-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Maven Dependency and Scrub Local Sources</name>
  <files>examples/android_shell_host/app/build.gradle, examples/android_shell_host/app/src/main/java/dev/crosswake/shell/ActivationCoordinator.kt, examples/android_shell_host/app/src/main/java/dev/crosswake/shell/BridgeChannel.kt</files>
  <action>
    Per D-01 and D-02, update the `build.gradle` file in `examples/android_shell_host/app` to include the standalone dependency `dev.crosswake:shell-core-android:0.1.0`. Ensure we use the published packages instead of local files.
    Per D-03, use a shell command to delete the locally generated `ActivationCoordinator.kt` and `BridgeChannel.kt` from `examples/android_shell_host/app/src/main/java/dev/crosswake/shell/`. The native host must not contain any generated boilerplate code.
  </action>
  <verify>
    <automated>grep "shell-core-android" examples/android_shell_host/app/build.gradle && test ! -f examples/android_shell_host/app/src/main/java/dev/crosswake/shell/ActivationCoordinator.kt && test ! -f examples/android_shell_host/app/src/main/java/dev/crosswake/shell/BridgeChannel.kt</automated>
  </verify>
  <done>Maven dependency added and old local sources are deleted.</done>
</task>

<task type="auto">
  <name>Task 2: Refactor MainActivity to Jetpack Compose</name>
  <files>examples/android_shell_host/app/src/main/java/dev/crosswake/shell/MainActivity.kt</files>
  <action>
    Per D-04, rewrite `MainActivity.kt` to leverage modern native UI stacks, specifically Jetpack Compose. Replace the older XML/View-based boilerplate with a modern `setContent { ... }` block that instantiates the standalone `CrosswakeShell` dependency and renders its content. The app must launch successfully with the new UI stack.
  </action>
  <verify>
    <automated>grep "setContent" examples/android_shell_host/app/src/main/java/dev/crosswake/shell/MainActivity.kt</automated>
  </verify>
  <done>MainActivity modernized to Jetpack Compose and uses the standalone shell.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Native App -> Dependency Registry | Fetching Android library from Maven Central |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-80-01-1 | Tampering | build.gradle | accept | Trusting official Maven Central registry for `dev.crosswake:shell-core-android` package. |
| T-80-01-SC | Tampering | npm/pip/cargo/maven installs | mitigate | Verified `[ASSUMED]` via research, using standard dependency resolution. |
</threat_model>

<verification>
- `examples/android_shell_host/app/build.gradle` has Maven dependency.
- `ActivationCoordinator.kt` and `BridgeChannel.kt` do not exist.
- `MainActivity.kt` uses Jetpack Compose.
</verification>

<success_criteria>
Android demo project updated successfully and cleanly relies on `shell-core-android` without any generated boilerplate.
</success_criteria>

<output>
Create `.planning/phases/80-standalone-dependency-bootstrap/80-01-SUMMARY.md` when done
</output>