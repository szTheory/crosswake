---
phase: 138-crosswake-chimeway-extraction
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/crosswake_chimeway/mix.exs
  - packages/crosswake_chimeway/config/config.exs
  - packages/crosswake_chimeway/README.md
  - packages/crosswake_chimeway/CHANGELOG.md
  - packages/crosswake_chimeway/LICENSE
  - packages/crosswake_chimeway/test/test_helper.exs
  - packages/crosswake_chimeway/test/support/study_session_live.ex
  - packages/crosswake_chimeway/lib/crosswake/companions/chimeway.ex
  - packages/crosswake_chimeway/lib/crosswake/companions/chimeway/
  - lib/crosswake/companions/chimeway.ex
  - lib/crosswake/companions/chimeway/
  - test/support/stub_companion.ex
  - mix.exs
autonomous: true
requirements: [CHIME-01, CHIME-02]
must_haves:
  truths:
    - "all Crosswake.Companions.Chimeway.* sub-modules (6) + facade resolve from packages/crosswake_chimeway/ with the namespace preserved (non-breaking) — SC#1"
    - "packages/crosswake_chimeway compiles --warnings-as-errors and its mix.exs lists {:crosswake, ...} as its ONLY Crosswake dep — no crosswake_sigra entry (CHIME-02, SC#2)"
    - "no Crosswake.Companions.Chimeway reference remains anywhere in core lib/"
    - "the NotificationOpenEvidence auth_context field stays typed map() and carries a moduledoc guard note forbidding a tighten to any Sigra AuthContext type (CHIME-02, SC#2)"
    - "StubChimewayAbsentCompanion exists beside the rulestead/rindle/sigra absent stubs and does NOT implement auth_authority?/0 (chimeway is notification-only)"
  artifacts:
    - "packages/crosswake_chimeway/ — mix.exs (@version 0.1.0, no engine dep, no crosswake_sigra dep), config/config.exs, README, CHANGELOG, LICENSE, test/test_helper.exs, test/support/study_session_live.ex"
    - "packages/crosswake_chimeway/lib/crosswake/companions/chimeway.ex + chimeway/{contracts,denial_codes,intent_consumer,redaction,resolver,telemetry}.ex"
    - "test/support/stub_companion.ex — Crosswake.TestSupport.StubChimewayAbsentCompanion (no auth_authority?/0)"
  key_links:
    - "core mix.exs :companions env — Crosswake.Companions.Chimeway removed; core no longer compiles the facade"
    - "resolver.ex alias Crosswake.Shell.Denial — KEEP; Denial is a core type the package depends on (no Finding-boundary refactor, unlike sigra D-137-A)"
    - "NotificationOpenEvidence.auth_context map() guard note — the single defense against inter-companion dep creep (D-8)"
---

<objective>
Perform the mechanical chimeway extraction (CHIME-01) and lock the no-sigra-dep invariant at the
source level (CHIME-02). Create the `packages/crosswake_chimeway/` skeleton by cloning the shipped
`crosswake_sigra` package block (dropping nothing — chimeway, like sigra, has no engine dep), MOVE
all `Crosswake.Companions.Chimeway.*` source (6 sub-modules + facade, namespace preserved), remove
chimeway from core (`lib/` files + the `:companions` env entry), add the `auth_context: map()`
moduledoc guard note to `NotificationOpenEvidence`, and add `StubChimewayAbsentCompanion` beside the
existing absent stubs. Core is wired to depend on `crosswake_chimeway` via a `path:` dep so the
dress rehearsal (Plan 02) exercises the package.

This is the direct mechanical analog of Phase 137 Plan 03, MINUS the Finding-boundary refactor:
chimeway's `Resolver` returns `Crosswake.Shell.Denial` directly (a core type the package legitimately
depends on), so there is NO D-138-A equivalent to sigra's D-137-A — the `alias Crosswake.Shell.Denial`
in resolver.ex STAYS. StubChimewayAbsentCompanion copies StubSigraAbsentCompanion but OMITS
`auth_authority?/0` (chimeway is a notification companion, not an auth authority).

Purpose: This is the irreversible-adjacent structural move. Getting the namespace preservation and
the no-sigra-dep invariant right at the source is what makes the clean-room proof (Plan 02) and the
publish gate (Plans 03-04) trustworthy.
Output: standalone crosswake_chimeway package (compiles green), core with chimeway removed (compiles
green), StubChimewayAbsentCompanion in place, path-dep wired for the dress rehearsal.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/138-crosswake-chimeway-extraction/138-RESEARCH.md
@.planning/phases/138-crosswake-chimeway-extraction/138-PATTERNS.md
@.planning/phases/138-crosswake-chimeway-extraction/138-VALIDATION.md
@script/extract_companion.md
@packages/crosswake_sigra/mix.exs
</context>

<artifacts_this_phase_produces>
This plan (01) produces:
- `packages/crosswake_chimeway/` full skeleton: `mix.exs` (@version "0.1.0" # x-release-please-version, no engine dep, no crosswake_sigra dep), `config/config.exs`, `README.md`, `CHANGELOG.md`, `LICENSE`, `test/test_helper.exs`, `test/support/study_session_live.ex`
- moved source: `lib/crosswake/companions/chimeway.ex` + `lib/crosswake/companions/chimeway/{contracts,denial_codes,intent_consumer,redaction,resolver,telemetry}.ex`
- `NotificationOpenEvidence` moduledoc/@doc guard note pinning `auth_context: map()`
- `Crosswake.TestSupport.StubChimewayAbsentCompanion` in `test/support/stub_companion.ex` (no auth_authority?/0)
- core changes: `Crosswake.Companions.Chimeway` removed from `mix.exs` `:companions` env; facade + source deleted from core `lib/`; core `mix.exs` gains the env-conditional `{:crosswake_chimeway, path: "packages/crosswake_chimeway"}` dress-rehearsal dep
(Plan 02 moves/splits the tests + adds the clean-room proof; Plan 03 wires CI + release-please; Plan 04 is the human publish gate.)
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold packages/crosswake_chimeway/, move source, add the auth_context map() guard note, remove chimeway from core</name>
  <files>packages/crosswake_chimeway/mix.exs, packages/crosswake_chimeway/config/config.exs, packages/crosswake_chimeway/README.md, packages/crosswake_chimeway/CHANGELOG.md, packages/crosswake_chimeway/LICENSE, packages/crosswake_chimeway/test/test_helper.exs, packages/crosswake_chimeway/test/support/study_session_live.ex, packages/crosswake_chimeway/lib/crosswake/companions/, lib/crosswake/companions/chimeway.ex, lib/crosswake/companions/chimeway/, mix.exs</files>
  <read_first>
    - packages/crosswake_sigra/mix.exs (full — the shipped no-engine package template: @version, elixirc_paths with NO engine_present branch, deps/0 = single crosswake_dep/0, package/0, description, files:)
    - packages/crosswake_sigra/test/test_helper.exs (ExUnit.start exclude tags), packages/crosswake_sigra/config/config.exs, packages/crosswake_sigra/README.md / CHANGELOG.md / LICENSE shape
    - packages/crosswake_sigra/test/support/study_session_live.ex (copy verbatim — needed by phase71 in Plan 02)
    - 138-PATTERNS.md § packages/crosswake_chimeway/mix.exs (the exact chimeway substitutions) + § test_helper + § study_session_live
    - 138-RESEARCH.md § Recommended Package Structure, § Pattern 1 (mix.exs), § No Engine Optional Dep, § Source Files to Move
    - mix.exs (core) — the `env: [companions: [Crosswake.Companions.Chimeway]]` line at ~L28-31 and the "Phase-138 extraction: remove ... when extracted" comment
    - lib/crosswake/companions/chimeway.ex + lib/crosswake/companions/chimeway/*.ex (the 6 sub-modules + facade to move)
    - lib/crosswake/companions/chimeway/contracts.ex L269-279 (the NotificationOpenEvidence struct with auth_context: map() at ~L275) — where the guard note goes
    - lib/crosswake/companions/chimeway/resolver.ex L13 (alias Crosswake.Shell.Denial — KEEP), L99-111 (Denial.new in deny_no_route/3 + deny/2 — KEEP, no refactor)
    - script/extract_companion.md Steps 1, 3-5 (source move + mix.exs)
  </read_first>
  <action>
    Create `packages/crosswake_chimeway/mix.exs` by cloning the shipped `packages/crosswake_sigra/mix.exs` with `s/sigra/chimeway/g`, `s/Sigra/Chimeway/g`: `@version "0.1.0" # x-release-please-version` (first-publish one-shot, independent of core 0.1.2), `app: :crosswake_chimeway`, `name: "crosswake_chimeway"`, `elixir: "~> 1.19"`, `elixirc_paths(:test) -> ["lib", "test/support"]` / `elixirc_paths(_) -> ["lib"]` (NO engine_present branch — chimeway has no engine), `deps/0` returning ONLY the env-conditional `crosswake_dep/0` (`{:crosswake, "~> 0.1"}` when `System.get_env("CROSSWAKE_RELEASE") == "1"`, else `{:crosswake, path: "../.."}`) — there is NO optional engine dep and NO crosswake_sigra dep. Set `description` to "Chimeway notification companion adapter for the Crosswake route-policy system." `package/0`: `licenses: ["Apache-2.0"]`, the standard links map (Changelog/Documentation/GitHub for crosswake_chimeway), and `files: ~w(lib mix.exs README.md LICENSE CHANGELOG.md)` (test/ excluded from the tarball — this is why a later test-only sigra dep never ships).
    Create `config/config.exs` (minimal — chimeway reads no flag source), `README.md`, `CHANGELOG.md` (clean [Unreleased] skeleton — fresh 0.1.0 companion, NOT core-inherited history), `LICENSE` (Apache-2.0, clone sigra's), and `test/test_helper.exs` with `ExUnit.start(exclude: [:requires_example_host, :advisory_only])`. Copy `test/support/study_session_live.ex` verbatim from `packages/crosswake_sigra/test/support/`.
    MOVE the 6 sub-modules + the facade from `lib/crosswake/companions/chimeway{.ex,/}` into `packages/crosswake_chimeway/lib/crosswake/companions/` preserving the `Crosswake.Companions.Chimeway.*` module names (namespace-preserving = non-breaking). The moved `resolver.ex` KEEPS its `alias Crosswake.Shell.Denial` and its `Denial.new(...)` calls unchanged — Denial is a core type the package depends on via `{:crosswake, ...}` (this is the deliberate divergence from sigra: NO Finding-boundary refactor). Delete the chimeway files from core `lib/`.
    In the moved `packages/crosswake_chimeway/lib/crosswake/companions/chimeway/contracts.ex`, add a moduledoc (or a `@doc` note on the `NotificationOpenEvidence` sub-module) stating that `auth_context` is intentionally typed `map()` and must not be tightened to any Sigra AuthContext type, because doing so would create an inter-companion dependency and break the CHIME-02 no-sigra-dep invariant (D-8). Do NOT reference a literal that a downstream negative-grep for `crosswake_sigra` would trip — phrase the note by concept (say "the sigra auth-context struct"), not by writing the package atom.
    In core `mix.exs`: remove `Crosswake.Companions.Chimeway` from the `:companions` env list (it becomes `env: []` if the key must remain, or drop the `env:` entry entirely if empty is acceptable — match whatever shape core currently requires), remove the Phase-138 extraction comment, and add an env-conditional dress-rehearsal dep `{:crosswake_chimeway, path: "packages/crosswake_chimeway"}` added when NOT `CROSSWAKE_RELEASE=1` (mirror exactly how core already wires the crosswake_sigra / crosswake_rindle path deps for their dress rehearsals — verify the exact core-side conditional against the sigra wiring).
    Run `cd packages/crosswake_chimeway && mix deps.get`.
  </action>
  <verify>
    <automated>cd packages/crosswake_chimeway && mix deps.get && mix compile --warnings-as-errors</automated>
    <automated>grep -rn "Crosswake.Companions.Chimeway" lib/ 2>/dev/null; test "$(grep -rl "Crosswake.Companions.Chimeway" lib/ 2>/dev/null | wc -l | tr -d ' ')" = "0"</automated>
    <automated>grep -rn "crosswake_sigra" packages/crosswake_chimeway/mix.exs; test "$(grep -c "crosswake_sigra" packages/crosswake_chimeway/mix.exs)" = "0"</automated>
  </verify>
  <acceptance_criteria>
    - packages/crosswake_chimeway compiles --warnings-as-errors with all 6 sub-modules + facade under the preserved Crosswake.Companions.Chimeway.* namespace
    - mix.exs has @version "0.1.0", no engine dep, no engine_present elixirc_paths branch, and NO crosswake_sigra dependency
    - NotificationOpenEvidence carries the auth_context: map() guard note
    - zero Crosswake.Companions.Chimeway references remain in core lib/
    - Crosswake.Companions.Chimeway removed from core :companions env; core mix.exs has the crosswake_chimeway path-dep dress-rehearsal wiring
    - resolver.ex still uses Crosswake.Shell.Denial (no Finding-boundary refactor performed)
  </acceptance_criteria>
  <done>The standalone crosswake_chimeway package exists and compiles with the namespace preserved and no sigra dep; chimeway source is gone from core; core is wired to the package via path dep for the dress rehearsal.</done>
</task>

<task type="auto">
  <name>Task 2: Add StubChimewayAbsentCompanion (no auth_authority?/0) beside the existing absent stubs</name>
  <files>test/support/stub_companion.ex</files>
  <read_first>
    - test/support/stub_companion.ex — the existing StubRulesteadAbsentCompanion / StubRindleAbsentCompanion / StubSigraAbsentCompanion (full file, to mirror module shape, @behaviour, report_state/0 State struct); note StubSigraAbsentCompanion's `@impl true def auth_authority?, do: false` — chimeway MUST NOT copy that
    - lib/crosswake/companion.ex (the @behaviour callbacks incl the OPTIONAL auth_authority?/0)
    - 138-RESEARCH.md § Code Examples — StubChimewayAbsentCompanion (the drafted module) + § Pitfall 5 (no auth_authority?/0)
    - 138-PATTERNS.md § test/support/stub_companion.ex (the exact substitutions + the CRITICAL OMISSION note)
  </read_first>
  <action>
    Add `Crosswake.TestSupport.StubChimewayAbsentCompanion` to `test/support/stub_companion.ex`, beside the existing absent stubs, mirroring StubSigraAbsentCompanion structurally with `s/:sigra/:chimeway/`, `s/Sigra/Chimeway/`: `companion_id/0` returns `:chimeway`; `enabled?/1` reads `Map.get(config, :enabled, false)` (the absent-stub models a disabled default — this matches the sigra stub and is CORRECT for the stub even though the real chimeway facade defaults enabled? to true); `route_gated?/2` returns `:pass`; `kill_switch_active?/1` returns false; `validate_dependency/0` returns `{:error, [Crosswake.Companions.Chimeway]}` to model chimeway absent from core deps post-extraction; `report_state/0` returns a `Crosswake.Companion.State` with `dependency_status: {:missing, [Crosswake.Companions.Chimeway]}` and `:unconfigured` gate/kill-switch, matching the other absent stubs.
    CRITICAL OMISSION: do NOT add an `auth_authority?/0` callback — chimeway is NOT an auth companion. Also do NOT add `forbidden_metadata_keys/0` or `telemetry_events/0` to the stub (when chimeway is absent, core's telemetry aggregation correctly gets zero contribution).
    Add a moduledoc noting the stub models the post-Phase-138 extracted state where `Crosswake.Companions.Chimeway` is not in core deps, and that auth_authority?/0 is intentionally absent (notification-only companion). Do NOT write a self-invalidating negative-grep literal in a comment (comment-text discipline).
  </action>
  <verify>
    <automated>mix compile --warnings-as-errors</automated>
    <automated>grep -q "StubChimewayAbsentCompanion" test/support/stub_companion.ex && echo OK</automated>
    <automated>mix test --exclude requires_example_host --exclude engine_present</automated>
  </verify>
  <acceptance_criteria>
    - Crosswake.TestSupport.StubChimewayAbsentCompanion compiles and implements @behaviour Crosswake.Companion
    - companion_id/0 == :chimeway; validate_dependency/0 returns {:error, [Crosswake.Companions.Chimeway]}
    - the stub does NOT implement auth_authority?/0 (chimeway is notification-only)
    - the core suite (excluding host/engine tags) stays green after chimeway extraction
  </acceptance_criteria>
  <done>The absent-chimeway stub exists beside the rulestead/rindle/sigra stubs with no auth_authority?/0, ready for the core-side notification support-truth proof (Plan 02).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| package ↔ core (path dep) | dress rehearsal exercises the extracted package as an external dep; namespace preservation must hold |
| host config → :companions registry | packages cannot self-register; registration is Application-env based and host-owned |
| future contributor → NotificationOpenEvidence.auth_context | a type-tighten to a sigra auth-context struct would silently create an inter-companion dep (the CHIME-02 invariant) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-138-01 | Elevation of Privilege (inter-companion dep creep) | NotificationOpenEvidence.auth_context | high | mitigate | Keep auth_context typed map() and add the moduledoc guard note forbidding a tighten to the sigra auth-context struct; mix.exs lists no crosswake_sigra dep (verified by the no-sigra-dep grep, Task 1). This is the CHIME-02 linchpin. |
| T-138-02 | Spoofing (fail-open regression) | chimeway removed from core :companions env | high | mitigate | Removing chimeway from core's env + StubChimewayAbsentCompanion ensures a core-only build models the absent-companion state; the notification support-truth proof (Plan 02) stays honest. Chimeway is notification-only so no auth fail-closed path is involved. |
| T-138-03 | Information Disclosure | DenialCodes.sanitize_details/1 + Redaction move with chimeway | medium | mitigate | The PII source-scrub (denial_codes.ex allowlist, redaction.ex HMAC fingerprint) moves WITH chimeway so token/PII scrubbing stays at source; no raw-token field is added. Verified structurally by the moved-package compile + the raw-token-absence proof (Plan 02 phase59). |
| T-138-SC | Tampering | npm/pip/cargo installs | low | accept | No package installs — crosswake_chimeway is a first-party source move (RESEARCH Package Legitimacy Audit: first-party, no new external deps). No [ASSUMED]/[SUS] packages. |
</threat_model>

<verification>
- `cd packages/crosswake_chimeway && mix deps.get && mix compile --warnings-as-errors` — package compiles with the preserved namespace and no sigra dep.
- `grep -rl "Crosswake.Companions.Chimeway" lib/` returns no files (chimeway gone from core lib/).
- `grep -c "crosswake_sigra" packages/crosswake_chimeway/mix.exs` == 0 (CHIME-02 no-sigra-dep at the package manifest).
- `mix compile --warnings-as-errors` (core) + `mix test --exclude requires_example_host --exclude engine_present` — core green after extraction with the new stub.
</verification>

<success_criteria>
- crosswake_chimeway is a standalone package (namespace preserved) that compiles with `{:crosswake, ...}` as its only Crosswake dep (SC#1, SC#2).
- The auth_context: map() guard note is present on NotificationOpenEvidence (CHIME-02).
- No chimeway source remains in core lib/; chimeway removed from the :companions env.
- StubChimewayAbsentCompanion exists with no auth_authority?/0; the core suite is green.
- resolver.ex retains its Crosswake.Shell.Denial usage (no Finding-boundary refactor — chimeway divergence from sigra honored).
</success_criteria>

<output>
Create `.planning/phases/138-crosswake-chimeway-extraction/138-01-SUMMARY.md` when done
</output>
