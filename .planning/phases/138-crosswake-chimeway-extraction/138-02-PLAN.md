---
phase: 138-crosswake-chimeway-extraction
plan: 02
type: execute
wave: 2
depends_on: [138-01]
files_modified:
  - packages/crosswake_chimeway/test/crosswake/companions/chimeway_test.exs
  - packages/crosswake_chimeway/test/crosswake/companions/chimeway/
  - packages/crosswake_chimeway/test/crosswake/proof/phase59_chimeway_contract_test.exs
  - packages/crosswake_chimeway/test/crosswake/proof/phase71_notification_workflow_proof_test.exs
  - packages/crosswake_chimeway/test/crosswake/proof/phase138_chimeway_cleanroom_test.exs
  - packages/crosswake_chimeway/mix.exs
  - test/crosswake/companions/chimeway_test.exs
  - test/crosswake/companions/chimeway/
  - test/crosswake/proof/phase59_chimeway_contract_test.exs
  - test/crosswake/proof/phase59_chimeway_support_truth_test.exs
  - packages/crosswake_sigra/test/crosswake/proof/phase71_notification_workflow_proof_test.exs
autonomous: true
requirements: [CHIME-01, CHIME-02]
must_haves:
  truths:
    - "the 5 chimeway-internal unit tests + facade test move to the package lane and pass; no chimeway-internal test remains in core test/crosswake/companions/chimeway/ (CHIME-01)"
    - "phase59 is SPLIT: the SupportMatrix.notification_support_truth/0 assertion stays in core (non-vacuous, chimeway registered via path dep + put_env); the other 4 tests move to the package"
    - "phase71 moves from the crosswake_sigra package to the crosswake_chimeway package (its primary subject is Chimeway.Resolver) and passes there"
    - "the non-vacuous clean-room proof registers chimeway via put_env and asserts chimeway telemetry_events/0 appear in Crosswake.Telemetry.events/0 AND chimeway forbidden keys land in the attach-time handler forbidden set (real seam, not the non-existent forbidden_metadata_keys/0 aggregator)"
    - "chimeway is confirmed NOT an auth authority (function_exported?(Chimeway, :auth_authority?, 0) == false) and the package carries no crosswake_sigra runtime/prod dep (CHIME-02)"
  artifacts:
    - "packages/crosswake_chimeway/test/crosswake/companions/chimeway/{contracts,denial_codes,redaction,resolver,telemetry}_test.exs + chimeway_test.exs (MOVED)"
    - "packages/crosswake_chimeway/test/crosswake/proof/phase59_chimeway_contract_test.exs (4 tests, split) + phase71_...proof_test.exs (MOVED from sigra pkg) + phase138_chimeway_cleanroom_test.exs (NEW)"
    - "test/crosswake/proof/phase59_chimeway_support_truth_test.exs — the SupportMatrix.notification_support_truth/0 assertion retained in core"
  key_links:
    - "phase59 split: report_state + raw-token + struct-alias + delivery_accepted → package; notification_support_truth/0 → core (needs chimeway registered via path dep + put_env else [] sentinel = vacuous)"
    - "phase71 Sigra.Contracts dependency → resolved by a test-only {:crosswake_sigra, path:, only: :test} dep in the chimeway package (integration proof needs the real validated %AuthContext{} struct; map literals cannot reproduce sigra's evaluation) — the tarball files: allowlist excludes test/ so this never ships and the clean-room never sees it"
    - "clean-room forbidden-keys proof reads handler.config[:forbidden_keys] after attach_default_logger (the REAL public seam) — Crosswake.Telemetry.forbidden_metadata_keys/0 does NOT exist (RESEARCH A2 corrected here)"
---

<objective>
Move and split the chimeway test suite (CHIME-01) and prove the no-sigra-dep clean-room invariant
non-vacuously (CHIME-02). MOVE the 5 chimeway-internal unit tests + facade test to the package;
SPLIT phase59 so the `SupportMatrix.notification_support_truth/0` assertion stays in core (registered
+ non-vacuous) while the 4 chimeway-internal tests move; MOVE phase71 from the crosswake_sigra
package to the crosswake_chimeway package (its primary subject is `Chimeway.Resolver`); and add the
non-vacuous `phase138_chimeway_cleanroom_test.exs`.

This is the analog of Phase 137 Plan 03 Tasks 2-3. Two research assumptions are CORRECTED here:
- RESEARCH A2 (`Crosswake.Telemetry.forbidden_metadata_keys/0` is public) is FALSE — that aggregator
  does not exist. The forbidden-keys non-vacuity proof instead attaches the default logger and reads
  the attach-time `handler.config[:forbidden_keys]` MapSet (the real DECOUPLE-05 seam), asserting
  chimeway's keys are present when chimeway is registered.
- RESEARCH Open Question 3 (phase71's Sigra.Contracts can be replaced with plain map literals) is
  resolved AGAINST the map-literal route: phase71 builds a real validated `%SigraContracts.AuthContext{}`
  (via `new_session_authority_lane/1` + `new_auth_context/1`) and drives sigra's evaluator through
  RouteGate to produce the `auth.step_up.*` codes — plain maps cannot reproduce that evaluation, so
  phase71 gets a TEST-ONLY `{:crosswake_sigra, path: ..., only: :test}` dep. This does NOT violate
  CHIME-02: the invariant governs the package `lib/` source and the runtime/prod deps; a test-only
  integration dep never ships (the mix.exs `files:` allowlist excludes `test/`) and never appears in
  the clean-room lane (which installs the published tarball).

Purpose: The publish gate (Plans 03-04) is only trustworthy if the clean-room proves REAL chimeway
notification behavior with sigra absent — not merely that the package compiles.
Output: the chimeway package test lane is green (moved + split + moved-in phase71 + new cleanroom
proof); the core-retained notification support-truth assertion is non-vacuous; the path-dep dress
rehearsal is green.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/138-crosswake-chimeway-extraction/138-RESEARCH.md
@.planning/phases/138-crosswake-chimeway-extraction/138-PATTERNS.md
@.planning/phases/138-crosswake-chimeway-extraction/138-VALIDATION.md
@.planning/phases/138-crosswake-chimeway-extraction/138-01-SUMMARY.md
</context>

<artifacts_this_phase_produces>
This plan (02) produces:
- MOVED chimeway-internal unit tests: `contracts_test.exs`, `denial_codes_test.exs`, `redaction_test.exs`, `resolver_test.exs`, `telemetry_test.exs`, and the facade `chimeway_test.exs` → `packages/crosswake_chimeway/test/crosswake/companions/...`
- SPLIT `phase59_chimeway_contract_test.exs`: 4 tests (TOKN-02 lifecycle, raw-token absence, public-struct aliases, delivery_accepted) → package; the `notification_support_truth/0` assertion → core as NEW `test/crosswake/proof/phase59_chimeway_support_truth_test.exs`
- MOVED `phase71_notification_workflow_proof_test.exs` from the crosswake_sigra package → crosswake_chimeway package
- TEST-ONLY `{:crosswake_sigra, path: "../../packages/crosswake_sigra", only: :test}` dep added to `packages/crosswake_chimeway/mix.exs` deps/0 (for phase71's real AuthContext construction)
- NEW `packages/crosswake_chimeway/test/crosswake/proof/phase138_chimeway_cleanroom_test.exs` — non-vacuous clean-room proof (telemetry events + attach-time forbidden-keys + not-an-auth-authority + no-sigra-runtime-dep)
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Move the chimeway-internal tests and split phase59 (notification support truth stays in core)</name>
  <files>packages/crosswake_chimeway/test/crosswake/companions/chimeway/, packages/crosswake_chimeway/test/crosswake/companions/chimeway_test.exs, packages/crosswake_chimeway/test/crosswake/proof/phase59_chimeway_contract_test.exs, test/crosswake/companions/chimeway/, test/crosswake/companions/chimeway_test.exs, test/crosswake/proof/phase59_chimeway_contract_test.exs, test/crosswake/proof/phase59_chimeway_support_truth_test.exs</files>
  <read_first>
    - test/crosswake/companions/chimeway/{contracts,denial_codes,redaction,resolver,telemetry}_test.exs (the 5 straight MOVEs) + test/crosswake/companions/chimeway_test.exs (facade MOVE)
    - test/crosswake/proof/phase59_chimeway_contract_test.exs (full — the 5 tests; aliases at L4-14 incl `alias Crosswake.SupportMatrix`; the 4 tests that MOVE and the `notification_support_truth/0` assertion inside the "companion state and support matrix..." test at ~L85-100 that STAYS in core)
    - 138-RESEARCH.md § Test Split Verification (the phase59 per-test lane table) + § phase59 split result
    - 138-PATTERNS.md § Phase59 split pattern
    - lib/crosswake/support_matrix/support_matrix.ex notification_support_truth/0 (reads from the :companions runtime registry — the core-retained test must register chimeway to populate it, else the [] sentinel makes the assertion vacuous)
    - test/crosswake/companions/chimeway/resolver_test.exs L164-172 (the auth-predicated route test asserting denial.reason == :dependency_missing — confirms chimeway never evaluates auth itself; it moves to the package and must still pass there)
  </read_first>
  <action>
    MOVE the 5 chimeway-internal unit tests (`contracts_test.exs`, `denial_codes_test.exs`, `redaction_test.exs`, `resolver_test.exs`, `telemetry_test.exs`) from `test/crosswake/companions/chimeway/` to `packages/crosswake_chimeway/test/crosswake/companions/chimeway/`; MOVE `chimeway_test.exs` (facade) to `packages/crosswake_chimeway/test/crosswake/companions/`. Delete all six from core. Ensure each moved file's aliases resolve inside the package (`Crosswake.Companions.Chimeway.*`, `Crosswake.Compatibility.*`, `Crosswake.Shell.Denial`, and `Crosswake.SupportMatrix` — all reachable via the `{:crosswake, path:}` dep). The resolver_test `:dependency_missing` fail-closed assertion must still pass in the package lane (no auth-authority companion is registered there, so RouteGate returns the fail-closed sentinel — the CORRECT chimeway behavior).
    SPLIT `phase59_chimeway_contract_test.exs`: MOVE the 4 chimeway-internal tests (TOKN-02 lifecycle mapping, "seeded raw token is absent...", "public Chimeway structs never define raw token aliases", "delivery_accepted remains provider handoff evidence only") into `packages/crosswake_chimeway/test/crosswake/proof/phase59_chimeway_contract_test.exs`, dropping the `alias Crosswake.SupportMatrix` from that moved file. RETAIN only the `SupportMatrix.notification_support_truth/0` assertion (from the "companion state and support matrix..." test) in core, relocated into a NEW `test/crosswake/proof/phase59_chimeway_support_truth_test.exs`. Because `notification_support_truth/0` sources its truth from the `:companions` runtime registry, the core-retained test MUST register chimeway in setup — `Application.put_env(:crosswake, :companions, [Crosswake.Companions.Chimeway])` with `on_exit` restore (chimeway resolves in core via the Plan-01 path dep) — otherwise the truth is the `[]`/empty sentinel and the assertion is vacuous. Keep the `Chimeway.report_state()` portion of that test with whichever side legitimately owns it: the `report_state().details` (notification support, not delivery) assertion needs the chimeway facade and can stay in the core support-truth test (chimeway is path-dep-registered there) OR move to the package with the other 4 — choose based on what keeps BOTH lanes green; document the decision in the SUMMARY. The `notification_support_truth/0` assertion itself is the non-negotiable core retention.
  </action>
  <verify>
    <automated>cd packages/crosswake_chimeway && mix test test/crosswake/companions/ test/crosswake/proof/phase59_chimeway_contract_test.exs</automated>
    <automated>mix test test/crosswake/proof/phase59_chimeway_support_truth_test.exs</automated>
    <automated>test ! -d test/crosswake/companions/chimeway || test -z "$(ls -A test/crosswake/companions/chimeway 2>/dev/null)"</automated>
  </verify>
  <acceptance_criteria>
    - the 5 unit tests + facade test + 4 phase59 tests pass in the package lane
    - phase59_chimeway_support_truth_test.exs stays in core, registers chimeway, and its notification_support_truth/0 assertion is non-vacuous (populated, not the [] sentinel)
    - no chimeway-internal test remains in core test/crosswake/companions/chimeway/
    - the resolver_test :dependency_missing fail-closed assertion passes in the package lane
  </acceptance_criteria>
  <done>Tests are split exactly per the phase59 lane table; the notification support-truth assertion stays non-vacuous in core; the moved chimeway unit + contract tests are green in the package lane.</done>
</task>

<task type="auto">
  <name>Task 2: Move phase71 to the chimeway package with a test-only crosswake_sigra dep for real AuthContext construction</name>
  <files>packages/crosswake_chimeway/test/crosswake/proof/phase71_notification_workflow_proof_test.exs, packages/crosswake_chimeway/mix.exs, packages/crosswake_sigra/test/crosswake/proof/phase71_notification_workflow_proof_test.exs</files>
  <read_first>
    - packages/crosswake_sigra/test/crosswake/proof/phase71_notification_workflow_proof_test.exs (full — primary subject is Crosswake.Companions.Chimeway.Resolver; it builds a real %SigraContracts.AuthContext{} via SigraContracts.new_session_authority_lane/1 + new_auth_context/1 at L307-320 and pattern-matches %SigraContracts.AuthContext{} at L323; setup registers Sigra via put_env at L67-72; it mounts StatefulIntentConsumer implementing Crosswake.Companions.Chimeway.IntentConsumer)
    - packages/crosswake_sigra/lib/crosswake/companions/sigra/contracts.ex new_auth_context/1 (returns {:ok, %AuthContext{}} — a validated sigra struct, NOT a plain map: confirmed L189-196) and new_session_authority_lane/1 (returns {:ok, %SessionAuthorityLane.t()})
    - packages/crosswake_chimeway/mix.exs deps/0 (from Plan 01 — currently just crosswake_dep/0)
    - packages/crosswake_chimeway/test/support/study_session_live.ex (the LiveView phase71 needs — copied in Plan 01)
    - 138-RESEARCH.md § Pitfall 4 + Open Question 3 (the two dependency-strategy options) + 138-PATTERNS.md § Phase71 test move (the only: :test path-dep option)
  </read_first>
  <action>
    MOVE `phase71_notification_workflow_proof_test.exs` from `packages/crosswake_sigra/test/crosswake/proof/` to `packages/crosswake_chimeway/test/crosswake/proof/` (delete from the sigra package — after chimeway extraction the sigra package no longer depends on chimeway, so `Chimeway.Resolver` would not resolve there). Keep the test body byte-for-byte where possible; its `alias Crosswake.Companions.Sigra.Contracts, as: SigraContracts` and its `setup` that registers `Crosswake.Companions.Sigra` via `Application.put_env` stay AS-IS — the proof's whole point is that Chimeway.Resolver + a REAL registered Sigra evaluator produce the `auth.step_up.*` codes end-to-end.
    RESOLVE the Sigra.Contracts dependency by adding a TEST-ONLY dep to `packages/crosswake_chimeway/mix.exs` deps/0: `{:crosswake_sigra, path: "../../packages/crosswake_sigra", only: :test}`. Rationale: `SigraContracts.new_auth_context/1` returns a validated `%AuthContext{}` struct (not a plain map) and the test pattern-matches `%SigraContracts.AuthContext{}` and constructs a real `SessionAuthorityLane` — plain map literals CANNOT reproduce sigra's validation + evaluation, so the map-literal route would make the integration proof vacuous or fail. The `only: :test` scoping means this dep is NEVER compiled into the runtime/prod package and NEVER shipped: the `package/0` `files:` allowlist (`~w(lib mix.exs README.md LICENSE CHANGELOG.md)`) excludes `test/`, so the Hex tarball and the clean-room lane (which installs the published tarball with sigra deliberately absent) never see it. This preserves the CHIME-02 no-sigra-dep invariant for source, runtime, prod, and the clean-room — the ONLY place sigra appears is a test-only integration proof.
    Run `cd packages/crosswake_chimeway && mix deps.get` to resolve the new test-only dep.
    Guard note: the no-sigra-dep source guard (Task 3 / Plan 03 clean-room) must grep `packages/crosswake_chimeway/lib/` and the runtime deps of `mix.exs`, NOT the `only: :test` line — ensure the Plan 03 grep is scoped to `lib/` and to non-test deps so this legitimate test-only dep does not trip it.
  </action>
  <verify>
    <automated>cd packages/crosswake_chimeway && mix deps.get && mix test test/crosswake/proof/phase71_notification_workflow_proof_test.exs</automated>
    <automated>test ! -f packages/crosswake_sigra/test/crosswake/proof/phase71_notification_workflow_proof_test.exs</automated>
    <automated>grep -q "only: :test" packages/crosswake_chimeway/mix.exs && grep -q "crosswake_sigra" packages/crosswake_chimeway/mix.exs && echo test-only-dep-present</automated>
  </verify>
  <acceptance_criteria>
    - phase71 lives in the chimeway package and passes there (Chimeway.Resolver + real registered Sigra evaluator produce the auth.step_up.* codes)
    - phase71 no longer exists in the crosswake_sigra package
    - packages/crosswake_chimeway/mix.exs carries {:crosswake_sigra, path: ..., only: :test} — test-scoped only, absent from the files: tarball allowlist
    - mix deps.get resolves and the sigra package (still path-dep) compiles for the test lane
  </acceptance_criteria>
  <done>phase71 is in the chimeway package proving real Chimeway+Sigra notification-auth integration, with sigra scoped test-only so the CHIME-02 no-sigra-dep invariant holds for source/runtime/prod/clean-room.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Non-vacuous chimeway clean-room proof (telemetry events + attach-time forbidden keys + not-an-auth-authority)</name>
  <files>packages/crosswake_chimeway/test/crosswake/proof/phase138_chimeway_cleanroom_test.exs</files>
  <read_first>
    - 138-RESEARCH.md § Pattern 6 (the drafted cleanroom test) + § Assumptions Log A2 (the forbidden_metadata_keys/0 caveat) + § Pitfall 3 (vacuous clean-room)
    - 138-PATTERNS.md § phase138_chimeway_cleanroom_test.exs (the drafted assertions + the Planner note on A2)
    - lib/crosswake/telemetry.ex L89-102 (events/0 — aggregates registered companions' telemetry_events/0), L222-253 (attach_default_logger/1 — builds the forbidden-key MapSet at attach time and stores it in handler config[:forbidden_keys]), L70 (baseline_forbidden_metadata_keys/0). NOTE: there is NO public Crosswake.Telemetry.forbidden_metadata_keys/0 aggregator — A2 is FALSE.
    - test/crosswake/telemetry_test.exs L18 (@handler_id "crosswake-default-logger"), L40-51 (attach + :telemetry.list_handlers([:crosswake]) + reading handler config), L26 (detach in on_exit) — the precedent for reading handler.config[:forbidden_keys]
    - lib/crosswake/companions/chimeway/telemetry.ex event_names/0 (10 events) + forbidden_metadata_keys/0 (the chimeway forbidden-key list)
  </read_first>
  <behavior>
    - Test: with `Application.put_env(:crosswake, :companions, [Chimeway])` in setup (on_exit restore), `Crosswake.Telemetry.events/0` includes at least one event whose `.event` is in `ChimewayTelemetry.event_names()` (proves the telemetry_events/0 registry callback ran — non-vacuous: without registration the catalog would carry zero chimeway events).
    - Test: after `Crosswake.Telemetry.attach_default_logger()` (detach in on_exit), reading the `crosswake-default-logger` handler's `config[:forbidden_keys]` MapSet via `:telemetry.list_handlers([:crosswake])` shows every key from `ChimewayTelemetry.forbidden_metadata_keys()` present (proves the attach-time forbidden-key aggregation picked up chimeway's contribution — this is the REAL seam; there is no public aggregator function).
    - Test: `refute function_exported?(Chimeway, :auth_authority?, 0)` — chimeway is notification-only, not an auth authority (CHIME-02).
    - Test: the chimeway package lists no NON-TEST crosswake_sigra dep — `Mix.Project.config()[:deps]` filtered to deps whose opts do NOT include `only: :test` must not contain `:crosswake_sigra` (the phase71 test-only dep is explicitly allowed; a runtime/prod sigra dep is forbidden).
  </behavior>
  <action>
    Create `packages/crosswake_chimeway/test/crosswake/proof/phase138_chimeway_cleanroom_test.exs`: `use ExUnit.Case, async: false` (companion registration + logger attach are process-global). Alias `Crosswake.Companions.Chimeway` and `Crosswake.Companions.Chimeway.Telemetry, as: ChimewayTelemetry`.
    setup: save `Application.get_env(:crosswake, :companions, [])`, `Application.put_env(:crosswake, :companions, [Chimeway])`, `on_exit` restore.
    Test 1 (telemetry events aggregation): filter `Crosswake.Telemetry.events()` to entries whose `.event` is in `ChimewayTelemetry.event_names()`; assert the filtered list is non-empty with a message naming the vacuity risk.
    Test 2 (forbidden keys — the A2 correction): `Crosswake.Telemetry.attach_default_logger()`; `on_exit(fn -> Crosswake.Telemetry.detach_default_logger() end)`; find the `"crosswake-default-logger"` handler via `:telemetry.list_handlers([:crosswake])`, read its `config[:forbidden_keys]` (a MapSet); assert every key in `ChimewayTelemetry.forbidden_metadata_keys()` is a member. Do NOT call a `Crosswake.Telemetry.forbidden_metadata_keys/0` — that function does not exist; the attach-time handler config is the real seam. (If detach/attach idempotency requires a pre-detach, mirror the telemetry_test.exs on_exit detach pattern.)
    Test 3 (not an auth authority): `refute function_exported?(Chimeway, :auth_authority?, 0)`.
    Test 4 (no runtime sigra dep — vacuity guard): read `Mix.Project.config()[:deps]`, normalize each entry to `{name, opts}` (opts may be a keyword list or absent), keep only entries where `Keyword.get(opts, :only)` is not `:test` (and not `[:test]`), map to the dep name, and `refute :crosswake_sigra in runtime_dep_names` with a message that the phase71 test-only sigra dep is the sole permitted sigra reference.
    async: false and the on_exit restores are mandatory (process-global state).
  </action>
  <verify>
    <automated>cd packages/crosswake_chimeway && mix test test/crosswake/proof/phase138_chimeway_cleanroom_test.exs</automated>
    <automated>cd packages/crosswake_chimeway && mix test</automated>
    <automated>mix test --exclude requires_example_host --exclude engine_present</automated>
  </verify>
  <acceptance_criteria>
    - the cleanroom proof asserts chimeway events appear in Crosswake.Telemetry.events/0 (non-empty, non-vacuous) with chimeway registered
    - the forbidden-keys assertion reads the attach-time handler config[:forbidden_keys] (NOT a non-existent aggregator) and finds chimeway's keys
    - refute function_exported?(Chimeway, :auth_authority?, 0) passes (notification-only)
    - the runtime-deps vacuity guard permits the only: :test sigra dep but forbids a runtime/prod one
    - the full chimeway package lane is green AND the path-dep dress rehearsal (core suite) is green
  </acceptance_criteria>
  <done>A non-vacuous clean-room proof exists proving real chimeway telemetry contribution + forbidden-key aggregation with sigra runtime-absent, using the real attach-time seam (RESEARCH A2 corrected); the dress rehearsal is green end-to-end.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| package test lane ↔ core (path dep) | dress rehearsal exercises the extracted package; the notification support-truth must stay non-vacuous |
| chimeway telemetry callbacks → core aggregation | core reads chimeway's telemetry_events/0 + forbidden_metadata_keys/0 at runtime; the clean-room proof must exercise the REAL seam, not a phantom aggregator |
| test-only sigra dep → tarball/clean-room | the `only: :test` sigra dep must never reach the published tarball or the clean-room lane |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-138-04 | Tampering (test bypass / vacuity) | phase138_chimeway_cleanroom_test.exs | high | mitigate | setup MUST put_env chimeway and the events assertion MUST require a non-empty filtered list; the forbidden-keys assertion reads the attach-time handler config (the real seam) not a non-existent aggregator — without registration both would be empty and the proof would pass vacuously. Non-vacuity is the explicit guard (Task 3). |
| T-138-05 | Elevation of Privilege (inter-companion dep creep) | crosswake_chimeway mix.exs deps | high | mitigate | The sigra dep is `only: :test` for the phase71 integration proof only; the runtime-deps vacuity guard (Task 3 Test 4) refutes any non-test crosswake_sigra dep, and the `files:` allowlist keeps it out of the tarball/clean-room. CHIME-02 holds for source/runtime/prod/clean-room. |
| T-138-06 | Information Disclosure | phase59 raw-token-absence + phase71 hostile-metadata proofs | high | mitigate | The moved phase59 "seeded raw token is absent..." and phase71 "hostile notification metadata is absent from denial details and inspect output" tests continue to assert no raw token / PII leaks in serialized/telemetry-sanitized output; they move WITH chimeway so the source-scrub stays proven (TOKN-02). |
| T-138-07 | Tampering (vacuous support truth) | phase59 split — notification_support_truth/0 | medium | mitigate | The core-retained support-truth test registers chimeway via put_env so notification_support_truth/0 is populated from the runtime registry (not the [] sentinel) — a vacuous empty-vs-empty pass is prevented (Task 1). |
| T-138-SC | Tampering | npm/pip/cargo installs | low | accept | No package installs; the test-only sigra dep is a first-party path dep (RESEARCH Package Legitimacy Audit: first-party). No [ASSUMED]/[SUS] packages. |
</threat_model>

<verification>
- `cd packages/crosswake_chimeway && mix test` — moved unit/contract/facade tests + moved phase71 + new cleanroom proof all green.
- `mix test test/crosswake/proof/phase59_chimeway_support_truth_test.exs` — non-vacuous notification_support_truth/0 assertion in core.
- `test ! -f packages/crosswake_sigra/test/crosswake/proof/phase71_notification_workflow_proof_test.exs` — phase71 gone from the sigra package.
- `mix test --exclude requires_example_host --exclude engine_present` — path-dep dress rehearsal (core) green.
</verification>

<success_criteria>
- The chimeway-internal tests + facade test move to the package and pass; none remain in core (CHIME-01).
- phase59 is split: notification_support_truth/0 stays non-vacuous in core; the 4 contract tests move.
- phase71 moves to the chimeway package (with a test-only sigra dep) and proves real Chimeway+Sigra integration.
- The non-vacuous clean-room proof passes using the real telemetry seams (RESEARCH A2 corrected) and confirms chimeway is not an auth authority with no runtime sigra dep (CHIME-02).
- The path-dep dress rehearsal (core + package) is green before any CI/publish wiring.
</success_criteria>

<output>
Create `.planning/phases/138-crosswake-chimeway-extraction/138-02-SUMMARY.md` when done
</output>
