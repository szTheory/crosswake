---
phase: 138-crosswake-chimeway-extraction
plan: 03
type: execute
wave: 3
depends_on: [138-02]
files_modified:
  - script/verify_companion_cleanroom.sh
  - release-please-config.json
  - .release-please-manifest.json
  - .github/workflows/release-please.yml
  - examples/phoenix_host/mix.exs
  - guides/companion_compatibility.md
autonomous: true
requirements: [CHIME-02, CHIME-03]
must_haves:
  truths:
    - "verify_companion_cleanroom.sh no-engine smoke test is chimeway-correct: the refute enabled?(%{}) assertion is replaced by assert enabled?(%{}) when PACKAGE=crosswake_chimeway (chimeway defaults enabled? true), plus a non-empty Telemetry.event_names/0 canary so the lane is vacuity-safe with sigra absent (CHIME-02/CHIME-03)"
    - "crosswake_chimeway is registered as an independent elixir release-please component (NOT in linked-versions), with the one-shot release-as: 0.1.0 (CHIME-03)"
    - "release-please.yml has chimeway_release_created/tag_name/version outputs, a publish-hex-chimeway job, a clean-room-proof-chimeway job invoking the no-engine cleanroom for crosswake_chimeway, and chimeway folded into release-as-cleanup + release-failure-alert"
    - "the clean-room lane installs crosswake + crosswake_chimeway but NOT crosswake_sigra (no-engine invocation) — the CHIME-02 no-sigra-dep invariant enforced at the CI level"
    - "examples/phoenix_host adds the crosswake_chimeway path dep and guides/companion_compatibility.md gains the chimeway row (engine dep: none)"
  artifacts:
    - "script/verify_companion_cleanroom.sh — chimeway enabled?(%{}) assertion guard + Telemetry.event_names/0 canary"
    - "release-please-config.json — crosswake_chimeway component block; .release-please-manifest.json — packages/crosswake_chimeway: 0.1.0"
    - ".github/workflows/release-please.yml — chimeway outputs + publish-hex-chimeway + clean-room-proof-chimeway + extended cleanup/alert (~100 lines)"
    - "examples/phoenix_host/mix.exs — crosswake_chimeway path dep; guides/companion_compatibility.md — chimeway row"
  key_links:
    - "per-component gate: all chimeway jobs key on chimeway_release_created (NOT the aggregate releases_created) — keeps the sequential sigra→chimeway→threadline publish honest"
    - "publish-hex-chimeway sets CROSSWAKE_RELEASE=1 on all mix steps — else hex.build rejects the path: dep"
    - "clean-room-proof-chimeway invokes verify_companion_cleanroom.sh crosswake_chimeway <version> (no engine args) — no-engine mode, sigra deliberately absent (CHIME-02)"
    - "the chimeway enabled?(%{}) smoke assertion MUST flip to assert (defaults true) — the existing refute would FAIL the chimeway clean-room (RESEARCH Pitfall 1)"
---

<objective>
Wire the CI publish pipeline (CHIME-03) for crosswake_chimeway as an INDEPENDENT release-please
component and make the clean-room lane vacuity-safe for chimeway (CHIME-02). Patch
verify_companion_cleanroom.sh so the no-engine smoke test is chimeway-correct — chimeway's
enabled?(%{}) defaults to true (unlike the current refute enabled?(%{}) no-engine assertion, which
would FAIL for chimeway) — and add a non-empty Chimeway.Telemetry.event_names/0 canary so the lane
proves REAL notification behavior with sigra absent (not merely that the package compiles). Register
the component block (with the one-shot release-as 0.1.0), add the manifest entry, add the ~100 lines
to release-please.yml (chimeway outputs + publish-hex-chimeway + clean-room-proof-chimeway + extended
release-as-cleanup/release-failure-alert), add the crosswake_chimeway path dep to the example host,
and add the chimeway row to the compat matrix.

This is the direct analog of Phase 137 Plan 04 (s/sigra/chimeway/g throughout), plus the
chimeway-specific enabled? fix that sigra did not need. All jobs key on the per-component
chimeway_release_created output (never the aggregate releases_created), preserving independent,
sequential publishing (D-8, D-9). The clean-room lane installs crosswake + crosswake_chimeway with NO
crosswake_sigra — the CHIME-02 no-sigra-dep invariant enforced at the CI level.

Purpose: The publish gate chain (Plan 04) depends entirely on these lanes existing and being
green-able. The chimeway enabled? fix is the difference between a passing and a spuriously-failing
clean-room-proof-chimeway.
Output: chimeway registered as an independent component; 3 new CI jobs; a vacuity-safe chimeway
clean-room smoke path; example-host + compat-matrix updated.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/138-crosswake-chimeway-extraction/138-RESEARCH.md
@.planning/phases/138-crosswake-chimeway-extraction/138-PATTERNS.md
@.planning/phases/138-crosswake-chimeway-extraction/138-VALIDATION.md
@.planning/phases/138-crosswake-chimeway-extraction/138-02-SUMMARY.md
</context>

<artifacts_this_phase_produces>
This plan (03) produces:
- `script/verify_companion_cleanroom.sh` — a chimeway guard in the NO_ENGINE smoke test: when PACKAGE=crosswake_chimeway, emit `assert Chimeway.enabled?(%{})` (defaults true) instead of the default `refute enabled?(%{})`, plus a `Chimeway.Telemetry.event_names/0` == 10-events canary (proves Telemetry shipped, vacuity-safe)
- `release-please-config.json` — `packages/crosswake_chimeway` component block (component "crosswake_chimeway", release-type "elixir", separate-pull-requests true, one-shot `release-as: "0.1.0"` + `_TODO_release_as` citing Phase 138), NOT in `linked-versions`
- `.release-please-manifest.json` — `"packages/crosswake_chimeway": "0.1.0"`
- `.github/workflows/release-please.yml` — `chimeway_release_created`/`chimeway_tag_name`/`chimeway_version` outputs, `publish-hex-chimeway` job, `clean-room-proof-chimeway` job, chimeway folded into `release-as-cleanup` if/strip and `release-failure-alert` needs/body
- `examples/phoenix_host/mix.exs` — `{:crosswake_chimeway, path: "../../packages/crosswake_chimeway"}` path dep
- `guides/companion_compatibility.md` — the chimeway row (engine dep: none)
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Make the no-engine clean-room smoke test chimeway-correct (assert enabled?(%{}) + Telemetry canary)</name>
  <files>script/verify_companion_cleanroom.sh</files>
  <read_first>
    - script/verify_companion_cleanroom.sh L45-65 (arg parsing, NO_ENGINE flag), L241-276 (the NO_ENGINE smoke test block — esp the refute enabled?(%{}) at L268-270 that is WRONG for chimeway), L307-320 (the existing `if [ "$PACKAGE" = "crosswake_rindle" ]` engine-mode canary pattern to mirror for a chimeway no-engine canary)
    - 138-RESEARCH.md § Pattern 5 (clean-room smoke modification) + § Pitfall 1 (enabled?(%{}) mismatch) + § Open Question 2 (patch unconditionally: chimeway defaults true)
    - 138-PATTERNS.md § script/verify_companion_cleanroom.sh — chimeway canary patch (the exact chimeway elif branch + the note that the standard refute must be suppressed for chimeway)
    - lib/... chimeway.ex enabled?/1 (defaults true when no :enabled key) + telemetry.ex event_names/0 (10 events)
  </read_first>
  <action>
    In the NO_ENGINE smoke-test heredoc block (L245-276), make the enabled?/1 default assertion package-aware. The current test that does `refute ${COMPANION_MODULE_SUFFIX}.enabled?(%{})` is correct for sigra (defaults false) but WRONG for chimeway (defaults true). Guard it: when PACKAGE = crosswake_chimeway, emit an `assert ${COMPANION_MODULE_SUFFIX}.enabled?(%{})` test titled to reflect the chimeway-enabled-by-default contract; otherwise keep the existing refute for sigra and any future no-engine companion that defaults false. Use the same `$(if [ "$PACKAGE" = "..." ]; then cat <<'EOF' ... fi)` shell-interpolation idiom already used for the rindle engine-mode canary at L307 — restructure the no-engine enabled? assertion so exactly one of the two forms is emitted per package (do NOT emit both, which would contradict). Keep the `enabled?(%{enabled: true})` == true assertion unchanged (holds for both).
    Add a chimeway-specific Telemetry canary in the NO_ENGINE block (mirror the rindle Contracts canary shape): when PACKAGE = crosswake_chimeway, emit a test asserting `Crosswake.Companions.Chimeway.Telemetry.event_names()` is a list of length 10, with a message that a missing/short list means the Telemetry sub-module did not ship in the tarball. This is the vacuity-safe canary — it proves real chimeway notification machinery shipped and runs with sigra absent, not merely that the facade compiles.
    Keep the default (sigra) no-engine path and the engine (rulestead/rindle) path 100 percent unchanged — the chimeway behavior is strictly additive, gated on PACKAGE = crosswake_chimeway. Update the usage-header comment to note the chimeway invocation form.
    Do NOT hardcode a self-invalidating literal that a downstream negative-grep would trip (comment-text discipline); reference sigra by concept in any comment, not as the package atom.
  </action>
  <verify>
    <automated>bash -n script/verify_companion_cleanroom.sh</automated>
    <automated>grep -q "crosswake_chimeway" script/verify_companion_cleanroom.sh && grep -q "event_names" script/verify_companion_cleanroom.sh && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - the NO_ENGINE smoke test emits assert enabled?(%{}) for crosswake_chimeway (defaults true) and keeps refute enabled?(%{}) for sigra — exactly one form per package, never both
    - a Chimeway.Telemetry.event_names/0 == 10 canary is emitted for crosswake_chimeway (vacuity-safe proof that Telemetry shipped)
    - the sigra no-engine path and the rulestead/rindle engine path are unchanged (additive only)
    - bash -n parses clean
  </acceptance_criteria>
  <done>verify_companion_cleanroom.sh cleanly supports the crosswake_chimeway invocation with the chimeway-correct enabled? assertion and a non-empty Telemetry canary — the lane is vacuity-safe with sigra absent.</done>
</task>

<task type="auto">
  <name>Task 2: Register crosswake_chimeway as an independent release-please component + example-host path dep + compat-matrix row</name>
  <files>release-please-config.json, .release-please-manifest.json, examples/phoenix_host/mix.exs, guides/companion_compatibility.md</files>
  <read_first>
    - release-please-config.json — the crosswake_sigra component block (clone it verbatim, retargeted to crosswake_chimeway), the linked-versions group (chimeway MUST NOT be added there — D-8), the _TODO_release_as + release-as one-shot precedent
    - .release-please-manifest.json — the existing keys (., ios, android, rulestead, rindle, sigra); chimeway becomes the next key
    - examples/phoenix_host/mix.exs L46-49 (the rindle/sigra path deps + the comment about the Hex form)
    - guides/companion_compatibility.md L24 (the sigra row — the table column shape)
    - 138-PATTERNS.md § release-please-config.json + § .release-please-manifest.json + § examples/phoenix_host/mix.exs + § guides/companion_compatibility.md (the exact chimeway additions)
    - 138-RESEARCH.md § Pattern 2 + § Pattern 3 + § Pitfall 2 (release-as one-shot) + § Pitfall 6 (compat-matrix row)
  </read_first>
  <action>
    Add a "packages/crosswake_chimeway" block to release-please-config.json cloning the crosswake_sigra block with s/sigra/chimeway/g: component "crosswake_chimeway", release-type "elixir", separate-pull-requests true, a _TODO_release_as note explaining the one-shot removal-after-first-merge and citing Phase 138 / recipe Step 12f / Pitfall 6, release-as "0.1.0" (one-shot), extra-files ["packages/crosswake_chimeway/mix.exs"], and the standard changelog-sections array (same as sigra). Do NOT add crosswake_chimeway to the linked-versions lockstep group — it is independently versioned (D-8).
    Add "packages/crosswake_chimeway": "0.1.0" to .release-please-manifest.json. Keep valid JSON (trailing-comma-free, appended consistently with the existing style).
    In examples/phoenix_host/mix.exs, add {:crosswake_chimeway, path: "../../packages/crosswake_chimeway"} after the sigra path dep, and update the L46 comment to mention crosswake_chimeway alongside rulestead/rindle/sigra.
    In guides/companion_compatibility.md, add the chimeway row after the sigra row with columns matching the sigra row exactly: package crosswake_chimeway, companion_id :chimeway, version 0.1.0, requires crosswake ~> 0.1, engine dependency "none (pure-Elixir notification machinery)", and the hexdocs.pm/crosswake_chimeway docs link. This pre-empts the compat-matrix drift test failing once chimeway publishes (Pitfall 6).
  </action>
  <verify>
    <automated>python3 -c "import json; json.load(open('release-please-config.json')); json.load(open('.release-please-manifest.json')); print('valid json')"</automated>
    <automated>python3 -c "import json; c=json.load(open('release-please-config.json')); p=c['packages']['packages/crosswake_chimeway']; assert p['component']=='crosswake_chimeway' and p['release-as']=='0.1.0'; print('chimeway component OK')"</automated>
    <automated>python3 -c "import json; m=json.load(open('.release-please-manifest.json')); assert m['packages/crosswake_chimeway']=='0.1.0'; print('manifest OK')"</automated>
  </verify>
  <acceptance_criteria>
    - release-please-config.json has a crosswake_chimeway component block (elixir, separate-pull-requests, release-as 0.1.0), NOT in linked-versions
    - .release-please-manifest.json has "packages/crosswake_chimeway": "0.1.0"
    - examples/phoenix_host/mix.exs has the crosswake_chimeway path dep; guides/companion_compatibility.md has the chimeway row (engine dep none)
    - both JSON files remain valid
  </acceptance_criteria>
  <done>crosswake_chimeway is an independent elixir release-please component with the one-shot 0.1.0 pin absent from lockstep; the example host depends on it and the compat matrix carries its row.</done>
</task>

<task type="auto">
  <name>Task 3: Add chimeway outputs + publish-hex-chimeway + clean-room-proof-chimeway jobs and extend cleanup/alert</name>
  <files>.github/workflows/release-please.yml</files>
  <read_first>
    - .github/workflows/release-please.yml — the outputs block (sigra_release_created/tag_name/version), the publish-hex-sigra job (to mirror verbatim s/sigra/chimeway/g — incl the D-8/D-07 per-component gate comment and CROSSWAKE_RELEASE=1 on all mix steps), the clean-room-proof-sigra job (invoking verify_companion_cleanroom.sh crosswake_sigra "<version>" with no engine args), the release-as-cleanup job (its if: condition + the sigra strip block), the release-failure-alert job (needs + body)
    - 138-PATTERNS.md § release-please.yml outputs / publish-hex-chimeway job / clean-room-proof-chimeway job / release-as-cleanup + release-failure-alert patches (the exact sub-blocks with s/sigra/chimeway/g)
    - 138-RESEARCH.md § Pattern 4 (all four sub-blocks) + Common Pitfalls (CROSSWAKE_RELEASE=1, per-component gate)
  </read_first>
  <action>
    Add three outputs entries mirroring the sigra double-dash to dot-alias contract: chimeway_release_created, chimeway_tag_name, chimeway_version (each reading steps.release.outputs['packages/crosswake_chimeway--*']).
    Add a publish-hex-chimeway job cloning publish-hex-sigra: needs release-please, if needs.release-please.outputs.chimeway_release_created == 'true', env CROSSWAKE_RELEASE "1" on all mix steps (else hex.build rejects the path: dep), checkout ref chimeway_tag_name, working-directory packages/crosswake_chimeway, version-grep packages/crosswake_chimeway/mix.exs, poll hex.pm/api/packages/crosswake_chimeway/releases/<chimeway_version>. Preserve the D-8/D-07 per-component gate comment (gate on chimeway_release_created, never the aggregate releases_created).
    Add a clean-room-proof-chimeway job cloning clean-room-proof-sigra: needs [release-please, publish-hex-chimeway], if chimeway_release_created == 'true', setup-beam via .tool-versions strict, install hex+rebar, then `bash script/verify_companion_cleanroom.sh crosswake_chimeway "${{ needs.release-please.outputs.chimeway_version }}"` (no engine args — uses the Task-1 no-engine chimeway mode; installs crosswake + crosswake_chimeway, NOT crosswake_sigra).
    Extend release-as-cleanup: add `|| needs.release-please.outputs.chimeway_release_created == 'true'` to its if:; add a strip block `if chimeway_release_created == 'true' then python3 script/strip_release_as.py crosswake_chimeway`.
    Extend release-failure-alert: add publish-hex-chimeway and clean-room-proof-chimeway to its needs: and add their results to the issue body (its if: failure() needs no change).
    All chimeway jobs MUST gate on chimeway_release_created (never the aggregate releases_created) so core-only releases never publish chimeway (per-component gate — D-8).
  </action>
  <verify>
    <automated>python3 -c "import yaml; yaml.safe_load(open('.github/workflows/release-please.yml')); print('valid yaml')"</automated>
    <automated>grep -q "publish-hex-chimeway" .github/workflows/release-please.yml && grep -q "clean-room-proof-chimeway" .github/workflows/release-please.yml && grep -q "chimeway_release_created" .github/workflows/release-please.yml && echo OK</automated>
    <automated>grep -q "strip_release_as.py crosswake_chimeway" .github/workflows/release-please.yml && echo cleanup-wired</automated>
  </verify>
  <acceptance_criteria>
    - chimeway_release_created/tag_name/version outputs exist
    - publish-hex-chimeway sets CROSSWAKE_RELEASE=1 and gates on chimeway_release_created
    - clean-room-proof-chimeway invokes verify_companion_cleanroom.sh with only PACKAGE+VERSION (no-engine, sigra absent)
    - release-as-cleanup strips crosswake_chimeway on chimeway_release_created; release-failure-alert covers the new jobs
    - the workflow file is valid YAML
  </acceptance_criteria>
  <done>The full chimeway publish pipeline exists in release-please.yml, per-component gated, with the one-shot release-as cleanup and failure alerting wired; the clean-room lane runs with sigra deliberately absent.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI → Hex.pm (irreversible publish) | the name/version coordinate is irreversible; the gate chain protects it |
| release-please config → publish jobs | a mis-scoped gate could publish chimeway on every core release |
| clean-room lane → sigra absence | the lane must install crosswake + crosswake_chimeway WITHOUT crosswake_sigra (CHIME-02 at CI level) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-138-08 | Tampering (supply chain) | release-as: 0.1.0 one-shot | high | mitigate | The one-shot pin is required for first publish but re-targets 0.1.0 forever if left; release-as-cleanup fires on chimeway_release_created → strip_release_as.py crosswake_chimeway (PROOF-03 auto). Verified by the cleanup-wired grep (Task 3). |
| T-138-09 | Tampering (over-publish) | chimeway job gating | high | mitigate | All chimeway jobs gate on the per-component chimeway_release_created, never the aggregate releases_created — a core-only release never publishes chimeway (Task 3). |
| T-138-10 | Information Disclosure (raw token / PII leak in notification metadata) | chimeway clean-room smoke + Telemetry canary | high | mitigate | The vacuity-safe Telemetry canary proves the real notification machinery (with its forbidden-key denylist) shipped and runs with sigra absent; the moved phase59/phase71 raw-token-absence proofs (Plan 02) cover TOKN-02. The clean-room installs crosswake + crosswake_chimeway only, exercising real behavior. |
| T-138-11 | Tampering (vacuous clean-room) | chimeway enabled?(%{}) smoke assertion | high | mitigate | The existing refute enabled?(%{}) no-engine assertion would FAIL for chimeway (defaults true); flipping it to assert plus adding a non-empty event_names canary makes the lane both correct and non-vacuous (Task 1). Without the fix the lane fails spuriously or (if silently skipped) proves nothing. |
| T-138-12 | Denial of Service (bad build) | publish-hex-chimeway CROSSWAKE_RELEASE | medium | mitigate | CROSSWAKE_RELEASE=1 on all mix steps so hex.build sees the {:crosswake, "~> 0.1"} Hex dep, not the path: dep it rejects (Task 3). |
| T-138-SC | Tampering | npm/pip/cargo installs | low | accept | No package installs; CI/config-only changes. crosswake_chimeway is first-party (RESEARCH Package Legitimacy Audit). |
</threat_model>

<verification>
- `bash -n script/verify_companion_cleanroom.sh` + the chimeway enabled?/event_names grep — the no-engine smoke test is chimeway-correct and vacuity-safe.
- `python3 -c "import json..."` — config + manifest valid; chimeway component independent; manifest entry present.
- `python3 -c "import yaml..."` — release-please.yml valid.
- grep confirms publish-hex-chimeway, clean-room-proof-chimeway, chimeway_release_created, strip_release_as.py crosswake_chimeway all present.
</verification>

<success_criteria>
- crosswake_chimeway is an independent elixir release-please component (not lockstep) with the one-shot release-as (CHIME-03).
- release-please.yml has chimeway outputs + publish-hex-chimeway + clean-room-proof-chimeway, per-component gated, with cleanup + failure alerting extended.
- verify_companion_cleanroom.sh handles the no-engine chimeway invocation with the correct enabled? assertion and a non-empty Telemetry canary (vacuity-safe, sigra absent — CHIME-02).
- the example host depends on crosswake_chimeway and the compat matrix carries its row.
</success_criteria>

<output>
Create `.planning/phases/138-crosswake-chimeway-extraction/138-03-SUMMARY.md` when done
</output>
