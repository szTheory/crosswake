---
phase: "15"
plan: "03"
type: execute
wave: 1
depends_on: []
files_modified:
  - examples/android_shell_host/app/src/main/java/dev/crosswake/shell/BridgeChannel.kt
  - examples/android_shell_host/app/src/main/java/dev/crosswake/shell/LiveViewFragment.kt
autonomous: true
requirements:
  - CAP-HAPTICS
  - CAP-SHARE
  - CAP-APPINFO
must_haves:
  truths:
    - Android Shell can receive share.invoke and launch the system intent.
    - Android Shell provides accurate App Info via PackageManager.
    - Android Shell correctly triggers haptic feedback on the WebView.
  artifacts:
    - path: examples/android_shell_host/app/src/main/java/dev/crosswake/shell/BridgeChannel.kt
      provides: SHARE_INVOKE command and delegation
    - path: examples/android_shell_host/app/src/main/java/dev/crosswake/shell/LiveViewFragment.kt
      provides: Native Android API implementations for the capabilities
  key_links:
    - from: examples/android_shell_host/app/src/main/java/dev/crosswake/shell/LiveViewFragment.kt
      to: examples/android_shell_host/app/src/main/java/dev/crosswake/shell/BridgeChannel.kt
      via: BridgeChannel constructor with injected handlers
      pattern: "shareHandler"
---

<objective>
Implement the base capability bridges for Android Shell (Haptics, Share, App Info).

Purpose: Connect the cross-platform bridge envelopes to native Android APIs (`Intent.ACTION_SEND`, `HapticFeedbackConstants`, `PackageManager`).
Output: Working native implementations for the three base capabilities in Android.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@examples/android_shell_host/app/src/main/java/dev/crosswake/shell/BridgeChannel.kt
@examples/android_shell_host/app/src/main/java/dev/crosswake/shell/LiveViewFragment.kt
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend BridgeChannel for Share Capability</name>
  <files>examples/android_shell_host/app/src/main/java/dev/crosswake/shell/BridgeChannel.kt</files>
  <action>
    Modify `BridgeCommand` enum to add `SHARE_INVOKE("share.invoke")`.
    In `BridgeChannel` constructor, add `private val shareHandler: (Map<String, String>) -> Unit`.
    In `evaluate(...)`, add the `BridgeCommand.SHARE_INVOKE` case. Perform the exact same `session.capabilities[command.capability]` validation as `HAPTICS_IMPACT`.
    On success, call `shareHandler(request.payload)` and return `ok(request, emptyMap())`.
  </action>
  <verify>
    <automated>grep -q "SHARE_INVOKE" examples/android_shell_host/app/src/main/java/dev/crosswake/shell/BridgeChannel.kt</automated>
  </verify>
  <done>BridgeChannel accepts and delegates share.invoke.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Android Native Capabilities</name>
  <files>examples/android_shell_host/app/src/main/java/dev/crosswake/shell/LiveViewFragment.kt</files>
  <action>
    Update `LiveViewFragment`'s `configureWebView` where `BridgeChannel` is instantiated:
    1. **App Info (`appInfoProvider`)**: Return a map with `version` (`packageInfo.versionName`), `build` (`packageInfo.longVersionCode` or `versionCode`), and `bundle_id` (`context.packageName`) fetched via `requireContext().packageManager`.
    2. **Haptics (`hapticsHandler`)**: Map the `"style"` string payload to `HapticFeedbackConstants` (e.g. `"heavy"` -> `LONG_PRESS`, `"light"` -> `KEYBOARD_TAP`, else `VIRTUAL_KEY`). Trigger via `webView.performHapticFeedback(...)` since `webView` is accessible in `configureWebView`.
    3. **Share (`shareHandler`)**: Extract `url`, `text`, `title`. Build an `Intent(Intent.ACTION_SEND)` with `type = "text/plain"`. Put the combined text in `EXTRA_TEXT` and title in `EXTRA_TITLE`. Start it via `startActivity(Intent.createChooser(...))`.
  </action>
  <verify>
    <automated>grep -q "Intent.ACTION_SEND" examples/android_shell_host/app/src/main/java/dev/crosswake/shell/LiveViewFragment.kt</automated>
  </verify>
  <done>Native implementations for all three capabilities are correctly wired.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Shell to OS | Shell initiating system intent |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-03 | Spoofing | Share Intent | mitigate | Limit Intent action strictly to ACTION_SEND with text/plain, ignoring any complex intent extras that could elevate privileges. |
</threat_model>

<verification>
- Verify `./gradlew assembleDebug` compiles successfully (or check code syntax).
- Ensure Haptics uses `webView.performHapticFeedback` to avoid `VIBRATE` permission requirements.
</verification>

<success_criteria>
- The Android shell builds successfully and the handlers execute the respective native Android APIs.
</success_criteria>

<output>
After completion, create `.planning/phases/15-base-capability-bridges/15-03-SUMMARY.md`
</output>
