---
phase: "15"
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/crosswake/bridge/registry.ex
  - lib/crosswake/bridge/commands/haptics.ex
  - lib/crosswake/bridge/commands/share.ex
  - lib/crosswake/bridge/commands/app_info.ex
autonomous: true
requirements:
  - CAP-HAPTICS
  - CAP-SHARE
  - CAP-APPINFO
must_haves:
  truths:
    - Elixir host correctly categorizes share.invoke as a valid capability.
    - Elixir host accepts valid capability payloads and rejects invalid ones for haptics, share, and app_info.
  artifacts:
    - path: lib/crosswake/bridge/registry.ex
      provides: Updated capability allowlist
    - path: lib/crosswake/bridge/commands/haptics.ex
      provides: Typed payload struct for haptics.impact
    - path: lib/crosswake/bridge/commands/share.ex
      provides: Typed payload struct for share.invoke
    - path: lib/crosswake/bridge/commands/app_info.ex
      provides: Typed payload struct for app.info.get
  key_links:
    - from: lib/crosswake/bridge/registry.ex
      to: share.invoke
      via: "@capability_commands"
      pattern: "\"share.invoke\" => \"share.invoke\""
---

<objective>
Implement the base Elixir capability models and registry allowlist for Haptics, Share, and App Info commands.

Purpose: Solidify payload serialization contracts on the Elixir host and ensure precise invocation logic before dispatching native APIs.
Output: Three new typed command modules and an updated bridge registry.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@lib/crosswake/bridge/registry.ex
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add share.invoke to capability registry</name>
  <files>lib/crosswake/bridge/registry.ex</files>
  <action>
    Add `"share.invoke" => "share.invoke"` to `@capability_commands` in `Crosswake.Bridge.Registry`.
    This enforces a fail-closed paradigm for the share command.
  </action>
  <verify>
    <automated>grep -q '"share.invoke" => "share.invoke"' lib/crosswake/bridge/registry.ex</automated>
  </verify>
  <done>share.invoke is mapped in @capability_commands.</done>
</task>

<task type="auto">
  <name>Task 2: Define capability payload structs</name>
  <files>
    lib/crosswake/bridge/commands/haptics.ex,
    lib/crosswake/bridge/commands/share.ex,
    lib/crosswake/bridge/commands/app_info.ex
  </files>
  <action>
    Create typed payload struct modules for the three base capabilities:
    1. `Crosswake.Bridge.Commands.Haptics.Request` with `style` (String.t(), e.g. "light", "medium", "heavy").
    2. `Crosswake.Bridge.Commands.Share.Request` with optional `url`, `text`, and `title` (all String.t() | nil).
    3. `Crosswake.Bridge.Commands.AppInfo.Request` (empty struct) and `Crosswake.Bridge.Commands.AppInfo.Response` with `version`, `build`, `bundle_id` (all String.t()).

    Ensure strong typing with `@type` and `defstruct`. Enforce keys where appropriate (e.g. `style` for haptics, `version`, `build`, `bundle_id` for app info response).
    Analog reference: `lib/crosswake/bridge/contract.ex`
  </action>
  <verify>
    <automated>mix format lib/crosswake/bridge/commands/*.ex && mix compile</automated>
  </verify>
  <done>Typed payload modules created and project compiles successfully.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client API to Host | Capability dispatch from remote clients (native shells) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-01 | Elevation of Privilege | Registry Allowlist | mitigate | Explicitly allowlist share.invoke to reject unmapped commands, maintaining a strict fail-closed boundary. |
</threat_model>

<verification>
- Verify `mix test` passes without breaking existing router/bridge tests.
- Verify `share.invoke` is listed as supported.
</verification>

<success_criteria>
- The registry explicitly allows the three base capabilities (`app.info.get`, `haptics.impact`, `share.invoke`).
- The payload structs strictly enforce the data contracts.
</success_criteria>

<output>
After completion, create `.planning/phases/15-base-capability-bridges/15-01-SUMMARY.md`
</output>
