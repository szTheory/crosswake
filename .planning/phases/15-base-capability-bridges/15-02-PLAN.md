---
phase: "15"
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - examples/ios_shell_host/CrosswakeShell/BridgeChannel.swift
  - examples/ios_shell_host/CrosswakeShell/LiveViewContainerViewController.swift
autonomous: true
requirements:
  - CAP-HAPTICS
  - CAP-SHARE
  - CAP-APPINFO
must_haves:
  truths:
    - iOS Shell can receive share.invoke and display the system share sheet.
    - iOS Shell provides accurate App Info via bundle introspection.
    - iOS Shell correctly triggers haptic feedback based on requested style.
  artifacts:
    - path: examples/ios_shell_host/CrosswakeShell/BridgeChannel.swift
      provides: shareInvoke command and delegation
    - path: examples/ios_shell_host/CrosswakeShell/LiveViewContainerViewController.swift
      provides: Native iOS API implementations for the capabilities
  key_links:
    - from: examples/ios_shell_host/CrosswakeShell/LiveViewContainerViewController.swift
      to: examples/ios_shell_host/CrosswakeShell/BridgeChannel.swift
      via: BridgeChannel initialization with injected handlers
      pattern: "shareHandler"
---

<objective>
Implement the base capability bridges for iOS Shell (Haptics, Share, App Info).

Purpose: Connect the cross-platform bridge envelopes to native iOS APIs (`UIActivityViewController`, `UIImpactFeedbackGenerator`, `Bundle`).
Output: Working native implementations for the three base capabilities in iOS.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@examples/ios_shell_host/CrosswakeShell/BridgeChannel.swift
@examples/ios_shell_host/CrosswakeShell/LiveViewContainerViewController.swift
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend BridgeChannel for Share Capability</name>
  <files>examples/ios_shell_host/CrosswakeShell/BridgeChannel.swift</files>
  <action>
    Modify `BridgeCommand` to add `case shareInvoke = "share.invoke"`.
    In `BridgeChannel` initializer, add a new injected handler: `shareHandler: @escaping ([String: String]) -> Void`.
    In `userContentController(...)`, add `case .shareInvoke:` handler block. Perform the exact same `session.capabilities[command.capability]` validation as `hapticsImpact`.
    On success, call `shareHandler(request.payload)` and reply via `replySink(ok(request, payload: [:]))`.
  </action>
  <verify>
    <automated>grep -q "case shareInvoke" examples/ios_shell_host/CrosswakeShell/BridgeChannel.swift</automated>
  </verify>
  <done>BridgeChannel accepts and delegates share.invoke.</done>
</task>

<task type="auto">
  <name>Task 2: Implement iOS Native Capabilities</name>
  <files>examples/ios_shell_host/CrosswakeShell/LiveViewContainerViewController.swift</files>
  <action>
    Update `LiveViewContainerViewController`'s initialization of `BridgeChannel` to pass real closures instead of mocks:
    1. **App Info (`appInfoProvider`)**: Return a dictionary with `version` (`CFBundleShortVersionString`), `build` (`CFBundleVersion`), and `bundle_id` (`CFBundleIdentifier`) from `Bundle.main.infoDictionary`.
    2. **Haptics (`hapticsHandler`)**: Map the `"style"` string payload (`"light"`, `"medium"`, `"heavy"`) to `UIImpactFeedbackStyle` and call `UIImpactFeedbackGenerator(style: ...).impactOccurred()`.
    3. **Share (`shareHandler`)**: Extract `url`, `text`, and `title` from the payload. Combine text and url or pass URL objects appropriately to an array. Present a `UIActivityViewController`. *Crucially, handle iPad crashes by setting `popoverPresentationController.sourceView = self.view` and `sourceRect` before presenting.*
  </action>
  <verify>
    <automated>grep -q "UIActivityViewController" examples/ios_shell_host/CrosswakeShell/LiveViewContainerViewController.swift</automated>
  </verify>
  <done>Native implementations for all three capabilities are correctly wired.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Shell to Host | Shell processing payload strings into UI elements |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-02 | Spoofing | Share UI | mitigate | Share URLs/strings are passed natively without executing or rendering them in web contexts. |
</threat_model>

<verification>
- Verify xcodebuild or `swift build` passes if applicable (or manually verify the syntax).
- Ensure iPad popover source view is provided.
</verification>

<success_criteria>
- The iOS shell builds successfully and the handlers execute the respective native Apple APIs.
</success_criteria>

<output>
After completion, create `.planning/phases/15-base-capability-bridges/15-02-SUMMARY.md`
</output>
