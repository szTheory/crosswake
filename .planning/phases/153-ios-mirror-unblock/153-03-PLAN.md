---
phase: 153-ios-mirror-unblock
plan: 03
type: execute
wave: 3
depends_on: ["153-01"]
files_modified:
  - .github/workflows/release-please.yml
  - script/check_release_workflow_integrity.exs
  - test/crosswake/proof/phase153_ios_mirror_unblock_test.exs
autonomous: true
requirements: [MIRROR-02]
user_setup: []

must_haves:
  truths:
    - "D-03/D-04: `publish-ios-core` checks out with `persist-credentials: false` and pushes over SSH with MIRROR_DEPLOY_KEY — the extraheader hijack cannot reach the mirror remote."
    - "D-11: `publish-ios-core` splits from `ref: ${{ needs.release-please.outputs.tag_name }}` with `fetch-depth: 0`, so a retroactive `autorelease: pending` release can never publish a NEWER tree under a correct-looking tag."
    - "D-12: `publish-ios-core` gates on `needs: [release-please, publish-hex]` and NOT on `publish-android-core` — least-recoverable-registry-last, without fighting the `native_proof_decoupled` invariant."
    - "D-13: the mirror push is ONE atomic command carrying both `main` and the tag, with an explicit lease scoped to `main` alone — no partial state where `main` advances and the tag never lands."
    - "D-14: `permissions: contents: read` on `publish-ios-core` is UNCHANGED — it scopes GITHUB_TOKEN only; the push authenticates via the deploy key."
    - "D-15: `release-failure-alert.needs` includes the four native jobs plus the rollup, so a mirror-push failure opens a tracking issue instead of nothing."
    - "D-17: `native-release-rollup` exits 1 when native_core is not complete, after writing its artifact, so a partial release stops claiming success."
    - "D-02: the 4th touch point — `android-publish-fire-drill`'s secret preflight — is updated, so retiring the old secret does not break an unrelated job."
    - "D-20: every one of these fixes has a matching invariant in check_release_workflow_integrity.exs with decoy tests."
  artifacts:
    - .github/workflows/release-please.yml
    - script/check_release_workflow_integrity.exs
    - test/crosswake/proof/phase153_ios_mirror_unblock_test.exs
  key_links:
    - "release-please.tag_name -> publish-ios-core checkout ref -> splitsh-lite split -> atomic leased push"
    - "publish-hex -> publish-ios-core (needs gate) -> clean-room-proof-ios -> native-release-rollup -> release-failure-alert -> gh issue create"
---

<objective>
Fix the release job itself so that a future release reaches Hex, Maven, and the iOS mirror in one coordinated pass, and so that a mirror-push failure lands somewhere a human will actually see it.

Purpose: **MIRROR-02.** Plan 02 repaired the current damage; this plan stops it recurring. Two facts drive every decision here: (1) `publish-ios-core` ALREADY hard-fails — it was red for ~3 months and nobody saw it, so "make it louder" is a non-fix; the variable is WHERE red lands, and the only surface with persistence and an inbox is the GitHub issues list. (2) The mirror git tag is, by this project's own policy, the LEAST recoverable of the three registries — and it currently races the most recoverable one.

Output: a release lane that cannot publish the wrong tree, cannot leave a partial state, cannot fail silently, and cannot regress without failing CI.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/153-ios-mirror-unblock/153-CONTEXT.md
@.planning/phases/153-ios-mirror-unblock/153-RESEARCH.md
@.planning/phases/153-ios-mirror-unblock/153-PATTERNS.md
@.planning/phases/153-ios-mirror-unblock/153-01-SUMMARY.md
</context>

<artifacts_this_phase_produces>
| Symbol | Kind | Location |
|---|---|---|
| `release.ios.checkout_ref_pinned` | new scanner check id (D-11/D-20) | `script/check_release_workflow_integrity.exs` |
| `release.ios.ssh_transport` | new scanner check id (D-03/D-04) — REPLACES `release.mirror_token.preflight` | `script/check_release_workflow_integrity.exs` |
| `release.ios.hex_gated` | new scanner check id (D-12) | `script/check_release_workflow_integrity.exs` |
| `release.ios.atomic_leased_push` | new scanner check id (D-13) — REPLACES `release.mirror_token.write_preflight` | `script/check_release_workflow_integrity.exs` |
| `release.workflow.native_rollup_fails_closed` | new scanner check id (D-17) | `script/check_release_workflow_integrity.exs` |
| `release.workflow.release_failure_alert_native` | new scanner check id (D-15) | `script/check_release_workflow_integrity.exs` |
| `release_ios_checkout_ref_pinned/1`, `release_ios_ssh_transport/1`, `release_ios_hex_gated/1`, `release_ios_atomic_leased_push/1`, `native_rollup_fails_closed/1`, `release_failure_alert_native/1` | new checker functions | `script/check_release_workflow_integrity.exs` |
| `MIRROR_DEPLOY_KEY` | secret name, now referenced from `publish-ios-core` and `android-publish-fire-drill` | `.github/workflows/release-please.yml` |

RETIRED here: scanner check ids `release.mirror_token.preflight`, `release.workflow.mirror_token_preflight`, `release.mirror_token.write_preflight` — these assert on HTTPS-token machinery that no longer exists. They are REWRITTEN, not renamed; a rename would leave them asserting a mechanism that literally disappeared.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: publish-ios-core — SSH transport, tag-pinned checkout, Hex gate, atomic leased push</name>

  <read_first>
    - `.github/workflows/release-please.yml` lines 386-450 (the whole `publish-ios-core` job: checkout at 394-396 with NO `ref:` and no `persist-credentials`; the HTTPS remote at 425; the two separate non-atomic pushes at 449-450)
    - `.github/workflows/release-please.yml` lines 452-479 (`publish-android-core` — it uses `ref: tag_name` but has NO `fetch-depth: 0`. Read it to understand the pattern, then DO NOT copy its checkout block verbatim: dropping `fetch-depth: 0` silently hands `splitsh-lite` a shallow clone. This is D-11's stated trap.)
    - `.planning/phases/153-ios-mirror-unblock/153-RESEARCH.md` §Q1 "Definitive recommendation for D-13" (the exact push command, empirically verified) and §Q3 (the exact SSH block)
    - `script/verify_ios_mirror_backfill.sh` (post-plan-01 — it already encodes the exact-ref discipline this job violates: it REJECTS `main`/`master`/`HEAD`/`refs/heads/*` and demands an exact `refs/tags/ios-core-v${VERSION}`. The backfill script has been right all along; the release job is the one that's wrong.)
  </read_first>

  <files>.github/workflows/release-please.yml</files>

  <action>
    Edit the `publish-ios-core` job (lines 386-450). Four changes.

    **(a) D-12 — gate on Hex.** Change `needs: release-please` to `needs: [release-please, publish-hex]`.

    Rationale, so you do not "improve" it later: Hex is revertible for 60 minutes; Maven Central is permanently immutable once PUBLISHED; the mirror git tag is immutable by this project's own policy. The mirror is therefore the LEAST recoverable registry, and it currently races the MOST recoverable one. Gating cannot create a new bad state — it can only convert the irreversible one (mirror tag live for a version with no Hex package) into the reversible one (Hex failed, nothing published, re-run).

    ⚠️ Do NOT add `needs: publish-android-core`. `script/check_release_workflow_integrity.exs`'s `native_proof_decoupled` check DELIBERATELY requires the iOS lane not to depend on Android — coupling them would fight an encoded decision and let an Android flake block a recoverable mirror push. Gate on `publish-hex` ONLY. This is safe precisely because the release-please linked-versions group is asserted to be exactly `{hex, ios-core, android-core}`, so `publish-hex` cannot silently skip.

    ⚠️ Do NOT touch `permissions: contents: read` (lines 391-392). It scopes `GITHUB_TOKEN` only; the push authenticates via the deploy key. Raising it would be strictly worse. This is recorded explicitly (D-14) so it is not "fixed" by a well-meaning drive-by.

    **(b) D-11 — pin the split to the release tag.** The checkout currently has NO `ref:`, so `splitsh-lite` splits from `github.sha`. This is not a "main advances" race — checkout pins to the event SHA. The real path is release-please's retroactive `autorelease: pending` behavior: if the release step fails or is cancelled, release-please creates the Release and tags on a LATER push, where `github.sha` is a newer, unrelated commit. `publish-hex` and `publish-android-core` (which uses `ref: tag_name`) would build the correct tree while `publish-ios-core` splits the NEWER tree and publishes it under tag `v0.2.0` — wrong content under a correct-looking tag, silently, on the one registry whose tags this project forbids moving. This repo has already been on that recovery path.

    Add `ref: ${{ needs.release-please.outputs.tag_name }}` and KEEP `fetch-depth: 0`. Both. `splitsh-lite` needs full history.

    **(c) D-03/D-04 — SSH transport.** Replace the checkout + credential setup with RESEARCH §Q3's verified three-step block, SHA-pinned per this repo's dominant discipline:

    ```
    - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
      with:
        ref: ${{ needs.release-please.outputs.tag_name }}
        fetch-depth: 0
        persist-credentials: false

    - uses: webfactory/ssh-agent@e83874834305fe9a4a2997156cb26c5de65a8555 # v0.10.0
      with:
        ssh-private-key: ${{ secrets.MIRROR_DEPLOY_KEY }}

    - name: Add GitHub to known_hosts
      run: |
        mkdir -p ~/.ssh
        ssh-keyscan -t ed25519 github.com >> ~/.ssh/known_hosts
    ```

    Then in the push step, replace the HTTPS remote at line 425 with:

    ```
    git remote add mirror git@github.com:szTheory/crosswake-shell-core-ios.git
    ```

    Replace the old secret's env binding (line 413) with a `MIRROR_DEPLOY_KEY`-presence preflight: fail fast with a named message if the deploy key secret is empty. Keep the `VERSION: ${{ needs.release-please.outputs.version }}` env and its existing comment — SwiftPM resolves on a bare semver tag, and the component-prefixed release-please `tag_name` would NOT resolve, so the mirror tag must stay derived from the plain `version` output.
    <!-- planner-discipline-allow: MIRROR_PUSH_TOKEN -->

    Delete the `git ls-remote mirror HEAD` "read check" and its failure message. It is worse than useless: the mirror is a PUBLIC repo, so that probe succeeds anonymously regardless of the credential. It is not a permission gate, and it is one of the things that made the original 403 look like a token problem. The dry-run push below is the real gate. Never `cat`/`echo` the deploy key in any step.

    Keep the tag-SHA-equality short-circuit at 432-437 exactly as it is (idempotent re-run: "already points at … no push needed"; fail-closed on `points at <X>, expected <Y>`). That is D-10 and it is correct.

    **(d) D-13 — one atomic push with an explicit lease.** Today the `--dry-run` probes both refspecs in ONE command but the real push is TWO separate commands (`main`, then the tag), so a tag-push failure leaves mirror `main` advanced with no `v0.2.0` tag — a partial state on the exact surface MIRROR-02 exists to eliminate. Replace both push lines (449-450) with this exact form. **Copy it character-for-character. A paraphrase here silently re-arms the fuse.**

    ```
    CURRENT_MAIN_SHA="$(git ls-remote mirror refs/heads/main | awk '{print $1}' | head -1)"
    if [ -n "$CURRENT_MAIN_SHA" ]; then
      git push --atomic mirror \
        --force-with-lease="refs/heads/main:${CURRENT_MAIN_SHA}" \
        "${SPLIT_SHA}:refs/heads/main" \
        "${SPLIT_SHA}:refs/tags/v${VERSION}"
    else
      git push --atomic mirror \
        "${SPLIT_SHA}:refs/heads/main" \
        "${SPLIT_SHA}:refs/tags/v${VERSION}"
    fi
    ```

    Three things about this that are NOT negotiable:

    1. **The explicit-lease form is the ONLY form that works here.** A bare `--force-with-lease`, and the named-without-expect form `--force-with-lease=refs/heads/main`, BOTH require a remote-tracking ref (`refs/remotes/mirror/main`) to compare against — and this job never runs `git fetch mirror`. Git treats "no known remote-tracking value" as "expect the ref not to exist" and rejects with `stale info`, on every single run. RESEARCH verified this empirically. Writing the bare form here would produce a THIRD misleading failure mode layered on the two this repo has already suffered.
    2. **Do NOT add a `git fetch mirror` step.** The explicit-lease form works even when `<expect>` is an object that does not exist locally — verified directly against the real mirror, whose `main` (`6417ae65…`) is unknown to this repo.
    3. **The lease is scoped to `main` ALONE, deliberately.** That leaves the tag refspec genuinely unforced INSIDE the same atomic push, so an existing published tag is protected by git itself, not merely by policy (D-10). A bare lease would have applied to the tag too — actively dangerous. This is load-bearing; do not "simplify" it.

    Keep a `git push --dry-run --porcelain` of the same atomic form immediately before the real push. Fix its failure message: it currently reports every failure as "cannot push", which misattributes a lineage problem to a credential problem — a single message that has now caused TWO misdiagnoses. Emit a message that distinguishes auth failure from non-fast-forward/lease failure from unknown object, names the adopter impact, and gives one command to fix.
  </action>

  <verify>
    <automated>python3 -c "import yaml,sys; yaml.safe_load(open('.github/workflows/release-please.yml')); print('yaml ok')" && elixir script/check_release_workflow_integrity.exs</automated>
  </verify>

  <acceptance_criteria>
    - `.github/workflows/release-please.yml` parses as valid YAML.
    - In the `publish-ios-core` job block: `needs` contains both `release-please` and `publish-hex`, and does NOT contain `publish-android-core` (D-12).
    - `grep -c 'ref: \${{ needs.release-please.outputs.tag_name }}' .github/workflows/release-please.yml` returns ≥ 2 (publish-ios-core plus the pre-existing publish-android-core).
    - The `publish-ios-core` checkout carries BOTH `ref:` and `fetch-depth: 0` and `persist-credentials: false` (D-11 trap avoided).
    - `permissions: contents: read` on `publish-ios-core` is UNCHANGED (D-14).
    - `grep -c 'force-with-lease="refs/heads/main:${CURRENT_MAIN_SHA}"' .github/workflows/release-please.yml` returns 1.
    - `grep -c 'push --atomic mirror' .github/workflows/release-please.yml` returns ≥ 2 (the dry-run and the real push).
    - `grep -vE '^\s*#' .github/workflows/release-please.yml | grep -c -- '--force-with-lease=refs/heads/main[^:]'` returns 0 (the broken form is absent).
    - `grep -vE '^\s*#' .github/workflows/release-please.yml | grep -c 'git fetch mirror'` returns 0 (no fetch was added — the lease works without it).
    - `grep -vE '^\s*#' .github/workflows/release-please.yml | grep -c 'x-access-token'` returns 0.
    - `grep -c 'webfactory/ssh-agent@e83874834305fe9a4a2997156cb26c5de65a8555' .github/workflows/release-please.yml` returns 1.
    - `grep -c 'git ls-remote mirror HEAD' .github/workflows/release-please.yml` returns 0 (the useless anonymous read-check is gone).
    - The two separate sequential pushes of `${SPLIT_SHA}:refs/heads/main` and `${SPLIT_SHA}:refs/tags/v${VERSION}` no longer exist as distinct `git push` commands — they appear only inside the atomic command.
  </acceptance_criteria>

  <done>
    `publish-ios-core` splits the correct tree, pushes it over an unhijackable transport, gates behind the recoverable registry, and lands both refs or neither.
  </done>
</task>

<task type="auto">
  <name>Task 2: Escalation and secret retirement across the rest of release-please.yml</name>

  <read_first>
    - `.github/workflows/release-please.yml` lines 593-701 (`native-release-rollup` — computes `native_core=partial`, renders it, and exits 0; the `next_action` string at 658; the artifact upload at 695-701)
    - `.github/workflows/release-please.yml` lines 1213-1266 (`release-failure-alert` — its `needs:` lists TEN companion jobs and zero native jobs; the "Job results" echo block starting ~1249)
    - `.github/workflows/release-please.yml` lines 703-750 (`android-publish-fire-drill` — the "8 required secrets" preflight. RESEARCH found this 4th touch point; CONTEXT implies only 3. It has NOTHING to do with iOS — the old secret is one of 8 checked for presence only — but retiring that secret without updating this list makes an unrelated fire-drill job fail with `MISSING SECRET`.)
    - `.planning/phases/153-ios-mirror-unblock/153-PATTERNS.md` §`.github/workflows/release-please.yml` — MODIFY (the exact current-state excerpts)
  </read_first>

  <files>.github/workflows/release-please.yml</files>

  <action>
    **(a) D-15 — extend `release-failure-alert` to the native jobs. This is the persistence surface, and it already exists.** Its `needs:` currently lists only the ten companion jobs: a companion failure opens a tracking issue; a mirror-push failure opens NOTHING. The surface that actually broke is the only one with no alerting — PROOF-03c was built for companions and never extended to native. That is why a red release went unnoticed for three months.

    Add these five to `release-failure-alert.needs`: `publish-ios-core`, `clean-room-proof-ios`, `publish-android-core`, `clean-room-proof-android`, `native-release-rollup`. Extend the "Job results" echo block with a matching `- <job>: ${{ needs.<job>.result }}` line for each, following the existing `${{ needs.publish-hex-rulestead.result }}` shape exactly. Update the job's `name:` and the issue title/body so they no longer claim the failure is necessarily a *companion* failure.

    `if: ${{ failure() }}` stays as-is — skipped jobs (no native package released this run) are not failures, so this remains dormant on the happy path. Red now lands in the issues list and the maintainer's inbox: the only surface with persistence.

    **(b) D-17 — `native-release-rollup` must exit 1 when the release is partial.** It currently computes `native_core=partial`, renders it to the step summary and the JSON artifact, and exits 0 — so MIRROR-02's "hard named failure" is literally unbuilt and a partial release still CLAIMS success.

    After the JSON artifact is written and the step summary is emitted, exit 1 when `native_core` is not `complete` AND at least one native package was released this run (`native_core=none` — nothing native released — must stay exit 0; that is the normal companion-only release path).

    Two ordering constraints, both load-bearing:
    - Write `native-release-status.json` and the step summary BEFORE exiting, or you lose the very diagnostic you just failed on.
    - Give the upload-artifact step `if: always()` so it still runs after the failing step. (`always()` on the JOB governs whether the job runs, not its exit code — so the existing `release.workflow.native_rollup_summary` invariant continues to pass.)

    Per D-02, this is explicitly NOT trusted to reach a human. It exists to stop the release LYING, not to alarm. The alarm is (a).

    **(c) Retire the old secret name across its remaining touch points.**
    <!-- planner-discipline-allow: MIRROR_PUSH_TOKEN -->
    - `native-release-rollup`'s `next_action` string (line 658) names the retired secret. Update it to name `MIRROR_DEPLOY_KEY` and the backfill workflow. Note `script/check_release_workflow_integrity.exs`'s `native_rollup_summary` check asserts on this EXACT substring — task 3 updates it in lockstep. Change both or CI breaks.
    - `android-publish-fire-drill`'s secret preflight (lines ~728/739/746): swap the retired name for `MIRROR_DEPLOY_KEY` in the `env:` block AND in the `for var in ... ; do` list. The count stays 8 — it is the same number of secrets, one renamed. Miss this and an unrelated Android fire-drill starts failing the moment the old secret is deleted.

    Do NOT SHA-pin `actions/upload-artifact@v4` (line 696) as a drive-by. It is a deliberately tracked exception: `native_status_artifact` asserts the literal string `actions/upload-artifact@v4`, so pinning it requires a lockstep checker edit that is out of scope here.
  </action>

  <verify>
    <automated>python3 -c "import yaml,sys; yaml.safe_load(open('.github/workflows/release-please.yml')); print('yaml ok')" && elixir script/check_release_workflow_integrity.exs</automated>
  </verify>

  <acceptance_criteria>
    - `.github/workflows/release-please.yml` parses as valid YAML.
    - `release-failure-alert`'s `needs:` list contains all five of `publish-ios-core`, `clean-room-proof-ios`, `publish-android-core`, `clean-room-proof-android`, `native-release-rollup`.
    - The alert's "Job results" echo block contains a `needs.<job>.result` expression for each of those five jobs.
    - The `native-release-rollup` job block contains an `exit 1` guarded on `native_core` not being `complete`, and that guard appears AFTER the `native-release-status.json` heredoc and after the `$GITHUB_STEP_SUMMARY` write.
    - The upload-artifact step in `native-release-rollup` carries `if: always()`.
    - `grep -c 'actions/upload-artifact@v4' .github/workflows/release-please.yml` returns 1 (not SHA-pinned — the tracked exception is preserved).
    - `grep -rn 'MIRROR_PUSH_TOKEN' .github/workflows/ script/` returns ZERO matches across the whole repo — all four touch points (publish-ios-core, native-release-rollup's next_action, android-publish-fire-drill's preflight, ios-mirror-backfill.yml) plus the checker are clean.
    - `grep -c 'MIRROR_DEPLOY_KEY' .github/workflows/release-please.yml` returns ≥ 3 (publish-ios-core env, rollup next_action, fire-drill preflight).
    - `elixir script/check_release_workflow_integrity.exs` exits 0 (requires task 3's lockstep checker edits — if run before task 3, expect the `native_rollup_summary` substring assertion to fail; that is the expected intermediate state).
  </acceptance_criteria>

  <done>
    A native release failure now opens a tracking issue. A partial native release now exits non-zero instead of claiming success. The retired secret name is gone from all four touch points, including the one CONTEXT missed.
  </done>
</task>

<task type="auto">
  <name>Task 3: Rewrite the mirror-token scanner checks into SSH/atomic invariants, add the D-11/D-12/D-15/D-17 checks, extend the decoy tests</name>

  <read_first>
    - `script/check_release_workflow_integrity.exs` lines 60-115 (the `checks = [...]` registry — a checker that is defined but not registered here NEVER RUNS)
    - `script/check_release_workflow_integrity.exs` lines 751-846 (`workflow_native_proof_decoupled/1` — the canonical `job_needs?`/`job_if` shape to copy; `mirror_token_preflight/1`, `workflow_mirror_token_preflight/1`, `mirror_token_write_preflight/1` — the three to REWRITE; `native_rollup_summary/1` — the substring that must move in lockstep with task 2c; `native_status_artifact/1` — do not disturb)
    - `test/crosswake/proof/phase142_release_integrity_test.exs` lines 1-90 (the `@phaseNNN_ids ~w(...)` + decoy house style)
    - `test/crosswake/proof/phase153_ios_mirror_unblock_test.exs` (post-plan-01 — extend `@phase153_ids` here)
  </read_first>

  <files>script/check_release_workflow_integrity.exs, test/crosswake/proof/phase153_ios_mirror_unblock_test.exs</files>

  <action>
    Every fix in tasks 1-2 gets a matching invariant. This is the repo's established pattern — every release invariant has a checker function with decoy tests — and it is what stops the fix from regressing (D-20).

    **(a) REWRITE, do not rename, the three mirror-token checks.** All three assert on machinery that literally disappeared when SSH transport landed. A find-and-replace on the secret name would leave them asserting a mechanism that no longer exists — a check that passes while proving nothing is worse than no check.

    - `mirror_token_preflight/1` and `workflow_mirror_token_preflight/1` both assert `includes?(block, "git ls-remote mirror HEAD")` (a probe task 1 DELETED — it succeeds anonymously on a public repo and proves nothing) and the old secret's fail-fast string. Collapse these two duplicates into ONE check, `release.ios.ssh_transport`, asserting the `publish-ios-core` block contains: `persist-credentials: false`, the SHA-pinned `webfactory/ssh-agent` line, an `ssh-keyscan` step, the `git@github.com:szTheory/crosswake-shell-core-ios.git` SSH remote, a `MIRROR_DEPLOY_KEY` presence preflight that fails fast, and NO `x-access-token` substring.
    - `mirror_token_write_preflight/1` asserts the TWO-refspec dry-run shape from the old two-command implementation. Rewrite as `release.ios.atomic_leased_push`, asserting the block contains `push --atomic mirror`, the exact substring `--force-with-lease="refs/heads/main:${CURRENT_MAIN_SHA}"`, both refspecs, a `--dry-run --porcelain` probe of the same atomic form, and — critically — that the block does NOT contain a bare/named-without-expect `--force-with-lease` form. That negative clause is the one that would have caught the fuse.

    Remove the three retired ids from the `checks = [...]` registry and add the new ones. Also remove them from any `@phase145_*_ids` list in `phase142_release_integrity_test.exs` that references them, or that test will fail looking for ids that no longer exist.

    **(b) Add four NEW checks**, each in the established `defp <name>(jobs) do block = job_block(jobs, "<job-id>"); check("<dotted.id>", <boolean>, "<message ending with 'run elixir script/check_release_workflow_integrity.exs'>") end` shape, and REGISTER each in the `checks` list:

    - `release.ios.checkout_ref_pinned` (D-11/D-20) — asserts the `publish-ios-core` block contains BOTH `ref: ${{ needs.release-please.outputs.tag_name }}` AND `fetch-depth: 0`. Both clauses are required: the trap is copying `publish-android-core`'s checkout, which has the ref but drops the depth, silently handing `splitsh-lite` a shallow clone.
    - `release.ios.hex_gated` (D-12) — uses `job_needs?/3` to assert `publish-ios-core` needs `release-please` AND `publish-hex` AND does NOT need `publish-android-core`. Mirror the `workflow_native_proof_decoupled/1` shape exactly.
    - `release.workflow.native_rollup_fails_closed` (D-17) — asserts the `native-release-rollup` block contains an `exit 1` gated on non-`complete` `native_core`, and that its upload step carries `if: always()`.
    - `release.workflow.release_failure_alert_native` (D-15) — uses `job_needs?/3` to assert `release-failure-alert` needs all five of `publish-ios-core`, `clean-room-proof-ios`, `publish-android-core`, `clean-room-proof-android`, `native-release-rollup`, and that the block echoes each of their `.result` expressions.

    **(c) Lockstep fix for `native_rollup_summary/1`.** Its assertion on the old `next_action` substring (line ~830) must move to whatever task 2c wrote. Re-read the file and match the actual string; do not guess.

    Line numbers throughout this task are pre-edit and WILL drift as you edit. Re-grep after every edit; do not carry cumulative line numbers across a multi-edit session.

    **(d) Extend `@phase153_ids`** in `test/crosswake/proof/phase153_ios_mirror_unblock_test.exs` with all six new ids, following phase142's decoy pattern (assert each id is emitted `:ok` by the scanner, and that a deliberately-broken fixture makes it fail). The decoys are the point: an invariant with no decoy test can silently become vacuous.
  </action>

  <verify>
    <automated>elixir script/check_release_workflow_integrity.exs && mix test</automated>
  </verify>

  <acceptance_criteria>
    - `elixir script/check_release_workflow_integrity.exs` exits 0.
    - Its output contains all six new ids: `release.ios.ssh_transport`, `release.ios.atomic_leased_push`, `release.ios.checkout_ref_pinned`, `release.ios.hex_gated`, `release.workflow.native_rollup_fails_closed`, `release.workflow.release_failure_alert_native` — i.e. all six are REGISTERED in the `checks` list, not merely defined.
    - Its output contains NONE of `release.mirror_token.preflight`, `release.workflow.mirror_token_preflight`, `release.mirror_token.write_preflight` (retired, not renamed).
    - `grep -rn 'MIRROR_PUSH_TOKEN' script/ .github/` returns zero matches.
    - `grep -c 'ls-remote mirror HEAD' script/check_release_workflow_integrity.exs` returns 0 (the check no longer asserts on the deleted anonymous read-probe).
    - `@phase153_ids` in `test/crosswake/proof/phase153_ios_mirror_unblock_test.exs` lists all six new ids plus the three from plan 01 (nine total), each with a decoy test.
    - `mix test` full suite green — including `phase142_release_integrity_test.exs` and `phase145_ios_backfill_script_test.exs`.
    - `mix test --only phase153_ios_mirror_unblock` green.
  </acceptance_criteria>

  <done>
    Every fix in this plan is guarded by a structural invariant that fails CI on regression, and every invariant has a decoy proving it is not vacuous.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GitHub Actions runner → `szTheory/crosswake-shell-core-ios` | Cross-repo push at release time; the credential is the whole attack surface |
| `actions/checkout` workspace `.git/config` → the `mirror` remote added later in the same job | An extraheader written by checkout applies to remotes checkout never saw |
| release-please's `tag_name` output → the tree that gets published under a public, immutable tag | A wrong `ref:` publishes wrong content under a correct-looking tag |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-153-01 | Spoofing / EoP | `actions/checkout` in `publish-ios-core` writing `http.https://github.com/.extraheader`, which has no username component and therefore matches EVERY `https://github.com/*` remote — beating the URL userinfo and authenticating the mirror push as `github-actions[bot]` | high | mitigate | `persist-credentials: false` (task 1c) plus SSH transport, which an `http.*` extraheader structurally cannot target. Asserted by `release.ios.ssh_transport`, with a decoy test. |
| T-153-02 | Information Disclosure | `MIRROR_DEPLOY_KEY` leaked into job logs by a step that echoes or cats it | high | mitigate | Loaded exclusively by `webfactory/ssh-agent`, which never prints it. No step may `cat`/`echo` the key. GitHub log secret-masking is defense in depth. |
| T-153-03 | Tampering | Moving an already-published, adopter-resolved SwiftPM tag (SwiftPM caches aggressively) | high | mitigate | Structural: the lease is scoped to `refs/heads/main` ALONE, so the tag refspec stays unforced even inside the atomic multi-ref push, and `refs/tags/*` rejects updates to existing refs absent an explicit force on that exact refspec. The SHA-equality short-circuit fail-closes on mismatch. Asserted by `release.ios.atomic_leased_push`. |
| T-153-04 | Tampering | A stale lease value silently overwriting concurrent legitimate mirror state, OR a partial push leaving `main` advanced with no tag | high | mitigate | `--atomic` + explicit-lease: a lease mismatch fails the ENTIRE transaction with no partial apply. Empirically verified. |
| T-153-07 | Tampering | Publishing a NEWER tree under tag `v0.2.0` via release-please's retroactive `autorelease: pending` path — wrong content under a correct-looking tag, on the least-recoverable registry | high | mitigate | `ref: ${{ needs.release-please.outputs.tag_name }}` with `fetch-depth: 0` (D-11). Asserted by `release.ios.checkout_ref_pinned`, whose two-clause condition specifically catches the shallow-clone trap. |
| T-153-08 | Repudiation | A native release fails and NOTHING records it — the release run reports success while adopters are broken (the actual 3-month incident) | high | mitigate | `release-failure-alert.needs` extended to the four native jobs + rollup, so failure opens a tracking issue in the only surface with persistence (D-15); `native-release-rollup` exits 1 on partial (D-17) so the release stops claiming success. Asserted by `release.workflow.release_failure_alert_native` and `release.workflow.native_rollup_fails_closed`. |
| T-153-SC | Tampering | Supply chain — `webfactory/ssh-agent` | medium | mitigate | SHA-pinned to `e83874834305fe9a4a2997156cb26c5de65a8555` (v0.10.0), matching this repo's action-pinning discipline. No `[SLOP]`/`[SUS]` findings in RESEARCH's legitimacy audit. No package-manager installs occur in this plan. |
</threat_model>

<verification>
- `elixir script/check_release_workflow_integrity.exs` exits 0 with all six new ids `:ok` and all three retired ids gone.
- `mix test` full suite green.
- `grep -rn 'MIRROR_PUSH_TOKEN' .github/ script/` returns nothing — all four touch points retired.
- `.github/workflows/release-please.yml` parses as valid YAML.
</verification>

<success_criteria>
MIRROR-02's release-time half: one release run publishes Hex, Maven, and the mirror in a coordinated pass; the mirror push is gated behind the recoverable registry, splits the correct tree, and lands both refs or neither; a missing or invalid deploy key fails CI with a hard, named error; and a native failure opens a tracking issue in the maintainer's inbox instead of dying silently in a post-merge Actions run nobody watches.
</success_criteria>

<output>
Create `.planning/phases/153-ios-mirror-unblock/153-03-SUMMARY.md` when done. Record: the final atomic push command as landed, the full `needs:` list of `release-failure-alert`, and confirmation that the retired secret name appears nowhere in `.github/` or `script/`.
</output>
