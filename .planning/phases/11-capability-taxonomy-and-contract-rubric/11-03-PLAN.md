---
phase: 11-capability-taxonomy-and-contract-rubric
plan: 03
type: execute
wave: 3
depends_on:
  - 11-01
  - 11-02
files_modified:
  - guides/capabilities.md
  - guides/adopter_profiles.md
  - lib/crosswake/support_matrix/renderer.ex
  - guides/support_matrix.md
  - test/crosswake/guides/capabilities_test.exs
  - test/crosswake/support_matrix/renderer_test.exs
autonomous: true
requirements:
  - CAPA-02
  - CAPA-03
must_haves:
  truths:
    - "Crosswake publishes explicit `core`, `companion`, `example/docs-only`, and `defer` examples for candidate capability families."
    - "The first public example set stays narrow and exemplar-aligned instead of marketing a broad capability catalog."
    - "Deferred or out-of-thesis families are stated plainly with reasons."
    - "Rendered support-matrix output publishes the capability-family metadata and support posture prepared in canonical typed truth."
  artifacts:
    - path: "guides/capabilities.md"
      provides: "Example family classifications and defer rules"
    - path: "guides/adopter_profiles.md"
      provides: "Cross-links from exemplar lanes to the new capability classification posture"
    - path: "guides/support_matrix.md"
      provides: "Generated capability-family support section consistent with public examples"
    - path: "lib/crosswake/support_matrix/renderer.ex"
      provides: "Rendered capability-family classification output and parity surface"
  key_links:
    - from: "guides/capabilities.md"
      to: "guides/adopter_profiles.md"
      via: "example families mapped back to exemplar lanes"
    - from: "guides/capabilities.md"
      to: "guides/support_matrix.md"
      via: "family classification examples aligned to generated support truth"
---

<objective>
Document the first public capability classifications, package-boundary examples, and explicit defer rules without widening Crosswake's support claims.

Purpose: After the rubric and typed support truth exist, Crosswake still needs concrete examples that teach what belongs in `core`, what pressures a `companion`, what is docs-only guidance, and what remains deferred. This plan turns the abstract rubric into actionable exemplar-aligned examples while preserving the narrow product posture.
Output: Updated capability guide, aligned exemplar guide copy, and tests that lock the published examples.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-capability-taxonomy-and-contract-rubric/11-CONTEXT.md
@.planning/phases/11-capability-taxonomy-and-contract-rubric/11-RESEARCH.md
@.planning/phases/11-capability-taxonomy-and-contract-rubric/11-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Publish first-target family classifications, package-boundary rules, and defer rules</name>
  <files>guides/capabilities.md, guides/adopter_profiles.md</files>
  <read_first>
    - guides/capabilities.md
    - guides/adopter_profiles.md
    - guides/support_matrix.md
    - .planning/phases/11-capability-taxonomy-and-contract-rubric/11-CONTEXT.md
  </read_first>
  <action>
    Expand `guides/capabilities.md` with these exact sections: `## Package Boundary Rules`, `## First Public Example Set`, `## Package Class Examples`, and `## Explicit Defers`.

    In `## Package Boundary Rules`, publish explicit rule statements that answer when a family belongs in each package class. Include these exact rule labels and meanings:
    - `core` = semantic family is low-frequency, typed, route-local, and supportable without provider-specific native package sprawl
    - `companion` = family needs extra native/package choreography, backend/operator coupling, or release-boundary pressure beyond core
    - `example/docs-only` = family is useful for guidance and classification, but Crosswake is not claiming first-class shipped support yet
    - `defer` = family is outside the current thesis, proof budget, or honest support posture

    Also state this explicit rule sentence: `Package class does not override runtime ownership.` The section must say a family can still be `native_screen` and `companion`, or `backend_seam` and `example/docs-only`.

    In `## First Public Example Set`, publish these exact example classifications:
    - `deep_link`, `app_info`, `haptics`, and `share` as the first canonical `core` examples
    - `media_capture`, `notification_token`, `rollout`, and `audit` as `companion`-leaning or boundary-heavy examples
    - `permissions.status`, `auth`, `paywall`, `purchase`, `restore`, and `entitlement_snapshot` as `example/docs-only` guidance in Phase 11
    - `scanner` and `document_scan` as explicit `defer` examples for now

    In `## Explicit Defers`, explain the exact reasons `scanner` and `document_scan` remain deferred: heavier native ownership, policy burden, and broader proof/support-matrix breadth than Phase 11 can honestly claim.

    Update `guides/adopter_profiles.md` to reference how the SaaS lane maps to `app_info`, `haptics`, and `deep_link`, how the selective-native lane maps to `media_capture`, and how the local-first lane reinforces backend-truth and bounded-route semantics rather than broad capability support.

    Decision coverage: `D-05`, `D-23`, `D-24`, `D-25`, `D-26`, `D-27`, `D-28`, and `D-29`.
    Pattern anchor: use the exact-self guide analogs from `11-PATTERNS.md` for `guides/support_matrix.md` and the existing guide surfaces, and use `guides/adopter_profiles.md` itself as the continuity analog for exemplar-lane phrasing so the new classification language matches the established adopter-profile narrative. Preserve the narrow exemplar-aligned documentation posture rather than expanding into a feature catalog.
  </action>
  <acceptance_criteria>
    - `guides/capabilities.md` contains `## Package Boundary Rules`
    - `guides/capabilities.md` contains `## First Public Example Set`
    - `guides/capabilities.md` contains `## Package Class Examples`
    - `guides/capabilities.md` contains `## Explicit Defers`
    - `guides/capabilities.md` contains `Package class does not override runtime ownership`
    - `guides/capabilities.md` contains `example/docs-only`
    - `guides/capabilities.md` contains `notification_token`
    - `guides/capabilities.md` contains `scanner`
    - `guides/adopter_profiles.md` contains `media_capture`
  </acceptance_criteria>
  <verify>
    <automated>rg -n "Package Boundary Rules|First Public Example Set|Package Class Examples|Explicit Defers|Package class does not override runtime ownership|example/docs-only|notification_token|scanner" guides/capabilities.md && rg -n "media_capture" guides/adopter_profiles.md</automated>
  </verify>
  <done>The public guide explains concrete family examples and explicit defers instead of leaving package/scope interpretation implicit.</done>
</task>

<task type="auto">
  <name>Task 2: Lock the published classifications against generated support truth</name>
  <files>lib/crosswake/support_matrix/renderer.ex, guides/support_matrix.md, test/crosswake/guides/capabilities_test.exs, test/crosswake/support_matrix/renderer_test.exs</files>
  <read_first>
    - lib/crosswake/support_matrix/renderer.ex
    - guides/support_matrix.md
    - test/crosswake/guides/capabilities_test.exs
    - test/crosswake/support_matrix/renderer_test.exs
    - guides/capabilities.md
  </read_first>
  <action>
    Update `Crosswake.SupportMatrix.Renderer` so `guides/support_matrix.md` renders a capability-family section derived from manifest capability registry metadata through the typed support truth prepared in Plan 11-02. The renderer must not maintain a second handwritten family-classification ledger. The rendered section must include these exact columns: `Family`, `Owner`, `Package`, `Proof`, `Rebuild`, `Prerequisites`, `Denial`, `Fallback`, and `Guide`.

    Regenerate `guides/support_matrix.md` from the renderer.

    Extend the guide-facing tests so the published classifications in `guides/capabilities.md` stay aligned with the generated support output in `guides/support_matrix.md`.

    Update `test/crosswake/guides/capabilities_test.exs` to assert the exact example families and package-class words from Task 1 are present, including `core`, `companion`, `example/docs-only`, and `defer`.

    Update `test/crosswake/support_matrix/renderer_test.exs` to assert that the rendered support guide contains `## Capability Families`, the exact capability-family table header, and classification parity lines proving these mappings are rendered from manifest-derived capability metadata:
    - `deep_link` with `core`
    - `app_info` with `core`
    - `haptics` with `core`
    - `share` with `core`
    - `media_capture` with `companion`
    - `notification_token` with `companion`
    - `permissions.status` with `example/docs-only`
    - `paywall` with `example/docs-only`
    - `purchase` with `example/docs-only`
    - `restore` with `example/docs-only`
    - `entitlement_snapshot` with `example/docs-only`
    - `scanner` with `defer`

    Decision coverage: `D-18`, `D-19`, `D-20`, `D-21`, `D-22`, `D-25`, `D-26`, and `D-27`.
    Pattern anchor: follow the exact-self analogs from `11-PATTERNS.md` for `lib/crosswake/support_matrix/renderer.ex` and `guides/support_matrix.md`; specifically reuse the deterministic markdown assembly pattern, stable table-row rendering pattern, and mechanically checked generated-guide pattern.
  </action>
  <acceptance_criteria>
    - `lib/crosswake/support_matrix/renderer.ex` contains `| Family | Owner | Package | Proof | Rebuild | Prerequisites | Denial | Fallback | Guide |`
    - `guides/support_matrix.md` contains `## Capability Families`
    - `guides/support_matrix.md` contains `| app_info |`
    - `guides/support_matrix.md` contains `| deep_link |`
    - `guides/support_matrix.md` contains `| share |`
    - `guides/support_matrix.md` contains `| media_capture |`
    - `guides/support_matrix.md` contains `| notification_token |`
    - `guides/support_matrix.md` contains `| permissions.status |`
    - `guides/support_matrix.md` contains `| paywall |`
    - `guides/support_matrix.md` contains `| purchase |`
    - `guides/support_matrix.md` contains `| restore |`
    - `guides/support_matrix.md` contains `| entitlement_snapshot |`
    - `guides/support_matrix.md` contains `| scanner |`
    - `test/crosswake/guides/capabilities_test.exs` contains `notification_token`
    - `test/crosswake/guides/capabilities_test.exs` contains `scanner`
    - `test/crosswake/support_matrix/renderer_test.exs` contains `core`
    - `test/crosswake/support_matrix/renderer_test.exs` contains `companion`
    - `test/crosswake/support_matrix/renderer_test.exs` contains `example/docs-only`
    - `test/crosswake/support_matrix/renderer_test.exs` contains `defer`
    - `test/crosswake/support_matrix/renderer_test.exs` contains `manifest-derived` or equivalent derivation assertion wording
    - `mix test test/crosswake/guides/capabilities_test.exs test/crosswake/support_matrix/renderer_test.exs` exits 0
  </acceptance_criteria>
  <verify>
    <automated>mix test test/crosswake/guides/capabilities_test.exs test/crosswake/support_matrix/renderer_test.exs</automated>
  </verify>
  <done>The public example set stays mechanically aligned to generated support truth.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Example classifications -> adopters | Public examples strongly influence what teams assume Crosswake officially supports. |
| Generated support guide -> hand-authored capability guide | Package and support examples must not contradict canonical rendered truth. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-06 | Information Disclosure | `guides/capabilities.md` | mitigate | State explicit defer reasons and docs-only posture so readers do not infer broader first-party support. |
| T-11-07 | Tampering | `guides/support_matrix.md`, guide tests | mitigate | Lock the example set to generated support output with tests that assert the published family names remain aligned. |
</threat_model>

<verification>
mix test test/crosswake/guides/capabilities_test.exs test/crosswake/support_matrix/renderer_test.exs
</verification>

<success_criteria>
Adopters can tell which example families belong in core, which carry companion pressure, which remain docs-only for now, and which are explicitly deferred.
</success_criteria>

<output>
After completion, create `.planning/phases/11-capability-taxonomy-and-contract-rubric/11-capability-taxonomy-and-contract-rubric-03-SUMMARY.md`
</output>
