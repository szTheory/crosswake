---
phase: 11-capability-taxonomy-and-contract-rubric
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - guides/capabilities.md
  - guides/bridge.md
  - guides/native_shell.md
  - test/crosswake/guides/capabilities_test.exs
autonomous: true
requirements:
  - CAPA-01
must_haves:
  truths:
    - "Crosswake publishes one ownership-first capability rubric instead of plugin-style feature framing."
    - "Bounded bridge, native screen, and backend-seam/defer outcomes are explained with explicit route-owner rules."
    - "The public taxonomy stays semantic and family-first, not command-first."
  artifacts:
    - path: "guides/capabilities.md"
      provides: "Canonical capability-family inventory and ownership rubric"
    - path: "guides/bridge.md"
      provides: "Bounded-bridge framing aligned to the new capability taxonomy"
    - path: "guides/native_shell.md"
      provides: "Native-screen ownership rules aligned to the new capability taxonomy"
    - path: "test/crosswake/guides/capabilities_test.exs"
      provides: "Mechanical proof that the guide and linked public terms stay present"
  key_links:
    - from: "guides/capabilities.md"
      to: "guides/bridge.md"
      via: "bounded_bridge terminology and examples"
    - from: "guides/capabilities.md"
      to: "guides/native_shell.md"
      via: "native_screen terminology and examples"
---

<objective>
Publish Crosswake's capability-family inventory and ownership rubric as a first-class public contract.

Purpose: Phase 11 needs one explicit answer to "who should own this interaction loop?" before future capability breadth ships. The public docs must teach semantic family ids, bounded bridge versus native screen versus backend/defer outcomes, and the reasons those boundaries exist.
Output: A new `guides/capabilities.md` plus aligned bridge/native-shell guides and a doc contract test.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-capability-taxonomy-and-contract-rubric/11-CONTEXT.md
@.planning/phases/11-capability-taxonomy-and-contract-rubric/11-RESEARCH.md
@.planning/phases/11-capability-taxonomy-and-contract-rubric/11-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Publish the capability-family taxonomy guide and ownership rubric</name>
  <files>guides/capabilities.md, guides/bridge.md, guides/native_shell.md</files>
  <read_first>
    - guides/bridge.md
    - guides/native_shell.md
    - guides/adopter_profiles.md
    - .planning/phases/11-capability-taxonomy-and-contract-rubric/11-CONTEXT.md
    - .planning/phases/11-capability-taxonomy-and-contract-rubric/11-RESEARCH.md
  </read_first>
  <action>
    Create `guides/capabilities.md` as the canonical public guide for Phase 11. It must contain these exact top-level sections: `## Ownership-First Rubric`, `## Capability Families`, `## Bounded Bridge`, `## Native Screen`, `## Backend Seams And Deferred Surfaces`, and `## Family Naming Rules`.

    In `## Ownership-First Rubric`, define these exact outcome labels and meanings:
    - `bounded_bridge` = Phoenix-owned route, low-frequency semantic request/reply action, no native authority over the interaction loop
    - `native_screen` = native code owns permission choreography, capture or scan session, or policy-sensitive UI loop
    - `backend_seam` = Phoenix/backend remains the control plane and route-local bridge calls do not own truth
    - `defer` = outside current thesis, support posture, or proof budget

    In `## Capability Families`, publish semantic family ids and explicitly say that family ids are public vocabulary while bridge commands are subordinate protocol details. Include these exact family ids in the first inventory: `deep_link`, `app_info`, `haptics`, `share`, `permissions.status`, `notification_token`, `media_capture`, `scanner`, `document_scan`, `paywall`, `purchase`, `restore`, and `entitlement_snapshot`.

    In the `deep_link` entry and anywhere `deep_link` is referenced in `guides/bridge.md` or `guides/native_shell.md`, state this exact rule: `deep_link remains manifest-first shell activation truth, not route-local bridge or navigation authority.`

    Update `guides/bridge.md` so it uses the family-first terms `app_info`, `haptics`, and `share` when explaining bounded bridge posture, while still clarifying that concrete bridge commands remain lower-level protocol details.

    Update `guides/native_shell.md` so it explicitly says `media_capture`, `scanner`, and `document_scan` are native-screen-first families because native code owns session loops and permission choreography.

    Decision coverage: `D-01`, `D-02`, `D-03`, `D-06`, `D-07`, `D-08`, `D-09`, `D-10`, `D-11`, `D-12`, `D-23`, `D-24`, `D-27`, `D-28`, and `D-29`.
    Pattern anchor: use the guide exact-self analogs called out in `11-PATTERNS.md` for `guides/bridge.md` and `guides/native_shell.md`; preserve the documented split between family-level contract language and lower-level bridge command wording.
  </action>
  <acceptance_criteria>
    - `guides/capabilities.md` contains `## Ownership-First Rubric`
    - `guides/capabilities.md` contains `bounded_bridge`
    - `guides/capabilities.md` contains `native_screen`
    - `guides/capabilities.md` contains `backend_seam`
    - `guides/capabilities.md` contains `defer`
    - `guides/capabilities.md` contains `deep_link`
    - `guides/capabilities.md` contains `manifest-first shell activation truth`
    - `guides/capabilities.md` contains `permissions.status`
    - `guides/capabilities.md` contains `notification_token`
    - `guides/capabilities.md` contains `scanner`
    - `guides/capabilities.md` contains `document_scan`
    - `guides/capabilities.md` contains `paywall`
    - `guides/capabilities.md` contains `purchase`
    - `guides/capabilities.md` contains `restore`
    - `guides/capabilities.md` contains `entitlement_snapshot`
    - `guides/bridge.md` contains `app_info`
    - `guides/native_shell.md` contains `media_capture`
  </acceptance_criteria>
  <verify>
    <automated>rg -n "Ownership-First Rubric|bounded_bridge|native_screen|backend_seam|defer|deep_link|manifest-first shell activation truth|permissions.status|notification_token|scanner|document_scan|paywall|purchase|restore|entitlement_snapshot" guides/capabilities.md && rg -n "app_info" guides/bridge.md && rg -n "media_capture" guides/native_shell.md</automated>
  </verify>
  <done>The public docs define one ownership-first taxonomy and align bridge/native-screen language to it.</done>
</task>

<task type="auto">
  <name>Task 2: Add a mechanical doc contract test for the taxonomy guide</name>
  <files>test/crosswake/guides/capabilities_test.exs</files>
  <read_first>
    - test/crosswake/support_matrix/renderer_test.exs
    - guides/capabilities.md
    - guides/bridge.md
    - guides/native_shell.md
  </read_first>
  <action>
    Create `test/crosswake/guides/capabilities_test.exs` as an ExUnit doc contract test. Read the guide files directly and assert the public contract terms added in Task 1 are present.

    The test file must assert that:
    - `guides/capabilities.md` contains `Ownership-First Rubric`
    - `guides/capabilities.md` contains each of `bounded_bridge`, `native_screen`, `backend_seam`, and `defer`
    - `guides/capabilities.md` contains each of `deep_link`, `app_info`, `haptics`, `share`, `permissions.status`, `notification_token`, `media_capture`, `scanner`, `document_scan`, `paywall`, `purchase`, `restore`, and `entitlement_snapshot`
    - `guides/capabilities.md` contains `manifest-first shell activation truth`
    - `guides/bridge.md` contains `bounded bridge`
    - `guides/native_shell.md` contains `native screen`

    Decision coverage: `D-20`, `D-28`, and `D-29` by mechanically locking fail-closed public wording and least-surprise taxonomy terms.
    Pattern anchor: follow the direct file-read doc-contract pattern used in `test/crosswake/support_matrix/renderer_test.exs`, as highlighted by `11-PATTERNS.md` for generated and guide-facing contract checks.
  </action>
  <acceptance_criteria>
    - `test/crosswake/guides/capabilities_test.exs` contains `Ownership-First Rubric`
    - `test/crosswake/guides/capabilities_test.exs` contains `bounded_bridge`
    - `test/crosswake/guides/capabilities_test.exs` contains `permissions.status`
    - `test/crosswake/guides/capabilities_test.exs` contains `notification_token`
    - `test/crosswake/guides/capabilities_test.exs` contains `scanner`
    - `test/crosswake/guides/capabilities_test.exs` contains `document_scan`
    - `test/crosswake/guides/capabilities_test.exs` contains `paywall`
    - `test/crosswake/guides/capabilities_test.exs` contains `purchase`
    - `test/crosswake/guides/capabilities_test.exs` contains `restore`
    - `test/crosswake/guides/capabilities_test.exs` contains `entitlement_snapshot`
    - `mix test test/crosswake/guides/capabilities_test.exs` exits 0
  </acceptance_criteria>
  <verify>
    <automated>mix test test/crosswake/guides/capabilities_test.exs</automated>
  </verify>
  <done>The public taxonomy guide is mechanically checked instead of relying on manual review.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Public docs -> adopters | Capability docs shape architecture choices before implementation starts. |
| Taxonomy guide -> future plans | Later capability work will treat these public terms as contract truth. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-01 | Tampering | `guides/capabilities.md` | mitigate | Use an ExUnit doc contract test so the ownership-first rubric and family inventory cannot silently drift. |
| T-11-02 | Information Disclosure | `guides/bridge.md`, `guides/native_shell.md` | mitigate | State explicit owner-class boundaries so docs do not imply broader runtime authority than Crosswake actually supports. |
</threat_model>

<verification>
mix test test/crosswake/guides/capabilities_test.exs
</verification>

<success_criteria>
Phoenix teams can read one guide that classifies candidate surfaces into bounded bridge, native screen, backend seam, or defer outcomes without generic plugin framing.
</success_criteria>

<output>
After completion, create `.planning/phases/11-capability-taxonomy-and-contract-rubric/11-capability-taxonomy-and-contract-rubric-01-SUMMARY.md`
</output>
