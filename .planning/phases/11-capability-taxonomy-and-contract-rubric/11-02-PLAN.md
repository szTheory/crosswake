---
phase: 11-capability-taxonomy-and-contract-rubric
plan: 02
type: execute
wave: 2
depends_on:
  - 11-01
files_modified:
  - lib/crosswake/manifest/types.ex
  - lib/crosswake/manifest/builder.ex
  - lib/crosswake/manifest/validator.ex
  - lib/crosswake/bridge/registry.ex
  - lib/crosswake/support_matrix/support_matrix.ex
  - test/crosswake/manifest/manifest_test.exs
  - test/crosswake/manifest/validator_test.exs
  - test/crosswake/bridge/registry_test.exs
  - test/crosswake/support_matrix/support_matrix_test.exs
autonomous: true
requirements:
  - CAPA-02
must_haves:
  truths:
    - "Capability-family support posture is encoded in canonical typed manifest metadata."
    - "Routes stay lean and reference capability ids while the registry owns owner/package/proof/rebuild/prerequisite truth."
    - "Support output is generated from canonical state and remains fail-closed."
  artifacts:
    - path: "lib/crosswake/manifest/types.ex"
      provides: "Expanded typed capability metadata model"
    - path: "lib/crosswake/manifest/builder.ex"
      provides: "Canonical registry construction for capability families and legacy compatibility shims"
    - path: "lib/crosswake/manifest/validator.ex"
      provides: "Fail-closed validation of capability-family metadata vocabularies"
    - path: "lib/crosswake/support_matrix/support_matrix.ex"
      provides: "Canonical typed support-matrix slots for capability-family posture"
  key_links:
    - from: "lib/crosswake/manifest/types.ex"
      to: "lib/crosswake/manifest/validator.ex"
      via: "owner/package/proof/rebuild/prerequisite vocabulary validation"
    - from: "lib/crosswake/manifest/builder.ex"
      to: "lib/crosswake/bridge/registry.ex"
      via: "legacy command-to-family compatibility seam"
    - from: "lib/crosswake/manifest/types.ex"
      to: "lib/crosswake/support_matrix/support_matrix.ex"
      via: "typed support posture consumed later by the renderer"
---

<objective>
Extend Crosswake's manifest and typed support surfaces so capability-family metadata and support posture are declared explicitly in canonical state.

Purpose: Phase 11 must make capability-family metadata machine-readable before future breadth lands. The manifest registry should own the family contract, the validator should reject unsupported vocabulary drift, the bridge should preserve a compatibility seam for command-shaped ids, and the canonical support-matrix state should be ready for rendered publication in the next plan.
Output: Updated manifest/support code and passing typed-state tests.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-capability-taxonomy-and-contract-rubric/11-CONTEXT.md
@.planning/phases/11-capability-taxonomy-and-contract-rubric/11-RESEARCH.md
@.planning/phases/11-capability-taxonomy-and-contract-rubric/11-PATTERNS.md
</context>

<tasks>

<task type="tdd">
  <name>Task 1: Add typed capability-family metadata and validate it fail closed</name>
  <files>lib/crosswake/manifest/types.ex, lib/crosswake/manifest/builder.ex, lib/crosswake/manifest/validator.ex, test/crosswake/manifest/manifest_test.exs, test/crosswake/manifest/validator_test.exs</files>
  <read_first>
    - lib/crosswake/manifest/types.ex
    - lib/crosswake/manifest/builder.ex
    - lib/crosswake/manifest/validator.ex
    - test/crosswake/manifest/manifest_test.exs
    - test/crosswake/manifest/validator_test.exs
    - .planning/phases/11-capability-taxonomy-and-contract-rubric/11-CONTEXT.md
  </read_first>
  <action>
    Expand `Crosswake.Manifest.Types.Capability` with explicit typed metadata fields and thread them through `new_capability/1` and `to_map/1`. Use these exact field names and vocabularies:
    - `family` = canonical public family id such as `app_info` or `haptics`
    - `owner` = `:bounded_bridge | :native_screen | :backend_seam | :defer`
    - `package_class` = `:core | :companion | :example_docs_only | :defer`
    - `proof_class` = `:merge_blocking | :advisory`
    - `rebuild` = `:none | :native_required | :companion_required`
    - `prerequisites` = list of non-empty strings
    - `denial` = non-empty string naming fail-closed denial behavior
    - `fallback` = non-empty string naming the explicit fallback behavior
    - `guide` = non-empty string path or anchor for the public contract guide
    - `legacy_ids` = list of legacy command-shaped ids kept temporarily for compatibility

    Update `Crosswake.Manifest.Builder.capability_registry/1` so it no longer emits bare `id/version/status` entries only. Introduce a canonical family catalog in the builder that assigns metadata for the Phase 11 public families. Keep routes lean: route entries still declare capability ids only, but registry entries must carry the family metadata.

    Add a compatibility seam in the builder for current command-shaped ids by setting these exact `legacy_ids`:
    - `app_info` includes `app.info.get`
    - `haptics` includes `haptics.impact`

    Do NOT map `files.pick` to the public `share` family in this phase. `files.pick` must remain a compatibility-only bridge command until Crosswake has a truthful semantic family for that surface. Phase 11 can preserve the concrete command in `Crosswake.Bridge.Registry`, but it must not publish `files.pick` as `share` metadata.

    Update `Crosswake.Manifest.Validator` to reject unsupported `owner`, `package_class`, `proof_class`, or `rebuild` values, and to reject empty `prerequisites`, `denial`, `fallback`, or `guide` values on capability entries.

    Add or update tests in `test/crosswake/manifest/manifest_test.exs` and `test/crosswake/manifest/validator_test.exs` so the manifest root exposes the new metadata and invalid vocabulary is rejected with concrete validation errors.

    Decision coverage: `D-04`, `D-05`, `D-13`, `D-14`, `D-15`, `D-16`, `D-17`, `D-18`, `D-20`, `D-21`, and `D-22`.
    Pattern anchor: follow the exact-self analogs from `11-PATTERNS.md` for `lib/crosswake/manifest/types.ex`, `lib/crosswake/manifest/builder.ex`, and `lib/crosswake/manifest/validator.ex`; specifically reuse the typed struct expansion pattern, registry derivation pattern, and fail-closed validation pipeline.
  </action>
  <acceptance_criteria>
    - `lib/crosswake/manifest/types.ex` contains `owner`
    - `lib/crosswake/manifest/types.ex` contains `package_class`
    - `lib/crosswake/manifest/types.ex` contains `proof_class`
    - `lib/crosswake/manifest/types.ex` contains `legacy_ids`
    - `lib/crosswake/manifest/builder.ex` contains `app.info.get`
    - `lib/crosswake/manifest/validator.ex` contains `package_class`
    - `mix test test/crosswake/manifest/manifest_test.exs test/crosswake/manifest/validator_test.exs` exits 0
  </acceptance_criteria>
  <verify>
    <automated>mix test test/crosswake/manifest/manifest_test.exs test/crosswake/manifest/validator_test.exs</automated>
  </verify>
  <done>Manifest capability entries are typed, validated, and explicit about ownership/support posture.</done>
</task>

<task type="tdd">
  <name>Task 2: Keep bridge lookup and typed support-matrix truth aligned to the family registry</name>
  <files>lib/crosswake/bridge/registry.ex, lib/crosswake/support_matrix/support_matrix.ex, test/crosswake/bridge/registry_test.exs, test/crosswake/support_matrix/support_matrix_test.exs</files>
  <read_first>
    - lib/crosswake/bridge/registry.ex
    - lib/crosswake/support_matrix/support_matrix.ex
    - test/crosswake/bridge/registry_test.exs
    - test/crosswake/support_matrix/support_matrix_test.exs
    - guides/capabilities.md
  </read_first>
  <action>
    Update `Crosswake.Bridge.Registry` so command lookup still uses concrete protocol commands but resolves them against the new family metadata where a public family exists. Preserve fail-closed behavior. The command layer remains concrete (`app.info.get`, `haptics.impact`, `files.pick`, transfer commands), but only `app.info.get` and `haptics.impact` should resolve through public family metadata in Phase 11. `files.pick` must remain a bounded compatibility command without being surfaced as the public `share` family.

    Update `Crosswake.SupportMatrix` so its canonical typed truth is derived from manifest capability registry metadata and exposes capability-family posture that the renderer will publish in Plan 11-03. Keep the support matrix as the canonical source of typed status vocabulary and proof posture, but require capability-family support rows to be computed from manifest capability entries rather than a second handwritten capability ledger in the support-matrix module or guides.

    Add or update tests so bridge lookup remains fail-closed and support-matrix canonical truth exposes the new family support slots needed by later rendering. At least one test must prove the support-matrix family posture is built from manifest capability registry entries rather than a separate hard-coded family table.

    Decision coverage: `D-02`, `D-12`, `D-17`, `D-18`, `D-19`, `D-20`, `D-21`, and `D-22`.
    Pattern anchor: follow the exact-self analogs from `11-PATTERNS.md` for `lib/crosswake/bridge/registry.ex` and `lib/crosswake/support_matrix/support_matrix.ex`; specifically reuse the command-versus-family separation pattern, fail-closed lookup pattern, canonical truth builder pattern, and narrow-status vocabulary pattern.
  </action>
  <acceptance_criteria>
    - `lib/crosswake/bridge/registry.ex` contains a family-aware lookup helper or equivalent
    - `lib/crosswake/bridge/registry.ex` still contains `files.pick`
    - `test/crosswake/bridge/registry_test.exs` contains an assertion that `files.pick` is not published as `share`
    - `lib/crosswake/support_matrix/support_matrix.ex` contains a manifest-derived capability-family support helper or equivalent
    - `test/crosswake/support_matrix/support_matrix_test.exs` contains an assertion that capability-family support data derives from manifest capability entries
    - `mix test test/crosswake/bridge/registry_test.exs test/crosswake/support_matrix/support_matrix_test.exs` exits 0
  </acceptance_criteria>
  <verify>
    <automated>mix test test/crosswake/bridge/registry_test.exs test/crosswake/support_matrix/support_matrix_test.exs</automated>
  </verify>
  <done>Bridge lookup remains bounded and typed support-matrix truth is ready for guide rendering without collapsing `files.pick` into `share`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Route declarations -> manifest registry | Lean route ids compile into canonical family metadata consumed by shells, docs, and tests. |
| Manifest registry -> generated support docs | Support claims are published from canonical typed state. |
| Bridge commands -> capability entries | Low-level commands must not widen into generic runtime authority during migration. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-03 | Tampering | `lib/crosswake/manifest/types.ex`, `lib/crosswake/manifest/validator.ex` | mitigate | Enforce exact vocabularies for owner/package/proof/rebuild metadata and reject incomplete support facts. |
| T-11-04 | Elevation of Privilege | `lib/crosswake/bridge/registry.ex` | mitigate | Preserve fail-closed command lookup and treat family metadata as descriptive truth, not a broader command allowlist. |
| T-11-05 | Information Disclosure | `guides/support_matrix.md` | mitigate | Render prerequisites, denial, fallback, and rebuild truth directly from canonical metadata so public support claims stay honest. |
</threat_model>

<verification>
mix test test/crosswake/manifest/manifest_test.exs test/crosswake/manifest/validator_test.exs test/crosswake/bridge/registry_test.exs test/crosswake/support_matrix/support_matrix_test.exs
</verification>

<success_criteria>
Manifest and canonical support state can express capability-family ownership and support posture explicitly without pushing that truth down into route-local declarations.
</success_criteria>

<output>
After completion, create `.planning/phases/11-capability-taxonomy-and-contract-rubric/11-capability-taxonomy-and-contract-rubric-02-SUMMARY.md`
</output>
