---
phase: 41-gating-doctor-and-support-matrix-truth
plan: 02
type: execute
wave: 2
depends_on: ["41-01"]
files_modified:
  - lib/crosswake/support_matrix/support_matrix.ex
  - test/crosswake/proof/phase41_gating_doctor_test.exs
autonomous: true
requirements: [GATE-05]

must_haves:
  truths:
    - "SupportMatrix exposes a runtime gating accessor that reads report_state/0 on each registered companion and returns a gate-state display string per companion"
    - "gate_status: :active maps to \"gated\"; {:rolling_out, n} maps to \"rolling_out (N%)\"; kill_switch_status: :active maps to \"killed\" and overrides gate_status"
    - "The gate-state column is labeled runtime-distinct from build-proof state so rolling_out (10%) is never read as supported"
    - "SC#2 proof asserts the gated/rolling_out (N%)/killed display strings from report_state/0 fixtures"
  artifacts:
    - path: "lib/crosswake/support_matrix/support_matrix.ex"
      provides: "gating_truth/0 runtime gate-state accessor + gate_state_display/1 mapping"
      contains: "gating_truth"
    - path: "test/crosswake/proof/phase41_gating_doctor_test.exs"
      provides: "SC#2 support-matrix display assertions"
      contains: "sc2"
  key_links:
    - from: "SupportMatrix.gating_truth/0"
      to: "companion.report_state/0"
      via: "Application.get_env(:crosswake, :companions, []) iteration"
      pattern: "report_state"
    - from: "gate_state_display/1"
      to: "Companion.State kill_switch_status / gate_status"
      via: "pattern match clauses (kill switch first for precedence)"
      pattern: "kill_switch_status: :active"
---

<objective>
Add the runtime support-matrix half of GATE-05: a `gating_truth/0` accessor on `SupportMatrix`
that reads each registered companion's `report_state/0` and maps the `gate_status` /
`kill_switch_status` fields to the locked D-08 display strings (`"gated"`, `"rolling_out (N%)"`,
`"killed"`), labeled runtime-distinct from build-proof posture. Then complete the hermetic proof
with SC#2 assertions.

Purpose: Operators reading the support matrix must distinguish runtime gate state from
build-proof support claims — `rolling_out (10%)` must never be misread as "supported." This is
the support-matrix-truth half of GATE-05.

Output: `gating_truth/0` + `gate_state_display/1` on SupportMatrix, and SC#2 proof coverage in
the phase41 proof file.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/41-gating-doctor-and-support-matrix-truth/41-CONTEXT.md
@.planning/phases/41-gating-doctor-and-support-matrix-truth/41-RESEARCH.md
@.planning/phases/41-gating-doctor-and-support-matrix-truth/41-PATTERNS.md
@.planning/phases/41-gating-doctor-and-support-matrix-truth/41-01-SUMMARY.md
</context>

<dependency_note>
Depends on 41-01: the proof file `phase41_gating_doctor_test.exs` is created by plan 01 (SC#1
coverage) and extended here with SC#2 tests. The `{:rolling_out, non_neg_integer()}` typespec
arm (plan 01 Task 1) must already exist so fixture companions can return that gate_status without
a Dialyzer/compile concern. This is a sequential dependency — wave 2.
</dependency_note>

<tasks>

<task type="auto">
  <name>Task 1: Add gating_truth/0 accessor and gate_state_display/1 mapping</name>
  <files>lib/crosswake/support_matrix/support_matrix.ex</files>
  <read_first>
    - lib/crosswake/support_matrix/support_matrix.ex (commerce_corridors/0 lines 232-233 and commerce_corridor_proof_classes/0 lines 259-273 — module-level accessor naming and derived-computation pattern; existing @spec style)
    - lib/crosswake/companion/state.ex (Companion.State struct fields gate_status / kill_switch_status, including the rolling_out arm added in plan 01)
    - lib/crosswake/companion.ex (report_state/0 callback contract returning State.t())
  </read_first>
  <action>
    Add a public `gating_truth/0` to SupportMatrix following the `commerce_corridors/0` /
    `commerce_corridor_proof_classes/0` naming and derived-computation precedent. It reads
    `Application.get_env(:crosswake, :companions, [])` (NOT compile_env — must be runtime-readable
    so the proof test can register fixtures via put_env), maps each companion to a map
    `%{companion_id: state.companion_id, gate_state: gate_state_display(state)}` where `state` is
    `companion.report_state()`. Add `@spec gating_truth() :: [map()]`.

    Add a private `gate_state_display/1` matching on `%Crosswake.Companion.State{}` implementing the
    D-08 mapping. Clause ORDER matters for kill-switch precedence (RESEARCH critical pitfall) — the
    kill-switch clause MUST come first:
    - `%{kill_switch_status: :active}` -> `"killed"` (overrides gate_status)
    - `%{gate_status: :active}` -> `"gated"`
    - `%{gate_status: {:rolling_out, n}}` -> `"rolling_out (#{n}%)"`
    - `%{gate_status: :inactive}` -> `nil` (route not currently gated)
    - `%{gate_status: :unconfigured}` -> `nil` (companion not configured)

    Label the runtime distinction: add a public accessor (e.g. `gating_truth_label/0`) or a module
    `@doc`/comment string returning/containing the runtime-distinct label text, e.g.
    "Runtime Gate State — not build-proof posture" (D-08 requires the column be labeled
    runtime-distinct from build-proof state). Choose the exact accessor name/placement to match the
    SupportMatrix conventions; the rendered support-matrix section ordering follows the existing
    commerce precedent. Do NOT store companion state at module level — companions are runtime-
    registered, so the accessor computes at call time (RESEARCH Open Question 2 recommendation).
  </action>
  <verify>
    <automated>cd /Users/jon/projects/crosswake && grep -q "def gating_truth" lib/crosswake/support_matrix/support_matrix.ex && grep -q "kill_switch_status: :active" lib/crosswake/support_matrix/support_matrix.ex && grep -q "rolling_out (#{" lib/crosswake/support_matrix/support_matrix.ex && mix compile --warnings-as-errors 2>&1 | grep -v '^#' | grep -qi warning && echo FAIL || echo OK</automated>
  </verify>
  <acceptance_criteria>
    - support_matrix.ex defines `def gating_truth` with `@spec gating_truth() :: [map()]`
    - gating_truth reads `Application.get_env(:crosswake, :companions, [])` and calls `report_state()` per companion
    - A private `gate_state_display/1` exists with a `kill_switch_status: :active` clause appearing BEFORE any `gate_status` clause
    - The display clauses return `"killed"`, `"gated"`, and a `"rolling_out (N%)"` interpolation respectively; `:inactive` and `:unconfigured` return nil
    - A runtime-distinct label (text containing "runtime" / "not build-proof") is exposed or documented for the gate-state column
    - `mix compile --warnings-as-errors` exits 0
  </acceptance_criteria>
  <done>SupportMatrix exposes gating_truth/0 mapping report_state/0 to the D-08 display strings with kill-switch precedence, labeled runtime-distinct from build-proof state.</done>
</task>

<task type="auto">
  <name>Task 2: Add SC#2 support-matrix proof coverage</name>
  <files>test/crosswake/proof/phase41_gating_doctor_test.exs</files>
  <read_first>
    - test/crosswake/proof/phase41_gating_doctor_test.exs (the file plan 01 created — reuse its setup, inline fixture companion modules, and Application.put_env/on_exit pattern)
    - lib/crosswake/support_matrix/support_matrix.ex (gating_truth/0 shape added in Task 1)
    - test/crosswake/proof/phase40_gate_evaluation_test.exs (inline defmodule companion fixtures with report_state/0 returning specific gate_status values)
    - .planning/phases/41-gating-doctor-and-support-matrix-truth/41-RESEARCH.md (Code Examples: GatingActiveCompanion gate_status: :active, RollingOutCompanion gate_status: {:rolling_out, 10})
  </read_first>
  <action>
    Extend `test/crosswake/proof/phase41_gating_doctor_test.exs` with SC#2 tests for the support-
    matrix gate-state column. Tag them `@tag :sc2` so they run via
    `mix test test/crosswake/proof/phase41_gating_doctor_test.exs --only sc2`.

    Add (or reuse) inline fixture companion modules whose `report_state/0` returns distinct states:
    one with `gate_status: :active` (expect display `"gated"`), one with
    `gate_status: {:rolling_out, 10}` (expect `"rolling_out (10%)"`), and one with
    `kill_switch_status: :active` AND a non-inactive `gate_status` to prove kill-switch precedence
    (expect `"killed"` regardless of gate_status). Register them via
    `Application.put_env(:crosswake, :companions, [...])` with a paired
    `on_exit(fn -> Application.delete_env(:crosswake, :companions) end)`.

    Assert `SupportMatrix.gating_truth/0` returns entries whose `gate_state` values match the
    expected display strings for each fixture (`"gated"`, `"rolling_out (10%)"`, `"killed"`). Add an
    assertion that the runtime-distinct label/accessor returns text containing "runtime" (or "not
    build-proof") so SC#2's runtime-distinct labeling requirement is proven, not just the values.

    Keep `async: false`. Do not duplicate the hermeticity self-assertion (plan 01 added it).
  </action>
  <verify>
    <automated>cd /Users/jon/projects/crosswake && mix test test/crosswake/proof/phase41_gating_doctor_test.exs 2>&1 | tail -6</automated>
  </verify>
  <acceptance_criteria>
    - phase41_gating_doctor_test.exs contains `@tag :sc2` on the support-matrix tests
    - Tests assert `SupportMatrix.gating_truth/0` produces gate_state `"gated"` for a `gate_status: :active` fixture
    - Tests assert gate_state `"rolling_out (10%)"` for a `gate_status: {:rolling_out, 10}` fixture
    - Tests assert gate_state `"killed"` for a `kill_switch_status: :active` fixture whose gate_status is not :inactive (proving precedence)
    - Tests assert the runtime-distinct label text (contains "runtime" or "not build-proof")
    - `mix test test/crosswake/proof/phase41_gating_doctor_test.exs` exits 0 with both sc1 and sc2 tests passing
  </acceptance_criteria>
  <done>SC#2 proof passes: support-matrix gate-state column renders gated / rolling_out (N%) / killed with kill-switch precedence, labeled runtime-distinct; full phase41 proof file is green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| host app config -> SupportMatrix | `Application.get_env(:crosswake, :companions, [])` reads host-supplied trusted modules and calls report_state/0 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-41-04 | Spoofing | companion.report_state/0 returns gate_status used for display | accept | Companions are the host's own trusted modules (their deps); display is operator-facing diagnostics, not an access-control decision. Runtime gate enforcement lives in Phase 40 RouteGate, not here. |
| T-41-05 | Denial of Service | gate_state_display/1 on unexpected State shape | mitigate | Display clauses cover all five D-08 cases; pattern matches on the typed Companion.State struct. The gate_status union (extended in 41-01) bounds the input domain. |
| T-41-06 | Information Disclosure | gating_truth/0 exposes companion_id + gate state | accept | companion_id and rollout percentage are operator-facing configuration truth, surfaced intentionally. No secrets. RESEARCH Security Domain confirms no ASVS categories apply. |
| T-41-SC | Tampering | npm/pip/cargo installs | accept | No package-manager installs this phase. RESEARCH Package Legitimacy Audit confirms zero external packages; pure in-tree Elixir extension. |
</threat_model>

<verification>
- `cd /Users/jon/projects/crosswake && mix compile --warnings-as-errors` exits 0
- `mix test test/crosswake/proof/phase41_gating_doctor_test.exs` exits 0 (both sc1 and sc2)
- `mix test --exclude requires_example_host` exits 0 (per-wave merge gate)
</verification>

<success_criteria>
- `SupportMatrix.gating_truth/0` maps report_state/0 to gated / rolling_out (N%) / killed with kill-switch precedence
- Gate-state column labeled runtime-distinct from build-proof state
- SC#2 proof asserts all three display strings + the runtime-distinct label
- Full phase41 proof file green
</success_criteria>

<output>
Create `.planning/phases/41-gating-doctor-and-support-matrix-truth/41-02-SUMMARY.md` when done
</output>
