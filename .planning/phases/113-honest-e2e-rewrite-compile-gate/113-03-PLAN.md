---
phase: 113-honest-e2e-rewrite-compile-gate
plan: 03
type: execute
wave: 2
depends_on: ["113-01"]
files_modified:
  - .github/workflows/offline-sync-e2e-gate.yml
autonomous: true
requirements: [E2E-04]

must_haves:
  truths:
    - "offline-sync-e2e-gate.yml runs a `MIX_ENV=test mix compile --warnings-as-errors` step (working-directory examples/phoenix_host) BEFORE the Playwright steps, so a demo-app compile break fails as a compile error instead of masquerading as a Playwright port-connection timeout (the v6.0 failure mode)"
    - "The compile step is positioned after 'Install Mix dependencies' and before 'Install dependencies' (npm ci) / 'Install Playwright Browsers' / 'Run Playwright tests'"
    - "The job is still named e2e-offline-sync — NOT renamed (Phase 114 GATE-01 owns the rename to merge-blocking-offline-sync-e2e)"
  artifacts:
    - path: ".github/workflows/offline-sync-e2e-gate.yml"
      provides: "loud compile gate before Playwright"
      contains: "mix compile --warnings-as-errors"
  key_links:
    - from: ".github/workflows/offline-sync-e2e-gate.yml"
      to: "examples/phoenix_host"
      via: "MIX_ENV=test mix compile --warnings-as-errors step with working-directory examples/phoenix_host"
      pattern: "mix compile --warnings-as-errors"
  prohibitions:
    - "Do NOT rename the job `e2e-offline-sync` (Phase 114 GATE-01 renames it; GitHub matches required status checks by exact job-name string, so renaming early drops it from any future required-checks list)"
    - "Do NOT use MIX_ENV=dev (or omit MIX_ENV) — MIX_ENV=test is mandatory; dev misses the elixirc_paths(:test)/test/support tree and the `if Mix.env() in [:test, :e2e]` _e2e route, which is exactly the v6.0 break path the gate must catch"
    - "Do NOT restructure other steps, add continue-on-error, or add ::notice advisory markers (that is Phase 114 GATE-01); this plan inserts ONE step only"
    - "Do NOT add the compile step AFTER the Playwright steps — ordering is the whole point (loud-before-timeout)"
---

<objective>
Add a loud compile gate to `.github/workflows/offline-sync-e2e-gate.yml`: a `MIX_ENV=test mix compile --warnings-as-errors` step in `examples/phoenix_host`, positioned BEFORE the Playwright steps (E2E-04).

Today a demo-app compile break does not surface as a compile error — the webServer fails to boot and Playwright reports a port-connection timeout instead (the v6.0 failure mode, still structurally present). Running `mix compile --warnings-as-errors` before npm/Playwright makes a compile break fail loudly and unambiguously.

This plan depends on Plan 01's pre-flight fix (the `create_progress` → `upsert_progress` warning) so the gate lands GREEN on its first run — a gate born red provides no value. With Plan 01 merged, `MIX_ENV=test mix compile --warnings-as-errors` passes clean.

Purpose: A compile break becomes a compile error, not a masked Playwright timeout.
Output: One new named step in the CI workflow, ordered before Playwright.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/113-honest-e2e-rewrite-compile-gate/113-CONTEXT.md
@.planning/phases/113-honest-e2e-rewrite-compile-gate/113-RESEARCH.md
@.planning/phases/113-honest-e2e-rewrite-compile-gate/113-PATTERNS.md
@.github/workflows/offline-sync-e2e-gate.yml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Insert MIX_ENV=test compile gate before Playwright in offline-sync-e2e-gate.yml (D-04)</name>
  <files>.github/workflows/offline-sync-e2e-gate.yml</files>
  <read_first>
    - .github/workflows/offline-sync-e2e-gate.yml (the full workflow — confirm job name `e2e-offline-sync`, the "Install Mix dependencies" step, and the "Install dependencies"/npm ci step; the new step goes between them)
    - 113-PATTERNS.md § "offline-sync-e2e-gate.yml" (the verbatim step + insertion anchors) and 113-RESEARCH.md § "Pattern 6: CI Compile Gate (D-04)"
  </read_first>
  <action>
    Insert a single new step into the `e2e-offline-sync` job, AFTER the "Install Mix dependencies" step (`run: mix deps.get`) and BEFORE the "Install dependencies" (`run: npm ci`) step:

    name: "Compile (warnings as errors)"
    run: "MIX_ENV=test mix compile --warnings-as-errors"
    working-directory: "examples/phoenix_host"

    Match the surrounding YAML indentation exactly (steps are indented under `steps:`; mirror the sibling steps' two-space-per-level structure). `MIX_ENV=test` is mandatory — it compiles the same tree the webServer boots, including the `if Mix.env() in [:test, :e2e]` _e2e route and `elixirc_paths(:test)`/test/support (the exact v6.0 break path a dev-env compile would miss). `--warnings-as-errors` flags the demo's own code plus the path-dep parent lib (`{:crosswake, path: "../.."}`), not hex deps.

    Do NOT rename the job. Do NOT touch any other step. Do NOT add continue-on-error or ::notice (Phase 114). This is a single-step insertion with correct ordering.
  </action>
  <acceptance_criteria>
    - `offline-sync-e2e-gate.yml` contains a step with `run: MIX_ENV=test mix compile --warnings-as-errors` and `working-directory: examples/phoenix_host`
    - The compile step appears BEFORE the npm-ci / Playwright steps in file order (the `mix compile` line precedes the first `npx playwright` line)
    - The job is still named `e2e-offline-sync` (unchanged)
    - The workflow is valid YAML (parses without error)
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/jon/projects/crosswake && awk '/mix compile --warnings-as-errors/{c=NR} /playwright/{if(!p)p=NR} END{exit !(c>0 && c<p)}' .github/workflows/offline-sync-e2e-gate.yml && grep -q 'name: e2e-offline-sync' .github/workflows/offline-sync-e2e-gate.yml && echo OK</automated>
  </verify>
  <done>offline-sync-e2e-gate.yml runs MIX_ENV=test mix compile --warnings-as-errors in examples/phoenix_host before the Playwright steps; the job is still named e2e-offline-sync.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GitHub Actions runner → repo build | CI YAML change only; adds a build-time compile step before the Playwright lane. No runtime/server surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-113-05 | Tampering / Denial of Service (CI honesty) | offline-sync-e2e-gate.yml masking a compile break as a Playwright timeout | mitigate | Adding `MIX_ENV=test mix compile --warnings-as-errors` before Playwright converts a silent webServer-boot failure (v6.0 masked timeout) into a loud, attributable compile error — the exact CI-honesty goal of the milestone. |
| T-113-06 | Repudiation (required-check drift) | renaming the job would silently drop it from future branch protection | mitigate | This plan does NOT rename the job (`e2e-offline-sync` preserved); Phase 114 GATE-01 owns the rename + required-check registration. Prohibition encoded in must_haves. |
| T-113-SC | Tampering | npm/pip/cargo installs | accept | No packages installed this phase (RESEARCH § Package Legitimacy Audit empty). No supply-chain surface. |
</threat_model>

<verification>
- `awk` ordering check: the `mix compile --warnings-as-errors` line precedes the first `playwright` line in offline-sync-e2e-gate.yml
- `grep -q 'name: e2e-offline-sync'` — job not renamed
- Manual (per VALIDATION.md Manual-Only): introduce a deliberate compile error under examples/phoenix_host/lib/, push, confirm CI fails on the compile step (not the Playwright step), then revert
</verification>

<success_criteria>
- A demo-app compile break fails loudly as a compile error before Playwright (E2E-04).
- The gate lands green on first run (Plan 01 pre-flight fix merged).
- The job is not renamed (Phase 114 owns GATE-01).
</success_criteria>

<output>
Create `.planning/phases/113-honest-e2e-rewrite-compile-gate/113-03-SUMMARY.md` when done
</output>