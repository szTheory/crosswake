---
phase: 122-drift-guards
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/crosswake/doctor/publish_readiness.ex
  - test/crosswake/doctor/publish_readiness_test.exs
autonomous: true
requirements: [GUARD-03]
must_haves:
  truths:
    - "mix crosswake.doctor emits a contract_version_parity finding alongside the existing generator_coordinate_parity check."
    - "The check is a read-only parse: it reads and Jason-decodes the two manifests + the three generated JSONs from cwd and compares each top-level bridge_protocol_version to Crosswake.Bridge.Contract.version/0; it never shells out to mix crosswake.contract.gen and never writes."
    - "On drift the finding is severity :error, blocking true, proof_class :merge_blocking (same as generator_coordinate_parity); green when all surfaces agree."
    - "message + details name the exact drifted file, its found version, and the expected Contract.version(); hint points at the fix verb mix crosswake.contract.gen."
  artifacts:
    - path: "lib/crosswake/doctor/publish_readiness.ex"
      provides: "contract_version_parity_check/1 sibling to generator_coordinate_parity_check/1, registered in build_checks/4"
      contains: "contract_version_parity"
    - path: "test/crosswake/doctor/publish_readiness_test.exs"
      provides: "tests proving contract_version_parity present, green on agreement, and blocking on synthetic drift"
      contains: "contract_version_parity"
  key_links:
    - from: "lib/crosswake/doctor/publish_readiness.ex"
      to: "lib/crosswake/bridge/contract.ex"
      via: "compares each parsed surface to Crosswake.Bridge.Contract.version()"
      pattern: "Crosswake\\.Bridge\\.Contract\\.version"
    - from: "lib/crosswake/doctor/doctor.ex"
      to: "lib/crosswake/doctor/publish_readiness.ex"
      via: "PublishReadiness.findings/1 aggregates contract_version_parity into doctor findings so the task exits non-zero on drift"
      pattern: "PublishReadiness\\.findings"
  prohibitions:
    - "contract_version_parity must NOT shell out to mix crosswake.contract.gen and must NOT write or regenerate any file — it is read-only/inspect-only (mirrors generator_coordinate_parity which only reads templates)."
    - "It must NOT extract the version by text grep — it Jason-decodes the top-level bridge_protocol_version key (same decoy-prose hazard as GUARD-01)."
    - "It must NOT use advisory severity — contract drift is a deterministic correctness defect, so result_check (:error/:fail/blocking) is used, not advisory_check."
---

<objective>
Ship GUARD-03: a `contract_version_parity` doctor check in `Crosswake.Doctor.PublishReadiness`, a sibling to the existing `generator_coordinate_parity_check/1`. It is a read-only parse check (D-07) that decodes each committed contract surface from cwd and compares its top-level `bridge_protocol_version` to `Crosswake.Bridge.Contract.version()`, reporting drift to operators at `:error`/`:merge_blocking` severity (D-08) without their having to read CI logs.

Purpose: Defense-in-depth — the operator-facing doctor tells the same truth as the ExUnit test (GUARD-01) and the CI diff (GUARD-02). The redundant-but-consistent verdict is the point.
Output: a new check function + its registration in `build_checks/4` in `lib/crosswake/doctor/publish_readiness.ex`, plus tests in `publish_readiness_test.exs`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/122-drift-guards/122-CONTEXT.md
@lib/crosswake/doctor/publish_readiness.ex
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add contract_version_parity_check/1 and register it in build_checks</name>
  <files>lib/crosswake/doctor/publish_readiness.ex</files>
  <read_first>
    - lib/crosswake/doctor/publish_readiness.ex:536-577 (generator_coordinate_parity_check/1 — the read-only, file-based, blocking parity-check idiom to clone: same result_check shape, proof_class :merge_blocking, details map)
    - lib/crosswake/doctor/publish_readiness.ex:162-180 (build_checks/4 — where to register the new check in the list)
    - lib/crosswake/doctor/publish_readiness.ex:679-718 (result_check/check helpers — result_check forces severity :error + blocking when passed? is false, exactly D-08)
    - lib/crosswake/doctor/check.ex:1-19 (the Check finding struct shape)
    - lib/crosswake/doctor/doctor.ex:156-188 (how PublishReadiness findings aggregate and how status becomes :error on any :error finding)
    - lib/mix/tasks/crosswake.contract.gen.ex:33-36 (the surface paths to enumerate; the manifests are the two hand-maintained crosswake_manifest.json, NOT a gen output)
  </read_first>
  <behavior>
    - contract_version_parity_check/1 takes cwd, reads Crosswake.Bridge.Contract.version() as expected, enumerates the two manifests + three generated JSONs as committed relative paths under cwd.
    - For each surface: read + Jason.decode + top-level "bridge_protocol_version"; collect a drift error string when it != expected (or when the file is missing/undecodable).
    - Returns a result_check with id "contract.version_parity", category :contract_version_parity, code diag.contract.version_parity_ok / _failed, passed? = errors empty, proof_class :merge_blocking, docs_reference (a contract guide path), and details carrying the expected version + per-surface relative paths + the errors list.
    - It is registered in build_checks/4 so it appears in every report.
    - It never invokes Mix.Task.run, never writes a file.
  </behavior>
  <action>
    In lib/crosswake/doctor/publish_readiness.ex add a private contract_version_parity_check(cwd) modeled line-for-line on generator_coordinate_parity_check/1 (lines 536-577). Resolve expected = Crosswake.Bridge.Contract.version(). Define two relative-path groups exactly as in lib/mix/tasks/crosswake.contract.gen.ex:33-36 plus the two hand-maintained manifests (examples/ios_shell_host/Fixtures/crosswake_manifest.json, examples/android_shell_host/app/src/main/assets/crosswake_manifest.json). For each, Path.join(cwd, rel), File.read, Jason.decode, fetch top-level "bridge_protocol_version"; accumulate an error string `<rel>: found <actual>, expected <expected>` on mismatch and a distinct error on missing/undecodable file. Build the finding with result_check(id: "contract.version_parity", code: errors==[] && "diag.contract.version_parity_ok" || "diag.contract.version_parity_failed", category: :contract_version_parity, passed?: errors == [], message: green/red text naming the drifted surfaces, hint: the regenerate verb, docs_reference: a contract guide, proof_class: :merge_blocking, claim_scope: "Contract version parity across committed surfaces", details: %{version: expected, surfaces: [...rel...], errors: errors}). Register the function call in build_checks/4 (lines 162-180) next to generator_coordinate_parity_check(cwd, ...). Do NOT call Mix.Task.run or write any file. Decode JSON; never grep.
    <!-- planner-discipline-allow: mix crosswake.contract.gen -->
  </action>
  <verify>
    <automated>mix compile --warnings-as-errors && mix test test/crosswake/doctor/publish_readiness_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: publish_readiness.ex contains `defp contract_version_parity_check` and registers it in build_checks; it contains `Crosswake.Bridge.Contract.version` and `Jason.decode` and does NOT contain `Mix.Task.run` or `File.write` inside the new check.
    - Behavior assertion: a doctor report built on the current tree contains a `:contract_version_parity` check with result `:pass` (all surfaces at 1.1.0).
    - CLI output: `mix compile --warnings-as-errors` succeeds.
  </acceptance_criteria>
  <done>The doctor report includes a passing contract_version_parity check on the current tree; the check reads and parses surfaces without shelling out or writing.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Test contract_version_parity present, green on agreement, blocking on drift</name>
  <files>test/crosswake/doctor/publish_readiness_test.exs</files>
  <read_first>
    - test/crosswake/doctor/publish_readiness_test.exs:113-192 (the category-presence test list and the generator_coordinate_parity drift test using tmp_dir!, readiness_report(cwd: target), find_check!, and assertions on blocking/result/severity/proof_class/details — clone this exactly)
    - lib/crosswake/doctor/publish_readiness.ex:111-134 (findings/1, which drops :pass and maps to Check structs — proves a failing parity check becomes an :error doctor finding)
  </read_first>
  <behavior>
    - A test asserts :contract_version_parity is in the report categories and a diag.contract. code prefix is present.
    - A test asserts the check result is :pass / not blocking on the current real tree (all surfaces agree).
    - A drift test writes a tmp cwd with one surface (e.g. an ios crosswake_manifest.json) carrying a wrong top-level bridge_protocol_version, runs readiness_report(cwd: target), finds the contract_version_parity check, and asserts blocking, result :fail, severity :error, proof_class :merge_blocking, details.version == Contract.version(), and the drifted relative path appears in details.errors.
  </behavior>
  <action>
    In test/crosswake/doctor/publish_readiness_test.exs add :contract_version_parity to the category-presence assertions and a diag.contract. code-prefix assertion (lines 113-137 pattern). Add a positive test asserting the contract_version_parity check is result :pass / not blocking on the real tree. Add a drift test cloning the generator_coordinate_parity drift test (lines 139-192): tmp_dir!, write a minimal JSON at examples/ios_shell_host/Fixtures/crosswake_manifest.json under the tmp cwd with a wrong "bridge_protocol_version", also write the other four surfaces with the correct Crosswake.Bridge.Contract.version() so only the seeded one drifts, call readiness_report(cwd: target), find_check!(report, :contract_version_parity), and assert blocking == true, result == :fail, severity == :error, proof_class == :merge_blocking, details.version == (Application/Contract canonical version), and that the drifted relative path is present in details.errors. Use Crosswake.Bridge.Contract.version() for expected, never a "1.1.0" literal.
  </action>
  <verify>
    <automated>mix test test/crosswake/doctor/publish_readiness_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: the test file contains a drift test that seeds a tmp cwd with a wrong bridge_protocol_version and asserts the contract_version_parity check is `blocking` with result `:fail` and severity `:error`.
    - Behavior assertion: a test asserts `:contract_version_parity` is among report categories.
    - Test command: `mix test test/crosswake/doctor/publish_readiness_test.exs` passes.
  </acceptance_criteria>
  <done>Tests prove the check is present, green on the real tree, and blocking with an :error finding when a surface drifts in a tmp cwd.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator checkout → doctor | the committed surfaces in an arbitrary operator cwd are the untrusted input the check inspects |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-122-04 | Tampering | a mutating doctor check that regenerates surfaces as a side effect masks drift | mitigate | check is read-only parse, never calls Mix.Task.run / File.write — verified by a source assertion |
| T-122-05 | Information disclosure | check reads files outside the repo via path traversal | accept | paths are fixed relative literals joined under cwd; no operator-supplied path enters the check |
| T-122-06 | Repudiation | check silently passes on a missing/undecodable surface | mitigate | missing/undecodable file accumulates an explicit error string and blocks |
</threat_model>

<verification>
- `mix test test/crosswake/doctor/publish_readiness_test.exs` is green.
- `mix compile --warnings-as-errors` succeeds.
- A tmp-cwd drift seed makes the contract_version_parity check blocking with an :error result.
</verification>

<success_criteria>
mix crosswake.doctor emits a contract_version_parity finding alongside generator_coordinate_parity; it is a read-only parse at :error/:merge_blocking severity, green when all committed surfaces agree, blocking with the drifted file named when one diverges.
</success_criteria>

<output>
Create `.planning/phases/122-drift-guards/122-02-SUMMARY.md` when done.
</output>
