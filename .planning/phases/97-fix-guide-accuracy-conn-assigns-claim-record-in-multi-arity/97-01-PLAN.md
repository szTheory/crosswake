---
phase: 97-fix-guide-accuracy-conn-assigns-claim-record-in-multi-arity
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - guides/threadline.md
  - test/crosswake/proof/phase96_threadline_docs_contract_test.exs
autonomous: true
requirements: [WR-03, WR-02, D-03]

must_haves:
  truths:
    - "guides/threadline.md no longer claims the thread id is placed in conn.assigns[:thread_id]"
    - "guides/threadline.md tells adopters to read the thread id via Logger.metadata()[:crosswake_thread_id]"
    - "guides/threadline.md documents record_in_multi/3 (not /2)"
    - "A regression assertion fails if the conn.assigns claim ever reappears (the Logger.metadata read-path string disappears)"
    - "A regression assertion fails if record_in_multi/3 ever reverts to /2"
    - "The existing 21 phase-96 parity assertions still pass after the guide edits"
  artifacts:
    - path: "guides/threadline.md"
      provides: "Corrected Propagation Contract wording (WR-03) and corrected record_in_multi arity (WR-02)"
      contains: "Logger.metadata()[:crosswake_thread_id]"
    - path: "test/crosswake/proof/phase96_threadline_docs_contract_test.exs"
      provides: "Two regression-prevention assertions guarding WR-03 and WR-02"
      contains: "record_in_multi/3"
  key_links:
    - from: "test/crosswake/proof/phase96_threadline_docs_contract_test.exs"
      to: "guides/threadline.md"
      via: "File.read!(\"guides/threadline.md\") =~ substring"
      pattern: "Logger.metadata\\(\\)\\[:crosswake_thread_id\\]"
    - from: "test/crosswake/proof/phase96_threadline_docs_contract_test.exs"
      to: "guides/threadline.md"
      via: "File.read!(\"guides/threadline.md\") =~ substring"
      pattern: "record_in_multi/3"
---

<objective>
Fix two documentation inaccuracies in `guides/threadline.md` surfaced by the v7.0 milestone audit, and add two regression-prevention assertions so neither bug can silently reappear.

- WR-03: The guide claims the plug "places it in `conn.assigns[:thread_id]`" but `Crosswake.Plug.Threadline` never calls `Conn.assign/3` — it stores the id in `Logger.metadata` and echoes it in the response header.
- WR-02: The guide says "Use `record_in_multi/2`" but the generated template ships `record_in_multi(multi, name, attrs)` — arity 3.

Purpose: Close two adoption footguns. Adopters following the old guide get `nil` from `conn.assigns` and a `FunctionClauseError` from `record_in_multi/2`.
Output: Two corrected guide lines + two new hermetic parity assertions, in one atomic commit.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/97-fix-guide-accuracy-conn-assigns-claim-record-in-multi-arity/97-CONTEXT.md
@.planning/phases/97-fix-guide-accuracy-conn-assigns-claim-record-in-multi-arity/97-RESEARCH.md

<interfaces>
<!-- Verified code truths the executor relies on. No codebase exploration needed. -->

Source of truth for WR-03 — lib/crosswake/plug/threadline.ex (call/2):
  - Stores id via Logger.metadata([{logger_key, id}]); logger_key defaults to :crosswake_thread_id.
  - Echoes id via Conn.put_resp_header(conn, header_name, id).
  - NEVER calls Conn.assign/3 anywhere in the module.

Source of truth for WR-02 — priv/templates/crosswake/audit/ledger.ex.eex:
  - Generated function signature: record_in_multi(multi, name, attrs) — arity 3.

Stable read-path surface — test/crosswake/plug/threadline_test.exs:
  - Exercises Logger.metadata()[:crosswake_thread_id] verbatim (asserted public surface).

Existing parity-test pattern — test/crosswake/proof/phase96_threadline_docs_contract_test.exs:
  - Each contract is its own `test "..." do` block.
  - Body: `guide = File.read!("guides/threadline.md")` then `assert guide =~ "exact string", "guides/threadline.md must ... — update the ... section"`.
  - File ends at line 241 with the module-closing `end`. New tests go before that final `end`.

Pre-fix state (verified by grep, must change):
  - guides/threadline.md line 22 contains `conn.assigns[:thread_id]` (1 occurrence).
  - guides/threadline.md line 128 contains `record_in_multi/2` (1 occurrence).
  - `Logger.metadata()[:crosswake_thread_id]` currently has 0 occurrences in the guide.
  - `record_in_multi/3` currently has 0 occurrences in the guide.
  - WARNING: `Logger.metadata` (without the read path) ALREADY appears in the guide at lines 7 and 15 in unrelated prose. A bare `assert guide =~ "Logger.metadata"` would pass even with the bug present, so it CANNOT detect the regression. The assertion MUST check the read-path string instead (see Task 1).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix WR-03 + WR-02 guide lines and add two regression-prevention assertions (one atomic commit)</name>
  <files>guides/threadline.md, test/crosswake/proof/phase96_threadline_docs_contract_test.exs</files>
  <read_first>
    - guides/threadline.md (full current state — confirm line 22 conn.assigns text and line 128 record_in_multi/2 text before editing)
    - test/crosswake/proof/phase96_threadline_docs_contract_test.exs (full file — replicate the exact `test "..." do` + `File.read!` + `assert guide =~` + custom-failure-message shape; note the module-closing `end` on the last line)
    - .planning/phases/97-fix-guide-accuracy-conn-assigns-claim-record-in-multi-arity/97-CONTEXT.md (D-01 verbatim replacement wording, D-02, D-03)
    - lib/crosswake/plug/threadline.ex (confirm storage is Logger.metadata, never Conn.assign/3)
    - priv/templates/crosswake/audit/ledger.ex.eex (confirm generated record_in_multi/3 signature)
  </read_first>
  <action>
    Make all edits, then commit once. The guide edit and the test assertions must land together — splitting them would leave CI red between commits.

    WR-03 (guides/threadline.md line 22): Replace the sentence ending in "and places it in `conn.assigns[:thread_id]`." Use the D-01 verbatim wording: keep the existing first sentence through the posture rhythm ("The header name is `X-Crosswake-Thread-Id`. The plug reads an inbound header (posture: `:inbound`) or mints a new id (posture: `:minted`)."), then append a second sentence: "It stores the id in `Logger.metadata` under the `:crosswake_thread_id` key — read it in downstream plugs or controllers via `Logger.metadata()[:crosswake_thread_id]`." The literal substring `Logger.metadata()[:crosswake_thread_id]` MUST appear. Do not weaken to a bare "Logger.metadata" mention. After editing, the string `conn.assigns` must have 0 occurrences in the guide.

    WR-02 (guides/threadline.md line 128): Change `record_in_multi/2` to `record_in_multi/3`. No other prose on that line changes. After editing, `record_in_multi/2` must have 0 occurrences in the guide and `record_in_multi/3` must have exactly 1.

    Regression assertions (test/crosswake/proof/phase96_threadline_docs_contract_test.exs): Add two new `test "..." do` blocks before the final module-closing `end` (currently line 241), following the existing custom-failure-message pattern.
      1. WR-03 guard — assert `guide =~ "Logger.metadata()[:crosswake_thread_id]"` (the read-path string, NOT bare "Logger.metadata" which already exists in unrelated prose at lines 7/15 and would not detect the bug). Failure message must name the contract element and point to the Propagation Contract section, noting the plug never calls Conn.assign/3. Reference WR-03.
      2. WR-02 guard — assert `guide =~ "record_in_multi/3"`. Failure message must name the contract element and point to the Operations > Scaffolding the ledger section, noting the generated template ships arity 3. Reference WR-02.

    Do NOT add a new test file. Do NOT touch lib/ or priv/ — those are already correct and are the source of truth.
  </action>
  <verify>
    <automated>grep -c "conn.assigns" guides/threadline.md | grep -qx 0 && grep -c "record_in_multi/2" guides/threadline.md | grep -qx 0 && grep -qF "Logger.metadata()[:crosswake_thread_id]" guides/threadline.md && grep -qF "record_in_multi/3" guides/threadline.md && mix test test/crosswake/proof/phase96_threadline_docs_contract_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "conn.assigns" guides/threadline.md` outputs 0
    - `grep -c "record_in_multi/2" guides/threadline.md` outputs 0
    - `grep -F "Logger.metadata()[:crosswake_thread_id]" guides/threadline.md` matches (read-path string present)
    - `grep -F "record_in_multi/3" guides/threadline.md` matches exactly once
    - test/crosswake/proof/phase96_threadline_docs_contract_test.exs contains a test asserting `guide =~ "Logger.metadata()[:crosswake_thread_id]"` with a WR-03 custom failure message
    - test/crosswake/proof/phase96_threadline_docs_contract_test.exs contains a test asserting `guide =~ "record_in_multi/3"` with a WR-02 custom failure message
    - `mix test test/crosswake/proof/phase96_threadline_docs_contract_test.exs` exits 0 (all assertions including the original 21 pass)
  </acceptance_criteria>
  <done>
    Both guide lines corrected, both regression assertions added, the phase-96 parity test suite passes green, and no new test file was created. All changes in a single commit.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | This phase edits only markdown documentation and a hermetic test file. No untrusted input crosses any boundary; no runtime surface changes. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-97-01 | Tampering | guides/threadline.md doc drift | mitigate | Two hermetic parity assertions fail CI if the conn.assigns claim or record_in_multi/2 arity reappears |
| T-97-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this phase (RESEARCH.md §Package Legitimacy Audit: zero external packages); no slopcheck surface |
</threat_model>

<verification>
- `mix test test/crosswake/proof/phase96_threadline_docs_contract_test.exs` passes (new + existing assertions green).
- `mix test` full suite passes (no collateral breakage from the guide edits).
- `grep "conn.assigns" guides/threadline.md` and `grep "record_in_multi/2" guides/threadline.md` both return nothing.
</verification>

<success_criteria>
- WR-03 corrected: guide directs adopters to `Logger.metadata()[:crosswake_thread_id]`; zero `conn.assigns` occurrences.
- WR-02 corrected: guide documents `record_in_multi/3`; zero `record_in_multi/2` occurrences.
- Both regression assertions present and passing; they genuinely fail against the pre-fix state (read-path string and `/3` arity are absent before the fix).
- All pre-existing phase-96 parity assertions still pass.
- Single atomic commit.
</success_criteria>

<output>
Create `.planning/phases/97-fix-guide-accuracy-conn-assigns-claim-record-in-multi-arity/97-01-SUMMARY.md` when done
</output>
