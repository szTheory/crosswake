---
phase: "71"
plan: "03"
type: execute
wave: 2
depends_on:
  - "71-01"
  - "71-02"
files_modified:
  - .github/workflows/phase71-proof.yml
  - guides/support_matrix.md
  - guides/companions.md
  - guides/user_flows.md
  - lib/crosswake/support_matrix/support_matrix.ex
  - lib/crosswake/operator_inspection.ex
  - test/crosswake/support_matrix/support_matrix_test.exs
  - test/crosswake/operator_inspection/operator_inspection_test.exs
autonomous: true
requirements:
  - NOTF-01
  - NOTF-02
must_haves:
  truths:
    - "D-05: Phase 71 has a targeted CI workflow with pinned actions, `permissions: contents: read`, compile with warnings as errors, and a named merge-blocking proof job."
    - "D-21: CI and documentation do not assert APNs/FCM delivery, tray display, Focus/Doze/background behavior, provider credentials, push metrics, read receipts, or real-device opens."
    - "D-22: Main deliverable remains proof-first; docs/support/operator updates are narrow and only prevent adopter confusion."
    - "D-23: Support truth says notification-open workflow proof is hermetic route activation proof, not delivery proof."
    - "D-24: Guidance clarifies Chimeway + Sigra interplay: token/open evidence is not auth authority, backend records own binding/intents, and RouteGate/Sigra decide activation."
    - "D-25: If any example-host UI route is touched, it stays a compact Phoenix-owned proof/status panel rather than a notification center, delivery dashboard, or native tray simulation."
    - "D-26: Any touched UI keeps existing Phoenix/LiveView defaults, semantic headings, real buttons/links, visible focus states, system light/dark behavior, and a `role=\"status\"` region; no theme switcher or component library is added."
    - "D-27: Allowed copy includes `Notification open resolved through RouteGate`, `Recent authentication required before opening this route`, `Open intent expired. No route was activated`, `Binding revoked. Notification open denied`, `APNs/FCM delivery is not part of this proof`, and `Token evidence is bound by the backend; possession does not grant access`."
    - "D-28: Forbidden copy includes `push delivered`, `notification guaranteed`, `opened from APNs/FCM`, `user authenticated by notification`, and `real-time push workflow`."
  artifacts:
    - path: .github/workflows/phase71-proof.yml
      provides: Phase 71 merge-blocking hermetic proof workflow plus advisory non-delivery lane notices
    - path: guides/support_matrix.md
      provides: Public support wording that separates notification-open route activation from APNs/FCM delivery
    - path: guides/companions.md
      provides: Chimeway + Sigra route activation guidance
    - path: guides/user_flows.md
      provides: Notification re-entry user-flow wording without delivery/auth overclaims
    - path: lib/crosswake/support_matrix/support_matrix.ex
      provides: Machine-readable support posture for notification-open workflow proof
    - path: lib/crosswake/operator_inspection.ex
      provides: Operator inspection truth for notification-open route policy and auth posture
  key_links:
    - from: .github/workflows/phase71-proof.yml
      to: test/crosswake/proof/phase71_notification_workflow_proof_test.exs
      via: targeted merge-blocking proof command
    - from: guides/support_matrix.md
      to: lib/crosswake/support_matrix/support_matrix.ex
      via: support truth parity
    - from: guides/companions.md
      to: lib/crosswake/operator_inspection.ex
      via: Chimeway/Sigra operator wording
---

<objective>
Add Phase 71 CI proof wiring and narrow support/operator/docs truth so the completed notification-driven workflow proof is merge-blocking, hermetic, and explicitly not a push delivery or device proof.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/MILESTONE-ARC.md
@.planning/phases/71-notification-driven-workflow-proof/71-CONTEXT.md
@.planning/phases/71-notification-driven-workflow-proof/71-RESEARCH.md
@.planning/phases/71-notification-driven-workflow-proof/71-VALIDATION.md
@.planning/phases/71-notification-driven-workflow-proof/71-PATTERNS.md
@.planning/phases/71-notification-driven-workflow-proof/71-01-PLAN.md
@.planning/phases/71-notification-driven-workflow-proof/71-02-PLAN.md
@.planning/phases/70-subscription-saas-commerce-proof/70-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 71-03-01: Add Phase 71 CI Proof Workflow</name>
  <files>.github/workflows/phase71-proof.yml</files>
  <read_first>
    - .github/workflows/phase71-proof.yml
    - .github/workflows/phase70-proof.yml
    - .github/workflows/phase48-proof.yml
    - test/crosswake/proof/phase71_notification_workflow_proof_test.exs
    - .planning/phases/71-notification-driven-workflow-proof/71-VALIDATION.md
    - .planning/phases/71-notification-driven-workflow-proof/71-PATTERNS.md
  </read_first>
  <action>Create `.github/workflows/phase71-proof.yml` based on `.github/workflows/phase70-proof.yml`. Set workflow name `Phase 71 Proof`, `permissions: contents: read`, pinned `actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd`, pinned `erlef/setup-beam@fc68ffb90438ef2936bbb3251622353b3dcb2f93`, Elixir `1.19.5`, OTP `27.3`, PR/push/workflow_dispatch/schedule triggers, `lane` input values `merge-blocking`, `advisory`, and `all`, and `DEVELOPER_DIR: /Applications/Xcode_26.0.app/Contents/Developer`. Add merge-blocking macOS job `merge-blocking-notification-workflow-proof` that runs `mix deps.get`, `mix compile --warnings-as-errors`, and `mix test test/crosswake/proof/phase71_notification_workflow_proof_test.exs`. Add advisory push provider/device job with `continue-on-error: true` and notices stating APNs/FCM delivery, tray behavior, Focus/Doze/background delivery, and real devices are advisory only, never gate merge, and do not auto-promote support posture.</action>
  <verify>
    <automated>grep -q "mix test test/crosswake/proof/phase71_notification_workflow_proof_test.exs" .github/workflows/phase71-proof.yml</automated>
    <automated>grep -q "continue-on-error: true" .github/workflows/phase71-proof.yml</automated>
    <automated>grep -q "permissions: contents: read" .github/workflows/phase71-proof.yml</automated>
  </verify>
  <acceptance_criteria>
    - Workflow file exists at `.github/workflows/phase71-proof.yml`.
    - Workflow contains `permissions: contents: read`.
    - Merge-blocking job runs `mix compile --warnings-as-errors` and `mix test test/crosswake/proof/phase71_notification_workflow_proof_test.exs`.
    - Advisory job contains `continue-on-error: true`.
    - Workflow text states APNs/FCM delivery, tray behavior, Focus/Doze/background delivery, and real-device proof are advisory and non-promoting.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 71-03-02: Update Support Truth For Notification Route Activation Proof</name>
  <files>guides/support_matrix.md, lib/crosswake/support_matrix/support_matrix.ex, test/crosswake/support_matrix/support_matrix_test.exs</files>
  <read_first>
    - guides/support_matrix.md
    - lib/crosswake/support_matrix/support_matrix.ex
    - test/crosswake/support_matrix/support_matrix_test.exs
    - test/crosswake/support_matrix/renderer_test.exs
    - lib/crosswake/companions/chimeway/denial_codes.ex
    - lib/crosswake/compatibility/route_gate.ex
    - .planning/phases/71-notification-driven-workflow-proof/71-CONTEXT.md
    - .planning/phases/71-notification-driven-workflow-proof/71-RESEARCH.md
  </read_first>
  <action>Update support truth only where current wording would overclaim or omit the Phase 71 proof posture. Keep Chimeway delivery support as unsupported/advisory. Add or refine support text to say `notification-open workflow proof is hermetic route activation proof`, `RouteGate and Sigra decide activation`, `notification token/open evidence is not auth authority`, and `APNs/FCM delivery is not part of this proof`. If `SupportMatrix` has a machine-readable Chimeway notification-open field, add a concise proof posture value such as `route_activation_proof: :hermetic` or equivalent existing data-shape field; otherwise keep the change to existing copy fields. Extend support matrix tests to assert delivery remains unsupported/advisory and route-activation proof wording is present.</action>
  <verify>
    <automated>mix test test/crosswake/support_matrix/support_matrix_test.exs test/crosswake/support_matrix/renderer_test.exs</automated>
    <automated>rg -n "push delivered|notification guaranteed|opened from APNs/FCM|user authenticated by notification|real-time push workflow" guides/support_matrix.md lib/crosswake/support_matrix/support_matrix.ex &amp;&amp; exit 1 || exit 0</automated>
  </verify>
  <acceptance_criteria>
    - Support truth explicitly distinguishes notification-open route activation proof from APNs/FCM delivery proof.
    - Support truth preserves `delivery_supported: false` or the existing equivalent unsupported/advisory delivery posture.
    - Tests assert route activation proof wording or machine-readable equivalent exists.
    - Tests assert APNs/FCM delivery remains unsupported/advisory.
    - Forbidden phrases `push delivered`, `notification guaranteed`, `opened from APNs/FCM`, `user authenticated by notification`, and `real-time push workflow` are absent from touched support files.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 71-03-03: Update Companion And User-Flow Guidance Without Delivery Overclaims</name>
  <files>guides/companions.md, guides/user_flows.md</files>
  <read_first>
    - guides/companions.md
    - guides/user_flows.md
    - guides/support_matrix.md
    - lib/crosswake/companions/chimeway/resolver.ex
    - lib/crosswake/companions/chimeway/contracts.ex
    - lib/crosswake/companions/sigra/evaluator.ex
    - .planning/phases/71-notification-driven-workflow-proof/71-CONTEXT.md
    - .planning/phases/71-notification-driven-workflow-proof/71-PATTERNS.md
  </read_first>
  <action>Update only Chimeway/Sigra notification re-entry guidance. Add concise copy explaining that backend-owned token binding and one-time open intents feed `Crosswake.Companions.Chimeway.Resolver.resolve/3`, and route activation still flows through manifest-known routes, `RouteGate`, and Sigra recent-auth checks. Use allowed phrases from context such as `Notification open resolved through RouteGate`, `Recent authentication required before opening this route`, `Open intent expired. No route was activated`, `Binding revoked. Notification open denied`, `APNs/FCM delivery is not part of this proof`, and `Token evidence is bound by the backend; possession does not grant access`. Remove or rewrite any stale language implying push delivery, real-time guarantee, notification-granted auth, direct mutation, or route bypass. Do not add a notification center, action registry, native tray flow, provider credential setup, or full step-up continuation guide.</action>
  <verify>
    <automated>rg -n "Notification open resolved through RouteGate|Recent authentication required before opening this route|APNs/FCM delivery is not part of this proof|Token evidence is bound by the backend; possession does not grant access" guides/companions.md guides/user_flows.md</automated>
    <automated>rg -n "push delivered|notification guaranteed|opened from APNs/FCM|user authenticated by notification|real-time push workflow|notification center|action registry" guides/companions.md guides/user_flows.md &amp;&amp; exit 1 || exit 0</automated>
  </verify>
  <acceptance_criteria>
    - Guides describe Chimeway + Sigra interplay through backend token binding, one-time open intent, manifest-known route, RouteGate, and Sigra recent-auth checks.
    - Guides include at least two allowed proof/truth phrases listed in the task action.
    - Guides do not imply notification payload possession grants auth or route access.
    - Guides do not add notification center, generic action registry, native tray, provider credential, or step-up continuation scope.
    - Forbidden copy scan exits 0.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 71-03-04: Align Operator Inspection And Final Phase Verification</name>
  <files>lib/crosswake/operator_inspection.ex, test/crosswake/operator_inspection/operator_inspection_test.exs</files>
  <read_first>
    - lib/crosswake/operator_inspection.ex
    - test/crosswake/operator_inspection/operator_inspection_test.exs
    - test/crosswake/operator_inspection/formatter_test.exs
    - test/crosswake/operator_inspection/json_formatter_test.exs
    - lib/crosswake/support_matrix/support_matrix.ex
    - lib/crosswake/compatibility/route_gate.ex
    - .planning/phases/71-notification-driven-workflow-proof/71-VALIDATION.md
  </read_first>
  <action>Inspect operator output for notification-open route truth. If current operator inspection already exposes route-local `notification_open` posture without overclaiming delivery, add tests that lock the existing behavior for route id `saas_approval` or a local notification-open route fixture. If the operator surface needs a narrow truth correction, add only support-safe fields or copy that identify manifest-known notification-open route policy, action allowlist, and auth/RouteGate posture; do not add APNs/FCM delivery status, device proof, tray state, push metrics, or notification-center concepts. Run the full Phase 71 verification set from `71-VALIDATION.md` plus final compile.</action>
  <verify>
    <automated>mix test test/crosswake/operator_inspection/operator_inspection_test.exs test/crosswake/operator_inspection/formatter_test.exs test/crosswake/operator_inspection/json_formatter_test.exs</automated>
    <automated>mix compile --warnings-as-errors &amp;&amp; mix test test/crosswake/proof/phase71_notification_workflow_proof_test.exs test/crosswake/companions/chimeway/resolver_test.exs test/crosswake/compatibility/route_gate_test.exs</automated>
    <automated>mix test test/crosswake/proof/phase46_sigra_auth_contract_test.exs</automated>
    <automated>git diff --check -- .github/workflows/phase71-proof.yml guides/support_matrix.md guides/companions.md guides/user_flows.md lib/crosswake/support_matrix/support_matrix.ex lib/crosswake/operator_inspection.ex test/crosswake/support_matrix/support_matrix_test.exs test/crosswake/operator_inspection/operator_inspection_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - Operator inspection tests exit 0.
    - Phase 71 validation command exits 0.
    - `mix test test/crosswake/proof/phase46_sigra_auth_contract_test.exs` exits 0.
    - `git diff --check` exits 0 for all files touched by Plan 71-03.
    - Operator output does not include APNs/FCM delivery support, push metrics, native tray state, notification center, or provider credential claims as Phase 71 merge-blocking truth.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI workflow -> merge gate | CI must clearly separate hermetic route-activation proof from advisory provider/device delivery evidence. |
| Support matrix -> public claims | Public support truth must not promote APNs/FCM delivery or device behavior from hermetic evidence. |
| Guides -> adopter implementation | Guidance must not teach users that notification payloads grant auth or bypass RouteGate. |
| Operator inspection -> production diagnostics | Operator output must expose route policy/auth posture without creating delivery or push-platform claims. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-71-11 | Repudiation | CI proof posture | medium | mitigate | Workflow names merge-blocking and advisory jobs explicitly and runs the targeted Phase 71 proof command. |
| T-71-12 | Elevation of Privilege | Public guidance | high | mitigate | Guides state token/open evidence is not auth authority and RouteGate/Sigra decide activation. |
| T-71-13 | Information Disclosure | Support/operator truth | medium | mitigate | Support/operator copy stays posture-level and avoids raw tokens, payloads, route params, actor/session refs, and device details. |
| T-71-14 | Spoofing | Delivery support claims | medium | mitigate | CI/support docs assert APNs/FCM delivery and real-device proof remain advisory/non-promoting. |

## High Severity Block

Block completion if any public guide or operator/support surface implies notification evidence authenticates a user, bypasses RouteGate, grants mutation authority, or silently opens a fallback route.
</threat_model>

<verification>
- `grep -q "mix test test/crosswake/proof/phase71_notification_workflow_proof_test.exs" .github/workflows/phase71-proof.yml`
- `grep -q "continue-on-error: true" .github/workflows/phase71-proof.yml`
- `grep -q "permissions: contents: read" .github/workflows/phase71-proof.yml`
- `mix test test/crosswake/support_matrix/support_matrix_test.exs test/crosswake/support_matrix/renderer_test.exs`
- `mix test test/crosswake/operator_inspection/operator_inspection_test.exs test/crosswake/operator_inspection/formatter_test.exs test/crosswake/operator_inspection/json_formatter_test.exs`
- `mix compile --warnings-as-errors &amp;&amp; mix test test/crosswake/proof/phase71_notification_workflow_proof_test.exs test/crosswake/companions/chimeway/resolver_test.exs test/crosswake/compatibility/route_gate_test.exs`
- `mix test test/crosswake/proof/phase46_sigra_auth_contract_test.exs`
- `git diff --check -- .github/workflows/phase71-proof.yml guides/support_matrix.md guides/companions.md guides/user_flows.md lib/crosswake/support_matrix/support_matrix.ex lib/crosswake/operator_inspection.ex test/crosswake/support_matrix/support_matrix_test.exs test/crosswake/operator_inspection/operator_inspection_test.exs`
</verification>

<success_criteria>
- Phase 71 CI workflow exists with merge-blocking hermetic route-activation proof and non-blocking advisory provider/device notices.
- Support matrix, guide, and operator truth distinguish notification-open route activation proof from APNs/FCM delivery proof.
- Public guidance states notification evidence is not auth authority and RouteGate/Sigra decide activation.
- All Phase 71 targeted verification commands pass without manual device/provider verification.
</success_criteria>

<output>
Create `.planning/phases/71-notification-driven-workflow-proof/71-03-SUMMARY.md` when done.
</output>
