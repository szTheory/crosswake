---
phase: "63"
plan: "03"
type: execute
wave: 2
depends_on: ["63-01", "63-02"]
files_modified:
  - "lib/crosswake/planning/closeout_verifier.ex"
  - "test/crosswake/planning/closeout_verifier_test.exs"
  - ".planning/ROADMAP.md"
autonomous: true
requirements:
  - PROOF-01
  - PROOF-02
must_haves:
  truths:
    - "Milestone v3.9 closeout checks are enforced by the CloseoutVerifier."
    - "Phase 63 and the v3.9 Milestone are marked complete in ROADMAP.md."
  artifacts:
    - path: "lib/crosswake/planning/closeout_verifier.ex"
      provides: "Closeout validation for v3.9 milestone."
    - path: ".planning/ROADMAP.md"
      provides: "Updated shipped state for v3.9."
  key_links:
    - from: "lib/crosswake/planning/closeout_verifier.ex"
      to: "test/crosswake/planning/closeout_verifier_test.exs"
      via: "test validation"
---

<objective>
Update milestone closeout verification and roadmap to mark v3.9 shipped.

Purpose: Guarantee that v3.9 closeout enforces correct milestone requirement mappings, phase verifications, and explicitly transitions Phase 63 to a completed state.
Output: Modifications to CloseoutVerifier, tests, and ROADMAP.md.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/63-hermetic-proof-and-advisory-promotion-criteria/63-RESEARCH.md
@.planning/phases/63-hermetic-proof-and-advisory-promotion-criteria/63-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Update Closeout Verifier for v3.9</name>
  <files>lib/crosswake/planning/closeout_verifier.ex, test/crosswake/planning/closeout_verifier_test.exs</files>
  <behavior>
    - Extend the closeout verifier to support v3.9 validation logic.
    - Check for `v3.9-CLOSEOUT.md` explicitly, handle phases `59` through `63`, and assert that there are no false claims about first-party push delivery guarantees.
    - Tests accurately mock these structures.
  </behavior>
  <action>Modify `lib/crosswake/planning/closeout_verifier.ex` to extract milestone dependencies (e.g. phases 59-63 for v3.9) and support checking `v3.9-CLOSEOUT.md`. Add validation checks asserting the required notification logic mapping. Update `test/crosswake/planning/closeout_verifier_test.exs` with tests covering these new assertions.</action>
  <verify>
    <automated>mix test test/crosswake/planning/closeout_verifier_test.exs</automated>
  </verify>
  <done>The CloseoutVerifier processes v3.9 milestone structures perfectly, failing if closeout artifacts are missing.</done>
</task>

<task type="auto">
  <name>Task 2: Mark Phase and Milestone Complete</name>
  <files>.planning/ROADMAP.md</files>
  <action>Update `.planning/ROADMAP.md`. 
1. Check off Phase 63 (e.g., replace `[ ]` with `[x]`).
2. Update the v3.9 milestone status in the Milestones list to `✅` and marked as "shipped".
3. Record the shipped date matching today. Do not alter unrelated phases.</action>
  <verify>
    <automated>grep -q "✅.*v3.9 Chimeway Notification Seam" .planning/ROADMAP.md</automated>
  </verify>
  <done>ROADMAP correctly reflects Phase 63 and the v3.9 milestone as fully shipped.</done>
</task>

</tasks>

<verification>
`mix test test/crosswake/planning/closeout_verifier_test.exs` passes and `grep -q "✅.*v3.9" .planning/ROADMAP.md` finds the milestone.
</verification>

<success_criteria>
v3.9 milestone closeout logic is structurally enforced and the ROADMAP reflects completion.
</success_criteria>

<output>
Create `.planning/phases/63-hermetic-proof-and-advisory-promotion-criteria/63-03-SUMMARY.md` when done
</output>
