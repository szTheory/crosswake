---
phase: 87-online-liveview-architecture
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - examples/phoenix_host/priv/static/css/app.css
  - examples/phoenix_host/lib/crosswake_example/flashcards/deck_live/index.ex
  - examples/phoenix_host/lib/crosswake_example/flashcards/deck_live/show.ex
  - examples/phoenix_host/lib/crosswake_example/router.ex
autonomous: true
requirements:
  - DEMO-01
  - DEMO-03
must_haves:
  truths:
    - "User can view available decks in the dashboard"
    - "User can select a deck to view its details"
    - "User can download a deck pack"
    - "Online routes use Crosswake Brand Book styles"
  artifacts:
    - path: "examples/phoenix_host/priv/static/css/app.css"
      provides: "Brand Book CSS"
    - path: "examples/phoenix_host/lib/crosswake_example/flashcards/deck_live/index.ex"
      provides: "Dashboard LiveView"
    - path: "examples/phoenix_host/lib/crosswake_example/flashcards/deck_live/show.ex"
      provides: "Deck details and download UI"
    - path: "examples/phoenix_host/lib/crosswake_example/router.ex"
      provides: "Router configuration"
  key_links:
    - from: "examples/phoenix_host/lib/crosswake_example/router.ex"
      to: "examples/phoenix_host/lib/crosswake_example/flashcards/deck_live/index.ex"
      via: "live /decks"
      pattern: "live.*decks"
---

<objective>
Build the online dashboard, course selection, and "Download Pack" UI for the demo app, utilizing the underlying Flashcard data model.

Purpose: Provide a LiveView dashboard for deck management and demonstrate the Brand Book styling for online routes.
Output: Brand styling CSS, `DeckLive.Index` and `DeckLive.Show` LiveViews, and updated routing.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@prompts/crosswake-brand-book.md
@examples/phoenix_host/lib/crosswake_example/router.ex
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Crosswake Brand Book Styling</name>
  <files>examples/phoenix_host/priv/static/css/app.css</files>
  <action>Create a basic CSS stylesheet following `prompts/crosswake-brand-book.md`. Define `:root` variables for the core palette (e.g., `--cw-current-950`, `--cw-foam-50`, `--cw-wake-700`, `--cw-brass-500`). Add basic classes for structural elements like cards, primary buttons, secondary buttons, and page backgrounds. Apply the typography fonts (`Space Grotesk`, `Atkinson Hyperlegible Next`, `JetBrains Mono`). Do not use Tailwind; write standard CSS classes.</action>
  <verify>
    <automated>grep -c 'cw-current-950' examples/phoenix_host/priv/static/css/app.css</automated>
  </verify>
  <done>CSS file created with brand colors and typography variables.</done>
</task>

<task type="auto">
  <name>Task 2: Build DeckLive.Index and DeckLive.Show LiveViews</name>
  <files>examples/phoenix_host/lib/crosswake_example/flashcards/deck_live/index.ex, examples/phoenix_host/lib/crosswake_example/flashcards/deck_live/show.ex</files>
  <action>Implement `CrosswakeExample.Flashcards.DeckLive.Index` to list decks fetched from the data model, and `CrosswakeExample.Flashcards.DeckLive.Show` to display deck details and a "Download Pack" button. Because there may not be a root layout, include `<link rel="stylesheet" href="/css/app.css" />` directly in their rendered `~H` sigils. Apply the new CSS classes for styling (e.g. use the background and text color classes to align with the coastal technical palette).</action>
  <verify>
    <automated>cd examples/phoenix_host && mix compile</automated>
  </verify>
  <done>Both LiveViews exist, compile successfully, and apply brand styles.</done>
</task>

<task type="auto">
  <name>Task 3: Wire LiveViews into Router</name>
  <files>examples/phoenix_host/lib/crosswake_example/router.ex</files>
  <action>Map `live("/decks", DeckLive.Index)` and `live("/decks/:id", DeckLive.Show)` in the `Router` within a new scope (or browser pipeline). Apply a `crosswake_defaults` block specifying `runtime: :live_view, offline: :cached_read_only, security: :standard`.</action>
  <verify>
    <automated>grep -v '^#' examples/phoenix_host/lib/crosswake_example/router.ex | grep -c '/decks'</automated>
  </verify>
  <done>Routes defined and compile cleanly.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client -> LiveView WebSocket | Untrusted UI interaction and form submissions from browser client |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-87-01 | Tampering | DeckLive.Show | mitigate | Verify deck ID exists and user has permission to view/download before serving content. |
| T-87-02 | Info Disclosure | DeckLive.Index | accept | Deck listing is assumed public for this demo app. |
</threat_model>

<verification>
- `app.css` contains brand variables.
- `DeckLive.Index` and `DeckLive.Show` use the CSS classes.
- Router is updated with `/decks`.
- App compiles.
</verification>

<success_criteria>
LiveView pages for Deck Index and Show are accessible, styled according to the brand book, and wired through the router with proper runtime policies.
</success_criteria>

<output>
Create `.planning/phases/87-online-liveview-architecture/87-01-SUMMARY.md` when done
</output>
