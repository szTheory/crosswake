---
phase: "70"
plan: "01"
type: execute
wave: 0
depends_on: []
files_modified:
  - test/crosswake/proof/phase70_subscription_saas_commerce_proof_test.exs
autonomous: true
requirements:
  - SAAS-01
  - SAAS-02
must_haves:
  truths:
    - Phase 70 has an automated hermetic proof contract before implementation work makes it pass.
    - Storefront/provider evidence is asserted as non-authoritative until backend projection verifies it.
    - StoreKit and Play Billing purchase/restore corridors are represented in the proof matrix.
  artifacts:
    - path: test/crosswake/proof/phase70_subscription_saas_commerce_proof_test.exs
      provides: Wave 0 red proof contract for subscription SaaS commerce corridor
  key_links:
    - from: test/crosswake/proof/phase70_subscription_saas_commerce_proof_test.exs
      to: examples/phoenix_host/lib/crosswake_example/commerce/provider_adapter_storefront.ex
      via: provider facade purchase/restore assertions
    - from: test/crosswake/proof/phase70_subscription_saas_commerce_proof_test.exs
      to: examples/phoenix_host/lib/crosswake_example/commerce/entitlement_projection.ex
      via: backend-only projection assertions
---

<objective>
Create the Wave 0 automated proof contract for Phase 70 before implementation changes. The proof must be hermetic ExUnit, targeted at `SAAS-01` and `SAAS-02`, and intentionally expose the missing deterministic verifier/provider-matrix behaviors that later plans must satisfy.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/70-subscription-saas-commerce-proof/70-CONTEXT.md
@.planning/phases/70-subscription-saas-commerce-proof/70-RESEARCH.md
@.planning/phases/70-subscription-saas-commerce-proof/70-VALIDATION.md
@.planning/phases/70-subscription-saas-commerce-proof/70-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 70-01-01: Create Phase 70 Hermetic Proof Shell</name>
  <files>test/crosswake/proof/phase70_subscription_saas_commerce_proof_test.exs</files>
  <read_first>
    - test/crosswake/proof/phase34_paywall_corridor_proof_test.exs
    - test/crosswake/proof/phase48_provider_adapter_proof_test.exs
    - .planning/phases/70-subscription-saas-commerce-proof/70-VALIDATION.md
    - .planning/phases/70-subscription-saas-commerce-proof/70-PATTERNS.md
  </read_first>
  <action>Create `Crosswake.Proof.Phase70SubscriptionSaasCommerceProofTest` with `use ExUnit.Case, async: false`. Add `Code.require_file` calls only for pure commerce modules listed in `70-PATTERNS.md`: `reconciliation_keys.ex`, `reconciliation_inbox.ex`, `entitlement_projection.ex`, `mock_backend.ex`, `storefront_adapter.ex`, `provider_adapter_storefront.ex`, and `mock_storefront.ex`. Add Application env cleanup helpers for `:paywall_storefront_adapter` and `:paywall_storefront_provider`. Add a hermeticity self-scan that fails if the proof requires runtime/server files or contains process/server tokens.</action>
  <verify>
    <automated>mix test test/crosswake/proof/phase70_subscription_saas_commerce_proof_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - Source contains module name `Crosswake.Proof.Phase70SubscriptionSaasCommerceProofTest`.
    - Source contains `use ExUnit.Case, async: false`.
    - Source contains exactly the allowed pure-commerce `Code.require_file` targets, with no `_live`, `endpoint`, `application`, `router`, `repo`, or `_web` required paths.
    - Running `mix test test/crosswake/proof/phase70_subscription_saas_commerce_proof_test.exs` reaches the new proof file; failures are acceptable in Wave 0 only when they correspond to missing Phase 70 implementation assertions from Tasks 70-01-02 or 70-01-03.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 70-01-02: Add Red Provider Purchase/Restore Matrix Assertions</name>
  <files>test/crosswake/proof/phase70_subscription_saas_commerce_proof_test.exs</files>
  <read_first>
    - test/crosswake/proof/phase48_provider_adapter_proof_test.exs
    - examples/phoenix_host/lib/crosswake_example/commerce/provider_adapter_storefront.ex
    - examples/phoenix_host/lib/crosswake_example/commerce/reconciliation_inbox.ex
    - examples/phoenix_host/lib/crosswake_example/commerce/reconciliation_keys.ex
    - lib/crosswake/companions/store_kit/evidence.ex
    - lib/crosswake/companions/play_billing/evidence.ex
  </read_first>
  <action>Add tests named to cover `StoreKit purchase`, `StoreKit restore`, `Play Billing purchase`, and `Play Billing restore`. Each test must emit provider-shaped evidence through `ProviderAdapterStorefront` direct helpers or configured facade, assert provider-neutral `event_kind` values of `"purchase"` or `"restore"`, ingest through `ReconciliationInbox.ingest_evidence/2`, assert `status == :awaiting_verification`, assert subject/event keys follow `ReconciliationKeys`, and assert no grant occurs before backend projection.</action>
  <verify>
    <automated>mix test test/crosswake/proof/phase70_subscription_saas_commerce_proof_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - Proof file contains test names or describe blocks for all four matrix rows: StoreKit purchase, StoreKit restore, Play Billing purchase, Play Billing restore.
    - Assertions include StoreKit lineage subject identity based on `original_transaction_id` and Play Billing subject identity based on `purchase_token`.
    - Assertions include `attempt.status == :awaiting_verification`.
    - Assertions include rejection of pending projection via `EntitlementProjection.project_snapshot(nil, pending_snapshot)`.
    - Wave 0 command may fail red, but failure output must identify missing implementation or helper behavior rather than syntax/compile errors.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 70-01-03: Add Red Authority-Fence Negative Assertions</name>
  <files>test/crosswake/proof/phase70_subscription_saas_commerce_proof_test.exs</files>
  <read_first>
    - test/crosswake/proof/phase34_paywall_corridor_proof_test.exs
    - test/crosswake/proof/phase34_mock_storefront_test.exs
    - examples/phoenix_host/lib/crosswake_example/commerce/entitlement_projection.ex
    - examples/phoenix_host/lib/crosswake_example/commerce/reconciliation_inbox.ex
    - lib/crosswake/commerce/reconciliation.ex
    - lib/crosswake/commerce/provider_evidence.ex
  </read_first>
  <action>Add negative tests for `SAAS-02`: storefront/device success alone cannot grant, direct `authority_state` evidence override returns `{:error, :authority_lane_mutation_forbidden}`, duplicate replay with a different `correlation_id` keeps the same event key and is marked replay, stale incoming authority returns `{:error, {:stale_authority, snapshot}}`, pending purchase/restore derives `:pending`, refunded/revoked/expired verified snapshots derive `:denied`, and invalid provider event vocabulary is rejected before projection.</action>
  <verify>
    <automated>mix test test/crosswake/proof/phase70_subscription_saas_commerce_proof_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - Proof file directly asserts `Crosswake.Commerce.Reconciliation.authority_mutation_allowed_from_evidence?(evidence) == false`.
    - Proof file directly asserts `{:error, :authority_lane_mutation_forbidden}` for authority override attempts.
    - Proof file contains replay coverage where provider identity is stable but `correlation_id` differs.
    - Proof file contains stale-authority, refunded, revoked, expired, and invalid-provider-vocabulary cases.
    - No assertion grants access from raw client/storefront/device evidence without a verified backend entitlement snapshot.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Storefront/provider evidence -> backend projection | Client/native/provider-shaped evidence enters reconciliation but must not cross into authority without backend verification. |
| Proof file -> example-host modules | Hermetic tests require pure example-host commerce modules without starting Phoenix runtime infrastructure. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-70-01 | Elevation of Privilege | Entitlement projection | high | mitigate | Red proof asserts evidence-only purchase/restore cannot grant entitlement before verified backend projection. |
| T-70-02 | Tampering | Provider identity/replay | medium | mitigate | Proof asserts event/subject identity uses provider keys, not mutable correlation IDs. |
| T-70-03 | Spoofing | Provider vocabulary | medium | mitigate | Proof asserts raw provider enum/status values are rejected before projection. |
| T-70-04 | Denial of Service | Hermetic proof lane | low | mitigate | Self-scan prevents server/runtime starts in merge-blocking proof. |

## High Severity Block

Execution must not proceed past Wave 0 if the proof cannot express `T-70-01` as an automated assertion.
</threat_model>

<verification>
- `mix test test/crosswake/proof/phase70_subscription_saas_commerce_proof_test.exs`
- Wave 0 expected state: compile succeeds and the new proof contract is present; red failures are acceptable only for missing implementation behavior scheduled in Wave 1.
</verification>

<success_criteria>
- Phase 70 has a targeted automated proof file for `SAAS-01` and `SAAS-02`.
- The proof file is hermetic and merge-blocking-safe.
- The red assertions cover provider purchase/restore matrix and backend-only authority boundaries.
</success_criteria>

<output>
Create `.planning/phases/70-subscription-saas-commerce-proof/70-01-SUMMARY.md` when done.
</output>
