---
phase: "94"
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - priv/templates/crosswake/audit/ledger.ex.eex
  - priv/templates/crosswake/audit/migration.exs.eex
autonomous: true
requirements:
  - LEDG-02
  - LEDG-03
  - LEDG-04
  - LEDG-05
  - LEDG-06
must_haves:
  truths:
    - Schema template includes an explicit fail-closed PII guard for metadata
    - Schema template exposes record/1 and record_in_multi/2 without update/delete helpers
    - Migration template defines an append-only structure with required columns
  artifacts:
    - path: priv/templates/crosswake/audit/ledger.ex.eex
      provides: Schema module template with business logic
    - path: priv/templates/crosswake/audit/migration.exs.eex
      provides: Migration template
  key_links: []
---

<objective>
Create the standard templates for the Ecto Audit Schema and Migration.

Purpose: Supply the files that `mix crosswake.gen.audit` will evaluate to scaffold the host-owned ledger.
Output: Two `.eex` templates for schema and migration.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Create Schema Template</name>
  <files>priv/templates/crosswake/audit/ledger.ex.eex</files>
  <action>
    Create the template defining `defmodule <%= app_module %>.Audit.Ledger do`.
    - Use `Ecto.Schema` and `import Ecto.Changeset`.
    - Define schema `"crosswake_audit_events"`.
    - Include all canonical columns matching the `Crosswake.Audit.Ledger` shape. Use `:utc_datetime_usec` for `occurred_at` and `recorded_at` if timestamps aren't used. Alternatively, use standard Ecto timestamps. Let's explicitly define `occurred_at` as `:utc_datetime_usec` and `recorded_at` as `:utc_datetime_usec`.
    - Set `provenance` to `Ecto.Enum` with values `[:device_claimed, :backend_accepted]`.
    - Set `metadata` to `:map`.
    - Define a `changeset/2` that casts and validates required fields.
    - Implement `reject_pii_in_metadata/1` which checks the `metadata` map for a `@forbidden_keys` list (including `email`, `phone`, `ip_address`, `ssn`, `name`, `first_name`, `last_name`, `address`). It MUST check for both atom and string keys and add an error to the changeset if found.
    - Implement `compute_hashes/1` that takes the changeset, builds a payload (e.g., `thread_id|actor_ref|event_class`), hashes it with `:crypto.hash(:sha256, payload)`, and fetches a best-effort `prev_hash` (or "genesis") before `put_change`.
    - Implement `record(attrs)` and `record_in_multi(multi, name, attrs)` with explicit docstrings warning about transactionality.
  </action>
  <verify>
    <automated>test -f priv/templates/crosswake/audit/ledger.ex.eex</automated>
  </verify>
  <done>Template file created with all required Ecto logic.</done>
</task>

<task type="auto">
  <name>Task 2: Create Migration Template</name>
  <files>priv/templates/crosswake/audit/migration.exs.eex</files>
  <action>
    Create the template defining `defmodule <%= app_module %>.Repo.Migrations.CreateCrosswakeAuditEvents do`.
    - Define a `change/0` function.
    - `create table(:crosswake_audit_events) do` ... `end`
    - Add all standard columns: `:thread_id`, `:correlation_id`, `:route_id`, `:actor_ref`, `:actor_kind`, `:event_class`, `:event_type`, `:outcome`, `:provenance` (as `:string`), `:occurred_at` (`:utc_datetime_usec`), `:recorded_at` (`:utc_datetime_usec`), `:idempotency_key`, `:metadata` (`:map`), `:row_hash`, `:prev_hash`.
    - Do NOT generate default timestamps if we use explicit columns. Or if we use timestamps, configure it correctly. Let's use explicit `add` for all columns.
    - Add a `create unique_index(:crosswake_audit_events, [:idempotency_key])`.
  </action>
  <verify>
    <automated>test -f priv/templates/crosswake/audit/migration.exs.eex</automated>
  </verify>
  <done>Template file created with correct schema table and index.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Host DB <-> App | Ecto schema inserts untrusted metadata |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-94-02 | Information Disclosure | `reject_pii_in_metadata/1` | mitigate | Explicit fail-closed guard looping over `@forbidden_keys` rejecting any match. |
| T-94-03 | Tampering | `row_hash`/`prev_hash` | mitigate | Compute advisory hash chain at insert time using `:crypto.hash(:sha256)`. |
| T-94-04 | Tampering | `provenance` | mitigate | Enforce values using `Ecto.Enum` (`:device_claimed`, `:backend_accepted`). |
</threat_model>
