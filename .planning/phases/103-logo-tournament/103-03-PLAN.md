---
phase: 103-logo-tournament
plan: 03
type: execute
wave: 2
depends_on: ["103-01"]
files_modified:
  - brandbook/logo/tournament/candidates/E.svg
  - brandbook/logo/tournament/candidates/F.svg
  - brandbook/logo/tournament/candidates/G.svg
autonomous: true
requirements: [LOGO-01, LOGO-03]
user_setup: []

must_haves:
  truths:
    - "Three integrated typemark candidates (E Wake-W, F Crossing-SS, G Terminal Cuts) exist as path-only SVGs built on the hand-cut wordmark"
    - "Each typemark is a fully integrated type treatment (not a mark beside plain text) — the wake geometry lives inside the letterforms"
    - "E re-cuts the w apexes to the 20-degree wake angle with a trailing hairline wake; F runs the route line through the cross|wake boundary; G applies quiet 20-degree terminal cuts on k/w plus one notch"
    - "No typemark contains a <text> element, a full-viewBox <rect>, or a bounding shape; each carries the mandatory D-06 w/k cuts"
  artifacts:
    - path: "brandbook/logo/tournament/candidates/E.svg"
      provides: "Wake-W typemark — w apexes re-cut to 20 degrees + trailing hairline wake"
      contains: "glyph-5-w"
    - path: "brandbook/logo/tournament/candidates/F.svg"
      provides: "Crossing-SS typemark — route line through the cross|wake boundary"
      contains: "glyph-7-k"
    - path: "brandbook/logo/tournament/candidates/G.svg"
      provides: "Terminal Cuts typemark — low-risk 20-degree k/w terminal cuts + one notch"
  key_links:
    - from: "brandbook/logo/tournament/candidates/E.svg"
      to: "wordmark-custom.svg"
      via: "wordmark glyph paths with further integrated wake-angle cuts on w"
      pattern: "currentColor"
    - from: "brandbook/logo/tournament/candidates/F.svg"
      to: "20-degree route line"
      via: "stroke crossing the wordmark at the cross|wake glyph boundary"
      pattern: "stroke=\"currentColor\""
---

<objective>
Author the three integrated typemark candidates (E–G) at equal production fidelity, each derived from the Wave 1 hand-cut wordmark with the wake geometry integrated INTO the letterforms.

Purpose: LOGO-01/D-01 require 3 integrated typemarks; D-02 says typemarks stand alone (no separate mark/lockup); D-06 mandates the w/k wake cuts on all wordmark renderings. These three plus the four logomarks (103-02) complete the 7-candidate field. The key quality bar (D-specifics): "not just a shitty icon to the left of basic text" — these are unified type treatments.

Output: E.svg (Wake-W), F.svg (Crossing-SS), G.svg (Terminal Cuts) — path-only, currentColor, built on wordmark-custom.svg, committed in candidates/.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/103-logo-tournament/103-CONTEXT.md
@.planning/phases/103-logo-tournament/103-RESEARCH.md
@.planning/phases/103-logo-tournament/103-VALIDATION.md

<!-- DEPENDENCY: 103-01 produced wordmark-custom.svg and check-candidates.mjs. -->
<!-- This plan READS wordmark-custom.svg (does not modify it) and is validated by check-candidates.mjs. -->
<!-- DISJOINT from 103-02: that plan owns A-D + lockups; this plan owns only E/F/G. Both run in Wave 2. -->

<interfaces>
<!-- wordmark-custom.svg (from 103-01): per-glyph paths id="glyph-0-C" .. "glyph-8-e" inside -->
<!--   <g id="wordmark-base" fill="currentColor" fill-rule="evenodd">, viewBox "vbX 0 vbW 72". -->
<!--   Glyph order: C(0) r(1) o(2) s(3) s(4) w(5) a(6) k(7) e(8). The cross|wake boundary is between s(4) and w(5). -->
<!--   Already carries the baseline D-06 cuts on glyph-5-w and glyph-7-k. -->

<!-- 20-degree wake geometry (103-RESEARCH.md "Candidate Geometry Math" + Pattern 4): -->
<!--   slope -tan(20) = -0.364; dx per unit y = 1/tan(20) = 2.747; notch = 1.5x local stroke width. -->
<!--   Surgical node edits, never hand-drawn letterforms (D-09). -->

<!-- HARD CONSTRAINTS (D-04/D-05/D-06, LOGO-03): no <text>, no full-viewBox <rect>, no wrapper shape, -->
<!--   fill/stroke=currentColor; the w/k cuts are mandatory (uncut wordmark candidates are DISQUALIFIED per D-06). -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Candidate E (Wake-W typemark)</name>
  <files>brandbook/logo/tournament/candidates/E.svg</files>
  <read_first>
    - brandbook/logo/tournament/candidates/wordmark-custom.svg (copy glyph paths + viewBox; integrate further on glyph-5-w; do NOT modify the source)
    - 103-CONTEXT.md D-01 E definition (w apexes re-cut to 20° wake angle + trailing hairline wake) and D-06 rider
    - 103-RESEARCH.md "Pattern 4" (apex-cut technique, dx_per_unit_y = 2.747)
    - brandbook/AUDIT.md §8 "D-11 MANDATORY RIDER"
    - 103-CONTEXT.md specifics: "not just a shitty icon to the left of basic text" — fully integrated treatment
  </read_first>
  <action>
    Build E.svg from the wordmark-custom glyph paths. Deepen the integration on glyph-5-w: re-cut both w apexes to the 20-degree wake angle (beyond the baseline D-06 cut) so the w visibly carries the crossing geometry, and add a trailing hairline wake stroke emanating from the w that echoes the route-line slope (-tan20 = -0.364). The wake hairline is a thin stroke="currentColor" element integrated into the wordmark composition, not a separate mark. Keep all other glyphs as the hand-cut wordmark. Single unified viewBox. fill/stroke=currentColor, fill-rule="evenodd" on glyph fills. No <text>, no <rect>, no wrapper. Provenance header noting E integrates the wake into the w apexes.
  </action>
  <acceptance_criteria>
    - E.svg parses, declares a viewBox, contains glyph-5-w, uses currentColor; `node brandbook/tools/check-candidates.mjs` exits 0
    - no `<text` and no `<rect`: `grep -L '<text' E.svg` returns it; `grep -c '<rect' E.svg` returns 0
    - the trailing hairline wake exists as a stroke element at ~20° slope (record measured slope in SUMMARY)
    - w apex cuts visibly deeper than the baseline wordmark (manual; record)
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/jon/projects/crosswake && node brandbook/tools/check-candidates.mjs && grep -q 'glyph-5-w' brandbook/logo/tournament/candidates/E.svg && grep -q 'currentColor' brandbook/logo/tournament/candidates/E.svg && test "$(grep -c '<text' brandbook/logo/tournament/candidates/E.svg)" -eq 0</automated>
    <human-check>Render E at 256px and 24px: the w reads as a wake form with a trailing hairline; the treatment is integrated, not a mark beside text.</human-check>
  </verify>
  <done>E.svg integrates 20-degree wake cuts into the w apexes with a trailing hairline wake, path-only, currentColor, built on the hand-cut wordmark.</done>
</task>

<task type="auto">
  <name>Task 2: Candidate F (Crossing-SS typemark)</name>
  <files>brandbook/logo/tournament/candidates/F.svg</files>
  <read_first>
    - brandbook/logo/tournament/candidates/wordmark-custom.svg (glyph order; the cross|wake boundary is between s(4) and w(5))
    - 103-CONTEXT.md D-01 F definition (route line passes through the wordmark, break lands at the "cross|wake" boundary)
    - 103-RESEARCH.md "Candidate Geometry Math" (route slope -0.364, 1.5x-stroke notch)
    - brandbook/AUDIT.md §8 "Wake Mark geometry" (notch/break = 1.5x stroke, round caps)
  </read_first>
  <action>
    Build F.svg from the wordmark-custom glyph paths (retaining the mandatory glyph-5-w / glyph-7-k cuts). Add a route line stroke crossing through the wordmark at ~20° (-tan20 slope), positioned so its 1.5x-stroke notch/break lands precisely at the "cross|wake" boundary — i.e. between glyph-4-s and glyph-5-w. The route is a stroke="currentColor" round-cap line integrated with the type, split into two segments around the notch at the boundary. The crossing must read as intentional brand geometry, not decoration. Single unified viewBox. No <text>, no <rect>, no wrapper. Provenance header noting the break lands at cross|wake.
  </action>
  <acceptance_criteria>
    - F.svg parses, viewBox present, contains glyph-7-k (proves hand-cut wordmark used), uses currentColor; check-candidates passes
    - the route stroke uses stroke-linecap="round" and a notch break (two segments); recorded in SUMMARY with the boundary x-position relative to glyph-4-s/glyph-5-w
    - no `<text` and no `<rect>` (grep checks pass)
    - route slope within 16–24°; recorded in SUMMARY
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/jon/projects/crosswake && node brandbook/tools/check-candidates.mjs && grep -q 'glyph-7-k' brandbook/logo/tournament/candidates/F.svg && grep -q 'stroke-linecap="round"' brandbook/logo/tournament/candidates/F.svg && test "$(grep -c '<text' brandbook/logo/tournament/candidates/F.svg)" -eq 0</automated>
    <human-check>Render F at 256px and 24px: the route line crosses the wordmark with its break at the cross|wake boundary, reading as deliberate brand geometry.</human-check>
  </verify>
  <done>F.svg runs a 20-degree route line through the wordmark with the notch landing at the cross|wake boundary, path-only, currentColor.</done>
</task>

<task type="auto">
  <name>Task 3: Candidate G (Terminal Cuts typemark — low-risk fallback)</name>
  <files>brandbook/logo/tournament/candidates/G.svg</files>
  <read_first>
    - brandbook/logo/tournament/candidates/wordmark-custom.svg (glyph paths for k(7) and w(5))
    - 103-CONTEXT.md D-01 G definition (quiet 20° terminal cuts on k/w + one notch detail — deliberate low-risk fallback)
    - 103-RESEARCH.md "Pattern 4" (terminal-cut technique)
    - brandbook/logo/tournament/candidates/E.svg (match fidelity / stroke conventions for parity)
    - 103-CONTEXT.md "Equal production fidelity across all 7"
  </read_first>
  <action>
    Build G.svg from the wordmark-custom glyph paths. Apply QUIET 20-degree terminal cuts on the k terminals and the w terminals (subtle truncations at the 20° slope on the outermost terminals, not full apex re-cuts like E) plus one single notch detail somewhere in the wordmark to register the wake geometry without drama. This is the deliberate low-risk fallback — restrained, durable, the candidate that reads cleanly at 16px. Match the stroke/cap conventions and overall fidelity of E so G is not visibly less finished. Single unified viewBox. fill/stroke=currentColor, fill-rule="evenodd". No <text>, no <rect>, no wrapper. Provenance header noting G is the restrained terminal-cuts fallback.
  </action>
  <acceptance_criteria>
    - G.svg parses, viewBox present, contains glyph-7-k and glyph-5-w, uses currentColor; check-candidates passes
    - no `<text` and no `<rect>` (grep checks pass)
    - the cuts are present but restrained vs E (manual comparison; record in SUMMARY)
    - one notch detail present; recorded in SUMMARY
    - equal fidelity to E/F (same stroke/cap conventions); recorded in SUMMARY
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/jon/projects/crosswake && node brandbook/tools/check-candidates.mjs && grep -q 'glyph-7-k' brandbook/logo/tournament/candidates/G.svg && grep -q 'glyph-5-w' brandbook/logo/tournament/candidates/G.svg && test "$(grep -c '<text' brandbook/logo/tournament/candidates/G.svg)" -eq 0</automated>
    <human-check>Render G at 256px, 24px, 16px: quiet terminal cuts on k/w plus one notch read cleanly; the treatment is restrained but recognizably carries the wake geometry, and stays legible at 16px.</human-check>
  </verify>
  <done>G.svg applies restrained 20-degree terminal cuts on k/w plus one notch, equal fidelity to E/F, path-only, currentColor.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| committed SVG → consumer (gallery/README/hexdocs) | Path-only text artifacts rendered inline elsewhere |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-103-02 | Elevation of Privilege | typemark SVGs | mitigate | All typemarks are path/stroke-only; check-candidates.mjs rejects `<text>`; no `<script>`, no event handlers, no embedded fonts authored |
| T-103-03 | Information Disclosure | provenance comments | accept | Header comments name only the candidate and technique — no secrets, low-value |
</threat_model>

<verification>
- `node brandbook/tools/check-candidates.mjs` exits 0 over all of candidates/
- E/F/G each embed the hand-cut wordmark glyphs (glyph-5-w / glyph-7-k) and contain no <text>
- No typemark contains a full-viewBox <rect> or wrapper shape
- E, F, G are visibly equal fidelity to each other and to the 103-02 logomarks
</verification>

<success_criteria>
- Three distinct, equal-fidelity integrated typemarks (E Wake-W, F Crossing-SS, G Terminal Cuts) exist as path-only SVGs built on the hand-cut wordmark
- Each carries the mandatory D-06 w/k cuts and integrates wake geometry into the letterforms (not a mark beside plain text)
- LOGO-03 structural rules hold across every file
</success_criteria>

<output>
Create `.planning/phases/103-logo-tournament/103-03-SUMMARY.md` when done. Record: measured wake-hairline slope for E, the boundary x-position for F's break, G's restraint vs E, and the stroke/cap conventions used (for fidelity parity with 103-02).
</output>
