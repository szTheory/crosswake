---
phase: 103-logo-tournament
plan: 04
type: execute
wave: 3
depends_on: ["103-02", "103-03"]
files_modified:
  - brandbook/logo/tournament/index.html
autonomous: false
requirements: [LOGO-02, LOGO-04]
user_setup: []

must_haves:
  truths:
    - "Opening brandbook/logo/tournament/index.html from file:// shows all 7 candidates (A-G) at equal production fidelity with no console errors"
    - "Each candidate card shows 256px renders on Foam 50 / Current 950 / white, a monochrome render, 24px + 16px inline renders, a browser-tab favicon mock, and (for A-D) horizontal + stacked lockups"
    - "Each card carries a 2-3 sentence design rationale AND a stated risk; the page closes with an equal-size lineup grid of all 7"
    - "The gallery includes a maintainer recommendation with reasoning rendered identically to the other candidates, invites franken-picks, and frames selection on durability"
    - "The phase ends at a blocking user checkpoint where the user selects a direction"
  artifacts:
    - path: "brandbook/logo/tournament/index.html"
      provides: "Standalone file:// tournament gallery consuming Phase 102 tokens.css"
      contains: "tokens.css"
  key_links:
    - from: "brandbook/logo/tournament/index.html"
      to: "brandbook/tokens/tokens.css"
      via: "relative stylesheet link + --cw-primitive-* swatch colors"
      pattern: "tokens.css"
    - from: "brandbook/logo/tournament/index.html"
      to: "candidates/*.svg"
      via: "inline SVG paths with fill/stroke=currentColor driven by per-swatch CSS color"
      pattern: "currentColor"
---

<objective>
Build the standalone tournament gallery presenting all 7 candidates across every required context, then run the LOGO-04 blocking user checkpoint that ends the phase.

Purpose: LOGO-02 requires the full presentation matrix per candidate; D-10/D-11/D-12 lock the layout, rationale+risk, lineup grid, maintainer recommendation, franken-pick invitation, and durability framing. LOGO-04/D-14 require a blocking checkpoint where the user picks a direction (or franken-combo) recorded for Phase 104.

Output: brandbook/logo/tournament/index.html (build-free, file://-compatible, tokens.css-styled), and a recorded user selection.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/103-logo-tournament/103-CONTEXT.md
@.planning/phases/103-logo-tournament/103-RESEARCH.md
@.planning/phases/103-logo-tournament/103-VALIDATION.md

<!-- DEPENDENCY: 103-02 produced A-D + 8 lockups; 103-03 produced E-G. All live in candidates/. -->
<!-- This plan inlines those SVG paths into index.html. tokens.css is from Phase 102. -->

<interfaces>
<!-- tokens.css primitives (Phase 102, VERIFIED present) for the three swatch contexts: -->
<!--   --cw-primitive-foam-50: #F7F1E6   --cw-primitive-current-950: #09141A   --cw-primitive-white: #FFFFFF -->
<!--   --cw-primitive-wake-700: #2B756A  --cw-primitive-brass-500: #C98A2E -->
<!-- Swatch CSS drives color (inherited by inline SVG fill/stroke=currentColor): -->
<!--   .swatch-foam  { background: var(--cw-primitive-foam-50);    color: var(--cw-primitive-current-950); } -->
<!--   .swatch-dark  { background: var(--cw-primitive-current-950); color: var(--cw-primitive-foam-50); } -->
<!--   .swatch-white { background: var(--cw-primitive-white);       color: var(--cw-primitive-current-950); } -->

<!-- Gallery patterns (103-RESEARCH.md Pattern 5 color swap, Pattern 6 favicon mock). -->
<!-- Link: <link rel="stylesheet" href="../../tokens/tokens.css"> (relative path from logo/tournament/). -->
<!-- Candidate files: A.svg B.svg C.svg D.svg E.svg F.svg G.svg + A-D *-lockup-horizontal/stacked.svg. -->
<!-- Inline the SVG markup directly (img src= prevents currentColor inheritance — DO NOT use <img>). -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Gallery scaffold — page shell, tokens.css, swatch + favicon-mock CSS</name>
  <files>brandbook/logo/tournament/index.html</files>
  <read_first>
    - 103-RESEARCH.md "Pattern 5: Gallery HTML Color Context Swap" and "Pattern 6: Browser-Tab Favicon Mock" (exact CSS)
    - brandbook/tokens/tokens.css (confirm --cw-primitive-* names and the relative path ../../tokens/tokens.css from logo/tournament/)
    - 103-CONTEXT.md D-10 (standalone, no build, file://, tokens.css, per-candidate render matrix)
    - 103-VALIDATION.md (index.html opens from file:// with no console errors)
  </read_first>
  <action>
    Create brandbook/logo/tournament/index.html as a standalone, build-free page that opens from file://. Link Phase 102 tokens via `<link rel="stylesheet" href="../../tokens/tokens.css">`. Define page CSS using the semantic/primitive tokens: the three swatch classes (.swatch-foam/.swatch-dark/.swatch-white) per Pattern 5 (background + color, so inline-SVG currentColor inherits), a monochrome render context, inline 24px and 16px render sizes (CSS width on inline SVG), and the .tab-bar/.tab-mock/.favicon-area favicon-mock CSS per Pattern 6. Add a page header naming the tournament and the durability framing line ("which still works at 16px, in one color, in five years"). No JavaScript build step; if any JS is used it must be inline and degrade gracefully (the page must render fully with JS disabled). Verify the file loads from file:// in a browser with zero console errors.
  </action>
  <acceptance_criteria>
    - index.html parses as well-formed HTML and references tokens.css: `grep -q 'tokens.css' index.html`
    - the three swatch classes and the favicon-mock classes are defined: `grep -q 'swatch-foam' && swatch-dark && swatch-white && favicon-area`
    - opens from file:// with no console errors (manual; record in SUMMARY)
    - no `<img` used for candidate rendering (currentColor requires inline SVG): `grep -c '<img' index.html` is 0 for candidate renders
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/jon/projects/crosswake && grep -q 'tokens.css' brandbook/logo/tournament/index.html && grep -q 'swatch-foam' brandbook/logo/tournament/index.html && grep -q 'swatch-dark' brandbook/logo/tournament/index.html && grep -q 'favicon-area' brandbook/logo/tournament/index.html</automated>
    <human-check>Open brandbook/logo/tournament/index.html from file:// in a browser: page renders with tokens.css styling and zero console errors.</human-check>
  </verify>
  <done>Standalone file:// gallery shell exists, links tokens.css, and defines the swatch + favicon-mock CSS contexts.</done>
</task>

<task type="auto">
  <name>Task 2: Per-candidate cards (all 7) + lineup grid + maintainer recommendation</name>
  <files>brandbook/logo/tournament/index.html</files>
  <read_first>
    - brandbook/logo/tournament/candidates/*.svg (all 7 + the 8 A-D lockups — inline their path markup)
    - .planning/phases/103-logo-tournament/103-02-SUMMARY.md and 103-03-SUMMARY.md (per-candidate notes, angles, decisions for rationale/risk text)
    - 103-CONTEXT.md D-11 (2-3 sentence rationale + stated risk per card; closing equal-size lineup grid) and D-12 (maintainer recommendation rendered identically, franken-picks invited, durability framing)
    - brandbook/AUDIT.md §8 "Logo do/don't" and "Minimum sizes" (inform risk statements, e.g. 16px legibility)
  </read_first>
  <action>
    For each candidate A–G, build one card. Inline the candidate SVG markup (paths use fill/stroke=currentColor). Each card shows: 256px render on .swatch-foam, .swatch-dark, .swatch-white; a monochrome render; 24px and 16px inline renders; a browser-tab favicon mock (16px scaled, simplified for marks per D's favicon-first construction); and for A–D the horizontal + stacked lockups. Add a 2–3 sentence design rationale AND an explicit stated risk per D-11 (draw on the SUMMARY notes — e.g. "reads as a checkmark below 20px"). 

    After the seven cards, add a closing equal-size lineup grid showing all 7 at identical size for direct comparison (D-11). Add the maintainer recommendation section (D-12): pick a candidate (or franken-combo) from the durability criteria after seeing all 7 rendered, render the recommendation identically to other candidates (no visual favoritism), state the reasoning, and explicitly invite franken-picks ("mark B + type treatment F"). Frame the selection question on durability, not preference.

    All 7 candidates must appear at equal production fidelity (Pitfall 5) — no card visibly less finished than another.
  </action>
  <acceptance_criteria>
    - all 7 candidate ids appear in the page: `grep -o 'candidate-[A-G]' index.html | sort -u` returns 7 (or equivalent per-candidate anchor scheme — record scheme in SUMMARY)
    - each candidate inlines its SVG (currentColor present; no `<img` for candidate renders)
    - 256px, 24px, 16px render blocks present (grep for the size markers); favicon mock present per candidate
    - A-D cards include horizontal + stacked lockups (grep glyph-5-w appears in the lockup blocks)
    - per-candidate rationale AND risk text present (manual review; record count = 7 each in SUMMARY)
    - lineup grid of all 7 present; maintainer recommendation section present with franken-pick invitation and durability framing (grep "franken" and the durability line)
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/jon/projects/crosswake && node brandbook/tools/check-candidates.mjs && grep -qi 'franken' brandbook/logo/tournament/index.html && test "$(grep -o 'currentColor' brandbook/logo/tournament/index.html | wc -l | tr -d ' ')" -gt 0</automated>
    <human-check>Open the gallery from file://: all 7 candidates appear at equal fidelity across foam/dark/white/mono/24px/16px/favicon; A-D show both lockups; every card has rationale + risk; the lineup grid and maintainer recommendation (with franken-pick invite, durability framing) close the page.</human-check>
  </verify>
  <done>The gallery presents all 7 candidates at equal fidelity across every required context with rationale + risk, an equal-size lineup grid, and a maintainer recommendation inviting franken-picks under durability framing.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: LOGO-04 blocking tournament selection checkpoint</name>
  <what-built>
    A standalone tournament gallery at brandbook/logo/tournament/index.html presenting all 7 candidates (A Canonical Wake Mark, B Seam Shift, C Crossing Lanes, D Wake Monogram, E Wake-W typemark, F Crossing-SS typemark, G Terminal Cuts typemark) at equal production fidelity across foam/dark/white backgrounds, monochrome, 256px/24px/16px, favicon mocks, A-D lockups, per-candidate rationale + risk, an equal-size lineup grid, and a maintainer recommendation with reasoning.
  </what-built>
  <how-to-verify>
    1. Open `brandbook/logo/tournament/index.html` from file:// in a browser.
    2. Review all 7 candidates across every context; pay attention to the 16px and one-color renders (durability framing: "which still works at 16px, in one color, in five years").
    3. Read the maintainer recommendation and reasoning.
    4. Select a direction: a single candidate, a franken-combination (e.g. "mark B + type treatment F"), or request adjustments (which re-enter this phase).
  </how-to-verify>
  <resume-signal>
    Reply with your selection (candidate letter, franken-combo, or "adjust: ..."). The executor records the pick verbatim in 103-04-SUMMARY.md and in STATE.md (Accumulated Context > Decisions) for Phase 104 to consume.
  </resume-signal>
  <action>Present the gallery to the user as a CHECKPOINT REACHED: state the gallery file path (brandbook/logo/tournament/index.html), give a one-line description per candidate A–G, state the maintainer recommendation and its reasoning, and explicitly invite a franken-pick. Then block — do NOT auto-advance. This is a human decision gate (D-14, LOGO-04). The user's standing instruction is "lmk when the logos are there" — notify explicitly that the gallery is ready. When the user replies, record the pick verbatim in 103-04-SUMMARY.md and STATE.md.</action>
  <verify>
    <human-check>User has replied with a direction selection (single candidate, franken-combination, or an "adjust:" request that re-enters this phase). The selection is recorded verbatim in 103-04-SUMMARY.md and STATE.md Accumulated Context > Decisions.</human-check>
  </verify>
  <done>User has made an explicit tournament direction selection (or franken-combo); the pick is recorded in the phase summary and STATE.md for Phase 104 to consume.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| file:// gallery → browser | Local static HTML rendered in the user's browser; no server, no network |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-103-02 | Elevation of Privilege | inline SVG in index.html | mitigate | Candidate SVGs are path-only (already validated by check-candidates.mjs); no `<script>` shipped; any inline JS must be optional and degrade gracefully |
| T-103-04 | Information Disclosure | tokens.css link | accept | tokens.css is a committed public brand asset; no secrets; loaded via relative path, no external CDN |
</threat_model>

<verification>
- index.html opens from file:// with no console errors
- All 7 candidates render at equal fidelity across foam/dark/white/mono/256/24/16/favicon; A-D show both lockups
- Each card has rationale + risk; lineup grid + maintainer recommendation present with franken-pick invite and durability framing
- `node brandbook/tools/check-candidates.mjs` still green (no candidate regressed)
- User selection recorded in 103-04-SUMMARY.md and STATE.md
</verification>

<success_criteria>
- LOGO-02: full presentation matrix per candidate in a build-free file:// gallery styled by tokens.css
- LOGO-04: blocking checkpoint completed with an explicit user direction selection (or franken-combo) recorded for Phase 104
</success_criteria>

<output>
Create `.planning/phases/103-logo-tournament/103-04-SUMMARY.md` when done. Record the user's verbatim selection (candidate / franken-combo / adjustment), and write the same selection into STATE.md Accumulated Context > Decisions so Phase 104 consumes it.
</output>
