---
phase: 103-logo-tournament
plan: 02
type: execute
wave: 2
depends_on: ["103-01"]
files_modified:
  - brandbook/logo/tournament/candidates/A.svg
  - brandbook/logo/tournament/candidates/B.svg
  - brandbook/logo/tournament/candidates/C.svg
  - brandbook/logo/tournament/candidates/D.svg
  - brandbook/logo/tournament/candidates/A-lockup-horizontal.svg
  - brandbook/logo/tournament/candidates/A-lockup-stacked.svg
  - brandbook/logo/tournament/candidates/B-lockup-horizontal.svg
  - brandbook/logo/tournament/candidates/B-lockup-stacked.svg
  - brandbook/logo/tournament/candidates/C-lockup-horizontal.svg
  - brandbook/logo/tournament/candidates/C-lockup-stacked.svg
  - brandbook/logo/tournament/candidates/D-lockup-horizontal.svg
  - brandbook/logo/tournament/candidates/D-lockup-stacked.svg
autonomous: true
requirements: [LOGO-01, LOGO-03]
user_setup: []

must_haves:
  truths:
    - "Four distinct logomark concepts (A Canonical Wake Mark, B Seam Shift, C Crossing Lanes, D Wake Monogram) exist as path-only stroke SVGs on a 64-unit grid in brandbook/logo/tournament/candidates/ (D-04, D-13: committed candidates, currentColor canonical colorway)"
    - "Candidate A executes the AUDIT §8 spec straight: route line at 20 degrees, 3 lines total, 1.5x-stroke notch at the crossing, round caps"
    - "Each logomark A-D has a close-set horizontal lockup AND a stacked lockup pairing the mark with the hand-cut wordmark"
    - "No mark or lockup contains a <text> element, a full-viewBox <rect>, or any bounding shape"
  artifacts:
    - path: "brandbook/logo/tournament/candidates/A.svg"
      provides: "Canonical Wake Mark — 3-line 20-degree route+wake on 64-grid"
      contains: "stroke=\"currentColor\""
    - path: "brandbook/logo/tournament/candidates/D.svg"
      provides: "Minimal Wake Monogram (favicon-first, C or cw from wake geometry)"
    - path: "brandbook/logo/tournament/candidates/A-lockup-horizontal.svg"
      provides: "Mark A + hand-cut wordmark, close-set, no subtitle"
      contains: "glyph-5-w"
  key_links:
    - from: "brandbook/logo/tournament/candidates/A-lockup-horizontal.svg"
      to: "wordmark-custom.svg"
      via: "wordmark glyph paths embedded beside the mark with a one-stroke-width gap"
      pattern: "glyph-5-w"
    - from: "brandbook/logo/tournament/candidates/A.svg"
      to: "AUDIT §8 geometry"
      via: "20-degree route line with 1.5x-stroke notch at center"
      pattern: "stroke-linecap=\"round\""
---

<objective>
Author the four logomark candidates (A–D) at equal production fidelity, each with its close-set horizontal and stacked lockup built on the Wave 1 hand-cut wordmark.

Purpose: LOGO-01 requires 4 logomark concepts (D-01) as path-only SVGs; D-02 requires each shown as horizontal AND stacked lockup with the wordmark; LOGO-03 forbids container backgrounds and demands a one-stroke-width logotype gap with no subtitle (D-05). These four marks plus the three typemarks (103-03) form the full tournament field.

Output: A.svg, B.svg, C.svg, D.svg and eight lockup SVGs (horizontal + stacked per mark), all stroke-based on a 64-unit grid, fill/stroke=currentColor.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/103-logo-tournament/103-CONTEXT.md
@.planning/phases/103-logo-tournament/103-RESEARCH.md
@.planning/phases/103-logo-tournament/103-VALIDATION.md

<!-- DEPENDENCY: 103-01 produced wordmark-custom.svg (the hand-cut wordmark) and check-candidates.mjs. -->
<!-- This plan READS wordmark-custom.svg (does not modify it) and is validated by check-candidates.mjs. -->

<interfaces>
<!-- Canonical geometry (103-RESEARCH.md "Candidate Geometry Math", 64-unit grid, 20-degree) for Candidate A: -->
<!--   stroke width: 6.667u   notch (1.5x stroke): 10.0u   wake spacing (2x stroke): 13.333u -->
<!--   route line: (0, 37.82) -> (64, 26.18), slope -tan(20) = -0.364 -->
<!--   notch break at crossing (32,32): start (27.30, 33.71), end (36.70, 30.29) -->
<!--   wake line 1 center (36.56, 44.53), wake line 2 center (41.12, 57.06), same slope, ~30-40u long -->
<!--   16px simplification: drop wake line 2, set stroke-width ~8 on 64-grid (=2px at 16px render) -->

<!-- Candidate SVG shell (103-RESEARCH.md "Candidate SVG Shell"): -->
<!--   <svg xmlns viewBox="0 0 64 64"> with <g fill="none" stroke="currentColor" stroke-width="6.667" stroke-linecap="round"> -->
<!--   route line split into 2 segments around the notch gap; 2 trailing wake lines. -->

<!-- wordmark-custom.svg (from 103-01) provides per-glyph paths id="glyph-0-C".."glyph-8-e" inside -->
<!--   <g id="wordmark-base" fill="currentColor" fill-rule="evenodd">, viewBox "vbX 0 vbW 72". -->
<!--   Embed these glyph paths into lockups; never re-typeset, never use <text>. -->

<!-- HARD CONSTRAINTS (D-04/D-05, LOGO-03): no <text>, no full-viewBox <rect>, no circle/square/blob wrapper, -->
<!--   lockup gap approx one stroke width, NO subtitle/slogan paths in main lockups, fill/stroke=currentColor. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Candidate A (Canonical Wake Mark) + B (Seam Shift) marks</name>
  <files>brandbook/logo/tournament/candidates/A.svg, brandbook/logo/tournament/candidates/B.svg</files>
  <read_first>
    - 103-RESEARCH.md "Candidate Geometry Math" (the precomputed A coordinates) and "Candidate SVG Shell"
    - brandbook/AUDIT.md §8 "Wake Mark geometry (mandatory tournament constraints)" (20°, 3 lines, 2.5px@24, 1.5x notch, round caps, 2-stroke at ≤16px)
    - brandbook/AUDIT.md §8 "Logo do/don't" (no boats/anchors/waves/X-without-wake; geometric strokes; diagonal crossing preserved)
    - prompts/crosswake-brand-book.md §10 (original Wake Mark anti-patterns: must not resemble React atom, Phoenix flame, Hotwire wire, compass rose)
    - 103-CONTEXT.md D-01 A and B definitions
  </read_first>
  <action>
    A.svg (Canonical Wake Mark, D-01/D-03): execute the AUDIT §8 spec straight on a viewBox="0 0 64 64". Use the precomputed coordinates: route line from (0,37.82) to (64,26.18) split into two <line> segments around the notch gap — segment 1 (0,37.82)->(27.30,33.71), segment 2 (36.70,30.29)->(64,26.18) — giving a 1.5x-stroke (10u) break at the crossing. Add exactly two trailing wake lines parallel to the route (wake centers 36.56,44.53 and 41.12,57.06), each ~30-40u long. Single <g fill="none" stroke="currentColor" stroke-width="6.667" stroke-linecap="round">. Three lines total (route counts as one despite the notch split). No notch fill, no background.

    B.svg (Seam Shift, D-01): instead of a clean break, OFFSET the route line at a perpendicular seam — the route visibly steps across the boundary and continues (boundary changes the route but does not stop it). Stay within D-03 constraints (20° ±4°, round caps, stroke ~6.667u, strokes-as-strokes). Keep it visibly distinct from A while reading as the same family.

    Both: fill/stroke=currentColor only, path/stroke-only, no <text>, no <rect>, no wrapper shape. Add a provenance header comment naming the candidate and that geometry is hand-authored on the 64-grid.
  </action>
  <acceptance_criteria>
    - A.svg and B.svg parse and contain a viewBox; `node brandbook/tools/check-candidates.mjs` exits 0 with both present
    - A.svg uses stroke-linecap="round" and stroke="currentColor": both grep-present
    - A.svg has exactly 3 logical wake lines: route (2 segments around notch) + 2 wake lines = 4 <line>/<path> stroke elements; document the count in the SUMMARY
    - neither file contains `<text` or `<rect`: `grep -L '<text' A.svg B.svg` returns both; `grep -c '<rect' A.svg B.svg` returns 0 each
    - the route line angle measures within 16–24 degrees (compute from segment endpoints; record in SUMMARY)
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/jon/projects/crosswake && node brandbook/tools/check-candidates.mjs && grep -q 'stroke-linecap="round"' brandbook/logo/tournament/candidates/A.svg && grep -q 'stroke="currentColor"' brandbook/logo/tournament/candidates/A.svg && test "$(grep -c '<rect' brandbook/logo/tournament/candidates/A.svg brandbook/logo/tournament/candidates/B.svg | grep -c ':0')" -eq 2</automated>
    <human-check>Render A.svg and B.svg at 256px, 24px, 16px: A reads as a clean 20-degree wake with notch; B reads as a route stepping across a seam; neither resembles a checkmark, X, atom, or flame.</human-check>
  </verify>
  <done>A.svg executes the canonical §8 Wake Mark spec; B.svg is a distinct seam-shift variant; both path-only, currentColor, no container shapes.</done>
</task>

<task type="auto">
  <name>Task 2: Candidate C (Crossing Lanes) + D (Wake Monogram, favicon-first) marks</name>
  <files>brandbook/logo/tournament/candidates/C.svg, brandbook/logo/tournament/candidates/D.svg</files>
  <read_first>
    - 103-CONTEXT.md D-01 C and D definitions; D-03 constraints; Claude's Discretion (D monogram "C" vs "cw")
    - 103-RESEARCH.md "Open Questions" #2 (author "C" first, evaluate at 16px in favicon mock, pivot to "cw" only if "C" reads generic)
    - brandbook/AUDIT.md §8 "Minimum sizes" (favicon 16px simplified 2-stroke) and "Logo do/don't"
    - brandbook/logo/tournament/candidates/A.svg (match stroke weight / cap conventions for equal fidelity)
  </read_first>
  <action>
    C.svg (Crossing Lanes, D-01): two runtime lanes (parallel strokes) with the route line crossing between them through a gate notch. Lanes read as the runtime-tier metaphor; the route crosses through a 1.5x-stroke gate gap. 20° ±4°, round caps, stroke ~6.667u on 64-grid, currentColor.

    D.svg (Minimal Wake Monogram, D-01, favicon-first): construct a "C" (or "cw") from wake geometry with the counter broken by the route line. Author "C" first per the research recommendation; render at 16px in isolation; only pivot to "cw" if the "C" reads generic. Because this is favicon-first, ensure it survives the ≤16px 2-stroke simplification (D-03) — keep the construction to 2 dominant strokes. currentColor, no fill background, no wrapper shape.

    Both: equal stroke weight / cap style to A and B for equal production fidelity (Pitfall 5 — no candidate may be visibly less finished). No <text>, no <rect>, no bounding shape. Provenance header per file; for D record whether "C" or "cw" was chosen and why.
  </action>
  <acceptance_criteria>
    - C.svg and D.svg parse with a viewBox; `node brandbook/tools/check-candidates.mjs` exits 0
    - both use stroke="currentColor"; neither contains `<text` or `<rect` (grep checks pass)
    - D.svg reduces to <= 2 dominant strokes (favicon-survivable); recorded in SUMMARY with the C-vs-cw decision and 16px legibility note
    - C/D stroke-width and stroke-linecap match A/B values (equal fidelity); recorded in SUMMARY
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/jon/projects/crosswake && node brandbook/tools/check-candidates.mjs && grep -q 'stroke="currentColor"' brandbook/logo/tournament/candidates/C.svg && grep -q 'stroke="currentColor"' brandbook/logo/tournament/candidates/D.svg && test "$(grep -c '<text' brandbook/logo/tournament/candidates/C.svg brandbook/logo/tournament/candidates/D.svg | grep -c ':0')" -eq 2</automated>
    <human-check>Render C at 256px (lanes + crossing route legible) and D at 16px in a favicon context (monogram still reads as C/cw, not a blob).</human-check>
  </verify>
  <done>C.svg shows crossing lanes through a gate notch; D.svg is a favicon-survivable wake monogram; both equal fidelity to A/B, path-only, currentColor.</done>
</task>

<task type="auto">
  <name>Task 3: Eight lockups (horizontal + stacked) for A–D using the hand-cut wordmark</name>
  <files>brandbook/logo/tournament/candidates/A-lockup-horizontal.svg, brandbook/logo/tournament/candidates/A-lockup-stacked.svg, brandbook/logo/tournament/candidates/B-lockup-horizontal.svg, brandbook/logo/tournament/candidates/B-lockup-stacked.svg, brandbook/logo/tournament/candidates/C-lockup-horizontal.svg, brandbook/logo/tournament/candidates/C-lockup-stacked.svg, brandbook/logo/tournament/candidates/D-lockup-horizontal.svg, brandbook/logo/tournament/candidates/D-lockup-stacked.svg</files>
  <read_first>
    - brandbook/logo/tournament/candidates/wordmark-custom.svg (the hand-cut wordmark — copy its glyph paths and viewBox; do NOT modify it)
    - brandbook/AUDIT.md §8 "Phase 103 tournament brief" table (horizontal lockup min 128px wide; stacked min 64px wide; mark + wordmark on one line / mark above wordmark)
    - brandbook/AUDIT.md §8 "Clearspace" (horizontal lockup clearspace = wordmark x-height)
    - 103-CONTEXT.md D-02 (each logomark shown horizontal AND stacked) and D-05 (gap ≈ one stroke width, NO subtitle on main lockups)
    - 103-VALIDATION.md "Manual-Only Verifications" (optical centering / x-height alignment)
  </read_first>
  <action>
    For each of A, B, C, D produce two lockup SVGs that compose the mark paths with the wordmark-custom glyph paths into a single SVG (path-only — embed the glyph <path> elements directly, never a <text> element and never re-typeset). 

    Horizontal lockups: mark on the left, wordmark on one line to the right, separated by a gap of approximately ONE stroke width (D-05). Optically center the mark within the wordmark x-height band (not geometric center) per the manual verification note. Target a layout legible at the 128px-wide minimum.

    Stacked lockups: mark above, wordmark below, same one-stroke-width gap, for social/README hero use; target the 64px-wide minimum.

    NO subtitle/slogan paths in any main lockup (D-05 — the with-subtitle variant is Phase 104). All paths fill/stroke=currentColor. Compose a unified viewBox per lockup so mark and wordmark scale together. Provenance header per file noting it consumes wordmark-custom.svg.
  </action>
  <acceptance_criteria>
    - all 8 lockup files exist, parse, and declare a viewBox; `node brandbook/tools/check-candidates.mjs` exits 0 across the full candidates dir
    - each lockup embeds the wordmark glyphs: `grep -l 'glyph-5-w' *-lockup-*.svg` returns all 8 (proves the hand-cut wordmark is used, not stock type and not <text>)
    - no lockup contains `<text`: `grep -L '<text' *-lockup-*.svg` returns all 8
    - no lockup contains a full-viewBox `<rect>` or wrapper shape (check-candidates passes)
    - no subtitle paths present (manual review; record confirmation in SUMMARY)
    - gap between mark and wordmark ≈ one stroke width (manual review at 256px; record measured gap in SUMMARY)
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/jon/projects/crosswake && node brandbook/tools/check-candidates.mjs && test "$(grep -l 'glyph-5-w' brandbook/logo/tournament/candidates/*-lockup-*.svg | wc -l | tr -d ' ')" -eq 8 && test "$(grep -L '<text' brandbook/logo/tournament/candidates/*-lockup-*.svg | wc -l | tr -d ' ')" -eq 8</automated>
    <human-check>Render all 8 lockups at 256px: mark optically centered in the wordmark x-height band, gap ≈ one stroke width, w/k cuts visible in the wordmark, no subtitle, no container box.</human-check>
  </verify>
  <done>Eight lockups (horizontal + stacked for A–D) compose each mark with the hand-cut wordmark at a one-stroke-width gap, no subtitle, no container shape, path-only.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| committed SVG → consumer (gallery/README/hexdocs) | Path-only text artifacts rendered inline elsewhere |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-103-02 | Elevation of Privilege | candidate/lockup SVGs | mitigate | All marks and lockups are path/stroke-only; check-candidates.mjs rejects `<text>`; no `<script>`, no event handlers, no embedded fonts authored |
| T-103-03 | Information Disclosure | provenance comments | accept | Header comments name only the candidate, grid, and angle — no secrets, low-value |
</threat_model>

<verification>
- `node brandbook/tools/check-candidates.mjs` exits 0 over all of candidates/
- A.svg matches the §8 canonical spec (3 lines, 20°, 1.5x notch, round caps)
- All 8 lockups embed glyph-5-w (the hand-cut wordmark) and contain no <text>
- No mark or lockup contains a full-viewBox <rect> or wrapper shape
</verification>

<success_criteria>
- Four distinct, equal-fidelity logomark concepts (A canonical, B seam-shift, C crossing-lanes, D wake-monogram) exist as path-only stroke SVGs on the 64-grid
- Each has a close-set horizontal AND stacked lockup using the hand-cut wordmark, one-stroke-width gap, no subtitle
- LOGO-03 structural rules hold across every file (no container backgrounds, no <text>)
</success_criteria>

<output>
Create `.planning/phases/103-logo-tournament/103-02-SUMMARY.md` when done. Record: measured route angle for A, the D monogram C-vs-cw decision + 16px note, stroke-width/cap values used (for fidelity parity with 103-03), and measured lockup gaps.
</output>
