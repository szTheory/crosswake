---
phase: 103-logo-tournament
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - brandbook/tools/package.json
  - brandbook/tools/package-lock.json
  - brandbook/tools/fetch-fonts.sh
  - brandbook/tools/gen-wordmark.mjs
  - brandbook/tools/gen-wordmark.test.mjs
  - brandbook/tools/check-candidates.mjs
  - brandbook/logo/tournament/candidates/wordmark-base.svg
  - brandbook/logo/tournament/candidates/wordmark-custom.svg
  - brandbook/logo/tournament/candidates/.gitkeep
autonomous: true
requirements: [LOGO-01, LOGO-03]
user_setup: []

must_haves:
  truths:
    - "Running gen-wordmark.mjs produces a path-only Crosswake wordmark SVG with one <path> per glyph (9 paths)"
    - "The generated wordmark renders upright (not y-flipped) in SemiBold weight with hollow counters"
    - "A committed wordmark-custom.svg carries the mandatory 20-degree w and k wake-angle cuts (not typesettable in unmodified Space Grotesk)"
    - "check-candidates.mjs flags any candidate SVG containing <text> or a full-viewBox <rect> (enforces D-04: path-only SVG, no rectangular backgrounds, no embedded fonts)"
  artifacts:
    - path: "brandbook/tools/gen-wordmark.mjs"
      provides: "opentype.js wordmark path generator with flipY:false + variation.set workarounds"
      contains: "getPaths"
    - path: "brandbook/tools/fetch-fonts.sh"
      provides: "Pinned-commit Space Grotesk variable TTF downloader"
      contains: "877f8918ee661764418e085766dc0b073260a3ef"
    - path: "brandbook/tools/check-candidates.mjs"
      provides: "Structural SVG validator (no <text>, no full-bleed <rect>, viewBox present)"
    - path: "brandbook/logo/tournament/candidates/wordmark-custom.svg"
      provides: "Hand-cut Crosswake wordmark consumed by all lockups and typemarks"
      contains: "glyph-5-w"
    - path: "brandbook/tools/package.json"
      provides: "Isolated opentype.js 2.0.0 dependency declaration"
      contains: "opentype.js"
  key_links:
    - from: "brandbook/tools/gen-wordmark.mjs"
      to: "opentype.js"
      via: "font.variation.set + font.getPaths + toPathData"
      pattern: "variation\\.set"
    - from: "brandbook/logo/tournament/candidates/wordmark-custom.svg"
      to: "wordmark-base.svg"
      via: "surgical w/k node edits committed as a separate provenance commit"
      pattern: "currentColor"
---

<objective>
Build the wordmark-generation toolchain and produce the single canonical hand-cut wordmark that every downstream candidate consumes.

Purpose: D-07/D-08/D-09 pull LOGO-07's tooling forward because the tournament cannot show integrated typemarks or lockups without a real Space Grotesk SemiBold wordmark carrying the mandatory D-06 w/k wake cuts. Making the cut wordmark a Wave 1 deliverable means both Wave 2 candidate-authoring plans consume one source of truth (no duplicated cut effort, guaranteed consistency).

Output: brandbook/tools/{package.json, package-lock.json, fetch-fonts.sh, gen-wordmark.mjs, gen-wordmark.test.mjs, check-candidates.mjs}, the generated wordmark-base.svg (provenance), and the hand-edited wordmark-custom.svg (the artifact Wave 2 uses).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/103-logo-tournament/103-CONTEXT.md
@.planning/phases/103-logo-tournament/103-RESEARCH.md
@.planning/phases/103-logo-tournament/103-VALIDATION.md

<interfaces>
<!-- Phase 102 tooling conventions (brandbook/tools/) the executor must match -->
<!-- Existing files: compile-tokens.js, compile-tokens.test.mjs, contrast.mjs, contrast.test.mjs -->
<!-- Convention: zero-dep-first Node ESM, const + named functions, tests alongside as *.test.mjs run via `node --test` -->
<!-- contrast.mjs already exists and is importable — do NOT rebuild WCAG math -->

<!-- .gitignore (already in place from Phase 102): -->
<!--   /brandbook/tools/node_modules/  -->
<!--   /brandbook/tools/fonts/         -->
<!-- node_modules and fonts/ are gitignored; package-lock.json and generated SVGs ARE committed. -->

<!-- opentype.js 2.0.0 API contract (from 103-RESEARCH.md Patterns 1-2, VERIFIED): -->
<!--   const font = await opentype.load(ttfPath)                                   -->
<!--   font.variation.set({ wght: 600 })   // MUST precede getPaths — default is 400 -->
<!--   const baseline = font.ascender * (fontSize / font.unitsPerEm)  // NOT 0      -->
<!--   const paths = font.getPaths(text, 0, baseline, fontSize, { kerning: true }) // plural -> per-glyph array -->
<!--   path.toPathData({ flipY: false, decimalPlaces: 2 })  // flipY:false avoids v2.0.0 double-flip -->
<!--   wrap all paths in <g fill-rule="evenodd"> so o/e/a/s/C counters render hollow -->

<!-- Token primitives for any future color reference (gallery owns colors, not this plan): -->
<!--   --cw-primitive-current-950: #09141A  (one-color canonical mark color) -->
<!--   --cw-primitive-foam-50: #F7F1E6 -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Tooling scaffold — package.json, lockfile, fetch-fonts.sh</name>
  <files>brandbook/tools/package.json, brandbook/tools/package-lock.json, brandbook/tools/fetch-fonts.sh, brandbook/logo/tournament/candidates/.gitkeep</files>
  <read_first>
    - 103-RESEARCH.md "Pattern 3: fetch-fonts.sh with Pinned Commit" (exact URL, pinned SHA, size echo)
    - 103-RESEARCH.md "Package Legitimacy Audit" (opentype.js 2.0.0 [OK], no postinstall)
    - brandbook/tools/compile-tokens.js (match existing Phase 102 file conventions: ESM, const, named functions)
    - .gitignore lines 34-36 (confirm node_modules/ and fonts/ already excluded)
  </read_first>
  <action>
    Create brandbook/tools/package.json declaring the single dependency opentype.js at "2.0.0" (exact, not caret — provenance). Set "type":"module", "private":true, and a "name" consistent with Phase 102 tooling. Run `cd brandbook/tools && npm install opentype.js@2.0.0` to generate package-lock.json (commit the lockfile; node_modules stays gitignored). Verify no postinstall script ran (npm install output shows zero lifecycle scripts for opentype.js per the legitimacy audit).

    Create brandbook/tools/fetch-fonts.sh exactly per Pattern 3: shebang `#!/usr/bin/env bash`, `set -euo pipefail`, COMMIT pinned to 877f8918ee661764418e085766dc0b073260a3ef, download the variable file `ofl/spacegrotesk/SpaceGrotesk%5Bwght%5D.ttf` from `raw.githubusercontent.com/google/fonts/${COMMIT}/...` via `curl -fL` into `$(dirname "$0")/fonts/` (gitignored), idempotent (skip if already present), and echo the downloaded byte count. After downloading, assert the file size is 136676 bytes (the verified pinned size) and exit non-zero with a clear message if it differs — this is the integrity check for the download (threat T-103-SC mitigation).

    Make fetch-fonts.sh executable (chmod +x) and run it once to populate fonts/ for downstream tasks.

    Create an empty brandbook/logo/tournament/candidates/.gitkeep so the candidates directory exists and the check script has a target before any SVGs land.
  </action>
  <acceptance_criteria>
    - `test -f brandbook/tools/package.json && grep -q 'opentype.js' brandbook/tools/package.json` exits 0
    - `test -f brandbook/tools/package-lock.json` exits 0 (lockfile committed)
    - `grep -q '877f8918ee661764418e085766dc0b073260a3ef' brandbook/tools/fetch-fonts.sh` exits 0
    - `bash brandbook/tools/fetch-fonts.sh && test -f 'brandbook/tools/fonts/SpaceGrotesk[wght].ttf'` exits 0
    - downloaded TTF is exactly 136676 bytes: `test "$(wc -c < 'brandbook/tools/fonts/SpaceGrotesk[wght].ttf')" -eq 136676`
    - `git status --porcelain brandbook/tools/node_modules brandbook/tools/fonts` returns empty (gitignored)
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/jon/projects/crosswake && grep -q '877f8918ee661764418e085766dc0b073260a3ef' brandbook/tools/fetch-fonts.sh && test -f brandbook/tools/package-lock.json && bash brandbook/tools/fetch-fonts.sh && test "$(wc -c < 'brandbook/tools/fonts/SpaceGrotesk[wght].ttf')" -eq 136676</automated>
  </verify>
  <done>opentype.js 2.0.0 installed with committed lockfile; fetch-fonts.sh downloads the exact pinned 136676-byte TTF into gitignored fonts/; candidates/ directory exists.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: gen-wordmark.mjs + test, generate and commit wordmark-base.svg</name>
  <files>brandbook/tools/gen-wordmark.mjs, brandbook/tools/gen-wordmark.test.mjs, brandbook/logo/tournament/candidates/wordmark-base.svg</files>
  <read_first>
    - 103-RESEARCH.md "Pattern 1", "Pattern 2", "Code Examples: gen-wordmark.mjs Complete Pattern"
    - 103-RESEARCH.md "Common Pitfalls" 1-4 (double y-flip, missing wght, fill-rule, currentColor)
    - 103-RESEARCH.md "Validation Architecture > Phase Requirements to Test Map" (path count = 9, baseline positive)
    - brandbook/tools/compile-tokens.test.mjs (match Phase 102 node --test structure)
  </read_first>
  <behavior>
    - Test 1: generated SVG contains exactly 9 <path elements (one per glyph in "Crosswake": C r o s s w a k e)
    - Test 2: each glyph path id follows id="glyph-{i}-{char}" with the w at index 5 and k at index 7 (id="glyph-5-w", id="glyph-7-k" both present)
    - Test 3: the wrapping group carries fill-rule="evenodd" (counters render hollow)
    - Test 4: baseline used is positive — assert the generator's computed baseline equals font.ascender * (fontSize/unitsPerEm) and is > 0 (regression guard against the y=0 / y-flip pitfalls)
    - Test 5: no <text element appears in the output SVG (path-only)
  </behavior>
  <action>
    Implement brandbook/tools/gen-wordmark.mjs per the research Code Example: load the gitignored TTF via `import.meta.url`-relative path, call `font.variation.set({ wght: 600 })` BEFORE any getPaths, compute `baseline = font.ascender * (fontSize / font.unitsPerEm)` (fontSize=72), call `font.getPaths('Crosswake', 0, baseline, fontSize, { kerning: true })` (plural — per-glyph array), emit each glyph as `<path id="glyph-${i}-${char}" d="...">` using `toPathData({ flipY: false, decimalPlaces: 2 })`, wrap all paths in `<g id="wordmark-base" fill="currentColor" fill-rule="evenodd">`, compute viewBox from the merged-path bounding box (`vbX 0 vbW fontSize`), and write to brandbook/logo/tournament/candidates/wordmark-base.svg with a GENERATED provenance header comment naming the pinned commit 877f891 and wght=600. Print to stdout the glyph index/name table and a reminder that glyph-5-w and glyph-7-k need hand cuts (this stdout is the inspection the next task depends on).

    Write brandbook/tools/gen-wordmark.test.mjs covering the five behaviors above using `node --test` (zero-dep, matching Phase 102). The test runs gen-wordmark (or imports a generator function) against the real downloaded TTF and asserts on the produced SVG string.

    Run `node brandbook/tools/gen-wordmark.mjs`, then open wordmark-base.svg in a browser to confirm the wordmark renders UPRIGHT (not vertically mirrored), in SemiBold weight, with hollow counters in o/e/a/s/C. Commit wordmark-base.svg as a standalone provenance commit (message: generated base, before cuts) so the w/k hand-edit lands as a separate, diffable commit.
  </action>
  <acceptance_criteria>
    - `node --test brandbook/tools/gen-wordmark.test.mjs` passes (all 5 behaviors green)
    - `grep -c '<path' brandbook/logo/tournament/candidates/wordmark-base.svg` returns 9
    - `grep -q 'id="glyph-5-w"' ... && grep -q 'id="glyph-7-k"' ...` both exit 0
    - `grep -q 'fill-rule="evenodd"' brandbook/logo/tournament/candidates/wordmark-base.svg` exits 0
    - `grep -q '<text' brandbook/logo/tournament/candidates/wordmark-base.svg` exits 1 (no text element)
    - `grep -q 'flipY: false' brandbook/tools/gen-wordmark.mjs && grep -q "variation.set" brandbook/tools/gen-wordmark.mjs` both exit 0
    - wordmark-base.svg committed as its own commit (visible in `git log --oneline -1 -- brandbook/logo/tournament/candidates/wordmark-base.svg`)
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/jon/projects/crosswake && node --test brandbook/tools/gen-wordmark.test.mjs && test "$(grep -c '<path' brandbook/logo/tournament/candidates/wordmark-base.svg)" -eq 9 && grep -q 'id="glyph-5-w"' brandbook/logo/tournament/candidates/wordmark-base.svg && grep -q 'id="glyph-7-k"' brandbook/logo/tournament/candidates/wordmark-base.svg</automated>
  </verify>
  <done>gen-wordmark.mjs emits a 9-path, upright, SemiBold, evenodd-countered wordmark SVG; tests green; wordmark-base.svg committed separately as provenance with glyph-5-w and glyph-7-k inspected.</done>
</task>

<task type="auto">
  <name>Task 3: Apply D-06 w/k cuts (wordmark-custom.svg) + check-candidates.mjs validator</name>
  <files>brandbook/logo/tournament/candidates/wordmark-custom.svg, brandbook/tools/check-candidates.mjs</files>
  <read_first>
    - brandbook/logo/tournament/candidates/wordmark-base.svg (the generated source — READ the actual glyph-5-w and glyph-7-k path node data before editing; node topology is only knowable from real output)
    - 103-RESEARCH.md "Pattern 4: w/k Surgical Path Node Edit" (apex-cut and arm/leg-notch technique, dx_per_unit_y = 2.747)
    - brandbook/AUDIT.md §8 "D-11 MANDATORY RIDER" (w cut echoes 20°, k arm/leg notch at same slope; uncut candidates disqualified)
    - 103-RESEARCH.md "Candidate Geometry Math" (20° slope, notch = 1.5x stroke)
    - 103-VALIDATION.md "Test Infrastructure" (check-candidates.mjs is the quick-run command: parses, no <text>, no full-bleed <rect>, viewBox present)
  </read_first>
  <action>
    Copy wordmark-base.svg to wordmark-custom.svg. On glyph-5-w: locate the two inner apex node sequences and replace the 2-4 nodes at each apex with a straight-line truncation at the 20-degree wake slope (dx per unit y = 1/tan(20°) = 2.747), notch half-depth = 1.5x the local stroke width per Pattern 4. On glyph-7-k: locate the arm/leg inner vertex and replace that vertex node with two offset nodes forming an angular notch at the same 20-degree slope. These are surgical node replacements on the generated path data — NEVER hand-drawn full letterforms (D-09). Keep fill="currentColor" and fill-rule="evenodd". Update the provenance header comment to record "hand-cut w(apex) + k(arm/leg) at 20-deg per D-06". Open in a browser at 400% zoom and confirm the cuts are visible at 100% zoom and the wordmark is no longer typesettable in stock Space Grotesk.

    Commit wordmark-custom.svg as a SEPARATE commit from wordmark-base.svg (message: surgical w/k wake cuts per D-06) so the diff isolates exactly the cut edits for provenance.

    Implement brandbook/tools/check-candidates.mjs (zero-dep ESM, `node` runnable) that globs brandbook/logo/tournament/candidates/*.svg and for each file asserts: (a) it parses as well-formed XML; (b) it contains no `<text` element; (c) it contains no `<rect` whose width/height spans the full viewBox (full-bleed background rect) and no bounding `<circle`/wrapper rect; (d) it declares a viewBox. Exit non-zero listing every violating file. This is the LOGO-03 structural gate and the validation quick-run command. Run it against the current candidates dir (wordmark-base.svg + wordmark-custom.svg) and confirm it exits 0.
  </action>
  <acceptance_criteria>
    - wordmark-custom.svg differs from wordmark-base.svg ONLY within glyph-5-w and glyph-7-k path data plus the header comment (`git diff --no-index` scoped review confirms cut-only changes)
    - `grep -q 'currentColor' brandbook/logo/tournament/candidates/wordmark-custom.svg` exits 0
    - wordmark-base.svg and wordmark-custom.svg are two distinct commits (git log shows base commit precedes cut commit)
    - `node brandbook/tools/check-candidates.mjs` exits 0 on the current candidates dir
    - check-candidates.mjs detects violations: a temp SVG containing `<text>` or a full-viewBox `<rect>` causes non-zero exit (self-test the validator before finalizing)
    - cuts visible at 100% zoom in browser (manual — record in SUMMARY that the wordmark is not typesettable in unmodified Space Grotesk)
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/jon/projects/crosswake && test -f brandbook/logo/tournament/candidates/wordmark-custom.svg && grep -q 'currentColor' brandbook/logo/tournament/candidates/wordmark-custom.svg && node brandbook/tools/check-candidates.mjs</automated>
    <human-check>Open wordmark-custom.svg in a browser at 400% zoom: confirm the w apex carries a visible 20-degree cut and the k arm/leg carries an angular notch; the wordmark must read as not-stock Space Grotesk at 100% zoom.</human-check>
  </verify>
  <done>wordmark-custom.svg carries the D-06 w/k cuts committed as a separate provenance commit; check-candidates.mjs validates the candidates dir and rejects <text>/full-bleed-<rect> SVGs.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GitHub raw URL → local fonts/ | Untrusted binary download crosses into the workspace (fetch-fonts.sh) |
| npm registry → node_modules/ | Third-party package install (opentype.js) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-103-01 | Tampering | fetch-fonts.sh download | mitigate | Pin commit SHA 877f8918ee661764418e085766dc0b073260a3ef in the URL; assert downloaded file is exactly 136676 bytes and exit non-zero on mismatch |
| T-103-SC | Tampering | npm install opentype.js | mitigate | opentype.js 2.0.0 has no postinstall script (legitimacy audit [OK], 13yr/1M-dl); commit package-lock.json for reproducibility; node_modules gitignored, never shipped in the library |
| T-103-02 | Elevation of Privilege | generated/committed SVG | mitigate | All committed SVGs are path-only text; check-candidates.mjs rejects any `<text>`; no `<script>` or event handlers authored; no runtime code ships in the Elixir library |
</threat_model>

<verification>
- `node --test brandbook/tools/gen-wordmark.test.mjs` green
- `node brandbook/tools/check-candidates.mjs` exits 0
- `bash brandbook/tools/fetch-fonts.sh` downloads the exact 136676-byte pinned TTF
- `git log --oneline` shows wordmark-base.svg and wordmark-custom.svg as two separate provenance commits
- `git status --porcelain brandbook/tools/{node_modules,fonts}` empty (gitignored)
</verification>

<success_criteria>
- opentype.js 2.0.0 isolated under brandbook/tools/ with committed lockfile
- gen-wordmark.mjs produces a 9-path, upright, SemiBold, evenodd wordmark with the flipY:false and variation.set workarounds
- wordmark-custom.svg carries the mandatory D-06 w/k 20-degree cuts (not typesettable in stock Space Grotesk), committed separately from the generated base
- check-candidates.mjs enforces the LOGO-03 structural rules (no <text>, no full-bleed <rect>, viewBox present)
</success_criteria>

<output>
Create `.planning/phases/103-logo-tournament/103-01-SUMMARY.md` when done. Record: the actual glyph-5-w / glyph-7-k node topology observed, the cut technique applied, and a note confirming the wordmark is not typesettable in unmodified Space Grotesk.
</output>
