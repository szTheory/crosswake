---
phase: 13-commerce-and-entitlement-contract
plan: 01
type: execute
wave: 1
depends_on:
  - 12-03
files_modified:
  - lib/crosswake/commerce/contracts.ex
  - lib/crosswake/commerce.ex
  - lib/crosswake/manifest/types.ex
  - lib/crosswake/manifest/builder.ex
  - lib/crosswake/policy/validator.ex
  - test/crosswake/commerce/contracts_test.exs
  - test/crosswake/manifest/manifest_test.exs
  - test/crosswake/policy/compiler_test.exs
autonomous: true
requirements:
  - COMM-01
must_haves:
  truths:
    - "Phoenix teams can reference one normalized commerce vocabulary in core: `paywall_entry`, `purchase_intent`, `restore_intent`, `entitlement_snapshot`, and `reconciliation_evidence`."
    - "Commerce contract types stay small, semantic, and Phoenix-facing instead of widening into a generic billing bus or session object."
    - "Manifest truth publishes the same normalized commerce family names that the core contract uses."
    - "Route policy validation accepts the normalized commerce capability ids and rejects drift back to ambiguous provider-shaped identifiers."
  artifacts:
    - path: "lib/crosswake/commerce/contracts.ex"
      provides: "Typed commerce structs and enums for the five core commerce surfaces"
    - path: "lib/crosswake/commerce.ex"
      provides: "Thin behaviour/orchestration seam for Phoenix-owned commerce intent and snapshot hooks"
    - path: "lib/crosswake/manifest/builder.ex"
      provides: "Canonical commerce capability catalog entries using normalized family ids"
  key_links:
    - from: "lib/crosswake/commerce/contracts.ex"
      to: "lib/crosswake/manifest/types.ex"
      via: "shared typed enums and struct-to-manifest serialization shape"
    - from: "lib/crosswake/policy/validator.ex"
      to: "test/crosswake/policy/compiler_test.exs"
      via: "accepted normalized commerce capability ids"
---

<objective>
Define the normalized Phoenix-facing commerce vocabulary in code and manifest truth.

Purpose: Phase 13 starts by freezing the contract names and typed shapes Crosswake will use everywhere else. This plan turns the existing docs-only `paywall` / `purchase` / `restore` vocabulary into the locked normalized seam from D-01 through D-13, and it does so in the same typed-struct style already used by manifest and offline contracts.
Output: New core commerce contract modules, manifest wiring, route-policy vocabulary validation, and tests for the normalized commerce contract.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-commerce-and-entitlement-contract/13-CONTEXT.md
@.planning/phases/13-commerce-and-entitlement-contract/13-RESEARCH.md
@guides/capabilities.md
@lib/crosswake/manifest/builder.ex
@lib/crosswake/manifest/types.ex
@lib/crosswake/offline/contracts.ex
@lib/crosswake/policy/validator.ex

<interfaces>
From `lib/crosswake/offline/contracts.ex`:
```elixir
defmodule Crosswake.Offline.Contracts.CacheRoute do
  @enforce_keys [:id, :route_id, :staleness, :hydration, :storage, :restrictions]
end

defmodule Crosswake.Offline.Contracts.StudySessionIsland do
  @enforce_keys [:id, :route_id, :sync_seam, :draft_surface, :storage, :journal_mode, :reconciliation, :checkpoint_requirement, :authoritative_source]
end
```

From `lib/crosswake/manifest/types.ex`:
```elixir
defmodule Crosswake.Manifest.Types.Capability do
  @enforce_keys [:id, :version]
  defstruct [:id, :version, :family, :owner, :package_class, :proof_class, :rebuild, :denial, :fallback, :guide, status: :supported, prerequisites: [], legacy_ids: []]
end
```

From `lib/crosswake/manifest/builder.ex`:
```elixir
[
  id: "purchase",
  family: "purchase",
  owner: :backend_seam,
  package_class: :example_docs_only
]
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create the five typed commerce surfaces and thin orchestration seam</name>
  <files>lib/crosswake/commerce/contracts.ex, lib/crosswake/commerce.ex, lib/crosswake/manifest/types.ex, test/crosswake/commerce/contracts_test.exs</files>
  <behavior>
    - Test 1: `paywall_entry`, `purchase_intent`, `restore_intent`, `entitlement_snapshot`, and `reconciliation_evidence` each compile to explicit small structs with no provider enum leakage, per D-01, D-02, D-06, and D-12.
    - Test 2: `entitlement_snapshot` keeps `authority_state`, `access_state`, `reconciliation_state`, `checked_at`, `stale_after`, and `effective_until` distinct, per D-07 through D-11.
    - Test 3: the public seam exposes one thin behaviour for intent submission / snapshot refresh / evidence ingestion hooks instead of a session object or command bus, per D-02 and D-19.
  </behavior>
  <action>
    Add `Crosswake.Commerce.Contracts` and define the five core surface structs per D-01: `paywall_entry`, `purchase_intent`, `restore_intent`, `entitlement_snapshot`, and `reconciliation_evidence`. Keep the project’s typed-contract style from `Crosswake.Offline.Contracts`: explicit `@enforce_keys`, explicit enums, small fields, and no provider SDK payload objects in core.

    Model the entitlement snapshot exactly as a dual-lane backend read model per D-04 and D-07 through D-13. The struct must separate `authority_state` from `access_state`, preserve `canceled_scheduled_end` versus `revoked`, include freshness fields (`checked_at`, `stale_after`, `effective_until`), and carry bounded evidence metadata without collapsing to `is_subscribed`.

    Add one thin behaviour/orchestration seam in `lib/crosswake/commerce.ex` for Phoenix-owned operations such as submitting purchase/restore intent, ingesting reconciliation evidence, and fetching snapshots. Do not introduce a command bus, provider lifecycle state machine, or monolithic `CommerceSession`; those violate D-02 and the phase non-goals.

    Extend `lib/crosswake/manifest/types.ex` only where needed so these structs serialize and interoperate cleanly with existing typed manifest/support truth. Keep provider-specific detail out of the public type system per D-06 and D-12.
  </action>
  <verify>
    <automated>mix test test/crosswake/commerce/contracts_test.exs</automated>
  </verify>
  <done>The repo has explicit first-class typed commerce contract surfaces and one thin Phoenix-facing orchestration seam without widening Crosswake into a billing framework.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire normalized commerce vocabulary into manifest truth and route validation</name>
  <files>lib/crosswake/manifest/builder.ex, lib/crosswake/policy/validator.ex, test/crosswake/manifest/manifest_test.exs, test/crosswake/policy/compiler_test.exs</files>
  <behavior>
    - Test 1: manifest capability outputs publish `paywall_entry`, `purchase_intent`, `restore_intent`, `entitlement_snapshot`, and `reconciliation_evidence` instead of the pre-normalized commerce names, per D-01 and D-06.
    - Test 2: each normalized commerce family stays `backend_seam`, keeps fail-closed denial/fallback wording, and stays explicitly limited to core seam semantics rather than provider logic, per D-05, D-06, and D-20.
    - Test 3: route policy compilation accepts the normalized commerce capability ids and keeps rejecting unknown or provider-shaped drift, per D-01, D-06, and the Phase 11/12 capability discipline.
  </behavior>
  <action>
    Replace the existing Phase 11 docs-only manifest catalog rows for `paywall`, `purchase`, and `restore` with the normalized commerce ids from D-01. Add a fifth canonical row for `reconciliation_evidence`. Each entry must remain `owner: :backend_seam`, use honest prerequisites/fallback text, and keep StoreKit / Play Billing / provider SDK behavior outside core per D-06 and D-20.

    Update `Crosswake.Policy.Validator` so normalized commerce capability ids are known vocabulary. Preserve fail-closed validation; do not add provider ids such as `storekit` or `play_billing` as sanctioned route-local commerce families. The point is to let route policy name the normalized Crosswake seam, not provider internals.

    Extend the existing manifest and compiler tests so they assert the exact normalized ids, backend-seam ownership, and validator parity. Leave support-matrix/public-guide rendering to Phase 13-03, where the package and native-corridor boundaries are published together.
  </action>
  <verify>
    <automated>mix test test/crosswake/commerce/contracts_test.exs test/crosswake/manifest/manifest_test.exs test/crosswake/policy/compiler_test.exs</automated>
  </verify>
  <done>Core contracts, manifest truth, and route-policy validation all speak the same normalized commerce vocabulary without dragging support-matrix and public-boundary rendering into the first execution wave.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Phoenix route policy -> core commerce vocabulary | Untrusted route declarations choose commerce capability ids that must stay inside the normalized contract set. |
| Device/storefront callbacks -> `reconciliation_evidence` | External purchase or restore signals cross into core vocabulary but must not become entitlement authority. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-01 | Spoofing | `lib/crosswake/policy/validator.ex` | mitigate | Accept only normalized Crosswake commerce ids and reject provider-shaped route capability drift so route policy cannot spoof StoreKit or Play Billing support inside core. |
| T-13-02 | Tampering | `lib/crosswake/commerce/contracts.ex`, `lib/crosswake/commerce.ex` | mitigate | Keep `entitlement_snapshot` backend-issued and `reconciliation_evidence` evidence-only per D-04, D-05, and D-17 so device-side success callbacks cannot mutate authority semantics. |
| T-13-03 | Information Disclosure | `lib/crosswake/commerce/contracts.ex` | mitigate | Bound evidence metadata and explicitly exclude raw provider enums or SDK payload shapes from the public core structs per D-06 and D-12. |
</threat_model>

<verification>
mix test test/crosswake/commerce/contracts_test.exs test/crosswake/manifest/manifest_test.exs test/crosswake/policy/compiler_test.exs
</verification>

<success_criteria>
Crosswake core exposes exactly five typed Phoenix-facing commerce surfaces, the manifest publishes the same names, and no plan step widens commerce into device-authoritative or provider-specific core behavior.
</success_criteria>

<output>
After completion, create `.planning/phases/13-commerce-and-entitlement-contract/13-commerce-and-entitlement-contract-01-SUMMARY.md`
</output>
