---
phase: "89-e2e-integration-ui-polish"
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - examples/phoenix_host/assets/js/app.js
  - examples/phoenix_host/assets/css/app.css
autonomous: true
requirements: ["DEMO-03"]
must_haves:
  truths:
    - User sees polished UI transitions and animations
    - Native shell triggers are exposed to the offline island
  artifacts:
    - path: examples/phoenix_host/assets/css/app.css
      provides: Brand Book CSS styling and animations
    - path: examples/phoenix_host/assets/js/app.js
      provides: Native shell integration bindings
  key_links:
    - from: examples/phoenix_host/assets/js/app.js
      to: Native Shell
      via: Crosswake event bridge
---

<objective>
Connect the offline island to the native shell, apply Brand Book CSS/UI guidelines, and polish animations/microcopy.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
@prompts/crosswake-brand-book.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Integrate Native Shell Bindings in Frontend</name>
  <files>examples/phoenix_host/assets/js/app.js</files>
  <action>
    Integrate the native shell bindings in the frontend JavaScript.
    - Expose download triggers and offline route transition handlers via Crosswake event bridge.
    - Ensure close integration with v5.0 standalone shell configurations.
    - Add event listeners for offline sync progress and status.
  </action>
  <verify>
    <automated>grep -q 'Crosswake' examples/phoenix_host/assets/js/app.js || echo "Need to create test or ensure syntax is present"</automated>
  </verify>
  <done>Frontend JS handles native shell triggers and route transitions.</done>
</task>

<task type="auto">
  <name>Task 2: Refine Brand Book CSS and UI Polish</name>
  <files>examples/phoenix_host/assets/css/app.css</files>
  <action>
    Refine the Brand Book CSS to ensure smooth animations, layout consistency, and high-quality typography according to `references/ui-brand.md` or `prompts/crosswake-brand-book.md`.
    - Implement smooth CSS transitions for offline mode toggles and navigation.
    - Apply consistent typography and spacing.
    - Add polished microcopy styling for loading and sync states.
  </action>
  <verify>
    <automated>grep -q 'transition' examples/phoenix_host/assets/css/app.css || echo "Need to add transitions"</automated>
  </verify>
  <done>Brand Book CSS is applied with smooth animations and consistent styling.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Frontend to Native Shell | Untrusted UI interactions crossing to native shell context via event bridge |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-89-01 | Spoofing | Native Shell Bridge | mitigate | Validate event payloads at the JS/Native boundary to ensure only authorized actions (e.g., download triggers) are executed |
| T-89-02 | Tampering | Frontend JS | mitigate | Serve JS assets over HTTPS and use Content Security Policy |
</threat_model>

<verification>
- `app.js` correctly maps Crosswake event bridge methods.
- `app.css` includes polished animations and layout constraints.
</verification>

<success_criteria>
- The offline island is successfully connected to the native shell.
- UI transitions are polished and match Brand Book guidelines.
</success_criteria>

<output>
Create `.planning/phases/89-e2e-integration-ui-polish/89-01-SUMMARY.md` when done
</output>