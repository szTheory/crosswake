---
phase: 119-native-evidence-classification
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - examples/ios_shell_host/CrosswakeShell.xcodeproj/project.pbxproj
  - examples/android_shell_host/app/build.gradle
  - examples/ios_shell_host/README.md
  - examples/android_shell_host/README.md
autonomous: true
requirements:
  - NATIVE-01
must_haves:
  truths:
    - "NATIVE-01/D-01/D-02/D-03: the checked-in iOS and Android hosts use published-coordinate defaults, `--local` remains an explicit maintainer path, and no second host pair or ambiguous hybrid proof is introduced."
    - "NATIVE-01/D-05/D-06/D-07/D-08: checked-in host evidence uses the strict dimensional taxonomy, including proof class, coordinate mode, execution environment, and action status, and it says what the evidence does and does not prove."
    - "D-21/D-22/D-23: the checked-in host story stays skeptical-adopter-readable, maintainer-local work stays labeled as `--local`, and the public host READMEs remain compact enough to reuse in future captions."
  artifacts:
    - path: "examples/ios_shell_host/CrosswakeShell.xcodeproj/project.pbxproj"
      provides: "Checked-in iOS host published-coordinate default"
      contains: "XCRemoteSwiftPackageReference"
    - path: "examples/android_shell_host/app/build.gradle"
      provides: "Checked-in Android host published-coordinate default"
      contains: "io.github.sztheory:crosswake-shell-core-android"
    - path: "examples/ios_shell_host/README.md"
      provides: "Checked-in iOS host evidence label copy"
      contains: "checked-in public-coordinate proof"
    - path: "examples/android_shell_host/README.md"
      provides: "Checked-in Android host evidence label copy"
      contains: "checked-in public-coordinate proof"
  key_links:
    - from: "examples/ios_shell_host/README.md"
      to: "examples/ios_shell_host/CrosswakeShell.xcodeproj/project.pbxproj"
      via: "Checked-in iOS README must match the host project coordinate mode"
      pattern: "XCLocalSwiftPackageReference|crosswake-shell-core-ios"
    - from: "examples/android_shell_host/README.md"
      to: "examples/android_shell_host/app/build.gradle"
      via: "Checked-in Android README must match the host project coordinate mode"
      pattern: "dev.crosswake:shell-core-android|io.github.sztheory:crosswake-shell-core-android"
---

<objective>
Promote the checked-in native hosts to published-coordinate defaults and label them as checked-in public-coordinate proof.

Purpose: Phase 119 must stop letting the checked-in iOS and Android hosts look like stale local-development fixtures when they are meant to be public proof artifacts. This plan implements the coordinate switch and the host-local evidence labels without adding new capability breadth or device-support claims.
Output: checked-in iOS and Android host project files that resolve published coordinates by default, plus checked-in host READMEs that say what the evidence proves and does not prove.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/119-native-evidence-classification/119-CONTEXT.md
@.planning/phases/119-native-evidence-classification/119-DISCUSSION-LOG.md
@.planning/phases/119-native-evidence-classification/119-RESEARCH.md
@examples/ios_shell_host/CrosswakeShell.xcodeproj/project.pbxproj
@examples/android_shell_host/app/build.gradle
@examples/ios_shell_host/README.md
@examples/android_shell_host/README.md
@priv/templates/crosswake/shell/ios/README.md.eex
@priv/templates/crosswake/shell/android/README.md.eex
@guides/support_matrix.md
@guides/native_shell.md
@guides/android_uat.md
</context>

## Artifacts this phase produces

- Plan file: `.planning/phases/119-native-evidence-classification/119-01-PLAN.md`.
- Changed checked-in native host project files in `examples/ios_shell_host` and `examples/android_shell_host`.
- Changed checked-in native host READMEs in `examples/ios_shell_host` and `examples/android_shell_host`.

<tasks>

<task type="auto">
  <name>Task 1: Move the checked-in iOS and Android hosts to published-coordinate defaults</name>
  <read_first>
    .planning/phases/119-native-evidence-classification/119-CONTEXT.md
    .planning/phases/119-native-evidence-classification/119-RESEARCH.md
    examples/ios_shell_host/CrosswakeShell.xcodeproj/project.pbxproj
    examples/android_shell_host/app/build.gradle
    priv/templates/crosswake/shell/ios/CrosswakeShell.xcodeproj/project.pbxproj.eex
    priv/templates/crosswake/shell/android/app/build.gradle.eex
    priv/templates/crosswake/shell/android/settings.gradle.eex
  </read_first>
  <files>examples/ios_shell_host/CrosswakeShell.xcodeproj/project.pbxproj, examples/android_shell_host/app/build.gradle</files>
  <action>Update the checked-in iOS host so the project file uses the published SwiftPM mirror by default instead of a silent `XCLocalSwiftPackageReference`, and update the checked-in Android host so `app/build.gradle` depends on `io.github.sztheory:crosswake-shell-core-android:<current Crosswake version>` instead of `dev.crosswake:shell-core-android:0.1.0`. Preserve the explicit local-maintainer escape hatch in the generator/template path, but do not let the checked-in hosts default to local-dev coordinates. Keep the host-owned shell posture intact: this is a proof-label correction, not a runtime redesign.</action>
  <acceptance_criteria>
    - `examples/ios_shell_host/CrosswakeShell.xcodeproj/project.pbxproj` resolves the checked-in host against the published SwiftPM mirror by default.
    - `examples/ios_shell_host/CrosswakeShell.xcodeproj/project.pbxproj` does not silently use `XCLocalSwiftPackageReference` as the public host default.
    - `examples/android_shell_host/app/build.gradle` depends on `io.github.sztheory:crosswake-shell-core-android` at the current Crosswake version.
    - `examples/android_shell_host/app/build.gradle` no longer presents `dev.crosswake:shell-core-android:0.1.0` as the checked-in default.
    - The generator/template local path remains available for explicit `--local` maintainer flows.
    - The checked-in host strategy is published-coordinate proof, not a device-support claim or a new capability.
  </acceptance_criteria>
  <verify>
    <automated>mix test test/mix/tasks/crosswake_gen_shell_test.exs test/crosswake/doctor/publish_readiness_test.exs</automated>
  </verify>
  <done>The checked-in iOS and Android hosts now default to published native coordinates.</done>
</task>

<task type="auto">
  <name>Task 2: Label the checked-in host READMEs as checked-in public-coordinate proof</name>
  <read_first>
    .planning/phases/119-native-evidence-classification/119-CONTEXT.md
    .planning/phases/119-native-evidence-classification/119-RESEARCH.md
    examples/ios_shell_host/README.md
    examples/android_shell_host/README.md
    guides/support_matrix.md
    guides/native_shell.md
  </read_first>
  <files>examples/ios_shell_host/README.md, examples/android_shell_host/README.md</files>
  <action>Update the checked-in host READMEs so they name the evidence class explicitly as `checked-in public-coordinate proof`, repeat the coordinate mode beside the host path, and state the limitation that these hosts do not prove simulator, emulator, or physical-device support. Keep the maintainer-local `--local` path visible as an explicit alternate path, but do not let that path become the public default. Keep the copy short and operational, with no generic WebView-wrapper, React Native, plugin-platform, or everything-offline language.</action>
  <acceptance_criteria>
    - Both checked-in host READMEs use `checked-in public-coordinate proof` or equivalent explicit evidence wording.
    - Both checked-in host READMEs mention the published-coordinate mode next to the host path.
    - Both checked-in host READMEs mention `--local` as the explicit maintainer path, not the public default.
    - Neither README implies simulator, emulator, or physical-device support.
    - Neither README overstates native authority, background sync, or broad mobile-framework claims.
  </acceptance_criteria>
  <verify>
    <automated>mix test test/mix/tasks/crosswake_gen_shell_test.exs test/crosswake/doctor/publish_readiness_test.exs</automated>
  </verify>
  <done>The checked-in host READMEs now match the published-coordinate default and label the evidence honestly.</done>
</task>

</tasks>

## Source Audit

| SOURCE | ID | Feature / Requirement | Plan | Status | Notes |
|--------|----|-----------------------|------|--------|-------|
| GOAL | Phase 119 | Users can tell exactly whether native evidence proves published coordinates, local-development hosts, or advisory simulator/emulator collateral | 119-01, 119-02, 119-03 | COVERED | This plan handles the checked-in host coordinate choice. |
| REQ | NATIVE-01 | Checked-in iOS and Android hosts either prove published `0.1.2` coordinates or are explicitly labeled local-development proof | 119-01, 119-03 | COVERED | 119-01 switches the checked-in hosts to the preferred published-coordinate default. |
| CONTEXT | D-01 | Promote checked-in hosts to published-coordinate defaults | 119-01 | COVERED | Task 1. |
| CONTEXT | D-02 | Preserve `--local` as the explicit maintainer/development path | 119-01 | COVERED | Task 1 and Task 2. |
| CONTEXT | D-03 | Do not create a second pair of checked-in hosts or a hybrid proof story | 119-01 | COVERED | Task 1. |
| CONTEXT | D-04 | Checked-in host proof does not imply device or app-store support | 119-01, 119-02, 119-03 | COVERED | Task 2 reinforces the limitation. |
| CONTEXT | D-05..D-08 | Strict native evidence taxonomy and anti-overclaim wording | 119-01, 119-02, 119-03 | COVERED | 119-01 establishes the host class before docs and drift guards. |
| CONTEXT | D-21..D-23 | Skeptical adopter reader, maintainer-local path, compact captions | 119-01, 119-02, 119-03 | COVERED | The checked-in host READMEs stay short and explicit. |

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| checked-in host project -> adopter install story | Readers may treat checked-in native hosts as proof of the public install path. |
| host README -> proof label | A short README line can accidentally turn into a support claim if the label is missing. |
| published coordinates -> local maintainer path | `--local` must stay available without becoming the public default. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-119-01 | Spoofing | checked-in iOS host coordinate mode | mitigate | Replace silent local Swift package defaults with published-coordinate defaults and label the host README explicitly. |
| T-119-02 | Spoofing | checked-in Android host coordinate mode | mitigate | Replace stale `dev.crosswake:shell-core-android:0.1.0` defaults with current published coordinates and label the host README explicitly. |
| T-119-03 | Elevation of Privilege | native proof wording | mitigate | Keep device-support, background-sync, and broad mobile-framework claims out of the checked-in host READMEs. |
</threat_model>

