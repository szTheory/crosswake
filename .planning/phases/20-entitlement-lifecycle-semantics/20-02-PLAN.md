---
phase: 20-entitlement-lifecycle-semantics
plan: 02
type: execute
wave: 2
depends_on:
  - 20-01
files_modified:
  - lib/crosswake/commerce/contracts.ex
  - lib/crosswake/commerce/reconciliation.ex
  - lib/crosswake/commerce.ex
  - lib/crosswake/manifest/validator.ex
  - test/crosswake/commerce/reconciliation_test.exs
  - test/crosswake/commerce/contracts_test.exs
  - test/crosswake/manifest/validator_test.exs
autonomous: true
requirements:
  - ENTL-03
  - ENTL-02
user_setup: []
must_haves:
  truths:
    - "Reconciliation evidence uses bounded source vocabulary `device`, `storefront`, `webhook`, `support` and bounded metadata fields."
    - "Evidence ingestion can advance reconciliation state but cannot directly grant entitlement authority."
    - "Provider-specific state terms remain outside core contract lanes and are rejected in core validation paths."
    - "Unknown evidence/status mappings fail closed into non-granting reconciliation states."
  artifacts:
    - path: lib/crosswake/commerce/contracts.ex
      provides: "Bounded, provider-neutral reconciliation evidence envelope"
      contains: "defmodule ReconciliationEvidence"
    - path: lib/crosswake/commerce.ex
      provides: "Backend seam contract that keeps authority projection separate from evidence ingestion"
      contains: "ingest_reconciliation_evidence"
    - path: test/crosswake/commerce/reconciliation_test.exs
      provides: "Negative proof that evidence cannot directly grant authority"
      contains: "awaiting_verification"
  key_links:
    - from: lib/crosswake/commerce/contracts.ex
      to: lib/crosswake/commerce.ex
      via: "Typed evidence envelope is accepted by ingestion callback that returns reconciliation result, not authority snapshot mutation"
      pattern: "ingest_reconciliation_evidence"
    - from: lib/crosswake/commerce/reconciliation.ex
      to: test/crosswake/commerce/reconciliation_test.exs
      via: "Evidence processing outcomes are asserted as non-authoritative and fail-closed"
      pattern: "verification_failed"
---

<objective>
Implement authority-separation enforcement for evidence ingestion and lock ENTL-03 invariants.

Purpose: Ensure device/storefront/webhook/support evidence feeds reconciliation only and cannot mutate entitlement authority directly.
Output: Updated evidence envelope, backend seam contracts, validator protections, and merge-blocking negative tests proving non-authoritative evidence behavior.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/20-entitlement-lifecycle-semantics/20-CONTEXT.md
@.planning/phases/20-entitlement-lifecycle-semantics/20-RESEARCH.md
@.planning/phases/20-entitlement-lifecycle-semantics/20-PATTERNS.md
@lib/crosswake/commerce/contracts.ex
@lib/crosswake/commerce/reconciliation.ex
@lib/crosswake/commerce.ex
@lib/crosswake/manifest/validator.ex
@test/crosswake/commerce/reconciliation_test.exs
@test/crosswake/manifest/validator_test.exs
</context>

<tasks>
<task type="auto">
  <name>Task 1: Normalize reconciliation evidence envelope to bounded provenance fields</name>
  <files>lib/crosswake/commerce/contracts.ex, test/crosswake/commerce/contracts_test.exs</files>
  <read_first>lib/crosswake/commerce/contracts.ex, test/crosswake/commerce/contracts_test.exs, .planning/phases/20-entitlement-lifecycle-semantics/20-RESEARCH.md</read_first>
  <action>Update `ReconciliationEvidence` to use normalized source vocabulary `:device | :storefront | :webhook | :support` (replace `:device_callback`). Require bounded provenance metadata fields: `provider`, `provider_reference`, `event_kind`, `evidence_ref`, and `captured_at`; keep optional integrity metadata bounded to deterministic fields such as `integrity_digest` and `idempotency_ref`. Keep provider payload blobs and raw provider enums out of the struct and typespec.</action>
  <verify>mix test test/crosswake/commerce/contracts_test.exs</verify>
  <acceptance_criteria>
    - `lib/crosswake/commerce/contracts.ex` contains `:device | :storefront | :webhook | :support`
    - `lib/crosswake/commerce/contracts.ex` contains `provider_reference`, `event_kind`, and `evidence_ref` in `ReconciliationEvidence`
    - `lib/crosswake/commerce/contracts.ex` does not contain `:device_callback` in `ReconciliationEvidence.source`
    - `test/crosswake/commerce/contracts_test.exs` asserts the normalized evidence source vocabulary
  </acceptance_criteria>
  <done>Evidence contracts are explicit, bounded, and normalized for backend reconciliation without provider payload leakage.</done>
</task>

<task type="auto">
  <name>Task 2: Keep evidence ingestion non-authoritative in the backend seam and reconciliation module</name>
  <files>lib/crosswake/commerce.ex, lib/crosswake/commerce/reconciliation.ex, test/crosswake/commerce/reconciliation_test.exs</files>
  <read_first>lib/crosswake/commerce.ex, lib/crosswake/commerce/reconciliation.ex, test/crosswake/commerce/reconciliation_test.exs, lib/crosswake/commerce/contracts.ex, .planning/REQUIREMENTS.md</read_first>
  <action>Update `Crosswake.Commerce` callback contract so `ingest_reconciliation_evidence/1` returns an evidence-processing result (`{:ok, %Crosswake.Commerce.Reconciliation.EvidenceResult{}} | {:error, term()}`) instead of implying direct authority mutation. In reconciliation processing helpers, map success-like evidence to non-granting states (`:awaiting_verification` or `:projection_refreshed` only after backend verification marker) and block any direct write path that sets authority lane values from evidence input.</action>
  <verify>mix test test/crosswake/commerce/reconciliation_test.exs</verify>
  <acceptance_criteria>
    - `lib/crosswake/commerce.ex` callback `ingest_reconciliation_evidence` returns a reconciliation evidence result type
    - `lib/crosswake/commerce/reconciliation.ex` contains explicit non-granting mapping to `:awaiting_verification` for unverified evidence
    - `test/crosswake/commerce/reconciliation_test.exs` contains `refute` assertions that evidence ingestion directly yields authority `:active`
    - `test/crosswake/commerce/reconciliation_test.exs` contains replay/idempotency assertions that duplicate evidence does not mutate authority directly
  </acceptance_criteria>
  <done>Evidence ingestion stays backend-owned and non-authoritative by contract and test proof.</done>
</task>

<task type="auto">
  <name>Task 3: Harden provider-leak and authority-shortcut checks in core validators/tests</name>
  <files>lib/crosswake/manifest/validator.ex, test/crosswake/manifest/validator_test.exs, test/crosswake/commerce/reconciliation_test.exs</files>
  <read_first>lib/crosswake/manifest/validator.ex, test/crosswake/manifest/validator_test.exs, test/crosswake/commerce/reconciliation_test.exs, .planning/phases/20-entitlement-lifecycle-semantics/20-PATTERNS.md</read_first>
  <action>Add validator/test coverage that rejects provider-specific commerce vocabulary in new entitlement/evidence-related fields and rejects attempts to pass provider status terms as core lane states. Add explicit negative tests for terms like `storekit`, `play_billing`, and `revenuecat` appearing in core entitlement/evidence contract surfaces, and enforce fail-closed handling to `:awaiting_verification` or `:verification_failed` (never implicit grant).</action>
  <verify>mix test test/crosswake/manifest/validator_test.exs test/crosswake/commerce/reconciliation_test.exs</verify>
  <acceptance_criteria>
    - `test/crosswake/manifest/validator_test.exs` contains provider-term rejection assertions for `storekit`, `play_billing`, and `revenuecat`
    - `test/crosswake/commerce/reconciliation_test.exs` contains fail-closed assertions to `:awaiting_verification` or `:verification_failed` for unknown/unverified evidence
    - `lib/crosswake/manifest/validator.ex` contains provider-specific vocabulary guards that apply to nested commerce/entitlement structures
    - test suite passes for manifest validator and reconciliation tests
  </acceptance_criteria>
  <done>ENTL-03 enforcement and provider-neutral core boundaries are locked with deterministic negative tests.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

- Device/storefront/webhook/support evidence enters backend processing through `ingest_reconciliation_evidence/1`.
- Evidence and entitlement contract values cross from host adapter boundaries into core provider-neutral types.

## STRIDE Threat Register

| threat_id | category | component | disposition | mitigation_plan |
| --- | --- | --- | --- | --- |
| P20-02-T01 | spoofing | Evidence source identity | mitigated | Restrict evidence source to explicit `:device | :storefront | :webhook | :support` and require provider-aware references. |
| P20-02-T02 | tampering | Evidence payload attempting authority mutation | mitigated | Keep ingestion callback result non-authoritative and block direct authority lane writes from evidence path. |
| P20-02-T03 | information_disclosure | Provider enum leakage into core contracts | mitigated | Reject provider-specific terms in entitlement/evidence validation and lock rejection via manifest/contract tests. |
| P20-02-T04 | elevation_of_privilege | Evidence success interpreted as entitlement grant | mitigated | Enforce non-granting default mappings (`:awaiting_verification` / `:verification_failed`) and add explicit negative tests for direct authority grants. |
</threat_model>

<verification>
Before declaring plan complete:
- [ ] mix test test/crosswake/commerce/contracts_test.exs
- [ ] mix test test/crosswake/commerce/reconciliation_test.exs
- [ ] mix test test/crosswake/manifest/validator_test.exs
- [ ] rg "device_callback|storefront|awaiting_verification|verification_failed|storekit|play_billing|revenuecat" lib/crosswake/commerce lib/crosswake/manifest test/crosswake/commerce test/crosswake/manifest
</verification>

<success_criteria>
- All tasks completed
- ENTL-03 evidence inputs are bounded and non-authoritative by contract and tests
- ENTL-02 lifecycle handling remains fail-closed and provider-neutral under unknown/unverified evidence
- Core semantics remain scoped to entitlement lifecycle contracts, not provider adapter implementation
</success_criteria>

<output>
After completion, create `.planning/phases/20-entitlement-lifecycle-semantics/20-02-SUMMARY.md`
</output>
