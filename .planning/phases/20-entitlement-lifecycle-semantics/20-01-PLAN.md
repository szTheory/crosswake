---
phase: 20-entitlement-lifecycle-semantics
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/crosswake/commerce/contracts.ex
  - lib/crosswake/commerce/reconciliation.ex
  - test/crosswake/commerce/contracts_test.exs
  - test/crosswake/commerce/reconciliation_test.exs
autonomous: true
requirements:
  - ENTL-01
  - ENTL-02
user_setup: []
must_haves:
  truths:
    - "Entitlement snapshots are lane-structured with explicit authority, access, reconciliation, freshness, effective window, and evidence metadata lanes."
    - "Authority/access/reconciliation/freshness vocabularies are orthogonal and use Crosswake-owned states."
    - "Pending and verification states remain reconciliation-only states and cannot be interpreted as authority grants."
    - "Core contract structs remain provider-neutral and additive without introducing provider adapter implementation."
  artifacts:
    - path: lib/crosswake/commerce/contracts.ex
      provides: "Lane-structured `EntitlementSnapshot` and explicit lane vocabularies"
      contains: "defmodule EntitlementSnapshot"
    - path: lib/crosswake/commerce/reconciliation.ex
      provides: "Canonical reconciliation outcomes aligned to entitlement lanes"
      contains: "def outcome_vocabulary"
    - path: test/crosswake/commerce/contracts_test.exs
      provides: "Contract shape and lane vocabulary lock tests"
      contains: "authority"
  key_links:
    - from: lib/crosswake/commerce/contracts.ex
      to: lib/crosswake/commerce/reconciliation.ex
      via: "Reconciliation outcomes are constrained to reconciliation lane vocabulary and never authority lane values"
      pattern: "pending_purchase"
    - from: lib/crosswake/commerce/contracts.ex
      to: test/crosswake/commerce/contracts_test.exs
      via: "Lane struct keys and allowed states are locked by contract tests"
      pattern: "EntitlementSnapshot"
---

<objective>
Implement the Phase 20 core entitlement snapshot lane model and lifecycle taxonomy.

Purpose: Satisfy ENTL-01 and ENTL-02 by making authority/access/reconciliation/freshness/effective/evidence semantics explicit, typed, and provider-neutral.
Output: Updated commerce contract and reconciliation modules plus tests proving orthogonal lane semantics and canonical lifecycle vocabulary.
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/20-entitlement-lifecycle-semantics/20-CONTEXT.md
@.planning/phases/20-entitlement-lifecycle-semantics/20-RESEARCH.md
@.planning/phases/20-entitlement-lifecycle-semantics/20-PATTERNS.md
@lib/crosswake/commerce/contracts.ex
@lib/crosswake/commerce/reconciliation.ex
@test/crosswake/commerce/contracts_test.exs
@test/crosswake/commerce/reconciliation_test.exs
</context>

<tasks>
<task type="auto">
  <name>Task 1: Reshape `EntitlementSnapshot` into explicit lane structs with enforced keys</name>
  <files>lib/crosswake/commerce/contracts.ex, test/crosswake/commerce/contracts_test.exs</files>
  <read_first>lib/crosswake/commerce/contracts.ex, test/crosswake/commerce/contracts_test.exs, .planning/phases/20-entitlement-lifecycle-semantics/20-CONTEXT.md, .planning/phases/20-entitlement-lifecycle-semantics/20-RESEARCH.md</read_first>
  <action>Replace flat `EntitlementSnapshot` fields (`authority_state`, `access_state`, `checked_at`, `stale_after`, `effective_until`) with explicit lane fields named `authority`, `access`, `reconciliation`, `freshness`, `effective`, and `evidence`, plus monotonic ordering marker `as_of`. Add nested lane structs with exact vocabularies: authority states `:none | :active | :grace | :billing_retry | :canceled_scheduled_end | :revoked | :refunded | :expired`; access decisions `:granted | :denied`; freshness states `:fresh | :stale | :unknown`; reconciliation states `:pending_purchase | :pending_restore | :awaiting_verification | :projection_refreshed | :conflict | :verification_failed | :stale_authority`. Keep all lane contracts provider-neutral and typed with `@enforce_keys`.</action>
  <verify>mix test test/crosswake/commerce/contracts_test.exs</verify>
  <acceptance_criteria>
    - `lib/crosswake/commerce/contracts.ex` contains `:grace`, `:billing_retry`, and `:refunded` in authority vocabulary
    - `lib/crosswake/commerce/contracts.ex` contains lane fields `:authority`, `:access`, `:reconciliation`, `:freshness`, `:effective`, `:evidence`, and `:as_of`
    - `lib/crosswake/commerce/contracts.ex` does not contain `authority_state` or `checked_at` in `EntitlementSnapshot` struct definition
    - `test/crosswake/commerce/contracts_test.exs` asserts required lane keys for `EntitlementSnapshot`
  </acceptance_criteria>
  <done>Snapshot model encodes all required Phase 20 lanes and lifecycle semantics as typed, explicit structures.</done>
</task>

<task type="auto">
  <name>Task 2: Align reconciliation vocabulary to lane taxonomy without authority leakage</name>
  <files>lib/crosswake/commerce/reconciliation.ex, test/crosswake/commerce/reconciliation_test.exs</files>
  <read_first>lib/crosswake/commerce/reconciliation.ex, test/crosswake/commerce/reconciliation_test.exs, lib/crosswake/commerce/contracts.ex, .planning/REQUIREMENTS.md</read_first>
  <action>Keep `outcome_vocabulary/0` as canonical reconciliation states and add explicit guards/helpers that classify these outcomes as reconciliation-only (for example `reconciliation_outcome?/1`) and never authority states. Ensure `pending_purchase`, `pending_restore`, and `awaiting_verification` are treated as unresolved reconciliation states and never mapped to access `:granted`. Keep `projection_refreshed`, `verification_failed`, `conflict`, and `stale_authority` bounded to reconciliation workflow reporting.</action>
  <verify>mix test test/crosswake/commerce/reconciliation_test.exs</verify>
  <acceptance_criteria>
    - `lib/crosswake/commerce/reconciliation.ex` contains `:pending_purchase`, `:pending_restore`, and `:awaiting_verification`
    - `lib/crosswake/commerce/reconciliation.ex` contains a helper that explicitly classifies reconciliation outcomes
    - `test/crosswake/commerce/reconciliation_test.exs` contains assertions that pending/verification outcomes are not authority grants
    - `test/crosswake/commerce/reconciliation_test.exs` contains assertions that reconciliation outcomes do not imply access `:granted`
  </acceptance_criteria>
  <done>Reconciliation lifecycle semantics are explicit and cannot be confused with authority or access lanes.</done>
</task>

<task type="auto">
  <name>Task 3: Lock ENTL-01 and ENTL-02 with contract taxonomy and placement tests</name>
  <files>test/crosswake/commerce/contracts_test.exs, test/crosswake/commerce/reconciliation_test.exs</files>
  <read_first>test/crosswake/commerce/contracts_test.exs, test/crosswake/commerce/reconciliation_test.exs, .planning/ROADMAP.md, .planning/REQUIREMENTS.md</read_first>
  <action>Add positive and negative tests that assert exact lane/state placement: authority supports `:active`, `:grace`, `:billing_retry`, `:canceled_scheduled_end`, `:revoked`, `:refunded`, `:expired`; reconciliation supports `:pending_purchase`, `:pending_restore`, `:awaiting_verification`; freshness supports `:fresh`, `:stale`, `:unknown`; access is only `:granted | :denied`. Reject invalid mixed placement such as `authority: :pending_restore` or `freshness: :active` through constructor or validation tests.</action>
  <verify>mix test test/crosswake/commerce/contracts_test.exs test/crosswake/commerce/reconciliation_test.exs</verify>
  <acceptance_criteria>
    - `test/crosswake/commerce/contracts_test.exs` contains `:billing_retry` and `:unknown`
    - `test/crosswake/commerce/contracts_test.exs` contains a rejection case for `authority: :pending_restore`
    - `test/crosswake/commerce/reconciliation_test.exs` contains a rejection case for reconciliation outcomes treated as authority states
    - test suite passes for both commerce contract and reconciliation test files
  </acceptance_criteria>
  <done>Phase 20 lane/taxonomy semantics are merge-blocking and executable through deterministic tests.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries

- Commerce evidence and lifecycle status terms cross from host/runtime edges into backend-owned contract structs.
- Entitlement lane semantics cross from core structs to downstream route/support/docs surfaces.

## STRIDE Threat Register

| threat_id | category | component | disposition | mitigation_plan |
| --- | --- | --- | --- | --- |
| P20-01-T01 | spoofing | Reconciliation status classification | mitigated | Keep a canonical reconciliation vocabulary helper and reject non-canonical status placement into authority/access lanes. |
| P20-01-T02 | tampering | Snapshot lane fields | mitigated | Use explicit lane structs with `@enforce_keys` and typed vocabularies; remove ambiguous flat fields that allow mixed semantics. |
| P20-01-T03 | repudiation | Lifecycle interpretation | mitigated | Add tests that prove pending/verification states are reconciliation-only and never interpreted as authority grants. |
| P20-01-T04 | elevation_of_privilege | Authority lane assignment | mitigated | Reject reconciliation states in authority lane and enforce authority vocabulary limited to backend-owned entitlement states. |
</threat_model>

<verification>
Before declaring plan complete:
- [ ] mix test test/crosswake/commerce/contracts_test.exs
- [ ] mix test test/crosswake/commerce/reconciliation_test.exs
- [ ] rg "billing_retry|refunded|pending_restore|awaiting_verification|:fresh|:stale|:unknown" lib/crosswake/commerce test/crosswake/commerce
</verification>

<success_criteria>
- All tasks completed
- ENTL-01 snapshot lanes are explicit and typed
- ENTL-02 lifecycle states are represented with Crosswake-owned vocabulary in the correct lanes
- Core contract semantics remain provider-neutral and backend-authoritative
</success_criteria>

<output>
After completion, create `.planning/phases/20-entitlement-lifecycle-semantics/20-01-SUMMARY.md`
</output>
