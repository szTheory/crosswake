---
phase: 04-honest-offline-contract
plan: 02
type: execute
wave: 2
depends_on:
  - 04-01
files_modified:
  - lib/crosswake/offline/contracts.ex
  - lib/crosswake/offline/journal.ex
  - lib/crosswake/offline/replay.ex
  - lib/crosswake/offline/runtime.ex
  - lib/crosswake/manifest/types.ex
  - lib/crosswake/manifest/builder.ex
  - priv/templates/crosswake/shell/ios/OfflineStore.swift.eex
  - priv/templates/crosswake/shell/ios/StudySessionIsland.swift.eex
  - priv/templates/crosswake/shell/android/app/src/main/java/dev/crosswake/shell/offline/OfflineStore.kt.eex
  - priv/templates/crosswake/shell/android/app/src/main/java/dev/crosswake/shell/offline/StudySessionIsland.kt.eex
  - test/crosswake/offline/contracts_test.exs
  - test/crosswake/offline/journal_test.exs
  - test/crosswake/offline/replay_test.exs
  - test/crosswake/offline/runtime_test.exs
  - test/crosswake/manifest/manifest_test.exs
autonomous: true
requirements:
  - OFFL-02
  - OFFL-03
must_haves:
  truths:
    - "The study-session offline island has one typed local-first contract with explicit draft, journal, and replay seams."
    - "Committed offline actions are immutable journal entries with identity, checkpoint, and idempotency metadata."
    - "Replay and reconciliation outcomes are explicit accepted, rejected, or conflict results under Phoenix-authoritative truth."
  artifacts:
    - path: "lib/crosswake/offline/contracts.ex"
      provides: "Typed study-session island and replay contract definitions"
    - path: "lib/crosswake/offline/journal.ex"
      provides: "Immutable journal entry and outbox entry structures"
    - path: "lib/crosswake/offline/replay.ex"
      provides: "Typed replay request and outcome contract"
    - path: "lib/crosswake/offline/runtime.ex"
      provides: "Runtime-level cached hydration and study-session offline execution contract"
    - path: "priv/templates/crosswake/shell/ios/OfflineStore.swift.eex"
      provides: "SQLite-backed iOS persistence template for cached routes and journal durability"
    - path: "priv/templates/crosswake/shell/android/app/src/main/java/dev/crosswake/shell/offline/OfflineStore.kt.eex"
      provides: "SQLite-backed Android persistence template for cached routes and journal durability"
  key_links:
    - from: "lib/crosswake/offline/journal.ex"
      to: "lib/crosswake/offline/replay.ex"
      via: "queued entries become explicit replay requests and typed outcomes"
      pattern: "idempotency_key|status|accepted|rejected|conflict"
    - from: "lib/crosswake/offline/contracts.ex"
      to: "lib/crosswake/manifest/types.ex"
      via: "study-session exemplar truth is embedded in manifest route entries"
      pattern: "study.session|sync_seam|draft|journal"
    - from: "lib/crosswake/offline/runtime.ex"
      to: "priv/templates/crosswake/shell/ios/OfflineStore.swift.eex"
      via: "typed runtime contract is implemented through generated SQLite-backed shell surfaces"
      pattern: "sqlite|cache|draft|journal|replay"
---

<objective>
Define and wire the narrow local-first mutation runtime for the one named Phase 4 exemplar.

Purpose: Fulfill OFFL-02 and OFFL-03 by giving Crosswake one real offline-island contract with SQLite-backed durability, runnable offline execution, replay, and explicit reconciliation semantics.
Output: Typed offline-island modules, runtime wiring targets for both shell templates, journal/replay contract tests, runtime tests, and manifest coverage for the study-session exemplar.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-honest-offline-contract/04-CONTEXT.md
@.planning/phases/04-honest-offline-contract/04-RESEARCH.md
@.planning/phases/04-honest-offline-contract/04-PATTERNS.md
@.planning/phases/04-honest-offline-contract/04-01-PLAN.md
@lib/crosswake/manifest/types.ex
@lib/crosswake/manifest/builder.ex
@lib/crosswake/bridge/contract.ex
@lib/crosswake/shell/activation.ex

<interfaces>
From 04-01:
```text
Route and manifest truth already distinguish cache contracts from island contracts.
```

From lib/crosswake/bridge/contract.ex:
```elixir
Typed request/reply structs carry explicit ids, status, payload, and `to_map/1`.
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define the study-session island, draft, journal, and runtime execution contract</name>
  <read_first>.planning/phases/04-honest-offline-contract/04-CONTEXT.md, .planning/phases/04-honest-offline-contract/04-RESEARCH.md, .planning/phases/04-honest-offline-contract/04-PATTERNS.md, .planning/phases/04-honest-offline-contract/04-01-PLAN.md, lib/crosswake/manifest/types.ex, lib/crosswake/bridge/contract.ex</read_first>
  <files>lib/crosswake/offline/contracts.ex, lib/crosswake/offline/journal.ex, lib/crosswake/offline/runtime.ex, test/crosswake/offline/contracts_test.exs, test/crosswake/offline/journal_test.exs, test/crosswake/offline/runtime_test.exs</files>
  <behavior>
    - Test 1: The study-session island contract names its sync seam, local draft surface, queueing posture, and checkpoint requirements explicitly.
    - Test 2: Journal entries are immutable and carry route id, sync seam, operation, payload, client mutation id, idempotency key, base checkpoint, timestamps, and status.
    - Test 3: The runtime contract names SQLite-backed local persistence and executable read/write behavior for cached hydration, draft state, and journal durability on iOS and Android.
    - Test 4: The contract does not imply direct local mutation of canonical server rows.
  </behavior>
  <action>Create `Crosswake.Offline.Contracts`, `Crosswake.Offline.Journal`, and `Crosswake.Offline.Runtime` using the repo’s typed-struct and typed-decision patterns. Encode D-10 through D-17 directly: draft state is local and in-progress; committed semantic actions append immutable journal entries; and the exemplar names SQLite-backed persistence and runtime-owned reads for cached and island routes on both iOS and Android. Keep the scope to one study/session workflow and one neighboring cached route, but make the runtime contract executable enough that later shell work has a concrete persistence and hydration target rather than only abstract policy metadata.</action>
  <verify>
    <automated>mix test test/crosswake/offline/contracts_test.exs test/crosswake/offline/journal_test.exs test/crosswake/offline/runtime_test.exs</automated>
  </verify>
  <acceptance_criteria>
    <automated>rg -n 'study\\.session|draft|journal|idempotency|checkpoint|queued|client_mutation_id|sqlite|cached|hydration' lib/crosswake/offline/contracts.ex lib/crosswake/offline/journal.ex lib/crosswake/offline/runtime.ex test/crosswake/offline/contracts_test.exs test/crosswake/offline/journal_test.exs test/crosswake/offline/runtime_test.exs</automated>
    <automated>mix test test/crosswake/offline/contracts_test.exs test/crosswake/offline/journal_test.exs test/crosswake/offline/runtime_test.exs</automated>
  </acceptance_criteria>
  <done>The repo has a typed study-session island contract, immutable journal truth, and a runtime execution contract for SQLite-backed cached and local-first behavior.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire typed replay and executable local persistence into manifest and generated shell truth</name>
  <read_first>.planning/phases/04-honest-offline-contract/04-CONTEXT.md, .planning/phases/04-honest-offline-contract/04-PATTERNS.md, lib/crosswake/offline/contracts.ex, lib/crosswake/offline/journal.ex, lib/crosswake/manifest/types.ex, lib/crosswake/manifest/builder.ex, test/crosswake/manifest/manifest_test.exs</read_first>
  <files>lib/crosswake/offline/replay.ex, lib/crosswake/manifest/types.ex, lib/crosswake/manifest/builder.ex, priv/templates/crosswake/shell/ios/OfflineStore.swift.eex, priv/templates/crosswake/shell/ios/StudySessionIsland.swift.eex, priv/templates/crosswake/shell/android/app/src/main/java/dev/crosswake/shell/offline/OfflineStore.kt.eex, priv/templates/crosswake/shell/android/app/src/main/java/dev/crosswake/shell/offline/StudySessionIsland.kt.eex, test/crosswake/offline/replay_test.exs, test/crosswake/offline/runtime_test.exs, test/crosswake/manifest/manifest_test.exs</files>
  <behavior>
    - Test 1: Replay requests are typed and route-scoped rather than free-form sync calls.
    - Test 2: Replay outcomes are explicit `accepted`, `rejected`, or `conflict`, with authoritative state or checkpoint metadata where applicable.
    - Test 3: Generated iOS and Android shell assets receive SQLite-backed store and study-session runtime templates that can hydrate a cached route and persist draft/journal state offline without LiveView round-trips.
    - Test 4: Manifest truth for the study-session island includes the replay seam and local persistence posture needed for honest support claims.
  </behavior>
  <action>Create `Crosswake.Offline.Replay` as the typed replay request/outcome surface, modeled like the repo’s other request/decision contracts. Encode D-11 through D-13 directly: Phoenix stays authoritative, replay is explicit, and conflict is a first-class result rather than silent last-write-wins. Then add generated-shell template seams for iOS and Android that implement SQLite-backed cached hydration and study-session draft/journal persistence from the same typed runtime contract. Finally, thread the replay seam and local-store posture into manifest route truth so one route entry proves the actual executable offline contract Crosswake supports in Phase 4 rather than only describing it.</action>
  <verify>
    <automated>mix test test/crosswake/offline/replay_test.exs test/crosswake/offline/runtime_test.exs test/crosswake/manifest/manifest_test.exs</automated>
  </verify>
  <acceptance_criteria>
    <automated>test -f priv/templates/crosswake/shell/ios/OfflineStore.swift.eex && test -f priv/templates/crosswake/shell/android/app/src/main/java/dev/crosswake/shell/offline/OfflineStore.kt.eex</automated>
    <automated>rg -n 'accepted|rejected|conflict|replay|authoritative|checkpoint|sync_seam|sqlite|cached|draft|journal' lib/crosswake/offline/replay.ex lib/crosswake/manifest/types.ex lib/crosswake/manifest/builder.ex priv/templates/crosswake/shell/ios/OfflineStore.swift.eex priv/templates/crosswake/shell/ios/StudySessionIsland.swift.eex priv/templates/crosswake/shell/android/app/src/main/java/dev/crosswake/shell/offline/OfflineStore.kt.eex priv/templates/crosswake/shell/android/app/src/main/java/dev/crosswake/shell/offline/StudySessionIsland.kt.eex test/crosswake/offline/replay_test.exs test/crosswake/offline/runtime_test.exs test/crosswake/manifest/manifest_test.exs</automated>
    <automated>mix test test/crosswake/offline/replay_test.exs test/crosswake/offline/runtime_test.exs test/crosswake/manifest/manifest_test.exs</automated>
  </acceptance_criteria>
  <done>The study-session exemplar now carries typed replay, SQLite-backed local persistence, and generated-shell runtime truth through the manifest and shell seams.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local draft/journal state -> server reconciliation | Hidden or mutable local truth here would undermine conflict safety and product honesty |
| typed runtime contract -> generated shell persistence layer | If shell assets do not implement the same SQLite-backed posture, OFFL-01 and OFFL-02 remain aspirational |
| replay outcome -> user/operator surface | If replay outcomes are vague, support and UX cannot distinguish queued, failed, or conflicting state |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-04 | T | `lib/crosswake/offline/journal.ex` | mitigate | Keep committed actions immutable and append-only so replay history is auditable |
| T-04-05 | R | `lib/crosswake/offline/replay.ex` | mitigate | Return explicit accepted/rejected/conflict outcomes plus checkpoint/state metadata |
| T-04-06 | E | local-first contract | mitigate | Preserve Phoenix-authoritative reconciliation and reject direct local mutation of canonical rows |
| T-04-06a | T | generated shell offline store templates | mitigate | Implement the same SQLite-backed cached-hydration and journal-durability posture on both platforms from typed contract truth |
</threat_model>

<verification>
Run `mix test test/crosswake/offline/contracts_test.exs test/crosswake/offline/journal_test.exs test/crosswake/offline/replay_test.exs test/crosswake/offline/runtime_test.exs test/crosswake/manifest/manifest_test.exs` and confirm the exemplar has explicit draft, journal, replay, SQLite-backed persistence, and conflict semantics.
</verification>

<success_criteria>
- One study-session island contract exists as the only supported local-first exemplar.
- Offline mutations are journaled immutably with replay metadata.
- Replay results are explicit and server-authoritative.
- Cached-route hydration and study-session local persistence have concrete generated-shell runtime targets on both platforms.
</success_criteria>

<output>
After completion, create `.planning/phases/04-honest-offline-contract/04-honest-offline-contract-02-SUMMARY.md`
</output>
