---
phase: 04-honest-offline-contract
plan: 03
type: execute
wave: 3
depends_on:
  - 04-01
  - 04-02
files_modified:
  - lib/crosswake/offline/status.ex
  - lib/crosswake/offline/telemetry.ex
  - lib/crosswake/doctor/doctor.ex
  - lib/crosswake/doctor/check.ex
  - lib/crosswake/doctor/formatter.ex
  - lib/crosswake/doctor/json_formatter.ex
  - test/crosswake/offline/telemetry_test.exs
  - test/crosswake/doctor/doctor_test.exs
  - test/mix/tasks/crosswake_doctor_test.exs
  - test/crosswake/offline/status_test.exs
autonomous: true
requirements:
  - OFFL-04
  - OFFL-01
  - OFFL-03
must_haves:
  truths:
    - "Crosswake exposes stable route-local offline state vocabulary for cached, saved-local, queued, failed, and conflict-required conditions."
    - "Doctor and JSON output report offline posture from typed contract truth instead of inferred heuristics."
    - "Operator-facing machine-readable findings carry route id, runtime, offline class, sync seam, journal entry id, manifest/runtime versions, correlation id, and terminal outcome where applicable."
  artifacts:
    - path: "lib/crosswake/offline/status.ex"
      provides: "Stable offline status vocabulary and optional details/hints"
    - path: "lib/crosswake/offline/telemetry.ex"
      provides: "Stable offline transition and terminal-outcome instrumentation contract"
    - path: "lib/crosswake/doctor/doctor.ex"
      provides: "Offline posture inspection and findings aggregation"
    - path: "lib/crosswake/doctor/json_formatter.ex"
      provides: "Machine-readable offline report payload"
  key_links:
    - from: "lib/crosswake/offline/status.ex"
      to: "lib/crosswake/doctor/formatter.ex"
      via: "stable route-local statuses become user-facing support and diagnostic language"
      pattern: "cached_read_only|saved_locally|queued_for_replay|replay_failed|conflict_requires_attention"
    - from: "lib/crosswake/offline/telemetry.ex"
      to: "lib/crosswake/doctor/doctor.ex"
      via: "instrumentation metadata defines the operator-facing fields doctor validates and renders"
      pattern: "route_id|sync_seam|journal_entry_id|correlation_id|terminal_outcome"
    - from: "lib/crosswake/doctor/doctor.ex"
      to: "lib/crosswake/doctor/json_formatter.ex"
      via: "structured offline posture and findings are emitted in human and JSON formats"
      pattern: "offline|sync_seam|journal_entry_id|terminal_outcome"
---

<objective>
Turn the offline contract into inspectable developer and operator product surface.

Purpose: Fulfill OFFL-04 and the visibility portions of OFFL-01/OFFL-03 by extending route-local state vocabulary, doctor posture, and machine-readable diagnostics.
Output: Offline status contract, offline telemetry/instrumentation contract, doctor/report updates, formatter updates, and tests proving stable support language and operator metadata.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-honest-offline-contract/04-CONTEXT.md
@.planning/phases/04-honest-offline-contract/04-RESEARCH.md
@.planning/phases/04-honest-offline-contract/04-PATTERNS.md
@.planning/phases/04-honest-offline-contract/04-01-PLAN.md
@.planning/phases/04-honest-offline-contract/04-02-PLAN.md
@lib/crosswake/doctor/doctor.ex
@lib/crosswake/doctor/check.ex
@lib/crosswake/doctor/formatter.ex
@lib/crosswake/doctor/json_formatter.ex

<interfaces>
From lib/crosswake/doctor/check.ex:
```elixir
@enforce_keys [:severity, :code, :message, :check]
defstruct [:severity, :code, :message, :hint, :check, details: %{}]
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define stable route-local offline status vocabulary</name>
  <read_first>.planning/phases/04-honest-offline-contract/04-CONTEXT.md, .planning/phases/04-honest-offline-contract/04-RESEARCH.md, .planning/phases/04-honest-offline-contract/04-PATTERNS.md, .planning/phases/04-honest-offline-contract/04-02-PLAN.md</read_first>
  <files>lib/crosswake/offline/status.ex, test/crosswake/offline/status_test.exs</files>
  <behavior>
    - Test 1: Cached/stale and local-first replay states are clearly distinct.
    - Test 2: Conflict-required and replay-failed states can carry interrupting hints/details without changing the stable vocabulary.
    - Test 3: The status contract stays route-local and does not collapse into a generic app-wide offline flag.
  </behavior>
  <action>Create `Crosswake.Offline.Status` as a stable, typed product vocabulary for route-local offline truth. Encode D-19 through D-21 directly with explicit states such as cached-read-only or stale, saved locally, queued for replay, replay failed, and conflict requires attention. Follow the small typed model pattern already used for denials and doctor findings so these statuses can be rendered consistently across docs, JSON, and future shell surfaces.</action>
  <verify>
    <automated>mix test test/crosswake/offline/status_test.exs</automated>
  </verify>
  <acceptance_criteria>
    <automated>rg -n 'cached|stale|saved_locally|queued_for_replay|replay_failed|conflict_requires_attention' lib/crosswake/offline/status.ex test/crosswake/offline/status_test.exs</automated>
    <automated>mix test test/crosswake/offline/status_test.exs</automated>
  </acceptance_criteria>
  <done>Crosswake now has one stable route-local offline status vocabulary.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add offline telemetry contract and extend doctor/JSON output with structured findings</name>
  <read_first>.planning/phases/04-honest-offline-contract/04-CONTEXT.md, .planning/phases/04-honest-offline-contract/04-PATTERNS.md, lib/crosswake/offline/status.ex, lib/crosswake/doctor/doctor.ex, lib/crosswake/doctor/formatter.ex, lib/crosswake/doctor/json_formatter.ex, test/crosswake/doctor/doctor_test.exs</read_first>
  <files>lib/crosswake/offline/telemetry.ex, lib/crosswake/doctor/doctor.ex, lib/crosswake/doctor/check.ex, lib/crosswake/doctor/formatter.ex, lib/crosswake/doctor/json_formatter.ex, test/crosswake/offline/telemetry_test.exs, test/crosswake/doctor/doctor_test.exs, test/mix/tasks/crosswake_doctor_test.exs</files>
  <behavior>
    - Test 1: Doctor reports whether explicit cache/island contracts, replay seams, and route-local statuses are present and coherent.
    - Test 2: Human-readable and JSON output both expose offline posture without widening support claims beyond one exemplar.
    - Test 3: A stable instrumentation contract exists for offline state transitions and terminal outcomes keyed by route id, runtime, offline mode, sync seam, journal entry id, manifest/runtime versions, correlation id, and terminal outcome.
    - Test 4: Findings and JSON payloads carry that machine-readable metadata where available.
  </behavior>
  <action>Create `Crosswake.Offline.Telemetry` as the stable instrumentation contract for state transitions and terminal outcomes before extending doctor. Encode D-23 and D-24 directly: define route id, runtime, offline mode, sync seam, journal entry id, manifest/runtime versions, correlation id, transition, and terminal outcome as the canonical operator fields, and keep the event surface focused on state transitions and terminal outcomes instead of noisy low-level chatter. Then extend `Crosswake.Doctor` and its formatters to validate and render the presence and coherence of that offline instrumentation contract using the same product-level posture approach already used for shells and bridge support. Keep the claim narrow per D-22 through D-27 and do not add a mounted diagnostics dashboard in this phase.</action>
  <verify>
    <automated>mix test test/crosswake/offline/telemetry_test.exs test/crosswake/doctor/doctor_test.exs test/mix/tasks/crosswake_doctor_test.exs</automated>
  </verify>
  <acceptance_criteria>
    <automated>rg -n 'offline|transition|terminal_outcome|sync_seam|journal_entry_id|correlation_id|queued_for_replay|conflict_requires_attention' lib/crosswake/offline/telemetry.ex lib/crosswake/doctor/doctor.ex lib/crosswake/doctor/check.ex lib/crosswake/doctor/formatter.ex lib/crosswake/doctor/json_formatter.ex test/crosswake/offline/telemetry_test.exs test/crosswake/doctor/doctor_test.exs test/mix/tasks/crosswake_doctor_test.exs</automated>
    <automated>mix test test/crosswake/offline/telemetry_test.exs test/crosswake/doctor/doctor_test.exs test/mix/tasks/crosswake_doctor_test.exs</automated>
  </acceptance_criteria>
  <done>Crosswake now has a stable offline telemetry contract and doctor surfaces that render its posture and metadata.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| offline contract truth -> user/operator diagnostics | Weak or vague states here would turn support posture into marketing instead of evidence |
| offline runtime -> telemetry/log contract | Missing instrumentation would leave operator-facing transition/outcome truth unproven |
| replay metadata -> JSON findings | Missing metadata would make sync and conflict failures hard to triage |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-07 | R | `lib/crosswake/offline/status.ex` | mitigate | Keep a stable product vocabulary so route-level states are auditable and comparable across surfaces |
| T-04-07a | R | `lib/crosswake/offline/telemetry.ex` | mitigate | Define one stable transition/outcome instrumentation contract keyed by route, seam, journal id, and correlation id |
| T-04-08 | I | `lib/crosswake/doctor/json_formatter.ex` | mitigate | Emit explicit machine-readable fields instead of ambiguous prose-only findings |
| T-04-09 | D | `lib/crosswake/doctor/doctor.ex` | mitigate | Favor route-level transition and terminal outcome truth over noisy low-level event streams |
</threat_model>

<verification>
Run `mix test test/crosswake/offline/status_test.exs test/crosswake/offline/telemetry_test.exs test/crosswake/doctor/doctor_test.exs test/mix/tasks/crosswake_doctor_test.exs` and confirm offline posture and operator metadata are explicit in both instrumentation and doctor output.
</verification>

<success_criteria>
- Offline states are stable, route-local, and distinct.
- Offline transition and terminal-outcome instrumentation is explicit and keyed to the locked operator metadata set.
- Doctor output reflects offline contract truth directly.
- Machine-readable diagnostics are sufficient to inspect replay and conflict posture.
</success_criteria>

<output>
After completion, create `.planning/phases/04-honest-offline-contract/04-honest-offline-contract-03-SUMMARY.md`
</output>
