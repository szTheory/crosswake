---
phase: 04-honest-offline-contract
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/crosswake/policy/schema.ex
  - lib/crosswake/policy/route.ex
  - lib/crosswake/manifest/types.ex
  - lib/crosswake/manifest/builder.ex
  - test/support/router_fixtures.ex
  - test/crosswake/policy/schema_test.exs
  - test/crosswake/policy/route_test.exs
  - test/crosswake/manifest/manifest_test.exs
autonomous: true
requirements:
  - OFFL-01
  - OFFL-02
must_haves:
  truths:
    - "Crosswake distinguishes cached read-only degradation from offline-island local-first mutation in typed route and manifest contracts."
    - "Route-local offline behavior remains Phoenix-first and additive; it is not inferred from `sync`, `packs`, or rule ordering."
    - "One named cached-route contract and one named study-session island contract can be compiled into manifest truth without expanding the public runtime taxonomy."
  artifacts:
    - path: "lib/crosswake/policy/schema.ex"
      provides: "Typed route-policy options for explicit cache and island contract references"
    - path: "lib/crosswake/policy/route.ex"
      provides: "Normalized route contract carrying offline subcontracts"
    - path: "lib/crosswake/manifest/types.ex"
      provides: "Typed manifest route-entry and offline contract structs"
    - path: "lib/crosswake/manifest/builder.ex"
      provides: "Manifest compilation of route-local cache and island contract truth"
  key_links:
    - from: "lib/crosswake/policy/schema.ex"
      to: "lib/crosswake/manifest/types.ex"
      via: "validated route options compile directly into typed offline contract payloads"
      pattern: "offline|cache_contract|island_contract"
    - from: "lib/crosswake/policy/route.ex"
      to: "lib/crosswake/manifest/builder.ex"
      via: "normalized route-local contracts become manifest route entries"
      pattern: "Route|new_route_entry|routes"
---

<objective>
Define the public contract surface for honest offline behavior before adding replay or diagnostics logic.

Purpose: Fulfill the contract half of OFFL-01 and OFFL-02 by making cached-read routes and offline islands explicit, typed, and route-local.
Output: Additive route-policy fields, normalized route structs, typed manifest offline subcontracts, fixture coverage, and tests proving the distinction.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-honest-offline-contract/04-CONTEXT.md
@.planning/phases/04-honest-offline-contract/04-RESEARCH.md
@.planning/phases/04-honest-offline-contract/04-PATTERNS.md
@lib/crosswake/policy/schema.ex
@lib/crosswake/policy/route.ex
@lib/crosswake/manifest/types.ex
@lib/crosswake/manifest/builder.ex
@test/support/router_fixtures.ex

<interfaces>
From lib/crosswake/policy/schema.ex:
```elixir
@offline_values [:unavailable, :cached_read_only, :local_first]
```

From lib/crosswake/manifest/types.ex:
```elixir
defmodule RouteEntry do
  @enforce_keys [:id, :path, :runtime]
  defstruct [:id, :path, :runtime, :offline, ...]
end
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend route policy with explicit cache and island contract references</name>
  <read_first>.planning/phases/04-honest-offline-contract/04-CONTEXT.md, .planning/phases/04-honest-offline-contract/04-RESEARCH.md, .planning/phases/04-honest-offline-contract/04-PATTERNS.md, lib/crosswake/policy/schema.ex, lib/crosswake/policy/route.ex, test/support/router_fixtures.ex</read_first>
  <files>lib/crosswake/policy/schema.ex, lib/crosswake/policy/route.ex, test/support/router_fixtures.ex, test/crosswake/policy/schema_test.exs, test/crosswake/policy/route_test.exs</files>
  <behavior>
    - Test 1: Cached routes can declare an explicit cache contract without implying local mutation support.
    - Test 2: Offline-island routes can declare an explicit island contract without broadening the runtime taxonomy.
    - Test 3: Invalid combinations such as cache-only metadata on mutating routes or island-only metadata on cached-read routes fail with actionable validation.
  </behavior>
  <action>Add additive route options for explicit cache and island contract references or payloads while preserving the existing `offline` summary class and route-local validation posture. Keep the DSL small per D-05 through D-09: no giant offline enum, no separate config file, and no inferred behavior from `sync` or `packs`. Update router fixtures so one neighboring LiveView route demonstrates cached read-only posture and one study/session route demonstrates offline-island posture with the exemplar contract name.</action>
  <verify>
    <automated>mix test test/crosswake/policy/schema_test.exs test/crosswake/policy/route_test.exs</automated>
  </verify>
  <acceptance_criteria>
    <automated>rg -n 'cache_contract|island_contract|cached_read_only|local_first' lib/crosswake/policy/schema.ex lib/crosswake/policy/route.ex test/support/router_fixtures.ex test/crosswake/policy/schema_test.exs test/crosswake/policy/route_test.exs</automated>
    <automated>mix test test/crosswake/policy/schema_test.exs test/crosswake/policy/route_test.exs</automated>
  </acceptance_criteria>
  <done>Route policy now expresses honest cached-route and offline-island subcontracts explicitly.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Compile explicit offline subcontracts into manifest route truth</name>
  <read_first>.planning/phases/04-honest-offline-contract/04-CONTEXT.md, .planning/phases/04-honest-offline-contract/04-PATTERNS.md, lib/crosswake/policy/route.ex, lib/crosswake/manifest/types.ex, lib/crosswake/manifest/builder.ex, test/support/router_fixtures.ex, test/crosswake/manifest/manifest_test.exs</read_first>
  <files>lib/crosswake/manifest/types.ex, lib/crosswake/manifest/builder.ex, test/crosswake/manifest/manifest_test.exs</files>
  <behavior>
    - Test 1: Manifest route entries for cached routes expose explicit staleness/cache restriction truth rather than only `offline: :cached_read_only`.
    - Test 2: Manifest route entries for offline islands expose explicit island-contract truth rather than only `offline: :local_first`.
    - Test 3: The study/session exemplar and neighboring cached route compile into one canonical route-first manifest without changing the runtime ownership model.
  </behavior>
  <action>Extend `Crosswake.Manifest.Types` with typed offline subcontracts and thread them through `RouteEntry`, preserving the repo’s nested-module, `new_*`, and `to_map/1` style. Update `Crosswake.Manifest.Builder` to compile normalized route contract data into route entries for the cached lesson or library route and the study/session island route. Keep manifest truth route-first and explicit per D-06, D-07, and D-25 rather than deriving semantics indirectly from route ordering or generic sync metadata.</action>
  <verify>
    <automated>mix test test/crosswake/manifest/manifest_test.exs</automated>
  </verify>
  <acceptance_criteria>
    <automated>rg -n 'cache_contract|island_contract|staleness|reconciliation|routes' lib/crosswake/manifest/types.ex lib/crosswake/manifest/builder.ex test/crosswake/manifest/manifest_test.exs</automated>
    <automated>mix test test/crosswake/manifest/manifest_test.exs</automated>
  </acceptance_criteria>
  <done>The manifest now makes the cached-route and offline-island distinction explicit and machine-readable.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Phoenix route DSL -> manifest contract | Ambiguous offline configuration here would create dishonest or unsafe downstream support claims |
| cached route vs offline island contract | If these collapse into one shape, adopters cannot reason about mutation guarantees |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | T | `lib/crosswake/policy/schema.ex` | mitigate | Fail validation on ambiguous or conflicting cache/island declarations so route-level offline truth is explicit |
| T-04-02 | T | `lib/crosswake/manifest/builder.ex` | mitigate | Compile route-local subcontracts directly from normalized `%Route{}` values so manifest truth cannot drift from policy truth |
| T-04-03 | R | manifest route contract | mitigate | Keep cached and island subcontracts separate and serializable so doctor/docs can prove exactly what each route guarantees |
</threat_model>

<verification>
Run `mix test test/crosswake/policy/schema_test.exs test/crosswake/policy/route_test.exs test/crosswake/manifest/manifest_test.exs` and confirm cached and local-first routes compile into distinct, explicit contract truth.
</verification>

<success_criteria>
- Crosswake exposes typed cache and island subcontracts without changing runtime ownership rules.
- Cached read-only and offline-island routes are distinguishable in policy and manifest output.
- The study/session exemplar and neighboring cached route are encoded in shared fixtures and manifest tests.
</success_criteria>

<output>
After completion, create `.planning/phases/04-honest-offline-contract/04-honest-offline-contract-01-SUMMARY.md`
</output>
