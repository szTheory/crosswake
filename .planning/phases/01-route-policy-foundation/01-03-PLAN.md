---
phase: 01-route-policy-foundation
plan: 03
type: execute
wave: 3
depends_on:
  - 01-01
  - 01-02
files_modified:
  - lib/crosswake/policy/compiler.ex
  - lib/crosswake/policy/validator.ex
  - lib/crosswake/policy/diagnostic.ex
  - lib/crosswake/policy/error.ex
  - lib/crosswake/policy/warning.ex
  - test/support/compile_router_case.ex
  - test/crosswake/policy/compiler_test.exs
  - test/crosswake/policy/compile_error_test.exs
  - test/crosswake/policy/warning_test.exs
autonomous: true
requirements:
  - ROUTE-01
  - ROUTE-02
  - ROUTE-03
  - ROUTE-04
must_haves:
  truths:
    - "Invalid or internally inconsistent Crosswake route policy fails at compile time with route-local, actionable error messages."
    - "Phase 1 compile-time checks are strict about declarative contradictions but do not claim environment truth."
    - "Incremental adoption gaps can warn without blocking compilation."
  artifacts:
    - path: "lib/crosswake/policy/compiler.ex"
      provides: "Compile pipeline from router metadata to normalized policy set"
    - path: "lib/crosswake/policy/validator.ex"
      provides: "Semantic invariant checks for runtime, offline, capability, pack, sync, and security combinations"
    - path: "lib/crosswake/policy/diagnostic.ex"
      provides: "Aggregated compile-time error and warning formatting with file/line context"
  key_links:
    - from: "lib/crosswake/policy/compiler.ex"
      to: "lib/crosswake/policy/validator.ex"
      via: "validated normalized routes before compiler emits metadata"
      pattern: "validate"
    - from: "lib/crosswake/policy/validator.ex"
      to: "lib/crosswake/policy/diagnostic.ex"
      via: "semantic failures rendered as actionable compile diagnostics"
      pattern: "diagnostic|error|warning"
---

<objective>
Implement the strict compile-time enforcement layer for Phase 1 so contradictory route declarations fail early, with diagnostics precise enough for Phoenix developers to fix without guesswork.

Purpose: Fulfill ROUTE-04 while explicitly honoring the locked decision that Phase 1 validates local declarative truth, not shell, manifest, or environment truth.
Output: Policy compiler and validator modules, aggregated diagnostic formatting, compile-warning support for partial adoption, and negative-path tests.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-route-policy-foundation/01-CONTEXT.md
@.planning/phases/01-route-policy-foundation/01-RESEARCH.md
@.planning/phases/01-route-policy-foundation/01-01-PLAN.md
@.planning/phases/01-route-policy-foundation/01-02-PLAN.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build the route-policy compiler and semantic validator</name>
  <read_first>.planning/phases/01-route-policy-foundation/01-CONTEXT.md, .planning/phases/01-route-policy-foundation/01-01-PLAN.md, .planning/phases/01-route-policy-foundation/01-02-PLAN.md, lib/crosswake/policy/compiler.ex, lib/crosswake/policy/validator.ex, test/crosswake/policy/compiler_test.exs</read_first>
  <files>lib/crosswake/policy/compiler.ex, lib/crosswake/policy/validator.ex, test/crosswake/policy/compiler_test.exs</files>
  <behavior>
    - Test 1: Valid routes across all three public runtimes compile from router metadata into normalized policy output.
    - Test 2: Duplicate route ids, missing required metadata, and invalid enum values fail compilation.
    - Test 3: Semantic contradictions fail, while environment-dependent checks are intentionally absent per D-14 and D-15.
  </behavior>
  <action>Write the initial compiler test coverage in this task, then implement the pipeline that walks Crosswake-managed Phoenix routes, normalizes merged metadata, and enforces semantic invariants on top of the Plan 01 schemas. Include contradictions such as incompatible offline/security combinations, invalid capability/pack/sync declarations, and duplicate `id` values. Explicitly reject `runtime: :adapter` with a diagnostic that explains it is reserved for later extension per D-05/D-06, which compensates for the older roadmap wording without reopening the decision.</action>
  <verify>
    <automated>mix test test/crosswake/policy/compiler_test.exs</automated>
  </verify>
  <acceptance_criteria>
    <automated>test -f lib/crosswake/policy/compiler.ex && test -f lib/crosswake/policy/validator.ex && test -f test/crosswake/policy/compiler_test.exs</automated>
    <automated>rg -n 'duplicate|adapter|offline_island|native_screen|live_view' test/crosswake/policy/compiler_test.exs</automated>
    <automated>mix test test/crosswake/policy/compiler_test.exs</automated>
  </acceptance_criteria>
  <done>Crosswake can compile valid router policy and halt compilation on local declarative contradictions with no shell or filesystem dependencies.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Format aggregated compile-time diagnostics with fix hints</name>
  <read_first>.planning/phases/01-route-policy-foundation/01-CONTEXT.md, lib/crosswake/policy/compiler.ex, lib/crosswake/policy/validator.ex, lib/crosswake/policy/diagnostic.ex, lib/crosswake/policy/error.ex, test/crosswake/policy/compile_error_test.exs</read_first>
  <files>lib/crosswake/policy/diagnostic.ex, lib/crosswake/policy/error.ex, test/crosswake/policy/compile_error_test.exs</files>
  <behavior>
    - Test 1: Compile failures include route path/helper context plus file and line information when available.
    - Test 2: Diagnostics include offending keys, the reason for failure, and a minimal fix hint per D-17.
    - Test 3: Multiple invalid routes aggregate into one compile failure payload instead of failing only on the first issue.
  </behavior>
  <action>Create diagnostic structs and formatting helpers that aggregate policy errors by router/policy module. Keep the messaging Phoenix/Ecto-like rather than DSL-compiler jargon. The `:adapter` rejection message must explicitly say the public Phase 1 runtime taxonomy is `:live_view | :offline_island | :native_screen`, and that adapter remains reserved for future extension.</action>
  <verify>
    <automated>mix test test/crosswake/policy/compile_error_test.exs</automated>
  </verify>
  <acceptance_criteria>
    <automated>test -f lib/crosswake/policy/diagnostic.ex && test -f lib/crosswake/policy/error.ex && test -f test/crosswake/policy/compile_error_test.exs</automated>
    <automated>rg -n 'fix hint|offline_island|native_screen|live_view|adapter' lib/crosswake/policy/diagnostic.ex test/crosswake/policy/compile_error_test.exs</automated>
    <automated>mix test test/crosswake/policy/compile_error_test.exs</automated>
  </acceptance_criteria>
  <done>Compile-time failures point directly to the conflicting declaration and state the smallest corrective action needed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Emit non-blocking adoption warnings for uncovered routes</name>
  <read_first>.planning/phases/01-route-policy-foundation/01-CONTEXT.md, lib/crosswake/policy/compiler.ex, lib/crosswake/policy/validator.ex, lib/crosswake/policy/warning.ex, test/support/compile_router_case.ex, test/crosswake/policy/warning_test.exs</read_first>
  <files>lib/crosswake/policy/warning.ex, test/support/compile_router_case.ex, test/crosswake/policy/warning_test.exs</files>
  <behavior>
    - Test 1: Routers with unmanaged routes can emit an adoption warning without failing compilation per D-16.
    - Test 2: Fully managed routers compile cleanly with no warnings.
    - Test 3: Warning output is clearly distinguished from hard validation failures.
  </behavior>
  <action>Add the warning pathway for partial router coverage so Phase 1 supports incremental adoption honestly. Keep the warning limited to local declarative coverage gaps; do not broaden it into support-matrix or environment diagnostics, which belong to later doctor/manifests phases.</action>
  <verify>
    <automated>mix test test/crosswake/policy/warning_test.exs</automated>
  </verify>
  <acceptance_criteria>
    <automated>test -f lib/crosswake/policy/warning.ex && test -f test/support/compile_router_case.ex && test -f test/crosswake/policy/warning_test.exs</automated>
    <automated>rg -n 'warning|uncovered|incremental adoption' lib/crosswake/policy/warning.ex test/crosswake/policy/warning_test.exs</automated>
    <automated>mix test test/crosswake/policy/warning_test.exs</automated>
  </acceptance_criteria>
  <done>Crosswake distinguishes between hard compile failures and advisory adoption warnings without weakening validation rigor.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Phoenix router metadata -> compile-time validator | Route declarations become trusted product contract inputs for later phases |
| Validation result -> developer remediation | Poor diagnostics can hide insecure or unsupported declarations behind ambiguous failures |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-07 | T | `lib/crosswake/policy/validator.ex` | mitigate | Enforce semantic invariants on normalized route policy and reject contradictory declarations before later phases consume them |
| T-01-08 | R | `lib/crosswake/policy/diagnostic.ex` | mitigate | Include route/module/file/line and offending keys in errors so authors cannot plausibly deny the source of invalid declarations |
| T-01-09 | I | warning and error surfaces | accept | Phase 1 diagnostics reveal only local declarative truth, which is acceptable because they do not expose secrets or environment data |
</threat_model>

<verification>
Run `mix test test/crosswake/policy/compiler_test.exs test/crosswake/policy/compile_error_test.exs test/crosswake/policy/warning_test.exs` and confirm diagnostics distinguish reserved-runtime, contradiction, duplicate-id, and warning-only cases.
</verification>

<success_criteria>
- Compile-time validation catches missing metadata, unknown values, duplicate identifiers, and semantic contradictions.
- Error messages identify the route declaration and conflicting keys, and include a minimal fix hint.
- The roadmap/context mismatch around `adapter` is resolved by explicit diagnostics and tests that reserve it for future extension instead of exposing it as a Phase 1 runtime.
</success_criteria>

<output>
After completion, create `.planning/phases/01-route-policy-foundation/01-route-policy-foundation-03-SUMMARY.md`
</output>
