---
phase: 59-chimeway-contract-and-token-binding-semantics
plan: "03"
title: "Chimeway Phase Proof And Narrow Guide Anchor"
type: execute
wave: 3
depends_on: [59-01, 59-02]
status: pending
requirements: [TOKN-01, TOKN-02]
requirements_addressed: [TOKN-01, TOKN-02]
autonomous: true
files_modified:
  - test/crosswake/proof/phase59_chimeway_contract_test.exs
  - guides/companions.md
  - test/crosswake/guides/companions_test.exs
must_haves:
  truths:
    - "TOKN-01: bridge token evidence and backend-owned token binding remain separate and proven."
    - "TOKN-02: all required active/rotated/revoked/stale/invalid/permission-denied/environment-mismatched/app-identity-mismatched semantics are proven."
    - "D-30/D-37: raw token strings stay absent from fixtures, docs, proof output, and support-facing maps."
    - "Phase 59 does not promote delivery, notification-open routing, provider credentials, or topic APIs."
  artifacts:
    - "test/crosswake/proof/phase59_chimeway_contract_test.exs enforces lifecycle, redaction, provider-neutral vocabulary, and non-claim boundaries."
    - "guides/companions.md adds a narrow Chimeway contract anchor without pulling Phase 60-62 documentation scope forward."
    - "test/crosswake/guides/companions_test.exs locks Chimeway wording around token_ref, token_fingerprint, backend-owned binding, and deferred delivery/open claims."
  key_links:
    - "phase59 proof reads Crosswake.SupportMatrix.notification_support_truth/0 and keeps provider snapshot wording unchanged."
    - "guide text references Crosswake.Companions.Chimeway, TokenEvidence, TokenBinding, ProviderFeedback, BindingEvent, and BindingResult as contract surfaces only."
    - "proof tests assert :delivery_accepted remains provider handoff evidence only."
---

# Plan 59-03: Chimeway Phase Proof And Narrow Guide Anchor

<objective>
Add the merge-blocking phase proof and the minimal companion-guide anchor that make Phase 59’s contract scope, redaction guarantees, and non-claims mechanically enforceable.
</objective>

<context>
This plan should not widen into Phase 60 registry flows, Phase 61 route-open behavior, or Phase 62 broad diagnostics/support/docs work. The only public doc change in scope is a narrow Chimeway anchor that describes the shipped contract family and the explicit non-claims.
</context>

<tasks>
  <task id="59-03-01" type="execute">
    <name>Lock the Phase 59 proof surface for lifecycle semantics, redaction, and non-claims</name>
    <files>
      test/crosswake/proof/phase59_chimeway_contract_test.exs
    </files>
    <read_first>
      AGENTS.md
      .planning/PROJECT.md
      .planning/ROADMAP.md
      .planning/phases/59-chimeway-contract-and-token-binding-semantics/59-CONTEXT.md
      .planning/phases/59-chimeway-contract-and-token-binding-semantics/59-RESEARCH.md
      .planning/phases/59-chimeway-contract-and-token-binding-semantics/59-VALIDATION.md
      lib/crosswake/companions/chimeway.ex
      lib/crosswake/companions/chimeway/contracts.ex
      lib/crosswake/companions/chimeway/redaction.ex
      lib/crosswake/companions/chimeway/telemetry.ex
      lib/crosswake/support_matrix/support_matrix.ex
      test/crosswake/proof/phase59_chimeway_contract_test.exs
    </read_first>
    <action>Create `test/crosswake/proof/phase59_chimeway_contract_test.exs` as the merge-blocking contract proof for Phase 59. The proof must assert the exact TOKN-02 mapping table used in `59-01-02`; assert that `raw_apns_token_should_not_leak_123` is absent from `inspect/1`, `Contracts.to_map/1`, and `Crosswake.Companions.Chimeway.Telemetry.metadata/1`; assert that `Crosswake.Companions.Chimeway.report_state().details` still publish `delivery_support: :not_shipped` and `open_routing: :not_shipped`; assert that `Crosswake.SupportMatrix.notification_support_truth/0` still exposes the provider-snapshot row with deferred `:chimeway_delivery` and `:notification_open_routing`; assert that public Chimeway contracts do not define `:token` or other raw token aliases; and assert that `:delivery_accepted` is treated as provider handoff evidence only, not delivered/opened/route-activated truth.</action>
    <verify>
      <automated>mix test test/crosswake/proof/phase59_chimeway_contract_test.exs --trace</automated>
    </verify>
    <done>The phase proof blocks any raw-token leakage, lifecycle drift, widened notification support claim, or delivery/open overclaim in public contract surfaces.</done>
    <acceptance_criteria>
      - `mix test test/crosswake/proof/phase59_chimeway_contract_test.exs --trace` passes.
      - The proof file contains an explicit assertion for each TOKN-02 semantic, not a partial or inferred coverage check.
      - The proof reads `Crosswake.SupportMatrix.notification_support_truth/0` and fails if the notification row stops being provider-snapshot only.
      - The proof fails if `raw_apns_token_should_not_leak_123` appears in any inspected, serialized, or telemetry-sanitized output.
      - The proof fails if any public contract struct defines `:token`, `:raw_token`, `:device_token`, `:registration_token`, `:apns_token`, or `:fcm_token`.
    </acceptance_criteria>
  </task>

  <task id="59-03-02" type="execute">
    <name>Add the narrow Chimeway guide anchor and lock its non-claims</name>
    <files>
      guides/companions.md
      test/crosswake/guides/companions_test.exs
    </files>
    <read_first>
      .planning/phases/59-chimeway-contract-and-token-binding-semantics/59-CONTEXT.md
      .planning/phases/59-chimeway-contract-and-token-binding-semantics/59-RESEARCH.md
      guides/companions.md
      test/crosswake/guides/companions_test.exs
      test/crosswake/proof/phase59_chimeway_contract_test.exs
    </read_first>
    <action>Update `guides/companions.md` with one narrow Chimeway anchor that describes the shipped contract surface as `Crosswake.Companions.Chimeway`, `TokenEvidence`, `TokenBinding`, `ProviderFeedback`, `BindingEvent`, `BindingResult`, `token_ref`, and `token_fingerprint`, and that states plainly that `notifications.token.get` plus provider feedback are evidence only while backend-owned binding records remain authoritative. The same guide anchor must explicitly say that APNs/FCM delivery, provider credentials, notification-open routing, RouteGate activation via notification, topic APIs, and tray behavior are not shipped in Phase 59. Update `test/crosswake/guides/companions_test.exs` so the doc contract requires `token_ref`, `token_fingerprint`, evidence-only wording, backend-owned binding wording, and the explicit non-claims listed above, while also rejecting any raw public `token` field wording in the guide.</action>
    <verify>
      <automated>mix test test/crosswake/guides/companions_test.exs test/crosswake/proof/phase59_chimeway_contract_test.exs --trace</automated>
    </verify>
    <done>The companion guide contains a narrow Chimeway contract anchor with explicit evidence-only wording, explicit non-claims, and no raw-token or delivery/open overclaim language.</done>
    <acceptance_criteria>
      - `mix test test/crosswake/guides/companions_test.exs test/crosswake/proof/phase59_chimeway_contract_test.exs --trace` passes.
      - `guides/companions.md` names `token_ref` and `token_fingerprint` explicitly.
      - `guides/companions.md` states that `notifications.token.get` and provider feedback are evidence only and that backend-owned binding records remain authoritative.
      - `guides/companions.md` explicitly says APNs/FCM delivery, provider credentials, notification-open routing, RouteGate notification activation, topic APIs, and tray behavior are not shipped in Phase 59.
      - The guide text does not describe any normal public raw `token` field on Chimeway structs.
    </acceptance_criteria>
  </task>
</tasks>

<threat_model>
| ID | STRIDE | Task | Asset | Threat | Control |
|----|--------|------|-------|--------|---------|
| T-59-03A | Information Disclosure | 59-03-01, 59-03-02 | Proof/docs surfaces | Raw tokens or unsafe support data leak through proof fixtures, guide text, or serialized examples. | Seeded-token proof assertions plus guide tests that require token_ref/token_fingerprint wording instead of raw token fields. |
| T-59-03B | Elevation of Privilege | 59-03-01 | Support truth | Evidence, telemetry, or provider handoff events are misread as auth/session/route/delivery authority. | Proof asserts evidence-only wording, backend-owned binding authority, and non-promotional companion state/support-matrix posture. |
| T-59-03C | Spoofing | 59-03-02 | Public docs | The new guide anchor reads like APNs/FCM delivery or notification-open routing support. | Docs-contract tests require explicit non-claims for delivery, open routing, credentials, topics, and tray behavior. |
| T-59-03D | Tampering | 59-03-01 | Existing notification support row | Phase 59 silently widens `notification_support_truth/0` beyond provider snapshot posture. | Proof reads the exported support row directly and fails on widened delivery/open claims. |
</threat_model>

<verification>
Run:

```sh
mix test test/crosswake/proof/phase59_chimeway_contract_test.exs test/crosswake/guides/companions_test.exs --trace
```
</verification>

<success_criteria>
- Phase 59 has a merge-blocking proof file that covers lifecycle semantics, redaction, support-truth boundaries, and provider-neutral vocabulary.
- `guides/companions.md` has a minimal Chimeway contract anchor without pulling Phase 60-62 documentation scope into Phase 59.
- The proof and guide tests fail if raw tokens leak or if delivery/open support is overclaimed.
</success_criteria>

<must_haves>
- Proof explicitly enforces TOKN-01 and TOKN-02 rather than inferring them from unit tests alone.
- Existing provider-snapshot notification support truth remains intact until Phase 62.
- The guide tells readers exactly what shipped and exactly what did not.
- Phase 59 finishes with contract-proof truth, not aspirational delivery claims.
</must_haves>
