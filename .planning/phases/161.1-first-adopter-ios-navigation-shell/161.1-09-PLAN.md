---
phase: 161.1-first-adopter-ios-navigation-shell
plan: "09"
type: execute
wave: 8
depends_on: ["161.1-08"]
files_modified:
  - lib/crosswake/proof_lane/navigation_shell_advisory.ex
  - lib/crosswake/proof_lane/evidence.ex
  - lib/mix/tasks/crosswake.proof_lane.verify_navigation_shell.ex
  - test/crosswake/proof_lane/navigation_shell_advisory_test.exs
  - test/mix/tasks/crosswake_proof_lane_verify_navigation_shell_test.exs
  - scripts/verify_phase_161_1.sh
  - .planning/phases/161.1-first-adopter-ios-navigation-shell/161.1-VALIDATION.md
autonomous: true
requirements: [NAV-01, NAV-02, NAV-03, NAV-04, NAV-05, NAV-06, NAV-07]
must_haves:
  truths:
    - "One runnable same-tree command exercises topology, Phoenix/web/WebKit transition delivery, UIKit/layout/accessibility contracts, generated proof, support truth, and the Phoenix-confirm fallback before retaining any Phase 161.1 result (D-01-D-12; NAV-01-NAV-07)."
    - "The retained result is built from a closed allowlisted advisory object whose subjects are current repository artifacts identified by opaque fixed IDs and SHA-256 digests, never URLs, route payloads/history, identity, screenshots, transcripts, or raw output (D-04, D-10, D-11)."
    - "Canonical retained bytes are compared exactly, scanned through the existing Evidence boundary, and checked against the same subject bytes both before publication and after re-reading the final tree; any subject change fails closed (D-06, D-11)."
    - "Simulator/XCUITest results remain advisory and can neither assert physical-iPhone/adopter-instance success nor widen Android or generic-navigation support; Phase 162 alone owns device promotion (D-11, D-12)."
  artifacts:
    - path: "lib/crosswake/proof_lane/navigation_shell_advisory.ex"
      provides: "Exact-key advisory schema, fixed current-artifact subject allowlist, canonical encoder, privacy scan, and subject digest builder."
    - path: "lib/mix/tasks/crosswake.proof_lane.verify_navigation_shell.ex"
      provides: "Before/after subject rebuild, canonical retained-byte comparison, Evidence scan/check, and closed final outcome."
    - path: "scripts/verify_phase_161_1.sh"
      provides: "Single non-vacuous Phase 161.1 same-tree validation/evidence command."
    - path: ".planning/phases/161.1-first-adopter-ios-navigation-shell/161.1-VALIDATION.md"
      provides: "Existing validation contract reconciled with task commands and observed final-gate results; it is updated, never created by this plan."
  key_links:
    - from: "scripts/verify_phase_161_1.sh"
      to: "Mix.Tasks.Crosswake.ProofLane.VerifyNavigationShell"
      via: "all required suites succeed before the task is invoked with an explicit safe destination"
      pattern: "verify_navigation_shell|--destination"
    - from: "Crosswake.ProofLane.NavigationShellAdvisory"
      to: "Crosswake.ProofLane.Evidence"
      via: "canonical advisory bytes supplied as the approved navigation-shell subject kind"
      pattern: "canonical_bytes|navigation_shell_advisory|approved_hashes"
    - from: "pre-publication subject digest"
      to: "post-publication subject digest"
      via: "re-read every fixed allowlisted artifact and constant-time compare before Evidence.check/2"
      pattern: "before_subject_digest|after_subject_digest|hash_equals"
    - from: "retained proof-lane-evidence.json"
      to: "canonical Evidence.to_map bytes"
      via: "byte-for-byte equality plus Evidence.scan_stage/1 and Evidence.check/2"
      pattern: "proof-lane-evidence.json|scan_stage|check"
  prohibitions:
    - "requirement_id: NAV-01 | category: values | statement: MUST NOT turn the bounded first-adopter topology or coordinator into a generic navigation framework, generic WebView wrapper, browser-history router, or second host route authority. | status: unresolved | verification: flagged-unverified"
    - "requirement_id: NAV-03 | category: privacy | statement: MUST NOT carry or retain route payloads, account or device identifiers, credentials, tokens, browser history, URLs, or stable identity across navigation, layout, diagnostics, or proof seams. | status: unresolved | verification: flagged-unverified"
    - "requirement_id: NAV-06 | category: transparency | statement: MUST NOT present synthetic fixtures, package tests, simulator runs, or generated-host results as adopter-instance, physical-iPhone, Android, or generic-navigation proof. | status: unresolved | verification: flagged-unverified"
    - "requirement_id: NAV-07 | category: values | statement: MUST NOT add native alert or confirm, or describe Phoenix-owned confirmation as optional, before physical-iPhone proof, a demonstrated active-adopter blocker, and an explicit maintainer roadmap decision. | status: unresolved | verification: flagged-unverified"
flagged_assumptions:
  - "[UNRESOLVED-SPEC-PROBE][NAV-01 unclassified] CONTEXT D-01 through D-03 and REQUIREMENTS.md are exhaustive; no additional topology field, real route fact, or ownership transfer is inferred."
  - "[UNRESOLVED-SPEC-PROBE][NAV-02 adjacency] Only compiled root/parent relationships may be traversed; no arbitrary sibling, cross-tab, URL, or browser-history adjacency is inferred."
  - "[UNRESOLVED-SPEC-PROBE][NAV-02 empty input] Zero eligible validated rows remains unknown_blocking and produces no native tab or promoted shell claim."
  - "[UNRESOLVED-SPEC-PROBE][NAV-02 ordering] No user-facing tab ordering semantics are supplied; synthetic vectors may be deterministic, but adopter order remains unknown_blocking until sanitized input explicitly authorizes it."
  - "[UNRESOLVED-SPEC-PROBE][NAV-03 unclassified] D-04 through D-06 define the complete transition vocabulary; no third transition kind or browser-history state is inferred."
  - "[UNRESOLVED-SPEC-PROBE][NAV-04 unclassified] D-08 defines exactly four safe-area variables and one separate keyboard variable; no additional layout or identity fact is inferred."
  - "[UNRESOLVED-SPEC-PROBE][NAV-05 unclassified] D-08 defines a non-identifying document-start marker only; no custom user agent or host/account/device marker is inferred."
  - "[UNRESOLVED-SPEC-PROBE][NAV-06 unclassified] D-10 and D-11 define deterministic contract proof plus advisory simulator proof; physical-device and Android promotion remain excluded."
  - "[UNRESOLVED-SPEC-PROBE][NAV-07 unclassified] D-12 and the governing ADR define Phoenix-owned confirmation and the entire reversal condition; no native alert/confirm work is inferred."
---

<objective>
Run and retain one privacy-safe, same-tree Phase 161.1 validation gate after support/fallback reconciliation.

Purpose: Prevent stale, vacuous, or simulator-laundered evidence from promoting the bounded shell while binding the retained result to the exact final-tree artifacts it evaluated.
Output: Closed advisory builder, digest-bound evidence task, one-command gate, negative regressions, and reconciled existing validation evidence.
</objective>

<artifacts_this_phase_produces>
- New `Crosswake.ProofLane.NavigationShellAdvisory` with an exact versioned schema and a fixed allowlist of opaque subject IDs for topology/transition vectors, production WebKit registration, UIKit/layout tests, generated proof templates, support truth, and Phoenix fallback truth.
- New approved Evidence source kind `:navigation_shell_advisory`, scanned only by the advisory module's canonical decoder; no hash-anything or arbitrary path API.
- New `mix crosswake.proof_lane.verify_navigation_shell --destination PATH` that builds pre-publication canonical subject bytes, promotes one closed Evidence object, re-reads current subjects, compares pre/post digests, compares retained bytes to canonical bytes, scans the retained stage, and calls `Evidence.check/2` with post-read sources.
- New `scripts/verify_phase_161_1.sh DESTINATION` that runs the complete required focused suites and then the digest-bound Mix task; simulator/XCUITest records are labeled advisory and never device promotion.
- Updated existing `161.1-VALIDATION.md` task map and observed final result; the validation file is not created by an execution task.
- No raw test output, URL, route payload/history, account/device/token/stable ID, screenshot, transcript, `.xcresult`, Android work, device CI, physical-device claim, or native confirmation surface.
</artifacts_this_phase_produces>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/ADR-FIRST-B2C-ADOPTER.md
@.planning/phases/161.1-first-adopter-ios-navigation-shell/161.1-CONTEXT.md
@.planning/phases/161.1-first-adopter-ios-navigation-shell/161.1-RESEARCH.md
@.planning/phases/161.1-first-adopter-ios-navigation-shell/161.1-08-SUMMARY.md
@.planning/phases/161.1-first-adopter-ios-navigation-shell/161.1-VALIDATION.md
@lib/crosswake/proof_lane/evidence.ex
@lib/crosswake/proof_lane/native_promotion.ex
@test/crosswake/proof_lane/evidence_test.exs

<interfaces>
- Reuse `Evidence.build/1`, `Evidence.promote/3`, `Evidence.scan_stage/1`, and `Evidence.check/2`; extend its approved-source dispatch only for the new exact canonical advisory type.
- The fixed subject allowlist names code-local artifacts with opaque enum IDs in retained data. Filesystem paths stay implementation-local and cannot be caller supplied.
- The gate reads subjects before publication and again afterward. A hook-driven mutation regression must prove a digest mismatch blocks success even when every earlier test exited zero.
- Xcode simulator execution is required contract evidence for this phase but its recorded proof class is advisory. It cannot set device-verified, adopter-instance, Android, or generic support truth.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build and retain a closed digest-bound advisory object</name>
  <reversibility rating="reversible">The advisory schema is phase-local and additive to the existing retained evidence source-kind allowlist.</reversibility>
  <read_first>
    - .planning/phases/161.1-first-adopter-ios-navigation-shell/161.1-CONTEXT.md
    - lib/crosswake/proof_lane/evidence.ex
    - lib/crosswake/proof_lane/native_promotion.ex
    - test/crosswake/proof_lane/evidence_test.exs
    - packages/crosswake-shell-core-ios/Tests/CrosswakeShellCoreTests/Resources/navigation_transition_vectors.json
    - priv/templates/crosswake/proof_lane/ios/CrosswakeProofLaneUITests/ProofLaneUITests.swift.eex
    - lib/crosswake/capability_map.ex
    - priv/templates/crosswake/native_controls_ui/crosswake_fallbacks.ex.eex
  </read_first>
  <files>lib/crosswake/proof_lane/navigation_shell_advisory.ex, lib/crosswake/proof_lane/evidence.ex, lib/mix/tasks/crosswake.proof_lane.verify_navigation_shell.ex, test/crosswake/proof_lane/navigation_shell_advisory_test.exs, test/mix/tasks/crosswake_proof_lane_verify_navigation_shell_test.exs</files>
  <behavior>
    - "RED: the builder accepts only schema_version, phase_id, proof_class=advisory, closed assertion ID/outcome pairs, and the complete fixed subject-ID digest map; missing/extra keys, unknown IDs/outcomes, caller paths, or sensitive values fail non-echoingly."
    - "RED: canonical bytes decode and re-encode byte-identically; reordered, whitespace-changed, malformed, or sensitive bytes fail the source scanner."
    - "RED: the Mix task hashes canonical advisory bytes before promotion, publishes one canonical Evidence artifact, rebuilds subjects from current files, constant-time compares before/after SHA-256, then requires retained-byte equality, scan_stage/1, and check/2 against post-read bytes."
    - "RED: subject mutation at the post-publish barrier, altered retained bytes/marker, extra retained files, absent current artifacts, and forged physical/device/Android/generic claims all fail without a completion outcome."
  </behavior>
  <action>Create `Crosswake.ProofLane.NavigationShellAdvisory` per D-10/D-11 with explicit structs/to_map decoding and exact canonical JSON, not a generic map or arbitrary-path collector. Hard-code an exhaustive private subject table mapping opaque IDs to the current topology/transition vector files, production `LiveViewContainerViewController` registration source, host `NavigationShellTests`, generated contract/UI templates, capability/support canonical sources and guides, and Phoenix fallback generator/template. Require regular files, hash their bytes with SHA-256, retain only the opaque subject IDs/digests and closed assertion outcomes, and scan keys plus values for the existing sensitive/collateral classes. Extend Evidence with only `:navigation_shell_advisory` and dispatch its source scan to this module; preserve the existing evidence-json behavior. Implement the Mix task so the destination is explicit and safely contained, pre-subject canonical bytes are passed to `Evidence.promote/3`, all subjects are re-read after publication, pre/post digests use constant-time equality, retained JSON is compared byte-for-byte with `Jason.encode!(Evidence.to_map(evidence))`, and `Evidence.scan_stage/1` plus `Evidence.check/2` both pass using the post-read canonical source. Add deterministic hooks for mutation tests. Output only one closed advisory outcome/rule ID and digest prefixes are not printed. No simulator result may select physical/device/adopter/Android/generic support.</action>
  <acceptance_criteria>
    - The advisory schema and subject table are exact, closed, code-owned, and contain no caller-supplied paths or sensitive/collateral fields.
    - Retained bytes equal the canonical Evidence bytes and pass both recursive stage scan and approved-source check.
    - The same current artifact set is digest-bound before and after publication; any changed, missing, replaced, or non-regular subject fails closed.
    - Negative tests demonstrate that canonicality, privacy, retained-stage integrity, and simulator non-promotion checks are non-vacuous.
  </acceptance_criteria>
  <verify><automated>mix test test/crosswake/proof_lane/navigation_shell_advisory_test.exs test/mix/tasks/crosswake_proof_lane_verify_navigation_shell_test.exs test/crosswake/proof_lane/evidence_test.exs</automated></verify>
  <done>Current Phase 161.1 artifacts can produce exactly one canonical, scanned, before/after digest-bound advisory evidence object and no wider claim.</done>
</task>

<task type="auto">
  <name>Task 2: Run the final same-tree command and reconcile existing validation</name>
  <reversibility rating="reversible">The script and validation ledger record reproducible final-tree evidence without changing runtime contracts.</reversibility>
  <read_first>
    - .planning/phases/161.1-first-adopter-ios-navigation-shell/161.1-CONTEXT.md
    - .planning/phases/161.1-first-adopter-ios-navigation-shell/161.1-VALIDATION.md
    - .planning/phases/161.1-first-adopter-ios-navigation-shell/161.1-08-SUMMARY.md
    - lib/mix/tasks/crosswake.proof_lane.verify_navigation_shell.ex
    - test/crosswake/proof_lane/navigation_shell_advisory_test.exs
    - test/crosswake/proof_lane/ios_verifier_test.exs
  </read_first>
  <files>scripts/verify_phase_161_1.sh, .planning/phases/161.1-first-adopter-ios-navigation-shell/161.1-VALIDATION.md</files>
  <action>Add an executable fail-fast script that requires one explicit destination argument and runs, on the same tree: the Phase 161.1 topology/manifest ExUnit tests; Phoenix transition and Node hook tests; full Swift topology/transition/coordinator tests; iOS host build and the named `NavigationShellTests`; generated proof-lane generator/template/verifier/evidence suites; capability/support renderer drift and Phoenix fallback tests; the new advisory/evidence negative suite; `mix format --check-formatted` over changed Elixir files; and `git diff --check`. Invoke `mix crosswake.proof_lane.verify_navigation_shell --destination "$1"` only after every preceding command succeeds. The script must classify simulator/XCUITest output as advisory and must not translate it into physical-device, adopter-instance, Android, or generic support. After the one-command gate passes, edit the already-existing `161.1-VALIDATION.md`: reconcile every 161.1-01 through 161.1-09 task to its automated command, record only closed assertion IDs/outcomes and safe aggregate pass counts, name the retained evidence destination/digest binding without raw bytes, and leave TODO-002/adopter topology `unknown_blocking` plus physical-iPhone promotion assigned to Phase 162. Do not create or replace VALIDATION.md and do not claim independent security approval.</action>
  <acceptance_criteria>
    - `scripts/verify_phase_161_1.sh DESTINATION` is executable, non-vacuous, fail-fast, and invokes the digest-bound Mix task only after every named suite passes.
    - The script's result remains advisory for simulator/XCUITest and cannot promote physical-device, adopter-instance, Android, or generic-navigation truth.
    - The existing validation file maps all plan tasks to runnable commands and records only observed, privacy-safe final-tree outcomes.
    - TODO-002, real adopter topology, and Phase 162 physical-iPhone evidence remain explicitly non-passing or unknown_blocking.
  </acceptance_criteria>
  <verify><automated>phase161_stage="$(mktemp -d "${TMPDIR:-/tmp}/crosswake-161-1.XXXXXX")" &amp;&amp; bash scripts/verify_phase_161_1.sh "$phase161_stage/retained"</automated></verify>
  <done>One reproducible command proves the final tree, retains a closed digest-bound advisory artifact, and reconciles the existing validation ledger without laundering simulator evidence.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| current repository subjects -> advisory builder | Files may be stale, missing, replaced, or contain forbidden collateral. |
| canonical advisory bytes -> retained Evidence | Only one approved subject kind and exact canonical bytes may cross. |
| publication -> post-read check | Subjects or retained bytes may change between initial validation and completion. |
| simulator/XCUITest -> support truth | Advisory platform evidence must not become physical-device or generic support authority. |

## STRIDE Threat Register — ASVS L1, block on high

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-161.1-01 | Tampering | final topology/host subjects | high | mitigate | Tasks 1-2 use a fixed subject allowlist, current-file hashes, and same-tree complete tests. |
| T-161.1-02 | Tampering / Repudiation | retained evidence | high | mitigate | Task 1 enforces canonical byte equality, recursive scan, complete marker, and approved-source check. |
| T-161.1-03 | Elevation of Privilege | digest time-of-check/time-of-use | high | mitigate | Task 1 re-reads every subject and constant-time compares before/after digests before completion. |
| T-161.1-04 | Information Disclosure | advisory/retained fields | high | mitigate | Task 1 retains only closed IDs, outcomes, and full digests; sensitive/collateral values fail non-echoingly. |
| T-161.1-05 | Information Disclosure / Repudiation | raw test/simulator output | high | mitigate | Task 2 retains only closed aggregate outcomes and forbids raw output, screenshots, `.xcresult`, paths, and identifiers. |
| T-161.1-06 | Spoofing / Elevation of Privilege | simulator promotion | high | mitigate | Tasks 1-2 fix proof_class to advisory and keep Phase 162/Android/generic non-claims explicit. |
| T-161.1-SC | Tampering | dependency supply chain | low | accept | No dependency/package/workflow install occurs. |
</threat_model>

<source_coverage_audit>
| Source | ID | Feature / requirement | Plans | Status | Notes |
|---|---|---|---|---|---|
| GOAL | — | Native-feeling first-adopter iOS roots/details without generic navigation or leaf-owner change. | 01-09 | COVERED | Plan 09 executes and digest-binds the complete final-tree result. |
| REQ | NAV-01 | Compiled stable topology and leaf ownership. | 01,02,09 | COVERED | Canonical graph tests and final allowlisted subject. |
| REQ | NAV-02 | Native tabs/push/back with resolver authority. | 01,04,05,09 | COVERED | Coordinator/host tests and final gate. |
| REQ | NAV-03 | Typed/versioned fail-closed patch/navigate synchronization. | 01,03,04,09 | COVERED | Production Phoenix/WebKit path, dedupe/patch vectors, final gate. |
| REQ | NAV-04 | Four live safe-area vars and separate keyboard. | 05,09 | COVERED | Host tests and digest-bound subject. |
| REQ | NAV-05 | Synchronous non-identifying marker. | 05,09 | COVERED | Host tests and digest-bound subject. |
| REQ | NAV-06 | Executable bounded proof, no Android/general claim. | 06-09 | COVERED | Generated proof, canonical/rendered support truth, one-command advisory evidence. |
| REQ | NAV-07 | Phoenix confirmation fallback and reversal condition. | 07-09 | COVERED | Generated/canonical/rendered drift tests included in final command. |
| RESEARCH | — | Compiled topology, production transition seam, UIKit/layout, Phase 159 proof/evidence. | 01-09 | COVERED | Plan 09 binds the exact final artifacts without new packages. |
| CONTEXT | D-01-D-03 | Compiled bounded graph, unknown_blocking, resolver-authorized state. | 01-05,09 | COVERED | Final gate preserves blocker and re-runs graph/state contracts. |
| CONTEXT | D-04-D-06 | Closed production transition seam, exact effects, explicit denial/fallback. | 03,04,07-09 | COVERED | Final command covers Phoenix/web/WebKit/native delivery and fallback drift. |
| CONTEXT | D-07-D-09 | UIKit shell, marker/insets, system UX/accessibility. | 05,09 | COVERED | Required host build/tests remain advisory platform evidence. |
| CONTEXT | D-10-D-12 | Generated proof, privacy/promotion boundary, support truth. | 06-09 | COVERED | Canonical scanned retention and no-promotion checks. |
| CONTEXT | Deferred ideas | Generic/cross-platform navigation, Android, renderer, modal breadth, telemetry, device CI, native confirm. | — | EXCLUDED | Explicitly deferred/stopped. |
</source_coverage_audit>

<verification>
Run the exact Task 2 script command against a fresh explicit destination. Completion requires all deterministic suites, the advisory host test, canonical retained-byte equality, recursive scanning, approved-source checking, and equal before/after subject digests. No manual UAT or human checkpoint is permitted.
</verification>

<success_criteria>
- The final-tree gate is one runnable command and fails when any required test, subject, canonical byte, scanner, or digest-binding control fails.
- Retained evidence contains only closed advisory IDs/outcomes and approved subject digests.
- Simulator/XCUITest remains advisory; Phase 162 alone may promote physical-iPhone evidence.
- Existing validation is reconciled, while TODO-002 and adopter-instance facts remain unknown_blocking.
</success_criteria>

<output>
Create `.planning/phases/161.1-first-adopter-ios-navigation-shell/161.1-09-SUMMARY.md` when done.
</output>
