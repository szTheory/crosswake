---
phase: 161.1-first-adopter-ios-navigation-shell
plan: "10"
subsystem: ios-reference-host
tags: [ios, uikit, navigation, simulator, accessibility]
requires: [161.1-09]
provides: [production-host-navigation-observation]
affects: [NAV-01, NAV-02, NAV-03, NAV-04, NAV-05, NAV-06]
tech-stack:
  added: []
  patterns: [exact-test-launch-seam, production-coordinator, side-effect-observation]
key-files:
  created: []
  modified:
    - examples/ios_shell_host/CrosswakeShell/CrosswakeShellApp.swift
    - examples/ios_shell_host/CrosswakeShell/NavigationShellViewController.swift
    - examples/ios_shell_host/CrosswakeShell/LiveViewContainerViewController.swift
    - examples/ios_shell_host/CrosswakeShellTests/NavigationShellTests.swift
    - examples/ios_shell_host/CrosswakeShellUITests/NavigationShellTests.swift
decisions:
  - Exact synthetic launch fixtures remain opaque candidates and traverse the production coordinator.
  - Accessibility and layout seams observe existing production side effects only.
metrics:
  tasks_completed: 2
status: complete
---

# Phase 161.1 Plan 10: Production iOS Navigation Observation Summary

The reference host now drives a validated opaque synthetic topology through the production coordinator and UIKit container, with bounded simulator observations only.

## Completed Work

- Added the exact `-crosswake-navigation-synthetic` XCUITest launch seam, isolated from normal compiled-manifest startup.
- Exercised native tab selection, one authorized push, duplicate inertness, patch depth invariance, cancelled and completed pop behavior, and one completed-navigation focus handoff using production objects.
- Added side-effect-only accessibility and layout observation seams; layout delivery keeps four safe-area values separate from keyboard occlusion.

## Verification

Passed:

```text
xcodebuild test -project examples/ios_shell_host/CrosswakeShell.xcodeproj -scheme CrosswakeShell -destination 'platform=iOS Simulator,name=iPhone 17' -only-testing:CrosswakeShellTests/NavigationShellTests -only-testing:CrosswakeShellUITests/NavigationShellTests CODE_SIGNING_ALLOWED=NO
```

The simulator result is advisory and iOS-only. It does not claim adopter-instance, physical-iPhone, Android, generic-navigation, or native-confirmation support.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] Restored numeric interpolation in the production layout JavaScript.**
- **Found during:** Task 2 verification
- **Issue:** The extracted layout-delivery helper retained escaped interpolation tokens instead of emitting numeric CSS values.
- **Fix:** Restored Swift interpolation and verified all five numeric facts through XCTest/XCUITest.
- **Commit:** `c484f72d`

## Known Stubs

None.

## Self-Check: PASSED

- Production commits `f90e4ca6`, `1ead62e6`, `e0eb7688`, and `c484f72d` exist.
- All modified host and test artifacts exist.
