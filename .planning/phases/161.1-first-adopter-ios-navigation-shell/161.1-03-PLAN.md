---
phase: 161.1-first-adopter-ios-navigation-shell
plan: "03"
type: execute
wave: 2
depends_on: ["161.1-01"]
files_modified:
  - lib/crosswake/navigation_transition.ex
  - test/crosswake/navigation_transition_test.exs
  - priv/static/crosswake.esm.js
  - test/js/crosswake_esm_test.mjs
autonomous: true
requirements: [NAV-02, NAV-03]
must_haves:
  truths:
    - "A Phoenix LiveView calls Crosswake.NavigationTransition.push/2 and the library hook forwards the exact validated envelope only through the separate crosswakeNavigation WebKit handler (D-04; NAV-03)."
    - "The typed/versioned low-frequency envelope carries only transition_id, closed push_patch/push_navigate kind, destination route_id, and optional opaque restoration_ref (D-04; NAV-03)."
    - "Malformed, incompatible, missing-handler, or throwing-handler input fails closed without BridgeCommand, browser-history, or confirmation authority (D-06; NAV-02, NAV-03)."
  artifacts:
    - path: "lib/crosswake/navigation_transition.ex"
      provides: "Phoenix-side closed envelope validation and reserved LiveView event delivery."
    - path: "priv/static/crosswake.esm.js"
      provides: "Library-owned exact-key validation and separate crosswakeNavigation WebKit transport."
  key_links:
    - from: "Crosswake.NavigationTransition.push/2"
      to: "window.webkit.messageHandlers.crosswakeNavigation"
      via: "crosswake:navigation_transition -> CrosswakeBridge hook -> dedicated navigation transport"
      pattern: "crosswake:navigation_transition|crosswakeNavigation"
---

<objective>
Deliver the Phoenix/library half of the explicit production transition path.

Purpose: Establish the exact D-04 event and browser-library transport before the dependent Swift/WebKit/coordinator registration plan consumes it.
Output: Phoenix event API, library hook transport, and focused cross-language boundary tests.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/161.1-first-adopter-ios-navigation-shell/161.1-CONTEXT.md
@.planning/phases/161.1-first-adopter-ios-navigation-shell/161.1-RESEARCH.md
@.planning/phases/161.1-first-adopter-ios-navigation-shell/161.1-PATTERNS.md
@.planning/phases/161.1-first-adopter-ios-navigation-shell/161.1-01-SUMMARY.md
@lib/crosswake/bridge.ex
@priv/static/crosswake.esm.js
@test/js/crosswake_esm_test.mjs

<interfaces>
- `Crosswake.NavigationTransition.push/2` owns Phoenix envelope admission and the reserved LiveView event.
- The existing `CrosswakeBridge` hook owns single-page event receipt but posts navigation only to `crosswakeNavigation`, never `crosswakeBridge`.
- Plan 04 consumes this exact event/handler contract and completes the production WebKit registration path.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Deliver one closed transition from Phoenix through the library hook</name>
  <reversibility rating="costly">D-04 publishes a protocol consumed by Phoenix/web, iOS, shared vectors, and generated hosts, so incompatible changes require coordinated migration.</reversibility>
  <read_first>
    - .planning/phases/161.1-first-adopter-ios-navigation-shell/161.1-CONTEXT.md
    - .planning/phases/161.1-first-adopter-ios-navigation-shell/161.1-PATTERNS.md
    - lib/crosswake/bridge.ex
    - priv/static/crosswake.esm.js
    - test/js/crosswake_esm_test.mjs
  </read_first>
  <files>lib/crosswake/navigation_transition.ex, test/crosswake/navigation_transition_test.exs, priv/static/crosswake.esm.js, test/js/crosswake_esm_test.mjs</files>
  <behavior>
    - "RED: Crosswake.NavigationTransition.push/2 accepts exactly protocol, version, transition_id, kind, route_id, and optional restoration_ref, then emits one crosswake:navigation_transition LiveView event."
    - "RED: the owning CrosswakeBridge hook receives that event once and posts canonical JSON only to window.webkit.messageHandlers.crosswakeNavigation."
    - "Malformed, unknown-key, unknown-kind, incompatible, missing-handler, or throwing-handler input produces a closed no-post outcome and never falls through to crosswakeBridge or browser navigation."
    - "The event and posted object contain no payload, account, device, token, URL, browser-history, capability, reply, or stable-identity field."
  </behavior>
  <action>Write the Phoenix and Node failures first. Add `Crosswake.NavigationTransition` as a separate non-capability module with `push(socket, attrs)`: validate the exact D-04 key set, closed protocol/version/kind, bounded opaque transition and route refs, optional opaque restoration ref, and reject unknown or sensitive keys before calling `Phoenix.LiveView.push_event(socket, "crosswake:navigation_transition", envelope)`. Extend the existing single-owner `CrosswakeBridge` hook to register that reserved event and route it through a dedicated `findNavigationTransport`; accept only `window.webkit.messageHandlers.crosswakeNavigation.postMessage`, pass canonical JSON once, and return a closed local delivery outcome for missing/throwing transport. Do not reuse `findTransport`, `crosswakeBridge`, ACK/reply/in-flight machinery, `BridgeCommand`, `pushEvent`, DOM navigation, `history`, or location APIs. Test exact event bytes, duplicate-hook single ownership, missing/throwing handler, unknown keys, forbidden fields, and source absence of bridge/history authority per D-04/D-06.</action>
  <acceptance_criteria>
    - Phoenix emits exactly one reserved event containing the exact privacy-safe D-04 envelope.
    - The library hook posts that envelope exactly once to the separate navigation handler and nowhere else.
    - Missing, throwing, malformed, incompatible, or unknown-field inputs yield closed no-post outcomes.
    - Browser history and BridgeCommand/capability/reply authority are absent from the production delivery seam.
  </acceptance_criteria>
  <verify><automated>mix test test/crosswake/navigation_transition_test.exs &amp;&amp; node --test test/js/crosswake_esm_test.mjs</automated></verify>
  <done>A real Phoenix LiveView can emit the bounded transition contract and the library client delivers it only to a separate native navigation channel.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Phoenix LiveView event -> library hook -> WebKit handler name | Untrusted input crosses the Phoenix/browser seam before native authority. |

## STRIDE Threat Register — ASVS L1, block on high

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-161.1-01 | Tampering | transition event | high | mitigate | Task 1 enforces exact keys, version, kind, opaque IDs, and canonical single delivery. |
| T-161.1-02 | Information Disclosure | envelope/errors | high | mitigate | Task 1 excludes sensitive/browser fields and uses non-echoing no-post outcomes. |
| T-161.1-03 | Elevation of Privilege | transport fallback | high | mitigate | Task 1 permits only the dedicated handler and rejects BridgeCommand/history authority. |
| T-161.1-SC | Tampering | dependency supply chain | low | accept | No package install occurs. |
</threat_model>

<source_coverage_audit>
| Source | ID | Contract | Coverage | Status |
|---|---|---|---|---|
| REQ | NAV-02 | Native navigation remains resolver-owned. | Transport is candidate delivery only; Plan 04 authorizes effects. | COVERED |
| REQ | NAV-03 | Typed/versioned fail-closed synchronization. | Exact Phoenix event and library transport. | COVERED |
| CONTEXT | D-04-D-06 | Closed protocol, exact effects, explicit denial/fallback. | D-04/D-06 transport boundary here; D-05 effects in dependent Plan 04. | COVERED |
| CONTEXT | Deferred ideas | Generic navigation, Android, native confirm, browser authority. | Explicitly excluded. | EXCLUDED |
</source_coverage_audit>

<verification>
Run the Phoenix event and Node hook transport suites; exact event bytes and handler selection are the primary contract.
</verification>

<success_criteria>
- The exact privacy-safe event reaches only the dedicated handler.
- Invalid delivery cannot mutate browser/native navigation or acquire confirmation authority.
</success_criteria>

<output>
Create `.planning/phases/161.1-first-adopter-ios-navigation-shell/161.1-03-SUMMARY.md` when done.
</output>
