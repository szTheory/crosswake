---
phase: 161.1-first-adopter-ios-navigation-shell
plan: "04"
subsystem: ios-navigation-transition
tags: [swift, webkit, phoenix, navigation, fail-closed]
requires:
  - phase: 161.1-03
    provides: closed Phoenix and JavaScript crosswakeNavigation envelope delivery
provides:
  - Exact Codable WebKit navigation-transition boundary
  - Main-actor resolver-first transition and reconstruction coordinator
  - Production host registration and teardown for crosswakeNavigation
affects: [ios-shell-host, navigation-proof, 161.1-05]
tech-stack:
  added: []
  patterns: [exact-key WebKit admission, bounded transition ledger, atomic reconstruction]
key-files:
  created:
    - packages/crosswake-shell-core-ios/Sources/CrosswakeShellCore/NavigationTransition.swift
    - packages/crosswake-shell-core-ios/Sources/CrosswakeShellCore/NavigationTransitionChannel.swift
    - packages/crosswake-shell-core-ios/Tests/CrosswakeShellCoreTests/NavigationTransitionTests.swift
  modified:
    - packages/crosswake-shell-core-ios/Sources/CrosswakeShellCore/NavigationCoordinator.swift
    - packages/crosswake-shell-core-ios/Sources/CrosswakeShellCore/CrosswakeShell.swift
    - examples/ios_shell_host/CrosswakeShell/LiveViewContainerViewController.swift
key-decisions:
  - "crosswakeNavigation is a separate WebKit handler and never shares BridgeCommand authority."
  - "Patch and navigate effects, native pop, deep link, and restoration stay serialized in NavigationCoordinator."
patterns-established:
  - "Decode exact dictionary keys before the main-actor hop; authorize before every mutation."
requirements-completed: [NAV-02, NAV-03]
coverage:
  - id: D1
    description: "Closed WebKit transition delivery applies patch without stack growth and navigate once."
    requirement: NAV-03
    verification:
      - kind: unit
        ref: "swift test --package-path packages/crosswake-shell-core-ios --filter NavigationTransitionTests"
        status: pass
    human_judgment: false
  - id: D2
    description: "Native pop and deep-link/restoration reconstruction are resolver-authorized and atomic."
    requirement: NAV-02
    verification:
      - kind: unit
        ref: "swift test --package-path packages/crosswake-shell-core-ios --filter NavigationCoordinatorTests"
        status: pass
    human_judgment: false
metrics:
  duration: 7min
  completed: "2026-08-04"
status: complete
---

# Phase 161.1 Plan 04: Native Navigation Transition Summary

**Dedicated WebKit transition delivery now reaches one resolver-authorized main-actor coordinator with idempotent push effects and atomic native reconstruction.**

## Performance

- **Duration:** 7 min
- **Tasks:** 2/2
- **Files modified:** 9

## Accomplishments

- Added the exact-key, version-gated `NavigationTransition` envelope and separate `crosswakeNavigation` WebKit channel.
- Made patch non-growing, navigate idempotent, and all rejected candidates side-effect free through a bounded coordinator ledger.
- Registered and tore down the dedicated production handler while preserving the separate bridge handler; native pop, deep links, and restoration share atomic resolver-first coordination.

## Task Commits

1. **Task 1: Decode WebKit delivery into the idempotent patch/navigate funnel** — `5d115319` (RED tests), `2b3fdcce` (implementation).
2. **Task 2: Register the production channel and funnel native reconstruction** — `c511c611` (RED tests), `6bf2aa18` (implementation), `d0e497fd` (test repair).

## Files Created/Modified

- `NavigationTransition.swift` and `NavigationTransitionChannel.swift` — closed D-04 decoding and WebKit boundary.
- `NavigationCoordinator.swift` — ledger-backed patch/navigate, retained tab stacks, completed-pop, and atomic reconstruction.
- `CrosswakeShell.swift` and `ActivationCoordinator.swift` — manifest/resolver-owned navigation channel creation.
- `LiveViewContainerViewController.swift` — separate `crosswakeNavigation` registration and teardown.
- `NavigationTransitionTests.swift`, `NavigationCoordinatorTests.swift`, and `navigation_transition_vectors.json` — deterministic transition and reconstruction coverage.

## Decisions Made

- Kept the navigation envelope separate from the bridge request/reply system and excluded browser navigation authority.
- Loaded absent topology as `unknown_blocking`, so hosts remain fail-closed until a compiled topology is available.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] Made the production-host source contract test independent of SwiftPM's working directory.**
- **Found during:** Task 2 verification.
- **Issue:** The initial test resolved the host source relative to the package directory, causing the focused Swift test to fail.
- **Fix:** Located the repository root by walking from the test file before reading the host source.
- **Files modified:** `packages/crosswake-shell-core-ios/Tests/CrosswakeShellCoreTests/NavigationTransitionTests.swift`.
- **Verification:** Focused transition and coordinator suites, Phoenix test, and Node test pass.
- **Committed in:** `d0e497fd`.

**Total deviations:** 1 auto-fixed (1 Rule 1). **Impact:** Tightens deterministic proof without changing the transition contract or scope.

## Verification

- `swift test --package-path packages/crosswake-shell-core-ios --filter NavigationTransitionTests` — pass (3 tests).
- `swift test --package-path packages/crosswake-shell-core-ios --filter NavigationCoordinatorTests` — pass (7 tests).
- `mix test test/crosswake/navigation_transition_test.exs` — pass (3 tests).
- `node --test test/js/crosswake_esm_test.mjs` — pass (26 tests).

## Known Stubs

None.

## Next Phase Readiness

The host now has the closed transition ingress and reconstruction semantics required for UIKit composition. Missing topology remains explicitly `unknown_blocking`; no route payload, browser history, native confirmation, or Android authority was introduced.

## Self-Check: PASSED

- All created transition source, vector, and test artifacts exist.
- Task commits `5d115319`, `2b3fdcce`, `c511c611`, `6bf2aa18`, and `d0e497fd` exist.
