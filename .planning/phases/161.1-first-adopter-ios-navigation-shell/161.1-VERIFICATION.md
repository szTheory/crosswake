---
phase: 161.1-first-adopter-ios-navigation-shell
verified: 2026-08-04T18:00:00Z
status: complete
score: 5/5 must-haves verified
behavior_unverified: 0
overrides_applied: 0
gaps: []
---

# Phase 161.1: First-Adopter iOS Navigation Shell Verification Report

**Phase Goal:** Make bounded first-adopter root tabs and pushed detail routes feel native on iOS without turning Crosswake into a generic navigation framework or changing leaf-route runtime ownership.
**Re-verification:** Yes — Plan 12 replaced the fixed advisory outcome with current-run production-host observations.
**Status:** complete, advisory iOS host/simulator proof only.

## Fresh Evidence

One fresh invocation of `bash scripts/verify_phase_161_1.sh DESTINATION` passed all required deterministic suites: 177 ExUnit tests, 26 Node tests, 37 Swift package tests, the named reference-host XCTest/XCUITest suite, formatting, and `git diff --check`.

The shell command retained no raw host transcript, screenshot, result bundle, route, payload, history, identity, or device data. Its private Xcode output was reduced only after the named tests passed exactly once to these ordered closed IDs:

`PL-IOS-NAV-TOPOLOGY`, `PL-IOS-NAV-PATCH-DEPTH`, `PL-IOS-NAV-NAVIGATE-ONCE`, `PL-IOS-NAV-RESTORE`, `PL-IOS-NAV-TABS-BACK`, `PL-IOS-NAV-MARKER-INSETS`, and `PL-IOS-NAV-FOCUS`.

The Mix promotion task accepts that canonical private-run observation only when its nonce matches the invocation root, its schema/scope/order are exact, its input is a regular non-symlink file, and final-tree subject digests are unchanged before and after promotion. It then invalidates the observation. Retained evidence contains only the closed assertion outcomes and content digests.

## Observable Truths

| # | Truth | Status | Fresh direct evidence |
| --- | --- | --- | --- |
| 1 | Root-tab selection and pushed-detail/back transitions preserve explicit route ownership. | ✓ VERIFIED | The named host XCTest/XCUITest flow mounts the synthetic validated topology through the production coordinator and UIKit containers, selects tabs, pushes one resolver-authorized destination, and observes completed/cancelled pop behavior. Real adopter topology remains `unknown_blocking`. |
| 2 | Patch-only steps never create native history; navigate transitions never duplicate it. | ✓ VERIFIED | Current Swift coordinator and host container tests observe patch depth invariance, one navigation push, and duplicate inertness. |
| 3 | Safe-area values remain correct through lifecycle delivery while keyboard occlusion stays separate. | ✓ VERIFIED | The host test observes all four safe-area properties and independent keyboard value through the production layout-delivery seam; the reduced marker is `PL-IOS-NAV-MARKER-INSETS`. |
| 4 | A privacy-safe document-root marker distinguishes web from the iOS shell. | ✓ VERIFIED | The current host test confirms the fixed non-identifying marker and five default properties at document start. |
| 5 | Executable proof covers synchronization, restoration/pop, edge-swipe/back, and accessibility focus handoff while Android parity remains an explicit non-claim. | ✓ VERIFIED | The fresh host run emits every required closed marker exactly once; the generated path remains unavailable unless separately production-backed and cannot substitute pack/audio success. |

## Requirement and Boundary Reconciliation

| Requirement | Status | Evidence / retained boundary |
| --- | --- | --- |
| NAV-01 | ✓ SATISFIED, bounded | Compiled synthetic topology and resolver ownership are covered. TODO-002 and real adopter topology remain `unknown_blocking`; no concrete adopter route claim is made. |
| NAV-02 | ✓ VERIFIED | Fresh production-host UIKit tab, push, back/pop, and resolver-authority observations are required by the advisory gate. |
| NAV-03 | ✓ SATISFIED, bounded | Typed/versioned patch/navigate behavior remains directly tested; no browser-history authority is added. |
| NAV-04 | ✓ VERIFIED | Fresh host layout observation requires separate safe-area and keyboard values. |
| NAV-05 | ✓ SATISFIED, bounded | The synchronous non-identifying marker/default contract remains host-tested; no custom user agent or identity field is introduced. |
| NAV-06 | ✓ VERIFIED, advisory only | Current-run, canonical, nonce-bound host observations plus final-tree digests are required for retained advisory evidence. Generated default navigation remains unavailable; simulator success is not adopter/device/general authority. |
| NAV-07 | ✓ SATISFIED, boundary retained | Drift coverage preserves Phoenix-owned confirmation. Native alert/confirm remains absent and reversible only after physical-iPhone proof, an active-adopter route blocker, and an explicit maintainer roadmap decision. |

## No-Promotion Record

- Android remains frozen at its current generator, Maven, JVM, and vector posture.
- Simulator evidence is advisory and does not establish physical-iPhone, adopter-instance, generic-navigation, Android, or native alert/confirm truth.
- Phase 162 alone owns physical-iPhone promotion.
- Phoenix-owned confirmation remains required; no native confirmation capability was promoted.
- TODO-002 and real adopter topology remain `unknown_blocking`.
- This report does not claim an independent security audit.

## Key Link Verification

| From | To | Status |
| --- | --- | --- |
| Named host XCTest/XCUITest result | closed current-run observation | ✓ Each required named test must pass exactly once before reduction. |
| private observation | advisory builder | ✓ Exact canonical schema, advisory scope, ordered IDs, and matching nonce are required. |
| advisory | retained evidence | ✓ Canonical bytes, privacy scan, approved-source verification, and pre/post subject-digest equality are required. |
| simulator advisory | support truth | ✓ Explicit no-promotion tests preserve the Android, Phoenix-confirmation, generated-unavailable, and Phase-162 boundaries. |

**Verified:** 2026-08-04T18:00:00Z  
**Verifier:** automated same-tree gate with planning reconciliation
