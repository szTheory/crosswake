---
phase: 161.1-first-adopter-ios-navigation-shell
verified: 2026-08-04T18:38:17Z
status: gaps_found
score: 5/6 must-haves verified
behavior_unverified: 0
overrides_applied: 0
gaps:
  - truth: "A fresh same-tree gate derives every retained navigation assertion from the current host XCTest/XCUITest run, rejecting absent, duplicate, reordered, or forged observations."
    status: failed
    reason: "The gate confirms five XCTest/XCUITest test-case names, then unconditionally writes all seven passed assertion IDs into its observation JSON. No marker is extracted from or counted in the host transcript."
    artifacts:
      - path: "scripts/verify_phase_161_1.sh"
        issue: "Lines 64-73 count only test names; lines 79-82 synthesize the passed observation from a literal ID list."
      - path: "lib/crosswake/proof_lane/navigation_shell_advisory.ex"
        issue: "The advisory validates the supplied canonical JSON but has no provenance link to the Xcode transcript."
    missing:
      - "Emit a closed, non-sensitive assertion marker for each host behavior and reduce/count those markers directly from the invocation-private Xcode transcript before advisory promotion."
      - "Add a regression that makes a host test pass without one required marker and proves the complete gate fails."
behavior_unverified_items: []
---

# Phase 161.1: First-Adopter iOS Navigation Shell Verification Report

**Phase Goal:** Make bounded first-adopter root tabs and pushed detail routes feel native on iOS without turning Crosswake into a generic navigation framework or changing leaf-route runtime ownership.

**Status:** gaps found — the bounded implementation and no-promotion policy are present, but the claimed fresh advisory evidence is not actually derived from the host-test transcript.

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
| --- | --- | --- | --- |
| 1 | Root-tab selection and pushed-detail/back transitions preserve explicit route ownership. | ✓ VERIFIED | `NavigationTopology` accepts only closed validated entries and `NavigationCoordinator` resolves before selected-tab/stack mutation; host tests exercise roots, push, and completed/cancelled pop. |
| 2 | Patch-only steps never create native history; navigate transitions never duplicate it. | ✓ VERIFIED | `NavigationCoordinator.apply/1` sends `push_patch` to `patchSink` without a stack write, records transition IDs, and denies duplicates; coordinator and host tests cover these paths. |
| 3 | Safe-area values remain separate from keyboard occlusion and are delivered to the document seam. | ✓ VERIFIED | The host layout test checks all four `--cw-safe-area-*` names and separate `--cw-keyboard-inset-bottom`; production layout delivery is covered by the named host test. |
| 4 | A privacy-safe document-root marker distinguishes the iOS shell without identity data. | ✓ VERIFIED | The host test checks the fixed `cwNativeShell = "ios"` marker and the five fixed CSS defaults; no account, device, token, or custom-user-agent field was found in the navigation artifacts. |
| 5 | Android, physical-iPhone, adopter-instance, generic-navigation, and native-confirmation promotion remain explicitly excluded. | ✓ VERIFIED | Canonical capability/support sources retain `unknown_blocking`, advisory-only simulator evidence, Phase-162-only device promotion, Android freeze, and Phoenix-owned confirmation; no native alert/confirm surface was found. |
| 6 | A fresh same-tree advisory proof derives the seven retained assertion IDs from the current production-host run. | ✗ FAILED | `scripts/verify_phase_161_1.sh` checks five passing test-case names, then emits the seven `PL-IOS-NAV-*` IDs and `passed` outcome as literals. `NavigationShellAdvisory` validates that JSON but cannot prove it came from Xcode output. |

**Score:** 5/6 must-haves verified.

### Required Artifacts and Key Links

| Artifact/link | Status | Details |
| --- | --- | --- |
| Closed topology compiler → iOS topology/coordinator | ✓ VERIFIED | Opaque closed contract, validation, and resolver-first main-actor mutation are implemented. |
| Phoenix transition → native coordinator | ✓ VERIFIED | Typed/versioned `push_patch` / `push_navigate` envelope is validated in Elixir and decoded by Swift. |
| UIKit shell → marker, insets, focus | ✓ VERIFIED | Reference-host XCTest/XCUITest covers the production container seams. |
| Host XCTest/XCUITest transcript → current-run advisory observation | ✗ NOT WIRED | The shell script only checks test-case names; it does not reduce or count the required assertion markers from transcript output. |
| Advisory object → retained evidence | ⚠️ PARTIAL | Canonical JSON, nonce, private-directory, digest, scan, and post-write subject checks exist, but the input assertion outcomes are synthesized rather than observed. |

### Requirements Coverage

| Requirement | Status | Evidence |
| --- | --- | --- |
| NAV-01 | ✓ SATISFIED, bounded | Closed synthetic topology plus resolver-first root/push state; real adopter topology remains `unknown_blocking`. |
| NAV-02 | ✓ SATISFIED | Host-owned UIKit tabs/pushed detail and resolver authority are implemented and host-tested. |
| NAV-03 | ✓ SATISFIED | Typed transition protocol, patch-depth invariance, navigate-once, and pop behavior are implemented/tested. |
| NAV-04 | ✓ SATISFIED | Separate safe-area and keyboard custom-property contract is implemented and host-tested. |
| NAV-05 | ✓ SATISFIED | Fixed document-start iOS marker and five defaults are implemented without identity fields. |
| NAV-06 | ✗ BLOCKED | The required fresh, executable advisory proof has an observable provenance gap: seven passed markers are literal post-test output, not current-run host observations. |
| NAV-07 | ✓ SATISFIED, boundary retained | Phoenix-owned confirmation remains the fallback; native alert/confirm remains absent and gated by all three reversal prerequisites. |

### Behavioral Spot-Checks

| Check | Result | Status |
| --- | --- | --- |
| `mix test test/crosswake/proof_lane/navigation_shell_advisory_test.exs test/mix/tasks/crosswake_proof_lane_verify_navigation_shell_test.exs` | 4 tests, 0 failures | ✓ PASS — validates advisory input handling, not transcript provenance. |
| Fresh complete gate | Not rerun; its documented fresh pass was accepted as prior execution evidence per scope. Source inspection disproves its marker-provenance claim. | ✗ FAIL (static evidence) |

### Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
| --- | --- | --- | --- | --- |
| `scripts/verify_phase_161_1.sh` | 79-82 | Literal seven-ID `passed` observation created after broad test-name checks | 🛑 Blocker | A passing gate can retain an advisory result without observing the asserted marker-level behaviors. |

## Gaps Summary

The implementation is intentionally bounded and the no-promotion boundaries are correctly represented in code. However, the existing completion record incorrectly states that the seven advisory IDs are reduced from current host XCTest/XCUITest output. They are authored by the shell script. This blocks NAV-06 and therefore the phase’s “fresh production-observing advisory evidence” closure.

_Verified: 2026-08-04T18:38:17Z_
_Verifier: independent goal-backward code inspection_
