---
phase: 161.1-first-adopter-ios-navigation-shell
verified: 2026-08-04T19:32:55Z
status: passed
score: 6/6 must-haves verified
behavior_unverified: 0
overrides_applied: 0
re_verification:
  previous_status: gaps_found
  previous_score: 5/6
  gaps_closed:
    - "A fresh same-tree advisory proof derives the seven retained assertion IDs from the current production-host run."
  gaps_remaining: []
  regressions: []
---

# Phase 161.1: First-Adopter iOS Navigation Shell Verification Report

**Phase Goal:** Make the first adopter's root tabs and pushed detail routes feel native on iOS without turning Crosswake into a generic navigation framework or changing leaf-route runtime ownership.

**Verified:** 2026-08-04T19:32:55Z
**Status:** passed
**Re-verification:** Yes — after Plan 13 gap closure

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
| --- | --- | --- | --- |
| 1 | Root-tab selection and pushed-detail/back transitions preserve explicit route ownership. | ✓ VERIFIED | `NavigationTopology` validates the closed graph and `NavigationCoordinator` resolves every destination before it mutates main-actor tab/stack state. Fresh coordinator and host tests passed. |
| 2 | Patch-only study steps never create native history; navigate transitions never duplicate it. | ✓ VERIFIED | `apply(.pushPatch)` calls only `patchSink`; `.pushNavigate` appends only after the transition-ID guard. `NavigationCoordinatorTests` and the production-host marker path passed. |
| 3 | Safe-area values remain separate from keyboard occlusion and are delivered to the document seam. | ✓ VERIFIED | `LiveViewContainerViewController` publishes four `--cw-safe-area-*` properties and the separate `--cw-keyboard-inset-bottom`; the selected host test asserts the exact production scripts before emitting `PL-IOS-NAV-MARKER-INSETS`. |
| 4 | A privacy-safe document-root marker distinguishes the iOS shell without identity data. | ✓ VERIFIED | The document-start script sets only `cwNativeShell = "ios"` plus the fixed layout defaults; focused source scan found no added identity, URL, token, payload, or history field in the navigation/evidence path. |
| 5 | Android, physical-iPhone, adopter-instance, generic-navigation, and native-confirmation promotion remain explicitly excluded. | ✓ VERIFIED | Canonical capability/support sources retain simulator advisory scope, TODO-002 `unknown_blocking`, Android freeze, Phase-162-only physical-iPhone promotion, and Phoenix-owned confirmation. No native alert/confirm implementation was found. |
| 6 | A fresh same-tree advisory proof derives the seven retained assertion IDs from the current production-host run. | ✓ VERIFIED | The selected XCTest method emits seven anchored markers after its corresponding assertions. The gate extracts only those lines from its invocation-private Xcode transcript, requires the exact ordered vocabulary, encodes the extracted IDs, then nonce/digest-validates and promotes advisory evidence. Fresh full gate and missing/duplicate/reordered marker regressions passed. |

**Score:** 6/6 truths verified (0 present, behavior-unverified).

### Required Artifacts

| Artifact | Expected | Status | Details |
| --- | --- | --- | --- |
| `lib/crosswake/adoption/navigation_topology.ex` | Closed topology compiler | ✓ VERIFIED | Validated opaque rows only; empty/incomplete input yields `unknown_blocking`. |
| `NavigationTopology.swift` / `NavigationCoordinator.swift` | Versioned decoded topology and resolver-first state | ✓ VERIFIED | Closed validation plus main-actor selection, transition, reconstruction, and pop behavior; Swift package tests passed. |
| `NavigationShellViewController.swift` / `LiveViewContainerViewController.swift` | Native containers, shell marker, layout and focus seams | ✓ VERIFIED | Exercised by the selected production-host XCTest/XCUITest path in the fresh gate. |
| `NavigationShellTests.swift` | Current-host behavior markers | ✓ VERIFIED | Exactly seven closed `PL-IOS-NAV-*` marker emissions occur after the relevant assertions. |
| `scripts/verify_phase_161_1.sh` | Transcript-to-observation reducer | ✓ VERIFIED | Anchored extraction rejects malformed marker lines and compares extracted sequence—not a literal shell success list—to the canonical vocabulary before JSON creation. |
| `phase161_1_navigation_gate_integrity_test.exs` | Non-vacuous provenance regression | ✓ VERIFIED | Simulates zero-exit, named-passing host output; missing, duplicate, and reordered markers all fail before promotion. |

### Key Link Verification

| From | To | Via | Status | Details |
| --- | --- | --- | --- | --- |
| Phoenix topology compiler | iOS coordinator | Closed versioned JSON and resolver-first selection | ✓ WIRED | Elixir and Swift topology/coordinator tests passed in the fresh gate. |
| Phoenix transition | Native stack state | Typed `push_patch` / `push_navigate` protocol | ✓ WIRED | Fresh Swift transition/coordinator tests and production-host assertions cover patch depth, navigate-once, and pop. |
| Production host assertions | Invocation-private Xcode transcript | Seven exact `PL-IOS-NAV-*: passed` lines | ✓ WIRED | Markers are emitted by `testOrderedProductionNavigationProofEmitsClosedMarkers`; the complete Xcode run was executed afresh. |
| Xcode transcript | Canonical observation | Anchored `awk` reduction, exact-order equality, extracted IDs passed to JSON encoder | ✓ WIRED | Shell lines 77-104 read only marker lines; a test-name-only zero-exit fixture cannot advance. |
| Canonical observation | Retained advisory evidence | Private root, nonce, canonical-byte, subject-digest, scan and invalidation controls | ✓ WIRED | `mix crosswake.proof_lane.verify_navigation_shell` completes in the fresh gate; consumed observation and nonce are invalidated. |

### Data-Flow Trace (Level 4)

| Artifact | Data Variable | Source | Produces Real Data | Status |
| --- | --- | --- | --- | --- |
| Navigation advisory | `assertion_ids` | Exact markers from the current host-test transcript | Seven ordered behavior-derived IDs, not a shell-authored literal list | ✓ FLOWING |
| Native layout delivery | Five CSS values | Live safe-area/keyboard layout helper | Four safe-area facts and independent keyboard fact | ✓ FLOWING |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
| --- | --- | --- | --- |
| Marker provenance negative matrix | `mix test test/crosswake/proof/phase161_1_navigation_gate_integrity_test.exs test/crosswake/proof_lane/navigation_shell_advisory_test.exs test/mix/tasks/crosswake_proof_lane_verify_navigation_shell_test.exs` | 6 tests, 0 failures | ✓ PASS |
| Complete same-tree phase gate | `bash scripts/verify_phase_161_1.sh DESTINATION` | 177 ExUnit, 26 Node, 37 Swift-package tests and selected host XCTest/XCUITest passed; retained evidence was created | ✓ PASS |

### Requirements Coverage

| Requirement | Status | Evidence |
| --- | --- | --- |
| NAV-01 | ✓ SATISFIED, bounded | Compiled opaque topology reaches resolver-authorized native root/push state; real adopter topology remains blocked. |
| NAV-02 | ✓ SATISFIED | Host-owned UIKit tabs/details use the existing resolver for every destination. |
| NAV-03 | ✓ SATISFIED | Typed/versioned transitions enforce patch-depth invariance, idempotent navigate, and synchronized pop behavior. |
| NAV-04 | ✓ SATISFIED | Safe-area values and keyboard occlusion remain separate at the document seam. |
| NAV-05 | ✓ SATISFIED | Synchronous fixed iOS shell marker is present without user-agent or identity data. |
| NAV-06 | ✓ SATISFIED, advisory-only | Executable graph/transition/host/accessibility proof now has current-host marker provenance; simulator proof remains non-promoting. |
| NAV-07 | ✓ SATISFIED, boundary retained | Phoenix-owned confirmation is required; all three conditions for native alert/confirm remain unmet and no such surface was added. |

### Anti-Patterns Found

None. The phase-owned source/evidence artifacts contain no unreferenced `TBD`/`FIXME`/`XXX` marker, placeholder implementation, generic navigation renderer, or native alert/confirm surface. `git diff --check` passed on the current dirty working tree.

### Human Verification Required

None. The stateful navigation, cancellation, ordering, and proof-provenance claims have named automated coverage that passed in this verification. A physical-iPhone claim is deliberately not a Phase 161.1 acceptance criterion; Phase 162 owns it.

## Gaps Summary

The previous NAV-06 blocker is closed. Plan 13 replaced the shell-authored success list with seven current-run host markers, reduces those markers from an invocation-private transcript, and has a full-gate regression proving named passing tests alone cannot promote advisory evidence. All stated no-promotion boundaries remain intact.

---

_Verified: 2026-08-04T19:32:55Z_
_Verifier: independent goal-backward code inspection_
