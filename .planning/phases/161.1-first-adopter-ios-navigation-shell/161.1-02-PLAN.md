---
phase: 161.1-first-adopter-ios-navigation-shell
plan: "02"
type: execute
wave: 2
depends_on: ["161.1-01"]
files_modified:
  - lib/crosswake/adoption/navigation_topology.ex
  - lib/crosswake/manifest/types.ex
  - lib/crosswake/manifest/builder.ex
  - lib/crosswake/manifest/validator.ex
  - lib/crosswake/manifest/serializer.ex
  - test/crosswake/adoption/navigation_topology_test.exs
  - test/crosswake/manifest/builder_test.exs
  - test/crosswake/manifest/validator_test.exs
autonomous: true
requirements: [NAV-01]
must_haves:
  truths:
    - "The route manifest contains one additive versioned navigation_topology compiled from the same validated sanitized rows, never host Swift configuration or example-route defaults (D-01; NAV-01)."
    - "The builder emits unknown_blocking for absent/ineligible TODO-002 rows and the validator rejects duplicate roots, invalid/cyclic/cross-tab parents, unsupported runtime/presentation pairs, unknown routes, and topology/manifest mismatch before serialization (D-02)."
    - "Canonical rendering is deterministic and validation failures remain stable, explicit, and non-echoing; leaf runtime fields remain unchanged."
  artifacts:
    - path: "lib/crosswake/manifest/types.ex"
      provides: "Typed additive NavigationTopology manifest section."
    - path: "lib/crosswake/manifest/builder.ex"
      provides: "Single-source compilation from validated route rows beside routes."
    - path: "lib/crosswake/manifest/validator.ex"
      provides: "Manifest/topology compatibility and graph validation."
    - path: "lib/crosswake/manifest/serializer.ex"
      provides: "Deterministic topology serialization through existing manifest writer."
  key_links:
    - from: "Manifest.Builder.build/3"
      to: "Crosswake.Adoption.NavigationTopology.compile/1"
      via: "the same normalized validated route inventory"
      pattern: "navigation_topology|NavigationTopology.compile"
    - from: "Manifest.Validator.validate/1"
      to: "manifest.routes and manifest.navigation_topology"
      via: "route identity, runtime/presentation, parent graph, and version binding"
      pattern: "validate_navigation_topology|manifest_schema_version"
  prohibitions:
    - "requirement_id: NAV-01 | category: values | statement: MUST NOT turn the bounded first-adopter topology or coordinator into a generic navigation framework, generic WebView wrapper, browser-history router, or second host route authority. | status: unresolved | verification: flagged-unverified"
    - "requirement_id: NAV-03 | category: privacy | statement: MUST NOT carry or retain route payloads, account or device identifiers, credentials, tokens, browser history, URLs, or stable identity across navigation, layout, diagnostics, or proof seams. | status: unresolved | verification: flagged-unverified"
    - "requirement_id: NAV-06 | category: transparency | statement: MUST NOT present synthetic fixtures, package tests, simulator runs, or generated-host results as adopter-instance, physical-iPhone, Android, or generic-navigation proof. | status: unresolved | verification: flagged-unverified"
    - "requirement_id: NAV-07 | category: values | statement: MUST NOT add native alert or confirm, or describe Phoenix-owned confirmation as optional, before physical-iPhone proof, a demonstrated active-adopter blocker, and an explicit maintainer roadmap decision. | status: unresolved | verification: flagged-unverified"
flagged_assumptions:
  - "[UNRESOLVED-SPEC-PROBE][NAV-01 unclassified] CONTEXT D-01 through D-03 and REQUIREMENTS.md are exhaustive; no additional topology field, real route fact, or ownership transfer is inferred."
  - "[UNRESOLVED-SPEC-PROBE][NAV-02 adjacency] Only compiled root/parent relationships may be traversed; no arbitrary sibling, cross-tab, URL, or browser-history adjacency is inferred."
  - "[UNRESOLVED-SPEC-PROBE][NAV-02 empty input] Zero eligible validated rows remains unknown_blocking and produces no native tab or promoted shell claim."
  - "[UNRESOLVED-SPEC-PROBE][NAV-02 ordering] No user-facing tab ordering semantics are supplied; synthetic vectors may be deterministic, but adopter order remains unknown_blocking until sanitized input explicitly authorizes it."
  - "[UNRESOLVED-SPEC-PROBE][NAV-03 unclassified] D-04 through D-06 define the complete transition vocabulary; no third transition kind or browser-history state is inferred."
  - "[UNRESOLVED-SPEC-PROBE][NAV-04 unclassified] D-08 defines exactly four safe-area variables and one separate keyboard variable; no additional layout or identity fact is inferred."
  - "[UNRESOLVED-SPEC-PROBE][NAV-05 unclassified] D-08 defines a non-identifying document-start marker only; no custom user agent or host/account/device marker is inferred."
  - "[UNRESOLVED-SPEC-PROBE][NAV-06 unclassified] D-10 and D-11 define deterministic contract proof plus advisory simulator proof; physical-device and Android promotion remain excluded."
  - "[UNRESOLVED-SPEC-PROBE][NAV-07 unclassified] D-12 and the governing ADR define Phoenix-owned confirmation and the entire reversal condition; no native alert/confirm work is inferred."
---

<objective>
Bind the proven topology contract into the canonical Phoenix manifest lifecycle.

Purpose: Remove any possibility that Swift host configuration or fabricated examples become a second route truth.
Output: Typed manifest section, builder integration, exhaustive validator, deterministic serializer, and focused ExUnit coverage.
</objective>

<artifacts_this_phase_produces>
- New `Crosswake.Manifest.Types.NavigationTopology` and `NavigationTopologyEntry` structs and a `navigation_topology` field on `Types.Root`.
- New `Builder.navigation_topology/2` integration that consumes `Crosswake.Adoption.NavigationTopology` output from the same route rows used for `routes`.
- New `Validator.validate_navigation_topology/2` checks and stable rule IDs for unknown route, duplicate root, missing/cross-tab/cyclic parent, runtime/presentation mismatch, unsupported posture, and schema/version mismatch.
- Changed `Serializer.render/1` output includes canonical ordered `navigation_topology`; existing idempotent `write/2` outcomes stay unchanged.
- Expanded `navigation_topology_test.exs`, `builder_test.exs`, and `validator_test.exs`; no new host config key, CLI flag, external dependency, real adopter row, Android artifact, or leaf runtime field.
</artifacts_this_phase_produces>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/161.1-first-adopter-ios-navigation-shell/161.1-CONTEXT.md
@.planning/phases/161.1-first-adopter-ios-navigation-shell/161.1-RESEARCH.md
@.planning/phases/161.1-first-adopter-ios-navigation-shell/161.1-PATTERNS.md
@.planning/phases/161.1-first-adopter-ios-navigation-shell/161.1-01-SUMMARY.md
@lib/crosswake/manifest/types.ex
@lib/crosswake/manifest/builder.ex
@lib/crosswake/manifest/validator.ex
@lib/crosswake/manifest/serializer.ex

<interfaces>
- Preserve existing `Types.Root`, `Builder.build/3`, `Validator.validate/1`, `Serializer.render/1`, and `Serializer.write/2` entry points; the topology is additive and version-bound.
- The builder already zips route policy with managed route rows; use that normalized source instead of accepting a new option or Swift-host payload.
- Validator errors accumulate deterministically and must not echo route payload/path/input values.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Emit topology from the canonical manifest build</name>
  <reversibility rating="costly">D-01 makes the additive manifest/topology schema an adopter-facing compatibility contract across generators and iOS consumers.</reversibility>
  <read_first>
    - .planning/phases/161.1-first-adopter-ios-navigation-shell/161.1-CONTEXT.md
    - .planning/phases/161.1-first-adopter-ios-navigation-shell/161.1-PATTERNS.md
    - .planning/phases/161.1-first-adopter-ios-navigation-shell/161.1-01-SUMMARY.md
    - lib/crosswake/adoption/navigation_topology.ex
    - lib/crosswake/manifest/types.ex
    - lib/crosswake/manifest/builder.ex
    - lib/crosswake/manifest/serializer.ex
    - test/crosswake/manifest/builder_test.exs
  </read_first>
  <files>lib/crosswake/adoption/navigation_topology.ex, lib/crosswake/manifest/types.ex, lib/crosswake/manifest/builder.ex, lib/crosswake/manifest/serializer.ex, test/crosswake/manifest/builder_test.exs</files>
  <behavior>
    - "RED: building one eligible synthetic route set yields routes and navigation_topology from the same IDs/runtime truth and deterministic bytes."
    - "RED: building absent or unknown_blocking inventory yields an explicit unknown_blocking topology with zero entries, not fabricated roots."
    - "No builder option or host configuration accepts app-specific Swift topology data."
    - "Existing route runtime, offline, auth, capability, pack, and support fields remain byte/semantic compatible."
  </behavior>
  <action>Add typed nested manifest structs and construct the topology from `Crosswake.Adoption.NavigationTopology.compile/1` inside the existing build flow per D-01. Bind `topology_schema_version` to the manifest compatibility section and serialize it through the existing canonical ordered map/idempotent writer. Keep `status: unknown_blocking` and empty entries when the validated inventory cannot promote per D-02. Do not accept topology through opts, application config, Swift, a template, or fixture defaults. Extend builder tests with opaque synthetic IDs and regression assertions that existing leaf-owner/runtime fields do not change.</action>
  <acceptance_criteria>
    - One build invocation produces route entries and topology entries from one normalized route source.
    - Unknown/incomplete input serializes an explicit unknown_blocking topology and no root tabs.
    - Repeated render/write is deterministic and idempotent.
    - Existing manifest consumers and route-owner fields remain compatible.
  </acceptance_criteria>
  <verify><automated>mix test test/crosswake/manifest/builder_test.exs test/crosswake/adoption/navigation_topology_test.exs</automated></verify>
  <done>The canonical manifest is the only durable topology source and remains honest while TODO-002 is blocked.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Reject every invalid manifest/topology relationship</name>
  <reversibility rating="reversible">The fail-closed validator can add stable guards without changing the published topology fields.</reversibility>
  <read_first>
    - .planning/phases/161.1-first-adopter-ios-navigation-shell/161.1-CONTEXT.md
    - lib/crosswake/manifest/types.ex
    - lib/crosswake/manifest/validator.ex
    - test/crosswake/manifest/validator_test.exs
    - test/crosswake/adoption/navigation_topology_test.exs
  </read_first>
  <files>lib/crosswake/manifest/validator.ex, lib/crosswake/manifest/serializer.ex, test/crosswake/manifest/validator_test.exs, test/crosswake/adoption/navigation_topology_test.exs</files>
  <behavior>
    - "Unknown route, duplicate root, missing/cross-tab parent, parent cycle, and invalid root/push pairing all produce stable validation errors."
    - "Topology and manifest schema mismatch rejects the complete artifact before write or Swift consumption."
    - "Unsupported leaf runtime/presentation and deep-link/restoration posture fail closed without changing route truth."
    - "Errors include only rule ID, safe field, and opaque route ref; rejected values/paths never appear."
  </behavior>
  <action>Insert `validate_navigation_topology/2` into the existing deterministic validation pipeline per D-02. Validate the complete graph before output: all entries reference manifest routes; roots are unique and parentless; pushes have one valid same-tab parent; the directed graph is acyclic; closed presentation/runtime and entry posture combinations are allowed; topology/manifest versions are compatible; and unknown_blocking has no promoted entries. Accumulate stable non-echoing failures rather than raising on the first row. Add discriminating tests for every relationship and a scan of rendered errors/output for forbidden sensitive categories.</action>
  <acceptance_criteria>
    - Every D-02 graph and compatibility failure has a focused negative test.
    - Invalid topology cannot be serialized/written as a valid manifest.
    - Validator output is deterministic and non-echoing.
    - No validation path changes leaf runtime ownership or creates generic fallback behavior.
  </acceptance_criteria>
  <verify><automated>mix test test/crosswake/manifest/validator_test.exs test/crosswake/manifest/builder_test.exs test/crosswake/adoption/navigation_topology_test.exs &amp;&amp; mix format --check-formatted lib/crosswake/adoption/navigation_topology.ex lib/crosswake/manifest/types.ex lib/crosswake/manifest/builder.ex lib/crosswake/manifest/validator.ex lib/crosswake/manifest/serializer.ex</automated></verify>
  <done>Only a complete, version-compatible, route-consistent bounded graph can leave the Phoenix manifest boundary.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| RouteInventory rows -> manifest builder | Sanitized input remains untrusted until closed compiler and graph validation complete. |
| topology section -> route manifest | Topology references must not alter or bypass route runtime/auth/offline authority. |
| manifest bytes -> native shell | Version and graph compatibility must be proven before mounting. |

## STRIDE Threat Register — ASVS L1, block on high

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-161.1-01 | Tampering | topology compiler/manifest | high | mitigate | Both tasks enforce single-source input, closed fields, full graph validation, and version binding. |
| T-161.1-02 | Tampering / Repudiation | transitions | high | mitigate | This plan publishes no transition input; Plan 03 binds typed IDs and dedupe to the validated topology. |
| T-161.1-03 | Elevation of Privilege | restore/deep links | high | mitigate | Only closed postures are compiled; Plan 03 reauthorizes every reconstruction candidate. |
| T-161.1-04 | Information Disclosure | manifest/errors | high | mitigate | No sensitive fields exist and error tests assert stable non-echoing output. |
| T-161.1-05 | Information Disclosure / Repudiation | proof/evidence | high | mitigate | Only opaque test IDs are added; Plans 05-06 own retained evidence. |
| T-161.1-06 | Spoofing / Elevation of Privilege | fallback | high | mitigate | Unknown/invalid graphs are explicit non-passing results and cannot become generic WebView navigation. |
| T-161.1-SC | Tampering | dependency supply chain | low | accept | No package install or dependency change is planned. |
</threat_model>

<source_coverage_audit>
| Source | ID | Contract | Coverage | Status |
|---|---|---|---|---|
| GOAL | Phase 161.1 | Native shell preserves Phoenix route truth. | Canonical manifest integration. | COVERED |
| REQ | NAV-01 | Compiled topology with stable roots/details/parents and leaf owners. | Tasks 1-2. | COVERED |
| RESEARCH | Manifest patterns | Typed additive root, deterministic validation/serialization. | Tasks 1-2 use mapped files. | COVERED |
| CONTEXT | D-01-D-02 | Single source, version binding, exhaustive fail-closed graph. | Tasks 1-2. | COVERED |
| EDGE | nine rows | Unresolved semantics. | All retained in frontmatter. | FLAGGED |
| PROHIBITION | four bespoke constraints | Scope/privacy/proof/fallback. | Descriptor-less flagged-unverified. | FLAGGED |
</source_coverage_audit>

<verification>
Run builder/compiler tests before validator breadth; finish with the complete focused manifest suite and formatter against the exact touched Elixir files.
</verification>

<success_criteria>
- One Phoenix declaration produces one canonical topology alongside the route manifest.
- Unknown or invalid topology is explicitly non-passing and non-echoing.
- Leaf runtime ownership and all existing manifest contracts remain intact.
</success_criteria>

<output>
Create `.planning/phases/161.1-first-adopter-ios-navigation-shell/161.1-02-SUMMARY.md` when done.
</output>
