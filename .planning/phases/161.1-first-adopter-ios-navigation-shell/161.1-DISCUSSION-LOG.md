# Phase 161.1: first-adopter-ios-navigation-shell - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-08-04
**Phase:** 161.1-first-adopter-ios-navigation-shell
**Areas discussed:** Navigation topology, Web/native transition contract, Safe-area and shell signals, Proof boundary

---

## Navigation topology

| Option | Description | Selected |
|---|---|---|
| Compiled manifest topology | Versioned topology compiled from validated sanitized route rows. | ✓ |
| Host-local Swift router | Fast local configuration but a second route-truth source. | |
| WebView history authority | Lowest code churn but cannot provide honest native tabs/back. | |
| Generic cross-platform framework | Broad future abstraction with unwanted scope/parity obligations. | |

**User's choice:** Accepted the research recommendation: compiled, manifest-adjacent bounded topology.
**Notes:** Concrete route graph remains `unknown_blocking` until TODO-002 rows validate.

---

## Web/native transition contract

| Option | Description | Selected |
|---|---|---|
| Typed idempotent coordinator | Versioned semantic transition contract with closed patch/navigate behavior. | ✓ |
| URL inference | Infer native state from web URL changes. | |
| Browser history authority | Use WebView history as native stack truth. | |

**User's choice:** Accepted typed low-frequency synchronization with idempotency.
**Notes:** `push_patch` stays in-frame; `push_navigate` makes one authorized native transition.

---

## Safe-area and shell signals

| Option | Description | Selected |
|---|---|---|
| UIKit shell | Native tabs/stacks wrapped by current SwiftUI composition. | ✓ |
| SwiftUI navigation shell | Compact API but less reliable interactive-pop/WebKit identity control. | |
| Web-only navigation | Keeps browser authority and does not meet the native shell goal. | |

**User's choice:** Accepted bounded UIKit tabs/stacks, document-start shell marker, and separate inset signals.
**Notes:** Standard platform affordances, accessibility focus handoff, and task-oriented host copy are required.

---

## Proof boundary

| Option | Description | Selected |
|---|---|---|
| Extend proof lane | Add deterministic vectors and narrow generated iOS adapter checks. | ✓ |
| Standalone demo workflow | Separate exploration harness and proof system. | |
| Required simulator/device CI | Promote simulator/device runs into a recurring required lane. | |

**User's choice:** Accepted proof-lane extension with simulator advisory and Phase 162 physical-device promotion.
**Notes:** Retained evidence stays closed and privacy-safe; Android remains frozen.

---

## the agent's Discretion

- The user explicitly requested a research-backed one-shot recommendation and accepted it. Exact internal
  names, coordinator mechanics, fixture encoding, identifiers, and generated filenames remain planner
  discretion within the locked CONTEXT.md boundaries.

## Deferred Ideas

- Generic navigation, Android parity, native Phoenix rendering, broad restoration, navigation telemetry,
  device farm/required device CI, and native alert/confirm remain outside this phase.
