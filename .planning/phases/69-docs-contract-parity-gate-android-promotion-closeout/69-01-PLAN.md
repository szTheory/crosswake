---
phase: "69"
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - test/crosswake/proof/phase69_docs_contract_parity_test.exs
  - lib/crosswake/support_matrix/support_matrix.ex
  - guides/compatibility.md
  - guides/native_shell.md
  - guides/support_matrix.md
autonomous: true
requirements:
  - PROOF-01
  - PROOF-02

must_haves:
  truths:
    - Guides accurately reflect the typed Elixir support matrix truth without drift.
    - Android support status is `:supported` based strictly on CI evidence.
    - Parity test enforces consistency between all checked guides (`native_shell.md`, `compatibility.md`, `support_matrix.md`), manifest (`gen_manifest.exs`), shell fixtures, doctor JSON output, and support matrix.
  artifacts:
    - path: test/crosswake/proof/phase69_docs_contract_parity_test.exs
      provides: Docs contract parity test
    - path: guides/native_shell.md
      provides: Parity locked native shell docs
    - path: guides/compatibility.md
      provides: Parity locked compatibility docs
    - path: guides/support_matrix.md
      provides: Parity locked support matrix docs
    - path: gen_manifest.exs
      provides: Crosswake generator manifest
    - path: test/crosswake/proof/phase69_docs_contract_parity_test.exs
      provides: Doctor JSON output capture
  key_links:
    - from: test/crosswake/proof/phase69_docs_contract_parity_test.exs
      to: guides/native_shell.md
      via: File read and regex assertion
    - from: test/crosswake/proof/phase69_docs_contract_parity_test.exs
      to: guides/compatibility.md
      via: File read and regex assertion
    - from: test/crosswake/proof/phase69_docs_contract_parity_test.exs
      to: guides/support_matrix.md
      via: File read and regex assertion
    - from: test/crosswake/proof/phase69_docs_contract_parity_test.exs
      to: gen_manifest.exs
      via: Manifest parity assertions
    - from: test/crosswake/proof/phase69_docs_contract_parity_test.exs
      to: mix crosswake.doctor --format json
      via: Doctor output parity assertions
---

<objective>
Promote Android support truth from `:verification_required` to `:supported` and ship the merge-blocking docs-contract parity gate to enforce alignment between the manifest, shell fixtures, guides, and doctor outputs.
Purpose: Prevent documentation drift and finalize Android CI-hermetic support.
Output: Android promoted in SupportMatrix, updated guides, and a passing parity test.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/69-docs-contract-parity-gate-android-promotion-closeout/69-RESEARCH.md
@.planning/phases/69-docs-contract-parity-gate-android-promotion-closeout/69-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Promote Android SupportMatrix</name>
  <files>lib/crosswake/support_matrix/support_matrix.ex</files>
  <behavior>
    - Android status and proof_status update to `:supported`
    - Retain notes indicating CI hermetic evidence
  </behavior>
  <action>Update `SupportMatrix` to promote Android support. Change `:verification_required` to `:supported` for the `android` and `android_shell` entries. Keep the `proof` pointing to JVM hermetic scripts and explicitly document in `notes` that the support is JVM hermetic.</action>
  <verify>
    <automated>mix test test/crosswake/support_matrix/support_matrix_test.exs</automated>
  </verify>
  <done>Android support entry reflects `:supported` status.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Write Docs-Contract Parity Test</name>
  <files>test/crosswake/proof/phase69_docs_contract_parity_test.exs</files>
  <behavior>
    - Parses guides for runtime line policy and support metrics
    - Asserts parity across manifest, shell fixture, doctor JSON output, and all specified domains (rebuild, permission/entitlement, diagnostics)
    - Asserts content matches SupportMatrix exactly
  </behavior>
  <action>Create the parity test using `ExUnit.Case`. Import `Crosswake.TestSupport.ProofAssertions`. Read `guides/support_matrix.md`, `guides/native_shell.md`, and `guides/compatibility.md`. Read and parse `gen_manifest.exs`, shell fixtures, and run the doctor JSON output using `capture_io`. Assert that they contain strings matching `SupportMatrix.canonical()` representations of runtime-line truth and `:jvm_hermetic` Android verification posture. Enforce parity across manifest, shell fixture, doctor JSON output, and all specified domains (rebuild, permission/entitlement, diagnostics). Reference analog: `test/crosswake/proof/phase64_runtime_line_policy_test.exs`.

Pattern excerpt:
`message = ProofAssertions.stable_id_message(
  "proof.phase69.parity.domain",
  "Parity assertion description",
  "Target component",
  "Failure description",
  "File path",
  "Resolution instructions",
  :merge_blocking
)
assert actual == expected, message`</action>
  <verify>
    <automated>mix test test/crosswake/proof/phase69_docs_contract_parity_test.exs</automated>
  </verify>
  <done>Test exists and correctly identifies missing guide content and asserts parity across domains.</done>
</task>

<task type="auto">
  <name>Task 3: Update Parity-Locked Guides</name>
  <files>guides/compatibility.md, guides/native_shell.md, guides/support_matrix.md</files>
  <action>Update the Markdown guides to pass the test from Task 2. Explicitly document the runtime-line policy, rebuild/compatibility matrix, permission/entitlement templates, and diagnostics export. For Android, clearly state that support is `:jvm_hermetic` and device-UAT is advisory. Ensure exact phrase alignment with the SupportMatrix outputs expected by the test.</action>
  <verify>
    <automated>mix test test/crosswake/proof/phase69_docs_contract_parity_test.exs</automated>
  </verify>
  <done>Guides updated and the docs-contract parity test passes cleanly.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test → Markdown | Parsing external documentation for explicit policy string truth |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-69-01 | Tampering | Guides | mitigate | The parity test ensures guides cannot silently drop policy warnings or support matrix caveats. |
</threat_model>

<verification>
mix test test/crosswake/proof/phase69_docs_contract_parity_test.exs
</verification>

<success_criteria>
- Android is `:supported` in SupportMatrix.
- Docs contract parity test is added and passes against manifest, shell fixtures, and doctor output.
- Guides accurately reflect the typed Elixir truth, diagnostics export policy, and Android verification constraints.
</success_criteria>

<output>
Create `.planning/phases/69-docs-contract-parity-gate-android-promotion-closeout/69-01-SUMMARY.md` when done
</output>