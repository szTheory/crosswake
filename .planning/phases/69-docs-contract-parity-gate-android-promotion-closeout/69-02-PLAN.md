---
phase: "69"
plan: "02"
type: execute
wave: 2
depends_on: ["69-01"]
files_modified:
  - .planning/milestones/v4.0-CLOSEOUT.md
  - test/crosswake/planning/closeout_ci_parity_test.exs
  - test/mix/tasks/closeout_verify_test.exs
  - .github/workflows/phase69-proof.yml
autonomous: true
requirements:
  - PROOF-03

must_haves:
  truths:
    - Milestone closeout command deterministically passes over the v4.0 milestone requirements and constraints.
    - No unsupported claims are made for standalone native shell packages or device-verified Android without evidence.
    - CI executes `mix closeout.verify` and treats it as merge-blocking.
  artifacts:
    - path: .planning/milestones/v4.0-CLOSEOUT.md
      provides: Milestone v4.0 closeout ledger
    - path: .github/workflows/phase69-proof.yml
      provides: Merge-blocking CI proof lane for phase 69
  key_links:
    - from: .github/workflows/phase69-proof.yml
      to: mix closeout.verify
      via: Bash command execution in CI
---

<objective>
Ship the v4.0 milestone closeout proof. This includes executing `mix closeout.verify`, ensuring the requirements state and roadmap parity pass, verifying no false claims are made regarding deferred surfaces, and enforcing the phase 69 parity tests in a merge-blocking CI workflow.
Purpose: Lock the milestone state immutably before v4.0 release.
Output: A valid v4.0 closeout ledger, updated closeout verification tests, and a CI workflow gating the milestone.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/69-docs-contract-parity-gate-android-promotion-closeout/69-RESEARCH.md
@.planning/phases/69-docs-contract-parity-gate-android-promotion-closeout/69-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Generate v4.0 Closeout Ledger</name>
  <files>.planning/milestones/v4.0-CLOSEOUT.md</files>
  <action>Create the `.planning/milestones/v4.0-CLOSEOUT.md` file. Add the exact frontmatter shape required by `Crosswake.Planning.CloseoutVerifier`, specifying `milestone: v4.0`, expected phases (64-69), and `status: complete` for `requirements_state`, `roadmap_parity`, `phase_verification_coverage`, `summary_frontmatter_coverage`, `validation_ledger_status`, and `thread_seed_disposition`. If necessary, capture deferred items explicitly using the `deferred_with_reason` exception shape.</action>
  <verify>
    <automated>mix closeout.verify --cwd . --closeout-path .planning/milestones/v4.0-CLOSEOUT.md</automated>
  </verify>
  <done>Closeout file exists and `mix closeout.verify` successfully parses the ledger frontmatter without missing-key failures.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Update Closeout CI Parity Test & Verify CLI Test</name>
  <files>test/crosswake/planning/closeout_ci_parity_test.exs, test/mix/tasks/closeout_verify_test.exs</files>
  <behavior>
    - CI parity test asserts `phase69-proof.yml` exists and runs `mix closeout.verify`
    - Closeout task test uses the newly expected v4.0 milestone or CLI flags
  </behavior>
  <action>Update `test/crosswake/planning/closeout_ci_parity_test.exs` to target `.github/workflows/phase69-proof.yml`. Assert that the workflow runs `mix closeout.verify --closeout-path .planning/milestones/v4.0-CLOSEOUT.md` and runs the `phase69_docs_contract_parity_test.exs`. Modify `test/mix/tasks/closeout_verify_test.exs` if needed to reflect the new closeout paths or Phase 69 context so existing checks do not fail against v3.9.</action>
  <verify>
    <automated>mix test test/crosswake/planning/closeout_ci_parity_test.exs test/mix/tasks/closeout_verify_test.exs</automated>
  </verify>
  <done>Tests are updated and assert accurately against the Phase 69 environment context.</done>
</task>

<task type="auto">
  <name>Task 3: Create Phase 69 CI Proof Workflow</name>
  <files>.github/workflows/phase69-proof.yml</files>
  <action>Create `.github/workflows/phase69-proof.yml` (copying from `phase68-proof.yml` if useful). Add jobs to run `mix test test/crosswake/proof/phase69_docs_contract_parity_test.exs`, `test/mix/tasks/closeout_verify_test.exs`, and `mix closeout.verify --closeout-path .planning/milestones/v4.0-CLOSEOUT.md`. Ensure the job is merge-blocking (`continue-on-error: false`).</action>
  <verify>
    <automated>mix test test/crosswake/planning/closeout_ci_parity_test.exs</automated>
  </verify>
  <done>The GitHub Actions workflow is created and passes the CI parity assertions.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI Pipeline → Release | Final validation gate to prevent release of incomplete artifacts |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-69-02 | Elevation of Privilege | Milestone Release | mitigate | `mix closeout.verify` strictly rejects unsupported release claims, verifying frontmatter directly. |
</threat_model>

<verification>
mix closeout.verify --closeout-path .planning/milestones/v4.0-CLOSEOUT.md
</verification>

<success_criteria>
- The `.planning/milestones/v4.0-CLOSEOUT.md` is valid according to `mix closeout.verify`.
- Phase 69 has a CI workflow asserting the new proofs and the closeout gate.
- CI parity tests correctly parse the new GitHub action workflow.
</success_criteria>

<output>
Create `.planning/phases/69-docs-contract-parity-gate-android-promotion-closeout/69-02-SUMMARY.md` when done
</output>