---
phase: 38-companion-seam-contract
plan: 02
type: execute
wave: 2
depends_on: ["38-01"]
files_modified:
  - lib/crosswake/doctor/doctor.ex
  - test/support/stub_companion.ex
  - test/crosswake/proof/phase38_companion_contract_test.exs
autonomous: true
requirements: [COMP-02, COMP-03]
must_haves:
  truths:
    - "A companion enabled in host config whose optional dependency is missing causes mix crosswake.doctor to emit an :error finding code companion.dependency_missing that names the missing module(s) — never silent, never a crash"
    - "The doctor reads the companion registry from Application.compile_env(:crosswake, :companions, []) and iterates registered companions"
    - "Calling validate_dependency/0 through the doctor emits a real [:crosswake, :companion, :validate_dependency] telemetry span (start/stop) with companion_id metadata"
    - "A test-support fixture companion implements all 6 Crosswake.Companion callbacks and compiles satisfying the behaviour with no extra boilerplate (SC#1)"
    - "The hermetic phase38 proof test passes inside the existing mix test --exclude requires_example_host lane with no network/SDK/device dependency"
  artifacts:
    - path: "lib/crosswake/doctor/doctor.ex"
      provides: "phase_38_companion_seam_findings/0 emitting the fail-closed :error finding (D-04) + the validate_dependency telemetry span (D-11b)"
      contains: "phase_38_companion_seam_findings"
    - path: "test/support/stub_companion.ex"
      provides: "Crosswake.Companion fixture implementing all 6 callbacks (D-13, SC#1)"
      contains: "@behaviour Crosswake.Companion"
    - path: "test/crosswake/proof/phase38_companion_contract_test.exs"
      provides: "Hermetic proof of SC#1/SC#2/SC#4 (D-13)"
      contains: "companion.dependency_missing"
  key_links:
    - from: "lib/crosswake/doctor/doctor.ex"
      to: "Application.compile_env(:crosswake, :companions, [])"
      via: "registry read in phase_38_companion_seam_findings/0"
      pattern: "compile_env\\(:crosswake, :companions"
    - from: "lib/crosswake/doctor/doctor.ex"
      to: ":telemetry.span"
      via: "wraps validate_dependency/0 call"
      pattern: ":telemetry\\.span"
    - from: "test/crosswake/proof/phase38_companion_contract_test.exs"
      to: "lib/crosswake/doctor/doctor.ex"
      via: "Doctor.run asserts companion.dependency_missing finding"
      pattern: "companion\\.dependency_missing"
---

<objective>
Wire the Phase 38 contract into `mix crosswake.doctor` and prove it. Add `phase_38_companion_seam_findings/0` to `doctor.ex` (mirroring `phase_19_commerce_corridor_posture`) that reads the host companion registry, runs each companion's `validate_dependency/0` inside the `[:crosswake, :companion, :validate_dependency]` telemetry span, and emits the fail-closed `:error` finding `companion.dependency_missing` when an enabled companion's optional library is absent (D-04). Then create the test-support fixture companion (D-13) and a hermetic `phase38_companion_contract_test.exs` proving SC#1 (behaviour compiles), SC#2 (fail-closed doctor error), and SC#4 (telemetry span emits).

Purpose: Make silent fail-open structurally impossible (the single most dangerous outcome — `Report.status` already escalates to `:error` on any `:error` finding) and prove the contract in the existing hermetic merge-blocking lane with no new CI plumbing. Satisfies COMP-02 (fail-closed doctor error) and COMP-03 (real telemetry span).
Output: doctor wiring, fixture companion, and the hermetic proof test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/38-companion-seam-contract/38-CONTEXT.md
@.planning/phases/38-companion-seam-contract/38-PATTERNS.md
@.planning/phases/36-hermetic-proof-lane/36-CONTEXT.md
@.planning/phases/38-companion-seam-contract/38-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create the test-support fixture companion (SC#1)</name>
  <files>test/support/stub_companion.ex</files>
  <read_first>
    - lib/crosswake/companion.ex (the behaviour from Plan 01 — the fixture must implement all 6 callbacks)
    - lib/crosswake/companion/state.ex (the State struct the fixture's report_state/0 returns)
    - test/support/router_fixtures.ex (Crosswake.TestSupport.* namespace + @moduledoc false + plain-module no-`use` convention)
    - test/support/example_host.ex (single-purpose test-support module convention)
    - .planning/phases/38-companion-seam-contract/38-PATTERNS.md (section "test/support/<name>_companion.ex" — fixture pattern + the SC#2 broken-variant note; mix.exs elixirc_paths(:test) auto-compiles test/support)
  </read_first>
  <action>
    Create `Crosswake.TestSupport.StubCompanion` in `test/support/stub_companion.ex`, `@moduledoc false`, `@behaviour Crosswake.Companion`, implementing all 6 callbacks with `@impl true` each:
    - `companion_id/0` → `:stub_companion`
    - `enabled?/1` → `true` (ignores the config map)
    - `route_gated?/2` → `:pass` (the closed no-deny return per D-06)
    - `kill_switch_active?/1` → `false`
    - `validate_dependency/0` → `:ok`
    - `report_state/0` → `%Crosswake.Companion.State{companion_id: :stub_companion, enabled: true, dependency_status: :present, gate_status: :unconfigured, kill_switch_status: :unconfigured, checked_at: System.monotonic_time(:millisecond)}` (use `:unconfigured` for gate/kill since no gating wiring exists until Phase 40 — Claude's discretion per CONTEXT.md).

    In the SAME file, add a second fixture `Crosswake.TestSupport.BrokenCompanion` for the SC#2 fail-closed path: identical to StubCompanion EXCEPT `companion_id/0` → `:broken_companion`, `validate_dependency/0` → `{:error, [Crosswake.TestSupport.DeliberatelyAbsentLib]}` (a module that does NOT exist anywhere in the project — confirm via `Code.ensure_loaded?/1` returning false; do NOT define it), and `report_state/0`'s `dependency_status:` → `{:missing, [Crosswake.TestSupport.DeliberatelyAbsentLib]}`. Both fixtures live in `test/support/` so `elixirc_paths(:test)` auto-compiles them (no `Code.require_file/2`). Keep these as pure modules: no network, no SDK, no device — hermetic-lane discipline.
  </action>
  <verify>
    <automated>test -f test/support/stub_companion.ex && grep -q "@behaviour Crosswake.Companion" test/support/stub_companion.ex && grep -q "Crosswake.TestSupport.BrokenCompanion" test/support/stub_companion.ex && MIX_ENV=test mix compile --warnings-as-errors</automated>
  </verify>
  <acceptance_criteria>
    - test/support/stub_companion.ex defines `Crosswake.TestSupport.StubCompanion` with `@behaviour Crosswake.Companion` and all 6 callbacks tagged `@impl true`
    - File defines `Crosswake.TestSupport.BrokenCompanion` whose `validate_dependency/0` returns `{:error, [Crosswake.TestSupport.DeliberatelyAbsentLib]}`
    - `MIX_ENV=test mix compile --warnings-as-errors` exits 0 — proving the behaviour is satisfiable with no extra boilerplate and no missing-callback warnings (SC#1)
    - The referenced `Crosswake.TestSupport.DeliberatelyAbsentLib` is NOT defined anywhere (it is the deliberately-absent dependency)
  </acceptance_criteria>
  <done>Both fixtures compile under MIX_ENV=test with `--warnings-as-errors`; the compiler raises no "callback not implemented" warning, demonstrating the behaviour is satisfiable with exactly the 6 callbacks (SC#1).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire phase_38_companion_seam_findings/0 + validate_dependency telemetry span into doctor.ex (D-04, D-11b)</name>
  <files>lib/crosswake/doctor/doctor.ex</files>
  <behavior>
    - When `Application.compile_env(:crosswake, :companions, [])` lists a companion whose `enabled?/1` returns true AND whose `validate_dependency/0` returns `{:error, mods}`, doctor emits exactly one `Check` with `severity: :error`, `code: "companion.dependency_missing"`, `check: "companion.#{companion_id}"`, `details: %{missing_modules: mods}`, and a message naming the module(s).
    - When `enabled?/1` is true and `validate_dependency/0` returns `:ok` (dependency present), doctor emits NO error finding for that companion.
    - When `enabled?/1` is false and dependency is present, doctor emits an `:advisory` finding (D-09 mapping note), not an `:error`.
    - Each `validate_dependency/0` call is wrapped in `:telemetry.span([:crosswake, :companion, :validate_dependency], %{companion_id: id, route_id: nil}, fn -> ... end)` so a `:stop` event fires with `result` in its metadata.
    - With an empty registry (no `:companions` configured), `phase_38_companion_seam_findings/0` returns `[]` and adds nothing to the report.
  </behavior>
  <read_first>
    - lib/crosswake/doctor/doctor.ex (READ: the `run/1` integration block lines 113-146, the `phase_19_commerce_corridor_posture` analog lines 484-499, the `check/6` private helper lines ~1339-1348, the `Report` struct lines 21-46 including `commerce_summary: %{}`, and the alias block lines 7-19)
    - lib/crosswake/doctor/check.ex (the Check struct fields: severity :error|:warning|:advisory, code, message, hint, check, details: %{})
    - lib/crosswake/companion.ex (the behaviour — calls `companion.validate_dependency/0`, `companion.enabled?/1`, `companion.companion_id/0`)
    - .planning/phases/38-companion-seam-contract/38-PATTERNS.md (sections "lib/crosswake/doctor/doctor.ex" and "Telemetry span idiom" — the :telemetry.span/3 shape)
  </read_first>
  <action>
    Add a private `phase_38_companion_seam_findings/0` to `doctor.ex` mirroring the `phase_19_commerce_corridor_posture` shape (read config/state → map to `Check` findings via the existing `check/6` helper — do NOT add a new check helper).

    - Read the registry: `Application.compile_env(:crosswake, :companions, [])` (D-02). NOTE on env-key for the `enabled?/1` config map (D-03, Claude's discretion): pass the companion-scoped host config map; mirror how existing core reads host config — use `Application.compile_env(:crosswake, companion.companion_id(), %{})` to fetch the per-companion map, defaulting `%{}`. Document this choice in the SUMMARY.
    - For each companion module: call `companion.enabled?(config_map)`. Wrap the dependency check in a SYNCHRONOUS `:telemetry.span/3` (D-11c — NO `Task.start`):
      `:telemetry.span([:crosswake, :companion, :validate_dependency], %{companion_id: companion.companion_id(), route_id: nil}, fn -> result = companion.validate_dependency(); {result, %{companion_id: companion.companion_id(), route_id: nil, result: result}} end)`
      The span auto-emits `:start`/`:stop`/`:exception`; the `:stop` metadata carries `result: :ok | {:error, [module()]}` (D-11b).
    - Fail-closed mapping (D-04): if `enabled? == true` AND span result is `{:error, mods}`, emit `check(:error, "companion.dependency_missing", "companion.#{companion.companion_id()}", message, hint, %{missing_modules: mods})`. The `message` MUST name the missing module(s). The `hint` MUST tell the host to add the real dependency to THEIR app's deps (the `optional: true` non-transitivity footgun — the dep is not pulled transitively; copy/hint wording is Claude's discretion).
    - Advisory mapping (D-09): if `enabled? == false` AND dependency is present (`:ok`), emit `check(:advisory, ...)`. (Exact code string/message is Claude's discretion — keep it distinct from the error code.)
    - Thread it into `run/1`: add `phase_38_findings = phase_38_companion_seam_findings()` near the other `phase_NN_findings` bindings and append `phase_38_findings` to the `findings ++ ...` concatenation (lines ~124-134). `Report.status` (line 137) then escalates to `:error` automatically — fail-open is structurally impossible (D-04).
    - Add `alias Crosswake.Companion` to the alias block (lines 7-19). Do NOT modify `commerce_summary` handling or any commerce finding function (D-12). Adding a `companion_summary` Report field is OPTIONAL and out of this phase's required scope — do NOT add it unless a finding genuinely needs it; prefer keeping the Report struct unchanged to stay minimal.
  </action>
  <verify>
    <automated>grep -q "phase_38_companion_seam_findings" lib/crosswake/doctor/doctor.ex && grep -q 'Application.compile_env(:crosswake, :companions' lib/crosswake/doctor/doctor.ex && grep -q ':telemetry.span' lib/crosswake/doctor/doctor.ex && grep -q 'companion.dependency_missing' lib/crosswake/doctor/doctor.ex && grep -q "phase_38_findings" lib/crosswake/doctor/doctor.ex && mix compile --warnings-as-errors && git diff --quiet lib/crosswake/commerce.ex</automated>
  </verify>
  <acceptance_criteria>
    - `lib/crosswake/doctor/doctor.ex` defines `defp phase_38_companion_seam_findings()` (0-arity) reading `Application.compile_env(:crosswake, :companions, [])`
    - The function wraps `validate_dependency/0` in `:telemetry.span([:crosswake, :companion, :validate_dependency], ...)` synchronously (no `Task.start`)
    - On enabled + `{:error, mods}` it builds a Check with `code: "companion.dependency_missing"`, `severity: :error`, `details: %{missing_modules: mods}`
    - `run/1` binds `phase_38_findings = phase_38_companion_seam_findings()` and appends it to the findings concatenation
    - `mix compile --warnings-as-errors` exits 0
    - `git diff --quiet lib/crosswake/commerce.ex` exits 0 (commerce untouched, D-12)
  </acceptance_criteria>
  <done>Doctor iterates the companion registry, emits the fail-closed `:error` finding with the missing module list when an enabled companion's dependency is absent, emits the `:validate_dependency` telemetry span on every check, leaves commerce untouched, and compiles warning-free.</done>
</task>

<task type="auto">
  <name>Task 3: Hermetic phase38 proof test — SC#1, SC#2, SC#4 (D-13)</name>
  <files>test/crosswake/proof/phase38_companion_contract_test.exs</files>
  <read_first>
    - test/crosswake/proof/phase33_commerce_corridor_routes_test.exs (hermetic untagged proof structure: `use ExUnit.Case, async: true`, hermeticity self-assertion test reading `__ENV__.file`, no example-host dependency)
    - test/crosswake/proof/phase23_commerce_support_proof_test.exs (the `setup` block writing a temp install manifest via `System.tmp_dir!()` + `Jason.encode!`, and the `Doctor.run(...)` + findings assertion pattern)
    - lib/crosswake/doctor/doctor.ex (the `phase_38_companion_seam_findings/0` wiring + `Doctor.run/1` opts from Task 2)
    - test/support/stub_companion.ex (the StubCompanion + BrokenCompanion fixtures from Task 1)
    - lib/crosswake/companion.ex and lib/crosswake/companion/state.ex (the contract under test)
    - .planning/phases/38-companion-seam-contract/38-PATTERNS.md (section "phase38_companion_contract_test.exs" — the three test-group templates)
    - .planning/phases/36-hermetic-proof-lane/36-CONTEXT.md (hermetic-lane discipline: pure modules, no network/SDK/device)
  </read_first>
  <action>
    Create `Crosswake.Proof.Phase38CompanionContractTest` in `test/crosswake/proof/phase38_companion_contract_test.exs`: `use ExUnit.Case, async: true`, NO `@moduletag` (untagged so the existing `phase34-proof.yml` `mix test --exclude requires_example_host` lane picks it up — D-13, no new CI file). Include a moduledoc noting it is hermetic and merge-blocking.

    Four tests:
    - HERMETICITY self-assertion (mirror phase33): read `File.read!(__ENV__.file) |> String.downcase()`; `refute` it references the example-host router or uses `Code.require_file(`. Keeps the lane hermetic.
    - SC#1 — behaviour satisfaction: assert `Crosswake.TestSupport.StubCompanion.companion_id() == :stub_companion`, `enabled?(%{}) == true`, `route_gated?(route_entry, target) == :pass`, `kill_switch_active?(target) == false`, `validate_dependency() == :ok`, and `match?(%Crosswake.Companion.State{}, report_state())`. Build a minimal `%Crosswake.Manifest.Types.RouteEntry{}` and `%Crosswake.Compatibility.Target{}` inline for the call args (read those structs' `@enforce_keys` to supply required fields).
    - SC#2 — fail-closed doctor error: register the broken fixture via `Application.put_env(:crosswake, :companions, [Crosswake.TestSupport.BrokenCompanion])` and ensure the `enabled?/1` config map resolves to enabled (per Task 2's env-key choice, e.g. `Application.put_env(:crosswake, :broken_companion, %{enabled: true})`); `on_exit` deletes both env keys. Build a temp install manifest in `setup` (phase23 pattern) so `Doctor.run/1` runs hermetically. Run `Doctor.run(...)`, then `finding = Enum.find(report.findings, &(&1.code == "companion.dependency_missing"))`; assert `finding != nil`, `finding.severity == :error`, `%{missing_modules: [Crosswake.TestSupport.DeliberatelyAbsentLib]} = finding.details`, and that the finding `message` contains the absent module name.
    - SC#4 — telemetry span: `:telemetry.attach("phase38-test-handler", [:crosswake, :companion, :validate_dependency, :stop], handler_sending_metadata_to_self, nil)` with `on_exit` detaching it; trigger `Doctor.run/1` (or a direct companion check through the doctor path); `assert_receive {:telemetry_stop, %{companion_id: id, result: result}}, 1000`. Use the StubCompanion (result `:ok`) or BrokenCompanion variant — either proves the `:stop` event fires with `companion_id` metadata.

    Note: `Application.compile_env` is read at COMPILE time. If the doctor reads the registry via `compile_env`, the SC#2/SC#4 runtime `put_env` will not be observed unless the doctor reads it at runtime. Resolve this in implementation: have `phase_38_companion_seam_findings/0` read the registry via `Application.get_env(:crosswake, :companions, [])` at runtime for testability (the doctor is a runtime mix task, not a compiled macro), OR inject the companion list through a `Doctor.run/1` opt. Pick the approach that lets the proof test register fixtures at runtime; document the choice in the SUMMARY and keep D-02's "host registers a list under :crosswake" config shape intact. (If you change Task 2's read from `compile_env` to `get_env`, update Task 2's grep accordingly — runtime read is the correct call for a doctor mix task.)
  </action>
  <verify>
    <automated>mix test test/crosswake/proof/phase38_companion_contract_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - `mix test test/crosswake/proof/phase38_companion_contract_test.exs` exits 0 with all four tests passing
    - `mix test --exclude requires_example_host` exits 0 (the phase38 test runs untagged in the hermetic lane and the suite stays green)
    - The SC#2 test asserts a finding with `code == "companion.dependency_missing"`, `severity == :error`, and `details.missing_modules` naming `Crosswake.TestSupport.DeliberatelyAbsentLib`
    - The SC#4 test receives a `[:crosswake, :companion, :validate_dependency, :stop]` event with `companion_id` in metadata
    - The hermeticity self-assertion test passes (no example-host / Code.require_file references)
  </acceptance_criteria>
  <done>The hermetic proof passes in the existing merge-blocking lane, proving SC#1 (behaviour satisfiable), SC#2 (enabled-but-missing companion → fail-closed `:error` naming the dep), and SC#4 (the real `:validate_dependency` telemetry span emits with `companion_id` metadata) — with no new CI file and no network/SDK/device dependency.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| host config → doctor | The host registers companion impl modules (`config :crosswake, companions: [...]`) and supplies each companion's `enabled?/1` config map. Untrusted-to-core data crosses into `phase_38_companion_seam_findings/0`. |
| companion impl → doctor | `validate_dependency/0` / `report_state/0` return values cross from a host-controlled module into the doctor's finding stream. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-38-04 | Tampering / Repudiation | `phase_38_companion_seam_findings/0` fail-closed path (D-04) | mitigate | The single most dangerous outcome is silent fail-OPEN when a companion is enabled-but-missing. Mitigation: emit `severity: :error` finding `companion.dependency_missing`; `Report.status` (doctor.ex line 137) derives `:error` from `Enum.any?(findings, &(&1.severity == :error))`, so a missing dep cannot silently pass. The SC#2 proof test asserts the absence path explicitly in a hermetic no-dep lane. |
| T-38-05 | Denial of Service | companion callback invocation inside doctor | mitigate | `validate_dependency/0` runs inside `:telemetry.span/3` which emits an `:exception` event on raise (D-11b); the doctor must not let a crashing companion silently swallow the failure into a fail-open. The proof exercises the `{:error, mods}` return path; a raising companion surfaces via the span `:exception` rather than a silent skip. (Full crash-isolation hardening of the registry loop is acceptable-as-is for Phase 38 since the registry is host-controlled, not attacker-controlled input.) |
| T-38-06 | Information disclosure | `:validate_dependency` telemetry span metadata (D-11b) | mitigate | Span metadata is restricted to `%{companion_id: atom(), route_id: nil, result: :ok | {:error, [module()]}}` — NO host config map, NO secrets, NO `enabled?/1` argument is placed in span metadata. The `result` carries only module atoms. The SC#4 test asserts the metadata shape is exactly `companion_id` + `result`. |
| T-38-07 | Information disclosure | doctor finding `hint`/`message` copy | accept | The fail-closed finding names the missing MODULE and instructs adding the dep (the `optional: true` non-transitivity footgun); module names are not secrets. No PII/secret in the finding text. Low-value target. |
| T-38-SC | Tampering | hex deps | accept | No package-manager install task in this plan; `:telemetry` already declared in Plan 01 and resolved in mix.lock. No `[ASSUMED]`/`[SUS]` package; no legitimacy checkpoint required. |
</threat_model>

<verification>
- `mix compile --warnings-as-errors` exits 0.
- `mix test test/crosswake/proof/phase38_companion_contract_test.exs` exits 0 (all four tests).
- `mix test --exclude requires_example_host` exits 0 (hermetic lane stays green with the untagged phase38 test).
- `git diff --quiet lib/crosswake/commerce.ex` exits 0 (D-12, commerce untouched).
- No new file under `.github/workflows/` (D-13, reuse the existing lane).
- No file created under `lib/crosswake/companions/` (D-14, convention documented only).
</verification>

<success_criteria>
- COMP-02 / SC#2: an enabled companion with a missing optional library makes `mix crosswake.doctor` emit an `:error` finding naming the dependency — never silent, never a crash.
- COMP-03 / SC#4: the `[:crosswake, :companion, :validate_dependency]` span emits for real with `companion_id` metadata (Keathley static-name convention).
- SC#1: the fixture satisfies all 6 `Crosswake.Companion` callbacks with no extra boilerplate.
- Hermetic lane discipline honored; commerce untouched; no Phase 39/40/41/42 work pulled forward.
</success_criteria>

<output>
Create `.planning/phases/38-companion-seam-contract/38-02-SUMMARY.md` when done.
</output>
