---
phase: 27-versioning-and-changelog
plan: 02
type: execute
wave: 2
depends_on: ["01"]
files_modified: ["CHANGELOG.md"]
autonomous: true
requirements: [LOG-03, LOG-04]
must_haves:
  truths:
    - "Preamble explicitly disambiguates planning milestones vs Hex releases"
    - "`[0.1.0]` release entry describes 4-5 core capabilities"
    - "`[0.1.0]` entry contains explicit non-claims about provider adapters and companions"
    - "Roadmap traceability subsection links internal milestones"
  artifacts:
    - path: "CHANGELOG.md"
      provides: "Full 0.1.0 capability and history synthesis"
  key_links: []
---

<objective>
Synthesize the `0.1.0` capabilities and roadmap traceability into `CHANGELOG.md`.

Purpose: Provide an honest description of what the 0.1.0 release delivers without misleading adopters about internal planning labels. Fulfills the "narrow honest claims" principle.
Output: `CHANGELOG.md` populated with the `[0.1.0]` entry and disambiguation preamble.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Preamble and Link Definitions</name>
  <files>CHANGELOG.md</files>
  <action>
    Insert the `## Planning milestones vs Hex releases` preamble below the introductory Keep-a-Changelog text and above the `## [Unreleased]` anchor.
    
    Preamble text to insert:
    ```markdown
    ## Planning milestones vs Hex releases

    This changelog uses **[Semantic Versioning](https://semver.org/spec/v2.0.0.html)** headings like **`[0.1.0]`** for **published Hex releases**. Separately, maintainers track **planning milestones** labeled **v1.0–v3.2** in **`.planning/MILESTONES.md`** — those labels describe shipped *tranches of work*, **not** a second installable version axis on Hex (this repo remains **0.x** on Hex until a real **1.0.0**). When in doubt, treat **`.planning/MILESTONES.md`** as canonical for milestone dates and archive paths.
    ```

    At the bottom of the file, add the markdown link definitions:
    ```markdown
    [Unreleased]: https://github.com/szTheory/crosswake/compare/v0.1.0...HEAD
    [0.1.0]: https://github.com/szTheory/crosswake/releases/tag/v0.1.0
    ```
  </action>
  <verify>
    <automated>grep -q "Planning milestones vs Hex releases" CHANGELOG.md && echo "Preamble found"</automated>
  </verify>
  <done>Preamble disambiguating milestones from hex versions and link definitions are added.</done>
</task>

<task type="auto">
  <name>Task 2: Synthesize [0.1.0] Capability Entry</name>
  <files>CHANGELOG.md</files>
  <action>
    Insert the `## [0.1.0] — 2026-05-28` entry immediately below the `[Unreleased]` section.
    
    Add the `### Added` section with these capability bullets:
    ```markdown
    ## [0.1.0] — 2026-05-28

    ### Added

    * Route policy DSL for declaring per-route runtime ownership: LiveView, offline island, native screen, or adapter. Runtime manifest and compatibility contract generated from route policy declarations.
    * Bounded bridge contract for low-frequency native capability families (`haptics`, `share`, `app_info`, `deep_link`, `permissions.status`, `notification_token`, `file_picker`) with route-local enforcement and typed command envelopes.
    * Offline semantics: cached read-only routes, offline islands with append-only journals, sync endpoints, and server-authoritative reconciliation — each with explicit contract boundaries and doctor diagnostics.
    * Commerce corridor declarations with provider-neutral `commerce.corridor.*` denial vocabulary, entitlement lifecycle lane semantics (authority/access/reconciliation/freshness/evidence), and a Phoenix-owned reconciliation inbox example. Provider adapters (StoreKit, Play Billing) are not included; this release operationalizes the seam contract only. There are no first-party companions yet.
    * `mix crosswake.doctor` diagnostics, support matrix, and proof lanes verified against three adopter-shaped exemplar lanes: Phoenix SaaS portal, selective-native flow, and local-first study flow.

    ### Roadmap traceability

    Internal planning milestones v1.0 (Route Policy Foundation), v2.0 (Adopter Stress Profiles), v3.0 (Capability Contract And Packaging), v3.1 (Native Capabilities and Bridge Expansion), and v3.2 (Commerce And Entitlement Seams) are archived in `.planning/MILESTONES.md`. These are not separate Hex releases. See `.planning/PROJECT.md` for overarching goals.
    ```
  </action>
  <verify>
    <automated>grep -q "Roadmap traceability" CHANGELOG.md && echo "Traceability found"</automated>
  </verify>
  <done>The 0.1.0 entry clearly describes shipped capabilities and explicitly disclaims provider adapters and companions.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| File system | Documentation artifacts |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27-02 | Information Disclosure | CHANGELOG.md | mitigate | Strict preamble inclusion prevents internal project plans from being misinterpreted as supported software claims. |
</threat_model>

<verification>
`grep "Planning milestones" CHANGELOG.md` succeeds, and `grep "Roadmap traceability" CHANGELOG.md` succeeds.
</verification>

<success_criteria>
- Preamble disambiguating planning milestones from Hex releases is present.
- Single `[0.1.0]` release entry describes 4-5 core capabilities accurately and disclaims adapters and companions.
</success_criteria>

<output>
After completion, create `.planning/phases/27-versioning-and-changelog/27-02-SUMMARY.md`
</output>