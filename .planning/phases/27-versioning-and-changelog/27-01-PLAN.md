---
phase: 27-versioning-and-changelog
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: ["mix.exs", "CHANGELOG.md"]
autonomous: true
requirements: [VER-01, LOG-01, LOG-02]
must_haves:
  truths:
    - "`mix.exs` `@version` module attribute is exactly `\"0.1.0\"`"
    - `"CHANGELOG.md"` is included in the `mix.exs` `docs/0` extras list
    - "`CHANGELOG.md` exists with Keep-a-Changelog boilerplate and `[Unreleased]` anchor"
  artifacts:
    - path: "mix.exs"
      provides: "Package version and docs configuration"
    - path: "CHANGELOG.md"
      provides: "Keep-a-changelog baseline"
  key_links:
    - from: "mix.exs"
      to: "CHANGELOG.md"
      via: "docs/0 extras list"
---

<objective>
Pin the version to `0.1.0` and initialize the Keep-a-Changelog structure in `CHANGELOG.md`.

Purpose: Fulfills the mechanical requirements for the `0.1.0` release by providing a valid insertion anchor (`## [Unreleased]`) for the release-please pipeline and ensuring it is rendered via `ex_doc`.
Output: `mix.exs` modified, `CHANGELOG.md` created.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add CHANGELOG.md to mix.exs docs</name>
  <files>mix.exs</files>
  <action>
    Verify `@version` is pinned to `"0.1.0"` in `mix.exs`.
    Then, locate the `defp docs do` function and add `"CHANGELOG.md",` to the `extras` list.
    Preserve existing indentation, list ordering, and formatting (including trailing commas).
  </action>
  <verify>
    <automated>mix compile</automated>
  </verify>
  <done>mix.exs includes CHANGELOG.md and compiles with no errors</done>
</task>

<task type="auto">
  <name>Task 2: Initialize CHANGELOG.md boilerplate</name>
  <files>CHANGELOG.md</files>
  <action>
    Create `CHANGELOG.md` in the project root with the basic Keep-a-Changelog `# Changelog` header, introductory text, and an `## [Unreleased]` section.
  
    Use the exact following skeleton:
    ```markdown
    # Changelog

    All notable changes to this project will be documented in this file.

    The format is based on [Keep a Changelog](https://keepachangelog.com/en/1.1.0/),
    and this project adheres to [Semantic Versioning](https://semver.org/spec/v2.0.0.html).

    ## [Unreleased]

    ### Added

    * Initial public release.
    ```
  </action>
  <verify>
    <automated>grep -q "## \[Unreleased\]" CHANGELOG.md && echo "Anchor found"</automated>
  </verify>
  <done>CHANGELOG.md contains the Keep-a-Changelog structure and [Unreleased] anchor.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| File system | Documentation artifacts |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27-01 | Tampering | CHANGELOG.md | mitigate | Strict adherence to Keep-a-Changelog format prevents release-please misinterpretation. |
</threat_model>

<verification>
`mix compile` succeeds, `CHANGELOG.md` exists, and it has the `[Unreleased]` section.
</verification>

<success_criteria>
- `mix.exs` `@version` module attribute is exactly `"0.1.0"`.
- `CHANGELOG.md` structure contains the Keep-a-changelog baseline and the `[Unreleased]` header.
</success_criteria>

<output>
After completion, create `.planning/phases/27-versioning-and-changelog/27-01-SUMMARY.md`
</output>