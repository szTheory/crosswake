---
phase: 156-native-menu-action-button-control
plan: 06
type: execute
wave: 4
depends_on: [156-03, 156-04, 156-05]
files_modified:
  - examples/phoenix_host/lib/crosswake_example/saas_portal/approval_live.ex
  - examples/phoenix_host/lib/crosswake_example_web/router.ex
  - examples/phoenix_host/test/crosswake_example/saas_portal/approval_live_test.exs
  - examples/phoenix_host/e2e/native-controls.spec.ts
  - priv/templates/crosswake/native_controls_ui/crosswake_fallbacks.ex.eex
  - priv/templates/crosswake/native_controls_ui/crosswake_fallback.css.eex
autonomous: true
requirements: [MENU-01, MENU-02, MENU-03]
must_haves:
  truths:
    - "E1 loading: the Phoenix-owned trigger alone carries the pending affordance; no row spinner is introduced."
    - "E1 error: anchor/dispatch failures resolve as typed denial while the fallback stays usable."
    - "E1 overflow: the 48px trigger may reflow without clipping."
    - "E1 long-text: trigger copy is fixed to Job actions, Actions, or More actions."
    - "E2 empty: zero actions retain Choose an action, render No actions are available for this record., and keep Cancel reachable."
    - "E2 loading: pending disables action rows and Cancel while the trigger carries pending state."
    - "E2 error: failures use the inline fail-closed alert or destructive-confirmation path."
    - "E2 populated: heading, one-to-eight rows, Cancel outside the scroll region, no icons, destructive last."
    - "E2 partial: nil-id explanatory rows stay visible and disabled with the reason in the label."
    - "E2 overflow: rows scroll within max-height min(60vh, 420px), with heading and Cancel outside."
    - "E2 zero-one-many: zero is honest empty state, one-to-eight render, and more than eight fails before dispatch."
    - statement: "Preserve a visual or browser check at 320px proving labels wrap, row height grows, and controls remain reachable."
      verification: backstop
    - "E3 empty: native chrome is not presented for an empty projection."
    - "E3 loading: native presentation starts only after projection and anchor validation; no native loading view."
    - "E3 error: presenter construction or anchor failure denies before native UI."
    - "E3 populated: both native variants receive the same validated projection while the OS owns chrome."
    - "E3 partial: disabled explanatory actions stay present and disabled."
    - "E3 overflow: the eight-row ceiling bounds menu length."
    - "E3 zero-one-many: zero denies/falls back, one-to-eight present, and more than eight fails before dispatch."
    - statement: "Use the 80-grapheme ceiling and retain native-harness plus advisory device evidence for readable scaled labels."
      verification: backstop
    - "E4 overflow: the inline alert has no fixed height and wraps in normal flow."
    - "E4 long-text: fixed alert literals wrap without clipping."
    - "E5 empty: destructive confirmation does not render without its consequence body."
    - "E5 loading: pending label remains Deleting… and duplicate mutation is prevented."
    - "E5 error: confirmation failure leaves the panel open with role=alert and no mutation ambiguity."
    - "E5 partial: incomplete destructive body/consequence fails closed."
    - "E5 overflow: the existing confirmation scroll container keeps Cancel and Delete reachable."
    - "E5 long-text: confirmation copy wraps/reflows without fixed-height clipping."
  artifacts:
    - "approval_live.ex provides the Phoenix-owned Job actions trigger and fallback-first native enhancement exemplar."
    - "native-controls.spec.ts proves denial/no-mutation behavior and the 320px wrapping backstop."
  key_links:
    - "router D-09 action_menu policy -> ApprovalLive runtime projection"
    - "trigger DOM id -> aria-controls/focus return and Bridge.push anchor_id"
    - "native/fallback selected id -> Phoenix reauthorization -> neutral result or destructive confirm"
  prohibitions:
    - statement: "MUST NOT add shell-owned toolbar/navigation chrome, DOM-scanned authority, native confirmation, or native mutation."
      status: resolved
      verification: test
    - statement: "MUST NOT expose bridge, shell, capability, policy, manifest, hook, reply, denial, protocol, native-enhancement, or fallback terminology in end-user copy."
      status: resolved
      verification: test
---

<objective>
Wire the production exemplar and generated fallback to the new native enhancement while satisfying every approved UI state consideration.

Purpose: Make the route author and end-user experience concrete without changing Phoenix ownership or overclaiming native evidence.
Output: D-09 example route, accessible trigger, shared projection, typed reply handling, destructive reauthorization flow, browser route-tour, and 320px backstop.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/156-native-menu-action-button-control/156-CONTEXT.md
@.planning/phases/156-native-menu-action-button-control/156-UI-SPEC.md
@.planning/phases/156-native-menu-action-button-control/156-PATTERNS.md
@.planning/phases/155-host-owned-fallback-components/155-06-SUMMARY.md
@.planning/phases/155-host-owned-fallback-components/155-07-SUMMARY.md
@.planning/phases/156-native-menu-action-button-control/156-03-SUMMARY.md
@.planning/phases/156-native-menu-action-button-control/156-04-SUMMARY.md
@.planning/phases/156-native-menu-action-button-control/156-05-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wire the AdminPilot route, trigger, fallback, and typed outcomes</name>
  <files>examples/phoenix_host/lib/crosswake_example_web/router.ex, examples/phoenix_host/lib/crosswake_example/saas_portal/approval_live.ex, examples/phoenix_host/test/crosswake_example/saas_portal/approval_live_test.exs</files>
  <read_first>
    - examples/phoenix_host/lib/crosswake_example_web/router.ex
    - examples/phoenix_host/lib/crosswake_example/saas_portal/approval_live.ex
    - examples/phoenix_host/test/crosswake_example/saas_portal/approval_live_test.exs
    - .planning/phases/156-native-menu-action-button-control/156-UI-SPEC.md
    - .planning/phases/156-native-menu-action-button-control/156-PATTERNS.md
  </read_first>
  <behavior>
    - "D-01/D-05/D-16: Job actions is ordinary accessible LiveView markup whose id is shared by aria-controls, focus return, and anchor_id."
    - "D-12/D-28: the fallback is assigned the same localized runtime projection before Bridge.push/3 enhances it."
    - "D-23/D-27/D-30: Phoenix reauthorizes selected ids; neutral action mutates only after authorization, destructive action opens existing confirm, dismissal closes with no mutation."
  </behavior>
  <action>Add the exact D-09 route declaration to the AdminPilot route. Reuse the `156-PATTERNS.md` current `ApprovalLive` fallback plus haptics-seam integration analog: preserve its existing assigns-first fallback opening, frozen runtime action projection, trigger/focus wiring, and family-form bridge invocation shape, while Phase 156 changes the invoked family to `action_menu` and handles its typed selected/dismissed reply. Make the existing Phoenix-owned trigger set `menu_open` and the frozen runtime actions before calling `Bridge.push/3` with `ref: :approval_actions`, the same explicit trigger id as `anchor_id:`, and `timeout: :infinity` per D-01/D-05/D-16. Handle typed selected/dismissed replies by closing the fallback; reauthorize selection against current domain state, route destructive selection to the existing Phoenix-owned confirmation, and perform no native mutation per D-23/D-27. Preserve fallback-first behavior and fixed end-user copy per D-28. D-02/D-04 are intentional non-execution rationale: do not create persistent shell toolbar/navigation state or continuous native authority.</action>
  <acceptance_criteria>The declared route opens the existing fallback and native enhancement from one trigger, selected/dismissed outcomes close coherently, destructive selection requires Phoenix confirm, and denial leaves fallback usable.</acceptance_criteria>
  <done>The AdminPilot route and typed outcome flow satisfy the acceptance criteria and LiveView tests pass.</done>
  <verify>
    <automated>mix test examples/phoenix_host/test/crosswake_example/saas_portal/approval_live_test.exs -x</automated>
  </verify>
  <reversibility rating="reversible">The exemplar wiring can evolve behind the published route and reply contracts.</reversibility>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Enforce approved fallback states, copy, wrapping, and reachability</name>
  <files>priv/templates/crosswake/native_controls_ui/crosswake_fallbacks.ex.eex, priv/templates/crosswake/native_controls_ui/crosswake_fallback.css.eex, examples/phoenix_host/e2e/native-controls.spec.ts, examples/phoenix_host/test/crosswake_example/saas_portal/approval_live_test.exs</files>
  <read_first>
    - priv/templates/crosswake/native_controls_ui/crosswake_fallbacks.ex.eex
    - priv/templates/crosswake/native_controls_ui/crosswake_fallback.css.eex
    - examples/phoenix_host/e2e/native-controls.spec.ts
    - examples/phoenix_host/test/crosswake_example/saas_portal/approval_live_test.exs
    - .planning/phases/156-native-menu-action-button-control/156-UI-SPEC.md
    - .planning/phases/156-native-menu-action-button-control/156-PATTERNS.md
  </read_first>
  <behavior>
    - "Every E1..E5 covered truth in frontmatter is exercised by source, LiveView, or browser assertions."
    - "At 320px, long labels wrap, row height grows, and heading/Cancel/actions remain reachable."
    - "Stale binary uses update guidance; unavailable/undeclared uses Nothing was changed; unreachable adds no end-user error."
  </behavior>
  <action>Apply the approved UI-SPEC by reusing the `156-PATTERNS.md` Phase 155 generated fallback-template contract: retain its host-owned action projection, disabled nil-id explanatory rows, destructive/dismiss semantics, and accessible fallback structure, while Phase 156 adds the approved action-menu state, copy, wrapping, and reachability assertions without redesigning that contract. Keep 48px targets, heading and Cancel outside the `min(60vh, 420px)` row scroller, wrapping rows with no fixed height, nil-id reason labels, destructive last, no icons, trigger-level pending only, and the existing accessible destructive-confirm follow-on. Reuse the existing route-tour/`native-controls.spec.ts` proof pattern—semantic assertions before bounded screenshots and denial/no-mutation coverage—while Phase 156 adds a 320px check for wrapping, increased row height, and reachable controls plus empty/one/eight/over-eight states. Assert the fixed copy contract and reject protocol terminology in rendered end-user surfaces. Keep semantic native/device evidence advisory per D-25/D-37.</action>
  <acceptance_criteria>All 26 covered UI considerations and both structured backstops are represented by executable checks or explicit advisory evidence routing, with no clipped/unreachable control at 320px.</acceptance_criteria>
  <done>All approved UI considerations and backstops satisfy the acceptance criteria and browser/LiveView tests pass.</done>
  <verify>
    <automated>mix test examples/phoenix_host/test/crosswake_example/saas_portal/approval_live_test.exs -x &amp;&amp; npm --prefix examples/phoenix_host run test:e2e -- --grep 'native controls|action menu'</automated>
  </verify>
  <reversibility rating="reversible">Generated fallback presentation remains adopter-owned and can evolve within the frozen semantic contract.</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Route declaration -> LiveView projection | Static policy constrains record-dependent localized UI. |
| Native/fallback reply -> domain mutation | User choice must be reauthorized by Phoenix. |
| Generated UI -> adopter browser | Accessible fail-closed states and copy must remain usable under denial. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-156-U1 | Elevation of Privilege | selected action handler | high | mitigate | Reauthorize current domain state and require Phoenix confirmation before destructive mutation. |
| T-156-U2 | Spoofing | action id event | high | mitigate | Match only the validated in-flight projection and reject unknown/disabled ids. |
| T-156-U3 | Denial of Service | narrow/long fallback UI | medium | mitigate | Enforce row/label ceilings and 320px reachability browser proof. |
| T-156-U4 | Information Disclosure | fail-closed copy | low | mitigate | Use fixed non-technical end-user literals with no record or protocol details. |

ASVS Level 1: unresolved high-severity threats block plan completion. T-156-U1 and T-156-U2 are enforced by Task 1's reauthorization/destructive-confirm acceptance and LiveView test command; the UI and denial constraints remain tied to Task 2's browser/LiveView acceptance.
</threat_model>

<verification>
Run targeted LiveView tests and the merge-blocking browser native-controls route-tour, including the 320px wrapping/reachability backstop.
</verification>

<success_criteria>
A route author can declare and invoke the action menu; the browser fallback remains accessible and honest; native answers return to Phoenix for reauthorization; all approved UI considerations are covered.
</success_criteria>

## Artifacts this phase produces

- AdminPilot route `action_menu:` declaration and runtime projection.
- Phoenix trigger id/anchor id `native-controls-menu-trigger` (or the existing exact exemplar id retained by implementation).
- LiveView reply handlers for `%{"outcome" => "selected", "action_id" => id}` and `%{"outcome" => "dismissed"}`.
- Browser checks for 320px wrapping/reachability, state matrix, denial, and no-mutation behavior.
- No new shell toolbar, native confirmation, mutation API, or generic UI component path.

<output>
Create `.planning/phases/156-native-menu-action-button-control/156-06-SUMMARY.md` when done.
</output>
