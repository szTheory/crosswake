---
phase: 156-native-menu-action-button-control
plan: 04
type: execute
wave: 3
depends_on: [156-02]
files_modified:
  - packages/crosswake-shell-core-android/src/main/java/dev/crosswake/shell/core/BridgeChannel.kt
  - packages/crosswake-shell-core-android/src/main/java/dev/crosswake/shell/core/ActionMenuPresenter.kt
  - packages/crosswake-shell-core-android/src/test/java/dev/crosswake/shell/core/BridgeConformanceTest.kt
  - packages/crosswake-shell-core-android/src/test/java/dev/crosswake/shell/core/ActionMenuPresenterTest.kt
autonomous: true
requirements: [MENU-02, MENU-03, PROOF-03]
must_haves:
  truths:
    - "Android renders D-17's real PopupMenu through one bounded anchor adapter and returns exactly one selected or dismissed outcome."
    - "Disabled explanatory rows remain present and disabled; destructive actions sort last without custom color parity or mutation authority."
    - "JVM tests prove semantic presenter wiring and Back/tap-outside callback mapping without claiming TalkBack speech or final pixels."
  artifacts:
    - "packages/crosswake-shell-core-android/src/main/java/dev/crosswake/shell/core/ActionMenuPresenter.kt contains the bounded presenter and PopupMenu adapter."
    - "packages/crosswake-shell-core-android/src/test/java/dev/crosswake/shell/core/ActionMenuPresenterTest.kt proves hostless semantics."
  key_links:
    - "BridgeChannel ACTION_MENU_PRESENT -> ActionMenuPresenter -> one completion authority -> reply JSON"
    - "WebView anchor rectangle -> bounded Android anchor view -> PopupMenu"
  prohibitions:
    - statement: "MUST NOT add a Material bottom-sheet dependency or centered custom menu to simulate iOS parity."
      status: resolved
      verification: test
    - statement: "MUST NOT claim JVM/source proof establishes real TalkBack speech, physical dismissal behavior, or final pixels."
      status: resolved
      verification: judgment
---

<objective>
Implement the production Android `action_menu` presenter and fake-presenter JVM harness behind contract 1.2.0.

Purpose: Deliver Android MENU-02/MENU-03 with a bounded native anchor and honest platform-semantic proof.
Output: Structured JSON decoding, presenter seam, real PopupMenu adapter, and hostless selection/dismissal tests.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/156-native-menu-action-button-control/156-CONTEXT.md
@.planning/phases/156-native-menu-action-button-control/156-RESEARCH.md
@.planning/phases/156-native-menu-action-button-control/156-PATTERNS.md
@.planning/phases/156-native-menu-action-button-control/156-UI-SPEC.md
@.planning/phases/156-native-menu-action-button-control/156-02-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Decode the shared action projection and drive a fake Android presenter exactly once</name>
  <files>packages/crosswake-shell-core-android/src/main/java/dev/crosswake/shell/core/BridgeChannel.kt, packages/crosswake-shell-core-android/src/main/java/dev/crosswake/shell/core/ActionMenuPresenter.kt, packages/crosswake-shell-core-android/src/test/java/dev/crosswake/shell/core/BridgeConformanceTest.kt, packages/crosswake-shell-core-android/src/test/java/dev/crosswake/shell/core/ActionMenuPresenterTest.kt</files>
  <read_first>
    - packages/crosswake-shell-core-android/src/main/java/dev/crosswake/shell/core/BridgeChannel.kt
    - packages/crosswake-shell-core-android/src/test/java/dev/crosswake/shell/core/BridgeConformanceTest.kt
    - .planning/phases/156-native-menu-action-button-control/156-PATTERNS.md
    - .planning/phases/156-native-menu-action-button-control/156-UI-SPEC.md
  </read_first>
  <behavior>
    - "D-31: Kotlin decodes structured JSON arrays/objects while existing scalar payloads remain valid."
    - "D-14/D-22/D-23: fake presenter records identical labels/enabled/destructive/order projection and never mutates."
    - "D-26/D-27: selected and dismissed callbacks complete once; duplicate, unknown, and disabled selections fail closed."
  </behavior>
  <action>Preflight Java before RED: if Homebrew `openjdk@17` is absent, install that bottled formula, then set `JAVA_HOME="$(brew --prefix openjdk@17)/libexec/openjdk.jdk/Contents/Home"` and prepend its `bin` directory; verify Java 17 before invoking Gradle. This is the reproducible local path for the current Java-less macOS environment and preserves the existing CI-backed `android-package-unit` proof lane. Then start with failing JVM tests and follow PATTERNS.md's exact `BridgeCommand`/`BridgeRequestEnvelope`/`evaluate` dispatch analog plus the `FilesPickDelegate` deferred-result and delegate-check seam in `BridgeChannel.kt`. Widen request/reply payload handling to structured JSON, add `ACTION_MENU_PRESENT` mapped to `action_menu`, and introduce a bounded presenter interface plus immutable model. Decode the same validated projection and anchor metadata produced by the tracer, preserving disabled explanatory rows and destructive-last ordering. Route selection/dismissal through one guarded completion authority returning D-26 payloads, and treat ids as untrusted per D-27. Do not add business mutation or native confirmation.</action>
  <acceptance_criteria>The fake presenter records the shared projection; selected/dismissed reply exactly once; malformed, duplicate, disabled, and unknown ids fail closed; prior vector behavior remains green.</acceptance_criteria>
  <done>The Android fake-presenter contract satisfies the acceptance criteria and JVM tests pass.</done>
  <verify>
    <automated>(brew list openjdk@17 &gt;/dev/null 2&gt;&amp;1 || brew install openjdk@17) &amp;&amp; export JAVA_HOME="$(brew --prefix openjdk@17)/libexec/openjdk.jdk/Contents/Home" &amp;&amp; export PATH="$JAVA_HOME/bin:$PATH" &amp;&amp; "$JAVA_HOME/bin/java" -version &amp;&amp; ./gradlew -p packages/crosswake-shell-core-android test --tests '*ActionMenuPresenterTest' --tests '*BridgeConformanceTest'</automated>
  </verify>
  <reversibility rating="costly">Implements D-26/D-31 after the blocking one-way publication checkpoint in 156-01.</reversibility>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Present a real anchored PopupMenu with native dismissal semantics</name>
  <files>packages/crosswake-shell-core-android/src/main/java/dev/crosswake/shell/core/ActionMenuPresenter.kt, packages/crosswake-shell-core-android/src/test/java/dev/crosswake/shell/core/ActionMenuPresenterTest.kt</files>
  <read_first>
    - packages/crosswake-shell-core-android/src/main/java/dev/crosswake/shell/core/ActionMenuPresenter.kt
    - packages/crosswake-shell-core-android/src/test/java/dev/crosswake/shell/core/ActionMenuPresenterTest.kt
    - .planning/phases/156-native-menu-action-button-control/156-UI-SPEC.md
  </read_first>
  <behavior>
    - "D-17/D-18: a real PopupMenu anchors through one bounded adapter; unmappable geometry denies with no centered/custom substitution."
    - "D-21/D-22: labels are native item names, disabled rows remain present, and Back/tap-outside maps once to dismissed."
    - "D-20/D-23: Android divergence is documented; destructive sorting is semantic and no custom color hack implies parity."
  </behavior>
  <action>Implement the Android adapter with platform `PopupMenu` and one private bounded anchor view derived from the validated WebView rectangle, using PATTERNS.md's `FilesPickDelegate` deferred result path and BridgeChannel delegate checks as the closest presenter/callback analog. Use native menu labels, enabled state, focus/font scaling/appearance, and dismiss listener behavior. Deny if a safe anchor cannot be constructed. Preserve destructive-last ordering but do not simulate unsupported destructive color treatment. Per D-18/D-20/D-24, add no Material dependency, custom bottom sheet, centered fallback, or icons.</action>
  <acceptance_criteria>The production adapter invokes PopupMenu against a bounded anchor, keeps disabled rows visible, maps selection and Back/tap-outside dismissal once, and refuses invalid anchoring.</acceptance_criteria>
  <done>The PopupMenu adapter satisfies the acceptance criteria and the full Android JVM suite passes.</done>
  <verify>
    <automated>export JAVA_HOME="$(brew --prefix openjdk@17)/libexec/openjdk.jdk/Contents/Home" &amp;&amp; export PATH="$JAVA_HOME/bin:$PATH" &amp;&amp; "$JAVA_HOME/bin/java" -version &amp;&amp; ./gradlew -p packages/crosswake-shell-core-android test</automated>
  </verify>
  <reversibility rating="reversible">The private Android presenter/anchor adapter can evolve behind the stable semantic contract.</reversibility>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| JSON envelope -> Kotlin model | Untrusted structured request data enters native decoding. |
| WebView coordinates -> Android view | Client geometry constructs a bounded PopupMenu anchor. |
| PopupMenu callback -> bridge reply | User selection becomes untrusted action-id data. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-156-A1 | Tampering | Kotlin action projection | high | mitigate | Decode bounded typed rows and reject malformed, duplicate, unknown, or disabled selections. |
| T-156-A2 | Denial of Service | anchor adapter | high | mitigate | Refuse invalid/unmappable geometry and never substitute centered custom UI. |
| T-156-A3 | Elevation of Privilege | presenter callback | high | mitigate | Native returns ids only; core and Phoenix reauthorize before mutation. |
| T-156-A4 | Repudiation | duplicate callbacks | medium | mitigate | One guarded completion authority records exactly one outcome. |

ASVS Level 1: unresolved high-severity threats block plan completion. T-156-A1 and T-156-A3 are enforced by Task 1's malformed/unknown/disabled and exactly-once acceptance plus Java-17-backed filtered JVM tests; T-156-A2 is enforced by Task 2's invalid-anchor refusal acceptance and full Java-17-backed Android suite.
</threat_model>

<verification>
Run the full Android JVM suite; source checks establish adapter wiring only, while device TalkBack/visual evidence remains advisory.
</verification>

<success_criteria>
Android presents a real, anchored, semantic PopupMenu and returns exactly one typed selected/dismissed reply through a fake-testable presenter boundary.
</success_criteria>

## Artifacts this phase produces

- Kotlin enum value `BridgeCommand.ACTION_MENU_PRESENT`.
- Kotlin structured JSON payload representation for bridge envelopes.
- New path `src/main/java/dev/crosswake/shell/core/ActionMenuPresenter.kt`.
- Private Kotlin action-menu model, presenter interface, fake presenter, bounded anchor adapter, and PopupMenu adapter.
- New test path `src/test/java/dev/crosswake/shell/core/ActionMenuPresenterTest.kt`.

<output>
Create `.planning/phases/156-native-menu-action-button-control/156-04-SUMMARY.md` when done.
</output>
