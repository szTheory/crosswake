---
phase: 83-bounded-bridge-proof-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - examples/phoenix_host/lib/crosswake_example/bridge_proof_live.ex
  - examples/phoenix_host/lib/crosswake_example/router.ex
  - examples/phoenix_host/test/crosswake_example/bridge_proof_live_test.exs
autonomous: true
requirements:
  - BRIDGE-01
must_haves:
  truths:
    - "Native Share dialog is triggered from a LiveView route button."
  artifacts:
    - path: "examples/phoenix_host/lib/crosswake_example/bridge_proof_live.ex"
      provides: "LiveView module with share button and bridge_script"
      min_lines: 30
    - path: "examples/phoenix_host/test/crosswake_example/bridge_proof_live_test.exs"
      provides: "Test verifying share button assigns bridge_request"
      min_lines: 10
  key_links:
    - from: "examples/phoenix_host/lib/crosswake_example/router.ex"
      to: "examples/phoenix_host/lib/crosswake_example/bridge_proof_live.ex"
      via: "Live route /bridge-proof"
      pattern: "live\\(\"/bridge-proof\", CrosswakeExample.BridgeProofLive"
---

<objective>
Implement the `BridgeProofLive` module and configure its route to demonstrate bounded bridge capability integration in Phoenix.
Purpose: Proves that Phoenix can invoke native commands directly from LiveView by explicitly injecting bridge dispatch scripts based on manifest capabilities.
Output: A new route `/bridge-proof` hosting the proof LiveView, its associated test, and proper registry in `router.ex`.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/83-bounded-bridge-proof-polish/83-CONTEXT.md
@.planning/phases/83-bounded-bridge-proof-polish/83-RESEARCH.md
@examples/phoenix_host/lib/crosswake_example/saas_portal/approval_live.ex
@lib/crosswake/bridge/contract.ex
@examples/phoenix_host/lib/crosswake_example/router.ex
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create BridgeProofLive module</name>
  <files>examples/phoenix_host/lib/crosswake_example/bridge_proof_live.ex</files>
  <action>
    Create a new LiveView module `CrosswakeExample.BridgeProofLive`. 
    - Implement `mount/3` initializing `bridge_request: nil`.
    - Implement `render/1` returning a template with a button `phx-click="share"`.
    - If `@bridge_request` is present, render a `<script>` tag matching the pattern in `approval_live.ex` that invokes `postMessage` on the injected `crosswakeBridge`. Ensure the script ID includes the `correlation_id` so Phoenix patches the DOM correctly.
    - Implement `handle_event("share", ...)` to assign `@bridge_request` with a payload conforming to `Crosswake.Bridge.Contract.Request`. The payload must use `protocol: "crosswake.bridge"`, `version: "1.0.0"`, `command: "share.invoke"`, `capability: "share"`, `route_id: "bridge-proof"`, `active_route_id: "bridge-proof"`, and an appropriate `payload` map (`title`, `text`, `url`).
  </action>
  <verify>
    <automated>cd examples/phoenix_host && mix compile --force</automated>
  </verify>
  <done>Module compiles cleanly and implements the standard injected bridge dispatch pattern.</done>
</task>

<task type="auto">
  <name>Task 2: Register bridge-proof route</name>
  <files>examples/phoenix_host/lib/crosswake_example/router.ex</files>
  <action>
    Add a new `live` route for `"/bridge-proof"` within the `scope "/", CrosswakeExample` block that uses `crosswake_defaults runtime: :live_view`.
    Configure it with the `crosswake` map containing: `id: "bridge-proof"`, `runtime: :live_view`, `capabilities: ["share"]`, `offline: :cached_read_only`, `security: :standard`.
  </action>
  <verify>
    <automated>cd examples/phoenix_host && mix crosswake.doctor --router Elixir.CrosswakeExample.Router</automated>
  </verify>
  <done>Route compiles and `crosswake.doctor` successfully validates the route capabilities.</done>
</task>

<task type="auto">
  <name>Task 3: Test BridgeProofLive functionality</name>
  <files>examples/phoenix_host/test/crosswake_example/bridge_proof_live_test.exs</files>
  <action>
    Create an `ExUnit` test module using `Phoenix.LiveViewTest` to verify that `BridgeProofLive` works.
    - Write a test to ensure the page renders initially without the bridge script.
    - Write a test simulating a click on the "share" button and verify the resulting HTML includes the expected `<script>` tag invoking the crosswake bridge payload.
  </action>
  <verify>
    <automated>cd examples/phoenix_host && mix test test/crosswake_example/bridge_proof_live_test.exs</automated>
  </verify>
  <done>LiveView test passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client JS -> Native Shell | Untrusted JS payload invokes bounded native capabilities. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-83-01 | Elevation of Privilege | bridge_proof_live.ex | mitigate | Rely on Crosswake manifest validation in Shell to reject undeclared 'share' capability requests. Format data cleanly in Elixir with Jason. |
</threat_model>

<verification>
Automated tests pass. Project compiles and routes successfully check out via doctor.
</verification>

<success_criteria>
Bridge proof component is created, hooked up to the Phoenix router, and passing automated tests.
</success_criteria>

<output>
Create `.planning/phases/83-bounded-bridge-proof-polish/83-01-SUMMARY.md` when done
</output>