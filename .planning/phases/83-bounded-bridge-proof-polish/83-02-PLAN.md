---
phase: 83-bounded-bridge-proof-polish
plan: 02
type: execute
wave: 2
depends_on: 
  - 83-01
files_modified:
  - examples/QUICK_START.md
autonomous: true
requirements:
  - BRIDGE-01
must_haves:
  truths:
    - "Demo app includes a Quick Start guide for adopters."
    - "All demo features work on both iOS and Android physical devices/simulators."
  artifacts:
    - path: "examples/QUICK_START.md"
      provides: "Developer instructions for testing the bridge demo"
      min_lines: 10
  key_links: []
---

<objective>
Provide a Quick Start guide for adopters to easily test the bounded bridge proof and verify end-to-end functionality via human checkpoint.
Purpose: "Runnable documentation" requires clear guidance. The Quick Start acts as the entry point for testing the bridge command locally on iOS and Android.
Output: A `QUICK_START.md` guide and human verification of the native Share dialog.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/83-bounded-bridge-proof-polish/83-CONTEXT.md
@.planning/phases/83-bounded-bridge-proof-polish/83-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Quick Start guide</name>
  <files>examples/QUICK_START.md</files>
  <action>
    Create `examples/QUICK_START.md`. Include step-by-step instructions for:
    1. Starting the Phoenix server in `examples/phoenix_host` (`mix setup` and `mix phx.server`).
    2. Starting the iOS Shell (`examples/ios_shell_host`) via Xcode or `xcodebuild`.
    3. Starting the Android Shell (`examples/android_shell_host`) via Android Studio or `./gradlew`.
    4. Navigating to `/bridge-proof` within the native shell to test the Native Share bridge action.
    Format clearly using Markdown headers and code blocks.
  </action>
  <verify>
    <automated>test -f examples/QUICK_START.md</automated>
  </verify>
  <done>File exists and properly documents the end-to-end demo execution.</done>
</task>

<task type="auto">
  <name>Task 2: Verify Bounded Bridge Proof</name>
  <files>script/verify_bounded_bridge_proof.sh</files>
  <action>
    Create a verification script `script/verify_bounded_bridge_proof.sh` that asserts:
    - QUICK_START.md exists and documents the /bridge-proof route.
    - The ExUnit tests for `bridge_proof_live_test.exs` pass, proving backend capability formatting.
  </action>
  <verify>
    <automated>./script/verify_bounded_bridge_proof.sh</automated>
  </verify>
  <done>Script exists and executes successfully, proving integration.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Documentation | Guide directs users to run local commands. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-83-02 | Info Disclosure | QUICK_START.md | mitigate | Ensure instructions use localhost and safe default configurations. |
</threat_model>

<verification>
A user can follow the Quick Start guide and successfully trigger a native share dialog from LiveView on iOS and Android.
</verification>

<success_criteria>
Quick Start guide exists and manual checkpoint confirms functionality on actual native shells.
</success_criteria>

<output>
Create `.planning/phases/83-bounded-bridge-proof-polish/83-02-SUMMARY.md` when done
</output>