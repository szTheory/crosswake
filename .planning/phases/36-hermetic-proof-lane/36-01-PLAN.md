---
phase: 36-hermetic-proof-lane
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - test/crosswake/proof/phase34_paywall_corridor_proof_test.exs
autonomous: true
requirements: [PROOF-01, PROOF-03]

must_haves:
  truths:
    # PROOF-01 / SC#1 — four derived states, distinctly asserted
    - "derived_state/1 returns :stale for a fresh-overridden stale snapshot (freshness.state :stale)"
    - "derived_state/1 returns :pending for a fresh + :awaiting_verification reconciliation snapshot"
    - "derived_state/1 returns :denied for a fresh + verified + non-granting snapshot (access :denied)"
    - "derived_state/1 returns :granted for a snapshot produced by MockBackend.build_verified_snapshot/2 + project_snapshot/2"
    # PROOF-01 / SC#2 — :pending -> :granted transition
    - "ReconciliationInbox.ingest_evidence/2 on inline mock purchase evidence returns {:ok, map} with status == :awaiting_verification (the :pending origin)"
    - "MockBackend.build_verified_snapshot/2 -> project_snapshot(nil, verified) -> {:ok, projected} -> derived_state == :granted"
    # PROOF-03 / SC#3 (D-06 reinterpretation) — mock-boundary fence, three real truths
    - "Crosswake.Commerce.Reconciliation.authority_mutation_allowed_from_evidence?/1 == false for mock-produced evidence"
    - "EntitlementProjection.project_snapshot(nil, unverified) returns {:error, :unverified_reconciliation_outcome} for :awaiting_verification reconciliation"
    - "A verified-but-not-refreshed snapshot (:verification_failed) projects but does NOT derive :granted"
    # SC#4 (D-02 reinterpretation) — hermeticity discipline + self-scan guard
    - "The proof file uses async: false, is untagged (no @moduletag :requires_example_host), and a self-scan guard structurally enforces no runtime-path require_file and no process/server tokens"
  artifacts:
    - path: "test/crosswake/proof/phase34_paywall_corridor_proof_test.exs"
      provides: "Hermetic merge-blocking proof of the mock paywall corridor"
      contains: "use ExUnit.Case, async: false"
  key_links:
    - from: "test/crosswake/proof/phase34_paywall_corridor_proof_test.exs"
      to: "examples/phoenix_host/lib/crosswake_example/commerce/entitlement_projection.ex"
      via: "Code.require_file at module scope"
      pattern: "Code\\.require_file.*entitlement_projection\\.ex"
    - from: "test/crosswake/proof/phase34_paywall_corridor_proof_test.exs"
      to: "examples/phoenix_host/lib/crosswake_example/commerce/mock_backend.ex"
      via: "Code.require_file at module scope; build_verified_snapshot/2 call"
      pattern: "build_verified_snapshot"
    - from: "test/crosswake/proof/phase34_paywall_corridor_proof_test.exs"
      to: "lib/crosswake/commerce/reconciliation.ex"
      via: "authority_mutation_allowed_from_evidence?/1 fence call (compilation path, no require_file)"
      pattern: "authority_mutation_allowed_from_evidence\\?"
---

<objective>
Author the single merge-blocking, fully hermetic ExUnit proof for the mock paywall
corridor: `test/crosswake/proof/phase34_paywall_corridor_proof_test.exs`. It drives the
full lane (inline `ReconciliationEvidence` -> `ingest_evidence/2` -> `project_snapshot/2`
-> `derived_state/1`), asserts all four derived states distinctly (PROOF-01 / SC#1), the
`:pending` -> `:granted` transition (PROOF-01 / SC#2), the mock-boundary fence as the three
real truths (PROOF-03 / SC#3, per D-06 reinterpretation), and carries a self-scan
hermeticity guard (SC#4, per D-02 reinterpretation).

Purpose: This proof is the merge-blocking artifact that guarantees the v3.4 paywall corridor
keeps working and that mock evidence can never directly grant entitlement authority. The
running example (LiveView) and this proof share ONE verification core (MockBackend +
EntitlementProjection) — the proof exercises it synchronously (no Task, no PubSub), proving
the Phase 35 D-02 "same code path" thesis.

Output: ONE new test file. NO product code is added. NOTHING in `lib/crosswake/commerce/*`
or the example-host modules is modified — those are SHIPPED/locked from Phases 21/34/35 and
asserted *against*.

Scope-fidelity notes (locked SC reinterpretations — encoded as asserted, flagged for verifier):
- D-02 (SC#4): "no Code.require_file on example-host paths" means example-host *runtime*
  paths only (`*_live.ex`, `endpoint.ex`, `application.ex`, `router.ex`, `repo.ex`, `*_web.ex`).
  Requiring the FOUR PURE commerce modules is the hermetic idiom, NOT a violation.
- D-06 (SC#3): `project_snapshot/2` does NOT reject all non-`:projection_refreshed` states —
  it accepts FOUR verified states (`@verified_reconciliation_states`); only the GRANT requires
  `:projection_refreshed`. The proof asserts the THREE real truths (D-06.1/.2/.3), not the
  literal-but-false SC#3 wording. The verifier evaluates SC#3/SC#4 against these reinterpretations.

Open questions resolved (do not re-open):
- D-07 fixture form: private `defp phase34_*`-prefixed helper functions inside the test module
  (phase21 precedent; research-recommended for simplicity). NOT a nested defmodule, NOT a
  require_file of the phase21 test file.
- The `:granted` snapshot/transition routes through SHIPPED `MockBackend.build_verified_snapshot/2`
  (REQUIRED per D-05.3 / SC#2 anti-vacuity) — yes, it does.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/36-hermetic-proof-lane/36-CONTEXT.md
@.planning/phases/36-hermetic-proof-lane/36-RESEARCH.md
@.planning/phases/36-hermetic-proof-lane/36-VALIDATION.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: require_file header + hermeticity moduledoc + async:false skeleton + inline phase34_-prefixed builders</name>
  <files>test/crosswake/proof/phase34_paywall_corridor_proof_test.exs</files>
  <read_first>
    - test/crosswake/proof/phase34_mock_storefront_test.exs (lines 1-30) — COPY the exact
      `Code.require_file(..., __DIR__)` header form, the moduledoc hermeticity-contract prose, the
      untagged idiom, `use ExUnit.Case, async: false`, and the alias block form. Mirror this file's
      structure precisely.
    - test/crosswake/proof/phase21_reconciliation_example_test.exs (lines 180-229) — the
      snapshot-builder helper signatures to REPRODUCE inline (snapshot/1 with overrides + base map,
      authority_lane/1, access_lane/1, reconciliation_lane/2, freshness_lane/1, EffectiveLane +
      EvidenceLane inlined in snapshot/1 base). DO NOT require_file this test file (D-07).
    - lib/crosswake/commerce/contracts.ex — @enforce_keys for ReconciliationEvidence
      (source, provider, provider_reference, event_kind, evidence_ref, captured_at) and
      EntitlementSnapshot + all lane structs (AuthorityLane :state, AccessLane :decision,
      ReconciliationLane :state, FreshnessLane :state+:checked_at, EffectiveLane :effective_from,
      EvidenceLane :source+:reference). The structs the inline builders construct.
    - examples/phoenix_host/lib/crosswake_example/commerce/mock_backend.ex (verify the alias on
      ~line 38 and @subscription_entry_id "sub_pro_monthly" ~line 106) — confirms require_file load order.
  </read_first>
  <action>
    Create the new file. Header: FOUR `Code.require_file("../../../examples/phoenix_host/lib/crosswake_example/commerce/<mod>.ex", __DIR__)`
    lines in dependency order — reconciliation_keys.ex, then reconciliation_inbox.ex, then
    entitlement_projection.ex, then mock_backend.ex. entitlement_projection.ex MUST precede
    mock_backend.ex (mock_backend aliases EntitlementProjection; load order matters). VERIFY the
    actual require_file lines/paths in phase34_mock_storefront_test.exs and the phase21 header rather
    than trusting this list verbatim; if reconciliation_keys.ex turns out NOT to be a real dependency
    of reconciliation_inbox.ex, flag it in the SUMMARY rather than silently dropping it.

    Module name `Crosswake.Proof.Phase34PaywallCorridorProofTest`. Moduledoc states the hermeticity
    contract explicitly (mirror phase34_mock_storefront_test.exs prose): asserts PROOF-01 + PROOF-03,
    drives the full lane, intentionally UNtagged (no @moduletag :requires_example_host) so it runs in
    the merge-blocking lane under `mix test --exclude requires_example_host`. `use ExUnit.Case, async: false`.
    Alias `Crosswake.Commerce.Contracts`, `CrosswakeExample.Commerce.ReconciliationInbox`,
    `CrosswakeExample.Commerce.EntitlementProjection`, `CrosswakeExample.Commerce.MockBackend`. Do NOT
    alias the lib `Crosswake.Commerce.Reconciliation` ambiguously with the example-host inbox — call
    the fence function fully-qualified (Pitfall 3: the lib ingest_evidence/2 differs from the
    example-host one; the proof uses the example-host ReconciliationInbox.ingest_evidence/2 and accesses
    `result.status` on a plain MAP).

    Add private `defp phase34_`-prefixed builder helpers reproducing the phase21 pattern (D-07 form
    decided: prefixed private functions, NOT a nested module, NOT require_file of phase21):
    `phase34_snapshot/1` (overrides \\ %{}; base map mirrors phase21: authority phase34_authority_lane(:none),
    access phase34_access_lane(:denied), reconciliation phase34_reconciliation_lane(:projection_refreshed),
    freshness phase34_freshness_lane(:fresh), EffectiveLane + EvidenceLane inlined, fixed string timestamps,
    as_of integer; build via `struct!(Contracts.EntitlementSnapshot, Map.merge(base, overrides))`),
    `phase34_authority_lane/1`, `phase34_access_lane/1`, `phase34_reconciliation_lane/2`,
    `phase34_freshness_lane/1`. Use `struct!` (raises on missing enforce_keys), not `struct`. Use FIXED
    timestamp strings (e.g. "2026-05-29T00:00:00Z") for determinism — no DateTime.utc_now in fixtures.
    Add a `phase34_mock_evidence/0` helper returning `%Contracts.ReconciliationEvidence{}` with
    `source: :storefront`, `provider: "mock"`, `provider_reference: "mock_txn_sub_pro_monthly"`,
    `event_kind: "purchase"`, `evidence_ref: "mock_evt_sub_pro_monthly_purchase"`,
    `captured_at: "2026-05-29T00:00:00Z"` (fixed). Module attribute `@group_id "sub_pro_monthly"`
    anchoring MockBackend/@subscription_entry_id.

    No fenced code blocks in this action — replicate shapes from the <read_first> files. Tasks 2-4
    add the actual test bodies; this task establishes the loadable skeleton + fixtures and must compile.
  </action>
  <verify>
    <automated>mix compile --warnings-as-errors 2>&1 | tail -5; mix test test/crosswake/proof/phase34_paywall_corridor_proof_test.exs 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - File compiles: `mix compile --warnings-as-errors` is clean (no undefined-function/unused warnings
      from the file or the four Code.require_file'd modules).
    - `mix test test/crosswake/proof/phase34_paywall_corridor_proof_test.exs` runs (0 tests at this point
      is acceptable; it must NOT fail to load/compile).
    - File begins with exactly FOUR `Code.require_file(...)` lines in order: reconciliation_keys,
      reconciliation_inbox, entitlement_projection, mock_backend.
    - File contains `use ExUnit.Case, async: false`.
    - File contains NO `@moduletag :requires_example_host`.
    - Private builders are `phase34_`-prefixed and use `struct!` with fixed timestamp strings.
  </acceptance_criteria>
  <done>The proof file exists, compiles warnings-clean, loads the four pure commerce modules in
  dependency order, is untagged + async:false, and exposes phase34_-prefixed inline fixture builders.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Four-state coverage (SC#1) + :pending -> :granted transition (SC#2) assertions</name>
  <files>test/crosswake/proof/phase34_paywall_corridor_proof_test.exs</files>
  <read_first>
    - examples/phoenix_host/lib/crosswake_example/commerce/entitlement_projection.ex (lines 11-13,
      39-63) — `@pending_reconciliation_states`, `@grantable_authority_states`,
      `@verified_reconciliation_states`; the `derived_state/1` cond precedence
      (stale -> pending -> granted -> denied); `granted_snapshot?/1` (fresh + resolved_reconciliation?
      + grantable authority + access :granted); `resolved_reconciliation?/1` (true only for
      :projection_refreshed). The exact source for the four-state anchors.
    - examples/phoenix_host/lib/crosswake_example/commerce/reconciliation_inbox.ex (lines 14-44) —
      `ingest_evidence/2` returns `{:ok, map()}` (NOT a struct); `evidence_status/1`:
      `@success_like_event_kinds` includes "purchase" -> `:awaiting_verification`. Access `result.status`.
    - examples/phoenix_host/lib/crosswake_example/commerce/mock_backend.ex (lines 106-141) —
      `build_verified_snapshot(evidence, group_id)` returns `%EntitlementSnapshot{}` directly
      (NOT `{:ok, _}`), reconciliation.state :projection_refreshed, authority :active, access :granted,
      freshness :fresh. DO NOT call `verify_and_broadcast/2` (it touches Phoenix.PubSub — Pitfall 2).
    - 36-RESEARCH.md "Four-state assertions" + "Full pipeline" code examples (lines 674-746) — the
      exact override maps proven to route to each state.
  </read_first>
  <action>
    Add a describe block "four derived states (SC#1)" with FOUR distinct assertions, each on a
    DIFFERENT snapshot built via phase34_snapshot/1 (Pitfall 1 anti-vacuity — specify the FULL
    distinguishing override per state so no base default leaks):
    - `:stale` — `phase34_snapshot(%{freshness: phase34_freshness_lane(:stale)})` -> assert
      `EntitlementProjection.derived_state(snap) == :stale`. (Stale wins over everything.)
    - `:pending` — `phase34_snapshot(%{reconciliation: phase34_reconciliation_lane(:awaiting_verification)})`
      (base freshness :fresh) -> assert `== :pending`.
    - `:denied` — `phase34_snapshot()` with base defaults (freshness :fresh, reconciliation
      :projection_refreshed, access :denied -> granted_snapshot? false -> :denied fallthrough)
      -> assert `== :denied`. Make the non-granting intent explicit in the assertion message.
    - `:granted` — route through SHIPPED MockBackend: `verified = MockBackend.build_verified_snapshot(phase34_mock_evidence(), @group_id)`;
      `{:ok, projected} = EntitlementProjection.project_snapshot(nil, verified)`; assert
      `EntitlementProjection.derived_state(projected) == :granted`. (Anti-vacuity per SC#2 mitigation:
      :granted is NOT a hand-built snapshot.)

    Add a describe block ":pending -> :granted transition (SC#2)" asserting the three-step real path
    (D-05): (1) `{:ok, result} = ReconciliationInbox.ingest_evidence(phase34_mock_evidence())`;
    `assert result.status == :awaiting_verification` (the :pending origin / WIRE-01). (2) explicit derived
    `:pending`: `assert EntitlementProjection.derived_state(phase34_snapshot(%{reconciliation:
    phase34_reconciliation_lane(:awaiting_verification)})) == :pending`. (3) verified -> project ->
    granted: `verified = MockBackend.build_verified_snapshot(phase34_mock_evidence(), @group_id)`;
    `{:ok, projected} = EntitlementProjection.project_snapshot(nil, verified)`;
    `assert EntitlementProjection.derived_state(projected) == :granted`. Call `project_snapshot(nil, _)`
    (nil first arg skips monotonicity — Pitfall 6); never pass a non-nil current with a freshly-built verified.

    No fenced code blocks — replicate from <read_first>. Do NOT call verify_and_broadcast/2.
  </action>
  <verify>
    <automated>mix test test/crosswake/proof/phase34_paywall_corridor_proof_test.exs 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - File contains FOUR distinct `EntitlementProjection.derived_state(...) == :stale|:pending|:denied|:granted`
      assertions, each on a different snapshot.
    - The `:granted` assertion routes through `MockBackend.build_verified_snapshot/2` then
      `EntitlementProjection.project_snapshot(nil, _)` (not a hand-built granted snapshot).
    - File contains `assert result.status == :awaiting_verification` from
      `ReconciliationInbox.ingest_evidence(...)` (the example-host inbox, result is a MAP).
    - File contains the verified -> `{:ok, projected} = project_snapshot(nil, verified)` ->
      `derived_state(projected) == :granted` transition.
    - File does NOT call `verify_and_broadcast/2`.
    - `mix test test/crosswake/proof/phase34_paywall_corridor_proof_test.exs` exits 0.
    - Anti-vacuity self-check: flipping any one of the four state assertions to a wrong expected value
      makes that test FAIL (note this confirmation in the SUMMARY).
  </acceptance_criteria>
  <done>All four states + the :pending->:granted transition are asserted distinctly via the real
  shipped pipeline, and the test is green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Mock-boundary fence (SC#3 / PROOF-03, D-06 three real truths)</name>
  <files>test/crosswake/proof/phase34_paywall_corridor_proof_test.exs</files>
  <read_first>
    - lib/crosswake/commerce/reconciliation.ex (lines 141-142) —
      `authority_mutation_allowed_from_evidence?(%Contracts.ReconciliationEvidence{})` returns `false`
      unconditionally (the fence anchor, D-06.1). In the compilation path — call fully-qualified, NO
      require_file. NOTE: a lib `ingest_evidence/2` (~line 114) returns `{:ok, %EvidenceResult{}}` — do
      NOT use it; the proof's ingest path is the example-host ReconciliationInbox (Pitfall 3).
    - examples/phoenix_host/lib/crosswake_example/commerce/entitlement_projection.ex (the
      `ensure_verified_reconciliation/1` / `@verified_reconciliation_states` gate and
      `{:error, :unverified_reconciliation_outcome}` path) — `:awaiting_verification` is NOT verified ->
      project_snapshot returns the error; `:verification_failed` IS verified (passes the gate) but is NOT
      `:projection_refreshed` so resolved_reconciliation?/1 is false -> not granted (D-06.2 / D-06.3).
    - 36-RESEARCH.md "Mock-boundary fence assertions" code example (lines 708-732) and the SC#3 row of
      the Success-Criteria -> Assertion Map (lines 786-790) for the exact asserted forms and the
      "documents lib contract" message guidance.
  </read_first>
  <action>
    Add a describe block "mock-boundary fence (SC#3 / PROOF-03)" with the THREE real truths (D-06 —
    NOT the literal-but-false SC#3 wording; the verifier evaluates SC#3 against this reinterpretation):
    - D-06.1: `assert Crosswake.Commerce.Reconciliation.authority_mutation_allowed_from_evidence?(phase34_mock_evidence()) == false`.
      Assertion message states this documents the lib contract (the function returns false
      unconditionally for any ReconciliationEvidence) — it is the anti-grant fence anchor, not a branch test.
    - D-06.2: build an UNVERIFIED snapshot `phase34_snapshot(%{reconciliation:
      phase34_reconciliation_lane(:awaiting_verification)})` and
      `assert {:error, :unverified_reconciliation_outcome} = EntitlementProjection.project_snapshot(nil, unverified)`.
      Raw ingested (awaiting-verification) evidence cannot be projected.
    - D-06.3: build a VERIFIED-BUT-NOT-REFRESHED snapshot `phase34_snapshot(%{reconciliation:
      phase34_reconciliation_lane(:verification_failed), authority: phase34_authority_lane(:active),
      access: phase34_access_lane(:granted), freshness: phase34_freshness_lane(:fresh)})`. Assert it
      passes the gate (`{:ok, projected} = EntitlementProjection.project_snapshot(nil, vfailed_snap)`)
      and then `refute EntitlementProjection.derived_state(projected) == :granted` — only a real
      `:projection_refreshed` outcome grants. Together: mock evidence can never directly grant authority.

    No fenced code blocks — replicate from <read_first>.
  </action>
  <verify>
    <automated>mix test test/crosswake/proof/phase34_paywall_corridor_proof_test.exs 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - File contains `assert ...authority_mutation_allowed_from_evidence?(...) == false` (fully-qualified
      lib call, no require_file).
    - File contains `{:error, :unverified_reconciliation_outcome}` assertion for an :awaiting_verification
      snapshot via `project_snapshot(nil, _)`.
    - File contains a `:verification_failed` snapshot that projects `{:ok, _}` but whose derived state is
      `refute`'d against `:granted`.
    - `mix test test/crosswake/proof/phase34_paywall_corridor_proof_test.exs` exits 0.
  </acceptance_criteria>
  <done>The fence is asserted as the three real D-06 truths and the test is green — proving mock evidence
  can never directly grant entitlement authority.</done>
</task>

<task type="auto">
  <name>Task 4: Self-scan hermeticity guard (SC#4) + green run under CI flags + confirm phase34-proof.yml discovery</name>
  <files>test/crosswake/proof/phase34_paywall_corridor_proof_test.exs</files>
  <read_first>
    - test/crosswake/proof/phase23_commerce_support_proof_test.exs (lines 509-549) — the richer
      self-scan guard: split source into lines, filter `Code.require_file(` call lines by call-form
      regex, assert each allowed; escape-hatch regex patterns. The model for the require-line scan
      (produces a useful failure message naming the offending line).
    - test/crosswake/proof/phase33_commerce_corridor_routes_test.exs (lines 88-99) — the simpler
      token-presence self-scan form.
    - test/crosswake/proof/phase34_mock_storefront_test.exs (lines 34-40) — the concatenated-token
      construction technique (`"store" <> "kit"`) that prevents the guard from matching its own source.
    - 36-RESEARCH.md "Self-scan guard mechanics — D-03 design" (lines 565-607) — the exact four
      assertions and the concatenation requirement.
    - .github/workflows/phase34-proof.yml (the hermetic job step ~lines 75-83) — confirm it runs
      `mix compile --warnings-as-errors` then `mix test --exclude requires_example_host` (a glob, not a
      file enumeration), so the untagged file is auto-discovered (D-09). No workflow edit unless it
      enumerates files.
  </read_first>
  <action>
    Add a describe block "hermeticity self-scan guard (SC#4 / D-03)". Read the proof's own source:
    `source = File.read!(__ENV__.file)` (downcased for substring checks where appropriate). Assert:
    1. No runtime-path require_file: split source into lines, filter those matching a call-form regex
       (`~r/^\s*code\.require_file\s*\(/i`); for each such line, `refute String.contains?(line, forbidden)`
       for forbidden runtime substrings `["_live", "endpoint", "application", "router", "repo", "_web"]`,
       with a message naming the offending line. Additionally assert each require line points at one of the
       FOUR allowed pure-commerce modules (reconciliation_keys, reconciliation_inbox, entitlement_projection,
       mock_backend) — only those four may be required.
    2. No process-start/server tokens in the body. Build forbidden tokens via CONCATENATION so the guard
       does not match its own assertion lines: `"start" <> "_supervised"`, `"phoenix" <> ".pubsub"`,
       `"genserver" <> ".start"`, `"endpoint"` (already covered above but check call-form), `"liveviewtest"`.
       Use `refute Regex.match?(...)` on call-form regexes or `refute String.contains?(source, token)`.
       (Pitfall 4 — concatenation prevents self-matching.)
    3. `assert String.contains?(source, "async: false")` (build the literal carefully so it does not need
       to dodge itself; use the concatenation idiom if necessary).
    4. `refute String.contains?(source, "@module" <> "tag :requires_example_host")` — file must be untagged.

    After implementing, run the proof under the CI flag combination to mirror SC#5:
    `mix compile --warnings-as-errors` then `mix test --exclude requires_example_host`. Confirm the new
    untagged file is picked up by the glob and passes. Confirm `.github/workflows/phase34-proof.yml`
    globs `test/crosswake/proof/` (D-09) — make NO workflow change if it globs (it does, per research);
    only add an explicit path reference if the workflow enumerates files. Record the discovery confirmation
    in the SUMMARY.
  </action>
  <verify>
    <automated>mix compile --warnings-as-errors 2>&1 | tail -3; mix test --exclude requires_example_host 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - File contains a self-scan guard test reading `File.read!(__ENV__.file)`.
    - Guard filters require_file lines by call-form regex (not bare substring) and asserts only the four
      allowed pure-commerce paths; forbids runtime substrings (`_live`, `endpoint`, `application`,
      `router`, `repo`, `_web`).
    - Guard forbids process/server tokens built via concatenation (so the guard does not trip on its own source).
    - Guard asserts `async: false` present and `@moduletag :requires_example_host` absent.
    - `mix compile --warnings-as-errors` is clean (SC#5).
    - `mix test --exclude requires_example_host` exits 0 with the new file included (the full hermetic
      lane is green).
    - `.github/workflows/phase34-proof.yml` is unmodified and confirmed to auto-discover the file via glob
      (or, if it enumerates, a single path line is added — note which in the SUMMARY).
  </acceptance_criteria>
  <done>The self-scan guard structurally enforces hermeticity, the full hermetic lane passes under the CI
  flag combination, compilation is warnings-clean, and phase34-proof.yml auto-discovery is confirmed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | This phase adds a single hermetic ExUnit test. No untrusted input crosses any boundary. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-36-NA | N/A | test/crosswake/proof/phase34_paywall_corridor_proof_test.exs | accept | No applicable STRIDE category. The deliverable is a hermetic ExUnit test with no inputs, no network, no persistence, no process start, no authentication, no external services, and no changes to shipped/runtime code. Spoofing/Tampering/Repudiation/Info-disclosure/DoS/EoP each require a runtime trust boundary or attacker-reachable surface — none exists. The self-scan guard (Task 4) additionally enforces structurally that no network/process/server escape hatch is introduced. |
| T-36-SC | Tampering | npm/pip/cargo installs | accept | No package-manager installs in this phase (zero new dependencies — confirmed in RESEARCH "Standard Stack"). Supply-chain threat is not reachable. |
</threat_model>

<verification>
- `mix compile --warnings-as-errors` is clean (SC#5 — CI promotes warnings to errors).
- `mix test test/crosswake/proof/phase34_paywall_corridor_proof_test.exs` exits 0.
- `mix test --exclude requires_example_host` exits 0 with the new untagged file auto-discovered (SC#5 / D-09).
- The file contains four distinct `derived_state(...) == :stale|:pending|:denied|:granted` assertions (SC#1).
- The :pending->:granted transition asserts `result.status == :awaiting_verification` then verified ->
  `project_snapshot(nil, _)` -> `derived_state == :granted` via `MockBackend.build_verified_snapshot/2` (SC#2).
- The fence asserts the three D-06 truths: `authority_mutation_allowed_from_evidence?/1 == false`,
  `{:error, :unverified_reconciliation_outcome}` for unverified, and `:verification_failed` not granting (SC#3, reinterpreted).
- The self-scan guard enforces async:false, untagged, and four-only pure-commerce require_file paths (SC#4, reinterpreted).
- No changes to `lib/crosswake/commerce/*` or any example-host module.
</verification>

<success_criteria>
- ONE new file `test/crosswake/proof/phase34_paywall_corridor_proof_test.exs` exists; nothing else modified.
- All four `derived_state/1` outcomes asserted distinctly (PROOF-01 / SC#1).
- `:pending` -> `:granted` transition asserted via the real shipped pipeline (PROOF-01 / SC#2).
- Mock-boundary fence asserted as the three real D-06 truths (PROOF-03 / SC#3, reinterpreted).
- Self-scan hermeticity guard present; file async:false + untagged (SC#4, reinterpreted).
- Full hermetic lane green under `mix test --exclude requires_example_host` with `mix compile --warnings-as-errors` clean (SC#5).
</success_criteria>

<output>
Create `.planning/phases/36-hermetic-proof-lane/36-01-SUMMARY.md` when done. Record:
- The confirmed require_file load order and whether reconciliation_keys.ex is a real dependency.
- The anti-vacuity self-check result (flipping a state assertion fails as expected).
- Confirmation that phase34-proof.yml auto-discovers the file (no workflow change), or the single path line added.
</output>
