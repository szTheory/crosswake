---
phase: 33-corridor-routes-and-ci-infrastructure
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .github/workflows/phase34-proof.yml
autonomous: true
requirements: [PROOF-02]
must_haves:
  truths:
    - "A .github/workflows/phase34-proof.yml file exists with two jobs: a hermetic merge-blocking job and an advisory job"
    - "The hermetic job runs `mix compile --warnings-as-errors` then `mix test --exclude requires_example_host` and is guarded to run on pull_request / push / workflow_dispatch (not schedule)"
    - "The advisory job has `continue-on-error: true` and is guarded to run only on schedule / workflow_dispatch, so it never gates a merge"
    - "The workflow carries the 4-condition promotion_path comment block mirroring phase23-proof.yml, with milestone references updated to v3.4 / Phase 34"
  artifacts:
    - path: ".github/workflows/phase34-proof.yml"
      provides: "Two-job hermetic+advisory CI split gating every v3.4 PR"
      contains: "continue-on-error: true"
  key_links:
    - from: ".github/workflows/phase34-proof.yml"
      to: "mix test --exclude requires_example_host"
      via: "hermetic job run step"
      pattern: "mix test --exclude requires_example_host"
    - from: ".github/workflows/phase34-proof.yml advisory job"
      to: "merge gate"
      via: "schedule/workflow_dispatch if-guard + continue-on-error"
      pattern: "continue-on-error: true"
---

<objective>
Create the `phase34-proof.yml` two-job CI workflow that becomes the standing merge gate for the
rest of milestone v3.4: a hermetic merge-blocking job and an advisory job that can never block a
merge.

Purpose: Satisfies PROOF-02. The hermetic lane runs the broad `mix test --exclude
requires_example_host` suite on every PR so the Phase 36 proof file is picked up automatically by
tag discipline; the advisory lane documents where future provider/storefront/device checks will
live without ever gating merges. Mirrors the established `phase23-proof.yml` posture exactly.

Output: A new `.github/workflows/phase34-proof.yml` — a direct structural copy-and-adapt of
`.github/workflows/phase23-proof.yml` with the documented swaps.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/33-corridor-routes-and-ci-infrastructure/33-CONTEXT.md
@.planning/phases/33-corridor-routes-and-ci-infrastructure/33-RESEARCH.md
@.planning/phases/33-corridor-routes-and-ci-infrastructure/33-PATTERNS.md
@.github/workflows/phase23-proof.yml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create phase34-proof.yml as a two-job hermetic+advisory split adapted from phase23-proof.yml</name>
  <files>.github/workflows/phase34-proof.yml</files>
  <read_first>
    - .github/workflows/phase23-proof.yml (the entire 179-line source template — copy structure, triggers, BEAM versions, both `if:` guards, `continue-on-error: true`, and the promotion_path comment block lines 18-32 verbatim before adapting milestone references)
    - .planning/phases/33-corridor-routes-and-ci-infrastructure/33-PATTERNS.md (lines 128-247: documented swaps and the four critical structural invariants)
    - .planning/phases/33-corridor-routes-and-ci-infrastructure/33-CONTEXT.md (D-06..D-10: job topology, triggers, filename lock)
    - .planning/ROADMAP.md (Phase 33 Success Criteria #3 and #4)
  </read_first>
  <action>
    Create `.github/workflows/phase34-proof.yml` (D-10 — filename is phase34-proof.yml, NOT phase33; it is named for the milestone proof surface it gates). Copy the full structure of `.github/workflows/phase23-proof.yml` and apply exactly these swaps:

    Top of file: `name: Phase 34 Proof`. Adapt the header comment framing from "Phase 23 / v3.2 commerce support" to "Phase 34+ / v3.4 paywall corridor proof", but copy the 4-condition `promotion_path` comment block (phase23 lines 18-28) VERBATIM (conditions 1-4 are milestone-agnostic and stay unchanged; D-06).

    `on:` triggers (copy verbatim from phase23 lines 34-43): `pull_request:`, `push: branches: [main]`, `workflow_dispatch:`, and `schedule: - cron: "0 6 * * 1"`.

    Hermetic job `merge-blocking-commerce-proof` (D-07): keep `name:`, `runs-on: macos-15`, `timeout-minutes: 20`, and the `if:` guard `${{ github.event_name == 'pull_request' || github.event_name == 'push' || github.event_name == 'workflow_dispatch' }}` (skips on schedule). Steps: Checkout (`actions/checkout@v6`), Setup BEAM (`erlef/setup-beam@v1`, elixir 1.19.5 / otp 27.3), `mix deps.get`, then `mix compile --warnings-as-errors` as a SEPARATE explicit step (RESEARCH.md Pitfall 4). REPLACE the phase23 explicit-file test steps (phase23 lines 77-97) with a SINGLE broad hermetic run step: `run: mix test --exclude requires_example_host`. This auto-discovers the Phase 36 proof file because it stays untagged (D-07/D-08); no per-file path list is maintained.

    Advisory job `advisory-commerce-proof` (D-09): keep `name:`, `runs-on: ubuntu-latest`, `timeout-minutes: 30`, the `if:` guard `${{ github.event_name == 'schedule' || github.event_name == 'workflow_dispatch' }}`, AND `continue-on-error: true` (both required — RESEARCH.md Pitfall 3; never gates merge). Steps: Checkout, Setup BEAM, `mix deps.get`, then the three placeholder echo steps (StoreKit simulator / Play Billing test-track / Device-or-storefront smoke) plus the advisory-lane status-summary `::notice::` step. Keep the echo content as advisory placeholders; update any "v3.2"/"Phase 23" milestone references in echo and comment text to the v3.4 paywall corridor surface.

    Do not add the advisory job to any required status check. The two `if:` guards together guarantee the advisory job never appears as a required check on PRs.
  </action>
  <verify>
    <automated>test -f .github/workflows/phase34-proof.yml && grep -q 'continue-on-error: true' .github/workflows/phase34-proof.yml && grep -q 'mix test --exclude requires_example_host' .github/workflows/phase34-proof.yml && python3 -c "import yaml; yaml.safe_load(open('.github/workflows/phase34-proof.yml'))" && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `.github/workflows/phase34-proof.yml` exists and parses as valid YAML (`python3 -c "import yaml; yaml.safe_load(open('.github/workflows/phase34-proof.yml'))"` exits 0).
    - File top-level `name:` is `Phase 34 Proof`.
    - File contains two jobs whose keys are `merge-blocking-commerce-proof` and `advisory-commerce-proof`.
    - Hermetic job contains `mix compile --warnings-as-errors` AND `mix test --exclude requires_example_host` as run steps, and its `if:` guard matches `pull_request || push || workflow_dispatch` (NOT schedule).
    - Advisory job contains `continue-on-error: true` AND its `if:` guard is `${{ github.event_name == 'schedule' || github.event_name == 'workflow_dispatch' }}`.
    - File contains the 4-condition promotion_path comment text (grep for the four numbered conditions, e.g. the phrase `requires:` and the four `1.`/`2.`/`3.`/`4.` lines) and references the v3.4 / Phase 34 milestone surface.
    - File contains the `schedule:` trigger with `cron: "0 6 * * 1"` and all four `on:` events (`pull_request`, `push`, `workflow_dispatch`, `schedule`).
  </acceptance_criteria>
  <done>`phase34-proof.yml` exists as a valid two-job workflow: hermetic merge-blocking lane running `mix compile --warnings-as-errors` then `mix test --exclude requires_example_host`, and an advisory lane with `continue-on-error: true` guarded to schedule/dispatch only, carrying the verbatim 4-condition promotion_path comment.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI runner -> repo | The workflow executes only trusted, checked-in repo commands (`mix deps.get`, `mix compile`, `mix test`, echo placeholders). No third-party secrets, no untrusted input, no provider SDK, no network beyond Hex dependency fetch via the pinned BEAM setup action. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-33-CI-01 | Tampering | Advisory job accidentally gating merge | mitigate | Advisory job carries BOTH `continue-on-error: true` AND an `if:` guard restricting it to `schedule`/`workflow_dispatch`; verified by grep assertions in acceptance criteria (RESEARCH.md Pitfall 3). |
| T-33-CI-02 | Tampering | Hermetic lane skipping warnings detection | mitigate | `mix compile --warnings-as-errors` kept as an explicit separate step before `mix test` (RESEARCH.md Pitfall 4). |
| T-33-CI-03 | Elevation of Privilege | Pinned GitHub Action versions | accept | Reuses the same `actions/checkout@v6` and `erlef/setup-beam@v1` references as the existing phase23-proof.yml (project-established posture). Retroactive SHA-pinning of pre-v3.3 proof workflows is explicitly deferred (CONTEXT.md Deferred Ideas); not in this phase's scope. |
| T-33-SC | Tampering | npm/pip/cargo installs | mitigate | N/A — zero package installs this phase. No `[ASSUMED]`/`[SUS]` packages; no install checkpoint required. |
</threat_model>

<verification>
- `.github/workflows/phase34-proof.yml` exists and is valid YAML.
- `grep -c 'continue-on-error: true' .github/workflows/phase34-proof.yml` >= 1 (advisory job only).
- `grep -q 'mix test --exclude requires_example_host' .github/workflows/phase34-proof.yml`.
- The advisory `if:` guard restricts to schedule/workflow_dispatch; the hermetic `if:` guard restricts to pull_request/push/workflow_dispatch.
- The promotion_path 4-condition comment block is present with v3.4 / Phase 34 references.
</verification>

<success_criteria>
- `phase34-proof.yml` exists with a hermetic merge-blocking job and an advisory job (`continue-on-error: true`), mirroring phase23-proof.yml (Success Criterion #3).
- The hermetic job runs `mix test --exclude requires_example_host` and is the only merge-gating job; the advisory job never gates a merge (Success Criterion #4).
- The 4-condition promotion_path comment block is present, updated for the v3.4 milestone surface.
</success_criteria>

<output>
Create `.planning/phases/33-corridor-routes-and-ci-infrastructure/33-02-SUMMARY.md` when done.
</output>
