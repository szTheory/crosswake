---
phase: 33-corridor-routes-and-ci-infrastructure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - examples/phoenix_host/lib/crosswake_example/router.ex
  - test/crosswake/proof/phase33_commerce_corridor_routes_test.exs
autonomous: true
requirements: [PWAL-01]
must_haves:
  truths:
    - "examples/phoenix_host router declares a /commerce scope with paywall_entry (live), purchase_intent (post), and restore_intent (post) corridor routes"
    - "The example host compiles cleanly under mix compile --warnings-as-errors with PaywallEntryLive and CorridorController forward-referenced (no stub modules)"
    - "Manifest.compile(CrosswakeExample.Router) discovers a subscription_default commerce corridor whose role_ownership maps paywall_entry to :phoenix_owned and purchase_intent/restore_intent to :native_or_companion_required"
    - "The paywall route declaration uses the canonical adopter-facing DSL: crosswake: [id: ..., runtime: :live_view, commerce: [corridor: :subscription_default, role: :paywall_entry]]"
  artifacts:
    - path: "examples/phoenix_host/lib/crosswake_example/router.ex"
      provides: "Three subscription_default corridor route declarations + two @compile no_warn_undefined forward-reference lines"
      contains: "scope \"/commerce\""
    - path: "test/crosswake/proof/phase33_commerce_corridor_routes_test.exs"
      provides: "Manifest-introspection assertion that the three corridor routes land with correct role_ownership"
      contains: "Manifest.compile(CrosswakeExample.Router)"
  key_links:
    - from: "examples/phoenix_host/lib/crosswake_example/router.ex"
      to: "Crosswake.Policy.Schema.validate_commerce_declaration/1"
      via: "crosswake_defaults macro -> Route.new! at compile time"
      pattern: "commerce: \\[corridor: :subscription_default"
    - from: "examples/phoenix_host/lib/crosswake_example/router.ex"
      to: "Crosswake.Manifest.Builder.commerce_corridor_registry/1"
      via: "Manifest.compile reads route commerce metadata"
      pattern: "subscription_default"
---

<objective>
Declare the three `subscription_default` corridor routes in the `examples/phoenix_host`
router so they land in the runtime manifest with correct corridor role-ownership metadata,
and forward-reference the two Phase 35 target modules without creating stub modules.

Purpose: Establishes the adopter-facing teaching artifact (PWAL-01) — the canonical commerce
route DSL an adopter copies — and seeds the full corridor role topology into the manifest that
Phases 34-37 build on. This phase declares the scaffold only: no LiveView/controller bodies, no
commerce logic.

Output: A modified `router.ex` carrying a `/commerce` scope with one `live` route
(`paywall_entry`) and two `post` controller routes (`purchase_intent`, `restore_intent`), plus
two `@compile {:no_warn_undefined, ...}` lines; and a `:requires_example_host`-tagged proof test
asserting the routes land in the manifest with correct `role_ownership`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/33-corridor-routes-and-ci-infrastructure/33-CONTEXT.md
@.planning/phases/33-corridor-routes-and-ci-infrastructure/33-RESEARCH.md
@.planning/phases/33-corridor-routes-and-ci-infrastructure/33-PATTERNS.md

# CRITICAL CORRECTION TO SOURCE-ARTIFACT DSL SHAPE
# The router block recommended verbatim in 33-CONTEXT.md (lines 138-151),
# 33-RESEARCH.md (lines 147-163), and 33-PATTERNS.md (lines 79-93) is API-INCORRECT
# and will NOT compile against the real Crosswake.Router macro. The locked DECISIONS
# (D-01..D-05: which routes, corridor, roles, live-vs-post, forward-refs) are honored
# exactly; only the SYNTAX is corrected to the verified working form. Evidence:
#   - lib/crosswake/router.ex:38 — `crosswake_defaults(defaults, do: block)` has NO
#     arity-1/bare form. `crosswake_defaults do ... end` (no defaults arg) FAILS to compile.
#   - lib/crosswake/router.ex:100-114 + lib/crosswake/router/scope_defaults.ex:64-73 —
#     only the `crosswake:` keyword is processed; `commerce:` is a key INSIDE `crosswake:`,
#     NOT a top-level sibling route option (schema.ex:50-53 lists `commerce` as a `crosswake`
#     schema field alongside `id`, `runtime`).
#   - lib/crosswake/policy/route.ex:11 — `@enforce_keys [:id, :runtime]`; every route needs
#     an `id` and a `runtime` in `[:live_view | :offline_island | :native_screen]`.
# The verified canonical form (production usage) is in:
@test/crosswake/proof/phase23_commerce_support_proof_test.exs   # lines 42-86: working commerce DSL
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add /commerce corridor scope + forward-reference @compile lines to the example router</name>
  <files>examples/phoenix_host/lib/crosswake_example/router.ex</files>
  <read_first>
    - examples/phoenix_host/lib/crosswake_example/router.ex (the file being modified; existing scopes lines 46-217, existing @compile line 28)
    - test/crosswake/proof/phase23_commerce_support_proof_test.exs (lines 42-86: verified working commerce DSL — commerce nested inside crosswake with id + runtime)
    - lib/crosswake/router.ex (lines 38-67: crosswake_defaults REQUIRES a defaults arg; live/3 and http verb macro arities)
    - lib/crosswake/router/scope_defaults.ex (lines 64-73: only `crosswake:` key is merged/processed; `commerce:` lives inside it)
    - lib/crosswake/policy/schema.ex (lines 50-53 commerce schema field; 80-85 commerce_role values; 124-127 atom-to-string normalization)
    - lib/crosswake/policy/route.ex (lines 11-24: @enforce_keys [:id, :runtime]; runtime must be a valid runtime value)
    - lib/crosswake/policy/corridor_profiles.ex (lines 17-34: role_ownership map for subscription_default)
  </read_first>
  <action>
    In examples/phoenix_host/lib/crosswake_example/router.ex:

    (a) Immediately after the existing `@compile {:no_warn_undefined, CrosswakeExample.Crosswake.Policy}` (line 28), add TWO new lines — ONE module per line (list form is not used in this codebase, per PATTERNS.md line 70; per D-05):
        `@compile {:no_warn_undefined, CrosswakeExample.PaywallEntryLive}`
        `@compile {:no_warn_undefined, CrosswakeExample.CorridorController}`
    Do NOT create stub modules for either (D-04 — Phoenix routes are quoted AST and compile without targets; the manifest builder reads only route `commerce:` metadata, never the target module).

    (b) Add a new scope block at the end of the router module (before the module's closing `end`) — this is D-01 (declare all three corridor routes in a `scope "/commerce", CrosswakeExample` block so every role_ownership entry lands in the manifest, satisfying Success Criterion #2):
        - `scope "/commerce", CrosswakeExample do` with `pipe_through [:browser]`.
        - Inside it, wrap the three routes in `crosswake_defaults runtime: :live_view, offline: :unavailable, security: :standard do`. The defaults arg is MANDATORY (the macro has no bare form — see read_first router.ex:38). These defaults are inherited only as fallbacks into each route's `crosswake:` block; each route overrides `runtime:` explicitly below so the defaults do not misrepresent ownership (addresses RESEARCH.md Pitfall 1: post routes must not silently read as `:live_view`).
        - Route 1 (D-02, paywall_entry, Phoenix-owned): `live "/paywall", PaywallEntryLive, :index, crosswake: [id: "commerce-paywall-entry", runtime: :live_view, commerce: [corridor: :subscription_default, role: :paywall_entry]]`
        - Route 2 (D-03, purchase_intent, native_or_companion_required): `post "/purchase", CorridorController, :purchase, crosswake: [id: "commerce-purchase-intent", runtime: :native_screen, commerce: [corridor: :subscription_default, role: :purchase_intent]]`
        - Route 3 (D-03, restore_intent, native_or_companion_required): `post "/restore", CorridorController, :restore, crosswake: [id: "commerce-restore-intent", runtime: :native_screen, commerce: [corridor: :subscription_default, role: :restore_intent]]`

    Rationale for runtime values: `paywall_entry` is `:phoenix_owned` -> `runtime: :live_view` (honest Phoenix screen). `purchase_intent`/`restore_intent` are `:native_or_companion_required` -> `runtime: :native_screen` (honest native-owned seam; matches the verified PurchaseCorridorRouter fixture in phase23 proof test line 61-66). Use the atom form `corridor: :subscription_default` (NOT the string form) — the validator normalizes atoms via Atom.to_string/1 (schema.ex:126); atom form is the canonical adopter-facing DSL and satisfies Success Criterion #1.

    Each route's `commerce:` MUST be nested inside its `crosswake:` keyword list with a unique `id:` — it is NOT a top-level route option. This corrects the API-incorrect block shown verbatim in CONTEXT/RESEARCH/PATTERNS (see <context> note).
  </action>
  <verify>
    <automated>cd examples/phoenix_host && mix compile --warnings-as-errors</automated>
  </verify>
  <acceptance_criteria>
    - `examples/phoenix_host/lib/crosswake_example/router.ex` contains the literal string `scope "/commerce"`.
    - The file contains `@compile {:no_warn_undefined, CrosswakeExample.PaywallEntryLive}` and `@compile {:no_warn_undefined, CrosswakeExample.CorridorController}` on separate lines.
    - The file contains all three of: `role: :paywall_entry`, `role: :purchase_intent`, `role: :restore_intent`, each on a route carrying `commerce: [corridor: :subscription_default, ...]` nested inside a `crosswake:` keyword list.
    - The paywall route is a `live` route with `runtime: :live_view`; the purchase and restore routes are `post` controller routes (`CorridorController`, `:purchase`/`:restore`) with `runtime: :native_screen`.
    - No `PaywallEntryLive` or `CorridorController` module is defined anywhere in `examples/phoenix_host/` (forward-reference only, D-04): `grep -rL` confirms no `defmodule CrosswakeExample.PaywallEntryLive` / `defmodule CrosswakeExample.CorridorController`.
    - `cd examples/phoenix_host && mix compile --warnings-as-errors` exits 0.
  </acceptance_criteria>
  <done>The example router declares the three corridor routes in the corrected canonical DSL and compiles warning-free with both Phase 35 modules forward-referenced and unstubbed.</done>
</task>

<task type="auto">
  <name>Task 2: Add a requires_example_host proof test asserting the corridor routes land in the manifest</name>
  <files>test/crosswake/proof/phase33_commerce_corridor_routes_test.exs</files>
  <read_first>
    - test/crosswake/proof/phase8_selective_native_lane_test.exs (lines 1-60: exact analog — `@moduletag :requires_example_host`, `ExampleHost.load!()` in setup_all, `Manifest.compile(CrosswakeExample.Router)`, manifest.routes introspection by path/id)
    - test/support/example_host.ex (the ExampleHost.load! loader)
    - lib/crosswake/manifest/manifest.ex (lines 17-36: Manifest.compile/1 returns {:ok, %{manifest: ...}})
    - lib/crosswake/manifest/builder.ex (lines 158-187 commerce_corridor_registry/1; 207-214 route_commerce/1)
    - lib/crosswake/policy/corridor_profiles.ex (lines 17-34: expected role_ownership values)
    - test/crosswake/manifest/manifest_test.exs (lines 67-91: commerce_corridors + routes[...].commerce assertion shape)
  </read_first>
  <action>
    Create test/crosswake/proof/phase33_commerce_corridor_routes_test.exs mirroring the phase8 selective-native lane test structure:
      - `defmodule Crosswake.Proof.Phase33CommerceCorridorRoutesTest do`, `use ExUnit.Case, async: false`.
      - Add `@moduletag :requires_example_host` (this test loads the compiled example host, so it belongs in the existing example-host CI lane / phase5-proof.yml, NOT the new hermetic `--exclude requires_example_host` lane — per D-08 only the hermetic Phase 36 proof stays untagged).
      - `alias Crosswake.Manifest`.
      - `setup_all do Crosswake.TestSupport.ExampleHost.load!(); :ok end`.
      - Test "the example host registers a subscription_default commerce corridor with correct role ownership":
        assert `{:ok, %{manifest: manifest}} = Manifest.compile(CrosswakeExample.Router)`;
        bind `corridor = manifest.commerce_corridors["subscription_default"]`; assert it is non-nil;
        assert `corridor.role_ownership.paywall_entry == :phoenix_owned`;
        assert `corridor.role_ownership.purchase_intent == :native_or_companion_required`;
        assert `corridor.role_ownership.restore_intent == :native_or_companion_required`.
      - Test "all three corridor route declarations land in the manifest with subscription_default + correct role":
        from the same compiled manifest, locate the three routes by their ids ("commerce-paywall-entry", "commerce-purchase-intent", "commerce-restore-intent") in `manifest.routes`;
        for each, assert `route.commerce.corridor_ref == "subscription_default"` (string — post-normalization) and assert `route.commerce.role` equals `:paywall_entry` / `:purchase_intent` / `:restore_intent` respectively;
        assert the paywall route `runtime == :live_view` and the purchase/restore routes `runtime == :native_screen`.
    Do not hit any HTTP route at runtime (no route handler exists in Phase 33). This is a compile-time/manifest-introspection assertion only.
  </action>
  <verify>
    <automated>mix test test/crosswake/proof/phase33_commerce_corridor_routes_test.exs --include requires_example_host</automated>
  </verify>
  <acceptance_criteria>
    - `test/crosswake/proof/phase33_commerce_corridor_routes_test.exs` exists and contains `@moduletag :requires_example_host`.
    - The test file contains `Manifest.compile(CrosswakeExample.Router)` and asserts `commerce_corridors["subscription_default"]` role_ownership for paywall_entry (:phoenix_owned), purchase_intent (:native_or_companion_required), restore_intent (:native_or_companion_required).
    - The test asserts each route's `commerce.corridor_ref == "subscription_default"` and the correct `commerce.role` atom, and the correct `runtime` per route.
    - `mix test test/crosswake/proof/phase33_commerce_corridor_routes_test.exs --include requires_example_host` exits 0 (after the example host is compiled via `cd examples/phoenix_host && mix compile`).
    - The test is EXCLUDED by `mix test --exclude requires_example_host` (verify it does not run in the hermetic lane): the command reports the module as skipped/excluded.
  </acceptance_criteria>
  <done>A tagged proof test proves the three corridor routes appear in the example host manifest with the canonical subscription_default role_ownership, and it correctly stays out of the hermetic merge-blocking lane.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none introduced this phase) | Routes are compile-time declaration artifacts. `PaywallEntryLive` and `CorridorController` are forward-referenced with NO action bodies in Phase 33, so there is no request-handling code to attack yet. No auth, no session reads, no user input flows through these routes at runtime. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-33-01 | Tampering | `commerce:` DSL values in router.ex | mitigate | Compile-time validation via `Crosswake.Policy.Schema.validate_commerce_declaration/1` (NimbleOptions custom validator) rejects malformed corridor/role values; `mix compile --warnings-as-errors` is the gate. |
| T-33-02 | Spoofing/Elevation | purchase_intent / restore_intent `post` routes | accept | No action bodies exist in Phase 33 (forward-referenced, land in Phase 35). No request handling, no entitlement grant path, no PII. The native-ownership semantics (`:native_or_companion_required`) and any auth are Phase 35 concerns. Low surface: declaration only. |
| T-33-SC | Tampering | npm/pip/cargo installs | mitigate | N/A — zero package installs this phase (no mix.exs change). No `[ASSUMED]`/`[SUS]` packages; no install checkpoint required. |
</threat_model>

<verification>
- `cd examples/phoenix_host && mix compile --warnings-as-errors` exits 0 (Success Criterion #1: canonical DSL compiles; forward-references suppressed).
- `mix test test/crosswake/proof/phase33_commerce_corridor_routes_test.exs --include requires_example_host` exits 0 (Success Criterion #2: three routes land in manifest with correct corridor metadata).
- `grep -c 'role: :paywall_entry\|role: :purchase_intent\|role: :restore_intent' examples/phoenix_host/lib/crosswake_example/router.ex` returns 3.
- No stub modules: no `defmodule CrosswakeExample.PaywallEntryLive` / `defmodule CrosswakeExample.CorridorController` anywhere under `examples/phoenix_host/`.
</verification>

<success_criteria>
- The `/commerce` scope declares paywall_entry (live), purchase_intent (post), restore_intent (post) with `commerce: [corridor: :subscription_default, role: ...]` nested inside each route's `crosswake:` block.
- `PaywallEntryLive` and `CorridorController` are forward-referenced via `@compile {:no_warn_undefined, ...}` (one line each), with no stub modules created.
- The example host compiles under `--warnings-as-errors`.
- `Manifest.compile(CrosswakeExample.Router)` yields a `subscription_default` commerce corridor with the role_ownership map from corridor_profiles.ex, and all three routes carry `corridor_ref == "subscription_default"` with the correct role.
- The manifest proof test is `:requires_example_host`-tagged and excluded by the hermetic lane.
</success_criteria>

<output>
Create `.planning/phases/33-corridor-routes-and-ci-infrastructure/33-01-SUMMARY.md` when done.
</output>
