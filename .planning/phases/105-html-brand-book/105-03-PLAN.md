---
phase: 105-html-brand-book
plan: 03
type: execute
wave: 3
depends_on: ["105-02"]
files_modified:
  - brandbook/BRAND-SPEC.md
autonomous: true
requirements: [BOOK-03]
must_haves:
  truths:
    - "brandbook/BRAND-SPEC.md v1.0 exists as the audited successor to the prompts/ seed, every AUDIT verdict applied (D-05)"
    - "Spec carries the ratified palette incl. Stone 600 #756D63, the 12-state mapping table, dark-mode hierarchy, and logo geometry numbers (D-05)"
    - "prompts/crosswake-brand-book.md is UNTOUCHED — git diff --exit-code clean (D-05)"
    - "Committed brandbook/ total is <=800KB and prompts/ is untouched (D-09 phase self-check)"
  artifacts:
    - path: "brandbook/BRAND-SPEC.md"
      provides: "Audited brand book v1.0"
      contains: "v1.0"
      min_lines: 400
  key_links:
    - from: "brandbook/BRAND-SPEC.md"
      to: "brandbook/AUDIT.md verdicts"
      via: "every TIGHTEN/ADD applied, KEEPs carried"
      pattern: "Stone 600|#756D63"
---

<objective>
Author brandbook/BRAND-SPEC.md v1.0 — the audited successor to the prompts/ seed — by carrying the seed's structure forward with every AUDIT verdict applied (KEEPs carried, TIGHTENs rewritten, ADDs included). Split into bounded tasks by section group with explicit section ranges and the AUDIT verdicts that apply to each. Final task runs the phase self-check (size <=800KB, prompts/ untouched).

Purpose: Deliver BOOK-03 — the authoritative v1.0 spec; the prompts/ draft remains the historical seed.
Output: brandbook/BRAND-SPEC.md.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/105-html-brand-book/105-CONTEXT.md
@.planning/phases/105-html-brand-book/105-VALIDATION.md

<interfaces>
Seed structure (prompts/crosswake-brand-book.md, 25 sections, ~1289 lines) to carry forward into BRAND-SPEC.md with the same kebab-case heading anchors:
  1 brand-summary, 2 brand-essence, 3 competitive-guardrails, 4 naming-system, 5 taglines-messaging,
  6 voice-and-tone, 7 visual-identity, 8 color-system, 9 typography, 10 logo-direction,
  11 graphic-elements, 12 iconography, 13 layout-ui-system, 14 documentation-brand-system, 15 microcopy-library,
  16 landing-page-direction, 17 acceptable-imagery, 18 motion-interaction, 19 ui-component-examples, 20 api-naming-tone,
  21 accessibility-standards, 22 community-oss-identity, 23 brand-do-dont-summary, 24 llm-context-block, 25 first-implementation-checklist.

AUDIT verdicts to apply (brandbook/AUDIT.md):
  - section 6 (Recommended Upgrades): the TIGHTEN/ADD list — section 8 color TIGHTEN (Stone 600 #756D63 + semantic role split: Stone 600 text.muted on light, Stone 500 -> subtle only), section 6 voice ADD (release-announcement surface), section 9/13 ADD (conference slide guidance), section 13 ADD (mobile breakpoints), social-preview ADD, section 10 logo TIGHTEN (wake-mark geometry: 20deg crossing, stroke weight, 1.5x notch, rounded caps, 16px 2-stroke simplification).
  - section 7: design token spec (primitive/semantic/state tiers) + the 12-State Mapping table (default/hover/active/focus/disabled/selected/success/warning/error/info/subtle/muted) + Stone 600 remediation.
  - section 8: logo numbers (clearspace, min sizes 128/64/24-32/16, do/don't, 5 misuse examples).
  - section 14: ratification record / final quality gate.
  No REWORK verdicts were issued (audit section 6) — the core is sound; carry KEEPs verbatim in substance.

Mark the document v1.0 in its header/front matter. Keep markdown + kebab-case anchors mirroring the seed.
</interfaces>

# Content sources:
@prompts/crosswake-brand-book.md
@brandbook/AUDIT.md
@brandbook/tokens/tokens.css
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| spec authoring | Pure markdown; no code, no external fetch, no user input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-105-04 | Tampering | prompts/ historical seed | mitigate | Never edit prompts/crosswake-brand-book.md; verify with git diff --exit-code prompts/ in the final self-check |
| T-105-SC | Tampering | npm/pip/cargo installs | mitigate | Zero installs; pure markdown authoring |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: BRAND-SPEC sections 1-9 (summary..typography), color TIGHTEN applied</name>
  <read_first>
    - prompts/crosswake-brand-book.md sections 1-9 (the seed text to carry forward)
    - brandbook/AUDIT.md section 6 (color TIGHTEN: Stone 600 #756D63, role split) and section 7 (token spec, 12-state mapping, Stone 600 remediation)
    - brandbook/tokens/tokens.css (ratified hex values)
  </read_first>
  <files>brandbook/BRAND-SPEC.md</files>
  <action>
    Create brandbook/BRAND-SPEC.md with a v1.0 header (title + "v1.0" + a one-line note that it supersedes prompts/crosswake-brand-book.md, which remains the historical seed). Author sections 1-9 carrying the seed forward with kebab-case anchors. Apply AUDIT verdicts: in section 8 color-system, add Stone 600 #756D63 to the core palette with role "text.muted on light surfaces, 4.53:1 on Foam 50 PASS"; correct the semantic role split (Stone 600 = text.muted on light; Stone 500 = subtle only); carry the ratified palette + approved pairings; include the 12-state mapping table and the token tiers from AUDIT section 7. Author by appending bounded chunks (D-08, no giant single Write — write the header + a couple sections, then append the rest section-by-section). Commit.
  </action>
  <acceptance_criteria>
    BRAND-SPEC.md exists, marked v1.0, sections 1-9 present with kebab-case anchors; color section contains Stone 600 #756D63 + corrected role split; 12-state mapping table present.
  </acceptance_criteria>
  <verify>
    <automated>test -f brandbook/BRAND-SPEC.md && grep -q "v1.0" brandbook/BRAND-SPEC.md && grep -q "#756D63" brandbook/BRAND-SPEC.md && grep -qiE "color.system|color-system" brandbook/BRAND-SPEC.md && grep -qiE "typography" brandbook/BRAND-SPEC.md && git diff --exit-code prompts/crosswake-brand-book.md</automated>
  </verify>
  <done>Sections 1-9 authored as v1.0 with color TIGHTEN (Stone 600) + 12-state mapping applied; prompts/ untouched.</done>
</task>

<task type="auto">
  <name>Task 2: BRAND-SPEC sections 10-17 (logo..acceptable-imagery), logo geometry + ADDs applied</name>
  <read_first>
    - prompts/crosswake-brand-book.md sections 10-17 (seed text to carry forward)
    - brandbook/AUDIT.md section 8 (logo numbers: clearspace, min sizes 128/64/24-32/16, do/don't, 5 misuse examples) and section 6 logo TIGHTEN (wake-mark geometry: 20deg crossing, stroke weight, 1.5x notch, rounded caps, 16px 2-stroke)
    - brandbook/AUDIT.md section 6 ADDs (conference slide guidance for section 9/13, mobile breakpoints for section 13, social-preview)
  </read_first>
  <files>brandbook/BRAND-SPEC.md</files>
  <action>
    Append sections 10-17 to brandbook/BRAND-SPEC.md (D-08, bounded appends). Apply AUDIT verdicts: section 10 logo-direction gets the TIGHTENed wake-mark geometry numbers (crossing angle 20deg, stroke weight, 1.5x stroke-width notch at the crossing, rounded caps, simplify to 2 strokes at <=16px) plus clearspace + minimum sizes (horizontal 128px, stacked 64px, icon 24px UI / 32px README, favicon 16px) and the do/don't + 5 misuse examples from AUDIT section 8; carry the ratified colorways. Fold in the mobile-breakpoints ADD into section 13 layout, and the acceptable-imagery section 17 carried from seed. Keep kebab-case anchors. Commit.
  </action>
  <acceptance_criteria>
    sections 10-17 present; logo section carries geometry numbers (20deg, notch, min sizes 128/64) + misuse examples; mobile breakpoints folded into layout; anchors kebab-case.
  </acceptance_criteria>
  <verify>
    <automated>grep -qiE "logo.direction|logo-direction" brandbook/BRAND-SPEC.md && grep -qE "128px|64px" brandbook/BRAND-SPEC.md && grep -qE "20" brandbook/BRAND-SPEC.md && grep -qiE "misuse" brandbook/BRAND-SPEC.md && git diff --exit-code prompts/crosswake-brand-book.md</automated>
  </verify>
  <done>Sections 10-17 authored with logo geometry/min-sizes/misuse + breakpoint ADD applied; prompts/ untouched.</done>
</task>

<task type="auto">
  <name>Task 3: BRAND-SPEC sections 18-25 (motion..checklist) + ratification + phase self-check</name>
  <read_first>
    - prompts/crosswake-brand-book.md sections 18-25 (seed text to carry forward)
    - brandbook/AUDIT.md section 14 (final quality gate / ratification record) and section 6 voice ADD (release-announcement surface)
    - 105-CONTEXT.md D-05 (audited successor), D-09 (size self-check)
  </read_first>
  <files>brandbook/BRAND-SPEC.md</files>
  <action>
    Append sections 18-25 to brandbook/BRAND-SPEC.md (D-08, bounded appends). Apply: section 6/voice-area release-announcement surface ADD (carry into voice/microcopy or a release-notes subsection per the seed structure), section 21 accessibility-standards carried + reflecting Stone 600 remediation, section 24 llm-context-block and section 25 first-implementation-checklist carried forward and updated to reflect that logo + tokens now SHIP (not pending). Add a brief ratification note referencing AUDT-04 (audit-driven font/color changes ratified 2026-06-11) so the spec records its provenance. Then run the PHASE SELF-CHECK: confirm committed brandbook/ total is <=800KB (git ls-files brandbook/ piped through du), confirm prompts/crosswake-brand-book.md is byte-identical to HEAD (git diff --exit-code), and confirm all three book artifacts exist (index.html, assets/brandbook.{css,js}, BRAND-SPEC.md). Commit.
  </action>
  <acceptance_criteria>
    sections 18-25 present; ratification note references AUDT-04; checklist updated to "ships"; committed brandbook <=800KB; prompts/ untouched; all book artifacts exist.
  </acceptance_criteria>
  <verify>
    <automated>grep -qiE "first.implementation|implementation-checklist" brandbook/BRAND-SPEC.md && grep -qE "AUDT-04|ratif" brandbook/BRAND-SPEC.md && [ "$(wc -l < brandbook/BRAND-SPEC.md)" -ge 400 ] && git diff --exit-code prompts/crosswake-brand-book.md && test -f brandbook/index.html && test -f brandbook/assets/brandbook.css && test -f brandbook/assets/brandbook.js && [ "$(git ls-files brandbook/ | xargs du -k 2>/dev/null | awk '{s+=$1} END{print s}')" -le 800 ]</automated>
  </verify>
  <done>Sections 18-25 authored, ratification recorded; phase self-check passes: brandbook <=800KB, prompts/ untouched, all book artifacts present.</done>
</task>

</tasks>

<verification>
- brandbook/BRAND-SPEC.md exists, marked v1.0, all 25 seed sections carried forward with kebab-case anchors.
- Every applicable AUDIT verdict applied: Stone 600 #756D63 + role split, 12-state mapping, logo geometry numbers + min sizes + misuse, mobile breakpoints ADD, release-announcement ADD, ratification note.
- git diff --exit-code prompts/crosswake-brand-book.md is clean (seed untouched, D-05).
- Committed brandbook/ <=800KB (D-09 self-check); index.html + assets + BRAND-SPEC all present.
</verification>

<success_criteria>
- BOOK-03 complete: BRAND-SPEC.md v1.0 is the audited successor with all verdicts applied; prompts/ seed preserved.
- Phase 105 self-check green: size budget honored, all three book artifacts shipped.
</success_criteria>

<output>
Create `.planning/phases/105-html-brand-book/105-03-SUMMARY.md` when done. Keep narration short, commit per task, never echo file contents into the summary (executor crash avoidance).
</output>
