---
phase: 105-html-brand-book
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - brandbook/logo/tournament/index.html
  - brandbook/logo/tournament/README.md
  - brandbook/tools/build-gallery.mjs
  - brandbook/tools/gallery-content.mjs
  - brandbook/assets/brandbook.css
  - brandbook/assets/brandbook.js
  - brandbook/index.html
autonomous: true
requirements: [BOOK-01]
must_haves:
  truths:
    - "Committed brandbook/ total is <=800KB after trim, leaving >=200KB for phase 106 (D-09)"
    - "index.html links tokens/tokens.css via relative path and brandbook.css layers layout on top of token variables (D-03)"
    - "Opening brandbook/index.html from file:// renders a dark cover hero with the real horizontal-dark lockup and the promise, with zero console errors (D-01, D-06)"
    - "The page loads fonts via Google Fonts CDN link with full system fallback stacks and degrades gracefully offline (D-02)"
    - "tournament/index.html is replaced by a <=2KB provenance README; round3.html and candidates/ remain (D-09)"
  artifacts:
    - path: "brandbook/logo/tournament/README.md"
      provides: "Provenance pointer replacing the deleted 351KB gallery"
      max_bytes: 2048
    - path: "brandbook/assets/brandbook.css"
      provides: "Token-layered layout/typography (foam/current rhythm)"
      min_lines: 60
    - path: "brandbook/assets/brandbook.js"
      provides: "scroll-spy, copy-hex, inlined WCAG contrast fn"
      contains: "function contrast"
    - path: "brandbook/index.html"
      provides: "Long-scroll shell: nav + 10 section skeletons + dark hero"
      contains: "id=\"hero\""
  key_links:
    - from: "brandbook/index.html"
      to: "../tokens/tokens.css"
      via: "relative link rel=stylesheet"
      pattern: "tokens/tokens.css"
    - from: "brandbook/index.html"
      to: "assets/brandbook.css, assets/brandbook.js"
      via: "relative link + script"
      pattern: "assets/brandbook\\.(css|js)"
    - from: "brandbook/index.html"
      to: "logo/crosswake-lockup-horizontal-dark.svg"
      via: "img src in hero"
      pattern: "crosswake-lockup-horizontal-dark\\.svg"
---

<objective>
Free the size budget (D-09) and scaffold the brand book page: the trim, the two asset files, and the index.html shell with nav + 10 empty section anchors + a finished dark cover hero. After this plan the page opens from file://, shows the money-shot hero, and has the structural skeleton that plan 105-02 fills in.

Purpose: Establish the page foundation (BOOK-01 — file://, zero-build, Google Fonts + fallbacks) and reclaim ~410KB so the finished book fits the 1MB cap.
Output: trimmed tournament dir, brandbook.css, brandbook.js, index.html shell with hero.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/105-html-brand-book/105-CONTEXT.md
@.planning/phases/105-html-brand-book/105-VALIDATION.md

<interfaces>
From brandbook/tokens/tokens.css — the page is styled FROM these (D-03). Semantic-tier names available (primitives also present):
  Surfaces: --cw-surface-default (foam), --cw-surface-inverse (current-950), --cw-surface-raised, --cw-surface-inset
  Text: --cw-text-default, --cw-text-inverse, --cw-text-muted, --cw-text-subtle
  Action: --cw-action-bg, --cw-action-fg, --cw-action-focus-ring, --cw-action-hover
  Borders: --cw-border-default, --cw-border-strong, --cw-border-subtle
  Runtime: --cw-runtime-liveview, --cw-runtime-offline, --cw-runtime-native, --cw-runtime-sensitive, --cw-runtime-bridge
  Status: --cw-status-success/-warning/-error/-info
  tokens.css carries a prefers-color-scheme dark block AND a [data-theme] block.

From brandbook/tools/contrast.mjs — inline this logic (~15 lines) into brandbook.js for live badges:
  parseHex(hex) -> r,g,b; linearize(c); luminance(r,g,b); contrast(hex1,hex2) -> ratio number.
  WCAG: AA text passes at >=4.5, non-text UI at >=3.0.

Production logo files in brandbook/logo/ (reference via img, D-10): crosswake-mark.svg, crosswake-mark-mono.svg, crosswake-lockup-horizontal.svg, crosswake-lockup-horizontal-dark.svg, crosswake-lockup-stacked.svg, crosswake-lockup-subtitle.svg, crosswake-typemark.svg, favicon.svg.

Fonts (D-02): Space Grotesk 500/600/700 (display headings), Atkinson Hyperlegible Next 400/500/600 (body/UI), JetBrains Mono 400/500 (code). Full system fallback stacks live in tokens.css / brand book section 9 fallback stacks.
</interfaces>
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| page to Google Fonts CDN | Only external reference; a link to fonts.googleapis.com / fonts.gstatic.com |
| user to page | No user input fields; copy-hex reads static DOM text only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-105-01 | Information Disclosure | Google Fonts CDN link | accept | Static public fonts, no PII in request; full system fallbacks mean a blocked/offline CDN degrades gracefully (D-02) |
| T-105-02 | Tampering | brandbook.js DOM scripting | mitigate | No eval, no innerHTML from variables, no remote script; copy uses navigator.clipboard.writeText on static textContent only |
| T-105-SC | Tampering | npm/pip/cargo installs | mitigate | Zero new installs; Playwright reused from examples/phoenix_host/node_modules |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Trim the tournament gallery to reclaim ~410KB (D-09)</name>
  <read_first>
    - brandbook/tools/build-gallery.mjs (confirmed during planning: its only writeFileSync target is brandbook/logo/tournament/index.html)
    - brandbook/tools/gallery-content.mjs (confirmed: imported ONLY by build-gallery.mjs)
    - 105-CONTEXT.md D-09 (what stays vs goes)
  </read_first>
  <files>brandbook/logo/tournament/index.html, brandbook/logo/tournament/README.md, brandbook/tools/build-gallery.mjs, brandbook/tools/gallery-content.mjs</files>
  <action>
    Delete brandbook/logo/tournament/index.html (the 351KB round-1/2 gallery) and BOTH generator scripts that exclusively served it — brandbook/tools/build-gallery.mjs and brandbook/tools/gallery-content.mjs (verified during planning: build-gallery's sole output is the deleted page; gallery-content is imported only by build-gallery; round3.html and candidates/ are NOT produced by them). Use git rm so deletions are tracked. Create brandbook/logo/tournament/README.md (must be at most 2KB) as a provenance pointer: explain the round-1/2 candidate gallery was retired to save committed space, point readers at round3.html (the decision page, retained), the candidates/ SVGs (retained), and git log --follow brandbook/logo/tournament/index.html for the full history. Do NOT touch round3.html or candidates/.
  </action>
  <acceptance_criteria>
    tournament/index.html gone; both generators gone (git rm); README.md at most 2048 bytes; round3.html + candidates/ untouched; check-production passes.
  </acceptance_criteria>
  <verify>
    <automated>test ! -f brandbook/logo/tournament/index.html && test ! -f brandbook/tools/build-gallery.mjs && test ! -f brandbook/tools/gallery-content.mjs && test -f brandbook/logo/tournament/round3.html && [ "$(wc -c < brandbook/logo/tournament/README.md)" -le 2048 ] && node brandbook/tools/check-production.mjs</automated>
  </verify>
  <done>Gallery page + its two exclusive generators removed via git rm; small provenance README in place; round3.html + candidates/ untouched; check-production passes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Author brandbook.css + brandbook.js (token-layered, scroll-spy, copy-hex, live contrast)</name>
  <read_first>
    - brandbook/tokens/tokens.css (token variables this CSS layers on top of — see interfaces block)
    - brandbook/tools/contrast.mjs (parseHex/linearize/luminance/contrast to inline into JS)
    - brandbook/logo/round3.html (established foam-bg/current-text aesthetic to evolve)
    - 105-CONTEXT.md D-01, D-02, D-07
  </read_first>
  <files>brandbook/assets/brandbook.css, brandbook/assets/brandbook.js</files>
  <behavior>
    - contrast('#FFFFFF','#000000') returns ~21 (max); contrast of identical colors returns 1.
    - A swatch contrast >=4.5 yields a PASS badge; <4.5 yields FAIL (computed, never hardcoded — D-04/D-07).
    - Copy-hex button writes the swatch hex to navigator.clipboard (no remote calls, no eval).
    - Scroll-spy marks exactly one nav link active as sections cross the viewport.
  </behavior>
  <action>
    Write brandbook/assets/brandbook.css by hand in bounded chunks (D-08, no giant write): a token-layer on top of tokens.css custom properties implementing the foam/current rhythm — base surface foam (--cw-surface-default), Space Grotesk display headings, Atkinson body, JetBrains mono for code; sticky nav styling; a .section rhythm with generous vertical spacing; a dark-section utility using --cw-surface-inverse/--cw-text-inverse; and reusable primitives later plans consume: swatch grid, badge (pass/fail variants), copy-button, code-block, callout, route-card. Respect prefers-reduced-motion (no transitions beyond trivial) per D-07. Write brandbook/assets/brandbook.js (at most ~60 logic lines, D-01): (a) inline parseHex/linearize/luminance/contrast from contrast.mjs; (b) on DOMContentLoaded, find elements carrying data-fg/data-bg, compute the ratio, write the number + PASS/FAIL into their .badge child via textContent + classList; (c) copy-hex: a delegated click handler on [data-copy-hex] calling navigator.clipboard.writeText(hex); (d) scroll-spy via IntersectionObserver toggling .active on matching nav links. No eval; never assign innerHTML from a variable.
  </action>
  <acceptance_criteria>
    brandbook.js has a contrast function and navigator.clipboard usage, no eval/innerHTML-from-variable, at most ~90 non-comment lines; brandbook.css has at least 60 lines and references --cw- tokens.
  </acceptance_criteria>
  <verify>
    <automated>grep -q "function contrast" brandbook/assets/brandbook.js && grep -q "navigator.clipboard" brandbook/assets/brandbook.js && ! grep -qE "eval\(|innerHTML[[:space:]]*=" brandbook/assets/brandbook.js && [ "$(grep -vc '^[[:space:]]*//' brandbook/assets/brandbook.js)" -le 90 ] && [ "$(wc -l < brandbook/assets/brandbook.css)" -ge 60 ] && grep -q -- "--cw-" brandbook/assets/brandbook.css</automated>
  </verify>
  <done>brandbook.css layers --cw- tokens; brandbook.js has the inlined contrast fn + clipboard copy + scroll-spy, no eval/innerHTML.</done>
</task>

<task type="auto">
  <name>Task 3: index.html shell — nav, 10 section anchors, finished dark cover hero (render-verify)</name>
  <read_first>
    - brandbook/tokens/tokens.css (linked relatively from the page)
    - brandbook/logo/crosswake-lockup-horizontal-dark.svg (hero lockup)
    - brandbook/tools/render-verify.mjs (mandatory visual QA harness; run: node brandbook/tools/render-verify.mjs INPUT OUTPUT.png, then READ the PNG)
    - 105-CONTEXT.md D-01, D-02, D-04 (section list + order), D-06, D-07
  </read_first>
  <files>brandbook/index.html</files>
  <action>
    Author brandbook/index.html via incremental section-by-section Edits (D-08 — NEVER one giant Write; start from a minimal valid doc, then add each block with follow-up Edits). Head: the Google Fonts CDN link for the three families (D-02), a relative link to ../tokens/tokens.css then assets/brandbook.css, and a deferred script for assets/brandbook.js. Body uses semantic landmarks: a sticky nav with anchor links to all 10 sections, and a main element containing 10 section elements with these exact ids IN ORDER (D-04): hero, essence, logo-system, color, typography, tokens, motifs, voice, ui-specimens, asset-index. Every non-hero section is a skeleton for plan 105-02 (an h2 heading plus an empty placeholder div). FINISH the hero now: dark surface (--cw-surface-inverse / current-950), crosswake-lockup-horizontal-dark.svg referenced via an img with alt="Crosswake" (D-10), and the promise "Declare the crossing. Keep the boundary honest." as the lead statement. Keep heading order h1 then h2 then h3 (D-07). Then run render-verify and READ the screenshot: confirm the dark hero renders, the lockup is legible, fonts loaded, and the 10 nav links resolve.
  </action>
  <acceptance_criteria>
    All 10 section ids present in order; Google Fonts link + relative tokens.css + assets links present; hero references the horizontal-dark lockup; render-verify screenshot READ and shows a dark hero with the lockup and the promise; zero console errors.
  </acceptance_criteria>
  <verify>
    <automated>node brandbook/tools/render-verify.mjs brandbook/index.html /tmp/105-01-hero.png && for id in hero essence logo-system color typography tokens motifs voice ui-specimens asset-index; do grep -q "id=\"$id\"" brandbook/index.html || { echo "MISSING $id"; exit 1; }; done && grep -q "fonts.googleapis.com" brandbook/index.html && grep -q "tokens/tokens.css" brandbook/index.html && grep -q "assets/brandbook.css" brandbook/index.html && grep -q "crosswake-lockup-horizontal-dark.svg" brandbook/index.html</automated>
  </verify>
  <done>index.html opens from file:// with the finished dark hero and 10 ordered section skeletons; nav + asset links resolve; screenshot READ confirms hero quality; zero console errors.</done>
</task>

</tasks>

<verification>
- Committed brandbook total after trim is well under 800KB (gallery page + 2 generators removed, ~410KB reclaimed).
- node brandbook/tools/render-verify.mjs brandbook/index.html /tmp/105-01-hero.png succeeds; executor READs the PNG and confirms a dark hero with the real lockup + promise.
- All 10 section ids present and ordered; Google Fonts + tokens.css + assets links resolve relatively.
- brandbook.js: contrast fn inlined, clipboard copy, scroll-spy; no eval/innerHTML.
</verification>

<success_criteria>
- BOOK-01 foundation in place: zero-build file:// page, Google Fonts CDN + system fallbacks, two small asset files (D-01/D-02).
- Size budget freed (D-09): tournament/index.html replaced by a small README; round3.html + candidates/ retained.
- Dark cover hero finished and render-verified (D-04/D-06); 10 section skeletons ready for 105-02.
</success_criteria>

<output>
Create `.planning/phases/105-html-brand-book/105-01-SUMMARY.md` when done. Keep narration short, commit per task, and never echo file contents into the summary (executor crash avoidance).
</output>
