---
phase: 105-html-brand-book
plan: 02
type: execute
wave: 2
depends_on: ["105-01"]
files_modified:
  - brandbook/index.html
autonomous: true
requirements: [BOOK-02]
must_haves:
  truths:
    - "All 9 content sections (essence..asset-index) are populated per D-04 with real Crosswake copy, no lorem ipsum (D-04)"
    - "Color section shows live JS-computed contrast PASS/FAIL badges and working copy-hex buttons (D-04, D-07)"
    - "Logo misuse examples are rendered live in CSS/SVG (cyan recolor, boxed mark, stretched, subtitle-on-main), each shown crossed out — not screenshots (D-04)"
    - "Motifs (wake seams + runtime lanes) render as live inline SVG, sparse (D-04)"
    - "Page renders cleanly at 1200w AND 390w with zero console errors (D-06, D-07)"
  artifacts:
    - path: "brandbook/index.html"
      provides: "All 10 sections populated; the full brand book"
      contains: "data-copy-hex"
  key_links:
    - from: "color swatches"
      to: "brandbook.js contrast/copy logic"
      via: "data-fg/data-bg + data-copy-hex attributes"
      pattern: "data-(fg|bg|copy-hex)"
    - from: "ui-specimens + color"
      to: "../tokens/tokens.css custom properties"
      via: "var(--cw-*) only, no hardcoded brand colors"
      pattern: "var\\(--cw-"
---

<objective>
Fill sections 2 through 10 of brandbook/index.html with real Crosswake content per D-04: essence + pillars, the live logo system with rendered misuse examples, the color grid with live contrast badges + copy-hex, typography specimens + scale, the token usage guide, live motif SVGs, voice do/don't tables, token-built UI specimens, and the asset index. Section-by-section Edits with per-task commits; render-verify at 1200w AND 390w after each task.

Purpose: Deliver BOOK-02 — the complete set of brand-book sections with live, computed, token-driven specimens (not screenshots, not hardcoded).
Output: a fully populated brandbook/index.html.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/105-html-brand-book/105-CONTEXT.md
@.planning/phases/105-html-brand-book/105-VALIDATION.md

<interfaces>
brandbook/index.html already has (from 105-01): the 10 section skeletons (ids hero, essence, logo-system, color, typography, tokens, motifs, voice, ui-specimens, asset-index), sticky nav, linked tokens.css + brandbook.css + brandbook.js.

brandbook.js (from 105-01) hooks these attributes — emit them, do NOT recompute in markup:
  - data-fg="#HEX" data-bg="#HEX" on a swatch -> JS writes ratio + PASS/FAIL into the child .badge.
  - data-copy-hex="#HEX" on a button -> JS copies the hex to clipboard.
  - nav anchor href="#id" -> scroll-spy adds .active.

Reusable CSS primitives from brandbook.css (105-01): swatch grid, .badge, .copy-button, .code-block, .callout, .route-card, dark-section utility.

Tokens (use var(--cw-*) for every specimen color — D-04 "built from tokens.css only"): primitives include brass-500 #C98A2E, current-950 #09141A, foam-50 #F7F1E6, wake-700 #2B756A, stone-600 #756D63. Semantic + runtime + status names are listed in tokens.css.

Logo files in brandbook/logo/ to render in the logo-system section via img (D-10): all 8 production SVGs. Inline a copy ONLY where color-swapping is required (e.g., the cyan-recolor misuse example).
</interfaces>

# Content sources (cite these literally in section text):
@brandbook/AUDIT.md
@prompts/crosswake-brand-book.md
@brandbook/tokens/tokens.css
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| page to Google Fonts CDN | Inherited from 105-01; only external reference |
| user to page | No user input; copy-hex reads static attributes only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-105-03 | Tampering | Inline SVG motifs + misuse examples | mitigate | Author static markup only; no script-generated SVG, no innerHTML; misuse boxed/stretched effects via CSS transforms, not injected HTML |
| T-105-SC | Tampering | npm/pip/cargo installs | mitigate | Zero new installs; Playwright reused from examples/phoenix_host/node_modules |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Sections essence + logo-system + color</name>
  <read_first>
    - brandbook/AUDIT.md section 2 (Brand DNA: essence, pillars, personality, anti-traits) for #essence
    - brandbook/AUDIT.md section 8 (clearspace, minimum sizes, do/don't, the 5 misuse examples) for #logo-system
    - brandbook/AUDIT.md section 7 + section 8 Stone 600 remediation; brandbook/tokens/tokens.css for #color swatches + semantic roles
    - brandbook/logo/ (the 8 production SVGs to render)
  </read_first>
  <files>brandbook/index.html</files>
  <action>
    Edit brandbook/index.html section-by-section (D-08, no giant write). (1) #essence: brand essence + pillars from AUDIT section 2 DNA — essence statement, personality traits, anti-traits; real copy only (D-04). (2) #logo-system: render all 8 production SVGs from logo/ via img tags with descriptive alt/aria-labels (D-07/D-10); a clearspace + minimum-sizes panel using the AUDIT section 8 numbers (horizontal lockup 128px, stacked 64px, icon 24/32px, favicon 16px); and the misuse examples RENDERED LIVE in CSS/SVG, each visibly crossed out — cyan recolor (inline a mark SVG and override its fill to cyan, per D-10), mark inside a boxed/blob card, stretched/rotated mark (CSS transform), subtitle variant misused as the main lockup. NOT screenshots. (3) #color: a swatch grid of the brand palette built from var(--cw-*) tokens; each swatch carries data-fg/data-bg so brandbook.js prints the live WCAG ratio + PASS/FAIL badge, plus a copy-hex button (data-copy-hex); include the semantic role table covering runtime.* tokens (liveview/offline/native/sensitive/bridge) and status tokens, noting the Stone 600 #756D63 remediation. Then render-verify at 1200w AND 390w (two screenshots), READ both, fix any defects, commit.
  </action>
  <acceptance_criteria>
    essence has real DNA copy; logo-system renders 8 SVGs + 4 live crossed-out misuse examples + clearspace/min-size numbers; color grid has data-fg/data-bg + data-copy-hex on swatches and a runtime role table; both screenshots READ; zero console errors.
  </acceptance_criteria>
  <verify>
    <automated>node brandbook/tools/render-verify.mjs brandbook/index.html /tmp/105-02-a-1200.png && node brandbook/tools/render-verify.mjs brandbook/index.html /tmp/105-02-a-390.png 390 && grep -q "data-copy-hex" brandbook/index.html && grep -q "data-fg" brandbook/index.html && grep -qE "runtime-(liveview|offline|native|sensitive|bridge)" brandbook/index.html && [ "$(grep -oc "logo/crosswake-" brandbook/index.html)" -ge 6 ]</automated>
  </verify>
  <done>Three sections populated with live logo/misuse/color specimens; both viewport screenshots READ and clean.</done>
</task>

<task type="auto">
  <name>Task 2: Sections typography + tokens + motifs</name>
  <read_first>
    - prompts/crosswake-brand-book.md section 9 (typography: display/body/code faces, fallback stacks, type scale) and section 11 (motifs: wake seams, runtime lanes, sounding lines)
    - brandbook/AUDIT.md section 7 (token architecture overview, tiers) for the tokens usage guide
    - brandbook/tokens/tokens.css + brandbook/tokens/crosswake.tokens.json (link targets)
  </read_first>
  <files>brandbook/index.html</files>
  <action>
    Edit brandbook/index.html (D-08, no giant write). (1) #typography: live specimens of all three faces (Space Grotesk display, Atkinson Hyperlegible Next body, JetBrains Mono code) using var(--cw-*) text tokens, plus the type-scale table with the brand book section 9 values; show fallback stacks. (2) #tokens: a usage guide explaining the primitive then semantic then state/runtime tiers (AUDIT section 7), with relative links to ../tokens/tokens.css and ../tokens/crosswake.tokens.json; show 2-3 worked examples of consuming a semantic token. (3) #motifs: render wake seams and runtime lanes as LIVE inline SVG (sparse, per brand book section 11 — restraint is the point); brief captions describing where each motif is used. Then render-verify at 1200w AND 390w, READ both, fix, commit.
  </action>
  <acceptance_criteria>
    typography shows all 3 faces + scale table; tokens section links both token files and explains the 3 tiers; motifs render as inline SVG (wake seams + runtime lanes), sparse; both screenshots READ; zero console errors.
  </acceptance_criteria>
  <verify>
    <automated>node brandbook/tools/render-verify.mjs brandbook/index.html /tmp/105-02-b-1200.png && node brandbook/tools/render-verify.mjs brandbook/index.html /tmp/105-02-b-390.png 390 && grep -q "tokens/crosswake.tokens.json" brandbook/index.html && grep -qiE "Space Grotesk|Atkinson|JetBrains" brandbook/index.html && [ "$(grep -oc "<svg" brandbook/index.html)" -ge 2 ]</automated>
  </verify>
  <done>typography/tokens/motifs populated; motifs are live inline SVG; both screenshots READ and clean.</done>
</task>

<task type="auto">
  <name>Task 3: Sections voice + ui-specimens + asset-index (final zero-console-error pass)</name>
  <read_first>
    - brandbook/AUDIT.md section 10 (voice verdict, ready-to-use copy blocks, voice rules summary)
    - prompts/crosswake-brand-book.md section 6 (write this / do not write this) and section 15 microcopy / status labels
    - brandbook/tokens/tokens.css (every ui-specimen color must be var(--cw-*))
    - brandbook/logo/README.md (asset table source); 105-CONTEXT.md integration points (phase 106 rows)
  </read_first>
  <files>brandbook/index.html</files>
  <action>
    Edit brandbook/index.html (D-08, no giant write). (1) #voice: write-this/not-this tables and the runtime/status label glossary + error-message examples, sourced from AUDIT section 10 ready-to-use copy and brand book section 6/section 15 — real Crosswake microcopy only, no lorem ipsum (D-04). (2) #ui-specimens: a route card, runtime badges (liveview/offline/native/sensitive/bridge), buttons (default/hover/focus states), a callout, and a code block — ALL built from var(--cw-*) custom properties only (D-04; grep must find no hardcoded brand hex in these specimens). (3) #asset-index: a file table for logo/ + tokens/ with paths, and collateral rows marked "ships in v9.0 phase 106". Final pass: render-verify at 1200w AND 390w, READ both, AND confirm zero console errors via the render-verify harness console capture (if not built in, add a quick inline Playwright page.on('console') check). Fix any defect, commit.
  </action>
  <acceptance_criteria>
    voice has real do/don't tables + glossary; ui-specimens use only var(--cw-*); asset-index lists logo/tokens files + phase-106 collateral rows; both screenshots READ; zero console errors confirmed.
  </acceptance_criteria>
  <verify>
    <automated>node brandbook/tools/render-verify.mjs brandbook/index.html /tmp/105-02-c-1200.png && node brandbook/tools/render-verify.mjs brandbook/index.html /tmp/105-02-c-390.png 390 && grep -qi "phase 106" brandbook/index.html && grep -q "var(--cw-" brandbook/index.html && node -e "const h=require('fs').readFileSync('brandbook/index.html','utf8'); const ids=['essence','logo-system','color','typography','tokens','motifs','voice','ui-specimens','asset-index']; const miss=ids.filter(i=>!h.includes('id=\"'+i+'\"')); if(miss.length){console.error('MISSING',miss);process.exit(1)}"</automated>
  </verify>
  <done>voice/ui-specimens/asset-index populated; specimens are token-driven; both screenshots READ; zero console errors confirmed across the whole page.</done>
</task>

</tasks>

<verification>
- After each task, render-verify ran at 1200w AND 390w and the executor READ both screenshots before commit (D-06).
- Color swatches drive live JS contrast badges (data-fg/data-bg) and copy-hex (data-copy-hex) — computed, not hardcoded (D-07).
- Logo misuse examples are live CSS/SVG and crossed out, not screenshots (D-04).
- Motifs render as live inline SVG, sparse (D-04).
- UI specimens use only var(--cw-*) tokens (D-04).
- All 10 section ids present; final zero-console-errors check passed.
</verification>

<success_criteria>
- BOOK-02 complete: all required sections present and populated with real Crosswake copy (no lorem ipsum), live contrast badges + copy-hex, live logo misuse examples, live motifs, token-built UI specimens, and the asset index.
- Page clean at 1200w and 390w with zero console errors.
</success_criteria>

<output>
Create `.planning/phases/105-html-brand-book/105-02-SUMMARY.md` when done. Keep narration short, commit per task, never echo file contents into the summary (executor crash avoidance).
</output>
