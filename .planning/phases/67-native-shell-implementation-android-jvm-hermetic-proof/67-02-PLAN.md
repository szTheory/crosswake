---
phase: 67-native-shell-implementation-android-jvm-hermetic-proof
plan: 02
type: execute
wave: 2
depends_on:
  - 67-01
files_modified:
  - examples/android_shell_host/app/src/main/java/dev/crosswake/shell/ActivationCoordinator.kt
  - examples/android_shell_host/app/src/test/java/dev/crosswake/shell/ActivationCoordinatorTest.kt
  - examples/ios_shell_host/CrosswakeShell/ActivationCoordinator.swift
  - examples/ios_shell_host/CrosswakeShellTests/ActivationCoordinatorTests.swift
autonomous: true
requirements:
  - AVER-01
must_haves:
  truths:
    - The ActivationCoordinator enforces the native_runtime_version rebuild check for both iOS and Android.
  artifacts:
    - path: examples/android_shell_host/app/src/main/java/dev/crosswake/shell/ActivationCoordinator.kt
      provides: Android rebuild block check
    - path: examples/ios_shell_host/CrosswakeShell/ActivationCoordinator.swift
      provides: iOS rebuild block check
  key_links:
    - from: examples/android_shell_host/app/src/main/java/dev/crosswake/shell/ActivationCoordinator.kt
      to: examples/android_shell_host/app/src/main/assets/crosswake_manifest.json
      via: JSON parsing `compatibility.native_runtime_version`
---

<objective>
Enforce the `native_runtime_version` rebuild check in the `ActivationCoordinator` for both iOS and Android.

Purpose: Mirror the locked Elixir contracts by ensuring the shell validates its runtime version against the manifest, throwing a compatibility mismatch if they diverge.
Output: Updated iOS and Android ActivationCoordinators and their respective unit tests asserting the rebuild block.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Android ActivationCoordinator Rebuild Enforcement</name>
  <files>
    - examples/android_shell_host/app/src/main/java/dev/crosswake/shell/ActivationCoordinator.kt
    - examples/android_shell_host/app/src/test/java/dev/crosswake/shell/ActivationCoordinatorTest.kt
  </files>
  <action>
    Update `ActivationCoordinator.kt`:
    - Extend `ShellManifest` to parse a `nativeRuntimeVersion` field from a `compatibility` JSON object (read from `crosswake_manifest.json`).
    - In `ActivationCoordinator.resolve()`, before routing logic, add a check: if `request.nativeRuntimeVersion != manifest.nativeRuntimeVersion`, return a `ShellPresentation.Denied` with `RouteDenialReason.COMPATIBILITY_MISMATCH` stating "This route requires a newer shell binary to boot."

    Update `ActivationCoordinatorTest.kt`:
    - Add a unit test (Robolectric JVM test) simulating a mismatched `nativeRuntimeVersion` in the ActivationRequest compared to the ShellManifest. Assert that the presentation is `Denied` with `COMPATIBILITY_MISMATCH`.
  </action>
  <verify>
    <automated>cd examples/android_shell_host && ./gradlew :app:testDebugUnitTest --tests "*ActivationCoordinatorTest*"</automated>
  </verify>
  <done>Android `ActivationCoordinator` validates `native_runtime_version` parity and blocks boot if mismatched.</done>
</task>

<task type="auto">
  <name>Task 2: iOS ActivationCoordinator Rebuild Enforcement</name>
  <files>
    - examples/ios_shell_host/CrosswakeShell/ActivationCoordinator.swift
    - examples/ios_shell_host/CrosswakeShellTests/ActivationCoordinatorTests.swift
  </files>
  <action>
    Update `ActivationCoordinator.swift`:
    - Extend the `ShellManifest` struct (or Decodable) to parse the `compatibility.native_runtime_version` JSON field.
    - Inside `resolve(request:manifest:)`, add a check ensuring `request.nativeRuntimeVersion == manifest.compatibility.nativeRuntimeVersion`.
    - If they mismatch, return a `.denied` presentation using `RouteDenialReason.compatibilityMismatch`.

    Update `ActivationCoordinatorTests.swift`:
    - Add a test that initializes a request with an out-of-sync `nativeRuntimeVersion` and verifies the `.denied` case is returned.
  </action>
  <verify>
    <automated>cd examples/ios_shell_host/CrosswakeShell && xcodebuild test -project CrosswakeShell.xcodeproj -scheme CrosswakeShell -destination "platform=iOS Simulator,name=iPhone 15,OS=latest" | grep -A 5 "Test Suite"</automated>
  </verify>
  <done>iOS `ActivationCoordinator` validates `native_runtime_version` parity and blocks boot if mismatched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Network -> App | Manifest loading via OTA update could contain newer runtime versions requiring a shell rebuild. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-67-02 | Spoofing | Activation Coordinator | mitigate | Strict equality check on native_runtime_version strings prevents booting on incompatible shell versions. |
</threat_model>

<verification>
Both iOS and Android unit tests for `ActivationCoordinator` pass, verifying the version parity lock.
</verification>

<success_criteria>
`native_runtime_version` check is strictly enforced in both native shells.
</success_criteria>

<output>
Create `.planning/phases/67-native-shell-implementation-android-jvm-hermetic-proof/67-02-SUMMARY.md` when done
</output>
