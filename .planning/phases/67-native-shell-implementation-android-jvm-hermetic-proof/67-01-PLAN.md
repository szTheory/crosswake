---
phase: 67-native-shell-implementation-android-jvm-hermetic-proof
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - examples/android_shell_host/build.gradle
  - examples/android_shell_host/app/build.gradle
  - .github/workflows/phase67-proof.yml
autonomous: true
requirements:
  - AVER-02
  - AVER-01
must_haves:
  truths:
    - Android builds against modern toolchain floors (minSdk 30, compileSdk 35).
    - Android CI provides a merge-blocking hermetic proof lane.
  artifacts:
    - path: examples/android_shell_host/app/build.gradle
      provides: Updated compileSdk and Robolectric test dependency
    - path: .github/workflows/phase67-proof.yml
      provides: GitHub Actions workflow for Phase 67 Android Hermetic proof
  key_links:
    - from: .github/workflows/phase67-proof.yml
      to: examples/android_shell_host/app/build.gradle
      via: ./gradlew :app:testDebugUnitTest
---

<objective>
Bump Android toolchain floors to meet modern requirements (AGP 8.5, Kotlin 1.9.24, minSdk 30, compileSdk/targetSdk 35) and set up the merge-blocking Robolectric JVM test workflow for CI.

Purpose: Ensures Android builds are App Store / Google Play compliant (targetSdk 35) and testing is reliable without flaky emulators.
Output: Updated gradle files and a new GitHub Actions workflow running JVM tests.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Bump Android Toolchain and Add Robolectric</name>
  <files>
    - examples/android_shell_host/build.gradle
    - examples/android_shell_host/app/build.gradle
  </files>
  <action>
    Update `examples/android_shell_host/build.gradle`:
    - Bump `com.android.application` version to `8.5.0`.
    - Bump `org.jetbrains.kotlin.android` version to `1.9.24`.

    Update `examples/android_shell_host/app/build.gradle`:
    - Bump `compileSdk` and `targetSdk` to 35.
    - Bump `minSdk` to 30.
    - Add `testImplementation 'org.robolectric:robolectric:4.13'` to dependencies.
    - Enable Android resources for unit tests inside `testOptions`:
      ```groovy
      unitTests {
        includeAndroidResources = true
      }
      ```
  </action>
  <verify>
    <automated>grep -q "compileSdk 35" examples/android_shell_host/app/build.gradle</automated>
  </verify>
  <done>Gradle configuration matches the required versions (D-01, D-02).</done>
</task>

<task type="auto">
  <name>Task 2: Setup Phase 67 CI Workflow</name>
  <files>
    - .github/workflows/phase67-proof.yml
  </files>
  <action>
    Create `.github/workflows/phase67-proof.yml` mimicking `phase66-proof.yml` or `phase58-proof.yml`.
    - Trigger on `push` to `main` and `pull_request`.
    - Define a job `android-jvm-proof` using `ubuntu-latest`.
    - Set up JDK 17.
    - Run `./gradlew :app:testDebugUnitTest` inside `examples/android_shell_host`.
    - Ensure it uses the Gradle wrapper from `examples/android_shell_host`.
  </action>
  <verify>
    <automated>grep -q "testDebugUnitTest" .github/workflows/phase67-proof.yml</automated>
  </verify>
  <done>Workflow file is committed and points to the Android shell host directory for testing.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI -> Test Runner | CI workflow fetches dependencies and executes untrusted code. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-67-01 | Tampering | Gradle Dependencies | mitigate | Rely on exact version locks for AGP and Robolectric. |
</threat_model>

<verification>
`./gradlew :app:testDebugUnitTest` runs successfully and `compileSdk 35` is present.
</verification>

<success_criteria>
Android build files are updated and a CI workflow for hermetic JVM tests is ready to block merges if tests fail.
</success_criteria>

<output>
Create `.planning/phases/67-native-shell-implementation-android-jvm-hermetic-proof/67-01-SUMMARY.md` when done
</output>
