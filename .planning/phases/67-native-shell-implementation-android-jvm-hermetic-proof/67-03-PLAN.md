---
phase: 67-native-shell-implementation-android-jvm-hermetic-proof
plan: 03
type: execute
wave: 3
depends_on:
  - 67-01
files_modified:
  - examples/android_shell_host/app/src/main/java/dev/crosswake/shell/DiagnosticExportManager.kt
  - examples/android_shell_host/app/src/main/java/dev/crosswake/shell/MainActivity.kt
  - examples/android_shell_host/app/src/test/java/dev/crosswake/shell/DiagnosticExportTest.kt
  - examples/android_shell_host/app/src/main/AndroidManifest.xml
autonomous: true
requirements:
  - AVER-01
must_haves:
  truths:
    - Android shell captures crash diagnostics natively without third-party dependencies.
    - Export acts as a fire-and-forget HTTP POST of the mapped Envelope.
  artifacts:
    - path: examples/android_shell_host/app/src/main/java/dev/crosswake/shell/DiagnosticExportManager.kt
      provides: Native async crash capture logic mapping to Phase 65 Envelope
    - path: examples/android_shell_host/app/src/test/java/dev/crosswake/shell/DiagnosticExportTest.kt
      provides: Robolectric shadow tests for ApplicationExitInfo capture
  key_links:
    - from: examples/android_shell_host/app/src/main/java/dev/crosswake/shell/MainActivity.kt
      to: examples/android_shell_host/app/src/main/java/dev/crosswake/shell/DiagnosticExportManager.kt
      via: boot initialization hook
---

<objective>
Implement OS-native async diagnostic capture in the Android shell using `ApplicationExitInfo` and map it to the Phase 65 Envelope structure via a fire-and-forget HTTP POST (`HttpURLConnection`).

Purpose: Satisfies the native portion of the Diagnostics Export Seam, remaining dependency-free and verifying the JVM proof lane with Robolectric.
Output: `DiagnosticExportManager.kt` capturing exit reasons, firing HTTP POSTs, and hermetically tested.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/65-diagnostic-export-seam-elixir/65-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Android DiagnosticExportManager Implementation</name>
  <files>
    - examples/android_shell_host/app/src/main/java/dev/crosswake/shell/DiagnosticExportManager.kt
    - examples/android_shell_host/app/src/main/java/dev/crosswake/shell/MainActivity.kt
    - examples/android_shell_host/app/src/main/AndroidManifest.xml
  </files>
  <action>
    Create `DiagnosticExportManager.kt`:
    - Define the JSON serialization for the Phase 65 Envelope struct. Envelope structure expects `schema_version`, `layer="native"`, `platform="android"`, `native_runtime_version`, `kind`, `correlation_id`, `observed_at`, and `native_diagnostic` (containing `source="app_exit_info"`, `exit_reason`).
    - Use `ActivityManager.getHistoricalProcessExitReasons(packageName, 0, 10)` (only runs on Android 11 / API 30+).
    - Map `REASON_CRASH`, `REASON_ANR`, etc. to Envelope values (`kind="crash"`, `exit_reason="crash"`, etc.).
    - Exclude raw payloads/messages; map only closed enums as enforced by D-11 (allowlist by construction).
    - Execute a fire-and-forget HTTP POST to a host-configured URL (e.g. read from string resources or manifest) using `java.net.HttpURLConnection`. Run the networking on a background thread.
    
    Update `MainActivity.kt` (or application class):
    - On `onCreate()`, trigger `DiagnosticExportManager.start(this, "https://api.example.com/diagnostics/export", runtimeVersion)`.
    
    Ensure `AndroidManifest.xml` has `INTERNET` permissions if not already present.
  </action>
  <verify>
    <automated>grep -q "HttpURLConnection" examples/android_shell_host/app/src/main/java/dev/crosswake/shell/DiagnosticExportManager.kt</automated>
  </verify>
  <done>Zero-dependency HTTP POST diagnostic export is implemented.</done>
</task>

<task type="auto">
  <name>Task 2: Robolectric Diagnostic Export Test</name>
  <files>
    - examples/android_shell_host/app/src/test/java/dev/crosswake/shell/DiagnosticExportTest.kt
  </files>
  <action>
    Create `DiagnosticExportTest.kt`:
    - Setup Robolectric `@Config(shadows = [ShadowActivityManager::class])`.
    - Shadow the `ActivityManager` to mock `getHistoricalProcessExitReasons` returning an `ApplicationExitInfo` with `REASON_CRASH`.
    - Shadow or Mock `HttpURLConnection` to assert that the JSON Envelope conforms to the Phase 65 schema (`schema_version`, `layer="native"`, `platform="android"`, etc.) and no raw trace is included.
  </action>
  <verify>
    <automated>cd examples/android_shell_host && ./gradlew :app:testDebugUnitTest --tests "*DiagnosticExportTest*"</automated>
  </verify>
  <done>Robolectric JVM proof passes confirming the HTTP network call structure and Envelope shape.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| OS -> App | The OS supplies crash information; this is normalized into closed-enum Envelope schemas. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-67-03 | Information Disclosure | Diagnostic Export | mitigate | Rely STRICTLY on typed ENUMS for exit_reasons. Discard all exception messages and stack traces to prevent PII leakage. |
</threat_model>

<verification>
Robolectric JVM tests for `DiagnosticExportTest` pass successfully.
</verification>

<success_criteria>
Android diagnostic exports fire HTTP POSTs securely and hermetically.
</success_criteria>

<output>
Create `.planning/phases/67-native-shell-implementation-android-jvm-hermetic-proof/67-03-SUMMARY.md` when done
</output>
