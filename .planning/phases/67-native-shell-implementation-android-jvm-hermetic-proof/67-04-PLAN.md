---
phase: 67-native-shell-implementation-android-jvm-hermetic-proof
plan: 04
type: execute
wave: 3
depends_on: []
files_modified:
  - examples/ios_shell_host/CrosswakeShell/DiagnosticExportManager.swift
  - examples/ios_shell_host/CrosswakeShell/CrosswakeShellApp.swift
  - examples/ios_shell_host/CrosswakeShellTests/DiagnosticExportManagerTests.swift
autonomous: true
requirements:
  - AVER-01
must_haves:
  truths:
    - iOS shell captures crash and hang diagnostics natively via MetricKit.
    - Export acts as a fire-and-forget HTTP POST using `URLSession`.
  artifacts:
    - path: examples/ios_shell_host/CrosswakeShell/DiagnosticExportManager.swift
      provides: Native iOS MetricKit capture mapped to Phase 65 Envelope
    - path: examples/ios_shell_host/CrosswakeShellTests/DiagnosticExportManagerTests.swift
      provides: Unit tests validating the Envelope serialization
  key_links:
    - from: examples/ios_shell_host/CrosswakeShell/CrosswakeShellApp.swift
      to: examples/ios_shell_host/CrosswakeShell/DiagnosticExportManager.swift
      via: addSubscriber in application initialization
---

<objective>
Implement OS-native async diagnostic capture in the iOS shell using `MetricKit` and map it to the Phase 65 Envelope structure via a fire-and-forget HTTP POST (`URLSession`).

Purpose: Ensures the iOS Shell natively captures crashes and hang diagnostics and ships them seamlessly, remaining zero-dependency.
Output: `DiagnosticExportManager.swift` leveraging `MXMetricManagerSubscriber`, and related unit tests.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/65-diagnostic-export-seam-elixir/65-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: iOS DiagnosticExportManager Implementation</name>
  <files>
    - examples/ios_shell_host/CrosswakeShell/DiagnosticExportManager.swift
    - examples/ios_shell_host/CrosswakeShell/CrosswakeShellApp.swift
  </files>
  <action>
    Create `DiagnosticExportManager.swift`:
    - Implement `NSObject, MXMetricManagerSubscriber`.
    - In `didReceive(_ payloads: [MXDiagnosticPayload])`, filter for `MXCrashDiagnostic` and `MXHangDiagnostic`.
    - Serialize these events into the Phase 65 Envelope format (`schema_version`, `layer="native"`, `platform="ios"`, `native_diagnostic.source="metrickit"`, etc).
    - Discard raw exception messages and strings. Only pass typed enums as required by the privacy posture.
    - Use `URLSession.shared.dataTask(with: request)` to fire an asynchronous HTTP POST.
    
    Update `CrosswakeShellApp.swift` (or `AppDelegate.swift` if applicable):
    - On application start, register the subscriber: `DiagnosticExportManager.shared.start()`.
  </action>
  <verify>
    <automated>grep -q "MXMetricManagerSubscriber" examples/ios_shell_host/CrosswakeShell/DiagnosticExportManager.swift</automated>
  </verify>
  <done>Zero-dependency HTTP POST diagnostic export is implemented for iOS MetricKit.</done>
</task>

<task type="auto">
  <name>Task 2: iOS Diagnostic Export Serialization Tests</name>
  <files>
    - examples/ios_shell_host/CrosswakeShellTests/DiagnosticExportManagerTests.swift
  </files>
  <action>
    Create `DiagnosticExportManagerTests.swift`:
    - Mock a scenario where `DiagnosticExportManager` maps a pseudo-crash event to the `Envelope`.
    - Assert that the output JSON shape precisely matches the expected contract.
    - Ensure `raw_payload` or raw exception messages are omitted to enforce the fail-closed redaction logic.
  </action>
  <verify>
    <automated>cd examples/ios_shell_host/CrosswakeShell && xcodebuild test -project CrosswakeShell.xcodeproj -scheme CrosswakeShell -destination "platform=iOS Simulator,name=iPhone 15,OS=latest" | grep -A 5 "Test Suite"</automated>
  </verify>
  <done>iOS Unit tests pass verifying envelope construction and mapping.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| OS -> App | MetricKit payload passes crash information to the subscriber. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-67-04 | Information Disclosure | iOS MetricKit Export | mitigate | Extract strictly enum-based logic (`MXCrashDiagnostic`) and discard descriptive exception traces. |
</threat_model>

<verification>
The iOS `xcodebuild test` passes asserting the JSON structures.
</verification>

<success_criteria>
iOS diagnostic exports map faithfully to the contract over zero-dep `URLSession`.
</success_criteria>

<output>
Create `.planning/phases/67-native-shell-implementation-android-jvm-hermetic-proof/67-04-SUMMARY.md` when done
</output>
