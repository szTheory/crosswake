---
phase: "16-system-context-bridges"
plan: "03"
type: execute
wave: 2
depends_on:
  - "16-01"
files_modified:
  - "lib/crosswake/bridge/contract.ex"
  - "lib/crosswake/bridge/commands/permissions_status.ex"
  - "lib/crosswake/bridge/registry.ex"
  - "lib/crosswake/manifest/builder.ex"
  - "test/crosswake/bridge/registry_test.exs"
autonomous: true
requirements:
  - "CAP-PERMSTATUS"
must_haves:
  truths:
    - "Phoenix routes can issue a typed, read-only `permissions.status` query keyed by a narrow Crosswake-owned alias per D-07 through D-09."
    - "The primary reply shape uses a small normalized Crosswake status with optional secondary detail per D-10 through D-12."
    - "Undeclared routes or unsupported aliases fail closed through the existing registry path."
  artifacts:
    - path: "lib/crosswake/bridge/commands/permissions_status.ex"
      provides: "Typed request and response structs for the permissions-status bridge contract"
    - path: "lib/crosswake/bridge/registry.ex"
      provides: "Allowlisted command mapping for permissions.status"
    - path: "lib/crosswake/manifest/builder.ex"
      provides: "Capability-catalog truth for the first supported permissions-status surface"
  key_links:
    - from: "lib/crosswake/bridge/registry.ex"
      to: "lib/crosswake/bridge/commands/permissions_status.ex"
      via: "command allowlist and typed payload contract"
      pattern: "permissions.status"
    - from: "lib/crosswake/manifest/builder.ex"
      to: "lib/crosswake/bridge/registry.ex"
      via: "public capability truth stays aligned with registered bridge support"
      pattern: "permissions.status|notifications"
---

<objective>
Define the Elixir-side `permissions.status` contract, allowlist, and capability-catalog truth.

Purpose: The first permission-status surface must be small, typed, and honest before any native provider code lands.
Output: Typed request/response modules, registry wiring, normalized reply expectations, and manifest capability metadata aligned to the resolved research scope.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/16-system-context-bridges/16-CONTEXT.md
@.planning/phases/16-system-context-bridges/16-RESEARCH.md
@.planning/phases/16-system-context-bridges/16-PATTERNS.md
@.planning/phases/16-system-context-bridges/16-01-PLAN.md
@lib/crosswake/bridge/contract.ex
@lib/crosswake/bridge/registry.ex
@lib/crosswake/manifest/builder.ex

<interfaces>
From lib/crosswake/bridge/contract.ex:
```elixir
def ok_reply(%Request{} = request, payload \\ %{}) when is_map(payload)
def deny_reply(%Request{} = request, denial_payload) when is_map(denial_payload)
```

From lib/crosswake/bridge/registry.ex:
```elixir
def allowed_commands do
  Map.keys(@capability_commands) ++ Map.keys(@transfer_commands)
end
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add the typed permissions-status request and response modules</name>
  <files>lib/crosswake/bridge/contract.ex, lib/crosswake/bridge/commands/permissions_status.ex</files>
  <behavior>
    - Test 1: Request payload is alias-keyed and read-only rather than a broad OS snapshot request per D-07 and D-08.
    - Test 2: Response payload exposes one normalized primary status plus optional `detail` per D-10 through D-12.
    - Test 3: The public contract matches the resolved research scope and does not reintroduce alias-scope ambiguity.
  </behavior>
  <action>Create `Crosswake.Bridge.Commands.PermissionsStatus` with typed request/response structs and any minimal bridge-contract helpers needed for a normalized status reply. Keep the public alias set narrowed to the resolved research decision: `notifications` only. Use a small Crosswake-owned primary enum for status branching and reserve platform-native nuance for an optional secondary `detail` field. Do not expose request orchestration or broad permission inventory semantics.</action>
  <verify>
    <automated>mix test test/crosswake/bridge/registry_test.exs</automated>
  </verify>
  <done>The Elixir-side permissions-status payload contract is typed, narrow, and aligned to the resolved research decisions.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Register the command and promote capability-catalog truth</name>
  <files>lib/crosswake/bridge/registry.ex, lib/crosswake/manifest/builder.ex, test/crosswake/bridge/registry_test.exs</files>
  <behavior>
    - Test 1: Only routes that declare `permissions.status` can invoke the command through the registry.
    - Test 2: Unsupported alias usage denies fail closed instead of returning ad hoc status values.
    - Test 3: Manifest capability metadata reflects the real first-phase support scope and prerequisite truth.
  </behavior>
  <action>Register the `permissions.status` command through the normal bounded-bridge registry and update manifest capability metadata so the public catalog matches the actual first shipped scope. Use the resolved research posture explicitly: `notifications` is the only initial public alias, and its prerequisites, denial semantics, fallback posture, and package/support status must be truthful. Avoid any docs-only or example-only drift now that the capability is execution-planned.</action>
  <verify>
    <automated>mix test test/crosswake/bridge/registry_test.exs</automated>
  </verify>
  <done>The command is allowlisted, test-backed, and represented honestly in capability metadata without generic permission-broker scope creep.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Route code -> bridge registry | Route-owned logic can only reach narrow allowlisted bridge commands here. |
| Capability catalog -> public contract | Overstating alias scope here would mislead adopters before native implementation exists. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-07 | E | bridge registry | mitigate | Route-allowlist the command and deny undeclared usage through the normal bounded-bridge path. |
| T-16-08 | T | permission contract shape | mitigate | Keep the public alias scope to `notifications` only and normalize the primary status instead of exposing platform-specific strings. |
| T-16-09 | R | support truth drift | mitigate | Promote manifest capability metadata in the same plan as command registration so public truth cannot lag behind implementation. |
</threat_model>

<verification>
Run `mix test test/crosswake/bridge/registry_test.exs`. Confirm the command registry, typed request/response modules, and capability metadata all reflect `notifications` as the only initial public alias.
</verification>

<success_criteria>
The Elixir bridge contract for `permissions.status` is small, typed, route-allowlisted, and aligned with the resolved research scope.
</success_criteria>

<output>
After completion, create `.planning/phases/16-system-context-bridges/16-03-SUMMARY.md`
</output>
