---
phase: "16-system-context-bridges"
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - "lib/crosswake/policy/defaults.ex"
  - "lib/crosswake/policy/schema.ex"
  - "lib/crosswake/policy/route.ex"
  - "lib/crosswake/router/scope_defaults.ex"
  - "lib/crosswake/policy/validator.ex"
  - "lib/crosswake/manifest/types.ex"
  - "lib/crosswake/manifest/builder.ex"
  - "lib/crosswake/manifest/validator.ex"
  - "examples/phoenix_host/lib/crosswake_example/router.ex"
  - "test/crosswake/policy/schema_test.exs"
  - "test/crosswake/policy/route_test.exs"
  - "test/crosswake/policy/compiler_test.exs"
  - "test/crosswake/manifest/validator_test.exs"
autonomous: true
requirements:
  - "CAP-DEEPLINK"
must_haves:
  truths:
    - "Inbound deep-link entry is denied by default unless a route explicitly opts into external entry per D-04."
    - "Scope defaults can set external-entry posture, but per-route overrides remain explicit and do not silently widen sensitive routes per D-05."
    - "Manifest route entries carry external-entry truth so native shells do not infer deep-linkability from route presence alone per D-01 and D-03."
  artifacts:
    - path: "lib/crosswake/policy/route.ex"
      provides: "Typed route-entry policy carried with compiled route policy"
    - path: "lib/crosswake/manifest/types.ex"
      provides: "Manifest route-entry field for explicit external-entry approval"
    - path: "lib/crosswake/manifest/builder.ex"
      provides: "Manifest serialization path from route policy to route-entry truth"
  key_links:
    - from: "lib/crosswake/policy/route.ex"
      to: "lib/crosswake/manifest/builder.ex"
      via: "compiled route-entry policy becomes manifest route metadata"
      pattern: "entry|external"
    - from: "lib/crosswake/router/scope_defaults.ex"
      to: "lib/crosswake/policy/validator.ex"
      via: "scope defaults merge before route-level validation"
      pattern: "defaults|entry"
---

<objective>
Define the deep-link entry contract in Phoenix route policy and manifest truth before any shell enforcement work.

Purpose: Phase 16 must keep inbound deep links manifest-first and fail-closed rather than ambient or bridge-driven.
Output: Route-policy fields, scope-default behavior, manifest serialization, validation coverage, and example-host route fixtures for explicit external-entry approval.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-system-context-bridges/16-CONTEXT.md
@.planning/phases/16-system-context-bridges/16-RESEARCH.md
@.planning/phases/16-system-context-bridges/16-PATTERNS.md
@lib/crosswake/policy/schema.ex
@lib/crosswake/policy/route.ex
@lib/crosswake/manifest/types.ex
@lib/crosswake/manifest/builder.ex

<interfaces>
From lib/crosswake/policy/route.ex:
```elixir
defstruct [
  :id,
  :runtime,
  :security,
  :cache_contract,
  :island_contract,
  offline: :unavailable,
  capabilities: [],
  packs: [],
  sync: [],
  transfers: []
]
```

From lib/crosswake/manifest/types.ex:
```elixir
defmodule RouteEntry do
  @enforce_keys [:id, :path, :runtime]
  defstruct [
    :id,
    :path,
    :runtime,
    :offline,
    :cache_contract,
    :island_contract,
    :security,
    capabilities: [],
    packs: [],
    sync: [],
    transfers: [],
    allowlisted_origins: []
  ]
end
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add explicit route-entry policy fields and default-merging rules</name>
  <files>lib/crosswake/policy/defaults.ex, lib/crosswake/policy/schema.ex, lib/crosswake/policy/route.ex, lib/crosswake/router/scope_defaults.ex, lib/crosswake/policy/validator.ex, test/crosswake/policy/schema_test.exs, test/crosswake/policy/route_test.exs, test/crosswake/policy/compiler_test.exs</files>
  <behavior>
    - Test 1: Route policy denies external entry by default unless the route or scope default opts in per D-04.
    - Test 2: Scope defaults plus route overrides behave explicitly and preserve fail-closed semantics per D-05.
    - Test 3: Validation errors name invalid or unsafe route-entry declarations as route-entry policy problems, not bridge-capability problems.
  </behavior>
  <action>Extend the route policy DSL with one explicit route-entry policy field that describes external entry approval rather than bridge authority, per D-01 through D-05. Thread it through defaults, NimbleOptions schema, compiled route structs, scope default merging, and semantic validation. Preserve fail-closed defaults. Keep this field distinct from `capabilities` so `deep_link` does not become a route-local navigation command surface. Use the resolved research posture: a small structured field rather than a one-off boolean if that fits existing schema patterns, but keep the first public semantics limited to external-entry allow or deny.</action>
  <verify>
    <automated>mix test test/crosswake/policy/schema_test.exs test/crosswake/policy/route_test.exs test/crosswake/policy/compiler_test.exs</automated>
  </verify>
  <done>Compiled route policy carries explicit external-entry truth with scope-default and per-route behavior covered by tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Serialize and validate route-entry truth in the manifest</name>
  <files>lib/crosswake/manifest/types.ex, lib/crosswake/manifest/builder.ex, lib/crosswake/manifest/validator.ex, examples/phoenix_host/lib/crosswake_example/router.ex, test/crosswake/manifest/validator_test.exs</files>
  <behavior>
    - Test 1: Manifest route entries include explicit external-entry truth derived from the compiled route policy per D-03 and D-04.
    - Test 2: Manifest validation rejects malformed route-entry values instead of letting shells infer behavior.
    - Test 3: Example-host routes prove one approved inbound route and one non-approved route for later shell tests.
  </behavior>
  <action>Extend manifest route-entry types, builder output, and manifest validation so external-entry approval is part of canonical manifest truth. Update the Phoenix example router with fixtures that exercise both approved and non-approved inbound routes. Do not add shell-local allowlists or a second routing config surface; the manifest stays the only source of truth for deep-link entry policy.</action>
  <verify>
    <automated>mix test test/crosswake/manifest/validator_test.exs</automated>
  </verify>
  <done>Manifest generation and validation expose explicit external-entry truth that downstream shells can enforce without local inference.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Route DSL -> compiled policy | Author-provided route metadata becomes activation authority here. |
| Compiled policy -> manifest | Any serialization drift here could widen external entry unexpectedly. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-01 | E | route-entry policy | mitigate | Default to deny and require explicit route-entry approval in route policy and manifest per D-04. |
| T-16-02 | T | manifest serialization | mitigate | Validate route-entry shape in manifest generation and validator tests so shells never infer from path presence alone. |
| T-16-03 | R | route policy errors | mitigate | Emit route-entry-specific validation failures so misconfiguration is diagnosable before shell runtime. |
</threat_model>

<verification>
Run `mix test test/crosswake/policy/schema_test.exs test/crosswake/policy/route_test.exs test/crosswake/policy/compiler_test.exs test/crosswake/manifest/validator_test.exs`. Confirm the example-host router now includes explicit external-entry fixtures and that manifest validation exposes route-entry failures as configuration errors.
</verification>

<success_criteria>
Route policy and manifest truth make external entry an explicit opt-in, not ambient behavior, and provide stable input for shell enforcement.
</success_criteria>

<output>
After completion, create `.planning/phases/16-system-context-bridges/16-01-SUMMARY.md`
</output>
