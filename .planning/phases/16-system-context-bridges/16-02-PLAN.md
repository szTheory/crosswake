---
phase: "16-system-context-bridges"
plan: "02"
type: execute
wave: 2
depends_on:
  - "16-01"
files_modified:
  - "lib/crosswake/shell/activation.ex"
  - "lib/crosswake/shell/denial.ex"
  - "lib/crosswake/compatibility/compatibility.ex"
  - "lib/crosswake/compatibility/route_gate.ex"
  - "test/crosswake/shell/activation_test.exs"
  - "examples/ios_shell_host/CrosswakeShell/ActivationCoordinator.swift"
  - "examples/ios_shell_host/CrosswakeShellTests/ActivationCoordinatorTests.swift"
  - "examples/android_shell_host/app/src/main/java/dev/crosswake/shell/ActivationCoordinator.kt"
  - "examples/android_shell_host/app/src/test/java/dev/crosswake/shell/ActivationCoordinatorTest.kt"
autonomous: true
requirements:
  - "CAP-DEEPLINK"
must_haves:
  truths:
    - "Inbound URLs only activate routes when manifest truth says the route both exists and allows external entry per D-01 and D-04."
    - "Activation denials distinguish inactive routes from known routes that reject external entry per D-06."
    - "Dynamic-segment route matching behaves consistently across Elixir, iOS, and Android."
  artifacts:
    - path: "lib/crosswake/shell/activation.ex"
      provides: "Manifest-first activation enforcement with entry denial split"
    - path: "examples/ios_shell_host/CrosswakeShell/ActivationCoordinator.swift"
      provides: "iOS URL normalization and entry enforcement aligned to shared denial vocabulary"
    - path: "examples/android_shell_host/app/src/main/java/dev/crosswake/shell/ActivationCoordinator.kt"
      provides: "Android URL normalization and entry enforcement aligned to shared denial vocabulary"
  key_links:
    - from: "lib/crosswake/shell/activation.ex"
      to: "lib/crosswake/compatibility/route_gate.ex"
      via: "route-gate evaluation uses manifest entry truth before runtime mount"
      pattern: "entry|inactive_route"
    - from: "lib/crosswake/shell/activation.ex"
      to: "examples/ios_shell_host/CrosswakeShell/ActivationCoordinator.swift"
      via: "shared denial reason and segment-matching semantics"
      pattern: "entry|inactive_route"
---

<objective>
Enforce the deep-link entry contract across Elixir and both checked-in native shells.

Purpose: External entry is only credible if every runtime tier makes the same allow-or-deny decision before the route mounts.
Output: Shared denial vocabulary, segment-aware path matching parity, and iOS/Android activation coordinators that mirror the Elixir contract.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/16-system-context-bridges/16-CONTEXT.md
@.planning/phases/16-system-context-bridges/16-RESEARCH.md
@.planning/phases/16-system-context-bridges/16-PATTERNS.md
@.planning/phases/16-system-context-bridges/16-01-PLAN.md
@lib/crosswake/shell/activation.ex
@lib/crosswake/shell/denial.ex
@examples/ios_shell_host/CrosswakeShell/ActivationCoordinator.swift
@examples/android_shell_host/app/src/main/java/dev/crosswake/shell/ActivationCoordinator.kt

<interfaces>
From lib/crosswake/shell/activation.ex:
```elixir
@spec new_request(keyword()) :: Request.t()
@spec resolve(Root.t(), Request.t()) :: Decision.t()
@spec allow(Request.t(), RouteEntry.t()) :: Decision.t()
@spec deny(Request.t(), String.t(), Denial.t()) :: Decision.t()
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Split inactive-route from external-entry-denied activation results</name>
  <files>lib/crosswake/shell/activation.ex, lib/crosswake/shell/denial.ex, lib/crosswake/compatibility/compatibility.ex, lib/crosswake/compatibility/route_gate.ex, test/crosswake/shell/activation_test.exs</files>
  <behavior>
    - Test 1: Unknown paths still deny as `inactive_route`.
    - Test 2: A manifest route that exists but does not allow external entry denies with its own machine-readable reason per D-06.
    - Test 3: Route-gate and compatibility mapping preserve fail-closed recovery behavior rather than falling through to a generic mismatch.
  </behavior>
  <action>Update shared activation evaluation, denial vocabulary, and compatibility-to-denial mapping so inbound requests fail closed with a distinct reason when a route exists but external entry is not approved. Keep `inactive_route` reserved for unknown or inactive routes. Preserve manifest-first evaluation and do not add any outbound `deep_link` bridge command or navigation authority per D-02 and D-03.</action>
  <verify>
    <automated>mix test test/crosswake/shell/activation_test.exs</automated>
  </verify>
  <done>Elixir activation logic distinguishes route absence from entry rejection and keeps the denial surface typed and test-backed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Align iOS and Android activation coordinators with shared path and denial semantics</name>
  <files>examples/ios_shell_host/CrosswakeShell/ActivationCoordinator.swift, examples/ios_shell_host/CrosswakeShellTests/ActivationCoordinatorTests.swift, examples/android_shell_host/app/src/main/java/dev/crosswake/shell/ActivationCoordinator.kt, examples/android_shell_host/app/src/test/java/dev/crosswake/shell/ActivationCoordinatorTest.kt</files>
  <behavior>
    - Test 1: iOS and Android deny entry-disallowed routes with the same conceptual reason as Elixir.
    - Test 2: Dynamic-segment routes such as `/.../:id` match consistently across both native shells and the shared Elixir tests.
    - Test 3: Incoming URLs still route through manifest-first activation before any runtime mounts per D-01.
  </behavior>
  <action>Update both native activation coordinators to consume the new manifest route-entry truth and to mirror the shared denial split. Align segment-aware path matching with the Android precedent noted in research so Elixir, iOS, and Android treat manifest paths the same way. Keep native logic scoped to inbound activation; do not introduce any route-local navigation helpers or shell-local allowlist file.</action>
  <verify>
    <automated>xcodebuild test -project examples/ios_shell_host/CrosswakeShell.xcodeproj -scheme CrosswakeShell -destination 'platform=iOS Simulator,name=iPhone 16'</automated>
    <automated>java -version && (cd examples/android_shell_host && ./gradlew test --tests dev.crosswake.shell.ActivationCoordinatorTest)</automated>
  </verify>
  <done>Both checked-in shells enforce the same deep-link entry contract and dynamic-path matching behavior as the shared Elixir layer.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| External URL -> shell activation | Untrusted app-entry URLs can try to reach routes before runtime mount. |
| Elixir denial contract -> native denial UI | Drift here would mask fail-closed behavior or mislead operators. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-04 | E | activation path | mitigate | Require manifest route existence plus explicit external-entry approval before allow. |
| T-16-05 | T | cross-tier path matching | mitigate | Mirror segment-aware path rules across Elixir, iOS, and Android with dedicated tests. |
| T-16-06 | R | denial reason drift | mitigate | Assert a stable denial split in Elixir and native activation tests. |
</threat_model>

<verification>
Run `mix test test/crosswake/shell/activation_test.exs`, then `xcodebuild test -project examples/ios_shell_host/CrosswakeShell.xcodeproj -scheme CrosswakeShell -destination 'platform=iOS Simulator,name=iPhone 16'`. Treat Android verification as a preflight-gated step: run `java -version` first, then run `(cd examples/android_shell_host && ./gradlew test --tests dev.crosswake.shell.ActivationCoordinatorTest)` only when a JDK is available locally or in CI.
</verification>

<success_criteria>
Elixir, iOS, and Android all deny or allow the same inbound route entry, and operators can tell the difference between an unknown route and an entry-disallowed route.
</success_criteria>

<output>
After completion, create `.planning/phases/16-system-context-bridges/16-02-SUMMARY.md`
</output>
