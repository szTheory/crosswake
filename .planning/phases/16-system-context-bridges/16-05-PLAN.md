---
phase: "16-system-context-bridges"
plan: "05"
type: execute
wave: 4
depends_on:
  - "16-02"
  - "16-03"
  - "16-04"
files_modified:
  - "lib/crosswake/support_matrix/support_matrix.ex"
  - "lib/crosswake/doctor/doctor.ex"
  - "test/crosswake/doctor/doctor_test.exs"
  - "guides/native_shell.md"
  - "guides/bridge.md"
  - "guides/capabilities.md"
  - "guides/support_matrix.md"
autonomous: true
requirements:
  - "CAP-DEEPLINK"
  - "CAP-PERMSTATUS"
must_haves:
  truths:
    - "Doctor and support-matrix output distinguish inactive routes from external-entry-denied routes for deep-link activation per D-06."
    - "Doctor and guides state the exact `permissions.status` scope: read-only, `notifications` alias only, no request orchestration."
    - "Public docs keep `deep_link` positioned as inbound shell activation truth rather than a bridge navigation bus."
  artifacts:
    - path: "lib/crosswake/doctor/doctor.ex"
      provides: "Doctor checks and reporting aligned to deep-link and permissions-status reality"
    - path: "lib/crosswake/support_matrix/support_matrix.ex"
      provides: "Support truth for denial reasons, prerequisites, and fallback behavior"
    - path: "guides/support_matrix.md"
      provides: "Public support posture aligned to the implementation and doctor output"
  key_links:
    - from: "lib/crosswake/doctor/doctor.ex"
      to: "lib/crosswake/support_matrix/support_matrix.ex"
      via: "diagnostic output follows the same denial and prerequisite truth as public support data"
      pattern: "deep_link|permissions.status|notifications"
    - from: "guides/bridge.md"
      to: "guides/native_shell.md"
      via: "public docs keep bridge scope and shell activation scope distinct"
      pattern: "deep_link|permissions.status"
---

<objective>
Publish the doctor, support-matrix, and guide truth for both Phase 16 capability surfaces.

Purpose: Crosswake treats diagnostics and rough-edge docs as product surface, so the runtime contract is incomplete until operator-facing truth matches the implementation.
Output: Updated doctor checks, support-matrix truth, and public guides for inbound deep-link entry policy and narrow permissions-status support.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/16-system-context-bridges/16-CONTEXT.md
@.planning/phases/16-system-context-bridges/16-RESEARCH.md
@.planning/phases/16-system-context-bridges/16-02-PLAN.md
@.planning/phases/16-system-context-bridges/16-03-PLAN.md
@.planning/phases/16-system-context-bridges/16-04-PLAN.md
@lib/crosswake/support_matrix/support_matrix.ex
@lib/crosswake/doctor/doctor.ex
@guides/native_shell.md
@guides/bridge.md
@guides/capabilities.md
@guides/support_matrix.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Align support-matrix and doctor truth to the shipped Phase 16 behavior</name>
  <files>lib/crosswake/support_matrix/support_matrix.ex, lib/crosswake/doctor/doctor.ex, test/crosswake/doctor/doctor_test.exs</files>
  <behavior>
    - Test 1: Doctor output distinguishes inactive routes from entry-disallowed routes for inbound deep links.
    - Test 2: Doctor output names the exact `permissions.status` scope and prerequisites instead of implying a generic permission broker.
    - Test 3: Support truth for both capability surfaces matches the fail-closed implementation and denial posture.
  </behavior>
  <action>Update support-matrix data and `mix crosswake.doctor` checks so they surface the real Phase 16 posture. For deep links, explicitly separate inactive-route versus external-entry-denied activation outcomes per D-06. For permissions status, state that the feature is read-only, alias-keyed, and initially supports `notifications` only, with request flows deferred. Keep denial behavior and fallback language aligned with implementation and support truth rather than optimistic prose.</action>
  <verify>
    <automated>mix test test/crosswake/doctor/doctor_test.exs test/crosswake/bridge/registry_test.exs test/crosswake/shell/activation_test.exs && mix crosswake.doctor</automated>
  </verify>
  <done>Doctor and support-matrix surfaces expose the same denial, prerequisite, and fallback truth as the runtime implementation.</done>
</task>

<task type="auto">
  <name>Task 2: Refresh public guides without reintroducing bridge or alias ambiguity</name>
  <files>guides/native_shell.md, guides/bridge.md, guides/capabilities.md, guides/support_matrix.md</files>
  <action>Update the public guides so they preserve the resolved Phase 16 boundaries. `deep_link` must remain inbound shell activation only per D-01 through D-03, with explicit external-entry approval required per D-04 and a clear denial split per D-06. `permissions.status` must be documented as a narrow read-only bridge with `notifications` as the only initial public alias, a normalized primary status, optional secondary detail, and no permission-request orchestration per D-07 through D-12. Do not reintroduce any open-question language already resolved in `16-RESEARCH.md`.</action>
  <verify>
    <automated>mix test test/crosswake/doctor/doctor_test.exs && rg -n "notifications|external entry|inactive_route|deep_link" guides/native_shell.md guides/bridge.md guides/capabilities.md guides/support_matrix.md</automated>
  </verify>
  <done>Public documentation states the exact Phase 16 contract surfaces and preserves the resolved research decisions without ambiguity.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Runtime truth -> operator diagnostics | If doctor output drifts, operators cannot trust failures or support posture. |
| Internal contract -> public docs | If guides overstate support, adopters will design against behavior that does not exist. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-13 | R | doctor output | mitigate | Assert denial and prerequisite wording in doctor tests and `mix crosswake.doctor` output. |
| T-16-14 | I | public support docs | mitigate | Document exact alias scope and deep-link authority boundaries, with no generic broker or navigation-bus language. |
| T-16-15 | E | docs-driven misuse | mitigate | Keep guides explicit that `deep_link` is shell activation only and `permissions.status` is read-only, one-shot, and narrow. |
</threat_model>

<verification>
Run `mix test test/crosswake/doctor/doctor_test.exs test/crosswake/bridge/registry_test.exs test/crosswake/shell/activation_test.exs && mix crosswake.doctor`, then confirm the guide files explicitly mention external-entry approval, inactive-route versus entry-denied behavior, and `notifications` as the sole initial permission alias.
</verification>

<success_criteria>
Doctor output, support truth, and public guides all tell the same story as the shipped Phase 16 implementation and preserve the resolved research decisions.
</success_criteria>

<output>
After completion, create `.planning/phases/16-system-context-bridges/16-05-SUMMARY.md`
</output>
