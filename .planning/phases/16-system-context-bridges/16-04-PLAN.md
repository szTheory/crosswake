---
phase: "16-system-context-bridges"
plan: "04"
type: execute
wave: 3
depends_on:
  - "16-03"
files_modified:
  - "examples/ios_shell_host/CrosswakeShell/BridgeChannel.swift"
  - "examples/ios_shell_host/CrosswakeShell/PermissionStatusProvider.swift"
  - "examples/ios_shell_host/CrosswakeShellTests/BridgeChannelTests.swift"
  - "examples/android_shell_host/app/src/main/java/dev/crosswake/shell/BridgeChannel.kt"
  - "examples/android_shell_host/app/src/main/java/dev/crosswake/shell/PermissionStatusProvider.kt"
  - "examples/android_shell_host/app/src/test/java/dev/crosswake/shell/BridgeChannelTest.kt"
autonomous: true
requirements:
  - "CAP-PERMSTATUS"
must_haves:
  truths:
    - "iOS and Android can answer a one-shot `permissions.status` query for the supported alias without prompting the user."
    - "Both shells normalize the primary reply into the shared Crosswake status contract and keep richer platform facts inside `detail`."
    - "Unsupported aliases or undeclared route use deny through the bounded bridge rather than guessing."
  artifacts:
    - path: "examples/ios_shell_host/CrosswakeShell/PermissionStatusProvider.swift"
      provides: "iOS runtime permission-status provider for the supported alias"
    - path: "examples/android_shell_host/app/src/main/java/dev/crosswake/shell/PermissionStatusProvider.kt"
      provides: "Android runtime permission-status provider for the supported alias"
    - path: "examples/ios_shell_host/CrosswakeShell/BridgeChannel.swift"
      provides: "Native bridge dispatch for permissions.status"
  key_links:
    - from: "examples/ios_shell_host/CrosswakeShell/BridgeChannel.swift"
      to: "examples/ios_shell_host/CrosswakeShell/PermissionStatusProvider.swift"
      via: "command dispatch invokes the provider and returns normalized payload"
      pattern: "permissions.status|detail|notifications"
    - from: "examples/android_shell_host/app/src/main/java/dev/crosswake/shell/BridgeChannel.kt"
      to: "examples/android_shell_host/app/src/main/java/dev/crosswake/shell/PermissionStatusProvider.kt"
      via: "command dispatch invokes the provider and returns normalized payload"
      pattern: "permissions.status|detail|notifications"
---

<objective>
Implement the native `permissions.status` providers and bridge handlers on iOS and Android.

Purpose: The Elixir contract is only useful if both checked-in shells can execute it honestly at call time without widening bridge authority.
Output: iOS and Android provider helpers plus bridge-channel branches that return normalized permission status for the supported alias.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/16-system-context-bridges/16-CONTEXT.md
@.planning/phases/16-system-context-bridges/16-RESEARCH.md
@.planning/phases/16-system-context-bridges/16-PATTERNS.md
@.planning/phases/16-system-context-bridges/16-03-PLAN.md
@examples/ios_shell_host/CrosswakeShell/BridgeChannel.swift
@examples/android_shell_host/app/src/main/java/dev/crosswake/shell/BridgeChannel.kt
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add bounded-bridge dispatch for permissions-status on both shells</name>
  <files>examples/ios_shell_host/CrosswakeShell/BridgeChannel.swift, examples/ios_shell_host/CrosswakeShellTests/BridgeChannelTests.swift, examples/android_shell_host/app/src/main/java/dev/crosswake/shell/BridgeChannel.kt, examples/android_shell_host/app/src/test/java/dev/crosswake/shell/BridgeChannelTest.kt</files>
  <behavior>
    - Test 1: The bridge checks the normal capability/version allowlist before executing `permissions.status`.
    - Test 2: Unsupported aliases deny through the normal bridge error path instead of synthesizing platform-specific fallback values.
    - Test 3: The command remains one-shot and read-only; no request flow or background observer is introduced.
  </behavior>
  <action>Add one native bridge-channel branch per platform for `permissions.status`, following the existing low-frequency command pattern. Reuse the same allowlist and version checks as other bounded bridge commands before calling platform-specific providers. Keep the command low-frequency and read-only. Do not add permission request orchestration, change observers, or app-wide permission inventory payloads per D-08.</action>
  <verify>
    <automated>xcodebuild test -project examples/ios_shell_host/CrosswakeShell.xcodeproj -scheme CrosswakeShell -destination 'platform=iOS Simulator,name=iPhone 16'</automated>
    <automated>java -version && (cd examples/android_shell_host && ./gradlew test --tests dev.crosswake.shell.BridgeChannelTest)</automated>
  </verify>
  <done>Native bridge dispatch exists for `permissions.status` and rejects undeclared or unsupported use through the bounded bridge.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement iOS and Android permission-status providers</name>
  <files>examples/ios_shell_host/CrosswakeShell/PermissionStatusProvider.swift, examples/android_shell_host/app/src/main/java/dev/crosswake/shell/PermissionStatusProvider.kt, examples/ios_shell_host/CrosswakeShellTests/BridgeChannelTests.swift, examples/android_shell_host/app/src/test/java/dev/crosswake/shell/BridgeChannelTest.kt</files>
  <behavior>
    - Test 1: The supported `notifications` alias queries live platform state at call time.
    - Test 2: The primary reply is normalized into the shared Crosswake status enum, while native-specific nuance stays in `detail`.
    - Test 3: Platforms do not claim richer cross-platform authority than the read-only check can support.
  </behavior>
  <action>Create provider helpers for iOS and Android that implement the resolved alias scope: `notifications` only. Query runtime state at call time using the platform APIs identified in research, map that into the shared normalized status, and include optional secondary detail only when it is honest and stable enough to help support or caller diagnostics. Keep the providers query-only. Do not cache stale permission snapshots or expose a generic permission broker surface.</action>
  <verify>
    <automated>xcodebuild test -project examples/ios_shell_host/CrosswakeShell.xcodeproj -scheme CrosswakeShell -destination 'platform=iOS Simulator,name=iPhone 16'</automated>
    <automated>java -version && (cd examples/android_shell_host && ./gradlew test --tests dev.crosswake.shell.BridgeChannelTest)</automated>
  </verify>
  <done>Both native shells can answer the narrow `permissions.status` query honestly and return the shared Crosswake reply contract.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Native bridge -> platform APIs | Platform-specific state is interpreted and normalized here. |
| Native provider -> shared bridge reply | Over-claiming permission state here would mislead Phoenix route logic. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-10 | E | native bridge handler | mitigate | Keep `permissions.status` behind existing capability/version checks and deny unsupported aliases. |
| T-16-11 | T | provider results | mitigate | Query platform state at call time instead of returning cached permission truth. |
| T-16-12 | I | permission detail payload | mitigate | Keep platform-specific nuance secondary in `detail` and avoid exposing a broad app-level permission inventory. |
</threat_model>

<verification>
Run `xcodebuild test -project examples/ios_shell_host/CrosswakeShell.xcodeproj -scheme CrosswakeShell -destination 'platform=iOS Simulator,name=iPhone 16'`. Treat Android verification as a preflight-gated step: run `java -version` first, then run `(cd examples/android_shell_host && ./gradlew test --tests dev.crosswake.shell.BridgeChannelTest)` only when a JDK is available locally or in CI.
</verification>

<success_criteria>
Both checked-in shells implement the narrow read-only permissions-status surface without widening bridge authority or alias scope.
</success_criteria>

<output>
After completion, create `.planning/phases/16-system-context-bridges/16-04-SUMMARY.md`
</output>
