---
phase: 162-physical-iphone-adoption-proof
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/crosswake/proof_lane/physical_iphone_contract.ex
  - lib/crosswake/proof_lane/physical_iphone_preflight.ex
  - lib/mix/tasks/crosswake.proof_lane.physical_iphone.ex
  - test/crosswake/proof_lane/physical_iphone_preflight_test.exs
  - test/mix/tasks/crosswake.proof_lane.physical_iphone_test.exs
autonomous: true
requirements: [DEVICE-01, DEVICE-02, DEVICE-03, DEVICE-04, DEVICE-05, DEVICE-06, DEVICE-07]
must_haves:
  truths:
    - "Per D-01 and D-04, one production command evaluates the validated route row, generated-lane version, signed physical destination, runnable host, fixture/media adapter, and backend authority controls before reset, staging, capture, or promotion."
    - "An absent, malformed, simulator, or untrusted prerequisite produces one stable non-echoing blocked result and zero downstream side effects; a simulator can never satisfy physical proof."
    - "Per D-02 and D-05, one fixed DEVICE assertion vocabulary assigns device-local assertions to XCTest/XCUITest and backend-authority assertions to Phoenix without allowing either side to impersonate the other."
  artifacts:
    - path: "lib/crosswake/proof_lane/physical_iphone_contract.ex"
      provides: "Versioned physical_iphone device class, iOS runtime-line grammar, fixed DEVICE assertion IDs, owners, and closed outcomes."
    - path: "lib/crosswake/proof_lane/physical_iphone_preflight.ex"
      provides: "Ordered, callback-driven, fail-closed preflight with stable blocked rule IDs."
    - path: "lib/mix/tasks/crosswake.proof_lane.physical_iphone.ex"
      provides: "Single host-owned physical-iPhone entry command and injectable runner boundary."
  key_links:
    - from: "Mix.Tasks.Crosswake.ProofLane.PhysicalIphone.run/1"
      to: "Crosswake.ProofLane.PhysicalIphonePreflight.check/1"
      via: "preflight must return ready before runner invocation"
      pattern: "PhysicalIphonePreflight|blocked|runner"
    - from: "PhysicalIphoneContract.assertions/0"
      to: "later generated device and Phoenix reports"
      via: "one fixed owner-tagged assertion manifest"
      pattern: "device_local|backend_authority|DEVICE"
  prohibitions:
    - "requirement_id: DEVICE-01 | category: values | statement: The proof must remain one first-adopter offline-study flow on one iOS runtime line and must not become Android, a device farm, background replay, generic sync, or generic storage. | status: unresolved | verification: flagged-unverified"
    - "requirement_id: DEVICE-04 | category: safety | statement: Old-scope retained work must never replay, render, delete, or gain authority under a logged-out or newly active account. | status: unresolved | verification: flagged-unverified"
    - "requirement_id: DEVICE-06 | category: safety | statement: Durable evidence must never contain or fingerprint raw answers, media, transcripts, credentials, account or stable device identifiers, tokens, routes/paths, endpoint values, logs, screenshots, video, raw output, xcresult bundles, fixtures, or binaries. | status: unresolved | verification: flagged-unverified"
flagged_assumptions:
  - "[UNRESOLVED-SPEC-PROBE][DEVICE-01 unclassified] D-01/D-02 and the approved pack seam completely define physical install/audio proof; no codec, archive, media path, device identity, or second pack behavior is inferred."
  - "[UNRESOLVED-SPEC-PROBE][DEVICE-02 unclassified] D-01-D-03 define the sequential selected/free-form persistence and exactly-once boundary; no background replay, batch concurrency, or second island is inferred."
  - "[UNRESOLVED-SPEC-PROBE][DEVICE-03 unclassified] D-07-D-09 define visible retained reject/conflict recovery; no domain resolver, automatic retry, deletion, or last-write-wins policy is inferred."
  - "[UNRESOLVED-SPEC-PROBE][DEVICE-04 unclassified] D-02/D-08 define fence-first logout/account switching and retained old partitions; no account mapping, deletion policy, or client authority is inferred."
  - "[UNRESOLVED-SPEC-PROBE][DEVICE-05 unclassified] D-02/D-08 define independent entry and replay disablement with queue retention; no Crosswake flag service, rollout policy, or online-only downgrade is inferred."
  - "[UNRESOLVED-SPEC-PROBE][DEVICE-06 unclassified] D-05/D-06 define the complete retained schema, approved hash sources, canonical reparse, scan, and no-replace promotion; no diagnostic bundle or extra metadata is inferred."
  - "[UNRESOLVED-SPEC-PROBE][DEVICE-07 unclassified] The roadmap and D-05/D-06 define the complete support claim as one first-adopter flow on one iOS runtime line; no Android, all-iPhone, generic sync/storage, background, or multi-island claim is inferred."
---

<objective>
Prove the hardest constraint first: the physical run cannot begin until every host, route, device, signing, media, and backend-authority prerequisite is trusted.

Purpose: Make unsafe or unavailable environments stop deterministically before any destructive fixture reset or evidence activity, while establishing one fixed assertion contract for the later driver.
Output: Closed physical-iPhone contract, ordered preflight, and one production Mix entry point.
</objective>

<artifacts_this_phase_produces>
- `Crosswake.ProofLane.PhysicalIphoneContract` with `device_class/0`, `schema_version/0`, `assertions/0`, `ios_runtime_line/1`, and fixed device/backend ownership.
- `Crosswake.ProofLane.PhysicalIphonePreflight.check/1` returning only `{:ready, run_contract}` or `{:blocked, rule_id}` without rejected values.
- `mix crosswake.proof_lane.physical_iphone` as the sole run/promotion entry point; later plans fill its stable runner and promotion seams.
</artifacts_this_phase_produces>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/ADR-FIRST-B2C-ADOPTER.md
@.planning/FIRST-B2C-ADOPTER-ROUTE-POLICY-MAP.md
@.planning/todos/TODO-002-first-b2c-adopter-route-inputs.md
@.planning/phases/162-physical-iphone-adoption-proof/162-CONTEXT.md
@.planning/phases/162-physical-iphone-adoption-proof/162-RESEARCH.md
@.planning/phases/162-physical-iphone-adoption-proof/162-PATTERNS.md
@lib/crosswake/adoption/route_inventory.ex
@lib/crosswake/proof_lane/config.ex
@lib/crosswake/proof_lane/evidence.ex

<interfaces>
- `Crosswake.Adoption.RouteInventory.validate_inventory/1` plus `promotion_status/1` is the only route-row admission authority; empty or unknown-blocking input cannot be promoted.
- `Crosswake.ProofLane.Config.load/0` and normalized config are the existing host-owned proof-lane source; do not add sensitive CLI flags.
- Existing proof results are closed to `passed`, `blocked`, and `unavailable`; physical preflight uses `blocked` for an unmet prerequisite and never creates partial evidence.
</interfaces>
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Trace one command through preflight to a gated runner</name>
  <reversibility rating="costly">D-01 and D-04 establish a host-consumed run sequence and stable denial contract; changing the ordering later requires coordinated generator, host, test-target, and evidence changes.</reversibility>
  <read_first>
    - AGENTS.md
    - .planning/phases/162-physical-iphone-adoption-proof/162-CONTEXT.md
    - .planning/phases/162-physical-iphone-adoption-proof/162-RESEARCH.md
    - .planning/todos/TODO-002-first-b2c-adopter-route-inputs.md
    - lib/crosswake/adoption/route_inventory.ex
    - lib/crosswake/proof_lane/config.ex
    - lib/mix/tasks/crosswake.gen.proof_lane.ex
  </read_first>
  <files>lib/crosswake/proof_lane/physical_iphone_preflight.ex, lib/mix/tasks/crosswake.proof_lane.physical_iphone.ex, test/crosswake/proof_lane/physical_iphone_preflight_test.exs, test/mix/tasks/crosswake.proof_lane.physical_iphone_test.exs</files>
  <behavior>
    - "RED: current repository state returns one JSON blocked result for TODO-002/device/host readiness and never calls the injected runner."
    - "RED: a complete synthetic closed prerequisite set invokes the runner exactly once only after every preflight callback succeeds."
    - "Exceptions, malformed callback output, simulator destinations, and missing signing/host/backend capabilities collapse to stable non-echoing blocked rules."
    - "No preflight branch resets fixtures, creates a destination, starts Xcode capture, or publishes evidence."
  </behavior>
  <action>Implement the production command as the D-01/D-04 vertical tracer. Resolve normalized proof-lane configuration, validate a supplied sanitized inventory through `RouteInventory`, require eligible promotion status, then evaluate generated-lane freshness, signed physical destination, runnable host/backend, configured host fixture/pack adapter, exact media requirement readiness, replay/reject/conflict/scope/feature controls, and a writable-but-absent destination parent using injected zero-arity/one-arity callbacks with closed return values. Preserve this order. Return only a stable rule ID and `blocked`; do not echo callback data or environment values. After ready, pass an immutable sanitized run contract to an injected runner once. Make `--preflight-only --json` an exact machine interface and reject unknown flags. The current missing TODO-002/device state is an expected closed result, not proof success.</action>
  <acceptance_criteria>
    - The command reaches a runner only when all D-04 prerequisites pass in order.
    - Every absent/untrusted prerequisite returns one stable JSON `blocked` result, exits nonzero, and produces no reset/stage/capture/promotion call.
    - Simulator and ambiguous destination classes are rejected before runner invocation.
    - Output contains no route path, device identifier, endpoint, credential, token, fixture, media, or callback detail.
  </acceptance_criteria>
  <verify><automated>mix test test/crosswake/proof_lane/physical_iphone_preflight_test.exs test/mix/tasks/crosswake.proof_lane.physical_iphone_test.exs --max-failures 1 &amp;&amp; output=$(mix crosswake.proof_lane.physical_iphone --preflight-only --json 2&gt;&amp;1); status=$?; test "$status" -ne 0; printf '%s\n' "$output" | jq -e '.outcome == "blocked" and (.rule_id | type == "string") and (keys | sort == ["outcome","rule_id"])' &gt;/dev/null</automated></verify>
  <done>The one production entry point proves fail-closed preflight end to end and cannot cross into device work before trusted readiness.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Freeze the physical DEVICE assertion and runtime contract</name>
  <reversibility rating="costly">D-05 fixes a versioned device class, runtime line, and assertion vocabulary consumed by generated hosts and retained evidence.</reversibility>
  <read_first>
    - .planning/phases/162-physical-iphone-adoption-proof/162-CONTEXT.md
    - .planning/phases/162-physical-iphone-adoption-proof/162-VALIDATION.md
    - lib/crosswake/proof_lane/evidence.ex
    - priv/templates/crosswake/proof_lane/ios/ProofLaneDriver.swift.eex
  </read_first>
  <files>lib/crosswake/proof_lane/physical_iphone_contract.ex, test/crosswake/proof_lane/physical_iphone_preflight_test.exs</files>
  <behavior>
    - "The contract accepts only device_class physical_iphone and a bounded low-cardinality iOS major/minor runtime line."
    - "The fixed assertion manifest covers pack install/audio, offline selected/free-form persistence, relaunch, accepted exactly-once/empty outbox, rejection/conflict retention, logout/account-switch fencing, entry disablement, replay disablement, and redacted promotion."
    - "Each assertion has exactly one owner: device_local or backend_authority; every outcome is passed, blocked, or unavailable."
    - "Unknown, duplicate, missing, reordered, or cross-owner assertion reports fail closed with stable non-echoing rules."
  </behavior>
  <action>Create the closed schema consumed by later templates and evidence. Use stable identifiers prefixed `PI-` and map every DEVICE-01..07 behavior to a fixed assertion with exactly one D-02 owner. Treat the complete ordered manifest as required; do not accept subsets or open strings. Allow only a low-cardinality iOS runtime line such as major.minor, never build numbers, model names, UDIDs, serials, or other fingerprinting detail. Keep assertion validation pure and canonical so Elixir, generated Swift, Phoenix output, and evidence can compare the same order.</action>
  <acceptance_criteria>
    - The manifest completely covers the ten-step exit test and DEVICE-01..07 without an open extension bag.
    - Device-local versus backend-authority ownership is explicit and mutually exclusive.
    - Runtime validation rejects fingerprinting precision and device identifiers.
    - Exact-manifest and negative contract tests pass.
  </acceptance_criteria>
  <verify><automated>mix test test/crosswake/proof_lane/physical_iphone_preflight_test.exs --max-failures 1 &amp;&amp; mix run -e 'alias Crosswake.ProofLane.PhysicalIphoneContract, as: C; true = C.device_class() == :physical_iphone; true = Enum.all?(C.assertions(), &amp;(&amp;1.owner in [:device_local, :backend_authority]))'</automated></verify>
  <done>All later layers share one closed physical-only assertion, owner, outcome, and runtime vocabulary.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| host config/inventory -> preflight | Host-controlled values may be absent, malformed, sensitive, or identifying. |
| local Xcode/device discovery -> runner | Simulator or untrusted device/signing state must not gain physical proof authority. |
| preflight -> runner | Any side effect before complete readiness could fabricate or partially promote evidence. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-162-01 | Spoofing | destination classification | high | mitigate | Task 1 accepts only a trusted physical destination result and rejects simulator/unknown before runner invocation. |
| T-162-02 | Information Disclosure | preflight errors/output | high | mitigate | Task 1 returns only stable rule IDs and closed outcomes; negative tests inject sensitive callback values and assert non-echoing output. |
| T-162-03 | Tampering | prerequisite ordering | high | mitigate | Task 1 tests runner/reset/stage/capture/promotion counters remain zero until the ordered preflight completes. |
| T-162-04 | Elevation of Privilege | assertion ownership | high | mitigate | Task 2 assigns each assertion exactly one device-local or backend-authority owner and rejects cross-owner reports. |
| T-162-05 | Denial of Service | missing external readiness | low | accept | A stable blocked result is the intended bounded state until the adopter supplies the required route, host, signing, and phone prerequisites. |

ASVS L1: all high-severity threats above are mitigated by focused executable tests before later waves consume the contracts.
</threat_model>

<verification>
- `mix test test/crosswake/proof_lane/physical_iphone_preflight_test.exs test/mix/tasks/crosswake.proof_lane.physical_iphone_test.exs --max-failures 1`
- Current environment returns exactly one non-echoing blocked JSON object and retains no evidence.
- `mix format --check-formatted` passes for all Phase 162 Elixir sources/tests created here.
</verification>

<success_criteria>
- D-01/D-04 preflight is the sole gateway to the physical runner.
- D-02/D-05 assertion authority and physical device/runtime vocabulary are closed and versioned.
- All seven spec-less DEVICE assumptions remain visibly flagged; all recalled values/safety prohibitions remain descriptor-less and unverified rather than silently treated as resolved.
</success_criteria>

<output>
Create `.planning/phases/162-physical-iphone-adoption-proof/162-01-SUMMARY.md` when done.
</output>
