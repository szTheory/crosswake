---
phase: 162-physical-iphone-adoption-proof
plan: "14"
type: execute
wave: 14
depends_on: [162-12, 162-13]
files_modified:
  - lib/crosswake/support_matrix/renderer.ex
  - test/crosswake/support_matrix/renderer_test.exs
  - guides/support_matrix.md
  - .planning/phases/162-physical-iphone-adoption-proof/evidence/physical_iphone/proof-lane-evidence.json
  - .planning/phases/162-physical-iphone-adoption-proof/evidence/physical_iphone/.complete
autonomous: true
gap_closure: true
requirements: [DEVICE-01, DEVICE-02, DEVICE-03, DEVICE-04, DEVICE-05, DEVICE-06, DEVICE-07]
user_setup:
  - service: first-adopter-host
    why: "The standard proof task requires the validated TODO-002 row plus configured host fixture/media/replay/session/scope/gate controls and a device-reachable Phoenix endpoint."
    env_vars:
      - name: CROSSWAKE_PHYSICAL_IPHONE_HOST_BASE_URL
        source: "Device-reachable base URL for the configured examples/phoenix_host MIX_ENV=test endpoint"
  - service: physical-iphone
    why: "Apple signing and one connected trusted physical iPhone are unavoidable preflight inputs; the standard task discovers and validates the destination."
    dashboard_config:
      - task: "Connect and trust one physical iPhone and make the existing CrosswakeProofLane scheme signable."
        location: "Local Xcode signing/device setup"
must_haves:
  truths:
    - "The public physical-study row is first restored to `verification required`; a prior closed evidence envelope or completed summary cannot keep the support claim promoted while repaired behavior/serialization proof is absent."
    - "The row returns to existing `device evidence` vocabulary only after `(cd examples/phoenix_host && MIX_ENV=test mix crosswake.proof_lane.physical_iphone --run --promote --json)` passes its closed D-04 preflight, executes the repaired device plus Phoenix producers, atomically publishes a new no-replace record, and completes its internal source-bound Evidence.check/2 against the canonical bytes from that run."
    - "The pre-repair physical record is explicitly invalidated and removed only after support is withdrawn; neither it nor hand-constructed canonical bytes can authorize the repaired claim."
    - "Per D-05/D-06 and DEVICE-07, promoted prose remains one first-adopter flow on one recorded iOS runtime line with every Android/background/generic-storage/generic-sync/multiple-island/simulator/every-iPhone non-claim."
    - "Renderer output, focused tests, and `guides/support_matrix.md` remain byte-identical; no runtime filesystem probe or new support label is introduced."
  prohibitions:
    - statement: "File existence, summaries, requirement checkboxes, unavailable simulator output, or source-bound envelope validity alone must not promote the physical-study support row."
      verification: flagged-unverified
    - statement: "Public prose must not expose raw answers, markers, scope/account/device identifiers, host facts, paths, media, credentials, or identifying adopter facts."
      verification: flagged-unverified
    - statement: "The repaired claim must not widen to Android, background behavior, generic storage or sync, multiple islands, simulator substitution, or every-iPhone coverage."
      verification: flagged-unverified
  artifacts:
    - path: "lib/crosswake/support_matrix/renderer.ex"
      provides: "Fail-closed then freshly re-earned canonical physical-study support row."
    - path: "test/crosswake/support_matrix/renderer_test.exs"
      provides: "Regression proving fresh repaired behavior plus serialization/source-bound admission before promotion."
    - path: "guides/support_matrix.md"
      provides: "Regenerated narrow public support truth."
    - path: ".planning/phases/162-physical-iphone-adoption-proof/evidence/physical_iphone/proof-lane-evidence.json"
      provides: "Fresh repaired-tree record created only by the standard host-owned production task."
  key_links:
    - from: "fresh Plan 162-12 repair and Plan 162-13 serialization/parser proof"
      to: "physical-iPhone offline study support row"
      via: "Task 2's standard physical Mix task reruns closed preflight, both real producers, no-replace promotion, and internal Evidence.check/2 before the canonical re-promotion edit"
      pattern: "crosswake\.proof_lane\.physical_iphone --run --promote --json"
    - from: "authorized canonical source bytes"
      to: "retained physical evidence"
      via: "the standard Mix task passes the canonical bytes produced by this same run into Evidence.check/2 after no-replace promotion"
      pattern: "Evidence\.check"
    - from: "Crosswake.SupportMatrix.Renderer.render/1"
      to: "guides/support_matrix.md"
      via: "deterministic regeneration and byte equality"
      pattern: "physical-iPhone offline study"
---

<objective>
Withdraw the unearned public physical-study claim and invalidate the stale record, then re-promote the same narrow wording only through the standard closed-preflight physical Mix task and its fresh no-replace source-bound evidence.

Purpose: Make support truth fail closed across partial execution and ensure the prior structurally valid record cannot mask CR-01 or CR-02.
Output: A canonical support row that is either honestly verification-required or freshly re-earned under the repaired final tree.
</objective>

<artifacts_this_phase_produces>
- A RED/fail-closed support regression and intermediate `verification required` canonical/guide state.
- A GREEN admission regression tying device-evidence wording to one fresh successful standard physical Mix run after all D-04 prerequisites are ready.
- A newly promoted fixed evidence directory whose canonical bytes are produced and checked by that same standard run, never reconstructed from the stale record.
- Regenerated `guides/support_matrix.md` with unchanged vocabulary and explicit narrow non-claims.
- No new support status, runtime environment probe, evidence schema, or platform claim.
</artifacts_this_phase_produces>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/ADR-FIRST-B2C-ADOPTER.md
@.planning/phases/162-physical-iphone-adoption-proof/162-CONTEXT.md
@.planning/phases/162-physical-iphone-adoption-proof/162-VERIFICATION.md
@.planning/phases/162-physical-iphone-adoption-proof/162-12-SUMMARY.md
@.planning/phases/162-physical-iphone-adoption-proof/162-13-SUMMARY.md
@.planning/phases/162-physical-iphone-adoption-proof/162-09-SUMMARY.md
@lib/crosswake/proof_lane/evidence.ex
@lib/mix/tasks/crosswake.proof_lane.physical_iphone.ex
@examples/phoenix_host/lib/crosswake_example/physical_iphone_proof_host.ex
@lib/crosswake/support_matrix/renderer.ex
@test/crosswake/support_matrix/renderer_test.exs
@guides/support_matrix.md

<interfaces>
- `Crosswake.SupportMatrix.Renderer.render/1` remains the sole canonical public-guide source and must stay deterministic.
- `Mix.Tasks.Crosswake.ProofLane.PhysicalIphone.promote_if_requested/2` obtains `approved_hashes` from `PhysicalIphoneProofHost.evidence_input/1`, promotes with no replacement, and immediately calls `Evidence.check(path, approved_hashes)`. A passed `--run --promote --json` result therefore proves `/2` used the canonical bytes produced by that run; do not reconstruct them later or claim source-less `/1` passes.
- The configured proof host lives in `examples/phoenix_host/config/config.exs`; invoke readiness/run from `examples/phoenix_host` under `MIX_ENV=test`, where the host adapter, Phoenix endpoint, session fixture, route, media, and physical producers are loaded.
- The support vocabulary already contains `device evidence` and `verification required`; this plan changes only the physical-study row and its tests.
- Plan 162-12 supplies fresh physical value/relaunch evidence; Plan 162-13 supplies executable serialization plus real parser/join evidence. Both are required in addition to source-bound record validity.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Restore fail-closed public support truth before re-evaluation</name>
  <read_first>
    - AGENTS.md
    - .planning/phases/162-physical-iphone-adoption-proof/162-VERIFICATION.md
    - lib/crosswake/support_matrix/renderer.ex
    - test/crosswake/support_matrix/renderer_test.exs
    - guides/support_matrix.md
  </read_first>
  <files>lib/crosswake/support_matrix/renderer.ex, test/crosswake/support_matrix/renderer_test.exs, guides/support_matrix.md, .planning/phases/162-physical-iphone-adoption-proof/evidence/physical_iphone/proof-lane-evidence.json, .planning/phases/162-physical-iphone-adoption-proof/evidence/physical_iphone/.complete</files>
  <behavior>
    - "Without current repaired-behavior and serialization admission, the physical-study row is verification required."
    - "All existing narrow non-claim text and the frozen Android row remain present."
    - "The guide is regenerated from canonical renderer output and contains no private values."
  </behavior>
  <action>Write the regression first, then change only the physical-study row back to the existing `verification required` vocabulary and regenerate the guide. This is the fail-closed baseline demanded by D-04 and the verifier: prior evidence-file presence, Plan 09-11 summaries, and checked requirement boxes are insufficient after CR-01/CR-02. After renderer/guide parity passes in this withdrawn state, use `162-VERIFICATION.md` plus the exact fixed two-file path to identify the invalid pre-repair record, record its invalidation in the Plan summary without copying its bytes, then remove that directory so the later standard production command can exercise `Evidence.promote/2`'s atomic no-replace path. Do not reuse its approved digest, canonical bytes, captured time, or completion marker, and do not perform a hand-constructed source check. Preserve the one-flow/one-runtime boundary explanation as the exact promotion ceiling, every public non-claim, Android freeze, and all unrelated bytes/rows. Do not add an environment-dependent renderer branch, new status label, or identifying data.</action>
  <acceptance_criteria>
    - The physical-study row no longer says device evidence before the repaired gates are evaluated.
    - Renderer tests and generated guide agree byte-for-byte on the fail-closed row.
    - Android and every narrow support boundary remain unchanged.
    - The pre-repair record is absent before Task 2; its bytes/digest are not copied into a new location or reused as repair authority.
  </acceptance_criteria>
  <verify><automated>mix test test/crosswake/support_matrix/renderer_test.exs test/crosswake/support_matrix/support_matrix_test.exs --max-failures 1 &amp;&amp; mix run -e 'alias Crosswake.SupportMatrix; alias Crosswake.SupportMatrix.Renderer; true = File.read!("guides/support_matrix.md") == Renderer.render(SupportMatrix.canonical())' &amp;&amp; test ! -e .planning/phases/162-physical-iphone-adoption-proof/evidence/physical_iphone</automated></verify>
  <done>The public guide is fail closed and the identified pre-repair record is absent, leaving the fixed destination available for one new no-replace standard run.</done>
</task>

<task type="auto">
  <name>Task 2: Run the sole fresh closed-preflight production proof and promotion</name>
  <precondition>Plans 162-12 and 162-13 have complete summaries, the fixed evidence destination is absent after Task 1, and `(cd examples/phoenix_host &amp;&amp; MIX_ENV=test mix crosswake.proof_lane.physical_iphone --readiness --json)` exits zero with outcome `ready`, a nonempty checks array, and every check state `ready`.</precondition>
  <reversibility rating="costly">D-05/D-06 make this newly promoted no-replace record durable release truth; correction requires invalidating it and executing another fully preflighted run.</reversibility>
  <read_first>
    - AGENTS.md
    - .planning/phases/162-physical-iphone-adoption-proof/162-CONTEXT.md
    - .planning/phases/162-physical-iphone-adoption-proof/162-12-SUMMARY.md
    - .planning/phases/162-physical-iphone-adoption-proof/162-13-SUMMARY.md
    - .planning/todos/TODO-002-first-b2c-adopter-route-inputs.md
    - lib/mix/tasks/crosswake.proof_lane.physical_iphone.ex
    - lib/crosswake/proof_lane/evidence.ex
    - examples/phoenix_host/lib/crosswake_example/physical_iphone_proof_host.ex
    - examples/phoenix_host/lib/crosswake_example/local_first/physical_iphone_authority.ex
  </read_first>
  <files>.planning/phases/162-physical-iphone-adoption-proof/evidence/physical_iphone/proof-lane-evidence.json, .planning/phases/162-physical-iphone-adoption-proof/evidence/physical_iphone/.complete</files>
  <action>D-04: treat this task's automated verify as the sole production invocation. Do not invoke the run/promotion command while exploring. The command must run from `examples/phoenix_host` with `MIX_ENV=test`, so the configured `CrosswakeExample.PhysicalIphoneProofHost`, validated TODO-002 inventory row, generated lane, signed physical destination, host fixture/media, device-reachable Phoenix endpoint, replay/session/scope/gate controls, device producer, backend producer, and fixed evidence destination all participate. Require the read-only readiness command to return a nonempty all-ready check array and require the fixed destination absent before invoking `(cd examples/phoenix_host &amp;&amp; MIX_ENV=test mix crosswake.proof_lane.physical_iphone --run --promote --json)` exactly once. Accept only a passed physical_iphone result with the complete fixed owner-disjoint assertion set. This standard task must execute the Plan 12 journal-to-replay path, let `PhysicalIphoneAuthority.report/1` verify and duplicate the device-created mutation before its independent cases, obtain canonical source bytes from `PhysicalIphoneProofHost.evidence_input/1`, publish through `Evidence.promote/2` without replacement, and pass the task's internal `Evidence.check/2` using those same bytes. Capture stdout only in a mode-0700 temporary root and delete it on every exit. Do not reconstruct canonical bytes, reuse the stale digest/marker, call source-less `/1` as pass authority, or run direct XCUITest as a substitute. If any prerequisite or assertion blocks, leave Task 1's verification-required support state and absent destination unchanged.</action>
  <acceptance_criteria>
    - The standard host-owned command is invoked exactly once only after every D-04 readiness check is ready and the fixed destination is absent.
    - Its real device producer proves offline value persistence, scope-transition fencing, relaunch, authorized replay, duplicate idempotency, and empty outbox; its real Phoenix producer verifies that device-created value/effect before independent backend cases.
    - Success creates a new two-file record via no-replace promotion and the command cannot return passed unless internal source-bound Evidence.check/2 validates the canonical bytes produced by this run.
    - Failure leaves no evidence destination and cannot authorize support or durable completion.
  </acceptance_criteria>
  <verify><automated>set -euo pipefail; proof_run_root=$(mktemp -d); trap 'rm -rf -- "$proof_run_root"' EXIT; chmod 700 "$proof_run_root"; test ! -e .planning/phases/162-physical-iphone-adoption-proof/evidence/physical_iphone; (cd examples/phoenix_host &amp;&amp; MIX_ENV=test mix crosswake.proof_lane.physical_iphone --readiness --json) &gt;"$proof_run_root/readiness.json"; jq -e '.outcome == "ready" and (.checks | type == "array" and length &gt; 0) and all(.checks[]; .state == "ready")' "$proof_run_root/readiness.json" &gt;/dev/null; (cd examples/phoenix_host &amp;&amp; MIX_ENV=test mix crosswake.proof_lane.physical_iphone --run --promote --json) &gt;"$proof_run_root/result.json"; jq -e '.outcome == "passed" and .device_class == "physical_iphone" and (.assertions | type == "array" and length == 10) and all(.assertions[]; .outcome == "passed")' "$proof_run_root/result.json" &gt;/dev/null; test "$(find .planning/phases/162-physical-iphone-adoption-proof/evidence/physical_iphone -mindepth 1 -maxdepth 1 -type f | wc -l | tr -d ' ')" = "2"; test "$(wc -c &lt; .planning/phases/162-physical-iphone-adoption-proof/evidence/physical_iphone/.complete | tr -d ' ')" = "64"; jq -e '.outcome == "passed" and .device_class == "physical_iphone" and (.approved_hashes | length == 1) and .captured_at != null' .planning/phases/162-physical-iphone-adoption-proof/evidence/physical_iphone/proof-lane-evidence.json &gt;/dev/null</automated></verify>
  <done>A new repaired-tree record exists only because the standard Mix task passed closed preflight, both real producers, no-replace publication, and its source-bound Evidence.check/2.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Re-earn only the narrow public support row from the fresh standard record</name>
  <read_first>
    - AGENTS.md
    - .planning/phases/162-physical-iphone-adoption-proof/162-CONTEXT.md
    - .planning/phases/162-physical-iphone-adoption-proof/162-12-SUMMARY.md
    - .planning/phases/162-physical-iphone-adoption-proof/162-13-SUMMARY.md
    - lib/crosswake/support_matrix/renderer.ex
    - test/crosswake/support_matrix/renderer_test.exs
    - guides/support_matrix.md
    - .planning/phases/162-physical-iphone-adoption-proof/evidence/physical_iphone/proof-lane-evidence.json
  </read_first>
  <files>lib/crosswake/support_matrix/renderer.ex, test/crosswake/support_matrix/renderer_test.exs, guides/support_matrix.md</files>
  <behavior>
    - "Only the fresh successful Task 2 standard-run result allows the physical-study row to say device evidence."
    - "The row remains one first-adopter flow on the newly recorded iOS runtime line and preserves every broader non-claim."
    - "Renderer output and the public guide remain byte-identical."
  </behavior>
  <action>D-04: require Task 2's passed automated result and newly created record; do not accept the old Plan 09-11 summaries, a direct XCUITest result, the advisory serialization command, or record existence alone. D-05/D-06: inspect only the fresh allowlisted evidence fields and never reconstruct its approved source bytes. Change only the physical-study row back to existing `device evidence` wording, using the record's bounded iOS runtime line and exact one-flow ceiling, then update the regression and regenerate the guide. D-07: keep learner-facing truth free of replay/session mechanics. Preserve Android freeze and all background/generic-storage/generic-sync/multiple-island/simulator/every-iPhone non-claims. Keep the renderer deterministic and introduce no runtime evidence probe.</action>
  <acceptance_criteria>
    - Re-promotion follows Task 2's one fresh successful standard run and cannot be earned by any stale, focused, advisory, or hand-constructed substitute.
    - Success restores only the prior narrow physical-study row and no other support status or wording.
    - Renderer tests and generated guide agree byte-for-byte with every privacy/non-claim boundary intact.
  </acceptance_criteria>
  <verify><automated>mix test test/crosswake/support_matrix/renderer_test.exs test/crosswake/support_matrix/support_matrix_test.exs --max-failures 1 &amp;&amp; mix run -e 'alias Crosswake.SupportMatrix; alias Crosswake.SupportMatrix.Renderer; true = File.read!("guides/support_matrix.md") == Renderer.render(SupportMatrix.canonical())' &amp;&amp; rg -q 'device evidence' guides/support_matrix.md</automated></verify>
  <done>The public row is restored only from the newly promoted standard-run record and stays within the locked one-flow/one-runtime claim.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| repaired executable gates -> canonical renderer edit | Only fresh behavior/serialization/parser/source evidence may authorize public promotion. |
| canonical renderer -> public guide | Deterministic bytes must preserve privacy and narrow support language. |
| prior evidence/status -> current admission | Structurally valid stale evidence cannot mask a demonstrated behavior defect. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-162-71 | Spoofing | stale evidence as repaired behavior | high | mitigate | Task 1 invalidates/removes the stale record and Task 2 requires a new no-replace standard run whose internal Evidence.check/2 uses its own canonical source bytes. |
| T-162-72 | Elevation of Privilege | direct/advisory test as physical proof | high | mitigate | Task 2 accepts only the configured standard Mix task after complete D-04 readiness; focused/advisory commands cannot promote. |
| T-162-73 | Tampering | support promotion ordering | high | mitigate | Task 1 commits fail-closed truth, Task 2 creates fresh evidence, and only Task 3 restores support. |
| T-162-74 | Information Disclosure | public guide/device logs | high | mitigate | Task 2 confines stdout to a deleted mode-private root; Task 3 renders only public narrow prose. |
| T-162-75 | Repudiation | unsupported completion claim | medium | mitigate | The standard task's passed JSON, new no-replace record, internal source check, and deterministic guide equality form the admission boundary. |
| T-162-76 | Denial of Service | missing device/Xcode | low | accept | Missing prerequisites leave support at verification required; no manual approval bypass exists. |
</threat_model>

<verification>
- Observe the fail-closed intermediate guide before running promotion admission.
- Run the standard physical Mix task once after all-ready preflight; require its real device/Phoenix join, new no-replace promotion, and internal source-bound Evidence.check/2, then run renderer tests and guide parity.
- Confirm every broader platform/storage/sync/island/device non-claim remains present.
</verification>

<success_criteria>
- Public support cannot remain promoted after the demonstrated defect without fresh repair proof.
- Successful final prose remains exactly one flow on one iOS runtime line.
- Authorized Evidence.check/2 semantics and privacy boundaries are unchanged.
</success_criteria>

<output>
Create `.planning/phases/162-physical-iphone-adoption-proof/162-14-SUMMARY.md` when done.
</output>
