---
phase: 162-physical-iphone-adoption-proof
plan: "16"
type: execute
wave: 16
depends_on: [162-15]
files_modified:
  - script/retain_physical_iphone_evidence_transaction.sh
  - test/crosswake/proof_lane/physical_iphone_evidence_transaction_test.exs
  - test/mix/tasks/crosswake.proof_lane.physical_iphone_test.exs
  - examples/phoenix_host/test/crosswake_example/physical_iphone_proof_host_test.exs
  - lib/crosswake/support_matrix/renderer.ex
  - test/crosswake/support_matrix/renderer_test.exs
  - guides/support_matrix.md
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - .planning/STATE.md
  - .planning/phases/162-physical-iphone-adoption-proof/162-VALIDATION.md
  - .planning/phases/162-physical-iphone-adoption-proof/evidence/physical_iphone/proof-lane-evidence.json
  - .planning/phases/162-physical-iphone-adoption-proof/evidence/physical_iphone/.complete
autonomous: true
gap_closure: true
requirements: [DEVICE-01, DEVICE-02, DEVICE-03, DEVICE-04, DEVICE-05, DEVICE-06, DEVICE-07]
must_haves:
  truths:
    - "Per D-04 through D-06, stale physical evidence and every support/completion claim derived from it are withdrawn before a new device transaction can begin."
    - "The fresh standard run executes code descending from b79bce8b, c11886b7, and e46f5136, so nonce binding, expected-mutation binding, and all-exit cleanup are physically attested rather than inferred from deterministic tests."
    - "Per D-01/D-02, exactly one canonical host-owned transaction joins the real signed-device producer with the independent Phoenix producer for the complete ordered owner-disjoint assertion set."
    - "The new record is admitted only after internal source-bound Evidence.check/2, atomic no-replace publication, an exact two-file evidence-only commit, marker/privacy checks, and a commit_ref naming the corrected pre-ledger code commit."
    - "DEVICE/support truth returns only after every fresh deterministic and retained-transaction gate passes, and the independent Phase 162 verifier is rerun afterward."
    - "Per D-03 and D-07 through D-09, host authority, learner recovery semantics, privacy, accessibility, and all narrow non-claims remain unchanged; no Android, simulator, background, generic sync/storage, or additional island work enters the plan."
  artifacts:
    - path: "script/retain_physical_iphone_evidence_transaction.sh"
      provides: "A new one-shot corrected-provenance ledger, exact canonical run boundary, evidence-only commit, and fresh-process source-bound admission."
    - path: "test/crosswake/proof_lane/physical_iphone_evidence_transaction_test.exs"
      provides: "Deterministic isolated-Git transaction coverage proving ordering, single invocation, provenance ancestry, exact staging, and failure cleanup."
    - path: ".planning/phases/162-physical-iphone-adoption-proof/evidence/physical_iphone/proof-lane-evidence.json"
      provides: "Fresh allowlisted physical record whose commit_ref identifies corrected code."
    - path: ".planning/phases/162-physical-iphone-adoption-proof/evidence/physical_iphone/.complete"
      provides: "Exact 64-byte SHA-256 marker for the fresh record."
    - path: "lib/crosswake/support_matrix/renderer.ex"
      provides: "Fail-closed then conditionally re-earned one-flow/one-runtime support truth."
    - path: ".planning/phases/162-physical-iphone-adoption-proof/162-VALIDATION.md"
      provides: "Aggregate-only ledger distinguishing fresh physical provenance, deterministic cleanup/provenance tests, source-bound evidence, and public support parity."
  key_links:
    - from: "b79bce8b/c11886b7/e46f5136"
      to: "fresh evidence commit_ref"
      via: "transaction records a clean pre-ledger HEAD that is a descendant of all three fixes and passes it unchanged into evidence_input/1"
      pattern: "commit_ref|TRANSACTION_CODE_COMMIT"
    - from: "canonical physical command"
      to: "two-file evidence-only commit"
      via: "one-shot ledger, exact result validation, no-replace promotion, exact pathspec staging, and committed-blob equality"
      pattern: "crosswake\.proof_lane\.physical_iphone --run --promote --json"
    - from: "same-run approved_hashes"
      to: "retained record"
      via: "mode-private exact-term handoff and fresh-process Evidence.check/2 before transaction success"
      pattern: "Evidence\.check"
    - from: "fresh retained authority"
      to: "support/requirements/roadmap/state truth"
      via: "post-transaction deterministic gates followed by scoped reconciliation"
      pattern: "device evidence|Complete"
---

<objective>
Close the verifier's sole remaining stale-provenance gap with one corrected-code physical transaction and no scope expansion.

Purpose: Replace the pre-fix physical authority with a record that actually executes the nonce, mutation-binding, and all-exit cleanup fixes while preserving fail-closed support, privacy, and one-shot device semantics.
Output: Deterministically proven transaction controls, one fresh corrected-provenance two-file record, conditionally restored narrow truth, and an independent-verifier handoff.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/ADR-FIRST-B2C-ADOPTER.md
@.planning/FIRST-B2C-ADOPTER-ADOPTION-BRIEF.md
@.planning/FIRST-B2C-ADOPTER-ROUTE-POLICY-MAP.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/162-physical-iphone-adoption-proof/162-CONTEXT.md
@.planning/phases/162-physical-iphone-adoption-proof/162-VERIFICATION.md
@.planning/phases/162-physical-iphone-adoption-proof/162-REVIEW.md
@.planning/phases/162-physical-iphone-adoption-proof/162-REVIEW-FIX.md
@.planning/phases/162-physical-iphone-adoption-proof/162-14-PLAN.md
@.planning/phases/162-physical-iphone-adoption-proof/162-14-SUMMARY.md
@.planning/phases/162-physical-iphone-adoption-proof/162-15-PLAN.md
@.planning/phases/162-physical-iphone-adoption-proof/162-15-SUMMARY.md
@script/retain_physical_iphone_evidence_transaction.sh
@test/crosswake/proof_lane/physical_iphone_evidence_transaction_test.exs
@lib/mix/tasks/crosswake.proof_lane.physical_iphone.ex
@examples/phoenix_host/lib/crosswake_example/physical_iphone_proof_host.ex
@lib/crosswake/proof_lane/evidence.ex
@lib/crosswake/proof_lane/native_promotion.ex
@lib/crosswake/support_matrix/renderer.ex

<interfaces>
- `Mix.Tasks.Crosswake.ProofLane.PhysicalIphone.invoke_runner/2` now requires `cleanup_run/0` on every device/backend/join/promotion exit and returns only `PI-HOST-CLEANUP` when cleanup cannot confirm success.
- `PhysicalIphoneProofHost` binds the opaque proof nonce and expected mutation ID through the signed-device environment, Phoenix replay admission, persisted-row authority, backend report, and idempotent cleanup without retaining either value.
- `Evidence.promote/2` performs canonical/source validation, privacy scanning, atomic no-replace publication, and source-bound `Evidence.check/2`; source-less `/1` remains intentionally non-authoritative for approved-hash physical records.
- The existing Plan 14 ledger and evidence commits remain immutable history. Plan 16 uses a distinct stable retry subject and may retire only the two currently tracked stale evidence leaves before the new no-replace transaction.
- The user's local pbxproj, `.xcworkspace`, and `.planning/debug/` changes are outside plan ownership and must remain untouched, unstaged, and uncleaned.
</interfaces>
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Fail closed and prove the corrected-provenance transaction without device execution</name>
  <reversibility rating="costly">D-04 through D-06 make the one-shot ledger and corrected code provenance durable proof authority; changing them later requires another explicitly authorized physical transaction.</reversibility>
  <read_first>
    - .planning/phases/162-physical-iphone-adoption-proof/162-VERIFICATION.md
    - .planning/phases/162-physical-iphone-adoption-proof/162-REVIEW-FIX.md
    - .planning/phases/162-physical-iphone-adoption-proof/162-14-PLAN.md
    - script/retain_physical_iphone_evidence_transaction.sh
    - test/crosswake/proof_lane/physical_iphone_evidence_transaction_test.exs
    - lib/mix/tasks/crosswake.proof_lane.physical_iphone.ex
    - test/mix/tasks/crosswake.proof_lane.physical_iphone_test.exs
    - examples/phoenix_host/lib/crosswake_example/physical_iphone_proof_host.ex
    - examples/phoenix_host/test/crosswake_example/physical_iphone_proof_host_test.exs
    - lib/crosswake/support_matrix/renderer.ex
    - .planning/REQUIREMENTS.md
    - .planning/ROADMAP.md
    - .planning/STATE.md
    - .planning/phases/162-physical-iphone-adoption-proof/162-VALIDATION.md
  </read_first>
  <files>script/retain_physical_iphone_evidence_transaction.sh, test/crosswake/proof_lane/physical_iphone_evidence_transaction_test.exs, test/mix/tasks/crosswake.proof_lane.physical_iphone_test.exs, examples/phoenix_host/test/crosswake_example/physical_iphone_proof_host_test.exs, lib/crosswake/support_matrix/renderer.ex, test/crosswake/support_matrix/renderer_test.exs, guides/support_matrix.md, .planning/REQUIREMENTS.md, .planning/ROADMAP.md, .planning/STATE.md, .planning/phases/162-physical-iphone-adoption-proof/162-VALIDATION.md, .planning/phases/162-physical-iphone-adoption-proof/evidence/physical_iphone/proof-lane-evidence.json, .planning/phases/162-physical-iphone-adoption-proof/evidence/physical_iphone/.complete</files>
  <behavior>
    - "RED: the transaction refuses to reuse Plan 14's ledger or stale record and lacks a distinct Plan 16 one-shot provenance contract."
    - "GREEN: isolated temporary-Git tests prove a distinct ledger, corrected-code ancestry, ledger-before-run ordering, exactly one injected run, exact owner-disjoint passed assertions, exact two-file staging/commit, committed-blob equality, fresh-process source-bound refusal propagation, and cleanup of every private capture leaf."
    - "Existing focused regressions prove the nonce/expected-mutation binding and required cleanup after malformed device bytes, malformed backend bytes, join refusal, promotion refusal, successful completion, and cleanup callback failure."
    - "No deterministic test invokes Xcode, xcodebuild, XCTest/XCUITest, a simulator, a physical destination, the canonical production command, or the fixed production evidence destination."
    - "Before the physical transaction, the public row says verification required, DEVICE checkboxes/traceability and Phase 162 status say gaps found, and the stale evidence pair is absent."
  </behavior>
  <action>Per D-04 through D-06, first make stale authority impossible. Change only the physical-study renderer row/test/guide to `verification required`; uncheck DEVICE-01 through DEVICE-07 and keep all seven traceability rows `Gaps Found`; mark Phase 162/Plan 16 as gap closure in ROADMAP and STATE; supersede the old physical row in `162-VALIDATION.md`. Retire the stale record with exact Git pathspec removal of only `proof-lane-evidence.json` and `.complete`, preserving recovery from commit `5dd24b6c`; do not use recursive deletion, broad cleanup, glob deletion, reset, checkout, or any command touching user-local Xcode/workspace/debug paths.

Then update the durable transaction for a distinct Plan 16 attempt subject. Build an actual isolated-temporary-repository contract suite rather than a source-text assertion: SAFE executable-path injection remains available only under the existing explicit isolated-fixture guard, while production hard-selects the literal readiness, run, and fresh verifier boundaries. Prove pre-ledger refusal for non-ready output, stale destination, dirty tracked non-evidence paths, malformed results, and missing corrected-code ancestry; post-ledger refusal must consume the attempt without a second run. Prove the ledger precedes exactly one run, the run receives the clean pre-ledger full code commit, the record names that commit rather than either ledger, the evidence-only commit changes exactly the two leaves and has the ledger as parent, committed bytes equal working bytes, strict safe term decoding feeds only same-run canonical sources to Evidence.check/2, and every capture root is removed. Preserve the canonical command as one literal production boundary and never place it behind shell fragments or eval.

Extend focused root and Phoenix tests only where needed to lock the b79bce8b/c11886b7/e46f5136 behavior: an active ticket and expected mutation pair are required, stale/wrong pairs reject, and cleanup is called exactly once on every runner exit and remains idempotent. Do not change production provenance/cleanup code unless a RED regression proves a current defect. Run and commit all deterministic RED/GREEN work plus fail-closed truth withdrawal before any readiness or device command. Assert the resulting clean code commit is a descendant of all three fix commits; this becomes the only commit eligible for the fresh record's commit_ref.</action>
  <acceptance_criteria>
    - Stale support, requirement, roadmap/state, and validation authority are withdrawn before any physical execution.
    - Only the two stale evidence leaves are retired, through exact Git pathspecs; all user-local Xcode/workspace/debug changes remain present and unstaged.
    - The deterministic transaction suite executes real script behavior in isolated Git fixtures and proves the full one-shot/ancestry/commit/source-cleanup contract without native tools or the production destination.
    - Focused current-code tests prove provenance binding and all-exit cleanup under success and every closed failure class.
    - The committed pre-run HEAD is a descendant of b79bce8b, c11886b7, and e46f5136 and contains every Plan 16 GREEN transaction/test change.
  </acceptance_criteria>
  <verify><automated>set -euo pipefail; mix test test/crosswake/proof_lane/physical_iphone_evidence_transaction_test.exs test/mix/tasks/crosswake.proof_lane.physical_iphone_test.exs --max-failures 1; (cd examples/phoenix_host &amp;&amp; MIX_ENV=test mix test test/crosswake_example/physical_iphone_proof_host_test.exs test/crosswake_example/local_first/physical_iphone_authority_test.exs --max-failures 1); mix test test/crosswake/support_matrix/renderer_test.exs test/crosswake/support_matrix/support_matrix_test.exs --max-failures 1; git merge-base --is-ancestor b79bce8b HEAD; git merge-base --is-ancestor c11886b7 HEAD; git merge-base --is-ancestor e46f5136 HEAD; rg -q '\| physical-iPhone offline study \| verification required \|' guides/support_matrix.md; test "$(rg -c '^- \[ \] \*\*DEVICE-0[1-7]:' .planning/REQUIREMENTS.md)" = "7"; test "$(rg -c '^\| DEVICE-0[1-7] \| Phase 162 \| Gaps Found \|' .planning/REQUIREMENTS.md)" = "7"; test ! -e .planning/phases/162-physical-iphone-adoption-proof/evidence/physical_iphone; test -n "$(git status --short -- examples/phoenix_host/native/ios/CrosswakeProofLane.xcodeproj/project.pbxproj examples/phoenix_host/native/ios/CrosswakeProofLane.xcodeproj/project.xcworkspace .planning/debug)"</automated></verify>
  <done>The repository is safely fail closed and a committed corrected-provenance transaction is deterministically ready without spending the single physical run.</done>
</task>

<task type="auto">
  <name>Task 2: Execute exactly one fresh standard corrected-code transaction</name>
  <precondition>The physical iPhone remains connected, trusted, unlocked, Local Network-enabled, and Auto-Lock-disabled; the Personal Team remains configured only in the user-local pbxproj; the fixed evidence destination is absent; and Task 1's clean committed HEAD descends from b79bce8b, c11886b7, and e46f5136.</precondition>
  <reversibility rating="costly">The new evidence-only commit becomes durable device authority; invalidation requires explicit fail-closed withdrawal and a separately authorized future run.</reversibility>
  <read_first>
    - .planning/phases/162-physical-iphone-adoption-proof/162-16-PLAN.md
    - script/retain_physical_iphone_evidence_transaction.sh
    - examples/phoenix_host/lib/crosswake_example/physical_iphone_proof_host.ex
    - lib/mix/tasks/crosswake.proof_lane.physical_iphone.ex
    - lib/crosswake/proof_lane/evidence.ex
  </read_first>
  <files>.planning/phases/162-physical-iphone-adoption-proof/evidence/physical_iphone/proof-lane-evidence.json, .planning/phases/162-physical-iphone-adoption-proof/evidence/physical_iphone/.complete</files>
  <action>Execute `script/retain_physical_iphone_evidence_transaction.sh` exactly once. Its production path may perform the read-only all-ready readiness check, create the distinct Plan 16 empty one-shot ledger, and then invoke exactly once the literal `(cd examples/phoenix_host &amp;&amp; MIX_ENV=test mix crosswake.proof_lane.physical_iphone --run --promote --json)`. This is the only Xcode, xcodebuild, XCTest/XCUITest, signed-device, Phoenix-producer, promotion, or physical-run authority permitted anywhere in the plan. Do not invoke the canonical command directly in addition to the transaction; do not run advisory simulator or serialization scripts; do not retry after any post-ledger failure.

Require the real signed-device and independent Phoenix reports to join the full ordered owner-disjoint passed set under the active nonce/expected-mutation binding, with required cleanup completing on exit. Require atomic no-replace publication, exact two-file shape, same-run internal Evidence.check/2, a commit_ref equal to Task 1's clean corrected code commit, a distinct ledger commit, and a subsequent evidence-only commit whose parent is that ledger and whose changed paths are exactly the artifact and marker. The transaction must remove its mode-private stdout/source root on every exit and emit only a closed result. If any gate fails, keep support and completion truth withdrawn; the consumed ledger forbids a retry.</action>
  <acceptance_criteria>
    - Exactly one fresh standard host-owned physical command runs, and no simulator/advisory/native command runs before or after it.
    - All readiness checks are ready; real signed-device and Phoenix producers supply their own disjoint assertion subsets; all assertions pass.
    - The record's commit_ref resolves to Task 1's clean committed HEAD and that commit is a descendant of all three provenance/cleanup fixes.
    - The fresh two-file record is atomically no-replace published, internally source-bound checked, and committed in an evidence-only commit directly after the distinct Plan 16 ledger.
    - Any failure leaves verification-required/gaps-found truth intact and cannot trigger another physical attempt.
  </acceptance_criteria>
  <verify><automated>set -euo pipefail; evidence_dir=.planning/phases/162-physical-iphone-adoption-proof/evidence/physical_iphone; test "$(find "$evidence_dir" -mindepth 1 -maxdepth 1 -type f | wc -l | tr -d ' ')" = "2"; test "$(wc -c &lt; "$evidence_dir/.complete" | tr -d ' ')" = "64"; code_ref=$(jq -er '.commit_ref | sub("^git-"; "")' "$evidence_dir/proof-lane-evidence.json"); git cat-file -e "$code_ref^{commit}"; git merge-base --is-ancestor b79bce8b "$code_ref"; git merge-base --is-ancestor c11886b7 "$code_ref"; git merge-base --is-ancestor e46f5136 "$code_ref"; evidence_commit=$(git log -1 --format=%H -- "$evidence_dir/proof-lane-evidence.json"); test "$(git show --format= --name-only "$evidence_commit" | sed '/^$/d' | sort | tr '\n' ' ')" = ".planning/phases/162-physical-iphone-adoption-proof/evidence/physical_iphone/.complete .planning/phases/162-physical-iphone-adoption-proof/evidence/physical_iphone/proof-lane-evidence.json "; ledger_commit=$(git rev-parse "$evidence_commit^"); test "$(git show -s --format=%s "$ledger_commit")" = "chore(162-16): consume corrected-provenance run"; test "$(git show "$evidence_commit:$evidence_dir/proof-lane-evidence.json" | shasum -a 256 | cut -d' ' -f1)" = "$(shasum -a 256 "$evidence_dir/proof-lane-evidence.json" | cut -d' ' -f1)"; test "$(tr -d '\r\n' &lt; "$evidence_dir/.complete")" = "$(shasum -a 256 "$evidence_dir/proof-lane-evidence.json" | cut -d' ' -f1)"</automated></verify>
  <done>One and only one corrected-code physical transaction produced a committed, provenance-pinned, privacy-safe record; no second native/device run occurred.</done>
</task>

<task type="auto">
  <name>Task 3: Reconcile narrow truth and hand off to independent verification</name>
  <precondition>Task 2's distinct ledger and evidence-only commit exist; the evidence record pins corrected code descending from b79bce8b/c11886b7/e46f5136; the exact two-file marker/commit/privacy checks pass; and the transaction recorded successful same-run internal Evidence.check/2.</precondition>
  <reversibility rating="costly">Public support and milestone completion are durable claims; a failed independent verifier requires another explicit fail-closed withdrawal.</reversibility>
  <read_first>
    - .planning/phases/162-physical-iphone-adoption-proof/162-VERIFICATION.md
    - .planning/phases/162-physical-iphone-adoption-proof/evidence/physical_iphone/proof-lane-evidence.json
    - lib/crosswake/support_matrix/renderer.ex
    - test/crosswake/support_matrix/renderer_test.exs
    - guides/support_matrix.md
    - .planning/REQUIREMENTS.md
    - .planning/ROADMAP.md
    - .planning/STATE.md
    - .planning/phases/162-physical-iphone-adoption-proof/162-VALIDATION.md
  </read_first>
  <files>lib/crosswake/support_matrix/renderer.ex, test/crosswake/support_matrix/renderer_test.exs, guides/support_matrix.md, .planning/REQUIREMENTS.md, .planning/ROADMAP.md, .planning/STATE.md, .planning/phases/162-physical-iphone-adoption-proof/162-VALIDATION.md</files>
  <action>Do not execute readiness, the transaction helper, Xcode/xcodebuild, XCTest/XCUITest, the canonical command, promotion, a physical-device command, or any simulator/advisory path. Re-run only deterministic current-tree gates for the transaction, template/evidence, production parser/join, provenance/cleanup, Phoenix authority/recovery, renderer, and guide parity. Validate the retained artifact through its exact two-file/marker/commit/privacy invariants and the transaction's recorded internal source-bound result; do not reconstruct canonical bytes and do not represent Evidence.check/1 as passing.

If and only if every gate passes, restore only the existing narrow physical-study row to `device evidence`, mark DEVICE-01 through DEVICE-07 complete in definitions and traceability, mark Plan 16 and Phase 162 internally complete/pending independent verification, and replace `162-VALIDATION.md` with aggregate-only fresh results. Record the corrected code commit, distinct ledger, and evidence-only commit as commit hashes only; retain no raw answer, nonce, mutation, scope, account, endpoint, signing-team, device identifier, private path, log, media, transcript, credential, token, stdout, or canonical source bytes. Preserve TODO-002, Android freeze, all stopped/deferred work, and explicit background/generic-storage/generic-sync/multiple-island/simulator/every-iPhone non-claims.

Leave `162-VERIFICATION.md` untouched during execution. After the plan summary is committed, the orchestrator must dispatch a fresh independent Phase 162 verifier. That verifier must evaluate the new corrected-provenance record and current truth; if it does not return `passed`, immediately treat its gap report as authority and re-withdraw support/completion truth rather than self-certifying or rerunning the device.</action>
  <acceptance_criteria>
    - Deterministic provenance/cleanup, transaction, parser/join, Phoenix recovery/authority, renderer, and guide gates pass without any native/device/simulator execution.
    - Support and DEVICE truth are restored only from the fresh corrected-provenance transaction and remain one first-adopter flow on one recorded iOS runtime line.
    - REQUIREMENTS, ROADMAP, STATE, VALIDATION, renderer, and generated guide agree without widening any claim or retaining private data.
    - The independent verifier is the final post-plan authority; a non-passing verdict triggers fail-closed withdrawal and never an automatic second physical run.
  </acceptance_criteria>
  <verify><automated>set -euo pipefail; mix test test/crosswake/proof_lane/physical_iphone_evidence_transaction_test.exs test/crosswake/proof_lane/template_contract_test.exs test/crosswake/proof_lane/evidence_test.exs test/mix/tasks/crosswake.proof_lane.physical_iphone_test.exs test/crosswake/support_matrix/renderer_test.exs test/crosswake/support_matrix/support_matrix_test.exs --max-failures 1; (cd examples/phoenix_host &amp;&amp; MIX_ENV=test mix test test/crosswake_example/physical_iphone_proof_host_test.exs test/crosswake_example/local_first/physical_iphone_authority_test.exs test/crosswake_example/router_test.exs --max-failures 1 &amp;&amp; npm test -- --project=chromium e2e/offline_sync.spec.ts --grep "study status retains rejected work"); mix run -e 'alias Crosswake.SupportMatrix; alias Crosswake.SupportMatrix.Renderer; true = File.read!("guides/support_matrix.md") == Renderer.render(SupportMatrix.canonical())'; test "$(rg -c '^- \[x\] \*\*DEVICE-0[1-7]:' .planning/REQUIREMENTS.md)" = "7"; test "$(rg -c '^\| DEVICE-0[1-7] \| Phase 162 \| Complete \|' .planning/REQUIREMENTS.md)" = "7"; rg -q '\| physical-iPhone offline study \| device evidence \|' guides/support_matrix.md</automated></verify>
  <done>The corrected physical authority is internally reconciled and queued for a fresh independent verifier; no further device authority exists.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| stale retained record -> support/completion truth | Pre-fix evidence must lose authority before any new transaction. |
| deterministic fixture -> production transaction | Fake executables and synthetic bytes must remain isolated and incapable of reaching the fixed destination or public support. |
| corrected code commit -> ledger -> physical run | Evidence must identify the exact pre-ledger code containing all provenance/cleanup fixes. |
| signed device + Phoenix producers -> joined candidate | Neither producer may satisfy the other's assertion ownership. |
| same-run canonical sources -> retained evidence | Only internal source-bound Evidence.check/2 may admit the approved-hash record. |
| working evidence -> Git history -> public truth | Exact no-replace files, evidence-only commit structure, and privacy checks gate support restoration. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-162-87 | Spoofing | stale pre-fix evidence | high | mitigate | Task 1 withdraws all dependent truth and retires only the exact tracked pair before any run. |
| T-162-88 | Tampering | code provenance | high | mitigate | Tasks 1-2 require the evidence commit_ref to descend from b79bce8b, c11886b7, and e46f5136 and to differ from both ledgers. |
| T-162-89 | Elevation of Privilege | producer ownership | high | mitigate | Task 2 uses only the real signed-device and independent Phoenix callback slots and requires the complete ordered owner-disjoint manifest. |
| T-162-90 | Information Disclosure | private run/source material | high | mitigate | Mode-private capture, strict allowlist, internal Evidence.check/2, aggregate-only durable prose, and unconditional cleanup prevent egress. |
| T-162-91 | Tampering | evidence publication/commit | high | mitigate | Atomic no-replace promotion, exact two-file marker checks, explicit staging, evidence-only commit parentage, and committed-blob equality gate success. |
| T-162-92 | Repudiation | repeated device attempts | high | mitigate | A distinct durable ledger precedes the single canonical run; post-ledger failure is terminal and every later task forbids native/device commands. |
| T-162-93 | Spoofing | simulator/advisory substitution | high | mitigate | All tasks explicitly prohibit simulator and advisory paths; only the canonical physical transaction can restore truth. |
| T-162-94 | Tampering | user-local setup | high | mitigate | Exact transaction exclusions and status assertions preserve unstaged pbxproj/workspace/debug changes. |
| T-162-95 | Repudiation | self-certified completion | medium | mitigate | Task 3 leaves VERIFICATION unchanged and requires a fresh independent verifier after plan execution. |
</threat_model>

<source_coverage_audit>
| SOURCE | ID | Feature/Requirement | Plan | Status | Notes |
|---|---|---|---|---|---|
| GOAL | — | Current-provenance ten-step physical-iPhone proof with narrow support truth | 162-16 | COVERED | One corrected-code physical transaction replaces stale authority. |
| REQ | DEVICE-01 | Verified pack and offline audio | 162-16 Task 2 | COVERED | Real signed-device producer only. |
| REQ | DEVICE-02 | Selected/free-form persistence, relaunch, exactly-once drain | 162-16 Tasks 1-2 | COVERED | Corrected nonce/mutation binding is tested then physically executed. |
| REQ | DEVICE-03 | Visible retained rejection/conflict | 162-16 Tasks 2-3 | COVERED | Phoenix producer plus deterministic recovery rerun. |
| REQ | DEVICE-04 | Logout/account-switch fencing | 162-16 Tasks 2-3 | COVERED | Owner-disjoint physical/backend authority plus deterministic scope checks. |
| REQ | DEVICE-05 | Entry/replay disablement with retained work | 162-16 Tasks 2-3 | COVERED | Independent Phoenix authority remains required. |
| REQ | DEVICE-06 | Dated redacted source-bound record | 162-16 Tasks 1-2 | COVERED | Corrected commit_ref, same-run Evidence.check/2, exact two-file evidence-only commit. |
| REQ | DEVICE-07 | One-flow/one-runtime claim | 162-16 Tasks 1-3 | COVERED | Withdrawn first; restored only after all gates. |
| RESEARCH | Sequential dual authority | One host-owned run with device/Phoenix ownership split | 162-16 Task 2 | COVERED | No second run or advisory producer. |
| RESEARCH | Closed preflight/evidence | All-ready, canonical, atomic no-replace, source-bound, privacy-safe | 162-16 Tasks 1-2 | COVERED | Deterministic transaction proof precedes execution. |
| RESEARCH | Support boundary | Physical evidence only, explicit non-claims | 162-16 Tasks 1-3 | COVERED | Renderer remains narrow and fail closed. |
| CONTEXT | D-01 | Sequential uninterrupted host-owned exit test | 162-16 Task 2 | COVERED | Exact canonical command once. |
| CONTEXT | D-02 | Device-local versus Phoenix authority | 162-16 Tasks 1-2 | COVERED | Ownership cannot cross report slots. |
| CONTEXT | D-03 | Host-owned domain and storage boundary | 162-16 Tasks 1-3 | COVERED | No new reusable sync/storage API. |
| CONTEXT | D-04 | All-ready fail-closed preflight | 162-16 Tasks 1-2 | COVERED | Stale truth withdrawn before transaction. |
| CONTEXT | D-05 | Fixed physical vocabulary and no-replace promotion | 162-16 Tasks 1-2 | COVERED | Exact manifest, runtime, and record shape retained. |
| CONTEXT | D-06 | Strict evidence privacy | 162-16 Tasks 1-3 | COVERED | No sensitive or private run material is retained. |
| CONTEXT | D-07-D-09 | Honest accessible recovery surface | 162-16 Tasks 2-3 | COVERED | Existing behavior is rerun without UI breadth. |
</source_coverage_audit>

<verification>
- Task 1 uses deterministic non-native tests only and commits a fail-closed clean corrected-code HEAD.
- Task 2 executes the transaction helper exactly once; only that helper may invoke the canonical physical command.
- Task 2 verification is read-only and cannot rerun the transaction or any native tool.
- Task 3 reruns deterministic gates only, restores narrow truth conditionally, and hands the unchanged verifier artifact to a fresh independent verifier.
</verification>

<success_criteria>
- The stale `fcac1360...` record no longer authorizes support or completion.
- One new record pins code descending from b79bce8b/c11886b7/e46f5136 and is retained by an exact evidence-only commit after a distinct Plan 16 ledger.
- The canonical physical command runs exactly once; no extra Xcode/device/simulator/advisory execution occurs.
- Exact two-file, marker, privacy, owner-disjoint, cleanup, source-bound, and one-shot ledger gates all pass.
- Only the narrow DEVICE/support truth returns, and a fresh independent verifier makes the final verdict.
- User-local Xcode signing, workspace, and `.planning/debug/` changes remain untouched and unstaged.
</success_criteria>

<output>
Create `.planning/phases/162-physical-iphone-adoption-proof/162-16-SUMMARY.md` when done. Then dispatch the independent Phase 162 verifier; do not authorize another physical run if it reports a gap.
</output>
