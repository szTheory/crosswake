---
phase: 162-physical-iphone-adoption-proof
plan: "10"
type: execute
wave: 10
depends_on: [162-09]
files_modified:
  - lib/crosswake/support_matrix/renderer.ex
  - test/crosswake/support_matrix/renderer_test.exs
  - guides/support_matrix.md
autonomous: true
gap_closure: true
requirements: [DEVICE-07]
must_haves:
  truths:
    - "A public physical-iPhone support row changes only after the fixed evidence directory passes `Evidence.check/1`; before that evidence-gated edit, `verification required` remains unchanged."
    - "Per D-05/D-06 and DEVICE-07, the promoted wording uses existing support vocabulary and says only one first-adopter flow on the recorded iOS runtime line, with explicit Android, background, generic storage/sync, multiple-island, simulator, and every-iPhone non-claims."
    - "Canonical renderer output, renderer tests, and checked-in `guides/support_matrix.md` remain byte-consistent."
  artifacts:
    - path: "lib/crosswake/support_matrix/renderer.ex"
      provides: "Evidence-gated narrow physical-iPhone support wording using existing support vocabulary."
    - path: "test/crosswake/support_matrix/renderer_test.exs"
      provides: "Positive evidence-backed claim and fail-closed non-claim regression coverage."
    - path: "guides/support_matrix.md"
      provides: "Generated public first-adopter support truth after the physical record passes."
  key_links:
    - from: ".planning/phases/162-physical-iphone-adoption-proof/evidence/physical_iphone"
      to: "Crosswake.SupportMatrix.Renderer first-adopter readiness section"
      via: "the task precondition and verification require successful `Evidence.check/1` before the canonical row is edited"
      pattern: "physical-iPhone offline study|device evidence|verification required"
    - from: "Crosswake.SupportMatrix.Renderer.render/1"
      to: "guides/support_matrix.md"
      via: "deterministic generated guide write"
      pattern: "First Adopter Readiness"
---

<objective>
Derive the narrow public support truth from the checked physical evidence and nothing else.

Purpose: Make DEVICE-07 observable without widening one device run into a platform, Android, storage, or synchronization claim.
Output: Evidence-gated canonical renderer contract, regression tests, and regenerated support guide.
</objective>

<artifacts_this_phase_produces>
- One evidence-backed `physical-iPhone offline study` row using the existing support vocabulary.
- Explicit one-flow/one-runtime boundaries and Android/background/generic-storage/generic-sync/multiple-island/simulator/every-iPhone non-claims.
- No new support label family, capability taxonomy, Android status, or release-wide iOS claim.
</artifacts_this_phase_produces>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/162-physical-iphone-adoption-proof/162-CONTEXT.md
@.planning/phases/162-physical-iphone-adoption-proof/162-VERIFICATION.md
@.planning/phases/162-physical-iphone-adoption-proof/162-09-SUMMARY.md
@lib/crosswake/proof_lane/evidence.ex
@lib/crosswake/support_matrix/renderer.ex
@test/crosswake/support_matrix/renderer_test.exs
@guides/support_matrix.md

<interfaces>
- `Crosswake.ProofLane.Evidence.check/1` is the task admission gate; the renderer remains deterministic canonical support prose rather than an environment-dependent runtime probe.
- `Crosswake.SupportMatrix.Renderer.render/1` is the canonical source for `guides/support_matrix.md`.
- Existing public support vocabulary includes `device evidence` and `verification required`; this plan adds no label.
</interfaces>
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Render one evidence-backed narrow physical-iPhone support row</name>
  <precondition>`Crosswake.ProofLane.Evidence.check/1` returns `:ok` for `.planning/phases/162-physical-iphone-adoption-proof/evidence/physical_iphone`, and Plan 162-09's summary records a passed production physical run.</precondition>
  <reversibility rating="costly">The generated guide is public support truth; widening or retracting it requires coordinated canonical-renderer, test, guide, and requirement-history changes.</reversibility>
  <read_first>
    - AGENTS.md
    - .planning/phases/162-physical-iphone-adoption-proof/162-CONTEXT.md
    - .planning/phases/162-physical-iphone-adoption-proof/162-09-SUMMARY.md
    - lib/crosswake/proof_lane/evidence.ex
    - lib/crosswake/support_matrix/renderer.ex
    - test/crosswake/support_matrix/renderer_test.exs
    - guides/support_matrix.md
  </read_first>
  <files>lib/crosswake/support_matrix/renderer.ex, test/crosswake/support_matrix/renderer_test.exs, guides/support_matrix.md</files>
  <behavior>
    - "RED: before the evidence-gated renderer edit, the physical-iPhone row remains verification required and tests reject an unearned device-evidence phrase."
    - "After the checked physical record exists, only that row changes to the existing device evidence vocabulary and identifies one first-adopter flow plus its low-cardinality iOS runtime line."
    - "The boundary explicitly rejects Android, background replay/sync, generic storage/sync, multiple offline islands, simulator substitution, and every-iPhone coverage."
    - "Generated public prose uses first adopter and contains no durable codename, private host fact, route input, identifier, payload, media, credential, or device value."
  </behavior>
  <action>Write the renderer regression first, after the evidence precondition passes. Update only the canonical physical-study row; do not make the renderer depend on ambient filesystem state and do not infer promotion from package versions, Phase 163 completion, a completion summary, simulator/advisory output, or file existence alone. The plan's precondition plus repeated `Evidence.check/1` in verification are the promotion gate. Use the existing `device evidence` vocabulary and render only the low-cardinality runtime line admitted by D-05/D-06. State that the proof covers one first-adopter offline-study flow, and preserve explicit non-claims for Android, background behavior, generic storage/sync, multiple islands, simulator substitution, and all-device coverage. Do not add a new status or label family. Regenerate `guides/support_matrix.md` through `Renderer.write/2` from `SupportMatrix.canonical()` and keep all unrelated rows byte-equivalent.</action>
  <acceptance_criteria>
    - The canonical edit is admitted only after the fixed record passes `Evidence.check/1`, and the physical row then uses the existing `device evidence` vocabulary.
    - The promoted row names only one first-adopter flow and the bounded recorded iOS runtime line.
    - All required non-claims remain explicit, Android posture is unchanged, and no new support vocabulary is introduced.
    - Renderer output and checked-in `guides/support_matrix.md` are identical and contain no private or identifying values.
  </acceptance_criteria>
  <verify><automated>mix run -e 'alias Crosswake.ProofLane.Evidence; :ok = Evidence.check(".planning/phases/162-physical-iphone-adoption-proof/evidence/physical_iphone")' &amp;&amp; mix test test/crosswake/support_matrix/renderer_test.exs test/crosswake/support_matrix/support_matrix_test.exs --max-failures 1 &amp;&amp; mix run -e 'alias Crosswake.SupportMatrix; alias Crosswake.SupportMatrix.Renderer; expected = Renderer.render(SupportMatrix.canonical()); true = File.read!("guides/support_matrix.md") == expected'</automated></verify>
  <done>The public guide reports only the device evidence actually earned by the checked record, while every broader support boundary remains explicit.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| retained physical record -> canonical support renderer | A narrow record must not be accepted by existence alone or generalized beyond its device/runtime/flow scope. |
| canonical renderer -> public guide | Generated prose must preserve privacy vocabulary and remain byte-consistent with canonical truth. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-162-51 | Spoofing | evidence-backed support state | high | mitigate | Task 1 requires `Evidence.check/1` as both precondition and automated verification before accepting the canonical support edit. |
| T-162-52 | Elevation of Privilege | support-scope widening | high | mitigate | Task 1 fixes one-flow/one-runtime wording and explicit platform/storage/sync/device non-claims in renderer tests. |
| T-162-53 | Information Disclosure | public support prose | high | mitigate | Task 1 permits only public first-adopter wording and the already bounded runtime line; private host/device/route values never render. |
| T-162-54 | Tampering | renderer/guide drift | medium | mitigate | Task 1 regenerates the guide from canonical truth and requires a clean guide diff after deterministic generation. |
</threat_model>

<source_coverage_audit>
| SOURCE | ID | Feature/Requirement | Plan | Status | Notes |
|---|---|---|---|---|---|
| GOAL | — | Evidence-backed completion without a broader claim | 162-10 | COVERED | Public truth follows only from Plan 09's checked artifact. |
| REQ | DEVICE-07 | One adopter flow, one iOS runtime line, explicit exclusions | 162-10 | COVERED | Existing device-evidence vocabulary is used. |
| RESEARCH | Support boundary | Promotion derives from a fresh physical record, not advisory evidence | 162-10 | COVERED | Evidence check gates the canonical renderer edit without adding ambient filesystem behavior. |
| CONTEXT | D-05 | Physical device/runtime evidence class | 162-10 | COVERED | Renderer consumes only the checked physical class and bounded runtime. |
| CONTEXT | D-06 | Privacy-safe retained/public fields | 162-10 | COVERED | Public prose cannot render private proof inputs. |
| CONTEXT | D-01-D-04, D-07-D-09 | Physical behavior and UX contracts | 162-09 | COVERED | Implemented and proven upstream; this plan does not duplicate or weaken them. |
</source_coverage_audit>

<verification>
- Verify the fixed evidence destination before rendering.
- Run renderer and canonical support-matrix tests.
- Regenerate `guides/support_matrix.md` from canonical code and require no subsequent diff.
- Re-run the repository privacy scanner used by the final phase gate before committing public prose.
</verification>

<success_criteria>
- DEVICE-07 is expressed with existing vocabulary and exact one-flow/one-runtime scope.
- No support claim changes before Plan 09 evidence passes.
- The public guide remains generated, privacy-safe, and explicit about every locked non-claim.
</success_criteria>

<output>
Create `.planning/phases/162-physical-iphone-adoption-proof/162-10-SUMMARY.md` when done.
</output>
