---
phase: 162-physical-iphone-adoption-proof
plan: "04"
type: execute
wave: 4
depends_on: [162-03]
files_modified:
  - examples/phoenix_host/lib/crosswake_example_web/controllers/offline_html/index.html.heex
  - examples/phoenix_host/priv/static/offline_study.js
  - examples/phoenix_host/e2e/offline_sync.spec.ts
  - priv/templates/crosswake/proof_lane/ios/CrosswakeProofLaneUITests/ProofLaneUITests.swift.eex
autonomous: true
requirements: [DEVICE-03, DEVICE-04, DEVICE-05]
must_haves:
  truths:
    - "Per D-07/D-09, one combined icon + short label + learner sentence status row sits below the study heading and above answer controls, using exactly saved_locally, syncing, needs_attention, and sync_paused."
    - "Absent or failed required-pack state remains unavailable; the study status row never implies offline audio availability."
    - "A labeled indeterminate indicator appears only during active replay/install/loading; pack progress is determinate only from trustworthy host byte progress."
    - "The four locked learner sentences are visible in their states, retained work is not deleted, and no generic retry or technical reason is shown."
    - "An empty recovery destination shows the approved empty heading/body; a validated populated destination retains host domain presentation."
    - "Until a validated recovery destination exists, needs_attention shows no record detail, speculative control, or futile action."
    - "Zero, one, and many recovery records use the same host-owned entry point and never display a count as server acceptance."
    - "Verified pack availability remains represented only by the established pack surface, never inferred from sync status."
    - "The Review saved answers action appears only for needs_attention with a validated host destination; it uses visible text, semantic accent, and a nonsensitive host-owned identifier."
    - "Every transition makes one VoiceOver announcement while preserving focus; Reduce Motion disables motion, Dynamic Type wraps, and controls remain at least 44pt."
    - "Light/dark/system appearance uses existing semantic tokens, SF Symbols/text carry state together, and no native alert, toast, modal, diagnostic card, generic dashboard, or generic sync UI is added."
    - statement: "At Accessibility XXXL, status and recovery content wrap and scroll while every 44pt control remains reachable."
      verification: backstop
    - statement: "At Accessibility XXXL, locked sentences, CTA, and host recovery labels have no ellipsis or clipping and remain hittable."
      verification: backstop
  artifacts:
    - path: "examples/phoenix_host/lib/crosswake_example_web/controllers/offline_html/index.html.heex"
      provides: "Approved in-flow semantic study status/recovery row and token-based styling."
    - path: "examples/phoenix_host/priv/static/offline_study.js"
      provides: "Closed state presenter driven by local replay lifecycle without technical/sensitive copy."
    - path: "examples/phoenix_host/e2e/offline_sync.spec.ts"
      provides: "Browser state/copy/retention/action contract tests."
    - path: "priv/templates/crosswake/proof_lane/ios/CrosswakeProofLaneUITests/ProofLaneUITests.swift.eex"
      provides: "Physical XCUITest accessibility, lifecycle, and backstop assertions."
  key_links:
    - from: "offline_study.js replay lifecycle"
      to: "#crosswake-study-status"
      via: "closed renderStudyStatus(state) mapping"
      pattern: "saved_locally|syncing|needs_attention|sync_paused"
    - from: "generated XCUITest"
      to: "host-owned nonsensitive accessibility identifiers"
      via: "visible label, focus, hit-target, Dynamic Type, and retained-queue assertions"
      pattern: "crosswake-study-status|crosswake-review-saved-answers"
---

<objective>
Deliver the approved learner-visible recovery surface and prove it through browser and physical-driver selectors.

Purpose: Tell the learner what is safely local versus server-accepted without exposing backend mechanics or offering destructive/futile recovery.
Output: In-flow status row, closed state presenter, browser behavior tests, and XCUITest accessibility/backstop coverage.
</objective>

<artifacts_this_phase_produces>
- Host status IDs `crosswake-study-status`, `crosswake-study-status-label`, `crosswake-study-status-message`, and conditional `crosswake-review-saved-answers`; IDs encode no route/scope/mutation/account/device fact.
- Closed JS `renderStudyStatus(state, options)` presentation mapping and one announcement/focus-preserving transition effect.
- Browser plus generated XCUITest coverage for all approved covered/backstop considerations; no native alert/confirm or generic recovery destination.
</artifacts_this_phase_produces>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/162-physical-iphone-adoption-proof/162-CONTEXT.md
@.planning/phases/162-physical-iphone-adoption-proof/162-UI-SPEC.md
@examples/phoenix_host/lib/crosswake_example_web/controllers/offline_html/index.html.heex
@examples/phoenix_host/priv/static/offline_study.js
@examples/phoenix_host/e2e/offline_sync.spec.ts
@examples/ios_shell_host/CrosswakeShell/RequiredPackView.swift
@examples/ios_shell_host/CrosswakeShellUITests/RequiredPackViewAccessibilityTests.swift

<interfaces>
- `offline_study.js` owns local IndexedDB/replay lifecycle and already fences scope before asynchronous shutdown.
- Plan 03 XCUITest owns observable device-local state only; Phoenix authority remains Plan 02.
- `RequiredPackView` establishes combined semantics, Dynamic Type, 44pt controls, focus preservation, and one announcement effect.
- The approved UI-SPEC state copies and tokens are exact contracts, not discretionary wording.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Render the four-state in-flow study status and recovery contract</name>
  <reversibility rating="reversible">The host presentation is local and replaceable behind the locked semantic state contract.</reversibility>
  <read_first>
    - .planning/phases/162-physical-iphone-adoption-proof/162-UI-SPEC.md
    - examples/phoenix_host/lib/crosswake_example_web/controllers/offline_html/index.html.heex
    - examples/phoenix_host/priv/static/offline_study.js
    - examples/phoenix_host/e2e/offline_sync.spec.ts
  </read_first>
  <files>examples/phoenix_host/lib/crosswake_example_web/controllers/offline_html/index.html.heex, examples/phoenix_host/priv/static/offline_study.js, examples/phoenix_host/e2e/offline_sync.spec.ts</files>
  <behavior>
    - "Offline queued work renders the exact saved_locally copy; active replay alone renders syncing; reject/conflict renders needs_attention; fence or gate denial renders sync_paused."
    - "needs_attention retains work and shows Review saved answers only with a validated host destination; otherwise it has no action."
    - "Logout/account switch and entry/replay disablement show sync_paused without technical reason, retry, deletion, or online-only downgrade."
    - "Combined semantic element announces once per changed state and does not steal current answer-control focus."
  </behavior>
  <action>Replace the free-form `updateStatus(message)` surface with a closed presenter driven by the four D-07 states. Put one combined status row below the heading and above answer controls with SF-symbol-equivalent inline semantic icon treatment, visible label, exact locked sentence, `role=status`, polite atomic live behavior, and host-owned identifiers. Use the exact UI-SPEC tokens, sizes, weights, 16px padding, and 44pt action target. Preserve card-position text outside the sync status row so it cannot overwrite recovery state. Remove reconnect copy that promises retries; replay occurs only from the existing active foreground lifecycle. Validate a recovery destination through a host-owned boolean/URL-safe seam before showing the CTA, while rendering no destination details in partial state.</action>
  <acceptance_criteria>
    - All four exact copies and state mappings are tested through real IndexedDB/replay behavior.
    - Rejected/conflicted/disabled/fenced work remains stored and visible; no routine retry control exists.
    - Recovery CTA is absent without validated destination and present/hittable only with one.
    - No sensitive or technical term appears in learner status output.
  </acceptance_criteria>
  <verify><automated>(cd examples/phoenix_host &amp;&amp; npm run proof:offline-island -- --grep "study status|recovery|account switch|feature disablement") &amp;&amp; mix test test/crosswake/offline/status_test.exs --max-failures 1</automated></verify>
  <done>The study flow truthfully distinguishes local save, active replay, retained attention, and safe pause using the approved in-flow UI.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prove accessibility, layout, motion, and focus through the physical driver</name>
  <reversibility rating="reversible">Accessibility assertions refine the stable UI contract without changing backend or evidence authority.</reversibility>
  <read_first>
    - .planning/phases/162-physical-iphone-adoption-proof/162-UI-SPEC.md
    - priv/templates/crosswake/proof_lane/ios/CrosswakeProofLaneUITests/ProofLaneUITests.swift.eex
    - examples/ios_shell_host/CrosswakeShellUITests/RequiredPackViewAccessibilityTests.swift
    - examples/phoenix_host/lib/crosswake_example_web/controllers/offline_html/index.html.heex
  </read_first>
  <files>priv/templates/crosswake/proof_lane/ios/CrosswakeProofLaneUITests/ProofLaneUITests.swift.eex, examples/phoenix_host/e2e/offline_sync.spec.ts</files>
  <behavior>
    - "Accessibility XXXL shows full locked copy/CTA with wrapping, scrolling, no ellipsis, and reachable 44pt controls."
    - "Each state change emits one combined announcement effect and preserves the currently focused answer control."
    - "Reduce Motion disables status motion while active syncing remains semantically labeled."
    - "Light/dark/system states retain semantic icon + text + token contrast and color is never the sole cue."
  </behavior>
  <action>Extend the existing generated physical XCUITest target using only the host-owned identifiers from Task 1. Drive all four states through host adapter controls, not DOM implementation hooks. At Accessibility XXXL assert full labels, horizontal containment/wrapping, scroll reachability, 44pt/hittable CTA, and no ellipsis. Capture the focused answer control before transitions and assert it remains focused/hittable while exactly one status announcement effect is recorded by the existing test seam. Exercise Reduce Motion plus light/dark/system appearance with semantic state labels/icons. Keep all XCUITest evidence as fixed assertion outcomes; do not retain screenshots or raw accessibility trees.</action>
  <acceptance_criteria>
    - Both UI-SPEC backstops pass in generated XCUITest.
    - Dynamic Type, focus, VoiceOver announcement, 44pt, Reduce Motion, and semantic appearance tests are discriminating.
    - Tests use no sensitive selectors and emit no UI tree/screenshots into retained evidence.
    - Phoenix authority assertions remain untouched and independent.
  </acceptance_criteria>
  <verify><automated>mix test test/crosswake/proof_lane/template_contract_test.exs --max-failures 1 &amp;&amp; (cd examples/phoenix_host &amp;&amp; npm run proof:offline-island -- --grep "accessibility|study status")</automated></verify>
  <done>The physical driver can automatically evaluate every approved covered/backstop UI consideration without human verification.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| replay/backend outcome -> learner copy | Technical or sensitive denial detail must not enter the UI. |
| host recovery destination -> action | An absent/unvalidated destination must not produce a futile or unsafe action. |
| accessibility selectors/output -> evidence | Selectors and raw UI artifacts must not become identifying retained evidence. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-162-16 | Information Disclosure | study status copy | high | mitigate | Closed state-to-copy mapping and tests reject technical/sensitive values. |
| T-162-17 | Tampering | recovery action | medium | mitigate | CTA renders only after validated host destination and never mutates/deletes queue itself. |
| T-162-18 | Spoofing | UI authority | high | mitigate | Plans 02/03 keep backend assertion ownership outside UI; Task 2 tests only device-local states. |
| T-162-19 | Denial of Service | inaccessible recovery | medium | mitigate | XXXL wrap/scroll, 44pt, focus, motion, and contrast backstops are automated. |
| T-162-20 | Repudiation | announcement count | low | accept | One deterministic test seam records the semantic announcement effect; raw VoiceOver output is not retained. |

ASVS L1: high disclosure and authority threats are mitigated at the backend-to-UI boundary.
</threat_model>

<verification>
- Browser proof exercises state, retention, CTA, and forbidden-copy behavior.
- Generated XCUITest contract verifies UI-SPEC covered and backstop considerations.
- No checkpoint:human-verify or UAT handoff is created; automated assertions are the gate.
</verification>

<success_criteria>
- D-07 through D-09 are implemented exactly.
- DEVICE-03..05 visible recovery/fencing/disablement states are usable and honest.
- Every covered UI consideration is an observable must-have and both backstops are explicitly automated.
</success_criteria>

<output>
Create `.planning/phases/162-physical-iphone-adoption-proof/162-04-SUMMARY.md` when done.
</output>
