---
phase: 162-physical-iphone-adoption-proof
plan: "13"
type: execute
wave: 13
depends_on: [162-12]
files_modified:
  - priv/templates/crosswake/proof_lane/ios/ProofLaneDriver.swift.eex
  - priv/templates/crosswake/proof_lane/ios/CrosswakeProofLaneTests/ProofLaneContractTests.swift.eex
  - script/verify_physical_iphone_report_contract.sh
  - test/crosswake/proof_lane/physical_iphone_report_contract_script_test.exs
autonomous: true
gap_closure: true
requirements: [DEVICE-02, DEVICE-06]
must_haves:
  truths:
    - "The generated physical adapter contract carries an actual free-form value and its all-pass contract test proves the value reached the host seam before free-form/relaunch assertions pass (D-01/D-03, DEVICE-02)."
    - "The advisory simulator command has a real successful serialization-only path: it parses the owner-free generated envelope with production Elixir code, requires the exact device-local assertion IDs and closed unavailable outcomes, and never represents that simulator envelope as an all-passed physical report."
    - "A separate integration regression invokes the real `PhysicalIphone.parse_report/2` and `join_report_entries/2` semantics with exact owner-disjoint passed serialization fixtures; neither `mix` nor the join is replaced by grep/shell success stubs."
    - "The advisory command cannot promote evidence or public support, and its JSON result states serialization-only advisory scope (D-05/D-06, DEVICE-06)."
  prohibitions:
    - statement: "An unavailable simulator report must not be joined, labeled, or retained as all-passed physical-device evidence."
      verification: flagged-unverified
    - statement: "Shell grep stubs must not substitute for the production Elixir parser or owner-disjoint join semantics."
      verification: flagged-unverified
    - statement: "Serialization fixtures and output must not contain raw answers, scopes, accounts, devices, media, credentials, paths, or test-only integrity material."
      verification: flagged-unverified
  artifacts:
    - path: "priv/templates/crosswake/proof_lane/ios/ProofLaneDriver.swift.eex"
      provides: "Value-carrying generated PhysicalIphoneHostAdapter and sequence contract."
    - path: "priv/templates/crosswake/proof_lane/ios/CrosswakeProofLaneTests/ProofLaneContractTests.swift.eex"
      provides: "Explicit serialization-only simulator envelope and value-forwarding adapter regressions."
    - path: "script/verify_physical_iphone_report_contract.sh"
      provides: "Executable real-project simulator envelope validator with no physical join or promotion authority."
    - path: "test/crosswake/proof_lane/physical_iphone_report_contract_script_test.exs"
      provides: "Integration proof of the script success path plus real Elixir parser/join behavior."
  key_links:
    - from: "generated `testPhysicalContractModeEmitsOwnerFreeReport` stdout"
      to: "PhysicalIphone.parse_report/2"
      via: "the advisory script passes exact captured bytes to production Elixir envelope parsing"
      pattern: "parse_report"
    - from: "owner-disjoint serialization fixtures"
      to: "PhysicalIphone.join_report_entries/2"
      via: "ExUnit invokes the real compiled Mix task module rather than a replacement executable"
      pattern: "join_report_entries"
    - from: "advisory script result"
      to: "support/evidence promotion"
      via: "no link exists; output is explicitly serialization-only and has no destination/promote input"
      pattern: "PI-CONTRACT-SERIALIZATION"
---

<objective>
Make the advisory physical-report contract executable and honest while propagating the repaired value-carrying host seam into generated proof lanes.

Purpose: Close CR-02 without converting unavailable simulator observations into physical success or masking the production parser/join behind shell stubs.
Output: Generated contract parity, a real serialization-only script success path, and integration tests that execute production Elixir parser/join semantics.
</objective>

<artifacts_this_phase_produces>
- Changed generated `PhysicalIphoneHostAdapter.submitFreeFormAnswerOffline(_:)` signature and sequence call.
- A recording/serialization test adapter that records the exact value only inside the Swift test process and emits no value-bearing report.
- Revised `testPhysicalContractModeEmitsOwnerFreeReport` contract documenting/producing exact unavailable simulator observations.
- Revised `verify_physical_iphone_report_contract.sh` CLI behavior: successful output is advisory serialization validation only and performs no physical join/promotion.
- New/rewritten ExUnit tests that run the real generated project path and production `parse_report/2` and `join_report_entries/2` functions.
</artifacts_this_phase_produces>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/162-physical-iphone-adoption-proof/162-CONTEXT.md
@.planning/phases/162-physical-iphone-adoption-proof/162-VERIFICATION.md
@.planning/phases/162-physical-iphone-adoption-proof/162-REVIEW.md
@.planning/phases/162-physical-iphone-adoption-proof/162-12-SUMMARY.md
@priv/templates/crosswake/proof_lane/ios/ProofLaneDriver.swift.eex
@priv/templates/crosswake/proof_lane/ios/CrosswakeProofLaneTests/ProofLaneContractTests.swift.eex
@script/verify_physical_iphone_report_contract.sh
@test/crosswake/proof_lane/physical_iphone_report_contract_script_test.exs
@lib/mix/tasks/crosswake.proof_lane.physical_iphone.ex
@test/mix/tasks/crosswake.proof_lane.physical_iphone_test.exs

<interfaces>
- `Mix.Tasks.Crosswake.ProofLane.PhysicalIphone.parse_report/2` accepts exact owner-free envelopes and assigns ownership from the trusted slot; malformed keys, wrong IDs, and wrong-slot IDs return stable PI rules.
- `join_report_entries/2` accepts only the exact ordered all-passed union of device-local and backend-authority entries. It is production physical completion semantics and must not consume the unavailable simulator envelope.
- The generated factory intentionally returns nil. Contract mode therefore produces the exact device-local IDs with unavailable outcomes; this is valid serialization evidence, not a physical pass.
- Plan 162-12 changed the reference-host protocol to carry the actual String; the generated template must match that signature and test value forwarding without adding payload fields to `PhysicalIphoneDeviceReport`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Propagate value-carrying generated contracts and label simulator output honestly</name>
  <reversibility rating="costly">Generated proof lanes consume this protocol/template shape; undo requires regenerating or coordinating every host-owned copy.</reversibility>
  <read_first>
    - AGENTS.md
    - .planning/phases/162-physical-iphone-adoption-proof/162-CONTEXT.md
    - .planning/phases/162-physical-iphone-adoption-proof/162-VERIFICATION.md
    - .planning/phases/162-physical-iphone-adoption-proof/162-12-SUMMARY.md
    - priv/templates/crosswake/proof_lane/ios/ProofLaneDriver.swift.eex
    - priv/templates/crosswake/proof_lane/ios/CrosswakeProofLaneTests/ProofLaneContractTests.swift.eex
  </read_first>
  <files>priv/templates/crosswake/proof_lane/ios/ProofLaneDriver.swift.eex, priv/templates/crosswake/proof_lane/ios/CrosswakeProofLaneTests/ProofLaneContractTests.swift.eex</files>
  <behavior>
    - "The generated protocol and sequence pass a nonempty contract-only String into the host adapter exactly once."
    - "Recording adapter tests prove value arrival internally while encoded reports remain the exact three-key owner-free envelope."
    - "Nil-adapter simulator contract mode returns every exact device-local assertion as unavailable, never passed."
  </behavior>
  <action>Update the generated driver/template protocol in lockstep with Plan 162-12 so `PhysicalIphoneSequence` supplies a nonempty contract-only value and recording adapters prove it was received before the free-form operation returns passed. Keep the value entirely inside Swift test memory and assert encoded report bytes omit it. Make the contract-mode test explicitly assert the nil-adapter report has exact schema/class/ordered IDs and all unavailable outcomes before printing the owner-free JSON. D-04: preserve the nil-factory fail-closed prerequisite/default and every existing first-nonpassing/operation-order test. Do not introduce a production adapter, simulator pass adapter, new evidence field, raw value output, or implicit host authority.</action>
  <acceptance_criteria>
    - Generated source compiles with the value-carrying adapter contract and all conformers/callers are updated.
    - All-pass sequence tests fail if the contract value is discarded, altered, or not observed by the recording adapter.
    - Contract mode emits an exact unavailable owner-free envelope and contains no value, scope, owner, marker, path, or host detail.
  </acceptance_criteria>
  <verify><automated>mix test test/crosswake/proof_lane/template_contract_test.exs --max-failures 1</automated></verify>
  <done>Newly generated proof lanes carry the repaired semantic value seam while their simulator contract output remains explicitly unavailable and non-promoting.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Execute the real serialization parser and owner-disjoint join in integration proof</name>
  <read_first>
    - AGENTS.md
    - .planning/phases/162-physical-iphone-adoption-proof/162-VERIFICATION.md
    - .planning/phases/162-physical-iphone-adoption-proof/162-REVIEW.md
    - script/verify_physical_iphone_report_contract.sh
    - test/crosswake/proof_lane/physical_iphone_report_contract_script_test.exs
    - lib/mix/tasks/crosswake.proof_lane.physical_iphone.ex
    - test/mix/tasks/crosswake.proof_lane.physical_iphone_test.exs
  </read_first>
  <files>script/verify_physical_iphone_report_contract.sh, test/crosswake/proof_lane/physical_iphone_report_contract_script_test.exs</files>
  <behavior>
    - "The real generated simulator test plus production Elixir parser returns advisory serialization success for the exact unavailable device envelope."
    - "Malformed, passed-spoofed, partial, extra-key, wrong-class, or wrong-ID simulator envelopes fail before success output."
    - "A separate ExUnit integration case feeds exact passed device/backend owner-free bytes through the real parser and real join and receives the canonical passed candidate."
    - "Replacing xcodebuild for negative shell isolation is allowed, but `mix`, `parse_report/2`, and `join_report_entries/2` are never stubbed in the positive semantics test."
  </behavior>
  <action>Rewrite the script's final Elixir step so the real generated nil-adapter simulator bytes are parsed in the device-local slot and validated as the exact ordered device-local assertion set with all outcomes unavailable. Remove the Phoenix producer and all-passed join from this serialization-only CLI path; its only success JSON must say advisory/serialization and it must have no evidence destination or promotion input. D-05: keep the physical class/fixed assertion vocabulary exact while ensuring simulator output never satisfies promotion. D-06: keep every fixture and output synthetic, closed, and free of adopter, payload, identity, media, scope, or path data. In ExUnit, retain cheap xcodebuild fixtures for malformed/closed shell cases but stop placing a fake `mix` executable on PATH. Add an integration case that invokes the compiled production `parse_report/2` for exact passed device and backend owner-free fixtures and then calls the real `join_report_entries/2`; assert wrong ownership, unavailable device input, malformed input, and partial input do not join. Ensure the positive script case reaches real `mix run` and the positive join case reaches the actual Elixir functions, so grep-only success cannot satisfy either.</action>
  <acceptance_criteria>
    - Running the script against the generated simulator project succeeds by validating an unavailable serialization envelope, not by joining or labeling it physical success.
    - The script no longer requires or accepts a Phoenix producer for its advisory result and has no promotion/evidence write path.
    - ExUnit executes production parser and join code for the success case and proves unavailable simulator entries cannot satisfy the join.
    - No fake `mix` binary or grep-only join appears in positive coverage.
  </acceptance_criteria>
  <verify><automated>mix test test/crosswake/proof_lane/physical_iphone_report_contract_script_test.exs test/mix/tasks/crosswake.proof_lane.physical_iphone_test.exs --max-failures 1 &amp;&amp; script/verify_physical_iphone_report_contract.sh</automated></verify>
  <done>The advisory CLI has a real executable serialization success path, and production parser/join authority is exercised separately without treating simulator unavailability as physical success.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| generated simulator stdout -> shell extraction | Untrusted tool output must yield one exact owner-free JSON envelope. |
| captured envelope -> Elixir parser | Production parser assigns trusted owner slots and rejects malformed or wrong-slot assertions. |
| serialization fixtures -> production join | Only exact owner-disjoint passed fixtures may exercise join success; simulator unavailability remains non-passing. |
| advisory result -> evidence/support | Serialization success has no authority to promote physical evidence or support. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-162-65 | Spoofing | simulator report as physical pass | high | mitigate | Tasks 1-2 require exact unavailable outcomes and never feed them to the all-passed join. |
| T-162-66 | Tampering | parser/join test substitution | high | mitigate | Task 2 removes the fake mix path and calls compiled production parser/join functions in the positive integration test. |
| T-162-67 | Elevation of Privilege | advisory result promoting support/evidence | high | mitigate | Task 2 removes producer/join/promotion inputs and emits explicit serialization-only advisory scope. |
| T-162-68 | Information Disclosure | generated report and fixtures | high | mitigate | Both tasks enforce exact closed keys and synthetic fixed IDs/outcomes with no values, identities, media, scopes, or paths. |
| T-162-69 | Repudiation | false success path | medium | mitigate | The real-project script and focused ExUnit command are both required and report stable PI contract rules. |
| T-162-70 | Denial of Service | missing Xcode simulator/toolchain | low | accept | The advisory gate fails closed with its existing stable rule and cannot affect physical support truth. |
</threat_model>

<verification>
- Render and compile the generated Swift contract through existing template tests.
- Run the focused script integration and Mix-task parser/join tests with the real Elixir runtime.
- Run the actual advisory script against the generated simulator project and require only serialization-scoped success.
</verification>

<success_criteria>
- CR-02 is closed by an executable honest simulator envelope path.
- Real parser/join semantics are proven without stubbing and unavailable device entries remain non-passing.
- Generated lanes preserve the repaired free-form value contract without widening evidence or platform scope.
</success_criteria>

<output>
Create `.planning/phases/162-physical-iphone-adoption-proof/162-13-SUMMARY.md` when done.
</output>
