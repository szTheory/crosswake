---
phase: 162-physical-iphone-adoption-proof
plan: "09"
type: execute
wave: 9
depends_on: [162-08]
files_modified:
  - .planning/phases/162-physical-iphone-adoption-proof/evidence/physical_iphone/proof-lane-evidence.json
  - .planning/phases/162-physical-iphone-adoption-proof/evidence/physical_iphone/.complete
autonomous: true
gap_closure: true
requirements: [DEVICE-01, DEVICE-02, DEVICE-03, DEVICE-04, DEVICE-05, DEVICE-06, DEVICE-07]
user_setup:
  - service: first-adopter-host
    why: "The physical proof requires the eligible sanitized route row and the Phase 163 signed host/backend, fixture, media, replay, recovery, scoped-session, and feature-gate adapters."
    dashboard_config:
      - task: "Make the host-owned Crosswake.ProofLane.PhysicalIphoneHost adapter available through the host's existing runtime configuration without copying credentials or adopter data into this repository."
        location: "Eligible host runtime configuration"
  - service: physical-iphone
    why: "Apple signing and one selected physical iPhone are unavoidable execution prerequisites; the automated driver owns every assertion after connection."
    dashboard_config:
      - task: "Connect and trust one physical iPhone and make the existing generated proof scheme signable for that device."
        location: "Local Xcode signing/device setup"
must_haves:
  truths:
    - "Per D-01/D-04, the signed run starts only when the closed readiness command reports every route, generated-lane, host, signing, media, backend-control, destination, and physical-device prerequisite ready; otherwise the existing stable blocked result remains the only outcome and no evidence directory exists."
    - "Per D-01/D-02/D-03, one uninterrupted host-owned run proves verified offline pronunciation bytes, selected and free-form local mutations, kill/relaunch persistence, reconnect, backend-authorized exactly-once application, retained rejection/conflict recovery, scope fencing, and independent entry/replay disablement."
    - "Per D-05/D-06, success atomically publishes one canonical physical_iphone record at the fixed destination, with a bounded iOS runtime line, every fixed assertion passed, approved sanitized hashes, and no retained raw output or identifying values."
    - "Per D-07/D-08/D-09, the physical run observes the exact accessible four-state learner surface, retains rejected/conflicted and fenced work, and exposes recovery only through the approved host destination without technical or sensitive copy."
    - "DEVICE-07 remains narrow: this record proves one first-adopter flow on one iOS runtime line and grants no Android, background, generic storage/sync, multi-island, simulator, or every-iPhone claim."
  artifacts:
    - path: ".planning/phases/162-physical-iphone-adoption-proof/evidence/physical_iphone/proof-lane-evidence.json"
      provides: "Dated canonical redacted physical-iPhone evidence for DEVICE-01 through DEVICE-06."
    - path: ".planning/phases/162-physical-iphone-adoption-proof/evidence/physical_iphone/.complete"
      provides: "Digest marker binding the retained canonical evidence bytes after no-replace publication."
  key_links:
    - from: "mix crosswake.proof_lane.physical_iphone --readiness --json"
      to: "mix crosswake.proof_lane.physical_iphone --run --promote --json"
      via: "all closed readiness checks must be ready before the production runner is invoked"
      pattern: "ready|PI-PREFLIGHT"
    - from: "device-local XCUITest/XCTest and Phoenix backend-authority reports"
      to: "Crosswake.ProofLane.Evidence.promote/2"
      via: "owner-disjoint exact assertion join followed by canonical validation and no-replace publication"
      pattern: "device_local|backend_authority|physical_iphone"
    - from: "proof-lane-evidence.json"
      to: "Crosswake.ProofLane.Evidence.check/1"
      via: "final-byte reparse, digest-marker verification, approved-source checks, and directory allowlist"
      pattern: "proof-lane-evidence.json|.complete"
---

<objective>
Run the actual signed physical-iPhone exit test and publish the sole missing dated redacted artifact.

Purpose: Close the verified external-evidence gap without substituting simulator, browser, fixture, advisory, or conversational evidence for the real host/device run.
Output: One checked no-replace physical evidence directory, or the unchanged stable blocked outcome while any external prerequisite is absent.
</objective>

<artifacts_this_phase_produces>
- The fixed `evidence/physical_iphone/` directory containing exactly canonical `proof-lane-evidence.json` and its matching `.complete` marker after one successful signed run.
- Fresh automated evidence for DEVICE-01 through DEVICE-06 from the existing physical driver and independent Phoenix authority producer.
- No screenshots, video, `.xcresult`, logs, raw output, device identifiers, route paths, endpoints, credentials, account data, payloads, media, fixtures, or binaries.
</artifacts_this_phase_produces>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/ADR-FIRST-B2C-ADOPTER.md
@.planning/FIRST-B2C-ADOPTER-ROUTE-POLICY-MAP.md
@.planning/todos/TODO-002-first-b2c-adopter-route-inputs.md
@.planning/phases/162-physical-iphone-adoption-proof/162-CONTEXT.md
@.planning/phases/162-physical-iphone-adoption-proof/162-VERIFICATION.md
@.planning/phases/162-physical-iphone-adoption-proof/162-08-SUMMARY.md
@lib/mix/tasks/crosswake.proof_lane.physical_iphone.ex
@lib/crosswake/proof_lane/physical_iphone_host.ex
@lib/crosswake/proof_lane/physical_iphone_preflight.ex
@lib/crosswake/proof_lane/evidence.ex

<interfaces>
- `mix crosswake.proof_lane.physical_iphone --readiness --json` is the read-only prerequisite view; it emits only closed check IDs/states and exits 2 while blocked.
- `mix crosswake.proof_lane.physical_iphone --run --promote --json` is the sole production driver and promotion entry point.
- The configured `Crosswake.ProofLane.PhysicalIphoneHost` adapter owns sanitized inventory/check callbacks, exact device and backend reports, canonical evidence input, and the fixed destination.
- `Crosswake.ProofLane.Evidence.check/1` is the post-publication artifact authority; raw tool output and host/device values are never evidence.
</interfaces>
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Execute and promote one real signed physical-iPhone proof</name>
  <precondition>`mix crosswake.proof_lane.physical_iphone --readiness --json` exits zero with outcome `ready` and every declared check state `ready`; this requires the eligible sanitized TODO-002 row, completed eligible Phase 163 host integration, host-only adapters/credentials, valid media requirements, backend controls, signing, and one selected physical iPhone.</precondition>
  <reversibility rating="costly">D-05/D-06 make the first accepted no-replace evidence record durable release truth; correction requires a separately versioned proof rather than overwriting these bytes.</reversibility>
  <read_first>
    - AGENTS.md
    - .planning/phases/162-physical-iphone-adoption-proof/162-CONTEXT.md
    - .planning/phases/162-physical-iphone-adoption-proof/162-VERIFICATION.md
    - .planning/phases/162-physical-iphone-adoption-proof/162-08-SUMMARY.md
    - .planning/todos/TODO-002-first-b2c-adopter-route-inputs.md
    - lib/mix/tasks/crosswake.proof_lane.physical_iphone.ex
    - lib/crosswake/proof_lane/physical_iphone_host.ex
    - lib/crosswake/proof_lane/evidence.ex
  </read_first>
  <files>.planning/phases/162-physical-iphone-adoption-proof/evidence/physical_iphone/proof-lane-evidence.json, .planning/phases/162-physical-iphone-adoption-proof/evidence/physical_iphone/.complete</files>
  <action>Execute the D-01 sequence through the existing production Mix task, not through an injected test seam. Treat this task's `<verify><automated>` command as the single production invocation; do not run the promotion command during exploratory checks and then run it again. Per D-04, run the automated command with `set -euo pipefail`, install an EXIT cleanup trap immediately after creating the invocation-owned mode-0700 temporary directory, and join readiness-predicate sample checks, readiness capture, exact JSON validation, and the absent-destination assertion with `&amp;&amp;` before the production invocation. Use the same exact array-scoped jq predicate `.outcome == "ready" and (.checks | type == "array" and length &gt; 0) and all(.checks[]; .state == "ready")` for the known-ready sample and captured production readiness. Before invoking production, require that predicate to accept a nonempty ready-check sample and reject both a blocked-check sample and malformed JSON; a sample-gate failure must stop the chain. The readiness command's nonzero status, malformed JSON, any outcome other than exact `ready`, an empty check set, any declared check state other than exact `ready`, or a pre-existing fixed destination must terminate the shell before `mix crosswake.proof_lane.physical_iphone --run --promote --json` is invoked. An existing destination is a no-replace stop condition, never permission to delete or overwrite it. Only after those hard gates pass, run the production command exactly once against the configured signed host and selected physical iPhone. Require the D-02 authority split: generated XCTest/XCUITest supplies only device-local lifecycle/audio/recovery observations, while the executed Phoenix producer supplies only replay admission, transactional idempotency/effect, conflict, scope, and feature-gate observations. Require the complete fixed assertion set to pass, then run `Evidence.check/1` on the fixed destination and independently require exactly the two allowlisted regular files. Keep all command captures ephemeral and remove the private run root through the EXIT trap on success or failure. Do not print, copy, summarize, commit, or retain raw command output, device/destination identities, paths, routes, endpoints, credentials, account/scope/mutation values, payloads, media, test transcripts, logs, screenshots, video, `.xcresult`, fixtures, or binaries. If readiness is not met, preserve D-04 by returning the existing stable blocked outcome and leaving the destination absent; do not fabricate an artifact or alter support/requirements truth.</action>
  <acceptance_criteria>
    - The production driver runs only after every closed readiness check is ready and it targets a signed physical iPhone, never a simulator or injected report fixture.
    - One uninterrupted D-01 sequence proves DEVICE-01 through DEVICE-05 with exact owner-disjoint passed assertions from the real device and Phoenix host.
    - The fixed destination contains exactly canonical `proof-lane-evidence.json` plus the matching 64-byte `.complete` marker, and `Evidence.check/1` passes after publication.
    - Retained evidence contains only the D-06 allowlist and approved sanitized hashes; all raw host/device/tool material is removed with the invocation-owned temporary root.
    - Before promotion can run, the array-scoped readiness predicate accepts a known-ready nonempty check array and rejects both a blocked check array and malformed JSON; the latter samples cannot advance the `&amp;&amp;` chain to the production invocation.
    - When any prerequisite is absent, the command remains non-passing with one stable blocked rule and no evidence destination, support promotion, or requirement completion.
  </acceptance_criteria>
  <verify><automated>set -euo pipefail; proof_run_root=$(mktemp -d); trap 'rm -rf -- "$proof_run_root"' EXIT; chmod 700 "$proof_run_root" &amp;&amp; printf '%s\n' '{"outcome":"ready","checks":[{"state":"ready"}]}' &gt; "$proof_run_root/ready-sample.json" &amp;&amp; printf '%s\n' '{"outcome":"blocked","checks":[{"state":"blocked"}]}' &gt; "$proof_run_root/blocked-sample.json" &amp;&amp; printf '%s\n' '{"outcome":"ready","checks":' &gt; "$proof_run_root/malformed-sample.json" &amp;&amp; jq -e '.outcome == "ready" and (.checks | type == "array" and length &gt; 0) and all(.checks[]; .state == "ready")' "$proof_run_root/ready-sample.json" &gt;/dev/null &amp;&amp; ! jq -e '.outcome == "ready" and (.checks | type == "array" and length &gt; 0) and all(.checks[]; .state == "ready")' "$proof_run_root/blocked-sample.json" &gt;/dev/null &amp;&amp; ! jq -e '.outcome == "ready" and (.checks | type == "array" and length &gt; 0) and all(.checks[]; .state == "ready")' "$proof_run_root/malformed-sample.json" &gt;/dev/null 2&gt;&amp;1 &amp;&amp; mix crosswake.proof_lane.physical_iphone --readiness --json &gt; "$proof_run_root/readiness.json" &amp;&amp; jq -e '.outcome == "ready" and (.checks | type == "array" and length &gt; 0) and all(.checks[]; .state == "ready")' "$proof_run_root/readiness.json" &gt;/dev/null &amp;&amp; test ! -e .planning/phases/162-physical-iphone-adoption-proof/evidence/physical_iphone &amp;&amp; mix crosswake.proof_lane.physical_iphone --run --promote --json &gt; "$proof_run_root/result.json" &amp;&amp; jq -e '.outcome == "passed" and .device_class == "physical_iphone" and (.assertions | type == "array" and length &gt; 0) and all(.outcome == "passed")' "$proof_run_root/result.json" &gt;/dev/null &amp;&amp; mix run -e 'alias Crosswake.ProofLane.Evidence; :ok = Evidence.check(".planning/phases/162-physical-iphone-adoption-proof/evidence/physical_iphone")' &amp;&amp; test "$(find .planning/phases/162-physical-iphone-adoption-proof/evidence/physical_iphone -mindepth 1 -maxdepth 1 -type f | wc -l | tr -d ' ')" = "2" &amp;&amp; test "$(wc -c &lt; .planning/phases/162-physical-iphone-adoption-proof/evidence/physical_iphone/.complete | tr -d ' ')" = "64"</automated></verify>
  <done>A fresh signed physical-iPhone run has produced the one canonical checked artifact, or execution remains honestly stopped at the unchanged external preflight gate with no partial evidence.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| eligible host/runtime config -> physical preflight | External route, signing, device, media, credentials, and backend controls are sensitive and cannot be inferred or emitted. |
| signed physical iPhone and Phoenix producer -> joined candidate | Device-local observation and backend authority are independent; neither producer may fill the other's assertion slots. |
| ephemeral run output -> retained evidence | Raw Xcode/Phoenix/device output can contain forbidden identifiers and must not cross into the canonical allowlist. |
| canonical candidate -> no-replace destination | Publication must resist partial writes, replacement, stale artifacts, and post-write tampering. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-162-45 | Spoofing | physical destination and reports | high | mitigate | Task 1 requires closed ready preflight, the configured signed physical destination, and exact contract-derived owner slots before promotion. |
| T-162-46 | Tampering | retained evidence publication | high | mitigate | Task 1 requires absent destination, existing descriptor-safe no-replace promotion, matching completion digest, exact two-file allowlist, and post-publication `Evidence.check/1`. |
| T-162-47 | Information Disclosure | temporary device/host output | high | mitigate | Task 1 confines captures to a mode-0700 invocation root, retains only D-06 fields, and removes the root after automated validation. |
| T-162-48 | Elevation of Privilege | device UI presented as backend authority | high | mitigate | Task 1 accepts owner-disjoint complete reports only; Phoenix alone supplies backend assertions per D-02/D-03. |
| T-162-49 | Repudiation | incomplete or stale physical proof | high | mitigate | Task 1 runs the production command once on the current tree and checks the final canonical artifact and marker before any downstream claim. |
| T-162-50 | Denial of Service | unavailable external prerequisites | low | accept | Stable fail-closed blocking is the intended safe state until the adopter host and connected signed iPhone genuinely exist. |
</threat_model>

<source_coverage_audit>
| SOURCE | ID | Feature/Requirement | Plan | Status | Notes |
|---|---|---|---|---|---|
| GOAL | — | Dated redacted physical-iPhone artifact proving the ten-step exit test | 162-09 | COVERED | Task 1 is the real production tracer, not a substitute lane. |
| REQ | DEVICE-01 | Verified pack install and offline pronunciation audio | 162-09 | COVERED | Physical device-local assertion. |
| REQ | DEVICE-02 | Offline selected/free-form answers, relaunch, exactly-once drain | 162-09 | COVERED | Uninterrupted device lifecycle plus Phoenix authority. |
| REQ | DEVICE-03 | Visible retained rejection/conflict recovery | 162-09 | COVERED | Physical status observation plus host-owned retained recovery result. |
| REQ | DEVICE-04 | Logout/account switch cannot cross scope | 162-09 | COVERED | Device fence and Phoenix denial join. |
| REQ | DEVICE-05 | Entry/replay disablement retains queued work | 162-09 | COVERED | Independent backend gate assertions. |
| REQ | DEVICE-06 | Dated redacted allowlist artifact | 162-09 | COVERED | Canonical no-replace promotion and post-write check. |
| REQ | DEVICE-07 | One-flow/one-runtime narrow claim | 162-09, 162-10 | COVERED | Evidence scope is fixed here; public wording follows only in Plan 10. |
| RESEARCH | Sequence/authority | Sequential host-owned driver with dual device/Phoenix authority | 162-09 | COVERED | Existing production seams execute without injected reports. |
| RESEARCH | Safety/evidence | Hard preflight, physical-only class, closed assertions, canonical atomic publication | 162-09 | COVERED | Missing prerequisites remain blocked; success is checked from final bytes. |
| RESEARCH | UX | Accessible four-state retained-recovery observation | 162-09 | COVERED | Existing required XCUITests run only with host composition. |
| CONTEXT | D-01 | Sequential rerunnable physical exit test | 162-09 | COVERED | Task action preserves exact order and uninterrupted mutation/relaunch/replay lifetime. |
| CONTEXT | D-02 | Independent device-local and Phoenix authority | 162-09 | COVERED | Exact owner-disjoint reports are required. |
| CONTEXT | D-03 | Idiomatic host/library ownership boundary | 162-09 | COVERED | Existing configured host callbacks and backend transaction authority are consumed unchanged. |
| CONTEXT | D-04 | Complete preflight and no partial artifact | 162-09 | COVERED | Readiness must be fully ready; blocked state remains non-promoting. |
| CONTEXT | D-05 | Physical class, fixed assertions, canonical promotion | 162-09 | COVERED | Final artifact and marker are checked after no-replace publication. |
| CONTEXT | D-06 | Exact privacy allowlist | 162-09 | COVERED | Raw material stays ephemeral and forbidden. |
| CONTEXT | D-07/D-08/D-09 | Honest accessible recovery/status experience | 162-09 | COVERED | Existing physical UI assertions execute as part of the signed run. |
</source_coverage_audit>

<verification>
- Before prerequisites exist, `mix crosswake.proof_lane.physical_iphone --readiness --json` and `--run --promote --json` remain closed blocked commands and the fixed evidence directory remains absent.
- After prerequisites exist, the Task 1 automated command requires all readiness checks, a passed physical result, final `Evidence.check/1`, exactly two retained files, and a matching 64-byte completion marker.
- Re-run the focused Phase 162 contract/evidence suite after the physical run: `mix test test/crosswake/proof_lane/physical_iphone_preflight_test.exs test/mix/tasks/crosswake.proof_lane.physical_iphone_test.exs test/crosswake/proof_lane/evidence_test.exs test/crosswake/proof_lane/template_contract_test.exs test/crosswake/proof_lane/physical_iphone_report_contract_script_test.exs --max-failures 1`.
</verification>

<success_criteria>
- The sole remaining verification gap is closed by real dated physical-device evidence.
- DEVICE-01 through DEVICE-06 are proven by one complete owner-disjoint signed run.
- The retained directory is canonical, privacy-safe, digest-bound, and no-replace.
- Until external prerequisites are genuinely ready, the repository retains its current blocked result, absent artifact, pending requirements, and verification-required support truth.
</success_criteria>

<output>
Create `.planning/phases/162-physical-iphone-adoption-proof/162-09-SUMMARY.md` when done.
</output>
