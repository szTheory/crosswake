---
phase: 162-physical-iphone-adoption-proof
plan: "08"
type: execute
wave: 8
depends_on: [162-07]
files_modified:
  - script/verify_physical_iphone_report_contract.sh
  - priv/templates/crosswake/proof_lane/test/crosswake_proof_lane_test.exs.eex
  - priv/templates/crosswake/proof_lane/ios/CrosswakeProofLaneUITests/ProofLaneUITests.swift.eex
  - test/crosswake/proof_lane/template_contract_test.exs
  - test/crosswake/proof_lane/physical_iphone_report_contract_script_test.exs
  - examples/phoenix_host/lib/crosswake_example_web/controllers/offline_controller.ex
  - examples/phoenix_host/lib/crosswake_example_web/controllers/offline_html/index.html.heex
  - examples/phoenix_host/priv/static/offline_study.js
  - examples/phoenix_host/e2e/offline_sync.spec.ts
autonomous: true
gap_closure: true
requirements: [DEVICE-02, DEVICE-03, DEVICE-04, DEVICE-05, DEVICE-06]
must_haves:
  truths:
    - "Per D-02, D-03, and D-05, the advisory dual-authority contract gate can pass only when the rendered Phoenix producer executes its host callbacks successfully and its exact owner-free report bytes join with the independently produced device report."
    - "A missing, failing, silent, malformed, or owner-incompatible Phoenix producer makes the verifier non-passing; the verifier never substitutes backend assertion success or suppresses producer failure."
    - "Per D-07 and D-09, every required generated study-status accessibility test is explicitly non-passing when the host status adapter is absent; the serialization-only simulator command selects only its intended contract test and cannot count omitted UI assertions."
    - "Per D-08, a recovery action appears only for a closed server-approved recovery-route capability; arbitrary same-origin, cross-origin, malformed, or unapproved destinations remain absent while rejected work remains visible and retained."
    - "Per D-04 and D-06, all code-only verification remains advisory, aggregate, non-promoting, and non-sensitive: no physical artifact, completion marker, support claim, raw callback output, route input, or device identity is retained."
    - "TODO-002, an eligible signed host/backend, host adapters, and a physical iPhone remain required before the production promotion command can pass; Android, generic storage, generic sync, and unrelated user changes remain untouched."
  artifacts:
    - path: "script/verify_physical_iphone_report_contract.sh"
      provides: "Fail-closed advisory join of exact generated device and Phoenix producer reports."
    - path: "priv/templates/crosswake/proof_lane/test/crosswake_proof_lane_test.exs.eex"
      provides: "Rendered host-owned Phoenix authority producer that emits a canonical report only after every real callback observation succeeds."
    - path: "priv/templates/crosswake/proof_lane/ios/CrosswakeProofLaneUITests/ProofLaneUITests.swift.eex"
      provides: "Required accessibility tests whose missing host adapter is an explicit XCTest failure."
    - path: "examples/phoenix_host/priv/static/offline_study.js"
      provides: "Recovery-action renderer consuming only the server-approved closed route capability."
    - path: "examples/phoenix_host/e2e/offline_sync.spec.ts"
      provides: "Browser regressions for approved, absent, malformed, and unapproved recovery destinations while queued work remains retained."
  key_links:
    - from: "script/verify_physical_iphone_report_contract.sh"
      to: "generated Crosswake Phoenix authority producer"
      via: "failing command whose stdout is captured byte-for-byte into BACKEND_REPORT_FILE before parse/join"
      pattern: "BACKEND_REPORT_FILE|PHOENIX_OUT|parse_report\(:backend_authority\)|join_report_entries"
    - from: "generated study-status XCUITests"
      to: "CROSSWAKE_PROOF_LANE_STUDY_HOST_ADAPTER"
      via: "required adapter assertion before any accessibility behavior assertion"
      pattern: "CROSSWAKE_PROOF_LANE_STUDY_HOST_ADAPTER|XCTAssert"
    - from: "OfflineController/index.html.heex"
      to: "validatedRecoveryDestination()"
      via: "closed server-resolved approval marker plus destination; origin equality alone is insufficient"
      pattern: "recovery.*approved|recoveryDestination|validatedRecoveryDestination"
---

<objective>
Close review findings BL-01, WR-01, and WR-02 without fabricating the externally blocked physical proof.

Purpose: Ensure advisory report verification consumes two real independent producers, required accessibility assertions cannot disappear as passing tests, and learner recovery navigation is exposed only through a bounded host-approved route capability.
Output: A fail-closed dual-authority verifier and producer regression, explicit XCUITest adapter admission, and a server-approved recovery-route seam with browser coverage.
</objective>

<artifacts_this_phase_produces>
- Executable negative coverage for absent, failing, silent, and malformed Phoenix authority production, plus exact-byte positive joining.
- Generated host-owned Phoenix report emission after the existing callback suite proves backend authority; no literal assertion-success substitute.
- Generated accessibility tests that fail when required host status composition is unavailable, while the narrow serialization command excludes unrelated UI targets.
- One closed Phoenix-owned recovery-route capability and browser tests proving same-origin is not approval.
- No physical evidence artifact, support-matrix edit, support claim, simulator promotion, Android work, generic storage/sync surface, or unrelated worktree cleanup.
</artifacts_this_phase_produces>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/ADR-FIRST-B2C-ADOPTER.md
@.planning/FIRST-B2C-ADOPTER-ADOPTION-BRIEF.md
@.planning/FIRST-B2C-ADOPTER-ROUTE-POLICY-MAP.md
@.planning/phases/162-physical-iphone-adoption-proof/162-CONTEXT.md
@.planning/phases/162-physical-iphone-adoption-proof/162-VERIFICATION.md
@.planning/phases/162-physical-iphone-adoption-proof/162-REVIEW.md
@.planning/phases/162-physical-iphone-adoption-proof/162-07-SUMMARY.md

<interfaces>
- `CrosswakeProofLaneHostAuthority` already requires `reset_fixture/0`, accepted/duplicate replay, retained rejection/conflict, scope fence, entry disablement, and replay disablement callbacks. The generated producer must derive its four backend assertion outcomes only after those callbacks return their exact closed shapes.
- `PhysicalIphone.parse_report/2` admits owner-free JSON envelopes only in a trusted `:device_local` or `:backend_authority` slot; `join_report_entries/2` supplies ownership from that trusted slot and rejects incomplete, overlapping, or mismatched reports.
- `CROSSWAKE_PROOF_LANE_STUDY_HOST_ADAPTER=1` is the generated XCUITest composition signal. Absence is not coverage and must record a test failure for each required study-status test.
- Recovery is host-owned per D-08. The browser may render a fixed destination only after Phoenix resolves an approved closed route capability; URL syntax and same-origin checks are secondary validation, not approval.
</interfaces>
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Join exact output from the executed Phoenix authority producer</name>
  <reversibility rating="costly">D-02 and D-05 bind generated host callbacks, trusted report slots, and the advisory join command; changing this producer contract requires coordinated template, script, and host-proof updates.</reversibility>
  <read_first>
    - .planning/phases/162-physical-iphone-adoption-proof/162-REVIEW.md
    - .planning/phases/162-physical-iphone-adoption-proof/162-CONTEXT.md
    - script/verify_physical_iphone_report_contract.sh
    - priv/templates/crosswake/proof_lane/test/crosswake_proof_lane_test.exs.eex
    - examples/phoenix_host/test/crosswake_example/local_first/physical_iphone_authority_test.exs
    - test/crosswake/proof_lane/template_contract_test.exs
    - test/mix/tasks/crosswake.proof_lane.physical_iphone_test.exs
  </read_first>
  <files>script/verify_physical_iphone_report_contract.sh, priv/templates/crosswake/proof_lane/test/crosswake_proof_lane_test.exs.eex, test/crosswake/proof_lane/template_contract_test.exs, test/crosswake/proof_lane/physical_iphone_report_contract_script_test.exs</files>
  <behavior>
    - "RED: the verifier exits nonzero when the rendered Phoenix producer command fails, emits no report, emits malformed JSON, omits an authority assertion, or emits a device-owned assertion; no join command runs after those failures."
    - "RED: a successful rendered producer executes every host authority callback and emits exactly one canonical owner-free backend envelope; the exact emitted bytes become BACKEND_REPORT_FILE and join with the independently captured device bytes."
    - "Changing produced backend bytes changes the bytes parsed by the join; no script-local success envelope can mask producer drift."
    - "The successful command still prints only the existing advisory PI-CONTRACT-SERIALIZATION result and cannot write a promotion destination or physical evidence."
  </behavior>
  <action>Start with a focused ExUnit shell regression that runs the script through explicit test-owned command seams or extracted helpers without requiring a physical device. Cover producer failure, empty output, malformed output, wrong trusted-slot contents, and a successful exact-byte join; keep those seams test-only or narrowly command-injection-oriented and reject unset/invalid overrides in ordinary execution. Extend the rendered Phoenix template with one producer entry point that calls the existing host-owned authority adapter and returns/emits the canonical owner-free backend report only after every callback assertion succeeds per D-02/D-03; callback exceptions, wrong shapes, missing adapters, and failed assertions must terminate nonzero with a stable non-echoing result. Update the shell verifier so `set -euo pipefail` propagates the producer status, captures that producer's stdout byte-for-byte into `PHOENIX_OUT`, requires a non-empty regular file, and passes those unchanged bytes to `PhysicalIphone.parse_report(:backend_authority)` and `join_report_entries/2`. Remove the failure suppression and all script-authored backend assertion outcomes. Keep device and backend commands independent, keep raw stdout ephemeral under `RUN_ROOT`, and ensure logs or test-framework chatter cannot be mistaken for the single JSON envelope. Narrow the simulator serialization invocation with `-only-testing` to the intended contract serialization XCTest so Task 2's required UI tests are neither implicitly exercised nor counted by this advisory gate. Preserve D-04/D-06: no destination, promotion call, support mutation, retained raw output, route input, or physical claim.</action>
  <acceptance_criteria>
    - Producer exit failure, empty output, malformed JSON, incomplete authority assertions, and wrong-slot assertions all make the verifier non-passing before join success.
    - A passing verifier has executed the rendered Phoenix callback producer and parsed its exact stdout bytes from `BACKEND_REPORT_FILE`.
    - No literal all-passed backend JSON or ignored producer status remains in the verifier.
    - The advisory simulator command runs only the serialization contract test and creates no retained evidence.
  </acceptance_criteria>
  <verify><automated>mix test test/crosswake/proof_lane/physical_iphone_report_contract_script_test.exs test/crosswake/proof_lane/template_contract_test.exs --max-failures 1 &amp;&amp; bash script/verify_physical_iphone_report_contract.sh</automated></verify>
  <done>BL-01 is closed: advisory dual-authority verification cannot pass unless an executed Phoenix authority producer supplies the exact valid backend report that is joined.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Make missing study-status accessibility composition non-passing</name>
  <read_first>
    - .planning/phases/162-physical-iphone-adoption-proof/162-REVIEW.md
    - .planning/phases/162-physical-iphone-adoption-proof/162-CONTEXT.md
    - priv/templates/crosswake/proof_lane/ios/CrosswakeProofLaneUITests/ProofLaneUITests.swift.eex
    - priv/templates/crosswake/proof_lane/ios/CrosswakeProofLaneTests/ProofLaneContractTests.swift.eex
    - script/verify_physical_iphone_report_contract.sh
    - test/crosswake/proof_lane/template_contract_test.exs
  </read_first>
  <files>priv/templates/crosswake/proof_lane/ios/CrosswakeProofLaneUITests/ProofLaneUITests.swift.eex, test/crosswake/proof_lane/template_contract_test.exs</files>
  <behavior>
    - "RED: each of the three required study-status XCUITests records an explicit XCTest failure with a stable non-sensitive message when CROSSWAKE_PROOF_LANE_STUDY_HOST_ADAPTER is absent or not exactly 1."
    - "With the adapter signal present, all existing Dynamic Type, recovery action, 44pt target, appearance, Reduce Motion, icon/text, focus-preservation, and announcement-count assertions execute unchanged."
    - "The serialization-only script selects only PhysicalIphoneContractSerializationTests and therefore does not turn intentionally unconfigured UI composition into passing or skipped accessibility evidence."
  </behavior>
  <action>Write the source/render regression first. Replace each bare adapter guard in the three study-status XCUITests with one shared fail-closed admission helper that records an `XCTFail`/failed assertion using a stable generic message when the environment value is absent or wrong, then stops that test before querying UI. Do not use `XCTSkip`, a successful early return, a default adapter, or a synthetic UI success. Preserve the current host launch arguments and every existing accessibility assertion per D-07/D-09. Confirm Task 1's `-only-testing` selection isolates the serialization-only simulator gate rather than weakening the required physical-status target. Do not add new UI states, native alert/confirm, screenshots, retained transcripts, or Android parity.</action>
  <acceptance_criteria>
    - All three study-status tests fail rather than pass or skip when the required adapter signal is absent or invalid.
    - With the signal present, every current accessibility and recovery assertion still runs.
    - The serialization advisory gate does not execute or claim the UI tests.
  </acceptance_criteria>
  <verify><automated>mix test test/crosswake/proof_lane/template_contract_test.exs --max-failures 1 &amp;&amp; bash script/verify_physical_iphone_report_contract.sh</automated></verify>
  <done>WR-01 is closed: required physical study-status accessibility coverage cannot silently disappear as passing XCTest results.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Require a server-approved recovery-route capability</name>
  <read_first>
    - .planning/phases/162-physical-iphone-adoption-proof/162-REVIEW.md
    - .planning/phases/162-physical-iphone-adoption-proof/162-CONTEXT.md
    - .planning/todos/TODO-002-first-b2c-adopter-route-inputs.md
    - examples/phoenix_host/lib/crosswake_example/router.ex
    - examples/phoenix_host/lib/crosswake_example_web/controllers/offline_controller.ex
    - examples/phoenix_host/lib/crosswake_example_web/controllers/offline_html/index.html.heex
    - examples/phoenix_host/priv/static/offline_study.js
    - examples/phoenix_host/e2e/offline_sync.spec.ts
  </read_first>
  <files>examples/phoenix_host/lib/crosswake_example_web/controllers/offline_controller.ex, examples/phoenix_host/lib/crosswake_example_web/controllers/offline_html/index.html.heex, examples/phoenix_host/priv/static/offline_study.js, examples/phoenix_host/e2e/offline_sync.spec.ts</files>
  <behavior>
    - "RED: setting only a syntactically valid same-origin dataset destination never exposes Review saved answers."
    - "Cross-origin, protocol-relative, malformed, unknown-route, non-GET, and unapproved same-origin values never create the action; rejected work remains queued and the needs_attention status stays visible."
    - "An action can render only when Phoenix supplies the closed approved capability and its fixed path; the browser still verifies same-origin/protocol before creating the anchor."
    - "Because TODO-002 has not established an adopter recovery route, the ordinary example-host response supplies the closed unavailable capability and exposes no action."
  </behavior>
  <action>Write the Playwright cases first around the existing rejected-work test. Replace the test's direct dataset-only positive case with negative cases proving that same-origin syntax is not host approval and that queued work remains retained. In the Phoenix controller/template, introduce a closed host-owned recovery-route capability with only `unavailable` or an approved fixed GET route resolved server-side from an explicit route identifier; do not accept or reflect an arbitrary path, URL, query value, client flag, or replay response. Since TODO-002 remains unknown-blocking and the example router has no approved saved-answer recovery route, its normal response must be `unavailable` and render no destination. In `offline_study.js`, require the server-rendered approved capability before performing the existing origin/protocol validation and creating the CTA. Treat missing, contradictory, malformed, or unknown capability fields as unavailable without echoing supplied values. Preserve D-07/D-08 learner copy, retained queue, no automatic retry/deletion, and no generic recovery router. Do not invent an adopter route, create a substitute destination, weaken route inventory validation, or change support truth.</action>
  <acceptance_criteria>
    - Same-origin alone is insufficient; all unapproved or malformed values leave the recovery action absent.
    - The server owns the closed approval decision and the browser consumes only its fixed approved GET destination.
    - The current unknown-blocking route inventory yields no recovery destination while retained work and needs_attention remain observable.
    - No raw denial, scope, account, mutation, route input, or backend detail enters learner UI or logs.
  </acceptance_criteria>
  <verify><automated>cd examples/phoenix_host &amp;&amp; npm test -- --project=chromium e2e/offline_sync.spec.ts --grep "study status retains rejected work" &amp;&amp; MIX_ENV=test mix test test/crosswake_example/router_test.exs --max-failures 1</automated></verify>
  <done>WR-02 is closed: recovery navigation is fail-closed behind Phoenix's explicit approved-route capability, not inferred from URL origin.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| generated Phoenix host callbacks -> advisory backend report | Host-owned backend observations cross into the verifier; a script-authored substitute would spoof authority. |
| generated XCUITest process -> host study-status adapter | Required UI/accessibility assertions depend on real host composition and must not pass when it is absent. |
| Phoenix route approval -> browser recovery action | A host-owned closed capability crosses into untrusted browser state; URL origin alone cannot authorize navigation. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-162-39 | Spoofing | advisory Phoenix report in `verify_physical_iphone_report_contract.sh` | high | mitigate | Task 1 removes script-authored outcomes, executes the rendered producer, captures exact stdout, and tests absent/failing/malformed producers. |
| T-162-40 | Tampering | device/backend report join | high | mitigate | Task 1 parses producer bytes only in their trusted ownership slot and rejects incomplete, overlapping, or altered reports before advisory success. |
| T-162-41 | Repudiation | silently omitted accessibility assertions | medium | mitigate | Task 2 makes missing host composition an explicit XCTest failure and isolates the unrelated serialization-only test target. |
| T-162-42 | Elevation of Privilege | learner recovery link | high | mitigate | Task 3 requires Phoenix's closed approved GET-route capability in addition to browser origin/protocol validation. |
| T-162-43 | Information Disclosure | producer/UI/recovery failures | high | mitigate | All tasks retain only stable closed results and forbid raw callbacks, routes, payloads, accounts, device data, logs, and transcripts from evidence or learner output. |
| T-162-44 | Spoofing | code-only/simulator result presented as physical proof | high | mitigate | The plan leaves preflight, promotion, support truth, and physical evidence destinations unchanged; the verifier remains explicitly advisory and non-promoting. |

ASVS L1 with `security_block_on: high`: every high-severity threat is mitigated by automated negative tests before any eligible physical host is exposed to promotion.
</threat_model>

<source_coverage_audit>
| SOURCE | ID | Feature/Requirement | Plan | Status | Notes |
|---|---|---|---|---|---|
| GOAL | — | Honest physical-iPhone proof with independent authority and bounded recovery | 162-08 + external gate | COVERED | Code review gaps close here; genuine physical proof remains externally blocked. |
| REQ | DEVICE-01 | Verified offline pack/audio | existing 162-01/03/07 + external gate | COVERED | Excluded from this repair; no audio or physical claim changes. |
| REQ | DEVICE-02 | Offline answers, relaunch, exactly-once | 162-08 Task 1 | COVERED | Backend exactly-once report must come from executed host callbacks. |
| REQ | DEVICE-03 | Visible retained recovery | 162-08 Tasks 1-3 | COVERED | Backend recovery remains independent; accessibility and approved recovery routing fail closed. |
| REQ | DEVICE-04 | Logout/account isolation | 162-08 Task 1 | COVERED | Scope-fence success cannot be synthesized by the verifier. |
| REQ | DEVICE-05 | Entry/replay disablement | 162-08 Task 1 | COVERED | Both backend outcomes must be produced by real callbacks. |
| REQ | DEVICE-06 | Redacted dated evidence | 162-08 Task 1 + external gate | COVERED | Exact aggregate producer bytes are ephemeral; no artifact is fabricated. |
| REQ | DEVICE-07 | Narrow support claim | external gate | COVERED | Current verification-required wording remains unchanged. |
| RESEARCH | Dual authority | Phoenix independently proves backend outcomes | 162-08 Task 1 | COVERED | Exact generated producer output replaces literal success. |
| RESEARCH | Accessible recovery | Dynamic Type, focus, announcement, and approved recovery | 162-08 Tasks 2-3 | COVERED | Missing adapter and unapproved route both become non-passing/unavailable. |
| RESEARCH | Closed evidence | Stable low-cardinality non-sensitive outcomes | 162-08 Tasks 1-3 | COVERED | No raw output or supplied values are retained or echoed. |
| CONTEXT | D-01 | Sequential host-owned exit test | 162-08 Tasks 1-2 | COVERED | Existing sequence stays fixed; producer/accessibility proof becomes honest. |
| CONTEXT | D-02, D-03 | Independent Phoenix authority and narrow host boundary | 162-08 Task 1 | COVERED | Script cannot author backend success. |
| CONTEXT | D-04 | Hard preflight and no partial artifact | 162-08 Tasks 1-3 | COVERED | Repairs remain advisory and non-promoting. |
| CONTEXT | D-05, D-06 | Fixed physical evidence and privacy allowlist | 162-08 Task 1 | COVERED | Existing report parser consumes exact ephemeral producer bytes; retained schema does not change. |
| CONTEXT | D-07, D-08, D-09 | Learner status, retained recovery, accessibility | 162-08 Tasks 2-3 | COVERED | Required UI assertions fail closed and recovery requires host approval. |
| REVIEW | BL-01 | Fabricated Phoenix authority success | 162-08 Task 1 | COVERED | Producer failures block; exact stdout is joined. |
| REVIEW | WR-01 | Missing adapter silently passes accessibility tests | 162-08 Task 2 | COVERED | Missing adapter records XCTest failure. |
| REVIEW | WR-02 | Same-origin mistaken for approved recovery route | 162-08 Task 3 | COVERED | Closed Phoenix capability is required. |
| VERIFICATION | External physical artifact | TODO-002, signed host/backend, adapters, physical iPhone | external gate | COVERED | No simulation, artifact, or support promotion is planned. |
</source_coverage_audit>

<verification>
- Backend producer integrity: `mix test test/crosswake/proof_lane/physical_iphone_report_contract_script_test.exs test/crosswake/proof_lane/template_contract_test.exs --max-failures 1` proves producer failures cannot be hidden and template output remains closed.
- End-to-end advisory join: `bash script/verify_physical_iphone_report_contract.sh` executes the selected generated XCTest serialization producer and rendered Phoenix authority producer, then joins their exact report bytes without promotion.
- Accessibility admission: generated source/render tests prove all required status tests fail on missing adapter; the advisory script's `-only-testing` selector excludes those UI tests rather than counting them.
- Recovery routing: `cd examples/phoenix_host && npm test -- --project=chromium e2e/offline_sync.spec.ts --grep "study status retains rejected work"` proves unapproved same-origin and malformed destinations stay absent while work remains queued.
- Focused Elixir regression: `mix test test/crosswake/proof_lane/physical_iphone_preflight_test.exs test/mix/tasks/crosswake.proof_lane.physical_iphone_test.exs test/crosswake/proof_lane/evidence_test.exs test/crosswake/proof_lane/template_contract_test.exs test/crosswake/proof_lane/physical_iphone_report_contract_script_test.exs --max-failures 1`.
- External gate preservation: `mix crosswake.proof_lane.physical_iphone --run --promote --json` must still exit 2 with `PI-PREFLIGHT-INVENTORY`, and the physical evidence destination must remain absent.
- Support truth preservation: `mix crosswake.support_matrix.gen --check` must pass without changing the verification-required physical-iPhone row.
</verification>

<success_criteria>
- BL-01 is closed by an executed generated Phoenix producer whose exact output is required for advisory join success.
- WR-01 is closed because missing required study-status composition produces XCTest failures rather than passing or skipped assertions.
- WR-02 is closed because only a server-approved fixed recovery-route capability can create the learner action.
- Fail-closed, dual-authority, privacy, TODO-002, iOS-only, and narrow-support boundaries remain unchanged.
- No physical evidence or completion claim is created until the real external prerequisites and physical iPhone exist.
</success_criteria>

<output>
Create `.planning/phases/162-physical-iphone-adoption-proof/162-08-SUMMARY.md` when done.
</output>
