---
phase: 162-physical-iphone-adoption-proof
plan: "17"
type: execute
wave: 17
depends_on: [162-16]
files_modified:
  - script/retain_physical_iphone_evidence_transaction.sh
  - test/crosswake/proof_lane/physical_iphone_evidence_transaction_test.exs
  - test/crosswake/proof_lane/evidence_test.exs
  - .planning/phases/162-physical-iphone-adoption-proof/evidence/physical_iphone/proof-lane-evidence.json
  - .planning/phases/162-physical-iphone-adoption-proof/evidence/physical_iphone/.complete
  - lib/crosswake/support_matrix/renderer.ex
  - test/crosswake/support_matrix/renderer_test.exs
  - guides/support_matrix.md
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - .planning/STATE.md
  - .planning/phases/162-physical-iphone-adoption-proof/162-VALIDATION.md
  - .planning/phases/162-physical-iphone-adoption-proof/162-16-SUMMARY.md
autonomous: true
gap_closure: true
requirements: [DEVICE-01, DEVICE-02, DEVICE-03, DEVICE-04, DEVICE-05, DEVICE-06, DEVICE-07]
must_haves:
  truths:
    - "Per D-04 through D-06, recovery stops unless the untracked retained output is exactly the canonical evidence JSON plus completion marker, is canonical/schema-valid/privacy-safe, and pins code_ref to ledger parent e649e6ed."
    - "The transaction wrapper regression proves a passed run JSON may omit an optional nested evidence object; the wrapper validates commit_ref from the promoted evidence JSON and invokes no device, readiness, transaction, Xcode, promotion, or simulator command during recovery."
    - "The physical run's code authority remains e649e6ed and ledger d59c9182 remains immutable; the Plan 17 docs commit plus wrapper-only RED/GREEN commits are explicitly bounded between that ledger and the distinct evidence-only commit, with no amend, reset, rebase, or published-history rewrite."
    - "Per D-01/D-02, the existing production path demonstrates that the two retained leaves can be published only after same-run approved-source validation and internal Evidence.check/2; recovery never reconstructs the cleaned private capture or substitutes source-less Evidence.check/1."
    - "DEVICE/support truth is restored only after exact evidence topology, marker, allowlist, privacy, one-ledger/one-evidence-subject, deterministic contract, and current support-parity gates all pass."
    - "Per D-03 and D-07 through D-09, recovery preserves the one-flow/one-runtime claim, host authority, learner recovery semantics, TODO-002, Android freeze, and every background/generic-storage/generic-sync/multiple-island/simulator/every-iPhone non-claim."
  artifacts:
    - path: "script/retain_physical_iphone_evidence_transaction.sh"
      provides: "Correct post-promotion validation that reads commit_ref from the promoted evidence record, not optional run JSON."
    - path: "test/crosswake/proof_lane/physical_iphone_evidence_transaction_test.exs"
      provides: "RED/GREEN isolated-Git regression for the exact wrapper failure and bounded recovery topology."
    - path: "test/crosswake/proof_lane/evidence_test.exs"
      provides: "Executable proof that source validation and internal Evidence.check/2 bracket publication of physical evidence."
    - path: ".planning/phases/162-physical-iphone-adoption-proof/evidence/physical_iphone/proof-lane-evidence.json"
      provides: "Fresh canonical redacted record whose code_ref remains the exact physically executed ledger parent."
    - path: ".planning/phases/162-physical-iphone-adoption-proof/evidence/physical_iphone/.complete"
      provides: "Exact lowercase SHA-256 completion marker for the retained JSON bytes."
    - path: ".planning/phases/162-physical-iphone-adoption-proof/162-VALIDATION.md"
      provides: "Aggregate-only recovery ledger separating physical-run authority, wrapper repair, retained evidence, deterministic tests, and independent verification."
  key_links:
    - from: "d59c9182"
      to: "e649e6ed"
      via: "the ledger's sole parent is the immutable code commit physically executed by Plan 16"
      pattern: "chore\\(162-16\\): consume corrected-provenance run"
    - from: "passed run JSON"
      to: "promoted proof-lane-evidence.json"
      via: "wrapper accepts the closed run outcome/assertions, then obtains commit_ref only from the promoted canonical artifact"
      pattern: "proof-lane-evidence\\.json|commit_ref"
    - from: "Evidence.promote/3"
      to: "NativePromotion.publish/2"
      via: "approved canonical sources are verified before publication and Evidence.check/2 rechecks the published directory before success"
      pattern: "verify_sources|NativePromotion\\.publish|check\\(destination, sources\\)"
    - from: "evidence-only commit"
      to: "support/requirements/roadmap/state truth"
      via: "exact topology and deterministic reconciliation gates, followed by a fresh independent Phase 162 verifier"
      pattern: "device evidence|Complete|passed"
---

<objective>
Recover the already-produced corrected-provenance evidence after Plan 16's wrapper-only post-promotion failure, without another physical attempt or history rewrite.

Purpose: Preserve the physical run's exact code provenance while repairing the retention validator, committing only the fresh allowlisted pair, and restoring only the narrow truth that the evidence earns.
Output: RED/GREEN wrapper regression and fix, one bounded evidence-only commit after the immutable ledger through an explicitly validated wrapper-fix chain, aggregate-only reconciliation, and independent-verifier handoff.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/ADR-FIRST-B2C-ADOPTER.md
@.planning/FIRST-B2C-ADOPTER-ADOPTION-BRIEF.md
@.planning/FIRST-B2C-ADOPTER-ROUTE-POLICY-MAP.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/162-physical-iphone-adoption-proof/162-CONTEXT.md
@.planning/phases/162-physical-iphone-adoption-proof/162-RESEARCH.md
@.planning/phases/162-physical-iphone-adoption-proof/162-VERIFICATION.md
@.planning/phases/162-physical-iphone-adoption-proof/162-REVIEW.md
@.planning/phases/162-physical-iphone-adoption-proof/162-REVIEW-FIX.md
@.planning/phases/162-physical-iphone-adoption-proof/162-16-PLAN.md
@script/retain_physical_iphone_evidence_transaction.sh
@test/crosswake/proof_lane/physical_iphone_evidence_transaction_test.exs
@lib/mix/tasks/crosswake.proof_lane.physical_iphone.ex
@lib/crosswake/proof_lane/evidence.ex
@lib/crosswake/proof_lane/native_promotion.ex
@lib/crosswake/support_matrix/renderer.ex

<interfaces>
- Immutable recovery base: `d59c9182` has subject `chore(162-16): consume corrected-provenance run`, has sole parent `e649e6ed`, and changes no tree bytes. The fresh record's `commit_ref` must resolve exactly to `e649e6ed`; this is the code that the completed physical transaction executed.
- Current retained output: `.planning/phases/162-physical-iphone-adoption-proof/evidence/physical_iphone/` is untracked and contains exactly `.complete` plus `proof-lane-evidence.json`; the marker matches the JSON and `Evidence.scan_stage/1` performs exact-leaf enumeration, marker verification, canonical reparse, closed-schema validation, and privacy scanning without needing the cleaned canonical-source capture.
- `Evidence.promote/3` verifies same-run approved canonical sources before `NativePromotion.publish/2` and calls source-bound `Evidence.check/2` after publication. `Mix.Tasks.Crosswake.ProofLane.PhysicalIphone.promote_if_requested/2` requires `Evidence.promote/2` and another source-bound `Evidence.check/2` before returning a passed promotion assertion.
- Wrapper failure: the canonical command returned a closed passed result and promoted the evidence, but `run.json` does not promise `.evidence.commit_ref`; the shell wrapper failed only because it read that optional nested path instead of the canonical promoted file.
- Safe topology: retain `d59c9182` and `e649e6ed` unchanged. The committed recovery plan `docs(162): plan retained-evidence recovery` is the first child of the ledger and changes only `162-17-PLAN.md` plus the scoped Phase 162 roadmap entry. Place only the wrapper RED/GREEN commits after that planning commit, then make the evidence-only commit's parent the GREEN fix. Validate every bounded commit and ensure the artifact still pins `e649e6ed`. This is authoritative because planning and wrapper validation changed after the physical run while physical execution provenance did not.
- Preserved user-local paths remain outside plan ownership: `examples/phoenix_host/native/ios/CrosswakeProofLane.xcodeproj/project.pbxproj`, its `project.xcworkspace/`, and `.planning/debug/`. They must remain unstaged, uncleaned, and uninspected for sensitive values.
</interfaces>
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Reproduce and repair the wrapper-only post-promotion failure</name>
  <precondition>HEAD has exact subject `docs(162): plan retained-evidence recovery`, has sole parent d59c9182, and changes only `162-17-PLAN.md` plus the scoped Phase 162 roadmap entry; d59c9182's sole parent is e649e6ed; the exact fresh two-leaf evidence directory and only the preserved user-local dirty paths are present; the index is empty; and no native/device/readiness/transaction/promotion process is running.</precondition>
  <reversibility rating="costly">The wrapper is durable one-shot transaction authority; an incorrect recovery rule could either discard valid physical proof or admit unrelated bytes.</reversibility>
  <read_first>
    - script/retain_physical_iphone_evidence_transaction.sh
    - test/crosswake/proof_lane/physical_iphone_evidence_transaction_test.exs
    - lib/mix/tasks/crosswake.proof_lane.physical_iphone.ex
    - lib/crosswake/proof_lane/evidence.ex
    - test/crosswake/proof_lane/evidence_test.exs
    - .planning/phases/162-physical-iphone-adoption-proof/162-VERIFICATION.md
    - .planning/phases/162-physical-iphone-adoption-proof/162-REVIEW-FIX.md
  </read_first>
  <files>script/retain_physical_iphone_evidence_transaction.sh, test/crosswake/proof_lane/physical_iphone_evidence_transaction_test.exs, test/crosswake/proof_lane/evidence_test.exs</files>
  <behavior>
    - RED: In an isolated temporary Git fixture, a fake canonical runner returns a closed passed result with the complete passed assertion count and deliberately omits any nested evidence object, while writing a canonical promoted evidence pair whose commit_ref equals the fixture ledger parent; the current wrapper fails after promotion.
    - GREEN: The wrapper validates outcome/assertion completeness from run JSON, then validates commit_ref from `proof-lane-evidence.json`; the isolated transaction completes with the expected ledger/evidence topology and exact two-file evidence commit.
    - Negative: A missing, extra, non-regular, symlinked, marker-mismatched, noncanonical, privacy-invalid, wrong-code-ref, or schema/closed-assertion-invalid artifact blocks without staging or committing evidence.
    - Ordering: Production evidence regressions prove approved same-run sources are checked before destination publication and source-bound Evidence.check/2 succeeds before the Mix task reports promotion success; no wrapper-only assertion substitutes for this production invariant.
  </behavior>
  <action>Create the failing isolated-Git regression first and record its RED commit. The fixture must exercise only the existing test-guard fake commands, use neutral generated values, and never access the repository's retained artifact or user-local paths. Fix the shell wrapper so it does not require commit provenance inside optional run JSON: keep the closed passed outcome and complete assertion checks there, then read and validate the Git reference only from the promoted evidence JSON after exact-leaf, regular-file, marker, canonical-schema, closed-assertion, and privacy checks. Preserve the existing production command literals and one-shot semantics.

Add or strengthen the production ordering regression around `Evidence.promote/3` and `promote_if_requested/2`: invalid or mismatched same-run approved sources leave the destination absent, valid sources publish the pair, and the task cannot return passed unless internal source-bound Evidence.check/2 has completed. Do not weaken atomic no-replace publication, add a source-less authority path, expose the private capture, or introduce a recovery invocation into the physical transaction script. Record the GREEN commit separately. Neither RED nor GREEN may stage the untracked evidence pair or any preserved user-local path.</action>
  <acceptance_criteria>
    - The isolated regression fails for the exact absent nested run-JSON evidence field before the fix and passes after commit_ref is sourced from the promoted evidence JSON.
    - Malformed artifact, provenance, marker, schema, assertion, privacy, or topology cases block before an evidence commit.
    - Tests prove production publication remains gated by same-run approved-source validation and internal Evidence.check/2.
    - RED and GREEN commits descend directly from the exact Plan 17 planning commit, touch only the three declared files, and leave the real evidence pair plus user-local paths unstaged and unchanged.
    - No device, readiness, transaction, promotion, Xcode, XCTest/XCUITest, or simulator command runs.
  </acceptance_criteria>
  <verify><automated>set -euo pipefail; test "$(git rev-parse d59c9182^)" = e649e6ed593542b9e685f2b303d18e5cd133d867; mix test test/crosswake/proof_lane/physical_iphone_evidence_transaction_test.exs test/crosswake/proof_lane/evidence_test.exs test/mix/tasks/crosswake.proof_lane.physical_iphone_test.exs --max-failures 1; git diff --cached --quiet</automated></verify>
  <done>The wrapper bug has a committed RED/GREEN regression and fix, production same-run check ordering is executable, and the already-produced evidence remains untouched and uncommitted.</done>
</task>

<task type="auto">
  <name>Task 2: Admit and commit exactly the retained evidence pair under bounded topology</name>
  <precondition>The exact Plan 17 planning commit followed by Task 1's RED/GREEN commits are the only commits after immutable ledger d59c9182, their path sets match their declared planning or wrapper/test ownership, the artifact still pins e649e6ed, and no source capture or private run directory remains.</precondition>
  <reversibility rating="costly">The evidence-only commit becomes durable physical-proof authority; correction requires explicit fail-closed withdrawal, not history rewriting.</reversibility>
  <read_first>
    - .planning/phases/162-physical-iphone-adoption-proof/162-16-PLAN.md
    - script/retain_physical_iphone_evidence_transaction.sh
    - lib/crosswake/proof_lane/evidence.ex
    - .planning/phases/162-physical-iphone-adoption-proof/evidence/physical_iphone/proof-lane-evidence.json
    - .planning/phases/162-physical-iphone-adoption-proof/evidence/physical_iphone/.complete
  </read_first>
  <files>.planning/phases/162-physical-iphone-adoption-proof/evidence/physical_iphone/proof-lane-evidence.json, .planning/phases/162-physical-iphone-adoption-proof/evidence/physical_iphone/.complete</files>
  <action>Perform a read-only recovery admission without invoking the transaction wrapper or any native/device path. Fail closed unless: ledger d59c9182 exists once by exact subject, is an empty-tree commit with sole parent exactly e649e6ed; the fresh directory contains exactly the two allowlisted regular nonsymlink leaves; `.complete` is exactly the lowercase SHA-256 of the canonical JSON bytes; `Evidence.scan_stage/1` accepts exact enumeration, canonical encoding, schema, closed physical fields/assertions, and privacy; the record's code_ref is exactly `git-e649e6ed593542b9e685f2b303d18e5cd133d867`; the evidence subject does not yet exist; and the index/worktree contains nothing except Task 1's committed state, the two evidence leaves, and the preserved pbxproj/workspace/debug paths.

Validate the safe non-rewriting topology before staging: the first commit after d59c9182 must have exact subject `docs(162): plan retained-evidence recovery` and change only this plan plus the scoped Phase 162 roadmap entry; every later commit through Task 1 GREEN must be one of Task 1's exact RED/GREEN subjects and may modify only the three declared wrapper/test files. This bounded interposition is permitted because it records and repairs only post-run retention validation; it does not claim that planning or wrapper-fix code ran on the phone, and it does not change the record's physical code authority. Do not amend, reset, rebase, cherry-pick, replace, or force-update any existing commit.

If and only if all checks pass, stage the two explicit evidence pathspecs, prove the index contains those two paths and no others, and create one distinct evidence-only commit with subject `feat(162-16): retain corrected physical iPhone evidence`. Then prove its immediate parent is Task 1 GREEN, its diff contains exactly the two leaves, both committed blobs equal the admitted working bytes, its commit_ref still names e649e6ed, the ledger and evidence subjects each occur exactly once, and the preserved user-local paths remain unstaged and unchanged. Never print artifact field values or inspect private user-local file contents.</action>
  <acceptance_criteria>
    - Ledger d59c9182 and code commit e649e6ed remain byte-for-byte immutable and retain their exact parent relationship.
    - Exact allowlist, regular-file, marker, canonical-schema, closed-assertion, privacy, code_ref, subject-count, and clean-index/worktree gates pass before staging.
    - The evidence-only commit immediately follows the bounded Plan 17 docs plus Task 1 RED/GREEN chain and changes exactly the two evidence leaves.
    - The artifact code_ref stays e649e6ed; wrapper commits are explicitly non-physical retention logic and do not alter physical provenance.
    - No private capture is reconstructed and no device/readiness/transaction/promotion/native/simulator action occurs.
  </acceptance_criteria>
  <verify><automated>set -euo pipefail; E=.planning/phases/162-physical-iphone-adoption-proof/evidence/physical_iphone; test "$(git rev-parse d59c9182^)" = e649e6ed593542b9e685f2b303d18e5cd133d867; test "$(git show -s --format=%s d59c9182)" = 'chore(162-16): consume corrected-provenance run'; test "$(git diff-tree --no-commit-id --name-only -r d59c9182 | wc -l | tr -d ' ')" = 0; test "$(git log --all --format=%s | awk '$0=="chore(162-16): consume corrected-provenance run"{n++} END{print n+0}')" = 1; test "$(git log --all --format=%s | awk '$0=="feat(162-16): retain corrected physical iPhone evidence"{n++} END{print n+0}')" = 1; test "$(git diff-tree --no-commit-id --name-only -r HEAD | sort)" = "$(printf '%s\n%s' '.planning/phases/162-physical-iphone-adoption-proof/evidence/physical_iphone/.complete' '.planning/phases/162-physical-iphone-adoption-proof/evidence/physical_iphone/proof-lane-evidence.json')"; test "$(jq -er '.commit_ref | sub("^git-"; "")' "$E/proof-lane-evidence.json")" = e649e6ed593542b9e685f2b303d18e5cd133d867; test "$(cat "$E/.complete")" = "$(shasum -a 256 "$E/proof-lane-evidence.json" | cut -d' ' -f1)"; mix run -e 'case Crosswake.ProofLane.Evidence.scan_stage(".planning/phases/162-physical-iphone-adoption-proof/evidence/physical_iphone") do :ok -> :ok; _ -> System.halt(2) end'; git diff --cached --quiet</automated></verify>
  <done>The fresh corrected-provenance pair is retained in one exact evidence-only commit through a validated, non-rewriting wrapper-fix topology.</done>
</task>

<task type="auto">
  <name>Task 3: Reconcile narrow support and hand authority to an independent verifier</name>
  <precondition>The unique evidence-only commit passes Task 2's artifact, topology, code_ref, marker, privacy, and subject-count gates; e649e6ed remains the physical code authority; and all preserved user-local paths remain unstaged.</precondition>
  <reversibility rating="costly">Public support and milestone completion are durable claims; a non-passing independent verdict requires immediate scoped withdrawal.</reversibility>
  <read_first>
    - .planning/phases/162-physical-iphone-adoption-proof/162-VERIFICATION.md
    - .planning/phases/162-physical-iphone-adoption-proof/162-15-SUMMARY.md
    - .planning/phases/162-physical-iphone-adoption-proof/162-16-PLAN.md
    - .planning/phases/162-physical-iphone-adoption-proof/evidence/physical_iphone/proof-lane-evidence.json
    - lib/crosswake/support_matrix/renderer.ex
    - test/crosswake/support_matrix/renderer_test.exs
    - guides/support_matrix.md
    - .planning/REQUIREMENTS.md
    - .planning/ROADMAP.md
    - .planning/STATE.md
    - .planning/phases/162-physical-iphone-adoption-proof/162-VALIDATION.md
  </read_first>
  <files>lib/crosswake/support_matrix/renderer.ex, test/crosswake/support_matrix/renderer_test.exs, guides/support_matrix.md, .planning/REQUIREMENTS.md, .planning/ROADMAP.md, .planning/STATE.md, .planning/phases/162-physical-iphone-adoption-proof/162-VALIDATION.md, .planning/phases/162-physical-iphone-adoption-proof/162-16-SUMMARY.md</files>
  <action>Run only deterministic current-tree tests for transaction recovery, evidence/template/parser/join contracts, provenance cleanup, Phoenix authority/recovery, renderer, and guide parity. Re-run Task 2's read-only artifact/topology checks. Do not invoke readiness, the transaction wrapper, the canonical physical command, Xcode/xcodebuild, XCTest/XCUITest, promotion, device tooling, or simulator/advisory paths.

If and only if every gate passes, restore the existing narrow physical-study row to `device evidence`, mark DEVICE-01 through DEVICE-07 complete in definitions and traceability, update the Phase 162 roadmap/state through Plan 17 as internally complete pending independent verification, and replace `162-VALIDATION.md` with aggregate-only recovery results. Create `162-16-SUMMARY.md` as an honest partial-execution record: withdrawal and corrected physical run completed, ledger d59c9182 was committed, wrapper failed after promotion, private capture was cleaned, and Plan 17 recovered the retained pair without a second run. Record only commit hashes, aggregate counts, stable IDs, closed outcomes, and topology relationships. Do not retain or print artifact values, raw output, canonical bytes, payloads, nonce/mutation/scope/account data, endpoints, signing/device identifiers, credentials, media, transcripts, logs, or private paths.

Preserve TODO-002 and every scope exclusion. Leave `162-VERIFICATION.md` untouched during execution. After the Plan 17 summary commit, dispatch a fresh independent Phase 162 verifier against the committed recovery topology and current truth. If the verdict is not `passed`, treat that report as authority and immediately re-withdraw only the support/completion truth; never authorize another physical attempt.</action>
  <acceptance_criteria>
    - Deterministic transaction/evidence, production parser/join, Phoenix authority/recovery, renderer, and guide gates pass without native or device execution.
    - Support and DEVICE truth derive from the committed fresh record and remain limited to one first-adopter flow on one recorded iOS runtime line.
    - Plan 16's partial outcome and Plan 17's recovery are summarized without private values or false claims that wrapper code ran physically.
    - REQUIREMENTS, ROADMAP, STATE, VALIDATION, renderer, and generated guide agree and retain all explicit non-claims.
    - The independent verifier is final authority; a non-passing result triggers fail-closed truth withdrawal and never another device run.
  </acceptance_criteria>
  <verify><automated>set -euo pipefail; mix test test/crosswake/proof_lane/physical_iphone_evidence_transaction_test.exs test/crosswake/proof_lane/template_contract_test.exs test/crosswake/proof_lane/evidence_test.exs test/crosswake/proof_lane/physical_iphone_report_contract_script_test.exs test/mix/tasks/crosswake.proof_lane.physical_iphone_test.exs test/crosswake/support_matrix/renderer_test.exs test/crosswake/support_matrix/support_matrix_test.exs --max-failures 1; (cd examples/phoenix_host &amp;&amp; MIX_ENV=test mix test test/crosswake_example/physical_iphone_proof_host_test.exs test/crosswake_example/local_first/physical_iphone_authority_test.exs test/crosswake_example/router_test.exs --max-failures 1 &amp;&amp; npm test -- --project=chromium e2e/offline_sync.spec.ts --grep "study status retains rejected work"); mix run -e 'alias Crosswake.SupportMatrix; alias Crosswake.SupportMatrix.Renderer; true = File.read!("guides/support_matrix.md") == Renderer.render(SupportMatrix.canonical())'; test "$(rg -c '^- \[x\] \*\*DEVICE-0[1-7]:' .planning/REQUIREMENTS.md)" = 7; test "$(rg -c '^\| DEVICE-0[1-7] \| Phase 162 \| Complete \|' .planning/REQUIREMENTS.md)" = 7; rg -q '^\| physical-iPhone offline study \| device evidence \|' guides/support_matrix.md; test -f .planning/phases/162-physical-iphone-adoption-proof/162-16-SUMMARY.md; git diff --cached --quiet</automated></verify>
  <done>The recovered authority is internally reconciled, the partial Plan 16 history is documented honestly, and a fresh independent verifier is the only remaining completion authority.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| completed physical run -> optional run JSON | Closed run outcome is authoritative for execution success, but optional nested presentation fields cannot invalidate already-promoted canonical evidence. |
| same-run canonical sources -> retained evidence | Production promotion must validate the private same-run source before and after publication; recovery may validate the retained canonical artifact but cannot recreate the cleaned source. |
| immutable code e649e6ed -> ledger d59c9182 -> recovery plan -> wrapper fix -> evidence commit | Physical code authority and later planning/retention-validator changes must remain distinct and topologically explicit. |
| untracked pair -> Git history -> public truth | Exact leaves, marker, schema, privacy, provenance, clean index, and subject counts gate the evidence-only commit and support restoration. |
| user-local signing/workspace/debug state -> recovery commands | Preserved paths must remain untouched, unstaged, and outside artifact inspection. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-162-96 | Tampering | post-promotion wrapper | high | mitigate | Task 1 reads commit_ref only from the canonical promoted artifact and negative-tests missing/wrong artifact provenance. |
| T-162-97 | Repudiation | physical attempt count | high | mitigate | All tasks prohibit physical/readiness/transaction/native commands; the unique ledger remains the durable record of the sole corrected attempt. |
| T-162-98 | Tampering | commit topology | high | mitigate | Task 2 preserves e649e6ed/d59c9182, bounds the exact planning and wrapper-only commits, forbids history rewriting, and requires an exact evidence-only descendant. |
| T-162-99 | Information Disclosure | retained artifact and cleaned capture | high | mitigate | Evidence.scan_stage/1, exact allowlist, aggregate-only summaries, non-echoing checks, and no capture reconstruction prevent sensitive egress. |
| T-162-100 | Spoofing | source-bound admission | high | mitigate | Task 1 proves production verify-sources -> publish -> Evidence.check/2 ordering; recovery never treats Evidence.check/1 or artifact structure alone as same-run authority. |
| T-162-101 | Elevation of Privilege | support restoration | high | mitigate | Task 3 requires committed evidence, deterministic authority gates, narrow renderer parity, and a fresh independent verifier. |
| T-162-102 | Tampering | user-local setup | high | mitigate | Every task allowlists and preserves pbxproj/workspace/debug state outside staging and content inspection. |
| T-162-103 | Spoofing | simulator/advisory substitution | high | mitigate | No task invokes or accepts simulator/advisory output; only the already-completed corrected physical transaction can underwrite the record. |
</threat_model>

<source_coverage_audit>
| SOURCE | ID | Feature/Requirement | Plan | Status | Notes |
|---|---|---|---|---|---|
| GOAL | — | Current-provenance physical-iPhone proof with narrow support truth | 162-17 | COVERED | Recover the completed run's exact retained record without another attempt. |
| REQ | DEVICE-01 | Verified pack and offline audio | 162-17 Tasks 2-3 | COVERED | Existing physical assertion remains bound to e649e6ed. |
| REQ | DEVICE-02 | Selected/free-form persistence, relaunch, exactly-once drain | 162-17 Tasks 2-3 | COVERED | Corrected physical record is retained and reconciled. |
| REQ | DEVICE-03 | Visible retained rejection/conflict | 162-17 Tasks 2-3 | COVERED | Physical/backend record plus deterministic recovery gates. |
| REQ | DEVICE-04 | Logout/account-switch fencing | 162-17 Tasks 2-3 | COVERED | Retained physical/backend authority plus current deterministic checks. |
| REQ | DEVICE-05 | Entry/replay disablement with retained work | 162-17 Tasks 2-3 | COVERED | Retained record and Phoenix authority tests remain required. |
| REQ | DEVICE-06 | Dated redacted source-bound record | 162-17 Tasks 1-2 | COVERED | Exact allowlist, marker, privacy, code_ref, production source-bound ordering, and evidence-only commit. |
| REQ | DEVICE-07 | One-flow/one-runtime support claim | 162-17 Task 3 | COVERED | Restored only after committed recovery gates. |
| RESEARCH | Sequential dual authority | Preserve the already-completed signed-device/Phoenix run | 162-17 Tasks 1-3 | COVERED | No second producer or device attempt. |
| RESEARCH | Closed evidence publication | Canonical, privacy-safe, atomic, source-bound retained proof | 162-17 Tasks 1-2 | COVERED | Production ordering plus read-only recovery admission. |
| RESEARCH | Narrow support boundary | Physical evidence with explicit non-claims | 162-17 Task 3 | COVERED | Renderer and planning truth remain scoped. |
| CONTEXT | D-01 | Sequential uninterrupted host-owned exit test | 162-17 Tasks 1-2 | COVERED | Completed Plan 16 run is retained; never repeated. |
| CONTEXT | D-02 | Device-local versus Phoenix authority | 162-17 Tasks 2-3 | COVERED | Existing joined record is preserved without producer substitution. |
| CONTEXT | D-03 | Host-owned domain/storage boundary | 162-17 Task 3 | COVERED | No runtime or storage breadth. |
| CONTEXT | D-04 | All-ready fail-closed preflight | 162-17 Tasks 1-3 | COVERED | Recovery admits only the already-produced output and cannot start a run. |
| CONTEXT | D-05 | Fixed physical vocabulary and no-replace promotion | 162-17 Tasks 1-2 | COVERED | Canonical schema and production publication invariants are executable. |
| CONTEXT | D-06 | Strict evidence privacy | 162-17 Tasks 1-3 | COVERED | Exact artifact scan and aggregate-only durable metadata. |
| CONTEXT | D-07 | Honest study status semantics | 162-17 Task 3 | COVERED | Existing deterministic recovery coverage reruns. |
| CONTEXT | D-08 | Retained recovery/fencing/disablement | 162-17 Task 3 | COVERED | Phoenix and browser authority checks rerun without physical execution. |
| CONTEXT | D-09 | Accessible in-flow presentation | 162-17 Task 3 | COVERED | No UI changes or native alert breadth; existing truth is preserved. |
</source_coverage_audit>

<verification>
- Task 1 is the tracer: an isolated RED/GREEN fixture reproduces the wrapper failure and proves the corrected post-promotion path without touching the real artifact or invoking a device.
- Task 2 is read-only until all evidence/topology/worktree gates pass, then stages and commits exactly two explicit leaves.
- Task 3 runs deterministic gates only, reconciles the narrow claim, and leaves final authority to a fresh independent verifier.
- At every task boundary the index is empty and preserved user-local pbxproj/workspace/debug paths remain outside plan ownership.
</verification>

<success_criteria>
- The exact two fresh leaves are retained without another physical, readiness, transaction, promotion, Xcode, XCTest/XCUITest, or simulator invocation.
- Evidence code_ref is exactly immutable ledger parent e649e6ed; ledger d59c9182 exists exactly once and remains unchanged.
- The exact Plan 17 docs commit and wrapper-only RED/GREEN commits are the sole bounded interposition before an exact evidence-only commit; no shared history is rewritten.
- Production tests prove approved-source validation and internal Evidence.check/2 gate file publication and task success.
- Marker, schema, closed assertions, allowlist, privacy, commit contents, subject counts, support parity, requirements/state/roadmap/validation, and summaries all pass.
- A fresh independent verifier either passes Phase 162 or triggers immediate narrow truth withdrawal; it never authorizes another physical attempt.
</success_criteria>

<output>
Create `.planning/phases/162-physical-iphone-adoption-proof/162-17-SUMMARY.md` when done, then dispatch the independent Phase 162 verifier. Do not authorize another physical attempt under any verifier outcome.
</output>
