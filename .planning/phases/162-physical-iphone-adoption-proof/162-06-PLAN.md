---
phase: 162-physical-iphone-adoption-proof
plan: "06"
type: execute
wave: 6
depends_on: [162-05]
files_modified:
  - lib/crosswake/proof_lane/physical_iphone_contract.ex
  - lib/crosswake/proof_lane/physical_iphone_host.ex
  - lib/mix/tasks/crosswake.proof_lane.physical_iphone.ex
  - lib/crosswake/proof_lane/evidence.ex
  - script/verify_physical_iphone_report_contract.sh
  - priv/templates/crosswake/proof_lane/ios/ProofLaneDriver.swift.eex
  - priv/templates/crosswake/proof_lane/ios/CrosswakeProofLaneTests/ProofLaneContractTests.swift.eex
  - priv/templates/crosswake/proof_lane/test/crosswake_proof_lane_test.exs.eex
  - test/mix/tasks/crosswake.proof_lane.physical_iphone_test.exs
  - test/crosswake/proof_lane/evidence_test.exs
  - test/crosswake/proof_lane/template_contract_test.exs
  - examples/phoenix_host/test/crosswake_example/local_first/physical_iphone_authority_test.exs
autonomous: true
gap_closure: true
requirements: [DEVICE-01, DEVICE-02, DEVICE-03, DEVICE-04, DEVICE-05, DEVICE-06, DEVICE-07]
must_haves:
  truths:
    - "Per D-01 through D-06, an eligible host can execute one closed --run --promote --json path from host-owned configuration through preflight, canonical dual-authority report parsing, complete join, Evidence.promote/3, Evidence.check/2, and no-replace publication."
    - "The Swift, Phoenix, and Elixir seams share one exact serialized report envelope; producer role is selected by the trusted callback slot and every assertion owner is derived from PhysicalIphoneContract, never accepted from report bytes."
    - "PI-REDACTED-PROMOTION is absent from Phoenix authority output and can become passed only inside successful canonical evidence build, approved-source verification, publication, and post-publication check."
    - "Per D-05, the actual route-entry gate and replay admission gate are exercised independently, with one allowed while the other is denied in each case and queued work retained."
    - "Per D-07 through D-09, the existing accessible saved/syncing/attention/paused UI remains truthful and unchanged by backend proof authority repairs."
    - "Without a TODO-002-eligible sanitized route, signed host/backend, configured adapters, selected physical iPhone, and real physical reports, promotion remains a stable non-passing external gate and no support claim or evidence artifact is created."
  artifacts:
    - path: "lib/crosswake/proof_lane/physical_iphone_host.ex"
      provides: "Closed host adapter/configuration contract for production preflight, report production, evidence inputs, and fixed destination selection."
    - path: "lib/mix/tasks/crosswake.proof_lane.physical_iphone.ex"
      provides: "Sole preflight-gated run/promote orchestration and exact serialized report parser."
    - path: "priv/templates/crosswake/proof_lane/ios/ProofLaneDriver.swift.eex"
      provides: "Canonical device-local serialized report producer without owner fields."
    - path: "priv/templates/crosswake/proof_lane/test/crosswake_proof_lane_test.exs.eex"
      provides: "Canonical Phoenix serialized report producer without fabricated promotion authority."
    - path: "script/verify_physical_iphone_report_contract.sh"
      provides: "Deterministic generated-host harness that executes the rendered Swift and Phoenix producers and submits their emitted bytes to the public parser/join contract test without promotion authority."
    - path: "examples/phoenix_host/test/crosswake_example/local_first/physical_iphone_authority_test.exs"
      provides: "Independent actual route-entry and replay-disablement authority proof."
  key_links:
    - from: "Application :crosswake physical_iphone_proof_host"
      to: "PhysicalIphonePreflight.check/1 and report callbacks"
      via: "PhysicalIphoneHost validates one module against an exact callback contract before any run"
      pattern: "physical_iphone_proof_host|PhysicalIphoneHost"
    - from: "Swift/Phoenix JSON envelopes"
      to: "PhysicalIphoneContract assertions"
      via: "exact-key decode, schema/device validation, callback-slot subset validation, and contract-derived owners"
      pattern: "schema_version|device_class|assertions"
    - from: "complete device plus backend join"
      to: "Evidence.promote/3 and Evidence.check/2"
      via: "promotion-owned assertion insertion only inside the evidence publication transaction"
      pattern: "PI-REDACTED-PROMOTION|Evidence.promote|Evidence.check"
    - from: "PhysicalIphoneAuthorityFixture.entry_disablement/0"
      to: "Crosswake.Compatibility.RouteGate.evaluate/4"
      via: "real gated RouteEntry/Root/Target fixture with replay authority separately allowed"
      pattern: "RouteGate.evaluate"
---

<objective>
Close the Phase 162 production-wiring and authority gaps without claiming that unavailable physical prerequisites have passed.

Purpose: Make the already-designed physical proof executable and truthfully promotable when the adopter supplies the external route, signed host/backend, and physical iPhone.
Output: One closed host configuration seam, one canonical cross-language report envelope/parser, real evidence-owned promotion, and independent route-entry/replay disablement tests.
</objective>

<artifacts_this_phase_produces>
- A closed host-owned production adapter selected from application configuration; no arbitrary keyword injection or unvalidated module is accepted by `run/1`.
- One canonical JSON report envelope with exact top-level `schema_version`, `device_class`, and `assertions` keys and exact per-entry `id` and `outcome` keys; owner is never serialized.
- A `--run --promote --json` path that publishes only after both producer-specific assertion subsets join completely and pass.
- Evidence-owned `PI-REDACTED-PROMOTION` authority plus post-publication verification; Phoenix cannot manufacture this outcome.
- An example-host proof that invokes the actual `RouteGate.evaluate/4` entry gate independently from `SyncController.sync_events/4` replay admission.
- No physical evidence file and no altered support claim until the external physical command itself succeeds.
</artifacts_this_phase_produces>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/ADR-FIRST-B2C-ADOPTER.md
@.planning/FIRST-B2C-ADOPTER-ADOPTION-BRIEF.md
@.planning/FIRST-B2C-ADOPTER-ROUTE-POLICY-MAP.md
@.planning/phases/162-physical-iphone-adoption-proof/162-CONTEXT.md
@.planning/phases/162-physical-iphone-adoption-proof/162-VERIFICATION.md
@.planning/phases/162-physical-iphone-adoption-proof/162-REVIEW.md
@.planning/phases/162-physical-iphone-adoption-proof/162-05-SUMMARY.md

<interfaces>
- `PhysicalIphonePreflight.check/1` accepts the validated inventory/proof-lane config and ordered zero-arity readiness callbacks; it must remain the first side-effect boundary.
- `PhysicalIphoneContract.assertions/0` is the sole assertion ID/owner source. Add `:evidence_promotion` as the owner of `PI-REDACTED-PROMOTION`; device and backend reports each carry only their exact contract-derived subset.
- Canonical producer bytes have exactly `{schema_version, device_class, assertions}`; assertion entries have exactly `{id, outcome}`. Invoke the parser with the trusted producer slot (`:device_local` or `:backend_authority`) and derive owner after validation.
- `Evidence.promote/3` already performs build, approved-source verification, safe destination validation, final-byte scan, descriptor-safe no-replace publication, and `check/2`; preserve that implementation boundary.
- `Crosswake.Compatibility.RouteGate.evaluate/4` is the actual route-entry gate. `SyncController.sync_events/4` is replay admission; neither is evidence for the other.
</interfaces>
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Execute generated Swift and Phoenix envelopes through the public parser and join</name>
  <reversibility rating="costly">The cross-language report envelope is a generated host contract, so changing it later requires coordinated Swift, Phoenix, Elixir, fixture, and evidence updates.</reversibility>
  <read_first>
    - .planning/phases/162-physical-iphone-adoption-proof/162-VERIFICATION.md
    - .planning/phases/162-physical-iphone-adoption-proof/162-REVIEW.md
    - lib/crosswake/proof_lane/physical_iphone_contract.ex
    - lib/mix/tasks/crosswake.proof_lane.physical_iphone.ex
    - lib/crosswake/proof_lane/generator.ex
    - priv/templates/crosswake/proof_lane/ios/ProofLaneDriver.swift.eex
    - priv/templates/crosswake/proof_lane/ios/CrosswakeProofLaneTests/ProofLaneContractTests.swift.eex
    - priv/templates/crosswake/proof_lane/test/crosswake_proof_lane_test.exs.eex
    - script/verify_phoenix_host_proof_lane.sh
    - script/verify_generated_ios_shell.sh
    - test/mix/tasks/crosswake.proof_lane.physical_iphone_test.exs
    - test/crosswake/proof_lane/template_contract_test.exs
  </read_first>
  <files>lib/crosswake/proof_lane/physical_iphone_contract.ex, lib/mix/tasks/crosswake.proof_lane.physical_iphone.ex, script/verify_physical_iphone_report_contract.sh, priv/templates/crosswake/proof_lane/ios/ProofLaneDriver.swift.eex, priv/templates/crosswake/proof_lane/ios/CrosswakeProofLaneTests/ProofLaneContractTests.swift.eex, priv/templates/crosswake/proof_lane/test/crosswake_proof_lane_test.exs.eex, test/mix/tasks/crosswake.proof_lane.physical_iphone_test.exs, test/crosswake/proof_lane/template_contract_test.exs</files>
  <behavior>
    - "Per D-02/D-03, rendered Swift and Phoenix code each executes as a generated producer and emits the same exact owner-free envelope and exact producer-owned entries; no hand-built Elixir fixture can stand in for either producer."
    - "The public Elixir report parser rejects malformed JSON, string/atom aliases, extra/missing keys, wrong schema/class, duplicate/reordered/wrong-subset IDs, owner input, unknown outcomes, or trailing bytes."
    - "Device and backend callback slots can satisfy only their PhysicalIphoneContract-owned subsets; the join is complete before evidence build/promotion begins."
    - "PI-REDACTED-PROMOTION is absent from both generated producer outputs and the generated-contract harness has no evidence-promotion path."
    - "Simulator execution may validate generated serialization but remains advisory and cannot create, promote, or substitute for physical-iPhone evidence."
  </behavior>
  <action>Start with failing contract tests, then make the public report parser/join accept producer bytes plus the trusted callback slot (`:device_local` or `:backend_authority`) and derive owners only from `PhysicalIphoneContract`; do not expose a path that accepts serialized owners or caller-selected assertion subsets. Change `PhysicalIphoneContract` so `PI-REDACTED-PROMOTION` belongs only to `:evidence_promotion`, while the pre-promotion join requires every device/backend-owned assertion exactly once in contract order. Per D-02/D-03, update the rendered Swift driver/XCTest and Phoenix ExUnit producer so each writes its actual canonical JSON bytes to a harness-owned temporary output when explicitly invoked in contract mode, while normal generated proof behavior stays unchanged. Add `script/verify_physical_iphone_report_contract.sh` as a deterministic, cleanup-safe command: create an invocation-private temporary host, render it through the real proof-lane generator, execute the generated Phoenix producer under its generated Mix project, compile and execute the rendered Swift producer through the generated XCTest target, capture each producer's raw output file without printing it, and run the focused public parser/join ExUnit case with those two file paths. The ExUnit case must read the emitted bytes unchanged, parse each under its trusted slot, join them, and run exact-envelope negative mutations. Fail closed if `swift`, Xcode, generation, either producer, byte capture, parser, or join is unavailable; emit only a stable rule. The harness must neither call `Evidence.promote/3` nor accept a destination, physical success marker, device identifier, or report override. Treat all simulator/XCTest results as contract-advisory and non-promoting; they prove serialization only and cannot satisfy DEVICE-01 through DEVICE-07 physical observations.</action>
  <acceptance_criteria>
    - `bash script/verify_physical_iphone_report_contract.sh` renders a fresh generated host, executes both actual generated producers, and submits their unmodified emitted bytes to the public parser/join test.
    - The actual generated Swift and Phoenix serialized bytes parse and join; every exact-envelope or exact-subset mutation fails closed before any evidence boundary.
    - Neither generated producer output nor its template contains a promotion assertion or an owner field.
    - The harness has no destination or promotion capability, creates no retained artifact, and labels simulator execution advisory/non-promoting.
  </acceptance_criteria>
  <verify><automated>bash script/verify_physical_iphone_report_contract.sh &amp;&amp; mix test test/mix/tasks/crosswake.proof_lane.physical_iphone_test.exs test/crosswake/proof_lane/template_contract_test.exs --max-failures 1 &amp;&amp; mix format --check-formatted lib/crosswake/proof_lane/physical_iphone_contract.ex lib/mix/tasks/crosswake.proof_lane.physical_iphone.ex test/mix/tasks/crosswake.proof_lane.physical_iphone_test.exs test/crosswake/proof_lane/template_contract_test.exs</automated></verify>
  <done>Freshly rendered and executed Swift/Phoenix producers share one enforced owner-free byte contract that the public parser/join consumes, while generated simulator execution remains advisory and incapable of promotion.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Orchestrate closed host reports through checked no-replace promotion</name>
  <read_first>
    - .planning/phases/162-physical-iphone-adoption-proof/162-VERIFICATION.md
    - .planning/phases/162-physical-iphone-adoption-proof/162-REVIEW.md
    - lib/crosswake/proof_lane/physical_iphone_contract.ex
    - lib/crosswake/proof_lane/physical_iphone_preflight.ex
    - lib/mix/tasks/crosswake.proof_lane.physical_iphone.ex
    - lib/crosswake/proof_lane/evidence.ex
    - test/mix/tasks/crosswake.proof_lane.physical_iphone_test.exs
    - test/crosswake/proof_lane/evidence_test.exs
  </read_first>
  <files>lib/crosswake/proof_lane/physical_iphone_host.ex, lib/mix/tasks/crosswake.proof_lane.physical_iphone.ex, lib/crosswake/proof_lane/evidence.ex, test/mix/tasks/crosswake.proof_lane.physical_iphone_test.exs, test/crosswake/proof_lane/evidence_test.exs</files>
  <behavior>
    - "Per D-01/D-04, --run --promote --json is the only promoting mode; --promote without --run, any extra option, missing JSON mode, malformed application configuration, or missing adapter blocks with a stable non-echoing rule before report or filesystem work."
    - "The production entry point loads exactly one configured host adapter module, validates every required callback, obtains preflight inputs/readiness callbacks through that adapter, and does not use synthetic test options as production authority."
    - "The Task 1 public parser consumes both raw producer reports under trusted slots, and the exact complete join succeeds before evidence build or publication begins."
    - "Per D-06, Evidence adds PI-REDACTED-PROMOTION only on the verified publication path and success returns only after Evidence.promote/3 and Evidence.check/2 succeed on the no-replace destination."
    - "A second promotion or concurrent destination winner remains non-passing and preserves the first published bytes."
  </behavior>
  <action>Start with the failing configured-host end-to-end test and require Task 1's generated-contract command to be green before using its parser/join seam. Implement `Crosswake.ProofLane.PhysicalIphoneHost` as the closed behavior/loader for inventory, normalized proof-lane config, ordered readiness callbacks, device/backend serialized report production, low-cardinality evidence metadata, approved canonical sources, and destination; validate the configured module and every callback before preflight. Keep values runtime-only and collapse malformed or raised callbacks to stable PI rules without echoing paths, reports, config, identifiers, or source bytes. Per D-01/D-04, extend the Mix switches so promotion is valid only with run+JSON, keep any injectable runner private to tests, and route production through preflight, the Task 1 exact parser/join, canonical build inputs, `Evidence.promote/3`, and `Evidence.check/2` in that order. Per D-06, let the Evidence boundary alone add and validate `PI-REDACTED-PROMOTION` as part of the canonical artifact; require successful post-publication check before returning passed. Cover duplicate and concurrent no-replace destinations, incomplete/blocked/unavailable reports, callback failures, approved-source failures, and parser rejection with zero destination creation or replacement. Preserve the TODO-002 external gate: repository-default execution must stop at inventory before callbacks or filesystem activity, create no evidence artifact, and change no support claim.</action>
  <acceptance_criteria>
    - A configured synthetic host reaches exactly one real no-replace publication through the same public orchestration used by production, and the published directory passes `Evidence.check/2`.
    - Promotion begins only after Task 1's exact generated producer contract and the complete device/backend join pass; a partial, unavailable, blocked, malformed, or duplicate report leaves no destination.
    - PI-REDACTED-PROMOTION enters the canonical artifact only inside successful Evidence publication/check and is never accepted from a report callback.
    - With repository-default absent external configuration, the exact command remains blocked at TODO-002 and no physical evidence artifact or support promotion is created.
  </acceptance_criteria>
  <verify><automated>bash script/verify_physical_iphone_report_contract.sh &amp;&amp; mix test test/mix/tasks/crosswake.proof_lane.physical_iphone_test.exs test/crosswake/proof_lane/physical_iphone_preflight_test.exs test/crosswake/proof_lane/evidence_test.exs --max-failures 1 &amp;&amp; mix format --check-formatted lib/crosswake/proof_lane/physical_iphone_host.ex lib/mix/tasks/crosswake.proof_lane.physical_iphone.ex lib/crosswake/proof_lane/evidence.ex test/mix/tasks/crosswake.proof_lane.physical_iphone_test.exs test/crosswake/proof_lane/evidence_test.exs &amp;&amp; test "$(mix crosswake.proof_lane.physical_iphone --run --promote --json 2&gt;/dev/null; test $? -eq 2)" = '{"outcome":"blocked","rule_id":"PI-PREFLIGHT-INVENTORY"}' &amp;&amp; test ! -e .planning/phases/162-physical-iphone-adoption-proof/evidence/physical_iphone/proof-lane-evidence.json</automated></verify>
  <done>An eligible configured host has one truthful production path from preflight through Task 1's executable dual-authority byte contract to checked no-replace evidence, while TODO-002 remains externally blocked.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Remove fabricated backend promotion and prove entry/replay disablement independently</name>
  <read_first>
    - .planning/phases/162-physical-iphone-adoption-proof/162-CONTEXT.md
    - examples/phoenix_host/test/crosswake_example/local_first/physical_iphone_authority_test.exs
    - examples/phoenix_host/test/crosswake_example/local_first/replay_auth_test.exs
    - examples/phoenix_host/lib/crosswake_example/e2e/replay_authority.ex
    - lib/crosswake/compatibility/route_gate.ex
    - lib/crosswake/manifest/types.ex
    - lib/crosswake/compatibility/target.ex
    - priv/templates/crosswake/proof_lane/test/crosswake_proof_lane_test.exs.eex
    - test/crosswake/proof_lane/template_contract_test.exs
  </read_first>
  <files>examples/phoenix_host/test/crosswake_example/local_first/physical_iphone_authority_test.exs, priv/templates/crosswake/proof_lane/test/crosswake_proof_lane_test.exs.eex, test/crosswake/proof_lane/template_contract_test.exs</files>
  <behavior>
    - "Phoenix authority reports exactly the four backend-owned contract assertions and never reports PI-REDACTED-PROMOTION."
    - "Entry disablement calls RouteGate.evaluate/4 with a real gated manifest route and denied host companion while replay admission is configured to allow."
    - "Replay disablement calls SyncController.sync_events/4 with route entry configured to allow while request-time replay feature authority denies."
    - "Both cases retain queued/persisted work, and mutating either independent gate to allow makes only its corresponding assertion fail."
  </behavior>
  <action>Write the independence regressions first. Remove `PI-REDACTED-PROMOTION` from the generated and example Phoenix authority manifests and return only the exact backend subset derived from `PhysicalIphoneContract`. Replace `entry_disablement/0` with a real `Crosswake.Compatibility.RouteGate.evaluate/4` call using a valid Root/RouteEntry/Target fixture and a host companion whose current gate evaluation denies that route; configure the replay fixture to allow and assert the route decision is deny/stay-put or halt as appropriate while the previously queued aggregate is unchanged. Keep `replay_disablement/0` on `SyncController.sync_events/4`, configure the route-entry companion to allow, deny request-time replay feature authority, and assert the same retained aggregate. Add paired allow/deny mutation tests so identical implementations cannot satisfy both assertion IDs. Keep all fixture values private to the test process and return only stable counts/booleans and owner-free canonical report bytes per D-02/D-03/D-05.</action>
  <acceptance_criteria>
    - Phoenix output contains all and only contract-owned backend authority IDs; redacted promotion appears only after Task 2 evidence publication succeeds.
    - Entry denial is observed from RouteGate.evaluate/4 while replay is allowed, and replay denial is observed from SyncController.sync_events/4 while entry is allowed.
    - Each denial independently retains work and the paired mutation tests fail if the two implementations collapse onto one gate.
    - The generated host contract remains host-owned and emits no payload, scope, account, endpoint, flag, route path, or backend-reason values.
  </acceptance_criteria>
  <verify><automated>mix test test/crosswake/proof_lane/template_contract_test.exs --max-failures 1 &amp;&amp; (cd examples/phoenix_host &amp;&amp; MIX_ENV=test mix test test/crosswake_example/local_first/physical_iphone_authority_test.exs test/crosswake_example/local_first/replay_auth_test.exs test/crosswake_example/local_first/sync_controller_test.exs --max-failures 1) &amp;&amp; mix format --check-formatted examples/phoenix_host/test/crosswake_example/local_first/physical_iphone_authority_test.exs priv/templates/crosswake/proof_lane/test/crosswake_proof_lane_test.exs.eex test/crosswake/proof_lane/template_contract_test.exs</automated></verify>
  <done>Phoenix proves only its real backend authority, and DEVICE-05 has independent executable entry and replay disablement evidence without fabricated promotion.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| host application config -> physical runner | Host modules/callbacks and runtime values are external authority inputs and may be missing, malformed, or sensitive. |
| Swift/Phoenix serialized bytes -> Elixir join | Reports are untrusted bytes and must not choose owners, add fields, or cross assertion authority. |
| complete join -> retained evidence | Promotion authority must not be asserted before build, source verification, no-replace publication, and final check succeed. |
| route entry -> replay endpoint | Entry and replay are separate server-controlled gates; proving one cannot stand in for the other. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-162-27 | Spoofing | producer ownership | high | mitigate | Trusted callback slot plus contract-derived owner subset; serialized owner input and cross-owner IDs reject. |
| T-162-28 | Tampering | report envelope | high | mitigate | Exact recursive keys, canonical schema/class/outcome/ID/order validation, deterministic execution of both freshly rendered producers through the public parser/join, and trailing-byte rejection. |
| T-162-29 | Elevation of Privilege | PI-REDACTED-PROMOTION | high | mitigate | Remove from Phoenix; Evidence alone supplies it after complete join and verifies publication before success. |
| T-162-30 | Information Disclosure | host config/reports/evidence | high | mitigate | Closed adapter, non-echoing stable rules, approved-source scanner, retained allowlist, and no raw output/path/config persistence. |
| T-162-31 | Tampering | evidence destination | high | mitigate | Existing descriptor-safe no-replace publication, collision tests, marker verification, and Evidence.check/2. |
| T-162-32 | Spoofing | entry disablement | high | mitigate | Invoke RouteGate.evaluate/4 separately from replay admission and prove inverse allow/deny cases. |
| T-162-33 | Denial of Service | absent physical prerequisites | medium | accept | Stable blocked preflight is the required state; no retry loop, partial artifact, or support promotion is permitted. |

ASVS L1: all high-severity trust-boundary threats are mitigated with executable negative and positive tests before any physical publication.
</threat_model>

<source_coverage_audit>
| SOURCE | ID | Feature/Requirement | Plan | Status | Notes |
|---|---|---|---|---|---|
| GOAL | — | Dated redacted physical-iPhone ten-step exit artifact | 162-06 + external gate | COVERED | Code path becomes executable; real artifact remains correctly blocked until physical prerequisites arrive. |
| REQ | DEVICE-01 | Verified pack and offline audio | 162-06 + external gate | COVERED | Device report parsing/promotion path repaired; physical observation cannot be synthesized. |
| REQ | DEVICE-02 | Offline answers survive relaunch and replay exactly once | 162-06 + external gate | COVERED | Exact device/backend join plus real physical run prerequisite. |
| REQ | DEVICE-03 | Visible retained rejection/conflict recovery | 162-06 + external gate | COVERED | Backend report authority repaired; existing D-07/D-08 UI regressions retained. |
| REQ | DEVICE-04 | Logout/account-switch isolation | 162-06 + external gate | COVERED | Backend subset remains contract-derived; physical result remains required. |
| REQ | DEVICE-05 | Independent entry/replay disablement without loss | 162-06 | COVERED | Task 3 invokes both actual gates with inverse controls. |
| REQ | DEVICE-06 | Redacted dated allowlist artifact | 162-06 + external gate | COVERED | Task 2 wires real build/promote/check/no-replace; no artifact while blocked. |
| REQ | DEVICE-07 | Narrow one-flow/one-runtime claim | 162-06 + external gate | COVERED | Existing guide remains verification-required until checked physical publication. |
| RESEARCH | Architecture | Closed host-owned sequential driver and dual authority | 162-06 | COVERED | Production config, serialized reports, trusted slots, and complete join. |
| RESEARCH | Evidence | Exact canonical envelope, approved hashes, atomic publication | 162-06 | COVERED | Evidence owns promotion after complete join. |
| RESEARCH | Safety | Physical-only preflight and non-passing missing prerequisites | 162-06 | COVERED | Default command must block and publish nothing. |
| RESEARCH | UX | Four semantic states and accessible contextual status | existing 162-04 + regressions | COVERED | Repair does not alter UI; exhaustive browser command below protects it. |
| CONTEXT | D-01-D-04 | Sequence, authority split, host boundary, hard preflight | 162-06 | COVERED | Task 1 proves the executable dual-producer envelope; Task 2 closes production orchestration and hard preflight. |
| CONTEXT | D-05-D-06 | Physical evidence vocabulary/privacy/promotion | 162-06 | COVERED | Exact envelope, evidence-owned promotion, final check, no-replace. |
| CONTEXT | D-07-D-09 | Retained recovery and accessible UI | existing 162-04 + verification | COVERED | No backend mechanics leak into UI and existing contract remains green. |
| REVIEW | CR-01 | Production run/promote path | 162-06 Task 2 | COVERED | Closed config through Evidence.promote/3 and check/2. |
| REVIEW | CR-02 | Cross-language report mismatch | 162-06 Task 1 | COVERED | One actual serialized envelope; owner derived. |
| REVIEW | CR-03 | Fabricated promotion | 162-06 Tasks 1-3 | COVERED | Generated producers exclude promotion; Evidence alone supplies it after real publication/check. |
| REVIEW | CR-04 | Entry path not exercised | 162-06 Task 3 | COVERED | RouteGate and replay endpoint tested independently. |
</source_coverage_audit>

<verification>
- Focused core gate: `mix test test/crosswake/proof_lane/physical_iphone_preflight_test.exs test/mix/tasks/crosswake.proof_lane.physical_iphone_test.exs test/crosswake/proof_lane/evidence_test.exs test/crosswake/proof_lane/template_contract_test.exs --max-failures 1`.
- Deterministic generated producer gate: `bash script/verify_physical_iphone_report_contract.sh`; it must render a fresh host, execute the actual Swift and Phoenix producers, and pass their emitted bytes unchanged through the public parser/join test. Its simulator/XCTest result is contract-advisory and has no promotion authority.
- Phoenix authority gate: `(cd examples/phoenix_host &amp;&amp; MIX_ENV=test mix test test/crosswake_example/local_first/physical_iphone_authority_test.exs test/crosswake_example/local_first/replay_auth_test.exs test/crosswake_example/local_first/sync_controller_test.exs --max-failures 1)`.
- Generated-host/browser contract: `bash script/verify_phoenix_host_proof_lane.sh &amp;&amp; npm run typecheck:offline-route-proof &amp;&amp; npx playwright test examples/phoenix_host/e2e/offline_sync.spec.ts --config examples/phoenix_host/playwright.config.ts`.
- Broader generated iOS contract: `bash script/verify_generated_ios_shell.sh --proof-lane --reference-pack-adapter`; this remains simulator advisory and cannot promote physical evidence.
- UI/accessibility regression: the offline Playwright command must keep the D-07/D-09 copy, semantic status, focus, announcement, recovery-link, and retained-queue assertions green.
- Privacy and support non-promotion: run the evidence adversarial corpus and `mix crosswake.support_matrix.gen --check`; `guides/support_matrix.md` must remain verification-required/no shipped claim while no physical artifact exists.
- External non-passing gate: `mix crosswake.proof_lane.physical_iphone --run --promote --json` must exit 2 with only `PI-PREFLIGHT-INVENTORY` and create no artifact until TODO-002, the generated lane, signed host/backend, configured fixture/media/authority controls, and selected physical iPhone are real.
- Physical completion command after those prerequisites exist: run the same exact command, require `.outcome == "passed"`, then run `Evidence.check/2` on the fixed destination. This command is not replaceable by unit tests, simulator output, synthetic configuration, or manual approval.
</verification>

<success_criteria>
- All four actionable findings in `162-VERIFICATION.md` and `162-REVIEW.md` have executable positive and negative coverage.
- The production CLI accepts only `--run --promote --json`, loads a closed host adapter, joins actual canonical serialized producer reports completely, and invokes checked no-replace evidence publication.
- Phoenix cannot claim evidence promotion, and entry/replay disablement are independent actual authority paths.
- The absent route/host/device prerequisites remain an explicit non-passing external gate; no physical artifact, support claim, Android work, generic sync/storage, background replay, or broader product surface is fabricated.
</success_criteria>

<output>
Create `.planning/phases/162-physical-iphone-adoption-proof/162-06-SUMMARY.md` when done.
</output>
