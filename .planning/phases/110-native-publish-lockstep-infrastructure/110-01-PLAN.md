---
phase: 110-native-publish-lockstep-infrastructure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - mix.exs
  - release-please-config.json
  - .release-please-manifest.json
  - packages/crosswake-shell-core-android/build.gradle.kts
autonomous: true
requirements: [LOCK-01, PUB-02, PUB-03]
must_haves:
  truths:
    - "mix.exs @version equals the published Hex version (0.1.0), not 0.1.2"
    - "release-please groups Hex + iOS + Android under one linked-versions component group"
    - "The release-please manifest tracks all three packages baselined at 0.1.0"
    - "The Android library declares a complete, signed Maven Central publish config under io.github.sztheory"
    - "The Android publish stops at VALIDATED (automaticRelease=false), never auto-publishing"
  artifacts:
    - path: "mix.exs"
      provides: "Reverted @version with x-release-please-version annotation"
      contains: "@version \"0.1.0\""
    - path: "release-please-config.json"
      provides: "linked-versions plugin + 3 component-keyed packages + release-as pin"
      contains: "linked-versions"
    - path: ".release-please-manifest.json"
      provides: "Three-package manifest baselined at 0.1.0"
      contains: "packages/crosswake-shell-core-android"
    - path: "packages/crosswake-shell-core-android/build.gradle.kts"
      provides: "Vanniktech publish block, complete POM, version annotation"
      contains: "com.vanniktech.maven.publish"
  key_links:
    - from: "release-please-config.json"
      to: "packages/crosswake-shell-core-android/build.gradle.kts"
      via: "extra-files generic updater bumping the x-release-please-version line"
      pattern: "x-release-please-version"
    - from: "build.gradle.kts mavenPublishing"
      to: "io.github.sztheory:crosswake-shell-core-android"
      via: "coordinates() call"
      pattern: "coordinates\\(\"io.github.sztheory\""
---

<objective>
Reconcile the version drift and wire lockstep versioning + Android Maven Central publish config — WITHOUT performing any real publish. This plan establishes the static configuration (release-please manifest mode + linked-versions, reverted mix.exs, complete signed Android POM) that the Wave 2 CI jobs consume. No CI job, no secret, no network call happens here; this is pure config-as-source-of-truth.

Purpose: A single release-please run must be able to advance Hex, the iOS mirror tag, and the Android Maven artifact to identical version numbers (LOCK-01). The Android core must carry a complete, signed, Central-Portal-ready POM (PUB-02 config + PUB-03 POM completeness). The one-time `release-as: "0.1.2"` pin reconciles the documented target version while keeping the manifest baseline at the true published `0.1.0` (D-02/D-04).

Output: Edited `mix.exs`, `release-please-config.json`, `.release-please-manifest.json`, `build.gradle.kts`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/110-native-publish-lockstep-infrastructure/110-CONTEXT.md
@.planning/phases/110-native-publish-lockstep-infrastructure/110-PATTERNS.md
@.planning/research/STACK.md

<interfaces>
<!-- Concrete current-state and target-state for every file is in 110-PATTERNS.md. Executor uses those excerpts directly; no codebase exploration needed. -->

Critical D-level overrides the executor MUST honor (these DIFFER from STACK.md's verbatim examples):
- D-04: manifest `.` stays at `0.1.0` (NOT `0.1.2`). STACK.md shows `0.1.2` — that is OVERRIDDEN.
- D-04: `mix.exs @version` is reverted to `0.1.0` (currently `0.1.2`). STACK.md's annotation example shows `0.1.2` — OVERRIDDEN.
- D-07: Vanniktech `publishToMavenCentral(SonatypeHost.CENTRAL_PORTAL, automaticRelease = false)`. STACK.md shows `automaticRelease = true` — that is OVERRIDDEN; false is load-bearing for D-01.
- D-04: `release-as: "0.1.2"` pin is added to the `.` package ONLY. (Removing it is a Phase-111 task — do NOT remove it here.)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Revert mix.exs version and configure release-please manifest mode + linked-versions</name>
  <files>mix.exs, release-please-config.json, .release-please-manifest.json</files>
  <read_first>
    - mix.exs (lines 1-12 — the @version block, already in context)
    - release-please-config.json (full file — current-state excerpt in 110-PATTERNS.md "release-please-config.json" section)
    - .release-please-manifest.json (full file — current-state excerpt in 110-PATTERNS.md)
    - 110-PATTERNS.md sections "mix.exs", "release-please-config.json", ".release-please-manifest.json" (verbatim target states)
    - .planning/research/STACK.md "release-please-config.json — Updated shape" (note the D-04 overrides above)
  </read_first>
  <action>
    Edit mix.exs line 4: change `@version "0.1.2"` to `@version "0.1.0" # x-release-please-version`. This reverts to the true published Hex version per D-04 so release-please writes the correct bump at the real cut, and the annotation lets the `extra-files` generic updater locate the line.

    Extend release-please-config.json IN PLACE (do not rewrite the changelog-sections, bootstrap-sha, or bump flags). Add a top-level `"plugins"` array containing one object: `{ "type": "linked-versions", "groupName": "crosswake", "components": ["hex", "ios-core", "android-core"] }`. On the existing `.` package add `"component": "hex"`, `"release-as": "0.1.2"` (the one-time bootstrap pin per D-02/D-04 — do NOT remove it, that is a Phase-111 task), and `"extra-files": ["mix.exs"]`. Add a `"packages/crosswake-shell-core-ios"` package entry with `"component": "ios-core"`, `"release-type": "simple"`, `"skip-changelog": true` and NO extra-files (the git tag is the SwiftPM version; there is no version field to update). Add a `"packages/crosswake-shell-core-android"` package entry with `"component": "android-core"`, `"release-type": "simple"`, `"skip-changelog": true`, and `"extra-files"` containing one object `{ "type": "generic", "path": "packages/crosswake-shell-core-android/build.gradle.kts" }` (repo-root-relative path required because build.gradle.kts is not at the package root). Do NOT add Package.swift to any extra-files. Match the exact target JSON in 110-PATTERNS.md.

    Edit .release-please-manifest.json: keep `".": "0.1.0"` (NOT 0.1.2 — D-04), add `"packages/crosswake-shell-core-ios": "0.1.0"` and `"packages/crosswake-shell-core-android": "0.1.0"`.
  </action>
  <verify>
    <automated>grep -q '@version "0.1.0" # x-release-please-version' mix.exs && python3 -c "import json; c=json.load(open('release-please-config.json')); assert c['plugins'][0]['type']=='linked-versions'; assert c['plugins'][0]['components']==['hex','ios-core','android-core']; assert c['packages']['.']['component']=='hex'; assert c['packages']['.']['release-as']=='0.1.2'; assert c['packages']['.']['extra-files']==['mix.exs']; assert c['packages']['packages/crosswake-shell-core-ios']['component']=='ios-core'; assert c['packages']['packages/crosswake-shell-core-android']['component']=='android-core'; assert c['packages']['packages/crosswake-shell-core-android']['extra-files'][0]['type']=='generic'; m=json.load(open('.release-please-manifest.json')); assert m['.']=='0.1.0'; assert m['packages/crosswake-shell-core-ios']=='0.1.0'; assert m['packages/crosswake-shell-core-android']=='0.1.0'; print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - mix.exs line 4 reads `@version "0.1.0" # x-release-please-version`
    - release-please-config.json contains `"plugins"` with a `linked-versions` object whose `components` are exactly `["hex", "ios-core", "android-core"]`
    - The `.` package has `"component": "hex"`, `"release-as": "0.1.2"`, and `"extra-files": ["mix.exs"]`
    - `packages/crosswake-shell-core-ios` and `packages/crosswake-shell-core-android` package entries exist with correct `component` keys
    - The Android package `extra-files` entry is `{ "type": "generic", "path": "packages/crosswake-shell-core-android/build.gradle.kts" }`
    - Package.swift appears in NO extra-files array
    - .release-please-manifest.json has all three keys, every value `"0.1.0"`
    - Both JSON files parse without error (the python3 assertion exits 0)
  </acceptance_criteria>
  <done>Version drift reconciled per D-04; release-please is in linked-versions manifest mode covering all three components; the python3 assertion above exits 0.</done>
</task>

<task type="auto">
  <name>Task 2: Add Vanniktech Maven Central publish config + complete signed POM to the Android core</name>
  <files>packages/crosswake-shell-core-android/build.gradle.kts</files>
  <read_first>
    - packages/crosswake-shell-core-android/build.gradle.kts (full file — current-state excerpt in 110-PATTERNS.md "build.gradle.kts" section)
    - 110-PATTERNS.md section "packages/crosswake-shell-core-android/build.gradle.kts" (verbatim target additions + the automaticRelease=false override note)
    - .planning/research/STACK.md "build.gradle.kts — Additions" (note: automaticRelease in STACK.md is `true` — OVERRIDDEN to `false` per D-07)
    - .planning/research/PITFALLS.md Pitfall 1 (burned version), Pitfall 12 (namespace vs Android namespace distinction)
  </read_first>
  <action>
    Edit packages/crosswake-shell-core-android/build.gradle.kts, additions only (preserve all existing android{}, dependencies{}, compileOptions, kotlinOptions blocks verbatim).

    Add `id("com.vanniktech.maven.publish") version "0.31.0"` as the last entry in the existing `plugins {}` block. Use 0.31.0 exactly — NOT 0.36.0 (0.36.0 requires Gradle 9 + AGP 8.13 which exceeds the project's gradle-8.7 + compileSdk 34).

    Immediately after the closing brace of `plugins {}`, add a top-level line: `version = "0.1.0" // x-release-please-version`. This is the line the release-please generic updater (configured in Task 1) bumps; it must match the manifest baseline `0.1.0`.

    After the `dependencies {}` block, add a `mavenPublishing {}` block. First line: `publishToMavenCentral(SonatypeHost.CENTRAL_PORTAL, automaticRelease = false)` — `automaticRelease = false` is LOAD-BEARING (D-07): it keeps the deployment in USER_MANAGED mode, stopping at VALIDATED instead of auto-publishing, which is what makes the D-01 "prove without burning a coordinate" guarantee possible. NEVER use `publishAndReleaseToMavenCentral` and NEVER `automaticRelease = true`. Then `signAllPublications()`. Then `coordinates("io.github.sztheory", "crosswake-shell-core-android", version.toString())` — note the Maven groupId is `io.github.sztheory` (lowercase, the verified Central Portal namespace), which is DISTINCT from the Android `namespace = "dev.crosswake.shell.core"` that stays unchanged. Then a `pom { }` block with the complete metadata from 110-PATTERNS.md verbatim: name "Crosswake Shell Core Android", description "Android native shell core for the Crosswake Phoenix library.", inceptionYear "2024", url "https://github.com/szTheory/crosswake", a `licenses { license { } }` (MIT, name/url/distribution), a `developers { developer { } }` (id/name/url all szTheory), and an `scm { }` (url/connection/developerConnection pointing at github.com/szTheory/crosswake). All six POM completeness fields (name, description, url, license, developers, scm) MUST be present — PUB-03 dry-run gate (Wave 2) asserts each.
  </action>
  <verify>
    <automated>cd packages/crosswake-shell-core-android && grep -q 'id("com.vanniktech.maven.publish") version "0.31.0"' build.gradle.kts && grep -q 'version = "0.1.0" // x-release-please-version' build.gradle.kts && grep -q 'publishToMavenCentral(SonatypeHost.CENTRAL_PORTAL, automaticRelease = false)' build.gradle.kts && grep -q 'coordinates("io.github.sztheory", "crosswake-shell-core-android"' build.gradle.kts && grep -q 'signAllPublications()' build.gradle.kts && ! grep -q 'publishAndReleaseToMavenCentral' build.gradle.kts && ! grep -q 'automaticRelease = true' build.gradle.kts && for f in 'name.set' 'description.set' 'url.set' 'licenses' 'developers' 'scm'; do grep -q "$f" build.gradle.kts || { echo "POM missing: $f"; exit 1; }; done && echo "OK"</automated>
  </verify>
  <acceptance_criteria>
    - build.gradle.kts plugins block contains `id("com.vanniktech.maven.publish") version "0.31.0"`
    - A top-level `version = "0.1.0" // x-release-please-version` line exists
    - mavenPublishing block uses `publishToMavenCentral(SonatypeHost.CENTRAL_PORTAL, automaticRelease = false)`
    - build.gradle.kts does NOT contain `publishAndReleaseToMavenCentral` and does NOT contain `automaticRelease = true`
    - coordinates() call uses groupId `io.github.sztheory` and artifactId `crosswake-shell-core-android`
    - `signAllPublications()` is present
    - POM block contains all six fields: name, description, url, license(s), developers, scm
    - The Android `namespace = "dev.crosswake.shell.core"` line is unchanged (distinct from the Maven groupId)
    - The grep verification chain above exits 0
  </acceptance_criteria>
  <done>Android core has a complete signed Maven Central publish config in USER_MANAGED (validated-stop) mode; the verify chain exits 0; no auto-publish path exists in the build file.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| build.gradle.kts → Maven Central | Publish coordinates + POM declare the artifact identity an adopter resolves; a wrong groupId or incomplete POM is rejected/burned at upload |
| release-please-config.json → release tags | Config decides which version coordinate every registry advances to; a wrong manifest baseline burns a coordinate via a bad first bump |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-110-01 | Tampering | build.gradle.kts publish block (auto-publish risk) | mitigate | Pin `automaticRelease = false`; verify step asserts `publishAndReleaseToMavenCentral`/`automaticRelease = true` are absent. Burning an immutable coordinate (PITFALLS #1) is the worst outcome; the USER_MANAGED stop-at-VALIDATED setting is the structural guard. |
| T-110-02 | Repudiation | Unsigned/incomplete POM | mitigate | `signAllPublications()` + all six POM fields asserted by the verify chain; the Wave 2 fire-drill re-asserts `.asc` signatures and POM completeness before any Central upload. |
| T-110-03 | Tampering | release-please manifest off-by-one (burned bump) | mitigate | Manifest baselined at the true published `0.1.0` + one-time `release-as: "0.1.2"` pin (D-04); python3 assertion verifies all three baselines and the pin. No real release runs in 110, so a mis-bump cannot reach a registry this phase. |
| T-110-04 | Information Disclosure | Secrets in build.gradle.kts | accept | No secret values are written to the build file; signing/credentials are injected at CI time via `ORG_GRADLE_PROJECT_*` env (Wave 2). Build file references nothing sensitive. |

No HIGH-severity threat is left unmitigated. All package installs in this plan (`com.vanniktech.maven.publish` 0.31.0) are version-pinned coordinates from the verified STACK.md research with a HIGH-confidence source — see the Package Legitimacy note below.
</threat_model>

<verification>
- `python3` JSON assertions in Task 1 verify confirm linked-versions config + manifest baselines.
- `grep` chain in Task 2 verify confirms Vanniktech config, USER_MANAGED mode, POM completeness, and absence of any auto-publish path.
- No CI job, secret, or network call is exercised by this plan — the Wave 2 plan (110-03) consumes this config.
- Package legitimacy: `com.vanniktech.maven.publish` 0.31.0 is a Gradle plugin (not npm/pip/cargo); STACK.md verified it against the official plugin releases page at HIGH confidence. No npm/pip/cargo install task exists in this plan, so the Package Legitimacy Gate table is not required.
</verification>

<success_criteria>
- mix.exs @version is `0.1.0` with the release-please annotation
- release-please-config.json is in linked-versions manifest mode covering hex/ios-core/android-core with the one-time `release-as: "0.1.2"` pin retained
- .release-please-manifest.json tracks all three packages baselined at `0.1.0`
- build.gradle.kts has a complete, signed, USER_MANAGED Maven Central publish config under `io.github.sztheory`
- No real publish, no auto-publish path, no burned coordinate (D-01 honored)
</success_criteria>

<output>
Create `.planning/phases/110-native-publish-lockstep-infrastructure/110-01-SUMMARY.md` when done.
</output>
