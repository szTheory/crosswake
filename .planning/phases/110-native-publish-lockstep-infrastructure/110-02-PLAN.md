---
phase: 110-native-publish-lockstep-infrastructure
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - SETUP.md
autonomous: true
requirements: [PUB-03, PUB-01]
must_haves:
  truths:
    - "A human operator can follow SETUP.md to provision every credential the publish path needs, out-of-band, before any CI run"
    - "The GPG keypair instructions produce a primary-key-with-signing-capability, no signing subkey (the Maven Central footgun)"
    - "The runbook records all 8 repo secrets by exact name and the empty-mirror-repo creation step"
    - "The runbook documents the un-checkable limitations (PAT scope, namespace status) so the operator knows what the preflight cannot assert"
  artifacts:
    - path: "SETUP.md"
      provides: "One-time provisioning runbook for GPG, Sonatype, mirror repo, secrets, tag ruleset"
      contains: "ORG_GRADLE_PROJECT_signingInMemoryKey"
      min_lines: 80
  key_links:
    - from: "SETUP.md"
      to: "the Wave 2 preflight job"
      via: "the 8 secret names the preflight asserts must match the names SETUP.md tells the operator to set"
      pattern: "gh secret list"
---

<objective>
Produce SETUP.md — the human runbook for out-of-band credential provisioning (D-06). The GSD executor is non-interactive (no browser OAuth, no TTY, no keygen), so the human completes all credential setup BEFORE execution, guided by this document; the Wave 2 preflight then VERIFIES presence and fails fast. This plan writes the runbook only; it does not run any provisioning step.

Purpose: PUB-03 requires publishing prerequisites to be established AND RECORDED so the first publish cannot silently fail or burn an immutable version. The recording surface is SETUP.md. It also creates the empty iOS mirror repo (PUB-01 precondition, D-09) and the best-effort tag ruleset (D-10).

Output: `SETUP.md` at repo root.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/110-native-publish-lockstep-infrastructure/110-CONTEXT.md
@.planning/phases/110-native-publish-lockstep-infrastructure/110-PATTERNS.md
@.planning/research/STACK.md

<interfaces>
<!-- The exact 8-item SETUP.md content scope is enumerated in 110-PATTERNS.md "SETUP.md" section. -->

The 8 repo secret names (must match Wave 2 preflight exactly):
- HEX_API_KEY (scope package:crosswake — already exists)
- ORG_GRADLE_PROJECT_mavenCentralUsername
- ORG_GRADLE_PROJECT_mavenCentralPassword
- ORG_GRADLE_PROJECT_signingInMemoryKey
- ORG_GRADLE_PROJECT_signingInMemoryKeyId
- ORG_GRADLE_PROJECT_signingInMemoryKeyPassword
- MIRROR_PUSH_TOKEN
- RELEASE_PLEASE_TOKEN

GPG keygen command (the footgun-safe form, D-06): gpg --quick-generate-key "szTheory <qiksnare13@gmail.com>" rsa4096 sign 0
GPG export: gpg --export-secret-keys --armor <KEYID>
Tag ruleset API: gh api POST /repos/szTheory/crosswake-shell-core-ios/rulesets --input ruleset.json (target tag, rules non_fast_forward + deletion, enforcement active, conditions refs/tags/*)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write SETUP.md provisioning runbook covering all 8 ordered items</name>
  <files>SETUP.md</files>
  <read_first>
    - 110-PATTERNS.md section "SETUP.md — new one-time provisioning runbook" (the authoritative 8-item content scope)
    - .planning/phases/110-native-publish-lockstep-infrastructure/110-CONTEXT.md decisions D-06, D-09, D-10 (GPG footgun, empty mirror repo, tag ruleset)
    - .planning/research/STACK.md "io.github.sztheory Namespace — One-Time Setup" and "Required GitHub Secrets for Android publish CI"
    - .planning/research/REC-PIPELINE.md "HEX_API_KEY Guidance" (scope package:crosswake) and "Step 2: Grant Actions write" (release-please permissions footgun)
    - .planning/research/PITFALLS.md Pitfall 2 (GPG keyserver), Pitfall 12 (namespace verification)
  </read_first>
  <action>
    Create SETUP.md at the repo root. It is a numbered, calm/explicit/technical runbook (house voice) the operator runs ONCE before Phase 110 execution. Cover these 8 items in order, each as a section with the exact commands:

    1. Sonatype namespace provisioning — sign in at central.sonatype.com with GitHub OAuth as szTheory; `io.github.sztheory` auto-provisions on first GitHub-authenticated login; document the manual fallback (View Namespaces → Add Namespace → verify via temp repo). State the limitation: the namespace has NO status API, so the Wave 2 preflight cannot assert verification — the validated-upload→drop fire-drill (110-03) IS the namespace check.

    2. Sonatype user token — generate at central.sonatype.com → User Token (the generated token, NOT the login email/password). Set `ORG_GRADLE_PROJECT_mavenCentralUsername` and `ORG_GRADLE_PROJECT_mavenCentralPassword` repo secrets via `gh secret set`.

    3. GPG key generation — the footgun-safe form: `gpg --quick-generate-key "szTheory <qiksnare13@gmail.com>" rsa4096 sign 0`. Explain WHY: this produces a primary key WITH signing capability and NO signing subkey; Maven Central rejects subkey-only exports with a misleading "Invalid signature for file" error. Export with `gpg --export-secret-keys --armor <KEYID>` → set as `ORG_GRADLE_PROJECT_signingInMemoryKey`. Short key ID (last 8 hex chars) → `ORG_GRADLE_PROJECT_signingInMemoryKeyId`. Passphrase → `ORG_GRADLE_PROJECT_signingInMemoryKeyPassword`.

    4. GPG public-key upload — upload to BOTH `keys.openpgp.org` (note keys.openpgp.org requires the email-confirmation VKS flow for identity binding) and `keyserver.ubuntu.com` (HKP `--send-keys`). Central Portal verification checks both; document the verify-from-clean-environment recv-keys check.

    5. iOS mirror repo creation — `gh repo create szTheory/crosswake-shell-core-ios --public` with NO README and NO initial commit (empty repo; CI seeds it on the first release in Phase 111 per D-09). State that it stays empty through Phase 110.

    6. MIRROR_PUSH_TOKEN — create a fine-grained PAT scoped to `szTheory/crosswake-shell-core-ios` ONLY with `Contents: write` and nothing else (least privilege). Set as `MIRROR_PUSH_TOKEN` repo secret on the crosswake repo. State the limitation: PAT scope is NOT API-checkable — the Wave 2 preflight asserts PRESENCE only; true scope is validated by the first mirror-push run (Phase 111).

    7. Tag ruleset on the mirror repo — `gh api POST /repos/szTheory/crosswake-shell-core-ios/rulesets --input ruleset.json` with `target: tag`, rules `non_fast_forward` + `deletion`, `enforcement: active`, conditions `refs/tags/*` (`refs/tags/**/*`). Include the literal ruleset.json body. State per D-10: this is best-effort defense-in-depth; the load-bearing tag-immutability guard is the no-`--force` CI push command (wired in 110-03), so do not block setup on the ruleset succeeding. Note this is a DIFFERENT API surface than the legacy branch-protection UI that was previously harness-blocked.

    8. Secret verification checklist — run `gh secret list` and confirm all 8 names are present: HEX_API_KEY, ORG_GRADLE_PROJECT_mavenCentralUsername, ORG_GRADLE_PROJECT_mavenCentralPassword, ORG_GRADLE_PROJECT_signingInMemoryKey, ORG_GRADLE_PROJECT_signingInMemoryKeyId, ORG_GRADLE_PROJECT_signingInMemoryKeyPassword, MIRROR_PUSH_TOKEN, RELEASE_PLEASE_TOKEN.

    Add a short closing "What this runbook does NOT do" section: it never performs a real publish, never cuts a release, and the mirror repo stays empty until Phase 111. Add a "Limitations the preflight cannot check" subsection consolidating the namespace-status and PAT-scope caveats from items 1 and 6.

    Do NOT paste secret values anywhere. Use placeholders like `<KEYID>` and `<paste-token-when-prompted>`.
  </action>
  <verify>
    <automated>test -f SETUP.md && grep -q 'quick-generate-key "szTheory <qiksnare13@gmail.com>" rsa4096 sign 0' SETUP.md && grep -q 'gh repo create szTheory/crosswake-shell-core-ios' SETUP.md && grep -q 'non_fast_forward' SETUP.md && grep -q 'package:crosswake' SETUP.md && for s in HEX_API_KEY ORG_GRADLE_PROJECT_mavenCentralUsername ORG_GRADLE_PROJECT_mavenCentralPassword ORG_GRADLE_PROJECT_signingInMemoryKey ORG_GRADLE_PROJECT_signingInMemoryKeyId ORG_GRADLE_PROJECT_signingInMemoryKeyPassword MIRROR_PUSH_TOKEN RELEASE_PLEASE_TOKEN; do grep -q "$s" SETUP.md || { echo "SETUP.md missing secret name: $s"; exit 1; }; done && [ "$(wc -l < SETUP.md)" -ge 80 ] && echo "OK"</automated>
  </verify>
  <acceptance_criteria>
    - SETUP.md exists at repo root and is at least 80 lines
    - GPG section contains the exact `gpg --quick-generate-key "szTheory <qiksnare13@gmail.com>" rsa4096 sign 0` command AND explains the no-signing-subkey rationale
    - Sonatype section uses USER TOKEN (not login) language and notes the namespace has no status API
    - HEX_API_KEY section references the `package:crosswake` scope
    - Mirror-repo section creates an EMPTY public repo (no README, no initial commit)
    - MIRROR_PUSH_TOKEN section specifies `Contents: write` on the mirror repo ONLY and documents the presence-only preflight limitation
    - Tag-ruleset section includes `non_fast_forward` + `deletion` rules and marks the ruleset best-effort
    - All 8 secret names appear verbatim
    - No real secret value appears anywhere (placeholders only)
    - The verify chain exits 0
  </acceptance_criteria>
  <done>SETUP.md is a complete, ordered, footgun-aware provisioning runbook; the verify chain exits 0; an operator can provision the full publish path out-of-band from it.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator's machine → GitHub repo secrets | GPG private key + Sonatype user token + PATs cross from the human's keyring into Actions secret storage; the runbook governs how |
| documentation → published artifact identity | The runbook prescribes the GPG keypair shape and keyserver presence that Maven Central's signature verification depends on |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-110-05 | Information Disclosure | Secret values leaking into SETUP.md or git history | mitigate | Runbook uses placeholders only; the verify chain does not write secrets; explicit "do not paste secret values" instruction. SETUP.md teaches `gh secret set` (hidden prompt), never echoing a key. |
| T-110-06 | Spoofing | GPG key not on a keyserver → Central Portal signature verification fails (or a different key signs) | mitigate | Item 4 mandates upload to BOTH keys.openpgp.org (VKS) and keyserver.ubuntu.com (HKP) with a clean-environment recv-keys verification (PITFALLS #2). |
| T-110-07 | Elevation of Privilege | Over-scoped MIRROR_PUSH_TOKEN (broad PAT) | mitigate | Item 6 mandates fine-grained PAT, `Contents: write` on the mirror repo ONLY; documents that true scope is validated by the first mirror-push run since it is not API-checkable. |
| T-110-08 | Tampering | Mirror tag force-moved/deleted after an adopter resolves it | mitigate | Item 7 tag ruleset (`non_fast_forward` + `deletion`) as defense-in-depth; the durable guard is the no-`--force` CI push wired in 110-03. |

No HIGH-severity threat left unmitigated. No package-manager installs in this plan; Package Legitimacy Gate table not applicable.
</threat_model>

<verification>
- Task 1 verify chain confirms the GPG footgun command, the empty-mirror creation, the ruleset rules, the HEX scope, and all 8 secret names.
- This plan produces documentation only — no CI run, no provisioning execution, no network call. The human executes the runbook out-of-band; the Wave 2 preflight verifies the result.
</verification>

<success_criteria>
- SETUP.md fully records the one-time provisioning path with footgun-safe GPG, empty mirror repo, best-effort tag ruleset, and all 8 named secrets
- Un-checkable limitations (namespace status, PAT scope) are documented so the operator understands the preflight's blind spots
- No secret values committed; no real publish performed
</success_criteria>

<output>
Create `.planning/phases/110-native-publish-lockstep-infrastructure/110-02-SUMMARY.md` when done.
</output>
