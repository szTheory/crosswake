---
phase: 137-crosswake-sigra-extraction
plan: 02
type: execute
wave: 2
depends_on: [137-01]
files_modified:
  - lib/crosswake/companions/sigra/evaluator.ex
  - lib/crosswake/companions/sigra/step_up_ceremony.ex
  - lib/crosswake/companions/sigra.ex
  - examples/phoenix_host/lib/crosswake_example/saas_portal/step_up_plug.ex
  - examples/phoenix_host/lib/crosswake_example/saas_portal/step_up_on_mount.ex
  - examples/phoenix_host/lib/crosswake_example/saas_portal/handoff.ex
  - test/crosswake/proof/phase56_step_up_ceremony_test.exs
  - test/crosswake/proof/phase73_auth_sensitive_admin_workflow_proof_test.exs
autonomous: true
requirements: [SIGRA-02]
must_haves:
  truths:
    - "Evaluator.deny/4 returns {:deny, %Finding{axis: :auth, code:, details: sanitized, message: @generic_message, route_id:}} — the sole Finding-construction site; DenialCodes.sanitize_details/1 runs once here at source"
    - "StepUpCeremony matches %Finding{axis: :auth, code:} for challengeable codes and reads finding.details[\"max_auth_age_seconds\"] in issue_attrs (security guard preserved)"
    - "no alias Crosswake.Shell.Denial remains in evaluator.ex or step_up_ceremony.ex; sigra internals never touch Denial"
    - "the host issue_intent error contract returns {:error, Finding.t()} and normalize_issue_result handles it without referencing Denial"
    - "the sigra facade shim added in Plan 01 is removed — the facade passes the evaluator's Finding through unchanged"
  artifacts:
    - "lib/crosswake/companions/sigra/evaluator.ex — deny/4 emits %Finding{axis: :auth}"
    - "lib/crosswake/companions/sigra/step_up_ceremony.ex — Finding-based match, normalize_issue_result, issue_attrs with max_auth_age_seconds guard"
    - "examples/phoenix_host/.../step_up_plug.ex, step_up_on_mount.ex, handoff.ex — issue_intent error arm returns Finding"
  key_links:
    - "Evaluator.deny/4 sanitize_details → %Finding{details: sanitized} → RouteGate.finding_to_denial (Plan 01) — the single sanitize-at-source seam"
    - "finding.details[\"max_auth_age_seconds\"] in issue_attrs — dropping it silently removes the step-up max-age constraint (EoP)"
---

<objective>
Flip the SIGRA-INTERNAL side of the D-4 Finding boundary so sigra emits `%Finding{axis: :auth}`
end-to-end and never references `Crosswake.Shell.Denial`. This completes the boundary begun in
Plan 01: the evaluator's sole deny site constructs a Finding (sanitizing details once at source),
StepUpCeremony re-points its semantic match and its `normalize_issue_result` fallback/host-error
arm to Finding, and the `issue_intent` host-callback error contract becomes `{:error, Finding.t()}`.
The example-host callbacks and the two core proof tests (phase56, phase73) that construct
`evaluator_result: {:deny, Denial.new(...)}` fixtures are updated to the Finding shape so the core
suite stays green in place — BEFORE the package move (Plan 03).

Implements D-137-A sigra edits (Evaluator.deny/4, StepUpCeremony re-point, max_auth_age guard,
remove Denial aliases) and the sanitize-at-source half of D-137-B. Resolves RESEARCH Open
Question 1 (normalize_issue_result host contract) concretely: the host issue_intent error arm
returns a Finding.

Purpose: "sigra never touches Denial" is absolute — but the live host `issue_intent` error
contract returns `{:error, %Denial{}}`. This plan migrates that contract to Finding so the
package (Plan 03) compiles with no Denial reference. Removing the Plan 01 facade shim here keeps
exactly one Finding-construction path.
Output: sigra internals + example host + affected core proof fixtures all on the Finding boundary.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/137-crosswake-sigra-extraction/137-CONTEXT.md
@.planning/phases/137-crosswake-sigra-extraction/137-RESEARCH.md
@.planning/phases/137-crosswake-sigra-extraction/137-01-SUMMARY.md
</context>

<artifacts_this_phase_produces>
This plan (02) produces:
- `Evaluator.deny/4` returning `%Finding{axis: :auth, code:, details: sanitized, message:, route_id:}`
- `StepUpCeremony` Finding-based match + `normalize_issue_result` + `issue_attrs` max-age guard
- host `issue_intent` error contract: `{:error, Finding.t()}` in step_up_plug.ex / step_up_on_mount.ex / handoff.ex
- removal of the Plan-01 facade Denial→Finding shim in `sigra.ex`
- updated phase56 / phase73 core-test fixtures on the Finding shape
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Evaluator.deny/4 emits %Finding{axis: :auth}; sanitize once at source; drop Denial alias</name>
  <files>lib/crosswake/companions/sigra/evaluator.ex</files>
  <read_first>
    - lib/crosswake/companions/sigra/evaluator.ex L1-20 (aliases incl `alias Crosswake.Shell.Denial` at L14, @generic_message)
    - lib/crosswake/companions/sigra/evaluator.ex L241-260 (deny/4 — the sanitize pipeline + Denial.new call, the sole Finding-construction site)
    - 137-RESEARCH.md § evaluator.ex drift section + § Pattern 5 Step 4
    - lib/crosswake/compatibility/compatibility.ex Finding struct (post-Plan-01: has :code/:details, :route_id)
  </read_first>
  <behavior>
    - Test (via the moved step_up/phase54 lanes, or an inline evaluator assertion): Evaluator.evaluate_route_auth/3 on a route that must step up returns {:deny, %Finding{axis: :auth}} where finding.code is a populated dotted code (never nil), finding.message == @generic_message, and finding.details is the sanitized map (passed through DenialCodes.sanitize_details/1).
    - Test: finding.details contains only allowlisted keys (sanitize ran) and includes "max_auth_age_seconds" when the route sets requires_recent_auth.
  </behavior>
  <action>
    In `deny/4`, keep the existing sanitize pipeline exactly (Map.put_new :evaluated_at, maybe_put_ref for challenge_ref/step_up_token_ref, then `DenialCodes.sanitize_details/1`) — this is the once-at-source PII scrub. Replace the returned `Denial.new(reason: :step_up_required, code: code, message: @generic_message, route_id: route.id, details: sanitized)` with `{:deny, %Finding{axis: :auth, code: code, message: @generic_message, route_id: route.id, details: sanitized}}`. `finding.code` MUST always be the caller-supplied dotted code (never nil) so it does not silently downgrade in core's fallback. `message` MUST stay the `@generic_message` constant (it bypasses sanitize and flows into Denial.message — PII guard).
    Remove `alias Crosswake.Shell.Denial` (L14). Add `alias Crosswake.Compatibility.Finding`.
    Verify no other `Denial.new`/`%Denial{}` reference remains in evaluator.ex after the change.
  </action>
  <verify>
    <automated>mix compile --warnings-as-errors</automated>
    <automated>grep -rn "Crosswake.Shell.Denial" lib/crosswake/companions/sigra/evaluator.ex; test $(grep -c "Crosswake.Shell.Denial" lib/crosswake/companions/sigra/evaluator.ex) -eq 0</automated>
    <automated>mix test test/crosswake/proof/phase54_sigra_session_authority_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - Evaluator.deny/4 returns {:deny, %Finding{axis: :auth, ...}} with code populated, message == @generic_message, details sanitized once
    - no Crosswake.Shell.Denial reference in evaluator.ex
    - phase54 evaluator assertions pass against the Finding shape
  </acceptance_criteria>
  <done>The evaluator's sole deny site emits a Finding with sanitized details; sigra's evaluator no longer references Denial.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: StepUpCeremony on Finding — re-point match, normalize_issue_result, and preserve the max_auth_age_seconds guard</name>
  <files>lib/crosswake/companions/sigra/step_up_ceremony.ex</files>
  <read_first>
    - lib/crosswake/companions/sigra/step_up_ceremony.ex full file (L1-105): aliases (L15 `alias Crosswake.Shell.Denial`), @spec evaluate_or_issue (L22-25), the semantic match at L39-45, issue_challenge/4 (L48-57), normalize_issue_result/1 (L59-78, incl the {:error, %Denial{}} host arm at L69 and the Denial.new fallback at L72-77), issue_attrs/4 (L80-99, esp `Map.get(denial.details, "max_auth_age_seconds")` at L94 and `denial.code` at L96)
    - 137-RESEARCH.md § step_up_ceremony.ex drift + § Pitfall 4 + Open Question 1
    - examples/phoenix_host issue_intent callers (Task 3 partners this change)
  </read_first>
  <behavior>
    - Test (moved step_up lane): a challengeable Finding (code in @challengeable_codes) routes to issue_challenge; issue_attrs populates max_auth_age_seconds from finding.details["max_auth_age_seconds"] (falls back to route.requires_recent_auth then 300). Dropping the finding.details lookup is a test failure (security-regression guard).
    - Test: a non-challengeable Finding returns {:deny, finding} unchanged.
    - Test: issue_intent returning {:error, %Finding{}} is normalized to {:deny, finding} (Finding, not Denial); the projection-failed fallback constructs a %Finding{axis: :auth, code: "auth.step_up_intent.projection_failed"}.
  </behavior>
  <action>
    Change the semantic match at L39 from `{:deny, %Denial{reason: :step_up_required, code: code} = denial}` to `{:deny, %Finding{axis: :auth, code: code} = finding}` (guard `when code in @challengeable_codes` unchanged) and pass `finding` into issue_challenge. Change the catch-all deny arm `{:deny, %Denial{} = denial}` to `{:deny, %Finding{axis: :auth} = finding}`. Update the `@spec evaluate_or_issue/3` deny return from `{:deny, Denial.t()}` to `{:deny, Finding.t()}` (per research A5 — the return type changes throughout).
    In `issue_challenge/4`, rename the bound `denial` param to `finding`. In `normalize_issue_result/1`: change the host-error arm from `{:error, %Denial{} = denial}` to `{:error, %Finding{axis: :auth} = finding}` returning `{:deny, finding}` (resolves Open Question 1 — the host contract migrates to Finding; Task 3 updates the host callers to match); change the fallback `Denial.new(...)` to `%Finding{axis: :auth, code: "auth.step_up_intent.projection_failed", message: @generic_or_existing_message, details: %{}}` (reuse the existing message string; do not introduce PII).
    In `issue_attrs/4`: change the pattern-match param `%Denial{} = denial` to `%Finding{axis: :auth} = finding`; change `Map.get(denial.details, "max_auth_age_seconds")` to `Map.get(finding.details, "max_auth_age_seconds")` (KEEP this lookup — dropping it silently removes the step-up max-age = EoP regression); change `route_denial_code: denial.code` to `finding.code`.
    Remove `alias Crosswake.Shell.Denial` (L15). Add `alias Crosswake.Compatibility.Finding`. Verify no Denial reference remains.
  </action>
  <verify>
    <automated>mix compile --warnings-as-errors</automated>
    <automated>grep -rn "Crosswake.Shell.Denial" lib/crosswake/companions/sigra/step_up_ceremony.ex; test $(grep -c "Crosswake.Shell.Denial" lib/crosswake/companions/sigra/step_up_ceremony.ex) -eq 0</automated>
    <automated>mix test test/crosswake/proof/phase56_step_up_ceremony_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - StepUpCeremony matches %Finding{axis: :auth} for both challengeable and non-challengeable arms
    - issue_attrs reads finding.details["max_auth_age_seconds"] (max-age guard preserved) and finding.code
    - normalize_issue_result host-error arm accepts {:error, %Finding{}} and the fallback constructs a Finding
    - no Crosswake.Shell.Denial reference in step_up_ceremony.ex
    - phase56 ceremony tests pass (after Task 4 fixture updates)
  </acceptance_criteria>
  <done>StepUpCeremony is fully Finding-based; the max-age security guard is preserved; sigra's ceremony no longer references Denial.</done>
</task>

<task type="auto">
  <name>Task 3: Migrate the host issue_intent error contract to Finding and remove the Plan-01 facade shim</name>
  <files>examples/phoenix_host/lib/crosswake_example/saas_portal/step_up_plug.ex, examples/phoenix_host/lib/crosswake_example/saas_portal/step_up_on_mount.ex, examples/phoenix_host/lib/crosswake_example/saas_portal/handoff.ex, lib/crosswake/companions/sigra.ex</files>
  <read_first>
    - examples/phoenix_host/.../step_up_plug.ex L20-60 (evaluate_or_issue call, issue_intent/1, the {:deny, %Denial{}} handling + denied_path)
    - examples/phoenix_host/.../step_up_on_mount.ex L18-60 (mirror of the plug)
    - examples/phoenix_host/.../handoff.ex L55-80, L305-355 ({:error, %Denial{}} arms and %{denial: %Denial{}} projections)
    - lib/crosswake/companions/sigra.ex L79-96 (the facade evaluate_auth/3; the Plan-01 shim to remove)
    - 137-RESEARCH.md Open Question 1 + § example host issue_intent contract
  </read_first>
  <action>
    Update the example-host `issue_intent/1` callbacks so their ERROR return is `{:error, %Finding{axis: :auth, code:, message:, details:}}` rather than `{:error, %Denial{}}`, matching StepUpCeremony's new host contract (Task 2). Where the host receives the ceremony's `{:deny, ...}` result and pattern-matches `%Denial{}` for `denied_path/1`, decide per call site: the ceremony now returns `{:deny, Finding.t()}`, so the host's post-ceremony deny handling must match `%Finding{axis: :auth}` (extract `finding.code`/`finding.message` for the denied_path). In `handoff.ex`, the `%Denial{}` arms that are part of the sigra step-up path migrate to Finding; any `%Denial{}` arms that belong to a DIFFERENT (non-sigra) host contract stay as Denial — inspect each arm and only migrate the step-up/ceremony-facing ones. Keep the host examples compiling `--warnings-as-errors`.
    In `sigra.ex`, REMOVE the temporary Denial→Finding shim added by Plan 01 Task 2: the evaluator now returns a Finding natively, so the facade `evaluate_auth/3` deny arm simply passes the Finding through unchanged (`{:deny, finding} -> {:deny, finding}`), with a comment noting the D-137-A Finding boundary is realized. (This file is removed entirely in Plan 03; this edit just keeps core coherent this wave.)
  </action>
  <verify>
    <automated>mix compile --warnings-as-errors</automated>
    <automated>cd examples/phoenix_host && mix compile --warnings-as-errors</automated>
  </verify>
  <acceptance_criteria>
    - host issue_intent error arm returns {:error, %Finding{axis: :auth}}
    - host post-ceremony deny handling matches %Finding{axis: :auth} for step-up paths; unrelated Denial arms untouched
    - sigra.ex facade passes the evaluator's Finding through with no shim
    - core AND examples/phoenix_host compile --warnings-as-errors
  </acceptance_criteria>
  <done>The host issue_intent contract is Finding-based end-to-end and the facade shim is gone; the entire step-up path is Denial-free on the sigra side.</done>
</task>

<task type="auto">
  <name>Task 4: Update phase56 and phase73 core-proof fixtures to the Finding shape</name>
  <files>test/crosswake/proof/phase56_step_up_ceremony_test.exs, test/crosswake/proof/phase73_auth_sensitive_admin_workflow_proof_test.exs</files>
  <read_first>
    - test/crosswake/proof/phase56_step_up_ceremony_test.exs L1-10 (aliases incl `alias Crosswake.Shell.Denial` L7), L133-202 (the evaluator_result: {:deny, Denial.new(...)} fixtures, the host_denial issue_intent, the {:deny, ^host_denial} assertions), L318+ (@tag :requires_example_host)
    - test/crosswake/proof/phase73_auth_sensitive_admin_workflow_proof_test.exs L1-15, L220-240 (evaluate_or_issue usage), any Denial.new / %Denial{} fixtures
    - Plan 02 Task 2 (the new host contract these fixtures must match)
  </read_first>
  <action>
    In phase56, change every `evaluator_result: {:deny, Denial.new(reason: :step_up_required, code: ..., message: ...)}` fixture to `{:deny, %Finding{axis: :auth, code: ..., message: ..., details: %{}}}` (the evaluator's shape). Change the `issue_intent: fn _attrs -> {:error, host_denial} end` fixture so `host_denial` is a `%Finding{axis: :auth, code:, message:}` and the assertion `{:deny, ^host_denial}` still holds (now a Finding). Keep the assertions that check DenialCodes/sanitize_details behaviour unchanged (those still exercise DenialCodes directly). Replace `alias Crosswake.Shell.Denial` with `alias Crosswake.Compatibility.Finding` where the test now constructs Findings; keep Denial alias ONLY if a remaining assertion legitimately inspects the translated core Denial (e.g. via RouteGate) — otherwise remove it. The `@tag :requires_example_host` test stays as-is (it is excluded by the test_helper tag filter).
    In phase73, apply the same Finding-shape migration to any `evaluator_result`/`%Denial{}` fixtures used with `StepUpCeremony.evaluate_or_issue`; leave host-source-reading assertions untouched.
    Note for the executor: phase56 and phase73 both directly `alias Crosswake.Companions.Sigra.StepUpCeremony`/`DenialCodes`. After Plan 03 extracts sigra, these can no longer resolve from core-only deps. This plan keeps them green IN CORE now; Plan 03 Task (test move/split) decides their final home (they MOVE to the package with the other sigra-internal tests, except the `@tag :requires_example_host` host-integration cases which stay gated in core). Do NOT move them in this plan — only migrate the fixtures to the Finding shape so the wave stays green.
  </action>
  <verify>
    <automated>mix test test/crosswake/proof/phase56_step_up_ceremony_test.exs --exclude requires_example_host</automated>
    <automated>mix test test/crosswake/proof/phase73_auth_sensitive_admin_workflow_proof_test.exs --exclude requires_example_host</automated>
    <automated>mix test --exclude requires_example_host --exclude engine_present</automated>
  </verify>
  <acceptance_criteria>
    - phase56 and phase73 fixtures construct %Finding{axis: :auth} where they previously built Denial.new for evaluator_result / issue_intent errors
    - both proof files pass (excluding host-DB-tagged cases)
    - the full core suite (minus host/engine tags) is green
  </acceptance_criteria>
  <done>The two core proof tests exercise the Finding boundary and stay green; the whole core suite is green before the package move.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| sigra evaluator → Finding | PII-bearing auth evidence must be sanitized here, once, at construction |
| host issue_intent callback → StepUpCeremony | host-returned error result crosses into sigra; contract shape (Finding) must hold |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-137-05 | Information Disclosure | Evaluator.deny/4 details | high | mitigate | DenialCodes.sanitize_details/1 runs once at source before the Finding is built; Finding.message stays the @generic_message constant (bypasses sanitize) — no raw PII in message/details. Verified by the allowlisted-details assertion (Task 1). |
| T-137-06 | Elevation of Privilege | StepUpCeremony.issue_attrs max_auth_age | high | mitigate | issue_attrs MUST read finding.details["max_auth_age_seconds"]; dropping it silently removes the step-up max-age constraint. Test-guarded (Task 2 behavior). |
| T-137-07 | Spoofing / code-downgrade | Evaluator.deny/4 finding.code | medium | mitigate | deny/4 always populates a dotted finding.code (never nil) so core's `code || Atom.to_string(reason)` fallback never silently downgrades the sub-classification. Verified by the populated-code assertion (Task 1). |
| T-137-08 | Tampering | host issue_intent error contract | medium | mitigate | Migrating the error arm to {:error, Finding.t()} keeps sigra free of any Denial reference (package precondition) while preserving the host's ability to signal a typed denial; only step-up-facing arms migrate, unrelated Denial contracts stay put (Task 3). |
| T-137-SC | Tampering | npm/pip/cargo installs | low | accept | No package installs in this plan (source refactor only); no [ASSUMED]/[SUS] packages per RESEARCH Package Legitimacy Audit. |
</threat_model>

<verification>
- `mix compile --warnings-as-errors` (core) and `cd examples/phoenix_host && mix compile --warnings-as-errors`.
- `grep -c "Crosswake.Shell.Denial"` == 0 in evaluator.ex and step_up_ceremony.ex.
- `mix test test/crosswake/proof/phase54_sigra_session_authority_test.exs`, `phase56_...`, `phase73_...` (excluding host tag) green.
- `mix test --exclude requires_example_host --exclude engine_present` — full core suite green.
</verification>

<success_criteria>
- sigra internals emit %Finding{axis: :auth} end-to-end; zero Denial references in evaluator.ex / step_up_ceremony.ex.
- sanitize-at-source and the max_auth_age_seconds security guard preserved and test-verified.
- host issue_intent error contract migrated to Finding; facade shim removed.
- core + example host + affected proofs all green before the package move.
</success_criteria>

<output>
Create `.planning/phases/137-crosswake-sigra-extraction/137-02-SUMMARY.md` when done
</output>
