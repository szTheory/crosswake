---
phase: 137-crosswake-sigra-extraction
plan: 04
type: execute
wave: 4
depends_on: [137-03]
files_modified:
  - script/verify_companion_cleanroom.sh
  - release-please-config.json
  - .release-please-manifest.json
  - .github/workflows/release-please.yml
autonomous: true
requirements: [SIGRA-03]
must_haves:
  truths:
    - "verify_companion_cleanroom.sh supports a no-engine mode (sigra) — no engine dep line, no engine-present smoke assertion — invoked as `verify_companion_cleanroom.sh crosswake_sigra <version>`"
    - "crosswake_sigra is registered as an independent elixir release-please component (NOT in linked-versions), with the one-shot release-as: 0.1.0"
    - "release-please.yml has sigra_release_created/sigra_tag_name/sigra_version outputs, a publish-hex-sigra job, a clean-room-proof-sigra job, and sigra folded into release-as-cleanup + release-failure-alert"
  artifacts:
    - "script/verify_companion_cleanroom.sh — no-engine branch"
    - "release-please-config.json — crosswake_sigra component block"
    - ".release-please-manifest.json — packages/crosswake_sigra: 0.1.0"
    - ".github/workflows/release-please.yml — 3 new jobs + outputs + extended conditions (~100 lines)"
  key_links:
    - "per-component gate: all sigra jobs key on sigra_release_created (NOT the aggregate releases_created) — keeps the sequential sigra→chimeway→threadline publish honest"
    - "publish-hex-sigra sets CROSSWAKE_RELEASE=1 on all mix steps — else hex.build rejects the path: dep"
    - "release-as-cleanup fires on sigra_release_created → strip_release_as.py crosswake_sigra (one-shot 0.1.0 removal)"
---

<objective>
Wire the CI publish pipeline (SIGRA-03) for `crosswake_sigra` as an INDEPENDENT release-please
component: register the component block (with the one-shot `release-as: "0.1.0"`), add the manifest
entry, add the ~100 lines to `release-please.yml` (sigra outputs + `publish-hex-sigra` +
`clean-room-proof-sigra` + extended `release-as-cleanup`/`release-failure-alert`), and — because
sigra has NO engine library — add a no-engine mode to `verify_companion_cleanroom.sh` (resolving
RESEARCH Open Question 2 / Pitfall 5: the script currently hardcodes `rulestead`/`Rulestead`
defaults for the engine dep + smoke assertion).

Implements SIGRA-03 CI registration + the clean-room lane plumbing per D-137-C (the ~100-line
release-please.yml clone, independent component registration, and the release-as one-shot cleanup
wiring). All jobs key on the per-component `sigra_release_created` output (never the aggregate),
preserving independent, sequential publishing (D-8, D-137-C).

Purpose: The publish gate chain (Plan 05) depends entirely on these lanes existing and being
green-able. The no-engine script fix prevents a `clean-room-proof-sigra` failure with spurious
rulestead references.
Output: sigra registered as an independent component; 3 new CI jobs; a no-engine clean-room path.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/137-crosswake-sigra-extraction/137-CONTEXT.md
@.planning/phases/137-crosswake-sigra-extraction/137-RESEARCH.md
@.planning/phases/137-crosswake-sigra-extraction/137-03-SUMMARY.md
</context>

<artifacts_this_phase_produces>
This plan (04) produces:
- `script/verify_companion_cleanroom.sh` — a no-engine branch (skip the `{:ENGINE_PACKAGE, "~> 0.1"}` dep line + skip the engine-present smoke assertion) triggered when engine args are empty/`none`
- `release-please-config.json` — `packages/crosswake_sigra` component block (component "crosswake_sigra", release-type "elixir", separate-pull-requests true, one-shot `release-as: "0.1.0"` + `_TODO_release_as`, extra-files, changelog-sections), NOT in `linked-versions`
- `.release-please-manifest.json` — `"packages/crosswake_sigra": "0.1.0"`
- `.github/workflows/release-please.yml` — `sigra_release_created`/`sigra_tag_name`/`sigra_version` outputs, `publish-hex-sigra` job, `clean-room-proof-sigra` job, `sigra_release_created` folded into `release-as-cleanup` if/strip and `release-failure-alert` needs/body
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Add a no-engine mode to verify_companion_cleanroom.sh</name>
  <files>script/verify_companion_cleanroom.sh</files>
  <read_first>
    - script/verify_companion_cleanroom.sh full file — esp. the arg parsing (PACKAGE $1, VERSION $2, ENGINE_PACKAGE $3 default rulestead, ENGINE_MODULE $4 default Rulestead at L39-46), the deps patch python block adding `{:"${ENGINE_PACKAGE}", "~> 0.1"}` (L111-139), the smoke test generation asserting `validate_dependency() == :ok when engine (${ENGINE_MODULE}) is installed` (L192-241), and the config/runtime.exs engine registration (L244-259)
    - 137-RESEARCH.md § Pitfall 5, Open Question 2, § No Engine Optional Dep (sigra's validate_dependency/0 returns :ok unconditionally — no engine gate)
    - lib/... (already extracted) sigra facade validate_dependency/0 == :ok
  </read_first>
  <action>
    Add an explicit no-engine mode to the script: when the engine args ($3/$4) are omitted OR passed as an empty string / the sentinel `none`, set an internal `NO_ENGINE=1` flag instead of falling back to the `rulestead`/`Rulestead` defaults. Under `NO_ENGINE=1`: (a) the deps-patch python block must NOT emit the `{:"${ENGINE_PACKAGE}", "~> 0.1"}` line (deps become crosswake + PACKAGE only); (b) the smoke test must NOT assert the engine-present branch (`validate_dependency() == :ok when engine ... installed`) — replace it with an unconditional `validate_dependency() == :ok` assertion (sigra has no engine gate, so validate_dependency is always :ok) plus the existing companion_id/enabled? assertions; (c) the config/runtime.exs registration must NOT include an engine registration line. Keep the default (engine) path 100% unchanged for rulestead/rindle back-compat — the no-engine behaviour is strictly additive, gated on the flag. Update the usage header comment to document the no-engine invocation.
    Do NOT hardcode a self-invalidating literal that a downstream negative-grep would trip (comment-text discipline).
  </action>
  <verify>
    <automated>bash -n script/verify_companion_cleanroom.sh</automated>
    <automated>grep -q "NO_ENGINE" script/verify_companion_cleanroom.sh && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - the script has a NO_ENGINE branch: no engine dep line, no engine-present smoke assertion, no engine config registration when invoked without engine args
    - the default rulestead/rindle engine path is unchanged (additive only)
    - `bash -n` parses clean
  </acceptance_criteria>
  <done>verify_companion_cleanroom.sh cleanly supports `verify_companion_cleanroom.sh crosswake_sigra <version>` with no engine references.</done>
</task>

<task type="auto">
  <name>Task 2: Register crosswake_sigra as an independent release-please component (config + manifest)</name>
  <files>release-please-config.json, .release-please-manifest.json</files>
  <read_first>
    - release-please-config.json — the rindle component block (clone it verbatim, retargeted to crosswake_sigra), the `linked-versions` group (sigra MUST NOT be added there — D-8), the rulestead/rindle `_TODO_release_as` + `release-as` one-shot precedent
    - .release-please-manifest.json — the 5 existing keys (., ios, android, crosswake_rulestead, crosswake_rindle); sigra becomes the 6th
    - 137-RESEARCH.md § Pattern 2 (config block) + § Pattern 3 (manifest addition) + § Pitfall 1 (release-as one-shot)
  </read_first>
  <action>
    Add a `"packages/crosswake_sigra"` block to `release-please-config.json` cloning the rindle block: `component: "crosswake_sigra"`, `release-type: "elixir"`, `separate-pull-requests: true`, a `_TODO_release_as` note explaining the one-shot removal-after-first-merge, `release-as: "0.1.0"` (one-shot), `extra-files: ["packages/crosswake_sigra/mix.exs"]`, and the standard `changelog-sections` array (same as rindle). Do NOT add `crosswake_sigra` to the `linked-versions` lockstep group — it is independently versioned (D-8).
    Add `"packages/crosswake_sigra": "0.1.0"` to `.release-please-manifest.json` (the 6th key). Keep valid JSON (trailing-comma-free, sorted-or-appended consistently with the existing style).
  </action>
  <verify>
    <automated>python3 -c "import json; json.load(open('release-please-config.json')); json.load(open('.release-please-manifest.json')); print('valid json')"</automated>
    <automated>python3 -c "import json; c=json.load(open('release-please-config.json')); p=c['packages']['packages/crosswake_sigra']; assert p['component']=='crosswake_sigra' and p['release-as']=='0.1.0'; print('sigra component OK')"</automated>
    <automated>python3 -c "import json; m=json.load(open('.release-please-manifest.json')); assert m['packages/crosswake_sigra']=='0.1.0'; print('manifest OK')"</automated>
  </verify>
  <acceptance_criteria>
    - release-please-config.json has a crosswake_sigra component block (elixir, separate-pull-requests, release-as 0.1.0), NOT in linked-versions
    - .release-please-manifest.json has "packages/crosswake_sigra": "0.1.0"
    - both files remain valid JSON
  </acceptance_criteria>
  <done>crosswake_sigra is an independent elixir release-please component with the one-shot 0.1.0 pin, absent from the lockstep group.</done>
</task>

<task type="auto">
  <name>Task 3: Add sigra outputs + publish-hex-sigra + clean-room-proof-sigra jobs and extend cleanup/alert</name>
  <files>.github/workflows/release-please.yml</files>
  <read_first>
    - .github/workflows/release-please.yml — the outputs block (rindle_release_created at L53, rulestead at L47), the publish-hex-rindle job (~L243+) to mirror, the rindle clean-room job (L764+ invoking verify_companion_cleanroom.sh at L782-789), the release-as-cleanup job (L795-830, if: at L803, strip block at L819-824), the release-failure-alert job (L842+ needs/body)
    - 137-RESEARCH.md § Pattern 4 (all four sub-blocks with exact line anchors) + Common Pitfalls (CROSSWAKE_RELEASE=1, per-component gate, required-check-before-green deadlock)
  </read_first>
  <action>
    Add three `outputs:` entries mirroring the rindle double-dash→dot-alias contract: `sigra_release_created`, `sigra_tag_name`, `sigra_version` (each reading `steps.release.outputs['packages/crosswake_sigra--*']`).
    Add a `publish-hex-sigra` job cloning `publish-hex-rindle`: `needs: release-please`, `if: needs.release-please.outputs.sigra_release_created == 'true'`, `env: CROSSWAKE_RELEASE: "1"` on all mix steps (else hex.build rejects the path: dep), checkout `ref: sigra_tag_name`, `working-directory: packages/crosswake_sigra`, version-grep `packages/crosswake_sigra/mix.exs`, poll `hex.pm/api/packages/crosswake_sigra/releases/${sigra_version}`.
    Add a `clean-room-proof-sigra` job: `needs: [release-please, publish-hex-sigra]`, `if: sigra_release_created == 'true'`, setup-beam via `.tool-versions` strict, install hex+rebar, then `bash script/verify_companion_cleanroom.sh crosswake_sigra "${{ needs.release-please.outputs.sigra_version }}"` (no engine args — uses the Task-1 no-engine mode).
    Extend `release-as-cleanup`: add `|| needs.release-please.outputs.sigra_release_created == 'true'` to its `if:`; add a strip block `if sigra_release_created == 'true' then python3 script/strip_release_as.py crosswake_sigra`.
    Extend `release-failure-alert`: add `publish-hex-sigra` and `clean-room-proof-sigra` to its `needs:` and add their results to the issue body (its `if: failure()` needs no change).
    All sigra jobs MUST gate on `sigra_release_created` (never the aggregate `releases_created`) so core-only releases never publish sigra (per-component gate — D-8).
  </action>
  <verify>
    <automated>python3 -c "import yaml; yaml.safe_load(open('.github/workflows/release-please.yml')); print('valid yaml')"</automated>
    <automated>grep -q "publish-hex-sigra" .github/workflows/release-please.yml && grep -q "clean-room-proof-sigra" .github/workflows/release-please.yml && grep -q "sigra_release_created" .github/workflows/release-please.yml && echo OK</automated>
    <automated>grep -q "strip_release_as.py crosswake_sigra" .github/workflows/release-please.yml && echo cleanup-wired</automated>
  </verify>
  <acceptance_criteria>
    - sigra_release_created/sigra_tag_name/sigra_version outputs exist
    - publish-hex-sigra sets CROSSWAKE_RELEASE=1 and gates on sigra_release_created
    - clean-room-proof-sigra invokes verify_companion_cleanroom.sh with only PACKAGE+VERSION (no-engine)
    - release-as-cleanup strips crosswake_sigra on sigra_release_created; release-failure-alert covers the new jobs
    - the workflow file is valid YAML
  </acceptance_criteria>
  <done>The full sigra publish pipeline exists in release-please.yml, per-component gated, with the one-shot release-as cleanup and failure alerting wired.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI → Hex.pm (irreversible publish) | the name/version coordinate is irreversible; the gate chain protects it |
| release-please config → publish jobs | a mis-scoped gate could publish sigra on every core release |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-137-13 | Tampering (supply chain) | release-as: "0.1.0" one-shot | high | mitigate | The one-shot pin is required for first publish but re-targets 0.1.0 forever if left; release-as-cleanup fires on sigra_release_created → strip_release_as.py crosswake_sigra (PROOF-03 auto). Verified by the cleanup-wired grep (Task 3). |
| T-137-14 | Tampering (over-publish) | sigra job gating | high | mitigate | All sigra jobs gate on the per-component sigra_release_created, never the aggregate releases_created — a core-only release never publishes sigra (Task 3). |
| T-137-15 | Denial of Service (bad build) | publish-hex-sigra CROSSWAKE_RELEASE | medium | mitigate | CROSSWAKE_RELEASE=1 on all mix steps so hex.build sees the `{:crosswake, "~> 0.1"}` Hex dep, not the path: dep it rejects (Task 3). |
| T-137-16 | Spoofing (spurious engine) | clean-room-proof-sigra | medium | mitigate | The no-engine script mode (Task 1) prevents the rulestead/Rulestead defaults from adding a phantom engine dep + smoke assertion that would fail the sigra clean-room (RESEARCH Pitfall 5). |
| T-137-SC | Tampering | npm/pip/cargo installs | low | accept | No package installs; CI/config-only changes. crosswake_sigra is first-party (RESEARCH Package Legitimacy Audit). |
</threat_model>

<verification>
- `python3 -c "import json..."` — config + manifest valid; sigra component independent; manifest entry present.
- `python3 -c "import yaml..."` — release-please.yml valid.
- grep confirms publish-hex-sigra, clean-room-proof-sigra, sigra_release_created, strip_release_as.py crosswake_sigra all present.
- `bash -n script/verify_companion_cleanroom.sh` + NO_ENGINE branch present.
</verification>

<success_criteria>
- crosswake_sigra is an independent elixir release-please component (not lockstep) with the one-shot release-as.
- release-please.yml has sigra outputs + publish-hex-sigra + clean-room-proof-sigra, per-component gated, with cleanup + failure alerting extended.
- verify_companion_cleanroom.sh cleanly handles the no-engine sigra invocation.
</success_criteria>

<output>
Create `.planning/phases/137-crosswake-sigra-extraction/137-04-SUMMARY.md` when done
</output>