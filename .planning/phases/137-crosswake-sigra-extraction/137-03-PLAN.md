---
phase: 137-crosswake-sigra-extraction
plan: 03
type: execute
wave: 3
depends_on: [137-02]
files_modified:
  - packages/crosswake_sigra/mix.exs
  - packages/crosswake_sigra/README.md
  - packages/crosswake_sigra/CHANGELOG.md
  - packages/crosswake_sigra/LICENSE
  - packages/crosswake_sigra/config/config.exs
  - packages/crosswake_sigra/test/test_helper.exs
  - packages/crosswake_sigra/test/support/study_session_live.ex
  - packages/crosswake_sigra/lib/crosswake/companions/sigra.ex
  - packages/crosswake_sigra/lib/crosswake/companions/sigra/
  - packages/crosswake_sigra/test/crosswake/
  - lib/crosswake/companions/sigra.ex
  - lib/crosswake/companions/sigra/
  - test/crosswake/companions/sigra/
  - test/crosswake/proof/phase54_sigra_session_authority_test.exs
  - test/crosswake/proof/phase54_sigra_support_truth_test.exs
  - mix.exs
autonomous: true
requirements: [SIGRA-01, SIGRA-02]
must_haves:
  truths:
    - "all Crosswake.Companions.Sigra.* sub-modules resolve from packages/crosswake_sigra/ (namespace preserved)"
    - "packages/crosswake_sigra compiles --warnings-as-errors and its moved+split tests pass in the package lane"
    - "no Crosswake.Companions.Sigra reference remains in core lib/ and no Crosswake.Shell.Denial reference exists inside packages/crosswake_sigra/lib/"
    - "the non-vacuous clean-room proof registers sigra via put_env and asserts RouteGate yields :step_up_required (not :dependency_missing)"
    - "core depends on crosswake_sigra via path: (dress-rehearsal) and the full core suite stays green"
  artifacts:
    - "packages/crosswake_sigra/ — mix.exs (@version 0.1.0), config, README, CHANGELOG, LICENSE, test_helper, test/support"
    - "packages/crosswake_sigra/test/crosswake/proof/phase137_sigra_cleanroom_test.exs — non-vacuous clean-room proof"
    - "test/crosswake/proof/phase54_sigra_support_truth_test.exs — the SupportMatrix.auth_contract_truth/0 assertion retained in core"
  key_links:
    - "core mix.exs :companions env — Crosswake.Companions.Sigra removed; core no longer compiles the facade"
    - "clean-room proof Application.put_env(:crosswake, :companions, [Crosswake.Companions.Sigra]) — without it the denial is :dependency_missing (vacuous)"
    - "phase54 split: Evaluator/DenialCodes/Contracts assertions → package; auth_contract_truth/0 → core (needs sigra registered via path dep + put_env)"
---

<objective>
Perform the mechanical extraction (SIGRA-01) on the now-Finding-clean sigra source: create the
`packages/crosswake_sigra/` skeleton by cloning the rindle package block (dropping the engine dep —
sigra has no engine), MOVE all `Crosswake.Companions.Sigra.*` source (namespace preserved), MOVE
the sigra-internal tests and SPLIT phase54 per D-137-D, add the non-vacuous clean-room proof
(audit fix ③), remove sigra from core (`lib/` files + the `:companions` env entry), and wire core
to depend on `crosswake_sigra` via a `path:` dep for the dress rehearsal.

Implements SIGRA-01 (source + tests move, namespace preserved), the packaging half of SIGRA-02
(DenialCodes/sanitize live in the package; no Denial reference inside it), and D-137-D (test split,
clean-room non-vacuity, StubSigraAbsentCompanion already added in Plan 01).

Resolves RESEARCH Open Question 3 concretely: the phase54 `SupportMatrix.auth_contract_truth/0`
assertion stays in core in a new `phase54_sigra_support_truth_test.exs` that registers sigra (via
the path-dep + `Application.put_env`) so `auth_contract_truth/0` populates denial_codes from the
runtime registry — otherwise it returns the `[]` sentinel and the assertion is vacuous.

Purpose: This is the irreversible-adjacent structural move; getting the split + non-vacuity right
is what makes the publish gate (Plans 04-05) trustworthy.
Output: standalone crosswake_sigra package (green), core with sigra removed (green), path-dep dress
rehearsal green.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/137-crosswake-sigra-extraction/137-CONTEXT.md
@.planning/phases/137-crosswake-sigra-extraction/137-RESEARCH.md
@.planning/phases/137-crosswake-sigra-extraction/137-VALIDATION.md
@.planning/phases/137-crosswake-sigra-extraction/137-02-SUMMARY.md
@script/extract_companion.md
</context>

<artifacts_this_phase_produces>
This plan (03) produces:
- `packages/crosswake_sigra/` full skeleton: `mix.exs` (@version "0.1.0" # x-release-please-version), `config/config.exs`, `README.md`, `CHANGELOG.md`, `LICENSE`, `test/test_helper.exs`, `test/support/study_session_live.ex`
- moved source: `lib/crosswake/companions/sigra.ex` + `lib/crosswake/companions/sigra/{auth_return,contracts,denial_codes,evaluator,handoff,step_up,step_up_ceremony,telemetry}.ex`
- moved tests: `contracts_test.exs`, `handoff_test.exs`, `step_up_test.exs`, `telemetry_test.exs`, plus phase56 + phase73 sigra-internal portions
- split: `phase54_sigra_session_authority_test.exs` (6 tests) → package; `phase54_sigra_support_truth_test.exs` (SupportMatrix assertion) → core (NEW)
- NEW: `packages/crosswake_sigra/test/crosswake/proof/phase137_sigra_cleanroom_test.exs`
- core changes: `Crosswake.Companions.Sigra` removed from `mix.exs` `:companions` env; facade + source deleted from core `lib/`; core `mix.exs` gains `{:crosswake_sigra, path: "packages/crosswake_sigra"}` (dress-rehearsal, env-conditional)
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold packages/crosswake_sigra/ (clone rindle, drop engine), move source, remove sigra from core</name>
  <files>packages/crosswake_sigra/mix.exs, packages/crosswake_sigra/config/config.exs, packages/crosswake_sigra/README.md, packages/crosswake_sigra/CHANGELOG.md, packages/crosswake_sigra/LICENSE, packages/crosswake_sigra/test/test_helper.exs, packages/crosswake_sigra/test/support/study_session_live.ex, packages/crosswake_sigra/lib/crosswake/companions/, lib/crosswake/companions/sigra.ex, lib/crosswake/companions/sigra/, mix.exs</files>
  <read_first>
    - packages/crosswake_rindle/mix.exs (full — precedent for @version, package/0, description, files:; NOTE: DROP the engine dep + engine_present elixirc_paths branch — sigra has no engine)
    - packages/crosswake_rindle/test/test_helper.exs (ExUnit.start exclude tags)
    - packages/crosswake_rindle/test/support/study_session_live.ex (copy verbatim), packages/crosswake_rindle/config/config.exs, README/CHANGELOG/LICENSE shape
    - packages/crosswake_rulestead/mix.exs L4, L83-98 (package block precedent)
    - 137-RESEARCH.md § Recommended Package Structure, § Pattern 1 (mix.exs), § No Engine Optional Dep, Wave 0 Requirements
    - mix.exs (core) — the `env: [companions: [Crosswake.Companions.Sigra, Crosswake.Companions.Chimeway]]` at ~L30-32 and the "remove Sigra when extracted" comment
    - script/extract_companion.md Steps 1, 3-5 (source move + mix.exs)
    - lib/crosswake/companions/sigra.ex + lib/crosswake/companions/sigra/*.ex (the 8 sub-modules + facade to move — enumerated in RESEARCH § Source Files to Move)
  </read_first>
  <action>
    Create `packages/crosswake_sigra/mix.exs` by cloning the rindle package block: `@version "0.1.0" # x-release-please-version` (first-publish one-shot, independent of core 0.1.2), `app: :crosswake_sigra`, `elixir: "~> 1.19"`, `elixirc_paths(:test) -> ["lib", "test/support"]` / `elixirc_paths(_) -> ["lib"]` (NO engine_present branch), `deps/0` returning only the env-conditional `crosswake_dep/0` (`{:crosswake, "~> 0.1"}` when `CROSSWAKE_RELEASE == "1"`, else `{:crosswake, path: "../.."}`) — NO optional engine dep, description "Sigra auth companion adapter for the Crosswake route-policy system.", `package/0` with `licenses: ["Apache-2.0"]`, the standard links map (Changelog/Documentation/GitHub for crosswake_sigra), and `files: ~w(lib mix.exs README.md LICENSE CHANGELOG.md)` (test/ excluded).
    Create `config/config.exs` (minimal — no flag_source needed), `README.md`, `CHANGELOG.md`, `LICENSE` (Apache-2.0, clone rindle's), and `test/test_helper.exs` with `ExUnit.start(exclude: [:requires_example_host, :advisory_only])` (mirror rindle minus :engine_present since there is no engine lane). Copy `test/support/study_session_live.ex` verbatim from crosswake_rindle/test/support/.
    MOVE the 8 sub-modules + the facade from `lib/crosswake/companions/sigra{.ex,/}` into `packages/crosswake_sigra/lib/crosswake/companions/` preserving the `Crosswake.Companions.Sigra.*` module names (namespace-preserving = non-breaking). Delete them from core `lib/`.
    In core `mix.exs`: remove `Crosswake.Companions.Sigra` from the `:companions` env list (leaving Chimeway), and add an env-conditional dress-rehearsal dep `{:crosswake_sigra, path: "packages/crosswake_sigra"}` (added when NOT `CROSSWAKE_RELEASE=1`, so the dress rehearsal exercises the package via path while the Hex build stays clean — mirror how core references rulestead/rindle for their dress rehearsals; verify the exact core-side pattern against how rindle's path dep is wired).
    Run `cd packages/crosswake_sigra && mix deps.get`.
  </action>
  <verify>
    <automated>cd packages/crosswake_sigra && mix deps.get && mix compile --warnings-as-errors</automated>
    <automated>grep -rn "Crosswake.Companions.Sigra" lib/; test $(grep -rc "Crosswake.Companions.Sigra" lib/ | awk -F: '{s+=$2} END{print s+0}') -eq 0</automated>
    <automated>grep -rn "Crosswake.Shell.Denial" packages/crosswake_sigra/lib/; test $(grep -rc "Crosswake.Shell.Denial" packages/crosswake_sigra/lib/ | awk -F: '{s+=$2} END{print s+0}') -eq 0</automated>
  </verify>
  <acceptance_criteria>
    - packages/crosswake_sigra compiles --warnings-as-errors with all 8 sub-modules + facade under the preserved namespace
    - mix.exs has @version "0.1.0", no engine dep, no engine_present elixirc_paths branch
    - zero Crosswake.Companions.Sigra references remain in core lib/
    - zero Crosswake.Shell.Denial references inside packages/crosswake_sigra/lib/
    - Crosswake.Companions.Sigra removed from core :companions env; core mix.exs has the path-dep dress-rehearsal wiring
  </acceptance_criteria>
  <done>The standalone package exists and compiles; sigra source is gone from core; core is wired to the package via path dep for the dress rehearsal.</done>
</task>

<task type="auto">
  <name>Task 2: Move sigra-internal tests and split phase54 (SupportMatrix assertion stays in core)</name>
  <files>packages/crosswake_sigra/test/crosswake/companions/sigra/, packages/crosswake_sigra/test/crosswake/proof/phase54_sigra_session_authority_test.exs, test/crosswake/companions/sigra/, test/crosswake/proof/phase54_sigra_session_authority_test.exs, test/crosswake/proof/phase54_sigra_support_truth_test.exs, test/crosswake/proof/phase56_step_up_ceremony_test.exs, test/crosswake/proof/phase73_auth_sensitive_admin_workflow_proof_test.exs, packages/crosswake_sigra/test/crosswake/proof/phase56_step_up_ceremony_test.exs, packages/crosswake_sigra/test/crosswake/proof/phase73_auth_sensitive_admin_workflow_proof_test.exs</files>
  <read_first>
    - 137-CONTEXT.md D-137-D test-split table + 137-RESEARCH.md § Test Split Verification (the phase54 per-test lane table) + § phase54 SupportMatrix assertion in core (Open Question 3)
    - test/crosswake/companions/sigra/{contracts,handoff,step_up,telemetry}_test.exs (the 4 straight MOVEs)
    - test/crosswake/proof/phase54_sigra_session_authority_test.exs (full — the 7 tests; 6 move, the "support truth locks route posture..." test at L160-177 STAYS in core; the head aliases)
    - test/crosswake/proof/phase56_step_up_ceremony_test.exs + phase73_...exs (sigra-internal unit portions move; the @tag :requires_example_host host-DB cases stay gated in core)
    - lib/crosswake/support_matrix/support_matrix.ex auth_contract_truth/0 (reads denial_codes from the :companions runtime registry — the core-retained test must register sigra to populate it)
  </read_first>
  <action>
    MOVE the 4 sigra-internal unit tests (`contracts_test.exs`, `handoff_test.exs`, `step_up_test.exs`, `telemetry_test.exs`) from `test/crosswake/companions/sigra/` to `packages/crosswake_sigra/test/crosswake/companions/sigra/`; delete from core.
    SPLIT `phase54_sigra_session_authority_test.exs`: MOVE the 6 tests that call sigra internals (Contracts.*, DenialCodes.*, Evaluator.evaluate_route_auth/3, and the self-referential source-text test) into `packages/crosswake_sigra/test/crosswake/proof/phase54_sigra_session_authority_test.exs`; RETAIN only the "support truth locks route posture vocabulary, denial codes, and later-phase non-claims" test in core, relocated into a NEW `test/crosswake/proof/phase54_sigra_support_truth_test.exs`. Because that test asserts `row.denial_codes == DenialCodes.codes()` and `auth_contract_truth/0` sources denial_codes from the `:companions` runtime registry, the core-retained test MUST register sigra in setup — `Application.put_env(:crosswake, :companions, [Crosswake.Companions.Sigra])` with `on_exit` restore (sigra is available in core via the Task-1 path dep, so `Crosswake.Companions.Sigra.DenialCodes` resolves) — otherwise denial_codes is the `[]` sentinel and the assertion is vacuous. If aliasing `DenialCodes` from the package in a core test file is undesirable, assert against the registry-populated `auth_contract_truth()` row's denial_codes directly rather than re-deriving from `DenialCodes.codes()`; keep the posture/vocabulary assertions unchanged.
    MOVE the sigra-internal unit portions of phase56 and phase73 into the package (they directly drive `StepUpCeremony`/`DenialCodes`/`Evaluator` and cannot resolve from core-only deps post-extraction); KEEP the `@tag :requires_example_host` host-DB-coupled cases in core (they need the example host and are excluded by the core tag filter — split the file if it mixes both, or move the whole file to the package if every retained core case is host-tagged). Decide per file by inspecting which tests survive in a core-only (no sigra dep at release time) context; document the split in the SUMMARY.
    Ensure each moved test file's aliases resolve inside the package (Crosswake.Companions.Sigra.* + Crosswake.Compatibility.*).
  </action>
  <verify>
    <automated>cd packages/crosswake_sigra && mix test</automated>
    <automated>mix test test/crosswake/proof/phase54_sigra_support_truth_test.exs</automated>
    <automated>grep -rn "Crosswake.Companions.Sigra" test/crosswake/companions/sigra/ 2>/dev/null; test ! -d test/crosswake/companions/sigra || test -z "$(ls -A test/crosswake/companions/sigra 2>/dev/null)"</automated>
  </verify>
  <acceptance_criteria>
    - the 4 unit tests + 6 phase54 tests + sigra-internal phase56/phase73 portions pass in the package lane
    - phase54_sigra_support_truth_test.exs stays in core, registers sigra, and its denial_codes assertion is non-vacuous (populated, not [])
    - no sigra-internal test remains in core test/crosswake/companions/sigra/
    - package lane `mix test` is green
  </acceptance_criteria>
  <done>Tests are split exactly per D-137-D; the SupportMatrix assertion stays non-vacuous in core; the package test lane is green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Non-vacuous clean-room proof + dress-rehearsal green</name>
  <files>packages/crosswake_sigra/test/crosswake/proof/phase137_sigra_cleanroom_test.exs</files>
  <read_first>
    - 137-RESEARCH.md § Code Examples — "Clean-room proof setup (D-137-D audit fix ③)" (the full drafted test with put_env setup, AuthRouter, RouteGate.evaluate/4, the :step_up_required + != :dependency_missing assertions)
    - 137-VALIDATION.md § Backstop Tests #1 (non-vacuity requirement)
    - lib/crosswake/compatibility/route_gate.ex evaluate/4 signature (L33) + Decision shape (denial: Denial.t() | nil)
    - packages/crosswake_sigra/test/support/study_session_live.ex (the LiveView the AuthRouter mounts)
    - packages/crosswake_rindle proof test that drives RouteGate for shape precedent
  </read_first>
  <behavior>
    - Test: with `Application.put_env(:crosswake, :companions, [Crosswake.Companions.Sigra])` in setup (on_exit restore), compiling an auth-predicated route (auth_min_level: :mfa) and driving RouteGate.evaluate/4 yields decision.status == :deny AND decision.denial.reason == :step_up_required.
    - Non-vacuity guard: explicitly `refute decision.denial.reason == :dependency_missing` — proving registry dispatch + Finding→Denial translation actually ran (without the put_env this would be :dependency_missing and the test would fail, catching a vacuous setup).
  </behavior>
  <action>
    Create `packages/crosswake_sigra/test/crosswake/proof/phase137_sigra_cleanroom_test.exs` following the RESEARCH § Code Examples draft: `use ExUnit.Case, async: false`; a `defmodule AuthRouter` using `Crosswake.Router` with an auth-predicated route (`auth_min_level: :mfa`) mounting `Crosswake.TestSupport.StudySessionLive`; a `setup` block that saves the current `:companions` env, `Application.put_env(:crosswake, :companions, [Crosswake.Companions.Sigra])`, and `on_exit` restores the original; one test that compiles the manifest, builds a Target from the manifest host origin, calls `RouteGate.evaluate/4`, and asserts `decision.status == :deny`, `decision.denial.reason == :step_up_required`, and `refute decision.denial.reason == :dependency_missing`. Adapt exact aliases/manifest-compile call to whatever the package's other proof tests use (mirror phase54 in-package for the compile+evaluate idiom).
    Then run the dress rehearsal: the core suite with `crosswake_sigra` as the `path:` dep (from Plan 03 Task 1) must be green, and the package lane must be green.
  </action>
  <verify>
    <automated>cd packages/crosswake_sigra && mix test test/crosswake/proof/phase137_sigra_cleanroom_test.exs</automated>
    <automated>cd packages/crosswake_sigra && mix test</automated>
    <automated>mix test --exclude requires_example_host --exclude engine_present</automated>
  </verify>
  <acceptance_criteria>
    - the clean-room proof asserts :step_up_required AND refutes :dependency_missing (non-vacuous)
    - the proof passes in the package lane
    - the path-dep dress rehearsal (core suite) is green
    - the full package lane is green
  </acceptance_criteria>
  <done>A non-vacuous clean-room proof exists proving registry dispatch + Finding→Denial translation with only crosswake + crosswake_sigra; the dress rehearsal is green end-to-end.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| package ↔ core (path dep) | dress rehearsal exercises the extracted package as an external dep; namespace preservation must hold |
| host config → :companions registry | packages cannot self-register; registration is Application-env based and host-owned |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-137-09 | Tampering (test bypass) | phase137_sigra_cleanroom_test.exs | high | mitigate | Setup MUST put_env the companion and the test MUST `refute :dependency_missing`; without registration the fail-closed path yields :dependency_missing and the proof would be vacuously green. Non-vacuity is the explicit guard (Task 3). |
| T-137-10 | Spoofing (fail-open regression) | sigra removed from core :companions env | high | mitigate | Removing sigra from core's env + StubSigraAbsentCompanion (Plan 01) ensures a core-only build fails closed (:dependency_missing) on auth routes, never a silent allow. Verified by the retained core fail-closed proofs staying green. |
| T-137-11 | Information Disclosure | DenialCodes.sanitize_details/1 lives in package | medium | mitigate | sanitize_details moves WITH sigra so PII scrub stays at source; grep-gate confirms no Denial leak and the package owns the allowlist. Verified by the no-Denial grep + package lane green. |
| T-137-12 | Tampering | phase54 split vacuity | medium | mitigate | The core-retained SupportMatrix assertion registers sigra so denial_codes is populated (not the [] sentinel) — a vacuous [] == [] pass is prevented by asserting against the registered codes (Task 2). |
| T-137-SC | Tampering | npm/pip/cargo installs | low | accept | No new external packages; crosswake_sigra is first-party (RESEARCH Package Legitimacy Audit: first-party, no registry check applicable). |
</threat_model>

<verification>
- `cd packages/crosswake_sigra && mix compile --warnings-as-errors && mix test` — package compiles and its moved/split/new tests pass.
- `grep -rc "Crosswake.Companions.Sigra" lib/` sums to 0; `grep -rc "Crosswake.Shell.Denial" packages/crosswake_sigra/lib/` sums to 0.
- `mix test test/crosswake/proof/phase54_sigra_support_truth_test.exs` — non-vacuous SupportMatrix assertion in core.
- `mix test --exclude requires_example_host --exclude engine_present` — path-dep dress rehearsal green.
</verification>

<success_criteria>
- crosswake_sigra is a standalone package (namespace preserved) that compiles and passes its lane.
- No sigra source/tests remain in core except the intentionally-retained core proofs (phase46, phase54 SupportMatrix, host-tagged phase56/73).
- The clean-room proof is non-vacuous (:step_up_required, not :dependency_missing).
- The path-dep dress rehearsal (core + package) is green before any publish wiring.
</success_criteria>

<output>
Create `.planning/phases/137-crosswake-sigra-extraction/137-03-SUMMARY.md` when done
</output>
