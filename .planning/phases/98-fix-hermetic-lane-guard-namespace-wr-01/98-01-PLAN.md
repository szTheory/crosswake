---
phase: "98-fix-hermetic-lane-guard-namespace-wr-01"
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - test/crosswake/proof/phase96_threadline_docs_contract_test.exs
autonomous: true
requirements:
  - WR-01
must_haves:
  truths:
    - "The hermetic lane self-guard properly uses Regex.match? with an escaped pattern to prevent the anti-quine bug."
    - "The broken string concatenation is completely removed."
    - "The test suite passes cleanly, proving the new regex guard does not trigger itself."
  artifacts:
    - path: "test/crosswake/proof/phase96_threadline_docs_contract_test.exs"
      provides: "Hermetic lane self-guard"
  key_links:
    - from: "test/crosswake/proof/phase96_threadline_docs_contract_test.exs"
      to: "Regex.match?/2"
      pattern: "Regex.match\\?\\(~r/Crosswake\\\\\\.Example\\\\\\./, source\\)"
---

<objective>
Fix the broken string concatenation in the hermetic lane self-guard to properly check for `Crosswake.Example.` namespace occurrences without triggering the guard itself.

Purpose: The previous guard `"Crosswake" <> "Example."` evaluates to `"CrosswakeExample."`, which misses the dot and fails to correctly enforce the hermetic isolation of the test from the example host (WR-01).
Output: A corrected `phase96_threadline_docs_contract_test.exs` using an escaped `Regex.match?` pattern.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Bug context and constraints
@.planning/phases/98-fix-hermetic-lane-guard-namespace-wr-01/98-CONTEXT.md
@.planning/phases/98-fix-hermetic-lane-guard-namespace-wr-01/98-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace String.contains? with Regex.match? in hermetic lane guard</name>
  <read_first>
    - test/crosswake/proof/phase96_threadline_docs_contract_test.exs
  </read_first>
  <action>
    Modify the hermetic lane self-guard assertion around line 44.
    Replace: `refute String.contains?(source, "Crosswake" <> "Example.")`
    With: `refute Regex.match?(~r/Crosswake\.Example\./, source)`
    Leave the error message string unchanged. Do not alter any other tests.
    This fulfills D-01: Shift from `String.contains?` with broken concatenation to `Regex.match?` with an escaped pattern.
  </action>
  <acceptance_criteria>
    - `test/crosswake/proof/phase96_threadline_docs_contract_test.exs` contains `refute Regex.match?(~r/Crosswake\.Example\./, source)`
    - `test/crosswake/proof/phase96_threadline_docs_contract_test.exs` DOES NOT contain `String.contains?(source, "Crosswake" <> "Example.")`
    - `mix test test/crosswake/proof/phase96_threadline_docs_contract_test.exs` exits 0 with 0 failures
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Internal Test Suite | Elixir test runner executing local code |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-98-01 | Tampering | test/crosswake/proof/phase96_threadline_docs_contract_test.exs | accept | Internal test file modification only; no external attack surface. |
</threat_model>

<verification>
mix test test/crosswake/proof/phase96_threadline_docs_contract_test.exs passes with 0 failures.
</verification>

<success_criteria>
- The hermetic lane self-guard in `test/crosswake/proof/phase96_threadline_docs_contract_test.exs` is updated to use `Regex.match?` with `~r/Crosswake\.Example\./`.
- The test file runs and passes cleanly.
</success_criteria>

<output>
Create `.planning/phases/98-fix-hermetic-lane-guard-namespace-wr-01/98-01-SUMMARY.md` when done
</output>
