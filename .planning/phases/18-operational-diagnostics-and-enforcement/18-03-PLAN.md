---
phase: 18-operational-diagnostics-and-enforcement
plan: "03"
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/crosswake/manifest/types.ex
  - lib/crosswake/manifest/builder.ex
  - lib/crosswake/support_matrix/support_matrix.ex
  - lib/crosswake/support_matrix/renderer.ex
  - guides/support_matrix.md
  - test/crosswake/manifest/manifest_test.exs
  - test/crosswake/support_matrix/support_matrix_test.exs
  - test/crosswake/support_matrix/renderer_test.exs
autonomous: true
requirements:
  - OPS-SUPPORT
must_haves:
  truths:
    - "Crosswake publishes baseline platform support separately from proof-lane verification state and capability-family posture."
    - "Android support remains structurally marked as `verification_required` where the Phase 17 JVM proof is still unresolved."
    - "`notification_token` renders as advisory and companion-leaning rather than as a merge-blocking core family."
    - "`deep_link` is described as activation-first truth instead of another route-local bounded bridge capability."
  artifacts:
    - path: lib/crosswake/support_matrix/support_matrix.ex
      provides: "Canonical support data with separated baseline, proof, and capability posture"
    - path: lib/crosswake/support_matrix/renderer.ex
      provides: "Public Markdown rendering of support truth"
    - path: guides/support_matrix.md
      provides: "Generated support guide aligned to the canonical matrix"
  key_links:
    - from: lib/crosswake/manifest/builder.ex
      to: lib/crosswake/support_matrix/support_matrix.ex
      via: "manifest capability metadata drives capability-family support rows"
      pattern: "capability_catalog|proof_class|prerequisites|denial|fallback"
    - from: lib/crosswake/support_matrix/renderer.ex
      to: guides/support_matrix.md
      via: "deterministic guide generation"
      pattern: "verification required|deep_link|notification_token|file_picker"
---

<objective>
Finish the public support-truth model for v3.1 so rendered docs expose baseline support, pending proof, and capability-family posture without caveat-only prose.

Purpose: Support claims are part of the product surface and must stay structurally honest.
Output: Updated support-matrix schema/renderer, manifest-derived capability-family metadata, and regenerated docs/tests aligned with the unresolved Android proof lane.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-operational-diagnostics-and-enforcement/18-CONTEXT.md
@.planning/phases/18-operational-diagnostics-and-enforcement/18-RESEARCH.md
@.planning/phases/17-user-prompted-capabilities/17-01-SUMMARY.md
@.planning/phases/17-user-prompted-capabilities/17-02-SUMMARY.md
@.planning/phases/17-user-prompted-capabilities/17-04-SUMMARY.md

<interfaces>
From lib/crosswake/manifest/types.ex:
```elixir
@type status :: :supported | :verification_required | :unsupported
def new_support_matrix(attrs) when is_list(attrs) do
  ...
end
def new_capability_support_entry(attrs) when is_list(attrs) do
  ...
end
```

From lib/crosswake/support_matrix/support_matrix.ex:
```elixir
def canonical(opts \\ []) do
  ...
  capability_families: capability_family_entries(capability_registry),
  ...
end
```

Current support guide row in lib/crosswake/support_matrix/renderer.ex:
```elixir
"| #{entry.family} | #{Atom.to_string(entry.owner)} | #{format_package_class(entry.package_class)} | #{format_proof_class(entry.proof_class)} | #{format_rebuild(entry.rebuild)} | #{prerequisites} | #{entry.denial || "-"} | #{entry.fallback || "-"} | #{guide} |"
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Separate baseline support from proof and capability posture in canonical data</name>
  <read_first>
    lib/crosswake/manifest/types.ex
    lib/crosswake/manifest/builder.ex
    lib/crosswake/support_matrix/support_matrix.ex
    .planning/phases/18-operational-diagnostics-and-enforcement/18-CONTEXT.md
    test/crosswake/manifest/manifest_test.exs
    test/crosswake/support_matrix/support_matrix_test.exs
  </read_first>
  <files>lib/crosswake/manifest/types.ex, lib/crosswake/manifest/builder.ex, lib/crosswake/support_matrix/support_matrix.ex, test/crosswake/manifest/manifest_test.exs, test/crosswake/support_matrix/support_matrix_test.exs</files>
  <action>Per D-04, D-15, D-16, D-17, D-18, and D-19, extend the typed manifest/support structures so baseline platform support rows, proof-lane verification state, and capability-family posture are represented as separate fields instead of one overloaded status. Update capability metadata and canonical support construction so Android rows can stay baseline-supported while the pending Phase 17 JVM lane renders `verification_required`; keep `notification_token` advisory/companion-sensitive; and express `deep_link` as activation-first truth rather than another bounded-bridge allowlist story.</action>
  <acceptance_criteria>
    `mix test test/crosswake/manifest/manifest_test.exs test/crosswake/support_matrix/support_matrix_test.exs` passes.
    `rg -n "verification_required|deep_link|notification_token|file_picker|proof_class" lib/crosswake/manifest/builder.ex lib/crosswake/support_matrix/support_matrix.ex lib/crosswake/manifest/types.ex` shows the separated support dimensions and capability posture metadata.
    `rg -n "deep_link|notification_token|verification_required" test/crosswake/support_matrix/support_matrix_test.exs test/crosswake/manifest/manifest_test.exs` proves the new structure is asserted directly.
  </acceptance_criteria>
  <verify>
    <automated>mix test test/crosswake/manifest/manifest_test.exs test/crosswake/support_matrix/support_matrix_test.exs</automated>
  </verify>
  <done>Canonical support truth carries explicit fields for baseline support and pending proof, and manifest-derived family rows reflect the exact Phase 18 posture for `deep_link`, `notification_token`, and `file_picker`.</done>
</task>

<task type="auto">
  <name>Task 2: Regenerate public support docs from the canonical Phase 18 truth</name>
  <read_first>
    lib/crosswake/support_matrix/renderer.ex
    guides/support_matrix.md
    test/crosswake/support_matrix/renderer_test.exs
  </read_first>
  <files>lib/crosswake/support_matrix/renderer.ex, guides/support_matrix.md, test/crosswake/support_matrix/renderer_test.exs</files>
  <action>Per D-05, D-15, D-18, and D-19, update the support renderer and generated support guide so the public docs show baseline support, proof status, rebuild posture, prerequisites, denial behavior, fallback, and guide links without burying pending proof in footnotes. Fix the current `deep_link` ambiguity by rendering it as activation-first rather than a route-local bridge family, keep `file_picker` explicitly transfer-backed in the support guide, and ensure the regenerated `guides/support_matrix.md` exactly matches the renderer output.</action>
  <acceptance_criteria>
    `mix test test/crosswake/support_matrix/renderer_test.exs` passes.
    `test "$(cat guides/support_matrix.md)" = "$(mix run -e 'IO.write(Crosswake.SupportMatrix.Renderer.render(Crosswake.SupportMatrix.canonical()))')"` exits 0.
    `rg -n "verification required|activation-first|native_picker|notification_token" guides/support_matrix.md` matches the updated public wording.
  </acceptance_criteria>
  <verify>
    <automated>mix test test/crosswake/support_matrix/renderer_test.exs</automated>
  </verify>
  <done>The published support guide is generated from canonical code, reflects the unresolved Android proof structurally, and explains `deep_link` and `file_picker` according to the locked architecture boundaries.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Manifest metadata -> support matrix | Capability metadata becomes public support truth and must not drift from actual runtime boundaries. |
| Support matrix -> generated guides | Rendered docs are a release artifact and can overclaim support if the renderer hides verification gaps. |
| Baseline support -> proof status | Conflating these dimensions creates false operator confidence about unproven platforms. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-07 | Information Disclosure | support matrix renderer | mitigate | Render `verification_required` and proof metadata structurally so pending Android proof cannot masquerade as support per D-16 and D-18. |
| T-18-08 | Repudiation | generated guides | mitigate | Generate guides from canonical support code and assert exact renderer parity in tests to prevent hand-edited drift per D-05. |
| T-18-09 | Tampering | capability posture metadata | mitigate | Keep capability-family rows manifest-derived and typed so `deep_link`, `notification_token`, and `file_picker` retain their intended boundaries per D-04, D-17, and D-19. |
</threat_model>

<verification>
Run `mix test test/crosswake/manifest/manifest_test.exs test/crosswake/support_matrix/support_matrix_test.exs test/crosswake/support_matrix/renderer_test.exs`.
</verification>

<success_criteria>
Support rows and public guides clearly separate baseline support from pending proof, keep Android Phase 17 JVM work marked `verification_required`, and describe `deep_link`, `notification_token`, and `file_picker` with the exact posture required by Phase 18.
</success_criteria>

<output>
After completion, create `.planning/phases/18-operational-diagnostics-and-enforcement/18-03-SUMMARY.md`
</output>
