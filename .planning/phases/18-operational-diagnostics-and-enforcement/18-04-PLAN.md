---
phase: 18-operational-diagnostics-and-enforcement
plan: "04"
type: execute
wave: 3
depends_on:
  - 18-01
  - 18-02
  - 18-03
autonomous: false
files_modified:
  - test/crosswake/proof/phase18_bounded_family_lane_test.exs
  - test/crosswake/proof/phase18_deep_link_activation_lane_test.exs
  - test/crosswake/shell/activation_test.exs
  - examples/ios_shell_host/CrosswakeShellTests/BridgeChannelTests.swift
  - examples/android_shell_host/app/src/test/java/dev/crosswake/shell/BridgeChannelTest.kt
requirements:
  - OPS-DOCTOR
  - OPS-SUPPORT
must_haves:
  truths:
    - "Crosswake has one believable integrated proof slice for the bounded bridge families: `app_info`, `haptics`, `share`, `permissions.status`, `notification_token`, and `file_picker`."
    - "`deep_link` is proven separately through shell activation behavior rather than folded into the bridge lane."
    - "ExUnit invariants remain the fast merge-blocking truth anchors."
    - "The Android JVM proof lane is an explicit completion gate with the real Gradle command, not a silently skipped follow-up."
  artifacts:
    - path: test/crosswake/proof/phase18_bounded_family_lane_test.exs
      provides: "Integrated bounded-family acceptance proof"
    - path: test/crosswake/proof/phase18_deep_link_activation_lane_test.exs
      provides: "Separate deep-link activation acceptance proof"
    - path: examples/ios_shell_host/CrosswakeShellTests/BridgeChannelTests.swift
      provides: "Targeted iOS shell denials and bounded-family bridge proof"
  key_links:
    - from: test/crosswake/proof/phase18_bounded_family_lane_test.exs
      to: lib/crosswake/bridge/registry.ex
      via: "route-natural integrated family acceptance"
      pattern: "app_info|haptics|share|permissions\\.status|notification_token|file_picker"
    - from: test/crosswake/proof/phase18_deep_link_activation_lane_test.exs
      to: test/crosswake/shell/activation_test.exs
      via: "activation-first deep link proof"
      pattern: "deep_link|route_unavailable|source: :deep_link"
---

<objective>
Finish Phase 18 with layered proof that matches the architecture: fast ExUnit invariants, targeted shell tests, one bounded-family acceptance lane, and one separate deep-link activation lane.

Purpose: Crosswake support claims must be backed by believable proof without collapsing distinct runtime boundaries into one demo script.
Output: New Phase 18 proof files, shell-test updates, and an explicit Java-enabled Android verification checkpoint required to close the phase honestly.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-operational-diagnostics-and-enforcement/18-CONTEXT.md
@.planning/phases/18-operational-diagnostics-and-enforcement/18-RESEARCH.md
@.planning/phases/17-user-prompted-capabilities/17-03-SUMMARY.md
@.planning/phases/17-user-prompted-capabilities/17-04-SUMMARY.md

<interfaces>
From test/crosswake/shell/activation_test.exs:
```elixir
assert Enum.map(requests, & &1.source) == [:cold_start, :deep_link, :notification, :in_app_navigation]
```

From examples/ios_shell_host/CrosswakeShellTests/BridgeChannelTests.swift:
```swift
private func request(
    command: String,
    capability: String,
    correlationID: String,
    capabilities: [String: String],
    payload: [String: String] = [:]
) -> BridgeRequestEnvelope
```

From examples/android_shell_host/app/src/test/java/dev/crosswake/shell/BridgeChannelTest.kt:
```kotlin
private fun request(
    command: String,
    capability: String,
    payload: Map<String, String> = emptyMap(),
    capabilities: Map<String, String>,
    correlationId: String = "bridge-1"
): BridgeRequestEnvelope
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the layered ExUnit acceptance slices</name>
  <read_first>
    test/crosswake/proof/phase7_saas_lane_test.exs
    test/crosswake/proof/phase8_selective_native_lane_test.exs
    test/crosswake/proof/phase9_local_first_lane_test.exs
    test/crosswake/shell/activation_test.exs
    .planning/phases/18-operational-diagnostics-and-enforcement/18-CONTEXT.md
  </read_first>
  <files>test/crosswake/proof/phase18_bounded_family_lane_test.exs, test/crosswake/proof/phase18_deep_link_activation_lane_test.exs, test/crosswake/shell/activation_test.exs</files>
  <action>Per D-20 through D-25, add two explicit ExUnit proof slices instead of one hero lane. The bounded-family slice must exercise `app_info`, `haptics`, `share`, `permissions.status`, `notification_token`, and `file_picker` together in a route-natural flow that still checks transfer-backed picker authority. The deep-link slice must prove activation-first entry, route-unavailable behavior, and manifest path matching separately so `deep_link` does not become a bridge allowlist concern. Update shared activation tests only where needed to reinforce this boundary.</action>
  <acceptance_criteria>
    `mix test test/crosswake/proof/phase18_bounded_family_lane_test.exs test/crosswake/proof/phase18_deep_link_activation_lane_test.exs test/crosswake/shell/activation_test.exs` passes.
    `rg -n "notification_token|file_picker|permissions.status|deep_link|route_unavailable" test/crosswake/proof/phase18_bounded_family_lane_test.exs test/crosswake/proof/phase18_deep_link_activation_lane_test.exs test/crosswake/shell/activation_test.exs` shows the separated proof slices.
  </acceptance_criteria>
  <verify>
    <automated>mix test test/crosswake/proof/phase18_bounded_family_lane_test.exs test/crosswake/proof/phase18_deep_link_activation_lane_test.exs test/crosswake/shell/activation_test.exs</automated>
  </verify>
  <done>Phase 18 has one integrated bounded-family proof lane and one separate deep-link activation lane, each with route-natural assertions and no architecture collapse.</done>
</task>

<task type="auto">
  <name>Task 2: Extend platform shell proofs without pretending Android is locally complete</name>
  <read_first>
    examples/ios_shell_host/CrosswakeShellTests/BridgeChannelTests.swift
    examples/android_shell_host/app/src/test/java/dev/crosswake/shell/BridgeChannelTest.kt
    .planning/phases/17-user-prompted-capabilities/17-03-SUMMARY.md
    .planning/phases/17-user-prompted-capabilities/17-04-SUMMARY.md
    .planning/STATE.md
  </read_first>
  <files>examples/ios_shell_host/CrosswakeShellTests/BridgeChannelTests.swift, examples/android_shell_host/app/src/test/java/dev/crosswake/shell/BridgeChannelTest.kt</files>
  <action>Per D-21, D-22, and D-24, add only the targeted shell assertions needed for Phase 18 operational truth: family-first capability mapping, explicit denials, transfer-backed picker seams, and activation-bound deep-link posture. Run the iOS test suite locally. On Android, add the assertions and keep the plan’s acceptance tied to the repository truth that JVM execution is still pending in this environment; do not alter support posture to imply the Gradle lane has already passed. The code and tests should be ready for `java -version && (cd examples/android_shell_host && ./gradlew test --tests dev.crosswake.shell.BridgeChannelTest)` in CI or on a Java-enabled workstation, but the local Phase 18 proof must keep that lane represented as pending verification.</action>
  <acceptance_criteria>
    `xcodebuild test -project examples/ios_shell_host/CrosswakeShell.xcodeproj -scheme CrosswakeShell -destination 'platform=iOS Simulator,name=iPhone 17'` passes.
    `rg -n "notification_token|file_picker|share|deep_link|undeclared_capability|verification_required" examples/ios_shell_host/CrosswakeShellTests/BridgeChannelTests.swift examples/android_shell_host/app/src/test/java/dev/crosswake/shell/BridgeChannelTest.kt .planning/STATE.md` confirms the platform tests and pending Android verification truth are aligned.
    `rg -n "java -version && \\(cd examples/android_shell_host && \\./gradlew test --tests dev\\.crosswake\\.shell\\.BridgeChannelTest\\)" .planning/phases/18-operational-diagnostics-and-enforcement/18-04-PLAN.md` confirms the canonical Android proof command is preserved in the plan.
  </acceptance_criteria>
  <verify>
    <automated>xcodebuild test -project examples/ios_shell_host/CrosswakeShell.xcodeproj -scheme CrosswakeShell -destination 'platform=iOS Simulator,name=iPhone 17'</automated>
  </verify>
  <done>iOS shell proof runs locally, Android shell assertions are landed, and the plan leaves one explicit Java-enabled Android proof gate to close before the phase can be marked complete.</done>
</task>

<task type="auto">
  <name>Task 3: Close the Android JVM proof gate in CI or on a Java-enabled workstation</name>
  <read_first>
    examples/android_shell_host/app/src/test/java/dev/crosswake/shell/BridgeChannelTest.kt
    .planning/ROADMAP.md
    .planning/STATE.md
    .planning/phases/18-operational-diagnostics-and-enforcement/18-CONTEXT.md
  </read_first>
  <files>examples/android_shell_host/app/src/test/java/dev/crosswake/shell/BridgeChannelTest.kt, .planning/STATE.md</files>
  <action>Run the canonical Android proof command `java -version && (cd examples/android_shell_host && ./gradlew test --tests dev.crosswake.shell.BridgeChannelTest)` in CI or on a Java-enabled workstation after Tasks 1 and 2 land. If the command passes, update `.planning/STATE.md` and the execution summary to record that Phase 17/18 Android JVM proof is now closed. If it fails, record the failing step and keep support posture at `verification_required`; do not mark the phase complete until the Android shell proof genuinely passes.</action>
  <acceptance_criteria>
    `java -version && (cd examples/android_shell_host && ./gradlew test --tests dev.crosswake.shell.BridgeChannelTest)` exits 0 in a Java-enabled environment.
    `.planning/STATE.md` records either the passing Android proof closure date or the concrete failing command/result without changing support posture to supported prematurely.
    No execution summary for Phase 18 claims full shell proof completion unless both the iOS `xcodebuild test` command and the Android Gradle command have passed.
  </acceptance_criteria>
  <verify>
    <automated>java -version && (cd examples/android_shell_host && ./gradlew test --tests dev.crosswake.shell.BridgeChannelTest)</automated>
  </verify>
  <done>Phase 18 has an explicit cross-environment Android verification gate that must pass before the roadmap's two-platform proof claim is considered satisfied.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ExUnit proof lanes -> support claims | Acceptance tests become evidence for public support posture and must mirror real architecture boundaries. |
| iOS/Android shell tests -> bridge/runtime contract | Platform-specific proofs validate the command-aware bridge and activation contracts. |
| Local workstation -> Android proof status | Missing Java must not be translated into a false claim that Android verification has completed. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-10 | Repudiation | proof artifacts | mitigate | Split bounded-family and deep-link proof lanes so each support claim maps to a specific verified artifact per D-20 through D-25. |
| T-18-11 | Tampering | shell proof posture | mitigate | Keep Android proof status pending unless the explicit Gradle/JVM lane runs, and assert that truth through doctor/support tests rather than prose. |
| T-18-12 | Elevation of Privilege | integrated bounded-family lane | mitigate | Ensure the proof lane still checks route-local family declarations and `file_picker` transfer seams so acceptance tests cannot normalize broader authority than runtime code allows. |
</threat_model>

<verification>
Run `mix test test/crosswake/proof/phase18_bounded_family_lane_test.exs test/crosswake/proof/phase18_deep_link_activation_lane_test.exs test/crosswake/shell/activation_test.exs`, `xcodebuild test -project examples/ios_shell_host/CrosswakeShell.xcodeproj -scheme CrosswakeShell -destination 'platform=iOS Simulator,name=iPhone 17'`, and `java -version && (cd examples/android_shell_host && ./gradlew test --tests dev.crosswake.shell.BridgeChannelTest)` in a Java-enabled environment.
</verification>

<success_criteria>
Phase 18 ends with layered proof that matches the architecture, the iOS shell coverage passes locally, and the Android JVM shell proof is an explicit completion gate that must pass in CI or on a Java-enabled workstation before the phase is considered fully verified.
</success_criteria>

<output>
After completion, create `.planning/phases/18-operational-diagnostics-and-enforcement/18-04-SUMMARY.md`
</output>
