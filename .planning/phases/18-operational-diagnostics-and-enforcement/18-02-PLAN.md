---
phase: 18-operational-diagnostics-and-enforcement
plan: "02"
type: execute
wave: 2
depends_on:
  - 18-03
files_modified:
  - lib/crosswake/doctor/finding_policy.ex
  - lib/crosswake/doctor/doctor.ex
  - lib/crosswake/doctor/formatter.ex
  - lib/crosswake/doctor/json_formatter.ex
  - lib/mix/tasks/crosswake.doctor.ex
  - test/crosswake/doctor/doctor_test.exs
  - test/crosswake/doctor/formatter_test.exs
  - test/mix/tasks/crosswake_doctor_test.exs
autonomous: true
requirements:
  - OPS-DOCTOR
must_haves:
  truths:
    - "`mix crosswake.doctor` distinguishes blocking contract/support dishonesty from warning or advisory operational state."
    - "Doctor output tells operators exactly what to add, rerun, rebuild, or declare when a capability is unavailable."
    - "Pending Android JVM proof remains visible as `verification_required` instead of being flattened into `supported`."
    - "Notification permission denials and provider token unavailability stay advisory or warning signals rather than fake support claims."
  artifacts:
    - path: lib/crosswake/doctor/doctor.ex
      provides: "Doctor report assembly and severity assignment"
    - path: lib/crosswake/doctor/finding_policy.ex
      provides: "Centralized Phase 18 taxonomy for blocking, warning, and advisory findings"
    - path: lib/mix/tasks/crosswake.doctor.ex
      provides: "Doctor CLI exit behavior and format selection"
  key_links:
    - from: lib/crosswake/doctor/doctor.ex
      to: lib/crosswake/support_matrix/support_matrix.ex
      via: "support posture and capability-family metadata"
      pattern: "verification_required|capability_families|proof_statuses"
    - from: lib/mix/tasks/crosswake.doctor.ex
      to: lib/crosswake/doctor/formatter.ex
      via: "human/json output and exit status"
      pattern: "Formatter\\.render|JSONFormatter\\.render|report.status"
---

<objective>
Make doctor diagnostics explicit, severity-stable, and honest about pending proof versus real contract failures for the first v3.1 capability families.

Purpose: Operators need actionable diagnostics that preserve fail-closed behavior without overclaiming support.
Output: A first-class doctor finding taxonomy, updated report rendering, and CLI/tests that preserve `verification_required` for the unresolved Android lane.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-operational-diagnostics-and-enforcement/18-CONTEXT.md
@.planning/phases/18-operational-diagnostics-and-enforcement/18-RESEARCH.md
@.planning/phases/17-user-prompted-capabilities/17-04-SUMMARY.md

<interfaces>
From lib/crosswake/doctor/doctor.ex:
```elixir
def run(opts \\ []) do
  ...
  %{
    status: if(Enum.any?(findings, &(&1.severity == :error)), do: :error, else: :ok),
    support: support_posture(shells, manifest),
    findings: findings
  }
end
```

From lib/crosswake/doctor/formatter.ex:
```elixir
@spec render(map()) :: String.t()
```

Current proof and support posture hooks in lib/crosswake/doctor/doctor.ex:
```elixir
status: if(blocking_platforms == [], do: :supported, else: :verification_required),
blocking_platforms: blocking_platforms,
proof_statuses: proof_statuses
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Centralize doctor severity and remediation policy</name>
  <read_first>
    lib/crosswake/doctor/doctor.ex
    lib/crosswake/doctor/check.ex
    .planning/phases/18-operational-diagnostics-and-enforcement/18-CONTEXT.md
    test/crosswake/doctor/doctor_test.exs
  </read_first>
  <files>lib/crosswake/doctor/finding_policy.ex, lib/crosswake/doctor/doctor.ex, test/crosswake/doctor/doctor_test.exs</files>
  <action>Per D-10 through D-14, add a dedicated finding-taxonomy helper that classifies blocking versus warning/advisory outcomes for route/runtime misuse, undeclared capability authority, missing `file_picker` transfer seams, shell capability mismatches, missing static native prerequisites, companion compatibility gaps, rebuild-sensitive runtime mismatches, user-denied permission snapshots, transient token unavailability, and missing local Java proof tooling. Update `Crosswake.Doctor.run/1` to use that helper so remediation copy is concrete and the unresolved Android Phase 17 JVM lane still surfaces as `verification_required` per D-12 and D-16 instead of collapsing to a supported posture.</action>
  <acceptance_criteria>
    `mix test test/crosswake/doctor/doctor_test.exs` passes.
    `rg -n "verification_required|warning|advisory|missing local Java|native_picker|token" lib/crosswake/doctor/doctor.ex lib/crosswake/doctor/finding_policy.ex` shows explicit Phase 18 severity handling.
    `rg -n "support_claim_verification_required|proof_hook_verification_required|bridge_posture|shell_activation" test/crosswake/doctor/doctor_test.exs` proves the taxonomy is exercised by automated tests.
  </acceptance_criteria>
  <verify>
    <automated>mix test test/crosswake/doctor/doctor_test.exs</automated>
  </verify>
  <done>Doctor findings use one stable severity policy, blocking issues still fail the command, and environment-sensitive proof gaps remain explicit and non-deceptive.</done>
</task>

<task type="auto">
  <name>Task 2: Render operator-facing doctor output with explicit proof posture</name>
  <read_first>
    lib/crosswake/doctor/formatter.ex
    lib/crosswake/doctor/json_formatter.ex
    lib/mix/tasks/crosswake.doctor.ex
    test/crosswake/doctor/formatter_test.exs
    test/mix/tasks/crosswake_doctor_test.exs
  </read_first>
  <files>lib/crosswake/doctor/formatter.ex, lib/crosswake/doctor/json_formatter.ex, lib/mix/tasks/crosswake.doctor.ex, test/crosswake/doctor/formatter_test.exs, test/mix/tasks/crosswake_doctor_test.exs</files>
  <action>Per D-13, D-15, D-16, D-17, and D-18, update the human and JSON renderers plus the Mix task contract so doctor output separates baseline support, proof verification state, and capability-family posture instead of flattening them into one coarse line. Keep blocking findings as non-zero exit status, preserve human-readable remediation copy for missing permission/config prerequisites and unsupported shell capability surfaces, and ensure JSON output carries the same structured truth so downstream tooling can detect `verification_required` without scraping prose.</action>
  <acceptance_criteria>
    `mix test test/crosswake/doctor/formatter_test.exs test/mix/tasks/crosswake_doctor_test.exs` passes.
    `mix test test/mix/tasks/crosswake_doctor_test.exs --only doctor` exits green and the assertions include `verification_required` for Android when proof hooks are absent.
    `rg -n "verification required|baseline|proof|capability" lib/crosswake/doctor/formatter.ex lib/crosswake/doctor/json_formatter.ex test/mix/tasks/crosswake_doctor_test.exs` matches the separated render surfaces.
  </acceptance_criteria>
  <verify>
    <automated>mix test test/crosswake/doctor/formatter_test.exs test/mix/tasks/crosswake_doctor_test.exs</automated>
  </verify>
  <done>Doctor output is actionable, deterministic, and structurally honest about pending proof, and the CLI still fails only on genuinely blocking findings.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Manifest/support data -> doctor report | Internal metadata can still mislead operators if severity or proof posture is rendered dishonestly. |
| Local environment -> doctor proof hooks | Workstation-specific state such as missing Java must not be misreported as completed support. |
| CLI output -> operator decisions | Human and JSON output become a product surface that may influence release or support claims. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-04 | Information Disclosure | doctor output surfaces | mitigate | Render baseline support, proof state, and capability posture as separate structured facts per D-15 through D-18 so users cannot misread pending proof as support. |
| T-18-05 | Repudiation | `mix crosswake.doctor` | mitigate | Keep deterministic findings and concrete remediation copy so support claims and failures are auditable per D-13. |
| T-18-06 | Denial of Service | doctor exit behavior | accept | Non-zero exits for blocking failures are intentional and low-risk because the task is an operator-facing verification command, not a production path. |
</threat_model>

<verification>
Run `mix test test/crosswake/doctor/doctor_test.exs test/crosswake/doctor/formatter_test.exs test/mix/tasks/crosswake_doctor_test.exs`.
</verification>

<success_criteria>
Doctor output blocks on contract dishonesty or unsupported runtime posture, warns or advises on environment-sensitive state, and preserves the unresolved Android JVM lane as `verification_required` until a Java-enabled proof run exists.
</success_criteria>

<output>
After completion, create `.planning/phases/18-operational-diagnostics-and-enforcement/18-02-SUMMARY.md`
</output>
