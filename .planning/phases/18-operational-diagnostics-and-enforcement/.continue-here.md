---
context: phase
phase: 18-operational-diagnostics-and-enforcement
task: 3
total_tasks: 3
status: blocked
last_updated: 2026-05-21T21:14:42Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

> These are not suggestions. Each constraint below was discovered through failure.
> Acknowledge each one explicitly before proceeding.

- [ ] CONSTRAINT: Local Android proof needs more disk than this machine currently has under `~/.android/avd` — the emulator failed before Gradle tests with `Available: 4682.45 MB ... need 7372.80 MB` — free space or switch to the new laptop before retrying the local Android lane.

**Do not proceed until all boxes are checked.**

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Fake-local-CI parity with `act` | `act` was considered as a local substitute, but this workflow is `macos-15` and includes `xcodebuild`, so a Linux Docker runner does not validate the real path. | advisory | Treat `act` as irrelevant for this workflow; use direct host execution for local checks and GitHub Actions for the full proof lane. |
| Android proof claim before emulator boot | The local Android verifier can fail at emulator provisioning before any tests run, so "script started" is not evidence of proof closure. | blocking | Require either a green `Phase 18 Proof` workflow or a full successful local run of `script/verify_generated_android_shell.sh` before updating support posture. |

<current_state>
Paused in Phase 18, plan `18-04`, Task 3. Tasks 1 and 2 are complete. The dedicated CI proof lane has been added in `.github/workflows/phase18-proof.yml`, the focused Elixir verifier in `script/verify_phase18_contract.sh` passes locally (`48 tests, 0 failures`), and iOS proof already passed locally. The only unresolved item is Android proof closure.
</current_state>

<completed_work>

Completed Tasks:
- Task 1: Add the layered ExUnit acceptance slices - Done
- Task 2: Extend platform shell proofs without pretending Android is locally complete - Done
- Task 3: Close the Android JVM proof gate - In progress, with CI workflow added and local Android verifier attempted
</completed_work>

<remaining_work>

- Task 3: Run `GitHub Actions: Phase 18 Proof` and record the result honestly
- Task 3: Optionally rerun the local Android verifier on a machine with enough disk for the emulator
- After proof passes: update `.planning/ROADMAP.md`, `.planning/STATE.md`, and `18-04-SUMMARY.md` to mark Phase 18 fully verified
</remaining_work>

<decisions_made>

- Shifted the remaining Android proof gate into `.github/workflows/phase18-proof.yml` so Phase 18 closes on CI evidence instead of manual human verification.
- Kept planning artifacts aligned with that shift-left path, including the Phase 17 summary and Phase 18 state/story.
- Rejected `act` as the local workflow runner because the real job is macOS-only and includes `xcodebuild`.
</decisions_made>

<blockers>
- Local Android verifier: blocked by emulator disk-space requirement under `~/.android/avd` (`need 7372.80 MB`, `available 4682.45 MB`).
- Full workflow execution: should happen in GitHub Actions or on the new roomier laptop.
</blockers>

## Required Reading (in order)
1. `.planning/STATE.md` — current phase state and next-step framing
1. `.planning/phases/18-operational-diagnostics-and-enforcement/18-04-PLAN.md` — exact acceptance criteria for the remaining proof gate
1. `.planning/phases/18-operational-diagnostics-and-enforcement/18-04-SUMMARY.md` — what was already verified locally and what remains open
1. `.github/workflows/phase18-proof.yml` — CI-owned closure path for Phases 17 and 18
1. `script/verify_phase18_contract.sh` — focused local Elixir proof lane

## Critical Anti-Patterns (do NOT repeat these)
- Do NOT mark Phase 18 complete because Elixir and iOS are green; Android proof is still the gate.
- Do NOT use `act` as evidence for this workflow.
- Do NOT revert unrelated dirty worktree changes while resuming; this repo was already dirty.

## Infrastructure State
- No background jobs intentionally left running.
- `act` is installed, but not useful for the real Phase 18 workflow.
- Docker is installed.
- Local Android SDK/JDK provisioning path in `script/verify_generated_android_shell.sh` works far enough to start emulator setup on-host.

<context>
This is a clean stopping point. The code and planning work for Phase 18 are in place. The next session should not re-open design questions unless the Android proof actually fails. The only thing to learn next is whether the proof lane passes on better hardware or in GitHub Actions, and then the planning state should be closed honestly.
</context>

<next_action>
Start with: run `GitHub Actions: Phase 18 Proof`. If you prefer a local retry on the new laptop first, run `export CROSSWAKE_ANDROID_PROJECT_ROOT="examples/android_shell_host" && bash script/verify_generated_android_shell.sh`. Only after a real Android pass should you update `.planning/STATE.md` and `.planning/phases/18-operational-diagnostics-and-enforcement/18-04-SUMMARY.md` to close the phase.
</next_action>
