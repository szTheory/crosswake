---
phase: 104-logo-refinement-production-suite
plan: 03
type: execute
wave: 3
depends_on: [104-02]
files_modified:
  - brandbook/logo/crosswake-mark.svg
  - brandbook/logo/crosswake-mark-mono.svg
  - brandbook/logo/crosswake-typemark.svg
  - brandbook/logo/crosswake-lockup-horizontal.svg
  - brandbook/logo/crosswake-lockup-horizontal-dark.svg
  - brandbook/logo/crosswake-lockup-stacked.svg
  - brandbook/logo/crosswake-lockup-subtitle.svg
  - brandbook/logo/favicon.svg
  - brandbook/logo/README.md
autonomous: true
requirements: [LOGO-06, LOGO-07]
must_haves:
  truths:
    - "8 production SVGs ship in brandbook/logo/: mark, mark-mono, typemark, lockup-horizontal (light), lockup-horizontal-dark, lockup-stacked, lockup-subtitle, favicon (D-09)"
    - "Embeddable variants (mark, typemark) use currentColor; distribution lockups use literal hex fills so they render correctly downloaded standalone (light #09141A, dark #F7F1E6) (D-10)"
    - "Only crosswake-lockup-subtitle.svg carries the tagline 'Declare the crossing.', set as PATHS generated via gen-wordmark.mjs in Space Grotesk Medium — never <text> (D-09)"
    - "favicon.svg is a dedicated 16-grid redraw (viewBox 0 0 16 16), max 2 wake strokes, pixel-snapped, with an internal prefers-color-scheme dark swap — NOT a scaled-down mark (D-09)"
    - "All 8 files are self-contained (no external/<use> refs that GitHub strips), built from the signed-off variant, with no blind path-node surgery (D-04), 2-decimal precision (D-12)"
    - "gen-wordmark.mjs regenerates reproducibly: re-running it diffs clean against the committed base, and the regen workflow is documented (D-11, LOGO-07)"
    - "Committed brandbook/ stays under the 1MB cap after the suite is added (D-12)"
    - "Every production file was browser-rendered and screenshot-inspected by the executor before commit (D-05)"
  artifacts:
    - path: "brandbook/logo/crosswake-mark.svg"
      provides: "icon mark, currentColor, embeddable"
      contains: "currentColor"
    - path: "brandbook/logo/crosswake-lockup-horizontal.svg"
      provides: "light distribution lockup, literal #09141A fills"
      contains: "#09141A"
    - path: "brandbook/logo/crosswake-lockup-horizontal-dark.svg"
      provides: "dark distribution lockup, literal #F7F1E6 fills"
      contains: "#F7F1E6"
    - path: "brandbook/logo/crosswake-lockup-subtitle.svg"
      provides: "only tagline-bearing lockup, tagline as paths"
      contains: "lockup-subtitle"
    - path: "brandbook/logo/favicon.svg"
      provides: "16-grid favicon with prefers-color-scheme swap"
      contains: "prefers-color-scheme"
    - path: "brandbook/logo/README.md"
      provides: "asset index + LOGO-07 regen workflow note"
      contains: "gen-wordmark"
  key_links:
    - from: "brandbook/logo/crosswake-lockup-subtitle.svg"
      to: "brandbook/tools/gen-wordmark.mjs"
      via: "tagline rendered as opentype.js paths (Space Grotesk Medium), not <text>"
      pattern: "Declare the crossing"
    - from: "brandbook/logo/favicon.svg"
      to: "browser tab theme"
      via: "internal <style> prefers-color-scheme dark fill swap"
      pattern: "prefers-color-scheme"
    - from: "brandbook/tools/gen-wordmark.mjs"
      to: "brandbook/logo/tournament/candidates/wordmark-base.svg"
      via: "reproducible regeneration, diff-clean"
      pattern: "wordmark-base"
---

<objective>
Ship the LOGO-06 / LOGO-07 production suite: 8 path-only SVGs in `brandbook/logo/`, built from
the variant the user signed off in 104-02. Embeddable variants use currentColor; distribution
lockups use literal hex so they render correctly when downloaded standalone (D-10). The subtitle
lockup is the only file carrying the tagline, set as opentype.js paths. A dedicated 16-grid favicon
with an internal prefers-color-scheme swap. Then confirm gen-wordmark.mjs regenerates reproducibly
(LOGO-07) and the committed brandbook stays under 1MB (D-12). Every file is render-verified (D-05).

Purpose: Produce the durable, self-contained logo assets phases 105/106 consume.
Output: 8 SVGs + brandbook/logo/README.md asset index with the regen workflow note.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/104-logo-refinement-production-suite/104-CONTEXT.md
@.planning/phases/104-logo-refinement-production-suite/104-02-SIGNOFF.md

# Source-of-truth artifacts
@brandbook/logo/tournament/candidates/A.svg
@brandbook/logo/tournament/candidates/R3-A-lockup-horizontal.svg
@brandbook/tools/gen-wordmark.mjs

<interfaces>
<!-- SIGNED-OFF VARIANT: read 104-02-SIGNOFF.md FIRST. Build every file from that variant's geometry. -->
<!-- If V1: use baseline Mark A (D-01). If V2/V3: use the notch/stroke deltas from brandbook/logo/variants/. -->

<!-- Wordmark technique (D-02): wordmark-r3 = Space Grotesk SemiBold outlines + clipPath 20° wedge -->
<!-- shears on two terminals only — w final-stroke tip M237.46 35.28 L245.66 35.28 L245.66 38.265 Z -->
<!-- and k arm tip M317.02 35.28 L327.89 35.28 L327.89 39.236 Z. Reuse from R3-A-lockup-horizontal.svg. -->

<!-- Lockup layout (D-03): mark scale 1.125 (→72u), gap 7.5u, wordmark translate +75.76, -->
<!-- horizontal viewBox 0 0 442.16 72. -->

<!-- Colorway hexes (D-07/D-10): Current 950 #09141A | Foam 50 #F7F1E6 | Brass 500 #C98A2E | Wake 700 #2B756A -->

<!-- gen-wordmark.mjs: exports generateWordmark(ttfPath, {inspectBaseline}). Default TEXT='Crosswake', -->
<!-- FONT_SIZE 72, WEIGHT 600. For the tagline, invoke with TEXT='Declare the crossing.' at Medium (500). -->
<!-- Only Space Grotesk is present in brandbook/tools/fonts/ — use Space Grotesk Medium per D-09 -->
<!-- (Atkinson is not fetched; do NOT add a fonts fetch — prefer the available family). -->

<!-- render-verify.mjs (from 104-01): node brandbook/tools/render-verify.mjs <file> <out.png> -->
<!-- check-production.mjs (from 104-01): validates brandbook/logo/ path-only + favicon viewBox 0 0 16 16 -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Mark + typemark family (currentColor embeddables) + render-verify</name>
  <files>brandbook/logo/crosswake-mark.svg, brandbook/logo/crosswake-mark-mono.svg, brandbook/logo/crosswake-typemark.svg</files>
  <read_first>
    - .planning/phases/104-logo-refinement-production-suite/104-02-SIGNOFF.md (which variant to build from)
    - brandbook/logo/tournament/candidates/A.svg (baseline mark + provenance header style)
    - brandbook/logo/tournament/candidates/R3-A-lockup-horizontal.svg (wordmark-r3 group + clip-cuts, D-02)
  </read_first>
  <action>
    Build from the signed-off variant geometry (read SIGNOFF first):
    - `crosswake-mark.svg`: the wake mark alone, viewBox 0 0 64 64, stroke="currentColor", stroke-width
      per signed-off variant, round caps (D-09 currentColor embeddable). No fill, no rect, no text.
    - `crosswake-mark-mono.svg`: same geometry but with a literal single-color fill stroke (Current 950
      #09141A) for contexts that strip currentColor — still path/line-only.
    - `crosswake-typemark.svg`: the W1 wordmark standalone — reuse the wordmark-r3 `<g>` paths + the
      clipPath wedge cuts (D-02) from R3-A-lockup-horizontal.svg, fill="currentColor", fill-rule="evenodd",
      viewBox tight to the wordmark. No <text>.
    Each file: provenance header comment citing D-01/D-02/D-09/D-10 and the source variant; 2-decimal
    precision (D-12); lean (a few KB). Run render-verify.mjs on each, Read each PNG, confirm the mark/
    wordmark render correctly (no clipping, cuts visible) BEFORE commit (D-05).
  </action>
  <verify>
    <automated>node brandbook/tools/check-production.mjs && grep -q currentColor brandbook/logo/crosswake-mark.svg && grep -q currentColor brandbook/logo/crosswake-typemark.svg && grep -q 'fill-rule="evenodd"' brandbook/logo/crosswake-typemark.svg && grep -q '#09141A' brandbook/logo/crosswake-mark-mono.svg && node brandbook/tools/render-verify.mjs brandbook/logo/crosswake-typemark.svg /tmp/typemark.png && test -f /tmp/typemark.png</automated>
  </verify>
  <done>mark, mark-mono, typemark exist; mark + typemark use currentColor; all path-only; all render-verified (executor Read the PNGs).</done>
</task>

<task type="auto">
  <name>Task 2: Lockup family + subtitle tagline-as-paths (literal-hex distribution) + render-verify</name>
  <files>brandbook/logo/crosswake-lockup-horizontal.svg, brandbook/logo/crosswake-lockup-horizontal-dark.svg, brandbook/logo/crosswake-lockup-stacked.svg, brandbook/logo/crosswake-lockup-subtitle.svg</files>
  <read_first>
    - brandbook/logo/tournament/candidates/R3-A-lockup-horizontal.svg (D-03 layout: scale 1.125, gap 7.5u, translate 75.76, viewBox 0 0 442.16 72)
    - brandbook/tools/gen-wordmark.mjs (invoke for the tagline paths)
    - .planning/phases/104-logo-refinement-production-suite/104-02-SIGNOFF.md
  </read_first>
  <action>
    Distribution lockups use LITERAL hex fills (D-10 — must render standalone when downloaded), self-contained
    (inline all paths, NO external/<use> refs that GitHub strips, D-12). Build from the signed-off variant:
    - `crosswake-lockup-horizontal.svg`: mark + wordmark per D-03 layout, ALL fills literal Current 950
      #09141A (light). viewBox 0 0 442.16 72.
    - `crosswake-lockup-horizontal-dark.svg`: identical layout, ALL fills literal Foam 50 #F7F1E6 (dark).
    - `crosswake-lockup-stacked.svg`: mark above wordmark (stacked proportions at Claude's discretion per
      §8 — 64px min width), literal Current 950 fills.
    - `crosswake-lockup-subtitle.svg`: the ONLY file carrying the tagline "Declare the crossing." Generate
      the tagline glyphs as PATHS via gen-wordmark.mjs (Space Grotesk Medium/500 — Atkinson is not fetched,
      use the available family per D-09), place below the lockup at a small subtitle size/tracking (§9 rules,
      Claude's discretion), literal Current 950 fills. NEVER a <text> element. Accept path duplication of the
      wordmark across files (D-12 — self-contained is required; keep precision at 2 decimals to trim bytes).
    Provenance headers cite D-02/D-03/D-09/D-10. Render-verify EACH file (render-verify.mjs → Read PNG),
    confirm fills are correct color, tagline legible and is paths, no clipping, BEFORE commit (D-05).
  </action>
  <verify>
    <automated>node brandbook/tools/check-production.mjs && grep -q '#09141A' brandbook/logo/crosswake-lockup-horizontal.svg && grep -q '#F7F1E6' brandbook/logo/crosswake-lockup-horizontal-dark.svg && grep -q 'Declare the crossing' brandbook/logo/crosswake-lockup-subtitle.svg && ! grep -l '<text' brandbook/logo/crosswake-lockup-subtitle.svg && node brandbook/tools/render-verify.mjs brandbook/logo/crosswake-lockup-subtitle.svg /tmp/subtitle.png && test -f /tmp/subtitle.png</automated>
  </verify>
  <done>4 lockups exist with correct literal-hex fills; subtitle is the only tagline-bearing file and its tagline is paths (no <text>); all self-contained; all render-verified.</done>
</task>

<task type="auto">
  <name>Task 3: Favicon (16-grid + prefers-color-scheme) + LOGO-07 regen + size budget + README</name>
  <files>brandbook/logo/favicon.svg, brandbook/logo/README.md</files>
  <read_first>
    - brandbook/AUDIT.md §8 (favicon: simplified 2-stroke, legible at 16px, lines 547/558/596)
    - brandbook/tools/gen-wordmark.mjs (LOGO-07 reproducibility — header documents regen workflow)
    - .planning/research/PITFALLS.md (GitHub SVG sanitization — no external refs in distribution files)
  </read_first>
  <action>
    Favicon (D-09): a DEDICATED 16-grid redraw, viewBox "0 0 16 16" — NOT a scaled-down mark. Max 2 wake
    strokes, pixel-snapped coordinates (integer/half-integer on the 16-grid for crisp 16px rendering), round
    caps preserved, 20° crossing preserved. Include an internal `<style>` with `@media (prefers-color-scheme: dark)`
    that swaps the stroke fill (light default #09141A → dark #F7F1E6) for browser tabs. Self-contained (no external
    refs — GitHub README handling is 106). Verify at literal 16px: render-verify.mjs → Read PNG → confirm legible
    and the two strokes don't merge; iterate before commit (D-05).

    LOGO-07 regen reproducibility (D-11): run `node brandbook/tools/gen-wordmark.mjs`, then
    `git diff --exit-code brandbook/logo/tournament/candidates/wordmark-base.svg` — confirm regeneration is
    deterministic (diff clean). Document the regen workflow (command + provenance: SpaceGrotesk wght=600,
    opentype.js 2.0.0, fonts gitignored) in brandbook/logo/README.md and in the suite header comments.

    Create `brandbook/logo/README.md`: asset index listing all 8 production files with one-line purpose +
    colorway/usage each, plus the LOGO-07 regen note.

    Size budget (D-12): after all files committed-equivalent, assert committed brandbook/ < 1MB:
    `git add -A brandbook/logo && git ls-files brandbook/ | xargs stat -f%z | awk '{s+=$1} END {exit s>1048576}'`.
    If over, trim precision/duplication (do NOT drop required files; do NOT use <use> refs).
  </action>
  <verify>
    <automated>node brandbook/tools/check-production.mjs && grep -q 'viewBox="0 0 16 16"' brandbook/logo/favicon.svg && grep -q 'prefers-color-scheme' brandbook/logo/favicon.svg && node brandbook/tools/gen-wordmark.mjs && git diff --exit-code brandbook/logo/tournament/candidates/wordmark-base.svg && node brandbook/tools/render-verify.mjs brandbook/logo/favicon.svg /tmp/favicon.png && printf '<img src="favicon.svg" width="16" height="16"><img src="favicon.svg" width="32" height="32">' > brandbook/logo/.favicon-16-test.html && node brandbook/tools/render-verify.mjs brandbook/logo/.favicon-16-test.html /tmp/favicon-16.png && rm brandbook/logo/.favicon-16-test.html && git add -A brandbook/logo brandbook/tools && git ls-files brandbook/ | xargs stat -f%z | awk '{s+=$1} END {exit s>1048576}'</automated>
  </verify>
  <done>favicon.svg is a 16-grid redraw (viewBox 0 0 16 16) with prefers-color-scheme swap; executor READ /tmp/favicon-16.png (favicon embedded at literal 16px and 32px) and confirmed no stroke merging — this is the SC3 16px legibility gate. Provenance header notes: strokes retained per D-09 (not outlined to filled paths) — pixel-snapped round-cap strokes give equivalent 16px legibility with smaller files and simpler regen; gen-wordmark.mjs regenerates diff-clean (LOGO-07); README.md indexes all 8 files + regen workflow; committed brandbook/ < 1MB (D-12).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local toolchain → committed distribution SVGs | Files ship to users / GitHub / hexdocs; sanitization strips external refs |
| gen-wordmark.mjs → committed wordmark base | Generated paths enter the repo; non-determinism = supply-chain drift |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-104-03a | Tampering | distribution lockups with <use>/external refs | mitigate | self-contained literal-hex files only; check-production gates path-only; GitHub sanitization avoided per PITFALLS.md (D-10/D-12) |
| T-104-03b | Tampering | gen-wordmark non-reproducibility | mitigate | LOGO-07 gate runs script + `git diff --exit-code` on wordmark-base; pinned font + opentype.js 2.0.0 (D-11) |
| T-104-03c | Information disclosure | favicon prefers-color-scheme <style> | accept | inline CSS media query only, no script, no network; standard SVG favicon pattern |
| T-104-SC | Tampering | npm/pip/cargo installs | mitigate | none added — reuses committed opentype.js 2.0.0 + examples/phoenix_host Playwright; zero new installs |
</threat_model>

<verification>
- `node brandbook/tools/check-production.mjs` exits 0 (all 8 files path-only, favicon viewBox 0 0 16 16)
- Embeddables (mark, typemark) grep currentColor; distribution lockups grep literal #09141A / #F7F1E6 (D-10)
- Only crosswake-lockup-subtitle.svg contains "Declare the crossing." and it has no <text> (D-09)
- favicon.svg viewBox 0 0 16 16 + prefers-color-scheme present; render-verified at 16px (D-05)
- `node brandbook/tools/gen-wordmark.mjs` then `git diff --exit-code wordmark-base.svg` clean (LOGO-07, D-11)
- committed brandbook/ < 1MB (D-12)
- Every production file render-verified by the executor (screenshots Read) before commit (D-05)
</verification>

<success_criteria>
- 8 production SVGs ship with correct currentColor vs literal-hex split (D-09, D-10)
- Tagline lives only in the subtitle lockup, as paths (D-09)
- favicon is a dedicated 16-grid redraw with dark-mode swap, legible at 16px (D-09)
- LOGO-07 reproducibility confirmed and documented (D-11)
- brandbook stays under 1MB (D-12); no blind path-node surgery (D-04)
</success_criteria>

<output>
Create `.planning/phases/104-logo-refinement-production-suite/104-03-SUMMARY.md` when done.
List each file's render-verify inspection outcome and the final committed brandbook/ size.
</output>
