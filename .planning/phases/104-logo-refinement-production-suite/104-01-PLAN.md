---
phase: 104-logo-refinement-production-suite
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - brandbook/logo/refinement.html
  - brandbook/logo/variants/v1-baseline.svg
  - brandbook/logo/variants/v2-notch.svg
  - brandbook/logo/variants/v3-stroke.svg
  - brandbook/tools/check-production.mjs
  - brandbook/tools/render-verify.mjs
autonomous: true
requirements: [LOGO-05]
must_haves:
  truths:
    - "User can open brandbook/logo/refinement.html from file:// and see 3 micro-variants (V1/V2/V3) each rendered in all 4 ratified colorways (D-06, D-07)"
    - "Each variant differs from the baseline on exactly ONE axis (V1 baseline, V2 notch width, V3 stroke weight) (D-06)"
    - "The page is the color reveal — every colorway uses the exact ratified §8 hexes (D-07)"
    - "Every variant SVG is path-only, no <text>, no full-bleed rect, and was browser-rendered + screenshot-inspected by the executor before commit (D-04, D-05)"
    - "Variant geometry derives from the locked winner Mark A route + notch + wake lines (D-01) with no blind path-node surgery (D-04)"
  artifacts:
    - path: "brandbook/logo/refinement.html"
      provides: "file://-compatible sign-off page: 3 variants x 4 colorways grid"
      contains: "v1-baseline"
    - path: "brandbook/logo/variants/v2-notch.svg"
      provides: "V2 notch-width axis variant of Mark A"
      contains: "mark-a"
    - path: "brandbook/logo/variants/v3-stroke.svg"
      provides: "V3 stroke-weight axis variant of Mark A"
      contains: "stroke-width"
    - path: "brandbook/tools/check-production.mjs"
      provides: "structural validator pointed at brandbook/logo/ (production + variants)"
      exports: ["checkSvg"]
  key_links:
    - from: "brandbook/logo/refinement.html"
      to: "brandbook/logo/variants/*.svg"
      via: "inline SVG markup (file://-safe, no external <img src> refs)"
      pattern: "mark-a"
    - from: "brandbook/tools/render-verify.mjs"
      to: "brandbook/logo/refinement.html"
      via: "Playwright chromium file:// goto + fullPage screenshot"
      pattern: "screenshot"
---

<objective>
Build the LOGO-05 micro-variant refinement round. Produce 3 single-axis variants of the
locked winner (Mark A + W1 wordmark) and a small file://-compatible sign-off page that
renders all 3 variants across the 4 ratified colorways. This is the color reveal moment —
the user sees the logo in color for the first time. Also add a production-file validator
(`check-production.mjs`) and a reusable Playwright render-verify helper.

Purpose: Give the user 3 disciplined choices (one axis each, "all killer no filler") in
color, screenshot-inspected by the executor before they ever see it (D-05).
Output: `refinement.html`, 3 variant SVGs, `check-production.mjs`, `render-verify.mjs`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/104-logo-refinement-production-suite/104-CONTEXT.md

# Source-of-truth artifacts (read before authoring variants)
@brandbook/logo/tournament/candidates/A.svg
@brandbook/logo/tournament/candidates/R3-A-lockup-horizontal.svg
@brandbook/tools/check-candidates.mjs

<interfaces>
<!-- Locked winner geometry (D-01), Mark A on 64-grid, stroke-width 6.667, round caps: -->
<!-- route seg1: (0,43.65)→(27.30,33.71)  route seg2: (36.70,30.29)→(64,20.35) -->
<!-- notch gap = 1.5x stroke = 10u between (27.30,33.71) and (36.70,30.29) -->
<!-- wake line 1: (20.59,50.34)→(52.53,38.71)  wake line 2: (25.15,62.87)→(57.10,51.24) -->
<!-- stroke="currentColor" so colorways set via wrapper `color:` CSS -->

<!-- Ratified §8 colorway hexes (D-07), from crosswake.tokens.json: -->
<!-- Current 950 = #09141A | Foam 50 = #F7F1E6 | Brass 500 = #C98A2E | Wake 700 = #2B756A -->
<!-- Light primary:  mark #09141A, wordmark #09141A, bg #F7F1E6 (and a white-bg cell) -->
<!-- Dark primary:   mark #F7F1E6, wordmark #F7F1E6, bg #09141A -->
<!-- Signal:         mark #C98A2E, wordmark #F7F1E6, bg #09141A -->
<!-- OSS badge:      mark #2B756A, wordmark #09141A, bg #F7F1E6 -->

<!-- check-candidates.mjs exports checkSvg(svgContent, filename) -> string[] violations -->
<!-- Playwright lives at examples/phoenix_host/node_modules (chromium already installed) -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add render-verify helper and production validator</name>
  <files>brandbook/tools/render-verify.mjs, brandbook/tools/check-production.mjs</files>
  <read_first>
    - brandbook/tools/check-candidates.mjs (reuse checkSvg — import it, do not re-implement)
    - examples/phoenix_host/playwright.config.ts (confirm chromium availability path)
  </read_first>
  <action>
    Create `render-verify.mjs`: a small ESM script that imports `chromium` from
    `examples/phoenix_host/node_modules/playwright` (use an absolute or relative resolve from
    brandbook/tools to that node_modules path — Playwright is NOT installed under brandbook/tools).
    It launches headless chromium, `page.goto('file://' + absolutePath)` for an HTML or SVG file
    passed as argv[2], sets a deterministic viewport (e.g. 1200x900), waits for load, takes a
    `fullPage: true` screenshot to a path given by argv[3] (default `/tmp/render-verify.png`), and
    closes the browser. Print the screenshot path on success. This is the mandatory D-05 loop tool:
    the executing agent runs it then Reads the PNG to visually inspect before commit/presentation.

    Create `check-production.mjs`: import { checkSvg } from './check-candidates.mjs' and run it over
    every `*.svg` under `brandbook/logo/` recursively EXCLUDING `brandbook/logo/tournament/`
    (those are already gated by check-candidates). Add two production-only assertions on top of
    checkSvg: (1) reject any literal `<text` element (D-09 path-only), (2) for files whose name
    contains `favicon`, assert the root viewBox equals `0 0 16 16`. Exit non-zero listing each
    violating file, mirroring check-candidates output style. Comment headers cite D-04/D-05/D-09.
  </action>
  <verify>
    <automated>node brandbook/tools/check-production.mjs; test $? -eq 0 || test $? -eq 1</automated>
  </verify>
  <done>render-verify.mjs launches chromium + screenshots a file:// target; check-production.mjs imports checkSvg and scans brandbook/logo/ (excluding tournament/) with favicon-viewBox + no-text gates; both run with zero npm installs.</done>
</task>

<task type="auto">
  <name>Task 2: Author the 3 single-axis micro-variants (D-06)</name>
  <files>brandbook/logo/variants/v1-baseline.svg, brandbook/logo/variants/v2-notch.svg, brandbook/logo/variants/v3-stroke.svg</files>
  <read_first>
    - brandbook/logo/tournament/candidates/A.svg (exact baseline geometry + provenance header style)
    - brandbook/logo/tournament/candidates/R3-A-lockup-horizontal.svg (wordmark clip-cut technique, D-02)
  </read_first>
  <action>
    Create 3 variant SVGs of the Mark A wake mark on the 64-grid, each a `currentColor` stroke group
    (no fills, no rect, no text — D-04). Each differs from baseline on exactly ONE axis (D-06):
    - `v1-baseline.svg`: Mark A exactly as locked (D-01) — route (0,43.65)→(27.30,33.71) and
      (36.70,30.29)→(64,20.35), wake lines (20.59,50.34)→(52.53,38.71) and (25.15,62.87)→(57.10,51.24),
      stroke-width 6.667, round caps. This is the as-picked control.
    - `v2-notch.svg`: identical geometry and stroke, EXCEPT the crossing notch gap widens from 1.5x
      stroke (10u) to ~1.25x→1.5x per D-06 — recompute the two route-segment endpoints around the
      notch center along the 20° route direction unit vector (0.9397, -0.3420) so the gap changes but
      the route line stays collinear. ONLY the notch endpoints move; everything else is byte-identical
      to baseline. No blind node surgery (D-04) — these are line endpoints, recomputed, not dragged.
    - `v3-stroke.svg`: identical geometry as baseline EXCEPT stroke-width changes to ~7.2u (heavier).
      Nothing else moves.
    Each file carries a provenance header comment naming the axis it varies and citing D-01/D-06, 2-decimal
    precision (D-12). Keep them lean (target < 1.5KB each, like A.svg).
  </action>
  <verify>
    <automated>node brandbook/tools/check-production.mjs && grep -L '<text' brandbook/logo/variants/v1-baseline.svg brandbook/logo/variants/v2-notch.svg brandbook/logo/variants/v3-stroke.svg | wc -l | grep -q 3</automated>
  </verify>
  <done>3 variant SVGs exist, all path/line-only, all pass check-production.mjs; v2 differs only in notch gap, v3 differs only in stroke-width, v1 == locked baseline geometry.</done>
</task>

<task type="auto">
  <name>Task 3: Build refinement.html color reveal page + render-verify it (D-05, D-07)</name>
  <files>brandbook/logo/refinement.html</files>
  <read_first>
    - brandbook/logo/variants/v1-baseline.svg, v2-notch.svg, v3-stroke.svg (inline their markup)
    - brandbook/logo/tournament/candidates/R3-A-lockup-horizontal.svg (lockup composition for the colorway cells)
  </read_first>
  <action>
    Create `brandbook/logo/refinement.html`: a small, zero-build, file://-compatible page (no external
    <img src> refs, no CDN required — inline the SVG markup directly so it works from file://). Layout a
    grid: 3 rows (V1 baseline / V2 notch / V3 stroke), each row showing the variant's mark+wordmark lockup
    across the 4 ratified colorways as labeled cells (D-07): Light primary on #F7F1E6 AND a white cell,
    Dark primary on #09141A, Signal (mark #C98A2E + wordmark #F7F1E6) on #09141A, OSS badge (mark #2B756A
    + wordmark #09141A) on #F7F1E6. Drive mark color via wrapper `color:` (mark uses currentColor); the
    wordmark fill is set per colorway. Under each variant row add a one-line caption naming the axis
    ("V2 — wider crossing notch"). Keep the colorway grid prominent (this is the reveal). Page total small.

    MANDATORY render-verify loop (D-05): after writing the page, run
    `node brandbook/tools/render-verify.mjs brandbook/logo/refinement.html /tmp/refinement.png`, then
    Read /tmp/refinement.png and visually confirm: all 3 variants render, all 4 colorways show correct
    fg/bg, no clipped or invisible marks, wordmark legible. Fix and re-render until correct BEFORE commit.
    Record the inspection outcome in the SUMMARY.
  </action>
  <verify>
    <automated>node brandbook/tools/render-verify.mjs brandbook/logo/refinement.html /tmp/refinement.png && test -f /tmp/refinement.png && grep -q 'v2-notch\|V2' brandbook/logo/refinement.html && grep -cq '#09141A' brandbook/logo/refinement.html</automated>
  </verify>
  <done>refinement.html opens from file://, shows 3 variants x 4 colorways with exact §8 hexes, render-verify.mjs produced a screenshot the executor Read and confirmed legible; no external refs.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local toolchain → committed assets | Generated SVG/HTML enter the repo and ship to users |
| Playwright → local file:// | Headless chromium renders only local files; no network |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-104-01 | Tampering | refinement.html / variant SVGs | mitigate | check-production.mjs gates path-only + no full-bleed rect; render-verify screenshot inspected before commit |
| T-104-02 | Information disclosure | render-verify.mjs file:// goto | accept | local-only file paths, headless, no network; no secrets in brandbook |
| T-104-SC | Tampering | npm/pip/cargo installs | mitigate | none added — reuses examples/phoenix_host Playwright + existing opentype.js; zero new installs this plan |
</threat_model>

<verification>
- `node brandbook/tools/check-production.mjs` exits 0 (all variants structurally valid)
- `node brandbook/tools/render-verify.mjs brandbook/logo/refinement.html /tmp/refinement.png` produces a screenshot the executor Read and confirmed
- Variant axis isolation: diff v1 vs v2 shows only notch endpoints changed; diff v1 vs v3 shows only stroke-width changed
</verification>

<success_criteria>
- 3 single-axis micro-variants exist and pass production validation (D-06)
- refinement.html renders all 3 variants in all 4 ratified colorways from file:// (D-07)
- Executor screenshot-inspected the page before commit (D-05)
- No new npm installs; no external SVG refs in the page
</success_criteria>

<output>
Create `.planning/phases/104-logo-refinement-production-suite/104-01-SUMMARY.md` when done.
Record the render-verify inspection outcome (what the screenshot showed) explicitly.
</output>
