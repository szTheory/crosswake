---
phase: 104-logo-refinement-production-suite
plan: 02
type: execute
wave: 2
depends_on: [104-01]
files_modified:
  - .planning/phases/104-logo-refinement-production-suite/104-02-SIGNOFF.md
autonomous: false
requirements: [LOGO-05]
must_haves:
  truths:
    - "User is presented refinement.html + a one-line description of each variant and picks V1/V2/V3 (or requests an adjustment) at a blocking checkpoint (D-08)"
    - "The executor returns CHECKPOINT REACHED and never self-approves the pick (D-05, D-08)"
    - "The chosen variant is recorded so the production suite (104-03) builds from the exact signed-off geometry (D-08)"
  artifacts:
    - path: ".planning/phases/104-logo-refinement-production-suite/104-02-SIGNOFF.md"
      provides: "recorded user pick (V1/V2/V3 or adjustment) for the suite build"
      contains: "Selected variant"
  key_links:
    - from: ".planning/phases/104-logo-refinement-production-suite/104-02-SIGNOFF.md"
      to: "brandbook/logo/variants/*.svg"
      via: "named winning variant file path consumed by 104-03"
      pattern: "v[123]-"
---

<objective>
The LOGO-05 sign-off gate. Present the refinement page and three variant one-liners to the
user and capture their pick. This is a blocking human checkpoint (D-08): the executor presents,
the user decides, and the chosen variant is recorded for the production suite build.

Purpose: No production assets get built until the user has seen the logo in color and chosen a
direction. The pick is the contract 104-03 implements against.
Output: `104-02-SIGNOFF.md` recording the selected variant (or adjustment request).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/104-logo-refinement-production-suite/104-CONTEXT.md
@.planning/phases/104-logo-refinement-production-suite/104-01-SUMMARY.md
@brandbook/logo/refinement.html
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: LOGO-05 variant sign-off (blocking)</name>
  <what-built>
    Plan 104-01 produced 3 single-axis micro-variants of the winning Mark A + W1 wordmark and a
    file://-compatible color reveal page (refinement.html) showing each variant across all 4 ratified
    colorways (Light primary, Dark primary, Signal, OSS badge). The executor already screenshot-inspected
    the page (D-05) and confirmed all variants render legibly in every colorway.
  </what-built>
  <how-to-verify>
    1. Open `brandbook/logo/refinement.html` in a browser (works from file:// — just double-click or
       drag into a browser tab; no server needed).
    2. Compare the three variant rows across the four colorway columns:
       - V1 — baseline (as picked at the tournament; the control)
       - V2 — wider crossing notch (more open seam break)
       - V3 — heavier stroke weight (~7.2u vs 6.667u)
    3. Judge which reads best across all four colorways, especially Signal (brass mark on dark) and OSS
       badge (wake-teal mark on foam), where weight/notch differences show most.
    4. Note legibility of the wordmark terminal cuts (w tip, k arm) in each.
  </how-to-verify>
  <action>
    Do NOT self-approve. Present the three variant one-liners and the screenshot/page, then STOP and
    return CHECKPOINT REACHED. Record the user's response verbatim in
    `.planning/phases/104-logo-refinement-production-suite/104-02-SIGNOFF.md` under a "Selected variant"
    heading, naming the exact variant file (e.g. `brandbook/logo/variants/v2-notch.svg`). If the user
    requests an adjustment instead of a clean pick, record the adjustment and note that 104-03 must apply
    it before building the suite.
  </action>
  <resume-signal>Reply with "V1", "V2", or "V3" — or describe an adjustment (e.g. "V2 notch but V3 weight").</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user decision → recorded artifact | The pick becomes a locked input to 104-03; misrecording corrupts the suite |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-104-02a | Repudiation | recorded pick | mitigate | pick recorded verbatim in 104-02-SIGNOFF.md with the exact variant file path; no executor self-approval (D-08) |
| T-104-02b | Tampering | self-approval risk | mitigate | checkpoint is blocking + non-autonomous; executor returns CHECKPOINT REACHED, never proceeds on its own |
</threat_model>

<verification>
- `104-02-SIGNOFF.md` exists and names exactly one winning variant file path (or a clearly stated adjustment)
- Executor did not advance past the checkpoint without a user response
</verification>

<success_criteria>
- User saw the logo in color and chose a direction (D-08)
- The pick is recorded as the contract for 104-03
- No self-approval occurred (D-05, D-08)
</success_criteria>

<output>
Create `.planning/phases/104-logo-refinement-production-suite/104-02-SIGNOFF.md` recording the pick.
</output>
