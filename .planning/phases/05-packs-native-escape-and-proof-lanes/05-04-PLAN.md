---
phase: 05-packs-native-escape-and-proof-lanes
plan: 04
type: execute
wave: 2
depends_on:
  - 05-01
files_modified:
  - lib/crosswake/policy/schema.ex
  - lib/crosswake/policy/route.ex
  - lib/crosswake/policy/validator.ex
  - lib/crosswake/manifest/types.ex
  - lib/crosswake/manifest/builder.ex
  - lib/crosswake/manifest/validator.ex
  - lib/crosswake/transfer/contracts.ex
  - lib/crosswake/transfer/runtime.ex
  - test/support/router_fixtures.ex
  - test/crosswake/policy/schema_test.exs
  - test/crosswake/policy/route_test.exs
  - test/crosswake/manifest/manifest_test.exs
  - test/crosswake/manifest/validator_test.exs
  - test/crosswake/transfer/contracts_test.exs
autonomous: true
requirements:
  - PACK-04
must_haves:
  truths:
    - "Upload, download, import, and export seams are explicit route-local Crosswake contracts, not implicit WebView container behavior."
    - "Transfer intents and results are typed, versioned, semantic, and bounded to the active route."
    - "The manifest is the canonical truth for declared transfer seams before any bridge or shell execution is wired."
  artifacts:
    - path: "lib/crosswake/transfer/contracts.ex"
      provides: "Typed transfer intents, results, and route-local state vocabulary"
    - path: "lib/crosswake/manifest/types.ex"
      provides: "Manifest route-entry transfer seam declarations"
    - path: "lib/crosswake/policy/schema.ex"
      provides: "Typed route-policy transfer declarations"
  key_links:
    - from: "lib/crosswake/policy/schema.ex"
      to: "lib/crosswake/transfer/contracts.ex"
      via: "validated route transfer declarations compile into semantic transfer intents"
      pattern: "transfer|upload|download|import|export"
    - from: "lib/crosswake/manifest/builder.ex"
      to: "lib/crosswake/manifest/types.ex"
      via: "normalized route transfer truth becomes manifest-declared seams"
      pattern: "transfer|route|manifest"
---

<objective>
Define the transfer declaration and manifest truth before bridge exposure or native execution is added.

Purpose: Fulfill the declaration half of PACK-04 by replacing generic container file behavior with typed route-local upload, download, import, and export seams.
Output: Transfer DSL and manifest truth, typed transfer modules, fixtures, and tests for later bridge and shell execution work.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-packs-native-escape-and-proof-lanes/05-RESEARCH.md
@.planning/phases/05-packs-native-escape-and-proof-lanes/05-PATTERNS.md
@.planning/phases/05-packs-native-escape-and-proof-lanes/05-UI-SPEC.md
@lib/crosswake/policy/schema.ex
@lib/crosswake/policy/route.ex
@lib/crosswake/policy/validator.ex
@lib/crosswake/manifest/types.ex
@lib/crosswake/manifest/builder.ex
@test/support/router_fixtures.ex

<interfaces>
From Phase 5 UI spec:
```text
Allowed transfer states: queued, preparing, transferring, awaiting_network, verifying, complete, failed, canceled.
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add route-local transfer declarations and typed transfer state contracts</name>
  <files>lib/crosswake/policy/schema.ex, lib/crosswake/policy/route.ex, lib/crosswake/policy/validator.ex, lib/crosswake/transfer/contracts.ex, lib/crosswake/transfer/runtime.ex, test/support/router_fixtures.ex, test/crosswake/policy/schema_test.exs, test/crosswake/policy/route_test.exs, test/crosswake/transfer/contracts_test.exs</files>
  <behavior>
    - Test 1: Routes can declare explicit transfer seams for upload, download, import, and export with typed intent metadata and no dependence on generic file chooser behavior.
    - Test 2: Transfer state vocabulary matches the Phase 5 route-local UI contract and remains scoped to the active route.
    - Test 3: Validation rejects undeclared, ambiguous, or runtime-inconsistent transfer configurations.
  </behavior>
  <action>Extend route truth with explicit transfer declarations per PACK-04. Use semantic transfer intents with direction, source or destination policy, and verification requirements. Thread the Phase 5 transfer states directly into typed Elixir contracts so later native surfaces can render them consistently. Keep Phase 5 foreground-first and do not make WebView open-panel behavior or container download hooks the primary contract.</action>
  <verify>
    <automated>mix test test/crosswake/policy/schema_test.exs test/crosswake/policy/route_test.exs test/crosswake/transfer/contracts_test.exs</automated>
  </verify>
  <done>Crosswake now has typed route-local transfer seams and a stable transfer state vocabulary.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Compile transfer seam truth into the canonical manifest</name>
  <files>lib/crosswake/manifest/types.ex, lib/crosswake/manifest/builder.ex, lib/crosswake/manifest/validator.ex, test/crosswake/manifest/manifest_test.exs, test/crosswake/manifest/validator_test.exs</files>
  <behavior>
    - Test 1: The manifest exposes declared transfer seams per route using typed, versioned truth.
    - Test 2: Manifest validation fails when transfer seams are missing required metadata or conflict with route runtime truth.
    - Test 3: This plan stops at declaration and manifest truth; bridge exposure and platform execution are deferred to later plans.
  </behavior>
  <action>Compile normalized route transfer declarations into the manifest so Phoenix remains authoritative for PACK-04. Validate those seams at manifest build time and keep the contract route-local instead of inventing shell-local transfer metadata. Do not extend bridge commands or shell templates in this plan.</action>
  <verify>
    <automated>mix test test/crosswake/manifest/manifest_test.exs test/crosswake/manifest/validator_test.exs</automated>
  </verify>
  <done>The manifest now carries typed transfer truth that later bridge and generated-shell work can consume directly.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| route transfer declarations -> manifest truth | Ambiguous seam declarations here would blur route ownership and verification requirements |
| transfer results -> Phoenix upload or download workflows | Untyped or route-agnostic handoff would obscure authority and recovery behavior |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-10 | T | `lib/crosswake/transfer/contracts.ex` | mitigate | Use typed intent and result structs with explicit verification and route ownership fields |
| T-05-11 | I | generic WebView file behavior | mitigate | Keep container file chooser hooks out of the primary contract and expose only explicit transfer seams |
| T-05-12 | T | `lib/crosswake/manifest/validator.ex` | mitigate | Reject undeclared or runtime-inconsistent transfer seams before any shell consumes them |
</threat_model>

<verification>
Run `mix test test/crosswake/policy/schema_test.exs test/crosswake/policy/route_test.exs test/crosswake/manifest/manifest_test.exs test/crosswake/manifest/validator_test.exs test/crosswake/transfer/contracts_test.exs` and confirm transfer seams remain route-local and manifest-owned.
</verification>

<success_criteria>
- Routes declare explicit upload, download, import, and export seams.
- Transfer state vocabulary is typed and route-local.
- The manifest owns transfer seam truth before any bridge or shell execution is added.
</success_criteria>

<output>
After completion, create `.planning/phases/05-packs-native-escape-and-proof-lanes/05-packs-native-escape-and-proof-lanes-04-SUMMARY.md`
</output>
