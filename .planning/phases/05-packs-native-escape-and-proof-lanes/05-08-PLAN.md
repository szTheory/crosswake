---
phase: 05-packs-native-escape-and-proof-lanes
plan: 08
type: execute
wave: 6
depends_on:
  - 05-03
  - 05-06
  - 05-07
files_modified:
  - .github/workflows/phase5-proof.yml
  - examples/phoenix_host/mix.exs
  - examples/phoenix_host/config/config.exs
  - examples/phoenix_host/lib/crosswake_example/router.ex
  - examples/ios_shell_host/CrosswakeShell.xcodeproj/project.pbxproj
  - examples/ios_shell_host/CrosswakeShellApp.swift
  - examples/android_shell_host/app/build.gradle
  - examples/android_shell_host/app/src/main/AndroidManifest.xml
  - examples/android_shell_host/app/src/main/java/dev/crosswake/shell/MainActivity.kt
  - script/verify_phase5_example_hosts.sh
  - script/verify_generated_ios_shell.sh
  - script/verify_generated_android_shell.sh
  - test/crosswake/proof/phase5_proof_lane_test.exs
autonomous: true
requirements:
  - DX-03
must_haves:
  truths:
    - "Checked-in Phoenix, iOS, and Android example hosts verify the documented public install path for packs, transfer seams, and native capture."
    - "Generated-host smoke and proof lanes remain secondary verification and pass alongside the checked-in example hosts."
    - "Phase 5 proof is deterministic in CI and exits green for Phoenix, iOS, and Android rather than preserving a blocked posture."
  artifacts:
    - path: ".github/workflows/phase5-proof.yml"
      provides: "Deterministic CI lane for Phase 5 public install proof"
    - path: "script/verify_phase5_example_hosts.sh"
      provides: "Single entrypoint that verifies checked-in example hosts across Phoenix, iOS, and Android"
    - path: "examples/ios_shell_host/CrosswakeShellApp.swift"
      provides: "Checked-in iOS example host artifact used by the public proof lane"
  key_links:
    - from: "script/verify_phase5_example_hosts.sh"
      to: "examples/phoenix_host/lib/crosswake_example/router.ex"
      via: "proof script exercises the same checked-in example route surfaces documented to adopters"
      pattern: "packs|transfer|native_screen|media_capture"
    - from: ".github/workflows/phase5-proof.yml"
      to: "script/verify_generated_ios_shell.sh"
      via: "CI runs generated-host verification as secondary proof after example-host checks"
      pattern: "verify_phase5_example_hosts|verify_generated_ios_shell|verify_generated_android_shell"
---

<objective>
Make DX-03 genuinely achievable by landing passing proof lanes before any final publication work.

Purpose: Fulfill the hard proof requirement in Phase 5 by converting the current blocked posture into deterministic Phoenix, iOS, and Android install verification.
Output: Checked-in example hosts, passing example and generated proof scripts, CI workflow wiring, and proof tests for the public install path.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-packs-native-escape-and-proof-lanes/05-RESEARCH.md
@.planning/phases/05-packs-native-escape-and-proof-lanes/05-PATTERNS.md
@.planning/phases/05-packs-native-escape-and-proof-lanes/05-UI-SPEC.md
@.planning/phases/03-native-shell-boot-and-bounded-bridge/03-native-shell-boot-and-bounded-bridge-06-SUMMARY.md
@.planning/phases/04-honest-offline-contract/04-honest-offline-contract-04-SUMMARY.md
@script/verify_generated_ios_shell.sh
@script/verify_generated_android_shell.sh
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Check in runnable Phoenix, iOS, and Android example hosts for the public install path</name>
  <files>examples/phoenix_host/mix.exs, examples/phoenix_host/config/config.exs, examples/phoenix_host/lib/crosswake_example/router.ex, examples/ios_shell_host/CrosswakeShell.xcodeproj/project.pbxproj, examples/ios_shell_host/CrosswakeShellApp.swift, examples/android_shell_host/app/build.gradle, examples/android_shell_host/app/src/main/AndroidManifest.xml, examples/android_shell_host/app/src/main/java/dev/crosswake/shell/MainActivity.kt</files>
  <behavior>
    - Test 1: The repo contains checked-in example hosts for Phoenix, iOS, and Android that exercise declared pack, transfer, and native-capture route surfaces.
    - Test 2: Example hosts stay faithful to the public install path rather than using private scaffolding or ad hoc local edits.
    - Test 3: The example hosts keep the first escape hatch limited to one `:native_screen` media-capture flow.
  </behavior>
  <action>Create checked-in example hosts that represent the public artifact class for DX-03. The Phoenix host should expose one pack-backed route, one explicit transfer seam, and one `:native_screen` media-capture route. The iOS and Android example hosts should consume those same declarations without hidden local patches.</action>
  <verify>
    <automated>bash script/verify_phase5_example_hosts.sh</automated>
  </verify>
  <done>The repo now contains checked-in example hosts that the Phase 5 public proof lane can exercise directly.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Make example-host and generated-host proof lanes pass deterministically in CI</name>
  <files>.github/workflows/phase5-proof.yml, script/verify_phase5_example_hosts.sh, script/verify_generated_ios_shell.sh, script/verify_generated_android_shell.sh, test/crosswake/proof/phase5_proof_lane_test.exs</files>
  <behavior>
    - Test 1: `script/verify_phase5_example_hosts.sh` exits green only when Phoenix, iOS, and Android example-host checks all pass.
    - Test 2: `script/verify_generated_ios_shell.sh` and `script/verify_generated_android_shell.sh` pass as secondary proof lanes instead of remaining blocked.
    - Test 3: CI runs the same proof entrypoints deterministically and fails closed if any public install lane regresses.
  </behavior>
  <action>Replace the current blocked proof posture with real passing lanes. Fix the iOS and Android verification scripts, add a dedicated CI workflow, and keep proof output separated per surface so failures remain actionable. The goal of this plan is green proof, not better wording about red proof.</action>
  <verify>
    <automated>bash script/verify_phase5_example_hosts.sh</automated>
    <automated>bash script/verify_generated_ios_shell.sh</automated>
    <automated>bash script/verify_generated_android_shell.sh</automated>
    <automated>mix test test/crosswake/proof/phase5_proof_lane_test.exs</automated>
  </verify>
  <done>Phoenix, iOS, and Android proof lanes now pass deterministically in both checked-in example-host and generated-host form.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| proof scripts -> public support claims | A soft or partial proof lane would overstate install support |
| checked-in example hosts -> generated-host verification | Divergence here would make the public artifact class untrustworthy |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-22 | R | proof scripts and CI workflow | mitigate | Run the same explicit example-host and generated-host entrypoints in CI and fail closed on any red lane |
| T-05-23 | T | checked-in example hosts | mitigate | Keep example hosts aligned to the same route declarations and proof scripts exercised by generated hosts |
| T-05-24 | D | platform proof posture | mitigate | Require green Phoenix, iOS, and Android lanes before downstream doctor or guide publication can claim support |
</threat_model>

<verification>
Run `bash script/verify_phase5_example_hosts.sh`, `bash script/verify_generated_ios_shell.sh`, `bash script/verify_generated_android_shell.sh`, and `mix test test/crosswake/proof/phase5_proof_lane_test.exs` and confirm all proof lanes pass.
</verification>

<success_criteria>
- Checked-in example hosts and generated-host lanes both pass.
- CI runs the same proof entrypoints deterministically.
- Phase 5 no longer relies on a blocked or verification-required posture for DX-03 completion.
</success_criteria>

<output>
After completion, create `.planning/phases/05-packs-native-escape-and-proof-lanes/05-packs-native-escape-and-proof-lanes-08-SUMMARY.md`
</output>
