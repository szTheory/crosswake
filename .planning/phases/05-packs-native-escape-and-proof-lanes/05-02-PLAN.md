---
phase: 05-packs-native-escape-and-proof-lanes
plan: 02
type: execute
wave: 2
depends_on:
  - 05-01
files_modified:
  - lib/crosswake/packs/contracts.ex
  - lib/crosswake/packs/inventory.ex
  - lib/crosswake/packs/runtime.ex
  - lib/crosswake/compatibility/compatibility.ex
  - lib/crosswake/shell/activation.ex
  - test/crosswake/packs/contracts_test.exs
  - test/crosswake/packs/runtime_test.exs
  - test/crosswake/compatibility/compatibility_test.exs
  - test/crosswake/shell/activation_test.exs
autonomous: true
requirements:
  - PACK-02
must_haves:
  truths:
    - "Pack availability is explicit and fail-closed: a route cannot activate until its declared packs are installed and compatible."
    - "Pack lifecycle state covers install, verify, availability, stale, invalidation, and failure without drifting into a generic asset loader."
    - "Crosswake exposes one typed pack lifecycle vocabulary that generated shells and proof lanes can consume directly."
  artifacts:
    - path: "lib/crosswake/packs/contracts.ex"
      provides: "Typed pack lifecycle structs and state vocabulary"
    - path: "lib/crosswake/packs/inventory.ex"
      provides: "Installed-pack inventory contract with verification metadata"
    - path: "lib/crosswake/compatibility/compatibility.ex"
      provides: "Fail-closed pack lifecycle gating wired into shared denial posture"
  key_links:
    - from: "lib/crosswake/packs/runtime.ex"
      to: "lib/crosswake/compatibility/compatibility.ex"
      via: "runtime lifecycle state becomes compatibility-safe activation decisions"
      pattern: "install|available|stale|invalidate|pack_incompatible"
    - from: "lib/crosswake/packs/inventory.ex"
      to: "lib/crosswake/shell/activation.ex"
      via: "installed-pack truth governs route allow or deny decisions"
      pattern: "installed_packs|required_packs|verified_at|pack_incompatible"
---

<objective>
Define the pack lifecycle contract and fail-closed activation rules before any generated shell runtime work starts.

Purpose: Fulfill the contract/gating half of PACK-02 with one typed lifecycle vocabulary and explicit availability checks.
Output: Pack lifecycle modules, compatibility and activation gating, and focused tests for install, verification, stale detection, invalidation, and denial behavior.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-packs-native-escape-and-proof-lanes/05-RESEARCH.md
@.planning/phases/05-packs-native-escape-and-proof-lanes/05-PATTERNS.md
@.planning/phases/05-packs-native-escape-and-proof-lanes/05-UI-SPEC.md
@.planning/phases/05-packs-native-escape-and-proof-lanes/05-01-PLAN.md
@lib/crosswake/compatibility/compatibility.ex
@lib/crosswake/shell/activation.ex

<interfaces>
From Phase 5 UI spec:
```text
Allowed pack states: checking, not_installed, installing, available, stale, invalidating, failed.
```

From lib/crosswake/shell/activation.ex:
```elixir
installed_packs
declared_pack_requirements
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add typed pack lifecycle and inventory contracts</name>
  <files>lib/crosswake/packs/contracts.ex, lib/crosswake/packs/inventory.ex, lib/crosswake/packs/runtime.ex, test/crosswake/packs/contracts_test.exs, test/crosswake/packs/runtime_test.exs</files>
  <behavior>
    - Test 1: Pack lifecycle types encode install, verify, available, stale, invalidating, and failed states with metadata needed by shell UI and proof lanes.
    - Test 2: Installed-pack inventory records required version, installed version, bytes, integrity status, and latest verification timestamp.
    - Test 3: Invalidation preserves the last known state and exposes enough metadata for deterministic retry or update paths.
  </behavior>
  <action>Create a dedicated `Crosswake.Packs` contract namespace so lifecycle semantics stay explicit per PACK-02. Use the Phase 5 pack-state vocabulary directly and keep scope narrow: install, availability, verify, invalidation, retry, and failure metadata only. Do not turn this into a generic asset manager, background downloader, or sync subsystem.</action>
  <verify>
    <automated>mix test test/crosswake/packs/contracts_test.exs test/crosswake/packs/runtime_test.exs</automated>
  </verify>
  <done>Crosswake has one typed pack lifecycle vocabulary and inventory contract for all later shell, doctor, and proof work.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Enforce fail-closed pack lifecycle gating in compatibility and activation</name>
  <files>lib/crosswake/compatibility/compatibility.ex, lib/crosswake/shell/activation.ex, test/crosswake/compatibility/compatibility_test.exs, test/crosswake/shell/activation_test.exs</files>
  <behavior>
    - Test 1: Missing, stale, invalidated, or unverified packs deny route activation with the existing `pack_incompatible` posture.
    - Test 2: A route activates only when installed-pack inventory satisfies manifest-declared version and integrity truth.
    - Test 3: This plan stops at contract and gating truth; it does not yet claim generated-shell install execution.
  </behavior>
  <action>Thread the new lifecycle truth into compatibility and activation so pack checks fail closed immediately per PACK-02. Reuse the existing denial vocabulary and recovery posture instead of inventing a new failure surface. Keep generated-shell install, inventory rendering, and template wiring out of this plan; that execution work belongs in the next slice.</action>
  <verify>
    <automated>mix test test/crosswake/compatibility/compatibility_test.exs test/crosswake/shell/activation_test.exs</automated>
  </verify>
  <done>Compatibility and activation now block routes until required packs verify as installed and current.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| manifest pack registry -> on-device inventory | If installed state drifts from declared version or integrity truth, unsafe routes may activate |
| pack lifecycle state -> route activation | Incorrect allow decisions here would bypass explicit pack requirements |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-04 | T | `lib/crosswake/packs/inventory.ex` | mitigate | Require installed version, integrity status, and verification timestamp in inventory records used for availability checks |
| T-05-05 | T | `lib/crosswake/compatibility/compatibility.ex` | mitigate | Deny activation for missing, stale, invalidated, or incompatible packs and reuse `pack_incompatible` recovery posture |
| T-05-06 | D | `lib/crosswake/shell/activation.ex` | mitigate | Keep activation fail-closed until lifecycle state resolves to available rather than allowing partial pack installs to proceed |
</threat_model>

<verification>
Run `mix test test/crosswake/packs/contracts_test.exs test/crosswake/packs/runtime_test.exs test/crosswake/compatibility/compatibility_test.exs test/crosswake/shell/activation_test.exs` and confirm pack lifecycle state governs route availability deterministically.
</verification>

<success_criteria>
- Crosswake exposes typed install, verify, availability, stale, invalidation, and failure pack states.
- Compatibility and activation deny routes when declared packs are missing or stale.
- Generated-shell runtime execution remains deferred to the next plan instead of being mixed into this contract slice.
</success_criteria>

<output>
After completion, create `.planning/phases/05-packs-native-escape-and-proof-lanes/05-packs-native-escape-and-proof-lanes-02-SUMMARY.md`
</output>
