---
phase: 05-packs-native-escape-and-proof-lanes
plan: 05
type: execute
wave: 3
depends_on:
  - 05-04
files_modified:
  - lib/crosswake/bridge/contract.ex
  - lib/crosswake/bridge/registry.ex
  - test/crosswake/bridge/contract_test.exs
  - test/crosswake/bridge/registry_test.exs
autonomous: true
requirements:
  - PACK-04
must_haves:
  truths:
    - "The bridge allowlist exposes only the explicit transfer commands Crosswake supports."
    - "Transfer commands are bounded to manifest-declared route seams and active-route checks."
    - "This slice narrows PACK-04 to bridge exposure truth only; platform execution remains downstream."
  artifacts:
    - path: "lib/crosswake/bridge/contract.ex"
      provides: "Versioned bridge envelope and explicit transfer command vocabulary"
    - path: "lib/crosswake/bridge/registry.ex"
      provides: "Manifest-backed allowlist for transfer commands"
  key_links:
    - from: "lib/crosswake/bridge/registry.ex"
      to: "lib/crosswake/manifest/builder.ex"
      via: "manifest-declared transfer seams determine which bridge commands are allowlisted"
      pattern: "transfer|command|registry|route"
---

<objective>
Bound the bridge surface to explicit transfer commands after transfer seam truth exists.

Purpose: Fulfill the bridge exposure half of PACK-04 without mixing in native-shell execution.
Output: Updated bridge contract and registry plus focused tests proving undeclared transfer commands fail closed.
</objective>

<execution_context>
@/Users/jon/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jon/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-packs-native-escape-and-proof-lanes/05-RESEARCH.md
@.planning/phases/05-packs-native-escape-and-proof-lanes/05-PATTERNS.md
@.planning/phases/05-packs-native-escape-and-proof-lanes/05-04-PLAN.md
@lib/crosswake/bridge/contract.ex
@lib/crosswake/bridge/registry.ex

<interfaces>
From lib/crosswake/bridge/contract.ex:
```elixir
@commands ["app.info.get", "haptics.impact", "files.pick"]
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend the bridge contract with explicit transfer commands</name>
  <files>lib/crosswake/bridge/contract.ex, test/crosswake/bridge/contract_test.exs</files>
  <behavior>
    - Test 1: The bridge contract exposes only the explicit transfer commands needed for Phase 5 with typed request and reply envelopes.
    - Test 2: The command vocabulary stays noun-first and bounded rather than exposing generic file or URL authority.
    - Test 3: This plan does not yet claim iOS or Android command execution.
  </behavior>
  <action>Extend the bounded bridge surface with explicit commands such as `transfer.import`, `transfer.export`, `transfer.download`, and `transfer.upload.prepare` per PACK-04. Preserve the request and reply-only posture and do not widen the bridge into generic file chooser, share sheet, or browser authority.</action>
  <verify>
    <automated>mix test test/crosswake/bridge/contract_test.exs</automated>
  </verify>
  <done>The bridge advertises only explicit transfer seams and remains typed, versioned, and bounded.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Allowlist transfer commands from manifest-declared route truth only</name>
  <files>lib/crosswake/bridge/registry.ex, test/crosswake/bridge/registry_test.exs</files>
  <behavior>
    - Test 1: Transfer commands are allowlisted from manifest and route truth just like existing bounded bridge commands.
    - Test 2: Undeclared transfer commands fail closed and do not execute side effects.
    - Test 3: Platform bridge-channel wiring remains deferred to the later shell execution plan.
  </behavior>
  <action>Keep the existing manifest-backed allowlist posture and active-route enforcement. Make transfer commands depend on manifest-declared route transfer seams and reject everything else fail-closed. Do not wire iOS or Android channel handlers in this plan; native execution belongs in the later generated-shell slice.</action>
  <verify>
    <automated>mix test test/crosswake/bridge/registry_test.exs</automated>
  </verify>
  <done>The registry now exposes only manifest-declared transfer commands and leaves native execution to a downstream plan.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| route transfer declarations -> bridge commands | Overbroad command exposure here would turn Crosswake into a generic container file bridge |
| transfer command allowlist -> platform handlers | If undeclared commands leak through, later shell code inherits unsafe authority |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-13 | E | `lib/crosswake/bridge/registry.ex` | mitigate | Restrict transfer commands to manifest-declared route seams and reject undeclared commands fail-closed |
| T-05-14 | T | `lib/crosswake/bridge/contract.ex` | mitigate | Use typed request and reply envelopes for a small explicit transfer command set |
| T-05-15 | I | generic WebView file behavior | mitigate | Keep container file chooser hooks out of the bridge contract and registry |
</threat_model>

<verification>
Run `mix test test/crosswake/bridge/contract_test.exs test/crosswake/bridge/registry_test.exs` and confirm the bridge exposes only explicit manifest-backed transfer commands.
</verification>

<success_criteria>
- The bridge exposes only explicit transfer commands Phase 5 supports.
- Transfer commands are allowlisted from manifest and route truth.
- Native bridge-channel execution remains deferred instead of being mixed into this contract slice.
</success_criteria>

<output>
After completion, create `.planning/phases/05-packs-native-escape-and-proof-lanes/05-packs-native-escape-and-proof-lanes-05-SUMMARY.md`
</output>
