---
phase: 102-brand-audit-token-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .gitignore
  - brandbook/AUDIT.md
  - brandbook/tools/contrast.mjs
autonomous: true
requirements: [AUDT-02]
must_haves:
  truths:
    - "Running `node brandbook/tools/contrast.mjs` prints a markdown contrast matrix with >=20 pairings"
    - "Stone 500 on Foam 50 reports FAIL for AA normal text"
    - "Stone 600 on Foam 50 reports PASS for AA normal text"
    - "brandbook/AUDIT.md exists with all 14 numbered section headings plus an Appendix A heading"
    - ".gitignore excludes brandbook/tools/node_modules and brandbook/tools/fonts"
  artifacts:
    - path: "brandbook/tools/contrast.mjs"
      provides: "WCAG 2.2 contrast matrix script (zero-dependency Node ESM)"
      contains: "0.04045"
      min_lines: 60
    - path: "brandbook/AUDIT.md"
      provides: "14-section audit scaffold with section headings"
      contains: "## §1 Executive Judgment"
    - path: ".gitignore"
      provides: "brandbook tooling exclusions"
      contains: "/brandbook/tools/node_modules/"
  key_links:
    - from: "brandbook/tools/contrast.mjs"
      to: "WCAG relative-luminance computation"
      via: "linearize() with 0.04045 threshold"
      pattern: "0\\.04045"
---

<objective>
Establish the Phase 102 brandbook/ foundation and the WCAG contrast matrix script that all downstream audit verdicts and token $descriptions cite as ground truth (AUDT-02).

Purpose: The contrast matrix is the dependency root of the phase — audit color verdicts (Plan 03/04) and token usage restrictions (Plan 02) must cite reproducible, computed contrast numbers, not estimates. This plan creates the directory structure, the .gitignore exclusions (before any node_modules can appear), the 14-section AUDIT.md scaffold (so later prose tasks fill, not invent, structure), and the contrast.mjs script.

Output: brandbook/ + brandbook/tools/ directories, brandbook/AUDIT.md (14-section scaffold), brandbook/tools/contrast.mjs (runnable, producing the matrix), .gitignore additions.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/102-brand-audit-token-foundation/102-CONTEXT.md
@.planning/phases/102-brand-audit-token-foundation/102-RESEARCH.md
@.planning/phases/102-brand-audit-token-foundation/102-PATTERNS.md
@.planning/phases/102-brand-audit-token-foundation/102-AUDIT-BRIEF.md

<interfaces>
<!-- The 17 palette primitives the contrast matrix operates over (from RESEARCH.md Finding 3). -->
<!-- These hex values are LOCKED — do not alter. Stone 600 (#756D63) is the only addition per D-02. -->
current-950 #09141A · current-900 #0F1E26 · current-800 #162B35 · harbor-700 #254855
wake-700 #2B756A · wake-500 #4E9A8E · kelp-800 #123B36 · brass-500 #C98A2E · brass-700 #946017
foam-50 #F7F1E6 · foam-100 #EFE6D6 · mist-200 #C9D4CF · stone-500 #7C746A · stone-600 #756D63
rust-600 #9A4D35 · plum-700 #372D4C · white #FFFFFF

<!-- Verified expected ratios (RESEARCH.md Finding 4) the script output MUST reproduce: -->
<!-- foam-50/current-950 16.58:1 PASS · stone-500/foam-50 4.09:1 FAIL · stone-600/foam-50 4.53:1 PASS -->
<!-- brass-700/foam-50 4.74:1 PASS · wake-700/foam-50 4.85:1 PASS · wake-500/foam-50 2.95:1 FAIL -->
<!-- mist-200/foam-50 1.35:1 FAIL · mist-200/current-950 12.25:1 PASS · stone-600/current-950 3.66:1 (non-text only) -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create brandbook structure and .gitignore exclusions</name>
  <files>brandbook/tools/ (directory), brandbook/tokens/ (directory), .gitignore</files>
  <read_first>
    - .gitignore (current state — see existing `# Local tooling / harness state` block at the end, lines ~28-32)
    - .planning/phases/102-brand-audit-token-foundation/102-PATTERNS.md (.gitignore group pattern section)
  </read_first>
  <action>
    Create directories `brandbook/tools/` and `brandbook/tokens/` (use a .gitkeep or let subsequent tasks populate them — directories with files committed by later tasks are fine). Append exactly two ignore lines to the END of .gitignore, after the existing `# Local tooling / harness state` block, matching its `# comment` then `/path/` style: a comment line `# brandbook tooling (Node processing — not committed)` followed by `/brandbook/tools/node_modules/` and `/brandbook/tools/fonts/`. Do NOT ignore brandbook/ itself or brandbook/tools/ — only the node_modules and fonts subdirectories. The brandbook/ directory is committed.
  </action>
  <verify>
    <automated>grep -q '^/brandbook/tools/node_modules/$' .gitignore && grep -q '^/brandbook/tools/fonts/$' .gitignore && test -d brandbook/tools && test -d brandbook/tokens && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `.gitignore` contains the literal line `/brandbook/tools/node_modules/`
    - `.gitignore` contains the literal line `/brandbook/tools/fonts/`
    - `.gitignore` does NOT contain a bare `/brandbook/` or `/brandbook/tools/` ignore line
    - Directories `brandbook/tools/` and `brandbook/tokens/` exist
  </acceptance_criteria>
  <done>brandbook/tools/ and brandbook/tokens/ exist; .gitignore excludes only node_modules and fonts subdirectories under tools/.</done>
</task>

<task type="auto">
  <name>Task 2: Scaffold brandbook/AUDIT.md with the 14-section structure</name>
  <files>brandbook/AUDIT.md</files>
  <read_first>
    - .planning/phases/102-brand-audit-token-foundation/102-AUDIT-BRIEF.md (the "Required output structure" — exact 14 section titles)
    - .planning/phases/102-brand-audit-token-foundation/102-PATTERNS.md (the "Section heading scaffold" block — copy its heading list verbatim)
    - guides/threadline.md (lines 1-40 — declarative no-hedge markdown convention to match)
  </read_first>
  <action>
    Create brandbook/AUDIT.md with: a top-level `# Crosswake Brand System Audit` title; a 3-line header block (`**Audited:** 2026-06-11`, `**Subject:** prompts/crosswake-brand-book.md`, `**Auditor posture:** Senior brand systems director / design-token architect / OSS maintainer`); a `---` rule; then the 14 numbered section headings exactly as titled in the brief: `## §1 Executive Judgment`, `## §2 Brand DNA Extraction`, `## §3 Pressure-Test Scorecard`, `## §4 Stress Tests`, `## §5 Gaps and Risks`, `## §6 Recommended Brand Book Upgrades`, `## §7 Design Token Specification`, `## §8 Logo and Mark System`, `## §9 Visual Examples and Screenshot Guidance`, `## §10 Brand Voice and Microcopy`, `## §11 Landing Page and Docs Blueprint`, `## §12 Repo-Ready Artifact Plan`, `## §13 Prioritized Action Plan`, `## §14 Final Quality Gate`; a `---` rule; then `## Appendix A: WCAG Contrast Matrix`. Under each section heading leave a single placeholder line `_(pending)_` so the file parses and the section count is verifiable. Sections will be filled by Plans 03 and 04. Do NOT use YAML frontmatter (AUDIT.md is a prose brand document, not a planning artifact — per PATTERNS.md).
  </action>
  <verify>
    <automated>test "$(grep -c '^## §' brandbook/AUDIT.md)" -ge 14 && grep -q '^## §1 Executive Judgment' brandbook/AUDIT.md && grep -q '^## §14 Final Quality Gate' brandbook/AUDIT.md && grep -q '^## Appendix A' brandbook/AUDIT.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '^## §' brandbook/AUDIT.md` returns exactly 14
    - Section headings `## §1 Executive Judgment` through `## §14 Final Quality Gate` all present in order
    - `## Appendix A: WCAG Contrast Matrix` heading present
    - No YAML frontmatter (first line is `# Crosswake Brand System Audit`)
  </acceptance_criteria>
  <done>AUDIT.md has all 14 numbered section headings plus Appendix A, each with a `_(pending)_` placeholder, no frontmatter.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Write contrast.mjs WCAG matrix script</name>
  <files>brandbook/tools/contrast.mjs</files>
  <behavior>
    - linearize(c) uses the 0.04045 threshold (not 0.03928): s <= 0.04045 ? s/12.92 : ((s+0.055)/1.055)^2.4
    - contrast(foam-50, current-950) returns ~16.58, classified PASS for AA normal text
    - contrast(stone-500, foam-50) returns ~4.09, classified FAIL for AA normal text (< 4.5)
    - contrast(stone-600, foam-50) returns ~4.53, classified PASS for AA normal text (>= 4.5)
    - contrast(wake-500, foam-50) returns ~2.95, classified FAIL
    - parseHex rejects a malformed hex string (e.g. "#ZZZ") by throwing
    - Running the script prints a markdown table with a header row and >=20 pairing rows
  </behavior>
  <read_first>
    - .planning/phases/102-brand-audit-token-foundation/102-PATTERNS.md (contrast.mjs structure block + parseHex input-validation block — vanilla JS conventions: const, named functions, semicolons, try/catch)
    - .planning/phases/102-brand-audit-token-foundation/102-RESEARCH.md (Finding 4 — full PAIRS list with expected ratios; algorithm with verified WCAG formula)
    - priv/templates/crosswake/offline_ui/offline.js.eex (lines 1-43 — the JS module style analog)
  </read_first>
  <action>
    Create brandbook/tools/contrast.mjs as a zero-dependency Node ESM script (`#!/usr/bin/env node`, no imports). Implement: `linearize(c)` using the WCAG 2.2 corrected threshold 0.04045 (off-by-one on this threshold produces wrong AA verdicts for boundary colors like Stone 500 at 4.09 — use 0.04045 exactly, not 0.03928); `luminance(r,g,b)` = 0.2126*lin(r)+0.7152*lin(g)+0.0722*lin(b); `parseHex(hex)` that validates against `/^[0-9a-fA-F]{6}$/` after stripping `#` and throws `Invalid hex color` on failure (ASVS V5 input validation); `contrast(hex1,hex2)` returning (Llight+0.05)/(Ldark+0.05). Hardcode the 17-primitive PALETTE constant (all hex from the interfaces block — including stone-600 #756D63). Define a PAIRS array covering at minimum the 20 pairings enumerated in RESEARCH.md Finding 4 (foam-50/current-950, white/wake-700, current-950/brass-500, wake-500/current-950, wake-700/foam-50, white/rust-600, foam-50/plum-700, stone-500/foam-50 [FAIL], stone-600/foam-50 [PASS], stone-600/white, brass-700/foam-50, wake-500/foam-50 [FAIL], mist-200/foam-50 [FAIL], foam-50/current-900, mist-200/current-950, wake-500/current-900, brass-500/current-950, white/kelp-800, white/harbor-700, and stone-600/current-950 [non-text only]). Print a markdown table with columns Foreground | Background | Ratio | AA | AAA, where AA = PASS if ratio >= 4.5 else FAIL, AAA = PASS if ratio >= 7.0 else FAIL; round ratios to 2 decimals. End with a summary line counting total pairings and how many fail AA normal text. Output format is Claude's discretion within these columns.
  </action>
  <verify>
    <automated>node brandbook/tools/contrast.mjs | grep -E 'PASS|FAIL' | head -1 && test "$(node brandbook/tools/contrast.mjs | grep -cE 'PASS|FAIL')" -ge 20 && node brandbook/tools/contrast.mjs | grep -E 'stone-500.*foam-50|foam-50.*stone-500' | grep -q FAIL && node brandbook/tools/contrast.mjs | grep -E 'stone-600.*foam-50|foam-50.*stone-600' | grep -q PASS && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `node brandbook/tools/contrast.mjs` exits 0 and prints a markdown table
    - Output contains >= 20 rows matching `PASS|FAIL`
    - The stone-500 / foam-50 row shows FAIL for AA
    - The stone-600 / foam-50 row shows PASS for AA
    - The wake-500 / foam-50 row shows FAIL for AA
    - Source contains the literal `0.04045` (correct WCAG threshold)
    - `node -e "import('./brandbook/tools/contrast.mjs')"` does not error on a malformed-hex unit path (parseHex throws on invalid input)
  </acceptance_criteria>
  <done>contrast.mjs runs zero-dep, prints a >=20-row matrix with correct AA/AAA verdicts; Stone 500/Foam 50 = FAIL, Stone 600/Foam 50 = PASS reproduced.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| developer → contrast.mjs hex input | Hex color strings parsed into integers; malformed input crosses here |
| repo → git working tree | Generated/committed files must not leak secrets or node_modules |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-102-01 | Tampering | contrast.mjs hex parsing | mitigate | parseHex validates `/^[0-9a-fA-F]{6}$/` before parseInt; throws on malformed input (ASVS V5) |
| T-102-02 | Information disclosure | committed brandbook/ files | accept | Files are color hex + markdown only; no secrets, no PII, low-value target |
| T-102-03 | Tampering | accidental node_modules commit | mitigate | .gitignore excludes /brandbook/tools/node_modules/ and /brandbook/tools/fonts/ before any install |
| T-102-SC | Tampering | npm/pip/cargo installs | mitigate | No package installs this phase — both scripts are zero-dependency Node; nothing to audit |
</threat_model>

<verification>
- `node brandbook/tools/contrast.mjs` produces the full matrix (>=20 rows) with correct boundary verdicts
- `grep -c '^## §' brandbook/AUDIT.md` == 14
- `.gitignore` contains both brandbook tooling exclusion lines
- No npm packages installed (zero-dependency requirement honored)
</verification>

<success_criteria>
- brandbook/ structure exists; AUDIT.md scaffolded with 14 sections + Appendix A
- contrast.mjs runs zero-dep and reproduces the verified WCAG matrix (AUDT-02 ground truth)
- .gitignore prevents node_modules/fonts from ever being committed
</success_criteria>

<output>
Create `.planning/phases/102-brand-audit-token-foundation/102-01-SUMMARY.md` when done
</output>
