---
phase: 102-brand-audit-token-foundation
plan: 02
type: execute
wave: 2
depends_on: ["102-01"]
files_modified:
  - brandbook/tokens/crosswake.tokens.json
  - brandbook/tools/compile-tokens.js
  - brandbook/tokens/tokens.css
autonomous: true
requirements: [TOKN-01, TOKN-02, TOKN-03]
must_haves:
  truths:
    - "crosswake.tokens.json parses as valid JSON and exposes primitive, surface, text, action, border, status, runtime groups"
    - "The JSON contains primitive.stone.600 = #756D63 and all 5 runtime tokens (liveview, offline, native, sensitive, bridge)"
    - "Running `node brandbook/tools/compile-tokens.js` regenerates tokens.css deterministically"
    - "tokens.css semantic tokens reference var(--cw-primitive-*), never inline hex"
    - "tokens.css contains a prefers-color-scheme:dark block and a [data-theme=dark] block"
    - "All 12 states are representable: hover/focus tokens, status.{success,warning,error,info}, text.{muted,subtle} present"
  artifacts:
    - path: "brandbook/tokens/crosswake.tokens.json"
      provides: "DTCG 2025.10 token source of truth (primitive -> semantic tiers + non-color tokens)"
      contains: "\"runtime\""
      min_lines: 120
    - path: "brandbook/tools/compile-tokens.js"
      provides: "Zero-dep JSON -> CSS compiler with alias resolution"
      contains: "crosswake.tokens.json"
      min_lines: 40
    - path: "brandbook/tokens/tokens.css"
      provides: "Generated, committed CSS custom properties (primitive + semantic tiers, light/dark)"
      contains: "GENERATED from crosswake.tokens.json"
  key_links:
    - from: "brandbook/tools/compile-tokens.js"
      to: "brandbook/tokens/tokens.css"
      via: "DTCG {primitive.x.y} alias resolution to var(--cw-primitive-x-y)"
      pattern: "var\\(--cw-primitive-"
    - from: "brandbook/tokens/crosswake.tokens.json"
      to: "primitive.stone.600"
      via: "D-02 math-forced primitive addition"
      pattern: "756D63"
---

<objective>
Emit the design-token foundation: the DTCG 2025.10 source of truth (crosswake.tokens.json), the zero-dependency compiler (compile-tokens.js), and the committed generated CSS (tokens.css) covering primitive -> semantic tiers, light/dark theming, and all 12 states (TOKN-01, TOKN-02, TOKN-03).

Purpose: This is the enforceable output of the audit — the locked palette (D-01..D-04) and bounded semantic vocabulary (D-05..D-09) encoded as machine-readable tokens. Usage restrictions live in DTCG $description fields (D-03), not prose. Phases 103-106 build CSS specimens and the HTML brand book directly on these tokens.

Output: brandbook/tokens/crosswake.tokens.json (~62 tokens), brandbook/tools/compile-tokens.js (<80 LOC), brandbook/tokens/tokens.css (generated, committed).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/102-brand-audit-token-foundation/102-CONTEXT.md
@.planning/phases/102-brand-audit-token-foundation/102-RESEARCH.md
@.planning/phases/102-brand-audit-token-foundation/102-PATTERNS.md

<interfaces>
<!-- Full token inventory from RESEARCH.md Finding 3. These values are LOCKED. -->
<!-- 17 PRIMITIVES (JSON dot-path primitive.family.stop): -->
current.950 #09141A · current.900 #0F1E26 · current.800 #162B35 · harbor.700 #254855
wake.700 #2B756A · wake.500 #4E9A8E · kelp.800 #123B36 · brass.500 #C98A2E · brass.700 #946017
foam.50 #F7F1E6 · foam.100 #EFE6D6 · mist.200 #C9D4CF · stone.500 #7C746A · stone.600 #756D63 (NEW, D-02)
rust.600 #9A4D35 · plum.700 #372D4C · white #FFFFFF

<!-- 27 SEMANTIC tokens (light value / dark value), aliased to primitives via {primitive.x.y}: -->
surface.default foam.50/current.950 · surface.raised foam.100/current.900 · surface.inset white/current.800 · surface.inverse current.950/foam.50
text.default current.950/foam.50 · text.muted stone.600/mist.200 · text.subtle stone.500/mist.200 · text.inverse foam.50/current.950 · text.code foam.50/foam.50
action.bg wake.700/brass.500 · action.fg white/current.950 · action.bg-dark brass.500/wake.700 · action.fg-dark current.950/white · action.hover current.950/foam.100 · action.focus-ring brass.500/wake.500
border.default mist.200/harbor.700 · border.subtle foam.100/current.800 · border.strong wake.700/mist.200
status.success kelp.800/wake.500 · status.warning brass.700/brass.500 · status.error rust.600/rust.600 · status.info harbor.700/mist.200
runtime.liveview harbor.700/mist.200 · runtime.offline kelp.800/wake.500 · runtime.native brass.500/brass.500 · runtime.sensitive rust.600/rust.600 · runtime.bridge plum.700/foam.50

<!-- CRITICAL dark-mode rule (Pitfall 3): text.muted dark = mist.200 (12.25:1), NOT stone.600 (3.66:1 fails normal text) -->

<!-- ~18 NON-COLOR tokens: font.{display,body,mono}; text.{xs,sm,md,lg,xl} + display.{sm,md,lg} sizes; -->
<!-- spacing.base 4px; radius.{sm 6,md 10,lg 14,xl 20}; focus.ring-width 2px; text.tracking-tight -0.02em -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author crosswake.tokens.json (DTCG 2025.10 source of truth)</name>
  <files>brandbook/tokens/crosswake.tokens.json</files>
  <read_first>
    - .planning/phases/102-brand-audit-token-foundation/102-RESEARCH.md (Finding 3 — full token tables for all 17 primitives, 27 semantic, ~18 non-color; the DTCG JSON structure pattern; D-03 $description restriction strings)
    - .planning/phases/102-brand-audit-token-foundation/102-PATTERNS.md (DTCG JSON structure block + $value/$dark/$description conventions + the existing 16-var app.css ground truth)
    - examples/phoenix_host/assets/css/app.css (the 16 existing --cw-* names — primitive names must stay compatible)
  </read_first>
  <action>
    Create brandbook/tokens/crosswake.tokens.json in DTCG 2025.10 format. Group-level `$type: "color"` on each color group (do not repeat $type per leaf). Top-level color groups: `primitive`, `surface`, `text`, `action`, `border`, `status`, `runtime`. Primitive leaves are `{family}.{stop}` (e.g. primitive.current.950) carrying `$value` hex + `$description`. Include all 17 primitives from the interfaces block — Stone 600 #756D63 is the math-forced addition per D-02 with $description "text.muted mapping. Passes AA on Foam 50 (4.53:1) and white (5.09:1).". Encode usage restrictions in $description per D-03: Stone 500 $description must state "Narrow use only: large text >=24px, disabled, decorative. Fails AA normal text on Foam 50 (4.09:1)."; Wake 500 $description "light-surface text forbidden: 2.95:1 on Foam 50"; Mist 200 $description "border and dark-surface text only: 1.35:1 on Foam 50". Semantic tokens reference primitives via `{primitive.family.stop}` alias syntax in `$value` (light) and a sibling `$dark` property (dark value) — both as aliases, never inline hex. Map all 27 semantic tokens exactly per the interfaces block. CRITICAL (Pitfall 3): text.muted `$dark` MUST be `{primitive.mist.200}` (12.25:1), NOT stone.600 (which is 3.66:1 and fails normal text on dark). Add non-color groups: `font` ($type fontFamily: display/body/mono stacks from RESEARCH.md Finding 3); type-scale dimension tokens (text.xs..text.xl, display.sm..display.lg with size + line-height); spacing.base 4px; radius.sm/md/lg/xl (6/10/14/20px); focus.ring-width 2px; tracking -0.02em — all `$type: "dimension"` (or fontFamily). Keep semantic color count at 27 (D-06 hard cap 30 — do not add tokens beyond the inventory).
  </action>
  <verify>
    <automated>node -e "const t=JSON.parse(require('fs').readFileSync('brandbook/tokens/crosswake.tokens.json','utf8')); const g=['primitive','surface','text','action','border','status','runtime']; if(!g.every(k=>t[k])) throw new Error('missing group'); if(t.primitive.stone['600'].\$value!=='#756D63') throw new Error('stone600'); const r=Object.keys(t.runtime).filter(k=>!k.startsWith('\$')); if(!['liveview','offline','native','sensitive','bridge'].every(x=>r.includes(x))) throw new Error('runtime'); console.log('OK',r.join(','));"</automated>
  </verify>
  <acceptance_criteria>
    - JSON parses without throwing
    - Top-level groups present: primitive, surface, text, action, border, status, runtime
    - `primitive.stone.600.$value` === `#756D63`
    - `runtime` group contains exactly liveview, offline, native, sensitive, bridge
    - `text.muted.$dark` resolves to `{primitive.mist.200}` (NOT stone.600) — verifiable by grep `"$dark": "{primitive.mist.200}"` near a text.muted block
    - Stone 500 $description contains "Fails AA normal text on Foam 50 (4.09:1)"
    - Count of semantic color leaves across surface/text/action/border/status/runtime == 27
  </acceptance_criteria>
  <done>crosswake.tokens.json is valid DTCG with 17 primitives (incl. Stone 600), 27 aliased semantic tokens (text.muted dark = mist.200), and ~18 non-color tokens; restrictions encoded in $description.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Write compile-tokens.js and generate committed tokens.css</name>
  <files>brandbook/tools/compile-tokens.js, brandbook/tokens/tokens.css</files>
  <behavior>
    - flattenTokens turns the nested JSON into a dot-path -> token map (skips $-prefixed keys)
    - resolveAlias converts "{primitive.foam.50}" -> "var(--cw-primitive-foam-50)"
    - A primitive leaf emits `--cw-primitive-{family}-{stop}: {hex};`
    - A semantic token emits `--cw-{group}-{name}: var(--cw-primitive-...);` (never inline hex)
    - The script emits a :root primitive block, a :root semantic block, an @media(prefers-color-scheme:dark) :root:not([data-theme]) block, and a [data-theme="dark"] block
    - Re-running the script twice produces byte-identical tokens.css (deterministic)
  </behavior>
  <read_first>
    - .planning/phases/102-brand-audit-token-foundation/102-PATTERNS.md (compile-tokens.js block: ROOT/JSON_PATH/CSS_PATH constants, flattenTokens, resolveAlias, output pattern; the generated tokens.css structure block)
    - .planning/phases/102-brand-audit-token-foundation/102-RESEARCH.md (Finding 4 — alias resolution algorithm, generated CSS structure incl. forbidden-pairings comment block)
    - brandbook/tokens/crosswake.tokens.json (the input this script consumes — read after Task 1 completes)
  </read_first>
  <action>
    Create brandbook/tools/compile-tokens.js as a zero-dependency Node CJS script (<80 LOC per D-07; `require('fs')`/`require('path')` only). Read brandbook/tokens/crosswake.tokens.json, write brandbook/tokens/tokens.css. Implement flattenTokens (recursive, skips $-keys, collects leaves with $value), and resolveAlias (replace `{path.to.token}` with `var(--cw-` + path.replace(/\./g,'-') + `)`). Emit in order: (1) the two-line header `/* GENERATED from crosswake.tokens.json — do not edit */` and `/* Edit crosswake.tokens.json, then run: node brandbook/tools/compile-tokens.js */`; (2) `:root` primitive block with section comment `/* ─── Primitive tier (internal — do not reference directly in component CSS) ─── */` emitting `--cw-primitive-{family}-{stop}: {hex};` for all 17 primitives (inline hex is correct for the PRIMITIVE tier only); (3) `:root` semantic block with comment `/* ─── Semantic tier (public contract) ─── */` emitting each semantic token resolving its `$value` alias to `var(--cw-primitive-*)`; (4) `@media (prefers-color-scheme: dark) { :root:not([data-theme]) { ... } }` block emitting each semantic token's resolved `$dark` value; (5) `[data-theme="dark"]` block identical to the dark @media block (D-08); (6) a forbidden-pairings comment block listing stone-500/foam-50 4.09:1, wake-500/foam-50 2.95:1, mist-200/foam-50 1.35:1 as DO-NOT-USE. Then RUN the script (`node brandbook/tools/compile-tokens.js`) to produce the committed tokens.css. Anti-pattern to avoid (PATTERNS.md): semantic tokens MUST use var(--cw-primitive-*), never inline hex.
  </action>
  <verify>
    <automated>node brandbook/tools/compile-tokens.js && head -1 brandbook/tokens/tokens.css | grep -q 'GENERATED' && test "$(grep -cE 'var\(--cw-primitive-' brandbook/tokens/tokens.css)" -ge 20 && grep -q 'prefers-color-scheme: dark' brandbook/tokens/tokens.css && grep -q '\[data-theme="dark"\]' brandbook/tokens/tokens.css && cp brandbook/tokens/tokens.css /tmp/css1 && node brandbook/tools/compile-tokens.js && diff -q /tmp/css1 brandbook/tokens/tokens.css && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `node brandbook/tools/compile-tokens.js` exits 0 and writes brandbook/tokens/tokens.css
    - First line of tokens.css contains `GENERATED from crosswake.tokens.json`
    - >= 20 occurrences of `var(--cw-primitive-` in tokens.css (semantic tier resolves to primitive vars)
    - No semantic-tier line inlines a `#RRGGBB` hex (only the primitive block carries hex)
    - tokens.css contains `prefers-color-scheme: dark` and `[data-theme="dark"]` blocks
    - Re-running the script produces a byte-identical tokens.css (deterministic / committed-output safe)
    - compile-tokens.js source < 80 lines (excluding blank/comment-only padding) and requires no npm packages
  </acceptance_criteria>
  <done>compile-tokens.js (zero-dep, deterministic) generates tokens.css with primitive+semantic tiers, light/dark blocks, and forbidden-pairings comments; semantic tokens reference primitive vars only.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| crosswake.tokens.json → compile-tokens.js | JSON parsed and walked; malformed/unexpected structure crosses here |
| compile-tokens.js → committed tokens.css | Generated output committed to repo; must be reproducible and free of injected content |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-102-04 | Tampering | compile-tokens.js JSON read | mitigate | JSON.parse throws on malformed input; script fails loudly (process.exit(1)) rather than emitting partial CSS |
| T-102-05 | Tampering | hand-edited tokens.css drift | mitigate | GENERATED header warns; deterministic re-generation lets reviewers diff committed vs regenerated output |
| T-102-06 | Information disclosure | tokens.json/tokens.css contents | accept | Color hex + dimensions only; no secrets, no PII |
| T-102-SC | Tampering | npm/pip/cargo installs | mitigate | Zero npm dependencies — compile-tokens.js uses only Node stdlib (fs/path); nothing to audit |
</threat_model>

<verification>
- JSON parses; has all 7 color groups + 5 runtime tokens + Stone 600
- compile-tokens.js regenerates tokens.css deterministically (diff clean on re-run)
- tokens.css: semantic tier uses var(--cw-primitive-*) only; light/dark blocks present
- All 12 states representable (hover/focus tokens + status.* + text.muted/subtle)
</verification>

<success_criteria>
- TOKN-01: crosswake.tokens.json is valid DTCG 2025.10 with primitive -> semantic tiers incl. all 5 runtime tokens
- TOKN-02: tokens.css generated from JSON, primitive-var referenced, contrast-annotated (forbidden-pairings block)
- TOKN-03: all 12 states covered via tokens + documented patterns
</success_criteria>

<output>
Create `.planning/phases/102-brand-audit-token-foundation/102-02-SUMMARY.md` when done
</output>
