---
phase: 140-family-discipline-close
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/crosswake_sigra/test/crosswake/companions/sigra/telemetry_test.exs
  - packages/crosswake_chimeway/test/crosswake/companions/chimeway/telemetry_test.exs
  - packages/crosswake_threadline/test/crosswake/threadline/telemetry_test.exs
  - test/crosswake/proof/phase133_telemetry_contract_test.exs
autonomous: true
requirements: [FAMILY-03]
must_haves:
  truths:
    - "crosswake_sigra carries a catalog-driven Side-A test that drives a real emission path and asserts declared measurement/metadata keys are present via Map.has_key? (subset, not exact-count)."
    - "crosswake_chimeway carries the same catalog-driven Side-A test in its own suite."
    - "crosswake_threadline has a catalog-iterating Side-A test over Telemetry.event_names/0 asserting :thread_id metadata, plus one plug-behavior test that drives Crosswake.Plug.Threadline through an error path and observes the :exception event fire."
    - "A merge-blocking regression guard asserts the `>= 24` count-assertion stays absent from phase133_telemetry_contract_test.exs."
  artifacts:
    - packages/crosswake_sigra/test/crosswake/companions/sigra/telemetry_test.exs
    - packages/crosswake_chimeway/test/crosswake/companions/chimeway/telemetry_test.exs
    - packages/crosswake_threadline/test/crosswake/threadline/telemetry_test.exs
    - test/crosswake/proof/phase133_telemetry_contract_test.exs
  key_links:
    - "Each Side-A test pulls declared names from that package's own Telemetry.event_names/0 — the catalog is the single source, so declaring a new event without emitting it fails the test."
  prohibitions:
    - "MUST NOT assert an exact event/key COUNT in any Side-A test — exact-count re-creates the >=24 cross-package coupling. Subset (Map.has_key?) only."
    - "MUST NOT add a core-side aggregation test that reaches into companion emission — emission proof lives in the emitter's own suite."
    - "MUST NOT rebuild the already-removed `>= 24` assertion or the Phase-136 shape assertion — only guard them against regression."
---

<objective>
Give each companion package its own Side-A "declared ⇔ emitted" telemetry contract test and lock the
already-done `>= 24` removal against regression. Implements FAMILY-03 per D-15, D-16, D-17, D-18.

The `>= 24` count-assertion was already replaced by the Phase-136 shape assertion (live at
phase133_telemetry_contract_test.exs ~L358), and the Phase-139 anti-drift subset invariant is already
live. This plan does NOT rebuild them. It adds the missing per-package emission proofs (sigra and
chimeway have none; threadline's phase92 coverage hardcodes 3 names and never drives `:exception`
live) and one regression guard.

Purpose: Emission proof must live where the emitter lives — central aggregation would re-create the
`>= 24` cross-package coupling that independent versioning forbids. Each package proves its own
catalog is fully emitted; the subset (Map.has_key?) assertion means "declared a new event, forgot to
emit" fails loudly without pinning an exact count.
Output: Three per-package Side-A tests + one `>= 24`-stays-gone regression guard.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/140-family-discipline-close/140-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add catalog-driven Side-A emission tests to sigra and chimeway</name>
  <read_first>
    - packages/crosswake_sigra/lib/crosswake/companions/sigra/telemetry.ex (event_names/0, metadata_keys/0, execute/3, new_event/1)
    - packages/crosswake_sigra/test/crosswake/companions/sigra/telemetry_test.exs (existing membership-style test to extend)
    - packages/crosswake_chimeway/lib/crosswake/companions/chimeway/telemetry.ex (event_names/0, metadata_keys/0, execute/3)
    - packages/crosswake_chimeway/test/crosswake/companions/chimeway/telemetry_test.exs (existing tests, incl. the single hardcoded-name live test to generalize)
  </read_first>
  <files>packages/crosswake_sigra/test/crosswake/companions/sigra/telemetry_test.exs, packages/crosswake_chimeway/test/crosswake/companions/chimeway/telemetry_test.exs</files>
  <action>
    Add one new Side-A "declared ⇔ emitted" test to EACH package's existing telemetry_test.exs (D-16). Do NOT delete the existing tests in either file.

    The shape (identical for both packages, adapted to each catalog):
    1. Iterate every event name returned by that package's `Telemetry.event_names/0` (do not hardcode the list — pull it live from the catalog).
    2. For each event name, attach a handler using `:telemetry_test.attach_event_handlers(self(), [event_name])` (the OTP `:telemetry_test` helper that forwards `{:telemetry_event, name, measurements, metadata, config}` to the test process), OR use `:telemetry.attach/4` with an `on_exit` detach if you prefer the lower-level API — either is acceptable, but use `:telemetry_test.attach_event_handlers` where practical since it auto-detaches.
    3. Drive a real emission path: call `Telemetry.execute(event_name, %{...measurements...}, %{...metadata...})` supplying at least one declared metadata key from `Telemetry.metadata_keys/0` (e.g. sigra `:route_id`/`:correlation_id`; chimeway `:provider`/`:correlation_id`). This is the emitter code path, not a fabricated `:telemetry.execute`.
    4. `assert_received` / `assert_receive` the forwarded event, then assert the supplied declared metadata keys are PRESENT via `Map.has_key?(metadata, key)`. Assert SUBSET only — do NOT assert `map_size` equals a number and do NOT assert the full metadata_keys list is present (some are optional per event). This is the hard rule: exact-count re-introduces the `>= 24` coupling.
    5. Because sigra's events are `:reserved` in core (declared-not-emitted-by-core), a comment must note that this in-package Side-A test is what proves emission exists — the two facts (core declares reserved + package emits) together are the full contract.

    Keep both tests `async: true` (they attach uniquely-named or auto-managed handlers and mutate no Application state). Use a unique handler id per event when using `:telemetry.attach/4` (e.g. suffix with `System.unique_integer()`), and detach in `on_exit`.
  </action>
  <verify>
    <automated>cd packages/crosswake_sigra && mix test test/crosswake/companions/sigra/telemetry_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - `cd packages/crosswake_sigra && mix test test/crosswake/companions/sigra/telemetry_test.exs` passes.
    - `cd packages/crosswake_chimeway && mix test test/crosswake/companions/chimeway/telemetry_test.exs` passes.
    - Each new test iterates over `Telemetry.event_names/0` (not a hardcoded literal list): `grep -n 'event_names()' packages/crosswake_sigra/test/crosswake/companions/sigra/telemetry_test.exs` shows the catalog is iterated in the new test.
    - Each new test asserts key presence via `Map.has_key?` and contains NO `map_size(... ) ==` count assertion in the new test body.
    - The pre-existing tests in both files still pass (nothing deleted).
  </acceptance_criteria>
  <done>sigra and chimeway each carry a catalog-driven Side-A emission test that drives Telemetry.execute over every declared event and asserts declared metadata keys present via subset Map.has_key?, with no exact-count assertion.</done>
</task>

<task type="auto">
  <name>Task 2: Upgrade threadline Side-A — catalog iteration + drive the :exception event live</name>
  <read_first>
    - packages/crosswake_threadline/lib/crosswake/threadline/telemetry.ex (event_names/0, execute/3, metadata_keys/0 — note :thread_id is the always-present key)
    - packages/crosswake_threadline/test/crosswake/threadline/telemetry_test.exs (existing exact-list contract tests to extend)
    - packages/crosswake_threadline/lib/crosswake/plug/threadline.ex (call/2 — the rescue clause emits prefix ++ [:exception] then reraises; guarded by `if Code.ensure_loaded?(Plug.Conn)`)
    - packages/crosswake_threadline/test/crosswake/proof/phase92_server_propagation_closeout_test.exs (existing plug-driving pattern using Plug.Test conn/2 and :telemetry.attach — the model for the new exception test; note it hardcodes 3 names and never drives :exception)
  </read_first>
  <files>packages/crosswake_threadline/test/crosswake/threadline/telemetry_test.exs</files>
  <action>
    Add a ~20-line Side-A upgrade to packages/crosswake_threadline/test/crosswake/threadline/telemetry_test.exs (D-17). Do NOT delete the existing exact-list contract tests (those are legitimate published-contract equality checks for the observer's stable 3-event list — they stay).

    Part (a) — catalog-iterating Side-A test (~10 lines): iterate `for event_name <- Telemetry.event_names() do ... end`. For each name: `:telemetry.attach` a handler (unique id + `on_exit` detach), call `Telemetry.execute(event_name, %{}, %{thread_id: "tid-catalog"})`, `assert_receive` the event, and assert `Map.has_key?(metadata, :thread_id)`. Subset assertion only — this catches "declared a 4th event, forgot to emit it". Do NOT assert an exact metadata count.

    Part (b) — ONE plug-behavior test that drives the currently-unproven `:exception` event. Mirror the phase92 pattern: `import Plug.Test`, attach a handler to `[:crosswake, :threadline, :request, :exception]`, then call `Crosswake.Plug.Threadline` through an error path so its `rescue` clause fires. The rescue in call/2 triggers when the try-body raises; the cleanest driver is to pass `opts` that make `Conn.get_req_header/2` or a downstream call raise — e.g. invoke `Crosswake.Plug.Threadline.call/2` with a `conn` value or `opts` shape that forces a raise inside the try block (the executor determines the minimal trigger by reading call/2; a `conn` that is not a valid `Plug.Conn` struct where `Conn.get_req_header` is invoked will raise and be rescued). Assert the `:exception` telemetry is received AND that the reraise propagates (wrap the call in `assert_raise`). Because the module is guarded by `if Code.ensure_loaded?(Plug.Conn)`, this test runs in the threadline package where `:plug` is available in the test env — confirm `:plug` is a test-available dep before relying on `Crosswake.Plug.Threadline` compiling; if it is not loadable in this suite, place the plug-behavior test where phase92 already drives the plug (that proof already imports Plug.Test), and keep only Part (a) in telemetry_test.exs. Prefer keeping both in telemetry_test.exs if `:plug` is available there.

    Keep the test `async: true`; detach every handler in `on_exit`.
  </action>
  <verify>
    <automated>cd packages/crosswake_threadline && mix test test/crosswake/threadline/telemetry_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - `cd packages/crosswake_threadline && mix test test/crosswake/threadline/telemetry_test.exs` passes.
    - A new test iterates `Telemetry.event_names()` and asserts `Map.has_key?(metadata, :thread_id)` for each emitted event (subset, no exact metadata-count assertion).
    - A test drives `Crosswake.Plug.Threadline` through an error path, observes the `[:crosswake, :threadline, :request, :exception]` event, and asserts the raise propagates (the previously-declared-but-never-driven `:exception` event is now proven emitted). If `:plug` is unavailable in the telemetry_test suite, this assertion instead lives in the phase92 proof file and that file's test count increased — either placement is acceptable so long as `:exception` is driven live somewhere in the threadline package suite.
    - The pre-existing exact-list contract tests in telemetry_test.exs still pass.
  </acceptance_criteria>
  <done>threadline has a catalog-iterating Side-A test over event_names/0 asserting :thread_id, and the :exception event is driven live through Crosswake.Plug.Threadline's error path with the reraise asserted.</done>
</task>

<task type="auto">
  <name>Task 3: Lock the `>= 24` removal against regression in the core telemetry contract test</name>
  <read_first>
    - test/crosswake/proof/phase133_telemetry_contract_test.exs (the file — confirm the Phase-136 shape assertion at ~L358 and that `>= 24` is already absent; this guard prevents its return)
  </read_first>
  <files>test/crosswake/proof/phase133_telemetry_contract_test.exs</files>
  <action>
    Add ONE self-referential regression guard test to test/crosswake/proof/phase133_telemetry_contract_test.exs that locks the already-done `>= 24` removal (D-15). Do NOT rebuild or re-add the count assertion — this test asserts its ABSENCE.

    The test reads this file's own source (`File.read!(__ENV__.file)` or the explicit repo-relative path `test/crosswake/proof/phase133_telemetry_contract_test.exs`) and asserts the count of lines containing the count-assertion literal is zero. To avoid the comment-text self-invalidation trap, count only NON-comment occurrences of the reserved-event count-assertion idiom: filter out lines beginning with `#` (after trimming) before counting, and match the operator-plus-count idiom applied to a reserved-event length (e.g. a `length(` … `) >= 24` shape), NOT the bare two-character string. Assert the filtered count is 0 with a `ProofAssertions.stable_id_message/7`-style merge-blocking message (stable id e.g. `proof.telem_04.no_reserved_count_assertion`) explaining that the count assertion was replaced by the Phase-136 shape assertion and must stay gone.

    Also add a short comment near the existing shape-assertion test (the `TELEM-04 :reserved tier events are excluded` test) noting the Phase-139 anti-drift subset invariant lives in `test/crosswake/telemetry_test.exs` and is intentionally NOT duplicated here (D-18 — keep the existing one, do not rebuild).
  </action>
  <verify>
    <automated>mix test test/crosswake/proof/phase133_telemetry_contract_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - `mix test test/crosswake/proof/phase133_telemetry_contract_test.exs` passes.
    - A test with a stable id like `proof.telem_04.no_reserved_count_assertion` asserts the non-comment reserved-event count-assertion idiom count in the file is 0.
    - The guard does not itself reintroduce a live count assertion on `Crosswake.Telemetry` output (it only inspects source text).
    - The pre-existing Phase-136 shape-assertion test and fail-closed test still pass.
  </acceptance_criteria>
  <done>A merge-blocking regression guard asserts the reserved-event `>= 24` count assertion stays absent from phase133_telemetry_contract_test.exs, without rebuilding it, and references the Phase-139 anti-drift invariant's home.</done>
</task>

</tasks>

<artifacts>
## Artifacts this phase produces

- `packages/crosswake_sigra/test/crosswake/companions/sigra/telemetry_test.exs` — new catalog-driven Side-A emission test.
- `packages/crosswake_chimeway/test/crosswake/companions/chimeway/telemetry_test.exs` — new catalog-driven Side-A emission test.
- `packages/crosswake_threadline/test/crosswake/threadline/telemetry_test.exs` — new catalog-iterating Side-A test + live `:exception`-driving plug-behavior test.
- `test/crosswake/proof/phase133_telemetry_contract_test.exs` — new regression guard `proof.telem_04.no_reserved_count_assertion`.
</artifacts>

<verification>
- All three package telemetry suites are green; the core phase133 suite is green.
- No Side-A test contains an exact event/key count assertion (subset Map.has_key? only).
- `:exception` for threadline is driven live at least once in the threadline package.
</verification>

<success_criteria>
FAMILY-03 satisfied: each companion proves its own declared⇔emitted telemetry contract in its own
suite via subset assertions, and the `>= 24` removal plus anti-drift subset invariant are guarded
against regression without being rebuilt.
</success_criteria>

<output>
Create `.planning/phases/140-family-discipline-close/140-02-SUMMARY.md` when done.
</output>
