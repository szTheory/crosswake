---
phase: 140-family-discipline-close
plan: 05
type: execute
wave: 2
depends_on: [140-04]
files_modified: []
autonomous: false
requirements: [FAMILY-04]
must_haves:
  truths:
    - "The three companions are published to Hex sequentially (sigra → chimeway → threadline), one release-please component per PR, with each companion's publish + clean-room proof green and hexdocs resolving before the next begins."
    - "The carried register_required_checks.sh ship-gate is run green-first (DRY_RUN=1 then DRY_RUN=0) so new v17.0 merge-blocking lanes are registered before being relied upon."
    - "No companion publish-hex-*/clean-room-proof-* job is registered as a required check."
  artifacts: []
  key_links:
    - "Each publish is gated behind a blocking human checkpoint — hex.publish is irreversible after the ~60-minute grace window; the human is the go/no-go authority."
  prohibitions:
    - "MUST NOT run inside Phase-140 autonomous execution — this plan is autonomous: false and fires only on a separate human trigger AFTER origin-sync + Phase 137/138/139 execution-verification."
    - "MUST NOT batch Release PRs — exactly one companion Release PR is merged at a time, verified green before the next."
    - "MUST NOT register publish-hex-*/clean-room-proof-* jobs as required checks (permanent deadlock)."
---

<objective>
Execute the batched family publish and the carried ship-gate registration — the irreversible half of
FAMILY-04. Implements D-01 (execution), D-03 (per-companion loop), D-04 (ship-gate ordering).

This plan is `autonomous: false` BY DESIGN (D-01, USER-LOCKED). It does NOT run during normal
Phase-140 autonomous execution. It is the documented, human-gated execution wave that the user fires
SEPARATELY, only after: (1) origin-sync has landed local main → origin with CI green
(docs/MILESTONE-BOUNDARY-HYGIENE.md), and (2) Phases 137/138/139 are execution-verified. Until then,
"readiness delivered (plan 04), execution deferred by design" is the intended complete state for
FAMILY-04 in Phase 140.

Every publish step is a blocking human checkpoint because `hex.publish` is irreversible after the
~60-minute grace window. The runbook authored in plan 04 (`docs/COMPANION-PUBLISH-RUNBOOK.md`) is the
operating procedure; this plan is its checkpoint-gated execution skeleton.

Purpose: publish sigra → chimeway → threadline safely and register the carried merge-blocking lanes,
with the human as go/no-go authority at each irreversible step.
Output: three companions live on hex.pm at 0.1.0 with docs resolving; new v17.0 merge-blocking lanes
registered as required checks.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/140-family-discipline-close/140-CONTEXT.md
@docs/COMPANION-PUBLISH-RUNBOOK.md
@docs/MILESTONE-BOUNDARY-HYGIENE.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Preconditions gate for the batched family publish</name>
  <action>Blocking human checkpoint. Do NOT proceed to any publish until the human confirms the D-01 preconditions below are met. This is a gate, not automated work.</action>
  <what-built>
    Preconditions gate for the batched family publish. The publish runbook (docs/COMPANION-PUBLISH-RUNBOOK.md)
    is authored (plan 04) and all pipeline primitives are verified present.
  </what-built>
  <how-to-verify>
    Confirm ALL of the following are true before any publish (per D-01 preconditions):
    1. Local main has been synced to origin and CI is green on main (see docs/MILESTONE-BOUNDARY-HYGIENE.md).
    2. Phases 137, 138, and 139 are execution-verified (their extraction work is green in-tree / on main).
    3. You have read docs/COMPANION-PUBLISH-RUNBOOK.md and accept the sequential, one-PR-at-a-time discipline.
    4. You hold repo-admin gh auth (needed later for register_required_checks.sh DRY_RUN=0).
    If any is not yet true, STOP — this execution plan is deferred until they are.
  </how-to-verify>
  <resume-signal>Type "preconditions met" to begin the sequential publish, or "defer" to stop.</resume-signal>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Publish crosswake_sigra (irreversible)</name>
  <action>Blocking human action — hex.publish is irreversible. The human executes the sigra publish loop from docs/COMPANION-PUBLISH-RUNBOOK.md exactly as detailed in how-to-verify below; Claude performs no hex publish.</action>
  <what-built>
    The crosswake_sigra release-please component is registered and its publish-hex-sigra +
    clean-room-proof-sigra jobs are wired. Merging the sigra Release PR triggers the irreversible
    hex.publish.
  </what-built>
  <how-to-verify>
    Follow docs/COMPANION-PUBLISH-RUNBOOK.md for sigra ONLY:
    1. Merge exactly ONE Release PR: the `crosswake_sigra` release-please PR (do not touch chimeway/threadline PRs).
    2. Wait for `publish-hex-sigra` AND `clean-room-proof-sigra` to go GREEN on main.
    3. Confirm https://hexdocs.pm/crosswake_sigra/0.1.0 resolves.
    4. Merge the auto-opened `release-as-cleanup` PR for crosswake_sigra (clears the `merge-blocking-release-as-staleness` RED).
    If `release-failure-alert` opens a tracking issue, STOP and investigate before proceeding.
    Revert window: a mistaken publish is corrected with `mix hex.retire crosswake_sigra 0.1.0` within
    the ~60-minute grace window — NOT by re-pushing. Do not retry a failed publish without checking it.
  </how-to-verify>
  <resume-signal>Type "sigra live" once publish-hex-sigra + clean-room-proof-sigra are green, hexdocs resolves, and the sigra cleanup PR is merged.</resume-signal>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Publish crosswake_chimeway (irreversible)</name>
  <action>Blocking human action — hex.publish is irreversible. Proceed ONLY after sigra is fully live. The human executes the chimeway publish loop from docs/COMPANION-PUBLISH-RUNBOOK.md as detailed in how-to-verify; Claude performs no hex publish.</action>
  <what-built>
    crosswake_chimeway component registered; publish-hex-chimeway + clean-room-proof-chimeway wired.
    Begins only after sigra is fully live.
  </what-built>
  <how-to-verify>
    Follow docs/COMPANION-PUBLISH-RUNBOOK.md for chimeway ONLY (sigra must already be live):
    1. Merge exactly ONE Release PR: the `crosswake_chimeway` release-please PR.
    2. Wait for `publish-hex-chimeway` AND `clean-room-proof-chimeway` to go GREEN on main.
    3. Confirm https://hexdocs.pm/crosswake_chimeway/0.1.0 resolves.
    4. Merge the auto-opened `release-as-cleanup` PR for crosswake_chimeway.
    If `release-failure-alert` fires, STOP. Revert window: `mix hex.retire crosswake_chimeway 0.1.0`.
  </how-to-verify>
  <resume-signal>Type "chimeway live" once publish-hex-chimeway + clean-room-proof-chimeway are green, hexdocs resolves, and the chimeway cleanup PR is merged.</resume-signal>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Publish crosswake_threadline (irreversible, observer last)</name>
  <action>Blocking human action — hex.publish is irreversible. Proceed ONLY after sigra AND chimeway are fully live (observer published last). The human executes the threadline publish loop from docs/COMPANION-PUBLISH-RUNBOOK.md as detailed in how-to-verify; Claude performs no hex publish.</action>
  <what-built>
    crosswake_threadline component registered; publish-hex-threadline + clean-room-proof-threadline
    wired. The observer is published LAST (belt-and-suspenders) — only after sigra AND chimeway are live.
  </what-built>
  <how-to-verify>
    Follow docs/COMPANION-PUBLISH-RUNBOOK.md for threadline ONLY (sigra + chimeway must already be live):
    1. Merge exactly ONE Release PR: the `crosswake_threadline` release-please PR.
    2. Wait for `publish-hex-threadline` AND `clean-room-proof-threadline` to go GREEN on main.
    3. Confirm https://hexdocs.pm/crosswake_threadline/0.1.0 resolves.
    4. Merge the auto-opened `release-as-cleanup` PR for crosswake_threadline.
    If `release-failure-alert` fires, STOP. Revert window: `mix hex.retire crosswake_threadline 0.1.0`.
  </how-to-verify>
  <resume-signal>Type "threadline live" once publish-hex-threadline + clean-room-proof-threadline are green, hexdocs resolves, and the threadline cleanup PR is merged. The family is now fully published.</resume-signal>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Run the carried ship-gate: register new v17.0 merge-blocking lanes (green-first)</name>
  <action>Blocking human action — branch-protection change requires repo-admin gh auth. The human runs register_required_checks.sh green-first (DRY_RUN=1 then DRY_RUN=0) as detailed in how-to-verify; publish-hex-*/clean-room-proof-* must NOT be registered.</action>
  <what-built>
    script/register_required_checks.sh (green-first, idempotent, parametric) is present. This step
    registers any NEW v17.0 `merge-blocking-*` lane as a required check, green-first.
  </what-built>
  <how-to-verify>
    Per D-04 and docs/COMPANION-PUBLISH-RUNBOOK.md, AFTER Phase 140 has landed and any new
    `merge-blocking-*` lane has run GREEN at least once on main:
    1. Run `script/register_required_checks.sh` with DRY_RUN=1 (default) and review the preview.
    2. Run `DRY_RUN=0 script/register_required_checks.sh` (repo-admin gh auth required) to apply.
    3. Confirm the daily `required-checks-audit.yml` reports the expected required-check set.
    HARD RULE: the companion `publish-hex-*` and `clean-room-proof-*` jobs are post-merge jobs that
    skip on normal PRs — they MUST NOT be registered as required checks (requiring them = permanent PR
    deadlock). The script's green-first preflight already skips lanes with no green run on HEAD; do not
    override it.
  </how-to-verify>
  <resume-signal>Type "ship-gate registered" once DRY_RUN=0 registration succeeded and the required-checks audit confirms the new lanes (with publish-hex-*/clean-room-proof-* excluded).</resume-signal>
</task>

</tasks>

<artifacts>
## Artifacts this phase produces

- (No repository files.) This plan's outputs are external/operational side effects:
  - `crosswake_sigra`, `crosswake_chimeway`, `crosswake_threadline` live on hex.pm at 0.1.0 with hexdocs resolving.
  - Three `release-as-cleanup` PRs merged (release-as pins stripped; staleness gate green).
  - New v17.0 `merge-blocking-*` lanes registered as required checks via `register_required_checks.sh` (publish-hex-*/clean-room-proof-* deliberately excluded).
</artifacts>

<verification>
- All three companions resolve at hexdocs.pm/crosswake_<name>/0.1.0.
- Each companion's publish-hex + clean-room-proof went green before the next companion started.
- `register_required_checks.sh` was applied green-first with the required-check exclusions honored.
- This plan ran only under a separate human trigger, never during Phase-140 autonomous execution.
</verification>

<success_criteria>
FAMILY-04 execution satisfied (deferred, human-gated): the family is published sequentially and safely
in the fixed order sigra → chimeway → threadline, and the carried ship-gate registers the new
merge-blocking lanes green-first without ever requiring the post-merge publish/clean-room jobs.
</success_criteria>

<output>
Create `.planning/phases/140-family-discipline-close/140-05-SUMMARY.md` when done.
</output>
