---
phase: 140-family-discipline-close
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - guides/companion_compatibility.md
  - test/crosswake/proof/phase132_compat_matrix_drift_test.exs
autonomous: true
requirements: [FAMILY-01]
must_haves:
  truths:
    - "guides/companion_compatibility.md has exactly one data row per companion with a single `Requires crosswake` column and no inter-companion columns."
    - "The Threadline row's Companion ID and Engine Dependency cells are one-liners; the plug/on_mount wiring explanation lives in prose below the table."
    - "Every Current Version cell reads `unpublished` (backtick-wrapped), and a standalone HTML comment above the table forbids hand-editing that column."
    - "phase132_compat_matrix_drift_test.exs fails the build if any header column beyond col 1 matches `crosswake_\\w+` (O(N) structural guard)."
    - "phase132_compat_matrix_drift_test.exs fails the build if any Current Version cell is neither `unpublished` nor a bare semver literal."
  artifacts:
    - guides/companion_compatibility.md
    - test/crosswake/proof/phase132_compat_matrix_drift_test.exs
  key_links:
    - "The drift test's header-parse must key on the pinned HTML-comment column contract so column reorders do not silently pass."
  prohibitions:
    - "MUST NOT assert a specific published version number anywhere (hex.pm stays the version authority)."
    - "MUST NOT add any new column to the matrix, and MUST NOT add a Companion ID drift test."
    - "MUST NOT automate pipeline write-back of the Current Version column."
---

<objective>
Discipline the adopter-facing compat matrix (`guides/companion_compatibility.md`) and lock its
O(N)-shape and version-cell honesty with two new drift-test assertions. This implements FAMILY-01
per locked decisions D-05, D-06, D-07, D-08, D-09.

The matrix already has all five rows plus a bidirectional drift test (Phase 132). This plan does
NOT rebuild it — it (1) cleans up prose-in-cell so each cell is data not a paragraph, (2) sets
every Current Version cell to an honest pre-publish `unpublished` literal, and (3) adds the two
machine-checks that make the "no inter-companion columns" invariant and the version-cell format
real.

Purpose: The matrix answers one question — "which core version + engine do I need?" A 300-char
table cell and a stale `0.1.0` both erode that. The O(N) guard is the one check that keeps a future
contributor from adding a "Requires crosswake_sigra" column and passing CI.
Output: Disciplined matrix doc + two new merge-blocking drift-test assertions.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/140-family-discipline-close/140-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Discipline the compat-matrix cells and set honest pre-publish version cells</name>
  <read_first>
    - guides/companion_compatibility.md (the full doc — the table is at lines ~18-29, prose sections follow)
  </read_first>
  <files>guides/companion_compatibility.md</files>
  <action>
    Edit guides/companion_compatibility.md to implement D-05 and D-06. Do NOT add or remove columns; the header stays exactly `Hex Package | Companion ID | Current Version | Requires \`crosswake\` | Engine Dependency | hexdocs` (D-09).

    D-05 (Threadline row cell discipline): the current Threadline data row stuffs a ~300-char wiring paragraph into the Companion ID cell. Replace that cell's content with the one-liner `N/A (observer — not a :companions registrant)`. The Threadline Engine Dependency cell must read the one-liner `none (optional :plug + :phoenix_live_view for surface modules)`. Then add a new prose subsection BELOW the table (a new `## Threadline wiring` section placed after the existing intro prose that follows the table) that carries the wiring explanation the cell used to hold: that Threadline is wired via the `Crosswake.Plug.Threadline` plug and `on_mount: Crosswake.Live.Threadline`, not through the `:companions` registry. Keep the four other rows' Companion ID cells as their existing short backticked atoms (`:rulestead`, `:rindle`, `:sigra`, `:chimeway`).

    D-06 (honest pre-publish version cells): set the `Current Version` cell for ALL FIVE rows to the literal backtick-wrapped token `\`unpublished\``. Do NOT write a semver number in any Current Version cell in this phase — the human writes real numbers back post-publish. Add a standalone HTML comment on its own line immediately ABOVE the table (not inside any cell) stating that the Current Version column is not to be hand-edited and is updated post-publish from the confirmed hex.pm release page. The existing `<!-- compat-03 contract: ... -->` column-contract comment above the table must be preserved (the drift test keys on it); add the new Current Version comment as a separate line alongside it.

    Also update the existing prose sentence that currently says the Current Version column "records the last published line" so it is consistent with the `unpublished` state (it should say the column reads `unpublished` until each package's first Hex release, after which the human writes the confirmed number back).

    Do NOT touch the `Requires crosswake` cells (each stays `\`~> 0.1\``) — those are already bidirectionally drift-tested and must keep matching each package's mix.exs.
  </action>
  <verify>
    <automated>grep -c 'unpublished' guides/companion_compatibility.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '`unpublished`' guides/companion_compatibility.md` returns 5 (one honest version cell per companion row).
    - The Threadline data row line contains `N/A (observer` and does NOT contain `Crosswake.Plug.Threadline` inside a table cell: `grep -n 'Crosswake.Plug.Threadline' guides/companion_compatibility.md` shows it only on a non-table (non-leading-pipe) prose line.
    - A `## Threadline wiring` (or equivalently-named) prose section exists below the table naming `Crosswake.Plug.Threadline` and `on_mount: Crosswake.Live.Threadline`.
    - An HTML comment above the table references the Current Version column and forbids hand-editing; the pre-existing `compat-03 contract` HTML comment is still present.
    - The table header line still contains exactly the six original column names and no `crosswake_` token appears in the header row.
    - `mix test test/crosswake/proof/phase132_compat_matrix_drift_test.exs` passes (existing forward/reverse parity + non-vacuity assertions still green with the disciplined cells).
  </acceptance_criteria>
  <done>The matrix has one-liner Threadline cells, wiring prose below the table, five `unpublished` version cells, an anti-hand-edit HTML comment above the table, and the existing Phase 132 drift test still passes.</done>
</task>

<task type="auto">
  <name>Task 2: Add O(N) column guard and version-cell format guard to the drift test</name>
  <read_first>
    - test/crosswake/proof/phase132_compat_matrix_drift_test.exs (the full drift test — note the existing `row_cells/1`, `requires_crosswake_column_index/1`, `@row_regex`, and header-detection helpers)
    - guides/companion_compatibility.md (target doc, after Task 1 discipline)
  </read_first>
  <files>test/crosswake/proof/phase132_compat_matrix_drift_test.exs</files>
  <action>
    Extend test/crosswake/proof/phase132_compat_matrix_drift_test.exs in place with two new merge-blocking assertions. Reuse the existing `row_cells/1` cell-splitter and the existing header-row detection idiom (the `String.match?(line, ~r/^\\|.*Hex Package.*\\|/)` pattern already used by `requires_crosswake_column_index/1`). Follow the existing `ProofAssertions.stable_id_message/7` message shape and `:merge_blocking` severity used by every other assertion in the file.

    D-07 (O(N) structural guard): add a test that parses the header row, drops column index 0 (the `Hex Package` column, which legitimately names `crosswake_*` packages in its DATA rows but the HEADER cell is `Hex Package`), and `refute`s that any HEADER cell beyond col 1 matches `~r/crosswake_\\w+/`. Use a stable id such as `proof.compat_03.no_inter_companion_columns`. This is the machine-check that enforces "no inter-companion columns" — a future `Requires crosswake_sigra` header column must fail this.

    D-08 (version-cell FORMAT guard): add a test that locates the `Current Version` column index (find the header cell containing the literal `Current Version`, using the same header-detection idiom), then for every companion data row asserts the trimmed Current Version cell matches EITHER `\`unpublished\`` OR a backticked bare semver `~r/^`\\d+\\.\\d+\\.\\d+`$/`. Do NOT assert a specific version — assert the FORMAT only (hex.pm stays authority, per the prohibition). Iterate the same `crosswake_*` data rows the existing `@row_regex`/`doc_package_rows/1` helper already recognizes so the loop is non-vacuous. Use a stable id such as `proof.compat_03.version_cell_format`.

    D-09 (parser TODO for the eventual core-1.0 compound constraint): add a `# TODO` comment near the `extract_hex_req_from_if/1` / requirement-matching helpers noting that when core reaches 1.0 a companion may declare a compound `~> 0.1 or ~> 1.0` requirement, and that this parser must be widened to accept the `or`-constraint form so it does not surface as a cryptic drift failure. This is a comment only — do NOT add an `or`-parsing implementation or a new column now.

    Do NOT weaken, delete, or rename any existing assertion. Do NOT add a `Companion ID` drift test (D-09 forbids it).
  </action>
  <verify>
    <automated>mix test test/crosswake/proof/phase132_compat_matrix_drift_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - `mix test test/crosswake/proof/phase132_compat_matrix_drift_test.exs` passes with the new assertions.
    - The file contains a test with stable id `proof.compat_03.no_inter_companion_columns` that refutes any header column beyond col 1 matching `crosswake_\w+`.
    - The file contains a test with stable id `proof.compat_03.version_cell_format` asserting each Current Version cell is `` `unpublished` `` or a backticked bare semver.
    - `grep -c 'TODO' test/crosswake/proof/phase132_compat_matrix_drift_test.exs` is >= 1 and the TODO mentions the compound `or` core-1.0 constraint.
    - Negative check — the O(N) guard actually fails on a bad header: temporarily appending a `Requires crosswake_sigra` column to a scratch copy of the doc and pointing the test at it fails the `no_inter_companion_columns` assertion (executor may prove this via an inline scratch assertion in the test run, then remove it; the shipped test must NOT depend on a scratch file).
    - No existing Phase 132 assertion was removed or renamed: `grep -c 'proof.compat_03' test/crosswake/proof/phase132_compat_matrix_drift_test.exs` is >= 7 (the original five stable ids plus the two new ones).
  </acceptance_criteria>
  <done>Two new merge-blocking drift-test assertions (O(N) column guard + version-cell format guard) pass, a compound-constraint parser TODO exists, and all prior Phase 132 assertions remain intact.</done>
</task>

</tasks>

<artifacts>
## Artifacts this phase produces

- `guides/companion_compatibility.md` — disciplined: one-liner Threadline cells, `## Threadline wiring` prose section, five `unpublished` Current Version cells, anti-hand-edit HTML comment above the table.
- `test/crosswake/proof/phase132_compat_matrix_drift_test.exs` — two new tests:
  - `proof.compat_03.no_inter_companion_columns` (O(N) header-column guard, D-07)
  - `proof.compat_03.version_cell_format` (Current Version cell format guard, D-08)
  - a `# TODO` comment for the eventual core-1.0 compound `or` requirement (D-09)
</artifacts>

<verification>
- `mix test test/crosswake/proof/phase132_compat_matrix_drift_test.exs` is fully green.
- `grep -c '`unpublished`' guides/companion_compatibility.md` == 5.
- No new columns; no `crosswake_` token in the header row; no specific version literal asserted by the tests.
</verification>

<success_criteria>
FAMILY-01 satisfied: matrix is O(N) and disciplined, version cells are honest pre-publish, and two
machine-checks lock the shape + format without fencing the version value.
</success_criteria>

<output>
Create `.planning/phases/140-family-discipline-close/140-01-SUMMARY.md` when done.
</output>
