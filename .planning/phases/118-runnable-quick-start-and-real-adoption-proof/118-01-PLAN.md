---
phase: 118-runnable-quick-start-and-real-adoption-proof
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - examples/phoenix_host/mix.exs
  - examples/phoenix_host/priv/repo/seeds.exs
  - examples/QUICK_START.md
autonomous: true
requirements:
  - QUICK-01
must_haves:
  truths:
    - "QUICK-01/D-01: The Phoenix host exposes real `setup`, `ecto.setup`, and `ecto.reset` aliases before `examples/QUICK_START.md` tells a clean-checkout reader to run `mix setup`."
    - "QUICK-01/D-02: `examples/QUICK_START.md` is walkthrough first and proof second: first run the Phoenix host at `http://localhost:4002`, then run deterministic proof commands."
    - "QUICK-01/D-03/D-04: The quick start shows `npm ci`, `npx playwright install chromium`, and `npx playwright test e2e/offline_sync.spec.ts`, and tells users to stop the dev server first because Playwright starts its own `MIX_ENV=test` server on port `4002`."
    - "QUICK-01/D-05/D-06: The quick start names exact current paths and routes while keeping checked-in iOS/Android steps advisory/local-development until Phase 119."
    - "QUICK-01/D-07/D-08/D-09/D-10: The proof ladder is Phoenix smoke -> required Playwright offline E2E -> bounded bridge proof -> native-owned/route-unavailable contract proof -> optional advisory native UI steps, not smoke-only, CI-only, or native-first."
    - "D-24/D-25/D-26/D-27: Copy is precise, short, example-heavy, candid, route-owner-first, and excludes broad UI/collateral work and generic mobile framework positioning."
  artifacts:
    - path: "examples/phoenix_host/mix.exs"
      provides: "Phoenix host clean-checkout setup aliases"
      contains: "setup"
    - path: "examples/phoenix_host/priv/repo/seeds.exs"
      provides: "Explicit seed hook used by `mix ecto.setup`"
      contains: "offline_study.js"
    - path: "examples/QUICK_START.md"
      provides: "Command-verified walkthrough and proof ladder"
      contains: "http://localhost:4002"
  key_links:
    - from: "examples/QUICK_START.md"
      to: "examples/phoenix_host/mix.exs"
      via: "`mix setup` in the guide maps to real Phoenix host aliases"
      pattern: "setup"
    - from: "examples/QUICK_START.md"
      to: "examples/phoenix_host/playwright.config.ts"
      via: "Guide explains Playwright owns the `MIX_ENV=test` server and port `4002`"
      pattern: "MIX_ENV=test|4002"
    - from: "examples/QUICK_START.md"
      to: "script/verify_bounded_bridge_proof.sh"
      via: "Guide keeps `/bridge-proof` and Share wording compatible with the bounded bridge verifier"
      pattern: "/bridge-proof|Share"
    - from: "examples/QUICK_START.md"
      to: "script/verify_phase5_example_hosts.sh"
      via: "Guide documents the native-skipped contract proof with `CROSSWAKE_PHASE5_NATIVE_PROOFS=0`"
      pattern: "CROSSWAKE_PHASE5_NATIVE_PROOFS=0"
---

<objective>
Rewrite the quick start so a skeptical adopter can run the Phoenix host from a clean checkout and then prove current v11/v12 architecture without stale commands or native overclaim.

Purpose: QUICK-01 needs the first hands-on path to be real, path-exact, command-verified, and honest about advisory native evidence. This plan implements D-01 through D-10 and D-24 through D-27.
Output: Phoenix host setup/reset aliases, an explicit seed hook, and a rewritten `examples/QUICK_START.md` that keeps `script/verify_bounded_bridge_proof.sh` compatible.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/118-runnable-quick-start-and-real-adoption-proof/118-CONTEXT.md
@.planning/phases/118-runnable-quick-start-and-real-adoption-proof/118-RESEARCH.md
@.planning/phases/118-runnable-quick-start-and-real-adoption-proof/118-VALIDATION.md
@.planning/phases/116-proof-debt-and-release-truth/116-CONTEXT.md
@.planning/phases/116-proof-debt-and-release-truth/116-02-SUMMARY.md
@.planning/phases/117-route-policy-and-support-truth-guide-foundation/117-CONTEXT.md
@.planning/phases/117-route-policy-and-support-truth-guide-foundation/117-03-SUMMARY.md
@examples/phoenix_host/mix.exs
@examples/phoenix_host/config/config.exs
@examples/phoenix_host/playwright.config.ts
@examples/phoenix_host/package.json
@examples/phoenix_host/e2e/offline_sync.spec.ts
@examples/phoenix_host/lib/crosswake_example/router.ex
@examples/ios_shell_host/CrosswakeShell.xcodeproj/project.pbxproj
@examples/android_shell_host/app/build.gradle
@examples/QUICK_START.md
@guides/route_policy.md
@guides/web_to_mobile_migration.md
@guides/support_matrix.md
@script/verify_bounded_bridge_proof.sh
@script/verify_phase5_example_hosts.sh
@script/check-e2e-honesty.mjs
</context>

## Artifacts this phase produces

- Plan file: `.planning/phases/118-runnable-quick-start-and-real-adoption-proof/118-01-PLAN.md`.
- Plan file: `.planning/phases/118-runnable-quick-start-and-real-adoption-proof/118-02-PLAN.md`.
- Plan file: `.planning/phases/118-runnable-quick-start-and-real-adoption-proof/118-03-PLAN.md`.
- Changed aliases in `examples/phoenix_host/mix.exs`: `setup`, `ecto.setup`, and `ecto.reset`; the existing `test` alias remains.
- New seed hook: `examples/phoenix_host/priv/repo/seeds.exs`, used by `mix ecto.setup` without fabricating offline proof data.
- Rewritten doc: `examples/QUICK_START.md`.
- Target quick-start headings or equivalent job-order sections: `First run`, `See the route owners`, `Prove offline replay`, `Prove bounded bridge`, `Native steps are advisory`, `Troubleshooting quick checks`, and `What this does not prove`.
- Rewritten doc owned by Plan 118-02: `guides/adoption.md`.
- Target adoption headings or equivalent job-order sections: `Proof walkthrough`, `Reusable offline-island recipe`, `Replay outcomes`, and `What this does not prove`.
- New test file owned by Plan 118-03: `test/crosswake/guides/quick_start_adoption_drift_test.exs`.
- New test module owned by Plan 118-03: `Crosswake.Guides.QuickStartAdoptionDriftTest`.
- New helper functions owned by Plan 118-03: `current_version/0`, `phoenix_host_port/0`, `playwright_port/0`, `setup_aliases/0`, `existing_path?/1`, `scan_quick_start/1`, `scan_adoption/1`, `failure/3`, `format_failures/1`, and `assert_no_drift_failures/1`.

<tasks>

<task type="auto">
  <name>Task 1: Add Phoenix host setup/reset aliases before documenting `mix setup`</name>
  <read_first>
    .planning/phases/118-runnable-quick-start-and-real-adoption-proof/118-CONTEXT.md
    .planning/phases/118-runnable-quick-start-and-real-adoption-proof/118-RESEARCH.md
    .planning/phases/118-runnable-quick-start-and-real-adoption-proof/118-VALIDATION.md
    examples/phoenix_host/mix.exs
    examples/phoenix_host/config/config.exs
    examples/phoenix_host/playwright.config.ts
  </read_first>
  <files>examples/phoenix_host/mix.exs, examples/phoenix_host/priv/repo/seeds.exs</files>
  <action>Implement D-01 in `examples/phoenix_host/mix.exs` with small Phoenix-style aliases: `setup: ["deps.get", "ecto.setup"]`, `ecto.setup: ["ecto.create", "ecto.migrate", "run priv/repo/seeds.exs"]`, and `ecto.reset: ["ecto.drop", "ecto.setup"]`. Preserve the existing `test` alias behavior. If `examples/phoenix_host/priv/repo/seeds.exs` is still absent, create it as an explicit seed hook that documents that the current offline study cards are app-owned browser data seeded by `examples/phoenix_host/priv/static/offline_study.js`; do not invent server-side rows just to make the seed step look busy. Keep these aliases transparent and do not hide the proof ladder behind one opaque quick-start script.</action>
  <acceptance_criteria>
    - `examples/phoenix_host/mix.exs` contains `setup`, `ecto.setup`, and `ecto.reset` aliases.
    - `setup` runs dependency fetch and delegates to `ecto.setup`.
    - `ecto.setup` creates, migrates, and runs `priv/repo/seeds.exs` for the SQLite database.
    - `ecto.reset` drops and reruns `ecto.setup`.
    - `examples/phoenix_host/priv/repo/seeds.exs` exists and does not create fake offline proof data that belongs to the app-owned IndexedDB island.
    - The existing `test` alias still creates and migrates before running tests.
    - `cd examples/phoenix_host && mix setup` exits successfully from the current checkout.
    - `cd examples/phoenix_host && mix ecto.reset` exits successfully and leaves the example host migrated.
  </acceptance_criteria>
  <verify>
    <automated>cd examples/phoenix_host && mix setup</automated>
    <automated>cd examples/phoenix_host && mix ecto.reset</automated>
  </verify>
  <done>The Phoenix host has real clean-checkout setup/reset aliases that the quick start can honestly document.</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite the quick start as walkthrough first and proof second</name>
  <read_first>
    .planning/phases/118-runnable-quick-start-and-real-adoption-proof/118-CONTEXT.md
    .planning/phases/118-runnable-quick-start-and-real-adoption-proof/118-RESEARCH.md
    .planning/phases/118-runnable-quick-start-and-real-adoption-proof/118-VALIDATION.md
    examples/QUICK_START.md
    examples/phoenix_host/mix.exs
    examples/phoenix_host/config/config.exs
    examples/phoenix_host/playwright.config.ts
    examples/phoenix_host/package.json
    examples/phoenix_host/e2e/offline_sync.spec.ts
    examples/phoenix_host/lib/crosswake_example/router.ex
    examples/ios_shell_host/CrosswakeShell.xcodeproj/project.pbxproj
    examples/android_shell_host/app/build.gradle
    guides/route_policy.md
    guides/web_to_mobile_migration.md
    guides/support_matrix.md
    script/verify_bounded_bridge_proof.sh
    script/verify_phase5_example_hosts.sh
    script/check-e2e-honesty.mjs
  </read_first>
  <files>examples/QUICK_START.md</files>
  <action>Replace the Phase 116 safety note with a complete D-02 walkthrough-first and proof-second quick start. The first-run walkthrough must tell readers to run `cd examples/phoenix_host`, `mix setup`, and `PORT=4002 mix phx.server`, then visit `http://localhost:4002/`, `http://localhost:4002/offline`, and `http://localhost:4002/bridge-proof` with expected route-owner observations. Per D-03 and D-04, the first required correctness proof after smoke must say to stop the dev server and then run `npm ci`, `npx playwright install chromium`, and `npx playwright test e2e/offline_sync.spec.ts` from `examples/phoenix_host`, explaining that `playwright.config.ts` starts its own `MIX_ENV=test` Phoenix server on port `4002` and exposes `/_e2e`. Per D-05, name the current paths `examples/phoenix_host`, `examples/ios_shell_host/CrosswakeShell.xcodeproj`, `examples/android_shell_host`, route `/offline`, route `/bridge-proof`, and endpoint `/study/sync`. Per D-07 and D-08, keep the proof ladder in this order: Phoenix smoke, Playwright offline proof of IndexedDB queue/reconnect app flush/Ecto row/idempotent duplicate/accepted outbox deletion, repo-root `bash script/verify_bounded_bridge_proof.sh`, repo-root `CROSSWAKE_PHASE5_NATIVE_PROOFS=0 bash script/verify_phase5_example_hosts.sh`, then optional native UI steps. Keep `/bridge-proof` and the word `Share` in the bounded-bridge section so `script/verify_bounded_bridge_proof.sh` remains compatible; update that script only if the assertion contract is deliberately changed and still proves the same route/capability truth. Per D-06 and D-09, iOS and Android instructions must be labeled advisory/local-development and must not present checked-in native hosts as published-coordinate proof. Per D-10, do not collapse the guide to smoke-only. Per D-24 through D-27, use precise route-owner and proof-label copy, include troubleshooting quick checks and a "what this does not prove" caveat, and do not add screenshots, recordings, artifact manifests, visual collateral, generic WebView-wrapper claims, React Native clone positioning, LiveView-to-native rendering claims, plugin-platform language, or everything-offline promises.</action>
  <acceptance_criteria>
    - `examples/QUICK_START.md` no longer reads as a temporary Phase 116 safety note.
    - The walkthrough begins with `cd examples/phoenix_host`, `mix setup`, and `PORT=4002 mix phx.server`.
    - The walkthrough names `http://localhost:4002/`, `/offline`, `/bridge-proof`, and `/study/sync`.
    - The quick start explains expected observations for Phoenix-owned route ownership, the offline island, and bounded bridge route.
    - The proof section tells users to stop the dev server before Playwright because Playwright starts its own `MIX_ENV=test` server on port `4002` and exposes `/_e2e`.
    - The proof section includes `npm ci`, `npx playwright install chromium`, and `npx playwright test e2e/offline_sync.spec.ts` from `examples/phoenix_host`.
    - The bounded bridge section includes repo-root `bash script/verify_bounded_bridge_proof.sh`, `/bridge-proof`, and `Share`.
    - The native-owned/route-unavailable contract section includes repo-root `CROSSWAKE_PHASE5_NATIVE_PROOFS=0 bash script/verify_phase5_example_hosts.sh`.
    - Optional iOS instructions name `examples/ios_shell_host/CrosswakeShell.xcodeproj` and are labeled advisory/local-development until Phase 119.
    - Optional Android instructions name `examples/android_shell_host` and are labeled advisory/local-development until Phase 119.
    - The quick start does not claim simulator, emulator, or physical-device evidence is merge-blocking or required in Phase 118.
    - The quick start does not claim checked-in native hosts are published-coordinate proof.
    - The quick start does not describe the bridge as offline mutation authority or imply broad background sync.
  </acceptance_criteria>
  <verify>
    <automated>cd examples/phoenix_host && mix setup</automated>
    <automated>cd examples/phoenix_host && npm ci && npx playwright install chromium && npx playwright test e2e/offline_sync.spec.ts</automated>
    <automated>bash script/verify_bounded_bridge_proof.sh</automated>
    <automated>CROSSWAKE_PHASE5_NATIVE_PROOFS=0 bash script/verify_phase5_example_hosts.sh</automated>
    <automated>node script/check-e2e-honesty.mjs</automated>
  </verify>
  <done>`examples/QUICK_START.md` is runnable from a clean checkout, command-verified against current repo paths and port `4002`, and honest about advisory native steps.</done>
</task>

</tasks>

## Source Audit

| SOURCE | ID | Feature / Requirement | Plan | Status | Notes |
|--------|----|-----------------------|------|--------|-------|
| GOAL | Phase 118 | Skeptical adopter can follow the first hands-on path from clean checkout and see current v11/v12 architecture without stale commands or fictional offline authority | 118-01, 118-02, 118-03 | COVERED | Quick start, adoption guide, and drift guard map directly to the goal. |
| REQ | QUICK-01 | Runnable quick start with exact setup/server/native commands, paths, expected output, proof commands, and advisory native steps | 118-01, 118-03 | COVERED | This plan rewrites the quick start; 118-03 guards it. |
| REQ | ADOPT-01 | Adoption guide teaches v12 app-owned IndexedDB outbox/reconnect/Ecto truth | 118-02, 118-03 | COVERED | Covered by 118-02 and scanner checks in 118-03. |
| REQ | DRIFT-02 | Quick-start and adoption drift is mechanically guarded | 118-03 | COVERED | ExUnit docs-contract scanner in Wave 2. |
| RESEARCH | Phoenix setup aliases missing | 118-01 | COVERED | D-01 task adds setup/reset aliases and seed hook. |
| RESEARCH | Port/path/proof ladder truth | 118-01 | COVERED | D-02 through D-10 task names port `4002`, routes, scripts, and native caveats. |
| RESEARCH | Bounded bridge verifier depends on quick-start text | 118-01 | COVERED | Task keeps `/bridge-proof` and `Share` wording or updates verifier intentionally. |
| RESEARCH | App-owned offline proof and adoption rewrite | 118-02 | COVERED | Covered by the adoption plan. |
| RESEARCH | ExUnit scanner with source-derived facts | 118-03 | COVERED | Covered by the drift-guard plan. |
| CONTEXT | D-01 | Add Phoenix setup/reset aliases before documenting `mix setup` | 118-01 | COVERED | Task 1. |
| CONTEXT | D-02 | Walkthrough first, proof second | 118-01 | COVERED | Task 2. |
| CONTEXT | D-03 | Explicit Node/Playwright setup commands | 118-01 | COVERED | Task 2. |
| CONTEXT | D-04 | Stop dev server before Playwright proof | 118-01 | COVERED | Task 2. |
| CONTEXT | D-05 | Exact paths/routes/endpoints | 118-01 | COVERED | Task 2. |
| CONTEXT | D-06 | Native steps advisory/local-development | 118-01, 118-03 | COVERED | Task 2 and scanner plan. |
| CONTEXT | D-07 | Tiered proof ladder | 118-01 | COVERED | Task 2. |
| CONTEXT | D-08 | Playwright offline proof first required correctness proof | 118-01, 118-02 | COVERED | Task 2 and adoption proof walkthrough. |
| CONTEXT | D-09 | Native simulator/emulator/device evidence not merge-blocking or first-run required | 118-01 | COVERED | Task 2. |
| CONTEXT | D-10 | No smoke-only guide | 118-01 | COVERED | Task 2. |
| CONTEXT | D-11..D-17 | Adoption guide story and offline truth | 118-02 | COVERED | Adoption plan owns these decisions. |
| CONTEXT | D-18..D-23 | DRIFT-02 guard shape | 118-03 | COVERED | Drift-guard plan owns these decisions. |
| CONTEXT | D-24..D-27 | Voice, headings, no visual scope, route-owner thesis | 118-01, 118-02, 118-03 | COVERED | Quick start, adoption, and guard acceptance criteria include these constraints. |

Deferred scope is preserved: no native evidence classification, native coordinate fixes, screenshots, recordings, artifact manifests, broad troubleshooting docs, executable markdown command runner, new offline engine, or full conflict-resolution UI is planned.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| clean checkout -> quick-start commands | Reader runs documented commands against the local repo and expects current setup/proof truth. |
| quick-start prose -> proof scripts | Public text must remain compatible with existing deterministic proof scripts. |
| quick-start native steps -> support truth | Advisory/local-development steps can be misread as published-coordinate or device support proof. |
| quick-start offline claims -> route-owner authority | Offline proof language can accidentally promote bridge or app-wide background sync authority. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-118-01 | Repudiation | `examples/QUICK_START.md` setup/proof commands | mitigate | Add real Mix aliases first, document exact commands, and verify `mix setup`, Playwright, bounded bridge, native-skipped phase5 proof, and E2E honesty guard. |
| T-118-02 | Denial of Service | Playwright proof port ownership | mitigate | Require stop-dev-server caveat because Playwright starts its own `MIX_ENV=test` server on port `4002`. |
| T-118-03 | Spoofing | checked-in native host evidence | mitigate | Label iOS/Android walkthroughs advisory/local-development and state Phase 119 owns evidence classification. |
| T-118-04 | Elevation of Privilege | offline authority wording | mitigate | Keep bridge out of mutation authority, name the app-owned outbox, and avoid broad background sync claims. |
| T-118-05 | Tampering | `script/verify_bounded_bridge_proof.sh` compatibility | mitigate | Keep `/bridge-proof` and `Share` in quick-start text or update the verifier deliberately with equivalent proof semantics. |
| T-118-SC | Tampering | package installs | accept | No new npm, pip, or cargo dependencies are introduced; `npm ci` only installs the existing locked Playwright dev dependencies for proof. |
</threat_model>

<verification>
Plan-level checks:

<automated>cd examples/phoenix_host && mix setup</automated>
<automated>cd examples/phoenix_host && npm ci && npx playwright install chromium && npx playwright test e2e/offline_sync.spec.ts</automated>
<automated>bash script/verify_bounded_bridge_proof.sh</automated>
<automated>CROSSWAKE_PHASE5_NATIVE_PROOFS=0 bash script/verify_phase5_example_hosts.sh</automated>
<automated>node script/check-e2e-honesty.mjs</automated>

After Wave 2 exists, also run `mix test test/crosswake/guides/quick_start_adoption_drift_test.exs` to prove the quick-start assertions are locked.
</verification>

<success_criteria>
QUICK-01 is satisfied: a clean-checkout reader can run the Phoenix host on port `4002`, inspect `/`, `/offline`, and `/bridge-proof`, run required proof commands for the real offline and bounded-bridge paths, understand native contract proof without simulator/device overclaim, and see native UI steps labeled advisory/local-development until Phase 119.
</success_criteria>

<output>
Create `.planning/phases/118-runnable-quick-start-and-real-adoption-proof/118-01-SUMMARY.md` when done.
</output>
