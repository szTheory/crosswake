---
phase: "72"
plan: "01"
type: execute
wave: 0
depends_on: []
files_modified:
  - test/crosswake/proof/phase72_media_evidence_workflow_proof_test.exs
autonomous: true
requirements:
  - MED-01
  - MED-02
must_haves:
  truths:
    - "D-01: Model Phase 72 as route/workflow degradation plus a narrow proof-only local queue fixture and backend verification gate."
    - "D-02: Use narrow terms such as `QueuedCapture`, `LocalUploadQueue`, `degraded_capture_recorded`, `queued_capture`, `upload_attempt_failed`, `awaiting_verification`, and `backend_verified_available`."
    - "D-03: Prove upload grant -> degraded local capture -> queued evidence -> recovery into `ReconciliationInbox` -> `MediaProjection` uploaded/scanning -> explicit backend verification to `:available`."
    - "D-04: Keep local recovery state in the proof/example-host media namespace; do not add Ecto schemas, migrations, production outboxes, background workers, native upload APIs, or Crosswake core sync primitives."
    - "D-06: Build `test/crosswake/proof/phase72_media_evidence_workflow_proof_test.exs` as the targeted merge-blocking proof file."
    - "D-07: The primary proof layer is pure ExUnit over Rindle contracts and example-host media modules, with deterministic degraded-network/recovery fixtures and no runtime/server/provider/device dependency."
    - "D-08: Add a hermeticity self-scan guard similar to Phase 70/71."
    - "D-11: Use one integrated product-shaped positive path: `degraded_capture_reconciles_after_recovery`."
    - "D-12: Positive path proves backend verification with `verification_ref` and `authoritative_at` is required before media becomes `:available`."
    - "D-13: Include `multipart_completion_requires_full_payload` so partial multipart evidence cannot complete availability."
    - "D-14: Include `idempotent_replay_ignores_trace_correlation` so changed `correlation_id` cannot duplicate availability or media objects."
    - "D-15: Include `scan_started_then_verified`; upload evidence can project `:scanning`, but availability waits for backend verification."
    - "D-16: Include stale/expired grant, missing payload identity, partial multipart, corrupt hash, unsupported integrity, scan failure, direct override, invalid source, redaction leak, and missing backend-field negatives."
    - "D-17: Keep denials/rejections in Rindle/media vocabulary and do not invent provider-storage denial codes unless a real vocabulary gap appears."
    - "D-18: Redaction proof covers hostile raw payload, local path, route params, actor/session refs, device IDs, email, IP, user agent, raw storage credentials, and authority/availability hints."
  artifacts:
    - path: test/crosswake/proof/phase72_media_evidence_workflow_proof_test.exs
      provides: Wave 0 red proof contract for MED-01 and MED-02
  key_links:
    - from: test/crosswake/proof/phase72_media_evidence_workflow_proof_test.exs
      to: lib/crosswake/companions/rindle/contracts.ex
      via: upload grant, capture evidence, and backend-owned media object contracts
    - from: test/crosswake/proof/phase72_media_evidence_workflow_proof_test.exs
      to: examples/phoenix_host/lib/crosswake_example/media/reconciliation_inbox.ex
      via: evidence ingestion and replay metadata
    - from: test/crosswake/proof/phase72_media_evidence_workflow_proof_test.exs
      to: examples/phoenix_host/lib/crosswake_example/media/media_projection.ex
      via: evidence projection and backend verification promotion
---

<objective>
Create the Wave 0 automated proof contract for Phase 72. The proof must be hermetic ExUnit, targeted at `MED-01` and `MED-02`, and intentionally expose any missing degraded-capture, replay, multipart, integrity, redaction, or backend-authority behavior that later plans must satisfy.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/MILESTONE-ARC.md
@.planning/phases/72-media-evidence-workflow-proof/72-CONTEXT.md
@.planning/phases/72-media-evidence-workflow-proof/72-RESEARCH.md
@.planning/phases/72-media-evidence-workflow-proof/72-VALIDATION.md
@.planning/phases/72-media-evidence-workflow-proof/72-PATTERNS.md
@.planning/phases/71-notification-driven-workflow-proof/71-03-SUMMARY.md
@.planning/phases/70-subscription-saas-commerce-proof/70-03-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 72-01-01: Create Hermetic Media Recovery Proof Shell</name>
  <files>test/crosswake/proof/phase72_media_evidence_workflow_proof_test.exs</files>
  <read_first>
    - test/crosswake/proof/phase72_media_evidence_workflow_proof_test.exs
    - test/crosswake/proof/phase45_rindle_mock_media_test.exs
    - test/crosswake/proof/phase70_subscription_saas_commerce_proof_test.exs
    - lib/crosswake/companions/rindle/contracts.ex
    - lib/crosswake/companions/rindle/reconciliation.ex
    - examples/phoenix_host/lib/crosswake_example/media/mock_capture.ex
    - examples/phoenix_host/lib/crosswake_example/media/reconciliation_inbox.ex
    - examples/phoenix_host/lib/crosswake_example/media/media_projection.ex
    - .planning/phases/72-media-evidence-workflow-proof/72-VALIDATION.md
    - .planning/phases/72-media-evidence-workflow-proof/72-PATTERNS.md
  </read_first>
  <action>Create `Crosswake.Proof.Phase72MediaEvidenceWorkflowProofTest` with `use ExUnit.Case, async: false`. Add `Code.require_file/2` calls only for `reconciliation_keys.ex`, `reconciliation_inbox.ex`, `mock_capture.ex`, and `media_projection.ex`. Alias `Crosswake.Companions.Rindle.Contracts`, `Crosswake.Companions.Rindle.Reconciliation`, `CrosswakeExample.Media.MockCapture`, `CrosswakeExample.Media.ReconciliationInbox`, and `CrosswakeExample.Media.MediaProjection`. Add inline proof-only structs or helpers named `QueuedCapture` and `LocalUploadQueue` that model `:degraded_capture_recorded`, `:upload_attempt_failed`, `:queued_capture`, and `:network_recovered` without starting processes or persisting to disk. Add the primary red test `degraded_capture_reconciles_after_recovery_without_availability` proving grant issuance, local queue record, failed upload state, recovery replay through Rindle ingestion, projection to `:uploaded` or `:scanning`, and later backend verification to `:available` only after `verification_ref: "verify_media_72"` and `authoritative_at: "2026-06-05T00:00:00Z"`.</action>
  <verify>
    <automated>mix test test/crosswake/proof/phase72_media_evidence_workflow_proof_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - Source contains module name `Crosswake.Proof.Phase72MediaEvidenceWorkflowProofTest`.
    - Source contains `use ExUnit.Case, async: false`.
    - Source contains helper names `QueuedCapture` and `LocalUploadQueue`.
    - Source contains exact state atoms `:degraded_capture_recorded`, `:upload_attempt_failed`, `:queued_capture`, and `:network_recovered`.
    - Primary test asserts local queued capture and Rindle evidence ingestion do not produce `:available`.
    - Primary test asserts backend verification with `verify_media_72` is required before `media_object.state == :available`.
    - Running `mix test test/crosswake/proof/phase72_media_evidence_workflow_proof_test.exs` reaches the new proof file; red failures are acceptable in Wave 0 only when they correspond to missing Phase 72 behavior, not syntax or compile errors.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 72-01-02: Add Recovery Authority Matrix And Replay Negatives</name>
  <files>test/crosswake/proof/phase72_media_evidence_workflow_proof_test.exs</files>
  <read_first>
    - test/crosswake/proof/phase72_media_evidence_workflow_proof_test.exs
    - test/crosswake/companions/rindle/contracts_test.exs
    - test/crosswake/companions/rindle/reconciliation_test.exs
    - test/crosswake/proof/phase45_rindle_mock_media_test.exs
    - lib/crosswake/companions/rindle/contracts.ex
    - lib/crosswake/companions/rindle/reconciliation.ex
    - examples/phoenix_host/lib/crosswake_example/media/reconciliation_keys.ex
  </read_first>
  <action>Add tests for `multipart_completion_requires_full_payload`, `idempotent_replay_ignores_trace_correlation`, `scan_started_then_verified`, `stale_grant_is_rejected_before_reconciliation`, `missing_payload_identity_is_rejected`, `partial_multipart_cannot_complete_upload`, `corrupt_hash_or_unsupported_integrity_is_rejected`, `backend_scan_failure_projects_rejected_or_verification_failed`, `direct_availability_override_is_forbidden`, `invalid_or_spoofed_evidence_source_is_rejected`, and `available_media_requires_backend_fields`. Use exact Rindle statuses `:awaiting_verification`, `:verification_in_progress`, `:verification_failed`, `:rejected`, `:conflict`, and `:stale_authority` where the current implementation supports them. If a status is missing, leave the proof red instead of inventing storage/provider denial codes. Assert replay uses the same event/idempotency identity even when `correlation_id` changes from `corr_first` to `corr_second`.</action>
  <verify>
    <automated>mix test test/crosswake/proof/phase72_media_evidence_workflow_proof_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - Proof file contains test names listed in the task action.
    - Replay test asserts `replay? == true`, event identity is unchanged, and trace metadata can show `corr_second`.
    - Direct override test asserts `Reconciliation.ingest_capture_evidence/2` rejects `availability_state: :available` and `authority_state: :verified`.
    - Available media test asserts `Contracts.new_media_object/1` rejects `state: :available` without `verification_ref` and `authoritative_at`.
    - No assertion treats `CaptureEvidence`, `trace_metadata`, `correlation_id`, multipart metadata, or UI state as authority.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 72-01-03: Add Redaction And Hermeticity Self-Scan Guards</name>
  <files>test/crosswake/proof/phase72_media_evidence_workflow_proof_test.exs</files>
  <read_first>
    - test/crosswake/proof/phase72_media_evidence_workflow_proof_test.exs
    - test/crosswake/proof/phase70_subscription_saas_commerce_proof_test.exs
    - test/crosswake/proof/phase71_notification_workflow_proof_test.exs
    - test/support/proof_assertions.ex
    - lib/crosswake/companions/rindle/contracts.ex
    - examples/phoenix_host/lib/crosswake_example/media/reconciliation_inbox.ex
  </read_first>
  <action>Add hostile metadata values `raw-payload-must-not-leak`, `/private/var/mobile/capture.jpg`, `route-secret`, `actor-private`, `session-private`, `device-private`, `person@example.test`, `203.0.113.72`, `private-user-agent`, `raw-storage-credential`, `availability:available`, and `authority:verified`. Assert public proof/support maps, denial/rejection details, and inspected proof output do not contain those values. Add a hermeticity self-scan that extracts all `Code.require_file` paths and asserts exactly the allowed media helper files are required. Forbid required paths containing `_live`, `endpoint`, `application`, `router`, `repo`, `_web`, `storage`, `native`, `ios`, `android`, `provider`, and `background`. Add a source scan forbidding `System.cmd`, `start_supervised`, `Phoenix.PubSub`, `LiveViewTest`, `Req.`, `Finch.`, provider SDK names, `s3`, `gcs`, `azure`, `tus`, `URLSession`, `WorkManager`, and `BGProcessingTask` in the merge-blocking proof body.</action>
  <verify>
    <automated>mix test test/crosswake/proof/phase72_media_evidence_workflow_proof_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - Proof file contains all hostile metadata values from the action and refutes them from public details/output.
    - Hermeticity self-scan asserts only `reconciliation_keys.ex`, `reconciliation_inbox.ex`, `mock_capture.ex`, and `media_projection.ex` are required.
    - Proof file source scan forbids process/server/network/provider/native/storage tokens listed in the action.
    - Proof file does not claim real storage upload, native camera capture, background transfer, or local-first sync.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Local capture queue -> Rindle evidence | Local queued capture may preserve evidence identity but cannot grant media availability. |
| Device evidence -> backend projection | Device evidence can move a projection to uploaded/scanning, not available. |
| Trace metadata -> public proof output | Raw payloads, paths, credentials, actor/session/device data, and authority hints must not leak. |
| Hermetic proof -> public support posture | Simulated degradation proof must not become a storage, native, background upload, or sync claim. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-72-01 | Tampering | Local queue fixture | medium | mitigate | Proof asserts queued state records stable evidence identity and does not mutate availability. |
| T-72-02 | Elevation of Privilege | Media projection | high | mitigate | Proof asserts backend verification fields are required for `:available`. |
| T-72-03 | Repudiation | Replay/idempotency | medium | mitigate | Proof asserts changed correlation IDs do not create duplicate event identity. |
| T-72-04 | Tampering | Multipart/integrity metadata | medium | mitigate | Proof asserts partial/corrupt evidence cannot complete upload or verification. |
| T-72-05 | Information Disclosure | Proof/support output | medium | mitigate | Proof asserts hostile metadata is redacted from public details and inspected output. |

## High Severity Block

Execution must not proceed past Wave 0 unless the proof can express `T-72-02` as an automated assertion: local/device evidence never directly produces `:available`; backend verification is required.
</threat_model>

<verification>
- `mix test test/crosswake/proof/phase72_media_evidence_workflow_proof_test.exs`
- Wave 0 expected state: compile succeeds and the new proof contract is present; red failures are acceptable only for behavior scheduled in Wave 1.
</verification>

<success_criteria>
- Phase 72 has a targeted automated proof file for `MED-01` and `MED-02`.
- The proof file is hermetic and merge-blocking-safe.
- The red assertions cover degraded capture recovery, replay/idempotency, multipart/integrity rejection, backend-only availability, redaction, and hermeticity.
</success_criteria>

<output>
Create `.planning/phases/72-media-evidence-workflow-proof/72-01-SUMMARY.md` when done.
</output>
