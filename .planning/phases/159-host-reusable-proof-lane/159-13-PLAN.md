---
phase: 159-host-reusable-proof-lane
plan: "13"
type: execute
wave: 7
depends_on: [159-12]
files_modified:
  - priv/native/crosswake_proof_lane_fs.c
  - test/mix/tasks/crosswake_gen_proof_lane_test.exs
autonomous: true
gap_closure: true
requirements: [PROOF-01]
must_haves:
  truths:
    - "After exclusive destination creation, every copy read, write, and fsync failure removes the newly created destination before returning the original failure result (D-02, D-22, PROOF-01)."
    - "A no-replace manifest publication collision removes the helper-owned staging file before returning reuse; if staging cleanup fails, publication returns failure instead of reuse (D-02, D-22, PROOF-01)."
    - "Deterministic regressions prove both cleanup paths, cleanup-failure classification, concurrent-winner preservation, and a clean rerun with no retained helper-owned debris."
  artifacts:
    - path: "priv/native/crosswake_proof_lane_fs.c"
      provides: "Cleanup-complete exclusive write and no-replace publish lifecycle"
    - path: "test/mix/tasks/crosswake_gen_proof_lane_test.exs"
      provides: "Deterministic post-create and publish-collision cleanup regressions"
  key_links:
    - from: "Crosswake.ProofLane.GeneratorFS.write/4"
      to: "write_file"
      via: "closed native-helper exit status after shared post-create cleanup"
      pattern: "GeneratorFS.write|write_file|unlinkat"
    - from: "Crosswake.ProofLane.Generator.promote_manifest/4"
      to: "publish_file"
      via: "no-replace link collision followed by mandatory staging cleanup"
      pattern: "GeneratorFS.publish|publish_file|EEXIST"
  prohibitions:
    - statement: "MUST NOT remove or replace a pre-existing host-owned destination or a concurrent publication winner."
      category: product-ownership
      verification: flagged-unverified
    - statement: "MUST NOT report a manifest collision as reused while any helper-owned staging file remains or its cleanup result is unknown."
      category: integrity
      verification: flagged-unverified
    - statement: "MUST NOT echo native helper output, host configuration values, sensitive payloads, identities, credentials, tokens, media, transcripts, or stable device identifiers."
      category: privacy
      verification: flagged-unverified
    - statement: "MUST NOT add Android work, generic sync or storage, new UI breadth, dashboards, device packs, commerce, background sync, companions, or Phase 160-162 behavior."
      category: scope
      verification: flagged-unverified
flagged_assumptions:
  - "[FLAGGED/UNVERIFIED] No plain Phase SPEC exists; PROOF-01 concurrency is interpreted from the binding verifier evidence as preserving the concurrent destination winner while removing only helper-owned failed-write or staging artifacts."
  - "[FLAGGED/UNVERIFIED] PROOF-02 idempotency and concurrency semantics are unchanged because this repair adds no configuration or manifest representation; the existing rerun and two-generator tests remain mandatory regression controls."
  - "[FLAGGED/UNVERIFIED] PROOF-03 and PROOF-04 are unchanged by this filesystem repair and remain subject to the complete fresh final-tree gate in Plan 159-14."
---

<objective>
Close the two reproduced proof-lane filesystem cleanup gaps without changing the host-owned generator contract.

Purpose: A failed exclusive write must not become a retained host file, and a manifest collision must not leave helper-owned staging debris while claiming safe reuse.
Output: Cleanup-complete native helper behavior and deterministic regressions for both failure paths.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/ADR-FIRST-B2C-ADOPTER.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/159-host-reusable-proof-lane/159-CONTEXT.md
@.planning/phases/159-host-reusable-proof-lane/159-RESEARCH.md
@.planning/phases/159-host-reusable-proof-lane/159-PATTERNS.md
@.planning/phases/159-host-reusable-proof-lane/159-VERIFICATION.md
@.planning/phases/159-host-reusable-proof-lane/159-08-SUMMARY.md
@.planning/phases/159-host-reusable-proof-lane/159-12-SUMMARY.md
@lib/crosswake/proof_lane/generator.ex
@lib/crosswake/proof_lane/generator_fs.ex
@priv/native/crosswake_proof_lane_fs.c
@test/mix/tasks/crosswake_gen_proof_lane_test.exs

<interfaces>
Keep `GeneratorFS.write/4` and `publish/3` return contracts unchanged. Native exit 0 remains created, 10 remains reused, 12 remains unsafe destination, and every other native result remains `PL-GENERATE-WRITE`. Preserve the existing descriptor-relative `write_file` and `publish_file` entry points; this plan changes their cleanup guarantees, not their public shape.
</interfaces>
</context>

<assumption_delta_decision>
Primary noun: proof-lane filesystem cleanup contract.
Decision: no-change.
Rationale: This bounded cleanup introduces no second identity model, authority source, platform, or representation.
</assumption_delta_decision>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Make an interrupted exclusive write leave no destination</name>
  <reversibility rating="reversible">The change is local to the repository-owned native helper behind the unchanged GeneratorFS contract.</reversibility>
  <read_first>
    - .planning/phases/159-host-reusable-proof-lane/159-VERIFICATION.md
    - .planning/phases/159-host-reusable-proof-lane/159-08-SUMMARY.md
    - lib/crosswake/proof_lane/generator_fs.ex
    - priv/native/crosswake_proof_lane_fs.c
    - test/mix/tasks/crosswake_gen_proof_lane_test.exs
  </read_first>
  <files>priv/native/crosswake_proof_lane_fs.c, test/mix/tasks/crosswake_gen_proof_lane_test.exs</files>
  <behavior>
    - "Named `post-create read failure removes destination and permits full-byte rerun`, tagged `post_create_fault: :read`, independently injects a copy-read failure after exclusive final-path creation; it preserves the sanitized `PL-GENERATE-WRITE` result with only the safe relative destination path, leaves the destination absent, then reruns normally and creates the complete expected bytes."
    - "Named `post-create write failure removes destination and permits full-byte rerun`, tagged `post_create_fault: :write`, independently injects a copy-write failure after exclusive final-path creation; it preserves the sanitized `PL-GENERATE-WRITE` result with only the safe relative destination path, leaves the destination absent, then reruns normally and creates the complete expected bytes."
    - "Named `post-create fsync failure removes destination and permits full-byte rerun`, tagged `post_create_fault: :fsync`, independently injects a final-fsync failure after exclusive final-path creation; it preserves the sanitized `PL-GENERATE-WRITE` result with only the safe relative destination path, leaves the destination absent, then reruns normally and creates the complete expected bytes."
  </behavior>
  <action>Start RED with three named ExUnit regressions invoking the real compiled helper through `GeneratorFS.write/4`: `post-create read failure removes destination and permits full-byte rerun` tagged `post_create_fault: :read`, `post-create write failure removes destination and permits full-byte rerun` tagged `post_create_fault: :write`, and `post-create fsync failure removes destination and permits full-byte rerun` tagged `post_create_fault: :fsync`. Give each tag its own closed test-only fault selector so the read, write, and final-fsync branches are independently controlled only after `openat(... O_CREAT | O_EXCL ...)` succeeds; do not let a selector choose an arbitrary cleanup target or combine multiple fault branches. In every case assert the unchanged sanitized `{:error, {"PL-GENERATE-WRITE", safe_relative_destination}}` result, assert the newly created destination is absent, disable that case's selector, rerun normally, and assert the complete output is byte-identical to the expected source. In `write_file`, funnel every post-create read, write, and fsync failure through one cleanup path: retain the original failure classification, close the input and destination descriptors in a defined order, call `unlinkat(parent, leaf(relative), 0)` while the descriptor-pinned parent is still open, then close parent/root and return the original result. Preserve the existing-file byte-preservation regression so no pre-existing destination is ever removed, per D-02 and D-22.</action>
  <acceptance_criteria>
    - The named `post_create_fault: :read` regression independently forces the post-create read branch and proves the original sanitized `PL-GENERATE-WRITE` result, destination absence, and a successful byte-identical full-output rerun.
    - The named `post_create_fault: :write` regression independently forces the post-create write branch and proves the original sanitized `PL-GENERATE-WRITE` result, destination absence, and a successful byte-identical full-output rerun.
    - The named `post_create_fault: :fsync` regression independently forces the post-create fsync branch and proves the original sanitized `PL-GENERATE-WRITE` result, destination absence, and a successful byte-identical full-output rerun.
    - All three post-create error branches converge on cleanup before returning their original failure result, while existing-file reuse remains byte-preserving.
  </acceptance_criteria>
  <verify>
    <automated>mix test test/mix/tasks/crosswake_gen_proof_lane_test.exs --only post_create_fault:read &amp;&amp; mix test test/mix/tasks/crosswake_gen_proof_lane_test.exs --only post_create_fault:write &amp;&amp; mix test test/mix/tasks/crosswake_gen_proof_lane_test.exs --only post_create_fault:fsync &amp;&amp; mix test test/mix/tasks/crosswake_gen_proof_lane_test.exs</automated>
  </verify>
  <done>A failed exclusive scaffold write cannot leave a partial host-owned destination for a later rerun to reuse.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Clean manifest staging on collision and fail closed on cleanup failure</name>
  <reversibility rating="reversible">The no-replace manifest publication contract remains unchanged; only helper-owned staging cleanup becomes complete.</reversibility>
  <read_first>
    - .planning/phases/159-host-reusable-proof-lane/159-VERIFICATION.md
    - lib/crosswake/proof_lane/generator.ex
    - lib/crosswake/proof_lane/generator_fs.ex
    - priv/native/crosswake_proof_lane_fs.c
    - test/mix/tasks/crosswake_gen_proof_lane_test.exs
  </read_first>
  <files>priv/native/crosswake_proof_lane_fs.c, test/mix/tasks/crosswake_gen_proof_lane_test.exs</files>
  <behavior>
    - "When `linkat` reports an existing destination, the helper removes its staging source and returns reuse only after cleanup succeeds."
    - "When collision cleanup fails, the helper returns a write failure and never reports reuse."
    - "The pre-existing destination/concurrent winner remains byte-identical in both cases."
  </behavior>
  <action>Start RED with a deterministic real-helper collision: create a complete destination plus a helper-owned staging file, invoke publication, then assert the destination bytes are unchanged and the staging path is absent. Add a second deterministic control that forces the collision-cleanup operation itself to fail and asserts `GeneratorFS.publish/3` returns `PL-GENERATE-WRITE`, not reuse. In `publish_file`, handle `linkat` destination-exists separately: save that collision state, remove the staging leaf through the already pinned `stage_parent`, return EXISTS only when `unlinkat` succeeds, and return FAILURE when cleanup fails. Preserve the current successful-publication unlink behavior, descriptor closure, no-follow traversal, and non-echoing result mapping. Finish by rerunning the existing concurrent generators and interrupted-manifest controls so repeated/parallel runs preserve exactly one complete winner with no `.staging-*` file, per D-02, D-20, and D-22.</action>
  <acceptance_criteria>
    - A normal destination collision leaves the winner byte-identical, removes the helper-owned staging file, and maps to `{:ok, :reused}`.
    - A forced staging-cleanup failure maps to `{:error, {"PL-GENERATE-WRITE", ".crosswake/proof_lane.json"}}` and cannot be observed as reuse.
    - Concurrent and interrupted generator regressions find no retained `.staging-*` paths after successful/reused outcomes.
  </acceptance_criteria>
  <verify>
    <automated>mix test test/mix/tasks/crosswake_gen_proof_lane_test.exs &amp;&amp; mix format --check-formatted test/mix/tasks/crosswake_gen_proof_lane_test.exs &amp;&amp; cc -std=c11 -O2 -Wall -Wextra -Werror -o /tmp/crosswake-proof-lane-fs-plan-159-13 priv/native/crosswake_proof_lane_fs.c &amp;&amp; test -x /tmp/crosswake-proof-lane-fs-plan-159-13</automated>
  </verify>
  <done>Manifest publication returns reuse only after its own staging input is gone, and cleanup failure remains explicitly non-passing.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| rendered input bytes → exclusive host destination | A helper-owned create becomes a permanent host-owned file only after complete write and fsync. |
| helper-owned staging file → host manifest destination | A no-replace publication races with a possible host/concurrent winner. |

## STRIDE Threat Register — ASVS L1, block on high

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- | --- |
| T-159-G13-01 | Tampering | `write_file` post-create failures | high | mitigate | Shared unlink-before-return cleanup plus deterministic failure injection and complete-rerun regression. |
| T-159-G13-02 | Tampering / Repudiation | `publish_file` collision result | high | mitigate | Return EXISTS only after staging unlink succeeds; cleanup failure returns FAILURE and is regression-tested. |
| T-159-G13-03 | Information Disclosure | native helper failure mapping | medium | mitigate | Preserve stable rule plus safe relative path only; discard helper output and rejected values. |
| T-159-G13-SC | Tampering | package supply chain | low | accept | No install task or external package is introduced; the existing repository-owned C source compiles locally. |
</threat_model>

<verification>
Run the focused generator suite after both changes, compile the native helper with warnings as errors, and confirm deterministic tests cover destination absence, staging absence, cleanup-failure non-reuse, winner preservation, rerun completion, interruption, and two-writer behavior.
</verification>

<success_criteria>
- Both reproduced helper leaks are closed by real deterministic regressions.
- PROOF-01 missing-only ownership remains honest across post-create failure, collision, interruption, rerun, and concurrency.
- No public generator/config/evidence interface, external integration, UI, platform, or later-phase behavior changes.
</success_criteria>

## Multi-Source Coverage Audit

| Source | ID | Feature / requirement | Plan | Status | Notes |
| --- | --- | --- | --- | --- | --- |
| GOAL | — | Configurable host-owned proof scaffolding | 159-13, 159-14 | COVERED | Cleanup repair restores non-destructive generation; the following plan reruns end-to-end proof. |
| REQ | PROOF-01 | Non-overwriting host-owned scaffold | 159-13, 159-14 | COVERED | Both reproduced filesystem lifecycle failures and final proof are explicit. |
| REQ | PROOF-02 | Closed proof-lane configuration | existing 159-01 through 159-12; 159-14 | COVERED | No config change; final focused suite rechecks it. |
| REQ | PROOF-03 | Preserve primary browser corpus and bounded native proof | existing 159-01 through 159-12; 159-14 | COVERED | Real Phoenix browser and generated shared scheme rerun after cleanup. |
| REQ | PROOF-04 | Privacy-safe retained evidence | existing 159-01 through 159-12; 159-14 | COVERED | Evidence suite reruns; cleanup does not widen evidence. |
| RESEARCH | — | No-new-dependency generator/library stack | 159-13 | COVERED | Existing C helper and ExUnit only. |
| RESEARCH | — | Host ownership and collision-safe lifecycle | 159-13 | COVERED | Cleanup is descriptor-relative and preserves the winner. |
| CONTEXT | D-02, D-20, D-22 | Missing-only writes, honest inspection, atomic fail-closed lifecycle | 159-13 | COVERED | Actions cite and test each relevant locked decision. |
| CONTEXT | D-01, D-03-D-19, D-21, D-23 | Remaining Phase 159 locked contract | existing 159-01 through 159-12; 159-14 | COVERED | No decision is weakened; fresh final gate confirms retained behavior. |

Deferred Phase 160 replay/auth, Phase 161 pack/audio, Phase 162 physical-device evidence, Android work, generic sync/storage/test orchestration, dashboards, and support promotion remain excluded. No source item is missing.

## Artifacts this phase produces

- No new production symbol or path.
- Modified existing native functions: `write_file` and `publish_file` in `priv/native/crosswake_proof_lane_fs.c`.
- New regression test cases in `test/mix/tasks/crosswake_gen_proof_lane_test.exs`; no new product interface, schema, package, endpoint, UI surface, or external API integration.

<output>
Create `.planning/phases/159-host-reusable-proof-lane/159-13-SUMMARY.md` when done.
</output>
