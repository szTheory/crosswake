---
phase: 159-host-reusable-proof-lane
plan: "10"
type: execute
wave: 4
depends_on: [159-06]
files_modified:
  - examples/phoenix_host/e2e/support/offline_route_proof.ts
  - examples/phoenix_host/e2e/crosswake_proof_lane/browser_online_restore.spec.ts
  - examples/phoenix_host/e2e/crosswake_proof_lane/support/proof_lane.ts
  - examples/phoenix_host/package.json
  - priv/templates/crosswake/proof_lane/e2e/support/proof_lane.ts.eex
  - script/verify_phoenix_host_proof_lane.sh
  - test/crosswake/proof_lane/template_contract_test.exs
autonomous: true
gap_closure: true
requirements: [PROOF-02, PROOF-03, PROOF-04]
must_haves:
  truths:
    - "One automated command starts the configured Phoenix test host and executes the primary offline Playwright corpus through real UI mutation, IndexedDB queueing, app reconnect, one backend confirmation, empty outbox, and duplicate idempotency (D-03, PROOF-03)."
    - "Repository and generated helpers accept only the closed lowercase opaque mutation-ID shape before backend/evidence callbacks and reject malformed or identity-like values with a stable non-echoing rule (D-07, D-09, D-17, PROOF-02, PROOF-04)."
    - "The live proof preserves the host's existing Playwright configuration, fixtures, app, and primary `offline_sync.spec.ts`; generated support remains additive and cleanup restores online state (D-01 through D-03, D-22)."
  artifacts:
    - path: "script/verify_phoenix_host_proof_lane.sh"
      provides: "One-command typecheck plus runnable Phoenix-host Playwright proof"
    - path: "examples/phoenix_host/e2e/support/offline_route_proof.ts"
      provides: "Primary host browser semantics with strict opaque mutation-ID validation"
    - path: "priv/templates/crosswake/proof_lane/e2e/support/proof_lane.ts.eex"
      provides: "Generated host adapter with the same non-echoing opaque-ID boundary"
  key_links:
    - from: "script/verify_phoenix_host_proof_lane.sh"
      to: "examples/phoenix_host/playwright.config.ts webServer"
      via: "the existing Playwright runner starts the MIX_ENV=test Phoenix host"
      pattern: "offline_sync.spec.ts"
    - from: "extractMutationId"
      to: "backend confirmation and duplicate-idempotency callbacks"
      via: "strict validation before the opaque value crosses either callback boundary"
      pattern: "PL-BROWSER-MUTATION-ID"
---

<objective>
Turn the browser seam into a repeatable Phoenix-host behavioral gate and close its opaque mutation-ID boundary.

Purpose: Type correctness alone does not prove the preserved browser/offline-island corpus runs.
Output: One executable host command plus strict repository/generated mutation-ID validation.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/FIRST-B2C-ADOPTER-ADOPTION-BRIEF.md
@.planning/phases/159-host-reusable-proof-lane/159-CONTEXT.md
@.planning/phases/159-host-reusable-proof-lane/159-VERIFICATION.md
@.planning/phases/159-host-reusable-proof-lane/159-06-SUMMARY.md
@examples/phoenix_host/playwright.config.ts
@examples/phoenix_host/package.json
@examples/phoenix_host/e2e/offline_sync.spec.ts
@examples/phoenix_host/e2e/support/offline_route_proof.ts
@priv/templates/crosswake/proof_lane/e2e/support/proof_lane.ts.eex
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Run the primary offline-island proof against its Phoenix host</name>
  <reversibility rating="reversible">The command wraps the existing Playwright and Phoenix configuration without creating a new test system or CI lane.</reversibility>
  <read_first>
    - .planning/phases/159-host-reusable-proof-lane/159-VERIFICATION.md
    - examples/phoenix_host/playwright.config.ts
    - examples/phoenix_host/package.json
    - examples/phoenix_host/e2e/offline_sync.spec.ts
    - examples/phoenix_host/e2e/crosswake_proof_lane/browser_online_restore.spec.ts
  </read_first>
  <files>examples/phoenix_host/package.json, script/verify_phoenix_host_proof_lane.sh</files>
  <behavior>
    - "The command runs the focused TypeScript gate, then Playwright's primary `offline_sync.spec.ts` and proof-lane cleanup regression."
    - "Playwright uses its existing webServer command to prepare the test database and start Phoenix on port 4700."
    - "Any host-start, browser, UI, IndexedDB, backend, empty-outbox, or idempotency failure exits non-zero."
  </behavior>
  <action>Add a `proof:offline-island` package script that selects the existing primary offline spec plus `browser_online_restore.spec.ts`; do not replace the unfiltered `test` script or modify Playwright config/fixtures. Add a root-invokable shell wrapper that checks existing Node dependencies without installing them, runs `typecheck:offline-route-proof`, and invokes that package script from `examples/phoenix_host`. Let the existing Playwright `webServer` own database setup/server lifecycle and propagate every non-zero status. Keep logs ephemeral and do not create/upload a new report/evidence lane. This is the runnable automated control requested by the verifier, not a CI promotion.</action>
  <verify>
    <automated>bash script/verify_phoenix_host_proof_lane.sh</automated>
  </verify>
  <acceptance_criteria>
    - The command starts a fresh test Phoenix host when none is running and completes the real offline mutation/replay path.
    - The primary browser spec and its fixtures are unchanged and remain authoritative.
    - A deliberately unavailable host dependency or failing assertion produces a non-zero command outcome.
  </acceptance_criteria>
  <done>The behavior-unverified browser truth has a zero-human, one-command Phoenix-host execution.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Enforce one opaque mutation-ID shape before browser callbacks</name>
  <reversibility rating="reversible">The validation tightens a generated test helper around the already locked opaque-ID contract.</reversibility>
  <read_first>
    - .planning/phases/159-host-reusable-proof-lane/159-CONTEXT.md
    - examples/phoenix_host/e2e/support/offline_route_proof.ts
    - examples/phoenix_host/e2e/crosswake_proof_lane/browser_online_restore.spec.ts
    - priv/templates/crosswake/proof_lane/e2e/support/proof_lane.ts.eex
    - test/crosswake/proof_lane/template_contract_test.exs
  </read_first>
  <files>examples/phoenix_host/e2e/support/offline_route_proof.ts, examples/phoenix_host/e2e/crosswake_proof_lane/browser_online_restore.spec.ts, examples/phoenix_host/e2e/crosswake_proof_lane/support/proof_lane.ts, priv/templates/crosswake/proof_lane/e2e/support/proof_lane.ts.eex, test/crosswake/proof_lane/template_contract_test.exs</files>
  <behavior>
    - "Only a lowercase UUID-shaped opaque correlation reference (`8-4-4-4-12` hexadecimal groups) is returned by `extractMutationId`."
    - "Empty, arbitrary, uppercase, malformed-hyphen, path/URL, and identity-like strings fail with `PL-BROWSER-MUTATION-ID` before reconnect/backend/idempotency callbacks."
    - "The error does not contain the rejected value, queued record, field path, endpoint, or configuration."
  </behavior>
  <action>Centralize the same anchored lowercase UUID-shaped predicate in the repository helper and generated template, replacing the permissive string assertion and the repository's loose 36-character pattern. Throw one stable non-echoing error before the value reaches reconnect, backend confirmation, evidence, or duplicate callbacks. Add discriminating valid/invalid cases to the executable Playwright regression and update the byte-identical generated fixture. Extend template contract tests to require the anchored shape and stable rule while rejecting value interpolation. Preserve D-07's declarative field-path extraction and do not introduce a domain schema, evaluated callback, payload inspection, stable device/account identifier, or logging.</action>
  <verify>
    <automated>npm --prefix examples/phoenix_host run typecheck:offline-route-proof &amp;&amp; npm --prefix examples/phoenix_host test -- e2e/crosswake_proof_lane/browser_online_restore.spec.ts &amp;&amp; mix test test/crosswake/proof_lane/template_contract_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - Valid lowercase opaque IDs continue through confirmation, empty-outbox, and duplicate-idempotency assertions.
    - Every malformed class fails before downstream callbacks and no canary appears in the rendered failure.
    - Repository/template/fixture implementations remain byte-aligned and restore online state in `finally`.
  </acceptance_criteria>
  <done>PROOF-02 and PROOF-04 enforce an opaque, non-echoing browser mutation reference while PROOF-03 remains runnable.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| browser IndexedDB record → proof helper | Host-owned queued data contains an untrusted extracted correlation value. |
| Playwright runner → Phoenix test host | Browser automation crosses into application and backend state. |

## STRIDE Threat Register — ASVS L1, block on high

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- | --- |
| T-159-G10-01 | Information Disclosure / Linkability | mutation ID extraction | high | mitigate | Anchored opaque shape, pre-callback validation, stable non-echoing failures, and negative controls. |
| T-159-G10-02 | Repudiation | browser proof result | high | mitigate | Real Phoenix webServer plus UI/IndexedDB/backend/empty/idempotency assertions in one failing command. |
| T-159-G10-03 | Tampering | host corpus | medium | mitigate | Additive command/helper changes; existing spec, fixtures, config, and unfiltered runner stay primary. |
| T-159-G10-SC | Tampering | package supply chain | low | accept | No install task or dependency change; use the locked existing Playwright/TypeScript toolchain. |
</threat_model>

<verification>
Run the new one-command proof from a state with no Phoenix server already active, then run the focused TypeScript, Playwright, and ExUnit controls.
</verification>

<success_criteria>
- The primary browser/offline-island corpus runs automatically against Phoenix and proves real state transitions.
- Mutation IDs are opaque, closed-shape, and non-echoing across repository and generated helpers.
- Existing browser/unit/fixture ownership is preserved; no new CI lane or external service is added.
</success_criteria>

## Artifacts this phase produces

- New command: `bash script/verify_phoenix_host_proof_lane.sh`.
- New package script: `proof:offline-island`, selecting the existing authoritative specs.
- Strengthened generated/repository `extractMutationId` contract with stable `PL-BROWSER-MUTATION-ID` failure.
- No schema, dependency, endpoint, Android artifact, raw evidence, or `COVERAGE.md` change.

<output>
Create `.planning/phases/159-host-reusable-proof-lane/159-10-SUMMARY.md` when done.
</output>
