---
phase: 159-host-reusable-proof-lane
plan: "25"
type: execute
wave: 16
depends_on: ["159-24"]
files_modified:
  - lib/crosswake/proof_lane/generator_fs.ex
  - test/mix/tasks/crosswake_gen_proof_lane_test.exs
autonomous: true
gap_closure: true
requirements: [PROOF-01, PROOF-02]
must_haves:
  truths:
    - "Generate, check, diff, read, status, and regular probes execute only a helper compiled inside a newly created process-private restrictive directory; a predictable shared-temp winner is never selected or executed (D-02, D-06, D-19, D-20, D-21, D-22, PROOF-01)."
    - "Helper compilation, execution, and cleanup remain safe under concurrent generator calls while preserving missing-only, idempotent, host-owned destinations and the closed nine-key configuration contract (D-02, D-05, D-06, D-20, D-21, PROOF-01, PROOF-02)."
    - "A deterministic poisoned-cache regression pre-creates the former digest-derived shared path with executable canary behavior and proves every GeneratorFS action ignores it without executing or echoing it (D-02, D-09, D-19, D-22, PROOF-01)."
    - "No Android, browser, evidence schema, iOS UI, downstream replay/pack/device behavior, support claim, or TODO-002 state changes (D-01, D-03, D-04, D-07 through D-18, D-23)."
  artifacts:
    - path: lib/crosswake/proof_lane/generator_fs.ex
      provides: "Process-private restrictive helper build/execute lifecycle with guaranteed cleanup."
    - path: test/mix/tasks/crosswake_gen_proof_lane_test.exs
      provides: "Poisoned former-cache, action coverage, concurrency, idempotency, cleanup, and non-echo regressions."
  key_links:
    - from: Crosswake.ProofLane.GeneratorFS write/read/regular?/status
      to: priv/native/crosswake_proof_lane_fs.c
      via: "one freshly compiled executable inside the invocation-owned restrictive directory"
      pattern: "File.mkdir|File.chmod|System.cmd|Port.open|File.rm_rf"
    - from: Generator.generate/check/diff
      to: GeneratorFS helper lifecycle
      via: "all filesystem actions use the same private-build boundary and preserve host-owned bytes"
      pattern: "GeneratorFS.(write|read|regular?|status)"
  prohibitions:
    - statement: "No executable may be reused from a predictable path in a shared temporary directory."
      category: security
      verification: automated
    - statement: "No canary bytes, helper output, compiler output, absolute temporary path, payload, identity, credential, token, media, transcript, or stable device identifier may enter diagnostics or evidence."
      category: privacy
      verification: automated
    - statement: "No new package, persistent cache, Android surface, generic filesystem API, UI, or downstream phase behavior may be introduced."
      category: scope
      verification: artifact-inspection
flagged_assumptions:
  - "[RESOLVED-BY-TEST] The former shared cache path is attacker-writable input, never executable provenance; the poisoned-path test must fail on the pre-repair implementation."
  - "[UNRESOLVED-SPEC-PROBE] PROOF-01 concurrency has no plain phase SPEC classification; preserve it as an explicit assumption while running the existing and new concurrent-generator regressions."
  - "[UNRESOLVED-SPEC-PROBE] PROOF-02 idempotency and concurrency have no plain phase SPEC classification; do not infer broader guarantees beyond the closed config and observed generator tests."
  - "[VERIFIED] No package installation is needed; the repository-owned C source, existing compiler seam, OTP crypto, and Elixir filesystem APIs are sufficient."
---

<objective>
Close the shared-temp executable provenance gap by moving the generator helper into an invocation-owned restrictive build-and-execute boundary.

Purpose: Prevent a local shared-temp writer from substituting executable code into generate, check, diff, or read-only filesystem actions.
Output: Private helper lifecycle plus deterministic poisoned-cache, concurrency, idempotency, cleanup, and anti-echo regressions.

Decision fidelity: implement D-02, D-06, D-09, D-19, D-20, D-21, and D-22 for PROOF-01/PROOF-02; preserve D-01, D-03 through D-05, D-07 through D-18, and D-23. Deferred Phase 160 replay/auth, Phase 161 pack/audio, Phase 162 device evidence, Android work, generic sync/storage, dashboards, support promotion, and TODO-002 inputs remain excluded.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/ADR-FIRST-B2C-ADOPTER.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/159-host-reusable-proof-lane/159-CONTEXT.md
@.planning/phases/159-host-reusable-proof-lane/159-VERIFICATION.md
@.planning/phases/159-host-reusable-proof-lane/159-23-SUMMARY.md
@lib/crosswake/proof_lane/generator_fs.ex
@test/mix/tasks/crosswake_gen_proof_lane_test.exs
@priv/native/crosswake_proof_lane_fs.c
</context>

<assumption_delta_decision>
Primary noun: generator helper executable provenance.
Decision: promote invocation-owned private directory creation and exact freshly compiled file identity to the authority boundary; demote the old content-addressed shared pathname to poisoned legacy input that is ignored.
Rationale: source-content naming proves expected source bytes but does not prove who created the executable selected from a shared namespace.
</assumption_delta_decision>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Execute only an invocation-owned restrictive helper</name>
  <reversibility rating="reversible">The helper build/cache behavior is private behind unchanged GeneratorFS and Mix-task contracts.</reversibility>
  <read_first>
    - AGENTS.md
    - .planning/phases/159-host-reusable-proof-lane/159-CONTEXT.md
    - .planning/phases/159-host-reusable-proof-lane/159-VERIFICATION.md
    - .planning/phases/159-host-reusable-proof-lane/159-23-SUMMARY.md
    - lib/crosswake/proof_lane/generator_fs.ex
    - priv/native/crosswake_proof_lane_fs.c
    - test/mix/tasks/crosswake_gen_proof_lane_test.exs
  </read_first>
  <files>lib/crosswake/proof_lane/generator_fs.ex, test/mix/tasks/crosswake_gen_proof_lane_test.exs</files>
  <behavior>
    - "A pre-created executable at the former `crosswake-proof-lane-fs-&lt;source-digest&gt;` shared-temp pathname is not opened or executed by write, read, regular?, status, generate, check, or diff."
    - "Each helper lifecycle creates an unpredictable directory with exclusive creation, restricts it to the current user, compiles to a file inside that directory, verifies the expected regular-file type, executes that exact path, and removes only that owned directory in an `after` cleanup."
    - "Compiler failure, malformed helper state, callback failure, timeout, and cleanup failure stay fail-closed behind existing stable rule/path-only results and never echo compiler/helper output or absolute temporary paths."
    - "Concurrent generation remains deterministic, preserves one host-owned destination winner, and leaves neither predictable helper cache files nor invocation-owned helper directories after completion."
    - "Repeated generation remains idempotent: existing host bytes are reused, missing scaffold is filled, and check/diff remain read-only."
  </behavior>
  <action>First add a deterministic regression that derives the former public source-digest pathname, installs an executable poison file there whose only observable action is a test-owned canary side effect, and invokes every GeneratorFS action plus the generator lifecycle. Assert the canary action never occurs, the poison file remains untouched as untrusted external state until test teardown, errors and inspected results do not echo canary content or absolute paths, and the regression fails against the current shared-cache implementation. Add concurrent and repeated-run assertions that distinguish host-destination idempotency from helper reuse.

Refactor the private `build/1` plus callers into an owned lifecycle such as `with_helper/1`: generate an unpredictable directory name from cryptographically random bytes, create it exclusively beneath the system temporary parent, set restrictive permissions, verify with `File.lstat/1` that the owned leaf is a directory, and compile `priv/native/crosswake_proof_lane_fs.c` to a fixed child leaf inside it. Verify the compiled child is a regular file in the owned directory, restrict its mode, and pass that exact path directly to `System.cmd/3` or `Port.open/2`. Never consult, create, rename onto, or execute the former digest-derived shared path. Wrap both read and write invocations in `try/after` so only the invocation-owned directory is removed on success, error, callback abort, or timeout. Treat inability to prove creation, type, mode, compile success, or cleanup as unavailable/fail-closed through the existing sanitized result surface; discard compiler/helper output.

Keep the public GeneratorFS functions, bounded stdin frame, barrier tokens, destination semantics, and native helper API unchanged. Preserve D-02 missing-only host ownership, D-09/D-19 non-echoing diagnostics, D-20/D-21 read-only check/diff, and D-22 fail-closed behavior. Do not add a persistent cache, lock daemon, external package, generic filesystem abstraction, Android branch, or new public configuration.</action>
  <verify>
    <automated>mix test test/mix/tasks/crosswake_gen_proof_lane_test.exs &amp;&amp; cc -std=c11 -O2 -Wall -Wextra -Werror -o /tmp/crosswake-proof-lane-fs-159-25 priv/native/crosswake_proof_lane_fs.c &amp;&amp; mix format --check-formatted lib/crosswake/proof_lane/generator_fs.ex test/mix/tasks/crosswake_gen_proof_lane_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - The poisoned former cache is never executed by any GeneratorFS or generator action, and the regression is discriminating against the pre-repair implementation.
    - Every actual helper executable is freshly compiled and executed inside an exclusively created restrictive invocation-owned directory that is cleaned on every terminal path.
    - Existing concurrent-generator, descriptor-publication, missing-only, host-winner, callback, check, and diff contracts remain green without a persistent helper cache.
    - Failures remain stable, relative-path-only, and non-echoing; no new package or public contract is added.
  </acceptance_criteria>
  <done>GeneratorFS cannot execute a predictable shared-temp-file winner and retains automated concurrency/idempotency evidence without widening scope.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| shared temporary parent -> owned helper directory | Shared entries are untrusted; only an exclusively created restrictive directory establishes helper ownership. |
| repository C source -> compiler output | The compiler must write the exact executable child inside the owned directory; stdout/stderr are untrusted and non-retained. |
| helper executable -> GeneratorFS actions | Every read/write action executes only the freshly proven child path. |
| lifecycle cleanup -> host filesystem | Cleanup authority is limited to the invocation-owned directory and never reaches generated host destinations or external poison files. |

## STRIDE Threat Register — ASVS L1, block on high

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- | --- |
| T-159-G25-01 | Spoofing / Elevation of Privilege | helper selection | critical | mitigate | Exclusive unpredictable private directory, exact compiled child, regular-type/mode checks, and no shared-cache lookup. |
| T-159-G25-02 | Tampering | concurrent helper builds | high | mitigate | Per-invocation namespaces and deterministic concurrent regression with no shared executable winner. |
| T-159-G25-03 | Information Disclosure | compiler/helper diagnostics | high | mitigate | Discard raw output and expose only existing stable relative-path rules; anti-echo canary assertions. |
| T-159-G25-04 | Denial of Service | helper cleanup/timeout | medium | mitigate | `try/after` bounded lifecycle, timeout handling, owned-scope cleanup, and fail-closed result. |
| T-159-G25-SC | Tampering | package supply chain | low | accept | No install occurs; repository source and existing platform compiler are reused. |
</threat_model>

<verification>
Run the focused generator suite, warning-clean native compile, and targeted formatter. Require the poisoned-cache case, all action seams, concurrent and repeated generation, private-directory cleanup, descriptor publication, collision preservation, callback totality, and anti-echo assertions.
</verification>

<success_criteria>
- The former shared executable pathname is inert attacker-controlled input, never a code source.
- Helper ownership, type, permissions, exact execution path, and cleanup are proved for every action.
- Generator concurrency, idempotency, host ownership, and read-only check/diff remain intact.
- Android, UI, evidence, downstream phases, support truth, and TODO-002 do not change.
</success_criteria>

## Multi-Source Coverage Audit

| Source | ID | Feature / requirement | Plan | Status | Notes |
| --- | --- | --- | --- | --- | --- |
| GOAL | — | Safe configurable host-owned proof scaffolding | 159-25 | COVERED | Closes executable provenance for all generator actions. |
| REQ | PROOF-01 | Non-overwriting host-owned scaffold | 159-25 | COVERED | Private helper boundary plus poison/concurrency/idempotency regressions. |
| REQ | PROOF-02 | Closed configuration contract | 159-25 | COVERED-PRESERVED | Generator actions retain normalized nine-key input and rerun its focused contracts. |
| REQ | PROOF-03 | Primary browser corpus | 159-25 | PRESERVED | No browser files change; Plan 159-27 reruns executable browser proof. |
| REQ | PROOF-04 | Privacy-safe evidence | 159-25 | PRESERVED | Non-echoing generator diagnostics remain required; evidence repair is Plan 159-26. |
| RESEARCH | — | Existing repository C helper and no-new-dependency stack | 159-25 | COVERED | Uses the current compiler/native boundary without a package. |
| CONTEXT | D-02, D-06, D-09, D-19-D-22 | Host ownership, closed actions, non-echoing fail-closed lifecycle | 159-25 | COVERED | Exact decisions are cited in task behavior and action. |
| CONTEXT | D-01, D-03-D-05, D-07-D-18, D-23 | Additive browser/iOS/evidence/time-box boundaries | 159-25 | PRESERVED | No deferred or forbidden surface changes. |
| UI | — | Existing overflow contract and accessibility-size backstop | 159-25 | PRESERVED | No UI artifacts change; Plan 159-27 reruns deterministic contracts. |
| API | — | No external API integration | 159-25 | PRESERVED | Local compiler/helper work only; existing `COVERAGE.md` remains unchanged. |

Spec-less unresolved probes remain explicit: PROOF-01 concurrency and PROOF-02 idempotency/concurrency are exercised but not silently classified as broader guarantees. Deferred Phase 160-162 behavior, Android work, generic sync/storage, dashboards, support promotion, and TODO-002 inputs remain excluded.

## Artifacts this phase produces

- Modified private helper lifecycle in `lib/crosswake/proof_lane/generator_fs.ex`.
- Modified focused regressions in `test/mix/tasks/crosswake_gen_proof_lane_test.exs`.
- No newly created symbol/path, dependency, schema, generated host path, UI, Android artifact, external API, or support claim.

<output>
Create `.planning/phases/159-host-reusable-proof-lane/159-25-SUMMARY.md` when done.
</output>
