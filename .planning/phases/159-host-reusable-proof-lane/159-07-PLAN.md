---
phase: 159-host-reusable-proof-lane
plan: "07"
type: execute
wave: 3
depends_on: [159-04]
files_modified:
  - lib/crosswake/proof_lane/evidence.ex
  - lib/crosswake/proof_lane/native_promotion.ex
  - priv/native/crosswake_evidence_promote.c
  - priv/templates/crosswake/proof_lane/test/crosswake_proof_lane_test.exs.eex
  - test/crosswake/proof_lane/evidence_test.exs
autonomous: true
gap_closure: true
requirements: [PROOF-04]
must_haves:
  truths:
    - "Per D-16 and D-19, retained revision and assertion references use closed opaque forms; identity-like caller strings are rejected without echoing them (PROOF-04)."
    - "Per D-18, caller-provided digest strings are never trusted: each retained SHA-256 value is computed from an allowlisted kind's canonical sanitized bytes, and checking a non-empty hash set requires those same approved sources (PROOF-04)."
    - "Per D-15 and D-22, evidence is fully serialized, recursively scanned, and promoted through an atomic no-replace operation; a destination created after staging is preserved byte-for-byte and the promoter returns `PL-EVIDENCE-COLLISION` (PROOF-04)."
    - "Interrupted and parallel promoters leave exactly one complete canonical winner, clean only their own stage/claim artifacts, and never expose a partial retained artifact (PROOF-04)."
  artifacts:
    - path: "lib/crosswake/proof_lane/evidence.ex"
      provides: "Closed retained identifiers, source-bound hashes, and atomic no-replace evidence promotion"
    - path: "lib/crosswake/proof_lane/native_promotion.ex"
      provides: "Argument-safe, fail-closed build and port invocation boundary for one evidence-directory rename"
    - path: "priv/native/crosswake_evidence_promote.c"
      provides: "Packaged Linux renameat2 and Darwin renameatx_np no-replace directory primitive"
    - path: "test/crosswake/proof_lane/evidence_test.exs"
      provides: "Identity-like ID, arbitrary digest, source binding, injected race, interruption, and concurrency regressions"
    - path: "priv/templates/crosswake/proof_lane/test/crosswake_proof_lane_test.exs.eex"
      provides: "Generated host contract using only closed assertion/revision forms"
  key_links:
    - from: "Evidence.build/1"
      to: "closed revision/assertion vocabularies"
      via: "field-specific validation rather than the former shared open identifier regex"
      pattern: "commit_ref|assertion_ids"
    - from: "approved canonical sources"
      to: "Evidence.to_map/1 approved_hashes"
      via: "kind-specific canonical validation followed by internal SHA-256 computation"
      pattern: "approved_hash|canonical_bytes"
    - from: "Evidence.promote/2"
      to: "retained destination"
      via: "final scan plus NativePromotion argv invocation of a one-purpose OS no-replace helper"
      pattern: "renameat2|renameatx_np|PL-EVIDENCE-COLLISION"
  prohibitions:
    - "MUST NOT overwrite, merge, reorganize, or replace an adopter's existing specs, fixtures, router, application code, Playwright configuration, or edited generated files."
    - "MUST NOT turn a missing replay/auth, pack/audio, signing, simulator, or device prerequisite into a passing proof claim."
    - "MUST NOT retain or echo raw answers, mutation payloads, identity/account/customer data, credentials, tokens, transcripts, media, endpoints, stable device identifiers, screenshots, traces, logs, raw output, or hashes of unapproved bytes."
    - "MUST NOT add Android work, generic sync/storage/test orchestration, an Xcode project manager, a device service, an evidence dashboard, or later-phase support claims."
---

<objective>
Bind every retained identifier and digest to a closed privacy-safe contract and close the final evidence-promotion race.

Purpose: Prevent identity-shaped labels, sensitive-data fingerprints, or concurrent destination replacement from crossing the retained proof boundary.
Output: Closed identifier vocabularies, canonical-source-derived hashes, atomic no-replace promotion, and exhaustive non-echoing regressions.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/ADR-FIRST-B2C-ADOPTER.md
@.planning/FIRST-B2C-ADOPTER-ROUTE-POLICY-MAP.md
@.planning/phases/159-host-reusable-proof-lane/159-CONTEXT.md
@.planning/phases/159-host-reusable-proof-lane/159-VERIFICATION.md
@.planning/phases/159-host-reusable-proof-lane/159-04-SUMMARY.md
@lib/crosswake/proof_lane/evidence.ex
@lib/crosswake/proof_lane/native_promotion.ex
@priv/native/crosswake_evidence_promote.c
@priv/templates/crosswake/proof_lane/test/crosswake_proof_lane_test.exs.eex
@test/crosswake/proof_lane/evidence_test.exs
</context>

<assumption_delta_decision>
Decision: no-change.
Rationale: The detector phrase “instead of inventing a second test system” is a negated false positive; this plan narrows the existing retained-evidence contract and does not change an identity model.
</assumption_delta_decision>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Build one retained record from closed IDs and canonical approved bytes</name>
  <reversibility rating="costly">D-16 is a versioned retained artifact contract; tightening accepted identifiers and hash inputs requires coordinated host-template updates but no stored-data migration exists yet.</reversibility>
  <read_first>
    - AGENTS.md
    - .planning/phases/159-host-reusable-proof-lane/159-CONTEXT.md
    - .planning/phases/159-host-reusable-proof-lane/159-VERIFICATION.md
    - .planning/phases/159-host-reusable-proof-lane/159-04-SUMMARY.md
    - lib/crosswake/proof_lane/evidence.ex
    - priv/templates/crosswake/proof_lane/test/crosswake_proof_lane_test.exs.eex
    - test/crosswake/proof_lane/evidence_test.exs
  </read_first>
  <files>lib/crosswake/proof_lane/evidence.ex, priv/templates/crosswake/proof_lane/test/crosswake_proof_lane_test.exs.eex, test/crosswake/proof_lane/evidence_test.exs</files>
  <behavior>
    - "`schema_version` and `template_version` are bounded decimal versions; `crosswake_version` is a bounded semantic version; `commit_ref` is `git-` plus a full 40- or 64-character lowercase hexadecimal object ID."
    - "Assertion IDs are selected only from `browser_offline_island`, `shell_boot`, `auth_continuity`, `relaunch_persistence`, `replay_prerequisite`, and `pack_audio_prerequisite`, with a non-empty duplicate-free bounded list."
    - "`alice_123`, `unreviewed_identifier`, host labels, arbitrary strings matching the former generic regex, and non-full Git refs fail with stable non-echoing identifier rules."
    - "External build input supplies `%{kind: approved_kind, canonical_bytes: bytes}`; `Evidence.build/1` validates the kind-specific canonical sanitized shape, computes SHA-256 internally, and stores only `%{kind, digest}`. A caller-supplied `digest` key is rejected."
    - "A retained non-empty hash list can be checked only when the verifier supplies matching approved canonical sources; a missing or mismatched source fails closed."
  </behavior>
  <action>Begin with the verifier's exact identity-like `commit_ref`, arbitrary `assertion_ids`, and caller-selected 64-hex digest controls. Split `valid_versions/1` into field-specific closed validators: bounded numeric schema/template versions, bounded semantic Crosswake version, and a full opaque Git object reference with a fixed `git-` prefix. Replace the open assertion regex with the exact Phase 159 assertion allowlist named in the behavior and reject duplicates. Preserve the twelve retained JSON keys per D-16. Change the external `approved_hashes` build input so entries contain only an approved kind plus `canonical_bytes`; reject any caller `digest`, validate the bytes with the kind-specific canonical decoder/scanner, compute SHA-256 inside Evidence, and serialize only kind/digest. Add source-aware final verification used by promotion and by a new `check/2`; keep `check/1` valid for empty hashes and fail with `PL-EVIDENCE-HASH-SOURCE` for non-empty hashes that cannot be independently verified. Update the generated host ExUnit template to use a full synthetic Git ref and the closed prerequisite assertion IDs. Keep all errors rule/path/remediation-only per D-19, including nested canaries.</action>
  <verify>
    <automated>mix test test/crosswake/proof_lane/evidence_test.exs &amp;&amp; mix format --check-formatted lib/crosswake/proof_lane/evidence.ex priv/templates/crosswake/proof_lane/test/crosswake_proof_lane_test.exs.eex test/crosswake/proof_lane/evidence_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - The verifier's `alice_123`, `unreviewed_identifier`, and arbitrary 64-hex digest input all fail without appearing in errors.
    - Every retained non-empty digest equals a recomputation from the supplied allowlisted canonical bytes.
    - Missing, wrong-kind, modified, or sensitive canonical sources fail; empty approved hashes remain valid.
    - The generated host evidence test renders and passes with the closed revision/assertion forms.
  </acceptance_criteria>
  <done>PROOF-04 retains only closed opaque references and digests that are demonstrably derived from approved canonical sanitized bytes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Promote scanned evidence with atomic no-replace collision semantics</name>
  <reversibility rating="reversible">The promotion helper is internal and preserves the destination/artifact format while strengthening its collision primitive.</reversibility>
  <read_first>
    - .planning/phases/159-host-reusable-proof-lane/159-CONTEXT.md
    - .planning/phases/159-host-reusable-proof-lane/159-VERIFICATION.md
    - .planning/phases/159-host-reusable-proof-lane/159-04-SUMMARY.md
    - lib/crosswake/proof_lane/evidence.ex
    - mix.exs
    - test/crosswake/proof_lane/evidence_test.exs
  </read_first>
  <files>lib/crosswake/proof_lane/evidence.ex, lib/crosswake/proof_lane/native_promotion.ex, priv/native/crosswake_evidence_promote.c, test/crosswake/proof_lane/evidence_test.exs</files>
  <behavior>
    - "Promotion no longer performs an authoritative `absent(destination)` check followed by replacement-capable `File.rename/2`."
    - "On Linux, the packaged helper invokes `renameat2(AT_FDCWD, source, AT_FDCWD, destination, RENAME_NOREPLACE)`; on Darwin it invokes `renameatx_np(source, destination, RENAME_EXCL)`. Both calls operate on the sibling staged directory and succeed only if the destination name is unclaimed."
    - "The helper build, OS selection, compiler lookup, kernel/filesystem unsupported status, malformed invocation, and unexpected native failure all fail closed without falling back to `File.rename/2` or an absence check."
    - "A test hook that creates an empty destination after scanning but before promotion retains that exact destination and returns `PL-EVIDENCE-COLLISION`."
    - "Two parallel promoters yield one complete canonical winner and one stable loser; interruption leaves no promoted destination and cleans only the operation's unique stage/claim."
  </behavior>
  <action>Remove the pre-stage `absent/1` decision as the collision authority. Add the narrowly named internal module `Crosswake.ProofLane.NativePromotion` and packaged source `priv/native/crosswake_evidence_promote.c`; do not introduce a NIF, dependency, general filesystem API, or reusable command runner per D-23. `NativePromotion.rename_noreplace/2` supports only `{:unix, :linux}` and `{:unix, :darwin}`; an internal `/3` form accepts only `os_type:` and `compiler:` seams for deterministic capability tests, while Evidence always calls `/2`. For each call, accept `CC` only when the entire value matches `\A[A-Za-z0-9_+.\-/]+\z` with no whitespace or appended flags, otherwise use the literal `cc`, and resolve that single name with `System.find_executable/1`. Compile the packaged source to a `:crypto.strong_rand_bytes/1`-named operation-owned temporary executable with `System.cmd(compiler, ["-std=c11", "-O2", "-Wall", "-Wextra", "-Werror", "-o", executable, source])`; invoke it as `System.cmd(executable, [stage, destination])`, retain no native output, map closed exit 0 to success, 10 to collision, 20 to unavailable kernel/filesystem capability, and every other status to a stable promotion failure, then remove only that executable in `after`. Missing compiler/source, compile failure, unsupported OS, `ENOSYS`, `ENOTSUP`/`EOPNOTSUPP`, and unsupported-flag `EINVAL` return `PL-EVIDENCE-PROMOTION-UNAVAILABLE` with safe fixed path/remediation only. The C program accepts exactly two path arguments, verifies the source is a directory, performs no destination preflight, and calls `renameat2(AT_FDCWD, source, AT_FDCWD, destination, RENAME_NOREPLACE)` through the Linux syscall boundary or `renameatx_np(source, destination, RENAME_EXCL)` on Darwin. It maps `EEXIST`/`ENOTEMPTY` to 10 without touching the winner; it never calls ordinary `rename`. Keep D-15's build → unique sibling stage → recursive final scan → atomic promotion ordering and D-22 own-stage cleanup. Add a test callback immediately before `NativePromotion.rename_noreplace/2` so a regression can create a sentinel-bearing destination in the former race window; assert that destination remains byte-identical and the stage is removed. Exercise the real compiled kernel helper on the current supported OS for a successful directory promotion, an occupied destination, and two simultaneous contenders; the repository's existing Linux full-test lanes execute the same test while the Phase 159 developer run covers Darwin. Use the `/3` seams for unsupported-platform, missing-compiler, and compile-failure cases, add an injected interruption immediately before promotion, and verify the winner through Task 1's source-aware scanner. Do not delete, enter, or write inside a destination this operation did not atomically create.</action>
  <verify>
    <automated>mix test test/crosswake/proof_lane/evidence_test.exs &amp;&amp; mix format --check-formatted lib/crosswake/proof_lane/evidence.ex lib/crosswake/proof_lane/native_promotion.ex test/crosswake/proof_lane/evidence_test.exs &amp;&amp; test -s priv/native/crosswake_evidence_promote.c</automated>
  </verify>
  <acceptance_criteria>
    - The injected post-scan destination race never replaces or modifies the concurrent destination.
    - Parallel promotion has exactly one verified winner and a non-echoing `PL-EVIDENCE-COLLISION` loser.
    - The native regression executes Darwin `RENAME_EXCL` on macOS and Linux `RENAME_NOREPLACE` in existing Linux full-test CI; either platform fails closed when its compiler or kernel/filesystem capability is unavailable.
    - No code path uses replacement-capable rename as a fallback after checking destination absence.
    - Interrupted, unsupported-platform, scan-failure, and collision paths leave no partial retained artifact or orphaned operation-owned stage/claim.
  </acceptance_criteria>
  <done>Evidence crosses the durable boundary only through final scanning and a genuine atomic no-replace promotion.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| Caller map → retained identifiers | Caller-controlled labels could encode identity or stable correlation data. |
| Canonical approved bytes → retained digest | Hashing can preserve a sensitive fingerprint unless source kind and bytes are validated. |
| Scanned stage → retained destination | A concurrent filesystem writer can claim the final name during promotion. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- | --- |
| T-159-G07-01 | Information Disclosure / Linkability | `commit_ref` and `assertion_ids` | high | mitigate | Full opaque Git refs, exact assertion allowlist, duplicate rejection, and identity-like non-echo controls. |
| T-159-G07-02 | Information Disclosure / Linkability | `approved_hashes` | high | mitigate | Reject supplied digests; validate allowlisted canonical source bytes, compute internally, and require matching sources for final verification. |
| T-159-G07-03 | Tampering | `Evidence.promote/2` destination | high | mitigate | Repository-owned argv-invoked helper calls Linux `renameat2(RENAME_NOREPLACE)` or Darwin `renameatx_np(RENAME_EXCL)`; no ordinary-rename fallback, plus injected and concurrent winner-preservation tests. |
| T-159-G07-04 | Denial of Service | native helper build/invocation | medium | mitigate | Exact compiler executable, fixed argv, closed exit map, operation-owned cleanup, stable fail-closed unavailable error, and no native output propagation. |
</threat_model>

<verification>
- Run the complete evidence suite after each task and from the final tree.
- Run the generated host evidence contract through the existing generator suite if the template changes affect rendering.
- Run formatter over all touched Elixir/template files.
</verification>

<success_criteria>
- PROOF-04 rejects identity-shaped retained labels and unbound caller digests.
- Every retained digest is reproducible from an approved canonical sanitized source.
- Promotion never replaces a destination created in the former TOCTOU window.
- TODO-002 remains `unknown_blocking`; no device, replay/auth, pack/audio, Android, or broader support claim changes.
</success_criteria>

## Specless prohibition-probe audit

Seven candidates were recalled: host-corpus replacement, false native passing claims, retained
sensitive or identifying data, framework/platform scope expansion, path traversal, command/config
injection, and dependency tampering. The first four values/privacy/transparency constraints are
kept exactly as descriptor-less `must_haves.prohibitions` strings in all three gap plans. The last
three are canonical engineering/security threats covered by the ASVS-L1 threat models and the
existing no-install research finding. Equality holds: 7 recalled = 4 kept + 3 canon-dropped.

## Multi-Source Coverage Audit

| Source | ID | Feature / requirement | Gap plan | Status | Notes |
| --- | --- | --- | --- | --- | --- |
| GOAL | — | Configurable host-owned browser, shell, offline-island, and device proof scaffold | 159-05, 159-06, 159-07 | COVERED | Confinement, executable proof outcomes, and retained evidence are closed without widening scope. |
| REQ | PROOF-01 | Non-overwriting ExUnit, Playwright, shell, and device scaffold | 159-05, 159-06 | COVERED | Safe roots plus non-passing target availability. |
| REQ | PROOF-02 | Exact route/storage/mutation/endpoint/router/shell config | 159-05 | COVERED | Exact normalized `native/ios` contract and rerun/concurrency checks. |
| REQ | PROOF-03 | Preserve and run the primary browser/unit/fixture corpus | 159-06 | COVERED | Mutation ID, zero-error focused typecheck, unchanged Playwright control, online cleanup. |
| REQ | PROOF-04 | Reject sensitive/identifying evidence | 159-07 | COVERED | Closed IDs, source-bound hashes, no-replace promotion. |
| RESEARCH | — | No-new-dependency generator-plus-library stack | 159-05 through 159-07 | COVERED | Existing Elixir/OTP, TypeScript/Playwright, Bash, and OS primitives only. |
| RESEARCH | — | Normalize once, preserve host ownership, boundary-specific tests, validate-stage-scan-promote | 159-05, 159-06, 159-07 | COVERED | Each failed link is repaired at its current owning tier. |
| CONTEXT | D-01-D-09 | Additive host ownership and one closed config | 159-05, 159-06 | COVERED | No host corpus reorganization; root normalization is fail closed. |
| CONTEXT | D-10-D-14 | Honest executable XCTest/XCUITest and advisory native posture | 159-06 | COVERED | Unavailable/build-blocked is non-zero and machine-readable. |
| CONTEXT | D-15-D-19 | Narrow allowlist, safe identifiers and approved-byte hashes | 159-07 | COVERED | No arbitrary labels or caller digests cross retention. |
| CONTEXT | D-20-D-22 | Read-only checks and atomic fail-closed lifecycle | 159-05, 159-06, 159-07 | COVERED | Idempotency, cleanup, exit semantics, and no-replace promotion are executable. |
| CONTEXT | D-23 | Three-day stop; no generalized test/device system | 159-05 through 159-07 | COVERED | Focused repairs only; no new framework or service. |

Deferred Phase 160 replay/auth behavior, Phase 161 pack/audio behavior, Phase 162 physical-device
evidence, Android work, generic sync/storage/test orchestration, raw native artifacts, dashboards,
and support promotion remain excluded. TODO-002 remains `unknown_blocking`. No source item is
missing from this gap-closure set.

## Artifacts this phase produces

- New public function: `Evidence.check/2` for retained hashes verified against approved canonical sources.
- New internal boundary: `Crosswake.ProofLane.NativePromotion.rename_noreplace/2` with closed build/invocation outcomes.
- New file: `priv/native/crosswake_evidence_promote.c`, packaged through the existing `priv` Hex allowlist and limited to Linux/Darwin evidence-directory promotion.
- New file: `lib/crosswake/proof_lane/native_promotion.ex`.
- Modified artifacts: `evidence.ex`, its focused tests, and the generated host ExUnit contract template.
- No new schema field, endpoint, external API integration, package, ORM file, Android file, support taxonomy, or device claim.

<output>
Create `.planning/phases/159-host-reusable-proof-lane/159-07-SUMMARY.md` when done.
</output>
