---
phase: 159-host-reusable-proof-lane
plan: "05"
type: execute
wave: 3
depends_on: [159-02]
files_modified:
  - lib/crosswake/proof_lane/config.ex
  - lib/crosswake/proof_lane/generator.ex
  - test/crosswake/proof_lane/config_test.exs
  - test/mix/tasks/crosswake_gen_proof_lane_test.exs
autonomous: true
gap_closure: true
requirements: [PROOF-01, PROOF-02]
must_haves:
  truths:
    - "Per D-06, `ios_shell_root` is a normalized absolute path whose final components are exactly `native/ios`, and its derived host root is neither `/` nor outside that declared layout (PROOF-02)."
    - "Per D-01 and D-02, generate, check, and diff derive every destination from the validated host root and fail closed before filesystem access when a caller supplies an unsafe `%Config{}` directly (PROOF-01)."
    - "Rerunning the same normalized configuration remains idempotent: only missing scaffold is created and all existing host-owned bytes remain unchanged (PROOF-01, PROOF-02)."
    - "Interrupted and parallel generation preserve existing host files, cannot publish an incomplete manifest, and report unsafe-root or destination collisions as non-echoing failures (PROOF-01, PROOF-02)."
  artifacts:
    - path: "lib/crosswake/proof_lane/config.ex"
      provides: "Canonical iOS shell-root suffix validation and safe host-root derivation"
    - path: "lib/crosswake/proof_lane/generator.ex"
      provides: "Defense-in-depth refusal of unsafe direct Config structs before destination derivation"
    - path: "test/crosswake/proof_lane/config_test.exs"
      provides: "Unsafe-root normalization regressions"
    - path: "test/mix/tasks/crosswake_gen_proof_lane_test.exs"
      provides: "Direct-struct confinement plus rerun/interruption/concurrency regressions"
  key_links:
    - from: "Crosswake.ProofLane.Config.normalize/1"
      to: "validated `ios_shell_root` and derived host root"
      via: "exact final path components plus non-root derivation"
      pattern: "native.*ios|host_root"
    - from: "Crosswake.ProofLane.Generator.generate/1, check/1, diff/1"
      to: "Crosswake.ProofLane.Config"
      via: "all actions revalidate or use the checked host-root helper before `desired/1`"
      pattern: "Config\.(normalize|host_root)"
  prohibitions:
    - "MUST NOT overwrite, merge, reorganize, or replace an adopter's existing specs, fixtures, router, application code, Playwright configuration, or edited generated files."
    - "MUST NOT turn a missing replay/auth, pack/audio, signing, simulator, or device prerequisite into a passing proof claim."
    - "MUST NOT retain or echo raw answers, mutation payloads, identity/account/customer data, credentials, tokens, transcripts, media, endpoints, stable device identifiers, screenshots, traces, logs, raw output, or hashes of unapproved bytes."
    - "MUST NOT add Android work, generic sync/storage/test orchestration, an Xcode project manager, a device service, an evidence dashboard, or later-phase support claims."
---

<objective>
Close the proof-lane filesystem-confinement gap from Phoenix config through every generator action.

Purpose: Prevent an accepted or directly constructed iOS shell root from deriving `/` or routing host-owned scaffold outside the intended Phoenix host.
Output: A canonical `native/ios` root contract, defense-in-depth generator validation, and adversarial idempotency/concurrency coverage.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/159-host-reusable-proof-lane/159-CONTEXT.md
@.planning/phases/159-host-reusable-proof-lane/159-VERIFICATION.md
@.planning/phases/159-host-reusable-proof-lane/159-02-SUMMARY.md
@lib/crosswake/proof_lane/config.ex
@lib/crosswake/proof_lane/generator.ex
@test/crosswake/proof_lane/config_test.exs
@test/mix/tasks/crosswake_gen_proof_lane_test.exs
</context>

<assumption_delta_decision>
Decision: no-change.
Rationale: The detector phrase “instead of inventing a second test system” is a negated false positive; the singular host-owned proof lane and its identity model are unchanged.
</assumption_delta_decision>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Reject an unsafe iOS shell root before it can escape the host</name>
  <reversibility rating="reversible">This tightens one internal validator around the already locked D-06 path contract; reverting changes only local validation and tests.</reversibility>
  <read_first>
    - .planning/phases/159-host-reusable-proof-lane/159-CONTEXT.md
    - .planning/phases/159-host-reusable-proof-lane/159-VERIFICATION.md
    - .planning/phases/159-host-reusable-proof-lane/159-02-SUMMARY.md
    - lib/crosswake/proof_lane/config.ex
    - lib/crosswake/proof_lane/generator.ex
    - test/crosswake/proof_lane/config_test.exs
  </read_first>
  <files>lib/crosswake/proof_lane/config.ex, test/crosswake/proof_lane/config_test.exs</files>
  <behavior>
    - "A normalized absolute path ending in the exact components `native/ios` is accepted when removing those components yields a non-root host directory."
    - "`/tmp/not-the-native-ios-root`, `/native/ios`, extra descendants after `ios`, lookalike component names, traversal, repeated separators, trailing non-normal form, and relative paths are rejected with `PL-CONFIG-VALUE` on `ios_shell_root`."
    - "Errors retain only stable rule/key/remediation and never echo the rejected path."
  </behavior>
  <action>First add table-driven RED cases reproducing the verifier's arbitrary-root input and the derived-root `/` case. Tighten D-06 in `Config.normalize/1`: require `Path.expand(value) == value`, require the final two `Path.split/1` components to be exactly `native` and `ios`, derive the host root by removing exactly those two components, and reject an empty or filesystem-root result. Centralize that derivation in a checked `Config.host_root/1` interface returning `{:ok, host_root} | {:error, %Config.Error{}}`; do not leave separate suffix logic in Generator. Keep the existing exact nine-key schema and non-echoing `PL-CONFIG-*` contract unchanged.</action>
  <verify>
    <automated>mix test test/crosswake/proof_lane/config_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - The exact verifier input `/tmp/not-the-native-ios-root` is rejected.
    - `/native/ios` is rejected because it derives `/`; a temporary host root ending in `native/ios` is accepted and returns that temporary host from `Config.host_root/1`.
    - Every unsafe-root failure contains only the stable rule, `ios_shell_root`, and remediation.
  </acceptance_criteria>
  <done>PROOF-02 can no longer normalize an arbitrary absolute shell path or derive the filesystem root.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Enforce confinement for generate, check, and diff under direct-struct and concurrent use</name>
  <reversibility rating="reversible">The generator keeps its public actions and desired-state layout; it only fails earlier on an invalid internal struct.</reversibility>
  <read_first>
    - .planning/phases/159-host-reusable-proof-lane/159-CONTEXT.md
    - .planning/phases/159-host-reusable-proof-lane/159-VERIFICATION.md
    - lib/crosswake/proof_lane/config.ex
    - lib/crosswake/proof_lane/generator.ex
    - test/mix/tasks/crosswake_gen_proof_lane_test.exs
  </read_first>
  <files>lib/crosswake/proof_lane/generator.ex, test/mix/tasks/crosswake_gen_proof_lane_test.exs</files>
  <behavior>
    - "A manually constructed `%Config{ios_shell_root: unsafe}` raises the non-echoing `Config.Error` from generate/check/diff before `desired/1`, directory creation, file reads, or writes."
    - "All iOS destinations remain under the exact validated `ios_shell_root`; ExUnit, Playwright, and manifest destinations remain under its derived host root."
    - "Same-config reruns, injected interruption, and two parallel generators preserve the previously proven missing-only and manifest guarantees."
  </behavior>
  <action>Replace the unchecked two-`Path.dirname/1` `host_root/1` implementation with the checked Config interface from Task 1 and make `generate/1`, `check/1`, and `diff/1` call it before `desired/1`. Preserve all three successful return shapes; if a caller bypasses `normalize/1` with an invalid direct struct, raise the returned `%Config.Error{}` without inspecting or embedding the path. Add a temporary-root regression that constructs the verifier's unsafe `%Config{}` directly, asserts that same stable exception for all three actions, and asserts recursive before/after snapshots are identical. Retain and rerun the existing partial-tree, byte-preserving rerun, interrupted-manifest, and parallel-generator cases so the PROOF-01/PROOF-02 spec-less idempotency and concurrency guarantees remain executable.</action>
  <verify>
    <automated>mix test test/mix/tasks/crosswake_gen_proof_lane_test.exs test/crosswake/proof_lane/config_test.exs &amp;&amp; mix format --check-formatted lib/crosswake/proof_lane/config.ex lib/crosswake/proof_lane/generator.ex test/crosswake/proof_lane/config_test.exs test/mix/tasks/crosswake_gen_proof_lane_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - No unsafe direct Config action creates `/e2e`, `/test`, `/.crosswake`, or any path outside its temporary test root.
    - Valid reruns create only missing files and preserve every edited host byte.
    - Interrupted and parallel runs retain one complete manifest outcome and never overwrite a host destination.
  </acceptance_criteria>
  <done>All generator actions share one fail-closed root derivation while retaining non-destructive and concurrency-safe behavior.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| Phoenix config or direct `%Config{}` → path derivation | Untrusted absolute paths select host and native destinations. |
| Derived desired state → host filesystem | Missing-only writes and concurrent generators touch permanently host-owned paths. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- | --- |
| T-159-G05-01 | Tampering / Elevation of Privilege | `Config.host_root/1` and `Generator` destinations | high | mitigate | Exact normalized `native/ios` suffix, non-root derivation, direct-struct revalidation, and recursive no-write controls. |
| T-159-G05-02 | Tampering | parallel/interrupted generation | high | mitigate | Preserve exclusive missing-only files, staged manifest promotion, destination containment, and concurrency/interruption tests. |
| T-159-G05-03 | Information Disclosure | config errors | medium | mitigate | Stable rule plus canonical key/remediation only; injected paths are absent from rendered errors. |
| T-159-G05-SC | Tampering | dependency supply chain | low | accept | No dependency install or external integration is introduced. |
</threat_model>

<verification>
- Run the focused config and generator suites from the final tree.
- Run `mix format --check-formatted` over all four touched Elixir files.
- Confirm the unsafe direct-struct test snapshots a temporary parent recursively and observes no filesystem mutation.
</verification>

<success_criteria>
- PROOF-01 and PROOF-02 fail closed for arbitrary or root-deriving iOS paths.
- Valid normalized configuration remains idempotent under rerun, interruption, and parallel generation.
- No Android, later-phase behavior, support label, dependency, external API, or schema surface is added.
</success_criteria>

## Artifacts this phase produces

- New symbol: `Crosswake.ProofLane.Config.host_root/1`, the sole checked derivation from `<host>/native/ios` to `<host>`.
- New files: None.
- Modified artifacts: `config.ex`, `generator.ex`, and their two existing focused test files.

<output>
Create `.planning/phases/159-host-reusable-proof-lane/159-05-SUMMARY.md` when done.
</output>
