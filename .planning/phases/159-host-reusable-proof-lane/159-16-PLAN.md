---
phase: 159-host-reusable-proof-lane
plan: "16"
type: execute
wave: 9
depends_on: [159-14]
files_modified:
  - lib/crosswake/proof_lane/config.ex
  - lib/crosswake/proof_lane/generator.ex
  - test/crosswake/proof_lane/config_test.exs
  - test/mix/tasks/crosswake_gen_proof_lane_test.exs
autonomous: true
gap_closure: true
requirements: [PROOF-01, PROOF-02]
must_haves:
  truths:
    - "Config.normalize/1 rejects double-quote and backslash characters in sync_path and evidence_path with stable non-echoing PL-CONFIG-VALUE errors before any generator filesystem action (D-06, D-08, D-09, PROOF-02)."
    - "Direct Config structs, application configuration, and selected --config input all reach the same normalization boundary; unsafe endpoint values create no output root or partial generated file (D-05, D-06, D-09, PROOF-01, PROOF-02)."
    - "A valid generated browser helper still passes the repository's TypeScript check after the closed endpoint grammar is tightened, preserving the existing browser corpus and template surface (D-03, D-09, PROOF-03)."
  artifacts:
    - path: "lib/crosswake/proof_lane/config.ex"
      provides: "Closed host-local endpoint grammar that excludes TypeScript-literal-breaking quote and backslash input"
    - path: "lib/crosswake/proof_lane/generator.ex"
      provides: "Full direct-Config revalidation before destination derivation or filesystem authority"
    - path: "test/crosswake/proof_lane/config_test.exs"
      provides: "Non-echoing normalization regressions for both endpoint keys and both unsafe character classes"
    - path: "test/mix/tasks/crosswake_gen_proof_lane_test.exs"
      provides: "Pre-write generator regressions proving unsafe endpoints leave no output"
  key_links:
    - from: "Config.normalize/1"
      to: "Generator.generate/1 and every rendered language surface"
      via: "one closed host_local_path? predicate used before host-root or destination derivation"
      pattern: "host_local_path\?|sync_path|evidence_path"
    - from: "safe normalized Config"
      to: "proof_lane.ts.eex"
      via: "existing canonical EEx assigns and generated TypeScript typecheck"
      pattern: "proofLaneConfig|syncPath|evidencePath"
  prohibitions:
    - statement: "MUST NOT allow quote or backslash endpoint input to reach EEx rendering, destination creation, manifest staging, or host-owned files."
      category: input-validation
      verification: flagged-unverified
    - statement: "MUST NOT echo a rejected endpoint value in exceptions, Mix output, diffs, manifests, tests, or retained evidence."
      category: privacy
      verification: flagged-unverified
    - statement: "MUST NOT add a second config representation, encoder dependency, endpoint URL mode, credential-bearing endpoint, schema migration, or external API integration."
      category: architecture
      verification: flagged-unverified
    - statement: "MUST NOT change Android, browser corpus ownership, evidence schema, UI breadth, generic sync/storage, or Phase 160-162 behavior."
      category: scope
      verification: flagged-unverified
flagged_assumptions:
  - "[FLAGGED/UNVERIFIED] No plain Phase SPEC exists; PROOF-01 concurrency remains the existing missing-only and concurrent-winner contract, and this repair must prove unsafe input reaches no write path."
  - "[FLAGGED/UNVERIFIED] No plain Phase SPEC exists; PROOF-02 idempotency and concurrency require the existing rerun and two-generator regressions to remain green after endpoint normalization changes."
  - "[FLAGGED/UNVERIFIED] PROOF-03 was unclassified by the spec-less edge probe; the existing safe generated helper must still typecheck, but this repair adds no new browser flow or test corpus."
  - "[FLAGGED/UNVERIFIED] PROOF-04 was unclassified by the spec-less edge probe; rejected endpoint bytes never enter generated or retained evidence, while the existing typed evidence boundary remains unchanged."
---

<objective>
Reject TypeScript-string-unsafe endpoint configuration at the canonical normalization boundary before any file is written.

Purpose: D-06/D-08/D-09 define endpoints as closed host-local paths; quote and backslash characters have no required path meaning and must not reach raw TypeScript interpolation.
Output: Narrow endpoint grammar repair plus normalization, generation no-write, idempotency/concurrency, and safe-render typecheck regressions.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/ADR-FIRST-B2C-ADOPTER.md
@.planning/FIRST-B2C-ADOPTER-ADOPTION-BRIEF.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/159-host-reusable-proof-lane/159-CONTEXT.md
@.planning/phases/159-host-reusable-proof-lane/159-RESEARCH.md
@.planning/phases/159-host-reusable-proof-lane/159-VERIFICATION.md
@.planning/phases/159-host-reusable-proof-lane/159-02-SUMMARY.md
@.planning/phases/159-host-reusable-proof-lane/159-14-SUMMARY.md
@lib/crosswake/proof_lane/config.ex
@lib/crosswake/proof_lane/generator.ex
@lib/mix/tasks/crosswake.gen.proof_lane.ex
@priv/templates/crosswake/proof_lane/e2e/support/proof_lane.ts.eex
@test/crosswake/proof_lane/config_test.exs
@test/mix/tasks/crosswake_gen_proof_lane_test.exs
@script/verify_phoenix_host_proof_lane.sh

<interfaces>
Preserve the exact nine-key Config struct and PL-CONFIG-VALUE error shape. Choose the narrow closed-contract repair: quote and backslash are invalid in host-local endpoint paths, so reject them in Config.normalize/1 instead of adding a renderer or a second serialization representation.
</interfaces>
</context>

<assumption_delta_decision>
Primary noun: proof-lane endpoint configuration.
Decision: no-change.
Rationale: This validation repair adds no second identity model, authority source, platform, test-system abstraction, configuration representation, or endpoint mode.
</assumption_delta_decision>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Reject unsafe endpoint characters before generation and retain a type-safe valid render</name>
  <reversibility rating="reversible">The accepted endpoint grammar is narrowed inside the existing closed normalizer; reverting changes one predicate and its focused tests without data migration.</reversibility>
  <read_first>
    - AGENTS.md
    - .planning/ADR-FIRST-B2C-ADOPTER.md
    - .planning/FIRST-B2C-ADOPTER-ADOPTION-BRIEF.md
    - .planning/REQUIREMENTS.md
    - .planning/STATE.md
    - .planning/phases/159-host-reusable-proof-lane/159-CONTEXT.md
    - .planning/phases/159-host-reusable-proof-lane/159-RESEARCH.md
    - .planning/phases/159-host-reusable-proof-lane/159-VERIFICATION.md
    - .planning/phases/159-host-reusable-proof-lane/159-02-SUMMARY.md
    - .planning/phases/159-host-reusable-proof-lane/159-14-SUMMARY.md
    - lib/crosswake/proof_lane/config.ex
    - lib/crosswake/proof_lane/generator.ex
    - lib/mix/tasks/crosswake.gen.proof_lane.ex
    - priv/templates/crosswake/proof_lane/e2e/support/proof_lane.ts.eex
    - test/crosswake/proof_lane/config_test.exs
    - test/mix/tasks/crosswake_gen_proof_lane_test.exs
    - script/verify_phoenix_host_proof_lane.sh
  </read_first>
  <files>lib/crosswake/proof_lane/config.ex, lib/crosswake/proof_lane/generator.ex, test/crosswake/proof_lane/config_test.exs, test/mix/tasks/crosswake_gen_proof_lane_test.exs</files>
  <behavior>
    - Test 1: A double quote in sync_path or evidence_path returns PL-CONFIG-VALUE for the canonical key without echoing the supplied value.
    - Test 2: A backslash in sync_path or evidence_path returns the same stable non-echoing error.
    - Test 3: Mix-task, application-config, selected-config, and direct-Config generation attempts with either unsafe character fail before output-root creation and leave the surrounding filesystem snapshot byte-identical.
    - Test 4: Safe endpoints still normalize, generate, rerun idempotently, survive the existing two-generator winner test, and the generated browser helper typechecks.
  </behavior>
  <action>Start RED with a table covering sync_path and evidence_path crossed with double-quote and backslash inputs. Assert Config.normalize/1 returns only PL-CONFIG-VALUE, the canonical key, and the existing remediation without including the rejected bytes. Add production-path generator/Mix-task cases that set each unsafe endpoint through the supported configuration seams, snapshot the temporary parent before invocation, and prove normalization aborts before output-root creation, manifest staging, or template writes. Include a direct `%Config{}` case and strengthen Generator.generate/1 to normalize the complete struct map, not only derive its host root, before acquiring filesystem authority; this preserves the existing direct-struct revalidation decision for every key. Then extend the single host_local_path?/1 exclusion set to reject both character classes per D-06/D-08/D-09; do not alter the nine-key struct, raw EEx template, or add an encoder dependency because the closed host-local endpoint contract does not require those characters. Rerun the existing byte-identical rerun and concurrent-winner controls, then execute the Phoenix-host proof command so a valid generated helper is compiled/typechecked through the existing corpus. Keep errors non-echoing and preserve route-path, shell-root, check/diff, manifest, and evidence behavior.</action>
  <acceptance_criteria>
    - Both unsafe character classes are rejected for both endpoint keys by Config.normalize/1 with stable key-only errors.
    - Every supported generation entry point rejects before the first write; the output root and staging artifacts are absent and the parent snapshot is byte-identical.
    - Existing idempotent rerun and concurrent-generation tests remain green.
    - Safe endpoint configuration still generates a browser helper that passes the existing TypeScript/Playwright host proof command.
  </acceptance_criteria>
  <verify>
    <automated>mix test test/crosswake/proof_lane/config_test.exs test/mix/tasks/crosswake_gen_proof_lane_test.exs &amp;&amp; bash script/verify_phoenix_host_proof_lane.sh &amp;&amp; mix format --check-formatted lib/crosswake/proof_lane/config.ex lib/crosswake/proof_lane/generator.ex test/crosswake/proof_lane/config_test.exs test/mix/tasks/crosswake_gen_proof_lane_test.exs</automated>
  </verify>
  <done>Quote and backslash endpoint input is rejected non-echoingly before any write, while valid generation remains idempotent, collision-safe, and TypeScript-clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| Phoenix configuration → Config.normalize/1 | Host-supplied values enter the closed proof-lane contract. |
| normalized Config → EEx/filesystem | Canonical values influence generated TypeScript and host-owned paths. |

## STRIDE Threat Register — ASVS L1, block on high

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- | --- |
| T-159-G16-01 | Tampering | endpoint values rendered into TypeScript | high | mitigate | Reject quote and backslash at the canonical normalizer before rendering or filesystem activity. |
| T-159-G16-02 | Information Disclosure | config errors and Mix output | high | mitigate | Return only PL-CONFIG-VALUE, canonical key, and fixed remediation; tests assert rejected bytes are absent. |
| T-159-G16-03 | Denial of Service | malformed generated helper | medium | mitigate | Prove safe output typechecks and unsafe values produce no generated tree. |
| T-159-G16-04 | Race / Tampering | concurrent generator invocation | medium | mitigate | Retain existing two-generator winner and missing-only no-clobber regressions after normalization changes. |
| T-159-G16-SC | Tampering | package supply chain | low | accept | No package is installed and no external API, service, or schema integration is added. |
</threat_model>

<verification>
Focused normalization and generator tests cover both endpoint keys and character classes before writes; the existing host proof command typechecks the valid rendered helper.
</verification>

<success_criteria>
- The reproduced raw TypeScript interpolation defect is unreachable through normalized configuration.
- D-05/D-06/D-08/D-09 remain one closed Phoenix configuration contract with non-echoing failures.
- PROOF-01 concurrency/idempotency and existing browser ownership do not regress.
</success_criteria>

## Multi-Source Coverage Audit

| Source | ID | Feature / requirement | Gap plan | Status | Notes |
| --- | --- | --- | --- | --- | --- |
| GOAL | — | Configurable host-owned browser proof scaffolding | 159-16 | COVERED | Unsafe endpoint config is rejected before scaffold writes. |
| REQ | PROOF-01 | Non-destructive generation | 159-16 | COVERED | Invalid config produces no files; rerun and concurrency controls stay green. |
| REQ | PROOF-02 | Closed endpoint configuration | 159-16 | COVERED | Both endpoint keys reject quote/backslash at normalization. |
| RESEARCH | — | Normalize once and render all surfaces from one Config | 159-16 | COVERED | No second encoder/config representation is introduced. |
| CONTEXT | D-03 | Preserve browser semantic proof | 159-16 | COVERED | Existing valid generated helper still typechecks/runs. |
| CONTEXT | D-05-D-09 | Phoenix config, closed validation, local endpoints, non-echoing errors | 159-16 | COVERED | This plan directly repairs the reproduced violation. |
| CONTEXT | D-20-D-22 | Read-only/no-partial lifecycle | 159-16 plus executed plans | COVERED | Invalid input is rejected before any write; existing lifecycle tests rerun. |
| API | — | No external API integration | 159-16 | COVERED | `COVERAGE.md` remains unchanged; endpoint values remain host-local paths. |

PROOF-03, PROOF-04, D-01/D-02/D-04, D-10 through D-19, and D-23 retain their executed implementations and are rerun by Plan 159-17. Deferred Phase 160-162 behavior, Android, schemas, generic sync/storage, and new UI breadth remain excluded.

## Artifacts this phase produces

- A narrower closed endpoint validator in `Config`.
- Non-echoing normalization and production-generation no-write regressions.
- Fresh valid-render TypeScript/browser proof evidence through the existing one-command lane.
- No encoder dependency, second configuration representation, external API matrix, schema push, Android change, or UI change.

<output>
Create `.planning/phases/159-host-reusable-proof-lane/159-16-SUMMARY.md` when done.
</output>
