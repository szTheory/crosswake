---
phase: 159-host-reusable-proof-lane
plan: "15"
type: execute
wave: 9
depends_on: [159-14]
files_modified:
  - priv/templates/crosswake/proof_lane/ios/CrosswakeProofLaneTests/ProofLaneContractTests.swift.eex
  - priv/templates/crosswake/proof_lane/ios/CrosswakeProofLaneUITests/ProofLaneUITests.swift.eex
  - script/verify_generated_ios_shell.sh
  - test/crosswake/proof_lane/template_contract_test.exs
  - test/crosswake/proof_lane/ios_verifier_test.exs
autonomous: true
gap_closure: true
requirements: [PROOF-01, PROOF-03]
must_haves:
  truths:
    - "An untouched generated iOS lane whose ProofLaneHostAdapterFactory.make() returns nil exits non-zero with one stable blocked result and can never be reported as passed (D-10, D-11, D-12, D-14, PROOF-01, PROOF-03)."
    - "The verifier reports passed only when the shared scheme records the exact host-adapter XCTest assertion and adapter-derived XCUITest lifecycle assertion, not merely any executed test in each bundle (D-10, D-11, D-13)."
    - "Blocked and unavailable remain legitimate rendered probe states but are non-passing proof outcomes; only adapter-derived Passed satisfies the generated UI lifecycle assertion (D-11 and the approved UI contract)."
    - "Repository-controlled fixture regressions prove the generated accessibility-size XCUITest contract checks 24px horizontal insets, wrapped text containment without ellipsis or horizontal scrolling, and the conditional retry control's 44x44pt target; actual simulator execution remains advisory and non-promoting per D-14."
  artifacts:
    - path: "priv/templates/crosswake/proof_lane/ios/CrosswakeProofLaneTests/ProofLaneContractTests.swift.eex"
      provides: "Generated contract assertion that requires installed host-adapter evidence before success"
    - path: "priv/templates/crosswake/proof_lane/ios/CrosswakeProofLaneUITests/ProofLaneUITests.swift.eex"
      provides: "Generated lifecycle assertion that rejects a displayed Unavailable state as proof success"
    - path: "script/verify_generated_ios_shell.sh"
      provides: "Exact adapter-evidence transcript gate before the passed JSON outcome"
    - path: "test/crosswake/proof_lane/ios_verifier_test.exs"
      provides: "Deterministic connected and unconnected verifier regressions, including the exact accessibility XCUITest completion marker"
  key_links:
    - from: "ProofLaneHostAdapterFactory.make()"
      to: "ProofLaneContractTests and ProofLaneUITests"
      via: "generated host-adapter contract and adapter-derived observable outcome"
      pattern: "ProofLaneHostAdapterFactory|hostAdapter|Passed"
    - from: "test-without-building transcript"
      to: "emit_proof_outcome passed"
      via: "exact bundle-qualified host-adapter and lifecycle test markers"
      pattern: "PL-IOS-TEST-EVIDENCE|host.*adapter|Lifecycle"
  prohibitions:
    - statement: "MUST NOT accept target enumeration, build-for-testing, bundle execution, a nil factory, a fixed label, or an unavailable prerequisite as adapter evidence."
      category: integrity
      verification: flagged-unverified
    - statement: "MUST NOT give the generated probe auth, replay, mutation, pack, credential, account, or backend authority."
      category: authorization
      verification: flagged-unverified
    - statement: "MUST NOT retain or echo raw native output, payloads, identities, credentials, tokens, endpoints, media, transcripts, stable device identifiers, screenshots, traces, logs, or xcresult bundles."
      category: privacy
      verification: flagged-unverified
    - statement: "MUST NOT add Android work, physical-device promotion, generic sync or storage, new UI breadth, a dashboard, a second test system, or Phase 160-162 behavior."
      category: scope
      verification: flagged-unverified
flagged_assumptions:
  - "[FLAGGED/UNVERIFIED] No plain Phase SPEC exists; PROOF-01 concurrency remains the existing missing-only/concurrent-winner contract and this native verifier repair introduces no generator write path."
  - "[FLAGGED/UNVERIFIED] No plain Phase SPEC exists; PROOF-02 idempotency and concurrency remain governed by the existing rerun and two-generator controls and are unchanged by this native-only repair."
  - "[FLAGGED/UNVERIFIED] PROOF-03 was unclassified by the spec-less edge probe; this plan treats exact adapter-backed XCTest/XCUITest evidence as the narrow correction to the reproduced false-pass defect, without adding another proof corpus."
  - "[FLAGGED/UNVERIFIED] PROOF-04 was unclassified by the spec-less edge probe; native verifier output remains closed JSON and the existing evidence allowlist/scanner remains authoritative."
---

<objective>
Make an unwired generated iOS proof lane deterministically non-passing while preserving a real host-adapter path to passed.

Purpose: Generated XCTest/XCUITest execution must prove host integration, not merely prove that unwired targets compiled and ran.
Output: Adapter-evidence assertions, exact verifier markers, and a regression showing the untouched generated lane cannot exit passed.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/ADR-FIRST-B2C-ADOPTER.md
@.planning/FIRST-B2C-ADOPTER-ADOPTION-BRIEF.md
@.planning/FIRST-B2C-ADOPTER-ROUTE-POLICY-MAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/159-host-reusable-proof-lane/159-CONTEXT.md
@.planning/phases/159-host-reusable-proof-lane/159-UI-SPEC.md
@.planning/phases/159-host-reusable-proof-lane/159-VERIFICATION.md
@.planning/phases/159-host-reusable-proof-lane/159-09-SUMMARY.md
@.planning/phases/159-host-reusable-proof-lane/159-14-SUMMARY.md
@priv/templates/crosswake/proof_lane/ios/ProofLaneDriver.swift.eex
@priv/templates/crosswake/proof_lane/ios/ProofLaneApp.swift.eex
@priv/templates/crosswake/proof_lane/ios/CrosswakeProofLaneTests/ProofLaneContractTests.swift.eex
@priv/templates/crosswake/proof_lane/ios/CrosswakeProofLaneUITests/ProofLaneUITests.swift.eex
@script/verify_generated_ios_shell.sh
@test/crosswake/proof_lane/template_contract_test.exs
@test/crosswake/proof_lane/ios_verifier_test.exs

<interfaces>
Keep ProofLaneHostAdapterFactory.make() as the host-owned integration seam and keep the closed passed/blocked/unavailable JSON vocabulary and exit codes 0/2/3. Strengthen only what evidence permits exit 0: an exact host-adapter contract assertion plus an exact adapter-derived UI lifecycle assertion from the generated shared scheme.
</interfaces>
</context>

<assumption_delta_decision>
Primary noun: generated iOS proof lane.
Decision: no-change.
Rationale: The prose about avoiding a second test system is a negated boundary; this repair adds no second identity model, authority source, platform, test-system abstraction, or representation.
</assumption_delta_decision>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Require host-adapter evidence across generated XCTest, XCUITest, and verifier output</name>
  <reversibility rating="reversible">The repair changes only generated test assertions and verifier evidence selection behind the existing host-owned adapter and closed outcome contracts.</reversibility>
  <read_first>
    - AGENTS.md
    - .planning/ADR-FIRST-B2C-ADOPTER.md
    - .planning/FIRST-B2C-ADOPTER-ADOPTION-BRIEF.md
    - .planning/FIRST-B2C-ADOPTER-ROUTE-POLICY-MAP.md
    - .planning/REQUIREMENTS.md
    - .planning/STATE.md
    - .planning/phases/159-host-reusable-proof-lane/159-CONTEXT.md
    - .planning/phases/159-host-reusable-proof-lane/159-UI-SPEC.md
    - .planning/phases/159-host-reusable-proof-lane/159-VERIFICATION.md
    - .planning/phases/159-host-reusable-proof-lane/159-PATTERNS.md
    - .planning/phases/159-host-reusable-proof-lane/159-09-SUMMARY.md
    - .planning/phases/159-host-reusable-proof-lane/159-14-SUMMARY.md
    - priv/templates/crosswake/shell/ios/CrosswakeShell.xcodeproj/project.pbxproj.eex
    - priv/templates/crosswake/proof_lane/ios/ProofLaneDriver.swift.eex
    - priv/templates/crosswake/proof_lane/ios/ProofLaneApp.swift.eex
    - priv/templates/crosswake/proof_lane/ios/CrosswakeProofLaneTests/ProofLaneContractTests.swift.eex
    - priv/templates/crosswake/proof_lane/ios/CrosswakeProofLaneUITests/ProofLaneUITests.swift.eex
    - script/verify_generated_ios_shell.sh
    - test/crosswake/proof_lane/template_contract_test.exs
    - test/crosswake/proof_lane/ios_verifier_test.exs
  </read_first>
  <files>priv/templates/crosswake/proof_lane/ios/CrosswakeProofLaneTests/ProofLaneContractTests.swift.eex, priv/templates/crosswake/proof_lane/ios/CrosswakeProofLaneUITests/ProofLaneUITests.swift.eex, script/verify_generated_ios_shell.sh, test/crosswake/proof_lane/template_contract_test.exs, test/crosswake/proof_lane/ios_verifier_test.exs</files>
  <behavior>
    - Test 1: The untouched generated lane with a nil factory executes as a stable non-zero blocked outcome and never emits passed.
    - Test 2: A transcript containing generic bundle tests but lacking the exact host-adapter assertion remains blocked with PL-IOS-TEST-EVIDENCE.
    - Test 3: Passed requires exact bundle-qualified completion markers for the adapter-backed contract assertion and the adapter-derived lifecycle assertion.
    - Test 4: Generated XCUITest at accessibility text size requires adapter-derived Passed after launch/relaunch; Unavailable is observable but cannot satisfy proof success.
    - Test 5: The generated accessibility XCUITest asserts every text frame remains inside the window's 24pt horizontal insets, preserves the full visible label without truncation, exposes no horizontal scroll view, and gives a present retry control a 44x44pt minimum hittable frame; the hermetic connected-adapter fixture includes this exact bundle-qualified test marker before verifier success.
  </behavior>
  <action>Start RED by changing the hermetic xcodebuild fixture so its former generic successful transcript demonstrates the false pass, and add a generated-lane contract regression that invokes `script/verify_generated_ios_shell.sh --proof-lane` through the repository-controlled xcodebuild fixture without editing ProofLaneHostAdapterFactory.make(). Apply the static, declarative Xcode-target pattern mapped in `159-PATTERNS.md` from `priv/templates/crosswake/shell/ios/CrosswakeShell.xcodeproj/project.pbxproj.eex`: keep proof-owned unit and UI tests in distinct explicit targets with declared file references, build files, source phases, product/module names, XCTest host settings, and `TestTargetID`; do not introduce project-manager tooling or in-place adopter-project mutation. Apply the existing verifier extension pattern from `script/verify_generated_ios_shell.sh`: retain its tool preflight, shared-scheme/destination discovery, simulator fallback, and build-for-testing flow, and tighten only the post-test evidence gate. Require the unconnected fixture to return exit 2 with a stable blocked rule and never exit 0. Per D-10/D-11/D-12/D-13, replace the generated XCTest missing-adapter acceptance with a named host-adapter evidence assertion that requires a non-nil installed adapter, shell boot, and adapter-derived passed outcome; replace XCUITest's acceptance of Unavailable with a named lifecycle assertion that launches, observes adapter-derived Passed, terminates, relaunches, and observes Passed again through accessibility identifiers. Add a named accessibility reflow assertion to the generated XCUITest: launch with `UIContentSizeCategoryAccessibilityXXXL`; for title, authority posture, and outcome elements, assert the full expected safe label remains observable and each frame stays within the window frame inset by 24pt; assert no horizontal scroll view exists; when retry is present, assert its full label, frame width and height of at least 44pt, hittability, and containment within the same insets. Extend `template_contract_test.exs` to pin every generated assertion and extend the hermetic connected-adapter xcodebuild fixture in `ios_verifier_test.exs` to emit the exact bundle-qualified accessibility assertion marker alongside the adapter contract and lifecycle markers. Update the verifier to require all three exact markers after successful shared-scheme execution before emitting passed; missing any marker returns PL-IOS-TEST-EVIDENCE/blocked. These repository-controlled fixture tests are the deterministic required gate for the generated contract. Actual simulator/XCUITest execution is separate advisory evidence under D-14 and cannot block or promote Phase 159. Keep Blocked/Unavailable rendering and conditional retry behavior intact as honest non-passing states. Preserve the approved UI composition, accessibility identifiers, no-op omission, no retained transcript, and closed JSON output. Do not add a flag that bypasses adapter evidence.</action>
  <acceptance_criteria>
    - An unedited generated lane whose factory returns nil exits 2 with one stable blocked JSON result; no execution path reports passed for it.
    - Generic test-bundle execution without the exact adapter-backed XCTest and XCUITest markers returns PL-IOS-TEST-EVIDENCE and exit 2.
    - Exact adapter-backed completion markers are necessary for exit 0/passed, while missing tools, target, build, or test execution retain their existing unavailable/blocked classifications.
    - The deterministic generated-project fixture requires the exact accessibility reflow marker and structural assertions for 24pt inset containment, full untruncated labels, no horizontal scroll view, and the conditional 44x44pt retry target before its connected-adapter verifier outcome can pass.
    - The generated probe still exposes only closed outcomes and stable accessibility state; actual simulator execution is recorded only as advisory under D-14, and no new authority, UI surface, platform, or retained output is introduced.
  </acceptance_criteria>
  <verify>
    <automated>mix test test/crosswake/proof_lane/template_contract_test.exs test/crosswake/proof_lane/ios_verifier_test.exs &amp;&amp; bash -n script/verify_generated_ios_shell.sh &amp;&amp; mix format --check-formatted test/crosswake/proof_lane/template_contract_test.exs test/crosswake/proof_lane/ios_verifier_test.exs</automated>
  </verify>
  <done>The generated iOS lane can emit passed only from exact host-adapter-backed XCTest/XCUITest evidence including the accessibility reflow assertion, its untouched unwired form is deterministically blocked, and D-14 keeps actual native execution advisory.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| host-owned adapter → generated XCTest/XCUITest | Host code supplies proof state across a test-only interface with no backend authority. |
| xcodebuild transcript → verifier outcome | Untrusted tool output may influence whether the lane emits passed. |

## STRIDE Threat Register — ASVS L1, block on high

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- | --- |
| T-159-G15-01 | Spoofing / Repudiation | `verify_generated_ios_shell.sh` | high | mitigate | Require exact bundle-qualified adapter-backed assertion markers and a non-nil adapter-derived Passed lifecycle before exit 0. |
| T-159-G15-02 | Elevation of Privilege | generated probe adapter | high | mitigate | Keep the adapter test-only and observational; do not add credential, replay, pack, mutation, or backend authority. |
| T-159-G15-03 | Information Disclosure | native transcript and JSON output | high | mitigate | Retain no transcript and emit only closed outcome, stable rule ID, and safe scope. |
| T-159-G15-04 | Tampering | host-owned generated sources | medium | mitigate | Preserve missing-only generation and require the host to install the adapter through its owned seam; do not overwrite host edits. |
| T-159-G15-SC | Tampering | package supply chain | low | accept | This plan installs no package and adds no external API or service integration. |
</threat_model>

<verification>
Focused template/verifier tests prove both sides of the deterministic gate: the untouched generated lane is blocked, while only the exact adapter-backed contract, lifecycle, and accessibility-reflow markers can produce passed. Actual simulator execution is advisory under D-14.
</verification>

<success_criteria>
- The reproduced unwired-adapter false pass is closed with deterministic production-path evidence.
- The generated accessibility-size XCUITest and connected-adapter fixture deterministically pin 24pt inset containment, full labels without truncation, no horizontal scroll view, and the conditional 44x44pt retry target.
- D-10 through D-14 remain intact without adding Phase 160/161 behavior or physical-device promotion.
- Native output remains closed, non-echoing, advisory, and iOS-only.
</success_criteria>

## Multi-Source Coverage Audit

| Source | ID | Feature / requirement | Gap plan | Status | Notes |
| --- | --- | --- | --- | --- | --- |
| GOAL | — | Host-owned executable iOS proof scaffolding | 159-15 | COVERED | The scaffold now fails closed until its host adapter is truly installed. |
| REQ | PROOF-01 | Host-owned iOS scaffold without overwrite | 159-15 | COVERED | Existing ownership stays unchanged; false passing is removed. |
| REQ | PROOF-03 | Add only native boundaries the browser cannot prove | 159-15 | COVERED | Exact host-adapter XCTest/XCUITest evidence remains the bounded native seam. |
| RESEARCH | — | Closed outcomes and false-device-proof mitigation | 159-15 | COVERED | Exact test markers and adapter-derived state gate promotion. |
| CONTEXT | D-01-D-04 | Additive host-owned executable wiring | 159-15 plus executed plans | COVERED | Only generated test files and verifier evidence are tightened. |
| CONTEXT | D-10-D-14 | Real XCTest/XCUITest, explicit non-passing prerequisites, advisory native execution | 159-15 | COVERED | Deterministic fixture regressions gate the generated contract; actual simulator execution cannot block or promote. |
| UI | backstop | Accessibility-size wrapping/reflow, 24px insets, 44x44pt retry, no clipping/ellipsis/horizontal scroll | 159-15 | COVERED-CONTRACT / ADVISORY-RUNTIME | Generated XCUITest assertions and their connected-adapter fixture marker are required deterministically; actual simulator rendering remains an advisory backstop per D-14. |
| CONTEXT | D-23 | No broader test/platform machinery | 159-15 | COVERED | Existing Swift/Xcode/bash seams only. |
| API | — | No external API integration | 159-15 | COVERED | The existing `COVERAGE.md` reason remains correct and unchanged. |

PROOF-02, PROOF-04, D-05 through D-09, and D-15 through D-22 retain their executed implementations and are rerun by Plan 159-17. Deferred Phase 160 scoped replay/auth, Phase 161 pack/audio, Phase 162 physical-device proof, Android, generic sync/storage, and new UI breadth remain excluded.

## Artifacts this phase produces

- Generated XCTest/XCUITest templates whose exact adapter-backed assertions are necessary for proof success.
- A verifier that recognizes only those exact assertions as passing evidence.
- Deterministic regressions for unconnected blocked and connected-evidence paths.
- A deterministic connected-adapter fixture path that requires the generated accessibility-reflow XCUITest marker without depending on simulator availability.
- No external API matrix, schema push, Android artifact, raw native artifact, or new UI surface.

<output>
Create `.planning/phases/159-host-reusable-proof-lane/159-15-SUMMARY.md` when done.
</output>
