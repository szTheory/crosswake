---
phase: 159-host-reusable-proof-lane
plan: "19"
type: execute
wave: 11
depends_on: [159-17]
files_modified:
  - lib/crosswake/proof_lane/config.ex
  - test/crosswake/proof_lane/config_test.exs
  - test/mix/tasks/crosswake_gen_proof_lane_test.exs
autonomous: true
gap_closure: true
requirements: [PROOF-01, PROOF-02]
must_haves:
  truths:
    - "Config.normalize/1 rejects exactly one backslash byte in either sync_path or evidence_path with a stable, non-echoing PL-CONFIG-VALUE error (D-06, D-08, D-09, PROOF-02)."
    - "Direct Config structs, application config, and selected --config input reject that byte before host-root derivation, template rendering, output-root creation, or GeneratorFS authority (D-05, D-06, D-09, PROOF-01, PROOF-02)."
    - "Safe input still renders type-correct generated TypeScript, while repeated and concurrent generation preserve the existing missing-only, byte-preserving, concurrent-winner behavior (D-02, D-09, PROOF-01, PROOF-02)."
  artifacts:
    - path: "lib/crosswake/proof_lane/config.ex"
      provides: "Canonical host-local path predicate rejecting one backslash byte"
    - path: "test/crosswake/proof_lane/config_test.exs"
      provides: "Direct normalization and non-echoing regressions for both endpoint keys"
    - path: "test/mix/tasks/crosswake_gen_proof_lane_test.exs"
      provides: "Application, selected-config, direct-Config, pre-render/pre-write, rerun, and concurrent-generator regressions"
  key_links:
    - from: "Config.normalize/1"
      to: "Generator.generate/1, check/1, and diff/1"
      via: "normalize! before host_root!, desired rendering, and GeneratorFS calls"
      pattern: "normalize!|host_local_path?"
    - from: "normalized endpoint paths"
      to: "priv/templates/crosswake/proof_lane/e2e/support/proof_lane.ts.eex"
      via: "raw EEx assignment is safe only because the canonical boundary excludes quote and every backslash byte"
      pattern: "syncPath|evidencePath"
  prohibitions:
    - statement: "Rejected endpoint values do not appear in exceptions, Mix output, rendered files, manifests, diffs, or retained evidence."
      category: privacy
      verification: "mix test test/crosswake/proof_lane/config_test.exs test/mix/tasks/crosswake_gen_proof_lane_test.exs"
    - statement: "No second config representation, renderer encoder, endpoint URL mode, credential-bearing endpoint, dependency, or external API is introduced."
      category: architecture
      verification: flagged-unverified
    - statement: "Android, browser corpus ownership, evidence schema, UI breadth, generic sync/storage, and Phase 160-162 behavior remain unchanged."
      category: scope
      verification: flagged-unverified
flagged_assumptions:
  - "[RESOLVED] PROOF-02 idempotency probe: safe generation is rerun against the same normalized input and must preserve all existing host-owned bytes."
  - "[RESOLVED] PROOF-02 concurrency probe: the existing two-generator regression must preserve the destination winner and leave no staging paths after the predicate repair."
  - "[RESOLVED] PROOF-01 concurrency probe for this boundary: unsafe input reaches no filesystem action, so concurrent unsafe attempts cannot create or replace scaffold output."
---

<objective>
Repair the canonical endpoint predicate so one backslash byte cannot cross into generated TypeScript or filesystem authority.

Purpose: Close the exact current-tree input-validation escape without adding an alternate renderer or configuration surface.
Output: One-character boundary fix plus direct, application, selected-config, render, pre-write, idempotency, and concurrency regressions.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/159-host-reusable-proof-lane/159-CONTEXT.md
@.planning/phases/159-host-reusable-proof-lane/159-VERIFICATION.md
@.planning/phases/159-host-reusable-proof-lane/159-16-SUMMARY.md
@lib/crosswake/proof_lane/config.ex
@lib/crosswake/proof_lane/generator.ex
@priv/templates/crosswake/proof_lane/e2e/support/proof_lane.ts.eex
@test/crosswake/proof_lane/config_test.exs
@test/mix/tasks/crosswake_gen_proof_lane_test.exs
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Reject one backslash through every configuration and generation seam</name>
  <reversibility rating="reversible">This is a local correction to the locked closed-path grammar and removes an unintended accepted byte.</reversibility>
  <read_first>
    - .planning/phases/159-host-reusable-proof-lane/159-VERIFICATION.md
    - .planning/phases/159-host-reusable-proof-lane/159-CONTEXT.md
    - lib/crosswake/proof_lane/config.ex
    - lib/crosswake/proof_lane/generator.ex
    - priv/templates/crosswake/proof_lane/e2e/support/proof_lane.ts.eex
    - test/crosswake/proof_lane/config_test.exs
    - test/mix/tasks/crosswake_gen_proof_lane_test.exs
  </read_first>
  <files>lib/crosswake/proof_lane/config.ex, test/crosswake/proof_lane/config_test.exs, test/mix/tasks/crosswake_gen_proof_lane_test.exs</files>
  <behavior>
    - "RED: a value built with exactly one byte 92 in sync_path or evidence_path currently normalizes successfully; direct Config, application config, and selected config demonstrate the same defect."
    - "GREEN: both endpoint keys return only PL-CONFIG-VALUE plus the safe key/remediation, and the rejected value is absent from the exception and captured Mix output."
    - "Every generator action rejects before output-root creation or change; safe generation still renders TypeScript, reruns byte-identically, and preserves concurrent winners."
  </behavior>
  <action>Write the failing cases first using runtime construction with exactly one backslash byte so the test cannot accidentally encode two. Cover both endpoint keys through direct `Config.normalize/1`; direct `%Config{}` calls to `Generator.generate/1`, `check/1`, and `diff/1`; application configuration; and `--config` selection. Assert the stable `PL-CONFIG-VALUE` key/remediation shape, non-echoing exceptions/captured output, and an unchanged filesystem snapshot with no output root for invalid inputs. Correct `host_local_path?/1` to reject one backslash character at the canonical normalizer, before `host_root!`, `desired/1` EEx rendering, manifest staging, or `GeneratorFS`. Add a valid render/typecheck assertion and rerun the existing idempotent and two-generator collision tests so the repair does not disturb D-02. Keep raw EEx safe through the closed input grammar; do not add escaping machinery, another config format, or any scope beyond the two endpoint keys.</action>
  <acceptance_criteria>
    - Exactly one backslash byte is rejected for sync_path and evidence_path in direct normalization.
    - Direct Config, application, and selected-config generator entry points fail before rendering or filesystem changes and never echo the rejected value.
    - Valid generated TypeScript remains type-correct.
    - Same-input rerun and concurrent-generator regressions remain green, with host-owned bytes and collision winners preserved.
  </acceptance_criteria>
  <verify>
    <automated>mix test test/crosswake/proof_lane/config_test.exs test/mix/tasks/crosswake_gen_proof_lane_test.exs &amp;&amp; npm --prefix examples/phoenix_host run typecheck:offline-route-proof</automated>
  </verify>
  <done>Every one-backslash endpoint attempt fails non-echoingly at the single canonical boundary before render or write authority.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| Phoenix configuration -> canonical Config struct | Host-controlled endpoint text enters the trusted generator model. |
| normalized Config -> EEx and GeneratorFS | Accepted values gain source-rendering and filesystem influence. |

## STRIDE Threat Register — ASVS L1, block on high

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- | --- |
| T-159-G19-01 | Tampering | TypeScript endpoint rendering | high | mitigate | Reject byte 92 and quote at Config.normalize/1 before any EEx evaluation. |
| T-159-G19-02 | Information Disclosure | Config errors and Mix output | high | mitigate | Stable rule/key/remediation only, with runtime canaries proving no rejected value is echoed. |
| T-159-G19-03 | Tampering | generator filesystem | high | mitigate | Revalidate direct structs and snapshot all generate/check/diff, application, and selected-config invalid paths before writes. |
| T-159-G19-04 | Repudiation | idempotency/concurrency claims | medium | mitigate | Rerun same-input byte preservation and two-generator winner/staging regressions. |
| T-159-G19-SC | Tampering | package supply chain | low | accept | No install, dependency, or external service is involved. |
</threat_model>

<verification>
The focused ExUnit suites distinguish one byte from two, prove all configuration seams fail before rendering/writes, and preserve safe rendering plus generation idempotency/concurrency.
</verification>

<success_criteria>
- The one-backslash counterexample is rejected identically for both endpoint keys and every config entry path.
- No unsafe endpoint reaches TypeScript rendering or generator filesystem authority.
- Safe rerun/concurrency behavior and the existing browser TypeScript surface remain intact.
</success_criteria>

## Artifacts this phase produces

- Corrected `Crosswake.ProofLane.Config.host_local_path?/1` behavior for one backslash byte.
- Expanded endpoint boundary regressions in `config_test.exs` and `crosswake_gen_proof_lane_test.exs`.
- No new path, public symbol, schema, dependency, API matrix, external integration, Android artifact, or UI surface.

<output>
Create `.planning/phases/159-host-reusable-proof-lane/159-19-SUMMARY.md` when done.
</output>
