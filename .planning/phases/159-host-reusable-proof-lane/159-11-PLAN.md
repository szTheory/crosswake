---
phase: 159-host-reusable-proof-lane
plan: "11"
type: execute
wave: 4
depends_on: [159-07]
files_modified:
  - lib/crosswake/proof_lane/evidence.ex
  - test/crosswake/proof_lane/evidence_test.exs
autonomous: true
gap_closure: true
requirements: [PROOF-04]
must_haves:
  truths:
    - "Every malformed, raising, throwing, or exiting evidence lifecycle hook fails closed with a sanitized `PL-EVIDENCE-PROMOTE` error and leaves no promoted or staged artifact (D-15, D-19, D-22, PROOF-04)."
    - "Only `nil` or a zero-arity hook returning exactly `:ok` permits promotion to continue; arbitrary callback return values cannot raise a protocol error or bypass validation."
    - "Hook failures never echo exception messages, thrown terms, exit reasons, callback output, candidate evidence, or destination values."
  artifacts:
    - path: "lib/crosswake/proof_lane/evidence.ex"
      provides: "Closed fail-closed hook normalization inside atomic evidence promotion"
    - path: "test/crosswake/proof_lane/evidence_test.exs"
      provides: "Malformed-return and exception-class negative controls with cleanup assertions"
  key_links:
    - from: "Evidence.promote/3"
      to: "run_hook/1"
      via: "closed normalization before native no-replace promotion"
      pattern: "PL-EVIDENCE-PROMOTE"
---

<objective>
Make the evidence promotion hook a total, sanitized fail-closed boundary.

Purpose: A malformed hook must never raise outside the evidence error contract or leave partial state.
Output: Closed hook outcomes and adversarial cleanup/non-echo regressions.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/phases/159-host-reusable-proof-lane/159-CONTEXT.md
@.planning/phases/159-host-reusable-proof-lane/159-VERIFICATION.md
@.planning/phases/159-host-reusable-proof-lane/159-07-SUMMARY.md
@lib/crosswake/proof_lane/evidence.ex
@test/crosswake/proof_lane/evidence_test.exs
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Collapse all malformed evidence hooks into one safe non-passing result</name>
  <reversibility rating="reversible">This tightens an internal test/race hook without changing the retained evidence schema or promotion success contract.</reversibility>
  <read_first>
    - .planning/phases/159-host-reusable-proof-lane/159-CONTEXT.md
    - .planning/phases/159-host-reusable-proof-lane/159-VERIFICATION.md
    - lib/crosswake/proof_lane/evidence.ex
    - test/crosswake/proof_lane/evidence_test.exs
  </read_first>
  <files>lib/crosswake/proof_lane/evidence.ex, test/crosswake/proof_lane/evidence_test.exs</files>
  <behavior>
    - "A hook returning `:ok` reaches the existing atomic no-replace promotion path."
    - "Hooks returning `nil`, booleans, tuples, maps, or arbitrary values return the same sanitized promotion error."
    - "Raise, throw, and exit are caught at the hook boundary and return that error without promoting a destination or retaining a stage."
  </behavior>
  <action>Start RED with table-driven malformed return values and separate raise/throw/exit canaries. Change `run_hook/1` so `nil` means no hook, while an installed zero-arity callback must return exactly `:ok`; normalize every other return and all three exception classes to `error("PL-EVIDENCE-PROMOTE", "artifact", "inspect the safe evidence inputs")`. Catch only around callback invocation so library defects elsewhere remain visible. Do not inspect, log, interpolate, or retain callback values/exceptions. For every negative case assert the destination does not exist, no sibling `.stage-*` remains, and the rendered error lacks canaries. Retain the existing post-scan collision hook returning `:ok` and concurrent-winner behavior.</action>
  <verify>
    <automated>mix test test/crosswake/proof_lane/evidence_test.exs &amp;&amp; mix format --check-formatted lib/crosswake/proof_lane/evidence.ex test/crosswake/proof_lane/evidence_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - Installed callbacks have exactly one accepted result: `:ok`.
    - Every malformed/exception case returns `%Evidence.Error{rule_id: "PL-EVIDENCE-PROMOTE", path: "artifact"}` without a raised process exit.
    - No canary, destination, candidate map, native output, or staged byte appears in the error or filesystem after failure.
    - Existing final scan, source-hash verification, post-scan collision, and no-replace winner tests remain green.
  </acceptance_criteria>
  <done>PROOF-04 remains total and fail closed even when the internal evidence hook misbehaves.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| lifecycle callback → evidence promoter | Test/race callback behavior enters the atomic retained-artifact path. |

## STRIDE Threat Register — ASVS L1, block on high

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- | --- |
| T-159-G11-01 | Tampering / Denial of Service | `run_hook/1` | high | mitigate | Exact `:ok` allowlist, raise/throw/exit normalization, and destination/stage cleanup tests. |
| T-159-G11-02 | Information Disclosure | hook failure rendering | high | mitigate | One stable error independent of callback return/exception with explicit canary non-echo tests. |
| T-159-G11-SC | Tampering | package supply chain | low | accept | No install, package, native-source, or external-service change. |
</threat_model>

<verification>
Run the complete evidence suite and formatter; confirm the retained twelve-field schema and `COVERAGE.md` are byte-unchanged.
</verification>

<success_criteria>
- Evidence hook failures are sanitized, non-raising, non-promoting, and cleanup-complete.
- Existing typed allowlist, canonical hashing, final scanning, and atomic no-replace promotion remain intact.
- No schema, endpoint, external API, Android, replay/auth, pack/audio, or device-proof surface is added.
</success_criteria>

## Artifacts this phase produces

- Strengthened internal `run_hook/1` totality contract and focused negative controls.
- No new file, schema field, package, external API, Android artifact, or `COVERAGE.md` change.

<output>
Create `.planning/phases/159-host-reusable-proof-lane/159-11-SUMMARY.md` when done.
</output>
