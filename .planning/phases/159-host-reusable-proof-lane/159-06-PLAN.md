---
phase: 159-host-reusable-proof-lane
plan: "06"
type: execute
wave: 3
depends_on: [159-03]
files_modified:
  - examples/phoenix_host/e2e/support/offline_route_proof.ts
  - examples/phoenix_host/e2e/crosswake_proof_lane/browser_online_restore.spec.ts
  - examples/phoenix_host/e2e/crosswake_proof_lane/support/proof_lane.ts
  - examples/phoenix_host/e2e/support/offline_route_proof.typecheck.d.ts
  - examples/phoenix_host/tsconfig.offline_route_proof.json
  - examples/phoenix_host/package.json
  - priv/templates/crosswake/proof_lane/e2e/support/proof_lane.ts.eex
  - script/verify_generated_ios_shell.sh
  - test/crosswake/proof_lane/template_contract_test.exs
  - test/crosswake/proof_lane/ios_verifier_test.exs
autonomous: true
gap_closure: true
requirements: [PROOF-01, PROOF-03]
must_haves:
  truths:
    - "Per D-03, the unchanged primary browser corpus compiles through a focused TypeScript configuration and retains the mutation ID produced by `runOfflineIslandProof` for the later history assertion (PROOF-03)."
    - "Per D-03, the existing offline Playwright spec remains runnable and continues to prove real UI mutation, IndexedDB observation, app reconnect, backend confirmation, empty outbox, and duplicate idempotency (PROOF-03)."
    - "Per D-22, both the repository helper and generated host adapter restore the browser context online in `finally`, even when an offline mutation or assertion throws (PROOF-03)."
    - "Per D-11 and D-14, unavailable Xcode, target enumeration, or proof-target build returns a closed machine-readable `blocked` or `unavailable` outcome with a non-zero status; only a built generated target graph exits zero, without promoting simulator/device support (PROOF-01)."
  artifacts:
    - path: "examples/phoenix_host/e2e/support/offline_route_proof.ts"
      provides: "Runnable primary browser wrapper with retained opaque mutation ID and online-state cleanup"
    - path: "examples/phoenix_host/tsconfig.offline_route_proof.json"
      provides: "Focused zero-error TypeScript gate for the primary proof module"
    - path: "examples/phoenix_host/e2e/support/offline_route_proof.typecheck.d.ts"
      provides: "Narrow declarations for pre-existing Node and host-window ambient seams"
    - path: "examples/phoenix_host/e2e/crosswake_proof_lane/browser_online_restore.spec.ts"
      provides: "Executable failure-path regression for repository and rendered generated browser helpers"
    - path: "examples/phoenix_host/e2e/crosswake_proof_lane/support/proof_lane.ts"
      provides: "Deterministically rendered generated-helper fixture executed by the runtime regression"
    - path: "script/verify_generated_ios_shell.sh"
      provides: "Non-zero machine-readable proof-lane blocked/unavailable outcomes"
    - path: "test/crosswake/proof_lane/ios_verifier_test.exs"
      provides: "Hermetic fake-toolchain exit-status and JSON outcome regressions"
  key_links:
    - from: "runOfflineIslandProof"
      to: "proveLearnLoopRoute history assertion"
      via: "returned opaque mutation ID retained as a local value"
      pattern: "return mutationId|capturedId"
    - from: "npm run typecheck:offline-route-proof"
      to: "offline_route_proof.ts"
      via: "focused tsconfig plus explicit ambient declarations"
      pattern: "tsconfig\.offline_route_proof\.json"
    - from: "browser_online_restore.spec.ts"
      to: "repository helper and rendered proof-lane helper"
      via: "throwing adapter plus ordered BrowserContext.setOffline call log"
      pattern: "setOffline(false).*reject"
    - from: "script/verify_generated_ios_shell.sh --proof-lane"
      to: "automation caller"
      via: "JSON outcome and closed exit-code contract"
      pattern: "outcome.*(passed|blocked|unavailable)"
  prohibitions:
    - "MUST NOT overwrite, merge, reorganize, or replace an adopter's existing specs, fixtures, router, application code, Playwright configuration, or edited generated files."
    - "MUST NOT turn a missing replay/auth, pack/audio, signing, simulator, or device prerequisite into a passing proof claim."
    - "MUST NOT retain or echo raw answers, mutation payloads, identity/account/customer data, credentials, tokens, transcripts, media, endpoints, stable device identifiers, screenshots, traces, logs, raw output, or hashes of unapproved bytes."
    - "MUST NOT add Android work, generic sync/storage/test orchestration, an Xcode project manager, a device service, an evidence dashboard, or later-phase support claims."
---

<objective>
Restore the primary browser proof as a runnable, type-checked corpus and make generated iOS proof verification fail closed.

Purpose: Ensure both browser and native proof seams communicate real executable outcomes rather than failing from an undefined identifier, leaking offline state, or reporting unavailable prerequisites as green.
Output: A focused TypeScript gate, cleanup-safe browser helpers, and a machine-readable non-passing iOS verifier contract.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/FIRST-B2C-ADOPTER-ADOPTION-BRIEF.md
@.planning/phases/159-host-reusable-proof-lane/159-CONTEXT.md
@.planning/phases/159-host-reusable-proof-lane/159-VERIFICATION.md
@.planning/phases/159-host-reusable-proof-lane/159-03-SUMMARY.md
@examples/phoenix_host/e2e/support/offline_route_proof.ts
@priv/templates/crosswake/proof_lane/e2e/support/proof_lane.ts.eex
@script/verify_generated_ios_shell.sh
@test/crosswake/proof_lane/template_contract_test.exs
</context>

<assumption_delta_decision>
Decision: no-change.
Rationale: The detector phrase “instead of inventing a second test system” is a negated false positive; this plan repairs the single preserved browser corpus and does not change a singular identity model into a plural one.
</assumption_delta_decision>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Carry the opaque mutation ID through a cleanup-safe, focused type-checked browser proof</name>
  <reversibility rating="reversible">The helper return value and focused compiler configuration are local test interfaces and can be changed without data or public API migration.</reversibility>
  <read_first>
    - .planning/phases/159-host-reusable-proof-lane/159-CONTEXT.md
    - .planning/phases/159-host-reusable-proof-lane/159-VERIFICATION.md
    - .planning/phases/159-host-reusable-proof-lane/159-03-SUMMARY.md
    - examples/phoenix_host/e2e/support/offline_route_proof.ts
    - examples/phoenix_host/e2e/offline_sync.spec.ts
    - examples/phoenix_host/package.json
    - priv/templates/crosswake/proof_lane/e2e/support/proof_lane.ts.eex
  </read_first>
  <files>examples/phoenix_host/e2e/support/offline_route_proof.ts, examples/phoenix_host/e2e/support/offline_route_proof.typecheck.d.ts, examples/phoenix_host/tsconfig.offline_route_proof.json, examples/phoenix_host/package.json, priv/templates/crosswake/proof_lane/e2e/support/proof_lane.ts.eex</files>
  <behavior>
    - "`runOfflineIslandProof` returns the extracted opaque mutation ID after backend, empty-outbox, and idempotency assertions complete."
    - "`proveLearnLoopRoute` retains that return value and uses it in the history assertion; no undeclared identifier remains."
    - "Both helper implementations enter offline mode inside `try` and restore online mode in `finally`; reconnect and proof assertions run only after explicit online restoration."
    - "A focused `tsc --noEmit` gate exits zero while declaring only the existing `node:fs`, `node:path`, `process`, and `window.liveSocket` ambient seams needed by this file."
  </behavior>
  <action>Change both repository and template `runOfflineIslandProof` implementations to return `Promise&lt;string&gt;`; assign the repository result in `proveLearnLoopRoute` and use that local for the history text assertion, closing the verifier's undefined `capturedId` path without changing `offline_sync.spec.ts`, fixtures, selectors, router, application code, or backend schema per D-01/D-03. Wrap the offline portion so `context.setOffline(false)` always runs in `finally`; reconnect and backend/outbox/idempotency assertions begin only after explicit online restoration. Add a narrow ambient declaration file and focused `tsconfig.offline_route_proof.json` that explicitly model the already-present Node imports, `process.cwd`, and host `Window.liveSocket`; use `skipLibCheck` only for dependency declarations, keep strict checking on the proof source, and add `typecheck:offline-route-proof` to the existing package scripts. Do not whitelist compiler diagnostics or convert a non-zero compiler run into success.</action>
  <verify>
    <automated>npm --prefix examples/phoenix_host run typecheck:offline-route-proof &amp;&amp; npm --prefix examples/phoenix_host test -- offline_sync.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - The focused TypeScript command exits zero; ambient noise is resolved by explicit narrow declarations, not filtered output.
    - `capturedId` is declared from the helper result and the primary Playwright control passes without changes to its spec or fixtures.
  </acceptance_criteria>
  <done>PROOF-03's primary browser corpus is runnable and type-checked, with its opaque mutation reference and browser online state preserved correctly.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Execute online restoration for repository and rendered generated helpers</name>
  <reversibility rating="reversible">The focused proof-owned regression and rendered fixture can be changed with the internal helper contract.</reversibility>
  <read_first>
    - .planning/phases/159-host-reusable-proof-lane/159-CONTEXT.md
    - .planning/phases/159-host-reusable-proof-lane/159-VERIFICATION.md
    - examples/phoenix_host/e2e/support/offline_route_proof.ts
    - priv/templates/crosswake/proof_lane/e2e/support/proof_lane.ts.eex
    - test/crosswake/proof_lane/template_contract_test.exs
  </read_first>
  <files>examples/phoenix_host/e2e/crosswake_proof_lane/browser_online_restore.spec.ts, examples/phoenix_host/e2e/crosswake_proof_lane/support/proof_lane.ts, test/crosswake/proof_lane/template_contract_test.exs</files>
  <behavior>
    - "The template contract renders `proof_lane.ts.eex` with one fixed safe Phase 159 config and byte-compares it with the proof-owned TypeScript fixture used by the runtime test."
    - "A BrowserContext-shaped mock records `[true, false]` when the repository helper receives a mutation adapter that throws."
    - "The same mock and throwing condition records `[true, false]` when the rendered generated helper runs."
    - "In both cases, the exact injected rejection reaches the test only after the false transition is recorded; reconnect and later assertions are not called."
  </behavior>
  <action>Start RED by adding `browser_online_restore.spec.ts` and importing `runOfflineIslandProof` from both the repository helper and `e2e/crosswake_proof_lane/support/proof_lane.ts`. Pass each import a BrowserContext-shaped mock whose `setOffline` appends ordered boolean events and a complete adapter whose mutation callback throws a unique Error object; catch only at the test boundary, assert the exact object propagated, assert the call log is `[true, false]`, and assert reconnect plus downstream callbacks were not reached. Render the generated-helper fixture from `proof_lane.ts.eex` using a fixed safe config. Extend `template_contract_test.exs` to perform that same render and byte-compare it with the committed runtime fixture, so textual `try/finally` assertions alone cannot satisfy the test. Keep this under the isolated Crosswake proof namespace per D-01/D-03; do not modify the primary `offline_sync.spec.ts`, create generic orchestration, or require a browser process.</action>
  <verify>
    <automated>mix test test/crosswake/proof_lane/template_contract_test.exs &amp;&amp; npm --prefix examples/phoenix_host test -- e2e/crosswake_proof_lane/browser_online_restore.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - The executable regression invokes both exported helper implementations with a throwing adapter.
    - Each helper restores online state before propagating the same rejection.
    - The generated helper under test is byte-identical to a fresh EEx render for the fixed config.
  </acceptance_criteria>
  <done>A throwing host callback cannot strand either the repository or generated proof helper offline.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Make every unavailable generated-iOS proof result machine-readable and non-passing</name>
  <reversibility rating="reversible">The proof-lane-only exit/status protocol is an internal script contract and introduces no retained data or published runtime API.</reversibility>
  <read_first>
    - .planning/phases/159-host-reusable-proof-lane/159-CONTEXT.md
    - .planning/phases/159-host-reusable-proof-lane/159-VERIFICATION.md
    - .planning/phases/159-host-reusable-proof-lane/159-03-SUMMARY.md
    - script/verify_generated_ios_shell.sh
    - test/crosswake/proof_lane/template_contract_test.exs
  </read_first>
  <files>script/verify_generated_ios_shell.sh, test/crosswake/proof_lane/ios_verifier_test.exs, test/crosswake/proof_lane/template_contract_test.exs</files>
  <behavior>
    - "Missing `xcodebuild` emits one JSON `unavailable` record with a stable rule ID and exits 3."
    - "An unenumerable project or failed `build-for-testing` emits a closed `blocked` or `unavailable` JSON record and exits non-zero."
    - "A target graph missing XCTest or XCUITest emits `blocked` and exits 2."
    - "Only successful enumeration and build of both generated targets emits `passed` and exits 0; the record explicitly states that no simulator/device support claim was promoted."
    - "The non-proof generated-shell behavior retains its existing exit/output contract."
  </behavior>
  <action>Create `ios_verifier_test.exs` with temporary executable `xcodebuild` shims and `CROSSWAKE_IOS_PROJECT_ROOT` so tests deterministically exercise missing-tool, list failure, missing-target, build failure, and successful-build branches without a simulator or signing setup. Define the proof-lane machine contract as JSON Lines with only `outcome`, stable `rule_id`, and low-cardinality scope; use exit 2 for `blocked`, exit 3 for `unavailable`, and exit 0 only for `passed`. Update every proof-lane early branch in `verify_generated_ios_shell.sh`, including the current missing-xcodebuild, enumeration, and build-for-testing advisory branches; preserve stderr discipline and do not print captured xcodebuild output in proof-lane failure records. Keep normal generated-shell verification unchanged. Add `bash -n` and a structural assertion that every proof-lane unavailable branch is non-zero. This implements D-11/D-14 without claiming Phase 160 replay/auth, Phase 161 pack/audio, or Phase 162 physical-device completion.</action>
  <verify>
    <automated>mix test test/crosswake/proof_lane/ios_verifier_test.exs test/crosswake/proof_lane/template_contract_test.exs &amp;&amp; bash -n script/verify_generated_ios_shell.sh</automated>
  </verify>
  <acceptance_criteria>
    - Each unavailable/blocked fixture produces parseable JSON, the expected stable rule ID, and a non-zero status.
    - The successful fake target graph/build is the sole proof-lane fixture returning zero.
    - No failure output contains xcodebuild stdout/stderr, configuration values, paths beyond safe fixed labels, or later-phase support claims.
  </acceptance_criteria>
  <done>The generated iOS verifier can no longer make an unavailable prerequisite look green to Phase 160/161 automation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| Browser adapter → shared context | A failed host callback can strand subsequent primary-corpus tests offline. |
| TypeScript source → compiler gate | Missing declarations or names determine whether the primary proof is executable. |
| Xcode/toolchain → shell exit protocol | External availability and build failures become machine-consumed proof outcomes. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- | --- |
| T-159-G06-01 | Tampering / Repudiation | browser mutation-ID flow | high | mitigate | Return and retain the app-generated opaque ID; zero-error focused typecheck and unchanged Playwright control. |
| T-159-G06-02 | Denial of Service | shared browser context | medium | mitigate | `try/finally` restores online state in repository and generated adapters, including injected failure tests. |
| T-159-G06-03 | Repudiation | iOS verifier exit semantics | high | mitigate | Closed JSON outcomes, distinct non-zero blocked/unavailable statuses, fake-toolchain regression matrix, and zero only after both targets build. |
| T-159-G06-04 | Information Disclosure | tool failure output | medium | mitigate | Stable rule/scope records only; raw xcodebuild output is not included in proof-lane machine failures. |
</threat_model>

<verification>
- Run the focused zero-error TypeScript gate and unchanged offline Playwright spec.
- Run template contracts, hermetic verifier tests, and `bash -n`.
- Do not require a simulator, signing identity, physical device, or human checkpoint.
</verification>

<success_criteria>
- PROOF-03's primary browser helper compiles and the existing browser corpus remains runnable.
- A browser assertion failure cannot contaminate the next test's online state.
- Every unmet native prerequisite is machine-readable and non-passing; no physical-device claim changes.
</success_criteria>

## Artifacts this phase produces

- New file: `examples/phoenix_host/tsconfig.offline_route_proof.json`.
- New file: `examples/phoenix_host/e2e/support/offline_route_proof.typecheck.d.ts`.
- New file: `examples/phoenix_host/e2e/crosswake_proof_lane/browser_online_restore.spec.ts`.
- New file: `examples/phoenix_host/e2e/crosswake_proof_lane/support/proof_lane.ts`, byte-checked against the rendered template.
- New file: `test/crosswake/proof_lane/ios_verifier_test.exs`.
- New npm script: `typecheck:offline-route-proof`.
- Modified symbol: `runOfflineIslandProof(...): Promise&lt;string&gt;` in repository and generated host helper surfaces.
- New script contract: proof-lane JSON outcomes `passed | blocked | unavailable` with exit statuses `0 | 2 | 3` respectively.

<output>
Create `.planning/phases/159-host-reusable-proof-lane/159-06-SUMMARY.md` when done.
</output>
