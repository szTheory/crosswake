---
phase: 159-host-reusable-proof-lane
plan: "04"
type: execute
wave: 2
depends_on: [159-01]
files_modified:
  - lib/crosswake/proof_lane/evidence.ex
  - priv/templates/crosswake/proof_lane/test/crosswake_proof_lane_test.exs.eex
  - test/crosswake/proof_lane/evidence_test.exs
autonomous: true
requirements: [PROOF-04]
must_haves:
  truths:
    - "Generation and evidence promotion use collision-safe staged writes; interruption or a concurrent writer cannot overwrite host files or expose a partial retained artifact."
    - "Retained evidence is constructed from the exact D-16 typed allowlist and has no free-form metadata, attachment, log, screenshot, trace, media, or raw-output field (D-15 through D-18, PROOF-04)."
    - "Every staged path is re-enumerated and scanned after serialization; unsafe content or an unexpected path leaves no final artifact and returns only a stable rule plus relative path/key (D-19, D-22)."
    - "Hashes are accepted only for specifically approved sanitized contract/artifact bytes; payload, token, account/device identifier, endpoint, or unreviewed binary input is rejected before hashing (D-18)."
    - "Negative controls prove sensitive keys and values fail, while anti-vacuity controls prove newly added staged paths cannot bypass final scanning (D-19)."
  artifacts:
    - path: "lib/crosswake/proof_lane/evidence.ex"
      provides: "Versioned typed evidence schema, approved-byte hashing, final scan, and atomic promotion"
    - path: "test/crosswake/proof_lane/evidence_test.exs"
      provides: "Allowlist, sensitive-key/value, non-echo, anti-vacuity, concurrency, interruption, and atomic-promotion controls"
    - path: "priv/templates/crosswake/proof_lane/test/crosswake_proof_lane_test.exs.eex"
      provides: "Host-owned evidence contract test using opaque refs and low-cardinality outcomes only"
  key_links:
    - from: "Evidence.build/1"
      to: "Evidence.to_map/1"
      via: "The constructor validates exact keys/types/closed values before explicit serialization"
      pattern: "@enforce_keys|to_map"
    - from: "Evidence.promote/2"
      to: "Evidence.scan_stage/1"
      via: "The final sibling staging directory is recursively enumerated and scanned before atomic rename"
      pattern: "scan_stage|File\.rename"
    - from: "Evidence.approved_hash/2"
      to: "approved sanitized bytes"
      via: "Callers must supply a closed artifact kind and sanitized byte source before SHA-256 calculation"
      pattern: "approved_hash|sha256"
  prohibitions:
    - statement: "MUST NOT overwrite, merge, reorganize, or replace an adopter's existing specs, fixtures, router, application code, Playwright configuration, or edited generated files."
      category: product-ownership
      verification: flagged-unverified
    - statement: "MUST NOT turn a missing replay/auth, pack/audio, signing, simulator, or device prerequisite into a passing, skipped, or no-op proof claim."
      category: transparency
      verification: flagged-unverified
    - statement: "MUST NOT retain or echo raw answers, selected-answer content, mutation payloads, account/customer identifiers, credentials, tokens, transcripts, media, endpoints, archive details, stable device identifiers, screenshots, traces, console logs, raw test output, or unsafe hashes."
      category: privacy
      verification: flagged-unverified
    - statement: "MUST NOT add Android scope, generic sync/storage/test orchestration, an Xcode project manager, a device service, an evidence dashboard, or later-phase support claims."
      category: scope
      verification: flagged-unverified
flagged_assumptions:
  - "[FLAGGED/UNVERIFIED] PROOF-03 was unclassified by the specless probe and is not expanded here; generated host evidence tests consume opaque outcomes from the preserved browser/native seams without introducing another proof corpus."
  - "[FLAGGED/UNVERIFIED] PROOF-04 was unclassified by the specless probe; this plan treats D-15 through D-19 as the authoritative complete allowlist/scan boundary and rejects all fields outside it rather than silently backstopping unknown evidence shapes."
---

<objective>
Finish privacy-safe retained evidence with a closed schema, final staged scan, atomic promotion, and
non-vacuous negative controls.

Purpose: Ensure proof artifacts cannot become a payload, identity, media, credential, or device
data exfiltration surface.
Output: A versioned evidence compiler/scanner and exhaustive automated safety proof.
</objective>

<execution_context>
@/Users/jon/.codex/gsd-core/workflows/execute-plan.md
@/Users/jon/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/ADR-FIRST-B2C-ADOPTER.md
@.planning/FIRST-B2C-ADOPTER-ROUTE-POLICY-MAP.md
@.planning/phases/159-host-reusable-proof-lane/159-CONTEXT.md
@.planning/phases/159-host-reusable-proof-lane/159-RESEARCH.md
@.planning/phases/159-host-reusable-proof-lane/159-PATTERNS.md
@.planning/phases/159-host-reusable-proof-lane/159-01-SUMMARY.md
@lib/crosswake/proof_lane/evidence.ex
@priv/templates/crosswake/proof_lane/test/crosswake_proof_lane_test.exs.eex
@lib/crosswake/shell/diagnostic_export.ex
@lib/crosswake/planning/first_adopter_context.ex
@test/crosswake/shell/diagnostic_export_test.exs
@test/crosswake/planning/first_adopter_context_test.exs

<interfaces>
Complete `%Crosswake.ProofLane.Evidence{schema_version, crosswake_version, template_version,
commit_ref, route_id, assertion_ids, status, outcome, captured_at, retention_label, device_class,
approved_hashes}`. `build/1` returns `{:ok, %Evidence{}} | {:error,
%Evidence.Error{rule_id, path, remediation}}`; `to_map/1` emits only declared string keys;
`approved_hash/2` accepts a closed sanitized artifact kind; `promote/2` stages, scans, and atomically
renames; `check/1` and `scan_stage/1` are read-only.
</interfaces>
</context>

<assumption_delta_decision>
Primary noun: host-owned proof lane.
Decision: no-change.
Rationale: Evidence compiles low-cardinality results from the existing parameterized proof posture;
it does not create another test or authority system.
</assumption_delta_decision>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Enforce the versioned evidence allowlist and approved-byte hashing boundary</name>
  <reversibility rating="costly">D-16 is a versioned retained-artifact contract; incompatible field or meaning changes require a schema version and migration guidance.</reversibility>
  <read_first>
    - AGENTS.md
    - .planning/phases/159-host-reusable-proof-lane/159-CONTEXT.md
    - .planning/phases/159-host-reusable-proof-lane/159-RESEARCH.md
    - .planning/phases/159-host-reusable-proof-lane/159-01-SUMMARY.md
    - lib/crosswake/proof_lane/evidence.ex
    - lib/crosswake/shell/diagnostic_export.ex
    - test/crosswake/shell/diagnostic_export_test.exs
  </read_first>
  <files>lib/crosswake/proof_lane/evidence.ex, priv/templates/crosswake/proof_lane/test/crosswake_proof_lane_test.exs.eex, test/crosswake/proof_lane/evidence_test.exs</files>
  <behavior>
    - "Only the twelve D-16 fields are accepted and explicitly serialized; unknown, nested metadata, attachment, or output fields fail closed."
    - "Route/assertion refs, status/outcome, retention label, and device class use closed low-cardinality grammars; capture time is UTC and versions/commit are bounded sanitized strings."
    - "Sensitive keys and representative sensitive values from every D-17 class are rejected before serialization without appearing in the error."
    - "`approved_hash/2` accepts only reviewed sanitized contract/artifact kinds and bytes that pass the same scanner before SHA-256; all other kinds or bytes fail."
  </behavior>
  <action>Write the full allowlist and sensitive negative-control table before completing `Evidence.build/1`. Define the exact D-16 struct fields with `@enforce_keys`; accept maps only after rejecting non-atom keys and unknown keys, normalize closed values, and serialize through explicit `to_map/1` string keys rather than a generic encoder per D-15/D-16. Validate opaque route IDs and assertion IDs, `passed | blocked | unavailable` status/outcome vocabulary, UTC timestamps, bounded version/commit values, retention label, device class, and a list of approved hash records. Reject all D-17 key classes and value canaries, including nested/list values, raw answer/selection/payload, identity/account/customer, credentials/tokens, transcript/media/archive, endpoint, stable device ID, screenshot/trace/log/raw-output, and `.xcresult` references. Errors must be `%Evidence.Error{rule_id, path, remediation}` and must omit key values, matches, serialized candidates, stdout/stderr, and context per D-19. Implement `approved_hash/2` with a closed artifact-kind vocabulary and scan the supplied bytes before `:crypto.hash(:sha256, bytes)`; do not offer a hash-anything helper per D-18. Update the generated host ExUnit template to build only synthetic opaque/low-cardinality evidence and to assert future prerequisite outcomes cannot be recorded as passed.</action>
  <verify>
    <automated>mix test test/crosswake/proof_lane/evidence_test.exs &amp;&amp; mix format --check-formatted lib/crosswake/proof_lane/evidence.ex test/crosswake/proof_lane/evidence_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - A table covers every D-17 forbidden class as both a key and representative nested/value injection where applicable.
    - `to_map/1` emits exactly the versioned D-16 keys and no metadata/attachment/output escape hatch.
    - Errors contain only stable rule, safe relative key/path, and remediation; injected canaries are absent from all rendered forms.
    - Hash tests prove reviewed sanitized bytes pass and payload/token/account/device/unreviewed-binary inputs fail before digest creation.
  </acceptance_criteria>
  <done>The evidence boundary can represent only the intentionally small, versioned proof record and cannot disguise sensitive bytes as metadata or hashes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Scan every staged path and atomically promote only safe complete artifacts</name>
  <read_first>
    - .planning/phases/159-host-reusable-proof-lane/159-CONTEXT.md
    - .planning/phases/159-host-reusable-proof-lane/159-PATTERNS.md
    - .planning/phases/159-host-reusable-proof-lane/159-01-SUMMARY.md
    - lib/crosswake/proof_lane/evidence.ex
    - test/crosswake/proof_lane/evidence_test.exs
    - lib/crosswake/planning/first_adopter_context.ex
    - test/crosswake/planning/first_adopter_context_test.exs
  </read_first>
  <files>lib/crosswake/proof_lane/evidence.ex, test/crosswake/proof_lane/evidence_test.exs</files>
  <behavior>
    - "Promotion writes a sibling staging directory, recursively enumerates every staged entry, scans final serialized bytes and paths, then performs one atomic final rename."
    - "An unexpected file, symlink, unreadable path, unsafe key/value, malformed JSON, forbidden extension, or injected failure leaves no final artifact."
    - "A newly added staged file is detected by anti-vacuity controls even when it is absent from the original writer allowlist."
    - "Two concurrent promoters cannot replace one another's final artifact; the losing operation returns a stable collision rule and preserves the winner."
    - "`check/1` uses the same final scanner in read-only mode and emits rule/path only."
  </behavior>
  <action>Start RED with temporary-directory controls for unexpected nested files, sensitive JSON keys/values, raw text, `.xcresult`, screenshot/trace/log/media extensions, symlinks, unreadable/unclassified entries, malformed evidence, concurrent promotion, and injected interruption before rename. Add a positive clean artifact and a negative anti-vacuity case that creates a new staged path after serialization so the test proves recursive discovery rather than a fixed file list. Implement D-15's exact sequence: validate/build, create a unique sibling stage, write the canonical evidence JSON with an atomic file operation, recursively re-enumerate every final staged entry, reject anything outside the closed artifact path/type set, scan final bytes, fsync/close as supported, and atomically rename only when the destination is absent. On any failure, remove only the operation's own stage and leave no partial final path; on a concurrent final collision preserve the winner and fail closed per D-22 and the PROOF-01 concurrency truth. Reuse the same scanner from `check/1`; do not trust the pre-serialization object or writer list as final evidence. Return stable `PL-EVIDENCE-*` rule IDs plus relative path/key and calm next action only, never matched content or runtime output per D-19/D-22.</action>
  <verify>
    <automated>mix test test/crosswake/proof_lane/evidence_test.exs &amp;&amp; mix format --check-formatted lib/crosswake/proof_lane/evidence.ex test/crosswake/proof_lane/evidence_test.exs</automated>
  </verify>
  <acceptance_criteria>
    - Every failure class leaves the final destination absent and does not delete or replace a pre-existing winner.
    - Anti-vacuity proves a newly added nested staged path is discovered and rejected.
    - Concurrent promotion has exactly one intact winner and a stable non-echoing loser result.
    - Check and promotion share the same final scanner, and check writes nothing.
    - No `.xcresult`, screenshot, trace, log, raw output, media, endpoint, stable device detail, sensitive value, or unsafe digest appears in retained output or diagnostics.
  </acceptance_criteria>
  <done>Only a fully enumerated, final-byte-scanned allowlisted artifact can cross the atomic promotion boundary; interruption, concurrency, and unsafe additions fail closed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| test/runtime result → `Evidence.build/1` | Candidate maps may contain sensitive or open-ended data. |
| approved bytes → hash helper | Sanitized contract bytes become stable retained digests. |
| stage directory → final artifact | Filesystem contents become durable proof only after final scanning. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- | --- |
| T-159-13 | Information Disclosure | evidence schema/serialization | high | mitigate | Exact typed allowlist, explicit serializer, closed values, nested negative controls, and non-echoing errors. |
| T-159-14 | Linkability / Information Disclosure | `approved_hash/2` | high | mitigate | Closed artifact kinds and pre-hash scanning reject payload, identity, endpoint, device, token, and unreviewed bytes. |
| T-159-15 | Tampering | staged artifact tree | high | mitigate | Recursive post-write enumeration, symlink/type rejection, final-byte scan, anti-vacuity controls, and atomic rename. |
| T-159-16 | Denial of Service | concurrent/interrupted promotion | medium | mitigate | Unique sibling stages, destination-absent atomic promotion, winner preservation, and own-stage cleanup only. |
| T-159-SC | Tampering | serialization/crypto dependency | low | accept | Existing locked Jason plus OTP `File`, `Path`, and `:crypto` only; no package installation. |
</threat_model>

<verification>
- Run the complete evidence test file after each task and after final edits.
- Confirm sensitive-key/value, non-echo, approved-hash, anti-vacuity, interruption, and concurrency controls all execute.
- Run formatter and the generated host evidence-contract test through Plan 01's generator test before phase close.
</verification>

<success_criteria>
- PROOF-04 rejects every named sensitive class before retention and after final serialization.
- Final staged scanning has discriminating negative controls and cannot pass vacuously when paths are added.
- Evidence promotion is atomic, collision-safe, brief, and non-echoing.
</success_criteria>

## Specless prohibition-probe audit

Stage 1 recalled seven candidates: host-corpus replacement, false passing device claims, retained
sensitive proof data, framework/platform scope expansion, path traversal, command/config injection,
and generic dependency tampering. Stage 2 kept the first four bespoke product/privacy/transparency
constraints exactly as the four descriptor-less `must_haves.prohibitions` entries above. Three
canon candidates were dropped with breadcrumbs: path traversal and command/config injection are
canon input-validation threats covered by each plan's ASVS-L1 threat model and `$gsd-secure-phase`;
generic dependency tampering is canon supply-chain review and the package audit confirms this phase
installs nothing. Equality holds: 7 recalled = 4 kept + 3 canon-dropped; no candidate disappeared.

## Multi-Source Coverage Audit

| Source | ID | Feature / requirement | Covering plans | Status | Notes |
| --- | --- | --- | --- | --- | --- |
| GOAL | — | Generate configurable host-owned browser, shell, offline-island, and device proof scaffolding | 159-01 through 159-04 | COVERED | Tracer proves the full path; expansions harden lifecycle, browser/native, and evidence. |
| REQ | PROOF-01 | Non-overwriting ExUnit, Playwright, shell, and device scaffold | 159-01, 159-02, 159-03 | COVERED | Missing-only, collision-safe lifecycle plus executable generated targets. |
| REQ | PROOF-02 | Exact route/storage/mutation/endpoint/router/shell config | 159-01, 159-02 | COVERED | One closed normalized Phoenix config boundary. |
| REQ | PROOF-03 | Preserve existing browser/unit/fixture corpus and add only unavailable boundaries | 159-01, 159-03 | COVERED | Existing browser spec remains primary; XCTest/XCUITest cover native-only seams. |
| REQ | PROOF-04 | Reject sensitive evidence | 159-01, 159-04 | COVERED | Typed allowlist, approved-byte hashing, final scan, atomic promotion. |
| RESEARCH | — | No-new-dependency generator-plus-library stack | 159-01 through 159-04 | COVERED | Existing Elixir/Jason/Playwright and Apple frameworks only. |
| RESEARCH | — | Normalize once, render many | 159-01, 159-02 | COVERED | One `%Config{}` feeds manifest and all templates. |
| RESEARCH | — | Host ownership plus desired-state observability | 159-01, 159-02 | COVERED | Stamps, missing-only writes, manifest, check/diff. |
| RESEARCH | — | Boundary-specific browser/XCTest/XCUITest targets | 159-01, 159-03 | COVERED | Existing browser semantics remain primary; process behavior stays in XCUITest. |
| RESEARCH | — | Validate, stage, scan, promote | 159-01, 159-04 | COVERED | Final-byte recursive scan precedes atomic rename. |
| RESEARCH | A1 | Static proof-owned Xcode wiring may be insufficient | 159-01, 159-03 | COVERED | D-23 escape hatch is executable and forbids a project manager. |
| CONTEXT | D-01-D-04 | Additive host-owned namespace, preserved corpus, executable iOS wiring | 159-01, 159-03 | COVERED | No host project/suite reorganization. |
| CONTEXT | D-05-D-09 | Phoenix config, closed normalization, declarative ID path, local endpoints, non-echo errors | 159-01, 159-02 | COVERED | D-05 remains recorded as costly. |
| CONTEXT | D-10-D-14 | Real XCTest/XCUITest, closed outcomes, lifecycle boundary, advisory native execution | 159-01, 159-03 | COVERED | Future prerequisites block explicitly; no physical-device CI. |
| CONTEXT | D-15-D-19 | Typed evidence allowlist, narrow schema, exclusions, approved hashes, non-echo scan | 159-01, 159-04 | COVERED | D-16 remains recorded as costly. |
| CONTEXT | D-20-D-22 | Read-only check/diff and atomic fail-closed evidence lifecycle | 159-02, 159-04 | COVERED | Snapshot, interruption, concurrency, negative-control coverage. |
| CONTEXT | D-23 | Three-day stop and adopter-specific escape hatch | 159-01, 159-03 | COVERED | No generic Xcode/test/device framework fallback. |

No source item is missing. Deferred Phase 160 replay/auth behavior, Phase 161 pack/audio behavior,
Phase 162 physical-device evidence, Android, generic sync/storage/test orchestration, raw native
artifacts, dashboard, and support promotion remain explicitly excluded.

## Artifacts this phase produces

- New Elixir symbols: `%Crosswake.ProofLane.Evidence{schema_version, crosswake_version, template_version, commit_ref, route_id, assertion_ids, status, outcome, captured_at, retention_label, device_class, approved_hashes}`, `%Evidence.Error{rule_id, path, remediation}`, `Evidence.build/1`, `Evidence.to_map/1`, `Evidence.approved_hash/2`, `Evidence.check/1`, `Evidence.scan_stage/1`, and `Evidence.promote/2`.
- New repository test path: `test/crosswake/proof_lane/evidence_test.exs`; completed generated template: `priv/templates/crosswake/proof_lane/test/crosswake_proof_lane_test.exs.eex`.
- New generated retained artifact path: host-selected safe evidence directory containing only `proof-lane-evidence.json`; sibling staging paths are transient and never retained.
- Phase-wide CLI flags and non-evidence symbols remain those enumerated in Plans 01-03; this plan adds no flag, schema, endpoint, external service, Android file, screenshot/log/trace attachment, or support claim.

<output>
Create `.planning/phases/159-host-reusable-proof-lane/159-04-SUMMARY.md` when done.
</output>
