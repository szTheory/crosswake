# Phase 48.1: close-gap-adpt-01-adpt-02-provider-facade-paywall-swap-target-contract - Pattern Map

**Mapped:** 2026-06-01
**Files analyzed:** 6
**Analogs found:** 6 / 6

## File Classification

| New/Modified File | Role | Data Flow | Closest Analog | Match Quality |
|---|---|---|---|---|
| `examples/phoenix_host/lib/crosswake_example/commerce/storefront_adapter.ex` | service | request-response | `lib/crosswake/commerce.ex` | exact |
| `examples/phoenix_host/lib/crosswake_example/commerce/provider_adapter_storefront.ex` | service | transform | `examples/phoenix_host/lib/crosswake_example/commerce/provider_adapter_storefront.ex` | exact |
| `examples/phoenix_host/lib/crosswake_example/commerce/mock_storefront.ex` | service | transform | `examples/phoenix_host/lib/crosswake_example/commerce/mock_storefront.ex` | exact |
| `examples/phoenix_host/lib/crosswake_example/paywall_entry_live.ex` | component | event-driven | `examples/phoenix_host/lib/crosswake_example/paywall_entry_live.ex` | exact |
| `test/crosswake/proof/phase48_provider_adapter_proof_test.exs` | test | request-response | `test/crosswake/proof/phase48_provider_adapter_proof_test.exs` | exact |
| `test/crosswake/guides/commerce_test.exs` | test | transform | `test/crosswake/guides/commerce_test.exs` | exact |

## Pattern Assignments

### `examples/phoenix_host/lib/crosswake_example/commerce/storefront_adapter.ex` (service, request-response)

**Analog:** `lib/crosswake/commerce.ex`

**Behaviour callback pattern** (lines 6-27):
```elixir
alias Crosswake.Commerce.Contracts

@callback submit_purchase_intent(Contracts.PurchaseIntent.t()) :: :ok | {:error, term()}
@callback submit_restore_intent(Contracts.RestoreIntent.t()) :: :ok | {:error, term()}
@callback ingest_reconciliation_evidence(Contracts.ReconciliationEvidence.t()) ::
            {:ok, Crosswake.Commerce.Reconciliation.EvidenceResult.t()} | {:error, term()}
@callback fetch_entitlement_snapshot(String.t()) :: {:ok, Contracts.EntitlementSnapshot.t()} | {:error, term()}
```

**Companion behaviour style precedent** (lines 54-67, 83-96, 110-123 in `lib/crosswake/companion.ex`):
```elixir
@callback companion_id() :: atom()
@callback enabled?(config :: map()) :: boolean()
@callback route_gated?(route :: RouteEntry.t(), context :: Target.t()) :: {:deny, Finding.t()} | :pass
@callback kill_switch_active?(context :: Target.t()) :: boolean()
@callback validate_dependency() :: :ok | {:error, [module()]}
@callback report_state() :: State.t()
```

---

### `examples/phoenix_host/lib/crosswake_example/commerce/provider_adapter_storefront.ex` (service, transform)

**Analog:** `examples/phoenix_host/lib/crosswake_example/commerce/provider_adapter_storefront.ex`

**Imports + evidence normalizer pattern** (lines 9-12, 30-32, 70-72):
```elixir
alias Crosswake.Commerce.Contracts
alias Crosswake.Companions.PlayBilling.Evidence, as: PlayBillingEvidence
alias Crosswake.Companions.StoreKit.Evidence, as: StoreKitEvidence

with {:ok, normalized} <- StoreKitEvidence.new(storekit_attrs) do
  StoreKitEvidence.to_reconciliation_evidence(normalized)
end

with {:ok, normalized} <- PlayBillingEvidence.new(play_attrs) do
  PlayBillingEvidence.to_reconciliation_evidence(normalized)
end
```

**Typed return contract precedent** (lines 15-17, 35-37, 55-57, 75-77):
```elixir
@spec simulate_storekit_purchase(Contracts.PurchaseIntent.t(), keyword()) ::
        {:ok, Contracts.ReconciliationEvidence.t()} | {:error, term()}
```

**Closed config + fail-closed pattern to copy** (lines 37-39 in `lib/crosswake/companions/store_kit.ex`, lines 37-39 in `lib/crosswake/companions/play_billing.ex`):
```elixir
config = Application.get_env(:crosswake, :storekit, %{})
enabled = Map.get(config, :enabled, false)
```

Use the same explicit `Application.get_env/3` boundary for `:paywall_storefront_provider`, and return `{:error, {:invalid_provider, provider}}` for unknown/missing values.

---

### `examples/phoenix_host/lib/crosswake_example/commerce/mock_storefront.ex` (service, transform)

**Analog:** `examples/phoenix_host/lib/crosswake_example/commerce/mock_storefront.ex`

**Current callback surface to adapt** (lines 52-67):
```elixir
@spec simulate_purchase(Contracts.PurchaseIntent.t(), keyword()) ::
        Contracts.ReconciliationEvidence.t()
def simulate_purchase(%Contracts.PurchaseIntent{} = intent, opts \\ []) do
  ...
end

@spec simulate_restore(Contracts.RestoreIntent.t(), keyword()) ::
        Contracts.ReconciliationEvidence.t()
def simulate_restore(%Contracts.RestoreIntent{} = _intent, opts \\ []) do
  ...
end
```

**Evidence construction pattern** (lines 55-62, 68-75):
```elixir
%Contracts.ReconciliationEvidence{
  source: :storefront,
  provider: "mock",
  event_kind: "purchase",
  provider_reference: provider_reference(intent.entry_id),
  evidence_ref: evidence_ref(intent.entry_id, "purchase"),
  captured_at: Keyword.get(opts, :captured_at, DateTime.utc_now() |> DateTime.to_iso8601())
}
```

---

### `examples/phoenix_host/lib/crosswake_example/paywall_entry_live.ex` (component, event-driven)

**Analog:** `examples/phoenix_host/lib/crosswake_example/paywall_entry_live.ex`

**Adapter boundary pattern** (lines 32-35, 65-68, 351-353):
```elixir
evidence = storefront_adapter().simulate_purchase(intent)
case ReconciliationInbox.ingest_evidence(evidence) do
  {:ok, _attempt} -> ...
end

evidence = storefront_adapter().simulate_restore(intent)
case ReconciliationInbox.ingest_evidence(evidence) do
  {:ok, _attempt} -> ...
end

defp storefront_adapter do
  Application.get_env(:crosswake_example, :paywall_storefront_adapter, @default_storefront_adapter)
end
```

**Fail-closed UX error branch** (lines 49-55, 82-88):
```elixir
{:error, _reason} ->
  {:noreply,
   put_flash(
     socket,
     :error,
     "Something went wrong submitting your purchase. Please try again."
   )}
```

---

### `test/crosswake/proof/phase48_provider_adapter_proof_test.exs` (test, request-response)

**Analog:** `test/crosswake/proof/phase48_provider_adapter_proof_test.exs`

**Provider evidence + authority fence proof pattern** (lines 26-38, 40-48, 75-78):
```elixir
assert {:ok, evidence} = ProviderAdapterStorefront.simulate_storekit_purchase(intent, ...)
assert {:ok, attempt} = ReconciliationInbox.ingest_evidence(evidence)
assert attempt.status == :awaiting_verification

verified = MockBackend.build_verified_snapshot(evidence, @group_id)
assert {:ok, projected} = EntitlementProjection.project_snapshot(nil, verified)
assert EntitlementProjection.derived_state(projected) == :granted

assert EntitlementProjection.derived_state(pending_snapshot) == :pending
assert {:error, :unverified_reconciliation_outcome} =
         EntitlementProjection.project_snapshot(nil, pending_snapshot)
```

**Closed vocabulary assertions** (lines 96-97, 112-114, 123-124):
```elixir
assert storekit_evidence.event_kind in ProviderEvidence.event_kind_vocabulary()
assert play_restore_evidence.event_kind in ProviderEvidence.event_kind_vocabulary()
assert {:error, {:invalid_event_kind, _details}} = StoreKitEvidence.new(...)
```

---

### `test/crosswake/guides/commerce_test.exs` (test, transform)

**Analog:** `test/crosswake/guides/commerce_test.exs`

**Docs-contract export lock pattern** (lines 519-523, 525-530):
```elixir
assert function_exported?(CrosswakeExample.Commerce.MockStorefront, :simulate_purchase, 2)
assert function_exported?(CrosswakeExample.Commerce.MockStorefront, :simulate_restore, 2)
assert function_exported?(CrosswakeExample.Commerce.ReconciliationInbox, :ingest_evidence, 2)
```

Use this same `function_exported?/3` shape for the provider facade swap-target callbacks and the new behaviour-backed adapter contract.

## Shared Patterns

### Explicit Runtime Config Boundary
**Source:** `examples/phoenix_host/lib/crosswake_example/paywall_entry_live.ex:351-353`, `lib/crosswake/companions/store_kit.ex:37-39`
**Apply to:** provider facade module + paywall adapter selection
```elixir
Application.get_env(:crosswake_example, :paywall_storefront_adapter, @default_storefront_adapter)

config = Application.get_env(:crosswake, :storekit, %{})
enabled = Map.get(config, :enabled, false)
```

### Behaviour-Backed Contract
**Source:** `lib/crosswake/commerce.ex:11-27`, `lib/crosswake/companion.ex:54-123`
**Apply to:** new `StorefrontAdapter` module; `@behaviour` in mock/provider modules
```elixir
@callback ... :: ... | {:error, term()}
```

### Evidence-Only Authority Fence
**Source:** `examples/phoenix_host/lib/crosswake_example/commerce/reconciliation_inbox.ex:14-45`, `test/crosswake/proof/phase48_provider_adapter_proof_test.exs:32-38`
**Apply to:** LiveView and proof assertions
```elixir
assert {:ok, attempt} = ReconciliationInbox.ingest_evidence(evidence)
assert attempt.status == :awaiting_verification
```

### LiveView Fail-Closed Error Handling
**Source:** `examples/phoenix_host/lib/crosswake_example/paywall_entry_live.ex:49-55,82-88`
**Apply to:** `subscribe` and `restore` branches after tagged adapter returns
```elixir
{:error, _reason} -> {:noreply, put_flash(socket, :error, "...")}
```

## No Analog Found

None.

## Metadata

**Analog search scope:** `examples/phoenix_host/lib/crosswake_example/**`, `lib/crosswake/**`, `test/crosswake/proof/**`, `test/crosswake/guides/**`
**Files scanned:** 12
**Pattern extraction date:** 2026-06-01
